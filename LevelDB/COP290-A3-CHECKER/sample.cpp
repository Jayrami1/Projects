#include <fstream>
#include <iostream>
#include <string>
#include <utility>
#include <vector>

#include "leveldb/db.h"
#include "leveldb/iterator.h"

int main(int argc, char** argv) {
  std::string output_path = (argc > 1) ? argv[1] : "ans.txt";
  std::ofstream out(output_path, std::ios::out | std::ios::trunc);
  if (!out.is_open()) {
    std::cerr << "Failed to open output file: " << output_path << std::endl;
    return 1;
  }

  leveldb::DB* db;
  leveldb::Options options;
  options.create_if_missing = true;
  leveldb::Status status = leveldb::DB::Open(options, "/tmp/testdb", &db);
  if (!status.ok()) {
    std::cerr << "Failed to open database: " << status.ToString() << std::endl;
    return 1;
  }
  leveldb::WriteOptions write_options;
  leveldb::ReadOptions read_options;
  std::string value;
  std::vector<std::pair<std::string, std::string>> scan_result;
  leveldb::Iterator* it = nullptr;

  out << "seed=67, ops=54269" << std::endl;

  // op 1: put
  db->Put(write_options, "key1902", "value100401");

  // op 2: get
  status = db->Get(read_options, "key7661", &value);
  if (status.ok()) {
    out << "GET key7661 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3: get
  status = db->Get(read_options, "key4372", &value);
  if (status.ok()) {
    out << "GET key4372 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4: delete
  db->Delete(write_options, "key7036");

  // op 5: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4555", "key10184", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4555, key10184) size=" << scan_result.size() << std::endl;
  }

  // op 6: put
  db->Put(write_options, "key10548", "value5208");

  // op 7: delete_range
  status = db->DeleteRange(write_options, "key2896", "key4718");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8: put
  db->Put(write_options, "key2588", "value57601");

  // op 9: put
  db->Put(write_options, "key2015", "value89487");

  // op 10: put
  db->Put(write_options, "key8047", "value40398");

  // op 11: put
  db->Put(write_options, "key6565", "value98043");

  // op 12: delete
  db->Delete(write_options, "key0208");

  // op 13: get
  status = db->Get(read_options, "key10648", &value);
  if (status.ok()) {
    out << "GET key10648 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2631", "key11547", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2631, key11547) size=" << scan_result.size() << std::endl;
  }

  // op 15: put
  db->Put(write_options, "key6076", "value68058");

  // op 16: put
  db->Put(write_options, "key11495", "value58239");

  // op 17: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 18: delete_range
  status = db->DeleteRange(write_options, "key3817", "key10662");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 19: delete_range
  status = db->DeleteRange(write_options, "key6370", "key11038");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 20: put
  db->Put(write_options, "key5865", "value20988");

  // op 21: get
  status = db->Get(read_options, "key9188", &value);
  if (status.ok()) {
    out << "GET key9188 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22: put
  db->Put(write_options, "key9758", "value112197");

  // op 23: get
  status = db->Get(read_options, "key5727", &value);
  if (status.ok()) {
    out << "GET key5727 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24: put
  db->Put(write_options, "key0426", "value17914");

  // op 25: delete
  db->Delete(write_options, "key0021");

  // op 26: put
  db->Put(write_options, "key9960", "value31431");

  // op 27: delete_range
  status = db->DeleteRange(write_options, "key7197", "key11017");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 28: put
  db->Put(write_options, "key6488", "value3233");

  // op 29: put
  db->Put(write_options, "key9418", "value65363");

  // op 30: put
  db->Put(write_options, "key3118", "value69570");

  // op 31: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0354", "key11344", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0354, key11344) size=" << scan_result.size() << std::endl;
  }

  // op 32: delete
  db->Delete(write_options, "key10598");

  // op 33: get
  status = db->Get(read_options, "key4905", &value);
  if (status.ok()) {
    out << "GET key4905 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1969", "key11440", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1969, key11440) size=" << scan_result.size() << std::endl;
  }

  // op 35: get
  status = db->Get(read_options, "key1031", &value);
  if (status.ok()) {
    out << "GET key1031 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36: put
  db->Put(write_options, "key7183", "value50019");

  // op 37: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 38: put
  db->Put(write_options, "key0126", "value109894");

  // op 39: put
  db->Put(write_options, "key8231", "value59641");

  // op 40: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4021", "key5145", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4021, key5145) size=" << scan_result.size() << std::endl;
  }

  // op 41: put
  db->Put(write_options, "key7892", "value105804");

  // op 42: put
  db->Put(write_options, "key5030", "value76847");

  // op 43: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6348", "key11722", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6348, key11722) size=" << scan_result.size() << std::endl;
  }

  // op 44: delete_range
  status = db->DeleteRange(write_options, "key5897", "key8518");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 45: put
  db->Put(write_options, "key10633", "value13083");

  // op 46: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 47: put
  db->Put(write_options, "key1218", "value65982");

  // op 48: delete_range
  status = db->DeleteRange(write_options, "key4042", "key7550");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 49: put
  db->Put(write_options, "key2280", "value81265");

  // op 50: put
  db->Put(write_options, "key4680", "value87227");

  // op 51: put
  db->Put(write_options, "key8815", "value7082");

  // op 52: get
  status = db->Get(read_options, "key9831", &value);
  if (status.ok()) {
    out << "GET key9831 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0277", "key11609", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0277, key11609) size=" << scan_result.size() << std::endl;
  }

  // op 54: delete
  db->Delete(write_options, "key4427");

  // op 55: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4984", "key10200", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4984, key10200) size=" << scan_result.size() << std::endl;
  }

  // op 56: put
  db->Put(write_options, "key11303", "value29488");

  // op 57: put
  db->Put(write_options, "key2481", "value58690");

  // op 58: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 59: put
  db->Put(write_options, "key7540", "value65900");

  // op 60: get
  status = db->Get(read_options, "key2456", &value);
  if (status.ok()) {
    out << "GET key2456 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 61: put
  db->Put(write_options, "key10837", "value45141");

  // op 62: delete_range
  status = db->DeleteRange(write_options, "key10229", "key10992");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 63: put
  db->Put(write_options, "key10192", "value47039");

  // op 64: delete
  db->Delete(write_options, "key11091");

  // op 65: delete
  db->Delete(write_options, "key0979");

  // op 66: get
  status = db->Get(read_options, "key10039", &value);
  if (status.ok()) {
    out << "GET key10039 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 67: put
  db->Put(write_options, "key1792", "value26153");

  // op 68: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1555", "key1638", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1555, key1638) size=" << scan_result.size() << std::endl;
  }

  // op 69: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0517", "key7919", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0517, key7919) size=" << scan_result.size() << std::endl;
  }

  // op 70: put
  db->Put(write_options, "key5968", "value97490");

  // op 71: delete
  db->Delete(write_options, "key5052");

  // op 72: put
  db->Put(write_options, "key9931", "value109435");

  // op 73: put
  db->Put(write_options, "key7393", "value58001");

  // op 74: delete_range
  status = db->DeleteRange(write_options, "key5412", "key11040");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 75: delete_range
  status = db->DeleteRange(write_options, "key5045", "key7787");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 76: put
  db->Put(write_options, "key9004", "value74633");

  // op 77: delete
  db->Delete(write_options, "key3893");

  // op 78: get
  status = db->Get(read_options, "key0474", &value);
  if (status.ok()) {
    out << "GET key0474 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 79: put
  db->Put(write_options, "key5200", "value97388");

  // op 80: put
  db->Put(write_options, "key0028", "value73375");

  // op 81: put
  db->Put(write_options, "key8578", "value9297");

  // op 82: get
  status = db->Get(read_options, "key5133", &value);
  if (status.ok()) {
    out << "GET key5133 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 83: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1888", "key9574", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1888, key9574) size=" << scan_result.size() << std::endl;
  }

  // op 84: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1118", "key8204", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1118, key8204) size=" << scan_result.size() << std::endl;
  }

  // op 85: put
  db->Put(write_options, "key1008", "value52280");

  // op 86: delete_range
  status = db->DeleteRange(write_options, "key10266", "key10811");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 87: put
  db->Put(write_options, "key6892", "value8435");

  // op 88: get
  status = db->Get(read_options, "key11237", &value);
  if (status.ok()) {
    out << "GET key11237 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 89: get
  status = db->Get(read_options, "key6692", &value);
  if (status.ok()) {
    out << "GET key6692 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 90: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3301", "key8833", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3301, key8833) size=" << scan_result.size() << std::endl;
  }

  // op 91: get
  status = db->Get(read_options, "key5727", &value);
  if (status.ok()) {
    out << "GET key5727 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 92: get
  status = db->Get(read_options, "key3578", &value);
  if (status.ok()) {
    out << "GET key3578 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 93: delete
  db->Delete(write_options, "key2070");

  // op 94: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5523", "key9270", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5523, key9270) size=" << scan_result.size() << std::endl;
  }

  // op 95: get
  status = db->Get(read_options, "key5559", &value);
  if (status.ok()) {
    out << "GET key5559 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 96: delete
  db->Delete(write_options, "key2941");

  // op 97: put
  db->Put(write_options, "key5343", "value81408");

  // op 98: put
  db->Put(write_options, "key0050", "value91003");

  // op 99: put
  db->Put(write_options, "key3611", "value45013");

  // op 100: get
  status = db->Get(read_options, "key2704", &value);
  if (status.ok()) {
    out << "GET key2704 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 101: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2344", "key8804", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2344, key8804) size=" << scan_result.size() << std::endl;
  }

  // op 102: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7096", "key8635", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7096, key8635) size=" << scan_result.size() << std::endl;
  }

  // op 103: put
  db->Put(write_options, "key8853", "value94018");

  // op 104: put
  db->Put(write_options, "key5443", "value76410");

  // op 105: get
  status = db->Get(read_options, "key9321", &value);
  if (status.ok()) {
    out << "GET key9321 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 106: delete
  db->Delete(write_options, "key8798");

  // op 107: put
  db->Put(write_options, "key11680", "value110555");

  // op 108: put
  db->Put(write_options, "key4993", "value45067");

  // op 109: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5967", "key7878", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5967, key7878) size=" << scan_result.size() << std::endl;
  }

  // op 110: delete
  db->Delete(write_options, "key0866");

  // op 111: get
  status = db->Get(read_options, "key3351", &value);
  if (status.ok()) {
    out << "GET key3351 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 112: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 113: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 114: put
  db->Put(write_options, "key7258", "value80261");

  // op 115: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1473", "key10124", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1473, key10124) size=" << scan_result.size() << std::endl;
  }

  // op 116: get
  status = db->Get(read_options, "key6208", &value);
  if (status.ok()) {
    out << "GET key6208 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 117: put
  db->Put(write_options, "key0130", "value4358");

  // op 118: put
  db->Put(write_options, "key8867", "value113922");

  // op 119: get
  status = db->Get(read_options, "key9537", &value);
  if (status.ok()) {
    out << "GET key9537 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 120: get
  status = db->Get(read_options, "key6421", &value);
  if (status.ok()) {
    out << "GET key6421 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 121: delete_range
  status = db->DeleteRange(write_options, "key9646", "key10626");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 122: put
  db->Put(write_options, "key5637", "value41746");

  // op 123: delete_range
  status = db->DeleteRange(write_options, "key5137", "key8753");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 124: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 125: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4469", "key6847", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4469, key6847) size=" << scan_result.size() << std::endl;
  }

  // op 126: delete_range
  status = db->DeleteRange(write_options, "key8960", "key11371");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 127: delete_range
  status = db->DeleteRange(write_options, "key1056", "key6645");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 128: put
  db->Put(write_options, "key8941", "value24639");

  // op 129: get
  status = db->Get(read_options, "key3462", &value);
  if (status.ok()) {
    out << "GET key3462 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 130: put
  db->Put(write_options, "key9130", "value4390");

  // op 131: get
  status = db->Get(read_options, "key0852", &value);
  if (status.ok()) {
    out << "GET key0852 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 132: get
  status = db->Get(read_options, "key11722", &value);
  if (status.ok()) {
    out << "GET key11722 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 133: put
  db->Put(write_options, "key11524", "value74267");

  // op 134: get
  status = db->Get(read_options, "key10080", &value);
  if (status.ok()) {
    out << "GET key10080 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 135: put
  db->Put(write_options, "key0030", "value33456");

  // op 136: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4667", "key4817", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4667, key4817) size=" << scan_result.size() << std::endl;
  }

  // op 137: put
  db->Put(write_options, "key0286", "value66768");

  // op 138: put
  db->Put(write_options, "key4459", "value9991");

  // op 139: put
  db->Put(write_options, "key4845", "value78995");

  // op 140: delete_range
  status = db->DeleteRange(write_options, "key7422", "key10732");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 141: put
  db->Put(write_options, "key7673", "value43236");

  // op 142: put
  db->Put(write_options, "key11571", "value66921");

  // op 143: put
  db->Put(write_options, "key8206", "value79281");

  // op 144: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2185", "key9191", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2185, key9191) size=" << scan_result.size() << std::endl;
  }

  // op 145: put
  db->Put(write_options, "key6293", "value65261");

  // op 146: put
  db->Put(write_options, "key0116", "value90340");

  // op 147: put
  db->Put(write_options, "key4682", "value18996");

  // op 148: put
  db->Put(write_options, "key3599", "value0006");

  // op 149: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 150: put
  db->Put(write_options, "key2578", "value25853");

  // op 151: delete
  db->Delete(write_options, "key6479");

  // op 152: put
  db->Put(write_options, "key0648", "value96123");

  // op 153: put
  db->Put(write_options, "key6945", "value56598");

  // op 154: put
  db->Put(write_options, "key11461", "value27566");

  // op 155: get
  status = db->Get(read_options, "key0802", &value);
  if (status.ok()) {
    out << "GET key0802 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 156: put
  db->Put(write_options, "key9858", "value59917");

  // op 157: delete_range
  status = db->DeleteRange(write_options, "key1355", "key3132");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 158: put
  db->Put(write_options, "key10531", "value39175");

  // op 159: put
  db->Put(write_options, "key0229", "value77403");

  // op 160: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1310", "key1797", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1310, key1797) size=" << scan_result.size() << std::endl;
  }

  // op 161: put
  db->Put(write_options, "key11147", "value62643");

  // op 162: put
  db->Put(write_options, "key10067", "value96696");

  // op 163: put
  db->Put(write_options, "key5977", "value98015");

  // op 164: put
  db->Put(write_options, "key1278", "value52897");

  // op 165: put
  db->Put(write_options, "key4591", "value49201");

  // op 166: delete
  db->Delete(write_options, "key0717");

  // op 167: put
  db->Put(write_options, "key8825", "value57484");

  // op 168: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8491", "key9976", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8491, key9976) size=" << scan_result.size() << std::endl;
  }

  // op 169: delete
  db->Delete(write_options, "key3251");

  // op 170: put
  db->Put(write_options, "key5819", "value3200");

  // op 171: get
  status = db->Get(read_options, "key11395", &value);
  if (status.ok()) {
    out << "GET key11395 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 172: get
  status = db->Get(read_options, "key3643", &value);
  if (status.ok()) {
    out << "GET key3643 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 173: put
  db->Put(write_options, "key9620", "value31817");

  // op 174: put
  db->Put(write_options, "key10757", "value23798");

  // op 175: put
  db->Put(write_options, "key4579", "value65486");

  // op 176: put
  db->Put(write_options, "key2598", "value59681");

  // op 177: delete_range
  status = db->DeleteRange(write_options, "key3557", "key4779");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 178: get
  status = db->Get(read_options, "key4741", &value);
  if (status.ok()) {
    out << "GET key4741 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 179: put
  db->Put(write_options, "key8831", "value112777");

  // op 180: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7557", "key8450", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7557, key8450) size=" << scan_result.size() << std::endl;
  }

  // op 181: put
  db->Put(write_options, "key6814", "value87716");

  // op 182: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0794", "key10722", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0794, key10722) size=" << scan_result.size() << std::endl;
  }

  // op 183: put
  db->Put(write_options, "key10364", "value7837");

  // op 184: get
  status = db->Get(read_options, "key10406", &value);
  if (status.ok()) {
    out << "GET key10406 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 185: put
  db->Put(write_options, "key10573", "value78199");

  // op 186: put
  db->Put(write_options, "key6973", "value85014");

  // op 187: put
  db->Put(write_options, "key5814", "value94338");

  // op 188: put
  db->Put(write_options, "key9561", "value107395");

  // op 189: get
  status = db->Get(read_options, "key10597", &value);
  if (status.ok()) {
    out << "GET key10597 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 190: put
  db->Put(write_options, "key1921", "value91398");

  // op 191: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 192: delete
  db->Delete(write_options, "key10122");

  // op 193: get
  status = db->Get(read_options, "key0220", &value);
  if (status.ok()) {
    out << "GET key0220 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 194: delete
  db->Delete(write_options, "key9229");

  // op 195: put
  db->Put(write_options, "key6423", "value17184");

  // op 196: get
  status = db->Get(read_options, "key3516", &value);
  if (status.ok()) {
    out << "GET key3516 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 197: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 198: put
  db->Put(write_options, "key2305", "value17737");

  // op 199: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 200: delete_range
  status = db->DeleteRange(write_options, "key3903", "key10846");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 201: put
  db->Put(write_options, "key2444", "value111978");

  // op 202: delete
  db->Delete(write_options, "key2207");

  // op 203: delete
  db->Delete(write_options, "key5538");

  // op 204: put
  db->Put(write_options, "key2656", "value1483");

  // op 205: put
  db->Put(write_options, "key3930", "value109241");

  // op 206: put
  db->Put(write_options, "key5311", "value54138");

  // op 207: get
  status = db->Get(read_options, "key3240", &value);
  if (status.ok()) {
    out << "GET key3240 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 208: put
  db->Put(write_options, "key11405", "value40525");

  // op 209: put
  db->Put(write_options, "key7877", "value3556");

  // op 210: put
  db->Put(write_options, "key5297", "value22914");

  // op 211: get
  status = db->Get(read_options, "key5650", &value);
  if (status.ok()) {
    out << "GET key5650 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 212: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8867", "key8892", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8867, key8892) size=" << scan_result.size() << std::endl;
  }

  // op 213: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5696", "key8173", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5696, key8173) size=" << scan_result.size() << std::endl;
  }

  // op 214: put
  db->Put(write_options, "key6264", "value106212");

  // op 215: delete
  db->Delete(write_options, "key7805");

  // op 216: put
  db->Put(write_options, "key7365", "value9596");

  // op 217: delete
  db->Delete(write_options, "key11469");

  // op 218: put
  db->Put(write_options, "key2709", "value40471");

  // op 219: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4148", "key10346", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4148, key10346) size=" << scan_result.size() << std::endl;
  }

  // op 220: put
  db->Put(write_options, "key0266", "value26577");

  // op 221: delete_range
  status = db->DeleteRange(write_options, "key1237", "key7035");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 222: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 223: put
  db->Put(write_options, "key8681", "value86540");

  // op 224: get
  status = db->Get(read_options, "key3847", &value);
  if (status.ok()) {
    out << "GET key3847 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 225: delete
  db->Delete(write_options, "key3629");

  // op 226: put
  db->Put(write_options, "key3543", "value13634");

  // op 227: delete_range
  status = db->DeleteRange(write_options, "key5584", "key10493");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 228: get
  status = db->Get(read_options, "key5175", &value);
  if (status.ok()) {
    out << "GET key5175 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 229: delete
  db->Delete(write_options, "key6291");

  // op 230: get
  status = db->Get(read_options, "key9996", &value);
  if (status.ok()) {
    out << "GET key9996 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 231: put
  db->Put(write_options, "key6980", "value29838");

  // op 232: get
  status = db->Get(read_options, "key0644", &value);
  if (status.ok()) {
    out << "GET key0644 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 233: get
  status = db->Get(read_options, "key5370", &value);
  if (status.ok()) {
    out << "GET key5370 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 234: put
  db->Put(write_options, "key6061", "value48996");

  // op 235: get
  status = db->Get(read_options, "key5091", &value);
  if (status.ok()) {
    out << "GET key5091 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 236: put
  db->Put(write_options, "key8205", "value9023");

  // op 237: get
  status = db->Get(read_options, "key4636", &value);
  if (status.ok()) {
    out << "GET key4636 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 238: put
  db->Put(write_options, "key0679", "value0423");

  // op 239: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0895", "key1513", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0895, key1513) size=" << scan_result.size() << std::endl;
  }

  // op 240: get
  status = db->Get(read_options, "key0761", &value);
  if (status.ok()) {
    out << "GET key0761 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 241: get
  status = db->Get(read_options, "key5178", &value);
  if (status.ok()) {
    out << "GET key5178 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 242: get
  status = db->Get(read_options, "key1387", &value);
  if (status.ok()) {
    out << "GET key1387 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 243: put
  db->Put(write_options, "key2220", "value14591");

  // op 244: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5970", "key11439", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5970, key11439) size=" << scan_result.size() << std::endl;
  }

  // op 245: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4155", "key5285", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4155, key5285) size=" << scan_result.size() << std::endl;
  }

  // op 246: put
  db->Put(write_options, "key9873", "value61996");

  // op 247: put
  db->Put(write_options, "key5225", "value107087");

  // op 248: get
  status = db->Get(read_options, "key0723", &value);
  if (status.ok()) {
    out << "GET key0723 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 249: put
  db->Put(write_options, "key5451", "value87331");

  // op 250: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1824", "key8733", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1824, key8733) size=" << scan_result.size() << std::endl;
  }

  // op 251: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8938", "key9319", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8938, key9319) size=" << scan_result.size() << std::endl;
  }

  // op 252: get
  status = db->Get(read_options, "key6476", &value);
  if (status.ok()) {
    out << "GET key6476 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 253: put
  db->Put(write_options, "key5635", "value28180");

  // op 254: get
  status = db->Get(read_options, "key4640", &value);
  if (status.ok()) {
    out << "GET key4640 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 255: delete_range
  status = db->DeleteRange(write_options, "key5626", "key7086");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 256: put
  db->Put(write_options, "key6835", "value58849");

  // op 257: put
  db->Put(write_options, "key4259", "value116738");

  // op 258: put
  db->Put(write_options, "key0315", "value42824");

  // op 259: put
  db->Put(write_options, "key3141", "value67537");

  // op 260: get
  status = db->Get(read_options, "key9769", &value);
  if (status.ok()) {
    out << "GET key9769 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 261: delete_range
  status = db->DeleteRange(write_options, "key1721", "key11595");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 262: get
  status = db->Get(read_options, "key8576", &value);
  if (status.ok()) {
    out << "GET key8576 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 263: delete
  db->Delete(write_options, "key5613");

  // op 264: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0691", "key11273", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0691, key11273) size=" << scan_result.size() << std::endl;
  }

  // op 265: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1484", "key10444", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1484, key10444) size=" << scan_result.size() << std::endl;
  }

  // op 266: delete
  db->Delete(write_options, "key9414");

  // op 267: get
  status = db->Get(read_options, "key4245", &value);
  if (status.ok()) {
    out << "GET key4245 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 268: get
  status = db->Get(read_options, "key0970", &value);
  if (status.ok()) {
    out << "GET key0970 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 269: put
  db->Put(write_options, "key3348", "value40341");

  // op 270: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 271: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 272: get
  status = db->Get(read_options, "key6856", &value);
  if (status.ok()) {
    out << "GET key6856 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 273: delete
  db->Delete(write_options, "key6658");

  // op 274: put
  db->Put(write_options, "key0857", "value78317");

  // op 275: delete_range
  status = db->DeleteRange(write_options, "key4996", "key5972");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 276: put
  db->Put(write_options, "key5590", "value91446");

  // op 277: put
  db->Put(write_options, "key7440", "value71648");

  // op 278: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 279: put
  db->Put(write_options, "key6200", "value85629");

  // op 280: get
  status = db->Get(read_options, "key8285", &value);
  if (status.ok()) {
    out << "GET key8285 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 281: put
  db->Put(write_options, "key0913", "value26806");

  // op 282: delete
  db->Delete(write_options, "key2137");

  // op 283: get
  status = db->Get(read_options, "key10951", &value);
  if (status.ok()) {
    out << "GET key10951 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 284: delete
  db->Delete(write_options, "key3820");

  // op 285: delete
  db->Delete(write_options, "key9249");

  // op 286: delete_range
  status = db->DeleteRange(write_options, "key6291", "key7688");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 287: delete
  db->Delete(write_options, "key10924");

  // op 288: delete
  db->Delete(write_options, "key8104");

  // op 289: get
  status = db->Get(read_options, "key1975", &value);
  if (status.ok()) {
    out << "GET key1975 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 290: put
  db->Put(write_options, "key0469", "value82082");

  // op 291: put
  db->Put(write_options, "key8924", "value2134");

  // op 292: get
  status = db->Get(read_options, "key6630", &value);
  if (status.ok()) {
    out << "GET key6630 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 293: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0455", "key9191", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0455, key9191) size=" << scan_result.size() << std::endl;
  }

  // op 294: delete_range
  status = db->DeleteRange(write_options, "key0695", "key3450");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 295: get
  status = db->Get(read_options, "key5140", &value);
  if (status.ok()) {
    out << "GET key5140 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 296: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1822", "key1975", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1822, key1975) size=" << scan_result.size() << std::endl;
  }

  // op 297: get
  status = db->Get(read_options, "key6082", &value);
  if (status.ok()) {
    out << "GET key6082 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 298: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3307", "key4192", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3307, key4192) size=" << scan_result.size() << std::endl;
  }

  // op 299: put
  db->Put(write_options, "key1556", "value2198");

  // op 300: put
  db->Put(write_options, "key11088", "value63820");

  // op 301: get
  status = db->Get(read_options, "key6741", &value);
  if (status.ok()) {
    out << "GET key6741 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 302: delete_range
  status = db->DeleteRange(write_options, "key1306", "key4261");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 303: get
  status = db->Get(read_options, "key8354", &value);
  if (status.ok()) {
    out << "GET key8354 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 304: get
  status = db->Get(read_options, "key9554", &value);
  if (status.ok()) {
    out << "GET key9554 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 305: put
  db->Put(write_options, "key9204", "value88048");

  // op 306: get
  status = db->Get(read_options, "key2298", &value);
  if (status.ok()) {
    out << "GET key2298 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 307: put
  db->Put(write_options, "key3344", "value82009");

  // op 308: delete
  db->Delete(write_options, "key11138");

  // op 309: put
  db->Put(write_options, "key3795", "value64151");

  // op 310: put
  db->Put(write_options, "key6349", "value7729");

  // op 311: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0141", "key8045", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0141, key8045) size=" << scan_result.size() << std::endl;
  }

  // op 312: get
  status = db->Get(read_options, "key3403", &value);
  if (status.ok()) {
    out << "GET key3403 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 313: get
  status = db->Get(read_options, "key1645", &value);
  if (status.ok()) {
    out << "GET key1645 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 314: put
  db->Put(write_options, "key8831", "value115700");

  // op 315: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1581", "key7319", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1581, key7319) size=" << scan_result.size() << std::endl;
  }

  // op 316: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4621", "key7267", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4621, key7267) size=" << scan_result.size() << std::endl;
  }

  // op 317: get
  status = db->Get(read_options, "key0208", &value);
  if (status.ok()) {
    out << "GET key0208 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 318: delete
  db->Delete(write_options, "key7970");

  // op 319: put
  db->Put(write_options, "key11042", "value65342");

  // op 320: delete_range
  status = db->DeleteRange(write_options, "key2438", "key6269");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 321: get
  status = db->Get(read_options, "key10523", &value);
  if (status.ok()) {
    out << "GET key10523 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 322: put
  db->Put(write_options, "key8305", "value13809");

  // op 323: delete
  db->Delete(write_options, "key0336");

  // op 324: put
  db->Put(write_options, "key2267", "value92059");

  // op 325: get
  status = db->Get(read_options, "key11618", &value);
  if (status.ok()) {
    out << "GET key11618 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 326: get
  status = db->Get(read_options, "key10010", &value);
  if (status.ok()) {
    out << "GET key10010 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 327: delete
  db->Delete(write_options, "key0551");

  // op 328: put
  db->Put(write_options, "key11032", "value12607");

  // op 329: get
  status = db->Get(read_options, "key2262", &value);
  if (status.ok()) {
    out << "GET key2262 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 330: delete_range
  status = db->DeleteRange(write_options, "key3925", "key9231");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 331: get
  status = db->Get(read_options, "key0550", &value);
  if (status.ok()) {
    out << "GET key0550 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 332: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6098", "key11055", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6098, key11055) size=" << scan_result.size() << std::endl;
  }

  // op 333: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0602", "key4788", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0602, key4788) size=" << scan_result.size() << std::endl;
  }

  // op 334: put
  db->Put(write_options, "key10652", "value3992");

  // op 335: get
  status = db->Get(read_options, "key3540", &value);
  if (status.ok()) {
    out << "GET key3540 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 336: put
  db->Put(write_options, "key9377", "value81492");

  // op 337: put
  db->Put(write_options, "key6982", "value16041");

  // op 338: delete_range
  status = db->DeleteRange(write_options, "key3890", "key7022");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 339: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 340: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3877", "key5336", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3877, key5336) size=" << scan_result.size() << std::endl;
  }

  // op 341: get
  status = db->Get(read_options, "key9488", &value);
  if (status.ok()) {
    out << "GET key9488 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 342: put
  db->Put(write_options, "key9460", "value68169");

  // op 343: put
  db->Put(write_options, "key7346", "value102447");

  // op 344: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8410", "key9470", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8410, key9470) size=" << scan_result.size() << std::endl;
  }

  // op 345: delete
  db->Delete(write_options, "key9502");

  // op 346: put
  db->Put(write_options, "key6812", "value110263");

  // op 347: put
  db->Put(write_options, "key11336", "value115429");

  // op 348: put
  db->Put(write_options, "key8871", "value87830");

  // op 349: delete
  db->Delete(write_options, "key10722");

  // op 350: delete
  db->Delete(write_options, "key0475");

  // op 351: delete_range
  status = db->DeleteRange(write_options, "key2070", "key2096");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 352: put
  db->Put(write_options, "key4482", "value2118");

  // op 353: delete_range
  status = db->DeleteRange(write_options, "key3405", "key8952");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 354: get
  status = db->Get(read_options, "key4505", &value);
  if (status.ok()) {
    out << "GET key4505 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 355: get
  status = db->Get(read_options, "key0134", &value);
  if (status.ok()) {
    out << "GET key0134 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 356: put
  db->Put(write_options, "key4339", "value81140");

  // op 357: delete_range
  status = db->DeleteRange(write_options, "key5802", "key7055");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 358: get
  status = db->Get(read_options, "key3829", &value);
  if (status.ok()) {
    out << "GET key3829 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 359: put
  db->Put(write_options, "key4159", "value97808");

  // op 360: delete
  db->Delete(write_options, "key9805");

  // op 361: get
  status = db->Get(read_options, "key9332", &value);
  if (status.ok()) {
    out << "GET key9332 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 362: delete
  db->Delete(write_options, "key8788");

  // op 363: get
  status = db->Get(read_options, "key5521", &value);
  if (status.ok()) {
    out << "GET key5521 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 364: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 365: delete_range
  status = db->DeleteRange(write_options, "key3426", "key8880");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 366: put
  db->Put(write_options, "key7510", "value68198");

  // op 367: get
  status = db->Get(read_options, "key4223", &value);
  if (status.ok()) {
    out << "GET key4223 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 368: get
  status = db->Get(read_options, "key5067", &value);
  if (status.ok()) {
    out << "GET key5067 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 369: put
  db->Put(write_options, "key4594", "value100633");

  // op 370: get
  status = db->Get(read_options, "key9596", &value);
  if (status.ok()) {
    out << "GET key9596 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 371: put
  db->Put(write_options, "key9035", "value37262");

  // op 372: put
  db->Put(write_options, "key5196", "value15646");

  // op 373: put
  db->Put(write_options, "key8328", "value63226");

  // op 374: delete_range
  status = db->DeleteRange(write_options, "key3766", "key9149");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 375: put
  db->Put(write_options, "key5733", "value115680");

  // op 376: get
  status = db->Get(read_options, "key10549", &value);
  if (status.ok()) {
    out << "GET key10549 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 377: put
  db->Put(write_options, "key2152", "value7002");

  // op 378: get
  status = db->Get(read_options, "key8745", &value);
  if (status.ok()) {
    out << "GET key8745 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 379: delete_range
  status = db->DeleteRange(write_options, "key2249", "key9178");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 380: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 381: delete_range
  status = db->DeleteRange(write_options, "key5685", "key6809");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 382: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7580", "key9334", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7580, key9334) size=" << scan_result.size() << std::endl;
  }

  // op 383: put
  db->Put(write_options, "key2761", "value91837");

  // op 384: get
  status = db->Get(read_options, "key3419", &value);
  if (status.ok()) {
    out << "GET key3419 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 385: get
  status = db->Get(read_options, "key3081", &value);
  if (status.ok()) {
    out << "GET key3081 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 386: get
  status = db->Get(read_options, "key11285", &value);
  if (status.ok()) {
    out << "GET key11285 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 387: get
  status = db->Get(read_options, "key3327", &value);
  if (status.ok()) {
    out << "GET key3327 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 388: delete
  db->Delete(write_options, "key7265");

  // op 389: delete
  db->Delete(write_options, "key6817");

  // op 390: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6601", "key11473", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6601, key11473) size=" << scan_result.size() << std::endl;
  }

  // op 391: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6072", "key9499", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6072, key9499) size=" << scan_result.size() << std::endl;
  }

  // op 392: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 393: put
  db->Put(write_options, "key3706", "value47934");

  // op 394: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4306", "key9251", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4306, key9251) size=" << scan_result.size() << std::endl;
  }

  // op 395: get
  status = db->Get(read_options, "key0879", &value);
  if (status.ok()) {
    out << "GET key0879 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 396: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1300", "key10121", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1300, key10121) size=" << scan_result.size() << std::endl;
  }

  // op 397: put
  db->Put(write_options, "key6727", "value98461");

  // op 398: delete
  db->Delete(write_options, "key0158");

  // op 399: delete
  db->Delete(write_options, "key10332");

  // op 400: get
  status = db->Get(read_options, "key7235", &value);
  if (status.ok()) {
    out << "GET key7235 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 401: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6450", "key7864", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6450, key7864) size=" << scan_result.size() << std::endl;
  }

  // op 402: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3245", "key4038", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3245, key4038) size=" << scan_result.size() << std::endl;
  }

  // op 403: put
  db->Put(write_options, "key8193", "value82047");

  // op 404: get
  status = db->Get(read_options, "key9733", &value);
  if (status.ok()) {
    out << "GET key9733 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 405: get
  status = db->Get(read_options, "key1198", &value);
  if (status.ok()) {
    out << "GET key1198 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 406: get
  status = db->Get(read_options, "key0320", &value);
  if (status.ok()) {
    out << "GET key0320 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 407: put
  db->Put(write_options, "key5842", "value1538");

  // op 408: put
  db->Put(write_options, "key3400", "value22614");

  // op 409: get
  status = db->Get(read_options, "key7041", &value);
  if (status.ok()) {
    out << "GET key7041 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 410: put
  db->Put(write_options, "key7518", "value111716");

  // op 411: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2198", "key5000", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2198, key5000) size=" << scan_result.size() << std::endl;
  }

  // op 412: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6447", "key8251", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6447, key8251) size=" << scan_result.size() << std::endl;
  }

  // op 413: get
  status = db->Get(read_options, "key1799", &value);
  if (status.ok()) {
    out << "GET key1799 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 414: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5316", "key6975", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5316, key6975) size=" << scan_result.size() << std::endl;
  }

  // op 415: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1457", "key11642", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1457, key11642) size=" << scan_result.size() << std::endl;
  }

  // op 416: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 417: put
  db->Put(write_options, "key7647", "value24331");

  // op 418: delete
  db->Delete(write_options, "key9070");

  // op 419: delete
  db->Delete(write_options, "key5470");

  // op 420: put
  db->Put(write_options, "key5190", "value27169");

  // op 421: get
  status = db->Get(read_options, "key3515", &value);
  if (status.ok()) {
    out << "GET key3515 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 422: put
  db->Put(write_options, "key5143", "value105460");

  // op 423: delete
  db->Delete(write_options, "key8575");

  // op 424: delete_range
  status = db->DeleteRange(write_options, "key0087", "key6290");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 425: get
  status = db->Get(read_options, "key7936", &value);
  if (status.ok()) {
    out << "GET key7936 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 426: put
  db->Put(write_options, "key1065", "value35988");

  // op 427: put
  db->Put(write_options, "key1857", "value19183");

  // op 428: delete_range
  status = db->DeleteRange(write_options, "key0213", "key5676");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 429: put
  db->Put(write_options, "key3457", "value103144");

  // op 430: get
  status = db->Get(read_options, "key5726", &value);
  if (status.ok()) {
    out << "GET key5726 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 431: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3059", "key7577", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3059, key7577) size=" << scan_result.size() << std::endl;
  }

  // op 432: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 433: get
  status = db->Get(read_options, "key4667", &value);
  if (status.ok()) {
    out << "GET key4667 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 434: put
  db->Put(write_options, "key11428", "value84420");

  // op 435: delete
  db->Delete(write_options, "key7965");

  // op 436: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1357", "key3395", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1357, key3395) size=" << scan_result.size() << std::endl;
  }

  // op 437: put
  db->Put(write_options, "key2453", "value55643");

  // op 438: put
  db->Put(write_options, "key8440", "value63226");

  // op 439: delete
  db->Delete(write_options, "key1067");

  // op 440: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 441: put
  db->Put(write_options, "key0184", "value37914");

  // op 442: delete_range
  status = db->DeleteRange(write_options, "key5404", "key10486");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 443: delete_range
  status = db->DeleteRange(write_options, "key2311", "key10411");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 444: get
  status = db->Get(read_options, "key1061", &value);
  if (status.ok()) {
    out << "GET key1061 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 445: put
  db->Put(write_options, "key9007", "value29926");

  // op 446: delete
  db->Delete(write_options, "key6303");

  // op 447: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5547", "key7862", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5547, key7862) size=" << scan_result.size() << std::endl;
  }

  // op 448: delete_range
  status = db->DeleteRange(write_options, "key8231", "key8327");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 449: put
  db->Put(write_options, "key7658", "value58129");

  // op 450: delete
  db->Delete(write_options, "key10646");

  // op 451: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6056", "key6093", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6056, key6093) size=" << scan_result.size() << std::endl;
  }

  // op 452: delete
  db->Delete(write_options, "key2686");

  // op 453: put
  db->Put(write_options, "key9335", "value7665");

  // op 454: delete
  db->Delete(write_options, "key8459");

  // op 455: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 456: put
  db->Put(write_options, "key5923", "value50600");

  // op 457: put
  db->Put(write_options, "key4979", "value67134");

  // op 458: put
  db->Put(write_options, "key5811", "value104756");

  // op 459: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2641", "key5389", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2641, key5389) size=" << scan_result.size() << std::endl;
  }

  // op 460: delete_range
  status = db->DeleteRange(write_options, "key0313", "key2767");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 461: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7957", "key8786", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7957, key8786) size=" << scan_result.size() << std::endl;
  }

  // op 462: get
  status = db->Get(read_options, "key6791", &value);
  if (status.ok()) {
    out << "GET key6791 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 463: delete_range
  status = db->DeleteRange(write_options, "key4167", "key10188");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 464: put
  db->Put(write_options, "key11605", "value49853");

  // op 465: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6334", "key8962", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6334, key8962) size=" << scan_result.size() << std::endl;
  }

  // op 466: put
  db->Put(write_options, "key10465", "value56333");

  // op 467: get
  status = db->Get(read_options, "key1232", &value);
  if (status.ok()) {
    out << "GET key1232 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 468: put
  db->Put(write_options, "key1095", "value33875");

  // op 469: get
  status = db->Get(read_options, "key3733", &value);
  if (status.ok()) {
    out << "GET key3733 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 470: get
  status = db->Get(read_options, "key1878", &value);
  if (status.ok()) {
    out << "GET key1878 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 471: put
  db->Put(write_options, "key1323", "value63399");

  // op 472: get
  status = db->Get(read_options, "key7932", &value);
  if (status.ok()) {
    out << "GET key7932 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 473: put
  db->Put(write_options, "key6010", "value24766");

  // op 474: delete
  db->Delete(write_options, "key0143");

  // op 475: delete_range
  status = db->DeleteRange(write_options, "key1099", "key5044");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 476: put
  db->Put(write_options, "key7899", "value67440");

  // op 477: put
  db->Put(write_options, "key0219", "value115622");

  // op 478: put
  db->Put(write_options, "key6021", "value86810");

  // op 479: delete
  db->Delete(write_options, "key9456");

  // op 480: put
  db->Put(write_options, "key5496", "value63763");

  // op 481: put
  db->Put(write_options, "key3002", "value77393");

  // op 482: put
  db->Put(write_options, "key7776", "value94998");

  // op 483: put
  db->Put(write_options, "key10824", "value49159");

  // op 484: put
  db->Put(write_options, "key0680", "value80619");

  // op 485: get
  status = db->Get(read_options, "key7688", &value);
  if (status.ok()) {
    out << "GET key7688 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 486: put
  db->Put(write_options, "key10688", "value11912");

  // op 487: put
  db->Put(write_options, "key4206", "value82544");

  // op 488: put
  db->Put(write_options, "key3522", "value24304");

  // op 489: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5811", "key11490", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5811, key11490) size=" << scan_result.size() << std::endl;
  }

  // op 490: delete_range
  status = db->DeleteRange(write_options, "key6464", "key11408");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 491: get
  status = db->Get(read_options, "key8730", &value);
  if (status.ok()) {
    out << "GET key8730 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 492: put
  db->Put(write_options, "key8553", "value79274");

  // op 493: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 494: put
  db->Put(write_options, "key1833", "value71154");

  // op 495: get
  status = db->Get(read_options, "key8595", &value);
  if (status.ok()) {
    out << "GET key8595 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 496: get
  status = db->Get(read_options, "key11240", &value);
  if (status.ok()) {
    out << "GET key11240 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 497: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6587", "key9910", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6587, key9910) size=" << scan_result.size() << std::endl;
  }

  // op 498: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 499: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0461", "key10882", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0461, key10882) size=" << scan_result.size() << std::endl;
  }

  // op 500: put
  db->Put(write_options, "key6395", "value1142");

  // op 501: put
  db->Put(write_options, "key1946", "value2008");

  // op 502: put
  db->Put(write_options, "key8477", "value34414");

  // op 503: delete_range
  status = db->DeleteRange(write_options, "key1932", "key9973");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 504: put
  db->Put(write_options, "key2125", "value36941");

  // op 505: delete
  db->Delete(write_options, "key6566");

  // op 506: delete
  db->Delete(write_options, "key4319");

  // op 507: get
  status = db->Get(read_options, "key9526", &value);
  if (status.ok()) {
    out << "GET key9526 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 508: get
  status = db->Get(read_options, "key2302", &value);
  if (status.ok()) {
    out << "GET key2302 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 509: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 510: put
  db->Put(write_options, "key9622", "value118255");

  // op 511: put
  db->Put(write_options, "key7244", "value86682");

  // op 512: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1208", "key2643", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1208, key2643) size=" << scan_result.size() << std::endl;
  }

  // op 513: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 514: put
  db->Put(write_options, "key5300", "value107733");

  // op 515: put
  db->Put(write_options, "key6134", "value58109");

  // op 516: get
  status = db->Get(read_options, "key8703", &value);
  if (status.ok()) {
    out << "GET key8703 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 517: put
  db->Put(write_options, "key5158", "value86852");

  // op 518: put
  db->Put(write_options, "key2692", "value3480");

  // op 519: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1013", "key9209", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1013, key9209) size=" << scan_result.size() << std::endl;
  }

  // op 520: get
  status = db->Get(read_options, "key6610", &value);
  if (status.ok()) {
    out << "GET key6610 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 521: delete
  db->Delete(write_options, "key1539");

  // op 522: get
  status = db->Get(read_options, "key11011", &value);
  if (status.ok()) {
    out << "GET key11011 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 523: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7683", "key11277", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7683, key11277) size=" << scan_result.size() << std::endl;
  }

  // op 524: delete_range
  status = db->DeleteRange(write_options, "key2356", "key9215");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 525: delete
  db->Delete(write_options, "key11515");

  // op 526: get
  status = db->Get(read_options, "key3374", &value);
  if (status.ok()) {
    out << "GET key3374 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 527: put
  db->Put(write_options, "key2517", "value97052");

  // op 528: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 529: put
  db->Put(write_options, "key4834", "value117688");

  // op 530: put
  db->Put(write_options, "key5454", "value55861");

  // op 531: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1624", "key10209", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1624, key10209) size=" << scan_result.size() << std::endl;
  }

  // op 532: put
  db->Put(write_options, "key6577", "value51668");

  // op 533: delete_range
  status = db->DeleteRange(write_options, "key8785", "key10662");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 534: delete
  db->Delete(write_options, "key5899");

  // op 535: put
  db->Put(write_options, "key8401", "value80988");

  // op 536: put
  db->Put(write_options, "key10873", "value44586");

  // op 537: put
  db->Put(write_options, "key7341", "value64998");

  // op 538: delete_range
  status = db->DeleteRange(write_options, "key2953", "key9456");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 539: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4006", "key7961", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4006, key7961) size=" << scan_result.size() << std::endl;
  }

  // op 540: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4398", "key5180", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4398, key5180) size=" << scan_result.size() << std::endl;
  }

  // op 541: delete_range
  status = db->DeleteRange(write_options, "key6968", "key9821");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 542: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6976", "key8058", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6976, key8058) size=" << scan_result.size() << std::endl;
  }

  // op 543: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3977", "key4849", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3977, key4849) size=" << scan_result.size() << std::endl;
  }

  // op 544: get
  status = db->Get(read_options, "key7495", &value);
  if (status.ok()) {
    out << "GET key7495 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 545: delete
  db->Delete(write_options, "key7226");

  // op 546: put
  db->Put(write_options, "key1393", "value1715");

  // op 547: get
  status = db->Get(read_options, "key7126", &value);
  if (status.ok()) {
    out << "GET key7126 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 548: delete
  db->Delete(write_options, "key6078");

  // op 549: get
  status = db->Get(read_options, "key5123", &value);
  if (status.ok()) {
    out << "GET key5123 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 550: get
  status = db->Get(read_options, "key2561", &value);
  if (status.ok()) {
    out << "GET key2561 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 551: put
  db->Put(write_options, "key8798", "value90516");

  // op 552: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0142", "key3193", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0142, key3193) size=" << scan_result.size() << std::endl;
  }

  // op 553: put
  db->Put(write_options, "key2149", "value90580");

  // op 554: delete_range
  status = db->DeleteRange(write_options, "key6135", "key7438");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 555: get
  status = db->Get(read_options, "key10057", &value);
  if (status.ok()) {
    out << "GET key10057 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 556: put
  db->Put(write_options, "key0019", "value17144");

  // op 557: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10681", "key10694", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10681, key10694) size=" << scan_result.size() << std::endl;
  }

  // op 558: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 559: get
  status = db->Get(read_options, "key11098", &value);
  if (status.ok()) {
    out << "GET key11098 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 560: get
  status = db->Get(read_options, "key8013", &value);
  if (status.ok()) {
    out << "GET key8013 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 561: get
  status = db->Get(read_options, "key6063", &value);
  if (status.ok()) {
    out << "GET key6063 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 562: delete_range
  status = db->DeleteRange(write_options, "key5484", "key7673");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 563: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4617", "key5349", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4617, key5349) size=" << scan_result.size() << std::endl;
  }

  // op 564: delete
  db->Delete(write_options, "key5718");

  // op 565: put
  db->Put(write_options, "key5273", "value61121");

  // op 566: put
  db->Put(write_options, "key10809", "value10052");

  // op 567: get
  status = db->Get(read_options, "key4328", &value);
  if (status.ok()) {
    out << "GET key4328 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 568: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 569: delete_range
  status = db->DeleteRange(write_options, "key0889", "key6764");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 570: get
  status = db->Get(read_options, "key9211", &value);
  if (status.ok()) {
    out << "GET key9211 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 571: get
  status = db->Get(read_options, "key4154", &value);
  if (status.ok()) {
    out << "GET key4154 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 572: delete
  db->Delete(write_options, "key5874");

  // op 573: delete_range
  status = db->DeleteRange(write_options, "key4233", "key9250");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 574: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5412", "key5808", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5412, key5808) size=" << scan_result.size() << std::endl;
  }

  // op 575: put
  db->Put(write_options, "key5320", "value51195");

  // op 576: put
  db->Put(write_options, "key3915", "value56356");

  // op 577: get
  status = db->Get(read_options, "key0606", &value);
  if (status.ok()) {
    out << "GET key0606 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 578: delete
  db->Delete(write_options, "key1713");

  // op 579: get
  status = db->Get(read_options, "key9179", &value);
  if (status.ok()) {
    out << "GET key9179 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 580: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5062", "key11215", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5062, key11215) size=" << scan_result.size() << std::endl;
  }

  // op 581: get
  status = db->Get(read_options, "key8152", &value);
  if (status.ok()) {
    out << "GET key8152 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 582: get
  status = db->Get(read_options, "key5612", &value);
  if (status.ok()) {
    out << "GET key5612 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 583: get
  status = db->Get(read_options, "key7107", &value);
  if (status.ok()) {
    out << "GET key7107 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 584: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2039", "key3584", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2039, key3584) size=" << scan_result.size() << std::endl;
  }

  // op 585: put
  db->Put(write_options, "key3880", "value98682");

  // op 586: put
  db->Put(write_options, "key5106", "value7623");

  // op 587: put
  db->Put(write_options, "key6660", "value80699");

  // op 588: get
  status = db->Get(read_options, "key0022", &value);
  if (status.ok()) {
    out << "GET key0022 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 589: put
  db->Put(write_options, "key2530", "value100908");

  // op 590: put
  db->Put(write_options, "key10628", "value44187");

  // op 591: delete_range
  status = db->DeleteRange(write_options, "key4230", "key4988");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 592: put
  db->Put(write_options, "key8784", "value5103");

  // op 593: put
  db->Put(write_options, "key2178", "value96669");

  // op 594: put
  db->Put(write_options, "key1502", "value10886");

  // op 595: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9885", "key10523", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9885, key10523) size=" << scan_result.size() << std::endl;
  }

  // op 596: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 597: put
  db->Put(write_options, "key4250", "value72727");

  // op 598: delete
  db->Delete(write_options, "key9058");

  // op 599: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1286", "key8717", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1286, key8717) size=" << scan_result.size() << std::endl;
  }

  // op 600: get
  status = db->Get(read_options, "key1453", &value);
  if (status.ok()) {
    out << "GET key1453 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 601: put
  db->Put(write_options, "key9234", "value32850");

  // op 602: get
  status = db->Get(read_options, "key7803", &value);
  if (status.ok()) {
    out << "GET key7803 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 603: delete_range
  status = db->DeleteRange(write_options, "key3315", "key6806");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 604: get
  status = db->Get(read_options, "key4954", &value);
  if (status.ok()) {
    out << "GET key4954 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 605: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 606: get
  status = db->Get(read_options, "key5654", &value);
  if (status.ok()) {
    out << "GET key5654 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 607: put
  db->Put(write_options, "key8337", "value113763");

  // op 608: get
  status = db->Get(read_options, "key5509", &value);
  if (status.ok()) {
    out << "GET key5509 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 609: get
  status = db->Get(read_options, "key4559", &value);
  if (status.ok()) {
    out << "GET key4559 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 610: put
  db->Put(write_options, "key11198", "value102491");

  // op 611: get
  status = db->Get(read_options, "key10604", &value);
  if (status.ok()) {
    out << "GET key10604 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 612: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0285", "key9512", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0285, key9512) size=" << scan_result.size() << std::endl;
  }

  // op 613: put
  db->Put(write_options, "key10366", "value53420");

  // op 614: put
  db->Put(write_options, "key6871", "value113604");

  // op 615: put
  db->Put(write_options, "key7855", "value46914");

  // op 616: get
  status = db->Get(read_options, "key7339", &value);
  if (status.ok()) {
    out << "GET key7339 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 617: delete_range
  status = db->DeleteRange(write_options, "key8331", "key9499");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 618: put
  db->Put(write_options, "key4320", "value101025");

  // op 619: put
  db->Put(write_options, "key3388", "value47120");

  // op 620: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 621: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5334", "key11631", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5334, key11631) size=" << scan_result.size() << std::endl;
  }

  // op 622: delete
  db->Delete(write_options, "key1759");

  // op 623: delete_range
  status = db->DeleteRange(write_options, "key2651", "key10880");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 624: put
  db->Put(write_options, "key6814", "value85258");

  // op 625: get
  status = db->Get(read_options, "key9003", &value);
  if (status.ok()) {
    out << "GET key9003 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 626: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1849", "key7098", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1849, key7098) size=" << scan_result.size() << std::endl;
  }

  // op 627: put
  db->Put(write_options, "key7370", "value1503");

  // op 628: delete_range
  status = db->DeleteRange(write_options, "key5121", "key11174");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 629: delete
  db->Delete(write_options, "key9972");

  // op 630: get
  status = db->Get(read_options, "key3140", &value);
  if (status.ok()) {
    out << "GET key3140 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 631: delete
  db->Delete(write_options, "key11076");

  // op 632: get
  status = db->Get(read_options, "key1232", &value);
  if (status.ok()) {
    out << "GET key1232 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 633: get
  status = db->Get(read_options, "key6142", &value);
  if (status.ok()) {
    out << "GET key6142 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 634: put
  db->Put(write_options, "key11450", "value56982");

  // op 635: put
  db->Put(write_options, "key5392", "value27881");

  // op 636: put
  db->Put(write_options, "key6970", "value49866");

  // op 637: put
  db->Put(write_options, "key7954", "value73361");

  // op 638: get
  status = db->Get(read_options, "key9188", &value);
  if (status.ok()) {
    out << "GET key9188 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 639: get
  status = db->Get(read_options, "key5714", &value);
  if (status.ok()) {
    out << "GET key5714 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 640: put
  db->Put(write_options, "key9319", "value65417");

  // op 641: get
  status = db->Get(read_options, "key7253", &value);
  if (status.ok()) {
    out << "GET key7253 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 642: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3217", "key7668", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3217, key7668) size=" << scan_result.size() << std::endl;
  }

  // op 643: get
  status = db->Get(read_options, "key2904", &value);
  if (status.ok()) {
    out << "GET key2904 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 644: delete_range
  status = db->DeleteRange(write_options, "key6556", "key10145");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 645: delete_range
  status = db->DeleteRange(write_options, "key3468", "key4032");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 646: get
  status = db->Get(read_options, "key6301", &value);
  if (status.ok()) {
    out << "GET key6301 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 647: put
  db->Put(write_options, "key8290", "value107938");

  // op 648: put
  db->Put(write_options, "key0306", "value49624");

  // op 649: delete
  db->Delete(write_options, "key8526");

  // op 650: put
  db->Put(write_options, "key1658", "value45722");

  // op 651: delete_range
  status = db->DeleteRange(write_options, "key2642", "key9925");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 652: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7976", "key11608", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7976, key11608) size=" << scan_result.size() << std::endl;
  }

  // op 653: delete_range
  status = db->DeleteRange(write_options, "key2864", "key3026");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 654: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3420", "key4765", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3420, key4765) size=" << scan_result.size() << std::endl;
  }

  // op 655: put
  db->Put(write_options, "key11722", "value11931");

  // op 656: get
  status = db->Get(read_options, "key3436", &value);
  if (status.ok()) {
    out << "GET key3436 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 657: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 658: delete_range
  status = db->DeleteRange(write_options, "key0055", "key1971");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 659: get
  status = db->Get(read_options, "key4310", &value);
  if (status.ok()) {
    out << "GET key4310 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 660: get
  status = db->Get(read_options, "key9803", &value);
  if (status.ok()) {
    out << "GET key9803 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 661: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7325", "key8895", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7325, key8895) size=" << scan_result.size() << std::endl;
  }

  // op 662: put
  db->Put(write_options, "key5598", "value102969");

  // op 663: put
  db->Put(write_options, "key5807", "value44692");

  // op 664: put
  db->Put(write_options, "key0012", "value51465");

  // op 665: get
  status = db->Get(read_options, "key10808", &value);
  if (status.ok()) {
    out << "GET key10808 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 666: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1381", "key7619", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1381, key7619) size=" << scan_result.size() << std::endl;
  }

  // op 667: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 668: get
  status = db->Get(read_options, "key9800", &value);
  if (status.ok()) {
    out << "GET key9800 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 669: get
  status = db->Get(read_options, "key4355", &value);
  if (status.ok()) {
    out << "GET key4355 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 670: get
  status = db->Get(read_options, "key5852", &value);
  if (status.ok()) {
    out << "GET key5852 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 671: get
  status = db->Get(read_options, "key2024", &value);
  if (status.ok()) {
    out << "GET key2024 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 672: put
  db->Put(write_options, "key2786", "value110352");

  // op 673: put
  db->Put(write_options, "key6839", "value27066");

  // op 674: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0375", "key4011", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0375, key4011) size=" << scan_result.size() << std::endl;
  }

  // op 675: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1741", "key2478", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1741, key2478) size=" << scan_result.size() << std::endl;
  }

  // op 676: put
  db->Put(write_options, "key6267", "value20878");

  // op 677: get
  status = db->Get(read_options, "key5984", &value);
  if (status.ok()) {
    out << "GET key5984 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 678: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4521", "key9702", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4521, key9702) size=" << scan_result.size() << std::endl;
  }

  // op 679: put
  db->Put(write_options, "key8039", "value27419");

  // op 680: get
  status = db->Get(read_options, "key10599", &value);
  if (status.ok()) {
    out << "GET key10599 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 681: put
  db->Put(write_options, "key10625", "value66149");

  // op 682: get
  status = db->Get(read_options, "key3809", &value);
  if (status.ok()) {
    out << "GET key3809 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 683: delete
  db->Delete(write_options, "key10186");

  // op 684: get
  status = db->Get(read_options, "key2571", &value);
  if (status.ok()) {
    out << "GET key2571 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 685: get
  status = db->Get(read_options, "key11658", &value);
  if (status.ok()) {
    out << "GET key11658 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 686: delete_range
  status = db->DeleteRange(write_options, "key2617", "key8962");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 687: delete_range
  status = db->DeleteRange(write_options, "key0652", "key2067");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 688: get
  status = db->Get(read_options, "key11297", &value);
  if (status.ok()) {
    out << "GET key11297 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 689: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9856", "key11353", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9856, key11353) size=" << scan_result.size() << std::endl;
  }

  // op 690: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3027", "key3798", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3027, key3798) size=" << scan_result.size() << std::endl;
  }

  // op 691: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3994", "key9990", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3994, key9990) size=" << scan_result.size() << std::endl;
  }

  // op 692: delete_range
  status = db->DeleteRange(write_options, "key6441", "key8938");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 693: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7626", "key8132", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7626, key8132) size=" << scan_result.size() << std::endl;
  }

  // op 694: put
  db->Put(write_options, "key9910", "value45099");

  // op 695: put
  db->Put(write_options, "key1385", "value58546");

  // op 696: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9751", "key10001", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9751, key10001) size=" << scan_result.size() << std::endl;
  }

  // op 697: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1337", "key1699", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1337, key1699) size=" << scan_result.size() << std::endl;
  }

  // op 698: put
  db->Put(write_options, "key11497", "value17001");

  // op 699: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1813", "key9597", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1813, key9597) size=" << scan_result.size() << std::endl;
  }

  // op 700: put
  db->Put(write_options, "key2493", "value89555");

  // op 701: delete_range
  status = db->DeleteRange(write_options, "key0541", "key10377");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 702: put
  db->Put(write_options, "key11395", "value78226");

  // op 703: put
  db->Put(write_options, "key4246", "value56906");

  // op 704: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0385", "key7430", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0385, key7430) size=" << scan_result.size() << std::endl;
  }

  // op 705: get
  status = db->Get(read_options, "key6241", &value);
  if (status.ok()) {
    out << "GET key6241 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 706: delete_range
  status = db->DeleteRange(write_options, "key0568", "key8814");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 707: get
  status = db->Get(read_options, "key2258", &value);
  if (status.ok()) {
    out << "GET key2258 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 708: delete_range
  status = db->DeleteRange(write_options, "key4860", "key10865");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 709: put
  db->Put(write_options, "key11371", "value72883");

  // op 710: put
  db->Put(write_options, "key9858", "value32877");

  // op 711: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2866", "key4194", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2866, key4194) size=" << scan_result.size() << std::endl;
  }

  // op 712: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5799", "key8724", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5799, key8724) size=" << scan_result.size() << std::endl;
  }

  // op 713: put
  db->Put(write_options, "key0799", "value111619");

  // op 714: put
  db->Put(write_options, "key1421", "value46664");

  // op 715: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6088", "key10493", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6088, key10493) size=" << scan_result.size() << std::endl;
  }

  // op 716: put
  db->Put(write_options, "key10069", "value68528");

  // op 717: delete
  db->Delete(write_options, "key2410");

  // op 718: put
  db->Put(write_options, "key11189", "value35265");

  // op 719: get
  status = db->Get(read_options, "key4291", &value);
  if (status.ok()) {
    out << "GET key4291 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 720: get
  status = db->Get(read_options, "key10275", &value);
  if (status.ok()) {
    out << "GET key10275 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 721: put
  db->Put(write_options, "key9502", "value106686");

  // op 722: delete_range
  status = db->DeleteRange(write_options, "key0012", "key6928");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 723: get
  status = db->Get(read_options, "key0755", &value);
  if (status.ok()) {
    out << "GET key0755 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 724: put
  db->Put(write_options, "key6760", "value9414");

  // op 725: put
  db->Put(write_options, "key9512", "value116075");

  // op 726: put
  db->Put(write_options, "key11277", "value41371");

  // op 727: put
  db->Put(write_options, "key10701", "value40398");

  // op 728: put
  db->Put(write_options, "key2406", "value78220");

  // op 729: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3510", "key9850", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3510, key9850) size=" << scan_result.size() << std::endl;
  }

  // op 730: put
  db->Put(write_options, "key4365", "value76386");

  // op 731: get
  status = db->Get(read_options, "key7946", &value);
  if (status.ok()) {
    out << "GET key7946 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 732: put
  db->Put(write_options, "key8970", "value97384");

  // op 733: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10998", "key11528", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10998, key11528) size=" << scan_result.size() << std::endl;
  }

  // op 734: put
  db->Put(write_options, "key1930", "value85347");

  // op 735: get
  status = db->Get(read_options, "key11579", &value);
  if (status.ok()) {
    out << "GET key11579 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 736: put
  db->Put(write_options, "key4368", "value20677");

  // op 737: delete
  db->Delete(write_options, "key1534");

  // op 738: put
  db->Put(write_options, "key4290", "value11516");

  // op 739: put
  db->Put(write_options, "key1935", "value19427");

  // op 740: put
  db->Put(write_options, "key5998", "value47279");

  // op 741: put
  db->Put(write_options, "key3086", "value67250");

  // op 742: delete
  db->Delete(write_options, "key10842");

  // op 743: put
  db->Put(write_options, "key7626", "value65556");

  // op 744: get
  status = db->Get(read_options, "key11080", &value);
  if (status.ok()) {
    out << "GET key11080 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 745: delete_range
  status = db->DeleteRange(write_options, "key1555", "key2043");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 746: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 747: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 748: delete
  db->Delete(write_options, "key4099");

  // op 749: get
  status = db->Get(read_options, "key2424", &value);
  if (status.ok()) {
    out << "GET key2424 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 750: get
  status = db->Get(read_options, "key6160", &value);
  if (status.ok()) {
    out << "GET key6160 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 751: delete_range
  status = db->DeleteRange(write_options, "key2856", "key4468");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 752: get
  status = db->Get(read_options, "key6087", &value);
  if (status.ok()) {
    out << "GET key6087 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 753: get
  status = db->Get(read_options, "key3118", &value);
  if (status.ok()) {
    out << "GET key3118 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 754: put
  db->Put(write_options, "key8058", "value100106");

  // op 755: put
  db->Put(write_options, "key7507", "value66378");

  // op 756: put
  db->Put(write_options, "key0317", "value45986");

  // op 757: get
  status = db->Get(read_options, "key8128", &value);
  if (status.ok()) {
    out << "GET key8128 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 758: get
  status = db->Get(read_options, "key10617", &value);
  if (status.ok()) {
    out << "GET key10617 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 759: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7533", "key9673", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7533, key9673) size=" << scan_result.size() << std::endl;
  }

  // op 760: get
  status = db->Get(read_options, "key1731", &value);
  if (status.ok()) {
    out << "GET key1731 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 761: put
  db->Put(write_options, "key0361", "value115647");

  // op 762: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3424", "key9554", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3424, key9554) size=" << scan_result.size() << std::endl;
  }

  // op 763: delete
  db->Delete(write_options, "key8153");

  // op 764: get
  status = db->Get(read_options, "key8773", &value);
  if (status.ok()) {
    out << "GET key8773 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 765: get
  status = db->Get(read_options, "key8675", &value);
  if (status.ok()) {
    out << "GET key8675 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 766: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 767: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1196", "key8653", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1196, key8653) size=" << scan_result.size() << std::endl;
  }

  // op 768: delete_range
  status = db->DeleteRange(write_options, "key1039", "key2158");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 769: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1582", "key7111", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1582, key7111) size=" << scan_result.size() << std::endl;
  }

  // op 770: put
  db->Put(write_options, "key4514", "value27213");

  // op 771: get
  status = db->Get(read_options, "key1931", &value);
  if (status.ok()) {
    out << "GET key1931 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 772: get
  status = db->Get(read_options, "key3162", &value);
  if (status.ok()) {
    out << "GET key3162 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 773: put
  db->Put(write_options, "key5152", "value38554");

  // op 774: put
  db->Put(write_options, "key5489", "value19569");

  // op 775: get
  status = db->Get(read_options, "key4645", &value);
  if (status.ok()) {
    out << "GET key4645 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 776: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 777: delete_range
  status = db->DeleteRange(write_options, "key5717", "key10759");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 778: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6207", "key6486", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6207, key6486) size=" << scan_result.size() << std::endl;
  }

  // op 779: get
  status = db->Get(read_options, "key9844", &value);
  if (status.ok()) {
    out << "GET key9844 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 780: put
  db->Put(write_options, "key9097", "value54562");

  // op 781: delete_range
  status = db->DeleteRange(write_options, "key2395", "key8606");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 782: put
  db->Put(write_options, "key2522", "value47862");

  // op 783: put
  db->Put(write_options, "key0482", "value103627");

  // op 784: put
  db->Put(write_options, "key4101", "value4583");

  // op 785: get
  status = db->Get(read_options, "key6418", &value);
  if (status.ok()) {
    out << "GET key6418 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 786: put
  db->Put(write_options, "key1638", "value22240");

  // op 787: put
  db->Put(write_options, "key4873", "value107079");

  // op 788: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 789: put
  db->Put(write_options, "key0294", "value18627");

  // op 790: delete
  db->Delete(write_options, "key2966");

  // op 791: get
  status = db->Get(read_options, "key3841", &value);
  if (status.ok()) {
    out << "GET key3841 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 792: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7828", "key10803", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7828, key10803) size=" << scan_result.size() << std::endl;
  }

  // op 793: delete
  db->Delete(write_options, "key10487");

  // op 794: delete
  db->Delete(write_options, "key9359");

  // op 795: get
  status = db->Get(read_options, "key2904", &value);
  if (status.ok()) {
    out << "GET key2904 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 796: put
  db->Put(write_options, "key3519", "value109388");

  // op 797: put
  db->Put(write_options, "key9049", "value30070");

  // op 798: get
  status = db->Get(read_options, "key3755", &value);
  if (status.ok()) {
    out << "GET key3755 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 799: get
  status = db->Get(read_options, "key8675", &value);
  if (status.ok()) {
    out << "GET key8675 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 800: put
  db->Put(write_options, "key4936", "value6889");

  // op 801: put
  db->Put(write_options, "key6026", "value18699");

  // op 802: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0071", "key0115", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0071, key0115) size=" << scan_result.size() << std::endl;
  }

  // op 803: put
  db->Put(write_options, "key10181", "value114685");

  // op 804: get
  status = db->Get(read_options, "key3563", &value);
  if (status.ok()) {
    out << "GET key3563 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 805: put
  db->Put(write_options, "key8792", "value69396");

  // op 806: delete
  db->Delete(write_options, "key11155");

  // op 807: put
  db->Put(write_options, "key11470", "value0232");

  // op 808: put
  db->Put(write_options, "key8824", "value90877");

  // op 809: put
  db->Put(write_options, "key0705", "value66503");

  // op 810: put
  db->Put(write_options, "key9740", "value33415");

  // op 811: delete_range
  status = db->DeleteRange(write_options, "key1577", "key10487");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 812: delete
  db->Delete(write_options, "key8772");

  // op 813: put
  db->Put(write_options, "key8667", "value65417");

  // op 814: put
  db->Put(write_options, "key3099", "value109156");

  // op 815: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7412", "key11211", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7412, key11211) size=" << scan_result.size() << std::endl;
  }

  // op 816: put
  db->Put(write_options, "key11121", "value50301");

  // op 817: get
  status = db->Get(read_options, "key9289", &value);
  if (status.ok()) {
    out << "GET key9289 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 818: put
  db->Put(write_options, "key6506", "value95684");

  // op 819: put
  db->Put(write_options, "key2539", "value112586");

  // op 820: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6380", "key8360", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6380, key8360) size=" << scan_result.size() << std::endl;
  }

  // op 821: put
  db->Put(write_options, "key10076", "value73126");

  // op 822: get
  status = db->Get(read_options, "key5174", &value);
  if (status.ok()) {
    out << "GET key5174 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 823: put
  db->Put(write_options, "key10670", "value60151");

  // op 824: get
  status = db->Get(read_options, "key3271", &value);
  if (status.ok()) {
    out << "GET key3271 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 825: delete
  db->Delete(write_options, "key8951");

  // op 826: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1297", "key4552", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1297, key4552) size=" << scan_result.size() << std::endl;
  }

  // op 827: put
  db->Put(write_options, "key0329", "value56511");

  // op 828: delete_range
  status = db->DeleteRange(write_options, "key6315", "key11414");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 829: delete_range
  status = db->DeleteRange(write_options, "key3118", "key7805");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 830: get
  status = db->Get(read_options, "key0301", &value);
  if (status.ok()) {
    out << "GET key0301 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 831: delete_range
  status = db->DeleteRange(write_options, "key7538", "key10187");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 832: get
  status = db->Get(read_options, "key5330", &value);
  if (status.ok()) {
    out << "GET key5330 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 833: put
  db->Put(write_options, "key2268", "value47901");

  // op 834: get
  status = db->Get(read_options, "key0232", &value);
  if (status.ok()) {
    out << "GET key0232 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 835: put
  db->Put(write_options, "key7895", "value63325");

  // op 836: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5292", "key8483", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5292, key8483) size=" << scan_result.size() << std::endl;
  }

  // op 837: delete_range
  status = db->DeleteRange(write_options, "key8451", "key8818");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 838: put
  db->Put(write_options, "key2088", "value9891");

  // op 839: get
  status = db->Get(read_options, "key10955", &value);
  if (status.ok()) {
    out << "GET key10955 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 840: put
  db->Put(write_options, "key7321", "value19947");

  // op 841: delete_range
  status = db->DeleteRange(write_options, "key0726", "key7593");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 842: get
  status = db->Get(read_options, "key10541", &value);
  if (status.ok()) {
    out << "GET key10541 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 843: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6513", "key9969", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6513, key9969) size=" << scan_result.size() << std::endl;
  }

  // op 844: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5865", "key11532", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5865, key11532) size=" << scan_result.size() << std::endl;
  }

  // op 845: delete_range
  status = db->DeleteRange(write_options, "key1038", "key3243");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 846: put
  db->Put(write_options, "key8775", "value38464");

  // op 847: delete
  db->Delete(write_options, "key4794");

  // op 848: put
  db->Put(write_options, "key2974", "value117766");

  // op 849: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2611", "key8039", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2611, key8039) size=" << scan_result.size() << std::endl;
  }

  // op 850: delete
  db->Delete(write_options, "key3795");

  // op 851: delete_range
  status = db->DeleteRange(write_options, "key1981", "key10106");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 852: delete
  db->Delete(write_options, "key6800");

  // op 853: put
  db->Put(write_options, "key5942", "value24088");

  // op 854: put
  db->Put(write_options, "key1276", "value23358");

  // op 855: delete_range
  status = db->DeleteRange(write_options, "key3774", "key9698");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 856: get
  status = db->Get(read_options, "key10871", &value);
  if (status.ok()) {
    out << "GET key10871 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 857: delete_range
  status = db->DeleteRange(write_options, "key5230", "key5796");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 858: put
  db->Put(write_options, "key0508", "value60638");

  // op 859: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4939", "key5564", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4939, key5564) size=" << scan_result.size() << std::endl;
  }

  // op 860: put
  db->Put(write_options, "key7695", "value33950");

  // op 861: put
  db->Put(write_options, "key8198", "value0260");

  // op 862: get
  status = db->Get(read_options, "key5545", &value);
  if (status.ok()) {
    out << "GET key5545 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 863: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4784", "key9756", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4784, key9756) size=" << scan_result.size() << std::endl;
  }

  // op 864: get
  status = db->Get(read_options, "key5905", &value);
  if (status.ok()) {
    out << "GET key5905 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 865: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3645", "key11295", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3645, key11295) size=" << scan_result.size() << std::endl;
  }

  // op 866: put
  db->Put(write_options, "key10035", "value37662");

  // op 867: delete_range
  status = db->DeleteRange(write_options, "key2328", "key11306");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 868: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4713", "key10057", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4713, key10057) size=" << scan_result.size() << std::endl;
  }

  // op 869: get
  status = db->Get(read_options, "key3268", &value);
  if (status.ok()) {
    out << "GET key3268 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 870: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 871: put
  db->Put(write_options, "key10213", "value69938");

  // op 872: put
  db->Put(write_options, "key6898", "value64125");

  // op 873: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3001", "key9019", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3001, key9019) size=" << scan_result.size() << std::endl;
  }

  // op 874: delete
  db->Delete(write_options, "key1957");

  // op 875: put
  db->Put(write_options, "key8038", "value117109");

  // op 876: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 877: get
  status = db->Get(read_options, "key10147", &value);
  if (status.ok()) {
    out << "GET key10147 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 878: delete
  db->Delete(write_options, "key6614");

  // op 879: put
  db->Put(write_options, "key8634", "value95488");

  // op 880: put
  db->Put(write_options, "key11546", "value75389");

  // op 881: get
  status = db->Get(read_options, "key7285", &value);
  if (status.ok()) {
    out << "GET key7285 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 882: put
  db->Put(write_options, "key8995", "value16901");

  // op 883: put
  db->Put(write_options, "key8869", "value44579");

  // op 884: put
  db->Put(write_options, "key6314", "value44294");

  // op 885: put
  db->Put(write_options, "key7860", "value80977");

  // op 886: put
  db->Put(write_options, "key8829", "value16482");

  // op 887: get
  status = db->Get(read_options, "key8691", &value);
  if (status.ok()) {
    out << "GET key8691 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 888: put
  db->Put(write_options, "key9442", "value5899");

  // op 889: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0254", "key2561", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0254, key2561) size=" << scan_result.size() << std::endl;
  }

  // op 890: get
  status = db->Get(read_options, "key9898", &value);
  if (status.ok()) {
    out << "GET key9898 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 891: get
  status = db->Get(read_options, "key1939", &value);
  if (status.ok()) {
    out << "GET key1939 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 892: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3068", "key5057", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3068, key5057) size=" << scan_result.size() << std::endl;
  }

  // op 893: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1063", "key2244", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1063, key2244) size=" << scan_result.size() << std::endl;
  }

  // op 894: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2791", "key10820", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2791, key10820) size=" << scan_result.size() << std::endl;
  }

  // op 895: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1202", "key2004", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1202, key2004) size=" << scan_result.size() << std::endl;
  }

  // op 896: get
  status = db->Get(read_options, "key8772", &value);
  if (status.ok()) {
    out << "GET key8772 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 897: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8652", "key9400", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8652, key9400) size=" << scan_result.size() << std::endl;
  }

  // op 898: get
  status = db->Get(read_options, "key2670", &value);
  if (status.ok()) {
    out << "GET key2670 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 899: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6037", "key11689", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6037, key11689) size=" << scan_result.size() << std::endl;
  }

  // op 900: delete
  db->Delete(write_options, "key10680");

  // op 901: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8868", "key11617", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8868, key11617) size=" << scan_result.size() << std::endl;
  }

  // op 902: put
  db->Put(write_options, "key2776", "value2392");

  // op 903: put
  db->Put(write_options, "key2667", "value110371");

  // op 904: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5237", "key9525", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5237, key9525) size=" << scan_result.size() << std::endl;
  }

  // op 905: get
  status = db->Get(read_options, "key0118", &value);
  if (status.ok()) {
    out << "GET key0118 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 906: put
  db->Put(write_options, "key6395", "value64548");

  // op 907: get
  status = db->Get(read_options, "key0412", &value);
  if (status.ok()) {
    out << "GET key0412 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 908: get
  status = db->Get(read_options, "key8784", &value);
  if (status.ok()) {
    out << "GET key8784 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 909: put
  db->Put(write_options, "key4230", "value62576");

  // op 910: put
  db->Put(write_options, "key11431", "value103572");

  // op 911: put
  db->Put(write_options, "key11572", "value4359");

  // op 912: put
  db->Put(write_options, "key3576", "value72138");

  // op 913: put
  db->Put(write_options, "key10605", "value22222");

  // op 914: put
  db->Put(write_options, "key3421", "value70320");

  // op 915: put
  db->Put(write_options, "key11107", "value87134");

  // op 916: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6234", "key7315", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6234, key7315) size=" << scan_result.size() << std::endl;
  }

  // op 917: get
  status = db->Get(read_options, "key3801", &value);
  if (status.ok()) {
    out << "GET key3801 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 918: delete
  db->Delete(write_options, "key2386");

  // op 919: delete
  db->Delete(write_options, "key7086");

  // op 920: delete_range
  status = db->DeleteRange(write_options, "key0412", "key5281");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 921: delete_range
  status = db->DeleteRange(write_options, "key8521", "key9252");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 922: put
  db->Put(write_options, "key0593", "value7472");

  // op 923: put
  db->Put(write_options, "key1113", "value51313");

  // op 924: delete
  db->Delete(write_options, "key1159");

  // op 925: put
  db->Put(write_options, "key10261", "value46520");

  // op 926: get
  status = db->Get(read_options, "key7067", &value);
  if (status.ok()) {
    out << "GET key7067 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 927: put
  db->Put(write_options, "key3348", "value77337");

  // op 928: get
  status = db->Get(read_options, "key6676", &value);
  if (status.ok()) {
    out << "GET key6676 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 929: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1255", "key3526", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1255, key3526) size=" << scan_result.size() << std::endl;
  }

  // op 930: delete
  db->Delete(write_options, "key2214");

  // op 931: get
  status = db->Get(read_options, "key6998", &value);
  if (status.ok()) {
    out << "GET key6998 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 932: put
  db->Put(write_options, "key4360", "value40618");

  // op 933: put
  db->Put(write_options, "key5425", "value109749");

  // op 934: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4045", "key10556", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4045, key10556) size=" << scan_result.size() << std::endl;
  }

  // op 935: put
  db->Put(write_options, "key1724", "value82731");

  // op 936: delete
  db->Delete(write_options, "key8289");

  // op 937: put
  db->Put(write_options, "key4254", "value97228");

  // op 938: get
  status = db->Get(read_options, "key5831", &value);
  if (status.ok()) {
    out << "GET key5831 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 939: get
  status = db->Get(read_options, "key1508", &value);
  if (status.ok()) {
    out << "GET key1508 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 940: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 941: get
  status = db->Get(read_options, "key6546", &value);
  if (status.ok()) {
    out << "GET key6546 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 942: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 943: put
  db->Put(write_options, "key1976", "value79940");

  // op 944: put
  db->Put(write_options, "key4246", "value73796");

  // op 945: put
  db->Put(write_options, "key3422", "value14338");

  // op 946: delete
  db->Delete(write_options, "key9236");

  // op 947: put
  db->Put(write_options, "key0920", "value48300");

  // op 948: put
  db->Put(write_options, "key11183", "value73646");

  // op 949: get
  status = db->Get(read_options, "key0805", &value);
  if (status.ok()) {
    out << "GET key0805 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 950: get
  status = db->Get(read_options, "key6746", &value);
  if (status.ok()) {
    out << "GET key6746 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 951: delete
  db->Delete(write_options, "key2886");

  // op 952: delete_range
  status = db->DeleteRange(write_options, "key6026", "key10585");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 953: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6376", "key11295", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6376, key11295) size=" << scan_result.size() << std::endl;
  }

  // op 954: get
  status = db->Get(read_options, "key9824", &value);
  if (status.ok()) {
    out << "GET key9824 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 955: put
  db->Put(write_options, "key7095", "value95511");

  // op 956: get
  status = db->Get(read_options, "key7464", &value);
  if (status.ok()) {
    out << "GET key7464 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 957: put
  db->Put(write_options, "key7341", "value40833");

  // op 958: put
  db->Put(write_options, "key3774", "value12759");

  // op 959: put
  db->Put(write_options, "key2382", "value83641");

  // op 960: get
  status = db->Get(read_options, "key10890", &value);
  if (status.ok()) {
    out << "GET key10890 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 961: put
  db->Put(write_options, "key3680", "value12758");

  // op 962: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6696", "key8721", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6696, key8721) size=" << scan_result.size() << std::endl;
  }

  // op 963: delete_range
  status = db->DeleteRange(write_options, "key0436", "key0915");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 964: get
  status = db->Get(read_options, "key8372", &value);
  if (status.ok()) {
    out << "GET key8372 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 965: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5009", "key7769", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5009, key7769) size=" << scan_result.size() << std::endl;
  }

  // op 966: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0819", "key9502", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0819, key9502) size=" << scan_result.size() << std::endl;
  }

  // op 967: delete_range
  status = db->DeleteRange(write_options, "key3779", "key10127");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 968: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3121", "key9750", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3121, key9750) size=" << scan_result.size() << std::endl;
  }

  // op 969: delete
  db->Delete(write_options, "key8025");

  // op 970: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 971: delete
  db->Delete(write_options, "key2661");

  // op 972: put
  db->Put(write_options, "key0202", "value76187");

  // op 973: get
  status = db->Get(read_options, "key0275", &value);
  if (status.ok()) {
    out << "GET key0275 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 974: put
  db->Put(write_options, "key7782", "value39376");

  // op 975: put
  db->Put(write_options, "key7885", "value16843");

  // op 976: put
  db->Put(write_options, "key10374", "value102027");

  // op 977: put
  db->Put(write_options, "key6444", "value53110");

  // op 978: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7161", "key7519", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7161, key7519) size=" << scan_result.size() << std::endl;
  }

  // op 979: put
  db->Put(write_options, "key1074", "value64593");

  // op 980: put
  db->Put(write_options, "key5382", "value1730");

  // op 981: put
  db->Put(write_options, "key9780", "value69180");

  // op 982: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1204", "key5297", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1204, key5297) size=" << scan_result.size() << std::endl;
  }

  // op 983: put
  db->Put(write_options, "key3001", "value62891");

  // op 984: put
  db->Put(write_options, "key9843", "value28714");

  // op 985: delete_range
  status = db->DeleteRange(write_options, "key4798", "key6710");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 986: put
  db->Put(write_options, "key4959", "value27590");

  // op 987: get
  status = db->Get(read_options, "key1653", &value);
  if (status.ok()) {
    out << "GET key1653 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 988: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6265", "key10948", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6265, key10948) size=" << scan_result.size() << std::endl;
  }

  // op 989: delete_range
  status = db->DeleteRange(write_options, "key3957", "key10562");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 990: put
  db->Put(write_options, "key2562", "value54324");

  // op 991: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 992: delete
  db->Delete(write_options, "key7395");

  // op 993: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3813", "key8613", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3813, key8613) size=" << scan_result.size() << std::endl;
  }

  // op 994: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 995: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 996: get
  status = db->Get(read_options, "key3108", &value);
  if (status.ok()) {
    out << "GET key3108 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 997: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7564", "key7964", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7564, key7964) size=" << scan_result.size() << std::endl;
  }

  // op 998: get
  status = db->Get(read_options, "key6123", &value);
  if (status.ok()) {
    out << "GET key6123 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 999: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 1000: get
  status = db->Get(read_options, "key10552", &value);
  if (status.ok()) {
    out << "GET key10552 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1001: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4524", "key9902", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4524, key9902) size=" << scan_result.size() << std::endl;
  }

  // op 1002: delete_range
  status = db->DeleteRange(write_options, "key4611", "key8164");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 1003: delete_range
  status = db->DeleteRange(write_options, "key0954", "key2160");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 1004: delete
  db->Delete(write_options, "key1204");

  // op 1005: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 1006: delete_range
  status = db->DeleteRange(write_options, "key0274", "key3895");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 1007: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1643", "key6778", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1643, key6778) size=" << scan_result.size() << std::endl;
  }

  // op 1008: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2905", "key9752", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2905, key9752) size=" << scan_result.size() << std::endl;
  }

  // op 1009: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1545", "key9544", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1545, key9544) size=" << scan_result.size() << std::endl;
  }

  // op 1010: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5969", "key10916", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5969, key10916) size=" << scan_result.size() << std::endl;
  }

  // op 1011: delete_range
  status = db->DeleteRange(write_options, "key1178", "key4013");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 1012: delete
  db->Delete(write_options, "key1670");

  // op 1013: put
  db->Put(write_options, "key10868", "value82313");

  // op 1014: put
  db->Put(write_options, "key5099", "value89154");

  // op 1015: put
  db->Put(write_options, "key7045", "value1213");

  // op 1016: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5340", "key8312", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5340, key8312) size=" << scan_result.size() << std::endl;
  }

  // op 1017: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9168", "key9302", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9168, key9302) size=" << scan_result.size() << std::endl;
  }

  // op 1018: get
  status = db->Get(read_options, "key4121", &value);
  if (status.ok()) {
    out << "GET key4121 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1019: put
  db->Put(write_options, "key8013", "value36956");

  // op 1020: get
  status = db->Get(read_options, "key9987", &value);
  if (status.ok()) {
    out << "GET key9987 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1021: put
  db->Put(write_options, "key10488", "value44960");

  // op 1022: delete_range
  status = db->DeleteRange(write_options, "key1938", "key4391");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 1023: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 1024: get
  status = db->Get(read_options, "key1552", &value);
  if (status.ok()) {
    out << "GET key1552 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1025: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5730", "key9625", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5730, key9625) size=" << scan_result.size() << std::endl;
  }

  // op 1026: get
  status = db->Get(read_options, "key8996", &value);
  if (status.ok()) {
    out << "GET key8996 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1027: put
  db->Put(write_options, "key3102", "value73267");

  // op 1028: get
  status = db->Get(read_options, "key10234", &value);
  if (status.ok()) {
    out << "GET key10234 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1029: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2466", "key4016", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2466, key4016) size=" << scan_result.size() << std::endl;
  }

  // op 1030: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3882", "key4390", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3882, key4390) size=" << scan_result.size() << std::endl;
  }

  // op 1031: delete
  db->Delete(write_options, "key4436");

  // op 1032: delete_range
  status = db->DeleteRange(write_options, "key1199", "key4648");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 1033: delete_range
  status = db->DeleteRange(write_options, "key2180", "key6084");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 1034: put
  db->Put(write_options, "key3371", "value15837");

  // op 1035: delete
  db->Delete(write_options, "key9950");

  // op 1036: get
  status = db->Get(read_options, "key3497", &value);
  if (status.ok()) {
    out << "GET key3497 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1037: put
  db->Put(write_options, "key9110", "value38561");

  // op 1038: put
  db->Put(write_options, "key10539", "value82402");

  // op 1039: delete_range
  status = db->DeleteRange(write_options, "key1568", "key6017");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 1040: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 1041: put
  db->Put(write_options, "key0512", "value70967");

  // op 1042: put
  db->Put(write_options, "key3158", "value10363");

  // op 1043: put
  db->Put(write_options, "key5202", "value3882");

  // op 1044: get
  status = db->Get(read_options, "key1618", &value);
  if (status.ok()) {
    out << "GET key1618 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1045: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10878", "key11666", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10878, key11666) size=" << scan_result.size() << std::endl;
  }

  // op 1046: get
  status = db->Get(read_options, "key8890", &value);
  if (status.ok()) {
    out << "GET key8890 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1047: delete
  db->Delete(write_options, "key0059");

  // op 1048: delete
  db->Delete(write_options, "key3694");

  // op 1049: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2665", "key6308", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2665, key6308) size=" << scan_result.size() << std::endl;
  }

  // op 1050: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5865", "key6195", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5865, key6195) size=" << scan_result.size() << std::endl;
  }

  // op 1051: put
  db->Put(write_options, "key0419", "value61893");

  // op 1052: put
  db->Put(write_options, "key2500", "value37786");

  // op 1053: put
  db->Put(write_options, "key10587", "value4799");

  // op 1054: get
  status = db->Get(read_options, "key7366", &value);
  if (status.ok()) {
    out << "GET key7366 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1055: put
  db->Put(write_options, "key1144", "value107786");

  // op 1056: put
  db->Put(write_options, "key0392", "value44548");

  // op 1057: get
  status = db->Get(read_options, "key0161", &value);
  if (status.ok()) {
    out << "GET key0161 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1058: put
  db->Put(write_options, "key9437", "value40170");

  // op 1059: get
  status = db->Get(read_options, "key6334", &value);
  if (status.ok()) {
    out << "GET key6334 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1060: put
  db->Put(write_options, "key1732", "value87842");

  // op 1061: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 1062: get
  status = db->Get(read_options, "key6395", &value);
  if (status.ok()) {
    out << "GET key6395 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1063: delete_range
  status = db->DeleteRange(write_options, "key3091", "key4408");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 1064: get
  status = db->Get(read_options, "key10544", &value);
  if (status.ok()) {
    out << "GET key10544 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1065: put
  db->Put(write_options, "key7771", "value12742");

  // op 1066: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 1067: put
  db->Put(write_options, "key1554", "value47730");

  // op 1068: get
  status = db->Get(read_options, "key9128", &value);
  if (status.ok()) {
    out << "GET key9128 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1069: delete
  db->Delete(write_options, "key7840");

  // op 1070: put
  db->Put(write_options, "key5986", "value59404");

  // op 1071: get
  status = db->Get(read_options, "key11406", &value);
  if (status.ok()) {
    out << "GET key11406 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1072: put
  db->Put(write_options, "key2746", "value95781");

  // op 1073: get
  status = db->Get(read_options, "key8272", &value);
  if (status.ok()) {
    out << "GET key8272 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1074: put
  db->Put(write_options, "key9672", "value84987");

  // op 1075: put
  db->Put(write_options, "key2750", "value62959");

  // op 1076: put
  db->Put(write_options, "key1778", "value73049");

  // op 1077: delete
  db->Delete(write_options, "key5059");

  // op 1078: put
  db->Put(write_options, "key3516", "value98498");

  // op 1079: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4591", "key11441", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4591, key11441) size=" << scan_result.size() << std::endl;
  }

  // op 1080: put
  db->Put(write_options, "key5589", "value63579");

  // op 1081: delete_range
  status = db->DeleteRange(write_options, "key5955", "key7617");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 1082: get
  status = db->Get(read_options, "key5818", &value);
  if (status.ok()) {
    out << "GET key5818 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1083: get
  status = db->Get(read_options, "key9379", &value);
  if (status.ok()) {
    out << "GET key9379 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1084: put
  db->Put(write_options, "key6566", "value76406");

  // op 1085: put
  db->Put(write_options, "key1781", "value30907");

  // op 1086: delete_range
  status = db->DeleteRange(write_options, "key4848", "key6252");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 1087: put
  db->Put(write_options, "key5228", "value101927");

  // op 1088: get
  status = db->Get(read_options, "key10075", &value);
  if (status.ok()) {
    out << "GET key10075 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1089: put
  db->Put(write_options, "key3411", "value73515");

  // op 1090: put
  db->Put(write_options, "key4123", "value63243");

  // op 1091: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 1092: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6587", "key8243", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6587, key8243) size=" << scan_result.size() << std::endl;
  }

  // op 1093: put
  db->Put(write_options, "key9694", "value21274");

  // op 1094: get
  status = db->Get(read_options, "key3051", &value);
  if (status.ok()) {
    out << "GET key3051 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1095: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0763", "key8665", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0763, key8665) size=" << scan_result.size() << std::endl;
  }

  // op 1096: delete_range
  status = db->DeleteRange(write_options, "key6782", "key10457");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 1097: get
  status = db->Get(read_options, "key0602", &value);
  if (status.ok()) {
    out << "GET key0602 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1098: get
  status = db->Get(read_options, "key4349", &value);
  if (status.ok()) {
    out << "GET key4349 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1099: delete_range
  status = db->DeleteRange(write_options, "key3688", "key4352");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 1100: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0367", "key0972", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0367, key0972) size=" << scan_result.size() << std::endl;
  }

  // op 1101: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5117", "key8802", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5117, key8802) size=" << scan_result.size() << std::endl;
  }

  // op 1102: delete_range
  status = db->DeleteRange(write_options, "key6624", "key10457");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 1103: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2489", "key9943", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2489, key9943) size=" << scan_result.size() << std::endl;
  }

  // op 1104: delete
  db->Delete(write_options, "key11362");

  // op 1105: put
  db->Put(write_options, "key5187", "value32550");

  // op 1106: put
  db->Put(write_options, "key11099", "value25429");

  // op 1107: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 1108: get
  status = db->Get(read_options, "key1117", &value);
  if (status.ok()) {
    out << "GET key1117 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1109: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1688", "key5869", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1688, key5869) size=" << scan_result.size() << std::endl;
  }

  // op 1110: delete
  db->Delete(write_options, "key1219");

  // op 1111: put
  db->Put(write_options, "key1499", "value72228");

  // op 1112: put
  db->Put(write_options, "key3518", "value56322");

  // op 1113: get
  status = db->Get(read_options, "key10836", &value);
  if (status.ok()) {
    out << "GET key10836 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1114: delete
  db->Delete(write_options, "key3217");

  // op 1115: put
  db->Put(write_options, "key7585", "value11685");

  // op 1116: put
  db->Put(write_options, "key5011", "value47591");

  // op 1117: put
  db->Put(write_options, "key2479", "value88505");

  // op 1118: put
  db->Put(write_options, "key0908", "value110926");

  // op 1119: put
  db->Put(write_options, "key2577", "value84334");

  // op 1120: get
  status = db->Get(read_options, "key4408", &value);
  if (status.ok()) {
    out << "GET key4408 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1121: put
  db->Put(write_options, "key10039", "value60266");

  // op 1122: put
  db->Put(write_options, "key5568", "value28076");

  // op 1123: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 1124: put
  db->Put(write_options, "key4806", "value90646");

  // op 1125: delete
  db->Delete(write_options, "key7505");

  // op 1126: delete
  db->Delete(write_options, "key2358");

  // op 1127: put
  db->Put(write_options, "key8627", "value4843");

  // op 1128: put
  db->Put(write_options, "key11303", "value67344");

  // op 1129: get
  status = db->Get(read_options, "key3389", &value);
  if (status.ok()) {
    out << "GET key3389 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1130: delete_range
  status = db->DeleteRange(write_options, "key4476", "key5587");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 1131: put
  db->Put(write_options, "key1235", "value37777");

  // op 1132: get
  status = db->Get(read_options, "key3449", &value);
  if (status.ok()) {
    out << "GET key3449 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1133: delete_range
  status = db->DeleteRange(write_options, "key0541", "key6399");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 1134: put
  db->Put(write_options, "key9674", "value78189");

  // op 1135: put
  db->Put(write_options, "key11643", "value86947");

  // op 1136: put
  db->Put(write_options, "key0208", "value104339");

  // op 1137: put
  db->Put(write_options, "key3604", "value117647");

  // op 1138: put
  db->Put(write_options, "key4480", "value27934");

  // op 1139: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 1140: delete
  db->Delete(write_options, "key9199");

  // op 1141: put
  db->Put(write_options, "key10312", "value35324");

  // op 1142: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 1143: get
  status = db->Get(read_options, "key4760", &value);
  if (status.ok()) {
    out << "GET key4760 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1144: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3222", "key9017", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3222, key9017) size=" << scan_result.size() << std::endl;
  }

  // op 1145: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2940", "key4661", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2940, key4661) size=" << scan_result.size() << std::endl;
  }

  // op 1146: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4617", "key5230", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4617, key5230) size=" << scan_result.size() << std::endl;
  }

  // op 1147: delete_range
  status = db->DeleteRange(write_options, "key5414", "key11687");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 1148: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8590", "key11275", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8590, key11275) size=" << scan_result.size() << std::endl;
  }

  // op 1149: delete
  db->Delete(write_options, "key6095");

  // op 1150: put
  db->Put(write_options, "key3633", "value7623");

  // op 1151: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 1152: put
  db->Put(write_options, "key7057", "value44389");

  // op 1153: put
  db->Put(write_options, "key9478", "value112471");

  // op 1154: put
  db->Put(write_options, "key5756", "value5565");

  // op 1155: put
  db->Put(write_options, "key10610", "value86799");

  // op 1156: delete
  db->Delete(write_options, "key3992");

  // op 1157: put
  db->Put(write_options, "key3341", "value66173");

  // op 1158: put
  db->Put(write_options, "key6830", "value73431");

  // op 1159: get
  status = db->Get(read_options, "key4022", &value);
  if (status.ok()) {
    out << "GET key4022 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1160: put
  db->Put(write_options, "key2451", "value88904");

  // op 1161: put
  db->Put(write_options, "key6042", "value25555");

  // op 1162: get
  status = db->Get(read_options, "key10670", &value);
  if (status.ok()) {
    out << "GET key10670 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1163: put
  db->Put(write_options, "key9484", "value13621");

  // op 1164: put
  db->Put(write_options, "key1814", "value43494");

  // op 1165: delete
  db->Delete(write_options, "key7428");

  // op 1166: get
  status = db->Get(read_options, "key10890", &value);
  if (status.ok()) {
    out << "GET key10890 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1167: put
  db->Put(write_options, "key5048", "value19984");

  // op 1168: put
  db->Put(write_options, "key7612", "value4109");

  // op 1169: get
  status = db->Get(read_options, "key5260", &value);
  if (status.ok()) {
    out << "GET key5260 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1170: get
  status = db->Get(read_options, "key11716", &value);
  if (status.ok()) {
    out << "GET key11716 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1171: put
  db->Put(write_options, "key8233", "value56503");

  // op 1172: put
  db->Put(write_options, "key8063", "value52151");

  // op 1173: put
  db->Put(write_options, "key7236", "value110080");

  // op 1174: get
  status = db->Get(read_options, "key4229", &value);
  if (status.ok()) {
    out << "GET key4229 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1175: put
  db->Put(write_options, "key11431", "value69763");

  // op 1176: get
  status = db->Get(read_options, "key3541", &value);
  if (status.ok()) {
    out << "GET key3541 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1177: put
  db->Put(write_options, "key1025", "value24659");

  // op 1178: put
  db->Put(write_options, "key5627", "value33770");

  // op 1179: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 1180: get
  status = db->Get(read_options, "key7944", &value);
  if (status.ok()) {
    out << "GET key7944 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1181: put
  db->Put(write_options, "key9254", "value117706");

  // op 1182: put
  db->Put(write_options, "key4157", "value19723");

  // op 1183: put
  db->Put(write_options, "key4839", "value38271");

  // op 1184: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7197", "key9987", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7197, key9987) size=" << scan_result.size() << std::endl;
  }

  // op 1185: get
  status = db->Get(read_options, "key3034", &value);
  if (status.ok()) {
    out << "GET key3034 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1186: put
  db->Put(write_options, "key0903", "value109791");

  // op 1187: put
  db->Put(write_options, "key6771", "value114832");

  // op 1188: put
  db->Put(write_options, "key1644", "value56692");

  // op 1189: delete_range
  status = db->DeleteRange(write_options, "key3926", "key8659");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 1190: put
  db->Put(write_options, "key4083", "value54119");

  // op 1191: put
  db->Put(write_options, "key1670", "value59381");

  // op 1192: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2768", "key5841", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2768, key5841) size=" << scan_result.size() << std::endl;
  }

  // op 1193: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0462", "key0668", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0462, key0668) size=" << scan_result.size() << std::endl;
  }

  // op 1194: put
  db->Put(write_options, "key9656", "value90751");

  // op 1195: put
  db->Put(write_options, "key4597", "value79145");

  // op 1196: get
  status = db->Get(read_options, "key3701", &value);
  if (status.ok()) {
    out << "GET key3701 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1197: delete_range
  status = db->DeleteRange(write_options, "key4916", "key10454");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 1198: delete
  db->Delete(write_options, "key4868");

  // op 1199: get
  status = db->Get(read_options, "key8559", &value);
  if (status.ok()) {
    out << "GET key8559 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1200: put
  db->Put(write_options, "key2848", "value52539");

  // op 1201: put
  db->Put(write_options, "key7717", "value56530");

  // op 1202: put
  db->Put(write_options, "key5157", "value66077");

  // op 1203: put
  db->Put(write_options, "key8557", "value99589");

  // op 1204: get
  status = db->Get(read_options, "key3680", &value);
  if (status.ok()) {
    out << "GET key3680 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1205: get
  status = db->Get(read_options, "key3789", &value);
  if (status.ok()) {
    out << "GET key3789 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1206: put
  db->Put(write_options, "key6341", "value98142");

  // op 1207: put
  db->Put(write_options, "key11489", "value65247");

  // op 1208: put
  db->Put(write_options, "key2666", "value105576");

  // op 1209: put
  db->Put(write_options, "key2759", "value112633");

  // op 1210: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9154", "key10383", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9154, key10383) size=" << scan_result.size() << std::endl;
  }

  // op 1211: put
  db->Put(write_options, "key8949", "value11987");

  // op 1212: delete_range
  status = db->DeleteRange(write_options, "key6133", "key6259");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 1213: get
  status = db->Get(read_options, "key2804", &value);
  if (status.ok()) {
    out << "GET key2804 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1214: put
  db->Put(write_options, "key11185", "value63096");

  // op 1215: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5814", "key6168", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5814, key6168) size=" << scan_result.size() << std::endl;
  }

  // op 1216: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4158", "key7435", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4158, key7435) size=" << scan_result.size() << std::endl;
  }

  // op 1217: get
  status = db->Get(read_options, "key4168", &value);
  if (status.ok()) {
    out << "GET key4168 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1218: put
  db->Put(write_options, "key0986", "value69646");

  // op 1219: put
  db->Put(write_options, "key1808", "value5581");

  // op 1220: put
  db->Put(write_options, "key7225", "value66850");

  // op 1221: delete
  db->Delete(write_options, "key8980");

  // op 1222: put
  db->Put(write_options, "key0773", "value73763");

  // op 1223: get
  status = db->Get(read_options, "key2978", &value);
  if (status.ok()) {
    out << "GET key2978 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1224: delete_range
  status = db->DeleteRange(write_options, "key6037", "key11005");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 1225: put
  db->Put(write_options, "key1430", "value23750");

  // op 1226: get
  status = db->Get(read_options, "key9359", &value);
  if (status.ok()) {
    out << "GET key9359 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1227: delete
  db->Delete(write_options, "key1418");

  // op 1228: delete_range
  status = db->DeleteRange(write_options, "key9072", "key11056");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 1229: put
  db->Put(write_options, "key3781", "value23520");

  // op 1230: put
  db->Put(write_options, "key0345", "value113797");

  // op 1231: put
  db->Put(write_options, "key9543", "value113756");

  // op 1232: delete
  db->Delete(write_options, "key6342");

  // op 1233: get
  status = db->Get(read_options, "key4769", &value);
  if (status.ok()) {
    out << "GET key4769 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1234: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2435", "key6963", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2435, key6963) size=" << scan_result.size() << std::endl;
  }

  // op 1235: put
  db->Put(write_options, "key3506", "value53665");

  // op 1236: get
  status = db->Get(read_options, "key11524", &value);
  if (status.ok()) {
    out << "GET key11524 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1237: put
  db->Put(write_options, "key0006", "value72110");

  // op 1238: delete_range
  status = db->DeleteRange(write_options, "key1906", "key7035");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 1239: put
  db->Put(write_options, "key9386", "value91695");

  // op 1240: put
  db->Put(write_options, "key1794", "value26748");

  // op 1241: delete_range
  status = db->DeleteRange(write_options, "key2186", "key6305");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 1242: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0017", "key0420", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0017, key0420) size=" << scan_result.size() << std::endl;
  }

  // op 1243: put
  db->Put(write_options, "key4350", "value32640");

  // op 1244: put
  db->Put(write_options, "key8621", "value56687");

  // op 1245: get
  status = db->Get(read_options, "key8207", &value);
  if (status.ok()) {
    out << "GET key8207 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1246: put
  db->Put(write_options, "key1312", "value59747");

  // op 1247: put
  db->Put(write_options, "key0239", "value19608");

  // op 1248: put
  db->Put(write_options, "key4331", "value70080");

  // op 1249: put
  db->Put(write_options, "key2939", "value82787");

  // op 1250: delete
  db->Delete(write_options, "key8805");

  // op 1251: put
  db->Put(write_options, "key8885", "value96442");

  // op 1252: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6280", "key9709", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6280, key9709) size=" << scan_result.size() << std::endl;
  }

  // op 1253: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0398", "key0744", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0398, key0744) size=" << scan_result.size() << std::endl;
  }

  // op 1254: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0154", "key1237", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0154, key1237) size=" << scan_result.size() << std::endl;
  }

  // op 1255: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1544", "key2695", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1544, key2695) size=" << scan_result.size() << std::endl;
  }

  // op 1256: delete_range
  status = db->DeleteRange(write_options, "key0959", "key6167");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 1257: put
  db->Put(write_options, "key3648", "value8116");

  // op 1258: get
  status = db->Get(read_options, "key9155", &value);
  if (status.ok()) {
    out << "GET key9155 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1259: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0255", "key9897", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0255, key9897) size=" << scan_result.size() << std::endl;
  }

  // op 1260: get
  status = db->Get(read_options, "key3152", &value);
  if (status.ok()) {
    out << "GET key3152 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1261: put
  db->Put(write_options, "key0988", "value105325");

  // op 1262: put
  db->Put(write_options, "key8609", "value38188");

  // op 1263: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2995", "key6367", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2995, key6367) size=" << scan_result.size() << std::endl;
  }

  // op 1264: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2294", "key11678", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2294, key11678) size=" << scan_result.size() << std::endl;
  }

  // op 1265: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5523", "key8749", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5523, key8749) size=" << scan_result.size() << std::endl;
  }

  // op 1266: put
  db->Put(write_options, "key7586", "value32215");

  // op 1267: get
  status = db->Get(read_options, "key10742", &value);
  if (status.ok()) {
    out << "GET key10742 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1268: delete
  db->Delete(write_options, "key11447");

  // op 1269: put
  db->Put(write_options, "key1155", "value47816");

  // op 1270: put
  db->Put(write_options, "key10769", "value55512");

  // op 1271: put
  db->Put(write_options, "key5309", "value68856");

  // op 1272: delete_range
  status = db->DeleteRange(write_options, "key2245", "key10719");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 1273: get
  status = db->Get(read_options, "key4540", &value);
  if (status.ok()) {
    out << "GET key4540 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1274: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8155", "key9021", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8155, key9021) size=" << scan_result.size() << std::endl;
  }

  // op 1275: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5580", "key7845", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5580, key7845) size=" << scan_result.size() << std::endl;
  }

  // op 1276: get
  status = db->Get(read_options, "key0089", &value);
  if (status.ok()) {
    out << "GET key0089 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1277: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 1278: put
  db->Put(write_options, "key7992", "value64893");

  // op 1279: get
  status = db->Get(read_options, "key9816", &value);
  if (status.ok()) {
    out << "GET key9816 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1280: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1784", "key11046", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1784, key11046) size=" << scan_result.size() << std::endl;
  }

  // op 1281: put
  db->Put(write_options, "key3188", "value51449");

  // op 1282: get
  status = db->Get(read_options, "key8201", &value);
  if (status.ok()) {
    out << "GET key8201 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1283: delete
  db->Delete(write_options, "key2862");

  // op 1284: put
  db->Put(write_options, "key2564", "value0314");

  // op 1285: delete
  db->Delete(write_options, "key5264");

  // op 1286: put
  db->Put(write_options, "key5096", "value74487");

  // op 1287: get
  status = db->Get(read_options, "key0668", &value);
  if (status.ok()) {
    out << "GET key0668 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1288: get
  status = db->Get(read_options, "key10812", &value);
  if (status.ok()) {
    out << "GET key10812 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1289: get
  status = db->Get(read_options, "key6295", &value);
  if (status.ok()) {
    out << "GET key6295 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1290: put
  db->Put(write_options, "key10113", "value92433");

  // op 1291: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4232", "key7929", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4232, key7929) size=" << scan_result.size() << std::endl;
  }

  // op 1292: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0838", "key1792", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0838, key1792) size=" << scan_result.size() << std::endl;
  }

  // op 1293: put
  db->Put(write_options, "key3378", "value33708");

  // op 1294: get
  status = db->Get(read_options, "key4568", &value);
  if (status.ok()) {
    out << "GET key4568 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1295: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 1296: delete
  db->Delete(write_options, "key4622");

  // op 1297: get
  status = db->Get(read_options, "key1835", &value);
  if (status.ok()) {
    out << "GET key1835 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1298: put
  db->Put(write_options, "key4893", "value114389");

  // op 1299: delete
  db->Delete(write_options, "key9093");

  // op 1300: get
  status = db->Get(read_options, "key0822", &value);
  if (status.ok()) {
    out << "GET key0822 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1301: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10778", "key10837", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10778, key10837) size=" << scan_result.size() << std::endl;
  }

  // op 1302: get
  status = db->Get(read_options, "key7202", &value);
  if (status.ok()) {
    out << "GET key7202 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1303: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3966", "key10626", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3966, key10626) size=" << scan_result.size() << std::endl;
  }

  // op 1304: put
  db->Put(write_options, "key9785", "value28994");

  // op 1305: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5851", "key9795", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5851, key9795) size=" << scan_result.size() << std::endl;
  }

  // op 1306: put
  db->Put(write_options, "key4841", "value34279");

  // op 1307: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3924", "key9244", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3924, key9244) size=" << scan_result.size() << std::endl;
  }

  // op 1308: get
  status = db->Get(read_options, "key0651", &value);
  if (status.ok()) {
    out << "GET key0651 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1309: delete
  db->Delete(write_options, "key6846");

  // op 1310: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5733", "key9009", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5733, key9009) size=" << scan_result.size() << std::endl;
  }

  // op 1311: put
  db->Put(write_options, "key2488", "value31274");

  // op 1312: put
  db->Put(write_options, "key5205", "value94858");

  // op 1313: get
  status = db->Get(read_options, "key2268", &value);
  if (status.ok()) {
    out << "GET key2268 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1314: delete_range
  status = db->DeleteRange(write_options, "key7013", "key11262");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 1315: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 1316: put
  db->Put(write_options, "key2342", "value54839");

  // op 1317: delete
  db->Delete(write_options, "key10916");

  // op 1318: put
  db->Put(write_options, "key0232", "value14599");

  // op 1319: put
  db->Put(write_options, "key4511", "value52584");

  // op 1320: get
  status = db->Get(read_options, "key10195", &value);
  if (status.ok()) {
    out << "GET key10195 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1321: get
  status = db->Get(read_options, "key9389", &value);
  if (status.ok()) {
    out << "GET key9389 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1322: delete
  db->Delete(write_options, "key7023");

  // op 1323: put
  db->Put(write_options, "key7896", "value48544");

  // op 1324: put
  db->Put(write_options, "key8966", "value87426");

  // op 1325: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5517", "key5853", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5517, key5853) size=" << scan_result.size() << std::endl;
  }

  // op 1326: delete
  db->Delete(write_options, "key9076");

  // op 1327: put
  db->Put(write_options, "key1907", "value26686");

  // op 1328: delete_range
  status = db->DeleteRange(write_options, "key9107", "key11236");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 1329: put
  db->Put(write_options, "key2721", "value69348");

  // op 1330: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6689", "key7263", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6689, key7263) size=" << scan_result.size() << std::endl;
  }

  // op 1331: delete
  db->Delete(write_options, "key5483");

  // op 1332: put
  db->Put(write_options, "key6718", "value92664");

  // op 1333: get
  status = db->Get(read_options, "key6985", &value);
  if (status.ok()) {
    out << "GET key6985 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1334: put
  db->Put(write_options, "key2610", "value82749");

  // op 1335: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3278", "key4245", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3278, key4245) size=" << scan_result.size() << std::endl;
  }

  // op 1336: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1947", "key2083", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1947, key2083) size=" << scan_result.size() << std::endl;
  }

  // op 1337: delete
  db->Delete(write_options, "key4897");

  // op 1338: get
  status = db->Get(read_options, "key4956", &value);
  if (status.ok()) {
    out << "GET key4956 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1339: delete
  db->Delete(write_options, "key5825");

  // op 1340: get
  status = db->Get(read_options, "key5121", &value);
  if (status.ok()) {
    out << "GET key5121 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1341: put
  db->Put(write_options, "key5614", "value33513");

  // op 1342: get
  status = db->Get(read_options, "key5759", &value);
  if (status.ok()) {
    out << "GET key5759 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1343: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 1344: put
  db->Put(write_options, "key6036", "value50406");

  // op 1345: get
  status = db->Get(read_options, "key3269", &value);
  if (status.ok()) {
    out << "GET key3269 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1346: put
  db->Put(write_options, "key0951", "value48040");

  // op 1347: delete_range
  status = db->DeleteRange(write_options, "key5027", "key6182");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 1348: put
  db->Put(write_options, "key4111", "value96782");

  // op 1349: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0416", "key9729", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0416, key9729) size=" << scan_result.size() << std::endl;
  }

  // op 1350: get
  status = db->Get(read_options, "key9942", &value);
  if (status.ok()) {
    out << "GET key9942 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1351: put
  db->Put(write_options, "key4582", "value66028");

  // op 1352: put
  db->Put(write_options, "key1101", "value19628");

  // op 1353: delete
  db->Delete(write_options, "key6134");

  // op 1354: put
  db->Put(write_options, "key6177", "value56646");

  // op 1355: delete
  db->Delete(write_options, "key10807");

  // op 1356: get
  status = db->Get(read_options, "key9579", &value);
  if (status.ok()) {
    out << "GET key9579 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1357: get
  status = db->Get(read_options, "key3015", &value);
  if (status.ok()) {
    out << "GET key3015 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1358: put
  db->Put(write_options, "key5575", "value96930");

  // op 1359: delete_range
  status = db->DeleteRange(write_options, "key1279", "key2650");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 1360: delete
  db->Delete(write_options, "key0841");

  // op 1361: get
  status = db->Get(read_options, "key6839", &value);
  if (status.ok()) {
    out << "GET key6839 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1362: put
  db->Put(write_options, "key11548", "value116137");

  // op 1363: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2007", "key4576", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2007, key4576) size=" << scan_result.size() << std::endl;
  }

  // op 1364: delete
  db->Delete(write_options, "key3267");

  // op 1365: put
  db->Put(write_options, "key1303", "value84538");

  // op 1366: put
  db->Put(write_options, "key4891", "value102337");

  // op 1367: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2160", "key6027", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2160, key6027) size=" << scan_result.size() << std::endl;
  }

  // op 1368: put
  db->Put(write_options, "key11679", "value5543");

  // op 1369: get
  status = db->Get(read_options, "key10646", &value);
  if (status.ok()) {
    out << "GET key10646 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1370: get
  status = db->Get(read_options, "key6327", &value);
  if (status.ok()) {
    out << "GET key6327 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1371: delete_range
  status = db->DeleteRange(write_options, "key2500", "key3996");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 1372: put
  db->Put(write_options, "key4804", "value111263");

  // op 1373: delete_range
  status = db->DeleteRange(write_options, "key11590", "key11686");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 1374: put
  db->Put(write_options, "key8204", "value75125");

  // op 1375: put
  db->Put(write_options, "key10488", "value57397");

  // op 1376: delete
  db->Delete(write_options, "key0390");

  // op 1377: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1972", "key2680", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1972, key2680) size=" << scan_result.size() << std::endl;
  }

  // op 1378: put
  db->Put(write_options, "key11458", "value66549");

  // op 1379: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0926", "key6400", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0926, key6400) size=" << scan_result.size() << std::endl;
  }

  // op 1380: put
  db->Put(write_options, "key0063", "value96899");

  // op 1381: get
  status = db->Get(read_options, "key8376", &value);
  if (status.ok()) {
    out << "GET key8376 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1382: put
  db->Put(write_options, "key5031", "value50939");

  // op 1383: put
  db->Put(write_options, "key7055", "value113545");

  // op 1384: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6383", "key9214", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6383, key9214) size=" << scan_result.size() << std::endl;
  }

  // op 1385: delete
  db->Delete(write_options, "key3916");

  // op 1386: put
  db->Put(write_options, "key6745", "value75283");

  // op 1387: delete
  db->Delete(write_options, "key6276");

  // op 1388: put
  db->Put(write_options, "key4670", "value80442");

  // op 1389: delete_range
  status = db->DeleteRange(write_options, "key0557", "key5525");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 1390: put
  db->Put(write_options, "key1825", "value91498");

  // op 1391: put
  db->Put(write_options, "key6616", "value52484");

  // op 1392: put
  db->Put(write_options, "key6055", "value63590");

  // op 1393: delete
  db->Delete(write_options, "key1815");

  // op 1394: put
  db->Put(write_options, "key5083", "value66470");

  // op 1395: delete
  db->Delete(write_options, "key7048");

  // op 1396: get
  status = db->Get(read_options, "key9817", &value);
  if (status.ok()) {
    out << "GET key9817 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1397: put
  db->Put(write_options, "key8177", "value80686");

  // op 1398: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4389", "key5520", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4389, key5520) size=" << scan_result.size() << std::endl;
  }

  // op 1399: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0157", "key5973", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0157, key5973) size=" << scan_result.size() << std::endl;
  }

  // op 1400: put
  db->Put(write_options, "key5363", "value4897");

  // op 1401: get
  status = db->Get(read_options, "key6278", &value);
  if (status.ok()) {
    out << "GET key6278 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1402: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4027", "key6554", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4027, key6554) size=" << scan_result.size() << std::endl;
  }

  // op 1403: put
  db->Put(write_options, "key1478", "value99611");

  // op 1404: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7415", "key8549", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7415, key8549) size=" << scan_result.size() << std::endl;
  }

  // op 1405: put
  db->Put(write_options, "key0040", "value10154");

  // op 1406: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2963", "key7050", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2963, key7050) size=" << scan_result.size() << std::endl;
  }

  // op 1407: get
  status = db->Get(read_options, "key7153", &value);
  if (status.ok()) {
    out << "GET key7153 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1408: put
  db->Put(write_options, "key11078", "value74627");

  // op 1409: put
  db->Put(write_options, "key1967", "value42362");

  // op 1410: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 1411: delete_range
  status = db->DeleteRange(write_options, "key2059", "key10021");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 1412: put
  db->Put(write_options, "key4451", "value17875");

  // op 1413: put
  db->Put(write_options, "key8174", "value77140");

  // op 1414: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 1415: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2005", "key9860", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2005, key9860) size=" << scan_result.size() << std::endl;
  }

  // op 1416: put
  db->Put(write_options, "key3494", "value14279");

  // op 1417: put
  db->Put(write_options, "key1173", "value26248");

  // op 1418: delete
  db->Delete(write_options, "key6921");

  // op 1419: get
  status = db->Get(read_options, "key9831", &value);
  if (status.ok()) {
    out << "GET key9831 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1420: get
  status = db->Get(read_options, "key9589", &value);
  if (status.ok()) {
    out << "GET key9589 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1421: get
  status = db->Get(read_options, "key10389", &value);
  if (status.ok()) {
    out << "GET key10389 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1422: put
  db->Put(write_options, "key1912", "value51323");

  // op 1423: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 1424: delete
  db->Delete(write_options, "key8318");

  // op 1425: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4674", "key6107", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4674, key6107) size=" << scan_result.size() << std::endl;
  }

  // op 1426: delete_range
  status = db->DeleteRange(write_options, "key0649", "key10231");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 1427: get
  status = db->Get(read_options, "key7692", &value);
  if (status.ok()) {
    out << "GET key7692 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1428: delete
  db->Delete(write_options, "key11641");

  // op 1429: get
  status = db->Get(read_options, "key2056", &value);
  if (status.ok()) {
    out << "GET key2056 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1430: delete
  db->Delete(write_options, "key1356");

  // op 1431: put
  db->Put(write_options, "key11094", "value7626");

  // op 1432: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0172", "key3608", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0172, key3608) size=" << scan_result.size() << std::endl;
  }

  // op 1433: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5976", "key10508", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5976, key10508) size=" << scan_result.size() << std::endl;
  }

  // op 1434: put
  db->Put(write_options, "key11040", "value31182");

  // op 1435: put
  db->Put(write_options, "key0238", "value28099");

  // op 1436: delete_range
  status = db->DeleteRange(write_options, "key6079", "key7712");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 1437: put
  db->Put(write_options, "key0635", "value60576");

  // op 1438: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2270", "key6920", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2270, key6920) size=" << scan_result.size() << std::endl;
  }

  // op 1439: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2720", "key10773", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2720, key10773) size=" << scan_result.size() << std::endl;
  }

  // op 1440: delete_range
  status = db->DeleteRange(write_options, "key7374", "key11717");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 1441: get
  status = db->Get(read_options, "key3120", &value);
  if (status.ok()) {
    out << "GET key3120 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1442: put
  db->Put(write_options, "key9504", "value89285");

  // op 1443: get
  status = db->Get(read_options, "key3857", &value);
  if (status.ok()) {
    out << "GET key3857 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1444: put
  db->Put(write_options, "key3615", "value35593");

  // op 1445: delete
  db->Delete(write_options, "key7522");

  // op 1446: delete_range
  status = db->DeleteRange(write_options, "key0002", "key7113");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 1447: get
  status = db->Get(read_options, "key2553", &value);
  if (status.ok()) {
    out << "GET key2553 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1448: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0091", "key3669", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0091, key3669) size=" << scan_result.size() << std::endl;
  }

  // op 1449: get
  status = db->Get(read_options, "key4878", &value);
  if (status.ok()) {
    out << "GET key4878 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1450: delete_range
  status = db->DeleteRange(write_options, "key4816", "key11083");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 1451: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8418", "key11512", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8418, key11512) size=" << scan_result.size() << std::endl;
  }

  // op 1452: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1090", "key6578", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1090, key6578) size=" << scan_result.size() << std::endl;
  }

  // op 1453: get
  status = db->Get(read_options, "key1099", &value);
  if (status.ok()) {
    out << "GET key1099 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1454: delete
  db->Delete(write_options, "key1935");

  // op 1455: delete_range
  status = db->DeleteRange(write_options, "key0975", "key9329");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 1456: put
  db->Put(write_options, "key2009", "value43751");

  // op 1457: get
  status = db->Get(read_options, "key8536", &value);
  if (status.ok()) {
    out << "GET key8536 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1458: get
  status = db->Get(read_options, "key7267", &value);
  if (status.ok()) {
    out << "GET key7267 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1459: put
  db->Put(write_options, "key3593", "value87103");

  // op 1460: delete_range
  status = db->DeleteRange(write_options, "key3174", "key6355");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 1461: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 1462: put
  db->Put(write_options, "key10922", "value14647");

  // op 1463: put
  db->Put(write_options, "key11594", "value28496");

  // op 1464: put
  db->Put(write_options, "key2050", "value47674");

  // op 1465: put
  db->Put(write_options, "key1214", "value60731");

  // op 1466: get
  status = db->Get(read_options, "key1951", &value);
  if (status.ok()) {
    out << "GET key1951 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1467: put
  db->Put(write_options, "key5908", "value13244");

  // op 1468: put
  db->Put(write_options, "key5016", "value73453");

  // op 1469: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2564", "key4460", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2564, key4460) size=" << scan_result.size() << std::endl;
  }

  // op 1470: delete
  db->Delete(write_options, "key6190");

  // op 1471: put
  db->Put(write_options, "key6976", "value63458");

  // op 1472: put
  db->Put(write_options, "key4612", "value52836");

  // op 1473: put
  db->Put(write_options, "key9920", "value50330");

  // op 1474: get
  status = db->Get(read_options, "key5270", &value);
  if (status.ok()) {
    out << "GET key5270 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1475: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 1476: put
  db->Put(write_options, "key1755", "value63326");

  // op 1477: get
  status = db->Get(read_options, "key5537", &value);
  if (status.ok()) {
    out << "GET key5537 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1478: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 1479: get
  status = db->Get(read_options, "key9247", &value);
  if (status.ok()) {
    out << "GET key9247 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1480: put
  db->Put(write_options, "key2395", "value32163");

  // op 1481: put
  db->Put(write_options, "key1534", "value16274");

  // op 1482: put
  db->Put(write_options, "key10544", "value43518");

  // op 1483: delete_range
  status = db->DeleteRange(write_options, "key0095", "key4086");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 1484: delete_range
  status = db->DeleteRange(write_options, "key3779", "key4891");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 1485: delete_range
  status = db->DeleteRange(write_options, "key9139", "key10157");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 1486: put
  db->Put(write_options, "key8041", "value1560");

  // op 1487: put
  db->Put(write_options, "key9320", "value6736");

  // op 1488: put
  db->Put(write_options, "key5736", "value16487");

  // op 1489: put
  db->Put(write_options, "key10863", "value8930");

  // op 1490: put
  db->Put(write_options, "key4769", "value92254");

  // op 1491: put
  db->Put(write_options, "key9630", "value28030");

  // op 1492: put
  db->Put(write_options, "key8962", "value61752");

  // op 1493: get
  status = db->Get(read_options, "key1147", &value);
  if (status.ok()) {
    out << "GET key1147 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1494: delete
  db->Delete(write_options, "key8742");

  // op 1495: get
  status = db->Get(read_options, "key2899", &value);
  if (status.ok()) {
    out << "GET key2899 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1496: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5796", "key11080", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5796, key11080) size=" << scan_result.size() << std::endl;
  }

  // op 1497: delete_range
  status = db->DeleteRange(write_options, "key3349", "key4523");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 1498: put
  db->Put(write_options, "key5521", "value64497");

  // op 1499: put
  db->Put(write_options, "key6407", "value58408");

  // op 1500: put
  db->Put(write_options, "key4005", "value58564");

  // op 1501: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 1502: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2116", "key5903", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2116, key5903) size=" << scan_result.size() << std::endl;
  }

  // op 1503: get
  status = db->Get(read_options, "key9822", &value);
  if (status.ok()) {
    out << "GET key9822 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1504: put
  db->Put(write_options, "key8297", "value107047");

  // op 1505: delete_range
  status = db->DeleteRange(write_options, "key2981", "key8237");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 1506: put
  db->Put(write_options, "key2747", "value33318");

  // op 1507: put
  db->Put(write_options, "key6777", "value37805");

  // op 1508: put
  db->Put(write_options, "key6721", "value28001");

  // op 1509: put
  db->Put(write_options, "key9360", "value3826");

  // op 1510: delete_range
  status = db->DeleteRange(write_options, "key6314", "key6392");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 1511: put
  db->Put(write_options, "key3193", "value39882");

  // op 1512: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2745", "key11238", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2745, key11238) size=" << scan_result.size() << std::endl;
  }

  // op 1513: get
  status = db->Get(read_options, "key1133", &value);
  if (status.ok()) {
    out << "GET key1133 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1514: delete
  db->Delete(write_options, "key11256");

  // op 1515: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0621", "key9179", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0621, key9179) size=" << scan_result.size() << std::endl;
  }

  // op 1516: delete_range
  status = db->DeleteRange(write_options, "key5063", "key10879");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 1517: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 1518: delete_range
  status = db->DeleteRange(write_options, "key9912", "key11029");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 1519: put
  db->Put(write_options, "key6693", "value12276");

  // op 1520: put
  db->Put(write_options, "key11033", "value38261");

  // op 1521: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 1522: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1011", "key2559", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1011, key2559) size=" << scan_result.size() << std::endl;
  }

  // op 1523: put
  db->Put(write_options, "key11665", "value19430");

  // op 1524: get
  status = db->Get(read_options, "key0210", &value);
  if (status.ok()) {
    out << "GET key0210 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1525: put
  db->Put(write_options, "key8742", "value109105");

  // op 1526: put
  db->Put(write_options, "key10492", "value11904");

  // op 1527: get
  status = db->Get(read_options, "key5254", &value);
  if (status.ok()) {
    out << "GET key5254 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1528: get
  status = db->Get(read_options, "key2942", &value);
  if (status.ok()) {
    out << "GET key2942 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1529: put
  db->Put(write_options, "key9184", "value6278");

  // op 1530: get
  status = db->Get(read_options, "key2668", &value);
  if (status.ok()) {
    out << "GET key2668 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1531: put
  db->Put(write_options, "key1803", "value5409");

  // op 1532: put
  db->Put(write_options, "key0938", "value5138");

  // op 1533: put
  db->Put(write_options, "key8400", "value115349");

  // op 1534: put
  db->Put(write_options, "key0201", "value44901");

  // op 1535: delete_range
  status = db->DeleteRange(write_options, "key6834", "key7404");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 1536: put
  db->Put(write_options, "key6830", "value118290");

  // op 1537: put
  db->Put(write_options, "key10775", "value103012");

  // op 1538: put
  db->Put(write_options, "key9845", "value85886");

  // op 1539: put
  db->Put(write_options, "key10626", "value115265");

  // op 1540: put
  db->Put(write_options, "key9460", "value92930");

  // op 1541: put
  db->Put(write_options, "key1802", "value83018");

  // op 1542: delete_range
  status = db->DeleteRange(write_options, "key5849", "key5990");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 1543: delete_range
  status = db->DeleteRange(write_options, "key2072", "key2357");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 1544: put
  db->Put(write_options, "key7419", "value42153");

  // op 1545: delete_range
  status = db->DeleteRange(write_options, "key1517", "key2949");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 1546: delete_range
  status = db->DeleteRange(write_options, "key0915", "key2310");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 1547: get
  status = db->Get(read_options, "key3005", &value);
  if (status.ok()) {
    out << "GET key3005 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1548: get
  status = db->Get(read_options, "key10406", &value);
  if (status.ok()) {
    out << "GET key10406 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1549: put
  db->Put(write_options, "key2557", "value62882");

  // op 1550: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7012", "key11726", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7012, key11726) size=" << scan_result.size() << std::endl;
  }

  // op 1551: put
  db->Put(write_options, "key3794", "value64663");

  // op 1552: delete
  db->Delete(write_options, "key11425");

  // op 1553: delete
  db->Delete(write_options, "key6843");

  // op 1554: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1960", "key8537", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1960, key8537) size=" << scan_result.size() << std::endl;
  }

  // op 1555: get
  status = db->Get(read_options, "key3112", &value);
  if (status.ok()) {
    out << "GET key3112 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1556: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3646", "key6134", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3646, key6134) size=" << scan_result.size() << std::endl;
  }

  // op 1557: delete_range
  status = db->DeleteRange(write_options, "key2048", "key4928");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 1558: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1932", "key3837", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1932, key3837) size=" << scan_result.size() << std::endl;
  }

  // op 1559: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0934", "key10396", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0934, key10396) size=" << scan_result.size() << std::endl;
  }

  // op 1560: put
  db->Put(write_options, "key5328", "value32260");

  // op 1561: get
  status = db->Get(read_options, "key4101", &value);
  if (status.ok()) {
    out << "GET key4101 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1562: get
  status = db->Get(read_options, "key0620", &value);
  if (status.ok()) {
    out << "GET key0620 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1563: get
  status = db->Get(read_options, "key1825", &value);
  if (status.ok()) {
    out << "GET key1825 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1564: put
  db->Put(write_options, "key9977", "value87527");

  // op 1565: get
  status = db->Get(read_options, "key9093", &value);
  if (status.ok()) {
    out << "GET key9093 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1566: get
  status = db->Get(read_options, "key6910", &value);
  if (status.ok()) {
    out << "GET key6910 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1567: put
  db->Put(write_options, "key10131", "value43972");

  // op 1568: put
  db->Put(write_options, "key1949", "value90516");

  // op 1569: put
  db->Put(write_options, "key6403", "value15561");

  // op 1570: put
  db->Put(write_options, "key11668", "value117102");

  // op 1571: get
  status = db->Get(read_options, "key3437", &value);
  if (status.ok()) {
    out << "GET key3437 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1572: get
  status = db->Get(read_options, "key3565", &value);
  if (status.ok()) {
    out << "GET key3565 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1573: put
  db->Put(write_options, "key0341", "value83121");

  // op 1574: delete
  db->Delete(write_options, "key6702");

  // op 1575: get
  status = db->Get(read_options, "key4807", &value);
  if (status.ok()) {
    out << "GET key4807 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1576: put
  db->Put(write_options, "key2029", "value117375");

  // op 1577: delete
  db->Delete(write_options, "key3379");

  // op 1578: put
  db->Put(write_options, "key0267", "value27244");

  // op 1579: put
  db->Put(write_options, "key11640", "value41948");

  // op 1580: get
  status = db->Get(read_options, "key2079", &value);
  if (status.ok()) {
    out << "GET key2079 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1581: get
  status = db->Get(read_options, "key2177", &value);
  if (status.ok()) {
    out << "GET key2177 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1582: put
  db->Put(write_options, "key3411", "value85921");

  // op 1583: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0430", "key11143", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0430, key11143) size=" << scan_result.size() << std::endl;
  }

  // op 1584: put
  db->Put(write_options, "key0856", "value76359");

  // op 1585: put
  db->Put(write_options, "key0316", "value94692");

  // op 1586: get
  status = db->Get(read_options, "key7646", &value);
  if (status.ok()) {
    out << "GET key7646 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1587: get
  status = db->Get(read_options, "key5903", &value);
  if (status.ok()) {
    out << "GET key5903 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1588: put
  db->Put(write_options, "key0690", "value10014");

  // op 1589: put
  db->Put(write_options, "key10100", "value105945");

  // op 1590: put
  db->Put(write_options, "key5413", "value70340");

  // op 1591: put
  db->Put(write_options, "key9905", "value104571");

  // op 1592: put
  db->Put(write_options, "key2910", "value62655");

  // op 1593: get
  status = db->Get(read_options, "key3012", &value);
  if (status.ok()) {
    out << "GET key3012 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1594: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3004", "key4178", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3004, key4178) size=" << scan_result.size() << std::endl;
  }

  // op 1595: delete
  db->Delete(write_options, "key7904");

  // op 1596: put
  db->Put(write_options, "key5325", "value93547");

  // op 1597: put
  db->Put(write_options, "key9414", "value2991");

  // op 1598: put
  db->Put(write_options, "key2994", "value69578");

  // op 1599: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4620", "key11354", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4620, key11354) size=" << scan_result.size() << std::endl;
  }

  // op 1600: put
  db->Put(write_options, "key11679", "value48858");

  // op 1601: delete
  db->Delete(write_options, "key10344");

  // op 1602: get
  status = db->Get(read_options, "key10601", &value);
  if (status.ok()) {
    out << "GET key10601 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1603: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6345", "key10817", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6345, key10817) size=" << scan_result.size() << std::endl;
  }

  // op 1604: delete_range
  status = db->DeleteRange(write_options, "key5075", "key9308");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 1605: delete_range
  status = db->DeleteRange(write_options, "key3628", "key5839");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 1606: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7646", "key10512", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7646, key10512) size=" << scan_result.size() << std::endl;
  }

  // op 1607: put
  db->Put(write_options, "key1640", "value33901");

  // op 1608: delete
  db->Delete(write_options, "key9833");

  // op 1609: delete_range
  status = db->DeleteRange(write_options, "key9856", "key11407");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 1610: put
  db->Put(write_options, "key7872", "value25883");

  // op 1611: put
  db->Put(write_options, "key0691", "value55621");

  // op 1612: put
  db->Put(write_options, "key1625", "value52482");

  // op 1613: get
  status = db->Get(read_options, "key5303", &value);
  if (status.ok()) {
    out << "GET key5303 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1614: put
  db->Put(write_options, "key0642", "value49968");

  // op 1615: put
  db->Put(write_options, "key3904", "value14826");

  // op 1616: delete
  db->Delete(write_options, "key4075");

  // op 1617: get
  status = db->Get(read_options, "key10528", &value);
  if (status.ok()) {
    out << "GET key10528 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1618: put
  db->Put(write_options, "key7544", "value101400");

  // op 1619: put
  db->Put(write_options, "key9534", "value42704");

  // op 1620: put
  db->Put(write_options, "key2756", "value33205");

  // op 1621: put
  db->Put(write_options, "key7985", "value42374");

  // op 1622: delete
  db->Delete(write_options, "key8686");

  // op 1623: put
  db->Put(write_options, "key7821", "value31310");

  // op 1624: delete
  db->Delete(write_options, "key10025");

  // op 1625: put
  db->Put(write_options, "key4399", "value14591");

  // op 1626: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3844", "key11601", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3844, key11601) size=" << scan_result.size() << std::endl;
  }

  // op 1627: get
  status = db->Get(read_options, "key9779", &value);
  if (status.ok()) {
    out << "GET key9779 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1628: put
  db->Put(write_options, "key5069", "value34112");

  // op 1629: put
  db->Put(write_options, "key10743", "value101415");

  // op 1630: put
  db->Put(write_options, "key9934", "value114126");

  // op 1631: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1135", "key7193", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1135, key7193) size=" << scan_result.size() << std::endl;
  }

  // op 1632: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1070", "key2161", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1070, key2161) size=" << scan_result.size() << std::endl;
  }

  // op 1633: get
  status = db->Get(read_options, "key2063", &value);
  if (status.ok()) {
    out << "GET key2063 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1634: put
  db->Put(write_options, "key1784", "value45003");

  // op 1635: delete
  db->Delete(write_options, "key5733");

  // op 1636: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9394", "key11496", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9394, key11496) size=" << scan_result.size() << std::endl;
  }

  // op 1637: delete
  db->Delete(write_options, "key11374");

  // op 1638: put
  db->Put(write_options, "key2743", "value105629");

  // op 1639: get
  status = db->Get(read_options, "key4709", &value);
  if (status.ok()) {
    out << "GET key4709 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1640: delete_range
  status = db->DeleteRange(write_options, "key0289", "key9860");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 1641: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 1642: delete_range
  status = db->DeleteRange(write_options, "key7522", "key9253");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 1643: get
  status = db->Get(read_options, "key9327", &value);
  if (status.ok()) {
    out << "GET key9327 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1644: put
  db->Put(write_options, "key1713", "value83434");

  // op 1645: put
  db->Put(write_options, "key2276", "value74893");

  // op 1646: get
  status = db->Get(read_options, "key9007", &value);
  if (status.ok()) {
    out << "GET key9007 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1647: delete_range
  status = db->DeleteRange(write_options, "key7676", "key7870");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 1648: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3645", "key8748", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3645, key8748) size=" << scan_result.size() << std::endl;
  }

  // op 1649: get
  status = db->Get(read_options, "key7330", &value);
  if (status.ok()) {
    out << "GET key7330 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1650: delete
  db->Delete(write_options, "key7682");

  // op 1651: put
  db->Put(write_options, "key4475", "value20037");

  // op 1652: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5038", "key11177", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5038, key11177) size=" << scan_result.size() << std::endl;
  }

  // op 1653: put
  db->Put(write_options, "key9219", "value14653");

  // op 1654: delete_range
  status = db->DeleteRange(write_options, "key3936", "key10825");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 1655: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0151", "key4135", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0151, key4135) size=" << scan_result.size() << std::endl;
  }

  // op 1656: put
  db->Put(write_options, "key5381", "value26987");

  // op 1657: put
  db->Put(write_options, "key3791", "value23590");

  // op 1658: put
  db->Put(write_options, "key11302", "value27181");

  // op 1659: put
  db->Put(write_options, "key7309", "value75952");

  // op 1660: put
  db->Put(write_options, "key9935", "value77411");

  // op 1661: put
  db->Put(write_options, "key1027", "value20026");

  // op 1662: put
  db->Put(write_options, "key3105", "value107945");

  // op 1663: put
  db->Put(write_options, "key7669", "value28537");

  // op 1664: delete
  db->Delete(write_options, "key6998");

  // op 1665: delete_range
  status = db->DeleteRange(write_options, "key5092", "key9456");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 1666: put
  db->Put(write_options, "key0386", "value30898");

  // op 1667: delete_range
  status = db->DeleteRange(write_options, "key1189", "key2965");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 1668: get
  status = db->Get(read_options, "key5715", &value);
  if (status.ok()) {
    out << "GET key5715 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1669: put
  db->Put(write_options, "key11064", "value43856");

  // op 1670: put
  db->Put(write_options, "key1263", "value26762");

  // op 1671: get
  status = db->Get(read_options, "key7910", &value);
  if (status.ok()) {
    out << "GET key7910 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1672: put
  db->Put(write_options, "key4771", "value93550");

  // op 1673: get
  status = db->Get(read_options, "key1108", &value);
  if (status.ok()) {
    out << "GET key1108 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1674: get
  status = db->Get(read_options, "key6949", &value);
  if (status.ok()) {
    out << "GET key6949 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1675: put
  db->Put(write_options, "key0751", "value36196");

  // op 1676: delete
  db->Delete(write_options, "key0060");

  // op 1677: put
  db->Put(write_options, "key9616", "value29057");

  // op 1678: delete
  db->Delete(write_options, "key0382");

  // op 1679: put
  db->Put(write_options, "key6840", "value60898");

  // op 1680: put
  db->Put(write_options, "key9111", "value88309");

  // op 1681: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1676", "key9505", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1676, key9505) size=" << scan_result.size() << std::endl;
  }

  // op 1682: get
  status = db->Get(read_options, "key8048", &value);
  if (status.ok()) {
    out << "GET key8048 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1683: delete_range
  status = db->DeleteRange(write_options, "key3551", "key4291");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 1684: delete_range
  status = db->DeleteRange(write_options, "key2009", "key3534");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 1685: put
  db->Put(write_options, "key11410", "value16671");

  // op 1686: put
  db->Put(write_options, "key3902", "value10572");

  // op 1687: put
  db->Put(write_options, "key0217", "value49093");

  // op 1688: put
  db->Put(write_options, "key8057", "value10604");

  // op 1689: put
  db->Put(write_options, "key8710", "value20623");

  // op 1690: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7529", "key11006", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7529, key11006) size=" << scan_result.size() << std::endl;
  }

  // op 1691: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1288", "key3410", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1288, key3410) size=" << scan_result.size() << std::endl;
  }

  // op 1692: put
  db->Put(write_options, "key0229", "value88600");

  // op 1693: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10980", "key11047", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10980, key11047) size=" << scan_result.size() << std::endl;
  }

  // op 1694: put
  db->Put(write_options, "key3370", "value21085");

  // op 1695: delete_range
  status = db->DeleteRange(write_options, "key0006", "key11385");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 1696: put
  db->Put(write_options, "key3023", "value116706");

  // op 1697: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3312", "key6586", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3312, key6586) size=" << scan_result.size() << std::endl;
  }

  // op 1698: put
  db->Put(write_options, "key10487", "value57601");

  // op 1699: get
  status = db->Get(read_options, "key8626", &value);
  if (status.ok()) {
    out << "GET key8626 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1700: put
  db->Put(write_options, "key0202", "value30993");

  // op 1701: delete_range
  status = db->DeleteRange(write_options, "key2308", "key6214");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 1702: get
  status = db->Get(read_options, "key9762", &value);
  if (status.ok()) {
    out << "GET key9762 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1703: delete_range
  status = db->DeleteRange(write_options, "key0769", "key8822");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 1704: get
  status = db->Get(read_options, "key0209", &value);
  if (status.ok()) {
    out << "GET key0209 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1705: put
  db->Put(write_options, "key2520", "value0013");

  // op 1706: put
  db->Put(write_options, "key9387", "value34871");

  // op 1707: put
  db->Put(write_options, "key4851", "value13578");

  // op 1708: put
  db->Put(write_options, "key0018", "value11388");

  // op 1709: get
  status = db->Get(read_options, "key2006", &value);
  if (status.ok()) {
    out << "GET key2006 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1710: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 1711: put
  db->Put(write_options, "key11156", "value66630");

  // op 1712: delete_range
  status = db->DeleteRange(write_options, "key2869", "key11709");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 1713: put
  db->Put(write_options, "key1493", "value5417");

  // op 1714: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7292", "key9164", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7292, key9164) size=" << scan_result.size() << std::endl;
  }

  // op 1715: get
  status = db->Get(read_options, "key1718", &value);
  if (status.ok()) {
    out << "GET key1718 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1716: delete
  db->Delete(write_options, "key5179");

  // op 1717: put
  db->Put(write_options, "key6457", "value41698");

  // op 1718: delete
  db->Delete(write_options, "key11664");

  // op 1719: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 1720: delete
  db->Delete(write_options, "key0308");

  // op 1721: delete_range
  status = db->DeleteRange(write_options, "key10400", "key10951");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 1722: get
  status = db->Get(read_options, "key1295", &value);
  if (status.ok()) {
    out << "GET key1295 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1723: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 1724: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3943", "key6803", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3943, key6803) size=" << scan_result.size() << std::endl;
  }

  // op 1725: get
  status = db->Get(read_options, "key4189", &value);
  if (status.ok()) {
    out << "GET key4189 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1726: get
  status = db->Get(read_options, "key7818", &value);
  if (status.ok()) {
    out << "GET key7818 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1727: put
  db->Put(write_options, "key10941", "value52555");

  // op 1728: put
  db->Put(write_options, "key6121", "value97619");

  // op 1729: delete_range
  status = db->DeleteRange(write_options, "key1722", "key7073");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 1730: delete_range
  status = db->DeleteRange(write_options, "key3681", "key7517");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 1731: put
  db->Put(write_options, "key5151", "value1644");

  // op 1732: delete
  db->Delete(write_options, "key2367");

  // op 1733: put
  db->Put(write_options, "key6697", "value112440");

  // op 1734: put
  db->Put(write_options, "key6926", "value65961");

  // op 1735: put
  db->Put(write_options, "key9112", "value62145");

  // op 1736: put
  db->Put(write_options, "key2887", "value51207");

  // op 1737: delete_range
  status = db->DeleteRange(write_options, "key4957", "key8802");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 1738: put
  db->Put(write_options, "key6063", "value29137");

  // op 1739: put
  db->Put(write_options, "key6304", "value11856");

  // op 1740: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 1741: put
  db->Put(write_options, "key2741", "value97943");

  // op 1742: put
  db->Put(write_options, "key10977", "value91800");

  // op 1743: put
  db->Put(write_options, "key1372", "value91147");

  // op 1744: put
  db->Put(write_options, "key0004", "value57413");

  // op 1745: put
  db->Put(write_options, "key6430", "value88535");

  // op 1746: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 1747: put
  db->Put(write_options, "key9051", "value92320");

  // op 1748: put
  db->Put(write_options, "key8410", "value35534");

  // op 1749: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0717", "key8341", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0717, key8341) size=" << scan_result.size() << std::endl;
  }

  // op 1750: put
  db->Put(write_options, "key2070", "value114819");

  // op 1751: delete
  db->Delete(write_options, "key0733");

  // op 1752: put
  db->Put(write_options, "key3704", "value64466");

  // op 1753: delete
  db->Delete(write_options, "key11027");

  // op 1754: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 1755: put
  db->Put(write_options, "key6307", "value43529");

  // op 1756: put
  db->Put(write_options, "key7714", "value73620");

  // op 1757: put
  db->Put(write_options, "key6231", "value23673");

  // op 1758: get
  status = db->Get(read_options, "key6384", &value);
  if (status.ok()) {
    out << "GET key6384 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1759: delete_range
  status = db->DeleteRange(write_options, "key5217", "key10843");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 1760: get
  status = db->Get(read_options, "key10141", &value);
  if (status.ok()) {
    out << "GET key10141 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1761: delete_range
  status = db->DeleteRange(write_options, "key2847", "key9184");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 1762: put
  db->Put(write_options, "key9380", "value76875");

  // op 1763: put
  db->Put(write_options, "key1993", "value36980");

  // op 1764: get
  status = db->Get(read_options, "key7086", &value);
  if (status.ok()) {
    out << "GET key7086 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1765: get
  status = db->Get(read_options, "key9501", &value);
  if (status.ok()) {
    out << "GET key9501 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1766: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0407", "key5011", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0407, key5011) size=" << scan_result.size() << std::endl;
  }

  // op 1767: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 1768: delete
  db->Delete(write_options, "key3797");

  // op 1769: get
  status = db->Get(read_options, "key3266", &value);
  if (status.ok()) {
    out << "GET key3266 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1770: put
  db->Put(write_options, "key6900", "value3858");

  // op 1771: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6160", "key10769", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6160, key10769) size=" << scan_result.size() << std::endl;
  }

  // op 1772: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1486", "key10601", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1486, key10601) size=" << scan_result.size() << std::endl;
  }

  // op 1773: put
  db->Put(write_options, "key6821", "value16736");

  // op 1774: put
  db->Put(write_options, "key1087", "value25042");

  // op 1775: get
  status = db->Get(read_options, "key5154", &value);
  if (status.ok()) {
    out << "GET key5154 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1776: delete
  db->Delete(write_options, "key7327");

  // op 1777: get
  status = db->Get(read_options, "key4701", &value);
  if (status.ok()) {
    out << "GET key4701 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1778: put
  db->Put(write_options, "key8404", "value1833");

  // op 1779: put
  db->Put(write_options, "key6908", "value40492");

  // op 1780: delete
  db->Delete(write_options, "key2080");

  // op 1781: put
  db->Put(write_options, "key3273", "value1165");

  // op 1782: delete_range
  status = db->DeleteRange(write_options, "key9768", "key10894");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 1783: get
  status = db->Get(read_options, "key6395", &value);
  if (status.ok()) {
    out << "GET key6395 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1784: put
  db->Put(write_options, "key1637", "value75721");

  // op 1785: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1150", "key3586", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1150, key3586) size=" << scan_result.size() << std::endl;
  }

  // op 1786: delete
  db->Delete(write_options, "key2531");

  // op 1787: delete_range
  status = db->DeleteRange(write_options, "key0912", "key9130");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 1788: put
  db->Put(write_options, "key10673", "value50872");

  // op 1789: put
  db->Put(write_options, "key7700", "value113778");

  // op 1790: put
  db->Put(write_options, "key1107", "value30740");

  // op 1791: put
  db->Put(write_options, "key2595", "value107083");

  // op 1792: delete
  db->Delete(write_options, "key11514");

  // op 1793: put
  db->Put(write_options, "key0113", "value74894");

  // op 1794: put
  db->Put(write_options, "key4310", "value107483");

  // op 1795: delete_range
  status = db->DeleteRange(write_options, "key3405", "key5076");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 1796: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0776", "key7636", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0776, key7636) size=" << scan_result.size() << std::endl;
  }

  // op 1797: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1743", "key5856", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1743, key5856) size=" << scan_result.size() << std::endl;
  }

  // op 1798: put
  db->Put(write_options, "key7978", "value102886");

  // op 1799: delete
  db->Delete(write_options, "key4147");

  // op 1800: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8112", "key9790", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8112, key9790) size=" << scan_result.size() << std::endl;
  }

  // op 1801: put
  db->Put(write_options, "key6129", "value76017");

  // op 1802: put
  db->Put(write_options, "key2216", "value43251");

  // op 1803: get
  status = db->Get(read_options, "key0121", &value);
  if (status.ok()) {
    out << "GET key0121 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1804: put
  db->Put(write_options, "key2413", "value26121");

  // op 1805: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 1806: put
  db->Put(write_options, "key8670", "value1285");

  // op 1807: put
  db->Put(write_options, "key11715", "value112357");

  // op 1808: put
  db->Put(write_options, "key11487", "value114512");

  // op 1809: delete
  db->Delete(write_options, "key2164");

  // op 1810: put
  db->Put(write_options, "key0767", "value92451");

  // op 1811: delete_range
  status = db->DeleteRange(write_options, "key10501", "key11436");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 1812: get
  status = db->Get(read_options, "key7546", &value);
  if (status.ok()) {
    out << "GET key7546 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1813: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0235", "key9466", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0235, key9466) size=" << scan_result.size() << std::endl;
  }

  // op 1814: delete
  db->Delete(write_options, "key7175");

  // op 1815: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 1816: put
  db->Put(write_options, "key1033", "value8786");

  // op 1817: get
  status = db->Get(read_options, "key6048", &value);
  if (status.ok()) {
    out << "GET key6048 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1818: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6722", "key9054", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6722, key9054) size=" << scan_result.size() << std::endl;
  }

  // op 1819: put
  db->Put(write_options, "key5391", "value62330");

  // op 1820: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 1821: get
  status = db->Get(read_options, "key11666", &value);
  if (status.ok()) {
    out << "GET key11666 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1822: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 1823: put
  db->Put(write_options, "key9009", "value61725");

  // op 1824: delete
  db->Delete(write_options, "key1929");

  // op 1825: get
  status = db->Get(read_options, "key10017", &value);
  if (status.ok()) {
    out << "GET key10017 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1826: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9441", "key11134", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9441, key11134) size=" << scan_result.size() << std::endl;
  }

  // op 1827: get
  status = db->Get(read_options, "key10716", &value);
  if (status.ok()) {
    out << "GET key10716 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1828: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5458", "key7035", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5458, key7035) size=" << scan_result.size() << std::endl;
  }

  // op 1829: get
  status = db->Get(read_options, "key7653", &value);
  if (status.ok()) {
    out << "GET key7653 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1830: get
  status = db->Get(read_options, "key2932", &value);
  if (status.ok()) {
    out << "GET key2932 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1831: delete
  db->Delete(write_options, "key6060");

  // op 1832: get
  status = db->Get(read_options, "key4922", &value);
  if (status.ok()) {
    out << "GET key4922 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1833: put
  db->Put(write_options, "key1169", "value114285");

  // op 1834: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 1835: put
  db->Put(write_options, "key9405", "value77437");

  // op 1836: put
  db->Put(write_options, "key10659", "value34811");

  // op 1837: put
  db->Put(write_options, "key5126", "value13604");

  // op 1838: put
  db->Put(write_options, "key7127", "value112837");

  // op 1839: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 1840: delete_range
  status = db->DeleteRange(write_options, "key3855", "key9296");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 1841: delete
  db->Delete(write_options, "key7058");

  // op 1842: put
  db->Put(write_options, "key5853", "value86990");

  // op 1843: delete
  db->Delete(write_options, "key11724");

  // op 1844: put
  db->Put(write_options, "key4993", "value77321");

  // op 1845: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 1846: get
  status = db->Get(read_options, "key10546", &value);
  if (status.ok()) {
    out << "GET key10546 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1847: put
  db->Put(write_options, "key4565", "value41074");

  // op 1848: delete
  db->Delete(write_options, "key3885");

  // op 1849: put
  db->Put(write_options, "key4337", "value99473");

  // op 1850: put
  db->Put(write_options, "key10795", "value66808");

  // op 1851: put
  db->Put(write_options, "key4827", "value81246");

  // op 1852: put
  db->Put(write_options, "key7355", "value70544");

  // op 1853: put
  db->Put(write_options, "key8089", "value84329");

  // op 1854: put
  db->Put(write_options, "key5502", "value0929");

  // op 1855: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2522", "key11715", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2522, key11715) size=" << scan_result.size() << std::endl;
  }

  // op 1856: put
  db->Put(write_options, "key7118", "value47180");

  // op 1857: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4647", "key8744", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4647, key8744) size=" << scan_result.size() << std::endl;
  }

  // op 1858: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5528", "key6267", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5528, key6267) size=" << scan_result.size() << std::endl;
  }

  // op 1859: get
  status = db->Get(read_options, "key2673", &value);
  if (status.ok()) {
    out << "GET key2673 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1860: put
  db->Put(write_options, "key7634", "value80019");

  // op 1861: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6620", "key8761", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6620, key8761) size=" << scan_result.size() << std::endl;
  }

  // op 1862: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 1863: put
  db->Put(write_options, "key3380", "value38044");

  // op 1864: get
  status = db->Get(read_options, "key7540", &value);
  if (status.ok()) {
    out << "GET key7540 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1865: put
  db->Put(write_options, "key6073", "value2065");

  // op 1866: get
  status = db->Get(read_options, "key8838", &value);
  if (status.ok()) {
    out << "GET key8838 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1867: put
  db->Put(write_options, "key5976", "value68656");

  // op 1868: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10807", "key11592", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10807, key11592) size=" << scan_result.size() << std::endl;
  }

  // op 1869: put
  db->Put(write_options, "key10377", "value109246");

  // op 1870: delete
  db->Delete(write_options, "key3983");

  // op 1871: put
  db->Put(write_options, "key7456", "value59293");

  // op 1872: put
  db->Put(write_options, "key6083", "value83039");

  // op 1873: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10305", "key11330", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10305, key11330) size=" << scan_result.size() << std::endl;
  }

  // op 1874: delete_range
  status = db->DeleteRange(write_options, "key2844", "key5482");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 1875: put
  db->Put(write_options, "key7365", "value7189");

  // op 1876: put
  db->Put(write_options, "key1022", "value74334");

  // op 1877: delete
  db->Delete(write_options, "key10954");

  // op 1878: delete_range
  status = db->DeleteRange(write_options, "key8315", "key11676");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 1879: delete
  db->Delete(write_options, "key9511");

  // op 1880: get
  status = db->Get(read_options, "key11611", &value);
  if (status.ok()) {
    out << "GET key11611 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1881: put
  db->Put(write_options, "key1653", "value76855");

  // op 1882: put
  db->Put(write_options, "key9255", "value4831");

  // op 1883: put
  db->Put(write_options, "key10269", "value13417");

  // op 1884: get
  status = db->Get(read_options, "key1146", &value);
  if (status.ok()) {
    out << "GET key1146 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1885: put
  db->Put(write_options, "key0908", "value38152");

  // op 1886: put
  db->Put(write_options, "key8453", "value67295");

  // op 1887: put
  db->Put(write_options, "key8845", "value17817");

  // op 1888: put
  db->Put(write_options, "key3615", "value90119");

  // op 1889: get
  status = db->Get(read_options, "key6677", &value);
  if (status.ok()) {
    out << "GET key6677 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1890: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0131", "key11213", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0131, key11213) size=" << scan_result.size() << std::endl;
  }

  // op 1891: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0986", "key5461", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0986, key5461) size=" << scan_result.size() << std::endl;
  }

  // op 1892: get
  status = db->Get(read_options, "key5955", &value);
  if (status.ok()) {
    out << "GET key5955 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1893: put
  db->Put(write_options, "key5285", "value106137");

  // op 1894: delete
  db->Delete(write_options, "key6294");

  // op 1895: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 1896: put
  db->Put(write_options, "key10685", "value81070");

  // op 1897: put
  db->Put(write_options, "key7374", "value102310");

  // op 1898: get
  status = db->Get(read_options, "key8658", &value);
  if (status.ok()) {
    out << "GET key8658 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1899: put
  db->Put(write_options, "key1636", "value63373");

  // op 1900: put
  db->Put(write_options, "key2911", "value108597");

  // op 1901: delete
  db->Delete(write_options, "key7603");

  // op 1902: put
  db->Put(write_options, "key11011", "value97124");

  // op 1903: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0927", "key8305", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0927, key8305) size=" << scan_result.size() << std::endl;
  }

  // op 1904: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 1905: put
  db->Put(write_options, "key7752", "value107588");

  // op 1906: get
  status = db->Get(read_options, "key1388", &value);
  if (status.ok()) {
    out << "GET key1388 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1907: delete_range
  status = db->DeleteRange(write_options, "key6698", "key8529");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 1908: get
  status = db->Get(read_options, "key4648", &value);
  if (status.ok()) {
    out << "GET key4648 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1909: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6897", "key10564", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6897, key10564) size=" << scan_result.size() << std::endl;
  }

  // op 1910: put
  db->Put(write_options, "key0475", "value26816");

  // op 1911: put
  db->Put(write_options, "key11511", "value39822");

  // op 1912: get
  status = db->Get(read_options, "key3406", &value);
  if (status.ok()) {
    out << "GET key3406 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1913: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3556", "key5361", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3556, key5361) size=" << scan_result.size() << std::endl;
  }

  // op 1914: put
  db->Put(write_options, "key9182", "value54476");

  // op 1915: put
  db->Put(write_options, "key9030", "value77183");

  // op 1916: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5064", "key7380", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5064, key7380) size=" << scan_result.size() << std::endl;
  }

  // op 1917: get
  status = db->Get(read_options, "key10620", &value);
  if (status.ok()) {
    out << "GET key10620 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1918: put
  db->Put(write_options, "key1205", "value37952");

  // op 1919: delete_range
  status = db->DeleteRange(write_options, "key3018", "key11677");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 1920: delete
  db->Delete(write_options, "key11048");

  // op 1921: put
  db->Put(write_options, "key6149", "value48472");

  // op 1922: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9076", "key10697", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9076, key10697) size=" << scan_result.size() << std::endl;
  }

  // op 1923: put
  db->Put(write_options, "key8826", "value7850");

  // op 1924: put
  db->Put(write_options, "key5023", "value83850");

  // op 1925: delete
  db->Delete(write_options, "key4657");

  // op 1926: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 1927: get
  status = db->Get(read_options, "key7791", &value);
  if (status.ok()) {
    out << "GET key7791 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1928: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7413", "key8483", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7413, key8483) size=" << scan_result.size() << std::endl;
  }

  // op 1929: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4265", "key8014", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4265, key8014) size=" << scan_result.size() << std::endl;
  }

  // op 1930: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4663", "key6551", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4663, key6551) size=" << scan_result.size() << std::endl;
  }

  // op 1931: put
  db->Put(write_options, "key11418", "value46167");

  // op 1932: put
  db->Put(write_options, "key1903", "value53084");

  // op 1933: delete
  db->Delete(write_options, "key6153");

  // op 1934: delete
  db->Delete(write_options, "key4713");

  // op 1935: put
  db->Put(write_options, "key10217", "value16702");

  // op 1936: delete_range
  status = db->DeleteRange(write_options, "key0329", "key1493");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 1937: get
  status = db->Get(read_options, "key11165", &value);
  if (status.ok()) {
    out << "GET key11165 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1938: put
  db->Put(write_options, "key2128", "value40649");

  // op 1939: get
  status = db->Get(read_options, "key5893", &value);
  if (status.ok()) {
    out << "GET key5893 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1940: put
  db->Put(write_options, "key0709", "value105038");

  // op 1941: delete_range
  status = db->DeleteRange(write_options, "key0645", "key0874");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 1942: put
  db->Put(write_options, "key8951", "value82370");

  // op 1943: put
  db->Put(write_options, "key6165", "value98450");

  // op 1944: get
  status = db->Get(read_options, "key10887", &value);
  if (status.ok()) {
    out << "GET key10887 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1945: put
  db->Put(write_options, "key11652", "value72439");

  // op 1946: put
  db->Put(write_options, "key3542", "value7276");

  // op 1947: put
  db->Put(write_options, "key11583", "value36430");

  // op 1948: delete
  db->Delete(write_options, "key1881");

  // op 1949: delete_range
  status = db->DeleteRange(write_options, "key2776", "key9939");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 1950: get
  status = db->Get(read_options, "key5705", &value);
  if (status.ok()) {
    out << "GET key5705 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1951: put
  db->Put(write_options, "key2559", "value64389");

  // op 1952: put
  db->Put(write_options, "key10335", "value98557");

  // op 1953: put
  db->Put(write_options, "key1337", "value22881");

  // op 1954: put
  db->Put(write_options, "key9527", "value10017");

  // op 1955: put
  db->Put(write_options, "key0692", "value95565");

  // op 1956: put
  db->Put(write_options, "key11251", "value47454");

  // op 1957: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6742", "key11604", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6742, key11604) size=" << scan_result.size() << std::endl;
  }

  // op 1958: get
  status = db->Get(read_options, "key3203", &value);
  if (status.ok()) {
    out << "GET key3203 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1959: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 1960: put
  db->Put(write_options, "key3443", "value18942");

  // op 1961: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 1962: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4094", "key5929", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4094, key5929) size=" << scan_result.size() << std::endl;
  }

  // op 1963: put
  db->Put(write_options, "key7564", "value4214");

  // op 1964: put
  db->Put(write_options, "key3023", "value7869");

  // op 1965: put
  db->Put(write_options, "key9172", "value12164");

  // op 1966: put
  db->Put(write_options, "key10821", "value33664");

  // op 1967: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 1968: delete
  db->Delete(write_options, "key0040");

  // op 1969: delete
  db->Delete(write_options, "key0826");

  // op 1970: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0283", "key3025", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0283, key3025) size=" << scan_result.size() << std::endl;
  }

  // op 1971: get
  status = db->Get(read_options, "key3982", &value);
  if (status.ok()) {
    out << "GET key3982 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1972: put
  db->Put(write_options, "key4729", "value24289");

  // op 1973: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1555", "key5213", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1555, key5213) size=" << scan_result.size() << std::endl;
  }

  // op 1974: put
  db->Put(write_options, "key11243", "value114967");

  // op 1975: get
  status = db->Get(read_options, "key4521", &value);
  if (status.ok()) {
    out << "GET key4521 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1976: get
  status = db->Get(read_options, "key7253", &value);
  if (status.ok()) {
    out << "GET key7253 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1977: put
  db->Put(write_options, "key6118", "value97137");

  // op 1978: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 1979: delete_range
  status = db->DeleteRange(write_options, "key0736", "key2653");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 1980: put
  db->Put(write_options, "key6334", "value21325");

  // op 1981: put
  db->Put(write_options, "key7097", "value40228");

  // op 1982: put
  db->Put(write_options, "key3637", "value96489");

  // op 1983: get
  status = db->Get(read_options, "key9981", &value);
  if (status.ok()) {
    out << "GET key9981 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1984: get
  status = db->Get(read_options, "key10379", &value);
  if (status.ok()) {
    out << "GET key10379 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1985: delete_range
  status = db->DeleteRange(write_options, "key1846", "key3002");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 1986: put
  db->Put(write_options, "key2580", "value18735");

  // op 1987: put
  db->Put(write_options, "key6381", "value57554");

  // op 1988: get
  status = db->Get(read_options, "key6654", &value);
  if (status.ok()) {
    out << "GET key6654 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1989: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6306", "key10398", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6306, key10398) size=" << scan_result.size() << std::endl;
  }

  // op 1990: put
  db->Put(write_options, "key9554", "value68450");

  // op 1991: delete_range
  status = db->DeleteRange(write_options, "key5450", "key9706");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 1992: delete_range
  status = db->DeleteRange(write_options, "key2015", "key7282");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 1993: get
  status = db->Get(read_options, "key0025", &value);
  if (status.ok()) {
    out << "GET key0025 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1994: delete
  db->Delete(write_options, "key11709");

  // op 1995: put
  db->Put(write_options, "key6544", "value52945");

  // op 1996: get
  status = db->Get(read_options, "key8074", &value);
  if (status.ok()) {
    out << "GET key8074 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 1997: delete
  db->Delete(write_options, "key5828");

  // op 1998: delete_range
  status = db->DeleteRange(write_options, "key4049", "key4518");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 1999: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 2000: get
  status = db->Get(read_options, "key1791", &value);
  if (status.ok()) {
    out << "GET key1791 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2001: put
  db->Put(write_options, "key0861", "value66071");

  // op 2002: get
  status = db->Get(read_options, "key8163", &value);
  if (status.ok()) {
    out << "GET key8163 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2003: put
  db->Put(write_options, "key6890", "value75129");

  // op 2004: put
  db->Put(write_options, "key8539", "value32923");

  // op 2005: put
  db->Put(write_options, "key10093", "value100208");

  // op 2006: put
  db->Put(write_options, "key9988", "value53420");

  // op 2007: delete_range
  status = db->DeleteRange(write_options, "key4434", "key9943");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 2008: put
  db->Put(write_options, "key1398", "value92832");

  // op 2009: put
  db->Put(write_options, "key0279", "value50429");

  // op 2010: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 2011: delete_range
  status = db->DeleteRange(write_options, "key6827", "key8755");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 2012: get
  status = db->Get(read_options, "key1909", &value);
  if (status.ok()) {
    out << "GET key1909 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2013: put
  db->Put(write_options, "key10883", "value115598");

  // op 2014: put
  db->Put(write_options, "key2308", "value112276");

  // op 2015: put
  db->Put(write_options, "key11335", "value111611");

  // op 2016: delete_range
  status = db->DeleteRange(write_options, "key5002", "key8529");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 2017: put
  db->Put(write_options, "key5832", "value87869");

  // op 2018: put
  db->Put(write_options, "key3300", "value53406");

  // op 2019: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 2020: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1336", "key10513", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1336, key10513) size=" << scan_result.size() << std::endl;
  }

  // op 2021: get
  status = db->Get(read_options, "key7715", &value);
  if (status.ok()) {
    out << "GET key7715 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2022: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6118", "key6669", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6118, key6669) size=" << scan_result.size() << std::endl;
  }

  // op 2023: put
  db->Put(write_options, "key1060", "value2857");

  // op 2024: delete
  db->Delete(write_options, "key8193");

  // op 2025: put
  db->Put(write_options, "key10019", "value103317");

  // op 2026: get
  status = db->Get(read_options, "key2194", &value);
  if (status.ok()) {
    out << "GET key2194 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2027: put
  db->Put(write_options, "key2402", "value113518");

  // op 2028: get
  status = db->Get(read_options, "key11280", &value);
  if (status.ok()) {
    out << "GET key11280 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2029: delete
  db->Delete(write_options, "key1750");

  // op 2030: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3365", "key5298", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3365, key5298) size=" << scan_result.size() << std::endl;
  }

  // op 2031: put
  db->Put(write_options, "key7037", "value69332");

  // op 2032: put
  db->Put(write_options, "key10812", "value86020");

  // op 2033: put
  db->Put(write_options, "key4958", "value52379");

  // op 2034: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3287", "key9958", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3287, key9958) size=" << scan_result.size() << std::endl;
  }

  // op 2035: get
  status = db->Get(read_options, "key3881", &value);
  if (status.ok()) {
    out << "GET key3881 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2036: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5859", "key6356", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5859, key6356) size=" << scan_result.size() << std::endl;
  }

  // op 2037: put
  db->Put(write_options, "key3900", "value9753");

  // op 2038: put
  db->Put(write_options, "key11206", "value87000");

  // op 2039: delete_range
  status = db->DeleteRange(write_options, "key3928", "key6144");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 2040: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9386", "key9531", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9386, key9531) size=" << scan_result.size() << std::endl;
  }

  // op 2041: put
  db->Put(write_options, "key9230", "value2128");

  // op 2042: get
  status = db->Get(read_options, "key7343", &value);
  if (status.ok()) {
    out << "GET key7343 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2043: put
  db->Put(write_options, "key11534", "value34388");

  // op 2044: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1666", "key5080", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1666, key5080) size=" << scan_result.size() << std::endl;
  }

  // op 2045: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1497", "key4764", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1497, key4764) size=" << scan_result.size() << std::endl;
  }

  // op 2046: put
  db->Put(write_options, "key10933", "value52270");

  // op 2047: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3488", "key7293", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3488, key7293) size=" << scan_result.size() << std::endl;
  }

  // op 2048: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1273", "key1685", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1273, key1685) size=" << scan_result.size() << std::endl;
  }

  // op 2049: delete_range
  status = db->DeleteRange(write_options, "key5828", "key7212");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 2050: get
  status = db->Get(read_options, "key1277", &value);
  if (status.ok()) {
    out << "GET key1277 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2051: put
  db->Put(write_options, "key8034", "value104006");

  // op 2052: delete_range
  status = db->DeleteRange(write_options, "key7177", "key8782");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 2053: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4245", "key4539", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4245, key4539) size=" << scan_result.size() << std::endl;
  }

  // op 2054: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 2055: put
  db->Put(write_options, "key10391", "value6855");

  // op 2056: put
  db->Put(write_options, "key7852", "value54027");

  // op 2057: put
  db->Put(write_options, "key1467", "value73666");

  // op 2058: delete
  db->Delete(write_options, "key7763");

  // op 2059: put
  db->Put(write_options, "key8171", "value21086");

  // op 2060: put
  db->Put(write_options, "key3263", "value56818");

  // op 2061: put
  db->Put(write_options, "key11627", "value39730");

  // op 2062: delete_range
  status = db->DeleteRange(write_options, "key3311", "key3356");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 2063: get
  status = db->Get(read_options, "key0921", &value);
  if (status.ok()) {
    out << "GET key0921 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2064: get
  status = db->Get(read_options, "key7352", &value);
  if (status.ok()) {
    out << "GET key7352 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2065: get
  status = db->Get(read_options, "key8650", &value);
  if (status.ok()) {
    out << "GET key8650 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2066: put
  db->Put(write_options, "key3522", "value30298");

  // op 2067: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2824", "key10032", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2824, key10032) size=" << scan_result.size() << std::endl;
  }

  // op 2068: put
  db->Put(write_options, "key5028", "value43770");

  // op 2069: put
  db->Put(write_options, "key0241", "value20016");

  // op 2070: delete_range
  status = db->DeleteRange(write_options, "key0670", "key6860");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 2071: put
  db->Put(write_options, "key4207", "value24131");

  // op 2072: put
  db->Put(write_options, "key4312", "value1025");

  // op 2073: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 2074: delete_range
  status = db->DeleteRange(write_options, "key7269", "key10876");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 2075: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2134", "key10508", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2134, key10508) size=" << scan_result.size() << std::endl;
  }

  // op 2076: put
  db->Put(write_options, "key4528", "value12156");

  // op 2077: put
  db->Put(write_options, "key0456", "value91535");

  // op 2078: put
  db->Put(write_options, "key1537", "value112023");

  // op 2079: get
  status = db->Get(read_options, "key11138", &value);
  if (status.ok()) {
    out << "GET key11138 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2080: put
  db->Put(write_options, "key3625", "value26447");

  // op 2081: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 2082: delete
  db->Delete(write_options, "key0060");

  // op 2083: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 2084: put
  db->Put(write_options, "key8600", "value93782");

  // op 2085: put
  db->Put(write_options, "key7455", "value66193");

  // op 2086: delete
  db->Delete(write_options, "key0944");

  // op 2087: put
  db->Put(write_options, "key6587", "value6290");

  // op 2088: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5824", "key9301", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5824, key9301) size=" << scan_result.size() << std::endl;
  }

  // op 2089: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 2090: put
  db->Put(write_options, "key4948", "value114630");

  // op 2091: get
  status = db->Get(read_options, "key0016", &value);
  if (status.ok()) {
    out << "GET key0016 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2092: get
  status = db->Get(read_options, "key10455", &value);
  if (status.ok()) {
    out << "GET key10455 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2093: get
  status = db->Get(read_options, "key6131", &value);
  if (status.ok()) {
    out << "GET key6131 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2094: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0588", "key6576", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0588, key6576) size=" << scan_result.size() << std::endl;
  }

  // op 2095: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 2096: get
  status = db->Get(read_options, "key3178", &value);
  if (status.ok()) {
    out << "GET key3178 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2097: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0423", "key4276", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0423, key4276) size=" << scan_result.size() << std::endl;
  }

  // op 2098: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0893", "key5888", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0893, key5888) size=" << scan_result.size() << std::endl;
  }

  // op 2099: put
  db->Put(write_options, "key11561", "value65113");

  // op 2100: put
  db->Put(write_options, "key3201", "value35203");

  // op 2101: put
  db->Put(write_options, "key3874", "value54479");

  // op 2102: delete
  db->Delete(write_options, "key4396");

  // op 2103: put
  db->Put(write_options, "key5632", "value26391");

  // op 2104: put
  db->Put(write_options, "key11493", "value3402");

  // op 2105: get
  status = db->Get(read_options, "key0637", &value);
  if (status.ok()) {
    out << "GET key0637 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2106: put
  db->Put(write_options, "key8949", "value15458");

  // op 2107: put
  db->Put(write_options, "key9629", "value41267");

  // op 2108: put
  db->Put(write_options, "key0668", "value99758");

  // op 2109: put
  db->Put(write_options, "key9926", "value9242");

  // op 2110: put
  db->Put(write_options, "key6855", "value86957");

  // op 2111: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 2112: get
  status = db->Get(read_options, "key8189", &value);
  if (status.ok()) {
    out << "GET key8189 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2113: get
  status = db->Get(read_options, "key0475", &value);
  if (status.ok()) {
    out << "GET key0475 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2114: put
  db->Put(write_options, "key4610", "value18819");

  // op 2115: get
  status = db->Get(read_options, "key2815", &value);
  if (status.ok()) {
    out << "GET key2815 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2116: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 2117: delete
  db->Delete(write_options, "key8917");

  // op 2118: put
  db->Put(write_options, "key8730", "value18288");

  // op 2119: get
  status = db->Get(read_options, "key3831", &value);
  if (status.ok()) {
    out << "GET key3831 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2120: put
  db->Put(write_options, "key8494", "value108845");

  // op 2121: put
  db->Put(write_options, "key11270", "value2618");

  // op 2122: put
  db->Put(write_options, "key3903", "value108771");

  // op 2123: get
  status = db->Get(read_options, "key2059", &value);
  if (status.ok()) {
    out << "GET key2059 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2124: put
  db->Put(write_options, "key4559", "value49486");

  // op 2125: put
  db->Put(write_options, "key2830", "value69392");

  // op 2126: put
  db->Put(write_options, "key8264", "value26597");

  // op 2127: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6747", "key9875", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6747, key9875) size=" << scan_result.size() << std::endl;
  }

  // op 2128: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0759", "key2550", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0759, key2550) size=" << scan_result.size() << std::endl;
  }

  // op 2129: put
  db->Put(write_options, "key0959", "value14841");

  // op 2130: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0833", "key10113", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0833, key10113) size=" << scan_result.size() << std::endl;
  }

  // op 2131: put
  db->Put(write_options, "key2632", "value68678");

  // op 2132: put
  db->Put(write_options, "key6369", "value14093");

  // op 2133: put
  db->Put(write_options, "key3938", "value86731");

  // op 2134: delete_range
  status = db->DeleteRange(write_options, "key6063", "key11191");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 2135: put
  db->Put(write_options, "key7380", "value68992");

  // op 2136: put
  db->Put(write_options, "key2608", "value69538");

  // op 2137: put
  db->Put(write_options, "key3810", "value24324");

  // op 2138: delete
  db->Delete(write_options, "key8055");

  // op 2139: put
  db->Put(write_options, "key11707", "value110954");

  // op 2140: get
  status = db->Get(read_options, "key6344", &value);
  if (status.ok()) {
    out << "GET key6344 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2141: put
  db->Put(write_options, "key7279", "value97888");

  // op 2142: put
  db->Put(write_options, "key9341", "value104902");

  // op 2143: get
  status = db->Get(read_options, "key5481", &value);
  if (status.ok()) {
    out << "GET key5481 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2144: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7973", "key10100", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7973, key10100) size=" << scan_result.size() << std::endl;
  }

  // op 2145: delete
  db->Delete(write_options, "key1653");

  // op 2146: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7641", "key9059", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7641, key9059) size=" << scan_result.size() << std::endl;
  }

  // op 2147: get
  status = db->Get(read_options, "key0541", &value);
  if (status.ok()) {
    out << "GET key0541 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2148: put
  db->Put(write_options, "key5585", "value110314");

  // op 2149: put
  db->Put(write_options, "key5509", "value73746");

  // op 2150: put
  db->Put(write_options, "key1732", "value74901");

  // op 2151: put
  db->Put(write_options, "key9432", "value69614");

  // op 2152: get
  status = db->Get(read_options, "key3452", &value);
  if (status.ok()) {
    out << "GET key3452 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2153: get
  status = db->Get(read_options, "key3972", &value);
  if (status.ok()) {
    out << "GET key3972 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2154: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7789", "key9227", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7789, key9227) size=" << scan_result.size() << std::endl;
  }

  // op 2155: get
  status = db->Get(read_options, "key1621", &value);
  if (status.ok()) {
    out << "GET key1621 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2156: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 2157: put
  db->Put(write_options, "key6397", "value84661");

  // op 2158: put
  db->Put(write_options, "key8040", "value17419");

  // op 2159: get
  status = db->Get(read_options, "key2030", &value);
  if (status.ok()) {
    out << "GET key2030 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2160: put
  db->Put(write_options, "key9202", "value104991");

  // op 2161: scan
  scan_result.clear();
  status = db->Scan(read_options, "key11120", "key11197", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key11120, key11197) size=" << scan_result.size() << std::endl;
  }

  // op 2162: get
  status = db->Get(read_options, "key4964", &value);
  if (status.ok()) {
    out << "GET key4964 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2163: put
  db->Put(write_options, "key0222", "value43199");

  // op 2164: put
  db->Put(write_options, "key8880", "value101722");

  // op 2165: put
  db->Put(write_options, "key5941", "value74832");

  // op 2166: delete
  db->Delete(write_options, "key10150");

  // op 2167: put
  db->Put(write_options, "key6721", "value98320");

  // op 2168: put
  db->Put(write_options, "key0232", "value62868");

  // op 2169: put
  db->Put(write_options, "key1382", "value24042");

  // op 2170: put
  db->Put(write_options, "key8900", "value115012");

  // op 2171: put
  db->Put(write_options, "key2266", "value24946");

  // op 2172: put
  db->Put(write_options, "key11553", "value13842");

  // op 2173: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9044", "key10470", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9044, key10470) size=" << scan_result.size() << std::endl;
  }

  // op 2174: put
  db->Put(write_options, "key2906", "value39680");

  // op 2175: delete_range
  status = db->DeleteRange(write_options, "key0309", "key9162");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 2176: delete
  db->Delete(write_options, "key1439");

  // op 2177: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6975", "key10063", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6975, key10063) size=" << scan_result.size() << std::endl;
  }

  // op 2178: delete_range
  status = db->DeleteRange(write_options, "key1566", "key8602");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 2179: put
  db->Put(write_options, "key7578", "value36032");

  // op 2180: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4441", "key8474", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4441, key8474) size=" << scan_result.size() << std::endl;
  }

  // op 2181: put
  db->Put(write_options, "key8810", "value90269");

  // op 2182: put
  db->Put(write_options, "key8626", "value59689");

  // op 2183: delete
  db->Delete(write_options, "key4741");

  // op 2184: delete
  db->Delete(write_options, "key0929");

  // op 2185: put
  db->Put(write_options, "key5508", "value0196");

  // op 2186: get
  status = db->Get(read_options, "key9518", &value);
  if (status.ok()) {
    out << "GET key9518 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2187: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1664", "key2028", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1664, key2028) size=" << scan_result.size() << std::endl;
  }

  // op 2188: put
  db->Put(write_options, "key5665", "value1195");

  // op 2189: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 2190: put
  db->Put(write_options, "key5754", "value81499");

  // op 2191: put
  db->Put(write_options, "key2111", "value9910");

  // op 2192: get
  status = db->Get(read_options, "key7207", &value);
  if (status.ok()) {
    out << "GET key7207 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2193: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 2194: get
  status = db->Get(read_options, "key11725", &value);
  if (status.ok()) {
    out << "GET key11725 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2195: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0333", "key11256", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0333, key11256) size=" << scan_result.size() << std::endl;
  }

  // op 2196: put
  db->Put(write_options, "key7332", "value61684");

  // op 2197: delete_range
  status = db->DeleteRange(write_options, "key0960", "key5160");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 2198: put
  db->Put(write_options, "key11362", "value36363");

  // op 2199: put
  db->Put(write_options, "key4312", "value64035");

  // op 2200: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 2201: delete
  db->Delete(write_options, "key8061");

  // op 2202: put
  db->Put(write_options, "key10391", "value36183");

  // op 2203: put
  db->Put(write_options, "key5826", "value91521");

  // op 2204: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2545", "key11603", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2545, key11603) size=" << scan_result.size() << std::endl;
  }

  // op 2205: put
  db->Put(write_options, "key9794", "value29603");

  // op 2206: delete_range
  status = db->DeleteRange(write_options, "key0059", "key6353");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 2207: put
  db->Put(write_options, "key2807", "value65139");

  // op 2208: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5629", "key8774", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5629, key8774) size=" << scan_result.size() << std::endl;
  }

  // op 2209: put
  db->Put(write_options, "key1613", "value82351");

  // op 2210: delete_range
  status = db->DeleteRange(write_options, "key1891", "key2800");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 2211: delete
  db->Delete(write_options, "key0774");

  // op 2212: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2313", "key10704", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2313, key10704) size=" << scan_result.size() << std::endl;
  }

  // op 2213: put
  db->Put(write_options, "key2959", "value92641");

  // op 2214: delete
  db->Delete(write_options, "key2175");

  // op 2215: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1410", "key10411", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1410, key10411) size=" << scan_result.size() << std::endl;
  }

  // op 2216: put
  db->Put(write_options, "key5349", "value2351");

  // op 2217: delete_range
  status = db->DeleteRange(write_options, "key3783", "key5227");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 2218: put
  db->Put(write_options, "key8103", "value35487");

  // op 2219: delete_range
  status = db->DeleteRange(write_options, "key2532", "key10058");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 2220: put
  db->Put(write_options, "key3606", "value20308");

  // op 2221: put
  db->Put(write_options, "key10529", "value114346");

  // op 2222: get
  status = db->Get(read_options, "key8330", &value);
  if (status.ok()) {
    out << "GET key8330 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2223: delete_range
  status = db->DeleteRange(write_options, "key0089", "key0574");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 2224: put
  db->Put(write_options, "key9112", "value71572");

  // op 2225: delete_range
  status = db->DeleteRange(write_options, "key2644", "key9408");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 2226: put
  db->Put(write_options, "key7678", "value24601");

  // op 2227: delete_range
  status = db->DeleteRange(write_options, "key5336", "key10256");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 2228: put
  db->Put(write_options, "key7861", "value37362");

  // op 2229: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4392", "key5510", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4392, key5510) size=" << scan_result.size() << std::endl;
  }

  // op 2230: get
  status = db->Get(read_options, "key2100", &value);
  if (status.ok()) {
    out << "GET key2100 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2231: put
  db->Put(write_options, "key8622", "value59871");

  // op 2232: put
  db->Put(write_options, "key5929", "value111590");

  // op 2233: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3990", "key7231", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3990, key7231) size=" << scan_result.size() << std::endl;
  }

  // op 2234: delete_range
  status = db->DeleteRange(write_options, "key1539", "key2468");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 2235: put
  db->Put(write_options, "key2056", "value2772");

  // op 2236: put
  db->Put(write_options, "key3735", "value24011");

  // op 2237: put
  db->Put(write_options, "key9962", "value58439");

  // op 2238: put
  db->Put(write_options, "key7332", "value17249");

  // op 2239: get
  status = db->Get(read_options, "key8154", &value);
  if (status.ok()) {
    out << "GET key8154 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2240: get
  status = db->Get(read_options, "key7406", &value);
  if (status.ok()) {
    out << "GET key7406 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2241: put
  db->Put(write_options, "key4394", "value57829");

  // op 2242: put
  db->Put(write_options, "key2536", "value97398");

  // op 2243: get
  status = db->Get(read_options, "key8086", &value);
  if (status.ok()) {
    out << "GET key8086 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2244: put
  db->Put(write_options, "key0097", "value109465");

  // op 2245: delete_range
  status = db->DeleteRange(write_options, "key8539", "key11709");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 2246: put
  db->Put(write_options, "key7134", "value81500");

  // op 2247: put
  db->Put(write_options, "key9178", "value97241");

  // op 2248: get
  status = db->Get(read_options, "key5093", &value);
  if (status.ok()) {
    out << "GET key5093 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2249: put
  db->Put(write_options, "key5698", "value83803");

  // op 2250: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7393", "key7565", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7393, key7565) size=" << scan_result.size() << std::endl;
  }

  // op 2251: get
  status = db->Get(read_options, "key9996", &value);
  if (status.ok()) {
    out << "GET key9996 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2252: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 2253: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4146", "key8328", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4146, key8328) size=" << scan_result.size() << std::endl;
  }

  // op 2254: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2497", "key8059", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2497, key8059) size=" << scan_result.size() << std::endl;
  }

  // op 2255: put
  db->Put(write_options, "key1929", "value48725");

  // op 2256: put
  db->Put(write_options, "key5255", "value108368");

  // op 2257: get
  status = db->Get(read_options, "key3834", &value);
  if (status.ok()) {
    out << "GET key3834 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2258: put
  db->Put(write_options, "key2195", "value49242");

  // op 2259: put
  db->Put(write_options, "key1104", "value12437");

  // op 2260: delete_range
  status = db->DeleteRange(write_options, "key8355", "key9930");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 2261: get
  status = db->Get(read_options, "key2297", &value);
  if (status.ok()) {
    out << "GET key2297 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2262: get
  status = db->Get(read_options, "key2329", &value);
  if (status.ok()) {
    out << "GET key2329 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2263: get
  status = db->Get(read_options, "key8707", &value);
  if (status.ok()) {
    out << "GET key8707 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2264: put
  db->Put(write_options, "key4241", "value8603");

  // op 2265: delete_range
  status = db->DeleteRange(write_options, "key3920", "key7906");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 2266: put
  db->Put(write_options, "key0704", "value110762");

  // op 2267: put
  db->Put(write_options, "key8981", "value5781");

  // op 2268: delete
  db->Delete(write_options, "key6256");

  // op 2269: delete_range
  status = db->DeleteRange(write_options, "key2380", "key6496");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 2270: put
  db->Put(write_options, "key2045", "value35442");

  // op 2271: put
  db->Put(write_options, "key10556", "value95555");

  // op 2272: put
  db->Put(write_options, "key5775", "value19166");

  // op 2273: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2456", "key11587", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2456, key11587) size=" << scan_result.size() << std::endl;
  }

  // op 2274: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7869", "key8805", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7869, key8805) size=" << scan_result.size() << std::endl;
  }

  // op 2275: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4531", "key8338", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4531, key8338) size=" << scan_result.size() << std::endl;
  }

  // op 2276: get
  status = db->Get(read_options, "key0498", &value);
  if (status.ok()) {
    out << "GET key0498 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2277: put
  db->Put(write_options, "key10523", "value2626");

  // op 2278: put
  db->Put(write_options, "key8686", "value39832");

  // op 2279: put
  db->Put(write_options, "key5438", "value66945");

  // op 2280: put
  db->Put(write_options, "key9403", "value101861");

  // op 2281: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2593", "key7178", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2593, key7178) size=" << scan_result.size() << std::endl;
  }

  // op 2282: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0368", "key5388", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0368, key5388) size=" << scan_result.size() << std::endl;
  }

  // op 2283: put
  db->Put(write_options, "key1947", "value110192");

  // op 2284: put
  db->Put(write_options, "key6602", "value33153");

  // op 2285: get
  status = db->Get(read_options, "key0516", &value);
  if (status.ok()) {
    out << "GET key0516 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2286: put
  db->Put(write_options, "key11182", "value95247");

  // op 2287: put
  db->Put(write_options, "key3232", "value10780");

  // op 2288: put
  db->Put(write_options, "key5653", "value69533");

  // op 2289: put
  db->Put(write_options, "key2816", "value57186");

  // op 2290: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2663", "key8166", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2663, key8166) size=" << scan_result.size() << std::endl;
  }

  // op 2291: delete
  db->Delete(write_options, "key6063");

  // op 2292: put
  db->Put(write_options, "key9197", "value21211");

  // op 2293: get
  status = db->Get(read_options, "key0414", &value);
  if (status.ok()) {
    out << "GET key0414 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2294: get
  status = db->Get(read_options, "key0015", &value);
  if (status.ok()) {
    out << "GET key0015 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2295: put
  db->Put(write_options, "key2678", "value66904");

  // op 2296: delete_range
  status = db->DeleteRange(write_options, "key0895", "key1943");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 2297: delete
  db->Delete(write_options, "key1791");

  // op 2298: put
  db->Put(write_options, "key7356", "value92893");

  // op 2299: put
  db->Put(write_options, "key3934", "value2973");

  // op 2300: put
  db->Put(write_options, "key7816", "value78002");

  // op 2301: get
  status = db->Get(read_options, "key6045", &value);
  if (status.ok()) {
    out << "GET key6045 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2302: get
  status = db->Get(read_options, "key10486", &value);
  if (status.ok()) {
    out << "GET key10486 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2303: delete
  db->Delete(write_options, "key8456");

  // op 2304: get
  status = db->Get(read_options, "key1304", &value);
  if (status.ok()) {
    out << "GET key1304 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2305: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0265", "key5270", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0265, key5270) size=" << scan_result.size() << std::endl;
  }

  // op 2306: put
  db->Put(write_options, "key3883", "value35087");

  // op 2307: delete_range
  status = db->DeleteRange(write_options, "key3148", "key8789");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 2308: put
  db->Put(write_options, "key1063", "value95950");

  // op 2309: delete_range
  status = db->DeleteRange(write_options, "key3610", "key5791");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 2310: get
  status = db->Get(read_options, "key7235", &value);
  if (status.ok()) {
    out << "GET key7235 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2311: get
  status = db->Get(read_options, "key4937", &value);
  if (status.ok()) {
    out << "GET key4937 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2312: put
  db->Put(write_options, "key5030", "value19625");

  // op 2313: put
  db->Put(write_options, "key9117", "value98366");

  // op 2314: put
  db->Put(write_options, "key0976", "value94517");

  // op 2315: put
  db->Put(write_options, "key5553", "value64577");

  // op 2316: get
  status = db->Get(read_options, "key10557", &value);
  if (status.ok()) {
    out << "GET key10557 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2317: get
  status = db->Get(read_options, "key1086", &value);
  if (status.ok()) {
    out << "GET key1086 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2318: put
  db->Put(write_options, "key3833", "value94357");

  // op 2319: delete_range
  status = db->DeleteRange(write_options, "key0258", "key6369");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 2320: get
  status = db->Get(read_options, "key10847", &value);
  if (status.ok()) {
    out << "GET key10847 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2321: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6271", "key8410", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6271, key8410) size=" << scan_result.size() << std::endl;
  }

  // op 2322: delete_range
  status = db->DeleteRange(write_options, "key3866", "key10826");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 2323: put
  db->Put(write_options, "key4907", "value76506");

  // op 2324: get
  status = db->Get(read_options, "key7421", &value);
  if (status.ok()) {
    out << "GET key7421 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2325: put
  db->Put(write_options, "key4275", "value100548");

  // op 2326: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1543", "key2711", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1543, key2711) size=" << scan_result.size() << std::endl;
  }

  // op 2327: put
  db->Put(write_options, "key0056", "value34050");

  // op 2328: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4020", "key5442", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4020, key5442) size=" << scan_result.size() << std::endl;
  }

  // op 2329: delete
  db->Delete(write_options, "key7513");

  // op 2330: put
  db->Put(write_options, "key8242", "value13266");

  // op 2331: put
  db->Put(write_options, "key11709", "value71160");

  // op 2332: put
  db->Put(write_options, "key1866", "value32932");

  // op 2333: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3520", "key6522", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3520, key6522) size=" << scan_result.size() << std::endl;
  }

  // op 2334: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0752", "key9583", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0752, key9583) size=" << scan_result.size() << std::endl;
  }

  // op 2335: delete_range
  status = db->DeleteRange(write_options, "key1953", "key8909");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 2336: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0737", "key6838", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0737, key6838) size=" << scan_result.size() << std::endl;
  }

  // op 2337: put
  db->Put(write_options, "key1619", "value22533");

  // op 2338: put
  db->Put(write_options, "key6138", "value3561");

  // op 2339: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3084", "key9527", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3084, key9527) size=" << scan_result.size() << std::endl;
  }

  // op 2340: put
  db->Put(write_options, "key6681", "value15331");

  // op 2341: put
  db->Put(write_options, "key10575", "value99856");

  // op 2342: get
  status = db->Get(read_options, "key1939", &value);
  if (status.ok()) {
    out << "GET key1939 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2343: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5738", "key10802", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5738, key10802) size=" << scan_result.size() << std::endl;
  }

  // op 2344: put
  db->Put(write_options, "key11134", "value76965");

  // op 2345: get
  status = db->Get(read_options, "key8311", &value);
  if (status.ok()) {
    out << "GET key8311 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2346: delete_range
  status = db->DeleteRange(write_options, "key4626", "key9608");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 2347: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0440", "key9533", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0440, key9533) size=" << scan_result.size() << std::endl;
  }

  // op 2348: put
  db->Put(write_options, "key4738", "value23510");

  // op 2349: delete
  db->Delete(write_options, "key7163");

  // op 2350: get
  status = db->Get(read_options, "key6455", &value);
  if (status.ok()) {
    out << "GET key6455 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2351: delete_range
  status = db->DeleteRange(write_options, "key3524", "key5115");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 2352: put
  db->Put(write_options, "key5088", "value65399");

  // op 2353: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2365", "key9722", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2365, key9722) size=" << scan_result.size() << std::endl;
  }

  // op 2354: put
  db->Put(write_options, "key6446", "value77170");

  // op 2355: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 2356: put
  db->Put(write_options, "key2310", "value102680");

  // op 2357: get
  status = db->Get(read_options, "key2957", &value);
  if (status.ok()) {
    out << "GET key2957 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2358: get
  status = db->Get(read_options, "key8941", &value);
  if (status.ok()) {
    out << "GET key8941 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2359: get
  status = db->Get(read_options, "key10413", &value);
  if (status.ok()) {
    out << "GET key10413 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2360: get
  status = db->Get(read_options, "key3594", &value);
  if (status.ok()) {
    out << "GET key3594 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2361: put
  db->Put(write_options, "key3836", "value31369");

  // op 2362: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3727", "key11344", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3727, key11344) size=" << scan_result.size() << std::endl;
  }

  // op 2363: delete
  db->Delete(write_options, "key3297");

  // op 2364: get
  status = db->Get(read_options, "key3106", &value);
  if (status.ok()) {
    out << "GET key3106 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2365: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5091", "key7895", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5091, key7895) size=" << scan_result.size() << std::endl;
  }

  // op 2366: put
  db->Put(write_options, "key7995", "value95790");

  // op 2367: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 2368: put
  db->Put(write_options, "key6880", "value12630");

  // op 2369: delete_range
  status = db->DeleteRange(write_options, "key1987", "key8525");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 2370: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1094", "key2692", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1094, key2692) size=" << scan_result.size() << std::endl;
  }

  // op 2371: delete_range
  status = db->DeleteRange(write_options, "key6598", "key10473");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 2372: get
  status = db->Get(read_options, "key8793", &value);
  if (status.ok()) {
    out << "GET key8793 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2373: get
  status = db->Get(read_options, "key4931", &value);
  if (status.ok()) {
    out << "GET key4931 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2374: put
  db->Put(write_options, "key4283", "value23666");

  // op 2375: put
  db->Put(write_options, "key3453", "value20368");

  // op 2376: get
  status = db->Get(read_options, "key9635", &value);
  if (status.ok()) {
    out << "GET key9635 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2377: get
  status = db->Get(read_options, "key8245", &value);
  if (status.ok()) {
    out << "GET key8245 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2378: get
  status = db->Get(read_options, "key9362", &value);
  if (status.ok()) {
    out << "GET key9362 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2379: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3529", "key4374", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3529, key4374) size=" << scan_result.size() << std::endl;
  }

  // op 2380: put
  db->Put(write_options, "key1935", "value36663");

  // op 2381: put
  db->Put(write_options, "key11416", "value28830");

  // op 2382: delete_range
  status = db->DeleteRange(write_options, "key6982", "key9813");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 2383: get
  status = db->Get(read_options, "key1518", &value);
  if (status.ok()) {
    out << "GET key1518 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2384: put
  db->Put(write_options, "key2874", "value10638");

  // op 2385: delete
  db->Delete(write_options, "key7084");

  // op 2386: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 2387: delete_range
  status = db->DeleteRange(write_options, "key4884", "key7252");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 2388: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1751", "key7650", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1751, key7650) size=" << scan_result.size() << std::endl;
  }

  // op 2389: put
  db->Put(write_options, "key4734", "value84226");

  // op 2390: put
  db->Put(write_options, "key0736", "value94672");

  // op 2391: put
  db->Put(write_options, "key9149", "value92656");

  // op 2392: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9247", "key10799", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9247, key10799) size=" << scan_result.size() << std::endl;
  }

  // op 2393: put
  db->Put(write_options, "key11203", "value63849");

  // op 2394: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 2395: delete_range
  status = db->DeleteRange(write_options, "key3136", "key7067");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 2396: get
  status = db->Get(read_options, "key8018", &value);
  if (status.ok()) {
    out << "GET key8018 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2397: put
  db->Put(write_options, "key10415", "value67397");

  // op 2398: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3935", "key9882", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3935, key9882) size=" << scan_result.size() << std::endl;
  }

  // op 2399: delete
  db->Delete(write_options, "key1933");

  // op 2400: delete_range
  status = db->DeleteRange(write_options, "key6120", "key8623");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 2401: delete_range
  status = db->DeleteRange(write_options, "key7368", "key10127");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 2402: delete_range
  status = db->DeleteRange(write_options, "key1921", "key10996");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 2403: put
  db->Put(write_options, "key1702", "value48263");

  // op 2404: delete_range
  status = db->DeleteRange(write_options, "key8287", "key10401");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 2405: delete
  db->Delete(write_options, "key0532");

  // op 2406: delete_range
  status = db->DeleteRange(write_options, "key6307", "key9713");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 2407: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 2408: put
  db->Put(write_options, "key8482", "value39690");

  // op 2409: put
  db->Put(write_options, "key11211", "value60508");

  // op 2410: put
  db->Put(write_options, "key1790", "value91970");

  // op 2411: put
  db->Put(write_options, "key2766", "value72020");

  // op 2412: delete_range
  status = db->DeleteRange(write_options, "key4203", "key8952");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 2413: get
  status = db->Get(read_options, "key7473", &value);
  if (status.ok()) {
    out << "GET key7473 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2414: delete_range
  status = db->DeleteRange(write_options, "key0790", "key6098");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 2415: put
  db->Put(write_options, "key8642", "value62736");

  // op 2416: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0911", "key10811", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0911, key10811) size=" << scan_result.size() << std::endl;
  }

  // op 2417: put
  db->Put(write_options, "key2638", "value79840");

  // op 2418: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2770", "key3600", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2770, key3600) size=" << scan_result.size() << std::endl;
  }

  // op 2419: put
  db->Put(write_options, "key2611", "value86150");

  // op 2420: put
  db->Put(write_options, "key10468", "value92515");

  // op 2421: delete
  db->Delete(write_options, "key8036");

  // op 2422: put
  db->Put(write_options, "key9131", "value66073");

  // op 2423: get
  status = db->Get(read_options, "key4666", &value);
  if (status.ok()) {
    out << "GET key4666 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2424: put
  db->Put(write_options, "key5452", "value31292");

  // op 2425: delete_range
  status = db->DeleteRange(write_options, "key7424", "key9165");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 2426: get
  status = db->Get(read_options, "key8799", &value);
  if (status.ok()) {
    out << "GET key8799 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2427: delete
  db->Delete(write_options, "key9834");

  // op 2428: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7067", "key9012", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7067, key9012) size=" << scan_result.size() << std::endl;
  }

  // op 2429: put
  db->Put(write_options, "key1055", "value2329");

  // op 2430: put
  db->Put(write_options, "key10564", "value24643");

  // op 2431: delete
  db->Delete(write_options, "key8020");

  // op 2432: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0052", "key3827", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0052, key3827) size=" << scan_result.size() << std::endl;
  }

  // op 2433: put
  db->Put(write_options, "key7367", "value31865");

  // op 2434: get
  status = db->Get(read_options, "key1165", &value);
  if (status.ok()) {
    out << "GET key1165 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2435: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2554", "key7218", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2554, key7218) size=" << scan_result.size() << std::endl;
  }

  // op 2436: put
  db->Put(write_options, "key9289", "value11870");

  // op 2437: put
  db->Put(write_options, "key3054", "value7492");

  // op 2438: put
  db->Put(write_options, "key2060", "value107560");

  // op 2439: delete_range
  status = db->DeleteRange(write_options, "key7317", "key7956");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 2440: get
  status = db->Get(read_options, "key5415", &value);
  if (status.ok()) {
    out << "GET key5415 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2441: delete
  db->Delete(write_options, "key5379");

  // op 2442: put
  db->Put(write_options, "key10563", "value30456");

  // op 2443: delete_range
  status = db->DeleteRange(write_options, "key2269", "key9819");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 2444: delete
  db->Delete(write_options, "key3997");

  // op 2445: get
  status = db->Get(read_options, "key10853", &value);
  if (status.ok()) {
    out << "GET key10853 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2446: delete
  db->Delete(write_options, "key10191");

  // op 2447: put
  db->Put(write_options, "key9848", "value60604");

  // op 2448: get
  status = db->Get(read_options, "key4248", &value);
  if (status.ok()) {
    out << "GET key4248 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2449: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1246", "key7103", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1246, key7103) size=" << scan_result.size() << std::endl;
  }

  // op 2450: delete_range
  status = db->DeleteRange(write_options, "key1450", "key8808");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 2451: put
  db->Put(write_options, "key6613", "value79154");

  // op 2452: put
  db->Put(write_options, "key9722", "value87601");

  // op 2453: put
  db->Put(write_options, "key11308", "value88500");

  // op 2454: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 2455: delete
  db->Delete(write_options, "key5950");

  // op 2456: delete_range
  status = db->DeleteRange(write_options, "key0529", "key6101");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 2457: put
  db->Put(write_options, "key5797", "value22774");

  // op 2458: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 2459: put
  db->Put(write_options, "key1815", "value9735");

  // op 2460: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5330", "key11466", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5330, key11466) size=" << scan_result.size() << std::endl;
  }

  // op 2461: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5290", "key7607", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5290, key7607) size=" << scan_result.size() << std::endl;
  }

  // op 2462: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5311", "key8101", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5311, key8101) size=" << scan_result.size() << std::endl;
  }

  // op 2463: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2447", "key5418", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2447, key5418) size=" << scan_result.size() << std::endl;
  }

  // op 2464: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 2465: put
  db->Put(write_options, "key11270", "value74661");

  // op 2466: put
  db->Put(write_options, "key0931", "value81379");

  // op 2467: delete_range
  status = db->DeleteRange(write_options, "key1537", "key9040");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 2468: put
  db->Put(write_options, "key4214", "value95983");

  // op 2469: put
  db->Put(write_options, "key10860", "value57119");

  // op 2470: get
  status = db->Get(read_options, "key4065", &value);
  if (status.ok()) {
    out << "GET key4065 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2471: put
  db->Put(write_options, "key10501", "value1513");

  // op 2472: put
  db->Put(write_options, "key1111", "value9329");

  // op 2473: delete_range
  status = db->DeleteRange(write_options, "key6160", "key9737");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 2474: put
  db->Put(write_options, "key8942", "value78475");

  // op 2475: put
  db->Put(write_options, "key10162", "value10738");

  // op 2476: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6033", "key9907", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6033, key9907) size=" << scan_result.size() << std::endl;
  }

  // op 2477: delete
  db->Delete(write_options, "key0256");

  // op 2478: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0019", "key8243", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0019, key8243) size=" << scan_result.size() << std::endl;
  }

  // op 2479: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1085", "key11273", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1085, key11273) size=" << scan_result.size() << std::endl;
  }

  // op 2480: delete
  db->Delete(write_options, "key2767");

  // op 2481: delete_range
  status = db->DeleteRange(write_options, "key4924", "key5344");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 2482: get
  status = db->Get(read_options, "key9342", &value);
  if (status.ok()) {
    out << "GET key9342 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2483: get
  status = db->Get(read_options, "key6744", &value);
  if (status.ok()) {
    out << "GET key6744 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2484: put
  db->Put(write_options, "key1532", "value95510");

  // op 2485: put
  db->Put(write_options, "key4319", "value20127");

  // op 2486: delete_range
  status = db->DeleteRange(write_options, "key8356", "key10996");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 2487: get
  status = db->Get(read_options, "key9139", &value);
  if (status.ok()) {
    out << "GET key9139 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2488: get
  status = db->Get(read_options, "key0844", &value);
  if (status.ok()) {
    out << "GET key0844 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2489: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3459", "key6902", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3459, key6902) size=" << scan_result.size() << std::endl;
  }

  // op 2490: delete_range
  status = db->DeleteRange(write_options, "key3572", "key7385");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 2491: delete
  db->Delete(write_options, "key8231");

  // op 2492: put
  db->Put(write_options, "key8962", "value16547");

  // op 2493: put
  db->Put(write_options, "key8687", "value34472");

  // op 2494: put
  db->Put(write_options, "key1245", "value85033");

  // op 2495: get
  status = db->Get(read_options, "key10228", &value);
  if (status.ok()) {
    out << "GET key10228 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2496: put
  db->Put(write_options, "key2512", "value39883");

  // op 2497: get
  status = db->Get(read_options, "key7296", &value);
  if (status.ok()) {
    out << "GET key7296 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2498: put
  db->Put(write_options, "key4483", "value61868");

  // op 2499: get
  status = db->Get(read_options, "key7708", &value);
  if (status.ok()) {
    out << "GET key7708 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2500: put
  db->Put(write_options, "key10159", "value30297");

  // op 2501: get
  status = db->Get(read_options, "key3133", &value);
  if (status.ok()) {
    out << "GET key3133 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2502: put
  db->Put(write_options, "key11669", "value86221");

  // op 2503: get
  status = db->Get(read_options, "key9274", &value);
  if (status.ok()) {
    out << "GET key9274 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2504: delete_range
  status = db->DeleteRange(write_options, "key3325", "key11499");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 2505: put
  db->Put(write_options, "key5295", "value96190");

  // op 2506: get
  status = db->Get(read_options, "key9172", &value);
  if (status.ok()) {
    out << "GET key9172 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2507: put
  db->Put(write_options, "key5466", "value73978");

  // op 2508: delete
  db->Delete(write_options, "key1658");

  // op 2509: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3593", "key4817", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3593, key4817) size=" << scan_result.size() << std::endl;
  }

  // op 2510: put
  db->Put(write_options, "key1486", "value95625");

  // op 2511: put
  db->Put(write_options, "key2099", "value92125");

  // op 2512: get
  status = db->Get(read_options, "key9503", &value);
  if (status.ok()) {
    out << "GET key9503 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2513: put
  db->Put(write_options, "key2106", "value107277");

  // op 2514: put
  db->Put(write_options, "key7079", "value95168");

  // op 2515: get
  status = db->Get(read_options, "key3871", &value);
  if (status.ok()) {
    out << "GET key3871 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2516: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2110", "key11150", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2110, key11150) size=" << scan_result.size() << std::endl;
  }

  // op 2517: delete
  db->Delete(write_options, "key0885");

  // op 2518: put
  db->Put(write_options, "key9258", "value0812");

  // op 2519: delete_range
  status = db->DeleteRange(write_options, "key2406", "key6716");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 2520: put
  db->Put(write_options, "key4157", "value53638");

  // op 2521: put
  db->Put(write_options, "key9716", "value2271");

  // op 2522: get
  status = db->Get(read_options, "key3228", &value);
  if (status.ok()) {
    out << "GET key3228 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2523: get
  status = db->Get(read_options, "key7060", &value);
  if (status.ok()) {
    out << "GET key7060 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2524: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0436", "key4798", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0436, key4798) size=" << scan_result.size() << std::endl;
  }

  // op 2525: put
  db->Put(write_options, "key10133", "value117686");

  // op 2526: get
  status = db->Get(read_options, "key3157", &value);
  if (status.ok()) {
    out << "GET key3157 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2527: put
  db->Put(write_options, "key3848", "value56979");

  // op 2528: put
  db->Put(write_options, "key7319", "value10743");

  // op 2529: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 2530: get
  status = db->Get(read_options, "key11539", &value);
  if (status.ok()) {
    out << "GET key11539 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2531: put
  db->Put(write_options, "key10537", "value66852");

  // op 2532: delete_range
  status = db->DeleteRange(write_options, "key3546", "key9298");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 2533: delete
  db->Delete(write_options, "key4187");

  // op 2534: get
  status = db->Get(read_options, "key3959", &value);
  if (status.ok()) {
    out << "GET key3959 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2535: get
  status = db->Get(read_options, "key6172", &value);
  if (status.ok()) {
    out << "GET key6172 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2536: put
  db->Put(write_options, "key11077", "value26347");

  // op 2537: put
  db->Put(write_options, "key5950", "value44536");

  // op 2538: delete_range
  status = db->DeleteRange(write_options, "key10457", "key11008");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 2539: get
  status = db->Get(read_options, "key7668", &value);
  if (status.ok()) {
    out << "GET key7668 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2540: get
  status = db->Get(read_options, "key10648", &value);
  if (status.ok()) {
    out << "GET key10648 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2541: put
  db->Put(write_options, "key1147", "value47124");

  // op 2542: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0291", "key5763", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0291, key5763) size=" << scan_result.size() << std::endl;
  }

  // op 2543: get
  status = db->Get(read_options, "key2917", &value);
  if (status.ok()) {
    out << "GET key2917 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2544: delete
  db->Delete(write_options, "key11000");

  // op 2545: delete
  db->Delete(write_options, "key6224");

  // op 2546: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2613", "key8003", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2613, key8003) size=" << scan_result.size() << std::endl;
  }

  // op 2547: delete_range
  status = db->DeleteRange(write_options, "key3854", "key5642");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 2548: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6438", "key8014", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6438, key8014) size=" << scan_result.size() << std::endl;
  }

  // op 2549: put
  db->Put(write_options, "key10843", "value58029");

  // op 2550: put
  db->Put(write_options, "key4567", "value117642");

  // op 2551: put
  db->Put(write_options, "key11196", "value48785");

  // op 2552: put
  db->Put(write_options, "key0392", "value64086");

  // op 2553: get
  status = db->Get(read_options, "key10200", &value);
  if (status.ok()) {
    out << "GET key10200 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2554: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2532", "key9021", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2532, key9021) size=" << scan_result.size() << std::endl;
  }

  // op 2555: put
  db->Put(write_options, "key3402", "value95564");

  // op 2556: put
  db->Put(write_options, "key7443", "value50328");

  // op 2557: get
  status = db->Get(read_options, "key0733", &value);
  if (status.ok()) {
    out << "GET key0733 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2558: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4532", "key7778", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4532, key7778) size=" << scan_result.size() << std::endl;
  }

  // op 2559: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2680", "key4739", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2680, key4739) size=" << scan_result.size() << std::endl;
  }

  // op 2560: get
  status = db->Get(read_options, "key6706", &value);
  if (status.ok()) {
    out << "GET key6706 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2561: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7447", "key9060", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7447, key9060) size=" << scan_result.size() << std::endl;
  }

  // op 2562: delete
  db->Delete(write_options, "key5373");

  // op 2563: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4869", "key9330", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4869, key9330) size=" << scan_result.size() << std::endl;
  }

  // op 2564: put
  db->Put(write_options, "key7210", "value27878");

  // op 2565: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5553", "key9342", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5553, key9342) size=" << scan_result.size() << std::endl;
  }

  // op 2566: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 2567: get
  status = db->Get(read_options, "key4961", &value);
  if (status.ok()) {
    out << "GET key4961 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2568: put
  db->Put(write_options, "key11032", "value50257");

  // op 2569: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 2570: delete_range
  status = db->DeleteRange(write_options, "key0418", "key0909");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 2571: put
  db->Put(write_options, "key3050", "value64020");

  // op 2572: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7985", "key8574", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7985, key8574) size=" << scan_result.size() << std::endl;
  }

  // op 2573: get
  status = db->Get(read_options, "key10998", &value);
  if (status.ok()) {
    out << "GET key10998 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2574: get
  status = db->Get(read_options, "key5950", &value);
  if (status.ok()) {
    out << "GET key5950 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2575: get
  status = db->Get(read_options, "key3899", &value);
  if (status.ok()) {
    out << "GET key3899 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2576: delete
  db->Delete(write_options, "key9036");

  // op 2577: delete
  db->Delete(write_options, "key2675");

  // op 2578: put
  db->Put(write_options, "key9042", "value80163");

  // op 2579: delete_range
  status = db->DeleteRange(write_options, "key3476", "key9669");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 2580: put
  db->Put(write_options, "key2406", "value100946");

  // op 2581: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 2582: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1259", "key9035", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1259, key9035) size=" << scan_result.size() << std::endl;
  }

  // op 2583: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 2584: put
  db->Put(write_options, "key8838", "value36872");

  // op 2585: delete
  db->Delete(write_options, "key1566");

  // op 2586: put
  db->Put(write_options, "key7559", "value23704");

  // op 2587: get
  status = db->Get(read_options, "key2926", &value);
  if (status.ok()) {
    out << "GET key2926 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2588: put
  db->Put(write_options, "key4011", "value26399");

  // op 2589: get
  status = db->Get(read_options, "key7852", &value);
  if (status.ok()) {
    out << "GET key7852 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2590: delete
  db->Delete(write_options, "key5727");

  // op 2591: delete
  db->Delete(write_options, "key10496");

  // op 2592: put
  db->Put(write_options, "key10890", "value91378");

  // op 2593: put
  db->Put(write_options, "key1805", "value65390");

  // op 2594: put
  db->Put(write_options, "key10867", "value94423");

  // op 2595: get
  status = db->Get(read_options, "key0763", &value);
  if (status.ok()) {
    out << "GET key0763 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2596: delete_range
  status = db->DeleteRange(write_options, "key2758", "key10922");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 2597: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3724", "key9742", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3724, key9742) size=" << scan_result.size() << std::endl;
  }

  // op 2598: delete
  db->Delete(write_options, "key3971");

  // op 2599: get
  status = db->Get(read_options, "key11403", &value);
  if (status.ok()) {
    out << "GET key11403 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2600: put
  db->Put(write_options, "key10044", "value45095");

  // op 2601: put
  db->Put(write_options, "key8025", "value60631");

  // op 2602: put
  db->Put(write_options, "key11378", "value117388");

  // op 2603: put
  db->Put(write_options, "key5249", "value97209");

  // op 2604: delete
  db->Delete(write_options, "key1607");

  // op 2605: get
  status = db->Get(read_options, "key1733", &value);
  if (status.ok()) {
    out << "GET key1733 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2606: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2730", "key5202", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2730, key5202) size=" << scan_result.size() << std::endl;
  }

  // op 2607: get
  status = db->Get(read_options, "key10988", &value);
  if (status.ok()) {
    out << "GET key10988 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2608: delete_range
  status = db->DeleteRange(write_options, "key1996", "key11361");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 2609: delete
  db->Delete(write_options, "key7057");

  // op 2610: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3764", "key4801", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3764, key4801) size=" << scan_result.size() << std::endl;
  }

  // op 2611: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 2612: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 2613: get
  status = db->Get(read_options, "key10244", &value);
  if (status.ok()) {
    out << "GET key10244 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2614: put
  db->Put(write_options, "key3862", "value34626");

  // op 2615: delete_range
  status = db->DeleteRange(write_options, "key1286", "key5445");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 2616: delete
  db->Delete(write_options, "key6736");

  // op 2617: put
  db->Put(write_options, "key11283", "value113258");

  // op 2618: get
  status = db->Get(read_options, "key1029", &value);
  if (status.ok()) {
    out << "GET key1029 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2619: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 2620: get
  status = db->Get(read_options, "key6284", &value);
  if (status.ok()) {
    out << "GET key6284 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2621: delete_range
  status = db->DeleteRange(write_options, "key6182", "key9259");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 2622: delete
  db->Delete(write_options, "key1057");

  // op 2623: put
  db->Put(write_options, "key4954", "value39505");

  // op 2624: put
  db->Put(write_options, "key10443", "value3319");

  // op 2625: get
  status = db->Get(read_options, "key2505", &value);
  if (status.ok()) {
    out << "GET key2505 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2626: put
  db->Put(write_options, "key6962", "value78057");

  // op 2627: delete
  db->Delete(write_options, "key8077");

  // op 2628: get
  status = db->Get(read_options, "key11420", &value);
  if (status.ok()) {
    out << "GET key11420 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2629: get
  status = db->Get(read_options, "key1764", &value);
  if (status.ok()) {
    out << "GET key1764 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2630: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2479", "key10280", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2479, key10280) size=" << scan_result.size() << std::endl;
  }

  // op 2631: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7755", "key8788", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7755, key8788) size=" << scan_result.size() << std::endl;
  }

  // op 2632: delete
  db->Delete(write_options, "key3332");

  // op 2633: put
  db->Put(write_options, "key3964", "value83706");

  // op 2634: delete_range
  status = db->DeleteRange(write_options, "key6377", "key9372");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 2635: put
  db->Put(write_options, "key2280", "value117395");

  // op 2636: put
  db->Put(write_options, "key1255", "value16937");

  // op 2637: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6594", "key7882", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6594, key7882) size=" << scan_result.size() << std::endl;
  }

  // op 2638: put
  db->Put(write_options, "key10954", "value90566");

  // op 2639: get
  status = db->Get(read_options, "key0550", &value);
  if (status.ok()) {
    out << "GET key0550 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2640: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4357", "key9987", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4357, key9987) size=" << scan_result.size() << std::endl;
  }

  // op 2641: put
  db->Put(write_options, "key0983", "value70414");

  // op 2642: delete
  db->Delete(write_options, "key2850");

  // op 2643: get
  status = db->Get(read_options, "key10193", &value);
  if (status.ok()) {
    out << "GET key10193 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2644: put
  db->Put(write_options, "key4916", "value28425");

  // op 2645: delete_range
  status = db->DeleteRange(write_options, "key3252", "key9754");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 2646: get
  status = db->Get(read_options, "key4256", &value);
  if (status.ok()) {
    out << "GET key4256 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2647: get
  status = db->Get(read_options, "key11466", &value);
  if (status.ok()) {
    out << "GET key11466 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2648: put
  db->Put(write_options, "key5347", "value56903");

  // op 2649: get
  status = db->Get(read_options, "key2915", &value);
  if (status.ok()) {
    out << "GET key2915 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2650: put
  db->Put(write_options, "key11669", "value38474");

  // op 2651: delete_range
  status = db->DeleteRange(write_options, "key8545", "key9711");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 2652: put
  db->Put(write_options, "key3431", "value50653");

  // op 2653: put
  db->Put(write_options, "key10389", "value53218");

  // op 2654: delete
  db->Delete(write_options, "key5631");

  // op 2655: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 2656: get
  status = db->Get(read_options, "key11007", &value);
  if (status.ok()) {
    out << "GET key11007 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2657: put
  db->Put(write_options, "key7408", "value18629");

  // op 2658: delete_range
  status = db->DeleteRange(write_options, "key6266", "key10509");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 2659: get
  status = db->Get(read_options, "key2868", &value);
  if (status.ok()) {
    out << "GET key2868 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2660: put
  db->Put(write_options, "key10758", "value0343");

  // op 2661: put
  db->Put(write_options, "key0382", "value2944");

  // op 2662: delete_range
  status = db->DeleteRange(write_options, "key2540", "key4214");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 2663: get
  status = db->Get(read_options, "key2480", &value);
  if (status.ok()) {
    out << "GET key2480 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2664: delete
  db->Delete(write_options, "key6504");

  // op 2665: get
  status = db->Get(read_options, "key11634", &value);
  if (status.ok()) {
    out << "GET key11634 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2666: put
  db->Put(write_options, "key10890", "value22944");

  // op 2667: delete_range
  status = db->DeleteRange(write_options, "key5410", "key8489");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 2668: delete
  db->Delete(write_options, "key6630");

  // op 2669: get
  status = db->Get(read_options, "key7042", &value);
  if (status.ok()) {
    out << "GET key7042 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2670: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1404", "key11013", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1404, key11013) size=" << scan_result.size() << std::endl;
  }

  // op 2671: delete_range
  status = db->DeleteRange(write_options, "key2555", "key8994");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 2672: put
  db->Put(write_options, "key3118", "value22835");

  // op 2673: get
  status = db->Get(read_options, "key9762", &value);
  if (status.ok()) {
    out << "GET key9762 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2674: get
  status = db->Get(read_options, "key8827", &value);
  if (status.ok()) {
    out << "GET key8827 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2675: delete_range
  status = db->DeleteRange(write_options, "key0016", "key11312");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 2676: put
  db->Put(write_options, "key5597", "value52148");

  // op 2677: put
  db->Put(write_options, "key3666", "value35347");

  // op 2678: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 2679: put
  db->Put(write_options, "key2181", "value48062");

  // op 2680: delete
  db->Delete(write_options, "key2611");

  // op 2681: put
  db->Put(write_options, "key0009", "value78354");

  // op 2682: put
  db->Put(write_options, "key1339", "value112079");

  // op 2683: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9152", "key11388", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9152, key11388) size=" << scan_result.size() << std::endl;
  }

  // op 2684: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0136", "key8288", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0136, key8288) size=" << scan_result.size() << std::endl;
  }

  // op 2685: put
  db->Put(write_options, "key5166", "value38824");

  // op 2686: put
  db->Put(write_options, "key6533", "value96365");

  // op 2687: get
  status = db->Get(read_options, "key6725", &value);
  if (status.ok()) {
    out << "GET key6725 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2688: get
  status = db->Get(read_options, "key6078", &value);
  if (status.ok()) {
    out << "GET key6078 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2689: delete
  db->Delete(write_options, "key4026");

  // op 2690: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6499", "key6726", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6499, key6726) size=" << scan_result.size() << std::endl;
  }

  // op 2691: put
  db->Put(write_options, "key0528", "value88210");

  // op 2692: put
  db->Put(write_options, "key11445", "value15183");

  // op 2693: get
  status = db->Get(read_options, "key2409", &value);
  if (status.ok()) {
    out << "GET key2409 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2694: put
  db->Put(write_options, "key8657", "value10093");

  // op 2695: get
  status = db->Get(read_options, "key1209", &value);
  if (status.ok()) {
    out << "GET key1209 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2696: put
  db->Put(write_options, "key0947", "value22901");

  // op 2697: put
  db->Put(write_options, "key0157", "value46652");

  // op 2698: put
  db->Put(write_options, "key8420", "value27228");

  // op 2699: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 2700: put
  db->Put(write_options, "key0600", "value22818");

  // op 2701: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2768", "key9822", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2768, key9822) size=" << scan_result.size() << std::endl;
  }

  // op 2702: put
  db->Put(write_options, "key10686", "value58998");

  // op 2703: get
  status = db->Get(read_options, "key8358", &value);
  if (status.ok()) {
    out << "GET key8358 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2704: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3177", "key4731", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3177, key4731) size=" << scan_result.size() << std::endl;
  }

  // op 2705: get
  status = db->Get(read_options, "key2388", &value);
  if (status.ok()) {
    out << "GET key2388 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2706: get
  status = db->Get(read_options, "key3395", &value);
  if (status.ok()) {
    out << "GET key3395 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2707: put
  db->Put(write_options, "key1002", "value30545");

  // op 2708: put
  db->Put(write_options, "key10959", "value42543");

  // op 2709: put
  db->Put(write_options, "key6805", "value14018");

  // op 2710: delete_range
  status = db->DeleteRange(write_options, "key3048", "key7535");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 2711: put
  db->Put(write_options, "key11519", "value59930");

  // op 2712: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1378", "key9422", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1378, key9422) size=" << scan_result.size() << std::endl;
  }

  // op 2713: get
  status = db->Get(read_options, "key2337", &value);
  if (status.ok()) {
    out << "GET key2337 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2714: delete
  db->Delete(write_options, "key10307");

  // op 2715: put
  db->Put(write_options, "key8337", "value99515");

  // op 2716: put
  db->Put(write_options, "key11046", "value12631");

  // op 2717: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2159", "key3851", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2159, key3851) size=" << scan_result.size() << std::endl;
  }

  // op 2718: put
  db->Put(write_options, "key3575", "value82760");

  // op 2719: put
  db->Put(write_options, "key10697", "value94497");

  // op 2720: put
  db->Put(write_options, "key4117", "value9573");

  // op 2721: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3537", "key4954", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3537, key4954) size=" << scan_result.size() << std::endl;
  }

  // op 2722: put
  db->Put(write_options, "key0487", "value23402");

  // op 2723: put
  db->Put(write_options, "key5155", "value3352");

  // op 2724: get
  status = db->Get(read_options, "key6278", &value);
  if (status.ok()) {
    out << "GET key6278 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2725: delete
  db->Delete(write_options, "key8828");

  // op 2726: put
  db->Put(write_options, "key5637", "value4720");

  // op 2727: delete_range
  status = db->DeleteRange(write_options, "key0931", "key7212");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 2728: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8983", "key10675", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8983, key10675) size=" << scan_result.size() << std::endl;
  }

  // op 2729: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0520", "key5028", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0520, key5028) size=" << scan_result.size() << std::endl;
  }

  // op 2730: get
  status = db->Get(read_options, "key10440", &value);
  if (status.ok()) {
    out << "GET key10440 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2731: put
  db->Put(write_options, "key2742", "value80324");

  // op 2732: put
  db->Put(write_options, "key10643", "value44594");

  // op 2733: delete_range
  status = db->DeleteRange(write_options, "key1373", "key3732");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 2734: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6694", "key7406", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6694, key7406) size=" << scan_result.size() << std::endl;
  }

  // op 2735: put
  db->Put(write_options, "key7316", "value74163");

  // op 2736: put
  db->Put(write_options, "key1093", "value42923");

  // op 2737: put
  db->Put(write_options, "key6468", "value62086");

  // op 2738: put
  db->Put(write_options, "key6019", "value68628");

  // op 2739: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3204", "key10333", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3204, key10333) size=" << scan_result.size() << std::endl;
  }

  // op 2740: delete_range
  status = db->DeleteRange(write_options, "key9145", "key10823");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 2741: get
  status = db->Get(read_options, "key1661", &value);
  if (status.ok()) {
    out << "GET key1661 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2742: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3826", "key6285", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3826, key6285) size=" << scan_result.size() << std::endl;
  }

  // op 2743: get
  status = db->Get(read_options, "key3383", &value);
  if (status.ok()) {
    out << "GET key3383 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2744: delete_range
  status = db->DeleteRange(write_options, "key2163", "key2940");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 2745: put
  db->Put(write_options, "key9634", "value35241");

  // op 2746: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1899", "key9720", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1899, key9720) size=" << scan_result.size() << std::endl;
  }

  // op 2747: put
  db->Put(write_options, "key4429", "value17555");

  // op 2748: delete
  db->Delete(write_options, "key5170");

  // op 2749: put
  db->Put(write_options, "key1938", "value56493");

  // op 2750: delete
  db->Delete(write_options, "key2229");

  // op 2751: get
  status = db->Get(read_options, "key2962", &value);
  if (status.ok()) {
    out << "GET key2962 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2752: get
  status = db->Get(read_options, "key8954", &value);
  if (status.ok()) {
    out << "GET key8954 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2753: delete_range
  status = db->DeleteRange(write_options, "key6168", "key11238");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 2754: get
  status = db->Get(read_options, "key5924", &value);
  if (status.ok()) {
    out << "GET key5924 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2755: put
  db->Put(write_options, "key2436", "value57320");

  // op 2756: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 2757: put
  db->Put(write_options, "key7749", "value37835");

  // op 2758: put
  db->Put(write_options, "key0571", "value26786");

  // op 2759: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0004", "key0089", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0004, key0089) size=" << scan_result.size() << std::endl;
  }

  // op 2760: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1175", "key11608", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1175, key11608) size=" << scan_result.size() << std::endl;
  }

  // op 2761: get
  status = db->Get(read_options, "key1202", &value);
  if (status.ok()) {
    out << "GET key1202 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2762: put
  db->Put(write_options, "key7899", "value66783");

  // op 2763: get
  status = db->Get(read_options, "key7453", &value);
  if (status.ok()) {
    out << "GET key7453 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2764: delete_range
  status = db->DeleteRange(write_options, "key2963", "key4809");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 2765: put
  db->Put(write_options, "key4755", "value18260");

  // op 2766: get
  status = db->Get(read_options, "key6761", &value);
  if (status.ok()) {
    out << "GET key6761 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2767: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0883", "key10140", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0883, key10140) size=" << scan_result.size() << std::endl;
  }

  // op 2768: delete
  db->Delete(write_options, "key5109");

  // op 2769: put
  db->Put(write_options, "key5976", "value72601");

  // op 2770: delete
  db->Delete(write_options, "key0637");

  // op 2771: put
  db->Put(write_options, "key11627", "value80949");

  // op 2772: delete_range
  status = db->DeleteRange(write_options, "key1571", "key4781");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 2773: delete_range
  status = db->DeleteRange(write_options, "key3517", "key6047");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 2774: put
  db->Put(write_options, "key0094", "value66383");

  // op 2775: get
  status = db->Get(read_options, "key0210", &value);
  if (status.ok()) {
    out << "GET key0210 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2776: put
  db->Put(write_options, "key3702", "value91307");

  // op 2777: put
  db->Put(write_options, "key0666", "value25128");

  // op 2778: put
  db->Put(write_options, "key11729", "value115320");

  // op 2779: put
  db->Put(write_options, "key1309", "value20092");

  // op 2780: get
  status = db->Get(read_options, "key8210", &value);
  if (status.ok()) {
    out << "GET key8210 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2781: get
  status = db->Get(read_options, "key2132", &value);
  if (status.ok()) {
    out << "GET key2132 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2782: put
  db->Put(write_options, "key10740", "value38430");

  // op 2783: put
  db->Put(write_options, "key11486", "value3179");

  // op 2784: delete
  db->Delete(write_options, "key3427");

  // op 2785: put
  db->Put(write_options, "key9252", "value40279");

  // op 2786: delete
  db->Delete(write_options, "key11592");

  // op 2787: put
  db->Put(write_options, "key4014", "value71201");

  // op 2788: put
  db->Put(write_options, "key2894", "value60780");

  // op 2789: delete
  db->Delete(write_options, "key5537");

  // op 2790: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2814", "key9034", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2814, key9034) size=" << scan_result.size() << std::endl;
  }

  // op 2791: put
  db->Put(write_options, "key7308", "value43614");

  // op 2792: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2767", "key10111", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2767, key10111) size=" << scan_result.size() << std::endl;
  }

  // op 2793: delete_range
  status = db->DeleteRange(write_options, "key7667", "key10545");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 2794: put
  db->Put(write_options, "key3321", "value94940");

  // op 2795: put
  db->Put(write_options, "key9920", "value109550");

  // op 2796: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1560", "key3417", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1560, key3417) size=" << scan_result.size() << std::endl;
  }

  // op 2797: put
  db->Put(write_options, "key11193", "value9570");

  // op 2798: get
  status = db->Get(read_options, "key2345", &value);
  if (status.ok()) {
    out << "GET key2345 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2799: put
  db->Put(write_options, "key8516", "value30601");

  // op 2800: delete_range
  status = db->DeleteRange(write_options, "key3368", "key3527");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 2801: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1161", "key8150", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1161, key8150) size=" << scan_result.size() << std::endl;
  }

  // op 2802: put
  db->Put(write_options, "key9297", "value65050");

  // op 2803: get
  status = db->Get(read_options, "key11683", &value);
  if (status.ok()) {
    out << "GET key11683 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2804: get
  status = db->Get(read_options, "key5697", &value);
  if (status.ok()) {
    out << "GET key5697 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2805: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1240", "key11050", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1240, key11050) size=" << scan_result.size() << std::endl;
  }

  // op 2806: put
  db->Put(write_options, "key6312", "value36224");

  // op 2807: get
  status = db->Get(read_options, "key10426", &value);
  if (status.ok()) {
    out << "GET key10426 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2808: put
  db->Put(write_options, "key4168", "value48232");

  // op 2809: put
  db->Put(write_options, "key6920", "value116771");

  // op 2810: get
  status = db->Get(read_options, "key3644", &value);
  if (status.ok()) {
    out << "GET key3644 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2811: put
  db->Put(write_options, "key8087", "value32189");

  // op 2812: delete_range
  status = db->DeleteRange(write_options, "key3812", "key9471");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 2813: delete_range
  status = db->DeleteRange(write_options, "key6873", "key10460");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 2814: put
  db->Put(write_options, "key7053", "value28491");

  // op 2815: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10132", "key11235", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10132, key11235) size=" << scan_result.size() << std::endl;
  }

  // op 2816: delete_range
  status = db->DeleteRange(write_options, "key3066", "key5265");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 2817: delete_range
  status = db->DeleteRange(write_options, "key2548", "key4885");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 2818: put
  db->Put(write_options, "key4992", "value103521");

  // op 2819: put
  db->Put(write_options, "key9580", "value103910");

  // op 2820: put
  db->Put(write_options, "key3434", "value9949");

  // op 2821: get
  status = db->Get(read_options, "key4919", &value);
  if (status.ok()) {
    out << "GET key4919 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2822: put
  db->Put(write_options, "key6574", "value43519");

  // op 2823: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 2824: put
  db->Put(write_options, "key2012", "value37668");

  // op 2825: put
  db->Put(write_options, "key4842", "value103848");

  // op 2826: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6431", "key9141", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6431, key9141) size=" << scan_result.size() << std::endl;
  }

  // op 2827: put
  db->Put(write_options, "key7172", "value10357");

  // op 2828: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4582", "key6432", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4582, key6432) size=" << scan_result.size() << std::endl;
  }

  // op 2829: put
  db->Put(write_options, "key10728", "value19507");

  // op 2830: get
  status = db->Get(read_options, "key5840", &value);
  if (status.ok()) {
    out << "GET key5840 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2831: put
  db->Put(write_options, "key6042", "value20601");

  // op 2832: delete
  db->Delete(write_options, "key5846");

  // op 2833: put
  db->Put(write_options, "key11594", "value25618");

  // op 2834: put
  db->Put(write_options, "key2726", "value100458");

  // op 2835: get
  status = db->Get(read_options, "key9520", &value);
  if (status.ok()) {
    out << "GET key9520 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2836: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 2837: get
  status = db->Get(read_options, "key2111", &value);
  if (status.ok()) {
    out << "GET key2111 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2838: delete_range
  status = db->DeleteRange(write_options, "key6400", "key6869");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 2839: put
  db->Put(write_options, "key7511", "value28352");

  // op 2840: get
  status = db->Get(read_options, "key6628", &value);
  if (status.ok()) {
    out << "GET key6628 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2841: put
  db->Put(write_options, "key7152", "value91505");

  // op 2842: put
  db->Put(write_options, "key6056", "value34763");

  // op 2843: put
  db->Put(write_options, "key1731", "value107970");

  // op 2844: get
  status = db->Get(read_options, "key7090", &value);
  if (status.ok()) {
    out << "GET key7090 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2845: put
  db->Put(write_options, "key2287", "value67912");

  // op 2846: delete_range
  status = db->DeleteRange(write_options, "key3729", "key6258");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 2847: put
  db->Put(write_options, "key0975", "value15286");

  // op 2848: get
  status = db->Get(read_options, "key3710", &value);
  if (status.ok()) {
    out << "GET key3710 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2849: put
  db->Put(write_options, "key10216", "value30194");

  // op 2850: get
  status = db->Get(read_options, "key8308", &value);
  if (status.ok()) {
    out << "GET key8308 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2851: put
  db->Put(write_options, "key7162", "value112866");

  // op 2852: put
  db->Put(write_options, "key6261", "value57958");

  // op 2853: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6670", "key8539", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6670, key8539) size=" << scan_result.size() << std::endl;
  }

  // op 2854: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3684", "key6366", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3684, key6366) size=" << scan_result.size() << std::endl;
  }

  // op 2855: put
  db->Put(write_options, "key10825", "value76099");

  // op 2856: delete
  db->Delete(write_options, "key7974");

  // op 2857: get
  status = db->Get(read_options, "key3102", &value);
  if (status.ok()) {
    out << "GET key3102 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2858: put
  db->Put(write_options, "key9699", "value19508");

  // op 2859: put
  db->Put(write_options, "key4305", "value90733");

  // op 2860: delete
  db->Delete(write_options, "key5687");

  // op 2861: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0307", "key10239", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0307, key10239) size=" << scan_result.size() << std::endl;
  }

  // op 2862: delete
  db->Delete(write_options, "key2580");

  // op 2863: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 2864: put
  db->Put(write_options, "key6888", "value21870");

  // op 2865: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7627", "key8878", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7627, key8878) size=" << scan_result.size() << std::endl;
  }

  // op 2866: delete_range
  status = db->DeleteRange(write_options, "key1873", "key4111");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 2867: get
  status = db->Get(read_options, "key3222", &value);
  if (status.ok()) {
    out << "GET key3222 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2868: get
  status = db->Get(read_options, "key4050", &value);
  if (status.ok()) {
    out << "GET key4050 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2869: put
  db->Put(write_options, "key4441", "value76487");

  // op 2870: put
  db->Put(write_options, "key7649", "value11796");

  // op 2871: put
  db->Put(write_options, "key1873", "value52663");

  // op 2872: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 2873: delete_range
  status = db->DeleteRange(write_options, "key2643", "key9569");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 2874: delete
  db->Delete(write_options, "key8429");

  // op 2875: put
  db->Put(write_options, "key7587", "value72327");

  // op 2876: put
  db->Put(write_options, "key1888", "value47408");

  // op 2877: put
  db->Put(write_options, "key8112", "value29947");

  // op 2878: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8106", "key8380", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8106, key8380) size=" << scan_result.size() << std::endl;
  }

  // op 2879: put
  db->Put(write_options, "key6186", "value2102");

  // op 2880: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 2881: put
  db->Put(write_options, "key7036", "value4149");

  // op 2882: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 2883: get
  status = db->Get(read_options, "key10655", &value);
  if (status.ok()) {
    out << "GET key10655 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2884: put
  db->Put(write_options, "key3071", "value62554");

  // op 2885: delete
  db->Delete(write_options, "key3354");

  // op 2886: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0519", "key3158", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0519, key3158) size=" << scan_result.size() << std::endl;
  }

  // op 2887: get
  status = db->Get(read_options, "key7411", &value);
  if (status.ok()) {
    out << "GET key7411 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2888: delete
  db->Delete(write_options, "key2516");

  // op 2889: put
  db->Put(write_options, "key10375", "value22922");

  // op 2890: put
  db->Put(write_options, "key8359", "value71237");

  // op 2891: put
  db->Put(write_options, "key5861", "value37441");

  // op 2892: delete_range
  status = db->DeleteRange(write_options, "key0802", "key1006");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 2893: get
  status = db->Get(read_options, "key6833", &value);
  if (status.ok()) {
    out << "GET key6833 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2894: put
  db->Put(write_options, "key4175", "value49044");

  // op 2895: put
  db->Put(write_options, "key0301", "value28457");

  // op 2896: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 2897: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0666", "key2617", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0666, key2617) size=" << scan_result.size() << std::endl;
  }

  // op 2898: put
  db->Put(write_options, "key10827", "value9575");

  // op 2899: get
  status = db->Get(read_options, "key3338", &value);
  if (status.ok()) {
    out << "GET key3338 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2900: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2697", "key8960", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2697, key8960) size=" << scan_result.size() << std::endl;
  }

  // op 2901: delete_range
  status = db->DeleteRange(write_options, "key4577", "key6653");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 2902: put
  db->Put(write_options, "key7128", "value55846");

  // op 2903: delete
  db->Delete(write_options, "key4229");

  // op 2904: get
  status = db->Get(read_options, "key11001", &value);
  if (status.ok()) {
    out << "GET key11001 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2905: put
  db->Put(write_options, "key9127", "value27053");

  // op 2906: put
  db->Put(write_options, "key1617", "value84169");

  // op 2907: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3255", "key10733", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3255, key10733) size=" << scan_result.size() << std::endl;
  }

  // op 2908: put
  db->Put(write_options, "key6261", "value42240");

  // op 2909: get
  status = db->Get(read_options, "key11501", &value);
  if (status.ok()) {
    out << "GET key11501 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2910: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 2911: get
  status = db->Get(read_options, "key4750", &value);
  if (status.ok()) {
    out << "GET key4750 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2912: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 2913: put
  db->Put(write_options, "key0562", "value51077");

  // op 2914: delete_range
  status = db->DeleteRange(write_options, "key4430", "key7116");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 2915: delete
  db->Delete(write_options, "key10849");

  // op 2916: put
  db->Put(write_options, "key0167", "value83029");

  // op 2917: get
  status = db->Get(read_options, "key10687", &value);
  if (status.ok()) {
    out << "GET key10687 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2918: put
  db->Put(write_options, "key7855", "value18549");

  // op 2919: put
  db->Put(write_options, "key11070", "value110721");

  // op 2920: delete
  db->Delete(write_options, "key7130");

  // op 2921: put
  db->Put(write_options, "key8234", "value80463");

  // op 2922: put
  db->Put(write_options, "key0715", "value48028");

  // op 2923: put
  db->Put(write_options, "key10194", "value33991");

  // op 2924: put
  db->Put(write_options, "key9762", "value53223");

  // op 2925: put
  db->Put(write_options, "key3451", "value105200");

  // op 2926: get
  status = db->Get(read_options, "key3092", &value);
  if (status.ok()) {
    out << "GET key3092 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2927: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8885", "key9687", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8885, key9687) size=" << scan_result.size() << std::endl;
  }

  // op 2928: get
  status = db->Get(read_options, "key8827", &value);
  if (status.ok()) {
    out << "GET key8827 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2929: get
  status = db->Get(read_options, "key3742", &value);
  if (status.ok()) {
    out << "GET key3742 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2930: put
  db->Put(write_options, "key4954", "value18771");

  // op 2931: put
  db->Put(write_options, "key2850", "value80537");

  // op 2932: put
  db->Put(write_options, "key8173", "value7879");

  // op 2933: put
  db->Put(write_options, "key2594", "value62347");

  // op 2934: put
  db->Put(write_options, "key4076", "value73659");

  // op 2935: delete_range
  status = db->DeleteRange(write_options, "key9663", "key10271");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 2936: delete
  db->Delete(write_options, "key11561");

  // op 2937: put
  db->Put(write_options, "key0434", "value54758");

  // op 2938: get
  status = db->Get(read_options, "key6822", &value);
  if (status.ok()) {
    out << "GET key6822 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2939: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0017", "key11166", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0017, key11166) size=" << scan_result.size() << std::endl;
  }

  // op 2940: delete
  db->Delete(write_options, "key7274");

  // op 2941: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7263", "key7931", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7263, key7931) size=" << scan_result.size() << std::endl;
  }

  // op 2942: get
  status = db->Get(read_options, "key5330", &value);
  if (status.ok()) {
    out << "GET key5330 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2943: delete_range
  status = db->DeleteRange(write_options, "key7166", "key11089");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 2944: put
  db->Put(write_options, "key4983", "value77007");

  // op 2945: delete
  db->Delete(write_options, "key3652");

  // op 2946: put
  db->Put(write_options, "key5815", "value72301");

  // op 2947: get
  status = db->Get(read_options, "key7522", &value);
  if (status.ok()) {
    out << "GET key7522 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2948: delete_range
  status = db->DeleteRange(write_options, "key1131", "key3185");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 2949: delete
  db->Delete(write_options, "key0727");

  // op 2950: put
  db->Put(write_options, "key3555", "value21920");

  // op 2951: put
  db->Put(write_options, "key6440", "value91777");

  // op 2952: put
  db->Put(write_options, "key11003", "value56237");

  // op 2953: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5329", "key10509", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5329, key10509) size=" << scan_result.size() << std::endl;
  }

  // op 2954: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 2955: delete
  db->Delete(write_options, "key1295");

  // op 2956: get
  status = db->Get(read_options, "key0530", &value);
  if (status.ok()) {
    out << "GET key0530 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2957: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3897", "key7361", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3897, key7361) size=" << scan_result.size() << std::endl;
  }

  // op 2958: get
  status = db->Get(read_options, "key5899", &value);
  if (status.ok()) {
    out << "GET key5899 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2959: delete
  db->Delete(write_options, "key7928");

  // op 2960: put
  db->Put(write_options, "key4547", "value7070");

  // op 2961: put
  db->Put(write_options, "key7464", "value0786");

  // op 2962: put
  db->Put(write_options, "key4682", "value72015");

  // op 2963: delete_range
  status = db->DeleteRange(write_options, "key1956", "key11502");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 2964: delete
  db->Delete(write_options, "key8294");

  // op 2965: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0521", "key10981", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0521, key10981) size=" << scan_result.size() << std::endl;
  }

  // op 2966: put
  db->Put(write_options, "key7282", "value1409");

  // op 2967: delete
  db->Delete(write_options, "key4391");

  // op 2968: delete
  db->Delete(write_options, "key7267");

  // op 2969: put
  db->Put(write_options, "key10287", "value109778");

  // op 2970: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4673", "key8161", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4673, key8161) size=" << scan_result.size() << std::endl;
  }

  // op 2971: put
  db->Put(write_options, "key9367", "value3810");

  // op 2972: put
  db->Put(write_options, "key10064", "value5412");

  // op 2973: put
  db->Put(write_options, "key2794", "value102366");

  // op 2974: put
  db->Put(write_options, "key2571", "value112165");

  // op 2975: put
  db->Put(write_options, "key6912", "value85751");

  // op 2976: get
  status = db->Get(read_options, "key3748", &value);
  if (status.ok()) {
    out << "GET key3748 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2977: delete_range
  status = db->DeleteRange(write_options, "key9033", "key10503");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 2978: delete
  db->Delete(write_options, "key1017");

  // op 2979: put
  db->Put(write_options, "key3339", "value67091");

  // op 2980: put
  db->Put(write_options, "key5718", "value41333");

  // op 2981: put
  db->Put(write_options, "key11282", "value84180");

  // op 2982: get
  status = db->Get(read_options, "key3717", &value);
  if (status.ok()) {
    out << "GET key3717 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2983: put
  db->Put(write_options, "key8344", "value51604");

  // op 2984: put
  db->Put(write_options, "key5460", "value47856");

  // op 2985: delete
  db->Delete(write_options, "key2643");

  // op 2986: put
  db->Put(write_options, "key4323", "value94660");

  // op 2987: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0581", "key9564", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0581, key9564) size=" << scan_result.size() << std::endl;
  }

  // op 2988: put
  db->Put(write_options, "key7435", "value91832");

  // op 2989: put
  db->Put(write_options, "key5280", "value36886");

  // op 2990: put
  db->Put(write_options, "key0627", "value3741");

  // op 2991: get
  status = db->Get(read_options, "key8946", &value);
  if (status.ok()) {
    out << "GET key8946 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2992: put
  db->Put(write_options, "key0979", "value12575");

  // op 2993: get
  status = db->Get(read_options, "key9064", &value);
  if (status.ok()) {
    out << "GET key9064 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2994: put
  db->Put(write_options, "key3918", "value73615");

  // op 2995: delete_range
  status = db->DeleteRange(write_options, "key5335", "key8199");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 2996: get
  status = db->Get(read_options, "key8526", &value);
  if (status.ok()) {
    out << "GET key8526 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2997: delete
  db->Delete(write_options, "key9611");

  // op 2998: get
  status = db->Get(read_options, "key9318", &value);
  if (status.ok()) {
    out << "GET key9318 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 2999: get
  status = db->Get(read_options, "key8778", &value);
  if (status.ok()) {
    out << "GET key8778 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3000: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 3001: put
  db->Put(write_options, "key6874", "value83832");

  // op 3002: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 3003: delete
  db->Delete(write_options, "key7727");

  // op 3004: delete_range
  status = db->DeleteRange(write_options, "key10005", "key10858");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 3005: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 3006: delete_range
  status = db->DeleteRange(write_options, "key11233", "key11377");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 3007: get
  status = db->Get(read_options, "key2830", &value);
  if (status.ok()) {
    out << "GET key2830 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3008: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0861", "key1119", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0861, key1119) size=" << scan_result.size() << std::endl;
  }

  // op 3009: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2241", "key5173", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2241, key5173) size=" << scan_result.size() << std::endl;
  }

  // op 3010: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 3011: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 3012: put
  db->Put(write_options, "key8742", "value64142");

  // op 3013: get
  status = db->Get(read_options, "key2152", &value);
  if (status.ok()) {
    out << "GET key2152 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3014: delete_range
  status = db->DeleteRange(write_options, "key5809", "key11108");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 3015: put
  db->Put(write_options, "key10892", "value5276");

  // op 3016: get
  status = db->Get(read_options, "key3325", &value);
  if (status.ok()) {
    out << "GET key3325 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3017: get
  status = db->Get(read_options, "key0029", &value);
  if (status.ok()) {
    out << "GET key0029 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3018: put
  db->Put(write_options, "key9693", "value113645");

  // op 3019: get
  status = db->Get(read_options, "key7234", &value);
  if (status.ok()) {
    out << "GET key7234 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3020: put
  db->Put(write_options, "key10642", "value23821");

  // op 3021: delete_range
  status = db->DeleteRange(write_options, "key1136", "key10225");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 3022: put
  db->Put(write_options, "key10191", "value27131");

  // op 3023: put
  db->Put(write_options, "key8479", "value75732");

  // op 3024: get
  status = db->Get(read_options, "key6161", &value);
  if (status.ok()) {
    out << "GET key6161 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3025: put
  db->Put(write_options, "key7281", "value72792");

  // op 3026: get
  status = db->Get(read_options, "key1533", &value);
  if (status.ok()) {
    out << "GET key1533 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3027: put
  db->Put(write_options, "key9923", "value15205");

  // op 3028: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0931", "key8728", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0931, key8728) size=" << scan_result.size() << std::endl;
  }

  // op 3029: put
  db->Put(write_options, "key10875", "value25561");

  // op 3030: delete
  db->Delete(write_options, "key11035");

  // op 3031: get
  status = db->Get(read_options, "key10480", &value);
  if (status.ok()) {
    out << "GET key10480 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3032: get
  status = db->Get(read_options, "key6009", &value);
  if (status.ok()) {
    out << "GET key6009 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3033: put
  db->Put(write_options, "key2629", "value109985");

  // op 3034: get
  status = db->Get(read_options, "key7324", &value);
  if (status.ok()) {
    out << "GET key7324 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3035: put
  db->Put(write_options, "key10783", "value4514");

  // op 3036: delete_range
  status = db->DeleteRange(write_options, "key6724", "key6812");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 3037: put
  db->Put(write_options, "key5711", "value58353");

  // op 3038: put
  db->Put(write_options, "key2937", "value69668");

  // op 3039: put
  db->Put(write_options, "key9395", "value43032");

  // op 3040: get
  status = db->Get(read_options, "key1260", &value);
  if (status.ok()) {
    out << "GET key1260 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3041: get
  status = db->Get(read_options, "key9239", &value);
  if (status.ok()) {
    out << "GET key9239 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3042: get
  status = db->Get(read_options, "key4953", &value);
  if (status.ok()) {
    out << "GET key4953 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3043: put
  db->Put(write_options, "key4377", "value60671");

  // op 3044: put
  db->Put(write_options, "key7706", "value65960");

  // op 3045: put
  db->Put(write_options, "key5358", "value80983");

  // op 3046: delete
  db->Delete(write_options, "key4950");

  // op 3047: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 3048: get
  status = db->Get(read_options, "key6021", &value);
  if (status.ok()) {
    out << "GET key6021 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3049: delete
  db->Delete(write_options, "key0509");

  // op 3050: put
  db->Put(write_options, "key7957", "value54433");

  // op 3051: put
  db->Put(write_options, "key7873", "value38775");

  // op 3052: get
  status = db->Get(read_options, "key10811", &value);
  if (status.ok()) {
    out << "GET key10811 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3053: delete
  db->Delete(write_options, "key7535");

  // op 3054: get
  status = db->Get(read_options, "key5183", &value);
  if (status.ok()) {
    out << "GET key5183 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3055: put
  db->Put(write_options, "key5578", "value116750");

  // op 3056: put
  db->Put(write_options, "key10847", "value16487");

  // op 3057: delete_range
  status = db->DeleteRange(write_options, "key8886", "key11134");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 3058: delete
  db->Delete(write_options, "key0051");

  // op 3059: delete_range
  status = db->DeleteRange(write_options, "key3183", "key10458");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 3060: get
  status = db->Get(read_options, "key11507", &value);
  if (status.ok()) {
    out << "GET key11507 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3061: put
  db->Put(write_options, "key7553", "value116044");

  // op 3062: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 3063: put
  db->Put(write_options, "key9738", "value18790");

  // op 3064: delete_range
  status = db->DeleteRange(write_options, "key4643", "key10394");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 3065: get
  status = db->Get(read_options, "key9964", &value);
  if (status.ok()) {
    out << "GET key9964 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3066: get
  status = db->Get(read_options, "key0450", &value);
  if (status.ok()) {
    out << "GET key0450 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3067: put
  db->Put(write_options, "key9037", "value66157");

  // op 3068: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 3069: put
  db->Put(write_options, "key11102", "value77898");

  // op 3070: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 3071: delete
  db->Delete(write_options, "key10885");

  // op 3072: put
  db->Put(write_options, "key1998", "value85680");

  // op 3073: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 3074: get
  status = db->Get(read_options, "key5020", &value);
  if (status.ok()) {
    out << "GET key5020 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3075: put
  db->Put(write_options, "key6253", "value78337");

  // op 3076: put
  db->Put(write_options, "key2061", "value73365");

  // op 3077: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3179", "key7896", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3179, key7896) size=" << scan_result.size() << std::endl;
  }

  // op 3078: put
  db->Put(write_options, "key2086", "value103341");

  // op 3079: put
  db->Put(write_options, "key6816", "value11907");

  // op 3080: get
  status = db->Get(read_options, "key6575", &value);
  if (status.ok()) {
    out << "GET key6575 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3081: put
  db->Put(write_options, "key10325", "value46623");

  // op 3082: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 3083: delete_range
  status = db->DeleteRange(write_options, "key1412", "key10719");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 3084: put
  db->Put(write_options, "key10181", "value85612");

  // op 3085: delete_range
  status = db->DeleteRange(write_options, "key4234", "key9437");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 3086: get
  status = db->Get(read_options, "key8987", &value);
  if (status.ok()) {
    out << "GET key8987 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3087: get
  status = db->Get(read_options, "key3585", &value);
  if (status.ok()) {
    out << "GET key3585 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3088: put
  db->Put(write_options, "key5907", "value46611");

  // op 3089: put
  db->Put(write_options, "key1691", "value101826");

  // op 3090: delete_range
  status = db->DeleteRange(write_options, "key4633", "key10205");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 3091: put
  db->Put(write_options, "key3305", "value95202");

  // op 3092: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4797", "key9331", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4797, key9331) size=" << scan_result.size() << std::endl;
  }

  // op 3093: put
  db->Put(write_options, "key8414", "value39099");

  // op 3094: get
  status = db->Get(read_options, "key2879", &value);
  if (status.ok()) {
    out << "GET key2879 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3095: put
  db->Put(write_options, "key11114", "value76656");

  // op 3096: put
  db->Put(write_options, "key1599", "value47027");

  // op 3097: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1030", "key7427", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1030, key7427) size=" << scan_result.size() << std::endl;
  }

  // op 3098: get
  status = db->Get(read_options, "key10383", &value);
  if (status.ok()) {
    out << "GET key10383 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3099: get
  status = db->Get(read_options, "key8828", &value);
  if (status.ok()) {
    out << "GET key8828 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3100: get
  status = db->Get(read_options, "key0779", &value);
  if (status.ok()) {
    out << "GET key0779 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3101: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 3102: put
  db->Put(write_options, "key0447", "value74655");

  // op 3103: put
  db->Put(write_options, "key2954", "value46342");

  // op 3104: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1253", "key4592", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1253, key4592) size=" << scan_result.size() << std::endl;
  }

  // op 3105: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5421", "key8776", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5421, key8776) size=" << scan_result.size() << std::endl;
  }

  // op 3106: put
  db->Put(write_options, "key5357", "value15241");

  // op 3107: delete_range
  status = db->DeleteRange(write_options, "key3480", "key7967");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 3108: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0712", "key6151", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0712, key6151) size=" << scan_result.size() << std::endl;
  }

  // op 3109: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0287", "key4842", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0287, key4842) size=" << scan_result.size() << std::endl;
  }

  // op 3110: delete
  db->Delete(write_options, "key6496");

  // op 3111: put
  db->Put(write_options, "key10264", "value28373");

  // op 3112: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5037", "key6780", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5037, key6780) size=" << scan_result.size() << std::endl;
  }

  // op 3113: get
  status = db->Get(read_options, "key7990", &value);
  if (status.ok()) {
    out << "GET key7990 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3114: get
  status = db->Get(read_options, "key4610", &value);
  if (status.ok()) {
    out << "GET key4610 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3115: delete
  db->Delete(write_options, "key4435");

  // op 3116: put
  db->Put(write_options, "key6402", "value83458");

  // op 3117: delete_range
  status = db->DeleteRange(write_options, "key1241", "key4025");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 3118: put
  db->Put(write_options, "key3798", "value9470");

  // op 3119: put
  db->Put(write_options, "key7599", "value100979");

  // op 3120: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3948", "key6399", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3948, key6399) size=" << scan_result.size() << std::endl;
  }

  // op 3121: put
  db->Put(write_options, "key1935", "value100358");

  // op 3122: get
  status = db->Get(read_options, "key4373", &value);
  if (status.ok()) {
    out << "GET key4373 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3123: put
  db->Put(write_options, "key8753", "value101757");

  // op 3124: put
  db->Put(write_options, "key5529", "value110043");

  // op 3125: delete
  db->Delete(write_options, "key2906");

  // op 3126: get
  status = db->Get(read_options, "key6375", &value);
  if (status.ok()) {
    out << "GET key6375 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3127: put
  db->Put(write_options, "key11577", "value54741");

  // op 3128: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6283", "key9362", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6283, key9362) size=" << scan_result.size() << std::endl;
  }

  // op 3129: delete_range
  status = db->DeleteRange(write_options, "key6128", "key7855");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 3130: put
  db->Put(write_options, "key3331", "value86662");

  // op 3131: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5000", "key7722", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5000, key7722) size=" << scan_result.size() << std::endl;
  }

  // op 3132: delete_range
  status = db->DeleteRange(write_options, "key3261", "key10487");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 3133: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4137", "key4858", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4137, key4858) size=" << scan_result.size() << std::endl;
  }

  // op 3134: get
  status = db->Get(read_options, "key4867", &value);
  if (status.ok()) {
    out << "GET key4867 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3135: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 3136: get
  status = db->Get(read_options, "key6598", &value);
  if (status.ok()) {
    out << "GET key6598 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3137: put
  db->Put(write_options, "key4845", "value90609");

  // op 3138: delete_range
  status = db->DeleteRange(write_options, "key0397", "key10029");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 3139: put
  db->Put(write_options, "key4998", "value92304");

  // op 3140: delete
  db->Delete(write_options, "key6957");

  // op 3141: put
  db->Put(write_options, "key3181", "value20818");

  // op 3142: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8322", "key9803", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8322, key9803) size=" << scan_result.size() << std::endl;
  }

  // op 3143: delete
  db->Delete(write_options, "key9669");

  // op 3144: delete
  db->Delete(write_options, "key6263");

  // op 3145: delete_range
  status = db->DeleteRange(write_options, "key0453", "key4876");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 3146: delete
  db->Delete(write_options, "key5575");

  // op 3147: get
  status = db->Get(read_options, "key11589", &value);
  if (status.ok()) {
    out << "GET key11589 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3148: put
  db->Put(write_options, "key7743", "value23587");

  // op 3149: delete
  db->Delete(write_options, "key5666");

  // op 3150: put
  db->Put(write_options, "key2239", "value113230");

  // op 3151: delete
  db->Delete(write_options, "key1560");

  // op 3152: put
  db->Put(write_options, "key4836", "value34936");

  // op 3153: get
  status = db->Get(read_options, "key6155", &value);
  if (status.ok()) {
    out << "GET key6155 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3154: get
  status = db->Get(read_options, "key0683", &value);
  if (status.ok()) {
    out << "GET key0683 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3155: delete
  db->Delete(write_options, "key6293");

  // op 3156: put
  db->Put(write_options, "key8747", "value77919");

  // op 3157: get
  status = db->Get(read_options, "key10771", &value);
  if (status.ok()) {
    out << "GET key10771 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3158: put
  db->Put(write_options, "key6451", "value104394");

  // op 3159: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1057", "key9844", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1057, key9844) size=" << scan_result.size() << std::endl;
  }

  // op 3160: put
  db->Put(write_options, "key2566", "value98053");

  // op 3161: put
  db->Put(write_options, "key3124", "value10675");

  // op 3162: get
  status = db->Get(read_options, "key2551", &value);
  if (status.ok()) {
    out << "GET key2551 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3163: put
  db->Put(write_options, "key5680", "value108270");

  // op 3164: put
  db->Put(write_options, "key0167", "value27299");

  // op 3165: get
  status = db->Get(read_options, "key10228", &value);
  if (status.ok()) {
    out << "GET key10228 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3166: delete_range
  status = db->DeleteRange(write_options, "key1023", "key5962");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 3167: delete_range
  status = db->DeleteRange(write_options, "key4769", "key6656");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 3168: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 3169: put
  db->Put(write_options, "key2560", "value84161");

  // op 3170: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 3171: put
  db->Put(write_options, "key9788", "value86360");

  // op 3172: get
  status = db->Get(read_options, "key2238", &value);
  if (status.ok()) {
    out << "GET key2238 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3173: get
  status = db->Get(read_options, "key1260", &value);
  if (status.ok()) {
    out << "GET key1260 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3174: delete
  db->Delete(write_options, "key10389");

  // op 3175: delete_range
  status = db->DeleteRange(write_options, "key6804", "key10656");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 3176: get
  status = db->Get(read_options, "key3515", &value);
  if (status.ok()) {
    out << "GET key3515 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3177: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0648", "key4755", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0648, key4755) size=" << scan_result.size() << std::endl;
  }

  // op 3178: put
  db->Put(write_options, "key8855", "value28323");

  // op 3179: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1618", "key7320", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1618, key7320) size=" << scan_result.size() << std::endl;
  }

  // op 3180: get
  status = db->Get(read_options, "key9399", &value);
  if (status.ok()) {
    out << "GET key9399 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3181: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 3182: put
  db->Put(write_options, "key1848", "value83232");

  // op 3183: put
  db->Put(write_options, "key1160", "value76102");

  // op 3184: put
  db->Put(write_options, "key2798", "value103823");

  // op 3185: put
  db->Put(write_options, "key2026", "value68627");

  // op 3186: delete_range
  status = db->DeleteRange(write_options, "key0393", "key9834");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 3187: delete_range
  status = db->DeleteRange(write_options, "key5577", "key5794");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 3188: get
  status = db->Get(read_options, "key7010", &value);
  if (status.ok()) {
    out << "GET key7010 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3189: get
  status = db->Get(read_options, "key2033", &value);
  if (status.ok()) {
    out << "GET key2033 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3190: delete_range
  status = db->DeleteRange(write_options, "key1084", "key6743");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 3191: put
  db->Put(write_options, "key8144", "value0251");

  // op 3192: put
  db->Put(write_options, "key8580", "value75515");

  // op 3193: delete_range
  status = db->DeleteRange(write_options, "key6297", "key10551");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 3194: put
  db->Put(write_options, "key0509", "value113342");

  // op 3195: put
  db->Put(write_options, "key5494", "value46274");

  // op 3196: delete_range
  status = db->DeleteRange(write_options, "key1766", "key10838");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 3197: get
  status = db->Get(read_options, "key6091", &value);
  if (status.ok()) {
    out << "GET key6091 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3198: get
  status = db->Get(read_options, "key0128", &value);
  if (status.ok()) {
    out << "GET key0128 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3199: put
  db->Put(write_options, "key6662", "value106801");

  // op 3200: delete
  db->Delete(write_options, "key5733");

  // op 3201: get
  status = db->Get(read_options, "key5574", &value);
  if (status.ok()) {
    out << "GET key5574 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3202: put
  db->Put(write_options, "key8503", "value6885");

  // op 3203: put
  db->Put(write_options, "key11564", "value95962");

  // op 3204: delete_range
  status = db->DeleteRange(write_options, "key0456", "key11430");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 3205: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4713", "key9293", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4713, key9293) size=" << scan_result.size() << std::endl;
  }

  // op 3206: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 3207: delete_range
  status = db->DeleteRange(write_options, "key1828", "key9889");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 3208: put
  db->Put(write_options, "key10858", "value110403");

  // op 3209: delete
  db->Delete(write_options, "key10715");

  // op 3210: put
  db->Put(write_options, "key6065", "value58728");

  // op 3211: put
  db->Put(write_options, "key8664", "value11144");

  // op 3212: delete_range
  status = db->DeleteRange(write_options, "key5671", "key5815");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 3213: delete
  db->Delete(write_options, "key2950");

  // op 3214: delete
  db->Delete(write_options, "key10903");

  // op 3215: put
  db->Put(write_options, "key3230", "value78710");

  // op 3216: put
  db->Put(write_options, "key9325", "value19875");

  // op 3217: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2553", "key4922", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2553, key4922) size=" << scan_result.size() << std::endl;
  }

  // op 3218: get
  status = db->Get(read_options, "key7665", &value);
  if (status.ok()) {
    out << "GET key7665 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3219: put
  db->Put(write_options, "key8256", "value73463");

  // op 3220: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6661", "key9721", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6661, key9721) size=" << scan_result.size() << std::endl;
  }

  // op 3221: put
  db->Put(write_options, "key8021", "value34533");

  // op 3222: delete
  db->Delete(write_options, "key2372");

  // op 3223: get
  status = db->Get(read_options, "key7564", &value);
  if (status.ok()) {
    out << "GET key7564 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3224: get
  status = db->Get(read_options, "key7433", &value);
  if (status.ok()) {
    out << "GET key7433 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3225: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0305", "key10678", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0305, key10678) size=" << scan_result.size() << std::endl;
  }

  // op 3226: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 3227: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 3228: delete_range
  status = db->DeleteRange(write_options, "key7659", "key9189");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 3229: put
  db->Put(write_options, "key5655", "value0987");

  // op 3230: delete
  db->Delete(write_options, "key2694");

  // op 3231: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 3232: put
  db->Put(write_options, "key5279", "value101101");

  // op 3233: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 3234: put
  db->Put(write_options, "key4574", "value85450");

  // op 3235: delete
  db->Delete(write_options, "key3134");

  // op 3236: get
  status = db->Get(read_options, "key9979", &value);
  if (status.ok()) {
    out << "GET key9979 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3237: get
  status = db->Get(read_options, "key6866", &value);
  if (status.ok()) {
    out << "GET key6866 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3238: get
  status = db->Get(read_options, "key1476", &value);
  if (status.ok()) {
    out << "GET key1476 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3239: put
  db->Put(write_options, "key10860", "value68570");

  // op 3240: put
  db->Put(write_options, "key4641", "value82582");

  // op 3241: put
  db->Put(write_options, "key4447", "value43958");

  // op 3242: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1638", "key3473", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1638, key3473) size=" << scan_result.size() << std::endl;
  }

  // op 3243: put
  db->Put(write_options, "key10209", "value3067");

  // op 3244: delete_range
  status = db->DeleteRange(write_options, "key1990", "key5421");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 3245: get
  status = db->Get(read_options, "key3981", &value);
  if (status.ok()) {
    out << "GET key3981 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3246: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4877", "key7063", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4877, key7063) size=" << scan_result.size() << std::endl;
  }

  // op 3247: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2137", "key7077", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2137, key7077) size=" << scan_result.size() << std::endl;
  }

  // op 3248: put
  db->Put(write_options, "key3903", "value74243");

  // op 3249: put
  db->Put(write_options, "key5106", "value76491");

  // op 3250: get
  status = db->Get(read_options, "key8604", &value);
  if (status.ok()) {
    out << "GET key8604 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3251: put
  db->Put(write_options, "key6242", "value112977");

  // op 3252: put
  db->Put(write_options, "key9353", "value53531");

  // op 3253: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10407", "key11643", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10407, key11643) size=" << scan_result.size() << std::endl;
  }

  // op 3254: delete
  db->Delete(write_options, "key5266");

  // op 3255: put
  db->Put(write_options, "key0152", "value37057");

  // op 3256: delete_range
  status = db->DeleteRange(write_options, "key1843", "key8077");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 3257: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0123", "key10016", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0123, key10016) size=" << scan_result.size() << std::endl;
  }

  // op 3258: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 3259: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1632", "key9297", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1632, key9297) size=" << scan_result.size() << std::endl;
  }

  // op 3260: get
  status = db->Get(read_options, "key8383", &value);
  if (status.ok()) {
    out << "GET key8383 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3261: delete
  db->Delete(write_options, "key5020");

  // op 3262: get
  status = db->Get(read_options, "key8037", &value);
  if (status.ok()) {
    out << "GET key8037 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3263: get
  status = db->Get(read_options, "key5266", &value);
  if (status.ok()) {
    out << "GET key5266 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3264: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1184", "key8287", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1184, key8287) size=" << scan_result.size() << std::endl;
  }

  // op 3265: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1590", "key6886", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1590, key6886) size=" << scan_result.size() << std::endl;
  }

  // op 3266: put
  db->Put(write_options, "key4896", "value29797");

  // op 3267: get
  status = db->Get(read_options, "key11314", &value);
  if (status.ok()) {
    out << "GET key11314 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3268: get
  status = db->Get(read_options, "key9748", &value);
  if (status.ok()) {
    out << "GET key9748 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3269: put
  db->Put(write_options, "key9000", "value29317");

  // op 3270: delete_range
  status = db->DeleteRange(write_options, "key4300", "key4507");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 3271: put
  db->Put(write_options, "key8557", "value85359");

  // op 3272: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6051", "key9353", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6051, key9353) size=" << scan_result.size() << std::endl;
  }

  // op 3273: delete
  db->Delete(write_options, "key0363");

  // op 3274: delete
  db->Delete(write_options, "key1087");

  // op 3275: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 3276: put
  db->Put(write_options, "key5949", "value111263");

  // op 3277: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3736", "key9709", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3736, key9709) size=" << scan_result.size() << std::endl;
  }

  // op 3278: put
  db->Put(write_options, "key10629", "value92724");

  // op 3279: delete
  db->Delete(write_options, "key5047");

  // op 3280: get
  status = db->Get(read_options, "key6092", &value);
  if (status.ok()) {
    out << "GET key6092 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3281: delete_range
  status = db->DeleteRange(write_options, "key1473", "key4464");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 3282: get
  status = db->Get(read_options, "key4080", &value);
  if (status.ok()) {
    out << "GET key4080 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3283: delete
  db->Delete(write_options, "key2724");

  // op 3284: put
  db->Put(write_options, "key6793", "value33036");

  // op 3285: put
  db->Put(write_options, "key11048", "value106950");

  // op 3286: get
  status = db->Get(read_options, "key2258", &value);
  if (status.ok()) {
    out << "GET key2258 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3287: get
  status = db->Get(read_options, "key10579", &value);
  if (status.ok()) {
    out << "GET key10579 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3288: delete
  db->Delete(write_options, "key7032");

  // op 3289: put
  db->Put(write_options, "key4456", "value8962");

  // op 3290: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7738", "key9894", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7738, key9894) size=" << scan_result.size() << std::endl;
  }

  // op 3291: put
  db->Put(write_options, "key11242", "value62926");

  // op 3292: get
  status = db->Get(read_options, "key2000", &value);
  if (status.ok()) {
    out << "GET key2000 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3293: put
  db->Put(write_options, "key3122", "value27788");

  // op 3294: delete
  db->Delete(write_options, "key2711");

  // op 3295: put
  db->Put(write_options, "key5435", "value87746");

  // op 3296: get
  status = db->Get(read_options, "key7084", &value);
  if (status.ok()) {
    out << "GET key7084 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3297: put
  db->Put(write_options, "key0116", "value8277");

  // op 3298: put
  db->Put(write_options, "key7548", "value67035");

  // op 3299: delete_range
  status = db->DeleteRange(write_options, "key2642", "key5480");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 3300: put
  db->Put(write_options, "key9149", "value50417");

  // op 3301: put
  db->Put(write_options, "key10411", "value103930");

  // op 3302: delete_range
  status = db->DeleteRange(write_options, "key9479", "key11009");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 3303: delete_range
  status = db->DeleteRange(write_options, "key1203", "key9775");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 3304: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 3305: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6212", "key11134", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6212, key11134) size=" << scan_result.size() << std::endl;
  }

  // op 3306: put
  db->Put(write_options, "key3855", "value23504");

  // op 3307: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10265", "key11242", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10265, key11242) size=" << scan_result.size() << std::endl;
  }

  // op 3308: get
  status = db->Get(read_options, "key5111", &value);
  if (status.ok()) {
    out << "GET key5111 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3309: put
  db->Put(write_options, "key9024", "value76087");

  // op 3310: delete_range
  status = db->DeleteRange(write_options, "key1334", "key9177");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 3311: delete
  db->Delete(write_options, "key5219");

  // op 3312: delete_range
  status = db->DeleteRange(write_options, "key6402", "key10932");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 3313: get
  status = db->Get(read_options, "key11308", &value);
  if (status.ok()) {
    out << "GET key11308 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3314: get
  status = db->Get(read_options, "key10056", &value);
  if (status.ok()) {
    out << "GET key10056 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3315: get
  status = db->Get(read_options, "key11672", &value);
  if (status.ok()) {
    out << "GET key11672 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3316: get
  status = db->Get(read_options, "key11559", &value);
  if (status.ok()) {
    out << "GET key11559 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3317: get
  status = db->Get(read_options, "key7280", &value);
  if (status.ok()) {
    out << "GET key7280 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3318: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 3319: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3089", "key11226", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3089, key11226) size=" << scan_result.size() << std::endl;
  }

  // op 3320: put
  db->Put(write_options, "key10505", "value52479");

  // op 3321: delete
  db->Delete(write_options, "key1150");

  // op 3322: delete
  db->Delete(write_options, "key10893");

  // op 3323: put
  db->Put(write_options, "key5702", "value67158");

  // op 3324: get
  status = db->Get(read_options, "key8914", &value);
  if (status.ok()) {
    out << "GET key8914 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3325: get
  status = db->Get(read_options, "key7308", &value);
  if (status.ok()) {
    out << "GET key7308 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3326: delete_range
  status = db->DeleteRange(write_options, "key4679", "key10006");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 3327: delete
  db->Delete(write_options, "key7604");

  // op 3328: put
  db->Put(write_options, "key10278", "value116811");

  // op 3329: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0347", "key8122", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0347, key8122) size=" << scan_result.size() << std::endl;
  }

  // op 3330: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3085", "key4258", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3085, key4258) size=" << scan_result.size() << std::endl;
  }

  // op 3331: delete
  db->Delete(write_options, "key5973");

  // op 3332: put
  db->Put(write_options, "key3348", "value73907");

  // op 3333: put
  db->Put(write_options, "key3676", "value22996");

  // op 3334: get
  status = db->Get(read_options, "key0596", &value);
  if (status.ok()) {
    out << "GET key0596 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3335: delete_range
  status = db->DeleteRange(write_options, "key0096", "key3445");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 3336: put
  db->Put(write_options, "key6813", "value92484");

  // op 3337: put
  db->Put(write_options, "key6964", "value74097");

  // op 3338: get
  status = db->Get(read_options, "key11087", &value);
  if (status.ok()) {
    out << "GET key11087 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3339: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3646", "key10354", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3646, key10354) size=" << scan_result.size() << std::endl;
  }

  // op 3340: get
  status = db->Get(read_options, "key6592", &value);
  if (status.ok()) {
    out << "GET key6592 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3341: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5900", "key9876", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5900, key9876) size=" << scan_result.size() << std::endl;
  }

  // op 3342: put
  db->Put(write_options, "key0672", "value2129");

  // op 3343: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 3344: delete
  db->Delete(write_options, "key3256");

  // op 3345: get
  status = db->Get(read_options, "key3761", &value);
  if (status.ok()) {
    out << "GET key3761 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3346: put
  db->Put(write_options, "key8682", "value52030");

  // op 3347: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2816", "key2952", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2816, key2952) size=" << scan_result.size() << std::endl;
  }

  // op 3348: get
  status = db->Get(read_options, "key7669", &value);
  if (status.ok()) {
    out << "GET key7669 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3349: get
  status = db->Get(read_options, "key9301", &value);
  if (status.ok()) {
    out << "GET key9301 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3350: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1771", "key11384", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1771, key11384) size=" << scan_result.size() << std::endl;
  }

  // op 3351: put
  db->Put(write_options, "key2149", "value110691");

  // op 3352: delete
  db->Delete(write_options, "key6832");

  // op 3353: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 3354: get
  status = db->Get(read_options, "key4631", &value);
  if (status.ok()) {
    out << "GET key4631 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3355: delete_range
  status = db->DeleteRange(write_options, "key0149", "key8847");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 3356: put
  db->Put(write_options, "key4475", "value109757");

  // op 3357: put
  db->Put(write_options, "key2017", "value46998");

  // op 3358: put
  db->Put(write_options, "key0909", "value42907");

  // op 3359: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4425", "key11239", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4425, key11239) size=" << scan_result.size() << std::endl;
  }

  // op 3360: put
  db->Put(write_options, "key3090", "value87730");

  // op 3361: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 3362: put
  db->Put(write_options, "key6975", "value37612");

  // op 3363: put
  db->Put(write_options, "key10407", "value115547");

  // op 3364: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1245", "key10232", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1245, key10232) size=" << scan_result.size() << std::endl;
  }

  // op 3365: put
  db->Put(write_options, "key8671", "value5641");

  // op 3366: put
  db->Put(write_options, "key2105", "value44487");

  // op 3367: put
  db->Put(write_options, "key1992", "value30904");

  // op 3368: get
  status = db->Get(read_options, "key2944", &value);
  if (status.ok()) {
    out << "GET key2944 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3369: put
  db->Put(write_options, "key2008", "value88834");

  // op 3370: get
  status = db->Get(read_options, "key6288", &value);
  if (status.ok()) {
    out << "GET key6288 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3371: put
  db->Put(write_options, "key4839", "value110252");

  // op 3372: delete
  db->Delete(write_options, "key4283");

  // op 3373: delete_range
  status = db->DeleteRange(write_options, "key5403", "key7854");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 3374: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7805", "key9273", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7805, key9273) size=" << scan_result.size() << std::endl;
  }

  // op 3375: put
  db->Put(write_options, "key0943", "value8363");

  // op 3376: put
  db->Put(write_options, "key5922", "value17023");

  // op 3377: get
  status = db->Get(read_options, "key6996", &value);
  if (status.ok()) {
    out << "GET key6996 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3378: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2075", "key2184", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2075, key2184) size=" << scan_result.size() << std::endl;
  }

  // op 3379: put
  db->Put(write_options, "key0185", "value69230");

  // op 3380: delete_range
  status = db->DeleteRange(write_options, "key3780", "key7111");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 3381: get
  status = db->Get(read_options, "key0700", &value);
  if (status.ok()) {
    out << "GET key0700 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3382: get
  status = db->Get(read_options, "key6253", &value);
  if (status.ok()) {
    out << "GET key6253 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3383: delete_range
  status = db->DeleteRange(write_options, "key3490", "key9996");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 3384: put
  db->Put(write_options, "key11288", "value23869");

  // op 3385: get
  status = db->Get(read_options, "key1812", &value);
  if (status.ok()) {
    out << "GET key1812 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3386: get
  status = db->Get(read_options, "key2050", &value);
  if (status.ok()) {
    out << "GET key2050 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3387: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 3388: delete
  db->Delete(write_options, "key11107");

  // op 3389: put
  db->Put(write_options, "key7319", "value83369");

  // op 3390: delete_range
  status = db->DeleteRange(write_options, "key0618", "key1967");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 3391: delete
  db->Delete(write_options, "key4571");

  // op 3392: get
  status = db->Get(read_options, "key5029", &value);
  if (status.ok()) {
    out << "GET key5029 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3393: get
  status = db->Get(read_options, "key0813", &value);
  if (status.ok()) {
    out << "GET key0813 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3394: get
  status = db->Get(read_options, "key4595", &value);
  if (status.ok()) {
    out << "GET key4595 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3395: delete
  db->Delete(write_options, "key2499");

  // op 3396: get
  status = db->Get(read_options, "key9639", &value);
  if (status.ok()) {
    out << "GET key9639 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3397: delete
  db->Delete(write_options, "key7043");

  // op 3398: put
  db->Put(write_options, "key10778", "value56153");

  // op 3399: delete
  db->Delete(write_options, "key7076");

  // op 3400: put
  db->Put(write_options, "key1796", "value95969");

  // op 3401: put
  db->Put(write_options, "key3055", "value61570");

  // op 3402: put
  db->Put(write_options, "key9310", "value82602");

  // op 3403: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2464", "key6063", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2464, key6063) size=" << scan_result.size() << std::endl;
  }

  // op 3404: put
  db->Put(write_options, "key3330", "value61072");

  // op 3405: put
  db->Put(write_options, "key9743", "value1399");

  // op 3406: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5267", "key6857", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5267, key6857) size=" << scan_result.size() << std::endl;
  }

  // op 3407: get
  status = db->Get(read_options, "key6296", &value);
  if (status.ok()) {
    out << "GET key6296 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3408: get
  status = db->Get(read_options, "key8372", &value);
  if (status.ok()) {
    out << "GET key8372 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3409: delete_range
  status = db->DeleteRange(write_options, "key3341", "key7849");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 3410: delete
  db->Delete(write_options, "key2892");

  // op 3411: delete
  db->Delete(write_options, "key4127");

  // op 3412: delete_range
  status = db->DeleteRange(write_options, "key1265", "key8852");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 3413: put
  db->Put(write_options, "key10192", "value69954");

  // op 3414: delete_range
  status = db->DeleteRange(write_options, "key2319", "key5313");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 3415: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5967", "key7382", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5967, key7382) size=" << scan_result.size() << std::endl;
  }

  // op 3416: put
  db->Put(write_options, "key3208", "value76030");

  // op 3417: get
  status = db->Get(read_options, "key5472", &value);
  if (status.ok()) {
    out << "GET key5472 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3418: put
  db->Put(write_options, "key0723", "value80813");

  // op 3419: put
  db->Put(write_options, "key6253", "value98592");

  // op 3420: delete_range
  status = db->DeleteRange(write_options, "key7107", "key8236");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 3421: delete_range
  status = db->DeleteRange(write_options, "key8849", "key9318");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 3422: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 3423: delete
  db->Delete(write_options, "key11347");

  // op 3424: get
  status = db->Get(read_options, "key10081", &value);
  if (status.ok()) {
    out << "GET key10081 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3425: put
  db->Put(write_options, "key4386", "value19219");

  // op 3426: get
  status = db->Get(read_options, "key7836", &value);
  if (status.ok()) {
    out << "GET key7836 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3427: put
  db->Put(write_options, "key6564", "value98806");

  // op 3428: get
  status = db->Get(read_options, "key3485", &value);
  if (status.ok()) {
    out << "GET key3485 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3429: delete_range
  status = db->DeleteRange(write_options, "key2831", "key11278");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 3430: put
  db->Put(write_options, "key9653", "value42974");

  // op 3431: delete_range
  status = db->DeleteRange(write_options, "key4798", "key6166");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 3432: put
  db->Put(write_options, "key4006", "value19215");

  // op 3433: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1986", "key2825", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1986, key2825) size=" << scan_result.size() << std::endl;
  }

  // op 3434: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3265", "key7798", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3265, key7798) size=" << scan_result.size() << std::endl;
  }

  // op 3435: put
  db->Put(write_options, "key8441", "value76343");

  // op 3436: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5083", "key5480", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5083, key5480) size=" << scan_result.size() << std::endl;
  }

  // op 3437: get
  status = db->Get(read_options, "key8998", &value);
  if (status.ok()) {
    out << "GET key8998 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3438: get
  status = db->Get(read_options, "key0803", &value);
  if (status.ok()) {
    out << "GET key0803 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3439: get
  status = db->Get(read_options, "key6535", &value);
  if (status.ok()) {
    out << "GET key6535 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3440: get
  status = db->Get(read_options, "key6039", &value);
  if (status.ok()) {
    out << "GET key6039 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3441: delete_range
  status = db->DeleteRange(write_options, "key2507", "key8273");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 3442: put
  db->Put(write_options, "key8641", "value17981");

  // op 3443: put
  db->Put(write_options, "key3298", "value75894");

  // op 3444: put
  db->Put(write_options, "key11614", "value64578");

  // op 3445: put
  db->Put(write_options, "key3387", "value51242");

  // op 3446: put
  db->Put(write_options, "key3280", "value25713");

  // op 3447: get
  status = db->Get(read_options, "key6896", &value);
  if (status.ok()) {
    out << "GET key6896 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3448: get
  status = db->Get(read_options, "key7768", &value);
  if (status.ok()) {
    out << "GET key7768 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3449: put
  db->Put(write_options, "key7335", "value17969");

  // op 3450: put
  db->Put(write_options, "key6766", "value3128");

  // op 3451: put
  db->Put(write_options, "key1594", "value59746");

  // op 3452: put
  db->Put(write_options, "key10799", "value93187");

  // op 3453: put
  db->Put(write_options, "key1101", "value88030");

  // op 3454: put
  db->Put(write_options, "key5548", "value53345");

  // op 3455: get
  status = db->Get(read_options, "key0651", &value);
  if (status.ok()) {
    out << "GET key0651 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3456: put
  db->Put(write_options, "key10891", "value92436");

  // op 3457: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0961", "key5978", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0961, key5978) size=" << scan_result.size() << std::endl;
  }

  // op 3458: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5589", "key9463", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5589, key9463) size=" << scan_result.size() << std::endl;
  }

  // op 3459: put
  db->Put(write_options, "key2345", "value112598");

  // op 3460: put
  db->Put(write_options, "key8528", "value9134");

  // op 3461: get
  status = db->Get(read_options, "key8301", &value);
  if (status.ok()) {
    out << "GET key8301 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3462: delete_range
  status = db->DeleteRange(write_options, "key0716", "key9420");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 3463: delete
  db->Delete(write_options, "key10736");

  // op 3464: put
  db->Put(write_options, "key5568", "value72110");

  // op 3465: delete
  db->Delete(write_options, "key5011");

  // op 3466: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2287", "key2827", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2287, key2827) size=" << scan_result.size() << std::endl;
  }

  // op 3467: get
  status = db->Get(read_options, "key0620", &value);
  if (status.ok()) {
    out << "GET key0620 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3468: delete_range
  status = db->DeleteRange(write_options, "key0756", "key2242");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 3469: delete
  db->Delete(write_options, "key9011");

  // op 3470: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9242", "key9470", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9242, key9470) size=" << scan_result.size() << std::endl;
  }

  // op 3471: put
  db->Put(write_options, "key0190", "value3545");

  // op 3472: delete_range
  status = db->DeleteRange(write_options, "key4386", "key11203");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 3473: put
  db->Put(write_options, "key4681", "value8220");

  // op 3474: get
  status = db->Get(read_options, "key11181", &value);
  if (status.ok()) {
    out << "GET key11181 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3475: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4351", "key9652", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4351, key9652) size=" << scan_result.size() << std::endl;
  }

  // op 3476: put
  db->Put(write_options, "key1937", "value103051");

  // op 3477: get
  status = db->Get(read_options, "key1345", &value);
  if (status.ok()) {
    out << "GET key1345 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3478: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4130", "key6755", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4130, key6755) size=" << scan_result.size() << std::endl;
  }

  // op 3479: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8053", "key9257", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8053, key9257) size=" << scan_result.size() << std::endl;
  }

  // op 3480: put
  db->Put(write_options, "key10045", "value74083");

  // op 3481: put
  db->Put(write_options, "key7792", "value60556");

  // op 3482: get
  status = db->Get(read_options, "key5177", &value);
  if (status.ok()) {
    out << "GET key5177 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3483: put
  db->Put(write_options, "key0055", "value2299");

  // op 3484: delete_range
  status = db->DeleteRange(write_options, "key0728", "key8462");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 3485: delete
  db->Delete(write_options, "key10460");

  // op 3486: delete_range
  status = db->DeleteRange(write_options, "key6506", "key7272");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 3487: put
  db->Put(write_options, "key0484", "value17980");

  // op 3488: get
  status = db->Get(read_options, "key8942", &value);
  if (status.ok()) {
    out << "GET key8942 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3489: put
  db->Put(write_options, "key2687", "value84476");

  // op 3490: get
  status = db->Get(read_options, "key5401", &value);
  if (status.ok()) {
    out << "GET key5401 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3491: scan
  scan_result.clear();
  status = db->Scan(read_options, "key11095", "key11107", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key11095, key11107) size=" << scan_result.size() << std::endl;
  }

  // op 3492: get
  status = db->Get(read_options, "key4070", &value);
  if (status.ok()) {
    out << "GET key4070 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3493: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 3494: put
  db->Put(write_options, "key2618", "value39410");

  // op 3495: delete_range
  status = db->DeleteRange(write_options, "key0753", "key11189");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 3496: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2580", "key2853", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2580, key2853) size=" << scan_result.size() << std::endl;
  }

  // op 3497: delete_range
  status = db->DeleteRange(write_options, "key4994", "key11722");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 3498: put
  db->Put(write_options, "key4616", "value40598");

  // op 3499: put
  db->Put(write_options, "key5477", "value42529");

  // op 3500: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1591", "key10729", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1591, key10729) size=" << scan_result.size() << std::endl;
  }

  // op 3501: put
  db->Put(write_options, "key3980", "value114858");

  // op 3502: get
  status = db->Get(read_options, "key10054", &value);
  if (status.ok()) {
    out << "GET key10054 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3503: put
  db->Put(write_options, "key3063", "value70925");

  // op 3504: get
  status = db->Get(read_options, "key2446", &value);
  if (status.ok()) {
    out << "GET key2446 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3505: get
  status = db->Get(read_options, "key2641", &value);
  if (status.ok()) {
    out << "GET key2641 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3506: put
  db->Put(write_options, "key7452", "value65423");

  // op 3507: get
  status = db->Get(read_options, "key5544", &value);
  if (status.ok()) {
    out << "GET key5544 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3508: put
  db->Put(write_options, "key1387", "value107408");

  // op 3509: delete
  db->Delete(write_options, "key7841");

  // op 3510: get
  status = db->Get(read_options, "key3186", &value);
  if (status.ok()) {
    out << "GET key3186 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3511: put
  db->Put(write_options, "key4481", "value89201");

  // op 3512: delete
  db->Delete(write_options, "key4876");

  // op 3513: delete_range
  status = db->DeleteRange(write_options, "key2532", "key7898");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 3514: put
  db->Put(write_options, "key3681", "value108346");

  // op 3515: put
  db->Put(write_options, "key4423", "value117664");

  // op 3516: get
  status = db->Get(read_options, "key11712", &value);
  if (status.ok()) {
    out << "GET key11712 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3517: delete
  db->Delete(write_options, "key7517");

  // op 3518: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 3519: put
  db->Put(write_options, "key5442", "value17604");

  // op 3520: delete
  db->Delete(write_options, "key4947");

  // op 3521: delete_range
  status = db->DeleteRange(write_options, "key8507", "key11383");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 3522: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1464", "key6098", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1464, key6098) size=" << scan_result.size() << std::endl;
  }

  // op 3523: put
  db->Put(write_options, "key1001", "value55010");

  // op 3524: get
  status = db->Get(read_options, "key9386", &value);
  if (status.ok()) {
    out << "GET key9386 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3525: get
  status = db->Get(read_options, "key0784", &value);
  if (status.ok()) {
    out << "GET key0784 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3526: put
  db->Put(write_options, "key7235", "value35356");

  // op 3527: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7939", "key11482", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7939, key11482) size=" << scan_result.size() << std::endl;
  }

  // op 3528: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10741", "key11558", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10741, key11558) size=" << scan_result.size() << std::endl;
  }

  // op 3529: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1091", "key10544", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1091, key10544) size=" << scan_result.size() << std::endl;
  }

  // op 3530: delete_range
  status = db->DeleteRange(write_options, "key9901", "key11363");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 3531: put
  db->Put(write_options, "key2374", "value0336");

  // op 3532: put
  db->Put(write_options, "key6650", "value64791");

  // op 3533: get
  status = db->Get(read_options, "key7903", &value);
  if (status.ok()) {
    out << "GET key7903 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3534: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 3535: get
  status = db->Get(read_options, "key8164", &value);
  if (status.ok()) {
    out << "GET key8164 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3536: delete
  db->Delete(write_options, "key3223");

  // op 3537: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0285", "key6926", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0285, key6926) size=" << scan_result.size() << std::endl;
  }

  // op 3538: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0014", "key3914", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0014, key3914) size=" << scan_result.size() << std::endl;
  }

  // op 3539: delete
  db->Delete(write_options, "key2057");

  // op 3540: delete_range
  status = db->DeleteRange(write_options, "key8303", "key9299");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 3541: delete
  db->Delete(write_options, "key7570");

  // op 3542: put
  db->Put(write_options, "key8524", "value60684");

  // op 3543: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1206", "key7389", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1206, key7389) size=" << scan_result.size() << std::endl;
  }

  // op 3544: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2748", "key10914", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2748, key10914) size=" << scan_result.size() << std::endl;
  }

  // op 3545: put
  db->Put(write_options, "key11278", "value57299");

  // op 3546: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9045", "key10819", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9045, key10819) size=" << scan_result.size() << std::endl;
  }

  // op 3547: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3080", "key3674", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3080, key3674) size=" << scan_result.size() << std::endl;
  }

  // op 3548: delete
  db->Delete(write_options, "key10595");

  // op 3549: put
  db->Put(write_options, "key8499", "value117326");

  // op 3550: get
  status = db->Get(read_options, "key7289", &value);
  if (status.ok()) {
    out << "GET key7289 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3551: put
  db->Put(write_options, "key2223", "value64792");

  // op 3552: put
  db->Put(write_options, "key6885", "value20927");

  // op 3553: put
  db->Put(write_options, "key6618", "value105374");

  // op 3554: put
  db->Put(write_options, "key5388", "value78186");

  // op 3555: put
  db->Put(write_options, "key9560", "value89347");

  // op 3556: get
  status = db->Get(read_options, "key2104", &value);
  if (status.ok()) {
    out << "GET key2104 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3557: put
  db->Put(write_options, "key0983", "value19775");

  // op 3558: put
  db->Put(write_options, "key9142", "value118142");

  // op 3559: get
  status = db->Get(read_options, "key11708", &value);
  if (status.ok()) {
    out << "GET key11708 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3560: delete_range
  status = db->DeleteRange(write_options, "key3176", "key8638");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 3561: put
  db->Put(write_options, "key1904", "value91801");

  // op 3562: put
  db->Put(write_options, "key8605", "value105148");

  // op 3563: put
  db->Put(write_options, "key5267", "value72979");

  // op 3564: get
  status = db->Get(read_options, "key5853", &value);
  if (status.ok()) {
    out << "GET key5853 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3565: put
  db->Put(write_options, "key11090", "value96127");

  // op 3566: delete
  db->Delete(write_options, "key5447");

  // op 3567: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6798", "key11072", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6798, key11072) size=" << scan_result.size() << std::endl;
  }

  // op 3568: put
  db->Put(write_options, "key10162", "value23814");

  // op 3569: get
  status = db->Get(read_options, "key0771", &value);
  if (status.ok()) {
    out << "GET key0771 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3570: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 3571: get
  status = db->Get(read_options, "key5195", &value);
  if (status.ok()) {
    out << "GET key5195 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3572: delete
  db->Delete(write_options, "key7349");

  // op 3573: get
  status = db->Get(read_options, "key2592", &value);
  if (status.ok()) {
    out << "GET key2592 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3574: put
  db->Put(write_options, "key0848", "value21861");

  // op 3575: get
  status = db->Get(read_options, "key5141", &value);
  if (status.ok()) {
    out << "GET key5141 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3576: delete
  db->Delete(write_options, "key1172");

  // op 3577: get
  status = db->Get(read_options, "key2307", &value);
  if (status.ok()) {
    out << "GET key2307 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3578: put
  db->Put(write_options, "key9398", "value35299");

  // op 3579: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 3580: put
  db->Put(write_options, "key9535", "value95521");

  // op 3581: get
  status = db->Get(read_options, "key6117", &value);
  if (status.ok()) {
    out << "GET key6117 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3582: put
  db->Put(write_options, "key10379", "value35465");

  // op 3583: get
  status = db->Get(read_options, "key0652", &value);
  if (status.ok()) {
    out << "GET key0652 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3584: put
  db->Put(write_options, "key11011", "value17906");

  // op 3585: put
  db->Put(write_options, "key6356", "value4700");

  // op 3586: put
  db->Put(write_options, "key7131", "value57631");

  // op 3587: put
  db->Put(write_options, "key0643", "value88087");

  // op 3588: get
  status = db->Get(read_options, "key1824", &value);
  if (status.ok()) {
    out << "GET key1824 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3589: put
  db->Put(write_options, "key5314", "value14696");

  // op 3590: get
  status = db->Get(read_options, "key2030", &value);
  if (status.ok()) {
    out << "GET key2030 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3591: delete
  db->Delete(write_options, "key7884");

  // op 3592: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7441", "key9149", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7441, key9149) size=" << scan_result.size() << std::endl;
  }

  // op 3593: put
  db->Put(write_options, "key2659", "value25155");

  // op 3594: delete_range
  status = db->DeleteRange(write_options, "key9749", "key10952");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 3595: put
  db->Put(write_options, "key4626", "value14969");

  // op 3596: put
  db->Put(write_options, "key6153", "value8591");

  // op 3597: put
  db->Put(write_options, "key9469", "value17988");

  // op 3598: put
  db->Put(write_options, "key2487", "value45701");

  // op 3599: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 3600: put
  db->Put(write_options, "key5696", "value80038");

  // op 3601: delete_range
  status = db->DeleteRange(write_options, "key1902", "key11048");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 3602: get
  status = db->Get(read_options, "key5398", &value);
  if (status.ok()) {
    out << "GET key5398 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3603: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10589", "key11014", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10589, key11014) size=" << scan_result.size() << std::endl;
  }

  // op 3604: get
  status = db->Get(read_options, "key3273", &value);
  if (status.ok()) {
    out << "GET key3273 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3605: get
  status = db->Get(read_options, "key11688", &value);
  if (status.ok()) {
    out << "GET key11688 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3606: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6761", "key11715", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6761, key11715) size=" << scan_result.size() << std::endl;
  }

  // op 3607: put
  db->Put(write_options, "key8624", "value81861");

  // op 3608: delete
  db->Delete(write_options, "key6402");

  // op 3609: get
  status = db->Get(read_options, "key5867", &value);
  if (status.ok()) {
    out << "GET key5867 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3610: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 3611: delete
  db->Delete(write_options, "key10846");

  // op 3612: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2174", "key8939", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2174, key8939) size=" << scan_result.size() << std::endl;
  }

  // op 3613: get
  status = db->Get(read_options, "key1668", &value);
  if (status.ok()) {
    out << "GET key1668 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3614: delete
  db->Delete(write_options, "key0453");

  // op 3615: delete
  db->Delete(write_options, "key6617");

  // op 3616: delete
  db->Delete(write_options, "key0619");

  // op 3617: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1250", "key3176", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1250, key3176) size=" << scan_result.size() << std::endl;
  }

  // op 3618: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3383", "key7154", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3383, key7154) size=" << scan_result.size() << std::endl;
  }

  // op 3619: get
  status = db->Get(read_options, "key9709", &value);
  if (status.ok()) {
    out << "GET key9709 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3620: get
  status = db->Get(read_options, "key6974", &value);
  if (status.ok()) {
    out << "GET key6974 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3621: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0755", "key1037", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0755, key1037) size=" << scan_result.size() << std::endl;
  }

  // op 3622: put
  db->Put(write_options, "key2706", "value94527");

  // op 3623: put
  db->Put(write_options, "key3851", "value56260");

  // op 3624: get
  status = db->Get(read_options, "key2407", &value);
  if (status.ok()) {
    out << "GET key2407 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3625: delete_range
  status = db->DeleteRange(write_options, "key6660", "key6700");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 3626: delete
  db->Delete(write_options, "key2609");

  // op 3627: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2726", "key2845", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2726, key2845) size=" << scan_result.size() << std::endl;
  }

  // op 3628: get
  status = db->Get(read_options, "key8513", &value);
  if (status.ok()) {
    out << "GET key8513 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3629: put
  db->Put(write_options, "key5845", "value111447");

  // op 3630: put
  db->Put(write_options, "key2662", "value41178");

  // op 3631: get
  status = db->Get(read_options, "key11302", &value);
  if (status.ok()) {
    out << "GET key11302 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3632: put
  db->Put(write_options, "key3355", "value61943");

  // op 3633: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2465", "key5321", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2465, key5321) size=" << scan_result.size() << std::endl;
  }

  // op 3634: get
  status = db->Get(read_options, "key6008", &value);
  if (status.ok()) {
    out << "GET key6008 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3635: get
  status = db->Get(read_options, "key1143", &value);
  if (status.ok()) {
    out << "GET key1143 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3636: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6925", "key11203", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6925, key11203) size=" << scan_result.size() << std::endl;
  }

  // op 3637: get
  status = db->Get(read_options, "key0022", &value);
  if (status.ok()) {
    out << "GET key0022 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3638: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9445", "key9662", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9445, key9662) size=" << scan_result.size() << std::endl;
  }

  // op 3639: get
  status = db->Get(read_options, "key9211", &value);
  if (status.ok()) {
    out << "GET key9211 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3640: put
  db->Put(write_options, "key7750", "value59129");

  // op 3641: put
  db->Put(write_options, "key7025", "value67509");

  // op 3642: put
  db->Put(write_options, "key0426", "value100327");

  // op 3643: put
  db->Put(write_options, "key10837", "value24605");

  // op 3644: put
  db->Put(write_options, "key9331", "value16170");

  // op 3645: put
  db->Put(write_options, "key2349", "value61125");

  // op 3646: put
  db->Put(write_options, "key4123", "value69883");

  // op 3647: delete_range
  status = db->DeleteRange(write_options, "key6343", "key8888");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 3648: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9096", "key9151", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9096, key9151) size=" << scan_result.size() << std::endl;
  }

  // op 3649: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4249", "key6370", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4249, key6370) size=" << scan_result.size() << std::endl;
  }

  // op 3650: put
  db->Put(write_options, "key8590", "value32084");

  // op 3651: put
  db->Put(write_options, "key5815", "value113054");

  // op 3652: put
  db->Put(write_options, "key10203", "value46391");

  // op 3653: get
  status = db->Get(read_options, "key1754", &value);
  if (status.ok()) {
    out << "GET key1754 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3654: put
  db->Put(write_options, "key5966", "value55511");

  // op 3655: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9561", "key9897", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9561, key9897) size=" << scan_result.size() << std::endl;
  }

  // op 3656: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0776", "key8462", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0776, key8462) size=" << scan_result.size() << std::endl;
  }

  // op 3657: put
  db->Put(write_options, "key0846", "value61720");

  // op 3658: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2864", "key10675", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2864, key10675) size=" << scan_result.size() << std::endl;
  }

  // op 3659: put
  db->Put(write_options, "key10649", "value77464");

  // op 3660: get
  status = db->Get(read_options, "key2152", &value);
  if (status.ok()) {
    out << "GET key2152 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3661: get
  status = db->Get(read_options, "key9528", &value);
  if (status.ok()) {
    out << "GET key9528 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3662: put
  db->Put(write_options, "key5349", "value3158");

  // op 3663: get
  status = db->Get(read_options, "key8782", &value);
  if (status.ok()) {
    out << "GET key8782 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3664: put
  db->Put(write_options, "key2967", "value11066");

  // op 3665: get
  status = db->Get(read_options, "key1562", &value);
  if (status.ok()) {
    out << "GET key1562 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3666: put
  db->Put(write_options, "key5912", "value78617");

  // op 3667: put
  db->Put(write_options, "key3965", "value42137");

  // op 3668: put
  db->Put(write_options, "key1234", "value27128");

  // op 3669: put
  db->Put(write_options, "key10082", "value70502");

  // op 3670: get
  status = db->Get(read_options, "key4409", &value);
  if (status.ok()) {
    out << "GET key4409 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3671: delete
  db->Delete(write_options, "key9096");

  // op 3672: put
  db->Put(write_options, "key0982", "value21174");

  // op 3673: delete_range
  status = db->DeleteRange(write_options, "key6492", "key7652");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 3674: put
  db->Put(write_options, "key5004", "value75828");

  // op 3675: get
  status = db->Get(read_options, "key3613", &value);
  if (status.ok()) {
    out << "GET key3613 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3676: put
  db->Put(write_options, "key7959", "value113490");

  // op 3677: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2863", "key9205", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2863, key9205) size=" << scan_result.size() << std::endl;
  }

  // op 3678: put
  db->Put(write_options, "key9868", "value106866");

  // op 3679: put
  db->Put(write_options, "key4613", "value83161");

  // op 3680: get
  status = db->Get(read_options, "key5712", &value);
  if (status.ok()) {
    out << "GET key5712 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3681: put
  db->Put(write_options, "key11166", "value60270");

  // op 3682: delete_range
  status = db->DeleteRange(write_options, "key5965", "key8915");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 3683: put
  db->Put(write_options, "key11121", "value69354");

  // op 3684: put
  db->Put(write_options, "key5111", "value11554");

  // op 3685: get
  status = db->Get(read_options, "key10310", &value);
  if (status.ok()) {
    out << "GET key10310 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3686: put
  db->Put(write_options, "key8683", "value64003");

  // op 3687: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 3688: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5799", "key9254", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5799, key9254) size=" << scan_result.size() << std::endl;
  }

  // op 3689: get
  status = db->Get(read_options, "key2165", &value);
  if (status.ok()) {
    out << "GET key2165 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3690: delete_range
  status = db->DeleteRange(write_options, "key2972", "key3716");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 3691: get
  status = db->Get(read_options, "key1559", &value);
  if (status.ok()) {
    out << "GET key1559 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3692: get
  status = db->Get(read_options, "key11382", &value);
  if (status.ok()) {
    out << "GET key11382 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3693: get
  status = db->Get(read_options, "key11503", &value);
  if (status.ok()) {
    out << "GET key11503 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3694: put
  db->Put(write_options, "key11556", "value77470");

  // op 3695: delete
  db->Delete(write_options, "key8029");

  // op 3696: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5507", "key8432", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5507, key8432) size=" << scan_result.size() << std::endl;
  }

  // op 3697: put
  db->Put(write_options, "key9960", "value18988");

  // op 3698: put
  db->Put(write_options, "key4526", "value88469");

  // op 3699: get
  status = db->Get(read_options, "key7332", &value);
  if (status.ok()) {
    out << "GET key7332 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3700: get
  status = db->Get(read_options, "key1684", &value);
  if (status.ok()) {
    out << "GET key1684 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3701: put
  db->Put(write_options, "key9009", "value82201");

  // op 3702: put
  db->Put(write_options, "key9916", "value15444");

  // op 3703: put
  db->Put(write_options, "key9511", "value78688");

  // op 3704: delete
  db->Delete(write_options, "key1316");

  // op 3705: delete
  db->Delete(write_options, "key2512");

  // op 3706: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3960", "key10340", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3960, key10340) size=" << scan_result.size() << std::endl;
  }

  // op 3707: delete_range
  status = db->DeleteRange(write_options, "key4072", "key8836");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 3708: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 3709: get
  status = db->Get(read_options, "key6874", &value);
  if (status.ok()) {
    out << "GET key6874 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3710: get
  status = db->Get(read_options, "key11124", &value);
  if (status.ok()) {
    out << "GET key11124 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3711: get
  status = db->Get(read_options, "key9688", &value);
  if (status.ok()) {
    out << "GET key9688 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3712: put
  db->Put(write_options, "key5800", "value21800");

  // op 3713: put
  db->Put(write_options, "key3959", "value10599");

  // op 3714: get
  status = db->Get(read_options, "key10608", &value);
  if (status.ok()) {
    out << "GET key10608 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3715: put
  db->Put(write_options, "key1995", "value33224");

  // op 3716: delete
  db->Delete(write_options, "key0029");

  // op 3717: delete
  db->Delete(write_options, "key7504");

  // op 3718: put
  db->Put(write_options, "key5297", "value1238");

  // op 3719: put
  db->Put(write_options, "key3886", "value44194");

  // op 3720: put
  db->Put(write_options, "key10080", "value111445");

  // op 3721: put
  db->Put(write_options, "key11364", "value43363");

  // op 3722: put
  db->Put(write_options, "key0751", "value70314");

  // op 3723: put
  db->Put(write_options, "key5718", "value90069");

  // op 3724: delete_range
  status = db->DeleteRange(write_options, "key4730", "key8610");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 3725: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0255", "key4610", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0255, key4610) size=" << scan_result.size() << std::endl;
  }

  // op 3726: put
  db->Put(write_options, "key1225", "value48428");

  // op 3727: get
  status = db->Get(read_options, "key6497", &value);
  if (status.ok()) {
    out << "GET key6497 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3728: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5573", "key8436", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5573, key8436) size=" << scan_result.size() << std::endl;
  }

  // op 3729: get
  status = db->Get(read_options, "key9816", &value);
  if (status.ok()) {
    out << "GET key9816 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3730: get
  status = db->Get(read_options, "key8413", &value);
  if (status.ok()) {
    out << "GET key8413 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3731: put
  db->Put(write_options, "key7117", "value98944");

  // op 3732: get
  status = db->Get(read_options, "key5927", &value);
  if (status.ok()) {
    out << "GET key5927 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3733: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5236", "key8019", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5236, key8019) size=" << scan_result.size() << std::endl;
  }

  // op 3734: delete
  db->Delete(write_options, "key5720");

  // op 3735: put
  db->Put(write_options, "key8895", "value105455");

  // op 3736: put
  db->Put(write_options, "key10184", "value29046");

  // op 3737: put
  db->Put(write_options, "key4265", "value33030");

  // op 3738: put
  db->Put(write_options, "key8867", "value13963");

  // op 3739: get
  status = db->Get(read_options, "key6255", &value);
  if (status.ok()) {
    out << "GET key6255 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3740: put
  db->Put(write_options, "key0511", "value115731");

  // op 3741: put
  db->Put(write_options, "key1882", "value69458");

  // op 3742: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5679", "key9547", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5679, key9547) size=" << scan_result.size() << std::endl;
  }

  // op 3743: delete
  db->Delete(write_options, "key6279");

  // op 3744: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1909", "key9034", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1909, key9034) size=" << scan_result.size() << std::endl;
  }

  // op 3745: put
  db->Put(write_options, "key5932", "value18901");

  // op 3746: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 3747: put
  db->Put(write_options, "key6089", "value13144");

  // op 3748: delete_range
  status = db->DeleteRange(write_options, "key3437", "key8366");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 3749: get
  status = db->Get(read_options, "key10573", &value);
  if (status.ok()) {
    out << "GET key10573 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3750: get
  status = db->Get(read_options, "key4797", &value);
  if (status.ok()) {
    out << "GET key4797 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3751: get
  status = db->Get(read_options, "key3664", &value);
  if (status.ok()) {
    out << "GET key3664 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3752: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 3753: put
  db->Put(write_options, "key7852", "value56647");

  // op 3754: put
  db->Put(write_options, "key11714", "value13664");

  // op 3755: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 3756: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2728", "key3156", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2728, key3156) size=" << scan_result.size() << std::endl;
  }

  // op 3757: delete_range
  status = db->DeleteRange(write_options, "key0852", "key4610");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 3758: put
  db->Put(write_options, "key2848", "value1628");

  // op 3759: delete
  db->Delete(write_options, "key6322");

  // op 3760: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1337", "key7773", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1337, key7773) size=" << scan_result.size() << std::endl;
  }

  // op 3761: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8889", "key11609", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8889, key11609) size=" << scan_result.size() << std::endl;
  }

  // op 3762: put
  db->Put(write_options, "key2401", "value114798");

  // op 3763: get
  status = db->Get(read_options, "key5414", &value);
  if (status.ok()) {
    out << "GET key5414 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3764: put
  db->Put(write_options, "key11543", "value113859");

  // op 3765: get
  status = db->Get(read_options, "key8525", &value);
  if (status.ok()) {
    out << "GET key8525 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3766: put
  db->Put(write_options, "key0781", "value103811");

  // op 3767: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3951", "key7919", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3951, key7919) size=" << scan_result.size() << std::endl;
  }

  // op 3768: delete
  db->Delete(write_options, "key9720");

  // op 3769: delete_range
  status = db->DeleteRange(write_options, "key1599", "key2008");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 3770: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6859", "key9270", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6859, key9270) size=" << scan_result.size() << std::endl;
  }

  // op 3771: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5175", "key8739", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5175, key8739) size=" << scan_result.size() << std::endl;
  }

  // op 3772: delete
  db->Delete(write_options, "key2694");

  // op 3773: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5339", "key6237", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5339, key6237) size=" << scan_result.size() << std::endl;
  }

  // op 3774: put
  db->Put(write_options, "key5648", "value67711");

  // op 3775: put
  db->Put(write_options, "key3055", "value61741");

  // op 3776: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 3777: get
  status = db->Get(read_options, "key1957", &value);
  if (status.ok()) {
    out << "GET key1957 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3778: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6288", "key10921", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6288, key10921) size=" << scan_result.size() << std::endl;
  }

  // op 3779: put
  db->Put(write_options, "key2754", "value109643");

  // op 3780: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0928", "key8489", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0928, key8489) size=" << scan_result.size() << std::endl;
  }

  // op 3781: put
  db->Put(write_options, "key4407", "value54578");

  // op 3782: get
  status = db->Get(read_options, "key4018", &value);
  if (status.ok()) {
    out << "GET key4018 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3783: delete_range
  status = db->DeleteRange(write_options, "key3388", "key3899");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 3784: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0590", "key9857", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0590, key9857) size=" << scan_result.size() << std::endl;
  }

  // op 3785: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0326", "key4575", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0326, key4575) size=" << scan_result.size() << std::endl;
  }

  // op 3786: put
  db->Put(write_options, "key0985", "value95692");

  // op 3787: get
  status = db->Get(read_options, "key2257", &value);
  if (status.ok()) {
    out << "GET key2257 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3788: delete_range
  status = db->DeleteRange(write_options, "key8302", "key10677");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 3789: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 3790: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 3791: put
  db->Put(write_options, "key8592", "value52498");

  // op 3792: get
  status = db->Get(read_options, "key8646", &value);
  if (status.ok()) {
    out << "GET key8646 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3793: get
  status = db->Get(read_options, "key5349", &value);
  if (status.ok()) {
    out << "GET key5349 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3794: get
  status = db->Get(read_options, "key5082", &value);
  if (status.ok()) {
    out << "GET key5082 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3795: put
  db->Put(write_options, "key3341", "value10610");

  // op 3796: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4020", "key7374", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4020, key7374) size=" << scan_result.size() << std::endl;
  }

  // op 3797: get
  status = db->Get(read_options, "key9019", &value);
  if (status.ok()) {
    out << "GET key9019 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3798: put
  db->Put(write_options, "key2431", "value16892");

  // op 3799: get
  status = db->Get(read_options, "key2396", &value);
  if (status.ok()) {
    out << "GET key2396 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3800: get
  status = db->Get(read_options, "key11575", &value);
  if (status.ok()) {
    out << "GET key11575 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3801: delete_range
  status = db->DeleteRange(write_options, "key9324", "key9497");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 3802: get
  status = db->Get(read_options, "key8787", &value);
  if (status.ok()) {
    out << "GET key8787 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3803: put
  db->Put(write_options, "key0552", "value90649");

  // op 3804: delete_range
  status = db->DeleteRange(write_options, "key3017", "key9022");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 3805: get
  status = db->Get(read_options, "key10174", &value);
  if (status.ok()) {
    out << "GET key10174 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3806: put
  db->Put(write_options, "key1068", "value111558");

  // op 3807: get
  status = db->Get(read_options, "key3007", &value);
  if (status.ok()) {
    out << "GET key3007 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3808: put
  db->Put(write_options, "key3853", "value67607");

  // op 3809: delete_range
  status = db->DeleteRange(write_options, "key4047", "key6328");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 3810: put
  db->Put(write_options, "key7448", "value115980");

  // op 3811: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 3812: delete
  db->Delete(write_options, "key11563");

  // op 3813: delete
  db->Delete(write_options, "key0679");

  // op 3814: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0178", "key1434", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0178, key1434) size=" << scan_result.size() << std::endl;
  }

  // op 3815: delete
  db->Delete(write_options, "key5745");

  // op 3816: delete_range
  status = db->DeleteRange(write_options, "key1870", "key2682");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 3817: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 3818: put
  db->Put(write_options, "key6193", "value24673");

  // op 3819: get
  status = db->Get(read_options, "key0330", &value);
  if (status.ok()) {
    out << "GET key0330 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3820: get
  status = db->Get(read_options, "key11369", &value);
  if (status.ok()) {
    out << "GET key11369 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3821: put
  db->Put(write_options, "key5180", "value60571");

  // op 3822: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 3823: delete_range
  status = db->DeleteRange(write_options, "key4019", "key8658");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 3824: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3882", "key6713", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3882, key6713) size=" << scan_result.size() << std::endl;
  }

  // op 3825: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2630", "key3886", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2630, key3886) size=" << scan_result.size() << std::endl;
  }

  // op 3826: get
  status = db->Get(read_options, "key7715", &value);
  if (status.ok()) {
    out << "GET key7715 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3827: delete_range
  status = db->DeleteRange(write_options, "key4797", "key8597");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 3828: put
  db->Put(write_options, "key8722", "value87632");

  // op 3829: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 3830: get
  status = db->Get(read_options, "key10898", &value);
  if (status.ok()) {
    out << "GET key10898 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3831: put
  db->Put(write_options, "key7933", "value60622");

  // op 3832: put
  db->Put(write_options, "key2298", "value31721");

  // op 3833: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5299", "key5568", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5299, key5568) size=" << scan_result.size() << std::endl;
  }

  // op 3834: delete
  db->Delete(write_options, "key7961");

  // op 3835: put
  db->Put(write_options, "key4364", "value67631");

  // op 3836: put
  db->Put(write_options, "key10488", "value102803");

  // op 3837: get
  status = db->Get(read_options, "key1882", &value);
  if (status.ok()) {
    out << "GET key1882 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3838: get
  status = db->Get(read_options, "key4427", &value);
  if (status.ok()) {
    out << "GET key4427 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3839: delete
  db->Delete(write_options, "key2884");

  // op 3840: delete
  db->Delete(write_options, "key5383");

  // op 3841: delete
  db->Delete(write_options, "key7016");

  // op 3842: get
  status = db->Get(read_options, "key2423", &value);
  if (status.ok()) {
    out << "GET key2423 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3843: get
  status = db->Get(read_options, "key3899", &value);
  if (status.ok()) {
    out << "GET key3899 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3844: get
  status = db->Get(read_options, "key8893", &value);
  if (status.ok()) {
    out << "GET key8893 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3845: delete
  db->Delete(write_options, "key0287");

  // op 3846: delete
  db->Delete(write_options, "key4018");

  // op 3847: put
  db->Put(write_options, "key0351", "value33277");

  // op 3848: put
  db->Put(write_options, "key11165", "value116759");

  // op 3849: put
  db->Put(write_options, "key2600", "value79690");

  // op 3850: get
  status = db->Get(read_options, "key3241", &value);
  if (status.ok()) {
    out << "GET key3241 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3851: put
  db->Put(write_options, "key9085", "value25490");

  // op 3852: put
  db->Put(write_options, "key6861", "value61241");

  // op 3853: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4773", "key10473", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4773, key10473) size=" << scan_result.size() << std::endl;
  }

  // op 3854: delete
  db->Delete(write_options, "key1588");

  // op 3855: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7633", "key8156", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7633, key8156) size=" << scan_result.size() << std::endl;
  }

  // op 3856: get
  status = db->Get(read_options, "key9673", &value);
  if (status.ok()) {
    out << "GET key9673 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3857: get
  status = db->Get(read_options, "key6423", &value);
  if (status.ok()) {
    out << "GET key6423 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3858: put
  db->Put(write_options, "key6460", "value116498");

  // op 3859: delete
  db->Delete(write_options, "key10342");

  // op 3860: put
  db->Put(write_options, "key2200", "value116252");

  // op 3861: put
  db->Put(write_options, "key1539", "value49786");

  // op 3862: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1805", "key9736", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1805, key9736) size=" << scan_result.size() << std::endl;
  }

  // op 3863: put
  db->Put(write_options, "key5103", "value61309");

  // op 3864: put
  db->Put(write_options, "key0698", "value42443");

  // op 3865: scan
  scan_result.clear();
  status = db->Scan(read_options, "key11135", "key11657", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key11135, key11657) size=" << scan_result.size() << std::endl;
  }

  // op 3866: delete_range
  status = db->DeleteRange(write_options, "key0679", "key10015");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 3867: delete
  db->Delete(write_options, "key0436");

  // op 3868: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4207", "key11341", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4207, key11341) size=" << scan_result.size() << std::endl;
  }

  // op 3869: get
  status = db->Get(read_options, "key4066", &value);
  if (status.ok()) {
    out << "GET key4066 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3870: delete
  db->Delete(write_options, "key10971");

  // op 3871: delete
  db->Delete(write_options, "key0907");

  // op 3872: get
  status = db->Get(read_options, "key1963", &value);
  if (status.ok()) {
    out << "GET key1963 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3873: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3882", "key5622", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3882, key5622) size=" << scan_result.size() << std::endl;
  }

  // op 3874: delete
  db->Delete(write_options, "key7714");

  // op 3875: delete
  db->Delete(write_options, "key10819");

  // op 3876: get
  status = db->Get(read_options, "key2906", &value);
  if (status.ok()) {
    out << "GET key2906 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3877: get
  status = db->Get(read_options, "key11669", &value);
  if (status.ok()) {
    out << "GET key11669 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3878: put
  db->Put(write_options, "key9278", "value84965");

  // op 3879: put
  db->Put(write_options, "key6841", "value38576");

  // op 3880: put
  db->Put(write_options, "key0636", "value96318");

  // op 3881: delete
  db->Delete(write_options, "key10958");

  // op 3882: put
  db->Put(write_options, "key9599", "value70884");

  // op 3883: put
  db->Put(write_options, "key2196", "value20205");

  // op 3884: put
  db->Put(write_options, "key0634", "value74173");

  // op 3885: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1020", "key5535", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1020, key5535) size=" << scan_result.size() << std::endl;
  }

  // op 3886: put
  db->Put(write_options, "key5635", "value5808");

  // op 3887: put
  db->Put(write_options, "key4608", "value1222");

  // op 3888: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6426", "key10401", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6426, key10401) size=" << scan_result.size() << std::endl;
  }

  // op 3889: put
  db->Put(write_options, "key3137", "value111143");

  // op 3890: get
  status = db->Get(read_options, "key10384", &value);
  if (status.ok()) {
    out << "GET key10384 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3891: put
  db->Put(write_options, "key4736", "value41954");

  // op 3892: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3059", "key11603", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3059, key11603) size=" << scan_result.size() << std::endl;
  }

  // op 3893: delete
  db->Delete(write_options, "key5820");

  // op 3894: put
  db->Put(write_options, "key4170", "value48039");

  // op 3895: delete_range
  status = db->DeleteRange(write_options, "key4889", "key10809");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 3896: put
  db->Put(write_options, "key11469", "value9758");

  // op 3897: get
  status = db->Get(read_options, "key6448", &value);
  if (status.ok()) {
    out << "GET key6448 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3898: get
  status = db->Get(read_options, "key1742", &value);
  if (status.ok()) {
    out << "GET key1742 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3899: delete
  db->Delete(write_options, "key1366");

  // op 3900: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2780", "key8478", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2780, key8478) size=" << scan_result.size() << std::endl;
  }

  // op 3901: put
  db->Put(write_options, "key2751", "value33415");

  // op 3902: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2288", "key10042", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2288, key10042) size=" << scan_result.size() << std::endl;
  }

  // op 3903: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1358", "key2233", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1358, key2233) size=" << scan_result.size() << std::endl;
  }

  // op 3904: delete
  db->Delete(write_options, "key10862");

  // op 3905: delete_range
  status = db->DeleteRange(write_options, "key2712", "key3019");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 3906: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5010", "key7931", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5010, key7931) size=" << scan_result.size() << std::endl;
  }

  // op 3907: put
  db->Put(write_options, "key8504", "value78364");

  // op 3908: put
  db->Put(write_options, "key11368", "value64346");

  // op 3909: put
  db->Put(write_options, "key10443", "value114194");

  // op 3910: put
  db->Put(write_options, "key9461", "value47895");

  // op 3911: put
  db->Put(write_options, "key6212", "value14550");

  // op 3912: put
  db->Put(write_options, "key8130", "value2341");

  // op 3913: put
  db->Put(write_options, "key8794", "value76881");

  // op 3914: put
  db->Put(write_options, "key7273", "value40813");

  // op 3915: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 3916: put
  db->Put(write_options, "key11401", "value50242");

  // op 3917: get
  status = db->Get(read_options, "key0636", &value);
  if (status.ok()) {
    out << "GET key0636 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3918: delete_range
  status = db->DeleteRange(write_options, "key6430", "key9453");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 3919: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 3920: get
  status = db->Get(read_options, "key7826", &value);
  if (status.ok()) {
    out << "GET key7826 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3921: put
  db->Put(write_options, "key9874", "value49158");

  // op 3922: put
  db->Put(write_options, "key1715", "value94056");

  // op 3923: get
  status = db->Get(read_options, "key1624", &value);
  if (status.ok()) {
    out << "GET key1624 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3924: put
  db->Put(write_options, "key6824", "value100667");

  // op 3925: put
  db->Put(write_options, "key5802", "value47747");

  // op 3926: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2521", "key6847", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2521, key6847) size=" << scan_result.size() << std::endl;
  }

  // op 3927: put
  db->Put(write_options, "key10337", "value46479");

  // op 3928: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1652", "key2948", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1652, key2948) size=" << scan_result.size() << std::endl;
  }

  // op 3929: delete
  db->Delete(write_options, "key10241");

  // op 3930: delete_range
  status = db->DeleteRange(write_options, "key0174", "key5519");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 3931: put
  db->Put(write_options, "key3849", "value34637");

  // op 3932: put
  db->Put(write_options, "key11039", "value34141");

  // op 3933: delete_range
  status = db->DeleteRange(write_options, "key4723", "key9570");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 3934: put
  db->Put(write_options, "key6333", "value82159");

  // op 3935: put
  db->Put(write_options, "key10263", "value65432");

  // op 3936: put
  db->Put(write_options, "key7721", "value117573");

  // op 3937: get
  status = db->Get(read_options, "key7361", &value);
  if (status.ok()) {
    out << "GET key7361 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3938: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1624", "key8806", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1624, key8806) size=" << scan_result.size() << std::endl;
  }

  // op 3939: delete_range
  status = db->DeleteRange(write_options, "key3418", "key6665");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 3940: put
  db->Put(write_options, "key5841", "value14009");

  // op 3941: delete
  db->Delete(write_options, "key0341");

  // op 3942: get
  status = db->Get(read_options, "key0240", &value);
  if (status.ok()) {
    out << "GET key0240 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3943: delete
  db->Delete(write_options, "key0122");

  // op 3944: put
  db->Put(write_options, "key8672", "value5068");

  // op 3945: put
  db->Put(write_options, "key4763", "value75322");

  // op 3946: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 3947: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3240", "key9384", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3240, key9384) size=" << scan_result.size() << std::endl;
  }

  // op 3948: get
  status = db->Get(read_options, "key5006", &value);
  if (status.ok()) {
    out << "GET key5006 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3949: delete_range
  status = db->DeleteRange(write_options, "key2585", "key2919");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 3950: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5654", "key9177", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5654, key9177) size=" << scan_result.size() << std::endl;
  }

  // op 3951: put
  db->Put(write_options, "key10828", "value83283");

  // op 3952: put
  db->Put(write_options, "key3712", "value92477");

  // op 3953: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 3954: put
  db->Put(write_options, "key3554", "value61375");

  // op 3955: put
  db->Put(write_options, "key1414", "value86688");

  // op 3956: put
  db->Put(write_options, "key5077", "value84708");

  // op 3957: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2836", "key7219", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2836, key7219) size=" << scan_result.size() << std::endl;
  }

  // op 3958: put
  db->Put(write_options, "key10184", "value101725");

  // op 3959: put
  db->Put(write_options, "key7240", "value33827");

  // op 3960: get
  status = db->Get(read_options, "key0313", &value);
  if (status.ok()) {
    out << "GET key0313 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3961: put
  db->Put(write_options, "key4270", "value9381");

  // op 3962: put
  db->Put(write_options, "key3223", "value24744");

  // op 3963: put
  db->Put(write_options, "key9774", "value81198");

  // op 3964: delete
  db->Delete(write_options, "key5608");

  // op 3965: put
  db->Put(write_options, "key4976", "value55247");

  // op 3966: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2348", "key8519", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2348, key8519) size=" << scan_result.size() << std::endl;
  }

  // op 3967: put
  db->Put(write_options, "key8796", "value45738");

  // op 3968: delete
  db->Delete(write_options, "key3377");

  // op 3969: put
  db->Put(write_options, "key2271", "value48212");

  // op 3970: put
  db->Put(write_options, "key9359", "value112293");

  // op 3971: delete_range
  status = db->DeleteRange(write_options, "key7029", "key9181");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 3972: get
  status = db->Get(read_options, "key10140", &value);
  if (status.ok()) {
    out << "GET key10140 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3973: get
  status = db->Get(read_options, "key4057", &value);
  if (status.ok()) {
    out << "GET key4057 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3974: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 3975: get
  status = db->Get(read_options, "key3008", &value);
  if (status.ok()) {
    out << "GET key3008 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3976: put
  db->Put(write_options, "key10675", "value65513");

  // op 3977: put
  db->Put(write_options, "key0953", "value92164");

  // op 3978: put
  db->Put(write_options, "key9518", "value50462");

  // op 3979: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 3980: get
  status = db->Get(read_options, "key5562", &value);
  if (status.ok()) {
    out << "GET key5562 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3981: delete_range
  status = db->DeleteRange(write_options, "key3902", "key5492");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 3982: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3661", "key10530", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3661, key10530) size=" << scan_result.size() << std::endl;
  }

  // op 3983: get
  status = db->Get(read_options, "key11302", &value);
  if (status.ok()) {
    out << "GET key11302 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3984: put
  db->Put(write_options, "key6489", "value8916");

  // op 3985: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1577", "key8964", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1577, key8964) size=" << scan_result.size() << std::endl;
  }

  // op 3986: put
  db->Put(write_options, "key9436", "value51626");

  // op 3987: put
  db->Put(write_options, "key6581", "value37350");

  // op 3988: delete
  db->Delete(write_options, "key1819");

  // op 3989: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6896", "key8735", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6896, key8735) size=" << scan_result.size() << std::endl;
  }

  // op 3990: put
  db->Put(write_options, "key6242", "value2564");

  // op 3991: put
  db->Put(write_options, "key1816", "value82545");

  // op 3992: get
  status = db->Get(read_options, "key7298", &value);
  if (status.ok()) {
    out << "GET key7298 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3993: put
  db->Put(write_options, "key3268", "value84638");

  // op 3994: put
  db->Put(write_options, "key3508", "value34808");

  // op 3995: put
  db->Put(write_options, "key5557", "value71703");

  // op 3996: get
  status = db->Get(read_options, "key6482", &value);
  if (status.ok()) {
    out << "GET key6482 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3997: delete_range
  status = db->DeleteRange(write_options, "key8649", "key9045");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 3998: get
  status = db->Get(read_options, "key0786", &value);
  if (status.ok()) {
    out << "GET key0786 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 3999: delete_range
  status = db->DeleteRange(write_options, "key4253", "key11079");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 4000: put
  db->Put(write_options, "key11706", "value85419");

  // op 4001: get
  status = db->Get(read_options, "key11692", &value);
  if (status.ok()) {
    out << "GET key11692 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4002: delete
  db->Delete(write_options, "key10154");

  // op 4003: put
  db->Put(write_options, "key4386", "value62645");

  // op 4004: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1516", "key2701", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1516, key2701) size=" << scan_result.size() << std::endl;
  }

  // op 4005: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3348", "key4881", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3348, key4881) size=" << scan_result.size() << std::endl;
  }

  // op 4006: put
  db->Put(write_options, "key4790", "value116446");

  // op 4007: put
  db->Put(write_options, "key7408", "value3810");

  // op 4008: put
  db->Put(write_options, "key8176", "value79187");

  // op 4009: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5847", "key6902", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5847, key6902) size=" << scan_result.size() << std::endl;
  }

  // op 4010: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1781", "key2222", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1781, key2222) size=" << scan_result.size() << std::endl;
  }

  // op 4011: put
  db->Put(write_options, "key4351", "value78746");

  // op 4012: get
  status = db->Get(read_options, "key8114", &value);
  if (status.ok()) {
    out << "GET key8114 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4013: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 4014: put
  db->Put(write_options, "key0361", "value24481");

  // op 4015: delete
  db->Delete(write_options, "key1873");

  // op 4016: put
  db->Put(write_options, "key6083", "value110617");

  // op 4017: put
  db->Put(write_options, "key2627", "value61027");

  // op 4018: get
  status = db->Get(read_options, "key7082", &value);
  if (status.ok()) {
    out << "GET key7082 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4019: put
  db->Put(write_options, "key2301", "value46964");

  // op 4020: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2037", "key5421", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2037, key5421) size=" << scan_result.size() << std::endl;
  }

  // op 4021: get
  status = db->Get(read_options, "key9769", &value);
  if (status.ok()) {
    out << "GET key9769 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4022: put
  db->Put(write_options, "key6617", "value72097");

  // op 4023: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 4024: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1346", "key2165", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1346, key2165) size=" << scan_result.size() << std::endl;
  }

  // op 4025: put
  db->Put(write_options, "key10085", "value72610");

  // op 4026: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 4027: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3217", "key9328", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3217, key9328) size=" << scan_result.size() << std::endl;
  }

  // op 4028: put
  db->Put(write_options, "key9098", "value81630");

  // op 4029: put
  db->Put(write_options, "key3364", "value6399");

  // op 4030: delete_range
  status = db->DeleteRange(write_options, "key2054", "key4042");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 4031: delete_range
  status = db->DeleteRange(write_options, "key3196", "key8976");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 4032: get
  status = db->Get(read_options, "key4899", &value);
  if (status.ok()) {
    out << "GET key4899 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4033: put
  db->Put(write_options, "key7710", "value9018");

  // op 4034: delete_range
  status = db->DeleteRange(write_options, "key5004", "key10788");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 4035: get
  status = db->Get(read_options, "key11284", &value);
  if (status.ok()) {
    out << "GET key11284 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4036: put
  db->Put(write_options, "key11634", "value83554");

  // op 4037: get
  status = db->Get(read_options, "key10562", &value);
  if (status.ok()) {
    out << "GET key10562 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4038: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8877", "key9234", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8877, key9234) size=" << scan_result.size() << std::endl;
  }

  // op 4039: get
  status = db->Get(read_options, "key4152", &value);
  if (status.ok()) {
    out << "GET key4152 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4040: put
  db->Put(write_options, "key7977", "value13585");

  // op 4041: put
  db->Put(write_options, "key3687", "value113155");

  // op 4042: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2677", "key7838", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2677, key7838) size=" << scan_result.size() << std::endl;
  }

  // op 4043: delete
  db->Delete(write_options, "key11672");

  // op 4044: delete
  db->Delete(write_options, "key10444");

  // op 4045: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3735", "key3867", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3735, key3867) size=" << scan_result.size() << std::endl;
  }

  // op 4046: put
  db->Put(write_options, "key9202", "value114512");

  // op 4047: put
  db->Put(write_options, "key2713", "value66272");

  // op 4048: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3434", "key5012", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3434, key5012) size=" << scan_result.size() << std::endl;
  }

  // op 4049: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0700", "key3383", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0700, key3383) size=" << scan_result.size() << std::endl;
  }

  // op 4050: delete
  db->Delete(write_options, "key9464");

  // op 4051: put
  db->Put(write_options, "key2790", "value68199");

  // op 4052: get
  status = db->Get(read_options, "key8810", &value);
  if (status.ok()) {
    out << "GET key8810 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4053: put
  db->Put(write_options, "key6303", "value53984");

  // op 4054: delete
  db->Delete(write_options, "key7468");

  // op 4055: put
  db->Put(write_options, "key7657", "value100114");

  // op 4056: delete_range
  status = db->DeleteRange(write_options, "key5258", "key10486");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 4057: delete_range
  status = db->DeleteRange(write_options, "key0504", "key6556");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 4058: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4350", "key9897", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4350, key9897) size=" << scan_result.size() << std::endl;
  }

  // op 4059: put
  db->Put(write_options, "key9199", "value25691");

  // op 4060: get
  status = db->Get(read_options, "key9366", &value);
  if (status.ok()) {
    out << "GET key9366 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4061: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0861", "key7676", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0861, key7676) size=" << scan_result.size() << std::endl;
  }

  // op 4062: put
  db->Put(write_options, "key6194", "value56002");

  // op 4063: put
  db->Put(write_options, "key3017", "value29958");

  // op 4064: delete_range
  status = db->DeleteRange(write_options, "key2036", "key6375");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 4065: delete_range
  status = db->DeleteRange(write_options, "key5461", "key11369");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 4066: put
  db->Put(write_options, "key8088", "value76816");

  // op 4067: put
  db->Put(write_options, "key1133", "value66102");

  // op 4068: get
  status = db->Get(read_options, "key5847", &value);
  if (status.ok()) {
    out << "GET key5847 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4069: put
  db->Put(write_options, "key8776", "value42991");

  // op 4070: get
  status = db->Get(read_options, "key1629", &value);
  if (status.ok()) {
    out << "GET key1629 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4071: get
  status = db->Get(read_options, "key2047", &value);
  if (status.ok()) {
    out << "GET key2047 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4072: delete
  db->Delete(write_options, "key6512");

  // op 4073: put
  db->Put(write_options, "key5572", "value113626");

  // op 4074: put
  db->Put(write_options, "key5075", "value53525");

  // op 4075: put
  db->Put(write_options, "key10782", "value86530");

  // op 4076: put
  db->Put(write_options, "key2240", "value105633");

  // op 4077: get
  status = db->Get(read_options, "key7462", &value);
  if (status.ok()) {
    out << "GET key7462 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4078: delete_range
  status = db->DeleteRange(write_options, "key1458", "key2845");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 4079: delete_range
  status = db->DeleteRange(write_options, "key2890", "key9479");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 4080: delete_range
  status = db->DeleteRange(write_options, "key0021", "key11289");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 4081: get
  status = db->Get(read_options, "key3565", &value);
  if (status.ok()) {
    out << "GET key3565 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4082: put
  db->Put(write_options, "key9684", "value42625");

  // op 4083: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9753", "key10552", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9753, key10552) size=" << scan_result.size() << std::endl;
  }

  // op 4084: get
  status = db->Get(read_options, "key8956", &value);
  if (status.ok()) {
    out << "GET key8956 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4085: get
  status = db->Get(read_options, "key2311", &value);
  if (status.ok()) {
    out << "GET key2311 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4086: put
  db->Put(write_options, "key10735", "value72842");

  // op 4087: delete
  db->Delete(write_options, "key9849");

  // op 4088: put
  db->Put(write_options, "key10486", "value44882");

  // op 4089: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5659", "key8290", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5659, key8290) size=" << scan_result.size() << std::endl;
  }

  // op 4090: get
  status = db->Get(read_options, "key1790", &value);
  if (status.ok()) {
    out << "GET key1790 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4091: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 4092: put
  db->Put(write_options, "key1593", "value15163");

  // op 4093: put
  db->Put(write_options, "key1822", "value94566");

  // op 4094: put
  db->Put(write_options, "key8028", "value25357");

  // op 4095: put
  db->Put(write_options, "key5461", "value33253");

  // op 4096: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 4097: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7970", "key11398", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7970, key11398) size=" << scan_result.size() << std::endl;
  }

  // op 4098: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 4099: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2269", "key7351", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2269, key7351) size=" << scan_result.size() << std::endl;
  }

  // op 4100: get
  status = db->Get(read_options, "key1891", &value);
  if (status.ok()) {
    out << "GET key1891 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4101: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1585", "key5009", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1585, key5009) size=" << scan_result.size() << std::endl;
  }

  // op 4102: get
  status = db->Get(read_options, "key0266", &value);
  if (status.ok()) {
    out << "GET key0266 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4103: delete_range
  status = db->DeleteRange(write_options, "key2301", "key10163");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 4104: delete_range
  status = db->DeleteRange(write_options, "key2371", "key8693");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 4105: put
  db->Put(write_options, "key7817", "value42460");

  // op 4106: delete
  db->Delete(write_options, "key6117");

  // op 4107: put
  db->Put(write_options, "key5819", "value10136");

  // op 4108: get
  status = db->Get(read_options, "key1939", &value);
  if (status.ok()) {
    out << "GET key1939 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4109: put
  db->Put(write_options, "key7365", "value91342");

  // op 4110: put
  db->Put(write_options, "key9397", "value6731");

  // op 4111: put
  db->Put(write_options, "key6781", "value17855");

  // op 4112: put
  db->Put(write_options, "key4859", "value99567");

  // op 4113: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3070", "key9264", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3070, key9264) size=" << scan_result.size() << std::endl;
  }

  // op 4114: scan
  scan_result.clear();
  status = db->Scan(read_options, "key11193", "key11618", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key11193, key11618) size=" << scan_result.size() << std::endl;
  }

  // op 4115: get
  status = db->Get(read_options, "key7104", &value);
  if (status.ok()) {
    out << "GET key7104 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4116: put
  db->Put(write_options, "key3081", "value20942");

  // op 4117: delete_range
  status = db->DeleteRange(write_options, "key7190", "key10226");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 4118: put
  db->Put(write_options, "key6106", "value43681");

  // op 4119: delete
  db->Delete(write_options, "key2268");

  // op 4120: put
  db->Put(write_options, "key8307", "value31777");

  // op 4121: get
  status = db->Get(read_options, "key1911", &value);
  if (status.ok()) {
    out << "GET key1911 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4122: get
  status = db->Get(read_options, "key3444", &value);
  if (status.ok()) {
    out << "GET key3444 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4123: get
  status = db->Get(read_options, "key5147", &value);
  if (status.ok()) {
    out << "GET key5147 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4124: put
  db->Put(write_options, "key9860", "value17681");

  // op 4125: put
  db->Put(write_options, "key9261", "value31779");

  // op 4126: put
  db->Put(write_options, "key0165", "value88966");

  // op 4127: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7013", "key11137", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7013, key11137) size=" << scan_result.size() << std::endl;
  }

  // op 4128: put
  db->Put(write_options, "key6009", "value71329");

  // op 4129: put
  db->Put(write_options, "key7761", "value44696");

  // op 4130: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 4131: delete_range
  status = db->DeleteRange(write_options, "key0498", "key2259");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 4132: put
  db->Put(write_options, "key0205", "value102008");

  // op 4133: delete
  db->Delete(write_options, "key4079");

  // op 4134: delete_range
  status = db->DeleteRange(write_options, "key2772", "key10283");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 4135: get
  status = db->Get(read_options, "key3214", &value);
  if (status.ok()) {
    out << "GET key3214 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4136: get
  status = db->Get(read_options, "key7523", &value);
  if (status.ok()) {
    out << "GET key7523 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4137: delete
  db->Delete(write_options, "key8762");

  // op 4138: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 4139: delete_range
  status = db->DeleteRange(write_options, "key4126", "key5729");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 4140: get
  status = db->Get(read_options, "key8423", &value);
  if (status.ok()) {
    out << "GET key8423 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4141: put
  db->Put(write_options, "key0073", "value74520");

  // op 4142: put
  db->Put(write_options, "key7598", "value44953");

  // op 4143: delete_range
  status = db->DeleteRange(write_options, "key5819", "key11502");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 4144: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 4145: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3256", "key6873", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3256, key6873) size=" << scan_result.size() << std::endl;
  }

  // op 4146: put
  db->Put(write_options, "key2037", "value37144");

  // op 4147: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7486", "key8096", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7486, key8096) size=" << scan_result.size() << std::endl;
  }

  // op 4148: delete
  db->Delete(write_options, "key5348");

  // op 4149: put
  db->Put(write_options, "key4271", "value91771");

  // op 4150: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7552", "key7565", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7552, key7565) size=" << scan_result.size() << std::endl;
  }

  // op 4151: put
  db->Put(write_options, "key7917", "value27136");

  // op 4152: delete_range
  status = db->DeleteRange(write_options, "key5676", "key6590");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 4153: put
  db->Put(write_options, "key9826", "value23067");

  // op 4154: delete
  db->Delete(write_options, "key9381");

  // op 4155: delete_range
  status = db->DeleteRange(write_options, "key0928", "key5805");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 4156: put
  db->Put(write_options, "key8066", "value118141");

  // op 4157: delete
  db->Delete(write_options, "key5405");

  // op 4158: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2153", "key5926", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2153, key5926) size=" << scan_result.size() << std::endl;
  }

  // op 4159: put
  db->Put(write_options, "key4465", "value44227");

  // op 4160: delete_range
  status = db->DeleteRange(write_options, "key4016", "key4876");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 4161: delete
  db->Delete(write_options, "key2926");

  // op 4162: get
  status = db->Get(read_options, "key7552", &value);
  if (status.ok()) {
    out << "GET key7552 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4163: put
  db->Put(write_options, "key2330", "value8293");

  // op 4164: put
  db->Put(write_options, "key0756", "value24178");

  // op 4165: delete_range
  status = db->DeleteRange(write_options, "key6444", "key7487");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 4166: put
  db->Put(write_options, "key11440", "value12260");

  // op 4167: put
  db->Put(write_options, "key6869", "value42531");

  // op 4168: delete
  db->Delete(write_options, "key5420");

  // op 4169: put
  db->Put(write_options, "key0759", "value97445");

  // op 4170: put
  db->Put(write_options, "key6915", "value76953");

  // op 4171: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2086", "key2315", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2086, key2315) size=" << scan_result.size() << std::endl;
  }

  // op 4172: delete
  db->Delete(write_options, "key5777");

  // op 4173: put
  db->Put(write_options, "key11299", "value88532");

  // op 4174: get
  status = db->Get(read_options, "key10487", &value);
  if (status.ok()) {
    out << "GET key10487 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4175: put
  db->Put(write_options, "key3867", "value86348");

  // op 4176: get
  status = db->Get(read_options, "key9368", &value);
  if (status.ok()) {
    out << "GET key9368 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4177: get
  status = db->Get(read_options, "key7537", &value);
  if (status.ok()) {
    out << "GET key7537 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4178: get
  status = db->Get(read_options, "key8928", &value);
  if (status.ok()) {
    out << "GET key8928 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4179: delete
  db->Delete(write_options, "key1206");

  // op 4180: get
  status = db->Get(read_options, "key5613", &value);
  if (status.ok()) {
    out << "GET key5613 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4181: put
  db->Put(write_options, "key8593", "value81184");

  // op 4182: delete_range
  status = db->DeleteRange(write_options, "key6536", "key7945");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 4183: put
  db->Put(write_options, "key4189", "value50196");

  // op 4184: delete_range
  status = db->DeleteRange(write_options, "key3633", "key10589");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 4185: put
  db->Put(write_options, "key3727", "value40523");

  // op 4186: put
  db->Put(write_options, "key5811", "value46630");

  // op 4187: put
  db->Put(write_options, "key2365", "value108118");

  // op 4188: put
  db->Put(write_options, "key6687", "value94652");

  // op 4189: put
  db->Put(write_options, "key5972", "value19293");

  // op 4190: get
  status = db->Get(read_options, "key10781", &value);
  if (status.ok()) {
    out << "GET key10781 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4191: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 4192: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6221", "key11680", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6221, key11680) size=" << scan_result.size() << std::endl;
  }

  // op 4193: get
  status = db->Get(read_options, "key0731", &value);
  if (status.ok()) {
    out << "GET key0731 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4194: put
  db->Put(write_options, "key10118", "value105942");

  // op 4195: delete_range
  status = db->DeleteRange(write_options, "key0091", "key11246");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 4196: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7786", "key11697", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7786, key11697) size=" << scan_result.size() << std::endl;
  }

  // op 4197: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7947", "key10563", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7947, key10563) size=" << scan_result.size() << std::endl;
  }

  // op 4198: get
  status = db->Get(read_options, "key1973", &value);
  if (status.ok()) {
    out << "GET key1973 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4199: get
  status = db->Get(read_options, "key5335", &value);
  if (status.ok()) {
    out << "GET key5335 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4200: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4969", "key5950", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4969, key5950) size=" << scan_result.size() << std::endl;
  }

  // op 4201: delete
  db->Delete(write_options, "key9936");

  // op 4202: put
  db->Put(write_options, "key8023", "value115010");

  // op 4203: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2214", "key10938", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2214, key10938) size=" << scan_result.size() << std::endl;
  }

  // op 4204: put
  db->Put(write_options, "key7688", "value62191");

  // op 4205: put
  db->Put(write_options, "key9223", "value29605");

  // op 4206: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 4207: put
  db->Put(write_options, "key8865", "value79449");

  // op 4208: get
  status = db->Get(read_options, "key5206", &value);
  if (status.ok()) {
    out << "GET key5206 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4209: get
  status = db->Get(read_options, "key1670", &value);
  if (status.ok()) {
    out << "GET key1670 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4210: delete_range
  status = db->DeleteRange(write_options, "key6828", "key8465");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 4211: put
  db->Put(write_options, "key11318", "value58497");

  // op 4212: put
  db->Put(write_options, "key3924", "value73465");

  // op 4213: put
  db->Put(write_options, "key3275", "value28055");

  // op 4214: put
  db->Put(write_options, "key0489", "value92672");

  // op 4215: put
  db->Put(write_options, "key3663", "value51773");

  // op 4216: put
  db->Put(write_options, "key7628", "value60458");

  // op 4217: get
  status = db->Get(read_options, "key11565", &value);
  if (status.ok()) {
    out << "GET key11565 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4218: put
  db->Put(write_options, "key0314", "value71911");

  // op 4219: get
  status = db->Get(read_options, "key4638", &value);
  if (status.ok()) {
    out << "GET key4638 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4220: get
  status = db->Get(read_options, "key0540", &value);
  if (status.ok()) {
    out << "GET key0540 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4221: put
  db->Put(write_options, "key9691", "value79504");

  // op 4222: put
  db->Put(write_options, "key1570", "value3313");

  // op 4223: put
  db->Put(write_options, "key3478", "value24520");

  // op 4224: put
  db->Put(write_options, "key3493", "value94624");

  // op 4225: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3510", "key11040", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3510, key11040) size=" << scan_result.size() << std::endl;
  }

  // op 4226: delete_range
  status = db->DeleteRange(write_options, "key5818", "key11519");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 4227: put
  db->Put(write_options, "key8969", "value57422");

  // op 4228: put
  db->Put(write_options, "key9179", "value66071");

  // op 4229: get
  status = db->Get(read_options, "key4426", &value);
  if (status.ok()) {
    out << "GET key4426 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4230: put
  db->Put(write_options, "key4041", "value33412");

  // op 4231: delete_range
  status = db->DeleteRange(write_options, "key3922", "key9518");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 4232: delete
  db->Delete(write_options, "key0726");

  // op 4233: get
  status = db->Get(read_options, "key5042", &value);
  if (status.ok()) {
    out << "GET key5042 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4234: delete
  db->Delete(write_options, "key4093");

  // op 4235: put
  db->Put(write_options, "key0301", "value114823");

  // op 4236: put
  db->Put(write_options, "key9092", "value47403");

  // op 4237: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4168", "key6323", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4168, key6323) size=" << scan_result.size() << std::endl;
  }

  // op 4238: put
  db->Put(write_options, "key0768", "value47153");

  // op 4239: get
  status = db->Get(read_options, "key11583", &value);
  if (status.ok()) {
    out << "GET key11583 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4240: delete_range
  status = db->DeleteRange(write_options, "key6766", "key10722");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 4241: delete_range
  status = db->DeleteRange(write_options, "key3639", "key10062");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 4242: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8783", "key10248", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8783, key10248) size=" << scan_result.size() << std::endl;
  }

  // op 4243: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0354", "key9959", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0354, key9959) size=" << scan_result.size() << std::endl;
  }

  // op 4244: delete_range
  status = db->DeleteRange(write_options, "key7812", "key10155");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 4245: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8909", "key10627", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8909, key10627) size=" << scan_result.size() << std::endl;
  }

  // op 4246: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 4247: get
  status = db->Get(read_options, "key1650", &value);
  if (status.ok()) {
    out << "GET key1650 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4248: get
  status = db->Get(read_options, "key2368", &value);
  if (status.ok()) {
    out << "GET key2368 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4249: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1047", "key9400", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1047, key9400) size=" << scan_result.size() << std::endl;
  }

  // op 4250: delete
  db->Delete(write_options, "key1536");

  // op 4251: get
  status = db->Get(read_options, "key6676", &value);
  if (status.ok()) {
    out << "GET key6676 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4252: put
  db->Put(write_options, "key4494", "value102116");

  // op 4253: put
  db->Put(write_options, "key7625", "value18528");

  // op 4254: put
  db->Put(write_options, "key5065", "value90932");

  // op 4255: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4071", "key10987", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4071, key10987) size=" << scan_result.size() << std::endl;
  }

  // op 4256: put
  db->Put(write_options, "key1062", "value58066");

  // op 4257: put
  db->Put(write_options, "key11258", "value101365");

  // op 4258: get
  status = db->Get(read_options, "key4201", &value);
  if (status.ok()) {
    out << "GET key4201 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4259: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1486", "key4723", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1486, key4723) size=" << scan_result.size() << std::endl;
  }

  // op 4260: get
  status = db->Get(read_options, "key3235", &value);
  if (status.ok()) {
    out << "GET key3235 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4261: delete
  db->Delete(write_options, "key0922");

  // op 4262: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9086", "key11696", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9086, key11696) size=" << scan_result.size() << std::endl;
  }

  // op 4263: put
  db->Put(write_options, "key6723", "value52115");

  // op 4264: delete
  db->Delete(write_options, "key3430");

  // op 4265: delete_range
  status = db->DeleteRange(write_options, "key3842", "key10166");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 4266: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5928", "key6784", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5928, key6784) size=" << scan_result.size() << std::endl;
  }

  // op 4267: put
  db->Put(write_options, "key3018", "value87911");

  // op 4268: get
  status = db->Get(read_options, "key7004", &value);
  if (status.ok()) {
    out << "GET key7004 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4269: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7044", "key9842", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7044, key9842) size=" << scan_result.size() << std::endl;
  }

  // op 4270: get
  status = db->Get(read_options, "key9323", &value);
  if (status.ok()) {
    out << "GET key9323 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4271: get
  status = db->Get(read_options, "key0007", &value);
  if (status.ok()) {
    out << "GET key0007 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4272: delete
  db->Delete(write_options, "key10357");

  // op 4273: put
  db->Put(write_options, "key7598", "value67720");

  // op 4274: put
  db->Put(write_options, "key8227", "value23343");

  // op 4275: put
  db->Put(write_options, "key5881", "value100103");

  // op 4276: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7750", "key10695", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7750, key10695) size=" << scan_result.size() << std::endl;
  }

  // op 4277: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1471", "key9729", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1471, key9729) size=" << scan_result.size() << std::endl;
  }

  // op 4278: put
  db->Put(write_options, "key4640", "value34332");

  // op 4279: put
  db->Put(write_options, "key0444", "value44237");

  // op 4280: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1096", "key11235", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1096, key11235) size=" << scan_result.size() << std::endl;
  }

  // op 4281: put
  db->Put(write_options, "key9307", "value117069");

  // op 4282: put
  db->Put(write_options, "key10140", "value91821");

  // op 4283: get
  status = db->Get(read_options, "key6090", &value);
  if (status.ok()) {
    out << "GET key6090 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4284: put
  db->Put(write_options, "key10903", "value60585");

  // op 4285: put
  db->Put(write_options, "key1560", "value76413");

  // op 4286: put
  db->Put(write_options, "key2987", "value74640");

  // op 4287: put
  db->Put(write_options, "key4744", "value42779");

  // op 4288: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 4289: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1700", "key11441", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1700, key11441) size=" << scan_result.size() << std::endl;
  }

  // op 4290: put
  db->Put(write_options, "key5777", "value86700");

  // op 4291: put
  db->Put(write_options, "key6045", "value64019");

  // op 4292: put
  db->Put(write_options, "key11193", "value10394");

  // op 4293: put
  db->Put(write_options, "key10789", "value16461");

  // op 4294: get
  status = db->Get(read_options, "key11502", &value);
  if (status.ok()) {
    out << "GET key11502 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4295: put
  db->Put(write_options, "key7141", "value24898");

  // op 4296: put
  db->Put(write_options, "key7464", "value25392");

  // op 4297: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 4298: get
  status = db->Get(read_options, "key0516", &value);
  if (status.ok()) {
    out << "GET key0516 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4299: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9002", "key10162", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9002, key10162) size=" << scan_result.size() << std::endl;
  }

  // op 4300: put
  db->Put(write_options, "key4691", "value6367");

  // op 4301: delete
  db->Delete(write_options, "key5879");

  // op 4302: get
  status = db->Get(read_options, "key7222", &value);
  if (status.ok()) {
    out << "GET key7222 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4303: delete
  db->Delete(write_options, "key10485");

  // op 4304: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 4305: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 4306: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 4307: delete
  db->Delete(write_options, "key10952");

  // op 4308: put
  db->Put(write_options, "key3190", "value106249");

  // op 4309: delete_range
  status = db->DeleteRange(write_options, "key5550", "key11613");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 4310: delete
  db->Delete(write_options, "key1273");

  // op 4311: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 4312: get
  status = db->Get(read_options, "key8076", &value);
  if (status.ok()) {
    out << "GET key8076 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4313: put
  db->Put(write_options, "key4430", "value78485");

  // op 4314: put
  db->Put(write_options, "key4716", "value81185");

  // op 4315: get
  status = db->Get(read_options, "key11021", &value);
  if (status.ok()) {
    out << "GET key11021 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4316: put
  db->Put(write_options, "key2010", "value99349");

  // op 4317: put
  db->Put(write_options, "key5350", "value61829");

  // op 4318: get
  status = db->Get(read_options, "key7363", &value);
  if (status.ok()) {
    out << "GET key7363 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4319: get
  status = db->Get(read_options, "key3375", &value);
  if (status.ok()) {
    out << "GET key3375 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4320: get
  status = db->Get(read_options, "key0884", &value);
  if (status.ok()) {
    out << "GET key0884 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4321: put
  db->Put(write_options, "key6604", "value115007");

  // op 4322: get
  status = db->Get(read_options, "key4123", &value);
  if (status.ok()) {
    out << "GET key4123 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4323: delete_range
  status = db->DeleteRange(write_options, "key1833", "key3217");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 4324: get
  status = db->Get(read_options, "key4157", &value);
  if (status.ok()) {
    out << "GET key4157 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4325: delete_range
  status = db->DeleteRange(write_options, "key0969", "key8108");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 4326: delete
  db->Delete(write_options, "key11202");

  // op 4327: put
  db->Put(write_options, "key7933", "value6591");

  // op 4328: put
  db->Put(write_options, "key7845", "value12518");

  // op 4329: put
  db->Put(write_options, "key1883", "value70536");

  // op 4330: put
  db->Put(write_options, "key7578", "value113882");

  // op 4331: put
  db->Put(write_options, "key9392", "value32546");

  // op 4332: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6029", "key7446", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6029, key7446) size=" << scan_result.size() << std::endl;
  }

  // op 4333: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2091", "key2327", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2091, key2327) size=" << scan_result.size() << std::endl;
  }

  // op 4334: get
  status = db->Get(read_options, "key1212", &value);
  if (status.ok()) {
    out << "GET key1212 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4335: put
  db->Put(write_options, "key4387", "value75371");

  // op 4336: put
  db->Put(write_options, "key1212", "value80937");

  // op 4337: put
  db->Put(write_options, "key10263", "value94118");

  // op 4338: put
  db->Put(write_options, "key2715", "value0716");

  // op 4339: get
  status = db->Get(read_options, "key5067", &value);
  if (status.ok()) {
    out << "GET key5067 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4340: put
  db->Put(write_options, "key3091", "value74252");

  // op 4341: get
  status = db->Get(read_options, "key0709", &value);
  if (status.ok()) {
    out << "GET key0709 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4342: get
  status = db->Get(read_options, "key6395", &value);
  if (status.ok()) {
    out << "GET key6395 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4343: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6215", "key9771", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6215, key9771) size=" << scan_result.size() << std::endl;
  }

  // op 4344: put
  db->Put(write_options, "key1276", "value49614");

  // op 4345: put
  db->Put(write_options, "key11122", "value83336");

  // op 4346: get
  status = db->Get(read_options, "key6392", &value);
  if (status.ok()) {
    out << "GET key6392 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4347: put
  db->Put(write_options, "key1333", "value22731");

  // op 4348: delete
  db->Delete(write_options, "key7040");

  // op 4349: put
  db->Put(write_options, "key0732", "value1581");

  // op 4350: put
  db->Put(write_options, "key2627", "value59616");

  // op 4351: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6049", "key8891", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6049, key8891) size=" << scan_result.size() << std::endl;
  }

  // op 4352: delete_range
  status = db->DeleteRange(write_options, "key5319", "key10038");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 4353: put
  db->Put(write_options, "key3922", "value33958");

  // op 4354: put
  db->Put(write_options, "key8294", "value77762");

  // op 4355: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3791", "key11186", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3791, key11186) size=" << scan_result.size() << std::endl;
  }

  // op 4356: put
  db->Put(write_options, "key8562", "value5502");

  // op 4357: delete
  db->Delete(write_options, "key7419");

  // op 4358: delete_range
  status = db->DeleteRange(write_options, "key3386", "key10149");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 4359: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 4360: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 4361: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1023", "key1682", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1023, key1682) size=" << scan_result.size() << std::endl;
  }

  // op 4362: put
  db->Put(write_options, "key0734", "value13919");

  // op 4363: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 4364: delete
  db->Delete(write_options, "key6376");

  // op 4365: get
  status = db->Get(read_options, "key9565", &value);
  if (status.ok()) {
    out << "GET key9565 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4366: put
  db->Put(write_options, "key1591", "value45724");

  // op 4367: put
  db->Put(write_options, "key0423", "value112589");

  // op 4368: put
  db->Put(write_options, "key8673", "value34565");

  // op 4369: put
  db->Put(write_options, "key5026", "value89586");

  // op 4370: put
  db->Put(write_options, "key6439", "value6107");

  // op 4371: put
  db->Put(write_options, "key9066", "value99590");

  // op 4372: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6042", "key9632", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6042, key9632) size=" << scan_result.size() << std::endl;
  }

  // op 4373: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4013", "key9041", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4013, key9041) size=" << scan_result.size() << std::endl;
  }

  // op 4374: get
  status = db->Get(read_options, "key3390", &value);
  if (status.ok()) {
    out << "GET key3390 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4375: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3789", "key9336", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3789, key9336) size=" << scan_result.size() << std::endl;
  }

  // op 4376: get
  status = db->Get(read_options, "key6417", &value);
  if (status.ok()) {
    out << "GET key6417 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4377: get
  status = db->Get(read_options, "key8268", &value);
  if (status.ok()) {
    out << "GET key8268 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4378: put
  db->Put(write_options, "key9909", "value53354");

  // op 4379: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4430", "key10359", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4430, key10359) size=" << scan_result.size() << std::endl;
  }

  // op 4380: delete_range
  status = db->DeleteRange(write_options, "key2136", "key3020");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 4381: put
  db->Put(write_options, "key2448", "value47528");

  // op 4382: delete_range
  status = db->DeleteRange(write_options, "key6933", "key7653");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 4383: delete_range
  status = db->DeleteRange(write_options, "key3600", "key6515");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 4384: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7298", "key10870", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7298, key10870) size=" << scan_result.size() << std::endl;
  }

  // op 4385: put
  db->Put(write_options, "key5257", "value69982");

  // op 4386: delete
  db->Delete(write_options, "key4483");

  // op 4387: delete_range
  status = db->DeleteRange(write_options, "key2572", "key7124");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 4388: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 4389: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 4390: put
  db->Put(write_options, "key2515", "value13408");

  // op 4391: delete
  db->Delete(write_options, "key1929");

  // op 4392: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6604", "key11664", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6604, key11664) size=" << scan_result.size() << std::endl;
  }

  // op 4393: get
  status = db->Get(read_options, "key9906", &value);
  if (status.ok()) {
    out << "GET key9906 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4394: delete
  db->Delete(write_options, "key7010");

  // op 4395: delete_range
  status = db->DeleteRange(write_options, "key8097", "key10738");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 4396: put
  db->Put(write_options, "key7094", "value83178");

  // op 4397: get
  status = db->Get(read_options, "key3020", &value);
  if (status.ok()) {
    out << "GET key3020 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4398: put
  db->Put(write_options, "key10493", "value98053");

  // op 4399: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3458", "key10745", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3458, key10745) size=" << scan_result.size() << std::endl;
  }

  // op 4400: put
  db->Put(write_options, "key3619", "value95213");

  // op 4401: delete_range
  status = db->DeleteRange(write_options, "key2949", "key4065");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 4402: put
  db->Put(write_options, "key11668", "value74889");

  // op 4403: put
  db->Put(write_options, "key1933", "value33257");

  // op 4404: delete_range
  status = db->DeleteRange(write_options, "key3448", "key5038");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 4405: get
  status = db->Get(read_options, "key1862", &value);
  if (status.ok()) {
    out << "GET key1862 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4406: delete_range
  status = db->DeleteRange(write_options, "key5690", "key6778");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 4407: put
  db->Put(write_options, "key2614", "value18524");

  // op 4408: put
  db->Put(write_options, "key6757", "value87205");

  // op 4409: delete_range
  status = db->DeleteRange(write_options, "key5122", "key6506");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 4410: get
  status = db->Get(read_options, "key11130", &value);
  if (status.ok()) {
    out << "GET key11130 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4411: get
  status = db->Get(read_options, "key8865", &value);
  if (status.ok()) {
    out << "GET key8865 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4412: put
  db->Put(write_options, "key1202", "value17156");

  // op 4413: get
  status = db->Get(read_options, "key7938", &value);
  if (status.ok()) {
    out << "GET key7938 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4414: delete
  db->Delete(write_options, "key9146");

  // op 4415: put
  db->Put(write_options, "key8902", "value81959");

  // op 4416: put
  db->Put(write_options, "key5221", "value70224");

  // op 4417: delete
  db->Delete(write_options, "key10787");

  // op 4418: put
  db->Put(write_options, "key6827", "value95019");

  // op 4419: get
  status = db->Get(read_options, "key0517", &value);
  if (status.ok()) {
    out << "GET key0517 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4420: get
  status = db->Get(read_options, "key7129", &value);
  if (status.ok()) {
    out << "GET key7129 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4421: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 4422: get
  status = db->Get(read_options, "key2536", &value);
  if (status.ok()) {
    out << "GET key2536 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4423: put
  db->Put(write_options, "key3416", "value45323");

  // op 4424: put
  db->Put(write_options, "key5923", "value53769");

  // op 4425: put
  db->Put(write_options, "key3359", "value69933");

  // op 4426: put
  db->Put(write_options, "key6135", "value11268");

  // op 4427: get
  status = db->Get(read_options, "key3533", &value);
  if (status.ok()) {
    out << "GET key3533 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4428: put
  db->Put(write_options, "key4116", "value53222");

  // op 4429: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 4430: get
  status = db->Get(read_options, "key2637", &value);
  if (status.ok()) {
    out << "GET key2637 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4431: put
  db->Put(write_options, "key0306", "value24062");

  // op 4432: put
  db->Put(write_options, "key6268", "value1982");

  // op 4433: put
  db->Put(write_options, "key8559", "value64563");

  // op 4434: put
  db->Put(write_options, "key11176", "value23620");

  // op 4435: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2031", "key10797", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2031, key10797) size=" << scan_result.size() << std::endl;
  }

  // op 4436: put
  db->Put(write_options, "key4408", "value39690");

  // op 4437: put
  db->Put(write_options, "key9531", "value32410");

  // op 4438: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1801", "key6952", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1801, key6952) size=" << scan_result.size() << std::endl;
  }

  // op 4439: put
  db->Put(write_options, "key10813", "value113771");

  // op 4440: put
  db->Put(write_options, "key10179", "value88467");

  // op 4441: get
  status = db->Get(read_options, "key5205", &value);
  if (status.ok()) {
    out << "GET key5205 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4442: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2242", "key9748", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2242, key9748) size=" << scan_result.size() << std::endl;
  }

  // op 4443: put
  db->Put(write_options, "key3691", "value3021");

  // op 4444: put
  db->Put(write_options, "key6415", "value91954");

  // op 4445: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 4446: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 4447: delete
  db->Delete(write_options, "key4198");

  // op 4448: get
  status = db->Get(read_options, "key7794", &value);
  if (status.ok()) {
    out << "GET key7794 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4449: put
  db->Put(write_options, "key4220", "value106864");

  // op 4450: delete_range
  status = db->DeleteRange(write_options, "key4591", "key8111");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 4451: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 4452: delete_range
  status = db->DeleteRange(write_options, "key3669", "key6069");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 4453: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9156", "key9602", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9156, key9602) size=" << scan_result.size() << std::endl;
  }

  // op 4454: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7305", "key8817", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7305, key8817) size=" << scan_result.size() << std::endl;
  }

  // op 4455: get
  status = db->Get(read_options, "key7485", &value);
  if (status.ok()) {
    out << "GET key7485 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4456: delete_range
  status = db->DeleteRange(write_options, "key2097", "key4691");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 4457: delete
  db->Delete(write_options, "key1347");

  // op 4458: put
  db->Put(write_options, "key5714", "value65311");

  // op 4459: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 4460: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 4461: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5694", "key7971", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5694, key7971) size=" << scan_result.size() << std::endl;
  }

  // op 4462: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4790", "key5041", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4790, key5041) size=" << scan_result.size() << std::endl;
  }

  // op 4463: put
  db->Put(write_options, "key3079", "value63807");

  // op 4464: delete
  db->Delete(write_options, "key8894");

  // op 4465: put
  db->Put(write_options, "key8724", "value116124");

  // op 4466: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 4467: delete
  db->Delete(write_options, "key4247");

  // op 4468: put
  db->Put(write_options, "key9845", "value108985");

  // op 4469: put
  db->Put(write_options, "key4897", "value36171");

  // op 4470: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4857", "key9700", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4857, key9700) size=" << scan_result.size() << std::endl;
  }

  // op 4471: delete
  db->Delete(write_options, "key6569");

  // op 4472: put
  db->Put(write_options, "key4250", "value72975");

  // op 4473: put
  db->Put(write_options, "key10531", "value55382");

  // op 4474: get
  status = db->Get(read_options, "key6016", &value);
  if (status.ok()) {
    out << "GET key6016 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4475: put
  db->Put(write_options, "key11672", "value2660");

  // op 4476: put
  db->Put(write_options, "key7944", "value101035");

  // op 4477: get
  status = db->Get(read_options, "key1842", &value);
  if (status.ok()) {
    out << "GET key1842 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4478: put
  db->Put(write_options, "key5178", "value66141");

  // op 4479: delete
  db->Delete(write_options, "key2409");

  // op 4480: put
  db->Put(write_options, "key8217", "value46112");

  // op 4481: put
  db->Put(write_options, "key8929", "value71059");

  // op 4482: delete
  db->Delete(write_options, "key6519");

  // op 4483: put
  db->Put(write_options, "key3621", "value86588");

  // op 4484: put
  db->Put(write_options, "key1601", "value117183");

  // op 4485: delete_range
  status = db->DeleteRange(write_options, "key2004", "key10222");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 4486: put
  db->Put(write_options, "key0495", "value67457");

  // op 4487: delete
  db->Delete(write_options, "key11160");

  // op 4488: get
  status = db->Get(read_options, "key10700", &value);
  if (status.ok()) {
    out << "GET key10700 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4489: get
  status = db->Get(read_options, "key8545", &value);
  if (status.ok()) {
    out << "GET key8545 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4490: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1535", "key3664", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1535, key3664) size=" << scan_result.size() << std::endl;
  }

  // op 4491: delete
  db->Delete(write_options, "key0614");

  // op 4492: delete_range
  status = db->DeleteRange(write_options, "key5689", "key8775");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 4493: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2336", "key4285", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2336, key4285) size=" << scan_result.size() << std::endl;
  }

  // op 4494: get
  status = db->Get(read_options, "key8722", &value);
  if (status.ok()) {
    out << "GET key8722 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4495: delete
  db->Delete(write_options, "key5243");

  // op 4496: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4774", "key6217", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4774, key6217) size=" << scan_result.size() << std::endl;
  }

  // op 4497: get
  status = db->Get(read_options, "key4131", &value);
  if (status.ok()) {
    out << "GET key4131 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4498: put
  db->Put(write_options, "key5215", "value57636");

  // op 4499: delete
  db->Delete(write_options, "key0418");

  // op 4500: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6942", "key9239", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6942, key9239) size=" << scan_result.size() << std::endl;
  }

  // op 4501: put
  db->Put(write_options, "key0908", "value11790");

  // op 4502: put
  db->Put(write_options, "key4414", "value38094");

  // op 4503: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 4504: delete
  db->Delete(write_options, "key10673");

  // op 4505: put
  db->Put(write_options, "key8986", "value12528");

  // op 4506: delete
  db->Delete(write_options, "key3682");

  // op 4507: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6706", "key11301", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6706, key11301) size=" << scan_result.size() << std::endl;
  }

  // op 4508: delete
  db->Delete(write_options, "key2586");

  // op 4509: get
  status = db->Get(read_options, "key3358", &value);
  if (status.ok()) {
    out << "GET key3358 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4510: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7320", "key9922", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7320, key9922) size=" << scan_result.size() << std::endl;
  }

  // op 4511: delete
  db->Delete(write_options, "key9364");

  // op 4512: get
  status = db->Get(read_options, "key6384", &value);
  if (status.ok()) {
    out << "GET key6384 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4513: put
  db->Put(write_options, "key3422", "value102526");

  // op 4514: put
  db->Put(write_options, "key5722", "value83708");

  // op 4515: get
  status = db->Get(read_options, "key0693", &value);
  if (status.ok()) {
    out << "GET key0693 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4516: put
  db->Put(write_options, "key0703", "value18584");

  // op 4517: get
  status = db->Get(read_options, "key10843", &value);
  if (status.ok()) {
    out << "GET key10843 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4518: delete_range
  status = db->DeleteRange(write_options, "key3981", "key8789");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 4519: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5534", "key10427", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5534, key10427) size=" << scan_result.size() << std::endl;
  }

  // op 4520: put
  db->Put(write_options, "key5999", "value42533");

  // op 4521: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1682", "key5096", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1682, key5096) size=" << scan_result.size() << std::endl;
  }

  // op 4522: put
  db->Put(write_options, "key9115", "value30832");

  // op 4523: delete_range
  status = db->DeleteRange(write_options, "key2783", "key3385");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 4524: put
  db->Put(write_options, "key8557", "value51539");

  // op 4525: get
  status = db->Get(read_options, "key5380", &value);
  if (status.ok()) {
    out << "GET key5380 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4526: put
  db->Put(write_options, "key3622", "value6769");

  // op 4527: put
  db->Put(write_options, "key7718", "value39056");

  // op 4528: delete
  db->Delete(write_options, "key0855");

  // op 4529: delete
  db->Delete(write_options, "key7388");

  // op 4530: put
  db->Put(write_options, "key4965", "value44424");

  // op 4531: delete
  db->Delete(write_options, "key2169");

  // op 4532: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5247", "key11195", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5247, key11195) size=" << scan_result.size() << std::endl;
  }

  // op 4533: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1802", "key11096", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1802, key11096) size=" << scan_result.size() << std::endl;
  }

  // op 4534: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8133", "key11613", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8133, key11613) size=" << scan_result.size() << std::endl;
  }

  // op 4535: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8105", "key9852", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8105, key9852) size=" << scan_result.size() << std::endl;
  }

  // op 4536: get
  status = db->Get(read_options, "key3084", &value);
  if (status.ok()) {
    out << "GET key3084 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4537: get
  status = db->Get(read_options, "key2372", &value);
  if (status.ok()) {
    out << "GET key2372 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4538: get
  status = db->Get(read_options, "key4742", &value);
  if (status.ok()) {
    out << "GET key4742 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4539: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0503", "key1343", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0503, key1343) size=" << scan_result.size() << std::endl;
  }

  // op 4540: put
  db->Put(write_options, "key8937", "value54995");

  // op 4541: get
  status = db->Get(read_options, "key6486", &value);
  if (status.ok()) {
    out << "GET key6486 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4542: delete
  db->Delete(write_options, "key3755");

  // op 4543: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0549", "key3177", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0549, key3177) size=" << scan_result.size() << std::endl;
  }

  // op 4544: delete
  db->Delete(write_options, "key11399");

  // op 4545: delete_range
  status = db->DeleteRange(write_options, "key5547", "key11057");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 4546: put
  db->Put(write_options, "key8662", "value64933");

  // op 4547: delete_range
  status = db->DeleteRange(write_options, "key3119", "key5670");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 4548: get
  status = db->Get(read_options, "key2856", &value);
  if (status.ok()) {
    out << "GET key2856 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4549: get
  status = db->Get(read_options, "key6287", &value);
  if (status.ok()) {
    out << "GET key6287 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4550: delete_range
  status = db->DeleteRange(write_options, "key3615", "key4125");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 4551: put
  db->Put(write_options, "key6526", "value2028");

  // op 4552: get
  status = db->Get(read_options, "key3000", &value);
  if (status.ok()) {
    out << "GET key3000 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4553: delete_range
  status = db->DeleteRange(write_options, "key3629", "key5398");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 4554: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2158", "key5742", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2158, key5742) size=" << scan_result.size() << std::endl;
  }

  // op 4555: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6858", "key10627", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6858, key10627) size=" << scan_result.size() << std::endl;
  }

  // op 4556: put
  db->Put(write_options, "key3344", "value45589");

  // op 4557: put
  db->Put(write_options, "key3944", "value87612");

  // op 4558: get
  status = db->Get(read_options, "key0092", &value);
  if (status.ok()) {
    out << "GET key0092 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4559: put
  db->Put(write_options, "key11039", "value17405");

  // op 4560: delete_range
  status = db->DeleteRange(write_options, "key7811", "key7953");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 4561: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3208", "key9690", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3208, key9690) size=" << scan_result.size() << std::endl;
  }

  // op 4562: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 4563: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5956", "key11493", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5956, key11493) size=" << scan_result.size() << std::endl;
  }

  // op 4564: delete
  db->Delete(write_options, "key11644");

  // op 4565: put
  db->Put(write_options, "key3125", "value1385");

  // op 4566: get
  status = db->Get(read_options, "key7729", &value);
  if (status.ok()) {
    out << "GET key7729 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4567: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7717", "key9190", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7717, key9190) size=" << scan_result.size() << std::endl;
  }

  // op 4568: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4944", "key7194", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4944, key7194) size=" << scan_result.size() << std::endl;
  }

  // op 4569: get
  status = db->Get(read_options, "key6701", &value);
  if (status.ok()) {
    out << "GET key6701 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4570: delete_range
  status = db->DeleteRange(write_options, "key5917", "key10991");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 4571: put
  db->Put(write_options, "key0466", "value70153");

  // op 4572: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3384", "key5389", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3384, key5389) size=" << scan_result.size() << std::endl;
  }

  // op 4573: put
  db->Put(write_options, "key11540", "value48452");

  // op 4574: put
  db->Put(write_options, "key9694", "value77135");

  // op 4575: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 4576: delete_range
  status = db->DeleteRange(write_options, "key1347", "key2277");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 4577: get
  status = db->Get(read_options, "key3768", &value);
  if (status.ok()) {
    out << "GET key3768 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4578: put
  db->Put(write_options, "key6894", "value111212");

  // op 4579: delete
  db->Delete(write_options, "key1353");

  // op 4580: get
  status = db->Get(read_options, "key2315", &value);
  if (status.ok()) {
    out << "GET key2315 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4581: get
  status = db->Get(read_options, "key5285", &value);
  if (status.ok()) {
    out << "GET key5285 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4582: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 4583: delete_range
  status = db->DeleteRange(write_options, "key1515", "key11649");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 4584: delete_range
  status = db->DeleteRange(write_options, "key4530", "key10973");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 4585: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0025", "key1133", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0025, key1133) size=" << scan_result.size() << std::endl;
  }

  // op 4586: get
  status = db->Get(read_options, "key6998", &value);
  if (status.ok()) {
    out << "GET key6998 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4587: put
  db->Put(write_options, "key9878", "value96606");

  // op 4588: get
  status = db->Get(read_options, "key1488", &value);
  if (status.ok()) {
    out << "GET key1488 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4589: delete
  db->Delete(write_options, "key0701");

  // op 4590: delete
  db->Delete(write_options, "key6384");

  // op 4591: put
  db->Put(write_options, "key2060", "value31659");

  // op 4592: put
  db->Put(write_options, "key6005", "value2082");

  // op 4593: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 4594: get
  status = db->Get(read_options, "key5314", &value);
  if (status.ok()) {
    out << "GET key5314 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4595: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1134", "key1660", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1134, key1660) size=" << scan_result.size() << std::endl;
  }

  // op 4596: get
  status = db->Get(read_options, "key9089", &value);
  if (status.ok()) {
    out << "GET key9089 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4597: delete
  db->Delete(write_options, "key9182");

  // op 4598: get
  status = db->Get(read_options, "key11364", &value);
  if (status.ok()) {
    out << "GET key11364 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4599: put
  db->Put(write_options, "key2924", "value112992");

  // op 4600: delete_range
  status = db->DeleteRange(write_options, "key1599", "key7296");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 4601: get
  status = db->Get(read_options, "key9781", &value);
  if (status.ok()) {
    out << "GET key9781 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4602: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 4603: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2576", "key10584", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2576, key10584) size=" << scan_result.size() << std::endl;
  }

  // op 4604: delete_range
  status = db->DeleteRange(write_options, "key0593", "key1971");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 4605: put
  db->Put(write_options, "key0830", "value42697");

  // op 4606: put
  db->Put(write_options, "key10991", "value95042");

  // op 4607: put
  db->Put(write_options, "key1064", "value117809");

  // op 4608: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 4609: put
  db->Put(write_options, "key8483", "value26703");

  // op 4610: put
  db->Put(write_options, "key11037", "value114729");

  // op 4611: put
  db->Put(write_options, "key3620", "value3734");

  // op 4612: get
  status = db->Get(read_options, "key1682", &value);
  if (status.ok()) {
    out << "GET key1682 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4613: put
  db->Put(write_options, "key10994", "value40098");

  // op 4614: delete
  db->Delete(write_options, "key6373");

  // op 4615: put
  db->Put(write_options, "key3407", "value32290");

  // op 4616: put
  db->Put(write_options, "key4936", "value11973");

  // op 4617: delete_range
  status = db->DeleteRange(write_options, "key3756", "key10687");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 4618: put
  db->Put(write_options, "key3863", "value83164");

  // op 4619: put
  db->Put(write_options, "key8368", "value62039");

  // op 4620: put
  db->Put(write_options, "key10830", "value49196");

  // op 4621: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 4622: put
  db->Put(write_options, "key5360", "value32823");

  // op 4623: delete_range
  status = db->DeleteRange(write_options, "key3652", "key4777");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 4624: put
  db->Put(write_options, "key1147", "value60922");

  // op 4625: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7541", "key11521", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7541, key11521) size=" << scan_result.size() << std::endl;
  }

  // op 4626: get
  status = db->Get(read_options, "key1475", &value);
  if (status.ok()) {
    out << "GET key1475 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4627: put
  db->Put(write_options, "key5543", "value87336");

  // op 4628: delete_range
  status = db->DeleteRange(write_options, "key7220", "key10988");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 4629: put
  db->Put(write_options, "key6336", "value41123");

  // op 4630: put
  db->Put(write_options, "key3301", "value51951");

  // op 4631: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4546", "key6404", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4546, key6404) size=" << scan_result.size() << std::endl;
  }

  // op 4632: get
  status = db->Get(read_options, "key1615", &value);
  if (status.ok()) {
    out << "GET key1615 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4633: put
  db->Put(write_options, "key8047", "value71798");

  // op 4634: put
  db->Put(write_options, "key8687", "value64101");

  // op 4635: delete
  db->Delete(write_options, "key3440");

  // op 4636: put
  db->Put(write_options, "key0209", "value64551");

  // op 4637: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5465", "key6623", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5465, key6623) size=" << scan_result.size() << std::endl;
  }

  // op 4638: put
  db->Put(write_options, "key8029", "value70841");

  // op 4639: delete_range
  status = db->DeleteRange(write_options, "key3662", "key6896");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 4640: get
  status = db->Get(read_options, "key3747", &value);
  if (status.ok()) {
    out << "GET key3747 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4641: delete_range
  status = db->DeleteRange(write_options, "key0200", "key2666");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 4642: get
  status = db->Get(read_options, "key5457", &value);
  if (status.ok()) {
    out << "GET key5457 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4643: delete_range
  status = db->DeleteRange(write_options, "key8020", "key8679");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 4644: get
  status = db->Get(read_options, "key5979", &value);
  if (status.ok()) {
    out << "GET key5979 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4645: delete_range
  status = db->DeleteRange(write_options, "key1822", "key6880");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 4646: delete_range
  status = db->DeleteRange(write_options, "key8641", "key11294");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 4647: put
  db->Put(write_options, "key1575", "value107852");

  // op 4648: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10186", "key10694", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10186, key10694) size=" << scan_result.size() << std::endl;
  }

  // op 4649: put
  db->Put(write_options, "key2965", "value11652");

  // op 4650: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0312", "key3562", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0312, key3562) size=" << scan_result.size() << std::endl;
  }

  // op 4651: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7419", "key7982", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7419, key7982) size=" << scan_result.size() << std::endl;
  }

  // op 4652: put
  db->Put(write_options, "key5076", "value62457");

  // op 4653: put
  db->Put(write_options, "key7305", "value77680");

  // op 4654: get
  status = db->Get(read_options, "key0487", &value);
  if (status.ok()) {
    out << "GET key0487 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4655: get
  status = db->Get(read_options, "key11483", &value);
  if (status.ok()) {
    out << "GET key11483 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4656: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0977", "key7469", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0977, key7469) size=" << scan_result.size() << std::endl;
  }

  // op 4657: put
  db->Put(write_options, "key5390", "value30566");

  // op 4658: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3003", "key7021", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3003, key7021) size=" << scan_result.size() << std::endl;
  }

  // op 4659: put
  db->Put(write_options, "key7190", "value114240");

  // op 4660: put
  db->Put(write_options, "key4031", "value3977");

  // op 4661: get
  status = db->Get(read_options, "key5600", &value);
  if (status.ok()) {
    out << "GET key5600 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4662: get
  status = db->Get(read_options, "key2497", &value);
  if (status.ok()) {
    out << "GET key2497 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4663: get
  status = db->Get(read_options, "key7655", &value);
  if (status.ok()) {
    out << "GET key7655 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4664: put
  db->Put(write_options, "key3839", "value101675");

  // op 4665: get
  status = db->Get(read_options, "key6627", &value);
  if (status.ok()) {
    out << "GET key6627 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4666: get
  status = db->Get(read_options, "key6418", &value);
  if (status.ok()) {
    out << "GET key6418 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4667: delete_range
  status = db->DeleteRange(write_options, "key5053", "key8260");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 4668: get
  status = db->Get(read_options, "key2201", &value);
  if (status.ok()) {
    out << "GET key2201 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4669: put
  db->Put(write_options, "key10638", "value9594");

  // op 4670: put
  db->Put(write_options, "key2206", "value48537");

  // op 4671: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0839", "key4736", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0839, key4736) size=" << scan_result.size() << std::endl;
  }

  // op 4672: put
  db->Put(write_options, "key9929", "value10655");

  // op 4673: get
  status = db->Get(read_options, "key0786", &value);
  if (status.ok()) {
    out << "GET key0786 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4674: get
  status = db->Get(read_options, "key11517", &value);
  if (status.ok()) {
    out << "GET key11517 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4675: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5142", "key9402", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5142, key9402) size=" << scan_result.size() << std::endl;
  }

  // op 4676: put
  db->Put(write_options, "key7150", "value31010");

  // op 4677: get
  status = db->Get(read_options, "key4670", &value);
  if (status.ok()) {
    out << "GET key4670 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4678: put
  db->Put(write_options, "key7644", "value94225");

  // op 4679: get
  status = db->Get(read_options, "key9447", &value);
  if (status.ok()) {
    out << "GET key9447 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4680: get
  status = db->Get(read_options, "key9704", &value);
  if (status.ok()) {
    out << "GET key9704 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4681: get
  status = db->Get(read_options, "key8140", &value);
  if (status.ok()) {
    out << "GET key8140 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4682: delete
  db->Delete(write_options, "key2854");

  // op 4683: put
  db->Put(write_options, "key1235", "value68844");

  // op 4684: delete
  db->Delete(write_options, "key0759");

  // op 4685: put
  db->Put(write_options, "key1789", "value61299");

  // op 4686: get
  status = db->Get(read_options, "key11482", &value);
  if (status.ok()) {
    out << "GET key11482 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4687: put
  db->Put(write_options, "key10980", "value117024");

  // op 4688: delete_range
  status = db->DeleteRange(write_options, "key4558", "key9530");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 4689: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5574", "key6852", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5574, key6852) size=" << scan_result.size() << std::endl;
  }

  // op 4690: get
  status = db->Get(read_options, "key3146", &value);
  if (status.ok()) {
    out << "GET key3146 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4691: put
  db->Put(write_options, "key7493", "value113110");

  // op 4692: put
  db->Put(write_options, "key4226", "value107125");

  // op 4693: delete
  db->Delete(write_options, "key6834");

  // op 4694: delete_range
  status = db->DeleteRange(write_options, "key2185", "key2470");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 4695: put
  db->Put(write_options, "key11127", "value20593");

  // op 4696: delete_range
  status = db->DeleteRange(write_options, "key7250", "key11127");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 4697: put
  db->Put(write_options, "key3730", "value77201");

  // op 4698: put
  db->Put(write_options, "key8809", "value35025");

  // op 4699: put
  db->Put(write_options, "key0271", "value11976");

  // op 4700: put
  db->Put(write_options, "key10144", "value85889");

  // op 4701: put
  db->Put(write_options, "key7496", "value28944");

  // op 4702: put
  db->Put(write_options, "key2890", "value70776");

  // op 4703: get
  status = db->Get(read_options, "key2067", &value);
  if (status.ok()) {
    out << "GET key2067 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4704: delete
  db->Delete(write_options, "key7166");

  // op 4705: delete_range
  status = db->DeleteRange(write_options, "key0614", "key11527");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 4706: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3817", "key3950", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3817, key3950) size=" << scan_result.size() << std::endl;
  }

  // op 4707: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 4708: get
  status = db->Get(read_options, "key4882", &value);
  if (status.ok()) {
    out << "GET key4882 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4709: get
  status = db->Get(read_options, "key7768", &value);
  if (status.ok()) {
    out << "GET key7768 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4710: get
  status = db->Get(read_options, "key1336", &value);
  if (status.ok()) {
    out << "GET key1336 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4711: get
  status = db->Get(read_options, "key9660", &value);
  if (status.ok()) {
    out << "GET key9660 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4712: put
  db->Put(write_options, "key3999", "value99446");

  // op 4713: get
  status = db->Get(read_options, "key3192", &value);
  if (status.ok()) {
    out << "GET key3192 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4714: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 4715: get
  status = db->Get(read_options, "key6468", &value);
  if (status.ok()) {
    out << "GET key6468 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4716: put
  db->Put(write_options, "key2779", "value26627");

  // op 4717: put
  db->Put(write_options, "key10001", "value6766");

  // op 4718: put
  db->Put(write_options, "key7193", "value0661");

  // op 4719: get
  status = db->Get(read_options, "key1696", &value);
  if (status.ok()) {
    out << "GET key1696 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4720: get
  status = db->Get(read_options, "key1931", &value);
  if (status.ok()) {
    out << "GET key1931 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4721: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8438", "key10243", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8438, key10243) size=" << scan_result.size() << std::endl;
  }

  // op 4722: put
  db->Put(write_options, "key0926", "value32347");

  // op 4723: get
  status = db->Get(read_options, "key2541", &value);
  if (status.ok()) {
    out << "GET key2541 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4724: get
  status = db->Get(read_options, "key3246", &value);
  if (status.ok()) {
    out << "GET key3246 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4725: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2131", "key4595", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2131, key4595) size=" << scan_result.size() << std::endl;
  }

  // op 4726: delete_range
  status = db->DeleteRange(write_options, "key5857", "key7328");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 4727: put
  db->Put(write_options, "key9391", "value57656");

  // op 4728: put
  db->Put(write_options, "key3969", "value99326");

  // op 4729: put
  db->Put(write_options, "key4255", "value74449");

  // op 4730: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4885", "key8202", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4885, key8202) size=" << scan_result.size() << std::endl;
  }

  // op 4731: get
  status = db->Get(read_options, "key5789", &value);
  if (status.ok()) {
    out << "GET key5789 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4732: put
  db->Put(write_options, "key5003", "value106088");

  // op 4733: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1664", "key3538", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1664, key3538) size=" << scan_result.size() << std::endl;
  }

  // op 4734: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 4735: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4691", "key7475", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4691, key7475) size=" << scan_result.size() << std::endl;
  }

  // op 4736: put
  db->Put(write_options, "key8561", "value93284");

  // op 4737: put
  db->Put(write_options, "key1667", "value66734");

  // op 4738: put
  db->Put(write_options, "key7250", "value3471");

  // op 4739: put
  db->Put(write_options, "key2675", "value89521");

  // op 4740: get
  status = db->Get(read_options, "key8161", &value);
  if (status.ok()) {
    out << "GET key8161 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4741: get
  status = db->Get(read_options, "key0572", &value);
  if (status.ok()) {
    out << "GET key0572 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4742: put
  db->Put(write_options, "key7222", "value98888");

  // op 4743: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 4744: put
  db->Put(write_options, "key5789", "value89925");

  // op 4745: delete_range
  status = db->DeleteRange(write_options, "key3059", "key10508");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 4746: get
  status = db->Get(read_options, "key8322", &value);
  if (status.ok()) {
    out << "GET key8322 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4747: get
  status = db->Get(read_options, "key1340", &value);
  if (status.ok()) {
    out << "GET key1340 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4748: delete_range
  status = db->DeleteRange(write_options, "key6726", "key7171");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 4749: delete_range
  status = db->DeleteRange(write_options, "key0584", "key7716");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 4750: delete
  db->Delete(write_options, "key3858");

  // op 4751: delete_range
  status = db->DeleteRange(write_options, "key8192", "key9378");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 4752: put
  db->Put(write_options, "key5406", "value8596");

  // op 4753: delete
  db->Delete(write_options, "key9515");

  // op 4754: delete_range
  status = db->DeleteRange(write_options, "key1405", "key10693");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 4755: put
  db->Put(write_options, "key3754", "value82758");

  // op 4756: put
  db->Put(write_options, "key6399", "value79507");

  // op 4757: put
  db->Put(write_options, "key8367", "value101732");

  // op 4758: get
  status = db->Get(read_options, "key2581", &value);
  if (status.ok()) {
    out << "GET key2581 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4759: put
  db->Put(write_options, "key5299", "value112998");

  // op 4760: put
  db->Put(write_options, "key9746", "value89768");

  // op 4761: put
  db->Put(write_options, "key0694", "value81934");

  // op 4762: delete_range
  status = db->DeleteRange(write_options, "key0112", "key6197");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 4763: delete_range
  status = db->DeleteRange(write_options, "key4968", "key8328");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 4764: get
  status = db->Get(read_options, "key0538", &value);
  if (status.ok()) {
    out << "GET key0538 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4765: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0848", "key10474", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0848, key10474) size=" << scan_result.size() << std::endl;
  }

  // op 4766: put
  db->Put(write_options, "key5003", "value96991");

  // op 4767: delete
  db->Delete(write_options, "key7677");

  // op 4768: get
  status = db->Get(read_options, "key6254", &value);
  if (status.ok()) {
    out << "GET key6254 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4769: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3599", "key7795", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3599, key7795) size=" << scan_result.size() << std::endl;
  }

  // op 4770: put
  db->Put(write_options, "key0031", "value25233");

  // op 4771: delete_range
  status = db->DeleteRange(write_options, "key0854", "key5977");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 4772: put
  db->Put(write_options, "key10588", "value37001");

  // op 4773: put
  db->Put(write_options, "key3693", "value68436");

  // op 4774: delete
  db->Delete(write_options, "key11158");

  // op 4775: delete
  db->Delete(write_options, "key6541");

  // op 4776: put
  db->Put(write_options, "key5566", "value108306");

  // op 4777: get
  status = db->Get(read_options, "key5704", &value);
  if (status.ok()) {
    out << "GET key5704 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4778: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3522", "key7826", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3522, key7826) size=" << scan_result.size() << std::endl;
  }

  // op 4779: put
  db->Put(write_options, "key10554", "value65187");

  // op 4780: get
  status = db->Get(read_options, "key6912", &value);
  if (status.ok()) {
    out << "GET key6912 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4781: put
  db->Put(write_options, "key5312", "value86960");

  // op 4782: put
  db->Put(write_options, "key8620", "value16515");

  // op 4783: get
  status = db->Get(read_options, "key8875", &value);
  if (status.ok()) {
    out << "GET key8875 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4784: put
  db->Put(write_options, "key7622", "value73519");

  // op 4785: put
  db->Put(write_options, "key11361", "value52398");

  // op 4786: put
  db->Put(write_options, "key3221", "value112003");

  // op 4787: delete_range
  status = db->DeleteRange(write_options, "key2663", "key11327");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 4788: put
  db->Put(write_options, "key1745", "value63400");

  // op 4789: delete
  db->Delete(write_options, "key1558");

  // op 4790: get
  status = db->Get(read_options, "key2173", &value);
  if (status.ok()) {
    out << "GET key2173 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4791: delete_range
  status = db->DeleteRange(write_options, "key6264", "key10006");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 4792: get
  status = db->Get(read_options, "key11296", &value);
  if (status.ok()) {
    out << "GET key11296 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4793: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7165", "key8012", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7165, key8012) size=" << scan_result.size() << std::endl;
  }

  // op 4794: put
  db->Put(write_options, "key1282", "value53940");

  // op 4795: get
  status = db->Get(read_options, "key9029", &value);
  if (status.ok()) {
    out << "GET key9029 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4796: delete_range
  status = db->DeleteRange(write_options, "key2463", "key5216");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 4797: delete
  db->Delete(write_options, "key0599");

  // op 4798: get
  status = db->Get(read_options, "key4147", &value);
  if (status.ok()) {
    out << "GET key4147 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4799: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0468", "key2995", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0468, key2995) size=" << scan_result.size() << std::endl;
  }

  // op 4800: delete
  db->Delete(write_options, "key6156");

  // op 4801: put
  db->Put(write_options, "key8216", "value11050");

  // op 4802: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0480", "key10568", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0480, key10568) size=" << scan_result.size() << std::endl;
  }

  // op 4803: put
  db->Put(write_options, "key3561", "value56870");

  // op 4804: put
  db->Put(write_options, "key2199", "value80508");

  // op 4805: put
  db->Put(write_options, "key7065", "value70005");

  // op 4806: put
  db->Put(write_options, "key5029", "value84247");

  // op 4807: get
  status = db->Get(read_options, "key2041", &value);
  if (status.ok()) {
    out << "GET key2041 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4808: put
  db->Put(write_options, "key11729", "value21846");

  // op 4809: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0354", "key7038", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0354, key7038) size=" << scan_result.size() << std::endl;
  }

  // op 4810: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3458", "key9595", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3458, key9595) size=" << scan_result.size() << std::endl;
  }

  // op 4811: put
  db->Put(write_options, "key5183", "value67590");

  // op 4812: delete_range
  status = db->DeleteRange(write_options, "key4968", "key9552");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 4813: get
  status = db->Get(read_options, "key5047", &value);
  if (status.ok()) {
    out << "GET key5047 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4814: get
  status = db->Get(read_options, "key9731", &value);
  if (status.ok()) {
    out << "GET key9731 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4815: delete
  db->Delete(write_options, "key0156");

  // op 4816: put
  db->Put(write_options, "key11668", "value79449");

  // op 4817: delete
  db->Delete(write_options, "key8169");

  // op 4818: get
  status = db->Get(read_options, "key8766", &value);
  if (status.ok()) {
    out << "GET key8766 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4819: put
  db->Put(write_options, "key9422", "value67744");

  // op 4820: put
  db->Put(write_options, "key8280", "value44477");

  // op 4821: put
  db->Put(write_options, "key8561", "value28023");

  // op 4822: put
  db->Put(write_options, "key7684", "value100031");

  // op 4823: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0235", "key1808", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0235, key1808) size=" << scan_result.size() << std::endl;
  }

  // op 4824: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1639", "key11310", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1639, key11310) size=" << scan_result.size() << std::endl;
  }

  // op 4825: put
  db->Put(write_options, "key7576", "value1426");

  // op 4826: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4445", "key7378", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4445, key7378) size=" << scan_result.size() << std::endl;
  }

  // op 4827: get
  status = db->Get(read_options, "key3015", &value);
  if (status.ok()) {
    out << "GET key3015 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4828: delete
  db->Delete(write_options, "key3755");

  // op 4829: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7279", "key9941", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7279, key9941) size=" << scan_result.size() << std::endl;
  }

  // op 4830: put
  db->Put(write_options, "key4534", "value48251");

  // op 4831: put
  db->Put(write_options, "key4173", "value14603");

  // op 4832: put
  db->Put(write_options, "key1238", "value110876");

  // op 4833: put
  db->Put(write_options, "key8007", "value45713");

  // op 4834: put
  db->Put(write_options, "key1456", "value95613");

  // op 4835: put
  db->Put(write_options, "key2191", "value57555");

  // op 4836: put
  db->Put(write_options, "key5052", "value21637");

  // op 4837: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 4838: get
  status = db->Get(read_options, "key0599", &value);
  if (status.ok()) {
    out << "GET key0599 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4839: delete
  db->Delete(write_options, "key6050");

  // op 4840: put
  db->Put(write_options, "key2664", "value28838");

  // op 4841: get
  status = db->Get(read_options, "key3793", &value);
  if (status.ok()) {
    out << "GET key3793 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4842: put
  db->Put(write_options, "key6689", "value82419");

  // op 4843: put
  db->Put(write_options, "key6655", "value62731");

  // op 4844: put
  db->Put(write_options, "key3261", "value93542");

  // op 4845: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0022", "key8569", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0022, key8569) size=" << scan_result.size() << std::endl;
  }

  // op 4846: put
  db->Put(write_options, "key9477", "value43636");

  // op 4847: delete
  db->Delete(write_options, "key9732");

  // op 4848: get
  status = db->Get(read_options, "key1092", &value);
  if (status.ok()) {
    out << "GET key1092 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4849: put
  db->Put(write_options, "key8518", "value100226");

  // op 4850: get
  status = db->Get(read_options, "key5798", &value);
  if (status.ok()) {
    out << "GET key5798 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4851: put
  db->Put(write_options, "key11494", "value90936");

  // op 4852: put
  db->Put(write_options, "key9812", "value25232");

  // op 4853: put
  db->Put(write_options, "key6726", "value42184");

  // op 4854: delete_range
  status = db->DeleteRange(write_options, "key5109", "key5875");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 4855: put
  db->Put(write_options, "key3445", "value5255");

  // op 4856: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 4857: put
  db->Put(write_options, "key8540", "value25519");

  // op 4858: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1398", "key6775", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1398, key6775) size=" << scan_result.size() << std::endl;
  }

  // op 4859: get
  status = db->Get(read_options, "key4652", &value);
  if (status.ok()) {
    out << "GET key4652 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4860: delete
  db->Delete(write_options, "key9760");

  // op 4861: delete_range
  status = db->DeleteRange(write_options, "key4908", "key10110");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 4862: delete
  db->Delete(write_options, "key8383");

  // op 4863: delete
  db->Delete(write_options, "key8198");

  // op 4864: put
  db->Put(write_options, "key11045", "value55652");

  // op 4865: put
  db->Put(write_options, "key7472", "value86708");

  // op 4866: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 4867: delete
  db->Delete(write_options, "key10461");

  // op 4868: delete_range
  status = db->DeleteRange(write_options, "key7877", "key11640");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 4869: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3289", "key11509", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3289, key11509) size=" << scan_result.size() << std::endl;
  }

  // op 4870: delete
  db->Delete(write_options, "key6431");

  // op 4871: put
  db->Put(write_options, "key9621", "value40672");

  // op 4872: put
  db->Put(write_options, "key1096", "value9190");

  // op 4873: put
  db->Put(write_options, "key10654", "value70134");

  // op 4874: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0398", "key3550", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0398, key3550) size=" << scan_result.size() << std::endl;
  }

  // op 4875: get
  status = db->Get(read_options, "key11162", &value);
  if (status.ok()) {
    out << "GET key11162 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4876: get
  status = db->Get(read_options, "key11530", &value);
  if (status.ok()) {
    out << "GET key11530 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4877: get
  status = db->Get(read_options, "key2648", &value);
  if (status.ok()) {
    out << "GET key2648 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4878: get
  status = db->Get(read_options, "key9069", &value);
  if (status.ok()) {
    out << "GET key9069 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4879: delete_range
  status = db->DeleteRange(write_options, "key1959", "key4994");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 4880: put
  db->Put(write_options, "key5746", "value32978");

  // op 4881: put
  db->Put(write_options, "key9855", "value78771");

  // op 4882: put
  db->Put(write_options, "key7000", "value69874");

  // op 4883: delete_range
  status = db->DeleteRange(write_options, "key9286", "key11467");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 4884: get
  status = db->Get(read_options, "key4757", &value);
  if (status.ok()) {
    out << "GET key4757 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4885: put
  db->Put(write_options, "key8958", "value47076");

  // op 4886: delete_range
  status = db->DeleteRange(write_options, "key2360", "key2702");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 4887: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7137", "key7825", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7137, key7825) size=" << scan_result.size() << std::endl;
  }

  // op 4888: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4533", "key10652", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4533, key10652) size=" << scan_result.size() << std::endl;
  }

  // op 4889: put
  db->Put(write_options, "key6427", "value30261");

  // op 4890: put
  db->Put(write_options, "key7925", "value71599");

  // op 4891: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3645", "key9462", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3645, key9462) size=" << scan_result.size() << std::endl;
  }

  // op 4892: put
  db->Put(write_options, "key6170", "value71755");

  // op 4893: get
  status = db->Get(read_options, "key0074", &value);
  if (status.ok()) {
    out << "GET key0074 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4894: put
  db->Put(write_options, "key8463", "value23016");

  // op 4895: get
  status = db->Get(read_options, "key1329", &value);
  if (status.ok()) {
    out << "GET key1329 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4896: delete_range
  status = db->DeleteRange(write_options, "key5460", "key6416");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 4897: put
  db->Put(write_options, "key2449", "value89076");

  // op 4898: put
  db->Put(write_options, "key10126", "value36582");

  // op 4899: put
  db->Put(write_options, "key6605", "value29023");

  // op 4900: get
  status = db->Get(read_options, "key6833", &value);
  if (status.ok()) {
    out << "GET key6833 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4901: put
  db->Put(write_options, "key3513", "value20955");

  // op 4902: put
  db->Put(write_options, "key6937", "value21979");

  // op 4903: put
  db->Put(write_options, "key0507", "value117656");

  // op 4904: delete_range
  status = db->DeleteRange(write_options, "key4681", "key5820");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 4905: delete_range
  status = db->DeleteRange(write_options, "key6905", "key7403");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 4906: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0397", "key6347", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0397, key6347) size=" << scan_result.size() << std::endl;
  }

  // op 4907: put
  db->Put(write_options, "key5592", "value79134");

  // op 4908: put
  db->Put(write_options, "key2828", "value3683");

  // op 4909: put
  db->Put(write_options, "key3830", "value69340");

  // op 4910: put
  db->Put(write_options, "key5290", "value37700");

  // op 4911: get
  status = db->Get(read_options, "key11227", &value);
  if (status.ok()) {
    out << "GET key11227 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4912: delete_range
  status = db->DeleteRange(write_options, "key10087", "key10986");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 4913: delete
  db->Delete(write_options, "key0492");

  // op 4914: put
  db->Put(write_options, "key1703", "value24354");

  // op 4915: delete
  db->Delete(write_options, "key6475");

  // op 4916: put
  db->Put(write_options, "key9632", "value102563");

  // op 4917: delete
  db->Delete(write_options, "key1484");

  // op 4918: put
  db->Put(write_options, "key5620", "value99504");

  // op 4919: delete_range
  status = db->DeleteRange(write_options, "key1942", "key6975");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 4920: delete_range
  status = db->DeleteRange(write_options, "key5612", "key10031");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 4921: put
  db->Put(write_options, "key6152", "value3941");

  // op 4922: put
  db->Put(write_options, "key2216", "value33301");

  // op 4923: delete_range
  status = db->DeleteRange(write_options, "key5851", "key9847");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 4924: delete_range
  status = db->DeleteRange(write_options, "key2721", "key7634");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 4925: delete
  db->Delete(write_options, "key8812");

  // op 4926: delete
  db->Delete(write_options, "key8601");

  // op 4927: get
  status = db->Get(read_options, "key3291", &value);
  if (status.ok()) {
    out << "GET key3291 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4928: get
  status = db->Get(read_options, "key1284", &value);
  if (status.ok()) {
    out << "GET key1284 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4929: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1393", "key9292", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1393, key9292) size=" << scan_result.size() << std::endl;
  }

  // op 4930: put
  db->Put(write_options, "key7232", "value85014");

  // op 4931: put
  db->Put(write_options, "key5592", "value37551");

  // op 4932: get
  status = db->Get(read_options, "key9415", &value);
  if (status.ok()) {
    out << "GET key9415 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4933: put
  db->Put(write_options, "key0607", "value71941");

  // op 4934: delete
  db->Delete(write_options, "key5940");

  // op 4935: get
  status = db->Get(read_options, "key0737", &value);
  if (status.ok()) {
    out << "GET key0737 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4936: delete_range
  status = db->DeleteRange(write_options, "key5305", "key7048");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 4937: put
  db->Put(write_options, "key10090", "value7215");

  // op 4938: get
  status = db->Get(read_options, "key8152", &value);
  if (status.ok()) {
    out << "GET key8152 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4939: delete
  db->Delete(write_options, "key0828");

  // op 4940: put
  db->Put(write_options, "key9229", "value3247");

  // op 4941: get
  status = db->Get(read_options, "key7272", &value);
  if (status.ok()) {
    out << "GET key7272 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4942: get
  status = db->Get(read_options, "key7001", &value);
  if (status.ok()) {
    out << "GET key7001 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4943: put
  db->Put(write_options, "key5232", "value36718");

  // op 4944: put
  db->Put(write_options, "key7068", "value90938");

  // op 4945: get
  status = db->Get(read_options, "key2955", &value);
  if (status.ok()) {
    out << "GET key2955 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4946: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3889", "key11070", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3889, key11070) size=" << scan_result.size() << std::endl;
  }

  // op 4947: delete_range
  status = db->DeleteRange(write_options, "key5850", "key6549");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 4948: delete_range
  status = db->DeleteRange(write_options, "key7148", "key7351");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 4949: put
  db->Put(write_options, "key6815", "value97301");

  // op 4950: delete_range
  status = db->DeleteRange(write_options, "key6918", "key8560");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 4951: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0823", "key11278", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0823, key11278) size=" << scan_result.size() << std::endl;
  }

  // op 4952: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3454", "key4465", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3454, key4465) size=" << scan_result.size() << std::endl;
  }

  // op 4953: put
  db->Put(write_options, "key8085", "value65193");

  // op 4954: get
  status = db->Get(read_options, "key0849", &value);
  if (status.ok()) {
    out << "GET key0849 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4955: put
  db->Put(write_options, "key9165", "value28375");

  // op 4956: get
  status = db->Get(read_options, "key10904", &value);
  if (status.ok()) {
    out << "GET key10904 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4957: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5122", "key9626", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5122, key9626) size=" << scan_result.size() << std::endl;
  }

  // op 4958: put
  db->Put(write_options, "key6850", "value113712");

  // op 4959: delete
  db->Delete(write_options, "key1739");

  // op 4960: put
  db->Put(write_options, "key9606", "value70026");

  // op 4961: delete
  db->Delete(write_options, "key11345");

  // op 4962: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 4963: get
  status = db->Get(read_options, "key8798", &value);
  if (status.ok()) {
    out << "GET key8798 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4964: get
  status = db->Get(read_options, "key4517", &value);
  if (status.ok()) {
    out << "GET key4517 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4965: put
  db->Put(write_options, "key4487", "value113300");

  // op 4966: put
  db->Put(write_options, "key5230", "value96646");

  // op 4967: get
  status = db->Get(read_options, "key3254", &value);
  if (status.ok()) {
    out << "GET key3254 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4968: put
  db->Put(write_options, "key6794", "value93341");

  // op 4969: delete
  db->Delete(write_options, "key11089");

  // op 4970: get
  status = db->Get(read_options, "key3478", &value);
  if (status.ok()) {
    out << "GET key3478 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4971: delete_range
  status = db->DeleteRange(write_options, "key1125", "key1270");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 4972: get
  status = db->Get(read_options, "key9888", &value);
  if (status.ok()) {
    out << "GET key9888 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4973: get
  status = db->Get(read_options, "key4207", &value);
  if (status.ok()) {
    out << "GET key4207 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4974: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2454", "key5285", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2454, key5285) size=" << scan_result.size() << std::endl;
  }

  // op 4975: put
  db->Put(write_options, "key5619", "value15796");

  // op 4976: put
  db->Put(write_options, "key11271", "value51640");

  // op 4977: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 4978: get
  status = db->Get(read_options, "key0797", &value);
  if (status.ok()) {
    out << "GET key0797 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4979: delete_range
  status = db->DeleteRange(write_options, "key1311", "key6539");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 4980: get
  status = db->Get(read_options, "key3828", &value);
  if (status.ok()) {
    out << "GET key3828 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4981: get
  status = db->Get(read_options, "key8429", &value);
  if (status.ok()) {
    out << "GET key8429 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4982: put
  db->Put(write_options, "key5534", "value109207");

  // op 4983: put
  db->Put(write_options, "key6878", "value2089");

  // op 4984: put
  db->Put(write_options, "key3578", "value3208");

  // op 4985: put
  db->Put(write_options, "key10620", "value84597");

  // op 4986: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 4987: put
  db->Put(write_options, "key4020", "value116109");

  // op 4988: delete_range
  status = db->DeleteRange(write_options, "key5044", "key5046");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 4989: get
  status = db->Get(read_options, "key8283", &value);
  if (status.ok()) {
    out << "GET key8283 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 4990: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8199", "key8382", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8199, key8382) size=" << scan_result.size() << std::endl;
  }

  // op 4991: delete
  db->Delete(write_options, "key8395");

  // op 4992: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7833", "key9470", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7833, key9470) size=" << scan_result.size() << std::endl;
  }

  // op 4993: delete
  db->Delete(write_options, "key5274");

  // op 4994: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 4995: put
  db->Put(write_options, "key7139", "value88660");

  // op 4996: delete
  db->Delete(write_options, "key6177");

  // op 4997: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 4998: put
  db->Put(write_options, "key6306", "value112750");

  // op 4999: get
  status = db->Get(read_options, "key2647", &value);
  if (status.ok()) {
    out << "GET key2647 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5000: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0480", "key5345", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0480, key5345) size=" << scan_result.size() << std::endl;
  }

  // op 5001: get
  status = db->Get(read_options, "key11608", &value);
  if (status.ok()) {
    out << "GET key11608 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5002: put
  db->Put(write_options, "key1391", "value96846");

  // op 5003: put
  db->Put(write_options, "key7551", "value83551");

  // op 5004: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0670", "key4872", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0670, key4872) size=" << scan_result.size() << std::endl;
  }

  // op 5005: put
  db->Put(write_options, "key6097", "value72508");

  // op 5006: get
  status = db->Get(read_options, "key3906", &value);
  if (status.ok()) {
    out << "GET key3906 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5007: put
  db->Put(write_options, "key3374", "value39711");

  // op 5008: put
  db->Put(write_options, "key4204", "value23244");

  // op 5009: get
  status = db->Get(read_options, "key2917", &value);
  if (status.ok()) {
    out << "GET key2917 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5010: put
  db->Put(write_options, "key0117", "value30250");

  // op 5011: put
  db->Put(write_options, "key1863", "value105045");

  // op 5012: delete_range
  status = db->DeleteRange(write_options, "key9197", "key9302");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 5013: put
  db->Put(write_options, "key9581", "value70132");

  // op 5014: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4108", "key6061", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4108, key6061) size=" << scan_result.size() << std::endl;
  }

  // op 5015: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1147", "key6457", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1147, key6457) size=" << scan_result.size() << std::endl;
  }

  // op 5016: put
  db->Put(write_options, "key0090", "value26110");

  // op 5017: get
  status = db->Get(read_options, "key8758", &value);
  if (status.ok()) {
    out << "GET key8758 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5018: put
  db->Put(write_options, "key3527", "value90689");

  // op 5019: delete
  db->Delete(write_options, "key0054");

  // op 5020: put
  db->Put(write_options, "key11353", "value60620");

  // op 5021: put
  db->Put(write_options, "key6332", "value47037");

  // op 5022: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0430", "key11415", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0430, key11415) size=" << scan_result.size() << std::endl;
  }

  // op 5023: delete_range
  status = db->DeleteRange(write_options, "key9395", "key11162");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 5024: delete_range
  status = db->DeleteRange(write_options, "key7972", "key9702");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 5025: delete
  db->Delete(write_options, "key0508");

  // op 5026: delete
  db->Delete(write_options, "key4820");

  // op 5027: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2728", "key8704", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2728, key8704) size=" << scan_result.size() << std::endl;
  }

  // op 5028: put
  db->Put(write_options, "key4395", "value40223");

  // op 5029: get
  status = db->Get(read_options, "key5435", &value);
  if (status.ok()) {
    out << "GET key5435 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5030: put
  db->Put(write_options, "key8378", "value87202");

  // op 5031: get
  status = db->Get(read_options, "key5448", &value);
  if (status.ok()) {
    out << "GET key5448 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5032: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4827", "key11363", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4827, key11363) size=" << scan_result.size() << std::endl;
  }

  // op 5033: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3628", "key3641", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3628, key3641) size=" << scan_result.size() << std::endl;
  }

  // op 5034: delete_range
  status = db->DeleteRange(write_options, "key6380", "key6822");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 5035: delete
  db->Delete(write_options, "key10562");

  // op 5036: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2085", "key10522", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2085, key10522) size=" << scan_result.size() << std::endl;
  }

  // op 5037: get
  status = db->Get(read_options, "key10214", &value);
  if (status.ok()) {
    out << "GET key10214 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5038: delete_range
  status = db->DeleteRange(write_options, "key3824", "key7716");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 5039: get
  status = db->Get(read_options, "key7652", &value);
  if (status.ok()) {
    out << "GET key7652 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5040: put
  db->Put(write_options, "key11046", "value27337");

  // op 5041: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4358", "key5112", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4358, key5112) size=" << scan_result.size() << std::endl;
  }

  // op 5042: get
  status = db->Get(read_options, "key7885", &value);
  if (status.ok()) {
    out << "GET key7885 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5043: put
  db->Put(write_options, "key0499", "value45908");

  // op 5044: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6504", "key6812", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6504, key6812) size=" << scan_result.size() << std::endl;
  }

  // op 5045: put
  db->Put(write_options, "key9174", "value9893");

  // op 5046: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5649", "key8378", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5649, key8378) size=" << scan_result.size() << std::endl;
  }

  // op 5047: put
  db->Put(write_options, "key4780", "value48492");

  // op 5048: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0368", "key9186", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0368, key9186) size=" << scan_result.size() << std::endl;
  }

  // op 5049: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 5050: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9335", "key11092", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9335, key11092) size=" << scan_result.size() << std::endl;
  }

  // op 5051: put
  db->Put(write_options, "key10392", "value5157");

  // op 5052: put
  db->Put(write_options, "key2883", "value112759");

  // op 5053: put
  db->Put(write_options, "key2314", "value71503");

  // op 5054: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2591", "key11150", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2591, key11150) size=" << scan_result.size() << std::endl;
  }

  // op 5055: delete
  db->Delete(write_options, "key1823");

  // op 5056: put
  db->Put(write_options, "key0403", "value32771");

  // op 5057: get
  status = db->Get(read_options, "key7010", &value);
  if (status.ok()) {
    out << "GET key7010 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5058: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8183", "key8565", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8183, key8565) size=" << scan_result.size() << std::endl;
  }

  // op 5059: get
  status = db->Get(read_options, "key3194", &value);
  if (status.ok()) {
    out << "GET key3194 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5060: delete
  db->Delete(write_options, "key9168");

  // op 5061: delete
  db->Delete(write_options, "key2051");

  // op 5062: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2337", "key4342", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2337, key4342) size=" << scan_result.size() << std::endl;
  }

  // op 5063: delete
  db->Delete(write_options, "key4634");

  // op 5064: delete_range
  status = db->DeleteRange(write_options, "key5167", "key7280");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 5065: put
  db->Put(write_options, "key6144", "value45376");

  // op 5066: get
  status = db->Get(read_options, "key10257", &value);
  if (status.ok()) {
    out << "GET key10257 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5067: put
  db->Put(write_options, "key6385", "value53911");

  // op 5068: put
  db->Put(write_options, "key11019", "value55448");

  // op 5069: put
  db->Put(write_options, "key10939", "value8871");

  // op 5070: get
  status = db->Get(read_options, "key4605", &value);
  if (status.ok()) {
    out << "GET key4605 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5071: get
  status = db->Get(read_options, "key8799", &value);
  if (status.ok()) {
    out << "GET key8799 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5072: get
  status = db->Get(read_options, "key1020", &value);
  if (status.ok()) {
    out << "GET key1020 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5073: get
  status = db->Get(read_options, "key10835", &value);
  if (status.ok()) {
    out << "GET key10835 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5074: delete
  db->Delete(write_options, "key3851");

  // op 5075: put
  db->Put(write_options, "key6323", "value1581");

  // op 5076: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6702", "key7382", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6702, key7382) size=" << scan_result.size() << std::endl;
  }

  // op 5077: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 5078: put
  db->Put(write_options, "key6907", "value28027");

  // op 5079: get
  status = db->Get(read_options, "key10810", &value);
  if (status.ok()) {
    out << "GET key10810 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5080: get
  status = db->Get(read_options, "key0064", &value);
  if (status.ok()) {
    out << "GET key0064 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5081: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 5082: get
  status = db->Get(read_options, "key8374", &value);
  if (status.ok()) {
    out << "GET key8374 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5083: delete
  db->Delete(write_options, "key4970");

  // op 5084: put
  db->Put(write_options, "key1171", "value33727");

  // op 5085: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4750", "key6528", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4750, key6528) size=" << scan_result.size() << std::endl;
  }

  // op 5086: put
  db->Put(write_options, "key7387", "value106538");

  // op 5087: put
  db->Put(write_options, "key6758", "value31304");

  // op 5088: put
  db->Put(write_options, "key10684", "value20665");

  // op 5089: delete
  db->Delete(write_options, "key4185");

  // op 5090: get
  status = db->Get(read_options, "key2804", &value);
  if (status.ok()) {
    out << "GET key2804 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5091: get
  status = db->Get(read_options, "key2336", &value);
  if (status.ok()) {
    out << "GET key2336 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5092: put
  db->Put(write_options, "key3504", "value28715");

  // op 5093: put
  db->Put(write_options, "key6919", "value77287");

  // op 5094: delete
  db->Delete(write_options, "key9453");

  // op 5095: put
  db->Put(write_options, "key0426", "value107495");

  // op 5096: put
  db->Put(write_options, "key0618", "value93687");

  // op 5097: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 5098: get
  status = db->Get(read_options, "key3243", &value);
  if (status.ok()) {
    out << "GET key3243 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5099: get
  status = db->Get(read_options, "key4219", &value);
  if (status.ok()) {
    out << "GET key4219 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5100: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 5101: delete_range
  status = db->DeleteRange(write_options, "key0910", "key3921");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 5102: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 5103: get
  status = db->Get(read_options, "key5560", &value);
  if (status.ok()) {
    out << "GET key5560 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5104: put
  db->Put(write_options, "key10942", "value62831");

  // op 5105: get
  status = db->Get(read_options, "key2390", &value);
  if (status.ok()) {
    out << "GET key2390 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5106: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3300", "key10426", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3300, key10426) size=" << scan_result.size() << std::endl;
  }

  // op 5107: delete_range
  status = db->DeleteRange(write_options, "key5551", "key10173");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 5108: delete
  db->Delete(write_options, "key8845");

  // op 5109: delete
  db->Delete(write_options, "key1001");

  // op 5110: put
  db->Put(write_options, "key11493", "value91514");

  // op 5111: delete_range
  status = db->DeleteRange(write_options, "key1335", "key5929");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 5112: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8033", "key10013", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8033, key10013) size=" << scan_result.size() << std::endl;
  }

  // op 5113: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1049", "key3143", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1049, key3143) size=" << scan_result.size() << std::endl;
  }

  // op 5114: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3154", "key5547", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3154, key5547) size=" << scan_result.size() << std::endl;
  }

  // op 5115: put
  db->Put(write_options, "key6503", "value112798");

  // op 5116: put
  db->Put(write_options, "key7892", "value104747");

  // op 5117: put
  db->Put(write_options, "key8639", "value84416");

  // op 5118: put
  db->Put(write_options, "key1207", "value13992");

  // op 5119: put
  db->Put(write_options, "key4587", "value20251");

  // op 5120: delete
  db->Delete(write_options, "key2531");

  // op 5121: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3999", "key9223", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3999, key9223) size=" << scan_result.size() << std::endl;
  }

  // op 5122: get
  status = db->Get(read_options, "key6700", &value);
  if (status.ok()) {
    out << "GET key6700 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5123: delete_range
  status = db->DeleteRange(write_options, "key1346", "key10713");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 5124: delete_range
  status = db->DeleteRange(write_options, "key0154", "key5010");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 5125: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 5126: get
  status = db->Get(read_options, "key1778", &value);
  if (status.ok()) {
    out << "GET key1778 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5127: delete_range
  status = db->DeleteRange(write_options, "key3123", "key5988");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 5128: get
  status = db->Get(read_options, "key2331", &value);
  if (status.ok()) {
    out << "GET key2331 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5129: get
  status = db->Get(read_options, "key7911", &value);
  if (status.ok()) {
    out << "GET key7911 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5130: delete
  db->Delete(write_options, "key6832");

  // op 5131: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 5132: put
  db->Put(write_options, "key8250", "value30876");

  // op 5133: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1290", "key3761", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1290, key3761) size=" << scan_result.size() << std::endl;
  }

  // op 5134: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2524", "key6120", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2524, key6120) size=" << scan_result.size() << std::endl;
  }

  // op 5135: put
  db->Put(write_options, "key10310", "value98336");

  // op 5136: put
  db->Put(write_options, "key8799", "value109148");

  // op 5137: put
  db->Put(write_options, "key6028", "value52439");

  // op 5138: put
  db->Put(write_options, "key2582", "value100253");

  // op 5139: put
  db->Put(write_options, "key6447", "value113042");

  // op 5140: get
  status = db->Get(read_options, "key5529", &value);
  if (status.ok()) {
    out << "GET key5529 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5141: put
  db->Put(write_options, "key5040", "value63382");

  // op 5142: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0496", "key11291", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0496, key11291) size=" << scan_result.size() << std::endl;
  }

  // op 5143: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 5144: put
  db->Put(write_options, "key7577", "value70517");

  // op 5145: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8289", "key9721", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8289, key9721) size=" << scan_result.size() << std::endl;
  }

  // op 5146: get
  status = db->Get(read_options, "key3383", &value);
  if (status.ok()) {
    out << "GET key3383 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5147: get
  status = db->Get(read_options, "key0267", &value);
  if (status.ok()) {
    out << "GET key0267 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5148: delete_range
  status = db->DeleteRange(write_options, "key0427", "key5731");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 5149: delete_range
  status = db->DeleteRange(write_options, "key6515", "key8398");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 5150: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0367", "key5945", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0367, key5945) size=" << scan_result.size() << std::endl;
  }

  // op 5151: delete_range
  status = db->DeleteRange(write_options, "key0587", "key2235");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 5152: put
  db->Put(write_options, "key6760", "value30862");

  // op 5153: put
  db->Put(write_options, "key9990", "value34717");

  // op 5154: get
  status = db->Get(read_options, "key6225", &value);
  if (status.ok()) {
    out << "GET key6225 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5155: put
  db->Put(write_options, "key2862", "value103833");

  // op 5156: delete
  db->Delete(write_options, "key7648");

  // op 5157: put
  db->Put(write_options, "key2967", "value81549");

  // op 5158: put
  db->Put(write_options, "key1891", "value2472");

  // op 5159: put
  db->Put(write_options, "key0639", "value55843");

  // op 5160: put
  db->Put(write_options, "key3813", "value64827");

  // op 5161: delete
  db->Delete(write_options, "key11694");

  // op 5162: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4580", "key9494", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4580, key9494) size=" << scan_result.size() << std::endl;
  }

  // op 5163: put
  db->Put(write_options, "key1448", "value113094");

  // op 5164: get
  status = db->Get(read_options, "key5184", &value);
  if (status.ok()) {
    out << "GET key5184 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5165: put
  db->Put(write_options, "key1322", "value84100");

  // op 5166: delete_range
  status = db->DeleteRange(write_options, "key3405", "key8897");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 5167: put
  db->Put(write_options, "key3136", "value11627");

  // op 5168: get
  status = db->Get(read_options, "key7531", &value);
  if (status.ok()) {
    out << "GET key7531 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5169: put
  db->Put(write_options, "key1833", "value38778");

  // op 5170: get
  status = db->Get(read_options, "key3812", &value);
  if (status.ok()) {
    out << "GET key3812 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5171: put
  db->Put(write_options, "key5044", "value79432");

  // op 5172: put
  db->Put(write_options, "key3078", "value27763");

  // op 5173: get
  status = db->Get(read_options, "key3530", &value);
  if (status.ok()) {
    out << "GET key3530 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5174: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0199", "key3080", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0199, key3080) size=" << scan_result.size() << std::endl;
  }

  // op 5175: put
  db->Put(write_options, "key6752", "value81269");

  // op 5176: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1743", "key4203", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1743, key4203) size=" << scan_result.size() << std::endl;
  }

  // op 5177: put
  db->Put(write_options, "key6774", "value27065");

  // op 5178: delete
  db->Delete(write_options, "key9080");

  // op 5179: put
  db->Put(write_options, "key0117", "value33496");

  // op 5180: put
  db->Put(write_options, "key11604", "value2769");

  // op 5181: put
  db->Put(write_options, "key8385", "value69459");

  // op 5182: put
  db->Put(write_options, "key5978", "value109259");

  // op 5183: put
  db->Put(write_options, "key9472", "value116857");

  // op 5184: get
  status = db->Get(read_options, "key9034", &value);
  if (status.ok()) {
    out << "GET key9034 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5185: put
  db->Put(write_options, "key7788", "value2685");

  // op 5186: put
  db->Put(write_options, "key5750", "value35877");

  // op 5187: put
  db->Put(write_options, "key0566", "value88339");

  // op 5188: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7603", "key7684", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7603, key7684) size=" << scan_result.size() << std::endl;
  }

  // op 5189: delete
  db->Delete(write_options, "key3415");

  // op 5190: put
  db->Put(write_options, "key11396", "value80979");

  // op 5191: put
  db->Put(write_options, "key11580", "value77737");

  // op 5192: delete
  db->Delete(write_options, "key6533");

  // op 5193: delete_range
  status = db->DeleteRange(write_options, "key0756", "key8830");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 5194: put
  db->Put(write_options, "key7017", "value3034");

  // op 5195: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6644", "key7170", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6644, key7170) size=" << scan_result.size() << std::endl;
  }

  // op 5196: put
  db->Put(write_options, "key9582", "value51916");

  // op 5197: delete_range
  status = db->DeleteRange(write_options, "key1594", "key10605");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 5198: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4320", "key6527", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4320, key6527) size=" << scan_result.size() << std::endl;
  }

  // op 5199: delete
  db->Delete(write_options, "key9675");

  // op 5200: get
  status = db->Get(read_options, "key11545", &value);
  if (status.ok()) {
    out << "GET key11545 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5201: put
  db->Put(write_options, "key2828", "value4882");

  // op 5202: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 5203: put
  db->Put(write_options, "key9660", "value97984");

  // op 5204: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0520", "key1087", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0520, key1087) size=" << scan_result.size() << std::endl;
  }

  // op 5205: put
  db->Put(write_options, "key7274", "value99703");

  // op 5206: put
  db->Put(write_options, "key7602", "value54609");

  // op 5207: put
  db->Put(write_options, "key6475", "value90782");

  // op 5208: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 5209: delete
  db->Delete(write_options, "key4785");

  // op 5210: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0808", "key0913", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0808, key0913) size=" << scan_result.size() << std::endl;
  }

  // op 5211: put
  db->Put(write_options, "key6806", "value92907");

  // op 5212: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4912", "key10912", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4912, key10912) size=" << scan_result.size() << std::endl;
  }

  // op 5213: put
  db->Put(write_options, "key2005", "value63817");

  // op 5214: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4179", "key4371", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4179, key4371) size=" << scan_result.size() << std::endl;
  }

  // op 5215: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 5216: get
  status = db->Get(read_options, "key4874", &value);
  if (status.ok()) {
    out << "GET key4874 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5217: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 5218: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2148", "key8149", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2148, key8149) size=" << scan_result.size() << std::endl;
  }

  // op 5219: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2595", "key9363", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2595, key9363) size=" << scan_result.size() << std::endl;
  }

  // op 5220: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7920", "key9195", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7920, key9195) size=" << scan_result.size() << std::endl;
  }

  // op 5221: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2302", "key4329", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2302, key4329) size=" << scan_result.size() << std::endl;
  }

  // op 5222: put
  db->Put(write_options, "key0839", "value44847");

  // op 5223: put
  db->Put(write_options, "key3489", "value89222");

  // op 5224: get
  status = db->Get(read_options, "key0129", &value);
  if (status.ok()) {
    out << "GET key0129 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5225: put
  db->Put(write_options, "key3471", "value25606");

  // op 5226: put
  db->Put(write_options, "key1690", "value49872");

  // op 5227: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 5228: get
  status = db->Get(read_options, "key11154", &value);
  if (status.ok()) {
    out << "GET key11154 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5229: delete
  db->Delete(write_options, "key5489");

  // op 5230: put
  db->Put(write_options, "key3779", "value96808");

  // op 5231: delete
  db->Delete(write_options, "key10865");

  // op 5232: delete_range
  status = db->DeleteRange(write_options, "key2299", "key8831");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 5233: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 5234: put
  db->Put(write_options, "key8973", "value18875");

  // op 5235: delete_range
  status = db->DeleteRange(write_options, "key0498", "key9558");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 5236: delete
  db->Delete(write_options, "key3291");

  // op 5237: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3464", "key6914", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3464, key6914) size=" << scan_result.size() << std::endl;
  }

  // op 5238: delete_range
  status = db->DeleteRange(write_options, "key4999", "key9071");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 5239: get
  status = db->Get(read_options, "key2590", &value);
  if (status.ok()) {
    out << "GET key2590 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5240: put
  db->Put(write_options, "key9742", "value73408");

  // op 5241: put
  db->Put(write_options, "key6957", "value3320");

  // op 5242: get
  status = db->Get(read_options, "key8066", &value);
  if (status.ok()) {
    out << "GET key8066 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5243: delete_range
  status = db->DeleteRange(write_options, "key5861", "key8628");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 5244: put
  db->Put(write_options, "key5081", "value47046");

  // op 5245: delete_range
  status = db->DeleteRange(write_options, "key5390", "key5699");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 5246: delete_range
  status = db->DeleteRange(write_options, "key4994", "key11368");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 5247: put
  db->Put(write_options, "key3446", "value117803");

  // op 5248: get
  status = db->Get(read_options, "key5051", &value);
  if (status.ok()) {
    out << "GET key5051 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5249: put
  db->Put(write_options, "key0485", "value116287");

  // op 5250: put
  db->Put(write_options, "key7648", "value86116");

  // op 5251: put
  db->Put(write_options, "key1465", "value29028");

  // op 5252: delete_range
  status = db->DeleteRange(write_options, "key0544", "key8399");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 5253: get
  status = db->Get(read_options, "key6020", &value);
  if (status.ok()) {
    out << "GET key6020 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5254: put
  db->Put(write_options, "key5032", "value114253");

  // op 5255: put
  db->Put(write_options, "key11423", "value57412");

  // op 5256: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8354", "key10784", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8354, key10784) size=" << scan_result.size() << std::endl;
  }

  // op 5257: get
  status = db->Get(read_options, "key0134", &value);
  if (status.ok()) {
    out << "GET key0134 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5258: put
  db->Put(write_options, "key5307", "value89552");

  // op 5259: put
  db->Put(write_options, "key1131", "value68719");

  // op 5260: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 5261: put
  db->Put(write_options, "key1524", "value3942");

  // op 5262: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0362", "key6245", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0362, key6245) size=" << scan_result.size() << std::endl;
  }

  // op 5263: get
  status = db->Get(read_options, "key9813", &value);
  if (status.ok()) {
    out << "GET key9813 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5264: delete
  db->Delete(write_options, "key5878");

  // op 5265: put
  db->Put(write_options, "key2026", "value41827");

  // op 5266: put
  db->Put(write_options, "key0377", "value4253");

  // op 5267: put
  db->Put(write_options, "key1218", "value108140");

  // op 5268: put
  db->Put(write_options, "key7985", "value93114");

  // op 5269: put
  db->Put(write_options, "key10293", "value18598");

  // op 5270: put
  db->Put(write_options, "key5841", "value100761");

  // op 5271: delete_range
  status = db->DeleteRange(write_options, "key3828", "key11485");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 5272: put
  db->Put(write_options, "key7108", "value72469");

  // op 5273: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6340", "key9246", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6340, key9246) size=" << scan_result.size() << std::endl;
  }

  // op 5274: delete_range
  status = db->DeleteRange(write_options, "key8399", "key9934");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 5275: delete
  db->Delete(write_options, "key1114");

  // op 5276: delete_range
  status = db->DeleteRange(write_options, "key8656", "key11458");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 5277: put
  db->Put(write_options, "key10585", "value50503");

  // op 5278: put
  db->Put(write_options, "key8865", "value88035");

  // op 5279: put
  db->Put(write_options, "key6492", "value80365");

  // op 5280: delete_range
  status = db->DeleteRange(write_options, "key3025", "key9489");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 5281: get
  status = db->Get(read_options, "key4454", &value);
  if (status.ok()) {
    out << "GET key4454 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5282: put
  db->Put(write_options, "key10669", "value74206");

  // op 5283: get
  status = db->Get(read_options, "key5865", &value);
  if (status.ok()) {
    out << "GET key5865 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5284: put
  db->Put(write_options, "key11141", "value74469");

  // op 5285: put
  db->Put(write_options, "key4491", "value26717");

  // op 5286: get
  status = db->Get(read_options, "key9586", &value);
  if (status.ok()) {
    out << "GET key9586 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5287: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2280", "key4322", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2280, key4322) size=" << scan_result.size() << std::endl;
  }

  // op 5288: delete
  db->Delete(write_options, "key3925");

  // op 5289: get
  status = db->Get(read_options, "key10087", &value);
  if (status.ok()) {
    out << "GET key10087 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5290: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4297", "key5967", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4297, key5967) size=" << scan_result.size() << std::endl;
  }

  // op 5291: put
  db->Put(write_options, "key10740", "value116540");

  // op 5292: delete_range
  status = db->DeleteRange(write_options, "key6646", "key9152");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 5293: put
  db->Put(write_options, "key5240", "value92485");

  // op 5294: get
  status = db->Get(read_options, "key1796", &value);
  if (status.ok()) {
    out << "GET key1796 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5295: put
  db->Put(write_options, "key10093", "value78780");

  // op 5296: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 5297: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6252", "key10489", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6252, key10489) size=" << scan_result.size() << std::endl;
  }

  // op 5298: put
  db->Put(write_options, "key10841", "value14645");

  // op 5299: delete_range
  status = db->DeleteRange(write_options, "key7872", "key9673");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 5300: delete_range
  status = db->DeleteRange(write_options, "key0853", "key5178");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 5301: put
  db->Put(write_options, "key7880", "value55261");

  // op 5302: put
  db->Put(write_options, "key1120", "value18152");

  // op 5303: put
  db->Put(write_options, "key2718", "value84083");

  // op 5304: put
  db->Put(write_options, "key0438", "value1232");

  // op 5305: put
  db->Put(write_options, "key11295", "value37326");

  // op 5306: get
  status = db->Get(read_options, "key9956", &value);
  if (status.ok()) {
    out << "GET key9956 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5307: put
  db->Put(write_options, "key5257", "value58911");

  // op 5308: delete_range
  status = db->DeleteRange(write_options, "key4767", "key11204");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 5309: delete_range
  status = db->DeleteRange(write_options, "key0193", "key8048");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 5310: put
  db->Put(write_options, "key4758", "value112285");

  // op 5311: get
  status = db->Get(read_options, "key3567", &value);
  if (status.ok()) {
    out << "GET key3567 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5312: put
  db->Put(write_options, "key9917", "value47394");

  // op 5313: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 5314: delete
  db->Delete(write_options, "key8608");

  // op 5315: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2855", "key5433", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2855, key5433) size=" << scan_result.size() << std::endl;
  }

  // op 5316: put
  db->Put(write_options, "key9143", "value96722");

  // op 5317: put
  db->Put(write_options, "key0700", "value1008");

  // op 5318: put
  db->Put(write_options, "key3730", "value51632");

  // op 5319: delete
  db->Delete(write_options, "key11551");

  // op 5320: put
  db->Put(write_options, "key2596", "value113431");

  // op 5321: put
  db->Put(write_options, "key9414", "value68483");

  // op 5322: put
  db->Put(write_options, "key5496", "value112375");

  // op 5323: delete
  db->Delete(write_options, "key11275");

  // op 5324: put
  db->Put(write_options, "key2534", "value9276");

  // op 5325: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10291", "key10339", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10291, key10339) size=" << scan_result.size() << std::endl;
  }

  // op 5326: put
  db->Put(write_options, "key8025", "value29712");

  // op 5327: put
  db->Put(write_options, "key7323", "value69253");

  // op 5328: delete
  db->Delete(write_options, "key0037");

  // op 5329: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 5330: get
  status = db->Get(read_options, "key3724", &value);
  if (status.ok()) {
    out << "GET key3724 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5331: delete_range
  status = db->DeleteRange(write_options, "key7270", "key8203");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 5332: get
  status = db->Get(read_options, "key1364", &value);
  if (status.ok()) {
    out << "GET key1364 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5333: put
  db->Put(write_options, "key4888", "value116826");

  // op 5334: delete
  db->Delete(write_options, "key9465");

  // op 5335: put
  db->Put(write_options, "key6162", "value54437");

  // op 5336: delete_range
  status = db->DeleteRange(write_options, "key0977", "key3548");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 5337: delete
  db->Delete(write_options, "key1487");

  // op 5338: put
  db->Put(write_options, "key3887", "value87254");

  // op 5339: delete_range
  status = db->DeleteRange(write_options, "key2047", "key4197");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 5340: delete_range
  status = db->DeleteRange(write_options, "key2165", "key2444");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 5341: put
  db->Put(write_options, "key0506", "value54485");

  // op 5342: put
  db->Put(write_options, "key2102", "value35512");

  // op 5343: put
  db->Put(write_options, "key4206", "value35369");

  // op 5344: put
  db->Put(write_options, "key1211", "value13759");

  // op 5345: put
  db->Put(write_options, "key6820", "value50589");

  // op 5346: get
  status = db->Get(read_options, "key8490", &value);
  if (status.ok()) {
    out << "GET key8490 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5347: put
  db->Put(write_options, "key7583", "value98719");

  // op 5348: delete_range
  status = db->DeleteRange(write_options, "key1073", "key9694");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 5349: delete
  db->Delete(write_options, "key5894");

  // op 5350: put
  db->Put(write_options, "key4775", "value32282");

  // op 5351: put
  db->Put(write_options, "key3623", "value8215");

  // op 5352: put
  db->Put(write_options, "key1708", "value29251");

  // op 5353: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1474", "key3288", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1474, key3288) size=" << scan_result.size() << std::endl;
  }

  // op 5354: put
  db->Put(write_options, "key2896", "value47897");

  // op 5355: put
  db->Put(write_options, "key3832", "value40033");

  // op 5356: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8807", "key9848", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8807, key9848) size=" << scan_result.size() << std::endl;
  }

  // op 5357: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3949", "key10091", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3949, key10091) size=" << scan_result.size() << std::endl;
  }

  // op 5358: put
  db->Put(write_options, "key10016", "value90075");

  // op 5359: put
  db->Put(write_options, "key6241", "value30094");

  // op 5360: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1128", "key9254", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1128, key9254) size=" << scan_result.size() << std::endl;
  }

  // op 5361: get
  status = db->Get(read_options, "key10869", &value);
  if (status.ok()) {
    out << "GET key10869 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5362: delete_range
  status = db->DeleteRange(write_options, "key10422", "key11581");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 5363: get
  status = db->Get(read_options, "key6925", &value);
  if (status.ok()) {
    out << "GET key6925 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5364: put
  db->Put(write_options, "key9464", "value95572");

  // op 5365: put
  db->Put(write_options, "key5266", "value37771");

  // op 5366: delete
  db->Delete(write_options, "key6796");

  // op 5367: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8019", "key8024", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8019, key8024) size=" << scan_result.size() << std::endl;
  }

  // op 5368: delete
  db->Delete(write_options, "key5735");

  // op 5369: delete
  db->Delete(write_options, "key4363");

  // op 5370: put
  db->Put(write_options, "key6909", "value74339");

  // op 5371: delete_range
  status = db->DeleteRange(write_options, "key2402", "key3586");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 5372: delete
  db->Delete(write_options, "key9763");

  // op 5373: put
  db->Put(write_options, "key1538", "value53578");

  // op 5374: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2920", "key5879", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2920, key5879) size=" << scan_result.size() << std::endl;
  }

  // op 5375: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 5376: delete
  db->Delete(write_options, "key5354");

  // op 5377: put
  db->Put(write_options, "key4832", "value71541");

  // op 5378: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1485", "key9794", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1485, key9794) size=" << scan_result.size() << std::endl;
  }

  // op 5379: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4655", "key7397", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4655, key7397) size=" << scan_result.size() << std::endl;
  }

  // op 5380: put
  db->Put(write_options, "key11140", "value73950");

  // op 5381: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 5382: delete
  db->Delete(write_options, "key1934");

  // op 5383: put
  db->Put(write_options, "key9388", "value116276");

  // op 5384: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1084", "key1911", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1084, key1911) size=" << scan_result.size() << std::endl;
  }

  // op 5385: scan
  scan_result.clear();
  status = db->Scan(read_options, "key11275", "key11395", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key11275, key11395) size=" << scan_result.size() << std::endl;
  }

  // op 5386: put
  db->Put(write_options, "key10064", "value51653");

  // op 5387: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0460", "key9603", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0460, key9603) size=" << scan_result.size() << std::endl;
  }

  // op 5388: delete
  db->Delete(write_options, "key8627");

  // op 5389: get
  status = db->Get(read_options, "key8974", &value);
  if (status.ok()) {
    out << "GET key8974 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5390: put
  db->Put(write_options, "key1294", "value106140");

  // op 5391: delete
  db->Delete(write_options, "key1363");

  // op 5392: delete_range
  status = db->DeleteRange(write_options, "key0627", "key1212");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 5393: put
  db->Put(write_options, "key5815", "value23711");

  // op 5394: put
  db->Put(write_options, "key11559", "value71949");

  // op 5395: put
  db->Put(write_options, "key3210", "value65601");

  // op 5396: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1814", "key4523", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1814, key4523) size=" << scan_result.size() << std::endl;
  }

  // op 5397: put
  db->Put(write_options, "key0481", "value91545");

  // op 5398: delete_range
  status = db->DeleteRange(write_options, "key0700", "key6106");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 5399: put
  db->Put(write_options, "key11348", "value73873");

  // op 5400: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 5401: put
  db->Put(write_options, "key7252", "value7519");

  // op 5402: put
  db->Put(write_options, "key3721", "value111030");

  // op 5403: put
  db->Put(write_options, "key7080", "value74800");

  // op 5404: delete_range
  status = db->DeleteRange(write_options, "key8982", "key9429");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 5405: put
  db->Put(write_options, "key11035", "value34664");

  // op 5406: get
  status = db->Get(read_options, "key9417", &value);
  if (status.ok()) {
    out << "GET key9417 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5407: get
  status = db->Get(read_options, "key10607", &value);
  if (status.ok()) {
    out << "GET key10607 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5408: get
  status = db->Get(read_options, "key5853", &value);
  if (status.ok()) {
    out << "GET key5853 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5409: delete_range
  status = db->DeleteRange(write_options, "key8507", "key11245");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 5410: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4527", "key8113", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4527, key8113) size=" << scan_result.size() << std::endl;
  }

  // op 5411: delete_range
  status = db->DeleteRange(write_options, "key6117", "key9389");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 5412: delete
  db->Delete(write_options, "key11270");

  // op 5413: put
  db->Put(write_options, "key10525", "value113034");

  // op 5414: delete
  db->Delete(write_options, "key8001");

  // op 5415: put
  db->Put(write_options, "key2484", "value51046");

  // op 5416: put
  db->Put(write_options, "key7897", "value42366");

  // op 5417: put
  db->Put(write_options, "key3415", "value107215");

  // op 5418: put
  db->Put(write_options, "key4807", "value6470");

  // op 5419: get
  status = db->Get(read_options, "key10688", &value);
  if (status.ok()) {
    out << "GET key10688 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5420: delete
  db->Delete(write_options, "key4033");

  // op 5421: get
  status = db->Get(read_options, "key11273", &value);
  if (status.ok()) {
    out << "GET key11273 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5422: put
  db->Put(write_options, "key0854", "value111202");

  // op 5423: get
  status = db->Get(read_options, "key7078", &value);
  if (status.ok()) {
    out << "GET key7078 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5424: put
  db->Put(write_options, "key11428", "value96432");

  // op 5425: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8139", "key10846", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8139, key10846) size=" << scan_result.size() << std::endl;
  }

  // op 5426: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0985", "key6828", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0985, key6828) size=" << scan_result.size() << std::endl;
  }

  // op 5427: delete_range
  status = db->DeleteRange(write_options, "key7324", "key10753");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 5428: get
  status = db->Get(read_options, "key0182", &value);
  if (status.ok()) {
    out << "GET key0182 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5429: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1779", "key2776", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1779, key2776) size=" << scan_result.size() << std::endl;
  }

  // op 5430: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 5431: put
  db->Put(write_options, "key9760", "value62908");

  // op 5432: put
  db->Put(write_options, "key0332", "value41862");

  // op 5433: get
  status = db->Get(read_options, "key1792", &value);
  if (status.ok()) {
    out << "GET key1792 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5434: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 5435: get
  status = db->Get(read_options, "key2611", &value);
  if (status.ok()) {
    out << "GET key2611 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5436: put
  db->Put(write_options, "key6880", "value58494");

  // op 5437: put
  db->Put(write_options, "key2576", "value60019");

  // op 5438: delete_range
  status = db->DeleteRange(write_options, "key3172", "key9104");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 5439: delete
  db->Delete(write_options, "key8183");

  // op 5440: delete_range
  status = db->DeleteRange(write_options, "key6693", "key8474");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 5441: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3003", "key9643", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3003, key9643) size=" << scan_result.size() << std::endl;
  }

  // op 5442: put
  db->Put(write_options, "key0525", "value0975");

  // op 5443: get
  status = db->Get(read_options, "key8202", &value);
  if (status.ok()) {
    out << "GET key8202 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5444: put
  db->Put(write_options, "key10263", "value32923");

  // op 5445: get
  status = db->Get(read_options, "key3145", &value);
  if (status.ok()) {
    out << "GET key3145 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5446: delete
  db->Delete(write_options, "key1441");

  // op 5447: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3920", "key8380", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3920, key8380) size=" << scan_result.size() << std::endl;
  }

  // op 5448: get
  status = db->Get(read_options, "key1208", &value);
  if (status.ok()) {
    out << "GET key1208 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5449: put
  db->Put(write_options, "key8026", "value75632");

  // op 5450: put
  db->Put(write_options, "key8379", "value67992");

  // op 5451: put
  db->Put(write_options, "key1070", "value21031");

  // op 5452: delete_range
  status = db->DeleteRange(write_options, "key1543", "key5401");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 5453: put
  db->Put(write_options, "key7835", "value49239");

  // op 5454: put
  db->Put(write_options, "key8581", "value7804");

  // op 5455: put
  db->Put(write_options, "key8521", "value8374");

  // op 5456: put
  db->Put(write_options, "key5546", "value117144");

  // op 5457: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5242", "key8477", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5242, key8477) size=" << scan_result.size() << std::endl;
  }

  // op 5458: put
  db->Put(write_options, "key1483", "value26414");

  // op 5459: put
  db->Put(write_options, "key8139", "value116170");

  // op 5460: get
  status = db->Get(read_options, "key2343", &value);
  if (status.ok()) {
    out << "GET key2343 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5461: delete_range
  status = db->DeleteRange(write_options, "key2761", "key9341");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 5462: put
  db->Put(write_options, "key1957", "value86102");

  // op 5463: delete
  db->Delete(write_options, "key7707");

  // op 5464: put
  db->Put(write_options, "key4458", "value45696");

  // op 5465: put
  db->Put(write_options, "key9769", "value108486");

  // op 5466: put
  db->Put(write_options, "key0047", "value118250");

  // op 5467: put
  db->Put(write_options, "key0892", "value10412");

  // op 5468: put
  db->Put(write_options, "key2748", "value71660");

  // op 5469: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0060", "key0191", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0060, key0191) size=" << scan_result.size() << std::endl;
  }

  // op 5470: delete
  db->Delete(write_options, "key4543");

  // op 5471: get
  status = db->Get(read_options, "key6419", &value);
  if (status.ok()) {
    out << "GET key6419 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5472: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 5473: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1044", "key6598", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1044, key6598) size=" << scan_result.size() << std::endl;
  }

  // op 5474: put
  db->Put(write_options, "key4038", "value14514");

  // op 5475: get
  status = db->Get(read_options, "key2292", &value);
  if (status.ok()) {
    out << "GET key2292 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5476: get
  status = db->Get(read_options, "key10131", &value);
  if (status.ok()) {
    out << "GET key10131 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5477: put
  db->Put(write_options, "key9286", "value108043");

  // op 5478: get
  status = db->Get(read_options, "key0042", &value);
  if (status.ok()) {
    out << "GET key0042 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5479: get
  status = db->Get(read_options, "key3491", &value);
  if (status.ok()) {
    out << "GET key3491 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5480: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5039", "key10482", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5039, key10482) size=" << scan_result.size() << std::endl;
  }

  // op 5481: put
  db->Put(write_options, "key3857", "value87792");

  // op 5482: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1213", "key7659", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1213, key7659) size=" << scan_result.size() << std::endl;
  }

  // op 5483: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3581", "key8751", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3581, key8751) size=" << scan_result.size() << std::endl;
  }

  // op 5484: get
  status = db->Get(read_options, "key2835", &value);
  if (status.ok()) {
    out << "GET key2835 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5485: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3460", "key5032", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3460, key5032) size=" << scan_result.size() << std::endl;
  }

  // op 5486: put
  db->Put(write_options, "key6812", "value42260");

  // op 5487: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3502", "key6135", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3502, key6135) size=" << scan_result.size() << std::endl;
  }

  // op 5488: put
  db->Put(write_options, "key7131", "value7457");

  // op 5489: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3237", "key6407", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3237, key6407) size=" << scan_result.size() << std::endl;
  }

  // op 5490: delete
  db->Delete(write_options, "key7193");

  // op 5491: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 5492: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1289", "key3538", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1289, key3538) size=" << scan_result.size() << std::endl;
  }

  // op 5493: put
  db->Put(write_options, "key9287", "value71756");

  // op 5494: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3857", "key11193", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3857, key11193) size=" << scan_result.size() << std::endl;
  }

  // op 5495: put
  db->Put(write_options, "key6459", "value100453");

  // op 5496: put
  db->Put(write_options, "key6350", "value49843");

  // op 5497: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3138", "key7383", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3138, key7383) size=" << scan_result.size() << std::endl;
  }

  // op 5498: delete
  db->Delete(write_options, "key1794");

  // op 5499: put
  db->Put(write_options, "key5462", "value44780");

  // op 5500: put
  db->Put(write_options, "key6142", "value108854");

  // op 5501: put
  db->Put(write_options, "key4030", "value88438");

  // op 5502: delete_range
  status = db->DeleteRange(write_options, "key6377", "key10038");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 5503: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 5504: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0063", "key1717", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0063, key1717) size=" << scan_result.size() << std::endl;
  }

  // op 5505: put
  db->Put(write_options, "key10534", "value116404");

  // op 5506: get
  status = db->Get(read_options, "key1194", &value);
  if (status.ok()) {
    out << "GET key1194 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5507: put
  db->Put(write_options, "key9923", "value16288");

  // op 5508: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4717", "key5157", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4717, key5157) size=" << scan_result.size() << std::endl;
  }

  // op 5509: get
  status = db->Get(read_options, "key10940", &value);
  if (status.ok()) {
    out << "GET key10940 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5510: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 5511: get
  status = db->Get(read_options, "key7825", &value);
  if (status.ok()) {
    out << "GET key7825 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5512: put
  db->Put(write_options, "key5005", "value12501");

  // op 5513: get
  status = db->Get(read_options, "key6643", &value);
  if (status.ok()) {
    out << "GET key6643 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5514: put
  db->Put(write_options, "key6628", "value38098");

  // op 5515: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1682", "key10279", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1682, key10279) size=" << scan_result.size() << std::endl;
  }

  // op 5516: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1273", "key6101", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1273, key6101) size=" << scan_result.size() << std::endl;
  }

  // op 5517: delete
  db->Delete(write_options, "key2222");

  // op 5518: put
  db->Put(write_options, "key6544", "value54026");

  // op 5519: put
  db->Put(write_options, "key10356", "value14220");

  // op 5520: delete
  db->Delete(write_options, "key10970");

  // op 5521: put
  db->Put(write_options, "key1843", "value9002");

  // op 5522: get
  status = db->Get(read_options, "key6178", &value);
  if (status.ok()) {
    out << "GET key6178 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5523: delete
  db->Delete(write_options, "key11179");

  // op 5524: put
  db->Put(write_options, "key6240", "value4965");

  // op 5525: get
  status = db->Get(read_options, "key8326", &value);
  if (status.ok()) {
    out << "GET key8326 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5526: put
  db->Put(write_options, "key3898", "value85726");

  // op 5527: delete
  db->Delete(write_options, "key5692");

  // op 5528: put
  db->Put(write_options, "key1545", "value98234");

  // op 5529: delete
  db->Delete(write_options, "key0983");

  // op 5530: put
  db->Put(write_options, "key0488", "value12690");

  // op 5531: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 5532: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0982", "key4156", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0982, key4156) size=" << scan_result.size() << std::endl;
  }

  // op 5533: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 5534: delete_range
  status = db->DeleteRange(write_options, "key3881", "key7734");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 5535: put
  db->Put(write_options, "key2663", "value33674");

  // op 5536: delete
  db->Delete(write_options, "key0513");

  // op 5537: get
  status = db->Get(read_options, "key0082", &value);
  if (status.ok()) {
    out << "GET key0082 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5538: get
  status = db->Get(read_options, "key1908", &value);
  if (status.ok()) {
    out << "GET key1908 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5539: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0821", "key9718", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0821, key9718) size=" << scan_result.size() << std::endl;
  }

  // op 5540: put
  db->Put(write_options, "key4171", "value91309");

  // op 5541: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1317", "key11202", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1317, key11202) size=" << scan_result.size() << std::endl;
  }

  // op 5542: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2401", "key8960", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2401, key8960) size=" << scan_result.size() << std::endl;
  }

  // op 5543: put
  db->Put(write_options, "key2562", "value43619");

  // op 5544: put
  db->Put(write_options, "key3025", "value11676");

  // op 5545: delete
  db->Delete(write_options, "key10975");

  // op 5546: get
  status = db->Get(read_options, "key2558", &value);
  if (status.ok()) {
    out << "GET key2558 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5547: put
  db->Put(write_options, "key0205", "value45548");

  // op 5548: put
  db->Put(write_options, "key0249", "value114166");

  // op 5549: delete
  db->Delete(write_options, "key5143");

  // op 5550: put
  db->Put(write_options, "key11281", "value11274");

  // op 5551: put
  db->Put(write_options, "key1022", "value53299");

  // op 5552: delete
  db->Delete(write_options, "key0176");

  // op 5553: delete
  db->Delete(write_options, "key7843");

  // op 5554: get
  status = db->Get(read_options, "key2155", &value);
  if (status.ok()) {
    out << "GET key2155 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5555: put
  db->Put(write_options, "key1215", "value78346");

  // op 5556: get
  status = db->Get(read_options, "key5300", &value);
  if (status.ok()) {
    out << "GET key5300 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5557: delete_range
  status = db->DeleteRange(write_options, "key0464", "key5554");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 5558: put
  db->Put(write_options, "key5477", "value7663");

  // op 5559: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2246", "key3706", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2246, key3706) size=" << scan_result.size() << std::endl;
  }

  // op 5560: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2278", "key3006", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2278, key3006) size=" << scan_result.size() << std::endl;
  }

  // op 5561: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0375", "key1189", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0375, key1189) size=" << scan_result.size() << std::endl;
  }

  // op 5562: delete
  db->Delete(write_options, "key5885");

  // op 5563: get
  status = db->Get(read_options, "key4547", &value);
  if (status.ok()) {
    out << "GET key4547 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5564: delete_range
  status = db->DeleteRange(write_options, "key5954", "key9590");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 5565: delete_range
  status = db->DeleteRange(write_options, "key4696", "key10348");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 5566: put
  db->Put(write_options, "key5636", "value9742");

  // op 5567: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0353", "key2027", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0353, key2027) size=" << scan_result.size() << std::endl;
  }

  // op 5568: delete
  db->Delete(write_options, "key8255");

  // op 5569: delete
  db->Delete(write_options, "key4323");

  // op 5570: put
  db->Put(write_options, "key7963", "value55222");

  // op 5571: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4396", "key6230", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4396, key6230) size=" << scan_result.size() << std::endl;
  }

  // op 5572: put
  db->Put(write_options, "key6148", "value86915");

  // op 5573: put
  db->Put(write_options, "key5660", "value53964");

  // op 5574: delete
  db->Delete(write_options, "key6924");

  // op 5575: put
  db->Put(write_options, "key8635", "value18904");

  // op 5576: get
  status = db->Get(read_options, "key5967", &value);
  if (status.ok()) {
    out << "GET key5967 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5577: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1833", "key9089", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1833, key9089) size=" << scan_result.size() << std::endl;
  }

  // op 5578: get
  status = db->Get(read_options, "key10926", &value);
  if (status.ok()) {
    out << "GET key10926 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5579: put
  db->Put(write_options, "key4737", "value35169");

  // op 5580: get
  status = db->Get(read_options, "key4576", &value);
  if (status.ok()) {
    out << "GET key4576 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5581: put
  db->Put(write_options, "key4418", "value102884");

  // op 5582: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2615", "key8654", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2615, key8654) size=" << scan_result.size() << std::endl;
  }

  // op 5583: put
  db->Put(write_options, "key5607", "value44299");

  // op 5584: get
  status = db->Get(read_options, "key10254", &value);
  if (status.ok()) {
    out << "GET key10254 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5585: put
  db->Put(write_options, "key11240", "value13624");

  // op 5586: delete_range
  status = db->DeleteRange(write_options, "key5426", "key9297");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 5587: delete
  db->Delete(write_options, "key4012");

  // op 5588: delete
  db->Delete(write_options, "key6624");

  // op 5589: put
  db->Put(write_options, "key1951", "value55313");

  // op 5590: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3303", "key5126", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3303, key5126) size=" << scan_result.size() << std::endl;
  }

  // op 5591: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6366", "key7098", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6366, key7098) size=" << scan_result.size() << std::endl;
  }

  // op 5592: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2288", "key8489", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2288, key8489) size=" << scan_result.size() << std::endl;
  }

  // op 5593: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 5594: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3075", "key9874", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3075, key9874) size=" << scan_result.size() << std::endl;
  }

  // op 5595: get
  status = db->Get(read_options, "key0171", &value);
  if (status.ok()) {
    out << "GET key0171 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5596: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 5597: put
  db->Put(write_options, "key5965", "value117831");

  // op 5598: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 5599: get
  status = db->Get(read_options, "key10280", &value);
  if (status.ok()) {
    out << "GET key10280 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5600: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2361", "key6349", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2361, key6349) size=" << scan_result.size() << std::endl;
  }

  // op 5601: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7733", "key9525", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7733, key9525) size=" << scan_result.size() << std::endl;
  }

  // op 5602: get
  status = db->Get(read_options, "key1877", &value);
  if (status.ok()) {
    out << "GET key1877 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5603: get
  status = db->Get(read_options, "key4011", &value);
  if (status.ok()) {
    out << "GET key4011 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5604: get
  status = db->Get(read_options, "key6130", &value);
  if (status.ok()) {
    out << "GET key6130 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5605: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0080", "key8141", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0080, key8141) size=" << scan_result.size() << std::endl;
  }

  // op 5606: put
  db->Put(write_options, "key11518", "value100264");

  // op 5607: put
  db->Put(write_options, "key8191", "value33259");

  // op 5608: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7311", "key7819", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7311, key7819) size=" << scan_result.size() << std::endl;
  }

  // op 5609: get
  status = db->Get(read_options, "key10724", &value);
  if (status.ok()) {
    out << "GET key10724 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5610: put
  db->Put(write_options, "key0973", "value55990");

  // op 5611: delete_range
  status = db->DeleteRange(write_options, "key5991", "key10190");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 5612: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4477", "key8244", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4477, key8244) size=" << scan_result.size() << std::endl;
  }

  // op 5613: delete_range
  status = db->DeleteRange(write_options, "key2307", "key7835");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 5614: put
  db->Put(write_options, "key1521", "value72742");

  // op 5615: get
  status = db->Get(read_options, "key4203", &value);
  if (status.ok()) {
    out << "GET key4203 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5616: delete
  db->Delete(write_options, "key5461");

  // op 5617: delete
  db->Delete(write_options, "key2417");

  // op 5618: put
  db->Put(write_options, "key6076", "value8969");

  // op 5619: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2253", "key7145", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2253, key7145) size=" << scan_result.size() << std::endl;
  }

  // op 5620: put
  db->Put(write_options, "key9817", "value88196");

  // op 5621: put
  db->Put(write_options, "key4536", "value83366");

  // op 5622: put
  db->Put(write_options, "key0536", "value43391");

  // op 5623: get
  status = db->Get(read_options, "key10894", &value);
  if (status.ok()) {
    out << "GET key10894 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5624: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0354", "key2865", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0354, key2865) size=" << scan_result.size() << std::endl;
  }

  // op 5625: delete_range
  status = db->DeleteRange(write_options, "key3366", "key7639");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 5626: delete
  db->Delete(write_options, "key7839");

  // op 5627: put
  db->Put(write_options, "key6844", "value116934");

  // op 5628: get
  status = db->Get(read_options, "key8555", &value);
  if (status.ok()) {
    out << "GET key8555 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5629: put
  db->Put(write_options, "key3954", "value39967");

  // op 5630: delete
  db->Delete(write_options, "key1660");

  // op 5631: get
  status = db->Get(read_options, "key8968", &value);
  if (status.ok()) {
    out << "GET key8968 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5632: put
  db->Put(write_options, "key2545", "value103283");

  // op 5633: delete_range
  status = db->DeleteRange(write_options, "key2507", "key6823");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 5634: put
  db->Put(write_options, "key0999", "value105510");

  // op 5635: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2521", "key5234", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2521, key5234) size=" << scan_result.size() << std::endl;
  }

  // op 5636: get
  status = db->Get(read_options, "key5510", &value);
  if (status.ok()) {
    out << "GET key5510 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5637: put
  db->Put(write_options, "key2106", "value18260");

  // op 5638: put
  db->Put(write_options, "key11310", "value12562");

  // op 5639: get
  status = db->Get(read_options, "key11353", &value);
  if (status.ok()) {
    out << "GET key11353 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5640: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1374", "key1744", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1374, key1744) size=" << scan_result.size() << std::endl;
  }

  // op 5641: put
  db->Put(write_options, "key3836", "value91866");

  // op 5642: put
  db->Put(write_options, "key9507", "value84201");

  // op 5643: put
  db->Put(write_options, "key7107", "value110056");

  // op 5644: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1107", "key8123", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1107, key8123) size=" << scan_result.size() << std::endl;
  }

  // op 5645: get
  status = db->Get(read_options, "key0379", &value);
  if (status.ok()) {
    out << "GET key0379 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5646: delete
  db->Delete(write_options, "key10326");

  // op 5647: put
  db->Put(write_options, "key1285", "value8915");

  // op 5648: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 5649: get
  status = db->Get(read_options, "key9738", &value);
  if (status.ok()) {
    out << "GET key9738 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5650: put
  db->Put(write_options, "key10989", "value14710");

  // op 5651: put
  db->Put(write_options, "key2864", "value20072");

  // op 5652: get
  status = db->Get(read_options, "key0930", &value);
  if (status.ok()) {
    out << "GET key0930 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5653: put
  db->Put(write_options, "key3348", "value68178");

  // op 5654: delete
  db->Delete(write_options, "key10881");

  // op 5655: get
  status = db->Get(read_options, "key8051", &value);
  if (status.ok()) {
    out << "GET key8051 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5656: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3703", "key8891", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3703, key8891) size=" << scan_result.size() << std::endl;
  }

  // op 5657: delete
  db->Delete(write_options, "key8537");

  // op 5658: put
  db->Put(write_options, "key3006", "value75690");

  // op 5659: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1571", "key6121", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1571, key6121) size=" << scan_result.size() << std::endl;
  }

  // op 5660: put
  db->Put(write_options, "key8502", "value113676");

  // op 5661: get
  status = db->Get(read_options, "key3478", &value);
  if (status.ok()) {
    out << "GET key3478 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5662: put
  db->Put(write_options, "key6010", "value45938");

  // op 5663: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4850", "key11380", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4850, key11380) size=" << scan_result.size() << std::endl;
  }

  // op 5664: delete_range
  status = db->DeleteRange(write_options, "key5368", "key8177");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 5665: put
  db->Put(write_options, "key4091", "value14251");

  // op 5666: put
  db->Put(write_options, "key4729", "value60938");

  // op 5667: delete
  db->Delete(write_options, "key2701");

  // op 5668: put
  db->Put(write_options, "key8792", "value109469");

  // op 5669: delete_range
  status = db->DeleteRange(write_options, "key0448", "key2412");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 5670: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5777", "key6076", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5777, key6076) size=" << scan_result.size() << std::endl;
  }

  // op 5671: delete_range
  status = db->DeleteRange(write_options, "key6501", "key11100");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 5672: delete
  db->Delete(write_options, "key0007");

  // op 5673: get
  status = db->Get(read_options, "key2233", &value);
  if (status.ok()) {
    out << "GET key2233 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5674: put
  db->Put(write_options, "key0619", "value12108");

  // op 5675: put
  db->Put(write_options, "key5848", "value82004");

  // op 5676: put
  db->Put(write_options, "key7427", "value117501");

  // op 5677: put
  db->Put(write_options, "key8657", "value9732");

  // op 5678: get
  status = db->Get(read_options, "key2992", &value);
  if (status.ok()) {
    out << "GET key2992 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5679: put
  db->Put(write_options, "key5478", "value89370");

  // op 5680: put
  db->Put(write_options, "key10999", "value84094");

  // op 5681: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4296", "key8301", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4296, key8301) size=" << scan_result.size() << std::endl;
  }

  // op 5682: get
  status = db->Get(read_options, "key5067", &value);
  if (status.ok()) {
    out << "GET key5067 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5683: put
  db->Put(write_options, "key10494", "value109401");

  // op 5684: put
  db->Put(write_options, "key0390", "value109836");

  // op 5685: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4680", "key10977", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4680, key10977) size=" << scan_result.size() << std::endl;
  }

  // op 5686: delete_range
  status = db->DeleteRange(write_options, "key2883", "key5426");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 5687: get
  status = db->Get(read_options, "key9440", &value);
  if (status.ok()) {
    out << "GET key9440 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5688: get
  status = db->Get(read_options, "key6061", &value);
  if (status.ok()) {
    out << "GET key6061 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5689: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6496", "key9675", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6496, key9675) size=" << scan_result.size() << std::endl;
  }

  // op 5690: put
  db->Put(write_options, "key8587", "value0454");

  // op 5691: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5235", "key8693", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5235, key8693) size=" << scan_result.size() << std::endl;
  }

  // op 5692: put
  db->Put(write_options, "key3070", "value40222");

  // op 5693: delete
  db->Delete(write_options, "key0226");

  // op 5694: put
  db->Put(write_options, "key5111", "value1843");

  // op 5695: put
  db->Put(write_options, "key2427", "value24680");

  // op 5696: put
  db->Put(write_options, "key9925", "value23587");

  // op 5697: put
  db->Put(write_options, "key1399", "value88744");

  // op 5698: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4659", "key5489", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4659, key5489) size=" << scan_result.size() << std::endl;
  }

  // op 5699: put
  db->Put(write_options, "key7517", "value19418");

  // op 5700: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 5701: put
  db->Put(write_options, "key5443", "value28893");

  // op 5702: delete_range
  status = db->DeleteRange(write_options, "key0746", "key9584");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 5703: put
  db->Put(write_options, "key5637", "value7173");

  // op 5704: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1226", "key11000", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1226, key11000) size=" << scan_result.size() << std::endl;
  }

  // op 5705: get
  status = db->Get(read_options, "key11213", &value);
  if (status.ok()) {
    out << "GET key11213 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5706: put
  db->Put(write_options, "key2067", "value73270");

  // op 5707: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4890", "key8073", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4890, key8073) size=" << scan_result.size() << std::endl;
  }

  // op 5708: get
  status = db->Get(read_options, "key10835", &value);
  if (status.ok()) {
    out << "GET key10835 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5709: get
  status = db->Get(read_options, "key8265", &value);
  if (status.ok()) {
    out << "GET key8265 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5710: get
  status = db->Get(read_options, "key6272", &value);
  if (status.ok()) {
    out << "GET key6272 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5711: delete
  db->Delete(write_options, "key6161");

  // op 5712: put
  db->Put(write_options, "key9153", "value15513");

  // op 5713: delete
  db->Delete(write_options, "key6362");

  // op 5714: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 5715: delete_range
  status = db->DeleteRange(write_options, "key0948", "key11280");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 5716: put
  db->Put(write_options, "key5254", "value22160");

  // op 5717: delete
  db->Delete(write_options, "key4107");

  // op 5718: get
  status = db->Get(read_options, "key0866", &value);
  if (status.ok()) {
    out << "GET key0866 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5719: put
  db->Put(write_options, "key5693", "value10083");

  // op 5720: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1774", "key7116", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1774, key7116) size=" << scan_result.size() << std::endl;
  }

  // op 5721: put
  db->Put(write_options, "key8152", "value97383");

  // op 5722: delete_range
  status = db->DeleteRange(write_options, "key4926", "key8752");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 5723: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6150", "key6311", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6150, key6311) size=" << scan_result.size() << std::endl;
  }

  // op 5724: get
  status = db->Get(read_options, "key4613", &value);
  if (status.ok()) {
    out << "GET key4613 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5725: delete_range
  status = db->DeleteRange(write_options, "key2626", "key10131");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 5726: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4471", "key8449", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4471, key8449) size=" << scan_result.size() << std::endl;
  }

  // op 5727: delete_range
  status = db->DeleteRange(write_options, "key3865", "key11252");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 5728: get
  status = db->Get(read_options, "key7886", &value);
  if (status.ok()) {
    out << "GET key7886 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5729: put
  db->Put(write_options, "key6054", "value62889");

  // op 5730: put
  db->Put(write_options, "key10277", "value31391");

  // op 5731: put
  db->Put(write_options, "key4650", "value39024");

  // op 5732: put
  db->Put(write_options, "key7750", "value96374");

  // op 5733: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5092", "key10763", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5092, key10763) size=" << scan_result.size() << std::endl;
  }

  // op 5734: put
  db->Put(write_options, "key1079", "value91607");

  // op 5735: put
  db->Put(write_options, "key3993", "value27096");

  // op 5736: put
  db->Put(write_options, "key3263", "value50109");

  // op 5737: put
  db->Put(write_options, "key9341", "value2003");

  // op 5738: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 5739: delete_range
  status = db->DeleteRange(write_options, "key4978", "key8051");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 5740: get
  status = db->Get(read_options, "key9981", &value);
  if (status.ok()) {
    out << "GET key9981 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5741: put
  db->Put(write_options, "key9724", "value74871");

  // op 5742: put
  db->Put(write_options, "key9956", "value71732");

  // op 5743: delete_range
  status = db->DeleteRange(write_options, "key1589", "key4390");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 5744: put
  db->Put(write_options, "key6080", "value7232");

  // op 5745: delete
  db->Delete(write_options, "key8955");

  // op 5746: put
  db->Put(write_options, "key0027", "value11929");

  // op 5747: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3048", "key9807", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3048, key9807) size=" << scan_result.size() << std::endl;
  }

  // op 5748: delete_range
  status = db->DeleteRange(write_options, "key0731", "key5895");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 5749: delete_range
  status = db->DeleteRange(write_options, "key1087", "key5698");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 5750: put
  db->Put(write_options, "key5374", "value45396");

  // op 5751: get
  status = db->Get(read_options, "key0551", &value);
  if (status.ok()) {
    out << "GET key0551 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5752: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4618", "key11616", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4618, key11616) size=" << scan_result.size() << std::endl;
  }

  // op 5753: put
  db->Put(write_options, "key6889", "value88657");

  // op 5754: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0537", "key2854", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0537, key2854) size=" << scan_result.size() << std::endl;
  }

  // op 5755: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7189", "key8960", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7189, key8960) size=" << scan_result.size() << std::endl;
  }

  // op 5756: put
  db->Put(write_options, "key7028", "value75934");

  // op 5757: get
  status = db->Get(read_options, "key10043", &value);
  if (status.ok()) {
    out << "GET key10043 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5758: put
  db->Put(write_options, "key10102", "value5058");

  // op 5759: delete
  db->Delete(write_options, "key2986");

  // op 5760: get
  status = db->Get(read_options, "key0990", &value);
  if (status.ok()) {
    out << "GET key0990 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5761: get
  status = db->Get(read_options, "key6998", &value);
  if (status.ok()) {
    out << "GET key6998 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5762: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 5763: get
  status = db->Get(read_options, "key7038", &value);
  if (status.ok()) {
    out << "GET key7038 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5764: delete_range
  status = db->DeleteRange(write_options, "key8208", "key8398");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 5765: get
  status = db->Get(read_options, "key1663", &value);
  if (status.ok()) {
    out << "GET key1663 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5766: put
  db->Put(write_options, "key5041", "value74214");

  // op 5767: put
  db->Put(write_options, "key10504", "value20563");

  // op 5768: delete
  db->Delete(write_options, "key0280");

  // op 5769: get
  status = db->Get(read_options, "key4999", &value);
  if (status.ok()) {
    out << "GET key4999 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5770: delete_range
  status = db->DeleteRange(write_options, "key3796", "key8471");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 5771: put
  db->Put(write_options, "key0738", "value99226");

  // op 5772: get
  status = db->Get(read_options, "key3542", &value);
  if (status.ok()) {
    out << "GET key3542 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5773: put
  db->Put(write_options, "key6391", "value79113");

  // op 5774: delete
  db->Delete(write_options, "key6103");

  // op 5775: delete_range
  status = db->DeleteRange(write_options, "key1997", "key3893");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 5776: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2423", "key8488", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2423, key8488) size=" << scan_result.size() << std::endl;
  }

  // op 5777: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1708", "key10539", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1708, key10539) size=" << scan_result.size() << std::endl;
  }

  // op 5778: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 5779: delete
  db->Delete(write_options, "key2551");

  // op 5780: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0654", "key3272", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0654, key3272) size=" << scan_result.size() << std::endl;
  }

  // op 5781: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 5782: get
  status = db->Get(read_options, "key6036", &value);
  if (status.ok()) {
    out << "GET key6036 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5783: put
  db->Put(write_options, "key2434", "value109469");

  // op 5784: put
  db->Put(write_options, "key5278", "value98441");

  // op 5785: get
  status = db->Get(read_options, "key10948", &value);
  if (status.ok()) {
    out << "GET key10948 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5786: delete_range
  status = db->DeleteRange(write_options, "key1555", "key4054");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 5787: delete
  db->Delete(write_options, "key10910");

  // op 5788: get
  status = db->Get(read_options, "key5129", &value);
  if (status.ok()) {
    out << "GET key5129 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5789: delete
  db->Delete(write_options, "key11393");

  // op 5790: put
  db->Put(write_options, "key3302", "value102265");

  // op 5791: put
  db->Put(write_options, "key9230", "value10614");

  // op 5792: delete_range
  status = db->DeleteRange(write_options, "key1027", "key7862");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 5793: delete_range
  status = db->DeleteRange(write_options, "key0205", "key10711");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 5794: get
  status = db->Get(read_options, "key6920", &value);
  if (status.ok()) {
    out << "GET key6920 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5795: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0905", "key7630", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0905, key7630) size=" << scan_result.size() << std::endl;
  }

  // op 5796: put
  db->Put(write_options, "key6654", "value25376");

  // op 5797: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1243", "key2931", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1243, key2931) size=" << scan_result.size() << std::endl;
  }

  // op 5798: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5174", "key7018", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5174, key7018) size=" << scan_result.size() << std::endl;
  }

  // op 5799: put
  db->Put(write_options, "key1277", "value84854");

  // op 5800: get
  status = db->Get(read_options, "key2502", &value);
  if (status.ok()) {
    out << "GET key2502 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5801: put
  db->Put(write_options, "key7463", "value113116");

  // op 5802: put
  db->Put(write_options, "key6566", "value62092");

  // op 5803: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 5804: delete
  db->Delete(write_options, "key11304");

  // op 5805: delete
  db->Delete(write_options, "key5704");

  // op 5806: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8152", "key9325", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8152, key9325) size=" << scan_result.size() << std::endl;
  }

  // op 5807: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3371", "key5350", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3371, key5350) size=" << scan_result.size() << std::endl;
  }

  // op 5808: put
  db->Put(write_options, "key0024", "value82064");

  // op 5809: delete_range
  status = db->DeleteRange(write_options, "key0786", "key7224");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 5810: get
  status = db->Get(read_options, "key11314", &value);
  if (status.ok()) {
    out << "GET key11314 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5811: put
  db->Put(write_options, "key6658", "value49520");

  // op 5812: delete_range
  status = db->DeleteRange(write_options, "key4514", "key8587");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 5813: put
  db->Put(write_options, "key3182", "value24299");

  // op 5814: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1004", "key6452", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1004, key6452) size=" << scan_result.size() << std::endl;
  }

  // op 5815: delete_range
  status = db->DeleteRange(write_options, "key1328", "key1822");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 5816: put
  db->Put(write_options, "key9744", "value57673");

  // op 5817: put
  db->Put(write_options, "key4895", "value2308");

  // op 5818: put
  db->Put(write_options, "key8147", "value70070");

  // op 5819: put
  db->Put(write_options, "key4698", "value7281");

  // op 5820: get
  status = db->Get(read_options, "key4757", &value);
  if (status.ok()) {
    out << "GET key4757 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5821: put
  db->Put(write_options, "key7661", "value79674");

  // op 5822: put
  db->Put(write_options, "key3043", "value4702");

  // op 5823: put
  db->Put(write_options, "key5141", "value72679");

  // op 5824: delete
  db->Delete(write_options, "key6576");

  // op 5825: put
  db->Put(write_options, "key11488", "value116276");

  // op 5826: put
  db->Put(write_options, "key11512", "value89259");

  // op 5827: put
  db->Put(write_options, "key6469", "value64758");

  // op 5828: put
  db->Put(write_options, "key4618", "value59591");

  // op 5829: get
  status = db->Get(read_options, "key1655", &value);
  if (status.ok()) {
    out << "GET key1655 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5830: put
  db->Put(write_options, "key2290", "value107992");

  // op 5831: put
  db->Put(write_options, "key5178", "value45245");

  // op 5832: put
  db->Put(write_options, "key1841", "value96164");

  // op 5833: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3195", "key3254", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3195, key3254) size=" << scan_result.size() << std::endl;
  }

  // op 5834: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1338", "key2840", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1338, key2840) size=" << scan_result.size() << std::endl;
  }

  // op 5835: put
  db->Put(write_options, "key5967", "value33510");

  // op 5836: put
  db->Put(write_options, "key8284", "value18866");

  // op 5837: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1074", "key9603", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1074, key9603) size=" << scan_result.size() << std::endl;
  }

  // op 5838: put
  db->Put(write_options, "key3668", "value70135");

  // op 5839: put
  db->Put(write_options, "key2303", "value117512");

  // op 5840: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2264", "key7954", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2264, key7954) size=" << scan_result.size() << std::endl;
  }

  // op 5841: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 5842: get
  status = db->Get(read_options, "key8351", &value);
  if (status.ok()) {
    out << "GET key8351 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5843: put
  db->Put(write_options, "key4052", "value70609");

  // op 5844: put
  db->Put(write_options, "key11018", "value27918");

  // op 5845: put
  db->Put(write_options, "key1897", "value52968");

  // op 5846: get
  status = db->Get(read_options, "key8088", &value);
  if (status.ok()) {
    out << "GET key8088 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5847: get
  status = db->Get(read_options, "key0233", &value);
  if (status.ok()) {
    out << "GET key0233 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5848: put
  db->Put(write_options, "key0565", "value27483");

  // op 5849: get
  status = db->Get(read_options, "key6005", &value);
  if (status.ok()) {
    out << "GET key6005 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5850: put
  db->Put(write_options, "key0650", "value68619");

  // op 5851: get
  status = db->Get(read_options, "key9101", &value);
  if (status.ok()) {
    out << "GET key9101 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5852: put
  db->Put(write_options, "key3000", "value70470");

  // op 5853: get
  status = db->Get(read_options, "key1796", &value);
  if (status.ok()) {
    out << "GET key1796 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5854: put
  db->Put(write_options, "key0086", "value14177");

  // op 5855: put
  db->Put(write_options, "key0421", "value5948");

  // op 5856: put
  db->Put(write_options, "key9597", "value93582");

  // op 5857: get
  status = db->Get(read_options, "key5050", &value);
  if (status.ok()) {
    out << "GET key5050 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5858: put
  db->Put(write_options, "key5285", "value89926");

  // op 5859: put
  db->Put(write_options, "key8277", "value77944");

  // op 5860: put
  db->Put(write_options, "key4612", "value57435");

  // op 5861: put
  db->Put(write_options, "key11493", "value0654");

  // op 5862: put
  db->Put(write_options, "key2157", "value11989");

  // op 5863: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3442", "key11052", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3442, key11052) size=" << scan_result.size() << std::endl;
  }

  // op 5864: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 5865: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 5866: put
  db->Put(write_options, "key10955", "value113476");

  // op 5867: put
  db->Put(write_options, "key8494", "value39923");

  // op 5868: get
  status = db->Get(read_options, "key5815", &value);
  if (status.ok()) {
    out << "GET key5815 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5869: put
  db->Put(write_options, "key9803", "value28044");

  // op 5870: put
  db->Put(write_options, "key3807", "value59732");

  // op 5871: get
  status = db->Get(read_options, "key11245", &value);
  if (status.ok()) {
    out << "GET key11245 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5872: put
  db->Put(write_options, "key6802", "value17551");

  // op 5873: get
  status = db->Get(read_options, "key11506", &value);
  if (status.ok()) {
    out << "GET key11506 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5874: delete
  db->Delete(write_options, "key7794");

  // op 5875: put
  db->Put(write_options, "key11349", "value57353");

  // op 5876: put
  db->Put(write_options, "key2167", "value20861");

  // op 5877: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6467", "key11607", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6467, key11607) size=" << scan_result.size() << std::endl;
  }

  // op 5878: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8213", "key11529", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8213, key11529) size=" << scan_result.size() << std::endl;
  }

  // op 5879: delete_range
  status = db->DeleteRange(write_options, "key7814", "key10181");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 5880: delete
  db->Delete(write_options, "key3750");

  // op 5881: put
  db->Put(write_options, "key11525", "value110298");

  // op 5882: put
  db->Put(write_options, "key3494", "value64780");

  // op 5883: delete
  db->Delete(write_options, "key6048");

  // op 5884: delete_range
  status = db->DeleteRange(write_options, "key6661", "key9271");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 5885: get
  status = db->Get(read_options, "key4052", &value);
  if (status.ok()) {
    out << "GET key4052 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5886: put
  db->Put(write_options, "key11153", "value11535");

  // op 5887: put
  db->Put(write_options, "key4856", "value12489");

  // op 5888: delete_range
  status = db->DeleteRange(write_options, "key4910", "key7132");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 5889: delete
  db->Delete(write_options, "key1484");

  // op 5890: get
  status = db->Get(read_options, "key0769", &value);
  if (status.ok()) {
    out << "GET key0769 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5891: put
  db->Put(write_options, "key8732", "value10536");

  // op 5892: put
  db->Put(write_options, "key11313", "value110959");

  // op 5893: delete_range
  status = db->DeleteRange(write_options, "key5243", "key5940");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 5894: delete_range
  status = db->DeleteRange(write_options, "key0859", "key1520");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 5895: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 5896: get
  status = db->Get(read_options, "key2497", &value);
  if (status.ok()) {
    out << "GET key2497 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5897: delete
  db->Delete(write_options, "key11059");

  // op 5898: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 5899: get
  status = db->Get(read_options, "key4551", &value);
  if (status.ok()) {
    out << "GET key4551 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5900: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6497", "key9851", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6497, key9851) size=" << scan_result.size() << std::endl;
  }

  // op 5901: delete
  db->Delete(write_options, "key1857");

  // op 5902: put
  db->Put(write_options, "key8183", "value68800");

  // op 5903: get
  status = db->Get(read_options, "key7201", &value);
  if (status.ok()) {
    out << "GET key7201 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5904: delete
  db->Delete(write_options, "key4595");

  // op 5905: put
  db->Put(write_options, "key0758", "value53694");

  // op 5906: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4389", "key5872", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4389, key5872) size=" << scan_result.size() << std::endl;
  }

  // op 5907: delete
  db->Delete(write_options, "key8216");

  // op 5908: put
  db->Put(write_options, "key4764", "value101375");

  // op 5909: delete_range
  status = db->DeleteRange(write_options, "key6649", "key9588");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 5910: delete_range
  status = db->DeleteRange(write_options, "key5298", "key11469");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 5911: delete
  db->Delete(write_options, "key3081");

  // op 5912: get
  status = db->Get(read_options, "key0660", &value);
  if (status.ok()) {
    out << "GET key0660 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5913: put
  db->Put(write_options, "key4744", "value89377");

  // op 5914: delete_range
  status = db->DeleteRange(write_options, "key2698", "key8789");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 5915: delete_range
  status = db->DeleteRange(write_options, "key5242", "key11502");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 5916: put
  db->Put(write_options, "key9422", "value59644");

  // op 5917: get
  status = db->Get(read_options, "key6195", &value);
  if (status.ok()) {
    out << "GET key6195 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5918: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 5919: put
  db->Put(write_options, "key6287", "value53565");

  // op 5920: put
  db->Put(write_options, "key0749", "value57414");

  // op 5921: get
  status = db->Get(read_options, "key11309", &value);
  if (status.ok()) {
    out << "GET key11309 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5922: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1371", "key9113", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1371, key9113) size=" << scan_result.size() << std::endl;
  }

  // op 5923: put
  db->Put(write_options, "key7041", "value51845");

  // op 5924: put
  db->Put(write_options, "key9146", "value32797");

  // op 5925: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8314", "key10618", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8314, key10618) size=" << scan_result.size() << std::endl;
  }

  // op 5926: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4407", "key7249", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4407, key7249) size=" << scan_result.size() << std::endl;
  }

  // op 5927: put
  db->Put(write_options, "key7283", "value5744");

  // op 5928: get
  status = db->Get(read_options, "key1274", &value);
  if (status.ok()) {
    out << "GET key1274 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5929: delete_range
  status = db->DeleteRange(write_options, "key0437", "key8901");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 5930: get
  status = db->Get(read_options, "key6719", &value);
  if (status.ok()) {
    out << "GET key6719 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5931: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1368", "key5567", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1368, key5567) size=" << scan_result.size() << std::endl;
  }

  // op 5932: put
  db->Put(write_options, "key9063", "value25938");

  // op 5933: put
  db->Put(write_options, "key1866", "value59558");

  // op 5934: put
  db->Put(write_options, "key1110", "value33751");

  // op 5935: get
  status = db->Get(read_options, "key11395", &value);
  if (status.ok()) {
    out << "GET key11395 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5936: get
  status = db->Get(read_options, "key11539", &value);
  if (status.ok()) {
    out << "GET key11539 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5937: get
  status = db->Get(read_options, "key5055", &value);
  if (status.ok()) {
    out << "GET key5055 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5938: get
  status = db->Get(read_options, "key1899", &value);
  if (status.ok()) {
    out << "GET key1899 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5939: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1199", "key2679", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1199, key2679) size=" << scan_result.size() << std::endl;
  }

  // op 5940: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5542", "key11726", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5542, key11726) size=" << scan_result.size() << std::endl;
  }

  // op 5941: put
  db->Put(write_options, "key7505", "value30243");

  // op 5942: put
  db->Put(write_options, "key10896", "value80070");

  // op 5943: get
  status = db->Get(read_options, "key11327", &value);
  if (status.ok()) {
    out << "GET key11327 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5944: put
  db->Put(write_options, "key6030", "value88002");

  // op 5945: get
  status = db->Get(read_options, "key8157", &value);
  if (status.ok()) {
    out << "GET key8157 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5946: get
  status = db->Get(read_options, "key2324", &value);
  if (status.ok()) {
    out << "GET key2324 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5947: put
  db->Put(write_options, "key2783", "value52986");

  // op 5948: get
  status = db->Get(read_options, "key0220", &value);
  if (status.ok()) {
    out << "GET key0220 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5949: put
  db->Put(write_options, "key10642", "value32213");

  // op 5950: delete
  db->Delete(write_options, "key5646");

  // op 5951: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6134", "key11061", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6134, key11061) size=" << scan_result.size() << std::endl;
  }

  // op 5952: put
  db->Put(write_options, "key9389", "value21224");

  // op 5953: put
  db->Put(write_options, "key2911", "value53731");

  // op 5954: delete
  db->Delete(write_options, "key1658");

  // op 5955: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5217", "key11557", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5217, key11557) size=" << scan_result.size() << std::endl;
  }

  // op 5956: get
  status = db->Get(read_options, "key9439", &value);
  if (status.ok()) {
    out << "GET key9439 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5957: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 5958: get
  status = db->Get(read_options, "key1243", &value);
  if (status.ok()) {
    out << "GET key1243 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5959: put
  db->Put(write_options, "key11259", "value16991");

  // op 5960: put
  db->Put(write_options, "key2348", "value17377");

  // op 5961: get
  status = db->Get(read_options, "key6277", &value);
  if (status.ok()) {
    out << "GET key6277 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5962: delete
  db->Delete(write_options, "key6251");

  // op 5963: put
  db->Put(write_options, "key2698", "value44488");

  // op 5964: put
  db->Put(write_options, "key5943", "value74177");

  // op 5965: put
  db->Put(write_options, "key2883", "value97784");

  // op 5966: get
  status = db->Get(read_options, "key10557", &value);
  if (status.ok()) {
    out << "GET key10557 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5967: put
  db->Put(write_options, "key9689", "value40572");

  // op 5968: delete
  db->Delete(write_options, "key4534");

  // op 5969: put
  db->Put(write_options, "key4065", "value28249");

  // op 5970: put
  db->Put(write_options, "key3112", "value87449");

  // op 5971: put
  db->Put(write_options, "key9810", "value70476");

  // op 5972: put
  db->Put(write_options, "key0401", "value92207");

  // op 5973: put
  db->Put(write_options, "key7488", "value47079");

  // op 5974: put
  db->Put(write_options, "key5637", "value59983");

  // op 5975: get
  status = db->Get(read_options, "key1058", &value);
  if (status.ok()) {
    out << "GET key1058 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5976: put
  db->Put(write_options, "key11054", "value50763");

  // op 5977: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6075", "key9826", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6075, key9826) size=" << scan_result.size() << std::endl;
  }

  // op 5978: put
  db->Put(write_options, "key1822", "value11083");

  // op 5979: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2637", "key11036", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2637, key11036) size=" << scan_result.size() << std::endl;
  }

  // op 5980: put
  db->Put(write_options, "key3218", "value0655");

  // op 5981: get
  status = db->Get(read_options, "key3205", &value);
  if (status.ok()) {
    out << "GET key3205 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5982: put
  db->Put(write_options, "key6276", "value67416");

  // op 5983: put
  db->Put(write_options, "key8929", "value62782");

  // op 5984: put
  db->Put(write_options, "key6304", "value79179");

  // op 5985: get
  status = db->Get(read_options, "key7345", &value);
  if (status.ok()) {
    out << "GET key7345 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5986: put
  db->Put(write_options, "key4941", "value96756");

  // op 5987: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 5988: get
  status = db->Get(read_options, "key3455", &value);
  if (status.ok()) {
    out << "GET key3455 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5989: delete_range
  status = db->DeleteRange(write_options, "key6226", "key6755");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 5990: delete
  db->Delete(write_options, "key0180");

  // op 5991: get
  status = db->Get(read_options, "key7126", &value);
  if (status.ok()) {
    out << "GET key7126 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5992: get
  status = db->Get(read_options, "key10387", &value);
  if (status.ok()) {
    out << "GET key10387 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5993: get
  status = db->Get(read_options, "key8687", &value);
  if (status.ok()) {
    out << "GET key8687 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5994: get
  status = db->Get(read_options, "key10040", &value);
  if (status.ok()) {
    out << "GET key10040 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5995: put
  db->Put(write_options, "key10028", "value113339");

  // op 5996: get
  status = db->Get(read_options, "key6767", &value);
  if (status.ok()) {
    out << "GET key6767 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 5997: put
  db->Put(write_options, "key7411", "value106869");

  // op 5998: put
  db->Put(write_options, "key0377", "value62084");

  // op 5999: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1721", "key11464", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1721, key11464) size=" << scan_result.size() << std::endl;
  }

  // op 6000: put
  db->Put(write_options, "key7651", "value25117");

  // op 6001: put
  db->Put(write_options, "key0743", "value64084");

  // op 6002: put
  db->Put(write_options, "key9981", "value52490");

  // op 6003: put
  db->Put(write_options, "key8692", "value58070");

  // op 6004: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1202", "key8701", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1202, key8701) size=" << scan_result.size() << std::endl;
  }

  // op 6005: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5176", "key10146", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5176, key10146) size=" << scan_result.size() << std::endl;
  }

  // op 6006: get
  status = db->Get(read_options, "key9264", &value);
  if (status.ok()) {
    out << "GET key9264 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6007: get
  status = db->Get(read_options, "key0360", &value);
  if (status.ok()) {
    out << "GET key0360 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6008: put
  db->Put(write_options, "key11507", "value82326");

  // op 6009: put
  db->Put(write_options, "key3315", "value57351");

  // op 6010: delete
  db->Delete(write_options, "key5301");

  // op 6011: get
  status = db->Get(read_options, "key5497", &value);
  if (status.ok()) {
    out << "GET key5497 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6012: put
  db->Put(write_options, "key6954", "value94392");

  // op 6013: get
  status = db->Get(read_options, "key4661", &value);
  if (status.ok()) {
    out << "GET key4661 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6014: put
  db->Put(write_options, "key6971", "value90152");

  // op 6015: get
  status = db->Get(read_options, "key5905", &value);
  if (status.ok()) {
    out << "GET key5905 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6016: put
  db->Put(write_options, "key0675", "value31506");

  // op 6017: put
  db->Put(write_options, "key9039", "value100361");

  // op 6018: get
  status = db->Get(read_options, "key11202", &value);
  if (status.ok()) {
    out << "GET key11202 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6019: delete_range
  status = db->DeleteRange(write_options, "key2797", "key7482");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 6020: delete
  db->Delete(write_options, "key10356");

  // op 6021: delete
  db->Delete(write_options, "key7681");

  // op 6022: get
  status = db->Get(read_options, "key3220", &value);
  if (status.ok()) {
    out << "GET key3220 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6023: get
  status = db->Get(read_options, "key8643", &value);
  if (status.ok()) {
    out << "GET key8643 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6024: get
  status = db->Get(read_options, "key7812", &value);
  if (status.ok()) {
    out << "GET key7812 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6025: put
  db->Put(write_options, "key2832", "value104513");

  // op 6026: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2052", "key6631", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2052, key6631) size=" << scan_result.size() << std::endl;
  }

  // op 6027: delete
  db->Delete(write_options, "key4759");

  // op 6028: put
  db->Put(write_options, "key8448", "value48054");

  // op 6029: get
  status = db->Get(read_options, "key4429", &value);
  if (status.ok()) {
    out << "GET key4429 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6030: delete
  db->Delete(write_options, "key6253");

  // op 6031: put
  db->Put(write_options, "key2775", "value92221");

  // op 6032: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 6033: delete
  db->Delete(write_options, "key1897");

  // op 6034: put
  db->Put(write_options, "key11598", "value92027");

  // op 6035: get
  status = db->Get(read_options, "key5495", &value);
  if (status.ok()) {
    out << "GET key5495 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6036: put
  db->Put(write_options, "key8062", "value36744");

  // op 6037: put
  db->Put(write_options, "key2705", "value22560");

  // op 6038: put
  db->Put(write_options, "key5022", "value3615");

  // op 6039: put
  db->Put(write_options, "key10162", "value25554");

  // op 6040: put
  db->Put(write_options, "key11600", "value70162");

  // op 6041: delete_range
  status = db->DeleteRange(write_options, "key2513", "key10170");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 6042: put
  db->Put(write_options, "key3093", "value98550");

  // op 6043: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 6044: delete
  db->Delete(write_options, "key4083");

  // op 6045: put
  db->Put(write_options, "key11353", "value35829");

  // op 6046: get
  status = db->Get(read_options, "key0122", &value);
  if (status.ok()) {
    out << "GET key0122 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6047: delete_range
  status = db->DeleteRange(write_options, "key0904", "key10995");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 6048: delete
  db->Delete(write_options, "key1637");

  // op 6049: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2513", "key5567", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2513, key5567) size=" << scan_result.size() << std::endl;
  }

  // op 6050: get
  status = db->Get(read_options, "key3234", &value);
  if (status.ok()) {
    out << "GET key3234 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6051: delete_range
  status = db->DeleteRange(write_options, "key1318", "key11683");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 6052: put
  db->Put(write_options, "key11576", "value0223");

  // op 6053: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7822", "key11578", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7822, key11578) size=" << scan_result.size() << std::endl;
  }

  // op 6054: put
  db->Put(write_options, "key10026", "value13454");

  // op 6055: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 6056: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 6057: delete
  db->Delete(write_options, "key9730");

  // op 6058: get
  status = db->Get(read_options, "key6894", &value);
  if (status.ok()) {
    out << "GET key6894 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6059: put
  db->Put(write_options, "key0411", "value80589");

  // op 6060: delete
  db->Delete(write_options, "key6016");

  // op 6061: delete
  db->Delete(write_options, "key7581");

  // op 6062: put
  db->Put(write_options, "key10201", "value72575");

  // op 6063: put
  db->Put(write_options, "key10786", "value74359");

  // op 6064: put
  db->Put(write_options, "key7981", "value56545");

  // op 6065: delete_range
  status = db->DeleteRange(write_options, "key5471", "key10556");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 6066: get
  status = db->Get(read_options, "key0511", &value);
  if (status.ok()) {
    out << "GET key0511 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6067: delete
  db->Delete(write_options, "key6820");

  // op 6068: put
  db->Put(write_options, "key4726", "value54149");

  // op 6069: get
  status = db->Get(read_options, "key8962", &value);
  if (status.ok()) {
    out << "GET key8962 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6070: get
  status = db->Get(read_options, "key6153", &value);
  if (status.ok()) {
    out << "GET key6153 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6071: delete_range
  status = db->DeleteRange(write_options, "key2125", "key9427");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 6072: put
  db->Put(write_options, "key1323", "value99674");

  // op 6073: get
  status = db->Get(read_options, "key3074", &value);
  if (status.ok()) {
    out << "GET key3074 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6074: put
  db->Put(write_options, "key6970", "value48381");

  // op 6075: put
  db->Put(write_options, "key0402", "value90586");

  // op 6076: put
  db->Put(write_options, "key5275", "value5351");

  // op 6077: delete
  db->Delete(write_options, "key9025");

  // op 6078: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3359", "key10549", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3359, key10549) size=" << scan_result.size() << std::endl;
  }

  // op 6079: delete
  db->Delete(write_options, "key2300");

  // op 6080: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 6081: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 6082: delete
  db->Delete(write_options, "key5089");

  // op 6083: get
  status = db->Get(read_options, "key3831", &value);
  if (status.ok()) {
    out << "GET key3831 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6084: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4324", "key9701", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4324, key9701) size=" << scan_result.size() << std::endl;
  }

  // op 6085: get
  status = db->Get(read_options, "key5933", &value);
  if (status.ok()) {
    out << "GET key5933 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6086: delete
  db->Delete(write_options, "key5743");

  // op 6087: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2375", "key4483", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2375, key4483) size=" << scan_result.size() << std::endl;
  }

  // op 6088: put
  db->Put(write_options, "key2625", "value15579");

  // op 6089: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 6090: delete_range
  status = db->DeleteRange(write_options, "key3729", "key9851");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 6091: delete
  db->Delete(write_options, "key10184");

  // op 6092: put
  db->Put(write_options, "key6082", "value6308");

  // op 6093: put
  db->Put(write_options, "key9321", "value18882");

  // op 6094: put
  db->Put(write_options, "key5077", "value0158");

  // op 6095: get
  status = db->Get(read_options, "key4363", &value);
  if (status.ok()) {
    out << "GET key4363 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6096: put
  db->Put(write_options, "key1661", "value24804");

  // op 6097: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1016", "key6510", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1016, key6510) size=" << scan_result.size() << std::endl;
  }

  // op 6098: get
  status = db->Get(read_options, "key6089", &value);
  if (status.ok()) {
    out << "GET key6089 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6099: put
  db->Put(write_options, "key10357", "value84285");

  // op 6100: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6883", "key11537", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6883, key11537) size=" << scan_result.size() << std::endl;
  }

  // op 6101: put
  db->Put(write_options, "key7724", "value9653");

  // op 6102: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 6103: put
  db->Put(write_options, "key4494", "value84028");

  // op 6104: get
  status = db->Get(read_options, "key9226", &value);
  if (status.ok()) {
    out << "GET key9226 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6105: delete_range
  status = db->DeleteRange(write_options, "key0711", "key10228");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 6106: delete
  db->Delete(write_options, "key2965");

  // op 6107: put
  db->Put(write_options, "key1463", "value71519");

  // op 6108: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 6109: put
  db->Put(write_options, "key6216", "value93009");

  // op 6110: put
  db->Put(write_options, "key2401", "value11722");

  // op 6111: get
  status = db->Get(read_options, "key5058", &value);
  if (status.ok()) {
    out << "GET key5058 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6112: get
  status = db->Get(read_options, "key2240", &value);
  if (status.ok()) {
    out << "GET key2240 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6113: get
  status = db->Get(read_options, "key2176", &value);
  if (status.ok()) {
    out << "GET key2176 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6114: delete_range
  status = db->DeleteRange(write_options, "key6962", "key10223");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 6115: delete
  db->Delete(write_options, "key4523");

  // op 6116: get
  status = db->Get(read_options, "key7198", &value);
  if (status.ok()) {
    out << "GET key7198 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6117: put
  db->Put(write_options, "key1703", "value107549");

  // op 6118: put
  db->Put(write_options, "key4767", "value23348");

  // op 6119: put
  db->Put(write_options, "key2350", "value103652");

  // op 6120: put
  db->Put(write_options, "key1519", "value17398");

  // op 6121: get
  status = db->Get(read_options, "key1083", &value);
  if (status.ok()) {
    out << "GET key1083 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6122: delete_range
  status = db->DeleteRange(write_options, "key4046", "key6518");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 6123: delete_range
  status = db->DeleteRange(write_options, "key2191", "key4932");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 6124: put
  db->Put(write_options, "key1101", "value61052");

  // op 6125: delete_range
  status = db->DeleteRange(write_options, "key5709", "key10102");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 6126: put
  db->Put(write_options, "key10855", "value30777");

  // op 6127: put
  db->Put(write_options, "key2150", "value52327");

  // op 6128: put
  db->Put(write_options, "key4643", "value85717");

  // op 6129: delete
  db->Delete(write_options, "key8806");

  // op 6130: put
  db->Put(write_options, "key6518", "value16313");

  // op 6131: put
  db->Put(write_options, "key8528", "value94364");

  // op 6132: delete_range
  status = db->DeleteRange(write_options, "key4345", "key7292");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 6133: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3785", "key8214", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3785, key8214) size=" << scan_result.size() << std::endl;
  }

  // op 6134: get
  status = db->Get(read_options, "key6402", &value);
  if (status.ok()) {
    out << "GET key6402 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6135: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3921", "key7004", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3921, key7004) size=" << scan_result.size() << std::endl;
  }

  // op 6136: put
  db->Put(write_options, "key6184", "value43392");

  // op 6137: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5164", "key7645", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5164, key7645) size=" << scan_result.size() << std::endl;
  }

  // op 6138: put
  db->Put(write_options, "key6657", "value28112");

  // op 6139: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4114", "key7501", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4114, key7501) size=" << scan_result.size() << std::endl;
  }

  // op 6140: delete_range
  status = db->DeleteRange(write_options, "key2367", "key7369");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 6141: delete_range
  status = db->DeleteRange(write_options, "key2190", "key11276");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 6142: get
  status = db->Get(read_options, "key8438", &value);
  if (status.ok()) {
    out << "GET key8438 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6143: get
  status = db->Get(read_options, "key7905", &value);
  if (status.ok()) {
    out << "GET key7905 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6144: get
  status = db->Get(read_options, "key8378", &value);
  if (status.ok()) {
    out << "GET key8378 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6145: put
  db->Put(write_options, "key0145", "value117042");

  // op 6146: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 6147: delete
  db->Delete(write_options, "key0666");

  // op 6148: put
  db->Put(write_options, "key2248", "value99167");

  // op 6149: get
  status = db->Get(read_options, "key10583", &value);
  if (status.ok()) {
    out << "GET key10583 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6150: put
  db->Put(write_options, "key1155", "value68460");

  // op 6151: delete
  db->Delete(write_options, "key5713");

  // op 6152: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 6153: put
  db->Put(write_options, "key9652", "value36578");

  // op 6154: delete
  db->Delete(write_options, "key0298");

  // op 6155: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2688", "key4428", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2688, key4428) size=" << scan_result.size() << std::endl;
  }

  // op 6156: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8735", "key10098", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8735, key10098) size=" << scan_result.size() << std::endl;
  }

  // op 6157: put
  db->Put(write_options, "key3318", "value22326");

  // op 6158: delete_range
  status = db->DeleteRange(write_options, "key0037", "key2855");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 6159: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1267", "key8558", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1267, key8558) size=" << scan_result.size() << std::endl;
  }

  // op 6160: get
  status = db->Get(read_options, "key6413", &value);
  if (status.ok()) {
    out << "GET key6413 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6161: delete_range
  status = db->DeleteRange(write_options, "key2969", "key7987");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 6162: put
  db->Put(write_options, "key7147", "value3077");

  // op 6163: delete
  db->Delete(write_options, "key11402");

  // op 6164: put
  db->Put(write_options, "key5544", "value92942");

  // op 6165: put
  db->Put(write_options, "key4694", "value77348");

  // op 6166: put
  db->Put(write_options, "key4036", "value96384");

  // op 6167: put
  db->Put(write_options, "key0072", "value29596");

  // op 6168: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 6169: put
  db->Put(write_options, "key4959", "value72218");

  // op 6170: get
  status = db->Get(read_options, "key11464", &value);
  if (status.ok()) {
    out << "GET key11464 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6171: get
  status = db->Get(read_options, "key4658", &value);
  if (status.ok()) {
    out << "GET key4658 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6172: put
  db->Put(write_options, "key11354", "value23758");

  // op 6173: put
  db->Put(write_options, "key7548", "value18242");

  // op 6174: delete
  db->Delete(write_options, "key1289");

  // op 6175: delete
  db->Delete(write_options, "key1994");

  // op 6176: get
  status = db->Get(read_options, "key4300", &value);
  if (status.ok()) {
    out << "GET key4300 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6177: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0341", "key8278", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0341, key8278) size=" << scan_result.size() << std::endl;
  }

  // op 6178: get
  status = db->Get(read_options, "key6927", &value);
  if (status.ok()) {
    out << "GET key6927 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6179: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10684", "key11520", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10684, key11520) size=" << scan_result.size() << std::endl;
  }

  // op 6180: delete_range
  status = db->DeleteRange(write_options, "key4054", "key10439");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 6181: get
  status = db->Get(read_options, "key2157", &value);
  if (status.ok()) {
    out << "GET key2157 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6182: delete_range
  status = db->DeleteRange(write_options, "key1274", "key7080");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 6183: put
  db->Put(write_options, "key4925", "value102592");

  // op 6184: put
  db->Put(write_options, "key5122", "value27342");

  // op 6185: put
  db->Put(write_options, "key5346", "value31464");

  // op 6186: delete
  db->Delete(write_options, "key7235");

  // op 6187: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 6188: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1025", "key9874", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1025, key9874) size=" << scan_result.size() << std::endl;
  }

  // op 6189: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1235", "key5965", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1235, key5965) size=" << scan_result.size() << std::endl;
  }

  // op 6190: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10721", "key10916", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10721, key10916) size=" << scan_result.size() << std::endl;
  }

  // op 6191: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0949", "key7281", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0949, key7281) size=" << scan_result.size() << std::endl;
  }

  // op 6192: put
  db->Put(write_options, "key7739", "value97936");

  // op 6193: put
  db->Put(write_options, "key9659", "value6432");

  // op 6194: put
  db->Put(write_options, "key2833", "value92356");

  // op 6195: put
  db->Put(write_options, "key1561", "value117627");

  // op 6196: get
  status = db->Get(read_options, "key8114", &value);
  if (status.ok()) {
    out << "GET key8114 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6197: delete
  db->Delete(write_options, "key7102");

  // op 6198: put
  db->Put(write_options, "key9709", "value114374");

  // op 6199: get
  status = db->Get(read_options, "key4621", &value);
  if (status.ok()) {
    out << "GET key4621 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6200: put
  db->Put(write_options, "key8691", "value94759");

  // op 6201: put
  db->Put(write_options, "key8185", "value81418");

  // op 6202: put
  db->Put(write_options, "key0071", "value15410");

  // op 6203: delete_range
  status = db->DeleteRange(write_options, "key1636", "key8153");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 6204: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0031", "key5316", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0031, key5316) size=" << scan_result.size() << std::endl;
  }

  // op 6205: put
  db->Put(write_options, "key4035", "value57493");

  // op 6206: delete
  db->Delete(write_options, "key3159");

  // op 6207: put
  db->Put(write_options, "key6553", "value113348");

  // op 6208: delete_range
  status = db->DeleteRange(write_options, "key7089", "key11011");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 6209: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0807", "key10010", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0807, key10010) size=" << scan_result.size() << std::endl;
  }

  // op 6210: get
  status = db->Get(read_options, "key9223", &value);
  if (status.ok()) {
    out << "GET key9223 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6211: put
  db->Put(write_options, "key2537", "value33831");

  // op 6212: get
  status = db->Get(read_options, "key5390", &value);
  if (status.ok()) {
    out << "GET key5390 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6213: get
  status = db->Get(read_options, "key3518", &value);
  if (status.ok()) {
    out << "GET key3518 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6214: delete
  db->Delete(write_options, "key2100");

  // op 6215: put
  db->Put(write_options, "key2073", "value81001");

  // op 6216: delete_range
  status = db->DeleteRange(write_options, "key8690", "key11053");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 6217: delete_range
  status = db->DeleteRange(write_options, "key3942", "key9479");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 6218: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3281", "key6566", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3281, key6566) size=" << scan_result.size() << std::endl;
  }

  // op 6219: get
  status = db->Get(read_options, "key8759", &value);
  if (status.ok()) {
    out << "GET key8759 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6220: put
  db->Put(write_options, "key11001", "value74873");

  // op 6221: put
  db->Put(write_options, "key11166", "value86230");

  // op 6222: get
  status = db->Get(read_options, "key11659", &value);
  if (status.ok()) {
    out << "GET key11659 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6223: put
  db->Put(write_options, "key8295", "value115289");

  // op 6224: put
  db->Put(write_options, "key5937", "value91034");

  // op 6225: put
  db->Put(write_options, "key11618", "value107234");

  // op 6226: get
  status = db->Get(read_options, "key8370", &value);
  if (status.ok()) {
    out << "GET key8370 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6227: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2194", "key10122", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2194, key10122) size=" << scan_result.size() << std::endl;
  }

  // op 6228: put
  db->Put(write_options, "key8123", "value42402");

  // op 6229: put
  db->Put(write_options, "key2223", "value50063");

  // op 6230: put
  db->Put(write_options, "key2472", "value74251");

  // op 6231: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3935", "key6466", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3935, key6466) size=" << scan_result.size() << std::endl;
  }

  // op 6232: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 6233: delete
  db->Delete(write_options, "key9958");

  // op 6234: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2111", "key9459", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2111, key9459) size=" << scan_result.size() << std::endl;
  }

  // op 6235: put
  db->Put(write_options, "key10161", "value55659");

  // op 6236: delete_range
  status = db->DeleteRange(write_options, "key0446", "key4200");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 6237: delete
  db->Delete(write_options, "key10928");

  // op 6238: put
  db->Put(write_options, "key3894", "value53081");

  // op 6239: delete
  db->Delete(write_options, "key10571");

  // op 6240: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 6241: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1694", "key6471", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1694, key6471) size=" << scan_result.size() << std::endl;
  }

  // op 6242: get
  status = db->Get(read_options, "key11469", &value);
  if (status.ok()) {
    out << "GET key11469 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6243: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2341", "key6262", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2341, key6262) size=" << scan_result.size() << std::endl;
  }

  // op 6244: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 6245: get
  status = db->Get(read_options, "key11423", &value);
  if (status.ok()) {
    out << "GET key11423 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6246: delete_range
  status = db->DeleteRange(write_options, "key1061", "key10981");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 6247: put
  db->Put(write_options, "key4145", "value113300");

  // op 6248: put
  db->Put(write_options, "key11591", "value55036");

  // op 6249: delete
  db->Delete(write_options, "key5183");

  // op 6250: put
  db->Put(write_options, "key5474", "value111823");

  // op 6251: put
  db->Put(write_options, "key8298", "value12794");

  // op 6252: put
  db->Put(write_options, "key8293", "value60297");

  // op 6253: put
  db->Put(write_options, "key1581", "value21749");

  // op 6254: put
  db->Put(write_options, "key0576", "value11434");

  // op 6255: delete
  db->Delete(write_options, "key4877");

  // op 6256: get
  status = db->Get(read_options, "key8293", &value);
  if (status.ok()) {
    out << "GET key8293 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6257: delete_range
  status = db->DeleteRange(write_options, "key9323", "key9639");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 6258: delete
  db->Delete(write_options, "key1908");

  // op 6259: get
  status = db->Get(read_options, "key11606", &value);
  if (status.ok()) {
    out << "GET key11606 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6260: put
  db->Put(write_options, "key2640", "value74801");

  // op 6261: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 6262: delete_range
  status = db->DeleteRange(write_options, "key1578", "key3714");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 6263: delete_range
  status = db->DeleteRange(write_options, "key3064", "key6648");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 6264: put
  db->Put(write_options, "key3402", "value111127");

  // op 6265: put
  db->Put(write_options, "key3185", "value88644");

  // op 6266: delete_range
  status = db->DeleteRange(write_options, "key5215", "key9446");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 6267: put
  db->Put(write_options, "key9640", "value104340");

  // op 6268: delete_range
  status = db->DeleteRange(write_options, "key9489", "key11252");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 6269: delete_range
  status = db->DeleteRange(write_options, "key4668", "key10976");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 6270: put
  db->Put(write_options, "key6350", "value43011");

  // op 6271: get
  status = db->Get(read_options, "key2651", &value);
  if (status.ok()) {
    out << "GET key2651 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6272: put
  db->Put(write_options, "key0371", "value4087");

  // op 6273: put
  db->Put(write_options, "key7605", "value85030");

  // op 6274: delete_range
  status = db->DeleteRange(write_options, "key4765", "key4977");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 6275: delete
  db->Delete(write_options, "key10150");

  // op 6276: put
  db->Put(write_options, "key6294", "value45276");

  // op 6277: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0030", "key9052", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0030, key9052) size=" << scan_result.size() << std::endl;
  }

  // op 6278: put
  db->Put(write_options, "key3436", "value50080");

  // op 6279: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5455", "key10610", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5455, key10610) size=" << scan_result.size() << std::endl;
  }

  // op 6280: put
  db->Put(write_options, "key6571", "value64699");

  // op 6281: delete_range
  status = db->DeleteRange(write_options, "key4409", "key5505");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 6282: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3034", "key5501", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3034, key5501) size=" << scan_result.size() << std::endl;
  }

  // op 6283: put
  db->Put(write_options, "key8751", "value19495");

  // op 6284: put
  db->Put(write_options, "key11497", "value30496");

  // op 6285: put
  db->Put(write_options, "key6052", "value97464");

  // op 6286: get
  status = db->Get(read_options, "key10398", &value);
  if (status.ok()) {
    out << "GET key10398 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6287: get
  status = db->Get(read_options, "key8640", &value);
  if (status.ok()) {
    out << "GET key8640 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6288: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1144", "key11017", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1144, key11017) size=" << scan_result.size() << std::endl;
  }

  // op 6289: put
  db->Put(write_options, "key7683", "value47627");

  // op 6290: put
  db->Put(write_options, "key1787", "value19757");

  // op 6291: get
  status = db->Get(read_options, "key8225", &value);
  if (status.ok()) {
    out << "GET key8225 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6292: put
  db->Put(write_options, "key7664", "value108160");

  // op 6293: put
  db->Put(write_options, "key4398", "value16258");

  // op 6294: put
  db->Put(write_options, "key0318", "value112200");

  // op 6295: put
  db->Put(write_options, "key8387", "value116482");

  // op 6296: delete_range
  status = db->DeleteRange(write_options, "key3465", "key11496");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 6297: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4039", "key10516", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4039, key10516) size=" << scan_result.size() << std::endl;
  }

  // op 6298: delete
  db->Delete(write_options, "key5466");

  // op 6299: get
  status = db->Get(read_options, "key1282", &value);
  if (status.ok()) {
    out << "GET key1282 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6300: get
  status = db->Get(read_options, "key11548", &value);
  if (status.ok()) {
    out << "GET key11548 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6301: put
  db->Put(write_options, "key8183", "value115073");

  // op 6302: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 6303: delete
  db->Delete(write_options, "key3688");

  // op 6304: put
  db->Put(write_options, "key8424", "value99617");

  // op 6305: put
  db->Put(write_options, "key4178", "value9159");

  // op 6306: put
  db->Put(write_options, "key1242", "value13587");

  // op 6307: put
  db->Put(write_options, "key4579", "value110088");

  // op 6308: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4804", "key5313", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4804, key5313) size=" << scan_result.size() << std::endl;
  }

  // op 6309: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2311", "key2675", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2311, key2675) size=" << scan_result.size() << std::endl;
  }

  // op 6310: put
  db->Put(write_options, "key11387", "value31193");

  // op 6311: delete
  db->Delete(write_options, "key6148");

  // op 6312: put
  db->Put(write_options, "key10809", "value34610");

  // op 6313: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 6314: put
  db->Put(write_options, "key4946", "value90091");

  // op 6315: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6526", "key7946", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6526, key7946) size=" << scan_result.size() << std::endl;
  }

  // op 6316: put
  db->Put(write_options, "key6243", "value8345");

  // op 6317: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 6318: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 6319: delete_range
  status = db->DeleteRange(write_options, "key1403", "key6158");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 6320: get
  status = db->Get(read_options, "key4300", &value);
  if (status.ok()) {
    out << "GET key4300 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6321: delete_range
  status = db->DeleteRange(write_options, "key2551", "key3440");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 6322: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1873", "key11595", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1873, key11595) size=" << scan_result.size() << std::endl;
  }

  // op 6323: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0428", "key4878", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0428, key4878) size=" << scan_result.size() << std::endl;
  }

  // op 6324: put
  db->Put(write_options, "key6064", "value110989");

  // op 6325: put
  db->Put(write_options, "key2743", "value95734");

  // op 6326: delete_range
  status = db->DeleteRange(write_options, "key0823", "key7331");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 6327: get
  status = db->Get(read_options, "key9548", &value);
  if (status.ok()) {
    out << "GET key9548 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6328: put
  db->Put(write_options, "key4188", "value54448");

  // op 6329: put
  db->Put(write_options, "key8290", "value90526");

  // op 6330: get
  status = db->Get(read_options, "key1595", &value);
  if (status.ok()) {
    out << "GET key1595 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6331: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2595", "key2857", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2595, key2857) size=" << scan_result.size() << std::endl;
  }

  // op 6332: delete
  db->Delete(write_options, "key7393");

  // op 6333: get
  status = db->Get(read_options, "key0968", &value);
  if (status.ok()) {
    out << "GET key0968 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6334: put
  db->Put(write_options, "key3077", "value112940");

  // op 6335: put
  db->Put(write_options, "key1023", "value31615");

  // op 6336: delete_range
  status = db->DeleteRange(write_options, "key1513", "key2851");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 6337: put
  db->Put(write_options, "key8979", "value46961");

  // op 6338: put
  db->Put(write_options, "key10022", "value2079");

  // op 6339: delete
  db->Delete(write_options, "key3408");

  // op 6340: put
  db->Put(write_options, "key6380", "value85114");

  // op 6341: put
  db->Put(write_options, "key9309", "value90502");

  // op 6342: delete_range
  status = db->DeleteRange(write_options, "key4091", "key11207");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 6343: put
  db->Put(write_options, "key2613", "value84473");

  // op 6344: delete
  db->Delete(write_options, "key10809");

  // op 6345: put
  db->Put(write_options, "key10104", "value79812");

  // op 6346: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8059", "key11253", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8059, key11253) size=" << scan_result.size() << std::endl;
  }

  // op 6347: put
  db->Put(write_options, "key3903", "value61315");

  // op 6348: delete
  db->Delete(write_options, "key5311");

  // op 6349: put
  db->Put(write_options, "key0035", "value24757");

  // op 6350: get
  status = db->Get(read_options, "key6554", &value);
  if (status.ok()) {
    out << "GET key6554 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6351: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 6352: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1024", "key7647", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1024, key7647) size=" << scan_result.size() << std::endl;
  }

  // op 6353: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3170", "key9162", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3170, key9162) size=" << scan_result.size() << std::endl;
  }

  // op 6354: put
  db->Put(write_options, "key9844", "value63471");

  // op 6355: put
  db->Put(write_options, "key4431", "value2290");

  // op 6356: get
  status = db->Get(read_options, "key0479", &value);
  if (status.ok()) {
    out << "GET key0479 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6357: get
  status = db->Get(read_options, "key6881", &value);
  if (status.ok()) {
    out << "GET key6881 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6358: put
  db->Put(write_options, "key8233", "value58664");

  // op 6359: get
  status = db->Get(read_options, "key4740", &value);
  if (status.ok()) {
    out << "GET key4740 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6360: put
  db->Put(write_options, "key11223", "value32151");

  // op 6361: put
  db->Put(write_options, "key7453", "value113658");

  // op 6362: put
  db->Put(write_options, "key6027", "value74230");

  // op 6363: put
  db->Put(write_options, "key6512", "value40775");

  // op 6364: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0001", "key5878", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0001, key5878) size=" << scan_result.size() << std::endl;
  }

  // op 6365: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 6366: delete
  db->Delete(write_options, "key4937");

  // op 6367: put
  db->Put(write_options, "key6114", "value48232");

  // op 6368: put
  db->Put(write_options, "key3668", "value100267");

  // op 6369: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 6370: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2209", "key3170", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2209, key3170) size=" << scan_result.size() << std::endl;
  }

  // op 6371: put
  db->Put(write_options, "key11282", "value23966");

  // op 6372: delete_range
  status = db->DeleteRange(write_options, "key0732", "key7202");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 6373: put
  db->Put(write_options, "key5425", "value47771");

  // op 6374: put
  db->Put(write_options, "key2487", "value94305");

  // op 6375: put
  db->Put(write_options, "key5869", "value113040");

  // op 6376: get
  status = db->Get(read_options, "key2158", &value);
  if (status.ok()) {
    out << "GET key2158 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6377: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6963", "key7162", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6963, key7162) size=" << scan_result.size() << std::endl;
  }

  // op 6378: put
  db->Put(write_options, "key2840", "value5551");

  // op 6379: put
  db->Put(write_options, "key5807", "value98331");

  // op 6380: put
  db->Put(write_options, "key10151", "value112732");

  // op 6381: put
  db->Put(write_options, "key9147", "value15379");

  // op 6382: get
  status = db->Get(read_options, "key9959", &value);
  if (status.ok()) {
    out << "GET key9959 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6383: put
  db->Put(write_options, "key9980", "value79161");

  // op 6384: delete
  db->Delete(write_options, "key10152");

  // op 6385: delete_range
  status = db->DeleteRange(write_options, "key4787", "key8241");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 6386: put
  db->Put(write_options, "key8816", "value78155");

  // op 6387: put
  db->Put(write_options, "key7669", "value37370");

  // op 6388: get
  status = db->Get(read_options, "key7793", &value);
  if (status.ok()) {
    out << "GET key7793 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6389: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7942", "key10960", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7942, key10960) size=" << scan_result.size() << std::endl;
  }

  // op 6390: get
  status = db->Get(read_options, "key10373", &value);
  if (status.ok()) {
    out << "GET key10373 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6391: get
  status = db->Get(read_options, "key2651", &value);
  if (status.ok()) {
    out << "GET key2651 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6392: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5981", "key6474", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5981, key6474) size=" << scan_result.size() << std::endl;
  }

  // op 6393: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0808", "key10554", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0808, key10554) size=" << scan_result.size() << std::endl;
  }

  // op 6394: put
  db->Put(write_options, "key2753", "value17504");

  // op 6395: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 6396: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1768", "key2870", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1768, key2870) size=" << scan_result.size() << std::endl;
  }

  // op 6397: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9709", "key11371", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9709, key11371) size=" << scan_result.size() << std::endl;
  }

  // op 6398: get
  status = db->Get(read_options, "key11576", &value);
  if (status.ok()) {
    out << "GET key11576 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6399: put
  db->Put(write_options, "key8137", "value52626");

  // op 6400: delete
  db->Delete(write_options, "key10651");

  // op 6401: put
  db->Put(write_options, "key0874", "value48488");

  // op 6402: get
  status = db->Get(read_options, "key9035", &value);
  if (status.ok()) {
    out << "GET key9035 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6403: delete_range
  status = db->DeleteRange(write_options, "key10335", "key10677");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 6404: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1725", "key5010", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1725, key5010) size=" << scan_result.size() << std::endl;
  }

  // op 6405: put
  db->Put(write_options, "key7559", "value92209");

  // op 6406: delete_range
  status = db->DeleteRange(write_options, "key1946", "key5374");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 6407: delete
  db->Delete(write_options, "key11631");

  // op 6408: put
  db->Put(write_options, "key10631", "value4139");

  // op 6409: put
  db->Put(write_options, "key2824", "value93190");

  // op 6410: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1758", "key6106", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1758, key6106) size=" << scan_result.size() << std::endl;
  }

  // op 6411: put
  db->Put(write_options, "key4098", "value30455");

  // op 6412: put
  db->Put(write_options, "key1815", "value18964");

  // op 6413: put
  db->Put(write_options, "key1526", "value82187");

  // op 6414: put
  db->Put(write_options, "key0828", "value8222");

  // op 6415: put
  db->Put(write_options, "key7789", "value103967");

  // op 6416: put
  db->Put(write_options, "key0742", "value87614");

  // op 6417: put
  db->Put(write_options, "key3253", "value114067");

  // op 6418: put
  db->Put(write_options, "key6222", "value34226");

  // op 6419: put
  db->Put(write_options, "key5820", "value50977");

  // op 6420: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 6421: put
  db->Put(write_options, "key5480", "value20674");

  // op 6422: get
  status = db->Get(read_options, "key10758", &value);
  if (status.ok()) {
    out << "GET key10758 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6423: put
  db->Put(write_options, "key11507", "value115883");

  // op 6424: put
  db->Put(write_options, "key2171", "value6474");

  // op 6425: put
  db->Put(write_options, "key5726", "value102029");

  // op 6426: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6892", "key11474", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6892, key11474) size=" << scan_result.size() << std::endl;
  }

  // op 6427: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4058", "key4750", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4058, key4750) size=" << scan_result.size() << std::endl;
  }

  // op 6428: put
  db->Put(write_options, "key7919", "value57446");

  // op 6429: delete
  db->Delete(write_options, "key3946");

  // op 6430: delete
  db->Delete(write_options, "key4814");

  // op 6431: put
  db->Put(write_options, "key4343", "value36141");

  // op 6432: put
  db->Put(write_options, "key8114", "value68053");

  // op 6433: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 6434: get
  status = db->Get(read_options, "key3061", &value);
  if (status.ok()) {
    out << "GET key3061 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6435: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5574", "key11332", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5574, key11332) size=" << scan_result.size() << std::endl;
  }

  // op 6436: delete_range
  status = db->DeleteRange(write_options, "key1589", "key9205");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 6437: delete_range
  status = db->DeleteRange(write_options, "key2307", "key5337");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 6438: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 6439: put
  db->Put(write_options, "key7823", "value56600");

  // op 6440: put
  db->Put(write_options, "key1916", "value50916");

  // op 6441: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6617", "key9565", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6617, key9565) size=" << scan_result.size() << std::endl;
  }

  // op 6442: put
  db->Put(write_options, "key6758", "value34700");

  // op 6443: get
  status = db->Get(read_options, "key9235", &value);
  if (status.ok()) {
    out << "GET key9235 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6444: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0301", "key6275", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0301, key6275) size=" << scan_result.size() << std::endl;
  }

  // op 6445: put
  db->Put(write_options, "key0679", "value58053");

  // op 6446: put
  db->Put(write_options, "key11664", "value84231");

  // op 6447: put
  db->Put(write_options, "key10410", "value68883");

  // op 6448: put
  db->Put(write_options, "key0330", "value101744");

  // op 6449: get
  status = db->Get(read_options, "key1520", &value);
  if (status.ok()) {
    out << "GET key1520 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6450: put
  db->Put(write_options, "key0845", "value107938");

  // op 6451: delete_range
  status = db->DeleteRange(write_options, "key7716", "key7992");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 6452: delete
  db->Delete(write_options, "key10448");

  // op 6453: put
  db->Put(write_options, "key3842", "value117599");

  // op 6454: get
  status = db->Get(read_options, "key0455", &value);
  if (status.ok()) {
    out << "GET key0455 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6455: put
  db->Put(write_options, "key8589", "value81329");

  // op 6456: delete
  db->Delete(write_options, "key7678");

  // op 6457: get
  status = db->Get(read_options, "key4744", &value);
  if (status.ok()) {
    out << "GET key4744 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6458: get
  status = db->Get(read_options, "key11241", &value);
  if (status.ok()) {
    out << "GET key11241 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6459: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10183", "key10933", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10183, key10933) size=" << scan_result.size() << std::endl;
  }

  // op 6460: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 6461: put
  db->Put(write_options, "key0074", "value14546");

  // op 6462: get
  status = db->Get(read_options, "key1794", &value);
  if (status.ok()) {
    out << "GET key1794 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6463: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3303", "key3476", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3303, key3476) size=" << scan_result.size() << std::endl;
  }

  // op 6464: put
  db->Put(write_options, "key0296", "value43730");

  // op 6465: get
  status = db->Get(read_options, "key9060", &value);
  if (status.ok()) {
    out << "GET key9060 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6466: get
  status = db->Get(read_options, "key10975", &value);
  if (status.ok()) {
    out << "GET key10975 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6467: get
  status = db->Get(read_options, "key2783", &value);
  if (status.ok()) {
    out << "GET key2783 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6468: get
  status = db->Get(read_options, "key3503", &value);
  if (status.ok()) {
    out << "GET key3503 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6469: get
  status = db->Get(read_options, "key11665", &value);
  if (status.ok()) {
    out << "GET key11665 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6470: delete_range
  status = db->DeleteRange(write_options, "key0056", "key10547");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 6471: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1431", "key7082", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1431, key7082) size=" << scan_result.size() << std::endl;
  }

  // op 6472: delete_range
  status = db->DeleteRange(write_options, "key10451", "key10493");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 6473: put
  db->Put(write_options, "key8706", "value113410");

  // op 6474: get
  status = db->Get(read_options, "key10289", &value);
  if (status.ok()) {
    out << "GET key10289 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6475: put
  db->Put(write_options, "key6599", "value31746");

  // op 6476: get
  status = db->Get(read_options, "key1395", &value);
  if (status.ok()) {
    out << "GET key1395 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6477: get
  status = db->Get(read_options, "key6536", &value);
  if (status.ok()) {
    out << "GET key6536 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6478: put
  db->Put(write_options, "key3746", "value107783");

  // op 6479: get
  status = db->Get(read_options, "key4643", &value);
  if (status.ok()) {
    out << "GET key4643 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6480: put
  db->Put(write_options, "key7319", "value69992");

  // op 6481: delete_range
  status = db->DeleteRange(write_options, "key5343", "key8835");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 6482: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 6483: put
  db->Put(write_options, "key3759", "value78417");

  // op 6484: delete
  db->Delete(write_options, "key3975");

  // op 6485: put
  db->Put(write_options, "key6462", "value27691");

  // op 6486: get
  status = db->Get(read_options, "key1212", &value);
  if (status.ok()) {
    out << "GET key1212 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6487: get
  status = db->Get(read_options, "key1446", &value);
  if (status.ok()) {
    out << "GET key1446 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6488: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3393", "key9577", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3393, key9577) size=" << scan_result.size() << std::endl;
  }

  // op 6489: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5523", "key8263", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5523, key8263) size=" << scan_result.size() << std::endl;
  }

  // op 6490: put
  db->Put(write_options, "key4039", "value53091");

  // op 6491: put
  db->Put(write_options, "key8448", "value95593");

  // op 6492: delete_range
  status = db->DeleteRange(write_options, "key8058", "key9861");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 6493: put
  db->Put(write_options, "key3664", "value102025");

  // op 6494: put
  db->Put(write_options, "key8026", "value50172");

  // op 6495: put
  db->Put(write_options, "key11537", "value48752");

  // op 6496: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4612", "key5888", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4612, key5888) size=" << scan_result.size() << std::endl;
  }

  // op 6497: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6571", "key10040", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6571, key10040) size=" << scan_result.size() << std::endl;
  }

  // op 6498: get
  status = db->Get(read_options, "key7356", &value);
  if (status.ok()) {
    out << "GET key7356 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6499: get
  status = db->Get(read_options, "key5714", &value);
  if (status.ok()) {
    out << "GET key5714 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6500: put
  db->Put(write_options, "key1582", "value109573");

  // op 6501: get
  status = db->Get(read_options, "key7690", &value);
  if (status.ok()) {
    out << "GET key7690 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6502: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2585", "key5327", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2585, key5327) size=" << scan_result.size() << std::endl;
  }

  // op 6503: delete_range
  status = db->DeleteRange(write_options, "key5201", "key8812");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 6504: put
  db->Put(write_options, "key0259", "value78944");

  // op 6505: put
  db->Put(write_options, "key0174", "value65834");

  // op 6506: get
  status = db->Get(read_options, "key4605", &value);
  if (status.ok()) {
    out << "GET key4605 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6507: put
  db->Put(write_options, "key8145", "value96588");

  // op 6508: get
  status = db->Get(read_options, "key8810", &value);
  if (status.ok()) {
    out << "GET key8810 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6509: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2858", "key7648", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2858, key7648) size=" << scan_result.size() << std::endl;
  }

  // op 6510: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1502", "key3722", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1502, key3722) size=" << scan_result.size() << std::endl;
  }

  // op 6511: put
  db->Put(write_options, "key0257", "value102706");

  // op 6512: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 6513: put
  db->Put(write_options, "key11019", "value30421");

  // op 6514: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7075", "key8585", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7075, key8585) size=" << scan_result.size() << std::endl;
  }

  // op 6515: delete_range
  status = db->DeleteRange(write_options, "key9555", "key9701");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 6516: put
  db->Put(write_options, "key1672", "value76826");

  // op 6517: put
  db->Put(write_options, "key6655", "value93513");

  // op 6518: delete
  db->Delete(write_options, "key6004");

  // op 6519: delete
  db->Delete(write_options, "key7936");

  // op 6520: get
  status = db->Get(read_options, "key1586", &value);
  if (status.ok()) {
    out << "GET key1586 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6521: put
  db->Put(write_options, "key7955", "value87191");

  // op 6522: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5365", "key7710", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5365, key7710) size=" << scan_result.size() << std::endl;
  }

  // op 6523: get
  status = db->Get(read_options, "key0984", &value);
  if (status.ok()) {
    out << "GET key0984 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6524: put
  db->Put(write_options, "key8409", "value90459");

  // op 6525: put
  db->Put(write_options, "key10335", "value100321");

  // op 6526: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 6527: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0178", "key7612", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0178, key7612) size=" << scan_result.size() << std::endl;
  }

  // op 6528: get
  status = db->Get(read_options, "key4273", &value);
  if (status.ok()) {
    out << "GET key4273 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6529: put
  db->Put(write_options, "key9904", "value61080");

  // op 6530: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 6531: put
  db->Put(write_options, "key8500", "value22233");

  // op 6532: delete_range
  status = db->DeleteRange(write_options, "key1572", "key10829");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 6533: put
  db->Put(write_options, "key4167", "value31664");

  // op 6534: put
  db->Put(write_options, "key2770", "value43205");

  // op 6535: get
  status = db->Get(read_options, "key10113", &value);
  if (status.ok()) {
    out << "GET key10113 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6536: put
  db->Put(write_options, "key6377", "value35702");

  // op 6537: delete_range
  status = db->DeleteRange(write_options, "key6061", "key6724");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 6538: get
  status = db->Get(read_options, "key5087", &value);
  if (status.ok()) {
    out << "GET key5087 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6539: get
  status = db->Get(read_options, "key2611", &value);
  if (status.ok()) {
    out << "GET key2611 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6540: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3439", "key6317", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3439, key6317) size=" << scan_result.size() << std::endl;
  }

  // op 6541: put
  db->Put(write_options, "key10257", "value92858");

  // op 6542: put
  db->Put(write_options, "key7104", "value11219");

  // op 6543: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0325", "key4554", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0325, key4554) size=" << scan_result.size() << std::endl;
  }

  // op 6544: get
  status = db->Get(read_options, "key3761", &value);
  if (status.ok()) {
    out << "GET key3761 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6545: put
  db->Put(write_options, "key1653", "value23210");

  // op 6546: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2773", "key5508", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2773, key5508) size=" << scan_result.size() << std::endl;
  }

  // op 6547: put
  db->Put(write_options, "key0244", "value114315");

  // op 6548: put
  db->Put(write_options, "key8355", "value12282");

  // op 6549: delete
  db->Delete(write_options, "key10206");

  // op 6550: delete_range
  status = db->DeleteRange(write_options, "key6423", "key6970");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 6551: put
  db->Put(write_options, "key9401", "value87214");

  // op 6552: put
  db->Put(write_options, "key1577", "value62801");

  // op 6553: put
  db->Put(write_options, "key9583", "value18962");

  // op 6554: delete_range
  status = db->DeleteRange(write_options, "key0734", "key8860");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 6555: put
  db->Put(write_options, "key4749", "value104909");

  // op 6556: get
  status = db->Get(read_options, "key5318", &value);
  if (status.ok()) {
    out << "GET key5318 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6557: put
  db->Put(write_options, "key1851", "value15194");

  // op 6558: put
  db->Put(write_options, "key0868", "value52682");

  // op 6559: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0032", "key6743", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0032, key6743) size=" << scan_result.size() << std::endl;
  }

  // op 6560: delete_range
  status = db->DeleteRange(write_options, "key2245", "key4495");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 6561: delete
  db->Delete(write_options, "key7129");

  // op 6562: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5027", "key8143", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5027, key8143) size=" << scan_result.size() << std::endl;
  }

  // op 6563: get
  status = db->Get(read_options, "key6024", &value);
  if (status.ok()) {
    out << "GET key6024 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6564: get
  status = db->Get(read_options, "key9860", &value);
  if (status.ok()) {
    out << "GET key9860 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6565: put
  db->Put(write_options, "key4493", "value51443");

  // op 6566: put
  db->Put(write_options, "key8169", "value42762");

  // op 6567: get
  status = db->Get(read_options, "key9015", &value);
  if (status.ok()) {
    out << "GET key9015 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6568: get
  status = db->Get(read_options, "key3062", &value);
  if (status.ok()) {
    out << "GET key3062 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6569: get
  status = db->Get(read_options, "key5524", &value);
  if (status.ok()) {
    out << "GET key5524 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6570: get
  status = db->Get(read_options, "key8294", &value);
  if (status.ok()) {
    out << "GET key8294 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6571: put
  db->Put(write_options, "key0744", "value9981");

  // op 6572: put
  db->Put(write_options, "key6915", "value91322");

  // op 6573: delete
  db->Delete(write_options, "key9884");

  // op 6574: put
  db->Put(write_options, "key3626", "value79800");

  // op 6575: put
  db->Put(write_options, "key3646", "value12615");

  // op 6576: get
  status = db->Get(read_options, "key4377", &value);
  if (status.ok()) {
    out << "GET key4377 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6577: delete
  db->Delete(write_options, "key3027");

  // op 6578: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4943", "key7465", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4943, key7465) size=" << scan_result.size() << std::endl;
  }

  // op 6579: get
  status = db->Get(read_options, "key4915", &value);
  if (status.ok()) {
    out << "GET key4915 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6580: put
  db->Put(write_options, "key8964", "value10392");

  // op 6581: delete
  db->Delete(write_options, "key8437");

  // op 6582: get
  status = db->Get(read_options, "key6119", &value);
  if (status.ok()) {
    out << "GET key6119 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6583: get
  status = db->Get(read_options, "key0319", &value);
  if (status.ok()) {
    out << "GET key0319 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6584: put
  db->Put(write_options, "key1998", "value74365");

  // op 6585: put
  db->Put(write_options, "key2003", "value44467");

  // op 6586: delete_range
  status = db->DeleteRange(write_options, "key3303", "key10997");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 6587: put
  db->Put(write_options, "key6742", "value41368");

  // op 6588: delete_range
  status = db->DeleteRange(write_options, "key6086", "key8272");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 6589: delete
  db->Delete(write_options, "key5796");

  // op 6590: get
  status = db->Get(read_options, "key7520", &value);
  if (status.ok()) {
    out << "GET key7520 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6591: get
  status = db->Get(read_options, "key9764", &value);
  if (status.ok()) {
    out << "GET key9764 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6592: put
  db->Put(write_options, "key8091", "value104356");

  // op 6593: put
  db->Put(write_options, "key0473", "value9472");

  // op 6594: delete_range
  status = db->DeleteRange(write_options, "key2794", "key10712");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 6595: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4056", "key7301", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4056, key7301) size=" << scan_result.size() << std::endl;
  }

  // op 6596: put
  db->Put(write_options, "key3228", "value37783");

  // op 6597: delete_range
  status = db->DeleteRange(write_options, "key3236", "key9427");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 6598: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 6599: get
  status = db->Get(read_options, "key1283", &value);
  if (status.ok()) {
    out << "GET key1283 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6600: delete
  db->Delete(write_options, "key5883");

  // op 6601: delete_range
  status = db->DeleteRange(write_options, "key0128", "key3845");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 6602: put
  db->Put(write_options, "key11268", "value17515");

  // op 6603: put
  db->Put(write_options, "key7095", "value41132");

  // op 6604: get
  status = db->Get(read_options, "key2102", &value);
  if (status.ok()) {
    out << "GET key2102 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6605: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5709", "key6666", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5709, key6666) size=" << scan_result.size() << std::endl;
  }

  // op 6606: get
  status = db->Get(read_options, "key3171", &value);
  if (status.ok()) {
    out << "GET key3171 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6607: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 6608: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5868", "key8615", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5868, key8615) size=" << scan_result.size() << std::endl;
  }

  // op 6609: put
  db->Put(write_options, "key10419", "value71192");

  // op 6610: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9703", "key11517", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9703, key11517) size=" << scan_result.size() << std::endl;
  }

  // op 6611: put
  db->Put(write_options, "key0651", "value41000");

  // op 6612: put
  db->Put(write_options, "key2210", "value32016");

  // op 6613: put
  db->Put(write_options, "key10382", "value25294");

  // op 6614: get
  status = db->Get(read_options, "key0231", &value);
  if (status.ok()) {
    out << "GET key0231 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6615: delete
  db->Delete(write_options, "key3745");

  // op 6616: put
  db->Put(write_options, "key10161", "value61419");

  // op 6617: put
  db->Put(write_options, "key1961", "value71337");

  // op 6618: put
  db->Put(write_options, "key4245", "value82084");

  // op 6619: put
  db->Put(write_options, "key10304", "value21428");

  // op 6620: get
  status = db->Get(read_options, "key5750", &value);
  if (status.ok()) {
    out << "GET key5750 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6621: put
  db->Put(write_options, "key3984", "value95570");

  // op 6622: put
  db->Put(write_options, "key5609", "value82564");

  // op 6623: get
  status = db->Get(read_options, "key5179", &value);
  if (status.ok()) {
    out << "GET key5179 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6624: get
  status = db->Get(read_options, "key10704", &value);
  if (status.ok()) {
    out << "GET key10704 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6625: delete
  db->Delete(write_options, "key11105");

  // op 6626: delete
  db->Delete(write_options, "key3005");

  // op 6627: get
  status = db->Get(read_options, "key1185", &value);
  if (status.ok()) {
    out << "GET key1185 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6628: put
  db->Put(write_options, "key10584", "value56994");

  // op 6629: get
  status = db->Get(read_options, "key10281", &value);
  if (status.ok()) {
    out << "GET key10281 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6630: get
  status = db->Get(read_options, "key11318", &value);
  if (status.ok()) {
    out << "GET key11318 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6631: delete
  db->Delete(write_options, "key0283");

  // op 6632: delete_range
  status = db->DeleteRange(write_options, "key10481", "key11005");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 6633: put
  db->Put(write_options, "key10620", "value93657");

  // op 6634: put
  db->Put(write_options, "key2946", "value21901");

  // op 6635: put
  db->Put(write_options, "key0078", "value67339");

  // op 6636: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5801", "key7301", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5801, key7301) size=" << scan_result.size() << std::endl;
  }

  // op 6637: put
  db->Put(write_options, "key6424", "value40808");

  // op 6638: put
  db->Put(write_options, "key0962", "value63217");

  // op 6639: get
  status = db->Get(read_options, "key10736", &value);
  if (status.ok()) {
    out << "GET key10736 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6640: get
  status = db->Get(read_options, "key11556", &value);
  if (status.ok()) {
    out << "GET key11556 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6641: delete_range
  status = db->DeleteRange(write_options, "key1135", "key9411");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 6642: put
  db->Put(write_options, "key0071", "value17093");

  // op 6643: get
  status = db->Get(read_options, "key9061", &value);
  if (status.ok()) {
    out << "GET key9061 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6644: put
  db->Put(write_options, "key9114", "value117962");

  // op 6645: put
  db->Put(write_options, "key0844", "value20929");

  // op 6646: put
  db->Put(write_options, "key7259", "value101369");

  // op 6647: delete_range
  status = db->DeleteRange(write_options, "key0367", "key10431");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 6648: put
  db->Put(write_options, "key5789", "value63158");

  // op 6649: put
  db->Put(write_options, "key10168", "value110574");

  // op 6650: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 6651: put
  db->Put(write_options, "key0544", "value109164");

  // op 6652: put
  db->Put(write_options, "key0632", "value104924");

  // op 6653: get
  status = db->Get(read_options, "key1703", &value);
  if (status.ok()) {
    out << "GET key1703 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6654: get
  status = db->Get(read_options, "key9828", &value);
  if (status.ok()) {
    out << "GET key9828 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6655: put
  db->Put(write_options, "key7374", "value97698");

  // op 6656: put
  db->Put(write_options, "key0602", "value55603");

  // op 6657: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6784", "key10223", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6784, key10223) size=" << scan_result.size() << std::endl;
  }

  // op 6658: put
  db->Put(write_options, "key9419", "value0828");

  // op 6659: put
  db->Put(write_options, "key6164", "value103253");

  // op 6660: put
  db->Put(write_options, "key8239", "value5312");

  // op 6661: delete
  db->Delete(write_options, "key7193");

  // op 6662: put
  db->Put(write_options, "key1304", "value74808");

  // op 6663: get
  status = db->Get(read_options, "key0494", &value);
  if (status.ok()) {
    out << "GET key0494 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6664: put
  db->Put(write_options, "key8297", "value73004");

  // op 6665: delete
  db->Delete(write_options, "key7525");

  // op 6666: put
  db->Put(write_options, "key10541", "value15543");

  // op 6667: put
  db->Put(write_options, "key10761", "value16988");

  // op 6668: delete
  db->Delete(write_options, "key11222");

  // op 6669: delete_range
  status = db->DeleteRange(write_options, "key0674", "key6588");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 6670: put
  db->Put(write_options, "key8612", "value76347");

  // op 6671: get
  status = db->Get(read_options, "key5697", &value);
  if (status.ok()) {
    out << "GET key5697 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6672: put
  db->Put(write_options, "key5185", "value68620");

  // op 6673: put
  db->Put(write_options, "key3438", "value89187");

  // op 6674: get
  status = db->Get(read_options, "key0566", &value);
  if (status.ok()) {
    out << "GET key0566 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6675: put
  db->Put(write_options, "key10512", "value67857");

  // op 6676: get
  status = db->Get(read_options, "key7437", &value);
  if (status.ok()) {
    out << "GET key7437 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6677: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2988", "key8297", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2988, key8297) size=" << scan_result.size() << std::endl;
  }

  // op 6678: get
  status = db->Get(read_options, "key5459", &value);
  if (status.ok()) {
    out << "GET key5459 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6679: delete
  db->Delete(write_options, "key6212");

  // op 6680: get
  status = db->Get(read_options, "key10423", &value);
  if (status.ok()) {
    out << "GET key10423 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6681: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8039", "key8090", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8039, key8090) size=" << scan_result.size() << std::endl;
  }

  // op 6682: put
  db->Put(write_options, "key3603", "value81540");

  // op 6683: get
  status = db->Get(read_options, "key9612", &value);
  if (status.ok()) {
    out << "GET key9612 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6684: put
  db->Put(write_options, "key6545", "value3281");

  // op 6685: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 6686: put
  db->Put(write_options, "key9851", "value92888");

  // op 6687: delete
  db->Delete(write_options, "key2621");

  // op 6688: delete_range
  status = db->DeleteRange(write_options, "key1455", "key3068");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 6689: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6970", "key8634", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6970, key8634) size=" << scan_result.size() << std::endl;
  }

  // op 6690: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3321", "key9308", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3321, key9308) size=" << scan_result.size() << std::endl;
  }

  // op 6691: delete
  db->Delete(write_options, "key1703");

  // op 6692: put
  db->Put(write_options, "key1180", "value41645");

  // op 6693: put
  db->Put(write_options, "key6671", "value87017");

  // op 6694: get
  status = db->Get(read_options, "key3182", &value);
  if (status.ok()) {
    out << "GET key3182 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6695: delete
  db->Delete(write_options, "key0244");

  // op 6696: put
  db->Put(write_options, "key2955", "value61480");

  // op 6697: get
  status = db->Get(read_options, "key9411", &value);
  if (status.ok()) {
    out << "GET key9411 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6698: put
  db->Put(write_options, "key11626", "value20836");

  // op 6699: get
  status = db->Get(read_options, "key5069", &value);
  if (status.ok()) {
    out << "GET key5069 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6700: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 6701: put
  db->Put(write_options, "key8902", "value39842");

  // op 6702: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1372", "key9940", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1372, key9940) size=" << scan_result.size() << std::endl;
  }

  // op 6703: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 6704: get
  status = db->Get(read_options, "key4573", &value);
  if (status.ok()) {
    out << "GET key4573 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6705: delete
  db->Delete(write_options, "key3238");

  // op 6706: get
  status = db->Get(read_options, "key10538", &value);
  if (status.ok()) {
    out << "GET key10538 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6707: get
  status = db->Get(read_options, "key2848", &value);
  if (status.ok()) {
    out << "GET key2848 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6708: put
  db->Put(write_options, "key1920", "value57338");

  // op 6709: delete_range
  status = db->DeleteRange(write_options, "key4713", "key6199");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 6710: put
  db->Put(write_options, "key8565", "value96890");

  // op 6711: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 6712: delete_range
  status = db->DeleteRange(write_options, "key0574", "key4844");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 6713: put
  db->Put(write_options, "key3458", "value5813");

  // op 6714: put
  db->Put(write_options, "key2513", "value45915");

  // op 6715: put
  db->Put(write_options, "key6157", "value100962");

  // op 6716: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4957", "key11186", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4957, key11186) size=" << scan_result.size() << std::endl;
  }

  // op 6717: put
  db->Put(write_options, "key7945", "value2980");

  // op 6718: put
  db->Put(write_options, "key3778", "value39447");

  // op 6719: get
  status = db->Get(read_options, "key7153", &value);
  if (status.ok()) {
    out << "GET key7153 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6720: delete
  db->Delete(write_options, "key8223");

  // op 6721: put
  db->Put(write_options, "key4240", "value102707");

  // op 6722: put
  db->Put(write_options, "key1289", "value39212");

  // op 6723: put
  db->Put(write_options, "key8255", "value32904");

  // op 6724: get
  status = db->Get(read_options, "key3557", &value);
  if (status.ok()) {
    out << "GET key3557 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6725: put
  db->Put(write_options, "key2410", "value2011");

  // op 6726: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6327", "key10273", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6327, key10273) size=" << scan_result.size() << std::endl;
  }

  // op 6727: put
  db->Put(write_options, "key2702", "value16081");

  // op 6728: put
  db->Put(write_options, "key1192", "value38035");

  // op 6729: get
  status = db->Get(read_options, "key5367", &value);
  if (status.ok()) {
    out << "GET key5367 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6730: put
  db->Put(write_options, "key8170", "value47417");

  // op 6731: delete
  db->Delete(write_options, "key11702");

  // op 6732: put
  db->Put(write_options, "key3360", "value90732");

  // op 6733: delete
  db->Delete(write_options, "key11721");

  // op 6734: put
  db->Put(write_options, "key7866", "value74861");

  // op 6735: delete_range
  status = db->DeleteRange(write_options, "key1950", "key11218");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 6736: put
  db->Put(write_options, "key5662", "value8469");

  // op 6737: put
  db->Put(write_options, "key3287", "value101035");

  // op 6738: put
  db->Put(write_options, "key8798", "value35978");

  // op 6739: get
  status = db->Get(read_options, "key4369", &value);
  if (status.ok()) {
    out << "GET key4369 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6740: delete
  db->Delete(write_options, "key3526");

  // op 6741: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6555", "key8445", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6555, key8445) size=" << scan_result.size() << std::endl;
  }

  // op 6742: put
  db->Put(write_options, "key4588", "value118187");

  // op 6743: put
  db->Put(write_options, "key0345", "value113636");

  // op 6744: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5071", "key9110", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5071, key9110) size=" << scan_result.size() << std::endl;
  }

  // op 6745: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 6746: delete_range
  status = db->DeleteRange(write_options, "key1735", "key7677");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 6747: put
  db->Put(write_options, "key9593", "value12374");

  // op 6748: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 6749: put
  db->Put(write_options, "key0049", "value104943");

  // op 6750: put
  db->Put(write_options, "key0219", "value91244");

  // op 6751: get
  status = db->Get(read_options, "key0334", &value);
  if (status.ok()) {
    out << "GET key0334 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6752: delete_range
  status = db->DeleteRange(write_options, "key2481", "key8018");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 6753: put
  db->Put(write_options, "key0839", "value4309");

  // op 6754: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 6755: put
  db->Put(write_options, "key0737", "value62199");

  // op 6756: delete_range
  status = db->DeleteRange(write_options, "key6029", "key7408");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 6757: delete_range
  status = db->DeleteRange(write_options, "key11488", "key11533");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 6758: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0876", "key8546", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0876, key8546) size=" << scan_result.size() << std::endl;
  }

  // op 6759: get
  status = db->Get(read_options, "key8666", &value);
  if (status.ok()) {
    out << "GET key8666 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6760: get
  status = db->Get(read_options, "key5713", &value);
  if (status.ok()) {
    out << "GET key5713 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6761: get
  status = db->Get(read_options, "key1406", &value);
  if (status.ok()) {
    out << "GET key1406 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6762: get
  status = db->Get(read_options, "key4316", &value);
  if (status.ok()) {
    out << "GET key4316 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6763: get
  status = db->Get(read_options, "key5130", &value);
  if (status.ok()) {
    out << "GET key5130 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6764: put
  db->Put(write_options, "key8294", "value5374");

  // op 6765: put
  db->Put(write_options, "key5594", "value111540");

  // op 6766: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 6767: put
  db->Put(write_options, "key9948", "value112026");

  // op 6768: get
  status = db->Get(read_options, "key6057", &value);
  if (status.ok()) {
    out << "GET key6057 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6769: put
  db->Put(write_options, "key0928", "value102940");

  // op 6770: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 6771: put
  db->Put(write_options, "key11321", "value63610");

  // op 6772: delete
  db->Delete(write_options, "key3477");

  // op 6773: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 6774: put
  db->Put(write_options, "key0513", "value62913");

  // op 6775: put
  db->Put(write_options, "key8656", "value35485");

  // op 6776: delete
  db->Delete(write_options, "key9099");

  // op 6777: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 6778: put
  db->Put(write_options, "key8968", "value28996");

  // op 6779: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3238", "key7733", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3238, key7733) size=" << scan_result.size() << std::endl;
  }

  // op 6780: put
  db->Put(write_options, "key9101", "value20930");

  // op 6781: delete_range
  status = db->DeleteRange(write_options, "key4909", "key5456");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 6782: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 6783: put
  db->Put(write_options, "key9444", "value91004");

  // op 6784: delete_range
  status = db->DeleteRange(write_options, "key4784", "key8526");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 6785: put
  db->Put(write_options, "key10391", "value76001");

  // op 6786: delete_range
  status = db->DeleteRange(write_options, "key2958", "key9993");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 6787: put
  db->Put(write_options, "key2287", "value54384");

  // op 6788: put
  db->Put(write_options, "key8726", "value87204");

  // op 6789: get
  status = db->Get(read_options, "key2803", &value);
  if (status.ok()) {
    out << "GET key2803 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6790: put
  db->Put(write_options, "key0003", "value66088");

  // op 6791: put
  db->Put(write_options, "key4100", "value96671");

  // op 6792: get
  status = db->Get(read_options, "key2257", &value);
  if (status.ok()) {
    out << "GET key2257 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6793: get
  status = db->Get(read_options, "key6931", &value);
  if (status.ok()) {
    out << "GET key6931 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6794: put
  db->Put(write_options, "key1415", "value41877");

  // op 6795: delete
  db->Delete(write_options, "key8144");

  // op 6796: get
  status = db->Get(read_options, "key5235", &value);
  if (status.ok()) {
    out << "GET key5235 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6797: put
  db->Put(write_options, "key9296", "value96989");

  // op 6798: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5188", "key10822", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5188, key10822) size=" << scan_result.size() << std::endl;
  }

  // op 6799: delete
  db->Delete(write_options, "key5985");

  // op 6800: delete
  db->Delete(write_options, "key4138");

  // op 6801: put
  db->Put(write_options, "key7346", "value70570");

  // op 6802: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2687", "key8725", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2687, key8725) size=" << scan_result.size() << std::endl;
  }

  // op 6803: get
  status = db->Get(read_options, "key1242", &value);
  if (status.ok()) {
    out << "GET key1242 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6804: put
  db->Put(write_options, "key10357", "value46907");

  // op 6805: delete_range
  status = db->DeleteRange(write_options, "key0264", "key10701");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 6806: put
  db->Put(write_options, "key10027", "value115665");

  // op 6807: put
  db->Put(write_options, "key7208", "value78408");

  // op 6808: delete_range
  status = db->DeleteRange(write_options, "key3646", "key7197");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 6809: delete
  db->Delete(write_options, "key7952");

  // op 6810: put
  db->Put(write_options, "key5218", "value38789");

  // op 6811: put
  db->Put(write_options, "key4420", "value66666");

  // op 6812: get
  status = db->Get(read_options, "key0735", &value);
  if (status.ok()) {
    out << "GET key0735 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6813: get
  status = db->Get(read_options, "key6777", &value);
  if (status.ok()) {
    out << "GET key6777 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6814: put
  db->Put(write_options, "key9393", "value56879");

  // op 6815: put
  db->Put(write_options, "key8928", "value71264");

  // op 6816: delete
  db->Delete(write_options, "key0120");

  // op 6817: delete
  db->Delete(write_options, "key1725");

  // op 6818: put
  db->Put(write_options, "key6585", "value22294");

  // op 6819: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1579", "key10077", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1579, key10077) size=" << scan_result.size() << std::endl;
  }

  // op 6820: put
  db->Put(write_options, "key2081", "value6328");

  // op 6821: delete
  db->Delete(write_options, "key4232");

  // op 6822: get
  status = db->Get(read_options, "key1416", &value);
  if (status.ok()) {
    out << "GET key1416 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6823: delete
  db->Delete(write_options, "key8255");

  // op 6824: put
  db->Put(write_options, "key3872", "value54499");

  // op 6825: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0961", "key6229", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0961, key6229) size=" << scan_result.size() << std::endl;
  }

  // op 6826: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 6827: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 6828: put
  db->Put(write_options, "key1230", "value39868");

  // op 6829: put
  db->Put(write_options, "key0107", "value44178");

  // op 6830: put
  db->Put(write_options, "key8312", "value92992");

  // op 6831: get
  status = db->Get(read_options, "key6030", &value);
  if (status.ok()) {
    out << "GET key6030 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6832: get
  status = db->Get(read_options, "key6386", &value);
  if (status.ok()) {
    out << "GET key6386 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6833: delete
  db->Delete(write_options, "key8257");

  // op 6834: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4225", "key10519", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4225, key10519) size=" << scan_result.size() << std::endl;
  }

  // op 6835: put
  db->Put(write_options, "key0344", "value16891");

  // op 6836: put
  db->Put(write_options, "key6845", "value10171");

  // op 6837: delete
  db->Delete(write_options, "key8281");

  // op 6838: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0705", "key6168", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0705, key6168) size=" << scan_result.size() << std::endl;
  }

  // op 6839: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3052", "key5213", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3052, key5213) size=" << scan_result.size() << std::endl;
  }

  // op 6840: put
  db->Put(write_options, "key1868", "value60724");

  // op 6841: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 6842: get
  status = db->Get(read_options, "key6525", &value);
  if (status.ok()) {
    out << "GET key6525 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6843: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6035", "key6162", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6035, key6162) size=" << scan_result.size() << std::endl;
  }

  // op 6844: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 6845: put
  db->Put(write_options, "key5299", "value55533");

  // op 6846: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3999", "key8331", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3999, key8331) size=" << scan_result.size() << std::endl;
  }

  // op 6847: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4913", "key7490", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4913, key7490) size=" << scan_result.size() << std::endl;
  }

  // op 6848: put
  db->Put(write_options, "key1109", "value62213");

  // op 6849: put
  db->Put(write_options, "key6715", "value72422");

  // op 6850: delete_range
  status = db->DeleteRange(write_options, "key6068", "key10065");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 6851: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1470", "key2948", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1470, key2948) size=" << scan_result.size() << std::endl;
  }

  // op 6852: put
  db->Put(write_options, "key3717", "value59784");

  // op 6853: delete_range
  status = db->DeleteRange(write_options, "key4465", "key8198");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 6854: delete_range
  status = db->DeleteRange(write_options, "key3212", "key9257");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 6855: get
  status = db->Get(read_options, "key0611", &value);
  if (status.ok()) {
    out << "GET key0611 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6856: put
  db->Put(write_options, "key4061", "value87630");

  // op 6857: delete_range
  status = db->DeleteRange(write_options, "key4974", "key7531");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 6858: put
  db->Put(write_options, "key10838", "value106981");

  // op 6859: get
  status = db->Get(read_options, "key10542", &value);
  if (status.ok()) {
    out << "GET key10542 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6860: get
  status = db->Get(read_options, "key0485", &value);
  if (status.ok()) {
    out << "GET key0485 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6861: delete
  db->Delete(write_options, "key6162");

  // op 6862: get
  status = db->Get(read_options, "key10450", &value);
  if (status.ok()) {
    out << "GET key10450 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6863: get
  status = db->Get(read_options, "key5582", &value);
  if (status.ok()) {
    out << "GET key5582 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6864: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0110", "key5318", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0110, key5318) size=" << scan_result.size() << std::endl;
  }

  // op 6865: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1803", "key2426", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1803, key2426) size=" << scan_result.size() << std::endl;
  }

  // op 6866: put
  db->Put(write_options, "key5340", "value85722");

  // op 6867: get
  status = db->Get(read_options, "key1285", &value);
  if (status.ok()) {
    out << "GET key1285 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6868: get
  status = db->Get(read_options, "key1381", &value);
  if (status.ok()) {
    out << "GET key1381 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6869: delete
  db->Delete(write_options, "key1804");

  // op 6870: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 6871: delete
  db->Delete(write_options, "key0190");

  // op 6872: put
  db->Put(write_options, "key11588", "value31695");

  // op 6873: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3378", "key9551", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3378, key9551) size=" << scan_result.size() << std::endl;
  }

  // op 6874: put
  db->Put(write_options, "key11580", "value109803");

  // op 6875: put
  db->Put(write_options, "key3955", "value13337");

  // op 6876: delete
  db->Delete(write_options, "key9472");

  // op 6877: put
  db->Put(write_options, "key11456", "value102204");

  // op 6878: put
  db->Put(write_options, "key10364", "value56556");

  // op 6879: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7151", "key8421", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7151, key8421) size=" << scan_result.size() << std::endl;
  }

  // op 6880: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5187", "key8469", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5187, key8469) size=" << scan_result.size() << std::endl;
  }

  // op 6881: get
  status = db->Get(read_options, "key9839", &value);
  if (status.ok()) {
    out << "GET key9839 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6882: get
  status = db->Get(read_options, "key11476", &value);
  if (status.ok()) {
    out << "GET key11476 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6883: get
  status = db->Get(read_options, "key6203", &value);
  if (status.ok()) {
    out << "GET key6203 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6884: delete_range
  status = db->DeleteRange(write_options, "key8554", "key11692");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 6885: delete_range
  status = db->DeleteRange(write_options, "key1974", "key8365");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 6886: delete
  db->Delete(write_options, "key5418");

  // op 6887: put
  db->Put(write_options, "key7326", "value75420");

  // op 6888: put
  db->Put(write_options, "key4810", "value62946");

  // op 6889: put
  db->Put(write_options, "key3573", "value110659");

  // op 6890: delete_range
  status = db->DeleteRange(write_options, "key3237", "key7734");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 6891: put
  db->Put(write_options, "key11460", "value41330");

  // op 6892: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3038", "key8070", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3038, key8070) size=" << scan_result.size() << std::endl;
  }

  // op 6893: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2446", "key10191", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2446, key10191) size=" << scan_result.size() << std::endl;
  }

  // op 6894: put
  db->Put(write_options, "key4917", "value88298");

  // op 6895: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2228", "key10495", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2228, key10495) size=" << scan_result.size() << std::endl;
  }

  // op 6896: put
  db->Put(write_options, "key10310", "value42256");

  // op 6897: get
  status = db->Get(read_options, "key8033", &value);
  if (status.ok()) {
    out << "GET key8033 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6898: put
  db->Put(write_options, "key7976", "value28865");

  // op 6899: delete_range
  status = db->DeleteRange(write_options, "key5960", "key10314");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 6900: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 6901: get
  status = db->Get(read_options, "key6321", &value);
  if (status.ok()) {
    out << "GET key6321 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6902: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3223", "key7399", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3223, key7399) size=" << scan_result.size() << std::endl;
  }

  // op 6903: put
  db->Put(write_options, "key10560", "value116526");

  // op 6904: put
  db->Put(write_options, "key1494", "value80626");

  // op 6905: put
  db->Put(write_options, "key11642", "value62220");

  // op 6906: put
  db->Put(write_options, "key9117", "value93800");

  // op 6907: put
  db->Put(write_options, "key10306", "value43987");

  // op 6908: get
  status = db->Get(read_options, "key2384", &value);
  if (status.ok()) {
    out << "GET key2384 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6909: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 6910: put
  db->Put(write_options, "key3889", "value33941");

  // op 6911: put
  db->Put(write_options, "key5663", "value81030");

  // op 6912: put
  db->Put(write_options, "key8668", "value5444");

  // op 6913: get
  status = db->Get(read_options, "key8122", &value);
  if (status.ok()) {
    out << "GET key8122 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6914: get
  status = db->Get(read_options, "key10410", &value);
  if (status.ok()) {
    out << "GET key10410 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6915: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7108", "key9432", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7108, key9432) size=" << scan_result.size() << std::endl;
  }

  // op 6916: put
  db->Put(write_options, "key10515", "value29956");

  // op 6917: put
  db->Put(write_options, "key3024", "value27088");

  // op 6918: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5081", "key5902", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5081, key5902) size=" << scan_result.size() << std::endl;
  }

  // op 6919: delete_range
  status = db->DeleteRange(write_options, "key5879", "key7731");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 6920: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 6921: delete
  db->Delete(write_options, "key10999");

  // op 6922: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2438", "key4804", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2438, key4804) size=" << scan_result.size() << std::endl;
  }

  // op 6923: put
  db->Put(write_options, "key2690", "value52019");

  // op 6924: delete_range
  status = db->DeleteRange(write_options, "key2293", "key5075");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 6925: put
  db->Put(write_options, "key11023", "value22411");

  // op 6926: put
  db->Put(write_options, "key1772", "value50742");

  // op 6927: delete_range
  status = db->DeleteRange(write_options, "key7374", "key8311");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 6928: put
  db->Put(write_options, "key6818", "value58665");

  // op 6929: delete
  db->Delete(write_options, "key3367");

  // op 6930: put
  db->Put(write_options, "key10130", "value49547");

  // op 6931: put
  db->Put(write_options, "key11653", "value82882");

  // op 6932: delete_range
  status = db->DeleteRange(write_options, "key2865", "key8876");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 6933: put
  db->Put(write_options, "key5152", "value38177");

  // op 6934: put
  db->Put(write_options, "key11197", "value6475");

  // op 6935: delete_range
  status = db->DeleteRange(write_options, "key1163", "key2742");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 6936: get
  status = db->Get(read_options, "key7030", &value);
  if (status.ok()) {
    out << "GET key7030 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6937: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 6938: put
  db->Put(write_options, "key2820", "value73016");

  // op 6939: delete
  db->Delete(write_options, "key11211");

  // op 6940: put
  db->Put(write_options, "key1149", "value28195");

  // op 6941: put
  db->Put(write_options, "key5672", "value32436");

  // op 6942: delete_range
  status = db->DeleteRange(write_options, "key4625", "key8326");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 6943: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 6944: delete_range
  status = db->DeleteRange(write_options, "key3949", "key11385");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 6945: delete_range
  status = db->DeleteRange(write_options, "key4538", "key11201");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 6946: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0512", "key11205", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0512, key11205) size=" << scan_result.size() << std::endl;
  }

  // op 6947: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4516", "key8253", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4516, key8253) size=" << scan_result.size() << std::endl;
  }

  // op 6948: get
  status = db->Get(read_options, "key9936", &value);
  if (status.ok()) {
    out << "GET key9936 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6949: get
  status = db->Get(read_options, "key9494", &value);
  if (status.ok()) {
    out << "GET key9494 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6950: delete_range
  status = db->DeleteRange(write_options, "key2127", "key7293");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 6951: put
  db->Put(write_options, "key10845", "value94339");

  // op 6952: put
  db->Put(write_options, "key0942", "value57411");

  // op 6953: get
  status = db->Get(read_options, "key5080", &value);
  if (status.ok()) {
    out << "GET key5080 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6954: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6276", "key8071", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6276, key8071) size=" << scan_result.size() << std::endl;
  }

  // op 6955: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 6956: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3559", "key4889", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3559, key4889) size=" << scan_result.size() << std::endl;
  }

  // op 6957: put
  db->Put(write_options, "key7259", "value106765");

  // op 6958: get
  status = db->Get(read_options, "key6595", &value);
  if (status.ok()) {
    out << "GET key6595 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6959: put
  db->Put(write_options, "key2520", "value106800");

  // op 6960: put
  db->Put(write_options, "key5390", "value91365");

  // op 6961: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2459", "key11690", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2459, key11690) size=" << scan_result.size() << std::endl;
  }

  // op 6962: delete
  db->Delete(write_options, "key5376");

  // op 6963: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8075", "key8234", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8075, key8234) size=" << scan_result.size() << std::endl;
  }

  // op 6964: put
  db->Put(write_options, "key9006", "value40350");

  // op 6965: get
  status = db->Get(read_options, "key10569", &value);
  if (status.ok()) {
    out << "GET key10569 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6966: put
  db->Put(write_options, "key10465", "value98910");

  // op 6967: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 6968: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2474", "key11298", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2474, key11298) size=" << scan_result.size() << std::endl;
  }

  // op 6969: get
  status = db->Get(read_options, "key6085", &value);
  if (status.ok()) {
    out << "GET key6085 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6970: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1585", "key4122", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1585, key4122) size=" << scan_result.size() << std::endl;
  }

  // op 6971: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4840", "key8276", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4840, key8276) size=" << scan_result.size() << std::endl;
  }

  // op 6972: delete_range
  status = db->DeleteRange(write_options, "key0248", "key9501");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 6973: put
  db->Put(write_options, "key4691", "value109414");

  // op 6974: put
  db->Put(write_options, "key9000", "value95901");

  // op 6975: delete_range
  status = db->DeleteRange(write_options, "key4752", "key9516");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 6976: delete
  db->Delete(write_options, "key0269");

  // op 6977: get
  status = db->Get(read_options, "key10099", &value);
  if (status.ok()) {
    out << "GET key10099 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6978: put
  db->Put(write_options, "key2936", "value49455");

  // op 6979: put
  db->Put(write_options, "key4983", "value64951");

  // op 6980: delete_range
  status = db->DeleteRange(write_options, "key0161", "key1748");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 6981: put
  db->Put(write_options, "key8272", "value99396");

  // op 6982: delete
  db->Delete(write_options, "key5713");

  // op 6983: put
  db->Put(write_options, "key9325", "value92435");

  // op 6984: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 6985: put
  db->Put(write_options, "key1711", "value43728");

  // op 6986: get
  status = db->Get(read_options, "key10381", &value);
  if (status.ok()) {
    out << "GET key10381 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6987: put
  db->Put(write_options, "key0187", "value44621");

  // op 6988: get
  status = db->Get(read_options, "key1111", &value);
  if (status.ok()) {
    out << "GET key1111 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6989: get
  status = db->Get(read_options, "key3482", &value);
  if (status.ok()) {
    out << "GET key3482 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6990: get
  status = db->Get(read_options, "key6114", &value);
  if (status.ok()) {
    out << "GET key6114 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 6991: put
  db->Put(write_options, "key5065", "value60610");

  // op 6992: put
  db->Put(write_options, "key8794", "value110863");

  // op 6993: put
  db->Put(write_options, "key4951", "value28503");

  // op 6994: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3760", "key4928", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3760, key4928) size=" << scan_result.size() << std::endl;
  }

  // op 6995: put
  db->Put(write_options, "key7815", "value9269");

  // op 6996: delete_range
  status = db->DeleteRange(write_options, "key5079", "key11668");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 6997: put
  db->Put(write_options, "key6446", "value4978");

  // op 6998: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3006", "key8540", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3006, key8540) size=" << scan_result.size() << std::endl;
  }

  // op 6999: get
  status = db->Get(read_options, "key4003", &value);
  if (status.ok()) {
    out << "GET key4003 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7000: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0911", "key7258", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0911, key7258) size=" << scan_result.size() << std::endl;
  }

  // op 7001: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4903", "key6891", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4903, key6891) size=" << scan_result.size() << std::endl;
  }

  // op 7002: delete
  db->Delete(write_options, "key5154");

  // op 7003: delete_range
  status = db->DeleteRange(write_options, "key0970", "key6059");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 7004: put
  db->Put(write_options, "key2436", "value33401");

  // op 7005: delete
  db->Delete(write_options, "key8288");

  // op 7006: put
  db->Put(write_options, "key8388", "value30612");

  // op 7007: delete
  db->Delete(write_options, "key5251");

  // op 7008: put
  db->Put(write_options, "key5447", "value53395");

  // op 7009: get
  status = db->Get(read_options, "key4220", &value);
  if (status.ok()) {
    out << "GET key4220 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7010: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4258", "key6284", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4258, key6284) size=" << scan_result.size() << std::endl;
  }

  // op 7011: get
  status = db->Get(read_options, "key0974", &value);
  if (status.ok()) {
    out << "GET key0974 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7012: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4595", "key6675", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4595, key6675) size=" << scan_result.size() << std::endl;
  }

  // op 7013: get
  status = db->Get(read_options, "key2362", &value);
  if (status.ok()) {
    out << "GET key2362 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7014: put
  db->Put(write_options, "key11290", "value88415");

  // op 7015: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5951", "key10805", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5951, key10805) size=" << scan_result.size() << std::endl;
  }

  // op 7016: delete
  db->Delete(write_options, "key0650");

  // op 7017: put
  db->Put(write_options, "key11514", "value106252");

  // op 7018: put
  db->Put(write_options, "key2949", "value10405");

  // op 7019: put
  db->Put(write_options, "key8415", "value80549");

  // op 7020: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3214", "key9862", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3214, key9862) size=" << scan_result.size() << std::endl;
  }

  // op 7021: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4250", "key7973", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4250, key7973) size=" << scan_result.size() << std::endl;
  }

  // op 7022: get
  status = db->Get(read_options, "key7212", &value);
  if (status.ok()) {
    out << "GET key7212 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7023: get
  status = db->Get(read_options, "key0451", &value);
  if (status.ok()) {
    out << "GET key0451 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7024: put
  db->Put(write_options, "key3777", "value72701");

  // op 7025: get
  status = db->Get(read_options, "key8687", &value);
  if (status.ok()) {
    out << "GET key8687 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7026: put
  db->Put(write_options, "key6250", "value73090");

  // op 7027: put
  db->Put(write_options, "key8094", "value61048");

  // op 7028: put
  db->Put(write_options, "key9840", "value110032");

  // op 7029: put
  db->Put(write_options, "key3467", "value14577");

  // op 7030: delete
  db->Delete(write_options, "key5509");

  // op 7031: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5637", "key6041", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5637, key6041) size=" << scan_result.size() << std::endl;
  }

  // op 7032: put
  db->Put(write_options, "key4362", "value9248");

  // op 7033: put
  db->Put(write_options, "key9137", "value1679");

  // op 7034: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 7035: get
  status = db->Get(read_options, "key10129", &value);
  if (status.ok()) {
    out << "GET key10129 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7036: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 7037: put
  db->Put(write_options, "key3495", "value98678");

  // op 7038: get
  status = db->Get(read_options, "key2898", &value);
  if (status.ok()) {
    out << "GET key2898 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7039: put
  db->Put(write_options, "key8522", "value83485");

  // op 7040: put
  db->Put(write_options, "key10524", "value113219");

  // op 7041: put
  db->Put(write_options, "key5756", "value90631");

  // op 7042: delete
  db->Delete(write_options, "key6524");

  // op 7043: get
  status = db->Get(read_options, "key10527", &value);
  if (status.ok()) {
    out << "GET key10527 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7044: put
  db->Put(write_options, "key6780", "value43400");

  // op 7045: put
  db->Put(write_options, "key2551", "value0406");

  // op 7046: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1201", "key11580", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1201, key11580) size=" << scan_result.size() << std::endl;
  }

  // op 7047: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1446", "key6589", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1446, key6589) size=" << scan_result.size() << std::endl;
  }

  // op 7048: put
  db->Put(write_options, "key8393", "value31190");

  // op 7049: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2323", "key6940", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2323, key6940) size=" << scan_result.size() << std::endl;
  }

  // op 7050: get
  status = db->Get(read_options, "key10241", &value);
  if (status.ok()) {
    out << "GET key10241 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7051: put
  db->Put(write_options, "key4646", "value57248");

  // op 7052: delete_range
  status = db->DeleteRange(write_options, "key5184", "key9449");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 7053: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1810", "key9008", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1810, key9008) size=" << scan_result.size() << std::endl;
  }

  // op 7054: delete
  db->Delete(write_options, "key11158");

  // op 7055: get
  status = db->Get(read_options, "key5411", &value);
  if (status.ok()) {
    out << "GET key5411 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7056: put
  db->Put(write_options, "key5036", "value90088");

  // op 7057: get
  status = db->Get(read_options, "key6651", &value);
  if (status.ok()) {
    out << "GET key6651 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7058: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8971", "key11250", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8971, key11250) size=" << scan_result.size() << std::endl;
  }

  // op 7059: delete_range
  status = db->DeleteRange(write_options, "key4477", "key6675");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 7060: delete_range
  status = db->DeleteRange(write_options, "key7099", "key7614");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 7061: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5435", "key10696", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5435, key10696) size=" << scan_result.size() << std::endl;
  }

  // op 7062: delete
  db->Delete(write_options, "key11521");

  // op 7063: get
  status = db->Get(read_options, "key8632", &value);
  if (status.ok()) {
    out << "GET key8632 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7064: put
  db->Put(write_options, "key9445", "value44190");

  // op 7065: get
  status = db->Get(read_options, "key0687", &value);
  if (status.ok()) {
    out << "GET key0687 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7066: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6611", "key9788", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6611, key9788) size=" << scan_result.size() << std::endl;
  }

  // op 7067: put
  db->Put(write_options, "key11346", "value38703");

  // op 7068: put
  db->Put(write_options, "key1872", "value34439");

  // op 7069: get
  status = db->Get(read_options, "key7550", &value);
  if (status.ok()) {
    out << "GET key7550 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7070: put
  db->Put(write_options, "key2521", "value1229");

  // op 7071: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1057", "key3820", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1057, key3820) size=" << scan_result.size() << std::endl;
  }

  // op 7072: put
  db->Put(write_options, "key8972", "value90343");

  // op 7073: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7929", "key10620", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7929, key10620) size=" << scan_result.size() << std::endl;
  }

  // op 7074: get
  status = db->Get(read_options, "key8650", &value);
  if (status.ok()) {
    out << "GET key8650 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7075: put
  db->Put(write_options, "key5112", "value82137");

  // op 7076: get
  status = db->Get(read_options, "key9163", &value);
  if (status.ok()) {
    out << "GET key9163 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7077: delete
  db->Delete(write_options, "key6381");

  // op 7078: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0489", "key1021", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0489, key1021) size=" << scan_result.size() << std::endl;
  }

  // op 7079: get
  status = db->Get(read_options, "key6312", &value);
  if (status.ok()) {
    out << "GET key6312 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7080: get
  status = db->Get(read_options, "key9859", &value);
  if (status.ok()) {
    out << "GET key9859 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7081: put
  db->Put(write_options, "key9493", "value63817");

  // op 7082: get
  status = db->Get(read_options, "key0130", &value);
  if (status.ok()) {
    out << "GET key0130 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7083: get
  status = db->Get(read_options, "key5737", &value);
  if (status.ok()) {
    out << "GET key5737 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7084: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1493", "key1637", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1493, key1637) size=" << scan_result.size() << std::endl;
  }

  // op 7085: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1356", "key1458", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1356, key1458) size=" << scan_result.size() << std::endl;
  }

  // op 7086: delete_range
  status = db->DeleteRange(write_options, "key3513", "key9109");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 7087: get
  status = db->Get(read_options, "key4912", &value);
  if (status.ok()) {
    out << "GET key4912 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7088: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 7089: put
  db->Put(write_options, "key8888", "value100227");

  // op 7090: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9170", "key9913", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9170, key9913) size=" << scan_result.size() << std::endl;
  }

  // op 7091: get
  status = db->Get(read_options, "key4858", &value);
  if (status.ok()) {
    out << "GET key4858 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7092: put
  db->Put(write_options, "key10845", "value104374");

  // op 7093: put
  db->Put(write_options, "key9743", "value107286");

  // op 7094: put
  db->Put(write_options, "key3813", "value60195");

  // op 7095: delete
  db->Delete(write_options, "key5864");

  // op 7096: put
  db->Put(write_options, "key10623", "value98110");

  // op 7097: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 7098: put
  db->Put(write_options, "key6279", "value90396");

  // op 7099: put
  db->Put(write_options, "key10128", "value93740");

  // op 7100: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3683", "key5853", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3683, key5853) size=" << scan_result.size() << std::endl;
  }

  // op 7101: put
  db->Put(write_options, "key9276", "value79879");

  // op 7102: put
  db->Put(write_options, "key7331", "value5067");

  // op 7103: delete_range
  status = db->DeleteRange(write_options, "key6569", "key7650");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 7104: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1864", "key4831", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1864, key4831) size=" << scan_result.size() << std::endl;
  }

  // op 7105: delete_range
  status = db->DeleteRange(write_options, "key2531", "key10027");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 7106: put
  db->Put(write_options, "key3379", "value46737");

  // op 7107: put
  db->Put(write_options, "key2729", "value109413");

  // op 7108: get
  status = db->Get(read_options, "key1574", &value);
  if (status.ok()) {
    out << "GET key1574 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7109: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5386", "key11004", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5386, key11004) size=" << scan_result.size() << std::endl;
  }

  // op 7110: put
  db->Put(write_options, "key4709", "value20397");

  // op 7111: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1964", "key10146", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1964, key10146) size=" << scan_result.size() << std::endl;
  }

  // op 7112: put
  db->Put(write_options, "key6312", "value100442");

  // op 7113: put
  db->Put(write_options, "key3360", "value115162");

  // op 7114: put
  db->Put(write_options, "key9029", "value51253");

  // op 7115: put
  db->Put(write_options, "key9125", "value73829");

  // op 7116: delete_range
  status = db->DeleteRange(write_options, "key8786", "key11347");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 7117: delete_range
  status = db->DeleteRange(write_options, "key4108", "key8545");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 7118: put
  db->Put(write_options, "key10856", "value102058");

  // op 7119: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4700", "key9607", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4700, key9607) size=" << scan_result.size() << std::endl;
  }

  // op 7120: get
  status = db->Get(read_options, "key4302", &value);
  if (status.ok()) {
    out << "GET key4302 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7121: get
  status = db->Get(read_options, "key6957", &value);
  if (status.ok()) {
    out << "GET key6957 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7122: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 7123: put
  db->Put(write_options, "key11003", "value69271");

  // op 7124: put
  db->Put(write_options, "key5587", "value23037");

  // op 7125: get
  status = db->Get(read_options, "key7217", &value);
  if (status.ok()) {
    out << "GET key7217 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7126: put
  db->Put(write_options, "key3043", "value98319");

  // op 7127: get
  status = db->Get(read_options, "key1082", &value);
  if (status.ok()) {
    out << "GET key1082 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7128: put
  db->Put(write_options, "key5218", "value53864");

  // op 7129: delete_range
  status = db->DeleteRange(write_options, "key3924", "key11262");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 7130: delete
  db->Delete(write_options, "key5124");

  // op 7131: get
  status = db->Get(read_options, "key5701", &value);
  if (status.ok()) {
    out << "GET key5701 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7132: get
  status = db->Get(read_options, "key9605", &value);
  if (status.ok()) {
    out << "GET key9605 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7133: delete
  db->Delete(write_options, "key8455");

  // op 7134: put
  db->Put(write_options, "key8007", "value107417");

  // op 7135: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10425", "key11328", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10425, key11328) size=" << scan_result.size() << std::endl;
  }

  // op 7136: delete_range
  status = db->DeleteRange(write_options, "key9597", "key10214");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 7137: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 7138: put
  db->Put(write_options, "key11067", "value95832");

  // op 7139: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1788", "key2636", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1788, key2636) size=" << scan_result.size() << std::endl;
  }

  // op 7140: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1196", "key5796", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1196, key5796) size=" << scan_result.size() << std::endl;
  }

  // op 7141: put
  db->Put(write_options, "key2877", "value116501");

  // op 7142: put
  db->Put(write_options, "key11355", "value59028");

  // op 7143: put
  db->Put(write_options, "key11212", "value14313");

  // op 7144: delete
  db->Delete(write_options, "key1852");

  // op 7145: put
  db->Put(write_options, "key0606", "value115325");

  // op 7146: put
  db->Put(write_options, "key2919", "value54317");

  // op 7147: get
  status = db->Get(read_options, "key2107", &value);
  if (status.ok()) {
    out << "GET key2107 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7148: put
  db->Put(write_options, "key3979", "value95712");

  // op 7149: get
  status = db->Get(read_options, "key0522", &value);
  if (status.ok()) {
    out << "GET key0522 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7150: delete_range
  status = db->DeleteRange(write_options, "key1936", "key7848");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 7151: get
  status = db->Get(read_options, "key3226", &value);
  if (status.ok()) {
    out << "GET key3226 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7152: put
  db->Put(write_options, "key5302", "value8348");

  // op 7153: delete
  db->Delete(write_options, "key8474");

  // op 7154: delete_range
  status = db->DeleteRange(write_options, "key5372", "key6782");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 7155: delete
  db->Delete(write_options, "key8596");

  // op 7156: delete_range
  status = db->DeleteRange(write_options, "key3115", "key8545");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 7157: delete_range
  status = db->DeleteRange(write_options, "key1598", "key2614");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 7158: put
  db->Put(write_options, "key4288", "value26871");

  // op 7159: get
  status = db->Get(read_options, "key0078", &value);
  if (status.ok()) {
    out << "GET key0078 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7160: get
  status = db->Get(read_options, "key3893", &value);
  if (status.ok()) {
    out << "GET key3893 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7161: get
  status = db->Get(read_options, "key5477", &value);
  if (status.ok()) {
    out << "GET key5477 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7162: put
  db->Put(write_options, "key6475", "value40700");

  // op 7163: get
  status = db->Get(read_options, "key0471", &value);
  if (status.ok()) {
    out << "GET key0471 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7164: get
  status = db->Get(read_options, "key3849", &value);
  if (status.ok()) {
    out << "GET key3849 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7165: delete_range
  status = db->DeleteRange(write_options, "key3644", "key9515");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 7166: put
  db->Put(write_options, "key11650", "value54533");

  // op 7167: get
  status = db->Get(read_options, "key6659", &value);
  if (status.ok()) {
    out << "GET key6659 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7168: put
  db->Put(write_options, "key7644", "value60775");

  // op 7169: put
  db->Put(write_options, "key3555", "value33114");

  // op 7170: get
  status = db->Get(read_options, "key5049", &value);
  if (status.ok()) {
    out << "GET key5049 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7171: delete
  db->Delete(write_options, "key4145");

  // op 7172: put
  db->Put(write_options, "key3378", "value41775");

  // op 7173: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0766", "key3678", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0766, key3678) size=" << scan_result.size() << std::endl;
  }

  // op 7174: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 7175: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1332", "key10079", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1332, key10079) size=" << scan_result.size() << std::endl;
  }

  // op 7176: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7535", "key9233", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7535, key9233) size=" << scan_result.size() << std::endl;
  }

  // op 7177: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4605", "key4658", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4605, key4658) size=" << scan_result.size() << std::endl;
  }

  // op 7178: put
  db->Put(write_options, "key11031", "value69157");

  // op 7179: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 7180: put
  db->Put(write_options, "key5411", "value103689");

  // op 7181: delete_range
  status = db->DeleteRange(write_options, "key0831", "key6424");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 7182: put
  db->Put(write_options, "key8405", "value11721");

  // op 7183: put
  db->Put(write_options, "key3699", "value94719");

  // op 7184: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7539", "key8253", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7539, key8253) size=" << scan_result.size() << std::endl;
  }

  // op 7185: put
  db->Put(write_options, "key2097", "value0636");

  // op 7186: put
  db->Put(write_options, "key2732", "value7873");

  // op 7187: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 7188: get
  status = db->Get(read_options, "key8084", &value);
  if (status.ok()) {
    out << "GET key8084 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7189: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1362", "key2659", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1362, key2659) size=" << scan_result.size() << std::endl;
  }

  // op 7190: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1491", "key9319", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1491, key9319) size=" << scan_result.size() << std::endl;
  }

  // op 7191: put
  db->Put(write_options, "key9541", "value60369");

  // op 7192: put
  db->Put(write_options, "key7593", "value106183");

  // op 7193: get
  status = db->Get(read_options, "key1490", &value);
  if (status.ok()) {
    out << "GET key1490 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7194: put
  db->Put(write_options, "key7625", "value94394");

  // op 7195: get
  status = db->Get(read_options, "key6127", &value);
  if (status.ok()) {
    out << "GET key6127 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7196: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3563", "key10157", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3563, key10157) size=" << scan_result.size() << std::endl;
  }

  // op 7197: delete_range
  status = db->DeleteRange(write_options, "key6529", "key9902");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 7198: delete
  db->Delete(write_options, "key0925");

  // op 7199: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6078", "key7336", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6078, key7336) size=" << scan_result.size() << std::endl;
  }

  // op 7200: get
  status = db->Get(read_options, "key2437", &value);
  if (status.ok()) {
    out << "GET key2437 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7201: put
  db->Put(write_options, "key10520", "value78608");

  // op 7202: put
  db->Put(write_options, "key10884", "value72363");

  // op 7203: put
  db->Put(write_options, "key4376", "value46841");

  // op 7204: put
  db->Put(write_options, "key6333", "value79611");

  // op 7205: delete_range
  status = db->DeleteRange(write_options, "key7655", "key8126");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 7206: put
  db->Put(write_options, "key5083", "value111562");

  // op 7207: delete_range
  status = db->DeleteRange(write_options, "key7275", "key10224");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 7208: put
  db->Put(write_options, "key9962", "value11366");

  // op 7209: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2977", "key4312", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2977, key4312) size=" << scan_result.size() << std::endl;
  }

  // op 7210: put
  db->Put(write_options, "key11042", "value16292");

  // op 7211: delete_range
  status = db->DeleteRange(write_options, "key3938", "key9067");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 7212: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 7213: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1043", "key7555", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1043, key7555) size=" << scan_result.size() << std::endl;
  }

  // op 7214: get
  status = db->Get(read_options, "key3940", &value);
  if (status.ok()) {
    out << "GET key3940 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7215: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4417", "key9638", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4417, key9638) size=" << scan_result.size() << std::endl;
  }

  // op 7216: delete_range
  status = db->DeleteRange(write_options, "key6327", "key8538");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 7217: put
  db->Put(write_options, "key4426", "value78736");

  // op 7218: delete_range
  status = db->DeleteRange(write_options, "key0958", "key11363");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 7219: get
  status = db->Get(read_options, "key0469", &value);
  if (status.ok()) {
    out << "GET key0469 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7220: get
  status = db->Get(read_options, "key8182", &value);
  if (status.ok()) {
    out << "GET key8182 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7221: put
  db->Put(write_options, "key11219", "value88796");

  // op 7222: delete
  db->Delete(write_options, "key10041");

  // op 7223: get
  status = db->Get(read_options, "key2708", &value);
  if (status.ok()) {
    out << "GET key2708 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7224: put
  db->Put(write_options, "key1002", "value5388");

  // op 7225: get
  status = db->Get(read_options, "key6281", &value);
  if (status.ok()) {
    out << "GET key6281 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7226: get
  status = db->Get(read_options, "key7401", &value);
  if (status.ok()) {
    out << "GET key7401 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7227: put
  db->Put(write_options, "key8780", "value57528");

  // op 7228: get
  status = db->Get(read_options, "key1355", &value);
  if (status.ok()) {
    out << "GET key1355 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7229: put
  db->Put(write_options, "key4376", "value92661");

  // op 7230: put
  db->Put(write_options, "key9718", "value73811");

  // op 7231: delete_range
  status = db->DeleteRange(write_options, "key2080", "key11522");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 7232: put
  db->Put(write_options, "key0291", "value9143");

  // op 7233: put
  db->Put(write_options, "key8349", "value74215");

  // op 7234: put
  db->Put(write_options, "key10415", "value114629");

  // op 7235: put
  db->Put(write_options, "key0369", "value85295");

  // op 7236: get
  status = db->Get(read_options, "key7762", &value);
  if (status.ok()) {
    out << "GET key7762 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7237: delete
  db->Delete(write_options, "key10070");

  // op 7238: put
  db->Put(write_options, "key10557", "value20532");

  // op 7239: delete_range
  status = db->DeleteRange(write_options, "key2106", "key6327");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 7240: put
  db->Put(write_options, "key5096", "value111719");

  // op 7241: get
  status = db->Get(read_options, "key1940", &value);
  if (status.ok()) {
    out << "GET key1940 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7242: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 7243: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 7244: put
  db->Put(write_options, "key8692", "value71990");

  // op 7245: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4914", "key9948", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4914, key9948) size=" << scan_result.size() << std::endl;
  }

  // op 7246: delete
  db->Delete(write_options, "key9566");

  // op 7247: put
  db->Put(write_options, "key7354", "value78900");

  // op 7248: put
  db->Put(write_options, "key10533", "value50107");

  // op 7249: get
  status = db->Get(read_options, "key5221", &value);
  if (status.ok()) {
    out << "GET key5221 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7250: get
  status = db->Get(read_options, "key2947", &value);
  if (status.ok()) {
    out << "GET key2947 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7251: put
  db->Put(write_options, "key11391", "value111070");

  // op 7252: put
  db->Put(write_options, "key1506", "value107007");

  // op 7253: put
  db->Put(write_options, "key3094", "value83036");

  // op 7254: delete
  db->Delete(write_options, "key3946");

  // op 7255: put
  db->Put(write_options, "key5756", "value24474");

  // op 7256: put
  db->Put(write_options, "key1981", "value9651");

  // op 7257: put
  db->Put(write_options, "key11516", "value60569");

  // op 7258: put
  db->Put(write_options, "key5739", "value101378");

  // op 7259: get
  status = db->Get(read_options, "key4184", &value);
  if (status.ok()) {
    out << "GET key4184 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7260: delete_range
  status = db->DeleteRange(write_options, "key6940", "key6954");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 7261: delete_range
  status = db->DeleteRange(write_options, "key1579", "key5163");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 7262: delete_range
  status = db->DeleteRange(write_options, "key5294", "key11560");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 7263: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2015", "key7217", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2015, key7217) size=" << scan_result.size() << std::endl;
  }

  // op 7264: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8859", "key9017", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8859, key9017) size=" << scan_result.size() << std::endl;
  }

  // op 7265: put
  db->Put(write_options, "key4685", "value92775");

  // op 7266: put
  db->Put(write_options, "key2719", "value74039");

  // op 7267: get
  status = db->Get(read_options, "key0166", &value);
  if (status.ok()) {
    out << "GET key0166 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7268: put
  db->Put(write_options, "key0214", "value69991");

  // op 7269: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3642", "key5777", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3642, key5777) size=" << scan_result.size() << std::endl;
  }

  // op 7270: get
  status = db->Get(read_options, "key10841", &value);
  if (status.ok()) {
    out << "GET key10841 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7271: put
  db->Put(write_options, "key1716", "value92200");

  // op 7272: put
  db->Put(write_options, "key9324", "value0771");

  // op 7273: put
  db->Put(write_options, "key3556", "value8918");

  // op 7274: put
  db->Put(write_options, "key11159", "value28803");

  // op 7275: get
  status = db->Get(read_options, "key4653", &value);
  if (status.ok()) {
    out << "GET key4653 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7276: put
  db->Put(write_options, "key10777", "value37434");

  // op 7277: get
  status = db->Get(read_options, "key9771", &value);
  if (status.ok()) {
    out << "GET key9771 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7278: put
  db->Put(write_options, "key10348", "value12676");

  // op 7279: put
  db->Put(write_options, "key9010", "value92325");

  // op 7280: put
  db->Put(write_options, "key8252", "value3896");

  // op 7281: put
  db->Put(write_options, "key1808", "value51032");

  // op 7282: put
  db->Put(write_options, "key7712", "value69865");

  // op 7283: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2090", "key5994", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2090, key5994) size=" << scan_result.size() << std::endl;
  }

  // op 7284: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0299", "key9726", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0299, key9726) size=" << scan_result.size() << std::endl;
  }

  // op 7285: put
  db->Put(write_options, "key8683", "value27298");

  // op 7286: get
  status = db->Get(read_options, "key5765", &value);
  if (status.ok()) {
    out << "GET key5765 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7287: get
  status = db->Get(read_options, "key5529", &value);
  if (status.ok()) {
    out << "GET key5529 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7288: put
  db->Put(write_options, "key4239", "value71804");

  // op 7289: put
  db->Put(write_options, "key10367", "value14348");

  // op 7290: put
  db->Put(write_options, "key4443", "value80941");

  // op 7291: put
  db->Put(write_options, "key9799", "value94472");

  // op 7292: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5164", "key9709", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5164, key9709) size=" << scan_result.size() << std::endl;
  }

  // op 7293: delete
  db->Delete(write_options, "key10388");

  // op 7294: put
  db->Put(write_options, "key5179", "value38514");

  // op 7295: put
  db->Put(write_options, "key4318", "value64968");

  // op 7296: put
  db->Put(write_options, "key10558", "value10515");

  // op 7297: get
  status = db->Get(read_options, "key10507", &value);
  if (status.ok()) {
    out << "GET key10507 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7298: delete_range
  status = db->DeleteRange(write_options, "key4243", "key10767");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 7299: put
  db->Put(write_options, "key6272", "value82848");

  // op 7300: delete
  db->Delete(write_options, "key5410");

  // op 7301: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2652", "key6139", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2652, key6139) size=" << scan_result.size() << std::endl;
  }

  // op 7302: get
  status = db->Get(read_options, "key4035", &value);
  if (status.ok()) {
    out << "GET key4035 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7303: get
  status = db->Get(read_options, "key5612", &value);
  if (status.ok()) {
    out << "GET key5612 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7304: delete
  db->Delete(write_options, "key8985");

  // op 7305: delete_range
  status = db->DeleteRange(write_options, "key3416", "key9338");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 7306: put
  db->Put(write_options, "key1136", "value40159");

  // op 7307: put
  db->Put(write_options, "key9900", "value23435");

  // op 7308: get
  status = db->Get(read_options, "key9287", &value);
  if (status.ok()) {
    out << "GET key9287 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7309: delete
  db->Delete(write_options, "key0877");

  // op 7310: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2029", "key9463", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2029, key9463) size=" << scan_result.size() << std::endl;
  }

  // op 7311: put
  db->Put(write_options, "key6051", "value47974");

  // op 7312: delete
  db->Delete(write_options, "key0511");

  // op 7313: get
  status = db->Get(read_options, "key6021", &value);
  if (status.ok()) {
    out << "GET key6021 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7314: put
  db->Put(write_options, "key6924", "value35877");

  // op 7315: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0829", "key11237", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0829, key11237) size=" << scan_result.size() << std::endl;
  }

  // op 7316: get
  status = db->Get(read_options, "key5280", &value);
  if (status.ok()) {
    out << "GET key5280 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7317: put
  db->Put(write_options, "key8923", "value43525");

  // op 7318: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4290", "key10208", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4290, key10208) size=" << scan_result.size() << std::endl;
  }

  // op 7319: put
  db->Put(write_options, "key4479", "value23435");

  // op 7320: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7817", "key10693", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7817, key10693) size=" << scan_result.size() << std::endl;
  }

  // op 7321: get
  status = db->Get(read_options, "key10799", &value);
  if (status.ok()) {
    out << "GET key10799 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7322: put
  db->Put(write_options, "key6033", "value43650");

  // op 7323: delete
  db->Delete(write_options, "key4072");

  // op 7324: put
  db->Put(write_options, "key7042", "value89088");

  // op 7325: delete_range
  status = db->DeleteRange(write_options, "key3743", "key4055");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 7326: delete
  db->Delete(write_options, "key5950");

  // op 7327: put
  db->Put(write_options, "key9421", "value76364");

  // op 7328: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5543", "key6216", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5543, key6216) size=" << scan_result.size() << std::endl;
  }

  // op 7329: delete
  db->Delete(write_options, "key0788");

  // op 7330: delete_range
  status = db->DeleteRange(write_options, "key0832", "key11619");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 7331: put
  db->Put(write_options, "key11113", "value60438");

  // op 7332: get
  status = db->Get(read_options, "key0608", &value);
  if (status.ok()) {
    out << "GET key0608 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7333: delete_range
  status = db->DeleteRange(write_options, "key0708", "key10283");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 7334: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7879", "key8443", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7879, key8443) size=" << scan_result.size() << std::endl;
  }

  // op 7335: delete
  db->Delete(write_options, "key0000");

  // op 7336: get
  status = db->Get(read_options, "key8792", &value);
  if (status.ok()) {
    out << "GET key8792 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7337: put
  db->Put(write_options, "key6387", "value23355");

  // op 7338: put
  db->Put(write_options, "key5161", "value6984");

  // op 7339: get
  status = db->Get(read_options, "key11009", &value);
  if (status.ok()) {
    out << "GET key11009 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7340: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0326", "key10241", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0326, key10241) size=" << scan_result.size() << std::endl;
  }

  // op 7341: delete
  db->Delete(write_options, "key3118");

  // op 7342: put
  db->Put(write_options, "key5658", "value44069");

  // op 7343: delete
  db->Delete(write_options, "key2891");

  // op 7344: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 7345: put
  db->Put(write_options, "key3059", "value9576");

  // op 7346: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2300", "key8034", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2300, key8034) size=" << scan_result.size() << std::endl;
  }

  // op 7347: put
  db->Put(write_options, "key8359", "value57526");

  // op 7348: get
  status = db->Get(read_options, "key3434", &value);
  if (status.ok()) {
    out << "GET key3434 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7349: get
  status = db->Get(read_options, "key1597", &value);
  if (status.ok()) {
    out << "GET key1597 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7350: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 7351: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0345", "key7630", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0345, key7630) size=" << scan_result.size() << std::endl;
  }

  // op 7352: put
  db->Put(write_options, "key1167", "value22338");

  // op 7353: put
  db->Put(write_options, "key3019", "value80967");

  // op 7354: put
  db->Put(write_options, "key1046", "value18118");

  // op 7355: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1753", "key11261", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1753, key11261) size=" << scan_result.size() << std::endl;
  }

  // op 7356: put
  db->Put(write_options, "key10600", "value46386");

  // op 7357: get
  status = db->Get(read_options, "key9923", &value);
  if (status.ok()) {
    out << "GET key9923 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7358: delete
  db->Delete(write_options, "key10169");

  // op 7359: put
  db->Put(write_options, "key4024", "value85379");

  // op 7360: put
  db->Put(write_options, "key5624", "value6942");

  // op 7361: put
  db->Put(write_options, "key1938", "value52670");

  // op 7362: put
  db->Put(write_options, "key9577", "value37638");

  // op 7363: put
  db->Put(write_options, "key10160", "value106989");

  // op 7364: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6343", "key10716", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6343, key10716) size=" << scan_result.size() << std::endl;
  }

  // op 7365: put
  db->Put(write_options, "key10135", "value1908");

  // op 7366: put
  db->Put(write_options, "key0524", "value6147");

  // op 7367: put
  db->Put(write_options, "key3414", "value2867");

  // op 7368: put
  db->Put(write_options, "key6064", "value67165");

  // op 7369: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1034", "key10466", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1034, key10466) size=" << scan_result.size() << std::endl;
  }

  // op 7370: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 7371: put
  db->Put(write_options, "key1149", "value100968");

  // op 7372: put
  db->Put(write_options, "key11649", "value56096");

  // op 7373: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3535", "key10588", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3535, key10588) size=" << scan_result.size() << std::endl;
  }

  // op 7374: put
  db->Put(write_options, "key10114", "value10620");

  // op 7375: put
  db->Put(write_options, "key3617", "value70273");

  // op 7376: put
  db->Put(write_options, "key0642", "value47376");

  // op 7377: put
  db->Put(write_options, "key2692", "value84831");

  // op 7378: put
  db->Put(write_options, "key10118", "value36513");

  // op 7379: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2821", "key6850", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2821, key6850) size=" << scan_result.size() << std::endl;
  }

  // op 7380: delete
  db->Delete(write_options, "key3831");

  // op 7381: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0110", "key9083", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0110, key9083) size=" << scan_result.size() << std::endl;
  }

  // op 7382: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2943", "key4246", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2943, key4246) size=" << scan_result.size() << std::endl;
  }

  // op 7383: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0336", "key3374", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0336, key3374) size=" << scan_result.size() << std::endl;
  }

  // op 7384: delete
  db->Delete(write_options, "key3174");

  // op 7385: delete_range
  status = db->DeleteRange(write_options, "key0678", "key4521");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 7386: delete_range
  status = db->DeleteRange(write_options, "key3928", "key4954");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 7387: delete
  db->Delete(write_options, "key6924");

  // op 7388: put
  db->Put(write_options, "key4275", "value48517");

  // op 7389: put
  db->Put(write_options, "key11211", "value93546");

  // op 7390: delete
  db->Delete(write_options, "key11575");

  // op 7391: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7081", "key9330", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7081, key9330) size=" << scan_result.size() << std::endl;
  }

  // op 7392: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1453", "key6887", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1453, key6887) size=" << scan_result.size() << std::endl;
  }

  // op 7393: put
  db->Put(write_options, "key4750", "value87634");

  // op 7394: put
  db->Put(write_options, "key1784", "value16085");

  // op 7395: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7691", "key8967", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7691, key8967) size=" << scan_result.size() << std::endl;
  }

  // op 7396: put
  db->Put(write_options, "key10809", "value111939");

  // op 7397: delete_range
  status = db->DeleteRange(write_options, "key2298", "key11454");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 7398: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 7399: delete_range
  status = db->DeleteRange(write_options, "key4615", "key6142");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 7400: put
  db->Put(write_options, "key1623", "value1477");

  // op 7401: put
  db->Put(write_options, "key0662", "value6107");

  // op 7402: delete
  db->Delete(write_options, "key1458");

  // op 7403: get
  status = db->Get(read_options, "key10965", &value);
  if (status.ok()) {
    out << "GET key10965 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7404: put
  db->Put(write_options, "key9435", "value116256");

  // op 7405: delete_range
  status = db->DeleteRange(write_options, "key10493", "key10976");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 7406: put
  db->Put(write_options, "key7552", "value13444");

  // op 7407: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 7408: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0351", "key11547", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0351, key11547) size=" << scan_result.size() << std::endl;
  }

  // op 7409: put
  db->Put(write_options, "key4579", "value70818");

  // op 7410: put
  db->Put(write_options, "key9549", "value46135");

  // op 7411: delete
  db->Delete(write_options, "key5305");

  // op 7412: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2247", "key5961", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2247, key5961) size=" << scan_result.size() << std::endl;
  }

  // op 7413: delete_range
  status = db->DeleteRange(write_options, "key2691", "key5496");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 7414: put
  db->Put(write_options, "key1078", "value28484");

  // op 7415: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 7416: get
  status = db->Get(read_options, "key2057", &value);
  if (status.ok()) {
    out << "GET key2057 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7417: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 7418: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0211", "key9030", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0211, key9030) size=" << scan_result.size() << std::endl;
  }

  // op 7419: put
  db->Put(write_options, "key5073", "value85379");

  // op 7420: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0201", "key0238", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0201, key0238) size=" << scan_result.size() << std::endl;
  }

  // op 7421: put
  db->Put(write_options, "key2507", "value51718");

  // op 7422: get
  status = db->Get(read_options, "key6470", &value);
  if (status.ok()) {
    out << "GET key6470 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7423: get
  status = db->Get(read_options, "key2215", &value);
  if (status.ok()) {
    out << "GET key2215 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7424: put
  db->Put(write_options, "key5662", "value80890");

  // op 7425: delete_range
  status = db->DeleteRange(write_options, "key7030", "key7177");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 7426: delete_range
  status = db->DeleteRange(write_options, "key5126", "key10346");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 7427: delete_range
  status = db->DeleteRange(write_options, "key2891", "key9236");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 7428: get
  status = db->Get(read_options, "key8336", &value);
  if (status.ok()) {
    out << "GET key8336 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7429: put
  db->Put(write_options, "key11729", "value77644");

  // op 7430: put
  db->Put(write_options, "key7184", "value24972");

  // op 7431: put
  db->Put(write_options, "key4704", "value111667");

  // op 7432: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3891", "key5648", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3891, key5648) size=" << scan_result.size() << std::endl;
  }

  // op 7433: put
  db->Put(write_options, "key10403", "value69872");

  // op 7434: put
  db->Put(write_options, "key2223", "value14242");

  // op 7435: put
  db->Put(write_options, "key0870", "value105333");

  // op 7436: put
  db->Put(write_options, "key0068", "value64958");

  // op 7437: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6402", "key10715", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6402, key10715) size=" << scan_result.size() << std::endl;
  }

  // op 7438: put
  db->Put(write_options, "key7874", "value85508");

  // op 7439: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4165", "key6933", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4165, key6933) size=" << scan_result.size() << std::endl;
  }

  // op 7440: delete
  db->Delete(write_options, "key8615");

  // op 7441: delete_range
  status = db->DeleteRange(write_options, "key0840", "key6291");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 7442: get
  status = db->Get(read_options, "key1109", &value);
  if (status.ok()) {
    out << "GET key1109 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7443: put
  db->Put(write_options, "key1025", "value57261");

  // op 7444: delete
  db->Delete(write_options, "key3067");

  // op 7445: put
  db->Put(write_options, "key8120", "value21393");

  // op 7446: put
  db->Put(write_options, "key7769", "value7225");

  // op 7447: put
  db->Put(write_options, "key2423", "value109750");

  // op 7448: put
  db->Put(write_options, "key2331", "value63588");

  // op 7449: get
  status = db->Get(read_options, "key11169", &value);
  if (status.ok()) {
    out << "GET key11169 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7450: get
  status = db->Get(read_options, "key3452", &value);
  if (status.ok()) {
    out << "GET key3452 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7451: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2436", "key8889", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2436, key8889) size=" << scan_result.size() << std::endl;
  }

  // op 7452: get
  status = db->Get(read_options, "key3116", &value);
  if (status.ok()) {
    out << "GET key3116 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7453: get
  status = db->Get(read_options, "key10043", &value);
  if (status.ok()) {
    out << "GET key10043 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7454: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9026", "key10464", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9026, key10464) size=" << scan_result.size() << std::endl;
  }

  // op 7455: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0715", "key5059", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0715, key5059) size=" << scan_result.size() << std::endl;
  }

  // op 7456: get
  status = db->Get(read_options, "key9669", &value);
  if (status.ok()) {
    out << "GET key9669 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7457: put
  db->Put(write_options, "key8837", "value34901");

  // op 7458: put
  db->Put(write_options, "key3862", "value92479");

  // op 7459: put
  db->Put(write_options, "key2991", "value110041");

  // op 7460: delete_range
  status = db->DeleteRange(write_options, "key2441", "key9364");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 7461: get
  status = db->Get(read_options, "key8827", &value);
  if (status.ok()) {
    out << "GET key8827 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7462: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4137", "key4768", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4137, key4768) size=" << scan_result.size() << std::endl;
  }

  // op 7463: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 7464: get
  status = db->Get(read_options, "key10800", &value);
  if (status.ok()) {
    out << "GET key10800 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7465: put
  db->Put(write_options, "key2477", "value60812");

  // op 7466: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5491", "key6802", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5491, key6802) size=" << scan_result.size() << std::endl;
  }

  // op 7467: get
  status = db->Get(read_options, "key0879", &value);
  if (status.ok()) {
    out << "GET key0879 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7468: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0543", "key4406", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0543, key4406) size=" << scan_result.size() << std::endl;
  }

  // op 7469: put
  db->Put(write_options, "key3259", "value1990");

  // op 7470: put
  db->Put(write_options, "key10870", "value100832");

  // op 7471: put
  db->Put(write_options, "key2186", "value40392");

  // op 7472: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1041", "key4713", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1041, key4713) size=" << scan_result.size() << std::endl;
  }

  // op 7473: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4260", "key8373", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4260, key8373) size=" << scan_result.size() << std::endl;
  }

  // op 7474: put
  db->Put(write_options, "key10773", "value15132");

  // op 7475: put
  db->Put(write_options, "key3028", "value66394");

  // op 7476: put
  db->Put(write_options, "key1478", "value13357");

  // op 7477: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 7478: put
  db->Put(write_options, "key2802", "value11960");

  // op 7479: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8769", "key10365", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8769, key10365) size=" << scan_result.size() << std::endl;
  }

  // op 7480: put
  db->Put(write_options, "key10470", "value75771");

  // op 7481: put
  db->Put(write_options, "key0725", "value15162");

  // op 7482: get
  status = db->Get(read_options, "key8953", &value);
  if (status.ok()) {
    out << "GET key8953 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7483: put
  db->Put(write_options, "key10138", "value87482");

  // op 7484: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 7485: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5410", "key6241", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5410, key6241) size=" << scan_result.size() << std::endl;
  }

  // op 7486: delete
  db->Delete(write_options, "key3593");

  // op 7487: put
  db->Put(write_options, "key6754", "value56452");

  // op 7488: put
  db->Put(write_options, "key1082", "value108181");

  // op 7489: put
  db->Put(write_options, "key3401", "value110748");

  // op 7490: put
  db->Put(write_options, "key0139", "value34520");

  // op 7491: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3055", "key8725", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3055, key8725) size=" << scan_result.size() << std::endl;
  }

  // op 7492: delete
  db->Delete(write_options, "key2362");

  // op 7493: put
  db->Put(write_options, "key4463", "value19492");

  // op 7494: delete
  db->Delete(write_options, "key7745");

  // op 7495: get
  status = db->Get(read_options, "key8030", &value);
  if (status.ok()) {
    out << "GET key8030 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7496: delete_range
  status = db->DeleteRange(write_options, "key0917", "key11482");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 7497: put
  db->Put(write_options, "key1142", "value42308");

  // op 7498: put
  db->Put(write_options, "key7863", "value105837");

  // op 7499: delete
  db->Delete(write_options, "key7035");

  // op 7500: put
  db->Put(write_options, "key6700", "value7934");

  // op 7501: get
  status = db->Get(read_options, "key11637", &value);
  if (status.ok()) {
    out << "GET key11637 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7502: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1817", "key9322", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1817, key9322) size=" << scan_result.size() << std::endl;
  }

  // op 7503: get
  status = db->Get(read_options, "key8383", &value);
  if (status.ok()) {
    out << "GET key8383 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7504: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2057", "key10016", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2057, key10016) size=" << scan_result.size() << std::endl;
  }

  // op 7505: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3283", "key9915", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3283, key9915) size=" << scan_result.size() << std::endl;
  }

  // op 7506: delete_range
  status = db->DeleteRange(write_options, "key1531", "key7572");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 7507: get
  status = db->Get(read_options, "key10892", &value);
  if (status.ok()) {
    out << "GET key10892 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7508: put
  db->Put(write_options, "key2520", "value114698");

  // op 7509: put
  db->Put(write_options, "key4645", "value68596");

  // op 7510: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 7511: put
  db->Put(write_options, "key6082", "value93419");

  // op 7512: put
  db->Put(write_options, "key1047", "value48461");

  // op 7513: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 7514: delete_range
  status = db->DeleteRange(write_options, "key3046", "key10991");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 7515: delete_range
  status = db->DeleteRange(write_options, "key1029", "key9365");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 7516: delete_range
  status = db->DeleteRange(write_options, "key4025", "key4276");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 7517: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4932", "key9336", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4932, key9336) size=" << scan_result.size() << std::endl;
  }

  // op 7518: put
  db->Put(write_options, "key3868", "value26979");

  // op 7519: delete_range
  status = db->DeleteRange(write_options, "key0158", "key7986");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 7520: put
  db->Put(write_options, "key10424", "value63183");

  // op 7521: get
  status = db->Get(read_options, "key11607", &value);
  if (status.ok()) {
    out << "GET key11607 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7522: get
  status = db->Get(read_options, "key1584", &value);
  if (status.ok()) {
    out << "GET key1584 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7523: get
  status = db->Get(read_options, "key0072", &value);
  if (status.ok()) {
    out << "GET key0072 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7524: put
  db->Put(write_options, "key5481", "value25528");

  // op 7525: put
  db->Put(write_options, "key3694", "value94265");

  // op 7526: put
  db->Put(write_options, "key8517", "value11869");

  // op 7527: delete_range
  status = db->DeleteRange(write_options, "key8823", "key10340");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 7528: put
  db->Put(write_options, "key7309", "value112380");

  // op 7529: delete_range
  status = db->DeleteRange(write_options, "key2350", "key3169");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 7530: put
  db->Put(write_options, "key2733", "value111649");

  // op 7531: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8774", "key9792", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8774, key9792) size=" << scan_result.size() << std::endl;
  }

  // op 7532: put
  db->Put(write_options, "key4408", "value11723");

  // op 7533: delete
  db->Delete(write_options, "key2170");

  // op 7534: put
  db->Put(write_options, "key0005", "value53949");

  // op 7535: put
  db->Put(write_options, "key1793", "value5046");

  // op 7536: put
  db->Put(write_options, "key0952", "value12423");

  // op 7537: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3370", "key4922", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3370, key4922) size=" << scan_result.size() << std::endl;
  }

  // op 7538: put
  db->Put(write_options, "key10964", "value67261");

  // op 7539: delete
  db->Delete(write_options, "key2813");

  // op 7540: delete_range
  status = db->DeleteRange(write_options, "key7874", "key8591");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 7541: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9652", "key11231", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9652, key11231) size=" << scan_result.size() << std::endl;
  }

  // op 7542: put
  db->Put(write_options, "key2286", "value97157");

  // op 7543: get
  status = db->Get(read_options, "key7361", &value);
  if (status.ok()) {
    out << "GET key7361 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7544: get
  status = db->Get(read_options, "key10066", &value);
  if (status.ok()) {
    out << "GET key10066 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7545: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7828", "key10440", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7828, key10440) size=" << scan_result.size() << std::endl;
  }

  // op 7546: put
  db->Put(write_options, "key7243", "value34386");

  // op 7547: get
  status = db->Get(read_options, "key0029", &value);
  if (status.ok()) {
    out << "GET key0029 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7548: put
  db->Put(write_options, "key11054", "value108835");

  // op 7549: delete
  db->Delete(write_options, "key9432");

  // op 7550: delete
  db->Delete(write_options, "key11513");

  // op 7551: delete_range
  status = db->DeleteRange(write_options, "key8505", "key11069");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 7552: get
  status = db->Get(read_options, "key7333", &value);
  if (status.ok()) {
    out << "GET key7333 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7553: delete_range
  status = db->DeleteRange(write_options, "key1417", "key5450");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 7554: get
  status = db->Get(read_options, "key8000", &value);
  if (status.ok()) {
    out << "GET key8000 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7555: delete
  db->Delete(write_options, "key3471");

  // op 7556: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0440", "key7474", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0440, key7474) size=" << scan_result.size() << std::endl;
  }

  // op 7557: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0816", "key6198", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0816, key6198) size=" << scan_result.size() << std::endl;
  }

  // op 7558: get
  status = db->Get(read_options, "key5940", &value);
  if (status.ok()) {
    out << "GET key5940 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7559: put
  db->Put(write_options, "key7040", "value3799");

  // op 7560: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5958", "key10758", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5958, key10758) size=" << scan_result.size() << std::endl;
  }

  // op 7561: put
  db->Put(write_options, "key7987", "value15515");

  // op 7562: delete_range
  status = db->DeleteRange(write_options, "key3005", "key5559");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 7563: get
  status = db->Get(read_options, "key10876", &value);
  if (status.ok()) {
    out << "GET key10876 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7564: delete_range
  status = db->DeleteRange(write_options, "key1260", "key7770");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 7565: delete
  db->Delete(write_options, "key10107");

  // op 7566: delete_range
  status = db->DeleteRange(write_options, "key4568", "key11636");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 7567: put
  db->Put(write_options, "key7590", "value105908");

  // op 7568: put
  db->Put(write_options, "key8895", "value3910");

  // op 7569: put
  db->Put(write_options, "key8103", "value43123");

  // op 7570: put
  db->Put(write_options, "key3355", "value92760");

  // op 7571: put
  db->Put(write_options, "key10065", "value11290");

  // op 7572: put
  db->Put(write_options, "key2017", "value105273");

  // op 7573: delete_range
  status = db->DeleteRange(write_options, "key1350", "key5699");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 7574: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9386", "key9994", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9386, key9994) size=" << scan_result.size() << std::endl;
  }

  // op 7575: delete_range
  status = db->DeleteRange(write_options, "key1876", "key7219");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 7576: put
  db->Put(write_options, "key2967", "value40064");

  // op 7577: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2643", "key4921", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2643, key4921) size=" << scan_result.size() << std::endl;
  }

  // op 7578: put
  db->Put(write_options, "key5524", "value52189");

  // op 7579: get
  status = db->Get(read_options, "key10283", &value);
  if (status.ok()) {
    out << "GET key10283 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7580: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1662", "key1777", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1662, key1777) size=" << scan_result.size() << std::endl;
  }

  // op 7581: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4558", "key9794", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4558, key9794) size=" << scan_result.size() << std::endl;
  }

  // op 7582: put
  db->Put(write_options, "key10523", "value24614");

  // op 7583: get
  status = db->Get(read_options, "key0464", &value);
  if (status.ok()) {
    out << "GET key0464 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7584: put
  db->Put(write_options, "key3892", "value51375");

  // op 7585: delete_range
  status = db->DeleteRange(write_options, "key2369", "key7376");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 7586: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4267", "key4300", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4267, key4300) size=" << scan_result.size() << std::endl;
  }

  // op 7587: put
  db->Put(write_options, "key8770", "value57014");

  // op 7588: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0977", "key4999", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0977, key4999) size=" << scan_result.size() << std::endl;
  }

  // op 7589: put
  db->Put(write_options, "key10480", "value104575");

  // op 7590: get
  status = db->Get(read_options, "key6858", &value);
  if (status.ok()) {
    out << "GET key6858 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7591: get
  status = db->Get(read_options, "key5081", &value);
  if (status.ok()) {
    out << "GET key5081 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7592: put
  db->Put(write_options, "key4395", "value109639");

  // op 7593: put
  db->Put(write_options, "key11222", "value106714");

  // op 7594: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7583", "key9661", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7583, key9661) size=" << scan_result.size() << std::endl;
  }

  // op 7595: put
  db->Put(write_options, "key10394", "value7308");

  // op 7596: put
  db->Put(write_options, "key6984", "value47727");

  // op 7597: put
  db->Put(write_options, "key5651", "value71750");

  // op 7598: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 7599: put
  db->Put(write_options, "key4398", "value92585");

  // op 7600: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1634", "key10225", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1634, key10225) size=" << scan_result.size() << std::endl;
  }

  // op 7601: put
  db->Put(write_options, "key5446", "value29476");

  // op 7602: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0170", "key10182", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0170, key10182) size=" << scan_result.size() << std::endl;
  }

  // op 7603: put
  db->Put(write_options, "key3756", "value74692");

  // op 7604: put
  db->Put(write_options, "key2782", "value24562");

  // op 7605: put
  db->Put(write_options, "key5019", "value88855");

  // op 7606: delete_range
  status = db->DeleteRange(write_options, "key5405", "key6222");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 7607: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 7608: put
  db->Put(write_options, "key5128", "value91401");

  // op 7609: put
  db->Put(write_options, "key4902", "value92545");

  // op 7610: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1455", "key8122", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1455, key8122) size=" << scan_result.size() << std::endl;
  }

  // op 7611: delete
  db->Delete(write_options, "key8637");

  // op 7612: delete_range
  status = db->DeleteRange(write_options, "key0980", "key10066");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 7613: delete
  db->Delete(write_options, "key4446");

  // op 7614: delete_range
  status = db->DeleteRange(write_options, "key8404", "key9082");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 7615: delete_range
  status = db->DeleteRange(write_options, "key3478", "key10244");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 7616: put
  db->Put(write_options, "key1930", "value116421");

  // op 7617: get
  status = db->Get(read_options, "key2396", &value);
  if (status.ok()) {
    out << "GET key2396 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7618: put
  db->Put(write_options, "key0253", "value12370");

  // op 7619: get
  status = db->Get(read_options, "key2341", &value);
  if (status.ok()) {
    out << "GET key2341 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7620: put
  db->Put(write_options, "key9655", "value100963");

  // op 7621: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1025", "key6782", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1025, key6782) size=" << scan_result.size() << std::endl;
  }

  // op 7622: put
  db->Put(write_options, "key8859", "value18001");

  // op 7623: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7772", "key8148", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7772, key8148) size=" << scan_result.size() << std::endl;
  }

  // op 7624: put
  db->Put(write_options, "key5296", "value67761");

  // op 7625: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1978", "key3744", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1978, key3744) size=" << scan_result.size() << std::endl;
  }

  // op 7626: put
  db->Put(write_options, "key7544", "value34320");

  // op 7627: delete
  db->Delete(write_options, "key4027");

  // op 7628: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5484", "key9777", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5484, key9777) size=" << scan_result.size() << std::endl;
  }

  // op 7629: delete_range
  status = db->DeleteRange(write_options, "key0936", "key5090");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 7630: get
  status = db->Get(read_options, "key7058", &value);
  if (status.ok()) {
    out << "GET key7058 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7631: get
  status = db->Get(read_options, "key5794", &value);
  if (status.ok()) {
    out << "GET key5794 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7632: put
  db->Put(write_options, "key11406", "value90634");

  // op 7633: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4107", "key10580", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4107, key10580) size=" << scan_result.size() << std::endl;
  }

  // op 7634: put
  db->Put(write_options, "key4968", "value0256");

  // op 7635: put
  db->Put(write_options, "key1039", "value27229");

  // op 7636: put
  db->Put(write_options, "key2802", "value89001");

  // op 7637: delete
  db->Delete(write_options, "key10614");

  // op 7638: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6478", "key9013", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6478, key9013) size=" << scan_result.size() << std::endl;
  }

  // op 7639: put
  db->Put(write_options, "key1214", "value115559");

  // op 7640: put
  db->Put(write_options, "key2357", "value81400");

  // op 7641: put
  db->Put(write_options, "key1455", "value38549");

  // op 7642: put
  db->Put(write_options, "key0331", "value43239");

  // op 7643: delete_range
  status = db->DeleteRange(write_options, "key4743", "key5324");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 7644: put
  db->Put(write_options, "key9439", "value81334");

  // op 7645: get
  status = db->Get(read_options, "key3529", &value);
  if (status.ok()) {
    out << "GET key3529 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7646: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7419", "key7959", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7419, key7959) size=" << scan_result.size() << std::endl;
  }

  // op 7647: get
  status = db->Get(read_options, "key10879", &value);
  if (status.ok()) {
    out << "GET key10879 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7648: put
  db->Put(write_options, "key0285", "value72286");

  // op 7649: put
  db->Put(write_options, "key10946", "value79596");

  // op 7650: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8668", "key9650", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8668, key9650) size=" << scan_result.size() << std::endl;
  }

  // op 7651: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 7652: get
  status = db->Get(read_options, "key2901", &value);
  if (status.ok()) {
    out << "GET key2901 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7653: put
  db->Put(write_options, "key6529", "value27102");

  // op 7654: put
  db->Put(write_options, "key0073", "value66169");

  // op 7655: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 7656: put
  db->Put(write_options, "key11507", "value94712");

  // op 7657: delete
  db->Delete(write_options, "key1186");

  // op 7658: put
  db->Put(write_options, "key8731", "value13551");

  // op 7659: put
  db->Put(write_options, "key7292", "value40208");

  // op 7660: put
  db->Put(write_options, "key4246", "value87991");

  // op 7661: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6246", "key10201", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6246, key10201) size=" << scan_result.size() << std::endl;
  }

  // op 7662: put
  db->Put(write_options, "key0003", "value104790");

  // op 7663: put
  db->Put(write_options, "key2103", "value113201");

  // op 7664: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1763", "key8507", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1763, key8507) size=" << scan_result.size() << std::endl;
  }

  // op 7665: get
  status = db->Get(read_options, "key6806", &value);
  if (status.ok()) {
    out << "GET key6806 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7666: delete
  db->Delete(write_options, "key1435");

  // op 7667: put
  db->Put(write_options, "key2911", "value99007");

  // op 7668: put
  db->Put(write_options, "key3275", "value30242");

  // op 7669: put
  db->Put(write_options, "key5315", "value89920");

  // op 7670: get
  status = db->Get(read_options, "key5224", &value);
  if (status.ok()) {
    out << "GET key5224 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7671: put
  db->Put(write_options, "key6407", "value44727");

  // op 7672: put
  db->Put(write_options, "key1588", "value50299");

  // op 7673: put
  db->Put(write_options, "key5451", "value96151");

  // op 7674: delete_range
  status = db->DeleteRange(write_options, "key3478", "key3815");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 7675: delete_range
  status = db->DeleteRange(write_options, "key2982", "key10332");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 7676: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4578", "key10863", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4578, key10863) size=" << scan_result.size() << std::endl;
  }

  // op 7677: put
  db->Put(write_options, "key5995", "value48550");

  // op 7678: put
  db->Put(write_options, "key2273", "value16264");

  // op 7679: put
  db->Put(write_options, "key10326", "value60120");

  // op 7680: put
  db->Put(write_options, "key9744", "value99811");

  // op 7681: put
  db->Put(write_options, "key5836", "value20767");

  // op 7682: put
  db->Put(write_options, "key10055", "value25181");

  // op 7683: get
  status = db->Get(read_options, "key10813", &value);
  if (status.ok()) {
    out << "GET key10813 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7684: put
  db->Put(write_options, "key5158", "value83239");

  // op 7685: put
  db->Put(write_options, "key0583", "value78001");

  // op 7686: put
  db->Put(write_options, "key7277", "value9767");

  // op 7687: get
  status = db->Get(read_options, "key2012", &value);
  if (status.ok()) {
    out << "GET key2012 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7688: get
  status = db->Get(read_options, "key3264", &value);
  if (status.ok()) {
    out << "GET key3264 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7689: put
  db->Put(write_options, "key3817", "value91200");

  // op 7690: put
  db->Put(write_options, "key11067", "value71478");

  // op 7691: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0307", "key11568", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0307, key11568) size=" << scan_result.size() << std::endl;
  }

  // op 7692: get
  status = db->Get(read_options, "key5884", &value);
  if (status.ok()) {
    out << "GET key5884 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7693: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10804", "key11317", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10804, key11317) size=" << scan_result.size() << std::endl;
  }

  // op 7694: delete_range
  status = db->DeleteRange(write_options, "key0623", "key5370");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 7695: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2346", "key8229", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2346, key8229) size=" << scan_result.size() << std::endl;
  }

  // op 7696: delete
  db->Delete(write_options, "key0584");

  // op 7697: get
  status = db->Get(read_options, "key2401", &value);
  if (status.ok()) {
    out << "GET key2401 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7698: put
  db->Put(write_options, "key10915", "value84024");

  // op 7699: put
  db->Put(write_options, "key4964", "value91470");

  // op 7700: put
  db->Put(write_options, "key6600", "value87066");

  // op 7701: get
  status = db->Get(read_options, "key4340", &value);
  if (status.ok()) {
    out << "GET key4340 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7702: get
  status = db->Get(read_options, "key10734", &value);
  if (status.ok()) {
    out << "GET key10734 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7703: delete_range
  status = db->DeleteRange(write_options, "key0699", "key1676");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 7704: put
  db->Put(write_options, "key1034", "value70472");

  // op 7705: put
  db->Put(write_options, "key10791", "value89867");

  // op 7706: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3280", "key9280", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3280, key9280) size=" << scan_result.size() << std::endl;
  }

  // op 7707: put
  db->Put(write_options, "key2236", "value46834");

  // op 7708: get
  status = db->Get(read_options, "key1774", &value);
  if (status.ok()) {
    out << "GET key1774 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7709: delete_range
  status = db->DeleteRange(write_options, "key4519", "key7640");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 7710: put
  db->Put(write_options, "key4847", "value87565");

  // op 7711: get
  status = db->Get(read_options, "key4855", &value);
  if (status.ok()) {
    out << "GET key4855 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7712: put
  db->Put(write_options, "key8001", "value17711");

  // op 7713: delete_range
  status = db->DeleteRange(write_options, "key0650", "key1901");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 7714: get
  status = db->Get(read_options, "key8998", &value);
  if (status.ok()) {
    out << "GET key8998 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7715: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 7716: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7758", "key8745", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7758, key8745) size=" << scan_result.size() << std::endl;
  }

  // op 7717: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0640", "key4972", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0640, key4972) size=" << scan_result.size() << std::endl;
  }

  // op 7718: delete
  db->Delete(write_options, "key11630");

  // op 7719: put
  db->Put(write_options, "key10479", "value51490");

  // op 7720: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4117", "key8548", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4117, key8548) size=" << scan_result.size() << std::endl;
  }

  // op 7721: put
  db->Put(write_options, "key3983", "value2427");

  // op 7722: delete
  db->Delete(write_options, "key4180");

  // op 7723: get
  status = db->Get(read_options, "key10139", &value);
  if (status.ok()) {
    out << "GET key10139 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7724: put
  db->Put(write_options, "key3984", "value69964");

  // op 7725: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 7726: put
  db->Put(write_options, "key11365", "value64060");

  // op 7727: get
  status = db->Get(read_options, "key5276", &value);
  if (status.ok()) {
    out << "GET key5276 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7728: put
  db->Put(write_options, "key3162", "value72430");

  // op 7729: put
  db->Put(write_options, "key5509", "value80755");

  // op 7730: get
  status = db->Get(read_options, "key5051", &value);
  if (status.ok()) {
    out << "GET key5051 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7731: put
  db->Put(write_options, "key0217", "value19748");

  // op 7732: put
  db->Put(write_options, "key7883", "value36589");

  // op 7733: delete_range
  status = db->DeleteRange(write_options, "key7147", "key7846");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 7734: put
  db->Put(write_options, "key8017", "value24240");

  // op 7735: delete
  db->Delete(write_options, "key8410");

  // op 7736: delete_range
  status = db->DeleteRange(write_options, "key6418", "key9208");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 7737: get
  status = db->Get(read_options, "key2843", &value);
  if (status.ok()) {
    out << "GET key2843 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7738: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 7739: delete
  db->Delete(write_options, "key0195");

  // op 7740: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 7741: put
  db->Put(write_options, "key4851", "value4560");

  // op 7742: put
  db->Put(write_options, "key2919", "value29784");

  // op 7743: put
  db->Put(write_options, "key0227", "value11179");

  // op 7744: put
  db->Put(write_options, "key7000", "value54090");

  // op 7745: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2400", "key2711", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2400, key2711) size=" << scan_result.size() << std::endl;
  }

  // op 7746: put
  db->Put(write_options, "key6796", "value35458");

  // op 7747: put
  db->Put(write_options, "key9159", "value71856");

  // op 7748: get
  status = db->Get(read_options, "key8926", &value);
  if (status.ok()) {
    out << "GET key8926 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7749: put
  db->Put(write_options, "key0829", "value26203");

  // op 7750: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2400", "key9244", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2400, key9244) size=" << scan_result.size() << std::endl;
  }

  // op 7751: put
  db->Put(write_options, "key5904", "value97847");

  // op 7752: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2795", "key9251", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2795, key9251) size=" << scan_result.size() << std::endl;
  }

  // op 7753: delete
  db->Delete(write_options, "key2984");

  // op 7754: delete_range
  status = db->DeleteRange(write_options, "key0407", "key1905");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 7755: put
  db->Put(write_options, "key0552", "value46765");

  // op 7756: delete_range
  status = db->DeleteRange(write_options, "key4064", "key11501");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 7757: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3294", "key7624", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3294, key7624) size=" << scan_result.size() << std::endl;
  }

  // op 7758: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4044", "key9948", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4044, key9948) size=" << scan_result.size() << std::endl;
  }

  // op 7759: put
  db->Put(write_options, "key1569", "value59188");

  // op 7760: delete
  db->Delete(write_options, "key3911");

  // op 7761: delete
  db->Delete(write_options, "key4597");

  // op 7762: get
  status = db->Get(read_options, "key1222", &value);
  if (status.ok()) {
    out << "GET key1222 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7763: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1184", "key2709", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1184, key2709) size=" << scan_result.size() << std::endl;
  }

  // op 7764: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4256", "key10902", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4256, key10902) size=" << scan_result.size() << std::endl;
  }

  // op 7765: delete
  db->Delete(write_options, "key8643");

  // op 7766: get
  status = db->Get(read_options, "key7816", &value);
  if (status.ok()) {
    out << "GET key7816 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7767: get
  status = db->Get(read_options, "key6626", &value);
  if (status.ok()) {
    out << "GET key6626 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7768: delete
  db->Delete(write_options, "key3565");

  // op 7769: put
  db->Put(write_options, "key4924", "value12046");

  // op 7770: put
  db->Put(write_options, "key5453", "value73944");

  // op 7771: delete
  db->Delete(write_options, "key10910");

  // op 7772: put
  db->Put(write_options, "key4553", "value0502");

  // op 7773: put
  db->Put(write_options, "key8556", "value69295");

  // op 7774: delete_range
  status = db->DeleteRange(write_options, "key0847", "key2114");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 7775: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5365", "key11477", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5365, key11477) size=" << scan_result.size() << std::endl;
  }

  // op 7776: put
  db->Put(write_options, "key0332", "value81132");

  // op 7777: put
  db->Put(write_options, "key10351", "value44618");

  // op 7778: put
  db->Put(write_options, "key3917", "value8534");

  // op 7779: put
  db->Put(write_options, "key5495", "value20495");

  // op 7780: delete
  db->Delete(write_options, "key8885");

  // op 7781: put
  db->Put(write_options, "key5318", "value79181");

  // op 7782: put
  db->Put(write_options, "key2779", "value107129");

  // op 7783: put
  db->Put(write_options, "key2444", "value109456");

  // op 7784: delete
  db->Delete(write_options, "key10009");

  // op 7785: put
  db->Put(write_options, "key5373", "value102625");

  // op 7786: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9711", "key10305", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9711, key10305) size=" << scan_result.size() << std::endl;
  }

  // op 7787: put
  db->Put(write_options, "key11687", "value62372");

  // op 7788: put
  db->Put(write_options, "key7387", "value85812");

  // op 7789: put
  db->Put(write_options, "key6794", "value107027");

  // op 7790: put
  db->Put(write_options, "key4456", "value73773");

  // op 7791: put
  db->Put(write_options, "key2008", "value85484");

  // op 7792: put
  db->Put(write_options, "key9193", "value106463");

  // op 7793: put
  db->Put(write_options, "key3685", "value46508");

  // op 7794: put
  db->Put(write_options, "key9092", "value65208");

  // op 7795: get
  status = db->Get(read_options, "key8826", &value);
  if (status.ok()) {
    out << "GET key8826 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7796: delete
  db->Delete(write_options, "key7170");

  // op 7797: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0475", "key4934", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0475, key4934) size=" << scan_result.size() << std::endl;
  }

  // op 7798: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3410", "key5654", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3410, key5654) size=" << scan_result.size() << std::endl;
  }

  // op 7799: put
  db->Put(write_options, "key9644", "value114407");

  // op 7800: put
  db->Put(write_options, "key10925", "value78390");

  // op 7801: delete
  db->Delete(write_options, "key7895");

  // op 7802: put
  db->Put(write_options, "key9395", "value19561");

  // op 7803: delete
  db->Delete(write_options, "key3700");

  // op 7804: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 7805: put
  db->Put(write_options, "key9371", "value102446");

  // op 7806: put
  db->Put(write_options, "key6607", "value100357");

  // op 7807: put
  db->Put(write_options, "key3175", "value50590");

  // op 7808: put
  db->Put(write_options, "key0203", "value6241");

  // op 7809: get
  status = db->Get(read_options, "key2619", &value);
  if (status.ok()) {
    out << "GET key2619 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7810: put
  db->Put(write_options, "key11143", "value57608");

  // op 7811: get
  status = db->Get(read_options, "key11183", &value);
  if (status.ok()) {
    out << "GET key11183 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7812: put
  db->Put(write_options, "key1428", "value26348");

  // op 7813: get
  status = db->Get(read_options, "key10465", &value);
  if (status.ok()) {
    out << "GET key10465 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7814: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 7815: get
  status = db->Get(read_options, "key0753", &value);
  if (status.ok()) {
    out << "GET key0753 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7816: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 7817: get
  status = db->Get(read_options, "key9129", &value);
  if (status.ok()) {
    out << "GET key9129 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7818: put
  db->Put(write_options, "key3880", "value36846");

  // op 7819: get
  status = db->Get(read_options, "key1071", &value);
  if (status.ok()) {
    out << "GET key1071 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7820: delete_range
  status = db->DeleteRange(write_options, "key5308", "key9706");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 7821: get
  status = db->Get(read_options, "key9734", &value);
  if (status.ok()) {
    out << "GET key9734 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7822: get
  status = db->Get(read_options, "key6760", &value);
  if (status.ok()) {
    out << "GET key6760 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7823: delete_range
  status = db->DeleteRange(write_options, "key5085", "key9467");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 7824: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5085", "key7886", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5085, key7886) size=" << scan_result.size() << std::endl;
  }

  // op 7825: get
  status = db->Get(read_options, "key0428", &value);
  if (status.ok()) {
    out << "GET key0428 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7826: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0338", "key3317", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0338, key3317) size=" << scan_result.size() << std::endl;
  }

  // op 7827: put
  db->Put(write_options, "key1092", "value13856");

  // op 7828: put
  db->Put(write_options, "key10451", "value17611");

  // op 7829: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 7830: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3211", "key7588", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3211, key7588) size=" << scan_result.size() << std::endl;
  }

  // op 7831: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7252", "key7925", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7252, key7925) size=" << scan_result.size() << std::endl;
  }

  // op 7832: get
  status = db->Get(read_options, "key7839", &value);
  if (status.ok()) {
    out << "GET key7839 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7833: put
  db->Put(write_options, "key1557", "value15921");

  // op 7834: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0083", "key9037", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0083, key9037) size=" << scan_result.size() << std::endl;
  }

  // op 7835: delete
  db->Delete(write_options, "key7604");

  // op 7836: get
  status = db->Get(read_options, "key9392", &value);
  if (status.ok()) {
    out << "GET key9392 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7837: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 7838: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6905", "key10739", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6905, key10739) size=" << scan_result.size() << std::endl;
  }

  // op 7839: put
  db->Put(write_options, "key9895", "value116145");

  // op 7840: get
  status = db->Get(read_options, "key2845", &value);
  if (status.ok()) {
    out << "GET key2845 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7841: put
  db->Put(write_options, "key5497", "value86974");

  // op 7842: delete_range
  status = db->DeleteRange(write_options, "key0945", "key5654");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 7843: put
  db->Put(write_options, "key1790", "value54376");

  // op 7844: get
  status = db->Get(read_options, "key5303", &value);
  if (status.ok()) {
    out << "GET key5303 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7845: get
  status = db->Get(read_options, "key10001", &value);
  if (status.ok()) {
    out << "GET key10001 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7846: delete_range
  status = db->DeleteRange(write_options, "key0065", "key9988");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 7847: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 7848: get
  status = db->Get(read_options, "key3866", &value);
  if (status.ok()) {
    out << "GET key3866 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7849: get
  status = db->Get(read_options, "key9964", &value);
  if (status.ok()) {
    out << "GET key9964 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7850: put
  db->Put(write_options, "key4581", "value1693");

  // op 7851: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0271", "key11690", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0271, key11690) size=" << scan_result.size() << std::endl;
  }

  // op 7852: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0220", "key5146", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0220, key5146) size=" << scan_result.size() << std::endl;
  }

  // op 7853: get
  status = db->Get(read_options, "key9455", &value);
  if (status.ok()) {
    out << "GET key9455 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7854: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0919", "key5979", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0919, key5979) size=" << scan_result.size() << std::endl;
  }

  // op 7855: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6180", "key6605", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6180, key6605) size=" << scan_result.size() << std::endl;
  }

  // op 7856: delete_range
  status = db->DeleteRange(write_options, "key3646", "key4496");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 7857: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5806", "key8825", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5806, key8825) size=" << scan_result.size() << std::endl;
  }

  // op 7858: put
  db->Put(write_options, "key10773", "value79236");

  // op 7859: put
  db->Put(write_options, "key3658", "value21671");

  // op 7860: delete_range
  status = db->DeleteRange(write_options, "key3574", "key7219");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 7861: get
  status = db->Get(read_options, "key3028", &value);
  if (status.ok()) {
    out << "GET key3028 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7862: delete_range
  status = db->DeleteRange(write_options, "key2413", "key6874");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 7863: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10173", "key11594", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10173, key11594) size=" << scan_result.size() << std::endl;
  }

  // op 7864: delete
  db->Delete(write_options, "key1364");

  // op 7865: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2277", "key10437", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2277, key10437) size=" << scan_result.size() << std::endl;
  }

  // op 7866: get
  status = db->Get(read_options, "key9179", &value);
  if (status.ok()) {
    out << "GET key9179 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7867: delete
  db->Delete(write_options, "key10422");

  // op 7868: put
  db->Put(write_options, "key3247", "value118194");

  // op 7869: put
  db->Put(write_options, "key4964", "value0112");

  // op 7870: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8768", "key10355", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8768, key10355) size=" << scan_result.size() << std::endl;
  }

  // op 7871: get
  status = db->Get(read_options, "key3996", &value);
  if (status.ok()) {
    out << "GET key3996 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7872: put
  db->Put(write_options, "key2470", "value16041");

  // op 7873: put
  db->Put(write_options, "key1384", "value56642");

  // op 7874: delete
  db->Delete(write_options, "key6707");

  // op 7875: put
  db->Put(write_options, "key7960", "value92786");

  // op 7876: get
  status = db->Get(read_options, "key10413", &value);
  if (status.ok()) {
    out << "GET key10413 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7877: put
  db->Put(write_options, "key5596", "value84246");

  // op 7878: put
  db->Put(write_options, "key1828", "value9623");

  // op 7879: put
  db->Put(write_options, "key5811", "value110419");

  // op 7880: put
  db->Put(write_options, "key10796", "value19337");

  // op 7881: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 7882: delete_range
  status = db->DeleteRange(write_options, "key2587", "key8661");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 7883: put
  db->Put(write_options, "key6223", "value96043");

  // op 7884: get
  status = db->Get(read_options, "key0627", &value);
  if (status.ok()) {
    out << "GET key0627 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7885: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8052", "key8964", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8052, key8964) size=" << scan_result.size() << std::endl;
  }

  // op 7886: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3044", "key5332", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3044, key5332) size=" << scan_result.size() << std::endl;
  }

  // op 7887: delete
  db->Delete(write_options, "key0093");

  // op 7888: put
  db->Put(write_options, "key6761", "value2540");

  // op 7889: delete_range
  status = db->DeleteRange(write_options, "key9629", "key10957");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 7890: delete_range
  status = db->DeleteRange(write_options, "key2294", "key11018");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 7891: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2445", "key10099", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2445, key10099) size=" << scan_result.size() << std::endl;
  }

  // op 7892: get
  status = db->Get(read_options, "key0063", &value);
  if (status.ok()) {
    out << "GET key0063 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7893: delete
  db->Delete(write_options, "key3597");

  // op 7894: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1886", "key4495", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1886, key4495) size=" << scan_result.size() << std::endl;
  }

  // op 7895: delete
  db->Delete(write_options, "key11168");

  // op 7896: put
  db->Put(write_options, "key7562", "value81947");

  // op 7897: put
  db->Put(write_options, "key6665", "value107907");

  // op 7898: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5790", "key7125", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5790, key7125) size=" << scan_result.size() << std::endl;
  }

  // op 7899: get
  status = db->Get(read_options, "key0869", &value);
  if (status.ok()) {
    out << "GET key0869 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7900: get
  status = db->Get(read_options, "key1046", &value);
  if (status.ok()) {
    out << "GET key1046 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7901: put
  db->Put(write_options, "key6267", "value62142");

  // op 7902: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 7903: get
  status = db->Get(read_options, "key0216", &value);
  if (status.ok()) {
    out << "GET key0216 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7904: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3401", "key6556", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3401, key6556) size=" << scan_result.size() << std::endl;
  }

  // op 7905: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 7906: get
  status = db->Get(read_options, "key1024", &value);
  if (status.ok()) {
    out << "GET key1024 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7907: put
  db->Put(write_options, "key1399", "value12908");

  // op 7908: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 7909: put
  db->Put(write_options, "key0930", "value87662");

  // op 7910: delete_range
  status = db->DeleteRange(write_options, "key0186", "key5700");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 7911: delete
  db->Delete(write_options, "key1229");

  // op 7912: delete_range
  status = db->DeleteRange(write_options, "key0105", "key2719");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 7913: delete
  db->Delete(write_options, "key6298");

  // op 7914: delete
  db->Delete(write_options, "key4133");

  // op 7915: get
  status = db->Get(read_options, "key5314", &value);
  if (status.ok()) {
    out << "GET key5314 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7916: delete
  db->Delete(write_options, "key10415");

  // op 7917: get
  status = db->Get(read_options, "key0325", &value);
  if (status.ok()) {
    out << "GET key0325 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7918: put
  db->Put(write_options, "key5119", "value117483");

  // op 7919: put
  db->Put(write_options, "key5887", "value74379");

  // op 7920: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10305", "key11054", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10305, key11054) size=" << scan_result.size() << std::endl;
  }

  // op 7921: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1730", "key11014", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1730, key11014) size=" << scan_result.size() << std::endl;
  }

  // op 7922: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 7923: get
  status = db->Get(read_options, "key6347", &value);
  if (status.ok()) {
    out << "GET key6347 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7924: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3295", "key5590", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3295, key5590) size=" << scan_result.size() << std::endl;
  }

  // op 7925: get
  status = db->Get(read_options, "key1178", &value);
  if (status.ok()) {
    out << "GET key1178 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7926: put
  db->Put(write_options, "key10499", "value63019");

  // op 7927: get
  status = db->Get(read_options, "key6416", &value);
  if (status.ok()) {
    out << "GET key6416 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7928: get
  status = db->Get(read_options, "key2671", &value);
  if (status.ok()) {
    out << "GET key2671 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7929: put
  db->Put(write_options, "key9349", "value104409");

  // op 7930: get
  status = db->Get(read_options, "key7832", &value);
  if (status.ok()) {
    out << "GET key7832 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7931: put
  db->Put(write_options, "key5866", "value45192");

  // op 7932: get
  status = db->Get(read_options, "key0029", &value);
  if (status.ok()) {
    out << "GET key0029 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7933: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7286", "key9197", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7286, key9197) size=" << scan_result.size() << std::endl;
  }

  // op 7934: put
  db->Put(write_options, "key4312", "value42066");

  // op 7935: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3372", "key10908", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3372, key10908) size=" << scan_result.size() << std::endl;
  }

  // op 7936: delete
  db->Delete(write_options, "key4913");

  // op 7937: put
  db->Put(write_options, "key0284", "value44572");

  // op 7938: put
  db->Put(write_options, "key7173", "value66780");

  // op 7939: put
  db->Put(write_options, "key7489", "value89951");

  // op 7940: delete
  db->Delete(write_options, "key11275");

  // op 7941: put
  db->Put(write_options, "key8675", "value56585");

  // op 7942: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0674", "key10659", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0674, key10659) size=" << scan_result.size() << std::endl;
  }

  // op 7943: put
  db->Put(write_options, "key4668", "value90941");

  // op 7944: put
  db->Put(write_options, "key1016", "value44612");

  // op 7945: put
  db->Put(write_options, "key10252", "value47069");

  // op 7946: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4412", "key6813", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4412, key6813) size=" << scan_result.size() << std::endl;
  }

  // op 7947: put
  db->Put(write_options, "key1492", "value87626");

  // op 7948: delete_range
  status = db->DeleteRange(write_options, "key5600", "key6361");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 7949: put
  db->Put(write_options, "key8248", "value115323");

  // op 7950: get
  status = db->Get(read_options, "key8968", &value);
  if (status.ok()) {
    out << "GET key8968 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7951: put
  db->Put(write_options, "key5615", "value0075");

  // op 7952: delete
  db->Delete(write_options, "key5703");

  // op 7953: put
  db->Put(write_options, "key5901", "value75730");

  // op 7954: get
  status = db->Get(read_options, "key0492", &value);
  if (status.ok()) {
    out << "GET key0492 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7955: delete_range
  status = db->DeleteRange(write_options, "key0001", "key10308");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 7956: put
  db->Put(write_options, "key1714", "value17807");

  // op 7957: put
  db->Put(write_options, "key1297", "value50976");

  // op 7958: get
  status = db->Get(read_options, "key5635", &value);
  if (status.ok()) {
    out << "GET key5635 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7959: put
  db->Put(write_options, "key1884", "value57222");

  // op 7960: delete
  db->Delete(write_options, "key6987");

  // op 7961: put
  db->Put(write_options, "key5588", "value3567");

  // op 7962: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1598", "key2523", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1598, key2523) size=" << scan_result.size() << std::endl;
  }

  // op 7963: get
  status = db->Get(read_options, "key5714", &value);
  if (status.ok()) {
    out << "GET key5714 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7964: get
  status = db->Get(read_options, "key8092", &value);
  if (status.ok()) {
    out << "GET key8092 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7965: delete
  db->Delete(write_options, "key1148");

  // op 7966: put
  db->Put(write_options, "key8195", "value70541");

  // op 7967: delete
  db->Delete(write_options, "key0036");

  // op 7968: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7007", "key9275", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7007, key9275) size=" << scan_result.size() << std::endl;
  }

  // op 7969: put
  db->Put(write_options, "key0444", "value57838");

  // op 7970: get
  status = db->Get(read_options, "key0491", &value);
  if (status.ok()) {
    out << "GET key0491 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7971: put
  db->Put(write_options, "key5934", "value49678");

  // op 7972: put
  db->Put(write_options, "key0661", "value92493");

  // op 7973: delete
  db->Delete(write_options, "key6234");

  // op 7974: put
  db->Put(write_options, "key4561", "value101832");

  // op 7975: put
  db->Put(write_options, "key1847", "value29368");

  // op 7976: put
  db->Put(write_options, "key9785", "value83567");

  // op 7977: get
  status = db->Get(read_options, "key3761", &value);
  if (status.ok()) {
    out << "GET key3761 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7978: put
  db->Put(write_options, "key1810", "value94531");

  // op 7979: get
  status = db->Get(read_options, "key6068", &value);
  if (status.ok()) {
    out << "GET key6068 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7980: put
  db->Put(write_options, "key0981", "value26440");

  // op 7981: delete
  db->Delete(write_options, "key3440");

  // op 7982: get
  status = db->Get(read_options, "key4228", &value);
  if (status.ok()) {
    out << "GET key4228 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7983: get
  status = db->Get(read_options, "key2601", &value);
  if (status.ok()) {
    out << "GET key2601 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7984: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4393", "key5213", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4393, key5213) size=" << scan_result.size() << std::endl;
  }

  // op 7985: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3418", "key7985", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3418, key7985) size=" << scan_result.size() << std::endl;
  }

  // op 7986: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1804", "key10484", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1804, key10484) size=" << scan_result.size() << std::endl;
  }

  // op 7987: put
  db->Put(write_options, "key5354", "value92763");

  // op 7988: put
  db->Put(write_options, "key2156", "value22475");

  // op 7989: get
  status = db->Get(read_options, "key8085", &value);
  if (status.ok()) {
    out << "GET key8085 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7990: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1907", "key9989", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1907, key9989) size=" << scan_result.size() << std::endl;
  }

  // op 7991: put
  db->Put(write_options, "key8704", "value7025");

  // op 7992: put
  db->Put(write_options, "key11332", "value20682");

  // op 7993: get
  status = db->Get(read_options, "key10450", &value);
  if (status.ok()) {
    out << "GET key10450 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7994: put
  db->Put(write_options, "key9426", "value42441");

  // op 7995: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 7996: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5610", "key8466", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5610, key8466) size=" << scan_result.size() << std::endl;
  }

  // op 7997: get
  status = db->Get(read_options, "key4718", &value);
  if (status.ok()) {
    out << "GET key4718 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 7998: put
  db->Put(write_options, "key7822", "value35718");

  // op 7999: put
  db->Put(write_options, "key11049", "value61241");

  // op 8000: get
  status = db->Get(read_options, "key4828", &value);
  if (status.ok()) {
    out << "GET key4828 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8001: put
  db->Put(write_options, "key0646", "value105278");

  // op 8002: get
  status = db->Get(read_options, "key3778", &value);
  if (status.ok()) {
    out << "GET key3778 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8003: put
  db->Put(write_options, "key0942", "value7915");

  // op 8004: get
  status = db->Get(read_options, "key7576", &value);
  if (status.ok()) {
    out << "GET key7576 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8005: put
  db->Put(write_options, "key2610", "value86920");

  // op 8006: delete_range
  status = db->DeleteRange(write_options, "key2342", "key10026");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8007: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2203", "key11687", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2203, key11687) size=" << scan_result.size() << std::endl;
  }

  // op 8008: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 8009: get
  status = db->Get(read_options, "key6583", &value);
  if (status.ok()) {
    out << "GET key6583 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8010: put
  db->Put(write_options, "key10908", "value39359");

  // op 8011: delete_range
  status = db->DeleteRange(write_options, "key4215", "key6198");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8012: get
  status = db->Get(read_options, "key2352", &value);
  if (status.ok()) {
    out << "GET key2352 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8013: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6487", "key10930", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6487, key10930) size=" << scan_result.size() << std::endl;
  }

  // op 8014: get
  status = db->Get(read_options, "key7021", &value);
  if (status.ok()) {
    out << "GET key7021 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8015: put
  db->Put(write_options, "key5964", "value42483");

  // op 8016: put
  db->Put(write_options, "key7290", "value37802");

  // op 8017: delete
  db->Delete(write_options, "key7206");

  // op 8018: put
  db->Put(write_options, "key11066", "value101869");

  // op 8019: get
  status = db->Get(read_options, "key5088", &value);
  if (status.ok()) {
    out << "GET key5088 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8020: put
  db->Put(write_options, "key0260", "value1418");

  // op 8021: put
  db->Put(write_options, "key7958", "value18511");

  // op 8022: put
  db->Put(write_options, "key5519", "value38509");

  // op 8023: delete_range
  status = db->DeleteRange(write_options, "key5345", "key10092");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8024: get
  status = db->Get(read_options, "key1621", &value);
  if (status.ok()) {
    out << "GET key1621 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8025: put
  db->Put(write_options, "key8267", "value92749");

  // op 8026: put
  db->Put(write_options, "key5064", "value81994");

  // op 8027: get
  status = db->Get(read_options, "key8044", &value);
  if (status.ok()) {
    out << "GET key8044 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8028: put
  db->Put(write_options, "key5666", "value70391");

  // op 8029: delete
  db->Delete(write_options, "key6638");

  // op 8030: put
  db->Put(write_options, "key6879", "value61397");

  // op 8031: put
  db->Put(write_options, "key3393", "value110749");

  // op 8032: put
  db->Put(write_options, "key8704", "value22919");

  // op 8033: put
  db->Put(write_options, "key6135", "value57853");

  // op 8034: put
  db->Put(write_options, "key9929", "value26520");

  // op 8035: delete
  db->Delete(write_options, "key7969");

  // op 8036: put
  db->Put(write_options, "key11507", "value54202");

  // op 8037: get
  status = db->Get(read_options, "key2216", &value);
  if (status.ok()) {
    out << "GET key2216 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8038: get
  status = db->Get(read_options, "key6025", &value);
  if (status.ok()) {
    out << "GET key6025 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8039: delete_range
  status = db->DeleteRange(write_options, "key5460", "key10830");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8040: put
  db->Put(write_options, "key7839", "value47080");

  // op 8041: put
  db->Put(write_options, "key0338", "value6408");

  // op 8042: get
  status = db->Get(read_options, "key7912", &value);
  if (status.ok()) {
    out << "GET key7912 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8043: put
  db->Put(write_options, "key1399", "value66726");

  // op 8044: delete_range
  status = db->DeleteRange(write_options, "key0575", "key2751");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8045: put
  db->Put(write_options, "key8080", "value94260");

  // op 8046: delete_range
  status = db->DeleteRange(write_options, "key2300", "key6918");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8047: put
  db->Put(write_options, "key0473", "value94767");

  // op 8048: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 8049: put
  db->Put(write_options, "key1507", "value71086");

  // op 8050: put
  db->Put(write_options, "key6355", "value33611");

  // op 8051: delete_range
  status = db->DeleteRange(write_options, "key0013", "key8518");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8052: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4983", "key5399", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4983, key5399) size=" << scan_result.size() << std::endl;
  }

  // op 8053: put
  db->Put(write_options, "key4198", "value1707");

  // op 8054: delete
  db->Delete(write_options, "key6485");

  // op 8055: delete_range
  status = db->DeleteRange(write_options, "key2997", "key5239");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8056: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2450", "key9997", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2450, key9997) size=" << scan_result.size() << std::endl;
  }

  // op 8057: get
  status = db->Get(read_options, "key8406", &value);
  if (status.ok()) {
    out << "GET key8406 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8058: get
  status = db->Get(read_options, "key2988", &value);
  if (status.ok()) {
    out << "GET key2988 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8059: put
  db->Put(write_options, "key0676", "value94083");

  // op 8060: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6047", "key8775", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6047, key8775) size=" << scan_result.size() << std::endl;
  }

  // op 8061: get
  status = db->Get(read_options, "key9941", &value);
  if (status.ok()) {
    out << "GET key9941 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8062: put
  db->Put(write_options, "key5773", "value13393");

  // op 8063: put
  db->Put(write_options, "key3149", "value11317");

  // op 8064: get
  status = db->Get(read_options, "key7228", &value);
  if (status.ok()) {
    out << "GET key7228 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8065: get
  status = db->Get(read_options, "key2729", &value);
  if (status.ok()) {
    out << "GET key2729 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8066: put
  db->Put(write_options, "key4983", "value25157");

  // op 8067: delete
  db->Delete(write_options, "key4256");

  // op 8068: put
  db->Put(write_options, "key7585", "value91523");

  // op 8069: get
  status = db->Get(read_options, "key3262", &value);
  if (status.ok()) {
    out << "GET key3262 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8070: put
  db->Put(write_options, "key0613", "value67252");

  // op 8071: put
  db->Put(write_options, "key7725", "value41118");

  // op 8072: delete_range
  status = db->DeleteRange(write_options, "key8184", "key9115");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8073: put
  db->Put(write_options, "key1303", "value32218");

  // op 8074: delete_range
  status = db->DeleteRange(write_options, "key1719", "key10127");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8075: delete
  db->Delete(write_options, "key6311");

  // op 8076: put
  db->Put(write_options, "key0887", "value116050");

  // op 8077: put
  db->Put(write_options, "key0009", "value9015");

  // op 8078: get
  status = db->Get(read_options, "key7575", &value);
  if (status.ok()) {
    out << "GET key7575 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8079: put
  db->Put(write_options, "key4120", "value87062");

  // op 8080: get
  status = db->Get(read_options, "key10091", &value);
  if (status.ok()) {
    out << "GET key10091 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8081: delete
  db->Delete(write_options, "key3811");

  // op 8082: put
  db->Put(write_options, "key7656", "value49441");

  // op 8083: get
  status = db->Get(read_options, "key0635", &value);
  if (status.ok()) {
    out << "GET key0635 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8084: put
  db->Put(write_options, "key10403", "value72300");

  // op 8085: put
  db->Put(write_options, "key5785", "value48705");

  // op 8086: put
  db->Put(write_options, "key1374", "value115439");

  // op 8087: get
  status = db->Get(read_options, "key1142", &value);
  if (status.ok()) {
    out << "GET key1142 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8088: delete_range
  status = db->DeleteRange(write_options, "key3134", "key9146");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8089: delete
  db->Delete(write_options, "key3335");

  // op 8090: delete_range
  status = db->DeleteRange(write_options, "key8723", "key9412");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8091: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7959", "key8253", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7959, key8253) size=" << scan_result.size() << std::endl;
  }

  // op 8092: put
  db->Put(write_options, "key4655", "value69908");

  // op 8093: get
  status = db->Get(read_options, "key2139", &value);
  if (status.ok()) {
    out << "GET key2139 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8094: delete
  db->Delete(write_options, "key10222");

  // op 8095: get
  status = db->Get(read_options, "key11427", &value);
  if (status.ok()) {
    out << "GET key11427 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8096: put
  db->Put(write_options, "key9998", "value16224");

  // op 8097: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3585", "key9344", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3585, key9344) size=" << scan_result.size() << std::endl;
  }

  // op 8098: get
  status = db->Get(read_options, "key0371", &value);
  if (status.ok()) {
    out << "GET key0371 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8099: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2936", "key3870", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2936, key3870) size=" << scan_result.size() << std::endl;
  }

  // op 8100: delete
  db->Delete(write_options, "key6437");

  // op 8101: delete_range
  status = db->DeleteRange(write_options, "key5835", "key10369");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8102: put
  db->Put(write_options, "key10948", "value38622");

  // op 8103: get
  status = db->Get(read_options, "key4398", &value);
  if (status.ok()) {
    out << "GET key4398 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8104: put
  db->Put(write_options, "key1723", "value0760");

  // op 8105: delete
  db->Delete(write_options, "key8475");

  // op 8106: delete_range
  status = db->DeleteRange(write_options, "key3208", "key4956");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8107: put
  db->Put(write_options, "key9785", "value73174");

  // op 8108: put
  db->Put(write_options, "key4217", "value38433");

  // op 8109: put
  db->Put(write_options, "key7965", "value92172");

  // op 8110: get
  status = db->Get(read_options, "key1367", &value);
  if (status.ok()) {
    out << "GET key1367 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8111: delete_range
  status = db->DeleteRange(write_options, "key1528", "key11704");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8112: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0636", "key5366", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0636, key5366) size=" << scan_result.size() << std::endl;
  }

  // op 8113: put
  db->Put(write_options, "key8530", "value99810");

  // op 8114: delete_range
  status = db->DeleteRange(write_options, "key10400", "key11215");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8115: get
  status = db->Get(read_options, "key1068", &value);
  if (status.ok()) {
    out << "GET key1068 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8116: delete_range
  status = db->DeleteRange(write_options, "key7008", "key11682");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8117: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2101", "key8878", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2101, key8878) size=" << scan_result.size() << std::endl;
  }

  // op 8118: put
  db->Put(write_options, "key1150", "value69986");

  // op 8119: put
  db->Put(write_options, "key6163", "value87251");

  // op 8120: get
  status = db->Get(read_options, "key1828", &value);
  if (status.ok()) {
    out << "GET key1828 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8121: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 8122: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0330", "key5621", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0330, key5621) size=" << scan_result.size() << std::endl;
  }

  // op 8123: put
  db->Put(write_options, "key2032", "value77096");

  // op 8124: put
  db->Put(write_options, "key10814", "value4318");

  // op 8125: put
  db->Put(write_options, "key10641", "value87440");

  // op 8126: delete_range
  status = db->DeleteRange(write_options, "key2003", "key9657");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8127: put
  db->Put(write_options, "key7372", "value16992");

  // op 8128: put
  db->Put(write_options, "key2789", "value114645");

  // op 8129: get
  status = db->Get(read_options, "key2284", &value);
  if (status.ok()) {
    out << "GET key2284 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8130: get
  status = db->Get(read_options, "key8320", &value);
  if (status.ok()) {
    out << "GET key8320 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8131: get
  status = db->Get(read_options, "key6321", &value);
  if (status.ok()) {
    out << "GET key6321 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8132: put
  db->Put(write_options, "key1343", "value79346");

  // op 8133: get
  status = db->Get(read_options, "key9269", &value);
  if (status.ok()) {
    out << "GET key9269 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8134: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3756", "key4218", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3756, key4218) size=" << scan_result.size() << std::endl;
  }

  // op 8135: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 8136: put
  db->Put(write_options, "key3589", "value66547");

  // op 8137: get
  status = db->Get(read_options, "key4563", &value);
  if (status.ok()) {
    out << "GET key4563 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8138: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 8139: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0637", "key2117", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0637, key2117) size=" << scan_result.size() << std::endl;
  }

  // op 8140: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9128", "key11409", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9128, key11409) size=" << scan_result.size() << std::endl;
  }

  // op 8141: put
  db->Put(write_options, "key11509", "value16922");

  // op 8142: put
  db->Put(write_options, "key8247", "value63608");

  // op 8143: delete
  db->Delete(write_options, "key4898");

  // op 8144: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0618", "key1520", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0618, key1520) size=" << scan_result.size() << std::endl;
  }

  // op 8145: get
  status = db->Get(read_options, "key9670", &value);
  if (status.ok()) {
    out << "GET key9670 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8146: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 8147: get
  status = db->Get(read_options, "key1804", &value);
  if (status.ok()) {
    out << "GET key1804 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8148: put
  db->Put(write_options, "key2914", "value7126");

  // op 8149: put
  db->Put(write_options, "key6626", "value13810");

  // op 8150: get
  status = db->Get(read_options, "key3922", &value);
  if (status.ok()) {
    out << "GET key3922 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8151: put
  db->Put(write_options, "key9494", "value38202");

  // op 8152: delete_range
  status = db->DeleteRange(write_options, "key1362", "key5741");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8153: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2718", "key8582", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2718, key8582) size=" << scan_result.size() << std::endl;
  }

  // op 8154: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9063", "key10610", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9063, key10610) size=" << scan_result.size() << std::endl;
  }

  // op 8155: delete
  db->Delete(write_options, "key4239");

  // op 8156: put
  db->Put(write_options, "key0216", "value100983");

  // op 8157: put
  db->Put(write_options, "key2630", "value32219");

  // op 8158: get
  status = db->Get(read_options, "key7829", &value);
  if (status.ok()) {
    out << "GET key7829 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8159: delete
  db->Delete(write_options, "key10454");

  // op 8160: put
  db->Put(write_options, "key11292", "value51940");

  // op 8161: delete_range
  status = db->DeleteRange(write_options, "key6935", "key10018");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8162: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2591", "key7380", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2591, key7380) size=" << scan_result.size() << std::endl;
  }

  // op 8163: delete_range
  status = db->DeleteRange(write_options, "key9569", "key10982");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8164: delete
  db->Delete(write_options, "key7506");

  // op 8165: put
  db->Put(write_options, "key11699", "value71069");

  // op 8166: delete
  db->Delete(write_options, "key10009");

  // op 8167: put
  db->Put(write_options, "key0712", "value111623");

  // op 8168: delete_range
  status = db->DeleteRange(write_options, "key10588", "key11430");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8169: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3344", "key9862", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3344, key9862) size=" << scan_result.size() << std::endl;
  }

  // op 8170: put
  db->Put(write_options, "key4474", "value93443");

  // op 8171: get
  status = db->Get(read_options, "key2765", &value);
  if (status.ok()) {
    out << "GET key2765 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8172: delete_range
  status = db->DeleteRange(write_options, "key2340", "key7770");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8173: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 8174: put
  db->Put(write_options, "key5908", "value114311");

  // op 8175: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2235", "key4307", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2235, key4307) size=" << scan_result.size() << std::endl;
  }

  // op 8176: put
  db->Put(write_options, "key3470", "value55084");

  // op 8177: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 8178: put
  db->Put(write_options, "key5191", "value45337");

  // op 8179: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1349", "key7767", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1349, key7767) size=" << scan_result.size() << std::endl;
  }

  // op 8180: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4928", "key5685", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4928, key5685) size=" << scan_result.size() << std::endl;
  }

  // op 8181: delete_range
  status = db->DeleteRange(write_options, "key1082", "key10639");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8182: get
  status = db->Get(read_options, "key11344", &value);
  if (status.ok()) {
    out << "GET key11344 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8183: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 8184: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8208", "key9774", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8208, key9774) size=" << scan_result.size() << std::endl;
  }

  // op 8185: get
  status = db->Get(read_options, "key5438", &value);
  if (status.ok()) {
    out << "GET key5438 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8186: delete_range
  status = db->DeleteRange(write_options, "key6419", "key8666");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8187: put
  db->Put(write_options, "key0483", "value85655");

  // op 8188: delete_range
  status = db->DeleteRange(write_options, "key2631", "key6882");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8189: put
  db->Put(write_options, "key2361", "value92001");

  // op 8190: delete
  db->Delete(write_options, "key2031");

  // op 8191: put
  db->Put(write_options, "key6298", "value45999");

  // op 8192: get
  status = db->Get(read_options, "key6497", &value);
  if (status.ok()) {
    out << "GET key6497 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8193: put
  db->Put(write_options, "key7710", "value50839");

  // op 8194: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0623", "key3091", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0623, key3091) size=" << scan_result.size() << std::endl;
  }

  // op 8195: put
  db->Put(write_options, "key2365", "value97674");

  // op 8196: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7211", "key9663", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7211, key9663) size=" << scan_result.size() << std::endl;
  }

  // op 8197: put
  db->Put(write_options, "key6785", "value100366");

  // op 8198: put
  db->Put(write_options, "key9252", "value47575");

  // op 8199: get
  status = db->Get(read_options, "key8883", &value);
  if (status.ok()) {
    out << "GET key8883 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8200: put
  db->Put(write_options, "key2817", "value48305");

  // op 8201: put
  db->Put(write_options, "key0887", "value71295");

  // op 8202: delete_range
  status = db->DeleteRange(write_options, "key3096", "key9921");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8203: put
  db->Put(write_options, "key2618", "value96679");

  // op 8204: put
  db->Put(write_options, "key8249", "value106366");

  // op 8205: put
  db->Put(write_options, "key10154", "value87207");

  // op 8206: put
  db->Put(write_options, "key0216", "value47686");

  // op 8207: delete
  db->Delete(write_options, "key4417");

  // op 8208: delete_range
  status = db->DeleteRange(write_options, "key7656", "key8021");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8209: get
  status = db->Get(read_options, "key9874", &value);
  if (status.ok()) {
    out << "GET key9874 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8210: delete
  db->Delete(write_options, "key0880");

  // op 8211: put
  db->Put(write_options, "key9812", "value29125");

  // op 8212: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5839", "key6488", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5839, key6488) size=" << scan_result.size() << std::endl;
  }

  // op 8213: get
  status = db->Get(read_options, "key9540", &value);
  if (status.ok()) {
    out << "GET key9540 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8214: delete_range
  status = db->DeleteRange(write_options, "key3350", "key7944");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8215: put
  db->Put(write_options, "key10899", "value96984");

  // op 8216: put
  db->Put(write_options, "key8298", "value117283");

  // op 8217: put
  db->Put(write_options, "key3830", "value14073");

  // op 8218: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4597", "key9090", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4597, key9090) size=" << scan_result.size() << std::endl;
  }

  // op 8219: delete_range
  status = db->DeleteRange(write_options, "key2549", "key7200");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8220: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 8221: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 8222: put
  db->Put(write_options, "key5413", "value25553");

  // op 8223: put
  db->Put(write_options, "key9360", "value111687");

  // op 8224: put
  db->Put(write_options, "key7721", "value16298");

  // op 8225: get
  status = db->Get(read_options, "key3500", &value);
  if (status.ok()) {
    out << "GET key3500 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8226: delete_range
  status = db->DeleteRange(write_options, "key8175", "key8776");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8227: put
  db->Put(write_options, "key5816", "value5043");

  // op 8228: get
  status = db->Get(read_options, "key11329", &value);
  if (status.ok()) {
    out << "GET key11329 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8229: put
  db->Put(write_options, "key1857", "value89477");

  // op 8230: delete_range
  status = db->DeleteRange(write_options, "key1716", "key9469");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8231: delete_range
  status = db->DeleteRange(write_options, "key7321", "key8295");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8232: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 8233: put
  db->Put(write_options, "key1650", "value93205");

  // op 8234: delete_range
  status = db->DeleteRange(write_options, "key5116", "key11613");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8235: put
  db->Put(write_options, "key5038", "value36729");

  // op 8236: put
  db->Put(write_options, "key4833", "value64299");

  // op 8237: delete_range
  status = db->DeleteRange(write_options, "key3244", "key9738");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8238: get
  status = db->Get(read_options, "key1443", &value);
  if (status.ok()) {
    out << "GET key1443 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8239: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1435", "key2391", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1435, key2391) size=" << scan_result.size() << std::endl;
  }

  // op 8240: put
  db->Put(write_options, "key2365", "value62189");

  // op 8241: put
  db->Put(write_options, "key8327", "value28226");

  // op 8242: put
  db->Put(write_options, "key3527", "value89722");

  // op 8243: delete
  db->Delete(write_options, "key11108");

  // op 8244: get
  status = db->Get(read_options, "key0568", &value);
  if (status.ok()) {
    out << "GET key0568 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8245: delete_range
  status = db->DeleteRange(write_options, "key4993", "key11187");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8246: get
  status = db->Get(read_options, "key0729", &value);
  if (status.ok()) {
    out << "GET key0729 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8247: put
  db->Put(write_options, "key2530", "value37507");

  // op 8248: delete
  db->Delete(write_options, "key11608");

  // op 8249: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0971", "key3098", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0971, key3098) size=" << scan_result.size() << std::endl;
  }

  // op 8250: delete
  db->Delete(write_options, "key3134");

  // op 8251: put
  db->Put(write_options, "key8449", "value46801");

  // op 8252: delete_range
  status = db->DeleteRange(write_options, "key4371", "key5796");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8253: put
  db->Put(write_options, "key8353", "value108598");

  // op 8254: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2310", "key2965", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2310, key2965) size=" << scan_result.size() << std::endl;
  }

  // op 8255: put
  db->Put(write_options, "key1227", "value59274");

  // op 8256: delete_range
  status = db->DeleteRange(write_options, "key2009", "key10283");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8257: get
  status = db->Get(read_options, "key5688", &value);
  if (status.ok()) {
    out << "GET key5688 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8258: get
  status = db->Get(read_options, "key2146", &value);
  if (status.ok()) {
    out << "GET key2146 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8259: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1786", "key11381", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1786, key11381) size=" << scan_result.size() << std::endl;
  }

  // op 8260: delete_range
  status = db->DeleteRange(write_options, "key4606", "key4791");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8261: get
  status = db->Get(read_options, "key9710", &value);
  if (status.ok()) {
    out << "GET key9710 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8262: put
  db->Put(write_options, "key3652", "value39498");

  // op 8263: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3799", "key10462", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3799, key10462) size=" << scan_result.size() << std::endl;
  }

  // op 8264: delete_range
  status = db->DeleteRange(write_options, "key3152", "key3207");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8265: get
  status = db->Get(read_options, "key11077", &value);
  if (status.ok()) {
    out << "GET key11077 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8266: put
  db->Put(write_options, "key9011", "value108167");

  // op 8267: get
  status = db->Get(read_options, "key4888", &value);
  if (status.ok()) {
    out << "GET key4888 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8268: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4930", "key11595", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4930, key11595) size=" << scan_result.size() << std::endl;
  }

  // op 8269: get
  status = db->Get(read_options, "key0541", &value);
  if (status.ok()) {
    out << "GET key0541 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8270: put
  db->Put(write_options, "key0363", "value62275");

  // op 8271: put
  db->Put(write_options, "key11428", "value49435");

  // op 8272: get
  status = db->Get(read_options, "key7368", &value);
  if (status.ok()) {
    out << "GET key7368 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8273: delete_range
  status = db->DeleteRange(write_options, "key1988", "key7413");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8274: delete
  db->Delete(write_options, "key1711");

  // op 8275: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8379", "key11140", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8379, key11140) size=" << scan_result.size() << std::endl;
  }

  // op 8276: delete_range
  status = db->DeleteRange(write_options, "key2187", "key2692");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8277: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1003", "key3785", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1003, key3785) size=" << scan_result.size() << std::endl;
  }

  // op 8278: put
  db->Put(write_options, "key9684", "value58988");

  // op 8279: put
  db->Put(write_options, "key3991", "value74265");

  // op 8280: delete_range
  status = db->DeleteRange(write_options, "key5789", "key9822");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8281: get
  status = db->Get(read_options, "key5990", &value);
  if (status.ok()) {
    out << "GET key5990 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8282: put
  db->Put(write_options, "key8876", "value99919");

  // op 8283: delete_range
  status = db->DeleteRange(write_options, "key7979", "key9004");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8284: delete
  db->Delete(write_options, "key2430");

  // op 8285: get
  status = db->Get(read_options, "key3655", &value);
  if (status.ok()) {
    out << "GET key3655 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8286: put
  db->Put(write_options, "key3802", "value17148");

  // op 8287: put
  db->Put(write_options, "key4681", "value108690");

  // op 8288: put
  db->Put(write_options, "key8440", "value2010");

  // op 8289: put
  db->Put(write_options, "key9274", "value26776");

  // op 8290: put
  db->Put(write_options, "key5210", "value60599");

  // op 8291: put
  db->Put(write_options, "key10289", "value57772");

  // op 8292: put
  db->Put(write_options, "key9326", "value65935");

  // op 8293: put
  db->Put(write_options, "key5814", "value42711");

  // op 8294: put
  db->Put(write_options, "key0386", "value98907");

  // op 8295: put
  db->Put(write_options, "key9693", "value33637");

  // op 8296: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5603", "key6051", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5603, key6051) size=" << scan_result.size() << std::endl;
  }

  // op 8297: put
  db->Put(write_options, "key2450", "value34373");

  // op 8298: get
  status = db->Get(read_options, "key0922", &value);
  if (status.ok()) {
    out << "GET key0922 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8299: put
  db->Put(write_options, "key10893", "value79033");

  // op 8300: delete
  db->Delete(write_options, "key7030");

  // op 8301: put
  db->Put(write_options, "key11472", "value72615");

  // op 8302: put
  db->Put(write_options, "key11454", "value53470");

  // op 8303: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4604", "key5559", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4604, key5559) size=" << scan_result.size() << std::endl;
  }

  // op 8304: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 8305: put
  db->Put(write_options, "key0270", "value15542");

  // op 8306: get
  status = db->Get(read_options, "key8475", &value);
  if (status.ok()) {
    out << "GET key8475 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8307: get
  status = db->Get(read_options, "key6512", &value);
  if (status.ok()) {
    out << "GET key6512 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8308: delete_range
  status = db->DeleteRange(write_options, "key0601", "key11617");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8309: get
  status = db->Get(read_options, "key1771", &value);
  if (status.ok()) {
    out << "GET key1771 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8310: get
  status = db->Get(read_options, "key2456", &value);
  if (status.ok()) {
    out << "GET key2456 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8311: put
  db->Put(write_options, "key10996", "value65757");

  // op 8312: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3596", "key9394", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3596, key9394) size=" << scan_result.size() << std::endl;
  }

  // op 8313: put
  db->Put(write_options, "key1965", "value7220");

  // op 8314: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1015", "key9748", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1015, key9748) size=" << scan_result.size() << std::endl;
  }

  // op 8315: delete
  db->Delete(write_options, "key2110");

  // op 8316: put
  db->Put(write_options, "key5993", "value110579");

  // op 8317: put
  db->Put(write_options, "key9262", "value4750");

  // op 8318: put
  db->Put(write_options, "key3042", "value37011");

  // op 8319: get
  status = db->Get(read_options, "key8397", &value);
  if (status.ok()) {
    out << "GET key8397 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8320: put
  db->Put(write_options, "key3606", "value63451");

  // op 8321: delete_range
  status = db->DeleteRange(write_options, "key6247", "key11653");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8322: put
  db->Put(write_options, "key10665", "value31352");

  // op 8323: get
  status = db->Get(read_options, "key0099", &value);
  if (status.ok()) {
    out << "GET key0099 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8324: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1591", "key10700", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1591, key10700) size=" << scan_result.size() << std::endl;
  }

  // op 8325: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5719", "key8506", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5719, key8506) size=" << scan_result.size() << std::endl;
  }

  // op 8326: put
  db->Put(write_options, "key4064", "value110764");

  // op 8327: get
  status = db->Get(read_options, "key11057", &value);
  if (status.ok()) {
    out << "GET key11057 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8328: put
  db->Put(write_options, "key3854", "value86754");

  // op 8329: get
  status = db->Get(read_options, "key6673", &value);
  if (status.ok()) {
    out << "GET key6673 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8330: get
  status = db->Get(read_options, "key5004", &value);
  if (status.ok()) {
    out << "GET key5004 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8331: put
  db->Put(write_options, "key2159", "value112535");

  // op 8332: put
  db->Put(write_options, "key5758", "value115969");

  // op 8333: put
  db->Put(write_options, "key2130", "value45113");

  // op 8334: delete_range
  status = db->DeleteRange(write_options, "key9090", "key11546");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8335: get
  status = db->Get(read_options, "key5857", &value);
  if (status.ok()) {
    out << "GET key5857 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8336: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8207", "key10734", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8207, key10734) size=" << scan_result.size() << std::endl;
  }

  // op 8337: put
  db->Put(write_options, "key8994", "value13520");

  // op 8338: put
  db->Put(write_options, "key8157", "value100878");

  // op 8339: put
  db->Put(write_options, "key0269", "value28259");

  // op 8340: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 8341: delete_range
  status = db->DeleteRange(write_options, "key9385", "key11445");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8342: get
  status = db->Get(read_options, "key0632", &value);
  if (status.ok()) {
    out << "GET key0632 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8343: put
  db->Put(write_options, "key7168", "value13807");

  // op 8344: put
  db->Put(write_options, "key0910", "value106996");

  // op 8345: get
  status = db->Get(read_options, "key5917", &value);
  if (status.ok()) {
    out << "GET key5917 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8346: put
  db->Put(write_options, "key7829", "value14620");

  // op 8347: put
  db->Put(write_options, "key10161", "value69185");

  // op 8348: put
  db->Put(write_options, "key5263", "value4285");

  // op 8349: put
  db->Put(write_options, "key5130", "value60111");

  // op 8350: put
  db->Put(write_options, "key10381", "value110613");

  // op 8351: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10895", "key11594", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10895, key11594) size=" << scan_result.size() << std::endl;
  }

  // op 8352: get
  status = db->Get(read_options, "key8791", &value);
  if (status.ok()) {
    out << "GET key8791 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8353: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5379", "key9405", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5379, key9405) size=" << scan_result.size() << std::endl;
  }

  // op 8354: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6051", "key7321", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6051, key7321) size=" << scan_result.size() << std::endl;
  }

  // op 8355: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0387", "key7401", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0387, key7401) size=" << scan_result.size() << std::endl;
  }

  // op 8356: put
  db->Put(write_options, "key1275", "value63562");

  // op 8357: delete
  db->Delete(write_options, "key3037");

  // op 8358: delete_range
  status = db->DeleteRange(write_options, "key2181", "key5133");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8359: put
  db->Put(write_options, "key6544", "value33202");

  // op 8360: delete
  db->Delete(write_options, "key10351");

  // op 8361: put
  db->Put(write_options, "key7834", "value82146");

  // op 8362: get
  status = db->Get(read_options, "key3286", &value);
  if (status.ok()) {
    out << "GET key3286 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8363: get
  status = db->Get(read_options, "key7203", &value);
  if (status.ok()) {
    out << "GET key7203 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8364: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7846", "key9039", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7846, key9039) size=" << scan_result.size() << std::endl;
  }

  // op 8365: delete
  db->Delete(write_options, "key9121");

  // op 8366: put
  db->Put(write_options, "key2341", "value9572");

  // op 8367: put
  db->Put(write_options, "key8096", "value33216");

  // op 8368: delete_range
  status = db->DeleteRange(write_options, "key3892", "key10269");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8369: delete
  db->Delete(write_options, "key8782");

  // op 8370: put
  db->Put(write_options, "key7552", "value37634");

  // op 8371: put
  db->Put(write_options, "key5485", "value75829");

  // op 8372: delete_range
  status = db->DeleteRange(write_options, "key7052", "key9008");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8373: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 8374: get
  status = db->Get(read_options, "key9685", &value);
  if (status.ok()) {
    out << "GET key9685 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8375: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 8376: get
  status = db->Get(read_options, "key9485", &value);
  if (status.ok()) {
    out << "GET key9485 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8377: get
  status = db->Get(read_options, "key5356", &value);
  if (status.ok()) {
    out << "GET key5356 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8378: put
  db->Put(write_options, "key11218", "value52117");

  // op 8379: put
  db->Put(write_options, "key11323", "value104940");

  // op 8380: delete_range
  status = db->DeleteRange(write_options, "key7548", "key9279");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8381: get
  status = db->Get(read_options, "key11713", &value);
  if (status.ok()) {
    out << "GET key11713 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8382: put
  db->Put(write_options, "key3877", "value64808");

  // op 8383: put
  db->Put(write_options, "key7268", "value79076");

  // op 8384: put
  db->Put(write_options, "key3755", "value33989");

  // op 8385: put
  db->Put(write_options, "key7848", "value15504");

  // op 8386: get
  status = db->Get(read_options, "key0552", &value);
  if (status.ok()) {
    out << "GET key0552 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8387: delete
  db->Delete(write_options, "key9567");

  // op 8388: put
  db->Put(write_options, "key7459", "value28388");

  // op 8389: get
  status = db->Get(read_options, "key10311", &value);
  if (status.ok()) {
    out << "GET key10311 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8390: put
  db->Put(write_options, "key10032", "value72218");

  // op 8391: put
  db->Put(write_options, "key3224", "value116780");

  // op 8392: get
  status = db->Get(read_options, "key10072", &value);
  if (status.ok()) {
    out << "GET key10072 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8393: put
  db->Put(write_options, "key6318", "value22406");

  // op 8394: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2694", "key6645", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2694, key6645) size=" << scan_result.size() << std::endl;
  }

  // op 8395: delete
  db->Delete(write_options, "key3414");

  // op 8396: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 8397: delete_range
  status = db->DeleteRange(write_options, "key2850", "key6143");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8398: delete_range
  status = db->DeleteRange(write_options, "key0325", "key5269");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8399: put
  db->Put(write_options, "key10101", "value17387");

  // op 8400: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0444", "key9067", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0444, key9067) size=" << scan_result.size() << std::endl;
  }

  // op 8401: delete
  db->Delete(write_options, "key0955");

  // op 8402: delete_range
  status = db->DeleteRange(write_options, "key8633", "key9151");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8403: delete_range
  status = db->DeleteRange(write_options, "key3825", "key4089");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8404: get
  status = db->Get(read_options, "key5308", &value);
  if (status.ok()) {
    out << "GET key5308 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8405: delete
  db->Delete(write_options, "key0302");

  // op 8406: put
  db->Put(write_options, "key2045", "value84856");

  // op 8407: put
  db->Put(write_options, "key1974", "value41439");

  // op 8408: get
  status = db->Get(read_options, "key2308", &value);
  if (status.ok()) {
    out << "GET key2308 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8409: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3167", "key9473", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3167, key9473) size=" << scan_result.size() << std::endl;
  }

  // op 8410: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7605", "key11601", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7605, key11601) size=" << scan_result.size() << std::endl;
  }

  // op 8411: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5141", "key10147", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5141, key10147) size=" << scan_result.size() << std::endl;
  }

  // op 8412: delete_range
  status = db->DeleteRange(write_options, "key0203", "key2804");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8413: delete_range
  status = db->DeleteRange(write_options, "key1782", "key4740");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8414: get
  status = db->Get(read_options, "key4996", &value);
  if (status.ok()) {
    out << "GET key4996 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8415: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3264", "key10413", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3264, key10413) size=" << scan_result.size() << std::endl;
  }

  // op 8416: put
  db->Put(write_options, "key2535", "value44901");

  // op 8417: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8121", "key11153", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8121, key11153) size=" << scan_result.size() << std::endl;
  }

  // op 8418: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1193", "key9287", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1193, key9287) size=" << scan_result.size() << std::endl;
  }

  // op 8419: delete
  db->Delete(write_options, "key10409");

  // op 8420: delete_range
  status = db->DeleteRange(write_options, "key1638", "key8293");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8421: get
  status = db->Get(read_options, "key10062", &value);
  if (status.ok()) {
    out << "GET key10062 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8422: get
  status = db->Get(read_options, "key3162", &value);
  if (status.ok()) {
    out << "GET key3162 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8423: put
  db->Put(write_options, "key7206", "value88477");

  // op 8424: put
  db->Put(write_options, "key4694", "value49731");

  // op 8425: put
  db->Put(write_options, "key1994", "value90666");

  // op 8426: get
  status = db->Get(read_options, "key0339", &value);
  if (status.ok()) {
    out << "GET key0339 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8427: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4723", "key6984", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4723, key6984) size=" << scan_result.size() << std::endl;
  }

  // op 8428: delete
  db->Delete(write_options, "key3306");

  // op 8429: delete_range
  status = db->DeleteRange(write_options, "key2254", "key11462");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8430: get
  status = db->Get(read_options, "key11559", &value);
  if (status.ok()) {
    out << "GET key11559 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8431: delete
  db->Delete(write_options, "key2264");

  // op 8432: put
  db->Put(write_options, "key6590", "value17349");

  // op 8433: delete
  db->Delete(write_options, "key3460");

  // op 8434: put
  db->Put(write_options, "key1405", "value50393");

  // op 8435: get
  status = db->Get(read_options, "key3968", &value);
  if (status.ok()) {
    out << "GET key3968 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8436: put
  db->Put(write_options, "key10811", "value8861");

  // op 8437: delete
  db->Delete(write_options, "key10294");

  // op 8438: put
  db->Put(write_options, "key2932", "value34481");

  // op 8439: put
  db->Put(write_options, "key8439", "value14156");

  // op 8440: put
  db->Put(write_options, "key11337", "value37058");

  // op 8441: put
  db->Put(write_options, "key6455", "value53070");

  // op 8442: delete
  db->Delete(write_options, "key2895");

  // op 8443: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4698", "key10393", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4698, key10393) size=" << scan_result.size() << std::endl;
  }

  // op 8444: delete_range
  status = db->DeleteRange(write_options, "key6462", "key11538");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8445: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2539", "key8598", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2539, key8598) size=" << scan_result.size() << std::endl;
  }

  // op 8446: put
  db->Put(write_options, "key3659", "value32546");

  // op 8447: put
  db->Put(write_options, "key10250", "value110736");

  // op 8448: put
  db->Put(write_options, "key4715", "value47846");

  // op 8449: get
  status = db->Get(read_options, "key9462", &value);
  if (status.ok()) {
    out << "GET key9462 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8450: delete
  db->Delete(write_options, "key3304");

  // op 8451: put
  db->Put(write_options, "key6344", "value115123");

  // op 8452: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5107", "key6885", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5107, key6885) size=" << scan_result.size() << std::endl;
  }

  // op 8453: get
  status = db->Get(read_options, "key7115", &value);
  if (status.ok()) {
    out << "GET key7115 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8454: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 8455: put
  db->Put(write_options, "key9836", "value100078");

  // op 8456: put
  db->Put(write_options, "key4743", "value115580");

  // op 8457: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3827", "key8175", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3827, key8175) size=" << scan_result.size() << std::endl;
  }

  // op 8458: get
  status = db->Get(read_options, "key9012", &value);
  if (status.ok()) {
    out << "GET key9012 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8459: put
  db->Put(write_options, "key1654", "value11700");

  // op 8460: put
  db->Put(write_options, "key10641", "value9414");

  // op 8461: delete_range
  status = db->DeleteRange(write_options, "key0608", "key8320");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8462: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0076", "key11686", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0076, key11686) size=" << scan_result.size() << std::endl;
  }

  // op 8463: put
  db->Put(write_options, "key4877", "value29128");

  // op 8464: get
  status = db->Get(read_options, "key9758", &value);
  if (status.ok()) {
    out << "GET key9758 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8465: put
  db->Put(write_options, "key11095", "value90941");

  // op 8466: put
  db->Put(write_options, "key3279", "value33472");

  // op 8467: put
  db->Put(write_options, "key0669", "value30916");

  // op 8468: get
  status = db->Get(read_options, "key9057", &value);
  if (status.ok()) {
    out << "GET key9057 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8469: get
  status = db->Get(read_options, "key3145", &value);
  if (status.ok()) {
    out << "GET key3145 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8470: get
  status = db->Get(read_options, "key6946", &value);
  if (status.ok()) {
    out << "GET key6946 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8471: put
  db->Put(write_options, "key1120", "value60119");

  // op 8472: put
  db->Put(write_options, "key6177", "value84674");

  // op 8473: delete_range
  status = db->DeleteRange(write_options, "key2700", "key4700");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8474: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3411", "key5347", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3411, key5347) size=" << scan_result.size() << std::endl;
  }

  // op 8475: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4414", "key8575", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4414, key8575) size=" << scan_result.size() << std::endl;
  }

  // op 8476: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4585", "key6826", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4585, key6826) size=" << scan_result.size() << std::endl;
  }

  // op 8477: get
  status = db->Get(read_options, "key8783", &value);
  if (status.ok()) {
    out << "GET key8783 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8478: put
  db->Put(write_options, "key11513", "value108789");

  // op 8479: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5254", "key8995", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5254, key8995) size=" << scan_result.size() << std::endl;
  }

  // op 8480: delete_range
  status = db->DeleteRange(write_options, "key5648", "key8637");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8481: get
  status = db->Get(read_options, "key4088", &value);
  if (status.ok()) {
    out << "GET key4088 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8482: delete_range
  status = db->DeleteRange(write_options, "key4235", "key11278");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8483: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0421", "key5247", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0421, key5247) size=" << scan_result.size() << std::endl;
  }

  // op 8484: put
  db->Put(write_options, "key0745", "value22097");

  // op 8485: get
  status = db->Get(read_options, "key8316", &value);
  if (status.ok()) {
    out << "GET key8316 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8486: get
  status = db->Get(read_options, "key6556", &value);
  if (status.ok()) {
    out << "GET key6556 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8487: get
  status = db->Get(read_options, "key7396", &value);
  if (status.ok()) {
    out << "GET key7396 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8488: get
  status = db->Get(read_options, "key11390", &value);
  if (status.ok()) {
    out << "GET key11390 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8489: get
  status = db->Get(read_options, "key1851", &value);
  if (status.ok()) {
    out << "GET key1851 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8490: put
  db->Put(write_options, "key10709", "value81471");

  // op 8491: delete_range
  status = db->DeleteRange(write_options, "key2134", "key10020");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8492: delete
  db->Delete(write_options, "key0567");

  // op 8493: get
  status = db->Get(read_options, "key10472", &value);
  if (status.ok()) {
    out << "GET key10472 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8494: put
  db->Put(write_options, "key6807", "value40853");

  // op 8495: get
  status = db->Get(read_options, "key10872", &value);
  if (status.ok()) {
    out << "GET key10872 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8496: put
  db->Put(write_options, "key2412", "value46576");

  // op 8497: put
  db->Put(write_options, "key5026", "value15806");

  // op 8498: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5974", "key8971", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5974, key8971) size=" << scan_result.size() << std::endl;
  }

  // op 8499: put
  db->Put(write_options, "key11262", "value117041");

  // op 8500: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 8501: put
  db->Put(write_options, "key11308", "value100749");

  // op 8502: delete_range
  status = db->DeleteRange(write_options, "key9410", "key10781");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8503: put
  db->Put(write_options, "key3080", "value77196");

  // op 8504: get
  status = db->Get(read_options, "key9488", &value);
  if (status.ok()) {
    out << "GET key9488 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8505: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 8506: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 8507: put
  db->Put(write_options, "key7789", "value53798");

  // op 8508: delete_range
  status = db->DeleteRange(write_options, "key9322", "key9799");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8509: put
  db->Put(write_options, "key5290", "value65854");

  // op 8510: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 8511: put
  db->Put(write_options, "key11056", "value60041");

  // op 8512: delete_range
  status = db->DeleteRange(write_options, "key10259", "key10541");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8513: get
  status = db->Get(read_options, "key4535", &value);
  if (status.ok()) {
    out << "GET key4535 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8514: delete_range
  status = db->DeleteRange(write_options, "key5897", "key7506");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8515: put
  db->Put(write_options, "key2895", "value61176");

  // op 8516: get
  status = db->Get(read_options, "key7809", &value);
  if (status.ok()) {
    out << "GET key7809 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8517: delete
  db->Delete(write_options, "key6287");

  // op 8518: put
  db->Put(write_options, "key1663", "value8890");

  // op 8519: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 8520: delete_range
  status = db->DeleteRange(write_options, "key0624", "key4252");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8521: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 8522: get
  status = db->Get(read_options, "key3972", &value);
  if (status.ok()) {
    out << "GET key3972 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8523: delete
  db->Delete(write_options, "key7404");

  // op 8524: get
  status = db->Get(read_options, "key7011", &value);
  if (status.ok()) {
    out << "GET key7011 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8525: delete_range
  status = db->DeleteRange(write_options, "key5925", "key8472");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8526: delete
  db->Delete(write_options, "key5100");

  // op 8527: delete
  db->Delete(write_options, "key3219");

  // op 8528: put
  db->Put(write_options, "key6156", "value76368");

  // op 8529: get
  status = db->Get(read_options, "key2447", &value);
  if (status.ok()) {
    out << "GET key2447 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8530: get
  status = db->Get(read_options, "key4706", &value);
  if (status.ok()) {
    out << "GET key4706 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8531: put
  db->Put(write_options, "key7731", "value79754");

  // op 8532: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0679", "key5526", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0679, key5526) size=" << scan_result.size() << std::endl;
  }

  // op 8533: get
  status = db->Get(read_options, "key10379", &value);
  if (status.ok()) {
    out << "GET key10379 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8534: put
  db->Put(write_options, "key2435", "value5739");

  // op 8535: put
  db->Put(write_options, "key5196", "value82187");

  // op 8536: delete
  db->Delete(write_options, "key3752");

  // op 8537: get
  status = db->Get(read_options, "key1773", &value);
  if (status.ok()) {
    out << "GET key1773 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8538: delete
  db->Delete(write_options, "key7372");

  // op 8539: delete_range
  status = db->DeleteRange(write_options, "key8958", "key10070");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8540: delete
  db->Delete(write_options, "key7061");

  // op 8541: get
  status = db->Get(read_options, "key6711", &value);
  if (status.ok()) {
    out << "GET key6711 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8542: put
  db->Put(write_options, "key0338", "value58243");

  // op 8543: put
  db->Put(write_options, "key3236", "value105323");

  // op 8544: put
  db->Put(write_options, "key10119", "value53048");

  // op 8545: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6356", "key8368", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6356, key8368) size=" << scan_result.size() << std::endl;
  }

  // op 8546: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 8547: get
  status = db->Get(read_options, "key3042", &value);
  if (status.ok()) {
    out << "GET key3042 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8548: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2848", "key9037", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2848, key9037) size=" << scan_result.size() << std::endl;
  }

  // op 8549: put
  db->Put(write_options, "key7334", "value17130");

  // op 8550: delete
  db->Delete(write_options, "key3693");

  // op 8551: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5284", "key9927", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5284, key9927) size=" << scan_result.size() << std::endl;
  }

  // op 8552: delete_range
  status = db->DeleteRange(write_options, "key2748", "key11191");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8553: put
  db->Put(write_options, "key3466", "value22371");

  // op 8554: put
  db->Put(write_options, "key2272", "value59303");

  // op 8555: get
  status = db->Get(read_options, "key9579", &value);
  if (status.ok()) {
    out << "GET key9579 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8556: get
  status = db->Get(read_options, "key8188", &value);
  if (status.ok()) {
    out << "GET key8188 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8557: put
  db->Put(write_options, "key4319", "value33946");

  // op 8558: delete
  db->Delete(write_options, "key7464");

  // op 8559: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1258", "key11505", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1258, key11505) size=" << scan_result.size() << std::endl;
  }

  // op 8560: put
  db->Put(write_options, "key5961", "value86445");

  // op 8561: put
  db->Put(write_options, "key0364", "value34816");

  // op 8562: get
  status = db->Get(read_options, "key1582", &value);
  if (status.ok()) {
    out << "GET key1582 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8563: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4823", "key4988", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4823, key4988) size=" << scan_result.size() << std::endl;
  }

  // op 8564: put
  db->Put(write_options, "key8512", "value99512");

  // op 8565: get
  status = db->Get(read_options, "key7879", &value);
  if (status.ok()) {
    out << "GET key7879 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8566: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4576", "key7860", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4576, key7860) size=" << scan_result.size() << std::endl;
  }

  // op 8567: get
  status = db->Get(read_options, "key5879", &value);
  if (status.ok()) {
    out << "GET key5879 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8568: put
  db->Put(write_options, "key8498", "value93968");

  // op 8569: put
  db->Put(write_options, "key2979", "value19449");

  // op 8570: put
  db->Put(write_options, "key9620", "value22910");

  // op 8571: put
  db->Put(write_options, "key2909", "value24612");

  // op 8572: delete
  db->Delete(write_options, "key10090");

  // op 8573: put
  db->Put(write_options, "key7091", "value27768");

  // op 8574: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 8575: get
  status = db->Get(read_options, "key6194", &value);
  if (status.ok()) {
    out << "GET key6194 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8576: get
  status = db->Get(read_options, "key1756", &value);
  if (status.ok()) {
    out << "GET key1756 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8577: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 8578: put
  db->Put(write_options, "key4705", "value88877");

  // op 8579: put
  db->Put(write_options, "key1583", "value57574");

  // op 8580: get
  status = db->Get(read_options, "key7131", &value);
  if (status.ok()) {
    out << "GET key7131 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8581: put
  db->Put(write_options, "key0958", "value46042");

  // op 8582: get
  status = db->Get(read_options, "key10605", &value);
  if (status.ok()) {
    out << "GET key10605 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8583: put
  db->Put(write_options, "key2411", "value28684");

  // op 8584: delete_range
  status = db->DeleteRange(write_options, "key3915", "key4693");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8585: delete
  db->Delete(write_options, "key11411");

  // op 8586: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 8587: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7448", "key10439", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7448, key10439) size=" << scan_result.size() << std::endl;
  }

  // op 8588: put
  db->Put(write_options, "key7049", "value35841");

  // op 8589: delete_range
  status = db->DeleteRange(write_options, "key1402", "key5729");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8590: delete_range
  status = db->DeleteRange(write_options, "key0399", "key10761");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8591: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2009", "key2440", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2009, key2440) size=" << scan_result.size() << std::endl;
  }

  // op 8592: put
  db->Put(write_options, "key3860", "value34552");

  // op 8593: get
  status = db->Get(read_options, "key2942", &value);
  if (status.ok()) {
    out << "GET key2942 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8594: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3292", "key5660", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3292, key5660) size=" << scan_result.size() << std::endl;
  }

  // op 8595: put
  db->Put(write_options, "key0407", "value52866");

  // op 8596: put
  db->Put(write_options, "key2091", "value25089");

  // op 8597: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3041", "key3716", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3041, key3716) size=" << scan_result.size() << std::endl;
  }

  // op 8598: delete_range
  status = db->DeleteRange(write_options, "key0738", "key4461");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8599: put
  db->Put(write_options, "key1240", "value74847");

  // op 8600: get
  status = db->Get(read_options, "key0443", &value);
  if (status.ok()) {
    out << "GET key0443 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8601: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1415", "key6397", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1415, key6397) size=" << scan_result.size() << std::endl;
  }

  // op 8602: get
  status = db->Get(read_options, "key3619", &value);
  if (status.ok()) {
    out << "GET key3619 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8603: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0277", "key4789", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0277, key4789) size=" << scan_result.size() << std::endl;
  }

  // op 8604: delete_range
  status = db->DeleteRange(write_options, "key1824", "key10497");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8605: put
  db->Put(write_options, "key10224", "value39113");

  // op 8606: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2279", "key4930", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2279, key4930) size=" << scan_result.size() << std::endl;
  }

  // op 8607: get
  status = db->Get(read_options, "key1268", &value);
  if (status.ok()) {
    out << "GET key1268 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8608: put
  db->Put(write_options, "key3783", "value6660");

  // op 8609: put
  db->Put(write_options, "key0212", "value66458");

  // op 8610: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1031", "key9309", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1031, key9309) size=" << scan_result.size() << std::endl;
  }

  // op 8611: delete
  db->Delete(write_options, "key7465");

  // op 8612: get
  status = db->Get(read_options, "key2676", &value);
  if (status.ok()) {
    out << "GET key2676 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8613: delete
  db->Delete(write_options, "key1754");

  // op 8614: put
  db->Put(write_options, "key0566", "value8066");

  // op 8615: put
  db->Put(write_options, "key2333", "value38632");

  // op 8616: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6644", "key7480", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6644, key7480) size=" << scan_result.size() << std::endl;
  }

  // op 8617: put
  db->Put(write_options, "key6156", "value36158");

  // op 8618: put
  db->Put(write_options, "key5351", "value56374");

  // op 8619: delete_range
  status = db->DeleteRange(write_options, "key6882", "key8596");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8620: put
  db->Put(write_options, "key6372", "value101327");

  // op 8621: put
  db->Put(write_options, "key6355", "value111052");

  // op 8622: delete
  db->Delete(write_options, "key3998");

  // op 8623: put
  db->Put(write_options, "key2220", "value104673");

  // op 8624: delete
  db->Delete(write_options, "key7707");

  // op 8625: put
  db->Put(write_options, "key6650", "value82810");

  // op 8626: put
  db->Put(write_options, "key0136", "value85166");

  // op 8627: put
  db->Put(write_options, "key10896", "value22838");

  // op 8628: delete
  db->Delete(write_options, "key11389");

  // op 8629: delete_range
  status = db->DeleteRange(write_options, "key6546", "key11329");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8630: put
  db->Put(write_options, "key8443", "value50450");

  // op 8631: put
  db->Put(write_options, "key10061", "value48897");

  // op 8632: get
  status = db->Get(read_options, "key1186", &value);
  if (status.ok()) {
    out << "GET key1186 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8633: put
  db->Put(write_options, "key6505", "value99166");

  // op 8634: put
  db->Put(write_options, "key10081", "value76777");

  // op 8635: put
  db->Put(write_options, "key6810", "value34092");

  // op 8636: put
  db->Put(write_options, "key4162", "value17931");

  // op 8637: get
  status = db->Get(read_options, "key6733", &value);
  if (status.ok()) {
    out << "GET key6733 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8638: put
  db->Put(write_options, "key4927", "value20290");

  // op 8639: put
  db->Put(write_options, "key10308", "value107472");

  // op 8640: delete_range
  status = db->DeleteRange(write_options, "key2328", "key6608");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8641: get
  status = db->Get(read_options, "key4451", &value);
  if (status.ok()) {
    out << "GET key4451 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8642: get
  status = db->Get(read_options, "key8837", &value);
  if (status.ok()) {
    out << "GET key8837 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8643: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5000", "key9010", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5000, key9010) size=" << scan_result.size() << std::endl;
  }

  // op 8644: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7459", "key10511", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7459, key10511) size=" << scan_result.size() << std::endl;
  }

  // op 8645: put
  db->Put(write_options, "key7498", "value70647");

  // op 8646: put
  db->Put(write_options, "key9277", "value83943");

  // op 8647: get
  status = db->Get(read_options, "key10884", &value);
  if (status.ok()) {
    out << "GET key10884 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8648: delete
  db->Delete(write_options, "key4041");

  // op 8649: delete_range
  status = db->DeleteRange(write_options, "key1017", "key6087");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8650: get
  status = db->Get(read_options, "key5131", &value);
  if (status.ok()) {
    out << "GET key5131 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8651: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1927", "key8012", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1927, key8012) size=" << scan_result.size() << std::endl;
  }

  // op 8652: put
  db->Put(write_options, "key6731", "value52448");

  // op 8653: delete_range
  status = db->DeleteRange(write_options, "key4497", "key6073");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8654: delete_range
  status = db->DeleteRange(write_options, "key4767", "key4923");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8655: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6594", "key8642", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6594, key8642) size=" << scan_result.size() << std::endl;
  }

  // op 8656: put
  db->Put(write_options, "key8140", "value18911");

  // op 8657: delete
  db->Delete(write_options, "key7004");

  // op 8658: get
  status = db->Get(read_options, "key3028", &value);
  if (status.ok()) {
    out << "GET key3028 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8659: put
  db->Put(write_options, "key5762", "value90302");

  // op 8660: get
  status = db->Get(read_options, "key2762", &value);
  if (status.ok()) {
    out << "GET key2762 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8661: put
  db->Put(write_options, "key5293", "value82884");

  // op 8662: delete_range
  status = db->DeleteRange(write_options, "key2049", "key6094");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8663: delete_range
  status = db->DeleteRange(write_options, "key7815", "key10105");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8664: get
  status = db->Get(read_options, "key8227", &value);
  if (status.ok()) {
    out << "GET key8227 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8665: delete_range
  status = db->DeleteRange(write_options, "key3199", "key6889");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8666: get
  status = db->Get(read_options, "key10226", &value);
  if (status.ok()) {
    out << "GET key10226 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8667: delete
  db->Delete(write_options, "key10240");

  // op 8668: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2376", "key3605", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2376, key3605) size=" << scan_result.size() << std::endl;
  }

  // op 8669: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4956", "key5723", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4956, key5723) size=" << scan_result.size() << std::endl;
  }

  // op 8670: delete
  db->Delete(write_options, "key4530");

  // op 8671: get
  status = db->Get(read_options, "key0371", &value);
  if (status.ok()) {
    out << "GET key0371 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8672: put
  db->Put(write_options, "key1505", "value61430");

  // op 8673: put
  db->Put(write_options, "key7039", "value55659");

  // op 8674: put
  db->Put(write_options, "key0628", "value64033");

  // op 8675: put
  db->Put(write_options, "key1557", "value66978");

  // op 8676: put
  db->Put(write_options, "key9705", "value30353");

  // op 8677: get
  status = db->Get(read_options, "key8770", &value);
  if (status.ok()) {
    out << "GET key8770 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8678: get
  status = db->Get(read_options, "key8679", &value);
  if (status.ok()) {
    out << "GET key8679 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8679: put
  db->Put(write_options, "key4807", "value102899");

  // op 8680: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3352", "key7398", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3352, key7398) size=" << scan_result.size() << std::endl;
  }

  // op 8681: put
  db->Put(write_options, "key1728", "value35414");

  // op 8682: delete
  db->Delete(write_options, "key4182");

  // op 8683: get
  status = db->Get(read_options, "key0619", &value);
  if (status.ok()) {
    out << "GET key0619 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8684: put
  db->Put(write_options, "key8022", "value39697");

  // op 8685: put
  db->Put(write_options, "key9028", "value11149");

  // op 8686: get
  status = db->Get(read_options, "key8551", &value);
  if (status.ok()) {
    out << "GET key8551 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8687: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 8688: get
  status = db->Get(read_options, "key2102", &value);
  if (status.ok()) {
    out << "GET key2102 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8689: put
  db->Put(write_options, "key9750", "value53403");

  // op 8690: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5157", "key6662", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5157, key6662) size=" << scan_result.size() << std::endl;
  }

  // op 8691: delete_range
  status = db->DeleteRange(write_options, "key0387", "key9997");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8692: put
  db->Put(write_options, "key10729", "value76788");

  // op 8693: put
  db->Put(write_options, "key7459", "value75096");

  // op 8694: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 8695: get
  status = db->Get(read_options, "key3797", &value);
  if (status.ok()) {
    out << "GET key3797 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8696: put
  db->Put(write_options, "key5038", "value37910");

  // op 8697: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6638", "key9227", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6638, key9227) size=" << scan_result.size() << std::endl;
  }

  // op 8698: delete
  db->Delete(write_options, "key11087");

  // op 8699: put
  db->Put(write_options, "key6730", "value89425");

  // op 8700: put
  db->Put(write_options, "key11228", "value52661");

  // op 8701: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4642", "key5074", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4642, key5074) size=" << scan_result.size() << std::endl;
  }

  // op 8702: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 8703: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 8704: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0029", "key1639", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0029, key1639) size=" << scan_result.size() << std::endl;
  }

  // op 8705: put
  db->Put(write_options, "key4727", "value17480");

  // op 8706: put
  db->Put(write_options, "key8118", "value82531");

  // op 8707: put
  db->Put(write_options, "key6390", "value65051");

  // op 8708: put
  db->Put(write_options, "key11495", "value34671");

  // op 8709: delete_range
  status = db->DeleteRange(write_options, "key4869", "key8119");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8710: get
  status = db->Get(read_options, "key10633", &value);
  if (status.ok()) {
    out << "GET key10633 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8711: get
  status = db->Get(read_options, "key6283", &value);
  if (status.ok()) {
    out << "GET key6283 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8712: put
  db->Put(write_options, "key2049", "value19815");

  // op 8713: get
  status = db->Get(read_options, "key0515", &value);
  if (status.ok()) {
    out << "GET key0515 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8714: put
  db->Put(write_options, "key2300", "value92391");

  // op 8715: put
  db->Put(write_options, "key7292", "value112750");

  // op 8716: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 8717: delete
  db->Delete(write_options, "key5016");

  // op 8718: put
  db->Put(write_options, "key7533", "value108979");

  // op 8719: delete
  db->Delete(write_options, "key6365");

  // op 8720: get
  status = db->Get(read_options, "key0369", &value);
  if (status.ok()) {
    out << "GET key0369 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8721: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 8722: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2045", "key11214", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2045, key11214) size=" << scan_result.size() << std::endl;
  }

  // op 8723: delete
  db->Delete(write_options, "key2328");

  // op 8724: get
  status = db->Get(read_options, "key0164", &value);
  if (status.ok()) {
    out << "GET key0164 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8725: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0794", "key5342", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0794, key5342) size=" << scan_result.size() << std::endl;
  }

  // op 8726: get
  status = db->Get(read_options, "key6999", &value);
  if (status.ok()) {
    out << "GET key6999 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8727: put
  db->Put(write_options, "key2252", "value62552");

  // op 8728: get
  status = db->Get(read_options, "key4661", &value);
  if (status.ok()) {
    out << "GET key4661 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8729: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 8730: put
  db->Put(write_options, "key9052", "value113185");

  // op 8731: put
  db->Put(write_options, "key5763", "value29065");

  // op 8732: delete_range
  status = db->DeleteRange(write_options, "key9778", "key10728");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8733: put
  db->Put(write_options, "key10158", "value48509");

  // op 8734: put
  db->Put(write_options, "key6929", "value66288");

  // op 8735: put
  db->Put(write_options, "key0921", "value48122");

  // op 8736: put
  db->Put(write_options, "key8342", "value85280");

  // op 8737: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1537", "key7725", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1537, key7725) size=" << scan_result.size() << std::endl;
  }

  // op 8738: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1583", "key4858", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1583, key4858) size=" << scan_result.size() << std::endl;
  }

  // op 8739: get
  status = db->Get(read_options, "key8444", &value);
  if (status.ok()) {
    out << "GET key8444 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8740: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3962", "key10904", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3962, key10904) size=" << scan_result.size() << std::endl;
  }

  // op 8741: put
  db->Put(write_options, "key4831", "value82266");

  // op 8742: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8118", "key9383", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8118, key9383) size=" << scan_result.size() << std::endl;
  }

  // op 8743: get
  status = db->Get(read_options, "key3172", &value);
  if (status.ok()) {
    out << "GET key3172 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8744: get
  status = db->Get(read_options, "key3529", &value);
  if (status.ok()) {
    out << "GET key3529 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8745: put
  db->Put(write_options, "key10275", "value45920");

  // op 8746: put
  db->Put(write_options, "key9003", "value112214");

  // op 8747: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5439", "key9637", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5439, key9637) size=" << scan_result.size() << std::endl;
  }

  // op 8748: get
  status = db->Get(read_options, "key5539", &value);
  if (status.ok()) {
    out << "GET key5539 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8749: delete_range
  status = db->DeleteRange(write_options, "key8175", "key10769");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8750: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3099", "key5213", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3099, key5213) size=" << scan_result.size() << std::endl;
  }

  // op 8751: put
  db->Put(write_options, "key2172", "value53213");

  // op 8752: get
  status = db->Get(read_options, "key11655", &value);
  if (status.ok()) {
    out << "GET key11655 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8753: put
  db->Put(write_options, "key8046", "value33467");

  // op 8754: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6867", "key9581", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6867, key9581) size=" << scan_result.size() << std::endl;
  }

  // op 8755: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4738", "key9932", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4738, key9932) size=" << scan_result.size() << std::endl;
  }

  // op 8756: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 8757: get
  status = db->Get(read_options, "key10027", &value);
  if (status.ok()) {
    out << "GET key10027 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8758: put
  db->Put(write_options, "key10808", "value63505");

  // op 8759: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4266", "key7365", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4266, key7365) size=" << scan_result.size() << std::endl;
  }

  // op 8760: put
  db->Put(write_options, "key10093", "value61709");

  // op 8761: put
  db->Put(write_options, "key4434", "value109370");

  // op 8762: get
  status = db->Get(read_options, "key9961", &value);
  if (status.ok()) {
    out << "GET key9961 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8763: get
  status = db->Get(read_options, "key1220", &value);
  if (status.ok()) {
    out << "GET key1220 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8764: put
  db->Put(write_options, "key4363", "value111377");

  // op 8765: put
  db->Put(write_options, "key7188", "value72329");

  // op 8766: delete_range
  status = db->DeleteRange(write_options, "key0147", "key7822");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8767: put
  db->Put(write_options, "key10889", "value17316");

  // op 8768: delete_range
  status = db->DeleteRange(write_options, "key3251", "key11626");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8769: delete
  db->Delete(write_options, "key11396");

  // op 8770: put
  db->Put(write_options, "key8580", "value104594");

  // op 8771: put
  db->Put(write_options, "key7084", "value96012");

  // op 8772: put
  db->Put(write_options, "key9795", "value113172");

  // op 8773: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1281", "key6543", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1281, key6543) size=" << scan_result.size() << std::endl;
  }

  // op 8774: delete
  db->Delete(write_options, "key1344");

  // op 8775: put
  db->Put(write_options, "key11419", "value26745");

  // op 8776: put
  db->Put(write_options, "key0408", "value102206");

  // op 8777: put
  db->Put(write_options, "key0053", "value65772");

  // op 8778: put
  db->Put(write_options, "key2375", "value12277");

  // op 8779: get
  status = db->Get(read_options, "key5296", &value);
  if (status.ok()) {
    out << "GET key5296 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8780: get
  status = db->Get(read_options, "key8659", &value);
  if (status.ok()) {
    out << "GET key8659 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8781: put
  db->Put(write_options, "key6646", "value95353");

  // op 8782: put
  db->Put(write_options, "key6529", "value29652");

  // op 8783: put
  db->Put(write_options, "key6088", "value44804");

  // op 8784: delete_range
  status = db->DeleteRange(write_options, "key4350", "key5986");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8785: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0083", "key5762", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0083, key5762) size=" << scan_result.size() << std::endl;
  }

  // op 8786: put
  db->Put(write_options, "key1320", "value10981");

  // op 8787: delete_range
  status = db->DeleteRange(write_options, "key6282", "key10579");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8788: delete
  db->Delete(write_options, "key9322");

  // op 8789: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 8790: put
  db->Put(write_options, "key4844", "value84863");

  // op 8791: get
  status = db->Get(read_options, "key5829", &value);
  if (status.ok()) {
    out << "GET key5829 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8792: delete
  db->Delete(write_options, "key3724");

  // op 8793: get
  status = db->Get(read_options, "key9963", &value);
  if (status.ok()) {
    out << "GET key9963 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8794: put
  db->Put(write_options, "key6495", "value8822");

  // op 8795: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6638", "key6763", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6638, key6763) size=" << scan_result.size() << std::endl;
  }

  // op 8796: put
  db->Put(write_options, "key6775", "value117925");

  // op 8797: put
  db->Put(write_options, "key8639", "value69240");

  // op 8798: put
  db->Put(write_options, "key5146", "value68623");

  // op 8799: put
  db->Put(write_options, "key1320", "value117773");

  // op 8800: put
  db->Put(write_options, "key11426", "value115229");

  // op 8801: delete_range
  status = db->DeleteRange(write_options, "key1182", "key5256");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8802: put
  db->Put(write_options, "key5025", "value107309");

  // op 8803: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4144", "key9542", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4144, key9542) size=" << scan_result.size() << std::endl;
  }

  // op 8804: get
  status = db->Get(read_options, "key4081", &value);
  if (status.ok()) {
    out << "GET key4081 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8805: put
  db->Put(write_options, "key0848", "value37051");

  // op 8806: delete
  db->Delete(write_options, "key7038");

  // op 8807: put
  db->Put(write_options, "key0807", "value57327");

  // op 8808: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8983", "key11318", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8983, key11318) size=" << scan_result.size() << std::endl;
  }

  // op 8809: put
  db->Put(write_options, "key6127", "value7150");

  // op 8810: get
  status = db->Get(read_options, "key7597", &value);
  if (status.ok()) {
    out << "GET key7597 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8811: put
  db->Put(write_options, "key1473", "value23493");

  // op 8812: put
  db->Put(write_options, "key11630", "value77253");

  // op 8813: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0873", "key8097", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0873, key8097) size=" << scan_result.size() << std::endl;
  }

  // op 8814: put
  db->Put(write_options, "key6663", "value50725");

  // op 8815: get
  status = db->Get(read_options, "key2884", &value);
  if (status.ok()) {
    out << "GET key2884 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8816: put
  db->Put(write_options, "key10628", "value83979");

  // op 8817: delete
  db->Delete(write_options, "key5884");

  // op 8818: get
  status = db->Get(read_options, "key5172", &value);
  if (status.ok()) {
    out << "GET key5172 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8819: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 8820: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2044", "key9292", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2044, key9292) size=" << scan_result.size() << std::endl;
  }

  // op 8821: put
  db->Put(write_options, "key4527", "value54843");

  // op 8822: get
  status = db->Get(read_options, "key7636", &value);
  if (status.ok()) {
    out << "GET key7636 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8823: put
  db->Put(write_options, "key9717", "value97470");

  // op 8824: delete
  db->Delete(write_options, "key0847");

  // op 8825: get
  status = db->Get(read_options, "key10970", &value);
  if (status.ok()) {
    out << "GET key10970 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8826: put
  db->Put(write_options, "key9324", "value29211");

  // op 8827: put
  db->Put(write_options, "key3622", "value64199");

  // op 8828: get
  status = db->Get(read_options, "key6320", &value);
  if (status.ok()) {
    out << "GET key6320 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8829: delete_range
  status = db->DeleteRange(write_options, "key5443", "key7228");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8830: get
  status = db->Get(read_options, "key9670", &value);
  if (status.ok()) {
    out << "GET key9670 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8831: put
  db->Put(write_options, "key11623", "value25569");

  // op 8832: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 8833: put
  db->Put(write_options, "key9055", "value72471");

  // op 8834: put
  db->Put(write_options, "key7654", "value102582");

  // op 8835: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1277", "key9969", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1277, key9969) size=" << scan_result.size() << std::endl;
  }

  // op 8836: get
  status = db->Get(read_options, "key3703", &value);
  if (status.ok()) {
    out << "GET key3703 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8837: delete_range
  status = db->DeleteRange(write_options, "key3858", "key11313");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8838: put
  db->Put(write_options, "key4971", "value104238");

  // op 8839: get
  status = db->Get(read_options, "key2868", &value);
  if (status.ok()) {
    out << "GET key2868 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8840: put
  db->Put(write_options, "key1292", "value16621");

  // op 8841: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 8842: get
  status = db->Get(read_options, "key10234", &value);
  if (status.ok()) {
    out << "GET key10234 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8843: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5164", "key5187", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5164, key5187) size=" << scan_result.size() << std::endl;
  }

  // op 8844: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7630", "key11364", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7630, key11364) size=" << scan_result.size() << std::endl;
  }

  // op 8845: put
  db->Put(write_options, "key9827", "value33500");

  // op 8846: get
  status = db->Get(read_options, "key3808", &value);
  if (status.ok()) {
    out << "GET key3808 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8847: put
  db->Put(write_options, "key8654", "value55294");

  // op 8848: put
  db->Put(write_options, "key8545", "value27134");

  // op 8849: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3102", "key6724", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3102, key6724) size=" << scan_result.size() << std::endl;
  }

  // op 8850: get
  status = db->Get(read_options, "key10706", &value);
  if (status.ok()) {
    out << "GET key10706 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8851: put
  db->Put(write_options, "key4053", "value31612");

  // op 8852: put
  db->Put(write_options, "key2618", "value37250");

  // op 8853: put
  db->Put(write_options, "key0413", "value49434");

  // op 8854: get
  status = db->Get(read_options, "key3510", &value);
  if (status.ok()) {
    out << "GET key3510 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8855: put
  db->Put(write_options, "key9481", "value40374");

  // op 8856: get
  status = db->Get(read_options, "key10320", &value);
  if (status.ok()) {
    out << "GET key10320 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8857: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3216", "key10274", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3216, key10274) size=" << scan_result.size() << std::endl;
  }

  // op 8858: delete_range
  status = db->DeleteRange(write_options, "key0363", "key2361");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8859: get
  status = db->Get(read_options, "key11067", &value);
  if (status.ok()) {
    out << "GET key11067 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8860: put
  db->Put(write_options, "key9140", "value93420");

  // op 8861: delete
  db->Delete(write_options, "key2002");

  // op 8862: put
  db->Put(write_options, "key4476", "value82484");

  // op 8863: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4606", "key10442", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4606, key10442) size=" << scan_result.size() << std::endl;
  }

  // op 8864: put
  db->Put(write_options, "key7793", "value93742");

  // op 8865: delete_range
  status = db->DeleteRange(write_options, "key2902", "key5422");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8866: get
  status = db->Get(read_options, "key2129", &value);
  if (status.ok()) {
    out << "GET key2129 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8867: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5268", "key5837", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5268, key5837) size=" << scan_result.size() << std::endl;
  }

  // op 8868: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2206", "key6327", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2206, key6327) size=" << scan_result.size() << std::endl;
  }

  // op 8869: put
  db->Put(write_options, "key5890", "value75154");

  // op 8870: put
  db->Put(write_options, "key0219", "value92876");

  // op 8871: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 8872: delete_range
  status = db->DeleteRange(write_options, "key3188", "key6313");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8873: put
  db->Put(write_options, "key5802", "value14367");

  // op 8874: get
  status = db->Get(read_options, "key7928", &value);
  if (status.ok()) {
    out << "GET key7928 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8875: delete
  db->Delete(write_options, "key11099");

  // op 8876: put
  db->Put(write_options, "key5896", "value0082");

  // op 8877: put
  db->Put(write_options, "key2280", "value69285");

  // op 8878: get
  status = db->Get(read_options, "key9030", &value);
  if (status.ok()) {
    out << "GET key9030 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8879: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8401", "key10834", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8401, key10834) size=" << scan_result.size() << std::endl;
  }

  // op 8880: get
  status = db->Get(read_options, "key0120", &value);
  if (status.ok()) {
    out << "GET key0120 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8881: get
  status = db->Get(read_options, "key3914", &value);
  if (status.ok()) {
    out << "GET key3914 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8882: delete
  db->Delete(write_options, "key3026");

  // op 8883: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 8884: get
  status = db->Get(read_options, "key3773", &value);
  if (status.ok()) {
    out << "GET key3773 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8885: delete
  db->Delete(write_options, "key11109");

  // op 8886: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9303", "key10333", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9303, key10333) size=" << scan_result.size() << std::endl;
  }

  // op 8887: get
  status = db->Get(read_options, "key9344", &value);
  if (status.ok()) {
    out << "GET key9344 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8888: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4804", "key7283", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4804, key7283) size=" << scan_result.size() << std::endl;
  }

  // op 8889: delete
  db->Delete(write_options, "key1465");

  // op 8890: delete
  db->Delete(write_options, "key11416");

  // op 8891: put
  db->Put(write_options, "key9355", "value16310");

  // op 8892: put
  db->Put(write_options, "key9962", "value6354");

  // op 8893: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4657", "key5944", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4657, key5944) size=" << scan_result.size() << std::endl;
  }

  // op 8894: put
  db->Put(write_options, "key2961", "value9926");

  // op 8895: put
  db->Put(write_options, "key8052", "value15004");

  // op 8896: get
  status = db->Get(read_options, "key2916", &value);
  if (status.ok()) {
    out << "GET key2916 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8897: put
  db->Put(write_options, "key11588", "value31502");

  // op 8898: put
  db->Put(write_options, "key2866", "value115418");

  // op 8899: delete
  db->Delete(write_options, "key0797");

  // op 8900: get
  status = db->Get(read_options, "key7091", &value);
  if (status.ok()) {
    out << "GET key7091 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8901: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2142", "key8352", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2142, key8352) size=" << scan_result.size() << std::endl;
  }

  // op 8902: get
  status = db->Get(read_options, "key5897", &value);
  if (status.ok()) {
    out << "GET key5897 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8903: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1201", "key8355", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1201, key8355) size=" << scan_result.size() << std::endl;
  }

  // op 8904: get
  status = db->Get(read_options, "key4066", &value);
  if (status.ok()) {
    out << "GET key4066 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8905: delete_range
  status = db->DeleteRange(write_options, "key3587", "key9972");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8906: get
  status = db->Get(read_options, "key3597", &value);
  if (status.ok()) {
    out << "GET key3597 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8907: put
  db->Put(write_options, "key6449", "value49065");

  // op 8908: put
  db->Put(write_options, "key3638", "value82482");

  // op 8909: put
  db->Put(write_options, "key5498", "value41001");

  // op 8910: delete
  db->Delete(write_options, "key10583");

  // op 8911: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0383", "key6977", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0383, key6977) size=" << scan_result.size() << std::endl;
  }

  // op 8912: delete_range
  status = db->DeleteRange(write_options, "key1793", "key5177");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8913: delete_range
  status = db->DeleteRange(write_options, "key5566", "key6368");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8914: delete_range
  status = db->DeleteRange(write_options, "key5087", "key5502");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8915: get
  status = db->Get(read_options, "key11490", &value);
  if (status.ok()) {
    out << "GET key11490 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8916: delete
  db->Delete(write_options, "key1102");

  // op 8917: put
  db->Put(write_options, "key0213", "value85738");

  // op 8918: get
  status = db->Get(read_options, "key6002", &value);
  if (status.ok()) {
    out << "GET key6002 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8919: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10374", "key11615", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10374, key11615) size=" << scan_result.size() << std::endl;
  }

  // op 8920: get
  status = db->Get(read_options, "key0711", &value);
  if (status.ok()) {
    out << "GET key0711 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8921: put
  db->Put(write_options, "key5121", "value10853");

  // op 8922: put
  db->Put(write_options, "key2895", "value17017");

  // op 8923: delete_range
  status = db->DeleteRange(write_options, "key1030", "key2350");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8924: get
  status = db->Get(read_options, "key5365", &value);
  if (status.ok()) {
    out << "GET key5365 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8925: put
  db->Put(write_options, "key1597", "value43464");

  // op 8926: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5043", "key11519", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5043, key11519) size=" << scan_result.size() << std::endl;
  }

  // op 8927: delete_range
  status = db->DeleteRange(write_options, "key5796", "key7693");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8928: delete
  db->Delete(write_options, "key9302");

  // op 8929: get
  status = db->Get(read_options, "key2026", &value);
  if (status.ok()) {
    out << "GET key2026 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8930: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8585", "key10807", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8585, key10807) size=" << scan_result.size() << std::endl;
  }

  // op 8931: put
  db->Put(write_options, "key0234", "value115646");

  // op 8932: put
  db->Put(write_options, "key3848", "value42711");

  // op 8933: put
  db->Put(write_options, "key0635", "value52690");

  // op 8934: put
  db->Put(write_options, "key3948", "value113185");

  // op 8935: get
  status = db->Get(read_options, "key8237", &value);
  if (status.ok()) {
    out << "GET key8237 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8936: delete
  db->Delete(write_options, "key5060");

  // op 8937: put
  db->Put(write_options, "key9135", "value109085");

  // op 8938: delete_range
  status = db->DeleteRange(write_options, "key0359", "key0666");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8939: put
  db->Put(write_options, "key11113", "value43257");

  // op 8940: get
  status = db->Get(read_options, "key3570", &value);
  if (status.ok()) {
    out << "GET key3570 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8941: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5657", "key9425", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5657, key9425) size=" << scan_result.size() << std::endl;
  }

  // op 8942: put
  db->Put(write_options, "key5962", "value53546");

  // op 8943: get
  status = db->Get(read_options, "key9323", &value);
  if (status.ok()) {
    out << "GET key9323 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8944: put
  db->Put(write_options, "key3058", "value58086");

  // op 8945: get
  status = db->Get(read_options, "key3140", &value);
  if (status.ok()) {
    out << "GET key3140 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8946: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2050", "key8076", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2050, key8076) size=" << scan_result.size() << std::endl;
  }

  // op 8947: delete_range
  status = db->DeleteRange(write_options, "key5769", "key7328");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8948: put
  db->Put(write_options, "key6547", "value51130");

  // op 8949: put
  db->Put(write_options, "key10008", "value81021");

  // op 8950: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9161", "key11056", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9161, key11056) size=" << scan_result.size() << std::endl;
  }

  // op 8951: put
  db->Put(write_options, "key6193", "value35955");

  // op 8952: put
  db->Put(write_options, "key3420", "value23452");

  // op 8953: get
  status = db->Get(read_options, "key6359", &value);
  if (status.ok()) {
    out << "GET key6359 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8954: get
  status = db->Get(read_options, "key5693", &value);
  if (status.ok()) {
    out << "GET key5693 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8955: delete_range
  status = db->DeleteRange(write_options, "key1607", "key4846");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8956: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1058", "key3856", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1058, key3856) size=" << scan_result.size() << std::endl;
  }

  // op 8957: get
  status = db->Get(read_options, "key5225", &value);
  if (status.ok()) {
    out << "GET key5225 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8958: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10179", "key10719", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10179, key10719) size=" << scan_result.size() << std::endl;
  }

  // op 8959: delete_range
  status = db->DeleteRange(write_options, "key9276", "key11542");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8960: delete_range
  status = db->DeleteRange(write_options, "key1839", "key8915");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8961: get
  status = db->Get(read_options, "key4438", &value);
  if (status.ok()) {
    out << "GET key4438 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8962: put
  db->Put(write_options, "key7097", "value105189");

  // op 8963: get
  status = db->Get(read_options, "key10194", &value);
  if (status.ok()) {
    out << "GET key10194 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8964: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7325", "key10575", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7325, key10575) size=" << scan_result.size() << std::endl;
  }

  // op 8965: put
  db->Put(write_options, "key11511", "value28304");

  // op 8966: get
  status = db->Get(read_options, "key1968", &value);
  if (status.ok()) {
    out << "GET key1968 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8967: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4312", "key5402", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4312, key5402) size=" << scan_result.size() << std::endl;
  }

  // op 8968: get
  status = db->Get(read_options, "key8125", &value);
  if (status.ok()) {
    out << "GET key8125 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8969: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6848", "key10299", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6848, key10299) size=" << scan_result.size() << std::endl;
  }

  // op 8970: put
  db->Put(write_options, "key9294", "value2541");

  // op 8971: get
  status = db->Get(read_options, "key0784", &value);
  if (status.ok()) {
    out << "GET key0784 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8972: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9934", "key10066", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9934, key10066) size=" << scan_result.size() << std::endl;
  }

  // op 8973: put
  db->Put(write_options, "key7455", "value56909");

  // op 8974: delete
  db->Delete(write_options, "key3943");

  // op 8975: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5298", "key5978", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5298, key5978) size=" << scan_result.size() << std::endl;
  }

  // op 8976: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 8977: get
  status = db->Get(read_options, "key7180", &value);
  if (status.ok()) {
    out << "GET key7180 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8978: put
  db->Put(write_options, "key6290", "value25598");

  // op 8979: put
  db->Put(write_options, "key0447", "value72745");

  // op 8980: delete_range
  status = db->DeleteRange(write_options, "key1847", "key5591");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8981: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1816", "key10783", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1816, key10783) size=" << scan_result.size() << std::endl;
  }

  // op 8982: put
  db->Put(write_options, "key1302", "value2284");

  // op 8983: get
  status = db->Get(read_options, "key9112", &value);
  if (status.ok()) {
    out << "GET key9112 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8984: delete
  db->Delete(write_options, "key4257");

  // op 8985: put
  db->Put(write_options, "key9834", "value106871");

  // op 8986: get
  status = db->Get(read_options, "key11498", &value);
  if (status.ok()) {
    out << "GET key11498 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8987: put
  db->Put(write_options, "key11695", "value83564");

  // op 8988: delete_range
  status = db->DeleteRange(write_options, "key5222", "key11095");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8989: put
  db->Put(write_options, "key4027", "value65225");

  // op 8990: put
  db->Put(write_options, "key0286", "value31264");

  // op 8991: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 8992: put
  db->Put(write_options, "key10414", "value69322");

  // op 8993: put
  db->Put(write_options, "key11216", "value108951");

  // op 8994: delete_range
  status = db->DeleteRange(write_options, "key0580", "key10426");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 8995: get
  status = db->Get(read_options, "key5292", &value);
  if (status.ok()) {
    out << "GET key5292 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8996: put
  db->Put(write_options, "key1090", "value103594");

  // op 8997: get
  status = db->Get(read_options, "key6112", &value);
  if (status.ok()) {
    out << "GET key6112 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8998: get
  status = db->Get(read_options, "key9726", &value);
  if (status.ok()) {
    out << "GET key9726 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 8999: put
  db->Put(write_options, "key2088", "value82867");

  // op 9000: put
  db->Put(write_options, "key11120", "value61662");

  // op 9001: get
  status = db->Get(read_options, "key10627", &value);
  if (status.ok()) {
    out << "GET key10627 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9002: delete_range
  status = db->DeleteRange(write_options, "key2616", "key2736");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 9003: delete_range
  status = db->DeleteRange(write_options, "key0571", "key8598");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 9004: put
  db->Put(write_options, "key11567", "value35145");

  // op 9005: put
  db->Put(write_options, "key4345", "value11541");

  // op 9006: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8355", "key11579", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8355, key11579) size=" << scan_result.size() << std::endl;
  }

  // op 9007: put
  db->Put(write_options, "key9002", "value7914");

  // op 9008: get
  status = db->Get(read_options, "key5922", &value);
  if (status.ok()) {
    out << "GET key5922 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9009: put
  db->Put(write_options, "key4482", "value85386");

  // op 9010: put
  db->Put(write_options, "key4487", "value55932");

  // op 9011: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5194", "key7186", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5194, key7186) size=" << scan_result.size() << std::endl;
  }

  // op 9012: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1159", "key5543", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1159, key5543) size=" << scan_result.size() << std::endl;
  }

  // op 9013: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2275", "key10807", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2275, key10807) size=" << scan_result.size() << std::endl;
  }

  // op 9014: put
  db->Put(write_options, "key2662", "value77309");

  // op 9015: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6638", "key9015", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6638, key9015) size=" << scan_result.size() << std::endl;
  }

  // op 9016: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3492", "key11075", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3492, key11075) size=" << scan_result.size() << std::endl;
  }

  // op 9017: delete_range
  status = db->DeleteRange(write_options, "key0264", "key5134");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 9018: delete_range
  status = db->DeleteRange(write_options, "key5134", "key8158");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 9019: delete
  db->Delete(write_options, "key2088");

  // op 9020: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7729", "key8431", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7729, key8431) size=" << scan_result.size() << std::endl;
  }

  // op 9021: delete_range
  status = db->DeleteRange(write_options, "key3540", "key9901");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 9022: get
  status = db->Get(read_options, "key6010", &value);
  if (status.ok()) {
    out << "GET key6010 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9023: put
  db->Put(write_options, "key6742", "value48381");

  // op 9024: put
  db->Put(write_options, "key3742", "value20836");

  // op 9025: get
  status = db->Get(read_options, "key4174", &value);
  if (status.ok()) {
    out << "GET key4174 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9026: put
  db->Put(write_options, "key2654", "value42027");

  // op 9027: put
  db->Put(write_options, "key11516", "value38426");

  // op 9028: get
  status = db->Get(read_options, "key8090", &value);
  if (status.ok()) {
    out << "GET key8090 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9029: put
  db->Put(write_options, "key8169", "value107405");

  // op 9030: delete
  db->Delete(write_options, "key5282");

  // op 9031: put
  db->Put(write_options, "key11078", "value21685");

  // op 9032: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3094", "key7464", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3094, key7464) size=" << scan_result.size() << std::endl;
  }

  // op 9033: put
  db->Put(write_options, "key9061", "value22053");

  // op 9034: get
  status = db->Get(read_options, "key8065", &value);
  if (status.ok()) {
    out << "GET key8065 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9035: delete
  db->Delete(write_options, "key6246");

  // op 9036: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 9037: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1806", "key6905", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1806, key6905) size=" << scan_result.size() << std::endl;
  }

  // op 9038: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1031", "key2710", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1031, key2710) size=" << scan_result.size() << std::endl;
  }

  // op 9039: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8202", "key11399", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8202, key11399) size=" << scan_result.size() << std::endl;
  }

  // op 9040: put
  db->Put(write_options, "key8262", "value97722");

  // op 9041: put
  db->Put(write_options, "key9069", "value70911");

  // op 9042: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 9043: delete
  db->Delete(write_options, "key8595");

  // op 9044: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1732", "key5242", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1732, key5242) size=" << scan_result.size() << std::endl;
  }

  // op 9045: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0824", "key2584", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0824, key2584) size=" << scan_result.size() << std::endl;
  }

  // op 9046: put
  db->Put(write_options, "key4736", "value50549");

  // op 9047: put
  db->Put(write_options, "key7889", "value66552");

  // op 9048: put
  db->Put(write_options, "key1042", "value12532");

  // op 9049: delete
  db->Delete(write_options, "key11031");

  // op 9050: put
  db->Put(write_options, "key6545", "value83883");

  // op 9051: put
  db->Put(write_options, "key4253", "value17233");

  // op 9052: put
  db->Put(write_options, "key0742", "value97320");

  // op 9053: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 9054: put
  db->Put(write_options, "key9546", "value32268");

  // op 9055: put
  db->Put(write_options, "key6413", "value59226");

  // op 9056: put
  db->Put(write_options, "key5329", "value40632");

  // op 9057: get
  status = db->Get(read_options, "key11633", &value);
  if (status.ok()) {
    out << "GET key11633 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9058: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 9059: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 9060: put
  db->Put(write_options, "key2485", "value40479");

  // op 9061: put
  db->Put(write_options, "key3286", "value52742");

  // op 9062: put
  db->Put(write_options, "key11590", "value111168");

  // op 9063: delete_range
  status = db->DeleteRange(write_options, "key0918", "key10916");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 9064: put
  db->Put(write_options, "key2838", "value46523");

  // op 9065: put
  db->Put(write_options, "key5334", "value35905");

  // op 9066: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3160", "key4001", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3160, key4001) size=" << scan_result.size() << std::endl;
  }

  // op 9067: get
  status = db->Get(read_options, "key9609", &value);
  if (status.ok()) {
    out << "GET key9609 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9068: put
  db->Put(write_options, "key5906", "value52346");

  // op 9069: delete_range
  status = db->DeleteRange(write_options, "key2893", "key2894");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 9070: delete
  db->Delete(write_options, "key4688");

  // op 9071: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6006", "key9290", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6006, key9290) size=" << scan_result.size() << std::endl;
  }

  // op 9072: put
  db->Put(write_options, "key6634", "value21085");

  // op 9073: put
  db->Put(write_options, "key0452", "value25481");

  // op 9074: put
  db->Put(write_options, "key11245", "value77318");

  // op 9075: get
  status = db->Get(read_options, "key3273", &value);
  if (status.ok()) {
    out << "GET key3273 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9076: delete
  db->Delete(write_options, "key0673");

  // op 9077: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3737", "key9999", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3737, key9999) size=" << scan_result.size() << std::endl;
  }

  // op 9078: get
  status = db->Get(read_options, "key0792", &value);
  if (status.ok()) {
    out << "GET key0792 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9079: put
  db->Put(write_options, "key9231", "value107812");

  // op 9080: put
  db->Put(write_options, "key11307", "value74690");

  // op 9081: put
  db->Put(write_options, "key0662", "value63206");

  // op 9082: put
  db->Put(write_options, "key3175", "value34469");

  // op 9083: get
  status = db->Get(read_options, "key2452", &value);
  if (status.ok()) {
    out << "GET key2452 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9084: put
  db->Put(write_options, "key3844", "value32317");

  // op 9085: put
  db->Put(write_options, "key10689", "value56010");

  // op 9086: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3663", "key11632", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3663, key11632) size=" << scan_result.size() << std::endl;
  }

  // op 9087: put
  db->Put(write_options, "key6536", "value99182");

  // op 9088: get
  status = db->Get(read_options, "key11204", &value);
  if (status.ok()) {
    out << "GET key11204 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9089: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4787", "key11123", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4787, key11123) size=" << scan_result.size() << std::endl;
  }

  // op 9090: delete
  db->Delete(write_options, "key11409");

  // op 9091: put
  db->Put(write_options, "key8596", "value36853");

  // op 9092: put
  db->Put(write_options, "key10982", "value17503");

  // op 9093: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7587", "key11440", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7587, key11440) size=" << scan_result.size() << std::endl;
  }

  // op 9094: put
  db->Put(write_options, "key7031", "value86452");

  // op 9095: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2874", "key10663", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2874, key10663) size=" << scan_result.size() << std::endl;
  }

  // op 9096: delete_range
  status = db->DeleteRange(write_options, "key0148", "key5863");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 9097: put
  db->Put(write_options, "key10650", "value70992");

  // op 9098: delete_range
  status = db->DeleteRange(write_options, "key7739", "key8367");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 9099: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 9100: put
  db->Put(write_options, "key9212", "value49906");

  // op 9101: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 9102: put
  db->Put(write_options, "key10255", "value57926");

  // op 9103: get
  status = db->Get(read_options, "key6898", &value);
  if (status.ok()) {
    out << "GET key6898 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9104: put
  db->Put(write_options, "key11110", "value100068");

  // op 9105: get
  status = db->Get(read_options, "key7391", &value);
  if (status.ok()) {
    out << "GET key7391 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9106: delete
  db->Delete(write_options, "key2896");

  // op 9107: put
  db->Put(write_options, "key0944", "value14796");

  // op 9108: put
  db->Put(write_options, "key3719", "value22981");

  // op 9109: get
  status = db->Get(read_options, "key0632", &value);
  if (status.ok()) {
    out << "GET key0632 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9110: get
  status = db->Get(read_options, "key0317", &value);
  if (status.ok()) {
    out << "GET key0317 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9111: put
  db->Put(write_options, "key3990", "value109378");

  // op 9112: delete_range
  status = db->DeleteRange(write_options, "key1151", "key5100");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 9113: put
  db->Put(write_options, "key7628", "value4846");

  // op 9114: get
  status = db->Get(read_options, "key3831", &value);
  if (status.ok()) {
    out << "GET key3831 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9115: put
  db->Put(write_options, "key7176", "value66836");

  // op 9116: delete
  db->Delete(write_options, "key8131");

  // op 9117: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 9118: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9145", "key10503", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9145, key10503) size=" << scan_result.size() << std::endl;
  }

  // op 9119: get
  status = db->Get(read_options, "key3942", &value);
  if (status.ok()) {
    out << "GET key3942 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9120: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10320", "key11487", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10320, key11487) size=" << scan_result.size() << std::endl;
  }

  // op 9121: put
  db->Put(write_options, "key5329", "value1029");

  // op 9122: get
  status = db->Get(read_options, "key10204", &value);
  if (status.ok()) {
    out << "GET key10204 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9123: get
  status = db->Get(read_options, "key9617", &value);
  if (status.ok()) {
    out << "GET key9617 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9124: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4799", "key7864", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4799, key7864) size=" << scan_result.size() << std::endl;
  }

  // op 9125: delete_range
  status = db->DeleteRange(write_options, "key2016", "key2957");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 9126: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5480", "key7467", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5480, key7467) size=" << scan_result.size() << std::endl;
  }

  // op 9127: get
  status = db->Get(read_options, "key4972", &value);
  if (status.ok()) {
    out << "GET key4972 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9128: delete_range
  status = db->DeleteRange(write_options, "key0611", "key8798");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 9129: put
  db->Put(write_options, "key4650", "value116882");

  // op 9130: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 9131: put
  db->Put(write_options, "key7054", "value91611");

  // op 9132: delete
  db->Delete(write_options, "key11554");

  // op 9133: put
  db->Put(write_options, "key0853", "value106130");

  // op 9134: get
  status = db->Get(read_options, "key4627", &value);
  if (status.ok()) {
    out << "GET key4627 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9135: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1941", "key2546", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1941, key2546) size=" << scan_result.size() << std::endl;
  }

  // op 9136: put
  db->Put(write_options, "key1117", "value57273");

  // op 9137: put
  db->Put(write_options, "key11299", "value100322");

  // op 9138: put
  db->Put(write_options, "key3846", "value53812");

  // op 9139: put
  db->Put(write_options, "key2930", "value90898");

  // op 9140: delete_range
  status = db->DeleteRange(write_options, "key8264", "key8618");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 9141: put
  db->Put(write_options, "key0589", "value60774");

  // op 9142: put
  db->Put(write_options, "key4309", "value47005");

  // op 9143: delete
  db->Delete(write_options, "key3042");

  // op 9144: get
  status = db->Get(read_options, "key8285", &value);
  if (status.ok()) {
    out << "GET key8285 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9145: delete
  db->Delete(write_options, "key11312");

  // op 9146: put
  db->Put(write_options, "key2351", "value93373");

  // op 9147: delete
  db->Delete(write_options, "key10438");

  // op 9148: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4027", "key8814", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4027, key8814) size=" << scan_result.size() << std::endl;
  }

  // op 9149: put
  db->Put(write_options, "key0092", "value12535");

  // op 9150: put
  db->Put(write_options, "key1569", "value10071");

  // op 9151: put
  db->Put(write_options, "key3580", "value95493");

  // op 9152: get
  status = db->Get(read_options, "key9940", &value);
  if (status.ok()) {
    out << "GET key9940 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9153: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3363", "key4814", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3363, key4814) size=" << scan_result.size() << std::endl;
  }

  // op 9154: put
  db->Put(write_options, "key4711", "value7108");

  // op 9155: put
  db->Put(write_options, "key11238", "value46691");

  // op 9156: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5322", "key10017", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5322, key10017) size=" << scan_result.size() << std::endl;
  }

  // op 9157: get
  status = db->Get(read_options, "key2472", &value);
  if (status.ok()) {
    out << "GET key2472 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9158: put
  db->Put(write_options, "key3972", "value49238");

  // op 9159: put
  db->Put(write_options, "key8055", "value100449");

  // op 9160: get
  status = db->Get(read_options, "key0523", &value);
  if (status.ok()) {
    out << "GET key0523 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9161: get
  status = db->Get(read_options, "key9649", &value);
  if (status.ok()) {
    out << "GET key9649 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9162: delete_range
  status = db->DeleteRange(write_options, "key1171", "key4927");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 9163: delete_range
  status = db->DeleteRange(write_options, "key9488", "key11192");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 9164: get
  status = db->Get(read_options, "key9595", &value);
  if (status.ok()) {
    out << "GET key9595 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9165: delete
  db->Delete(write_options, "key6126");

  // op 9166: put
  db->Put(write_options, "key5949", "value78350");

  // op 9167: put
  db->Put(write_options, "key7269", "value65849");

  // op 9168: put
  db->Put(write_options, "key6616", "value24415");

  // op 9169: put
  db->Put(write_options, "key1615", "value53889");

  // op 9170: put
  db->Put(write_options, "key0085", "value98001");

  // op 9171: delete_range
  status = db->DeleteRange(write_options, "key0376", "key4440");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 9172: put
  db->Put(write_options, "key9217", "value70770");

  // op 9173: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 9174: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0328", "key10463", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0328, key10463) size=" << scan_result.size() << std::endl;
  }

  // op 9175: put
  db->Put(write_options, "key6756", "value86859");

  // op 9176: get
  status = db->Get(read_options, "key5668", &value);
  if (status.ok()) {
    out << "GET key5668 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9177: put
  db->Put(write_options, "key5824", "value15122");

  // op 9178: put
  db->Put(write_options, "key5512", "value31767");

  // op 9179: put
  db->Put(write_options, "key1900", "value42453");

  // op 9180: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1418", "key6168", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1418, key6168) size=" << scan_result.size() << std::endl;
  }

  // op 9181: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6556", "key11671", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6556, key11671) size=" << scan_result.size() << std::endl;
  }

  // op 9182: get
  status = db->Get(read_options, "key4861", &value);
  if (status.ok()) {
    out << "GET key4861 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9183: put
  db->Put(write_options, "key2558", "value48711");

  // op 9184: put
  db->Put(write_options, "key8636", "value88820");

  // op 9185: put
  db->Put(write_options, "key6525", "value116980");

  // op 9186: put
  db->Put(write_options, "key8179", "value31400");

  // op 9187: get
  status = db->Get(read_options, "key11038", &value);
  if (status.ok()) {
    out << "GET key11038 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9188: delete
  db->Delete(write_options, "key7803");

  // op 9189: delete_range
  status = db->DeleteRange(write_options, "key0752", "key1480");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 9190: put
  db->Put(write_options, "key11028", "value94396");

  // op 9191: put
  db->Put(write_options, "key3552", "value23918");

  // op 9192: delete_range
  status = db->DeleteRange(write_options, "key7183", "key9957");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 9193: get
  status = db->Get(read_options, "key0089", &value);
  if (status.ok()) {
    out << "GET key0089 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9194: put
  db->Put(write_options, "key6954", "value115461");

  // op 9195: put
  db->Put(write_options, "key8934", "value56807");

  // op 9196: put
  db->Put(write_options, "key5080", "value107984");

  // op 9197: get
  status = db->Get(read_options, "key7759", &value);
  if (status.ok()) {
    out << "GET key7759 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9198: put
  db->Put(write_options, "key3905", "value107969");

  // op 9199: put
  db->Put(write_options, "key6050", "value48363");

  // op 9200: put
  db->Put(write_options, "key0565", "value12763");

  // op 9201: delete
  db->Delete(write_options, "key9462");

  // op 9202: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7019", "key9892", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7019, key9892) size=" << scan_result.size() << std::endl;
  }

  // op 9203: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0809", "key11336", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0809, key11336) size=" << scan_result.size() << std::endl;
  }

  // op 9204: put
  db->Put(write_options, "key6925", "value107172");

  // op 9205: delete
  db->Delete(write_options, "key1840");

  // op 9206: put
  db->Put(write_options, "key1512", "value25256");

  // op 9207: get
  status = db->Get(read_options, "key2428", &value);
  if (status.ok()) {
    out << "GET key2428 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9208: put
  db->Put(write_options, "key6555", "value109873");

  // op 9209: put
  db->Put(write_options, "key8474", "value94723");

  // op 9210: delete_range
  status = db->DeleteRange(write_options, "key3987", "key7444");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 9211: put
  db->Put(write_options, "key1645", "value31580");

  // op 9212: delete
  db->Delete(write_options, "key6041");

  // op 9213: delete
  db->Delete(write_options, "key6253");

  // op 9214: delete_range
  status = db->DeleteRange(write_options, "key8166", "key10356");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 9215: put
  db->Put(write_options, "key6744", "value88954");

  // op 9216: put
  db->Put(write_options, "key2391", "value106516");

  // op 9217: put
  db->Put(write_options, "key2350", "value56578");

  // op 9218: get
  status = db->Get(read_options, "key1123", &value);
  if (status.ok()) {
    out << "GET key1123 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9219: put
  db->Put(write_options, "key4205", "value64621");

  // op 9220: put
  db->Put(write_options, "key8945", "value98145");

  // op 9221: get
  status = db->Get(read_options, "key6473", &value);
  if (status.ok()) {
    out << "GET key6473 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9222: delete_range
  status = db->DeleteRange(write_options, "key9294", "key11243");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 9223: put
  db->Put(write_options, "key2791", "value23778");

  // op 9224: delete_range
  status = db->DeleteRange(write_options, "key2954", "key10356");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 9225: put
  db->Put(write_options, "key6117", "value85367");

  // op 9226: get
  status = db->Get(read_options, "key4832", &value);
  if (status.ok()) {
    out << "GET key4832 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9227: put
  db->Put(write_options, "key3589", "value66009");

  // op 9228: put
  db->Put(write_options, "key10354", "value35377");

  // op 9229: put
  db->Put(write_options, "key5365", "value60046");

  // op 9230: put
  db->Put(write_options, "key7630", "value98599");

  // op 9231: put
  db->Put(write_options, "key6083", "value48652");

  // op 9232: delete_range
  status = db->DeleteRange(write_options, "key3817", "key6002");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 9233: get
  status = db->Get(read_options, "key2928", &value);
  if (status.ok()) {
    out << "GET key2928 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9234: delete
  db->Delete(write_options, "key9205");

  // op 9235: put
  db->Put(write_options, "key4986", "value98778");

  // op 9236: delete
  db->Delete(write_options, "key6907");

  // op 9237: put
  db->Put(write_options, "key8471", "value32670");

  // op 9238: delete
  db->Delete(write_options, "key4069");

  // op 9239: get
  status = db->Get(read_options, "key0766", &value);
  if (status.ok()) {
    out << "GET key0766 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9240: get
  status = db->Get(read_options, "key4890", &value);
  if (status.ok()) {
    out << "GET key4890 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9241: get
  status = db->Get(read_options, "key2619", &value);
  if (status.ok()) {
    out << "GET key2619 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9242: delete_range
  status = db->DeleteRange(write_options, "key0892", "key3451");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 9243: get
  status = db->Get(read_options, "key3827", &value);
  if (status.ok()) {
    out << "GET key3827 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9244: put
  db->Put(write_options, "key6952", "value38866");

  // op 9245: put
  db->Put(write_options, "key9813", "value117568");

  // op 9246: put
  db->Put(write_options, "key7358", "value67983");

  // op 9247: put
  db->Put(write_options, "key6964", "value43874");

  // op 9248: delete
  db->Delete(write_options, "key4790");

  // op 9249: put
  db->Put(write_options, "key5960", "value60775");

  // op 9250: put
  db->Put(write_options, "key5904", "value34647");

  // op 9251: get
  status = db->Get(read_options, "key5306", &value);
  if (status.ok()) {
    out << "GET key5306 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9252: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2017", "key4623", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2017, key4623) size=" << scan_result.size() << std::endl;
  }

  // op 9253: put
  db->Put(write_options, "key1314", "value53023");

  // op 9254: delete
  db->Delete(write_options, "key1877");

  // op 9255: put
  db->Put(write_options, "key5147", "value12426");

  // op 9256: put
  db->Put(write_options, "key2317", "value58099");

  // op 9257: put
  db->Put(write_options, "key4323", "value27766");

  // op 9258: delete
  db->Delete(write_options, "key4554");

  // op 9259: put
  db->Put(write_options, "key4767", "value30989");

  // op 9260: put
  db->Put(write_options, "key8442", "value63894");

  // op 9261: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 9262: get
  status = db->Get(read_options, "key4610", &value);
  if (status.ok()) {
    out << "GET key4610 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9263: delete
  db->Delete(write_options, "key6669");

  // op 9264: put
  db->Put(write_options, "key5695", "value84679");

  // op 9265: delete
  db->Delete(write_options, "key9559");

  // op 9266: delete
  db->Delete(write_options, "key1050");

  // op 9267: put
  db->Put(write_options, "key9362", "value89678");

  // op 9268: put
  db->Put(write_options, "key1150", "value86904");

  // op 9269: delete
  db->Delete(write_options, "key5104");

  // op 9270: put
  db->Put(write_options, "key4163", "value1023");

  // op 9271: put
  db->Put(write_options, "key10228", "value41391");

  // op 9272: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 9273: put
  db->Put(write_options, "key2546", "value21228");

  // op 9274: get
  status = db->Get(read_options, "key0959", &value);
  if (status.ok()) {
    out << "GET key0959 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9275: get
  status = db->Get(read_options, "key4349", &value);
  if (status.ok()) {
    out << "GET key4349 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9276: get
  status = db->Get(read_options, "key10618", &value);
  if (status.ok()) {
    out << "GET key10618 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9277: put
  db->Put(write_options, "key5825", "value93815");

  // op 9278: put
  db->Put(write_options, "key8835", "value68213");

  // op 9279: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0084", "key9354", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0084, key9354) size=" << scan_result.size() << std::endl;
  }

  // op 9280: put
  db->Put(write_options, "key10247", "value90279");

  // op 9281: put
  db->Put(write_options, "key9089", "value21902");

  // op 9282: put
  db->Put(write_options, "key0011", "value42587");

  // op 9283: delete_range
  status = db->DeleteRange(write_options, "key0231", "key11376");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 9284: delete_range
  status = db->DeleteRange(write_options, "key3305", "key9511");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 9285: get
  status = db->Get(read_options, "key8251", &value);
  if (status.ok()) {
    out << "GET key8251 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9286: put
  db->Put(write_options, "key1285", "value107952");

  // op 9287: put
  db->Put(write_options, "key1073", "value77544");

  // op 9288: put
  db->Put(write_options, "key8942", "value81997");

  // op 9289: delete
  db->Delete(write_options, "key7319");

  // op 9290: delete
  db->Delete(write_options, "key7284");

  // op 9291: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3474", "key10479", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3474, key10479) size=" << scan_result.size() << std::endl;
  }

  // op 9292: get
  status = db->Get(read_options, "key7861", &value);
  if (status.ok()) {
    out << "GET key7861 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9293: delete_range
  status = db->DeleteRange(write_options, "key7037", "key8454");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 9294: delete
  db->Delete(write_options, "key6908");

  // op 9295: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4668", "key4842", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4668, key4842) size=" << scan_result.size() << std::endl;
  }

  // op 9296: get
  status = db->Get(read_options, "key5145", &value);
  if (status.ok()) {
    out << "GET key5145 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9297: put
  db->Put(write_options, "key0414", "value2751");

  // op 9298: put
  db->Put(write_options, "key0515", "value101352");

  // op 9299: put
  db->Put(write_options, "key6783", "value54446");

  // op 9300: delete_range
  status = db->DeleteRange(write_options, "key5599", "key9722");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 9301: put
  db->Put(write_options, "key0352", "value70425");

  // op 9302: get
  status = db->Get(read_options, "key7454", &value);
  if (status.ok()) {
    out << "GET key7454 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9303: put
  db->Put(write_options, "key0841", "value91610");

  // op 9304: put
  db->Put(write_options, "key6420", "value26306");

  // op 9305: get
  status = db->Get(read_options, "key7953", &value);
  if (status.ok()) {
    out << "GET key7953 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9306: put
  db->Put(write_options, "key5428", "value40965");

  // op 9307: put
  db->Put(write_options, "key3034", "value36824");

  // op 9308: put
  db->Put(write_options, "key7381", "value19093");

  // op 9309: get
  status = db->Get(read_options, "key1468", &value);
  if (status.ok()) {
    out << "GET key1468 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9310: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1467", "key1822", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1467, key1822) size=" << scan_result.size() << std::endl;
  }

  // op 9311: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3115", "key7116", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3115, key7116) size=" << scan_result.size() << std::endl;
  }

  // op 9312: delete
  db->Delete(write_options, "key8691");

  // op 9313: get
  status = db->Get(read_options, "key2945", &value);
  if (status.ok()) {
    out << "GET key2945 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9314: delete_range
  status = db->DeleteRange(write_options, "key0088", "key5695");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 9315: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4865", "key6596", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4865, key6596) size=" << scan_result.size() << std::endl;
  }

  // op 9316: delete_range
  status = db->DeleteRange(write_options, "key2058", "key2488");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 9317: delete_range
  status = db->DeleteRange(write_options, "key5538", "key11382");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 9318: get
  status = db->Get(read_options, "key5255", &value);
  if (status.ok()) {
    out << "GET key5255 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9319: get
  status = db->Get(read_options, "key8294", &value);
  if (status.ok()) {
    out << "GET key8294 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9320: delete_range
  status = db->DeleteRange(write_options, "key3974", "key7085");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 9321: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0134", "key3410", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0134, key3410) size=" << scan_result.size() << std::endl;
  }

  // op 9322: put
  db->Put(write_options, "key9136", "value81765");

  // op 9323: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0201", "key10138", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0201, key10138) size=" << scan_result.size() << std::endl;
  }

  // op 9324: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7481", "key7825", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7481, key7825) size=" << scan_result.size() << std::endl;
  }

  // op 9325: put
  db->Put(write_options, "key9457", "value83485");

  // op 9326: get
  status = db->Get(read_options, "key11384", &value);
  if (status.ok()) {
    out << "GET key11384 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9327: delete_range
  status = db->DeleteRange(write_options, "key3906", "key10571");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 9328: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1674", "key10158", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1674, key10158) size=" << scan_result.size() << std::endl;
  }

  // op 9329: put
  db->Put(write_options, "key7841", "value40966");

  // op 9330: get
  status = db->Get(read_options, "key10373", &value);
  if (status.ok()) {
    out << "GET key10373 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9331: put
  db->Put(write_options, "key5896", "value36764");

  // op 9332: put
  db->Put(write_options, "key5693", "value112342");

  // op 9333: put
  db->Put(write_options, "key3666", "value40857");

  // op 9334: put
  db->Put(write_options, "key4219", "value15506");

  // op 9335: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7306", "key10778", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7306, key10778) size=" << scan_result.size() << std::endl;
  }

  // op 9336: put
  db->Put(write_options, "key4680", "value22301");

  // op 9337: get
  status = db->Get(read_options, "key7547", &value);
  if (status.ok()) {
    out << "GET key7547 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9338: get
  status = db->Get(read_options, "key10505", &value);
  if (status.ok()) {
    out << "GET key10505 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9339: put
  db->Put(write_options, "key2318", "value18623");

  // op 9340: get
  status = db->Get(read_options, "key9254", &value);
  if (status.ok()) {
    out << "GET key9254 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9341: get
  status = db->Get(read_options, "key0280", &value);
  if (status.ok()) {
    out << "GET key0280 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9342: put
  db->Put(write_options, "key9180", "value44853");

  // op 9343: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0163", "key7450", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0163, key7450) size=" << scan_result.size() << std::endl;
  }

  // op 9344: delete_range
  status = db->DeleteRange(write_options, "key2583", "key8771");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 9345: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2822", "key8233", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2822, key8233) size=" << scan_result.size() << std::endl;
  }

  // op 9346: get
  status = db->Get(read_options, "key6882", &value);
  if (status.ok()) {
    out << "GET key6882 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9347: put
  db->Put(write_options, "key3006", "value60989");

  // op 9348: put
  db->Put(write_options, "key4190", "value111285");

  // op 9349: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1118", "key6435", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1118, key6435) size=" << scan_result.size() << std::endl;
  }

  // op 9350: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 9351: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 9352: put
  db->Put(write_options, "key8302", "value32968");

  // op 9353: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0464", "key8821", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0464, key8821) size=" << scan_result.size() << std::endl;
  }

  // op 9354: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 9355: put
  db->Put(write_options, "key3589", "value13014");

  // op 9356: put
  db->Put(write_options, "key1030", "value108190");

  // op 9357: get
  status = db->Get(read_options, "key11699", &value);
  if (status.ok()) {
    out << "GET key11699 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9358: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3834", "key7062", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3834, key7062) size=" << scan_result.size() << std::endl;
  }

  // op 9359: delete
  db->Delete(write_options, "key5973");

  // op 9360: put
  db->Put(write_options, "key11301", "value31472");

  // op 9361: get
  status = db->Get(read_options, "key11143", &value);
  if (status.ok()) {
    out << "GET key11143 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9362: put
  db->Put(write_options, "key3294", "value114990");

  // op 9363: get
  status = db->Get(read_options, "key9393", &value);
  if (status.ok()) {
    out << "GET key9393 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9364: put
  db->Put(write_options, "key5115", "value47539");

  // op 9365: put
  db->Put(write_options, "key8871", "value14279");

  // op 9366: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 9367: put
  db->Put(write_options, "key6661", "value19328");

  // op 9368: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1834", "key6161", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1834, key6161) size=" << scan_result.size() << std::endl;
  }

  // op 9369: delete_range
  status = db->DeleteRange(write_options, "key5162", "key8540");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 9370: put
  db->Put(write_options, "key9916", "value100621");

  // op 9371: delete
  db->Delete(write_options, "key10524");

  // op 9372: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6063", "key8361", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6063, key8361) size=" << scan_result.size() << std::endl;
  }

  // op 9373: delete
  db->Delete(write_options, "key3797");

  // op 9374: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7791", "key7813", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7791, key7813) size=" << scan_result.size() << std::endl;
  }

  // op 9375: put
  db->Put(write_options, "key7858", "value28654");

  // op 9376: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3261", "key6753", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3261, key6753) size=" << scan_result.size() << std::endl;
  }

  // op 9377: delete_range
  status = db->DeleteRange(write_options, "key4551", "key11098");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 9378: delete_range
  status = db->DeleteRange(write_options, "key3051", "key5133");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 9379: get
  status = db->Get(read_options, "key0605", &value);
  if (status.ok()) {
    out << "GET key0605 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9380: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 9381: put
  db->Put(write_options, "key4349", "value116353");

  // op 9382: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 9383: put
  db->Put(write_options, "key3749", "value98167");

  // op 9384: get
  status = db->Get(read_options, "key11054", &value);
  if (status.ok()) {
    out << "GET key11054 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9385: put
  db->Put(write_options, "key0191", "value86988");

  // op 9386: get
  status = db->Get(read_options, "key9553", &value);
  if (status.ok()) {
    out << "GET key9553 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9387: put
  db->Put(write_options, "key1114", "value34479");

  // op 9388: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4451", "key4839", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4451, key4839) size=" << scan_result.size() << std::endl;
  }

  // op 9389: put
  db->Put(write_options, "key1988", "value96317");

  // op 9390: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7643", "key10742", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7643, key10742) size=" << scan_result.size() << std::endl;
  }

  // op 9391: delete
  db->Delete(write_options, "key0393");

  // op 9392: delete
  db->Delete(write_options, "key0199");

  // op 9393: put
  db->Put(write_options, "key5842", "value85318");

  // op 9394: put
  db->Put(write_options, "key6933", "value5863");

  // op 9395: put
  db->Put(write_options, "key8822", "value79678");

  // op 9396: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2328", "key6425", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2328, key6425) size=" << scan_result.size() << std::endl;
  }

  // op 9397: put
  db->Put(write_options, "key5810", "value7580");

  // op 9398: get
  status = db->Get(read_options, "key1890", &value);
  if (status.ok()) {
    out << "GET key1890 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9399: delete_range
  status = db->DeleteRange(write_options, "key6409", "key9191");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 9400: put
  db->Put(write_options, "key3814", "value54513");

  // op 9401: put
  db->Put(write_options, "key9785", "value85016");

  // op 9402: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2488", "key11691", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2488, key11691) size=" << scan_result.size() << std::endl;
  }

  // op 9403: put
  db->Put(write_options, "key7326", "value98734");

  // op 9404: get
  status = db->Get(read_options, "key2377", &value);
  if (status.ok()) {
    out << "GET key2377 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9405: get
  status = db->Get(read_options, "key4610", &value);
  if (status.ok()) {
    out << "GET key4610 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9406: get
  status = db->Get(read_options, "key2119", &value);
  if (status.ok()) {
    out << "GET key2119 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9407: get
  status = db->Get(read_options, "key6565", &value);
  if (status.ok()) {
    out << "GET key6565 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9408: put
  db->Put(write_options, "key4021", "value38274");

  // op 9409: put
  db->Put(write_options, "key5933", "value9309");

  // op 9410: put
  db->Put(write_options, "key2862", "value58902");

  // op 9411: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2015", "key8394", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2015, key8394) size=" << scan_result.size() << std::endl;
  }

  // op 9412: put
  db->Put(write_options, "key11612", "value33870");

  // op 9413: delete
  db->Delete(write_options, "key3675");

  // op 9414: put
  db->Put(write_options, "key5602", "value11997");

  // op 9415: put
  db->Put(write_options, "key9444", "value83920");

  // op 9416: put
  db->Put(write_options, "key9868", "value9693");

  // op 9417: delete
  db->Delete(write_options, "key1856");

  // op 9418: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 9419: put
  db->Put(write_options, "key2017", "value74540");

  // op 9420: get
  status = db->Get(read_options, "key10549", &value);
  if (status.ok()) {
    out << "GET key10549 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9421: put
  db->Put(write_options, "key10896", "value60862");

  // op 9422: get
  status = db->Get(read_options, "key7171", &value);
  if (status.ok()) {
    out << "GET key7171 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9423: get
  status = db->Get(read_options, "key2992", &value);
  if (status.ok()) {
    out << "GET key2992 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9424: delete
  db->Delete(write_options, "key6088");

  // op 9425: delete
  db->Delete(write_options, "key10539");

  // op 9426: get
  status = db->Get(read_options, "key5360", &value);
  if (status.ok()) {
    out << "GET key5360 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9427: put
  db->Put(write_options, "key9839", "value61467");

  // op 9428: put
  db->Put(write_options, "key3808", "value101701");

  // op 9429: put
  db->Put(write_options, "key2758", "value32569");

  // op 9430: put
  db->Put(write_options, "key10586", "value37818");

  // op 9431: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1587", "key6232", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1587, key6232) size=" << scan_result.size() << std::endl;
  }

  // op 9432: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3175", "key6576", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3175, key6576) size=" << scan_result.size() << std::endl;
  }

  // op 9433: get
  status = db->Get(read_options, "key1013", &value);
  if (status.ok()) {
    out << "GET key1013 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9434: put
  db->Put(write_options, "key2231", "value83754");

  // op 9435: get
  status = db->Get(read_options, "key9624", &value);
  if (status.ok()) {
    out << "GET key9624 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9436: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3932", "key4735", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3932, key4735) size=" << scan_result.size() << std::endl;
  }

  // op 9437: get
  status = db->Get(read_options, "key7528", &value);
  if (status.ok()) {
    out << "GET key7528 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9438: put
  db->Put(write_options, "key5868", "value56134");

  // op 9439: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 9440: put
  db->Put(write_options, "key1148", "value108160");

  // op 9441: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7863", "key9565", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7863, key9565) size=" << scan_result.size() << std::endl;
  }

  // op 9442: put
  db->Put(write_options, "key0452", "value29916");

  // op 9443: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6972", "key9359", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6972, key9359) size=" << scan_result.size() << std::endl;
  }

  // op 9444: get
  status = db->Get(read_options, "key11238", &value);
  if (status.ok()) {
    out << "GET key11238 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9445: put
  db->Put(write_options, "key8021", "value29576");

  // op 9446: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6266", "key6589", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6266, key6589) size=" << scan_result.size() << std::endl;
  }

  // op 9447: put
  db->Put(write_options, "key7619", "value10556");

  // op 9448: put
  db->Put(write_options, "key10801", "value13339");

  // op 9449: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2257", "key11349", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2257, key11349) size=" << scan_result.size() << std::endl;
  }

  // op 9450: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3877", "key10197", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3877, key10197) size=" << scan_result.size() << std::endl;
  }

  // op 9451: get
  status = db->Get(read_options, "key5613", &value);
  if (status.ok()) {
    out << "GET key5613 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9452: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 9453: put
  db->Put(write_options, "key5005", "value8930");

  // op 9454: put
  db->Put(write_options, "key1697", "value26647");

  // op 9455: put
  db->Put(write_options, "key5701", "value116689");

  // op 9456: put
  db->Put(write_options, "key4416", "value86883");

  // op 9457: put
  db->Put(write_options, "key10789", "value81429");

  // op 9458: put
  db->Put(write_options, "key5929", "value64371");

  // op 9459: put
  db->Put(write_options, "key1008", "value49724");

  // op 9460: put
  db->Put(write_options, "key11031", "value97485");

  // op 9461: delete
  db->Delete(write_options, "key7714");

  // op 9462: put
  db->Put(write_options, "key9722", "value98971");

  // op 9463: get
  status = db->Get(read_options, "key10918", &value);
  if (status.ok()) {
    out << "GET key10918 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9464: get
  status = db->Get(read_options, "key8356", &value);
  if (status.ok()) {
    out << "GET key8356 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9465: get
  status = db->Get(read_options, "key4751", &value);
  if (status.ok()) {
    out << "GET key4751 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9466: put
  db->Put(write_options, "key6628", "value48831");

  // op 9467: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 9468: put
  db->Put(write_options, "key7669", "value69502");

  // op 9469: put
  db->Put(write_options, "key2018", "value103770");

  // op 9470: put
  db->Put(write_options, "key7998", "value71064");

  // op 9471: put
  db->Put(write_options, "key5425", "value37559");

  // op 9472: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7374", "key8891", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7374, key8891) size=" << scan_result.size() << std::endl;
  }

  // op 9473: put
  db->Put(write_options, "key3685", "value58169");

  // op 9474: put
  db->Put(write_options, "key7218", "value77419");

  // op 9475: put
  db->Put(write_options, "key0755", "value85780");

  // op 9476: put
  db->Put(write_options, "key11132", "value89114");

  // op 9477: get
  status = db->Get(read_options, "key3992", &value);
  if (status.ok()) {
    out << "GET key3992 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9478: put
  db->Put(write_options, "key0807", "value106636");

  // op 9479: put
  db->Put(write_options, "key6029", "value88527");

  // op 9480: put
  db->Put(write_options, "key8108", "value106411");

  // op 9481: delete
  db->Delete(write_options, "key8490");

  // op 9482: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5540", "key11569", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5540, key11569) size=" << scan_result.size() << std::endl;
  }

  // op 9483: put
  db->Put(write_options, "key4003", "value19516");

  // op 9484: delete
  db->Delete(write_options, "key11058");

  // op 9485: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 9486: put
  db->Put(write_options, "key6877", "value27780");

  // op 9487: get
  status = db->Get(read_options, "key8853", &value);
  if (status.ok()) {
    out << "GET key8853 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9488: put
  db->Put(write_options, "key8636", "value60449");

  // op 9489: delete
  db->Delete(write_options, "key4494");

  // op 9490: put
  db->Put(write_options, "key1397", "value97561");

  // op 9491: put
  db->Put(write_options, "key10599", "value105542");

  // op 9492: delete
  db->Delete(write_options, "key4984");

  // op 9493: put
  db->Put(write_options, "key0038", "value74920");

  // op 9494: put
  db->Put(write_options, "key4235", "value44749");

  // op 9495: get
  status = db->Get(read_options, "key1642", &value);
  if (status.ok()) {
    out << "GET key1642 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9496: get
  status = db->Get(read_options, "key4012", &value);
  if (status.ok()) {
    out << "GET key4012 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9497: get
  status = db->Get(read_options, "key0700", &value);
  if (status.ok()) {
    out << "GET key0700 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9498: put
  db->Put(write_options, "key2500", "value83862");

  // op 9499: put
  db->Put(write_options, "key3179", "value7801");

  // op 9500: put
  db->Put(write_options, "key3166", "value17814");

  // op 9501: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 9502: put
  db->Put(write_options, "key0784", "value10464");

  // op 9503: put
  db->Put(write_options, "key7985", "value89286");

  // op 9504: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0767", "key1902", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0767, key1902) size=" << scan_result.size() << std::endl;
  }

  // op 9505: put
  db->Put(write_options, "key11457", "value118284");

  // op 9506: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 9507: get
  status = db->Get(read_options, "key6118", &value);
  if (status.ok()) {
    out << "GET key6118 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9508: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6335", "key10998", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6335, key10998) size=" << scan_result.size() << std::endl;
  }

  // op 9509: put
  db->Put(write_options, "key11597", "value71380");

  // op 9510: delete
  db->Delete(write_options, "key6958");

  // op 9511: put
  db->Put(write_options, "key6429", "value67645");

  // op 9512: put
  db->Put(write_options, "key10523", "value67076");

  // op 9513: put
  db->Put(write_options, "key5372", "value65303");

  // op 9514: get
  status = db->Get(read_options, "key9942", &value);
  if (status.ok()) {
    out << "GET key9942 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9515: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2811", "key7140", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2811, key7140) size=" << scan_result.size() << std::endl;
  }

  // op 9516: put
  db->Put(write_options, "key10928", "value4670");

  // op 9517: delete
  db->Delete(write_options, "key1068");

  // op 9518: get
  status = db->Get(read_options, "key9327", &value);
  if (status.ok()) {
    out << "GET key9327 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9519: put
  db->Put(write_options, "key10827", "value57163");

  // op 9520: put
  db->Put(write_options, "key5555", "value81485");

  // op 9521: put
  db->Put(write_options, "key6528", "value117726");

  // op 9522: delete_range
  status = db->DeleteRange(write_options, "key0557", "key9837");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 9523: delete
  db->Delete(write_options, "key9303");

  // op 9524: put
  db->Put(write_options, "key2822", "value104271");

  // op 9525: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3494", "key10035", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3494, key10035) size=" << scan_result.size() << std::endl;
  }

  // op 9526: get
  status = db->Get(read_options, "key10195", &value);
  if (status.ok()) {
    out << "GET key10195 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9527: get
  status = db->Get(read_options, "key8728", &value);
  if (status.ok()) {
    out << "GET key8728 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9528: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5579", "key9813", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5579, key9813) size=" << scan_result.size() << std::endl;
  }

  // op 9529: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 9530: delete
  db->Delete(write_options, "key6992");

  // op 9531: delete_range
  status = db->DeleteRange(write_options, "key5699", "key11272");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 9532: put
  db->Put(write_options, "key5258", "value48131");

  // op 9533: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 9534: put
  db->Put(write_options, "key1484", "value111369");

  // op 9535: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0685", "key2769", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0685, key2769) size=" << scan_result.size() << std::endl;
  }

  // op 9536: delete_range
  status = db->DeleteRange(write_options, "key4250", "key11348");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 9537: put
  db->Put(write_options, "key3641", "value100559");

  // op 9538: delete
  db->Delete(write_options, "key5878");

  // op 9539: delete_range
  status = db->DeleteRange(write_options, "key1254", "key11063");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 9540: get
  status = db->Get(read_options, "key10051", &value);
  if (status.ok()) {
    out << "GET key10051 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9541: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0038", "key4858", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0038, key4858) size=" << scan_result.size() << std::endl;
  }

  // op 9542: get
  status = db->Get(read_options, "key4993", &value);
  if (status.ok()) {
    out << "GET key4993 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9543: put
  db->Put(write_options, "key11472", "value79054");

  // op 9544: put
  db->Put(write_options, "key7006", "value25366");

  // op 9545: delete_range
  status = db->DeleteRange(write_options, "key3251", "key4654");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 9546: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 9547: put
  db->Put(write_options, "key2500", "value24288");

  // op 9548: put
  db->Put(write_options, "key1640", "value17116");

  // op 9549: get
  status = db->Get(read_options, "key6196", &value);
  if (status.ok()) {
    out << "GET key6196 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9550: get
  status = db->Get(read_options, "key6499", &value);
  if (status.ok()) {
    out << "GET key6499 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9551: put
  db->Put(write_options, "key6195", "value106940");

  // op 9552: delete_range
  status = db->DeleteRange(write_options, "key4884", "key6406");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 9553: put
  db->Put(write_options, "key9526", "value115116");

  // op 9554: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7306", "key9537", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7306, key9537) size=" << scan_result.size() << std::endl;
  }

  // op 9555: put
  db->Put(write_options, "key9648", "value56292");

  // op 9556: delete
  db->Delete(write_options, "key11085");

  // op 9557: delete
  db->Delete(write_options, "key0001");

  // op 9558: delete_range
  status = db->DeleteRange(write_options, "key6962", "key9955");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 9559: delete_range
  status = db->DeleteRange(write_options, "key8355", "key10537");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 9560: put
  db->Put(write_options, "key9635", "value75908");

  // op 9561: delete
  db->Delete(write_options, "key2361");

  // op 9562: put
  db->Put(write_options, "key10028", "value86792");

  // op 9563: put
  db->Put(write_options, "key1828", "value8147");

  // op 9564: put
  db->Put(write_options, "key1128", "value16286");

  // op 9565: get
  status = db->Get(read_options, "key8977", &value);
  if (status.ok()) {
    out << "GET key8977 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9566: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4557", "key10512", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4557, key10512) size=" << scan_result.size() << std::endl;
  }

  // op 9567: delete_range
  status = db->DeleteRange(write_options, "key0878", "key8235");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 9568: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 9569: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2082", "key6533", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2082, key6533) size=" << scan_result.size() << std::endl;
  }

  // op 9570: put
  db->Put(write_options, "key10238", "value116876");

  // op 9571: put
  db->Put(write_options, "key9257", "value39845");

  // op 9572: get
  status = db->Get(read_options, "key5379", &value);
  if (status.ok()) {
    out << "GET key5379 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9573: put
  db->Put(write_options, "key0512", "value7917");

  // op 9574: delete
  db->Delete(write_options, "key7220");

  // op 9575: delete_range
  status = db->DeleteRange(write_options, "key0451", "key6166");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 9576: put
  db->Put(write_options, "key1197", "value80568");

  // op 9577: put
  db->Put(write_options, "key8967", "value66973");

  // op 9578: put
  db->Put(write_options, "key0220", "value100705");

  // op 9579: delete_range
  status = db->DeleteRange(write_options, "key2284", "key11643");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 9580: put
  db->Put(write_options, "key10644", "value33355");

  // op 9581: get
  status = db->Get(read_options, "key9961", &value);
  if (status.ok()) {
    out << "GET key9961 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9582: delete_range
  status = db->DeleteRange(write_options, "key3653", "key5886");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 9583: put
  db->Put(write_options, "key7769", "value23941");

  // op 9584: get
  status = db->Get(read_options, "key8400", &value);
  if (status.ok()) {
    out << "GET key8400 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9585: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 9586: get
  status = db->Get(read_options, "key4398", &value);
  if (status.ok()) {
    out << "GET key4398 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9587: delete
  db->Delete(write_options, "key9729");

  // op 9588: put
  db->Put(write_options, "key0577", "value8163");

  // op 9589: get
  status = db->Get(read_options, "key1129", &value);
  if (status.ok()) {
    out << "GET key1129 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9590: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 9591: delete
  db->Delete(write_options, "key5543");

  // op 9592: put
  db->Put(write_options, "key7301", "value81204");

  // op 9593: delete
  db->Delete(write_options, "key3359");

  // op 9594: put
  db->Put(write_options, "key1690", "value32275");

  // op 9595: put
  db->Put(write_options, "key2380", "value81300");

  // op 9596: put
  db->Put(write_options, "key9908", "value20654");

  // op 9597: get
  status = db->Get(read_options, "key9019", &value);
  if (status.ok()) {
    out << "GET key9019 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9598: delete
  db->Delete(write_options, "key5631");

  // op 9599: put
  db->Put(write_options, "key4592", "value42945");

  // op 9600: put
  db->Put(write_options, "key3224", "value69596");

  // op 9601: put
  db->Put(write_options, "key4435", "value38705");

  // op 9602: put
  db->Put(write_options, "key8442", "value1518");

  // op 9603: delete
  db->Delete(write_options, "key5529");

  // op 9604: delete_range
  status = db->DeleteRange(write_options, "key5055", "key11699");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 9605: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5241", "key8289", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5241, key8289) size=" << scan_result.size() << std::endl;
  }

  // op 9606: delete_range
  status = db->DeleteRange(write_options, "key1508", "key11373");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 9607: put
  db->Put(write_options, "key1220", "value27587");

  // op 9608: get
  status = db->Get(read_options, "key10959", &value);
  if (status.ok()) {
    out << "GET key10959 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9609: put
  db->Put(write_options, "key3222", "value3265");

  // op 9610: put
  db->Put(write_options, "key8063", "value47015");

  // op 9611: put
  db->Put(write_options, "key5747", "value16535");

  // op 9612: put
  db->Put(write_options, "key1079", "value55325");

  // op 9613: delete
  db->Delete(write_options, "key8957");

  // op 9614: put
  db->Put(write_options, "key8697", "value81957");

  // op 9615: delete
  db->Delete(write_options, "key0481");

  // op 9616: get
  status = db->Get(read_options, "key11170", &value);
  if (status.ok()) {
    out << "GET key11170 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9617: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7686", "key11132", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7686, key11132) size=" << scan_result.size() << std::endl;
  }

  // op 9618: get
  status = db->Get(read_options, "key9758", &value);
  if (status.ok()) {
    out << "GET key9758 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9619: delete
  db->Delete(write_options, "key2793");

  // op 9620: put
  db->Put(write_options, "key9785", "value50110");

  // op 9621: delete_range
  status = db->DeleteRange(write_options, "key7016", "key11447");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 9622: put
  db->Put(write_options, "key3099", "value70571");

  // op 9623: delete_range
  status = db->DeleteRange(write_options, "key5094", "key7201");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 9624: put
  db->Put(write_options, "key9554", "value113352");

  // op 9625: get
  status = db->Get(read_options, "key9659", &value);
  if (status.ok()) {
    out << "GET key9659 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9626: get
  status = db->Get(read_options, "key6535", &value);
  if (status.ok()) {
    out << "GET key6535 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9627: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3776", "key6729", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3776, key6729) size=" << scan_result.size() << std::endl;
  }

  // op 9628: put
  db->Put(write_options, "key8602", "value12129");

  // op 9629: put
  db->Put(write_options, "key6602", "value59468");

  // op 9630: put
  db->Put(write_options, "key10042", "value67389");

  // op 9631: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4645", "key10141", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4645, key10141) size=" << scan_result.size() << std::endl;
  }

  // op 9632: put
  db->Put(write_options, "key7922", "value96335");

  // op 9633: put
  db->Put(write_options, "key1205", "value20388");

  // op 9634: get
  status = db->Get(read_options, "key7556", &value);
  if (status.ok()) {
    out << "GET key7556 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9635: put
  db->Put(write_options, "key5433", "value113540");

  // op 9636: delete
  db->Delete(write_options, "key5853");

  // op 9637: delete
  db->Delete(write_options, "key2181");

  // op 9638: put
  db->Put(write_options, "key1585", "value20325");

  // op 9639: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 9640: put
  db->Put(write_options, "key10909", "value24232");

  // op 9641: delete_range
  status = db->DeleteRange(write_options, "key9138", "key11502");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 9642: delete_range
  status = db->DeleteRange(write_options, "key0768", "key10938");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 9643: get
  status = db->Get(read_options, "key10561", &value);
  if (status.ok()) {
    out << "GET key10561 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9644: delete_range
  status = db->DeleteRange(write_options, "key9489", "key10422");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 9645: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3089", "key10688", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3089, key10688) size=" << scan_result.size() << std::endl;
  }

  // op 9646: get
  status = db->Get(read_options, "key5566", &value);
  if (status.ok()) {
    out << "GET key5566 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9647: delete
  db->Delete(write_options, "key5735");

  // op 9648: get
  status = db->Get(read_options, "key11477", &value);
  if (status.ok()) {
    out << "GET key11477 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9649: get
  status = db->Get(read_options, "key10544", &value);
  if (status.ok()) {
    out << "GET key10544 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9650: put
  db->Put(write_options, "key0858", "value85767");

  // op 9651: delete_range
  status = db->DeleteRange(write_options, "key0752", "key4114");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 9652: get
  status = db->Get(read_options, "key9015", &value);
  if (status.ok()) {
    out << "GET key9015 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9653: put
  db->Put(write_options, "key0524", "value99182");

  // op 9654: put
  db->Put(write_options, "key2678", "value61802");

  // op 9655: put
  db->Put(write_options, "key0022", "value60002");

  // op 9656: put
  db->Put(write_options, "key10054", "value100759");

  // op 9657: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2803", "key2916", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2803, key2916) size=" << scan_result.size() << std::endl;
  }

  // op 9658: put
  db->Put(write_options, "key11263", "value81910");

  // op 9659: delete_range
  status = db->DeleteRange(write_options, "key4151", "key9237");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 9660: put
  db->Put(write_options, "key3069", "value12536");

  // op 9661: put
  db->Put(write_options, "key6200", "value95917");

  // op 9662: put
  db->Put(write_options, "key2042", "value115994");

  // op 9663: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 9664: put
  db->Put(write_options, "key8186", "value39888");

  // op 9665: put
  db->Put(write_options, "key2469", "value89542");

  // op 9666: put
  db->Put(write_options, "key1084", "value59193");

  // op 9667: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8711", "key9451", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8711, key9451) size=" << scan_result.size() << std::endl;
  }

  // op 9668: put
  db->Put(write_options, "key4593", "value6809");

  // op 9669: put
  db->Put(write_options, "key1279", "value7961");

  // op 9670: get
  status = db->Get(read_options, "key0720", &value);
  if (status.ok()) {
    out << "GET key0720 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9671: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 9672: put
  db->Put(write_options, "key9579", "value1943");

  // op 9673: delete
  db->Delete(write_options, "key1416");

  // op 9674: put
  db->Put(write_options, "key10159", "value64112");

  // op 9675: get
  status = db->Get(read_options, "key8020", &value);
  if (status.ok()) {
    out << "GET key8020 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9676: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 9677: put
  db->Put(write_options, "key7538", "value16272");

  // op 9678: put
  db->Put(write_options, "key6602", "value103865");

  // op 9679: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 9680: put
  db->Put(write_options, "key1902", "value104987");

  // op 9681: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4742", "key7559", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4742, key7559) size=" << scan_result.size() << std::endl;
  }

  // op 9682: get
  status = db->Get(read_options, "key7758", &value);
  if (status.ok()) {
    out << "GET key7758 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9683: get
  status = db->Get(read_options, "key4939", &value);
  if (status.ok()) {
    out << "GET key4939 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9684: put
  db->Put(write_options, "key2013", "value67723");

  // op 9685: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1266", "key4983", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1266, key4983) size=" << scan_result.size() << std::endl;
  }

  // op 9686: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6548", "key7637", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6548, key7637) size=" << scan_result.size() << std::endl;
  }

  // op 9687: put
  db->Put(write_options, "key8866", "value67852");

  // op 9688: get
  status = db->Get(read_options, "key10031", &value);
  if (status.ok()) {
    out << "GET key10031 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9689: get
  status = db->Get(read_options, "key0369", &value);
  if (status.ok()) {
    out << "GET key0369 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9690: delete_range
  status = db->DeleteRange(write_options, "key6467", "key9413");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 9691: delete
  db->Delete(write_options, "key5407");

  // op 9692: put
  db->Put(write_options, "key6776", "value108793");

  // op 9693: put
  db->Put(write_options, "key6254", "value89877");

  // op 9694: delete_range
  status = db->DeleteRange(write_options, "key10528", "key10918");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 9695: delete
  db->Delete(write_options, "key7233");

  // op 9696: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1005", "key1466", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1005, key1466) size=" << scan_result.size() << std::endl;
  }

  // op 9697: get
  status = db->Get(read_options, "key3875", &value);
  if (status.ok()) {
    out << "GET key3875 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9698: delete_range
  status = db->DeleteRange(write_options, "key3477", "key11696");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 9699: delete
  db->Delete(write_options, "key9752");

  // op 9700: put
  db->Put(write_options, "key0545", "value64693");

  // op 9701: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2032", "key5117", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2032, key5117) size=" << scan_result.size() << std::endl;
  }

  // op 9702: delete_range
  status = db->DeleteRange(write_options, "key5686", "key11096");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 9703: put
  db->Put(write_options, "key9592", "value53848");

  // op 9704: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5909", "key9716", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5909, key9716) size=" << scan_result.size() << std::endl;
  }

  // op 9705: put
  db->Put(write_options, "key0677", "value100067");

  // op 9706: put
  db->Put(write_options, "key0889", "value90993");

  // op 9707: delete_range
  status = db->DeleteRange(write_options, "key2175", "key9446");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 9708: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 9709: put
  db->Put(write_options, "key4340", "value41360");

  // op 9710: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 9711: put
  db->Put(write_options, "key0113", "value5670");

  // op 9712: delete
  db->Delete(write_options, "key8916");

  // op 9713: put
  db->Put(write_options, "key5843", "value105151");

  // op 9714: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8656", "key10744", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8656, key10744) size=" << scan_result.size() << std::endl;
  }

  // op 9715: put
  db->Put(write_options, "key4814", "value70690");

  // op 9716: put
  db->Put(write_options, "key6084", "value78225");

  // op 9717: get
  status = db->Get(read_options, "key0740", &value);
  if (status.ok()) {
    out << "GET key0740 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9718: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6646", "key10230", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6646, key10230) size=" << scan_result.size() << std::endl;
  }

  // op 9719: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 9720: put
  db->Put(write_options, "key6295", "value86821");

  // op 9721: delete_range
  status = db->DeleteRange(write_options, "key0103", "key8327");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 9722: delete
  db->Delete(write_options, "key10972");

  // op 9723: put
  db->Put(write_options, "key7928", "value100825");

  // op 9724: put
  db->Put(write_options, "key8606", "value80775");

  // op 9725: put
  db->Put(write_options, "key7984", "value20180");

  // op 9726: delete
  db->Delete(write_options, "key5894");

  // op 9727: delete_range
  status = db->DeleteRange(write_options, "key4012", "key8294");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 9728: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5634", "key10303", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5634, key10303) size=" << scan_result.size() << std::endl;
  }

  // op 9729: get
  status = db->Get(read_options, "key8452", &value);
  if (status.ok()) {
    out << "GET key8452 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9730: delete
  db->Delete(write_options, "key6452");

  // op 9731: put
  db->Put(write_options, "key11102", "value79932");

  // op 9732: get
  status = db->Get(read_options, "key8231", &value);
  if (status.ok()) {
    out << "GET key8231 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9733: delete_range
  status = db->DeleteRange(write_options, "key3183", "key8317");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 9734: put
  db->Put(write_options, "key11091", "value58484");

  // op 9735: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1409", "key10047", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1409, key10047) size=" << scan_result.size() << std::endl;
  }

  // op 9736: put
  db->Put(write_options, "key4730", "value117599");

  // op 9737: put
  db->Put(write_options, "key5197", "value114895");

  // op 9738: delete
  db->Delete(write_options, "key4498");

  // op 9739: get
  status = db->Get(read_options, "key3892", &value);
  if (status.ok()) {
    out << "GET key3892 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9740: put
  db->Put(write_options, "key9929", "value53835");

  // op 9741: put
  db->Put(write_options, "key11619", "value9881");

  // op 9742: delete_range
  status = db->DeleteRange(write_options, "key3223", "key10686");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 9743: put
  db->Put(write_options, "key5022", "value74398");

  // op 9744: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 9745: put
  db->Put(write_options, "key10133", "value94025");

  // op 9746: delete_range
  status = db->DeleteRange(write_options, "key2069", "key8162");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 9747: get
  status = db->Get(read_options, "key3565", &value);
  if (status.ok()) {
    out << "GET key3565 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9748: put
  db->Put(write_options, "key10235", "value22622");

  // op 9749: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3370", "key6175", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3370, key6175) size=" << scan_result.size() << std::endl;
  }

  // op 9750: delete
  db->Delete(write_options, "key11264");

  // op 9751: delete
  db->Delete(write_options, "key8233");

  // op 9752: put
  db->Put(write_options, "key10987", "value87599");

  // op 9753: put
  db->Put(write_options, "key0842", "value48766");

  // op 9754: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 9755: put
  db->Put(write_options, "key4327", "value100793");

  // op 9756: delete_range
  status = db->DeleteRange(write_options, "key6743", "key7598");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 9757: put
  db->Put(write_options, "key10158", "value102383");

  // op 9758: delete
  db->Delete(write_options, "key10366");

  // op 9759: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5079", "key11379", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5079, key11379) size=" << scan_result.size() << std::endl;
  }

  // op 9760: delete
  db->Delete(write_options, "key4790");

  // op 9761: delete
  db->Delete(write_options, "key0077");

  // op 9762: put
  db->Put(write_options, "key7853", "value115087");

  // op 9763: get
  status = db->Get(read_options, "key11017", &value);
  if (status.ok()) {
    out << "GET key11017 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9764: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5650", "key11541", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5650, key11541) size=" << scan_result.size() << std::endl;
  }

  // op 9765: put
  db->Put(write_options, "key4304", "value75285");

  // op 9766: put
  db->Put(write_options, "key10365", "value40274");

  // op 9767: get
  status = db->Get(read_options, "key2811", &value);
  if (status.ok()) {
    out << "GET key2811 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9768: put
  db->Put(write_options, "key9186", "value28135");

  // op 9769: put
  db->Put(write_options, "key3207", "value66916");

  // op 9770: put
  db->Put(write_options, "key1501", "value98240");

  // op 9771: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 9772: put
  db->Put(write_options, "key6379", "value107718");

  // op 9773: get
  status = db->Get(read_options, "key3940", &value);
  if (status.ok()) {
    out << "GET key3940 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9774: get
  status = db->Get(read_options, "key9849", &value);
  if (status.ok()) {
    out << "GET key9849 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9775: get
  status = db->Get(read_options, "key0634", &value);
  if (status.ok()) {
    out << "GET key0634 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9776: delete_range
  status = db->DeleteRange(write_options, "key1325", "key8468");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 9777: get
  status = db->Get(read_options, "key1742", &value);
  if (status.ok()) {
    out << "GET key1742 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9778: put
  db->Put(write_options, "key6052", "value3143");

  // op 9779: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5343", "key10853", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5343, key10853) size=" << scan_result.size() << std::endl;
  }

  // op 9780: delete
  db->Delete(write_options, "key0591");

  // op 9781: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2036", "key3145", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2036, key3145) size=" << scan_result.size() << std::endl;
  }

  // op 9782: put
  db->Put(write_options, "key7550", "value50026");

  // op 9783: put
  db->Put(write_options, "key7518", "value109975");

  // op 9784: put
  db->Put(write_options, "key9277", "value81062");

  // op 9785: delete
  db->Delete(write_options, "key1249");

  // op 9786: get
  status = db->Get(read_options, "key11439", &value);
  if (status.ok()) {
    out << "GET key11439 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9787: delete
  db->Delete(write_options, "key3654");

  // op 9788: put
  db->Put(write_options, "key2547", "value76590");

  // op 9789: put
  db->Put(write_options, "key1951", "value76761");

  // op 9790: put
  db->Put(write_options, "key11700", "value8088");

  // op 9791: get
  status = db->Get(read_options, "key5178", &value);
  if (status.ok()) {
    out << "GET key5178 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9792: delete
  db->Delete(write_options, "key6083");

  // op 9793: put
  db->Put(write_options, "key3353", "value117748");

  // op 9794: put
  db->Put(write_options, "key11379", "value15367");

  // op 9795: get
  status = db->Get(read_options, "key0320", &value);
  if (status.ok()) {
    out << "GET key0320 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9796: get
  status = db->Get(read_options, "key7701", &value);
  if (status.ok()) {
    out << "GET key7701 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9797: delete_range
  status = db->DeleteRange(write_options, "key3569", "key8401");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 9798: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 9799: put
  db->Put(write_options, "key3042", "value62860");

  // op 9800: delete
  db->Delete(write_options, "key8128");

  // op 9801: scan
  scan_result.clear();
  status = db->Scan(read_options, "key11279", "key11489", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key11279, key11489) size=" << scan_result.size() << std::endl;
  }

  // op 9802: put
  db->Put(write_options, "key6514", "value81705");

  // op 9803: put
  db->Put(write_options, "key6982", "value74780");

  // op 9804: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10987", "key11134", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10987, key11134) size=" << scan_result.size() << std::endl;
  }

  // op 9805: delete_range
  status = db->DeleteRange(write_options, "key0807", "key7249");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 9806: delete_range
  status = db->DeleteRange(write_options, "key2077", "key9520");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 9807: put
  db->Put(write_options, "key1745", "value103387");

  // op 9808: get
  status = db->Get(read_options, "key5672", &value);
  if (status.ok()) {
    out << "GET key5672 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9809: get
  status = db->Get(read_options, "key5674", &value);
  if (status.ok()) {
    out << "GET key5674 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9810: put
  db->Put(write_options, "key4748", "value17283");

  // op 9811: put
  db->Put(write_options, "key1205", "value47233");

  // op 9812: delete
  db->Delete(write_options, "key4200");

  // op 9813: get
  status = db->Get(read_options, "key1908", &value);
  if (status.ok()) {
    out << "GET key1908 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9814: get
  status = db->Get(read_options, "key7934", &value);
  if (status.ok()) {
    out << "GET key7934 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9815: get
  status = db->Get(read_options, "key7857", &value);
  if (status.ok()) {
    out << "GET key7857 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9816: delete
  db->Delete(write_options, "key4921");

  // op 9817: put
  db->Put(write_options, "key9620", "value20552");

  // op 9818: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3032", "key11267", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3032, key11267) size=" << scan_result.size() << std::endl;
  }

  // op 9819: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 9820: put
  db->Put(write_options, "key9599", "value46602");

  // op 9821: delete
  db->Delete(write_options, "key7022");

  // op 9822: put
  db->Put(write_options, "key5638", "value96787");

  // op 9823: put
  db->Put(write_options, "key8370", "value5643");

  // op 9824: put
  db->Put(write_options, "key5212", "value6820");

  // op 9825: put
  db->Put(write_options, "key3961", "value101765");

  // op 9826: get
  status = db->Get(read_options, "key3064", &value);
  if (status.ok()) {
    out << "GET key3064 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9827: put
  db->Put(write_options, "key7570", "value2632");

  // op 9828: get
  status = db->Get(read_options, "key0231", &value);
  if (status.ok()) {
    out << "GET key0231 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9829: put
  db->Put(write_options, "key10429", "value64325");

  // op 9830: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 9831: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 9832: get
  status = db->Get(read_options, "key6854", &value);
  if (status.ok()) {
    out << "GET key6854 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9833: put
  db->Put(write_options, "key1747", "value26426");

  // op 9834: get
  status = db->Get(read_options, "key8740", &value);
  if (status.ok()) {
    out << "GET key8740 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9835: get
  status = db->Get(read_options, "key9510", &value);
  if (status.ok()) {
    out << "GET key9510 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9836: put
  db->Put(write_options, "key2605", "value28797");

  // op 9837: put
  db->Put(write_options, "key9325", "value110251");

  // op 9838: put
  db->Put(write_options, "key9137", "value20430");

  // op 9839: put
  db->Put(write_options, "key5297", "value103102");

  // op 9840: put
  db->Put(write_options, "key11620", "value77444");

  // op 9841: put
  db->Put(write_options, "key0827", "value34040");

  // op 9842: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 9843: delete
  db->Delete(write_options, "key8603");

  // op 9844: put
  db->Put(write_options, "key0360", "value90458");

  // op 9845: delete_range
  status = db->DeleteRange(write_options, "key2662", "key8139");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 9846: put
  db->Put(write_options, "key9456", "value112116");

  // op 9847: put
  db->Put(write_options, "key9276", "value87133");

  // op 9848: delete_range
  status = db->DeleteRange(write_options, "key1612", "key8250");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 9849: put
  db->Put(write_options, "key10519", "value99013");

  // op 9850: put
  db->Put(write_options, "key4287", "value40581");

  // op 9851: delete_range
  status = db->DeleteRange(write_options, "key9754", "key10442");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 9852: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0290", "key6337", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0290, key6337) size=" << scan_result.size() << std::endl;
  }

  // op 9853: get
  status = db->Get(read_options, "key0782", &value);
  if (status.ok()) {
    out << "GET key0782 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9854: delete_range
  status = db->DeleteRange(write_options, "key6350", "key8971");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 9855: put
  db->Put(write_options, "key0299", "value59705");

  // op 9856: put
  db->Put(write_options, "key8720", "value76859");

  // op 9857: get
  status = db->Get(read_options, "key5430", &value);
  if (status.ok()) {
    out << "GET key5430 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9858: delete
  db->Delete(write_options, "key6539");

  // op 9859: put
  db->Put(write_options, "key11120", "value100065");

  // op 9860: delete_range
  status = db->DeleteRange(write_options, "key6336", "key7344");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 9861: put
  db->Put(write_options, "key0651", "value72793");

  // op 9862: put
  db->Put(write_options, "key4343", "value86788");

  // op 9863: put
  db->Put(write_options, "key11717", "value24673");

  // op 9864: get
  status = db->Get(read_options, "key4153", &value);
  if (status.ok()) {
    out << "GET key4153 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9865: put
  db->Put(write_options, "key3288", "value14659");

  // op 9866: delete
  db->Delete(write_options, "key10226");

  // op 9867: delete
  db->Delete(write_options, "key11680");

  // op 9868: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4008", "key8085", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4008, key8085) size=" << scan_result.size() << std::endl;
  }

  // op 9869: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0575", "key8475", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0575, key8475) size=" << scan_result.size() << std::endl;
  }

  // op 9870: put
  db->Put(write_options, "key0692", "value61099");

  // op 9871: delete
  db->Delete(write_options, "key5412");

  // op 9872: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2576", "key8892", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2576, key8892) size=" << scan_result.size() << std::endl;
  }

  // op 9873: put
  db->Put(write_options, "key0259", "value9680");

  // op 9874: delete_range
  status = db->DeleteRange(write_options, "key0935", "key9729");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 9875: delete
  db->Delete(write_options, "key0955");

  // op 9876: delete_range
  status = db->DeleteRange(write_options, "key1601", "key5843");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 9877: get
  status = db->Get(read_options, "key5659", &value);
  if (status.ok()) {
    out << "GET key5659 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9878: put
  db->Put(write_options, "key0003", "value19345");

  // op 9879: delete
  db->Delete(write_options, "key10468");

  // op 9880: put
  db->Put(write_options, "key6465", "value58256");

  // op 9881: get
  status = db->Get(read_options, "key1388", &value);
  if (status.ok()) {
    out << "GET key1388 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9882: put
  db->Put(write_options, "key4603", "value33803");

  // op 9883: put
  db->Put(write_options, "key1072", "value49734");

  // op 9884: delete_range
  status = db->DeleteRange(write_options, "key7828", "key8272");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 9885: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 9886: put
  db->Put(write_options, "key8742", "value88029");

  // op 9887: put
  db->Put(write_options, "key6253", "value46544");

  // op 9888: delete
  db->Delete(write_options, "key7550");

  // op 9889: get
  status = db->Get(read_options, "key2988", &value);
  if (status.ok()) {
    out << "GET key2988 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9890: put
  db->Put(write_options, "key0918", "value27963");

  // op 9891: get
  status = db->Get(read_options, "key3034", &value);
  if (status.ok()) {
    out << "GET key3034 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9892: put
  db->Put(write_options, "key5064", "value63212");

  // op 9893: get
  status = db->Get(read_options, "key3362", &value);
  if (status.ok()) {
    out << "GET key3362 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9894: get
  status = db->Get(read_options, "key5149", &value);
  if (status.ok()) {
    out << "GET key5149 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9895: delete
  db->Delete(write_options, "key3839");

  // op 9896: put
  db->Put(write_options, "key10807", "value102523");

  // op 9897: get
  status = db->Get(read_options, "key6670", &value);
  if (status.ok()) {
    out << "GET key6670 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9898: get
  status = db->Get(read_options, "key4646", &value);
  if (status.ok()) {
    out << "GET key4646 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9899: delete
  db->Delete(write_options, "key6984");

  // op 9900: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0669", "key3064", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0669, key3064) size=" << scan_result.size() << std::endl;
  }

  // op 9901: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 9902: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9416", "key9740", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9416, key9740) size=" << scan_result.size() << std::endl;
  }

  // op 9903: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 9904: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2441", "key11001", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2441, key11001) size=" << scan_result.size() << std::endl;
  }

  // op 9905: put
  db->Put(write_options, "key7294", "value74984");

  // op 9906: get
  status = db->Get(read_options, "key0191", &value);
  if (status.ok()) {
    out << "GET key0191 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9907: get
  status = db->Get(read_options, "key7877", &value);
  if (status.ok()) {
    out << "GET key7877 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9908: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0117", "key10072", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0117, key10072) size=" << scan_result.size() << std::endl;
  }

  // op 9909: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0223", "key10568", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0223, key10568) size=" << scan_result.size() << std::endl;
  }

  // op 9910: put
  db->Put(write_options, "key2092", "value96308");

  // op 9911: delete
  db->Delete(write_options, "key9905");

  // op 9912: put
  db->Put(write_options, "key9997", "value85802");

  // op 9913: put
  db->Put(write_options, "key4408", "value31516");

  // op 9914: get
  status = db->Get(read_options, "key7131", &value);
  if (status.ok()) {
    out << "GET key7131 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9915: put
  db->Put(write_options, "key3324", "value59232");

  // op 9916: delete_range
  status = db->DeleteRange(write_options, "key5613", "key9320");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 9917: put
  db->Put(write_options, "key2466", "value90414");

  // op 9918: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 9919: put
  db->Put(write_options, "key4692", "value2460");

  // op 9920: put
  db->Put(write_options, "key11150", "value75297");

  // op 9921: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0779", "key10065", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0779, key10065) size=" << scan_result.size() << std::endl;
  }

  // op 9922: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0457", "key2024", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0457, key2024) size=" << scan_result.size() << std::endl;
  }

  // op 9923: delete_range
  status = db->DeleteRange(write_options, "key2901", "key9504");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 9924: put
  db->Put(write_options, "key3041", "value21075");

  // op 9925: delete_range
  status = db->DeleteRange(write_options, "key0743", "key2807");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 9926: put
  db->Put(write_options, "key5354", "value29269");

  // op 9927: get
  status = db->Get(read_options, "key10237", &value);
  if (status.ok()) {
    out << "GET key10237 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9928: put
  db->Put(write_options, "key11084", "value31969");

  // op 9929: put
  db->Put(write_options, "key9910", "value63898");

  // op 9930: put
  db->Put(write_options, "key5269", "value27158");

  // op 9931: put
  db->Put(write_options, "key4426", "value87830");

  // op 9932: put
  db->Put(write_options, "key10009", "value39072");

  // op 9933: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4941", "key8136", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4941, key8136) size=" << scan_result.size() << std::endl;
  }

  // op 9934: put
  db->Put(write_options, "key6937", "value113017");

  // op 9935: get
  status = db->Get(read_options, "key3109", &value);
  if (status.ok()) {
    out << "GET key3109 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9936: get
  status = db->Get(read_options, "key1375", &value);
  if (status.ok()) {
    out << "GET key1375 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9937: put
  db->Put(write_options, "key6599", "value3967");

  // op 9938: put
  db->Put(write_options, "key10980", "value80643");

  // op 9939: delete_range
  status = db->DeleteRange(write_options, "key2349", "key7804");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 9940: get
  status = db->Get(read_options, "key9772", &value);
  if (status.ok()) {
    out << "GET key9772 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9941: delete_range
  status = db->DeleteRange(write_options, "key1007", "key2607");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 9942: delete_range
  status = db->DeleteRange(write_options, "key3649", "key5886");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 9943: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0216", "key10041", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0216, key10041) size=" << scan_result.size() << std::endl;
  }

  // op 9944: put
  db->Put(write_options, "key7415", "value88185");

  // op 9945: delete
  db->Delete(write_options, "key1362");

  // op 9946: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0727", "key7858", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0727, key7858) size=" << scan_result.size() << std::endl;
  }

  // op 9947: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5867", "key6338", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5867, key6338) size=" << scan_result.size() << std::endl;
  }

  // op 9948: delete_range
  status = db->DeleteRange(write_options, "key2390", "key2601");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 9949: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1016", "key1046", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1016, key1046) size=" << scan_result.size() << std::endl;
  }

  // op 9950: put
  db->Put(write_options, "key2479", "value67508");

  // op 9951: delete_range
  status = db->DeleteRange(write_options, "key2771", "key5000");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 9952: get
  status = db->Get(read_options, "key4982", &value);
  if (status.ok()) {
    out << "GET key4982 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9953: delete_range
  status = db->DeleteRange(write_options, "key7115", "key10183");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 9954: get
  status = db->Get(read_options, "key2301", &value);
  if (status.ok()) {
    out << "GET key2301 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9955: delete_range
  status = db->DeleteRange(write_options, "key6033", "key10253");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 9956: put
  db->Put(write_options, "key9025", "value109586");

  // op 9957: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2543", "key8063", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2543, key8063) size=" << scan_result.size() << std::endl;
  }

  // op 9958: put
  db->Put(write_options, "key1820", "value5564");

  // op 9959: get
  status = db->Get(read_options, "key8728", &value);
  if (status.ok()) {
    out << "GET key8728 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9960: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9602", "key10177", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9602, key10177) size=" << scan_result.size() << std::endl;
  }

  // op 9961: put
  db->Put(write_options, "key2692", "value42789");

  // op 9962: get
  status = db->Get(read_options, "key5712", &value);
  if (status.ok()) {
    out << "GET key5712 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9963: put
  db->Put(write_options, "key10187", "value65547");

  // op 9964: put
  db->Put(write_options, "key2823", "value86313");

  // op 9965: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2108", "key9243", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2108, key9243) size=" << scan_result.size() << std::endl;
  }

  // op 9966: get
  status = db->Get(read_options, "key3608", &value);
  if (status.ok()) {
    out << "GET key3608 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9967: get
  status = db->Get(read_options, "key11586", &value);
  if (status.ok()) {
    out << "GET key11586 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9968: get
  status = db->Get(read_options, "key7129", &value);
  if (status.ok()) {
    out << "GET key7129 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9969: delete_range
  status = db->DeleteRange(write_options, "key6430", "key9496");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 9970: put
  db->Put(write_options, "key6200", "value94236");

  // op 9971: get
  status = db->Get(read_options, "key7013", &value);
  if (status.ok()) {
    out << "GET key7013 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9972: put
  db->Put(write_options, "key2545", "value69484");

  // op 9973: put
  db->Put(write_options, "key8052", "value108518");

  // op 9974: delete_range
  status = db->DeleteRange(write_options, "key7923", "key9455");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 9975: delete
  db->Delete(write_options, "key8725");

  // op 9976: put
  db->Put(write_options, "key6383", "value78556");

  // op 9977: get
  status = db->Get(read_options, "key3635", &value);
  if (status.ok()) {
    out << "GET key3635 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9978: put
  db->Put(write_options, "key2241", "value113875");

  // op 9979: put
  db->Put(write_options, "key7246", "value35192");

  // op 9980: get
  status = db->Get(read_options, "key8892", &value);
  if (status.ok()) {
    out << "GET key8892 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9981: put
  db->Put(write_options, "key3502", "value72069");

  // op 9982: put
  db->Put(write_options, "key6660", "value69138");

  // op 9983: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2063", "key6980", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2063, key6980) size=" << scan_result.size() << std::endl;
  }

  // op 9984: put
  db->Put(write_options, "key10987", "value48044");

  // op 9985: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6020", "key9634", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6020, key9634) size=" << scan_result.size() << std::endl;
  }

  // op 9986: put
  db->Put(write_options, "key6282", "value44101");

  // op 9987: delete
  db->Delete(write_options, "key3705");

  // op 9988: delete
  db->Delete(write_options, "key8192");

  // op 9989: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 9990: put
  db->Put(write_options, "key5545", "value112048");

  // op 9991: put
  db->Put(write_options, "key10851", "value62786");

  // op 9992: get
  status = db->Get(read_options, "key5146", &value);
  if (status.ok()) {
    out << "GET key5146 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 9993: delete
  db->Delete(write_options, "key10004");

  // op 9994: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 9995: put
  db->Put(write_options, "key2698", "value95360");

  // op 9996: put
  db->Put(write_options, "key4743", "value103234");

  // op 9997: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6362", "key10366", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6362, key10366) size=" << scan_result.size() << std::endl;
  }

  // op 9998: delete_range
  status = db->DeleteRange(write_options, "key0140", "key6444");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 9999: delete_range
  status = db->DeleteRange(write_options, "key4812", "key5218");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 10000: put
  db->Put(write_options, "key9911", "value25514");

  // op 10001: get
  status = db->Get(read_options, "key5802", &value);
  if (status.ok()) {
    out << "GET key5802 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10002: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 10003: get
  status = db->Get(read_options, "key7077", &value);
  if (status.ok()) {
    out << "GET key7077 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10004: get
  status = db->Get(read_options, "key6732", &value);
  if (status.ok()) {
    out << "GET key6732 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10005: put
  db->Put(write_options, "key8634", "value38733");

  // op 10006: get
  status = db->Get(read_options, "key7788", &value);
  if (status.ok()) {
    out << "GET key7788 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10007: put
  db->Put(write_options, "key8799", "value35792");

  // op 10008: delete
  db->Delete(write_options, "key1947");

  // op 10009: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7526", "key10325", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7526, key10325) size=" << scan_result.size() << std::endl;
  }

  // op 10010: delete_range
  status = db->DeleteRange(write_options, "key1261", "key5192");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 10011: delete
  db->Delete(write_options, "key0123");

  // op 10012: get
  status = db->Get(read_options, "key1625", &value);
  if (status.ok()) {
    out << "GET key1625 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10013: put
  db->Put(write_options, "key6279", "value43333");

  // op 10014: delete_range
  status = db->DeleteRange(write_options, "key1938", "key6460");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 10015: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5863", "key7216", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5863, key7216) size=" << scan_result.size() << std::endl;
  }

  // op 10016: put
  db->Put(write_options, "key4854", "value70854");

  // op 10017: get
  status = db->Get(read_options, "key2572", &value);
  if (status.ok()) {
    out << "GET key2572 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10018: put
  db->Put(write_options, "key8459", "value75847");

  // op 10019: get
  status = db->Get(read_options, "key2409", &value);
  if (status.ok()) {
    out << "GET key2409 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10020: get
  status = db->Get(read_options, "key3890", &value);
  if (status.ok()) {
    out << "GET key3890 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10021: delete_range
  status = db->DeleteRange(write_options, "key8414", "key8528");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 10022: get
  status = db->Get(read_options, "key8646", &value);
  if (status.ok()) {
    out << "GET key8646 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10023: put
  db->Put(write_options, "key7574", "value99667");

  // op 10024: put
  db->Put(write_options, "key5113", "value67112");

  // op 10025: put
  db->Put(write_options, "key3726", "value63009");

  // op 10026: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 10027: get
  status = db->Get(read_options, "key6212", &value);
  if (status.ok()) {
    out << "GET key6212 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10028: put
  db->Put(write_options, "key1101", "value89866");

  // op 10029: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8273", "key10064", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8273, key10064) size=" << scan_result.size() << std::endl;
  }

  // op 10030: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0423", "key9253", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0423, key9253) size=" << scan_result.size() << std::endl;
  }

  // op 10031: put
  db->Put(write_options, "key8421", "value43110");

  // op 10032: delete
  db->Delete(write_options, "key4436");

  // op 10033: delete_range
  status = db->DeleteRange(write_options, "key3933", "key5818");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 10034: put
  db->Put(write_options, "key9117", "value25596");

  // op 10035: put
  db->Put(write_options, "key6044", "value109303");

  // op 10036: delete_range
  status = db->DeleteRange(write_options, "key2384", "key5362");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 10037: delete_range
  status = db->DeleteRange(write_options, "key2357", "key5578");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 10038: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 10039: get
  status = db->Get(read_options, "key6594", &value);
  if (status.ok()) {
    out << "GET key6594 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10040: get
  status = db->Get(read_options, "key5627", &value);
  if (status.ok()) {
    out << "GET key5627 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10041: get
  status = db->Get(read_options, "key0172", &value);
  if (status.ok()) {
    out << "GET key0172 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10042: delete
  db->Delete(write_options, "key9506");

  // op 10043: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4449", "key4615", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4449, key4615) size=" << scan_result.size() << std::endl;
  }

  // op 10044: put
  db->Put(write_options, "key11360", "value12947");

  // op 10045: put
  db->Put(write_options, "key4661", "value10306");

  // op 10046: delete_range
  status = db->DeleteRange(write_options, "key0886", "key3442");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 10047: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7054", "key11136", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7054, key11136) size=" << scan_result.size() << std::endl;
  }

  // op 10048: put
  db->Put(write_options, "key6187", "value77227");

  // op 10049: delete
  db->Delete(write_options, "key1891");

  // op 10050: put
  db->Put(write_options, "key8134", "value110466");

  // op 10051: put
  db->Put(write_options, "key0761", "value96647");

  // op 10052: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2002", "key7050", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2002, key7050) size=" << scan_result.size() << std::endl;
  }

  // op 10053: get
  status = db->Get(read_options, "key0083", &value);
  if (status.ok()) {
    out << "GET key0083 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10054: delete_range
  status = db->DeleteRange(write_options, "key1875", "key5239");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 10055: put
  db->Put(write_options, "key7954", "value23775");

  // op 10056: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1747", "key2492", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1747, key2492) size=" << scan_result.size() << std::endl;
  }

  // op 10057: get
  status = db->Get(read_options, "key6003", &value);
  if (status.ok()) {
    out << "GET key6003 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10058: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2623", "key7822", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2623, key7822) size=" << scan_result.size() << std::endl;
  }

  // op 10059: put
  db->Put(write_options, "key6294", "value17502");

  // op 10060: put
  db->Put(write_options, "key10512", "value27196");

  // op 10061: get
  status = db->Get(read_options, "key5189", &value);
  if (status.ok()) {
    out << "GET key5189 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10062: put
  db->Put(write_options, "key7491", "value100028");

  // op 10063: put
  db->Put(write_options, "key0228", "value40530");

  // op 10064: delete_range
  status = db->DeleteRange(write_options, "key0063", "key9408");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 10065: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2371", "key3207", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2371, key3207) size=" << scan_result.size() << std::endl;
  }

  // op 10066: put
  db->Put(write_options, "key7625", "value39784");

  // op 10067: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6688", "key8825", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6688, key8825) size=" << scan_result.size() << std::endl;
  }

  // op 10068: delete
  db->Delete(write_options, "key10508");

  // op 10069: put
  db->Put(write_options, "key7811", "value99348");

  // op 10070: get
  status = db->Get(read_options, "key5929", &value);
  if (status.ok()) {
    out << "GET key5929 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10071: put
  db->Put(write_options, "key8575", "value98442");

  // op 10072: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4038", "key7159", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4038, key7159) size=" << scan_result.size() << std::endl;
  }

  // op 10073: get
  status = db->Get(read_options, "key0545", &value);
  if (status.ok()) {
    out << "GET key0545 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10074: delete_range
  status = db->DeleteRange(write_options, "key2168", "key3224");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 10075: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2309", "key6472", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2309, key6472) size=" << scan_result.size() << std::endl;
  }

  // op 10076: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9171", "key11116", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9171, key11116) size=" << scan_result.size() << std::endl;
  }

  // op 10077: put
  db->Put(write_options, "key6907", "value50551");

  // op 10078: get
  status = db->Get(read_options, "key4601", &value);
  if (status.ok()) {
    out << "GET key4601 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10079: get
  status = db->Get(read_options, "key8181", &value);
  if (status.ok()) {
    out << "GET key8181 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10080: get
  status = db->Get(read_options, "key8272", &value);
  if (status.ok()) {
    out << "GET key8272 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10081: put
  db->Put(write_options, "key10115", "value61203");

  // op 10082: put
  db->Put(write_options, "key2309", "value71986");

  // op 10083: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 10084: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 10085: get
  status = db->Get(read_options, "key8345", &value);
  if (status.ok()) {
    out << "GET key8345 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10086: delete
  db->Delete(write_options, "key6435");

  // op 10087: delete
  db->Delete(write_options, "key10339");

  // op 10088: get
  status = db->Get(read_options, "key11252", &value);
  if (status.ok()) {
    out << "GET key11252 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10089: delete
  db->Delete(write_options, "key11221");

  // op 10090: delete_range
  status = db->DeleteRange(write_options, "key2168", "key3349");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 10091: put
  db->Put(write_options, "key8876", "value40724");

  // op 10092: delete
  db->Delete(write_options, "key9609");

  // op 10093: put
  db->Put(write_options, "key6848", "value117713");

  // op 10094: get
  status = db->Get(read_options, "key11345", &value);
  if (status.ok()) {
    out << "GET key11345 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10095: delete
  db->Delete(write_options, "key2594");

  // op 10096: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 10097: put
  db->Put(write_options, "key5642", "value86150");

  // op 10098: put
  db->Put(write_options, "key10616", "value77031");

  // op 10099: put
  db->Put(write_options, "key5227", "value11688");

  // op 10100: put
  db->Put(write_options, "key6401", "value90508");

  // op 10101: delete_range
  status = db->DeleteRange(write_options, "key5178", "key8241");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 10102: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3411", "key7666", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3411, key7666) size=" << scan_result.size() << std::endl;
  }

  // op 10103: put
  db->Put(write_options, "key5203", "value104688");

  // op 10104: get
  status = db->Get(read_options, "key0272", &value);
  if (status.ok()) {
    out << "GET key0272 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10105: put
  db->Put(write_options, "key6006", "value97077");

  // op 10106: put
  db->Put(write_options, "key7632", "value117794");

  // op 10107: put
  db->Put(write_options, "key6486", "value89058");

  // op 10108: put
  db->Put(write_options, "key10398", "value114554");

  // op 10109: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2354", "key9630", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2354, key9630) size=" << scan_result.size() << std::endl;
  }

  // op 10110: put
  db->Put(write_options, "key4480", "value37257");

  // op 10111: get
  status = db->Get(read_options, "key8805", &value);
  if (status.ok()) {
    out << "GET key8805 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10112: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6368", "key8857", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6368, key8857) size=" << scan_result.size() << std::endl;
  }

  // op 10113: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8470", "key9366", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8470, key9366) size=" << scan_result.size() << std::endl;
  }

  // op 10114: delete_range
  status = db->DeleteRange(write_options, "key4873", "key5586");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 10115: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9913", "key10637", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9913, key10637) size=" << scan_result.size() << std::endl;
  }

  // op 10116: put
  db->Put(write_options, "key11248", "value0056");

  // op 10117: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 10118: put
  db->Put(write_options, "key0119", "value26334");

  // op 10119: put
  db->Put(write_options, "key7833", "value55577");

  // op 10120: put
  db->Put(write_options, "key9095", "value51439");

  // op 10121: put
  db->Put(write_options, "key2923", "value38917");

  // op 10122: put
  db->Put(write_options, "key1974", "value13661");

  // op 10123: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6799", "key11057", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6799, key11057) size=" << scan_result.size() << std::endl;
  }

  // op 10124: put
  db->Put(write_options, "key9595", "value11809");

  // op 10125: put
  db->Put(write_options, "key1863", "value111092");

  // op 10126: get
  status = db->Get(read_options, "key9132", &value);
  if (status.ok()) {
    out << "GET key9132 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10127: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0003", "key5904", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0003, key5904) size=" << scan_result.size() << std::endl;
  }

  // op 10128: delete
  db->Delete(write_options, "key3134");

  // op 10129: get
  status = db->Get(read_options, "key9276", &value);
  if (status.ok()) {
    out << "GET key9276 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10130: put
  db->Put(write_options, "key9737", "value44718");

  // op 10131: delete_range
  status = db->DeleteRange(write_options, "key9055", "key10955");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 10132: put
  db->Put(write_options, "key10876", "value62809");

  // op 10133: put
  db->Put(write_options, "key8971", "value20181");

  // op 10134: delete_range
  status = db->DeleteRange(write_options, "key7486", "key9503");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 10135: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 10136: put
  db->Put(write_options, "key11441", "value56698");

  // op 10137: put
  db->Put(write_options, "key7936", "value26547");

  // op 10138: get
  status = db->Get(read_options, "key9991", &value);
  if (status.ok()) {
    out << "GET key9991 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10139: delete_range
  status = db->DeleteRange(write_options, "key1887", "key7438");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 10140: put
  db->Put(write_options, "key6470", "value18250");

  // op 10141: put
  db->Put(write_options, "key0788", "value93837");

  // op 10142: get
  status = db->Get(read_options, "key10811", &value);
  if (status.ok()) {
    out << "GET key10811 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10143: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1067", "key10792", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1067, key10792) size=" << scan_result.size() << std::endl;
  }

  // op 10144: get
  status = db->Get(read_options, "key0808", &value);
  if (status.ok()) {
    out << "GET key0808 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10145: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6148", "key8591", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6148, key8591) size=" << scan_result.size() << std::endl;
  }

  // op 10146: delete_range
  status = db->DeleteRange(write_options, "key6699", "key8567");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 10147: get
  status = db->Get(read_options, "key10057", &value);
  if (status.ok()) {
    out << "GET key10057 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10148: delete
  db->Delete(write_options, "key0111");

  // op 10149: put
  db->Put(write_options, "key3806", "value105777");

  // op 10150: delete
  db->Delete(write_options, "key8493");

  // op 10151: get
  status = db->Get(read_options, "key6534", &value);
  if (status.ok()) {
    out << "GET key6534 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10152: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 10153: get
  status = db->Get(read_options, "key8288", &value);
  if (status.ok()) {
    out << "GET key8288 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10154: get
  status = db->Get(read_options, "key1891", &value);
  if (status.ok()) {
    out << "GET key1891 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10155: put
  db->Put(write_options, "key7906", "value89741");

  // op 10156: delete_range
  status = db->DeleteRange(write_options, "key3194", "key5265");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 10157: put
  db->Put(write_options, "key1497", "value3272");

  // op 10158: get
  status = db->Get(read_options, "key8659", &value);
  if (status.ok()) {
    out << "GET key8659 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10159: put
  db->Put(write_options, "key8714", "value5586");

  // op 10160: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 10161: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 10162: get
  status = db->Get(read_options, "key8516", &value);
  if (status.ok()) {
    out << "GET key8516 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10163: put
  db->Put(write_options, "key9299", "value114381");

  // op 10164: delete_range
  status = db->DeleteRange(write_options, "key0302", "key7138");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 10165: delete
  db->Delete(write_options, "key2453");

  // op 10166: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2986", "key10565", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2986, key10565) size=" << scan_result.size() << std::endl;
  }

  // op 10167: get
  status = db->Get(read_options, "key5890", &value);
  if (status.ok()) {
    out << "GET key5890 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10168: put
  db->Put(write_options, "key7580", "value7031");

  // op 10169: delete_range
  status = db->DeleteRange(write_options, "key11198", "key11289");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 10170: delete
  db->Delete(write_options, "key3627");

  // op 10171: put
  db->Put(write_options, "key10748", "value4682");

  // op 10172: put
  db->Put(write_options, "key5215", "value53628");

  // op 10173: get
  status = db->Get(read_options, "key2390", &value);
  if (status.ok()) {
    out << "GET key2390 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10174: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3205", "key8316", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3205, key8316) size=" << scan_result.size() << std::endl;
  }

  // op 10175: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 10176: put
  db->Put(write_options, "key0841", "value61829");

  // op 10177: put
  db->Put(write_options, "key11466", "value80751");

  // op 10178: put
  db->Put(write_options, "key5817", "value92484");

  // op 10179: put
  db->Put(write_options, "key0617", "value61421");

  // op 10180: put
  db->Put(write_options, "key10117", "value17888");

  // op 10181: put
  db->Put(write_options, "key10087", "value97669");

  // op 10182: get
  status = db->Get(read_options, "key8657", &value);
  if (status.ok()) {
    out << "GET key8657 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10183: put
  db->Put(write_options, "key11319", "value76863");

  // op 10184: put
  db->Put(write_options, "key5671", "value38945");

  // op 10185: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2668", "key8925", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2668, key8925) size=" << scan_result.size() << std::endl;
  }

  // op 10186: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5949", "key8187", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5949, key8187) size=" << scan_result.size() << std::endl;
  }

  // op 10187: put
  db->Put(write_options, "key8981", "value31392");

  // op 10188: delete_range
  status = db->DeleteRange(write_options, "key2027", "key4283");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 10189: delete_range
  status = db->DeleteRange(write_options, "key3445", "key9338");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 10190: put
  db->Put(write_options, "key9794", "value19636");

  // op 10191: put
  db->Put(write_options, "key6490", "value11858");

  // op 10192: scan
  scan_result.clear();
  status = db->Scan(read_options, "key11280", "key11313", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key11280, key11313) size=" << scan_result.size() << std::endl;
  }

  // op 10193: put
  db->Put(write_options, "key3141", "value98266");

  // op 10194: delete_range
  status = db->DeleteRange(write_options, "key8865", "key11647");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 10195: delete_range
  status = db->DeleteRange(write_options, "key7472", "key9187");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 10196: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 10197: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8950", "key10106", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8950, key10106) size=" << scan_result.size() << std::endl;
  }

  // op 10198: put
  db->Put(write_options, "key4475", "value24591");

  // op 10199: put
  db->Put(write_options, "key7484", "value75127");

  // op 10200: delete
  db->Delete(write_options, "key1670");

  // op 10201: put
  db->Put(write_options, "key9420", "value89527");

  // op 10202: delete_range
  status = db->DeleteRange(write_options, "key10594", "key10889");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 10203: get
  status = db->Get(read_options, "key3256", &value);
  if (status.ok()) {
    out << "GET key3256 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10204: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5966", "key10341", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5966, key10341) size=" << scan_result.size() << std::endl;
  }

  // op 10205: put
  db->Put(write_options, "key8737", "value76426");

  // op 10206: delete
  db->Delete(write_options, "key11458");

  // op 10207: get
  status = db->Get(read_options, "key8804", &value);
  if (status.ok()) {
    out << "GET key8804 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10208: delete_range
  status = db->DeleteRange(write_options, "key1713", "key11713");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 10209: put
  db->Put(write_options, "key4158", "value64002");

  // op 10210: delete_range
  status = db->DeleteRange(write_options, "key0168", "key3826");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 10211: put
  db->Put(write_options, "key8216", "value75553");

  // op 10212: put
  db->Put(write_options, "key3717", "value60872");

  // op 10213: put
  db->Put(write_options, "key7797", "value45018");

  // op 10214: put
  db->Put(write_options, "key4491", "value14854");

  // op 10215: delete_range
  status = db->DeleteRange(write_options, "key9625", "key11419");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 10216: delete
  db->Delete(write_options, "key11067");

  // op 10217: delete
  db->Delete(write_options, "key6014");

  // op 10218: delete
  db->Delete(write_options, "key3462");

  // op 10219: put
  db->Put(write_options, "key10603", "value10432");

  // op 10220: put
  db->Put(write_options, "key10759", "value90422");

  // op 10221: put
  db->Put(write_options, "key1285", "value64143");

  // op 10222: put
  db->Put(write_options, "key6062", "value21980");

  // op 10223: delete_range
  status = db->DeleteRange(write_options, "key7511", "key11210");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 10224: get
  status = db->Get(read_options, "key9111", &value);
  if (status.ok()) {
    out << "GET key9111 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10225: put
  db->Put(write_options, "key3100", "value100310");

  // op 10226: get
  status = db->Get(read_options, "key7689", &value);
  if (status.ok()) {
    out << "GET key7689 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10227: put
  db->Put(write_options, "key8486", "value79918");

  // op 10228: delete
  db->Delete(write_options, "key11122");

  // op 10229: get
  status = db->Get(read_options, "key6852", &value);
  if (status.ok()) {
    out << "GET key6852 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10230: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 10231: put
  db->Put(write_options, "key7858", "value48893");

  // op 10232: put
  db->Put(write_options, "key1456", "value99247");

  // op 10233: put
  db->Put(write_options, "key5245", "value55109");

  // op 10234: get
  status = db->Get(read_options, "key4468", &value);
  if (status.ok()) {
    out << "GET key4468 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10235: put
  db->Put(write_options, "key5672", "value38030");

  // op 10236: put
  db->Put(write_options, "key2832", "value4749");

  // op 10237: delete_range
  status = db->DeleteRange(write_options, "key8004", "key9354");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 10238: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2883", "key3461", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2883, key3461) size=" << scan_result.size() << std::endl;
  }

  // op 10239: put
  db->Put(write_options, "key7678", "value96132");

  // op 10240: put
  db->Put(write_options, "key10082", "value113294");

  // op 10241: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 10242: get
  status = db->Get(read_options, "key0630", &value);
  if (status.ok()) {
    out << "GET key0630 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10243: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2113", "key9265", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2113, key9265) size=" << scan_result.size() << std::endl;
  }

  // op 10244: get
  status = db->Get(read_options, "key7702", &value);
  if (status.ok()) {
    out << "GET key7702 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10245: get
  status = db->Get(read_options, "key2782", &value);
  if (status.ok()) {
    out << "GET key2782 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10246: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4398", "key7502", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4398, key7502) size=" << scan_result.size() << std::endl;
  }

  // op 10247: put
  db->Put(write_options, "key10067", "value12113");

  // op 10248: get
  status = db->Get(read_options, "key7115", &value);
  if (status.ok()) {
    out << "GET key7115 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10249: put
  db->Put(write_options, "key0496", "value41572");

  // op 10250: get
  status = db->Get(read_options, "key8284", &value);
  if (status.ok()) {
    out << "GET key8284 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10251: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6244", "key10089", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6244, key10089) size=" << scan_result.size() << std::endl;
  }

  // op 10252: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4362", "key6320", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4362, key6320) size=" << scan_result.size() << std::endl;
  }

  // op 10253: put
  db->Put(write_options, "key8351", "value95659");

  // op 10254: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4658", "key8454", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4658, key8454) size=" << scan_result.size() << std::endl;
  }

  // op 10255: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2476", "key8380", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2476, key8380) size=" << scan_result.size() << std::endl;
  }

  // op 10256: put
  db->Put(write_options, "key6909", "value64076");

  // op 10257: delete
  db->Delete(write_options, "key5446");

  // op 10258: get
  status = db->Get(read_options, "key11084", &value);
  if (status.ok()) {
    out << "GET key11084 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10259: put
  db->Put(write_options, "key0010", "value112842");

  // op 10260: delete
  db->Delete(write_options, "key7366");

  // op 10261: delete_range
  status = db->DeleteRange(write_options, "key1761", "key6584");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 10262: delete
  db->Delete(write_options, "key0043");

  // op 10263: put
  db->Put(write_options, "key10280", "value0959");

  // op 10264: get
  status = db->Get(read_options, "key2548", &value);
  if (status.ok()) {
    out << "GET key2548 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10265: delete_range
  status = db->DeleteRange(write_options, "key4138", "key7049");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 10266: get
  status = db->Get(read_options, "key0160", &value);
  if (status.ok()) {
    out << "GET key0160 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10267: put
  db->Put(write_options, "key10782", "value47778");

  // op 10268: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1913", "key2841", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1913, key2841) size=" << scan_result.size() << std::endl;
  }

  // op 10269: get
  status = db->Get(read_options, "key0598", &value);
  if (status.ok()) {
    out << "GET key0598 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10270: put
  db->Put(write_options, "key0810", "value8763");

  // op 10271: delete
  db->Delete(write_options, "key11041");

  // op 10272: put
  db->Put(write_options, "key6673", "value84986");

  // op 10273: get
  status = db->Get(read_options, "key5886", &value);
  if (status.ok()) {
    out << "GET key5886 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10274: get
  status = db->Get(read_options, "key9116", &value);
  if (status.ok()) {
    out << "GET key9116 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10275: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 10276: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 10277: delete_range
  status = db->DeleteRange(write_options, "key2959", "key8348");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 10278: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 10279: delete_range
  status = db->DeleteRange(write_options, "key2353", "key9040");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 10280: put
  db->Put(write_options, "key1989", "value92332");

  // op 10281: put
  db->Put(write_options, "key5434", "value74191");

  // op 10282: put
  db->Put(write_options, "key6526", "value106175");

  // op 10283: get
  status = db->Get(read_options, "key5098", &value);
  if (status.ok()) {
    out << "GET key5098 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10284: put
  db->Put(write_options, "key2608", "value76804");

  // op 10285: put
  db->Put(write_options, "key9577", "value67924");

  // op 10286: put
  db->Put(write_options, "key4391", "value10622");

  // op 10287: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4676", "key9426", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4676, key9426) size=" << scan_result.size() << std::endl;
  }

  // op 10288: put
  db->Put(write_options, "key7570", "value36428");

  // op 10289: put
  db->Put(write_options, "key4640", "value103118");

  // op 10290: put
  db->Put(write_options, "key9284", "value71514");

  // op 10291: put
  db->Put(write_options, "key5956", "value77766");

  // op 10292: put
  db->Put(write_options, "key8263", "value70014");

  // op 10293: put
  db->Put(write_options, "key8832", "value29353");

  // op 10294: get
  status = db->Get(read_options, "key2578", &value);
  if (status.ok()) {
    out << "GET key2578 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10295: get
  status = db->Get(read_options, "key5750", &value);
  if (status.ok()) {
    out << "GET key5750 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10296: delete_range
  status = db->DeleteRange(write_options, "key1442", "key4028");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 10297: put
  db->Put(write_options, "key1797", "value1105");

  // op 10298: get
  status = db->Get(read_options, "key8259", &value);
  if (status.ok()) {
    out << "GET key8259 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10299: put
  db->Put(write_options, "key0973", "value23182");

  // op 10300: delete
  db->Delete(write_options, "key11208");

  // op 10301: get
  status = db->Get(read_options, "key8025", &value);
  if (status.ok()) {
    out << "GET key8025 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10302: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1317", "key3681", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1317, key3681) size=" << scan_result.size() << std::endl;
  }

  // op 10303: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 10304: get
  status = db->Get(read_options, "key0647", &value);
  if (status.ok()) {
    out << "GET key0647 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10305: put
  db->Put(write_options, "key10089", "value73368");

  // op 10306: delete_range
  status = db->DeleteRange(write_options, "key0224", "key0955");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 10307: delete
  db->Delete(write_options, "key9688");

  // op 10308: get
  status = db->Get(read_options, "key9664", &value);
  if (status.ok()) {
    out << "GET key9664 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10309: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 10310: put
  db->Put(write_options, "key2986", "value4328");

  // op 10311: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7524", "key9090", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7524, key9090) size=" << scan_result.size() << std::endl;
  }

  // op 10312: put
  db->Put(write_options, "key10559", "value26952");

  // op 10313: put
  db->Put(write_options, "key0173", "value47459");

  // op 10314: delete_range
  status = db->DeleteRange(write_options, "key6670", "key9068");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 10315: get
  status = db->Get(read_options, "key1984", &value);
  if (status.ok()) {
    out << "GET key1984 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10316: delete
  db->Delete(write_options, "key11007");

  // op 10317: put
  db->Put(write_options, "key1836", "value82227");

  // op 10318: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3507", "key11250", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3507, key11250) size=" << scan_result.size() << std::endl;
  }

  // op 10319: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7915", "key10941", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7915, key10941) size=" << scan_result.size() << std::endl;
  }

  // op 10320: put
  db->Put(write_options, "key10706", "value24344");

  // op 10321: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2051", "key9359", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2051, key9359) size=" << scan_result.size() << std::endl;
  }

  // op 10322: put
  db->Put(write_options, "key0439", "value31199");

  // op 10323: put
  db->Put(write_options, "key3713", "value115648");

  // op 10324: delete_range
  status = db->DeleteRange(write_options, "key5394", "key5548");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 10325: get
  status = db->Get(read_options, "key6090", &value);
  if (status.ok()) {
    out << "GET key6090 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10326: put
  db->Put(write_options, "key6319", "value78783");

  // op 10327: put
  db->Put(write_options, "key4197", "value23702");

  // op 10328: delete_range
  status = db->DeleteRange(write_options, "key4042", "key4766");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 10329: put
  db->Put(write_options, "key7249", "value102045");

  // op 10330: get
  status = db->Get(read_options, "key1213", &value);
  if (status.ok()) {
    out << "GET key1213 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10331: delete
  db->Delete(write_options, "key5754");

  // op 10332: delete
  db->Delete(write_options, "key8169");

  // op 10333: put
  db->Put(write_options, "key11611", "value103084");

  // op 10334: put
  db->Put(write_options, "key9130", "value34147");

  // op 10335: delete
  db->Delete(write_options, "key5375");

  // op 10336: put
  db->Put(write_options, "key7659", "value75415");

  // op 10337: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4285", "key10736", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4285, key10736) size=" << scan_result.size() << std::endl;
  }

  // op 10338: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 10339: delete_range
  status = db->DeleteRange(write_options, "key2636", "key7551");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 10340: put
  db->Put(write_options, "key3667", "value54499");

  // op 10341: put
  db->Put(write_options, "key5705", "value0671");

  // op 10342: put
  db->Put(write_options, "key9294", "value79989");

  // op 10343: put
  db->Put(write_options, "key10468", "value26274");

  // op 10344: get
  status = db->Get(read_options, "key8272", &value);
  if (status.ok()) {
    out << "GET key8272 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10345: put
  db->Put(write_options, "key9793", "value23220");

  // op 10346: put
  db->Put(write_options, "key11718", "value100739");

  // op 10347: delete_range
  status = db->DeleteRange(write_options, "key1623", "key9025");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 10348: get
  status = db->Get(read_options, "key2298", &value);
  if (status.ok()) {
    out << "GET key2298 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10349: put
  db->Put(write_options, "key7709", "value19259");

  // op 10350: delete
  db->Delete(write_options, "key9674");

  // op 10351: put
  db->Put(write_options, "key2191", "value70669");

  // op 10352: put
  db->Put(write_options, "key3671", "value16635");

  // op 10353: delete_range
  status = db->DeleteRange(write_options, "key2674", "key9351");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 10354: get
  status = db->Get(read_options, "key5171", &value);
  if (status.ok()) {
    out << "GET key5171 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10355: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1694", "key7771", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1694, key7771) size=" << scan_result.size() << std::endl;
  }

  // op 10356: get
  status = db->Get(read_options, "key4854", &value);
  if (status.ok()) {
    out << "GET key4854 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10357: get
  status = db->Get(read_options, "key5587", &value);
  if (status.ok()) {
    out << "GET key5587 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10358: put
  db->Put(write_options, "key5169", "value8351");

  // op 10359: delete_range
  status = db->DeleteRange(write_options, "key0295", "key5866");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 10360: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1002", "key4692", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1002, key4692) size=" << scan_result.size() << std::endl;
  }

  // op 10361: put
  db->Put(write_options, "key5512", "value45906");

  // op 10362: get
  status = db->Get(read_options, "key2246", &value);
  if (status.ok()) {
    out << "GET key2246 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10363: put
  db->Put(write_options, "key11563", "value87629");

  // op 10364: put
  db->Put(write_options, "key9396", "value65458");

  // op 10365: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3221", "key7694", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3221, key7694) size=" << scan_result.size() << std::endl;
  }

  // op 10366: put
  db->Put(write_options, "key10992", "value27418");

  // op 10367: delete_range
  status = db->DeleteRange(write_options, "key8980", "key11194");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 10368: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0296", "key10408", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0296, key10408) size=" << scan_result.size() << std::endl;
  }

  // op 10369: put
  db->Put(write_options, "key5949", "value67942");

  // op 10370: delete
  db->Delete(write_options, "key6068");

  // op 10371: delete
  db->Delete(write_options, "key5822");

  // op 10372: put
  db->Put(write_options, "key3105", "value14584");

  // op 10373: get
  status = db->Get(read_options, "key9333", &value);
  if (status.ok()) {
    out << "GET key9333 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10374: put
  db->Put(write_options, "key6091", "value41141");

  // op 10375: get
  status = db->Get(read_options, "key3766", &value);
  if (status.ok()) {
    out << "GET key3766 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10376: get
  status = db->Get(read_options, "key8174", &value);
  if (status.ok()) {
    out << "GET key8174 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10377: put
  db->Put(write_options, "key10511", "value8427");

  // op 10378: put
  db->Put(write_options, "key9674", "value100952");

  // op 10379: get
  status = db->Get(read_options, "key8947", &value);
  if (status.ok()) {
    out << "GET key8947 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10380: delete
  db->Delete(write_options, "key9077");

  // op 10381: delete
  db->Delete(write_options, "key2295");

  // op 10382: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4736", "key9651", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4736, key9651) size=" << scan_result.size() << std::endl;
  }

  // op 10383: put
  db->Put(write_options, "key3151", "value108079");

  // op 10384: get
  status = db->Get(read_options, "key2212", &value);
  if (status.ok()) {
    out << "GET key2212 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10385: delete_range
  status = db->DeleteRange(write_options, "key4822", "key11531");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 10386: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2097", "key7875", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2097, key7875) size=" << scan_result.size() << std::endl;
  }

  // op 10387: delete
  db->Delete(write_options, "key0585");

  // op 10388: get
  status = db->Get(read_options, "key10038", &value);
  if (status.ok()) {
    out << "GET key10038 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10389: get
  status = db->Get(read_options, "key6894", &value);
  if (status.ok()) {
    out << "GET key6894 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10390: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2933", "key8822", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2933, key8822) size=" << scan_result.size() << std::endl;
  }

  // op 10391: delete
  db->Delete(write_options, "key5261");

  // op 10392: put
  db->Put(write_options, "key8689", "value22999");

  // op 10393: get
  status = db->Get(read_options, "key8689", &value);
  if (status.ok()) {
    out << "GET key8689 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10394: put
  db->Put(write_options, "key2935", "value3128");

  // op 10395: get
  status = db->Get(read_options, "key3411", &value);
  if (status.ok()) {
    out << "GET key3411 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10396: get
  status = db->Get(read_options, "key4757", &value);
  if (status.ok()) {
    out << "GET key4757 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10397: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 10398: put
  db->Put(write_options, "key1738", "value60326");

  // op 10399: delete
  db->Delete(write_options, "key10157");

  // op 10400: put
  db->Put(write_options, "key6233", "value75962");

  // op 10401: get
  status = db->Get(read_options, "key7334", &value);
  if (status.ok()) {
    out << "GET key7334 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10402: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1589", "key8509", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1589, key8509) size=" << scan_result.size() << std::endl;
  }

  // op 10403: delete_range
  status = db->DeleteRange(write_options, "key1571", "key6132");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 10404: get
  status = db->Get(read_options, "key10698", &value);
  if (status.ok()) {
    out << "GET key10698 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10405: put
  db->Put(write_options, "key6856", "value57006");

  // op 10406: put
  db->Put(write_options, "key2372", "value56469");

  // op 10407: put
  db->Put(write_options, "key0369", "value89917");

  // op 10408: get
  status = db->Get(read_options, "key6556", &value);
  if (status.ok()) {
    out << "GET key6556 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10409: delete_range
  status = db->DeleteRange(write_options, "key0996", "key3543");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 10410: put
  db->Put(write_options, "key4946", "value95881");

  // op 10411: delete
  db->Delete(write_options, "key9561");

  // op 10412: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1083", "key8811", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1083, key8811) size=" << scan_result.size() << std::endl;
  }

  // op 10413: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2126", "key7408", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2126, key7408) size=" << scan_result.size() << std::endl;
  }

  // op 10414: put
  db->Put(write_options, "key0122", "value45087");

  // op 10415: get
  status = db->Get(read_options, "key0846", &value);
  if (status.ok()) {
    out << "GET key0846 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10416: get
  status = db->Get(read_options, "key0386", &value);
  if (status.ok()) {
    out << "GET key0386 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10417: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3281", "key5726", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3281, key5726) size=" << scan_result.size() << std::endl;
  }

  // op 10418: delete_range
  status = db->DeleteRange(write_options, "key3732", "key4415");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 10419: put
  db->Put(write_options, "key6877", "value36505");

  // op 10420: delete
  db->Delete(write_options, "key4444");

  // op 10421: get
  status = db->Get(read_options, "key5793", &value);
  if (status.ok()) {
    out << "GET key5793 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10422: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 10423: delete_range
  status = db->DeleteRange(write_options, "key5880", "key7029");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 10424: put
  db->Put(write_options, "key1563", "value36410");

  // op 10425: put
  db->Put(write_options, "key8549", "value54482");

  // op 10426: put
  db->Put(write_options, "key1102", "value48054");

  // op 10427: delete
  db->Delete(write_options, "key2016");

  // op 10428: delete
  db->Delete(write_options, "key8250");

  // op 10429: put
  db->Put(write_options, "key2672", "value95371");

  // op 10430: get
  status = db->Get(read_options, "key4405", &value);
  if (status.ok()) {
    out << "GET key4405 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10431: put
  db->Put(write_options, "key10508", "value12712");

  // op 10432: get
  status = db->Get(read_options, "key5509", &value);
  if (status.ok()) {
    out << "GET key5509 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10433: put
  db->Put(write_options, "key3674", "value47348");

  // op 10434: put
  db->Put(write_options, "key8560", "value62296");

  // op 10435: get
  status = db->Get(read_options, "key6159", &value);
  if (status.ok()) {
    out << "GET key6159 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10436: delete_range
  status = db->DeleteRange(write_options, "key6867", "key8787");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 10437: put
  db->Put(write_options, "key2218", "value7116");

  // op 10438: put
  db->Put(write_options, "key9598", "value19871");

  // op 10439: put
  db->Put(write_options, "key8850", "value9759");

  // op 10440: get
  status = db->Get(read_options, "key0511", &value);
  if (status.ok()) {
    out << "GET key0511 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10441: put
  db->Put(write_options, "key4107", "value0468");

  // op 10442: get
  status = db->Get(read_options, "key9544", &value);
  if (status.ok()) {
    out << "GET key9544 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10443: get
  status = db->Get(read_options, "key10322", &value);
  if (status.ok()) {
    out << "GET key10322 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10444: delete
  db->Delete(write_options, "key3355");

  // op 10445: delete
  db->Delete(write_options, "key3630");

  // op 10446: put
  db->Put(write_options, "key8220", "value101952");

  // op 10447: get
  status = db->Get(read_options, "key0400", &value);
  if (status.ok()) {
    out << "GET key0400 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10448: delete_range
  status = db->DeleteRange(write_options, "key6995", "key10241");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 10449: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 10450: put
  db->Put(write_options, "key3554", "value81873");

  // op 10451: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0087", "key10262", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0087, key10262) size=" << scan_result.size() << std::endl;
  }

  // op 10452: put
  db->Put(write_options, "key0749", "value45867");

  // op 10453: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7577", "key10745", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7577, key10745) size=" << scan_result.size() << std::endl;
  }

  // op 10454: put
  db->Put(write_options, "key1021", "value84353");

  // op 10455: put
  db->Put(write_options, "key8857", "value64061");

  // op 10456: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4568", "key11465", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4568, key11465) size=" << scan_result.size() << std::endl;
  }

  // op 10457: put
  db->Put(write_options, "key9919", "value51918");

  // op 10458: get
  status = db->Get(read_options, "key6128", &value);
  if (status.ok()) {
    out << "GET key6128 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10459: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 10460: delete
  db->Delete(write_options, "key2956");

  // op 10461: get
  status = db->Get(read_options, "key1335", &value);
  if (status.ok()) {
    out << "GET key1335 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10462: put
  db->Put(write_options, "key5433", "value37754");

  // op 10463: put
  db->Put(write_options, "key0743", "value46170");

  // op 10464: put
  db->Put(write_options, "key1408", "value110961");

  // op 10465: put
  db->Put(write_options, "key3598", "value9458");

  // op 10466: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3892", "key6496", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3892, key6496) size=" << scan_result.size() << std::endl;
  }

  // op 10467: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3730", "key10976", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3730, key10976) size=" << scan_result.size() << std::endl;
  }

  // op 10468: get
  status = db->Get(read_options, "key3916", &value);
  if (status.ok()) {
    out << "GET key3916 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10469: get
  status = db->Get(read_options, "key10994", &value);
  if (status.ok()) {
    out << "GET key10994 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10470: get
  status = db->Get(read_options, "key7746", &value);
  if (status.ok()) {
    out << "GET key7746 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10471: get
  status = db->Get(read_options, "key4242", &value);
  if (status.ok()) {
    out << "GET key4242 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10472: get
  status = db->Get(read_options, "key10824", &value);
  if (status.ok()) {
    out << "GET key10824 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10473: put
  db->Put(write_options, "key2349", "value44630");

  // op 10474: put
  db->Put(write_options, "key10133", "value29678");

  // op 10475: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2596", "key7774", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2596, key7774) size=" << scan_result.size() << std::endl;
  }

  // op 10476: put
  db->Put(write_options, "key10388", "value99355");

  // op 10477: put
  db->Put(write_options, "key10494", "value9392");

  // op 10478: delete
  db->Delete(write_options, "key8678");

  // op 10479: put
  db->Put(write_options, "key0871", "value50612");

  // op 10480: put
  db->Put(write_options, "key2784", "value49907");

  // op 10481: put
  db->Put(write_options, "key8011", "value71717");

  // op 10482: put
  db->Put(write_options, "key3198", "value46170");

  // op 10483: put
  db->Put(write_options, "key7926", "value101806");

  // op 10484: put
  db->Put(write_options, "key10270", "value67190");

  // op 10485: get
  status = db->Get(read_options, "key9008", &value);
  if (status.ok()) {
    out << "GET key9008 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10486: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 10487: put
  db->Put(write_options, "key11145", "value56792");

  // op 10488: put
  db->Put(write_options, "key6928", "value38448");

  // op 10489: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0704", "key5854", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0704, key5854) size=" << scan_result.size() << std::endl;
  }

  // op 10490: put
  db->Put(write_options, "key10310", "value110487");

  // op 10491: get
  status = db->Get(read_options, "key5301", &value);
  if (status.ok()) {
    out << "GET key5301 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10492: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9855", "key11326", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9855, key11326) size=" << scan_result.size() << std::endl;
  }

  // op 10493: get
  status = db->Get(read_options, "key6545", &value);
  if (status.ok()) {
    out << "GET key6545 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10494: put
  db->Put(write_options, "key10959", "value105138");

  // op 10495: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3435", "key3589", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3435, key3589) size=" << scan_result.size() << std::endl;
  }

  // op 10496: put
  db->Put(write_options, "key2463", "value9614");

  // op 10497: put
  db->Put(write_options, "key7053", "value102244");

  // op 10498: get
  status = db->Get(read_options, "key7871", &value);
  if (status.ok()) {
    out << "GET key7871 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10499: delete
  db->Delete(write_options, "key5364");

  // op 10500: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1226", "key1859", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1226, key1859) size=" << scan_result.size() << std::endl;
  }

  // op 10501: get
  status = db->Get(read_options, "key10820", &value);
  if (status.ok()) {
    out << "GET key10820 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10502: get
  status = db->Get(read_options, "key7717", &value);
  if (status.ok()) {
    out << "GET key7717 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10503: delete
  db->Delete(write_options, "key3248");

  // op 10504: delete_range
  status = db->DeleteRange(write_options, "key1477", "key11348");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 10505: delete
  db->Delete(write_options, "key0308");

  // op 10506: get
  status = db->Get(read_options, "key0298", &value);
  if (status.ok()) {
    out << "GET key0298 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10507: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 10508: delete
  db->Delete(write_options, "key11669");

  // op 10509: delete_range
  status = db->DeleteRange(write_options, "key2903", "key3220");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 10510: put
  db->Put(write_options, "key2984", "value114655");

  // op 10511: get
  status = db->Get(read_options, "key8679", &value);
  if (status.ok()) {
    out << "GET key8679 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10512: put
  db->Put(write_options, "key4206", "value65683");

  // op 10513: get
  status = db->Get(read_options, "key3624", &value);
  if (status.ok()) {
    out << "GET key3624 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10514: put
  db->Put(write_options, "key8121", "value49961");

  // op 10515: get
  status = db->Get(read_options, "key1071", &value);
  if (status.ok()) {
    out << "GET key1071 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10516: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8585", "key11101", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8585, key11101) size=" << scan_result.size() << std::endl;
  }

  // op 10517: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 10518: get
  status = db->Get(read_options, "key10802", &value);
  if (status.ok()) {
    out << "GET key10802 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10519: put
  db->Put(write_options, "key2482", "value48857");

  // op 10520: get
  status = db->Get(read_options, "key5376", &value);
  if (status.ok()) {
    out << "GET key5376 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10521: delete
  db->Delete(write_options, "key8225");

  // op 10522: get
  status = db->Get(read_options, "key8823", &value);
  if (status.ok()) {
    out << "GET key8823 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10523: delete_range
  status = db->DeleteRange(write_options, "key4741", "key9290");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 10524: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4235", "key9467", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4235, key9467) size=" << scan_result.size() << std::endl;
  }

  // op 10525: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2214", "key7899", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2214, key7899) size=" << scan_result.size() << std::endl;
  }

  // op 10526: get
  status = db->Get(read_options, "key10260", &value);
  if (status.ok()) {
    out << "GET key10260 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10527: get
  status = db->Get(read_options, "key8481", &value);
  if (status.ok()) {
    out << "GET key8481 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10528: put
  db->Put(write_options, "key2099", "value116093");

  // op 10529: put
  db->Put(write_options, "key0487", "value16529");

  // op 10530: put
  db->Put(write_options, "key9044", "value12800");

  // op 10531: delete_range
  status = db->DeleteRange(write_options, "key7498", "key8514");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 10532: get
  status = db->Get(read_options, "key6219", &value);
  if (status.ok()) {
    out << "GET key6219 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10533: get
  status = db->Get(read_options, "key11346", &value);
  if (status.ok()) {
    out << "GET key11346 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10534: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4415", "key8623", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4415, key8623) size=" << scan_result.size() << std::endl;
  }

  // op 10535: put
  db->Put(write_options, "key8583", "value103854");

  // op 10536: get
  status = db->Get(read_options, "key0548", &value);
  if (status.ok()) {
    out << "GET key0548 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10537: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 10538: delete_range
  status = db->DeleteRange(write_options, "key8920", "key9833");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 10539: put
  db->Put(write_options, "key10487", "value25949");

  // op 10540: put
  db->Put(write_options, "key5164", "value58728");

  // op 10541: put
  db->Put(write_options, "key8185", "value90459");

  // op 10542: put
  db->Put(write_options, "key6344", "value17562");

  // op 10543: put
  db->Put(write_options, "key0544", "value65717");

  // op 10544: put
  db->Put(write_options, "key1002", "value73677");

  // op 10545: put
  db->Put(write_options, "key1650", "value10600");

  // op 10546: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 10547: put
  db->Put(write_options, "key1457", "value2089");

  // op 10548: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3321", "key9595", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3321, key9595) size=" << scan_result.size() << std::endl;
  }

  // op 10549: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1428", "key7460", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1428, key7460) size=" << scan_result.size() << std::endl;
  }

  // op 10550: get
  status = db->Get(read_options, "key2629", &value);
  if (status.ok()) {
    out << "GET key2629 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10551: delete
  db->Delete(write_options, "key1985");

  // op 10552: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4918", "key10626", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4918, key10626) size=" << scan_result.size() << std::endl;
  }

  // op 10553: delete_range
  status = db->DeleteRange(write_options, "key8839", "key9455");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 10554: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6419", "key9234", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6419, key9234) size=" << scan_result.size() << std::endl;
  }

  // op 10555: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3977", "key10623", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3977, key10623) size=" << scan_result.size() << std::endl;
  }

  // op 10556: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 10557: delete_range
  status = db->DeleteRange(write_options, "key5455", "key7754");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 10558: put
  db->Put(write_options, "key0000", "value46533");

  // op 10559: get
  status = db->Get(read_options, "key0817", &value);
  if (status.ok()) {
    out << "GET key0817 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10560: get
  status = db->Get(read_options, "key3634", &value);
  if (status.ok()) {
    out << "GET key3634 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10561: delete
  db->Delete(write_options, "key3913");

  // op 10562: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 10563: delete
  db->Delete(write_options, "key7331");

  // op 10564: put
  db->Put(write_options, "key6350", "value1755");

  // op 10565: put
  db->Put(write_options, "key8568", "value89211");

  // op 10566: put
  db->Put(write_options, "key0735", "value81718");

  // op 10567: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7517", "key9240", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7517, key9240) size=" << scan_result.size() << std::endl;
  }

  // op 10568: delete_range
  status = db->DeleteRange(write_options, "key5375", "key7646");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 10569: delete
  db->Delete(write_options, "key3242");

  // op 10570: put
  db->Put(write_options, "key11487", "value38666");

  // op 10571: get
  status = db->Get(read_options, "key6555", &value);
  if (status.ok()) {
    out << "GET key6555 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10572: put
  db->Put(write_options, "key5042", "value16378");

  // op 10573: put
  db->Put(write_options, "key1394", "value46919");

  // op 10574: put
  db->Put(write_options, "key0028", "value102722");

  // op 10575: put
  db->Put(write_options, "key11628", "value107434");

  // op 10576: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3413", "key10783", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3413, key10783) size=" << scan_result.size() << std::endl;
  }

  // op 10577: get
  status = db->Get(read_options, "key2457", &value);
  if (status.ok()) {
    out << "GET key2457 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10578: put
  db->Put(write_options, "key2053", "value56915");

  // op 10579: delete_range
  status = db->DeleteRange(write_options, "key7228", "key10301");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 10580: put
  db->Put(write_options, "key4562", "value62871");

  // op 10581: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5674", "key6685", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5674, key6685) size=" << scan_result.size() << std::endl;
  }

  // op 10582: put
  db->Put(write_options, "key4843", "value70359");

  // op 10583: delete_range
  status = db->DeleteRange(write_options, "key5378", "key7661");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 10584: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 10585: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 10586: get
  status = db->Get(read_options, "key7023", &value);
  if (status.ok()) {
    out << "GET key7023 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10587: delete
  db->Delete(write_options, "key11601");

  // op 10588: delete_range
  status = db->DeleteRange(write_options, "key4016", "key9006");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 10589: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3403", "key11039", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3403, key11039) size=" << scan_result.size() << std::endl;
  }

  // op 10590: get
  status = db->Get(read_options, "key8868", &value);
  if (status.ok()) {
    out << "GET key8868 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10591: delete
  db->Delete(write_options, "key6694");

  // op 10592: delete_range
  status = db->DeleteRange(write_options, "key3185", "key4729");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 10593: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3642", "key6670", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3642, key6670) size=" << scan_result.size() << std::endl;
  }

  // op 10594: put
  db->Put(write_options, "key5476", "value117058");

  // op 10595: put
  db->Put(write_options, "key0211", "value69888");

  // op 10596: delete_range
  status = db->DeleteRange(write_options, "key2861", "key11226");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 10597: get
  status = db->Get(read_options, "key0900", &value);
  if (status.ok()) {
    out << "GET key0900 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10598: get
  status = db->Get(read_options, "key1018", &value);
  if (status.ok()) {
    out << "GET key1018 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10599: delete
  db->Delete(write_options, "key0743");

  // op 10600: put
  db->Put(write_options, "key1125", "value74124");

  // op 10601: get
  status = db->Get(read_options, "key1651", &value);
  if (status.ok()) {
    out << "GET key1651 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10602: get
  status = db->Get(read_options, "key1584", &value);
  if (status.ok()) {
    out << "GET key1584 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10603: put
  db->Put(write_options, "key3849", "value88473");

  // op 10604: get
  status = db->Get(read_options, "key7705", &value);
  if (status.ok()) {
    out << "GET key7705 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10605: delete
  db->Delete(write_options, "key6969");

  // op 10606: get
  status = db->Get(read_options, "key2817", &value);
  if (status.ok()) {
    out << "GET key2817 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10607: put
  db->Put(write_options, "key0183", "value34749");

  // op 10608: put
  db->Put(write_options, "key11574", "value42308");

  // op 10609: put
  db->Put(write_options, "key1714", "value74918");

  // op 10610: get
  status = db->Get(read_options, "key7165", &value);
  if (status.ok()) {
    out << "GET key7165 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10611: put
  db->Put(write_options, "key8636", "value32060");

  // op 10612: put
  db->Put(write_options, "key6164", "value32589");

  // op 10613: put
  db->Put(write_options, "key8404", "value58666");

  // op 10614: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4955", "key6210", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4955, key6210) size=" << scan_result.size() << std::endl;
  }

  // op 10615: delete_range
  status = db->DeleteRange(write_options, "key0780", "key11670");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 10616: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3039", "key5218", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3039, key5218) size=" << scan_result.size() << std::endl;
  }

  // op 10617: delete_range
  status = db->DeleteRange(write_options, "key1165", "key10136");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 10618: put
  db->Put(write_options, "key3146", "value109008");

  // op 10619: delete_range
  status = db->DeleteRange(write_options, "key5739", "key7973");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 10620: put
  db->Put(write_options, "key7215", "value8382");

  // op 10621: put
  db->Put(write_options, "key0494", "value35507");

  // op 10622: delete_range
  status = db->DeleteRange(write_options, "key2399", "key3185");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 10623: put
  db->Put(write_options, "key5716", "value82403");

  // op 10624: delete
  db->Delete(write_options, "key11110");

  // op 10625: delete
  db->Delete(write_options, "key0621");

  // op 10626: put
  db->Put(write_options, "key10595", "value31163");

  // op 10627: put
  db->Put(write_options, "key1310", "value55966");

  // op 10628: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4596", "key5301", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4596, key5301) size=" << scan_result.size() << std::endl;
  }

  // op 10629: put
  db->Put(write_options, "key11012", "value48671");

  // op 10630: put
  db->Put(write_options, "key9292", "value19504");

  // op 10631: put
  db->Put(write_options, "key3933", "value99595");

  // op 10632: put
  db->Put(write_options, "key10641", "value55378");

  // op 10633: delete
  db->Delete(write_options, "key5792");

  // op 10634: put
  db->Put(write_options, "key8793", "value36299");

  // op 10635: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 10636: put
  db->Put(write_options, "key6413", "value26476");

  // op 10637: delete
  db->Delete(write_options, "key9541");

  // op 10638: put
  db->Put(write_options, "key7115", "value65803");

  // op 10639: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3617", "key9114", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3617, key9114) size=" << scan_result.size() << std::endl;
  }

  // op 10640: put
  db->Put(write_options, "key9034", "value108760");

  // op 10641: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0112", "key9509", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0112, key9509) size=" << scan_result.size() << std::endl;
  }

  // op 10642: delete
  db->Delete(write_options, "key3148");

  // op 10643: put
  db->Put(write_options, "key9802", "value105639");

  // op 10644: delete_range
  status = db->DeleteRange(write_options, "key6189", "key8236");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 10645: put
  db->Put(write_options, "key3160", "value37657");

  // op 10646: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2132", "key3594", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2132, key3594) size=" << scan_result.size() << std::endl;
  }

  // op 10647: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 10648: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 10649: put
  db->Put(write_options, "key7294", "value2396");

  // op 10650: delete
  db->Delete(write_options, "key6443");

  // op 10651: delete
  db->Delete(write_options, "key6413");

  // op 10652: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5585", "key7383", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5585, key7383) size=" << scan_result.size() << std::endl;
  }

  // op 10653: put
  db->Put(write_options, "key11514", "value47741");

  // op 10654: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0359", "key8693", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0359, key8693) size=" << scan_result.size() << std::endl;
  }

  // op 10655: get
  status = db->Get(read_options, "key9640", &value);
  if (status.ok()) {
    out << "GET key9640 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10656: delete
  db->Delete(write_options, "key9258");

  // op 10657: get
  status = db->Get(read_options, "key2916", &value);
  if (status.ok()) {
    out << "GET key2916 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10658: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4276", "key9789", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4276, key9789) size=" << scan_result.size() << std::endl;
  }

  // op 10659: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4205", "key10897", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4205, key10897) size=" << scan_result.size() << std::endl;
  }

  // op 10660: delete_range
  status = db->DeleteRange(write_options, "key7424", "key9904");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 10661: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6252", "key6478", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6252, key6478) size=" << scan_result.size() << std::endl;
  }

  // op 10662: delete
  db->Delete(write_options, "key10073");

  // op 10663: delete
  db->Delete(write_options, "key2568");

  // op 10664: get
  status = db->Get(read_options, "key6127", &value);
  if (status.ok()) {
    out << "GET key6127 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10665: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0018", "key0582", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0018, key0582) size=" << scan_result.size() << std::endl;
  }

  // op 10666: put
  db->Put(write_options, "key2350", "value33037");

  // op 10667: put
  db->Put(write_options, "key0304", "value11347");

  // op 10668: put
  db->Put(write_options, "key4356", "value60561");

  // op 10669: get
  status = db->Get(read_options, "key8250", &value);
  if (status.ok()) {
    out << "GET key8250 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10670: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5643", "key6286", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5643, key6286) size=" << scan_result.size() << std::endl;
  }

  // op 10671: put
  db->Put(write_options, "key5749", "value25386");

  // op 10672: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9012", "key9043", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9012, key9043) size=" << scan_result.size() << std::endl;
  }

  // op 10673: put
  db->Put(write_options, "key4276", "value53095");

  // op 10674: put
  db->Put(write_options, "key5124", "value20573");

  // op 10675: put
  db->Put(write_options, "key6704", "value54970");

  // op 10676: get
  status = db->Get(read_options, "key5853", &value);
  if (status.ok()) {
    out << "GET key5853 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10677: get
  status = db->Get(read_options, "key7513", &value);
  if (status.ok()) {
    out << "GET key7513 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10678: put
  db->Put(write_options, "key9316", "value56195");

  // op 10679: put
  db->Put(write_options, "key3578", "value28910");

  // op 10680: get
  status = db->Get(read_options, "key11216", &value);
  if (status.ok()) {
    out << "GET key11216 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10681: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3565", "key4346", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3565, key4346) size=" << scan_result.size() << std::endl;
  }

  // op 10682: get
  status = db->Get(read_options, "key3974", &value);
  if (status.ok()) {
    out << "GET key3974 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10683: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7606", "key11325", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7606, key11325) size=" << scan_result.size() << std::endl;
  }

  // op 10684: put
  db->Put(write_options, "key1994", "value78261");

  // op 10685: delete
  db->Delete(write_options, "key6273");

  // op 10686: put
  db->Put(write_options, "key2844", "value97874");

  // op 10687: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 10688: get
  status = db->Get(read_options, "key10790", &value);
  if (status.ok()) {
    out << "GET key10790 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10689: put
  db->Put(write_options, "key3637", "value65231");

  // op 10690: delete_range
  status = db->DeleteRange(write_options, "key5276", "key10648");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 10691: get
  status = db->Get(read_options, "key6871", &value);
  if (status.ok()) {
    out << "GET key6871 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10692: put
  db->Put(write_options, "key8760", "value71399");

  // op 10693: get
  status = db->Get(read_options, "key6084", &value);
  if (status.ok()) {
    out << "GET key6084 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10694: delete
  db->Delete(write_options, "key8924");

  // op 10695: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1815", "key5127", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1815, key5127) size=" << scan_result.size() << std::endl;
  }

  // op 10696: delete
  db->Delete(write_options, "key3171");

  // op 10697: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0220", "key3064", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0220, key3064) size=" << scan_result.size() << std::endl;
  }

  // op 10698: put
  db->Put(write_options, "key7626", "value21603");

  // op 10699: put
  db->Put(write_options, "key9363", "value54229");

  // op 10700: put
  db->Put(write_options, "key9615", "value14731");

  // op 10701: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6543", "key10166", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6543, key10166) size=" << scan_result.size() << std::endl;
  }

  // op 10702: get
  status = db->Get(read_options, "key7116", &value);
  if (status.ok()) {
    out << "GET key7116 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10703: put
  db->Put(write_options, "key7325", "value9435");

  // op 10704: get
  status = db->Get(read_options, "key6639", &value);
  if (status.ok()) {
    out << "GET key6639 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10705: put
  db->Put(write_options, "key1789", "value43020");

  // op 10706: put
  db->Put(write_options, "key8455", "value21784");

  // op 10707: put
  db->Put(write_options, "key3194", "value112692");

  // op 10708: put
  db->Put(write_options, "key11409", "value46063");

  // op 10709: delete_range
  status = db->DeleteRange(write_options, "key1290", "key3054");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 10710: delete
  db->Delete(write_options, "key11295");

  // op 10711: delete
  db->Delete(write_options, "key8109");

  // op 10712: get
  status = db->Get(read_options, "key10668", &value);
  if (status.ok()) {
    out << "GET key10668 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10713: delete_range
  status = db->DeleteRange(write_options, "key7233", "key8940");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 10714: delete
  db->Delete(write_options, "key8657");

  // op 10715: put
  db->Put(write_options, "key7955", "value39218");

  // op 10716: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7647", "key9335", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7647, key9335) size=" << scan_result.size() << std::endl;
  }

  // op 10717: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0303", "key9338", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0303, key9338) size=" << scan_result.size() << std::endl;
  }

  // op 10718: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3217", "key9618", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3217, key9618) size=" << scan_result.size() << std::endl;
  }

  // op 10719: delete
  db->Delete(write_options, "key2553");

  // op 10720: delete_range
  status = db->DeleteRange(write_options, "key4546", "key11217");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 10721: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8663", "key11114", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8663, key11114) size=" << scan_result.size() << std::endl;
  }

  // op 10722: put
  db->Put(write_options, "key9507", "value111013");

  // op 10723: put
  db->Put(write_options, "key3662", "value114782");

  // op 10724: delete_range
  status = db->DeleteRange(write_options, "key0805", "key9810");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 10725: put
  db->Put(write_options, "key7747", "value37474");

  // op 10726: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0888", "key11305", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0888, key11305) size=" << scan_result.size() << std::endl;
  }

  // op 10727: get
  status = db->Get(read_options, "key3114", &value);
  if (status.ok()) {
    out << "GET key3114 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10728: get
  status = db->Get(read_options, "key4392", &value);
  if (status.ok()) {
    out << "GET key4392 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10729: get
  status = db->Get(read_options, "key10975", &value);
  if (status.ok()) {
    out << "GET key10975 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10730: delete
  db->Delete(write_options, "key10882");

  // op 10731: delete_range
  status = db->DeleteRange(write_options, "key5794", "key9563");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 10732: delete
  db->Delete(write_options, "key4394");

  // op 10733: put
  db->Put(write_options, "key6355", "value11337");

  // op 10734: get
  status = db->Get(read_options, "key2076", &value);
  if (status.ok()) {
    out << "GET key2076 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10735: get
  status = db->Get(read_options, "key4972", &value);
  if (status.ok()) {
    out << "GET key4972 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10736: put
  db->Put(write_options, "key1302", "value2558");

  // op 10737: get
  status = db->Get(read_options, "key10156", &value);
  if (status.ok()) {
    out << "GET key10156 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10738: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4433", "key11135", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4433, key11135) size=" << scan_result.size() << std::endl;
  }

  // op 10739: get
  status = db->Get(read_options, "key9407", &value);
  if (status.ok()) {
    out << "GET key9407 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10740: delete_range
  status = db->DeleteRange(write_options, "key0043", "key6670");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 10741: put
  db->Put(write_options, "key11537", "value96934");

  // op 10742: delete
  db->Delete(write_options, "key6876");

  // op 10743: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8599", "key9937", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8599, key9937) size=" << scan_result.size() << std::endl;
  }

  // op 10744: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5475", "key10808", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5475, key10808) size=" << scan_result.size() << std::endl;
  }

  // op 10745: delete_range
  status = db->DeleteRange(write_options, "key2135", "key3797");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 10746: delete
  db->Delete(write_options, "key2249");

  // op 10747: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 10748: put
  db->Put(write_options, "key5817", "value89813");

  // op 10749: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3322", "key10618", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3322, key10618) size=" << scan_result.size() << std::endl;
  }

  // op 10750: delete
  db->Delete(write_options, "key4257");

  // op 10751: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3583", "key5338", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3583, key5338) size=" << scan_result.size() << std::endl;
  }

  // op 10752: put
  db->Put(write_options, "key11047", "value25023");

  // op 10753: get
  status = db->Get(read_options, "key11447", &value);
  if (status.ok()) {
    out << "GET key11447 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10754: put
  db->Put(write_options, "key0252", "value41773");

  // op 10755: delete
  db->Delete(write_options, "key6538");

  // op 10756: get
  status = db->Get(read_options, "key0388", &value);
  if (status.ok()) {
    out << "GET key0388 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10757: get
  status = db->Get(read_options, "key0897", &value);
  if (status.ok()) {
    out << "GET key0897 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10758: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 10759: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0212", "key1734", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0212, key1734) size=" << scan_result.size() << std::endl;
  }

  // op 10760: get
  status = db->Get(read_options, "key6627", &value);
  if (status.ok()) {
    out << "GET key6627 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10761: put
  db->Put(write_options, "key9029", "value95382");

  // op 10762: put
  db->Put(write_options, "key0564", "value59881");

  // op 10763: put
  db->Put(write_options, "key10772", "value24781");

  // op 10764: put
  db->Put(write_options, "key11148", "value95186");

  // op 10765: put
  db->Put(write_options, "key10187", "value1060");

  // op 10766: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2657", "key8346", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2657, key8346) size=" << scan_result.size() << std::endl;
  }

  // op 10767: put
  db->Put(write_options, "key8159", "value38295");

  // op 10768: delete
  db->Delete(write_options, "key4538");

  // op 10769: delete
  db->Delete(write_options, "key9411");

  // op 10770: delete
  db->Delete(write_options, "key4006");

  // op 10771: put
  db->Put(write_options, "key10285", "value10421");

  // op 10772: get
  status = db->Get(read_options, "key10668", &value);
  if (status.ok()) {
    out << "GET key10668 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10773: put
  db->Put(write_options, "key9068", "value103605");

  // op 10774: put
  db->Put(write_options, "key11544", "value30026");

  // op 10775: put
  db->Put(write_options, "key2621", "value20960");

  // op 10776: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 10777: delete
  db->Delete(write_options, "key9192");

  // op 10778: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0675", "key6735", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0675, key6735) size=" << scan_result.size() << std::endl;
  }

  // op 10779: delete_range
  status = db->DeleteRange(write_options, "key1055", "key11188");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 10780: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0516", "key0822", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0516, key0822) size=" << scan_result.size() << std::endl;
  }

  // op 10781: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4534", "key7860", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4534, key7860) size=" << scan_result.size() << std::endl;
  }

  // op 10782: put
  db->Put(write_options, "key1532", "value114993");

  // op 10783: put
  db->Put(write_options, "key3730", "value62259");

  // op 10784: put
  db->Put(write_options, "key11244", "value25672");

  // op 10785: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9544", "key10208", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9544, key10208) size=" << scan_result.size() << std::endl;
  }

  // op 10786: delete_range
  status = db->DeleteRange(write_options, "key8064", "key8237");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 10787: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1266", "key9404", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1266, key9404) size=" << scan_result.size() << std::endl;
  }

  // op 10788: put
  db->Put(write_options, "key5904", "value72500");

  // op 10789: put
  db->Put(write_options, "key10710", "value99400");

  // op 10790: put
  db->Put(write_options, "key8203", "value27637");

  // op 10791: delete
  db->Delete(write_options, "key11502");

  // op 10792: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2582", "key5941", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2582, key5941) size=" << scan_result.size() << std::endl;
  }

  // op 10793: put
  db->Put(write_options, "key2459", "value111782");

  // op 10794: put
  db->Put(write_options, "key1223", "value22250");

  // op 10795: get
  status = db->Get(read_options, "key0388", &value);
  if (status.ok()) {
    out << "GET key0388 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10796: delete_range
  status = db->DeleteRange(write_options, "key0343", "key3281");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 10797: get
  status = db->Get(read_options, "key8242", &value);
  if (status.ok()) {
    out << "GET key8242 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10798: get
  status = db->Get(read_options, "key9986", &value);
  if (status.ok()) {
    out << "GET key9986 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10799: delete_range
  status = db->DeleteRange(write_options, "key1763", "key5239");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 10800: put
  db->Put(write_options, "key7210", "value61263");

  // op 10801: put
  db->Put(write_options, "key4041", "value30631");

  // op 10802: get
  status = db->Get(read_options, "key1141", &value);
  if (status.ok()) {
    out << "GET key1141 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10803: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9954", "key11364", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9954, key11364) size=" << scan_result.size() << std::endl;
  }

  // op 10804: get
  status = db->Get(read_options, "key11130", &value);
  if (status.ok()) {
    out << "GET key11130 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10805: get
  status = db->Get(read_options, "key5241", &value);
  if (status.ok()) {
    out << "GET key5241 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10806: get
  status = db->Get(read_options, "key11264", &value);
  if (status.ok()) {
    out << "GET key11264 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10807: put
  db->Put(write_options, "key3512", "value0633");

  // op 10808: get
  status = db->Get(read_options, "key2546", &value);
  if (status.ok()) {
    out << "GET key2546 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10809: put
  db->Put(write_options, "key2923", "value83171");

  // op 10810: get
  status = db->Get(read_options, "key5219", &value);
  if (status.ok()) {
    out << "GET key5219 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10811: get
  status = db->Get(read_options, "key10543", &value);
  if (status.ok()) {
    out << "GET key10543 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10812: put
  db->Put(write_options, "key3564", "value117380");

  // op 10813: put
  db->Put(write_options, "key6575", "value23077");

  // op 10814: get
  status = db->Get(read_options, "key2846", &value);
  if (status.ok()) {
    out << "GET key2846 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10815: put
  db->Put(write_options, "key9709", "value93885");

  // op 10816: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10546", "key11454", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10546, key11454) size=" << scan_result.size() << std::endl;
  }

  // op 10817: delete_range
  status = db->DeleteRange(write_options, "key1557", "key10325");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 10818: delete
  db->Delete(write_options, "key6642");

  // op 10819: put
  db->Put(write_options, "key9823", "value61225");

  // op 10820: put
  db->Put(write_options, "key9871", "value81534");

  // op 10821: get
  status = db->Get(read_options, "key8016", &value);
  if (status.ok()) {
    out << "GET key8016 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10822: delete
  db->Delete(write_options, "key9614");

  // op 10823: put
  db->Put(write_options, "key0199", "value15141");

  // op 10824: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0942", "key10433", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0942, key10433) size=" << scan_result.size() << std::endl;
  }

  // op 10825: put
  db->Put(write_options, "key10650", "value88965");

  // op 10826: put
  db->Put(write_options, "key1972", "value31756");

  // op 10827: put
  db->Put(write_options, "key0184", "value1973");

  // op 10828: put
  db->Put(write_options, "key7586", "value20899");

  // op 10829: put
  db->Put(write_options, "key1616", "value81201");

  // op 10830: delete_range
  status = db->DeleteRange(write_options, "key7405", "key9231");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 10831: put
  db->Put(write_options, "key8507", "value29603");

  // op 10832: put
  db->Put(write_options, "key2796", "value46616");

  // op 10833: put
  db->Put(write_options, "key5849", "value45189");

  // op 10834: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2904", "key4634", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2904, key4634) size=" << scan_result.size() << std::endl;
  }

  // op 10835: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5306", "key9901", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5306, key9901) size=" << scan_result.size() << std::endl;
  }

  // op 10836: put
  db->Put(write_options, "key5211", "value93427");

  // op 10837: delete
  db->Delete(write_options, "key3280");

  // op 10838: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1765", "key5103", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1765, key5103) size=" << scan_result.size() << std::endl;
  }

  // op 10839: put
  db->Put(write_options, "key8823", "value77653");

  // op 10840: put
  db->Put(write_options, "key6512", "value103875");

  // op 10841: get
  status = db->Get(read_options, "key1182", &value);
  if (status.ok()) {
    out << "GET key1182 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10842: delete
  db->Delete(write_options, "key10499");

  // op 10843: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0271", "key9917", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0271, key9917) size=" << scan_result.size() << std::endl;
  }

  // op 10844: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6703", "key8032", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6703, key8032) size=" << scan_result.size() << std::endl;
  }

  // op 10845: delete
  db->Delete(write_options, "key1315");

  // op 10846: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 10847: put
  db->Put(write_options, "key6928", "value45126");

  // op 10848: put
  db->Put(write_options, "key8039", "value116292");

  // op 10849: put
  db->Put(write_options, "key11095", "value66766");

  // op 10850: get
  status = db->Get(read_options, "key2223", &value);
  if (status.ok()) {
    out << "GET key2223 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10851: put
  db->Put(write_options, "key1823", "value13830");

  // op 10852: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0674", "key4961", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0674, key4961) size=" << scan_result.size() << std::endl;
  }

  // op 10853: get
  status = db->Get(read_options, "key1564", &value);
  if (status.ok()) {
    out << "GET key1564 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10854: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 10855: put
  db->Put(write_options, "key5920", "value81306");

  // op 10856: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 10857: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 10858: delete_range
  status = db->DeleteRange(write_options, "key5670", "key7314");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 10859: put
  db->Put(write_options, "key3413", "value13836");

  // op 10860: put
  db->Put(write_options, "key0209", "value58365");

  // op 10861: put
  db->Put(write_options, "key11050", "value100744");

  // op 10862: put
  db->Put(write_options, "key2712", "value74612");

  // op 10863: put
  db->Put(write_options, "key7214", "value17579");

  // op 10864: get
  status = db->Get(read_options, "key0337", &value);
  if (status.ok()) {
    out << "GET key0337 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10865: get
  status = db->Get(read_options, "key7737", &value);
  if (status.ok()) {
    out << "GET key7737 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10866: get
  status = db->Get(read_options, "key3870", &value);
  if (status.ok()) {
    out << "GET key3870 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10867: put
  db->Put(write_options, "key2208", "value53238");

  // op 10868: put
  db->Put(write_options, "key3378", "value33258");

  // op 10869: delete_range
  status = db->DeleteRange(write_options, "key0030", "key7457");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 10870: put
  db->Put(write_options, "key5511", "value63174");

  // op 10871: put
  db->Put(write_options, "key7104", "value109112");

  // op 10872: put
  db->Put(write_options, "key5141", "value59583");

  // op 10873: put
  db->Put(write_options, "key7865", "value23204");

  // op 10874: put
  db->Put(write_options, "key1816", "value42040");

  // op 10875: delete_range
  status = db->DeleteRange(write_options, "key2968", "key4927");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 10876: put
  db->Put(write_options, "key0587", "value35710");

  // op 10877: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1910", "key8305", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1910, key8305) size=" << scan_result.size() << std::endl;
  }

  // op 10878: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 10879: get
  status = db->Get(read_options, "key3254", &value);
  if (status.ok()) {
    out << "GET key3254 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10880: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6576", "key8955", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6576, key8955) size=" << scan_result.size() << std::endl;
  }

  // op 10881: put
  db->Put(write_options, "key0431", "value86320");

  // op 10882: put
  db->Put(write_options, "key10704", "value67812");

  // op 10883: put
  db->Put(write_options, "key3560", "value1381");

  // op 10884: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 10885: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5645", "key10917", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5645, key10917) size=" << scan_result.size() << std::endl;
  }

  // op 10886: delete
  db->Delete(write_options, "key11403");

  // op 10887: delete
  db->Delete(write_options, "key6298");

  // op 10888: put
  db->Put(write_options, "key5464", "value11544");

  // op 10889: put
  db->Put(write_options, "key6182", "value62194");

  // op 10890: put
  db->Put(write_options, "key10847", "value96792");

  // op 10891: put
  db->Put(write_options, "key3775", "value70011");

  // op 10892: put
  db->Put(write_options, "key0537", "value67742");

  // op 10893: put
  db->Put(write_options, "key5418", "value32184");

  // op 10894: put
  db->Put(write_options, "key5977", "value92082");

  // op 10895: get
  status = db->Get(read_options, "key5266", &value);
  if (status.ok()) {
    out << "GET key5266 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10896: delete_range
  status = db->DeleteRange(write_options, "key9722", "key10427");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 10897: delete
  db->Delete(write_options, "key7955");

  // op 10898: put
  db->Put(write_options, "key11632", "value104417");

  // op 10899: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 10900: put
  db->Put(write_options, "key4985", "value114022");

  // op 10901: put
  db->Put(write_options, "key6652", "value113119");

  // op 10902: delete
  db->Delete(write_options, "key9230");

  // op 10903: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0115", "key3503", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0115, key3503) size=" << scan_result.size() << std::endl;
  }

  // op 10904: get
  status = db->Get(read_options, "key8728", &value);
  if (status.ok()) {
    out << "GET key8728 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10905: delete
  db->Delete(write_options, "key4839");

  // op 10906: get
  status = db->Get(read_options, "key10356", &value);
  if (status.ok()) {
    out << "GET key10356 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10907: get
  status = db->Get(read_options, "key2670", &value);
  if (status.ok()) {
    out << "GET key2670 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10908: put
  db->Put(write_options, "key9227", "value38887");

  // op 10909: delete
  db->Delete(write_options, "key2453");

  // op 10910: get
  status = db->Get(read_options, "key8842", &value);
  if (status.ok()) {
    out << "GET key8842 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10911: delete
  db->Delete(write_options, "key5648");

  // op 10912: delete_range
  status = db->DeleteRange(write_options, "key6886", "key11082");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 10913: put
  db->Put(write_options, "key7523", "value19575");

  // op 10914: delete_range
  status = db->DeleteRange(write_options, "key7380", "key8334");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 10915: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5552", "key11693", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5552, key11693) size=" << scan_result.size() << std::endl;
  }

  // op 10916: put
  db->Put(write_options, "key8122", "value56252");

  // op 10917: delete
  db->Delete(write_options, "key11646");

  // op 10918: get
  status = db->Get(read_options, "key4852", &value);
  if (status.ok()) {
    out << "GET key4852 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10919: get
  status = db->Get(read_options, "key11407", &value);
  if (status.ok()) {
    out << "GET key11407 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10920: put
  db->Put(write_options, "key3156", "value19837");

  // op 10921: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5504", "key11396", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5504, key11396) size=" << scan_result.size() << std::endl;
  }

  // op 10922: get
  status = db->Get(read_options, "key1784", &value);
  if (status.ok()) {
    out << "GET key1784 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10923: get
  status = db->Get(read_options, "key5064", &value);
  if (status.ok()) {
    out << "GET key5064 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10924: put
  db->Put(write_options, "key4269", "value20543");

  // op 10925: delete_range
  status = db->DeleteRange(write_options, "key6654", "key9528");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 10926: put
  db->Put(write_options, "key2173", "value36278");

  // op 10927: put
  db->Put(write_options, "key9223", "value97059");

  // op 10928: put
  db->Put(write_options, "key6376", "value112447");

  // op 10929: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0023", "key4687", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0023, key4687) size=" << scan_result.size() << std::endl;
  }

  // op 10930: put
  db->Put(write_options, "key4707", "value11403");

  // op 10931: get
  status = db->Get(read_options, "key3898", &value);
  if (status.ok()) {
    out << "GET key3898 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10932: get
  status = db->Get(read_options, "key1729", &value);
  if (status.ok()) {
    out << "GET key1729 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10933: delete_range
  status = db->DeleteRange(write_options, "key3131", "key7572");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 10934: put
  db->Put(write_options, "key2121", "value111347");

  // op 10935: get
  status = db->Get(read_options, "key3936", &value);
  if (status.ok()) {
    out << "GET key3936 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10936: put
  db->Put(write_options, "key2516", "value11811");

  // op 10937: put
  db->Put(write_options, "key2497", "value50166");

  // op 10938: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 10939: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2380", "key11481", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2380, key11481) size=" << scan_result.size() << std::endl;
  }

  // op 10940: get
  status = db->Get(read_options, "key11256", &value);
  if (status.ok()) {
    out << "GET key11256 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10941: delete_range
  status = db->DeleteRange(write_options, "key5083", "key10679");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 10942: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0924", "key8737", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0924, key8737) size=" << scan_result.size() << std::endl;
  }

  // op 10943: delete_range
  status = db->DeleteRange(write_options, "key1910", "key10465");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 10944: delete_range
  status = db->DeleteRange(write_options, "key0054", "key4902");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 10945: put
  db->Put(write_options, "key0904", "value42043");

  // op 10946: put
  db->Put(write_options, "key9099", "value111595");

  // op 10947: put
  db->Put(write_options, "key6731", "value98764");

  // op 10948: put
  db->Put(write_options, "key5473", "value102152");

  // op 10949: put
  db->Put(write_options, "key5577", "value105312");

  // op 10950: put
  db->Put(write_options, "key4796", "value104221");

  // op 10951: delete_range
  status = db->DeleteRange(write_options, "key5837", "key9120");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 10952: get
  status = db->Get(read_options, "key3495", &value);
  if (status.ok()) {
    out << "GET key3495 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10953: put
  db->Put(write_options, "key7892", "value78432");

  // op 10954: put
  db->Put(write_options, "key7046", "value86695");

  // op 10955: put
  db->Put(write_options, "key2831", "value9087");

  // op 10956: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 10957: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9278", "key9882", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9278, key9882) size=" << scan_result.size() << std::endl;
  }

  // op 10958: get
  status = db->Get(read_options, "key10010", &value);
  if (status.ok()) {
    out << "GET key10010 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10959: put
  db->Put(write_options, "key7312", "value38944");

  // op 10960: get
  status = db->Get(read_options, "key4540", &value);
  if (status.ok()) {
    out << "GET key4540 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10961: delete_range
  status = db->DeleteRange(write_options, "key3431", "key8431");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 10962: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4047", "key9847", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4047, key9847) size=" << scan_result.size() << std::endl;
  }

  // op 10963: put
  db->Put(write_options, "key8166", "value95293");

  // op 10964: put
  db->Put(write_options, "key3085", "value21465");

  // op 10965: put
  db->Put(write_options, "key11699", "value63649");

  // op 10966: get
  status = db->Get(read_options, "key4913", &value);
  if (status.ok()) {
    out << "GET key4913 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10967: delete
  db->Delete(write_options, "key5626");

  // op 10968: get
  status = db->Get(read_options, "key10529", &value);
  if (status.ok()) {
    out << "GET key10529 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10969: put
  db->Put(write_options, "key9011", "value17177");

  // op 10970: put
  db->Put(write_options, "key9032", "value19749");

  // op 10971: get
  status = db->Get(read_options, "key6655", &value);
  if (status.ok()) {
    out << "GET key6655 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10972: put
  db->Put(write_options, "key0974", "value51455");

  // op 10973: delete
  db->Delete(write_options, "key4666");

  // op 10974: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 10975: delete_range
  status = db->DeleteRange(write_options, "key1460", "key8424");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 10976: delete
  db->Delete(write_options, "key5192");

  // op 10977: get
  status = db->Get(read_options, "key1412", &value);
  if (status.ok()) {
    out << "GET key1412 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10978: get
  status = db->Get(read_options, "key5737", &value);
  if (status.ok()) {
    out << "GET key5737 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10979: put
  db->Put(write_options, "key6580", "value22936");

  // op 10980: put
  db->Put(write_options, "key5174", "value44126");

  // op 10981: get
  status = db->Get(read_options, "key3964", &value);
  if (status.ok()) {
    out << "GET key3964 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10982: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5229", "key6719", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5229, key6719) size=" << scan_result.size() << std::endl;
  }

  // op 10983: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5140", "key8737", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5140, key8737) size=" << scan_result.size() << std::endl;
  }

  // op 10984: put
  db->Put(write_options, "key10428", "value36189");

  // op 10985: delete_range
  status = db->DeleteRange(write_options, "key6731", "key9381");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 10986: get
  status = db->Get(read_options, "key4580", &value);
  if (status.ok()) {
    out << "GET key4580 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10987: delete_range
  status = db->DeleteRange(write_options, "key7922", "key9277");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 10988: get
  status = db->Get(read_options, "key4759", &value);
  if (status.ok()) {
    out << "GET key4759 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10989: get
  status = db->Get(read_options, "key8431", &value);
  if (status.ok()) {
    out << "GET key8431 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10990: delete_range
  status = db->DeleteRange(write_options, "key4470", "key9898");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 10991: put
  db->Put(write_options, "key9060", "value35348");

  // op 10992: put
  db->Put(write_options, "key2369", "value117968");

  // op 10993: get
  status = db->Get(read_options, "key1304", &value);
  if (status.ok()) {
    out << "GET key1304 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 10994: put
  db->Put(write_options, "key4258", "value29937");

  // op 10995: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6542", "key9902", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6542, key9902) size=" << scan_result.size() << std::endl;
  }

  // op 10996: put
  db->Put(write_options, "key0746", "value10542");

  // op 10997: put
  db->Put(write_options, "key8844", "value15786");

  // op 10998: delete_range
  status = db->DeleteRange(write_options, "key0354", "key7549");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 10999: put
  db->Put(write_options, "key2387", "value20315");

  // op 11000: delete
  db->Delete(write_options, "key2643");

  // op 11001: delete
  db->Delete(write_options, "key4247");

  // op 11002: put
  db->Put(write_options, "key3702", "value105656");

  // op 11003: get
  status = db->Get(read_options, "key1318", &value);
  if (status.ok()) {
    out << "GET key1318 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11004: get
  status = db->Get(read_options, "key9639", &value);
  if (status.ok()) {
    out << "GET key9639 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11005: put
  db->Put(write_options, "key7471", "value13444");

  // op 11006: put
  db->Put(write_options, "key8581", "value78407");

  // op 11007: get
  status = db->Get(read_options, "key5492", &value);
  if (status.ok()) {
    out << "GET key5492 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11008: get
  status = db->Get(read_options, "key7825", &value);
  if (status.ok()) {
    out << "GET key7825 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11009: put
  db->Put(write_options, "key8299", "value72217");

  // op 11010: delete
  db->Delete(write_options, "key3743");

  // op 11011: scan
  scan_result.clear();
  status = db->Scan(read_options, "key11539", "key11626", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key11539, key11626) size=" << scan_result.size() << std::endl;
  }

  // op 11012: delete
  db->Delete(write_options, "key1454");

  // op 11013: get
  status = db->Get(read_options, "key8142", &value);
  if (status.ok()) {
    out << "GET key8142 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11014: put
  db->Put(write_options, "key3369", "value53684");

  // op 11015: put
  db->Put(write_options, "key7573", "value98095");

  // op 11016: delete
  db->Delete(write_options, "key2299");

  // op 11017: delete_range
  status = db->DeleteRange(write_options, "key5714", "key10847");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11018: put
  db->Put(write_options, "key6655", "value28027");

  // op 11019: put
  db->Put(write_options, "key9156", "value48790");

  // op 11020: put
  db->Put(write_options, "key7702", "value20442");

  // op 11021: get
  status = db->Get(read_options, "key3061", &value);
  if (status.ok()) {
    out << "GET key3061 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11022: delete
  db->Delete(write_options, "key0518");

  // op 11023: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 11024: get
  status = db->Get(read_options, "key2484", &value);
  if (status.ok()) {
    out << "GET key2484 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11025: get
  status = db->Get(read_options, "key3394", &value);
  if (status.ok()) {
    out << "GET key3394 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11026: delete_range
  status = db->DeleteRange(write_options, "key6765", "key8979");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11027: put
  db->Put(write_options, "key2502", "value24093");

  // op 11028: put
  db->Put(write_options, "key5593", "value41309");

  // op 11029: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0107", "key10074", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0107, key10074) size=" << scan_result.size() << std::endl;
  }

  // op 11030: put
  db->Put(write_options, "key4089", "value94079");

  // op 11031: delete
  db->Delete(write_options, "key6561");

  // op 11032: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1438", "key6325", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1438, key6325) size=" << scan_result.size() << std::endl;
  }

  // op 11033: put
  db->Put(write_options, "key11253", "value52900");

  // op 11034: delete
  db->Delete(write_options, "key1163");

  // op 11035: delete_range
  status = db->DeleteRange(write_options, "key9725", "key9925");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11036: put
  db->Put(write_options, "key0769", "value59871");

  // op 11037: delete
  db->Delete(write_options, "key6464");

  // op 11038: delete
  db->Delete(write_options, "key3471");

  // op 11039: delete_range
  status = db->DeleteRange(write_options, "key4822", "key9887");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11040: get
  status = db->Get(read_options, "key8420", &value);
  if (status.ok()) {
    out << "GET key8420 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11041: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 11042: delete
  db->Delete(write_options, "key7984");

  // op 11043: put
  db->Put(write_options, "key11591", "value69370");

  // op 11044: put
  db->Put(write_options, "key0474", "value101121");

  // op 11045: delete
  db->Delete(write_options, "key2941");

  // op 11046: put
  db->Put(write_options, "key8395", "value114071");

  // op 11047: delete
  db->Delete(write_options, "key4467");

  // op 11048: put
  db->Put(write_options, "key0823", "value30140");

  // op 11049: delete_range
  status = db->DeleteRange(write_options, "key1294", "key3199");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11050: put
  db->Put(write_options, "key3797", "value62463");

  // op 11051: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1040", "key11688", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1040, key11688) size=" << scan_result.size() << std::endl;
  }

  // op 11052: put
  db->Put(write_options, "key3152", "value103825");

  // op 11053: delete_range
  status = db->DeleteRange(write_options, "key10281", "key11656");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11054: get
  status = db->Get(read_options, "key10008", &value);
  if (status.ok()) {
    out << "GET key10008 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11055: put
  db->Put(write_options, "key6720", "value55773");

  // op 11056: delete
  db->Delete(write_options, "key8694");

  // op 11057: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1874", "key2941", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1874, key2941) size=" << scan_result.size() << std::endl;
  }

  // op 11058: delete_range
  status = db->DeleteRange(write_options, "key2691", "key2827");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11059: delete_range
  status = db->DeleteRange(write_options, "key0321", "key8837");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11060: delete
  db->Delete(write_options, "key8844");

  // op 11061: delete_range
  status = db->DeleteRange(write_options, "key1854", "key8244");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11062: put
  db->Put(write_options, "key10098", "value17204");

  // op 11063: put
  db->Put(write_options, "key9092", "value1168");

  // op 11064: get
  status = db->Get(read_options, "key6079", &value);
  if (status.ok()) {
    out << "GET key6079 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11065: put
  db->Put(write_options, "key9770", "value5356");

  // op 11066: put
  db->Put(write_options, "key6509", "value27564");

  // op 11067: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4025", "key6107", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4025, key6107) size=" << scan_result.size() << std::endl;
  }

  // op 11068: put
  db->Put(write_options, "key3294", "value76094");

  // op 11069: put
  db->Put(write_options, "key1521", "value29452");

  // op 11070: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2346", "key7940", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2346, key7940) size=" << scan_result.size() << std::endl;
  }

  // op 11071: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 11072: put
  db->Put(write_options, "key1464", "value4114");

  // op 11073: put
  db->Put(write_options, "key2977", "value27216");

  // op 11074: get
  status = db->Get(read_options, "key2634", &value);
  if (status.ok()) {
    out << "GET key2634 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11075: get
  status = db->Get(read_options, "key10575", &value);
  if (status.ok()) {
    out << "GET key10575 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11076: put
  db->Put(write_options, "key11353", "value50553");

  // op 11077: put
  db->Put(write_options, "key8674", "value5540");

  // op 11078: get
  status = db->Get(read_options, "key3643", &value);
  if (status.ok()) {
    out << "GET key3643 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11079: put
  db->Put(write_options, "key0646", "value11493");

  // op 11080: put
  db->Put(write_options, "key6078", "value95717");

  // op 11081: get
  status = db->Get(read_options, "key5369", &value);
  if (status.ok()) {
    out << "GET key5369 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11082: delete
  db->Delete(write_options, "key1247");

  // op 11083: put
  db->Put(write_options, "key3466", "value76852");

  // op 11084: put
  db->Put(write_options, "key7373", "value31444");

  // op 11085: get
  status = db->Get(read_options, "key4839", &value);
  if (status.ok()) {
    out << "GET key4839 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11086: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5367", "key7726", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5367, key7726) size=" << scan_result.size() << std::endl;
  }

  // op 11087: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1493", "key3764", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1493, key3764) size=" << scan_result.size() << std::endl;
  }

  // op 11088: get
  status = db->Get(read_options, "key1037", &value);
  if (status.ok()) {
    out << "GET key1037 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11089: get
  status = db->Get(read_options, "key8098", &value);
  if (status.ok()) {
    out << "GET key8098 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11090: get
  status = db->Get(read_options, "key6716", &value);
  if (status.ok()) {
    out << "GET key6716 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11091: put
  db->Put(write_options, "key0968", "value22421");

  // op 11092: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2655", "key8981", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2655, key8981) size=" << scan_result.size() << std::endl;
  }

  // op 11093: put
  db->Put(write_options, "key11540", "value23547");

  // op 11094: get
  status = db->Get(read_options, "key9965", &value);
  if (status.ok()) {
    out << "GET key9965 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11095: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 11096: delete
  db->Delete(write_options, "key0940");

  // op 11097: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5466", "key5935", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5466, key5935) size=" << scan_result.size() << std::endl;
  }

  // op 11098: put
  db->Put(write_options, "key10143", "value113140");

  // op 11099: put
  db->Put(write_options, "key1347", "value85728");

  // op 11100: get
  status = db->Get(read_options, "key10668", &value);
  if (status.ok()) {
    out << "GET key10668 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11101: delete_range
  status = db->DeleteRange(write_options, "key7306", "key9102");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11102: get
  status = db->Get(read_options, "key7662", &value);
  if (status.ok()) {
    out << "GET key7662 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11103: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0762", "key10513", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0762, key10513) size=" << scan_result.size() << std::endl;
  }

  // op 11104: put
  db->Put(write_options, "key6714", "value114142");

  // op 11105: get
  status = db->Get(read_options, "key9142", &value);
  if (status.ok()) {
    out << "GET key9142 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11106: put
  db->Put(write_options, "key7136", "value13379");

  // op 11107: put
  db->Put(write_options, "key9133", "value3413");

  // op 11108: get
  status = db->Get(read_options, "key0576", &value);
  if (status.ok()) {
    out << "GET key0576 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11109: put
  db->Put(write_options, "key7341", "value23795");

  // op 11110: put
  db->Put(write_options, "key1955", "value78094");

  // op 11111: delete
  db->Delete(write_options, "key7817");

  // op 11112: put
  db->Put(write_options, "key9087", "value67287");

  // op 11113: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6305", "key10367", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6305, key10367) size=" << scan_result.size() << std::endl;
  }

  // op 11114: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0740", "key3217", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0740, key3217) size=" << scan_result.size() << std::endl;
  }

  // op 11115: delete_range
  status = db->DeleteRange(write_options, "key4894", "key10617");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11116: put
  db->Put(write_options, "key6598", "value88909");

  // op 11117: get
  status = db->Get(read_options, "key6115", &value);
  if (status.ok()) {
    out << "GET key6115 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11118: put
  db->Put(write_options, "key0120", "value52217");

  // op 11119: get
  status = db->Get(read_options, "key2932", &value);
  if (status.ok()) {
    out << "GET key2932 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11120: put
  db->Put(write_options, "key6994", "value36241");

  // op 11121: delete_range
  status = db->DeleteRange(write_options, "key3367", "key8317");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11122: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2698", "key8328", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2698, key8328) size=" << scan_result.size() << std::endl;
  }

  // op 11123: put
  db->Put(write_options, "key6800", "value14479");

  // op 11124: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 11125: delete
  db->Delete(write_options, "key3088");

  // op 11126: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8958", "key9544", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8958, key9544) size=" << scan_result.size() << std::endl;
  }

  // op 11127: delete
  db->Delete(write_options, "key7106");

  // op 11128: get
  status = db->Get(read_options, "key1476", &value);
  if (status.ok()) {
    out << "GET key1476 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11129: delete_range
  status = db->DeleteRange(write_options, "key4071", "key9814");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11130: get
  status = db->Get(read_options, "key7713", &value);
  if (status.ok()) {
    out << "GET key7713 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11131: delete_range
  status = db->DeleteRange(write_options, "key5603", "key5916");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11132: delete
  db->Delete(write_options, "key8441");

  // op 11133: put
  db->Put(write_options, "key3842", "value52527");

  // op 11134: delete_range
  status = db->DeleteRange(write_options, "key7378", "key7617");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11135: put
  db->Put(write_options, "key6985", "value48304");

  // op 11136: get
  status = db->Get(read_options, "key4661", &value);
  if (status.ok()) {
    out << "GET key4661 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11137: put
  db->Put(write_options, "key1985", "value38887");

  // op 11138: get
  status = db->Get(read_options, "key5595", &value);
  if (status.ok()) {
    out << "GET key5595 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11139: get
  status = db->Get(read_options, "key6558", &value);
  if (status.ok()) {
    out << "GET key6558 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11140: get
  status = db->Get(read_options, "key10171", &value);
  if (status.ok()) {
    out << "GET key10171 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11141: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3416", "key11720", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3416, key11720) size=" << scan_result.size() << std::endl;
  }

  // op 11142: put
  db->Put(write_options, "key9313", "value80049");

  // op 11143: put
  db->Put(write_options, "key5615", "value114844");

  // op 11144: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 11145: delete
  db->Delete(write_options, "key1408");

  // op 11146: put
  db->Put(write_options, "key1634", "value81115");

  // op 11147: delete
  db->Delete(write_options, "key2777");

  // op 11148: delete
  db->Delete(write_options, "key5813");

  // op 11149: put
  db->Put(write_options, "key11536", "value116607");

  // op 11150: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2233", "key5316", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2233, key5316) size=" << scan_result.size() << std::endl;
  }

  // op 11151: put
  db->Put(write_options, "key10710", "value52895");

  // op 11152: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2591", "key6570", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2591, key6570) size=" << scan_result.size() << std::endl;
  }

  // op 11153: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4974", "key10990", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4974, key10990) size=" << scan_result.size() << std::endl;
  }

  // op 11154: get
  status = db->Get(read_options, "key8803", &value);
  if (status.ok()) {
    out << "GET key8803 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11155: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6795", "key9099", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6795, key9099) size=" << scan_result.size() << std::endl;
  }

  // op 11156: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0591", "key3116", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0591, key3116) size=" << scan_result.size() << std::endl;
  }

  // op 11157: put
  db->Put(write_options, "key8000", "value25404");

  // op 11158: delete_range
  status = db->DeleteRange(write_options, "key5053", "key9066");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11159: delete_range
  status = db->DeleteRange(write_options, "key1732", "key6012");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11160: put
  db->Put(write_options, "key10318", "value40211");

  // op 11161: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 11162: delete
  db->Delete(write_options, "key7620");

  // op 11163: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3722", "key5601", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3722, key5601) size=" << scan_result.size() << std::endl;
  }

  // op 11164: put
  db->Put(write_options, "key0730", "value72920");

  // op 11165: put
  db->Put(write_options, "key7928", "value53384");

  // op 11166: get
  status = db->Get(read_options, "key2685", &value);
  if (status.ok()) {
    out << "GET key2685 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11167: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 11168: put
  db->Put(write_options, "key1974", "value21830");

  // op 11169: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0027", "key10576", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0027, key10576) size=" << scan_result.size() << std::endl;
  }

  // op 11170: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 11171: get
  status = db->Get(read_options, "key0042", &value);
  if (status.ok()) {
    out << "GET key0042 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11172: get
  status = db->Get(read_options, "key1132", &value);
  if (status.ok()) {
    out << "GET key1132 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11173: get
  status = db->Get(read_options, "key10267", &value);
  if (status.ok()) {
    out << "GET key10267 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11174: delete_range
  status = db->DeleteRange(write_options, "key4818", "key5383");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11175: delete
  db->Delete(write_options, "key6919");

  // op 11176: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1129", "key2616", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1129, key2616) size=" << scan_result.size() << std::endl;
  }

  // op 11177: get
  status = db->Get(read_options, "key1891", &value);
  if (status.ok()) {
    out << "GET key1891 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11178: put
  db->Put(write_options, "key5517", "value58284");

  // op 11179: delete
  db->Delete(write_options, "key6515");

  // op 11180: delete
  db->Delete(write_options, "key0546");

  // op 11181: get
  status = db->Get(read_options, "key6578", &value);
  if (status.ok()) {
    out << "GET key6578 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11182: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0402", "key10679", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0402, key10679) size=" << scan_result.size() << std::endl;
  }

  // op 11183: put
  db->Put(write_options, "key1383", "value60768");

  // op 11184: delete
  db->Delete(write_options, "key7876");

  // op 11185: put
  db->Put(write_options, "key0339", "value91247");

  // op 11186: put
  db->Put(write_options, "key6732", "value13743");

  // op 11187: put
  db->Put(write_options, "key6873", "value89062");

  // op 11188: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7358", "key10819", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7358, key10819) size=" << scan_result.size() << std::endl;
  }

  // op 11189: delete
  db->Delete(write_options, "key4644");

  // op 11190: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0406", "key11167", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0406, key11167) size=" << scan_result.size() << std::endl;
  }

  // op 11191: put
  db->Put(write_options, "key11228", "value113004");

  // op 11192: delete_range
  status = db->DeleteRange(write_options, "key4962", "key9057");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11193: get
  status = db->Get(read_options, "key2810", &value);
  if (status.ok()) {
    out << "GET key2810 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11194: get
  status = db->Get(read_options, "key8536", &value);
  if (status.ok()) {
    out << "GET key8536 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11195: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1748", "key7393", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1748, key7393) size=" << scan_result.size() << std::endl;
  }

  // op 11196: put
  db->Put(write_options, "key10662", "value75360");

  // op 11197: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 11198: delete_range
  status = db->DeleteRange(write_options, "key3872", "key10749");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11199: get
  status = db->Get(read_options, "key7275", &value);
  if (status.ok()) {
    out << "GET key7275 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11200: put
  db->Put(write_options, "key7286", "value32910");

  // op 11201: put
  db->Put(write_options, "key11029", "value116368");

  // op 11202: delete_range
  status = db->DeleteRange(write_options, "key3659", "key10083");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11203: delete_range
  status = db->DeleteRange(write_options, "key2029", "key3525");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11204: put
  db->Put(write_options, "key5591", "value99218");

  // op 11205: delete_range
  status = db->DeleteRange(write_options, "key6152", "key11415");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11206: delete_range
  status = db->DeleteRange(write_options, "key7488", "key10557");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11207: put
  db->Put(write_options, "key1952", "value12498");

  // op 11208: put
  db->Put(write_options, "key7031", "value90259");

  // op 11209: delete_range
  status = db->DeleteRange(write_options, "key7260", "key9575");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11210: delete
  db->Delete(write_options, "key5677");

  // op 11211: get
  status = db->Get(read_options, "key10467", &value);
  if (status.ok()) {
    out << "GET key10467 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11212: put
  db->Put(write_options, "key11165", "value14145");

  // op 11213: delete_range
  status = db->DeleteRange(write_options, "key7096", "key8851");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11214: get
  status = db->Get(read_options, "key11036", &value);
  if (status.ok()) {
    out << "GET key11036 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11215: put
  db->Put(write_options, "key9147", "value69286");

  // op 11216: delete_range
  status = db->DeleteRange(write_options, "key5108", "key8618");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11217: put
  db->Put(write_options, "key1151", "value54574");

  // op 11218: put
  db->Put(write_options, "key2582", "value5528");

  // op 11219: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4148", "key9189", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4148, key9189) size=" << scan_result.size() << std::endl;
  }

  // op 11220: put
  db->Put(write_options, "key10197", "value11219");

  // op 11221: delete
  db->Delete(write_options, "key1666");

  // op 11222: put
  db->Put(write_options, "key10800", "value82856");

  // op 11223: put
  db->Put(write_options, "key4574", "value106796");

  // op 11224: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0298", "key5268", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0298, key5268) size=" << scan_result.size() << std::endl;
  }

  // op 11225: put
  db->Put(write_options, "key4791", "value24634");

  // op 11226: get
  status = db->Get(read_options, "key8935", &value);
  if (status.ok()) {
    out << "GET key8935 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11227: delete_range
  status = db->DeleteRange(write_options, "key4585", "key10907");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11228: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5999", "key7801", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5999, key7801) size=" << scan_result.size() << std::endl;
  }

  // op 11229: delete
  db->Delete(write_options, "key4367");

  // op 11230: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2896", "key11227", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2896, key11227) size=" << scan_result.size() << std::endl;
  }

  // op 11231: delete_range
  status = db->DeleteRange(write_options, "key1017", "key1175");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11232: get
  status = db->Get(read_options, "key5531", &value);
  if (status.ok()) {
    out << "GET key5531 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11233: get
  status = db->Get(read_options, "key9467", &value);
  if (status.ok()) {
    out << "GET key9467 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11234: get
  status = db->Get(read_options, "key11129", &value);
  if (status.ok()) {
    out << "GET key11129 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11235: put
  db->Put(write_options, "key10866", "value34441");

  // op 11236: put
  db->Put(write_options, "key7109", "value18810");

  // op 11237: get
  status = db->Get(read_options, "key9025", &value);
  if (status.ok()) {
    out << "GET key9025 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11238: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6839", "key8630", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6839, key8630) size=" << scan_result.size() << std::endl;
  }

  // op 11239: put
  db->Put(write_options, "key4179", "value5010");

  // op 11240: delete_range
  status = db->DeleteRange(write_options, "key6406", "key8670");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11241: put
  db->Put(write_options, "key3426", "value115229");

  // op 11242: get
  status = db->Get(read_options, "key5918", &value);
  if (status.ok()) {
    out << "GET key5918 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11243: get
  status = db->Get(read_options, "key10826", &value);
  if (status.ok()) {
    out << "GET key10826 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11244: put
  db->Put(write_options, "key1083", "value60034");

  // op 11245: put
  db->Put(write_options, "key4359", "value27254");

  // op 11246: put
  db->Put(write_options, "key2734", "value57457");

  // op 11247: get
  status = db->Get(read_options, "key9631", &value);
  if (status.ok()) {
    out << "GET key9631 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11248: delete_range
  status = db->DeleteRange(write_options, "key5441", "key7363");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11249: put
  db->Put(write_options, "key8918", "value15957");

  // op 11250: put
  db->Put(write_options, "key10880", "value101295");

  // op 11251: get
  status = db->Get(read_options, "key10800", &value);
  if (status.ok()) {
    out << "GET key10800 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11252: get
  status = db->Get(read_options, "key5822", &value);
  if (status.ok()) {
    out << "GET key5822 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11253: put
  db->Put(write_options, "key4278", "value3700");

  // op 11254: get
  status = db->Get(read_options, "key3924", &value);
  if (status.ok()) {
    out << "GET key3924 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11255: delete_range
  status = db->DeleteRange(write_options, "key7824", "key9471");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11256: put
  db->Put(write_options, "key5539", "value74257");

  // op 11257: put
  db->Put(write_options, "key8754", "value116565");

  // op 11258: get
  status = db->Get(read_options, "key9132", &value);
  if (status.ok()) {
    out << "GET key9132 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11259: delete_range
  status = db->DeleteRange(write_options, "key5029", "key6592");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11260: get
  status = db->Get(read_options, "key6766", &value);
  if (status.ok()) {
    out << "GET key6766 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11261: get
  status = db->Get(read_options, "key6606", &value);
  if (status.ok()) {
    out << "GET key6606 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11262: delete_range
  status = db->DeleteRange(write_options, "key7121", "key11432");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11263: put
  db->Put(write_options, "key3758", "value82586");

  // op 11264: get
  status = db->Get(read_options, "key3904", &value);
  if (status.ok()) {
    out << "GET key3904 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11265: delete_range
  status = db->DeleteRange(write_options, "key2378", "key10184");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11266: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4083", "key6732", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4083, key6732) size=" << scan_result.size() << std::endl;
  }

  // op 11267: get
  status = db->Get(read_options, "key0374", &value);
  if (status.ok()) {
    out << "GET key0374 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11268: get
  status = db->Get(read_options, "key3568", &value);
  if (status.ok()) {
    out << "GET key3568 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11269: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5270", "key5770", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5270, key5770) size=" << scan_result.size() << std::endl;
  }

  // op 11270: put
  db->Put(write_options, "key3559", "value4973");

  // op 11271: get
  status = db->Get(read_options, "key11566", &value);
  if (status.ok()) {
    out << "GET key11566 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11272: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7023", "key7379", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7023, key7379) size=" << scan_result.size() << std::endl;
  }

  // op 11273: put
  db->Put(write_options, "key2446", "value25792");

  // op 11274: put
  db->Put(write_options, "key5670", "value78720");

  // op 11275: put
  db->Put(write_options, "key5581", "value51294");

  // op 11276: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2209", "key5577", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2209, key5577) size=" << scan_result.size() << std::endl;
  }

  // op 11277: delete_range
  status = db->DeleteRange(write_options, "key0500", "key7755");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11278: put
  db->Put(write_options, "key3870", "value94378");

  // op 11279: put
  db->Put(write_options, "key3965", "value9317");

  // op 11280: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9575", "key11310", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9575, key11310) size=" << scan_result.size() << std::endl;
  }

  // op 11281: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 11282: put
  db->Put(write_options, "key7630", "value99052");

  // op 11283: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0834", "key11091", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0834, key11091) size=" << scan_result.size() << std::endl;
  }

  // op 11284: put
  db->Put(write_options, "key1913", "value34872");

  // op 11285: get
  status = db->Get(read_options, "key9869", &value);
  if (status.ok()) {
    out << "GET key9869 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11286: get
  status = db->Get(read_options, "key4861", &value);
  if (status.ok()) {
    out << "GET key4861 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11287: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 11288: put
  db->Put(write_options, "key5930", "value13989");

  // op 11289: delete
  db->Delete(write_options, "key11367");

  // op 11290: get
  status = db->Get(read_options, "key6371", &value);
  if (status.ok()) {
    out << "GET key6371 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11291: put
  db->Put(write_options, "key8294", "value37187");

  // op 11292: delete_range
  status = db->DeleteRange(write_options, "key10030", "key10347");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11293: put
  db->Put(write_options, "key10344", "value111921");

  // op 11294: put
  db->Put(write_options, "key9041", "value57975");

  // op 11295: put
  db->Put(write_options, "key0651", "value3247");

  // op 11296: get
  status = db->Get(read_options, "key5396", &value);
  if (status.ok()) {
    out << "GET key5396 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11297: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6558", "key8703", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6558, key8703) size=" << scan_result.size() << std::endl;
  }

  // op 11298: delete_range
  status = db->DeleteRange(write_options, "key0606", "key5966");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11299: put
  db->Put(write_options, "key9037", "value77167");

  // op 11300: get
  status = db->Get(read_options, "key3488", &value);
  if (status.ok()) {
    out << "GET key3488 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11301: delete_range
  status = db->DeleteRange(write_options, "key0665", "key7993");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11302: put
  db->Put(write_options, "key9027", "value26212");

  // op 11303: delete
  db->Delete(write_options, "key8357");

  // op 11304: delete
  db->Delete(write_options, "key10470");

  // op 11305: get
  status = db->Get(read_options, "key1986", &value);
  if (status.ok()) {
    out << "GET key1986 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11306: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2060", "key8862", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2060, key8862) size=" << scan_result.size() << std::endl;
  }

  // op 11307: put
  db->Put(write_options, "key5701", "value59596");

  // op 11308: put
  db->Put(write_options, "key9744", "value97148");

  // op 11309: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1808", "key1928", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1808, key1928) size=" << scan_result.size() << std::endl;
  }

  // op 11310: put
  db->Put(write_options, "key9859", "value16825");

  // op 11311: put
  db->Put(write_options, "key4280", "value11540");

  // op 11312: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 11313: delete
  db->Delete(write_options, "key1615");

  // op 11314: get
  status = db->Get(read_options, "key10410", &value);
  if (status.ok()) {
    out << "GET key10410 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11315: put
  db->Put(write_options, "key6981", "value82419");

  // op 11316: put
  db->Put(write_options, "key2760", "value115331");

  // op 11317: get
  status = db->Get(read_options, "key2328", &value);
  if (status.ok()) {
    out << "GET key2328 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11318: put
  db->Put(write_options, "key2221", "value80472");

  // op 11319: delete_range
  status = db->DeleteRange(write_options, "key2100", "key3585");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11320: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4029", "key6465", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4029, key6465) size=" << scan_result.size() << std::endl;
  }

  // op 11321: put
  db->Put(write_options, "key11495", "value3690");

  // op 11322: put
  db->Put(write_options, "key3819", "value47071");

  // op 11323: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2226", "key3188", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2226, key3188) size=" << scan_result.size() << std::endl;
  }

  // op 11324: put
  db->Put(write_options, "key5107", "value44367");

  // op 11325: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6340", "key10918", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6340, key10918) size=" << scan_result.size() << std::endl;
  }

  // op 11326: put
  db->Put(write_options, "key10862", "value56449");

  // op 11327: delete
  db->Delete(write_options, "key11424");

  // op 11328: put
  db->Put(write_options, "key7911", "value9018");

  // op 11329: put
  db->Put(write_options, "key4409", "value105877");

  // op 11330: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2429", "key8026", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2429, key8026) size=" << scan_result.size() << std::endl;
  }

  // op 11331: delete_range
  status = db->DeleteRange(write_options, "key4640", "key4952");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11332: put
  db->Put(write_options, "key7779", "value57965");

  // op 11333: put
  db->Put(write_options, "key2004", "value64845");

  // op 11334: put
  db->Put(write_options, "key0498", "value20251");

  // op 11335: delete_range
  status = db->DeleteRange(write_options, "key2160", "key8873");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11336: delete
  db->Delete(write_options, "key3079");

  // op 11337: put
  db->Put(write_options, "key6620", "value83625");

  // op 11338: get
  status = db->Get(read_options, "key11559", &value);
  if (status.ok()) {
    out << "GET key11559 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11339: delete
  db->Delete(write_options, "key2618");

  // op 11340: put
  db->Put(write_options, "key7752", "value5304");

  // op 11341: put
  db->Put(write_options, "key0356", "value82990");

  // op 11342: get
  status = db->Get(read_options, "key2581", &value);
  if (status.ok()) {
    out << "GET key2581 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11343: delete
  db->Delete(write_options, "key9433");

  // op 11344: delete_range
  status = db->DeleteRange(write_options, "key4057", "key7290");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11345: get
  status = db->Get(read_options, "key4782", &value);
  if (status.ok()) {
    out << "GET key4782 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11346: put
  db->Put(write_options, "key1282", "value96273");

  // op 11347: put
  db->Put(write_options, "key5023", "value112749");

  // op 11348: delete_range
  status = db->DeleteRange(write_options, "key0204", "key1724");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11349: put
  db->Put(write_options, "key0485", "value35735");

  // op 11350: delete
  db->Delete(write_options, "key1433");

  // op 11351: put
  db->Put(write_options, "key6882", "value44460");

  // op 11352: put
  db->Put(write_options, "key1452", "value112779");

  // op 11353: get
  status = db->Get(read_options, "key7229", &value);
  if (status.ok()) {
    out << "GET key7229 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11354: delete
  db->Delete(write_options, "key5148");

  // op 11355: put
  db->Put(write_options, "key10877", "value72206");

  // op 11356: get
  status = db->Get(read_options, "key5015", &value);
  if (status.ok()) {
    out << "GET key5015 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11357: put
  db->Put(write_options, "key2421", "value80088");

  // op 11358: get
  status = db->Get(read_options, "key2804", &value);
  if (status.ok()) {
    out << "GET key2804 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11359: put
  db->Put(write_options, "key4432", "value49229");

  // op 11360: put
  db->Put(write_options, "key5393", "value61615");

  // op 11361: delete
  db->Delete(write_options, "key5054");

  // op 11362: get
  status = db->Get(read_options, "key11169", &value);
  if (status.ok()) {
    out << "GET key11169 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11363: put
  db->Put(write_options, "key3098", "value94507");

  // op 11364: put
  db->Put(write_options, "key3710", "value100151");

  // op 11365: put
  db->Put(write_options, "key4767", "value10956");

  // op 11366: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 11367: get
  status = db->Get(read_options, "key5706", &value);
  if (status.ok()) {
    out << "GET key5706 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11368: put
  db->Put(write_options, "key6125", "value79197");

  // op 11369: put
  db->Put(write_options, "key1890", "value99345");

  // op 11370: put
  db->Put(write_options, "key9451", "value58552");

  // op 11371: put
  db->Put(write_options, "key3503", "value37842");

  // op 11372: get
  status = db->Get(read_options, "key5585", &value);
  if (status.ok()) {
    out << "GET key5585 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11373: delete
  db->Delete(write_options, "key5662");

  // op 11374: put
  db->Put(write_options, "key5973", "value90277");

  // op 11375: delete_range
  status = db->DeleteRange(write_options, "key3922", "key10283");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11376: put
  db->Put(write_options, "key0462", "value17476");

  // op 11377: put
  db->Put(write_options, "key6156", "value102778");

  // op 11378: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 11379: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 11380: get
  status = db->Get(read_options, "key7064", &value);
  if (status.ok()) {
    out << "GET key7064 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11381: get
  status = db->Get(read_options, "key6974", &value);
  if (status.ok()) {
    out << "GET key6974 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11382: put
  db->Put(write_options, "key0019", "value17599");

  // op 11383: delete_range
  status = db->DeleteRange(write_options, "key6105", "key9416");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11384: put
  db->Put(write_options, "key7577", "value94637");

  // op 11385: put
  db->Put(write_options, "key0114", "value97352");

  // op 11386: put
  db->Put(write_options, "key1427", "value78885");

  // op 11387: get
  status = db->Get(read_options, "key9957", &value);
  if (status.ok()) {
    out << "GET key9957 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11388: delete
  db->Delete(write_options, "key4123");

  // op 11389: get
  status = db->Get(read_options, "key6470", &value);
  if (status.ok()) {
    out << "GET key6470 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11390: put
  db->Put(write_options, "key2693", "value29909");

  // op 11391: put
  db->Put(write_options, "key5217", "value64181");

  // op 11392: put
  db->Put(write_options, "key3767", "value10403");

  // op 11393: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8347", "key10904", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8347, key10904) size=" << scan_result.size() << std::endl;
  }

  // op 11394: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2000", "key2793", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2000, key2793) size=" << scan_result.size() << std::endl;
  }

  // op 11395: get
  status = db->Get(read_options, "key4566", &value);
  if (status.ok()) {
    out << "GET key4566 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11396: get
  status = db->Get(read_options, "key1522", &value);
  if (status.ok()) {
    out << "GET key1522 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11397: get
  status = db->Get(read_options, "key0707", &value);
  if (status.ok()) {
    out << "GET key0707 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11398: delete_range
  status = db->DeleteRange(write_options, "key4105", "key8461");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11399: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7977", "key10638", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7977, key10638) size=" << scan_result.size() << std::endl;
  }

  // op 11400: put
  db->Put(write_options, "key10043", "value19050");

  // op 11401: delete
  db->Delete(write_options, "key0862");

  // op 11402: delete_range
  status = db->DeleteRange(write_options, "key4299", "key4797");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11403: put
  db->Put(write_options, "key3470", "value115304");

  // op 11404: put
  db->Put(write_options, "key6603", "value61457");

  // op 11405: put
  db->Put(write_options, "key5004", "value21333");

  // op 11406: put
  db->Put(write_options, "key10661", "value84507");

  // op 11407: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0472", "key5283", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0472, key5283) size=" << scan_result.size() << std::endl;
  }

  // op 11408: put
  db->Put(write_options, "key8648", "value110104");

  // op 11409: put
  db->Put(write_options, "key10287", "value13020");

  // op 11410: get
  status = db->Get(read_options, "key3909", &value);
  if (status.ok()) {
    out << "GET key3909 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11411: get
  status = db->Get(read_options, "key8119", &value);
  if (status.ok()) {
    out << "GET key8119 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11412: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 11413: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2876", "key5914", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2876, key5914) size=" << scan_result.size() << std::endl;
  }

  // op 11414: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0099", "key8087", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0099, key8087) size=" << scan_result.size() << std::endl;
  }

  // op 11415: put
  db->Put(write_options, "key0614", "value105948");

  // op 11416: put
  db->Put(write_options, "key10469", "value105385");

  // op 11417: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2211", "key10060", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2211, key10060) size=" << scan_result.size() << std::endl;
  }

  // op 11418: put
  db->Put(write_options, "key3983", "value0643");

  // op 11419: put
  db->Put(write_options, "key7965", "value77024");

  // op 11420: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 11421: delete_range
  status = db->DeleteRange(write_options, "key5524", "key9229");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11422: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 11423: get
  status = db->Get(read_options, "key10131", &value);
  if (status.ok()) {
    out << "GET key10131 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11424: delete_range
  status = db->DeleteRange(write_options, "key5464", "key5829");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11425: put
  db->Put(write_options, "key1699", "value20314");

  // op 11426: put
  db->Put(write_options, "key3293", "value16202");

  // op 11427: put
  db->Put(write_options, "key3657", "value104037");

  // op 11428: get
  status = db->Get(read_options, "key5145", &value);
  if (status.ok()) {
    out << "GET key5145 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11429: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2076", "key7391", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2076, key7391) size=" << scan_result.size() << std::endl;
  }

  // op 11430: get
  status = db->Get(read_options, "key4507", &value);
  if (status.ok()) {
    out << "GET key4507 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11431: delete
  db->Delete(write_options, "key3402");

  // op 11432: put
  db->Put(write_options, "key11350", "value29987");

  // op 11433: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1834", "key8492", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1834, key8492) size=" << scan_result.size() << std::endl;
  }

  // op 11434: put
  db->Put(write_options, "key8322", "value65655");

  // op 11435: delete_range
  status = db->DeleteRange(write_options, "key10515", "key11011");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11436: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9007", "key11009", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9007, key11009) size=" << scan_result.size() << std::endl;
  }

  // op 11437: put
  db->Put(write_options, "key6231", "value5884");

  // op 11438: get
  status = db->Get(read_options, "key2329", &value);
  if (status.ok()) {
    out << "GET key2329 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11439: get
  status = db->Get(read_options, "key11188", &value);
  if (status.ok()) {
    out << "GET key11188 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11440: delete
  db->Delete(write_options, "key3345");

  // op 11441: put
  db->Put(write_options, "key3640", "value80043");

  // op 11442: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 11443: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7537", "key8254", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7537, key8254) size=" << scan_result.size() << std::endl;
  }

  // op 11444: put
  db->Put(write_options, "key7992", "value69865");

  // op 11445: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9406", "key9944", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9406, key9944) size=" << scan_result.size() << std::endl;
  }

  // op 11446: put
  db->Put(write_options, "key0779", "value26100");

  // op 11447: put
  db->Put(write_options, "key6211", "value110205");

  // op 11448: put
  db->Put(write_options, "key0229", "value73146");

  // op 11449: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4713", "key11679", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4713, key11679) size=" << scan_result.size() << std::endl;
  }

  // op 11450: delete
  db->Delete(write_options, "key3176");

  // op 11451: put
  db->Put(write_options, "key6873", "value93635");

  // op 11452: put
  db->Put(write_options, "key8180", "value50595");

  // op 11453: get
  status = db->Get(read_options, "key3511", &value);
  if (status.ok()) {
    out << "GET key3511 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11454: put
  db->Put(write_options, "key0983", "value74059");

  // op 11455: get
  status = db->Get(read_options, "key6656", &value);
  if (status.ok()) {
    out << "GET key6656 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11456: delete
  db->Delete(write_options, "key0831");

  // op 11457: put
  db->Put(write_options, "key7928", "value80607");

  // op 11458: delete
  db->Delete(write_options, "key9956");

  // op 11459: put
  db->Put(write_options, "key8199", "value82725");

  // op 11460: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0842", "key1186", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0842, key1186) size=" << scan_result.size() << std::endl;
  }

  // op 11461: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7444", "key8142", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7444, key8142) size=" << scan_result.size() << std::endl;
  }

  // op 11462: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5753", "key10998", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5753, key10998) size=" << scan_result.size() << std::endl;
  }

  // op 11463: put
  db->Put(write_options, "key0632", "value61059");

  // op 11464: delete_range
  status = db->DeleteRange(write_options, "key8896", "key11016");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11465: put
  db->Put(write_options, "key11069", "value33132");

  // op 11466: put
  db->Put(write_options, "key9401", "value26510");

  // op 11467: get
  status = db->Get(read_options, "key5642", &value);
  if (status.ok()) {
    out << "GET key5642 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11468: put
  db->Put(write_options, "key4499", "value14352");

  // op 11469: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0250", "key10428", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0250, key10428) size=" << scan_result.size() << std::endl;
  }

  // op 11470: delete
  db->Delete(write_options, "key5852");

  // op 11471: get
  status = db->Get(read_options, "key0814", &value);
  if (status.ok()) {
    out << "GET key0814 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11472: put
  db->Put(write_options, "key0876", "value44784");

  // op 11473: put
  db->Put(write_options, "key7649", "value62993");

  // op 11474: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 11475: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1634", "key11629", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1634, key11629) size=" << scan_result.size() << std::endl;
  }

  // op 11476: put
  db->Put(write_options, "key4629", "value95286");

  // op 11477: put
  db->Put(write_options, "key0338", "value63585");

  // op 11478: put
  db->Put(write_options, "key1465", "value49630");

  // op 11479: put
  db->Put(write_options, "key3995", "value68410");

  // op 11480: get
  status = db->Get(read_options, "key7329", &value);
  if (status.ok()) {
    out << "GET key7329 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11481: delete_range
  status = db->DeleteRange(write_options, "key1084", "key8033");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11482: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1023", "key1574", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1023, key1574) size=" << scan_result.size() << std::endl;
  }

  // op 11483: delete_range
  status = db->DeleteRange(write_options, "key6294", "key9764");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11484: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2987", "key11233", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2987, key11233) size=" << scan_result.size() << std::endl;
  }

  // op 11485: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4918", "key6210", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4918, key6210) size=" << scan_result.size() << std::endl;
  }

  // op 11486: get
  status = db->Get(read_options, "key8983", &value);
  if (status.ok()) {
    out << "GET key8983 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11487: put
  db->Put(write_options, "key3726", "value71357");

  // op 11488: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 11489: put
  db->Put(write_options, "key6600", "value20972");

  // op 11490: put
  db->Put(write_options, "key10756", "value61677");

  // op 11491: put
  db->Put(write_options, "key5321", "value56009");

  // op 11492: put
  db->Put(write_options, "key7543", "value78962");

  // op 11493: put
  db->Put(write_options, "key0253", "value95303");

  // op 11494: delete_range
  status = db->DeleteRange(write_options, "key4398", "key10396");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11495: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10153", "key10176", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10153, key10176) size=" << scan_result.size() << std::endl;
  }

  // op 11496: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 11497: put
  db->Put(write_options, "key2460", "value116039");

  // op 11498: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1893", "key3061", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1893, key3061) size=" << scan_result.size() << std::endl;
  }

  // op 11499: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 11500: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1971", "key5213", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1971, key5213) size=" << scan_result.size() << std::endl;
  }

  // op 11501: put
  db->Put(write_options, "key1197", "value69924");

  // op 11502: delete_range
  status = db->DeleteRange(write_options, "key5785", "key7941");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11503: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3128", "key6040", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3128, key6040) size=" << scan_result.size() << std::endl;
  }

  // op 11504: get
  status = db->Get(read_options, "key6998", &value);
  if (status.ok()) {
    out << "GET key6998 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11505: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6417", "key11279", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6417, key11279) size=" << scan_result.size() << std::endl;
  }

  // op 11506: put
  db->Put(write_options, "key0282", "value64585");

  // op 11507: put
  db->Put(write_options, "key5323", "value46400");

  // op 11508: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1783", "key11225", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1783, key11225) size=" << scan_result.size() << std::endl;
  }

  // op 11509: put
  db->Put(write_options, "key7207", "value14848");

  // op 11510: put
  db->Put(write_options, "key10307", "value96464");

  // op 11511: put
  db->Put(write_options, "key5459", "value25627");

  // op 11512: put
  db->Put(write_options, "key2166", "value72096");

  // op 11513: put
  db->Put(write_options, "key2023", "value4768");

  // op 11514: get
  status = db->Get(read_options, "key9906", &value);
  if (status.ok()) {
    out << "GET key9906 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11515: put
  db->Put(write_options, "key7648", "value87243");

  // op 11516: delete_range
  status = db->DeleteRange(write_options, "key4159", "key9990");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11517: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 11518: put
  db->Put(write_options, "key1212", "value61751");

  // op 11519: put
  db->Put(write_options, "key0631", "value91198");

  // op 11520: delete_range
  status = db->DeleteRange(write_options, "key6426", "key8714");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11521: put
  db->Put(write_options, "key7513", "value23742");

  // op 11522: get
  status = db->Get(read_options, "key1275", &value);
  if (status.ok()) {
    out << "GET key1275 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11523: get
  status = db->Get(read_options, "key3796", &value);
  if (status.ok()) {
    out << "GET key3796 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11524: get
  status = db->Get(read_options, "key8837", &value);
  if (status.ok()) {
    out << "GET key8837 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11525: delete_range
  status = db->DeleteRange(write_options, "key1643", "key6929");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11526: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8763", "key11221", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8763, key11221) size=" << scan_result.size() << std::endl;
  }

  // op 11527: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2055", "key7191", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2055, key7191) size=" << scan_result.size() << std::endl;
  }

  // op 11528: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 11529: delete
  db->Delete(write_options, "key10794");

  // op 11530: delete
  db->Delete(write_options, "key9417");

  // op 11531: get
  status = db->Get(read_options, "key2782", &value);
  if (status.ok()) {
    out << "GET key2782 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11532: get
  status = db->Get(read_options, "key7086", &value);
  if (status.ok()) {
    out << "GET key7086 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11533: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 11534: put
  db->Put(write_options, "key1481", "value65938");

  // op 11535: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0445", "key0706", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0445, key0706) size=" << scan_result.size() << std::endl;
  }

  // op 11536: delete_range
  status = db->DeleteRange(write_options, "key5418", "key10888");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11537: delete_range
  status = db->DeleteRange(write_options, "key6450", "key7670");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11538: put
  db->Put(write_options, "key7300", "value104319");

  // op 11539: put
  db->Put(write_options, "key0430", "value34251");

  // op 11540: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0022", "key8953", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0022, key8953) size=" << scan_result.size() << std::endl;
  }

  // op 11541: put
  db->Put(write_options, "key2035", "value66380");

  // op 11542: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1764", "key7744", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1764, key7744) size=" << scan_result.size() << std::endl;
  }

  // op 11543: put
  db->Put(write_options, "key8086", "value54546");

  // op 11544: put
  db->Put(write_options, "key10130", "value76632");

  // op 11545: put
  db->Put(write_options, "key8175", "value18829");

  // op 11546: delete_range
  status = db->DeleteRange(write_options, "key2747", "key8675");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11547: delete_range
  status = db->DeleteRange(write_options, "key3855", "key11030");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11548: put
  db->Put(write_options, "key9781", "value64008");

  // op 11549: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4950", "key8064", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4950, key8064) size=" << scan_result.size() << std::endl;
  }

  // op 11550: delete
  db->Delete(write_options, "key5881");

  // op 11551: get
  status = db->Get(read_options, "key1998", &value);
  if (status.ok()) {
    out << "GET key1998 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11552: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 11553: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1009", "key2364", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1009, key2364) size=" << scan_result.size() << std::endl;
  }

  // op 11554: delete
  db->Delete(write_options, "key9733");

  // op 11555: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0537", "key8019", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0537, key8019) size=" << scan_result.size() << std::endl;
  }

  // op 11556: get
  status = db->Get(read_options, "key10814", &value);
  if (status.ok()) {
    out << "GET key10814 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11557: delete_range
  status = db->DeleteRange(write_options, "key8389", "key10817");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11558: delete
  db->Delete(write_options, "key7234");

  // op 11559: delete
  db->Delete(write_options, "key5446");

  // op 11560: put
  db->Put(write_options, "key2158", "value77733");

  // op 11561: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3662", "key9119", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3662, key9119) size=" << scan_result.size() << std::endl;
  }

  // op 11562: put
  db->Put(write_options, "key8976", "value62281");

  // op 11563: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2500", "key10199", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2500, key10199) size=" << scan_result.size() << std::endl;
  }

  // op 11564: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3704", "key4971", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3704, key4971) size=" << scan_result.size() << std::endl;
  }

  // op 11565: delete_range
  status = db->DeleteRange(write_options, "key0130", "key3709");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11566: delete
  db->Delete(write_options, "key9597");

  // op 11567: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 11568: put
  db->Put(write_options, "key9415", "value59556");

  // op 11569: get
  status = db->Get(read_options, "key0741", &value);
  if (status.ok()) {
    out << "GET key0741 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11570: put
  db->Put(write_options, "key1783", "value58919");

  // op 11571: delete_range
  status = db->DeleteRange(write_options, "key3221", "key9962");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11572: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5009", "key8672", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5009, key8672) size=" << scan_result.size() << std::endl;
  }

  // op 11573: delete_range
  status = db->DeleteRange(write_options, "key5548", "key6240");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11574: put
  db->Put(write_options, "key5964", "value83844");

  // op 11575: put
  db->Put(write_options, "key0761", "value32012");

  // op 11576: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0483", "key4302", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0483, key4302) size=" << scan_result.size() << std::endl;
  }

  // op 11577: put
  db->Put(write_options, "key4921", "value56997");

  // op 11578: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3909", "key10678", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3909, key10678) size=" << scan_result.size() << std::endl;
  }

  // op 11579: put
  db->Put(write_options, "key8307", "value5396");

  // op 11580: put
  db->Put(write_options, "key0938", "value40147");

  // op 11581: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3059", "key5512", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3059, key5512) size=" << scan_result.size() << std::endl;
  }

  // op 11582: get
  status = db->Get(read_options, "key1615", &value);
  if (status.ok()) {
    out << "GET key1615 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11583: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 11584: get
  status = db->Get(read_options, "key2073", &value);
  if (status.ok()) {
    out << "GET key2073 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11585: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3316", "key10966", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3316, key10966) size=" << scan_result.size() << std::endl;
  }

  // op 11586: put
  db->Put(write_options, "key2471", "value99393");

  // op 11587: put
  db->Put(write_options, "key1341", "value51503");

  // op 11588: delete_range
  status = db->DeleteRange(write_options, "key0012", "key9778");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11589: delete_range
  status = db->DeleteRange(write_options, "key1760", "key6320");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11590: put
  db->Put(write_options, "key6688", "value8057");

  // op 11591: delete
  db->Delete(write_options, "key4308");

  // op 11592: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7982", "key9484", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7982, key9484) size=" << scan_result.size() << std::endl;
  }

  // op 11593: put
  db->Put(write_options, "key4315", "value99009");

  // op 11594: put
  db->Put(write_options, "key2395", "value28585");

  // op 11595: delete_range
  status = db->DeleteRange(write_options, "key3648", "key5544");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11596: put
  db->Put(write_options, "key2245", "value76135");

  // op 11597: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2546", "key9047", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2546, key9047) size=" << scan_result.size() << std::endl;
  }

  // op 11598: delete
  db->Delete(write_options, "key3626");

  // op 11599: put
  db->Put(write_options, "key9795", "value99759");

  // op 11600: get
  status = db->Get(read_options, "key10239", &value);
  if (status.ok()) {
    out << "GET key10239 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11601: put
  db->Put(write_options, "key1504", "value97793");

  // op 11602: get
  status = db->Get(read_options, "key8922", &value);
  if (status.ok()) {
    out << "GET key8922 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11603: get
  status = db->Get(read_options, "key1876", &value);
  if (status.ok()) {
    out << "GET key1876 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11604: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1412", "key3348", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1412, key3348) size=" << scan_result.size() << std::endl;
  }

  // op 11605: get
  status = db->Get(read_options, "key7003", &value);
  if (status.ok()) {
    out << "GET key7003 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11606: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5332", "key10926", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5332, key10926) size=" << scan_result.size() << std::endl;
  }

  // op 11607: get
  status = db->Get(read_options, "key1577", &value);
  if (status.ok()) {
    out << "GET key1577 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11608: get
  status = db->Get(read_options, "key6896", &value);
  if (status.ok()) {
    out << "GET key6896 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11609: delete
  db->Delete(write_options, "key2285");

  // op 11610: get
  status = db->Get(read_options, "key6018", &value);
  if (status.ok()) {
    out << "GET key6018 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11611: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2023", "key6475", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2023, key6475) size=" << scan_result.size() << std::endl;
  }

  // op 11612: put
  db->Put(write_options, "key6074", "value116009");

  // op 11613: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 11614: put
  db->Put(write_options, "key0562", "value28796");

  // op 11615: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0951", "key7465", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0951, key7465) size=" << scan_result.size() << std::endl;
  }

  // op 11616: get
  status = db->Get(read_options, "key11369", &value);
  if (status.ok()) {
    out << "GET key11369 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11617: put
  db->Put(write_options, "key7092", "value61382");

  // op 11618: get
  status = db->Get(read_options, "key4035", &value);
  if (status.ok()) {
    out << "GET key4035 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11619: get
  status = db->Get(read_options, "key9802", &value);
  if (status.ok()) {
    out << "GET key9802 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11620: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2186", "key6326", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2186, key6326) size=" << scan_result.size() << std::endl;
  }

  // op 11621: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4579", "key9835", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4579, key9835) size=" << scan_result.size() << std::endl;
  }

  // op 11622: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6811", "key8737", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6811, key8737) size=" << scan_result.size() << std::endl;
  }

  // op 11623: delete
  db->Delete(write_options, "key9715");

  // op 11624: put
  db->Put(write_options, "key8925", "value30652");

  // op 11625: delete_range
  status = db->DeleteRange(write_options, "key0653", "key10007");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11626: delete
  db->Delete(write_options, "key3955");

  // op 11627: delete_range
  status = db->DeleteRange(write_options, "key5759", "key6950");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11628: put
  db->Put(write_options, "key2225", "value45029");

  // op 11629: get
  status = db->Get(read_options, "key3830", &value);
  if (status.ok()) {
    out << "GET key3830 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11630: get
  status = db->Get(read_options, "key1135", &value);
  if (status.ok()) {
    out << "GET key1135 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11631: put
  db->Put(write_options, "key6398", "value110127");

  // op 11632: put
  db->Put(write_options, "key2932", "value14029");

  // op 11633: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0461", "key5611", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0461, key5611) size=" << scan_result.size() << std::endl;
  }

  // op 11634: delete_range
  status = db->DeleteRange(write_options, "key4148", "key4771");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11635: get
  status = db->Get(read_options, "key0516", &value);
  if (status.ok()) {
    out << "GET key0516 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11636: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7237", "key9782", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7237, key9782) size=" << scan_result.size() << std::endl;
  }

  // op 11637: delete
  db->Delete(write_options, "key3124");

  // op 11638: put
  db->Put(write_options, "key10828", "value41941");

  // op 11639: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1861", "key5243", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1861, key5243) size=" << scan_result.size() << std::endl;
  }

  // op 11640: put
  db->Put(write_options, "key8433", "value84862");

  // op 11641: get
  status = db->Get(read_options, "key4396", &value);
  if (status.ok()) {
    out << "GET key4396 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11642: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4931", "key8281", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4931, key8281) size=" << scan_result.size() << std::endl;
  }

  // op 11643: get
  status = db->Get(read_options, "key9723", &value);
  if (status.ok()) {
    out << "GET key9723 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11644: delete
  db->Delete(write_options, "key0114");

  // op 11645: put
  db->Put(write_options, "key5134", "value9007");

  // op 11646: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6121", "key10196", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6121, key10196) size=" << scan_result.size() << std::endl;
  }

  // op 11647: put
  db->Put(write_options, "key2422", "value6720");

  // op 11648: delete_range
  status = db->DeleteRange(write_options, "key5910", "key8305");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11649: delete
  db->Delete(write_options, "key5088");

  // op 11650: delete
  db->Delete(write_options, "key9056");

  // op 11651: put
  db->Put(write_options, "key8444", "value76392");

  // op 11652: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0039", "key0414", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0039, key0414) size=" << scan_result.size() << std::endl;
  }

  // op 11653: put
  db->Put(write_options, "key6558", "value102143");

  // op 11654: put
  db->Put(write_options, "key8819", "value80550");

  // op 11655: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3678", "key3942", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3678, key3942) size=" << scan_result.size() << std::endl;
  }

  // op 11656: put
  db->Put(write_options, "key5770", "value89737");

  // op 11657: get
  status = db->Get(read_options, "key3137", &value);
  if (status.ok()) {
    out << "GET key3137 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11658: get
  status = db->Get(read_options, "key2559", &value);
  if (status.ok()) {
    out << "GET key2559 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11659: get
  status = db->Get(read_options, "key1251", &value);
  if (status.ok()) {
    out << "GET key1251 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11660: put
  db->Put(write_options, "key3435", "value36943");

  // op 11661: put
  db->Put(write_options, "key8026", "value10262");

  // op 11662: put
  db->Put(write_options, "key10265", "value58555");

  // op 11663: put
  db->Put(write_options, "key2399", "value4853");

  // op 11664: delete_range
  status = db->DeleteRange(write_options, "key4719", "key10179");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11665: get
  status = db->Get(read_options, "key4349", &value);
  if (status.ok()) {
    out << "GET key4349 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11666: delete
  db->Delete(write_options, "key5673");

  // op 11667: put
  db->Put(write_options, "key7305", "value19048");

  // op 11668: delete_range
  status = db->DeleteRange(write_options, "key5098", "key8787");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11669: delete_range
  status = db->DeleteRange(write_options, "key2010", "key7973");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11670: get
  status = db->Get(read_options, "key6068", &value);
  if (status.ok()) {
    out << "GET key6068 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11671: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2819", "key6261", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2819, key6261) size=" << scan_result.size() << std::endl;
  }

  // op 11672: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4245", "key4439", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4245, key4439) size=" << scan_result.size() << std::endl;
  }

  // op 11673: delete_range
  status = db->DeleteRange(write_options, "key3395", "key4547");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11674: get
  status = db->Get(read_options, "key5833", &value);
  if (status.ok()) {
    out << "GET key5833 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11675: put
  db->Put(write_options, "key3057", "value103106");

  // op 11676: put
  db->Put(write_options, "key1472", "value75702");

  // op 11677: delete_range
  status = db->DeleteRange(write_options, "key1512", "key7404");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11678: get
  status = db->Get(read_options, "key9708", &value);
  if (status.ok()) {
    out << "GET key9708 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11679: put
  db->Put(write_options, "key8760", "value81371");

  // op 11680: put
  db->Put(write_options, "key9145", "value16670");

  // op 11681: get
  status = db->Get(read_options, "key6830", &value);
  if (status.ok()) {
    out << "GET key6830 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11682: put
  db->Put(write_options, "key11517", "value91332");

  // op 11683: put
  db->Put(write_options, "key1679", "value106070");

  // op 11684: delete
  db->Delete(write_options, "key8409");

  // op 11685: get
  status = db->Get(read_options, "key1332", &value);
  if (status.ok()) {
    out << "GET key1332 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11686: put
  db->Put(write_options, "key8244", "value20330");

  // op 11687: delete_range
  status = db->DeleteRange(write_options, "key5465", "key5906");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11688: get
  status = db->Get(read_options, "key7674", &value);
  if (status.ok()) {
    out << "GET key7674 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11689: delete_range
  status = db->DeleteRange(write_options, "key2161", "key7207");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11690: get
  status = db->Get(read_options, "key9406", &value);
  if (status.ok()) {
    out << "GET key9406 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11691: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9753", "key11106", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9753, key11106) size=" << scan_result.size() << std::endl;
  }

  // op 11692: get
  status = db->Get(read_options, "key8895", &value);
  if (status.ok()) {
    out << "GET key8895 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11693: put
  db->Put(write_options, "key7696", "value75589");

  // op 11694: get
  status = db->Get(read_options, "key1285", &value);
  if (status.ok()) {
    out << "GET key1285 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11695: delete
  db->Delete(write_options, "key4874");

  // op 11696: put
  db->Put(write_options, "key4217", "value107969");

  // op 11697: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 11698: put
  db->Put(write_options, "key3146", "value27653");

  // op 11699: get
  status = db->Get(read_options, "key6212", &value);
  if (status.ok()) {
    out << "GET key6212 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11700: put
  db->Put(write_options, "key2168", "value28998");

  // op 11701: put
  db->Put(write_options, "key2839", "value28701");

  // op 11702: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 11703: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2205", "key10525", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2205, key10525) size=" << scan_result.size() << std::endl;
  }

  // op 11704: put
  db->Put(write_options, "key4186", "value89820");

  // op 11705: put
  db->Put(write_options, "key5149", "value91497");

  // op 11706: put
  db->Put(write_options, "key3142", "value93703");

  // op 11707: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 11708: put
  db->Put(write_options, "key3674", "value33259");

  // op 11709: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7316", "key7431", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7316, key7431) size=" << scan_result.size() << std::endl;
  }

  // op 11710: put
  db->Put(write_options, "key8274", "value11642");

  // op 11711: delete_range
  status = db->DeleteRange(write_options, "key1893", "key6000");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11712: get
  status = db->Get(read_options, "key1649", &value);
  if (status.ok()) {
    out << "GET key1649 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11713: delete
  db->Delete(write_options, "key10731");

  // op 11714: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8757", "key10985", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8757, key10985) size=" << scan_result.size() << std::endl;
  }

  // op 11715: delete_range
  status = db->DeleteRange(write_options, "key5891", "key9445");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11716: delete
  db->Delete(write_options, "key9499");

  // op 11717: get
  status = db->Get(read_options, "key11001", &value);
  if (status.ok()) {
    out << "GET key11001 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11718: delete_range
  status = db->DeleteRange(write_options, "key5174", "key6768");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11719: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1326", "key4895", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1326, key4895) size=" << scan_result.size() << std::endl;
  }

  // op 11720: delete_range
  status = db->DeleteRange(write_options, "key4828", "key9736");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11721: get
  status = db->Get(read_options, "key2903", &value);
  if (status.ok()) {
    out << "GET key2903 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11722: put
  db->Put(write_options, "key6901", "value100752");

  // op 11723: delete_range
  status = db->DeleteRange(write_options, "key6820", "key9530");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11724: put
  db->Put(write_options, "key10525", "value86563");

  // op 11725: put
  db->Put(write_options, "key7367", "value87419");

  // op 11726: get
  status = db->Get(read_options, "key6770", &value);
  if (status.ok()) {
    out << "GET key6770 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11727: delete
  db->Delete(write_options, "key10819");

  // op 11728: put
  db->Put(write_options, "key3012", "value64890");

  // op 11729: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0101", "key4713", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0101, key4713) size=" << scan_result.size() << std::endl;
  }

  // op 11730: delete_range
  status = db->DeleteRange(write_options, "key1746", "key10962");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11731: delete_range
  status = db->DeleteRange(write_options, "key7506", "key11590");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11732: put
  db->Put(write_options, "key6414", "value15421");

  // op 11733: put
  db->Put(write_options, "key10604", "value65298");

  // op 11734: delete
  db->Delete(write_options, "key3930");

  // op 11735: get
  status = db->Get(read_options, "key0316", &value);
  if (status.ok()) {
    out << "GET key0316 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11736: delete
  db->Delete(write_options, "key3751");

  // op 11737: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2734", "key9130", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2734, key9130) size=" << scan_result.size() << std::endl;
  }

  // op 11738: put
  db->Put(write_options, "key5528", "value1264");

  // op 11739: delete
  db->Delete(write_options, "key8623");

  // op 11740: put
  db->Put(write_options, "key8371", "value36360");

  // op 11741: delete_range
  status = db->DeleteRange(write_options, "key6408", "key9537");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11742: get
  status = db->Get(read_options, "key5664", &value);
  if (status.ok()) {
    out << "GET key5664 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11743: get
  status = db->Get(read_options, "key10263", &value);
  if (status.ok()) {
    out << "GET key10263 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11744: put
  db->Put(write_options, "key5377", "value49062");

  // op 11745: delete
  db->Delete(write_options, "key4377");

  // op 11746: put
  db->Put(write_options, "key8805", "value58154");

  // op 11747: get
  status = db->Get(read_options, "key8927", &value);
  if (status.ok()) {
    out << "GET key8927 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11748: get
  status = db->Get(read_options, "key2768", &value);
  if (status.ok()) {
    out << "GET key2768 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11749: get
  status = db->Get(read_options, "key9265", &value);
  if (status.ok()) {
    out << "GET key9265 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11750: put
  db->Put(write_options, "key1482", "value47414");

  // op 11751: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0355", "key3496", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0355, key3496) size=" << scan_result.size() << std::endl;
  }

  // op 11752: put
  db->Put(write_options, "key9537", "value31914");

  // op 11753: get
  status = db->Get(read_options, "key9815", &value);
  if (status.ok()) {
    out << "GET key9815 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11754: get
  status = db->Get(read_options, "key11221", &value);
  if (status.ok()) {
    out << "GET key11221 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11755: get
  status = db->Get(read_options, "key4031", &value);
  if (status.ok()) {
    out << "GET key4031 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11756: get
  status = db->Get(read_options, "key1083", &value);
  if (status.ok()) {
    out << "GET key1083 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11757: get
  status = db->Get(read_options, "key2426", &value);
  if (status.ok()) {
    out << "GET key2426 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11758: put
  db->Put(write_options, "key2303", "value15581");

  // op 11759: put
  db->Put(write_options, "key10300", "value55507");

  // op 11760: get
  status = db->Get(read_options, "key3269", &value);
  if (status.ok()) {
    out << "GET key3269 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11761: delete
  db->Delete(write_options, "key3113");

  // op 11762: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2736", "key7498", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2736, key7498) size=" << scan_result.size() << std::endl;
  }

  // op 11763: get
  status = db->Get(read_options, "key6591", &value);
  if (status.ok()) {
    out << "GET key6591 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11764: get
  status = db->Get(read_options, "key11594", &value);
  if (status.ok()) {
    out << "GET key11594 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11765: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1796", "key8528", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1796, key8528) size=" << scan_result.size() << std::endl;
  }

  // op 11766: put
  db->Put(write_options, "key10501", "value92371");

  // op 11767: put
  db->Put(write_options, "key8172", "value102706");

  // op 11768: get
  status = db->Get(read_options, "key3861", &value);
  if (status.ok()) {
    out << "GET key3861 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11769: delete
  db->Delete(write_options, "key3136");

  // op 11770: put
  db->Put(write_options, "key1256", "value22268");

  // op 11771: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7004", "key7947", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7004, key7947) size=" << scan_result.size() << std::endl;
  }

  // op 11772: get
  status = db->Get(read_options, "key6926", &value);
  if (status.ok()) {
    out << "GET key6926 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11773: get
  status = db->Get(read_options, "key2460", &value);
  if (status.ok()) {
    out << "GET key2460 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11774: put
  db->Put(write_options, "key7125", "value83897");

  // op 11775: put
  db->Put(write_options, "key6125", "value36605");

  // op 11776: put
  db->Put(write_options, "key5735", "value27455");

  // op 11777: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5254", "key7992", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5254, key7992) size=" << scan_result.size() << std::endl;
  }

  // op 11778: put
  db->Put(write_options, "key8645", "value107205");

  // op 11779: get
  status = db->Get(read_options, "key10018", &value);
  if (status.ok()) {
    out << "GET key10018 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11780: get
  status = db->Get(read_options, "key2519", &value);
  if (status.ok()) {
    out << "GET key2519 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11781: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7357", "key7359", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7357, key7359) size=" << scan_result.size() << std::endl;
  }

  // op 11782: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5077", "key6299", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5077, key6299) size=" << scan_result.size() << std::endl;
  }

  // op 11783: put
  db->Put(write_options, "key5967", "value2754");

  // op 11784: delete_range
  status = db->DeleteRange(write_options, "key1665", "key11549");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11785: get
  status = db->Get(read_options, "key3758", &value);
  if (status.ok()) {
    out << "GET key3758 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11786: put
  db->Put(write_options, "key6087", "value87629");

  // op 11787: put
  db->Put(write_options, "key10860", "value67645");

  // op 11788: put
  db->Put(write_options, "key2062", "value61740");

  // op 11789: put
  db->Put(write_options, "key2215", "value33405");

  // op 11790: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0096", "key6008", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0096, key6008) size=" << scan_result.size() << std::endl;
  }

  // op 11791: delete_range
  status = db->DeleteRange(write_options, "key1313", "key2212");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11792: get
  status = db->Get(read_options, "key5382", &value);
  if (status.ok()) {
    out << "GET key5382 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11793: put
  db->Put(write_options, "key4691", "value62985");

  // op 11794: delete_range
  status = db->DeleteRange(write_options, "key1318", "key3603");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11795: put
  db->Put(write_options, "key1644", "value69222");

  // op 11796: delete
  db->Delete(write_options, "key9563");

  // op 11797: put
  db->Put(write_options, "key7816", "value22151");

  // op 11798: delete_range
  status = db->DeleteRange(write_options, "key3410", "key4243");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11799: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1477", "key2468", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1477, key2468) size=" << scan_result.size() << std::endl;
  }

  // op 11800: get
  status = db->Get(read_options, "key10665", &value);
  if (status.ok()) {
    out << "GET key10665 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11801: get
  status = db->Get(read_options, "key6007", &value);
  if (status.ok()) {
    out << "GET key6007 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11802: put
  db->Put(write_options, "key1531", "value16412");

  // op 11803: put
  db->Put(write_options, "key7732", "value21057");

  // op 11804: delete_range
  status = db->DeleteRange(write_options, "key3380", "key7140");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11805: put
  db->Put(write_options, "key9412", "value109022");

  // op 11806: put
  db->Put(write_options, "key2820", "value63095");

  // op 11807: put
  db->Put(write_options, "key3612", "value25176");

  // op 11808: get
  status = db->Get(read_options, "key0447", &value);
  if (status.ok()) {
    out << "GET key0447 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11809: get
  status = db->Get(read_options, "key6603", &value);
  if (status.ok()) {
    out << "GET key6603 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11810: get
  status = db->Get(read_options, "key4834", &value);
  if (status.ok()) {
    out << "GET key4834 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11811: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7207", "key11225", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7207, key11225) size=" << scan_result.size() << std::endl;
  }

  // op 11812: put
  db->Put(write_options, "key6426", "value82882");

  // op 11813: delete_range
  status = db->DeleteRange(write_options, "key3028", "key4034");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11814: delete_range
  status = db->DeleteRange(write_options, "key5499", "key8428");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11815: get
  status = db->Get(read_options, "key1071", &value);
  if (status.ok()) {
    out << "GET key1071 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11816: put
  db->Put(write_options, "key4217", "value90388");

  // op 11817: put
  db->Put(write_options, "key3748", "value79021");

  // op 11818: get
  status = db->Get(read_options, "key10696", &value);
  if (status.ok()) {
    out << "GET key10696 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11819: delete
  db->Delete(write_options, "key6970");

  // op 11820: delete
  db->Delete(write_options, "key1649");

  // op 11821: put
  db->Put(write_options, "key4198", "value38570");

  // op 11822: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0871", "key7398", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0871, key7398) size=" << scan_result.size() << std::endl;
  }

  // op 11823: delete_range
  status = db->DeleteRange(write_options, "key3476", "key8601");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11824: put
  db->Put(write_options, "key3347", "value35679");

  // op 11825: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3108", "key4328", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3108, key4328) size=" << scan_result.size() << std::endl;
  }

  // op 11826: put
  db->Put(write_options, "key7458", "value58149");

  // op 11827: put
  db->Put(write_options, "key7222", "value76427");

  // op 11828: get
  status = db->Get(read_options, "key5619", &value);
  if (status.ok()) {
    out << "GET key5619 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11829: get
  status = db->Get(read_options, "key7671", &value);
  if (status.ok()) {
    out << "GET key7671 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11830: get
  status = db->Get(read_options, "key2034", &value);
  if (status.ok()) {
    out << "GET key2034 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11831: put
  db->Put(write_options, "key1736", "value12237");

  // op 11832: get
  status = db->Get(read_options, "key3929", &value);
  if (status.ok()) {
    out << "GET key3929 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11833: put
  db->Put(write_options, "key11713", "value34003");

  // op 11834: put
  db->Put(write_options, "key0635", "value2461");

  // op 11835: delete_range
  status = db->DeleteRange(write_options, "key2098", "key8700");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11836: put
  db->Put(write_options, "key11365", "value83998");

  // op 11837: put
  db->Put(write_options, "key7392", "value67174");

  // op 11838: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 11839: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7953", "key9230", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7953, key9230) size=" << scan_result.size() << std::endl;
  }

  // op 11840: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 11841: get
  status = db->Get(read_options, "key8275", &value);
  if (status.ok()) {
    out << "GET key8275 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11842: delete
  db->Delete(write_options, "key6277");

  // op 11843: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6330", "key10220", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6330, key10220) size=" << scan_result.size() << std::endl;
  }

  // op 11844: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7835", "key10459", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7835, key10459) size=" << scan_result.size() << std::endl;
  }

  // op 11845: get
  status = db->Get(read_options, "key6683", &value);
  if (status.ok()) {
    out << "GET key6683 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11846: delete_range
  status = db->DeleteRange(write_options, "key7811", "key9920");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11847: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5614", "key11090", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5614, key11090) size=" << scan_result.size() << std::endl;
  }

  // op 11848: delete_range
  status = db->DeleteRange(write_options, "key9388", "key10134");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11849: delete
  db->Delete(write_options, "key5374");

  // op 11850: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 11851: delete_range
  status = db->DeleteRange(write_options, "key5774", "key7469");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11852: put
  db->Put(write_options, "key7002", "value97579");

  // op 11853: put
  db->Put(write_options, "key1618", "value104125");

  // op 11854: put
  db->Put(write_options, "key1756", "value97318");

  // op 11855: put
  db->Put(write_options, "key9173", "value112453");

  // op 11856: delete
  db->Delete(write_options, "key2787");

  // op 11857: get
  status = db->Get(read_options, "key6945", &value);
  if (status.ok()) {
    out << "GET key6945 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11858: delete
  db->Delete(write_options, "key3745");

  // op 11859: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 11860: put
  db->Put(write_options, "key9087", "value25189");

  // op 11861: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7340", "key9118", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7340, key9118) size=" << scan_result.size() << std::endl;
  }

  // op 11862: delete_range
  status = db->DeleteRange(write_options, "key4427", "key9986");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11863: put
  db->Put(write_options, "key4582", "value89185");

  // op 11864: get
  status = db->Get(read_options, "key6536", &value);
  if (status.ok()) {
    out << "GET key6536 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11865: get
  status = db->Get(read_options, "key4601", &value);
  if (status.ok()) {
    out << "GET key4601 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11866: delete_range
  status = db->DeleteRange(write_options, "key2818", "key10417");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11867: get
  status = db->Get(read_options, "key6748", &value);
  if (status.ok()) {
    out << "GET key6748 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11868: get
  status = db->Get(read_options, "key9082", &value);
  if (status.ok()) {
    out << "GET key9082 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11869: delete_range
  status = db->DeleteRange(write_options, "key8654", "key9446");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11870: put
  db->Put(write_options, "key10111", "value93270");

  // op 11871: put
  db->Put(write_options, "key6069", "value111093");

  // op 11872: put
  db->Put(write_options, "key6048", "value4827");

  // op 11873: put
  db->Put(write_options, "key0647", "value25711");

  // op 11874: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0894", "key4469", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0894, key4469) size=" << scan_result.size() << std::endl;
  }

  // op 11875: put
  db->Put(write_options, "key2588", "value46405");

  // op 11876: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2534", "key3739", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2534, key3739) size=" << scan_result.size() << std::endl;
  }

  // op 11877: put
  db->Put(write_options, "key8367", "value77629");

  // op 11878: delete_range
  status = db->DeleteRange(write_options, "key7643", "key10501");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11879: get
  status = db->Get(read_options, "key10865", &value);
  if (status.ok()) {
    out << "GET key10865 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11880: get
  status = db->Get(read_options, "key11038", &value);
  if (status.ok()) {
    out << "GET key11038 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11881: get
  status = db->Get(read_options, "key6192", &value);
  if (status.ok()) {
    out << "GET key6192 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11882: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0401", "key2821", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0401, key2821) size=" << scan_result.size() << std::endl;
  }

  // op 11883: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2570", "key6361", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2570, key6361) size=" << scan_result.size() << std::endl;
  }

  // op 11884: put
  db->Put(write_options, "key4818", "value100204");

  // op 11885: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1808", "key11424", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1808, key11424) size=" << scan_result.size() << std::endl;
  }

  // op 11886: put
  db->Put(write_options, "key2957", "value28850");

  // op 11887: delete
  db->Delete(write_options, "key0775");

  // op 11888: delete_range
  status = db->DeleteRange(write_options, "key8524", "key8601");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11889: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 11890: delete
  db->Delete(write_options, "key9448");

  // op 11891: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3056", "key5263", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3056, key5263) size=" << scan_result.size() << std::endl;
  }

  // op 11892: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 11893: delete_range
  status = db->DeleteRange(write_options, "key0414", "key8390");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11894: delete_range
  status = db->DeleteRange(write_options, "key5484", "key9235");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11895: put
  db->Put(write_options, "key10370", "value6945");

  // op 11896: put
  db->Put(write_options, "key0758", "value27287");

  // op 11897: put
  db->Put(write_options, "key6270", "value112801");

  // op 11898: put
  db->Put(write_options, "key7817", "value46177");

  // op 11899: put
  db->Put(write_options, "key2405", "value110794");

  // op 11900: put
  db->Put(write_options, "key5760", "value25810");

  // op 11901: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2182", "key10551", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2182, key10551) size=" << scan_result.size() << std::endl;
  }

  // op 11902: delete_range
  status = db->DeleteRange(write_options, "key1096", "key2960");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11903: put
  db->Put(write_options, "key11046", "value29847");

  // op 11904: get
  status = db->Get(read_options, "key6029", &value);
  if (status.ok()) {
    out << "GET key6029 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11905: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1362", "key8778", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1362, key8778) size=" << scan_result.size() << std::endl;
  }

  // op 11906: get
  status = db->Get(read_options, "key8621", &value);
  if (status.ok()) {
    out << "GET key8621 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11907: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8847", "key10097", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8847, key10097) size=" << scan_result.size() << std::endl;
  }

  // op 11908: delete_range
  status = db->DeleteRange(write_options, "key9497", "key10870");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11909: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 11910: get
  status = db->Get(read_options, "key9484", &value);
  if (status.ok()) {
    out << "GET key9484 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11911: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0900", "key1608", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0900, key1608) size=" << scan_result.size() << std::endl;
  }

  // op 11912: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0283", "key4021", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0283, key4021) size=" << scan_result.size() << std::endl;
  }

  // op 11913: get
  status = db->Get(read_options, "key6264", &value);
  if (status.ok()) {
    out << "GET key6264 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11914: delete_range
  status = db->DeleteRange(write_options, "key9282", "key11346");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11915: delete_range
  status = db->DeleteRange(write_options, "key7035", "key9865");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11916: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 11917: get
  status = db->Get(read_options, "key8124", &value);
  if (status.ok()) {
    out << "GET key8124 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11918: get
  status = db->Get(read_options, "key11044", &value);
  if (status.ok()) {
    out << "GET key11044 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11919: delete_range
  status = db->DeleteRange(write_options, "key6634", "key9142");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11920: get
  status = db->Get(read_options, "key7412", &value);
  if (status.ok()) {
    out << "GET key7412 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11921: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3839", "key10032", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3839, key10032) size=" << scan_result.size() << std::endl;
  }

  // op 11922: delete
  db->Delete(write_options, "key7763");

  // op 11923: get
  status = db->Get(read_options, "key0611", &value);
  if (status.ok()) {
    out << "GET key0611 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11924: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8867", "key11622", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8867, key11622) size=" << scan_result.size() << std::endl;
  }

  // op 11925: get
  status = db->Get(read_options, "key2876", &value);
  if (status.ok()) {
    out << "GET key2876 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11926: get
  status = db->Get(read_options, "key3623", &value);
  if (status.ok()) {
    out << "GET key3623 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11927: delete
  db->Delete(write_options, "key11373");

  // op 11928: put
  db->Put(write_options, "key9709", "value81366");

  // op 11929: delete
  db->Delete(write_options, "key11619");

  // op 11930: delete_range
  status = db->DeleteRange(write_options, "key1814", "key4355");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11931: delete
  db->Delete(write_options, "key0749");

  // op 11932: put
  db->Put(write_options, "key1289", "value0183");

  // op 11933: put
  db->Put(write_options, "key6326", "value63913");

  // op 11934: put
  db->Put(write_options, "key3162", "value68281");

  // op 11935: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4478", "key9361", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4478, key9361) size=" << scan_result.size() << std::endl;
  }

  // op 11936: put
  db->Put(write_options, "key0733", "value100526");

  // op 11937: put
  db->Put(write_options, "key2879", "value28838");

  // op 11938: delete_range
  status = db->DeleteRange(write_options, "key5145", "key7548");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11939: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1854", "key5165", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1854, key5165) size=" << scan_result.size() << std::endl;
  }

  // op 11940: put
  db->Put(write_options, "key11436", "value12540");

  // op 11941: get
  status = db->Get(read_options, "key2224", &value);
  if (status.ok()) {
    out << "GET key2224 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11942: put
  db->Put(write_options, "key0862", "value36462");

  // op 11943: get
  status = db->Get(read_options, "key9199", &value);
  if (status.ok()) {
    out << "GET key9199 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11944: put
  db->Put(write_options, "key7540", "value20345");

  // op 11945: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3559", "key11322", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3559, key11322) size=" << scan_result.size() << std::endl;
  }

  // op 11946: put
  db->Put(write_options, "key7690", "value116421");

  // op 11947: get
  status = db->Get(read_options, "key1247", &value);
  if (status.ok()) {
    out << "GET key1247 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11948: put
  db->Put(write_options, "key0185", "value108910");

  // op 11949: put
  db->Put(write_options, "key9140", "value32315");

  // op 11950: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1855", "key3525", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1855, key3525) size=" << scan_result.size() << std::endl;
  }

  // op 11951: put
  db->Put(write_options, "key4676", "value31550");

  // op 11952: put
  db->Put(write_options, "key3932", "value86479");

  // op 11953: get
  status = db->Get(read_options, "key7992", &value);
  if (status.ok()) {
    out << "GET key7992 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11954: delete_range
  status = db->DeleteRange(write_options, "key4309", "key9780");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11955: put
  db->Put(write_options, "key1441", "value44167");

  // op 11956: delete_range
  status = db->DeleteRange(write_options, "key1814", "key5538");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11957: put
  db->Put(write_options, "key10362", "value92421");

  // op 11958: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8337", "key10387", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8337, key10387) size=" << scan_result.size() << std::endl;
  }

  // op 11959: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9665", "key10881", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9665, key10881) size=" << scan_result.size() << std::endl;
  }

  // op 11960: put
  db->Put(write_options, "key2993", "value44152");

  // op 11961: get
  status = db->Get(read_options, "key11497", &value);
  if (status.ok()) {
    out << "GET key11497 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11962: put
  db->Put(write_options, "key9000", "value104149");

  // op 11963: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8986", "key10272", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8986, key10272) size=" << scan_result.size() << std::endl;
  }

  // op 11964: put
  db->Put(write_options, "key6069", "value99936");

  // op 11965: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0159", "key0826", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0159, key0826) size=" << scan_result.size() << std::endl;
  }

  // op 11966: put
  db->Put(write_options, "key11285", "value76462");

  // op 11967: put
  db->Put(write_options, "key5828", "value71163");

  // op 11968: delete
  db->Delete(write_options, "key0209");

  // op 11969: put
  db->Put(write_options, "key0764", "value19627");

  // op 11970: delete
  db->Delete(write_options, "key8878");

  // op 11971: put
  db->Put(write_options, "key9205", "value21926");

  // op 11972: put
  db->Put(write_options, "key10468", "value112789");

  // op 11973: get
  status = db->Get(read_options, "key1006", &value);
  if (status.ok()) {
    out << "GET key1006 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 11974: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1110", "key4605", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1110, key4605) size=" << scan_result.size() << std::endl;
  }

  // op 11975: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5041", "key5362", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5041, key5362) size=" << scan_result.size() << std::endl;
  }

  // op 11976: put
  db->Put(write_options, "key9707", "value71830");

  // op 11977: delete
  db->Delete(write_options, "key11022");

  // op 11978: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4247", "key10440", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4247, key10440) size=" << scan_result.size() << std::endl;
  }

  // op 11979: put
  db->Put(write_options, "key7315", "value96052");

  // op 11980: delete
  db->Delete(write_options, "key3225");

  // op 11981: put
  db->Put(write_options, "key4418", "value41535");

  // op 11982: delete
  db->Delete(write_options, "key2251");

  // op 11983: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1869", "key8123", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1869, key8123) size=" << scan_result.size() << std::endl;
  }

  // op 11984: put
  db->Put(write_options, "key11395", "value85077");

  // op 11985: delete
  db->Delete(write_options, "key8734");

  // op 11986: delete_range
  status = db->DeleteRange(write_options, "key0261", "key8680");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11987: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 11988: delete
  db->Delete(write_options, "key4500");

  // op 11989: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2890", "key7729", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2890, key7729) size=" << scan_result.size() << std::endl;
  }

  // op 11990: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0475", "key10193", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0475, key10193) size=" << scan_result.size() << std::endl;
  }

  // op 11991: put
  db->Put(write_options, "key7967", "value53386");

  // op 11992: delete_range
  status = db->DeleteRange(write_options, "key2341", "key11478");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11993: delete
  db->Delete(write_options, "key7156");

  // op 11994: delete
  db->Delete(write_options, "key11682");

  // op 11995: put
  db->Put(write_options, "key7719", "value21778");

  // op 11996: delete
  db->Delete(write_options, "key2280");

  // op 11997: delete_range
  status = db->DeleteRange(write_options, "key4610", "key5549");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 11998: put
  db->Put(write_options, "key9513", "value113171");

  // op 11999: put
  db->Put(write_options, "key7930", "value88904");

  // op 12000: delete
  db->Delete(write_options, "key9434");

  // op 12001: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 12002: get
  status = db->Get(read_options, "key9721", &value);
  if (status.ok()) {
    out << "GET key9721 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12003: get
  status = db->Get(read_options, "key3765", &value);
  if (status.ok()) {
    out << "GET key3765 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12004: put
  db->Put(write_options, "key3182", "value55398");

  // op 12005: delete_range
  status = db->DeleteRange(write_options, "key6602", "key9196");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 12006: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2348", "key11108", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2348, key11108) size=" << scan_result.size() << std::endl;
  }

  // op 12007: delete_range
  status = db->DeleteRange(write_options, "key8353", "key8517");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 12008: delete_range
  status = db->DeleteRange(write_options, "key4081", "key11240");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 12009: put
  db->Put(write_options, "key4416", "value40115");

  // op 12010: delete_range
  status = db->DeleteRange(write_options, "key4874", "key10393");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 12011: get
  status = db->Get(read_options, "key5262", &value);
  if (status.ok()) {
    out << "GET key5262 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12012: put
  db->Put(write_options, "key4727", "value29227");

  // op 12013: put
  db->Put(write_options, "key7353", "value8919");

  // op 12014: delete_range
  status = db->DeleteRange(write_options, "key0181", "key2659");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 12015: put
  db->Put(write_options, "key3881", "value64575");

  // op 12016: put
  db->Put(write_options, "key9065", "value15574");

  // op 12017: put
  db->Put(write_options, "key3570", "value102832");

  // op 12018: delete_range
  status = db->DeleteRange(write_options, "key0125", "key5763");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 12019: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1103", "key3505", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1103, key3505) size=" << scan_result.size() << std::endl;
  }

  // op 12020: put
  db->Put(write_options, "key11596", "value34027");

  // op 12021: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1705", "key9556", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1705, key9556) size=" << scan_result.size() << std::endl;
  }

  // op 12022: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 12023: put
  db->Put(write_options, "key3422", "value24279");

  // op 12024: put
  db->Put(write_options, "key9023", "value3003");

  // op 12025: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 12026: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5305", "key8075", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5305, key8075) size=" << scan_result.size() << std::endl;
  }

  // op 12027: put
  db->Put(write_options, "key9429", "value18746");

  // op 12028: put
  db->Put(write_options, "key6620", "value84936");

  // op 12029: get
  status = db->Get(read_options, "key3415", &value);
  if (status.ok()) {
    out << "GET key3415 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12030: get
  status = db->Get(read_options, "key9246", &value);
  if (status.ok()) {
    out << "GET key9246 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12031: put
  db->Put(write_options, "key10815", "value61962");

  // op 12032: get
  status = db->Get(read_options, "key4616", &value);
  if (status.ok()) {
    out << "GET key4616 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12033: put
  db->Put(write_options, "key8279", "value50109");

  // op 12034: put
  db->Put(write_options, "key9480", "value115789");

  // op 12035: delete
  db->Delete(write_options, "key0471");

  // op 12036: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2834", "key5488", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2834, key5488) size=" << scan_result.size() << std::endl;
  }

  // op 12037: put
  db->Put(write_options, "key0941", "value66131");

  // op 12038: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4466", "key4963", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4466, key4963) size=" << scan_result.size() << std::endl;
  }

  // op 12039: get
  status = db->Get(read_options, "key5237", &value);
  if (status.ok()) {
    out << "GET key5237 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12040: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 12041: get
  status = db->Get(read_options, "key0288", &value);
  if (status.ok()) {
    out << "GET key0288 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12042: put
  db->Put(write_options, "key10981", "value56365");

  // op 12043: get
  status = db->Get(read_options, "key5467", &value);
  if (status.ok()) {
    out << "GET key5467 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12044: get
  status = db->Get(read_options, "key9009", &value);
  if (status.ok()) {
    out << "GET key9009 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12045: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7546", "key9892", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7546, key9892) size=" << scan_result.size() << std::endl;
  }

  // op 12046: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0288", "key10577", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0288, key10577) size=" << scan_result.size() << std::endl;
  }

  // op 12047: get
  status = db->Get(read_options, "key10865", &value);
  if (status.ok()) {
    out << "GET key10865 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12048: get
  status = db->Get(read_options, "key9161", &value);
  if (status.ok()) {
    out << "GET key9161 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12049: put
  db->Put(write_options, "key4161", "value22814");

  // op 12050: delete
  db->Delete(write_options, "key3498");

  // op 12051: put
  db->Put(write_options, "key1420", "value20800");

  // op 12052: get
  status = db->Get(read_options, "key4069", &value);
  if (status.ok()) {
    out << "GET key4069 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12053: delete
  db->Delete(write_options, "key4329");

  // op 12054: delete_range
  status = db->DeleteRange(write_options, "key10681", "key11544");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 12055: get
  status = db->Get(read_options, "key7600", &value);
  if (status.ok()) {
    out << "GET key7600 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12056: delete
  db->Delete(write_options, "key9544");

  // op 12057: put
  db->Put(write_options, "key3527", "value19456");

  // op 12058: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 12059: put
  db->Put(write_options, "key9906", "value91674");

  // op 12060: delete
  db->Delete(write_options, "key5562");

  // op 12061: get
  status = db->Get(read_options, "key10191", &value);
  if (status.ok()) {
    out << "GET key10191 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12062: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8053", "key8610", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8053, key8610) size=" << scan_result.size() << std::endl;
  }

  // op 12063: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6289", "key8366", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6289, key8366) size=" << scan_result.size() << std::endl;
  }

  // op 12064: get
  status = db->Get(read_options, "key11616", &value);
  if (status.ok()) {
    out << "GET key11616 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12065: get
  status = db->Get(read_options, "key1878", &value);
  if (status.ok()) {
    out << "GET key1878 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12066: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1245", "key2885", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1245, key2885) size=" << scan_result.size() << std::endl;
  }

  // op 12067: put
  db->Put(write_options, "key4236", "value62064");

  // op 12068: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1166", "key10034", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1166, key10034) size=" << scan_result.size() << std::endl;
  }

  // op 12069: delete_range
  status = db->DeleteRange(write_options, "key7657", "key11223");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 12070: delete
  db->Delete(write_options, "key9860");

  // op 12071: get
  status = db->Get(read_options, "key11290", &value);
  if (status.ok()) {
    out << "GET key11290 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12072: put
  db->Put(write_options, "key4003", "value76187");

  // op 12073: delete_range
  status = db->DeleteRange(write_options, "key4906", "key10025");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 12074: delete_range
  status = db->DeleteRange(write_options, "key6198", "key10532");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 12075: put
  db->Put(write_options, "key8924", "value99170");

  // op 12076: put
  db->Put(write_options, "key1570", "value60477");

  // op 12077: put
  db->Put(write_options, "key6998", "value52718");

  // op 12078: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2994", "key9374", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2994, key9374) size=" << scan_result.size() << std::endl;
  }

  // op 12079: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6574", "key9425", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6574, key9425) size=" << scan_result.size() << std::endl;
  }

  // op 12080: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9052", "key10301", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9052, key10301) size=" << scan_result.size() << std::endl;
  }

  // op 12081: delete
  db->Delete(write_options, "key3413");

  // op 12082: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 12083: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5081", "key6503", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5081, key6503) size=" << scan_result.size() << std::endl;
  }

  // op 12084: delete
  db->Delete(write_options, "key4315");

  // op 12085: delete
  db->Delete(write_options, "key10492");

  // op 12086: put
  db->Put(write_options, "key5970", "value60139");

  // op 12087: put
  db->Put(write_options, "key8779", "value33969");

  // op 12088: get
  status = db->Get(read_options, "key5172", &value);
  if (status.ok()) {
    out << "GET key5172 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12089: put
  db->Put(write_options, "key1988", "value81617");

  // op 12090: get
  status = db->Get(read_options, "key8955", &value);
  if (status.ok()) {
    out << "GET key8955 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12091: put
  db->Put(write_options, "key4478", "value108283");

  // op 12092: delete_range
  status = db->DeleteRange(write_options, "key5204", "key10856");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 12093: put
  db->Put(write_options, "key2820", "value38760");

  // op 12094: put
  db->Put(write_options, "key9200", "value93690");

  // op 12095: put
  db->Put(write_options, "key2003", "value80499");

  // op 12096: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7519", "key8543", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7519, key8543) size=" << scan_result.size() << std::endl;
  }

  // op 12097: get
  status = db->Get(read_options, "key6406", &value);
  if (status.ok()) {
    out << "GET key6406 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12098: get
  status = db->Get(read_options, "key0379", &value);
  if (status.ok()) {
    out << "GET key0379 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12099: put
  db->Put(write_options, "key2278", "value47677");

  // op 12100: delete
  db->Delete(write_options, "key9454");

  // op 12101: delete_range
  status = db->DeleteRange(write_options, "key1455", "key4358");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 12102: get
  status = db->Get(read_options, "key4390", &value);
  if (status.ok()) {
    out << "GET key4390 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12103: put
  db->Put(write_options, "key10144", "value93627");

  // op 12104: put
  db->Put(write_options, "key4691", "value116490");

  // op 12105: put
  db->Put(write_options, "key1040", "value99911");

  // op 12106: put
  db->Put(write_options, "key10235", "value1397");

  // op 12107: delete_range
  status = db->DeleteRange(write_options, "key0640", "key4475");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 12108: get
  status = db->Get(read_options, "key7063", &value);
  if (status.ok()) {
    out << "GET key7063 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12109: put
  db->Put(write_options, "key5628", "value53916");

  // op 12110: put
  db->Put(write_options, "key10591", "value74817");

  // op 12111: put
  db->Put(write_options, "key11549", "value61584");

  // op 12112: put
  db->Put(write_options, "key1059", "value31189");

  // op 12113: put
  db->Put(write_options, "key2118", "value31000");

  // op 12114: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 12115: delete
  db->Delete(write_options, "key11610");

  // op 12116: put
  db->Put(write_options, "key0987", "value76858");

  // op 12117: get
  status = db->Get(read_options, "key9099", &value);
  if (status.ok()) {
    out << "GET key9099 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12118: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0143", "key11517", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0143, key11517) size=" << scan_result.size() << std::endl;
  }

  // op 12119: get
  status = db->Get(read_options, "key1405", &value);
  if (status.ok()) {
    out << "GET key1405 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12120: delete
  db->Delete(write_options, "key4424");

  // op 12121: delete
  db->Delete(write_options, "key8099");

  // op 12122: get
  status = db->Get(read_options, "key10167", &value);
  if (status.ok()) {
    out << "GET key10167 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12123: delete
  db->Delete(write_options, "key3689");

  // op 12124: get
  status = db->Get(read_options, "key9981", &value);
  if (status.ok()) {
    out << "GET key9981 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12125: put
  db->Put(write_options, "key10256", "value96082");

  // op 12126: put
  db->Put(write_options, "key1755", "value104786");

  // op 12127: delete
  db->Delete(write_options, "key8429");

  // op 12128: put
  db->Put(write_options, "key5893", "value88991");

  // op 12129: put
  db->Put(write_options, "key2208", "value115106");

  // op 12130: get
  status = db->Get(read_options, "key4509", &value);
  if (status.ok()) {
    out << "GET key4509 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12131: delete
  db->Delete(write_options, "key5951");

  // op 12132: get
  status = db->Get(read_options, "key6364", &value);
  if (status.ok()) {
    out << "GET key6364 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12133: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 12134: get
  status = db->Get(read_options, "key10087", &value);
  if (status.ok()) {
    out << "GET key10087 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12135: get
  status = db->Get(read_options, "key8625", &value);
  if (status.ok()) {
    out << "GET key8625 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12136: put
  db->Put(write_options, "key8931", "value42562");

  // op 12137: delete_range
  status = db->DeleteRange(write_options, "key1294", "key2316");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 12138: put
  db->Put(write_options, "key3137", "value33514");

  // op 12139: get
  status = db->Get(read_options, "key6160", &value);
  if (status.ok()) {
    out << "GET key6160 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12140: put
  db->Put(write_options, "key5116", "value85109");

  // op 12141: delete
  db->Delete(write_options, "key3271");

  // op 12142: get
  status = db->Get(read_options, "key2643", &value);
  if (status.ok()) {
    out << "GET key2643 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12143: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 12144: put
  db->Put(write_options, "key5831", "value86728");

  // op 12145: delete
  db->Delete(write_options, "key3106");

  // op 12146: put
  db->Put(write_options, "key8048", "value84834");

  // op 12147: get
  status = db->Get(read_options, "key9117", &value);
  if (status.ok()) {
    out << "GET key9117 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12148: delete
  db->Delete(write_options, "key8590");

  // op 12149: get
  status = db->Get(read_options, "key2887", &value);
  if (status.ok()) {
    out << "GET key2887 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12150: put
  db->Put(write_options, "key10465", "value20008");

  // op 12151: get
  status = db->Get(read_options, "key6835", &value);
  if (status.ok()) {
    out << "GET key6835 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12152: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5784", "key5906", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5784, key5906) size=" << scan_result.size() << std::endl;
  }

  // op 12153: put
  db->Put(write_options, "key5750", "value70064");

  // op 12154: put
  db->Put(write_options, "key3068", "value86739");

  // op 12155: delete_range
  status = db->DeleteRange(write_options, "key0796", "key6960");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 12156: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10433", "key11220", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10433, key11220) size=" << scan_result.size() << std::endl;
  }

  // op 12157: delete
  db->Delete(write_options, "key6903");

  // op 12158: put
  db->Put(write_options, "key6817", "value10218");

  // op 12159: put
  db->Put(write_options, "key0152", "value93255");

  // op 12160: delete
  db->Delete(write_options, "key7216");

  // op 12161: get
  status = db->Get(read_options, "key3500", &value);
  if (status.ok()) {
    out << "GET key3500 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12162: delete
  db->Delete(write_options, "key1692");

  // op 12163: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 12164: delete_range
  status = db->DeleteRange(write_options, "key4273", "key4773");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 12165: delete
  db->Delete(write_options, "key1143");

  // op 12166: put
  db->Put(write_options, "key0302", "value9541");

  // op 12167: delete
  db->Delete(write_options, "key0254");

  // op 12168: put
  db->Put(write_options, "key9764", "value71040");

  // op 12169: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0349", "key11046", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0349, key11046) size=" << scan_result.size() << std::endl;
  }

  // op 12170: get
  status = db->Get(read_options, "key11715", &value);
  if (status.ok()) {
    out << "GET key11715 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12171: delete_range
  status = db->DeleteRange(write_options, "key5142", "key9807");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 12172: put
  db->Put(write_options, "key0593", "value72988");

  // op 12173: get
  status = db->Get(read_options, "key2707", &value);
  if (status.ok()) {
    out << "GET key2707 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12174: delete
  db->Delete(write_options, "key0645");

  // op 12175: put
  db->Put(write_options, "key9882", "value64188");

  // op 12176: put
  db->Put(write_options, "key1786", "value73158");

  // op 12177: get
  status = db->Get(read_options, "key6387", &value);
  if (status.ok()) {
    out << "GET key6387 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12178: delete
  db->Delete(write_options, "key7551");

  // op 12179: put
  db->Put(write_options, "key3727", "value94241");

  // op 12180: put
  db->Put(write_options, "key10777", "value108835");

  // op 12181: put
  db->Put(write_options, "key2463", "value71421");

  // op 12182: put
  db->Put(write_options, "key0484", "value36572");

  // op 12183: get
  status = db->Get(read_options, "key10662", &value);
  if (status.ok()) {
    out << "GET key10662 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12184: put
  db->Put(write_options, "key3471", "value74646");

  // op 12185: get
  status = db->Get(read_options, "key9597", &value);
  if (status.ok()) {
    out << "GET key9597 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12186: delete
  db->Delete(write_options, "key9005");

  // op 12187: delete_range
  status = db->DeleteRange(write_options, "key5010", "key5383");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 12188: put
  db->Put(write_options, "key5321", "value11911");

  // op 12189: get
  status = db->Get(read_options, "key11543", &value);
  if (status.ok()) {
    out << "GET key11543 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12190: delete_range
  status = db->DeleteRange(write_options, "key3733", "key8287");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 12191: put
  db->Put(write_options, "key9553", "value54512");

  // op 12192: get
  status = db->Get(read_options, "key6976", &value);
  if (status.ok()) {
    out << "GET key6976 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12193: delete_range
  status = db->DeleteRange(write_options, "key5426", "key8853");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 12194: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0545", "key10164", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0545, key10164) size=" << scan_result.size() << std::endl;
  }

  // op 12195: put
  db->Put(write_options, "key8749", "value75735");

  // op 12196: put
  db->Put(write_options, "key7932", "value109644");

  // op 12197: get
  status = db->Get(read_options, "key2474", &value);
  if (status.ok()) {
    out << "GET key2474 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12198: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6552", "key6668", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6552, key6668) size=" << scan_result.size() << std::endl;
  }

  // op 12199: put
  db->Put(write_options, "key9372", "value74275");

  // op 12200: put
  db->Put(write_options, "key6515", "value72569");

  // op 12201: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5784", "key10114", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5784, key10114) size=" << scan_result.size() << std::endl;
  }

  // op 12202: put
  db->Put(write_options, "key4023", "value67558");

  // op 12203: put
  db->Put(write_options, "key1920", "value20494");

  // op 12204: put
  db->Put(write_options, "key0964", "value57088");

  // op 12205: put
  db->Put(write_options, "key7048", "value27747");

  // op 12206: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2586", "key5036", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2586, key5036) size=" << scan_result.size() << std::endl;
  }

  // op 12207: delete_range
  status = db->DeleteRange(write_options, "key2080", "key9532");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 12208: put
  db->Put(write_options, "key4140", "value54637");

  // op 12209: delete
  db->Delete(write_options, "key2087");

  // op 12210: get
  status = db->Get(read_options, "key10679", &value);
  if (status.ok()) {
    out << "GET key10679 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12211: put
  db->Put(write_options, "key9390", "value17264");

  // op 12212: put
  db->Put(write_options, "key4412", "value114229");

  // op 12213: put
  db->Put(write_options, "key6631", "value0146");

  // op 12214: put
  db->Put(write_options, "key9376", "value87913");

  // op 12215: put
  db->Put(write_options, "key5297", "value6919");

  // op 12216: get
  status = db->Get(read_options, "key3675", &value);
  if (status.ok()) {
    out << "GET key3675 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12217: get
  status = db->Get(read_options, "key9748", &value);
  if (status.ok()) {
    out << "GET key9748 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12218: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1428", "key1865", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1428, key1865) size=" << scan_result.size() << std::endl;
  }

  // op 12219: put
  db->Put(write_options, "key9957", "value75449");

  // op 12220: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0132", "key2114", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0132, key2114) size=" << scan_result.size() << std::endl;
  }

  // op 12221: put
  db->Put(write_options, "key4291", "value87267");

  // op 12222: delete_range
  status = db->DeleteRange(write_options, "key1762", "key4201");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 12223: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3024", "key10712", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3024, key10712) size=" << scan_result.size() << std::endl;
  }

  // op 12224: delete_range
  status = db->DeleteRange(write_options, "key2431", "key9896");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 12225: put
  db->Put(write_options, "key8539", "value45931");

  // op 12226: put
  db->Put(write_options, "key0349", "value10229");

  // op 12227: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8112", "key11455", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8112, key11455) size=" << scan_result.size() << std::endl;
  }

  // op 12228: put
  db->Put(write_options, "key9540", "value89879");

  // op 12229: put
  db->Put(write_options, "key0703", "value62145");

  // op 12230: get
  status = db->Get(read_options, "key11690", &value);
  if (status.ok()) {
    out << "GET key11690 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12231: put
  db->Put(write_options, "key10387", "value91000");

  // op 12232: delete
  db->Delete(write_options, "key8624");

  // op 12233: delete_range
  status = db->DeleteRange(write_options, "key3299", "key5883");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 12234: put
  db->Put(write_options, "key8417", "value23207");

  // op 12235: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 12236: put
  db->Put(write_options, "key11570", "value23536");

  // op 12237: get
  status = db->Get(read_options, "key0324", &value);
  if (status.ok()) {
    out << "GET key0324 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12238: delete_range
  status = db->DeleteRange(write_options, "key5920", "key8731");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 12239: put
  db->Put(write_options, "key4535", "value30744");

  // op 12240: get
  status = db->Get(read_options, "key5427", &value);
  if (status.ok()) {
    out << "GET key5427 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12241: delete
  db->Delete(write_options, "key7934");

  // op 12242: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6178", "key10279", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6178, key10279) size=" << scan_result.size() << std::endl;
  }

  // op 12243: put
  db->Put(write_options, "key3297", "value113691");

  // op 12244: delete_range
  status = db->DeleteRange(write_options, "key5365", "key9656");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 12245: put
  db->Put(write_options, "key2442", "value11482");

  // op 12246: get
  status = db->Get(read_options, "key1887", &value);
  if (status.ok()) {
    out << "GET key1887 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12247: get
  status = db->Get(read_options, "key9143", &value);
  if (status.ok()) {
    out << "GET key9143 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12248: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1830", "key7104", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1830, key7104) size=" << scan_result.size() << std::endl;
  }

  // op 12249: put
  db->Put(write_options, "key0636", "value85345");

  // op 12250: delete_range
  status = db->DeleteRange(write_options, "key0477", "key7190");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 12251: get
  status = db->Get(read_options, "key4165", &value);
  if (status.ok()) {
    out << "GET key4165 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12252: put
  db->Put(write_options, "key1538", "value108032");

  // op 12253: delete_range
  status = db->DeleteRange(write_options, "key1664", "key5886");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 12254: get
  status = db->Get(read_options, "key8495", &value);
  if (status.ok()) {
    out << "GET key8495 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12255: put
  db->Put(write_options, "key11093", "value104657");

  // op 12256: put
  db->Put(write_options, "key6476", "value95074");

  // op 12257: put
  db->Put(write_options, "key11266", "value76907");

  // op 12258: put
  db->Put(write_options, "key1244", "value102686");

  // op 12259: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0194", "key1938", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0194, key1938) size=" << scan_result.size() << std::endl;
  }

  // op 12260: put
  db->Put(write_options, "key4767", "value96195");

  // op 12261: put
  db->Put(write_options, "key0170", "value37801");

  // op 12262: get
  status = db->Get(read_options, "key0025", &value);
  if (status.ok()) {
    out << "GET key0025 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12263: put
  db->Put(write_options, "key3869", "value86419");

  // op 12264: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8308", "key11197", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8308, key11197) size=" << scan_result.size() << std::endl;
  }

  // op 12265: delete
  db->Delete(write_options, "key8078");

  // op 12266: put
  db->Put(write_options, "key4202", "value114655");

  // op 12267: put
  db->Put(write_options, "key3289", "value59083");

  // op 12268: put
  db->Put(write_options, "key0141", "value94679");

  // op 12269: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 12270: put
  db->Put(write_options, "key4907", "value5633");

  // op 12271: put
  db->Put(write_options, "key8261", "value58647");

  // op 12272: put
  db->Put(write_options, "key10425", "value42465");

  // op 12273: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1021", "key9191", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1021, key9191) size=" << scan_result.size() << std::endl;
  }

  // op 12274: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0662", "key3710", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0662, key3710) size=" << scan_result.size() << std::endl;
  }

  // op 12275: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0770", "key1234", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0770, key1234) size=" << scan_result.size() << std::endl;
  }

  // op 12276: delete_range
  status = db->DeleteRange(write_options, "key0949", "key8776");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 12277: delete
  db->Delete(write_options, "key5693");

  // op 12278: put
  db->Put(write_options, "key11520", "value63310");

  // op 12279: get
  status = db->Get(read_options, "key0394", &value);
  if (status.ok()) {
    out << "GET key0394 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12280: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4871", "key7860", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4871, key7860) size=" << scan_result.size() << std::endl;
  }

  // op 12281: put
  db->Put(write_options, "key7760", "value81986");

  // op 12282: put
  db->Put(write_options, "key4544", "value69472");

  // op 12283: get
  status = db->Get(read_options, "key1686", &value);
  if (status.ok()) {
    out << "GET key1686 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12284: put
  db->Put(write_options, "key10073", "value21365");

  // op 12285: put
  db->Put(write_options, "key8261", "value100927");

  // op 12286: get
  status = db->Get(read_options, "key0878", &value);
  if (status.ok()) {
    out << "GET key0878 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12287: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0571", "key11619", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0571, key11619) size=" << scan_result.size() << std::endl;
  }

  // op 12288: get
  status = db->Get(read_options, "key3991", &value);
  if (status.ok()) {
    out << "GET key3991 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12289: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2062", "key6183", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2062, key6183) size=" << scan_result.size() << std::endl;
  }

  // op 12290: delete_range
  status = db->DeleteRange(write_options, "key5245", "key11486");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 12291: get
  status = db->Get(read_options, "key2492", &value);
  if (status.ok()) {
    out << "GET key2492 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12292: delete
  db->Delete(write_options, "key9701");

  // op 12293: put
  db->Put(write_options, "key2991", "value86688");

  // op 12294: get
  status = db->Get(read_options, "key1625", &value);
  if (status.ok()) {
    out << "GET key1625 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12295: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0247", "key10656", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0247, key10656) size=" << scan_result.size() << std::endl;
  }

  // op 12296: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2003", "key6754", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2003, key6754) size=" << scan_result.size() << std::endl;
  }

  // op 12297: get
  status = db->Get(read_options, "key2459", &value);
  if (status.ok()) {
    out << "GET key2459 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12298: get
  status = db->Get(read_options, "key9565", &value);
  if (status.ok()) {
    out << "GET key9565 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12299: delete
  db->Delete(write_options, "key6276");

  // op 12300: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3466", "key4876", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3466, key4876) size=" << scan_result.size() << std::endl;
  }

  // op 12301: get
  status = db->Get(read_options, "key6027", &value);
  if (status.ok()) {
    out << "GET key6027 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12302: put
  db->Put(write_options, "key5488", "value107611");

  // op 12303: get
  status = db->Get(read_options, "key5180", &value);
  if (status.ok()) {
    out << "GET key5180 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12304: put
  db->Put(write_options, "key1327", "value41536");

  // op 12305: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5418", "key10553", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5418, key10553) size=" << scan_result.size() << std::endl;
  }

  // op 12306: put
  db->Put(write_options, "key6454", "value11986");

  // op 12307: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3469", "key8358", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3469, key8358) size=" << scan_result.size() << std::endl;
  }

  // op 12308: get
  status = db->Get(read_options, "key5698", &value);
  if (status.ok()) {
    out << "GET key5698 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12309: delete_range
  status = db->DeleteRange(write_options, "key1996", "key6004");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 12310: get
  status = db->Get(read_options, "key5346", &value);
  if (status.ok()) {
    out << "GET key5346 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12311: put
  db->Put(write_options, "key8595", "value45690");

  // op 12312: put
  db->Put(write_options, "key10107", "value36958");

  // op 12313: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7598", "key9913", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7598, key9913) size=" << scan_result.size() << std::endl;
  }

  // op 12314: get
  status = db->Get(read_options, "key4518", &value);
  if (status.ok()) {
    out << "GET key4518 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12315: get
  status = db->Get(read_options, "key6845", &value);
  if (status.ok()) {
    out << "GET key6845 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12316: get
  status = db->Get(read_options, "key9496", &value);
  if (status.ok()) {
    out << "GET key9496 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12317: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4747", "key6364", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4747, key6364) size=" << scan_result.size() << std::endl;
  }

  // op 12318: put
  db->Put(write_options, "key9676", "value18235");

  // op 12319: put
  db->Put(write_options, "key8521", "value106024");

  // op 12320: delete_range
  status = db->DeleteRange(write_options, "key1214", "key5452");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 12321: put
  db->Put(write_options, "key3251", "value70936");

  // op 12322: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 12323: put
  db->Put(write_options, "key10333", "value8829");

  // op 12324: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4937", "key10632", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4937, key10632) size=" << scan_result.size() << std::endl;
  }

  // op 12325: put
  db->Put(write_options, "key0895", "value89273");

  // op 12326: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8058", "key11140", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8058, key11140) size=" << scan_result.size() << std::endl;
  }

  // op 12327: get
  status = db->Get(read_options, "key1407", &value);
  if (status.ok()) {
    out << "GET key1407 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12328: get
  status = db->Get(read_options, "key1587", &value);
  if (status.ok()) {
    out << "GET key1587 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12329: get
  status = db->Get(read_options, "key11140", &value);
  if (status.ok()) {
    out << "GET key11140 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12330: delete
  db->Delete(write_options, "key1710");

  // op 12331: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2507", "key9535", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2507, key9535) size=" << scan_result.size() << std::endl;
  }

  // op 12332: put
  db->Put(write_options, "key9857", "value0991");

  // op 12333: get
  status = db->Get(read_options, "key9138", &value);
  if (status.ok()) {
    out << "GET key9138 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12334: put
  db->Put(write_options, "key3429", "value85435");

  // op 12335: put
  db->Put(write_options, "key10447", "value5290");

  // op 12336: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0700", "key4032", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0700, key4032) size=" << scan_result.size() << std::endl;
  }

  // op 12337: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 12338: put
  db->Put(write_options, "key8554", "value31154");

  // op 12339: put
  db->Put(write_options, "key9122", "value37929");

  // op 12340: get
  status = db->Get(read_options, "key7673", &value);
  if (status.ok()) {
    out << "GET key7673 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12341: put
  db->Put(write_options, "key7208", "value90296");

  // op 12342: get
  status = db->Get(read_options, "key10779", &value);
  if (status.ok()) {
    out << "GET key10779 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12343: put
  db->Put(write_options, "key8039", "value28242");

  // op 12344: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 12345: get
  status = db->Get(read_options, "key0595", &value);
  if (status.ok()) {
    out << "GET key0595 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12346: put
  db->Put(write_options, "key6256", "value4735");

  // op 12347: delete_range
  status = db->DeleteRange(write_options, "key0415", "key7888");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 12348: delete
  db->Delete(write_options, "key10371");

  // op 12349: put
  db->Put(write_options, "key1930", "value91666");

  // op 12350: get
  status = db->Get(read_options, "key6740", &value);
  if (status.ok()) {
    out << "GET key6740 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12351: delete
  db->Delete(write_options, "key4528");

  // op 12352: delete
  db->Delete(write_options, "key5902");

  // op 12353: delete
  db->Delete(write_options, "key3182");

  // op 12354: put
  db->Put(write_options, "key5186", "value96564");

  // op 12355: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6588", "key10879", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6588, key10879) size=" << scan_result.size() << std::endl;
  }

  // op 12356: put
  db->Put(write_options, "key5331", "value49106");

  // op 12357: delete
  db->Delete(write_options, "key10982");

  // op 12358: get
  status = db->Get(read_options, "key8503", &value);
  if (status.ok()) {
    out << "GET key8503 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12359: delete
  db->Delete(write_options, "key2720");

  // op 12360: delete
  db->Delete(write_options, "key4817");

  // op 12361: put
  db->Put(write_options, "key9571", "value42596");

  // op 12362: put
  db->Put(write_options, "key8446", "value30827");

  // op 12363: delete_range
  status = db->DeleteRange(write_options, "key4179", "key11704");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 12364: put
  db->Put(write_options, "key3246", "value88290");

  // op 12365: put
  db->Put(write_options, "key7271", "value14293");

  // op 12366: put
  db->Put(write_options, "key4676", "value116768");

  // op 12367: get
  status = db->Get(read_options, "key9638", &value);
  if (status.ok()) {
    out << "GET key9638 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12368: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 12369: put
  db->Put(write_options, "key4416", "value21091");

  // op 12370: put
  db->Put(write_options, "key2241", "value9123");

  // op 12371: put
  db->Put(write_options, "key9347", "value117175");

  // op 12372: delete_range
  status = db->DeleteRange(write_options, "key3056", "key6396");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 12373: delete_range
  status = db->DeleteRange(write_options, "key6935", "key7354");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 12374: put
  db->Put(write_options, "key11344", "value37002");

  // op 12375: get
  status = db->Get(read_options, "key3095", &value);
  if (status.ok()) {
    out << "GET key3095 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12376: put
  db->Put(write_options, "key1221", "value110157");

  // op 12377: put
  db->Put(write_options, "key11327", "value86817");

  // op 12378: put
  db->Put(write_options, "key8366", "value73274");

  // op 12379: delete_range
  status = db->DeleteRange(write_options, "key2637", "key9632");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 12380: get
  status = db->Get(read_options, "key6357", &value);
  if (status.ok()) {
    out << "GET key6357 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12381: delete_range
  status = db->DeleteRange(write_options, "key5695", "key9489");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 12382: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 12383: put
  db->Put(write_options, "key7397", "value9930");

  // op 12384: put
  db->Put(write_options, "key5171", "value22501");

  // op 12385: put
  db->Put(write_options, "key3071", "value88284");

  // op 12386: put
  db->Put(write_options, "key6553", "value76761");

  // op 12387: delete_range
  status = db->DeleteRange(write_options, "key0811", "key5480");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 12388: delete
  db->Delete(write_options, "key11580");

  // op 12389: delete_range
  status = db->DeleteRange(write_options, "key8156", "key11139");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 12390: put
  db->Put(write_options, "key2256", "value4203");

  // op 12391: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0744", "key7589", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0744, key7589) size=" << scan_result.size() << std::endl;
  }

  // op 12392: put
  db->Put(write_options, "key11510", "value38891");

  // op 12393: put
  db->Put(write_options, "key2162", "value65028");

  // op 12394: delete
  db->Delete(write_options, "key1848");

  // op 12395: put
  db->Put(write_options, "key6549", "value3993");

  // op 12396: put
  db->Put(write_options, "key3342", "value85682");

  // op 12397: put
  db->Put(write_options, "key0637", "value27352");

  // op 12398: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 12399: delete_range
  status = db->DeleteRange(write_options, "key6394", "key10602");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 12400: put
  db->Put(write_options, "key8254", "value111036");

  // op 12401: put
  db->Put(write_options, "key8080", "value74422");

  // op 12402: put
  db->Put(write_options, "key2982", "value73519");

  // op 12403: put
  db->Put(write_options, "key8403", "value92817");

  // op 12404: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 12405: delete
  db->Delete(write_options, "key4152");

  // op 12406: get
  status = db->Get(read_options, "key10719", &value);
  if (status.ok()) {
    out << "GET key10719 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12407: put
  db->Put(write_options, "key8123", "value74715");

  // op 12408: get
  status = db->Get(read_options, "key5359", &value);
  if (status.ok()) {
    out << "GET key5359 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12409: delete
  db->Delete(write_options, "key5875");

  // op 12410: put
  db->Put(write_options, "key0217", "value79475");

  // op 12411: put
  db->Put(write_options, "key6456", "value5585");

  // op 12412: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7394", "key11567", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7394, key11567) size=" << scan_result.size() << std::endl;
  }

  // op 12413: delete_range
  status = db->DeleteRange(write_options, "key2132", "key10435");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 12414: put
  db->Put(write_options, "key6118", "value117975");

  // op 12415: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0354", "key2652", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0354, key2652) size=" << scan_result.size() << std::endl;
  }

  // op 12416: delete_range
  status = db->DeleteRange(write_options, "key1884", "key4878");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 12417: put
  db->Put(write_options, "key8475", "value64496");

  // op 12418: put
  db->Put(write_options, "key10955", "value80850");

  // op 12419: delete
  db->Delete(write_options, "key1776");

  // op 12420: put
  db->Put(write_options, "key6322", "value76685");

  // op 12421: get
  status = db->Get(read_options, "key7314", &value);
  if (status.ok()) {
    out << "GET key7314 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12422: put
  db->Put(write_options, "key4727", "value23226");

  // op 12423: delete_range
  status = db->DeleteRange(write_options, "key2968", "key8710");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 12424: get
  status = db->Get(read_options, "key4373", &value);
  if (status.ok()) {
    out << "GET key4373 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12425: get
  status = db->Get(read_options, "key0114", &value);
  if (status.ok()) {
    out << "GET key0114 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12426: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 12427: delete
  db->Delete(write_options, "key3612");

  // op 12428: delete
  db->Delete(write_options, "key8309");

  // op 12429: put
  db->Put(write_options, "key9106", "value3073");

  // op 12430: put
  db->Put(write_options, "key7449", "value62214");

  // op 12431: put
  db->Put(write_options, "key7068", "value115033");

  // op 12432: delete_range
  status = db->DeleteRange(write_options, "key0179", "key10255");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 12433: get
  status = db->Get(read_options, "key5771", &value);
  if (status.ok()) {
    out << "GET key5771 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12434: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2789", "key6938", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2789, key6938) size=" << scan_result.size() << std::endl;
  }

  // op 12435: delete_range
  status = db->DeleteRange(write_options, "key4249", "key8080");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 12436: put
  db->Put(write_options, "key6708", "value45909");

  // op 12437: put
  db->Put(write_options, "key2040", "value25285");

  // op 12438: get
  status = db->Get(read_options, "key10353", &value);
  if (status.ok()) {
    out << "GET key10353 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12439: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1004", "key4381", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1004, key4381) size=" << scan_result.size() << std::endl;
  }

  // op 12440: delete
  db->Delete(write_options, "key10387");

  // op 12441: delete_range
  status = db->DeleteRange(write_options, "key2676", "key10656");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 12442: put
  db->Put(write_options, "key9179", "value22659");

  // op 12443: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0971", "key9041", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0971, key9041) size=" << scan_result.size() << std::endl;
  }

  // op 12444: get
  status = db->Get(read_options, "key1260", &value);
  if (status.ok()) {
    out << "GET key1260 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12445: get
  status = db->Get(read_options, "key1114", &value);
  if (status.ok()) {
    out << "GET key1114 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12446: put
  db->Put(write_options, "key10713", "value72458");

  // op 12447: delete_range
  status = db->DeleteRange(write_options, "key3882", "key5979");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 12448: delete
  db->Delete(write_options, "key3015");

  // op 12449: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2831", "key8072", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2831, key8072) size=" << scan_result.size() << std::endl;
  }

  // op 12450: delete_range
  status = db->DeleteRange(write_options, "key3737", "key5533");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 12451: delete_range
  status = db->DeleteRange(write_options, "key1587", "key1852");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 12452: get
  status = db->Get(read_options, "key2519", &value);
  if (status.ok()) {
    out << "GET key2519 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12453: get
  status = db->Get(read_options, "key5556", &value);
  if (status.ok()) {
    out << "GET key5556 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12454: put
  db->Put(write_options, "key6439", "value49808");

  // op 12455: put
  db->Put(write_options, "key3517", "value88870");

  // op 12456: delete
  db->Delete(write_options, "key3596");

  // op 12457: put
  db->Put(write_options, "key11403", "value36712");

  // op 12458: put
  db->Put(write_options, "key2499", "value30246");

  // op 12459: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0125", "key7657", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0125, key7657) size=" << scan_result.size() << std::endl;
  }

  // op 12460: get
  status = db->Get(read_options, "key9757", &value);
  if (status.ok()) {
    out << "GET key9757 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12461: put
  db->Put(write_options, "key11104", "value67909");

  // op 12462: put
  db->Put(write_options, "key9501", "value105886");

  // op 12463: get
  status = db->Get(read_options, "key4666", &value);
  if (status.ok()) {
    out << "GET key4666 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12464: put
  db->Put(write_options, "key3680", "value10493");

  // op 12465: put
  db->Put(write_options, "key0910", "value34850");

  // op 12466: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9767", "key10104", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9767, key10104) size=" << scan_result.size() << std::endl;
  }

  // op 12467: delete_range
  status = db->DeleteRange(write_options, "key2474", "key9165");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 12468: delete_range
  status = db->DeleteRange(write_options, "key0433", "key7758");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 12469: put
  db->Put(write_options, "key8387", "value41967");

  // op 12470: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1779", "key3192", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1779, key3192) size=" << scan_result.size() << std::endl;
  }

  // op 12471: put
  db->Put(write_options, "key3366", "value58288");

  // op 12472: get
  status = db->Get(read_options, "key10234", &value);
  if (status.ok()) {
    out << "GET key10234 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12473: put
  db->Put(write_options, "key5890", "value83234");

  // op 12474: put
  db->Put(write_options, "key8362", "value7884");

  // op 12475: put
  db->Put(write_options, "key8778", "value114490");

  // op 12476: delete
  db->Delete(write_options, "key0561");

  // op 12477: delete
  db->Delete(write_options, "key4105");

  // op 12478: get
  status = db->Get(read_options, "key7511", &value);
  if (status.ok()) {
    out << "GET key7511 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12479: get
  status = db->Get(read_options, "key5583", &value);
  if (status.ok()) {
    out << "GET key5583 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12480: get
  status = db->Get(read_options, "key1841", &value);
  if (status.ok()) {
    out << "GET key1841 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12481: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6213", "key10259", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6213, key10259) size=" << scan_result.size() << std::endl;
  }

  // op 12482: put
  db->Put(write_options, "key10782", "value58940");

  // op 12483: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0093", "key7050", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0093, key7050) size=" << scan_result.size() << std::endl;
  }

  // op 12484: delete_range
  status = db->DeleteRange(write_options, "key1508", "key2640");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 12485: delete_range
  status = db->DeleteRange(write_options, "key0879", "key2693");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 12486: delete_range
  status = db->DeleteRange(write_options, "key7886", "key8645");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 12487: put
  db->Put(write_options, "key2877", "value52402");

  // op 12488: get
  status = db->Get(read_options, "key2273", &value);
  if (status.ok()) {
    out << "GET key2273 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12489: delete
  db->Delete(write_options, "key4124");

  // op 12490: get
  status = db->Get(read_options, "key0334", &value);
  if (status.ok()) {
    out << "GET key0334 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12491: put
  db->Put(write_options, "key9172", "value85308");

  // op 12492: put
  db->Put(write_options, "key4103", "value83988");

  // op 12493: put
  db->Put(write_options, "key1305", "value61826");

  // op 12494: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 12495: put
  db->Put(write_options, "key10864", "value24692");

  // op 12496: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 12497: get
  status = db->Get(read_options, "key1695", &value);
  if (status.ok()) {
    out << "GET key1695 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12498: delete_range
  status = db->DeleteRange(write_options, "key2755", "key9625");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 12499: put
  db->Put(write_options, "key5740", "value17563");

  // op 12500: get
  status = db->Get(read_options, "key11207", &value);
  if (status.ok()) {
    out << "GET key11207 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12501: put
  db->Put(write_options, "key6206", "value23951");

  // op 12502: get
  status = db->Get(read_options, "key5802", &value);
  if (status.ok()) {
    out << "GET key5802 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12503: get
  status = db->Get(read_options, "key8894", &value);
  if (status.ok()) {
    out << "GET key8894 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12504: put
  db->Put(write_options, "key7490", "value82884");

  // op 12505: delete
  db->Delete(write_options, "key6640");

  // op 12506: delete_range
  status = db->DeleteRange(write_options, "key6156", "key11246");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 12507: get
  status = db->Get(read_options, "key3007", &value);
  if (status.ok()) {
    out << "GET key3007 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12508: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6737", "key10823", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6737, key10823) size=" << scan_result.size() << std::endl;
  }

  // op 12509: delete_range
  status = db->DeleteRange(write_options, "key2385", "key2992");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 12510: delete_range
  status = db->DeleteRange(write_options, "key9493", "key11442");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 12511: put
  db->Put(write_options, "key3650", "value56027");

  // op 12512: get
  status = db->Get(read_options, "key8984", &value);
  if (status.ok()) {
    out << "GET key8984 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12513: put
  db->Put(write_options, "key10267", "value80632");

  // op 12514: put
  db->Put(write_options, "key7681", "value62353");

  // op 12515: delete_range
  status = db->DeleteRange(write_options, "key4436", "key6223");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 12516: put
  db->Put(write_options, "key11062", "value45172");

  // op 12517: delete_range
  status = db->DeleteRange(write_options, "key4604", "key6199");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 12518: put
  db->Put(write_options, "key2239", "value62925");

  // op 12519: put
  db->Put(write_options, "key3849", "value105031");

  // op 12520: put
  db->Put(write_options, "key3504", "value35960");

  // op 12521: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 12522: delete
  db->Delete(write_options, "key5531");

  // op 12523: put
  db->Put(write_options, "key3081", "value22271");

  // op 12524: get
  status = db->Get(read_options, "key1146", &value);
  if (status.ok()) {
    out << "GET key1146 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12525: get
  status = db->Get(read_options, "key10409", &value);
  if (status.ok()) {
    out << "GET key10409 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12526: get
  status = db->Get(read_options, "key4243", &value);
  if (status.ok()) {
    out << "GET key4243 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12527: put
  db->Put(write_options, "key3772", "value86590");

  // op 12528: put
  db->Put(write_options, "key8851", "value114031");

  // op 12529: put
  db->Put(write_options, "key8469", "value96517");

  // op 12530: get
  status = db->Get(read_options, "key4989", &value);
  if (status.ok()) {
    out << "GET key4989 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12531: put
  db->Put(write_options, "key6727", "value30400");

  // op 12532: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1574", "key7006", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1574, key7006) size=" << scan_result.size() << std::endl;
  }

  // op 12533: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1016", "key7616", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1016, key7616) size=" << scan_result.size() << std::endl;
  }

  // op 12534: put
  db->Put(write_options, "key7095", "value52563");

  // op 12535: put
  db->Put(write_options, "key4355", "value28452");

  // op 12536: put
  db->Put(write_options, "key10558", "value57429");

  // op 12537: put
  db->Put(write_options, "key2207", "value57502");

  // op 12538: put
  db->Put(write_options, "key6070", "value13297");

  // op 12539: put
  db->Put(write_options, "key11298", "value56280");

  // op 12540: put
  db->Put(write_options, "key3319", "value96770");

  // op 12541: put
  db->Put(write_options, "key9618", "value63956");

  // op 12542: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5594", "key6869", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5594, key6869) size=" << scan_result.size() << std::endl;
  }

  // op 12543: get
  status = db->Get(read_options, "key5946", &value);
  if (status.ok()) {
    out << "GET key5946 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12544: delete
  db->Delete(write_options, "key2022");

  // op 12545: put
  db->Put(write_options, "key3950", "value10087");

  // op 12546: get
  status = db->Get(read_options, "key4236", &value);
  if (status.ok()) {
    out << "GET key4236 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12547: put
  db->Put(write_options, "key8456", "value77323");

  // op 12548: delete_range
  status = db->DeleteRange(write_options, "key3951", "key10866");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 12549: put
  db->Put(write_options, "key4382", "value73129");

  // op 12550: put
  db->Put(write_options, "key2969", "value111300");

  // op 12551: delete
  db->Delete(write_options, "key11407");

  // op 12552: get
  status = db->Get(read_options, "key2591", &value);
  if (status.ok()) {
    out << "GET key2591 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12553: put
  db->Put(write_options, "key9656", "value13373");

  // op 12554: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4052", "key11409", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4052, key11409) size=" << scan_result.size() << std::endl;
  }

  // op 12555: put
  db->Put(write_options, "key2850", "value64072");

  // op 12556: get
  status = db->Get(read_options, "key8635", &value);
  if (status.ok()) {
    out << "GET key8635 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12557: put
  db->Put(write_options, "key7313", "value102770");

  // op 12558: get
  status = db->Get(read_options, "key0362", &value);
  if (status.ok()) {
    out << "GET key0362 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12559: delete
  db->Delete(write_options, "key11416");

  // op 12560: put
  db->Put(write_options, "key10124", "value84701");

  // op 12561: delete
  db->Delete(write_options, "key5267");

  // op 12562: put
  db->Put(write_options, "key9141", "value76513");

  // op 12563: put
  db->Put(write_options, "key11620", "value65990");

  // op 12564: put
  db->Put(write_options, "key4397", "value76169");

  // op 12565: put
  db->Put(write_options, "key4738", "value8624");

  // op 12566: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 12567: put
  db->Put(write_options, "key9208", "value28214");

  // op 12568: put
  db->Put(write_options, "key8395", "value6285");

  // op 12569: get
  status = db->Get(read_options, "key4721", &value);
  if (status.ok()) {
    out << "GET key4721 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12570: delete_range
  status = db->DeleteRange(write_options, "key2327", "key11615");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 12571: put
  db->Put(write_options, "key3587", "value40798");

  // op 12572: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 12573: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8585", "key9082", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8585, key9082) size=" << scan_result.size() << std::endl;
  }

  // op 12574: put
  db->Put(write_options, "key2462", "value88963");

  // op 12575: get
  status = db->Get(read_options, "key3075", &value);
  if (status.ok()) {
    out << "GET key3075 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12576: put
  db->Put(write_options, "key2347", "value117286");

  // op 12577: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7532", "key8368", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7532, key8368) size=" << scan_result.size() << std::endl;
  }

  // op 12578: delete
  db->Delete(write_options, "key4996");

  // op 12579: get
  status = db->Get(read_options, "key4796", &value);
  if (status.ok()) {
    out << "GET key4796 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12580: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4857", "key7746", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4857, key7746) size=" << scan_result.size() << std::endl;
  }

  // op 12581: put
  db->Put(write_options, "key4637", "value41557");

  // op 12582: get
  status = db->Get(read_options, "key6826", &value);
  if (status.ok()) {
    out << "GET key6826 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12583: put
  db->Put(write_options, "key4477", "value39082");

  // op 12584: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0752", "key7689", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0752, key7689) size=" << scan_result.size() << std::endl;
  }

  // op 12585: put
  db->Put(write_options, "key9853", "value61906");

  // op 12586: get
  status = db->Get(read_options, "key0861", &value);
  if (status.ok()) {
    out << "GET key0861 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12587: put
  db->Put(write_options, "key3645", "value45571");

  // op 12588: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 12589: delete_range
  status = db->DeleteRange(write_options, "key2762", "key11015");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 12590: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 12591: put
  db->Put(write_options, "key8288", "value36685");

  // op 12592: put
  db->Put(write_options, "key4813", "value101995");

  // op 12593: put
  db->Put(write_options, "key3012", "value27045");

  // op 12594: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8314", "key9767", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8314, key9767) size=" << scan_result.size() << std::endl;
  }

  // op 12595: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1606", "key2703", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1606, key2703) size=" << scan_result.size() << std::endl;
  }

  // op 12596: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 12597: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7704", "key10838", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7704, key10838) size=" << scan_result.size() << std::endl;
  }

  // op 12598: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 12599: put
  db->Put(write_options, "key7394", "value11730");

  // op 12600: put
  db->Put(write_options, "key1720", "value96546");

  // op 12601: put
  db->Put(write_options, "key3441", "value113292");

  // op 12602: delete_range
  status = db->DeleteRange(write_options, "key2157", "key9931");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 12603: put
  db->Put(write_options, "key0867", "value103544");

  // op 12604: delete_range
  status = db->DeleteRange(write_options, "key0663", "key5502");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 12605: get
  status = db->Get(read_options, "key8219", &value);
  if (status.ok()) {
    out << "GET key8219 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12606: put
  db->Put(write_options, "key9335", "value5493");

  // op 12607: put
  db->Put(write_options, "key7622", "value2815");

  // op 12608: delete
  db->Delete(write_options, "key8362");

  // op 12609: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 12610: delete_range
  status = db->DeleteRange(write_options, "key6032", "key7544");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 12611: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0149", "key4665", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0149, key4665) size=" << scan_result.size() << std::endl;
  }

  // op 12612: put
  db->Put(write_options, "key1226", "value25786");

  // op 12613: delete_range
  status = db->DeleteRange(write_options, "key1356", "key9595");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 12614: put
  db->Put(write_options, "key1567", "value95383");

  // op 12615: put
  db->Put(write_options, "key4422", "value76541");

  // op 12616: put
  db->Put(write_options, "key0427", "value15296");

  // op 12617: delete_range
  status = db->DeleteRange(write_options, "key7846", "key7850");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 12618: get
  status = db->Get(read_options, "key5382", &value);
  if (status.ok()) {
    out << "GET key5382 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12619: put
  db->Put(write_options, "key5331", "value102809");

  // op 12620: delete_range
  status = db->DeleteRange(write_options, "key3984", "key4389");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 12621: delete_range
  status = db->DeleteRange(write_options, "key6147", "key8440");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 12622: delete
  db->Delete(write_options, "key0288");

  // op 12623: put
  db->Put(write_options, "key4378", "value39480");

  // op 12624: put
  db->Put(write_options, "key7047", "value101146");

  // op 12625: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8303", "key9084", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8303, key9084) size=" << scan_result.size() << std::endl;
  }

  // op 12626: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 12627: get
  status = db->Get(read_options, "key9928", &value);
  if (status.ok()) {
    out << "GET key9928 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12628: get
  status = db->Get(read_options, "key3004", &value);
  if (status.ok()) {
    out << "GET key3004 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12629: put
  db->Put(write_options, "key5287", "value48301");

  // op 12630: delete_range
  status = db->DeleteRange(write_options, "key2289", "key7358");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 12631: delete_range
  status = db->DeleteRange(write_options, "key10314", "key11236");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 12632: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0664", "key5128", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0664, key5128) size=" << scan_result.size() << std::endl;
  }

  // op 12633: put
  db->Put(write_options, "key1596", "value93851");

  // op 12634: put
  db->Put(write_options, "key7392", "value105367");

  // op 12635: put
  db->Put(write_options, "key3159", "value117514");

  // op 12636: delete
  db->Delete(write_options, "key6849");

  // op 12637: put
  db->Put(write_options, "key4435", "value64259");

  // op 12638: get
  status = db->Get(read_options, "key11522", &value);
  if (status.ok()) {
    out << "GET key11522 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12639: delete_range
  status = db->DeleteRange(write_options, "key1479", "key9703");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 12640: get
  status = db->Get(read_options, "key10258", &value);
  if (status.ok()) {
    out << "GET key10258 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12641: put
  db->Put(write_options, "key1389", "value76821");

  // op 12642: put
  db->Put(write_options, "key6342", "value70972");

  // op 12643: put
  db->Put(write_options, "key6726", "value90609");

  // op 12644: get
  status = db->Get(read_options, "key8406", &value);
  if (status.ok()) {
    out << "GET key8406 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12645: get
  status = db->Get(read_options, "key0339", &value);
  if (status.ok()) {
    out << "GET key0339 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12646: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9676", "key11153", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9676, key11153) size=" << scan_result.size() << std::endl;
  }

  // op 12647: put
  db->Put(write_options, "key9191", "value69654");

  // op 12648: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2016", "key5899", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2016, key5899) size=" << scan_result.size() << std::endl;
  }

  // op 12649: put
  db->Put(write_options, "key4018", "value96026");

  // op 12650: put
  db->Put(write_options, "key0889", "value84191");

  // op 12651: get
  status = db->Get(read_options, "key7317", &value);
  if (status.ok()) {
    out << "GET key7317 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12652: put
  db->Put(write_options, "key8293", "value73910");

  // op 12653: get
  status = db->Get(read_options, "key1418", &value);
  if (status.ok()) {
    out << "GET key1418 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12654: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 12655: put
  db->Put(write_options, "key7426", "value29342");

  // op 12656: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3041", "key9004", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3041, key9004) size=" << scan_result.size() << std::endl;
  }

  // op 12657: put
  db->Put(write_options, "key10200", "value33600");

  // op 12658: put
  db->Put(write_options, "key1230", "value72096");

  // op 12659: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 12660: delete_range
  status = db->DeleteRange(write_options, "key7562", "key9348");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 12661: delete_range
  status = db->DeleteRange(write_options, "key1434", "key4400");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 12662: put
  db->Put(write_options, "key0298", "value33825");

  // op 12663: put
  db->Put(write_options, "key0695", "value102996");

  // op 12664: get
  status = db->Get(read_options, "key9381", &value);
  if (status.ok()) {
    out << "GET key9381 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12665: put
  db->Put(write_options, "key11228", "value90737");

  // op 12666: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3695", "key10399", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3695, key10399) size=" << scan_result.size() << std::endl;
  }

  // op 12667: delete
  db->Delete(write_options, "key1732");

  // op 12668: put
  db->Put(write_options, "key4136", "value40666");

  // op 12669: put
  db->Put(write_options, "key6186", "value116856");

  // op 12670: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 12671: put
  db->Put(write_options, "key4575", "value95361");

  // op 12672: put
  db->Put(write_options, "key1989", "value3574");

  // op 12673: put
  db->Put(write_options, "key4079", "value14224");

  // op 12674: get
  status = db->Get(read_options, "key6765", &value);
  if (status.ok()) {
    out << "GET key6765 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12675: get
  status = db->Get(read_options, "key4851", &value);
  if (status.ok()) {
    out << "GET key4851 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12676: put
  db->Put(write_options, "key9575", "value95973");

  // op 12677: put
  db->Put(write_options, "key7909", "value36202");

  // op 12678: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1507", "key6328", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1507, key6328) size=" << scan_result.size() << std::endl;
  }

  // op 12679: put
  db->Put(write_options, "key7893", "value50089");

  // op 12680: put
  db->Put(write_options, "key5435", "value17361");

  // op 12681: put
  db->Put(write_options, "key1219", "value43035");

  // op 12682: put
  db->Put(write_options, "key11588", "value113031");

  // op 12683: delete_range
  status = db->DeleteRange(write_options, "key0242", "key7081");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 12684: get
  status = db->Get(read_options, "key8777", &value);
  if (status.ok()) {
    out << "GET key8777 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12685: get
  status = db->Get(read_options, "key9757", &value);
  if (status.ok()) {
    out << "GET key9757 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12686: get
  status = db->Get(read_options, "key10467", &value);
  if (status.ok()) {
    out << "GET key10467 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12687: put
  db->Put(write_options, "key2372", "value88038");

  // op 12688: delete
  db->Delete(write_options, "key7370");

  // op 12689: put
  db->Put(write_options, "key1661", "value41385");

  // op 12690: put
  db->Put(write_options, "key5957", "value82979");

  // op 12691: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3141", "key4670", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3141, key4670) size=" << scan_result.size() << std::endl;
  }

  // op 12692: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2430", "key3741", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2430, key3741) size=" << scan_result.size() << std::endl;
  }

  // op 12693: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5123", "key5181", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5123, key5181) size=" << scan_result.size() << std::endl;
  }

  // op 12694: get
  status = db->Get(read_options, "key8493", &value);
  if (status.ok()) {
    out << "GET key8493 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12695: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2916", "key10257", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2916, key10257) size=" << scan_result.size() << std::endl;
  }

  // op 12696: delete_range
  status = db->DeleteRange(write_options, "key6494", "key6977");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 12697: put
  db->Put(write_options, "key11315", "value58397");

  // op 12698: put
  db->Put(write_options, "key5389", "value10786");

  // op 12699: put
  db->Put(write_options, "key6759", "value49873");

  // op 12700: delete_range
  status = db->DeleteRange(write_options, "key0598", "key10868");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 12701: get
  status = db->Get(read_options, "key10602", &value);
  if (status.ok()) {
    out << "GET key10602 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12702: delete
  db->Delete(write_options, "key7901");

  // op 12703: put
  db->Put(write_options, "key3364", "value96160");

  // op 12704: put
  db->Put(write_options, "key4755", "value79977");

  // op 12705: get
  status = db->Get(read_options, "key4165", &value);
  if (status.ok()) {
    out << "GET key4165 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12706: delete_range
  status = db->DeleteRange(write_options, "key11459", "key11610");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 12707: put
  db->Put(write_options, "key5232", "value78450");

  // op 12708: put
  db->Put(write_options, "key6876", "value96788");

  // op 12709: put
  db->Put(write_options, "key3422", "value60585");

  // op 12710: put
  db->Put(write_options, "key0798", "value55028");

  // op 12711: put
  db->Put(write_options, "key4047", "value113988");

  // op 12712: delete_range
  status = db->DeleteRange(write_options, "key2149", "key10885");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 12713: put
  db->Put(write_options, "key11444", "value92497");

  // op 12714: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6997", "key11660", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6997, key11660) size=" << scan_result.size() << std::endl;
  }

  // op 12715: put
  db->Put(write_options, "key6147", "value103138");

  // op 12716: get
  status = db->Get(read_options, "key6791", &value);
  if (status.ok()) {
    out << "GET key6791 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12717: delete_range
  status = db->DeleteRange(write_options, "key7222", "key10074");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 12718: put
  db->Put(write_options, "key1118", "value41368");

  // op 12719: delete
  db->Delete(write_options, "key6677");

  // op 12720: put
  db->Put(write_options, "key4671", "value101938");

  // op 12721: put
  db->Put(write_options, "key7406", "value97889");

  // op 12722: put
  db->Put(write_options, "key8820", "value95767");

  // op 12723: delete_range
  status = db->DeleteRange(write_options, "key4157", "key8581");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 12724: delete
  db->Delete(write_options, "key2915");

  // op 12725: delete
  db->Delete(write_options, "key7899");

  // op 12726: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 12727: get
  status = db->Get(read_options, "key5791", &value);
  if (status.ok()) {
    out << "GET key5791 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12728: put
  db->Put(write_options, "key8009", "value63331");

  // op 12729: put
  db->Put(write_options, "key11709", "value114126");

  // op 12730: get
  status = db->Get(read_options, "key5221", &value);
  if (status.ok()) {
    out << "GET key5221 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12731: get
  status = db->Get(read_options, "key10048", &value);
  if (status.ok()) {
    out << "GET key10048 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12732: delete_range
  status = db->DeleteRange(write_options, "key1966", "key7821");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 12733: delete
  db->Delete(write_options, "key10279");

  // op 12734: put
  db->Put(write_options, "key6985", "value3888");

  // op 12735: delete_range
  status = db->DeleteRange(write_options, "key2073", "key6263");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 12736: put
  db->Put(write_options, "key6261", "value118159");

  // op 12737: get
  status = db->Get(read_options, "key8150", &value);
  if (status.ok()) {
    out << "GET key8150 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12738: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1805", "key10228", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1805, key10228) size=" << scan_result.size() << std::endl;
  }

  // op 12739: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3319", "key8652", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3319, key8652) size=" << scan_result.size() << std::endl;
  }

  // op 12740: put
  db->Put(write_options, "key5015", "value94560");

  // op 12741: get
  status = db->Get(read_options, "key8145", &value);
  if (status.ok()) {
    out << "GET key8145 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12742: get
  status = db->Get(read_options, "key2405", &value);
  if (status.ok()) {
    out << "GET key2405 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12743: delete_range
  status = db->DeleteRange(write_options, "key6283", "key8996");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 12744: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2854", "key4569", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2854, key4569) size=" << scan_result.size() << std::endl;
  }

  // op 12745: put
  db->Put(write_options, "key9985", "value4461");

  // op 12746: put
  db->Put(write_options, "key0253", "value108559");

  // op 12747: get
  status = db->Get(read_options, "key0736", &value);
  if (status.ok()) {
    out << "GET key0736 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12748: delete
  db->Delete(write_options, "key6778");

  // op 12749: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2751", "key5022", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2751, key5022) size=" << scan_result.size() << std::endl;
  }

  // op 12750: get
  status = db->Get(read_options, "key1626", &value);
  if (status.ok()) {
    out << "GET key1626 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12751: get
  status = db->Get(read_options, "key2583", &value);
  if (status.ok()) {
    out << "GET key2583 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12752: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0607", "key6673", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0607, key6673) size=" << scan_result.size() << std::endl;
  }

  // op 12753: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1776", "key6005", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1776, key6005) size=" << scan_result.size() << std::endl;
  }

  // op 12754: delete_range
  status = db->DeleteRange(write_options, "key8526", "key9825");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 12755: get
  status = db->Get(read_options, "key8368", &value);
  if (status.ok()) {
    out << "GET key8368 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12756: put
  db->Put(write_options, "key0585", "value74968");

  // op 12757: put
  db->Put(write_options, "key7151", "value59927");

  // op 12758: delete
  db->Delete(write_options, "key0359");

  // op 12759: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3447", "key7719", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3447, key7719) size=" << scan_result.size() << std::endl;
  }

  // op 12760: put
  db->Put(write_options, "key11419", "value50688");

  // op 12761: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0910", "key7834", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0910, key7834) size=" << scan_result.size() << std::endl;
  }

  // op 12762: get
  status = db->Get(read_options, "key3404", &value);
  if (status.ok()) {
    out << "GET key3404 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12763: get
  status = db->Get(read_options, "key6539", &value);
  if (status.ok()) {
    out << "GET key6539 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12764: delete
  db->Delete(write_options, "key1004");

  // op 12765: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4459", "key6006", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4459, key6006) size=" << scan_result.size() << std::endl;
  }

  // op 12766: put
  db->Put(write_options, "key6141", "value102552");

  // op 12767: put
  db->Put(write_options, "key4535", "value33627");

  // op 12768: put
  db->Put(write_options, "key1203", "value46703");

  // op 12769: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9364", "key9803", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9364, key9803) size=" << scan_result.size() << std::endl;
  }

  // op 12770: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1824", "key10577", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1824, key10577) size=" << scan_result.size() << std::endl;
  }

  // op 12771: delete_range
  status = db->DeleteRange(write_options, "key3817", "key7852");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 12772: put
  db->Put(write_options, "key2128", "value75000");

  // op 12773: put
  db->Put(write_options, "key1833", "value67799");

  // op 12774: put
  db->Put(write_options, "key7069", "value79708");

  // op 12775: get
  status = db->Get(read_options, "key9559", &value);
  if (status.ok()) {
    out << "GET key9559 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12776: get
  status = db->Get(read_options, "key1550", &value);
  if (status.ok()) {
    out << "GET key1550 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12777: get
  status = db->Get(read_options, "key8720", &value);
  if (status.ok()) {
    out << "GET key8720 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12778: put
  db->Put(write_options, "key5620", "value14213");

  // op 12779: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2213", "key11064", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2213, key11064) size=" << scan_result.size() << std::endl;
  }

  // op 12780: put
  db->Put(write_options, "key2702", "value24569");

  // op 12781: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2670", "key10961", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2670, key10961) size=" << scan_result.size() << std::endl;
  }

  // op 12782: get
  status = db->Get(read_options, "key3534", &value);
  if (status.ok()) {
    out << "GET key3534 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12783: put
  db->Put(write_options, "key0062", "value107501");

  // op 12784: delete
  db->Delete(write_options, "key3775");

  // op 12785: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8009", "key8049", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8009, key8049) size=" << scan_result.size() << std::endl;
  }

  // op 12786: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 12787: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 12788: delete
  db->Delete(write_options, "key11099");

  // op 12789: put
  db->Put(write_options, "key2150", "value59196");

  // op 12790: get
  status = db->Get(read_options, "key10261", &value);
  if (status.ok()) {
    out << "GET key10261 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12791: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1378", "key2282", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1378, key2282) size=" << scan_result.size() << std::endl;
  }

  // op 12792: put
  db->Put(write_options, "key9486", "value65925");

  // op 12793: put
  db->Put(write_options, "key10426", "value3747");

  // op 12794: put
  db->Put(write_options, "key1822", "value59107");

  // op 12795: put
  db->Put(write_options, "key2220", "value74989");

  // op 12796: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 12797: put
  db->Put(write_options, "key4169", "value45099");

  // op 12798: delete
  db->Delete(write_options, "key8030");

  // op 12799: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5716", "key6824", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5716, key6824) size=" << scan_result.size() << std::endl;
  }

  // op 12800: get
  status = db->Get(read_options, "key9167", &value);
  if (status.ok()) {
    out << "GET key9167 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12801: delete
  db->Delete(write_options, "key8029");

  // op 12802: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1336", "key7467", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1336, key7467) size=" << scan_result.size() << std::endl;
  }

  // op 12803: put
  db->Put(write_options, "key2054", "value30426");

  // op 12804: get
  status = db->Get(read_options, "key11310", &value);
  if (status.ok()) {
    out << "GET key11310 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12805: get
  status = db->Get(read_options, "key0893", &value);
  if (status.ok()) {
    out << "GET key0893 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12806: delete
  db->Delete(write_options, "key1858");

  // op 12807: put
  db->Put(write_options, "key8809", "value61256");

  // op 12808: get
  status = db->Get(read_options, "key9139", &value);
  if (status.ok()) {
    out << "GET key9139 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12809: delete
  db->Delete(write_options, "key6295");

  // op 12810: delete
  db->Delete(write_options, "key10276");

  // op 12811: put
  db->Put(write_options, "key4096", "value36502");

  // op 12812: put
  db->Put(write_options, "key3933", "value91003");

  // op 12813: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9753", "key10977", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9753, key10977) size=" << scan_result.size() << std::endl;
  }

  // op 12814: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10594", "key11029", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10594, key11029) size=" << scan_result.size() << std::endl;
  }

  // op 12815: delete
  db->Delete(write_options, "key1603");

  // op 12816: delete_range
  status = db->DeleteRange(write_options, "key1131", "key10898");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 12817: put
  db->Put(write_options, "key1228", "value71026");

  // op 12818: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8946", "key10997", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8946, key10997) size=" << scan_result.size() << std::endl;
  }

  // op 12819: get
  status = db->Get(read_options, "key2293", &value);
  if (status.ok()) {
    out << "GET key2293 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12820: put
  db->Put(write_options, "key3273", "value52870");

  // op 12821: put
  db->Put(write_options, "key6802", "value114230");

  // op 12822: delete_range
  status = db->DeleteRange(write_options, "key0514", "key1861");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 12823: put
  db->Put(write_options, "key9449", "value24837");

  // op 12824: put
  db->Put(write_options, "key3670", "value30066");

  // op 12825: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 12826: put
  db->Put(write_options, "key8236", "value90660");

  // op 12827: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0685", "key6456", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0685, key6456) size=" << scan_result.size() << std::endl;
  }

  // op 12828: put
  db->Put(write_options, "key4916", "value39318");

  // op 12829: put
  db->Put(write_options, "key4553", "value21561");

  // op 12830: put
  db->Put(write_options, "key2116", "value4787");

  // op 12831: delete_range
  status = db->DeleteRange(write_options, "key1761", "key7459");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 12832: get
  status = db->Get(read_options, "key8033", &value);
  if (status.ok()) {
    out << "GET key8033 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12833: get
  status = db->Get(read_options, "key10878", &value);
  if (status.ok()) {
    out << "GET key10878 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12834: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6755", "key6808", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6755, key6808) size=" << scan_result.size() << std::endl;
  }

  // op 12835: get
  status = db->Get(read_options, "key4312", &value);
  if (status.ok()) {
    out << "GET key4312 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12836: delete
  db->Delete(write_options, "key9657");

  // op 12837: put
  db->Put(write_options, "key5015", "value41418");

  // op 12838: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1670", "key10722", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1670, key10722) size=" << scan_result.size() << std::endl;
  }

  // op 12839: put
  db->Put(write_options, "key0667", "value4446");

  // op 12840: put
  db->Put(write_options, "key9222", "value59730");

  // op 12841: get
  status = db->Get(read_options, "key6525", &value);
  if (status.ok()) {
    out << "GET key6525 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12842: put
  db->Put(write_options, "key9193", "value5469");

  // op 12843: put
  db->Put(write_options, "key4364", "value74702");

  // op 12844: delete
  db->Delete(write_options, "key11124");

  // op 12845: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 12846: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 12847: put
  db->Put(write_options, "key7209", "value81194");

  // op 12848: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4605", "key5988", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4605, key5988) size=" << scan_result.size() << std::endl;
  }

  // op 12849: put
  db->Put(write_options, "key7873", "value82284");

  // op 12850: delete_range
  status = db->DeleteRange(write_options, "key0105", "key1288");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 12851: delete_range
  status = db->DeleteRange(write_options, "key1539", "key3404");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 12852: put
  db->Put(write_options, "key10108", "value18060");

  // op 12853: put
  db->Put(write_options, "key2348", "value7571");

  // op 12854: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6113", "key6155", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6113, key6155) size=" << scan_result.size() << std::endl;
  }

  // op 12855: put
  db->Put(write_options, "key8092", "value83257");

  // op 12856: get
  status = db->Get(read_options, "key3300", &value);
  if (status.ok()) {
    out << "GET key3300 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12857: put
  db->Put(write_options, "key2084", "value41977");

  // op 12858: delete_range
  status = db->DeleteRange(write_options, "key6361", "key10081");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 12859: delete_range
  status = db->DeleteRange(write_options, "key2472", "key9436");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 12860: get
  status = db->Get(read_options, "key9790", &value);
  if (status.ok()) {
    out << "GET key9790 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12861: put
  db->Put(write_options, "key5051", "value102959");

  // op 12862: delete
  db->Delete(write_options, "key3188");

  // op 12863: get
  status = db->Get(read_options, "key10410", &value);
  if (status.ok()) {
    out << "GET key10410 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12864: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7263", "key8084", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7263, key8084) size=" << scan_result.size() << std::endl;
  }

  // op 12865: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0110", "key5006", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0110, key5006) size=" << scan_result.size() << std::endl;
  }

  // op 12866: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2870", "key10202", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2870, key10202) size=" << scan_result.size() << std::endl;
  }

  // op 12867: put
  db->Put(write_options, "key3275", "value15934");

  // op 12868: get
  status = db->Get(read_options, "key0902", &value);
  if (status.ok()) {
    out << "GET key0902 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12869: delete
  db->Delete(write_options, "key10368");

  // op 12870: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2747", "key7732", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2747, key7732) size=" << scan_result.size() << std::endl;
  }

  // op 12871: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 12872: put
  db->Put(write_options, "key1405", "value117137");

  // op 12873: get
  status = db->Get(read_options, "key10230", &value);
  if (status.ok()) {
    out << "GET key10230 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12874: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 12875: put
  db->Put(write_options, "key11078", "value49196");

  // op 12876: get
  status = db->Get(read_options, "key8304", &value);
  if (status.ok()) {
    out << "GET key8304 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12877: delete_range
  status = db->DeleteRange(write_options, "key1545", "key11392");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 12878: delete_range
  status = db->DeleteRange(write_options, "key2002", "key4260");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 12879: put
  db->Put(write_options, "key4166", "value98104");

  // op 12880: put
  db->Put(write_options, "key7961", "value93357");

  // op 12881: put
  db->Put(write_options, "key5114", "value99127");

  // op 12882: get
  status = db->Get(read_options, "key5477", &value);
  if (status.ok()) {
    out << "GET key5477 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12883: get
  status = db->Get(read_options, "key0912", &value);
  if (status.ok()) {
    out << "GET key0912 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12884: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 12885: get
  status = db->Get(read_options, "key9032", &value);
  if (status.ok()) {
    out << "GET key9032 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12886: put
  db->Put(write_options, "key9658", "value25810");

  // op 12887: put
  db->Put(write_options, "key9066", "value116311");

  // op 12888: put
  db->Put(write_options, "key11723", "value52191");

  // op 12889: delete_range
  status = db->DeleteRange(write_options, "key3899", "key8164");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 12890: delete
  db->Delete(write_options, "key11561");

  // op 12891: get
  status = db->Get(read_options, "key7626", &value);
  if (status.ok()) {
    out << "GET key7626 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12892: delete
  db->Delete(write_options, "key7720");

  // op 12893: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 12894: put
  db->Put(write_options, "key3488", "value41307");

  // op 12895: put
  db->Put(write_options, "key10057", "value96868");

  // op 12896: put
  db->Put(write_options, "key3428", "value74492");

  // op 12897: delete
  db->Delete(write_options, "key10745");

  // op 12898: get
  status = db->Get(read_options, "key1751", &value);
  if (status.ok()) {
    out << "GET key1751 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12899: delete_range
  status = db->DeleteRange(write_options, "key0172", "key7718");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 12900: put
  db->Put(write_options, "key10977", "value72378");

  // op 12901: get
  status = db->Get(read_options, "key7167", &value);
  if (status.ok()) {
    out << "GET key7167 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12902: put
  db->Put(write_options, "key6664", "value111016");

  // op 12903: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2784", "key8930", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2784, key8930) size=" << scan_result.size() << std::endl;
  }

  // op 12904: get
  status = db->Get(read_options, "key9663", &value);
  if (status.ok()) {
    out << "GET key9663 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12905: put
  db->Put(write_options, "key6915", "value47749");

  // op 12906: put
  db->Put(write_options, "key6345", "value76362");

  // op 12907: get
  status = db->Get(read_options, "key9042", &value);
  if (status.ok()) {
    out << "GET key9042 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12908: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1816", "key11144", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1816, key11144) size=" << scan_result.size() << std::endl;
  }

  // op 12909: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3913", "key11601", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3913, key11601) size=" << scan_result.size() << std::endl;
  }

  // op 12910: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 12911: put
  db->Put(write_options, "key0703", "value7675");

  // op 12912: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 12913: get
  status = db->Get(read_options, "key11177", &value);
  if (status.ok()) {
    out << "GET key11177 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12914: get
  status = db->Get(read_options, "key6104", &value);
  if (status.ok()) {
    out << "GET key6104 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12915: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5149", "key7392", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5149, key7392) size=" << scan_result.size() << std::endl;
  }

  // op 12916: put
  db->Put(write_options, "key5508", "value9341");

  // op 12917: get
  status = db->Get(read_options, "key1059", &value);
  if (status.ok()) {
    out << "GET key1059 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12918: put
  db->Put(write_options, "key1154", "value97271");

  // op 12919: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3721", "key7967", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3721, key7967) size=" << scan_result.size() << std::endl;
  }

  // op 12920: put
  db->Put(write_options, "key0518", "value0516");

  // op 12921: put
  db->Put(write_options, "key0737", "value25943");

  // op 12922: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6275", "key7151", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6275, key7151) size=" << scan_result.size() << std::endl;
  }

  // op 12923: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 12924: put
  db->Put(write_options, "key9683", "value30560");

  // op 12925: get
  status = db->Get(read_options, "key9492", &value);
  if (status.ok()) {
    out << "GET key9492 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12926: delete
  db->Delete(write_options, "key8329");

  // op 12927: delete_range
  status = db->DeleteRange(write_options, "key8710", "key9792");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 12928: delete
  db->Delete(write_options, "key1120");

  // op 12929: put
  db->Put(write_options, "key2068", "value7604");

  // op 12930: put
  db->Put(write_options, "key4226", "value50850");

  // op 12931: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1776", "key8684", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1776, key8684) size=" << scan_result.size() << std::endl;
  }

  // op 12932: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5910", "key6077", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5910, key6077) size=" << scan_result.size() << std::endl;
  }

  // op 12933: put
  db->Put(write_options, "key10149", "value92109");

  // op 12934: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1744", "key7072", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1744, key7072) size=" << scan_result.size() << std::endl;
  }

  // op 12935: put
  db->Put(write_options, "key2489", "value111200");

  // op 12936: delete
  db->Delete(write_options, "key7717");

  // op 12937: put
  db->Put(write_options, "key5552", "value14855");

  // op 12938: delete_range
  status = db->DeleteRange(write_options, "key1834", "key9247");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 12939: get
  status = db->Get(read_options, "key4889", &value);
  if (status.ok()) {
    out << "GET key4889 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12940: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4743", "key8930", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4743, key8930) size=" << scan_result.size() << std::endl;
  }

  // op 12941: get
  status = db->Get(read_options, "key2968", &value);
  if (status.ok()) {
    out << "GET key2968 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12942: put
  db->Put(write_options, "key3032", "value48784");

  // op 12943: delete_range
  status = db->DeleteRange(write_options, "key4455", "key8223");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 12944: put
  db->Put(write_options, "key1436", "value10459");

  // op 12945: delete_range
  status = db->DeleteRange(write_options, "key5624", "key7481");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 12946: get
  status = db->Get(read_options, "key11338", &value);
  if (status.ok()) {
    out << "GET key11338 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12947: get
  status = db->Get(read_options, "key2570", &value);
  if (status.ok()) {
    out << "GET key2570 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12948: get
  status = db->Get(read_options, "key7937", &value);
  if (status.ok()) {
    out << "GET key7937 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12949: put
  db->Put(write_options, "key1220", "value81335");

  // op 12950: put
  db->Put(write_options, "key8727", "value91020");

  // op 12951: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 12952: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5432", "key9103", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5432, key9103) size=" << scan_result.size() << std::endl;
  }

  // op 12953: delete
  db->Delete(write_options, "key4189");

  // op 12954: put
  db->Put(write_options, "key2454", "value58089");

  // op 12955: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4120", "key7783", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4120, key7783) size=" << scan_result.size() << std::endl;
  }

  // op 12956: put
  db->Put(write_options, "key4059", "value52504");

  // op 12957: put
  db->Put(write_options, "key2307", "value50117");

  // op 12958: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9094", "key9955", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9094, key9955) size=" << scan_result.size() << std::endl;
  }

  // op 12959: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8552", "key10818", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8552, key10818) size=" << scan_result.size() << std::endl;
  }

  // op 12960: delete_range
  status = db->DeleteRange(write_options, "key8319", "key11521");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 12961: get
  status = db->Get(read_options, "key7534", &value);
  if (status.ok()) {
    out << "GET key7534 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12962: delete
  db->Delete(write_options, "key2543");

  // op 12963: delete_range
  status = db->DeleteRange(write_options, "key0371", "key1853");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 12964: put
  db->Put(write_options, "key3110", "value87730");

  // op 12965: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 12966: get
  status = db->Get(read_options, "key8270", &value);
  if (status.ok()) {
    out << "GET key8270 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12967: put
  db->Put(write_options, "key0871", "value46315");

  // op 12968: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3530", "key9709", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3530, key9709) size=" << scan_result.size() << std::endl;
  }

  // op 12969: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1349", "key3858", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1349, key3858) size=" << scan_result.size() << std::endl;
  }

  // op 12970: get
  status = db->Get(read_options, "key2642", &value);
  if (status.ok()) {
    out << "GET key2642 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12971: put
  db->Put(write_options, "key9496", "value25738");

  // op 12972: put
  db->Put(write_options, "key2776", "value79697");

  // op 12973: delete
  db->Delete(write_options, "key7633");

  // op 12974: put
  db->Put(write_options, "key1564", "value106348");

  // op 12975: delete_range
  status = db->DeleteRange(write_options, "key6391", "key8009");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 12976: put
  db->Put(write_options, "key3511", "value40217");

  // op 12977: delete_range
  status = db->DeleteRange(write_options, "key9085", "key10289");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 12978: put
  db->Put(write_options, "key8270", "value38097");

  // op 12979: put
  db->Put(write_options, "key0369", "value66282");

  // op 12980: get
  status = db->Get(read_options, "key9646", &value);
  if (status.ok()) {
    out << "GET key9646 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12981: put
  db->Put(write_options, "key9615", "value50656");

  // op 12982: get
  status = db->Get(read_options, "key2901", &value);
  if (status.ok()) {
    out << "GET key2901 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12983: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 12984: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0483", "key10920", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0483, key10920) size=" << scan_result.size() << std::endl;
  }

  // op 12985: put
  db->Put(write_options, "key1434", "value28329");

  // op 12986: put
  db->Put(write_options, "key2273", "value40866");

  // op 12987: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2678", "key7219", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2678, key7219) size=" << scan_result.size() << std::endl;
  }

  // op 12988: put
  db->Put(write_options, "key8351", "value81970");

  // op 12989: put
  db->Put(write_options, "key8594", "value28510");

  // op 12990: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2574", "key3292", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2574, key3292) size=" << scan_result.size() << std::endl;
  }

  // op 12991: get
  status = db->Get(read_options, "key0466", &value);
  if (status.ok()) {
    out << "GET key0466 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 12992: put
  db->Put(write_options, "key5874", "value74959");

  // op 12993: put
  db->Put(write_options, "key4442", "value111945");

  // op 12994: put
  db->Put(write_options, "key5233", "value71634");

  // op 12995: put
  db->Put(write_options, "key3447", "value52873");

  // op 12996: put
  db->Put(write_options, "key7397", "value2030");

  // op 12997: put
  db->Put(write_options, "key4181", "value13811");

  // op 12998: put
  db->Put(write_options, "key2437", "value13636");

  // op 12999: delete
  db->Delete(write_options, "key6052");

  // op 13000: put
  db->Put(write_options, "key4549", "value12499");

  // op 13001: put
  db->Put(write_options, "key8547", "value32771");

  // op 13002: get
  status = db->Get(read_options, "key1300", &value);
  if (status.ok()) {
    out << "GET key1300 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13003: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3680", "key4873", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3680, key4873) size=" << scan_result.size() << std::endl;
  }

  // op 13004: get
  status = db->Get(read_options, "key9367", &value);
  if (status.ok()) {
    out << "GET key9367 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13005: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 13006: put
  db->Put(write_options, "key11571", "value53056");

  // op 13007: delete
  db->Delete(write_options, "key11669");

  // op 13008: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1175", "key9655", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1175, key9655) size=" << scan_result.size() << std::endl;
  }

  // op 13009: delete
  db->Delete(write_options, "key9060");

  // op 13010: put
  db->Put(write_options, "key6276", "value20464");

  // op 13011: get
  status = db->Get(read_options, "key6885", &value);
  if (status.ok()) {
    out << "GET key6885 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13012: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3113", "key4977", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3113, key4977) size=" << scan_result.size() << std::endl;
  }

  // op 13013: delete_range
  status = db->DeleteRange(write_options, "key2757", "key10300");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13014: put
  db->Put(write_options, "key11305", "value109799");

  // op 13015: get
  status = db->Get(read_options, "key9220", &value);
  if (status.ok()) {
    out << "GET key9220 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13016: delete
  db->Delete(write_options, "key0771");

  // op 13017: get
  status = db->Get(read_options, "key2156", &value);
  if (status.ok()) {
    out << "GET key2156 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13018: put
  db->Put(write_options, "key0651", "value42544");

  // op 13019: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5438", "key5781", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5438, key5781) size=" << scan_result.size() << std::endl;
  }

  // op 13020: get
  status = db->Get(read_options, "key4752", &value);
  if (status.ok()) {
    out << "GET key4752 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13021: put
  db->Put(write_options, "key6925", "value32127");

  // op 13022: delete_range
  status = db->DeleteRange(write_options, "key7213", "key9765");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13023: delete_range
  status = db->DeleteRange(write_options, "key7266", "key10220");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13024: put
  db->Put(write_options, "key9846", "value66000");

  // op 13025: put
  db->Put(write_options, "key1388", "value65370");

  // op 13026: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 13027: put
  db->Put(write_options, "key3554", "value80514");

  // op 13028: put
  db->Put(write_options, "key2496", "value96322");

  // op 13029: delete_range
  status = db->DeleteRange(write_options, "key6290", "key9784");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13030: delete
  db->Delete(write_options, "key0859");

  // op 13031: delete_range
  status = db->DeleteRange(write_options, "key0982", "key3531");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13032: get
  status = db->Get(read_options, "key6908", &value);
  if (status.ok()) {
    out << "GET key6908 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13033: get
  status = db->Get(read_options, "key10304", &value);
  if (status.ok()) {
    out << "GET key10304 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13034: put
  db->Put(write_options, "key3173", "value65648");

  // op 13035: put
  db->Put(write_options, "key11504", "value118279");

  // op 13036: get
  status = db->Get(read_options, "key8090", &value);
  if (status.ok()) {
    out << "GET key8090 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13037: delete_range
  status = db->DeleteRange(write_options, "key1451", "key10826");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13038: get
  status = db->Get(read_options, "key7076", &value);
  if (status.ok()) {
    out << "GET key7076 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13039: delete
  db->Delete(write_options, "key11256");

  // op 13040: put
  db->Put(write_options, "key11667", "value4389");

  // op 13041: delete
  db->Delete(write_options, "key0508");

  // op 13042: delete_range
  status = db->DeleteRange(write_options, "key4112", "key5624");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13043: put
  db->Put(write_options, "key5832", "value38358");

  // op 13044: delete_range
  status = db->DeleteRange(write_options, "key3917", "key4922");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13045: put
  db->Put(write_options, "key10119", "value79074");

  // op 13046: put
  db->Put(write_options, "key0597", "value11531");

  // op 13047: get
  status = db->Get(read_options, "key7304", &value);
  if (status.ok()) {
    out << "GET key7304 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13048: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0786", "key3855", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0786, key3855) size=" << scan_result.size() << std::endl;
  }

  // op 13049: get
  status = db->Get(read_options, "key1912", &value);
  if (status.ok()) {
    out << "GET key1912 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13050: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3899", "key7002", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3899, key7002) size=" << scan_result.size() << std::endl;
  }

  // op 13051: get
  status = db->Get(read_options, "key8046", &value);
  if (status.ok()) {
    out << "GET key8046 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13052: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3693", "key7680", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3693, key7680) size=" << scan_result.size() << std::endl;
  }

  // op 13053: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4778", "key9069", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4778, key9069) size=" << scan_result.size() << std::endl;
  }

  // op 13054: put
  db->Put(write_options, "key9588", "value93519");

  // op 13055: delete
  db->Delete(write_options, "key5874");

  // op 13056: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2558", "key9725", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2558, key9725) size=" << scan_result.size() << std::endl;
  }

  // op 13057: delete_range
  status = db->DeleteRange(write_options, "key6965", "key6996");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13058: put
  db->Put(write_options, "key3958", "value23549");

  // op 13059: put
  db->Put(write_options, "key6831", "value51016");

  // op 13060: get
  status = db->Get(read_options, "key5141", &value);
  if (status.ok()) {
    out << "GET key5141 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13061: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0199", "key10005", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0199, key10005) size=" << scan_result.size() << std::endl;
  }

  // op 13062: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9228", "key10587", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9228, key10587) size=" << scan_result.size() << std::endl;
  }

  // op 13063: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2519", "key5174", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2519, key5174) size=" << scan_result.size() << std::endl;
  }

  // op 13064: put
  db->Put(write_options, "key4397", "value74851");

  // op 13065: delete_range
  status = db->DeleteRange(write_options, "key2844", "key3693");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13066: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 13067: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0937", "key5277", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0937, key5277) size=" << scan_result.size() << std::endl;
  }

  // op 13068: get
  status = db->Get(read_options, "key1807", &value);
  if (status.ok()) {
    out << "GET key1807 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13069: delete_range
  status = db->DeleteRange(write_options, "key4207", "key7249");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13070: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3868", "key9277", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3868, key9277) size=" << scan_result.size() << std::endl;
  }

  // op 13071: put
  db->Put(write_options, "key0669", "value2463");

  // op 13072: put
  db->Put(write_options, "key11493", "value35585");

  // op 13073: delete_range
  status = db->DeleteRange(write_options, "key3502", "key10886");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13074: get
  status = db->Get(read_options, "key0512", &value);
  if (status.ok()) {
    out << "GET key0512 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13075: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 13076: get
  status = db->Get(read_options, "key0232", &value);
  if (status.ok()) {
    out << "GET key0232 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13077: get
  status = db->Get(read_options, "key9722", &value);
  if (status.ok()) {
    out << "GET key9722 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13078: put
  db->Put(write_options, "key1239", "value34363");

  // op 13079: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0375", "key2610", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0375, key2610) size=" << scan_result.size() << std::endl;
  }

  // op 13080: delete_range
  status = db->DeleteRange(write_options, "key3374", "key5455");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13081: put
  db->Put(write_options, "key10888", "value37977");

  // op 13082: get
  status = db->Get(read_options, "key2125", &value);
  if (status.ok()) {
    out << "GET key2125 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13083: get
  status = db->Get(read_options, "key8687", &value);
  if (status.ok()) {
    out << "GET key8687 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13084: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1091", "key8232", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1091, key8232) size=" << scan_result.size() << std::endl;
  }

  // op 13085: put
  db->Put(write_options, "key4509", "value102659");

  // op 13086: get
  status = db->Get(read_options, "key2715", &value);
  if (status.ok()) {
    out << "GET key2715 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13087: put
  db->Put(write_options, "key7570", "value52184");

  // op 13088: delete
  db->Delete(write_options, "key10830");

  // op 13089: get
  status = db->Get(read_options, "key2227", &value);
  if (status.ok()) {
    out << "GET key2227 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13090: put
  db->Put(write_options, "key11134", "value88306");

  // op 13091: delete
  db->Delete(write_options, "key8273");

  // op 13092: put
  db->Put(write_options, "key8761", "value13328");

  // op 13093: put
  db->Put(write_options, "key2277", "value1941");

  // op 13094: put
  db->Put(write_options, "key2832", "value41521");

  // op 13095: put
  db->Put(write_options, "key2328", "value99457");

  // op 13096: delete
  db->Delete(write_options, "key0008");

  // op 13097: delete
  db->Delete(write_options, "key9857");

  // op 13098: delete_range
  status = db->DeleteRange(write_options, "key7370", "key7737");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13099: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2253", "key2781", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2253, key2781) size=" << scan_result.size() << std::endl;
  }

  // op 13100: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2366", "key8388", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2366, key8388) size=" << scan_result.size() << std::endl;
  }

  // op 13101: put
  db->Put(write_options, "key8605", "value95773");

  // op 13102: put
  db->Put(write_options, "key2930", "value24941");

  // op 13103: put
  db->Put(write_options, "key10743", "value104750");

  // op 13104: get
  status = db->Get(read_options, "key3187", &value);
  if (status.ok()) {
    out << "GET key3187 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13105: put
  db->Put(write_options, "key10606", "value44348");

  // op 13106: put
  db->Put(write_options, "key6918", "value59706");

  // op 13107: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2886", "key10281", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2886, key10281) size=" << scan_result.size() << std::endl;
  }

  // op 13108: delete_range
  status = db->DeleteRange(write_options, "key4791", "key6030");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13109: get
  status = db->Get(read_options, "key3334", &value);
  if (status.ok()) {
    out << "GET key3334 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13110: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0341", "key6845", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0341, key6845) size=" << scan_result.size() << std::endl;
  }

  // op 13111: put
  db->Put(write_options, "key1999", "value28891");

  // op 13112: delete_range
  status = db->DeleteRange(write_options, "key4432", "key7527");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13113: delete
  db->Delete(write_options, "key6851");

  // op 13114: put
  db->Put(write_options, "key1894", "value33017");

  // op 13115: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0651", "key8194", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0651, key8194) size=" << scan_result.size() << std::endl;
  }

  // op 13116: put
  db->Put(write_options, "key9614", "value76194");

  // op 13117: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9396", "key10361", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9396, key10361) size=" << scan_result.size() << std::endl;
  }

  // op 13118: get
  status = db->Get(read_options, "key1128", &value);
  if (status.ok()) {
    out << "GET key1128 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13119: delete_range
  status = db->DeleteRange(write_options, "key6630", "key9602");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13120: put
  db->Put(write_options, "key8744", "value95442");

  // op 13121: put
  db->Put(write_options, "key8353", "value22968");

  // op 13122: get
  status = db->Get(read_options, "key10419", &value);
  if (status.ok()) {
    out << "GET key10419 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13123: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 13124: delete
  db->Delete(write_options, "key8563");

  // op 13125: delete_range
  status = db->DeleteRange(write_options, "key4010", "key7663");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13126: put
  db->Put(write_options, "key11149", "value61230");

  // op 13127: put
  db->Put(write_options, "key1070", "value52219");

  // op 13128: put
  db->Put(write_options, "key2710", "value101020");

  // op 13129: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 13130: put
  db->Put(write_options, "key9173", "value62062");

  // op 13131: put
  db->Put(write_options, "key11018", "value85669");

  // op 13132: delete
  db->Delete(write_options, "key7905");

  // op 13133: put
  db->Put(write_options, "key7077", "value5129");

  // op 13134: put
  db->Put(write_options, "key9167", "value28607");

  // op 13135: put
  db->Put(write_options, "key6824", "value27349");

  // op 13136: delete_range
  status = db->DeleteRange(write_options, "key9179", "key9345");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13137: get
  status = db->Get(read_options, "key8284", &value);
  if (status.ok()) {
    out << "GET key8284 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13138: put
  db->Put(write_options, "key8262", "value84888");

  // op 13139: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3852", "key7057", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3852, key7057) size=" << scan_result.size() << std::endl;
  }

  // op 13140: put
  db->Put(write_options, "key2696", "value45598");

  // op 13141: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0984", "key4438", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0984, key4438) size=" << scan_result.size() << std::endl;
  }

  // op 13142: put
  db->Put(write_options, "key6602", "value26533");

  // op 13143: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1113", "key4537", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1113, key4537) size=" << scan_result.size() << std::endl;
  }

  // op 13144: put
  db->Put(write_options, "key2933", "value66863");

  // op 13145: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0056", "key0110", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0056, key0110) size=" << scan_result.size() << std::endl;
  }

  // op 13146: put
  db->Put(write_options, "key6423", "value7086");

  // op 13147: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3451", "key5545", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3451, key5545) size=" << scan_result.size() << std::endl;
  }

  // op 13148: put
  db->Put(write_options, "key5771", "value98838");

  // op 13149: get
  status = db->Get(read_options, "key2899", &value);
  if (status.ok()) {
    out << "GET key2899 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13150: put
  db->Put(write_options, "key10849", "value21089");

  // op 13151: put
  db->Put(write_options, "key10872", "value52016");

  // op 13152: put
  db->Put(write_options, "key5680", "value79489");

  // op 13153: put
  db->Put(write_options, "key10264", "value8742");

  // op 13154: delete_range
  status = db->DeleteRange(write_options, "key0235", "key2367");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13155: put
  db->Put(write_options, "key9229", "value53422");

  // op 13156: put
  db->Put(write_options, "key6200", "value73757");

  // op 13157: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3427", "key7694", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3427, key7694) size=" << scan_result.size() << std::endl;
  }

  // op 13158: put
  db->Put(write_options, "key11523", "value3842");

  // op 13159: delete_range
  status = db->DeleteRange(write_options, "key7300", "key8211");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13160: delete_range
  status = db->DeleteRange(write_options, "key9829", "key9914");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13161: put
  db->Put(write_options, "key6010", "value44934");

  // op 13162: delete_range
  status = db->DeleteRange(write_options, "key7979", "key10596");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13163: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 13164: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0203", "key9003", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0203, key9003) size=" << scan_result.size() << std::endl;
  }

  // op 13165: get
  status = db->Get(read_options, "key2350", &value);
  if (status.ok()) {
    out << "GET key2350 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13166: put
  db->Put(write_options, "key4081", "value42080");

  // op 13167: delete_range
  status = db->DeleteRange(write_options, "key0736", "key3771");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13168: get
  status = db->Get(read_options, "key2664", &value);
  if (status.ok()) {
    out << "GET key2664 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13169: put
  db->Put(write_options, "key9520", "value104195");

  // op 13170: get
  status = db->Get(read_options, "key0206", &value);
  if (status.ok()) {
    out << "GET key0206 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13171: put
  db->Put(write_options, "key8174", "value59467");

  // op 13172: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0805", "key1154", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0805, key1154) size=" << scan_result.size() << std::endl;
  }

  // op 13173: delete_range
  status = db->DeleteRange(write_options, "key0619", "key10082");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13174: get
  status = db->Get(read_options, "key8407", &value);
  if (status.ok()) {
    out << "GET key8407 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13175: get
  status = db->Get(read_options, "key3582", &value);
  if (status.ok()) {
    out << "GET key3582 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13176: get
  status = db->Get(read_options, "key6956", &value);
  if (status.ok()) {
    out << "GET key6956 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13177: put
  db->Put(write_options, "key4681", "value93790");

  // op 13178: put
  db->Put(write_options, "key5941", "value20573");

  // op 13179: get
  status = db->Get(read_options, "key4177", &value);
  if (status.ok()) {
    out << "GET key4177 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13180: put
  db->Put(write_options, "key6619", "value48890");

  // op 13181: get
  status = db->Get(read_options, "key9230", &value);
  if (status.ok()) {
    out << "GET key9230 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13182: put
  db->Put(write_options, "key8455", "value112470");

  // op 13183: put
  db->Put(write_options, "key4381", "value63782");

  // op 13184: put
  db->Put(write_options, "key11177", "value4755");

  // op 13185: get
  status = db->Get(read_options, "key1721", &value);
  if (status.ok()) {
    out << "GET key1721 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13186: get
  status = db->Get(read_options, "key1404", &value);
  if (status.ok()) {
    out << "GET key1404 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13187: put
  db->Put(write_options, "key3316", "value89246");

  // op 13188: put
  db->Put(write_options, "key5830", "value52154");

  // op 13189: get
  status = db->Get(read_options, "key6670", &value);
  if (status.ok()) {
    out << "GET key6670 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13190: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10363", "key11364", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10363, key11364) size=" << scan_result.size() << std::endl;
  }

  // op 13191: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4285", "key6692", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4285, key6692) size=" << scan_result.size() << std::endl;
  }

  // op 13192: put
  db->Put(write_options, "key6432", "value97692");

  // op 13193: get
  status = db->Get(read_options, "key10178", &value);
  if (status.ok()) {
    out << "GET key10178 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13194: delete
  db->Delete(write_options, "key6851");

  // op 13195: delete_range
  status = db->DeleteRange(write_options, "key3995", "key8623");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13196: delete_range
  status = db->DeleteRange(write_options, "key2749", "key9410");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13197: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9740", "key11111", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9740, key11111) size=" << scan_result.size() << std::endl;
  }

  // op 13198: delete
  db->Delete(write_options, "key10584");

  // op 13199: put
  db->Put(write_options, "key3825", "value112446");

  // op 13200: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2924", "key11528", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2924, key11528) size=" << scan_result.size() << std::endl;
  }

  // op 13201: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2918", "key6195", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2918, key6195) size=" << scan_result.size() << std::endl;
  }

  // op 13202: get
  status = db->Get(read_options, "key5607", &value);
  if (status.ok()) {
    out << "GET key5607 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13203: delete
  db->Delete(write_options, "key9662");

  // op 13204: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6826", "key7107", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6826, key7107) size=" << scan_result.size() << std::endl;
  }

  // op 13205: put
  db->Put(write_options, "key7240", "value101888");

  // op 13206: put
  db->Put(write_options, "key8860", "value11767");

  // op 13207: put
  db->Put(write_options, "key9620", "value68246");

  // op 13208: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0853", "key8539", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0853, key8539) size=" << scan_result.size() << std::endl;
  }

  // op 13209: put
  db->Put(write_options, "key11416", "value28137");

  // op 13210: put
  db->Put(write_options, "key6136", "value63053");

  // op 13211: delete_range
  status = db->DeleteRange(write_options, "key0347", "key2940");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13212: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4009", "key10494", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4009, key10494) size=" << scan_result.size() << std::endl;
  }

  // op 13213: put
  db->Put(write_options, "key5356", "value72600");

  // op 13214: get
  status = db->Get(read_options, "key9311", &value);
  if (status.ok()) {
    out << "GET key9311 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13215: put
  db->Put(write_options, "key10885", "value5917");

  // op 13216: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0875", "key11239", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0875, key11239) size=" << scan_result.size() << std::endl;
  }

  // op 13217: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1856", "key5165", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1856, key5165) size=" << scan_result.size() << std::endl;
  }

  // op 13218: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2213", "key7009", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2213, key7009) size=" << scan_result.size() << std::endl;
  }

  // op 13219: put
  db->Put(write_options, "key0649", "value87896");

  // op 13220: put
  db->Put(write_options, "key1198", "value36949");

  // op 13221: put
  db->Put(write_options, "key2346", "value48607");

  // op 13222: get
  status = db->Get(read_options, "key2218", &value);
  if (status.ok()) {
    out << "GET key2218 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13223: put
  db->Put(write_options, "key5823", "value88631");

  // op 13224: delete_range
  status = db->DeleteRange(write_options, "key2411", "key9755");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13225: put
  db->Put(write_options, "key3272", "value66603");

  // op 13226: delete_range
  status = db->DeleteRange(write_options, "key1798", "key5427");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13227: delete
  db->Delete(write_options, "key2601");

  // op 13228: put
  db->Put(write_options, "key3315", "value39646");

  // op 13229: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2767", "key11417", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2767, key11417) size=" << scan_result.size() << std::endl;
  }

  // op 13230: put
  db->Put(write_options, "key0985", "value23204");

  // op 13231: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1497", "key5929", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1497, key5929) size=" << scan_result.size() << std::endl;
  }

  // op 13232: put
  db->Put(write_options, "key2271", "value10076");

  // op 13233: delete
  db->Delete(write_options, "key5311");

  // op 13234: delete
  db->Delete(write_options, "key2937");

  // op 13235: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 13236: put
  db->Put(write_options, "key7979", "value47012");

  // op 13237: put
  db->Put(write_options, "key11009", "value12013");

  // op 13238: get
  status = db->Get(read_options, "key8095", &value);
  if (status.ok()) {
    out << "GET key8095 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13239: put
  db->Put(write_options, "key4542", "value38676");

  // op 13240: put
  db->Put(write_options, "key6457", "value31009");

  // op 13241: put
  db->Put(write_options, "key10712", "value3602");

  // op 13242: put
  db->Put(write_options, "key6364", "value108762");

  // op 13243: put
  db->Put(write_options, "key9176", "value96640");

  // op 13244: put
  db->Put(write_options, "key11480", "value43245");

  // op 13245: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3697", "key4831", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3697, key4831) size=" << scan_result.size() << std::endl;
  }

  // op 13246: put
  db->Put(write_options, "key5942", "value19365");

  // op 13247: put
  db->Put(write_options, "key5754", "value105071");

  // op 13248: get
  status = db->Get(read_options, "key6846", &value);
  if (status.ok()) {
    out << "GET key6846 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13249: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 13250: put
  db->Put(write_options, "key7401", "value74162");

  // op 13251: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1504", "key4255", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1504, key4255) size=" << scan_result.size() << std::endl;
  }

  // op 13252: put
  db->Put(write_options, "key6523", "value22520");

  // op 13253: put
  db->Put(write_options, "key9959", "value83500");

  // op 13254: put
  db->Put(write_options, "key7754", "value78211");

  // op 13255: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7715", "key10829", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7715, key10829) size=" << scan_result.size() << std::endl;
  }

  // op 13256: put
  db->Put(write_options, "key1480", "value109802");

  // op 13257: delete
  db->Delete(write_options, "key4325");

  // op 13258: put
  db->Put(write_options, "key4953", "value102914");

  // op 13259: put
  db->Put(write_options, "key1285", "value70856");

  // op 13260: put
  db->Put(write_options, "key3015", "value34966");

  // op 13261: put
  db->Put(write_options, "key6378", "value3743");

  // op 13262: put
  db->Put(write_options, "key0798", "value96441");

  // op 13263: put
  db->Put(write_options, "key10320", "value51233");

  // op 13264: delete
  db->Delete(write_options, "key9738");

  // op 13265: put
  db->Put(write_options, "key7375", "value82208");

  // op 13266: put
  db->Put(write_options, "key1715", "value57713");

  // op 13267: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1834", "key3836", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1834, key3836) size=" << scan_result.size() << std::endl;
  }

  // op 13268: put
  db->Put(write_options, "key4682", "value43097");

  // op 13269: delete_range
  status = db->DeleteRange(write_options, "key6600", "key10355");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13270: get
  status = db->Get(read_options, "key1976", &value);
  if (status.ok()) {
    out << "GET key1976 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13271: get
  status = db->Get(read_options, "key11025", &value);
  if (status.ok()) {
    out << "GET key11025 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13272: put
  db->Put(write_options, "key8291", "value98969");

  // op 13273: get
  status = db->Get(read_options, "key11051", &value);
  if (status.ok()) {
    out << "GET key11051 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13274: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1965", "key10724", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1965, key10724) size=" << scan_result.size() << std::endl;
  }

  // op 13275: put
  db->Put(write_options, "key9442", "value21651");

  // op 13276: delete
  db->Delete(write_options, "key3139");

  // op 13277: delete_range
  status = db->DeleteRange(write_options, "key1721", "key5734");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13278: delete
  db->Delete(write_options, "key1387");

  // op 13279: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 13280: delete_range
  status = db->DeleteRange(write_options, "key1455", "key11006");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13281: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5525", "key7639", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5525, key7639) size=" << scan_result.size() << std::endl;
  }

  // op 13282: get
  status = db->Get(read_options, "key8710", &value);
  if (status.ok()) {
    out << "GET key8710 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13283: get
  status = db->Get(read_options, "key7133", &value);
  if (status.ok()) {
    out << "GET key7133 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13284: put
  db->Put(write_options, "key6098", "value115854");

  // op 13285: get
  status = db->Get(read_options, "key2479", &value);
  if (status.ok()) {
    out << "GET key2479 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13286: put
  db->Put(write_options, "key1691", "value55733");

  // op 13287: get
  status = db->Get(read_options, "key11505", &value);
  if (status.ok()) {
    out << "GET key11505 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13288: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 13289: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7797", "key9115", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7797, key9115) size=" << scan_result.size() << std::endl;
  }

  // op 13290: put
  db->Put(write_options, "key2946", "value116406");

  // op 13291: put
  db->Put(write_options, "key1227", "value34483");

  // op 13292: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 13293: delete_range
  status = db->DeleteRange(write_options, "key2944", "key4459");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13294: delete
  db->Delete(write_options, "key1168");

  // op 13295: put
  db->Put(write_options, "key4636", "value45246");

  // op 13296: put
  db->Put(write_options, "key8340", "value57562");

  // op 13297: put
  db->Put(write_options, "key3531", "value19507");

  // op 13298: put
  db->Put(write_options, "key0876", "value87852");

  // op 13299: put
  db->Put(write_options, "key6857", "value94311");

  // op 13300: delete_range
  status = db->DeleteRange(write_options, "key7809", "key10090");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13301: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6261", "key7137", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6261, key7137) size=" << scan_result.size() << std::endl;
  }

  // op 13302: put
  db->Put(write_options, "key7857", "value48811");

  // op 13303: put
  db->Put(write_options, "key2525", "value38379");

  // op 13304: get
  status = db->Get(read_options, "key6195", &value);
  if (status.ok()) {
    out << "GET key6195 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13305: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4105", "key8551", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4105, key8551) size=" << scan_result.size() << std::endl;
  }

  // op 13306: put
  db->Put(write_options, "key6281", "value29147");

  // op 13307: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 13308: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4504", "key5085", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4504, key5085) size=" << scan_result.size() << std::endl;
  }

  // op 13309: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8613", "key9267", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8613, key9267) size=" << scan_result.size() << std::endl;
  }

  // op 13310: put
  db->Put(write_options, "key2170", "value83147");

  // op 13311: put
  db->Put(write_options, "key8185", "value35545");

  // op 13312: put
  db->Put(write_options, "key11226", "value49855");

  // op 13313: put
  db->Put(write_options, "key4235", "value100759");

  // op 13314: delete
  db->Delete(write_options, "key1283");

  // op 13315: put
  db->Put(write_options, "key2888", "value50722");

  // op 13316: put
  db->Put(write_options, "key2743", "value77739");

  // op 13317: get
  status = db->Get(read_options, "key11720", &value);
  if (status.ok()) {
    out << "GET key11720 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13318: put
  db->Put(write_options, "key0904", "value105573");

  // op 13319: put
  db->Put(write_options, "key0777", "value9720");

  // op 13320: delete_range
  status = db->DeleteRange(write_options, "key4488", "key8161");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13321: delete_range
  status = db->DeleteRange(write_options, "key2559", "key8974");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13322: get
  status = db->Get(read_options, "key1810", &value);
  if (status.ok()) {
    out << "GET key1810 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13323: get
  status = db->Get(read_options, "key3500", &value);
  if (status.ok()) {
    out << "GET key3500 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13324: put
  db->Put(write_options, "key0997", "value53923");

  // op 13325: get
  status = db->Get(read_options, "key4331", &value);
  if (status.ok()) {
    out << "GET key4331 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13326: put
  db->Put(write_options, "key3271", "value46073");

  // op 13327: delete_range
  status = db->DeleteRange(write_options, "key2936", "key11285");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13328: delete
  db->Delete(write_options, "key3909");

  // op 13329: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8189", "key11669", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8189, key11669) size=" << scan_result.size() << std::endl;
  }

  // op 13330: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1574", "key3634", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1574, key3634) size=" << scan_result.size() << std::endl;
  }

  // op 13331: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5100", "key7840", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5100, key7840) size=" << scan_result.size() << std::endl;
  }

  // op 13332: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2604", "key9423", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2604, key9423) size=" << scan_result.size() << std::endl;
  }

  // op 13333: delete
  db->Delete(write_options, "key8491");

  // op 13334: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10875", "key11603", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10875, key11603) size=" << scan_result.size() << std::endl;
  }

  // op 13335: put
  db->Put(write_options, "key9306", "value56936");

  // op 13336: put
  db->Put(write_options, "key10717", "value114555");

  // op 13337: put
  db->Put(write_options, "key10254", "value60231");

  // op 13338: get
  status = db->Get(read_options, "key6705", &value);
  if (status.ok()) {
    out << "GET key6705 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13339: put
  db->Put(write_options, "key0080", "value75026");

  // op 13340: put
  db->Put(write_options, "key11408", "value90914");

  // op 13341: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9203", "key11385", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9203, key11385) size=" << scan_result.size() << std::endl;
  }

  // op 13342: put
  db->Put(write_options, "key2921", "value49672");

  // op 13343: put
  db->Put(write_options, "key0866", "value76661");

  // op 13344: put
  db->Put(write_options, "key11303", "value39069");

  // op 13345: delete
  db->Delete(write_options, "key2097");

  // op 13346: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5050", "key8048", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5050, key8048) size=" << scan_result.size() << std::endl;
  }

  // op 13347: put
  db->Put(write_options, "key7749", "value11636");

  // op 13348: put
  db->Put(write_options, "key4058", "value74589");

  // op 13349: get
  status = db->Get(read_options, "key1952", &value);
  if (status.ok()) {
    out << "GET key1952 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13350: put
  db->Put(write_options, "key8035", "value64787");

  // op 13351: get
  status = db->Get(read_options, "key6936", &value);
  if (status.ok()) {
    out << "GET key6936 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13352: get
  status = db->Get(read_options, "key4511", &value);
  if (status.ok()) {
    out << "GET key4511 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13353: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0433", "key6970", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0433, key6970) size=" << scan_result.size() << std::endl;
  }

  // op 13354: put
  db->Put(write_options, "key5646", "value8663");

  // op 13355: put
  db->Put(write_options, "key9896", "value19462");

  // op 13356: put
  db->Put(write_options, "key3118", "value83123");

  // op 13357: delete
  db->Delete(write_options, "key8807");

  // op 13358: put
  db->Put(write_options, "key5047", "value105582");

  // op 13359: put
  db->Put(write_options, "key7966", "value34720");

  // op 13360: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0285", "key4457", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0285, key4457) size=" << scan_result.size() << std::endl;
  }

  // op 13361: put
  db->Put(write_options, "key4191", "value117930");

  // op 13362: put
  db->Put(write_options, "key9555", "value66099");

  // op 13363: delete_range
  status = db->DeleteRange(write_options, "key1306", "key2105");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13364: put
  db->Put(write_options, "key5551", "value96705");

  // op 13365: put
  db->Put(write_options, "key6681", "value64046");

  // op 13366: put
  db->Put(write_options, "key6522", "value59928");

  // op 13367: get
  status = db->Get(read_options, "key11060", &value);
  if (status.ok()) {
    out << "GET key11060 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13368: put
  db->Put(write_options, "key5413", "value51582");

  // op 13369: put
  db->Put(write_options, "key6399", "value117192");

  // op 13370: put
  db->Put(write_options, "key1239", "value112830");

  // op 13371: put
  db->Put(write_options, "key3243", "value14421");

  // op 13372: put
  db->Put(write_options, "key4658", "value109700");

  // op 13373: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 13374: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1106", "key8290", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1106, key8290) size=" << scan_result.size() << std::endl;
  }

  // op 13375: get
  status = db->Get(read_options, "key9271", &value);
  if (status.ok()) {
    out << "GET key9271 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13376: put
  db->Put(write_options, "key5583", "value83948");

  // op 13377: put
  db->Put(write_options, "key5522", "value52394");

  // op 13378: delete
  db->Delete(write_options, "key5230");

  // op 13379: delete
  db->Delete(write_options, "key7151");

  // op 13380: get
  status = db->Get(read_options, "key3629", &value);
  if (status.ok()) {
    out << "GET key3629 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13381: put
  db->Put(write_options, "key9006", "value83467");

  // op 13382: put
  db->Put(write_options, "key8664", "value27523");

  // op 13383: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4525", "key9872", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4525, key9872) size=" << scan_result.size() << std::endl;
  }

  // op 13384: get
  status = db->Get(read_options, "key1164", &value);
  if (status.ok()) {
    out << "GET key1164 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13385: delete_range
  status = db->DeleteRange(write_options, "key1811", "key10565");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13386: delete_range
  status = db->DeleteRange(write_options, "key3407", "key3606");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13387: get
  status = db->Get(read_options, "key0119", &value);
  if (status.ok()) {
    out << "GET key0119 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13388: delete
  db->Delete(write_options, "key7010");

  // op 13389: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6706", "key8947", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6706, key8947) size=" << scan_result.size() << std::endl;
  }

  // op 13390: delete_range
  status = db->DeleteRange(write_options, "key1805", "key10753");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13391: delete
  db->Delete(write_options, "key5914");

  // op 13392: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 13393: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10277", "key10816", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10277, key10816) size=" << scan_result.size() << std::endl;
  }

  // op 13394: put
  db->Put(write_options, "key4964", "value55910");

  // op 13395: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4785", "key9891", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4785, key9891) size=" << scan_result.size() << std::endl;
  }

  // op 13396: get
  status = db->Get(read_options, "key0692", &value);
  if (status.ok()) {
    out << "GET key0692 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13397: get
  status = db->Get(read_options, "key8312", &value);
  if (status.ok()) {
    out << "GET key8312 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13398: put
  db->Put(write_options, "key8199", "value35088");

  // op 13399: put
  db->Put(write_options, "key7707", "value69591");

  // op 13400: delete_range
  status = db->DeleteRange(write_options, "key5366", "key11143");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13401: get
  status = db->Get(read_options, "key9079", &value);
  if (status.ok()) {
    out << "GET key9079 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13402: delete_range
  status = db->DeleteRange(write_options, "key0999", "key11556");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13403: delete
  db->Delete(write_options, "key7728");

  // op 13404: put
  db->Put(write_options, "key5040", "value73281");

  // op 13405: delete_range
  status = db->DeleteRange(write_options, "key3105", "key11353");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13406: get
  status = db->Get(read_options, "key7885", &value);
  if (status.ok()) {
    out << "GET key7885 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13407: put
  db->Put(write_options, "key6860", "value45790");

  // op 13408: put
  db->Put(write_options, "key4166", "value46879");

  // op 13409: get
  status = db->Get(read_options, "key10124", &value);
  if (status.ok()) {
    out << "GET key10124 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13410: delete
  db->Delete(write_options, "key8463");

  // op 13411: delete
  db->Delete(write_options, "key5762");

  // op 13412: delete_range
  status = db->DeleteRange(write_options, "key7769", "key9799");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13413: put
  db->Put(write_options, "key5738", "value66335");

  // op 13414: delete_range
  status = db->DeleteRange(write_options, "key2889", "key5714");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13415: put
  db->Put(write_options, "key9176", "value59957");

  // op 13416: delete
  db->Delete(write_options, "key0205");

  // op 13417: put
  db->Put(write_options, "key8020", "value89406");

  // op 13418: get
  status = db->Get(read_options, "key8465", &value);
  if (status.ok()) {
    out << "GET key8465 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13419: get
  status = db->Get(read_options, "key9144", &value);
  if (status.ok()) {
    out << "GET key9144 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13420: delete_range
  status = db->DeleteRange(write_options, "key2810", "key4606");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13421: delete_range
  status = db->DeleteRange(write_options, "key6551", "key7077");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13422: delete_range
  status = db->DeleteRange(write_options, "key0435", "key2803");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13423: put
  db->Put(write_options, "key6000", "value97813");

  // op 13424: put
  db->Put(write_options, "key3261", "value41349");

  // op 13425: get
  status = db->Get(read_options, "key10793", &value);
  if (status.ok()) {
    out << "GET key10793 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13426: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3896", "key4541", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3896, key4541) size=" << scan_result.size() << std::endl;
  }

  // op 13427: delete_range
  status = db->DeleteRange(write_options, "key7085", "key8286");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13428: get
  status = db->Get(read_options, "key0050", &value);
  if (status.ok()) {
    out << "GET key0050 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13429: put
  db->Put(write_options, "key1211", "value113066");

  // op 13430: put
  db->Put(write_options, "key11305", "value6875");

  // op 13431: put
  db->Put(write_options, "key1671", "value97612");

  // op 13432: put
  db->Put(write_options, "key0997", "value31747");

  // op 13433: delete
  db->Delete(write_options, "key9595");

  // op 13434: put
  db->Put(write_options, "key1576", "value47095");

  // op 13435: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3160", "key11133", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3160, key11133) size=" << scan_result.size() << std::endl;
  }

  // op 13436: put
  db->Put(write_options, "key5412", "value13301");

  // op 13437: put
  db->Put(write_options, "key5566", "value111918");

  // op 13438: put
  db->Put(write_options, "key5668", "value107379");

  // op 13439: get
  status = db->Get(read_options, "key2289", &value);
  if (status.ok()) {
    out << "GET key2289 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13440: put
  db->Put(write_options, "key1934", "value63634");

  // op 13441: delete_range
  status = db->DeleteRange(write_options, "key2033", "key9837");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13442: delete_range
  status = db->DeleteRange(write_options, "key2852", "key4105");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13443: put
  db->Put(write_options, "key0705", "value37807");

  // op 13444: get
  status = db->Get(read_options, "key2945", &value);
  if (status.ok()) {
    out << "GET key2945 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13445: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6882", "key7322", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6882, key7322) size=" << scan_result.size() << std::endl;
  }

  // op 13446: put
  db->Put(write_options, "key1224", "value103643");

  // op 13447: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1387", "key7549", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1387, key7549) size=" << scan_result.size() << std::endl;
  }

  // op 13448: delete
  db->Delete(write_options, "key11441");

  // op 13449: put
  db->Put(write_options, "key9066", "value98691");

  // op 13450: delete_range
  status = db->DeleteRange(write_options, "key2425", "key9426");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13451: get
  status = db->Get(read_options, "key2949", &value);
  if (status.ok()) {
    out << "GET key2949 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13452: put
  db->Put(write_options, "key0971", "value84198");

  // op 13453: put
  db->Put(write_options, "key11316", "value108129");

  // op 13454: delete
  db->Delete(write_options, "key9891");

  // op 13455: put
  db->Put(write_options, "key8606", "value6930");

  // op 13456: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6843", "key10908", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6843, key10908) size=" << scan_result.size() << std::endl;
  }

  // op 13457: delete
  db->Delete(write_options, "key11348");

  // op 13458: put
  db->Put(write_options, "key1585", "value40159");

  // op 13459: put
  db->Put(write_options, "key2789", "value107663");

  // op 13460: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 13461: get
  status = db->Get(read_options, "key6514", &value);
  if (status.ok()) {
    out << "GET key6514 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13462: put
  db->Put(write_options, "key4450", "value97811");

  // op 13463: delete
  db->Delete(write_options, "key1239");

  // op 13464: put
  db->Put(write_options, "key8978", "value21155");

  // op 13465: put
  db->Put(write_options, "key6335", "value22494");

  // op 13466: get
  status = db->Get(read_options, "key0350", &value);
  if (status.ok()) {
    out << "GET key0350 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13467: get
  status = db->Get(read_options, "key2915", &value);
  if (status.ok()) {
    out << "GET key2915 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13468: put
  db->Put(write_options, "key4055", "value86893");

  // op 13469: put
  db->Put(write_options, "key7222", "value6204");

  // op 13470: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1007", "key8481", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1007, key8481) size=" << scan_result.size() << std::endl;
  }

  // op 13471: get
  status = db->Get(read_options, "key0606", &value);
  if (status.ok()) {
    out << "GET key0606 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13472: delete_range
  status = db->DeleteRange(write_options, "key7827", "key11112");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13473: delete
  db->Delete(write_options, "key6521");

  // op 13474: get
  status = db->Get(read_options, "key2588", &value);
  if (status.ok()) {
    out << "GET key2588 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13475: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7793", "key10863", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7793, key10863) size=" << scan_result.size() << std::endl;
  }

  // op 13476: get
  status = db->Get(read_options, "key11721", &value);
  if (status.ok()) {
    out << "GET key11721 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13477: delete_range
  status = db->DeleteRange(write_options, "key2664", "key4503");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13478: get
  status = db->Get(read_options, "key1857", &value);
  if (status.ok()) {
    out << "GET key1857 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13479: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8474", "key10789", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8474, key10789) size=" << scan_result.size() << std::endl;
  }

  // op 13480: get
  status = db->Get(read_options, "key9337", &value);
  if (status.ok()) {
    out << "GET key9337 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13481: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 13482: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10575", "key11165", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10575, key11165) size=" << scan_result.size() << std::endl;
  }

  // op 13483: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 13484: get
  status = db->Get(read_options, "key10854", &value);
  if (status.ok()) {
    out << "GET key10854 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13485: put
  db->Put(write_options, "key8547", "value42313");

  // op 13486: put
  db->Put(write_options, "key1468", "value107450");

  // op 13487: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 13488: put
  db->Put(write_options, "key0246", "value4403");

  // op 13489: put
  db->Put(write_options, "key0416", "value96601");

  // op 13490: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1243", "key6003", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1243, key6003) size=" << scan_result.size() << std::endl;
  }

  // op 13491: put
  db->Put(write_options, "key10535", "value41036");

  // op 13492: delete
  db->Delete(write_options, "key11668");

  // op 13493: put
  db->Put(write_options, "key1339", "value83281");

  // op 13494: put
  db->Put(write_options, "key8304", "value7680");

  // op 13495: delete_range
  status = db->DeleteRange(write_options, "key1994", "key11665");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13496: put
  db->Put(write_options, "key11481", "value105095");

  // op 13497: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2825", "key10796", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2825, key10796) size=" << scan_result.size() << std::endl;
  }

  // op 13498: put
  db->Put(write_options, "key1241", "value60302");

  // op 13499: delete
  db->Delete(write_options, "key4274");

  // op 13500: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0812", "key5893", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0812, key5893) size=" << scan_result.size() << std::endl;
  }

  // op 13501: put
  db->Put(write_options, "key6456", "value94651");

  // op 13502: delete_range
  status = db->DeleteRange(write_options, "key9792", "key11602");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13503: get
  status = db->Get(read_options, "key10625", &value);
  if (status.ok()) {
    out << "GET key10625 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13504: put
  db->Put(write_options, "key6075", "value91245");

  // op 13505: get
  status = db->Get(read_options, "key5738", &value);
  if (status.ok()) {
    out << "GET key5738 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13506: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2194", "key8297", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2194, key8297) size=" << scan_result.size() << std::endl;
  }

  // op 13507: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 13508: put
  db->Put(write_options, "key6322", "value81034");

  // op 13509: delete_range
  status = db->DeleteRange(write_options, "key4519", "key7266");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13510: put
  db->Put(write_options, "key0217", "value40222");

  // op 13511: put
  db->Put(write_options, "key9889", "value40173");

  // op 13512: get
  status = db->Get(read_options, "key5722", &value);
  if (status.ok()) {
    out << "GET key5722 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13513: put
  db->Put(write_options, "key7471", "value52284");

  // op 13514: delete
  db->Delete(write_options, "key9731");

  // op 13515: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4620", "key11153", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4620, key11153) size=" << scan_result.size() << std::endl;
  }

  // op 13516: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4689", "key9771", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4689, key9771) size=" << scan_result.size() << std::endl;
  }

  // op 13517: get
  status = db->Get(read_options, "key11189", &value);
  if (status.ok()) {
    out << "GET key11189 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13518: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2186", "key5516", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2186, key5516) size=" << scan_result.size() << std::endl;
  }

  // op 13519: put
  db->Put(write_options, "key0241", "value88156");

  // op 13520: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 13521: get
  status = db->Get(read_options, "key2346", &value);
  if (status.ok()) {
    out << "GET key2346 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13522: get
  status = db->Get(read_options, "key8528", &value);
  if (status.ok()) {
    out << "GET key8528 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13523: put
  db->Put(write_options, "key1462", "value102156");

  // op 13524: put
  db->Put(write_options, "key6459", "value78066");

  // op 13525: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0055", "key9610", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0055, key9610) size=" << scan_result.size() << std::endl;
  }

  // op 13526: put
  db->Put(write_options, "key9823", "value28693");

  // op 13527: put
  db->Put(write_options, "key11203", "value46943");

  // op 13528: get
  status = db->Get(read_options, "key2184", &value);
  if (status.ok()) {
    out << "GET key2184 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13529: delete
  db->Delete(write_options, "key5260");

  // op 13530: delete_range
  status = db->DeleteRange(write_options, "key0014", "key4515");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13531: delete_range
  status = db->DeleteRange(write_options, "key6579", "key7479");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13532: put
  db->Put(write_options, "key5655", "value85073");

  // op 13533: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 13534: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5932", "key8051", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5932, key8051) size=" << scan_result.size() << std::endl;
  }

  // op 13535: delete
  db->Delete(write_options, "key0373");

  // op 13536: put
  db->Put(write_options, "key6911", "value113445");

  // op 13537: get
  status = db->Get(read_options, "key11701", &value);
  if (status.ok()) {
    out << "GET key11701 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13538: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8417", "key8534", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8417, key8534) size=" << scan_result.size() << std::endl;
  }

  // op 13539: get
  status = db->Get(read_options, "key1541", &value);
  if (status.ok()) {
    out << "GET key1541 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13540: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2884", "key7512", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2884, key7512) size=" << scan_result.size() << std::endl;
  }

  // op 13541: get
  status = db->Get(read_options, "key2672", &value);
  if (status.ok()) {
    out << "GET key2672 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13542: put
  db->Put(write_options, "key9008", "value99879");

  // op 13543: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3329", "key8212", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3329, key8212) size=" << scan_result.size() << std::endl;
  }

  // op 13544: put
  db->Put(write_options, "key8951", "value45658");

  // op 13545: put
  db->Put(write_options, "key6320", "value17876");

  // op 13546: delete
  db->Delete(write_options, "key11569");

  // op 13547: put
  db->Put(write_options, "key9493", "value94236");

  // op 13548: delete
  db->Delete(write_options, "key1598");

  // op 13549: delete
  db->Delete(write_options, "key3311");

  // op 13550: put
  db->Put(write_options, "key3177", "value53569");

  // op 13551: put
  db->Put(write_options, "key0065", "value22674");

  // op 13552: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2272", "key10772", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2272, key10772) size=" << scan_result.size() << std::endl;
  }

  // op 13553: put
  db->Put(write_options, "key11480", "value55737");

  // op 13554: delete
  db->Delete(write_options, "key3823");

  // op 13555: put
  db->Put(write_options, "key6381", "value104614");

  // op 13556: put
  db->Put(write_options, "key9136", "value25128");

  // op 13557: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 13558: put
  db->Put(write_options, "key7585", "value97938");

  // op 13559: get
  status = db->Get(read_options, "key5182", &value);
  if (status.ok()) {
    out << "GET key5182 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13560: put
  db->Put(write_options, "key10278", "value6404");

  // op 13561: get
  status = db->Get(read_options, "key2391", &value);
  if (status.ok()) {
    out << "GET key2391 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13562: delete
  db->Delete(write_options, "key8883");

  // op 13563: delete
  db->Delete(write_options, "key9673");

  // op 13564: put
  db->Put(write_options, "key4038", "value11031");

  // op 13565: put
  db->Put(write_options, "key8578", "value77823");

  // op 13566: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2588", "key4090", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2588, key4090) size=" << scan_result.size() << std::endl;
  }

  // op 13567: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1370", "key6051", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1370, key6051) size=" << scan_result.size() << std::endl;
  }

  // op 13568: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6554", "key8733", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6554, key8733) size=" << scan_result.size() << std::endl;
  }

  // op 13569: put
  db->Put(write_options, "key2050", "value71271");

  // op 13570: delete_range
  status = db->DeleteRange(write_options, "key2028", "key3801");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13571: delete_range
  status = db->DeleteRange(write_options, "key6858", "key9512");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13572: delete_range
  status = db->DeleteRange(write_options, "key5219", "key10966");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13573: put
  db->Put(write_options, "key1779", "value66450");

  // op 13574: delete_range
  status = db->DeleteRange(write_options, "key2771", "key7610");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13575: put
  db->Put(write_options, "key9269", "value46890");

  // op 13576: get
  status = db->Get(read_options, "key4941", &value);
  if (status.ok()) {
    out << "GET key4941 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13577: get
  status = db->Get(read_options, "key7078", &value);
  if (status.ok()) {
    out << "GET key7078 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13578: delete_range
  status = db->DeleteRange(write_options, "key1451", "key5955");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13579: put
  db->Put(write_options, "key2061", "value106757");

  // op 13580: put
  db->Put(write_options, "key2773", "value44475");

  // op 13581: put
  db->Put(write_options, "key7187", "value73169");

  // op 13582: put
  db->Put(write_options, "key3035", "value110590");

  // op 13583: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7517", "key9094", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7517, key9094) size=" << scan_result.size() << std::endl;
  }

  // op 13584: put
  db->Put(write_options, "key8033", "value48546");

  // op 13585: get
  status = db->Get(read_options, "key6266", &value);
  if (status.ok()) {
    out << "GET key6266 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13586: get
  status = db->Get(read_options, "key10480", &value);
  if (status.ok()) {
    out << "GET key10480 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13587: get
  status = db->Get(read_options, "key9778", &value);
  if (status.ok()) {
    out << "GET key9778 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13588: put
  db->Put(write_options, "key10975", "value110169");

  // op 13589: delete
  db->Delete(write_options, "key7985");

  // op 13590: put
  db->Put(write_options, "key3885", "value52612");

  // op 13591: put
  db->Put(write_options, "key4752", "value66429");

  // op 13592: put
  db->Put(write_options, "key8094", "value116622");

  // op 13593: put
  db->Put(write_options, "key9459", "value108729");

  // op 13594: delete
  db->Delete(write_options, "key11183");

  // op 13595: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4496", "key11328", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4496, key11328) size=" << scan_result.size() << std::endl;
  }

  // op 13596: get
  status = db->Get(read_options, "key4671", &value);
  if (status.ok()) {
    out << "GET key4671 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13597: get
  status = db->Get(read_options, "key9347", &value);
  if (status.ok()) {
    out << "GET key9347 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13598: get
  status = db->Get(read_options, "key8840", &value);
  if (status.ok()) {
    out << "GET key8840 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13599: put
  db->Put(write_options, "key7226", "value113589");

  // op 13600: put
  db->Put(write_options, "key4548", "value54860");

  // op 13601: put
  db->Put(write_options, "key8142", "value30663");

  // op 13602: get
  status = db->Get(read_options, "key5375", &value);
  if (status.ok()) {
    out << "GET key5375 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13603: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5546", "key6336", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5546, key6336) size=" << scan_result.size() << std::endl;
  }

  // op 13604: put
  db->Put(write_options, "key8921", "value62830");

  // op 13605: delete_range
  status = db->DeleteRange(write_options, "key1947", "key7493");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13606: get
  status = db->Get(read_options, "key5166", &value);
  if (status.ok()) {
    out << "GET key5166 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13607: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4807", "key10301", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4807, key10301) size=" << scan_result.size() << std::endl;
  }

  // op 13608: get
  status = db->Get(read_options, "key1672", &value);
  if (status.ok()) {
    out << "GET key1672 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13609: delete_range
  status = db->DeleteRange(write_options, "key10590", "key11611");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13610: delete_range
  status = db->DeleteRange(write_options, "key4674", "key8801");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13611: delete_range
  status = db->DeleteRange(write_options, "key5489", "key11718");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13612: get
  status = db->Get(read_options, "key5028", &value);
  if (status.ok()) {
    out << "GET key5028 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13613: delete_range
  status = db->DeleteRange(write_options, "key9085", "key10701");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13614: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0263", "key10985", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0263, key10985) size=" << scan_result.size() << std::endl;
  }

  // op 13615: delete_range
  status = db->DeleteRange(write_options, "key0407", "key6410");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13616: put
  db->Put(write_options, "key3524", "value112700");

  // op 13617: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3123", "key7568", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3123, key7568) size=" << scan_result.size() << std::endl;
  }

  // op 13618: put
  db->Put(write_options, "key8739", "value71836");

  // op 13619: get
  status = db->Get(read_options, "key4764", &value);
  if (status.ok()) {
    out << "GET key4764 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13620: delete_range
  status = db->DeleteRange(write_options, "key6480", "key9775");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13621: get
  status = db->Get(read_options, "key9725", &value);
  if (status.ok()) {
    out << "GET key9725 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13622: delete
  db->Delete(write_options, "key9989");

  // op 13623: delete
  db->Delete(write_options, "key5454");

  // op 13624: delete_range
  status = db->DeleteRange(write_options, "key7041", "key9512");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13625: put
  db->Put(write_options, "key10151", "value79893");

  // op 13626: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2429", "key5878", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2429, key5878) size=" << scan_result.size() << std::endl;
  }

  // op 13627: put
  db->Put(write_options, "key4273", "value75528");

  // op 13628: delete_range
  status = db->DeleteRange(write_options, "key4924", "key10062");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13629: get
  status = db->Get(read_options, "key4040", &value);
  if (status.ok()) {
    out << "GET key4040 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13630: put
  db->Put(write_options, "key6465", "value108888");

  // op 13631: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8059", "key11213", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8059, key11213) size=" << scan_result.size() << std::endl;
  }

  // op 13632: delete
  db->Delete(write_options, "key2543");

  // op 13633: put
  db->Put(write_options, "key7951", "value113846");

  // op 13634: get
  status = db->Get(read_options, "key11726", &value);
  if (status.ok()) {
    out << "GET key11726 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13635: get
  status = db->Get(read_options, "key1920", &value);
  if (status.ok()) {
    out << "GET key1920 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13636: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1237", "key1703", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1237, key1703) size=" << scan_result.size() << std::endl;
  }

  // op 13637: get
  status = db->Get(read_options, "key7740", &value);
  if (status.ok()) {
    out << "GET key7740 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13638: delete_range
  status = db->DeleteRange(write_options, "key0437", "key5096");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13639: get
  status = db->Get(read_options, "key2974", &value);
  if (status.ok()) {
    out << "GET key2974 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13640: put
  db->Put(write_options, "key5944", "value56804");

  // op 13641: get
  status = db->Get(read_options, "key5716", &value);
  if (status.ok()) {
    out << "GET key5716 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13642: delete_range
  status = db->DeleteRange(write_options, "key4709", "key5659");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13643: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6442", "key9617", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6442, key9617) size=" << scan_result.size() << std::endl;
  }

  // op 13644: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4739", "key5492", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4739, key5492) size=" << scan_result.size() << std::endl;
  }

  // op 13645: put
  db->Put(write_options, "key3393", "value77206");

  // op 13646: delete
  db->Delete(write_options, "key11663");

  // op 13647: put
  db->Put(write_options, "key10501", "value45549");

  // op 13648: put
  db->Put(write_options, "key1959", "value104508");

  // op 13649: get
  status = db->Get(read_options, "key7509", &value);
  if (status.ok()) {
    out << "GET key7509 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13650: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2011", "key10673", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2011, key10673) size=" << scan_result.size() << std::endl;
  }

  // op 13651: delete
  db->Delete(write_options, "key2801");

  // op 13652: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 13653: put
  db->Put(write_options, "key1203", "value37556");

  // op 13654: delete_range
  status = db->DeleteRange(write_options, "key3510", "key7232");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13655: get
  status = db->Get(read_options, "key5574", &value);
  if (status.ok()) {
    out << "GET key5574 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13656: delete
  db->Delete(write_options, "key8584");

  // op 13657: delete_range
  status = db->DeleteRange(write_options, "key4904", "key8589");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13658: put
  db->Put(write_options, "key3086", "value42456");

  // op 13659: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1439", "key5785", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1439, key5785) size=" << scan_result.size() << std::endl;
  }

  // op 13660: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5602", "key9104", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5602, key9104) size=" << scan_result.size() << std::endl;
  }

  // op 13661: delete_range
  status = db->DeleteRange(write_options, "key8441", "key11310");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13662: put
  db->Put(write_options, "key9534", "value105263");

  // op 13663: delete_range
  status = db->DeleteRange(write_options, "key4075", "key10449");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13664: put
  db->Put(write_options, "key8056", "value53504");

  // op 13665: put
  db->Put(write_options, "key0100", "value117043");

  // op 13666: put
  db->Put(write_options, "key9182", "value32962");

  // op 13667: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 13668: put
  db->Put(write_options, "key5701", "value33193");

  // op 13669: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0645", "key4071", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0645, key4071) size=" << scan_result.size() << std::endl;
  }

  // op 13670: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2216", "key2413", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2216, key2413) size=" << scan_result.size() << std::endl;
  }

  // op 13671: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7676", "key10441", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7676, key10441) size=" << scan_result.size() << std::endl;
  }

  // op 13672: put
  db->Put(write_options, "key8410", "value111147");

  // op 13673: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8241", "key8774", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8241, key8774) size=" << scan_result.size() << std::endl;
  }

  // op 13674: get
  status = db->Get(read_options, "key11468", &value);
  if (status.ok()) {
    out << "GET key11468 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13675: put
  db->Put(write_options, "key9830", "value91411");

  // op 13676: put
  db->Put(write_options, "key5482", "value20156");

  // op 13677: put
  db->Put(write_options, "key8401", "value111777");

  // op 13678: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4300", "key7512", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4300, key7512) size=" << scan_result.size() << std::endl;
  }

  // op 13679: put
  db->Put(write_options, "key9657", "value31711");

  // op 13680: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5421", "key9753", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5421, key9753) size=" << scan_result.size() << std::endl;
  }

  // op 13681: put
  db->Put(write_options, "key10497", "value3699");

  // op 13682: put
  db->Put(write_options, "key7469", "value26166");

  // op 13683: put
  db->Put(write_options, "key8407", "value75177");

  // op 13684: put
  db->Put(write_options, "key2106", "value76312");

  // op 13685: put
  db->Put(write_options, "key4328", "value97353");

  // op 13686: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 13687: put
  db->Put(write_options, "key8958", "value39859");

  // op 13688: get
  status = db->Get(read_options, "key11189", &value);
  if (status.ok()) {
    out << "GET key11189 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13689: put
  db->Put(write_options, "key5080", "value78139");

  // op 13690: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 13691: delete
  db->Delete(write_options, "key9547");

  // op 13692: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2113", "key7115", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2113, key7115) size=" << scan_result.size() << std::endl;
  }

  // op 13693: put
  db->Put(write_options, "key8556", "value55753");

  // op 13694: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 13695: get
  status = db->Get(read_options, "key1243", &value);
  if (status.ok()) {
    out << "GET key1243 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13696: put
  db->Put(write_options, "key7568", "value20301");

  // op 13697: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 13698: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4893", "key7795", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4893, key7795) size=" << scan_result.size() << std::endl;
  }

  // op 13699: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2146", "key7822", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2146, key7822) size=" << scan_result.size() << std::endl;
  }

  // op 13700: delete_range
  status = db->DeleteRange(write_options, "key5514", "key9083");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13701: put
  db->Put(write_options, "key5656", "value85832");

  // op 13702: put
  db->Put(write_options, "key8069", "value17870");

  // op 13703: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 13704: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6375", "key11103", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6375, key11103) size=" << scan_result.size() << std::endl;
  }

  // op 13705: put
  db->Put(write_options, "key4565", "value13848");

  // op 13706: get
  status = db->Get(read_options, "key10176", &value);
  if (status.ok()) {
    out << "GET key10176 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13707: delete_range
  status = db->DeleteRange(write_options, "key3302", "key8605");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13708: put
  db->Put(write_options, "key2445", "value3476");

  // op 13709: get
  status = db->Get(read_options, "key5346", &value);
  if (status.ok()) {
    out << "GET key5346 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13710: delete_range
  status = db->DeleteRange(write_options, "key8377", "key10780");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13711: put
  db->Put(write_options, "key4391", "value10945");

  // op 13712: put
  db->Put(write_options, "key7275", "value61461");

  // op 13713: delete
  db->Delete(write_options, "key6359");

  // op 13714: put
  db->Put(write_options, "key8983", "value67389");

  // op 13715: put
  db->Put(write_options, "key1721", "value78913");

  // op 13716: put
  db->Put(write_options, "key6914", "value117721");

  // op 13717: get
  status = db->Get(read_options, "key5778", &value);
  if (status.ok()) {
    out << "GET key5778 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13718: put
  db->Put(write_options, "key8886", "value55042");

  // op 13719: delete_range
  status = db->DeleteRange(write_options, "key0875", "key2225");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13720: get
  status = db->Get(read_options, "key3389", &value);
  if (status.ok()) {
    out << "GET key3389 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13721: get
  status = db->Get(read_options, "key4888", &value);
  if (status.ok()) {
    out << "GET key4888 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13722: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4588", "key9654", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4588, key9654) size=" << scan_result.size() << std::endl;
  }

  // op 13723: put
  db->Put(write_options, "key1264", "value9524");

  // op 13724: put
  db->Put(write_options, "key6326", "value39898");

  // op 13725: put
  db->Put(write_options, "key8098", "value55946");

  // op 13726: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1156", "key8964", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1156, key8964) size=" << scan_result.size() << std::endl;
  }

  // op 13727: put
  db->Put(write_options, "key3076", "value13347");

  // op 13728: delete
  db->Delete(write_options, "key2116");

  // op 13729: delete
  db->Delete(write_options, "key1354");

  // op 13730: delete_range
  status = db->DeleteRange(write_options, "key0886", "key0939");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13731: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 13732: put
  db->Put(write_options, "key10902", "value99822");

  // op 13733: put
  db->Put(write_options, "key11532", "value20687");

  // op 13734: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 13735: put
  db->Put(write_options, "key9017", "value11913");

  // op 13736: put
  db->Put(write_options, "key9096", "value113068");

  // op 13737: put
  db->Put(write_options, "key5285", "value17107");

  // op 13738: put
  db->Put(write_options, "key11251", "value35234");

  // op 13739: put
  db->Put(write_options, "key1112", "value28485");

  // op 13740: put
  db->Put(write_options, "key7992", "value25838");

  // op 13741: put
  db->Put(write_options, "key9740", "value109958");

  // op 13742: put
  db->Put(write_options, "key7758", "value25332");

  // op 13743: delete
  db->Delete(write_options, "key6203");

  // op 13744: get
  status = db->Get(read_options, "key7868", &value);
  if (status.ok()) {
    out << "GET key7868 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13745: put
  db->Put(write_options, "key1187", "value96729");

  // op 13746: put
  db->Put(write_options, "key8555", "value97249");

  // op 13747: put
  db->Put(write_options, "key2892", "value7845");

  // op 13748: put
  db->Put(write_options, "key9530", "value21487");

  // op 13749: delete_range
  status = db->DeleteRange(write_options, "key9652", "key10736");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13750: get
  status = db->Get(read_options, "key6181", &value);
  if (status.ok()) {
    out << "GET key6181 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13751: get
  status = db->Get(read_options, "key4930", &value);
  if (status.ok()) {
    out << "GET key4930 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13752: get
  status = db->Get(read_options, "key3527", &value);
  if (status.ok()) {
    out << "GET key3527 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13753: put
  db->Put(write_options, "key9431", "value65122");

  // op 13754: put
  db->Put(write_options, "key4513", "value110530");

  // op 13755: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7227", "key8889", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7227, key8889) size=" << scan_result.size() << std::endl;
  }

  // op 13756: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3876", "key7283", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3876, key7283) size=" << scan_result.size() << std::endl;
  }

  // op 13757: put
  db->Put(write_options, "key9997", "value47536");

  // op 13758: put
  db->Put(write_options, "key10832", "value73395");

  // op 13759: delete_range
  status = db->DeleteRange(write_options, "key4344", "key11665");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13760: delete_range
  status = db->DeleteRange(write_options, "key4739", "key7887");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13761: put
  db->Put(write_options, "key8095", "value109733");

  // op 13762: delete
  db->Delete(write_options, "key0769");

  // op 13763: delete_range
  status = db->DeleteRange(write_options, "key4481", "key10823");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13764: delete_range
  status = db->DeleteRange(write_options, "key0031", "key9351");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13765: delete
  db->Delete(write_options, "key10805");

  // op 13766: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0725", "key5899", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0725, key5899) size=" << scan_result.size() << std::endl;
  }

  // op 13767: put
  db->Put(write_options, "key9052", "value96515");

  // op 13768: delete_range
  status = db->DeleteRange(write_options, "key7883", "key9378");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13769: delete_range
  status = db->DeleteRange(write_options, "key0848", "key1212");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13770: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3356", "key10328", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3356, key10328) size=" << scan_result.size() << std::endl;
  }

  // op 13771: put
  db->Put(write_options, "key10757", "value40440");

  // op 13772: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 13773: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9518", "key10289", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9518, key10289) size=" << scan_result.size() << std::endl;
  }

  // op 13774: delete_range
  status = db->DeleteRange(write_options, "key3637", "key6224");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13775: get
  status = db->Get(read_options, "key2457", &value);
  if (status.ok()) {
    out << "GET key2457 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13776: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2187", "key5586", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2187, key5586) size=" << scan_result.size() << std::endl;
  }

  // op 13777: put
  db->Put(write_options, "key10234", "value104309");

  // op 13778: get
  status = db->Get(read_options, "key3641", &value);
  if (status.ok()) {
    out << "GET key3641 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13779: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 13780: delete
  db->Delete(write_options, "key2488");

  // op 13781: delete_range
  status = db->DeleteRange(write_options, "key0915", "key2257");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13782: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5069", "key9286", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5069, key9286) size=" << scan_result.size() << std::endl;
  }

  // op 13783: delete
  db->Delete(write_options, "key4239");

  // op 13784: put
  db->Put(write_options, "key10290", "value4323");

  // op 13785: put
  db->Put(write_options, "key8259", "value113162");

  // op 13786: get
  status = db->Get(read_options, "key8311", &value);
  if (status.ok()) {
    out << "GET key8311 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13787: put
  db->Put(write_options, "key4067", "value64580");

  // op 13788: put
  db->Put(write_options, "key3013", "value105088");

  // op 13789: put
  db->Put(write_options, "key8567", "value20207");

  // op 13790: put
  db->Put(write_options, "key6439", "value115625");

  // op 13791: put
  db->Put(write_options, "key3587", "value20543");

  // op 13792: get
  status = db->Get(read_options, "key0795", &value);
  if (status.ok()) {
    out << "GET key0795 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13793: put
  db->Put(write_options, "key1126", "value44930");

  // op 13794: put
  db->Put(write_options, "key1253", "value102150");

  // op 13795: put
  db->Put(write_options, "key10823", "value77060");

  // op 13796: put
  db->Put(write_options, "key6205", "value34055");

  // op 13797: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 13798: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2081", "key6075", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2081, key6075) size=" << scan_result.size() << std::endl;
  }

  // op 13799: put
  db->Put(write_options, "key10395", "value46274");

  // op 13800: put
  db->Put(write_options, "key11015", "value6464");

  // op 13801: get
  status = db->Get(read_options, "key0914", &value);
  if (status.ok()) {
    out << "GET key0914 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13802: put
  db->Put(write_options, "key4250", "value110509");

  // op 13803: put
  db->Put(write_options, "key10999", "value90162");

  // op 13804: put
  db->Put(write_options, "key4948", "value23435");

  // op 13805: delete_range
  status = db->DeleteRange(write_options, "key10630", "key10631");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13806: delete_range
  status = db->DeleteRange(write_options, "key0705", "key3828");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13807: delete_range
  status = db->DeleteRange(write_options, "key4085", "key8715");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13808: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 13809: get
  status = db->Get(read_options, "key8203", &value);
  if (status.ok()) {
    out << "GET key8203 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13810: put
  db->Put(write_options, "key6447", "value92118");

  // op 13811: put
  db->Put(write_options, "key4558", "value6761");

  // op 13812: get
  status = db->Get(read_options, "key1653", &value);
  if (status.ok()) {
    out << "GET key1653 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13813: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0554", "key10130", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0554, key10130) size=" << scan_result.size() << std::endl;
  }

  // op 13814: put
  db->Put(write_options, "key3729", "value19088");

  // op 13815: put
  db->Put(write_options, "key6776", "value16056");

  // op 13816: put
  db->Put(write_options, "key10148", "value77191");

  // op 13817: put
  db->Put(write_options, "key3610", "value17243");

  // op 13818: put
  db->Put(write_options, "key2226", "value12729");

  // op 13819: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 13820: put
  db->Put(write_options, "key6000", "value86895");

  // op 13821: put
  db->Put(write_options, "key0398", "value6371");

  // op 13822: put
  db->Put(write_options, "key10440", "value96356");

  // op 13823: put
  db->Put(write_options, "key3553", "value86582");

  // op 13824: put
  db->Put(write_options, "key1891", "value48268");

  // op 13825: get
  status = db->Get(read_options, "key11224", &value);
  if (status.ok()) {
    out << "GET key11224 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13826: put
  db->Put(write_options, "key10930", "value46164");

  // op 13827: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6177", "key9115", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6177, key9115) size=" << scan_result.size() << std::endl;
  }

  // op 13828: delete_range
  status = db->DeleteRange(write_options, "key0498", "key10407");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13829: delete_range
  status = db->DeleteRange(write_options, "key3191", "key5059");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13830: put
  db->Put(write_options, "key8801", "value94730");

  // op 13831: delete_range
  status = db->DeleteRange(write_options, "key2413", "key6187");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13832: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10173", "key10734", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10173, key10734) size=" << scan_result.size() << std::endl;
  }

  // op 13833: put
  db->Put(write_options, "key10893", "value43305");

  // op 13834: delete_range
  status = db->DeleteRange(write_options, "key1750", "key4510");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13835: put
  db->Put(write_options, "key5896", "value58362");

  // op 13836: put
  db->Put(write_options, "key4084", "value114379");

  // op 13837: put
  db->Put(write_options, "key5458", "value108965");

  // op 13838: get
  status = db->Get(read_options, "key1077", &value);
  if (status.ok()) {
    out << "GET key1077 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13839: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4115", "key7682", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4115, key7682) size=" << scan_result.size() << std::endl;
  }

  // op 13840: put
  db->Put(write_options, "key4847", "value88218");

  // op 13841: get
  status = db->Get(read_options, "key8534", &value);
  if (status.ok()) {
    out << "GET key8534 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13842: put
  db->Put(write_options, "key1835", "value50558");

  // op 13843: put
  db->Put(write_options, "key5389", "value32061");

  // op 13844: delete
  db->Delete(write_options, "key3640");

  // op 13845: put
  db->Put(write_options, "key8484", "value18827");

  // op 13846: delete_range
  status = db->DeleteRange(write_options, "key0154", "key2277");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13847: put
  db->Put(write_options, "key8009", "value2164");

  // op 13848: get
  status = db->Get(read_options, "key8077", &value);
  if (status.ok()) {
    out << "GET key8077 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13849: delete_range
  status = db->DeleteRange(write_options, "key1377", "key7521");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13850: put
  db->Put(write_options, "key11345", "value53028");

  // op 13851: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 13852: delete
  db->Delete(write_options, "key4745");

  // op 13853: delete
  db->Delete(write_options, "key5472");

  // op 13854: put
  db->Put(write_options, "key11037", "value56922");

  // op 13855: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4568", "key10477", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4568, key10477) size=" << scan_result.size() << std::endl;
  }

  // op 13856: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0381", "key0606", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0381, key0606) size=" << scan_result.size() << std::endl;
  }

  // op 13857: put
  db->Put(write_options, "key7521", "value107806");

  // op 13858: put
  db->Put(write_options, "key10971", "value94013");

  // op 13859: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 13860: delete_range
  status = db->DeleteRange(write_options, "key2843", "key5318");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13861: put
  db->Put(write_options, "key7211", "value32876");

  // op 13862: get
  status = db->Get(read_options, "key6096", &value);
  if (status.ok()) {
    out << "GET key6096 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13863: put
  db->Put(write_options, "key4059", "value14169");

  // op 13864: put
  db->Put(write_options, "key7338", "value42573");

  // op 13865: delete
  db->Delete(write_options, "key6208");

  // op 13866: get
  status = db->Get(read_options, "key9239", &value);
  if (status.ok()) {
    out << "GET key9239 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13867: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 13868: get
  status = db->Get(read_options, "key3682", &value);
  if (status.ok()) {
    out << "GET key3682 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13869: put
  db->Put(write_options, "key2084", "value36724");

  // op 13870: put
  db->Put(write_options, "key5900", "value65594");

  // op 13871: put
  db->Put(write_options, "key1331", "value44036");

  // op 13872: put
  db->Put(write_options, "key1212", "value68215");

  // op 13873: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6240", "key7077", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6240, key7077) size=" << scan_result.size() << std::endl;
  }

  // op 13874: put
  db->Put(write_options, "key5704", "value84103");

  // op 13875: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 13876: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 13877: delete
  db->Delete(write_options, "key1180");

  // op 13878: delete_range
  status = db->DeleteRange(write_options, "key9668", "key11616");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13879: put
  db->Put(write_options, "key2923", "value58419");

  // op 13880: put
  db->Put(write_options, "key0858", "value20281");

  // op 13881: put
  db->Put(write_options, "key11037", "value117952");

  // op 13882: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 13883: delete
  db->Delete(write_options, "key7720");

  // op 13884: put
  db->Put(write_options, "key8380", "value15306");

  // op 13885: get
  status = db->Get(read_options, "key11450", &value);
  if (status.ok()) {
    out << "GET key11450 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13886: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9786", "key11728", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9786, key11728) size=" << scan_result.size() << std::endl;
  }

  // op 13887: get
  status = db->Get(read_options, "key2231", &value);
  if (status.ok()) {
    out << "GET key2231 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13888: put
  db->Put(write_options, "key8148", "value30756");

  // op 13889: put
  db->Put(write_options, "key3999", "value7911");

  // op 13890: delete
  db->Delete(write_options, "key10876");

  // op 13891: put
  db->Put(write_options, "key4066", "value116302");

  // op 13892: delete_range
  status = db->DeleteRange(write_options, "key1661", "key7419");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13893: delete
  db->Delete(write_options, "key7514");

  // op 13894: get
  status = db->Get(read_options, "key4288", &value);
  if (status.ok()) {
    out << "GET key4288 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13895: get
  status = db->Get(read_options, "key8458", &value);
  if (status.ok()) {
    out << "GET key8458 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13896: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3041", "key5187", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3041, key5187) size=" << scan_result.size() << std::endl;
  }

  // op 13897: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6643", "key7075", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6643, key7075) size=" << scan_result.size() << std::endl;
  }

  // op 13898: put
  db->Put(write_options, "key4666", "value107967");

  // op 13899: put
  db->Put(write_options, "key10335", "value73776");

  // op 13900: put
  db->Put(write_options, "key7701", "value116401");

  // op 13901: put
  db->Put(write_options, "key9972", "value16675");

  // op 13902: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3947", "key8177", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3947, key8177) size=" << scan_result.size() << std::endl;
  }

  // op 13903: put
  db->Put(write_options, "key2149", "value21682");

  // op 13904: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 13905: delete
  db->Delete(write_options, "key6786");

  // op 13906: delete_range
  status = db->DeleteRange(write_options, "key6556", "key11234");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13907: put
  db->Put(write_options, "key3954", "value25346");

  // op 13908: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7509", "key11172", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7509, key11172) size=" << scan_result.size() << std::endl;
  }

  // op 13909: put
  db->Put(write_options, "key4155", "value27895");

  // op 13910: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1856", "key7479", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1856, key7479) size=" << scan_result.size() << std::endl;
  }

  // op 13911: put
  db->Put(write_options, "key7332", "value103426");

  // op 13912: get
  status = db->Get(read_options, "key2124", &value);
  if (status.ok()) {
    out << "GET key2124 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13913: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 13914: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 13915: get
  status = db->Get(read_options, "key11113", &value);
  if (status.ok()) {
    out << "GET key11113 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13916: delete
  db->Delete(write_options, "key6380");

  // op 13917: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3926", "key6258", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3926, key6258) size=" << scan_result.size() << std::endl;
  }

  // op 13918: put
  db->Put(write_options, "key3900", "value23989");

  // op 13919: get
  status = db->Get(read_options, "key0636", &value);
  if (status.ok()) {
    out << "GET key0636 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13920: put
  db->Put(write_options, "key10816", "value53988");

  // op 13921: delete
  db->Delete(write_options, "key7204");

  // op 13922: get
  status = db->Get(read_options, "key7833", &value);
  if (status.ok()) {
    out << "GET key7833 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13923: delete
  db->Delete(write_options, "key9620");

  // op 13924: put
  db->Put(write_options, "key7757", "value27286");

  // op 13925: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0212", "key6886", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0212, key6886) size=" << scan_result.size() << std::endl;
  }

  // op 13926: delete
  db->Delete(write_options, "key6267");

  // op 13927: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5485", "key9539", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5485, key9539) size=" << scan_result.size() << std::endl;
  }

  // op 13928: delete_range
  status = db->DeleteRange(write_options, "key6753", "key10533");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13929: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2058", "key4814", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2058, key4814) size=" << scan_result.size() << std::endl;
  }

  // op 13930: put
  db->Put(write_options, "key7531", "value99236");

  // op 13931: get
  status = db->Get(read_options, "key2315", &value);
  if (status.ok()) {
    out << "GET key2315 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13932: get
  status = db->Get(read_options, "key10189", &value);
  if (status.ok()) {
    out << "GET key10189 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13933: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 13934: get
  status = db->Get(read_options, "key5882", &value);
  if (status.ok()) {
    out << "GET key5882 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13935: delete_range
  status = db->DeleteRange(write_options, "key8735", "key10970");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13936: delete_range
  status = db->DeleteRange(write_options, "key1599", "key9353");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13937: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 13938: put
  db->Put(write_options, "key7726", "value13636");

  // op 13939: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3208", "key6665", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3208, key6665) size=" << scan_result.size() << std::endl;
  }

  // op 13940: delete
  db->Delete(write_options, "key10354");

  // op 13941: delete_range
  status = db->DeleteRange(write_options, "key2832", "key4104");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13942: get
  status = db->Get(read_options, "key6447", &value);
  if (status.ok()) {
    out << "GET key6447 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13943: put
  db->Put(write_options, "key1235", "value69111");

  // op 13944: get
  status = db->Get(read_options, "key7929", &value);
  if (status.ok()) {
    out << "GET key7929 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13945: get
  status = db->Get(read_options, "key7917", &value);
  if (status.ok()) {
    out << "GET key7917 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13946: delete
  db->Delete(write_options, "key7485");

  // op 13947: put
  db->Put(write_options, "key4598", "value91521");

  // op 13948: put
  db->Put(write_options, "key6863", "value73911");

  // op 13949: put
  db->Put(write_options, "key8358", "value56314");

  // op 13950: put
  db->Put(write_options, "key8934", "value7986");

  // op 13951: put
  db->Put(write_options, "key1326", "value71772");

  // op 13952: put
  db->Put(write_options, "key1906", "value97946");

  // op 13953: get
  status = db->Get(read_options, "key0274", &value);
  if (status.ok()) {
    out << "GET key0274 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13954: put
  db->Put(write_options, "key4273", "value49179");

  // op 13955: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 13956: delete
  db->Delete(write_options, "key6520");

  // op 13957: put
  db->Put(write_options, "key2117", "value51553");

  // op 13958: put
  db->Put(write_options, "key4669", "value32222");

  // op 13959: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0774", "key6395", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0774, key6395) size=" << scan_result.size() << std::endl;
  }

  // op 13960: put
  db->Put(write_options, "key1432", "value64170");

  // op 13961: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 13962: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6328", "key8189", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6328, key8189) size=" << scan_result.size() << std::endl;
  }

  // op 13963: put
  db->Put(write_options, "key11376", "value92518");

  // op 13964: delete_range
  status = db->DeleteRange(write_options, "key7005", "key10478");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13965: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1300", "key8031", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1300, key8031) size=" << scan_result.size() << std::endl;
  }

  // op 13966: put
  db->Put(write_options, "key6791", "value34971");

  // op 13967: put
  db->Put(write_options, "key11598", "value54991");

  // op 13968: delete
  db->Delete(write_options, "key8325");

  // op 13969: put
  db->Put(write_options, "key3109", "value106404");

  // op 13970: get
  status = db->Get(read_options, "key7537", &value);
  if (status.ok()) {
    out << "GET key7537 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13971: put
  db->Put(write_options, "key1101", "value2759");

  // op 13972: get
  status = db->Get(read_options, "key0099", &value);
  if (status.ok()) {
    out << "GET key0099 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13973: put
  db->Put(write_options, "key3390", "value87490");

  // op 13974: put
  db->Put(write_options, "key10937", "value110547");

  // op 13975: delete_range
  status = db->DeleteRange(write_options, "key1566", "key6118");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13976: delete_range
  status = db->DeleteRange(write_options, "key2213", "key11613");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13977: delete
  db->Delete(write_options, "key5686");

  // op 13978: get
  status = db->Get(read_options, "key1753", &value);
  if (status.ok()) {
    out << "GET key1753 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13979: get
  status = db->Get(read_options, "key1392", &value);
  if (status.ok()) {
    out << "GET key1392 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13980: get
  status = db->Get(read_options, "key5194", &value);
  if (status.ok()) {
    out << "GET key5194 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13981: get
  status = db->Get(read_options, "key9299", &value);
  if (status.ok()) {
    out << "GET key9299 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13982: delete
  db->Delete(write_options, "key7854");

  // op 13983: get
  status = db->Get(read_options, "key0287", &value);
  if (status.ok()) {
    out << "GET key0287 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13984: delete
  db->Delete(write_options, "key8758");

  // op 13985: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1767", "key3173", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1767, key3173) size=" << scan_result.size() << std::endl;
  }

  // op 13986: put
  db->Put(write_options, "key4612", "value107744");

  // op 13987: put
  db->Put(write_options, "key1134", "value36461");

  // op 13988: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8335", "key10928", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8335, key10928) size=" << scan_result.size() << std::endl;
  }

  // op 13989: put
  db->Put(write_options, "key10005", "value109788");

  // op 13990: get
  status = db->Get(read_options, "key5178", &value);
  if (status.ok()) {
    out << "GET key5178 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13991: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1247", "key4628", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1247, key4628) size=" << scan_result.size() << std::endl;
  }

  // op 13992: put
  db->Put(write_options, "key2687", "value23661");

  // op 13993: get
  status = db->Get(read_options, "key10875", &value);
  if (status.ok()) {
    out << "GET key10875 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 13994: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4280", "key9038", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4280, key9038) size=" << scan_result.size() << std::endl;
  }

  // op 13995: put
  db->Put(write_options, "key5949", "value33934");

  // op 13996: delete
  db->Delete(write_options, "key5891");

  // op 13997: delete
  db->Delete(write_options, "key0254");

  // op 13998: delete_range
  status = db->DeleteRange(write_options, "key1905", "key3751");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 13999: put
  db->Put(write_options, "key9017", "value10912");

  // op 14000: get
  status = db->Get(read_options, "key11091", &value);
  if (status.ok()) {
    out << "GET key11091 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14001: delete
  db->Delete(write_options, "key11687");

  // op 14002: put
  db->Put(write_options, "key8489", "value107944");

  // op 14003: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 14004: put
  db->Put(write_options, "key9227", "value67101");

  // op 14005: delete_range
  status = db->DeleteRange(write_options, "key1371", "key7527");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 14006: put
  db->Put(write_options, "key0161", "value95151");

  // op 14007: put
  db->Put(write_options, "key9487", "value37152");

  // op 14008: delete
  db->Delete(write_options, "key8719");

  // op 14009: get
  status = db->Get(read_options, "key11265", &value);
  if (status.ok()) {
    out << "GET key11265 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14010: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 14011: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 14012: get
  status = db->Get(read_options, "key6640", &value);
  if (status.ok()) {
    out << "GET key6640 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14013: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0071", "key9571", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0071, key9571) size=" << scan_result.size() << std::endl;
  }

  // op 14014: delete
  db->Delete(write_options, "key5705");

  // op 14015: delete
  db->Delete(write_options, "key8391");

  // op 14016: delete
  db->Delete(write_options, "key2325");

  // op 14017: put
  db->Put(write_options, "key0940", "value71248");

  // op 14018: put
  db->Put(write_options, "key7349", "value44638");

  // op 14019: delete
  db->Delete(write_options, "key1622");

  // op 14020: get
  status = db->Get(read_options, "key2934", &value);
  if (status.ok()) {
    out << "GET key2934 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14021: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1328", "key4575", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1328, key4575) size=" << scan_result.size() << std::endl;
  }

  // op 14022: put
  db->Put(write_options, "key5839", "value80101");

  // op 14023: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2384", "key9470", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2384, key9470) size=" << scan_result.size() << std::endl;
  }

  // op 14024: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7890", "key8911", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7890, key8911) size=" << scan_result.size() << std::endl;
  }

  // op 14025: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 14026: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 14027: put
  db->Put(write_options, "key7647", "value1919");

  // op 14028: delete
  db->Delete(write_options, "key7185");

  // op 14029: get
  status = db->Get(read_options, "key9547", &value);
  if (status.ok()) {
    out << "GET key9547 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14030: put
  db->Put(write_options, "key1551", "value4733");

  // op 14031: put
  db->Put(write_options, "key2219", "value92848");

  // op 14032: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3375", "key4246", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3375, key4246) size=" << scan_result.size() << std::endl;
  }

  // op 14033: get
  status = db->Get(read_options, "key3944", &value);
  if (status.ok()) {
    out << "GET key3944 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14034: delete_range
  status = db->DeleteRange(write_options, "key3975", "key10441");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 14035: delete_range
  status = db->DeleteRange(write_options, "key6237", "key8434");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 14036: put
  db->Put(write_options, "key0201", "value35064");

  // op 14037: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4937", "key5579", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4937, key5579) size=" << scan_result.size() << std::endl;
  }

  // op 14038: put
  db->Put(write_options, "key6895", "value92088");

  // op 14039: delete_range
  status = db->DeleteRange(write_options, "key3150", "key6200");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 14040: delete_range
  status = db->DeleteRange(write_options, "key3996", "key5137");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 14041: put
  db->Put(write_options, "key1900", "value84777");

  // op 14042: delete_range
  status = db->DeleteRange(write_options, "key2919", "key6724");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 14043: put
  db->Put(write_options, "key9767", "value75250");

  // op 14044: delete
  db->Delete(write_options, "key7012");

  // op 14045: delete_range
  status = db->DeleteRange(write_options, "key7535", "key10370");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 14046: get
  status = db->Get(read_options, "key7520", &value);
  if (status.ok()) {
    out << "GET key7520 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14047: delete_range
  status = db->DeleteRange(write_options, "key0646", "key11224");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 14048: put
  db->Put(write_options, "key7331", "value113859");

  // op 14049: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 14050: get
  status = db->Get(read_options, "key4969", &value);
  if (status.ok()) {
    out << "GET key4969 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14051: delete
  db->Delete(write_options, "key7392");

  // op 14052: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4648", "key6812", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4648, key6812) size=" << scan_result.size() << std::endl;
  }

  // op 14053: put
  db->Put(write_options, "key6302", "value82559");

  // op 14054: delete
  db->Delete(write_options, "key7907");

  // op 14055: get
  status = db->Get(read_options, "key11634", &value);
  if (status.ok()) {
    out << "GET key11634 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14056: put
  db->Put(write_options, "key4911", "value74141");

  // op 14057: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3476", "key10597", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3476, key10597) size=" << scan_result.size() << std::endl;
  }

  // op 14058: get
  status = db->Get(read_options, "key4948", &value);
  if (status.ok()) {
    out << "GET key4948 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14059: get
  status = db->Get(read_options, "key4105", &value);
  if (status.ok()) {
    out << "GET key4105 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14060: put
  db->Put(write_options, "key10930", "value117801");

  // op 14061: delete
  db->Delete(write_options, "key4202");

  // op 14062: put
  db->Put(write_options, "key8910", "value34674");

  // op 14063: put
  db->Put(write_options, "key1387", "value118231");

  // op 14064: put
  db->Put(write_options, "key0933", "value114057");

  // op 14065: put
  db->Put(write_options, "key4683", "value1082");

  // op 14066: put
  db->Put(write_options, "key7592", "value0547");

  // op 14067: put
  db->Put(write_options, "key8999", "value61186");

  // op 14068: get
  status = db->Get(read_options, "key9104", &value);
  if (status.ok()) {
    out << "GET key9104 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14069: put
  db->Put(write_options, "key6160", "value21514");

  // op 14070: delete_range
  status = db->DeleteRange(write_options, "key0998", "key6533");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 14071: get
  status = db->Get(read_options, "key4939", &value);
  if (status.ok()) {
    out << "GET key4939 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14072: put
  db->Put(write_options, "key5881", "value32083");

  // op 14073: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7864", "key11039", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7864, key11039) size=" << scan_result.size() << std::endl;
  }

  // op 14074: put
  db->Put(write_options, "key0256", "value92041");

  // op 14075: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2145", "key7520", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2145, key7520) size=" << scan_result.size() << std::endl;
  }

  // op 14076: get
  status = db->Get(read_options, "key7605", &value);
  if (status.ok()) {
    out << "GET key7605 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14077: put
  db->Put(write_options, "key8625", "value5462");

  // op 14078: put
  db->Put(write_options, "key5603", "value29895");

  // op 14079: get
  status = db->Get(read_options, "key11116", &value);
  if (status.ok()) {
    out << "GET key11116 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14080: put
  db->Put(write_options, "key8452", "value29008");

  // op 14081: put
  db->Put(write_options, "key4997", "value88111");

  // op 14082: put
  db->Put(write_options, "key11197", "value40520");

  // op 14083: delete_range
  status = db->DeleteRange(write_options, "key1506", "key2203");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 14084: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6868", "key11583", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6868, key11583) size=" << scan_result.size() << std::endl;
  }

  // op 14085: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5337", "key6393", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5337, key6393) size=" << scan_result.size() << std::endl;
  }

  // op 14086: delete
  db->Delete(write_options, "key6502");

  // op 14087: put
  db->Put(write_options, "key11449", "value3011");

  // op 14088: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6827", "key11653", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6827, key11653) size=" << scan_result.size() << std::endl;
  }

  // op 14089: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6556", "key8992", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6556, key8992) size=" << scan_result.size() << std::endl;
  }

  // op 14090: put
  db->Put(write_options, "key2818", "value43670");

  // op 14091: delete_range
  status = db->DeleteRange(write_options, "key6738", "key9992");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 14092: put
  db->Put(write_options, "key5938", "value42119");

  // op 14093: get
  status = db->Get(read_options, "key3570", &value);
  if (status.ok()) {
    out << "GET key3570 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14094: put
  db->Put(write_options, "key0395", "value22786");

  // op 14095: get
  status = db->Get(read_options, "key8853", &value);
  if (status.ok()) {
    out << "GET key8853 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14096: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0807", "key6054", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0807, key6054) size=" << scan_result.size() << std::endl;
  }

  // op 14097: delete
  db->Delete(write_options, "key4368");

  // op 14098: get
  status = db->Get(read_options, "key1253", &value);
  if (status.ok()) {
    out << "GET key1253 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14099: get
  status = db->Get(read_options, "key10950", &value);
  if (status.ok()) {
    out << "GET key10950 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14100: put
  db->Put(write_options, "key7822", "value7635");

  // op 14101: get
  status = db->Get(read_options, "key5409", &value);
  if (status.ok()) {
    out << "GET key5409 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14102: delete
  db->Delete(write_options, "key4252");

  // op 14103: put
  db->Put(write_options, "key4745", "value57906");

  // op 14104: get
  status = db->Get(read_options, "key5011", &value);
  if (status.ok()) {
    out << "GET key5011 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14105: delete_range
  status = db->DeleteRange(write_options, "key1210", "key6147");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 14106: put
  db->Put(write_options, "key2244", "value48978");

  // op 14107: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 14108: get
  status = db->Get(read_options, "key8749", &value);
  if (status.ok()) {
    out << "GET key8749 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14109: delete_range
  status = db->DeleteRange(write_options, "key2485", "key7428");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 14110: put
  db->Put(write_options, "key0700", "value82552");

  // op 14111: put
  db->Put(write_options, "key5411", "value110037");

  // op 14112: put
  db->Put(write_options, "key8707", "value69596");

  // op 14113: delete_range
  status = db->DeleteRange(write_options, "key7809", "key8398");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 14114: put
  db->Put(write_options, "key2965", "value70465");

  // op 14115: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8099", "key8586", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8099, key8586) size=" << scan_result.size() << std::endl;
  }

  // op 14116: get
  status = db->Get(read_options, "key7417", &value);
  if (status.ok()) {
    out << "GET key7417 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14117: put
  db->Put(write_options, "key11385", "value74604");

  // op 14118: put
  db->Put(write_options, "key3924", "value52754");

  // op 14119: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2735", "key3732", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2735, key3732) size=" << scan_result.size() << std::endl;
  }

  // op 14120: put
  db->Put(write_options, "key0959", "value44444");

  // op 14121: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0366", "key4236", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0366, key4236) size=" << scan_result.size() << std::endl;
  }

  // op 14122: put
  db->Put(write_options, "key5236", "value15066");

  // op 14123: delete
  db->Delete(write_options, "key2270");

  // op 14124: get
  status = db->Get(read_options, "key10745", &value);
  if (status.ok()) {
    out << "GET key10745 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14125: put
  db->Put(write_options, "key7032", "value74127");

  // op 14126: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5671", "key10973", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5671, key10973) size=" << scan_result.size() << std::endl;
  }

  // op 14127: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3131", "key7195", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3131, key7195) size=" << scan_result.size() << std::endl;
  }

  // op 14128: put
  db->Put(write_options, "key4612", "value20741");

  // op 14129: put
  db->Put(write_options, "key5404", "value34355");

  // op 14130: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0217", "key2517", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0217, key2517) size=" << scan_result.size() << std::endl;
  }

  // op 14131: delete_range
  status = db->DeleteRange(write_options, "key2592", "key7443");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 14132: get
  status = db->Get(read_options, "key1672", &value);
  if (status.ok()) {
    out << "GET key1672 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14133: put
  db->Put(write_options, "key2981", "value58518");

  // op 14134: get
  status = db->Get(read_options, "key1054", &value);
  if (status.ok()) {
    out << "GET key1054 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14135: put
  db->Put(write_options, "key5351", "value116459");

  // op 14136: put
  db->Put(write_options, "key11561", "value76604");

  // op 14137: put
  db->Put(write_options, "key0143", "value79280");

  // op 14138: put
  db->Put(write_options, "key9214", "value12266");

  // op 14139: delete_range
  status = db->DeleteRange(write_options, "key2986", "key5436");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 14140: delete_range
  status = db->DeleteRange(write_options, "key0227", "key3297");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 14141: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 14142: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 14143: put
  db->Put(write_options, "key3862", "value91247");

  // op 14144: put
  db->Put(write_options, "key4819", "value102655");

  // op 14145: put
  db->Put(write_options, "key11686", "value78625");

  // op 14146: put
  db->Put(write_options, "key6993", "value42261");

  // op 14147: get
  status = db->Get(read_options, "key3906", &value);
  if (status.ok()) {
    out << "GET key3906 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14148: put
  db->Put(write_options, "key8653", "value89692");

  // op 14149: put
  db->Put(write_options, "key7436", "value69168");

  // op 14150: delete_range
  status = db->DeleteRange(write_options, "key2499", "key9203");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 14151: put
  db->Put(write_options, "key6571", "value58550");

  // op 14152: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 14153: put
  db->Put(write_options, "key4531", "value12482");

  // op 14154: put
  db->Put(write_options, "key5382", "value112187");

  // op 14155: get
  status = db->Get(read_options, "key1122", &value);
  if (status.ok()) {
    out << "GET key1122 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14156: put
  db->Put(write_options, "key11048", "value42773");

  // op 14157: put
  db->Put(write_options, "key2976", "value76144");

  // op 14158: get
  status = db->Get(read_options, "key7243", &value);
  if (status.ok()) {
    out << "GET key7243 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14159: delete
  db->Delete(write_options, "key4467");

  // op 14160: get
  status = db->Get(read_options, "key7371", &value);
  if (status.ok()) {
    out << "GET key7371 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14161: put
  db->Put(write_options, "key10798", "value15744");

  // op 14162: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7651", "key10736", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7651, key10736) size=" << scan_result.size() << std::endl;
  }

  // op 14163: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3043", "key8543", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3043, key8543) size=" << scan_result.size() << std::endl;
  }

  // op 14164: get
  status = db->Get(read_options, "key7627", &value);
  if (status.ok()) {
    out << "GET key7627 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14165: put
  db->Put(write_options, "key11139", "value85668");

  // op 14166: put
  db->Put(write_options, "key4756", "value27414");

  // op 14167: get
  status = db->Get(read_options, "key4530", &value);
  if (status.ok()) {
    out << "GET key4530 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14168: delete
  db->Delete(write_options, "key7866");

  // op 14169: put
  db->Put(write_options, "key9684", "value23984");

  // op 14170: put
  db->Put(write_options, "key0185", "value111063");

  // op 14171: put
  db->Put(write_options, "key0591", "value39918");

  // op 14172: put
  db->Put(write_options, "key1331", "value12082");

  // op 14173: put
  db->Put(write_options, "key0937", "value37253");

  // op 14174: put
  db->Put(write_options, "key8360", "value70284");

  // op 14175: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2754", "key11196", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2754, key11196) size=" << scan_result.size() << std::endl;
  }

  // op 14176: get
  status = db->Get(read_options, "key8030", &value);
  if (status.ok()) {
    out << "GET key8030 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14177: put
  db->Put(write_options, "key11285", "value103566");

  // op 14178: put
  db->Put(write_options, "key3881", "value66020");

  // op 14179: get
  status = db->Get(read_options, "key4877", &value);
  if (status.ok()) {
    out << "GET key4877 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14180: put
  db->Put(write_options, "key7767", "value82662");

  // op 14181: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10323", "key11320", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10323, key11320) size=" << scan_result.size() << std::endl;
  }

  // op 14182: put
  db->Put(write_options, "key0593", "value59032");

  // op 14183: get
  status = db->Get(read_options, "key5201", &value);
  if (status.ok()) {
    out << "GET key5201 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14184: get
  status = db->Get(read_options, "key9985", &value);
  if (status.ok()) {
    out << "GET key9985 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14185: delete_range
  status = db->DeleteRange(write_options, "key0074", "key6005");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 14186: put
  db->Put(write_options, "key1818", "value101991");

  // op 14187: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7422", "key8752", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7422, key8752) size=" << scan_result.size() << std::endl;
  }

  // op 14188: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0025", "key8013", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0025, key8013) size=" << scan_result.size() << std::endl;
  }

  // op 14189: put
  db->Put(write_options, "key6480", "value73828");

  // op 14190: get
  status = db->Get(read_options, "key2053", &value);
  if (status.ok()) {
    out << "GET key2053 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14191: put
  db->Put(write_options, "key8995", "value105091");

  // op 14192: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 14193: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 14194: delete
  db->Delete(write_options, "key3208");

  // op 14195: put
  db->Put(write_options, "key4497", "value66311");

  // op 14196: put
  db->Put(write_options, "key2324", "value33105");

  // op 14197: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2052", "key11128", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2052, key11128) size=" << scan_result.size() << std::endl;
  }

  // op 14198: get
  status = db->Get(read_options, "key5400", &value);
  if (status.ok()) {
    out << "GET key5400 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14199: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1358", "key2857", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1358, key2857) size=" << scan_result.size() << std::endl;
  }

  // op 14200: delete
  db->Delete(write_options, "key1833");

  // op 14201: put
  db->Put(write_options, "key4233", "value6919");

  // op 14202: get
  status = db->Get(read_options, "key8705", &value);
  if (status.ok()) {
    out << "GET key8705 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14203: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2947", "key9027", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2947, key9027) size=" << scan_result.size() << std::endl;
  }

  // op 14204: put
  db->Put(write_options, "key6031", "value101166");

  // op 14205: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 14206: get
  status = db->Get(read_options, "key7641", &value);
  if (status.ok()) {
    out << "GET key7641 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14207: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0445", "key11655", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0445, key11655) size=" << scan_result.size() << std::endl;
  }

  // op 14208: delete_range
  status = db->DeleteRange(write_options, "key3412", "key7756");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 14209: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 14210: delete_range
  status = db->DeleteRange(write_options, "key0793", "key11302");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 14211: put
  db->Put(write_options, "key3377", "value73568");

  // op 14212: put
  db->Put(write_options, "key9553", "value49527");

  // op 14213: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1759", "key7967", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1759, key7967) size=" << scan_result.size() << std::endl;
  }

  // op 14214: put
  db->Put(write_options, "key2266", "value51346");

  // op 14215: delete
  db->Delete(write_options, "key1609");

  // op 14216: get
  status = db->Get(read_options, "key8026", &value);
  if (status.ok()) {
    out << "GET key8026 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14217: get
  status = db->Get(read_options, "key8212", &value);
  if (status.ok()) {
    out << "GET key8212 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14218: put
  db->Put(write_options, "key8763", "value55500");

  // op 14219: put
  db->Put(write_options, "key11331", "value59308");

  // op 14220: put
  db->Put(write_options, "key6261", "value36526");

  // op 14221: delete
  db->Delete(write_options, "key9104");

  // op 14222: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 14223: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0507", "key8329", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0507, key8329) size=" << scan_result.size() << std::endl;
  }

  // op 14224: get
  status = db->Get(read_options, "key4401", &value);
  if (status.ok()) {
    out << "GET key4401 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14225: delete_range
  status = db->DeleteRange(write_options, "key4712", "key10440");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 14226: delete_range
  status = db->DeleteRange(write_options, "key7967", "key8865");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 14227: put
  db->Put(write_options, "key3844", "value80664");

  // op 14228: put
  db->Put(write_options, "key4539", "value32925");

  // op 14229: put
  db->Put(write_options, "key6724", "value106006");

  // op 14230: delete_range
  status = db->DeleteRange(write_options, "key0050", "key4281");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 14231: delete_range
  status = db->DeleteRange(write_options, "key3879", "key6178");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 14232: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6675", "key10638", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6675, key10638) size=" << scan_result.size() << std::endl;
  }

  // op 14233: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0138", "key10728", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0138, key10728) size=" << scan_result.size() << std::endl;
  }

  // op 14234: put
  db->Put(write_options, "key4073", "value43563");

  // op 14235: put
  db->Put(write_options, "key9266", "value85543");

  // op 14236: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4041", "key8366", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4041, key8366) size=" << scan_result.size() << std::endl;
  }

  // op 14237: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2604", "key7523", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2604, key7523) size=" << scan_result.size() << std::endl;
  }

  // op 14238: get
  status = db->Get(read_options, "key6891", &value);
  if (status.ok()) {
    out << "GET key6891 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14239: put
  db->Put(write_options, "key11095", "value104192");

  // op 14240: get
  status = db->Get(read_options, "key3932", &value);
  if (status.ok()) {
    out << "GET key3932 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14241: put
  db->Put(write_options, "key8394", "value108165");

  // op 14242: get
  status = db->Get(read_options, "key7711", &value);
  if (status.ok()) {
    out << "GET key7711 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14243: delete_range
  status = db->DeleteRange(write_options, "key4612", "key5402");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 14244: delete
  db->Delete(write_options, "key3919");

  // op 14245: put
  db->Put(write_options, "key4723", "value15980");

  // op 14246: delete
  db->Delete(write_options, "key2881");

  // op 14247: delete
  db->Delete(write_options, "key10361");

  // op 14248: put
  db->Put(write_options, "key9431", "value49506");

  // op 14249: put
  db->Put(write_options, "key4994", "value23143");

  // op 14250: put
  db->Put(write_options, "key4563", "value6129");

  // op 14251: put
  db->Put(write_options, "key2811", "value71801");

  // op 14252: get
  status = db->Get(read_options, "key3300", &value);
  if (status.ok()) {
    out << "GET key3300 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14253: put
  db->Put(write_options, "key3467", "value46566");

  // op 14254: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 14255: delete_range
  status = db->DeleteRange(write_options, "key4865", "key5252");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 14256: delete_range
  status = db->DeleteRange(write_options, "key8962", "key9706");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 14257: put
  db->Put(write_options, "key2439", "value37175");

  // op 14258: delete
  db->Delete(write_options, "key1919");

  // op 14259: put
  db->Put(write_options, "key11077", "value88753");

  // op 14260: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0038", "key1979", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0038, key1979) size=" << scan_result.size() << std::endl;
  }

  // op 14261: put
  db->Put(write_options, "key4273", "value86553");

  // op 14262: delete
  db->Delete(write_options, "key9449");

  // op 14263: get
  status = db->Get(read_options, "key1398", &value);
  if (status.ok()) {
    out << "GET key1398 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14264: delete
  db->Delete(write_options, "key0745");

  // op 14265: put
  db->Put(write_options, "key0281", "value48026");

  // op 14266: get
  status = db->Get(read_options, "key9637", &value);
  if (status.ok()) {
    out << "GET key9637 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14267: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5169", "key5348", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5169, key5348) size=" << scan_result.size() << std::endl;
  }

  // op 14268: get
  status = db->Get(read_options, "key8806", &value);
  if (status.ok()) {
    out << "GET key8806 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14269: get
  status = db->Get(read_options, "key1318", &value);
  if (status.ok()) {
    out << "GET key1318 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14270: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4934", "key11685", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4934, key11685) size=" << scan_result.size() << std::endl;
  }

  // op 14271: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0956", "key6558", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0956, key6558) size=" << scan_result.size() << std::endl;
  }

  // op 14272: get
  status = db->Get(read_options, "key7704", &value);
  if (status.ok()) {
    out << "GET key7704 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14273: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0698", "key1891", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0698, key1891) size=" << scan_result.size() << std::endl;
  }

  // op 14274: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4757", "key6054", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4757, key6054) size=" << scan_result.size() << std::endl;
  }

  // op 14275: put
  db->Put(write_options, "key7978", "value99287");

  // op 14276: put
  db->Put(write_options, "key9311", "value66341");

  // op 14277: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 14278: delete_range
  status = db->DeleteRange(write_options, "key0224", "key7739");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 14279: get
  status = db->Get(read_options, "key5105", &value);
  if (status.ok()) {
    out << "GET key5105 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14280: put
  db->Put(write_options, "key2138", "value100002");

  // op 14281: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4497", "key8067", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4497, key8067) size=" << scan_result.size() << std::endl;
  }

  // op 14282: get
  status = db->Get(read_options, "key11562", &value);
  if (status.ok()) {
    out << "GET key11562 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14283: put
  db->Put(write_options, "key5639", "value89167");

  // op 14284: put
  db->Put(write_options, "key6171", "value20156");

  // op 14285: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2630", "key3997", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2630, key3997) size=" << scan_result.size() << std::endl;
  }

  // op 14286: put
  db->Put(write_options, "key4186", "value15333");

  // op 14287: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 14288: put
  db->Put(write_options, "key11712", "value87395");

  // op 14289: delete_range
  status = db->DeleteRange(write_options, "key5093", "key9594");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 14290: get
  status = db->Get(read_options, "key8082", &value);
  if (status.ok()) {
    out << "GET key8082 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14291: delete
  db->Delete(write_options, "key5913");

  // op 14292: delete
  db->Delete(write_options, "key9303");

  // op 14293: put
  db->Put(write_options, "key4406", "value53419");

  // op 14294: delete_range
  status = db->DeleteRange(write_options, "key2668", "key4738");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 14295: put
  db->Put(write_options, "key2872", "value111586");

  // op 14296: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 14297: get
  status = db->Get(read_options, "key7994", &value);
  if (status.ok()) {
    out << "GET key7994 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14298: put
  db->Put(write_options, "key10137", "value5153");

  // op 14299: delete
  db->Delete(write_options, "key10597");

  // op 14300: get
  status = db->Get(read_options, "key0232", &value);
  if (status.ok()) {
    out << "GET key0232 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14301: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 14302: put
  db->Put(write_options, "key4826", "value113680");

  // op 14303: put
  db->Put(write_options, "key10974", "value80468");

  // op 14304: delete_range
  status = db->DeleteRange(write_options, "key4077", "key5860");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 14305: put
  db->Put(write_options, "key3438", "value106565");

  // op 14306: put
  db->Put(write_options, "key3150", "value75057");

  // op 14307: put
  db->Put(write_options, "key6859", "value39048");

  // op 14308: delete
  db->Delete(write_options, "key8198");

  // op 14309: put
  db->Put(write_options, "key4165", "value2028");

  // op 14310: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7676", "key11248", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7676, key11248) size=" << scan_result.size() << std::endl;
  }

  // op 14311: put
  db->Put(write_options, "key6533", "value0309");

  // op 14312: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3771", "key4191", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3771, key4191) size=" << scan_result.size() << std::endl;
  }

  // op 14313: put
  db->Put(write_options, "key11380", "value61619");

  // op 14314: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3687", "key11165", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3687, key11165) size=" << scan_result.size() << std::endl;
  }

  // op 14315: delete
  db->Delete(write_options, "key9547");

  // op 14316: get
  status = db->Get(read_options, "key3097", &value);
  if (status.ok()) {
    out << "GET key3097 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14317: put
  db->Put(write_options, "key0328", "value74846");

  // op 14318: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5572", "key6946", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5572, key6946) size=" << scan_result.size() << std::endl;
  }

  // op 14319: get
  status = db->Get(read_options, "key8783", &value);
  if (status.ok()) {
    out << "GET key8783 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14320: get
  status = db->Get(read_options, "key0302", &value);
  if (status.ok()) {
    out << "GET key0302 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14321: get
  status = db->Get(read_options, "key4955", &value);
  if (status.ok()) {
    out << "GET key4955 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14322: get
  status = db->Get(read_options, "key11633", &value);
  if (status.ok()) {
    out << "GET key11633 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14323: put
  db->Put(write_options, "key0000", "value13753");

  // op 14324: get
  status = db->Get(read_options, "key8623", &value);
  if (status.ok()) {
    out << "GET key8623 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14325: put
  db->Put(write_options, "key6923", "value91957");

  // op 14326: delete_range
  status = db->DeleteRange(write_options, "key6290", "key9795");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 14327: put
  db->Put(write_options, "key3380", "value61154");

  // op 14328: put
  db->Put(write_options, "key11021", "value62345");

  // op 14329: delete_range
  status = db->DeleteRange(write_options, "key4581", "key5690");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 14330: put
  db->Put(write_options, "key9714", "value79827");

  // op 14331: delete_range
  status = db->DeleteRange(write_options, "key1293", "key5703");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 14332: delete_range
  status = db->DeleteRange(write_options, "key2525", "key7725");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 14333: get
  status = db->Get(read_options, "key5554", &value);
  if (status.ok()) {
    out << "GET key5554 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14334: delete_range
  status = db->DeleteRange(write_options, "key3498", "key6260");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 14335: put
  db->Put(write_options, "key1966", "value99582");

  // op 14336: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5746", "key9545", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5746, key9545) size=" << scan_result.size() << std::endl;
  }

  // op 14337: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1470", "key4415", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1470, key4415) size=" << scan_result.size() << std::endl;
  }

  // op 14338: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2855", "key3619", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2855, key3619) size=" << scan_result.size() << std::endl;
  }

  // op 14339: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8817", "key11259", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8817, key11259) size=" << scan_result.size() << std::endl;
  }

  // op 14340: put
  db->Put(write_options, "key1739", "value100835");

  // op 14341: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4233", "key11117", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4233, key11117) size=" << scan_result.size() << std::endl;
  }

  // op 14342: delete
  db->Delete(write_options, "key11448");

  // op 14343: get
  status = db->Get(read_options, "key9539", &value);
  if (status.ok()) {
    out << "GET key9539 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14344: put
  db->Put(write_options, "key6061", "value97002");

  // op 14345: put
  db->Put(write_options, "key10109", "value70221");

  // op 14346: put
  db->Put(write_options, "key5214", "value70499");

  // op 14347: put
  db->Put(write_options, "key6656", "value62750");

  // op 14348: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0563", "key2320", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0563, key2320) size=" << scan_result.size() << std::endl;
  }

  // op 14349: get
  status = db->Get(read_options, "key10357", &value);
  if (status.ok()) {
    out << "GET key10357 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14350: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2981", "key6327", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2981, key6327) size=" << scan_result.size() << std::endl;
  }

  // op 14351: delete_range
  status = db->DeleteRange(write_options, "key0496", "key1348");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 14352: get
  status = db->Get(read_options, "key3877", &value);
  if (status.ok()) {
    out << "GET key3877 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14353: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1668", "key7221", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1668, key7221) size=" << scan_result.size() << std::endl;
  }

  // op 14354: delete_range
  status = db->DeleteRange(write_options, "key4661", "key10431");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 14355: delete
  db->Delete(write_options, "key9642");

  // op 14356: put
  db->Put(write_options, "key11491", "value64650");

  // op 14357: get
  status = db->Get(read_options, "key7114", &value);
  if (status.ok()) {
    out << "GET key7114 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14358: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1636", "key10422", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1636, key10422) size=" << scan_result.size() << std::endl;
  }

  // op 14359: put
  db->Put(write_options, "key7415", "value71216");

  // op 14360: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 14361: get
  status = db->Get(read_options, "key7378", &value);
  if (status.ok()) {
    out << "GET key7378 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14362: delete_range
  status = db->DeleteRange(write_options, "key4459", "key9430");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 14363: put
  db->Put(write_options, "key11455", "value41156");

  // op 14364: get
  status = db->Get(read_options, "key0347", &value);
  if (status.ok()) {
    out << "GET key0347 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14365: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 14366: put
  db->Put(write_options, "key11122", "value11259");

  // op 14367: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3982", "key8358", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3982, key8358) size=" << scan_result.size() << std::endl;
  }

  // op 14368: put
  db->Put(write_options, "key0494", "value31572");

  // op 14369: delete
  db->Delete(write_options, "key9907");

  // op 14370: delete
  db->Delete(write_options, "key5709");

  // op 14371: delete_range
  status = db->DeleteRange(write_options, "key5134", "key8132");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 14372: put
  db->Put(write_options, "key10571", "value37066");

  // op 14373: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3584", "key5029", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3584, key5029) size=" << scan_result.size() << std::endl;
  }

  // op 14374: delete_range
  status = db->DeleteRange(write_options, "key1437", "key10305");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 14375: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2697", "key8900", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2697, key8900) size=" << scan_result.size() << std::endl;
  }

  // op 14376: delete
  db->Delete(write_options, "key0004");

  // op 14377: get
  status = db->Get(read_options, "key1676", &value);
  if (status.ok()) {
    out << "GET key1676 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14378: put
  db->Put(write_options, "key4160", "value38516");

  // op 14379: get
  status = db->Get(read_options, "key8937", &value);
  if (status.ok()) {
    out << "GET key8937 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14380: put
  db->Put(write_options, "key11628", "value96217");

  // op 14381: delete
  db->Delete(write_options, "key6069");

  // op 14382: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9478", "key10182", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9478, key10182) size=" << scan_result.size() << std::endl;
  }

  // op 14383: delete_range
  status = db->DeleteRange(write_options, "key1041", "key7032");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 14384: get
  status = db->Get(read_options, "key5733", &value);
  if (status.ok()) {
    out << "GET key5733 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14385: get
  status = db->Get(read_options, "key3178", &value);
  if (status.ok()) {
    out << "GET key3178 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14386: put
  db->Put(write_options, "key5086", "value1185");

  // op 14387: delete_range
  status = db->DeleteRange(write_options, "key10128", "key10144");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 14388: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 14389: get
  status = db->Get(read_options, "key9643", &value);
  if (status.ok()) {
    out << "GET key9643 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14390: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10198", "key10335", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10198, key10335) size=" << scan_result.size() << std::endl;
  }

  // op 14391: put
  db->Put(write_options, "key10737", "value97862");

  // op 14392: get
  status = db->Get(read_options, "key6396", &value);
  if (status.ok()) {
    out << "GET key6396 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14393: put
  db->Put(write_options, "key6609", "value72427");

  // op 14394: put
  db->Put(write_options, "key2830", "value80977");

  // op 14395: delete_range
  status = db->DeleteRange(write_options, "key1556", "key6134");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 14396: put
  db->Put(write_options, "key7269", "value102120");

  // op 14397: delete
  db->Delete(write_options, "key4216");

  // op 14398: delete
  db->Delete(write_options, "key9748");

  // op 14399: put
  db->Put(write_options, "key7598", "value17136");

  // op 14400: put
  db->Put(write_options, "key10492", "value46190");

  // op 14401: put
  db->Put(write_options, "key10963", "value19014");

  // op 14402: delete
  db->Delete(write_options, "key1810");

  // op 14403: put
  db->Put(write_options, "key3024", "value51103");

  // op 14404: put
  db->Put(write_options, "key3810", "value105302");

  // op 14405: put
  db->Put(write_options, "key10658", "value35785");

  // op 14406: put
  db->Put(write_options, "key7996", "value33453");

  // op 14407: put
  db->Put(write_options, "key8991", "value79171");

  // op 14408: delete_range
  status = db->DeleteRange(write_options, "key6054", "key10886");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 14409: get
  status = db->Get(read_options, "key9682", &value);
  if (status.ok()) {
    out << "GET key9682 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14410: put
  db->Put(write_options, "key10224", "value95471");

  // op 14411: put
  db->Put(write_options, "key6594", "value48353");

  // op 14412: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 14413: delete
  db->Delete(write_options, "key9403");

  // op 14414: put
  db->Put(write_options, "key4521", "value4377");

  // op 14415: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0589", "key9358", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0589, key9358) size=" << scan_result.size() << std::endl;
  }

  // op 14416: put
  db->Put(write_options, "key8763", "value115708");

  // op 14417: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6858", "key8294", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6858, key8294) size=" << scan_result.size() << std::endl;
  }

  // op 14418: put
  db->Put(write_options, "key6210", "value26836");

  // op 14419: put
  db->Put(write_options, "key3202", "value86563");

  // op 14420: get
  status = db->Get(read_options, "key7673", &value);
  if (status.ok()) {
    out << "GET key7673 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14421: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3910", "key4755", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3910, key4755) size=" << scan_result.size() << std::endl;
  }

  // op 14422: get
  status = db->Get(read_options, "key3330", &value);
  if (status.ok()) {
    out << "GET key3330 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14423: put
  db->Put(write_options, "key5417", "value34184");

  // op 14424: put
  db->Put(write_options, "key4326", "value114303");

  // op 14425: put
  db->Put(write_options, "key5568", "value42488");

  // op 14426: put
  db->Put(write_options, "key2698", "value43849");

  // op 14427: delete
  db->Delete(write_options, "key4017");

  // op 14428: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3946", "key7964", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3946, key7964) size=" << scan_result.size() << std::endl;
  }

  // op 14429: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2905", "key8204", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2905, key8204) size=" << scan_result.size() << std::endl;
  }

  // op 14430: get
  status = db->Get(read_options, "key6612", &value);
  if (status.ok()) {
    out << "GET key6612 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14431: get
  status = db->Get(read_options, "key5901", &value);
  if (status.ok()) {
    out << "GET key5901 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14432: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4811", "key9880", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4811, key9880) size=" << scan_result.size() << std::endl;
  }

  // op 14433: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0648", "key5760", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0648, key5760) size=" << scan_result.size() << std::endl;
  }

  // op 14434: put
  db->Put(write_options, "key4011", "value112879");

  // op 14435: put
  db->Put(write_options, "key3827", "value21791");

  // op 14436: put
  db->Put(write_options, "key6200", "value92055");

  // op 14437: put
  db->Put(write_options, "key0360", "value62861");

  // op 14438: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 14439: put
  db->Put(write_options, "key1828", "value69912");

  // op 14440: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 14441: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3908", "key4489", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3908, key4489) size=" << scan_result.size() << std::endl;
  }

  // op 14442: get
  status = db->Get(read_options, "key7796", &value);
  if (status.ok()) {
    out << "GET key7796 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14443: get
  status = db->Get(read_options, "key7940", &value);
  if (status.ok()) {
    out << "GET key7940 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14444: get
  status = db->Get(read_options, "key3543", &value);
  if (status.ok()) {
    out << "GET key3543 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14445: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7701", "key8908", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7701, key8908) size=" << scan_result.size() << std::endl;
  }

  // op 14446: delete_range
  status = db->DeleteRange(write_options, "key0944", "key2606");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 14447: put
  db->Put(write_options, "key0665", "value18218");

  // op 14448: put
  db->Put(write_options, "key2155", "value99193");

  // op 14449: delete_range
  status = db->DeleteRange(write_options, "key1404", "key11630");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 14450: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4372", "key8441", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4372, key8441) size=" << scan_result.size() << std::endl;
  }

  // op 14451: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10364", "key11101", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10364, key11101) size=" << scan_result.size() << std::endl;
  }

  // op 14452: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5206", "key5285", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5206, key5285) size=" << scan_result.size() << std::endl;
  }

  // op 14453: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0933", "key1931", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0933, key1931) size=" << scan_result.size() << std::endl;
  }

  // op 14454: put
  db->Put(write_options, "key9498", "value15174");

  // op 14455: get
  status = db->Get(read_options, "key9640", &value);
  if (status.ok()) {
    out << "GET key9640 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14456: delete
  db->Delete(write_options, "key2647");

  // op 14457: get
  status = db->Get(read_options, "key6077", &value);
  if (status.ok()) {
    out << "GET key6077 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14458: put
  db->Put(write_options, "key2425", "value65441");

  // op 14459: put
  db->Put(write_options, "key3402", "value21756");

  // op 14460: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2954", "key5627", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2954, key5627) size=" << scan_result.size() << std::endl;
  }

  // op 14461: get
  status = db->Get(read_options, "key11008", &value);
  if (status.ok()) {
    out << "GET key11008 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14462: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6366", "key6723", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6366, key6723) size=" << scan_result.size() << std::endl;
  }

  // op 14463: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9073", "key10589", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9073, key10589) size=" << scan_result.size() << std::endl;
  }

  // op 14464: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1013", "key6150", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1013, key6150) size=" << scan_result.size() << std::endl;
  }

  // op 14465: put
  db->Put(write_options, "key2056", "value64127");

  // op 14466: get
  status = db->Get(read_options, "key1680", &value);
  if (status.ok()) {
    out << "GET key1680 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14467: put
  db->Put(write_options, "key1002", "value104620");

  // op 14468: get
  status = db->Get(read_options, "key9994", &value);
  if (status.ok()) {
    out << "GET key9994 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14469: put
  db->Put(write_options, "key6924", "value106977");

  // op 14470: put
  db->Put(write_options, "key4772", "value77873");

  // op 14471: put
  db->Put(write_options, "key11725", "value95653");

  // op 14472: put
  db->Put(write_options, "key4188", "value110573");

  // op 14473: put
  db->Put(write_options, "key11698", "value45402");

  // op 14474: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3484", "key10791", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3484, key10791) size=" << scan_result.size() << std::endl;
  }

  // op 14475: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 14476: get
  status = db->Get(read_options, "key8488", &value);
  if (status.ok()) {
    out << "GET key8488 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14477: get
  status = db->Get(read_options, "key5750", &value);
  if (status.ok()) {
    out << "GET key5750 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14478: put
  db->Put(write_options, "key6327", "value99497");

  // op 14479: delete_range
  status = db->DeleteRange(write_options, "key2337", "key2603");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 14480: get
  status = db->Get(read_options, "key2946", &value);
  if (status.ok()) {
    out << "GET key2946 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14481: delete_range
  status = db->DeleteRange(write_options, "key2765", "key10282");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 14482: delete
  db->Delete(write_options, "key6127");

  // op 14483: get
  status = db->Get(read_options, "key3102", &value);
  if (status.ok()) {
    out << "GET key3102 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14484: put
  db->Put(write_options, "key3447", "value7389");

  // op 14485: put
  db->Put(write_options, "key11557", "value68256");

  // op 14486: get
  status = db->Get(read_options, "key1419", &value);
  if (status.ok()) {
    out << "GET key1419 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14487: delete_range
  status = db->DeleteRange(write_options, "key7840", "key11246");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 14488: get
  status = db->Get(read_options, "key4240", &value);
  if (status.ok()) {
    out << "GET key4240 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14489: delete
  db->Delete(write_options, "key6758");

  // op 14490: put
  db->Put(write_options, "key5637", "value13367");

  // op 14491: put
  db->Put(write_options, "key6704", "value27126");

  // op 14492: delete_range
  status = db->DeleteRange(write_options, "key4768", "key8858");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 14493: delete
  db->Delete(write_options, "key4435");

  // op 14494: put
  db->Put(write_options, "key11414", "value38219");

  // op 14495: put
  db->Put(write_options, "key2970", "value81922");

  // op 14496: put
  db->Put(write_options, "key0592", "value70586");

  // op 14497: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4106", "key8570", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4106, key8570) size=" << scan_result.size() << std::endl;
  }

  // op 14498: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 14499: delete
  db->Delete(write_options, "key6330");

  // op 14500: delete_range
  status = db->DeleteRange(write_options, "key0301", "key8901");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 14501: delete
  db->Delete(write_options, "key1457");

  // op 14502: delete
  db->Delete(write_options, "key2482");

  // op 14503: put
  db->Put(write_options, "key7023", "value85884");

  // op 14504: delete_range
  status = db->DeleteRange(write_options, "key2880", "key6018");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 14505: delete
  db->Delete(write_options, "key3582");

  // op 14506: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4934", "key6875", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4934, key6875) size=" << scan_result.size() << std::endl;
  }

  // op 14507: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1752", "key7642", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1752, key7642) size=" << scan_result.size() << std::endl;
  }

  // op 14508: put
  db->Put(write_options, "key7022", "value66868");

  // op 14509: put
  db->Put(write_options, "key11160", "value62829");

  // op 14510: get
  status = db->Get(read_options, "key2374", &value);
  if (status.ok()) {
    out << "GET key2374 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14511: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4696", "key9528", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4696, key9528) size=" << scan_result.size() << std::endl;
  }

  // op 14512: put
  db->Put(write_options, "key10423", "value101947");

  // op 14513: put
  db->Put(write_options, "key0225", "value100726");

  // op 14514: put
  db->Put(write_options, "key4458", "value117666");

  // op 14515: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1577", "key9375", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1577, key9375) size=" << scan_result.size() << std::endl;
  }

  // op 14516: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6623", "key8295", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6623, key8295) size=" << scan_result.size() << std::endl;
  }

  // op 14517: delete
  db->Delete(write_options, "key1235");

  // op 14518: put
  db->Put(write_options, "key0360", "value86451");

  // op 14519: put
  db->Put(write_options, "key4951", "value49078");

  // op 14520: get
  status = db->Get(read_options, "key1137", &value);
  if (status.ok()) {
    out << "GET key1137 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14521: put
  db->Put(write_options, "key7093", "value0832");

  // op 14522: get
  status = db->Get(read_options, "key9253", &value);
  if (status.ok()) {
    out << "GET key9253 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14523: put
  db->Put(write_options, "key5807", "value25613");

  // op 14524: get
  status = db->Get(read_options, "key8463", &value);
  if (status.ok()) {
    out << "GET key8463 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14525: delete_range
  status = db->DeleteRange(write_options, "key0568", "key1788");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 14526: put
  db->Put(write_options, "key3211", "value71688");

  // op 14527: get
  status = db->Get(read_options, "key6329", &value);
  if (status.ok()) {
    out << "GET key6329 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14528: put
  db->Put(write_options, "key0498", "value24038");

  // op 14529: get
  status = db->Get(read_options, "key6408", &value);
  if (status.ok()) {
    out << "GET key6408 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14530: put
  db->Put(write_options, "key4190", "value57092");

  // op 14531: get
  status = db->Get(read_options, "key5126", &value);
  if (status.ok()) {
    out << "GET key5126 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14532: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0754", "key10717", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0754, key10717) size=" << scan_result.size() << std::endl;
  }

  // op 14533: put
  db->Put(write_options, "key3237", "value100539");

  // op 14534: delete
  db->Delete(write_options, "key7837");

  // op 14535: get
  status = db->Get(read_options, "key11262", &value);
  if (status.ok()) {
    out << "GET key11262 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14536: get
  status = db->Get(read_options, "key0804", &value);
  if (status.ok()) {
    out << "GET key0804 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14537: put
  db->Put(write_options, "key6685", "value76654");

  // op 14538: get
  status = db->Get(read_options, "key9503", &value);
  if (status.ok()) {
    out << "GET key9503 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14539: delete_range
  status = db->DeleteRange(write_options, "key4174", "key10462");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 14540: put
  db->Put(write_options, "key5848", "value18393");

  // op 14541: put
  db->Put(write_options, "key10187", "value46612");

  // op 14542: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 14543: put
  db->Put(write_options, "key10174", "value115059");

  // op 14544: put
  db->Put(write_options, "key3673", "value55469");

  // op 14545: put
  db->Put(write_options, "key7875", "value99859");

  // op 14546: put
  db->Put(write_options, "key10226", "value42587");

  // op 14547: put
  db->Put(write_options, "key8942", "value3612");

  // op 14548: get
  status = db->Get(read_options, "key0589", &value);
  if (status.ok()) {
    out << "GET key0589 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14549: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0719", "key6634", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0719, key6634) size=" << scan_result.size() << std::endl;
  }

  // op 14550: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 14551: put
  db->Put(write_options, "key10738", "value84920");

  // op 14552: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 14553: delete_range
  status = db->DeleteRange(write_options, "key0749", "key2327");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 14554: delete_range
  status = db->DeleteRange(write_options, "key1438", "key4162");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 14555: put
  db->Put(write_options, "key6572", "value50171");

  // op 14556: get
  status = db->Get(read_options, "key11217", &value);
  if (status.ok()) {
    out << "GET key11217 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14557: delete_range
  status = db->DeleteRange(write_options, "key5728", "key11622");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 14558: get
  status = db->Get(read_options, "key10230", &value);
  if (status.ok()) {
    out << "GET key10230 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14559: put
  db->Put(write_options, "key1894", "value67723");

  // op 14560: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 14561: get
  status = db->Get(read_options, "key4312", &value);
  if (status.ok()) {
    out << "GET key4312 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14562: get
  status = db->Get(read_options, "key8638", &value);
  if (status.ok()) {
    out << "GET key8638 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14563: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8601", "key9089", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8601, key9089) size=" << scan_result.size() << std::endl;
  }

  // op 14564: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4380", "key9643", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4380, key9643) size=" << scan_result.size() << std::endl;
  }

  // op 14565: delete_range
  status = db->DeleteRange(write_options, "key1541", "key7767");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 14566: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1953", "key10647", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1953, key10647) size=" << scan_result.size() << std::endl;
  }

  // op 14567: put
  db->Put(write_options, "key10714", "value62019");

  // op 14568: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0434", "key8164", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0434, key8164) size=" << scan_result.size() << std::endl;
  }

  // op 14569: get
  status = db->Get(read_options, "key0574", &value);
  if (status.ok()) {
    out << "GET key0574 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14570: get
  status = db->Get(read_options, "key11610", &value);
  if (status.ok()) {
    out << "GET key11610 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14571: put
  db->Put(write_options, "key10692", "value106560");

  // op 14572: put
  db->Put(write_options, "key9389", "value11200");

  // op 14573: put
  db->Put(write_options, "key8519", "value103171");

  // op 14574: get
  status = db->Get(read_options, "key10177", &value);
  if (status.ok()) {
    out << "GET key10177 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14575: put
  db->Put(write_options, "key9425", "value77452");

  // op 14576: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 14577: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3094", "key9329", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3094, key9329) size=" << scan_result.size() << std::endl;
  }

  // op 14578: get
  status = db->Get(read_options, "key11693", &value);
  if (status.ok()) {
    out << "GET key11693 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14579: delete
  db->Delete(write_options, "key4209");

  // op 14580: put
  db->Put(write_options, "key4495", "value27936");

  // op 14581: put
  db->Put(write_options, "key0362", "value53118");

  // op 14582: put
  db->Put(write_options, "key8112", "value1028");

  // op 14583: delete_range
  status = db->DeleteRange(write_options, "key6321", "key11214");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 14584: get
  status = db->Get(read_options, "key3740", &value);
  if (status.ok()) {
    out << "GET key3740 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14585: put
  db->Put(write_options, "key9943", "value37445");

  // op 14586: put
  db->Put(write_options, "key6800", "value110366");

  // op 14587: put
  db->Put(write_options, "key9131", "value27805");

  // op 14588: put
  db->Put(write_options, "key1020", "value63348");

  // op 14589: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0881", "key7555", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0881, key7555) size=" << scan_result.size() << std::endl;
  }

  // op 14590: get
  status = db->Get(read_options, "key1131", &value);
  if (status.ok()) {
    out << "GET key1131 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14591: get
  status = db->Get(read_options, "key4127", &value);
  if (status.ok()) {
    out << "GET key4127 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14592: put
  db->Put(write_options, "key4392", "value47921");

  // op 14593: delete
  db->Delete(write_options, "key11680");

  // op 14594: get
  status = db->Get(read_options, "key6300", &value);
  if (status.ok()) {
    out << "GET key6300 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14595: put
  db->Put(write_options, "key6143", "value40041");

  // op 14596: put
  db->Put(write_options, "key7810", "value50057");

  // op 14597: delete
  db->Delete(write_options, "key3067");

  // op 14598: delete_range
  status = db->DeleteRange(write_options, "key0173", "key5978");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 14599: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3754", "key10244", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3754, key10244) size=" << scan_result.size() << std::endl;
  }

  // op 14600: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0124", "key7101", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0124, key7101) size=" << scan_result.size() << std::endl;
  }

  // op 14601: get
  status = db->Get(read_options, "key4851", &value);
  if (status.ok()) {
    out << "GET key4851 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14602: delete_range
  status = db->DeleteRange(write_options, "key0817", "key3647");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 14603: get
  status = db->Get(read_options, "key8347", &value);
  if (status.ok()) {
    out << "GET key8347 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14604: put
  db->Put(write_options, "key4451", "value114969");

  // op 14605: delete
  db->Delete(write_options, "key5875");

  // op 14606: put
  db->Put(write_options, "key8326", "value55912");

  // op 14607: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4985", "key5438", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4985, key5438) size=" << scan_result.size() << std::endl;
  }

  // op 14608: get
  status = db->Get(read_options, "key11654", &value);
  if (status.ok()) {
    out << "GET key11654 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14609: put
  db->Put(write_options, "key10612", "value89080");

  // op 14610: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1481", "key9529", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1481, key9529) size=" << scan_result.size() << std::endl;
  }

  // op 14611: delete
  db->Delete(write_options, "key3296");

  // op 14612: put
  db->Put(write_options, "key0137", "value50415");

  // op 14613: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 14614: put
  db->Put(write_options, "key11541", "value2994");

  // op 14615: get
  status = db->Get(read_options, "key5701", &value);
  if (status.ok()) {
    out << "GET key5701 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14616: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2886", "key11415", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2886, key11415) size=" << scan_result.size() << std::endl;
  }

  // op 14617: get
  status = db->Get(read_options, "key1906", &value);
  if (status.ok()) {
    out << "GET key1906 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14618: put
  db->Put(write_options, "key1496", "value102690");

  // op 14619: put
  db->Put(write_options, "key1898", "value66121");

  // op 14620: get
  status = db->Get(read_options, "key5982", &value);
  if (status.ok()) {
    out << "GET key5982 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14621: put
  db->Put(write_options, "key2619", "value111183");

  // op 14622: delete
  db->Delete(write_options, "key8833");

  // op 14623: put
  db->Put(write_options, "key11537", "value101141");

  // op 14624: get
  status = db->Get(read_options, "key2983", &value);
  if (status.ok()) {
    out << "GET key2983 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14625: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3650", "key5390", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3650, key5390) size=" << scan_result.size() << std::endl;
  }

  // op 14626: put
  db->Put(write_options, "key8725", "value36054");

  // op 14627: delete
  db->Delete(write_options, "key1256");

  // op 14628: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1174", "key9354", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1174, key9354) size=" << scan_result.size() << std::endl;
  }

  // op 14629: put
  db->Put(write_options, "key7879", "value49007");

  // op 14630: put
  db->Put(write_options, "key5559", "value70079");

  // op 14631: put
  db->Put(write_options, "key8609", "value99231");

  // op 14632: get
  status = db->Get(read_options, "key6132", &value);
  if (status.ok()) {
    out << "GET key6132 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14633: delete_range
  status = db->DeleteRange(write_options, "key10267", "key11654");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 14634: get
  status = db->Get(read_options, "key3229", &value);
  if (status.ok()) {
    out << "GET key3229 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14635: put
  db->Put(write_options, "key2997", "value36191");

  // op 14636: put
  db->Put(write_options, "key8044", "value23230");

  // op 14637: get
  status = db->Get(read_options, "key5180", &value);
  if (status.ok()) {
    out << "GET key5180 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14638: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3167", "key7100", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3167, key7100) size=" << scan_result.size() << std::endl;
  }

  // op 14639: put
  db->Put(write_options, "key7003", "value19524");

  // op 14640: get
  status = db->Get(read_options, "key10147", &value);
  if (status.ok()) {
    out << "GET key10147 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14641: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4573", "key5605", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4573, key5605) size=" << scan_result.size() << std::endl;
  }

  // op 14642: put
  db->Put(write_options, "key5382", "value115160");

  // op 14643: put
  db->Put(write_options, "key2185", "value8603");

  // op 14644: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 14645: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0702", "key1129", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0702, key1129) size=" << scan_result.size() << std::endl;
  }

  // op 14646: get
  status = db->Get(read_options, "key8077", &value);
  if (status.ok()) {
    out << "GET key8077 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14647: put
  db->Put(write_options, "key1028", "value93237");

  // op 14648: put
  db->Put(write_options, "key2743", "value21857");

  // op 14649: put
  db->Put(write_options, "key9379", "value31757");

  // op 14650: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1083", "key2152", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1083, key2152) size=" << scan_result.size() << std::endl;
  }

  // op 14651: put
  db->Put(write_options, "key5588", "value68359");

  // op 14652: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3991", "key9263", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3991, key9263) size=" << scan_result.size() << std::endl;
  }

  // op 14653: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2521", "key9531", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2521, key9531) size=" << scan_result.size() << std::endl;
  }

  // op 14654: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1759", "key10298", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1759, key10298) size=" << scan_result.size() << std::endl;
  }

  // op 14655: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 14656: put
  db->Put(write_options, "key3299", "value52490");

  // op 14657: delete
  db->Delete(write_options, "key5367");

  // op 14658: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3318", "key9276", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3318, key9276) size=" << scan_result.size() << std::endl;
  }

  // op 14659: put
  db->Put(write_options, "key1854", "value6080");

  // op 14660: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 14661: get
  status = db->Get(read_options, "key11224", &value);
  if (status.ok()) {
    out << "GET key11224 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14662: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 14663: get
  status = db->Get(read_options, "key3802", &value);
  if (status.ok()) {
    out << "GET key3802 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14664: put
  db->Put(write_options, "key2545", "value22318");

  // op 14665: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2083", "key7708", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2083, key7708) size=" << scan_result.size() << std::endl;
  }

  // op 14666: get
  status = db->Get(read_options, "key3670", &value);
  if (status.ok()) {
    out << "GET key3670 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14667: put
  db->Put(write_options, "key5980", "value51007");

  // op 14668: delete
  db->Delete(write_options, "key4794");

  // op 14669: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1424", "key9939", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1424, key9939) size=" << scan_result.size() << std::endl;
  }

  // op 14670: put
  db->Put(write_options, "key6614", "value52589");

  // op 14671: delete_range
  status = db->DeleteRange(write_options, "key0253", "key9192");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 14672: put
  db->Put(write_options, "key8443", "value90039");

  // op 14673: put
  db->Put(write_options, "key2343", "value18480");

  // op 14674: put
  db->Put(write_options, "key9704", "value26390");

  // op 14675: get
  status = db->Get(read_options, "key7676", &value);
  if (status.ok()) {
    out << "GET key7676 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14676: put
  db->Put(write_options, "key10330", "value111735");

  // op 14677: delete
  db->Delete(write_options, "key7879");

  // op 14678: get
  status = db->Get(read_options, "key7224", &value);
  if (status.ok()) {
    out << "GET key7224 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14679: delete
  db->Delete(write_options, "key8586");

  // op 14680: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0277", "key10192", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0277, key10192) size=" << scan_result.size() << std::endl;
  }

  // op 14681: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4596", "key8561", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4596, key8561) size=" << scan_result.size() << std::endl;
  }

  // op 14682: delete_range
  status = db->DeleteRange(write_options, "key3911", "key7107");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 14683: put
  db->Put(write_options, "key1380", "value53453");

  // op 14684: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0878", "key2114", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0878, key2114) size=" << scan_result.size() << std::endl;
  }

  // op 14685: put
  db->Put(write_options, "key5964", "value34246");

  // op 14686: put
  db->Put(write_options, "key1983", "value109500");

  // op 14687: get
  status = db->Get(read_options, "key2991", &value);
  if (status.ok()) {
    out << "GET key2991 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14688: put
  db->Put(write_options, "key6167", "value81719");

  // op 14689: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4222", "key5545", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4222, key5545) size=" << scan_result.size() << std::endl;
  }

  // op 14690: put
  db->Put(write_options, "key4439", "value100922");

  // op 14691: put
  db->Put(write_options, "key2359", "value1446");

  // op 14692: put
  db->Put(write_options, "key8876", "value27587");

  // op 14693: put
  db->Put(write_options, "key11303", "value0369");

  // op 14694: put
  db->Put(write_options, "key7252", "value80207");

  // op 14695: put
  db->Put(write_options, "key4462", "value6252");

  // op 14696: delete_range
  status = db->DeleteRange(write_options, "key7610", "key8094");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 14697: put
  db->Put(write_options, "key9358", "value103455");

  // op 14698: delete
  db->Delete(write_options, "key7551");

  // op 14699: put
  db->Put(write_options, "key9050", "value16869");

  // op 14700: put
  db->Put(write_options, "key0149", "value43878");

  // op 14701: delete_range
  status = db->DeleteRange(write_options, "key1851", "key2897");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 14702: put
  db->Put(write_options, "key1195", "value35832");

  // op 14703: delete
  db->Delete(write_options, "key1654");

  // op 14704: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3294", "key11013", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3294, key11013) size=" << scan_result.size() << std::endl;
  }

  // op 14705: get
  status = db->Get(read_options, "key4358", &value);
  if (status.ok()) {
    out << "GET key4358 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14706: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 14707: put
  db->Put(write_options, "key2998", "value69557");

  // op 14708: put
  db->Put(write_options, "key4411", "value30814");

  // op 14709: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4760", "key7662", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4760, key7662) size=" << scan_result.size() << std::endl;
  }

  // op 14710: get
  status = db->Get(read_options, "key10947", &value);
  if (status.ok()) {
    out << "GET key10947 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14711: put
  db->Put(write_options, "key8732", "value50030");

  // op 14712: put
  db->Put(write_options, "key3037", "value85487");

  // op 14713: put
  db->Put(write_options, "key10315", "value52414");

  // op 14714: put
  db->Put(write_options, "key10937", "value7328");

  // op 14715: delete
  db->Delete(write_options, "key2217");

  // op 14716: delete_range
  status = db->DeleteRange(write_options, "key3037", "key7787");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 14717: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0326", "key1512", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0326, key1512) size=" << scan_result.size() << std::endl;
  }

  // op 14718: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 14719: put
  db->Put(write_options, "key0440", "value79262");

  // op 14720: get
  status = db->Get(read_options, "key0704", &value);
  if (status.ok()) {
    out << "GET key0704 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14721: put
  db->Put(write_options, "key5629", "value84169");

  // op 14722: put
  db->Put(write_options, "key8011", "value13785");

  // op 14723: put
  db->Put(write_options, "key11158", "value114465");

  // op 14724: put
  db->Put(write_options, "key0918", "value3616");

  // op 14725: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 14726: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4294", "key5764", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4294, key5764) size=" << scan_result.size() << std::endl;
  }

  // op 14727: put
  db->Put(write_options, "key1868", "value104248");

  // op 14728: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 14729: put
  db->Put(write_options, "key4949", "value116546");

  // op 14730: get
  status = db->Get(read_options, "key3120", &value);
  if (status.ok()) {
    out << "GET key3120 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14731: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7100", "key9509", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7100, key9509) size=" << scan_result.size() << std::endl;
  }

  // op 14732: put
  db->Put(write_options, "key2600", "value15820");

  // op 14733: put
  db->Put(write_options, "key7443", "value8672");

  // op 14734: get
  status = db->Get(read_options, "key4676", &value);
  if (status.ok()) {
    out << "GET key4676 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14735: delete_range
  status = db->DeleteRange(write_options, "key6107", "key6354");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 14736: put
  db->Put(write_options, "key0442", "value1239");

  // op 14737: put
  db->Put(write_options, "key8726", "value65667");

  // op 14738: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7381", "key11720", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7381, key11720) size=" << scan_result.size() << std::endl;
  }

  // op 14739: put
  db->Put(write_options, "key8848", "value78050");

  // op 14740: get
  status = db->Get(read_options, "key1159", &value);
  if (status.ok()) {
    out << "GET key1159 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14741: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2395", "key10863", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2395, key10863) size=" << scan_result.size() << std::endl;
  }

  // op 14742: put
  db->Put(write_options, "key5707", "value80031");

  // op 14743: put
  db->Put(write_options, "key1104", "value47945");

  // op 14744: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5674", "key8588", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5674, key8588) size=" << scan_result.size() << std::endl;
  }

  // op 14745: put
  db->Put(write_options, "key0180", "value2153");

  // op 14746: delete_range
  status = db->DeleteRange(write_options, "key7292", "key8420");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 14747: put
  db->Put(write_options, "key7454", "value33883");

  // op 14748: get
  status = db->Get(read_options, "key10968", &value);
  if (status.ok()) {
    out << "GET key10968 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14749: get
  status = db->Get(read_options, "key2632", &value);
  if (status.ok()) {
    out << "GET key2632 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14750: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1569", "key9394", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1569, key9394) size=" << scan_result.size() << std::endl;
  }

  // op 14751: get
  status = db->Get(read_options, "key8484", &value);
  if (status.ok()) {
    out << "GET key8484 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14752: get
  status = db->Get(read_options, "key8836", &value);
  if (status.ok()) {
    out << "GET key8836 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14753: put
  db->Put(write_options, "key6620", "value47709");

  // op 14754: get
  status = db->Get(read_options, "key2844", &value);
  if (status.ok()) {
    out << "GET key2844 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14755: put
  db->Put(write_options, "key1883", "value113517");

  // op 14756: delete_range
  status = db->DeleteRange(write_options, "key1113", "key5728");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 14757: put
  db->Put(write_options, "key5598", "value9670");

  // op 14758: put
  db->Put(write_options, "key2553", "value67466");

  // op 14759: get
  status = db->Get(read_options, "key8163", &value);
  if (status.ok()) {
    out << "GET key8163 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14760: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0977", "key1448", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0977, key1448) size=" << scan_result.size() << std::endl;
  }

  // op 14761: delete_range
  status = db->DeleteRange(write_options, "key1952", "key7096");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 14762: delete
  db->Delete(write_options, "key0305");

  // op 14763: put
  db->Put(write_options, "key3939", "value20662");

  // op 14764: get
  status = db->Get(read_options, "key10978", &value);
  if (status.ok()) {
    out << "GET key10978 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14765: put
  db->Put(write_options, "key8516", "value88913");

  // op 14766: put
  db->Put(write_options, "key4221", "value78122");

  // op 14767: delete
  db->Delete(write_options, "key10194");

  // op 14768: get
  status = db->Get(read_options, "key3564", &value);
  if (status.ok()) {
    out << "GET key3564 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14769: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2755", "key10534", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2755, key10534) size=" << scan_result.size() << std::endl;
  }

  // op 14770: put
  db->Put(write_options, "key1155", "value41568");

  // op 14771: get
  status = db->Get(read_options, "key8965", &value);
  if (status.ok()) {
    out << "GET key8965 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14772: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2595", "key7258", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2595, key7258) size=" << scan_result.size() << std::endl;
  }

  // op 14773: put
  db->Put(write_options, "key8071", "value107492");

  // op 14774: delete_range
  status = db->DeleteRange(write_options, "key4677", "key6818");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 14775: put
  db->Put(write_options, "key2426", "value78562");

  // op 14776: put
  db->Put(write_options, "key5473", "value67181");

  // op 14777: put
  db->Put(write_options, "key11320", "value54464");

  // op 14778: get
  status = db->Get(read_options, "key9346", &value);
  if (status.ok()) {
    out << "GET key9346 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14779: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4893", "key7933", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4893, key7933) size=" << scan_result.size() << std::endl;
  }

  // op 14780: delete_range
  status = db->DeleteRange(write_options, "key4666", "key7443");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 14781: delete_range
  status = db->DeleteRange(write_options, "key0785", "key1571");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 14782: put
  db->Put(write_options, "key9158", "value69585");

  // op 14783: get
  status = db->Get(read_options, "key0205", &value);
  if (status.ok()) {
    out << "GET key0205 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14784: put
  db->Put(write_options, "key4414", "value41844");

  // op 14785: put
  db->Put(write_options, "key7183", "value4507");

  // op 14786: delete
  db->Delete(write_options, "key10065");

  // op 14787: put
  db->Put(write_options, "key6124", "value49161");

  // op 14788: delete
  db->Delete(write_options, "key2772");

  // op 14789: put
  db->Put(write_options, "key8517", "value98955");

  // op 14790: delete_range
  status = db->DeleteRange(write_options, "key2034", "key11190");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 14791: get
  status = db->Get(read_options, "key0512", &value);
  if (status.ok()) {
    out << "GET key0512 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14792: put
  db->Put(write_options, "key6215", "value105237");

  // op 14793: delete_range
  status = db->DeleteRange(write_options, "key3619", "key4559");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 14794: get
  status = db->Get(read_options, "key4416", &value);
  if (status.ok()) {
    out << "GET key4416 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14795: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2211", "key3914", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2211, key3914) size=" << scan_result.size() << std::endl;
  }

  // op 14796: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4385", "key11155", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4385, key11155) size=" << scan_result.size() << std::endl;
  }

  // op 14797: delete_range
  status = db->DeleteRange(write_options, "key3783", "key4774");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 14798: get
  status = db->Get(read_options, "key4255", &value);
  if (status.ok()) {
    out << "GET key4255 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14799: get
  status = db->Get(read_options, "key10053", &value);
  if (status.ok()) {
    out << "GET key10053 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14800: put
  db->Put(write_options, "key1172", "value41579");

  // op 14801: put
  db->Put(write_options, "key11200", "value38997");

  // op 14802: get
  status = db->Get(read_options, "key4345", &value);
  if (status.ok()) {
    out << "GET key4345 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14803: put
  db->Put(write_options, "key0675", "value11893");

  // op 14804: put
  db->Put(write_options, "key3511", "value111336");

  // op 14805: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9739", "key10930", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9739, key10930) size=" << scan_result.size() << std::endl;
  }

  // op 14806: put
  db->Put(write_options, "key3216", "value59833");

  // op 14807: put
  db->Put(write_options, "key2870", "value102121");

  // op 14808: put
  db->Put(write_options, "key8856", "value40757");

  // op 14809: put
  db->Put(write_options, "key5706", "value109365");

  // op 14810: put
  db->Put(write_options, "key9948", "value36945");

  // op 14811: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2416", "key10834", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2416, key10834) size=" << scan_result.size() << std::endl;
  }

  // op 14812: put
  db->Put(write_options, "key8738", "value70251");

  // op 14813: put
  db->Put(write_options, "key6989", "value70841");

  // op 14814: get
  status = db->Get(read_options, "key1197", &value);
  if (status.ok()) {
    out << "GET key1197 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14815: get
  status = db->Get(read_options, "key5033", &value);
  if (status.ok()) {
    out << "GET key5033 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14816: put
  db->Put(write_options, "key4489", "value95126");

  // op 14817: put
  db->Put(write_options, "key10877", "value25749");

  // op 14818: get
  status = db->Get(read_options, "key9199", &value);
  if (status.ok()) {
    out << "GET key9199 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14819: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4120", "key5557", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4120, key5557) size=" << scan_result.size() << std::endl;
  }

  // op 14820: get
  status = db->Get(read_options, "key11054", &value);
  if (status.ok()) {
    out << "GET key11054 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14821: put
  db->Put(write_options, "key2174", "value101958");

  // op 14822: put
  db->Put(write_options, "key8387", "value24783");

  // op 14823: get
  status = db->Get(read_options, "key0655", &value);
  if (status.ok()) {
    out << "GET key0655 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14824: get
  status = db->Get(read_options, "key3990", &value);
  if (status.ok()) {
    out << "GET key3990 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14825: get
  status = db->Get(read_options, "key0124", &value);
  if (status.ok()) {
    out << "GET key0124 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14826: put
  db->Put(write_options, "key2255", "value102256");

  // op 14827: delete
  db->Delete(write_options, "key7717");

  // op 14828: delete
  db->Delete(write_options, "key6702");

  // op 14829: delete_range
  status = db->DeleteRange(write_options, "key1340", "key7923");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 14830: put
  db->Put(write_options, "key1340", "value44842");

  // op 14831: put
  db->Put(write_options, "key2017", "value75934");

  // op 14832: put
  db->Put(write_options, "key8434", "value69067");

  // op 14833: put
  db->Put(write_options, "key5217", "value98015");

  // op 14834: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6882", "key10221", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6882, key10221) size=" << scan_result.size() << std::endl;
  }

  // op 14835: put
  db->Put(write_options, "key10537", "value83684");

  // op 14836: put
  db->Put(write_options, "key6484", "value49086");

  // op 14837: get
  status = db->Get(read_options, "key8056", &value);
  if (status.ok()) {
    out << "GET key8056 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14838: put
  db->Put(write_options, "key0866", "value1388");

  // op 14839: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2824", "key4956", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2824, key4956) size=" << scan_result.size() << std::endl;
  }

  // op 14840: get
  status = db->Get(read_options, "key2456", &value);
  if (status.ok()) {
    out << "GET key2456 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14841: get
  status = db->Get(read_options, "key1272", &value);
  if (status.ok()) {
    out << "GET key1272 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14842: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0063", "key3396", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0063, key3396) size=" << scan_result.size() << std::endl;
  }

  // op 14843: get
  status = db->Get(read_options, "key5977", &value);
  if (status.ok()) {
    out << "GET key5977 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14844: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2415", "key8438", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2415, key8438) size=" << scan_result.size() << std::endl;
  }

  // op 14845: put
  db->Put(write_options, "key4702", "value110698");

  // op 14846: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5611", "key10934", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5611, key10934) size=" << scan_result.size() << std::endl;
  }

  // op 14847: delete_range
  status = db->DeleteRange(write_options, "key7195", "key8415");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 14848: get
  status = db->Get(read_options, "key6492", &value);
  if (status.ok()) {
    out << "GET key6492 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14849: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1160", "key4679", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1160, key4679) size=" << scan_result.size() << std::endl;
  }

  // op 14850: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4505", "key6855", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4505, key6855) size=" << scan_result.size() << std::endl;
  }

  // op 14851: delete
  db->Delete(write_options, "key1572");

  // op 14852: put
  db->Put(write_options, "key9364", "value106262");

  // op 14853: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4432", "key4616", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4432, key4616) size=" << scan_result.size() << std::endl;
  }

  // op 14854: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6101", "key9481", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6101, key9481) size=" << scan_result.size() << std::endl;
  }

  // op 14855: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5873", "key8619", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5873, key8619) size=" << scan_result.size() << std::endl;
  }

  // op 14856: get
  status = db->Get(read_options, "key2316", &value);
  if (status.ok()) {
    out << "GET key2316 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14857: delete_range
  status = db->DeleteRange(write_options, "key1071", "key3165");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 14858: delete
  db->Delete(write_options, "key3599");

  // op 14859: delete_range
  status = db->DeleteRange(write_options, "key1569", "key10989");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 14860: get
  status = db->Get(read_options, "key8122", &value);
  if (status.ok()) {
    out << "GET key8122 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14861: delete
  db->Delete(write_options, "key8156");

  // op 14862: delete
  db->Delete(write_options, "key0230");

  // op 14863: put
  db->Put(write_options, "key3598", "value36025");

  // op 14864: put
  db->Put(write_options, "key5943", "value57819");

  // op 14865: put
  db->Put(write_options, "key6308", "value61138");

  // op 14866: delete
  db->Delete(write_options, "key6409");

  // op 14867: put
  db->Put(write_options, "key2742", "value57581");

  // op 14868: put
  db->Put(write_options, "key8313", "value22971");

  // op 14869: put
  db->Put(write_options, "key10778", "value94637");

  // op 14870: get
  status = db->Get(read_options, "key2516", &value);
  if (status.ok()) {
    out << "GET key2516 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14871: put
  db->Put(write_options, "key0652", "value70887");

  // op 14872: put
  db->Put(write_options, "key8855", "value76567");

  // op 14873: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1063", "key9952", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1063, key9952) size=" << scan_result.size() << std::endl;
  }

  // op 14874: delete
  db->Delete(write_options, "key7917");

  // op 14875: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1686", "key11072", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1686, key11072) size=" << scan_result.size() << std::endl;
  }

  // op 14876: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0605", "key4826", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0605, key4826) size=" << scan_result.size() << std::endl;
  }

  // op 14877: get
  status = db->Get(read_options, "key9411", &value);
  if (status.ok()) {
    out << "GET key9411 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14878: put
  db->Put(write_options, "key6246", "value54553");

  // op 14879: get
  status = db->Get(read_options, "key4637", &value);
  if (status.ok()) {
    out << "GET key4637 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14880: delete
  db->Delete(write_options, "key7147");

  // op 14881: put
  db->Put(write_options, "key8236", "value87032");

  // op 14882: put
  db->Put(write_options, "key6663", "value82600");

  // op 14883: get
  status = db->Get(read_options, "key0165", &value);
  if (status.ok()) {
    out << "GET key0165 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14884: delete
  db->Delete(write_options, "key11290");

  // op 14885: get
  status = db->Get(read_options, "key4410", &value);
  if (status.ok()) {
    out << "GET key4410 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14886: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 14887: delete
  db->Delete(write_options, "key2655");

  // op 14888: put
  db->Put(write_options, "key0771", "value89103");

  // op 14889: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3755", "key8158", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3755, key8158) size=" << scan_result.size() << std::endl;
  }

  // op 14890: get
  status = db->Get(read_options, "key11095", &value);
  if (status.ok()) {
    out << "GET key11095 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14891: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2534", "key8266", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2534, key8266) size=" << scan_result.size() << std::endl;
  }

  // op 14892: delete_range
  status = db->DeleteRange(write_options, "key4638", "key4716");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 14893: put
  db->Put(write_options, "key4938", "value13451");

  // op 14894: put
  db->Put(write_options, "key8160", "value70796");

  // op 14895: delete_range
  status = db->DeleteRange(write_options, "key0621", "key9319");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 14896: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2058", "key10211", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2058, key10211) size=" << scan_result.size() << std::endl;
  }

  // op 14897: put
  db->Put(write_options, "key8286", "value45684");

  // op 14898: delete
  db->Delete(write_options, "key4081");

  // op 14899: delete_range
  status = db->DeleteRange(write_options, "key4662", "key11531");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 14900: delete
  db->Delete(write_options, "key7866");

  // op 14901: get
  status = db->Get(read_options, "key1911", &value);
  if (status.ok()) {
    out << "GET key1911 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14902: put
  db->Put(write_options, "key5620", "value50782");

  // op 14903: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3812", "key11071", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3812, key11071) size=" << scan_result.size() << std::endl;
  }

  // op 14904: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3319", "key9820", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3319, key9820) size=" << scan_result.size() << std::endl;
  }

  // op 14905: put
  db->Put(write_options, "key2773", "value79275");

  // op 14906: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2902", "key11561", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2902, key11561) size=" << scan_result.size() << std::endl;
  }

  // op 14907: put
  db->Put(write_options, "key7595", "value38734");

  // op 14908: put
  db->Put(write_options, "key2066", "value40853");

  // op 14909: put
  db->Put(write_options, "key11416", "value82295");

  // op 14910: put
  db->Put(write_options, "key2944", "value19201");

  // op 14911: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2674", "key5083", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2674, key5083) size=" << scan_result.size() << std::endl;
  }

  // op 14912: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 14913: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8700", "key11046", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8700, key11046) size=" << scan_result.size() << std::endl;
  }

  // op 14914: put
  db->Put(write_options, "key8181", "value30455");

  // op 14915: get
  status = db->Get(read_options, "key3590", &value);
  if (status.ok()) {
    out << "GET key3590 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14916: get
  status = db->Get(read_options, "key9257", &value);
  if (status.ok()) {
    out << "GET key9257 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14917: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0831", "key2222", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0831, key2222) size=" << scan_result.size() << std::endl;
  }

  // op 14918: get
  status = db->Get(read_options, "key11071", &value);
  if (status.ok()) {
    out << "GET key11071 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14919: put
  db->Put(write_options, "key9309", "value72914");

  // op 14920: get
  status = db->Get(read_options, "key1468", &value);
  if (status.ok()) {
    out << "GET key1468 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14921: put
  db->Put(write_options, "key7933", "value100350");

  // op 14922: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5535", "key8596", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5535, key8596) size=" << scan_result.size() << std::endl;
  }

  // op 14923: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8189", "key9903", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8189, key9903) size=" << scan_result.size() << std::endl;
  }

  // op 14924: put
  db->Put(write_options, "key11387", "value65864");

  // op 14925: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2905", "key6170", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2905, key6170) size=" << scan_result.size() << std::endl;
  }

  // op 14926: delete
  db->Delete(write_options, "key2926");

  // op 14927: put
  db->Put(write_options, "key9659", "value19895");

  // op 14928: put
  db->Put(write_options, "key6105", "value44041");

  // op 14929: delete
  db->Delete(write_options, "key4619");

  // op 14930: get
  status = db->Get(read_options, "key7820", &value);
  if (status.ok()) {
    out << "GET key7820 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14931: put
  db->Put(write_options, "key9420", "value99636");

  // op 14932: delete_range
  status = db->DeleteRange(write_options, "key1381", "key9090");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 14933: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4603", "key11012", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4603, key11012) size=" << scan_result.size() << std::endl;
  }

  // op 14934: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 14935: delete
  db->Delete(write_options, "key10136");

  // op 14936: delete
  db->Delete(write_options, "key0631");

  // op 14937: put
  db->Put(write_options, "key6029", "value95047");

  // op 14938: delete_range
  status = db->DeleteRange(write_options, "key0484", "key10062");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 14939: get
  status = db->Get(read_options, "key7778", &value);
  if (status.ok()) {
    out << "GET key7778 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14940: put
  db->Put(write_options, "key10006", "value101142");

  // op 14941: get
  status = db->Get(read_options, "key8780", &value);
  if (status.ok()) {
    out << "GET key8780 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14942: delete_range
  status = db->DeleteRange(write_options, "key8228", "key11664");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 14943: get
  status = db->Get(read_options, "key8228", &value);
  if (status.ok()) {
    out << "GET key8228 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14944: get
  status = db->Get(read_options, "key4839", &value);
  if (status.ok()) {
    out << "GET key4839 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14945: put
  db->Put(write_options, "key1984", "value47819");

  // op 14946: delete
  db->Delete(write_options, "key10433");

  // op 14947: put
  db->Put(write_options, "key10980", "value41213");

  // op 14948: get
  status = db->Get(read_options, "key9981", &value);
  if (status.ok()) {
    out << "GET key9981 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14949: delete_range
  status = db->DeleteRange(write_options, "key0101", "key8400");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 14950: delete
  db->Delete(write_options, "key3778");

  // op 14951: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0605", "key2620", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0605, key2620) size=" << scan_result.size() << std::endl;
  }

  // op 14952: get
  status = db->Get(read_options, "key0329", &value);
  if (status.ok()) {
    out << "GET key0329 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14953: delete_range
  status = db->DeleteRange(write_options, "key0912", "key6832");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 14954: put
  db->Put(write_options, "key6472", "value23418");

  // op 14955: delete_range
  status = db->DeleteRange(write_options, "key6179", "key6653");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 14956: get
  status = db->Get(read_options, "key4151", &value);
  if (status.ok()) {
    out << "GET key4151 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14957: put
  db->Put(write_options, "key0054", "value103701");

  // op 14958: put
  db->Put(write_options, "key1130", "value1076");

  // op 14959: put
  db->Put(write_options, "key1884", "value92739");

  // op 14960: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 14961: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1991", "key3811", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1991, key3811) size=" << scan_result.size() << std::endl;
  }

  // op 14962: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 14963: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 14964: put
  db->Put(write_options, "key9512", "value5808");

  // op 14965: delete
  db->Delete(write_options, "key10059");

  // op 14966: delete_range
  status = db->DeleteRange(write_options, "key7097", "key7239");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 14967: get
  status = db->Get(read_options, "key4613", &value);
  if (status.ok()) {
    out << "GET key4613 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14968: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1800", "key11271", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1800, key11271) size=" << scan_result.size() << std::endl;
  }

  // op 14969: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0382", "key5411", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0382, key5411) size=" << scan_result.size() << std::endl;
  }

  // op 14970: put
  db->Put(write_options, "key4675", "value13659");

  // op 14971: delete_range
  status = db->DeleteRange(write_options, "key3191", "key4109");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 14972: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6045", "key8410", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6045, key8410) size=" << scan_result.size() << std::endl;
  }

  // op 14973: get
  status = db->Get(read_options, "key8908", &value);
  if (status.ok()) {
    out << "GET key8908 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14974: delete_range
  status = db->DeleteRange(write_options, "key6942", "key11719");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 14975: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1979", "key10912", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1979, key10912) size=" << scan_result.size() << std::endl;
  }

  // op 14976: delete_range
  status = db->DeleteRange(write_options, "key3166", "key11158");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 14977: put
  db->Put(write_options, "key11587", "value11010");

  // op 14978: get
  status = db->Get(read_options, "key6923", &value);
  if (status.ok()) {
    out << "GET key6923 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14979: put
  db->Put(write_options, "key6387", "value37705");

  // op 14980: put
  db->Put(write_options, "key0594", "value110284");

  // op 14981: get
  status = db->Get(read_options, "key4065", &value);
  if (status.ok()) {
    out << "GET key4065 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14982: get
  status = db->Get(read_options, "key8657", &value);
  if (status.ok()) {
    out << "GET key8657 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14983: put
  db->Put(write_options, "key7337", "value68046");

  // op 14984: get
  status = db->Get(read_options, "key3779", &value);
  if (status.ok()) {
    out << "GET key3779 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14985: put
  db->Put(write_options, "key2189", "value13906");

  // op 14986: put
  db->Put(write_options, "key11087", "value40735");

  // op 14987: get
  status = db->Get(read_options, "key2080", &value);
  if (status.ok()) {
    out << "GET key2080 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14988: put
  db->Put(write_options, "key9548", "value15099");

  // op 14989: get
  status = db->Get(read_options, "key4672", &value);
  if (status.ok()) {
    out << "GET key4672 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14990: get
  status = db->Get(read_options, "key3765", &value);
  if (status.ok()) {
    out << "GET key3765 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14991: put
  db->Put(write_options, "key4464", "value57305");

  // op 14992: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10248", "key10871", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10248, key10871) size=" << scan_result.size() << std::endl;
  }

  // op 14993: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0604", "key2721", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0604, key2721) size=" << scan_result.size() << std::endl;
  }

  // op 14994: get
  status = db->Get(read_options, "key4421", &value);
  if (status.ok()) {
    out << "GET key4421 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 14995: delete_range
  status = db->DeleteRange(write_options, "key0791", "key3872");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 14996: put
  db->Put(write_options, "key0895", "value79188");

  // op 14997: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 14998: put
  db->Put(write_options, "key6997", "value54353");

  // op 14999: put
  db->Put(write_options, "key1526", "value30021");

  // op 15000: get
  status = db->Get(read_options, "key2680", &value);
  if (status.ok()) {
    out << "GET key2680 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15001: put
  db->Put(write_options, "key8920", "value92253");

  // op 15002: put
  db->Put(write_options, "key7503", "value62054");

  // op 15003: put
  db->Put(write_options, "key8470", "value60634");

  // op 15004: put
  db->Put(write_options, "key9140", "value60496");

  // op 15005: delete_range
  status = db->DeleteRange(write_options, "key3079", "key4385");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 15006: put
  db->Put(write_options, "key5196", "value64157");

  // op 15007: delete_range
  status = db->DeleteRange(write_options, "key1438", "key6922");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 15008: delete
  db->Delete(write_options, "key9986");

  // op 15009: get
  status = db->Get(read_options, "key3228", &value);
  if (status.ok()) {
    out << "GET key3228 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15010: get
  status = db->Get(read_options, "key8794", &value);
  if (status.ok()) {
    out << "GET key8794 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15011: put
  db->Put(write_options, "key2135", "value12604");

  // op 15012: put
  db->Put(write_options, "key9564", "value27856");

  // op 15013: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0049", "key2447", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0049, key2447) size=" << scan_result.size() << std::endl;
  }

  // op 15014: put
  db->Put(write_options, "key5052", "value53028");

  // op 15015: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 15016: put
  db->Put(write_options, "key7976", "value49789");

  // op 15017: put
  db->Put(write_options, "key3725", "value75780");

  // op 15018: put
  db->Put(write_options, "key6320", "value71305");

  // op 15019: put
  db->Put(write_options, "key9742", "value45062");

  // op 15020: put
  db->Put(write_options, "key5324", "value27742");

  // op 15021: put
  db->Put(write_options, "key2415", "value65127");

  // op 15022: get
  status = db->Get(read_options, "key10166", &value);
  if (status.ok()) {
    out << "GET key10166 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15023: put
  db->Put(write_options, "key7473", "value69621");

  // op 15024: get
  status = db->Get(read_options, "key9534", &value);
  if (status.ok()) {
    out << "GET key9534 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15025: put
  db->Put(write_options, "key1581", "value1713");

  // op 15026: delete_range
  status = db->DeleteRange(write_options, "key5565", "key8277");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 15027: put
  db->Put(write_options, "key5850", "value92332");

  // op 15028: put
  db->Put(write_options, "key10565", "value39919");

  // op 15029: put
  db->Put(write_options, "key11522", "value78168");

  // op 15030: put
  db->Put(write_options, "key1331", "value58754");

  // op 15031: put
  db->Put(write_options, "key11126", "value109976");

  // op 15032: delete_range
  status = db->DeleteRange(write_options, "key10078", "key10635");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 15033: put
  db->Put(write_options, "key6076", "value108224");

  // op 15034: get
  status = db->Get(read_options, "key1093", &value);
  if (status.ok()) {
    out << "GET key1093 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15035: delete_range
  status = db->DeleteRange(write_options, "key8222", "key10882");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 15036: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9091", "key9276", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9091, key9276) size=" << scan_result.size() << std::endl;
  }

  // op 15037: get
  status = db->Get(read_options, "key1434", &value);
  if (status.ok()) {
    out << "GET key1434 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15038: put
  db->Put(write_options, "key9837", "value92459");

  // op 15039: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4281", "key10248", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4281, key10248) size=" << scan_result.size() << std::endl;
  }

  // op 15040: get
  status = db->Get(read_options, "key7241", &value);
  if (status.ok()) {
    out << "GET key7241 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15041: put
  db->Put(write_options, "key10191", "value43149");

  // op 15042: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3279", "key6245", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3279, key6245) size=" << scan_result.size() << std::endl;
  }

  // op 15043: get
  status = db->Get(read_options, "key10135", &value);
  if (status.ok()) {
    out << "GET key10135 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15044: put
  db->Put(write_options, "key5118", "value40364");

  // op 15045: delete_range
  status = db->DeleteRange(write_options, "key6043", "key6352");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 15046: delete
  db->Delete(write_options, "key7682");

  // op 15047: put
  db->Put(write_options, "key7924", "value78548");

  // op 15048: get
  status = db->Get(read_options, "key11087", &value);
  if (status.ok()) {
    out << "GET key11087 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15049: delete
  db->Delete(write_options, "key5324");

  // op 15050: get
  status = db->Get(read_options, "key0390", &value);
  if (status.ok()) {
    out << "GET key0390 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15051: get
  status = db->Get(read_options, "key0950", &value);
  if (status.ok()) {
    out << "GET key0950 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15052: get
  status = db->Get(read_options, "key0880", &value);
  if (status.ok()) {
    out << "GET key0880 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15053: put
  db->Put(write_options, "key8819", "value83381");

  // op 15054: get
  status = db->Get(read_options, "key3294", &value);
  if (status.ok()) {
    out << "GET key3294 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15055: put
  db->Put(write_options, "key4266", "value21710");

  // op 15056: delete_range
  status = db->DeleteRange(write_options, "key7763", "key11324");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 15057: delete_range
  status = db->DeleteRange(write_options, "key0990", "key11525");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 15058: put
  db->Put(write_options, "key1609", "value115138");

  // op 15059: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 15060: put
  db->Put(write_options, "key10679", "value95191");

  // op 15061: put
  db->Put(write_options, "key9788", "value69245");

  // op 15062: get
  status = db->Get(read_options, "key11236", &value);
  if (status.ok()) {
    out << "GET key11236 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15063: put
  db->Put(write_options, "key2846", "value33293");

  // op 15064: delete_range
  status = db->DeleteRange(write_options, "key6098", "key11332");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 15065: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0139", "key10124", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0139, key10124) size=" << scan_result.size() << std::endl;
  }

  // op 15066: put
  db->Put(write_options, "key0706", "value19270");

  // op 15067: get
  status = db->Get(read_options, "key5772", &value);
  if (status.ok()) {
    out << "GET key5772 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15068: put
  db->Put(write_options, "key9678", "value86276");

  // op 15069: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2261", "key6229", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2261, key6229) size=" << scan_result.size() << std::endl;
  }

  // op 15070: put
  db->Put(write_options, "key9559", "value84954");

  // op 15071: get
  status = db->Get(read_options, "key6587", &value);
  if (status.ok()) {
    out << "GET key6587 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15072: get
  status = db->Get(read_options, "key6521", &value);
  if (status.ok()) {
    out << "GET key6521 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15073: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4104", "key5486", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4104, key5486) size=" << scan_result.size() << std::endl;
  }

  // op 15074: put
  db->Put(write_options, "key3175", "value89277");

  // op 15075: delete_range
  status = db->DeleteRange(write_options, "key1719", "key9614");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 15076: delete_range
  status = db->DeleteRange(write_options, "key6592", "key8761");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 15077: get
  status = db->Get(read_options, "key7120", &value);
  if (status.ok()) {
    out << "GET key7120 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15078: put
  db->Put(write_options, "key7249", "value64174");

  // op 15079: get
  status = db->Get(read_options, "key9122", &value);
  if (status.ok()) {
    out << "GET key9122 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15080: put
  db->Put(write_options, "key7238", "value35839");

  // op 15081: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6402", "key8289", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6402, key8289) size=" << scan_result.size() << std::endl;
  }

  // op 15082: delete
  db->Delete(write_options, "key10531");

  // op 15083: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 15084: put
  db->Put(write_options, "key8003", "value24664");

  // op 15085: put
  db->Put(write_options, "key0379", "value58101");

  // op 15086: put
  db->Put(write_options, "key1894", "value58788");

  // op 15087: delete_range
  status = db->DeleteRange(write_options, "key2403", "key7672");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 15088: delete
  db->Delete(write_options, "key1659");

  // op 15089: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 15090: delete_range
  status = db->DeleteRange(write_options, "key0410", "key6117");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 15091: get
  status = db->Get(read_options, "key9191", &value);
  if (status.ok()) {
    out << "GET key9191 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15092: put
  db->Put(write_options, "key8739", "value95375");

  // op 15093: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 15094: put
  db->Put(write_options, "key1165", "value28743");

  // op 15095: put
  db->Put(write_options, "key2215", "value46572");

  // op 15096: delete
  db->Delete(write_options, "key9352");

  // op 15097: get
  status = db->Get(read_options, "key5220", &value);
  if (status.ok()) {
    out << "GET key5220 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15098: get
  status = db->Get(read_options, "key7046", &value);
  if (status.ok()) {
    out << "GET key7046 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15099: put
  db->Put(write_options, "key6738", "value47153");

  // op 15100: get
  status = db->Get(read_options, "key8993", &value);
  if (status.ok()) {
    out << "GET key8993 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15101: delete
  db->Delete(write_options, "key1343");

  // op 15102: delete
  db->Delete(write_options, "key8048");

  // op 15103: delete
  db->Delete(write_options, "key6088");

  // op 15104: delete
  db->Delete(write_options, "key1301");

  // op 15105: put
  db->Put(write_options, "key0320", "value24839");

  // op 15106: delete_range
  status = db->DeleteRange(write_options, "key4548", "key6899");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 15107: get
  status = db->Get(read_options, "key11278", &value);
  if (status.ok()) {
    out << "GET key11278 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15108: put
  db->Put(write_options, "key8696", "value87297");

  // op 15109: put
  db->Put(write_options, "key7398", "value75140");

  // op 15110: put
  db->Put(write_options, "key7381", "value116513");

  // op 15111: delete_range
  status = db->DeleteRange(write_options, "key6522", "key7852");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 15112: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2278", "key3333", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2278, key3333) size=" << scan_result.size() << std::endl;
  }

  // op 15113: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0516", "key4114", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0516, key4114) size=" << scan_result.size() << std::endl;
  }

  // op 15114: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7076", "key8820", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7076, key8820) size=" << scan_result.size() << std::endl;
  }

  // op 15115: delete
  db->Delete(write_options, "key5046");

  // op 15116: put
  db->Put(write_options, "key5943", "value30212");

  // op 15117: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 15118: put
  db->Put(write_options, "key2612", "value76246");

  // op 15119: put
  db->Put(write_options, "key10507", "value77729");

  // op 15120: put
  db->Put(write_options, "key3233", "value39264");

  // op 15121: put
  db->Put(write_options, "key0144", "value54893");

  // op 15122: put
  db->Put(write_options, "key4185", "value4921");

  // op 15123: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2969", "key7094", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2969, key7094) size=" << scan_result.size() << std::endl;
  }

  // op 15124: put
  db->Put(write_options, "key0523", "value37498");

  // op 15125: delete_range
  status = db->DeleteRange(write_options, "key8432", "key9075");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 15126: delete_range
  status = db->DeleteRange(write_options, "key2105", "key7972");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 15127: put
  db->Put(write_options, "key4337", "value82142");

  // op 15128: delete
  db->Delete(write_options, "key1368");

  // op 15129: put
  db->Put(write_options, "key6533", "value3025");

  // op 15130: get
  status = db->Get(read_options, "key2038", &value);
  if (status.ok()) {
    out << "GET key2038 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15131: put
  db->Put(write_options, "key10185", "value60889");

  // op 15132: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 15133: put
  db->Put(write_options, "key3245", "value45787");

  // op 15134: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1154", "key7155", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1154, key7155) size=" << scan_result.size() << std::endl;
  }

  // op 15135: put
  db->Put(write_options, "key2652", "value12662");

  // op 15136: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5484", "key7290", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5484, key7290) size=" << scan_result.size() << std::endl;
  }

  // op 15137: put
  db->Put(write_options, "key1543", "value66850");

  // op 15138: put
  db->Put(write_options, "key6168", "value42831");

  // op 15139: put
  db->Put(write_options, "key1729", "value22958");

  // op 15140: put
  db->Put(write_options, "key6242", "value92881");

  // op 15141: get
  status = db->Get(read_options, "key5262", &value);
  if (status.ok()) {
    out << "GET key5262 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15142: get
  status = db->Get(read_options, "key4124", &value);
  if (status.ok()) {
    out << "GET key4124 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15143: delete
  db->Delete(write_options, "key9574");

  // op 15144: delete
  db->Delete(write_options, "key9039");

  // op 15145: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0475", "key1847", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0475, key1847) size=" << scan_result.size() << std::endl;
  }

  // op 15146: get
  status = db->Get(read_options, "key6148", &value);
  if (status.ok()) {
    out << "GET key6148 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15147: put
  db->Put(write_options, "key11603", "value79133");

  // op 15148: put
  db->Put(write_options, "key10761", "value29353");

  // op 15149: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1024", "key8558", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1024, key8558) size=" << scan_result.size() << std::endl;
  }

  // op 15150: put
  db->Put(write_options, "key8667", "value111868");

  // op 15151: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 15152: delete_range
  status = db->DeleteRange(write_options, "key9732", "key10398");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 15153: put
  db->Put(write_options, "key6793", "value53536");

  // op 15154: put
  db->Put(write_options, "key10565", "value49524");

  // op 15155: delete
  db->Delete(write_options, "key6220");

  // op 15156: put
  db->Put(write_options, "key9472", "value116502");

  // op 15157: delete_range
  status = db->DeleteRange(write_options, "key5205", "key9020");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 15158: put
  db->Put(write_options, "key6173", "value115389");

  // op 15159: get
  status = db->Get(read_options, "key10513", &value);
  if (status.ok()) {
    out << "GET key10513 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15160: put
  db->Put(write_options, "key0516", "value53628");

  // op 15161: put
  db->Put(write_options, "key11535", "value64443");

  // op 15162: delete
  db->Delete(write_options, "key6486");

  // op 15163: get
  status = db->Get(read_options, "key8473", &value);
  if (status.ok()) {
    out << "GET key8473 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15164: delete_range
  status = db->DeleteRange(write_options, "key2935", "key9790");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 15165: put
  db->Put(write_options, "key10221", "value46104");

  // op 15166: delete
  db->Delete(write_options, "key10806");

  // op 15167: delete
  db->Delete(write_options, "key0857");

  // op 15168: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8698", "key9982", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8698, key9982) size=" << scan_result.size() << std::endl;
  }

  // op 15169: delete_range
  status = db->DeleteRange(write_options, "key1435", "key8114");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 15170: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2650", "key5461", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2650, key5461) size=" << scan_result.size() << std::endl;
  }

  // op 15171: put
  db->Put(write_options, "key4930", "value102139");

  // op 15172: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1151", "key10777", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1151, key10777) size=" << scan_result.size() << std::endl;
  }

  // op 15173: put
  db->Put(write_options, "key5887", "value75416");

  // op 15174: get
  status = db->Get(read_options, "key10449", &value);
  if (status.ok()) {
    out << "GET key10449 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15175: put
  db->Put(write_options, "key0393", "value74354");

  // op 15176: put
  db->Put(write_options, "key10980", "value83919");

  // op 15177: put
  db->Put(write_options, "key1123", "value45404");

  // op 15178: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4185", "key7875", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4185, key7875) size=" << scan_result.size() << std::endl;
  }

  // op 15179: delete_range
  status = db->DeleteRange(write_options, "key0978", "key9884");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 15180: put
  db->Put(write_options, "key6717", "value64539");

  // op 15181: delete
  db->Delete(write_options, "key3364");

  // op 15182: put
  db->Put(write_options, "key1776", "value52353");

  // op 15183: get
  status = db->Get(read_options, "key5805", &value);
  if (status.ok()) {
    out << "GET key5805 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15184: put
  db->Put(write_options, "key9679", "value111755");

  // op 15185: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6247", "key7536", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6247, key7536) size=" << scan_result.size() << std::endl;
  }

  // op 15186: get
  status = db->Get(read_options, "key5691", &value);
  if (status.ok()) {
    out << "GET key5691 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15187: get
  status = db->Get(read_options, "key8310", &value);
  if (status.ok()) {
    out << "GET key8310 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15188: delete
  db->Delete(write_options, "key11425");

  // op 15189: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1369", "key2229", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1369, key2229) size=" << scan_result.size() << std::endl;
  }

  // op 15190: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7389", "key7691", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7389, key7691) size=" << scan_result.size() << std::endl;
  }

  // op 15191: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3817", "key7364", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3817, key7364) size=" << scan_result.size() << std::endl;
  }

  // op 15192: put
  db->Put(write_options, "key8355", "value48176");

  // op 15193: put
  db->Put(write_options, "key5871", "value32003");

  // op 15194: delete
  db->Delete(write_options, "key3747");

  // op 15195: put
  db->Put(write_options, "key9968", "value2487");

  // op 15196: delete_range
  status = db->DeleteRange(write_options, "key1064", "key2226");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 15197: get
  status = db->Get(read_options, "key3701", &value);
  if (status.ok()) {
    out << "GET key3701 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15198: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0562", "key1080", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0562, key1080) size=" << scan_result.size() << std::endl;
  }

  // op 15199: put
  db->Put(write_options, "key9944", "value112093");

  // op 15200: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 15201: put
  db->Put(write_options, "key6178", "value47998");

  // op 15202: get
  status = db->Get(read_options, "key5042", &value);
  if (status.ok()) {
    out << "GET key5042 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15203: put
  db->Put(write_options, "key1878", "value47289");

  // op 15204: put
  db->Put(write_options, "key4225", "value38064");

  // op 15205: put
  db->Put(write_options, "key9820", "value55291");

  // op 15206: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0091", "key7148", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0091, key7148) size=" << scan_result.size() << std::endl;
  }

  // op 15207: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4321", "key11580", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4321, key11580) size=" << scan_result.size() << std::endl;
  }

  // op 15208: put
  db->Put(write_options, "key5448", "value107005");

  // op 15209: delete_range
  status = db->DeleteRange(write_options, "key3262", "key5612");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 15210: delete
  db->Delete(write_options, "key4262");

  // op 15211: put
  db->Put(write_options, "key5243", "value100158");

  // op 15212: put
  db->Put(write_options, "key2229", "value24015");

  // op 15213: put
  db->Put(write_options, "key7715", "value95555");

  // op 15214: put
  db->Put(write_options, "key5387", "value116802");

  // op 15215: delete_range
  status = db->DeleteRange(write_options, "key8682", "key10520");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 15216: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2538", "key11643", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2538, key11643) size=" << scan_result.size() << std::endl;
  }

  // op 15217: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 15218: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2585", "key9208", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2585, key9208) size=" << scan_result.size() << std::endl;
  }

  // op 15219: put
  db->Put(write_options, "key9380", "value22026");

  // op 15220: put
  db->Put(write_options, "key9851", "value93968");

  // op 15221: put
  db->Put(write_options, "key6908", "value65387");

  // op 15222: get
  status = db->Get(read_options, "key11161", &value);
  if (status.ok()) {
    out << "GET key11161 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15223: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8773", "key9140", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8773, key9140) size=" << scan_result.size() << std::endl;
  }

  // op 15224: delete
  db->Delete(write_options, "key0074");

  // op 15225: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0709", "key5421", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0709, key5421) size=" << scan_result.size() << std::endl;
  }

  // op 15226: put
  db->Put(write_options, "key1649", "value11339");

  // op 15227: get
  status = db->Get(read_options, "key0785", &value);
  if (status.ok()) {
    out << "GET key0785 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15228: put
  db->Put(write_options, "key6890", "value113332");

  // op 15229: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5424", "key6393", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5424, key6393) size=" << scan_result.size() << std::endl;
  }

  // op 15230: get
  status = db->Get(read_options, "key9466", &value);
  if (status.ok()) {
    out << "GET key9466 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15231: put
  db->Put(write_options, "key10625", "value12304");

  // op 15232: put
  db->Put(write_options, "key2619", "value59613");

  // op 15233: delete_range
  status = db->DeleteRange(write_options, "key4619", "key8864");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 15234: get
  status = db->Get(read_options, "key1876", &value);
  if (status.ok()) {
    out << "GET key1876 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15235: delete
  db->Delete(write_options, "key9840");

  // op 15236: delete
  db->Delete(write_options, "key7944");

  // op 15237: put
  db->Put(write_options, "key8021", "value85229");

  // op 15238: get
  status = db->Get(read_options, "key4237", &value);
  if (status.ok()) {
    out << "GET key4237 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15239: put
  db->Put(write_options, "key4864", "value70317");

  // op 15240: delete
  db->Delete(write_options, "key4151");

  // op 15241: put
  db->Put(write_options, "key4604", "value8010");

  // op 15242: put
  db->Put(write_options, "key1192", "value72632");

  // op 15243: put
  db->Put(write_options, "key5604", "value38628");

  // op 15244: put
  db->Put(write_options, "key1865", "value47116");

  // op 15245: put
  db->Put(write_options, "key5434", "value35938");

  // op 15246: put
  db->Put(write_options, "key10543", "value88244");

  // op 15247: get
  status = db->Get(read_options, "key1211", &value);
  if (status.ok()) {
    out << "GET key1211 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15248: delete
  db->Delete(write_options, "key1045");

  // op 15249: put
  db->Put(write_options, "key8878", "value27807");

  // op 15250: put
  db->Put(write_options, "key5427", "value85877");

  // op 15251: get
  status = db->Get(read_options, "key2724", &value);
  if (status.ok()) {
    out << "GET key2724 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15252: put
  db->Put(write_options, "key6923", "value61213");

  // op 15253: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 15254: get
  status = db->Get(read_options, "key5140", &value);
  if (status.ok()) {
    out << "GET key5140 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15255: put
  db->Put(write_options, "key7602", "value54690");

  // op 15256: put
  db->Put(write_options, "key0255", "value91795");

  // op 15257: get
  status = db->Get(read_options, "key8715", &value);
  if (status.ok()) {
    out << "GET key8715 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15258: delete_range
  status = db->DeleteRange(write_options, "key1431", "key7700");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 15259: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7792", "key8950", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7792, key8950) size=" << scan_result.size() << std::endl;
  }

  // op 15260: put
  db->Put(write_options, "key2750", "value70179");

  // op 15261: put
  db->Put(write_options, "key0677", "value97808");

  // op 15262: get
  status = db->Get(read_options, "key8654", &value);
  if (status.ok()) {
    out << "GET key8654 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15263: put
  db->Put(write_options, "key8055", "value49794");

  // op 15264: delete_range
  status = db->DeleteRange(write_options, "key8382", "key10477");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 15265: put
  db->Put(write_options, "key10051", "value97239");

  // op 15266: get
  status = db->Get(read_options, "key5573", &value);
  if (status.ok()) {
    out << "GET key5573 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15267: get
  status = db->Get(read_options, "key7083", &value);
  if (status.ok()) {
    out << "GET key7083 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15268: delete_range
  status = db->DeleteRange(write_options, "key2531", "key6628");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 15269: get
  status = db->Get(read_options, "key0980", &value);
  if (status.ok()) {
    out << "GET key0980 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15270: put
  db->Put(write_options, "key4923", "value53373");

  // op 15271: delete_range
  status = db->DeleteRange(write_options, "key0302", "key3315");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 15272: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1628", "key6251", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1628, key6251) size=" << scan_result.size() << std::endl;
  }

  // op 15273: get
  status = db->Get(read_options, "key8096", &value);
  if (status.ok()) {
    out << "GET key8096 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15274: get
  status = db->Get(read_options, "key8283", &value);
  if (status.ok()) {
    out << "GET key8283 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15275: put
  db->Put(write_options, "key4013", "value94544");

  // op 15276: get
  status = db->Get(read_options, "key10314", &value);
  if (status.ok()) {
    out << "GET key10314 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15277: delete_range
  status = db->DeleteRange(write_options, "key1936", "key8687");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 15278: delete
  db->Delete(write_options, "key6716");

  // op 15279: get
  status = db->Get(read_options, "key9683", &value);
  if (status.ok()) {
    out << "GET key9683 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15280: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0620", "key1274", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0620, key1274) size=" << scan_result.size() << std::endl;
  }

  // op 15281: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3658", "key6585", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3658, key6585) size=" << scan_result.size() << std::endl;
  }

  // op 15282: get
  status = db->Get(read_options, "key8393", &value);
  if (status.ok()) {
    out << "GET key8393 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15283: delete
  db->Delete(write_options, "key8556");

  // op 15284: delete
  db->Delete(write_options, "key3333");

  // op 15285: put
  db->Put(write_options, "key2977", "value86615");

  // op 15286: delete
  db->Delete(write_options, "key1855");

  // op 15287: put
  db->Put(write_options, "key11605", "value90486");

  // op 15288: put
  db->Put(write_options, "key9633", "value66234");

  // op 15289: delete
  db->Delete(write_options, "key2994");

  // op 15290: put
  db->Put(write_options, "key6506", "value66970");

  // op 15291: put
  db->Put(write_options, "key7716", "value73420");

  // op 15292: delete_range
  status = db->DeleteRange(write_options, "key1624", "key2840");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 15293: put
  db->Put(write_options, "key5701", "value66924");

  // op 15294: put
  db->Put(write_options, "key2953", "value76315");

  // op 15295: delete_range
  status = db->DeleteRange(write_options, "key5303", "key8956");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 15296: put
  db->Put(write_options, "key1784", "value59458");

  // op 15297: delete
  db->Delete(write_options, "key6550");

  // op 15298: put
  db->Put(write_options, "key10660", "value32875");

  // op 15299: put
  db->Put(write_options, "key1967", "value12012");

  // op 15300: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4372", "key9717", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4372, key9717) size=" << scan_result.size() << std::endl;
  }

  // op 15301: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3862", "key6158", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3862, key6158) size=" << scan_result.size() << std::endl;
  }

  // op 15302: put
  db->Put(write_options, "key3110", "value91187");

  // op 15303: put
  db->Put(write_options, "key3965", "value55023");

  // op 15304: put
  db->Put(write_options, "key8104", "value48363");

  // op 15305: put
  db->Put(write_options, "key8535", "value83464");

  // op 15306: put
  db->Put(write_options, "key3054", "value92044");

  // op 15307: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1778", "key11322", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1778, key11322) size=" << scan_result.size() << std::endl;
  }

  // op 15308: put
  db->Put(write_options, "key7616", "value89282");

  // op 15309: put
  db->Put(write_options, "key1462", "value96027");

  // op 15310: put
  db->Put(write_options, "key3585", "value25544");

  // op 15311: put
  db->Put(write_options, "key4190", "value25255");

  // op 15312: put
  db->Put(write_options, "key5673", "value86393");

  // op 15313: put
  db->Put(write_options, "key0487", "value75249");

  // op 15314: get
  status = db->Get(read_options, "key11495", &value);
  if (status.ok()) {
    out << "GET key11495 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15315: put
  db->Put(write_options, "key5811", "value111069");

  // op 15316: get
  status = db->Get(read_options, "key7091", &value);
  if (status.ok()) {
    out << "GET key7091 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15317: get
  status = db->Get(read_options, "key2953", &value);
  if (status.ok()) {
    out << "GET key2953 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15318: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10505", "key10832", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10505, key10832) size=" << scan_result.size() << std::endl;
  }

  // op 15319: delete_range
  status = db->DeleteRange(write_options, "key2997", "key9746");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 15320: put
  db->Put(write_options, "key5497", "value36100");

  // op 15321: put
  db->Put(write_options, "key7253", "value105780");

  // op 15322: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2272", "key11358", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2272, key11358) size=" << scan_result.size() << std::endl;
  }

  // op 15323: put
  db->Put(write_options, "key6938", "value102069");

  // op 15324: get
  status = db->Get(read_options, "key9001", &value);
  if (status.ok()) {
    out << "GET key9001 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15325: delete
  db->Delete(write_options, "key4597");

  // op 15326: delete_range
  status = db->DeleteRange(write_options, "key0914", "key10785");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 15327: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4316", "key6429", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4316, key6429) size=" << scan_result.size() << std::endl;
  }

  // op 15328: put
  db->Put(write_options, "key7722", "value98716");

  // op 15329: put
  db->Put(write_options, "key11340", "value16843");

  // op 15330: get
  status = db->Get(read_options, "key7019", &value);
  if (status.ok()) {
    out << "GET key7019 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15331: delete
  db->Delete(write_options, "key10153");

  // op 15332: put
  db->Put(write_options, "key2779", "value4409");

  // op 15333: delete
  db->Delete(write_options, "key2423");

  // op 15334: put
  db->Put(write_options, "key1912", "value31068");

  // op 15335: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 15336: put
  db->Put(write_options, "key7012", "value33322");

  // op 15337: get
  status = db->Get(read_options, "key5217", &value);
  if (status.ok()) {
    out << "GET key5217 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15338: put
  db->Put(write_options, "key10402", "value98758");

  // op 15339: get
  status = db->Get(read_options, "key0827", &value);
  if (status.ok()) {
    out << "GET key0827 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15340: delete
  db->Delete(write_options, "key5174");

  // op 15341: get
  status = db->Get(read_options, "key6024", &value);
  if (status.ok()) {
    out << "GET key6024 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15342: get
  status = db->Get(read_options, "key3957", &value);
  if (status.ok()) {
    out << "GET key3957 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15343: delete_range
  status = db->DeleteRange(write_options, "key2310", "key8671");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 15344: put
  db->Put(write_options, "key3170", "value74224");

  // op 15345: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0769", "key3126", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0769, key3126) size=" << scan_result.size() << std::endl;
  }

  // op 15346: get
  status = db->Get(read_options, "key4426", &value);
  if (status.ok()) {
    out << "GET key4426 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15347: put
  db->Put(write_options, "key8890", "value105492");

  // op 15348: delete_range
  status = db->DeleteRange(write_options, "key6696", "key10926");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 15349: put
  db->Put(write_options, "key6270", "value69974");

  // op 15350: put
  db->Put(write_options, "key4168", "value15588");

  // op 15351: delete
  db->Delete(write_options, "key10234");

  // op 15352: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 15353: put
  db->Put(write_options, "key10903", "value72867");

  // op 15354: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0934", "key4433", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0934, key4433) size=" << scan_result.size() << std::endl;
  }

  // op 15355: delete
  db->Delete(write_options, "key4871");

  // op 15356: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 15357: get
  status = db->Get(read_options, "key9582", &value);
  if (status.ok()) {
    out << "GET key9582 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15358: delete
  db->Delete(write_options, "key3182");

  // op 15359: put
  db->Put(write_options, "key9745", "value31533");

  // op 15360: put
  db->Put(write_options, "key8067", "value63321");

  // op 15361: delete_range
  status = db->DeleteRange(write_options, "key5213", "key5498");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 15362: get
  status = db->Get(read_options, "key3227", &value);
  if (status.ok()) {
    out << "GET key3227 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15363: get
  status = db->Get(read_options, "key7779", &value);
  if (status.ok()) {
    out << "GET key7779 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15364: put
  db->Put(write_options, "key4825", "value18741");

  // op 15365: put
  db->Put(write_options, "key6221", "value4057");

  // op 15366: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 15367: delete_range
  status = db->DeleteRange(write_options, "key9164", "key10741");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 15368: get
  status = db->Get(read_options, "key6027", &value);
  if (status.ok()) {
    out << "GET key6027 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15369: delete_range
  status = db->DeleteRange(write_options, "key2807", "key9521");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 15370: put
  db->Put(write_options, "key8287", "value113425");

  // op 15371: get
  status = db->Get(read_options, "key0073", &value);
  if (status.ok()) {
    out << "GET key0073 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15372: put
  db->Put(write_options, "key10468", "value39768");

  // op 15373: delete_range
  status = db->DeleteRange(write_options, "key0767", "key1059");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 15374: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5142", "key10503", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5142, key10503) size=" << scan_result.size() << std::endl;
  }

  // op 15375: put
  db->Put(write_options, "key0885", "value77830");

  // op 15376: get
  status = db->Get(read_options, "key7613", &value);
  if (status.ok()) {
    out << "GET key7613 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15377: put
  db->Put(write_options, "key8756", "value81516");

  // op 15378: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 15379: put
  db->Put(write_options, "key0054", "value43181");

  // op 15380: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8457", "key9248", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8457, key9248) size=" << scan_result.size() << std::endl;
  }

  // op 15381: put
  db->Put(write_options, "key4286", "value14184");

  // op 15382: delete_range
  status = db->DeleteRange(write_options, "key1888", "key4931");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 15383: get
  status = db->Get(read_options, "key7804", &value);
  if (status.ok()) {
    out << "GET key7804 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15384: put
  db->Put(write_options, "key1944", "value65231");

  // op 15385: delete_range
  status = db->DeleteRange(write_options, "key4527", "key9404");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 15386: delete
  db->Delete(write_options, "key7016");

  // op 15387: put
  db->Put(write_options, "key10025", "value84388");

  // op 15388: delete
  db->Delete(write_options, "key11328");

  // op 15389: delete_range
  status = db->DeleteRange(write_options, "key1209", "key6142");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 15390: delete
  db->Delete(write_options, "key4408");

  // op 15391: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7509", "key8231", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7509, key8231) size=" << scan_result.size() << std::endl;
  }

  // op 15392: put
  db->Put(write_options, "key5332", "value48749");

  // op 15393: delete_range
  status = db->DeleteRange(write_options, "key1979", "key3276");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 15394: put
  db->Put(write_options, "key8248", "value111499");

  // op 15395: put
  db->Put(write_options, "key3586", "value32912");

  // op 15396: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3886", "key11347", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3886, key11347) size=" << scan_result.size() << std::endl;
  }

  // op 15397: delete
  db->Delete(write_options, "key7485");

  // op 15398: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9661", "key10883", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9661, key10883) size=" << scan_result.size() << std::endl;
  }

  // op 15399: get
  status = db->Get(read_options, "key10276", &value);
  if (status.ok()) {
    out << "GET key10276 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15400: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5634", "key9249", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5634, key9249) size=" << scan_result.size() << std::endl;
  }

  // op 15401: put
  db->Put(write_options, "key9114", "value60460");

  // op 15402: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1402", "key2248", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1402, key2248) size=" << scan_result.size() << std::endl;
  }

  // op 15403: put
  db->Put(write_options, "key9856", "value1935");

  // op 15404: put
  db->Put(write_options, "key2860", "value18164");

  // op 15405: put
  db->Put(write_options, "key8180", "value63680");

  // op 15406: put
  db->Put(write_options, "key4895", "value96613");

  // op 15407: put
  db->Put(write_options, "key9679", "value93112");

  // op 15408: put
  db->Put(write_options, "key5117", "value61156");

  // op 15409: put
  db->Put(write_options, "key4778", "value74162");

  // op 15410: put
  db->Put(write_options, "key3759", "value10671");

  // op 15411: put
  db->Put(write_options, "key6793", "value80078");

  // op 15412: put
  db->Put(write_options, "key3270", "value30598");

  // op 15413: put
  db->Put(write_options, "key6779", "value112287");

  // op 15414: get
  status = db->Get(read_options, "key8046", &value);
  if (status.ok()) {
    out << "GET key8046 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15415: get
  status = db->Get(read_options, "key3722", &value);
  if (status.ok()) {
    out << "GET key3722 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15416: get
  status = db->Get(read_options, "key8781", &value);
  if (status.ok()) {
    out << "GET key8781 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15417: delete
  db->Delete(write_options, "key1368");

  // op 15418: put
  db->Put(write_options, "key2106", "value13550");

  // op 15419: get
  status = db->Get(read_options, "key3230", &value);
  if (status.ok()) {
    out << "GET key3230 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15420: put
  db->Put(write_options, "key11080", "value61766");

  // op 15421: put
  db->Put(write_options, "key8890", "value15188");

  // op 15422: delete
  db->Delete(write_options, "key1202");

  // op 15423: put
  db->Put(write_options, "key10170", "value34431");

  // op 15424: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2963", "key8836", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2963, key8836) size=" << scan_result.size() << std::endl;
  }

  // op 15425: put
  db->Put(write_options, "key6435", "value64441");

  // op 15426: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0838", "key10288", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0838, key10288) size=" << scan_result.size() << std::endl;
  }

  // op 15427: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 15428: put
  db->Put(write_options, "key6351", "value77163");

  // op 15429: get
  status = db->Get(read_options, "key8533", &value);
  if (status.ok()) {
    out << "GET key8533 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15430: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1880", "key7857", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1880, key7857) size=" << scan_result.size() << std::endl;
  }

  // op 15431: put
  db->Put(write_options, "key9715", "value41879");

  // op 15432: delete_range
  status = db->DeleteRange(write_options, "key7284", "key9584");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 15433: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2764", "key5658", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2764, key5658) size=" << scan_result.size() << std::endl;
  }

  // op 15434: get
  status = db->Get(read_options, "key10321", &value);
  if (status.ok()) {
    out << "GET key10321 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15435: put
  db->Put(write_options, "key11051", "value77096");

  // op 15436: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 15437: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0401", "key2272", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0401, key2272) size=" << scan_result.size() << std::endl;
  }

  // op 15438: get
  status = db->Get(read_options, "key3870", &value);
  if (status.ok()) {
    out << "GET key3870 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15439: get
  status = db->Get(read_options, "key6325", &value);
  if (status.ok()) {
    out << "GET key6325 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15440: put
  db->Put(write_options, "key5187", "value82517");

  // op 15441: delete_range
  status = db->DeleteRange(write_options, "key0869", "key9758");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 15442: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0896", "key3148", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0896, key3148) size=" << scan_result.size() << std::endl;
  }

  // op 15443: get
  status = db->Get(read_options, "key1966", &value);
  if (status.ok()) {
    out << "GET key1966 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15444: put
  db->Put(write_options, "key11342", "value69401");

  // op 15445: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5191", "key10376", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5191, key10376) size=" << scan_result.size() << std::endl;
  }

  // op 15446: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 15447: get
  status = db->Get(read_options, "key9113", &value);
  if (status.ok()) {
    out << "GET key9113 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15448: put
  db->Put(write_options, "key2626", "value6356");

  // op 15449: get
  status = db->Get(read_options, "key9842", &value);
  if (status.ok()) {
    out << "GET key9842 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15450: get
  status = db->Get(read_options, "key4314", &value);
  if (status.ok()) {
    out << "GET key4314 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15451: delete
  db->Delete(write_options, "key0039");

  // op 15452: delete_range
  status = db->DeleteRange(write_options, "key0036", "key7034");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 15453: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6895", "key11295", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6895, key11295) size=" << scan_result.size() << std::endl;
  }

  // op 15454: put
  db->Put(write_options, "key2476", "value27433");

  // op 15455: put
  db->Put(write_options, "key9691", "value93904");

  // op 15456: put
  db->Put(write_options, "key9307", "value7726");

  // op 15457: put
  db->Put(write_options, "key0865", "value86961");

  // op 15458: put
  db->Put(write_options, "key9810", "value52955");

  // op 15459: put
  db->Put(write_options, "key6698", "value52323");

  // op 15460: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1004", "key11255", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1004, key11255) size=" << scan_result.size() << std::endl;
  }

  // op 15461: put
  db->Put(write_options, "key5699", "value101992");

  // op 15462: get
  status = db->Get(read_options, "key8734", &value);
  if (status.ok()) {
    out << "GET key8734 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15463: get
  status = db->Get(read_options, "key2961", &value);
  if (status.ok()) {
    out << "GET key2961 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15464: put
  db->Put(write_options, "key11169", "value101641");

  // op 15465: put
  db->Put(write_options, "key2311", "value102087");

  // op 15466: get
  status = db->Get(read_options, "key0606", &value);
  if (status.ok()) {
    out << "GET key0606 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15467: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0258", "key2771", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0258, key2771) size=" << scan_result.size() << std::endl;
  }

  // op 15468: put
  db->Put(write_options, "key8862", "value78510");

  // op 15469: delete
  db->Delete(write_options, "key8019");

  // op 15470: put
  db->Put(write_options, "key9511", "value37115");

  // op 15471: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0927", "key2845", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0927, key2845) size=" << scan_result.size() << std::endl;
  }

  // op 15472: put
  db->Put(write_options, "key5164", "value101573");

  // op 15473: put
  db->Put(write_options, "key5185", "value53321");

  // op 15474: put
  db->Put(write_options, "key8508", "value44621");

  // op 15475: get
  status = db->Get(read_options, "key2230", &value);
  if (status.ok()) {
    out << "GET key2230 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15476: put
  db->Put(write_options, "key8389", "value32336");

  // op 15477: get
  status = db->Get(read_options, "key6741", &value);
  if (status.ok()) {
    out << "GET key6741 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15478: put
  db->Put(write_options, "key0651", "value98188");

  // op 15479: put
  db->Put(write_options, "key1705", "value45796");

  // op 15480: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3025", "key11365", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3025, key11365) size=" << scan_result.size() << std::endl;
  }

  // op 15481: get
  status = db->Get(read_options, "key9118", &value);
  if (status.ok()) {
    out << "GET key9118 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15482: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7718", "key10781", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7718, key10781) size=" << scan_result.size() << std::endl;
  }

  // op 15483: put
  db->Put(write_options, "key4279", "value103287");

  // op 15484: get
  status = db->Get(read_options, "key2119", &value);
  if (status.ok()) {
    out << "GET key2119 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15485: put
  db->Put(write_options, "key7167", "value8952");

  // op 15486: put
  db->Put(write_options, "key5825", "value6255");

  // op 15487: get
  status = db->Get(read_options, "key0727", &value);
  if (status.ok()) {
    out << "GET key0727 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15488: delete_range
  status = db->DeleteRange(write_options, "key3162", "key9363");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 15489: put
  db->Put(write_options, "key10978", "value33965");

  // op 15490: put
  db->Put(write_options, "key9505", "value8926");

  // op 15491: delete_range
  status = db->DeleteRange(write_options, "key4562", "key5016");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 15492: put
  db->Put(write_options, "key11296", "value58949");

  // op 15493: get
  status = db->Get(read_options, "key7025", &value);
  if (status.ok()) {
    out << "GET key7025 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15494: put
  db->Put(write_options, "key9569", "value9747");

  // op 15495: delete_range
  status = db->DeleteRange(write_options, "key6082", "key11322");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 15496: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 15497: put
  db->Put(write_options, "key9125", "value72404");

  // op 15498: get
  status = db->Get(read_options, "key8316", &value);
  if (status.ok()) {
    out << "GET key8316 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15499: get
  status = db->Get(read_options, "key7900", &value);
  if (status.ok()) {
    out << "GET key7900 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15500: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4027", "key4603", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4027, key4603) size=" << scan_result.size() << std::endl;
  }

  // op 15501: delete_range
  status = db->DeleteRange(write_options, "key6199", "key7323");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 15502: put
  db->Put(write_options, "key4592", "value47446");

  // op 15503: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1038", "key10236", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1038, key10236) size=" << scan_result.size() << std::endl;
  }

  // op 15504: delete
  db->Delete(write_options, "key11241");

  // op 15505: put
  db->Put(write_options, "key7713", "value46007");

  // op 15506: put
  db->Put(write_options, "key5641", "value43366");

  // op 15507: put
  db->Put(write_options, "key7587", "value40504");

  // op 15508: get
  status = db->Get(read_options, "key9788", &value);
  if (status.ok()) {
    out << "GET key9788 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15509: put
  db->Put(write_options, "key1778", "value22571");

  // op 15510: get
  status = db->Get(read_options, "key4515", &value);
  if (status.ok()) {
    out << "GET key4515 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15511: delete
  db->Delete(write_options, "key4937");

  // op 15512: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0280", "key2510", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0280, key2510) size=" << scan_result.size() << std::endl;
  }

  // op 15513: delete
  db->Delete(write_options, "key10438");

  // op 15514: put
  db->Put(write_options, "key7325", "value94781");

  // op 15515: delete
  db->Delete(write_options, "key4222");

  // op 15516: get
  status = db->Get(read_options, "key1681", &value);
  if (status.ok()) {
    out << "GET key1681 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15517: delete_range
  status = db->DeleteRange(write_options, "key1007", "key11250");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 15518: put
  db->Put(write_options, "key5729", "value24544");

  // op 15519: put
  db->Put(write_options, "key10683", "value112436");

  // op 15520: put
  db->Put(write_options, "key10796", "value93275");

  // op 15521: delete_range
  status = db->DeleteRange(write_options, "key0997", "key5567");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 15522: put
  db->Put(write_options, "key3268", "value32977");

  // op 15523: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2269", "key10280", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2269, key10280) size=" << scan_result.size() << std::endl;
  }

  // op 15524: delete
  db->Delete(write_options, "key6560");

  // op 15525: get
  status = db->Get(read_options, "key1234", &value);
  if (status.ok()) {
    out << "GET key1234 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15526: put
  db->Put(write_options, "key8230", "value4217");

  // op 15527: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0550", "key2515", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0550, key2515) size=" << scan_result.size() << std::endl;
  }

  // op 15528: delete_range
  status = db->DeleteRange(write_options, "key3335", "key11297");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 15529: delete_range
  status = db->DeleteRange(write_options, "key3637", "key4699");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 15530: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 15531: put
  db->Put(write_options, "key2290", "value15430");

  // op 15532: get
  status = db->Get(read_options, "key5268", &value);
  if (status.ok()) {
    out << "GET key5268 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15533: get
  status = db->Get(read_options, "key0768", &value);
  if (status.ok()) {
    out << "GET key0768 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15534: put
  db->Put(write_options, "key11402", "value113420");

  // op 15535: put
  db->Put(write_options, "key0041", "value30576");

  // op 15536: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2145", "key10022", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2145, key10022) size=" << scan_result.size() << std::endl;
  }

  // op 15537: put
  db->Put(write_options, "key7083", "value34206");

  // op 15538: put
  db->Put(write_options, "key9897", "value54521");

  // op 15539: put
  db->Put(write_options, "key3144", "value56610");

  // op 15540: put
  db->Put(write_options, "key4424", "value99082");

  // op 15541: get
  status = db->Get(read_options, "key7429", &value);
  if (status.ok()) {
    out << "GET key7429 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15542: put
  db->Put(write_options, "key2078", "value92972");

  // op 15543: put
  db->Put(write_options, "key8554", "value82582");

  // op 15544: put
  db->Put(write_options, "key11102", "value65163");

  // op 15545: get
  status = db->Get(read_options, "key6306", &value);
  if (status.ok()) {
    out << "GET key6306 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15546: delete
  db->Delete(write_options, "key3215");

  // op 15547: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3083", "key7016", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3083, key7016) size=" << scan_result.size() << std::endl;
  }

  // op 15548: get
  status = db->Get(read_options, "key0579", &value);
  if (status.ok()) {
    out << "GET key0579 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15549: put
  db->Put(write_options, "key9294", "value8595");

  // op 15550: get
  status = db->Get(read_options, "key7646", &value);
  if (status.ok()) {
    out << "GET key7646 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15551: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4343", "key9946", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4343, key9946) size=" << scan_result.size() << std::endl;
  }

  // op 15552: put
  db->Put(write_options, "key2512", "value82443");

  // op 15553: delete
  db->Delete(write_options, "key0873");

  // op 15554: put
  db->Put(write_options, "key8355", "value16358");

  // op 15555: put
  db->Put(write_options, "key2734", "value25896");

  // op 15556: put
  db->Put(write_options, "key4726", "value45067");

  // op 15557: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8488", "key11401", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8488, key11401) size=" << scan_result.size() << std::endl;
  }

  // op 15558: put
  db->Put(write_options, "key5661", "value91927");

  // op 15559: get
  status = db->Get(read_options, "key8701", &value);
  if (status.ok()) {
    out << "GET key8701 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15560: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 15561: put
  db->Put(write_options, "key11613", "value51108");

  // op 15562: put
  db->Put(write_options, "key1418", "value51266");

  // op 15563: get
  status = db->Get(read_options, "key4071", &value);
  if (status.ok()) {
    out << "GET key4071 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15564: delete_range
  status = db->DeleteRange(write_options, "key6376", "key9196");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 15565: put
  db->Put(write_options, "key8340", "value100735");

  // op 15566: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1687", "key5834", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1687, key5834) size=" << scan_result.size() << std::endl;
  }

  // op 15567: get
  status = db->Get(read_options, "key7175", &value);
  if (status.ok()) {
    out << "GET key7175 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15568: put
  db->Put(write_options, "key2906", "value113506");

  // op 15569: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 15570: put
  db->Put(write_options, "key4028", "value37655");

  // op 15571: delete
  db->Delete(write_options, "key5161");

  // op 15572: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 15573: put
  db->Put(write_options, "key9539", "value13202");

  // op 15574: get
  status = db->Get(read_options, "key2424", &value);
  if (status.ok()) {
    out << "GET key2424 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15575: delete
  db->Delete(write_options, "key2866");

  // op 15576: get
  status = db->Get(read_options, "key6557", &value);
  if (status.ok()) {
    out << "GET key6557 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15577: put
  db->Put(write_options, "key0862", "value7370");

  // op 15578: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3349", "key8334", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3349, key8334) size=" << scan_result.size() << std::endl;
  }

  // op 15579: put
  db->Put(write_options, "key10744", "value66571");

  // op 15580: get
  status = db->Get(read_options, "key5501", &value);
  if (status.ok()) {
    out << "GET key5501 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15581: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0478", "key6977", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0478, key6977) size=" << scan_result.size() << std::endl;
  }

  // op 15582: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5042", "key11686", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5042, key11686) size=" << scan_result.size() << std::endl;
  }

  // op 15583: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 15584: delete
  db->Delete(write_options, "key7312");

  // op 15585: put
  db->Put(write_options, "key6082", "value0763");

  // op 15586: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1431", "key6764", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1431, key6764) size=" << scan_result.size() << std::endl;
  }

  // op 15587: put
  db->Put(write_options, "key8105", "value86443");

  // op 15588: delete
  db->Delete(write_options, "key1328");

  // op 15589: put
  db->Put(write_options, "key5921", "value37676");

  // op 15590: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 15591: delete_range
  status = db->DeleteRange(write_options, "key4598", "key4762");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 15592: delete_range
  status = db->DeleteRange(write_options, "key0113", "key1336");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 15593: get
  status = db->Get(read_options, "key8306", &value);
  if (status.ok()) {
    out << "GET key8306 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15594: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2194", "key11353", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2194, key11353) size=" << scan_result.size() << std::endl;
  }

  // op 15595: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4655", "key4771", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4655, key4771) size=" << scan_result.size() << std::endl;
  }

  // op 15596: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 15597: put
  db->Put(write_options, "key9273", "value96955");

  // op 15598: put
  db->Put(write_options, "key10330", "value64063");

  // op 15599: delete_range
  status = db->DeleteRange(write_options, "key5437", "key5902");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 15600: get
  status = db->Get(read_options, "key2843", &value);
  if (status.ok()) {
    out << "GET key2843 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15601: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0494", "key8775", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0494, key8775) size=" << scan_result.size() << std::endl;
  }

  // op 15602: put
  db->Put(write_options, "key5659", "value99892");

  // op 15603: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0872", "key6210", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0872, key6210) size=" << scan_result.size() << std::endl;
  }

  // op 15604: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7020", "key9112", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7020, key9112) size=" << scan_result.size() << std::endl;
  }

  // op 15605: get
  status = db->Get(read_options, "key0445", &value);
  if (status.ok()) {
    out << "GET key0445 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15606: put
  db->Put(write_options, "key7370", "value39070");

  // op 15607: delete_range
  status = db->DeleteRange(write_options, "key0194", "key6758");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 15608: delete_range
  status = db->DeleteRange(write_options, "key2059", "key6619");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 15609: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4244", "key11556", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4244, key11556) size=" << scan_result.size() << std::endl;
  }

  // op 15610: put
  db->Put(write_options, "key5759", "value89998");

  // op 15611: put
  db->Put(write_options, "key3140", "value68960");

  // op 15612: put
  db->Put(write_options, "key4788", "value72074");

  // op 15613: put
  db->Put(write_options, "key5797", "value28067");

  // op 15614: put
  db->Put(write_options, "key0544", "value82792");

  // op 15615: get
  status = db->Get(read_options, "key5218", &value);
  if (status.ok()) {
    out << "GET key5218 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15616: put
  db->Put(write_options, "key7924", "value86112");

  // op 15617: get
  status = db->Get(read_options, "key10933", &value);
  if (status.ok()) {
    out << "GET key10933 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15618: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7950", "key9562", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7950, key9562) size=" << scan_result.size() << std::endl;
  }

  // op 15619: get
  status = db->Get(read_options, "key8461", &value);
  if (status.ok()) {
    out << "GET key8461 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15620: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0610", "key1505", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0610, key1505) size=" << scan_result.size() << std::endl;
  }

  // op 15621: get
  status = db->Get(read_options, "key6659", &value);
  if (status.ok()) {
    out << "GET key6659 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15622: put
  db->Put(write_options, "key7981", "value8284");

  // op 15623: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5096", "key8554", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5096, key8554) size=" << scan_result.size() << std::endl;
  }

  // op 15624: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6566", "key10037", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6566, key10037) size=" << scan_result.size() << std::endl;
  }

  // op 15625: get
  status = db->Get(read_options, "key0738", &value);
  if (status.ok()) {
    out << "GET key0738 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15626: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 15627: delete_range
  status = db->DeleteRange(write_options, "key3567", "key10704");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 15628: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2210", "key3773", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2210, key3773) size=" << scan_result.size() << std::endl;
  }

  // op 15629: put
  db->Put(write_options, "key1537", "value34022");

  // op 15630: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4397", "key10881", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4397, key10881) size=" << scan_result.size() << std::endl;
  }

  // op 15631: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6723", "key9783", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6723, key9783) size=" << scan_result.size() << std::endl;
  }

  // op 15632: get
  status = db->Get(read_options, "key4317", &value);
  if (status.ok()) {
    out << "GET key4317 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15633: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8522", "key10118", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8522, key10118) size=" << scan_result.size() << std::endl;
  }

  // op 15634: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7809", "key10962", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7809, key10962) size=" << scan_result.size() << std::endl;
  }

  // op 15635: put
  db->Put(write_options, "key6424", "value40412");

  // op 15636: get
  status = db->Get(read_options, "key10113", &value);
  if (status.ok()) {
    out << "GET key10113 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15637: delete
  db->Delete(write_options, "key4015");

  // op 15638: put
  db->Put(write_options, "key11343", "value91303");

  // op 15639: put
  db->Put(write_options, "key5407", "value48876");

  // op 15640: delete
  db->Delete(write_options, "key2693");

  // op 15641: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2897", "key7867", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2897, key7867) size=" << scan_result.size() << std::endl;
  }

  // op 15642: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4066", "key10970", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4066, key10970) size=" << scan_result.size() << std::endl;
  }

  // op 15643: put
  db->Put(write_options, "key2062", "value16280");

  // op 15644: put
  db->Put(write_options, "key7365", "value75829");

  // op 15645: put
  db->Put(write_options, "key0311", "value70481");

  // op 15646: delete_range
  status = db->DeleteRange(write_options, "key7613", "key9488");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 15647: delete_range
  status = db->DeleteRange(write_options, "key1776", "key9433");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 15648: put
  db->Put(write_options, "key9405", "value61381");

  // op 15649: get
  status = db->Get(read_options, "key0679", &value);
  if (status.ok()) {
    out << "GET key0679 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15650: delete
  db->Delete(write_options, "key0272");

  // op 15651: delete
  db->Delete(write_options, "key5868");

  // op 15652: put
  db->Put(write_options, "key8115", "value18072");

  // op 15653: delete
  db->Delete(write_options, "key10043");

  // op 15654: put
  db->Put(write_options, "key5190", "value31461");

  // op 15655: put
  db->Put(write_options, "key9029", "value44315");

  // op 15656: get
  status = db->Get(read_options, "key2591", &value);
  if (status.ok()) {
    out << "GET key2591 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15657: delete
  db->Delete(write_options, "key1674");

  // op 15658: put
  db->Put(write_options, "key9110", "value74610");

  // op 15659: put
  db->Put(write_options, "key5790", "value98762");

  // op 15660: get
  status = db->Get(read_options, "key0747", &value);
  if (status.ok()) {
    out << "GET key0747 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15661: put
  db->Put(write_options, "key11378", "value42165");

  // op 15662: put
  db->Put(write_options, "key1868", "value37430");

  // op 15663: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5019", "key8775", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5019, key8775) size=" << scan_result.size() << std::endl;
  }

  // op 15664: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6429", "key10173", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6429, key10173) size=" << scan_result.size() << std::endl;
  }

  // op 15665: delete
  db->Delete(write_options, "key5380");

  // op 15666: put
  db->Put(write_options, "key2367", "value95066");

  // op 15667: get
  status = db->Get(read_options, "key1421", &value);
  if (status.ok()) {
    out << "GET key1421 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15668: delete
  db->Delete(write_options, "key2880");

  // op 15669: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4780", "key5914", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4780, key5914) size=" << scan_result.size() << std::endl;
  }

  // op 15670: delete_range
  status = db->DeleteRange(write_options, "key3341", "key7243");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 15671: put
  db->Put(write_options, "key9574", "value60444");

  // op 15672: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5275", "key5969", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5275, key5969) size=" << scan_result.size() << std::endl;
  }

  // op 15673: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4197", "key6170", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4197, key6170) size=" << scan_result.size() << std::endl;
  }

  // op 15674: put
  db->Put(write_options, "key10148", "value23866");

  // op 15675: delete
  db->Delete(write_options, "key7346");

  // op 15676: get
  status = db->Get(read_options, "key5320", &value);
  if (status.ok()) {
    out << "GET key5320 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15677: put
  db->Put(write_options, "key10162", "value111885");

  // op 15678: get
  status = db->Get(read_options, "key1492", &value);
  if (status.ok()) {
    out << "GET key1492 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15679: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7815", "key10358", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7815, key10358) size=" << scan_result.size() << std::endl;
  }

  // op 15680: delete_range
  status = db->DeleteRange(write_options, "key3512", "key7159");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 15681: delete_range
  status = db->DeleteRange(write_options, "key7640", "key9484");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 15682: get
  status = db->Get(read_options, "key2331", &value);
  if (status.ok()) {
    out << "GET key2331 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15683: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8456", "key10953", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8456, key10953) size=" << scan_result.size() << std::endl;
  }

  // op 15684: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1805", "key4258", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1805, key4258) size=" << scan_result.size() << std::endl;
  }

  // op 15685: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7090", "key10742", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7090, key10742) size=" << scan_result.size() << std::endl;
  }

  // op 15686: put
  db->Put(write_options, "key6055", "value96741");

  // op 15687: put
  db->Put(write_options, "key5293", "value91410");

  // op 15688: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6798", "key8062", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6798, key8062) size=" << scan_result.size() << std::endl;
  }

  // op 15689: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0365", "key4396", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0365, key4396) size=" << scan_result.size() << std::endl;
  }

  // op 15690: delete
  db->Delete(write_options, "key1404");

  // op 15691: get
  status = db->Get(read_options, "key9057", &value);
  if (status.ok()) {
    out << "GET key9057 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15692: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6166", "key11651", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6166, key11651) size=" << scan_result.size() << std::endl;
  }

  // op 15693: put
  db->Put(write_options, "key9423", "value47150");

  // op 15694: put
  db->Put(write_options, "key1238", "value54315");

  // op 15695: put
  db->Put(write_options, "key1639", "value41432");

  // op 15696: put
  db->Put(write_options, "key8203", "value83798");

  // op 15697: put
  db->Put(write_options, "key9071", "value16516");

  // op 15698: delete
  db->Delete(write_options, "key8301");

  // op 15699: delete_range
  status = db->DeleteRange(write_options, "key4760", "key5947");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 15700: put
  db->Put(write_options, "key1846", "value113429");

  // op 15701: delete_range
  status = db->DeleteRange(write_options, "key5897", "key11443");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 15702: delete
  db->Delete(write_options, "key9707");

  // op 15703: put
  db->Put(write_options, "key6038", "value39577");

  // op 15704: put
  db->Put(write_options, "key2256", "value69596");

  // op 15705: put
  db->Put(write_options, "key5571", "value44596");

  // op 15706: put
  db->Put(write_options, "key0589", "value8768");

  // op 15707: get
  status = db->Get(read_options, "key5683", &value);
  if (status.ok()) {
    out << "GET key5683 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15708: delete
  db->Delete(write_options, "key6921");

  // op 15709: put
  db->Put(write_options, "key2754", "value69110");

  // op 15710: put
  db->Put(write_options, "key1750", "value92705");

  // op 15711: put
  db->Put(write_options, "key11574", "value3607");

  // op 15712: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0221", "key11363", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0221, key11363) size=" << scan_result.size() << std::endl;
  }

  // op 15713: get
  status = db->Get(read_options, "key5551", &value);
  if (status.ok()) {
    out << "GET key5551 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15714: delete
  db->Delete(write_options, "key2975");

  // op 15715: delete_range
  status = db->DeleteRange(write_options, "key6788", "key7973");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 15716: delete
  db->Delete(write_options, "key9891");

  // op 15717: delete
  db->Delete(write_options, "key1400");

  // op 15718: delete
  db->Delete(write_options, "key6099");

  // op 15719: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0151", "key3022", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0151, key3022) size=" << scan_result.size() << std::endl;
  }

  // op 15720: delete
  db->Delete(write_options, "key4420");

  // op 15721: put
  db->Put(write_options, "key6503", "value77612");

  // op 15722: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7542", "key10447", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7542, key10447) size=" << scan_result.size() << std::endl;
  }

  // op 15723: put
  db->Put(write_options, "key11300", "value14814");

  // op 15724: put
  db->Put(write_options, "key10339", "value88449");

  // op 15725: put
  db->Put(write_options, "key2858", "value18047");

  // op 15726: put
  db->Put(write_options, "key2478", "value68573");

  // op 15727: put
  db->Put(write_options, "key1215", "value57705");

  // op 15728: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4643", "key10113", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4643, key10113) size=" << scan_result.size() << std::endl;
  }

  // op 15729: put
  db->Put(write_options, "key2953", "value110692");

  // op 15730: put
  db->Put(write_options, "key10829", "value36942");

  // op 15731: put
  db->Put(write_options, "key11585", "value16728");

  // op 15732: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0611", "key5883", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0611, key5883) size=" << scan_result.size() << std::endl;
  }

  // op 15733: put
  db->Put(write_options, "key6577", "value23894");

  // op 15734: put
  db->Put(write_options, "key9303", "value13387");

  // op 15735: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0022", "key10208", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0022, key10208) size=" << scan_result.size() << std::endl;
  }

  // op 15736: delete
  db->Delete(write_options, "key0917");

  // op 15737: put
  db->Put(write_options, "key9114", "value17948");

  // op 15738: put
  db->Put(write_options, "key0109", "value25141");

  // op 15739: delete
  db->Delete(write_options, "key9716");

  // op 15740: put
  db->Put(write_options, "key10480", "value73602");

  // op 15741: delete
  db->Delete(write_options, "key11042");

  // op 15742: delete
  db->Delete(write_options, "key9191");

  // op 15743: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 15744: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5013", "key5376", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5013, key5376) size=" << scan_result.size() << std::endl;
  }

  // op 15745: get
  status = db->Get(read_options, "key5882", &value);
  if (status.ok()) {
    out << "GET key5882 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15746: get
  status = db->Get(read_options, "key3643", &value);
  if (status.ok()) {
    out << "GET key3643 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15747: get
  status = db->Get(read_options, "key9927", &value);
  if (status.ok()) {
    out << "GET key9927 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15748: delete
  db->Delete(write_options, "key3297");

  // op 15749: get
  status = db->Get(read_options, "key3116", &value);
  if (status.ok()) {
    out << "GET key3116 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15750: delete_range
  status = db->DeleteRange(write_options, "key1024", "key3644");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 15751: put
  db->Put(write_options, "key6263", "value84658");

  // op 15752: put
  db->Put(write_options, "key4531", "value45609");

  // op 15753: put
  db->Put(write_options, "key6583", "value17450");

  // op 15754: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5734", "key10724", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5734, key10724) size=" << scan_result.size() << std::endl;
  }

  // op 15755: get
  status = db->Get(read_options, "key4879", &value);
  if (status.ok()) {
    out << "GET key4879 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15756: put
  db->Put(write_options, "key10725", "value43625");

  // op 15757: delete
  db->Delete(write_options, "key6445");

  // op 15758: delete
  db->Delete(write_options, "key8361");

  // op 15759: put
  db->Put(write_options, "key1553", "value83951");

  // op 15760: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4471", "key6238", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4471, key6238) size=" << scan_result.size() << std::endl;
  }

  // op 15761: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2083", "key4822", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2083, key4822) size=" << scan_result.size() << std::endl;
  }

  // op 15762: put
  db->Put(write_options, "key6621", "value74811");

  // op 15763: put
  db->Put(write_options, "key1670", "value56560");

  // op 15764: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0719", "key11306", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0719, key11306) size=" << scan_result.size() << std::endl;
  }

  // op 15765: put
  db->Put(write_options, "key2261", "value108358");

  // op 15766: put
  db->Put(write_options, "key11014", "value69815");

  // op 15767: delete
  db->Delete(write_options, "key5964");

  // op 15768: put
  db->Put(write_options, "key7422", "value55046");

  // op 15769: put
  db->Put(write_options, "key1814", "value63644");

  // op 15770: delete
  db->Delete(write_options, "key5003");

  // op 15771: delete
  db->Delete(write_options, "key0460");

  // op 15772: put
  db->Put(write_options, "key2573", "value56782");

  // op 15773: put
  db->Put(write_options, "key9091", "value43426");

  // op 15774: put
  db->Put(write_options, "key10133", "value85285");

  // op 15775: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7715", "key10095", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7715, key10095) size=" << scan_result.size() << std::endl;
  }

  // op 15776: put
  db->Put(write_options, "key8480", "value10726");

  // op 15777: get
  status = db->Get(read_options, "key3767", &value);
  if (status.ok()) {
    out << "GET key3767 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15778: put
  db->Put(write_options, "key7143", "value89828");

  // op 15779: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2559", "key3254", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2559, key3254) size=" << scan_result.size() << std::endl;
  }

  // op 15780: put
  db->Put(write_options, "key8472", "value88882");

  // op 15781: put
  db->Put(write_options, "key10051", "value83890");

  // op 15782: put
  db->Put(write_options, "key4645", "value34943");

  // op 15783: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 15784: put
  db->Put(write_options, "key7717", "value83619");

  // op 15785: get
  status = db->Get(read_options, "key9590", &value);
  if (status.ok()) {
    out << "GET key9590 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15786: put
  db->Put(write_options, "key2671", "value32310");

  // op 15787: put
  db->Put(write_options, "key11035", "value61843");

  // op 15788: delete_range
  status = db->DeleteRange(write_options, "key4427", "key10847");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 15789: get
  status = db->Get(read_options, "key5829", &value);
  if (status.ok()) {
    out << "GET key5829 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15790: put
  db->Put(write_options, "key6704", "value70514");

  // op 15791: put
  db->Put(write_options, "key9579", "value30122");

  // op 15792: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1301", "key6924", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1301, key6924) size=" << scan_result.size() << std::endl;
  }

  // op 15793: delete
  db->Delete(write_options, "key10298");

  // op 15794: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8029", "key10297", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8029, key10297) size=" << scan_result.size() << std::endl;
  }

  // op 15795: get
  status = db->Get(read_options, "key9324", &value);
  if (status.ok()) {
    out << "GET key9324 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15796: get
  status = db->Get(read_options, "key1250", &value);
  if (status.ok()) {
    out << "GET key1250 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15797: delete_range
  status = db->DeleteRange(write_options, "key1885", "key4455");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 15798: get
  status = db->Get(read_options, "key5525", &value);
  if (status.ok()) {
    out << "GET key5525 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15799: put
  db->Put(write_options, "key9258", "value44078");

  // op 15800: get
  status = db->Get(read_options, "key4544", &value);
  if (status.ok()) {
    out << "GET key4544 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15801: put
  db->Put(write_options, "key3742", "value38817");

  // op 15802: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1744", "key10197", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1744, key10197) size=" << scan_result.size() << std::endl;
  }

  // op 15803: put
  db->Put(write_options, "key8532", "value42829");

  // op 15804: get
  status = db->Get(read_options, "key8496", &value);
  if (status.ok()) {
    out << "GET key8496 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15805: put
  db->Put(write_options, "key10747", "value89572");

  // op 15806: put
  db->Put(write_options, "key0197", "value47628");

  // op 15807: put
  db->Put(write_options, "key9125", "value37398");

  // op 15808: put
  db->Put(write_options, "key6001", "value56164");

  // op 15809: get
  status = db->Get(read_options, "key7565", &value);
  if (status.ok()) {
    out << "GET key7565 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15810: get
  status = db->Get(read_options, "key5754", &value);
  if (status.ok()) {
    out << "GET key5754 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15811: put
  db->Put(write_options, "key6918", "value89171");

  // op 15812: put
  db->Put(write_options, "key4801", "value37507");

  // op 15813: get
  status = db->Get(read_options, "key9621", &value);
  if (status.ok()) {
    out << "GET key9621 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15814: put
  db->Put(write_options, "key1261", "value90683");

  // op 15815: delete_range
  status = db->DeleteRange(write_options, "key0276", "key2029");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 15816: delete_range
  status = db->DeleteRange(write_options, "key2208", "key3150");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 15817: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 15818: put
  db->Put(write_options, "key5388", "value9529");

  // op 15819: put
  db->Put(write_options, "key9007", "value35146");

  // op 15820: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1661", "key10476", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1661, key10476) size=" << scan_result.size() << std::endl;
  }

  // op 15821: get
  status = db->Get(read_options, "key7720", &value);
  if (status.ok()) {
    out << "GET key7720 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15822: put
  db->Put(write_options, "key2605", "value35275");

  // op 15823: put
  db->Put(write_options, "key3052", "value51152");

  // op 15824: put
  db->Put(write_options, "key0785", "value106892");

  // op 15825: put
  db->Put(write_options, "key1577", "value65406");

  // op 15826: delete
  db->Delete(write_options, "key3270");

  // op 15827: put
  db->Put(write_options, "key4613", "value21295");

  // op 15828: put
  db->Put(write_options, "key11259", "value22087");

  // op 15829: put
  db->Put(write_options, "key2496", "value37727");

  // op 15830: get
  status = db->Get(read_options, "key8237", &value);
  if (status.ok()) {
    out << "GET key8237 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15831: put
  db->Put(write_options, "key3880", "value30736");

  // op 15832: put
  db->Put(write_options, "key5216", "value21359");

  // op 15833: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 15834: get
  status = db->Get(read_options, "key5254", &value);
  if (status.ok()) {
    out << "GET key5254 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15835: get
  status = db->Get(read_options, "key7828", &value);
  if (status.ok()) {
    out << "GET key7828 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15836: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3112", "key10841", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3112, key10841) size=" << scan_result.size() << std::endl;
  }

  // op 15837: put
  db->Put(write_options, "key5929", "value81785");

  // op 15838: get
  status = db->Get(read_options, "key0773", &value);
  if (status.ok()) {
    out << "GET key0773 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15839: put
  db->Put(write_options, "key10718", "value51945");

  // op 15840: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5817", "key11336", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5817, key11336) size=" << scan_result.size() << std::endl;
  }

  // op 15841: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2012", "key10495", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2012, key10495) size=" << scan_result.size() << std::endl;
  }

  // op 15842: delete_range
  status = db->DeleteRange(write_options, "key6530", "key11179");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 15843: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5344", "key8510", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5344, key8510) size=" << scan_result.size() << std::endl;
  }

  // op 15844: get
  status = db->Get(read_options, "key5304", &value);
  if (status.ok()) {
    out << "GET key5304 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15845: delete
  db->Delete(write_options, "key5054");

  // op 15846: delete
  db->Delete(write_options, "key0667");

  // op 15847: put
  db->Put(write_options, "key4184", "value110492");

  // op 15848: delete
  db->Delete(write_options, "key10170");

  // op 15849: delete_range
  status = db->DeleteRange(write_options, "key5246", "key6899");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 15850: get
  status = db->Get(read_options, "key1525", &value);
  if (status.ok()) {
    out << "GET key1525 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15851: delete_range
  status = db->DeleteRange(write_options, "key0267", "key3530");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 15852: put
  db->Put(write_options, "key5002", "value109521");

  // op 15853: put
  db->Put(write_options, "key6196", "value23433");

  // op 15854: put
  db->Put(write_options, "key3202", "value10021");

  // op 15855: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3409", "key6138", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3409, key6138) size=" << scan_result.size() << std::endl;
  }

  // op 15856: put
  db->Put(write_options, "key4848", "value33474");

  // op 15857: delete
  db->Delete(write_options, "key6333");

  // op 15858: get
  status = db->Get(read_options, "key7670", &value);
  if (status.ok()) {
    out << "GET key7670 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15859: put
  db->Put(write_options, "key7129", "value34560");

  // op 15860: get
  status = db->Get(read_options, "key5135", &value);
  if (status.ok()) {
    out << "GET key5135 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15861: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0089", "key3621", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0089, key3621) size=" << scan_result.size() << std::endl;
  }

  // op 15862: get
  status = db->Get(read_options, "key3467", &value);
  if (status.ok()) {
    out << "GET key3467 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15863: get
  status = db->Get(read_options, "key3425", &value);
  if (status.ok()) {
    out << "GET key3425 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15864: put
  db->Put(write_options, "key0726", "value76302");

  // op 15865: get
  status = db->Get(read_options, "key10147", &value);
  if (status.ok()) {
    out << "GET key10147 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15866: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4273", "key7179", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4273, key7179) size=" << scan_result.size() << std::endl;
  }

  // op 15867: get
  status = db->Get(read_options, "key2845", &value);
  if (status.ok()) {
    out << "GET key2845 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15868: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3641", "key3746", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3641, key3746) size=" << scan_result.size() << std::endl;
  }

  // op 15869: put
  db->Put(write_options, "key1751", "value1862");

  // op 15870: put
  db->Put(write_options, "key8833", "value80080");

  // op 15871: put
  db->Put(write_options, "key6934", "value115040");

  // op 15872: get
  status = db->Get(read_options, "key6965", &value);
  if (status.ok()) {
    out << "GET key6965 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15873: put
  db->Put(write_options, "key8609", "value2404");

  // op 15874: put
  db->Put(write_options, "key3867", "value40180");

  // op 15875: put
  db->Put(write_options, "key2007", "value103940");

  // op 15876: delete
  db->Delete(write_options, "key7121");

  // op 15877: delete_range
  status = db->DeleteRange(write_options, "key2839", "key11258");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 15878: put
  db->Put(write_options, "key10307", "value76425");

  // op 15879: get
  status = db->Get(read_options, "key1179", &value);
  if (status.ok()) {
    out << "GET key1179 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15880: put
  db->Put(write_options, "key7214", "value102610");

  // op 15881: put
  db->Put(write_options, "key11100", "value105423");

  // op 15882: get
  status = db->Get(read_options, "key4118", &value);
  if (status.ok()) {
    out << "GET key4118 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15883: get
  status = db->Get(read_options, "key10872", &value);
  if (status.ok()) {
    out << "GET key10872 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15884: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8068", "key9000", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8068, key9000) size=" << scan_result.size() << std::endl;
  }

  // op 15885: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4633", "key11234", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4633, key11234) size=" << scan_result.size() << std::endl;
  }

  // op 15886: get
  status = db->Get(read_options, "key7512", &value);
  if (status.ok()) {
    out << "GET key7512 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15887: put
  db->Put(write_options, "key10241", "value103179");

  // op 15888: delete_range
  status = db->DeleteRange(write_options, "key2536", "key4749");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 15889: delete_range
  status = db->DeleteRange(write_options, "key3123", "key10562");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 15890: put
  db->Put(write_options, "key7260", "value19557");

  // op 15891: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0619", "key6565", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0619, key6565) size=" << scan_result.size() << std::endl;
  }

  // op 15892: put
  db->Put(write_options, "key2690", "value58705");

  // op 15893: put
  db->Put(write_options, "key1157", "value52246");

  // op 15894: get
  status = db->Get(read_options, "key6450", &value);
  if (status.ok()) {
    out << "GET key6450 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15895: put
  db->Put(write_options, "key6804", "value33558");

  // op 15896: put
  db->Put(write_options, "key9360", "value46385");

  // op 15897: delete_range
  status = db->DeleteRange(write_options, "key0155", "key0603");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 15898: get
  status = db->Get(read_options, "key8254", &value);
  if (status.ok()) {
    out << "GET key8254 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15899: put
  db->Put(write_options, "key7103", "value28492");

  // op 15900: put
  db->Put(write_options, "key9699", "value107531");

  // op 15901: put
  db->Put(write_options, "key6157", "value44315");

  // op 15902: delete
  db->Delete(write_options, "key3691");

  // op 15903: delete
  db->Delete(write_options, "key7773");

  // op 15904: put
  db->Put(write_options, "key5654", "value91206");

  // op 15905: get
  status = db->Get(read_options, "key6476", &value);
  if (status.ok()) {
    out << "GET key6476 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15906: put
  db->Put(write_options, "key4325", "value0237");

  // op 15907: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3320", "key9503", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3320, key9503) size=" << scan_result.size() << std::endl;
  }

  // op 15908: put
  db->Put(write_options, "key8269", "value66158");

  // op 15909: put
  db->Put(write_options, "key9956", "value46981");

  // op 15910: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3178", "key3234", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3178, key3234) size=" << scan_result.size() << std::endl;
  }

  // op 15911: get
  status = db->Get(read_options, "key6538", &value);
  if (status.ok()) {
    out << "GET key6538 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15912: delete_range
  status = db->DeleteRange(write_options, "key8127", "key8782");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 15913: put
  db->Put(write_options, "key3788", "value110719");

  // op 15914: get
  status = db->Get(read_options, "key6164", &value);
  if (status.ok()) {
    out << "GET key6164 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15915: put
  db->Put(write_options, "key0473", "value70972");

  // op 15916: put
  db->Put(write_options, "key0592", "value23740");

  // op 15917: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 15918: get
  status = db->Get(read_options, "key10953", &value);
  if (status.ok()) {
    out << "GET key10953 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15919: get
  status = db->Get(read_options, "key9071", &value);
  if (status.ok()) {
    out << "GET key9071 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15920: get
  status = db->Get(read_options, "key1082", &value);
  if (status.ok()) {
    out << "GET key1082 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15921: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1631", "key10484", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1631, key10484) size=" << scan_result.size() << std::endl;
  }

  // op 15922: put
  db->Put(write_options, "key9253", "value88397");

  // op 15923: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 15924: put
  db->Put(write_options, "key1192", "value33262");

  // op 15925: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3144", "key8376", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3144, key8376) size=" << scan_result.size() << std::endl;
  }

  // op 15926: get
  status = db->Get(read_options, "key2050", &value);
  if (status.ok()) {
    out << "GET key2050 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15927: put
  db->Put(write_options, "key11020", "value7872");

  // op 15928: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 15929: delete_range
  status = db->DeleteRange(write_options, "key4678", "key11308");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 15930: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 15931: delete
  db->Delete(write_options, "key10116");

  // op 15932: get
  status = db->Get(read_options, "key0688", &value);
  if (status.ok()) {
    out << "GET key0688 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15933: delete_range
  status = db->DeleteRange(write_options, "key4746", "key7062");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 15934: delete_range
  status = db->DeleteRange(write_options, "key1609", "key9256");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 15935: put
  db->Put(write_options, "key2427", "value57005");

  // op 15936: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4093", "key9935", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4093, key9935) size=" << scan_result.size() << std::endl;
  }

  // op 15937: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1941", "key7425", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1941, key7425) size=" << scan_result.size() << std::endl;
  }

  // op 15938: delete_range
  status = db->DeleteRange(write_options, "key3752", "key4342");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 15939: get
  status = db->Get(read_options, "key2775", &value);
  if (status.ok()) {
    out << "GET key2775 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15940: put
  db->Put(write_options, "key11415", "value41969");

  // op 15941: delete_range
  status = db->DeleteRange(write_options, "key6900", "key10938");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 15942: get
  status = db->Get(read_options, "key2879", &value);
  if (status.ok()) {
    out << "GET key2879 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15943: get
  status = db->Get(read_options, "key8548", &value);
  if (status.ok()) {
    out << "GET key8548 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15944: put
  db->Put(write_options, "key9598", "value91950");

  // op 15945: put
  db->Put(write_options, "key10797", "value59930");

  // op 15946: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2696", "key9289", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2696, key9289) size=" << scan_result.size() << std::endl;
  }

  // op 15947: put
  db->Put(write_options, "key10484", "value29042");

  // op 15948: delete_range
  status = db->DeleteRange(write_options, "key3455", "key9328");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 15949: delete
  db->Delete(write_options, "key9657");

  // op 15950: get
  status = db->Get(read_options, "key10973", &value);
  if (status.ok()) {
    out << "GET key10973 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15951: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0778", "key8201", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0778, key8201) size=" << scan_result.size() << std::endl;
  }

  // op 15952: delete_range
  status = db->DeleteRange(write_options, "key5339", "key6649");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 15953: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0946", "key9780", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0946, key9780) size=" << scan_result.size() << std::endl;
  }

  // op 15954: put
  db->Put(write_options, "key1479", "value32185");

  // op 15955: delete_range
  status = db->DeleteRange(write_options, "key1485", "key7113");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 15956: put
  db->Put(write_options, "key1590", "value51684");

  // op 15957: put
  db->Put(write_options, "key1382", "value106014");

  // op 15958: get
  status = db->Get(read_options, "key10412", &value);
  if (status.ok()) {
    out << "GET key10412 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15959: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0493", "key7696", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0493, key7696) size=" << scan_result.size() << std::endl;
  }

  // op 15960: put
  db->Put(write_options, "key11437", "value116930");

  // op 15961: put
  db->Put(write_options, "key7423", "value28818");

  // op 15962: put
  db->Put(write_options, "key7428", "value34144");

  // op 15963: put
  db->Put(write_options, "key2494", "value114254");

  // op 15964: get
  status = db->Get(read_options, "key3447", &value);
  if (status.ok()) {
    out << "GET key3447 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15965: delete
  db->Delete(write_options, "key5736");

  // op 15966: delete_range
  status = db->DeleteRange(write_options, "key10101", "key10906");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 15967: delete
  db->Delete(write_options, "key6852");

  // op 15968: put
  db->Put(write_options, "key1915", "value61955");

  // op 15969: get
  status = db->Get(read_options, "key8013", &value);
  if (status.ok()) {
    out << "GET key8013 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15970: put
  db->Put(write_options, "key5256", "value30676");

  // op 15971: get
  status = db->Get(read_options, "key8183", &value);
  if (status.ok()) {
    out << "GET key8183 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15972: get
  status = db->Get(read_options, "key9994", &value);
  if (status.ok()) {
    out << "GET key9994 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15973: delete
  db->Delete(write_options, "key4389");

  // op 15974: put
  db->Put(write_options, "key7638", "value2871");

  // op 15975: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2792", "key7214", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2792, key7214) size=" << scan_result.size() << std::endl;
  }

  // op 15976: delete
  db->Delete(write_options, "key7891");

  // op 15977: put
  db->Put(write_options, "key7166", "value12179");

  // op 15978: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0625", "key7715", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0625, key7715) size=" << scan_result.size() << std::endl;
  }

  // op 15979: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5697", "key7434", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5697, key7434) size=" << scan_result.size() << std::endl;
  }

  // op 15980: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3326", "key3639", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3326, key3639) size=" << scan_result.size() << std::endl;
  }

  // op 15981: get
  status = db->Get(read_options, "key2505", &value);
  if (status.ok()) {
    out << "GET key2505 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15982: delete
  db->Delete(write_options, "key4242");

  // op 15983: delete
  db->Delete(write_options, "key7681");

  // op 15984: get
  status = db->Get(read_options, "key6664", &value);
  if (status.ok()) {
    out << "GET key6664 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15985: put
  db->Put(write_options, "key3874", "value25443");

  // op 15986: put
  db->Put(write_options, "key7574", "value71791");

  // op 15987: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2953", "key10729", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2953, key10729) size=" << scan_result.size() << std::endl;
  }

  // op 15988: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2674", "key6872", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2674, key6872) size=" << scan_result.size() << std::endl;
  }

  // op 15989: get
  status = db->Get(read_options, "key4992", &value);
  if (status.ok()) {
    out << "GET key4992 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 15990: put
  db->Put(write_options, "key6480", "value26498");

  // op 15991: put
  db->Put(write_options, "key7202", "value89888");

  // op 15992: put
  db->Put(write_options, "key5101", "value34106");

  // op 15993: put
  db->Put(write_options, "key9962", "value4032");

  // op 15994: delete_range
  status = db->DeleteRange(write_options, "key0485", "key7259");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 15995: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4081", "key11637", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4081, key11637) size=" << scan_result.size() << std::endl;
  }

  // op 15996: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 15997: put
  db->Put(write_options, "key2226", "value16219");

  // op 15998: put
  db->Put(write_options, "key7870", "value73748");

  // op 15999: delete
  db->Delete(write_options, "key7640");

  // op 16000: put
  db->Put(write_options, "key4538", "value2612");

  // op 16001: put
  db->Put(write_options, "key8115", "value58103");

  // op 16002: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 16003: put
  db->Put(write_options, "key3345", "value87813");

  // op 16004: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3120", "key5390", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3120, key5390) size=" << scan_result.size() << std::endl;
  }

  // op 16005: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3447", "key8526", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3447, key8526) size=" << scan_result.size() << std::endl;
  }

  // op 16006: delete
  db->Delete(write_options, "key6873");

  // op 16007: put
  db->Put(write_options, "key4410", "value13171");

  // op 16008: delete_range
  status = db->DeleteRange(write_options, "key1289", "key4403");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 16009: put
  db->Put(write_options, "key8827", "value3606");

  // op 16010: put
  db->Put(write_options, "key9244", "value101376");

  // op 16011: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2589", "key5630", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2589, key5630) size=" << scan_result.size() << std::endl;
  }

  // op 16012: put
  db->Put(write_options, "key3740", "value6762");

  // op 16013: delete_range
  status = db->DeleteRange(write_options, "key0557", "key8234");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 16014: put
  db->Put(write_options, "key5451", "value64789");

  // op 16015: get
  status = db->Get(read_options, "key7464", &value);
  if (status.ok()) {
    out << "GET key7464 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16016: delete
  db->Delete(write_options, "key10837");

  // op 16017: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 16018: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1219", "key5608", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1219, key5608) size=" << scan_result.size() << std::endl;
  }

  // op 16019: delete_range
  status = db->DeleteRange(write_options, "key0156", "key11081");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 16020: put
  db->Put(write_options, "key8790", "value116766");

  // op 16021: put
  db->Put(write_options, "key9297", "value14078");

  // op 16022: delete_range
  status = db->DeleteRange(write_options, "key3752", "key7681");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 16023: get
  status = db->Get(read_options, "key4863", &value);
  if (status.ok()) {
    out << "GET key4863 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16024: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6132", "key6375", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6132, key6375) size=" << scan_result.size() << std::endl;
  }

  // op 16025: get
  status = db->Get(read_options, "key2229", &value);
  if (status.ok()) {
    out << "GET key2229 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16026: delete_range
  status = db->DeleteRange(write_options, "key7835", "key10141");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 16027: get
  status = db->Get(read_options, "key7048", &value);
  if (status.ok()) {
    out << "GET key7048 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16028: get
  status = db->Get(read_options, "key1207", &value);
  if (status.ok()) {
    out << "GET key1207 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16029: put
  db->Put(write_options, "key10364", "value37488");

  // op 16030: put
  db->Put(write_options, "key3073", "value42626");

  // op 16031: put
  db->Put(write_options, "key1438", "value30201");

  // op 16032: get
  status = db->Get(read_options, "key7134", &value);
  if (status.ok()) {
    out << "GET key7134 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16033: get
  status = db->Get(read_options, "key9774", &value);
  if (status.ok()) {
    out << "GET key9774 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16034: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3396", "key9731", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3396, key9731) size=" << scan_result.size() << std::endl;
  }

  // op 16035: get
  status = db->Get(read_options, "key10349", &value);
  if (status.ok()) {
    out << "GET key10349 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16036: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0664", "key7942", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0664, key7942) size=" << scan_result.size() << std::endl;
  }

  // op 16037: delete_range
  status = db->DeleteRange(write_options, "key3679", "key9188");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 16038: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3217", "key4505", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3217, key4505) size=" << scan_result.size() << std::endl;
  }

  // op 16039: delete_range
  status = db->DeleteRange(write_options, "key5528", "key7911");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 16040: put
  db->Put(write_options, "key2496", "value97211");

  // op 16041: put
  db->Put(write_options, "key9479", "value86038");

  // op 16042: get
  status = db->Get(read_options, "key8820", &value);
  if (status.ok()) {
    out << "GET key8820 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16043: put
  db->Put(write_options, "key1446", "value70256");

  // op 16044: delete_range
  status = db->DeleteRange(write_options, "key0213", "key8720");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 16045: put
  db->Put(write_options, "key5505", "value35826");

  // op 16046: get
  status = db->Get(read_options, "key5618", &value);
  if (status.ok()) {
    out << "GET key5618 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16047: get
  status = db->Get(read_options, "key5050", &value);
  if (status.ok()) {
    out << "GET key5050 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16048: put
  db->Put(write_options, "key6526", "value28794");

  // op 16049: put
  db->Put(write_options, "key7164", "value78605");

  // op 16050: get
  status = db->Get(read_options, "key10976", &value);
  if (status.ok()) {
    out << "GET key10976 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16051: get
  status = db->Get(read_options, "key4460", &value);
  if (status.ok()) {
    out << "GET key4460 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16052: get
  status = db->Get(read_options, "key10279", &value);
  if (status.ok()) {
    out << "GET key10279 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16053: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4029", "key5741", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4029, key5741) size=" << scan_result.size() << std::endl;
  }

  // op 16054: put
  db->Put(write_options, "key7230", "value3331");

  // op 16055: get
  status = db->Get(read_options, "key2625", &value);
  if (status.ok()) {
    out << "GET key2625 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16056: get
  status = db->Get(read_options, "key5921", &value);
  if (status.ok()) {
    out << "GET key5921 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16057: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2098", "key4716", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2098, key4716) size=" << scan_result.size() << std::endl;
  }

  // op 16058: put
  db->Put(write_options, "key8789", "value81949");

  // op 16059: delete
  db->Delete(write_options, "key8445");

  // op 16060: delete
  db->Delete(write_options, "key5568");

  // op 16061: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4721", "key9208", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4721, key9208) size=" << scan_result.size() << std::endl;
  }

  // op 16062: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5005", "key10573", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5005, key10573) size=" << scan_result.size() << std::endl;
  }

  // op 16063: get
  status = db->Get(read_options, "key11158", &value);
  if (status.ok()) {
    out << "GET key11158 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16064: delete_range
  status = db->DeleteRange(write_options, "key1929", "key3614");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 16065: put
  db->Put(write_options, "key6971", "value58769");

  // op 16066: delete_range
  status = db->DeleteRange(write_options, "key6571", "key7699");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 16067: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 16068: put
  db->Put(write_options, "key2366", "value116259");

  // op 16069: put
  db->Put(write_options, "key11616", "value66978");

  // op 16070: put
  db->Put(write_options, "key10543", "value72973");

  // op 16071: put
  db->Put(write_options, "key0490", "value55583");

  // op 16072: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 16073: delete_range
  status = db->DeleteRange(write_options, "key3385", "key11368");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 16074: delete
  db->Delete(write_options, "key9807");

  // op 16075: put
  db->Put(write_options, "key9289", "value53947");

  // op 16076: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 16077: get
  status = db->Get(read_options, "key8831", &value);
  if (status.ok()) {
    out << "GET key8831 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16078: put
  db->Put(write_options, "key2529", "value46653");

  // op 16079: get
  status = db->Get(read_options, "key6929", &value);
  if (status.ok()) {
    out << "GET key6929 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16080: delete_range
  status = db->DeleteRange(write_options, "key3073", "key7252");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 16081: get
  status = db->Get(read_options, "key2253", &value);
  if (status.ok()) {
    out << "GET key2253 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16082: put
  db->Put(write_options, "key7360", "value1469");

  // op 16083: get
  status = db->Get(read_options, "key8592", &value);
  if (status.ok()) {
    out << "GET key8592 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16084: put
  db->Put(write_options, "key1564", "value111769");

  // op 16085: put
  db->Put(write_options, "key8488", "value105896");

  // op 16086: delete
  db->Delete(write_options, "key8291");

  // op 16087: delete
  db->Delete(write_options, "key8610");

  // op 16088: delete
  db->Delete(write_options, "key3860");

  // op 16089: put
  db->Put(write_options, "key2245", "value28095");

  // op 16090: put
  db->Put(write_options, "key6530", "value48694");

  // op 16091: put
  db->Put(write_options, "key3919", "value108612");

  // op 16092: put
  db->Put(write_options, "key7463", "value108774");

  // op 16093: get
  status = db->Get(read_options, "key3376", &value);
  if (status.ok()) {
    out << "GET key3376 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16094: put
  db->Put(write_options, "key5991", "value0281");

  // op 16095: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 16096: delete
  db->Delete(write_options, "key8251");

  // op 16097: delete_range
  status = db->DeleteRange(write_options, "key1747", "key4680");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 16098: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 16099: delete_range
  status = db->DeleteRange(write_options, "key7484", "key11528");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 16100: put
  db->Put(write_options, "key2927", "value76070");

  // op 16101: get
  status = db->Get(read_options, "key1661", &value);
  if (status.ok()) {
    out << "GET key1661 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16102: put
  db->Put(write_options, "key10833", "value13499");

  // op 16103: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2128", "key8458", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2128, key8458) size=" << scan_result.size() << std::endl;
  }

  // op 16104: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7204", "key11107", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7204, key11107) size=" << scan_result.size() << std::endl;
  }

  // op 16105: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2674", "key4436", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2674, key4436) size=" << scan_result.size() << std::endl;
  }

  // op 16106: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4379", "key9640", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4379, key9640) size=" << scan_result.size() << std::endl;
  }

  // op 16107: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3237", "key10514", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3237, key10514) size=" << scan_result.size() << std::endl;
  }

  // op 16108: put
  db->Put(write_options, "key11300", "value14396");

  // op 16109: put
  db->Put(write_options, "key9757", "value64153");

  // op 16110: get
  status = db->Get(read_options, "key1410", &value);
  if (status.ok()) {
    out << "GET key1410 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16111: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3775", "key10727", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3775, key10727) size=" << scan_result.size() << std::endl;
  }

  // op 16112: delete
  db->Delete(write_options, "key4596");

  // op 16113: delete
  db->Delete(write_options, "key10967");

  // op 16114: put
  db->Put(write_options, "key5045", "value84114");

  // op 16115: put
  db->Put(write_options, "key3700", "value44482");

  // op 16116: delete
  db->Delete(write_options, "key7252");

  // op 16117: get
  status = db->Get(read_options, "key9802", &value);
  if (status.ok()) {
    out << "GET key9802 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16118: delete_range
  status = db->DeleteRange(write_options, "key0115", "key1697");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 16119: put
  db->Put(write_options, "key0282", "value101093");

  // op 16120: put
  db->Put(write_options, "key4312", "value20209");

  // op 16121: put
  db->Put(write_options, "key5853", "value77884");

  // op 16122: delete_range
  status = db->DeleteRange(write_options, "key1014", "key4010");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 16123: put
  db->Put(write_options, "key7340", "value110086");

  // op 16124: put
  db->Put(write_options, "key2417", "value69803");

  // op 16125: get
  status = db->Get(read_options, "key2195", &value);
  if (status.ok()) {
    out << "GET key2195 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16126: put
  db->Put(write_options, "key10298", "value7731");

  // op 16127: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 16128: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 16129: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 16130: delete_range
  status = db->DeleteRange(write_options, "key6274", "key8101");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 16131: delete
  db->Delete(write_options, "key9523");

  // op 16132: delete
  db->Delete(write_options, "key3698");

  // op 16133: get
  status = db->Get(read_options, "key7564", &value);
  if (status.ok()) {
    out << "GET key7564 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16134: put
  db->Put(write_options, "key3020", "value22584");

  // op 16135: put
  db->Put(write_options, "key4549", "value28237");

  // op 16136: get
  status = db->Get(read_options, "key5376", &value);
  if (status.ok()) {
    out << "GET key5376 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16137: get
  status = db->Get(read_options, "key8286", &value);
  if (status.ok()) {
    out << "GET key8286 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16138: put
  db->Put(write_options, "key5880", "value52559");

  // op 16139: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 16140: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1564", "key2311", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1564, key2311) size=" << scan_result.size() << std::endl;
  }

  // op 16141: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1220", "key5175", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1220, key5175) size=" << scan_result.size() << std::endl;
  }

  // op 16142: get
  status = db->Get(read_options, "key3777", &value);
  if (status.ok()) {
    out << "GET key3777 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16143: get
  status = db->Get(read_options, "key5861", &value);
  if (status.ok()) {
    out << "GET key5861 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16144: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2981", "key8174", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2981, key8174) size=" << scan_result.size() << std::endl;
  }

  // op 16145: put
  db->Put(write_options, "key3837", "value84311");

  // op 16146: delete_range
  status = db->DeleteRange(write_options, "key10222", "key11681");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 16147: get
  status = db->Get(read_options, "key5277", &value);
  if (status.ok()) {
    out << "GET key5277 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16148: get
  status = db->Get(read_options, "key2603", &value);
  if (status.ok()) {
    out << "GET key2603 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16149: put
  db->Put(write_options, "key9410", "value92946");

  // op 16150: put
  db->Put(write_options, "key7212", "value35911");

  // op 16151: delete
  db->Delete(write_options, "key2389");

  // op 16152: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2569", "key10414", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2569, key10414) size=" << scan_result.size() << std::endl;
  }

  // op 16153: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6944", "key8674", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6944, key8674) size=" << scan_result.size() << std::endl;
  }

  // op 16154: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10226", "key10549", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10226, key10549) size=" << scan_result.size() << std::endl;
  }

  // op 16155: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 16156: delete
  db->Delete(write_options, "key4727");

  // op 16157: put
  db->Put(write_options, "key8173", "value82491");

  // op 16158: delete
  db->Delete(write_options, "key6569");

  // op 16159: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0919", "key9420", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0919, key9420) size=" << scan_result.size() << std::endl;
  }

  // op 16160: put
  db->Put(write_options, "key6371", "value116783");

  // op 16161: put
  db->Put(write_options, "key3298", "value53758");

  // op 16162: put
  db->Put(write_options, "key3407", "value29665");

  // op 16163: delete_range
  status = db->DeleteRange(write_options, "key8921", "key10778");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 16164: put
  db->Put(write_options, "key3622", "value77481");

  // op 16165: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0015", "key5035", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0015, key5035) size=" << scan_result.size() << std::endl;
  }

  // op 16166: delete_range
  status = db->DeleteRange(write_options, "key5124", "key8265");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 16167: delete_range
  status = db->DeleteRange(write_options, "key4573", "key7095");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 16168: get
  status = db->Get(read_options, "key0599", &value);
  if (status.ok()) {
    out << "GET key0599 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16169: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3843", "key5667", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3843, key5667) size=" << scan_result.size() << std::endl;
  }

  // op 16170: get
  status = db->Get(read_options, "key6620", &value);
  if (status.ok()) {
    out << "GET key6620 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16171: put
  db->Put(write_options, "key1029", "value94337");

  // op 16172: put
  db->Put(write_options, "key9165", "value5273");

  // op 16173: delete_range
  status = db->DeleteRange(write_options, "key3786", "key8582");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 16174: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5183", "key6501", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5183, key6501) size=" << scan_result.size() << std::endl;
  }

  // op 16175: put
  db->Put(write_options, "key8539", "value45891");

  // op 16176: delete
  db->Delete(write_options, "key4217");

  // op 16177: delete
  db->Delete(write_options, "key1090");

  // op 16178: delete_range
  status = db->DeleteRange(write_options, "key6019", "key10920");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 16179: put
  db->Put(write_options, "key11139", "value42669");

  // op 16180: get
  status = db->Get(read_options, "key3290", &value);
  if (status.ok()) {
    out << "GET key3290 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16181: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 16182: delete
  db->Delete(write_options, "key10666");

  // op 16183: get
  status = db->Get(read_options, "key0310", &value);
  if (status.ok()) {
    out << "GET key0310 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16184: get
  status = db->Get(read_options, "key4082", &value);
  if (status.ok()) {
    out << "GET key4082 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16185: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0008", "key9064", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0008, key9064) size=" << scan_result.size() << std::endl;
  }

  // op 16186: get
  status = db->Get(read_options, "key5787", &value);
  if (status.ok()) {
    out << "GET key5787 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16187: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 16188: delete
  db->Delete(write_options, "key5886");

  // op 16189: delete_range
  status = db->DeleteRange(write_options, "key0835", "key2974");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 16190: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4479", "key11139", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4479, key11139) size=" << scan_result.size() << std::endl;
  }

  // op 16191: put
  db->Put(write_options, "key1304", "value84862");

  // op 16192: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4819", "key7032", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4819, key7032) size=" << scan_result.size() << std::endl;
  }

  // op 16193: put
  db->Put(write_options, "key0660", "value67558");

  // op 16194: delete
  db->Delete(write_options, "key6613");

  // op 16195: delete_range
  status = db->DeleteRange(write_options, "key1668", "key6020");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 16196: get
  status = db->Get(read_options, "key8271", &value);
  if (status.ok()) {
    out << "GET key8271 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16197: delete
  db->Delete(write_options, "key7191");

  // op 16198: put
  db->Put(write_options, "key1888", "value40963");

  // op 16199: put
  db->Put(write_options, "key1654", "value48421");

  // op 16200: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0444", "key11527", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0444, key11527) size=" << scan_result.size() << std::endl;
  }

  // op 16201: put
  db->Put(write_options, "key1508", "value18946");

  // op 16202: delete
  db->Delete(write_options, "key7402");

  // op 16203: put
  db->Put(write_options, "key8391", "value85843");

  // op 16204: delete
  db->Delete(write_options, "key0715");

  // op 16205: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0096", "key10393", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0096, key10393) size=" << scan_result.size() << std::endl;
  }

  // op 16206: delete
  db->Delete(write_options, "key7096");

  // op 16207: put
  db->Put(write_options, "key8400", "value105185");

  // op 16208: put
  db->Put(write_options, "key2731", "value99062");

  // op 16209: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6399", "key7811", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6399, key7811) size=" << scan_result.size() << std::endl;
  }

  // op 16210: put
  db->Put(write_options, "key9171", "value65785");

  // op 16211: put
  db->Put(write_options, "key5330", "value0588");

  // op 16212: get
  status = db->Get(read_options, "key10510", &value);
  if (status.ok()) {
    out << "GET key10510 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16213: put
  db->Put(write_options, "key5227", "value114516");

  // op 16214: delete_range
  status = db->DeleteRange(write_options, "key4734", "key11381");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 16215: delete
  db->Delete(write_options, "key2475");

  // op 16216: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8877", "key10871", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8877, key10871) size=" << scan_result.size() << std::endl;
  }

  // op 16217: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8714", "key10994", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8714, key10994) size=" << scan_result.size() << std::endl;
  }

  // op 16218: get
  status = db->Get(read_options, "key7527", &value);
  if (status.ok()) {
    out << "GET key7527 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16219: get
  status = db->Get(read_options, "key8396", &value);
  if (status.ok()) {
    out << "GET key8396 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16220: delete_range
  status = db->DeleteRange(write_options, "key0180", "key7213");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 16221: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2243", "key5209", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2243, key5209) size=" << scan_result.size() << std::endl;
  }

  // op 16222: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10105", "key10813", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10105, key10813) size=" << scan_result.size() << std::endl;
  }

  // op 16223: delete
  db->Delete(write_options, "key8808");

  // op 16224: get
  status = db->Get(read_options, "key8594", &value);
  if (status.ok()) {
    out << "GET key8594 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16225: get
  status = db->Get(read_options, "key7762", &value);
  if (status.ok()) {
    out << "GET key7762 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16226: put
  db->Put(write_options, "key6330", "value101876");

  // op 16227: put
  db->Put(write_options, "key4795", "value41406");

  // op 16228: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3039", "key4257", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3039, key4257) size=" << scan_result.size() << std::endl;
  }

  // op 16229: put
  db->Put(write_options, "key1758", "value96253");

  // op 16230: delete_range
  status = db->DeleteRange(write_options, "key3075", "key3464");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 16231: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0370", "key8715", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0370, key8715) size=" << scan_result.size() << std::endl;
  }

  // op 16232: put
  db->Put(write_options, "key5776", "value33789");

  // op 16233: get
  status = db->Get(read_options, "key0787", &value);
  if (status.ok()) {
    out << "GET key0787 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16234: get
  status = db->Get(read_options, "key11303", &value);
  if (status.ok()) {
    out << "GET key11303 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16235: put
  db->Put(write_options, "key11638", "value110557");

  // op 16236: put
  db->Put(write_options, "key3688", "value98977");

  // op 16237: put
  db->Put(write_options, "key0155", "value32825");

  // op 16238: delete_range
  status = db->DeleteRange(write_options, "key7014", "key10681");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 16239: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5502", "key6292", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5502, key6292) size=" << scan_result.size() << std::endl;
  }

  // op 16240: delete
  db->Delete(write_options, "key11644");

  // op 16241: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6662", "key8061", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6662, key8061) size=" << scan_result.size() << std::endl;
  }

  // op 16242: put
  db->Put(write_options, "key9019", "value115422");

  // op 16243: delete_range
  status = db->DeleteRange(write_options, "key2513", "key6285");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 16244: put
  db->Put(write_options, "key8763", "value80159");

  // op 16245: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10696", "key11725", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10696, key11725) size=" << scan_result.size() << std::endl;
  }

  // op 16246: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4449", "key7332", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4449, key7332) size=" << scan_result.size() << std::endl;
  }

  // op 16247: delete
  db->Delete(write_options, "key11262");

  // op 16248: get
  status = db->Get(read_options, "key8871", &value);
  if (status.ok()) {
    out << "GET key8871 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16249: delete_range
  status = db->DeleteRange(write_options, "key8045", "key8473");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 16250: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2598", "key10941", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2598, key10941) size=" << scan_result.size() << std::endl;
  }

  // op 16251: delete
  db->Delete(write_options, "key10635");

  // op 16252: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 16253: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2855", "key8911", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2855, key8911) size=" << scan_result.size() << std::endl;
  }

  // op 16254: put
  db->Put(write_options, "key1783", "value27121");

  // op 16255: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1764", "key11667", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1764, key11667) size=" << scan_result.size() << std::endl;
  }

  // op 16256: put
  db->Put(write_options, "key9696", "value78728");

  // op 16257: delete_range
  status = db->DeleteRange(write_options, "key2104", "key8791");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 16258: delete_range
  status = db->DeleteRange(write_options, "key0555", "key8352");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 16259: delete
  db->Delete(write_options, "key9859");

  // op 16260: put
  db->Put(write_options, "key3156", "value9189");

  // op 16261: put
  db->Put(write_options, "key6700", "value63178");

  // op 16262: put
  db->Put(write_options, "key0103", "value23720");

  // op 16263: put
  db->Put(write_options, "key0320", "value4385");

  // op 16264: delete
  db->Delete(write_options, "key3364");

  // op 16265: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0614", "key7687", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0614, key7687) size=" << scan_result.size() << std::endl;
  }

  // op 16266: get
  status = db->Get(read_options, "key3366", &value);
  if (status.ok()) {
    out << "GET key3366 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16267: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6460", "key8438", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6460, key8438) size=" << scan_result.size() << std::endl;
  }

  // op 16268: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1737", "key4094", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1737, key4094) size=" << scan_result.size() << std::endl;
  }

  // op 16269: delete_range
  status = db->DeleteRange(write_options, "key0704", "key9941");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 16270: put
  db->Put(write_options, "key1206", "value71959");

  // op 16271: delete
  db->Delete(write_options, "key9829");

  // op 16272: put
  db->Put(write_options, "key11365", "value99745");

  // op 16273: put
  db->Put(write_options, "key1882", "value47604");

  // op 16274: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9288", "key10373", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9288, key10373) size=" << scan_result.size() << std::endl;
  }

  // op 16275: put
  db->Put(write_options, "key1485", "value55392");

  // op 16276: put
  db->Put(write_options, "key8129", "value76417");

  // op 16277: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8462", "key9986", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8462, key9986) size=" << scan_result.size() << std::endl;
  }

  // op 16278: get
  status = db->Get(read_options, "key11108", &value);
  if (status.ok()) {
    out << "GET key11108 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16279: put
  db->Put(write_options, "key7070", "value43815");

  // op 16280: put
  db->Put(write_options, "key7696", "value96210");

  // op 16281: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 16282: put
  db->Put(write_options, "key5211", "value100205");

  // op 16283: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8919", "key9510", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8919, key9510) size=" << scan_result.size() << std::endl;
  }

  // op 16284: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9199", "key10398", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9199, key10398) size=" << scan_result.size() << std::endl;
  }

  // op 16285: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4502", "key11119", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4502, key11119) size=" << scan_result.size() << std::endl;
  }

  // op 16286: get
  status = db->Get(read_options, "key6770", &value);
  if (status.ok()) {
    out << "GET key6770 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16287: put
  db->Put(write_options, "key2084", "value75191");

  // op 16288: put
  db->Put(write_options, "key6774", "value86204");

  // op 16289: put
  db->Put(write_options, "key9012", "value15087");

  // op 16290: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4856", "key7420", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4856, key7420) size=" << scan_result.size() << std::endl;
  }

  // op 16291: get
  status = db->Get(read_options, "key3884", &value);
  if (status.ok()) {
    out << "GET key3884 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16292: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8333", "key9198", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8333, key9198) size=" << scan_result.size() << std::endl;
  }

  // op 16293: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0823", "key4049", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0823, key4049) size=" << scan_result.size() << std::endl;
  }

  // op 16294: put
  db->Put(write_options, "key5527", "value49622");

  // op 16295: put
  db->Put(write_options, "key4323", "value69179");

  // op 16296: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0283", "key9121", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0283, key9121) size=" << scan_result.size() << std::endl;
  }

  // op 16297: get
  status = db->Get(read_options, "key1732", &value);
  if (status.ok()) {
    out << "GET key1732 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16298: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 16299: get
  status = db->Get(read_options, "key8469", &value);
  if (status.ok()) {
    out << "GET key8469 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16300: put
  db->Put(write_options, "key9817", "value85056");

  // op 16301: put
  db->Put(write_options, "key6113", "value33027");

  // op 16302: get
  status = db->Get(read_options, "key6542", &value);
  if (status.ok()) {
    out << "GET key6542 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16303: get
  status = db->Get(read_options, "key4585", &value);
  if (status.ok()) {
    out << "GET key4585 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16304: put
  db->Put(write_options, "key11174", "value7457");

  // op 16305: put
  db->Put(write_options, "key2603", "value59586");

  // op 16306: put
  db->Put(write_options, "key8749", "value2445");

  // op 16307: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0589", "key10843", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0589, key10843) size=" << scan_result.size() << std::endl;
  }

  // op 16308: get
  status = db->Get(read_options, "key9904", &value);
  if (status.ok()) {
    out << "GET key9904 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16309: put
  db->Put(write_options, "key0949", "value54411");

  // op 16310: delete
  db->Delete(write_options, "key7847");

  // op 16311: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 16312: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6945", "key11049", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6945, key11049) size=" << scan_result.size() << std::endl;
  }

  // op 16313: get
  status = db->Get(read_options, "key1830", &value);
  if (status.ok()) {
    out << "GET key1830 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16314: get
  status = db->Get(read_options, "key8754", &value);
  if (status.ok()) {
    out << "GET key8754 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16315: put
  db->Put(write_options, "key6391", "value83787");

  // op 16316: put
  db->Put(write_options, "key3447", "value48097");

  // op 16317: delete_range
  status = db->DeleteRange(write_options, "key8763", "key10762");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 16318: put
  db->Put(write_options, "key9244", "value90387");

  // op 16319: get
  status = db->Get(read_options, "key4688", &value);
  if (status.ok()) {
    out << "GET key4688 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16320: put
  db->Put(write_options, "key7327", "value105945");

  // op 16321: delete_range
  status = db->DeleteRange(write_options, "key2001", "key10508");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 16322: put
  db->Put(write_options, "key4409", "value24640");

  // op 16323: put
  db->Put(write_options, "key7683", "value99732");

  // op 16324: put
  db->Put(write_options, "key5311", "value8270");

  // op 16325: put
  db->Put(write_options, "key0560", "value101960");

  // op 16326: get
  status = db->Get(read_options, "key9406", &value);
  if (status.ok()) {
    out << "GET key9406 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16327: put
  db->Put(write_options, "key7933", "value67764");

  // op 16328: put
  db->Put(write_options, "key2477", "value111905");

  // op 16329: put
  db->Put(write_options, "key8451", "value45208");

  // op 16330: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0998", "key11113", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0998, key11113) size=" << scan_result.size() << std::endl;
  }

  // op 16331: get
  status = db->Get(read_options, "key5476", &value);
  if (status.ok()) {
    out << "GET key5476 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16332: put
  db->Put(write_options, "key5052", "value35930");

  // op 16333: delete
  db->Delete(write_options, "key6898");

  // op 16334: put
  db->Put(write_options, "key8092", "value1905");

  // op 16335: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1629", "key8281", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1629, key8281) size=" << scan_result.size() << std::endl;
  }

  // op 16336: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2548", "key4369", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2548, key4369) size=" << scan_result.size() << std::endl;
  }

  // op 16337: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2465", "key3177", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2465, key3177) size=" << scan_result.size() << std::endl;
  }

  // op 16338: delete
  db->Delete(write_options, "key7455");

  // op 16339: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2943", "key3718", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2943, key3718) size=" << scan_result.size() << std::endl;
  }

  // op 16340: put
  db->Put(write_options, "key7896", "value79032");

  // op 16341: put
  db->Put(write_options, "key11474", "value62938");

  // op 16342: delete_range
  status = db->DeleteRange(write_options, "key1218", "key2207");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 16343: put
  db->Put(write_options, "key4719", "value41778");

  // op 16344: put
  db->Put(write_options, "key7644", "value27251");

  // op 16345: get
  status = db->Get(read_options, "key0570", &value);
  if (status.ok()) {
    out << "GET key0570 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16346: get
  status = db->Get(read_options, "key9852", &value);
  if (status.ok()) {
    out << "GET key9852 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16347: put
  db->Put(write_options, "key5006", "value115859");

  // op 16348: get
  status = db->Get(read_options, "key8499", &value);
  if (status.ok()) {
    out << "GET key8499 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16349: get
  status = db->Get(read_options, "key5083", &value);
  if (status.ok()) {
    out << "GET key5083 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16350: get
  status = db->Get(read_options, "key9634", &value);
  if (status.ok()) {
    out << "GET key9634 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16351: get
  status = db->Get(read_options, "key0064", &value);
  if (status.ok()) {
    out << "GET key0064 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16352: get
  status = db->Get(read_options, "key1548", &value);
  if (status.ok()) {
    out << "GET key1548 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16353: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3505", "key4034", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3505, key4034) size=" << scan_result.size() << std::endl;
  }

  // op 16354: get
  status = db->Get(read_options, "key11032", &value);
  if (status.ok()) {
    out << "GET key11032 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16355: get
  status = db->Get(read_options, "key0289", &value);
  if (status.ok()) {
    out << "GET key0289 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16356: get
  status = db->Get(read_options, "key8535", &value);
  if (status.ok()) {
    out << "GET key8535 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16357: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 16358: put
  db->Put(write_options, "key5912", "value73336");

  // op 16359: delete
  db->Delete(write_options, "key2404");

  // op 16360: get
  status = db->Get(read_options, "key9552", &value);
  if (status.ok()) {
    out << "GET key9552 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16361: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0306", "key7186", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0306, key7186) size=" << scan_result.size() << std::endl;
  }

  // op 16362: delete
  db->Delete(write_options, "key6199");

  // op 16363: get
  status = db->Get(read_options, "key6367", &value);
  if (status.ok()) {
    out << "GET key6367 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16364: delete
  db->Delete(write_options, "key6981");

  // op 16365: delete
  db->Delete(write_options, "key3162");

  // op 16366: delete
  db->Delete(write_options, "key8967");

  // op 16367: put
  db->Put(write_options, "key8484", "value40442");

  // op 16368: get
  status = db->Get(read_options, "key6188", &value);
  if (status.ok()) {
    out << "GET key6188 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16369: put
  db->Put(write_options, "key1777", "value14207");

  // op 16370: put
  db->Put(write_options, "key9698", "value109227");

  // op 16371: delete
  db->Delete(write_options, "key7353");

  // op 16372: get
  status = db->Get(read_options, "key7300", &value);
  if (status.ok()) {
    out << "GET key7300 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16373: put
  db->Put(write_options, "key8508", "value51414");

  // op 16374: put
  db->Put(write_options, "key7058", "value97612");

  // op 16375: put
  db->Put(write_options, "key4151", "value7775");

  // op 16376: get
  status = db->Get(read_options, "key8939", &value);
  if (status.ok()) {
    out << "GET key8939 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16377: put
  db->Put(write_options, "key5019", "value49310");

  // op 16378: put
  db->Put(write_options, "key5749", "value94551");

  // op 16379: get
  status = db->Get(read_options, "key4532", &value);
  if (status.ok()) {
    out << "GET key4532 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16380: put
  db->Put(write_options, "key8379", "value1860");

  // op 16381: delete
  db->Delete(write_options, "key3388");

  // op 16382: put
  db->Put(write_options, "key4357", "value50691");

  // op 16383: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4081", "key8878", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4081, key8878) size=" << scan_result.size() << std::endl;
  }

  // op 16384: put
  db->Put(write_options, "key8946", "value41513");

  // op 16385: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 16386: put
  db->Put(write_options, "key9968", "value25271");

  // op 16387: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5220", "key9395", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5220, key9395) size=" << scan_result.size() << std::endl;
  }

  // op 16388: get
  status = db->Get(read_options, "key11054", &value);
  if (status.ok()) {
    out << "GET key11054 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16389: put
  db->Put(write_options, "key3016", "value88039");

  // op 16390: delete_range
  status = db->DeleteRange(write_options, "key0864", "key8342");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 16391: put
  db->Put(write_options, "key10012", "value12704");

  // op 16392: delete
  db->Delete(write_options, "key3608");

  // op 16393: put
  db->Put(write_options, "key2504", "value56208");

  // op 16394: delete_range
  status = db->DeleteRange(write_options, "key4834", "key8141");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 16395: delete
  db->Delete(write_options, "key7574");

  // op 16396: get
  status = db->Get(read_options, "key1095", &value);
  if (status.ok()) {
    out << "GET key1095 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16397: put
  db->Put(write_options, "key10972", "value32315");

  // op 16398: put
  db->Put(write_options, "key6395", "value19504");

  // op 16399: put
  db->Put(write_options, "key6947", "value28557");

  // op 16400: put
  db->Put(write_options, "key6237", "value81997");

  // op 16401: put
  db->Put(write_options, "key4962", "value44955");

  // op 16402: put
  db->Put(write_options, "key2797", "value26225");

  // op 16403: put
  db->Put(write_options, "key7311", "value94427");

  // op 16404: put
  db->Put(write_options, "key3746", "value7824");

  // op 16405: put
  db->Put(write_options, "key10808", "value99872");

  // op 16406: delete_range
  status = db->DeleteRange(write_options, "key7483", "key7781");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 16407: delete
  db->Delete(write_options, "key5286");

  // op 16408: get
  status = db->Get(read_options, "key11209", &value);
  if (status.ok()) {
    out << "GET key11209 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16409: get
  status = db->Get(read_options, "key0054", &value);
  if (status.ok()) {
    out << "GET key0054 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16410: put
  db->Put(write_options, "key6618", "value115170");

  // op 16411: get
  status = db->Get(read_options, "key10511", &value);
  if (status.ok()) {
    out << "GET key10511 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16412: delete
  db->Delete(write_options, "key4224");

  // op 16413: put
  db->Put(write_options, "key3927", "value75413");

  // op 16414: put
  db->Put(write_options, "key3406", "value7624");

  // op 16415: get
  status = db->Get(read_options, "key10671", &value);
  if (status.ok()) {
    out << "GET key10671 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16416: delete_range
  status = db->DeleteRange(write_options, "key3100", "key11481");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 16417: put
  db->Put(write_options, "key11082", "value66608");

  // op 16418: delete
  db->Delete(write_options, "key0392");

  // op 16419: put
  db->Put(write_options, "key3750", "value69872");

  // op 16420: put
  db->Put(write_options, "key11360", "value28453");

  // op 16421: put
  db->Put(write_options, "key11421", "value45949");

  // op 16422: put
  db->Put(write_options, "key1776", "value92823");

  // op 16423: put
  db->Put(write_options, "key0479", "value18299");

  // op 16424: put
  db->Put(write_options, "key9946", "value58628");

  // op 16425: delete
  db->Delete(write_options, "key1849");

  // op 16426: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1049", "key6631", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1049, key6631) size=" << scan_result.size() << std::endl;
  }

  // op 16427: put
  db->Put(write_options, "key6450", "value95913");

  // op 16428: delete_range
  status = db->DeleteRange(write_options, "key2140", "key4609");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 16429: put
  db->Put(write_options, "key9923", "value13256");

  // op 16430: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5886", "key11273", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5886, key11273) size=" << scan_result.size() << std::endl;
  }

  // op 16431: put
  db->Put(write_options, "key4166", "value117505");

  // op 16432: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1260", "key9445", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1260, key9445) size=" << scan_result.size() << std::endl;
  }

  // op 16433: get
  status = db->Get(read_options, "key6000", &value);
  if (status.ok()) {
    out << "GET key6000 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16434: put
  db->Put(write_options, "key11055", "value74848");

  // op 16435: delete_range
  status = db->DeleteRange(write_options, "key6180", "key11408");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 16436: get
  status = db->Get(read_options, "key5964", &value);
  if (status.ok()) {
    out << "GET key5964 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16437: delete_range
  status = db->DeleteRange(write_options, "key7390", "key8249");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 16438: delete
  db->Delete(write_options, "key2336");

  // op 16439: delete_range
  status = db->DeleteRange(write_options, "key0832", "key9063");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 16440: delete_range
  status = db->DeleteRange(write_options, "key2383", "key9304");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 16441: delete_range
  status = db->DeleteRange(write_options, "key2995", "key10466");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 16442: delete
  db->Delete(write_options, "key1659");

  // op 16443: get
  status = db->Get(read_options, "key7158", &value);
  if (status.ok()) {
    out << "GET key7158 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16444: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4267", "key8019", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4267, key8019) size=" << scan_result.size() << std::endl;
  }

  // op 16445: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6778", "key8255", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6778, key8255) size=" << scan_result.size() << std::endl;
  }

  // op 16446: put
  db->Put(write_options, "key7022", "value27076");

  // op 16447: put
  db->Put(write_options, "key4704", "value96878");

  // op 16448: put
  db->Put(write_options, "key4743", "value105677");

  // op 16449: delete
  db->Delete(write_options, "key5180");

  // op 16450: delete_range
  status = db->DeleteRange(write_options, "key4612", "key6800");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 16451: delete
  db->Delete(write_options, "key10713");

  // op 16452: put
  db->Put(write_options, "key7162", "value76810");

  // op 16453: get
  status = db->Get(read_options, "key0565", &value);
  if (status.ok()) {
    out << "GET key0565 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16454: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0951", "key1452", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0951, key1452) size=" << scan_result.size() << std::endl;
  }

  // op 16455: get
  status = db->Get(read_options, "key0029", &value);
  if (status.ok()) {
    out << "GET key0029 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16456: put
  db->Put(write_options, "key7597", "value5239");

  // op 16457: get
  status = db->Get(read_options, "key2207", &value);
  if (status.ok()) {
    out << "GET key2207 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16458: delete
  db->Delete(write_options, "key7629");

  // op 16459: get
  status = db->Get(read_options, "key1105", &value);
  if (status.ok()) {
    out << "GET key1105 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16460: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4815", "key10176", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4815, key10176) size=" << scan_result.size() << std::endl;
  }

  // op 16461: get
  status = db->Get(read_options, "key3268", &value);
  if (status.ok()) {
    out << "GET key3268 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16462: put
  db->Put(write_options, "key11076", "value32605");

  // op 16463: put
  db->Put(write_options, "key10613", "value116521");

  // op 16464: put
  db->Put(write_options, "key8015", "value20508");

  // op 16465: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6659", "key8238", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6659, key8238) size=" << scan_result.size() << std::endl;
  }

  // op 16466: get
  status = db->Get(read_options, "key4006", &value);
  if (status.ok()) {
    out << "GET key4006 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16467: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8853", "key9205", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8853, key9205) size=" << scan_result.size() << std::endl;
  }

  // op 16468: put
  db->Put(write_options, "key9721", "value7919");

  // op 16469: delete_range
  status = db->DeleteRange(write_options, "key6251", "key11580");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 16470: delete
  db->Delete(write_options, "key4039");

  // op 16471: put
  db->Put(write_options, "key8209", "value15055");

  // op 16472: get
  status = db->Get(read_options, "key8434", &value);
  if (status.ok()) {
    out << "GET key8434 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16473: delete
  db->Delete(write_options, "key4151");

  // op 16474: delete
  db->Delete(write_options, "key6396");

  // op 16475: put
  db->Put(write_options, "key11467", "value82400");

  // op 16476: put
  db->Put(write_options, "key1718", "value78012");

  // op 16477: get
  status = db->Get(read_options, "key6630", &value);
  if (status.ok()) {
    out << "GET key6630 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16478: delete_range
  status = db->DeleteRange(write_options, "key1398", "key5175");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 16479: put
  db->Put(write_options, "key3863", "value93106");

  // op 16480: put
  db->Put(write_options, "key5791", "value91008");

  // op 16481: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0914", "key1835", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0914, key1835) size=" << scan_result.size() << std::endl;
  }

  // op 16482: put
  db->Put(write_options, "key6459", "value35579");

  // op 16483: delete
  db->Delete(write_options, "key4541");

  // op 16484: put
  db->Put(write_options, "key4095", "value45384");

  // op 16485: put
  db->Put(write_options, "key2721", "value94494");

  // op 16486: put
  db->Put(write_options, "key9990", "value47108");

  // op 16487: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4496", "key6513", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4496, key6513) size=" << scan_result.size() << std::endl;
  }

  // op 16488: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5220", "key9686", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5220, key9686) size=" << scan_result.size() << std::endl;
  }

  // op 16489: put
  db->Put(write_options, "key3678", "value3274");

  // op 16490: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3005", "key8055", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3005, key8055) size=" << scan_result.size() << std::endl;
  }

  // op 16491: delete
  db->Delete(write_options, "key6051");

  // op 16492: get
  status = db->Get(read_options, "key5056", &value);
  if (status.ok()) {
    out << "GET key5056 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16493: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0873", "key9498", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0873, key9498) size=" << scan_result.size() << std::endl;
  }

  // op 16494: put
  db->Put(write_options, "key6793", "value96983");

  // op 16495: put
  db->Put(write_options, "key11262", "value83862");

  // op 16496: put
  db->Put(write_options, "key10173", "value105558");

  // op 16497: put
  db->Put(write_options, "key10931", "value95766");

  // op 16498: get
  status = db->Get(read_options, "key9635", &value);
  if (status.ok()) {
    out << "GET key9635 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16499: put
  db->Put(write_options, "key0376", "value82667");

  // op 16500: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0806", "key9708", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0806, key9708) size=" << scan_result.size() << std::endl;
  }

  // op 16501: get
  status = db->Get(read_options, "key10748", &value);
  if (status.ok()) {
    out << "GET key10748 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16502: put
  db->Put(write_options, "key9620", "value97839");

  // op 16503: delete_range
  status = db->DeleteRange(write_options, "key1557", "key3097");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 16504: get
  status = db->Get(read_options, "key9225", &value);
  if (status.ok()) {
    out << "GET key9225 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16505: delete_range
  status = db->DeleteRange(write_options, "key0184", "key8514");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 16506: put
  db->Put(write_options, "key5251", "value69081");

  // op 16507: get
  status = db->Get(read_options, "key4425", &value);
  if (status.ok()) {
    out << "GET key4425 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16508: get
  status = db->Get(read_options, "key0589", &value);
  if (status.ok()) {
    out << "GET key0589 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16509: delete
  db->Delete(write_options, "key8497");

  // op 16510: put
  db->Put(write_options, "key7965", "value72494");

  // op 16511: delete
  db->Delete(write_options, "key3798");

  // op 16512: delete
  db->Delete(write_options, "key0151");

  // op 16513: get
  status = db->Get(read_options, "key6180", &value);
  if (status.ok()) {
    out << "GET key6180 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16514: delete_range
  status = db->DeleteRange(write_options, "key10679", "key11403");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 16515: put
  db->Put(write_options, "key10253", "value114211");

  // op 16516: delete
  db->Delete(write_options, "key4439");

  // op 16517: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5391", "key8406", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5391, key8406) size=" << scan_result.size() << std::endl;
  }

  // op 16518: delete
  db->Delete(write_options, "key9421");

  // op 16519: delete_range
  status = db->DeleteRange(write_options, "key2957", "key5962");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 16520: delete_range
  status = db->DeleteRange(write_options, "key3391", "key11463");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 16521: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 16522: put
  db->Put(write_options, "key1814", "value62535");

  // op 16523: delete_range
  status = db->DeleteRange(write_options, "key4403", "key6239");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 16524: delete
  db->Delete(write_options, "key11081");

  // op 16525: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0171", "key11261", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0171, key11261) size=" << scan_result.size() << std::endl;
  }

  // op 16526: put
  db->Put(write_options, "key7172", "value116618");

  // op 16527: put
  db->Put(write_options, "key2237", "value19549");

  // op 16528: delete
  db->Delete(write_options, "key2688");

  // op 16529: put
  db->Put(write_options, "key1434", "value53569");

  // op 16530: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3869", "key8054", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3869, key8054) size=" << scan_result.size() << std::endl;
  }

  // op 16531: put
  db->Put(write_options, "key8491", "value19051");

  // op 16532: get
  status = db->Get(read_options, "key10760", &value);
  if (status.ok()) {
    out << "GET key10760 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16533: put
  db->Put(write_options, "key8886", "value40476");

  // op 16534: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 16535: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3998", "key5280", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3998, key5280) size=" << scan_result.size() << std::endl;
  }

  // op 16536: put
  db->Put(write_options, "key3030", "value77149");

  // op 16537: get
  status = db->Get(read_options, "key8141", &value);
  if (status.ok()) {
    out << "GET key8141 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16538: delete_range
  status = db->DeleteRange(write_options, "key4656", "key10642");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 16539: put
  db->Put(write_options, "key11249", "value41815");

  // op 16540: put
  db->Put(write_options, "key10406", "value88722");

  // op 16541: delete_range
  status = db->DeleteRange(write_options, "key5349", "key8330");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 16542: get
  status = db->Get(read_options, "key5788", &value);
  if (status.ok()) {
    out << "GET key5788 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16543: put
  db->Put(write_options, "key9336", "value36520");

  // op 16544: delete_range
  status = db->DeleteRange(write_options, "key2853", "key8035");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 16545: put
  db->Put(write_options, "key3253", "value7345");

  // op 16546: put
  db->Put(write_options, "key9111", "value81630");

  // op 16547: put
  db->Put(write_options, "key2794", "value90319");

  // op 16548: get
  status = db->Get(read_options, "key9020", &value);
  if (status.ok()) {
    out << "GET key9020 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16549: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3311", "key9873", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3311, key9873) size=" << scan_result.size() << std::endl;
  }

  // op 16550: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 16551: put
  db->Put(write_options, "key1465", "value3473");

  // op 16552: put
  db->Put(write_options, "key11054", "value60257");

  // op 16553: get
  status = db->Get(read_options, "key11325", &value);
  if (status.ok()) {
    out << "GET key11325 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16554: put
  db->Put(write_options, "key10138", "value80000");

  // op 16555: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1003", "key10455", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1003, key10455) size=" << scan_result.size() << std::endl;
  }

  // op 16556: get
  status = db->Get(read_options, "key8812", &value);
  if (status.ok()) {
    out << "GET key8812 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16557: get
  status = db->Get(read_options, "key7945", &value);
  if (status.ok()) {
    out << "GET key7945 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16558: put
  db->Put(write_options, "key1134", "value74117");

  // op 16559: get
  status = db->Get(read_options, "key9348", &value);
  if (status.ok()) {
    out << "GET key9348 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16560: put
  db->Put(write_options, "key4773", "value60674");

  // op 16561: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 16562: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4497", "key7182", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4497, key7182) size=" << scan_result.size() << std::endl;
  }

  // op 16563: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3750", "key3801", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3750, key3801) size=" << scan_result.size() << std::endl;
  }

  // op 16564: put
  db->Put(write_options, "key4058", "value104312");

  // op 16565: put
  db->Put(write_options, "key6788", "value99526");

  // op 16566: put
  db->Put(write_options, "key2186", "value60533");

  // op 16567: put
  db->Put(write_options, "key2982", "value97898");

  // op 16568: put
  db->Put(write_options, "key8860", "value107825");

  // op 16569: put
  db->Put(write_options, "key1472", "value71485");

  // op 16570: get
  status = db->Get(read_options, "key10892", &value);
  if (status.ok()) {
    out << "GET key10892 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16571: delete_range
  status = db->DeleteRange(write_options, "key3785", "key11042");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 16572: delete
  db->Delete(write_options, "key9818");

  // op 16573: put
  db->Put(write_options, "key8859", "value58514");

  // op 16574: delete_range
  status = db->DeleteRange(write_options, "key5363", "key6424");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 16575: put
  db->Put(write_options, "key3399", "value33535");

  // op 16576: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3378", "key4327", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3378, key4327) size=" << scan_result.size() << std::endl;
  }

  // op 16577: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6034", "key7330", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6034, key7330) size=" << scan_result.size() << std::endl;
  }

  // op 16578: put
  db->Put(write_options, "key1967", "value33614");

  // op 16579: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8289", "key10320", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8289, key10320) size=" << scan_result.size() << std::endl;
  }

  // op 16580: put
  db->Put(write_options, "key6171", "value7521");

  // op 16581: delete
  db->Delete(write_options, "key2797");

  // op 16582: put
  db->Put(write_options, "key6167", "value32774");

  // op 16583: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3151", "key3389", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3151, key3389) size=" << scan_result.size() << std::endl;
  }

  // op 16584: put
  db->Put(write_options, "key5754", "value18625");

  // op 16585: put
  db->Put(write_options, "key6302", "value59341");

  // op 16586: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7690", "key10269", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7690, key10269) size=" << scan_result.size() << std::endl;
  }

  // op 16587: put
  db->Put(write_options, "key1495", "value1601");

  // op 16588: delete_range
  status = db->DeleteRange(write_options, "key2642", "key8466");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 16589: delete
  db->Delete(write_options, "key2582");

  // op 16590: delete_range
  status = db->DeleteRange(write_options, "key7429", "key8866");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 16591: put
  db->Put(write_options, "key2592", "value101527");

  // op 16592: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 16593: delete
  db->Delete(write_options, "key6100");

  // op 16594: put
  db->Put(write_options, "key6771", "value107499");

  // op 16595: put
  db->Put(write_options, "key8817", "value10373");

  // op 16596: delete_range
  status = db->DeleteRange(write_options, "key3148", "key6555");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 16597: put
  db->Put(write_options, "key1044", "value56068");

  // op 16598: put
  db->Put(write_options, "key10170", "value65979");

  // op 16599: get
  status = db->Get(read_options, "key5169", &value);
  if (status.ok()) {
    out << "GET key5169 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16600: put
  db->Put(write_options, "key2018", "value104962");

  // op 16601: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0407", "key3380", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0407, key3380) size=" << scan_result.size() << std::endl;
  }

  // op 16602: delete_range
  status = db->DeleteRange(write_options, "key8505", "key8997");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 16603: put
  db->Put(write_options, "key3287", "value22524");

  // op 16604: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 16605: delete
  db->Delete(write_options, "key2060");

  // op 16606: put
  db->Put(write_options, "key3070", "value88918");

  // op 16607: delete
  db->Delete(write_options, "key5841");

  // op 16608: get
  status = db->Get(read_options, "key11386", &value);
  if (status.ok()) {
    out << "GET key11386 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16609: put
  db->Put(write_options, "key5783", "value99527");

  // op 16610: put
  db->Put(write_options, "key11604", "value44182");

  // op 16611: put
  db->Put(write_options, "key4797", "value12412");

  // op 16612: get
  status = db->Get(read_options, "key5377", &value);
  if (status.ok()) {
    out << "GET key5377 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16613: get
  status = db->Get(read_options, "key4779", &value);
  if (status.ok()) {
    out << "GET key4779 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16614: get
  status = db->Get(read_options, "key0291", &value);
  if (status.ok()) {
    out << "GET key0291 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16615: get
  status = db->Get(read_options, "key4095", &value);
  if (status.ok()) {
    out << "GET key4095 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16616: put
  db->Put(write_options, "key8658", "value34206");

  // op 16617: put
  db->Put(write_options, "key9534", "value50669");

  // op 16618: delete
  db->Delete(write_options, "key6305");

  // op 16619: delete
  db->Delete(write_options, "key10054");

  // op 16620: put
  db->Put(write_options, "key2837", "value75430");

  // op 16621: put
  db->Put(write_options, "key4080", "value112556");

  // op 16622: delete
  db->Delete(write_options, "key8290");

  // op 16623: put
  db->Put(write_options, "key2697", "value3206");

  // op 16624: put
  db->Put(write_options, "key6179", "value55583");

  // op 16625: delete_range
  status = db->DeleteRange(write_options, "key4593", "key7180");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 16626: put
  db->Put(write_options, "key4944", "value9585");

  // op 16627: get
  status = db->Get(read_options, "key4387", &value);
  if (status.ok()) {
    out << "GET key4387 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16628: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 16629: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1127", "key9941", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1127, key9941) size=" << scan_result.size() << std::endl;
  }

  // op 16630: delete_range
  status = db->DeleteRange(write_options, "key1080", "key10236");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 16631: put
  db->Put(write_options, "key11038", "value3150");

  // op 16632: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0945", "key11554", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0945, key11554) size=" << scan_result.size() << std::endl;
  }

  // op 16633: get
  status = db->Get(read_options, "key2486", &value);
  if (status.ok()) {
    out << "GET key2486 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16634: put
  db->Put(write_options, "key10270", "value26614");

  // op 16635: get
  status = db->Get(read_options, "key7551", &value);
  if (status.ok()) {
    out << "GET key7551 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16636: put
  db->Put(write_options, "key1398", "value42443");

  // op 16637: delete
  db->Delete(write_options, "key0397");

  // op 16638: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 16639: put
  db->Put(write_options, "key3869", "value19726");

  // op 16640: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8358", "key9358", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8358, key9358) size=" << scan_result.size() << std::endl;
  }

  // op 16641: put
  db->Put(write_options, "key1137", "value14842");

  // op 16642: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9814", "key10331", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9814, key10331) size=" << scan_result.size() << std::endl;
  }

  // op 16643: delete
  db->Delete(write_options, "key8227");

  // op 16644: put
  db->Put(write_options, "key9440", "value53202");

  // op 16645: get
  status = db->Get(read_options, "key4368", &value);
  if (status.ok()) {
    out << "GET key4368 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16646: get
  status = db->Get(read_options, "key8099", &value);
  if (status.ok()) {
    out << "GET key8099 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16647: put
  db->Put(write_options, "key8884", "value3789");

  // op 16648: get
  status = db->Get(read_options, "key7428", &value);
  if (status.ok()) {
    out << "GET key7428 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16649: get
  status = db->Get(read_options, "key2955", &value);
  if (status.ok()) {
    out << "GET key2955 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16650: put
  db->Put(write_options, "key4288", "value20120");

  // op 16651: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3332", "key5834", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3332, key5834) size=" << scan_result.size() << std::endl;
  }

  // op 16652: put
  db->Put(write_options, "key8527", "value108375");

  // op 16653: put
  db->Put(write_options, "key6158", "value28307");

  // op 16654: put
  db->Put(write_options, "key7858", "value103858");

  // op 16655: get
  status = db->Get(read_options, "key6138", &value);
  if (status.ok()) {
    out << "GET key6138 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16656: delete_range
  status = db->DeleteRange(write_options, "key3137", "key10660");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 16657: put
  db->Put(write_options, "key5437", "value118079");

  // op 16658: put
  db->Put(write_options, "key6197", "value100837");

  // op 16659: put
  db->Put(write_options, "key10307", "value29021");

  // op 16660: put
  db->Put(write_options, "key6296", "value36388");

  // op 16661: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5822", "key9909", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5822, key9909) size=" << scan_result.size() << std::endl;
  }

  // op 16662: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5159", "key6975", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5159, key6975) size=" << scan_result.size() << std::endl;
  }

  // op 16663: delete_range
  status = db->DeleteRange(write_options, "key1018", "key2443");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 16664: delete_range
  status = db->DeleteRange(write_options, "key6875", "key10437");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 16665: put
  db->Put(write_options, "key8116", "value65425");

  // op 16666: delete_range
  status = db->DeleteRange(write_options, "key6675", "key9243");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 16667: get
  status = db->Get(read_options, "key2018", &value);
  if (status.ok()) {
    out << "GET key2018 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16668: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 16669: get
  status = db->Get(read_options, "key10361", &value);
  if (status.ok()) {
    out << "GET key10361 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16670: get
  status = db->Get(read_options, "key8260", &value);
  if (status.ok()) {
    out << "GET key8260 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16671: get
  status = db->Get(read_options, "key1746", &value);
  if (status.ok()) {
    out << "GET key1746 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16672: put
  db->Put(write_options, "key3199", "value61541");

  // op 16673: get
  status = db->Get(read_options, "key6981", &value);
  if (status.ok()) {
    out << "GET key6981 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16674: get
  status = db->Get(read_options, "key10340", &value);
  if (status.ok()) {
    out << "GET key10340 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16675: put
  db->Put(write_options, "key7898", "value91234");

  // op 16676: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4209", "key8510", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4209, key8510) size=" << scan_result.size() << std::endl;
  }

  // op 16677: get
  status = db->Get(read_options, "key11266", &value);
  if (status.ok()) {
    out << "GET key11266 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16678: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5901", "key7134", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5901, key7134) size=" << scan_result.size() << std::endl;
  }

  // op 16679: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1867", "key10933", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1867, key10933) size=" << scan_result.size() << std::endl;
  }

  // op 16680: get
  status = db->Get(read_options, "key6014", &value);
  if (status.ok()) {
    out << "GET key6014 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16681: get
  status = db->Get(read_options, "key11037", &value);
  if (status.ok()) {
    out << "GET key11037 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16682: get
  status = db->Get(read_options, "key2894", &value);
  if (status.ok()) {
    out << "GET key2894 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16683: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0281", "key8487", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0281, key8487) size=" << scan_result.size() << std::endl;
  }

  // op 16684: put
  db->Put(write_options, "key6664", "value67415");

  // op 16685: put
  db->Put(write_options, "key5994", "value70595");

  // op 16686: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1991", "key5111", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1991, key5111) size=" << scan_result.size() << std::endl;
  }

  // op 16687: put
  db->Put(write_options, "key2605", "value26546");

  // op 16688: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2396", "key3385", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2396, key3385) size=" << scan_result.size() << std::endl;
  }

  // op 16689: put
  db->Put(write_options, "key5295", "value99139");

  // op 16690: put
  db->Put(write_options, "key3876", "value95921");

  // op 16691: get
  status = db->Get(read_options, "key7070", &value);
  if (status.ok()) {
    out << "GET key7070 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16692: delete
  db->Delete(write_options, "key4481");

  // op 16693: put
  db->Put(write_options, "key3602", "value101715");

  // op 16694: put
  db->Put(write_options, "key8067", "value111949");

  // op 16695: get
  status = db->Get(read_options, "key1530", &value);
  if (status.ok()) {
    out << "GET key1530 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16696: delete_range
  status = db->DeleteRange(write_options, "key2165", "key6349");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 16697: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2522", "key2695", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2522, key2695) size=" << scan_result.size() << std::endl;
  }

  // op 16698: delete
  db->Delete(write_options, "key6851");

  // op 16699: put
  db->Put(write_options, "key10315", "value109137");

  // op 16700: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2336", "key4654", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2336, key4654) size=" << scan_result.size() << std::endl;
  }

  // op 16701: put
  db->Put(write_options, "key8430", "value7460");

  // op 16702: get
  status = db->Get(read_options, "key9805", &value);
  if (status.ok()) {
    out << "GET key9805 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16703: get
  status = db->Get(read_options, "key11478", &value);
  if (status.ok()) {
    out << "GET key11478 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16704: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3370", "key7588", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3370, key7588) size=" << scan_result.size() << std::endl;
  }

  // op 16705: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0153", "key9694", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0153, key9694) size=" << scan_result.size() << std::endl;
  }

  // op 16706: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0917", "key8247", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0917, key8247) size=" << scan_result.size() << std::endl;
  }

  // op 16707: put
  db->Put(write_options, "key3902", "value41593");

  // op 16708: delete_range
  status = db->DeleteRange(write_options, "key5102", "key11705");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 16709: delete
  db->Delete(write_options, "key10438");

  // op 16710: put
  db->Put(write_options, "key9157", "value54910");

  // op 16711: delete
  db->Delete(write_options, "key3629");

  // op 16712: delete
  db->Delete(write_options, "key1514");

  // op 16713: delete
  db->Delete(write_options, "key2446");

  // op 16714: get
  status = db->Get(read_options, "key1960", &value);
  if (status.ok()) {
    out << "GET key1960 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16715: delete
  db->Delete(write_options, "key3085");

  // op 16716: delete
  db->Delete(write_options, "key6979");

  // op 16717: put
  db->Put(write_options, "key10507", "value27897");

  // op 16718: get
  status = db->Get(read_options, "key5989", &value);
  if (status.ok()) {
    out << "GET key5989 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16719: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1856", "key2841", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1856, key2841) size=" << scan_result.size() << std::endl;
  }

  // op 16720: put
  db->Put(write_options, "key2590", "value83899");

  // op 16721: delete_range
  status = db->DeleteRange(write_options, "key3398", "key5942");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 16722: put
  db->Put(write_options, "key8188", "value23129");

  // op 16723: put
  db->Put(write_options, "key1756", "value51118");

  // op 16724: delete_range
  status = db->DeleteRange(write_options, "key2279", "key8809");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 16725: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 16726: delete_range
  status = db->DeleteRange(write_options, "key1534", "key3611");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 16727: delete
  db->Delete(write_options, "key5298");

  // op 16728: put
  db->Put(write_options, "key4063", "value45780");

  // op 16729: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2289", "key3047", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2289, key3047) size=" << scan_result.size() << std::endl;
  }

  // op 16730: put
  db->Put(write_options, "key0502", "value104526");

  // op 16731: get
  status = db->Get(read_options, "key0778", &value);
  if (status.ok()) {
    out << "GET key0778 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16732: put
  db->Put(write_options, "key3061", "value23758");

  // op 16733: delete_range
  status = db->DeleteRange(write_options, "key0314", "key10624");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 16734: get
  status = db->Get(read_options, "key3682", &value);
  if (status.ok()) {
    out << "GET key3682 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16735: put
  db->Put(write_options, "key8625", "value62962");

  // op 16736: delete_range
  status = db->DeleteRange(write_options, "key2868", "key6354");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 16737: put
  db->Put(write_options, "key10712", "value89384");

  // op 16738: delete_range
  status = db->DeleteRange(write_options, "key3236", "key10271");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 16739: delete
  db->Delete(write_options, "key10092");

  // op 16740: put
  db->Put(write_options, "key1916", "value94951");

  // op 16741: get
  status = db->Get(read_options, "key0521", &value);
  if (status.ok()) {
    out << "GET key0521 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16742: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 16743: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3977", "key7467", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3977, key7467) size=" << scan_result.size() << std::endl;
  }

  // op 16744: put
  db->Put(write_options, "key6762", "value45995");

  // op 16745: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2258", "key8095", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2258, key8095) size=" << scan_result.size() << std::endl;
  }

  // op 16746: get
  status = db->Get(read_options, "key8399", &value);
  if (status.ok()) {
    out << "GET key8399 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16747: delete
  db->Delete(write_options, "key10654");

  // op 16748: get
  status = db->Get(read_options, "key0520", &value);
  if (status.ok()) {
    out << "GET key0520 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16749: put
  db->Put(write_options, "key7216", "value10820");

  // op 16750: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0175", "key10688", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0175, key10688) size=" << scan_result.size() << std::endl;
  }

  // op 16751: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 16752: get
  status = db->Get(read_options, "key0916", &value);
  if (status.ok()) {
    out << "GET key0916 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16753: put
  db->Put(write_options, "key4446", "value87898");

  // op 16754: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7892", "key9145", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7892, key9145) size=" << scan_result.size() << std::endl;
  }

  // op 16755: put
  db->Put(write_options, "key5051", "value115970");

  // op 16756: put
  db->Put(write_options, "key5067", "value54234");

  // op 16757: get
  status = db->Get(read_options, "key2406", &value);
  if (status.ok()) {
    out << "GET key2406 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16758: delete_range
  status = db->DeleteRange(write_options, "key8370", "key11377");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 16759: put
  db->Put(write_options, "key8241", "value56740");

  // op 16760: delete_range
  status = db->DeleteRange(write_options, "key4297", "key10667");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 16761: get
  status = db->Get(read_options, "key9169", &value);
  if (status.ok()) {
    out << "GET key9169 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16762: put
  db->Put(write_options, "key4790", "value0484");

  // op 16763: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7079", "key9021", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7079, key9021) size=" << scan_result.size() << std::endl;
  }

  // op 16764: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0421", "key10445", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0421, key10445) size=" << scan_result.size() << std::endl;
  }

  // op 16765: put
  db->Put(write_options, "key8528", "value94240");

  // op 16766: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 16767: put
  db->Put(write_options, "key6164", "value112140");

  // op 16768: put
  db->Put(write_options, "key0782", "value54814");

  // op 16769: delete_range
  status = db->DeleteRange(write_options, "key10320", "key10948");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 16770: delete
  db->Delete(write_options, "key8704");

  // op 16771: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 16772: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3390", "key11345", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3390, key11345) size=" << scan_result.size() << std::endl;
  }

  // op 16773: delete_range
  status = db->DeleteRange(write_options, "key9579", "key11233");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 16774: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8380", "key11121", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8380, key11121) size=" << scan_result.size() << std::endl;
  }

  // op 16775: put
  db->Put(write_options, "key10109", "value102690");

  // op 16776: put
  db->Put(write_options, "key0925", "value46104");

  // op 16777: put
  db->Put(write_options, "key1007", "value104657");

  // op 16778: delete
  db->Delete(write_options, "key8193");

  // op 16779: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7180", "key9043", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7180, key9043) size=" << scan_result.size() << std::endl;
  }

  // op 16780: delete
  db->Delete(write_options, "key2314");

  // op 16781: get
  status = db->Get(read_options, "key0920", &value);
  if (status.ok()) {
    out << "GET key0920 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16782: get
  status = db->Get(read_options, "key4075", &value);
  if (status.ok()) {
    out << "GET key4075 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16783: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8936", "key10240", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8936, key10240) size=" << scan_result.size() << std::endl;
  }

  // op 16784: put
  db->Put(write_options, "key5791", "value3444");

  // op 16785: get
  status = db->Get(read_options, "key3092", &value);
  if (status.ok()) {
    out << "GET key3092 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16786: get
  status = db->Get(read_options, "key1202", &value);
  if (status.ok()) {
    out << "GET key1202 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16787: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 16788: delete
  db->Delete(write_options, "key8432");

  // op 16789: put
  db->Put(write_options, "key3074", "value28712");

  // op 16790: delete
  db->Delete(write_options, "key2313");

  // op 16791: put
  db->Put(write_options, "key4773", "value85141");

  // op 16792: put
  db->Put(write_options, "key7576", "value15394");

  // op 16793: get
  status = db->Get(read_options, "key2899", &value);
  if (status.ok()) {
    out << "GET key2899 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16794: get
  status = db->Get(read_options, "key2820", &value);
  if (status.ok()) {
    out << "GET key2820 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16795: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9061", "key10704", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9061, key10704) size=" << scan_result.size() << std::endl;
  }

  // op 16796: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3995", "key11119", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3995, key11119) size=" << scan_result.size() << std::endl;
  }

  // op 16797: delete_range
  status = db->DeleteRange(write_options, "key0085", "key3344");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 16798: get
  status = db->Get(read_options, "key1549", &value);
  if (status.ok()) {
    out << "GET key1549 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16799: delete_range
  status = db->DeleteRange(write_options, "key0543", "key2733");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 16800: put
  db->Put(write_options, "key4802", "value91107");

  // op 16801: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 16802: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2637", "key7667", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2637, key7667) size=" << scan_result.size() << std::endl;
  }

  // op 16803: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5777", "key8837", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5777, key8837) size=" << scan_result.size() << std::endl;
  }

  // op 16804: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8395", "key9320", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8395, key9320) size=" << scan_result.size() << std::endl;
  }

  // op 16805: get
  status = db->Get(read_options, "key5114", &value);
  if (status.ok()) {
    out << "GET key5114 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16806: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7964", "key9471", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7964, key9471) size=" << scan_result.size() << std::endl;
  }

  // op 16807: put
  db->Put(write_options, "key1375", "value107282");

  // op 16808: get
  status = db->Get(read_options, "key3372", &value);
  if (status.ok()) {
    out << "GET key3372 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16809: get
  status = db->Get(read_options, "key4290", &value);
  if (status.ok()) {
    out << "GET key4290 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16810: get
  status = db->Get(read_options, "key3621", &value);
  if (status.ok()) {
    out << "GET key3621 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16811: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8151", "key10510", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8151, key10510) size=" << scan_result.size() << std::endl;
  }

  // op 16812: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0126", "key1659", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0126, key1659) size=" << scan_result.size() << std::endl;
  }

  // op 16813: delete_range
  status = db->DeleteRange(write_options, "key3004", "key6961");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 16814: put
  db->Put(write_options, "key0688", "value27718");

  // op 16815: get
  status = db->Get(read_options, "key11481", &value);
  if (status.ok()) {
    out << "GET key11481 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16816: get
  status = db->Get(read_options, "key7850", &value);
  if (status.ok()) {
    out << "GET key7850 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16817: delete
  db->Delete(write_options, "key3806");

  // op 16818: get
  status = db->Get(read_options, "key11222", &value);
  if (status.ok()) {
    out << "GET key11222 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16819: put
  db->Put(write_options, "key3422", "value49649");

  // op 16820: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 16821: delete_range
  status = db->DeleteRange(write_options, "key0642", "key5672");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 16822: put
  db->Put(write_options, "key10733", "value45051");

  // op 16823: get
  status = db->Get(read_options, "key4689", &value);
  if (status.ok()) {
    out << "GET key4689 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16824: get
  status = db->Get(read_options, "key4495", &value);
  if (status.ok()) {
    out << "GET key4495 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16825: delete_range
  status = db->DeleteRange(write_options, "key6947", "key10349");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 16826: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0448", "key9202", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0448, key9202) size=" << scan_result.size() << std::endl;
  }

  // op 16827: put
  db->Put(write_options, "key5045", "value45306");

  // op 16828: delete
  db->Delete(write_options, "key1299");

  // op 16829: put
  db->Put(write_options, "key10188", "value75079");

  // op 16830: delete
  db->Delete(write_options, "key2334");

  // op 16831: put
  db->Put(write_options, "key11366", "value66397");

  // op 16832: put
  db->Put(write_options, "key3731", "value32219");

  // op 16833: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 16834: put
  db->Put(write_options, "key8631", "value27245");

  // op 16835: delete
  db->Delete(write_options, "key2852");

  // op 16836: delete
  db->Delete(write_options, "key2219");

  // op 16837: put
  db->Put(write_options, "key9671", "value70072");

  // op 16838: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 16839: put
  db->Put(write_options, "key8439", "value18691");

  // op 16840: put
  db->Put(write_options, "key1579", "value85240");

  // op 16841: put
  db->Put(write_options, "key4033", "value24327");

  // op 16842: put
  db->Put(write_options, "key5459", "value22268");

  // op 16843: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3058", "key9964", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3058, key9964) size=" << scan_result.size() << std::endl;
  }

  // op 16844: put
  db->Put(write_options, "key5235", "value20852");

  // op 16845: put
  db->Put(write_options, "key10596", "value34909");

  // op 16846: put
  db->Put(write_options, "key1266", "value51738");

  // op 16847: put
  db->Put(write_options, "key9555", "value89299");

  // op 16848: put
  db->Put(write_options, "key7081", "value30939");

  // op 16849: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 16850: put
  db->Put(write_options, "key3896", "value92202");

  // op 16851: delete
  db->Delete(write_options, "key7917");

  // op 16852: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7450", "key7478", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7450, key7478) size=" << scan_result.size() << std::endl;
  }

  // op 16853: get
  status = db->Get(read_options, "key6902", &value);
  if (status.ok()) {
    out << "GET key6902 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16854: delete_range
  status = db->DeleteRange(write_options, "key7875", "key9762");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 16855: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5013", "key9451", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5013, key9451) size=" << scan_result.size() << std::endl;
  }

  // op 16856: put
  db->Put(write_options, "key6277", "value89197");

  // op 16857: put
  db->Put(write_options, "key1995", "value104980");

  // op 16858: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5515", "key11703", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5515, key11703) size=" << scan_result.size() << std::endl;
  }

  // op 16859: delete_range
  status = db->DeleteRange(write_options, "key1893", "key8561");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 16860: put
  db->Put(write_options, "key4878", "value82886");

  // op 16861: get
  status = db->Get(read_options, "key10106", &value);
  if (status.ok()) {
    out << "GET key10106 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16862: put
  db->Put(write_options, "key4908", "value31396");

  // op 16863: get
  status = db->Get(read_options, "key9104", &value);
  if (status.ok()) {
    out << "GET key9104 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16864: get
  status = db->Get(read_options, "key3029", &value);
  if (status.ok()) {
    out << "GET key3029 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16865: delete
  db->Delete(write_options, "key9824");

  // op 16866: delete
  db->Delete(write_options, "key8413");

  // op 16867: get
  status = db->Get(read_options, "key10801", &value);
  if (status.ok()) {
    out << "GET key10801 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16868: put
  db->Put(write_options, "key4265", "value63094");

  // op 16869: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 16870: put
  db->Put(write_options, "key10665", "value37645");

  // op 16871: get
  status = db->Get(read_options, "key6856", &value);
  if (status.ok()) {
    out << "GET key6856 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16872: delete_range
  status = db->DeleteRange(write_options, "key4797", "key11406");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 16873: put
  db->Put(write_options, "key10324", "value87816");

  // op 16874: delete_range
  status = db->DeleteRange(write_options, "key0764", "key10683");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 16875: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1788", "key11421", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1788, key11421) size=" << scan_result.size() << std::endl;
  }

  // op 16876: put
  db->Put(write_options, "key5789", "value23675");

  // op 16877: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 16878: delete
  db->Delete(write_options, "key3291");

  // op 16879: get
  status = db->Get(read_options, "key9635", &value);
  if (status.ok()) {
    out << "GET key9635 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16880: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3493", "key11074", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3493, key11074) size=" << scan_result.size() << std::endl;
  }

  // op 16881: get
  status = db->Get(read_options, "key9145", &value);
  if (status.ok()) {
    out << "GET key9145 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16882: delete
  db->Delete(write_options, "key11274");

  // op 16883: get
  status = db->Get(read_options, "key1522", &value);
  if (status.ok()) {
    out << "GET key1522 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16884: put
  db->Put(write_options, "key11227", "value18250");

  // op 16885: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6673", "key10093", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6673, key10093) size=" << scan_result.size() << std::endl;
  }

  // op 16886: put
  db->Put(write_options, "key11684", "value37342");

  // op 16887: delete_range
  status = db->DeleteRange(write_options, "key3902", "key7904");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 16888: put
  db->Put(write_options, "key3209", "value108231");

  // op 16889: put
  db->Put(write_options, "key7726", "value110600");

  // op 16890: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5730", "key6319", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5730, key6319) size=" << scan_result.size() << std::endl;
  }

  // op 16891: get
  status = db->Get(read_options, "key3640", &value);
  if (status.ok()) {
    out << "GET key3640 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16892: delete_range
  status = db->DeleteRange(write_options, "key6840", "key7187");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 16893: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1054", "key5205", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1054, key5205) size=" << scan_result.size() << std::endl;
  }

  // op 16894: put
  db->Put(write_options, "key10018", "value104567");

  // op 16895: put
  db->Put(write_options, "key0469", "value19836");

  // op 16896: put
  db->Put(write_options, "key9841", "value45167");

  // op 16897: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4389", "key5512", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4389, key5512) size=" << scan_result.size() << std::endl;
  }

  // op 16898: put
  db->Put(write_options, "key0292", "value31800");

  // op 16899: delete
  db->Delete(write_options, "key1181");

  // op 16900: put
  db->Put(write_options, "key11375", "value81713");

  // op 16901: delete_range
  status = db->DeleteRange(write_options, "key9874", "key11380");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 16902: delete
  db->Delete(write_options, "key10244");

  // op 16903: get
  status = db->Get(read_options, "key1483", &value);
  if (status.ok()) {
    out << "GET key1483 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16904: get
  status = db->Get(read_options, "key9339", &value);
  if (status.ok()) {
    out << "GET key9339 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16905: get
  status = db->Get(read_options, "key3101", &value);
  if (status.ok()) {
    out << "GET key3101 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16906: get
  status = db->Get(read_options, "key0945", &value);
  if (status.ok()) {
    out << "GET key0945 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16907: put
  db->Put(write_options, "key8564", "value117384");

  // op 16908: get
  status = db->Get(read_options, "key7956", &value);
  if (status.ok()) {
    out << "GET key7956 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16909: put
  db->Put(write_options, "key5867", "value3943");

  // op 16910: get
  status = db->Get(read_options, "key3138", &value);
  if (status.ok()) {
    out << "GET key3138 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16911: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 16912: put
  db->Put(write_options, "key6514", "value109087");

  // op 16913: put
  db->Put(write_options, "key5497", "value44283");

  // op 16914: get
  status = db->Get(read_options, "key5350", &value);
  if (status.ok()) {
    out << "GET key5350 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16915: put
  db->Put(write_options, "key8958", "value0191");

  // op 16916: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2243", "key4856", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2243, key4856) size=" << scan_result.size() << std::endl;
  }

  // op 16917: delete
  db->Delete(write_options, "key1729");

  // op 16918: put
  db->Put(write_options, "key0949", "value103072");

  // op 16919: put
  db->Put(write_options, "key9510", "value64719");

  // op 16920: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3295", "key5064", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3295, key5064) size=" << scan_result.size() << std::endl;
  }

  // op 16921: delete
  db->Delete(write_options, "key9585");

  // op 16922: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 16923: put
  db->Put(write_options, "key8876", "value75380");

  // op 16924: get
  status = db->Get(read_options, "key11594", &value);
  if (status.ok()) {
    out << "GET key11594 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16925: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9152", "key10447", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9152, key10447) size=" << scan_result.size() << std::endl;
  }

  // op 16926: put
  db->Put(write_options, "key1210", "value65342");

  // op 16927: put
  db->Put(write_options, "key6953", "value43516");

  // op 16928: put
  db->Put(write_options, "key1379", "value37173");

  // op 16929: get
  status = db->Get(read_options, "key11502", &value);
  if (status.ok()) {
    out << "GET key11502 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16930: put
  db->Put(write_options, "key5563", "value14158");

  // op 16931: delete_range
  status = db->DeleteRange(write_options, "key1939", "key3752");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 16932: put
  db->Put(write_options, "key6408", "value43248");

  // op 16933: put
  db->Put(write_options, "key5605", "value17207");

  // op 16934: put
  db->Put(write_options, "key4932", "value96149");

  // op 16935: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4965", "key9042", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4965, key9042) size=" << scan_result.size() << std::endl;
  }

  // op 16936: put
  db->Put(write_options, "key7971", "value81910");

  // op 16937: put
  db->Put(write_options, "key5071", "value71078");

  // op 16938: get
  status = db->Get(read_options, "key7085", &value);
  if (status.ok()) {
    out << "GET key7085 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16939: delete_range
  status = db->DeleteRange(write_options, "key3400", "key5293");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 16940: put
  db->Put(write_options, "key1090", "value117214");

  // op 16941: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1303", "key2027", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1303, key2027) size=" << scan_result.size() << std::endl;
  }

  // op 16942: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1607", "key5697", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1607, key5697) size=" << scan_result.size() << std::endl;
  }

  // op 16943: put
  db->Put(write_options, "key11678", "value17255");

  // op 16944: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9155", "key11624", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9155, key11624) size=" << scan_result.size() << std::endl;
  }

  // op 16945: delete
  db->Delete(write_options, "key8271");

  // op 16946: get
  status = db->Get(read_options, "key7486", &value);
  if (status.ok()) {
    out << "GET key7486 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16947: put
  db->Put(write_options, "key9911", "value96946");

  // op 16948: get
  status = db->Get(read_options, "key4460", &value);
  if (status.ok()) {
    out << "GET key4460 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16949: get
  status = db->Get(read_options, "key7412", &value);
  if (status.ok()) {
    out << "GET key7412 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16950: put
  db->Put(write_options, "key10418", "value34653");

  // op 16951: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 16952: get
  status = db->Get(read_options, "key4647", &value);
  if (status.ok()) {
    out << "GET key4647 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16953: put
  db->Put(write_options, "key8722", "value14000");

  // op 16954: get
  status = db->Get(read_options, "key2894", &value);
  if (status.ok()) {
    out << "GET key2894 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16955: put
  db->Put(write_options, "key4746", "value104373");

  // op 16956: delete
  db->Delete(write_options, "key3479");

  // op 16957: delete
  db->Delete(write_options, "key6004");

  // op 16958: put
  db->Put(write_options, "key10694", "value28425");

  // op 16959: delete
  db->Delete(write_options, "key4589");

  // op 16960: put
  db->Put(write_options, "key1641", "value81553");

  // op 16961: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3929", "key11546", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3929, key11546) size=" << scan_result.size() << std::endl;
  }

  // op 16962: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4876", "key8724", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4876, key8724) size=" << scan_result.size() << std::endl;
  }

  // op 16963: get
  status = db->Get(read_options, "key0779", &value);
  if (status.ok()) {
    out << "GET key0779 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16964: get
  status = db->Get(read_options, "key2080", &value);
  if (status.ok()) {
    out << "GET key2080 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16965: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5725", "key6892", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5725, key6892) size=" << scan_result.size() << std::endl;
  }

  // op 16966: put
  db->Put(write_options, "key10954", "value78316");

  // op 16967: put
  db->Put(write_options, "key9095", "value23537");

  // op 16968: get
  status = db->Get(read_options, "key11038", &value);
  if (status.ok()) {
    out << "GET key11038 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16969: delete_range
  status = db->DeleteRange(write_options, "key4994", "key10438");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 16970: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4608", "key6430", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4608, key6430) size=" << scan_result.size() << std::endl;
  }

  // op 16971: delete_range
  status = db->DeleteRange(write_options, "key4422", "key6585");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 16972: get
  status = db->Get(read_options, "key0129", &value);
  if (status.ok()) {
    out << "GET key0129 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16973: put
  db->Put(write_options, "key4328", "value58775");

  // op 16974: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1400", "key6479", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1400, key6479) size=" << scan_result.size() << std::endl;
  }

  // op 16975: get
  status = db->Get(read_options, "key7539", &value);
  if (status.ok()) {
    out << "GET key7539 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16976: get
  status = db->Get(read_options, "key6529", &value);
  if (status.ok()) {
    out << "GET key6529 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16977: put
  db->Put(write_options, "key9799", "value22816");

  // op 16978: put
  db->Put(write_options, "key6599", "value45729");

  // op 16979: put
  db->Put(write_options, "key10382", "value102570");

  // op 16980: get
  status = db->Get(read_options, "key4249", &value);
  if (status.ok()) {
    out << "GET key4249 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16981: delete
  db->Delete(write_options, "key5177");

  // op 16982: put
  db->Put(write_options, "key9608", "value108906");

  // op 16983: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0403", "key7333", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0403, key7333) size=" << scan_result.size() << std::endl;
  }

  // op 16984: put
  db->Put(write_options, "key9034", "value79448");

  // op 16985: delete
  db->Delete(write_options, "key1014");

  // op 16986: put
  db->Put(write_options, "key0634", "value12766");

  // op 16987: put
  db->Put(write_options, "key6401", "value105999");

  // op 16988: put
  db->Put(write_options, "key10739", "value44899");

  // op 16989: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0541", "key1923", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0541, key1923) size=" << scan_result.size() << std::endl;
  }

  // op 16990: put
  db->Put(write_options, "key1582", "value13007");

  // op 16991: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1435", "key2680", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1435, key2680) size=" << scan_result.size() << std::endl;
  }

  // op 16992: put
  db->Put(write_options, "key8436", "value82497");

  // op 16993: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6460", "key10485", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6460, key10485) size=" << scan_result.size() << std::endl;
  }

  // op 16994: get
  status = db->Get(read_options, "key11181", &value);
  if (status.ok()) {
    out << "GET key11181 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 16995: put
  db->Put(write_options, "key9188", "value8683");

  // op 16996: put
  db->Put(write_options, "key0207", "value41706");

  // op 16997: put
  db->Put(write_options, "key2746", "value66886");

  // op 16998: put
  db->Put(write_options, "key6267", "value57661");

  // op 16999: get
  status = db->Get(read_options, "key5558", &value);
  if (status.ok()) {
    out << "GET key5558 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17000: put
  db->Put(write_options, "key2944", "value41804");

  // op 17001: put
  db->Put(write_options, "key1505", "value91545");

  // op 17002: get
  status = db->Get(read_options, "key0058", &value);
  if (status.ok()) {
    out << "GET key0058 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17003: delete
  db->Delete(write_options, "key3769");

  // op 17004: get
  status = db->Get(read_options, "key1738", &value);
  if (status.ok()) {
    out << "GET key1738 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17005: put
  db->Put(write_options, "key4365", "value77411");

  // op 17006: get
  status = db->Get(read_options, "key6390", &value);
  if (status.ok()) {
    out << "GET key6390 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17007: get
  status = db->Get(read_options, "key5472", &value);
  if (status.ok()) {
    out << "GET key5472 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17008: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1905", "key8014", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1905, key8014) size=" << scan_result.size() << std::endl;
  }

  // op 17009: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7499", "key11075", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7499, key11075) size=" << scan_result.size() << std::endl;
  }

  // op 17010: get
  status = db->Get(read_options, "key0860", &value);
  if (status.ok()) {
    out << "GET key0860 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17011: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4482", "key7549", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4482, key7549) size=" << scan_result.size() << std::endl;
  }

  // op 17012: get
  status = db->Get(read_options, "key9138", &value);
  if (status.ok()) {
    out << "GET key9138 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17013: get
  status = db->Get(read_options, "key6248", &value);
  if (status.ok()) {
    out << "GET key6248 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17014: put
  db->Put(write_options, "key10405", "value92186");

  // op 17015: put
  db->Put(write_options, "key10834", "value5476");

  // op 17016: put
  db->Put(write_options, "key5513", "value54234");

  // op 17017: put
  db->Put(write_options, "key1639", "value102577");

  // op 17018: get
  status = db->Get(read_options, "key2936", &value);
  if (status.ok()) {
    out << "GET key2936 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17019: delete
  db->Delete(write_options, "key10091");

  // op 17020: get
  status = db->Get(read_options, "key7757", &value);
  if (status.ok()) {
    out << "GET key7757 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17021: put
  db->Put(write_options, "key3316", "value102231");

  // op 17022: put
  db->Put(write_options, "key6324", "value112834");

  // op 17023: get
  status = db->Get(read_options, "key10133", &value);
  if (status.ok()) {
    out << "GET key10133 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17024: put
  db->Put(write_options, "key2883", "value48347");

  // op 17025: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 17026: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1626", "key11703", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1626, key11703) size=" << scan_result.size() << std::endl;
  }

  // op 17027: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7757", "key8379", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7757, key8379) size=" << scan_result.size() << std::endl;
  }

  // op 17028: get
  status = db->Get(read_options, "key8941", &value);
  if (status.ok()) {
    out << "GET key8941 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17029: get
  status = db->Get(read_options, "key2797", &value);
  if (status.ok()) {
    out << "GET key2797 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17030: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3252", "key9023", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3252, key9023) size=" << scan_result.size() << std::endl;
  }

  // op 17031: put
  db->Put(write_options, "key6138", "value71122");

  // op 17032: delete_range
  status = db->DeleteRange(write_options, "key1026", "key4092");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 17033: get
  status = db->Get(read_options, "key10985", &value);
  if (status.ok()) {
    out << "GET key10985 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17034: get
  status = db->Get(read_options, "key11709", &value);
  if (status.ok()) {
    out << "GET key11709 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17035: put
  db->Put(write_options, "key4417", "value37171");

  // op 17036: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7714", "key9744", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7714, key9744) size=" << scan_result.size() << std::endl;
  }

  // op 17037: put
  db->Put(write_options, "key6988", "value105573");

  // op 17038: get
  status = db->Get(read_options, "key4284", &value);
  if (status.ok()) {
    out << "GET key4284 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17039: delete_range
  status = db->DeleteRange(write_options, "key1241", "key8824");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 17040: put
  db->Put(write_options, "key10083", "value13323");

  // op 17041: get
  status = db->Get(read_options, "key6916", &value);
  if (status.ok()) {
    out << "GET key6916 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17042: put
  db->Put(write_options, "key10754", "value61052");

  // op 17043: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5858", "key7354", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5858, key7354) size=" << scan_result.size() << std::endl;
  }

  // op 17044: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4544", "key8167", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4544, key8167) size=" << scan_result.size() << std::endl;
  }

  // op 17045: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8700", "key11476", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8700, key11476) size=" << scan_result.size() << std::endl;
  }

  // op 17046: put
  db->Put(write_options, "key1214", "value80771");

  // op 17047: put
  db->Put(write_options, "key2819", "value4437");

  // op 17048: get
  status = db->Get(read_options, "key5247", &value);
  if (status.ok()) {
    out << "GET key5247 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17049: put
  db->Put(write_options, "key0671", "value66161");

  // op 17050: put
  db->Put(write_options, "key2436", "value7156");

  // op 17051: put
  db->Put(write_options, "key2575", "value20585");

  // op 17052: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 17053: delete_range
  status = db->DeleteRange(write_options, "key3864", "key8083");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 17054: delete_range
  status = db->DeleteRange(write_options, "key2240", "key4900");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 17055: delete_range
  status = db->DeleteRange(write_options, "key6456", "key7929");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 17056: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 17057: put
  db->Put(write_options, "key4289", "value87806");

  // op 17058: get
  status = db->Get(read_options, "key7847", &value);
  if (status.ok()) {
    out << "GET key7847 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17059: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0210", "key3906", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0210, key3906) size=" << scan_result.size() << std::endl;
  }

  // op 17060: put
  db->Put(write_options, "key10926", "value67381");

  // op 17061: put
  db->Put(write_options, "key0099", "value104521");

  // op 17062: put
  db->Put(write_options, "key2243", "value68431");

  // op 17063: put
  db->Put(write_options, "key10420", "value62583");

  // op 17064: delete
  db->Delete(write_options, "key8059");

  // op 17065: get
  status = db->Get(read_options, "key7584", &value);
  if (status.ok()) {
    out << "GET key7584 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17066: get
  status = db->Get(read_options, "key5451", &value);
  if (status.ok()) {
    out << "GET key5451 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17067: put
  db->Put(write_options, "key7831", "value42217");

  // op 17068: delete
  db->Delete(write_options, "key8956");

  // op 17069: delete
  db->Delete(write_options, "key3667");

  // op 17070: put
  db->Put(write_options, "key7920", "value88546");

  // op 17071: get
  status = db->Get(read_options, "key11337", &value);
  if (status.ok()) {
    out << "GET key11337 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17072: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4728", "key9776", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4728, key9776) size=" << scan_result.size() << std::endl;
  }

  // op 17073: get
  status = db->Get(read_options, "key7240", &value);
  if (status.ok()) {
    out << "GET key7240 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17074: put
  db->Put(write_options, "key1427", "value95670");

  // op 17075: put
  db->Put(write_options, "key0090", "value66316");

  // op 17076: put
  db->Put(write_options, "key7919", "value73965");

  // op 17077: put
  db->Put(write_options, "key9528", "value111529");

  // op 17078: put
  db->Put(write_options, "key6407", "value21659");

  // op 17079: delete
  db->Delete(write_options, "key5761");

  // op 17080: put
  db->Put(write_options, "key7313", "value24322");

  // op 17081: get
  status = db->Get(read_options, "key1087", &value);
  if (status.ok()) {
    out << "GET key1087 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17082: delete
  db->Delete(write_options, "key8266");

  // op 17083: put
  db->Put(write_options, "key7595", "value73688");

  // op 17084: put
  db->Put(write_options, "key1447", "value52926");

  // op 17085: put
  db->Put(write_options, "key9751", "value35554");

  // op 17086: get
  status = db->Get(read_options, "key5903", &value);
  if (status.ok()) {
    out << "GET key5903 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17087: put
  db->Put(write_options, "key5867", "value81955");

  // op 17088: delete_range
  status = db->DeleteRange(write_options, "key7866", "key9326");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 17089: put
  db->Put(write_options, "key8936", "value45820");

  // op 17090: put
  db->Put(write_options, "key4249", "value30332");

  // op 17091: put
  db->Put(write_options, "key0180", "value91808");

  // op 17092: put
  db->Put(write_options, "key4313", "value76126");

  // op 17093: put
  db->Put(write_options, "key2861", "value98072");

  // op 17094: get
  status = db->Get(read_options, "key7439", &value);
  if (status.ok()) {
    out << "GET key7439 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17095: put
  db->Put(write_options, "key0271", "value78306");

  // op 17096: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 17097: put
  db->Put(write_options, "key6461", "value115466");

  // op 17098: delete
  db->Delete(write_options, "key2107");

  // op 17099: put
  db->Put(write_options, "key8270", "value110764");

  // op 17100: get
  status = db->Get(read_options, "key1560", &value);
  if (status.ok()) {
    out << "GET key1560 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17101: get
  status = db->Get(read_options, "key0140", &value);
  if (status.ok()) {
    out << "GET key0140 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17102: put
  db->Put(write_options, "key8582", "value43934");

  // op 17103: put
  db->Put(write_options, "key8770", "value91808");

  // op 17104: get
  status = db->Get(read_options, "key6354", &value);
  if (status.ok()) {
    out << "GET key6354 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17105: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2601", "key5367", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2601, key5367) size=" << scan_result.size() << std::endl;
  }

  // op 17106: put
  db->Put(write_options, "key5817", "value0098");

  // op 17107: put
  db->Put(write_options, "key10985", "value110420");

  // op 17108: put
  db->Put(write_options, "key3041", "value77249");

  // op 17109: put
  db->Put(write_options, "key2914", "value110221");

  // op 17110: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0766", "key6537", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0766, key6537) size=" << scan_result.size() << std::endl;
  }

  // op 17111: get
  status = db->Get(read_options, "key7660", &value);
  if (status.ok()) {
    out << "GET key7660 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17112: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2764", "key3856", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2764, key3856) size=" << scan_result.size() << std::endl;
  }

  // op 17113: get
  status = db->Get(read_options, "key10557", &value);
  if (status.ok()) {
    out << "GET key10557 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17114: put
  db->Put(write_options, "key9712", "value109310");

  // op 17115: get
  status = db->Get(read_options, "key11438", &value);
  if (status.ok()) {
    out << "GET key11438 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17116: put
  db->Put(write_options, "key7747", "value35960");

  // op 17117: delete
  db->Delete(write_options, "key0486");

  // op 17118: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 17119: delete_range
  status = db->DeleteRange(write_options, "key2446", "key4261");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 17120: put
  db->Put(write_options, "key1468", "value2010");

  // op 17121: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7496", "key10606", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7496, key10606) size=" << scan_result.size() << std::endl;
  }

  // op 17122: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0211", "key4622", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0211, key4622) size=" << scan_result.size() << std::endl;
  }

  // op 17123: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0143", "key8730", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0143, key8730) size=" << scan_result.size() << std::endl;
  }

  // op 17124: put
  db->Put(write_options, "key5540", "value56312");

  // op 17125: delete_range
  status = db->DeleteRange(write_options, "key4859", "key7793");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 17126: get
  status = db->Get(read_options, "key6765", &value);
  if (status.ok()) {
    out << "GET key6765 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17127: get
  status = db->Get(read_options, "key4267", &value);
  if (status.ok()) {
    out << "GET key4267 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17128: put
  db->Put(write_options, "key0030", "value50149");

  // op 17129: delete_range
  status = db->DeleteRange(write_options, "key3543", "key8053");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 17130: delete_range
  status = db->DeleteRange(write_options, "key4299", "key4528");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 17131: put
  db->Put(write_options, "key3161", "value102428");

  // op 17132: put
  db->Put(write_options, "key5319", "value16733");

  // op 17133: delete_range
  status = db->DeleteRange(write_options, "key8032", "key9393");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 17134: put
  db->Put(write_options, "key5373", "value31787");

  // op 17135: delete
  db->Delete(write_options, "key7479");

  // op 17136: put
  db->Put(write_options, "key6851", "value117277");

  // op 17137: delete
  db->Delete(write_options, "key10218");

  // op 17138: put
  db->Put(write_options, "key9326", "value70322");

  // op 17139: put
  db->Put(write_options, "key3564", "value79477");

  // op 17140: get
  status = db->Get(read_options, "key8248", &value);
  if (status.ok()) {
    out << "GET key8248 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17141: get
  status = db->Get(read_options, "key0504", &value);
  if (status.ok()) {
    out << "GET key0504 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17142: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0200", "key5868", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0200, key5868) size=" << scan_result.size() << std::endl;
  }

  // op 17143: delete
  db->Delete(write_options, "key1145");

  // op 17144: delete
  db->Delete(write_options, "key0340");

  // op 17145: put
  db->Put(write_options, "key11675", "value14724");

  // op 17146: put
  db->Put(write_options, "key3725", "value48819");

  // op 17147: put
  db->Put(write_options, "key2237", "value108876");

  // op 17148: put
  db->Put(write_options, "key7229", "value101891");

  // op 17149: delete_range
  status = db->DeleteRange(write_options, "key3640", "key6184");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 17150: delete
  db->Delete(write_options, "key5013");

  // op 17151: delete
  db->Delete(write_options, "key10251");

  // op 17152: put
  db->Put(write_options, "key11723", "value56746");

  // op 17153: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 17154: put
  db->Put(write_options, "key8245", "value55521");

  // op 17155: get
  status = db->Get(read_options, "key4545", &value);
  if (status.ok()) {
    out << "GET key4545 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17156: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1206", "key6730", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1206, key6730) size=" << scan_result.size() << std::endl;
  }

  // op 17157: delete
  db->Delete(write_options, "key3429");

  // op 17158: delete
  db->Delete(write_options, "key10059");

  // op 17159: put
  db->Put(write_options, "key6253", "value5470");

  // op 17160: get
  status = db->Get(read_options, "key7779", &value);
  if (status.ok()) {
    out << "GET key7779 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17161: delete_range
  status = db->DeleteRange(write_options, "key0332", "key2445");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 17162: put
  db->Put(write_options, "key8592", "value20193");

  // op 17163: put
  db->Put(write_options, "key7262", "value98249");

  // op 17164: get
  status = db->Get(read_options, "key0576", &value);
  if (status.ok()) {
    out << "GET key0576 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17165: put
  db->Put(write_options, "key1714", "value111195");

  // op 17166: put
  db->Put(write_options, "key9086", "value40811");

  // op 17167: get
  status = db->Get(read_options, "key0560", &value);
  if (status.ok()) {
    out << "GET key0560 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17168: delete_range
  status = db->DeleteRange(write_options, "key4101", "key11449");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 17169: get
  status = db->Get(read_options, "key10946", &value);
  if (status.ok()) {
    out << "GET key10946 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17170: delete
  db->Delete(write_options, "key1463");

  // op 17171: get
  status = db->Get(read_options, "key3090", &value);
  if (status.ok()) {
    out << "GET key3090 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17172: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4553", "key4937", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4553, key4937) size=" << scan_result.size() << std::endl;
  }

  // op 17173: get
  status = db->Get(read_options, "key3238", &value);
  if (status.ok()) {
    out << "GET key3238 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17174: put
  db->Put(write_options, "key10793", "value27423");

  // op 17175: get
  status = db->Get(read_options, "key1990", &value);
  if (status.ok()) {
    out << "GET key1990 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17176: get
  status = db->Get(read_options, "key1534", &value);
  if (status.ok()) {
    out << "GET key1534 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17177: delete_range
  status = db->DeleteRange(write_options, "key1708", "key10485");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 17178: delete_range
  status = db->DeleteRange(write_options, "key7532", "key11049");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 17179: put
  db->Put(write_options, "key10598", "value38446");

  // op 17180: delete_range
  status = db->DeleteRange(write_options, "key5254", "key8114");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 17181: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2423", "key6557", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2423, key6557) size=" << scan_result.size() << std::endl;
  }

  // op 17182: delete_range
  status = db->DeleteRange(write_options, "key3841", "key8342");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 17183: delete
  db->Delete(write_options, "key6742");

  // op 17184: delete
  db->Delete(write_options, "key3259");

  // op 17185: get
  status = db->Get(read_options, "key0752", &value);
  if (status.ok()) {
    out << "GET key0752 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17186: put
  db->Put(write_options, "key8866", "value8753");

  // op 17187: put
  db->Put(write_options, "key0319", "value70712");

  // op 17188: put
  db->Put(write_options, "key10279", "value70564");

  // op 17189: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1697", "key7694", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1697, key7694) size=" << scan_result.size() << std::endl;
  }

  // op 17190: put
  db->Put(write_options, "key1334", "value74227");

  // op 17191: delete
  db->Delete(write_options, "key6091");

  // op 17192: put
  db->Put(write_options, "key0333", "value98316");

  // op 17193: put
  db->Put(write_options, "key9244", "value105775");

  // op 17194: get
  status = db->Get(read_options, "key1831", &value);
  if (status.ok()) {
    out << "GET key1831 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17195: delete_range
  status = db->DeleteRange(write_options, "key0015", "key10626");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 17196: get
  status = db->Get(read_options, "key0012", &value);
  if (status.ok()) {
    out << "GET key0012 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17197: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 17198: put
  db->Put(write_options, "key9782", "value46445");

  // op 17199: delete_range
  status = db->DeleteRange(write_options, "key6475", "key6689");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 17200: get
  status = db->Get(read_options, "key4578", &value);
  if (status.ok()) {
    out << "GET key4578 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17201: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0958", "key10908", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0958, key10908) size=" << scan_result.size() << std::endl;
  }

  // op 17202: put
  db->Put(write_options, "key11244", "value86066");

  // op 17203: put
  db->Put(write_options, "key6556", "value83056");

  // op 17204: get
  status = db->Get(read_options, "key10792", &value);
  if (status.ok()) {
    out << "GET key10792 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17205: put
  db->Put(write_options, "key2646", "value47649");

  // op 17206: put
  db->Put(write_options, "key3210", "value115458");

  // op 17207: put
  db->Put(write_options, "key0964", "value109053");

  // op 17208: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8476", "key9386", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8476, key9386) size=" << scan_result.size() << std::endl;
  }

  // op 17209: put
  db->Put(write_options, "key9908", "value50500");

  // op 17210: delete_range
  status = db->DeleteRange(write_options, "key1309", "key2417");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 17211: delete
  db->Delete(write_options, "key3773");

  // op 17212: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5632", "key10837", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5632, key10837) size=" << scan_result.size() << std::endl;
  }

  // op 17213: get
  status = db->Get(read_options, "key0917", &value);
  if (status.ok()) {
    out << "GET key0917 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17214: get
  status = db->Get(read_options, "key0882", &value);
  if (status.ok()) {
    out << "GET key0882 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17215: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5319", "key8321", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5319, key8321) size=" << scan_result.size() << std::endl;
  }

  // op 17216: put
  db->Put(write_options, "key1911", "value40386");

  // op 17217: put
  db->Put(write_options, "key4778", "value30011");

  // op 17218: put
  db->Put(write_options, "key5418", "value12871");

  // op 17219: get
  status = db->Get(read_options, "key6800", &value);
  if (status.ok()) {
    out << "GET key6800 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17220: put
  db->Put(write_options, "key1241", "value98409");

  // op 17221: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0878", "key10324", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0878, key10324) size=" << scan_result.size() << std::endl;
  }

  // op 17222: delete_range
  status = db->DeleteRange(write_options, "key1623", "key9426");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 17223: get
  status = db->Get(read_options, "key6890", &value);
  if (status.ok()) {
    out << "GET key6890 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17224: delete_range
  status = db->DeleteRange(write_options, "key3239", "key11638");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 17225: get
  status = db->Get(read_options, "key8233", &value);
  if (status.ok()) {
    out << "GET key8233 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17226: delete_range
  status = db->DeleteRange(write_options, "key6184", "key7061");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 17227: put
  db->Put(write_options, "key9211", "value22763");

  // op 17228: put
  db->Put(write_options, "key6633", "value83151");

  // op 17229: put
  db->Put(write_options, "key5768", "value20135");

  // op 17230: put
  db->Put(write_options, "key5697", "value40748");

  // op 17231: put
  db->Put(write_options, "key2324", "value95448");

  // op 17232: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3378", "key3574", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3378, key3574) size=" << scan_result.size() << std::endl;
  }

  // op 17233: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1099", "key3234", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1099, key3234) size=" << scan_result.size() << std::endl;
  }

  // op 17234: get
  status = db->Get(read_options, "key11577", &value);
  if (status.ok()) {
    out << "GET key11577 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17235: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2435", "key4729", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2435, key4729) size=" << scan_result.size() << std::endl;
  }

  // op 17236: put
  db->Put(write_options, "key5291", "value71043");

  // op 17237: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0279", "key1282", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0279, key1282) size=" << scan_result.size() << std::endl;
  }

  // op 17238: get
  status = db->Get(read_options, "key7390", &value);
  if (status.ok()) {
    out << "GET key7390 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17239: get
  status = db->Get(read_options, "key7565", &value);
  if (status.ok()) {
    out << "GET key7565 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17240: put
  db->Put(write_options, "key6414", "value103919");

  // op 17241: delete
  db->Delete(write_options, "key7061");

  // op 17242: put
  db->Put(write_options, "key10691", "value41472");

  // op 17243: put
  db->Put(write_options, "key11651", "value23465");

  // op 17244: put
  db->Put(write_options, "key6561", "value2346");

  // op 17245: get
  status = db->Get(read_options, "key7724", &value);
  if (status.ok()) {
    out << "GET key7724 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17246: get
  status = db->Get(read_options, "key5472", &value);
  if (status.ok()) {
    out << "GET key5472 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17247: delete
  db->Delete(write_options, "key4086");

  // op 17248: put
  db->Put(write_options, "key2844", "value7185");

  // op 17249: put
  db->Put(write_options, "key10120", "value79874");

  // op 17250: put
  db->Put(write_options, "key5971", "value71839");

  // op 17251: put
  db->Put(write_options, "key2068", "value68656");

  // op 17252: get
  status = db->Get(read_options, "key4997", &value);
  if (status.ok()) {
    out << "GET key4997 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17253: delete
  db->Delete(write_options, "key4044");

  // op 17254: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3026", "key4078", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3026, key4078) size=" << scan_result.size() << std::endl;
  }

  // op 17255: put
  db->Put(write_options, "key6163", "value91724");

  // op 17256: delete
  db->Delete(write_options, "key10964");

  // op 17257: delete
  db->Delete(write_options, "key5199");

  // op 17258: put
  db->Put(write_options, "key6290", "value74411");

  // op 17259: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1582", "key3925", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1582, key3925) size=" << scan_result.size() << std::endl;
  }

  // op 17260: put
  db->Put(write_options, "key11229", "value89344");

  // op 17261: get
  status = db->Get(read_options, "key0973", &value);
  if (status.ok()) {
    out << "GET key0973 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17262: delete
  db->Delete(write_options, "key6632");

  // op 17263: put
  db->Put(write_options, "key4712", "value91786");

  // op 17264: put
  db->Put(write_options, "key4360", "value47175");

  // op 17265: put
  db->Put(write_options, "key6426", "value45489");

  // op 17266: delete
  db->Delete(write_options, "key5365");

  // op 17267: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1855", "key4980", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1855, key4980) size=" << scan_result.size() << std::endl;
  }

  // op 17268: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1159", "key10197", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1159, key10197) size=" << scan_result.size() << std::endl;
  }

  // op 17269: put
  db->Put(write_options, "key9299", "value81957");

  // op 17270: delete_range
  status = db->DeleteRange(write_options, "key5669", "key8947");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 17271: put
  db->Put(write_options, "key9333", "value59386");

  // op 17272: put
  db->Put(write_options, "key6267", "value11270");

  // op 17273: put
  db->Put(write_options, "key3769", "value31850");

  // op 17274: delete
  db->Delete(write_options, "key2561");

  // op 17275: delete
  db->Delete(write_options, "key9669");

  // op 17276: put
  db->Put(write_options, "key3719", "value108538");

  // op 17277: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5434", "key7471", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5434, key7471) size=" << scan_result.size() << std::endl;
  }

  // op 17278: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4250", "key10894", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4250, key10894) size=" << scan_result.size() << std::endl;
  }

  // op 17279: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0115", "key7927", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0115, key7927) size=" << scan_result.size() << std::endl;
  }

  // op 17280: put
  db->Put(write_options, "key10238", "value63980");

  // op 17281: put
  db->Put(write_options, "key9376", "value33549");

  // op 17282: put
  db->Put(write_options, "key4560", "value53171");

  // op 17283: put
  db->Put(write_options, "key3051", "value99543");

  // op 17284: put
  db->Put(write_options, "key2134", "value87752");

  // op 17285: put
  db->Put(write_options, "key7474", "value65783");

  // op 17286: put
  db->Put(write_options, "key4048", "value67301");

  // op 17287: put
  db->Put(write_options, "key10088", "value94111");

  // op 17288: put
  db->Put(write_options, "key9243", "value84255");

  // op 17289: delete_range
  status = db->DeleteRange(write_options, "key9737", "key11063");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 17290: put
  db->Put(write_options, "key7261", "value38968");

  // op 17291: delete_range
  status = db->DeleteRange(write_options, "key4953", "key7072");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 17292: get
  status = db->Get(read_options, "key3508", &value);
  if (status.ok()) {
    out << "GET key3508 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17293: put
  db->Put(write_options, "key2443", "value49702");

  // op 17294: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9281", "key10969", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9281, key10969) size=" << scan_result.size() << std::endl;
  }

  // op 17295: delete
  db->Delete(write_options, "key11142");

  // op 17296: get
  status = db->Get(read_options, "key2584", &value);
  if (status.ok()) {
    out << "GET key2584 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17297: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8328", "key9916", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8328, key9916) size=" << scan_result.size() << std::endl;
  }

  // op 17298: get
  status = db->Get(read_options, "key1476", &value);
  if (status.ok()) {
    out << "GET key1476 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17299: delete
  db->Delete(write_options, "key11655");

  // op 17300: put
  db->Put(write_options, "key2747", "value100425");

  // op 17301: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2479", "key4274", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2479, key4274) size=" << scan_result.size() << std::endl;
  }

  // op 17302: put
  db->Put(write_options, "key7048", "value63935");

  // op 17303: put
  db->Put(write_options, "key8868", "value81031");

  // op 17304: get
  status = db->Get(read_options, "key5027", &value);
  if (status.ok()) {
    out << "GET key5027 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17305: put
  db->Put(write_options, "key0966", "value38807");

  // op 17306: put
  db->Put(write_options, "key10405", "value5362");

  // op 17307: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3640", "key5857", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3640, key5857) size=" << scan_result.size() << std::endl;
  }

  // op 17308: delete_range
  status = db->DeleteRange(write_options, "key11290", "key11600");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 17309: delete_range
  status = db->DeleteRange(write_options, "key3198", "key8165");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 17310: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0091", "key10862", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0091, key10862) size=" << scan_result.size() << std::endl;
  }

  // op 17311: get
  status = db->Get(read_options, "key4853", &value);
  if (status.ok()) {
    out << "GET key4853 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17312: get
  status = db->Get(read_options, "key4723", &value);
  if (status.ok()) {
    out << "GET key4723 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17313: get
  status = db->Get(read_options, "key7598", &value);
  if (status.ok()) {
    out << "GET key7598 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17314: put
  db->Put(write_options, "key4346", "value5164");

  // op 17315: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2762", "key9931", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2762, key9931) size=" << scan_result.size() << std::endl;
  }

  // op 17316: put
  db->Put(write_options, "key7794", "value9354");

  // op 17317: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3897", "key7014", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3897, key7014) size=" << scan_result.size() << std::endl;
  }

  // op 17318: put
  db->Put(write_options, "key2245", "value95751");

  // op 17319: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 17320: put
  db->Put(write_options, "key3776", "value77555");

  // op 17321: put
  db->Put(write_options, "key4349", "value102038");

  // op 17322: get
  status = db->Get(read_options, "key9287", &value);
  if (status.ok()) {
    out << "GET key9287 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17323: get
  status = db->Get(read_options, "key0303", &value);
  if (status.ok()) {
    out << "GET key0303 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17324: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 17325: put
  db->Put(write_options, "key2996", "value19221");

  // op 17326: put
  db->Put(write_options, "key9020", "value114627");

  // op 17327: put
  db->Put(write_options, "key9952", "value57243");

  // op 17328: put
  db->Put(write_options, "key8872", "value54431");

  // op 17329: get
  status = db->Get(read_options, "key1366", &value);
  if (status.ok()) {
    out << "GET key1366 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17330: get
  status = db->Get(read_options, "key8294", &value);
  if (status.ok()) {
    out << "GET key8294 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17331: delete
  db->Delete(write_options, "key11152");

  // op 17332: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4517", "key11117", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4517, key11117) size=" << scan_result.size() << std::endl;
  }

  // op 17333: put
  db->Put(write_options, "key10699", "value1762");

  // op 17334: put
  db->Put(write_options, "key4868", "value86275");

  // op 17335: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6083", "key8038", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6083, key8038) size=" << scan_result.size() << std::endl;
  }

  // op 17336: delete_range
  status = db->DeleteRange(write_options, "key0325", "key4145");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 17337: get
  status = db->Get(read_options, "key4651", &value);
  if (status.ok()) {
    out << "GET key4651 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17338: get
  status = db->Get(read_options, "key1152", &value);
  if (status.ok()) {
    out << "GET key1152 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17339: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0602", "key10537", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0602, key10537) size=" << scan_result.size() << std::endl;
  }

  // op 17340: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 17341: put
  db->Put(write_options, "key8079", "value83681");

  // op 17342: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8390", "key10048", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8390, key10048) size=" << scan_result.size() << std::endl;
  }

  // op 17343: put
  db->Put(write_options, "key8564", "value71378");

  // op 17344: put
  db->Put(write_options, "key2513", "value95398");

  // op 17345: put
  db->Put(write_options, "key6628", "value77581");

  // op 17346: get
  status = db->Get(read_options, "key2780", &value);
  if (status.ok()) {
    out << "GET key2780 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17347: delete
  db->Delete(write_options, "key6944");

  // op 17348: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0110", "key1350", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0110, key1350) size=" << scan_result.size() << std::endl;
  }

  // op 17349: put
  db->Put(write_options, "key7647", "value35427");

  // op 17350: put
  db->Put(write_options, "key2979", "value58962");

  // op 17351: get
  status = db->Get(read_options, "key1340", &value);
  if (status.ok()) {
    out << "GET key1340 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17352: get
  status = db->Get(read_options, "key0598", &value);
  if (status.ok()) {
    out << "GET key0598 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17353: put
  db->Put(write_options, "key4942", "value62338");

  // op 17354: get
  status = db->Get(read_options, "key7170", &value);
  if (status.ok()) {
    out << "GET key7170 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17355: put
  db->Put(write_options, "key2713", "value62068");

  // op 17356: put
  db->Put(write_options, "key11698", "value42331");

  // op 17357: delete
  db->Delete(write_options, "key2499");

  // op 17358: put
  db->Put(write_options, "key2775", "value29138");

  // op 17359: get
  status = db->Get(read_options, "key9552", &value);
  if (status.ok()) {
    out << "GET key9552 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17360: put
  db->Put(write_options, "key2695", "value10841");

  // op 17361: put
  db->Put(write_options, "key9171", "value8684");

  // op 17362: delete
  db->Delete(write_options, "key11483");

  // op 17363: delete
  db->Delete(write_options, "key4963");

  // op 17364: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6008", "key10717", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6008, key10717) size=" << scan_result.size() << std::endl;
  }

  // op 17365: delete_range
  status = db->DeleteRange(write_options, "key2460", "key3186");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 17366: put
  db->Put(write_options, "key5598", "value62797");

  // op 17367: put
  db->Put(write_options, "key10613", "value103594");

  // op 17368: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5152", "key10428", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5152, key10428) size=" << scan_result.size() << std::endl;
  }

  // op 17369: delete_range
  status = db->DeleteRange(write_options, "key0962", "key1234");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 17370: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 17371: put
  db->Put(write_options, "key2484", "value19073");

  // op 17372: delete
  db->Delete(write_options, "key4926");

  // op 17373: put
  db->Put(write_options, "key8384", "value110404");

  // op 17374: put
  db->Put(write_options, "key6852", "value27658");

  // op 17375: put
  db->Put(write_options, "key8298", "value81984");

  // op 17376: get
  status = db->Get(read_options, "key10318", &value);
  if (status.ok()) {
    out << "GET key10318 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17377: get
  status = db->Get(read_options, "key5652", &value);
  if (status.ok()) {
    out << "GET key5652 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17378: delete
  db->Delete(write_options, "key8778");

  // op 17379: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 17380: put
  db->Put(write_options, "key7858", "value95245");

  // op 17381: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1265", "key5568", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1265, key5568) size=" << scan_result.size() << std::endl;
  }

  // op 17382: put
  db->Put(write_options, "key5073", "value9205");

  // op 17383: put
  db->Put(write_options, "key10571", "value0828");

  // op 17384: put
  db->Put(write_options, "key5836", "value59851");

  // op 17385: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2127", "key2766", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2127, key2766) size=" << scan_result.size() << std::endl;
  }

  // op 17386: put
  db->Put(write_options, "key10207", "value14460");

  // op 17387: get
  status = db->Get(read_options, "key9830", &value);
  if (status.ok()) {
    out << "GET key9830 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17388: put
  db->Put(write_options, "key5677", "value1348");

  // op 17389: get
  status = db->Get(read_options, "key9912", &value);
  if (status.ok()) {
    out << "GET key9912 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17390: delete_range
  status = db->DeleteRange(write_options, "key3163", "key8970");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 17391: put
  db->Put(write_options, "key4819", "value88839");

  // op 17392: get
  status = db->Get(read_options, "key11281", &value);
  if (status.ok()) {
    out << "GET key11281 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17393: get
  status = db->Get(read_options, "key4640", &value);
  if (status.ok()) {
    out << "GET key4640 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17394: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 17395: put
  db->Put(write_options, "key0072", "value47603");

  // op 17396: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0558", "key2278", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0558, key2278) size=" << scan_result.size() << std::endl;
  }

  // op 17397: put
  db->Put(write_options, "key2611", "value6655");

  // op 17398: get
  status = db->Get(read_options, "key9514", &value);
  if (status.ok()) {
    out << "GET key9514 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17399: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1497", "key8904", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1497, key8904) size=" << scan_result.size() << std::endl;
  }

  // op 17400: get
  status = db->Get(read_options, "key3254", &value);
  if (status.ok()) {
    out << "GET key3254 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17401: put
  db->Put(write_options, "key10364", "value81859");

  // op 17402: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 17403: delete_range
  status = db->DeleteRange(write_options, "key2280", "key4182");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 17404: put
  db->Put(write_options, "key11685", "value72906");

  // op 17405: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3385", "key10614", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3385, key10614) size=" << scan_result.size() << std::endl;
  }

  // op 17406: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 17407: put
  db->Put(write_options, "key3090", "value34866");

  // op 17408: delete
  db->Delete(write_options, "key8652");

  // op 17409: get
  status = db->Get(read_options, "key10303", &value);
  if (status.ok()) {
    out << "GET key10303 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17410: put
  db->Put(write_options, "key10177", "value72881");

  // op 17411: get
  status = db->Get(read_options, "key6507", &value);
  if (status.ok()) {
    out << "GET key6507 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17412: put
  db->Put(write_options, "key1894", "value60562");

  // op 17413: put
  db->Put(write_options, "key11439", "value5809");

  // op 17414: get
  status = db->Get(read_options, "key1512", &value);
  if (status.ok()) {
    out << "GET key1512 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17415: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9486", "key11656", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9486, key11656) size=" << scan_result.size() << std::endl;
  }

  // op 17416: put
  db->Put(write_options, "key10506", "value82252");

  // op 17417: delete
  db->Delete(write_options, "key4964");

  // op 17418: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1638", "key5953", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1638, key5953) size=" << scan_result.size() << std::endl;
  }

  // op 17419: delete
  db->Delete(write_options, "key4218");

  // op 17420: put
  db->Put(write_options, "key1529", "value72757");

  // op 17421: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3002", "key3252", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3002, key3252) size=" << scan_result.size() << std::endl;
  }

  // op 17422: get
  status = db->Get(read_options, "key5273", &value);
  if (status.ok()) {
    out << "GET key5273 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17423: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 17424: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9558", "key10383", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9558, key10383) size=" << scan_result.size() << std::endl;
  }

  // op 17425: get
  status = db->Get(read_options, "key3232", &value);
  if (status.ok()) {
    out << "GET key3232 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17426: put
  db->Put(write_options, "key3315", "value81745");

  // op 17427: put
  db->Put(write_options, "key0823", "value20872");

  // op 17428: delete
  db->Delete(write_options, "key8875");

  // op 17429: delete_range
  status = db->DeleteRange(write_options, "key3120", "key9852");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 17430: delete
  db->Delete(write_options, "key1217");

  // op 17431: delete
  db->Delete(write_options, "key4139");

  // op 17432: delete
  db->Delete(write_options, "key2326");

  // op 17433: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1925", "key10505", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1925, key10505) size=" << scan_result.size() << std::endl;
  }

  // op 17434: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 17435: put
  db->Put(write_options, "key10907", "value7552");

  // op 17436: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1354", "key11013", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1354, key11013) size=" << scan_result.size() << std::endl;
  }

  // op 17437: get
  status = db->Get(read_options, "key8429", &value);
  if (status.ok()) {
    out << "GET key8429 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17438: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 17439: get
  status = db->Get(read_options, "key2029", &value);
  if (status.ok()) {
    out << "GET key2029 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17440: put
  db->Put(write_options, "key8844", "value30800");

  // op 17441: delete
  db->Delete(write_options, "key3933");

  // op 17442: delete_range
  status = db->DeleteRange(write_options, "key1699", "key8376");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 17443: put
  db->Put(write_options, "key5158", "value10800");

  // op 17444: put
  db->Put(write_options, "key4440", "value51595");

  // op 17445: put
  db->Put(write_options, "key8721", "value65487");

  // op 17446: put
  db->Put(write_options, "key4843", "value47125");

  // op 17447: put
  db->Put(write_options, "key0721", "value13660");

  // op 17448: put
  db->Put(write_options, "key7595", "value95562");

  // op 17449: get
  status = db->Get(read_options, "key3832", &value);
  if (status.ok()) {
    out << "GET key3832 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17450: put
  db->Put(write_options, "key3863", "value63870");

  // op 17451: get
  status = db->Get(read_options, "key1002", &value);
  if (status.ok()) {
    out << "GET key1002 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17452: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2066", "key10567", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2066, key10567) size=" << scan_result.size() << std::endl;
  }

  // op 17453: put
  db->Put(write_options, "key6840", "value90657");

  // op 17454: get
  status = db->Get(read_options, "key5083", &value);
  if (status.ok()) {
    out << "GET key5083 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17455: delete
  db->Delete(write_options, "key10297");

  // op 17456: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 17457: put
  db->Put(write_options, "key3016", "value11857");

  // op 17458: get
  status = db->Get(read_options, "key8065", &value);
  if (status.ok()) {
    out << "GET key8065 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17459: delete_range
  status = db->DeleteRange(write_options, "key3463", "key3792");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 17460: put
  db->Put(write_options, "key2201", "value84645");

  // op 17461: delete
  db->Delete(write_options, "key4517");

  // op 17462: delete_range
  status = db->DeleteRange(write_options, "key5137", "key9309");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 17463: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10286", "key10694", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10286, key10694) size=" << scan_result.size() << std::endl;
  }

  // op 17464: put
  db->Put(write_options, "key8824", "value36003");

  // op 17465: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0262", "key5851", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0262, key5851) size=" << scan_result.size() << std::endl;
  }

  // op 17466: get
  status = db->Get(read_options, "key9040", &value);
  if (status.ok()) {
    out << "GET key9040 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17467: get
  status = db->Get(read_options, "key10047", &value);
  if (status.ok()) {
    out << "GET key10047 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17468: put
  db->Put(write_options, "key2882", "value102006");

  // op 17469: get
  status = db->Get(read_options, "key11285", &value);
  if (status.ok()) {
    out << "GET key11285 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17470: put
  db->Put(write_options, "key11271", "value61504");

  // op 17471: get
  status = db->Get(read_options, "key8415", &value);
  if (status.ok()) {
    out << "GET key8415 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17472: delete_range
  status = db->DeleteRange(write_options, "key6819", "key7131");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 17473: get
  status = db->Get(read_options, "key2524", &value);
  if (status.ok()) {
    out << "GET key2524 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17474: put
  db->Put(write_options, "key3909", "value100777");

  // op 17475: put
  db->Put(write_options, "key11105", "value14330");

  // op 17476: put
  db->Put(write_options, "key11358", "value61892");

  // op 17477: delete_range
  status = db->DeleteRange(write_options, "key2079", "key6768");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 17478: put
  db->Put(write_options, "key2450", "value50449");

  // op 17479: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 17480: delete
  db->Delete(write_options, "key5900");

  // op 17481: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6091", "key11225", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6091, key11225) size=" << scan_result.size() << std::endl;
  }

  // op 17482: delete_range
  status = db->DeleteRange(write_options, "key7819", "key10518");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 17483: delete
  db->Delete(write_options, "key1439");

  // op 17484: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4218", "key7176", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4218, key7176) size=" << scan_result.size() << std::endl;
  }

  // op 17485: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 17486: put
  db->Put(write_options, "key7338", "value49116");

  // op 17487: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5677", "key8447", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5677, key8447) size=" << scan_result.size() << std::endl;
  }

  // op 17488: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3093", "key5396", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3093, key5396) size=" << scan_result.size() << std::endl;
  }

  // op 17489: get
  status = db->Get(read_options, "key8630", &value);
  if (status.ok()) {
    out << "GET key8630 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17490: delete
  db->Delete(write_options, "key11518");

  // op 17491: delete_range
  status = db->DeleteRange(write_options, "key5609", "key6150");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 17492: get
  status = db->Get(read_options, "key6040", &value);
  if (status.ok()) {
    out << "GET key6040 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17493: get
  status = db->Get(read_options, "key9245", &value);
  if (status.ok()) {
    out << "GET key9245 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17494: put
  db->Put(write_options, "key1199", "value13892");

  // op 17495: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0613", "key1922", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0613, key1922) size=" << scan_result.size() << std::endl;
  }

  // op 17496: put
  db->Put(write_options, "key4451", "value102441");

  // op 17497: put
  db->Put(write_options, "key2715", "value75520");

  // op 17498: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0886", "key5228", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0886, key5228) size=" << scan_result.size() << std::endl;
  }

  // op 17499: delete
  db->Delete(write_options, "key4721");

  // op 17500: get
  status = db->Get(read_options, "key8493", &value);
  if (status.ok()) {
    out << "GET key8493 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17501: delete_range
  status = db->DeleteRange(write_options, "key3770", "key9153");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 17502: get
  status = db->Get(read_options, "key4764", &value);
  if (status.ok()) {
    out << "GET key4764 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17503: put
  db->Put(write_options, "key9777", "value11065");

  // op 17504: get
  status = db->Get(read_options, "key2734", &value);
  if (status.ok()) {
    out << "GET key2734 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17505: delete_range
  status = db->DeleteRange(write_options, "key5546", "key11158");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 17506: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7545", "key10073", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7545, key10073) size=" << scan_result.size() << std::endl;
  }

  // op 17507: put
  db->Put(write_options, "key9620", "value7854");

  // op 17508: get
  status = db->Get(read_options, "key9255", &value);
  if (status.ok()) {
    out << "GET key9255 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17509: put
  db->Put(write_options, "key3195", "value75131");

  // op 17510: put
  db->Put(write_options, "key5240", "value34332");

  // op 17511: put
  db->Put(write_options, "key7094", "value41346");

  // op 17512: put
  db->Put(write_options, "key1507", "value84557");

  // op 17513: put
  db->Put(write_options, "key9024", "value13067");

  // op 17514: put
  db->Put(write_options, "key2148", "value117626");

  // op 17515: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4402", "key9852", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4402, key9852) size=" << scan_result.size() << std::endl;
  }

  // op 17516: get
  status = db->Get(read_options, "key4096", &value);
  if (status.ok()) {
    out << "GET key4096 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17517: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 17518: put
  db->Put(write_options, "key9804", "value23798");

  // op 17519: delete_range
  status = db->DeleteRange(write_options, "key2653", "key11667");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 17520: get
  status = db->Get(read_options, "key10842", &value);
  if (status.ok()) {
    out << "GET key10842 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17521: get
  status = db->Get(read_options, "key0640", &value);
  if (status.ok()) {
    out << "GET key0640 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17522: put
  db->Put(write_options, "key2534", "value98182");

  // op 17523: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 17524: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2436", "key4624", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2436, key4624) size=" << scan_result.size() << std::endl;
  }

  // op 17525: put
  db->Put(write_options, "key5435", "value76991");

  // op 17526: get
  status = db->Get(read_options, "key11648", &value);
  if (status.ok()) {
    out << "GET key11648 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17527: get
  status = db->Get(read_options, "key6010", &value);
  if (status.ok()) {
    out << "GET key6010 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17528: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6142", "key6630", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6142, key6630) size=" << scan_result.size() << std::endl;
  }

  // op 17529: delete_range
  status = db->DeleteRange(write_options, "key3906", "key10369");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 17530: put
  db->Put(write_options, "key11640", "value16751");

  // op 17531: put
  db->Put(write_options, "key2396", "value59830");

  // op 17532: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1519", "key11535", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1519, key11535) size=" << scan_result.size() << std::endl;
  }

  // op 17533: put
  db->Put(write_options, "key1766", "value9210");

  // op 17534: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1840", "key10133", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1840, key10133) size=" << scan_result.size() << std::endl;
  }

  // op 17535: delete_range
  status = db->DeleteRange(write_options, "key0555", "key2681");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 17536: delete_range
  status = db->DeleteRange(write_options, "key6732", "key11178");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 17537: put
  db->Put(write_options, "key0741", "value88713");

  // op 17538: put
  db->Put(write_options, "key1740", "value9988");

  // op 17539: get
  status = db->Get(read_options, "key6054", &value);
  if (status.ok()) {
    out << "GET key6054 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17540: get
  status = db->Get(read_options, "key10809", &value);
  if (status.ok()) {
    out << "GET key10809 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17541: delete_range
  status = db->DeleteRange(write_options, "key6100", "key7735");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 17542: put
  db->Put(write_options, "key3971", "value35194");

  // op 17543: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4195", "key10459", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4195, key10459) size=" << scan_result.size() << std::endl;
  }

  // op 17544: delete_range
  status = db->DeleteRange(write_options, "key0194", "key4549");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 17545: put
  db->Put(write_options, "key5236", "value17314");

  // op 17546: get
  status = db->Get(read_options, "key0745", &value);
  if (status.ok()) {
    out << "GET key0745 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17547: put
  db->Put(write_options, "key1947", "value23192");

  // op 17548: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0297", "key10056", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0297, key10056) size=" << scan_result.size() << std::endl;
  }

  // op 17549: delete_range
  status = db->DeleteRange(write_options, "key6480", "key7439");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 17550: put
  db->Put(write_options, "key3359", "value25084");

  // op 17551: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 17552: put
  db->Put(write_options, "key10786", "value44261");

  // op 17553: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 17554: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2829", "key8927", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2829, key8927) size=" << scan_result.size() << std::endl;
  }

  // op 17555: put
  db->Put(write_options, "key9736", "value114076");

  // op 17556: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 17557: put
  db->Put(write_options, "key9939", "value51129");

  // op 17558: delete
  db->Delete(write_options, "key8708");

  // op 17559: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 17560: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5755", "key11467", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5755, key11467) size=" << scan_result.size() << std::endl;
  }

  // op 17561: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1472", "key4792", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1472, key4792) size=" << scan_result.size() << std::endl;
  }

  // op 17562: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 17563: get
  status = db->Get(read_options, "key2531", &value);
  if (status.ok()) {
    out << "GET key2531 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17564: get
  status = db->Get(read_options, "key0512", &value);
  if (status.ok()) {
    out << "GET key0512 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17565: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1281", "key3688", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1281, key3688) size=" << scan_result.size() << std::endl;
  }

  // op 17566: get
  status = db->Get(read_options, "key3346", &value);
  if (status.ok()) {
    out << "GET key3346 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17567: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1111", "key8766", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1111, key8766) size=" << scan_result.size() << std::endl;
  }

  // op 17568: delete
  db->Delete(write_options, "key11261");

  // op 17569: delete_range
  status = db->DeleteRange(write_options, "key5046", "key7218");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 17570: delete_range
  status = db->DeleteRange(write_options, "key3759", "key7400");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 17571: get
  status = db->Get(read_options, "key8542", &value);
  if (status.ok()) {
    out << "GET key8542 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17572: put
  db->Put(write_options, "key5133", "value92242");

  // op 17573: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3952", "key5253", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3952, key5253) size=" << scan_result.size() << std::endl;
  }

  // op 17574: put
  db->Put(write_options, "key2255", "value48857");

  // op 17575: put
  db->Put(write_options, "key3449", "value8721");

  // op 17576: put
  db->Put(write_options, "key0607", "value100864");

  // op 17577: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 17578: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0902", "key10811", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0902, key10811) size=" << scan_result.size() << std::endl;
  }

  // op 17579: put
  db->Put(write_options, "key6267", "value100764");

  // op 17580: get
  status = db->Get(read_options, "key6461", &value);
  if (status.ok()) {
    out << "GET key6461 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17581: get
  status = db->Get(read_options, "key5316", &value);
  if (status.ok()) {
    out << "GET key5316 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17582: put
  db->Put(write_options, "key10153", "value67423");

  // op 17583: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0806", "key6014", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0806, key6014) size=" << scan_result.size() << std::endl;
  }

  // op 17584: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9476", "key10799", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9476, key10799) size=" << scan_result.size() << std::endl;
  }

  // op 17585: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3477", "key9820", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3477, key9820) size=" << scan_result.size() << std::endl;
  }

  // op 17586: delete_range
  status = db->DeleteRange(write_options, "key8913", "key11507");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 17587: delete
  db->Delete(write_options, "key4500");

  // op 17588: put
  db->Put(write_options, "key9095", "value107546");

  // op 17589: put
  db->Put(write_options, "key3621", "value91235");

  // op 17590: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5490", "key5688", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5490, key5688) size=" << scan_result.size() << std::endl;
  }

  // op 17591: get
  status = db->Get(read_options, "key1068", &value);
  if (status.ok()) {
    out << "GET key1068 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17592: put
  db->Put(write_options, "key3953", "value114356");

  // op 17593: get
  status = db->Get(read_options, "key7610", &value);
  if (status.ok()) {
    out << "GET key7610 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17594: put
  db->Put(write_options, "key0052", "value90887");

  // op 17595: put
  db->Put(write_options, "key3603", "value32197");

  // op 17596: get
  status = db->Get(read_options, "key3437", &value);
  if (status.ok()) {
    out << "GET key3437 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17597: put
  db->Put(write_options, "key4744", "value67525");

  // op 17598: get
  status = db->Get(read_options, "key6411", &value);
  if (status.ok()) {
    out << "GET key6411 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17599: put
  db->Put(write_options, "key1845", "value83603");

  // op 17600: put
  db->Put(write_options, "key8108", "value64752");

  // op 17601: delete
  db->Delete(write_options, "key9624");

  // op 17602: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0538", "key7090", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0538, key7090) size=" << scan_result.size() << std::endl;
  }

  // op 17603: put
  db->Put(write_options, "key10768", "value79382");

  // op 17604: get
  status = db->Get(read_options, "key3457", &value);
  if (status.ok()) {
    out << "GET key3457 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17605: put
  db->Put(write_options, "key2458", "value60493");

  // op 17606: put
  db->Put(write_options, "key10336", "value15390");

  // op 17607: put
  db->Put(write_options, "key8837", "value98382");

  // op 17608: get
  status = db->Get(read_options, "key5402", &value);
  if (status.ok()) {
    out << "GET key5402 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17609: get
  status = db->Get(read_options, "key0272", &value);
  if (status.ok()) {
    out << "GET key0272 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17610: put
  db->Put(write_options, "key3084", "value117047");

  // op 17611: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6223", "key10960", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6223, key10960) size=" << scan_result.size() << std::endl;
  }

  // op 17612: put
  db->Put(write_options, "key9515", "value96801");

  // op 17613: put
  db->Put(write_options, "key6921", "value74178");

  // op 17614: put
  db->Put(write_options, "key7226", "value89465");

  // op 17615: delete_range
  status = db->DeleteRange(write_options, "key3643", "key11588");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 17616: get
  status = db->Get(read_options, "key10679", &value);
  if (status.ok()) {
    out << "GET key10679 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17617: put
  db->Put(write_options, "key1089", "value19084");

  // op 17618: put
  db->Put(write_options, "key8230", "value79282");

  // op 17619: get
  status = db->Get(read_options, "key7388", &value);
  if (status.ok()) {
    out << "GET key7388 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17620: get
  status = db->Get(read_options, "key8412", &value);
  if (status.ok()) {
    out << "GET key8412 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17621: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4668", "key7623", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4668, key7623) size=" << scan_result.size() << std::endl;
  }

  // op 17622: get
  status = db->Get(read_options, "key9692", &value);
  if (status.ok()) {
    out << "GET key9692 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17623: delete_range
  status = db->DeleteRange(write_options, "key0499", "key8311");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 17624: put
  db->Put(write_options, "key9416", "value112600");

  // op 17625: put
  db->Put(write_options, "key8263", "value60037");

  // op 17626: put
  db->Put(write_options, "key9048", "value100365");

  // op 17627: put
  db->Put(write_options, "key7211", "value60330");

  // op 17628: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9272", "key10255", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9272, key10255) size=" << scan_result.size() << std::endl;
  }

  // op 17629: put
  db->Put(write_options, "key0316", "value87051");

  // op 17630: put
  db->Put(write_options, "key1767", "value44721");

  // op 17631: put
  db->Put(write_options, "key2929", "value24901");

  // op 17632: delete_range
  status = db->DeleteRange(write_options, "key0195", "key9614");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 17633: get
  status = db->Get(read_options, "key2965", &value);
  if (status.ok()) {
    out << "GET key2965 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17634: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 17635: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 17636: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6379", "key8697", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6379, key8697) size=" << scan_result.size() << std::endl;
  }

  // op 17637: delete_range
  status = db->DeleteRange(write_options, "key7331", "key8955");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 17638: get
  status = db->Get(read_options, "key2442", &value);
  if (status.ok()) {
    out << "GET key2442 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17639: get
  status = db->Get(read_options, "key7597", &value);
  if (status.ok()) {
    out << "GET key7597 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17640: put
  db->Put(write_options, "key2447", "value9498");

  // op 17641: get
  status = db->Get(read_options, "key2107", &value);
  if (status.ok()) {
    out << "GET key2107 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17642: delete_range
  status = db->DeleteRange(write_options, "key1220", "key8002");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 17643: delete_range
  status = db->DeleteRange(write_options, "key4020", "key5010");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 17644: get
  status = db->Get(read_options, "key2002", &value);
  if (status.ok()) {
    out << "GET key2002 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17645: put
  db->Put(write_options, "key0341", "value118269");

  // op 17646: put
  db->Put(write_options, "key10704", "value105097");

  // op 17647: put
  db->Put(write_options, "key7815", "value89049");

  // op 17648: put
  db->Put(write_options, "key9899", "value89306");

  // op 17649: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1498", "key2403", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1498, key2403) size=" << scan_result.size() << std::endl;
  }

  // op 17650: delete
  db->Delete(write_options, "key7183");

  // op 17651: get
  status = db->Get(read_options, "key11140", &value);
  if (status.ok()) {
    out << "GET key11140 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17652: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5165", "key6320", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5165, key6320) size=" << scan_result.size() << std::endl;
  }

  // op 17653: put
  db->Put(write_options, "key5756", "value3611");

  // op 17654: put
  db->Put(write_options, "key10087", "value85197");

  // op 17655: put
  db->Put(write_options, "key9772", "value40922");

  // op 17656: put
  db->Put(write_options, "key3416", "value49845");

  // op 17657: put
  db->Put(write_options, "key2390", "value95534");

  // op 17658: delete
  db->Delete(write_options, "key3149");

  // op 17659: put
  db->Put(write_options, "key10159", "value30475");

  // op 17660: delete_range
  status = db->DeleteRange(write_options, "key1513", "key2069");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 17661: get
  status = db->Get(read_options, "key2352", &value);
  if (status.ok()) {
    out << "GET key2352 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17662: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0266", "key1256", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0266, key1256) size=" << scan_result.size() << std::endl;
  }

  // op 17663: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2666", "key4980", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2666, key4980) size=" << scan_result.size() << std::endl;
  }

  // op 17664: put
  db->Put(write_options, "key3015", "value113415");

  // op 17665: get
  status = db->Get(read_options, "key6212", &value);
  if (status.ok()) {
    out << "GET key6212 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17666: put
  db->Put(write_options, "key4785", "value7541");

  // op 17667: put
  db->Put(write_options, "key8487", "value114309");

  // op 17668: put
  db->Put(write_options, "key1838", "value39510");

  // op 17669: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 17670: get
  status = db->Get(read_options, "key5862", &value);
  if (status.ok()) {
    out << "GET key5862 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17671: put
  db->Put(write_options, "key1506", "value73216");

  // op 17672: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2142", "key11335", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2142, key11335) size=" << scan_result.size() << std::endl;
  }

  // op 17673: put
  db->Put(write_options, "key10186", "value70380");

  // op 17674: get
  status = db->Get(read_options, "key0371", &value);
  if (status.ok()) {
    out << "GET key0371 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17675: put
  db->Put(write_options, "key2608", "value100347");

  // op 17676: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2672", "key8993", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2672, key8993) size=" << scan_result.size() << std::endl;
  }

  // op 17677: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 17678: delete
  db->Delete(write_options, "key5378");

  // op 17679: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4959", "key6934", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4959, key6934) size=" << scan_result.size() << std::endl;
  }

  // op 17680: put
  db->Put(write_options, "key2300", "value30833");

  // op 17681: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5655", "key9229", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5655, key9229) size=" << scan_result.size() << std::endl;
  }

  // op 17682: put
  db->Put(write_options, "key6841", "value15794");

  // op 17683: delete
  db->Delete(write_options, "key11628");

  // op 17684: get
  status = db->Get(read_options, "key1171", &value);
  if (status.ok()) {
    out << "GET key1171 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17685: put
  db->Put(write_options, "key6728", "value42407");

  // op 17686: put
  db->Put(write_options, "key7357", "value76847");

  // op 17687: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5832", "key9261", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5832, key9261) size=" << scan_result.size() << std::endl;
  }

  // op 17688: put
  db->Put(write_options, "key9358", "value4408");

  // op 17689: put
  db->Put(write_options, "key7106", "value8682");

  // op 17690: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3382", "key8876", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3382, key8876) size=" << scan_result.size() << std::endl;
  }

  // op 17691: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2990", "key6321", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2990, key6321) size=" << scan_result.size() << std::endl;
  }

  // op 17692: delete
  db->Delete(write_options, "key6058");

  // op 17693: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4017", "key9905", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4017, key9905) size=" << scan_result.size() << std::endl;
  }

  // op 17694: get
  status = db->Get(read_options, "key4101", &value);
  if (status.ok()) {
    out << "GET key4101 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17695: put
  db->Put(write_options, "key6666", "value89276");

  // op 17696: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0112", "key6446", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0112, key6446) size=" << scan_result.size() << std::endl;
  }

  // op 17697: delete
  db->Delete(write_options, "key3870");

  // op 17698: put
  db->Put(write_options, "key5140", "value117721");

  // op 17699: put
  db->Put(write_options, "key7710", "value66884");

  // op 17700: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1825", "key10069", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1825, key10069) size=" << scan_result.size() << std::endl;
  }

  // op 17701: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0634", "key6777", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0634, key6777) size=" << scan_result.size() << std::endl;
  }

  // op 17702: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0044", "key2459", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0044, key2459) size=" << scan_result.size() << std::endl;
  }

  // op 17703: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4609", "key6556", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4609, key6556) size=" << scan_result.size() << std::endl;
  }

  // op 17704: put
  db->Put(write_options, "key4898", "value46159");

  // op 17705: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4377", "key9307", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4377, key9307) size=" << scan_result.size() << std::endl;
  }

  // op 17706: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 17707: delete
  db->Delete(write_options, "key8403");

  // op 17708: put
  db->Put(write_options, "key8778", "value64163");

  // op 17709: put
  db->Put(write_options, "key1837", "value12918");

  // op 17710: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9127", "key9463", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9127, key9463) size=" << scan_result.size() << std::endl;
  }

  // op 17711: put
  db->Put(write_options, "key0808", "value91614");

  // op 17712: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0189", "key8962", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0189, key8962) size=" << scan_result.size() << std::endl;
  }

  // op 17713: get
  status = db->Get(read_options, "key4738", &value);
  if (status.ok()) {
    out << "GET key4738 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17714: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3214", "key6301", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3214, key6301) size=" << scan_result.size() << std::endl;
  }

  // op 17715: put
  db->Put(write_options, "key4643", "value110121");

  // op 17716: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 17717: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3627", "key6426", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3627, key6426) size=" << scan_result.size() << std::endl;
  }

  // op 17718: get
  status = db->Get(read_options, "key5494", &value);
  if (status.ok()) {
    out << "GET key5494 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17719: put
  db->Put(write_options, "key9377", "value66164");

  // op 17720: put
  db->Put(write_options, "key0916", "value49157");

  // op 17721: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0083", "key0699", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0083, key0699) size=" << scan_result.size() << std::endl;
  }

  // op 17722: put
  db->Put(write_options, "key9893", "value28445");

  // op 17723: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8836", "key11447", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8836, key11447) size=" << scan_result.size() << std::endl;
  }

  // op 17724: get
  status = db->Get(read_options, "key4674", &value);
  if (status.ok()) {
    out << "GET key4674 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17725: put
  db->Put(write_options, "key6821", "value108733");

  // op 17726: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3159", "key10470", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3159, key10470) size=" << scan_result.size() << std::endl;
  }

  // op 17727: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1193", "key4378", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1193, key4378) size=" << scan_result.size() << std::endl;
  }

  // op 17728: get
  status = db->Get(read_options, "key0414", &value);
  if (status.ok()) {
    out << "GET key0414 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17729: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1091", "key3605", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1091, key3605) size=" << scan_result.size() << std::endl;
  }

  // op 17730: delete
  db->Delete(write_options, "key8269");

  // op 17731: put
  db->Put(write_options, "key6016", "value41937");

  // op 17732: delete
  db->Delete(write_options, "key4336");

  // op 17733: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1011", "key1795", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1011, key1795) size=" << scan_result.size() << std::endl;
  }

  // op 17734: put
  db->Put(write_options, "key1313", "value12934");

  // op 17735: get
  status = db->Get(read_options, "key9751", &value);
  if (status.ok()) {
    out << "GET key9751 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17736: put
  db->Put(write_options, "key10654", "value51752");

  // op 17737: delete
  db->Delete(write_options, "key9127");

  // op 17738: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 17739: put
  db->Put(write_options, "key10557", "value76962");

  // op 17740: put
  db->Put(write_options, "key0076", "value101282");

  // op 17741: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1861", "key9948", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1861, key9948) size=" << scan_result.size() << std::endl;
  }

  // op 17742: put
  db->Put(write_options, "key3480", "value40917");

  // op 17743: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8063", "key10089", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8063, key10089) size=" << scan_result.size() << std::endl;
  }

  // op 17744: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6099", "key9328", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6099, key9328) size=" << scan_result.size() << std::endl;
  }

  // op 17745: get
  status = db->Get(read_options, "key3123", &value);
  if (status.ok()) {
    out << "GET key3123 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17746: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2412", "key2496", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2412, key2496) size=" << scan_result.size() << std::endl;
  }

  // op 17747: delete
  db->Delete(write_options, "key8692");

  // op 17748: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 17749: get
  status = db->Get(read_options, "key9574", &value);
  if (status.ok()) {
    out << "GET key9574 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17750: get
  status = db->Get(read_options, "key2910", &value);
  if (status.ok()) {
    out << "GET key2910 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17751: put
  db->Put(write_options, "key4197", "value81260");

  // op 17752: delete_range
  status = db->DeleteRange(write_options, "key7629", "key9517");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 17753: delete_range
  status = db->DeleteRange(write_options, "key2692", "key8962");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 17754: delete
  db->Delete(write_options, "key7312");

  // op 17755: put
  db->Put(write_options, "key6837", "value52191");

  // op 17756: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0676", "key4777", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0676, key4777) size=" << scan_result.size() << std::endl;
  }

  // op 17757: put
  db->Put(write_options, "key1856", "value24900");

  // op 17758: put
  db->Put(write_options, "key3067", "value45045");

  // op 17759: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0374", "key8052", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0374, key8052) size=" << scan_result.size() << std::endl;
  }

  // op 17760: put
  db->Put(write_options, "key4255", "value0448");

  // op 17761: delete
  db->Delete(write_options, "key9385");

  // op 17762: get
  status = db->Get(read_options, "key10073", &value);
  if (status.ok()) {
    out << "GET key10073 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17763: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1372", "key9557", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1372, key9557) size=" << scan_result.size() << std::endl;
  }

  // op 17764: delete_range
  status = db->DeleteRange(write_options, "key5122", "key7548");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 17765: put
  db->Put(write_options, "key6436", "value115382");

  // op 17766: put
  db->Put(write_options, "key9387", "value113731");

  // op 17767: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5718", "key10094", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5718, key10094) size=" << scan_result.size() << std::endl;
  }

  // op 17768: get
  status = db->Get(read_options, "key4877", &value);
  if (status.ok()) {
    out << "GET key4877 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17769: put
  db->Put(write_options, "key9630", "value39088");

  // op 17770: put
  db->Put(write_options, "key1746", "value3461");

  // op 17771: delete
  db->Delete(write_options, "key1744");

  // op 17772: get
  status = db->Get(read_options, "key8996", &value);
  if (status.ok()) {
    out << "GET key8996 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17773: delete_range
  status = db->DeleteRange(write_options, "key5533", "key9374");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 17774: get
  status = db->Get(read_options, "key8871", &value);
  if (status.ok()) {
    out << "GET key8871 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17775: delete
  db->Delete(write_options, "key2154");

  // op 17776: put
  db->Put(write_options, "key1649", "value53487");

  // op 17777: put
  db->Put(write_options, "key2281", "value56588");

  // op 17778: put
  db->Put(write_options, "key4487", "value26922");

  // op 17779: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1819", "key7509", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1819, key7509) size=" << scan_result.size() << std::endl;
  }

  // op 17780: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2025", "key2278", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2025, key2278) size=" << scan_result.size() << std::endl;
  }

  // op 17781: get
  status = db->Get(read_options, "key5506", &value);
  if (status.ok()) {
    out << "GET key5506 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17782: put
  db->Put(write_options, "key10455", "value45595");

  // op 17783: scan
  scan_result.clear();
  status = db->Scan(read_options, "key11339", "key11437", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key11339, key11437) size=" << scan_result.size() << std::endl;
  }

  // op 17784: delete_range
  status = db->DeleteRange(write_options, "key6822", "key9588");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 17785: delete_range
  status = db->DeleteRange(write_options, "key8344", "key10891");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 17786: get
  status = db->Get(read_options, "key7909", &value);
  if (status.ok()) {
    out << "GET key7909 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17787: put
  db->Put(write_options, "key5283", "value116616");

  // op 17788: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0233", "key9235", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0233, key9235) size=" << scan_result.size() << std::endl;
  }

  // op 17789: put
  db->Put(write_options, "key8517", "value28743");

  // op 17790: put
  db->Put(write_options, "key5344", "value40196");

  // op 17791: put
  db->Put(write_options, "key3545", "value110997");

  // op 17792: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4114", "key7209", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4114, key7209) size=" << scan_result.size() << std::endl;
  }

  // op 17793: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5554", "key9781", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5554, key9781) size=" << scan_result.size() << std::endl;
  }

  // op 17794: put
  db->Put(write_options, "key6774", "value3305");

  // op 17795: delete
  db->Delete(write_options, "key1773");

  // op 17796: put
  db->Put(write_options, "key9169", "value28849");

  // op 17797: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8060", "key8646", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8060, key8646) size=" << scan_result.size() << std::endl;
  }

  // op 17798: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4664", "key5263", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4664, key5263) size=" << scan_result.size() << std::endl;
  }

  // op 17799: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0443", "key6251", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0443, key6251) size=" << scan_result.size() << std::endl;
  }

  // op 17800: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6594", "key7060", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6594, key7060) size=" << scan_result.size() << std::endl;
  }

  // op 17801: get
  status = db->Get(read_options, "key4706", &value);
  if (status.ok()) {
    out << "GET key4706 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17802: get
  status = db->Get(read_options, "key0475", &value);
  if (status.ok()) {
    out << "GET key0475 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17803: delete
  db->Delete(write_options, "key1228");

  // op 17804: delete_range
  status = db->DeleteRange(write_options, "key8978", "key10414");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 17805: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 17806: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 17807: put
  db->Put(write_options, "key6656", "value38200");

  // op 17808: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 17809: get
  status = db->Get(read_options, "key4722", &value);
  if (status.ok()) {
    out << "GET key4722 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17810: put
  db->Put(write_options, "key7103", "value117852");

  // op 17811: delete
  db->Delete(write_options, "key4974");

  // op 17812: delete
  db->Delete(write_options, "key1764");

  // op 17813: put
  db->Put(write_options, "key2435", "value97291");

  // op 17814: put
  db->Put(write_options, "key2627", "value8907");

  // op 17815: put
  db->Put(write_options, "key3383", "value59205");

  // op 17816: get
  status = db->Get(read_options, "key1088", &value);
  if (status.ok()) {
    out << "GET key1088 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17817: put
  db->Put(write_options, "key1258", "value72538");

  // op 17818: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0941", "key2607", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0941, key2607) size=" << scan_result.size() << std::endl;
  }

  // op 17819: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1875", "key5826", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1875, key5826) size=" << scan_result.size() << std::endl;
  }

  // op 17820: get
  status = db->Get(read_options, "key7418", &value);
  if (status.ok()) {
    out << "GET key7418 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17821: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9773", "key11524", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9773, key11524) size=" << scan_result.size() << std::endl;
  }

  // op 17822: get
  status = db->Get(read_options, "key9254", &value);
  if (status.ok()) {
    out << "GET key9254 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17823: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3202", "key9094", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3202, key9094) size=" << scan_result.size() << std::endl;
  }

  // op 17824: put
  db->Put(write_options, "key0550", "value60940");

  // op 17825: delete_range
  status = db->DeleteRange(write_options, "key2703", "key2926");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 17826: put
  db->Put(write_options, "key2995", "value90779");

  // op 17827: put
  db->Put(write_options, "key8797", "value98460");

  // op 17828: put
  db->Put(write_options, "key8954", "value53403");

  // op 17829: delete
  db->Delete(write_options, "key3194");

  // op 17830: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 17831: delete
  db->Delete(write_options, "key2614");

  // op 17832: get
  status = db->Get(read_options, "key1738", &value);
  if (status.ok()) {
    out << "GET key1738 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17833: get
  status = db->Get(read_options, "key6977", &value);
  if (status.ok()) {
    out << "GET key6977 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17834: get
  status = db->Get(read_options, "key1924", &value);
  if (status.ok()) {
    out << "GET key1924 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17835: delete
  db->Delete(write_options, "key9074");

  // op 17836: put
  db->Put(write_options, "key1819", "value87905");

  // op 17837: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1357", "key3830", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1357, key3830) size=" << scan_result.size() << std::endl;
  }

  // op 17838: put
  db->Put(write_options, "key6967", "value71497");

  // op 17839: get
  status = db->Get(read_options, "key11275", &value);
  if (status.ok()) {
    out << "GET key11275 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17840: put
  db->Put(write_options, "key9410", "value90025");

  // op 17841: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7015", "key8725", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7015, key8725) size=" << scan_result.size() << std::endl;
  }

  // op 17842: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5054", "key10053", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5054, key10053) size=" << scan_result.size() << std::endl;
  }

  // op 17843: put
  db->Put(write_options, "key4021", "value35128");

  // op 17844: put
  db->Put(write_options, "key9224", "value57452");

  // op 17845: delete_range
  status = db->DeleteRange(write_options, "key4106", "key10399");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 17846: get
  status = db->Get(read_options, "key5976", &value);
  if (status.ok()) {
    out << "GET key5976 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17847: put
  db->Put(write_options, "key3503", "value8762");

  // op 17848: put
  db->Put(write_options, "key3391", "value13499");

  // op 17849: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2531", "key5685", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2531, key5685) size=" << scan_result.size() << std::endl;
  }

  // op 17850: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1231", "key2540", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1231, key2540) size=" << scan_result.size() << std::endl;
  }

  // op 17851: delete
  db->Delete(write_options, "key0441");

  // op 17852: get
  status = db->Get(read_options, "key1175", &value);
  if (status.ok()) {
    out << "GET key1175 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17853: delete
  db->Delete(write_options, "key7313");

  // op 17854: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 17855: delete_range
  status = db->DeleteRange(write_options, "key8680", "key9754");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 17856: get
  status = db->Get(read_options, "key7517", &value);
  if (status.ok()) {
    out << "GET key7517 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17857: delete
  db->Delete(write_options, "key9466");

  // op 17858: put
  db->Put(write_options, "key11064", "value3627");

  // op 17859: get
  status = db->Get(read_options, "key7144", &value);
  if (status.ok()) {
    out << "GET key7144 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17860: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3836", "key4784", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3836, key4784) size=" << scan_result.size() << std::endl;
  }

  // op 17861: put
  db->Put(write_options, "key6009", "value27943");

  // op 17862: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 17863: get
  status = db->Get(read_options, "key7507", &value);
  if (status.ok()) {
    out << "GET key7507 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17864: get
  status = db->Get(read_options, "key3062", &value);
  if (status.ok()) {
    out << "GET key3062 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17865: put
  db->Put(write_options, "key4343", "value12902");

  // op 17866: get
  status = db->Get(read_options, "key0016", &value);
  if (status.ok()) {
    out << "GET key0016 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17867: put
  db->Put(write_options, "key3728", "value35225");

  // op 17868: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2783", "key10621", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2783, key10621) size=" << scan_result.size() << std::endl;
  }

  // op 17869: get
  status = db->Get(read_options, "key4799", &value);
  if (status.ok()) {
    out << "GET key4799 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17870: put
  db->Put(write_options, "key9855", "value42987");

  // op 17871: delete_range
  status = db->DeleteRange(write_options, "key9315", "key10876");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 17872: get
  status = db->Get(read_options, "key3094", &value);
  if (status.ok()) {
    out << "GET key3094 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17873: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1716", "key5548", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1716, key5548) size=" << scan_result.size() << std::endl;
  }

  // op 17874: put
  db->Put(write_options, "key3881", "value102782");

  // op 17875: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0293", "key8754", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0293, key8754) size=" << scan_result.size() << std::endl;
  }

  // op 17876: get
  status = db->Get(read_options, "key7281", &value);
  if (status.ok()) {
    out << "GET key7281 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17877: get
  status = db->Get(read_options, "key2204", &value);
  if (status.ok()) {
    out << "GET key2204 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17878: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5452", "key11291", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5452, key11291) size=" << scan_result.size() << std::endl;
  }

  // op 17879: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 17880: delete_range
  status = db->DeleteRange(write_options, "key0918", "key1076");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 17881: put
  db->Put(write_options, "key7532", "value2845");

  // op 17882: get
  status = db->Get(read_options, "key3431", &value);
  if (status.ok()) {
    out << "GET key3431 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17883: delete
  db->Delete(write_options, "key7632");

  // op 17884: delete
  db->Delete(write_options, "key2398");

  // op 17885: put
  db->Put(write_options, "key8584", "value82376");

  // op 17886: delete
  db->Delete(write_options, "key1068");

  // op 17887: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6210", "key8733", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6210, key8733) size=" << scan_result.size() << std::endl;
  }

  // op 17888: delete
  db->Delete(write_options, "key2694");

  // op 17889: delete
  db->Delete(write_options, "key9571");

  // op 17890: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3842", "key10635", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3842, key10635) size=" << scan_result.size() << std::endl;
  }

  // op 17891: delete_range
  status = db->DeleteRange(write_options, "key1371", "key3575");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 17892: put
  db->Put(write_options, "key8089", "value38433");

  // op 17893: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6892", "key8575", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6892, key8575) size=" << scan_result.size() << std::endl;
  }

  // op 17894: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0003", "key1880", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0003, key1880) size=" << scan_result.size() << std::endl;
  }

  // op 17895: put
  db->Put(write_options, "key1770", "value111908");

  // op 17896: delete_range
  status = db->DeleteRange(write_options, "key1116", "key9144");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 17897: delete
  db->Delete(write_options, "key4668");

  // op 17898: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 17899: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 17900: get
  status = db->Get(read_options, "key3781", &value);
  if (status.ok()) {
    out << "GET key3781 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17901: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 17902: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 17903: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 17904: get
  status = db->Get(read_options, "key1626", &value);
  if (status.ok()) {
    out << "GET key1626 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17905: delete
  db->Delete(write_options, "key7089");

  // op 17906: delete_range
  status = db->DeleteRange(write_options, "key5329", "key10955");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 17907: get
  status = db->Get(read_options, "key9580", &value);
  if (status.ok()) {
    out << "GET key9580 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17908: put
  db->Put(write_options, "key10162", "value117190");

  // op 17909: put
  db->Put(write_options, "key2969", "value22073");

  // op 17910: put
  db->Put(write_options, "key10095", "value85982");

  // op 17911: put
  db->Put(write_options, "key10845", "value63020");

  // op 17912: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 17913: get
  status = db->Get(read_options, "key2273", &value);
  if (status.ok()) {
    out << "GET key2273 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17914: delete
  db->Delete(write_options, "key11624");

  // op 17915: delete_range
  status = db->DeleteRange(write_options, "key2867", "key7390");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 17916: get
  status = db->Get(read_options, "key8598", &value);
  if (status.ok()) {
    out << "GET key8598 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17917: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3874", "key8174", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3874, key8174) size=" << scan_result.size() << std::endl;
  }

  // op 17918: get
  status = db->Get(read_options, "key9004", &value);
  if (status.ok()) {
    out << "GET key9004 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17919: put
  db->Put(write_options, "key8874", "value81021");

  // op 17920: get
  status = db->Get(read_options, "key0274", &value);
  if (status.ok()) {
    out << "GET key0274 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17921: put
  db->Put(write_options, "key3540", "value27742");

  // op 17922: delete
  db->Delete(write_options, "key7592");

  // op 17923: put
  db->Put(write_options, "key7775", "value42049");

  // op 17924: put
  db->Put(write_options, "key0600", "value55867");

  // op 17925: get
  status = db->Get(read_options, "key1738", &value);
  if (status.ok()) {
    out << "GET key1738 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17926: delete
  db->Delete(write_options, "key6062");

  // op 17927: delete
  db->Delete(write_options, "key2544");

  // op 17928: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1578", "key11562", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1578, key11562) size=" << scan_result.size() << std::endl;
  }

  // op 17929: put
  db->Put(write_options, "key6225", "value45148");

  // op 17930: put
  db->Put(write_options, "key9491", "value95061");

  // op 17931: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2700", "key7951", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2700, key7951) size=" << scan_result.size() << std::endl;
  }

  // op 17932: delete
  db->Delete(write_options, "key6713");

  // op 17933: delete_range
  status = db->DeleteRange(write_options, "key0577", "key5011");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 17934: get
  status = db->Get(read_options, "key0525", &value);
  if (status.ok()) {
    out << "GET key0525 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17935: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7483", "key7704", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7483, key7704) size=" << scan_result.size() << std::endl;
  }

  // op 17936: delete
  db->Delete(write_options, "key4672");

  // op 17937: get
  status = db->Get(read_options, "key7513", &value);
  if (status.ok()) {
    out << "GET key7513 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17938: delete_range
  status = db->DeleteRange(write_options, "key8469", "key10571");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 17939: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10570", "key10721", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10570, key10721) size=" << scan_result.size() << std::endl;
  }

  // op 17940: get
  status = db->Get(read_options, "key9022", &value);
  if (status.ok()) {
    out << "GET key9022 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17941: get
  status = db->Get(read_options, "key6723", &value);
  if (status.ok()) {
    out << "GET key6723 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17942: put
  db->Put(write_options, "key0386", "value99561");

  // op 17943: delete
  db->Delete(write_options, "key10833");

  // op 17944: get
  status = db->Get(read_options, "key10740", &value);
  if (status.ok()) {
    out << "GET key10740 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17945: delete_range
  status = db->DeleteRange(write_options, "key5476", "key8340");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 17946: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5275", "key8837", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5275, key8837) size=" << scan_result.size() << std::endl;
  }

  // op 17947: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2737", "key11605", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2737, key11605) size=" << scan_result.size() << std::endl;
  }

  // op 17948: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0580", "key1115", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0580, key1115) size=" << scan_result.size() << std::endl;
  }

  // op 17949: put
  db->Put(write_options, "key8494", "value45739");

  // op 17950: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 17951: put
  db->Put(write_options, "key7518", "value27890");

  // op 17952: delete_range
  status = db->DeleteRange(write_options, "key6905", "key9348");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 17953: put
  db->Put(write_options, "key8923", "value114429");

  // op 17954: put
  db->Put(write_options, "key9064", "value21853");

  // op 17955: put
  db->Put(write_options, "key0736", "value40162");

  // op 17956: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1837", "key8835", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1837, key8835) size=" << scan_result.size() << std::endl;
  }

  // op 17957: get
  status = db->Get(read_options, "key1705", &value);
  if (status.ok()) {
    out << "GET key1705 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17958: get
  status = db->Get(read_options, "key11284", &value);
  if (status.ok()) {
    out << "GET key11284 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17959: put
  db->Put(write_options, "key11511", "value74372");

  // op 17960: delete
  db->Delete(write_options, "key0456");

  // op 17961: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 17962: put
  db->Put(write_options, "key6886", "value35694");

  // op 17963: put
  db->Put(write_options, "key11286", "value87141");

  // op 17964: delete
  db->Delete(write_options, "key5557");

  // op 17965: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3075", "key4660", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3075, key4660) size=" << scan_result.size() << std::endl;
  }

  // op 17966: put
  db->Put(write_options, "key4977", "value22878");

  // op 17967: get
  status = db->Get(read_options, "key10041", &value);
  if (status.ok()) {
    out << "GET key10041 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17968: get
  status = db->Get(read_options, "key8901", &value);
  if (status.ok()) {
    out << "GET key8901 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17969: delete
  db->Delete(write_options, "key3168");

  // op 17970: get
  status = db->Get(read_options, "key11387", &value);
  if (status.ok()) {
    out << "GET key11387 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17971: put
  db->Put(write_options, "key11269", "value0869");

  // op 17972: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3542", "key11297", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3542, key11297) size=" << scan_result.size() << std::endl;
  }

  // op 17973: put
  db->Put(write_options, "key9044", "value36110");

  // op 17974: put
  db->Put(write_options, "key1922", "value54248");

  // op 17975: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5352", "key10433", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5352, key10433) size=" << scan_result.size() << std::endl;
  }

  // op 17976: put
  db->Put(write_options, "key4829", "value113104");

  // op 17977: get
  status = db->Get(read_options, "key4839", &value);
  if (status.ok()) {
    out << "GET key4839 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17978: delete_range
  status = db->DeleteRange(write_options, "key1781", "key9161");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 17979: put
  db->Put(write_options, "key9937", "value100888");

  // op 17980: put
  db->Put(write_options, "key11201", "value116925");

  // op 17981: put
  db->Put(write_options, "key9368", "value12696");

  // op 17982: put
  db->Put(write_options, "key9064", "value107775");

  // op 17983: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4564", "key10902", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4564, key10902) size=" << scan_result.size() << std::endl;
  }

  // op 17984: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 17985: delete
  db->Delete(write_options, "key5282");

  // op 17986: put
  db->Put(write_options, "key9038", "value53247");

  // op 17987: get
  status = db->Get(read_options, "key2591", &value);
  if (status.ok()) {
    out << "GET key2591 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17988: get
  status = db->Get(read_options, "key1405", &value);
  if (status.ok()) {
    out << "GET key1405 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17989: put
  db->Put(write_options, "key0188", "value104496");

  // op 17990: delete
  db->Delete(write_options, "key8689");

  // op 17991: put
  db->Put(write_options, "key2310", "value45454");

  // op 17992: get
  status = db->Get(read_options, "key0557", &value);
  if (status.ok()) {
    out << "GET key0557 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17993: delete_range
  status = db->DeleteRange(write_options, "key2368", "key2376");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 17994: put
  db->Put(write_options, "key9302", "value101282");

  // op 17995: get
  status = db->Get(read_options, "key0053", &value);
  if (status.ok()) {
    out << "GET key0053 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 17996: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5455", "key5503", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5455, key5503) size=" << scan_result.size() << std::endl;
  }

  // op 17997: put
  db->Put(write_options, "key9412", "value83529");

  // op 17998: put
  db->Put(write_options, "key3790", "value0189");

  // op 17999: get
  status = db->Get(read_options, "key9735", &value);
  if (status.ok()) {
    out << "GET key9735 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18000: delete
  db->Delete(write_options, "key11297");

  // op 18001: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0229", "key4494", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0229, key4494) size=" << scan_result.size() << std::endl;
  }

  // op 18002: get
  status = db->Get(read_options, "key3207", &value);
  if (status.ok()) {
    out << "GET key3207 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18003: get
  status = db->Get(read_options, "key9904", &value);
  if (status.ok()) {
    out << "GET key9904 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18004: put
  db->Put(write_options, "key6306", "value36794");

  // op 18005: put
  db->Put(write_options, "key6657", "value62857");

  // op 18006: put
  db->Put(write_options, "key11300", "value19793");

  // op 18007: delete
  db->Delete(write_options, "key6581");

  // op 18008: put
  db->Put(write_options, "key3436", "value54891");

  // op 18009: put
  db->Put(write_options, "key2458", "value52140");

  // op 18010: put
  db->Put(write_options, "key5143", "value19865");

  // op 18011: get
  status = db->Get(read_options, "key2982", &value);
  if (status.ok()) {
    out << "GET key2982 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18012: put
  db->Put(write_options, "key2357", "value7208");

  // op 18013: get
  status = db->Get(read_options, "key8751", &value);
  if (status.ok()) {
    out << "GET key8751 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18014: delete
  db->Delete(write_options, "key5966");

  // op 18015: delete_range
  status = db->DeleteRange(write_options, "key2989", "key4984");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 18016: put
  db->Put(write_options, "key3147", "value19906");

  // op 18017: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4870", "key10920", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4870, key10920) size=" << scan_result.size() << std::endl;
  }

  // op 18018: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 18019: put
  db->Put(write_options, "key7235", "value108580");

  // op 18020: put
  db->Put(write_options, "key4960", "value89912");

  // op 18021: delete_range
  status = db->DeleteRange(write_options, "key0614", "key9969");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 18022: put
  db->Put(write_options, "key10664", "value20881");

  // op 18023: get
  status = db->Get(read_options, "key7083", &value);
  if (status.ok()) {
    out << "GET key7083 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18024: put
  db->Put(write_options, "key9174", "value76223");

  // op 18025: get
  status = db->Get(read_options, "key2639", &value);
  if (status.ok()) {
    out << "GET key2639 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18026: put
  db->Put(write_options, "key1575", "value80069");

  // op 18027: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7405", "key9065", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7405, key9065) size=" << scan_result.size() << std::endl;
  }

  // op 18028: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3149", "key8195", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3149, key8195) size=" << scan_result.size() << std::endl;
  }

  // op 18029: delete_range
  status = db->DeleteRange(write_options, "key8792", "key10154");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 18030: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5962", "key7650", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5962, key7650) size=" << scan_result.size() << std::endl;
  }

  // op 18031: put
  db->Put(write_options, "key0194", "value30698");

  // op 18032: get
  status = db->Get(read_options, "key4981", &value);
  if (status.ok()) {
    out << "GET key4981 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18033: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2061", "key7928", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2061, key7928) size=" << scan_result.size() << std::endl;
  }

  // op 18034: put
  db->Put(write_options, "key6118", "value25359");

  // op 18035: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 18036: delete_range
  status = db->DeleteRange(write_options, "key0182", "key4384");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 18037: put
  db->Put(write_options, "key10823", "value73956");

  // op 18038: put
  db->Put(write_options, "key4736", "value70602");

  // op 18039: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 18040: delete_range
  status = db->DeleteRange(write_options, "key4497", "key10778");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 18041: get
  status = db->Get(read_options, "key1695", &value);
  if (status.ok()) {
    out << "GET key1695 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18042: get
  status = db->Get(read_options, "key6310", &value);
  if (status.ok()) {
    out << "GET key6310 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18043: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0213", "key9440", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0213, key9440) size=" << scan_result.size() << std::endl;
  }

  // op 18044: put
  db->Put(write_options, "key8193", "value59866");

  // op 18045: put
  db->Put(write_options, "key10498", "value76255");

  // op 18046: delete_range
  status = db->DeleteRange(write_options, "key8257", "key10611");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 18047: get
  status = db->Get(read_options, "key9603", &value);
  if (status.ok()) {
    out << "GET key9603 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18048: delete_range
  status = db->DeleteRange(write_options, "key10682", "key11071");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 18049: get
  status = db->Get(read_options, "key4635", &value);
  if (status.ok()) {
    out << "GET key4635 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18050: put
  db->Put(write_options, "key8001", "value76656");

  // op 18051: put
  db->Put(write_options, "key11374", "value116429");

  // op 18052: put
  db->Put(write_options, "key6849", "value109824");

  // op 18053: put
  db->Put(write_options, "key8553", "value33266");

  // op 18054: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5602", "key6787", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5602, key6787) size=" << scan_result.size() << std::endl;
  }

  // op 18055: get
  status = db->Get(read_options, "key2169", &value);
  if (status.ok()) {
    out << "GET key2169 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18056: put
  db->Put(write_options, "key10772", "value79340");

  // op 18057: put
  db->Put(write_options, "key10676", "value71985");

  // op 18058: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0104", "key5887", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0104, key5887) size=" << scan_result.size() << std::endl;
  }

  // op 18059: delete
  db->Delete(write_options, "key10700");

  // op 18060: put
  db->Put(write_options, "key11230", "value99101");

  // op 18061: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6505", "key8919", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6505, key8919) size=" << scan_result.size() << std::endl;
  }

  // op 18062: delete
  db->Delete(write_options, "key11159");

  // op 18063: put
  db->Put(write_options, "key10354", "value45403");

  // op 18064: get
  status = db->Get(read_options, "key2557", &value);
  if (status.ok()) {
    out << "GET key2557 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18065: get
  status = db->Get(read_options, "key2726", &value);
  if (status.ok()) {
    out << "GET key2726 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18066: put
  db->Put(write_options, "key0793", "value99629");

  // op 18067: delete_range
  status = db->DeleteRange(write_options, "key6958", "key10798");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 18068: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2403", "key8168", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2403, key8168) size=" << scan_result.size() << std::endl;
  }

  // op 18069: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5442", "key8352", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5442, key8352) size=" << scan_result.size() << std::endl;
  }

  // op 18070: delete
  db->Delete(write_options, "key6163");

  // op 18071: get
  status = db->Get(read_options, "key3628", &value);
  if (status.ok()) {
    out << "GET key3628 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18072: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5495", "key6367", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5495, key6367) size=" << scan_result.size() << std::endl;
  }

  // op 18073: get
  status = db->Get(read_options, "key0712", &value);
  if (status.ok()) {
    out << "GET key0712 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18074: get
  status = db->Get(read_options, "key0086", &value);
  if (status.ok()) {
    out << "GET key0086 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18075: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2378", "key4920", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2378, key4920) size=" << scan_result.size() << std::endl;
  }

  // op 18076: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0442", "key7677", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0442, key7677) size=" << scan_result.size() << std::endl;
  }

  // op 18077: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 18078: get
  status = db->Get(read_options, "key7061", &value);
  if (status.ok()) {
    out << "GET key7061 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18079: delete_range
  status = db->DeleteRange(write_options, "key2454", "key6140");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 18080: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 18081: put
  db->Put(write_options, "key1260", "value116969");

  // op 18082: put
  db->Put(write_options, "key4053", "value31406");

  // op 18083: delete_range
  status = db->DeleteRange(write_options, "key1614", "key10082");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 18084: delete_range
  status = db->DeleteRange(write_options, "key3827", "key4005");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 18085: delete
  db->Delete(write_options, "key4145");

  // op 18086: put
  db->Put(write_options, "key1166", "value67761");

  // op 18087: put
  db->Put(write_options, "key9675", "value15400");

  // op 18088: get
  status = db->Get(read_options, "key9062", &value);
  if (status.ok()) {
    out << "GET key9062 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18089: put
  db->Put(write_options, "key8114", "value34788");

  // op 18090: delete
  db->Delete(write_options, "key9722");

  // op 18091: put
  db->Put(write_options, "key1983", "value49736");

  // op 18092: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 18093: put
  db->Put(write_options, "key7246", "value1538");

  // op 18094: put
  db->Put(write_options, "key6069", "value52619");

  // op 18095: delete
  db->Delete(write_options, "key6188");

  // op 18096: get
  status = db->Get(read_options, "key6270", &value);
  if (status.ok()) {
    out << "GET key6270 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18097: put
  db->Put(write_options, "key6132", "value54091");

  // op 18098: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0476", "key7827", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0476, key7827) size=" << scan_result.size() << std::endl;
  }

  // op 18099: delete
  db->Delete(write_options, "key8303");

  // op 18100: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 18101: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4862", "key6287", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4862, key6287) size=" << scan_result.size() << std::endl;
  }

  // op 18102: get
  status = db->Get(read_options, "key7417", &value);
  if (status.ok()) {
    out << "GET key7417 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18103: put
  db->Put(write_options, "key8524", "value79721");

  // op 18104: delete_range
  status = db->DeleteRange(write_options, "key2180", "key2737");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 18105: delete
  db->Delete(write_options, "key9421");

  // op 18106: delete
  db->Delete(write_options, "key2014");

  // op 18107: put
  db->Put(write_options, "key3302", "value100052");

  // op 18108: delete
  db->Delete(write_options, "key2772");

  // op 18109: get
  status = db->Get(read_options, "key0877", &value);
  if (status.ok()) {
    out << "GET key0877 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18110: delete_range
  status = db->DeleteRange(write_options, "key2288", "key2906");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 18111: put
  db->Put(write_options, "key10348", "value29884");

  // op 18112: put
  db->Put(write_options, "key6901", "value100912");

  // op 18113: delete
  db->Delete(write_options, "key7472");

  // op 18114: get
  status = db->Get(read_options, "key8865", &value);
  if (status.ok()) {
    out << "GET key8865 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18115: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1015", "key5790", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1015, key5790) size=" << scan_result.size() << std::endl;
  }

  // op 18116: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4278", "key10923", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4278, key10923) size=" << scan_result.size() << std::endl;
  }

  // op 18117: put
  db->Put(write_options, "key8364", "value46733");

  // op 18118: put
  db->Put(write_options, "key3954", "value20301");

  // op 18119: put
  db->Put(write_options, "key5320", "value115246");

  // op 18120: put
  db->Put(write_options, "key11320", "value13388");

  // op 18121: get
  status = db->Get(read_options, "key5160", &value);
  if (status.ok()) {
    out << "GET key5160 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18122: put
  db->Put(write_options, "key6317", "value89622");

  // op 18123: put
  db->Put(write_options, "key8121", "value98918");

  // op 18124: delete_range
  status = db->DeleteRange(write_options, "key4316", "key10814");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 18125: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4364", "key9911", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4364, key9911) size=" << scan_result.size() << std::endl;
  }

  // op 18126: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0194", "key9932", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0194, key9932) size=" << scan_result.size() << std::endl;
  }

  // op 18127: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7795", "key11234", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7795, key11234) size=" << scan_result.size() << std::endl;
  }

  // op 18128: put
  db->Put(write_options, "key5208", "value82193");

  // op 18129: put
  db->Put(write_options, "key6390", "value25926");

  // op 18130: put
  db->Put(write_options, "key2899", "value76985");

  // op 18131: put
  db->Put(write_options, "key7814", "value44730");

  // op 18132: put
  db->Put(write_options, "key7510", "value47890");

  // op 18133: get
  status = db->Get(read_options, "key3484", &value);
  if (status.ok()) {
    out << "GET key3484 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18134: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 18135: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 18136: put
  db->Put(write_options, "key3218", "value93850");

  // op 18137: delete
  db->Delete(write_options, "key9504");

  // op 18138: delete
  db->Delete(write_options, "key3505");

  // op 18139: delete
  db->Delete(write_options, "key10470");

  // op 18140: get
  status = db->Get(read_options, "key6340", &value);
  if (status.ok()) {
    out << "GET key6340 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18141: put
  db->Put(write_options, "key6462", "value92923");

  // op 18142: put
  db->Put(write_options, "key4127", "value32931");

  // op 18143: put
  db->Put(write_options, "key10415", "value81407");

  // op 18144: put
  db->Put(write_options, "key4650", "value25368");

  // op 18145: delete_range
  status = db->DeleteRange(write_options, "key0720", "key3651");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 18146: delete
  db->Delete(write_options, "key0893");

  // op 18147: put
  db->Put(write_options, "key1453", "value40605");

  // op 18148: put
  db->Put(write_options, "key2437", "value53844");

  // op 18149: put
  db->Put(write_options, "key7897", "value20566");

  // op 18150: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7738", "key8666", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7738, key8666) size=" << scan_result.size() << std::endl;
  }

  // op 18151: put
  db->Put(write_options, "key8717", "value14802");

  // op 18152: get
  status = db->Get(read_options, "key0525", &value);
  if (status.ok()) {
    out << "GET key0525 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18153: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4725", "key7136", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4725, key7136) size=" << scan_result.size() << std::endl;
  }

  // op 18154: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1232", "key1962", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1232, key1962) size=" << scan_result.size() << std::endl;
  }

  // op 18155: put
  db->Put(write_options, "key10888", "value49839");

  // op 18156: put
  db->Put(write_options, "key10520", "value70945");

  // op 18157: delete_range
  status = db->DeleteRange(write_options, "key0104", "key8908");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 18158: put
  db->Put(write_options, "key7961", "value7026");

  // op 18159: get
  status = db->Get(read_options, "key10411", &value);
  if (status.ok()) {
    out << "GET key10411 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18160: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3344", "key9999", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3344, key9999) size=" << scan_result.size() << std::endl;
  }

  // op 18161: put
  db->Put(write_options, "key8706", "value56505");

  // op 18162: put
  db->Put(write_options, "key10018", "value89519");

  // op 18163: put
  db->Put(write_options, "key11038", "value63149");

  // op 18164: delete
  db->Delete(write_options, "key7482");

  // op 18165: put
  db->Put(write_options, "key9956", "value57559");

  // op 18166: get
  status = db->Get(read_options, "key1088", &value);
  if (status.ok()) {
    out << "GET key1088 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18167: put
  db->Put(write_options, "key2754", "value52449");

  // op 18168: get
  status = db->Get(read_options, "key5892", &value);
  if (status.ok()) {
    out << "GET key5892 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18169: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6610", "key11704", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6610, key11704) size=" << scan_result.size() << std::endl;
  }

  // op 18170: put
  db->Put(write_options, "key9711", "value32784");

  // op 18171: put
  db->Put(write_options, "key2130", "value17051");

  // op 18172: put
  db->Put(write_options, "key0439", "value34073");

  // op 18173: put
  db->Put(write_options, "key1602", "value44453");

  // op 18174: put
  db->Put(write_options, "key0872", "value107152");

  // op 18175: get
  status = db->Get(read_options, "key0990", &value);
  if (status.ok()) {
    out << "GET key0990 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18176: put
  db->Put(write_options, "key0601", "value42875");

  // op 18177: delete
  db->Delete(write_options, "key7334");

  // op 18178: put
  db->Put(write_options, "key2640", "value112242");

  // op 18179: put
  db->Put(write_options, "key6040", "value13426");

  // op 18180: put
  db->Put(write_options, "key10201", "value75373");

  // op 18181: put
  db->Put(write_options, "key5107", "value115032");

  // op 18182: put
  db->Put(write_options, "key9668", "value96156");

  // op 18183: put
  db->Put(write_options, "key3162", "value74448");

  // op 18184: get
  status = db->Get(read_options, "key0409", &value);
  if (status.ok()) {
    out << "GET key0409 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18185: get
  status = db->Get(read_options, "key2996", &value);
  if (status.ok()) {
    out << "GET key2996 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18186: put
  db->Put(write_options, "key0073", "value106482");

  // op 18187: put
  db->Put(write_options, "key9195", "value9274");

  // op 18188: delete
  db->Delete(write_options, "key0381");

  // op 18189: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6418", "key8997", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6418, key8997) size=" << scan_result.size() << std::endl;
  }

  // op 18190: put
  db->Put(write_options, "key1917", "value71180");

  // op 18191: get
  status = db->Get(read_options, "key10801", &value);
  if (status.ok()) {
    out << "GET key10801 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18192: get
  status = db->Get(read_options, "key9512", &value);
  if (status.ok()) {
    out << "GET key9512 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18193: put
  db->Put(write_options, "key5068", "value72515");

  // op 18194: get
  status = db->Get(read_options, "key2006", &value);
  if (status.ok()) {
    out << "GET key2006 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18195: put
  db->Put(write_options, "key0095", "value53378");

  // op 18196: get
  status = db->Get(read_options, "key6875", &value);
  if (status.ok()) {
    out << "GET key6875 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18197: put
  db->Put(write_options, "key9655", "value36608");

  // op 18198: put
  db->Put(write_options, "key11720", "value5418");

  // op 18199: put
  db->Put(write_options, "key11020", "value86067");

  // op 18200: get
  status = db->Get(read_options, "key6231", &value);
  if (status.ok()) {
    out << "GET key6231 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18201: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5332", "key6588", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5332, key6588) size=" << scan_result.size() << std::endl;
  }

  // op 18202: get
  status = db->Get(read_options, "key4330", &value);
  if (status.ok()) {
    out << "GET key4330 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18203: delete
  db->Delete(write_options, "key10543");

  // op 18204: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3647", "key11379", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3647, key11379) size=" << scan_result.size() << std::endl;
  }

  // op 18205: delete
  db->Delete(write_options, "key10051");

  // op 18206: get
  status = db->Get(read_options, "key5374", &value);
  if (status.ok()) {
    out << "GET key5374 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18207: delete
  db->Delete(write_options, "key7801");

  // op 18208: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4011", "key9250", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4011, key9250) size=" << scan_result.size() << std::endl;
  }

  // op 18209: put
  db->Put(write_options, "key3897", "value105605");

  // op 18210: delete_range
  status = db->DeleteRange(write_options, "key0459", "key9555");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 18211: delete_range
  status = db->DeleteRange(write_options, "key3781", "key7871");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 18212: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8168", "key10279", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8168, key10279) size=" << scan_result.size() << std::endl;
  }

  // op 18213: put
  db->Put(write_options, "key3498", "value97509");

  // op 18214: put
  db->Put(write_options, "key4984", "value68672");

  // op 18215: delete_range
  status = db->DeleteRange(write_options, "key1057", "key9685");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 18216: put
  db->Put(write_options, "key10150", "value90930");

  // op 18217: delete
  db->Delete(write_options, "key2898");

  // op 18218: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 18219: delete
  db->Delete(write_options, "key7352");

  // op 18220: get
  status = db->Get(read_options, "key6598", &value);
  if (status.ok()) {
    out << "GET key6598 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18221: delete
  db->Delete(write_options, "key2492");

  // op 18222: put
  db->Put(write_options, "key6505", "value38265");

  // op 18223: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5260", "key11454", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5260, key11454) size=" << scan_result.size() << std::endl;
  }

  // op 18224: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 18225: delete
  db->Delete(write_options, "key4381");

  // op 18226: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7219", "key9341", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7219, key9341) size=" << scan_result.size() << std::endl;
  }

  // op 18227: get
  status = db->Get(read_options, "key8599", &value);
  if (status.ok()) {
    out << "GET key8599 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18228: delete_range
  status = db->DeleteRange(write_options, "key5556", "key9957");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 18229: delete_range
  status = db->DeleteRange(write_options, "key1072", "key10547");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 18230: delete
  db->Delete(write_options, "key11157");

  // op 18231: get
  status = db->Get(read_options, "key6017", &value);
  if (status.ok()) {
    out << "GET key6017 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18232: put
  db->Put(write_options, "key2325", "value12859");

  // op 18233: get
  status = db->Get(read_options, "key5392", &value);
  if (status.ok()) {
    out << "GET key5392 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18234: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2410", "key5718", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2410, key5718) size=" << scan_result.size() << std::endl;
  }

  // op 18235: put
  db->Put(write_options, "key1342", "value84845");

  // op 18236: get
  status = db->Get(read_options, "key1475", &value);
  if (status.ok()) {
    out << "GET key1475 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18237: put
  db->Put(write_options, "key10604", "value100268");

  // op 18238: get
  status = db->Get(read_options, "key1727", &value);
  if (status.ok()) {
    out << "GET key1727 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18239: delete
  db->Delete(write_options, "key10184");

  // op 18240: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 18241: delete
  db->Delete(write_options, "key2844");

  // op 18242: put
  db->Put(write_options, "key2269", "value58812");

  // op 18243: put
  db->Put(write_options, "key4298", "value98895");

  // op 18244: put
  db->Put(write_options, "key4769", "value18742");

  // op 18245: get
  status = db->Get(read_options, "key8089", &value);
  if (status.ok()) {
    out << "GET key8089 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18246: put
  db->Put(write_options, "key10963", "value37369");

  // op 18247: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4832", "key9452", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4832, key9452) size=" << scan_result.size() << std::endl;
  }

  // op 18248: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 18249: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3632", "key11424", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3632, key11424) size=" << scan_result.size() << std::endl;
  }

  // op 18250: put
  db->Put(write_options, "key7223", "value16011");

  // op 18251: delete_range
  status = db->DeleteRange(write_options, "key0684", "key2065");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 18252: get
  status = db->Get(read_options, "key5718", &value);
  if (status.ok()) {
    out << "GET key5718 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18253: delete_range
  status = db->DeleteRange(write_options, "key0096", "key8383");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 18254: put
  db->Put(write_options, "key7047", "value5889");

  // op 18255: put
  db->Put(write_options, "key0987", "value22499");

  // op 18256: delete
  db->Delete(write_options, "key11145");

  // op 18257: delete
  db->Delete(write_options, "key2527");

  // op 18258: put
  db->Put(write_options, "key10543", "value93664");

  // op 18259: get
  status = db->Get(read_options, "key9819", &value);
  if (status.ok()) {
    out << "GET key9819 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18260: delete
  db->Delete(write_options, "key9055");

  // op 18261: get
  status = db->Get(read_options, "key10924", &value);
  if (status.ok()) {
    out << "GET key10924 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18262: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1369", "key6815", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1369, key6815) size=" << scan_result.size() << std::endl;
  }

  // op 18263: put
  db->Put(write_options, "key0417", "value96051");

  // op 18264: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8927", "key10279", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8927, key10279) size=" << scan_result.size() << std::endl;
  }

  // op 18265: delete_range
  status = db->DeleteRange(write_options, "key11084", "key11558");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 18266: get
  status = db->Get(read_options, "key11065", &value);
  if (status.ok()) {
    out << "GET key11065 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18267: delete_range
  status = db->DeleteRange(write_options, "key2279", "key8237");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 18268: put
  db->Put(write_options, "key8130", "value107300");

  // op 18269: get
  status = db->Get(read_options, "key7056", &value);
  if (status.ok()) {
    out << "GET key7056 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18270: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3955", "key8254", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3955, key8254) size=" << scan_result.size() << std::endl;
  }

  // op 18271: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3640", "key11106", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3640, key11106) size=" << scan_result.size() << std::endl;
  }

  // op 18272: put
  db->Put(write_options, "key8276", "value114882");

  // op 18273: delete_range
  status = db->DeleteRange(write_options, "key3149", "key8911");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 18274: put
  db->Put(write_options, "key7710", "value65484");

  // op 18275: put
  db->Put(write_options, "key5551", "value34512");

  // op 18276: get
  status = db->Get(read_options, "key8488", &value);
  if (status.ok()) {
    out << "GET key8488 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18277: get
  status = db->Get(read_options, "key2610", &value);
  if (status.ok()) {
    out << "GET key2610 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18278: get
  status = db->Get(read_options, "key10731", &value);
  if (status.ok()) {
    out << "GET key10731 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18279: put
  db->Put(write_options, "key0827", "value28750");

  // op 18280: delete
  db->Delete(write_options, "key2421");

  // op 18281: put
  db->Put(write_options, "key3761", "value110685");

  // op 18282: put
  db->Put(write_options, "key8982", "value59545");

  // op 18283: put
  db->Put(write_options, "key10195", "value35422");

  // op 18284: get
  status = db->Get(read_options, "key8104", &value);
  if (status.ok()) {
    out << "GET key8104 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18285: put
  db->Put(write_options, "key3105", "value5850");

  // op 18286: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2365", "key3271", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2365, key3271) size=" << scan_result.size() << std::endl;
  }

  // op 18287: delete
  db->Delete(write_options, "key7418");

  // op 18288: delete
  db->Delete(write_options, "key2432");

  // op 18289: get
  status = db->Get(read_options, "key1749", &value);
  if (status.ok()) {
    out << "GET key1749 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18290: get
  status = db->Get(read_options, "key11536", &value);
  if (status.ok()) {
    out << "GET key11536 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18291: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0317", "key9849", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0317, key9849) size=" << scan_result.size() << std::endl;
  }

  // op 18292: get
  status = db->Get(read_options, "key7641", &value);
  if (status.ok()) {
    out << "GET key7641 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18293: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4229", "key11562", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4229, key11562) size=" << scan_result.size() << std::endl;
  }

  // op 18294: put
  db->Put(write_options, "key7665", "value82343");

  // op 18295: get
  status = db->Get(read_options, "key9692", &value);
  if (status.ok()) {
    out << "GET key9692 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18296: delete
  db->Delete(write_options, "key8718");

  // op 18297: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1803", "key9635", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1803, key9635) size=" << scan_result.size() << std::endl;
  }

  // op 18298: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10282", "key10698", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10282, key10698) size=" << scan_result.size() << std::endl;
  }

  // op 18299: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0088", "key3546", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0088, key3546) size=" << scan_result.size() << std::endl;
  }

  // op 18300: put
  db->Put(write_options, "key4076", "value45680");

  // op 18301: put
  db->Put(write_options, "key9405", "value58656");

  // op 18302: get
  status = db->Get(read_options, "key8830", &value);
  if (status.ok()) {
    out << "GET key8830 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18303: put
  db->Put(write_options, "key6278", "value57924");

  // op 18304: put
  db->Put(write_options, "key4137", "value24515");

  // op 18305: delete_range
  status = db->DeleteRange(write_options, "key9733", "key11643");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 18306: put
  db->Put(write_options, "key0772", "value93002");

  // op 18307: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4169", "key5688", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4169, key5688) size=" << scan_result.size() << std::endl;
  }

  // op 18308: put
  db->Put(write_options, "key4216", "value65170");

  // op 18309: delete
  db->Delete(write_options, "key5431");

  // op 18310: put
  db->Put(write_options, "key9687", "value83016");

  // op 18311: put
  db->Put(write_options, "key11205", "value0933");

  // op 18312: get
  status = db->Get(read_options, "key1738", &value);
  if (status.ok()) {
    out << "GET key1738 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18313: delete_range
  status = db->DeleteRange(write_options, "key7034", "key9577");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 18314: delete_range
  status = db->DeleteRange(write_options, "key7345", "key10632");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 18315: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0777", "key6588", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0777, key6588) size=" << scan_result.size() << std::endl;
  }

  // op 18316: put
  db->Put(write_options, "key11287", "value80712");

  // op 18317: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 18318: get
  status = db->Get(read_options, "key8233", &value);
  if (status.ok()) {
    out << "GET key8233 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18319: delete
  db->Delete(write_options, "key4319");

  // op 18320: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1776", "key3491", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1776, key3491) size=" << scan_result.size() << std::endl;
  }

  // op 18321: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 18322: put
  db->Put(write_options, "key8590", "value33914");

  // op 18323: get
  status = db->Get(read_options, "key1183", &value);
  if (status.ok()) {
    out << "GET key1183 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18324: delete
  db->Delete(write_options, "key10280");

  // op 18325: get
  status = db->Get(read_options, "key7845", &value);
  if (status.ok()) {
    out << "GET key7845 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18326: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1990", "key9978", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1990, key9978) size=" << scan_result.size() << std::endl;
  }

  // op 18327: get
  status = db->Get(read_options, "key6968", &value);
  if (status.ok()) {
    out << "GET key6968 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18328: put
  db->Put(write_options, "key2875", "value102912");

  // op 18329: put
  db->Put(write_options, "key6823", "value48893");

  // op 18330: get
  status = db->Get(read_options, "key1534", &value);
  if (status.ok()) {
    out << "GET key1534 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18331: put
  db->Put(write_options, "key5490", "value25771");

  // op 18332: put
  db->Put(write_options, "key8613", "value62642");

  // op 18333: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3625", "key5397", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3625, key5397) size=" << scan_result.size() << std::endl;
  }

  // op 18334: put
  db->Put(write_options, "key10685", "value6825");

  // op 18335: put
  db->Put(write_options, "key3211", "value59871");

  // op 18336: delete_range
  status = db->DeleteRange(write_options, "key2129", "key9843");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 18337: delete_range
  status = db->DeleteRange(write_options, "key2901", "key7850");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 18338: put
  db->Put(write_options, "key4521", "value97384");

  // op 18339: put
  db->Put(write_options, "key2718", "value58679");

  // op 18340: put
  db->Put(write_options, "key1780", "value64342");

  // op 18341: put
  db->Put(write_options, "key2360", "value117595");

  // op 18342: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2269", "key4750", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2269, key4750) size=" << scan_result.size() << std::endl;
  }

  // op 18343: get
  status = db->Get(read_options, "key2345", &value);
  if (status.ok()) {
    out << "GET key2345 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18344: put
  db->Put(write_options, "key10336", "value109765");

  // op 18345: put
  db->Put(write_options, "key1766", "value74771");

  // op 18346: delete
  db->Delete(write_options, "key8325");

  // op 18347: put
  db->Put(write_options, "key4341", "value52382");

  // op 18348: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 18349: delete_range
  status = db->DeleteRange(write_options, "key2587", "key10889");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 18350: put
  db->Put(write_options, "key11528", "value60381");

  // op 18351: get
  status = db->Get(read_options, "key1646", &value);
  if (status.ok()) {
    out << "GET key1646 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18352: put
  db->Put(write_options, "key7971", "value117513");

  // op 18353: delete_range
  status = db->DeleteRange(write_options, "key6355", "key11710");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 18354: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1745", "key6834", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1745, key6834) size=" << scan_result.size() << std::endl;
  }

  // op 18355: get
  status = db->Get(read_options, "key8160", &value);
  if (status.ok()) {
    out << "GET key8160 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18356: put
  db->Put(write_options, "key7342", "value63082");

  // op 18357: put
  db->Put(write_options, "key7291", "value0252");

  // op 18358: put
  db->Put(write_options, "key2538", "value38059");

  // op 18359: delete
  db->Delete(write_options, "key2025");

  // op 18360: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 18361: put
  db->Put(write_options, "key5062", "value57288");

  // op 18362: get
  status = db->Get(read_options, "key5360", &value);
  if (status.ok()) {
    out << "GET key5360 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18363: delete
  db->Delete(write_options, "key4016");

  // op 18364: get
  status = db->Get(read_options, "key8910", &value);
  if (status.ok()) {
    out << "GET key8910 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18365: get
  status = db->Get(read_options, "key4625", &value);
  if (status.ok()) {
    out << "GET key4625 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18366: delete_range
  status = db->DeleteRange(write_options, "key4425", "key9643");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 18367: delete_range
  status = db->DeleteRange(write_options, "key9355", "key10988");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 18368: delete_range
  status = db->DeleteRange(write_options, "key5494", "key5866");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 18369: put
  db->Put(write_options, "key5692", "value2913");

  // op 18370: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4107", "key5384", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4107, key5384) size=" << scan_result.size() << std::endl;
  }

  // op 18371: put
  db->Put(write_options, "key2142", "value89879");

  // op 18372: put
  db->Put(write_options, "key6828", "value93436");

  // op 18373: put
  db->Put(write_options, "key2964", "value19921");

  // op 18374: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3902", "key7152", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3902, key7152) size=" << scan_result.size() << std::endl;
  }

  // op 18375: get
  status = db->Get(read_options, "key3111", &value);
  if (status.ok()) {
    out << "GET key3111 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18376: get
  status = db->Get(read_options, "key8137", &value);
  if (status.ok()) {
    out << "GET key8137 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18377: delete
  db->Delete(write_options, "key4034");

  // op 18378: put
  db->Put(write_options, "key10096", "value114713");

  // op 18379: get
  status = db->Get(read_options, "key0527", &value);
  if (status.ok()) {
    out << "GET key0527 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18380: put
  db->Put(write_options, "key0829", "value81491");

  // op 18381: put
  db->Put(write_options, "key5136", "value24727");

  // op 18382: put
  db->Put(write_options, "key7328", "value35268");

  // op 18383: put
  db->Put(write_options, "key9084", "value109189");

  // op 18384: put
  db->Put(write_options, "key8338", "value107048");

  // op 18385: put
  db->Put(write_options, "key0936", "value79450");

  // op 18386: put
  db->Put(write_options, "key9692", "value56706");

  // op 18387: put
  db->Put(write_options, "key9772", "value46649");

  // op 18388: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9171", "key10808", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9171, key10808) size=" << scan_result.size() << std::endl;
  }

  // op 18389: put
  db->Put(write_options, "key0916", "value13811");

  // op 18390: get
  status = db->Get(read_options, "key3235", &value);
  if (status.ok()) {
    out << "GET key3235 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18391: put
  db->Put(write_options, "key0427", "value84698");

  // op 18392: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 18393: get
  status = db->Get(read_options, "key3868", &value);
  if (status.ok()) {
    out << "GET key3868 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18394: put
  db->Put(write_options, "key8614", "value26126");

  // op 18395: get
  status = db->Get(read_options, "key9164", &value);
  if (status.ok()) {
    out << "GET key9164 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18396: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 18397: delete
  db->Delete(write_options, "key0243");

  // op 18398: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 18399: get
  status = db->Get(read_options, "key2432", &value);
  if (status.ok()) {
    out << "GET key2432 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18400: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2765", "key9853", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2765, key9853) size=" << scan_result.size() << std::endl;
  }

  // op 18401: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4877", "key11401", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4877, key11401) size=" << scan_result.size() << std::endl;
  }

  // op 18402: put
  db->Put(write_options, "key5276", "value42038");

  // op 18403: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7271", "key11130", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7271, key11130) size=" << scan_result.size() << std::endl;
  }

  // op 18404: get
  status = db->Get(read_options, "key4634", &value);
  if (status.ok()) {
    out << "GET key4634 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18405: put
  db->Put(write_options, "key3224", "value16777");

  // op 18406: put
  db->Put(write_options, "key6676", "value110238");

  // op 18407: get
  status = db->Get(read_options, "key4472", &value);
  if (status.ok()) {
    out << "GET key4472 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18408: get
  status = db->Get(read_options, "key11177", &value);
  if (status.ok()) {
    out << "GET key11177 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18409: get
  status = db->Get(read_options, "key6383", &value);
  if (status.ok()) {
    out << "GET key6383 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18410: get
  status = db->Get(read_options, "key11440", &value);
  if (status.ok()) {
    out << "GET key11440 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18411: delete_range
  status = db->DeleteRange(write_options, "key3077", "key8082");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 18412: delete
  db->Delete(write_options, "key0373");

  // op 18413: delete_range
  status = db->DeleteRange(write_options, "key2896", "key11328");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 18414: put
  db->Put(write_options, "key11294", "value38400");

  // op 18415: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0850", "key3972", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0850, key3972) size=" << scan_result.size() << std::endl;
  }

  // op 18416: put
  db->Put(write_options, "key8505", "value19489");

  // op 18417: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5169", "key6878", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5169, key6878) size=" << scan_result.size() << std::endl;
  }

  // op 18418: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 18419: put
  db->Put(write_options, "key5998", "value8483");

  // op 18420: put
  db->Put(write_options, "key9122", "value17662");

  // op 18421: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1711", "key3152", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1711, key3152) size=" << scan_result.size() << std::endl;
  }

  // op 18422: delete
  db->Delete(write_options, "key0348");

  // op 18423: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2419", "key4187", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2419, key4187) size=" << scan_result.size() << std::endl;
  }

  // op 18424: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0132", "key8981", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0132, key8981) size=" << scan_result.size() << std::endl;
  }

  // op 18425: put
  db->Put(write_options, "key1743", "value18767");

  // op 18426: delete
  db->Delete(write_options, "key10939");

  // op 18427: put
  db->Put(write_options, "key6565", "value117197");

  // op 18428: delete
  db->Delete(write_options, "key1996");

  // op 18429: get
  status = db->Get(read_options, "key8529", &value);
  if (status.ok()) {
    out << "GET key8529 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18430: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 18431: put
  db->Put(write_options, "key1512", "value81812");

  // op 18432: get
  status = db->Get(read_options, "key3897", &value);
  if (status.ok()) {
    out << "GET key3897 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18433: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0811", "key5399", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0811, key5399) size=" << scan_result.size() << std::endl;
  }

  // op 18434: delete
  db->Delete(write_options, "key9915");

  // op 18435: get
  status = db->Get(read_options, "key5049", &value);
  if (status.ok()) {
    out << "GET key5049 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18436: put
  db->Put(write_options, "key9228", "value117997");

  // op 18437: put
  db->Put(write_options, "key11101", "value58209");

  // op 18438: get
  status = db->Get(read_options, "key4589", &value);
  if (status.ok()) {
    out << "GET key4589 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18439: put
  db->Put(write_options, "key4516", "value17747");

  // op 18440: put
  db->Put(write_options, "key7788", "value15328");

  // op 18441: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0421", "key1310", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0421, key1310) size=" << scan_result.size() << std::endl;
  }

  // op 18442: get
  status = db->Get(read_options, "key0295", &value);
  if (status.ok()) {
    out << "GET key0295 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18443: put
  db->Put(write_options, "key3039", "value21413");

  // op 18444: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4965", "key10343", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4965, key10343) size=" << scan_result.size() << std::endl;
  }

  // op 18445: get
  status = db->Get(read_options, "key11041", &value);
  if (status.ok()) {
    out << "GET key11041 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18446: get
  status = db->Get(read_options, "key8827", &value);
  if (status.ok()) {
    out << "GET key8827 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18447: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 18448: delete_range
  status = db->DeleteRange(write_options, "key2078", "key10116");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 18449: put
  db->Put(write_options, "key6983", "value49064");

  // op 18450: put
  db->Put(write_options, "key5016", "value60281");

  // op 18451: put
  db->Put(write_options, "key7651", "value41890");

  // op 18452: delete_range
  status = db->DeleteRange(write_options, "key7482", "key10058");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 18453: get
  status = db->Get(read_options, "key4262", &value);
  if (status.ok()) {
    out << "GET key4262 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18454: put
  db->Put(write_options, "key4651", "value19397");

  // op 18455: put
  db->Put(write_options, "key9981", "value83097");

  // op 18456: get
  status = db->Get(read_options, "key8188", &value);
  if (status.ok()) {
    out << "GET key8188 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18457: delete
  db->Delete(write_options, "key8404");

  // op 18458: put
  db->Put(write_options, "key8842", "value70540");

  // op 18459: get
  status = db->Get(read_options, "key3138", &value);
  if (status.ok()) {
    out << "GET key3138 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18460: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0769", "key1820", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0769, key1820) size=" << scan_result.size() << std::endl;
  }

  // op 18461: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1805", "key7362", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1805, key7362) size=" << scan_result.size() << std::endl;
  }

  // op 18462: put
  db->Put(write_options, "key9049", "value71980");

  // op 18463: delete_range
  status = db->DeleteRange(write_options, "key4684", "key6923");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 18464: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3861", "key5940", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3861, key5940) size=" << scan_result.size() << std::endl;
  }

  // op 18465: put
  db->Put(write_options, "key2662", "value113538");

  // op 18466: put
  db->Put(write_options, "key9575", "value40597");

  // op 18467: put
  db->Put(write_options, "key11554", "value97150");

  // op 18468: delete
  db->Delete(write_options, "key1259");

  // op 18469: put
  db->Put(write_options, "key0686", "value92278");

  // op 18470: put
  db->Put(write_options, "key9434", "value89829");

  // op 18471: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7465", "key10567", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7465, key10567) size=" << scan_result.size() << std::endl;
  }

  // op 18472: put
  db->Put(write_options, "key3298", "value32407");

  // op 18473: put
  db->Put(write_options, "key3794", "value98731");

  // op 18474: put
  db->Put(write_options, "key11684", "value99590");

  // op 18475: delete_range
  status = db->DeleteRange(write_options, "key2062", "key4210");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 18476: put
  db->Put(write_options, "key4625", "value94283");

  // op 18477: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 18478: delete
  db->Delete(write_options, "key1474");

  // op 18479: put
  db->Put(write_options, "key5701", "value112518");

  // op 18480: delete_range
  status = db->DeleteRange(write_options, "key3816", "key11046");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 18481: put
  db->Put(write_options, "key9077", "value117098");

  // op 18482: put
  db->Put(write_options, "key9796", "value1100");

  // op 18483: get
  status = db->Get(read_options, "key9699", &value);
  if (status.ok()) {
    out << "GET key9699 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18484: put
  db->Put(write_options, "key0992", "value34936");

  // op 18485: get
  status = db->Get(read_options, "key3022", &value);
  if (status.ok()) {
    out << "GET key3022 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18486: put
  db->Put(write_options, "key0507", "value85264");

  // op 18487: put
  db->Put(write_options, "key5369", "value47583");

  // op 18488: put
  db->Put(write_options, "key3276", "value106423");

  // op 18489: put
  db->Put(write_options, "key9227", "value33736");

  // op 18490: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1684", "key10047", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1684, key10047) size=" << scan_result.size() << std::endl;
  }

  // op 18491: delete_range
  status = db->DeleteRange(write_options, "key2762", "key8712");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 18492: put
  db->Put(write_options, "key6229", "value46655");

  // op 18493: put
  db->Put(write_options, "key0556", "value89890");

  // op 18494: get
  status = db->Get(read_options, "key9010", &value);
  if (status.ok()) {
    out << "GET key9010 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18495: delete
  db->Delete(write_options, "key9214");

  // op 18496: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 18497: delete
  db->Delete(write_options, "key9902");

  // op 18498: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1896", "key8939", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1896, key8939) size=" << scan_result.size() << std::endl;
  }

  // op 18499: put
  db->Put(write_options, "key6189", "value115838");

  // op 18500: get
  status = db->Get(read_options, "key0501", &value);
  if (status.ok()) {
    out << "GET key0501 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18501: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 18502: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5973", "key10458", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5973, key10458) size=" << scan_result.size() << std::endl;
  }

  // op 18503: put
  db->Put(write_options, "key7375", "value6017");

  // op 18504: get
  status = db->Get(read_options, "key2544", &value);
  if (status.ok()) {
    out << "GET key2544 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18505: put
  db->Put(write_options, "key3751", "value34158");

  // op 18506: get
  status = db->Get(read_options, "key3379", &value);
  if (status.ok()) {
    out << "GET key3379 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18507: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8873", "key10852", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8873, key10852) size=" << scan_result.size() << std::endl;
  }

  // op 18508: put
  db->Put(write_options, "key8674", "value116980");

  // op 18509: put
  db->Put(write_options, "key1542", "value25585");

  // op 18510: put
  db->Put(write_options, "key9742", "value105984");

  // op 18511: put
  db->Put(write_options, "key8047", "value74846");

  // op 18512: get
  status = db->Get(read_options, "key2153", &value);
  if (status.ok()) {
    out << "GET key2153 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18513: delete_range
  status = db->DeleteRange(write_options, "key2862", "key4285");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 18514: put
  db->Put(write_options, "key10366", "value115463");

  // op 18515: get
  status = db->Get(read_options, "key5555", &value);
  if (status.ok()) {
    out << "GET key5555 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18516: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 18517: delete
  db->Delete(write_options, "key5312");

  // op 18518: delete_range
  status = db->DeleteRange(write_options, "key7988", "key11527");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 18519: delete
  db->Delete(write_options, "key7968");

  // op 18520: put
  db->Put(write_options, "key4593", "value10805");

  // op 18521: put
  db->Put(write_options, "key8373", "value79084");

  // op 18522: put
  db->Put(write_options, "key5714", "value35494");

  // op 18523: put
  db->Put(write_options, "key5871", "value7680");

  // op 18524: get
  status = db->Get(read_options, "key11399", &value);
  if (status.ok()) {
    out << "GET key11399 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18525: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6590", "key7242", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6590, key7242) size=" << scan_result.size() << std::endl;
  }

  // op 18526: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1379", "key7472", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1379, key7472) size=" << scan_result.size() << std::endl;
  }

  // op 18527: put
  db->Put(write_options, "key7152", "value11932");

  // op 18528: get
  status = db->Get(read_options, "key2968", &value);
  if (status.ok()) {
    out << "GET key2968 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18529: put
  db->Put(write_options, "key4727", "value115542");

  // op 18530: put
  db->Put(write_options, "key5278", "value36499");

  // op 18531: put
  db->Put(write_options, "key8039", "value27170");

  // op 18532: put
  db->Put(write_options, "key9013", "value58391");

  // op 18533: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 18534: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0774", "key2809", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0774, key2809) size=" << scan_result.size() << std::endl;
  }

  // op 18535: put
  db->Put(write_options, "key2031", "value73134");

  // op 18536: get
  status = db->Get(read_options, "key4202", &value);
  if (status.ok()) {
    out << "GET key4202 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18537: get
  status = db->Get(read_options, "key0180", &value);
  if (status.ok()) {
    out << "GET key0180 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18538: put
  db->Put(write_options, "key9614", "value56793");

  // op 18539: put
  db->Put(write_options, "key0443", "value103870");

  // op 18540: delete
  db->Delete(write_options, "key0057");

  // op 18541: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0251", "key2886", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0251, key2886) size=" << scan_result.size() << std::endl;
  }

  // op 18542: put
  db->Put(write_options, "key5862", "value111001");

  // op 18543: put
  db->Put(write_options, "key11057", "value116331");

  // op 18544: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1894", "key8417", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1894, key8417) size=" << scan_result.size() << std::endl;
  }

  // op 18545: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2662", "key5918", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2662, key5918) size=" << scan_result.size() << std::endl;
  }

  // op 18546: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0508", "key4349", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0508, key4349) size=" << scan_result.size() << std::endl;
  }

  // op 18547: get
  status = db->Get(read_options, "key7088", &value);
  if (status.ok()) {
    out << "GET key7088 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18548: get
  status = db->Get(read_options, "key5063", &value);
  if (status.ok()) {
    out << "GET key5063 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18549: put
  db->Put(write_options, "key0963", "value109742");

  // op 18550: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9654", "key10418", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9654, key10418) size=" << scan_result.size() << std::endl;
  }

  // op 18551: get
  status = db->Get(read_options, "key6071", &value);
  if (status.ok()) {
    out << "GET key6071 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18552: delete
  db->Delete(write_options, "key9650");

  // op 18553: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3954", "key6616", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3954, key6616) size=" << scan_result.size() << std::endl;
  }

  // op 18554: put
  db->Put(write_options, "key6346", "value57135");

  // op 18555: get
  status = db->Get(read_options, "key10042", &value);
  if (status.ok()) {
    out << "GET key10042 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18556: put
  db->Put(write_options, "key1384", "value3643");

  // op 18557: put
  db->Put(write_options, "key9664", "value113467");

  // op 18558: get
  status = db->Get(read_options, "key1179", &value);
  if (status.ok()) {
    out << "GET key1179 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18559: get
  status = db->Get(read_options, "key5030", &value);
  if (status.ok()) {
    out << "GET key5030 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18560: get
  status = db->Get(read_options, "key8676", &value);
  if (status.ok()) {
    out << "GET key8676 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18561: get
  status = db->Get(read_options, "key8041", &value);
  if (status.ok()) {
    out << "GET key8041 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18562: delete_range
  status = db->DeleteRange(write_options, "key4917", "key8914");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 18563: put
  db->Put(write_options, "key3219", "value6192");

  // op 18564: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 18565: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5526", "key7033", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5526, key7033) size=" << scan_result.size() << std::endl;
  }

  // op 18566: put
  db->Put(write_options, "key11199", "value15496");

  // op 18567: get
  status = db->Get(read_options, "key0758", &value);
  if (status.ok()) {
    out << "GET key0758 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18568: put
  db->Put(write_options, "key11530", "value107521");

  // op 18569: put
  db->Put(write_options, "key9811", "value110046");

  // op 18570: delete_range
  status = db->DeleteRange(write_options, "key5904", "key7884");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 18571: get
  status = db->Get(read_options, "key8043", &value);
  if (status.ok()) {
    out << "GET key8043 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18572: put
  db->Put(write_options, "key8865", "value112248");

  // op 18573: put
  db->Put(write_options, "key11170", "value88710");

  // op 18574: delete_range
  status = db->DeleteRange(write_options, "key1216", "key4407");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 18575: delete
  db->Delete(write_options, "key6021");

  // op 18576: delete
  db->Delete(write_options, "key0784");

  // op 18577: put
  db->Put(write_options, "key8188", "value96870");

  // op 18578: delete_range
  status = db->DeleteRange(write_options, "key0782", "key3356");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 18579: put
  db->Put(write_options, "key2152", "value64629");

  // op 18580: delete
  db->Delete(write_options, "key5110");

  // op 18581: put
  db->Put(write_options, "key5073", "value12972");

  // op 18582: put
  db->Put(write_options, "key2454", "value75507");

  // op 18583: put
  db->Put(write_options, "key4144", "value94639");

  // op 18584: put
  db->Put(write_options, "key10692", "value73381");

  // op 18585: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 18586: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8373", "key9678", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8373, key9678) size=" << scan_result.size() << std::endl;
  }

  // op 18587: get
  status = db->Get(read_options, "key3081", &value);
  if (status.ok()) {
    out << "GET key3081 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18588: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8056", "key11360", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8056, key11360) size=" << scan_result.size() << std::endl;
  }

  // op 18589: get
  status = db->Get(read_options, "key3935", &value);
  if (status.ok()) {
    out << "GET key3935 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18590: get
  status = db->Get(read_options, "key4873", &value);
  if (status.ok()) {
    out << "GET key4873 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18591: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9037", "key10591", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9037, key10591) size=" << scan_result.size() << std::endl;
  }

  // op 18592: delete_range
  status = db->DeleteRange(write_options, "key4579", "key10988");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 18593: put
  db->Put(write_options, "key1709", "value117897");

  // op 18594: put
  db->Put(write_options, "key2993", "value70028");

  // op 18595: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7121", "key7456", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7121, key7456) size=" << scan_result.size() << std::endl;
  }

  // op 18596: put
  db->Put(write_options, "key1456", "value111963");

  // op 18597: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8046", "key9338", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8046, key9338) size=" << scan_result.size() << std::endl;
  }

  // op 18598: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5099", "key5388", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5099, key5388) size=" << scan_result.size() << std::endl;
  }

  // op 18599: delete_range
  status = db->DeleteRange(write_options, "key0607", "key6634");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 18600: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2488", "key2495", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2488, key2495) size=" << scan_result.size() << std::endl;
  }

  // op 18601: put
  db->Put(write_options, "key3427", "value28640");

  // op 18602: delete_range
  status = db->DeleteRange(write_options, "key3693", "key4411");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 18603: get
  status = db->Get(read_options, "key1244", &value);
  if (status.ok()) {
    out << "GET key1244 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18604: delete
  db->Delete(write_options, "key0456");

  // op 18605: put
  db->Put(write_options, "key6189", "value89932");

  // op 18606: put
  db->Put(write_options, "key1257", "value105460");

  // op 18607: put
  db->Put(write_options, "key5241", "value6535");

  // op 18608: put
  db->Put(write_options, "key8832", "value35818");

  // op 18609: delete_range
  status = db->DeleteRange(write_options, "key3347", "key8139");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 18610: put
  db->Put(write_options, "key11364", "value16103");

  // op 18611: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4522", "key7706", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4522, key7706) size=" << scan_result.size() << std::endl;
  }

  // op 18612: delete_range
  status = db->DeleteRange(write_options, "key7468", "key8710");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 18613: put
  db->Put(write_options, "key0900", "value23087");

  // op 18614: delete
  db->Delete(write_options, "key11330");

  // op 18615: put
  db->Put(write_options, "key8574", "value75065");

  // op 18616: put
  db->Put(write_options, "key3581", "value55207");

  // op 18617: delete
  db->Delete(write_options, "key1768");

  // op 18618: get
  status = db->Get(read_options, "key5297", &value);
  if (status.ok()) {
    out << "GET key5297 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18619: delete_range
  status = db->DeleteRange(write_options, "key0201", "key6958");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 18620: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 18621: put
  db->Put(write_options, "key7160", "value10400");

  // op 18622: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5604", "key9969", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5604, key9969) size=" << scan_result.size() << std::endl;
  }

  // op 18623: put
  db->Put(write_options, "key5395", "value51688");

  // op 18624: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6820", "key11393", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6820, key11393) size=" << scan_result.size() << std::endl;
  }

  // op 18625: put
  db->Put(write_options, "key5056", "value4331");

  // op 18626: put
  db->Put(write_options, "key1843", "value51694");

  // op 18627: put
  db->Put(write_options, "key3345", "value34890");

  // op 18628: delete_range
  status = db->DeleteRange(write_options, "key5676", "key7094");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 18629: put
  db->Put(write_options, "key9709", "value45812");

  // op 18630: put
  db->Put(write_options, "key0390", "value71360");

  // op 18631: delete_range
  status = db->DeleteRange(write_options, "key1090", "key2464");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 18632: delete
  db->Delete(write_options, "key11284");

  // op 18633: delete_range
  status = db->DeleteRange(write_options, "key3539", "key10460");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 18634: delete
  db->Delete(write_options, "key2626");

  // op 18635: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4686", "key5029", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4686, key5029) size=" << scan_result.size() << std::endl;
  }

  // op 18636: put
  db->Put(write_options, "key1330", "value113127");

  // op 18637: delete
  db->Delete(write_options, "key0302");

  // op 18638: put
  db->Put(write_options, "key8459", "value61877");

  // op 18639: put
  db->Put(write_options, "key6269", "value2017");

  // op 18640: get
  status = db->Get(read_options, "key9195", &value);
  if (status.ok()) {
    out << "GET key9195 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18641: get
  status = db->Get(read_options, "key4537", &value);
  if (status.ok()) {
    out << "GET key4537 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18642: delete_range
  status = db->DeleteRange(write_options, "key9820", "key10194");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 18643: get
  status = db->Get(read_options, "key4557", &value);
  if (status.ok()) {
    out << "GET key4557 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18644: put
  db->Put(write_options, "key2848", "value84690");

  // op 18645: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10183", "key11030", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10183, key11030) size=" << scan_result.size() << std::endl;
  }

  // op 18646: delete_range
  status = db->DeleteRange(write_options, "key0507", "key11305");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 18647: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2061", "key2876", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2061, key2876) size=" << scan_result.size() << std::endl;
  }

  // op 18648: get
  status = db->Get(read_options, "key10756", &value);
  if (status.ok()) {
    out << "GET key10756 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18649: get
  status = db->Get(read_options, "key4059", &value);
  if (status.ok()) {
    out << "GET key4059 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18650: get
  status = db->Get(read_options, "key3910", &value);
  if (status.ok()) {
    out << "GET key3910 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18651: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4282", "key4933", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4282, key4933) size=" << scan_result.size() << std::endl;
  }

  // op 18652: put
  db->Put(write_options, "key3298", "value63888");

  // op 18653: get
  status = db->Get(read_options, "key7519", &value);
  if (status.ok()) {
    out << "GET key7519 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18654: delete_range
  status = db->DeleteRange(write_options, "key9518", "key9603");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 18655: put
  db->Put(write_options, "key3341", "value89573");

  // op 18656: put
  db->Put(write_options, "key8577", "value29909");

  // op 18657: put
  db->Put(write_options, "key11710", "value36565");

  // op 18658: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7801", "key11181", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7801, key11181) size=" << scan_result.size() << std::endl;
  }

  // op 18659: delete_range
  status = db->DeleteRange(write_options, "key2669", "key9540");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 18660: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 18661: get
  status = db->Get(read_options, "key4091", &value);
  if (status.ok()) {
    out << "GET key4091 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18662: put
  db->Put(write_options, "key4049", "value68025");

  // op 18663: get
  status = db->Get(read_options, "key3833", &value);
  if (status.ok()) {
    out << "GET key3833 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18664: put
  db->Put(write_options, "key10656", "value84487");

  // op 18665: get
  status = db->Get(read_options, "key9087", &value);
  if (status.ok()) {
    out << "GET key9087 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18666: put
  db->Put(write_options, "key0969", "value37461");

  // op 18667: put
  db->Put(write_options, "key6351", "value73452");

  // op 18668: put
  db->Put(write_options, "key3271", "value17325");

  // op 18669: put
  db->Put(write_options, "key2738", "value85257");

  // op 18670: put
  db->Put(write_options, "key8255", "value71517");

  // op 18671: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 18672: delete
  db->Delete(write_options, "key7091");

  // op 18673: delete_range
  status = db->DeleteRange(write_options, "key4422", "key10719");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 18674: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2111", "key5937", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2111, key5937) size=" << scan_result.size() << std::endl;
  }

  // op 18675: put
  db->Put(write_options, "key6287", "value85220");

  // op 18676: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0347", "key3748", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0347, key3748) size=" << scan_result.size() << std::endl;
  }

  // op 18677: delete
  db->Delete(write_options, "key0956");

  // op 18678: get
  status = db->Get(read_options, "key9397", &value);
  if (status.ok()) {
    out << "GET key9397 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18679: put
  db->Put(write_options, "key4776", "value102666");

  // op 18680: put
  db->Put(write_options, "key0396", "value67603");

  // op 18681: put
  db->Put(write_options, "key8636", "value16249");

  // op 18682: put
  db->Put(write_options, "key5834", "value7858");

  // op 18683: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7182", "key11693", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7182, key11693) size=" << scan_result.size() << std::endl;
  }

  // op 18684: get
  status = db->Get(read_options, "key2803", &value);
  if (status.ok()) {
    out << "GET key2803 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18685: delete_range
  status = db->DeleteRange(write_options, "key1479", "key11707");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 18686: delete_range
  status = db->DeleteRange(write_options, "key6692", "key7892");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 18687: put
  db->Put(write_options, "key8496", "value56997");

  // op 18688: put
  db->Put(write_options, "key5579", "value48413");

  // op 18689: put
  db->Put(write_options, "key8113", "value44102");

  // op 18690: get
  status = db->Get(read_options, "key7078", &value);
  if (status.ok()) {
    out << "GET key7078 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18691: put
  db->Put(write_options, "key7286", "value34647");

  // op 18692: get
  status = db->Get(read_options, "key6213", &value);
  if (status.ok()) {
    out << "GET key6213 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18693: delete
  db->Delete(write_options, "key10516");

  // op 18694: get
  status = db->Get(read_options, "key7885", &value);
  if (status.ok()) {
    out << "GET key7885 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18695: put
  db->Put(write_options, "key7105", "value110540");

  // op 18696: put
  db->Put(write_options, "key6337", "value70610");

  // op 18697: delete_range
  status = db->DeleteRange(write_options, "key3672", "key5045");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 18698: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 18699: get
  status = db->Get(read_options, "key1102", &value);
  if (status.ok()) {
    out << "GET key1102 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18700: put
  db->Put(write_options, "key4782", "value11959");

  // op 18701: put
  db->Put(write_options, "key6665", "value75876");

  // op 18702: get
  status = db->Get(read_options, "key10418", &value);
  if (status.ok()) {
    out << "GET key10418 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18703: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9427", "key10228", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9427, key10228) size=" << scan_result.size() << std::endl;
  }

  // op 18704: put
  db->Put(write_options, "key1691", "value61847");

  // op 18705: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 18706: put
  db->Put(write_options, "key8275", "value27700");

  // op 18707: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10062", "key10987", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10062, key10987) size=" << scan_result.size() << std::endl;
  }

  // op 18708: put
  db->Put(write_options, "key10247", "value70035");

  // op 18709: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3010", "key7318", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3010, key7318) size=" << scan_result.size() << std::endl;
  }

  // op 18710: delete_range
  status = db->DeleteRange(write_options, "key3898", "key6401");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 18711: delete_range
  status = db->DeleteRange(write_options, "key4240", "key10488");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 18712: put
  db->Put(write_options, "key4934", "value90831");

  // op 18713: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0004", "key8114", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0004, key8114) size=" << scan_result.size() << std::endl;
  }

  // op 18714: put
  db->Put(write_options, "key7189", "value59643");

  // op 18715: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 18716: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 18717: delete_range
  status = db->DeleteRange(write_options, "key9179", "key11311");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 18718: get
  status = db->Get(read_options, "key1312", &value);
  if (status.ok()) {
    out << "GET key1312 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18719: put
  db->Put(write_options, "key0242", "value28142");

  // op 18720: delete_range
  status = db->DeleteRange(write_options, "key7943", "key9531");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 18721: get
  status = db->Get(read_options, "key2480", &value);
  if (status.ok()) {
    out << "GET key2480 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18722: put
  db->Put(write_options, "key0652", "value13420");

  // op 18723: put
  db->Put(write_options, "key4063", "value76397");

  // op 18724: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2513", "key10678", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2513, key10678) size=" << scan_result.size() << std::endl;
  }

  // op 18725: get
  status = db->Get(read_options, "key9981", &value);
  if (status.ok()) {
    out << "GET key9981 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18726: put
  db->Put(write_options, "key3554", "value100876");

  // op 18727: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3353", "key5534", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3353, key5534) size=" << scan_result.size() << std::endl;
  }

  // op 18728: put
  db->Put(write_options, "key9736", "value94165");

  // op 18729: put
  db->Put(write_options, "key9268", "value72791");

  // op 18730: put
  db->Put(write_options, "key5855", "value36088");

  // op 18731: put
  db->Put(write_options, "key6905", "value66184");

  // op 18732: delete_range
  status = db->DeleteRange(write_options, "key1014", "key3075");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 18733: get
  status = db->Get(read_options, "key10902", &value);
  if (status.ok()) {
    out << "GET key10902 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18734: put
  db->Put(write_options, "key7470", "value38229");

  // op 18735: delete_range
  status = db->DeleteRange(write_options, "key8403", "key8902");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 18736: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4279", "key9793", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4279, key9793) size=" << scan_result.size() << std::endl;
  }

  // op 18737: delete_range
  status = db->DeleteRange(write_options, "key4542", "key5079");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 18738: put
  db->Put(write_options, "key0536", "value11033");

  // op 18739: put
  db->Put(write_options, "key9790", "value107081");

  // op 18740: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1224", "key9876", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1224, key9876) size=" << scan_result.size() << std::endl;
  }

  // op 18741: get
  status = db->Get(read_options, "key2500", &value);
  if (status.ok()) {
    out << "GET key2500 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18742: put
  db->Put(write_options, "key3109", "value56907");

  // op 18743: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4551", "key4571", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4551, key4571) size=" << scan_result.size() << std::endl;
  }

  // op 18744: put
  db->Put(write_options, "key6465", "value14577");

  // op 18745: get
  status = db->Get(read_options, "key8816", &value);
  if (status.ok()) {
    out << "GET key8816 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18746: delete_range
  status = db->DeleteRange(write_options, "key6670", "key7452");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 18747: get
  status = db->Get(read_options, "key0091", &value);
  if (status.ok()) {
    out << "GET key0091 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18748: delete_range
  status = db->DeleteRange(write_options, "key4740", "key9451");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 18749: delete
  db->Delete(write_options, "key2527");

  // op 18750: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6456", "key7594", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6456, key7594) size=" << scan_result.size() << std::endl;
  }

  // op 18751: delete
  db->Delete(write_options, "key8114");

  // op 18752: get
  status = db->Get(read_options, "key5483", &value);
  if (status.ok()) {
    out << "GET key5483 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18753: get
  status = db->Get(read_options, "key10196", &value);
  if (status.ok()) {
    out << "GET key10196 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18754: put
  db->Put(write_options, "key10983", "value59206");

  // op 18755: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3897", "key7942", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3897, key7942) size=" << scan_result.size() << std::endl;
  }

  // op 18756: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4535", "key5297", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4535, key5297) size=" << scan_result.size() << std::endl;
  }

  // op 18757: put
  db->Put(write_options, "key9296", "value95699");

  // op 18758: put
  db->Put(write_options, "key4220", "value60053");

  // op 18759: get
  status = db->Get(read_options, "key9903", &value);
  if (status.ok()) {
    out << "GET key9903 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18760: delete_range
  status = db->DeleteRange(write_options, "key2354", "key4897");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 18761: get
  status = db->Get(read_options, "key6133", &value);
  if (status.ok()) {
    out << "GET key6133 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18762: get
  status = db->Get(read_options, "key3709", &value);
  if (status.ok()) {
    out << "GET key3709 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18763: delete
  db->Delete(write_options, "key7360");

  // op 18764: get
  status = db->Get(read_options, "key9272", &value);
  if (status.ok()) {
    out << "GET key9272 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18765: put
  db->Put(write_options, "key1645", "value77652");

  // op 18766: put
  db->Put(write_options, "key0137", "value101173");

  // op 18767: put
  db->Put(write_options, "key3419", "value7612");

  // op 18768: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1052", "key2354", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1052, key2354) size=" << scan_result.size() << std::endl;
  }

  // op 18769: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4595", "key7584", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4595, key7584) size=" << scan_result.size() << std::endl;
  }

  // op 18770: get
  status = db->Get(read_options, "key4374", &value);
  if (status.ok()) {
    out << "GET key4374 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18771: put
  db->Put(write_options, "key7490", "value72718");

  // op 18772: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2722", "key6023", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2722, key6023) size=" << scan_result.size() << std::endl;
  }

  // op 18773: put
  db->Put(write_options, "key2451", "value23520");

  // op 18774: get
  status = db->Get(read_options, "key10923", &value);
  if (status.ok()) {
    out << "GET key10923 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18775: put
  db->Put(write_options, "key0078", "value75412");

  // op 18776: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1129", "key4868", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1129, key4868) size=" << scan_result.size() << std::endl;
  }

  // op 18777: put
  db->Put(write_options, "key7001", "value28895");

  // op 18778: get
  status = db->Get(read_options, "key1348", &value);
  if (status.ok()) {
    out << "GET key1348 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18779: delete_range
  status = db->DeleteRange(write_options, "key2951", "key11283");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 18780: delete
  db->Delete(write_options, "key7751");

  // op 18781: delete
  db->Delete(write_options, "key1360");

  // op 18782: delete
  db->Delete(write_options, "key3405");

  // op 18783: put
  db->Put(write_options, "key4682", "value31899");

  // op 18784: delete
  db->Delete(write_options, "key7175");

  // op 18785: put
  db->Put(write_options, "key10521", "value89164");

  // op 18786: put
  db->Put(write_options, "key11119", "value102150");

  // op 18787: get
  status = db->Get(read_options, "key8252", &value);
  if (status.ok()) {
    out << "GET key8252 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18788: get
  status = db->Get(read_options, "key9435", &value);
  if (status.ok()) {
    out << "GET key9435 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18789: put
  db->Put(write_options, "key3986", "value12881");

  // op 18790: delete_range
  status = db->DeleteRange(write_options, "key0579", "key3366");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 18791: put
  db->Put(write_options, "key7768", "value73804");

  // op 18792: delete_range
  status = db->DeleteRange(write_options, "key2821", "key7439");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 18793: delete
  db->Delete(write_options, "key2061");

  // op 18794: delete_range
  status = db->DeleteRange(write_options, "key4418", "key6355");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 18795: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1584", "key5064", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1584, key5064) size=" << scan_result.size() << std::endl;
  }

  // op 18796: delete
  db->Delete(write_options, "key9553");

  // op 18797: put
  db->Put(write_options, "key1148", "value47499");

  // op 18798: delete_range
  status = db->DeleteRange(write_options, "key8327", "key8903");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 18799: put
  db->Put(write_options, "key8974", "value9189");

  // op 18800: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1459", "key1953", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1459, key1953) size=" << scan_result.size() << std::endl;
  }

  // op 18801: put
  db->Put(write_options, "key11102", "value90258");

  // op 18802: put
  db->Put(write_options, "key3984", "value5647");

  // op 18803: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6762", "key9804", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6762, key9804) size=" << scan_result.size() << std::endl;
  }

  // op 18804: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 18805: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5752", "key9371", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5752, key9371) size=" << scan_result.size() << std::endl;
  }

  // op 18806: put
  db->Put(write_options, "key7385", "value3991");

  // op 18807: get
  status = db->Get(read_options, "key7691", &value);
  if (status.ok()) {
    out << "GET key7691 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18808: put
  db->Put(write_options, "key6763", "value55190");

  // op 18809: get
  status = db->Get(read_options, "key6618", &value);
  if (status.ok()) {
    out << "GET key6618 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18810: put
  db->Put(write_options, "key6327", "value36430");

  // op 18811: get
  status = db->Get(read_options, "key6659", &value);
  if (status.ok()) {
    out << "GET key6659 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18812: delete
  db->Delete(write_options, "key5858");

  // op 18813: delete
  db->Delete(write_options, "key8178");

  // op 18814: put
  db->Put(write_options, "key1752", "value86008");

  // op 18815: put
  db->Put(write_options, "key6348", "value3145");

  // op 18816: put
  db->Put(write_options, "key3097", "value100553");

  // op 18817: put
  db->Put(write_options, "key0547", "value66789");

  // op 18818: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2849", "key2936", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2849, key2936) size=" << scan_result.size() << std::endl;
  }

  // op 18819: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7514", "key9190", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7514, key9190) size=" << scan_result.size() << std::endl;
  }

  // op 18820: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 18821: delete_range
  status = db->DeleteRange(write_options, "key2464", "key3378");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 18822: put
  db->Put(write_options, "key6782", "value17372");

  // op 18823: put
  db->Put(write_options, "key7711", "value35129");

  // op 18824: put
  db->Put(write_options, "key8000", "value103943");

  // op 18825: put
  db->Put(write_options, "key2581", "value47336");

  // op 18826: delete
  db->Delete(write_options, "key7588");

  // op 18827: delete_range
  status = db->DeleteRange(write_options, "key3629", "key4586");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 18828: put
  db->Put(write_options, "key0090", "value88364");

  // op 18829: put
  db->Put(write_options, "key6654", "value83146");

  // op 18830: delete_range
  status = db->DeleteRange(write_options, "key9308", "key10176");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 18831: put
  db->Put(write_options, "key7267", "value106739");

  // op 18832: put
  db->Put(write_options, "key0373", "value48826");

  // op 18833: put
  db->Put(write_options, "key9807", "value76457");

  // op 18834: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4434", "key8260", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4434, key8260) size=" << scan_result.size() << std::endl;
  }

  // op 18835: get
  status = db->Get(read_options, "key11371", &value);
  if (status.ok()) {
    out << "GET key11371 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18836: delete_range
  status = db->DeleteRange(write_options, "key4311", "key8361");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 18837: put
  db->Put(write_options, "key9342", "value78474");

  // op 18838: put
  db->Put(write_options, "key10532", "value24644");

  // op 18839: put
  db->Put(write_options, "key3589", "value60185");

  // op 18840: delete_range
  status = db->DeleteRange(write_options, "key2520", "key7777");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 18841: put
  db->Put(write_options, "key1400", "value17164");

  // op 18842: put
  db->Put(write_options, "key4947", "value55933");

  // op 18843: get
  status = db->Get(read_options, "key10042", &value);
  if (status.ok()) {
    out << "GET key10042 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18844: get
  status = db->Get(read_options, "key8620", &value);
  if (status.ok()) {
    out << "GET key8620 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18845: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1602", "key10195", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1602, key10195) size=" << scan_result.size() << std::endl;
  }

  // op 18846: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2808", "key5794", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2808, key5794) size=" << scan_result.size() << std::endl;
  }

  // op 18847: delete
  db->Delete(write_options, "key8063");

  // op 18848: delete_range
  status = db->DeleteRange(write_options, "key4325", "key11454");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 18849: get
  status = db->Get(read_options, "key10047", &value);
  if (status.ok()) {
    out << "GET key10047 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18850: get
  status = db->Get(read_options, "key6357", &value);
  if (status.ok()) {
    out << "GET key6357 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18851: put
  db->Put(write_options, "key1374", "value112059");

  // op 18852: delete_range
  status = db->DeleteRange(write_options, "key6162", "key9742");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 18853: delete
  db->Delete(write_options, "key5643");

  // op 18854: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 18855: put
  db->Put(write_options, "key5953", "value3778");

  // op 18856: put
  db->Put(write_options, "key9840", "value105338");

  // op 18857: delete
  db->Delete(write_options, "key2511");

  // op 18858: put
  db->Put(write_options, "key5847", "value88231");

  // op 18859: put
  db->Put(write_options, "key3827", "value85271");

  // op 18860: get
  status = db->Get(read_options, "key2448", &value);
  if (status.ok()) {
    out << "GET key2448 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18861: delete_range
  status = db->DeleteRange(write_options, "key0635", "key9456");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 18862: put
  db->Put(write_options, "key1143", "value116080");

  // op 18863: delete
  db->Delete(write_options, "key4789");

  // op 18864: delete_range
  status = db->DeleteRange(write_options, "key0453", "key7003");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 18865: put
  db->Put(write_options, "key5744", "value2194");

  // op 18866: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3543", "key6896", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3543, key6896) size=" << scan_result.size() << std::endl;
  }

  // op 18867: delete_range
  status = db->DeleteRange(write_options, "key2472", "key3265");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 18868: delete
  db->Delete(write_options, "key3296");

  // op 18869: get
  status = db->Get(read_options, "key10602", &value);
  if (status.ok()) {
    out << "GET key10602 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18870: put
  db->Put(write_options, "key3045", "value62385");

  // op 18871: get
  status = db->Get(read_options, "key7256", &value);
  if (status.ok()) {
    out << "GET key7256 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18872: put
  db->Put(write_options, "key4351", "value30768");

  // op 18873: put
  db->Put(write_options, "key0327", "value117642");

  // op 18874: put
  db->Put(write_options, "key0714", "value7308");

  // op 18875: get
  status = db->Get(read_options, "key1303", &value);
  if (status.ok()) {
    out << "GET key1303 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18876: put
  db->Put(write_options, "key1377", "value41314");

  // op 18877: put
  db->Put(write_options, "key10612", "value35649");

  // op 18878: put
  db->Put(write_options, "key2281", "value11606");

  // op 18879: delete
  db->Delete(write_options, "key5199");

  // op 18880: put
  db->Put(write_options, "key0477", "value93649");

  // op 18881: get
  status = db->Get(read_options, "key4426", &value);
  if (status.ok()) {
    out << "GET key4426 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18882: put
  db->Put(write_options, "key6858", "value12627");

  // op 18883: put
  db->Put(write_options, "key1439", "value96478");

  // op 18884: put
  db->Put(write_options, "key8145", "value5395");

  // op 18885: put
  db->Put(write_options, "key5102", "value35944");

  // op 18886: put
  db->Put(write_options, "key5738", "value3654");

  // op 18887: delete
  db->Delete(write_options, "key2580");

  // op 18888: put
  db->Put(write_options, "key10856", "value79052");

  // op 18889: put
  db->Put(write_options, "key6706", "value39363");

  // op 18890: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3073", "key9683", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3073, key9683) size=" << scan_result.size() << std::endl;
  }

  // op 18891: put
  db->Put(write_options, "key2251", "value115103");

  // op 18892: delete
  db->Delete(write_options, "key4667");

  // op 18893: put
  db->Put(write_options, "key0655", "value15826");

  // op 18894: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4004", "key10254", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4004, key10254) size=" << scan_result.size() << std::endl;
  }

  // op 18895: put
  db->Put(write_options, "key1246", "value104393");

  // op 18896: delete
  db->Delete(write_options, "key5050");

  // op 18897: delete_range
  status = db->DeleteRange(write_options, "key6422", "key11158");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 18898: put
  db->Put(write_options, "key1600", "value32135");

  // op 18899: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1339", "key2916", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1339, key2916) size=" << scan_result.size() << std::endl;
  }

  // op 18900: get
  status = db->Get(read_options, "key4898", &value);
  if (status.ok()) {
    out << "GET key4898 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18901: put
  db->Put(write_options, "key1649", "value40457");

  // op 18902: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1878", "key4064", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1878, key4064) size=" << scan_result.size() << std::endl;
  }

  // op 18903: put
  db->Put(write_options, "key9232", "value56068");

  // op 18904: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 18905: get
  status = db->Get(read_options, "key8292", &value);
  if (status.ok()) {
    out << "GET key8292 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18906: put
  db->Put(write_options, "key6633", "value105071");

  // op 18907: put
  db->Put(write_options, "key0326", "value5882");

  // op 18908: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 18909: put
  db->Put(write_options, "key1360", "value27480");

  // op 18910: delete
  db->Delete(write_options, "key9870");

  // op 18911: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0210", "key1056", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0210, key1056) size=" << scan_result.size() << std::endl;
  }

  // op 18912: get
  status = db->Get(read_options, "key7162", &value);
  if (status.ok()) {
    out << "GET key7162 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18913: put
  db->Put(write_options, "key3343", "value70705");

  // op 18914: delete_range
  status = db->DeleteRange(write_options, "key0131", "key6701");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 18915: delete_range
  status = db->DeleteRange(write_options, "key3834", "key10418");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 18916: put
  db->Put(write_options, "key2377", "value19098");

  // op 18917: delete
  db->Delete(write_options, "key6761");

  // op 18918: get
  status = db->Get(read_options, "key1904", &value);
  if (status.ok()) {
    out << "GET key1904 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18919: delete_range
  status = db->DeleteRange(write_options, "key2161", "key9076");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 18920: delete_range
  status = db->DeleteRange(write_options, "key3785", "key10190");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 18921: put
  db->Put(write_options, "key11395", "value70821");

  // op 18922: put
  db->Put(write_options, "key9526", "value27259");

  // op 18923: put
  db->Put(write_options, "key7833", "value101113");

  // op 18924: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7419", "key10273", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7419, key10273) size=" << scan_result.size() << std::endl;
  }

  // op 18925: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0003", "key6684", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0003, key6684) size=" << scan_result.size() << std::endl;
  }

  // op 18926: delete_range
  status = db->DeleteRange(write_options, "key5781", "key8413");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 18927: get
  status = db->Get(read_options, "key2737", &value);
  if (status.ok()) {
    out << "GET key2737 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18928: delete
  db->Delete(write_options, "key10324");

  // op 18929: delete
  db->Delete(write_options, "key1532");

  // op 18930: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0693", "key10745", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0693, key10745) size=" << scan_result.size() << std::endl;
  }

  // op 18931: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0667", "key9681", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0667, key9681) size=" << scan_result.size() << std::endl;
  }

  // op 18932: get
  status = db->Get(read_options, "key0561", &value);
  if (status.ok()) {
    out << "GET key0561 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18933: put
  db->Put(write_options, "key0410", "value44020");

  // op 18934: get
  status = db->Get(read_options, "key5838", &value);
  if (status.ok()) {
    out << "GET key5838 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18935: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5346", "key7305", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5346, key7305) size=" << scan_result.size() << std::endl;
  }

  // op 18936: put
  db->Put(write_options, "key2382", "value8237");

  // op 18937: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10890", "key11119", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10890, key11119) size=" << scan_result.size() << std::endl;
  }

  // op 18938: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 18939: get
  status = db->Get(read_options, "key3405", &value);
  if (status.ok()) {
    out << "GET key3405 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18940: get
  status = db->Get(read_options, "key4673", &value);
  if (status.ok()) {
    out << "GET key4673 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18941: put
  db->Put(write_options, "key2000", "value64466");

  // op 18942: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2376", "key3977", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2376, key3977) size=" << scan_result.size() << std::endl;
  }

  // op 18943: get
  status = db->Get(read_options, "key7857", &value);
  if (status.ok()) {
    out << "GET key7857 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18944: delete
  db->Delete(write_options, "key6828");

  // op 18945: put
  db->Put(write_options, "key8851", "value100965");

  // op 18946: put
  db->Put(write_options, "key1410", "value15181");

  // op 18947: put
  db->Put(write_options, "key10863", "value12277");

  // op 18948: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6577", "key11121", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6577, key11121) size=" << scan_result.size() << std::endl;
  }

  // op 18949: put
  db->Put(write_options, "key4557", "value2218");

  // op 18950: delete_range
  status = db->DeleteRange(write_options, "key6338", "key10377");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 18951: get
  status = db->Get(read_options, "key7746", &value);
  if (status.ok()) {
    out << "GET key7746 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18952: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 18953: put
  db->Put(write_options, "key11287", "value99426");

  // op 18954: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 18955: get
  status = db->Get(read_options, "key10800", &value);
  if (status.ok()) {
    out << "GET key10800 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18956: put
  db->Put(write_options, "key3053", "value1915");

  // op 18957: delete
  db->Delete(write_options, "key9007");

  // op 18958: put
  db->Put(write_options, "key4377", "value33381");

  // op 18959: put
  db->Put(write_options, "key6816", "value59419");

  // op 18960: delete
  db->Delete(write_options, "key5994");

  // op 18961: put
  db->Put(write_options, "key4200", "value0582");

  // op 18962: get
  status = db->Get(read_options, "key3950", &value);
  if (status.ok()) {
    out << "GET key3950 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18963: get
  status = db->Get(read_options, "key7598", &value);
  if (status.ok()) {
    out << "GET key7598 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18964: put
  db->Put(write_options, "key0307", "value98853");

  // op 18965: get
  status = db->Get(read_options, "key7159", &value);
  if (status.ok()) {
    out << "GET key7159 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18966: get
  status = db->Get(read_options, "key9680", &value);
  if (status.ok()) {
    out << "GET key9680 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18967: put
  db->Put(write_options, "key10672", "value101427");

  // op 18968: put
  db->Put(write_options, "key2869", "value71375");

  // op 18969: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 18970: put
  db->Put(write_options, "key1519", "value50316");

  // op 18971: put
  db->Put(write_options, "key10352", "value66012");

  // op 18972: put
  db->Put(write_options, "key4936", "value23909");

  // op 18973: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 18974: put
  db->Put(write_options, "key3671", "value46526");

  // op 18975: put
  db->Put(write_options, "key11509", "value22369");

  // op 18976: put
  db->Put(write_options, "key5723", "value5716");

  // op 18977: put
  db->Put(write_options, "key8849", "value66809");

  // op 18978: delete
  db->Delete(write_options, "key10275");

  // op 18979: put
  db->Put(write_options, "key7010", "value105580");

  // op 18980: put
  db->Put(write_options, "key2860", "value111477");

  // op 18981: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8172", "key11558", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8172, key11558) size=" << scan_result.size() << std::endl;
  }

  // op 18982: delete_range
  status = db->DeleteRange(write_options, "key2563", "key3255");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 18983: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7693", "key8473", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7693, key8473) size=" << scan_result.size() << std::endl;
  }

  // op 18984: get
  status = db->Get(read_options, "key10582", &value);
  if (status.ok()) {
    out << "GET key10582 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18985: put
  db->Put(write_options, "key10536", "value111755");

  // op 18986: delete_range
  status = db->DeleteRange(write_options, "key2199", "key7298");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 18987: get
  status = db->Get(read_options, "key6937", &value);
  if (status.ok()) {
    out << "GET key6937 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18988: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2987", "key11648", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2987, key11648) size=" << scan_result.size() << std::endl;
  }

  // op 18989: delete_range
  status = db->DeleteRange(write_options, "key6323", "key7875");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 18990: put
  db->Put(write_options, "key1130", "value68239");

  // op 18991: get
  status = db->Get(read_options, "key8077", &value);
  if (status.ok()) {
    out << "GET key8077 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 18992: delete
  db->Delete(write_options, "key11357");

  // op 18993: put
  db->Put(write_options, "key11195", "value58488");

  // op 18994: delete_range
  status = db->DeleteRange(write_options, "key1206", "key9351");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 18995: put
  db->Put(write_options, "key7183", "value62068");

  // op 18996: delete_range
  status = db->DeleteRange(write_options, "key5224", "key6494");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 18997: put
  db->Put(write_options, "key6623", "value114578");

  // op 18998: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2296", "key4403", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2296, key4403) size=" << scan_result.size() << std::endl;
  }

  // op 18999: put
  db->Put(write_options, "key6463", "value106694");

  // op 19000: put
  db->Put(write_options, "key1701", "value77469");

  // op 19001: delete
  db->Delete(write_options, "key9950");

  // op 19002: put
  db->Put(write_options, "key6404", "value112446");

  // op 19003: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 19004: get
  status = db->Get(read_options, "key3858", &value);
  if (status.ok()) {
    out << "GET key3858 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19005: get
  status = db->Get(read_options, "key6877", &value);
  if (status.ok()) {
    out << "GET key6877 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19006: delete_range
  status = db->DeleteRange(write_options, "key0399", "key4422");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 19007: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3348", "key9614", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3348, key9614) size=" << scan_result.size() << std::endl;
  }

  // op 19008: delete_range
  status = db->DeleteRange(write_options, "key1260", "key9717");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 19009: put
  db->Put(write_options, "key2636", "value99450");

  // op 19010: delete
  db->Delete(write_options, "key11046");

  // op 19011: delete_range
  status = db->DeleteRange(write_options, "key1725", "key7415");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 19012: get
  status = db->Get(read_options, "key5060", &value);
  if (status.ok()) {
    out << "GET key5060 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19013: delete
  db->Delete(write_options, "key6767");

  // op 19014: delete_range
  status = db->DeleteRange(write_options, "key2327", "key5371");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 19015: delete
  db->Delete(write_options, "key0283");

  // op 19016: put
  db->Put(write_options, "key4549", "value3680");

  // op 19017: put
  db->Put(write_options, "key7887", "value23745");

  // op 19018: put
  db->Put(write_options, "key9489", "value24106");

  // op 19019: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4523", "key8376", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4523, key8376) size=" << scan_result.size() << std::endl;
  }

  // op 19020: get
  status = db->Get(read_options, "key0908", &value);
  if (status.ok()) {
    out << "GET key0908 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19021: delete
  db->Delete(write_options, "key10401");

  // op 19022: delete_range
  status = db->DeleteRange(write_options, "key4358", "key8351");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 19023: delete_range
  status = db->DeleteRange(write_options, "key1127", "key11193");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 19024: put
  db->Put(write_options, "key9189", "value92403");

  // op 19025: put
  db->Put(write_options, "key0807", "value66173");

  // op 19026: put
  db->Put(write_options, "key9919", "value65066");

  // op 19027: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 19028: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0905", "key3305", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0905, key3305) size=" << scan_result.size() << std::endl;
  }

  // op 19029: put
  db->Put(write_options, "key9148", "value114163");

  // op 19030: put
  db->Put(write_options, "key6571", "value71022");

  // op 19031: get
  status = db->Get(read_options, "key0149", &value);
  if (status.ok()) {
    out << "GET key0149 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19032: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6077", "key8384", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6077, key8384) size=" << scan_result.size() << std::endl;
  }

  // op 19033: get
  status = db->Get(read_options, "key0441", &value);
  if (status.ok()) {
    out << "GET key0441 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19034: delete_range
  status = db->DeleteRange(write_options, "key5663", "key5707");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 19035: delete
  db->Delete(write_options, "key11055");

  // op 19036: delete
  db->Delete(write_options, "key0962");

  // op 19037: put
  db->Put(write_options, "key5970", "value0302");

  // op 19038: put
  db->Put(write_options, "key7428", "value52990");

  // op 19039: get
  status = db->Get(read_options, "key2850", &value);
  if (status.ok()) {
    out << "GET key2850 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19040: put
  db->Put(write_options, "key1528", "value8001");

  // op 19041: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3831", "key10797", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3831, key10797) size=" << scan_result.size() << std::endl;
  }

  // op 19042: get
  status = db->Get(read_options, "key8161", &value);
  if (status.ok()) {
    out << "GET key8161 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19043: get
  status = db->Get(read_options, "key7053", &value);
  if (status.ok()) {
    out << "GET key7053 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19044: put
  db->Put(write_options, "key8084", "value109389");

  // op 19045: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0733", "key11563", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0733, key11563) size=" << scan_result.size() << std::endl;
  }

  // op 19046: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0518", "key4408", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0518, key4408) size=" << scan_result.size() << std::endl;
  }

  // op 19047: put
  db->Put(write_options, "key8915", "value50503");

  // op 19048: delete
  db->Delete(write_options, "key3058");

  // op 19049: put
  db->Put(write_options, "key4643", "value12558");

  // op 19050: delete_range
  status = db->DeleteRange(write_options, "key0624", "key7761");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 19051: put
  db->Put(write_options, "key3840", "value102986");

  // op 19052: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 19053: put
  db->Put(write_options, "key6548", "value61473");

  // op 19054: put
  db->Put(write_options, "key5490", "value73759");

  // op 19055: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6888", "key9644", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6888, key9644) size=" << scan_result.size() << std::endl;
  }

  // op 19056: get
  status = db->Get(read_options, "key11386", &value);
  if (status.ok()) {
    out << "GET key11386 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19057: put
  db->Put(write_options, "key5207", "value57264");

  // op 19058: delete
  db->Delete(write_options, "key0375");

  // op 19059: put
  db->Put(write_options, "key6410", "value52874");

  // op 19060: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2172", "key4620", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2172, key4620) size=" << scan_result.size() << std::endl;
  }

  // op 19061: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 19062: put
  db->Put(write_options, "key1328", "value59461");

  // op 19063: put
  db->Put(write_options, "key4542", "value50949");

  // op 19064: put
  db->Put(write_options, "key2042", "value55292");

  // op 19065: put
  db->Put(write_options, "key0075", "value99520");

  // op 19066: put
  db->Put(write_options, "key7817", "value83298");

  // op 19067: get
  status = db->Get(read_options, "key2517", &value);
  if (status.ok()) {
    out << "GET key2517 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19068: get
  status = db->Get(read_options, "key2777", &value);
  if (status.ok()) {
    out << "GET key2777 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19069: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 19070: get
  status = db->Get(read_options, "key2046", &value);
  if (status.ok()) {
    out << "GET key2046 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19071: delete_range
  status = db->DeleteRange(write_options, "key0544", "key1694");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 19072: put
  db->Put(write_options, "key7951", "value12420");

  // op 19073: put
  db->Put(write_options, "key6328", "value93239");

  // op 19074: put
  db->Put(write_options, "key8342", "value2595");

  // op 19075: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3491", "key8130", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3491, key8130) size=" << scan_result.size() << std::endl;
  }

  // op 19076: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 19077: put
  db->Put(write_options, "key1860", "value36519");

  // op 19078: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10195", "key11496", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10195, key11496) size=" << scan_result.size() << std::endl;
  }

  // op 19079: delete_range
  status = db->DeleteRange(write_options, "key2401", "key4938");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 19080: delete
  db->Delete(write_options, "key0338");

  // op 19081: put
  db->Put(write_options, "key0921", "value44082");

  // op 19082: put
  db->Put(write_options, "key0667", "value37479");

  // op 19083: delete
  db->Delete(write_options, "key3606");

  // op 19084: delete_range
  status = db->DeleteRange(write_options, "key7703", "key11257");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 19085: delete
  db->Delete(write_options, "key3028");

  // op 19086: put
  db->Put(write_options, "key1505", "value58120");

  // op 19087: delete_range
  status = db->DeleteRange(write_options, "key3960", "key11309");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 19088: get
  status = db->Get(read_options, "key4425", &value);
  if (status.ok()) {
    out << "GET key4425 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19089: put
  db->Put(write_options, "key0390", "value62427");

  // op 19090: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 19091: put
  db->Put(write_options, "key7186", "value77467");

  // op 19092: put
  db->Put(write_options, "key9227", "value5429");

  // op 19093: get
  status = db->Get(read_options, "key1175", &value);
  if (status.ok()) {
    out << "GET key1175 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19094: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2390", "key3978", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2390, key3978) size=" << scan_result.size() << std::endl;
  }

  // op 19095: delete_range
  status = db->DeleteRange(write_options, "key1061", "key7908");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 19096: get
  status = db->Get(read_options, "key3607", &value);
  if (status.ok()) {
    out << "GET key3607 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19097: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3918", "key4424", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3918, key4424) size=" << scan_result.size() << std::endl;
  }

  // op 19098: delete_range
  status = db->DeleteRange(write_options, "key6246", "key8771");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 19099: put
  db->Put(write_options, "key2595", "value60820");

  // op 19100: delete
  db->Delete(write_options, "key10560");

  // op 19101: put
  db->Put(write_options, "key4485", "value15695");

  // op 19102: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7432", "key7849", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7432, key7849) size=" << scan_result.size() << std::endl;
  }

  // op 19103: put
  db->Put(write_options, "key3462", "value32569");

  // op 19104: delete_range
  status = db->DeleteRange(write_options, "key0305", "key10720");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 19105: put
  db->Put(write_options, "key3003", "value102498");

  // op 19106: delete_range
  status = db->DeleteRange(write_options, "key2122", "key9059");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 19107: put
  db->Put(write_options, "key2261", "value111024");

  // op 19108: delete
  db->Delete(write_options, "key8130");

  // op 19109: put
  db->Put(write_options, "key4088", "value115875");

  // op 19110: put
  db->Put(write_options, "key0148", "value118107");

  // op 19111: put
  db->Put(write_options, "key2289", "value93010");

  // op 19112: delete
  db->Delete(write_options, "key1629");

  // op 19113: put
  db->Put(write_options, "key4506", "value71110");

  // op 19114: get
  status = db->Get(read_options, "key2549", &value);
  if (status.ok()) {
    out << "GET key2549 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19115: get
  status = db->Get(read_options, "key0441", &value);
  if (status.ok()) {
    out << "GET key0441 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19116: delete
  db->Delete(write_options, "key4517");

  // op 19117: put
  db->Put(write_options, "key10240", "value69793");

  // op 19118: get
  status = db->Get(read_options, "key11364", &value);
  if (status.ok()) {
    out << "GET key11364 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19119: put
  db->Put(write_options, "key0212", "value59402");

  // op 19120: get
  status = db->Get(read_options, "key9835", &value);
  if (status.ok()) {
    out << "GET key9835 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19121: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 19122: get
  status = db->Get(read_options, "key6068", &value);
  if (status.ok()) {
    out << "GET key6068 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19123: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7882", "key11299", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7882, key11299) size=" << scan_result.size() << std::endl;
  }

  // op 19124: delete_range
  status = db->DeleteRange(write_options, "key0641", "key2279");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 19125: delete
  db->Delete(write_options, "key0298");

  // op 19126: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0540", "key0823", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0540, key0823) size=" << scan_result.size() << std::endl;
  }

  // op 19127: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4197", "key6489", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4197, key6489) size=" << scan_result.size() << std::endl;
  }

  // op 19128: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 19129: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6985", "key10634", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6985, key10634) size=" << scan_result.size() << std::endl;
  }

  // op 19130: get
  status = db->Get(read_options, "key0665", &value);
  if (status.ok()) {
    out << "GET key0665 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19131: delete_range
  status = db->DeleteRange(write_options, "key7229", "key9132");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 19132: put
  db->Put(write_options, "key7366", "value104397");

  // op 19133: put
  db->Put(write_options, "key8524", "value67114");

  // op 19134: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0199", "key4662", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0199, key4662) size=" << scan_result.size() << std::endl;
  }

  // op 19135: put
  db->Put(write_options, "key4006", "value6637");

  // op 19136: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6591", "key6905", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6591, key6905) size=" << scan_result.size() << std::endl;
  }

  // op 19137: put
  db->Put(write_options, "key6839", "value3738");

  // op 19138: put
  db->Put(write_options, "key0740", "value76971");

  // op 19139: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2342", "key8742", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2342, key8742) size=" << scan_result.size() << std::endl;
  }

  // op 19140: get
  status = db->Get(read_options, "key1616", &value);
  if (status.ok()) {
    out << "GET key1616 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19141: put
  db->Put(write_options, "key8132", "value4685");

  // op 19142: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2640", "key5853", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2640, key5853) size=" << scan_result.size() << std::endl;
  }

  // op 19143: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6481", "key7790", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6481, key7790) size=" << scan_result.size() << std::endl;
  }

  // op 19144: get
  status = db->Get(read_options, "key9546", &value);
  if (status.ok()) {
    out << "GET key9546 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19145: get
  status = db->Get(read_options, "key8871", &value);
  if (status.ok()) {
    out << "GET key8871 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19146: put
  db->Put(write_options, "key0098", "value109328");

  // op 19147: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2799", "key7844", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2799, key7844) size=" << scan_result.size() << std::endl;
  }

  // op 19148: put
  db->Put(write_options, "key10939", "value86978");

  // op 19149: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4692", "key6999", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4692, key6999) size=" << scan_result.size() << std::endl;
  }

  // op 19150: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4370", "key7835", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4370, key7835) size=" << scan_result.size() << std::endl;
  }

  // op 19151: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 19152: put
  db->Put(write_options, "key9828", "value75109");

  // op 19153: delete_range
  status = db->DeleteRange(write_options, "key4128", "key7182");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 19154: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2835", "key11148", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2835, key11148) size=" << scan_result.size() << std::endl;
  }

  // op 19155: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 19156: delete_range
  status = db->DeleteRange(write_options, "key0151", "key5125");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 19157: put
  db->Put(write_options, "key1876", "value6787");

  // op 19158: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 19159: delete_range
  status = db->DeleteRange(write_options, "key3627", "key4130");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 19160: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7181", "key9842", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7181, key9842) size=" << scan_result.size() << std::endl;
  }

  // op 19161: get
  status = db->Get(read_options, "key8282", &value);
  if (status.ok()) {
    out << "GET key8282 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19162: get
  status = db->Get(read_options, "key2672", &value);
  if (status.ok()) {
    out << "GET key2672 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19163: delete
  db->Delete(write_options, "key6120");

  // op 19164: put
  db->Put(write_options, "key7329", "value112518");

  // op 19165: get
  status = db->Get(read_options, "key2031", &value);
  if (status.ok()) {
    out << "GET key2031 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19166: get
  status = db->Get(read_options, "key1991", &value);
  if (status.ok()) {
    out << "GET key1991 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19167: put
  db->Put(write_options, "key6284", "value85815");

  // op 19168: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 19169: put
  db->Put(write_options, "key4657", "value99101");

  // op 19170: put
  db->Put(write_options, "key0456", "value48748");

  // op 19171: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6080", "key7458", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6080, key7458) size=" << scan_result.size() << std::endl;
  }

  // op 19172: put
  db->Put(write_options, "key3277", "value93215");

  // op 19173: get
  status = db->Get(read_options, "key0067", &value);
  if (status.ok()) {
    out << "GET key0067 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19174: delete_range
  status = db->DeleteRange(write_options, "key0554", "key2648");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 19175: put
  db->Put(write_options, "key11030", "value19001");

  // op 19176: get
  status = db->Get(read_options, "key3980", &value);
  if (status.ok()) {
    out << "GET key3980 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19177: put
  db->Put(write_options, "key10368", "value26442");

  // op 19178: delete_range
  status = db->DeleteRange(write_options, "key1344", "key8553");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 19179: put
  db->Put(write_options, "key3062", "value71005");

  // op 19180: put
  db->Put(write_options, "key5280", "value40000");

  // op 19181: get
  status = db->Get(read_options, "key9739", &value);
  if (status.ok()) {
    out << "GET key9739 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19182: put
  db->Put(write_options, "key8419", "value85822");

  // op 19183: put
  db->Put(write_options, "key10921", "value63567");

  // op 19184: put
  db->Put(write_options, "key8204", "value18169");

  // op 19185: put
  db->Put(write_options, "key3498", "value82065");

  // op 19186: put
  db->Put(write_options, "key8323", "value56242");

  // op 19187: put
  db->Put(write_options, "key3688", "value32928");

  // op 19188: delete
  db->Delete(write_options, "key9748");

  // op 19189: put
  db->Put(write_options, "key7800", "value17117");

  // op 19190: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 19191: put
  db->Put(write_options, "key6335", "value33551");

  // op 19192: put
  db->Put(write_options, "key7755", "value12512");

  // op 19193: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0278", "key8476", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0278, key8476) size=" << scan_result.size() << std::endl;
  }

  // op 19194: put
  db->Put(write_options, "key3658", "value15560");

  // op 19195: get
  status = db->Get(read_options, "key9283", &value);
  if (status.ok()) {
    out << "GET key9283 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19196: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3625", "key7944", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3625, key7944) size=" << scan_result.size() << std::endl;
  }

  // op 19197: get
  status = db->Get(read_options, "key6755", &value);
  if (status.ok()) {
    out << "GET key6755 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19198: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5714", "key11400", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5714, key11400) size=" << scan_result.size() << std::endl;
  }

  // op 19199: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5160", "key11254", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5160, key11254) size=" << scan_result.size() << std::endl;
  }

  // op 19200: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0149", "key6435", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0149, key6435) size=" << scan_result.size() << std::endl;
  }

  // op 19201: delete
  db->Delete(write_options, "key4060");

  // op 19202: put
  db->Put(write_options, "key8119", "value106685");

  // op 19203: get
  status = db->Get(read_options, "key0803", &value);
  if (status.ok()) {
    out << "GET key0803 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19204: get
  status = db->Get(read_options, "key2127", &value);
  if (status.ok()) {
    out << "GET key2127 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19205: get
  status = db->Get(read_options, "key5976", &value);
  if (status.ok()) {
    out << "GET key5976 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19206: put
  db->Put(write_options, "key9841", "value7035");

  // op 19207: get
  status = db->Get(read_options, "key9320", &value);
  if (status.ok()) {
    out << "GET key9320 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19208: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0842", "key2538", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0842, key2538) size=" << scan_result.size() << std::endl;
  }

  // op 19209: get
  status = db->Get(read_options, "key3511", &value);
  if (status.ok()) {
    out << "GET key3511 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19210: delete
  db->Delete(write_options, "key0215");

  // op 19211: put
  db->Put(write_options, "key8229", "value36469");

  // op 19212: put
  db->Put(write_options, "key3148", "value105104");

  // op 19213: get
  status = db->Get(read_options, "key10158", &value);
  if (status.ok()) {
    out << "GET key10158 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19214: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3233", "key6399", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3233, key6399) size=" << scan_result.size() << std::endl;
  }

  // op 19215: put
  db->Put(write_options, "key5284", "value76113");

  // op 19216: get
  status = db->Get(read_options, "key7172", &value);
  if (status.ok()) {
    out << "GET key7172 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19217: put
  db->Put(write_options, "key4331", "value37535");

  // op 19218: put
  db->Put(write_options, "key9879", "value66482");

  // op 19219: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2857", "key8742", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2857, key8742) size=" << scan_result.size() << std::endl;
  }

  // op 19220: get
  status = db->Get(read_options, "key6141", &value);
  if (status.ok()) {
    out << "GET key6141 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19221: get
  status = db->Get(read_options, "key1659", &value);
  if (status.ok()) {
    out << "GET key1659 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19222: put
  db->Put(write_options, "key2881", "value43431");

  // op 19223: put
  db->Put(write_options, "key0507", "value88325");

  // op 19224: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0423", "key9939", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0423, key9939) size=" << scan_result.size() << std::endl;
  }

  // op 19225: delete
  db->Delete(write_options, "key2769");

  // op 19226: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 19227: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5044", "key11142", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5044, key11142) size=" << scan_result.size() << std::endl;
  }

  // op 19228: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 19229: put
  db->Put(write_options, "key4772", "value35535");

  // op 19230: delete
  db->Delete(write_options, "key3074");

  // op 19231: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 19232: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5309", "key9798", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5309, key9798) size=" << scan_result.size() << std::endl;
  }

  // op 19233: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8253", "key8478", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8253, key8478) size=" << scan_result.size() << std::endl;
  }

  // op 19234: put
  db->Put(write_options, "key5153", "value33980");

  // op 19235: put
  db->Put(write_options, "key11435", "value27992");

  // op 19236: get
  status = db->Get(read_options, "key1558", &value);
  if (status.ok()) {
    out << "GET key1558 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19237: put
  db->Put(write_options, "key11648", "value107414");

  // op 19238: delete
  db->Delete(write_options, "key9496");

  // op 19239: get
  status = db->Get(read_options, "key5780", &value);
  if (status.ok()) {
    out << "GET key5780 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19240: put
  db->Put(write_options, "key6829", "value40603");

  // op 19241: put
  db->Put(write_options, "key6856", "value52225");

  // op 19242: delete_range
  status = db->DeleteRange(write_options, "key11155", "key11291");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 19243: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0056", "key2218", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0056, key2218) size=" << scan_result.size() << std::endl;
  }

  // op 19244: delete
  db->Delete(write_options, "key8383");

  // op 19245: delete
  db->Delete(write_options, "key4126");

  // op 19246: delete
  db->Delete(write_options, "key7654");

  // op 19247: put
  db->Put(write_options, "key7834", "value93575");

  // op 19248: delete
  db->Delete(write_options, "key6545");

  // op 19249: delete_range
  status = db->DeleteRange(write_options, "key0593", "key0942");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 19250: get
  status = db->Get(read_options, "key3518", &value);
  if (status.ok()) {
    out << "GET key3518 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19251: put
  db->Put(write_options, "key0504", "value75221");

  // op 19252: get
  status = db->Get(read_options, "key11656", &value);
  if (status.ok()) {
    out << "GET key11656 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19253: put
  db->Put(write_options, "key1687", "value109307");

  // op 19254: put
  db->Put(write_options, "key7738", "value56001");

  // op 19255: put
  db->Put(write_options, "key9974", "value94923");

  // op 19256: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5978", "key7824", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5978, key7824) size=" << scan_result.size() << std::endl;
  }

  // op 19257: put
  db->Put(write_options, "key10891", "value89524");

  // op 19258: put
  db->Put(write_options, "key9137", "value69449");

  // op 19259: get
  status = db->Get(read_options, "key8485", &value);
  if (status.ok()) {
    out << "GET key8485 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19260: get
  status = db->Get(read_options, "key7749", &value);
  if (status.ok()) {
    out << "GET key7749 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19261: delete_range
  status = db->DeleteRange(write_options, "key3848", "key10826");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 19262: put
  db->Put(write_options, "key3178", "value105501");

  // op 19263: put
  db->Put(write_options, "key1907", "value94719");

  // op 19264: put
  db->Put(write_options, "key10777", "value86821");

  // op 19265: get
  status = db->Get(read_options, "key1495", &value);
  if (status.ok()) {
    out << "GET key1495 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19266: put
  db->Put(write_options, "key1327", "value29625");

  // op 19267: put
  db->Put(write_options, "key3563", "value75308");

  // op 19268: get
  status = db->Get(read_options, "key0605", &value);
  if (status.ok()) {
    out << "GET key0605 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19269: delete
  db->Delete(write_options, "key9673");

  // op 19270: put
  db->Put(write_options, "key10887", "value39051");

  // op 19271: delete
  db->Delete(write_options, "key3686");

  // op 19272: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2224", "key2291", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2224, key2291) size=" << scan_result.size() << std::endl;
  }

  // op 19273: put
  db->Put(write_options, "key10476", "value105396");

  // op 19274: put
  db->Put(write_options, "key4799", "value115567");

  // op 19275: delete
  db->Delete(write_options, "key1011");

  // op 19276: put
  db->Put(write_options, "key0798", "value54857");

  // op 19277: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 19278: put
  db->Put(write_options, "key6928", "value102266");

  // op 19279: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0365", "key0548", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0365, key0548) size=" << scan_result.size() << std::endl;
  }

  // op 19280: put
  db->Put(write_options, "key5243", "value102159");

  // op 19281: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4263", "key9217", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4263, key9217) size=" << scan_result.size() << std::endl;
  }

  // op 19282: get
  status = db->Get(read_options, "key0023", &value);
  if (status.ok()) {
    out << "GET key0023 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19283: put
  db->Put(write_options, "key4955", "value53124");

  // op 19284: get
  status = db->Get(read_options, "key4616", &value);
  if (status.ok()) {
    out << "GET key4616 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19285: put
  db->Put(write_options, "key2623", "value73302");

  // op 19286: put
  db->Put(write_options, "key4346", "value26427");

  // op 19287: delete_range
  status = db->DeleteRange(write_options, "key0979", "key7419");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 19288: delete
  db->Delete(write_options, "key10375");

  // op 19289: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5360", "key7967", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5360, key7967) size=" << scan_result.size() << std::endl;
  }

  // op 19290: get
  status = db->Get(read_options, "key0291", &value);
  if (status.ok()) {
    out << "GET key0291 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19291: get
  status = db->Get(read_options, "key0194", &value);
  if (status.ok()) {
    out << "GET key0194 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19292: get
  status = db->Get(read_options, "key11033", &value);
  if (status.ok()) {
    out << "GET key11033 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19293: put
  db->Put(write_options, "key3324", "value73825");

  // op 19294: get
  status = db->Get(read_options, "key7150", &value);
  if (status.ok()) {
    out << "GET key7150 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19295: put
  db->Put(write_options, "key1255", "value63022");

  // op 19296: put
  db->Put(write_options, "key5043", "value64147");

  // op 19297: put
  db->Put(write_options, "key2401", "value28107");

  // op 19298: get
  status = db->Get(read_options, "key4389", &value);
  if (status.ok()) {
    out << "GET key4389 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19299: put
  db->Put(write_options, "key7819", "value2866");

  // op 19300: put
  db->Put(write_options, "key3117", "value30858");

  // op 19301: put
  db->Put(write_options, "key5505", "value20104");

  // op 19302: put
  db->Put(write_options, "key4402", "value62103");

  // op 19303: put
  db->Put(write_options, "key3883", "value19379");

  // op 19304: put
  db->Put(write_options, "key8066", "value0327");

  // op 19305: get
  status = db->Get(read_options, "key4411", &value);
  if (status.ok()) {
    out << "GET key4411 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19306: put
  db->Put(write_options, "key3694", "value116474");

  // op 19307: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 19308: get
  status = db->Get(read_options, "key9321", &value);
  if (status.ok()) {
    out << "GET key9321 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19309: get
  status = db->Get(read_options, "key11242", &value);
  if (status.ok()) {
    out << "GET key11242 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19310: get
  status = db->Get(read_options, "key1132", &value);
  if (status.ok()) {
    out << "GET key1132 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19311: put
  db->Put(write_options, "key3784", "value94038");

  // op 19312: put
  db->Put(write_options, "key3874", "value28278");

  // op 19313: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1934", "key4283", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1934, key4283) size=" << scan_result.size() << std::endl;
  }

  // op 19314: get
  status = db->Get(read_options, "key7851", &value);
  if (status.ok()) {
    out << "GET key7851 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19315: put
  db->Put(write_options, "key11578", "value104448");

  // op 19316: get
  status = db->Get(read_options, "key10179", &value);
  if (status.ok()) {
    out << "GET key10179 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19317: put
  db->Put(write_options, "key3724", "value73066");

  // op 19318: put
  db->Put(write_options, "key7411", "value112457");

  // op 19319: put
  db->Put(write_options, "key10734", "value110855");

  // op 19320: get
  status = db->Get(read_options, "key8986", &value);
  if (status.ok()) {
    out << "GET key8986 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19321: get
  status = db->Get(read_options, "key6286", &value);
  if (status.ok()) {
    out << "GET key6286 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19322: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6688", "key7878", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6688, key7878) size=" << scan_result.size() << std::endl;
  }

  // op 19323: get
  status = db->Get(read_options, "key4442", &value);
  if (status.ok()) {
    out << "GET key4442 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19324: put
  db->Put(write_options, "key10016", "value112176");

  // op 19325: get
  status = db->Get(read_options, "key3931", &value);
  if (status.ok()) {
    out << "GET key3931 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19326: delete
  db->Delete(write_options, "key7381");

  // op 19327: put
  db->Put(write_options, "key9255", "value72793");

  // op 19328: put
  db->Put(write_options, "key5621", "value112773");

  // op 19329: delete
  db->Delete(write_options, "key9955");

  // op 19330: put
  db->Put(write_options, "key11652", "value86835");

  // op 19331: put
  db->Put(write_options, "key1100", "value35444");

  // op 19332: put
  db->Put(write_options, "key3621", "value23156");

  // op 19333: put
  db->Put(write_options, "key1737", "value102201");

  // op 19334: put
  db->Put(write_options, "key3749", "value18559");

  // op 19335: delete_range
  status = db->DeleteRange(write_options, "key3651", "key9868");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 19336: get
  status = db->Get(read_options, "key3754", &value);
  if (status.ok()) {
    out << "GET key3754 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19337: put
  db->Put(write_options, "key3817", "value52440");

  // op 19338: get
  status = db->Get(read_options, "key7182", &value);
  if (status.ok()) {
    out << "GET key7182 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19339: delete
  db->Delete(write_options, "key2014");

  // op 19340: get
  status = db->Get(read_options, "key7113", &value);
  if (status.ok()) {
    out << "GET key7113 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19341: get
  status = db->Get(read_options, "key8435", &value);
  if (status.ok()) {
    out << "GET key8435 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19342: delete
  db->Delete(write_options, "key3568");

  // op 19343: get
  status = db->Get(read_options, "key4864", &value);
  if (status.ok()) {
    out << "GET key4864 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19344: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8012", "key8385", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8012, key8385) size=" << scan_result.size() << std::endl;
  }

  // op 19345: delete
  db->Delete(write_options, "key7769");

  // op 19346: delete
  db->Delete(write_options, "key8055");

  // op 19347: delete
  db->Delete(write_options, "key4946");

  // op 19348: put
  db->Put(write_options, "key0549", "value71334");

  // op 19349: get
  status = db->Get(read_options, "key11256", &value);
  if (status.ok()) {
    out << "GET key11256 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19350: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1035", "key8996", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1035, key8996) size=" << scan_result.size() << std::endl;
  }

  // op 19351: put
  db->Put(write_options, "key3353", "value5267");

  // op 19352: put
  db->Put(write_options, "key0581", "value19378");

  // op 19353: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4093", "key9619", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4093, key9619) size=" << scan_result.size() << std::endl;
  }

  // op 19354: get
  status = db->Get(read_options, "key3333", &value);
  if (status.ok()) {
    out << "GET key3333 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19355: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4924", "key6965", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4924, key6965) size=" << scan_result.size() << std::endl;
  }

  // op 19356: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1389", "key4232", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1389, key4232) size=" << scan_result.size() << std::endl;
  }

  // op 19357: get
  status = db->Get(read_options, "key3092", &value);
  if (status.ok()) {
    out << "GET key3092 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19358: delete
  db->Delete(write_options, "key6379");

  // op 19359: get
  status = db->Get(read_options, "key2965", &value);
  if (status.ok()) {
    out << "GET key2965 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19360: put
  db->Put(write_options, "key6899", "value10111");

  // op 19361: delete_range
  status = db->DeleteRange(write_options, "key1646", "key4152");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 19362: put
  db->Put(write_options, "key9175", "value0315");

  // op 19363: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 19364: put
  db->Put(write_options, "key1186", "value85896");

  // op 19365: put
  db->Put(write_options, "key7528", "value31290");

  // op 19366: get
  status = db->Get(read_options, "key9173", &value);
  if (status.ok()) {
    out << "GET key9173 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19367: put
  db->Put(write_options, "key10855", "value58142");

  // op 19368: put
  db->Put(write_options, "key11438", "value111094");

  // op 19369: get
  status = db->Get(read_options, "key0201", &value);
  if (status.ok()) {
    out << "GET key0201 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19370: put
  db->Put(write_options, "key10091", "value4247");

  // op 19371: put
  db->Put(write_options, "key11587", "value17362");

  // op 19372: put
  db->Put(write_options, "key0988", "value104060");

  // op 19373: put
  db->Put(write_options, "key1473", "value61619");

  // op 19374: delete_range
  status = db->DeleteRange(write_options, "key1218", "key9968");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 19375: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8913", "key10379", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8913, key10379) size=" << scan_result.size() << std::endl;
  }

  // op 19376: get
  status = db->Get(read_options, "key0904", &value);
  if (status.ok()) {
    out << "GET key0904 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19377: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5088", "key6157", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5088, key6157) size=" << scan_result.size() << std::endl;
  }

  // op 19378: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 19379: put
  db->Put(write_options, "key11696", "value63047");

  // op 19380: put
  db->Put(write_options, "key7994", "value12850");

  // op 19381: delete_range
  status = db->DeleteRange(write_options, "key8625", "key10344");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 19382: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 19383: put
  db->Put(write_options, "key4921", "value4020");

  // op 19384: get
  status = db->Get(read_options, "key11177", &value);
  if (status.ok()) {
    out << "GET key11177 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19385: get
  status = db->Get(read_options, "key2350", &value);
  if (status.ok()) {
    out << "GET key2350 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19386: put
  db->Put(write_options, "key10908", "value50091");

  // op 19387: put
  db->Put(write_options, "key0666", "value14358");

  // op 19388: put
  db->Put(write_options, "key11184", "value48622");

  // op 19389: put
  db->Put(write_options, "key10498", "value19074");

  // op 19390: delete
  db->Delete(write_options, "key1447");

  // op 19391: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7988", "key9356", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7988, key9356) size=" << scan_result.size() << std::endl;
  }

  // op 19392: put
  db->Put(write_options, "key3675", "value65626");

  // op 19393: get
  status = db->Get(read_options, "key2221", &value);
  if (status.ok()) {
    out << "GET key2221 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19394: put
  db->Put(write_options, "key3878", "value67182");

  // op 19395: get
  status = db->Get(read_options, "key8586", &value);
  if (status.ok()) {
    out << "GET key8586 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19396: scan
  scan_result.clear();
  status = db->Scan(read_options, "key11453", "key11456", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key11453, key11456) size=" << scan_result.size() << std::endl;
  }

  // op 19397: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4611", "key8696", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4611, key8696) size=" << scan_result.size() << std::endl;
  }

  // op 19398: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 19399: put
  db->Put(write_options, "key8691", "value22463");

  // op 19400: get
  status = db->Get(read_options, "key7469", &value);
  if (status.ok()) {
    out << "GET key7469 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19401: put
  db->Put(write_options, "key4754", "value74786");

  // op 19402: delete
  db->Delete(write_options, "key0968");

  // op 19403: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4050", "key6747", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4050, key6747) size=" << scan_result.size() << std::endl;
  }

  // op 19404: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1933", "key9341", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1933, key9341) size=" << scan_result.size() << std::endl;
  }

  // op 19405: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4747", "key8429", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4747, key8429) size=" << scan_result.size() << std::endl;
  }

  // op 19406: get
  status = db->Get(read_options, "key3374", &value);
  if (status.ok()) {
    out << "GET key3374 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19407: put
  db->Put(write_options, "key0718", "value80712");

  // op 19408: put
  db->Put(write_options, "key2657", "value6799");

  // op 19409: put
  db->Put(write_options, "key5721", "value3396");

  // op 19410: delete
  db->Delete(write_options, "key10275");

  // op 19411: put
  db->Put(write_options, "key7718", "value55197");

  // op 19412: get
  status = db->Get(read_options, "key7165", &value);
  if (status.ok()) {
    out << "GET key7165 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19413: delete
  db->Delete(write_options, "key6420");

  // op 19414: put
  db->Put(write_options, "key8211", "value99245");

  // op 19415: put
  db->Put(write_options, "key6129", "value89162");

  // op 19416: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0534", "key2438", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0534, key2438) size=" << scan_result.size() << std::endl;
  }

  // op 19417: delete
  db->Delete(write_options, "key5935");

  // op 19418: delete
  db->Delete(write_options, "key2188");

  // op 19419: put
  db->Put(write_options, "key1766", "value102045");

  // op 19420: put
  db->Put(write_options, "key2810", "value33980");

  // op 19421: get
  status = db->Get(read_options, "key9719", &value);
  if (status.ok()) {
    out << "GET key9719 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19422: delete
  db->Delete(write_options, "key11575");

  // op 19423: put
  db->Put(write_options, "key9325", "value1328");

  // op 19424: put
  db->Put(write_options, "key7884", "value94506");

  // op 19425: put
  db->Put(write_options, "key1013", "value14979");

  // op 19426: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7209", "key9319", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7209, key9319) size=" << scan_result.size() << std::endl;
  }

  // op 19427: delete
  db->Delete(write_options, "key0449");

  // op 19428: put
  db->Put(write_options, "key7048", "value29239");

  // op 19429: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1547", "key1571", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1547, key1571) size=" << scan_result.size() << std::endl;
  }

  // op 19430: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1208", "key10682", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1208, key10682) size=" << scan_result.size() << std::endl;
  }

  // op 19431: put
  db->Put(write_options, "key8574", "value111954");

  // op 19432: delete
  db->Delete(write_options, "key4382");

  // op 19433: put
  db->Put(write_options, "key8235", "value22935");

  // op 19434: put
  db->Put(write_options, "key10962", "value58708");

  // op 19435: get
  status = db->Get(read_options, "key9407", &value);
  if (status.ok()) {
    out << "GET key9407 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19436: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0324", "key6560", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0324, key6560) size=" << scan_result.size() << std::endl;
  }

  // op 19437: delete_range
  status = db->DeleteRange(write_options, "key4284", "key5489");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 19438: put
  db->Put(write_options, "key3861", "value97875");

  // op 19439: put
  db->Put(write_options, "key1578", "value71686");

  // op 19440: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 19441: get
  status = db->Get(read_options, "key6446", &value);
  if (status.ok()) {
    out << "GET key6446 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19442: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1819", "key8071", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1819, key8071) size=" << scan_result.size() << std::endl;
  }

  // op 19443: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5514", "key9929", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5514, key9929) size=" << scan_result.size() << std::endl;
  }

  // op 19444: put
  db->Put(write_options, "key2715", "value7734");

  // op 19445: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4362", "key9493", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4362, key9493) size=" << scan_result.size() << std::endl;
  }

  // op 19446: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5559", "key10726", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5559, key10726) size=" << scan_result.size() << std::endl;
  }

  // op 19447: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1603", "key5522", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1603, key5522) size=" << scan_result.size() << std::endl;
  }

  // op 19448: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3966", "key10346", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3966, key10346) size=" << scan_result.size() << std::endl;
  }

  // op 19449: delete
  db->Delete(write_options, "key9224");

  // op 19450: get
  status = db->Get(read_options, "key0632", &value);
  if (status.ok()) {
    out << "GET key0632 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19451: put
  db->Put(write_options, "key5629", "value25531");

  // op 19452: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 19453: delete
  db->Delete(write_options, "key9962");

  // op 19454: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7511", "key7779", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7511, key7779) size=" << scan_result.size() << std::endl;
  }

  // op 19455: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0128", "key10683", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0128, key10683) size=" << scan_result.size() << std::endl;
  }

  // op 19456: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5604", "key6324", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5604, key6324) size=" << scan_result.size() << std::endl;
  }

  // op 19457: put
  db->Put(write_options, "key2083", "value98946");

  // op 19458: get
  status = db->Get(read_options, "key10781", &value);
  if (status.ok()) {
    out << "GET key10781 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19459: put
  db->Put(write_options, "key1548", "value15606");

  // op 19460: get
  status = db->Get(read_options, "key2531", &value);
  if (status.ok()) {
    out << "GET key2531 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19461: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 19462: get
  status = db->Get(read_options, "key1254", &value);
  if (status.ok()) {
    out << "GET key1254 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19463: get
  status = db->Get(read_options, "key2970", &value);
  if (status.ok()) {
    out << "GET key2970 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19464: delete
  db->Delete(write_options, "key11500");

  // op 19465: put
  db->Put(write_options, "key6854", "value110815");

  // op 19466: delete_range
  status = db->DeleteRange(write_options, "key0729", "key4381");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 19467: put
  db->Put(write_options, "key4156", "value39661");

  // op 19468: delete_range
  status = db->DeleteRange(write_options, "key1706", "key5634");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 19469: put
  db->Put(write_options, "key3819", "value83893");

  // op 19470: put
  db->Put(write_options, "key4836", "value109134");

  // op 19471: delete
  db->Delete(write_options, "key10384");

  // op 19472: delete_range
  status = db->DeleteRange(write_options, "key1768", "key7391");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 19473: put
  db->Put(write_options, "key7865", "value56657");

  // op 19474: put
  db->Put(write_options, "key1869", "value47592");

  // op 19475: put
  db->Put(write_options, "key11159", "value52029");

  // op 19476: delete
  db->Delete(write_options, "key6429");

  // op 19477: put
  db->Put(write_options, "key4046", "value18477");

  // op 19478: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2777", "key10048", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2777, key10048) size=" << scan_result.size() << std::endl;
  }

  // op 19479: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2516", "key4731", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2516, key4731) size=" << scan_result.size() << std::endl;
  }

  // op 19480: delete_range
  status = db->DeleteRange(write_options, "key6879", "key10225");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 19481: put
  db->Put(write_options, "key4948", "value28588");

  // op 19482: put
  db->Put(write_options, "key0273", "value82910");

  // op 19483: put
  db->Put(write_options, "key2588", "value35717");

  // op 19484: delete
  db->Delete(write_options, "key10649");

  // op 19485: put
  db->Put(write_options, "key6311", "value42828");

  // op 19486: put
  db->Put(write_options, "key5598", "value23175");

  // op 19487: get
  status = db->Get(read_options, "key8283", &value);
  if (status.ok()) {
    out << "GET key8283 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19488: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 19489: put
  db->Put(write_options, "key9185", "value50517");

  // op 19490: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7897", "key9974", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7897, key9974) size=" << scan_result.size() << std::endl;
  }

  // op 19491: put
  db->Put(write_options, "key0924", "value59275");

  // op 19492: delete_range
  status = db->DeleteRange(write_options, "key7555", "key11491");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 19493: put
  db->Put(write_options, "key1209", "value2618");

  // op 19494: delete_range
  status = db->DeleteRange(write_options, "key6909", "key10186");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 19495: delete_range
  status = db->DeleteRange(write_options, "key1636", "key8714");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 19496: delete
  db->Delete(write_options, "key1097");

  // op 19497: put
  db->Put(write_options, "key1165", "value61221");

  // op 19498: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9116", "key9997", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9116, key9997) size=" << scan_result.size() << std::endl;
  }

  // op 19499: put
  db->Put(write_options, "key7871", "value2551");

  // op 19500: delete_range
  status = db->DeleteRange(write_options, "key6517", "key9203");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 19501: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1517", "key9455", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1517, key9455) size=" << scan_result.size() << std::endl;
  }

  // op 19502: put
  db->Put(write_options, "key5576", "value1979");

  // op 19503: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 19504: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0139", "key2335", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0139, key2335) size=" << scan_result.size() << std::endl;
  }

  // op 19505: get
  status = db->Get(read_options, "key6302", &value);
  if (status.ok()) {
    out << "GET key6302 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19506: put
  db->Put(write_options, "key8188", "value63332");

  // op 19507: put
  db->Put(write_options, "key4780", "value104423");

  // op 19508: put
  db->Put(write_options, "key11021", "value40322");

  // op 19509: put
  db->Put(write_options, "key3427", "value4420");

  // op 19510: delete
  db->Delete(write_options, "key10117");

  // op 19511: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 19512: put
  db->Put(write_options, "key4690", "value17846");

  // op 19513: delete
  db->Delete(write_options, "key4627");

  // op 19514: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1307", "key9854", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1307, key9854) size=" << scan_result.size() << std::endl;
  }

  // op 19515: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1672", "key4186", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1672, key4186) size=" << scan_result.size() << std::endl;
  }

  // op 19516: put
  db->Put(write_options, "key10736", "value102922");

  // op 19517: get
  status = db->Get(read_options, "key3867", &value);
  if (status.ok()) {
    out << "GET key3867 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19518: put
  db->Put(write_options, "key9120", "value40569");

  // op 19519: delete_range
  status = db->DeleteRange(write_options, "key4335", "key11433");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 19520: put
  db->Put(write_options, "key5481", "value73591");

  // op 19521: put
  db->Put(write_options, "key4501", "value71180");

  // op 19522: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4823", "key5169", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4823, key5169) size=" << scan_result.size() << std::endl;
  }

  // op 19523: put
  db->Put(write_options, "key1217", "value90883");

  // op 19524: put
  db->Put(write_options, "key10432", "value30727");

  // op 19525: put
  db->Put(write_options, "key3001", "value104955");

  // op 19526: delete_range
  status = db->DeleteRange(write_options, "key2760", "key2871");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 19527: get
  status = db->Get(read_options, "key6181", &value);
  if (status.ok()) {
    out << "GET key6181 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19528: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3315", "key8419", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3315, key8419) size=" << scan_result.size() << std::endl;
  }

  // op 19529: put
  db->Put(write_options, "key3913", "value91658");

  // op 19530: put
  db->Put(write_options, "key8416", "value109191");

  // op 19531: put
  db->Put(write_options, "key4863", "value68901");

  // op 19532: get
  status = db->Get(read_options, "key6815", &value);
  if (status.ok()) {
    out << "GET key6815 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19533: put
  db->Put(write_options, "key11443", "value27618");

  // op 19534: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8731", "key10323", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8731, key10323) size=" << scan_result.size() << std::endl;
  }

  // op 19535: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3864", "key3942", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3864, key3942) size=" << scan_result.size() << std::endl;
  }

  // op 19536: delete
  db->Delete(write_options, "key0263");

  // op 19537: get
  status = db->Get(read_options, "key0803", &value);
  if (status.ok()) {
    out << "GET key0803 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19538: put
  db->Put(write_options, "key4853", "value103110");

  // op 19539: put
  db->Put(write_options, "key0796", "value21906");

  // op 19540: get
  status = db->Get(read_options, "key4522", &value);
  if (status.ok()) {
    out << "GET key4522 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19541: delete
  db->Delete(write_options, "key1559");

  // op 19542: delete_range
  status = db->DeleteRange(write_options, "key4382", "key9183");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 19543: put
  db->Put(write_options, "key1920", "value55958");

  // op 19544: delete
  db->Delete(write_options, "key7091");

  // op 19545: delete_range
  status = db->DeleteRange(write_options, "key3650", "key6935");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 19546: put
  db->Put(write_options, "key6240", "value94196");

  // op 19547: put
  db->Put(write_options, "key10510", "value27996");

  // op 19548: put
  db->Put(write_options, "key0989", "value21904");

  // op 19549: delete
  db->Delete(write_options, "key6300");

  // op 19550: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6945", "key8751", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6945, key8751) size=" << scan_result.size() << std::endl;
  }

  // op 19551: put
  db->Put(write_options, "key2101", "value15484");

  // op 19552: put
  db->Put(write_options, "key6562", "value105754");

  // op 19553: delete
  db->Delete(write_options, "key6156");

  // op 19554: delete_range
  status = db->DeleteRange(write_options, "key7675", "key8968");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 19555: get
  status = db->Get(read_options, "key6907", &value);
  if (status.ok()) {
    out << "GET key6907 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19556: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2765", "key4967", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2765, key4967) size=" << scan_result.size() << std::endl;
  }

  // op 19557: put
  db->Put(write_options, "key2670", "value91704");

  // op 19558: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0316", "key1126", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0316, key1126) size=" << scan_result.size() << std::endl;
  }

  // op 19559: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1420", "key8861", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1420, key8861) size=" << scan_result.size() << std::endl;
  }

  // op 19560: delete_range
  status = db->DeleteRange(write_options, "key2460", "key11452");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 19561: delete
  db->Delete(write_options, "key8950");

  // op 19562: put
  db->Put(write_options, "key3206", "value35079");

  // op 19563: put
  db->Put(write_options, "key6857", "value82432");

  // op 19564: put
  db->Put(write_options, "key10270", "value63841");

  // op 19565: put
  db->Put(write_options, "key9868", "value18116");

  // op 19566: put
  db->Put(write_options, "key2724", "value54629");

  // op 19567: delete_range
  status = db->DeleteRange(write_options, "key7405", "key10175");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 19568: put
  db->Put(write_options, "key2590", "value50994");

  // op 19569: get
  status = db->Get(read_options, "key11585", &value);
  if (status.ok()) {
    out << "GET key11585 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19570: get
  status = db->Get(read_options, "key1592", &value);
  if (status.ok()) {
    out << "GET key1592 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19571: get
  status = db->Get(read_options, "key5750", &value);
  if (status.ok()) {
    out << "GET key5750 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19572: get
  status = db->Get(read_options, "key6683", &value);
  if (status.ok()) {
    out << "GET key6683 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19573: put
  db->Put(write_options, "key10444", "value95429");

  // op 19574: delete
  db->Delete(write_options, "key4756");

  // op 19575: delete
  db->Delete(write_options, "key6423");

  // op 19576: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4980", "key11010", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4980, key11010) size=" << scan_result.size() << std::endl;
  }

  // op 19577: put
  db->Put(write_options, "key3449", "value102950");

  // op 19578: delete
  db->Delete(write_options, "key11718");

  // op 19579: get
  status = db->Get(read_options, "key6117", &value);
  if (status.ok()) {
    out << "GET key6117 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19580: delete
  db->Delete(write_options, "key1951");

  // op 19581: put
  db->Put(write_options, "key9183", "value63839");

  // op 19582: get
  status = db->Get(read_options, "key2457", &value);
  if (status.ok()) {
    out << "GET key2457 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19583: get
  status = db->Get(read_options, "key1176", &value);
  if (status.ok()) {
    out << "GET key1176 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19584: get
  status = db->Get(read_options, "key2465", &value);
  if (status.ok()) {
    out << "GET key2465 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19585: delete
  db->Delete(write_options, "key5050");

  // op 19586: put
  db->Put(write_options, "key4230", "value23338");

  // op 19587: put
  db->Put(write_options, "key0421", "value64893");

  // op 19588: get
  status = db->Get(read_options, "key6102", &value);
  if (status.ok()) {
    out << "GET key6102 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19589: delete_range
  status = db->DeleteRange(write_options, "key2177", "key8029");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 19590: get
  status = db->Get(read_options, "key7969", &value);
  if (status.ok()) {
    out << "GET key7969 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19591: put
  db->Put(write_options, "key4486", "value40107");

  // op 19592: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 19593: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 19594: put
  db->Put(write_options, "key11066", "value71011");

  // op 19595: get
  status = db->Get(read_options, "key9549", &value);
  if (status.ok()) {
    out << "GET key9549 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19596: get
  status = db->Get(read_options, "key5826", &value);
  if (status.ok()) {
    out << "GET key5826 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19597: delete_range
  status = db->DeleteRange(write_options, "key2199", "key10808");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 19598: delete_range
  status = db->DeleteRange(write_options, "key0799", "key3796");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 19599: get
  status = db->Get(read_options, "key1539", &value);
  if (status.ok()) {
    out << "GET key1539 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19600: get
  status = db->Get(read_options, "key6456", &value);
  if (status.ok()) {
    out << "GET key6456 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19601: put
  db->Put(write_options, "key5913", "value88068");

  // op 19602: delete
  db->Delete(write_options, "key3447");

  // op 19603: put
  db->Put(write_options, "key9837", "value54554");

  // op 19604: get
  status = db->Get(read_options, "key8593", &value);
  if (status.ok()) {
    out << "GET key8593 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19605: get
  status = db->Get(read_options, "key11569", &value);
  if (status.ok()) {
    out << "GET key11569 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19606: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1672", "key4097", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1672, key4097) size=" << scan_result.size() << std::endl;
  }

  // op 19607: put
  db->Put(write_options, "key3713", "value97703");

  // op 19608: put
  db->Put(write_options, "key10596", "value2604");

  // op 19609: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0812", "key5369", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0812, key5369) size=" << scan_result.size() << std::endl;
  }

  // op 19610: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4766", "key5510", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4766, key5510) size=" << scan_result.size() << std::endl;
  }

  // op 19611: put
  db->Put(write_options, "key4766", "value90779");

  // op 19612: put
  db->Put(write_options, "key5262", "value45522");

  // op 19613: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5691", "key8749", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5691, key8749) size=" << scan_result.size() << std::endl;
  }

  // op 19614: put
  db->Put(write_options, "key11295", "value62355");

  // op 19615: delete_range
  status = db->DeleteRange(write_options, "key4350", "key10699");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 19616: put
  db->Put(write_options, "key1604", "value87041");

  // op 19617: put
  db->Put(write_options, "key3115", "value110400");

  // op 19618: get
  status = db->Get(read_options, "key0807", &value);
  if (status.ok()) {
    out << "GET key0807 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19619: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 19620: get
  status = db->Get(read_options, "key1422", &value);
  if (status.ok()) {
    out << "GET key1422 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19621: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4685", "key8986", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4685, key8986) size=" << scan_result.size() << std::endl;
  }

  // op 19622: delete_range
  status = db->DeleteRange(write_options, "key2809", "key11517");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 19623: put
  db->Put(write_options, "key10147", "value101589");

  // op 19624: put
  db->Put(write_options, "key10281", "value95963");

  // op 19625: scan
  scan_result.clear();
  status = db->Scan(read_options, "key11543", "key11640", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key11543, key11640) size=" << scan_result.size() << std::endl;
  }

  // op 19626: put
  db->Put(write_options, "key1781", "value4698");

  // op 19627: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7608", "key11585", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7608, key11585) size=" << scan_result.size() << std::endl;
  }

  // op 19628: put
  db->Put(write_options, "key4277", "value15738");

  // op 19629: delete_range
  status = db->DeleteRange(write_options, "key1656", "key6913");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 19630: get
  status = db->Get(read_options, "key2463", &value);
  if (status.ok()) {
    out << "GET key2463 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19631: get
  status = db->Get(read_options, "key5737", &value);
  if (status.ok()) {
    out << "GET key5737 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19632: put
  db->Put(write_options, "key4978", "value104610");

  // op 19633: get
  status = db->Get(read_options, "key9923", &value);
  if (status.ok()) {
    out << "GET key9923 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19634: put
  db->Put(write_options, "key8529", "value9925");

  // op 19635: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2559", "key11034", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2559, key11034) size=" << scan_result.size() << std::endl;
  }

  // op 19636: put
  db->Put(write_options, "key1632", "value53498");

  // op 19637: put
  db->Put(write_options, "key3241", "value2537");

  // op 19638: put
  db->Put(write_options, "key4178", "value38388");

  // op 19639: delete
  db->Delete(write_options, "key1403");

  // op 19640: put
  db->Put(write_options, "key4011", "value86762");

  // op 19641: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3169", "key9878", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3169, key9878) size=" << scan_result.size() << std::endl;
  }

  // op 19642: delete
  db->Delete(write_options, "key11390");

  // op 19643: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0070", "key8444", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0070, key8444) size=" << scan_result.size() << std::endl;
  }

  // op 19644: delete_range
  status = db->DeleteRange(write_options, "key6596", "key8718");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 19645: put
  db->Put(write_options, "key1707", "value17215");

  // op 19646: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1390", "key6844", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1390, key6844) size=" << scan_result.size() << std::endl;
  }

  // op 19647: put
  db->Put(write_options, "key10618", "value109954");

  // op 19648: delete
  db->Delete(write_options, "key5673");

  // op 19649: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4210", "key5198", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4210, key5198) size=" << scan_result.size() << std::endl;
  }

  // op 19650: get
  status = db->Get(read_options, "key2381", &value);
  if (status.ok()) {
    out << "GET key2381 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19651: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3416", "key11149", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3416, key11149) size=" << scan_result.size() << std::endl;
  }

  // op 19652: get
  status = db->Get(read_options, "key7089", &value);
  if (status.ok()) {
    out << "GET key7089 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19653: put
  db->Put(write_options, "key10365", "value26746");

  // op 19654: delete_range
  status = db->DeleteRange(write_options, "key3182", "key3543");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 19655: put
  db->Put(write_options, "key2092", "value22727");

  // op 19656: put
  db->Put(write_options, "key10684", "value8958");

  // op 19657: put
  db->Put(write_options, "key6640", "value40967");

  // op 19658: put
  db->Put(write_options, "key4490", "value47108");

  // op 19659: get
  status = db->Get(read_options, "key6713", &value);
  if (status.ok()) {
    out << "GET key6713 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19660: delete_range
  status = db->DeleteRange(write_options, "key7500", "key11114");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 19661: get
  status = db->Get(read_options, "key0804", &value);
  if (status.ok()) {
    out << "GET key0804 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19662: put
  db->Put(write_options, "key11353", "value31022");

  // op 19663: put
  db->Put(write_options, "key0527", "value39575");

  // op 19664: put
  db->Put(write_options, "key10525", "value115407");

  // op 19665: put
  db->Put(write_options, "key6047", "value66547");

  // op 19666: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8186", "key8638", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8186, key8638) size=" << scan_result.size() << std::endl;
  }

  // op 19667: put
  db->Put(write_options, "key5174", "value26815");

  // op 19668: put
  db->Put(write_options, "key5232", "value37864");

  // op 19669: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8509", "key8682", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8509, key8682) size=" << scan_result.size() << std::endl;
  }

  // op 19670: get
  status = db->Get(read_options, "key0401", &value);
  if (status.ok()) {
    out << "GET key0401 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19671: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 19672: put
  db->Put(write_options, "key11193", "value2940");

  // op 19673: get
  status = db->Get(read_options, "key6422", &value);
  if (status.ok()) {
    out << "GET key6422 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19674: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6238", "key6854", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6238, key6854) size=" << scan_result.size() << std::endl;
  }

  // op 19675: put
  db->Put(write_options, "key0449", "value8681");

  // op 19676: put
  db->Put(write_options, "key0036", "value32147");

  // op 19677: put
  db->Put(write_options, "key2913", "value114311");

  // op 19678: delete
  db->Delete(write_options, "key9623");

  // op 19679: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4665", "key6402", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4665, key6402) size=" << scan_result.size() << std::endl;
  }

  // op 19680: get
  status = db->Get(read_options, "key6030", &value);
  if (status.ok()) {
    out << "GET key6030 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19681: delete_range
  status = db->DeleteRange(write_options, "key2768", "key10236");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 19682: put
  db->Put(write_options, "key11572", "value40187");

  // op 19683: put
  db->Put(write_options, "key9127", "value107259");

  // op 19684: delete_range
  status = db->DeleteRange(write_options, "key2752", "key8253");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 19685: put
  db->Put(write_options, "key11282", "value106593");

  // op 19686: get
  status = db->Get(read_options, "key0144", &value);
  if (status.ok()) {
    out << "GET key0144 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19687: put
  db->Put(write_options, "key5643", "value94455");

  // op 19688: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1486", "key7711", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1486, key7711) size=" << scan_result.size() << std::endl;
  }

  // op 19689: delete_range
  status = db->DeleteRange(write_options, "key3496", "key11126");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 19690: put
  db->Put(write_options, "key0349", "value101596");

  // op 19691: get
  status = db->Get(read_options, "key7737", &value);
  if (status.ok()) {
    out << "GET key7737 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19692: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 19693: get
  status = db->Get(read_options, "key10534", &value);
  if (status.ok()) {
    out << "GET key10534 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19694: put
  db->Put(write_options, "key0214", "value114508");

  // op 19695: get
  status = db->Get(read_options, "key10267", &value);
  if (status.ok()) {
    out << "GET key10267 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19696: put
  db->Put(write_options, "key9417", "value58491");

  // op 19697: get
  status = db->Get(read_options, "key6446", &value);
  if (status.ok()) {
    out << "GET key6446 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19698: get
  status = db->Get(read_options, "key7488", &value);
  if (status.ok()) {
    out << "GET key7488 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19699: delete_range
  status = db->DeleteRange(write_options, "key4979", "key6279");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 19700: put
  db->Put(write_options, "key9960", "value85860");

  // op 19701: delete_range
  status = db->DeleteRange(write_options, "key3106", "key4788");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 19702: put
  db->Put(write_options, "key2250", "value22680");

  // op 19703: delete
  db->Delete(write_options, "key0158");

  // op 19704: get
  status = db->Get(read_options, "key2375", &value);
  if (status.ok()) {
    out << "GET key2375 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19705: put
  db->Put(write_options, "key11368", "value60829");

  // op 19706: get
  status = db->Get(read_options, "key7185", &value);
  if (status.ok()) {
    out << "GET key7185 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19707: put
  db->Put(write_options, "key10321", "value48979");

  // op 19708: delete_range
  status = db->DeleteRange(write_options, "key5993", "key9748");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 19709: get
  status = db->Get(read_options, "key2327", &value);
  if (status.ok()) {
    out << "GET key2327 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19710: get
  status = db->Get(read_options, "key2367", &value);
  if (status.ok()) {
    out << "GET key2367 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19711: put
  db->Put(write_options, "key3531", "value72872");

  // op 19712: get
  status = db->Get(read_options, "key10748", &value);
  if (status.ok()) {
    out << "GET key10748 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19713: put
  db->Put(write_options, "key4543", "value11983");

  // op 19714: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2321", "key8981", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2321, key8981) size=" << scan_result.size() << std::endl;
  }

  // op 19715: get
  status = db->Get(read_options, "key6148", &value);
  if (status.ok()) {
    out << "GET key6148 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19716: put
  db->Put(write_options, "key8814", "value16759");

  // op 19717: get
  status = db->Get(read_options, "key8859", &value);
  if (status.ok()) {
    out << "GET key8859 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19718: put
  db->Put(write_options, "key2398", "value113519");

  // op 19719: put
  db->Put(write_options, "key1875", "value22262");

  // op 19720: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0128", "key0150", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0128, key0150) size=" << scan_result.size() << std::endl;
  }

  // op 19721: put
  db->Put(write_options, "key9463", "value48849");

  // op 19722: delete
  db->Delete(write_options, "key5768");

  // op 19723: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6659", "key9765", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6659, key9765) size=" << scan_result.size() << std::endl;
  }

  // op 19724: put
  db->Put(write_options, "key0827", "value72678");

  // op 19725: put
  db->Put(write_options, "key0625", "value6854");

  // op 19726: delete
  db->Delete(write_options, "key3329");

  // op 19727: get
  status = db->Get(read_options, "key5898", &value);
  if (status.ok()) {
    out << "GET key5898 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19728: delete_range
  status = db->DeleteRange(write_options, "key3596", "key8723");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 19729: put
  db->Put(write_options, "key9811", "value30549");

  // op 19730: put
  db->Put(write_options, "key7314", "value111584");

  // op 19731: put
  db->Put(write_options, "key8285", "value75139");

  // op 19732: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8029", "key8786", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8029, key8786) size=" << scan_result.size() << std::endl;
  }

  // op 19733: put
  db->Put(write_options, "key8720", "value14816");

  // op 19734: put
  db->Put(write_options, "key10443", "value70953");

  // op 19735: get
  status = db->Get(read_options, "key3518", &value);
  if (status.ok()) {
    out << "GET key3518 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19736: get
  status = db->Get(read_options, "key9173", &value);
  if (status.ok()) {
    out << "GET key9173 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19737: get
  status = db->Get(read_options, "key1621", &value);
  if (status.ok()) {
    out << "GET key1621 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19738: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1510", "key2164", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1510, key2164) size=" << scan_result.size() << std::endl;
  }

  // op 19739: delete
  db->Delete(write_options, "key5965");

  // op 19740: put
  db->Put(write_options, "key7956", "value35730");

  // op 19741: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2135", "key8455", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2135, key8455) size=" << scan_result.size() << std::endl;
  }

  // op 19742: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2869", "key4709", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2869, key4709) size=" << scan_result.size() << std::endl;
  }

  // op 19743: put
  db->Put(write_options, "key10365", "value65026");

  // op 19744: put
  db->Put(write_options, "key10004", "value104865");

  // op 19745: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1969", "key2338", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1969, key2338) size=" << scan_result.size() << std::endl;
  }

  // op 19746: put
  db->Put(write_options, "key10429", "value60882");

  // op 19747: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 19748: delete_range
  status = db->DeleteRange(write_options, "key6247", "key9234");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 19749: get
  status = db->Get(read_options, "key6322", &value);
  if (status.ok()) {
    out << "GET key6322 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19750: put
  db->Put(write_options, "key3901", "value82198");

  // op 19751: get
  status = db->Get(read_options, "key3619", &value);
  if (status.ok()) {
    out << "GET key3619 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19752: delete
  db->Delete(write_options, "key1954");

  // op 19753: get
  status = db->Get(read_options, "key0135", &value);
  if (status.ok()) {
    out << "GET key0135 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19754: get
  status = db->Get(read_options, "key1098", &value);
  if (status.ok()) {
    out << "GET key1098 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19755: delete
  db->Delete(write_options, "key4573");

  // op 19756: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1845", "key2389", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1845, key2389) size=" << scan_result.size() << std::endl;
  }

  // op 19757: delete_range
  status = db->DeleteRange(write_options, "key2705", "key6565");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 19758: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3076", "key6611", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3076, key6611) size=" << scan_result.size() << std::endl;
  }

  // op 19759: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 19760: put
  db->Put(write_options, "key0824", "value94925");

  // op 19761: put
  db->Put(write_options, "key7762", "value12231");

  // op 19762: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7221", "key8248", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7221, key8248) size=" << scan_result.size() << std::endl;
  }

  // op 19763: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8259", "key9163", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8259, key9163) size=" << scan_result.size() << std::endl;
  }

  // op 19764: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2697", "key5501", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2697, key5501) size=" << scan_result.size() << std::endl;
  }

  // op 19765: get
  status = db->Get(read_options, "key5811", &value);
  if (status.ok()) {
    out << "GET key5811 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19766: put
  db->Put(write_options, "key0914", "value98621");

  // op 19767: put
  db->Put(write_options, "key0207", "value49779");

  // op 19768: delete_range
  status = db->DeleteRange(write_options, "key1300", "key4892");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 19769: put
  db->Put(write_options, "key0060", "value99957");

  // op 19770: put
  db->Put(write_options, "key9228", "value46219");

  // op 19771: delete_range
  status = db->DeleteRange(write_options, "key1423", "key10255");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 19772: put
  db->Put(write_options, "key11315", "value106122");

  // op 19773: delete_range
  status = db->DeleteRange(write_options, "key2348", "key5318");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 19774: put
  db->Put(write_options, "key11517", "value63786");

  // op 19775: put
  db->Put(write_options, "key5363", "value70827");

  // op 19776: delete_range
  status = db->DeleteRange(write_options, "key5042", "key6499");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 19777: delete
  db->Delete(write_options, "key3561");

  // op 19778: get
  status = db->Get(read_options, "key1238", &value);
  if (status.ok()) {
    out << "GET key1238 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19779: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2296", "key4276", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2296, key4276) size=" << scan_result.size() << std::endl;
  }

  // op 19780: delete
  db->Delete(write_options, "key2536");

  // op 19781: put
  db->Put(write_options, "key5556", "value102281");

  // op 19782: delete_range
  status = db->DeleteRange(write_options, "key3674", "key3751");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 19783: get
  status = db->Get(read_options, "key7911", &value);
  if (status.ok()) {
    out << "GET key7911 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19784: delete
  db->Delete(write_options, "key3259");

  // op 19785: get
  status = db->Get(read_options, "key0375", &value);
  if (status.ok()) {
    out << "GET key0375 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19786: delete_range
  status = db->DeleteRange(write_options, "key5475", "key10769");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 19787: get
  status = db->Get(read_options, "key9425", &value);
  if (status.ok()) {
    out << "GET key9425 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19788: put
  db->Put(write_options, "key9694", "value95389");

  // op 19789: put
  db->Put(write_options, "key9169", "value86089");

  // op 19790: delete_range
  status = db->DeleteRange(write_options, "key5215", "key5985");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 19791: get
  status = db->Get(read_options, "key8562", &value);
  if (status.ok()) {
    out << "GET key8562 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19792: get
  status = db->Get(read_options, "key7259", &value);
  if (status.ok()) {
    out << "GET key7259 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19793: put
  db->Put(write_options, "key8709", "value8162");

  // op 19794: put
  db->Put(write_options, "key0497", "value110385");

  // op 19795: get
  status = db->Get(read_options, "key4451", &value);
  if (status.ok()) {
    out << "GET key4451 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19796: delete_range
  status = db->DeleteRange(write_options, "key8139", "key10743");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 19797: get
  status = db->Get(read_options, "key5120", &value);
  if (status.ok()) {
    out << "GET key5120 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19798: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4525", "key10950", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4525, key10950) size=" << scan_result.size() << std::endl;
  }

  // op 19799: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8231", "key11579", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8231, key11579) size=" << scan_result.size() << std::endl;
  }

  // op 19800: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2240", "key7317", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2240, key7317) size=" << scan_result.size() << std::endl;
  }

  // op 19801: put
  db->Put(write_options, "key4602", "value46610");

  // op 19802: get
  status = db->Get(read_options, "key4260", &value);
  if (status.ok()) {
    out << "GET key4260 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19803: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4823", "key11431", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4823, key11431) size=" << scan_result.size() << std::endl;
  }

  // op 19804: put
  db->Put(write_options, "key7081", "value104141");

  // op 19805: put
  db->Put(write_options, "key0912", "value109517");

  // op 19806: put
  db->Put(write_options, "key10076", "value106233");

  // op 19807: put
  db->Put(write_options, "key1677", "value7220");

  // op 19808: put
  db->Put(write_options, "key5799", "value11015");

  // op 19809: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7620", "key8111", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7620, key8111) size=" << scan_result.size() << std::endl;
  }

  // op 19810: get
  status = db->Get(read_options, "key1054", &value);
  if (status.ok()) {
    out << "GET key1054 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19811: put
  db->Put(write_options, "key4943", "value87703");

  // op 19812: put
  db->Put(write_options, "key3585", "value75954");

  // op 19813: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6405", "key7047", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6405, key7047) size=" << scan_result.size() << std::endl;
  }

  // op 19814: get
  status = db->Get(read_options, "key0429", &value);
  if (status.ok()) {
    out << "GET key0429 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19815: get
  status = db->Get(read_options, "key2012", &value);
  if (status.ok()) {
    out << "GET key2012 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19816: put
  db->Put(write_options, "key3347", "value11419");

  // op 19817: put
  db->Put(write_options, "key4934", "value51616");

  // op 19818: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9538", "key11222", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9538, key11222) size=" << scan_result.size() << std::endl;
  }

  // op 19819: put
  db->Put(write_options, "key2779", "value9474");

  // op 19820: delete
  db->Delete(write_options, "key3669");

  // op 19821: get
  status = db->Get(read_options, "key0145", &value);
  if (status.ok()) {
    out << "GET key0145 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19822: get
  status = db->Get(read_options, "key8117", &value);
  if (status.ok()) {
    out << "GET key8117 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19823: delete
  db->Delete(write_options, "key5615");

  // op 19824: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6442", "key9360", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6442, key9360) size=" << scan_result.size() << std::endl;
  }

  // op 19825: put
  db->Put(write_options, "key7152", "value40535");

  // op 19826: delete_range
  status = db->DeleteRange(write_options, "key0115", "key4916");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 19827: put
  db->Put(write_options, "key2944", "value72061");

  // op 19828: get
  status = db->Get(read_options, "key6219", &value);
  if (status.ok()) {
    out << "GET key6219 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19829: put
  db->Put(write_options, "key5085", "value73571");

  // op 19830: get
  status = db->Get(read_options, "key2047", &value);
  if (status.ok()) {
    out << "GET key2047 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19831: put
  db->Put(write_options, "key6579", "value6050");

  // op 19832: put
  db->Put(write_options, "key1182", "value106897");

  // op 19833: put
  db->Put(write_options, "key7093", "value82168");

  // op 19834: put
  db->Put(write_options, "key10838", "value99348");

  // op 19835: put
  db->Put(write_options, "key5940", "value82195");

  // op 19836: put
  db->Put(write_options, "key0723", "value0807");

  // op 19837: delete
  db->Delete(write_options, "key0673");

  // op 19838: delete_range
  status = db->DeleteRange(write_options, "key5249", "key6391");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 19839: delete
  db->Delete(write_options, "key4071");

  // op 19840: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3059", "key7646", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3059, key7646) size=" << scan_result.size() << std::endl;
  }

  // op 19841: put
  db->Put(write_options, "key1409", "value85737");

  // op 19842: put
  db->Put(write_options, "key6433", "value41050");

  // op 19843: get
  status = db->Get(read_options, "key8930", &value);
  if (status.ok()) {
    out << "GET key8930 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19844: put
  db->Put(write_options, "key11169", "value26310");

  // op 19845: put
  db->Put(write_options, "key7630", "value22408");

  // op 19846: put
  db->Put(write_options, "key5963", "value29380");

  // op 19847: put
  db->Put(write_options, "key0625", "value36850");

  // op 19848: put
  db->Put(write_options, "key4307", "value80242");

  // op 19849: put
  db->Put(write_options, "key9201", "value22440");

  // op 19850: put
  db->Put(write_options, "key7039", "value96124");

  // op 19851: delete
  db->Delete(write_options, "key1894");

  // op 19852: put
  db->Put(write_options, "key11184", "value66966");

  // op 19853: put
  db->Put(write_options, "key6189", "value51268");

  // op 19854: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7689", "key10102", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7689, key10102) size=" << scan_result.size() << std::endl;
  }

  // op 19855: get
  status = db->Get(read_options, "key4782", &value);
  if (status.ok()) {
    out << "GET key4782 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19856: delete
  db->Delete(write_options, "key7706");

  // op 19857: get
  status = db->Get(read_options, "key10531", &value);
  if (status.ok()) {
    out << "GET key10531 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19858: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 19859: put
  db->Put(write_options, "key9223", "value48257");

  // op 19860: get
  status = db->Get(read_options, "key2066", &value);
  if (status.ok()) {
    out << "GET key2066 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19861: get
  status = db->Get(read_options, "key6133", &value);
  if (status.ok()) {
    out << "GET key6133 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19862: put
  db->Put(write_options, "key0770", "value33007");

  // op 19863: put
  db->Put(write_options, "key5627", "value30671");

  // op 19864: put
  db->Put(write_options, "key9065", "value110628");

  // op 19865: delete
  db->Delete(write_options, "key4511");

  // op 19866: put
  db->Put(write_options, "key4538", "value107886");

  // op 19867: put
  db->Put(write_options, "key6347", "value93481");

  // op 19868: delete_range
  status = db->DeleteRange(write_options, "key5445", "key11465");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 19869: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 19870: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5959", "key7647", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5959, key7647) size=" << scan_result.size() << std::endl;
  }

  // op 19871: put
  db->Put(write_options, "key10451", "value35204");

  // op 19872: delete
  db->Delete(write_options, "key0316");

  // op 19873: put
  db->Put(write_options, "key10342", "value36505");

  // op 19874: put
  db->Put(write_options, "key3526", "value80670");

  // op 19875: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 19876: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4709", "key9040", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4709, key9040) size=" << scan_result.size() << std::endl;
  }

  // op 19877: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0709", "key7495", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0709, key7495) size=" << scan_result.size() << std::endl;
  }

  // op 19878: put
  db->Put(write_options, "key3133", "value21515");

  // op 19879: put
  db->Put(write_options, "key4288", "value93846");

  // op 19880: get
  status = db->Get(read_options, "key2652", &value);
  if (status.ok()) {
    out << "GET key2652 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19881: delete_range
  status = db->DeleteRange(write_options, "key0173", "key0258");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 19882: put
  db->Put(write_options, "key3049", "value17017");

  // op 19883: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5194", "key8085", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5194, key8085) size=" << scan_result.size() << std::endl;
  }

  // op 19884: put
  db->Put(write_options, "key3285", "value44761");

  // op 19885: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0199", "key11573", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0199, key11573) size=" << scan_result.size() << std::endl;
  }

  // op 19886: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1045", "key11633", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1045, key11633) size=" << scan_result.size() << std::endl;
  }

  // op 19887: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8739", "key10675", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8739, key10675) size=" << scan_result.size() << std::endl;
  }

  // op 19888: delete_range
  status = db->DeleteRange(write_options, "key6456", "key9996");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 19889: delete_range
  status = db->DeleteRange(write_options, "key4430", "key9502");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 19890: put
  db->Put(write_options, "key6257", "value98490");

  // op 19891: put
  db->Put(write_options, "key6177", "value25364");

  // op 19892: get
  status = db->Get(read_options, "key5702", &value);
  if (status.ok()) {
    out << "GET key5702 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19893: get
  status = db->Get(read_options, "key8984", &value);
  if (status.ok()) {
    out << "GET key8984 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19894: put
  db->Put(write_options, "key2131", "value44970");

  // op 19895: delete_range
  status = db->DeleteRange(write_options, "key0914", "key6545");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 19896: delete_range
  status = db->DeleteRange(write_options, "key3121", "key5380");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 19897: get
  status = db->Get(read_options, "key8283", &value);
  if (status.ok()) {
    out << "GET key8283 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19898: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8130", "key10275", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8130, key10275) size=" << scan_result.size() << std::endl;
  }

  // op 19899: get
  status = db->Get(read_options, "key9937", &value);
  if (status.ok()) {
    out << "GET key9937 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19900: get
  status = db->Get(read_options, "key4162", &value);
  if (status.ok()) {
    out << "GET key4162 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19901: put
  db->Put(write_options, "key0620", "value21449");

  // op 19902: put
  db->Put(write_options, "key8108", "value23947");

  // op 19903: put
  db->Put(write_options, "key10994", "value95629");

  // op 19904: put
  db->Put(write_options, "key6286", "value58413");

  // op 19905: delete_range
  status = db->DeleteRange(write_options, "key1642", "key5118");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 19906: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5532", "key6871", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5532, key6871) size=" << scan_result.size() << std::endl;
  }

  // op 19907: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 19908: delete_range
  status = db->DeleteRange(write_options, "key4813", "key5930");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 19909: get
  status = db->Get(read_options, "key2759", &value);
  if (status.ok()) {
    out << "GET key2759 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19910: put
  db->Put(write_options, "key0205", "value96944");

  // op 19911: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1034", "key2979", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1034, key2979) size=" << scan_result.size() << std::endl;
  }

  // op 19912: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 19913: put
  db->Put(write_options, "key1637", "value6907");

  // op 19914: put
  db->Put(write_options, "key3267", "value76221");

  // op 19915: put
  db->Put(write_options, "key8746", "value39422");

  // op 19916: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 19917: put
  db->Put(write_options, "key11365", "value39095");

  // op 19918: delete_range
  status = db->DeleteRange(write_options, "key8888", "key9790");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 19919: delete_range
  status = db->DeleteRange(write_options, "key2903", "key9115");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 19920: get
  status = db->Get(read_options, "key7551", &value);
  if (status.ok()) {
    out << "GET key7551 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19921: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5630", "key5731", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5630, key5731) size=" << scan_result.size() << std::endl;
  }

  // op 19922: get
  status = db->Get(read_options, "key3166", &value);
  if (status.ok()) {
    out << "GET key3166 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19923: delete_range
  status = db->DeleteRange(write_options, "key2108", "key2470");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 19924: delete
  db->Delete(write_options, "key4400");

  // op 19925: get
  status = db->Get(read_options, "key4927", &value);
  if (status.ok()) {
    out << "GET key4927 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19926: delete
  db->Delete(write_options, "key6622");

  // op 19927: get
  status = db->Get(read_options, "key6329", &value);
  if (status.ok()) {
    out << "GET key6329 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19928: delete_range
  status = db->DeleteRange(write_options, "key3923", "key9220");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 19929: put
  db->Put(write_options, "key0186", "value14951");

  // op 19930: delete
  db->Delete(write_options, "key5862");

  // op 19931: put
  db->Put(write_options, "key7125", "value34111");

  // op 19932: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 19933: put
  db->Put(write_options, "key5742", "value41688");

  // op 19934: get
  status = db->Get(read_options, "key8733", &value);
  if (status.ok()) {
    out << "GET key8733 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19935: get
  status = db->Get(read_options, "key0681", &value);
  if (status.ok()) {
    out << "GET key0681 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19936: put
  db->Put(write_options, "key2858", "value107110");

  // op 19937: get
  status = db->Get(read_options, "key0836", &value);
  if (status.ok()) {
    out << "GET key0836 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19938: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4763", "key9722", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4763, key9722) size=" << scan_result.size() << std::endl;
  }

  // op 19939: get
  status = db->Get(read_options, "key10307", &value);
  if (status.ok()) {
    out << "GET key10307 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19940: get
  status = db->Get(read_options, "key1194", &value);
  if (status.ok()) {
    out << "GET key1194 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19941: get
  status = db->Get(read_options, "key7030", &value);
  if (status.ok()) {
    out << "GET key7030 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19942: put
  db->Put(write_options, "key3663", "value101519");

  // op 19943: get
  status = db->Get(read_options, "key5011", &value);
  if (status.ok()) {
    out << "GET key5011 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19944: put
  db->Put(write_options, "key1151", "value18716");

  // op 19945: put
  db->Put(write_options, "key6602", "value41248");

  // op 19946: put
  db->Put(write_options, "key4679", "value92267");

  // op 19947: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3289", "key7290", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3289, key7290) size=" << scan_result.size() << std::endl;
  }

  // op 19948: put
  db->Put(write_options, "key2549", "value77164");

  // op 19949: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 19950: put
  db->Put(write_options, "key9898", "value94703");

  // op 19951: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0399", "key2329", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0399, key2329) size=" << scan_result.size() << std::endl;
  }

  // op 19952: delete_range
  status = db->DeleteRange(write_options, "key0727", "key7341");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 19953: get
  status = db->Get(read_options, "key9970", &value);
  if (status.ok()) {
    out << "GET key9970 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19954: put
  db->Put(write_options, "key7577", "value21360");

  // op 19955: get
  status = db->Get(read_options, "key2669", &value);
  if (status.ok()) {
    out << "GET key2669 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19956: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7031", "key11349", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7031, key11349) size=" << scan_result.size() << std::endl;
  }

  // op 19957: put
  db->Put(write_options, "key3298", "value96102");

  // op 19958: put
  db->Put(write_options, "key5422", "value90560");

  // op 19959: put
  db->Put(write_options, "key7039", "value111018");

  // op 19960: put
  db->Put(write_options, "key0616", "value110969");

  // op 19961: put
  db->Put(write_options, "key2758", "value27921");

  // op 19962: put
  db->Put(write_options, "key7855", "value73282");

  // op 19963: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0710", "key2251", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0710, key2251) size=" << scan_result.size() << std::endl;
  }

  // op 19964: delete
  db->Delete(write_options, "key2456");

  // op 19965: get
  status = db->Get(read_options, "key11293", &value);
  if (status.ok()) {
    out << "GET key11293 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19966: delete_range
  status = db->DeleteRange(write_options, "key9017", "key11079");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 19967: get
  status = db->Get(read_options, "key10238", &value);
  if (status.ok()) {
    out << "GET key10238 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19968: put
  db->Put(write_options, "key9284", "value15063");

  // op 19969: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4204", "key10214", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4204, key10214) size=" << scan_result.size() << std::endl;
  }

  // op 19970: put
  db->Put(write_options, "key8375", "value50935");

  // op 19971: put
  db->Put(write_options, "key7893", "value72233");

  // op 19972: put
  db->Put(write_options, "key3404", "value99586");

  // op 19973: get
  status = db->Get(read_options, "key2756", &value);
  if (status.ok()) {
    out << "GET key2756 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19974: delete
  db->Delete(write_options, "key0716");

  // op 19975: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2718", "key7858", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2718, key7858) size=" << scan_result.size() << std::endl;
  }

  // op 19976: get
  status = db->Get(read_options, "key11125", &value);
  if (status.ok()) {
    out << "GET key11125 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19977: get
  status = db->Get(read_options, "key2837", &value);
  if (status.ok()) {
    out << "GET key2837 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19978: get
  status = db->Get(read_options, "key2763", &value);
  if (status.ok()) {
    out << "GET key2763 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19979: put
  db->Put(write_options, "key7487", "value58725");

  // op 19980: put
  db->Put(write_options, "key2383", "value10573");

  // op 19981: put
  db->Put(write_options, "key10569", "value2593");

  // op 19982: put
  db->Put(write_options, "key8998", "value15710");

  // op 19983: delete
  db->Delete(write_options, "key2281");

  // op 19984: get
  status = db->Get(read_options, "key6394", &value);
  if (status.ok()) {
    out << "GET key6394 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19985: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1472", "key5835", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1472, key5835) size=" << scan_result.size() << std::endl;
  }

  // op 19986: put
  db->Put(write_options, "key3375", "value83496");

  // op 19987: put
  db->Put(write_options, "key8986", "value93701");

  // op 19988: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1336", "key6319", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1336, key6319) size=" << scan_result.size() << std::endl;
  }

  // op 19989: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8327", "key8627", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8327, key8627) size=" << scan_result.size() << std::endl;
  }

  // op 19990: delete_range
  status = db->DeleteRange(write_options, "key1079", "key9801");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 19991: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0862", "key11475", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0862, key11475) size=" << scan_result.size() << std::endl;
  }

  // op 19992: get
  status = db->Get(read_options, "key1371", &value);
  if (status.ok()) {
    out << "GET key1371 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 19993: put
  db->Put(write_options, "key4854", "value84542");

  // op 19994: put
  db->Put(write_options, "key9895", "value80686");

  // op 19995: put
  db->Put(write_options, "key9010", "value83599");

  // op 19996: put
  db->Put(write_options, "key7465", "value62218");

  // op 19997: delete
  db->Delete(write_options, "key7388");

  // op 19998: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 19999: put
  db->Put(write_options, "key9861", "value50518");

  // op 20000: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4356", "key6664", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4356, key6664) size=" << scan_result.size() << std::endl;
  }

  // op 20001: get
  status = db->Get(read_options, "key2211", &value);
  if (status.ok()) {
    out << "GET key2211 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20002: put
  db->Put(write_options, "key8020", "value111103");

  // op 20003: put
  db->Put(write_options, "key1425", "value11789");

  // op 20004: get
  status = db->Get(read_options, "key9556", &value);
  if (status.ok()) {
    out << "GET key9556 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20005: put
  db->Put(write_options, "key2149", "value13866");

  // op 20006: put
  db->Put(write_options, "key10770", "value30711");

  // op 20007: get
  status = db->Get(read_options, "key2560", &value);
  if (status.ok()) {
    out << "GET key2560 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20008: put
  db->Put(write_options, "key9741", "value32003");

  // op 20009: delete
  db->Delete(write_options, "key8907");

  // op 20010: put
  db->Put(write_options, "key1894", "value10166");

  // op 20011: put
  db->Put(write_options, "key0565", "value111445");

  // op 20012: put
  db->Put(write_options, "key7771", "value28343");

  // op 20013: put
  db->Put(write_options, "key6146", "value32399");

  // op 20014: put
  db->Put(write_options, "key3906", "value78314");

  // op 20015: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2024", "key2526", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2024, key2526) size=" << scan_result.size() << std::endl;
  }

  // op 20016: delete_range
  status = db->DeleteRange(write_options, "key9196", "key10368");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 20017: put
  db->Put(write_options, "key8218", "value16620");

  // op 20018: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0865", "key4510", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0865, key4510) size=" << scan_result.size() << std::endl;
  }

  // op 20019: get
  status = db->Get(read_options, "key9013", &value);
  if (status.ok()) {
    out << "GET key9013 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20020: get
  status = db->Get(read_options, "key4897", &value);
  if (status.ok()) {
    out << "GET key4897 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20021: get
  status = db->Get(read_options, "key2671", &value);
  if (status.ok()) {
    out << "GET key2671 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20022: get
  status = db->Get(read_options, "key1001", &value);
  if (status.ok()) {
    out << "GET key1001 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20023: get
  status = db->Get(read_options, "key6548", &value);
  if (status.ok()) {
    out << "GET key6548 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20024: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3910", "key8097", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3910, key8097) size=" << scan_result.size() << std::endl;
  }

  // op 20025: put
  db->Put(write_options, "key3232", "value33899");

  // op 20026: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0498", "key0517", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0498, key0517) size=" << scan_result.size() << std::endl;
  }

  // op 20027: delete
  db->Delete(write_options, "key9332");

  // op 20028: delete
  db->Delete(write_options, "key3380");

  // op 20029: delete_range
  status = db->DeleteRange(write_options, "key3074", "key11311");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 20030: put
  db->Put(write_options, "key2180", "value107650");

  // op 20031: delete
  db->Delete(write_options, "key7226");

  // op 20032: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3139", "key4539", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3139, key4539) size=" << scan_result.size() << std::endl;
  }

  // op 20033: put
  db->Put(write_options, "key2325", "value87213");

  // op 20034: get
  status = db->Get(read_options, "key10997", &value);
  if (status.ok()) {
    out << "GET key10997 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20035: get
  status = db->Get(read_options, "key11105", &value);
  if (status.ok()) {
    out << "GET key11105 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20036: put
  db->Put(write_options, "key9922", "value88737");

  // op 20037: get
  status = db->Get(read_options, "key11177", &value);
  if (status.ok()) {
    out << "GET key11177 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20038: get
  status = db->Get(read_options, "key0937", &value);
  if (status.ok()) {
    out << "GET key0937 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20039: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2392", "key6134", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2392, key6134) size=" << scan_result.size() << std::endl;
  }

  // op 20040: put
  db->Put(write_options, "key9235", "value61061");

  // op 20041: get
  status = db->Get(read_options, "key8214", &value);
  if (status.ok()) {
    out << "GET key8214 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20042: delete_range
  status = db->DeleteRange(write_options, "key4204", "key4270");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 20043: put
  db->Put(write_options, "key5618", "value58729");

  // op 20044: put
  db->Put(write_options, "key10235", "value91633");

  // op 20045: get
  status = db->Get(read_options, "key1801", &value);
  if (status.ok()) {
    out << "GET key1801 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20046: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8470", "key9818", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8470, key9818) size=" << scan_result.size() << std::endl;
  }

  // op 20047: put
  db->Put(write_options, "key10474", "value116459");

  // op 20048: put
  db->Put(write_options, "key6593", "value50616");

  // op 20049: delete
  db->Delete(write_options, "key8633");

  // op 20050: put
  db->Put(write_options, "key8333", "value3189");

  // op 20051: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0780", "key3123", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0780, key3123) size=" << scan_result.size() << std::endl;
  }

  // op 20052: get
  status = db->Get(read_options, "key11256", &value);
  if (status.ok()) {
    out << "GET key11256 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20053: delete_range
  status = db->DeleteRange(write_options, "key7374", "key11323");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 20054: delete_range
  status = db->DeleteRange(write_options, "key2603", "key6930");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 20055: put
  db->Put(write_options, "key8653", "value42951");

  // op 20056: get
  status = db->Get(read_options, "key9137", &value);
  if (status.ok()) {
    out << "GET key9137 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20057: get
  status = db->Get(read_options, "key1155", &value);
  if (status.ok()) {
    out << "GET key1155 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20058: delete_range
  status = db->DeleteRange(write_options, "key3029", "key4360");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 20059: put
  db->Put(write_options, "key7826", "value57729");

  // op 20060: put
  db->Put(write_options, "key8349", "value52427");

  // op 20061: put
  db->Put(write_options, "key6153", "value19612");

  // op 20062: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4516", "key9625", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4516, key9625) size=" << scan_result.size() << std::endl;
  }

  // op 20063: get
  status = db->Get(read_options, "key5525", &value);
  if (status.ok()) {
    out << "GET key5525 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20064: delete
  db->Delete(write_options, "key6026");

  // op 20065: put
  db->Put(write_options, "key10591", "value107291");

  // op 20066: put
  db->Put(write_options, "key1259", "value65178");

  // op 20067: put
  db->Put(write_options, "key8938", "value0786");

  // op 20068: delete
  db->Delete(write_options, "key7538");

  // op 20069: delete
  db->Delete(write_options, "key7949");

  // op 20070: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3103", "key7967", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3103, key7967) size=" << scan_result.size() << std::endl;
  }

  // op 20071: put
  db->Put(write_options, "key3380", "value41658");

  // op 20072: delete
  db->Delete(write_options, "key11288");

  // op 20073: delete_range
  status = db->DeleteRange(write_options, "key0626", "key5464");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 20074: delete_range
  status = db->DeleteRange(write_options, "key5779", "key6671");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 20075: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4902", "key10484", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4902, key10484) size=" << scan_result.size() << std::endl;
  }

  // op 20076: put
  db->Put(write_options, "key9574", "value7202");

  // op 20077: get
  status = db->Get(read_options, "key8279", &value);
  if (status.ok()) {
    out << "GET key8279 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20078: put
  db->Put(write_options, "key0087", "value23473");

  // op 20079: delete
  db->Delete(write_options, "key8876");

  // op 20080: put
  db->Put(write_options, "key7003", "value28434");

  // op 20081: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7203", "key7663", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7203, key7663) size=" << scan_result.size() << std::endl;
  }

  // op 20082: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3548", "key6930", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3548, key6930) size=" << scan_result.size() << std::endl;
  }

  // op 20083: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 20084: put
  db->Put(write_options, "key5012", "value82885");

  // op 20085: get
  status = db->Get(read_options, "key11364", &value);
  if (status.ok()) {
    out << "GET key11364 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20086: put
  db->Put(write_options, "key3511", "value53466");

  // op 20087: put
  db->Put(write_options, "key2449", "value76633");

  // op 20088: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 20089: put
  db->Put(write_options, "key0747", "value82747");

  // op 20090: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4970", "key6837", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4970, key6837) size=" << scan_result.size() << std::endl;
  }

  // op 20091: delete_range
  status = db->DeleteRange(write_options, "key7010", "key8726");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 20092: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1459", "key2527", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1459, key2527) size=" << scan_result.size() << std::endl;
  }

  // op 20093: get
  status = db->Get(read_options, "key7743", &value);
  if (status.ok()) {
    out << "GET key7743 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20094: put
  db->Put(write_options, "key2646", "value16742");

  // op 20095: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1236", "key10771", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1236, key10771) size=" << scan_result.size() << std::endl;
  }

  // op 20096: delete
  db->Delete(write_options, "key8282");

  // op 20097: delete_range
  status = db->DeleteRange(write_options, "key0391", "key5763");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 20098: put
  db->Put(write_options, "key8479", "value3036");

  // op 20099: delete
  db->Delete(write_options, "key4435");

  // op 20100: get
  status = db->Get(read_options, "key6550", &value);
  if (status.ok()) {
    out << "GET key6550 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20101: delete
  db->Delete(write_options, "key3792");

  // op 20102: delete
  db->Delete(write_options, "key6127");

  // op 20103: get
  status = db->Get(read_options, "key0054", &value);
  if (status.ok()) {
    out << "GET key0054 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20104: put
  db->Put(write_options, "key5945", "value95276");

  // op 20105: put
  db->Put(write_options, "key10401", "value76339");

  // op 20106: delete_range
  status = db->DeleteRange(write_options, "key9544", "key9858");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 20107: delete_range
  status = db->DeleteRange(write_options, "key3316", "key3976");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 20108: delete_range
  status = db->DeleteRange(write_options, "key1327", "key5840");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 20109: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 20110: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1392", "key8590", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1392, key8590) size=" << scan_result.size() << std::endl;
  }

  // op 20111: delete_range
  status = db->DeleteRange(write_options, "key0099", "key2601");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 20112: delete
  db->Delete(write_options, "key1411");

  // op 20113: put
  db->Put(write_options, "key10951", "value116829");

  // op 20114: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0339", "key6424", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0339, key6424) size=" << scan_result.size() << std::endl;
  }

  // op 20115: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4373", "key7571", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4373, key7571) size=" << scan_result.size() << std::endl;
  }

  // op 20116: put
  db->Put(write_options, "key5743", "value22434");

  // op 20117: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5686", "key11549", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5686, key11549) size=" << scan_result.size() << std::endl;
  }

  // op 20118: put
  db->Put(write_options, "key11183", "value63249");

  // op 20119: get
  status = db->Get(read_options, "key10539", &value);
  if (status.ok()) {
    out << "GET key10539 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20120: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 20121: put
  db->Put(write_options, "key2713", "value43803");

  // op 20122: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2023", "key8417", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2023, key8417) size=" << scan_result.size() << std::endl;
  }

  // op 20123: put
  db->Put(write_options, "key0460", "value93807");

  // op 20124: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1729", "key4623", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1729, key4623) size=" << scan_result.size() << std::endl;
  }

  // op 20125: put
  db->Put(write_options, "key5787", "value71753");

  // op 20126: delete_range
  status = db->DeleteRange(write_options, "key2319", "key8629");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 20127: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4383", "key5356", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4383, key5356) size=" << scan_result.size() << std::endl;
  }

  // op 20128: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 20129: delete
  db->Delete(write_options, "key8876");

  // op 20130: delete_range
  status = db->DeleteRange(write_options, "key2412", "key10806");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 20131: put
  db->Put(write_options, "key3795", "value103416");

  // op 20132: put
  db->Put(write_options, "key6404", "value23005");

  // op 20133: put
  db->Put(write_options, "key11720", "value76631");

  // op 20134: put
  db->Put(write_options, "key8707", "value35701");

  // op 20135: get
  status = db->Get(read_options, "key3364", &value);
  if (status.ok()) {
    out << "GET key3364 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20136: put
  db->Put(write_options, "key10106", "value26876");

  // op 20137: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1411", "key2770", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1411, key2770) size=" << scan_result.size() << std::endl;
  }

  // op 20138: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2652", "key6059", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2652, key6059) size=" << scan_result.size() << std::endl;
  }

  // op 20139: put
  db->Put(write_options, "key10710", "value1447");

  // op 20140: get
  status = db->Get(read_options, "key7124", &value);
  if (status.ok()) {
    out << "GET key7124 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20141: put
  db->Put(write_options, "key7255", "value86035");

  // op 20142: put
  db->Put(write_options, "key5966", "value16677");

  // op 20143: put
  db->Put(write_options, "key4445", "value10782");

  // op 20144: put
  db->Put(write_options, "key8422", "value27715");

  // op 20145: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 20146: delete
  db->Delete(write_options, "key8765");

  // op 20147: put
  db->Put(write_options, "key10802", "value66747");

  // op 20148: get
  status = db->Get(read_options, "key9866", &value);
  if (status.ok()) {
    out << "GET key9866 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20149: put
  db->Put(write_options, "key2714", "value33563");

  // op 20150: put
  db->Put(write_options, "key5950", "value75449");

  // op 20151: put
  db->Put(write_options, "key11563", "value83096");

  // op 20152: put
  db->Put(write_options, "key7318", "value100567");

  // op 20153: delete
  db->Delete(write_options, "key9120");

  // op 20154: put
  db->Put(write_options, "key11148", "value18694");

  // op 20155: delete
  db->Delete(write_options, "key6727");

  // op 20156: put
  db->Put(write_options, "key5323", "value105616");

  // op 20157: get
  status = db->Get(read_options, "key1595", &value);
  if (status.ok()) {
    out << "GET key1595 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20158: put
  db->Put(write_options, "key3731", "value63635");

  // op 20159: get
  status = db->Get(read_options, "key6212", &value);
  if (status.ok()) {
    out << "GET key6212 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20160: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 20161: delete
  db->Delete(write_options, "key1295");

  // op 20162: put
  db->Put(write_options, "key4786", "value54790");

  // op 20163: put
  db->Put(write_options, "key7163", "value37047");

  // op 20164: get
  status = db->Get(read_options, "key1794", &value);
  if (status.ok()) {
    out << "GET key1794 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20165: put
  db->Put(write_options, "key5515", "value86887");

  // op 20166: get
  status = db->Get(read_options, "key2611", &value);
  if (status.ok()) {
    out << "GET key2611 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20167: delete_range
  status = db->DeleteRange(write_options, "key4695", "key5059");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 20168: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5462", "key10488", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5462, key10488) size=" << scan_result.size() << std::endl;
  }

  // op 20169: get
  status = db->Get(read_options, "key2358", &value);
  if (status.ok()) {
    out << "GET key2358 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20170: delete_range
  status = db->DeleteRange(write_options, "key5874", "key10981");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 20171: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1404", "key8914", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1404, key8914) size=" << scan_result.size() << std::endl;
  }

  // op 20172: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0562", "key1764", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0562, key1764) size=" << scan_result.size() << std::endl;
  }

  // op 20173: put
  db->Put(write_options, "key10798", "value108439");

  // op 20174: put
  db->Put(write_options, "key10245", "value114540");

  // op 20175: get
  status = db->Get(read_options, "key9534", &value);
  if (status.ok()) {
    out << "GET key9534 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20176: put
  db->Put(write_options, "key7657", "value34309");

  // op 20177: get
  status = db->Get(read_options, "key1965", &value);
  if (status.ok()) {
    out << "GET key1965 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20178: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0211", "key6055", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0211, key6055) size=" << scan_result.size() << std::endl;
  }

  // op 20179: put
  db->Put(write_options, "key5469", "value96736");

  // op 20180: delete
  db->Delete(write_options, "key3300");

  // op 20181: put
  db->Put(write_options, "key2214", "value107394");

  // op 20182: put
  db->Put(write_options, "key3808", "value36056");

  // op 20183: put
  db->Put(write_options, "key0336", "value1433");

  // op 20184: delete
  db->Delete(write_options, "key10968");

  // op 20185: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4330", "key7974", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4330, key7974) size=" << scan_result.size() << std::endl;
  }

  // op 20186: get
  status = db->Get(read_options, "key7689", &value);
  if (status.ok()) {
    out << "GET key7689 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20187: put
  db->Put(write_options, "key4030", "value111418");

  // op 20188: put
  db->Put(write_options, "key5975", "value88760");

  // op 20189: put
  db->Put(write_options, "key0682", "value14223");

  // op 20190: put
  db->Put(write_options, "key9090", "value26229");

  // op 20191: put
  db->Put(write_options, "key3385", "value27155");

  // op 20192: put
  db->Put(write_options, "key10559", "value53954");

  // op 20193: get
  status = db->Get(read_options, "key7049", &value);
  if (status.ok()) {
    out << "GET key7049 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20194: get
  status = db->Get(read_options, "key0534", &value);
  if (status.ok()) {
    out << "GET key0534 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20195: put
  db->Put(write_options, "key10208", "value99813");

  // op 20196: delete_range
  status = db->DeleteRange(write_options, "key6315", "key6707");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 20197: put
  db->Put(write_options, "key6951", "value104589");

  // op 20198: get
  status = db->Get(read_options, "key5101", &value);
  if (status.ok()) {
    out << "GET key5101 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20199: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 20200: put
  db->Put(write_options, "key7656", "value8587");

  // op 20201: put
  db->Put(write_options, "key1647", "value42321");

  // op 20202: delete_range
  status = db->DeleteRange(write_options, "key7947", "key10105");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 20203: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 20204: delete_range
  status = db->DeleteRange(write_options, "key4365", "key10109");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 20205: put
  db->Put(write_options, "key0711", "value18395");

  // op 20206: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2194", "key8421", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2194, key8421) size=" << scan_result.size() << std::endl;
  }

  // op 20207: delete_range
  status = db->DeleteRange(write_options, "key2327", "key9433");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 20208: delete
  db->Delete(write_options, "key8719");

  // op 20209: get
  status = db->Get(read_options, "key5858", &value);
  if (status.ok()) {
    out << "GET key5858 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20210: put
  db->Put(write_options, "key7234", "value112868");

  // op 20211: put
  db->Put(write_options, "key10265", "value49789");

  // op 20212: put
  db->Put(write_options, "key2255", "value78297");

  // op 20213: put
  db->Put(write_options, "key2361", "value113439");

  // op 20214: delete_range
  status = db->DeleteRange(write_options, "key3448", "key11169");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 20215: put
  db->Put(write_options, "key0462", "value52824");

  // op 20216: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 20217: put
  db->Put(write_options, "key2956", "value114066");

  // op 20218: get
  status = db->Get(read_options, "key6618", &value);
  if (status.ok()) {
    out << "GET key6618 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20219: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 20220: put
  db->Put(write_options, "key7685", "value22775");

  // op 20221: get
  status = db->Get(read_options, "key1717", &value);
  if (status.ok()) {
    out << "GET key1717 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20222: put
  db->Put(write_options, "key1632", "value39590");

  // op 20223: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0101", "key2305", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0101, key2305) size=" << scan_result.size() << std::endl;
  }

  // op 20224: get
  status = db->Get(read_options, "key1255", &value);
  if (status.ok()) {
    out << "GET key1255 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20225: put
  db->Put(write_options, "key7217", "value80936");

  // op 20226: put
  db->Put(write_options, "key3213", "value80508");

  // op 20227: delete_range
  status = db->DeleteRange(write_options, "key1209", "key3702");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 20228: delete_range
  status = db->DeleteRange(write_options, "key0454", "key6999");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 20229: get
  status = db->Get(read_options, "key11723", &value);
  if (status.ok()) {
    out << "GET key11723 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20230: get
  status = db->Get(read_options, "key0727", &value);
  if (status.ok()) {
    out << "GET key0727 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20231: delete_range
  status = db->DeleteRange(write_options, "key5442", "key7733");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 20232: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 20233: delete_range
  status = db->DeleteRange(write_options, "key6753", "key8420");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 20234: get
  status = db->Get(read_options, "key5865", &value);
  if (status.ok()) {
    out << "GET key5865 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20235: delete_range
  status = db->DeleteRange(write_options, "key6995", "key8099");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 20236: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9384", "key11484", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9384, key11484) size=" << scan_result.size() << std::endl;
  }

  // op 20237: delete
  db->Delete(write_options, "key5017");

  // op 20238: get
  status = db->Get(read_options, "key0516", &value);
  if (status.ok()) {
    out << "GET key0516 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20239: put
  db->Put(write_options, "key2058", "value27030");

  // op 20240: get
  status = db->Get(read_options, "key7700", &value);
  if (status.ok()) {
    out << "GET key7700 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20241: get
  status = db->Get(read_options, "key8674", &value);
  if (status.ok()) {
    out << "GET key8674 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20242: get
  status = db->Get(read_options, "key5810", &value);
  if (status.ok()) {
    out << "GET key5810 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20243: put
  db->Put(write_options, "key8556", "value69760");

  // op 20244: delete_range
  status = db->DeleteRange(write_options, "key9905", "key11602");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 20245: delete
  db->Delete(write_options, "key4987");

  // op 20246: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7814", "key7872", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7814, key7872) size=" << scan_result.size() << std::endl;
  }

  // op 20247: put
  db->Put(write_options, "key4361", "value28193");

  // op 20248: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7173", "key8945", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7173, key8945) size=" << scan_result.size() << std::endl;
  }

  // op 20249: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 20250: delete
  db->Delete(write_options, "key10257");

  // op 20251: put
  db->Put(write_options, "key2650", "value46930");

  // op 20252: put
  db->Put(write_options, "key7750", "value17523");

  // op 20253: put
  db->Put(write_options, "key0249", "value98761");

  // op 20254: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 20255: put
  db->Put(write_options, "key8960", "value61546");

  // op 20256: delete_range
  status = db->DeleteRange(write_options, "key2892", "key8655");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 20257: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 20258: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4223", "key7244", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4223, key7244) size=" << scan_result.size() << std::endl;
  }

  // op 20259: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 20260: put
  db->Put(write_options, "key4192", "value0338");

  // op 20261: put
  db->Put(write_options, "key3579", "value86279");

  // op 20262: delete_range
  status = db->DeleteRange(write_options, "key1338", "key2772");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 20263: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 20264: put
  db->Put(write_options, "key5490", "value31740");

  // op 20265: put
  db->Put(write_options, "key0851", "value7315");

  // op 20266: put
  db->Put(write_options, "key3992", "value57157");

  // op 20267: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 20268: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3538", "key5764", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3538, key5764) size=" << scan_result.size() << std::endl;
  }

  // op 20269: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0026", "key2750", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0026, key2750) size=" << scan_result.size() << std::endl;
  }

  // op 20270: get
  status = db->Get(read_options, "key8251", &value);
  if (status.ok()) {
    out << "GET key8251 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20271: get
  status = db->Get(read_options, "key6713", &value);
  if (status.ok()) {
    out << "GET key6713 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20272: delete_range
  status = db->DeleteRange(write_options, "key4894", "key5583");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 20273: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0858", "key3557", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0858, key3557) size=" << scan_result.size() << std::endl;
  }

  // op 20274: get
  status = db->Get(read_options, "key0069", &value);
  if (status.ok()) {
    out << "GET key0069 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20275: put
  db->Put(write_options, "key10992", "value43787");

  // op 20276: get
  status = db->Get(read_options, "key9988", &value);
  if (status.ok()) {
    out << "GET key9988 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20277: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0233", "key8569", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0233, key8569) size=" << scan_result.size() << std::endl;
  }

  // op 20278: get
  status = db->Get(read_options, "key0947", &value);
  if (status.ok()) {
    out << "GET key0947 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20279: get
  status = db->Get(read_options, "key3340", &value);
  if (status.ok()) {
    out << "GET key3340 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20280: put
  db->Put(write_options, "key6977", "value79342");

  // op 20281: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6657", "key8834", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6657, key8834) size=" << scan_result.size() << std::endl;
  }

  // op 20282: delete
  db->Delete(write_options, "key3660");

  // op 20283: delete_range
  status = db->DeleteRange(write_options, "key2990", "key7967");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 20284: put
  db->Put(write_options, "key4802", "value12764");

  // op 20285: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1536", "key2200", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1536, key2200) size=" << scan_result.size() << std::endl;
  }

  // op 20286: put
  db->Put(write_options, "key6019", "value0519");

  // op 20287: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 20288: get
  status = db->Get(read_options, "key3477", &value);
  if (status.ok()) {
    out << "GET key3477 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20289: put
  db->Put(write_options, "key3061", "value78081");

  // op 20290: put
  db->Put(write_options, "key0790", "value94297");

  // op 20291: put
  db->Put(write_options, "key11123", "value64117");

  // op 20292: delete
  db->Delete(write_options, "key3882");

  // op 20293: put
  db->Put(write_options, "key8224", "value85196");

  // op 20294: put
  db->Put(write_options, "key5346", "value52059");

  // op 20295: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 20296: delete_range
  status = db->DeleteRange(write_options, "key4552", "key11310");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 20297: put
  db->Put(write_options, "key5955", "value19093");

  // op 20298: put
  db->Put(write_options, "key1248", "value17617");

  // op 20299: put
  db->Put(write_options, "key9523", "value101716");

  // op 20300: put
  db->Put(write_options, "key7526", "value81906");

  // op 20301: put
  db->Put(write_options, "key0323", "value21601");

  // op 20302: delete_range
  status = db->DeleteRange(write_options, "key5556", "key10819");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 20303: put
  db->Put(write_options, "key0100", "value87034");

  // op 20304: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8446", "key10611", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8446, key10611) size=" << scan_result.size() << std::endl;
  }

  // op 20305: delete_range
  status = db->DeleteRange(write_options, "key4754", "key11524");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 20306: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0860", "key6784", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0860, key6784) size=" << scan_result.size() << std::endl;
  }

  // op 20307: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0276", "key1135", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0276, key1135) size=" << scan_result.size() << std::endl;
  }

  // op 20308: get
  status = db->Get(read_options, "key8195", &value);
  if (status.ok()) {
    out << "GET key8195 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20309: put
  db->Put(write_options, "key5337", "value51973");

  // op 20310: put
  db->Put(write_options, "key6580", "value95685");

  // op 20311: put
  db->Put(write_options, "key8622", "value27180");

  // op 20312: get
  status = db->Get(read_options, "key8590", &value);
  if (status.ok()) {
    out << "GET key8590 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20313: delete_range
  status = db->DeleteRange(write_options, "key0029", "key7002");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 20314: put
  db->Put(write_options, "key5935", "value68606");

  // op 20315: delete_range
  status = db->DeleteRange(write_options, "key1858", "key9538");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 20316: put
  db->Put(write_options, "key4614", "value54861");

  // op 20317: put
  db->Put(write_options, "key5021", "value46810");

  // op 20318: put
  db->Put(write_options, "key1316", "value62664");

  // op 20319: put
  db->Put(write_options, "key3970", "value40185");

  // op 20320: get
  status = db->Get(read_options, "key3144", &value);
  if (status.ok()) {
    out << "GET key3144 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20321: put
  db->Put(write_options, "key1046", "value20636");

  // op 20322: delete
  db->Delete(write_options, "key3080");

  // op 20323: put
  db->Put(write_options, "key6239", "value81837");

  // op 20324: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2982", "key8670", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2982, key8670) size=" << scan_result.size() << std::endl;
  }

  // op 20325: get
  status = db->Get(read_options, "key0593", &value);
  if (status.ok()) {
    out << "GET key0593 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20326: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1704", "key2185", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1704, key2185) size=" << scan_result.size() << std::endl;
  }

  // op 20327: get
  status = db->Get(read_options, "key2734", &value);
  if (status.ok()) {
    out << "GET key2734 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20328: delete
  db->Delete(write_options, "key10490");

  // op 20329: delete
  db->Delete(write_options, "key1211");

  // op 20330: put
  db->Put(write_options, "key7656", "value56680");

  // op 20331: delete
  db->Delete(write_options, "key5604");

  // op 20332: put
  db->Put(write_options, "key7058", "value7005");

  // op 20333: delete
  db->Delete(write_options, "key9069");

  // op 20334: get
  status = db->Get(read_options, "key9007", &value);
  if (status.ok()) {
    out << "GET key9007 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20335: put
  db->Put(write_options, "key1484", "value26660");

  // op 20336: get
  status = db->Get(read_options, "key2740", &value);
  if (status.ok()) {
    out << "GET key2740 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20337: put
  db->Put(write_options, "key6274", "value90442");

  // op 20338: delete_range
  status = db->DeleteRange(write_options, "key0282", "key1736");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 20339: put
  db->Put(write_options, "key6895", "value40144");

  // op 20340: get
  status = db->Get(read_options, "key10870", &value);
  if (status.ok()) {
    out << "GET key10870 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20341: put
  db->Put(write_options, "key9559", "value6693");

  // op 20342: put
  db->Put(write_options, "key0819", "value55613");

  // op 20343: put
  db->Put(write_options, "key10268", "value56500");

  // op 20344: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 20345: put
  db->Put(write_options, "key8171", "value32586");

  // op 20346: get
  status = db->Get(read_options, "key3082", &value);
  if (status.ok()) {
    out << "GET key3082 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20347: get
  status = db->Get(read_options, "key6953", &value);
  if (status.ok()) {
    out << "GET key6953 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20348: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1671", "key5992", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1671, key5992) size=" << scan_result.size() << std::endl;
  }

  // op 20349: put
  db->Put(write_options, "key6873", "value40775");

  // op 20350: put
  db->Put(write_options, "key1729", "value62313");

  // op 20351: put
  db->Put(write_options, "key7731", "value6994");

  // op 20352: get
  status = db->Get(read_options, "key8746", &value);
  if (status.ok()) {
    out << "GET key8746 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20353: get
  status = db->Get(read_options, "key7300", &value);
  if (status.ok()) {
    out << "GET key7300 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20354: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 20355: delete
  db->Delete(write_options, "key3612");

  // op 20356: get
  status = db->Get(read_options, "key9239", &value);
  if (status.ok()) {
    out << "GET key9239 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20357: put
  db->Put(write_options, "key6213", "value47806");

  // op 20358: delete
  db->Delete(write_options, "key11169");

  // op 20359: put
  db->Put(write_options, "key3006", "value1870");

  // op 20360: get
  status = db->Get(read_options, "key3694", &value);
  if (status.ok()) {
    out << "GET key3694 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20361: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2778", "key5021", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2778, key5021) size=" << scan_result.size() << std::endl;
  }

  // op 20362: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6587", "key8555", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6587, key8555) size=" << scan_result.size() << std::endl;
  }

  // op 20363: put
  db->Put(write_options, "key5712", "value21590");

  // op 20364: put
  db->Put(write_options, "key9567", "value59900");

  // op 20365: put
  db->Put(write_options, "key2814", "value54352");

  // op 20366: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 20367: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 20368: put
  db->Put(write_options, "key4897", "value75660");

  // op 20369: delete_range
  status = db->DeleteRange(write_options, "key7125", "key7948");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 20370: put
  db->Put(write_options, "key2041", "value11696");

  // op 20371: put
  db->Put(write_options, "key7421", "value95022");

  // op 20372: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0935", "key2468", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0935, key2468) size=" << scan_result.size() << std::endl;
  }

  // op 20373: put
  db->Put(write_options, "key3756", "value84587");

  // op 20374: delete
  db->Delete(write_options, "key10392");

  // op 20375: delete
  db->Delete(write_options, "key5082");

  // op 20376: get
  status = db->Get(read_options, "key3804", &value);
  if (status.ok()) {
    out << "GET key3804 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20377: put
  db->Put(write_options, "key9044", "value21949");

  // op 20378: put
  db->Put(write_options, "key0765", "value98750");

  // op 20379: delete
  db->Delete(write_options, "key6754");

  // op 20380: delete
  db->Delete(write_options, "key3239");

  // op 20381: put
  db->Put(write_options, "key10731", "value99172");

  // op 20382: put
  db->Put(write_options, "key5236", "value27471");

  // op 20383: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 20384: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 20385: delete
  db->Delete(write_options, "key9903");

  // op 20386: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0996", "key3202", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0996, key3202) size=" << scan_result.size() << std::endl;
  }

  // op 20387: delete
  db->Delete(write_options, "key0570");

  // op 20388: delete
  db->Delete(write_options, "key1713");

  // op 20389: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0291", "key3760", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0291, key3760) size=" << scan_result.size() << std::endl;
  }

  // op 20390: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2665", "key4173", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2665, key4173) size=" << scan_result.size() << std::endl;
  }

  // op 20391: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 20392: get
  status = db->Get(read_options, "key11404", &value);
  if (status.ok()) {
    out << "GET key11404 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20393: delete_range
  status = db->DeleteRange(write_options, "key0945", "key1774");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 20394: get
  status = db->Get(read_options, "key0388", &value);
  if (status.ok()) {
    out << "GET key0388 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20395: get
  status = db->Get(read_options, "key5006", &value);
  if (status.ok()) {
    out << "GET key5006 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20396: delete_range
  status = db->DeleteRange(write_options, "key2588", "key11261");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 20397: put
  db->Put(write_options, "key1347", "value114623");

  // op 20398: put
  db->Put(write_options, "key2417", "value17220");

  // op 20399: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 20400: put
  db->Put(write_options, "key2439", "value65477");

  // op 20401: scan
  scan_result.clear();
  status = db->Scan(read_options, "key11364", "key11664", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key11364, key11664) size=" << scan_result.size() << std::endl;
  }

  // op 20402: get
  status = db->Get(read_options, "key11136", &value);
  if (status.ok()) {
    out << "GET key11136 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20403: put
  db->Put(write_options, "key3685", "value56387");

  // op 20404: get
  status = db->Get(read_options, "key1769", &value);
  if (status.ok()) {
    out << "GET key1769 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20405: put
  db->Put(write_options, "key8591", "value56284");

  // op 20406: put
  db->Put(write_options, "key6847", "value76207");

  // op 20407: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1042", "key9494", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1042, key9494) size=" << scan_result.size() << std::endl;
  }

  // op 20408: get
  status = db->Get(read_options, "key1461", &value);
  if (status.ok()) {
    out << "GET key1461 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20409: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5398", "key11146", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5398, key11146) size=" << scan_result.size() << std::endl;
  }

  // op 20410: put
  db->Put(write_options, "key3239", "value61920");

  // op 20411: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9621", "key9798", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9621, key9798) size=" << scan_result.size() << std::endl;
  }

  // op 20412: put
  db->Put(write_options, "key2810", "value70255");

  // op 20413: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6590", "key10091", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6590, key10091) size=" << scan_result.size() << std::endl;
  }

  // op 20414: put
  db->Put(write_options, "key4173", "value27088");

  // op 20415: get
  status = db->Get(read_options, "key8359", &value);
  if (status.ok()) {
    out << "GET key8359 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20416: put
  db->Put(write_options, "key11577", "value58629");

  // op 20417: put
  db->Put(write_options, "key6308", "value0300");

  // op 20418: get
  status = db->Get(read_options, "key7525", &value);
  if (status.ok()) {
    out << "GET key7525 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20419: put
  db->Put(write_options, "key6981", "value69981");

  // op 20420: delete_range
  status = db->DeleteRange(write_options, "key4217", "key6625");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 20421: get
  status = db->Get(read_options, "key9845", &value);
  if (status.ok()) {
    out << "GET key9845 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20422: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 20423: put
  db->Put(write_options, "key5792", "value51216");

  // op 20424: put
  db->Put(write_options, "key8673", "value74351");

  // op 20425: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1757", "key3561", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1757, key3561) size=" << scan_result.size() << std::endl;
  }

  // op 20426: put
  db->Put(write_options, "key9293", "value106174");

  // op 20427: put
  db->Put(write_options, "key9899", "value68637");

  // op 20428: put
  db->Put(write_options, "key0169", "value106070");

  // op 20429: get
  status = db->Get(read_options, "key6022", &value);
  if (status.ok()) {
    out << "GET key6022 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20430: delete_range
  status = db->DeleteRange(write_options, "key3166", "key7372");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 20431: put
  db->Put(write_options, "key8235", "value115061");

  // op 20432: put
  db->Put(write_options, "key4047", "value106599");

  // op 20433: get
  status = db->Get(read_options, "key7105", &value);
  if (status.ok()) {
    out << "GET key7105 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20434: put
  db->Put(write_options, "key9162", "value31023");

  // op 20435: put
  db->Put(write_options, "key11568", "value87007");

  // op 20436: put
  db->Put(write_options, "key9991", "value76105");

  // op 20437: delete
  db->Delete(write_options, "key3621");

  // op 20438: delete
  db->Delete(write_options, "key4568");

  // op 20439: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3856", "key10829", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3856, key10829) size=" << scan_result.size() << std::endl;
  }

  // op 20440: delete
  db->Delete(write_options, "key9577");

  // op 20441: put
  db->Put(write_options, "key9399", "value45242");

  // op 20442: put
  db->Put(write_options, "key0674", "value7088");

  // op 20443: put
  db->Put(write_options, "key11266", "value18676");

  // op 20444: delete_range
  status = db->DeleteRange(write_options, "key9380", "key10716");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 20445: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1017", "key4627", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1017, key4627) size=" << scan_result.size() << std::endl;
  }

  // op 20446: delete_range
  status = db->DeleteRange(write_options, "key6728", "key7194");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 20447: put
  db->Put(write_options, "key6116", "value45519");

  // op 20448: put
  db->Put(write_options, "key6162", "value54025");

  // op 20449: delete_range
  status = db->DeleteRange(write_options, "key6471", "key7856");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 20450: delete_range
  status = db->DeleteRange(write_options, "key4185", "key9622");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 20451: delete
  db->Delete(write_options, "key3214");

  // op 20452: put
  db->Put(write_options, "key6314", "value25524");

  // op 20453: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 20454: put
  db->Put(write_options, "key1271", "value44650");

  // op 20455: get
  status = db->Get(read_options, "key3274", &value);
  if (status.ok()) {
    out << "GET key3274 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20456: get
  status = db->Get(read_options, "key6541", &value);
  if (status.ok()) {
    out << "GET key6541 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20457: put
  db->Put(write_options, "key5634", "value7497");

  // op 20458: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4065", "key8792", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4065, key8792) size=" << scan_result.size() << std::endl;
  }

  // op 20459: delete
  db->Delete(write_options, "key5870");

  // op 20460: get
  status = db->Get(read_options, "key1943", &value);
  if (status.ok()) {
    out << "GET key1943 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20461: put
  db->Put(write_options, "key1717", "value111736");

  // op 20462: get
  status = db->Get(read_options, "key10968", &value);
  if (status.ok()) {
    out << "GET key10968 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20463: delete
  db->Delete(write_options, "key4811");

  // op 20464: get
  status = db->Get(read_options, "key5105", &value);
  if (status.ok()) {
    out << "GET key5105 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20465: delete
  db->Delete(write_options, "key0286");

  // op 20466: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1636", "key2202", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1636, key2202) size=" << scan_result.size() << std::endl;
  }

  // op 20467: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 20468: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 20469: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2665", "key3114", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2665, key3114) size=" << scan_result.size() << std::endl;
  }

  // op 20470: put
  db->Put(write_options, "key7372", "value65789");

  // op 20471: put
  db->Put(write_options, "key7078", "value69018");

  // op 20472: delete_range
  status = db->DeleteRange(write_options, "key4895", "key11396");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 20473: put
  db->Put(write_options, "key0181", "value33577");

  // op 20474: put
  db->Put(write_options, "key8192", "value70808");

  // op 20475: get
  status = db->Get(read_options, "key5497", &value);
  if (status.ok()) {
    out << "GET key5497 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20476: delete
  db->Delete(write_options, "key4572");

  // op 20477: put
  db->Put(write_options, "key11052", "value67547");

  // op 20478: delete
  db->Delete(write_options, "key7139");

  // op 20479: delete
  db->Delete(write_options, "key8349");

  // op 20480: put
  db->Put(write_options, "key1644", "value76885");

  // op 20481: put
  db->Put(write_options, "key1054", "value81021");

  // op 20482: get
  status = db->Get(read_options, "key5707", &value);
  if (status.ok()) {
    out << "GET key5707 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20483: get
  status = db->Get(read_options, "key0675", &value);
  if (status.ok()) {
    out << "GET key0675 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20484: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0665", "key4379", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0665, key4379) size=" << scan_result.size() << std::endl;
  }

  // op 20485: get
  status = db->Get(read_options, "key2911", &value);
  if (status.ok()) {
    out << "GET key2911 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20486: put
  db->Put(write_options, "key10245", "value80144");

  // op 20487: put
  db->Put(write_options, "key6142", "value28851");

  // op 20488: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2501", "key9935", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2501, key9935) size=" << scan_result.size() << std::endl;
  }

  // op 20489: put
  db->Put(write_options, "key0268", "value2643");

  // op 20490: get
  status = db->Get(read_options, "key10372", &value);
  if (status.ok()) {
    out << "GET key10372 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20491: get
  status = db->Get(read_options, "key0177", &value);
  if (status.ok()) {
    out << "GET key0177 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20492: delete
  db->Delete(write_options, "key6941");

  // op 20493: put
  db->Put(write_options, "key2799", "value36752");

  // op 20494: get
  status = db->Get(read_options, "key4744", &value);
  if (status.ok()) {
    out << "GET key4744 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20495: get
  status = db->Get(read_options, "key0266", &value);
  if (status.ok()) {
    out << "GET key0266 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20496: put
  db->Put(write_options, "key0727", "value40318");

  // op 20497: get
  status = db->Get(read_options, "key8645", &value);
  if (status.ok()) {
    out << "GET key8645 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20498: get
  status = db->Get(read_options, "key9672", &value);
  if (status.ok()) {
    out << "GET key9672 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20499: get
  status = db->Get(read_options, "key1450", &value);
  if (status.ok()) {
    out << "GET key1450 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20500: put
  db->Put(write_options, "key5745", "value116976");

  // op 20501: put
  db->Put(write_options, "key6303", "value105685");

  // op 20502: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2557", "key5355", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2557, key5355) size=" << scan_result.size() << std::endl;
  }

  // op 20503: put
  db->Put(write_options, "key2093", "value95400");

  // op 20504: put
  db->Put(write_options, "key8965", "value81607");

  // op 20505: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4244", "key7635", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4244, key7635) size=" << scan_result.size() << std::endl;
  }

  // op 20506: delete_range
  status = db->DeleteRange(write_options, "key10689", "key11599");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 20507: delete
  db->Delete(write_options, "key3317");

  // op 20508: get
  status = db->Get(read_options, "key7158", &value);
  if (status.ok()) {
    out << "GET key7158 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20509: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7145", "key9274", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7145, key9274) size=" << scan_result.size() << std::endl;
  }

  // op 20510: put
  db->Put(write_options, "key10265", "value69406");

  // op 20511: put
  db->Put(write_options, "key3400", "value14584");

  // op 20512: get
  status = db->Get(read_options, "key1075", &value);
  if (status.ok()) {
    out << "GET key1075 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20513: get
  status = db->Get(read_options, "key5979", &value);
  if (status.ok()) {
    out << "GET key5979 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20514: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3764", "key4774", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3764, key4774) size=" << scan_result.size() << std::endl;
  }

  // op 20515: put
  db->Put(write_options, "key7957", "value44463");

  // op 20516: get
  status = db->Get(read_options, "key10187", &value);
  if (status.ok()) {
    out << "GET key10187 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20517: put
  db->Put(write_options, "key11278", "value93091");

  // op 20518: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0231", "key1630", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0231, key1630) size=" << scan_result.size() << std::endl;
  }

  // op 20519: get
  status = db->Get(read_options, "key2354", &value);
  if (status.ok()) {
    out << "GET key2354 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20520: delete
  db->Delete(write_options, "key8397");

  // op 20521: get
  status = db->Get(read_options, "key3219", &value);
  if (status.ok()) {
    out << "GET key3219 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20522: put
  db->Put(write_options, "key0989", "value114572");

  // op 20523: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6368", "key8911", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6368, key8911) size=" << scan_result.size() << std::endl;
  }

  // op 20524: put
  db->Put(write_options, "key5459", "value95732");

  // op 20525: put
  db->Put(write_options, "key8490", "value0973");

  // op 20526: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 20527: delete
  db->Delete(write_options, "key8064");

  // op 20528: get
  status = db->Get(read_options, "key4980", &value);
  if (status.ok()) {
    out << "GET key4980 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20529: put
  db->Put(write_options, "key1995", "value89096");

  // op 20530: put
  db->Put(write_options, "key2269", "value37370");

  // op 20531: delete
  db->Delete(write_options, "key3787");

  // op 20532: put
  db->Put(write_options, "key4162", "value89944");

  // op 20533: get
  status = db->Get(read_options, "key10863", &value);
  if (status.ok()) {
    out << "GET key10863 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20534: put
  db->Put(write_options, "key6917", "value61276");

  // op 20535: delete
  db->Delete(write_options, "key2859");

  // op 20536: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 20537: delete
  db->Delete(write_options, "key4479");

  // op 20538: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3113", "key9723", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3113, key9723) size=" << scan_result.size() << std::endl;
  }

  // op 20539: get
  status = db->Get(read_options, "key8528", &value);
  if (status.ok()) {
    out << "GET key8528 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20540: put
  db->Put(write_options, "key11689", "value109519");

  // op 20541: put
  db->Put(write_options, "key8771", "value1606");

  // op 20542: delete
  db->Delete(write_options, "key8418");

  // op 20543: get
  status = db->Get(read_options, "key2085", &value);
  if (status.ok()) {
    out << "GET key2085 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20544: put
  db->Put(write_options, "key1318", "value102036");

  // op 20545: get
  status = db->Get(read_options, "key5191", &value);
  if (status.ok()) {
    out << "GET key5191 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20546: put
  db->Put(write_options, "key8990", "value65915");

  // op 20547: delete
  db->Delete(write_options, "key10338");

  // op 20548: get
  status = db->Get(read_options, "key9465", &value);
  if (status.ok()) {
    out << "GET key9465 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20549: put
  db->Put(write_options, "key3921", "value117865");

  // op 20550: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 20551: put
  db->Put(write_options, "key4114", "value32440");

  // op 20552: get
  status = db->Get(read_options, "key2541", &value);
  if (status.ok()) {
    out << "GET key2541 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20553: put
  db->Put(write_options, "key5012", "value87714");

  // op 20554: put
  db->Put(write_options, "key7895", "value51680");

  // op 20555: put
  db->Put(write_options, "key9661", "value39746");

  // op 20556: put
  db->Put(write_options, "key1859", "value21889");

  // op 20557: put
  db->Put(write_options, "key9446", "value51245");

  // op 20558: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5759", "key6868", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5759, key6868) size=" << scan_result.size() << std::endl;
  }

  // op 20559: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 20560: put
  db->Put(write_options, "key0008", "value79411");

  // op 20561: put
  db->Put(write_options, "key8367", "value110394");

  // op 20562: delete_range
  status = db->DeleteRange(write_options, "key3237", "key3590");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 20563: put
  db->Put(write_options, "key5965", "value47755");

  // op 20564: put
  db->Put(write_options, "key8804", "value13636");

  // op 20565: put
  db->Put(write_options, "key6503", "value49047");

  // op 20566: delete_range
  status = db->DeleteRange(write_options, "key0853", "key6151");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 20567: delete_range
  status = db->DeleteRange(write_options, "key2071", "key11068");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 20568: delete
  db->Delete(write_options, "key3254");

  // op 20569: get
  status = db->Get(read_options, "key0488", &value);
  if (status.ok()) {
    out << "GET key0488 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20570: get
  status = db->Get(read_options, "key5664", &value);
  if (status.ok()) {
    out << "GET key5664 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20571: delete
  db->Delete(write_options, "key7396");

  // op 20572: delete_range
  status = db->DeleteRange(write_options, "key3222", "key3811");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 20573: get
  status = db->Get(read_options, "key3067", &value);
  if (status.ok()) {
    out << "GET key3067 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20574: get
  status = db->Get(read_options, "key4943", &value);
  if (status.ok()) {
    out << "GET key4943 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20575: put
  db->Put(write_options, "key3044", "value16405");

  // op 20576: get
  status = db->Get(read_options, "key2017", &value);
  if (status.ok()) {
    out << "GET key2017 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20577: put
  db->Put(write_options, "key4311", "value15043");

  // op 20578: get
  status = db->Get(read_options, "key8267", &value);
  if (status.ok()) {
    out << "GET key8267 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20579: put
  db->Put(write_options, "key1439", "value74476");

  // op 20580: put
  db->Put(write_options, "key5963", "value66801");

  // op 20581: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6739", "key10560", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6739, key10560) size=" << scan_result.size() << std::endl;
  }

  // op 20582: put
  db->Put(write_options, "key4299", "value87129");

  // op 20583: put
  db->Put(write_options, "key9939", "value55615");

  // op 20584: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 20585: put
  db->Put(write_options, "key0182", "value25634");

  // op 20586: delete
  db->Delete(write_options, "key0201");

  // op 20587: get
  status = db->Get(read_options, "key6516", &value);
  if (status.ok()) {
    out << "GET key6516 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20588: put
  db->Put(write_options, "key1563", "value11482");

  // op 20589: put
  db->Put(write_options, "key4171", "value80599");

  // op 20590: get
  status = db->Get(read_options, "key7332", &value);
  if (status.ok()) {
    out << "GET key7332 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20591: get
  status = db->Get(read_options, "key1537", &value);
  if (status.ok()) {
    out << "GET key1537 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20592: put
  db->Put(write_options, "key4668", "value75263");

  // op 20593: put
  db->Put(write_options, "key7480", "value51854");

  // op 20594: delete
  db->Delete(write_options, "key3074");

  // op 20595: put
  db->Put(write_options, "key8077", "value62644");

  // op 20596: put
  db->Put(write_options, "key0617", "value77712");

  // op 20597: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2581", "key5456", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2581, key5456) size=" << scan_result.size() << std::endl;
  }

  // op 20598: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0217", "key0346", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0217, key0346) size=" << scan_result.size() << std::endl;
  }

  // op 20599: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 20600: get
  status = db->Get(read_options, "key1443", &value);
  if (status.ok()) {
    out << "GET key1443 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20601: put
  db->Put(write_options, "key6083", "value11133");

  // op 20602: delete
  db->Delete(write_options, "key2076");

  // op 20603: delete_range
  status = db->DeleteRange(write_options, "key2566", "key6315");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 20604: delete_range
  status = db->DeleteRange(write_options, "key4652", "key5256");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 20605: delete
  db->Delete(write_options, "key11451");

  // op 20606: put
  db->Put(write_options, "key0345", "value48482");

  // op 20607: put
  db->Put(write_options, "key4122", "value87845");

  // op 20608: delete
  db->Delete(write_options, "key3984");

  // op 20609: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 20610: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4219", "key5219", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4219, key5219) size=" << scan_result.size() << std::endl;
  }

  // op 20611: put
  db->Put(write_options, "key7026", "value25325");

  // op 20612: put
  db->Put(write_options, "key7082", "value81865");

  // op 20613: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4508", "key5422", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4508, key5422) size=" << scan_result.size() << std::endl;
  }

  // op 20614: put
  db->Put(write_options, "key9826", "value88814");

  // op 20615: get
  status = db->Get(read_options, "key7902", &value);
  if (status.ok()) {
    out << "GET key7902 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20616: delete
  db->Delete(write_options, "key0083");

  // op 20617: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 20618: delete_range
  status = db->DeleteRange(write_options, "key6207", "key8789");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 20619: delete
  db->Delete(write_options, "key3255");

  // op 20620: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8012", "key8327", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8012, key8327) size=" << scan_result.size() << std::endl;
  }

  // op 20621: put
  db->Put(write_options, "key2724", "value115701");

  // op 20622: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 20623: get
  status = db->Get(read_options, "key9653", &value);
  if (status.ok()) {
    out << "GET key9653 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20624: put
  db->Put(write_options, "key9955", "value102497");

  // op 20625: delete
  db->Delete(write_options, "key11179");

  // op 20626: delete_range
  status = db->DeleteRange(write_options, "key10767", "key11136");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 20627: delete_range
  status = db->DeleteRange(write_options, "key1926", "key3467");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 20628: put
  db->Put(write_options, "key3120", "value32896");

  // op 20629: put
  db->Put(write_options, "key7768", "value117957");

  // op 20630: get
  status = db->Get(read_options, "key10537", &value);
  if (status.ok()) {
    out << "GET key10537 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20631: put
  db->Put(write_options, "key5153", "value76034");

  // op 20632: put
  db->Put(write_options, "key7225", "value21734");

  // op 20633: put
  db->Put(write_options, "key3285", "value74901");

  // op 20634: delete
  db->Delete(write_options, "key3236");

  // op 20635: put
  db->Put(write_options, "key3208", "value97124");

  // op 20636: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0286", "key10718", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0286, key10718) size=" << scan_result.size() << std::endl;
  }

  // op 20637: put
  db->Put(write_options, "key3143", "value81987");

  // op 20638: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 20639: get
  status = db->Get(read_options, "key4510", &value);
  if (status.ok()) {
    out << "GET key4510 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20640: put
  db->Put(write_options, "key8844", "value35490");

  // op 20641: put
  db->Put(write_options, "key2589", "value115663");

  // op 20642: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7601", "key8914", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7601, key8914) size=" << scan_result.size() << std::endl;
  }

  // op 20643: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0505", "key9962", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0505, key9962) size=" << scan_result.size() << std::endl;
  }

  // op 20644: delete
  db->Delete(write_options, "key5079");

  // op 20645: put
  db->Put(write_options, "key3507", "value1394");

  // op 20646: delete
  db->Delete(write_options, "key10567");

  // op 20647: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3443", "key10051", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3443, key10051) size=" << scan_result.size() << std::endl;
  }

  // op 20648: put
  db->Put(write_options, "key4298", "value83359");

  // op 20649: delete_range
  status = db->DeleteRange(write_options, "key5920", "key5945");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 20650: put
  db->Put(write_options, "key4427", "value40918");

  // op 20651: get
  status = db->Get(read_options, "key4791", &value);
  if (status.ok()) {
    out << "GET key4791 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20652: put
  db->Put(write_options, "key3355", "value42517");

  // op 20653: put
  db->Put(write_options, "key10052", "value67714");

  // op 20654: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 20655: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6257", "key10053", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6257, key10053) size=" << scan_result.size() << std::endl;
  }

  // op 20656: get
  status = db->Get(read_options, "key7966", &value);
  if (status.ok()) {
    out << "GET key7966 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20657: put
  db->Put(write_options, "key0385", "value45180");

  // op 20658: put
  db->Put(write_options, "key8663", "value105763");

  // op 20659: delete_range
  status = db->DeleteRange(write_options, "key0064", "key9273");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 20660: delete_range
  status = db->DeleteRange(write_options, "key3652", "key6779");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 20661: put
  db->Put(write_options, "key8802", "value108918");

  // op 20662: put
  db->Put(write_options, "key9358", "value37228");

  // op 20663: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4600", "key11114", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4600, key11114) size=" << scan_result.size() << std::endl;
  }

  // op 20664: put
  db->Put(write_options, "key1531", "value95207");

  // op 20665: put
  db->Put(write_options, "key5769", "value36930");

  // op 20666: put
  db->Put(write_options, "key5083", "value14077");

  // op 20667: delete
  db->Delete(write_options, "key4950");

  // op 20668: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3130", "key8487", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3130, key8487) size=" << scan_result.size() << std::endl;
  }

  // op 20669: delete_range
  status = db->DeleteRange(write_options, "key2658", "key4714");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 20670: put
  db->Put(write_options, "key2795", "value7071");

  // op 20671: put
  db->Put(write_options, "key4402", "value0377");

  // op 20672: put
  db->Put(write_options, "key9587", "value85378");

  // op 20673: put
  db->Put(write_options, "key5022", "value66473");

  // op 20674: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 20675: put
  db->Put(write_options, "key4207", "value74129");

  // op 20676: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0788", "key6162", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0788, key6162) size=" << scan_result.size() << std::endl;
  }

  // op 20677: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2478", "key2920", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2478, key2920) size=" << scan_result.size() << std::endl;
  }

  // op 20678: get
  status = db->Get(read_options, "key0298", &value);
  if (status.ok()) {
    out << "GET key0298 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20679: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7695", "key8537", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7695, key8537) size=" << scan_result.size() << std::endl;
  }

  // op 20680: get
  status = db->Get(read_options, "key0590", &value);
  if (status.ok()) {
    out << "GET key0590 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20681: delete_range
  status = db->DeleteRange(write_options, "key0867", "key2185");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 20682: delete
  db->Delete(write_options, "key7723");

  // op 20683: get
  status = db->Get(read_options, "key3341", &value);
  if (status.ok()) {
    out << "GET key3341 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20684: put
  db->Put(write_options, "key1956", "value9277");

  // op 20685: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 20686: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2658", "key7040", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2658, key7040) size=" << scan_result.size() << std::endl;
  }

  // op 20687: put
  db->Put(write_options, "key2688", "value27045");

  // op 20688: put
  db->Put(write_options, "key1034", "value105433");

  // op 20689: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0167", "key9371", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0167, key9371) size=" << scan_result.size() << std::endl;
  }

  // op 20690: get
  status = db->Get(read_options, "key8230", &value);
  if (status.ok()) {
    out << "GET key8230 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20691: put
  db->Put(write_options, "key0400", "value24698");

  // op 20692: delete
  db->Delete(write_options, "key4320");

  // op 20693: put
  db->Put(write_options, "key3953", "value107799");

  // op 20694: get
  status = db->Get(read_options, "key3183", &value);
  if (status.ok()) {
    out << "GET key3183 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20695: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4246", "key6625", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4246, key6625) size=" << scan_result.size() << std::endl;
  }

  // op 20696: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0486", "key4214", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0486, key4214) size=" << scan_result.size() << std::endl;
  }

  // op 20697: get
  status = db->Get(read_options, "key4596", &value);
  if (status.ok()) {
    out << "GET key4596 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20698: get
  status = db->Get(read_options, "key8035", &value);
  if (status.ok()) {
    out << "GET key8035 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20699: put
  db->Put(write_options, "key6871", "value58712");

  // op 20700: get
  status = db->Get(read_options, "key4946", &value);
  if (status.ok()) {
    out << "GET key4946 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20701: put
  db->Put(write_options, "key8091", "value26347");

  // op 20702: put
  db->Put(write_options, "key6120", "value58567");

  // op 20703: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1419", "key8101", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1419, key8101) size=" << scan_result.size() << std::endl;
  }

  // op 20704: get
  status = db->Get(read_options, "key11296", &value);
  if (status.ok()) {
    out << "GET key11296 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20705: delete_range
  status = db->DeleteRange(write_options, "key5224", "key8173");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 20706: put
  db->Put(write_options, "key3546", "value94852");

  // op 20707: delete_range
  status = db->DeleteRange(write_options, "key9577", "key11131");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 20708: delete
  db->Delete(write_options, "key9210");

  // op 20709: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0118", "key6287", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0118, key6287) size=" << scan_result.size() << std::endl;
  }

  // op 20710: put
  db->Put(write_options, "key2096", "value91274");

  // op 20711: put
  db->Put(write_options, "key11317", "value91750");

  // op 20712: get
  status = db->Get(read_options, "key5463", &value);
  if (status.ok()) {
    out << "GET key5463 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20713: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 20714: put
  db->Put(write_options, "key2237", "value107726");

  // op 20715: delete_range
  status = db->DeleteRange(write_options, "key2490", "key2674");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 20716: put
  db->Put(write_options, "key8381", "value18543");

  // op 20717: get
  status = db->Get(read_options, "key8172", &value);
  if (status.ok()) {
    out << "GET key8172 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20718: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4393", "key5777", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4393, key5777) size=" << scan_result.size() << std::endl;
  }

  // op 20719: get
  status = db->Get(read_options, "key4068", &value);
  if (status.ok()) {
    out << "GET key4068 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20720: get
  status = db->Get(read_options, "key9362", &value);
  if (status.ok()) {
    out << "GET key9362 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20721: put
  db->Put(write_options, "key5577", "value114465");

  // op 20722: delete
  db->Delete(write_options, "key1815");

  // op 20723: delete_range
  status = db->DeleteRange(write_options, "key1651", "key8207");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 20724: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7713", "key10175", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7713, key10175) size=" << scan_result.size() << std::endl;
  }

  // op 20725: put
  db->Put(write_options, "key1429", "value73377");

  // op 20726: put
  db->Put(write_options, "key2449", "value83036");

  // op 20727: delete
  db->Delete(write_options, "key7449");

  // op 20728: put
  db->Put(write_options, "key10072", "value114434");

  // op 20729: put
  db->Put(write_options, "key9817", "value83693");

  // op 20730: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2353", "key2354", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2353, key2354) size=" << scan_result.size() << std::endl;
  }

  // op 20731: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5612", "key5819", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5612, key5819) size=" << scan_result.size() << std::endl;
  }

  // op 20732: delete
  db->Delete(write_options, "key0826");

  // op 20733: get
  status = db->Get(read_options, "key5717", &value);
  if (status.ok()) {
    out << "GET key5717 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20734: put
  db->Put(write_options, "key7441", "value16373");

  // op 20735: delete_range
  status = db->DeleteRange(write_options, "key8577", "key11125");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 20736: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 20737: put
  db->Put(write_options, "key0107", "value53278");

  // op 20738: get
  status = db->Get(read_options, "key2086", &value);
  if (status.ok()) {
    out << "GET key2086 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20739: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1338", "key3476", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1338, key3476) size=" << scan_result.size() << std::endl;
  }

  // op 20740: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0152", "key0428", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0152, key0428) size=" << scan_result.size() << std::endl;
  }

  // op 20741: delete_range
  status = db->DeleteRange(write_options, "key0876", "key11712");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 20742: delete
  db->Delete(write_options, "key1356");

  // op 20743: put
  db->Put(write_options, "key6322", "value66511");

  // op 20744: put
  db->Put(write_options, "key9452", "value7966");

  // op 20745: delete
  db->Delete(write_options, "key0027");

  // op 20746: put
  db->Put(write_options, "key1545", "value55779");

  // op 20747: put
  db->Put(write_options, "key3210", "value113582");

  // op 20748: put
  db->Put(write_options, "key7762", "value107027");

  // op 20749: put
  db->Put(write_options, "key0661", "value73141");

  // op 20750: delete_range
  status = db->DeleteRange(write_options, "key8675", "key10868");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 20751: get
  status = db->Get(read_options, "key1376", &value);
  if (status.ok()) {
    out << "GET key1376 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20752: delete_range
  status = db->DeleteRange(write_options, "key0641", "key6438");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 20753: delete_range
  status = db->DeleteRange(write_options, "key6046", "key11368");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 20754: put
  db->Put(write_options, "key10404", "value31242");

  // op 20755: get
  status = db->Get(read_options, "key8368", &value);
  if (status.ok()) {
    out << "GET key8368 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20756: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4080", "key4966", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4080, key4966) size=" << scan_result.size() << std::endl;
  }

  // op 20757: get
  status = db->Get(read_options, "key7741", &value);
  if (status.ok()) {
    out << "GET key7741 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20758: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1086", "key8483", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1086, key8483) size=" << scan_result.size() << std::endl;
  }

  // op 20759: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 20760: put
  db->Put(write_options, "key2081", "value85951");

  // op 20761: put
  db->Put(write_options, "key1983", "value111478");

  // op 20762: delete
  db->Delete(write_options, "key7772");

  // op 20763: put
  db->Put(write_options, "key5491", "value36766");

  // op 20764: put
  db->Put(write_options, "key8948", "value7042");

  // op 20765: put
  db->Put(write_options, "key0740", "value46024");

  // op 20766: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8053", "key9212", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8053, key9212) size=" << scan_result.size() << std::endl;
  }

  // op 20767: get
  status = db->Get(read_options, "key5918", &value);
  if (status.ok()) {
    out << "GET key5918 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20768: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3809", "key3908", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3809, key3908) size=" << scan_result.size() << std::endl;
  }

  // op 20769: get
  status = db->Get(read_options, "key7609", &value);
  if (status.ok()) {
    out << "GET key7609 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20770: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0376", "key10733", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0376, key10733) size=" << scan_result.size() << std::endl;
  }

  // op 20771: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 20772: delete_range
  status = db->DeleteRange(write_options, "key8202", "key10452");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 20773: get
  status = db->Get(read_options, "key4737", &value);
  if (status.ok()) {
    out << "GET key4737 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20774: put
  db->Put(write_options, "key10446", "value73416");

  // op 20775: delete_range
  status = db->DeleteRange(write_options, "key8117", "key8403");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 20776: get
  status = db->Get(read_options, "key2534", &value);
  if (status.ok()) {
    out << "GET key2534 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20777: delete
  db->Delete(write_options, "key0720");

  // op 20778: put
  db->Put(write_options, "key4350", "value112375");

  // op 20779: put
  db->Put(write_options, "key8151", "value43440");

  // op 20780: put
  db->Put(write_options, "key1212", "value92129");

  // op 20781: delete
  db->Delete(write_options, "key8681");

  // op 20782: put
  db->Put(write_options, "key8516", "value35324");

  // op 20783: put
  db->Put(write_options, "key5276", "value105329");

  // op 20784: put
  db->Put(write_options, "key0630", "value52469");

  // op 20785: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10295", "key10359", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10295, key10359) size=" << scan_result.size() << std::endl;
  }

  // op 20786: delete
  db->Delete(write_options, "key10037");

  // op 20787: put
  db->Put(write_options, "key9104", "value9768");

  // op 20788: get
  status = db->Get(read_options, "key0305", &value);
  if (status.ok()) {
    out << "GET key0305 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20789: put
  db->Put(write_options, "key1317", "value64179");

  // op 20790: put
  db->Put(write_options, "key7604", "value48860");

  // op 20791: delete_range
  status = db->DeleteRange(write_options, "key7780", "key11321");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 20792: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4781", "key7188", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4781, key7188) size=" << scan_result.size() << std::endl;
  }

  // op 20793: get
  status = db->Get(read_options, "key8076", &value);
  if (status.ok()) {
    out << "GET key8076 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20794: get
  status = db->Get(read_options, "key10024", &value);
  if (status.ok()) {
    out << "GET key10024 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20795: get
  status = db->Get(read_options, "key11385", &value);
  if (status.ok()) {
    out << "GET key11385 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20796: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 20797: put
  db->Put(write_options, "key11531", "value79685");

  // op 20798: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 20799: get
  status = db->Get(read_options, "key1202", &value);
  if (status.ok()) {
    out << "GET key1202 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20800: put
  db->Put(write_options, "key1787", "value108358");

  // op 20801: put
  db->Put(write_options, "key8093", "value41102");

  // op 20802: get
  status = db->Get(read_options, "key7981", &value);
  if (status.ok()) {
    out << "GET key7981 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20803: get
  status = db->Get(read_options, "key5982", &value);
  if (status.ok()) {
    out << "GET key5982 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20804: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3604", "key3998", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3604, key3998) size=" << scan_result.size() << std::endl;
  }

  // op 20805: put
  db->Put(write_options, "key6600", "value111484");

  // op 20806: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0872", "key5138", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0872, key5138) size=" << scan_result.size() << std::endl;
  }

  // op 20807: put
  db->Put(write_options, "key3679", "value44841");

  // op 20808: put
  db->Put(write_options, "key11079", "value71120");

  // op 20809: put
  db->Put(write_options, "key6549", "value90343");

  // op 20810: delete
  db->Delete(write_options, "key7366");

  // op 20811: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 20812: put
  db->Put(write_options, "key0418", "value117426");

  // op 20813: put
  db->Put(write_options, "key9452", "value32453");

  // op 20814: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 20815: put
  db->Put(write_options, "key0152", "value97736");

  // op 20816: put
  db->Put(write_options, "key4646", "value0052");

  // op 20817: delete_range
  status = db->DeleteRange(write_options, "key7602", "key8556");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 20818: get
  status = db->Get(read_options, "key9811", &value);
  if (status.ok()) {
    out << "GET key9811 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20819: delete
  db->Delete(write_options, "key8059");

  // op 20820: put
  db->Put(write_options, "key3290", "value68081");

  // op 20821: delete
  db->Delete(write_options, "key7517");

  // op 20822: put
  db->Put(write_options, "key0850", "value47014");

  // op 20823: delete_range
  status = db->DeleteRange(write_options, "key2782", "key10456");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 20824: put
  db->Put(write_options, "key2501", "value56852");

  // op 20825: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 20826: put
  db->Put(write_options, "key0776", "value43684");

  // op 20827: put
  db->Put(write_options, "key11044", "value101579");

  // op 20828: put
  db->Put(write_options, "key11081", "value94597");

  // op 20829: put
  db->Put(write_options, "key8002", "value61922");

  // op 20830: get
  status = db->Get(read_options, "key10100", &value);
  if (status.ok()) {
    out << "GET key10100 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20831: put
  db->Put(write_options, "key10619", "value34750");

  // op 20832: put
  db->Put(write_options, "key1673", "value10599");

  // op 20833: put
  db->Put(write_options, "key1164", "value44087");

  // op 20834: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3463", "key8224", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3463, key8224) size=" << scan_result.size() << std::endl;
  }

  // op 20835: put
  db->Put(write_options, "key3923", "value111548");

  // op 20836: put
  db->Put(write_options, "key7733", "value22261");

  // op 20837: get
  status = db->Get(read_options, "key4791", &value);
  if (status.ok()) {
    out << "GET key4791 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20838: delete
  db->Delete(write_options, "key2663");

  // op 20839: put
  db->Put(write_options, "key4648", "value19733");

  // op 20840: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 20841: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 20842: put
  db->Put(write_options, "key2015", "value67290");

  // op 20843: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 20844: delete_range
  status = db->DeleteRange(write_options, "key5489", "key7261");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 20845: get
  status = db->Get(read_options, "key9454", &value);
  if (status.ok()) {
    out << "GET key9454 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20846: delete
  db->Delete(write_options, "key7654");

  // op 20847: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6452", "key7013", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6452, key7013) size=" << scan_result.size() << std::endl;
  }

  // op 20848: delete_range
  status = db->DeleteRange(write_options, "key4048", "key9455");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 20849: put
  db->Put(write_options, "key7084", "value101611");

  // op 20850: get
  status = db->Get(read_options, "key1090", &value);
  if (status.ok()) {
    out << "GET key1090 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20851: delete
  db->Delete(write_options, "key0662");

  // op 20852: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 20853: put
  db->Put(write_options, "key8741", "value39731");

  // op 20854: put
  db->Put(write_options, "key5691", "value84868");

  // op 20855: put
  db->Put(write_options, "key0249", "value46405");

  // op 20856: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2563", "key10782", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2563, key10782) size=" << scan_result.size() << std::endl;
  }

  // op 20857: put
  db->Put(write_options, "key7828", "value53730");

  // op 20858: put
  db->Put(write_options, "key3893", "value116098");

  // op 20859: delete
  db->Delete(write_options, "key4957");

  // op 20860: delete
  db->Delete(write_options, "key1491");

  // op 20861: get
  status = db->Get(read_options, "key0004", &value);
  if (status.ok()) {
    out << "GET key0004 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20862: get
  status = db->Get(read_options, "key3913", &value);
  if (status.ok()) {
    out << "GET key3913 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20863: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0511", "key9117", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0511, key9117) size=" << scan_result.size() << std::endl;
  }

  // op 20864: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 20865: put
  db->Put(write_options, "key2067", "value47585");

  // op 20866: put
  db->Put(write_options, "key9720", "value98304");

  // op 20867: delete_range
  status = db->DeleteRange(write_options, "key0168", "key2694");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 20868: delete
  db->Delete(write_options, "key0794");

  // op 20869: get
  status = db->Get(read_options, "key2963", &value);
  if (status.ok()) {
    out << "GET key2963 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20870: get
  status = db->Get(read_options, "key10325", &value);
  if (status.ok()) {
    out << "GET key10325 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20871: delete
  db->Delete(write_options, "key1052");

  // op 20872: delete
  db->Delete(write_options, "key9439");

  // op 20873: delete
  db->Delete(write_options, "key8309");

  // op 20874: put
  db->Put(write_options, "key6608", "value37976");

  // op 20875: put
  db->Put(write_options, "key7207", "value57135");

  // op 20876: delete
  db->Delete(write_options, "key5096");

  // op 20877: put
  db->Put(write_options, "key3715", "value12963");

  // op 20878: put
  db->Put(write_options, "key10514", "value86261");

  // op 20879: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 20880: get
  status = db->Get(read_options, "key5565", &value);
  if (status.ok()) {
    out << "GET key5565 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20881: delete_range
  status = db->DeleteRange(write_options, "key0342", "key2991");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 20882: delete
  db->Delete(write_options, "key1458");

  // op 20883: put
  db->Put(write_options, "key5649", "value98782");

  // op 20884: put
  db->Put(write_options, "key8623", "value80622");

  // op 20885: get
  status = db->Get(read_options, "key4638", &value);
  if (status.ok()) {
    out << "GET key4638 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20886: delete_range
  status = db->DeleteRange(write_options, "key1243", "key11020");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 20887: put
  db->Put(write_options, "key0180", "value86564");

  // op 20888: get
  status = db->Get(read_options, "key3372", &value);
  if (status.ok()) {
    out << "GET key3372 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20889: put
  db->Put(write_options, "key2061", "value80373");

  // op 20890: put
  db->Put(write_options, "key11462", "value80397");

  // op 20891: get
  status = db->Get(read_options, "key10348", &value);
  if (status.ok()) {
    out << "GET key10348 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20892: get
  status = db->Get(read_options, "key7431", &value);
  if (status.ok()) {
    out << "GET key7431 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20893: get
  status = db->Get(read_options, "key8656", &value);
  if (status.ok()) {
    out << "GET key8656 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20894: get
  status = db->Get(read_options, "key5948", &value);
  if (status.ok()) {
    out << "GET key5948 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20895: delete
  db->Delete(write_options, "key3282");

  // op 20896: put
  db->Put(write_options, "key7664", "value85944");

  // op 20897: put
  db->Put(write_options, "key1116", "value113874");

  // op 20898: put
  db->Put(write_options, "key11705", "value95531");

  // op 20899: put
  db->Put(write_options, "key9381", "value76604");

  // op 20900: put
  db->Put(write_options, "key8773", "value47858");

  // op 20901: put
  db->Put(write_options, "key7078", "value71365");

  // op 20902: put
  db->Put(write_options, "key1669", "value20362");

  // op 20903: put
  db->Put(write_options, "key3367", "value118197");

  // op 20904: get
  status = db->Get(read_options, "key4146", &value);
  if (status.ok()) {
    out << "GET key4146 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20905: put
  db->Put(write_options, "key7860", "value25681");

  // op 20906: get
  status = db->Get(read_options, "key3712", &value);
  if (status.ok()) {
    out << "GET key3712 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20907: delete_range
  status = db->DeleteRange(write_options, "key1180", "key4947");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 20908: put
  db->Put(write_options, "key3732", "value46384");

  // op 20909: delete
  db->Delete(write_options, "key3602");

  // op 20910: put
  db->Put(write_options, "key7358", "value92839");

  // op 20911: put
  db->Put(write_options, "key0501", "value46929");

  // op 20912: get
  status = db->Get(read_options, "key2433", &value);
  if (status.ok()) {
    out << "GET key2433 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20913: delete_range
  status = db->DeleteRange(write_options, "key0600", "key8514");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 20914: get
  status = db->Get(read_options, "key10829", &value);
  if (status.ok()) {
    out << "GET key10829 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20915: delete
  db->Delete(write_options, "key4691");

  // op 20916: delete_range
  status = db->DeleteRange(write_options, "key7277", "key9406");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 20917: put
  db->Put(write_options, "key5561", "value75505");

  // op 20918: delete
  db->Delete(write_options, "key1911");

  // op 20919: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 20920: delete_range
  status = db->DeleteRange(write_options, "key2298", "key3808");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 20921: delete_range
  status = db->DeleteRange(write_options, "key0220", "key7636");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 20922: delete_range
  status = db->DeleteRange(write_options, "key3629", "key10953");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 20923: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2882", "key6258", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2882, key6258) size=" << scan_result.size() << std::endl;
  }

  // op 20924: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 20925: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 20926: put
  db->Put(write_options, "key4919", "value89444");

  // op 20927: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6923", "key9181", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6923, key9181) size=" << scan_result.size() << std::endl;
  }

  // op 20928: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5402", "key6048", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5402, key6048) size=" << scan_result.size() << std::endl;
  }

  // op 20929: delete
  db->Delete(write_options, "key3765");

  // op 20930: delete
  db->Delete(write_options, "key8994");

  // op 20931: put
  db->Put(write_options, "key0844", "value111968");

  // op 20932: put
  db->Put(write_options, "key10401", "value59208");

  // op 20933: get
  status = db->Get(read_options, "key7557", &value);
  if (status.ok()) {
    out << "GET key7557 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20934: delete
  db->Delete(write_options, "key10945");

  // op 20935: get
  status = db->Get(read_options, "key1051", &value);
  if (status.ok()) {
    out << "GET key1051 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20936: get
  status = db->Get(read_options, "key1790", &value);
  if (status.ok()) {
    out << "GET key1790 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20937: put
  db->Put(write_options, "key5931", "value13684");

  // op 20938: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8945", "key9255", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8945, key9255) size=" << scan_result.size() << std::endl;
  }

  // op 20939: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0766", "key6125", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0766, key6125) size=" << scan_result.size() << std::endl;
  }

  // op 20940: put
  db->Put(write_options, "key8560", "value56350");

  // op 20941: put
  db->Put(write_options, "key0417", "value51633");

  // op 20942: delete_range
  status = db->DeleteRange(write_options, "key1917", "key11596");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 20943: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2173", "key10295", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2173, key10295) size=" << scan_result.size() << std::endl;
  }

  // op 20944: delete_range
  status = db->DeleteRange(write_options, "key2885", "key5966");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 20945: put
  db->Put(write_options, "key1362", "value113252");

  // op 20946: put
  db->Put(write_options, "key8582", "value48962");

  // op 20947: delete
  db->Delete(write_options, "key0477");

  // op 20948: delete
  db->Delete(write_options, "key2131");

  // op 20949: get
  status = db->Get(read_options, "key4011", &value);
  if (status.ok()) {
    out << "GET key4011 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20950: put
  db->Put(write_options, "key7893", "value64914");

  // op 20951: put
  db->Put(write_options, "key5012", "value77868");

  // op 20952: get
  status = db->Get(read_options, "key7429", &value);
  if (status.ok()) {
    out << "GET key7429 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20953: put
  db->Put(write_options, "key1914", "value19138");

  // op 20954: put
  db->Put(write_options, "key7953", "value111182");

  // op 20955: put
  db->Put(write_options, "key8721", "value9276");

  // op 20956: put
  db->Put(write_options, "key4478", "value40520");

  // op 20957: put
  db->Put(write_options, "key4633", "value62367");

  // op 20958: put
  db->Put(write_options, "key2918", "value81174");

  // op 20959: get
  status = db->Get(read_options, "key2560", &value);
  if (status.ok()) {
    out << "GET key2560 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20960: put
  db->Put(write_options, "key6515", "value77777");

  // op 20961: put
  db->Put(write_options, "key10652", "value36191");

  // op 20962: put
  db->Put(write_options, "key2349", "value115997");

  // op 20963: put
  db->Put(write_options, "key1763", "value23241");

  // op 20964: put
  db->Put(write_options, "key0064", "value34905");

  // op 20965: put
  db->Put(write_options, "key10621", "value86464");

  // op 20966: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0252", "key4085", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0252, key4085) size=" << scan_result.size() << std::endl;
  }

  // op 20967: get
  status = db->Get(read_options, "key5896", &value);
  if (status.ok()) {
    out << "GET key5896 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20968: put
  db->Put(write_options, "key4408", "value12411");

  // op 20969: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3290", "key9889", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3290, key9889) size=" << scan_result.size() << std::endl;
  }

  // op 20970: put
  db->Put(write_options, "key10210", "value16526");

  // op 20971: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3391", "key5507", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3391, key5507) size=" << scan_result.size() << std::endl;
  }

  // op 20972: delete
  db->Delete(write_options, "key1004");

  // op 20973: get
  status = db->Get(read_options, "key11023", &value);
  if (status.ok()) {
    out << "GET key11023 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20974: delete_range
  status = db->DeleteRange(write_options, "key4464", "key7535");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 20975: delete
  db->Delete(write_options, "key8120");

  // op 20976: put
  db->Put(write_options, "key1633", "value114417");

  // op 20977: put
  db->Put(write_options, "key9587", "value80936");

  // op 20978: get
  status = db->Get(read_options, "key0282", &value);
  if (status.ok()) {
    out << "GET key0282 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20979: delete_range
  status = db->DeleteRange(write_options, "key0502", "key0798");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 20980: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 20981: put
  db->Put(write_options, "key11286", "value89578");

  // op 20982: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2168", "key4250", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2168, key4250) size=" << scan_result.size() << std::endl;
  }

  // op 20983: put
  db->Put(write_options, "key11690", "value41715");

  // op 20984: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2769", "key3583", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2769, key3583) size=" << scan_result.size() << std::endl;
  }

  // op 20985: put
  db->Put(write_options, "key8225", "value15568");

  // op 20986: put
  db->Put(write_options, "key1999", "value48244");

  // op 20987: put
  db->Put(write_options, "key0835", "value118051");

  // op 20988: put
  db->Put(write_options, "key2866", "value2168");

  // op 20989: delete_range
  status = db->DeleteRange(write_options, "key1536", "key8905");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 20990: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5008", "key6472", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5008, key6472) size=" << scan_result.size() << std::endl;
  }

  // op 20991: get
  status = db->Get(read_options, "key5784", &value);
  if (status.ok()) {
    out << "GET key5784 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 20992: put
  db->Put(write_options, "key10868", "value10627");

  // op 20993: put
  db->Put(write_options, "key10461", "value23189");

  // op 20994: put
  db->Put(write_options, "key8763", "value11813");

  // op 20995: put
  db->Put(write_options, "key4822", "value33008");

  // op 20996: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8013", "key11563", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8013, key11563) size=" << scan_result.size() << std::endl;
  }

  // op 20997: delete
  db->Delete(write_options, "key2026");

  // op 20998: put
  db->Put(write_options, "key7151", "value97217");

  // op 20999: delete_range
  status = db->DeleteRange(write_options, "key1600", "key10070");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 21000: delete_range
  status = db->DeleteRange(write_options, "key0490", "key0708");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 21001: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4370", "key8516", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4370, key8516) size=" << scan_result.size() << std::endl;
  }

  // op 21002: delete_range
  status = db->DeleteRange(write_options, "key4977", "key8945");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 21003: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4193", "key7279", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4193, key7279) size=" << scan_result.size() << std::endl;
  }

  // op 21004: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1432", "key10258", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1432, key10258) size=" << scan_result.size() << std::endl;
  }

  // op 21005: put
  db->Put(write_options, "key11413", "value87963");

  // op 21006: put
  db->Put(write_options, "key6799", "value10728");

  // op 21007: delete
  db->Delete(write_options, "key9009");

  // op 21008: put
  db->Put(write_options, "key2996", "value81252");

  // op 21009: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3553", "key8380", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3553, key8380) size=" << scan_result.size() << std::endl;
  }

  // op 21010: get
  status = db->Get(read_options, "key9195", &value);
  if (status.ok()) {
    out << "GET key9195 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21011: delete
  db->Delete(write_options, "key6736");

  // op 21012: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7383", "key8292", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7383, key8292) size=" << scan_result.size() << std::endl;
  }

  // op 21013: get
  status = db->Get(read_options, "key6237", &value);
  if (status.ok()) {
    out << "GET key6237 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21014: put
  db->Put(write_options, "key3481", "value100174");

  // op 21015: delete_range
  status = db->DeleteRange(write_options, "key1877", "key3568");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 21016: delete
  db->Delete(write_options, "key6929");

  // op 21017: get
  status = db->Get(read_options, "key4411", &value);
  if (status.ok()) {
    out << "GET key4411 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21018: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 21019: put
  db->Put(write_options, "key9820", "value34674");

  // op 21020: put
  db->Put(write_options, "key8840", "value55542");

  // op 21021: delete
  db->Delete(write_options, "key5437");

  // op 21022: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9224", "key10711", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9224, key10711) size=" << scan_result.size() << std::endl;
  }

  // op 21023: get
  status = db->Get(read_options, "key0710", &value);
  if (status.ok()) {
    out << "GET key0710 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21024: delete_range
  status = db->DeleteRange(write_options, "key2603", "key5532");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 21025: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1669", "key9009", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1669, key9009) size=" << scan_result.size() << std::endl;
  }

  // op 21026: delete
  db->Delete(write_options, "key6481");

  // op 21027: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8940", "key11225", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8940, key11225) size=" << scan_result.size() << std::endl;
  }

  // op 21028: get
  status = db->Get(read_options, "key10336", &value);
  if (status.ok()) {
    out << "GET key10336 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21029: put
  db->Put(write_options, "key6387", "value87362");

  // op 21030: get
  status = db->Get(read_options, "key9336", &value);
  if (status.ok()) {
    out << "GET key9336 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21031: put
  db->Put(write_options, "key0416", "value14767");

  // op 21032: put
  db->Put(write_options, "key5106", "value70266");

  // op 21033: put
  db->Put(write_options, "key4743", "value84898");

  // op 21034: put
  db->Put(write_options, "key0821", "value86895");

  // op 21035: get
  status = db->Get(read_options, "key8589", &value);
  if (status.ok()) {
    out << "GET key8589 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21036: put
  db->Put(write_options, "key2649", "value88016");

  // op 21037: put
  db->Put(write_options, "key2531", "value84732");

  // op 21038: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 21039: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6176", "key10079", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6176, key10079) size=" << scan_result.size() << std::endl;
  }

  // op 21040: get
  status = db->Get(read_options, "key6677", &value);
  if (status.ok()) {
    out << "GET key6677 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21041: get
  status = db->Get(read_options, "key8136", &value);
  if (status.ok()) {
    out << "GET key8136 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21042: delete
  db->Delete(write_options, "key9175");

  // op 21043: put
  db->Put(write_options, "key7812", "value40904");

  // op 21044: put
  db->Put(write_options, "key7519", "value49665");

  // op 21045: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1534", "key9968", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1534, key9968) size=" << scan_result.size() << std::endl;
  }

  // op 21046: put
  db->Put(write_options, "key8860", "value105234");

  // op 21047: put
  db->Put(write_options, "key0839", "value81745");

  // op 21048: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2611", "key7259", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2611, key7259) size=" << scan_result.size() << std::endl;
  }

  // op 21049: get
  status = db->Get(read_options, "key10317", &value);
  if (status.ok()) {
    out << "GET key10317 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21050: put
  db->Put(write_options, "key7591", "value78883");

  // op 21051: delete
  db->Delete(write_options, "key9092");

  // op 21052: delete
  db->Delete(write_options, "key5918");

  // op 21053: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6177", "key8416", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6177, key8416) size=" << scan_result.size() << std::endl;
  }

  // op 21054: put
  db->Put(write_options, "key1107", "value2941");

  // op 21055: delete
  db->Delete(write_options, "key4736");

  // op 21056: put
  db->Put(write_options, "key9697", "value100389");

  // op 21057: put
  db->Put(write_options, "key5257", "value2657");

  // op 21058: put
  db->Put(write_options, "key9362", "value29101");

  // op 21059: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 21060: put
  db->Put(write_options, "key6296", "value101797");

  // op 21061: get
  status = db->Get(read_options, "key7531", &value);
  if (status.ok()) {
    out << "GET key7531 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21062: put
  db->Put(write_options, "key6518", "value109915");

  // op 21063: delete
  db->Delete(write_options, "key7299");

  // op 21064: put
  db->Put(write_options, "key11401", "value85617");

  // op 21065: get
  status = db->Get(read_options, "key9304", &value);
  if (status.ok()) {
    out << "GET key9304 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21066: put
  db->Put(write_options, "key3044", "value87393");

  // op 21067: delete
  db->Delete(write_options, "key0257");

  // op 21068: put
  db->Put(write_options, "key7357", "value115201");

  // op 21069: delete_range
  status = db->DeleteRange(write_options, "key3523", "key9827");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 21070: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 21071: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 21072: put
  db->Put(write_options, "key5058", "value13745");

  // op 21073: put
  db->Put(write_options, "key5509", "value28002");

  // op 21074: delete
  db->Delete(write_options, "key7990");

  // op 21075: delete_range
  status = db->DeleteRange(write_options, "key9691", "key9917");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 21076: put
  db->Put(write_options, "key8337", "value11817");

  // op 21077: put
  db->Put(write_options, "key4773", "value72518");

  // op 21078: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2895", "key11701", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2895, key11701) size=" << scan_result.size() << std::endl;
  }

  // op 21079: put
  db->Put(write_options, "key8398", "value36342");

  // op 21080: delete
  db->Delete(write_options, "key9648");

  // op 21081: get
  status = db->Get(read_options, "key10452", &value);
  if (status.ok()) {
    out << "GET key10452 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21082: delete
  db->Delete(write_options, "key10252");

  // op 21083: delete
  db->Delete(write_options, "key6570");

  // op 21084: delete_range
  status = db->DeleteRange(write_options, "key4069", "key6944");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 21085: put
  db->Put(write_options, "key2677", "value2224");

  // op 21086: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8450", "key10306", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8450, key10306) size=" << scan_result.size() << std::endl;
  }

  // op 21087: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 21088: put
  db->Put(write_options, "key5773", "value2055");

  // op 21089: delete
  db->Delete(write_options, "key6802");

  // op 21090: get
  status = db->Get(read_options, "key9908", &value);
  if (status.ok()) {
    out << "GET key9908 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21091: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5521", "key6126", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5521, key6126) size=" << scan_result.size() << std::endl;
  }

  // op 21092: put
  db->Put(write_options, "key10070", "value84891");

  // op 21093: put
  db->Put(write_options, "key1709", "value79547");

  // op 21094: delete_range
  status = db->DeleteRange(write_options, "key6026", "key7629");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 21095: get
  status = db->Get(read_options, "key8631", &value);
  if (status.ok()) {
    out << "GET key8631 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21096: delete
  db->Delete(write_options, "key3710");

  // op 21097: delete
  db->Delete(write_options, "key4578");

  // op 21098: put
  db->Put(write_options, "key11472", "value75651");

  // op 21099: delete
  db->Delete(write_options, "key5854");

  // op 21100: delete_range
  status = db->DeleteRange(write_options, "key4278", "key6948");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 21101: delete_range
  status = db->DeleteRange(write_options, "key2415", "key7687");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 21102: put
  db->Put(write_options, "key9678", "value74380");

  // op 21103: put
  db->Put(write_options, "key4296", "value19608");

  // op 21104: delete
  db->Delete(write_options, "key0768");

  // op 21105: get
  status = db->Get(read_options, "key5681", &value);
  if (status.ok()) {
    out << "GET key5681 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21106: put
  db->Put(write_options, "key1664", "value102508");

  // op 21107: delete
  db->Delete(write_options, "key3892");

  // op 21108: get
  status = db->Get(read_options, "key0999", &value);
  if (status.ok()) {
    out << "GET key0999 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21109: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9005", "key11702", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9005, key11702) size=" << scan_result.size() << std::endl;
  }

  // op 21110: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4268", "key9048", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4268, key9048) size=" << scan_result.size() << std::endl;
  }

  // op 21111: put
  db->Put(write_options, "key4838", "value48860");

  // op 21112: put
  db->Put(write_options, "key9663", "value117463");

  // op 21113: get
  status = db->Get(read_options, "key5742", &value);
  if (status.ok()) {
    out << "GET key5742 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21114: get
  status = db->Get(read_options, "key11721", &value);
  if (status.ok()) {
    out << "GET key11721 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21115: put
  db->Put(write_options, "key2262", "value85279");

  // op 21116: get
  status = db->Get(read_options, "key9241", &value);
  if (status.ok()) {
    out << "GET key9241 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21117: put
  db->Put(write_options, "key5063", "value81325");

  // op 21118: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5818", "key6456", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5818, key6456) size=" << scan_result.size() << std::endl;
  }

  // op 21119: put
  db->Put(write_options, "key3255", "value11330");

  // op 21120: get
  status = db->Get(read_options, "key10015", &value);
  if (status.ok()) {
    out << "GET key10015 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21121: put
  db->Put(write_options, "key6994", "value13496");

  // op 21122: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 21123: delete
  db->Delete(write_options, "key7504");

  // op 21124: put
  db->Put(write_options, "key5844", "value95150");

  // op 21125: get
  status = db->Get(read_options, "key10143", &value);
  if (status.ok()) {
    out << "GET key10143 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21126: get
  status = db->Get(read_options, "key6572", &value);
  if (status.ok()) {
    out << "GET key6572 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21127: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 21128: put
  db->Put(write_options, "key8780", "value89046");

  // op 21129: delete
  db->Delete(write_options, "key7701");

  // op 21130: put
  db->Put(write_options, "key1663", "value91525");

  // op 21131: put
  db->Put(write_options, "key3096", "value103610");

  // op 21132: put
  db->Put(write_options, "key10086", "value8763");

  // op 21133: delete_range
  status = db->DeleteRange(write_options, "key4459", "key5928");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 21134: put
  db->Put(write_options, "key9420", "value81603");

  // op 21135: put
  db->Put(write_options, "key0455", "value14000");

  // op 21136: put
  db->Put(write_options, "key0879", "value6481");

  // op 21137: put
  db->Put(write_options, "key0952", "value35985");

  // op 21138: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0042", "key4512", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0042, key4512) size=" << scan_result.size() << std::endl;
  }

  // op 21139: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0297", "key2278", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0297, key2278) size=" << scan_result.size() << std::endl;
  }

  // op 21140: delete_range
  status = db->DeleteRange(write_options, "key4111", "key9511");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 21141: get
  status = db->Get(read_options, "key2604", &value);
  if (status.ok()) {
    out << "GET key2604 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21142: put
  db->Put(write_options, "key2592", "value110802");

  // op 21143: get
  status = db->Get(read_options, "key1817", &value);
  if (status.ok()) {
    out << "GET key1817 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21144: put
  db->Put(write_options, "key7847", "value28891");

  // op 21145: delete
  db->Delete(write_options, "key6956");

  // op 21146: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1016", "key9589", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1016, key9589) size=" << scan_result.size() << std::endl;
  }

  // op 21147: put
  db->Put(write_options, "key4976", "value33971");

  // op 21148: put
  db->Put(write_options, "key1576", "value107746");

  // op 21149: put
  db->Put(write_options, "key8141", "value57968");

  // op 21150: put
  db->Put(write_options, "key5664", "value46485");

  // op 21151: put
  db->Put(write_options, "key9720", "value83199");

  // op 21152: get
  status = db->Get(read_options, "key0794", &value);
  if (status.ok()) {
    out << "GET key0794 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21153: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9196", "key10712", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9196, key10712) size=" << scan_result.size() << std::endl;
  }

  // op 21154: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 21155: delete_range
  status = db->DeleteRange(write_options, "key8539", "key8915");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 21156: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2032", "key6102", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2032, key6102) size=" << scan_result.size() << std::endl;
  }

  // op 21157: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7336", "key9825", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7336, key9825) size=" << scan_result.size() << std::endl;
  }

  // op 21158: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1747", "key1845", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1747, key1845) size=" << scan_result.size() << std::endl;
  }

  // op 21159: get
  status = db->Get(read_options, "key0716", &value);
  if (status.ok()) {
    out << "GET key0716 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21160: put
  db->Put(write_options, "key10729", "value79084");

  // op 21161: put
  db->Put(write_options, "key6308", "value62011");

  // op 21162: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2062", "key8508", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2062, key8508) size=" << scan_result.size() << std::endl;
  }

  // op 21163: put
  db->Put(write_options, "key4411", "value82605");

  // op 21164: put
  db->Put(write_options, "key8286", "value109487");

  // op 21165: put
  db->Put(write_options, "key8390", "value54654");

  // op 21166: delete
  db->Delete(write_options, "key0290");

  // op 21167: delete_range
  status = db->DeleteRange(write_options, "key1415", "key2880");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 21168: get
  status = db->Get(read_options, "key9975", &value);
  if (status.ok()) {
    out << "GET key9975 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21169: put
  db->Put(write_options, "key4161", "value70095");

  // op 21170: put
  db->Put(write_options, "key11504", "value21874");

  // op 21171: delete
  db->Delete(write_options, "key9452");

  // op 21172: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1990", "key4020", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1990, key4020) size=" << scan_result.size() << std::endl;
  }

  // op 21173: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6761", "key9513", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6761, key9513) size=" << scan_result.size() << std::endl;
  }

  // op 21174: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3985", "key5569", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3985, key5569) size=" << scan_result.size() << std::endl;
  }

  // op 21175: put
  db->Put(write_options, "key3568", "value29923");

  // op 21176: put
  db->Put(write_options, "key5883", "value44975");

  // op 21177: delete
  db->Delete(write_options, "key10588");

  // op 21178: put
  db->Put(write_options, "key4774", "value45853");

  // op 21179: put
  db->Put(write_options, "key6990", "value40287");

  // op 21180: delete_range
  status = db->DeleteRange(write_options, "key2458", "key7359");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 21181: get
  status = db->Get(read_options, "key10342", &value);
  if (status.ok()) {
    out << "GET key10342 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21182: delete
  db->Delete(write_options, "key0810");

  // op 21183: put
  db->Put(write_options, "key10511", "value3910");

  // op 21184: put
  db->Put(write_options, "key0080", "value59385");

  // op 21185: get
  status = db->Get(read_options, "key3034", &value);
  if (status.ok()) {
    out << "GET key3034 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21186: put
  db->Put(write_options, "key2408", "value35951");

  // op 21187: get
  status = db->Get(read_options, "key5241", &value);
  if (status.ok()) {
    out << "GET key5241 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21188: put
  db->Put(write_options, "key1846", "value67389");

  // op 21189: get
  status = db->Get(read_options, "key5844", &value);
  if (status.ok()) {
    out << "GET key5844 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21190: get
  status = db->Get(read_options, "key8309", &value);
  if (status.ok()) {
    out << "GET key8309 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21191: delete_range
  status = db->DeleteRange(write_options, "key2673", "key8015");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 21192: get
  status = db->Get(read_options, "key0727", &value);
  if (status.ok()) {
    out << "GET key0727 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21193: put
  db->Put(write_options, "key0124", "value42154");

  // op 21194: get
  status = db->Get(read_options, "key0969", &value);
  if (status.ok()) {
    out << "GET key0969 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21195: get
  status = db->Get(read_options, "key6332", &value);
  if (status.ok()) {
    out << "GET key6332 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21196: get
  status = db->Get(read_options, "key5073", &value);
  if (status.ok()) {
    out << "GET key5073 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21197: delete
  db->Delete(write_options, "key8516");

  // op 21198: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2241", "key8379", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2241, key8379) size=" << scan_result.size() << std::endl;
  }

  // op 21199: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2298", "key5468", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2298, key5468) size=" << scan_result.size() << std::endl;
  }

  // op 21200: put
  db->Put(write_options, "key8710", "value99388");

  // op 21201: put
  db->Put(write_options, "key6909", "value4875");

  // op 21202: put
  db->Put(write_options, "key8982", "value97262");

  // op 21203: put
  db->Put(write_options, "key5236", "value75156");

  // op 21204: get
  status = db->Get(read_options, "key4845", &value);
  if (status.ok()) {
    out << "GET key4845 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21205: put
  db->Put(write_options, "key2321", "value22290");

  // op 21206: put
  db->Put(write_options, "key0163", "value83222");

  // op 21207: put
  db->Put(write_options, "key3874", "value86250");

  // op 21208: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 21209: get
  status = db->Get(read_options, "key8948", &value);
  if (status.ok()) {
    out << "GET key8948 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21210: put
  db->Put(write_options, "key1712", "value75344");

  // op 21211: get
  status = db->Get(read_options, "key1689", &value);
  if (status.ok()) {
    out << "GET key1689 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21212: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 21213: delete
  db->Delete(write_options, "key2274");

  // op 21214: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8769", "key9912", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8769, key9912) size=" << scan_result.size() << std::endl;
  }

  // op 21215: delete_range
  status = db->DeleteRange(write_options, "key5113", "key7160");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 21216: put
  db->Put(write_options, "key10945", "value94124");

  // op 21217: delete
  db->Delete(write_options, "key5632");

  // op 21218: put
  db->Put(write_options, "key2788", "value79732");

  // op 21219: delete_range
  status = db->DeleteRange(write_options, "key9433", "key10560");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 21220: put
  db->Put(write_options, "key9622", "value63489");

  // op 21221: get
  status = db->Get(read_options, "key7857", &value);
  if (status.ok()) {
    out << "GET key7857 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21222: delete
  db->Delete(write_options, "key6475");

  // op 21223: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 21224: put
  db->Put(write_options, "key1319", "value28339");

  // op 21225: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5274", "key5962", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5274, key5962) size=" << scan_result.size() << std::endl;
  }

  // op 21226: put
  db->Put(write_options, "key11013", "value41563");

  // op 21227: put
  db->Put(write_options, "key0436", "value110756");

  // op 21228: put
  db->Put(write_options, "key3935", "value58394");

  // op 21229: get
  status = db->Get(read_options, "key4600", &value);
  if (status.ok()) {
    out << "GET key4600 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21230: put
  db->Put(write_options, "key9044", "value90465");

  // op 21231: put
  db->Put(write_options, "key8526", "value115896");

  // op 21232: delete_range
  status = db->DeleteRange(write_options, "key2168", "key2307");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 21233: get
  status = db->Get(read_options, "key9455", &value);
  if (status.ok()) {
    out << "GET key9455 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21234: put
  db->Put(write_options, "key7291", "value80207");

  // op 21235: put
  db->Put(write_options, "key8092", "value110046");

  // op 21236: delete_range
  status = db->DeleteRange(write_options, "key0272", "key6951");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 21237: put
  db->Put(write_options, "key4920", "value90819");

  // op 21238: get
  status = db->Get(read_options, "key4656", &value);
  if (status.ok()) {
    out << "GET key4656 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21239: put
  db->Put(write_options, "key4983", "value51385");

  // op 21240: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1325", "key5871", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1325, key5871) size=" << scan_result.size() << std::endl;
  }

  // op 21241: put
  db->Put(write_options, "key4268", "value81986");

  // op 21242: put
  db->Put(write_options, "key4792", "value19744");

  // op 21243: delete_range
  status = db->DeleteRange(write_options, "key3381", "key6580");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 21244: delete
  db->Delete(write_options, "key8416");

  // op 21245: delete
  db->Delete(write_options, "key4562");

  // op 21246: get
  status = db->Get(read_options, "key5957", &value);
  if (status.ok()) {
    out << "GET key5957 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21247: delete_range
  status = db->DeleteRange(write_options, "key9967", "key10263");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 21248: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0854", "key6642", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0854, key6642) size=" << scan_result.size() << std::endl;
  }

  // op 21249: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0026", "key4942", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0026, key4942) size=" << scan_result.size() << std::endl;
  }

  // op 21250: delete
  db->Delete(write_options, "key7123");

  // op 21251: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7030", "key11288", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7030, key11288) size=" << scan_result.size() << std::endl;
  }

  // op 21252: put
  db->Put(write_options, "key7505", "value105316");

  // op 21253: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 21254: get
  status = db->Get(read_options, "key9563", &value);
  if (status.ok()) {
    out << "GET key9563 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21255: put
  db->Put(write_options, "key8088", "value114120");

  // op 21256: put
  db->Put(write_options, "key9722", "value11931");

  // op 21257: put
  db->Put(write_options, "key1694", "value19138");

  // op 21258: put
  db->Put(write_options, "key10479", "value13261");

  // op 21259: put
  db->Put(write_options, "key10785", "value78155");

  // op 21260: put
  db->Put(write_options, "key3685", "value29564");

  // op 21261: delete
  db->Delete(write_options, "key7919");

  // op 21262: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6107", "key11594", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6107, key11594) size=" << scan_result.size() << std::endl;
  }

  // op 21263: put
  db->Put(write_options, "key0555", "value34232");

  // op 21264: get
  status = db->Get(read_options, "key11602", &value);
  if (status.ok()) {
    out << "GET key11602 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21265: delete_range
  status = db->DeleteRange(write_options, "key2149", "key8425");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 21266: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6871", "key11055", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6871, key11055) size=" << scan_result.size() << std::endl;
  }

  // op 21267: put
  db->Put(write_options, "key8254", "value50815");

  // op 21268: delete
  db->Delete(write_options, "key2199");

  // op 21269: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6885", "key9594", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6885, key9594) size=" << scan_result.size() << std::endl;
  }

  // op 21270: delete
  db->Delete(write_options, "key3900");

  // op 21271: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0822", "key1611", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0822, key1611) size=" << scan_result.size() << std::endl;
  }

  // op 21272: delete_range
  status = db->DeleteRange(write_options, "key7640", "key9258");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 21273: get
  status = db->Get(read_options, "key0473", &value);
  if (status.ok()) {
    out << "GET key0473 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21274: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1449", "key3425", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1449, key3425) size=" << scan_result.size() << std::endl;
  }

  // op 21275: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0615", "key3121", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0615, key3121) size=" << scan_result.size() << std::endl;
  }

  // op 21276: delete
  db->Delete(write_options, "key3131");

  // op 21277: put
  db->Put(write_options, "key2652", "value90879");

  // op 21278: delete_range
  status = db->DeleteRange(write_options, "key3750", "key5020");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 21279: get
  status = db->Get(read_options, "key7629", &value);
  if (status.ok()) {
    out << "GET key7629 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21280: put
  db->Put(write_options, "key3161", "value2428");

  // op 21281: put
  db->Put(write_options, "key6863", "value97409");

  // op 21282: put
  db->Put(write_options, "key1617", "value60354");

  // op 21283: put
  db->Put(write_options, "key2945", "value39843");

  // op 21284: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1730", "key9922", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1730, key9922) size=" << scan_result.size() << std::endl;
  }

  // op 21285: put
  db->Put(write_options, "key4221", "value0436");

  // op 21286: put
  db->Put(write_options, "key9039", "value79768");

  // op 21287: get
  status = db->Get(read_options, "key10256", &value);
  if (status.ok()) {
    out << "GET key10256 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21288: get
  status = db->Get(read_options, "key5034", &value);
  if (status.ok()) {
    out << "GET key5034 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21289: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3894", "key7773", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3894, key7773) size=" << scan_result.size() << std::endl;
  }

  // op 21290: put
  db->Put(write_options, "key11579", "value17520");

  // op 21291: put
  db->Put(write_options, "key7456", "value14496");

  // op 21292: delete
  db->Delete(write_options, "key0149");

  // op 21293: get
  status = db->Get(read_options, "key0153", &value);
  if (status.ok()) {
    out << "GET key0153 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21294: put
  db->Put(write_options, "key3357", "value22981");

  // op 21295: get
  status = db->Get(read_options, "key1725", &value);
  if (status.ok()) {
    out << "GET key1725 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21296: put
  db->Put(write_options, "key8776", "value14100");

  // op 21297: get
  status = db->Get(read_options, "key2243", &value);
  if (status.ok()) {
    out << "GET key2243 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21298: get
  status = db->Get(read_options, "key1935", &value);
  if (status.ok()) {
    out << "GET key1935 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21299: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6470", "key6862", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6470, key6862) size=" << scan_result.size() << std::endl;
  }

  // op 21300: delete
  db->Delete(write_options, "key2941");

  // op 21301: get
  status = db->Get(read_options, "key9175", &value);
  if (status.ok()) {
    out << "GET key9175 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21302: put
  db->Put(write_options, "key6616", "value15465");

  // op 21303: get
  status = db->Get(read_options, "key1375", &value);
  if (status.ok()) {
    out << "GET key1375 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21304: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5830", "key6139", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5830, key6139) size=" << scan_result.size() << std::endl;
  }

  // op 21305: get
  status = db->Get(read_options, "key10618", &value);
  if (status.ok()) {
    out << "GET key10618 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21306: put
  db->Put(write_options, "key0025", "value117788");

  // op 21307: get
  status = db->Get(read_options, "key11534", &value);
  if (status.ok()) {
    out << "GET key11534 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21308: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 21309: put
  db->Put(write_options, "key10398", "value109806");

  // op 21310: get
  status = db->Get(read_options, "key10355", &value);
  if (status.ok()) {
    out << "GET key10355 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21311: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8158", "key9907", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8158, key9907) size=" << scan_result.size() << std::endl;
  }

  // op 21312: put
  db->Put(write_options, "key9044", "value73381");

  // op 21313: put
  db->Put(write_options, "key6147", "value112641");

  // op 21314: put
  db->Put(write_options, "key9210", "value10734");

  // op 21315: put
  db->Put(write_options, "key11254", "value55191");

  // op 21316: put
  db->Put(write_options, "key4304", "value107372");

  // op 21317: get
  status = db->Get(read_options, "key5646", &value);
  if (status.ok()) {
    out << "GET key5646 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21318: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1552", "key4984", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1552, key4984) size=" << scan_result.size() << std::endl;
  }

  // op 21319: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 21320: put
  db->Put(write_options, "key10205", "value27985");

  // op 21321: get
  status = db->Get(read_options, "key2790", &value);
  if (status.ok()) {
    out << "GET key2790 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21322: put
  db->Put(write_options, "key1334", "value15639");

  // op 21323: delete_range
  status = db->DeleteRange(write_options, "key11134", "key11445");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 21324: put
  db->Put(write_options, "key4244", "value24655");

  // op 21325: put
  db->Put(write_options, "key5349", "value69096");

  // op 21326: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2019", "key6519", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2019, key6519) size=" << scan_result.size() << std::endl;
  }

  // op 21327: get
  status = db->Get(read_options, "key0936", &value);
  if (status.ok()) {
    out << "GET key0936 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21328: get
  status = db->Get(read_options, "key1220", &value);
  if (status.ok()) {
    out << "GET key1220 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21329: delete
  db->Delete(write_options, "key4680");

  // op 21330: put
  db->Put(write_options, "key2351", "value61997");

  // op 21331: delete_range
  status = db->DeleteRange(write_options, "key0878", "key9872");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 21332: put
  db->Put(write_options, "key6642", "value70833");

  // op 21333: put
  db->Put(write_options, "key5485", "value53729");

  // op 21334: put
  db->Put(write_options, "key10239", "value21169");

  // op 21335: get
  status = db->Get(read_options, "key0942", &value);
  if (status.ok()) {
    out << "GET key0942 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21336: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1530", "key9379", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1530, key9379) size=" << scan_result.size() << std::endl;
  }

  // op 21337: put
  db->Put(write_options, "key4115", "value25209");

  // op 21338: put
  db->Put(write_options, "key10315", "value10436");

  // op 21339: delete
  db->Delete(write_options, "key7862");

  // op 21340: get
  status = db->Get(read_options, "key10181", &value);
  if (status.ok()) {
    out << "GET key10181 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21341: put
  db->Put(write_options, "key3679", "value5049");

  // op 21342: put
  db->Put(write_options, "key0027", "value101274");

  // op 21343: delete_range
  status = db->DeleteRange(write_options, "key1379", "key7574");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 21344: put
  db->Put(write_options, "key10332", "value30740");

  // op 21345: delete
  db->Delete(write_options, "key9969");

  // op 21346: get
  status = db->Get(read_options, "key10353", &value);
  if (status.ok()) {
    out << "GET key10353 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21347: delete
  db->Delete(write_options, "key5743");

  // op 21348: delete
  db->Delete(write_options, "key4505");

  // op 21349: put
  db->Put(write_options, "key7791", "value99627");

  // op 21350: put
  db->Put(write_options, "key8500", "value46575");

  // op 21351: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0153", "key3309", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0153, key3309) size=" << scan_result.size() << std::endl;
  }

  // op 21352: put
  db->Put(write_options, "key0811", "value3732");

  // op 21353: get
  status = db->Get(read_options, "key3891", &value);
  if (status.ok()) {
    out << "GET key3891 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21354: get
  status = db->Get(read_options, "key4358", &value);
  if (status.ok()) {
    out << "GET key4358 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21355: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6285", "key11643", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6285, key11643) size=" << scan_result.size() << std::endl;
  }

  // op 21356: get
  status = db->Get(read_options, "key10540", &value);
  if (status.ok()) {
    out << "GET key10540 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21357: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5739", "key8351", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5739, key8351) size=" << scan_result.size() << std::endl;
  }

  // op 21358: get
  status = db->Get(read_options, "key11242", &value);
  if (status.ok()) {
    out << "GET key11242 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21359: put
  db->Put(write_options, "key4767", "value12410");

  // op 21360: put
  db->Put(write_options, "key9498", "value8808");

  // op 21361: get
  status = db->Get(read_options, "key5867", &value);
  if (status.ok()) {
    out << "GET key5867 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21362: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6332", "key7403", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6332, key7403) size=" << scan_result.size() << std::endl;
  }

  // op 21363: delete
  db->Delete(write_options, "key8687");

  // op 21364: delete_range
  status = db->DeleteRange(write_options, "key2482", "key10558");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 21365: put
  db->Put(write_options, "key11394", "value101466");

  // op 21366: put
  db->Put(write_options, "key8346", "value55406");

  // op 21367: delete_range
  status = db->DeleteRange(write_options, "key0581", "key9550");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 21368: delete_range
  status = db->DeleteRange(write_options, "key10934", "key11609");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 21369: put
  db->Put(write_options, "key9640", "value86375");

  // op 21370: put
  db->Put(write_options, "key3110", "value15817");

  // op 21371: put
  db->Put(write_options, "key4721", "value83170");

  // op 21372: put
  db->Put(write_options, "key2648", "value24465");

  // op 21373: get
  status = db->Get(read_options, "key11017", &value);
  if (status.ok()) {
    out << "GET key11017 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21374: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6435", "key10216", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6435, key10216) size=" << scan_result.size() << std::endl;
  }

  // op 21375: get
  status = db->Get(read_options, "key6888", &value);
  if (status.ok()) {
    out << "GET key6888 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21376: get
  status = db->Get(read_options, "key3095", &value);
  if (status.ok()) {
    out << "GET key3095 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21377: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2626", "key7993", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2626, key7993) size=" << scan_result.size() << std::endl;
  }

  // op 21378: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5976", "key7530", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5976, key7530) size=" << scan_result.size() << std::endl;
  }

  // op 21379: get
  status = db->Get(read_options, "key1501", &value);
  if (status.ok()) {
    out << "GET key1501 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21380: put
  db->Put(write_options, "key0896", "value73399");

  // op 21381: get
  status = db->Get(read_options, "key9114", &value);
  if (status.ok()) {
    out << "GET key9114 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21382: put
  db->Put(write_options, "key5095", "value58257");

  // op 21383: put
  db->Put(write_options, "key11016", "value113583");

  // op 21384: put
  db->Put(write_options, "key7221", "value36025");

  // op 21385: delete
  db->Delete(write_options, "key11340");

  // op 21386: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 21387: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 21388: get
  status = db->Get(read_options, "key5994", &value);
  if (status.ok()) {
    out << "GET key5994 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21389: get
  status = db->Get(read_options, "key11506", &value);
  if (status.ok()) {
    out << "GET key11506 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21390: put
  db->Put(write_options, "key11052", "value40533");

  // op 21391: put
  db->Put(write_options, "key10760", "value60857");

  // op 21392: delete
  db->Delete(write_options, "key5050");

  // op 21393: delete_range
  status = db->DeleteRange(write_options, "key8715", "key11532");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 21394: get
  status = db->Get(read_options, "key3425", &value);
  if (status.ok()) {
    out << "GET key3425 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21395: get
  status = db->Get(read_options, "key4166", &value);
  if (status.ok()) {
    out << "GET key4166 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21396: put
  db->Put(write_options, "key6084", "value49385");

  // op 21397: put
  db->Put(write_options, "key10507", "value77012");

  // op 21398: delete_range
  status = db->DeleteRange(write_options, "key6099", "key6187");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 21399: put
  db->Put(write_options, "key7292", "value89402");

  // op 21400: delete
  db->Delete(write_options, "key11512");

  // op 21401: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 21402: get
  status = db->Get(read_options, "key4904", &value);
  if (status.ok()) {
    out << "GET key4904 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21403: put
  db->Put(write_options, "key4962", "value95128");

  // op 21404: get
  status = db->Get(read_options, "key1833", &value);
  if (status.ok()) {
    out << "GET key1833 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21405: get
  status = db->Get(read_options, "key0517", &value);
  if (status.ok()) {
    out << "GET key0517 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21406: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 21407: get
  status = db->Get(read_options, "key8956", &value);
  if (status.ok()) {
    out << "GET key8956 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21408: delete
  db->Delete(write_options, "key10476");

  // op 21409: put
  db->Put(write_options, "key1685", "value107122");

  // op 21410: put
  db->Put(write_options, "key9400", "value18381");

  // op 21411: delete_range
  status = db->DeleteRange(write_options, "key4699", "key8324");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 21412: get
  status = db->Get(read_options, "key10736", &value);
  if (status.ok()) {
    out << "GET key10736 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21413: put
  db->Put(write_options, "key10438", "value4601");

  // op 21414: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2744", "key8267", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2744, key8267) size=" << scan_result.size() << std::endl;
  }

  // op 21415: delete_range
  status = db->DeleteRange(write_options, "key7236", "key10043");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 21416: put
  db->Put(write_options, "key1742", "value42901");

  // op 21417: delete_range
  status = db->DeleteRange(write_options, "key0351", "key10015");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 21418: put
  db->Put(write_options, "key9476", "value19598");

  // op 21419: put
  db->Put(write_options, "key9584", "value65313");

  // op 21420: delete
  db->Delete(write_options, "key7776");

  // op 21421: put
  db->Put(write_options, "key8498", "value32246");

  // op 21422: delete
  db->Delete(write_options, "key6292");

  // op 21423: put
  db->Put(write_options, "key0761", "value64635");

  // op 21424: get
  status = db->Get(read_options, "key4471", &value);
  if (status.ok()) {
    out << "GET key4471 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21425: put
  db->Put(write_options, "key10097", "value36149");

  // op 21426: get
  status = db->Get(read_options, "key4732", &value);
  if (status.ok()) {
    out << "GET key4732 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21427: get
  status = db->Get(read_options, "key2985", &value);
  if (status.ok()) {
    out << "GET key2985 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21428: delete_range
  status = db->DeleteRange(write_options, "key7137", "key11184");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 21429: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7915", "key9946", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7915, key9946) size=" << scan_result.size() << std::endl;
  }

  // op 21430: get
  status = db->Get(read_options, "key10651", &value);
  if (status.ok()) {
    out << "GET key10651 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21431: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8908", "key8965", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8908, key8965) size=" << scan_result.size() << std::endl;
  }

  // op 21432: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3634", "key7607", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3634, key7607) size=" << scan_result.size() << std::endl;
  }

  // op 21433: get
  status = db->Get(read_options, "key4328", &value);
  if (status.ok()) {
    out << "GET key4328 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21434: put
  db->Put(write_options, "key11018", "value93610");

  // op 21435: get
  status = db->Get(read_options, "key10429", &value);
  if (status.ok()) {
    out << "GET key10429 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21436: delete
  db->Delete(write_options, "key2458");

  // op 21437: delete
  db->Delete(write_options, "key6158");

  // op 21438: put
  db->Put(write_options, "key10026", "value102310");

  // op 21439: get
  status = db->Get(read_options, "key9220", &value);
  if (status.ok()) {
    out << "GET key9220 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21440: get
  status = db->Get(read_options, "key7739", &value);
  if (status.ok()) {
    out << "GET key7739 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21441: put
  db->Put(write_options, "key11315", "value13514");

  // op 21442: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 21443: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0567", "key6829", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0567, key6829) size=" << scan_result.size() << std::endl;
  }

  // op 21444: delete_range
  status = db->DeleteRange(write_options, "key6660", "key9027");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 21445: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4299", "key5766", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4299, key5766) size=" << scan_result.size() << std::endl;
  }

  // op 21446: delete_range
  status = db->DeleteRange(write_options, "key1430", "key9906");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 21447: get
  status = db->Get(read_options, "key1121", &value);
  if (status.ok()) {
    out << "GET key1121 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21448: put
  db->Put(write_options, "key3843", "value64563");

  // op 21449: put
  db->Put(write_options, "key10107", "value52593");

  // op 21450: put
  db->Put(write_options, "key9917", "value3047");

  // op 21451: put
  db->Put(write_options, "key3772", "value16646");

  // op 21452: get
  status = db->Get(read_options, "key5636", &value);
  if (status.ok()) {
    out << "GET key5636 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21453: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 21454: delete
  db->Delete(write_options, "key5829");

  // op 21455: get
  status = db->Get(read_options, "key9454", &value);
  if (status.ok()) {
    out << "GET key9454 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21456: get
  status = db->Get(read_options, "key8874", &value);
  if (status.ok()) {
    out << "GET key8874 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21457: delete
  db->Delete(write_options, "key10954");

  // op 21458: put
  db->Put(write_options, "key1593", "value75453");

  // op 21459: put
  db->Put(write_options, "key2055", "value28659");

  // op 21460: put
  db->Put(write_options, "key4793", "value86901");

  // op 21461: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 21462: delete_range
  status = db->DeleteRange(write_options, "key4302", "key11704");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 21463: get
  status = db->Get(read_options, "key2420", &value);
  if (status.ok()) {
    out << "GET key2420 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21464: delete
  db->Delete(write_options, "key7974");

  // op 21465: get
  status = db->Get(read_options, "key8113", &value);
  if (status.ok()) {
    out << "GET key8113 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21466: put
  db->Put(write_options, "key9626", "value89209");

  // op 21467: delete
  db->Delete(write_options, "key9271");

  // op 21468: get
  status = db->Get(read_options, "key11044", &value);
  if (status.ok()) {
    out << "GET key11044 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21469: put
  db->Put(write_options, "key11541", "value90984");

  // op 21470: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2915", "key4157", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2915, key4157) size=" << scan_result.size() << std::endl;
  }

  // op 21471: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 21472: put
  db->Put(write_options, "key8430", "value83041");

  // op 21473: delete
  db->Delete(write_options, "key9161");

  // op 21474: put
  db->Put(write_options, "key5772", "value2069");

  // op 21475: put
  db->Put(write_options, "key3531", "value44089");

  // op 21476: put
  db->Put(write_options, "key1532", "value64065");

  // op 21477: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4988", "key8283", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4988, key8283) size=" << scan_result.size() << std::endl;
  }

  // op 21478: delete_range
  status = db->DeleteRange(write_options, "key1972", "key2208");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 21479: delete_range
  status = db->DeleteRange(write_options, "key3195", "key3498");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 21480: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4198", "key6335", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4198, key6335) size=" << scan_result.size() << std::endl;
  }

  // op 21481: put
  db->Put(write_options, "key8842", "value14583");

  // op 21482: get
  status = db->Get(read_options, "key6404", &value);
  if (status.ok()) {
    out << "GET key6404 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21483: delete_range
  status = db->DeleteRange(write_options, "key0417", "key6622");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 21484: put
  db->Put(write_options, "key10854", "value29051");

  // op 21485: delete
  db->Delete(write_options, "key1169");

  // op 21486: get
  status = db->Get(read_options, "key5698", &value);
  if (status.ok()) {
    out << "GET key5698 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21487: put
  db->Put(write_options, "key1719", "value47015");

  // op 21488: put
  db->Put(write_options, "key6939", "value47199");

  // op 21489: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 21490: put
  db->Put(write_options, "key6766", "value81135");

  // op 21491: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0549", "key8951", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0549, key8951) size=" << scan_result.size() << std::endl;
  }

  // op 21492: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 21493: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1722", "key6359", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1722, key6359) size=" << scan_result.size() << std::endl;
  }

  // op 21494: get
  status = db->Get(read_options, "key6329", &value);
  if (status.ok()) {
    out << "GET key6329 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21495: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 21496: delete
  db->Delete(write_options, "key8810");

  // op 21497: get
  status = db->Get(read_options, "key7036", &value);
  if (status.ok()) {
    out << "GET key7036 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21498: put
  db->Put(write_options, "key3987", "value115935");

  // op 21499: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8745", "key11349", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8745, key11349) size=" << scan_result.size() << std::endl;
  }

  // op 21500: get
  status = db->Get(read_options, "key8601", &value);
  if (status.ok()) {
    out << "GET key8601 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21501: get
  status = db->Get(read_options, "key5266", &value);
  if (status.ok()) {
    out << "GET key5266 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21502: delete_range
  status = db->DeleteRange(write_options, "key2182", "key8833");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 21503: delete
  db->Delete(write_options, "key5554");

  // op 21504: get
  status = db->Get(read_options, "key3294", &value);
  if (status.ok()) {
    out << "GET key3294 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21505: put
  db->Put(write_options, "key9506", "value14311");

  // op 21506: get
  status = db->Get(read_options, "key4920", &value);
  if (status.ok()) {
    out << "GET key4920 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21507: put
  db->Put(write_options, "key7602", "value28537");

  // op 21508: put
  db->Put(write_options, "key2907", "value47187");

  // op 21509: delete
  db->Delete(write_options, "key6480");

  // op 21510: delete_range
  status = db->DeleteRange(write_options, "key9002", "key11506");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 21511: put
  db->Put(write_options, "key9018", "value6398");

  // op 21512: put
  db->Put(write_options, "key3836", "value13385");

  // op 21513: get
  status = db->Get(read_options, "key3264", &value);
  if (status.ok()) {
    out << "GET key3264 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21514: delete
  db->Delete(write_options, "key5707");

  // op 21515: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6725", "key9533", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6725, key9533) size=" << scan_result.size() << std::endl;
  }

  // op 21516: delete
  db->Delete(write_options, "key5570");

  // op 21517: put
  db->Put(write_options, "key8299", "value81018");

  // op 21518: delete
  db->Delete(write_options, "key3144");

  // op 21519: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1178", "key8666", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1178, key8666) size=" << scan_result.size() << std::endl;
  }

  // op 21520: put
  db->Put(write_options, "key2838", "value13982");

  // op 21521: put
  db->Put(write_options, "key4937", "value90082");

  // op 21522: get
  status = db->Get(read_options, "key9969", &value);
  if (status.ok()) {
    out << "GET key9969 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21523: delete_range
  status = db->DeleteRange(write_options, "key4413", "key5480");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 21524: delete_range
  status = db->DeleteRange(write_options, "key4361", "key9068");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 21525: delete_range
  status = db->DeleteRange(write_options, "key0176", "key6517");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 21526: put
  db->Put(write_options, "key1675", "value31479");

  // op 21527: put
  db->Put(write_options, "key8126", "value54378");

  // op 21528: put
  db->Put(write_options, "key6677", "value115953");

  // op 21529: put
  db->Put(write_options, "key5464", "value43533");

  // op 21530: delete_range
  status = db->DeleteRange(write_options, "key3066", "key10626");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 21531: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 21532: get
  status = db->Get(read_options, "key6721", &value);
  if (status.ok()) {
    out << "GET key6721 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21533: get
  status = db->Get(read_options, "key3951", &value);
  if (status.ok()) {
    out << "GET key3951 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21534: delete_range
  status = db->DeleteRange(write_options, "key0955", "key2901");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 21535: delete_range
  status = db->DeleteRange(write_options, "key3834", "key10740");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 21536: get
  status = db->Get(read_options, "key11277", &value);
  if (status.ok()) {
    out << "GET key11277 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21537: put
  db->Put(write_options, "key9305", "value49128");

  // op 21538: put
  db->Put(write_options, "key7671", "value83271");

  // op 21539: get
  status = db->Get(read_options, "key1433", &value);
  if (status.ok()) {
    out << "GET key1433 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21540: put
  db->Put(write_options, "key6736", "value40333");

  // op 21541: put
  db->Put(write_options, "key7087", "value20247");

  // op 21542: delete_range
  status = db->DeleteRange(write_options, "key3657", "key8109");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 21543: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4711", "key9712", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4711, key9712) size=" << scan_result.size() << std::endl;
  }

  // op 21544: put
  db->Put(write_options, "key10323", "value76518");

  // op 21545: delete
  db->Delete(write_options, "key0004");

  // op 21546: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 21547: put
  db->Put(write_options, "key5888", "value35131");

  // op 21548: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3915", "key5965", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3915, key5965) size=" << scan_result.size() << std::endl;
  }

  // op 21549: put
  db->Put(write_options, "key4407", "value65735");

  // op 21550: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 21551: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8224", "key9920", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8224, key9920) size=" << scan_result.size() << std::endl;
  }

  // op 21552: put
  db->Put(write_options, "key1976", "value109324");

  // op 21553: put
  db->Put(write_options, "key10836", "value40720");

  // op 21554: put
  db->Put(write_options, "key2191", "value32365");

  // op 21555: put
  db->Put(write_options, "key0451", "value92533");

  // op 21556: get
  status = db->Get(read_options, "key8388", &value);
  if (status.ok()) {
    out << "GET key8388 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21557: put
  db->Put(write_options, "key7910", "value115404");

  // op 21558: get
  status = db->Get(read_options, "key0961", &value);
  if (status.ok()) {
    out << "GET key0961 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21559: get
  status = db->Get(read_options, "key3256", &value);
  if (status.ok()) {
    out << "GET key3256 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21560: delete_range
  status = db->DeleteRange(write_options, "key3076", "key8487");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 21561: put
  db->Put(write_options, "key10638", "value112940");

  // op 21562: delete_range
  status = db->DeleteRange(write_options, "key2971", "key10955");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 21563: put
  db->Put(write_options, "key9791", "value52520");

  // op 21564: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8755", "key10168", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8755, key10168) size=" << scan_result.size() << std::endl;
  }

  // op 21565: delete
  db->Delete(write_options, "key3786");

  // op 21566: put
  db->Put(write_options, "key5702", "value95934");

  // op 21567: put
  db->Put(write_options, "key10934", "value64798");

  // op 21568: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 21569: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2969", "key3576", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2969, key3576) size=" << scan_result.size() << std::endl;
  }

  // op 21570: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3696", "key9792", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3696, key9792) size=" << scan_result.size() << std::endl;
  }

  // op 21571: put
  db->Put(write_options, "key0718", "value2391");

  // op 21572: put
  db->Put(write_options, "key5531", "value37273");

  // op 21573: get
  status = db->Get(read_options, "key4979", &value);
  if (status.ok()) {
    out << "GET key4979 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21574: get
  status = db->Get(read_options, "key0984", &value);
  if (status.ok()) {
    out << "GET key0984 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21575: put
  db->Put(write_options, "key2871", "value94595");

  // op 21576: get
  status = db->Get(read_options, "key2261", &value);
  if (status.ok()) {
    out << "GET key2261 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21577: get
  status = db->Get(read_options, "key10782", &value);
  if (status.ok()) {
    out << "GET key10782 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21578: delete_range
  status = db->DeleteRange(write_options, "key0263", "key5516");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 21579: put
  db->Put(write_options, "key2045", "value31270");

  // op 21580: put
  db->Put(write_options, "key11261", "value67553");

  // op 21581: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1807", "key11259", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1807, key11259) size=" << scan_result.size() << std::endl;
  }

  // op 21582: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3890", "key7475", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3890, key7475) size=" << scan_result.size() << std::endl;
  }

  // op 21583: get
  status = db->Get(read_options, "key8124", &value);
  if (status.ok()) {
    out << "GET key8124 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21584: put
  db->Put(write_options, "key6218", "value52760");

  // op 21585: get
  status = db->Get(read_options, "key9630", &value);
  if (status.ok()) {
    out << "GET key9630 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21586: delete
  db->Delete(write_options, "key3834");

  // op 21587: put
  db->Put(write_options, "key11000", "value61983");

  // op 21588: get
  status = db->Get(read_options, "key1701", &value);
  if (status.ok()) {
    out << "GET key1701 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21589: get
  status = db->Get(read_options, "key10578", &value);
  if (status.ok()) {
    out << "GET key10578 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21590: put
  db->Put(write_options, "key7490", "value16231");

  // op 21591: put
  db->Put(write_options, "key3676", "value104663");

  // op 21592: put
  db->Put(write_options, "key4166", "value87150");

  // op 21593: delete_range
  status = db->DeleteRange(write_options, "key1851", "key9280");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 21594: put
  db->Put(write_options, "key10603", "value25580");

  // op 21595: delete
  db->Delete(write_options, "key1541");

  // op 21596: delete_range
  status = db->DeleteRange(write_options, "key5280", "key5520");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 21597: delete
  db->Delete(write_options, "key1316");

  // op 21598: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1491", "key11421", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1491, key11421) size=" << scan_result.size() << std::endl;
  }

  // op 21599: put
  db->Put(write_options, "key1078", "value58826");

  // op 21600: get
  status = db->Get(read_options, "key1750", &value);
  if (status.ok()) {
    out << "GET key1750 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21601: get
  status = db->Get(read_options, "key5134", &value);
  if (status.ok()) {
    out << "GET key5134 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21602: delete
  db->Delete(write_options, "key2081");

  // op 21603: delete_range
  status = db->DeleteRange(write_options, "key7088", "key8374");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 21604: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1837", "key4507", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1837, key4507) size=" << scan_result.size() << std::endl;
  }

  // op 21605: put
  db->Put(write_options, "key1983", "value33336");

  // op 21606: get
  status = db->Get(read_options, "key5237", &value);
  if (status.ok()) {
    out << "GET key5237 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21607: delete
  db->Delete(write_options, "key5957");

  // op 21608: put
  db->Put(write_options, "key8989", "value21909");

  // op 21609: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4950", "key5567", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4950, key5567) size=" << scan_result.size() << std::endl;
  }

  // op 21610: delete_range
  status = db->DeleteRange(write_options, "key1229", "key6344");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 21611: put
  db->Put(write_options, "key7552", "value22417");

  // op 21612: put
  db->Put(write_options, "key0602", "value107639");

  // op 21613: put
  db->Put(write_options, "key9068", "value41148");

  // op 21614: put
  db->Put(write_options, "key7848", "value55825");

  // op 21615: get
  status = db->Get(read_options, "key11492", &value);
  if (status.ok()) {
    out << "GET key11492 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21616: delete_range
  status = db->DeleteRange(write_options, "key4069", "key11123");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 21617: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4800", "key5634", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4800, key5634) size=" << scan_result.size() << std::endl;
  }

  // op 21618: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2315", "key10714", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2315, key10714) size=" << scan_result.size() << std::endl;
  }

  // op 21619: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1605", "key7296", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1605, key7296) size=" << scan_result.size() << std::endl;
  }

  // op 21620: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0993", "key1472", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0993, key1472) size=" << scan_result.size() << std::endl;
  }

  // op 21621: delete_range
  status = db->DeleteRange(write_options, "key0627", "key6717");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 21622: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 21623: put
  db->Put(write_options, "key8768", "value74528");

  // op 21624: delete
  db->Delete(write_options, "key10082");

  // op 21625: put
  db->Put(write_options, "key3437", "value27797");

  // op 21626: delete_range
  status = db->DeleteRange(write_options, "key3012", "key6958");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 21627: put
  db->Put(write_options, "key11402", "value45094");

  // op 21628: put
  db->Put(write_options, "key7793", "value117400");

  // op 21629: delete
  db->Delete(write_options, "key7804");

  // op 21630: put
  db->Put(write_options, "key3209", "value105902");

  // op 21631: get
  status = db->Get(read_options, "key9140", &value);
  if (status.ok()) {
    out << "GET key9140 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21632: put
  db->Put(write_options, "key7764", "value31140");

  // op 21633: put
  db->Put(write_options, "key11571", "value114732");

  // op 21634: put
  db->Put(write_options, "key10090", "value102494");

  // op 21635: put
  db->Put(write_options, "key2841", "value104272");

  // op 21636: put
  db->Put(write_options, "key0951", "value103836");

  // op 21637: put
  db->Put(write_options, "key5077", "value65727");

  // op 21638: put
  db->Put(write_options, "key3850", "value69747");

  // op 21639: put
  db->Put(write_options, "key3767", "value2837");

  // op 21640: get
  status = db->Get(read_options, "key5476", &value);
  if (status.ok()) {
    out << "GET key5476 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21641: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 21642: get
  status = db->Get(read_options, "key5790", &value);
  if (status.ok()) {
    out << "GET key5790 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21643: put
  db->Put(write_options, "key7319", "value70397");

  // op 21644: delete_range
  status = db->DeleteRange(write_options, "key5870", "key8853");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 21645: get
  status = db->Get(read_options, "key1329", &value);
  if (status.ok()) {
    out << "GET key1329 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21646: put
  db->Put(write_options, "key5568", "value10673");

  // op 21647: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5279", "key7677", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5279, key7677) size=" << scan_result.size() << std::endl;
  }

  // op 21648: get
  status = db->Get(read_options, "key8973", &value);
  if (status.ok()) {
    out << "GET key8973 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21649: put
  db->Put(write_options, "key7174", "value21574");

  // op 21650: delete_range
  status = db->DeleteRange(write_options, "key3773", "key8977");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 21651: put
  db->Put(write_options, "key2786", "value53261");

  // op 21652: put
  db->Put(write_options, "key3152", "value45923");

  // op 21653: put
  db->Put(write_options, "key7913", "value61288");

  // op 21654: put
  db->Put(write_options, "key4154", "value90759");

  // op 21655: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 21656: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0580", "key8604", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0580, key8604) size=" << scan_result.size() << std::endl;
  }

  // op 21657: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5711", "key7935", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5711, key7935) size=" << scan_result.size() << std::endl;
  }

  // op 21658: get
  status = db->Get(read_options, "key7593", &value);
  if (status.ok()) {
    out << "GET key7593 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21659: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0953", "key5494", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0953, key5494) size=" << scan_result.size() << std::endl;
  }

  // op 21660: delete
  db->Delete(write_options, "key5478");

  // op 21661: put
  db->Put(write_options, "key11094", "value105042");

  // op 21662: delete
  db->Delete(write_options, "key2476");

  // op 21663: delete
  db->Delete(write_options, "key0003");

  // op 21664: get
  status = db->Get(read_options, "key8483", &value);
  if (status.ok()) {
    out << "GET key8483 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21665: put
  db->Put(write_options, "key0716", "value7441");

  // op 21666: put
  db->Put(write_options, "key8699", "value103292");

  // op 21667: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 21668: delete
  db->Delete(write_options, "key4186");

  // op 21669: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 21670: get
  status = db->Get(read_options, "key9045", &value);
  if (status.ok()) {
    out << "GET key9045 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21671: delete
  db->Delete(write_options, "key1134");

  // op 21672: delete_range
  status = db->DeleteRange(write_options, "key0433", "key3629");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 21673: get
  status = db->Get(read_options, "key3547", &value);
  if (status.ok()) {
    out << "GET key3547 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21674: put
  db->Put(write_options, "key0730", "value43666");

  // op 21675: put
  db->Put(write_options, "key7126", "value84425");

  // op 21676: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4553", "key5198", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4553, key5198) size=" << scan_result.size() << std::endl;
  }

  // op 21677: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0490", "key7499", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0490, key7499) size=" << scan_result.size() << std::endl;
  }

  // op 21678: get
  status = db->Get(read_options, "key1823", &value);
  if (status.ok()) {
    out << "GET key1823 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21679: put
  db->Put(write_options, "key2276", "value105365");

  // op 21680: get
  status = db->Get(read_options, "key3965", &value);
  if (status.ok()) {
    out << "GET key3965 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21681: put
  db->Put(write_options, "key3714", "value26729");

  // op 21682: delete
  db->Delete(write_options, "key1723");

  // op 21683: delete
  db->Delete(write_options, "key10805");

  // op 21684: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 21685: put
  db->Put(write_options, "key3468", "value76930");

  // op 21686: delete_range
  status = db->DeleteRange(write_options, "key6724", "key9489");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 21687: delete_range
  status = db->DeleteRange(write_options, "key1113", "key8446");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 21688: put
  db->Put(write_options, "key0050", "value91406");

  // op 21689: put
  db->Put(write_options, "key7591", "value4809");

  // op 21690: put
  db->Put(write_options, "key1789", "value79464");

  // op 21691: put
  db->Put(write_options, "key2114", "value46778");

  // op 21692: get
  status = db->Get(read_options, "key2790", &value);
  if (status.ok()) {
    out << "GET key2790 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21693: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5433", "key10866", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5433, key10866) size=" << scan_result.size() << std::endl;
  }

  // op 21694: delete
  db->Delete(write_options, "key1188");

  // op 21695: get
  status = db->Get(read_options, "key3243", &value);
  if (status.ok()) {
    out << "GET key3243 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21696: put
  db->Put(write_options, "key5726", "value73296");

  // op 21697: put
  db->Put(write_options, "key8739", "value10433");

  // op 21698: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2373", "key11549", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2373, key11549) size=" << scan_result.size() << std::endl;
  }

  // op 21699: put
  db->Put(write_options, "key8399", "value37100");

  // op 21700: delete
  db->Delete(write_options, "key7542");

  // op 21701: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0425", "key6303", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0425, key6303) size=" << scan_result.size() << std::endl;
  }

  // op 21702: get
  status = db->Get(read_options, "key3594", &value);
  if (status.ok()) {
    out << "GET key3594 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21703: put
  db->Put(write_options, "key9661", "value81772");

  // op 21704: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5539", "key7323", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5539, key7323) size=" << scan_result.size() << std::endl;
  }

  // op 21705: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3771", "key3943", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3771, key3943) size=" << scan_result.size() << std::endl;
  }

  // op 21706: delete_range
  status = db->DeleteRange(write_options, "key8155", "key9775");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 21707: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 21708: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 21709: delete_range
  status = db->DeleteRange(write_options, "key4440", "key10145");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 21710: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2063", "key11306", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2063, key11306) size=" << scan_result.size() << std::endl;
  }

  // op 21711: get
  status = db->Get(read_options, "key10201", &value);
  if (status.ok()) {
    out << "GET key10201 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21712: get
  status = db->Get(read_options, "key1414", &value);
  if (status.ok()) {
    out << "GET key1414 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21713: put
  db->Put(write_options, "key4275", "value8361");

  // op 21714: put
  db->Put(write_options, "key2549", "value23158");

  // op 21715: get
  status = db->Get(read_options, "key11235", &value);
  if (status.ok()) {
    out << "GET key11235 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21716: put
  db->Put(write_options, "key5748", "value85527");

  // op 21717: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6309", "key7896", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6309, key7896) size=" << scan_result.size() << std::endl;
  }

  // op 21718: put
  db->Put(write_options, "key0030", "value89804");

  // op 21719: delete_range
  status = db->DeleteRange(write_options, "key6195", "key7755");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 21720: delete
  db->Delete(write_options, "key9542");

  // op 21721: delete
  db->Delete(write_options, "key9651");

  // op 21722: delete
  db->Delete(write_options, "key6828");

  // op 21723: put
  db->Put(write_options, "key9038", "value73120");

  // op 21724: put
  db->Put(write_options, "key11096", "value94257");

  // op 21725: delete
  db->Delete(write_options, "key0158");

  // op 21726: delete
  db->Delete(write_options, "key11352");

  // op 21727: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0098", "key9271", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0098, key9271) size=" << scan_result.size() << std::endl;
  }

  // op 21728: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2475", "key2935", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2475, key2935) size=" << scan_result.size() << std::endl;
  }

  // op 21729: get
  status = db->Get(read_options, "key5275", &value);
  if (status.ok()) {
    out << "GET key5275 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21730: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7686", "key7987", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7686, key7987) size=" << scan_result.size() << std::endl;
  }

  // op 21731: put
  db->Put(write_options, "key9295", "value87145");

  // op 21732: delete
  db->Delete(write_options, "key3724");

  // op 21733: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 21734: put
  db->Put(write_options, "key1952", "value114529");

  // op 21735: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0389", "key4995", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0389, key4995) size=" << scan_result.size() << std::endl;
  }

  // op 21736: get
  status = db->Get(read_options, "key4726", &value);
  if (status.ok()) {
    out << "GET key4726 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21737: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 21738: get
  status = db->Get(read_options, "key8481", &value);
  if (status.ok()) {
    out << "GET key8481 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21739: put
  db->Put(write_options, "key4103", "value17840");

  // op 21740: get
  status = db->Get(read_options, "key7387", &value);
  if (status.ok()) {
    out << "GET key7387 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21741: get
  status = db->Get(read_options, "key3747", &value);
  if (status.ok()) {
    out << "GET key3747 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21742: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 21743: put
  db->Put(write_options, "key6075", "value75100");

  // op 21744: put
  db->Put(write_options, "key0416", "value86810");

  // op 21745: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2644", "key9161", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2644, key9161) size=" << scan_result.size() << std::endl;
  }

  // op 21746: put
  db->Put(write_options, "key5521", "value24850");

  // op 21747: delete
  db->Delete(write_options, "key4344");

  // op 21748: get
  status = db->Get(read_options, "key2904", &value);
  if (status.ok()) {
    out << "GET key2904 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21749: delete_range
  status = db->DeleteRange(write_options, "key2862", "key4323");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 21750: get
  status = db->Get(read_options, "key1847", &value);
  if (status.ok()) {
    out << "GET key1847 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21751: delete
  db->Delete(write_options, "key3261");

  // op 21752: delete_range
  status = db->DeleteRange(write_options, "key4794", "key10235");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 21753: put
  db->Put(write_options, "key8384", "value44352");

  // op 21754: delete
  db->Delete(write_options, "key6190");

  // op 21755: put
  db->Put(write_options, "key1387", "value35716");

  // op 21756: delete
  db->Delete(write_options, "key1534");

  // op 21757: get
  status = db->Get(read_options, "key11723", &value);
  if (status.ok()) {
    out << "GET key11723 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21758: put
  db->Put(write_options, "key3622", "value54500");

  // op 21759: delete_range
  status = db->DeleteRange(write_options, "key1775", "key5382");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 21760: put
  db->Put(write_options, "key10665", "value80418");

  // op 21761: get
  status = db->Get(read_options, "key7441", &value);
  if (status.ok()) {
    out << "GET key7441 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21762: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4117", "key10399", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4117, key10399) size=" << scan_result.size() << std::endl;
  }

  // op 21763: get
  status = db->Get(read_options, "key10525", &value);
  if (status.ok()) {
    out << "GET key10525 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21764: put
  db->Put(write_options, "key10407", "value108926");

  // op 21765: delete_range
  status = db->DeleteRange(write_options, "key4386", "key7489");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 21766: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0897", "key7472", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0897, key7472) size=" << scan_result.size() << std::endl;
  }

  // op 21767: put
  db->Put(write_options, "key3868", "value25844");

  // op 21768: put
  db->Put(write_options, "key0518", "value1042");

  // op 21769: put
  db->Put(write_options, "key0269", "value41839");

  // op 21770: put
  db->Put(write_options, "key8047", "value34343");

  // op 21771: delete
  db->Delete(write_options, "key5207");

  // op 21772: delete
  db->Delete(write_options, "key2695");

  // op 21773: get
  status = db->Get(read_options, "key0616", &value);
  if (status.ok()) {
    out << "GET key0616 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21774: put
  db->Put(write_options, "key0576", "value21170");

  // op 21775: put
  db->Put(write_options, "key9205", "value111642");

  // op 21776: put
  db->Put(write_options, "key5169", "value24022");

  // op 21777: put
  db->Put(write_options, "key11472", "value64719");

  // op 21778: put
  db->Put(write_options, "key3433", "value105066");

  // op 21779: get
  status = db->Get(read_options, "key11576", &value);
  if (status.ok()) {
    out << "GET key11576 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21780: put
  db->Put(write_options, "key2929", "value81430");

  // op 21781: delete
  db->Delete(write_options, "key8002");

  // op 21782: delete
  db->Delete(write_options, "key9827");

  // op 21783: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1288", "key11592", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1288, key11592) size=" << scan_result.size() << std::endl;
  }

  // op 21784: put
  db->Put(write_options, "key0518", "value99659");

  // op 21785: get
  status = db->Get(read_options, "key10889", &value);
  if (status.ok()) {
    out << "GET key10889 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21786: put
  db->Put(write_options, "key2737", "value104642");

  // op 21787: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 21788: put
  db->Put(write_options, "key4484", "value22745");

  // op 21789: delete_range
  status = db->DeleteRange(write_options, "key8388", "key10981");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 21790: put
  db->Put(write_options, "key1767", "value39885");

  // op 21791: delete_range
  status = db->DeleteRange(write_options, "key7946", "key10672");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 21792: put
  db->Put(write_options, "key8943", "value28880");

  // op 21793: delete
  db->Delete(write_options, "key1583");

  // op 21794: get
  status = db->Get(read_options, "key9709", &value);
  if (status.ok()) {
    out << "GET key9709 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21795: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5390", "key6910", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5390, key6910) size=" << scan_result.size() << std::endl;
  }

  // op 21796: delete_range
  status = db->DeleteRange(write_options, "key7744", "key9588");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 21797: get
  status = db->Get(read_options, "key4420", &value);
  if (status.ok()) {
    out << "GET key4420 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21798: delete
  db->Delete(write_options, "key2920");

  // op 21799: get
  status = db->Get(read_options, "key9858", &value);
  if (status.ok()) {
    out << "GET key9858 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21800: get
  status = db->Get(read_options, "key2206", &value);
  if (status.ok()) {
    out << "GET key2206 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21801: put
  db->Put(write_options, "key7080", "value27963");

  // op 21802: delete
  db->Delete(write_options, "key9046");

  // op 21803: put
  db->Put(write_options, "key4990", "value49646");

  // op 21804: get
  status = db->Get(read_options, "key6450", &value);
  if (status.ok()) {
    out << "GET key6450 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21805: delete
  db->Delete(write_options, "key7931");

  // op 21806: delete_range
  status = db->DeleteRange(write_options, "key1541", "key9601");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 21807: put
  db->Put(write_options, "key3047", "value106868");

  // op 21808: put
  db->Put(write_options, "key5483", "value97223");

  // op 21809: get
  status = db->Get(read_options, "key0105", &value);
  if (status.ok()) {
    out << "GET key0105 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21810: delete_range
  status = db->DeleteRange(write_options, "key1920", "key11362");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 21811: put
  db->Put(write_options, "key1608", "value20573");

  // op 21812: put
  db->Put(write_options, "key8995", "value46433");

  // op 21813: put
  db->Put(write_options, "key9572", "value28231");

  // op 21814: delete_range
  status = db->DeleteRange(write_options, "key5719", "key6092");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 21815: put
  db->Put(write_options, "key8289", "value61122");

  // op 21816: put
  db->Put(write_options, "key8301", "value116187");

  // op 21817: put
  db->Put(write_options, "key7197", "value54829");

  // op 21818: delete_range
  status = db->DeleteRange(write_options, "key7865", "key11666");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 21819: put
  db->Put(write_options, "key6139", "value63772");

  // op 21820: get
  status = db->Get(read_options, "key4253", &value);
  if (status.ok()) {
    out << "GET key4253 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21821: put
  db->Put(write_options, "key5671", "value13247");

  // op 21822: get
  status = db->Get(read_options, "key3969", &value);
  if (status.ok()) {
    out << "GET key3969 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21823: get
  status = db->Get(read_options, "key9821", &value);
  if (status.ok()) {
    out << "GET key9821 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21824: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1886", "key8371", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1886, key8371) size=" << scan_result.size() << std::endl;
  }

  // op 21825: get
  status = db->Get(read_options, "key10483", &value);
  if (status.ok()) {
    out << "GET key10483 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21826: put
  db->Put(write_options, "key2522", "value62713");

  // op 21827: put
  db->Put(write_options, "key4729", "value7936");

  // op 21828: put
  db->Put(write_options, "key8558", "value99289");

  // op 21829: get
  status = db->Get(read_options, "key8863", &value);
  if (status.ok()) {
    out << "GET key8863 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21830: put
  db->Put(write_options, "key9604", "value37266");

  // op 21831: get
  status = db->Get(read_options, "key9226", &value);
  if (status.ok()) {
    out << "GET key9226 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21832: put
  db->Put(write_options, "key8608", "value94229");

  // op 21833: put
  db->Put(write_options, "key9198", "value40705");

  // op 21834: put
  db->Put(write_options, "key11561", "value57793");

  // op 21835: put
  db->Put(write_options, "key3959", "value2877");

  // op 21836: delete_range
  status = db->DeleteRange(write_options, "key2170", "key2287");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 21837: put
  db->Put(write_options, "key5355", "value42900");

  // op 21838: delete
  db->Delete(write_options, "key4122");

  // op 21839: delete_range
  status = db->DeleteRange(write_options, "key0256", "key7290");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 21840: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0599", "key6321", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0599, key6321) size=" << scan_result.size() << std::endl;
  }

  // op 21841: delete_range
  status = db->DeleteRange(write_options, "key7268", "key11548");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 21842: delete_range
  status = db->DeleteRange(write_options, "key7768", "key9641");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 21843: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3656", "key4835", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3656, key4835) size=" << scan_result.size() << std::endl;
  }

  // op 21844: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1956", "key5919", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1956, key5919) size=" << scan_result.size() << std::endl;
  }

  // op 21845: delete
  db->Delete(write_options, "key6795");

  // op 21846: put
  db->Put(write_options, "key11505", "value88302");

  // op 21847: get
  status = db->Get(read_options, "key1170", &value);
  if (status.ok()) {
    out << "GET key1170 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21848: get
  status = db->Get(read_options, "key1012", &value);
  if (status.ok()) {
    out << "GET key1012 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21849: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1539", "key4946", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1539, key4946) size=" << scan_result.size() << std::endl;
  }

  // op 21850: put
  db->Put(write_options, "key3614", "value102185");

  // op 21851: put
  db->Put(write_options, "key9691", "value54761");

  // op 21852: put
  db->Put(write_options, "key9777", "value3376");

  // op 21853: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7198", "key7675", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7198, key7675) size=" << scan_result.size() << std::endl;
  }

  // op 21854: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2151", "key2786", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2151, key2786) size=" << scan_result.size() << std::endl;
  }

  // op 21855: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2688", "key8770", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2688, key8770) size=" << scan_result.size() << std::endl;
  }

  // op 21856: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0407", "key7437", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0407, key7437) size=" << scan_result.size() << std::endl;
  }

  // op 21857: put
  db->Put(write_options, "key7051", "value115201");

  // op 21858: put
  db->Put(write_options, "key4298", "value24724");

  // op 21859: get
  status = db->Get(read_options, "key10354", &value);
  if (status.ok()) {
    out << "GET key10354 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21860: delete
  db->Delete(write_options, "key2326");

  // op 21861: get
  status = db->Get(read_options, "key7391", &value);
  if (status.ok()) {
    out << "GET key7391 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21862: put
  db->Put(write_options, "key10983", "value79659");

  // op 21863: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0487", "key4202", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0487, key4202) size=" << scan_result.size() << std::endl;
  }

  // op 21864: get
  status = db->Get(read_options, "key2982", &value);
  if (status.ok()) {
    out << "GET key2982 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21865: delete_range
  status = db->DeleteRange(write_options, "key1111", "key8085");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 21866: delete_range
  status = db->DeleteRange(write_options, "key2159", "key4511");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 21867: get
  status = db->Get(read_options, "key9724", &value);
  if (status.ok()) {
    out << "GET key9724 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21868: put
  db->Put(write_options, "key5354", "value106142");

  // op 21869: get
  status = db->Get(read_options, "key1831", &value);
  if (status.ok()) {
    out << "GET key1831 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21870: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1199", "key5514", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1199, key5514) size=" << scan_result.size() << std::endl;
  }

  // op 21871: delete_range
  status = db->DeleteRange(write_options, "key1823", "key2720");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 21872: put
  db->Put(write_options, "key4740", "value117467");

  // op 21873: delete
  db->Delete(write_options, "key7721");

  // op 21874: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3333", "key10138", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3333, key10138) size=" << scan_result.size() << std::endl;
  }

  // op 21875: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0246", "key9483", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0246, key9483) size=" << scan_result.size() << std::endl;
  }

  // op 21876: put
  db->Put(write_options, "key10878", "value92993");

  // op 21877: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9537", "key10192", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9537, key10192) size=" << scan_result.size() << std::endl;
  }

  // op 21878: put
  db->Put(write_options, "key7500", "value3386");

  // op 21879: put
  db->Put(write_options, "key11017", "value99404");

  // op 21880: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3051", "key6461", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3051, key6461) size=" << scan_result.size() << std::endl;
  }

  // op 21881: put
  db->Put(write_options, "key8291", "value109452");

  // op 21882: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0564", "key11087", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0564, key11087) size=" << scan_result.size() << std::endl;
  }

  // op 21883: delete
  db->Delete(write_options, "key8368");

  // op 21884: put
  db->Put(write_options, "key0979", "value11454");

  // op 21885: put
  db->Put(write_options, "key10116", "value103351");

  // op 21886: get
  status = db->Get(read_options, "key4048", &value);
  if (status.ok()) {
    out << "GET key4048 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21887: put
  db->Put(write_options, "key5864", "value47795");

  // op 21888: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2202", "key9811", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2202, key9811) size=" << scan_result.size() << std::endl;
  }

  // op 21889: get
  status = db->Get(read_options, "key7791", &value);
  if (status.ok()) {
    out << "GET key7791 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21890: put
  db->Put(write_options, "key2331", "value110756");

  // op 21891: put
  db->Put(write_options, "key3463", "value111018");

  // op 21892: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10974", "key11358", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10974, key11358) size=" << scan_result.size() << std::endl;
  }

  // op 21893: put
  db->Put(write_options, "key7385", "value46413");

  // op 21894: put
  db->Put(write_options, "key6578", "value0234");

  // op 21895: delete_range
  status = db->DeleteRange(write_options, "key2540", "key6202");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 21896: get
  status = db->Get(read_options, "key10493", &value);
  if (status.ok()) {
    out << "GET key10493 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21897: delete
  db->Delete(write_options, "key3588");

  // op 21898: get
  status = db->Get(read_options, "key3411", &value);
  if (status.ok()) {
    out << "GET key3411 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21899: put
  db->Put(write_options, "key0909", "value59837");

  // op 21900: put
  db->Put(write_options, "key11427", "value54541");

  // op 21901: put
  db->Put(write_options, "key10684", "value27649");

  // op 21902: put
  db->Put(write_options, "key4560", "value39643");

  // op 21903: delete_range
  status = db->DeleteRange(write_options, "key1072", "key8863");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 21904: put
  db->Put(write_options, "key4376", "value30621");

  // op 21905: put
  db->Put(write_options, "key2489", "value23602");

  // op 21906: get
  status = db->Get(read_options, "key7177", &value);
  if (status.ok()) {
    out << "GET key7177 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21907: put
  db->Put(write_options, "key2582", "value89041");

  // op 21908: delete
  db->Delete(write_options, "key0692");

  // op 21909: get
  status = db->Get(read_options, "key8505", &value);
  if (status.ok()) {
    out << "GET key8505 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21910: delete
  db->Delete(write_options, "key1570");

  // op 21911: put
  db->Put(write_options, "key8944", "value77118");

  // op 21912: get
  status = db->Get(read_options, "key8652", &value);
  if (status.ok()) {
    out << "GET key8652 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21913: put
  db->Put(write_options, "key8386", "value35381");

  // op 21914: put
  db->Put(write_options, "key6831", "value65435");

  // op 21915: put
  db->Put(write_options, "key0116", "value23156");

  // op 21916: put
  db->Put(write_options, "key2676", "value113655");

  // op 21917: delete_range
  status = db->DeleteRange(write_options, "key5482", "key5849");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 21918: put
  db->Put(write_options, "key3885", "value45490");

  // op 21919: get
  status = db->Get(read_options, "key11705", &value);
  if (status.ok()) {
    out << "GET key11705 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21920: get
  status = db->Get(read_options, "key7281", &value);
  if (status.ok()) {
    out << "GET key7281 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21921: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1199", "key8137", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1199, key8137) size=" << scan_result.size() << std::endl;
  }

  // op 21922: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7099", "key10077", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7099, key10077) size=" << scan_result.size() << std::endl;
  }

  // op 21923: get
  status = db->Get(read_options, "key3066", &value);
  if (status.ok()) {
    out << "GET key3066 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21924: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9658", "key11257", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9658, key11257) size=" << scan_result.size() << std::endl;
  }

  // op 21925: get
  status = db->Get(read_options, "key9779", &value);
  if (status.ok()) {
    out << "GET key9779 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21926: delete
  db->Delete(write_options, "key1645");

  // op 21927: put
  db->Put(write_options, "key8852", "value97196");

  // op 21928: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 21929: put
  db->Put(write_options, "key8178", "value79211");

  // op 21930: delete_range
  status = db->DeleteRange(write_options, "key2392", "key2542");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 21931: get
  status = db->Get(read_options, "key9735", &value);
  if (status.ok()) {
    out << "GET key9735 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21932: get
  status = db->Get(read_options, "key4179", &value);
  if (status.ok()) {
    out << "GET key4179 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21933: put
  db->Put(write_options, "key0040", "value41546");

  // op 21934: delete_range
  status = db->DeleteRange(write_options, "key9157", "key11062");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 21935: put
  db->Put(write_options, "key4045", "value110507");

  // op 21936: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 21937: put
  db->Put(write_options, "key11570", "value63212");

  // op 21938: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2563", "key11469", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2563, key11469) size=" << scan_result.size() << std::endl;
  }

  // op 21939: get
  status = db->Get(read_options, "key6429", &value);
  if (status.ok()) {
    out << "GET key6429 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21940: get
  status = db->Get(read_options, "key8710", &value);
  if (status.ok()) {
    out << "GET key8710 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21941: get
  status = db->Get(read_options, "key5997", &value);
  if (status.ok()) {
    out << "GET key5997 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21942: delete
  db->Delete(write_options, "key10695");

  // op 21943: delete
  db->Delete(write_options, "key11413");

  // op 21944: put
  db->Put(write_options, "key8006", "value21336");

  // op 21945: get
  status = db->Get(read_options, "key7211", &value);
  if (status.ok()) {
    out << "GET key7211 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21946: put
  db->Put(write_options, "key6895", "value9425");

  // op 21947: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2456", "key9255", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2456, key9255) size=" << scan_result.size() << std::endl;
  }

  // op 21948: get
  status = db->Get(read_options, "key9205", &value);
  if (status.ok()) {
    out << "GET key9205 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21949: delete_range
  status = db->DeleteRange(write_options, "key0493", "key5494");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 21950: get
  status = db->Get(read_options, "key9629", &value);
  if (status.ok()) {
    out << "GET key9629 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21951: put
  db->Put(write_options, "key0198", "value90674");

  // op 21952: delete_range
  status = db->DeleteRange(write_options, "key0402", "key9089");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 21953: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0522", "key4024", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0522, key4024) size=" << scan_result.size() << std::endl;
  }

  // op 21954: put
  db->Put(write_options, "key3186", "value118185");

  // op 21955: delete_range
  status = db->DeleteRange(write_options, "key2196", "key9448");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 21956: put
  db->Put(write_options, "key0618", "value102320");

  // op 21957: put
  db->Put(write_options, "key4591", "value5858");

  // op 21958: put
  db->Put(write_options, "key11127", "value23880");

  // op 21959: delete
  db->Delete(write_options, "key9305");

  // op 21960: put
  db->Put(write_options, "key0152", "value47284");

  // op 21961: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 21962: put
  db->Put(write_options, "key10628", "value2872");

  // op 21963: delete_range
  status = db->DeleteRange(write_options, "key6311", "key10286");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 21964: get
  status = db->Get(read_options, "key2834", &value);
  if (status.ok()) {
    out << "GET key2834 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21965: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5420", "key5492", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5420, key5492) size=" << scan_result.size() << std::endl;
  }

  // op 21966: put
  db->Put(write_options, "key8202", "value7695");

  // op 21967: put
  db->Put(write_options, "key11030", "value38864");

  // op 21968: put
  db->Put(write_options, "key0532", "value61837");

  // op 21969: get
  status = db->Get(read_options, "key10510", &value);
  if (status.ok()) {
    out << "GET key10510 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21970: get
  status = db->Get(read_options, "key8504", &value);
  if (status.ok()) {
    out << "GET key8504 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21971: delete
  db->Delete(write_options, "key8215");

  // op 21972: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2628", "key5658", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2628, key5658) size=" << scan_result.size() << std::endl;
  }

  // op 21973: put
  db->Put(write_options, "key2476", "value114009");

  // op 21974: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 21975: delete
  db->Delete(write_options, "key10149");

  // op 21976: put
  db->Put(write_options, "key8067", "value5446");

  // op 21977: put
  db->Put(write_options, "key5125", "value69472");

  // op 21978: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6638", "key8088", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6638, key8088) size=" << scan_result.size() << std::endl;
  }

  // op 21979: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 21980: get
  status = db->Get(read_options, "key8063", &value);
  if (status.ok()) {
    out << "GET key8063 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21981: put
  db->Put(write_options, "key7748", "value40130");

  // op 21982: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1005", "key2729", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1005, key2729) size=" << scan_result.size() << std::endl;
  }

  // op 21983: get
  status = db->Get(read_options, "key5823", &value);
  if (status.ok()) {
    out << "GET key5823 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21984: get
  status = db->Get(read_options, "key8613", &value);
  if (status.ok()) {
    out << "GET key8613 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21985: delete
  db->Delete(write_options, "key2133");

  // op 21986: get
  status = db->Get(read_options, "key4337", &value);
  if (status.ok()) {
    out << "GET key4337 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21987: get
  status = db->Get(read_options, "key2353", &value);
  if (status.ok()) {
    out << "GET key2353 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21988: get
  status = db->Get(read_options, "key9848", &value);
  if (status.ok()) {
    out << "GET key9848 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21989: delete
  db->Delete(write_options, "key10757");

  // op 21990: put
  db->Put(write_options, "key7952", "value63581");

  // op 21991: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7047", "key8724", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7047, key8724) size=" << scan_result.size() << std::endl;
  }

  // op 21992: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2614", "key10720", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2614, key10720) size=" << scan_result.size() << std::endl;
  }

  // op 21993: put
  db->Put(write_options, "key1987", "value110505");

  // op 21994: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8954", "key10812", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8954, key10812) size=" << scan_result.size() << std::endl;
  }

  // op 21995: get
  status = db->Get(read_options, "key5739", &value);
  if (status.ok()) {
    out << "GET key5739 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21996: delete
  db->Delete(write_options, "key0355");

  // op 21997: get
  status = db->Get(read_options, "key4394", &value);
  if (status.ok()) {
    out << "GET key4394 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 21998: delete_range
  status = db->DeleteRange(write_options, "key9815", "key10496");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 21999: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2678", "key10587", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2678, key10587) size=" << scan_result.size() << std::endl;
  }

  // op 22000: put
  db->Put(write_options, "key0149", "value77822");

  // op 22001: delete_range
  status = db->DeleteRange(write_options, "key1909", "key3613");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 22002: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2579", "key7179", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2579, key7179) size=" << scan_result.size() << std::endl;
  }

  // op 22003: put
  db->Put(write_options, "key0391", "value85471");

  // op 22004: put
  db->Put(write_options, "key3827", "value34345");

  // op 22005: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3319", "key5796", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3319, key5796) size=" << scan_result.size() << std::endl;
  }

  // op 22006: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5659", "key5747", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5659, key5747) size=" << scan_result.size() << std::endl;
  }

  // op 22007: put
  db->Put(write_options, "key2483", "value19385");

  // op 22008: delete_range
  status = db->DeleteRange(write_options, "key0435", "key7637");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 22009: put
  db->Put(write_options, "key10358", "value116942");

  // op 22010: get
  status = db->Get(read_options, "key0585", &value);
  if (status.ok()) {
    out << "GET key0585 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22011: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9301", "key9383", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9301, key9383) size=" << scan_result.size() << std::endl;
  }

  // op 22012: put
  db->Put(write_options, "key8983", "value63914");

  // op 22013: get
  status = db->Get(read_options, "key8055", &value);
  if (status.ok()) {
    out << "GET key8055 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22014: delete
  db->Delete(write_options, "key10888");

  // op 22015: put
  db->Put(write_options, "key9219", "value53909");

  // op 22016: get
  status = db->Get(read_options, "key9335", &value);
  if (status.ok()) {
    out << "GET key9335 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22017: delete_range
  status = db->DeleteRange(write_options, "key2334", "key2341");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 22018: get
  status = db->Get(read_options, "key8934", &value);
  if (status.ok()) {
    out << "GET key8934 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22019: put
  db->Put(write_options, "key5742", "value103310");

  // op 22020: delete_range
  status = db->DeleteRange(write_options, "key1692", "key7589");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 22021: put
  db->Put(write_options, "key10088", "value96192");

  // op 22022: put
  db->Put(write_options, "key5659", "value85538");

  // op 22023: delete
  db->Delete(write_options, "key10600");

  // op 22024: delete
  db->Delete(write_options, "key5505");

  // op 22025: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0124", "key2318", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0124, key2318) size=" << scan_result.size() << std::endl;
  }

  // op 22026: get
  status = db->Get(read_options, "key3355", &value);
  if (status.ok()) {
    out << "GET key3355 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22027: put
  db->Put(write_options, "key8462", "value32185");

  // op 22028: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 22029: put
  db->Put(write_options, "key2567", "value24508");

  // op 22030: get
  status = db->Get(read_options, "key4579", &value);
  if (status.ok()) {
    out << "GET key4579 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22031: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4459", "key9602", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4459, key9602) size=" << scan_result.size() << std::endl;
  }

  // op 22032: delete
  db->Delete(write_options, "key7161");

  // op 22033: delete_range
  status = db->DeleteRange(write_options, "key5119", "key9828");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 22034: delete
  db->Delete(write_options, "key9451");

  // op 22035: put
  db->Put(write_options, "key9895", "value59584");

  // op 22036: put
  db->Put(write_options, "key0460", "value12553");

  // op 22037: put
  db->Put(write_options, "key1594", "value86295");

  // op 22038: delete
  db->Delete(write_options, "key6633");

  // op 22039: get
  status = db->Get(read_options, "key0272", &value);
  if (status.ok()) {
    out << "GET key0272 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22040: get
  status = db->Get(read_options, "key6261", &value);
  if (status.ok()) {
    out << "GET key6261 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22041: delete_range
  status = db->DeleteRange(write_options, "key0600", "key8415");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 22042: put
  db->Put(write_options, "key2958", "value71931");

  // op 22043: put
  db->Put(write_options, "key7017", "value60073");

  // op 22044: delete_range
  status = db->DeleteRange(write_options, "key1671", "key4785");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 22045: get
  status = db->Get(read_options, "key3457", &value);
  if (status.ok()) {
    out << "GET key3457 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22046: put
  db->Put(write_options, "key2646", "value41361");

  // op 22047: put
  db->Put(write_options, "key4423", "value108389");

  // op 22048: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7008", "key8399", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7008, key8399) size=" << scan_result.size() << std::endl;
  }

  // op 22049: get
  status = db->Get(read_options, "key4912", &value);
  if (status.ok()) {
    out << "GET key4912 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22050: put
  db->Put(write_options, "key5847", "value87585");

  // op 22051: delete_range
  status = db->DeleteRange(write_options, "key4678", "key11478");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 22052: put
  db->Put(write_options, "key8223", "value67622");

  // op 22053: delete
  db->Delete(write_options, "key0795");

  // op 22054: get
  status = db->Get(read_options, "key7786", &value);
  if (status.ok()) {
    out << "GET key7786 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22055: put
  db->Put(write_options, "key7586", "value17032");

  // op 22056: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5511", "key5939", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5511, key5939) size=" << scan_result.size() << std::endl;
  }

  // op 22057: put
  db->Put(write_options, "key10060", "value91577");

  // op 22058: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0923", "key9591", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0923, key9591) size=" << scan_result.size() << std::endl;
  }

  // op 22059: put
  db->Put(write_options, "key1697", "value11725");

  // op 22060: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5106", "key9837", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5106, key9837) size=" << scan_result.size() << std::endl;
  }

  // op 22061: put
  db->Put(write_options, "key6613", "value29116");

  // op 22062: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 22063: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8061", "key8578", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8061, key8578) size=" << scan_result.size() << std::endl;
  }

  // op 22064: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7719", "key9240", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7719, key9240) size=" << scan_result.size() << std::endl;
  }

  // op 22065: put
  db->Put(write_options, "key0484", "value65906");

  // op 22066: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 22067: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 22068: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1802", "key7054", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1802, key7054) size=" << scan_result.size() << std::endl;
  }

  // op 22069: delete
  db->Delete(write_options, "key4509");

  // op 22070: get
  status = db->Get(read_options, "key10026", &value);
  if (status.ok()) {
    out << "GET key10026 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22071: put
  db->Put(write_options, "key7312", "value86643");

  // op 22072: delete_range
  status = db->DeleteRange(write_options, "key2096", "key8243");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 22073: get
  status = db->Get(read_options, "key4334", &value);
  if (status.ok()) {
    out << "GET key4334 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22074: delete_range
  status = db->DeleteRange(write_options, "key8201", "key9579");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 22075: put
  db->Put(write_options, "key10390", "value64883");

  // op 22076: put
  db->Put(write_options, "key3660", "value29518");

  // op 22077: put
  db->Put(write_options, "key7807", "value82176");

  // op 22078: put
  db->Put(write_options, "key6813", "value82225");

  // op 22079: delete
  db->Delete(write_options, "key5850");

  // op 22080: get
  status = db->Get(read_options, "key4308", &value);
  if (status.ok()) {
    out << "GET key4308 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22081: put
  db->Put(write_options, "key8103", "value41250");

  // op 22082: put
  db->Put(write_options, "key8397", "value69863");

  // op 22083: get
  status = db->Get(read_options, "key8611", &value);
  if (status.ok()) {
    out << "GET key8611 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22084: get
  status = db->Get(read_options, "key9627", &value);
  if (status.ok()) {
    out << "GET key9627 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22085: get
  status = db->Get(read_options, "key0131", &value);
  if (status.ok()) {
    out << "GET key0131 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22086: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 22087: delete_range
  status = db->DeleteRange(write_options, "key4181", "key11264");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 22088: put
  db->Put(write_options, "key8410", "value79387");

  // op 22089: get
  status = db->Get(read_options, "key10242", &value);
  if (status.ok()) {
    out << "GET key10242 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22090: put
  db->Put(write_options, "key9203", "value81630");

  // op 22091: delete_range
  status = db->DeleteRange(write_options, "key7511", "key11640");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 22092: put
  db->Put(write_options, "key0316", "value5788");

  // op 22093: delete_range
  status = db->DeleteRange(write_options, "key3836", "key6082");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 22094: delete
  db->Delete(write_options, "key2026");

  // op 22095: delete_range
  status = db->DeleteRange(write_options, "key0305", "key10398");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 22096: get
  status = db->Get(read_options, "key0897", &value);
  if (status.ok()) {
    out << "GET key0897 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22097: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0268", "key9438", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0268, key9438) size=" << scan_result.size() << std::endl;
  }

  // op 22098: delete_range
  status = db->DeleteRange(write_options, "key3244", "key4168");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 22099: get
  status = db->Get(read_options, "key4989", &value);
  if (status.ok()) {
    out << "GET key4989 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22100: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8028", "key10163", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8028, key10163) size=" << scan_result.size() << std::endl;
  }

  // op 22101: get
  status = db->Get(read_options, "key10615", &value);
  if (status.ok()) {
    out << "GET key10615 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22102: put
  db->Put(write_options, "key10149", "value18839");

  // op 22103: put
  db->Put(write_options, "key4860", "value45721");

  // op 22104: put
  db->Put(write_options, "key7330", "value12765");

  // op 22105: get
  status = db->Get(read_options, "key6473", &value);
  if (status.ok()) {
    out << "GET key6473 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22106: put
  db->Put(write_options, "key9823", "value37588");

  // op 22107: get
  status = db->Get(read_options, "key9327", &value);
  if (status.ok()) {
    out << "GET key9327 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22108: get
  status = db->Get(read_options, "key10942", &value);
  if (status.ok()) {
    out << "GET key10942 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22109: put
  db->Put(write_options, "key3172", "value32308");

  // op 22110: delete
  db->Delete(write_options, "key9606");

  // op 22111: get
  status = db->Get(read_options, "key4013", &value);
  if (status.ok()) {
    out << "GET key4013 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22112: put
  db->Put(write_options, "key4111", "value9771");

  // op 22113: put
  db->Put(write_options, "key2122", "value116726");

  // op 22114: delete_range
  status = db->DeleteRange(write_options, "key0022", "key4934");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 22115: delete
  db->Delete(write_options, "key0289");

  // op 22116: delete_range
  status = db->DeleteRange(write_options, "key2343", "key6146");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 22117: get
  status = db->Get(read_options, "key10513", &value);
  if (status.ok()) {
    out << "GET key10513 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22118: delete
  db->Delete(write_options, "key8833");

  // op 22119: delete
  db->Delete(write_options, "key6104");

  // op 22120: get
  status = db->Get(read_options, "key3243", &value);
  if (status.ok()) {
    out << "GET key3243 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22121: put
  db->Put(write_options, "key3841", "value43678");

  // op 22122: put
  db->Put(write_options, "key8229", "value17339");

  // op 22123: delete_range
  status = db->DeleteRange(write_options, "key1870", "key4383");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 22124: delete_range
  status = db->DeleteRange(write_options, "key0682", "key11620");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 22125: get
  status = db->Get(read_options, "key7383", &value);
  if (status.ok()) {
    out << "GET key7383 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22126: delete
  db->Delete(write_options, "key0151");

  // op 22127: put
  db->Put(write_options, "key5791", "value12052");

  // op 22128: put
  db->Put(write_options, "key7867", "value97894");

  // op 22129: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 22130: get
  status = db->Get(read_options, "key4192", &value);
  if (status.ok()) {
    out << "GET key4192 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22131: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 22132: delete
  db->Delete(write_options, "key10062");

  // op 22133: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2393", "key11542", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2393, key11542) size=" << scan_result.size() << std::endl;
  }

  // op 22134: delete_range
  status = db->DeleteRange(write_options, "key0245", "key11105");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 22135: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2021", "key7584", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2021, key7584) size=" << scan_result.size() << std::endl;
  }

  // op 22136: put
  db->Put(write_options, "key7586", "value83009");

  // op 22137: put
  db->Put(write_options, "key7325", "value103009");

  // op 22138: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 22139: get
  status = db->Get(read_options, "key5955", &value);
  if (status.ok()) {
    out << "GET key5955 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22140: get
  status = db->Get(read_options, "key1529", &value);
  if (status.ok()) {
    out << "GET key1529 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22141: delete_range
  status = db->DeleteRange(write_options, "key7767", "key8274");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 22142: put
  db->Put(write_options, "key2168", "value93426");

  // op 22143: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10812", "key11113", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10812, key11113) size=" << scan_result.size() << std::endl;
  }

  // op 22144: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6149", "key11287", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6149, key11287) size=" << scan_result.size() << std::endl;
  }

  // op 22145: put
  db->Put(write_options, "key9621", "value113507");

  // op 22146: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10485", "key10595", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10485, key10595) size=" << scan_result.size() << std::endl;
  }

  // op 22147: delete
  db->Delete(write_options, "key3956");

  // op 22148: get
  status = db->Get(read_options, "key7356", &value);
  if (status.ok()) {
    out << "GET key7356 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22149: put
  db->Put(write_options, "key3647", "value12363");

  // op 22150: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4608", "key8669", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4608, key8669) size=" << scan_result.size() << std::endl;
  }

  // op 22151: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6867", "key11213", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6867, key11213) size=" << scan_result.size() << std::endl;
  }

  // op 22152: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 22153: get
  status = db->Get(read_options, "key9228", &value);
  if (status.ok()) {
    out << "GET key9228 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22154: put
  db->Put(write_options, "key10265", "value34552");

  // op 22155: put
  db->Put(write_options, "key8976", "value75780");

  // op 22156: get
  status = db->Get(read_options, "key2002", &value);
  if (status.ok()) {
    out << "GET key2002 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22157: put
  db->Put(write_options, "key9479", "value40714");

  // op 22158: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3609", "key11579", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3609, key11579) size=" << scan_result.size() << std::endl;
  }

  // op 22159: get
  status = db->Get(read_options, "key0308", &value);
  if (status.ok()) {
    out << "GET key0308 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22160: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 22161: put
  db->Put(write_options, "key8286", "value33132");

  // op 22162: delete_range
  status = db->DeleteRange(write_options, "key4350", "key4678");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 22163: get
  status = db->Get(read_options, "key10890", &value);
  if (status.ok()) {
    out << "GET key10890 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22164: delete_range
  status = db->DeleteRange(write_options, "key0114", "key3724");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 22165: put
  db->Put(write_options, "key4181", "value25831");

  // op 22166: delete
  db->Delete(write_options, "key5854");

  // op 22167: put
  db->Put(write_options, "key9065", "value28018");

  // op 22168: delete
  db->Delete(write_options, "key11509");

  // op 22169: put
  db->Put(write_options, "key5462", "value107388");

  // op 22170: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9105", "key9142", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9105, key9142) size=" << scan_result.size() << std::endl;
  }

  // op 22171: delete_range
  status = db->DeleteRange(write_options, "key1973", "key2161");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 22172: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 22173: delete
  db->Delete(write_options, "key6122");

  // op 22174: put
  db->Put(write_options, "key3427", "value36349");

  // op 22175: get
  status = db->Get(read_options, "key7022", &value);
  if (status.ok()) {
    out << "GET key7022 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22176: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3964", "key9006", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3964, key9006) size=" << scan_result.size() << std::endl;
  }

  // op 22177: put
  db->Put(write_options, "key1207", "value27160");

  // op 22178: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4180", "key7460", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4180, key7460) size=" << scan_result.size() << std::endl;
  }

  // op 22179: delete_range
  status = db->DeleteRange(write_options, "key1876", "key8982");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 22180: put
  db->Put(write_options, "key10076", "value117693");

  // op 22181: delete_range
  status = db->DeleteRange(write_options, "key1649", "key2941");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 22182: delete_range
  status = db->DeleteRange(write_options, "key5599", "key11049");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 22183: get
  status = db->Get(read_options, "key11724", &value);
  if (status.ok()) {
    out << "GET key11724 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22184: put
  db->Put(write_options, "key11244", "value86054");

  // op 22185: delete_range
  status = db->DeleteRange(write_options, "key5201", "key6012");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 22186: put
  db->Put(write_options, "key6837", "value89797");

  // op 22187: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4983", "key8206", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4983, key8206) size=" << scan_result.size() << std::endl;
  }

  // op 22188: delete
  db->Delete(write_options, "key7489");

  // op 22189: put
  db->Put(write_options, "key0514", "value58343");

  // op 22190: put
  db->Put(write_options, "key3647", "value94286");

  // op 22191: put
  db->Put(write_options, "key10496", "value94288");

  // op 22192: put
  db->Put(write_options, "key7374", "value53668");

  // op 22193: delete
  db->Delete(write_options, "key0566");

  // op 22194: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4129", "key7695", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4129, key7695) size=" << scan_result.size() << std::endl;
  }

  // op 22195: delete
  db->Delete(write_options, "key9358");

  // op 22196: put
  db->Put(write_options, "key2668", "value105672");

  // op 22197: put
  db->Put(write_options, "key10501", "value27559");

  // op 22198: get
  status = db->Get(read_options, "key7469", &value);
  if (status.ok()) {
    out << "GET key7469 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22199: delete
  db->Delete(write_options, "key2866");

  // op 22200: put
  db->Put(write_options, "key10792", "value113402");

  // op 22201: put
  db->Put(write_options, "key0542", "value111974");

  // op 22202: put
  db->Put(write_options, "key11564", "value45036");

  // op 22203: delete
  db->Delete(write_options, "key3263");

  // op 22204: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1864", "key6998", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1864, key6998) size=" << scan_result.size() << std::endl;
  }

  // op 22205: delete_range
  status = db->DeleteRange(write_options, "key6970", "key7152");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 22206: put
  db->Put(write_options, "key6324", "value107634");

  // op 22207: put
  db->Put(write_options, "key10251", "value80325");

  // op 22208: put
  db->Put(write_options, "key2969", "value112441");

  // op 22209: delete
  db->Delete(write_options, "key8208");

  // op 22210: put
  db->Put(write_options, "key10134", "value93436");

  // op 22211: put
  db->Put(write_options, "key10625", "value58614");

  // op 22212: get
  status = db->Get(read_options, "key9117", &value);
  if (status.ok()) {
    out << "GET key9117 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22213: delete_range
  status = db->DeleteRange(write_options, "key0521", "key8735");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 22214: delete
  db->Delete(write_options, "key2008");

  // op 22215: delete
  db->Delete(write_options, "key10830");

  // op 22216: get
  status = db->Get(read_options, "key5084", &value);
  if (status.ok()) {
    out << "GET key5084 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22217: put
  db->Put(write_options, "key7585", "value43901");

  // op 22218: get
  status = db->Get(read_options, "key11434", &value);
  if (status.ok()) {
    out << "GET key11434 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22219: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3790", "key8927", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3790, key8927) size=" << scan_result.size() << std::endl;
  }

  // op 22220: put
  db->Put(write_options, "key5782", "value93608");

  // op 22221: put
  db->Put(write_options, "key2755", "value110328");

  // op 22222: get
  status = db->Get(read_options, "key1803", &value);
  if (status.ok()) {
    out << "GET key1803 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22223: put
  db->Put(write_options, "key5312", "value12290");

  // op 22224: get
  status = db->Get(read_options, "key5059", &value);
  if (status.ok()) {
    out << "GET key5059 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22225: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7669", "key11432", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7669, key11432) size=" << scan_result.size() << std::endl;
  }

  // op 22226: delete
  db->Delete(write_options, "key7016");

  // op 22227: delete_range
  status = db->DeleteRange(write_options, "key1091", "key4794");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 22228: put
  db->Put(write_options, "key6408", "value12595");

  // op 22229: put
  db->Put(write_options, "key5527", "value43634");

  // op 22230: put
  db->Put(write_options, "key7888", "value17099");

  // op 22231: get
  status = db->Get(read_options, "key3117", &value);
  if (status.ok()) {
    out << "GET key3117 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22232: get
  status = db->Get(read_options, "key10385", &value);
  if (status.ok()) {
    out << "GET key10385 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22233: delete
  db->Delete(write_options, "key8712");

  // op 22234: put
  db->Put(write_options, "key5776", "value35150");

  // op 22235: put
  db->Put(write_options, "key7104", "value46638");

  // op 22236: delete
  db->Delete(write_options, "key0629");

  // op 22237: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 22238: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5040", "key11362", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5040, key11362) size=" << scan_result.size() << std::endl;
  }

  // op 22239: get
  status = db->Get(read_options, "key11612", &value);
  if (status.ok()) {
    out << "GET key11612 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22240: put
  db->Put(write_options, "key0477", "value41315");

  // op 22241: put
  db->Put(write_options, "key3835", "value17377");

  // op 22242: delete_range
  status = db->DeleteRange(write_options, "key2188", "key5613");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 22243: put
  db->Put(write_options, "key7663", "value2245");

  // op 22244: get
  status = db->Get(read_options, "key9688", &value);
  if (status.ok()) {
    out << "GET key9688 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22245: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 22246: get
  status = db->Get(read_options, "key10678", &value);
  if (status.ok()) {
    out << "GET key10678 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22247: put
  db->Put(write_options, "key4546", "value63077");

  // op 22248: get
  status = db->Get(read_options, "key4820", &value);
  if (status.ok()) {
    out << "GET key4820 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22249: delete_range
  status = db->DeleteRange(write_options, "key1745", "key8562");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 22250: delete_range
  status = db->DeleteRange(write_options, "key6296", "key9988");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 22251: put
  db->Put(write_options, "key0824", "value12324");

  // op 22252: put
  db->Put(write_options, "key2537", "value55503");

  // op 22253: get
  status = db->Get(read_options, "key7275", &value);
  if (status.ok()) {
    out << "GET key7275 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22254: put
  db->Put(write_options, "key7114", "value11678");

  // op 22255: put
  db->Put(write_options, "key1402", "value34502");

  // op 22256: put
  db->Put(write_options, "key5048", "value94485");

  // op 22257: get
  status = db->Get(read_options, "key9856", &value);
  if (status.ok()) {
    out << "GET key9856 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22258: put
  db->Put(write_options, "key2012", "value94554");

  // op 22259: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 22260: get
  status = db->Get(read_options, "key9679", &value);
  if (status.ok()) {
    out << "GET key9679 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22261: put
  db->Put(write_options, "key6678", "value52468");

  // op 22262: put
  db->Put(write_options, "key6091", "value61112");

  // op 22263: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8024", "key10244", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8024, key10244) size=" << scan_result.size() << std::endl;
  }

  // op 22264: delete
  db->Delete(write_options, "key5679");

  // op 22265: get
  status = db->Get(read_options, "key9480", &value);
  if (status.ok()) {
    out << "GET key9480 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22266: put
  db->Put(write_options, "key11616", "value43854");

  // op 22267: put
  db->Put(write_options, "key10676", "value70573");

  // op 22268: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6630", "key9792", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6630, key9792) size=" << scan_result.size() << std::endl;
  }

  // op 22269: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1053", "key4251", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1053, key4251) size=" << scan_result.size() << std::endl;
  }

  // op 22270: delete
  db->Delete(write_options, "key6238");

  // op 22271: delete_range
  status = db->DeleteRange(write_options, "key3756", "key6642");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 22272: put
  db->Put(write_options, "key8393", "value24658");

  // op 22273: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2879", "key6577", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2879, key6577) size=" << scan_result.size() << std::endl;
  }

  // op 22274: put
  db->Put(write_options, "key5391", "value82562");

  // op 22275: put
  db->Put(write_options, "key8682", "value103816");

  // op 22276: delete_range
  status = db->DeleteRange(write_options, "key2961", "key9896");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 22277: put
  db->Put(write_options, "key10896", "value23378");

  // op 22278: put
  db->Put(write_options, "key7314", "value45493");

  // op 22279: put
  db->Put(write_options, "key0640", "value74433");

  // op 22280: delete
  db->Delete(write_options, "key2021");

  // op 22281: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9949", "key10844", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9949, key10844) size=" << scan_result.size() << std::endl;
  }

  // op 22282: put
  db->Put(write_options, "key10184", "value96397");

  // op 22283: put
  db->Put(write_options, "key10928", "value41567");

  // op 22284: put
  db->Put(write_options, "key2650", "value91057");

  // op 22285: get
  status = db->Get(read_options, "key6570", &value);
  if (status.ok()) {
    out << "GET key6570 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22286: delete
  db->Delete(write_options, "key3026");

  // op 22287: put
  db->Put(write_options, "key4993", "value12562");

  // op 22288: put
  db->Put(write_options, "key4308", "value5255");

  // op 22289: delete_range
  status = db->DeleteRange(write_options, "key6915", "key11386");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 22290: put
  db->Put(write_options, "key4011", "value13798");

  // op 22291: get
  status = db->Get(read_options, "key1197", &value);
  if (status.ok()) {
    out << "GET key1197 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22292: delete_range
  status = db->DeleteRange(write_options, "key6059", "key8652");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 22293: put
  db->Put(write_options, "key8924", "value62629");

  // op 22294: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 22295: delete_range
  status = db->DeleteRange(write_options, "key1722", "key2854");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 22296: get
  status = db->Get(read_options, "key5587", &value);
  if (status.ok()) {
    out << "GET key5587 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22297: delete_range
  status = db->DeleteRange(write_options, "key0379", "key11399");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 22298: get
  status = db->Get(read_options, "key3851", &value);
  if (status.ok()) {
    out << "GET key3851 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22299: get
  status = db->Get(read_options, "key0771", &value);
  if (status.ok()) {
    out << "GET key0771 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22300: put
  db->Put(write_options, "key2035", "value52355");

  // op 22301: put
  db->Put(write_options, "key4035", "value113482");

  // op 22302: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8966", "key10674", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8966, key10674) size=" << scan_result.size() << std::endl;
  }

  // op 22303: delete
  db->Delete(write_options, "key8275");

  // op 22304: delete
  db->Delete(write_options, "key4310");

  // op 22305: delete_range
  status = db->DeleteRange(write_options, "key1231", "key2324");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 22306: get
  status = db->Get(read_options, "key10127", &value);
  if (status.ok()) {
    out << "GET key10127 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22307: put
  db->Put(write_options, "key7510", "value99181");

  // op 22308: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5582", "key7662", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5582, key7662) size=" << scan_result.size() << std::endl;
  }

  // op 22309: put
  db->Put(write_options, "key1964", "value2070");

  // op 22310: put
  db->Put(write_options, "key0787", "value90591");

  // op 22311: put
  db->Put(write_options, "key7929", "value69989");

  // op 22312: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 22313: put
  db->Put(write_options, "key6166", "value19494");

  // op 22314: put
  db->Put(write_options, "key4847", "value77025");

  // op 22315: delete
  db->Delete(write_options, "key3394");

  // op 22316: put
  db->Put(write_options, "key0164", "value116663");

  // op 22317: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6216", "key8234", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6216, key8234) size=" << scan_result.size() << std::endl;
  }

  // op 22318: delete
  db->Delete(write_options, "key6409");

  // op 22319: put
  db->Put(write_options, "key6698", "value36717");

  // op 22320: delete_range
  status = db->DeleteRange(write_options, "key8281", "key11055");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 22321: put
  db->Put(write_options, "key11565", "value46083");

  // op 22322: put
  db->Put(write_options, "key2658", "value114228");

  // op 22323: delete
  db->Delete(write_options, "key4008");

  // op 22324: delete
  db->Delete(write_options, "key1501");

  // op 22325: get
  status = db->Get(read_options, "key8652", &value);
  if (status.ok()) {
    out << "GET key8652 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22326: delete
  db->Delete(write_options, "key10862");

  // op 22327: put
  db->Put(write_options, "key11630", "value39339");

  // op 22328: put
  db->Put(write_options, "key10447", "value95155");

  // op 22329: delete_range
  status = db->DeleteRange(write_options, "key8039", "key8813");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 22330: put
  db->Put(write_options, "key4081", "value58362");

  // op 22331: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5715", "key10928", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5715, key10928) size=" << scan_result.size() << std::endl;
  }

  // op 22332: put
  db->Put(write_options, "key4837", "value74967");

  // op 22333: get
  status = db->Get(read_options, "key10838", &value);
  if (status.ok()) {
    out << "GET key10838 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22334: put
  db->Put(write_options, "key0142", "value57905");

  // op 22335: put
  db->Put(write_options, "key10257", "value19457");

  // op 22336: delete
  db->Delete(write_options, "key10932");

  // op 22337: put
  db->Put(write_options, "key0141", "value27388");

  // op 22338: delete
  db->Delete(write_options, "key2639");

  // op 22339: delete_range
  status = db->DeleteRange(write_options, "key3518", "key7689");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 22340: put
  db->Put(write_options, "key7900", "value88103");

  // op 22341: put
  db->Put(write_options, "key5496", "value12105");

  // op 22342: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0522", "key3476", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0522, key3476) size=" << scan_result.size() << std::endl;
  }

  // op 22343: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6762", "key11553", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6762, key11553) size=" << scan_result.size() << std::endl;
  }

  // op 22344: get
  status = db->Get(read_options, "key5261", &value);
  if (status.ok()) {
    out << "GET key5261 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22345: put
  db->Put(write_options, "key3917", "value65778");

  // op 22346: delete_range
  status = db->DeleteRange(write_options, "key8436", "key8644");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 22347: get
  status = db->Get(read_options, "key3730", &value);
  if (status.ok()) {
    out << "GET key3730 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22348: put
  db->Put(write_options, "key1561", "value58395");

  // op 22349: put
  db->Put(write_options, "key1901", "value24186");

  // op 22350: get
  status = db->Get(read_options, "key5351", &value);
  if (status.ok()) {
    out << "GET key5351 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22351: delete
  db->Delete(write_options, "key0096");

  // op 22352: put
  db->Put(write_options, "key11038", "value74301");

  // op 22353: put
  db->Put(write_options, "key3208", "value53447");

  // op 22354: put
  db->Put(write_options, "key5941", "value38345");

  // op 22355: put
  db->Put(write_options, "key6207", "value99324");

  // op 22356: put
  db->Put(write_options, "key9871", "value3423");

  // op 22357: put
  db->Put(write_options, "key11296", "value99063");

  // op 22358: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1611", "key2327", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1611, key2327) size=" << scan_result.size() << std::endl;
  }

  // op 22359: get
  status = db->Get(read_options, "key4850", &value);
  if (status.ok()) {
    out << "GET key4850 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22360: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4155", "key9829", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4155, key9829) size=" << scan_result.size() << std::endl;
  }

  // op 22361: put
  db->Put(write_options, "key2313", "value85661");

  // op 22362: delete
  db->Delete(write_options, "key0160");

  // op 22363: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6414", "key10144", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6414, key10144) size=" << scan_result.size() << std::endl;
  }

  // op 22364: get
  status = db->Get(read_options, "key10085", &value);
  if (status.ok()) {
    out << "GET key10085 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22365: put
  db->Put(write_options, "key3518", "value12026");

  // op 22366: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2814", "key9091", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2814, key9091) size=" << scan_result.size() << std::endl;
  }

  // op 22367: get
  status = db->Get(read_options, "key7775", &value);
  if (status.ok()) {
    out << "GET key7775 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22368: put
  db->Put(write_options, "key2823", "value95512");

  // op 22369: delete_range
  status = db->DeleteRange(write_options, "key5714", "key6693");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 22370: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6867", "key8661", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6867, key8661) size=" << scan_result.size() << std::endl;
  }

  // op 22371: put
  db->Put(write_options, "key5775", "value62282");

  // op 22372: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8399", "key10749", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8399, key10749) size=" << scan_result.size() << std::endl;
  }

  // op 22373: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1792", "key11650", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1792, key11650) size=" << scan_result.size() << std::endl;
  }

  // op 22374: put
  db->Put(write_options, "key6578", "value115586");

  // op 22375: put
  db->Put(write_options, "key10746", "value18253");

  // op 22376: delete
  db->Delete(write_options, "key5265");

  // op 22377: delete_range
  status = db->DeleteRange(write_options, "key0418", "key11187");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 22378: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7865", "key8247", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7865, key8247) size=" << scan_result.size() << std::endl;
  }

  // op 22379: delete_range
  status = db->DeleteRange(write_options, "key0086", "key7085");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 22380: delete_range
  status = db->DeleteRange(write_options, "key5096", "key5623");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 22381: get
  status = db->Get(read_options, "key5628", &value);
  if (status.ok()) {
    out << "GET key5628 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22382: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9039", "key9834", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9039, key9834) size=" << scan_result.size() << std::endl;
  }

  // op 22383: delete_range
  status = db->DeleteRange(write_options, "key4558", "key5103");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 22384: delete_range
  status = db->DeleteRange(write_options, "key0357", "key9894");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 22385: put
  db->Put(write_options, "key5252", "value54925");

  // op 22386: get
  status = db->Get(read_options, "key6491", &value);
  if (status.ok()) {
    out << "GET key6491 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22387: delete
  db->Delete(write_options, "key8807");

  // op 22388: put
  db->Put(write_options, "key7790", "value96900");

  // op 22389: delete_range
  status = db->DeleteRange(write_options, "key2867", "key6634");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 22390: put
  db->Put(write_options, "key2854", "value69553");

  // op 22391: get
  status = db->Get(read_options, "key1469", &value);
  if (status.ok()) {
    out << "GET key1469 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22392: get
  status = db->Get(read_options, "key8717", &value);
  if (status.ok()) {
    out << "GET key8717 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22393: delete_range
  status = db->DeleteRange(write_options, "key3283", "key3607");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 22394: put
  db->Put(write_options, "key9697", "value7278");

  // op 22395: put
  db->Put(write_options, "key4613", "value103164");

  // op 22396: put
  db->Put(write_options, "key11180", "value91365");

  // op 22397: put
  db->Put(write_options, "key9191", "value9992");

  // op 22398: delete
  db->Delete(write_options, "key5610");

  // op 22399: get
  status = db->Get(read_options, "key8384", &value);
  if (status.ok()) {
    out << "GET key8384 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22400: put
  db->Put(write_options, "key3101", "value0936");

  // op 22401: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3755", "key4303", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3755, key4303) size=" << scan_result.size() << std::endl;
  }

  // op 22402: put
  db->Put(write_options, "key4323", "value117678");

  // op 22403: delete
  db->Delete(write_options, "key7689");

  // op 22404: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5732", "key7137", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5732, key7137) size=" << scan_result.size() << std::endl;
  }

  // op 22405: get
  status = db->Get(read_options, "key4536", &value);
  if (status.ok()) {
    out << "GET key4536 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22406: delete_range
  status = db->DeleteRange(write_options, "key7032", "key10304");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 22407: get
  status = db->Get(read_options, "key4484", &value);
  if (status.ok()) {
    out << "GET key4484 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22408: put
  db->Put(write_options, "key0859", "value81831");

  // op 22409: get
  status = db->Get(read_options, "key5994", &value);
  if (status.ok()) {
    out << "GET key5994 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22410: put
  db->Put(write_options, "key8559", "value106578");

  // op 22411: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 22412: get
  status = db->Get(read_options, "key4649", &value);
  if (status.ok()) {
    out << "GET key4649 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22413: delete
  db->Delete(write_options, "key11457");

  // op 22414: put
  db->Put(write_options, "key10641", "value59471");

  // op 22415: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1977", "key2637", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1977, key2637) size=" << scan_result.size() << std::endl;
  }

  // op 22416: get
  status = db->Get(read_options, "key1160", &value);
  if (status.ok()) {
    out << "GET key1160 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22417: put
  db->Put(write_options, "key8898", "value78092");

  // op 22418: put
  db->Put(write_options, "key5727", "value69650");

  // op 22419: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 22420: put
  db->Put(write_options, "key2408", "value102097");

  // op 22421: put
  db->Put(write_options, "key2033", "value102606");

  // op 22422: get
  status = db->Get(read_options, "key10086", &value);
  if (status.ok()) {
    out << "GET key10086 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22423: delete_range
  status = db->DeleteRange(write_options, "key7591", "key11062");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 22424: delete
  db->Delete(write_options, "key8832");

  // op 22425: get
  status = db->Get(read_options, "key1189", &value);
  if (status.ok()) {
    out << "GET key1189 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22426: put
  db->Put(write_options, "key5628", "value8416");

  // op 22427: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0678", "key1554", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0678, key1554) size=" << scan_result.size() << std::endl;
  }

  // op 22428: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0885", "key9642", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0885, key9642) size=" << scan_result.size() << std::endl;
  }

  // op 22429: get
  status = db->Get(read_options, "key6049", &value);
  if (status.ok()) {
    out << "GET key6049 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22430: put
  db->Put(write_options, "key8797", "value98733");

  // op 22431: put
  db->Put(write_options, "key9183", "value41605");

  // op 22432: put
  db->Put(write_options, "key8185", "value100679");

  // op 22433: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1525", "key5693", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1525, key5693) size=" << scan_result.size() << std::endl;
  }

  // op 22434: get
  status = db->Get(read_options, "key10483", &value);
  if (status.ok()) {
    out << "GET key10483 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22435: put
  db->Put(write_options, "key5636", "value90953");

  // op 22436: get
  status = db->Get(read_options, "key10446", &value);
  if (status.ok()) {
    out << "GET key10446 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22437: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6172", "key10480", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6172, key10480) size=" << scan_result.size() << std::endl;
  }

  // op 22438: put
  db->Put(write_options, "key4110", "value100394");

  // op 22439: put
  db->Put(write_options, "key0972", "value58527");

  // op 22440: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3562", "key10948", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3562, key10948) size=" << scan_result.size() << std::endl;
  }

  // op 22441: put
  db->Put(write_options, "key11228", "value89203");

  // op 22442: get
  status = db->Get(read_options, "key5143", &value);
  if (status.ok()) {
    out << "GET key5143 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22443: get
  status = db->Get(read_options, "key1654", &value);
  if (status.ok()) {
    out << "GET key1654 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22444: put
  db->Put(write_options, "key5841", "value87325");

  // op 22445: delete_range
  status = db->DeleteRange(write_options, "key1290", "key6714");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 22446: put
  db->Put(write_options, "key8730", "value112237");

  // op 22447: get
  status = db->Get(read_options, "key2362", &value);
  if (status.ok()) {
    out << "GET key2362 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22448: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 22449: put
  db->Put(write_options, "key11046", "value84153");

  // op 22450: put
  db->Put(write_options, "key6024", "value22612");

  // op 22451: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 22452: delete_range
  status = db->DeleteRange(write_options, "key5351", "key6596");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 22453: put
  db->Put(write_options, "key10083", "value31413");

  // op 22454: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 22455: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7806", "key9949", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7806, key9949) size=" << scan_result.size() << std::endl;
  }

  // op 22456: put
  db->Put(write_options, "key6566", "value49169");

  // op 22457: delete
  db->Delete(write_options, "key7314");

  // op 22458: put
  db->Put(write_options, "key4122", "value19548");

  // op 22459: put
  db->Put(write_options, "key9340", "value80591");

  // op 22460: delete_range
  status = db->DeleteRange(write_options, "key0349", "key7854");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 22461: delete
  db->Delete(write_options, "key1755");

  // op 22462: get
  status = db->Get(read_options, "key1016", &value);
  if (status.ok()) {
    out << "GET key1016 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22463: put
  db->Put(write_options, "key6188", "value51875");

  // op 22464: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5383", "key6232", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5383, key6232) size=" << scan_result.size() << std::endl;
  }

  // op 22465: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5352", "key11017", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5352, key11017) size=" << scan_result.size() << std::endl;
  }

  // op 22466: get
  status = db->Get(read_options, "key2235", &value);
  if (status.ok()) {
    out << "GET key2235 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22467: put
  db->Put(write_options, "key11709", "value27521");

  // op 22468: get
  status = db->Get(read_options, "key3277", &value);
  if (status.ok()) {
    out << "GET key3277 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22469: get
  status = db->Get(read_options, "key6521", &value);
  if (status.ok()) {
    out << "GET key6521 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22470: get
  status = db->Get(read_options, "key3478", &value);
  if (status.ok()) {
    out << "GET key3478 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22471: get
  status = db->Get(read_options, "key6939", &value);
  if (status.ok()) {
    out << "GET key6939 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22472: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1070", "key9094", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1070, key9094) size=" << scan_result.size() << std::endl;
  }

  // op 22473: get
  status = db->Get(read_options, "key3133", &value);
  if (status.ok()) {
    out << "GET key3133 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22474: put
  db->Put(write_options, "key2710", "value89427");

  // op 22475: delete
  db->Delete(write_options, "key5570");

  // op 22476: get
  status = db->Get(read_options, "key8945", &value);
  if (status.ok()) {
    out << "GET key8945 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22477: get
  status = db->Get(read_options, "key10605", &value);
  if (status.ok()) {
    out << "GET key10605 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22478: put
  db->Put(write_options, "key9627", "value116473");

  // op 22479: put
  db->Put(write_options, "key0800", "value65018");

  // op 22480: get
  status = db->Get(read_options, "key5425", &value);
  if (status.ok()) {
    out << "GET key5425 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22481: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3877", "key11287", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3877, key11287) size=" << scan_result.size() << std::endl;
  }

  // op 22482: put
  db->Put(write_options, "key5571", "value117961");

  // op 22483: get
  status = db->Get(read_options, "key7052", &value);
  if (status.ok()) {
    out << "GET key7052 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22484: delete_range
  status = db->DeleteRange(write_options, "key4680", "key10458");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 22485: put
  db->Put(write_options, "key3626", "value24806");

  // op 22486: put
  db->Put(write_options, "key5781", "value80987");

  // op 22487: get
  status = db->Get(read_options, "key9989", &value);
  if (status.ok()) {
    out << "GET key9989 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22488: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9645", "key9847", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9645, key9847) size=" << scan_result.size() << std::endl;
  }

  // op 22489: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0424", "key7727", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0424, key7727) size=" << scan_result.size() << std::endl;
  }

  // op 22490: delete
  db->Delete(write_options, "key7478");

  // op 22491: put
  db->Put(write_options, "key7446", "value33039");

  // op 22492: put
  db->Put(write_options, "key7720", "value98913");

  // op 22493: delete_range
  status = db->DeleteRange(write_options, "key0629", "key1734");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 22494: put
  db->Put(write_options, "key10450", "value2707");

  // op 22495: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1427", "key6131", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1427, key6131) size=" << scan_result.size() << std::endl;
  }

  // op 22496: put
  db->Put(write_options, "key1665", "value73285");

  // op 22497: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 22498: get
  status = db->Get(read_options, "key8361", &value);
  if (status.ok()) {
    out << "GET key8361 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22499: put
  db->Put(write_options, "key9402", "value50733");

  // op 22500: get
  status = db->Get(read_options, "key9079", &value);
  if (status.ok()) {
    out << "GET key9079 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22501: delete_range
  status = db->DeleteRange(write_options, "key3147", "key5021");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 22502: get
  status = db->Get(read_options, "key10944", &value);
  if (status.ok()) {
    out << "GET key10944 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22503: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6356", "key7415", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6356, key7415) size=" << scan_result.size() << std::endl;
  }

  // op 22504: get
  status = db->Get(read_options, "key0260", &value);
  if (status.ok()) {
    out << "GET key0260 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22505: delete
  db->Delete(write_options, "key2227");

  // op 22506: get
  status = db->Get(read_options, "key11190", &value);
  if (status.ok()) {
    out << "GET key11190 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22507: put
  db->Put(write_options, "key9772", "value9534");

  // op 22508: put
  db->Put(write_options, "key11624", "value46641");

  // op 22509: get
  status = db->Get(read_options, "key5967", &value);
  if (status.ok()) {
    out << "GET key5967 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22510: put
  db->Put(write_options, "key1300", "value52744");

  // op 22511: get
  status = db->Get(read_options, "key4820", &value);
  if (status.ok()) {
    out << "GET key4820 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22512: get
  status = db->Get(read_options, "key10380", &value);
  if (status.ok()) {
    out << "GET key10380 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22513: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1295", "key6816", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1295, key6816) size=" << scan_result.size() << std::endl;
  }

  // op 22514: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1407", "key5206", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1407, key5206) size=" << scan_result.size() << std::endl;
  }

  // op 22515: put
  db->Put(write_options, "key7815", "value40181");

  // op 22516: get
  status = db->Get(read_options, "key5946", &value);
  if (status.ok()) {
    out << "GET key5946 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22517: put
  db->Put(write_options, "key3688", "value109099");

  // op 22518: get
  status = db->Get(read_options, "key7207", &value);
  if (status.ok()) {
    out << "GET key7207 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22519: get
  status = db->Get(read_options, "key10226", &value);
  if (status.ok()) {
    out << "GET key10226 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22520: get
  status = db->Get(read_options, "key8523", &value);
  if (status.ok()) {
    out << "GET key8523 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22521: put
  db->Put(write_options, "key0496", "value97131");

  // op 22522: put
  db->Put(write_options, "key3085", "value106443");

  // op 22523: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2525", "key6940", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2525, key6940) size=" << scan_result.size() << std::endl;
  }

  // op 22524: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 22525: put
  db->Put(write_options, "key7322", "value110135");

  // op 22526: delete_range
  status = db->DeleteRange(write_options, "key6667", "key10207");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 22527: put
  db->Put(write_options, "key5321", "value25764");

  // op 22528: put
  db->Put(write_options, "key3885", "value57834");

  // op 22529: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1494", "key8271", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1494, key8271) size=" << scan_result.size() << std::endl;
  }

  // op 22530: delete_range
  status = db->DeleteRange(write_options, "key0818", "key7750");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 22531: delete
  db->Delete(write_options, "key7258");

  // op 22532: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 22533: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1917", "key10946", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1917, key10946) size=" << scan_result.size() << std::endl;
  }

  // op 22534: put
  db->Put(write_options, "key8149", "value106621");

  // op 22535: get
  status = db->Get(read_options, "key4166", &value);
  if (status.ok()) {
    out << "GET key4166 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22536: get
  status = db->Get(read_options, "key10454", &value);
  if (status.ok()) {
    out << "GET key10454 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22537: get
  status = db->Get(read_options, "key10736", &value);
  if (status.ok()) {
    out << "GET key10736 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22538: delete
  db->Delete(write_options, "key3415");

  // op 22539: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2091", "key7099", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2091, key7099) size=" << scan_result.size() << std::endl;
  }

  // op 22540: put
  db->Put(write_options, "key10646", "value61112");

  // op 22541: delete_range
  status = db->DeleteRange(write_options, "key0274", "key6045");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 22542: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8121", "key9838", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8121, key9838) size=" << scan_result.size() << std::endl;
  }

  // op 22543: delete_range
  status = db->DeleteRange(write_options, "key3572", "key10836");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 22544: delete_range
  status = db->DeleteRange(write_options, "key4822", "key5180");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 22545: get
  status = db->Get(read_options, "key4049", &value);
  if (status.ok()) {
    out << "GET key4049 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22546: put
  db->Put(write_options, "key2479", "value84227");

  // op 22547: get
  status = db->Get(read_options, "key10869", &value);
  if (status.ok()) {
    out << "GET key10869 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22548: put
  db->Put(write_options, "key4818", "value5806");

  // op 22549: put
  db->Put(write_options, "key10002", "value25565");

  // op 22550: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0129", "key2819", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0129, key2819) size=" << scan_result.size() << std::endl;
  }

  // op 22551: delete_range
  status = db->DeleteRange(write_options, "key4938", "key10227");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 22552: put
  db->Put(write_options, "key8182", "value65834");

  // op 22553: put
  db->Put(write_options, "key0477", "value12377");

  // op 22554: delete
  db->Delete(write_options, "key4240");

  // op 22555: delete
  db->Delete(write_options, "key8612");

  // op 22556: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 22557: put
  db->Put(write_options, "key3725", "value53430");

  // op 22558: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1638", "key2556", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1638, key2556) size=" << scan_result.size() << std::endl;
  }

  // op 22559: delete_range
  status = db->DeleteRange(write_options, "key0660", "key10851");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 22560: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1507", "key3959", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1507, key3959) size=" << scan_result.size() << std::endl;
  }

  // op 22561: delete
  db->Delete(write_options, "key8331");

  // op 22562: put
  db->Put(write_options, "key6929", "value76237");

  // op 22563: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3199", "key8551", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3199, key8551) size=" << scan_result.size() << std::endl;
  }

  // op 22564: put
  db->Put(write_options, "key11061", "value55304");

  // op 22565: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 22566: put
  db->Put(write_options, "key4506", "value76383");

  // op 22567: put
  db->Put(write_options, "key4882", "value62972");

  // op 22568: put
  db->Put(write_options, "key5069", "value105093");

  // op 22569: put
  db->Put(write_options, "key8457", "value22758");

  // op 22570: put
  db->Put(write_options, "key3116", "value108338");

  // op 22571: put
  db->Put(write_options, "key10611", "value68890");

  // op 22572: delete_range
  status = db->DeleteRange(write_options, "key5141", "key11074");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 22573: get
  status = db->Get(read_options, "key11636", &value);
  if (status.ok()) {
    out << "GET key11636 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22574: get
  status = db->Get(read_options, "key3141", &value);
  if (status.ok()) {
    out << "GET key3141 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22575: delete
  db->Delete(write_options, "key6381");

  // op 22576: put
  db->Put(write_options, "key5439", "value17453");

  // op 22577: delete
  db->Delete(write_options, "key3801");

  // op 22578: delete
  db->Delete(write_options, "key9827");

  // op 22579: get
  status = db->Get(read_options, "key8086", &value);
  if (status.ok()) {
    out << "GET key8086 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22580: put
  db->Put(write_options, "key10385", "value115789");

  // op 22581: put
  db->Put(write_options, "key9015", "value13633");

  // op 22582: put
  db->Put(write_options, "key7023", "value61189");

  // op 22583: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3022", "key4711", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3022, key4711) size=" << scan_result.size() << std::endl;
  }

  // op 22584: get
  status = db->Get(read_options, "key2324", &value);
  if (status.ok()) {
    out << "GET key2324 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22585: get
  status = db->Get(read_options, "key10565", &value);
  if (status.ok()) {
    out << "GET key10565 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22586: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3866", "key3970", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3866, key3970) size=" << scan_result.size() << std::endl;
  }

  // op 22587: put
  db->Put(write_options, "key1064", "value60717");

  // op 22588: put
  db->Put(write_options, "key1141", "value70499");

  // op 22589: put
  db->Put(write_options, "key9677", "value6576");

  // op 22590: put
  db->Put(write_options, "key0701", "value84482");

  // op 22591: get
  status = db->Get(read_options, "key10125", &value);
  if (status.ok()) {
    out << "GET key10125 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22592: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1664", "key3774", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1664, key3774) size=" << scan_result.size() << std::endl;
  }

  // op 22593: put
  db->Put(write_options, "key6199", "value28466");

  // op 22594: delete
  db->Delete(write_options, "key0188");

  // op 22595: get
  status = db->Get(read_options, "key9503", &value);
  if (status.ok()) {
    out << "GET key9503 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22596: get
  status = db->Get(read_options, "key0360", &value);
  if (status.ok()) {
    out << "GET key0360 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22597: delete_range
  status = db->DeleteRange(write_options, "key0626", "key2961");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 22598: get
  status = db->Get(read_options, "key4983", &value);
  if (status.ok()) {
    out << "GET key4983 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22599: put
  db->Put(write_options, "key10623", "value7096");

  // op 22600: delete
  db->Delete(write_options, "key2262");

  // op 22601: delete
  db->Delete(write_options, "key0422");

  // op 22602: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 22603: put
  db->Put(write_options, "key8156", "value8702");

  // op 22604: put
  db->Put(write_options, "key5999", "value10865");

  // op 22605: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1654", "key6736", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1654, key6736) size=" << scan_result.size() << std::endl;
  }

  // op 22606: put
  db->Put(write_options, "key6871", "value36058");

  // op 22607: put
  db->Put(write_options, "key10435", "value107252");

  // op 22608: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6991", "key10339", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6991, key10339) size=" << scan_result.size() << std::endl;
  }

  // op 22609: delete_range
  status = db->DeleteRange(write_options, "key0404", "key10177");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 22610: put
  db->Put(write_options, "key9284", "value82385");

  // op 22611: put
  db->Put(write_options, "key8169", "value82868");

  // op 22612: put
  db->Put(write_options, "key6796", "value115539");

  // op 22613: put
  db->Put(write_options, "key1357", "value90393");

  // op 22614: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0910", "key5203", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0910, key5203) size=" << scan_result.size() << std::endl;
  }

  // op 22615: put
  db->Put(write_options, "key4746", "value105718");

  // op 22616: put
  db->Put(write_options, "key0505", "value68954");

  // op 22617: delete
  db->Delete(write_options, "key10254");

  // op 22618: put
  db->Put(write_options, "key10969", "value76302");

  // op 22619: delete_range
  status = db->DeleteRange(write_options, "key0919", "key3239");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 22620: put
  db->Put(write_options, "key2573", "value102459");

  // op 22621: delete
  db->Delete(write_options, "key3824");

  // op 22622: put
  db->Put(write_options, "key8258", "value16108");

  // op 22623: put
  db->Put(write_options, "key4188", "value12133");

  // op 22624: put
  db->Put(write_options, "key0784", "value7942");

  // op 22625: put
  db->Put(write_options, "key1516", "value61287");

  // op 22626: delete
  db->Delete(write_options, "key7674");

  // op 22627: put
  db->Put(write_options, "key6079", "value83749");

  // op 22628: put
  db->Put(write_options, "key11134", "value26158");

  // op 22629: put
  db->Put(write_options, "key3986", "value91526");

  // op 22630: put
  db->Put(write_options, "key1857", "value103831");

  // op 22631: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 22632: put
  db->Put(write_options, "key7676", "value33576");

  // op 22633: put
  db->Put(write_options, "key9223", "value43505");

  // op 22634: put
  db->Put(write_options, "key11196", "value55932");

  // op 22635: delete_range
  status = db->DeleteRange(write_options, "key2510", "key6237");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 22636: delete_range
  status = db->DeleteRange(write_options, "key6236", "key7413");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 22637: put
  db->Put(write_options, "key2947", "value85433");

  // op 22638: put
  db->Put(write_options, "key4076", "value103474");

  // op 22639: get
  status = db->Get(read_options, "key0020", &value);
  if (status.ok()) {
    out << "GET key0020 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22640: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2437", "key7835", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2437, key7835) size=" << scan_result.size() << std::endl;
  }

  // op 22641: put
  db->Put(write_options, "key5303", "value76049");

  // op 22642: put
  db->Put(write_options, "key3376", "value109509");

  // op 22643: put
  db->Put(write_options, "key2750", "value4961");

  // op 22644: put
  db->Put(write_options, "key5576", "value61846");

  // op 22645: delete
  db->Delete(write_options, "key3586");

  // op 22646: delete
  db->Delete(write_options, "key6866");

  // op 22647: get
  status = db->Get(read_options, "key11574", &value);
  if (status.ok()) {
    out << "GET key11574 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22648: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1275", "key8235", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1275, key8235) size=" << scan_result.size() << std::endl;
  }

  // op 22649: delete
  db->Delete(write_options, "key6507");

  // op 22650: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3159", "key5497", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3159, key5497) size=" << scan_result.size() << std::endl;
  }

  // op 22651: delete_range
  status = db->DeleteRange(write_options, "key6121", "key6924");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 22652: put
  db->Put(write_options, "key1922", "value13438");

  // op 22653: get
  status = db->Get(read_options, "key10606", &value);
  if (status.ok()) {
    out << "GET key10606 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22654: put
  db->Put(write_options, "key6734", "value16870");

  // op 22655: put
  db->Put(write_options, "key9378", "value99700");

  // op 22656: get
  status = db->Get(read_options, "key7002", &value);
  if (status.ok()) {
    out << "GET key7002 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22657: delete_range
  status = db->DeleteRange(write_options, "key8250", "key10566");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 22658: put
  db->Put(write_options, "key8639", "value66924");

  // op 22659: delete_range
  status = db->DeleteRange(write_options, "key6113", "key9598");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 22660: get
  status = db->Get(read_options, "key4330", &value);
  if (status.ok()) {
    out << "GET key4330 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22661: delete
  db->Delete(write_options, "key10195");

  // op 22662: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1581", "key6765", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1581, key6765) size=" << scan_result.size() << std::endl;
  }

  // op 22663: delete_range
  status = db->DeleteRange(write_options, "key2175", "key9165");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 22664: put
  db->Put(write_options, "key2302", "value29731");

  // op 22665: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 22666: delete_range
  status = db->DeleteRange(write_options, "key4800", "key11153");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 22667: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0669", "key10214", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0669, key10214) size=" << scan_result.size() << std::endl;
  }

  // op 22668: get
  status = db->Get(read_options, "key10987", &value);
  if (status.ok()) {
    out << "GET key10987 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22669: put
  db->Put(write_options, "key5717", "value30572");

  // op 22670: get
  status = db->Get(read_options, "key5015", &value);
  if (status.ok()) {
    out << "GET key5015 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22671: get
  status = db->Get(read_options, "key8912", &value);
  if (status.ok()) {
    out << "GET key8912 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22672: get
  status = db->Get(read_options, "key1168", &value);
  if (status.ok()) {
    out << "GET key1168 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22673: get
  status = db->Get(read_options, "key11190", &value);
  if (status.ok()) {
    out << "GET key11190 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22674: get
  status = db->Get(read_options, "key0135", &value);
  if (status.ok()) {
    out << "GET key0135 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22675: delete
  db->Delete(write_options, "key3059");

  // op 22676: delete
  db->Delete(write_options, "key11254");

  // op 22677: put
  db->Put(write_options, "key6821", "value12090");

  // op 22678: put
  db->Put(write_options, "key7021", "value45538");

  // op 22679: put
  db->Put(write_options, "key2874", "value55594");

  // op 22680: get
  status = db->Get(read_options, "key6453", &value);
  if (status.ok()) {
    out << "GET key6453 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22681: get
  status = db->Get(read_options, "key1974", &value);
  if (status.ok()) {
    out << "GET key1974 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22682: put
  db->Put(write_options, "key6114", "value22865");

  // op 22683: put
  db->Put(write_options, "key10504", "value40933");

  // op 22684: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 22685: delete_range
  status = db->DeleteRange(write_options, "key4650", "key5062");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 22686: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9336", "key11266", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9336, key11266) size=" << scan_result.size() << std::endl;
  }

  // op 22687: put
  db->Put(write_options, "key8018", "value72441");

  // op 22688: put
  db->Put(write_options, "key7280", "value1061");

  // op 22689: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 22690: get
  status = db->Get(read_options, "key10786", &value);
  if (status.ok()) {
    out << "GET key10786 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22691: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6585", "key10803", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6585, key10803) size=" << scan_result.size() << std::endl;
  }

  // op 22692: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 22693: put
  db->Put(write_options, "key7377", "value50709");

  // op 22694: get
  status = db->Get(read_options, "key6067", &value);
  if (status.ok()) {
    out << "GET key6067 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22695: delete_range
  status = db->DeleteRange(write_options, "key5121", "key5154");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 22696: put
  db->Put(write_options, "key3362", "value5032");

  // op 22697: put
  db->Put(write_options, "key1313", "value57603");

  // op 22698: put
  db->Put(write_options, "key0152", "value2060");

  // op 22699: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3503", "key5008", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3503, key5008) size=" << scan_result.size() << std::endl;
  }

  // op 22700: get
  status = db->Get(read_options, "key6986", &value);
  if (status.ok()) {
    out << "GET key6986 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22701: delete_range
  status = db->DeleteRange(write_options, "key0801", "key5654");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 22702: get
  status = db->Get(read_options, "key3919", &value);
  if (status.ok()) {
    out << "GET key3919 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22703: put
  db->Put(write_options, "key5993", "value31866");

  // op 22704: put
  db->Put(write_options, "key1245", "value3314");

  // op 22705: put
  db->Put(write_options, "key3251", "value79711");

  // op 22706: get
  status = db->Get(read_options, "key7584", &value);
  if (status.ok()) {
    out << "GET key7584 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22707: put
  db->Put(write_options, "key2396", "value41686");

  // op 22708: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1705", "key5834", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1705, key5834) size=" << scan_result.size() << std::endl;
  }

  // op 22709: put
  db->Put(write_options, "key8894", "value6346");

  // op 22710: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9613", "key10725", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9613, key10725) size=" << scan_result.size() << std::endl;
  }

  // op 22711: put
  db->Put(write_options, "key5586", "value19472");

  // op 22712: put
  db->Put(write_options, "key7927", "value100534");

  // op 22713: get
  status = db->Get(read_options, "key7686", &value);
  if (status.ok()) {
    out << "GET key7686 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22714: get
  status = db->Get(read_options, "key9895", &value);
  if (status.ok()) {
    out << "GET key9895 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22715: get
  status = db->Get(read_options, "key8708", &value);
  if (status.ok()) {
    out << "GET key8708 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22716: put
  db->Put(write_options, "key9027", "value117588");

  // op 22717: put
  db->Put(write_options, "key1732", "value111240");

  // op 22718: put
  db->Put(write_options, "key4630", "value47475");

  // op 22719: get
  status = db->Get(read_options, "key1576", &value);
  if (status.ok()) {
    out << "GET key1576 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22720: put
  db->Put(write_options, "key5365", "value37775");

  // op 22721: get
  status = db->Get(read_options, "key11681", &value);
  if (status.ok()) {
    out << "GET key11681 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22722: delete
  db->Delete(write_options, "key5189");

  // op 22723: put
  db->Put(write_options, "key8207", "value84958");

  // op 22724: delete
  db->Delete(write_options, "key3563");

  // op 22725: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7137", "key7274", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7137, key7274) size=" << scan_result.size() << std::endl;
  }

  // op 22726: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 22727: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1174", "key3337", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1174, key3337) size=" << scan_result.size() << std::endl;
  }

  // op 22728: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4920", "key7883", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4920, key7883) size=" << scan_result.size() << std::endl;
  }

  // op 22729: put
  db->Put(write_options, "key7295", "value115147");

  // op 22730: put
  db->Put(write_options, "key1848", "value99458");

  // op 22731: get
  status = db->Get(read_options, "key5035", &value);
  if (status.ok()) {
    out << "GET key5035 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22732: get
  status = db->Get(read_options, "key0627", &value);
  if (status.ok()) {
    out << "GET key0627 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22733: put
  db->Put(write_options, "key10605", "value29233");

  // op 22734: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7494", "key8968", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7494, key8968) size=" << scan_result.size() << std::endl;
  }

  // op 22735: put
  db->Put(write_options, "key9746", "value42816");

  // op 22736: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7709", "key10743", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7709, key10743) size=" << scan_result.size() << std::endl;
  }

  // op 22737: get
  status = db->Get(read_options, "key5002", &value);
  if (status.ok()) {
    out << "GET key5002 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22738: put
  db->Put(write_options, "key5455", "value111812");

  // op 22739: put
  db->Put(write_options, "key6684", "value79634");

  // op 22740: delete
  db->Delete(write_options, "key3665");

  // op 22741: put
  db->Put(write_options, "key7175", "value34612");

  // op 22742: put
  db->Put(write_options, "key9249", "value86332");

  // op 22743: put
  db->Put(write_options, "key7591", "value66313");

  // op 22744: put
  db->Put(write_options, "key1899", "value68185");

  // op 22745: get
  status = db->Get(read_options, "key2508", &value);
  if (status.ok()) {
    out << "GET key2508 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22746: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0377", "key2891", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0377, key2891) size=" << scan_result.size() << std::endl;
  }

  // op 22747: get
  status = db->Get(read_options, "key6644", &value);
  if (status.ok()) {
    out << "GET key6644 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22748: delete
  db->Delete(write_options, "key0648");

  // op 22749: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 22750: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4355", "key8662", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4355, key8662) size=" << scan_result.size() << std::endl;
  }

  // op 22751: put
  db->Put(write_options, "key8091", "value99930");

  // op 22752: delete_range
  status = db->DeleteRange(write_options, "key5370", "key5438");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 22753: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2846", "key8336", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2846, key8336) size=" << scan_result.size() << std::endl;
  }

  // op 22754: get
  status = db->Get(read_options, "key7358", &value);
  if (status.ok()) {
    out << "GET key7358 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22755: delete_range
  status = db->DeleteRange(write_options, "key5652", "key8077");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 22756: delete
  db->Delete(write_options, "key4400");

  // op 22757: get
  status = db->Get(read_options, "key0466", &value);
  if (status.ok()) {
    out << "GET key0466 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22758: delete_range
  status = db->DeleteRange(write_options, "key1097", "key3246");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 22759: delete_range
  status = db->DeleteRange(write_options, "key5065", "key10475");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 22760: put
  db->Put(write_options, "key0470", "value21015");

  // op 22761: delete
  db->Delete(write_options, "key6989");

  // op 22762: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4006", "key9754", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4006, key9754) size=" << scan_result.size() << std::endl;
  }

  // op 22763: delete_range
  status = db->DeleteRange(write_options, "key4019", "key8051");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 22764: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6963", "key8382", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6963, key8382) size=" << scan_result.size() << std::endl;
  }

  // op 22765: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2277", "key3803", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2277, key3803) size=" << scan_result.size() << std::endl;
  }

  // op 22766: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 22767: put
  db->Put(write_options, "key8678", "value25874");

  // op 22768: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2574", "key3550", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2574, key3550) size=" << scan_result.size() << std::endl;
  }

  // op 22769: put
  db->Put(write_options, "key9972", "value11172");

  // op 22770: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 22771: put
  db->Put(write_options, "key7844", "value112363");

  // op 22772: delete_range
  status = db->DeleteRange(write_options, "key2016", "key5976");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 22773: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 22774: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4983", "key9047", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4983, key9047) size=" << scan_result.size() << std::endl;
  }

  // op 22775: put
  db->Put(write_options, "key7966", "value21771");

  // op 22776: put
  db->Put(write_options, "key1779", "value51758");

  // op 22777: put
  db->Put(write_options, "key3873", "value36641");

  // op 22778: get
  status = db->Get(read_options, "key1635", &value);
  if (status.ok()) {
    out << "GET key1635 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22779: delete
  db->Delete(write_options, "key8257");

  // op 22780: put
  db->Put(write_options, "key1735", "value116488");

  // op 22781: put
  db->Put(write_options, "key9878", "value114794");

  // op 22782: get
  status = db->Get(read_options, "key5845", &value);
  if (status.ok()) {
    out << "GET key5845 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22783: get
  status = db->Get(read_options, "key3734", &value);
  if (status.ok()) {
    out << "GET key3734 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22784: get
  status = db->Get(read_options, "key4137", &value);
  if (status.ok()) {
    out << "GET key4137 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22785: put
  db->Put(write_options, "key0001", "value35897");

  // op 22786: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 22787: put
  db->Put(write_options, "key6453", "value85566");

  // op 22788: delete
  db->Delete(write_options, "key3456");

  // op 22789: delete
  db->Delete(write_options, "key2943");

  // op 22790: get
  status = db->Get(read_options, "key4050", &value);
  if (status.ok()) {
    out << "GET key4050 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22791: put
  db->Put(write_options, "key5562", "value59626");

  // op 22792: put
  db->Put(write_options, "key0832", "value112241");

  // op 22793: get
  status = db->Get(read_options, "key1641", &value);
  if (status.ok()) {
    out << "GET key1641 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22794: put
  db->Put(write_options, "key8985", "value68880");

  // op 22795: put
  db->Put(write_options, "key2949", "value17065");

  // op 22796: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2765", "key4788", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2765, key4788) size=" << scan_result.size() << std::endl;
  }

  // op 22797: put
  db->Put(write_options, "key7897", "value15525");

  // op 22798: put
  db->Put(write_options, "key10776", "value68567");

  // op 22799: put
  db->Put(write_options, "key3936", "value68866");

  // op 22800: put
  db->Put(write_options, "key6179", "value10600");

  // op 22801: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0024", "key4644", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0024, key4644) size=" << scan_result.size() << std::endl;
  }

  // op 22802: put
  db->Put(write_options, "key8699", "value58370");

  // op 22803: get
  status = db->Get(read_options, "key0498", &value);
  if (status.ok()) {
    out << "GET key0498 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22804: get
  status = db->Get(read_options, "key3755", &value);
  if (status.ok()) {
    out << "GET key3755 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22805: delete_range
  status = db->DeleteRange(write_options, "key5886", "key9159");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 22806: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 22807: put
  db->Put(write_options, "key9396", "value46534");

  // op 22808: get
  status = db->Get(read_options, "key7671", &value);
  if (status.ok()) {
    out << "GET key7671 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22809: get
  status = db->Get(read_options, "key4349", &value);
  if (status.ok()) {
    out << "GET key4349 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22810: get
  status = db->Get(read_options, "key9529", &value);
  if (status.ok()) {
    out << "GET key9529 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22811: get
  status = db->Get(read_options, "key9076", &value);
  if (status.ok()) {
    out << "GET key9076 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22812: put
  db->Put(write_options, "key1861", "value58798");

  // op 22813: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2704", "key5123", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2704, key5123) size=" << scan_result.size() << std::endl;
  }

  // op 22814: delete
  db->Delete(write_options, "key8331");

  // op 22815: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1394", "key8456", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1394, key8456) size=" << scan_result.size() << std::endl;
  }

  // op 22816: put
  db->Put(write_options, "key10740", "value100332");

  // op 22817: delete_range
  status = db->DeleteRange(write_options, "key0458", "key7904");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 22818: put
  db->Put(write_options, "key6297", "value15154");

  // op 22819: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 22820: put
  db->Put(write_options, "key3350", "value106708");

  // op 22821: delete
  db->Delete(write_options, "key4333");

  // op 22822: put
  db->Put(write_options, "key3490", "value25096");

  // op 22823: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9985", "key11632", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9985, key11632) size=" << scan_result.size() << std::endl;
  }

  // op 22824: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2100", "key4464", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2100, key4464) size=" << scan_result.size() << std::endl;
  }

  // op 22825: put
  db->Put(write_options, "key7641", "value100870");

  // op 22826: get
  status = db->Get(read_options, "key10728", &value);
  if (status.ok()) {
    out << "GET key10728 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22827: put
  db->Put(write_options, "key4080", "value114000");

  // op 22828: put
  db->Put(write_options, "key10253", "value45362");

  // op 22829: put
  db->Put(write_options, "key11646", "value39058");

  // op 22830: get
  status = db->Get(read_options, "key0933", &value);
  if (status.ok()) {
    out << "GET key0933 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22831: put
  db->Put(write_options, "key11301", "value109462");

  // op 22832: get
  status = db->Get(read_options, "key2814", &value);
  if (status.ok()) {
    out << "GET key2814 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22833: put
  db->Put(write_options, "key7490", "value18465");

  // op 22834: get
  status = db->Get(read_options, "key5311", &value);
  if (status.ok()) {
    out << "GET key5311 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22835: put
  db->Put(write_options, "key7594", "value12911");

  // op 22836: put
  db->Put(write_options, "key1048", "value114315");

  // op 22837: put
  db->Put(write_options, "key6384", "value111017");

  // op 22838: put
  db->Put(write_options, "key9180", "value42440");

  // op 22839: get
  status = db->Get(read_options, "key4020", &value);
  if (status.ok()) {
    out << "GET key4020 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22840: get
  status = db->Get(read_options, "key3522", &value);
  if (status.ok()) {
    out << "GET key3522 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22841: get
  status = db->Get(read_options, "key0487", &value);
  if (status.ok()) {
    out << "GET key0487 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22842: put
  db->Put(write_options, "key6060", "value20548");

  // op 22843: get
  status = db->Get(read_options, "key3663", &value);
  if (status.ok()) {
    out << "GET key3663 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22844: put
  db->Put(write_options, "key1206", "value23998");

  // op 22845: put
  db->Put(write_options, "key3513", "value73639");

  // op 22846: delete_range
  status = db->DeleteRange(write_options, "key0254", "key10724");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 22847: delete
  db->Delete(write_options, "key0762");

  // op 22848: put
  db->Put(write_options, "key8634", "value69042");

  // op 22849: put
  db->Put(write_options, "key2835", "value24891");

  // op 22850: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5293", "key6176", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5293, key6176) size=" << scan_result.size() << std::endl;
  }

  // op 22851: get
  status = db->Get(read_options, "key3045", &value);
  if (status.ok()) {
    out << "GET key3045 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22852: delete_range
  status = db->DeleteRange(write_options, "key1114", "key8584");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 22853: delete_range
  status = db->DeleteRange(write_options, "key1763", "key8244");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 22854: put
  db->Put(write_options, "key7478", "value114276");

  // op 22855: put
  db->Put(write_options, "key9227", "value17135");

  // op 22856: put
  db->Put(write_options, "key4442", "value115204");

  // op 22857: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2166", "key9617", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2166, key9617) size=" << scan_result.size() << std::endl;
  }

  // op 22858: delete
  db->Delete(write_options, "key4249");

  // op 22859: put
  db->Put(write_options, "key3358", "value117011");

  // op 22860: put
  db->Put(write_options, "key9815", "value90002");

  // op 22861: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 22862: delete_range
  status = db->DeleteRange(write_options, "key2170", "key10849");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 22863: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1520", "key3500", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1520, key3500) size=" << scan_result.size() << std::endl;
  }

  // op 22864: delete_range
  status = db->DeleteRange(write_options, "key0424", "key5993");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 22865: put
  db->Put(write_options, "key8956", "value49232");

  // op 22866: put
  db->Put(write_options, "key0029", "value67775");

  // op 22867: put
  db->Put(write_options, "key5029", "value6492");

  // op 22868: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1604", "key5462", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1604, key5462) size=" << scan_result.size() << std::endl;
  }

  // op 22869: get
  status = db->Get(read_options, "key5316", &value);
  if (status.ok()) {
    out << "GET key5316 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22870: delete_range
  status = db->DeleteRange(write_options, "key7662", "key10005");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 22871: get
  status = db->Get(read_options, "key10857", &value);
  if (status.ok()) {
    out << "GET key10857 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22872: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 22873: put
  db->Put(write_options, "key9015", "value89362");

  // op 22874: put
  db->Put(write_options, "key10271", "value41063");

  // op 22875: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4853", "key8084", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4853, key8084) size=" << scan_result.size() << std::endl;
  }

  // op 22876: get
  status = db->Get(read_options, "key8815", &value);
  if (status.ok()) {
    out << "GET key8815 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22877: get
  status = db->Get(read_options, "key6719", &value);
  if (status.ok()) {
    out << "GET key6719 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22878: delete
  db->Delete(write_options, "key8350");

  // op 22879: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6409", "key11359", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6409, key11359) size=" << scan_result.size() << std::endl;
  }

  // op 22880: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8158", "key10134", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8158, key10134) size=" << scan_result.size() << std::endl;
  }

  // op 22881: put
  db->Put(write_options, "key0631", "value25118");

  // op 22882: delete
  db->Delete(write_options, "key10659");

  // op 22883: put
  db->Put(write_options, "key9394", "value111049");

  // op 22884: put
  db->Put(write_options, "key8714", "value19280");

  // op 22885: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4936", "key5776", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4936, key5776) size=" << scan_result.size() << std::endl;
  }

  // op 22886: delete_range
  status = db->DeleteRange(write_options, "key2915", "key5721");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 22887: put
  db->Put(write_options, "key3590", "value17302");

  // op 22888: delete
  db->Delete(write_options, "key10222");

  // op 22889: delete
  db->Delete(write_options, "key8831");

  // op 22890: delete_range
  status = db->DeleteRange(write_options, "key3525", "key10402");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 22891: put
  db->Put(write_options, "key4474", "value67158");

  // op 22892: delete_range
  status = db->DeleteRange(write_options, "key0653", "key4476");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 22893: delete
  db->Delete(write_options, "key9349");

  // op 22894: put
  db->Put(write_options, "key0660", "value84062");

  // op 22895: put
  db->Put(write_options, "key3470", "value28476");

  // op 22896: delete_range
  status = db->DeleteRange(write_options, "key9137", "key11101");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 22897: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3257", "key9336", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3257, key9336) size=" << scan_result.size() << std::endl;
  }

  // op 22898: get
  status = db->Get(read_options, "key5533", &value);
  if (status.ok()) {
    out << "GET key5533 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22899: get
  status = db->Get(read_options, "key10200", &value);
  if (status.ok()) {
    out << "GET key10200 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22900: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8426", "key9712", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8426, key9712) size=" << scan_result.size() << std::endl;
  }

  // op 22901: get
  status = db->Get(read_options, "key5287", &value);
  if (status.ok()) {
    out << "GET key5287 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22902: put
  db->Put(write_options, "key9081", "value53410");

  // op 22903: get
  status = db->Get(read_options, "key9499", &value);
  if (status.ok()) {
    out << "GET key9499 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22904: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 22905: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2660", "key4431", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2660, key4431) size=" << scan_result.size() << std::endl;
  }

  // op 22906: put
  db->Put(write_options, "key7783", "value61348");

  // op 22907: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3871", "key4246", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3871, key4246) size=" << scan_result.size() << std::endl;
  }

  // op 22908: get
  status = db->Get(read_options, "key1620", &value);
  if (status.ok()) {
    out << "GET key1620 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22909: put
  db->Put(write_options, "key11617", "value22445");

  // op 22910: get
  status = db->Get(read_options, "key2529", &value);
  if (status.ok()) {
    out << "GET key2529 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22911: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 22912: delete_range
  status = db->DeleteRange(write_options, "key0456", "key5171");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 22913: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 22914: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1406", "key1692", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1406, key1692) size=" << scan_result.size() << std::endl;
  }

  // op 22915: get
  status = db->Get(read_options, "key3236", &value);
  if (status.ok()) {
    out << "GET key3236 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22916: put
  db->Put(write_options, "key11725", "value34312");

  // op 22917: delete_range
  status = db->DeleteRange(write_options, "key4032", "key4093");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 22918: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 22919: get
  status = db->Get(read_options, "key6175", &value);
  if (status.ok()) {
    out << "GET key6175 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22920: delete
  db->Delete(write_options, "key6467");

  // op 22921: get
  status = db->Get(read_options, "key0736", &value);
  if (status.ok()) {
    out << "GET key0736 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22922: put
  db->Put(write_options, "key3547", "value97725");

  // op 22923: put
  db->Put(write_options, "key9371", "value71594");

  // op 22924: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2797", "key3592", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2797, key3592) size=" << scan_result.size() << std::endl;
  }

  // op 22925: get
  status = db->Get(read_options, "key0047", &value);
  if (status.ok()) {
    out << "GET key0047 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22926: get
  status = db->Get(read_options, "key7388", &value);
  if (status.ok()) {
    out << "GET key7388 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22927: put
  db->Put(write_options, "key1969", "value31602");

  // op 22928: put
  db->Put(write_options, "key4465", "value18833");

  // op 22929: get
  status = db->Get(read_options, "key3535", &value);
  if (status.ok()) {
    out << "GET key3535 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22930: put
  db->Put(write_options, "key4458", "value111866");

  // op 22931: delete_range
  status = db->DeleteRange(write_options, "key7655", "key9401");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 22932: delete
  db->Delete(write_options, "key4665");

  // op 22933: put
  db->Put(write_options, "key8927", "value91074");

  // op 22934: put
  db->Put(write_options, "key9301", "value83391");

  // op 22935: delete_range
  status = db->DeleteRange(write_options, "key1936", "key2069");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 22936: delete_range
  status = db->DeleteRange(write_options, "key5888", "key8916");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 22937: get
  status = db->Get(read_options, "key2445", &value);
  if (status.ok()) {
    out << "GET key2445 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22938: put
  db->Put(write_options, "key7029", "value50102");

  // op 22939: put
  db->Put(write_options, "key4477", "value5554");

  // op 22940: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 22941: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6395", "key9979", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6395, key9979) size=" << scan_result.size() << std::endl;
  }

  // op 22942: put
  db->Put(write_options, "key10866", "value33068");

  // op 22943: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0481", "key8141", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0481, key8141) size=" << scan_result.size() << std::endl;
  }

  // op 22944: put
  db->Put(write_options, "key4122", "value53722");

  // op 22945: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3691", "key9846", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3691, key9846) size=" << scan_result.size() << std::endl;
  }

  // op 22946: delete
  db->Delete(write_options, "key1667");

  // op 22947: put
  db->Put(write_options, "key9740", "value32942");

  // op 22948: put
  db->Put(write_options, "key7370", "value99647");

  // op 22949: get
  status = db->Get(read_options, "key2352", &value);
  if (status.ok()) {
    out << "GET key2352 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22950: put
  db->Put(write_options, "key11727", "value40628");

  // op 22951: put
  db->Put(write_options, "key0402", "value20587");

  // op 22952: put
  db->Put(write_options, "key4721", "value17582");

  // op 22953: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 22954: put
  db->Put(write_options, "key11446", "value23766");

  // op 22955: delete
  db->Delete(write_options, "key6538");

  // op 22956: put
  db->Put(write_options, "key0168", "value7828");

  // op 22957: put
  db->Put(write_options, "key9100", "value50415");

  // op 22958: put
  db->Put(write_options, "key4302", "value44155");

  // op 22959: get
  status = db->Get(read_options, "key9363", &value);
  if (status.ok()) {
    out << "GET key9363 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22960: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6241", "key8643", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6241, key8643) size=" << scan_result.size() << std::endl;
  }

  // op 22961: put
  db->Put(write_options, "key3003", "value12373");

  // op 22962: delete_range
  status = db->DeleteRange(write_options, "key2642", "key3953");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 22963: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 22964: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5127", "key9654", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5127, key9654) size=" << scan_result.size() << std::endl;
  }

  // op 22965: put
  db->Put(write_options, "key9368", "value81855");

  // op 22966: put
  db->Put(write_options, "key8850", "value75760");

  // op 22967: put
  db->Put(write_options, "key4816", "value111027");

  // op 22968: put
  db->Put(write_options, "key10695", "value83355");

  // op 22969: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4554", "key9206", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4554, key9206) size=" << scan_result.size() << std::endl;
  }

  // op 22970: delete_range
  status = db->DeleteRange(write_options, "key7876", "key8418");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 22971: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7416", "key11113", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7416, key11113) size=" << scan_result.size() << std::endl;
  }

  // op 22972: get
  status = db->Get(read_options, "key8563", &value);
  if (status.ok()) {
    out << "GET key8563 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22973: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1739", "key6097", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1739, key6097) size=" << scan_result.size() << std::endl;
  }

  // op 22974: put
  db->Put(write_options, "key11157", "value46905");

  // op 22975: delete_range
  status = db->DeleteRange(write_options, "key7587", "key8230");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 22976: get
  status = db->Get(read_options, "key9934", &value);
  if (status.ok()) {
    out << "GET key9934 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22977: delete_range
  status = db->DeleteRange(write_options, "key4651", "key6449");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 22978: delete
  db->Delete(write_options, "key8052");

  // op 22979: delete_range
  status = db->DeleteRange(write_options, "key1360", "key9016");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 22980: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2605", "key6519", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2605, key6519) size=" << scan_result.size() << std::endl;
  }

  // op 22981: put
  db->Put(write_options, "key7212", "value72404");

  // op 22982: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8757", "key10856", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8757, key10856) size=" << scan_result.size() << std::endl;
  }

  // op 22983: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 22984: delete_range
  status = db->DeleteRange(write_options, "key0026", "key11630");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 22985: put
  db->Put(write_options, "key2883", "value48382");

  // op 22986: delete
  db->Delete(write_options, "key8700");

  // op 22987: delete_range
  status = db->DeleteRange(write_options, "key10406", "key11209");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 22988: get
  status = db->Get(read_options, "key0716", &value);
  if (status.ok()) {
    out << "GET key0716 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22989: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1950", "key6602", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1950, key6602) size=" << scan_result.size() << std::endl;
  }

  // op 22990: put
  db->Put(write_options, "key9789", "value18055");

  // op 22991: delete
  db->Delete(write_options, "key5568");

  // op 22992: delete_range
  status = db->DeleteRange(write_options, "key4817", "key5158");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 22993: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3076", "key9146", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3076, key9146) size=" << scan_result.size() << std::endl;
  }

  // op 22994: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1594", "key7404", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1594, key7404) size=" << scan_result.size() << std::endl;
  }

  // op 22995: put
  db->Put(write_options, "key4312", "value67833");

  // op 22996: get
  status = db->Get(read_options, "key8713", &value);
  if (status.ok()) {
    out << "GET key8713 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22997: get
  status = db->Get(read_options, "key10694", &value);
  if (status.ok()) {
    out << "GET key10694 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 22998: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1638", "key6668", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1638, key6668) size=" << scan_result.size() << std::endl;
  }

  // op 22999: put
  db->Put(write_options, "key0789", "value80474");

  // op 23000: get
  status = db->Get(read_options, "key11464", &value);
  if (status.ok()) {
    out << "GET key11464 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23001: delete
  db->Delete(write_options, "key0764");

  // op 23002: put
  db->Put(write_options, "key10902", "value93429");

  // op 23003: put
  db->Put(write_options, "key2168", "value53140");

  // op 23004: delete_range
  status = db->DeleteRange(write_options, "key6559", "key9489");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 23005: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0341", "key1822", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0341, key1822) size=" << scan_result.size() << std::endl;
  }

  // op 23006: get
  status = db->Get(read_options, "key1475", &value);
  if (status.ok()) {
    out << "GET key1475 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23007: delete_range
  status = db->DeleteRange(write_options, "key4803", "key4924");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 23008: put
  db->Put(write_options, "key5215", "value84961");

  // op 23009: put
  db->Put(write_options, "key2317", "value28559");

  // op 23010: put
  db->Put(write_options, "key0997", "value85700");

  // op 23011: put
  db->Put(write_options, "key10618", "value99112");

  // op 23012: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3496", "key7983", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3496, key7983) size=" << scan_result.size() << std::endl;
  }

  // op 23013: get
  status = db->Get(read_options, "key6208", &value);
  if (status.ok()) {
    out << "GET key6208 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23014: put
  db->Put(write_options, "key6165", "value23077");

  // op 23015: put
  db->Put(write_options, "key10067", "value87740");

  // op 23016: put
  db->Put(write_options, "key7913", "value109886");

  // op 23017: get
  status = db->Get(read_options, "key10155", &value);
  if (status.ok()) {
    out << "GET key10155 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23018: get
  status = db->Get(read_options, "key7100", &value);
  if (status.ok()) {
    out << "GET key7100 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23019: delete
  db->Delete(write_options, "key0090");

  // op 23020: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 23021: delete
  db->Delete(write_options, "key0300");

  // op 23022: put
  db->Put(write_options, "key2861", "value58426");

  // op 23023: put
  db->Put(write_options, "key7163", "value89097");

  // op 23024: get
  status = db->Get(read_options, "key8772", &value);
  if (status.ok()) {
    out << "GET key8772 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23025: put
  db->Put(write_options, "key5294", "value113932");

  // op 23026: put
  db->Put(write_options, "key10491", "value98014");

  // op 23027: put
  db->Put(write_options, "key11004", "value41469");

  // op 23028: put
  db->Put(write_options, "key5066", "value17214");

  // op 23029: put
  db->Put(write_options, "key10697", "value50985");

  // op 23030: put
  db->Put(write_options, "key10612", "value103725");

  // op 23031: delete_range
  status = db->DeleteRange(write_options, "key2973", "key3762");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 23032: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 23033: put
  db->Put(write_options, "key10490", "value111715");

  // op 23034: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5446", "key9378", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5446, key9378) size=" << scan_result.size() << std::endl;
  }

  // op 23035: put
  db->Put(write_options, "key10605", "value40681");

  // op 23036: get
  status = db->Get(read_options, "key0593", &value);
  if (status.ok()) {
    out << "GET key0593 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23037: put
  db->Put(write_options, "key8060", "value69712");

  // op 23038: put
  db->Put(write_options, "key10276", "value100598");

  // op 23039: put
  db->Put(write_options, "key8384", "value108747");

  // op 23040: get
  status = db->Get(read_options, "key2304", &value);
  if (status.ok()) {
    out << "GET key2304 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23041: delete
  db->Delete(write_options, "key5048");

  // op 23042: get
  status = db->Get(read_options, "key3832", &value);
  if (status.ok()) {
    out << "GET key3832 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23043: delete
  db->Delete(write_options, "key3486");

  // op 23044: delete
  db->Delete(write_options, "key4958");

  // op 23045: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8134", "key9289", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8134, key9289) size=" << scan_result.size() << std::endl;
  }

  // op 23046: delete_range
  status = db->DeleteRange(write_options, "key2789", "key5005");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 23047: get
  status = db->Get(read_options, "key2838", &value);
  if (status.ok()) {
    out << "GET key2838 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23048: put
  db->Put(write_options, "key6117", "value115518");

  // op 23049: get
  status = db->Get(read_options, "key2841", &value);
  if (status.ok()) {
    out << "GET key2841 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23050: put
  db->Put(write_options, "key2167", "value54279");

  // op 23051: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8694", "key8755", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8694, key8755) size=" << scan_result.size() << std::endl;
  }

  // op 23052: delete
  db->Delete(write_options, "key2439");

  // op 23053: put
  db->Put(write_options, "key3334", "value15809");

  // op 23054: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0592", "key11563", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0592, key11563) size=" << scan_result.size() << std::endl;
  }

  // op 23055: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 23056: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 23057: put
  db->Put(write_options, "key4257", "value9699");

  // op 23058: delete_range
  status = db->DeleteRange(write_options, "key5459", "key5704");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 23059: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4798", "key7522", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4798, key7522) size=" << scan_result.size() << std::endl;
  }

  // op 23060: delete_range
  status = db->DeleteRange(write_options, "key0115", "key11389");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 23061: delete
  db->Delete(write_options, "key7986");

  // op 23062: delete_range
  status = db->DeleteRange(write_options, "key0093", "key9916");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 23063: delete_range
  status = db->DeleteRange(write_options, "key6534", "key6732");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 23064: put
  db->Put(write_options, "key1978", "value36564");

  // op 23065: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5514", "key5601", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5514, key5601) size=" << scan_result.size() << std::endl;
  }

  // op 23066: put
  db->Put(write_options, "key5944", "value49117");

  // op 23067: put
  db->Put(write_options, "key11085", "value22878");

  // op 23068: delete_range
  status = db->DeleteRange(write_options, "key8673", "key10980");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 23069: get
  status = db->Get(read_options, "key8709", &value);
  if (status.ok()) {
    out << "GET key8709 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23070: delete_range
  status = db->DeleteRange(write_options, "key10269", "key10619");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 23071: get
  status = db->Get(read_options, "key6118", &value);
  if (status.ok()) {
    out << "GET key6118 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23072: put
  db->Put(write_options, "key8792", "value92870");

  // op 23073: delete
  db->Delete(write_options, "key2527");

  // op 23074: put
  db->Put(write_options, "key11168", "value56802");

  // op 23075: put
  db->Put(write_options, "key2017", "value14559");

  // op 23076: get
  status = db->Get(read_options, "key0052", &value);
  if (status.ok()) {
    out << "GET key0052 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23077: put
  db->Put(write_options, "key6749", "value57615");

  // op 23078: get
  status = db->Get(read_options, "key2587", &value);
  if (status.ok()) {
    out << "GET key2587 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23079: put
  db->Put(write_options, "key0163", "value100103");

  // op 23080: get
  status = db->Get(read_options, "key0299", &value);
  if (status.ok()) {
    out << "GET key0299 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23081: put
  db->Put(write_options, "key8190", "value48887");

  // op 23082: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3377", "key7887", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3377, key7887) size=" << scan_result.size() << std::endl;
  }

  // op 23083: delete
  db->Delete(write_options, "key9450");

  // op 23084: delete_range
  status = db->DeleteRange(write_options, "key6153", "key6358");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 23085: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1535", "key7583", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1535, key7583) size=" << scan_result.size() << std::endl;
  }

  // op 23086: put
  db->Put(write_options, "key9032", "value41478");

  // op 23087: put
  db->Put(write_options, "key5361", "value13982");

  // op 23088: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0467", "key6195", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0467, key6195) size=" << scan_result.size() << std::endl;
  }

  // op 23089: delete
  db->Delete(write_options, "key2812");

  // op 23090: delete_range
  status = db->DeleteRange(write_options, "key1484", "key5382");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 23091: delete
  db->Delete(write_options, "key0796");

  // op 23092: put
  db->Put(write_options, "key6437", "value69741");

  // op 23093: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3267", "key10070", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3267, key10070) size=" << scan_result.size() << std::endl;
  }

  // op 23094: put
  db->Put(write_options, "key4167", "value69492");

  // op 23095: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5887", "key8198", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5887, key8198) size=" << scan_result.size() << std::endl;
  }

  // op 23096: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0107", "key1006", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0107, key1006) size=" << scan_result.size() << std::endl;
  }

  // op 23097: put
  db->Put(write_options, "key6525", "value75631");

  // op 23098: get
  status = db->Get(read_options, "key4764", &value);
  if (status.ok()) {
    out << "GET key4764 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23099: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1463", "key1864", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1463, key1864) size=" << scan_result.size() << std::endl;
  }

  // op 23100: get
  status = db->Get(read_options, "key10402", &value);
  if (status.ok()) {
    out << "GET key10402 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23101: put
  db->Put(write_options, "key10205", "value57850");

  // op 23102: put
  db->Put(write_options, "key5483", "value29885");

  // op 23103: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1712", "key3280", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1712, key3280) size=" << scan_result.size() << std::endl;
  }

  // op 23104: put
  db->Put(write_options, "key6533", "value57995");

  // op 23105: put
  db->Put(write_options, "key2002", "value15861");

  // op 23106: get
  status = db->Get(read_options, "key8515", &value);
  if (status.ok()) {
    out << "GET key8515 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23107: get
  status = db->Get(read_options, "key9972", &value);
  if (status.ok()) {
    out << "GET key9972 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23108: put
  db->Put(write_options, "key4770", "value92168");

  // op 23109: put
  db->Put(write_options, "key10161", "value11449");

  // op 23110: get
  status = db->Get(read_options, "key0730", &value);
  if (status.ok()) {
    out << "GET key0730 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23111: put
  db->Put(write_options, "key11105", "value3218");

  // op 23112: put
  db->Put(write_options, "key0477", "value94936");

  // op 23113: delete
  db->Delete(write_options, "key1019");

  // op 23114: put
  db->Put(write_options, "key3886", "value43685");

  // op 23115: put
  db->Put(write_options, "key4683", "value116658");

  // op 23116: delete_range
  status = db->DeleteRange(write_options, "key0646", "key7456");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 23117: put
  db->Put(write_options, "key5369", "value111843");

  // op 23118: put
  db->Put(write_options, "key1714", "value16197");

  // op 23119: get
  status = db->Get(read_options, "key10559", &value);
  if (status.ok()) {
    out << "GET key10559 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23120: put
  db->Put(write_options, "key3818", "value71407");

  // op 23121: put
  db->Put(write_options, "key11380", "value9259");

  // op 23122: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 23123: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3695", "key8039", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3695, key8039) size=" << scan_result.size() << std::endl;
  }

  // op 23124: put
  db->Put(write_options, "key10443", "value81577");

  // op 23125: get
  status = db->Get(read_options, "key2324", &value);
  if (status.ok()) {
    out << "GET key2324 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23126: delete
  db->Delete(write_options, "key2410");

  // op 23127: put
  db->Put(write_options, "key5037", "value79861");

  // op 23128: delete
  db->Delete(write_options, "key0697");

  // op 23129: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 23130: put
  db->Put(write_options, "key8950", "value62242");

  // op 23131: delete_range
  status = db->DeleteRange(write_options, "key5098", "key8253");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 23132: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3606", "key4404", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3606, key4404) size=" << scan_result.size() << std::endl;
  }

  // op 23133: delete_range
  status = db->DeleteRange(write_options, "key1397", "key8604");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 23134: get
  status = db->Get(read_options, "key10062", &value);
  if (status.ok()) {
    out << "GET key10062 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23135: put
  db->Put(write_options, "key10316", "value98414");

  // op 23136: put
  db->Put(write_options, "key4674", "value70695");

  // op 23137: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0842", "key8323", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0842, key8323) size=" << scan_result.size() << std::endl;
  }

  // op 23138: put
  db->Put(write_options, "key7547", "value17939");

  // op 23139: put
  db->Put(write_options, "key5014", "value48821");

  // op 23140: put
  db->Put(write_options, "key3839", "value37312");

  // op 23141: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0567", "key3560", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0567, key3560) size=" << scan_result.size() << std::endl;
  }

  // op 23142: delete
  db->Delete(write_options, "key5097");

  // op 23143: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 23144: put
  db->Put(write_options, "key3639", "value98680");

  // op 23145: delete
  db->Delete(write_options, "key3689");

  // op 23146: get
  status = db->Get(read_options, "key1108", &value);
  if (status.ok()) {
    out << "GET key1108 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23147: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1364", "key4049", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1364, key4049) size=" << scan_result.size() << std::endl;
  }

  // op 23148: delete
  db->Delete(write_options, "key0015");

  // op 23149: put
  db->Put(write_options, "key7062", "value102135");

  // op 23150: get
  status = db->Get(read_options, "key7198", &value);
  if (status.ok()) {
    out << "GET key7198 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23151: put
  db->Put(write_options, "key10606", "value91290");

  // op 23152: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 23153: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3747", "key7933", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3747, key7933) size=" << scan_result.size() << std::endl;
  }

  // op 23154: put
  db->Put(write_options, "key9986", "value49059");

  // op 23155: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0063", "key6201", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0063, key6201) size=" << scan_result.size() << std::endl;
  }

  // op 23156: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 23157: delete
  db->Delete(write_options, "key9360");

  // op 23158: get
  status = db->Get(read_options, "key11555", &value);
  if (status.ok()) {
    out << "GET key11555 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23159: delete
  db->Delete(write_options, "key1023");

  // op 23160: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 23161: get
  status = db->Get(read_options, "key8975", &value);
  if (status.ok()) {
    out << "GET key8975 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23162: delete
  db->Delete(write_options, "key3017");

  // op 23163: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5804", "key11265", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5804, key11265) size=" << scan_result.size() << std::endl;
  }

  // op 23164: put
  db->Put(write_options, "key6760", "value5141");

  // op 23165: delete
  db->Delete(write_options, "key3759");

  // op 23166: put
  db->Put(write_options, "key9973", "value117533");

  // op 23167: put
  db->Put(write_options, "key3503", "value23846");

  // op 23168: get
  status = db->Get(read_options, "key3157", &value);
  if (status.ok()) {
    out << "GET key3157 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23169: put
  db->Put(write_options, "key5137", "value18132");

  // op 23170: put
  db->Put(write_options, "key5628", "value60924");

  // op 23171: put
  db->Put(write_options, "key2264", "value112484");

  // op 23172: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0875", "key10225", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0875, key10225) size=" << scan_result.size() << std::endl;
  }

  // op 23173: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0469", "key7915", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0469, key7915) size=" << scan_result.size() << std::endl;
  }

  // op 23174: get
  status = db->Get(read_options, "key3648", &value);
  if (status.ok()) {
    out << "GET key3648 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23175: put
  db->Put(write_options, "key6980", "value24726");

  // op 23176: get
  status = db->Get(read_options, "key8915", &value);
  if (status.ok()) {
    out << "GET key8915 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23177: put
  db->Put(write_options, "key7995", "value64504");

  // op 23178: put
  db->Put(write_options, "key11147", "value115559");

  // op 23179: get
  status = db->Get(read_options, "key6357", &value);
  if (status.ok()) {
    out << "GET key6357 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23180: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 23181: delete_range
  status = db->DeleteRange(write_options, "key8735", "key9474");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 23182: put
  db->Put(write_options, "key10740", "value5491");

  // op 23183: put
  db->Put(write_options, "key8253", "value11699");

  // op 23184: delete
  db->Delete(write_options, "key7250");

  // op 23185: put
  db->Put(write_options, "key5923", "value35567");

  // op 23186: put
  db->Put(write_options, "key7632", "value36717");

  // op 23187: put
  db->Put(write_options, "key8951", "value19863");

  // op 23188: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3704", "key4378", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3704, key4378) size=" << scan_result.size() << std::endl;
  }

  // op 23189: put
  db->Put(write_options, "key6547", "value15666");

  // op 23190: put
  db->Put(write_options, "key8114", "value6221");

  // op 23191: put
  db->Put(write_options, "key9524", "value15806");

  // op 23192: get
  status = db->Get(read_options, "key0288", &value);
  if (status.ok()) {
    out << "GET key0288 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23193: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4642", "key6743", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4642, key6743) size=" << scan_result.size() << std::endl;
  }

  // op 23194: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0187", "key2184", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0187, key2184) size=" << scan_result.size() << std::endl;
  }

  // op 23195: put
  db->Put(write_options, "key8850", "value17942");

  // op 23196: delete_range
  status = db->DeleteRange(write_options, "key1471", "key4769");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 23197: put
  db->Put(write_options, "key7143", "value21500");

  // op 23198: get
  status = db->Get(read_options, "key11493", &value);
  if (status.ok()) {
    out << "GET key11493 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23199: delete_range
  status = db->DeleteRange(write_options, "key3515", "key10171");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 23200: delete
  db->Delete(write_options, "key8415");

  // op 23201: get
  status = db->Get(read_options, "key11636", &value);
  if (status.ok()) {
    out << "GET key11636 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23202: put
  db->Put(write_options, "key10570", "value41585");

  // op 23203: get
  status = db->Get(read_options, "key3798", &value);
  if (status.ok()) {
    out << "GET key3798 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23204: get
  status = db->Get(read_options, "key2958", &value);
  if (status.ok()) {
    out << "GET key2958 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23205: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8769", "key8950", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8769, key8950) size=" << scan_result.size() << std::endl;
  }

  // op 23206: put
  db->Put(write_options, "key5118", "value87386");

  // op 23207: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2109", "key11323", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2109, key11323) size=" << scan_result.size() << std::endl;
  }

  // op 23208: delete_range
  status = db->DeleteRange(write_options, "key5744", "key11659");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 23209: put
  db->Put(write_options, "key7404", "value53816");

  // op 23210: put
  db->Put(write_options, "key0962", "value81866");

  // op 23211: put
  db->Put(write_options, "key3221", "value48893");

  // op 23212: put
  db->Put(write_options, "key9282", "value17463");

  // op 23213: get
  status = db->Get(read_options, "key10180", &value);
  if (status.ok()) {
    out << "GET key10180 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23214: put
  db->Put(write_options, "key10090", "value26231");

  // op 23215: get
  status = db->Get(read_options, "key4128", &value);
  if (status.ok()) {
    out << "GET key4128 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23216: put
  db->Put(write_options, "key6525", "value29509");

  // op 23217: put
  db->Put(write_options, "key6401", "value67984");

  // op 23218: put
  db->Put(write_options, "key2083", "value41320");

  // op 23219: put
  db->Put(write_options, "key9576", "value19471");

  // op 23220: get
  status = db->Get(read_options, "key7260", &value);
  if (status.ok()) {
    out << "GET key7260 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23221: delete
  db->Delete(write_options, "key0330");

  // op 23222: get
  status = db->Get(read_options, "key7412", &value);
  if (status.ok()) {
    out << "GET key7412 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23223: delete_range
  status = db->DeleteRange(write_options, "key3500", "key8795");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 23224: put
  db->Put(write_options, "key6597", "value0797");

  // op 23225: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0007", "key4217", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0007, key4217) size=" << scan_result.size() << std::endl;
  }

  // op 23226: get
  status = db->Get(read_options, "key0797", &value);
  if (status.ok()) {
    out << "GET key0797 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23227: put
  db->Put(write_options, "key7384", "value12362");

  // op 23228: get
  status = db->Get(read_options, "key6279", &value);
  if (status.ok()) {
    out << "GET key6279 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23229: put
  db->Put(write_options, "key8546", "value93156");

  // op 23230: put
  db->Put(write_options, "key5508", "value8736");

  // op 23231: put
  db->Put(write_options, "key9875", "value39590");

  // op 23232: put
  db->Put(write_options, "key7710", "value29547");

  // op 23233: put
  db->Put(write_options, "key9444", "value50590");

  // op 23234: put
  db->Put(write_options, "key7069", "value2520");

  // op 23235: get
  status = db->Get(read_options, "key6869", &value);
  if (status.ok()) {
    out << "GET key6869 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23236: delete
  db->Delete(write_options, "key7533");

  // op 23237: put
  db->Put(write_options, "key8326", "value98835");

  // op 23238: put
  db->Put(write_options, "key4111", "value105229");

  // op 23239: put
  db->Put(write_options, "key8943", "value83683");

  // op 23240: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1358", "key2925", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1358, key2925) size=" << scan_result.size() << std::endl;
  }

  // op 23241: put
  db->Put(write_options, "key4318", "value16344");

  // op 23242: put
  db->Put(write_options, "key6804", "value43427");

  // op 23243: put
  db->Put(write_options, "key5597", "value31601");

  // op 23244: delete
  db->Delete(write_options, "key8559");

  // op 23245: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 23246: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 23247: put
  db->Put(write_options, "key4097", "value90594");

  // op 23248: delete
  db->Delete(write_options, "key8512");

  // op 23249: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7623", "key8092", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7623, key8092) size=" << scan_result.size() << std::endl;
  }

  // op 23250: put
  db->Put(write_options, "key6609", "value105788");

  // op 23251: put
  db->Put(write_options, "key10062", "value56838");

  // op 23252: put
  db->Put(write_options, "key5710", "value59169");

  // op 23253: delete
  db->Delete(write_options, "key1660");

  // op 23254: put
  db->Put(write_options, "key8995", "value28858");

  // op 23255: get
  status = db->Get(read_options, "key6329", &value);
  if (status.ok()) {
    out << "GET key6329 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23256: put
  db->Put(write_options, "key11031", "value20877");

  // op 23257: get
  status = db->Get(read_options, "key8502", &value);
  if (status.ok()) {
    out << "GET key8502 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23258: get
  status = db->Get(read_options, "key4702", &value);
  if (status.ok()) {
    out << "GET key4702 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23259: delete
  db->Delete(write_options, "key7062");

  // op 23260: put
  db->Put(write_options, "key6249", "value4537");

  // op 23261: get
  status = db->Get(read_options, "key8361", &value);
  if (status.ok()) {
    out << "GET key8361 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23262: put
  db->Put(write_options, "key9440", "value80801");

  // op 23263: get
  status = db->Get(read_options, "key7762", &value);
  if (status.ok()) {
    out << "GET key7762 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23264: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5784", "key6857", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5784, key6857) size=" << scan_result.size() << std::endl;
  }

  // op 23265: put
  db->Put(write_options, "key3568", "value32013");

  // op 23266: get
  status = db->Get(read_options, "key0221", &value);
  if (status.ok()) {
    out << "GET key0221 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23267: put
  db->Put(write_options, "key1238", "value50401");

  // op 23268: get
  status = db->Get(read_options, "key5481", &value);
  if (status.ok()) {
    out << "GET key5481 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23269: put
  db->Put(write_options, "key11378", "value8296");

  // op 23270: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1980", "key6647", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1980, key6647) size=" << scan_result.size() << std::endl;
  }

  // op 23271: get
  status = db->Get(read_options, "key8613", &value);
  if (status.ok()) {
    out << "GET key8613 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23272: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6493", "key10694", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6493, key10694) size=" << scan_result.size() << std::endl;
  }

  // op 23273: put
  db->Put(write_options, "key8729", "value15577");

  // op 23274: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4154", "key8585", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4154, key8585) size=" << scan_result.size() << std::endl;
  }

  // op 23275: delete
  db->Delete(write_options, "key1529");

  // op 23276: delete_range
  status = db->DeleteRange(write_options, "key6532", "key9046");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 23277: put
  db->Put(write_options, "key2018", "value68843");

  // op 23278: get
  status = db->Get(read_options, "key3473", &value);
  if (status.ok()) {
    out << "GET key3473 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23279: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0667", "key11288", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0667, key11288) size=" << scan_result.size() << std::endl;
  }

  // op 23280: put
  db->Put(write_options, "key10289", "value101742");

  // op 23281: put
  db->Put(write_options, "key10491", "value76565");

  // op 23282: put
  db->Put(write_options, "key8688", "value83393");

  // op 23283: put
  db->Put(write_options, "key4899", "value31747");

  // op 23284: put
  db->Put(write_options, "key4603", "value69518");

  // op 23285: get
  status = db->Get(read_options, "key5031", &value);
  if (status.ok()) {
    out << "GET key5031 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23286: put
  db->Put(write_options, "key2391", "value18273");

  // op 23287: get
  status = db->Get(read_options, "key3275", &value);
  if (status.ok()) {
    out << "GET key3275 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23288: get
  status = db->Get(read_options, "key1285", &value);
  if (status.ok()) {
    out << "GET key1285 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23289: get
  status = db->Get(read_options, "key10841", &value);
  if (status.ok()) {
    out << "GET key10841 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23290: delete
  db->Delete(write_options, "key8985");

  // op 23291: get
  status = db->Get(read_options, "key11161", &value);
  if (status.ok()) {
    out << "GET key11161 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23292: delete_range
  status = db->DeleteRange(write_options, "key2852", "key3668");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 23293: delete_range
  status = db->DeleteRange(write_options, "key0301", "key5253");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 23294: delete
  db->Delete(write_options, "key9173");

  // op 23295: put
  db->Put(write_options, "key10980", "value92691");

  // op 23296: put
  db->Put(write_options, "key2309", "value40371");

  // op 23297: put
  db->Put(write_options, "key10716", "value33692");

  // op 23298: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1090", "key3315", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1090, key3315) size=" << scan_result.size() << std::endl;
  }

  // op 23299: put
  db->Put(write_options, "key8259", "value105186");

  // op 23300: put
  db->Put(write_options, "key7636", "value25044");

  // op 23301: delete
  db->Delete(write_options, "key7389");

  // op 23302: get
  status = db->Get(read_options, "key10234", &value);
  if (status.ok()) {
    out << "GET key10234 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23303: put
  db->Put(write_options, "key3665", "value56640");

  // op 23304: put
  db->Put(write_options, "key8707", "value88705");

  // op 23305: put
  db->Put(write_options, "key7246", "value79331");

  // op 23306: put
  db->Put(write_options, "key7181", "value10665");

  // op 23307: put
  db->Put(write_options, "key4235", "value60685");

  // op 23308: put
  db->Put(write_options, "key0593", "value109257");

  // op 23309: put
  db->Put(write_options, "key9835", "value85654");

  // op 23310: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 23311: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1500", "key6263", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1500, key6263) size=" << scan_result.size() << std::endl;
  }

  // op 23312: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9657", "key10359", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9657, key10359) size=" << scan_result.size() << std::endl;
  }

  // op 23313: get
  status = db->Get(read_options, "key3484", &value);
  if (status.ok()) {
    out << "GET key3484 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23314: put
  db->Put(write_options, "key5975", "value90471");

  // op 23315: get
  status = db->Get(read_options, "key4135", &value);
  if (status.ok()) {
    out << "GET key4135 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23316: delete
  db->Delete(write_options, "key3402");

  // op 23317: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0746", "key3666", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0746, key3666) size=" << scan_result.size() << std::endl;
  }

  // op 23318: get
  status = db->Get(read_options, "key1348", &value);
  if (status.ok()) {
    out << "GET key1348 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23319: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5812", "key9854", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5812, key9854) size=" << scan_result.size() << std::endl;
  }

  // op 23320: put
  db->Put(write_options, "key2083", "value47751");

  // op 23321: put
  db->Put(write_options, "key1195", "value19187");

  // op 23322: get
  status = db->Get(read_options, "key4011", &value);
  if (status.ok()) {
    out << "GET key4011 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23323: get
  status = db->Get(read_options, "key7707", &value);
  if (status.ok()) {
    out << "GET key7707 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23324: delete_range
  status = db->DeleteRange(write_options, "key3572", "key8019");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 23325: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0937", "key2491", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0937, key2491) size=" << scan_result.size() << std::endl;
  }

  // op 23326: put
  db->Put(write_options, "key8650", "value43650");

  // op 23327: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5841", "key9853", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5841, key9853) size=" << scan_result.size() << std::endl;
  }

  // op 23328: get
  status = db->Get(read_options, "key5990", &value);
  if (status.ok()) {
    out << "GET key5990 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23329: put
  db->Put(write_options, "key1237", "value19922");

  // op 23330: delete_range
  status = db->DeleteRange(write_options, "key2165", "key9970");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 23331: put
  db->Put(write_options, "key0903", "value105094");

  // op 23332: delete_range
  status = db->DeleteRange(write_options, "key1350", "key4709");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 23333: delete_range
  status = db->DeleteRange(write_options, "key3871", "key5822");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 23334: put
  db->Put(write_options, "key8436", "value117254");

  // op 23335: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4040", "key8218", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4040, key8218) size=" << scan_result.size() << std::endl;
  }

  // op 23336: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4275", "key10696", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4275, key10696) size=" << scan_result.size() << std::endl;
  }

  // op 23337: get
  status = db->Get(read_options, "key10365", &value);
  if (status.ok()) {
    out << "GET key10365 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23338: put
  db->Put(write_options, "key1337", "value11309");

  // op 23339: delete_range
  status = db->DeleteRange(write_options, "key1788", "key5991");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 23340: put
  db->Put(write_options, "key2274", "value4682");

  // op 23341: put
  db->Put(write_options, "key10272", "value38938");

  // op 23342: get
  status = db->Get(read_options, "key5750", &value);
  if (status.ok()) {
    out << "GET key5750 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23343: delete_range
  status = db->DeleteRange(write_options, "key2486", "key5579");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 23344: put
  db->Put(write_options, "key5082", "value20935");

  // op 23345: delete_range
  status = db->DeleteRange(write_options, "key0621", "key7803");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 23346: delete
  db->Delete(write_options, "key4637");

  // op 23347: get
  status = db->Get(read_options, "key7220", &value);
  if (status.ok()) {
    out << "GET key7220 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23348: put
  db->Put(write_options, "key8638", "value101686");

  // op 23349: get
  status = db->Get(read_options, "key8104", &value);
  if (status.ok()) {
    out << "GET key8104 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23350: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 23351: get
  status = db->Get(read_options, "key1483", &value);
  if (status.ok()) {
    out << "GET key1483 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23352: delete
  db->Delete(write_options, "key0443");

  // op 23353: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1710", "key7944", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1710, key7944) size=" << scan_result.size() << std::endl;
  }

  // op 23354: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4575", "key6926", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4575, key6926) size=" << scan_result.size() << std::endl;
  }

  // op 23355: delete
  db->Delete(write_options, "key5213");

  // op 23356: delete_range
  status = db->DeleteRange(write_options, "key5432", "key8845");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 23357: put
  db->Put(write_options, "key6833", "value83561");

  // op 23358: get
  status = db->Get(read_options, "key0119", &value);
  if (status.ok()) {
    out << "GET key0119 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23359: put
  db->Put(write_options, "key8417", "value65852");

  // op 23360: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1494", "key9880", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1494, key9880) size=" << scan_result.size() << std::endl;
  }

  // op 23361: put
  db->Put(write_options, "key7910", "value97432");

  // op 23362: delete_range
  status = db->DeleteRange(write_options, "key6086", "key8757");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 23363: get
  status = db->Get(read_options, "key9015", &value);
  if (status.ok()) {
    out << "GET key9015 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23364: get
  status = db->Get(read_options, "key1465", &value);
  if (status.ok()) {
    out << "GET key1465 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23365: put
  db->Put(write_options, "key9407", "value31855");

  // op 23366: get
  status = db->Get(read_options, "key6629", &value);
  if (status.ok()) {
    out << "GET key6629 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23367: put
  db->Put(write_options, "key2173", "value24524");

  // op 23368: get
  status = db->Get(read_options, "key0797", &value);
  if (status.ok()) {
    out << "GET key0797 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23369: put
  db->Put(write_options, "key0840", "value104746");

  // op 23370: put
  db->Put(write_options, "key3807", "value57544");

  // op 23371: put
  db->Put(write_options, "key6952", "value53972");

  // op 23372: get
  status = db->Get(read_options, "key1683", &value);
  if (status.ok()) {
    out << "GET key1683 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23373: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1841", "key6134", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1841, key6134) size=" << scan_result.size() << std::endl;
  }

  // op 23374: put
  db->Put(write_options, "key4552", "value66963");

  // op 23375: put
  db->Put(write_options, "key11225", "value19437");

  // op 23376: put
  db->Put(write_options, "key7720", "value49836");

  // op 23377: get
  status = db->Get(read_options, "key10612", &value);
  if (status.ok()) {
    out << "GET key10612 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23378: delete_range
  status = db->DeleteRange(write_options, "key2396", "key10923");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 23379: get
  status = db->Get(read_options, "key8145", &value);
  if (status.ok()) {
    out << "GET key8145 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23380: put
  db->Put(write_options, "key8245", "value75386");

  // op 23381: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2217", "key9354", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2217, key9354) size=" << scan_result.size() << std::endl;
  }

  // op 23382: get
  status = db->Get(read_options, "key9295", &value);
  if (status.ok()) {
    out << "GET key9295 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23383: put
  db->Put(write_options, "key5376", "value68160");

  // op 23384: get
  status = db->Get(read_options, "key5964", &value);
  if (status.ok()) {
    out << "GET key5964 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23385: delete_range
  status = db->DeleteRange(write_options, "key9701", "key9803");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 23386: delete
  db->Delete(write_options, "key1662");

  // op 23387: put
  db->Put(write_options, "key1153", "value98122");

  // op 23388: put
  db->Put(write_options, "key7580", "value95447");

  // op 23389: put
  db->Put(write_options, "key5943", "value106919");

  // op 23390: get
  status = db->Get(read_options, "key6458", &value);
  if (status.ok()) {
    out << "GET key6458 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23391: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5578", "key9599", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5578, key9599) size=" << scan_result.size() << std::endl;
  }

  // op 23392: get
  status = db->Get(read_options, "key8279", &value);
  if (status.ok()) {
    out << "GET key8279 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23393: put
  db->Put(write_options, "key7133", "value39456");

  // op 23394: put
  db->Put(write_options, "key10875", "value80801");

  // op 23395: get
  status = db->Get(read_options, "key2550", &value);
  if (status.ok()) {
    out << "GET key2550 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23396: get
  status = db->Get(read_options, "key4711", &value);
  if (status.ok()) {
    out << "GET key4711 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23397: put
  db->Put(write_options, "key1895", "value29937");

  // op 23398: put
  db->Put(write_options, "key6872", "value74299");

  // op 23399: put
  db->Put(write_options, "key4756", "value89089");

  // op 23400: delete_range
  status = db->DeleteRange(write_options, "key3790", "key8474");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 23401: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2320", "key10804", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2320, key10804) size=" << scan_result.size() << std::endl;
  }

  // op 23402: get
  status = db->Get(read_options, "key10103", &value);
  if (status.ok()) {
    out << "GET key10103 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23403: get
  status = db->Get(read_options, "key7508", &value);
  if (status.ok()) {
    out << "GET key7508 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23404: put
  db->Put(write_options, "key7827", "value98444");

  // op 23405: get
  status = db->Get(read_options, "key7314", &value);
  if (status.ok()) {
    out << "GET key7314 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23406: delete
  db->Delete(write_options, "key2433");

  // op 23407: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1787", "key2136", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1787, key2136) size=" << scan_result.size() << std::endl;
  }

  // op 23408: delete
  db->Delete(write_options, "key3993");

  // op 23409: get
  status = db->Get(read_options, "key1314", &value);
  if (status.ok()) {
    out << "GET key1314 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23410: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4798", "key9741", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4798, key9741) size=" << scan_result.size() << std::endl;
  }

  // op 23411: get
  status = db->Get(read_options, "key7400", &value);
  if (status.ok()) {
    out << "GET key7400 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23412: put
  db->Put(write_options, "key3532", "value93135");

  // op 23413: delete
  db->Delete(write_options, "key3242");

  // op 23414: get
  status = db->Get(read_options, "key2177", &value);
  if (status.ok()) {
    out << "GET key2177 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23415: put
  db->Put(write_options, "key5914", "value40493");

  // op 23416: delete
  db->Delete(write_options, "key4549");

  // op 23417: delete
  db->Delete(write_options, "key3204");

  // op 23418: put
  db->Put(write_options, "key10157", "value14740");

  // op 23419: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 23420: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 23421: get
  status = db->Get(read_options, "key2232", &value);
  if (status.ok()) {
    out << "GET key2232 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23422: put
  db->Put(write_options, "key0306", "value40569");

  // op 23423: put
  db->Put(write_options, "key6965", "value3362");

  // op 23424: delete_range
  status = db->DeleteRange(write_options, "key6659", "key8206");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 23425: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 23426: put
  db->Put(write_options, "key7559", "value101414");

  // op 23427: get
  status = db->Get(read_options, "key7769", &value);
  if (status.ok()) {
    out << "GET key7769 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23428: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4433", "key11707", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4433, key11707) size=" << scan_result.size() << std::endl;
  }

  // op 23429: put
  db->Put(write_options, "key4888", "value38199");

  // op 23430: put
  db->Put(write_options, "key0119", "value1123");

  // op 23431: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0855", "key3989", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0855, key3989) size=" << scan_result.size() << std::endl;
  }

  // op 23432: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 23433: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10093", "key10813", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10093, key10813) size=" << scan_result.size() << std::endl;
  }

  // op 23434: get
  status = db->Get(read_options, "key11099", &value);
  if (status.ok()) {
    out << "GET key11099 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23435: put
  db->Put(write_options, "key0273", "value14228");

  // op 23436: put
  db->Put(write_options, "key1186", "value79956");

  // op 23437: get
  status = db->Get(read_options, "key2308", &value);
  if (status.ok()) {
    out << "GET key2308 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23438: delete
  db->Delete(write_options, "key0337");

  // op 23439: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9030", "key10383", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9030, key10383) size=" << scan_result.size() << std::endl;
  }

  // op 23440: put
  db->Put(write_options, "key10527", "value35800");

  // op 23441: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1195", "key8575", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1195, key8575) size=" << scan_result.size() << std::endl;
  }

  // op 23442: get
  status = db->Get(read_options, "key2951", &value);
  if (status.ok()) {
    out << "GET key2951 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23443: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4535", "key11035", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4535, key11035) size=" << scan_result.size() << std::endl;
  }

  // op 23444: put
  db->Put(write_options, "key7976", "value94699");

  // op 23445: put
  db->Put(write_options, "key9698", "value112673");

  // op 23446: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0244", "key4706", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0244, key4706) size=" << scan_result.size() << std::endl;
  }

  // op 23447: put
  db->Put(write_options, "key11458", "value106844");

  // op 23448: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9263", "key9873", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9263, key9873) size=" << scan_result.size() << std::endl;
  }

  // op 23449: put
  db->Put(write_options, "key10361", "value72454");

  // op 23450: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 23451: put
  db->Put(write_options, "key10195", "value117268");

  // op 23452: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4392", "key9058", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4392, key9058) size=" << scan_result.size() << std::endl;
  }

  // op 23453: delete
  db->Delete(write_options, "key5317");

  // op 23454: delete
  db->Delete(write_options, "key2394");

  // op 23455: delete
  db->Delete(write_options, "key4986");

  // op 23456: put
  db->Put(write_options, "key2873", "value26652");

  // op 23457: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1046", "key8460", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1046, key8460) size=" << scan_result.size() << std::endl;
  }

  // op 23458: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0083", "key2426", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0083, key2426) size=" << scan_result.size() << std::endl;
  }

  // op 23459: get
  status = db->Get(read_options, "key7494", &value);
  if (status.ok()) {
    out << "GET key7494 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23460: delete_range
  status = db->DeleteRange(write_options, "key7554", "key10210");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 23461: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2518", "key6766", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2518, key6766) size=" << scan_result.size() << std::endl;
  }

  // op 23462: delete
  db->Delete(write_options, "key6572");

  // op 23463: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3356", "key7063", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3356, key7063) size=" << scan_result.size() << std::endl;
  }

  // op 23464: put
  db->Put(write_options, "key6091", "value37628");

  // op 23465: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 23466: put
  db->Put(write_options, "key10812", "value8714");

  // op 23467: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4951", "key9866", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4951, key9866) size=" << scan_result.size() << std::endl;
  }

  // op 23468: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3507", "key9496", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3507, key9496) size=" << scan_result.size() << std::endl;
  }

  // op 23469: put
  db->Put(write_options, "key5304", "value0345");

  // op 23470: delete_range
  status = db->DeleteRange(write_options, "key1666", "key7501");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 23471: delete_range
  status = db->DeleteRange(write_options, "key1768", "key5620");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 23472: delete
  db->Delete(write_options, "key7124");

  // op 23473: put
  db->Put(write_options, "key1769", "value24272");

  // op 23474: put
  db->Put(write_options, "key3687", "value61966");

  // op 23475: put
  db->Put(write_options, "key7988", "value61921");

  // op 23476: get
  status = db->Get(read_options, "key9144", &value);
  if (status.ok()) {
    out << "GET key9144 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23477: put
  db->Put(write_options, "key11279", "value107091");

  // op 23478: delete_range
  status = db->DeleteRange(write_options, "key3847", "key9036");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 23479: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2703", "key9526", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2703, key9526) size=" << scan_result.size() << std::endl;
  }

  // op 23480: delete
  db->Delete(write_options, "key1990");

  // op 23481: get
  status = db->Get(read_options, "key6808", &value);
  if (status.ok()) {
    out << "GET key6808 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23482: delete
  db->Delete(write_options, "key3131");

  // op 23483: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0271", "key6247", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0271, key6247) size=" << scan_result.size() << std::endl;
  }

  // op 23484: put
  db->Put(write_options, "key11145", "value49724");

  // op 23485: put
  db->Put(write_options, "key8483", "value21767");

  // op 23486: delete
  db->Delete(write_options, "key11390");

  // op 23487: get
  status = db->Get(read_options, "key6237", &value);
  if (status.ok()) {
    out << "GET key6237 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23488: put
  db->Put(write_options, "key4544", "value45650");

  // op 23489: put
  db->Put(write_options, "key2717", "value56223");

  // op 23490: put
  db->Put(write_options, "key8253", "value46531");

  // op 23491: get
  status = db->Get(read_options, "key4837", &value);
  if (status.ok()) {
    out << "GET key4837 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23492: delete
  db->Delete(write_options, "key0118");

  // op 23493: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 23494: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 23495: delete_range
  status = db->DeleteRange(write_options, "key7541", "key8952");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 23496: get
  status = db->Get(read_options, "key3314", &value);
  if (status.ok()) {
    out << "GET key3314 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23497: get
  status = db->Get(read_options, "key3013", &value);
  if (status.ok()) {
    out << "GET key3013 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23498: put
  db->Put(write_options, "key0547", "value61094");

  // op 23499: put
  db->Put(write_options, "key4838", "value59094");

  // op 23500: put
  db->Put(write_options, "key10220", "value71742");

  // op 23501: put
  db->Put(write_options, "key3094", "value1208");

  // op 23502: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0466", "key0977", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0466, key0977) size=" << scan_result.size() << std::endl;
  }

  // op 23503: put
  db->Put(write_options, "key2045", "value56892");

  // op 23504: put
  db->Put(write_options, "key6639", "value74713");

  // op 23505: put
  db->Put(write_options, "key8597", "value7407");

  // op 23506: put
  db->Put(write_options, "key7235", "value40076");

  // op 23507: get
  status = db->Get(read_options, "key8809", &value);
  if (status.ok()) {
    out << "GET key8809 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23508: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3563", "key9048", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3563, key9048) size=" << scan_result.size() << std::endl;
  }

  // op 23509: get
  status = db->Get(read_options, "key0260", &value);
  if (status.ok()) {
    out << "GET key0260 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23510: delete_range
  status = db->DeleteRange(write_options, "key5887", "key8681");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 23511: put
  db->Put(write_options, "key2869", "value85322");

  // op 23512: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0233", "key1804", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0233, key1804) size=" << scan_result.size() << std::endl;
  }

  // op 23513: delete
  db->Delete(write_options, "key7729");

  // op 23514: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 23515: put
  db->Put(write_options, "key6685", "value60360");

  // op 23516: get
  status = db->Get(read_options, "key10628", &value);
  if (status.ok()) {
    out << "GET key10628 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23517: put
  db->Put(write_options, "key2171", "value98878");

  // op 23518: put
  db->Put(write_options, "key3401", "value113784");

  // op 23519: delete_range
  status = db->DeleteRange(write_options, "key3708", "key11595");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 23520: put
  db->Put(write_options, "key9967", "value96684");

  // op 23521: put
  db->Put(write_options, "key3900", "value25156");

  // op 23522: get
  status = db->Get(read_options, "key6928", &value);
  if (status.ok()) {
    out << "GET key6928 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23523: delete
  db->Delete(write_options, "key2678");

  // op 23524: get
  status = db->Get(read_options, "key4004", &value);
  if (status.ok()) {
    out << "GET key4004 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23525: get
  status = db->Get(read_options, "key3202", &value);
  if (status.ok()) {
    out << "GET key3202 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23526: delete
  db->Delete(write_options, "key5621");

  // op 23527: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5902", "key9853", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5902, key9853) size=" << scan_result.size() << std::endl;
  }

  // op 23528: get
  status = db->Get(read_options, "key8252", &value);
  if (status.ok()) {
    out << "GET key8252 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23529: put
  db->Put(write_options, "key5649", "value109500");

  // op 23530: get
  status = db->Get(read_options, "key5988", &value);
  if (status.ok()) {
    out << "GET key5988 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23531: put
  db->Put(write_options, "key4046", "value42556");

  // op 23532: put
  db->Put(write_options, "key0826", "value89245");

  // op 23533: put
  db->Put(write_options, "key11490", "value56824");

  // op 23534: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1173", "key6079", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1173, key6079) size=" << scan_result.size() << std::endl;
  }

  // op 23535: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6024", "key7584", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6024, key7584) size=" << scan_result.size() << std::endl;
  }

  // op 23536: put
  db->Put(write_options, "key8079", "value103919");

  // op 23537: put
  db->Put(write_options, "key4316", "value42031");

  // op 23538: delete_range
  status = db->DeleteRange(write_options, "key2115", "key9073");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 23539: delete_range
  status = db->DeleteRange(write_options, "key6556", "key9768");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 23540: put
  db->Put(write_options, "key9397", "value48038");

  // op 23541: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5095", "key7358", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5095, key7358) size=" << scan_result.size() << std::endl;
  }

  // op 23542: put
  db->Put(write_options, "key2035", "value57684");

  // op 23543: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 23544: put
  db->Put(write_options, "key8841", "value99578");

  // op 23545: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 23546: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0398", "key5996", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0398, key5996) size=" << scan_result.size() << std::endl;
  }

  // op 23547: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0584", "key8415", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0584, key8415) size=" << scan_result.size() << std::endl;
  }

  // op 23548: delete_range
  status = db->DeleteRange(write_options, "key2333", "key3847");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 23549: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0669", "key6736", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0669, key6736) size=" << scan_result.size() << std::endl;
  }

  // op 23550: put
  db->Put(write_options, "key1914", "value112691");

  // op 23551: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 23552: put
  db->Put(write_options, "key1738", "value14106");

  // op 23553: get
  status = db->Get(read_options, "key3342", &value);
  if (status.ok()) {
    out << "GET key3342 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23554: put
  db->Put(write_options, "key2992", "value37767");

  // op 23555: put
  db->Put(write_options, "key1634", "value50651");

  // op 23556: put
  db->Put(write_options, "key11363", "value3553");

  // op 23557: delete
  db->Delete(write_options, "key5964");

  // op 23558: put
  db->Put(write_options, "key5270", "value102460");

  // op 23559: get
  status = db->Get(read_options, "key7192", &value);
  if (status.ok()) {
    out << "GET key7192 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23560: get
  status = db->Get(read_options, "key4391", &value);
  if (status.ok()) {
    out << "GET key4391 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23561: put
  db->Put(write_options, "key10093", "value73924");

  // op 23562: put
  db->Put(write_options, "key6844", "value22857");

  // op 23563: put
  db->Put(write_options, "key7973", "value82139");

  // op 23564: put
  db->Put(write_options, "key10245", "value89775");

  // op 23565: put
  db->Put(write_options, "key0216", "value45067");

  // op 23566: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0055", "key10488", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0055, key10488) size=" << scan_result.size() << std::endl;
  }

  // op 23567: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 23568: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 23569: delete
  db->Delete(write_options, "key10632");

  // op 23570: put
  db->Put(write_options, "key3777", "value112356");

  // op 23571: get
  status = db->Get(read_options, "key2005", &value);
  if (status.ok()) {
    out << "GET key2005 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23572: put
  db->Put(write_options, "key9582", "value87115");

  // op 23573: put
  db->Put(write_options, "key5294", "value115612");

  // op 23574: put
  db->Put(write_options, "key5992", "value37404");

  // op 23575: delete
  db->Delete(write_options, "key5579");

  // op 23576: put
  db->Put(write_options, "key1658", "value106387");

  // op 23577: delete_range
  status = db->DeleteRange(write_options, "key3755", "key8363");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 23578: get
  status = db->Get(read_options, "key8770", &value);
  if (status.ok()) {
    out << "GET key8770 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23579: delete
  db->Delete(write_options, "key5202");

  // op 23580: delete
  db->Delete(write_options, "key7016");

  // op 23581: delete_range
  status = db->DeleteRange(write_options, "key0701", "key10600");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 23582: delete
  db->Delete(write_options, "key1655");

  // op 23583: get
  status = db->Get(read_options, "key2249", &value);
  if (status.ok()) {
    out << "GET key2249 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23584: get
  status = db->Get(read_options, "key0220", &value);
  if (status.ok()) {
    out << "GET key0220 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23585: put
  db->Put(write_options, "key11579", "value13914");

  // op 23586: put
  db->Put(write_options, "key0432", "value93462");

  // op 23587: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4381", "key7796", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4381, key7796) size=" << scan_result.size() << std::endl;
  }

  // op 23588: put
  db->Put(write_options, "key1193", "value89186");

  // op 23589: delete_range
  status = db->DeleteRange(write_options, "key0306", "key2465");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 23590: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5669", "key8550", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5669, key8550) size=" << scan_result.size() << std::endl;
  }

  // op 23591: delete_range
  status = db->DeleteRange(write_options, "key1830", "key10828");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 23592: put
  db->Put(write_options, "key0247", "value49020");

  // op 23593: delete
  db->Delete(write_options, "key6456");

  // op 23594: put
  db->Put(write_options, "key2364", "value1725");

  // op 23595: get
  status = db->Get(read_options, "key8014", &value);
  if (status.ok()) {
    out << "GET key8014 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23596: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6300", "key8290", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6300, key8290) size=" << scan_result.size() << std::endl;
  }

  // op 23597: put
  db->Put(write_options, "key5066", "value41594");

  // op 23598: delete
  db->Delete(write_options, "key5858");

  // op 23599: put
  db->Put(write_options, "key6017", "value14709");

  // op 23600: put
  db->Put(write_options, "key7183", "value89775");

  // op 23601: put
  db->Put(write_options, "key11456", "value70133");

  // op 23602: get
  status = db->Get(read_options, "key5836", &value);
  if (status.ok()) {
    out << "GET key5836 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23603: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9456", "key10488", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9456, key10488) size=" << scan_result.size() << std::endl;
  }

  // op 23604: get
  status = db->Get(read_options, "key9552", &value);
  if (status.ok()) {
    out << "GET key9552 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23605: put
  db->Put(write_options, "key0810", "value62976");

  // op 23606: delete
  db->Delete(write_options, "key8216");

  // op 23607: delete_range
  status = db->DeleteRange(write_options, "key5421", "key5696");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 23608: get
  status = db->Get(read_options, "key7878", &value);
  if (status.ok()) {
    out << "GET key7878 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23609: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6506", "key9110", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6506, key9110) size=" << scan_result.size() << std::endl;
  }

  // op 23610: get
  status = db->Get(read_options, "key9820", &value);
  if (status.ok()) {
    out << "GET key9820 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23611: put
  db->Put(write_options, "key0872", "value73198");

  // op 23612: put
  db->Put(write_options, "key4961", "value66145");

  // op 23613: get
  status = db->Get(read_options, "key2555", &value);
  if (status.ok()) {
    out << "GET key2555 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23614: put
  db->Put(write_options, "key9237", "value96221");

  // op 23615: get
  status = db->Get(read_options, "key1033", &value);
  if (status.ok()) {
    out << "GET key1033 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23616: delete
  db->Delete(write_options, "key7936");

  // op 23617: put
  db->Put(write_options, "key11479", "value9591");

  // op 23618: put
  db->Put(write_options, "key5024", "value28916");

  // op 23619: get
  status = db->Get(read_options, "key11020", &value);
  if (status.ok()) {
    out << "GET key11020 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23620: get
  status = db->Get(read_options, "key5550", &value);
  if (status.ok()) {
    out << "GET key5550 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23621: get
  status = db->Get(read_options, "key10223", &value);
  if (status.ok()) {
    out << "GET key10223 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23622: put
  db->Put(write_options, "key9759", "value33527");

  // op 23623: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1882", "key10477", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1882, key10477) size=" << scan_result.size() << std::endl;
  }

  // op 23624: get
  status = db->Get(read_options, "key1866", &value);
  if (status.ok()) {
    out << "GET key1866 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23625: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4069", "key11725", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4069, key11725) size=" << scan_result.size() << std::endl;
  }

  // op 23626: get
  status = db->Get(read_options, "key9271", &value);
  if (status.ok()) {
    out << "GET key9271 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23627: put
  db->Put(write_options, "key5582", "value32345");

  // op 23628: put
  db->Put(write_options, "key8697", "value104425");

  // op 23629: delete
  db->Delete(write_options, "key9047");

  // op 23630: delete_range
  status = db->DeleteRange(write_options, "key9590", "key10727");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 23631: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2320", "key10923", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2320, key10923) size=" << scan_result.size() << std::endl;
  }

  // op 23632: put
  db->Put(write_options, "key7324", "value22910");

  // op 23633: delete
  db->Delete(write_options, "key7077");

  // op 23634: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 23635: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2411", "key8863", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2411, key8863) size=" << scan_result.size() << std::endl;
  }

  // op 23636: get
  status = db->Get(read_options, "key1001", &value);
  if (status.ok()) {
    out << "GET key1001 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23637: put
  db->Put(write_options, "key3269", "value102147");

  // op 23638: delete_range
  status = db->DeleteRange(write_options, "key2964", "key11447");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 23639: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1051", "key8753", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1051, key8753) size=" << scan_result.size() << std::endl;
  }

  // op 23640: get
  status = db->Get(read_options, "key11283", &value);
  if (status.ok()) {
    out << "GET key11283 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23641: delete
  db->Delete(write_options, "key3277");

  // op 23642: delete
  db->Delete(write_options, "key10051");

  // op 23643: delete
  db->Delete(write_options, "key0486");

  // op 23644: put
  db->Put(write_options, "key9417", "value99774");

  // op 23645: get
  status = db->Get(read_options, "key3399", &value);
  if (status.ok()) {
    out << "GET key3399 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23646: put
  db->Put(write_options, "key3443", "value95838");

  // op 23647: put
  db->Put(write_options, "key10892", "value39454");

  // op 23648: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 23649: put
  db->Put(write_options, "key0224", "value3014");

  // op 23650: put
  db->Put(write_options, "key6219", "value87235");

  // op 23651: put
  db->Put(write_options, "key2207", "value81466");

  // op 23652: put
  db->Put(write_options, "key7067", "value21003");

  // op 23653: put
  db->Put(write_options, "key4699", "value37982");

  // op 23654: delete_range
  status = db->DeleteRange(write_options, "key4494", "key9904");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 23655: delete
  db->Delete(write_options, "key8385");

  // op 23656: delete
  db->Delete(write_options, "key10195");

  // op 23657: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6316", "key6928", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6316, key6928) size=" << scan_result.size() << std::endl;
  }

  // op 23658: delete
  db->Delete(write_options, "key7868");

  // op 23659: put
  db->Put(write_options, "key10032", "value55080");

  // op 23660: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7187", "key11118", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7187, key11118) size=" << scan_result.size() << std::endl;
  }

  // op 23661: put
  db->Put(write_options, "key1870", "value64693");

  // op 23662: get
  status = db->Get(read_options, "key7301", &value);
  if (status.ok()) {
    out << "GET key7301 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23663: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 23664: put
  db->Put(write_options, "key6227", "value37615");

  // op 23665: put
  db->Put(write_options, "key4080", "value113023");

  // op 23666: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 23667: put
  db->Put(write_options, "key10501", "value87505");

  // op 23668: put
  db->Put(write_options, "key7992", "value96124");

  // op 23669: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2045", "key11307", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2045, key11307) size=" << scan_result.size() << std::endl;
  }

  // op 23670: delete_range
  status = db->DeleteRange(write_options, "key7040", "key7474");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 23671: delete
  db->Delete(write_options, "key8265");

  // op 23672: put
  db->Put(write_options, "key9242", "value24017");

  // op 23673: get
  status = db->Get(read_options, "key2576", &value);
  if (status.ok()) {
    out << "GET key2576 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23674: delete_range
  status = db->DeleteRange(write_options, "key3474", "key5294");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 23675: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 23676: delete_range
  status = db->DeleteRange(write_options, "key6894", "key7533");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 23677: put
  db->Put(write_options, "key8153", "value25604");

  // op 23678: get
  status = db->Get(read_options, "key0906", &value);
  if (status.ok()) {
    out << "GET key0906 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23679: delete
  db->Delete(write_options, "key6911");

  // op 23680: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5363", "key9111", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5363, key9111) size=" << scan_result.size() << std::endl;
  }

  // op 23681: delete_range
  status = db->DeleteRange(write_options, "key7237", "key9132");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 23682: get
  status = db->Get(read_options, "key9068", &value);
  if (status.ok()) {
    out << "GET key9068 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23683: put
  db->Put(write_options, "key9919", "value7296");

  // op 23684: delete_range
  status = db->DeleteRange(write_options, "key0306", "key4141");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 23685: put
  db->Put(write_options, "key5371", "value4048");

  // op 23686: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 23687: put
  db->Put(write_options, "key6724", "value117737");

  // op 23688: get
  status = db->Get(read_options, "key1872", &value);
  if (status.ok()) {
    out << "GET key1872 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23689: put
  db->Put(write_options, "key0707", "value103457");

  // op 23690: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2582", "key3026", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2582, key3026) size=" << scan_result.size() << std::endl;
  }

  // op 23691: put
  db->Put(write_options, "key4480", "value49807");

  // op 23692: put
  db->Put(write_options, "key9578", "value47421");

  // op 23693: put
  db->Put(write_options, "key6960", "value105300");

  // op 23694: put
  db->Put(write_options, "key2418", "value0556");

  // op 23695: get
  status = db->Get(read_options, "key0292", &value);
  if (status.ok()) {
    out << "GET key0292 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23696: put
  db->Put(write_options, "key10681", "value80739");

  // op 23697: put
  db->Put(write_options, "key3226", "value115794");

  // op 23698: put
  db->Put(write_options, "key8614", "value114808");

  // op 23699: delete_range
  status = db->DeleteRange(write_options, "key1610", "key3548");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 23700: get
  status = db->Get(read_options, "key9234", &value);
  if (status.ok()) {
    out << "GET key9234 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23701: delete_range
  status = db->DeleteRange(write_options, "key7406", "key9657");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 23702: put
  db->Put(write_options, "key0704", "value3301");

  // op 23703: put
  db->Put(write_options, "key10538", "value86814");

  // op 23704: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 23705: put
  db->Put(write_options, "key4670", "value44639");

  // op 23706: put
  db->Put(write_options, "key6855", "value29503");

  // op 23707: put
  db->Put(write_options, "key0223", "value78052");

  // op 23708: put
  db->Put(write_options, "key11465", "value106348");

  // op 23709: delete
  db->Delete(write_options, "key1331");

  // op 23710: put
  db->Put(write_options, "key5957", "value32743");

  // op 23711: delete
  db->Delete(write_options, "key8636");

  // op 23712: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 23713: put
  db->Put(write_options, "key4971", "value29234");

  // op 23714: get
  status = db->Get(read_options, "key7250", &value);
  if (status.ok()) {
    out << "GET key7250 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23715: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6923", "key10361", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6923, key10361) size=" << scan_result.size() << std::endl;
  }

  // op 23716: put
  db->Put(write_options, "key4837", "value67833");

  // op 23717: put
  db->Put(write_options, "key3058", "value73330");

  // op 23718: get
  status = db->Get(read_options, "key2892", &value);
  if (status.ok()) {
    out << "GET key2892 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23719: put
  db->Put(write_options, "key6270", "value116598");

  // op 23720: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0131", "key5787", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0131, key5787) size=" << scan_result.size() << std::endl;
  }

  // op 23721: delete_range
  status = db->DeleteRange(write_options, "key7386", "key7927");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 23722: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1852", "key2191", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1852, key2191) size=" << scan_result.size() << std::endl;
  }

  // op 23723: put
  db->Put(write_options, "key9779", "value37159");

  // op 23724: put
  db->Put(write_options, "key4196", "value87114");

  // op 23725: put
  db->Put(write_options, "key8546", "value93086");

  // op 23726: put
  db->Put(write_options, "key11636", "value42523");

  // op 23727: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 23728: put
  db->Put(write_options, "key5202", "value67046");

  // op 23729: get
  status = db->Get(read_options, "key1811", &value);
  if (status.ok()) {
    out << "GET key1811 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23730: get
  status = db->Get(read_options, "key0227", &value);
  if (status.ok()) {
    out << "GET key0227 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23731: delete
  db->Delete(write_options, "key8409");

  // op 23732: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4809", "key10035", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4809, key10035) size=" << scan_result.size() << std::endl;
  }

  // op 23733: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2221", "key4413", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2221, key4413) size=" << scan_result.size() << std::endl;
  }

  // op 23734: get
  status = db->Get(read_options, "key0037", &value);
  if (status.ok()) {
    out << "GET key0037 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23735: get
  status = db->Get(read_options, "key6880", &value);
  if (status.ok()) {
    out << "GET key6880 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23736: put
  db->Put(write_options, "key2920", "value29678");

  // op 23737: put
  db->Put(write_options, "key5263", "value64218");

  // op 23738: get
  status = db->Get(read_options, "key5800", &value);
  if (status.ok()) {
    out << "GET key5800 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23739: put
  db->Put(write_options, "key0387", "value34904");

  // op 23740: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4730", "key4913", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4730, key4913) size=" << scan_result.size() << std::endl;
  }

  // op 23741: delete
  db->Delete(write_options, "key6523");

  // op 23742: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4104", "key10214", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4104, key10214) size=" << scan_result.size() << std::endl;
  }

  // op 23743: get
  status = db->Get(read_options, "key1443", &value);
  if (status.ok()) {
    out << "GET key1443 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23744: delete
  db->Delete(write_options, "key0992");

  // op 23745: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4351", "key8201", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4351, key8201) size=" << scan_result.size() << std::endl;
  }

  // op 23746: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6384", "key6503", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6384, key6503) size=" << scan_result.size() << std::endl;
  }

  // op 23747: put
  db->Put(write_options, "key6648", "value75730");

  // op 23748: put
  db->Put(write_options, "key10331", "value105184");

  // op 23749: delete_range
  status = db->DeleteRange(write_options, "key3774", "key4422");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 23750: put
  db->Put(write_options, "key5099", "value91546");

  // op 23751: get
  status = db->Get(read_options, "key8094", &value);
  if (status.ok()) {
    out << "GET key8094 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23752: get
  status = db->Get(read_options, "key0011", &value);
  if (status.ok()) {
    out << "GET key0011 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23753: delete
  db->Delete(write_options, "key2363");

  // op 23754: get
  status = db->Get(read_options, "key8169", &value);
  if (status.ok()) {
    out << "GET key8169 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23755: delete_range
  status = db->DeleteRange(write_options, "key1267", "key2700");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 23756: get
  status = db->Get(read_options, "key10954", &value);
  if (status.ok()) {
    out << "GET key10954 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23757: get
  status = db->Get(read_options, "key4187", &value);
  if (status.ok()) {
    out << "GET key4187 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23758: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7067", "key11072", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7067, key11072) size=" << scan_result.size() << std::endl;
  }

  // op 23759: put
  db->Put(write_options, "key5293", "value28887");

  // op 23760: get
  status = db->Get(read_options, "key4102", &value);
  if (status.ok()) {
    out << "GET key4102 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23761: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8207", "key9523", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8207, key9523) size=" << scan_result.size() << std::endl;
  }

  // op 23762: put
  db->Put(write_options, "key3978", "value73108");

  // op 23763: put
  db->Put(write_options, "key3139", "value46449");

  // op 23764: get
  status = db->Get(read_options, "key5646", &value);
  if (status.ok()) {
    out << "GET key5646 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23765: put
  db->Put(write_options, "key0388", "value19761");

  // op 23766: get
  status = db->Get(read_options, "key8459", &value);
  if (status.ok()) {
    out << "GET key8459 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23767: get
  status = db->Get(read_options, "key7391", &value);
  if (status.ok()) {
    out << "GET key7391 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23768: put
  db->Put(write_options, "key2508", "value49022");

  // op 23769: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0498", "key9477", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0498, key9477) size=" << scan_result.size() << std::endl;
  }

  // op 23770: put
  db->Put(write_options, "key0741", "value50062");

  // op 23771: get
  status = db->Get(read_options, "key11401", &value);
  if (status.ok()) {
    out << "GET key11401 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23772: get
  status = db->Get(read_options, "key6152", &value);
  if (status.ok()) {
    out << "GET key6152 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23773: put
  db->Put(write_options, "key3310", "value65845");

  // op 23774: put
  db->Put(write_options, "key10151", "value9512");

  // op 23775: put
  db->Put(write_options, "key8558", "value105804");

  // op 23776: delete
  db->Delete(write_options, "key7191");

  // op 23777: get
  status = db->Get(read_options, "key11134", &value);
  if (status.ok()) {
    out << "GET key11134 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23778: put
  db->Put(write_options, "key3192", "value92227");

  // op 23779: put
  db->Put(write_options, "key0192", "value19240");

  // op 23780: put
  db->Put(write_options, "key1940", "value42735");

  // op 23781: get
  status = db->Get(read_options, "key9523", &value);
  if (status.ok()) {
    out << "GET key9523 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23782: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 23783: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2904", "key5317", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2904, key5317) size=" << scan_result.size() << std::endl;
  }

  // op 23784: put
  db->Put(write_options, "key9309", "value17304");

  // op 23785: get
  status = db->Get(read_options, "key4178", &value);
  if (status.ok()) {
    out << "GET key4178 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23786: put
  db->Put(write_options, "key6154", "value89265");

  // op 23787: get
  status = db->Get(read_options, "key3369", &value);
  if (status.ok()) {
    out << "GET key3369 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23788: put
  db->Put(write_options, "key7460", "value55075");

  // op 23789: put
  db->Put(write_options, "key0753", "value51992");

  // op 23790: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 23791: put
  db->Put(write_options, "key4683", "value77119");

  // op 23792: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3941", "key8261", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3941, key8261) size=" << scan_result.size() << std::endl;
  }

  // op 23793: put
  db->Put(write_options, "key10385", "value25890");

  // op 23794: get
  status = db->Get(read_options, "key7186", &value);
  if (status.ok()) {
    out << "GET key7186 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23795: put
  db->Put(write_options, "key8650", "value108222");

  // op 23796: put
  db->Put(write_options, "key9704", "value103195");

  // op 23797: put
  db->Put(write_options, "key11222", "value87400");

  // op 23798: put
  db->Put(write_options, "key10113", "value10130");

  // op 23799: get
  status = db->Get(read_options, "key6084", &value);
  if (status.ok()) {
    out << "GET key6084 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23800: put
  db->Put(write_options, "key11412", "value12725");

  // op 23801: put
  db->Put(write_options, "key3095", "value82923");

  // op 23802: put
  db->Put(write_options, "key1103", "value68880");

  // op 23803: delete
  db->Delete(write_options, "key3550");

  // op 23804: delete
  db->Delete(write_options, "key2052");

  // op 23805: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4863", "key9992", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4863, key9992) size=" << scan_result.size() << std::endl;
  }

  // op 23806: get
  status = db->Get(read_options, "key6592", &value);
  if (status.ok()) {
    out << "GET key6592 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23807: delete
  db->Delete(write_options, "key7217");

  // op 23808: get
  status = db->Get(read_options, "key4825", &value);
  if (status.ok()) {
    out << "GET key4825 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23809: delete_range
  status = db->DeleteRange(write_options, "key0534", "key1125");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 23810: put
  db->Put(write_options, "key5916", "value33921");

  // op 23811: get
  status = db->Get(read_options, "key0912", &value);
  if (status.ok()) {
    out << "GET key0912 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23812: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 23813: put
  db->Put(write_options, "key5840", "value9206");

  // op 23814: delete_range
  status = db->DeleteRange(write_options, "key0526", "key2574");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 23815: put
  db->Put(write_options, "key6363", "value90681");

  // op 23816: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7909", "key7951", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7909, key7951) size=" << scan_result.size() << std::endl;
  }

  // op 23817: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1254", "key8644", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1254, key8644) size=" << scan_result.size() << std::endl;
  }

  // op 23818: delete
  db->Delete(write_options, "key2972");

  // op 23819: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4043", "key8249", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4043, key8249) size=" << scan_result.size() << std::endl;
  }

  // op 23820: put
  db->Put(write_options, "key9450", "value37167");

  // op 23821: get
  status = db->Get(read_options, "key6165", &value);
  if (status.ok()) {
    out << "GET key6165 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23822: delete
  db->Delete(write_options, "key7601");

  // op 23823: get
  status = db->Get(read_options, "key4318", &value);
  if (status.ok()) {
    out << "GET key4318 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23824: put
  db->Put(write_options, "key1975", "value117829");

  // op 23825: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5388", "key8801", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5388, key8801) size=" << scan_result.size() << std::endl;
  }

  // op 23826: get
  status = db->Get(read_options, "key0252", &value);
  if (status.ok()) {
    out << "GET key0252 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23827: put
  db->Put(write_options, "key8701", "value107707");

  // op 23828: put
  db->Put(write_options, "key4906", "value113639");

  // op 23829: put
  db->Put(write_options, "key1221", "value70740");

  // op 23830: delete
  db->Delete(write_options, "key0965");

  // op 23831: delete
  db->Delete(write_options, "key0884");

  // op 23832: get
  status = db->Get(read_options, "key4578", &value);
  if (status.ok()) {
    out << "GET key4578 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23833: put
  db->Put(write_options, "key7001", "value82736");

  // op 23834: put
  db->Put(write_options, "key8173", "value97437");

  // op 23835: get
  status = db->Get(read_options, "key10537", &value);
  if (status.ok()) {
    out << "GET key10537 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23836: delete_range
  status = db->DeleteRange(write_options, "key0071", "key4578");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 23837: get
  status = db->Get(read_options, "key0884", &value);
  if (status.ok()) {
    out << "GET key0884 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23838: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 23839: delete
  db->Delete(write_options, "key10979");

  // op 23840: delete
  db->Delete(write_options, "key2187");

  // op 23841: get
  status = db->Get(read_options, "key8223", &value);
  if (status.ok()) {
    out << "GET key8223 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23842: get
  status = db->Get(read_options, "key10439", &value);
  if (status.ok()) {
    out << "GET key10439 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23843: put
  db->Put(write_options, "key2560", "value61986");

  // op 23844: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3462", "key6131", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3462, key6131) size=" << scan_result.size() << std::endl;
  }

  // op 23845: put
  db->Put(write_options, "key1827", "value97572");

  // op 23846: put
  db->Put(write_options, "key0814", "value111434");

  // op 23847: get
  status = db->Get(read_options, "key2032", &value);
  if (status.ok()) {
    out << "GET key2032 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23848: put
  db->Put(write_options, "key8922", "value12569");

  // op 23849: put
  db->Put(write_options, "key4240", "value6075");

  // op 23850: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4744", "key9997", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4744, key9997) size=" << scan_result.size() << std::endl;
  }

  // op 23851: put
  db->Put(write_options, "key8161", "value77061");

  // op 23852: get
  status = db->Get(read_options, "key2296", &value);
  if (status.ok()) {
    out << "GET key2296 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23853: delete
  db->Delete(write_options, "key9809");

  // op 23854: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2117", "key7875", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2117, key7875) size=" << scan_result.size() << std::endl;
  }

  // op 23855: put
  db->Put(write_options, "key10870", "value42511");

  // op 23856: get
  status = db->Get(read_options, "key6878", &value);
  if (status.ok()) {
    out << "GET key6878 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23857: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2867", "key2951", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2867, key2951) size=" << scan_result.size() << std::endl;
  }

  // op 23858: put
  db->Put(write_options, "key0774", "value4712");

  // op 23859: get
  status = db->Get(read_options, "key9575", &value);
  if (status.ok()) {
    out << "GET key9575 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23860: get
  status = db->Get(read_options, "key9143", &value);
  if (status.ok()) {
    out << "GET key9143 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23861: put
  db->Put(write_options, "key6043", "value62493");

  // op 23862: put
  db->Put(write_options, "key8020", "value44233");

  // op 23863: put
  db->Put(write_options, "key0803", "value103881");

  // op 23864: put
  db->Put(write_options, "key0090", "value5999");

  // op 23865: put
  db->Put(write_options, "key9776", "value71361");

  // op 23866: get
  status = db->Get(read_options, "key3129", &value);
  if (status.ok()) {
    out << "GET key3129 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23867: get
  status = db->Get(read_options, "key4379", &value);
  if (status.ok()) {
    out << "GET key4379 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23868: get
  status = db->Get(read_options, "key9192", &value);
  if (status.ok()) {
    out << "GET key9192 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23869: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2319", "key3906", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2319, key3906) size=" << scan_result.size() << std::endl;
  }

  // op 23870: get
  status = db->Get(read_options, "key6909", &value);
  if (status.ok()) {
    out << "GET key6909 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23871: put
  db->Put(write_options, "key2888", "value1003");

  // op 23872: get
  status = db->Get(read_options, "key4260", &value);
  if (status.ok()) {
    out << "GET key4260 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23873: put
  db->Put(write_options, "key11693", "value15523");

  // op 23874: put
  db->Put(write_options, "key4910", "value98127");

  // op 23875: get
  status = db->Get(read_options, "key3139", &value);
  if (status.ok()) {
    out << "GET key3139 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23876: delete_range
  status = db->DeleteRange(write_options, "key9456", "key10325");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 23877: put
  db->Put(write_options, "key10173", "value36282");

  // op 23878: put
  db->Put(write_options, "key9721", "value4914");

  // op 23879: get
  status = db->Get(read_options, "key7719", &value);
  if (status.ok()) {
    out << "GET key7719 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23880: get
  status = db->Get(read_options, "key9576", &value);
  if (status.ok()) {
    out << "GET key9576 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23881: get
  status = db->Get(read_options, "key7591", &value);
  if (status.ok()) {
    out << "GET key7591 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23882: put
  db->Put(write_options, "key4359", "value34986");

  // op 23883: put
  db->Put(write_options, "key8080", "value60064");

  // op 23884: put
  db->Put(write_options, "key6288", "value58234");

  // op 23885: put
  db->Put(write_options, "key1880", "value77539");

  // op 23886: put
  db->Put(write_options, "key3012", "value99917");

  // op 23887: delete
  db->Delete(write_options, "key1792");

  // op 23888: get
  status = db->Get(read_options, "key0275", &value);
  if (status.ok()) {
    out << "GET key0275 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23889: get
  status = db->Get(read_options, "key8275", &value);
  if (status.ok()) {
    out << "GET key8275 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23890: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9486", "key9697", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9486, key9697) size=" << scan_result.size() << std::endl;
  }

  // op 23891: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3000", "key3611", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3000, key3611) size=" << scan_result.size() << std::endl;
  }

  // op 23892: delete
  db->Delete(write_options, "key9228");

  // op 23893: get
  status = db->Get(read_options, "key1598", &value);
  if (status.ok()) {
    out << "GET key1598 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23894: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4496", "key10100", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4496, key10100) size=" << scan_result.size() << std::endl;
  }

  // op 23895: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0236", "key7545", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0236, key7545) size=" << scan_result.size() << std::endl;
  }

  // op 23896: put
  db->Put(write_options, "key4956", "value107359");

  // op 23897: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0495", "key9987", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0495, key9987) size=" << scan_result.size() << std::endl;
  }

  // op 23898: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0633", "key8575", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0633, key8575) size=" << scan_result.size() << std::endl;
  }

  // op 23899: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 23900: put
  db->Put(write_options, "key4248", "value46549");

  // op 23901: put
  db->Put(write_options, "key10277", "value44897");

  // op 23902: delete_range
  status = db->DeleteRange(write_options, "key4413", "key8980");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 23903: put
  db->Put(write_options, "key6127", "value10054");

  // op 23904: put
  db->Put(write_options, "key8048", "value105005");

  // op 23905: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1639", "key10277", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1639, key10277) size=" << scan_result.size() << std::endl;
  }

  // op 23906: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4072", "key7378", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4072, key7378) size=" << scan_result.size() << std::endl;
  }

  // op 23907: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5488", "key8118", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5488, key8118) size=" << scan_result.size() << std::endl;
  }

  // op 23908: delete
  db->Delete(write_options, "key2029");

  // op 23909: put
  db->Put(write_options, "key6093", "value92865");

  // op 23910: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 23911: delete
  db->Delete(write_options, "key4894");

  // op 23912: put
  db->Put(write_options, "key10334", "value34662");

  // op 23913: delete
  db->Delete(write_options, "key5539");

  // op 23914: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4320", "key5265", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4320, key5265) size=" << scan_result.size() << std::endl;
  }

  // op 23915: get
  status = db->Get(read_options, "key9519", &value);
  if (status.ok()) {
    out << "GET key9519 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23916: delete_range
  status = db->DeleteRange(write_options, "key1154", "key8782");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 23917: put
  db->Put(write_options, "key7893", "value60574");

  // op 23918: put
  db->Put(write_options, "key6779", "value98860");

  // op 23919: put
  db->Put(write_options, "key8638", "value66623");

  // op 23920: put
  db->Put(write_options, "key5078", "value16297");

  // op 23921: get
  status = db->Get(read_options, "key8702", &value);
  if (status.ok()) {
    out << "GET key8702 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23922: delete_range
  status = db->DeleteRange(write_options, "key7610", "key10658");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 23923: get
  status = db->Get(read_options, "key8022", &value);
  if (status.ok()) {
    out << "GET key8022 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23924: put
  db->Put(write_options, "key1080", "value24124");

  // op 23925: put
  db->Put(write_options, "key8376", "value32257");

  // op 23926: put
  db->Put(write_options, "key9884", "value55032");

  // op 23927: put
  db->Put(write_options, "key8588", "value33676");

  // op 23928: put
  db->Put(write_options, "key0611", "value70512");

  // op 23929: put
  db->Put(write_options, "key9270", "value116176");

  // op 23930: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3028", "key6834", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3028, key6834) size=" << scan_result.size() << std::endl;
  }

  // op 23931: get
  status = db->Get(read_options, "key0876", &value);
  if (status.ok()) {
    out << "GET key0876 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23932: get
  status = db->Get(read_options, "key6340", &value);
  if (status.ok()) {
    out << "GET key6340 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23933: put
  db->Put(write_options, "key5198", "value80403");

  // op 23934: put
  db->Put(write_options, "key0497", "value10750");

  // op 23935: delete_range
  status = db->DeleteRange(write_options, "key2372", "key2566");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 23936: put
  db->Put(write_options, "key5449", "value116116");

  // op 23937: get
  status = db->Get(read_options, "key1950", &value);
  if (status.ok()) {
    out << "GET key1950 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23938: delete_range
  status = db->DeleteRange(write_options, "key5013", "key9899");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 23939: put
  db->Put(write_options, "key10786", "value91769");

  // op 23940: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 23941: put
  db->Put(write_options, "key8957", "value104136");

  // op 23942: delete
  db->Delete(write_options, "key1987");

  // op 23943: get
  status = db->Get(read_options, "key2975", &value);
  if (status.ok()) {
    out << "GET key2975 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23944: put
  db->Put(write_options, "key2907", "value100616");

  // op 23945: delete
  db->Delete(write_options, "key6789");

  // op 23946: put
  db->Put(write_options, "key8021", "value94905");

  // op 23947: put
  db->Put(write_options, "key0925", "value100503");

  // op 23948: put
  db->Put(write_options, "key5295", "value68771");

  // op 23949: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7251", "key9585", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7251, key9585) size=" << scan_result.size() << std::endl;
  }

  // op 23950: delete
  db->Delete(write_options, "key10094");

  // op 23951: put
  db->Put(write_options, "key6684", "value92009");

  // op 23952: delete_range
  status = db->DeleteRange(write_options, "key8027", "key10428");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 23953: put
  db->Put(write_options, "key10481", "value29384");

  // op 23954: get
  status = db->Get(read_options, "key4951", &value);
  if (status.ok()) {
    out << "GET key4951 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23955: delete
  db->Delete(write_options, "key7226");

  // op 23956: put
  db->Put(write_options, "key4228", "value68232");

  // op 23957: get
  status = db->Get(read_options, "key10655", &value);
  if (status.ok()) {
    out << "GET key10655 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23958: get
  status = db->Get(read_options, "key0175", &value);
  if (status.ok()) {
    out << "GET key0175 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23959: delete_range
  status = db->DeleteRange(write_options, "key0684", "key3899");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 23960: put
  db->Put(write_options, "key1299", "value114733");

  // op 23961: delete
  db->Delete(write_options, "key6340");

  // op 23962: put
  db->Put(write_options, "key7495", "value55783");

  // op 23963: put
  db->Put(write_options, "key11342", "value3469");

  // op 23964: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2038", "key7708", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2038, key7708) size=" << scan_result.size() << std::endl;
  }

  // op 23965: get
  status = db->Get(read_options, "key2167", &value);
  if (status.ok()) {
    out << "GET key2167 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23966: put
  db->Put(write_options, "key9843", "value65782");

  // op 23967: get
  status = db->Get(read_options, "key11672", &value);
  if (status.ok()) {
    out << "GET key11672 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23968: get
  status = db->Get(read_options, "key0882", &value);
  if (status.ok()) {
    out << "GET key0882 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23969: put
  db->Put(write_options, "key8142", "value59804");

  // op 23970: put
  db->Put(write_options, "key4787", "value97049");

  // op 23971: get
  status = db->Get(read_options, "key8867", &value);
  if (status.ok()) {
    out << "GET key8867 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23972: put
  db->Put(write_options, "key1340", "value67406");

  // op 23973: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4421", "key7804", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4421, key7804) size=" << scan_result.size() << std::endl;
  }

  // op 23974: get
  status = db->Get(read_options, "key1850", &value);
  if (status.ok()) {
    out << "GET key1850 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23975: put
  db->Put(write_options, "key8095", "value15602");

  // op 23976: get
  status = db->Get(read_options, "key11028", &value);
  if (status.ok()) {
    out << "GET key11028 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23977: put
  db->Put(write_options, "key9247", "value3501");

  // op 23978: get
  status = db->Get(read_options, "key2524", &value);
  if (status.ok()) {
    out << "GET key2524 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23979: put
  db->Put(write_options, "key9149", "value116153");

  // op 23980: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4310", "key6336", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4310, key6336) size=" << scan_result.size() << std::endl;
  }

  // op 23981: delete_range
  status = db->DeleteRange(write_options, "key0512", "key9901");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 23982: get
  status = db->Get(read_options, "key8845", &value);
  if (status.ok()) {
    out << "GET key8845 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23983: put
  db->Put(write_options, "key6769", "value71825");

  // op 23984: delete
  db->Delete(write_options, "key3594");

  // op 23985: get
  status = db->Get(read_options, "key9699", &value);
  if (status.ok()) {
    out << "GET key9699 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23986: delete_range
  status = db->DeleteRange(write_options, "key4273", "key7367");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 23987: put
  db->Put(write_options, "key0529", "value35744");

  // op 23988: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1068", "key11292", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1068, key11292) size=" << scan_result.size() << std::endl;
  }

  // op 23989: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 23990: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0590", "key3412", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0590, key3412) size=" << scan_result.size() << std::endl;
  }

  // op 23991: delete
  db->Delete(write_options, "key9379");

  // op 23992: put
  db->Put(write_options, "key2900", "value83118");

  // op 23993: put
  db->Put(write_options, "key9464", "value46005");

  // op 23994: put
  db->Put(write_options, "key11694", "value109077");

  // op 23995: delete_range
  status = db->DeleteRange(write_options, "key3318", "key9340");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 23996: put
  db->Put(write_options, "key0961", "value85550");

  // op 23997: get
  status = db->Get(read_options, "key7224", &value);
  if (status.ok()) {
    out << "GET key7224 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 23998: put
  db->Put(write_options, "key1944", "value112741");

  // op 23999: put
  db->Put(write_options, "key6240", "value12307");

  // op 24000: put
  db->Put(write_options, "key1984", "value32323");

  // op 24001: put
  db->Put(write_options, "key0907", "value105289");

  // op 24002: put
  db->Put(write_options, "key7088", "value37083");

  // op 24003: delete
  db->Delete(write_options, "key7912");

  // op 24004: delete_range
  status = db->DeleteRange(write_options, "key0738", "key11229");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 24005: put
  db->Put(write_options, "key10077", "value46558");

  // op 24006: delete
  db->Delete(write_options, "key11198");

  // op 24007: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 24008: delete
  db->Delete(write_options, "key0313");

  // op 24009: get
  status = db->Get(read_options, "key6382", &value);
  if (status.ok()) {
    out << "GET key6382 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24010: put
  db->Put(write_options, "key10437", "value85975");

  // op 24011: put
  db->Put(write_options, "key7813", "value34191");

  // op 24012: get
  status = db->Get(read_options, "key2259", &value);
  if (status.ok()) {
    out << "GET key2259 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24013: put
  db->Put(write_options, "key8952", "value98474");

  // op 24014: get
  status = db->Get(read_options, "key2435", &value);
  if (status.ok()) {
    out << "GET key2435 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24015: delete
  db->Delete(write_options, "key6372");

  // op 24016: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 24017: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4789", "key10794", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4789, key10794) size=" << scan_result.size() << std::endl;
  }

  // op 24018: delete_range
  status = db->DeleteRange(write_options, "key0717", "key7948");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 24019: delete_range
  status = db->DeleteRange(write_options, "key2866", "key3704");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 24020: put
  db->Put(write_options, "key8715", "value25985");

  // op 24021: put
  db->Put(write_options, "key0260", "value10363");

  // op 24022: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 24023: put
  db->Put(write_options, "key0593", "value85473");

  // op 24024: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9612", "key11582", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9612, key11582) size=" << scan_result.size() << std::endl;
  }

  // op 24025: put
  db->Put(write_options, "key1231", "value91717");

  // op 24026: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5902", "key10810", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5902, key10810) size=" << scan_result.size() << std::endl;
  }

  // op 24027: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0667", "key3911", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0667, key3911) size=" << scan_result.size() << std::endl;
  }

  // op 24028: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2396", "key9650", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2396, key9650) size=" << scan_result.size() << std::endl;
  }

  // op 24029: put
  db->Put(write_options, "key10207", "value94989");

  // op 24030: get
  status = db->Get(read_options, "key1292", &value);
  if (status.ok()) {
    out << "GET key1292 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24031: get
  status = db->Get(read_options, "key2464", &value);
  if (status.ok()) {
    out << "GET key2464 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24032: put
  db->Put(write_options, "key2294", "value88880");

  // op 24033: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7649", "key11597", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7649, key11597) size=" << scan_result.size() << std::endl;
  }

  // op 24034: get
  status = db->Get(read_options, "key4830", &value);
  if (status.ok()) {
    out << "GET key4830 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24035: delete_range
  status = db->DeleteRange(write_options, "key8930", "key11397");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 24036: put
  db->Put(write_options, "key5698", "value28252");

  // op 24037: get
  status = db->Get(read_options, "key1899", &value);
  if (status.ok()) {
    out << "GET key1899 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24038: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6665", "key9230", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6665, key9230) size=" << scan_result.size() << std::endl;
  }

  // op 24039: put
  db->Put(write_options, "key0088", "value45695");

  // op 24040: delete
  db->Delete(write_options, "key5180");

  // op 24041: get
  status = db->Get(read_options, "key11306", &value);
  if (status.ok()) {
    out << "GET key11306 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24042: put
  db->Put(write_options, "key4504", "value18217");

  // op 24043: delete_range
  status = db->DeleteRange(write_options, "key5133", "key11327");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 24044: get
  status = db->Get(read_options, "key10033", &value);
  if (status.ok()) {
    out << "GET key10033 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24045: put
  db->Put(write_options, "key4567", "value77828");

  // op 24046: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 24047: get
  status = db->Get(read_options, "key5990", &value);
  if (status.ok()) {
    out << "GET key5990 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24048: put
  db->Put(write_options, "key8948", "value116927");

  // op 24049: get
  status = db->Get(read_options, "key10205", &value);
  if (status.ok()) {
    out << "GET key10205 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24050: put
  db->Put(write_options, "key5103", "value58184");

  // op 24051: delete
  db->Delete(write_options, "key7665");

  // op 24052: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0654", "key4775", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0654, key4775) size=" << scan_result.size() << std::endl;
  }

  // op 24053: get
  status = db->Get(read_options, "key11433", &value);
  if (status.ok()) {
    out << "GET key11433 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24054: put
  db->Put(write_options, "key10108", "value94399");

  // op 24055: put
  db->Put(write_options, "key4485", "value114073");

  // op 24056: put
  db->Put(write_options, "key5445", "value105978");

  // op 24057: get
  status = db->Get(read_options, "key8183", &value);
  if (status.ok()) {
    out << "GET key8183 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24058: delete
  db->Delete(write_options, "key2466");

  // op 24059: put
  db->Put(write_options, "key1425", "value90558");

  // op 24060: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3400", "key9434", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3400, key9434) size=" << scan_result.size() << std::endl;
  }

  // op 24061: put
  db->Put(write_options, "key2884", "value81378");

  // op 24062: get
  status = db->Get(read_options, "key6357", &value);
  if (status.ok()) {
    out << "GET key6357 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24063: get
  status = db->Get(read_options, "key1655", &value);
  if (status.ok()) {
    out << "GET key1655 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24064: delete_range
  status = db->DeleteRange(write_options, "key5926", "key10532");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 24065: put
  db->Put(write_options, "key1321", "value62650");

  // op 24066: get
  status = db->Get(read_options, "key4460", &value);
  if (status.ok()) {
    out << "GET key4460 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24067: get
  status = db->Get(read_options, "key7534", &value);
  if (status.ok()) {
    out << "GET key7534 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24068: put
  db->Put(write_options, "key6328", "value50121");

  // op 24069: put
  db->Put(write_options, "key2796", "value64226");

  // op 24070: delete
  db->Delete(write_options, "key1081");

  // op 24071: put
  db->Put(write_options, "key8615", "value103768");

  // op 24072: get
  status = db->Get(read_options, "key1754", &value);
  if (status.ok()) {
    out << "GET key1754 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24073: put
  db->Put(write_options, "key8985", "value5390");

  // op 24074: delete_range
  status = db->DeleteRange(write_options, "key2899", "key3780");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 24075: put
  db->Put(write_options, "key4003", "value96154");

  // op 24076: put
  db->Put(write_options, "key4553", "value62014");

  // op 24077: put
  db->Put(write_options, "key9650", "value44897");

  // op 24078: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 24079: put
  db->Put(write_options, "key1461", "value13331");

  // op 24080: get
  status = db->Get(read_options, "key8030", &value);
  if (status.ok()) {
    out << "GET key8030 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24081: get
  status = db->Get(read_options, "key9173", &value);
  if (status.ok()) {
    out << "GET key9173 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24082: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1968", "key8066", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1968, key8066) size=" << scan_result.size() << std::endl;
  }

  // op 24083: put
  db->Put(write_options, "key3802", "value89836");

  // op 24084: delete_range
  status = db->DeleteRange(write_options, "key8484", "key10670");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 24085: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 24086: put
  db->Put(write_options, "key0351", "value87336");

  // op 24087: delete
  db->Delete(write_options, "key8655");

  // op 24088: put
  db->Put(write_options, "key4088", "value29854");

  // op 24089: put
  db->Put(write_options, "key2198", "value14160");

  // op 24090: put
  db->Put(write_options, "key0768", "value115277");

  // op 24091: get
  status = db->Get(read_options, "key6577", &value);
  if (status.ok()) {
    out << "GET key6577 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24092: get
  status = db->Get(read_options, "key5650", &value);
  if (status.ok()) {
    out << "GET key5650 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24093: get
  status = db->Get(read_options, "key9926", &value);
  if (status.ok()) {
    out << "GET key9926 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24094: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0382", "key11308", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0382, key11308) size=" << scan_result.size() << std::endl;
  }

  // op 24095: delete_range
  status = db->DeleteRange(write_options, "key2539", "key4345");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 24096: put
  db->Put(write_options, "key2593", "value8066");

  // op 24097: put
  db->Put(write_options, "key9871", "value51691");

  // op 24098: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 24099: delete_range
  status = db->DeleteRange(write_options, "key0030", "key0717");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 24100: delete
  db->Delete(write_options, "key5972");

  // op 24101: delete
  db->Delete(write_options, "key5633");

  // op 24102: delete
  db->Delete(write_options, "key5486");

  // op 24103: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1733", "key4680", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1733, key4680) size=" << scan_result.size() << std::endl;
  }

  // op 24104: get
  status = db->Get(read_options, "key1046", &value);
  if (status.ok()) {
    out << "GET key1046 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24105: put
  db->Put(write_options, "key2351", "value93836");

  // op 24106: get
  status = db->Get(read_options, "key6851", &value);
  if (status.ok()) {
    out << "GET key6851 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24107: get
  status = db->Get(read_options, "key0953", &value);
  if (status.ok()) {
    out << "GET key0953 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24108: delete_range
  status = db->DeleteRange(write_options, "key2423", "key5189");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 24109: put
  db->Put(write_options, "key8640", "value81608");

  // op 24110: delete_range
  status = db->DeleteRange(write_options, "key4913", "key10472");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 24111: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5514", "key8364", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5514, key8364) size=" << scan_result.size() << std::endl;
  }

  // op 24112: get
  status = db->Get(read_options, "key9088", &value);
  if (status.ok()) {
    out << "GET key9088 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24113: put
  db->Put(write_options, "key2712", "value24453");

  // op 24114: delete
  db->Delete(write_options, "key7553");

  // op 24115: get
  status = db->Get(read_options, "key5699", &value);
  if (status.ok()) {
    out << "GET key5699 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24116: put
  db->Put(write_options, "key1574", "value89000");

  // op 24117: delete
  db->Delete(write_options, "key6984");

  // op 24118: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0360", "key10651", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0360, key10651) size=" << scan_result.size() << std::endl;
  }

  // op 24119: put
  db->Put(write_options, "key10702", "value64156");

  // op 24120: put
  db->Put(write_options, "key3400", "value23097");

  // op 24121: delete_range
  status = db->DeleteRange(write_options, "key5857", "key9721");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 24122: put
  db->Put(write_options, "key0157", "value14636");

  // op 24123: put
  db->Put(write_options, "key10546", "value67025");

  // op 24124: put
  db->Put(write_options, "key5803", "value61801");

  // op 24125: put
  db->Put(write_options, "key7315", "value76374");

  // op 24126: get
  status = db->Get(read_options, "key2472", &value);
  if (status.ok()) {
    out << "GET key2472 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24127: delete_range
  status = db->DeleteRange(write_options, "key0153", "key1240");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 24128: put
  db->Put(write_options, "key5861", "value10680");

  // op 24129: put
  db->Put(write_options, "key4616", "value85679");

  // op 24130: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2940", "key6353", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2940, key6353) size=" << scan_result.size() << std::endl;
  }

  // op 24131: get
  status = db->Get(read_options, "key9667", &value);
  if (status.ok()) {
    out << "GET key9667 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24132: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7558", "key8989", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7558, key8989) size=" << scan_result.size() << std::endl;
  }

  // op 24133: put
  db->Put(write_options, "key0283", "value76718");

  // op 24134: get
  status = db->Get(read_options, "key6161", &value);
  if (status.ok()) {
    out << "GET key6161 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24135: put
  db->Put(write_options, "key6882", "value105554");

  // op 24136: get
  status = db->Get(read_options, "key7487", &value);
  if (status.ok()) {
    out << "GET key7487 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24137: get
  status = db->Get(read_options, "key6955", &value);
  if (status.ok()) {
    out << "GET key6955 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24138: put
  db->Put(write_options, "key9813", "value25280");

  // op 24139: put
  db->Put(write_options, "key9335", "value25269");

  // op 24140: put
  db->Put(write_options, "key0417", "value23055");

  // op 24141: put
  db->Put(write_options, "key8253", "value82381");

  // op 24142: put
  db->Put(write_options, "key0337", "value97560");

  // op 24143: put
  db->Put(write_options, "key6338", "value2356");

  // op 24144: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 24145: put
  db->Put(write_options, "key7301", "value83403");

  // op 24146: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8679", "key9929", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8679, key9929) size=" << scan_result.size() << std::endl;
  }

  // op 24147: get
  status = db->Get(read_options, "key2574", &value);
  if (status.ok()) {
    out << "GET key2574 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24148: delete_range
  status = db->DeleteRange(write_options, "key0006", "key3951");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 24149: get
  status = db->Get(read_options, "key8701", &value);
  if (status.ok()) {
    out << "GET key8701 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24150: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0146", "key11710", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0146, key11710) size=" << scan_result.size() << std::endl;
  }

  // op 24151: put
  db->Put(write_options, "key11470", "value109396");

  // op 24152: put
  db->Put(write_options, "key2364", "value52202");

  // op 24153: put
  db->Put(write_options, "key8219", "value107127");

  // op 24154: delete
  db->Delete(write_options, "key8153");

  // op 24155: put
  db->Put(write_options, "key10546", "value39467");

  // op 24156: get
  status = db->Get(read_options, "key3950", &value);
  if (status.ok()) {
    out << "GET key3950 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24157: put
  db->Put(write_options, "key4421", "value75976");

  // op 24158: get
  status = db->Get(read_options, "key5166", &value);
  if (status.ok()) {
    out << "GET key5166 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24159: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 24160: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4899", "key11481", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4899, key11481) size=" << scan_result.size() << std::endl;
  }

  // op 24161: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3435", "key4176", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3435, key4176) size=" << scan_result.size() << std::endl;
  }

  // op 24162: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1521", "key3608", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1521, key3608) size=" << scan_result.size() << std::endl;
  }

  // op 24163: get
  status = db->Get(read_options, "key2562", &value);
  if (status.ok()) {
    out << "GET key2562 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24164: put
  db->Put(write_options, "key6795", "value57769");

  // op 24165: delete
  db->Delete(write_options, "key7486");

  // op 24166: put
  db->Put(write_options, "key10801", "value105765");

  // op 24167: put
  db->Put(write_options, "key3643", "value79095");

  // op 24168: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6295", "key8879", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6295, key8879) size=" << scan_result.size() << std::endl;
  }

  // op 24169: delete_range
  status = db->DeleteRange(write_options, "key5813", "key7677");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 24170: delete_range
  status = db->DeleteRange(write_options, "key6643", "key11445");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 24171: put
  db->Put(write_options, "key7866", "value57837");

  // op 24172: put
  db->Put(write_options, "key8655", "value79993");

  // op 24173: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7588", "key9311", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7588, key9311) size=" << scan_result.size() << std::endl;
  }

  // op 24174: put
  db->Put(write_options, "key7129", "value31329");

  // op 24175: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 24176: get
  status = db->Get(read_options, "key6172", &value);
  if (status.ok()) {
    out << "GET key6172 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24177: put
  db->Put(write_options, "key11503", "value23115");

  // op 24178: delete_range
  status = db->DeleteRange(write_options, "key2884", "key3641");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 24179: put
  db->Put(write_options, "key9169", "value103777");

  // op 24180: delete_range
  status = db->DeleteRange(write_options, "key4833", "key5266");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 24181: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 24182: put
  db->Put(write_options, "key5417", "value110458");

  // op 24183: get
  status = db->Get(read_options, "key3012", &value);
  if (status.ok()) {
    out << "GET key3012 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24184: delete
  db->Delete(write_options, "key0091");

  // op 24185: delete_range
  status = db->DeleteRange(write_options, "key0929", "key2397");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 24186: put
  db->Put(write_options, "key0733", "value2375");

  // op 24187: put
  db->Put(write_options, "key4293", "value111235");

  // op 24188: get
  status = db->Get(read_options, "key4717", &value);
  if (status.ok()) {
    out << "GET key4717 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24189: get
  status = db->Get(read_options, "key6983", &value);
  if (status.ok()) {
    out << "GET key6983 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24190: delete_range
  status = db->DeleteRange(write_options, "key0327", "key10918");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 24191: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3146", "key8123", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3146, key8123) size=" << scan_result.size() << std::endl;
  }

  // op 24192: delete
  db->Delete(write_options, "key9829");

  // op 24193: put
  db->Put(write_options, "key5989", "value109160");

  // op 24194: put
  db->Put(write_options, "key1568", "value60960");

  // op 24195: delete
  db->Delete(write_options, "key4606");

  // op 24196: put
  db->Put(write_options, "key1068", "value55289");

  // op 24197: get
  status = db->Get(read_options, "key6621", &value);
  if (status.ok()) {
    out << "GET key6621 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24198: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2720", "key4103", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2720, key4103) size=" << scan_result.size() << std::endl;
  }

  // op 24199: get
  status = db->Get(read_options, "key7609", &value);
  if (status.ok()) {
    out << "GET key7609 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24200: put
  db->Put(write_options, "key8219", "value6798");

  // op 24201: delete_range
  status = db->DeleteRange(write_options, "key8729", "key10222");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 24202: put
  db->Put(write_options, "key3395", "value26179");

  // op 24203: put
  db->Put(write_options, "key7357", "value101175");

  // op 24204: put
  db->Put(write_options, "key10603", "value31534");

  // op 24205: get
  status = db->Get(read_options, "key1430", &value);
  if (status.ok()) {
    out << "GET key1430 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24206: put
  db->Put(write_options, "key3669", "value113166");

  // op 24207: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7764", "key9603", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7764, key9603) size=" << scan_result.size() << std::endl;
  }

  // op 24208: get
  status = db->Get(read_options, "key9386", &value);
  if (status.ok()) {
    out << "GET key9386 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24209: get
  status = db->Get(read_options, "key10103", &value);
  if (status.ok()) {
    out << "GET key10103 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24210: delete
  db->Delete(write_options, "key8546");

  // op 24211: get
  status = db->Get(read_options, "key8581", &value);
  if (status.ok()) {
    out << "GET key8581 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24212: get
  status = db->Get(read_options, "key3991", &value);
  if (status.ok()) {
    out << "GET key3991 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24213: get
  status = db->Get(read_options, "key0642", &value);
  if (status.ok()) {
    out << "GET key0642 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24214: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3463", "key10872", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3463, key10872) size=" << scan_result.size() << std::endl;
  }

  // op 24215: put
  db->Put(write_options, "key3546", "value107090");

  // op 24216: get
  status = db->Get(read_options, "key11363", &value);
  if (status.ok()) {
    out << "GET key11363 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24217: get
  status = db->Get(read_options, "key5998", &value);
  if (status.ok()) {
    out << "GET key5998 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24218: delete
  db->Delete(write_options, "key11460");

  // op 24219: put
  db->Put(write_options, "key7922", "value43196");

  // op 24220: delete
  db->Delete(write_options, "key4828");

  // op 24221: put
  db->Put(write_options, "key6086", "value49037");

  // op 24222: get
  status = db->Get(read_options, "key4039", &value);
  if (status.ok()) {
    out << "GET key4039 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24223: put
  db->Put(write_options, "key7774", "value31797");

  // op 24224: get
  status = db->Get(read_options, "key1254", &value);
  if (status.ok()) {
    out << "GET key1254 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24225: put
  db->Put(write_options, "key7623", "value4501");

  // op 24226: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4533", "key6597", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4533, key6597) size=" << scan_result.size() << std::endl;
  }

  // op 24227: put
  db->Put(write_options, "key5339", "value58858");

  // op 24228: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5086", "key9048", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5086, key9048) size=" << scan_result.size() << std::endl;
  }

  // op 24229: put
  db->Put(write_options, "key7691", "value73651");

  // op 24230: put
  db->Put(write_options, "key3879", "value9060");

  // op 24231: put
  db->Put(write_options, "key1848", "value76592");

  // op 24232: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0251", "key5799", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0251, key5799) size=" << scan_result.size() << std::endl;
  }

  // op 24233: put
  db->Put(write_options, "key2591", "value102040");

  // op 24234: put
  db->Put(write_options, "key0298", "value94797");

  // op 24235: put
  db->Put(write_options, "key5018", "value88117");

  // op 24236: delete_range
  status = db->DeleteRange(write_options, "key1399", "key11283");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 24237: put
  db->Put(write_options, "key2975", "value92384");

  // op 24238: put
  db->Put(write_options, "key5617", "value83587");

  // op 24239: delete_range
  status = db->DeleteRange(write_options, "key4388", "key5082");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 24240: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 24241: get
  status = db->Get(read_options, "key3219", &value);
  if (status.ok()) {
    out << "GET key3219 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24242: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0009", "key11369", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0009, key11369) size=" << scan_result.size() << std::endl;
  }

  // op 24243: put
  db->Put(write_options, "key2511", "value35527");

  // op 24244: get
  status = db->Get(read_options, "key2496", &value);
  if (status.ok()) {
    out << "GET key2496 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24245: delete
  db->Delete(write_options, "key3129");

  // op 24246: delete_range
  status = db->DeleteRange(write_options, "key8013", "key11032");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 24247: delete
  db->Delete(write_options, "key2196");

  // op 24248: put
  db->Put(write_options, "key8473", "value107757");

  // op 24249: put
  db->Put(write_options, "key5922", "value117310");

  // op 24250: put
  db->Put(write_options, "key2899", "value50406");

  // op 24251: get
  status = db->Get(read_options, "key6997", &value);
  if (status.ok()) {
    out << "GET key6997 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24252: get
  status = db->Get(read_options, "key9788", &value);
  if (status.ok()) {
    out << "GET key9788 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24253: put
  db->Put(write_options, "key5169", "value14186");

  // op 24254: put
  db->Put(write_options, "key3847", "value92976");

  // op 24255: put
  db->Put(write_options, "key0260", "value12412");

  // op 24256: delete
  db->Delete(write_options, "key1260");

  // op 24257: delete
  db->Delete(write_options, "key7472");

  // op 24258: put
  db->Put(write_options, "key2478", "value77459");

  // op 24259: put
  db->Put(write_options, "key10325", "value44998");

  // op 24260: delete_range
  status = db->DeleteRange(write_options, "key7467", "key8764");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 24261: put
  db->Put(write_options, "key11417", "value12107");

  // op 24262: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2954", "key9763", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2954, key9763) size=" << scan_result.size() << std::endl;
  }

  // op 24263: put
  db->Put(write_options, "key9261", "value16272");

  // op 24264: get
  status = db->Get(read_options, "key10149", &value);
  if (status.ok()) {
    out << "GET key10149 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24265: put
  db->Put(write_options, "key8975", "value92707");

  // op 24266: delete_range
  status = db->DeleteRange(write_options, "key0007", "key2442");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 24267: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3243", "key4235", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3243, key4235) size=" << scan_result.size() << std::endl;
  }

  // op 24268: delete
  db->Delete(write_options, "key10549");

  // op 24269: put
  db->Put(write_options, "key9596", "value36464");

  // op 24270: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7242", "key8641", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7242, key8641) size=" << scan_result.size() << std::endl;
  }

  // op 24271: put
  db->Put(write_options, "key9124", "value92853");

  // op 24272: put
  db->Put(write_options, "key8362", "value20061");

  // op 24273: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4572", "key6077", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4572, key6077) size=" << scan_result.size() << std::endl;
  }

  // op 24274: put
  db->Put(write_options, "key5265", "value2791");

  // op 24275: put
  db->Put(write_options, "key10496", "value113811");

  // op 24276: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5938", "key7972", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5938, key7972) size=" << scan_result.size() << std::endl;
  }

  // op 24277: delete_range
  status = db->DeleteRange(write_options, "key3574", "key6120");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 24278: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 24279: put
  db->Put(write_options, "key11294", "value78007");

  // op 24280: delete
  db->Delete(write_options, "key6468");

  // op 24281: delete_range
  status = db->DeleteRange(write_options, "key5806", "key11481");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 24282: put
  db->Put(write_options, "key8908", "value18140");

  // op 24283: delete_range
  status = db->DeleteRange(write_options, "key7310", "key7536");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 24284: put
  db->Put(write_options, "key10769", "value51022");

  // op 24285: put
  db->Put(write_options, "key11474", "value70288");

  // op 24286: delete_range
  status = db->DeleteRange(write_options, "key2394", "key10569");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 24287: put
  db->Put(write_options, "key11572", "value76660");

  // op 24288: put
  db->Put(write_options, "key0934", "value36480");

  // op 24289: get
  status = db->Get(read_options, "key10214", &value);
  if (status.ok()) {
    out << "GET key10214 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24290: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 24291: put
  db->Put(write_options, "key4043", "value100481");

  // op 24292: put
  db->Put(write_options, "key1638", "value88606");

  // op 24293: get
  status = db->Get(read_options, "key1313", &value);
  if (status.ok()) {
    out << "GET key1313 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24294: put
  db->Put(write_options, "key4511", "value11429");

  // op 24295: put
  db->Put(write_options, "key11571", "value8098");

  // op 24296: put
  db->Put(write_options, "key7452", "value105014");

  // op 24297: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6332", "key9617", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6332, key9617) size=" << scan_result.size() << std::endl;
  }

  // op 24298: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3880", "key7227", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3880, key7227) size=" << scan_result.size() << std::endl;
  }

  // op 24299: delete
  db->Delete(write_options, "key7279");

  // op 24300: get
  status = db->Get(read_options, "key7492", &value);
  if (status.ok()) {
    out << "GET key7492 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24301: put
  db->Put(write_options, "key0826", "value96650");

  // op 24302: put
  db->Put(write_options, "key7328", "value87282");

  // op 24303: put
  db->Put(write_options, "key2716", "value75205");

  // op 24304: delete
  db->Delete(write_options, "key1530");

  // op 24305: get
  status = db->Get(read_options, "key3460", &value);
  if (status.ok()) {
    out << "GET key3460 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24306: put
  db->Put(write_options, "key0594", "value30718");

  // op 24307: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0646", "key1470", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0646, key1470) size=" << scan_result.size() << std::endl;
  }

  // op 24308: delete
  db->Delete(write_options, "key10344");

  // op 24309: delete_range
  status = db->DeleteRange(write_options, "key2801", "key8231");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 24310: delete_range
  status = db->DeleteRange(write_options, "key2547", "key6540");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 24311: get
  status = db->Get(read_options, "key7190", &value);
  if (status.ok()) {
    out << "GET key7190 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24312: put
  db->Put(write_options, "key11267", "value24790");

  // op 24313: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 24314: put
  db->Put(write_options, "key8281", "value48142");

  // op 24315: get
  status = db->Get(read_options, "key2267", &value);
  if (status.ok()) {
    out << "GET key2267 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24316: put
  db->Put(write_options, "key5613", "value25623");

  // op 24317: delete
  db->Delete(write_options, "key5878");

  // op 24318: get
  status = db->Get(read_options, "key8824", &value);
  if (status.ok()) {
    out << "GET key8824 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24319: put
  db->Put(write_options, "key3742", "value97791");

  // op 24320: put
  db->Put(write_options, "key8199", "value95066");

  // op 24321: put
  db->Put(write_options, "key1325", "value112129");

  // op 24322: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 24323: put
  db->Put(write_options, "key1389", "value24123");

  // op 24324: put
  db->Put(write_options, "key11271", "value50161");

  // op 24325: delete_range
  status = db->DeleteRange(write_options, "key0821", "key10553");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 24326: delete_range
  status = db->DeleteRange(write_options, "key6403", "key8536");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 24327: get
  status = db->Get(read_options, "key10866", &value);
  if (status.ok()) {
    out << "GET key10866 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24328: delete
  db->Delete(write_options, "key0437");

  // op 24329: put
  db->Put(write_options, "key9570", "value111958");

  // op 24330: get
  status = db->Get(read_options, "key7467", &value);
  if (status.ok()) {
    out << "GET key7467 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24331: delete
  db->Delete(write_options, "key5982");

  // op 24332: get
  status = db->Get(read_options, "key7600", &value);
  if (status.ok()) {
    out << "GET key7600 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24333: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3345", "key10220", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3345, key10220) size=" << scan_result.size() << std::endl;
  }

  // op 24334: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4694", "key11584", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4694, key11584) size=" << scan_result.size() << std::endl;
  }

  // op 24335: put
  db->Put(write_options, "key6991", "value116090");

  // op 24336: put
  db->Put(write_options, "key2532", "value59333");

  // op 24337: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5943", "key8346", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5943, key8346) size=" << scan_result.size() << std::endl;
  }

  // op 24338: get
  status = db->Get(read_options, "key0113", &value);
  if (status.ok()) {
    out << "GET key0113 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24339: delete_range
  status = db->DeleteRange(write_options, "key8958", "key10037");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 24340: get
  status = db->Get(read_options, "key0425", &value);
  if (status.ok()) {
    out << "GET key0425 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24341: get
  status = db->Get(read_options, "key1095", &value);
  if (status.ok()) {
    out << "GET key1095 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24342: put
  db->Put(write_options, "key9918", "value99665");

  // op 24343: put
  db->Put(write_options, "key0687", "value24206");

  // op 24344: put
  db->Put(write_options, "key6561", "value118284");

  // op 24345: put
  db->Put(write_options, "key11614", "value72540");

  // op 24346: delete_range
  status = db->DeleteRange(write_options, "key1305", "key9033");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 24347: put
  db->Put(write_options, "key3763", "value95769");

  // op 24348: put
  db->Put(write_options, "key5526", "value4656");

  // op 24349: put
  db->Put(write_options, "key6647", "value36829");

  // op 24350: delete
  db->Delete(write_options, "key11401");

  // op 24351: get
  status = db->Get(read_options, "key2574", &value);
  if (status.ok()) {
    out << "GET key2574 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24352: put
  db->Put(write_options, "key7937", "value61866");

  // op 24353: put
  db->Put(write_options, "key3414", "value14247");

  // op 24354: put
  db->Put(write_options, "key7517", "value115760");

  // op 24355: get
  status = db->Get(read_options, "key7010", &value);
  if (status.ok()) {
    out << "GET key7010 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24356: get
  status = db->Get(read_options, "key6437", &value);
  if (status.ok()) {
    out << "GET key6437 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24357: put
  db->Put(write_options, "key0763", "value4589");

  // op 24358: put
  db->Put(write_options, "key8034", "value35452");

  // op 24359: put
  db->Put(write_options, "key11665", "value33923");

  // op 24360: put
  db->Put(write_options, "key3997", "value99524");

  // op 24361: put
  db->Put(write_options, "key5048", "value83133");

  // op 24362: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 24363: put
  db->Put(write_options, "key6241", "value19692");

  // op 24364: get
  status = db->Get(read_options, "key1193", &value);
  if (status.ok()) {
    out << "GET key1193 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24365: delete
  db->Delete(write_options, "key6487");

  // op 24366: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2470", "key10854", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2470, key10854) size=" << scan_result.size() << std::endl;
  }

  // op 24367: put
  db->Put(write_options, "key3363", "value56877");

  // op 24368: put
  db->Put(write_options, "key10081", "value111710");

  // op 24369: delete_range
  status = db->DeleteRange(write_options, "key0858", "key10862");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 24370: put
  db->Put(write_options, "key3022", "value33974");

  // op 24371: put
  db->Put(write_options, "key9185", "value1889");

  // op 24372: get
  status = db->Get(read_options, "key10084", &value);
  if (status.ok()) {
    out << "GET key10084 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24373: get
  status = db->Get(read_options, "key4805", &value);
  if (status.ok()) {
    out << "GET key4805 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24374: put
  db->Put(write_options, "key2602", "value20160");

  // op 24375: put
  db->Put(write_options, "key4825", "value106572");

  // op 24376: get
  status = db->Get(read_options, "key1694", &value);
  if (status.ok()) {
    out << "GET key1694 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24377: delete
  db->Delete(write_options, "key0904");

  // op 24378: put
  db->Put(write_options, "key7634", "value104141");

  // op 24379: put
  db->Put(write_options, "key8953", "value69820");

  // op 24380: put
  db->Put(write_options, "key4898", "value37177");

  // op 24381: get
  status = db->Get(read_options, "key8020", &value);
  if (status.ok()) {
    out << "GET key8020 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24382: delete
  db->Delete(write_options, "key1084");

  // op 24383: delete_range
  status = db->DeleteRange(write_options, "key0046", "key10459");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 24384: delete
  db->Delete(write_options, "key0776");

  // op 24385: put
  db->Put(write_options, "key9837", "value47156");

  // op 24386: delete_range
  status = db->DeleteRange(write_options, "key2124", "key6016");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 24387: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0012", "key4832", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0012, key4832) size=" << scan_result.size() << std::endl;
  }

  // op 24388: put
  db->Put(write_options, "key4731", "value59476");

  // op 24389: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4756", "key10141", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4756, key10141) size=" << scan_result.size() << std::endl;
  }

  // op 24390: get
  status = db->Get(read_options, "key3929", &value);
  if (status.ok()) {
    out << "GET key3929 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24391: delete_range
  status = db->DeleteRange(write_options, "key4984", "key5506");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 24392: put
  db->Put(write_options, "key3113", "value29077");

  // op 24393: get
  status = db->Get(read_options, "key11231", &value);
  if (status.ok()) {
    out << "GET key11231 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24394: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 24395: delete_range
  status = db->DeleteRange(write_options, "key3928", "key11462");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 24396: get
  status = db->Get(read_options, "key2492", &value);
  if (status.ok()) {
    out << "GET key2492 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24397: delete
  db->Delete(write_options, "key9941");

  // op 24398: delete_range
  status = db->DeleteRange(write_options, "key5931", "key10008");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 24399: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6599", "key8410", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6599, key8410) size=" << scan_result.size() << std::endl;
  }

  // op 24400: put
  db->Put(write_options, "key11118", "value32217");

  // op 24401: delete
  db->Delete(write_options, "key1770");

  // op 24402: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0355", "key5146", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0355, key5146) size=" << scan_result.size() << std::endl;
  }

  // op 24403: get
  status = db->Get(read_options, "key9469", &value);
  if (status.ok()) {
    out << "GET key9469 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24404: put
  db->Put(write_options, "key1326", "value42864");

  // op 24405: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 24406: get
  status = db->Get(read_options, "key4479", &value);
  if (status.ok()) {
    out << "GET key4479 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24407: put
  db->Put(write_options, "key8170", "value100146");

  // op 24408: put
  db->Put(write_options, "key11641", "value108023");

  // op 24409: get
  status = db->Get(read_options, "key4038", &value);
  if (status.ok()) {
    out << "GET key4038 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24410: get
  status = db->Get(read_options, "key0841", &value);
  if (status.ok()) {
    out << "GET key0841 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24411: get
  status = db->Get(read_options, "key3264", &value);
  if (status.ok()) {
    out << "GET key3264 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24412: get
  status = db->Get(read_options, "key0094", &value);
  if (status.ok()) {
    out << "GET key0094 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24413: put
  db->Put(write_options, "key11595", "value67353");

  // op 24414: put
  db->Put(write_options, "key1715", "value50601");

  // op 24415: get
  status = db->Get(read_options, "key9434", &value);
  if (status.ok()) {
    out << "GET key9434 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24416: delete_range
  status = db->DeleteRange(write_options, "key1373", "key3789");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 24417: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 24418: put
  db->Put(write_options, "key4493", "value96471");

  // op 24419: delete_range
  status = db->DeleteRange(write_options, "key9877", "key10208");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 24420: delete_range
  status = db->DeleteRange(write_options, "key1359", "key7551");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 24421: put
  db->Put(write_options, "key2460", "value58924");

  // op 24422: get
  status = db->Get(read_options, "key1176", &value);
  if (status.ok()) {
    out << "GET key1176 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24423: put
  db->Put(write_options, "key0782", "value104840");

  // op 24424: put
  db->Put(write_options, "key11641", "value51934");

  // op 24425: delete
  db->Delete(write_options, "key0925");

  // op 24426: delete
  db->Delete(write_options, "key8553");

  // op 24427: put
  db->Put(write_options, "key4630", "value28355");

  // op 24428: put
  db->Put(write_options, "key2577", "value41654");

  // op 24429: delete
  db->Delete(write_options, "key10133");

  // op 24430: put
  db->Put(write_options, "key10903", "value39256");

  // op 24431: get
  status = db->Get(read_options, "key11719", &value);
  if (status.ok()) {
    out << "GET key11719 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24432: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 24433: delete_range
  status = db->DeleteRange(write_options, "key1729", "key9253");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 24434: put
  db->Put(write_options, "key1920", "value69090");

  // op 24435: delete_range
  status = db->DeleteRange(write_options, "key0473", "key10476");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 24436: put
  db->Put(write_options, "key9084", "value61198");

  // op 24437: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0096", "key5460", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0096, key5460) size=" << scan_result.size() << std::endl;
  }

  // op 24438: put
  db->Put(write_options, "key5915", "value86328");

  // op 24439: delete_range
  status = db->DeleteRange(write_options, "key4214", "key6012");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 24440: get
  status = db->Get(read_options, "key2931", &value);
  if (status.ok()) {
    out << "GET key2931 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24441: put
  db->Put(write_options, "key8023", "value39994");

  // op 24442: put
  db->Put(write_options, "key2526", "value40135");

  // op 24443: put
  db->Put(write_options, "key11543", "value108282");

  // op 24444: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2065", "key2919", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2065, key2919) size=" << scan_result.size() << std::endl;
  }

  // op 24445: put
  db->Put(write_options, "key7062", "value103511");

  // op 24446: delete_range
  status = db->DeleteRange(write_options, "key0366", "key7450");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 24447: get
  status = db->Get(read_options, "key9524", &value);
  if (status.ok()) {
    out << "GET key9524 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24448: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 24449: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0393", "key5287", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0393, key5287) size=" << scan_result.size() << std::endl;
  }

  // op 24450: delete_range
  status = db->DeleteRange(write_options, "key6543", "key9938");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 24451: put
  db->Put(write_options, "key0688", "value43903");

  // op 24452: put
  db->Put(write_options, "key3738", "value15935");

  // op 24453: put
  db->Put(write_options, "key0803", "value90230");

  // op 24454: delete
  db->Delete(write_options, "key11372");

  // op 24455: put
  db->Put(write_options, "key1795", "value71295");

  // op 24456: delete
  db->Delete(write_options, "key10065");

  // op 24457: put
  db->Put(write_options, "key1181", "value70239");

  // op 24458: scan
  scan_result.clear();
  status = db->Scan(read_options, "key11342", "key11555", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key11342, key11555) size=" << scan_result.size() << std::endl;
  }

  // op 24459: get
  status = db->Get(read_options, "key8794", &value);
  if (status.ok()) {
    out << "GET key8794 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24460: delete
  db->Delete(write_options, "key2536");

  // op 24461: put
  db->Put(write_options, "key7615", "value63244");

  // op 24462: delete
  db->Delete(write_options, "key5578");

  // op 24463: put
  db->Put(write_options, "key6377", "value44475");

  // op 24464: delete
  db->Delete(write_options, "key0360");

  // op 24465: put
  db->Put(write_options, "key1417", "value48508");

  // op 24466: delete
  db->Delete(write_options, "key5399");

  // op 24467: put
  db->Put(write_options, "key8642", "value14060");

  // op 24468: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4932", "key9201", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4932, key9201) size=" << scan_result.size() << std::endl;
  }

  // op 24469: put
  db->Put(write_options, "key4447", "value88464");

  // op 24470: get
  status = db->Get(read_options, "key6558", &value);
  if (status.ok()) {
    out << "GET key6558 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24471: get
  status = db->Get(read_options, "key7565", &value);
  if (status.ok()) {
    out << "GET key7565 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24472: put
  db->Put(write_options, "key10284", "value58375");

  // op 24473: put
  db->Put(write_options, "key0888", "value109313");

  // op 24474: delete
  db->Delete(write_options, "key2127");

  // op 24475: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7792", "key8459", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7792, key8459) size=" << scan_result.size() << std::endl;
  }

  // op 24476: put
  db->Put(write_options, "key5117", "value109213");

  // op 24477: get
  status = db->Get(read_options, "key0904", &value);
  if (status.ok()) {
    out << "GET key0904 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24478: delete
  db->Delete(write_options, "key0231");

  // op 24479: put
  db->Put(write_options, "key3230", "value72758");

  // op 24480: put
  db->Put(write_options, "key5842", "value61923");

  // op 24481: delete
  db->Delete(write_options, "key6883");

  // op 24482: get
  status = db->Get(read_options, "key5935", &value);
  if (status.ok()) {
    out << "GET key5935 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24483: put
  db->Put(write_options, "key10198", "value107199");

  // op 24484: put
  db->Put(write_options, "key0497", "value29151");

  // op 24485: put
  db->Put(write_options, "key7295", "value107824");

  // op 24486: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3794", "key5655", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3794, key5655) size=" << scan_result.size() << std::endl;
  }

  // op 24487: put
  db->Put(write_options, "key11687", "value53947");

  // op 24488: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 24489: put
  db->Put(write_options, "key7149", "value109181");

  // op 24490: put
  db->Put(write_options, "key3854", "value86273");

  // op 24491: put
  db->Put(write_options, "key0089", "value113727");

  // op 24492: put
  db->Put(write_options, "key6580", "value39216");

  // op 24493: get
  status = db->Get(read_options, "key2461", &value);
  if (status.ok()) {
    out << "GET key2461 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24494: get
  status = db->Get(read_options, "key2549", &value);
  if (status.ok()) {
    out << "GET key2549 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24495: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 24496: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0779", "key6919", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0779, key6919) size=" << scan_result.size() << std::endl;
  }

  // op 24497: get
  status = db->Get(read_options, "key2143", &value);
  if (status.ok()) {
    out << "GET key2143 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24498: get
  status = db->Get(read_options, "key2887", &value);
  if (status.ok()) {
    out << "GET key2887 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24499: put
  db->Put(write_options, "key11221", "value109736");

  // op 24500: get
  status = db->Get(read_options, "key2845", &value);
  if (status.ok()) {
    out << "GET key2845 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24501: put
  db->Put(write_options, "key4257", "value60660");

  // op 24502: put
  db->Put(write_options, "key6498", "value107111");

  // op 24503: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1015", "key9870", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1015, key9870) size=" << scan_result.size() << std::endl;
  }

  // op 24504: get
  status = db->Get(read_options, "key4431", &value);
  if (status.ok()) {
    out << "GET key4431 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24505: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5312", "key8346", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5312, key8346) size=" << scan_result.size() << std::endl;
  }

  // op 24506: put
  db->Put(write_options, "key9706", "value68081");

  // op 24507: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 24508: put
  db->Put(write_options, "key7156", "value61498");

  // op 24509: put
  db->Put(write_options, "key10423", "value36174");

  // op 24510: get
  status = db->Get(read_options, "key3820", &value);
  if (status.ok()) {
    out << "GET key3820 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24511: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0816", "key4208", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0816, key4208) size=" << scan_result.size() << std::endl;
  }

  // op 24512: put
  db->Put(write_options, "key10262", "value40939");

  // op 24513: put
  db->Put(write_options, "key1938", "value68266");

  // op 24514: get
  status = db->Get(read_options, "key10891", &value);
  if (status.ok()) {
    out << "GET key10891 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24515: get
  status = db->Get(read_options, "key6662", &value);
  if (status.ok()) {
    out << "GET key6662 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24516: put
  db->Put(write_options, "key4935", "value32859");

  // op 24517: get
  status = db->Get(read_options, "key10322", &value);
  if (status.ok()) {
    out << "GET key10322 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24518: put
  db->Put(write_options, "key10606", "value37794");

  // op 24519: get
  status = db->Get(read_options, "key10183", &value);
  if (status.ok()) {
    out << "GET key10183 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24520: get
  status = db->Get(read_options, "key11389", &value);
  if (status.ok()) {
    out << "GET key11389 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24521: get
  status = db->Get(read_options, "key8101", &value);
  if (status.ok()) {
    out << "GET key8101 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24522: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0508", "key4251", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0508, key4251) size=" << scan_result.size() << std::endl;
  }

  // op 24523: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 24524: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1275", "key5876", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1275, key5876) size=" << scan_result.size() << std::endl;
  }

  // op 24525: put
  db->Put(write_options, "key8905", "value56969");

  // op 24526: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0800", "key5234", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0800, key5234) size=" << scan_result.size() << std::endl;
  }

  // op 24527: delete
  db->Delete(write_options, "key2260");

  // op 24528: put
  db->Put(write_options, "key9926", "value71828");

  // op 24529: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2828", "key8205", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2828, key8205) size=" << scan_result.size() << std::endl;
  }

  // op 24530: put
  db->Put(write_options, "key2305", "value31865");

  // op 24531: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2156", "key4333", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2156, key4333) size=" << scan_result.size() << std::endl;
  }

  // op 24532: put
  db->Put(write_options, "key1554", "value67790");

  // op 24533: delete
  db->Delete(write_options, "key4380");

  // op 24534: delete_range
  status = db->DeleteRange(write_options, "key4189", "key4773");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 24535: get
  status = db->Get(read_options, "key5054", &value);
  if (status.ok()) {
    out << "GET key5054 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24536: put
  db->Put(write_options, "key7490", "value98906");

  // op 24537: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8465", "key8575", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8465, key8575) size=" << scan_result.size() << std::endl;
  }

  // op 24538: put
  db->Put(write_options, "key5214", "value63570");

  // op 24539: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1436", "key4227", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1436, key4227) size=" << scan_result.size() << std::endl;
  }

  // op 24540: put
  db->Put(write_options, "key7424", "value107412");

  // op 24541: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3267", "key11384", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3267, key11384) size=" << scan_result.size() << std::endl;
  }

  // op 24542: get
  status = db->Get(read_options, "key11727", &value);
  if (status.ok()) {
    out << "GET key11727 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24543: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2685", "key9608", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2685, key9608) size=" << scan_result.size() << std::endl;
  }

  // op 24544: get
  status = db->Get(read_options, "key8697", &value);
  if (status.ok()) {
    out << "GET key8697 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24545: get
  status = db->Get(read_options, "key6217", &value);
  if (status.ok()) {
    out << "GET key6217 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24546: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1194", "key4729", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1194, key4729) size=" << scan_result.size() << std::endl;
  }

  // op 24547: get
  status = db->Get(read_options, "key0939", &value);
  if (status.ok()) {
    out << "GET key0939 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24548: get
  status = db->Get(read_options, "key9867", &value);
  if (status.ok()) {
    out << "GET key9867 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24549: get
  status = db->Get(read_options, "key4581", &value);
  if (status.ok()) {
    out << "GET key4581 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24550: put
  db->Put(write_options, "key2836", "value74568");

  // op 24551: delete
  db->Delete(write_options, "key0775");

  // op 24552: delete
  db->Delete(write_options, "key2619");

  // op 24553: get
  status = db->Get(read_options, "key4448", &value);
  if (status.ok()) {
    out << "GET key4448 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24554: delete
  db->Delete(write_options, "key11301");

  // op 24555: get
  status = db->Get(read_options, "key4787", &value);
  if (status.ok()) {
    out << "GET key4787 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24556: put
  db->Put(write_options, "key4332", "value32671");

  // op 24557: get
  status = db->Get(read_options, "key1988", &value);
  if (status.ok()) {
    out << "GET key1988 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24558: put
  db->Put(write_options, "key10668", "value97985");

  // op 24559: get
  status = db->Get(read_options, "key7563", &value);
  if (status.ok()) {
    out << "GET key7563 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24560: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0528", "key11622", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0528, key11622) size=" << scan_result.size() << std::endl;
  }

  // op 24561: get
  status = db->Get(read_options, "key9223", &value);
  if (status.ok()) {
    out << "GET key9223 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24562: get
  status = db->Get(read_options, "key6421", &value);
  if (status.ok()) {
    out << "GET key6421 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24563: get
  status = db->Get(read_options, "key11012", &value);
  if (status.ok()) {
    out << "GET key11012 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24564: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0308", "key2437", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0308, key2437) size=" << scan_result.size() << std::endl;
  }

  // op 24565: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0189", "key9168", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0189, key9168) size=" << scan_result.size() << std::endl;
  }

  // op 24566: delete
  db->Delete(write_options, "key10347");

  // op 24567: put
  db->Put(write_options, "key0040", "value77066");

  // op 24568: delete
  db->Delete(write_options, "key7067");

  // op 24569: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2377", "key7526", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2377, key7526) size=" << scan_result.size() << std::endl;
  }

  // op 24570: delete_range
  status = db->DeleteRange(write_options, "key1507", "key1715");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 24571: put
  db->Put(write_options, "key2897", "value45419");

  // op 24572: get
  status = db->Get(read_options, "key2182", &value);
  if (status.ok()) {
    out << "GET key2182 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24573: delete
  db->Delete(write_options, "key1403");

  // op 24574: put
  db->Put(write_options, "key4329", "value73224");

  // op 24575: put
  db->Put(write_options, "key3082", "value59570");

  // op 24576: put
  db->Put(write_options, "key6875", "value30933");

  // op 24577: put
  db->Put(write_options, "key4367", "value25484");

  // op 24578: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 24579: delete
  db->Delete(write_options, "key5364");

  // op 24580: put
  db->Put(write_options, "key5216", "value36144");

  // op 24581: put
  db->Put(write_options, "key9071", "value23814");

  // op 24582: get
  status = db->Get(read_options, "key8562", &value);
  if (status.ok()) {
    out << "GET key8562 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24583: put
  db->Put(write_options, "key5978", "value97218");

  // op 24584: get
  status = db->Get(read_options, "key4743", &value);
  if (status.ok()) {
    out << "GET key4743 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24585: put
  db->Put(write_options, "key2269", "value15424");

  // op 24586: put
  db->Put(write_options, "key2822", "value47077");

  // op 24587: delete_range
  status = db->DeleteRange(write_options, "key5586", "key11323");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 24588: delete
  db->Delete(write_options, "key0837");

  // op 24589: delete
  db->Delete(write_options, "key3404");

  // op 24590: put
  db->Put(write_options, "key6663", "value87712");

  // op 24591: delete
  db->Delete(write_options, "key3310");

  // op 24592: put
  db->Put(write_options, "key4041", "value96879");

  // op 24593: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 24594: delete
  db->Delete(write_options, "key3715");

  // op 24595: delete_range
  status = db->DeleteRange(write_options, "key6318", "key9262");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 24596: put
  db->Put(write_options, "key6550", "value46521");

  // op 24597: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1313", "key2568", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1313, key2568) size=" << scan_result.size() << std::endl;
  }

  // op 24598: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3269", "key7877", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3269, key7877) size=" << scan_result.size() << std::endl;
  }

  // op 24599: delete
  db->Delete(write_options, "key1730");

  // op 24600: put
  db->Put(write_options, "key10416", "value9335");

  // op 24601: put
  db->Put(write_options, "key5314", "value10979");

  // op 24602: delete
  db->Delete(write_options, "key7577");

  // op 24603: get
  status = db->Get(read_options, "key0133", &value);
  if (status.ok()) {
    out << "GET key0133 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24604: put
  db->Put(write_options, "key9744", "value43172");

  // op 24605: put
  db->Put(write_options, "key6548", "value108590");

  // op 24606: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 24607: put
  db->Put(write_options, "key10031", "value56480");

  // op 24608: delete_range
  status = db->DeleteRange(write_options, "key0938", "key11130");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 24609: get
  status = db->Get(read_options, "key3928", &value);
  if (status.ok()) {
    out << "GET key3928 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24610: delete_range
  status = db->DeleteRange(write_options, "key4346", "key4558");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 24611: get
  status = db->Get(read_options, "key6253", &value);
  if (status.ok()) {
    out << "GET key6253 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24612: delete_range
  status = db->DeleteRange(write_options, "key5706", "key6326");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 24613: delete_range
  status = db->DeleteRange(write_options, "key2494", "key4260");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 24614: get
  status = db->Get(read_options, "key6249", &value);
  if (status.ok()) {
    out << "GET key6249 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24615: get
  status = db->Get(read_options, "key9454", &value);
  if (status.ok()) {
    out << "GET key9454 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24616: put
  db->Put(write_options, "key5376", "value107753");

  // op 24617: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2829", "key6818", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2829, key6818) size=" << scan_result.size() << std::endl;
  }

  // op 24618: delete
  db->Delete(write_options, "key7210");

  // op 24619: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0421", "key4243", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0421, key4243) size=" << scan_result.size() << std::endl;
  }

  // op 24620: get
  status = db->Get(read_options, "key11541", &value);
  if (status.ok()) {
    out << "GET key11541 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24621: put
  db->Put(write_options, "key0047", "value110224");

  // op 24622: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0793", "key4065", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0793, key4065) size=" << scan_result.size() << std::endl;
  }

  // op 24623: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1532", "key4408", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1532, key4408) size=" << scan_result.size() << std::endl;
  }

  // op 24624: delete
  db->Delete(write_options, "key3293");

  // op 24625: put
  db->Put(write_options, "key7542", "value72410");

  // op 24626: put
  db->Put(write_options, "key4301", "value91454");

  // op 24627: get
  status = db->Get(read_options, "key6594", &value);
  if (status.ok()) {
    out << "GET key6594 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24628: delete_range
  status = db->DeleteRange(write_options, "key1087", "key2356");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 24629: get
  status = db->Get(read_options, "key1951", &value);
  if (status.ok()) {
    out << "GET key1951 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24630: delete_range
  status = db->DeleteRange(write_options, "key1898", "key10901");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 24631: put
  db->Put(write_options, "key11259", "value94105");

  // op 24632: get
  status = db->Get(read_options, "key2950", &value);
  if (status.ok()) {
    out << "GET key2950 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24633: delete_range
  status = db->DeleteRange(write_options, "key2678", "key5290");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 24634: delete
  db->Delete(write_options, "key0567");

  // op 24635: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 24636: get
  status = db->Get(read_options, "key6640", &value);
  if (status.ok()) {
    out << "GET key6640 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24637: put
  db->Put(write_options, "key2209", "value32907");

  // op 24638: put
  db->Put(write_options, "key4135", "value56267");

  // op 24639: put
  db->Put(write_options, "key1050", "value45172");

  // op 24640: put
  db->Put(write_options, "key0712", "value7422");

  // op 24641: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 24642: delete
  db->Delete(write_options, "key2517");

  // op 24643: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4529", "key9497", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4529, key9497) size=" << scan_result.size() << std::endl;
  }

  // op 24644: delete_range
  status = db->DeleteRange(write_options, "key4802", "key9566");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 24645: put
  db->Put(write_options, "key9770", "value2219");

  // op 24646: get
  status = db->Get(read_options, "key3433", &value);
  if (status.ok()) {
    out << "GET key3433 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24647: get
  status = db->Get(read_options, "key0007", &value);
  if (status.ok()) {
    out << "GET key0007 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24648: put
  db->Put(write_options, "key8340", "value96663");

  // op 24649: get
  status = db->Get(read_options, "key0596", &value);
  if (status.ok()) {
    out << "GET key0596 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24650: put
  db->Put(write_options, "key8213", "value40744");

  // op 24651: put
  db->Put(write_options, "key3328", "value33325");

  // op 24652: put
  db->Put(write_options, "key5238", "value106669");

  // op 24653: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1513", "key4168", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1513, key4168) size=" << scan_result.size() << std::endl;
  }

  // op 24654: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 24655: put
  db->Put(write_options, "key0432", "value87350");

  // op 24656: delete_range
  status = db->DeleteRange(write_options, "key1502", "key9197");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 24657: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 24658: get
  status = db->Get(read_options, "key0884", &value);
  if (status.ok()) {
    out << "GET key0884 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24659: get
  status = db->Get(read_options, "key1849", &value);
  if (status.ok()) {
    out << "GET key1849 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24660: put
  db->Put(write_options, "key7062", "value79814");

  // op 24661: put
  db->Put(write_options, "key9309", "value20780");

  // op 24662: put
  db->Put(write_options, "key8733", "value10901");

  // op 24663: put
  db->Put(write_options, "key10272", "value46343");

  // op 24664: put
  db->Put(write_options, "key3455", "value71467");

  // op 24665: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0050", "key9695", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0050, key9695) size=" << scan_result.size() << std::endl;
  }

  // op 24666: delete_range
  status = db->DeleteRange(write_options, "key2868", "key5202");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 24667: put
  db->Put(write_options, "key9135", "value93505");

  // op 24668: put
  db->Put(write_options, "key2903", "value65345");

  // op 24669: put
  db->Put(write_options, "key11416", "value25652");

  // op 24670: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1787", "key2750", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1787, key2750) size=" << scan_result.size() << std::endl;
  }

  // op 24671: put
  db->Put(write_options, "key6063", "value6021");

  // op 24672: get
  status = db->Get(read_options, "key2758", &value);
  if (status.ok()) {
    out << "GET key2758 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24673: put
  db->Put(write_options, "key5858", "value32818");

  // op 24674: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 24675: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4503", "key9810", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4503, key9810) size=" << scan_result.size() << std::endl;
  }

  // op 24676: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3799", "key7558", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3799, key7558) size=" << scan_result.size() << std::endl;
  }

  // op 24677: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5347", "key7810", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5347, key7810) size=" << scan_result.size() << std::endl;
  }

  // op 24678: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2929", "key3858", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2929, key3858) size=" << scan_result.size() << std::endl;
  }

  // op 24679: put
  db->Put(write_options, "key7400", "value27240");

  // op 24680: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3972", "key7072", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3972, key7072) size=" << scan_result.size() << std::endl;
  }

  // op 24681: put
  db->Put(write_options, "key1443", "value59869");

  // op 24682: put
  db->Put(write_options, "key4388", "value98036");

  // op 24683: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10247", "key11243", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10247, key11243) size=" << scan_result.size() << std::endl;
  }

  // op 24684: get
  status = db->Get(read_options, "key8525", &value);
  if (status.ok()) {
    out << "GET key8525 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24685: delete_range
  status = db->DeleteRange(write_options, "key7092", "key11181");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 24686: put
  db->Put(write_options, "key9958", "value70407");

  // op 24687: delete
  db->Delete(write_options, "key5029");

  // op 24688: put
  db->Put(write_options, "key5674", "value62186");

  // op 24689: delete_range
  status = db->DeleteRange(write_options, "key3949", "key8479");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 24690: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0438", "key1695", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0438, key1695) size=" << scan_result.size() << std::endl;
  }

  // op 24691: put
  db->Put(write_options, "key4130", "value18993");

  // op 24692: put
  db->Put(write_options, "key9886", "value22179");

  // op 24693: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8071", "key11247", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8071, key11247) size=" << scan_result.size() << std::endl;
  }

  // op 24694: put
  db->Put(write_options, "key3411", "value21370");

  // op 24695: delete
  db->Delete(write_options, "key11619");

  // op 24696: get
  status = db->Get(read_options, "key3597", &value);
  if (status.ok()) {
    out << "GET key3597 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24697: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4963", "key6098", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4963, key6098) size=" << scan_result.size() << std::endl;
  }

  // op 24698: put
  db->Put(write_options, "key8217", "value10967");

  // op 24699: put
  db->Put(write_options, "key4882", "value56932");

  // op 24700: put
  db->Put(write_options, "key4739", "value115631");

  // op 24701: put
  db->Put(write_options, "key11155", "value77674");

  // op 24702: delete_range
  status = db->DeleteRange(write_options, "key2607", "key10787");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 24703: put
  db->Put(write_options, "key5552", "value105937");

  // op 24704: put
  db->Put(write_options, "key6511", "value98239");

  // op 24705: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4060", "key6009", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4060, key6009) size=" << scan_result.size() << std::endl;
  }

  // op 24706: put
  db->Put(write_options, "key4902", "value76292");

  // op 24707: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3480", "key6456", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3480, key6456) size=" << scan_result.size() << std::endl;
  }

  // op 24708: put
  db->Put(write_options, "key4513", "value70999");

  // op 24709: put
  db->Put(write_options, "key5281", "value79630");

  // op 24710: put
  db->Put(write_options, "key2103", "value111704");

  // op 24711: put
  db->Put(write_options, "key3812", "value44387");

  // op 24712: put
  db->Put(write_options, "key5655", "value2642");

  // op 24713: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1810", "key5123", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1810, key5123) size=" << scan_result.size() << std::endl;
  }

  // op 24714: put
  db->Put(write_options, "key2427", "value65262");

  // op 24715: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 24716: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2528", "key3521", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2528, key3521) size=" << scan_result.size() << std::endl;
  }

  // op 24717: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2690", "key5388", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2690, key5388) size=" << scan_result.size() << std::endl;
  }

  // op 24718: put
  db->Put(write_options, "key10714", "value5422");

  // op 24719: delete_range
  status = db->DeleteRange(write_options, "key3921", "key6959");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 24720: delete_range
  status = db->DeleteRange(write_options, "key1301", "key2573");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 24721: delete
  db->Delete(write_options, "key7114");

  // op 24722: get
  status = db->Get(read_options, "key3910", &value);
  if (status.ok()) {
    out << "GET key3910 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24723: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1165", "key1481", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1165, key1481) size=" << scan_result.size() << std::endl;
  }

  // op 24724: get
  status = db->Get(read_options, "key7783", &value);
  if (status.ok()) {
    out << "GET key7783 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24725: put
  db->Put(write_options, "key6322", "value92550");

  // op 24726: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2407", "key10131", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2407, key10131) size=" << scan_result.size() << std::endl;
  }

  // op 24727: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6295", "key6998", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6295, key6998) size=" << scan_result.size() << std::endl;
  }

  // op 24728: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 24729: delete_range
  status = db->DeleteRange(write_options, "key0807", "key9404");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 24730: put
  db->Put(write_options, "key7330", "value46627");

  // op 24731: delete
  db->Delete(write_options, "key10694");

  // op 24732: delete_range
  status = db->DeleteRange(write_options, "key4122", "key6684");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 24733: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 24734: delete_range
  status = db->DeleteRange(write_options, "key6261", "key9216");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 24735: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5470", "key11145", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5470, key11145) size=" << scan_result.size() << std::endl;
  }

  // op 24736: delete_range
  status = db->DeleteRange(write_options, "key10047", "key10346");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 24737: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 24738: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1791", "key8409", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1791, key8409) size=" << scan_result.size() << std::endl;
  }

  // op 24739: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0016", "key8690", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0016, key8690) size=" << scan_result.size() << std::endl;
  }

  // op 24740: put
  db->Put(write_options, "key0267", "value92615");

  // op 24741: delete_range
  status = db->DeleteRange(write_options, "key3885", "key5518");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 24742: delete_range
  status = db->DeleteRange(write_options, "key6626", "key7163");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 24743: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 24744: put
  db->Put(write_options, "key4607", "value35214");

  // op 24745: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4144", "key5265", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4144, key5265) size=" << scan_result.size() << std::endl;
  }

  // op 24746: put
  db->Put(write_options, "key4144", "value5784");

  // op 24747: delete
  db->Delete(write_options, "key8709");

  // op 24748: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0255", "key4237", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0255, key4237) size=" << scan_result.size() << std::endl;
  }

  // op 24749: put
  db->Put(write_options, "key6454", "value54862");

  // op 24750: get
  status = db->Get(read_options, "key8723", &value);
  if (status.ok()) {
    out << "GET key8723 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24751: put
  db->Put(write_options, "key9389", "value14426");

  // op 24752: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5326", "key10105", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5326, key10105) size=" << scan_result.size() << std::endl;
  }

  // op 24753: put
  db->Put(write_options, "key7810", "value79835");

  // op 24754: get
  status = db->Get(read_options, "key3153", &value);
  if (status.ok()) {
    out << "GET key3153 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24755: put
  db->Put(write_options, "key9717", "value44832");

  // op 24756: put
  db->Put(write_options, "key3132", "value3765");

  // op 24757: put
  db->Put(write_options, "key5375", "value6900");

  // op 24758: get
  status = db->Get(read_options, "key1210", &value);
  if (status.ok()) {
    out << "GET key1210 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24759: put
  db->Put(write_options, "key0163", "value50132");

  // op 24760: put
  db->Put(write_options, "key8894", "value93345");

  // op 24761: put
  db->Put(write_options, "key0709", "value29645");

  // op 24762: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10691", "key11571", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10691, key11571) size=" << scan_result.size() << std::endl;
  }

  // op 24763: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 24764: put
  db->Put(write_options, "key10928", "value86674");

  // op 24765: get
  status = db->Get(read_options, "key11519", &value);
  if (status.ok()) {
    out << "GET key11519 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24766: get
  status = db->Get(read_options, "key4631", &value);
  if (status.ok()) {
    out << "GET key4631 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24767: put
  db->Put(write_options, "key6522", "value97090");

  // op 24768: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 24769: put
  db->Put(write_options, "key7112", "value47714");

  // op 24770: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 24771: put
  db->Put(write_options, "key9158", "value52642");

  // op 24772: delete_range
  status = db->DeleteRange(write_options, "key8094", "key10091");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 24773: put
  db->Put(write_options, "key8111", "value108011");

  // op 24774: get
  status = db->Get(read_options, "key5438", &value);
  if (status.ok()) {
    out << "GET key5438 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24775: get
  status = db->Get(read_options, "key8170", &value);
  if (status.ok()) {
    out << "GET key8170 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24776: put
  db->Put(write_options, "key11165", "value16796");

  // op 24777: delete_range
  status = db->DeleteRange(write_options, "key4760", "key5941");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 24778: get
  status = db->Get(read_options, "key3078", &value);
  if (status.ok()) {
    out << "GET key3078 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24779: put
  db->Put(write_options, "key3066", "value56485");

  // op 24780: get
  status = db->Get(read_options, "key1628", &value);
  if (status.ok()) {
    out << "GET key1628 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24781: delete_range
  status = db->DeleteRange(write_options, "key8868", "key10377");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 24782: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5612", "key7789", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5612, key7789) size=" << scan_result.size() << std::endl;
  }

  // op 24783: delete_range
  status = db->DeleteRange(write_options, "key3325", "key6989");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 24784: delete
  db->Delete(write_options, "key2136");

  // op 24785: put
  db->Put(write_options, "key2925", "value69754");

  // op 24786: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2321", "key4824", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2321, key4824) size=" << scan_result.size() << std::endl;
  }

  // op 24787: put
  db->Put(write_options, "key5539", "value12848");

  // op 24788: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 24789: get
  status = db->Get(read_options, "key4398", &value);
  if (status.ok()) {
    out << "GET key4398 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24790: put
  db->Put(write_options, "key7069", "value72257");

  // op 24791: put
  db->Put(write_options, "key10197", "value32985");

  // op 24792: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 24793: get
  status = db->Get(read_options, "key2267", &value);
  if (status.ok()) {
    out << "GET key2267 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24794: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6092", "key6267", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6092, key6267) size=" << scan_result.size() << std::endl;
  }

  // op 24795: put
  db->Put(write_options, "key9077", "value85888");

  // op 24796: get
  status = db->Get(read_options, "key3473", &value);
  if (status.ok()) {
    out << "GET key3473 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24797: put
  db->Put(write_options, "key11211", "value50029");

  // op 24798: put
  db->Put(write_options, "key7395", "value9672");

  // op 24799: delete
  db->Delete(write_options, "key8782");

  // op 24800: put
  db->Put(write_options, "key10098", "value53994");

  // op 24801: put
  db->Put(write_options, "key8908", "value75034");

  // op 24802: put
  db->Put(write_options, "key10707", "value114743");

  // op 24803: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0374", "key4425", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0374, key4425) size=" << scan_result.size() << std::endl;
  }

  // op 24804: get
  status = db->Get(read_options, "key8152", &value);
  if (status.ok()) {
    out << "GET key8152 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24805: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3731", "key5705", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3731, key5705) size=" << scan_result.size() << std::endl;
  }

  // op 24806: delete
  db->Delete(write_options, "key9655");

  // op 24807: get
  status = db->Get(read_options, "key10896", &value);
  if (status.ok()) {
    out << "GET key10896 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24808: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4674", "key6276", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4674, key6276) size=" << scan_result.size() << std::endl;
  }

  // op 24809: delete_range
  status = db->DeleteRange(write_options, "key1025", "key10617");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 24810: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5545", "key8669", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5545, key8669) size=" << scan_result.size() << std::endl;
  }

  // op 24811: delete
  db->Delete(write_options, "key1847");

  // op 24812: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 24813: get
  status = db->Get(read_options, "key9809", &value);
  if (status.ok()) {
    out << "GET key9809 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24814: put
  db->Put(write_options, "key9731", "value52561");

  // op 24815: get
  status = db->Get(read_options, "key0956", &value);
  if (status.ok()) {
    out << "GET key0956 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24816: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6431", "key7913", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6431, key7913) size=" << scan_result.size() << std::endl;
  }

  // op 24817: put
  db->Put(write_options, "key10988", "value59829");

  // op 24818: get
  status = db->Get(read_options, "key11342", &value);
  if (status.ok()) {
    out << "GET key11342 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24819: put
  db->Put(write_options, "key7857", "value6978");

  // op 24820: get
  status = db->Get(read_options, "key6747", &value);
  if (status.ok()) {
    out << "GET key6747 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24821: put
  db->Put(write_options, "key7360", "value103694");

  // op 24822: get
  status = db->Get(read_options, "key3094", &value);
  if (status.ok()) {
    out << "GET key3094 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24823: get
  status = db->Get(read_options, "key2029", &value);
  if (status.ok()) {
    out << "GET key2029 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24824: delete_range
  status = db->DeleteRange(write_options, "key0771", "key9119");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 24825: delete
  db->Delete(write_options, "key1296");

  // op 24826: get
  status = db->Get(read_options, "key5514", &value);
  if (status.ok()) {
    out << "GET key5514 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24827: put
  db->Put(write_options, "key7690", "value12388");

  // op 24828: delete
  db->Delete(write_options, "key2512");

  // op 24829: put
  db->Put(write_options, "key10201", "value85732");

  // op 24830: get
  status = db->Get(read_options, "key1212", &value);
  if (status.ok()) {
    out << "GET key1212 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24831: put
  db->Put(write_options, "key9551", "value8994");

  // op 24832: delete_range
  status = db->DeleteRange(write_options, "key1841", "key10759");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 24833: put
  db->Put(write_options, "key0166", "value71833");

  // op 24834: put
  db->Put(write_options, "key5459", "value59754");

  // op 24835: delete
  db->Delete(write_options, "key1889");

  // op 24836: delete
  db->Delete(write_options, "key2708");

  // op 24837: delete_range
  status = db->DeleteRange(write_options, "key2950", "key7149");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 24838: delete_range
  status = db->DeleteRange(write_options, "key6407", "key9000");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 24839: get
  status = db->Get(read_options, "key4044", &value);
  if (status.ok()) {
    out << "GET key4044 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24840: delete_range
  status = db->DeleteRange(write_options, "key2440", "key4536");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 24841: put
  db->Put(write_options, "key9015", "value38246");

  // op 24842: put
  db->Put(write_options, "key7444", "value4377");

  // op 24843: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8166", "key8570", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8166, key8570) size=" << scan_result.size() << std::endl;
  }

  // op 24844: delete_range
  status = db->DeleteRange(write_options, "key10341", "key10634");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 24845: delete
  db->Delete(write_options, "key6299");

  // op 24846: put
  db->Put(write_options, "key1940", "value46094");

  // op 24847: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3602", "key7967", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3602, key7967) size=" << scan_result.size() << std::endl;
  }

  // op 24848: get
  status = db->Get(read_options, "key0504", &value);
  if (status.ok()) {
    out << "GET key0504 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24849: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 24850: delete
  db->Delete(write_options, "key1974");

  // op 24851: put
  db->Put(write_options, "key10364", "value11263");

  // op 24852: get
  status = db->Get(read_options, "key6421", &value);
  if (status.ok()) {
    out << "GET key6421 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24853: get
  status = db->Get(read_options, "key8829", &value);
  if (status.ok()) {
    out << "GET key8829 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24854: put
  db->Put(write_options, "key5750", "value87586");

  // op 24855: put
  db->Put(write_options, "key2015", "value113010");

  // op 24856: put
  db->Put(write_options, "key10177", "value76191");

  // op 24857: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0544", "key2570", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0544, key2570) size=" << scan_result.size() << std::endl;
  }

  // op 24858: delete_range
  status = db->DeleteRange(write_options, "key0605", "key3541");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 24859: get
  status = db->Get(read_options, "key7507", &value);
  if (status.ok()) {
    out << "GET key7507 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24860: delete
  db->Delete(write_options, "key8409");

  // op 24861: put
  db->Put(write_options, "key7318", "value9703");

  // op 24862: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9343", "key9437", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9343, key9437) size=" << scan_result.size() << std::endl;
  }

  // op 24863: delete_range
  status = db->DeleteRange(write_options, "key2472", "key3815");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 24864: get
  status = db->Get(read_options, "key4379", &value);
  if (status.ok()) {
    out << "GET key4379 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24865: get
  status = db->Get(read_options, "key5355", &value);
  if (status.ok()) {
    out << "GET key5355 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24866: put
  db->Put(write_options, "key4519", "value22687");

  // op 24867: delete
  db->Delete(write_options, "key10076");

  // op 24868: delete
  db->Delete(write_options, "key4740");

  // op 24869: put
  db->Put(write_options, "key6325", "value107406");

  // op 24870: put
  db->Put(write_options, "key0725", "value41745");

  // op 24871: delete_range
  status = db->DeleteRange(write_options, "key3497", "key11177");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 24872: delete
  db->Delete(write_options, "key5003");

  // op 24873: get
  status = db->Get(read_options, "key9824", &value);
  if (status.ok()) {
    out << "GET key9824 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24874: get
  status = db->Get(read_options, "key3496", &value);
  if (status.ok()) {
    out << "GET key3496 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24875: put
  db->Put(write_options, "key2136", "value2169");

  // op 24876: delete
  db->Delete(write_options, "key0460");

  // op 24877: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1206", "key9869", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1206, key9869) size=" << scan_result.size() << std::endl;
  }

  // op 24878: put
  db->Put(write_options, "key3661", "value103858");

  // op 24879: put
  db->Put(write_options, "key10312", "value100628");

  // op 24880: get
  status = db->Get(read_options, "key7758", &value);
  if (status.ok()) {
    out << "GET key7758 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24881: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7745", "key8824", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7745, key8824) size=" << scan_result.size() << std::endl;
  }

  // op 24882: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0271", "key5477", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0271, key5477) size=" << scan_result.size() << std::endl;
  }

  // op 24883: put
  db->Put(write_options, "key4829", "value32921");

  // op 24884: put
  db->Put(write_options, "key10544", "value29699");

  // op 24885: put
  db->Put(write_options, "key2156", "value98071");

  // op 24886: get
  status = db->Get(read_options, "key4086", &value);
  if (status.ok()) {
    out << "GET key4086 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24887: get
  status = db->Get(read_options, "key1547", &value);
  if (status.ok()) {
    out << "GET key1547 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24888: put
  db->Put(write_options, "key0043", "value91435");

  // op 24889: get
  status = db->Get(read_options, "key0664", &value);
  if (status.ok()) {
    out << "GET key0664 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24890: delete_range
  status = db->DeleteRange(write_options, "key4324", "key6693");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 24891: put
  db->Put(write_options, "key10906", "value6404");

  // op 24892: get
  status = db->Get(read_options, "key5407", &value);
  if (status.ok()) {
    out << "GET key5407 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24893: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1268", "key5579", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1268, key5579) size=" << scan_result.size() << std::endl;
  }

  // op 24894: get
  status = db->Get(read_options, "key1509", &value);
  if (status.ok()) {
    out << "GET key1509 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24895: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3683", "key8683", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3683, key8683) size=" << scan_result.size() << std::endl;
  }

  // op 24896: put
  db->Put(write_options, "key5600", "value61264");

  // op 24897: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0631", "key6086", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0631, key6086) size=" << scan_result.size() << std::endl;
  }

  // op 24898: get
  status = db->Get(read_options, "key11680", &value);
  if (status.ok()) {
    out << "GET key11680 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24899: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 24900: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8737", "key10738", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8737, key10738) size=" << scan_result.size() << std::endl;
  }

  // op 24901: put
  db->Put(write_options, "key7831", "value21837");

  // op 24902: put
  db->Put(write_options, "key8246", "value24275");

  // op 24903: put
  db->Put(write_options, "key2927", "value82537");

  // op 24904: put
  db->Put(write_options, "key0997", "value97673");

  // op 24905: delete
  db->Delete(write_options, "key4206");

  // op 24906: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3761", "key11434", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3761, key11434) size=" << scan_result.size() << std::endl;
  }

  // op 24907: put
  db->Put(write_options, "key1161", "value109281");

  // op 24908: get
  status = db->Get(read_options, "key8855", &value);
  if (status.ok()) {
    out << "GET key8855 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24909: put
  db->Put(write_options, "key3813", "value68204");

  // op 24910: delete_range
  status = db->DeleteRange(write_options, "key2382", "key9038");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 24911: put
  db->Put(write_options, "key5531", "value23049");

  // op 24912: put
  db->Put(write_options, "key10602", "value92963");

  // op 24913: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5388", "key9991", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5388, key9991) size=" << scan_result.size() << std::endl;
  }

  // op 24914: put
  db->Put(write_options, "key0070", "value91122");

  // op 24915: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4942", "key7124", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4942, key7124) size=" << scan_result.size() << std::endl;
  }

  // op 24916: delete_range
  status = db->DeleteRange(write_options, "key8691", "key8930");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 24917: delete_range
  status = db->DeleteRange(write_options, "key5276", "key8814");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 24918: put
  db->Put(write_options, "key10721", "value11906");

  // op 24919: put
  db->Put(write_options, "key3046", "value85700");

  // op 24920: delete_range
  status = db->DeleteRange(write_options, "key6003", "key9772");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 24921: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4407", "key7424", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4407, key7424) size=" << scan_result.size() << std::endl;
  }

  // op 24922: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 24923: put
  db->Put(write_options, "key3273", "value56626");

  // op 24924: put
  db->Put(write_options, "key8172", "value24359");

  // op 24925: put
  db->Put(write_options, "key6533", "value38756");

  // op 24926: get
  status = db->Get(read_options, "key8433", &value);
  if (status.ok()) {
    out << "GET key8433 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24927: put
  db->Put(write_options, "key9172", "value41779");

  // op 24928: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1568", "key7747", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1568, key7747) size=" << scan_result.size() << std::endl;
  }

  // op 24929: put
  db->Put(write_options, "key5831", "value44895");

  // op 24930: delete_range
  status = db->DeleteRange(write_options, "key3560", "key6846");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 24931: delete
  db->Delete(write_options, "key1234");

  // op 24932: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 24933: get
  status = db->Get(read_options, "key0229", &value);
  if (status.ok()) {
    out << "GET key0229 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24934: get
  status = db->Get(read_options, "key9038", &value);
  if (status.ok()) {
    out << "GET key9038 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24935: get
  status = db->Get(read_options, "key0504", &value);
  if (status.ok()) {
    out << "GET key0504 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24936: put
  db->Put(write_options, "key5142", "value84676");

  // op 24937: put
  db->Put(write_options, "key6527", "value78724");

  // op 24938: get
  status = db->Get(read_options, "key3632", &value);
  if (status.ok()) {
    out << "GET key3632 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24939: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 24940: get
  status = db->Get(read_options, "key7843", &value);
  if (status.ok()) {
    out << "GET key7843 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24941: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1580", "key8591", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1580, key8591) size=" << scan_result.size() << std::endl;
  }

  // op 24942: put
  db->Put(write_options, "key10429", "value87566");

  // op 24943: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4838", "key9163", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4838, key9163) size=" << scan_result.size() << std::endl;
  }

  // op 24944: delete_range
  status = db->DeleteRange(write_options, "key6457", "key7932");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 24945: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6519", "key6651", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6519, key6651) size=" << scan_result.size() << std::endl;
  }

  // op 24946: put
  db->Put(write_options, "key10391", "value91838");

  // op 24947: put
  db->Put(write_options, "key8310", "value114722");

  // op 24948: scan
  scan_result.clear();
  status = db->Scan(read_options, "key11449", "key11518", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key11449, key11518) size=" << scan_result.size() << std::endl;
  }

  // op 24949: get
  status = db->Get(read_options, "key1106", &value);
  if (status.ok()) {
    out << "GET key1106 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24950: put
  db->Put(write_options, "key2823", "value80263");

  // op 24951: get
  status = db->Get(read_options, "key4619", &value);
  if (status.ok()) {
    out << "GET key4619 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24952: delete
  db->Delete(write_options, "key4483");

  // op 24953: get
  status = db->Get(read_options, "key3758", &value);
  if (status.ok()) {
    out << "GET key3758 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24954: delete
  db->Delete(write_options, "key4860");

  // op 24955: get
  status = db->Get(read_options, "key7332", &value);
  if (status.ok()) {
    out << "GET key7332 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24956: put
  db->Put(write_options, "key8174", "value38165");

  // op 24957: put
  db->Put(write_options, "key11267", "value16718");

  // op 24958: put
  db->Put(write_options, "key1566", "value118096");

  // op 24959: delete
  db->Delete(write_options, "key0323");

  // op 24960: put
  db->Put(write_options, "key0429", "value93005");

  // op 24961: put
  db->Put(write_options, "key4582", "value51009");

  // op 24962: delete
  db->Delete(write_options, "key6799");

  // op 24963: get
  status = db->Get(read_options, "key10440", &value);
  if (status.ok()) {
    out << "GET key10440 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24964: put
  db->Put(write_options, "key0521", "value40952");

  // op 24965: get
  status = db->Get(read_options, "key11408", &value);
  if (status.ok()) {
    out << "GET key11408 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24966: get
  status = db->Get(read_options, "key8730", &value);
  if (status.ok()) {
    out << "GET key8730 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24967: put
  db->Put(write_options, "key3165", "value35319");

  // op 24968: put
  db->Put(write_options, "key8429", "value77253");

  // op 24969: delete
  db->Delete(write_options, "key4393");

  // op 24970: put
  db->Put(write_options, "key11191", "value59927");

  // op 24971: get
  status = db->Get(read_options, "key8823", &value);
  if (status.ok()) {
    out << "GET key8823 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24972: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2076", "key3496", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2076, key3496) size=" << scan_result.size() << std::endl;
  }

  // op 24973: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7944", "key9900", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7944, key9900) size=" << scan_result.size() << std::endl;
  }

  // op 24974: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7511", "key8696", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7511, key8696) size=" << scan_result.size() << std::endl;
  }

  // op 24975: get
  status = db->Get(read_options, "key1571", &value);
  if (status.ok()) {
    out << "GET key1571 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24976: get
  status = db->Get(read_options, "key5952", &value);
  if (status.ok()) {
    out << "GET key5952 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24977: delete
  db->Delete(write_options, "key9105");

  // op 24978: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10841", "key11629", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10841, key11629) size=" << scan_result.size() << std::endl;
  }

  // op 24979: get
  status = db->Get(read_options, "key6232", &value);
  if (status.ok()) {
    out << "GET key6232 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24980: get
  status = db->Get(read_options, "key2283", &value);
  if (status.ok()) {
    out << "GET key2283 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24981: put
  db->Put(write_options, "key7421", "value42634");

  // op 24982: delete_range
  status = db->DeleteRange(write_options, "key2150", "key7168");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 24983: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0258", "key2965", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0258, key2965) size=" << scan_result.size() << std::endl;
  }

  // op 24984: get
  status = db->Get(read_options, "key10618", &value);
  if (status.ok()) {
    out << "GET key10618 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24985: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1507", "key5171", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1507, key5171) size=" << scan_result.size() << std::endl;
  }

  // op 24986: put
  db->Put(write_options, "key1996", "value68338");

  // op 24987: put
  db->Put(write_options, "key0838", "value95528");

  // op 24988: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 24989: delete
  db->Delete(write_options, "key8295");

  // op 24990: put
  db->Put(write_options, "key9278", "value109760");

  // op 24991: delete_range
  status = db->DeleteRange(write_options, "key7817", "key9046");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 24992: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8314", "key9401", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8314, key9401) size=" << scan_result.size() << std::endl;
  }

  // op 24993: get
  status = db->Get(read_options, "key5966", &value);
  if (status.ok()) {
    out << "GET key5966 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24994: put
  db->Put(write_options, "key0255", "value25402");

  // op 24995: put
  db->Put(write_options, "key7437", "value100300");

  // op 24996: get
  status = db->Get(read_options, "key1831", &value);
  if (status.ok()) {
    out << "GET key1831 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 24997: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 24998: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0642", "key3603", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0642, key3603) size=" << scan_result.size() << std::endl;
  }

  // op 24999: put
  db->Put(write_options, "key3218", "value61492");

  // op 25000: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4172", "key8931", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4172, key8931) size=" << scan_result.size() << std::endl;
  }

  // op 25001: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4449", "key10206", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4449, key10206) size=" << scan_result.size() << std::endl;
  }

  // op 25002: get
  status = db->Get(read_options, "key5156", &value);
  if (status.ok()) {
    out << "GET key5156 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25003: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1874", "key8865", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1874, key8865) size=" << scan_result.size() << std::endl;
  }

  // op 25004: get
  status = db->Get(read_options, "key2482", &value);
  if (status.ok()) {
    out << "GET key2482 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25005: get
  status = db->Get(read_options, "key0262", &value);
  if (status.ok()) {
    out << "GET key0262 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25006: delete
  db->Delete(write_options, "key7456");

  // op 25007: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4413", "key5084", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4413, key5084) size=" << scan_result.size() << std::endl;
  }

  // op 25008: get
  status = db->Get(read_options, "key5600", &value);
  if (status.ok()) {
    out << "GET key5600 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25009: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 25010: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 25011: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5575", "key11118", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5575, key11118) size=" << scan_result.size() << std::endl;
  }

  // op 25012: get
  status = db->Get(read_options, "key5989", &value);
  if (status.ok()) {
    out << "GET key5989 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25013: get
  status = db->Get(read_options, "key6354", &value);
  if (status.ok()) {
    out << "GET key6354 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25014: put
  db->Put(write_options, "key2160", "value95672");

  // op 25015: put
  db->Put(write_options, "key3107", "value48034");

  // op 25016: get
  status = db->Get(read_options, "key2234", &value);
  if (status.ok()) {
    out << "GET key2234 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25017: get
  status = db->Get(read_options, "key10639", &value);
  if (status.ok()) {
    out << "GET key10639 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25018: get
  status = db->Get(read_options, "key10631", &value);
  if (status.ok()) {
    out << "GET key10631 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25019: get
  status = db->Get(read_options, "key7806", &value);
  if (status.ok()) {
    out << "GET key7806 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25020: delete_range
  status = db->DeleteRange(write_options, "key3064", "key10053");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 25021: put
  db->Put(write_options, "key3154", "value4908");

  // op 25022: put
  db->Put(write_options, "key2770", "value101345");

  // op 25023: get
  status = db->Get(read_options, "key5448", &value);
  if (status.ok()) {
    out << "GET key5448 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25024: put
  db->Put(write_options, "key0806", "value27296");

  // op 25025: get
  status = db->Get(read_options, "key9730", &value);
  if (status.ok()) {
    out << "GET key9730 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25026: put
  db->Put(write_options, "key6992", "value36060");

  // op 25027: put
  db->Put(write_options, "key7362", "value117990");

  // op 25028: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6580", "key10644", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6580, key10644) size=" << scan_result.size() << std::endl;
  }

  // op 25029: delete_range
  status = db->DeleteRange(write_options, "key1367", "key8239");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 25030: get
  status = db->Get(read_options, "key8974", &value);
  if (status.ok()) {
    out << "GET key8974 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25031: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2338", "key11670", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2338, key11670) size=" << scan_result.size() << std::endl;
  }

  // op 25032: put
  db->Put(write_options, "key5359", "value46775");

  // op 25033: put
  db->Put(write_options, "key0654", "value62036");

  // op 25034: put
  db->Put(write_options, "key6957", "value37000");

  // op 25035: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2146", "key10397", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2146, key10397) size=" << scan_result.size() << std::endl;
  }

  // op 25036: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 25037: put
  db->Put(write_options, "key11308", "value57743");

  // op 25038: get
  status = db->Get(read_options, "key2843", &value);
  if (status.ok()) {
    out << "GET key2843 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25039: delete
  db->Delete(write_options, "key11273");

  // op 25040: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4956", "key7084", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4956, key7084) size=" << scan_result.size() << std::endl;
  }

  // op 25041: put
  db->Put(write_options, "key11328", "value48881");

  // op 25042: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 25043: get
  status = db->Get(read_options, "key5961", &value);
  if (status.ok()) {
    out << "GET key5961 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25044: put
  db->Put(write_options, "key7057", "value0929");

  // op 25045: delete
  db->Delete(write_options, "key10023");

  // op 25046: put
  db->Put(write_options, "key2050", "value1882");

  // op 25047: delete_range
  status = db->DeleteRange(write_options, "key6040", "key6974");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 25048: put
  db->Put(write_options, "key1728", "value56581");

  // op 25049: get
  status = db->Get(read_options, "key0969", &value);
  if (status.ok()) {
    out << "GET key0969 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25050: get
  status = db->Get(read_options, "key7635", &value);
  if (status.ok()) {
    out << "GET key7635 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25051: get
  status = db->Get(read_options, "key1826", &value);
  if (status.ok()) {
    out << "GET key1826 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25052: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 25053: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6902", "key9321", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6902, key9321) size=" << scan_result.size() << std::endl;
  }

  // op 25054: delete_range
  status = db->DeleteRange(write_options, "key2414", "key4029");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 25055: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2214", "key11459", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2214, key11459) size=" << scan_result.size() << std::endl;
  }

  // op 25056: put
  db->Put(write_options, "key10853", "value81436");

  // op 25057: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 25058: get
  status = db->Get(read_options, "key1572", &value);
  if (status.ok()) {
    out << "GET key1572 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25059: put
  db->Put(write_options, "key4133", "value45672");

  // op 25060: delete_range
  status = db->DeleteRange(write_options, "key3622", "key4169");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 25061: put
  db->Put(write_options, "key1186", "value45685");

  // op 25062: put
  db->Put(write_options, "key2381", "value34603");

  // op 25063: get
  status = db->Get(read_options, "key6896", &value);
  if (status.ok()) {
    out << "GET key6896 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25064: put
  db->Put(write_options, "key11401", "value101641");

  // op 25065: put
  db->Put(write_options, "key10506", "value4353");

  // op 25066: put
  db->Put(write_options, "key4455", "value84736");

  // op 25067: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3346", "key10691", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3346, key10691) size=" << scan_result.size() << std::endl;
  }

  // op 25068: delete_range
  status = db->DeleteRange(write_options, "key0637", "key3137");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 25069: get
  status = db->Get(read_options, "key8044", &value);
  if (status.ok()) {
    out << "GET key8044 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25070: put
  db->Put(write_options, "key1685", "value3077");

  // op 25071: get
  status = db->Get(read_options, "key8854", &value);
  if (status.ok()) {
    out << "GET key8854 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25072: put
  db->Put(write_options, "key11054", "value81319");

  // op 25073: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7994", "key11394", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7994, key11394) size=" << scan_result.size() << std::endl;
  }

  // op 25074: get
  status = db->Get(read_options, "key2594", &value);
  if (status.ok()) {
    out << "GET key2594 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25075: delete
  db->Delete(write_options, "key9914");

  // op 25076: put
  db->Put(write_options, "key6859", "value106013");

  // op 25077: delete
  db->Delete(write_options, "key9018");

  // op 25078: delete_range
  status = db->DeleteRange(write_options, "key2224", "key6905");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 25079: put
  db->Put(write_options, "key1547", "value57531");

  // op 25080: put
  db->Put(write_options, "key6761", "value44852");

  // op 25081: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1115", "key11270", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1115, key11270) size=" << scan_result.size() << std::endl;
  }

  // op 25082: put
  db->Put(write_options, "key1265", "value112523");

  // op 25083: get
  status = db->Get(read_options, "key5938", &value);
  if (status.ok()) {
    out << "GET key5938 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25084: get
  status = db->Get(read_options, "key1149", &value);
  if (status.ok()) {
    out << "GET key1149 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25085: put
  db->Put(write_options, "key1090", "value56891");

  // op 25086: get
  status = db->Get(read_options, "key5708", &value);
  if (status.ok()) {
    out << "GET key5708 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25087: get
  status = db->Get(read_options, "key7322", &value);
  if (status.ok()) {
    out << "GET key7322 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25088: delete
  db->Delete(write_options, "key6569");

  // op 25089: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6359", "key10971", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6359, key10971) size=" << scan_result.size() << std::endl;
  }

  // op 25090: put
  db->Put(write_options, "key0452", "value115423");

  // op 25091: put
  db->Put(write_options, "key10262", "value6886");

  // op 25092: delete
  db->Delete(write_options, "key4598");

  // op 25093: put
  db->Put(write_options, "key8022", "value90669");

  // op 25094: get
  status = db->Get(read_options, "key7532", &value);
  if (status.ok()) {
    out << "GET key7532 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25095: put
  db->Put(write_options, "key5493", "value51297");

  // op 25096: put
  db->Put(write_options, "key2021", "value23708");

  // op 25097: get
  status = db->Get(read_options, "key11094", &value);
  if (status.ok()) {
    out << "GET key11094 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25098: get
  status = db->Get(read_options, "key9626", &value);
  if (status.ok()) {
    out << "GET key9626 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25099: put
  db->Put(write_options, "key11134", "value49612");

  // op 25100: put
  db->Put(write_options, "key6541", "value98340");

  // op 25101: delete
  db->Delete(write_options, "key3498");

  // op 25102: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 25103: put
  db->Put(write_options, "key0576", "value84527");

  // op 25104: get
  status = db->Get(read_options, "key5396", &value);
  if (status.ok()) {
    out << "GET key5396 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25105: get
  status = db->Get(read_options, "key0623", &value);
  if (status.ok()) {
    out << "GET key0623 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25106: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4587", "key7996", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4587, key7996) size=" << scan_result.size() << std::endl;
  }

  // op 25107: get
  status = db->Get(read_options, "key2256", &value);
  if (status.ok()) {
    out << "GET key2256 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25108: delete_range
  status = db->DeleteRange(write_options, "key3633", "key7964");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 25109: put
  db->Put(write_options, "key9379", "value97272");

  // op 25110: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3670", "key8624", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3670, key8624) size=" << scan_result.size() << std::endl;
  }

  // op 25111: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 25112: delete
  db->Delete(write_options, "key11210");

  // op 25113: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 25114: put
  db->Put(write_options, "key10888", "value34891");

  // op 25115: get
  status = db->Get(read_options, "key7646", &value);
  if (status.ok()) {
    out << "GET key7646 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25116: delete_range
  status = db->DeleteRange(write_options, "key2661", "key10737");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 25117: put
  db->Put(write_options, "key3731", "value42781");

  // op 25118: get
  status = db->Get(read_options, "key3439", &value);
  if (status.ok()) {
    out << "GET key3439 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25119: put
  db->Put(write_options, "key4776", "value49340");

  // op 25120: put
  db->Put(write_options, "key8742", "value48702");

  // op 25121: put
  db->Put(write_options, "key8212", "value5845");

  // op 25122: put
  db->Put(write_options, "key4773", "value101915");

  // op 25123: delete
  db->Delete(write_options, "key4655");

  // op 25124: delete_range
  status = db->DeleteRange(write_options, "key2663", "key7389");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 25125: put
  db->Put(write_options, "key9839", "value63242");

  // op 25126: get
  status = db->Get(read_options, "key5232", &value);
  if (status.ok()) {
    out << "GET key5232 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25127: put
  db->Put(write_options, "key9654", "value88839");

  // op 25128: put
  db->Put(write_options, "key9799", "value83553");

  // op 25129: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6936", "key11546", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6936, key11546) size=" << scan_result.size() << std::endl;
  }

  // op 25130: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 25131: get
  status = db->Get(read_options, "key8513", &value);
  if (status.ok()) {
    out << "GET key8513 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25132: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 25133: get
  status = db->Get(read_options, "key8919", &value);
  if (status.ok()) {
    out << "GET key8919 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25134: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9198", "key10528", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9198, key10528) size=" << scan_result.size() << std::endl;
  }

  // op 25135: put
  db->Put(write_options, "key6433", "value99097");

  // op 25136: delete_range
  status = db->DeleteRange(write_options, "key1679", "key2439");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 25137: get
  status = db->Get(read_options, "key7245", &value);
  if (status.ok()) {
    out << "GET key7245 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25138: get
  status = db->Get(read_options, "key6991", &value);
  if (status.ok()) {
    out << "GET key6991 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25139: put
  db->Put(write_options, "key9429", "value75152");

  // op 25140: get
  status = db->Get(read_options, "key1740", &value);
  if (status.ok()) {
    out << "GET key1740 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25141: put
  db->Put(write_options, "key0759", "value29749");

  // op 25142: get
  status = db->Get(read_options, "key7929", &value);
  if (status.ok()) {
    out << "GET key7929 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25143: put
  db->Put(write_options, "key7466", "value62200");

  // op 25144: delete_range
  status = db->DeleteRange(write_options, "key1577", "key11244");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 25145: get
  status = db->Get(read_options, "key8109", &value);
  if (status.ok()) {
    out << "GET key8109 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25146: delete
  db->Delete(write_options, "key8525");

  // op 25147: put
  db->Put(write_options, "key2313", "value86774");

  // op 25148: put
  db->Put(write_options, "key8028", "value100758");

  // op 25149: delete_range
  status = db->DeleteRange(write_options, "key9183", "key11458");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 25150: put
  db->Put(write_options, "key5176", "value82075");

  // op 25151: put
  db->Put(write_options, "key7244", "value56013");

  // op 25152: delete
  db->Delete(write_options, "key1114");

  // op 25153: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7790", "key8669", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7790, key8669) size=" << scan_result.size() << std::endl;
  }

  // op 25154: get
  status = db->Get(read_options, "key9289", &value);
  if (status.ok()) {
    out << "GET key9289 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25155: get
  status = db->Get(read_options, "key3158", &value);
  if (status.ok()) {
    out << "GET key3158 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25156: get
  status = db->Get(read_options, "key4771", &value);
  if (status.ok()) {
    out << "GET key4771 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25157: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4609", "key5934", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4609, key5934) size=" << scan_result.size() << std::endl;
  }

  // op 25158: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4431", "key7656", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4431, key7656) size=" << scan_result.size() << std::endl;
  }

  // op 25159: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3940", "key4995", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3940, key4995) size=" << scan_result.size() << std::endl;
  }

  // op 25160: put
  db->Put(write_options, "key7927", "value24409");

  // op 25161: put
  db->Put(write_options, "key5955", "value91927");

  // op 25162: delete_range
  status = db->DeleteRange(write_options, "key0241", "key11651");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 25163: get
  status = db->Get(read_options, "key11353", &value);
  if (status.ok()) {
    out << "GET key11353 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25164: get
  status = db->Get(read_options, "key1180", &value);
  if (status.ok()) {
    out << "GET key1180 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25165: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 25166: put
  db->Put(write_options, "key8226", "value83972");

  // op 25167: put
  db->Put(write_options, "key6210", "value9358");

  // op 25168: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7693", "key10154", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7693, key10154) size=" << scan_result.size() << std::endl;
  }

  // op 25169: put
  db->Put(write_options, "key8011", "value71521");

  // op 25170: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 25171: get
  status = db->Get(read_options, "key6415", &value);
  if (status.ok()) {
    out << "GET key6415 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25172: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6101", "key8422", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6101, key8422) size=" << scan_result.size() << std::endl;
  }

  // op 25173: delete_range
  status = db->DeleteRange(write_options, "key1810", "key8837");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 25174: put
  db->Put(write_options, "key11670", "value43567");

  // op 25175: put
  db->Put(write_options, "key8054", "value92246");

  // op 25176: get
  status = db->Get(read_options, "key1578", &value);
  if (status.ok()) {
    out << "GET key1578 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25177: get
  status = db->Get(read_options, "key5464", &value);
  if (status.ok()) {
    out << "GET key5464 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25178: put
  db->Put(write_options, "key2247", "value48509");

  // op 25179: put
  db->Put(write_options, "key8200", "value41572");

  // op 25180: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 25181: delete_range
  status = db->DeleteRange(write_options, "key1779", "key9005");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 25182: put
  db->Put(write_options, "key7644", "value15920");

  // op 25183: get
  status = db->Get(read_options, "key6636", &value);
  if (status.ok()) {
    out << "GET key6636 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25184: put
  db->Put(write_options, "key0919", "value63106");

  // op 25185: put
  db->Put(write_options, "key1242", "value5338");

  // op 25186: delete_range
  status = db->DeleteRange(write_options, "key10910", "key11472");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 25187: put
  db->Put(write_options, "key6752", "value66651");

  // op 25188: put
  db->Put(write_options, "key3313", "value108572");

  // op 25189: delete
  db->Delete(write_options, "key3516");

  // op 25190: delete
  db->Delete(write_options, "key8360");

  // op 25191: delete_range
  status = db->DeleteRange(write_options, "key2271", "key7957");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 25192: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 25193: put
  db->Put(write_options, "key1743", "value92588");

  // op 25194: get
  status = db->Get(read_options, "key7835", &value);
  if (status.ok()) {
    out << "GET key7835 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25195: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6768", "key11325", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6768, key11325) size=" << scan_result.size() << std::endl;
  }

  // op 25196: get
  status = db->Get(read_options, "key7152", &value);
  if (status.ok()) {
    out << "GET key7152 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25197: put
  db->Put(write_options, "key3247", "value32023");

  // op 25198: put
  db->Put(write_options, "key7507", "value86271");

  // op 25199: get
  status = db->Get(read_options, "key6396", &value);
  if (status.ok()) {
    out << "GET key6396 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25200: delete
  db->Delete(write_options, "key6049");

  // op 25201: put
  db->Put(write_options, "key10271", "value14095");

  // op 25202: put
  db->Put(write_options, "key9471", "value100190");

  // op 25203: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2725", "key9188", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2725, key9188) size=" << scan_result.size() << std::endl;
  }

  // op 25204: put
  db->Put(write_options, "key7646", "value50730");

  // op 25205: put
  db->Put(write_options, "key1544", "value41772");

  // op 25206: put
  db->Put(write_options, "key7299", "value16633");

  // op 25207: put
  db->Put(write_options, "key2687", "value35151");

  // op 25208: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2744", "key7696", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2744, key7696) size=" << scan_result.size() << std::endl;
  }

  // op 25209: delete_range
  status = db->DeleteRange(write_options, "key0930", "key5693");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 25210: delete
  db->Delete(write_options, "key5746");

  // op 25211: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9585", "key10878", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9585, key10878) size=" << scan_result.size() << std::endl;
  }

  // op 25212: put
  db->Put(write_options, "key3372", "value11400");

  // op 25213: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 25214: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6292", "key10182", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6292, key10182) size=" << scan_result.size() << std::endl;
  }

  // op 25215: get
  status = db->Get(read_options, "key9645", &value);
  if (status.ok()) {
    out << "GET key9645 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25216: delete_range
  status = db->DeleteRange(write_options, "key0869", "key9584");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 25217: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 25218: put
  db->Put(write_options, "key7177", "value112853");

  // op 25219: put
  db->Put(write_options, "key10882", "value37097");

  // op 25220: get
  status = db->Get(read_options, "key8388", &value);
  if (status.ok()) {
    out << "GET key8388 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25221: delete
  db->Delete(write_options, "key0492");

  // op 25222: delete
  db->Delete(write_options, "key9467");

  // op 25223: delete
  db->Delete(write_options, "key2310");

  // op 25224: get
  status = db->Get(read_options, "key11161", &value);
  if (status.ok()) {
    out << "GET key11161 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25225: put
  db->Put(write_options, "key5243", "value61972");

  // op 25226: delete
  db->Delete(write_options, "key5411");

  // op 25227: get
  status = db->Get(read_options, "key3889", &value);
  if (status.ok()) {
    out << "GET key3889 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25228: get
  status = db->Get(read_options, "key11010", &value);
  if (status.ok()) {
    out << "GET key11010 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25229: get
  status = db->Get(read_options, "key11297", &value);
  if (status.ok()) {
    out << "GET key11297 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25230: put
  db->Put(write_options, "key1158", "value108768");

  // op 25231: put
  db->Put(write_options, "key8051", "value80183");

  // op 25232: get
  status = db->Get(read_options, "key11319", &value);
  if (status.ok()) {
    out << "GET key11319 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25233: put
  db->Put(write_options, "key0643", "value32624");

  // op 25234: get
  status = db->Get(read_options, "key8587", &value);
  if (status.ok()) {
    out << "GET key8587 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25235: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2876", "key3654", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2876, key3654) size=" << scan_result.size() << std::endl;
  }

  // op 25236: delete
  db->Delete(write_options, "key9405");

  // op 25237: get
  status = db->Get(read_options, "key11497", &value);
  if (status.ok()) {
    out << "GET key11497 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25238: put
  db->Put(write_options, "key7172", "value73456");

  // op 25239: put
  db->Put(write_options, "key11061", "value89672");

  // op 25240: delete
  db->Delete(write_options, "key1513");

  // op 25241: put
  db->Put(write_options, "key4540", "value26212");

  // op 25242: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9664", "key10225", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9664, key10225) size=" << scan_result.size() << std::endl;
  }

  // op 25243: get
  status = db->Get(read_options, "key3004", &value);
  if (status.ok()) {
    out << "GET key3004 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25244: put
  db->Put(write_options, "key7498", "value83905");

  // op 25245: put
  db->Put(write_options, "key6977", "value33810");

  // op 25246: put
  db->Put(write_options, "key5719", "value40653");

  // op 25247: get
  status = db->Get(read_options, "key9127", &value);
  if (status.ok()) {
    out << "GET key9127 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25248: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1767", "key6829", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1767, key6829) size=" << scan_result.size() << std::endl;
  }

  // op 25249: delete
  db->Delete(write_options, "key11020");

  // op 25250: get
  status = db->Get(read_options, "key2490", &value);
  if (status.ok()) {
    out << "GET key2490 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25251: put
  db->Put(write_options, "key9884", "value87841");

  // op 25252: put
  db->Put(write_options, "key5897", "value20327");

  // op 25253: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0223", "key0260", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0223, key0260) size=" << scan_result.size() << std::endl;
  }

  // op 25254: put
  db->Put(write_options, "key5869", "value82007");

  // op 25255: put
  db->Put(write_options, "key2407", "value57612");

  // op 25256: put
  db->Put(write_options, "key1909", "value39960");

  // op 25257: get
  status = db->Get(read_options, "key2232", &value);
  if (status.ok()) {
    out << "GET key2232 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25258: put
  db->Put(write_options, "key3652", "value79945");

  // op 25259: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4470", "key4807", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4470, key4807) size=" << scan_result.size() << std::endl;
  }

  // op 25260: put
  db->Put(write_options, "key0234", "value40035");

  // op 25261: delete
  db->Delete(write_options, "key2237");

  // op 25262: put
  db->Put(write_options, "key9366", "value11471");

  // op 25263: put
  db->Put(write_options, "key2088", "value3592");

  // op 25264: get
  status = db->Get(read_options, "key2959", &value);
  if (status.ok()) {
    out << "GET key2959 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25265: delete_range
  status = db->DeleteRange(write_options, "key2004", "key10070");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 25266: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2608", "key11265", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2608, key11265) size=" << scan_result.size() << std::endl;
  }

  // op 25267: put
  db->Put(write_options, "key10306", "value93504");

  // op 25268: put
  db->Put(write_options, "key2939", "value40577");

  // op 25269: put
  db->Put(write_options, "key2539", "value69302");

  // op 25270: put
  db->Put(write_options, "key11696", "value112283");

  // op 25271: put
  db->Put(write_options, "key1770", "value114246");

  // op 25272: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4003", "key4821", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4003, key4821) size=" << scan_result.size() << std::endl;
  }

  // op 25273: put
  db->Put(write_options, "key7745", "value18677");

  // op 25274: delete_range
  status = db->DeleteRange(write_options, "key4007", "key7187");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 25275: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1594", "key10698", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1594, key10698) size=" << scan_result.size() << std::endl;
  }

  // op 25276: put
  db->Put(write_options, "key10830", "value108720");

  // op 25277: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4896", "key11403", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4896, key11403) size=" << scan_result.size() << std::endl;
  }

  // op 25278: get
  status = db->Get(read_options, "key11345", &value);
  if (status.ok()) {
    out << "GET key11345 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25279: delete_range
  status = db->DeleteRange(write_options, "key8297", "key8385");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 25280: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1591", "key6781", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1591, key6781) size=" << scan_result.size() << std::endl;
  }

  // op 25281: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10337", "key10983", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10337, key10983) size=" << scan_result.size() << std::endl;
  }

  // op 25282: delete
  db->Delete(write_options, "key0914");

  // op 25283: get
  status = db->Get(read_options, "key0221", &value);
  if (status.ok()) {
    out << "GET key0221 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25284: put
  db->Put(write_options, "key5461", "value79520");

  // op 25285: put
  db->Put(write_options, "key8471", "value116109");

  // op 25286: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7368", "key8429", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7368, key8429) size=" << scan_result.size() << std::endl;
  }

  // op 25287: get
  status = db->Get(read_options, "key1641", &value);
  if (status.ok()) {
    out << "GET key1641 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25288: put
  db->Put(write_options, "key0632", "value19994");

  // op 25289: delete_range
  status = db->DeleteRange(write_options, "key0177", "key2681");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 25290: get
  status = db->Get(read_options, "key10816", &value);
  if (status.ok()) {
    out << "GET key10816 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25291: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 25292: get
  status = db->Get(read_options, "key1509", &value);
  if (status.ok()) {
    out << "GET key1509 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25293: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1478", "key9312", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1478, key9312) size=" << scan_result.size() << std::endl;
  }

  // op 25294: get
  status = db->Get(read_options, "key7249", &value);
  if (status.ok()) {
    out << "GET key7249 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25295: put
  db->Put(write_options, "key6527", "value116490");

  // op 25296: put
  db->Put(write_options, "key3472", "value99598");

  // op 25297: get
  status = db->Get(read_options, "key0362", &value);
  if (status.ok()) {
    out << "GET key0362 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25298: put
  db->Put(write_options, "key1919", "value54332");

  // op 25299: delete
  db->Delete(write_options, "key1214");

  // op 25300: put
  db->Put(write_options, "key8841", "value48834");

  // op 25301: put
  db->Put(write_options, "key6928", "value6466");

  // op 25302: put
  db->Put(write_options, "key9033", "value55664");

  // op 25303: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 25304: get
  status = db->Get(read_options, "key10047", &value);
  if (status.ok()) {
    out << "GET key10047 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25305: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 25306: delete_range
  status = db->DeleteRange(write_options, "key1428", "key4230");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 25307: put
  db->Put(write_options, "key1424", "value67816");

  // op 25308: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1693", "key11400", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1693, key11400) size=" << scan_result.size() << std::endl;
  }

  // op 25309: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1388", "key5400", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1388, key5400) size=" << scan_result.size() << std::endl;
  }

  // op 25310: put
  db->Put(write_options, "key6755", "value105518");

  // op 25311: put
  db->Put(write_options, "key2395", "value105534");

  // op 25312: put
  db->Put(write_options, "key7083", "value107534");

  // op 25313: delete_range
  status = db->DeleteRange(write_options, "key1580", "key5791");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 25314: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3972", "key5149", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3972, key5149) size=" << scan_result.size() << std::endl;
  }

  // op 25315: put
  db->Put(write_options, "key8257", "value103972");

  // op 25316: get
  status = db->Get(read_options, "key2401", &value);
  if (status.ok()) {
    out << "GET key2401 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25317: put
  db->Put(write_options, "key3182", "value68112");

  // op 25318: put
  db->Put(write_options, "key6026", "value111375");

  // op 25319: put
  db->Put(write_options, "key10371", "value49127");

  // op 25320: put
  db->Put(write_options, "key6934", "value30005");

  // op 25321: get
  status = db->Get(read_options, "key9686", &value);
  if (status.ok()) {
    out << "GET key9686 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25322: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0632", "key8485", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0632, key8485) size=" << scan_result.size() << std::endl;
  }

  // op 25323: put
  db->Put(write_options, "key7364", "value62760");

  // op 25324: put
  db->Put(write_options, "key2866", "value24785");

  // op 25325: put
  db->Put(write_options, "key1092", "value82899");

  // op 25326: get
  status = db->Get(read_options, "key4314", &value);
  if (status.ok()) {
    out << "GET key4314 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25327: put
  db->Put(write_options, "key8035", "value8089");

  // op 25328: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6579", "key8631", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6579, key8631) size=" << scan_result.size() << std::endl;
  }

  // op 25329: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10389", "key11077", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10389, key11077) size=" << scan_result.size() << std::endl;
  }

  // op 25330: put
  db->Put(write_options, "key7143", "value61804");

  // op 25331: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5470", "key7785", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5470, key7785) size=" << scan_result.size() << std::endl;
  }

  // op 25332: get
  status = db->Get(read_options, "key4083", &value);
  if (status.ok()) {
    out << "GET key4083 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25333: delete_range
  status = db->DeleteRange(write_options, "key2186", "key8961");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 25334: put
  db->Put(write_options, "key8720", "value85371");

  // op 25335: put
  db->Put(write_options, "key7388", "value65507");

  // op 25336: put
  db->Put(write_options, "key0669", "value88672");

  // op 25337: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5946", "key8491", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5946, key8491) size=" << scan_result.size() << std::endl;
  }

  // op 25338: put
  db->Put(write_options, "key8463", "value66568");

  // op 25339: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 25340: delete
  db->Delete(write_options, "key0285");

  // op 25341: put
  db->Put(write_options, "key11407", "value9312");

  // op 25342: delete_range
  status = db->DeleteRange(write_options, "key4867", "key10281");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 25343: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3779", "key7503", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3779, key7503) size=" << scan_result.size() << std::endl;
  }

  // op 25344: get
  status = db->Get(read_options, "key9564", &value);
  if (status.ok()) {
    out << "GET key9564 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25345: put
  db->Put(write_options, "key10679", "value113809");

  // op 25346: put
  db->Put(write_options, "key2528", "value16873");

  // op 25347: put
  db->Put(write_options, "key2457", "value64560");

  // op 25348: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8490", "key10260", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8490, key10260) size=" << scan_result.size() << std::endl;
  }

  // op 25349: put
  db->Put(write_options, "key3482", "value80835");

  // op 25350: put
  db->Put(write_options, "key4504", "value19627");

  // op 25351: get
  status = db->Get(read_options, "key8656", &value);
  if (status.ok()) {
    out << "GET key8656 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25352: delete_range
  status = db->DeleteRange(write_options, "key1391", "key7466");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 25353: get
  status = db->Get(read_options, "key2183", &value);
  if (status.ok()) {
    out << "GET key2183 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25354: get
  status = db->Get(read_options, "key2386", &value);
  if (status.ok()) {
    out << "GET key2386 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25355: get
  status = db->Get(read_options, "key2120", &value);
  if (status.ok()) {
    out << "GET key2120 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25356: put
  db->Put(write_options, "key5156", "value81621");

  // op 25357: put
  db->Put(write_options, "key0736", "value64673");

  // op 25358: get
  status = db->Get(read_options, "key1706", &value);
  if (status.ok()) {
    out << "GET key1706 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25359: delete
  db->Delete(write_options, "key4963");

  // op 25360: delete
  db->Delete(write_options, "key11289");

  // op 25361: put
  db->Put(write_options, "key7663", "value59176");

  // op 25362: put
  db->Put(write_options, "key9918", "value12361");

  // op 25363: delete_range
  status = db->DeleteRange(write_options, "key1835", "key6587");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 25364: delete
  db->Delete(write_options, "key9426");

  // op 25365: get
  status = db->Get(read_options, "key10007", &value);
  if (status.ok()) {
    out << "GET key10007 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25366: put
  db->Put(write_options, "key2004", "value0625");

  // op 25367: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9234", "key9672", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9234, key9672) size=" << scan_result.size() << std::endl;
  }

  // op 25368: delete
  db->Delete(write_options, "key5279");

  // op 25369: get
  status = db->Get(read_options, "key0312", &value);
  if (status.ok()) {
    out << "GET key0312 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25370: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 25371: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8290", "key9808", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8290, key9808) size=" << scan_result.size() << std::endl;
  }

  // op 25372: delete
  db->Delete(write_options, "key3553");

  // op 25373: put
  db->Put(write_options, "key1915", "value103478");

  // op 25374: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1164", "key1406", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1164, key1406) size=" << scan_result.size() << std::endl;
  }

  // op 25375: delete_range
  status = db->DeleteRange(write_options, "key7880", "key8978");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 25376: delete
  db->Delete(write_options, "key6051");

  // op 25377: delete
  db->Delete(write_options, "key2136");

  // op 25378: delete
  db->Delete(write_options, "key6233");

  // op 25379: put
  db->Put(write_options, "key5907", "value7672");

  // op 25380: put
  db->Put(write_options, "key0465", "value99543");

  // op 25381: put
  db->Put(write_options, "key3120", "value102850");

  // op 25382: put
  db->Put(write_options, "key6398", "value108308");

  // op 25383: put
  db->Put(write_options, "key3033", "value10976");

  // op 25384: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 25385: put
  db->Put(write_options, "key11691", "value110934");

  // op 25386: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8461", "key10054", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8461, key10054) size=" << scan_result.size() << std::endl;
  }

  // op 25387: put
  db->Put(write_options, "key11671", "value24726");

  // op 25388: put
  db->Put(write_options, "key1851", "value15850");

  // op 25389: put
  db->Put(write_options, "key5383", "value10910");

  // op 25390: put
  db->Put(write_options, "key7442", "value62386");

  // op 25391: get
  status = db->Get(read_options, "key6230", &value);
  if (status.ok()) {
    out << "GET key6230 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25392: put
  db->Put(write_options, "key8501", "value14196");

  // op 25393: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10590", "key11469", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10590, key11469) size=" << scan_result.size() << std::endl;
  }

  // op 25394: put
  db->Put(write_options, "key5346", "value70526");

  // op 25395: delete
  db->Delete(write_options, "key8158");

  // op 25396: delete_range
  status = db->DeleteRange(write_options, "key7946", "key8063");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 25397: put
  db->Put(write_options, "key1949", "value94455");

  // op 25398: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1808", "key8408", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1808, key8408) size=" << scan_result.size() << std::endl;
  }

  // op 25399: put
  db->Put(write_options, "key10697", "value81989");

  // op 25400: get
  status = db->Get(read_options, "key0090", &value);
  if (status.ok()) {
    out << "GET key0090 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25401: delete
  db->Delete(write_options, "key6236");

  // op 25402: get
  status = db->Get(read_options, "key0327", &value);
  if (status.ok()) {
    out << "GET key0327 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25403: put
  db->Put(write_options, "key0626", "value76073");

  // op 25404: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3009", "key9365", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3009, key9365) size=" << scan_result.size() << std::endl;
  }

  // op 25405: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3130", "key3351", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3130, key3351) size=" << scan_result.size() << std::endl;
  }

  // op 25406: put
  db->Put(write_options, "key7599", "value40083");

  // op 25407: put
  db->Put(write_options, "key0039", "value35834");

  // op 25408: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3826", "key7384", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3826, key7384) size=" << scan_result.size() << std::endl;
  }

  // op 25409: put
  db->Put(write_options, "key2689", "value79122");

  // op 25410: put
  db->Put(write_options, "key4504", "value17522");

  // op 25411: delete
  db->Delete(write_options, "key7024");

  // op 25412: put
  db->Put(write_options, "key7703", "value45711");

  // op 25413: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3353", "key7824", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3353, key7824) size=" << scan_result.size() << std::endl;
  }

  // op 25414: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 25415: delete
  db->Delete(write_options, "key10819");

  // op 25416: delete
  db->Delete(write_options, "key4611");

  // op 25417: put
  db->Put(write_options, "key9121", "value50643");

  // op 25418: delete
  db->Delete(write_options, "key2848");

  // op 25419: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4899", "key5965", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4899, key5965) size=" << scan_result.size() << std::endl;
  }

  // op 25420: put
  db->Put(write_options, "key0931", "value101630");

  // op 25421: delete
  db->Delete(write_options, "key0312");

  // op 25422: put
  db->Put(write_options, "key4902", "value9383");

  // op 25423: put
  db->Put(write_options, "key10033", "value7047");

  // op 25424: delete_range
  status = db->DeleteRange(write_options, "key0889", "key6693");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 25425: put
  db->Put(write_options, "key4477", "value19468");

  // op 25426: put
  db->Put(write_options, "key7766", "value104564");

  // op 25427: put
  db->Put(write_options, "key6441", "value85332");

  // op 25428: put
  db->Put(write_options, "key5208", "value105958");

  // op 25429: put
  db->Put(write_options, "key9494", "value42379");

  // op 25430: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0275", "key2523", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0275, key2523) size=" << scan_result.size() << std::endl;
  }

  // op 25431: put
  db->Put(write_options, "key5043", "value1955");

  // op 25432: get
  status = db->Get(read_options, "key6975", &value);
  if (status.ok()) {
    out << "GET key6975 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25433: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7782", "key9413", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7782, key9413) size=" << scan_result.size() << std::endl;
  }

  // op 25434: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1203", "key7063", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1203, key7063) size=" << scan_result.size() << std::endl;
  }

  // op 25435: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5740", "key6387", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5740, key6387) size=" << scan_result.size() << std::endl;
  }

  // op 25436: put
  db->Put(write_options, "key9413", "value22784");

  // op 25437: put
  db->Put(write_options, "key5854", "value71830");

  // op 25438: put
  db->Put(write_options, "key0674", "value17648");

  // op 25439: put
  db->Put(write_options, "key4843", "value29589");

  // op 25440: get
  status = db->Get(read_options, "key5935", &value);
  if (status.ok()) {
    out << "GET key5935 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25441: put
  db->Put(write_options, "key3497", "value29499");

  // op 25442: put
  db->Put(write_options, "key1845", "value114294");

  // op 25443: delete
  db->Delete(write_options, "key1391");

  // op 25444: delete
  db->Delete(write_options, "key3677");

  // op 25445: put
  db->Put(write_options, "key6103", "value36114");

  // op 25446: delete
  db->Delete(write_options, "key5278");

  // op 25447: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3108", "key7308", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3108, key7308) size=" << scan_result.size() << std::endl;
  }

  // op 25448: get
  status = db->Get(read_options, "key9026", &value);
  if (status.ok()) {
    out << "GET key9026 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25449: get
  status = db->Get(read_options, "key5568", &value);
  if (status.ok()) {
    out << "GET key5568 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25450: put
  db->Put(write_options, "key9050", "value78255");

  // op 25451: put
  db->Put(write_options, "key1820", "value43625");

  // op 25452: delete
  db->Delete(write_options, "key4170");

  // op 25453: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5109", "key6834", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5109, key6834) size=" << scan_result.size() << std::endl;
  }

  // op 25454: put
  db->Put(write_options, "key1457", "value43773");

  // op 25455: delete_range
  status = db->DeleteRange(write_options, "key6706", "key7595");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 25456: get
  status = db->Get(read_options, "key0848", &value);
  if (status.ok()) {
    out << "GET key0848 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25457: delete_range
  status = db->DeleteRange(write_options, "key1944", "key7737");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 25458: delete
  db->Delete(write_options, "key2087");

  // op 25459: delete_range
  status = db->DeleteRange(write_options, "key1989", "key3953");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 25460: delete_range
  status = db->DeleteRange(write_options, "key4380", "key9526");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 25461: put
  db->Put(write_options, "key4492", "value113221");

  // op 25462: get
  status = db->Get(read_options, "key8480", &value);
  if (status.ok()) {
    out << "GET key8480 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25463: put
  db->Put(write_options, "key4307", "value116132");

  // op 25464: delete_range
  status = db->DeleteRange(write_options, "key5567", "key6621");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 25465: delete_range
  status = db->DeleteRange(write_options, "key1926", "key10750");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 25466: get
  status = db->Get(read_options, "key6653", &value);
  if (status.ok()) {
    out << "GET key6653 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25467: get
  status = db->Get(read_options, "key10100", &value);
  if (status.ok()) {
    out << "GET key10100 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25468: put
  db->Put(write_options, "key10748", "value42711");

  // op 25469: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4666", "key9647", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4666, key9647) size=" << scan_result.size() << std::endl;
  }

  // op 25470: get
  status = db->Get(read_options, "key11286", &value);
  if (status.ok()) {
    out << "GET key11286 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25471: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7811", "key11382", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7811, key11382) size=" << scan_result.size() << std::endl;
  }

  // op 25472: delete_range
  status = db->DeleteRange(write_options, "key9195", "key10108");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 25473: put
  db->Put(write_options, "key5299", "value66519");

  // op 25474: delete
  db->Delete(write_options, "key0159");

  // op 25475: put
  db->Put(write_options, "key2119", "value79775");

  // op 25476: get
  status = db->Get(read_options, "key6641", &value);
  if (status.ok()) {
    out << "GET key6641 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25477: put
  db->Put(write_options, "key10601", "value92945");

  // op 25478: get
  status = db->Get(read_options, "key1976", &value);
  if (status.ok()) {
    out << "GET key1976 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25479: put
  db->Put(write_options, "key6827", "value70225");

  // op 25480: put
  db->Put(write_options, "key7217", "value3335");

  // op 25481: delete_range
  status = db->DeleteRange(write_options, "key1238", "key6301");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 25482: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2057", "key10653", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2057, key10653) size=" << scan_result.size() << std::endl;
  }

  // op 25483: put
  db->Put(write_options, "key6059", "value66019");

  // op 25484: put
  db->Put(write_options, "key2386", "value66412");

  // op 25485: delete_range
  status = db->DeleteRange(write_options, "key7968", "key9111");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 25486: put
  db->Put(write_options, "key11223", "value113756");

  // op 25487: put
  db->Put(write_options, "key10748", "value25864");

  // op 25488: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 25489: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9736", "key10470", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9736, key10470) size=" << scan_result.size() << std::endl;
  }

  // op 25490: put
  db->Put(write_options, "key10890", "value58787");

  // op 25491: delete_range
  status = db->DeleteRange(write_options, "key8801", "key10922");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 25492: delete_range
  status = db->DeleteRange(write_options, "key3101", "key11428");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 25493: delete
  db->Delete(write_options, "key1959");

  // op 25494: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 25495: delete_range
  status = db->DeleteRange(write_options, "key1131", "key3246");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 25496: delete
  db->Delete(write_options, "key5294");

  // op 25497: delete
  db->Delete(write_options, "key6402");

  // op 25498: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1465", "key4948", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1465, key4948) size=" << scan_result.size() << std::endl;
  }

  // op 25499: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 25500: get
  status = db->Get(read_options, "key7881", &value);
  if (status.ok()) {
    out << "GET key7881 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25501: put
  db->Put(write_options, "key1813", "value68875");

  // op 25502: get
  status = db->Get(read_options, "key7512", &value);
  if (status.ok()) {
    out << "GET key7512 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25503: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 25504: get
  status = db->Get(read_options, "key9006", &value);
  if (status.ok()) {
    out << "GET key9006 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25505: put
  db->Put(write_options, "key0218", "value27975");

  // op 25506: get
  status = db->Get(read_options, "key4816", &value);
  if (status.ok()) {
    out << "GET key4816 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25507: put
  db->Put(write_options, "key10401", "value100636");

  // op 25508: get
  status = db->Get(read_options, "key9707", &value);
  if (status.ok()) {
    out << "GET key9707 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25509: delete_range
  status = db->DeleteRange(write_options, "key8687", "key9804");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 25510: get
  status = db->Get(read_options, "key9578", &value);
  if (status.ok()) {
    out << "GET key9578 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25511: get
  status = db->Get(read_options, "key11200", &value);
  if (status.ok()) {
    out << "GET key11200 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25512: get
  status = db->Get(read_options, "key0006", &value);
  if (status.ok()) {
    out << "GET key0006 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25513: put
  db->Put(write_options, "key11046", "value12699");

  // op 25514: put
  db->Put(write_options, "key2677", "value65728");

  // op 25515: delete_range
  status = db->DeleteRange(write_options, "key2530", "key4094");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 25516: get
  status = db->Get(read_options, "key3680", &value);
  if (status.ok()) {
    out << "GET key3680 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25517: delete_range
  status = db->DeleteRange(write_options, "key8100", "key9202");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 25518: delete_range
  status = db->DeleteRange(write_options, "key0114", "key0466");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 25519: put
  db->Put(write_options, "key3680", "value2380");

  // op 25520: put
  db->Put(write_options, "key2807", "value47482");

  // op 25521: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6740", "key9405", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6740, key9405) size=" << scan_result.size() << std::endl;
  }

  // op 25522: delete
  db->Delete(write_options, "key11554");

  // op 25523: put
  db->Put(write_options, "key7858", "value52427");

  // op 25524: put
  db->Put(write_options, "key4506", "value77147");

  // op 25525: put
  db->Put(write_options, "key10296", "value27872");

  // op 25526: delete_range
  status = db->DeleteRange(write_options, "key6132", "key7028");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 25527: put
  db->Put(write_options, "key2724", "value21050");

  // op 25528: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3840", "key8152", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3840, key8152) size=" << scan_result.size() << std::endl;
  }

  // op 25529: delete_range
  status = db->DeleteRange(write_options, "key3668", "key11222");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 25530: put
  db->Put(write_options, "key7713", "value60739");

  // op 25531: put
  db->Put(write_options, "key5540", "value80122");

  // op 25532: put
  db->Put(write_options, "key10718", "value48523");

  // op 25533: put
  db->Put(write_options, "key4798", "value79345");

  // op 25534: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 25535: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 25536: put
  db->Put(write_options, "key10792", "value116406");

  // op 25537: put
  db->Put(write_options, "key3540", "value108018");

  // op 25538: put
  db->Put(write_options, "key4074", "value4083");

  // op 25539: get
  status = db->Get(read_options, "key4090", &value);
  if (status.ok()) {
    out << "GET key4090 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25540: put
  db->Put(write_options, "key4606", "value27554");

  // op 25541: get
  status = db->Get(read_options, "key5427", &value);
  if (status.ok()) {
    out << "GET key5427 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25542: get
  status = db->Get(read_options, "key8699", &value);
  if (status.ok()) {
    out << "GET key8699 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25543: get
  status = db->Get(read_options, "key0514", &value);
  if (status.ok()) {
    out << "GET key0514 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25544: put
  db->Put(write_options, "key10533", "value0350");

  // op 25545: put
  db->Put(write_options, "key5072", "value34376");

  // op 25546: put
  db->Put(write_options, "key4624", "value3866");

  // op 25547: delete_range
  status = db->DeleteRange(write_options, "key0940", "key1094");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 25548: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8248", "key11056", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8248, key11056) size=" << scan_result.size() << std::endl;
  }

  // op 25549: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 25550: get
  status = db->Get(read_options, "key2792", &value);
  if (status.ok()) {
    out << "GET key2792 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25551: put
  db->Put(write_options, "key11636", "value53148");

  // op 25552: put
  db->Put(write_options, "key4993", "value45904");

  // op 25553: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0270", "key4040", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0270, key4040) size=" << scan_result.size() << std::endl;
  }

  // op 25554: put
  db->Put(write_options, "key2127", "value5519");

  // op 25555: put
  db->Put(write_options, "key8360", "value85831");

  // op 25556: put
  db->Put(write_options, "key2929", "value74636");

  // op 25557: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7566", "key9162", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7566, key9162) size=" << scan_result.size() << std::endl;
  }

  // op 25558: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 25559: put
  db->Put(write_options, "key0428", "value111119");

  // op 25560: delete_range
  status = db->DeleteRange(write_options, "key2605", "key9431");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 25561: put
  db->Put(write_options, "key1434", "value58823");

  // op 25562: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5468", "key11600", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5468, key11600) size=" << scan_result.size() << std::endl;
  }

  // op 25563: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3799", "key5154", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3799, key5154) size=" << scan_result.size() << std::endl;
  }

  // op 25564: delete_range
  status = db->DeleteRange(write_options, "key0126", "key11006");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 25565: put
  db->Put(write_options, "key11377", "value67181");

  // op 25566: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 25567: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7960", "key8556", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7960, key8556) size=" << scan_result.size() << std::endl;
  }

  // op 25568: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5026", "key6066", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5026, key6066) size=" << scan_result.size() << std::endl;
  }

  // op 25569: put
  db->Put(write_options, "key3428", "value56542");

  // op 25570: put
  db->Put(write_options, "key5371", "value40230");

  // op 25571: delete
  db->Delete(write_options, "key2548");

  // op 25572: put
  db->Put(write_options, "key2276", "value64722");

  // op 25573: get
  status = db->Get(read_options, "key0406", &value);
  if (status.ok()) {
    out << "GET key0406 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25574: put
  db->Put(write_options, "key10221", "value68375");

  // op 25575: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0099", "key3899", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0099, key3899) size=" << scan_result.size() << std::endl;
  }

  // op 25576: put
  db->Put(write_options, "key4318", "value91074");

  // op 25577: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2707", "key7296", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2707, key7296) size=" << scan_result.size() << std::endl;
  }

  // op 25578: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5622", "key11448", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5622, key11448) size=" << scan_result.size() << std::endl;
  }

  // op 25579: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3713", "key9988", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3713, key9988) size=" << scan_result.size() << std::endl;
  }

  // op 25580: delete_range
  status = db->DeleteRange(write_options, "key8988", "key9400");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 25581: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8358", "key9425", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8358, key9425) size=" << scan_result.size() << std::endl;
  }

  // op 25582: get
  status = db->Get(read_options, "key4558", &value);
  if (status.ok()) {
    out << "GET key4558 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25583: put
  db->Put(write_options, "key9338", "value41022");

  // op 25584: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2206", "key7525", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2206, key7525) size=" << scan_result.size() << std::endl;
  }

  // op 25585: put
  db->Put(write_options, "key6431", "value26993");

  // op 25586: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 25587: delete
  db->Delete(write_options, "key7014");

  // op 25588: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7421", "key7452", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7421, key7452) size=" << scan_result.size() << std::endl;
  }

  // op 25589: delete
  db->Delete(write_options, "key9755");

  // op 25590: put
  db->Put(write_options, "key2022", "value24197");

  // op 25591: delete
  db->Delete(write_options, "key10015");

  // op 25592: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8930", "key10037", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8930, key10037) size=" << scan_result.size() << std::endl;
  }

  // op 25593: delete
  db->Delete(write_options, "key4964");

  // op 25594: put
  db->Put(write_options, "key6385", "value108124");

  // op 25595: put
  db->Put(write_options, "key8408", "value91772");

  // op 25596: get
  status = db->Get(read_options, "key0597", &value);
  if (status.ok()) {
    out << "GET key0597 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25597: put
  db->Put(write_options, "key9854", "value1505");

  // op 25598: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4731", "key6104", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4731, key6104) size=" << scan_result.size() << std::endl;
  }

  // op 25599: put
  db->Put(write_options, "key7893", "value1543");

  // op 25600: delete_range
  status = db->DeleteRange(write_options, "key2377", "key11427");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 25601: put
  db->Put(write_options, "key6075", "value48406");

  // op 25602: put
  db->Put(write_options, "key9605", "value35154");

  // op 25603: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 25604: put
  db->Put(write_options, "key6427", "value104530");

  // op 25605: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1821", "key6698", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1821, key6698) size=" << scan_result.size() << std::endl;
  }

  // op 25606: put
  db->Put(write_options, "key8104", "value105278");

  // op 25607: delete_range
  status = db->DeleteRange(write_options, "key0080", "key5691");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 25608: delete
  db->Delete(write_options, "key2719");

  // op 25609: put
  db->Put(write_options, "key7357", "value39894");

  // op 25610: put
  db->Put(write_options, "key11301", "value9828");

  // op 25611: put
  db->Put(write_options, "key10212", "value50486");

  // op 25612: put
  db->Put(write_options, "key7214", "value17353");

  // op 25613: put
  db->Put(write_options, "key7712", "value114751");

  // op 25614: put
  db->Put(write_options, "key8271", "value34160");

  // op 25615: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6492", "key7875", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6492, key7875) size=" << scan_result.size() << std::endl;
  }

  // op 25616: put
  db->Put(write_options, "key9365", "value23635");

  // op 25617: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5262", "key5964", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5262, key5964) size=" << scan_result.size() << std::endl;
  }

  // op 25618: get
  status = db->Get(read_options, "key3970", &value);
  if (status.ok()) {
    out << "GET key3970 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25619: get
  status = db->Get(read_options, "key2516", &value);
  if (status.ok()) {
    out << "GET key2516 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25620: put
  db->Put(write_options, "key4194", "value51088");

  // op 25621: get
  status = db->Get(read_options, "key11490", &value);
  if (status.ok()) {
    out << "GET key11490 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25622: delete
  db->Delete(write_options, "key2138");

  // op 25623: put
  db->Put(write_options, "key7890", "value28114");

  // op 25624: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0458", "key9084", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0458, key9084) size=" << scan_result.size() << std::endl;
  }

  // op 25625: get
  status = db->Get(read_options, "key1625", &value);
  if (status.ok()) {
    out << "GET key1625 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25626: put
  db->Put(write_options, "key4167", "value31276");

  // op 25627: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2708", "key6458", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2708, key6458) size=" << scan_result.size() << std::endl;
  }

  // op 25628: put
  db->Put(write_options, "key10670", "value97862");

  // op 25629: delete_range
  status = db->DeleteRange(write_options, "key0036", "key0037");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 25630: delete
  db->Delete(write_options, "key6471");

  // op 25631: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3300", "key10409", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3300, key10409) size=" << scan_result.size() << std::endl;
  }

  // op 25632: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3167", "key8780", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3167, key8780) size=" << scan_result.size() << std::endl;
  }

  // op 25633: delete_range
  status = db->DeleteRange(write_options, "key2805", "key4762");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 25634: get
  status = db->Get(read_options, "key0565", &value);
  if (status.ok()) {
    out << "GET key0565 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25635: put
  db->Put(write_options, "key3534", "value44823");

  // op 25636: delete
  db->Delete(write_options, "key9843");

  // op 25637: put
  db->Put(write_options, "key11651", "value55134");

  // op 25638: put
  db->Put(write_options, "key10172", "value21989");

  // op 25639: delete
  db->Delete(write_options, "key10294");

  // op 25640: put
  db->Put(write_options, "key9074", "value118032");

  // op 25641: put
  db->Put(write_options, "key9919", "value54819");

  // op 25642: delete_range
  status = db->DeleteRange(write_options, "key5448", "key5982");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 25643: put
  db->Put(write_options, "key2680", "value114617");

  // op 25644: get
  status = db->Get(read_options, "key11171", &value);
  if (status.ok()) {
    out << "GET key11171 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25645: get
  status = db->Get(read_options, "key9075", &value);
  if (status.ok()) {
    out << "GET key9075 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25646: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 25647: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 25648: delete_range
  status = db->DeleteRange(write_options, "key3308", "key4158");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 25649: put
  db->Put(write_options, "key2121", "value26115");

  // op 25650: put
  db->Put(write_options, "key10522", "value106870");

  // op 25651: get
  status = db->Get(read_options, "key6683", &value);
  if (status.ok()) {
    out << "GET key6683 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25652: put
  db->Put(write_options, "key1285", "value48488");

  // op 25653: get
  status = db->Get(read_options, "key1325", &value);
  if (status.ok()) {
    out << "GET key1325 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25654: put
  db->Put(write_options, "key1126", "value118221");

  // op 25655: delete
  db->Delete(write_options, "key7255");

  // op 25656: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 25657: put
  db->Put(write_options, "key3555", "value54718");

  // op 25658: put
  db->Put(write_options, "key10629", "value96302");

  // op 25659: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 25660: delete_range
  status = db->DeleteRange(write_options, "key7446", "key8823");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 25661: put
  db->Put(write_options, "key2786", "value41158");

  // op 25662: put
  db->Put(write_options, "key3983", "value19694");

  // op 25663: put
  db->Put(write_options, "key1872", "value33627");

  // op 25664: put
  db->Put(write_options, "key2741", "value34173");

  // op 25665: delete
  db->Delete(write_options, "key1591");

  // op 25666: get
  status = db->Get(read_options, "key7076", &value);
  if (status.ok()) {
    out << "GET key7076 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25667: put
  db->Put(write_options, "key8945", "value75095");

  // op 25668: put
  db->Put(write_options, "key7018", "value36819");

  // op 25669: get
  status = db->Get(read_options, "key7062", &value);
  if (status.ok()) {
    out << "GET key7062 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25670: put
  db->Put(write_options, "key2743", "value94887");

  // op 25671: put
  db->Put(write_options, "key4338", "value111994");

  // op 25672: get
  status = db->Get(read_options, "key7543", &value);
  if (status.ok()) {
    out << "GET key7543 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25673: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0757", "key3017", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0757, key3017) size=" << scan_result.size() << std::endl;
  }

  // op 25674: put
  db->Put(write_options, "key11135", "value43612");

  // op 25675: put
  db->Put(write_options, "key6986", "value112787");

  // op 25676: get
  status = db->Get(read_options, "key9036", &value);
  if (status.ok()) {
    out << "GET key9036 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25677: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4346", "key11277", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4346, key11277) size=" << scan_result.size() << std::endl;
  }

  // op 25678: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3599", "key4907", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3599, key4907) size=" << scan_result.size() << std::endl;
  }

  // op 25679: delete
  db->Delete(write_options, "key1094");

  // op 25680: put
  db->Put(write_options, "key8109", "value33693");

  // op 25681: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2392", "key7425", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2392, key7425) size=" << scan_result.size() << std::endl;
  }

  // op 25682: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0712", "key7377", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0712, key7377) size=" << scan_result.size() << std::endl;
  }

  // op 25683: put
  db->Put(write_options, "key0711", "value107817");

  // op 25684: put
  db->Put(write_options, "key8334", "value11453");

  // op 25685: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 25686: get
  status = db->Get(read_options, "key10398", &value);
  if (status.ok()) {
    out << "GET key10398 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25687: delete_range
  status = db->DeleteRange(write_options, "key7215", "key8563");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 25688: get
  status = db->Get(read_options, "key1620", &value);
  if (status.ok()) {
    out << "GET key1620 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25689: put
  db->Put(write_options, "key8809", "value50890");

  // op 25690: get
  status = db->Get(read_options, "key3045", &value);
  if (status.ok()) {
    out << "GET key3045 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25691: put
  db->Put(write_options, "key7978", "value82707");

  // op 25692: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4757", "key10120", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4757, key10120) size=" << scan_result.size() << std::endl;
  }

  // op 25693: put
  db->Put(write_options, "key5033", "value58976");

  // op 25694: get
  status = db->Get(read_options, "key0139", &value);
  if (status.ok()) {
    out << "GET key0139 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25695: delete_range
  status = db->DeleteRange(write_options, "key9577", "key11380");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 25696: put
  db->Put(write_options, "key2046", "value112129");

  // op 25697: put
  db->Put(write_options, "key3722", "value18416");

  // op 25698: put
  db->Put(write_options, "key4259", "value66445");

  // op 25699: delete_range
  status = db->DeleteRange(write_options, "key1833", "key11421");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 25700: delete
  db->Delete(write_options, "key5961");

  // op 25701: get
  status = db->Get(read_options, "key8368", &value);
  if (status.ok()) {
    out << "GET key8368 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25702: put
  db->Put(write_options, "key8047", "value40215");

  // op 25703: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2106", "key5139", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2106, key5139) size=" << scan_result.size() << std::endl;
  }

  // op 25704: get
  status = db->Get(read_options, "key2531", &value);
  if (status.ok()) {
    out << "GET key2531 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25705: put
  db->Put(write_options, "key11718", "value17202");

  // op 25706: get
  status = db->Get(read_options, "key5816", &value);
  if (status.ok()) {
    out << "GET key5816 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25707: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8288", "key9736", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8288, key9736) size=" << scan_result.size() << std::endl;
  }

  // op 25708: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7997", "key10226", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7997, key10226) size=" << scan_result.size() << std::endl;
  }

  // op 25709: put
  db->Put(write_options, "key11682", "value83846");

  // op 25710: put
  db->Put(write_options, "key3772", "value98487");

  // op 25711: put
  db->Put(write_options, "key9619", "value10609");

  // op 25712: get
  status = db->Get(read_options, "key2191", &value);
  if (status.ok()) {
    out << "GET key2191 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25713: put
  db->Put(write_options, "key10615", "value40483");

  // op 25714: delete_range
  status = db->DeleteRange(write_options, "key2956", "key9874");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 25715: put
  db->Put(write_options, "key9394", "value8595");

  // op 25716: put
  db->Put(write_options, "key4089", "value54435");

  // op 25717: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4794", "key5910", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4794, key5910) size=" << scan_result.size() << std::endl;
  }

  // op 25718: delete
  db->Delete(write_options, "key10420");

  // op 25719: put
  db->Put(write_options, "key6505", "value101538");

  // op 25720: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1293", "key5592", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1293, key5592) size=" << scan_result.size() << std::endl;
  }

  // op 25721: put
  db->Put(write_options, "key5775", "value101517");

  // op 25722: get
  status = db->Get(read_options, "key1637", &value);
  if (status.ok()) {
    out << "GET key1637 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25723: delete_range
  status = db->DeleteRange(write_options, "key6443", "key11580");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 25724: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9532", "key10403", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9532, key10403) size=" << scan_result.size() << std::endl;
  }

  // op 25725: get
  status = db->Get(read_options, "key1517", &value);
  if (status.ok()) {
    out << "GET key1517 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25726: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6567", "key8601", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6567, key8601) size=" << scan_result.size() << std::endl;
  }

  // op 25727: delete
  db->Delete(write_options, "key3306");

  // op 25728: put
  db->Put(write_options, "key4813", "value113569");

  // op 25729: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 25730: put
  db->Put(write_options, "key0606", "value91009");

  // op 25731: delete_range
  status = db->DeleteRange(write_options, "key0547", "key8946");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 25732: delete
  db->Delete(write_options, "key8052");

  // op 25733: put
  db->Put(write_options, "key6776", "value84585");

  // op 25734: put
  db->Put(write_options, "key4235", "value67653");

  // op 25735: get
  status = db->Get(read_options, "key9964", &value);
  if (status.ok()) {
    out << "GET key9964 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25736: get
  status = db->Get(read_options, "key1224", &value);
  if (status.ok()) {
    out << "GET key1224 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25737: put
  db->Put(write_options, "key0110", "value116131");

  // op 25738: get
  status = db->Get(read_options, "key0262", &value);
  if (status.ok()) {
    out << "GET key0262 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25739: put
  db->Put(write_options, "key2641", "value97799");

  // op 25740: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 25741: put
  db->Put(write_options, "key11367", "value73642");

  // op 25742: get
  status = db->Get(read_options, "key1069", &value);
  if (status.ok()) {
    out << "GET key1069 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25743: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3926", "key6943", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3926, key6943) size=" << scan_result.size() << std::endl;
  }

  // op 25744: put
  db->Put(write_options, "key2802", "value54809");

  // op 25745: put
  db->Put(write_options, "key1968", "value12291");

  // op 25746: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1137", "key9726", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1137, key9726) size=" << scan_result.size() << std::endl;
  }

  // op 25747: get
  status = db->Get(read_options, "key10595", &value);
  if (status.ok()) {
    out << "GET key10595 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25748: delete_range
  status = db->DeleteRange(write_options, "key1382", "key2520");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 25749: get
  status = db->Get(read_options, "key2112", &value);
  if (status.ok()) {
    out << "GET key2112 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25750: get
  status = db->Get(read_options, "key1186", &value);
  if (status.ok()) {
    out << "GET key1186 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25751: put
  db->Put(write_options, "key9395", "value87025");

  // op 25752: put
  db->Put(write_options, "key3249", "value19064");

  // op 25753: put
  db->Put(write_options, "key5306", "value50433");

  // op 25754: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 25755: get
  status = db->Get(read_options, "key3307", &value);
  if (status.ok()) {
    out << "GET key3307 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25756: delete
  db->Delete(write_options, "key0098");

  // op 25757: put
  db->Put(write_options, "key7941", "value31658");

  // op 25758: put
  db->Put(write_options, "key8441", "value68169");

  // op 25759: delete_range
  status = db->DeleteRange(write_options, "key0125", "key7233");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 25760: put
  db->Put(write_options, "key11540", "value6795");

  // op 25761: delete
  db->Delete(write_options, "key9886");

  // op 25762: get
  status = db->Get(read_options, "key3520", &value);
  if (status.ok()) {
    out << "GET key3520 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25763: get
  status = db->Get(read_options, "key5465", &value);
  if (status.ok()) {
    out << "GET key5465 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25764: delete_range
  status = db->DeleteRange(write_options, "key4023", "key9477");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 25765: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1163", "key9900", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1163, key9900) size=" << scan_result.size() << std::endl;
  }

  // op 25766: put
  db->Put(write_options, "key8980", "value15137");

  // op 25767: put
  db->Put(write_options, "key8653", "value18360");

  // op 25768: delete_range
  status = db->DeleteRange(write_options, "key4589", "key6670");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 25769: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3670", "key7067", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3670, key7067) size=" << scan_result.size() << std::endl;
  }

  // op 25770: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8211", "key9598", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8211, key9598) size=" << scan_result.size() << std::endl;
  }

  // op 25771: put
  db->Put(write_options, "key9963", "value55586");

  // op 25772: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7402", "key11417", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7402, key11417) size=" << scan_result.size() << std::endl;
  }

  // op 25773: put
  db->Put(write_options, "key1548", "value23880");

  // op 25774: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4552", "key5430", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4552, key5430) size=" << scan_result.size() << std::endl;
  }

  // op 25775: get
  status = db->Get(read_options, "key10393", &value);
  if (status.ok()) {
    out << "GET key10393 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25776: put
  db->Put(write_options, "key7289", "value102090");

  // op 25777: get
  status = db->Get(read_options, "key10912", &value);
  if (status.ok()) {
    out << "GET key10912 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25778: put
  db->Put(write_options, "key4923", "value116986");

  // op 25779: delete
  db->Delete(write_options, "key4058");

  // op 25780: put
  db->Put(write_options, "key0683", "value93070");

  // op 25781: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 25782: delete
  db->Delete(write_options, "key3439");

  // op 25783: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1671", "key5455", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1671, key5455) size=" << scan_result.size() << std::endl;
  }

  // op 25784: put
  db->Put(write_options, "key6434", "value106470");

  // op 25785: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 25786: put
  db->Put(write_options, "key11278", "value55248");

  // op 25787: put
  db->Put(write_options, "key0723", "value81691");

  // op 25788: delete
  db->Delete(write_options, "key6847");

  // op 25789: put
  db->Put(write_options, "key4462", "value95831");

  // op 25790: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7179", "key10966", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7179, key10966) size=" << scan_result.size() << std::endl;
  }

  // op 25791: put
  db->Put(write_options, "key0323", "value45787");

  // op 25792: put
  db->Put(write_options, "key11021", "value60480");

  // op 25793: get
  status = db->Get(read_options, "key6349", &value);
  if (status.ok()) {
    out << "GET key6349 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25794: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8279", "key9678", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8279, key9678) size=" << scan_result.size() << std::endl;
  }

  // op 25795: put
  db->Put(write_options, "key5892", "value17319");

  // op 25796: put
  db->Put(write_options, "key6673", "value66703");

  // op 25797: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1577", "key2486", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1577, key2486) size=" << scan_result.size() << std::endl;
  }

  // op 25798: delete
  db->Delete(write_options, "key5764");

  // op 25799: delete_range
  status = db->DeleteRange(write_options, "key2494", "key7617");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 25800: put
  db->Put(write_options, "key0070", "value15257");

  // op 25801: put
  db->Put(write_options, "key8609", "value113460");

  // op 25802: get
  status = db->Get(read_options, "key9860", &value);
  if (status.ok()) {
    out << "GET key9860 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25803: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2557", "key6206", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2557, key6206) size=" << scan_result.size() << std::endl;
  }

  // op 25804: get
  status = db->Get(read_options, "key5201", &value);
  if (status.ok()) {
    out << "GET key5201 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25805: get
  status = db->Get(read_options, "key8215", &value);
  if (status.ok()) {
    out << "GET key8215 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25806: put
  db->Put(write_options, "key1039", "value46223");

  // op 25807: get
  status = db->Get(read_options, "key2732", &value);
  if (status.ok()) {
    out << "GET key2732 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25808: put
  db->Put(write_options, "key8080", "value94139");

  // op 25809: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3368", "key6047", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3368, key6047) size=" << scan_result.size() << std::endl;
  }

  // op 25810: put
  db->Put(write_options, "key9114", "value37414");

  // op 25811: delete
  db->Delete(write_options, "key7591");

  // op 25812: put
  db->Put(write_options, "key2830", "value64010");

  // op 25813: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0557", "key1738", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0557, key1738) size=" << scan_result.size() << std::endl;
  }

  // op 25814: put
  db->Put(write_options, "key11494", "value1093");

  // op 25815: put
  db->Put(write_options, "key8366", "value106454");

  // op 25816: get
  status = db->Get(read_options, "key9138", &value);
  if (status.ok()) {
    out << "GET key9138 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25817: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1399", "key10161", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1399, key10161) size=" << scan_result.size() << std::endl;
  }

  // op 25818: delete
  db->Delete(write_options, "key5059");

  // op 25819: put
  db->Put(write_options, "key6030", "value49799");

  // op 25820: put
  db->Put(write_options, "key6347", "value82332");

  // op 25821: get
  status = db->Get(read_options, "key11534", &value);
  if (status.ok()) {
    out << "GET key11534 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25822: delete
  db->Delete(write_options, "key7401");

  // op 25823: delete_range
  status = db->DeleteRange(write_options, "key0117", "key8420");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 25824: put
  db->Put(write_options, "key3733", "value92745");

  // op 25825: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 25826: put
  db->Put(write_options, "key10330", "value10023");

  // op 25827: get
  status = db->Get(read_options, "key5413", &value);
  if (status.ok()) {
    out << "GET key5413 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25828: delete
  db->Delete(write_options, "key10396");

  // op 25829: put
  db->Put(write_options, "key4496", "value74625");

  // op 25830: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 25831: get
  status = db->Get(read_options, "key1425", &value);
  if (status.ok()) {
    out << "GET key1425 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25832: put
  db->Put(write_options, "key10488", "value50119");

  // op 25833: put
  db->Put(write_options, "key8730", "value73286");

  // op 25834: get
  status = db->Get(read_options, "key1763", &value);
  if (status.ok()) {
    out << "GET key1763 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25835: get
  status = db->Get(read_options, "key10787", &value);
  if (status.ok()) {
    out << "GET key10787 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25836: get
  status = db->Get(read_options, "key3346", &value);
  if (status.ok()) {
    out << "GET key3346 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25837: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 25838: put
  db->Put(write_options, "key6572", "value106656");

  // op 25839: put
  db->Put(write_options, "key10498", "value50081");

  // op 25840: get
  status = db->Get(read_options, "key9733", &value);
  if (status.ok()) {
    out << "GET key9733 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25841: put
  db->Put(write_options, "key0041", "value25901");

  // op 25842: put
  db->Put(write_options, "key8439", "value3205");

  // op 25843: get
  status = db->Get(read_options, "key2336", &value);
  if (status.ok()) {
    out << "GET key2336 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25844: get
  status = db->Get(read_options, "key6371", &value);
  if (status.ok()) {
    out << "GET key6371 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25845: delete_range
  status = db->DeleteRange(write_options, "key0483", "key11240");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 25846: delete
  db->Delete(write_options, "key5398");

  // op 25847: get
  status = db->Get(read_options, "key6058", &value);
  if (status.ok()) {
    out << "GET key6058 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25848: delete_range
  status = db->DeleteRange(write_options, "key5603", "key7566");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 25849: put
  db->Put(write_options, "key1308", "value38957");

  // op 25850: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0069", "key2447", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0069, key2447) size=" << scan_result.size() << std::endl;
  }

  // op 25851: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3527", "key7823", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3527, key7823) size=" << scan_result.size() << std::endl;
  }

  // op 25852: get
  status = db->Get(read_options, "key8009", &value);
  if (status.ok()) {
    out << "GET key8009 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25853: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6208", "key9701", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6208, key9701) size=" << scan_result.size() << std::endl;
  }

  // op 25854: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2886", "key7918", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2886, key7918) size=" << scan_result.size() << std::endl;
  }

  // op 25855: get
  status = db->Get(read_options, "key0962", &value);
  if (status.ok()) {
    out << "GET key0962 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25856: get
  status = db->Get(read_options, "key6338", &value);
  if (status.ok()) {
    out << "GET key6338 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25857: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8356", "key10973", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8356, key10973) size=" << scan_result.size() << std::endl;
  }

  // op 25858: get
  status = db->Get(read_options, "key4689", &value);
  if (status.ok()) {
    out << "GET key4689 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25859: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8966", "key9598", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8966, key9598) size=" << scan_result.size() << std::endl;
  }

  // op 25860: get
  status = db->Get(read_options, "key1827", &value);
  if (status.ok()) {
    out << "GET key1827 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25861: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 25862: delete_range
  status = db->DeleteRange(write_options, "key2362", "key6639");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 25863: delete
  db->Delete(write_options, "key0555");

  // op 25864: get
  status = db->Get(read_options, "key2316", &value);
  if (status.ok()) {
    out << "GET key2316 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25865: delete
  db->Delete(write_options, "key10946");

  // op 25866: get
  status = db->Get(read_options, "key6277", &value);
  if (status.ok()) {
    out << "GET key6277 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25867: put
  db->Put(write_options, "key8018", "value42287");

  // op 25868: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 25869: get
  status = db->Get(read_options, "key6897", &value);
  if (status.ok()) {
    out << "GET key6897 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25870: put
  db->Put(write_options, "key6765", "value18880");

  // op 25871: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6343", "key11549", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6343, key11549) size=" << scan_result.size() << std::endl;
  }

  // op 25872: put
  db->Put(write_options, "key0272", "value53732");

  // op 25873: put
  db->Put(write_options, "key11034", "value99029");

  // op 25874: get
  status = db->Get(read_options, "key1724", &value);
  if (status.ok()) {
    out << "GET key1724 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25875: put
  db->Put(write_options, "key4949", "value66733");

  // op 25876: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3126", "key3858", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3126, key3858) size=" << scan_result.size() << std::endl;
  }

  // op 25877: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9705", "key11004", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9705, key11004) size=" << scan_result.size() << std::endl;
  }

  // op 25878: delete_range
  status = db->DeleteRange(write_options, "key4501", "key7737");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 25879: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 25880: put
  db->Put(write_options, "key10767", "value63350");

  // op 25881: delete
  db->Delete(write_options, "key1258");

  // op 25882: delete_range
  status = db->DeleteRange(write_options, "key2881", "key3324");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 25883: get
  status = db->Get(read_options, "key5666", &value);
  if (status.ok()) {
    out << "GET key5666 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25884: get
  status = db->Get(read_options, "key4987", &value);
  if (status.ok()) {
    out << "GET key4987 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25885: put
  db->Put(write_options, "key9175", "value102788");

  // op 25886: get
  status = db->Get(read_options, "key5164", &value);
  if (status.ok()) {
    out << "GET key5164 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25887: get
  status = db->Get(read_options, "key1596", &value);
  if (status.ok()) {
    out << "GET key1596 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25888: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4820", "key6607", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4820, key6607) size=" << scan_result.size() << std::endl;
  }

  // op 25889: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1650", "key7655", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1650, key7655) size=" << scan_result.size() << std::endl;
  }

  // op 25890: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0464", "key7828", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0464, key7828) size=" << scan_result.size() << std::endl;
  }

  // op 25891: put
  db->Put(write_options, "key1498", "value31245");

  // op 25892: put
  db->Put(write_options, "key8156", "value111275");

  // op 25893: put
  db->Put(write_options, "key6549", "value6967");

  // op 25894: put
  db->Put(write_options, "key1772", "value0681");

  // op 25895: delete
  db->Delete(write_options, "key10087");

  // op 25896: get
  status = db->Get(read_options, "key8561", &value);
  if (status.ok()) {
    out << "GET key8561 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25897: put
  db->Put(write_options, "key11403", "value20089");

  // op 25898: put
  db->Put(write_options, "key5559", "value61227");

  // op 25899: delete_range
  status = db->DeleteRange(write_options, "key1173", "key10960");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 25900: put
  db->Put(write_options, "key4824", "value21911");

  // op 25901: put
  db->Put(write_options, "key1051", "value83031");

  // op 25902: get
  status = db->Get(read_options, "key1507", &value);
  if (status.ok()) {
    out << "GET key1507 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25903: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7413", "key9433", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7413, key9433) size=" << scan_result.size() << std::endl;
  }

  // op 25904: get
  status = db->Get(read_options, "key9940", &value);
  if (status.ok()) {
    out << "GET key9940 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25905: put
  db->Put(write_options, "key0792", "value5971");

  // op 25906: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 25907: delete
  db->Delete(write_options, "key0056");

  // op 25908: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5844", "key9349", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5844, key9349) size=" << scan_result.size() << std::endl;
  }

  // op 25909: put
  db->Put(write_options, "key0381", "value29937");

  // op 25910: put
  db->Put(write_options, "key10850", "value100873");

  // op 25911: put
  db->Put(write_options, "key0580", "value19226");

  // op 25912: put
  db->Put(write_options, "key1985", "value70012");

  // op 25913: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 25914: delete
  db->Delete(write_options, "key11428");

  // op 25915: get
  status = db->Get(read_options, "key5320", &value);
  if (status.ok()) {
    out << "GET key5320 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25916: delete_range
  status = db->DeleteRange(write_options, "key0283", "key1615");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 25917: delete_range
  status = db->DeleteRange(write_options, "key7424", "key10520");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 25918: delete
  db->Delete(write_options, "key4741");

  // op 25919: put
  db->Put(write_options, "key2530", "value62559");

  // op 25920: put
  db->Put(write_options, "key10207", "value30255");

  // op 25921: put
  db->Put(write_options, "key5985", "value27094");

  // op 25922: put
  db->Put(write_options, "key4264", "value51571");

  // op 25923: get
  status = db->Get(read_options, "key4507", &value);
  if (status.ok()) {
    out << "GET key4507 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25924: put
  db->Put(write_options, "key3241", "value109666");

  // op 25925: put
  db->Put(write_options, "key9722", "value88052");

  // op 25926: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3920", "key6325", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3920, key6325) size=" << scan_result.size() << std::endl;
  }

  // op 25927: get
  status = db->Get(read_options, "key1353", &value);
  if (status.ok()) {
    out << "GET key1353 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25928: put
  db->Put(write_options, "key0730", "value31783");

  // op 25929: delete
  db->Delete(write_options, "key2780");

  // op 25930: get
  status = db->Get(read_options, "key7094", &value);
  if (status.ok()) {
    out << "GET key7094 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25931: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5922", "key6609", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5922, key6609) size=" << scan_result.size() << std::endl;
  }

  // op 25932: put
  db->Put(write_options, "key8781", "value14059");

  // op 25933: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 25934: delete_range
  status = db->DeleteRange(write_options, "key1836", "key7597");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 25935: put
  db->Put(write_options, "key10351", "value64958");

  // op 25936: put
  db->Put(write_options, "key5930", "value84995");

  // op 25937: put
  db->Put(write_options, "key10377", "value98312");

  // op 25938: put
  db->Put(write_options, "key8921", "value105317");

  // op 25939: get
  status = db->Get(read_options, "key8599", &value);
  if (status.ok()) {
    out << "GET key8599 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25940: put
  db->Put(write_options, "key3155", "value90970");

  // op 25941: put
  db->Put(write_options, "key10078", "value45162");

  // op 25942: delete_range
  status = db->DeleteRange(write_options, "key9948", "key11408");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 25943: put
  db->Put(write_options, "key1260", "value104876");

  // op 25944: put
  db->Put(write_options, "key6396", "value38906");

  // op 25945: delete_range
  status = db->DeleteRange(write_options, "key2155", "key5350");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 25946: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1165", "key3416", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1165, key3416) size=" << scan_result.size() << std::endl;
  }

  // op 25947: delete
  db->Delete(write_options, "key11036");

  // op 25948: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0581", "key5375", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0581, key5375) size=" << scan_result.size() << std::endl;
  }

  // op 25949: delete
  db->Delete(write_options, "key1807");

  // op 25950: put
  db->Put(write_options, "key2039", "value86173");

  // op 25951: get
  status = db->Get(read_options, "key11642", &value);
  if (status.ok()) {
    out << "GET key11642 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25952: delete_range
  status = db->DeleteRange(write_options, "key1079", "key9734");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 25953: put
  db->Put(write_options, "key2473", "value48031");

  // op 25954: get
  status = db->Get(read_options, "key9083", &value);
  if (status.ok()) {
    out << "GET key9083 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25955: put
  db->Put(write_options, "key5027", "value85023");

  // op 25956: get
  status = db->Get(read_options, "key3561", &value);
  if (status.ok()) {
    out << "GET key3561 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25957: put
  db->Put(write_options, "key9325", "value52205");

  // op 25958: put
  db->Put(write_options, "key9814", "value82642");

  // op 25959: delete
  db->Delete(write_options, "key6573");

  // op 25960: get
  status = db->Get(read_options, "key0758", &value);
  if (status.ok()) {
    out << "GET key0758 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25961: put
  db->Put(write_options, "key5266", "value32243");

  // op 25962: put
  db->Put(write_options, "key6634", "value40485");

  // op 25963: put
  db->Put(write_options, "key3864", "value55530");

  // op 25964: put
  db->Put(write_options, "key3406", "value53265");

  // op 25965: put
  db->Put(write_options, "key8725", "value116549");

  // op 25966: get
  status = db->Get(read_options, "key2541", &value);
  if (status.ok()) {
    out << "GET key2541 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25967: delete_range
  status = db->DeleteRange(write_options, "key0632", "key0692");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 25968: delete
  db->Delete(write_options, "key3707");

  // op 25969: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0269", "key3906", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0269, key3906) size=" << scan_result.size() << std::endl;
  }

  // op 25970: put
  db->Put(write_options, "key10436", "value102048");

  // op 25971: put
  db->Put(write_options, "key11182", "value115277");

  // op 25972: put
  db->Put(write_options, "key1242", "value80855");

  // op 25973: delete_range
  status = db->DeleteRange(write_options, "key8616", "key10402");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 25974: get
  status = db->Get(read_options, "key8176", &value);
  if (status.ok()) {
    out << "GET key8176 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25975: put
  db->Put(write_options, "key3600", "value2101");

  // op 25976: delete
  db->Delete(write_options, "key11623");

  // op 25977: put
  db->Put(write_options, "key9453", "value10109");

  // op 25978: delete_range
  status = db->DeleteRange(write_options, "key5727", "key5911");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 25979: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 25980: get
  status = db->Get(read_options, "key0028", &value);
  if (status.ok()) {
    out << "GET key0028 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25981: put
  db->Put(write_options, "key7647", "value18415");

  // op 25982: get
  status = db->Get(read_options, "key1752", &value);
  if (status.ok()) {
    out << "GET key1752 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25983: put
  db->Put(write_options, "key8784", "value117465");

  // op 25984: get
  status = db->Get(read_options, "key2916", &value);
  if (status.ok()) {
    out << "GET key2916 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25985: put
  db->Put(write_options, "key8488", "value21908");

  // op 25986: put
  db->Put(write_options, "key6019", "value95290");

  // op 25987: put
  db->Put(write_options, "key2754", "value46062");

  // op 25988: get
  status = db->Get(read_options, "key9127", &value);
  if (status.ok()) {
    out << "GET key9127 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25989: get
  status = db->Get(read_options, "key7756", &value);
  if (status.ok()) {
    out << "GET key7756 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25990: put
  db->Put(write_options, "key7345", "value63598");

  // op 25991: put
  db->Put(write_options, "key3977", "value56646");

  // op 25992: get
  status = db->Get(read_options, "key6653", &value);
  if (status.ok()) {
    out << "GET key6653 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25993: put
  db->Put(write_options, "key5626", "value11527");

  // op 25994: put
  db->Put(write_options, "key4903", "value79967");

  // op 25995: put
  db->Put(write_options, "key5923", "value40384");

  // op 25996: put
  db->Put(write_options, "key5046", "value12430");

  // op 25997: put
  db->Put(write_options, "key0213", "value50925");

  // op 25998: get
  status = db->Get(read_options, "key10667", &value);
  if (status.ok()) {
    out << "GET key10667 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 25999: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2008", "key5233", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2008, key5233) size=" << scan_result.size() << std::endl;
  }

  // op 26000: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2863", "key4655", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2863, key4655) size=" << scan_result.size() << std::endl;
  }

  // op 26001: get
  status = db->Get(read_options, "key10787", &value);
  if (status.ok()) {
    out << "GET key10787 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26002: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 26003: get
  status = db->Get(read_options, "key3851", &value);
  if (status.ok()) {
    out << "GET key3851 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26004: put
  db->Put(write_options, "key10885", "value54229");

  // op 26005: put
  db->Put(write_options, "key10491", "value45278");

  // op 26006: delete
  db->Delete(write_options, "key3737");

  // op 26007: get
  status = db->Get(read_options, "key8219", &value);
  if (status.ok()) {
    out << "GET key8219 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26008: put
  db->Put(write_options, "key2358", "value110496");

  // op 26009: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 26010: get
  status = db->Get(read_options, "key3577", &value);
  if (status.ok()) {
    out << "GET key3577 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26011: put
  db->Put(write_options, "key11697", "value10306");

  // op 26012: get
  status = db->Get(read_options, "key10527", &value);
  if (status.ok()) {
    out << "GET key10527 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26013: get
  status = db->Get(read_options, "key5971", &value);
  if (status.ok()) {
    out << "GET key5971 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26014: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7081", "key9563", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7081, key9563) size=" << scan_result.size() << std::endl;
  }

  // op 26015: put
  db->Put(write_options, "key4008", "value19756");

  // op 26016: put
  db->Put(write_options, "key2345", "value48939");

  // op 26017: put
  db->Put(write_options, "key5201", "value10327");

  // op 26018: get
  status = db->Get(read_options, "key9239", &value);
  if (status.ok()) {
    out << "GET key9239 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26019: put
  db->Put(write_options, "key3283", "value4136");

  // op 26020: put
  db->Put(write_options, "key4875", "value52875");

  // op 26021: put
  db->Put(write_options, "key11347", "value96338");

  // op 26022: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2594", "key4683", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2594, key4683) size=" << scan_result.size() << std::endl;
  }

  // op 26023: delete_range
  status = db->DeleteRange(write_options, "key1546", "key6494");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 26024: delete
  db->Delete(write_options, "key9526");

  // op 26025: delete_range
  status = db->DeleteRange(write_options, "key0442", "key6299");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 26026: put
  db->Put(write_options, "key10829", "value10334");

  // op 26027: delete
  db->Delete(write_options, "key6999");

  // op 26028: delete_range
  status = db->DeleteRange(write_options, "key4822", "key6751");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 26029: put
  db->Put(write_options, "key2585", "value30554");

  // op 26030: get
  status = db->Get(read_options, "key0910", &value);
  if (status.ok()) {
    out << "GET key0910 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26031: put
  db->Put(write_options, "key1773", "value96267");

  // op 26032: put
  db->Put(write_options, "key1038", "value21255");

  // op 26033: put
  db->Put(write_options, "key3451", "value108684");

  // op 26034: put
  db->Put(write_options, "key0333", "value48692");

  // op 26035: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 26036: put
  db->Put(write_options, "key11180", "value28703");

  // op 26037: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4598", "key5024", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4598, key5024) size=" << scan_result.size() << std::endl;
  }

  // op 26038: put
  db->Put(write_options, "key1062", "value97948");

  // op 26039: get
  status = db->Get(read_options, "key3694", &value);
  if (status.ok()) {
    out << "GET key3694 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26040: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9563", "key11264", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9563, key11264) size=" << scan_result.size() << std::endl;
  }

  // op 26041: put
  db->Put(write_options, "key4896", "value84606");

  // op 26042: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0517", "key8465", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0517, key8465) size=" << scan_result.size() << std::endl;
  }

  // op 26043: get
  status = db->Get(read_options, "key6668", &value);
  if (status.ok()) {
    out << "GET key6668 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26044: delete
  db->Delete(write_options, "key5066");

  // op 26045: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5000", "key6032", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5000, key6032) size=" << scan_result.size() << std::endl;
  }

  // op 26046: put
  db->Put(write_options, "key7376", "value55471");

  // op 26047: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6533", "key8678", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6533, key8678) size=" << scan_result.size() << std::endl;
  }

  // op 26048: get
  status = db->Get(read_options, "key4950", &value);
  if (status.ok()) {
    out << "GET key4950 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26049: get
  status = db->Get(read_options, "key10274", &value);
  if (status.ok()) {
    out << "GET key10274 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26050: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0991", "key8791", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0991, key8791) size=" << scan_result.size() << std::endl;
  }

  // op 26051: delete
  db->Delete(write_options, "key11272");

  // op 26052: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5919", "key7363", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5919, key7363) size=" << scan_result.size() << std::endl;
  }

  // op 26053: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6200", "key8432", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6200, key8432) size=" << scan_result.size() << std::endl;
  }

  // op 26054: delete_range
  status = db->DeleteRange(write_options, "key3930", "key10170");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 26055: delete
  db->Delete(write_options, "key5245");

  // op 26056: put
  db->Put(write_options, "key1225", "value88774");

  // op 26057: put
  db->Put(write_options, "key3733", "value45279");

  // op 26058: get
  status = db->Get(read_options, "key7884", &value);
  if (status.ok()) {
    out << "GET key7884 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26059: put
  db->Put(write_options, "key7341", "value86944");

  // op 26060: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 26061: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4495", "key10607", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4495, key10607) size=" << scan_result.size() << std::endl;
  }

  // op 26062: delete
  db->Delete(write_options, "key8018");

  // op 26063: put
  db->Put(write_options, "key5914", "value57272");

  // op 26064: delete_range
  status = db->DeleteRange(write_options, "key5791", "key8410");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 26065: put
  db->Put(write_options, "key1186", "value67056");

  // op 26066: put
  db->Put(write_options, "key11592", "value60740");

  // op 26067: put
  db->Put(write_options, "key5366", "value1179");

  // op 26068: put
  db->Put(write_options, "key2626", "value38236");

  // op 26069: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 26070: put
  db->Put(write_options, "key7102", "value106718");

  // op 26071: get
  status = db->Get(read_options, "key7733", &value);
  if (status.ok()) {
    out << "GET key7733 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26072: put
  db->Put(write_options, "key5935", "value50265");

  // op 26073: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2924", "key10496", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2924, key10496) size=" << scan_result.size() << std::endl;
  }

  // op 26074: put
  db->Put(write_options, "key9267", "value102637");

  // op 26075: get
  status = db->Get(read_options, "key0574", &value);
  if (status.ok()) {
    out << "GET key0574 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26076: put
  db->Put(write_options, "key10727", "value11535");

  // op 26077: put
  db->Put(write_options, "key0974", "value88550");

  // op 26078: delete_range
  status = db->DeleteRange(write_options, "key1415", "key11237");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 26079: put
  db->Put(write_options, "key3038", "value112467");

  // op 26080: get
  status = db->Get(read_options, "key4916", &value);
  if (status.ok()) {
    out << "GET key4916 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26081: delete_range
  status = db->DeleteRange(write_options, "key3050", "key3171");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 26082: delete
  db->Delete(write_options, "key11159");

  // op 26083: put
  db->Put(write_options, "key0766", "value17610");

  // op 26084: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 26085: delete
  db->Delete(write_options, "key4565");

  // op 26086: get
  status = db->Get(read_options, "key5126", &value);
  if (status.ok()) {
    out << "GET key5126 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26087: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2100", "key9302", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2100, key9302) size=" << scan_result.size() << std::endl;
  }

  // op 26088: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10295", "key11435", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10295, key11435) size=" << scan_result.size() << std::endl;
  }

  // op 26089: get
  status = db->Get(read_options, "key3646", &value);
  if (status.ok()) {
    out << "GET key3646 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26090: put
  db->Put(write_options, "key6252", "value14770");

  // op 26091: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4633", "key7762", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4633, key7762) size=" << scan_result.size() << std::endl;
  }

  // op 26092: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 26093: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 26094: delete
  db->Delete(write_options, "key1148");

  // op 26095: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 26096: put
  db->Put(write_options, "key0893", "value75207");

  // op 26097: put
  db->Put(write_options, "key8271", "value62942");

  // op 26098: get
  status = db->Get(read_options, "key4120", &value);
  if (status.ok()) {
    out << "GET key4120 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26099: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8746", "key9717", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8746, key9717) size=" << scan_result.size() << std::endl;
  }

  // op 26100: delete_range
  status = db->DeleteRange(write_options, "key0207", "key1382");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 26101: put
  db->Put(write_options, "key3739", "value20105");

  // op 26102: delete
  db->Delete(write_options, "key0798");

  // op 26103: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3981", "key10663", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3981, key10663) size=" << scan_result.size() << std::endl;
  }

  // op 26104: get
  status = db->Get(read_options, "key8148", &value);
  if (status.ok()) {
    out << "GET key8148 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26105: put
  db->Put(write_options, "key9931", "value86559");

  // op 26106: put
  db->Put(write_options, "key2743", "value31866");

  // op 26107: put
  db->Put(write_options, "key9816", "value37108");

  // op 26108: put
  db->Put(write_options, "key5348", "value80962");

  // op 26109: put
  db->Put(write_options, "key0782", "value80352");

  // op 26110: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9315", "key9700", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9315, key9700) size=" << scan_result.size() << std::endl;
  }

  // op 26111: put
  db->Put(write_options, "key3400", "value70798");

  // op 26112: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0176", "key4387", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0176, key4387) size=" << scan_result.size() << std::endl;
  }

  // op 26113: get
  status = db->Get(read_options, "key11665", &value);
  if (status.ok()) {
    out << "GET key11665 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26114: put
  db->Put(write_options, "key6457", "value4665");

  // op 26115: delete_range
  status = db->DeleteRange(write_options, "key0645", "key10021");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 26116: delete_range
  status = db->DeleteRange(write_options, "key9924", "key10675");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 26117: get
  status = db->Get(read_options, "key3478", &value);
  if (status.ok()) {
    out << "GET key3478 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26118: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0461", "key1286", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0461, key1286) size=" << scan_result.size() << std::endl;
  }

  // op 26119: delete
  db->Delete(write_options, "key8791");

  // op 26120: delete
  db->Delete(write_options, "key3727");

  // op 26121: get
  status = db->Get(read_options, "key10948", &value);
  if (status.ok()) {
    out << "GET key10948 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26122: put
  db->Put(write_options, "key9850", "value53669");

  // op 26123: put
  db->Put(write_options, "key3785", "value65234");

  // op 26124: put
  db->Put(write_options, "key8311", "value62720");

  // op 26125: delete
  db->Delete(write_options, "key9710");

  // op 26126: delete
  db->Delete(write_options, "key9202");

  // op 26127: put
  db->Put(write_options, "key7786", "value39380");

  // op 26128: put
  db->Put(write_options, "key7076", "value34930");

  // op 26129: get
  status = db->Get(read_options, "key1727", &value);
  if (status.ok()) {
    out << "GET key1727 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26130: delete
  db->Delete(write_options, "key1651");

  // op 26131: delete_range
  status = db->DeleteRange(write_options, "key0382", "key6169");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 26132: delete_range
  status = db->DeleteRange(write_options, "key1761", "key9725");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 26133: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3115", "key10288", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3115, key10288) size=" << scan_result.size() << std::endl;
  }

  // op 26134: put
  db->Put(write_options, "key7964", "value40006");

  // op 26135: put
  db->Put(write_options, "key1690", "value45593");

  // op 26136: put
  db->Put(write_options, "key10183", "value78731");

  // op 26137: get
  status = db->Get(read_options, "key9864", &value);
  if (status.ok()) {
    out << "GET key9864 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26138: delete_range
  status = db->DeleteRange(write_options, "key4084", "key4438");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 26139: put
  db->Put(write_options, "key2446", "value85753");

  // op 26140: put
  db->Put(write_options, "key4400", "value95561");

  // op 26141: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8008", "key10332", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8008, key10332) size=" << scan_result.size() << std::endl;
  }

  // op 26142: put
  db->Put(write_options, "key1713", "value72734");

  // op 26143: put
  db->Put(write_options, "key10869", "value103755");

  // op 26144: get
  status = db->Get(read_options, "key5362", &value);
  if (status.ok()) {
    out << "GET key5362 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26145: put
  db->Put(write_options, "key1941", "value61247");

  // op 26146: put
  db->Put(write_options, "key5908", "value116585");

  // op 26147: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 26148: put
  db->Put(write_options, "key6008", "value94357");

  // op 26149: get
  status = db->Get(read_options, "key2605", &value);
  if (status.ok()) {
    out << "GET key2605 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26150: delete
  db->Delete(write_options, "key4641");

  // op 26151: put
  db->Put(write_options, "key6546", "value57227");

  // op 26152: put
  db->Put(write_options, "key3836", "value94810");

  // op 26153: delete_range
  status = db->DeleteRange(write_options, "key3119", "key8120");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 26154: put
  db->Put(write_options, "key2368", "value110976");

  // op 26155: put
  db->Put(write_options, "key6282", "value52249");

  // op 26156: put
  db->Put(write_options, "key2398", "value46440");

  // op 26157: get
  status = db->Get(read_options, "key8958", &value);
  if (status.ok()) {
    out << "GET key8958 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26158: put
  db->Put(write_options, "key5202", "value115478");

  // op 26159: get
  status = db->Get(read_options, "key1118", &value);
  if (status.ok()) {
    out << "GET key1118 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26160: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0397", "key10018", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0397, key10018) size=" << scan_result.size() << std::endl;
  }

  // op 26161: get
  status = db->Get(read_options, "key2376", &value);
  if (status.ok()) {
    out << "GET key2376 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26162: put
  db->Put(write_options, "key1231", "value52964");

  // op 26163: put
  db->Put(write_options, "key3030", "value7157");

  // op 26164: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7542", "key10423", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7542, key10423) size=" << scan_result.size() << std::endl;
  }

  // op 26165: put
  db->Put(write_options, "key2883", "value33852");

  // op 26166: delete
  db->Delete(write_options, "key3378");

  // op 26167: get
  status = db->Get(read_options, "key2439", &value);
  if (status.ok()) {
    out << "GET key2439 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26168: put
  db->Put(write_options, "key7269", "value44454");

  // op 26169: get
  status = db->Get(read_options, "key1919", &value);
  if (status.ok()) {
    out << "GET key1919 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26170: delete_range
  status = db->DeleteRange(write_options, "key4478", "key8253");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 26171: put
  db->Put(write_options, "key3663", "value3444");

  // op 26172: delete
  db->Delete(write_options, "key2076");

  // op 26173: delete_range
  status = db->DeleteRange(write_options, "key2992", "key9697");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 26174: get
  status = db->Get(read_options, "key1469", &value);
  if (status.ok()) {
    out << "GET key1469 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26175: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 26176: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6305", "key8986", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6305, key8986) size=" << scan_result.size() << std::endl;
  }

  // op 26177: get
  status = db->Get(read_options, "key8995", &value);
  if (status.ok()) {
    out << "GET key8995 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26178: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0842", "key10774", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0842, key10774) size=" << scan_result.size() << std::endl;
  }

  // op 26179: put
  db->Put(write_options, "key5638", "value89809");

  // op 26180: get
  status = db->Get(read_options, "key10213", &value);
  if (status.ok()) {
    out << "GET key10213 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26181: put
  db->Put(write_options, "key3340", "value51794");

  // op 26182: delete
  db->Delete(write_options, "key4264");

  // op 26183: put
  db->Put(write_options, "key9333", "value69182");

  // op 26184: delete
  db->Delete(write_options, "key8046");

  // op 26185: put
  db->Put(write_options, "key2625", "value56974");

  // op 26186: put
  db->Put(write_options, "key0569", "value2382");

  // op 26187: put
  db->Put(write_options, "key11440", "value31498");

  // op 26188: put
  db->Put(write_options, "key10599", "value53400");

  // op 26189: put
  db->Put(write_options, "key0024", "value34182");

  // op 26190: delete_range
  status = db->DeleteRange(write_options, "key7662", "key8238");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 26191: get
  status = db->Get(read_options, "key8767", &value);
  if (status.ok()) {
    out << "GET key8767 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26192: put
  db->Put(write_options, "key10770", "value7409");

  // op 26193: get
  status = db->Get(read_options, "key4833", &value);
  if (status.ok()) {
    out << "GET key4833 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26194: put
  db->Put(write_options, "key8930", "value59843");

  // op 26195: delete_range
  status = db->DeleteRange(write_options, "key7040", "key8971");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 26196: put
  db->Put(write_options, "key9981", "value96520");

  // op 26197: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 26198: put
  db->Put(write_options, "key4348", "value3716");

  // op 26199: put
  db->Put(write_options, "key2234", "value98967");

  // op 26200: put
  db->Put(write_options, "key4109", "value116086");

  // op 26201: put
  db->Put(write_options, "key4979", "value96261");

  // op 26202: put
  db->Put(write_options, "key9461", "value62734");

  // op 26203: put
  db->Put(write_options, "key7265", "value85411");

  // op 26204: delete_range
  status = db->DeleteRange(write_options, "key3433", "key9505");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 26205: put
  db->Put(write_options, "key3649", "value80765");

  // op 26206: get
  status = db->Get(read_options, "key3636", &value);
  if (status.ok()) {
    out << "GET key3636 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26207: get
  status = db->Get(read_options, "key8526", &value);
  if (status.ok()) {
    out << "GET key8526 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26208: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7955", "key8019", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7955, key8019) size=" << scan_result.size() << std::endl;
  }

  // op 26209: put
  db->Put(write_options, "key10613", "value47074");

  // op 26210: delete
  db->Delete(write_options, "key1455");

  // op 26211: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3052", "key7973", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3052, key7973) size=" << scan_result.size() << std::endl;
  }

  // op 26212: get
  status = db->Get(read_options, "key0013", &value);
  if (status.ok()) {
    out << "GET key0013 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26213: put
  db->Put(write_options, "key2632", "value71176");

  // op 26214: delete
  db->Delete(write_options, "key0678");

  // op 26215: put
  db->Put(write_options, "key1595", "value92296");

  // op 26216: put
  db->Put(write_options, "key1477", "value13859");

  // op 26217: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9604", "key9666", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9604, key9666) size=" << scan_result.size() << std::endl;
  }

  // op 26218: delete_range
  status = db->DeleteRange(write_options, "key1962", "key8736");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 26219: get
  status = db->Get(read_options, "key8772", &value);
  if (status.ok()) {
    out << "GET key8772 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26220: delete
  db->Delete(write_options, "key1308");

  // op 26221: put
  db->Put(write_options, "key10121", "value35467");

  // op 26222: put
  db->Put(write_options, "key3468", "value81770");

  // op 26223: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6527", "key7755", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6527, key7755) size=" << scan_result.size() << std::endl;
  }

  // op 26224: delete_range
  status = db->DeleteRange(write_options, "key11499", "key11524");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 26225: get
  status = db->Get(read_options, "key11318", &value);
  if (status.ok()) {
    out << "GET key11318 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26226: put
  db->Put(write_options, "key9306", "value35882");

  // op 26227: delete
  db->Delete(write_options, "key1955");

  // op 26228: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 26229: put
  db->Put(write_options, "key8561", "value2264");

  // op 26230: put
  db->Put(write_options, "key11154", "value61920");

  // op 26231: delete
  db->Delete(write_options, "key7189");

  // op 26232: get
  status = db->Get(read_options, "key4795", &value);
  if (status.ok()) {
    out << "GET key4795 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26233: get
  status = db->Get(read_options, "key5409", &value);
  if (status.ok()) {
    out << "GET key5409 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26234: get
  status = db->Get(read_options, "key6163", &value);
  if (status.ok()) {
    out << "GET key6163 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26235: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1032", "key7042", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1032, key7042) size=" << scan_result.size() << std::endl;
  }

  // op 26236: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3377", "key7094", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3377, key7094) size=" << scan_result.size() << std::endl;
  }

  // op 26237: delete_range
  status = db->DeleteRange(write_options, "key2493", "key10548");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 26238: delete
  db->Delete(write_options, "key5669");

  // op 26239: delete_range
  status = db->DeleteRange(write_options, "key3950", "key4748");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 26240: get
  status = db->Get(read_options, "key3228", &value);
  if (status.ok()) {
    out << "GET key3228 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26241: put
  db->Put(write_options, "key6830", "value52082");

  // op 26242: put
  db->Put(write_options, "key8352", "value94203");

  // op 26243: delete
  db->Delete(write_options, "key9756");

  // op 26244: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2931", "key6504", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2931, key6504) size=" << scan_result.size() << std::endl;
  }

  // op 26245: get
  status = db->Get(read_options, "key1435", &value);
  if (status.ok()) {
    out << "GET key1435 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26246: put
  db->Put(write_options, "key4913", "value36282");

  // op 26247: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 26248: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4159", "key11015", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4159, key11015) size=" << scan_result.size() << std::endl;
  }

  // op 26249: get
  status = db->Get(read_options, "key3636", &value);
  if (status.ok()) {
    out << "GET key3636 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26250: put
  db->Put(write_options, "key9208", "value66889");

  // op 26251: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3158", "key3762", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3158, key3762) size=" << scan_result.size() << std::endl;
  }

  // op 26252: put
  db->Put(write_options, "key11637", "value113700");

  // op 26253: get
  status = db->Get(read_options, "key8714", &value);
  if (status.ok()) {
    out << "GET key8714 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26254: delete
  db->Delete(write_options, "key11618");

  // op 26255: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7370", "key10983", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7370, key10983) size=" << scan_result.size() << std::endl;
  }

  // op 26256: get
  status = db->Get(read_options, "key7298", &value);
  if (status.ok()) {
    out << "GET key7298 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26257: put
  db->Put(write_options, "key1691", "value84150");

  // op 26258: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3441", "key8691", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3441, key8691) size=" << scan_result.size() << std::endl;
  }

  // op 26259: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 26260: put
  db->Put(write_options, "key5041", "value60853");

  // op 26261: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2651", "key10186", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2651, key10186) size=" << scan_result.size() << std::endl;
  }

  // op 26262: delete
  db->Delete(write_options, "key0650");

  // op 26263: delete
  db->Delete(write_options, "key3729");

  // op 26264: put
  db->Put(write_options, "key1593", "value117141");

  // op 26265: get
  status = db->Get(read_options, "key1113", &value);
  if (status.ok()) {
    out << "GET key1113 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26266: put
  db->Put(write_options, "key0145", "value105325");

  // op 26267: get
  status = db->Get(read_options, "key10736", &value);
  if (status.ok()) {
    out << "GET key10736 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26268: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1441", "key8741", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1441, key8741) size=" << scan_result.size() << std::endl;
  }

  // op 26269: get
  status = db->Get(read_options, "key2083", &value);
  if (status.ok()) {
    out << "GET key2083 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26270: get
  status = db->Get(read_options, "key7744", &value);
  if (status.ok()) {
    out << "GET key7744 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26271: get
  status = db->Get(read_options, "key10643", &value);
  if (status.ok()) {
    out << "GET key10643 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26272: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0113", "key8039", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0113, key8039) size=" << scan_result.size() << std::endl;
  }

  // op 26273: get
  status = db->Get(read_options, "key9051", &value);
  if (status.ok()) {
    out << "GET key9051 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26274: get
  status = db->Get(read_options, "key10397", &value);
  if (status.ok()) {
    out << "GET key10397 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26275: delete
  db->Delete(write_options, "key7688");

  // op 26276: put
  db->Put(write_options, "key4239", "value96543");

  // op 26277: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 26278: delete_range
  status = db->DeleteRange(write_options, "key6176", "key9675");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 26279: delete
  db->Delete(write_options, "key4852");

  // op 26280: put
  db->Put(write_options, "key8393", "value32076");

  // op 26281: get
  status = db->Get(read_options, "key9790", &value);
  if (status.ok()) {
    out << "GET key9790 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26282: put
  db->Put(write_options, "key1317", "value47316");

  // op 26283: get
  status = db->Get(read_options, "key2540", &value);
  if (status.ok()) {
    out << "GET key2540 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26284: put
  db->Put(write_options, "key9966", "value48270");

  // op 26285: get
  status = db->Get(read_options, "key1008", &value);
  if (status.ok()) {
    out << "GET key1008 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26286: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0472", "key8536", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0472, key8536) size=" << scan_result.size() << std::endl;
  }

  // op 26287: put
  db->Put(write_options, "key11459", "value34010");

  // op 26288: get
  status = db->Get(read_options, "key4931", &value);
  if (status.ok()) {
    out << "GET key4931 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26289: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4476", "key8632", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4476, key8632) size=" << scan_result.size() << std::endl;
  }

  // op 26290: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0363", "key4444", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0363, key4444) size=" << scan_result.size() << std::endl;
  }

  // op 26291: get
  status = db->Get(read_options, "key8747", &value);
  if (status.ok()) {
    out << "GET key8747 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26292: put
  db->Put(write_options, "key3153", "value11016");

  // op 26293: delete_range
  status = db->DeleteRange(write_options, "key9601", "key10711");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 26294: get
  status = db->Get(read_options, "key4059", &value);
  if (status.ok()) {
    out << "GET key4059 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26295: put
  db->Put(write_options, "key6304", "value104826");

  // op 26296: put
  db->Put(write_options, "key8045", "value8529");

  // op 26297: put
  db->Put(write_options, "key10862", "value56797");

  // op 26298: put
  db->Put(write_options, "key3490", "value44184");

  // op 26299: get
  status = db->Get(read_options, "key10151", &value);
  if (status.ok()) {
    out << "GET key10151 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26300: delete_range
  status = db->DeleteRange(write_options, "key1655", "key11388");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 26301: get
  status = db->Get(read_options, "key4902", &value);
  if (status.ok()) {
    out << "GET key4902 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26302: put
  db->Put(write_options, "key7395", "value18035");

  // op 26303: put
  db->Put(write_options, "key10752", "value28721");

  // op 26304: get
  status = db->Get(read_options, "key0118", &value);
  if (status.ok()) {
    out << "GET key0118 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26305: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4829", "key5220", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4829, key5220) size=" << scan_result.size() << std::endl;
  }

  // op 26306: delete_range
  status = db->DeleteRange(write_options, "key2931", "key7081");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 26307: put
  db->Put(write_options, "key8987", "value56763");

  // op 26308: get
  status = db->Get(read_options, "key8153", &value);
  if (status.ok()) {
    out << "GET key8153 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26309: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0728", "key7504", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0728, key7504) size=" << scan_result.size() << std::endl;
  }

  // op 26310: delete_range
  status = db->DeleteRange(write_options, "key3177", "key9312");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 26311: put
  db->Put(write_options, "key2572", "value24471");

  // op 26312: get
  status = db->Get(read_options, "key9065", &value);
  if (status.ok()) {
    out << "GET key9065 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26313: delete
  db->Delete(write_options, "key5046");

  // op 26314: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 26315: put
  db->Put(write_options, "key1706", "value4327");

  // op 26316: delete_range
  status = db->DeleteRange(write_options, "key1772", "key11060");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 26317: delete
  db->Delete(write_options, "key6688");

  // op 26318: put
  db->Put(write_options, "key11244", "value38238");

  // op 26319: get
  status = db->Get(read_options, "key6670", &value);
  if (status.ok()) {
    out << "GET key6670 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26320: get
  status = db->Get(read_options, "key3384", &value);
  if (status.ok()) {
    out << "GET key3384 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26321: get
  status = db->Get(read_options, "key9231", &value);
  if (status.ok()) {
    out << "GET key9231 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26322: put
  db->Put(write_options, "key9133", "value98118");

  // op 26323: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5743", "key8096", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5743, key8096) size=" << scan_result.size() << std::endl;
  }

  // op 26324: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7065", "key10937", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7065, key10937) size=" << scan_result.size() << std::endl;
  }

  // op 26325: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7572", "key10741", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7572, key10741) size=" << scan_result.size() << std::endl;
  }

  // op 26326: get
  status = db->Get(read_options, "key0467", &value);
  if (status.ok()) {
    out << "GET key0467 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26327: delete
  db->Delete(write_options, "key1907");

  // op 26328: put
  db->Put(write_options, "key7560", "value30135");

  // op 26329: delete
  db->Delete(write_options, "key0467");

  // op 26330: get
  status = db->Get(read_options, "key1101", &value);
  if (status.ok()) {
    out << "GET key1101 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26331: get
  status = db->Get(read_options, "key11528", &value);
  if (status.ok()) {
    out << "GET key11528 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26332: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2649", "key6164", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2649, key6164) size=" << scan_result.size() << std::endl;
  }

  // op 26333: delete
  db->Delete(write_options, "key7189");

  // op 26334: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3487", "key4309", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3487, key4309) size=" << scan_result.size() << std::endl;
  }

  // op 26335: delete_range
  status = db->DeleteRange(write_options, "key11138", "key11517");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 26336: delete_range
  status = db->DeleteRange(write_options, "key7882", "key9380");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 26337: delete_range
  status = db->DeleteRange(write_options, "key7806", "key11350");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 26338: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3920", "key11715", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3920, key11715) size=" << scan_result.size() << std::endl;
  }

  // op 26339: put
  db->Put(write_options, "key8385", "value30546");

  // op 26340: put
  db->Put(write_options, "key1320", "value65067");

  // op 26341: put
  db->Put(write_options, "key3787", "value99053");

  // op 26342: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 26343: get
  status = db->Get(read_options, "key0587", &value);
  if (status.ok()) {
    out << "GET key0587 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26344: get
  status = db->Get(read_options, "key10691", &value);
  if (status.ok()) {
    out << "GET key10691 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26345: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 26346: delete
  db->Delete(write_options, "key5985");

  // op 26347: get
  status = db->Get(read_options, "key0584", &value);
  if (status.ok()) {
    out << "GET key0584 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26348: put
  db->Put(write_options, "key0678", "value57740");

  // op 26349: put
  db->Put(write_options, "key1397", "value108813");

  // op 26350: put
  db->Put(write_options, "key3161", "value11865");

  // op 26351: delete_range
  status = db->DeleteRange(write_options, "key1941", "key3615");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 26352: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0258", "key9686", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0258, key9686) size=" << scan_result.size() << std::endl;
  }

  // op 26353: put
  db->Put(write_options, "key8438", "value88607");

  // op 26354: get
  status = db->Get(read_options, "key5102", &value);
  if (status.ok()) {
    out << "GET key5102 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26355: get
  status = db->Get(read_options, "key9809", &value);
  if (status.ok()) {
    out << "GET key9809 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26356: put
  db->Put(write_options, "key9744", "value25094");

  // op 26357: delete_range
  status = db->DeleteRange(write_options, "key7868", "key9156");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 26358: delete_range
  status = db->DeleteRange(write_options, "key4947", "key11164");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 26359: delete_range
  status = db->DeleteRange(write_options, "key3358", "key8129");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 26360: delete
  db->Delete(write_options, "key5045");

  // op 26361: put
  db->Put(write_options, "key8113", "value21388");

  // op 26362: put
  db->Put(write_options, "key7956", "value91670");

  // op 26363: put
  db->Put(write_options, "key3034", "value56677");

  // op 26364: get
  status = db->Get(read_options, "key10532", &value);
  if (status.ok()) {
    out << "GET key10532 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26365: put
  db->Put(write_options, "key10648", "value60776");

  // op 26366: get
  status = db->Get(read_options, "key0342", &value);
  if (status.ok()) {
    out << "GET key0342 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26367: delete_range
  status = db->DeleteRange(write_options, "key5838", "key9032");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 26368: delete_range
  status = db->DeleteRange(write_options, "key1616", "key5864");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 26369: put
  db->Put(write_options, "key9748", "value31134");

  // op 26370: get
  status = db->Get(read_options, "key10181", &value);
  if (status.ok()) {
    out << "GET key10181 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26371: get
  status = db->Get(read_options, "key10181", &value);
  if (status.ok()) {
    out << "GET key10181 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26372: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 26373: delete
  db->Delete(write_options, "key3736");

  // op 26374: put
  db->Put(write_options, "key6410", "value15552");

  // op 26375: put
  db->Put(write_options, "key3762", "value89324");

  // op 26376: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7447", "key10096", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7447, key10096) size=" << scan_result.size() << std::endl;
  }

  // op 26377: put
  db->Put(write_options, "key0793", "value35040");

  // op 26378: put
  db->Put(write_options, "key4119", "value25921");

  // op 26379: put
  db->Put(write_options, "key2264", "value56789");

  // op 26380: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1720", "key11260", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1720, key11260) size=" << scan_result.size() << std::endl;
  }

  // op 26381: put
  db->Put(write_options, "key0597", "value71131");

  // op 26382: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0727", "key4772", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0727, key4772) size=" << scan_result.size() << std::endl;
  }

  // op 26383: put
  db->Put(write_options, "key5458", "value71071");

  // op 26384: get
  status = db->Get(read_options, "key1106", &value);
  if (status.ok()) {
    out << "GET key1106 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26385: put
  db->Put(write_options, "key10385", "value49599");

  // op 26386: get
  status = db->Get(read_options, "key10175", &value);
  if (status.ok()) {
    out << "GET key10175 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26387: delete_range
  status = db->DeleteRange(write_options, "key0825", "key2189");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 26388: delete_range
  status = db->DeleteRange(write_options, "key1772", "key6880");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 26389: get
  status = db->Get(read_options, "key0808", &value);
  if (status.ok()) {
    out << "GET key0808 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26390: delete
  db->Delete(write_options, "key6545");

  // op 26391: delete
  db->Delete(write_options, "key4187");

  // op 26392: put
  db->Put(write_options, "key9924", "value45829");

  // op 26393: delete_range
  status = db->DeleteRange(write_options, "key0351", "key1040");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 26394: put
  db->Put(write_options, "key9676", "value80355");

  // op 26395: delete_range
  status = db->DeleteRange(write_options, "key5958", "key6292");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 26396: get
  status = db->Get(read_options, "key4179", &value);
  if (status.ok()) {
    out << "GET key4179 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26397: delete
  db->Delete(write_options, "key6446");

  // op 26398: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0923", "key1652", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0923, key1652) size=" << scan_result.size() << std::endl;
  }

  // op 26399: delete_range
  status = db->DeleteRange(write_options, "key8461", "key9244");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 26400: delete_range
  status = db->DeleteRange(write_options, "key2407", "key4267");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 26401: put
  db->Put(write_options, "key9641", "value65519");

  // op 26402: delete_range
  status = db->DeleteRange(write_options, "key5530", "key9262");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 26403: put
  db->Put(write_options, "key5738", "value79292");

  // op 26404: put
  db->Put(write_options, "key9151", "value111782");

  // op 26405: put
  db->Put(write_options, "key9097", "value85601");

  // op 26406: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6106", "key11009", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6106, key11009) size=" << scan_result.size() << std::endl;
  }

  // op 26407: get
  status = db->Get(read_options, "key8854", &value);
  if (status.ok()) {
    out << "GET key8854 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26408: get
  status = db->Get(read_options, "key5313", &value);
  if (status.ok()) {
    out << "GET key5313 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26409: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4135", "key11088", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4135, key11088) size=" << scan_result.size() << std::endl;
  }

  // op 26410: delete
  db->Delete(write_options, "key11265");

  // op 26411: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9397", "key11296", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9397, key11296) size=" << scan_result.size() << std::endl;
  }

  // op 26412: get
  status = db->Get(read_options, "key1534", &value);
  if (status.ok()) {
    out << "GET key1534 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26413: delete
  db->Delete(write_options, "key11716");

  // op 26414: get
  status = db->Get(read_options, "key4874", &value);
  if (status.ok()) {
    out << "GET key4874 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26415: get
  status = db->Get(read_options, "key3042", &value);
  if (status.ok()) {
    out << "GET key3042 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26416: put
  db->Put(write_options, "key1476", "value32314");

  // op 26417: get
  status = db->Get(read_options, "key2043", &value);
  if (status.ok()) {
    out << "GET key2043 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26418: get
  status = db->Get(read_options, "key1810", &value);
  if (status.ok()) {
    out << "GET key1810 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26419: put
  db->Put(write_options, "key3469", "value88024");

  // op 26420: put
  db->Put(write_options, "key7108", "value61219");

  // op 26421: put
  db->Put(write_options, "key1866", "value17270");

  // op 26422: get
  status = db->Get(read_options, "key9075", &value);
  if (status.ok()) {
    out << "GET key9075 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26423: delete
  db->Delete(write_options, "key3935");

  // op 26424: put
  db->Put(write_options, "key3940", "value108311");

  // op 26425: get
  status = db->Get(read_options, "key8906", &value);
  if (status.ok()) {
    out << "GET key8906 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26426: put
  db->Put(write_options, "key2548", "value13893");

  // op 26427: put
  db->Put(write_options, "key7826", "value51799");

  // op 26428: get
  status = db->Get(read_options, "key6979", &value);
  if (status.ok()) {
    out << "GET key6979 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26429: put
  db->Put(write_options, "key2914", "value3413");

  // op 26430: put
  db->Put(write_options, "key8814", "value61023");

  // op 26431: get
  status = db->Get(read_options, "key6735", &value);
  if (status.ok()) {
    out << "GET key6735 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26432: get
  status = db->Get(read_options, "key3863", &value);
  if (status.ok()) {
    out << "GET key3863 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26433: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 26434: put
  db->Put(write_options, "key10808", "value43723");

  // op 26435: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4291", "key10921", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4291, key10921) size=" << scan_result.size() << std::endl;
  }

  // op 26436: put
  db->Put(write_options, "key3229", "value7065");

  // op 26437: get
  status = db->Get(read_options, "key5082", &value);
  if (status.ok()) {
    out << "GET key5082 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26438: put
  db->Put(write_options, "key1356", "value95229");

  // op 26439: put
  db->Put(write_options, "key9353", "value117499");

  // op 26440: put
  db->Put(write_options, "key7854", "value109445");

  // op 26441: get
  status = db->Get(read_options, "key11084", &value);
  if (status.ok()) {
    out << "GET key11084 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26442: get
  status = db->Get(read_options, "key0845", &value);
  if (status.ok()) {
    out << "GET key0845 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26443: put
  db->Put(write_options, "key6183", "value36704");

  // op 26444: delete_range
  status = db->DeleteRange(write_options, "key5316", "key11479");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 26445: delete_range
  status = db->DeleteRange(write_options, "key1457", "key9107");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 26446: put
  db->Put(write_options, "key10626", "value25953");

  // op 26447: put
  db->Put(write_options, "key5681", "value18169");

  // op 26448: put
  db->Put(write_options, "key9942", "value49804");

  // op 26449: delete_range
  status = db->DeleteRange(write_options, "key0159", "key9283");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 26450: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0760", "key3264", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0760, key3264) size=" << scan_result.size() << std::endl;
  }

  // op 26451: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6031", "key9572", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6031, key9572) size=" << scan_result.size() << std::endl;
  }

  // op 26452: put
  db->Put(write_options, "key4647", "value7800");

  // op 26453: put
  db->Put(write_options, "key7636", "value72819");

  // op 26454: delete
  db->Delete(write_options, "key11133");

  // op 26455: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0067", "key2605", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0067, key2605) size=" << scan_result.size() << std::endl;
  }

  // op 26456: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7584", "key8265", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7584, key8265) size=" << scan_result.size() << std::endl;
  }

  // op 26457: put
  db->Put(write_options, "key3836", "value109380");

  // op 26458: put
  db->Put(write_options, "key10116", "value107791");

  // op 26459: get
  status = db->Get(read_options, "key3764", &value);
  if (status.ok()) {
    out << "GET key3764 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26460: put
  db->Put(write_options, "key9765", "value29684");

  // op 26461: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4422", "key8066", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4422, key8066) size=" << scan_result.size() << std::endl;
  }

  // op 26462: delete_range
  status = db->DeleteRange(write_options, "key1949", "key11354");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 26463: put
  db->Put(write_options, "key3586", "value25887");

  // op 26464: get
  status = db->Get(read_options, "key10949", &value);
  if (status.ok()) {
    out << "GET key10949 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26465: put
  db->Put(write_options, "key3029", "value1093");

  // op 26466: put
  db->Put(write_options, "key3349", "value107769");

  // op 26467: delete
  db->Delete(write_options, "key1471");

  // op 26468: delete_range
  status = db->DeleteRange(write_options, "key9043", "key9508");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 26469: get
  status = db->Get(read_options, "key8901", &value);
  if (status.ok()) {
    out << "GET key8901 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26470: put
  db->Put(write_options, "key7135", "value9484");

  // op 26471: get
  status = db->Get(read_options, "key0960", &value);
  if (status.ok()) {
    out << "GET key0960 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26472: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 26473: put
  db->Put(write_options, "key2876", "value64215");

  // op 26474: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7407", "key8892", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7407, key8892) size=" << scan_result.size() << std::endl;
  }

  // op 26475: delete_range
  status = db->DeleteRange(write_options, "key0583", "key0856");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 26476: get
  status = db->Get(read_options, "key2255", &value);
  if (status.ok()) {
    out << "GET key2255 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26477: get
  status = db->Get(read_options, "key9255", &value);
  if (status.ok()) {
    out << "GET key9255 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26478: get
  status = db->Get(read_options, "key8874", &value);
  if (status.ok()) {
    out << "GET key8874 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26479: put
  db->Put(write_options, "key0495", "value114515");

  // op 26480: put
  db->Put(write_options, "key3825", "value98658");

  // op 26481: put
  db->Put(write_options, "key2721", "value89411");

  // op 26482: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5158", "key5816", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5158, key5816) size=" << scan_result.size() << std::endl;
  }

  // op 26483: put
  db->Put(write_options, "key2581", "value61152");

  // op 26484: get
  status = db->Get(read_options, "key0774", &value);
  if (status.ok()) {
    out << "GET key0774 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26485: put
  db->Put(write_options, "key10510", "value7600");

  // op 26486: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0256", "key6502", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0256, key6502) size=" << scan_result.size() << std::endl;
  }

  // op 26487: put
  db->Put(write_options, "key3933", "value49212");

  // op 26488: put
  db->Put(write_options, "key1228", "value59723");

  // op 26489: put
  db->Put(write_options, "key5358", "value47082");

  // op 26490: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5638", "key6797", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5638, key6797) size=" << scan_result.size() << std::endl;
  }

  // op 26491: put
  db->Put(write_options, "key9201", "value78398");

  // op 26492: put
  db->Put(write_options, "key1426", "value49332");

  // op 26493: delete_range
  status = db->DeleteRange(write_options, "key1857", "key2252");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 26494: delete
  db->Delete(write_options, "key8283");

  // op 26495: put
  db->Put(write_options, "key5731", "value57229");

  // op 26496: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8148", "key8945", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8148, key8945) size=" << scan_result.size() << std::endl;
  }

  // op 26497: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5291", "key5619", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5291, key5619) size=" << scan_result.size() << std::endl;
  }

  // op 26498: put
  db->Put(write_options, "key3305", "value41677");

  // op 26499: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1448", "key6859", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1448, key6859) size=" << scan_result.size() << std::endl;
  }

  // op 26500: put
  db->Put(write_options, "key7846", "value90963");

  // op 26501: get
  status = db->Get(read_options, "key2897", &value);
  if (status.ok()) {
    out << "GET key2897 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26502: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7525", "key8514", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7525, key8514) size=" << scan_result.size() << std::endl;
  }

  // op 26503: get
  status = db->Get(read_options, "key10637", &value);
  if (status.ok()) {
    out << "GET key10637 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26504: put
  db->Put(write_options, "key2674", "value91991");

  // op 26505: delete
  db->Delete(write_options, "key7591");

  // op 26506: put
  db->Put(write_options, "key2857", "value33867");

  // op 26507: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1939", "key3386", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1939, key3386) size=" << scan_result.size() << std::endl;
  }

  // op 26508: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1121", "key4797", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1121, key4797) size=" << scan_result.size() << std::endl;
  }

  // op 26509: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 26510: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3749", "key10595", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3749, key10595) size=" << scan_result.size() << std::endl;
  }

  // op 26511: put
  db->Put(write_options, "key8997", "value46449");

  // op 26512: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8102", "key8722", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8102, key8722) size=" << scan_result.size() << std::endl;
  }

  // op 26513: get
  status = db->Get(read_options, "key1177", &value);
  if (status.ok()) {
    out << "GET key1177 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26514: get
  status = db->Get(read_options, "key10594", &value);
  if (status.ok()) {
    out << "GET key10594 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26515: put
  db->Put(write_options, "key8231", "value113749");

  // op 26516: delete
  db->Delete(write_options, "key1472");

  // op 26517: put
  db->Put(write_options, "key6267", "value31001");

  // op 26518: delete_range
  status = db->DeleteRange(write_options, "key4248", "key6560");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 26519: put
  db->Put(write_options, "key2891", "value65823");

  // op 26520: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1913", "key2111", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1913, key2111) size=" << scan_result.size() << std::endl;
  }

  // op 26521: put
  db->Put(write_options, "key0787", "value5730");

  // op 26522: get
  status = db->Get(read_options, "key2910", &value);
  if (status.ok()) {
    out << "GET key2910 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26523: put
  db->Put(write_options, "key7946", "value108500");

  // op 26524: get
  status = db->Get(read_options, "key7350", &value);
  if (status.ok()) {
    out << "GET key7350 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26525: get
  status = db->Get(read_options, "key9763", &value);
  if (status.ok()) {
    out << "GET key9763 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26526: put
  db->Put(write_options, "key7364", "value24181");

  // op 26527: delete
  db->Delete(write_options, "key7071");

  // op 26528: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4732", "key10423", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4732, key10423) size=" << scan_result.size() << std::endl;
  }

  // op 26529: put
  db->Put(write_options, "key8303", "value85633");

  // op 26530: put
  db->Put(write_options, "key7780", "value78545");

  // op 26531: get
  status = db->Get(read_options, "key8234", &value);
  if (status.ok()) {
    out << "GET key8234 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26532: delete
  db->Delete(write_options, "key3140");

  // op 26533: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0599", "key1756", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0599, key1756) size=" << scan_result.size() << std::endl;
  }

  // op 26534: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7827", "key9108", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7827, key9108) size=" << scan_result.size() << std::endl;
  }

  // op 26535: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3222", "key9423", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3222, key9423) size=" << scan_result.size() << std::endl;
  }

  // op 26536: put
  db->Put(write_options, "key0353", "value69976");

  // op 26537: get
  status = db->Get(read_options, "key7947", &value);
  if (status.ok()) {
    out << "GET key7947 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26538: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0141", "key3890", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0141, key3890) size=" << scan_result.size() << std::endl;
  }

  // op 26539: get
  status = db->Get(read_options, "key1677", &value);
  if (status.ok()) {
    out << "GET key1677 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26540: get
  status = db->Get(read_options, "key3643", &value);
  if (status.ok()) {
    out << "GET key3643 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26541: get
  status = db->Get(read_options, "key1374", &value);
  if (status.ok()) {
    out << "GET key1374 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26542: get
  status = db->Get(read_options, "key10746", &value);
  if (status.ok()) {
    out << "GET key10746 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26543: get
  status = db->Get(read_options, "key11497", &value);
  if (status.ok()) {
    out << "GET key11497 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26544: put
  db->Put(write_options, "key1372", "value32838");

  // op 26545: get
  status = db->Get(read_options, "key2531", &value);
  if (status.ok()) {
    out << "GET key2531 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26546: get
  status = db->Get(read_options, "key7688", &value);
  if (status.ok()) {
    out << "GET key7688 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26547: put
  db->Put(write_options, "key8277", "value106628");

  // op 26548: get
  status = db->Get(read_options, "key9643", &value);
  if (status.ok()) {
    out << "GET key9643 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26549: get
  status = db->Get(read_options, "key4834", &value);
  if (status.ok()) {
    out << "GET key4834 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26550: delete
  db->Delete(write_options, "key10790");

  // op 26551: get
  status = db->Get(read_options, "key5503", &value);
  if (status.ok()) {
    out << "GET key5503 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26552: put
  db->Put(write_options, "key0763", "value19333");

  // op 26553: delete
  db->Delete(write_options, "key9965");

  // op 26554: get
  status = db->Get(read_options, "key6419", &value);
  if (status.ok()) {
    out << "GET key6419 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26555: get
  status = db->Get(read_options, "key4336", &value);
  if (status.ok()) {
    out << "GET key4336 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26556: delete
  db->Delete(write_options, "key3713");

  // op 26557: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2656", "key3868", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2656, key3868) size=" << scan_result.size() << std::endl;
  }

  // op 26558: get
  status = db->Get(read_options, "key7588", &value);
  if (status.ok()) {
    out << "GET key7588 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26559: put
  db->Put(write_options, "key10473", "value107642");

  // op 26560: put
  db->Put(write_options, "key3731", "value53741");

  // op 26561: put
  db->Put(write_options, "key3195", "value23606");

  // op 26562: put
  db->Put(write_options, "key5110", "value48108");

  // op 26563: delete
  db->Delete(write_options, "key3561");

  // op 26564: get
  status = db->Get(read_options, "key6587", &value);
  if (status.ok()) {
    out << "GET key6587 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26565: get
  status = db->Get(read_options, "key7177", &value);
  if (status.ok()) {
    out << "GET key7177 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26566: get
  status = db->Get(read_options, "key5854", &value);
  if (status.ok()) {
    out << "GET key5854 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26567: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7093", "key7447", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7093, key7447) size=" << scan_result.size() << std::endl;
  }

  // op 26568: delete
  db->Delete(write_options, "key8380");

  // op 26569: put
  db->Put(write_options, "key9708", "value72581");

  // op 26570: delete_range
  status = db->DeleteRange(write_options, "key3360", "key11246");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 26571: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 26572: get
  status = db->Get(read_options, "key1331", &value);
  if (status.ok()) {
    out << "GET key1331 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26573: delete
  db->Delete(write_options, "key8443");

  // op 26574: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10633", "key11302", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10633, key11302) size=" << scan_result.size() << std::endl;
  }

  // op 26575: get
  status = db->Get(read_options, "key9420", &value);
  if (status.ok()) {
    out << "GET key9420 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26576: get
  status = db->Get(read_options, "key8370", &value);
  if (status.ok()) {
    out << "GET key8370 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26577: put
  db->Put(write_options, "key11511", "value36204");

  // op 26578: put
  db->Put(write_options, "key0509", "value58669");

  // op 26579: delete
  db->Delete(write_options, "key6577");

  // op 26580: get
  status = db->Get(read_options, "key2880", &value);
  if (status.ok()) {
    out << "GET key2880 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26581: put
  db->Put(write_options, "key6906", "value67374");

  // op 26582: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2629", "key9526", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2629, key9526) size=" << scan_result.size() << std::endl;
  }

  // op 26583: put
  db->Put(write_options, "key10536", "value15967");

  // op 26584: delete
  db->Delete(write_options, "key7876");

  // op 26585: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5813", "key9297", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5813, key9297) size=" << scan_result.size() << std::endl;
  }

  // op 26586: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0197", "key10031", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0197, key10031) size=" << scan_result.size() << std::endl;
  }

  // op 26587: put
  db->Put(write_options, "key6652", "value95472");

  // op 26588: put
  db->Put(write_options, "key4645", "value44648");

  // op 26589: put
  db->Put(write_options, "key4983", "value89515");

  // op 26590: put
  db->Put(write_options, "key5476", "value112133");

  // op 26591: put
  db->Put(write_options, "key5152", "value106727");

  // op 26592: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7249", "key9196", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7249, key9196) size=" << scan_result.size() << std::endl;
  }

  // op 26593: get
  status = db->Get(read_options, "key2350", &value);
  if (status.ok()) {
    out << "GET key2350 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26594: put
  db->Put(write_options, "key7018", "value30438");

  // op 26595: get
  status = db->Get(read_options, "key0472", &value);
  if (status.ok()) {
    out << "GET key0472 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26596: put
  db->Put(write_options, "key2072", "value102537");

  // op 26597: put
  db->Put(write_options, "key7162", "value18304");

  // op 26598: delete
  db->Delete(write_options, "key0223");

  // op 26599: get
  status = db->Get(read_options, "key4630", &value);
  if (status.ok()) {
    out << "GET key4630 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26600: put
  db->Put(write_options, "key8192", "value107395");

  // op 26601: get
  status = db->Get(read_options, "key2732", &value);
  if (status.ok()) {
    out << "GET key2732 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26602: put
  db->Put(write_options, "key4512", "value5467");

  // op 26603: put
  db->Put(write_options, "key7141", "value49391");

  // op 26604: get
  status = db->Get(read_options, "key4022", &value);
  if (status.ok()) {
    out << "GET key4022 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26605: put
  db->Put(write_options, "key4686", "value19044");

  // op 26606: put
  db->Put(write_options, "key9718", "value118000");

  // op 26607: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 26608: delete
  db->Delete(write_options, "key5142");

  // op 26609: delete
  db->Delete(write_options, "key1185");

  // op 26610: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4415", "key11398", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4415, key11398) size=" << scan_result.size() << std::endl;
  }

  // op 26611: put
  db->Put(write_options, "key6491", "value71595");

  // op 26612: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 26613: put
  db->Put(write_options, "key10839", "value89469");

  // op 26614: put
  db->Put(write_options, "key6203", "value94112");

  // op 26615: delete
  db->Delete(write_options, "key11210");

  // op 26616: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3319", "key4306", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3319, key4306) size=" << scan_result.size() << std::endl;
  }

  // op 26617: put
  db->Put(write_options, "key3497", "value105904");

  // op 26618: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3298", "key4520", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3298, key4520) size=" << scan_result.size() << std::endl;
  }

  // op 26619: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4543", "key9932", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4543, key9932) size=" << scan_result.size() << std::endl;
  }

  // op 26620: delete_range
  status = db->DeleteRange(write_options, "key9096", "key10267");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 26621: put
  db->Put(write_options, "key10440", "value69280");

  // op 26622: get
  status = db->Get(read_options, "key8019", &value);
  if (status.ok()) {
    out << "GET key8019 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26623: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2027", "key5762", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2027, key5762) size=" << scan_result.size() << std::endl;
  }

  // op 26624: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 26625: get
  status = db->Get(read_options, "key1238", &value);
  if (status.ok()) {
    out << "GET key1238 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26626: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 26627: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5364", "key5937", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5364, key5937) size=" << scan_result.size() << std::endl;
  }

  // op 26628: put
  db->Put(write_options, "key6310", "value17297");

  // op 26629: delete_range
  status = db->DeleteRange(write_options, "key3741", "key6663");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 26630: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4276", "key7126", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4276, key7126) size=" << scan_result.size() << std::endl;
  }

  // op 26631: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4231", "key6026", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4231, key6026) size=" << scan_result.size() << std::endl;
  }

  // op 26632: put
  db->Put(write_options, "key10477", "value27363");

  // op 26633: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8244", "key10531", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8244, key10531) size=" << scan_result.size() << std::endl;
  }

  // op 26634: delete
  db->Delete(write_options, "key6756");

  // op 26635: put
  db->Put(write_options, "key9738", "value63258");

  // op 26636: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 26637: delete
  db->Delete(write_options, "key2697");

  // op 26638: delete
  db->Delete(write_options, "key6827");

  // op 26639: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1876", "key1998", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1876, key1998) size=" << scan_result.size() << std::endl;
  }

  // op 26640: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7309", "key9261", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7309, key9261) size=" << scan_result.size() << std::endl;
  }

  // op 26641: put
  db->Put(write_options, "key0766", "value59617");

  // op 26642: delete
  db->Delete(write_options, "key1613");

  // op 26643: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 26644: put
  db->Put(write_options, "key6748", "value100671");

  // op 26645: get
  status = db->Get(read_options, "key3202", &value);
  if (status.ok()) {
    out << "GET key3202 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26646: put
  db->Put(write_options, "key9804", "value18386");

  // op 26647: get
  status = db->Get(read_options, "key0290", &value);
  if (status.ok()) {
    out << "GET key0290 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26648: put
  db->Put(write_options, "key1559", "value24926");

  // op 26649: put
  db->Put(write_options, "key1381", "value79248");

  // op 26650: get
  status = db->Get(read_options, "key10211", &value);
  if (status.ok()) {
    out << "GET key10211 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26651: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 26652: put
  db->Put(write_options, "key5223", "value89306");

  // op 26653: put
  db->Put(write_options, "key1443", "value54714");

  // op 26654: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0249", "key8490", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0249, key8490) size=" << scan_result.size() << std::endl;
  }

  // op 26655: put
  db->Put(write_options, "key3035", "value112139");

  // op 26656: put
  db->Put(write_options, "key1959", "value99880");

  // op 26657: get
  status = db->Get(read_options, "key5734", &value);
  if (status.ok()) {
    out << "GET key5734 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26658: delete_range
  status = db->DeleteRange(write_options, "key0251", "key1678");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 26659: put
  db->Put(write_options, "key4457", "value18754");

  // op 26660: put
  db->Put(write_options, "key2566", "value109407");

  // op 26661: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6437", "key10905", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6437, key10905) size=" << scan_result.size() << std::endl;
  }

  // op 26662: put
  db->Put(write_options, "key4295", "value112159");

  // op 26663: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0293", "key7818", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0293, key7818) size=" << scan_result.size() << std::endl;
  }

  // op 26664: put
  db->Put(write_options, "key2262", "value54842");

  // op 26665: get
  status = db->Get(read_options, "key7670", &value);
  if (status.ok()) {
    out << "GET key7670 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26666: get
  status = db->Get(read_options, "key8647", &value);
  if (status.ok()) {
    out << "GET key8647 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26667: get
  status = db->Get(read_options, "key3952", &value);
  if (status.ok()) {
    out << "GET key3952 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26668: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8481", "key11451", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8481, key11451) size=" << scan_result.size() << std::endl;
  }

  // op 26669: get
  status = db->Get(read_options, "key2858", &value);
  if (status.ok()) {
    out << "GET key2858 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26670: delete_range
  status = db->DeleteRange(write_options, "key4068", "key6122");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 26671: put
  db->Put(write_options, "key6452", "value12218");

  // op 26672: put
  db->Put(write_options, "key9308", "value44690");

  // op 26673: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1743", "key8398", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1743, key8398) size=" << scan_result.size() << std::endl;
  }

  // op 26674: delete
  db->Delete(write_options, "key10128");

  // op 26675: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3415", "key9219", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3415, key9219) size=" << scan_result.size() << std::endl;
  }

  // op 26676: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2618", "key4251", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2618, key4251) size=" << scan_result.size() << std::endl;
  }

  // op 26677: delete_range
  status = db->DeleteRange(write_options, "key2086", "key4511");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 26678: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 26679: put
  db->Put(write_options, "key8236", "value77837");

  // op 26680: put
  db->Put(write_options, "key4761", "value42575");

  // op 26681: get
  status = db->Get(read_options, "key10220", &value);
  if (status.ok()) {
    out << "GET key10220 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26682: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0404", "key9908", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0404, key9908) size=" << scan_result.size() << std::endl;
  }

  // op 26683: put
  db->Put(write_options, "key0723", "value4155");

  // op 26684: get
  status = db->Get(read_options, "key4293", &value);
  if (status.ok()) {
    out << "GET key4293 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26685: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6988", "key9710", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6988, key9710) size=" << scan_result.size() << std::endl;
  }

  // op 26686: get
  status = db->Get(read_options, "key1378", &value);
  if (status.ok()) {
    out << "GET key1378 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26687: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0948", "key3257", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0948, key3257) size=" << scan_result.size() << std::endl;
  }

  // op 26688: get
  status = db->Get(read_options, "key6551", &value);
  if (status.ok()) {
    out << "GET key6551 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26689: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9504", "key9831", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9504, key9831) size=" << scan_result.size() << std::endl;
  }

  // op 26690: delete
  db->Delete(write_options, "key0302");

  // op 26691: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2825", "key3965", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2825, key3965) size=" << scan_result.size() << std::endl;
  }

  // op 26692: put
  db->Put(write_options, "key6925", "value21690");

  // op 26693: put
  db->Put(write_options, "key6312", "value44736");

  // op 26694: get
  status = db->Get(read_options, "key4869", &value);
  if (status.ok()) {
    out << "GET key4869 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26695: put
  db->Put(write_options, "key2724", "value73623");

  // op 26696: put
  db->Put(write_options, "key11490", "value38156");

  // op 26697: put
  db->Put(write_options, "key9423", "value3958");

  // op 26698: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4182", "key10443", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4182, key10443) size=" << scan_result.size() << std::endl;
  }

  // op 26699: get
  status = db->Get(read_options, "key0407", &value);
  if (status.ok()) {
    out << "GET key0407 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26700: delete
  db->Delete(write_options, "key5734");

  // op 26701: get
  status = db->Get(read_options, "key0060", &value);
  if (status.ok()) {
    out << "GET key0060 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26702: delete_range
  status = db->DeleteRange(write_options, "key5164", "key8062");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 26703: put
  db->Put(write_options, "key1412", "value37193");

  // op 26704: put
  db->Put(write_options, "key2554", "value113824");

  // op 26705: put
  db->Put(write_options, "key3148", "value91172");

  // op 26706: put
  db->Put(write_options, "key4113", "value112689");

  // op 26707: put
  db->Put(write_options, "key10246", "value117851");

  // op 26708: delete
  db->Delete(write_options, "key8583");

  // op 26709: delete
  db->Delete(write_options, "key0057");

  // op 26710: put
  db->Put(write_options, "key1782", "value29817");

  // op 26711: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1926", "key2612", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1926, key2612) size=" << scan_result.size() << std::endl;
  }

  // op 26712: put
  db->Put(write_options, "key7959", "value58854");

  // op 26713: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9350", "key10017", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9350, key10017) size=" << scan_result.size() << std::endl;
  }

  // op 26714: put
  db->Put(write_options, "key5627", "value39365");

  // op 26715: put
  db->Put(write_options, "key2065", "value86206");

  // op 26716: delete_range
  status = db->DeleteRange(write_options, "key3691", "key7891");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 26717: get
  status = db->Get(read_options, "key9378", &value);
  if (status.ok()) {
    out << "GET key9378 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26718: get
  status = db->Get(read_options, "key1929", &value);
  if (status.ok()) {
    out << "GET key1929 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26719: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 26720: put
  db->Put(write_options, "key6935", "value84494");

  // op 26721: put
  db->Put(write_options, "key1460", "value41055");

  // op 26722: get
  status = db->Get(read_options, "key11463", &value);
  if (status.ok()) {
    out << "GET key11463 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26723: put
  db->Put(write_options, "key6670", "value0863");

  // op 26724: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6458", "key6923", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6458, key6923) size=" << scan_result.size() << std::endl;
  }

  // op 26725: put
  db->Put(write_options, "key11399", "value75144");

  // op 26726: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0915", "key7300", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0915, key7300) size=" << scan_result.size() << std::endl;
  }

  // op 26727: delete
  db->Delete(write_options, "key5290");

  // op 26728: put
  db->Put(write_options, "key1821", "value45751");

  // op 26729: get
  status = db->Get(read_options, "key8506", &value);
  if (status.ok()) {
    out << "GET key8506 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26730: put
  db->Put(write_options, "key6932", "value35272");

  // op 26731: get
  status = db->Get(read_options, "key1475", &value);
  if (status.ok()) {
    out << "GET key1475 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26732: put
  db->Put(write_options, "key8219", "value64108");

  // op 26733: put
  db->Put(write_options, "key8025", "value91678");

  // op 26734: delete
  db->Delete(write_options, "key3057");

  // op 26735: delete_range
  status = db->DeleteRange(write_options, "key10234", "key10760");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 26736: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6074", "key7231", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6074, key7231) size=" << scan_result.size() << std::endl;
  }

  // op 26737: put
  db->Put(write_options, "key3212", "value79344");

  // op 26738: put
  db->Put(write_options, "key11168", "value0194");

  // op 26739: get
  status = db->Get(read_options, "key8847", &value);
  if (status.ok()) {
    out << "GET key8847 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26740: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1449", "key4558", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1449, key4558) size=" << scan_result.size() << std::endl;
  }

  // op 26741: put
  db->Put(write_options, "key3099", "value102894");

  // op 26742: get
  status = db->Get(read_options, "key0211", &value);
  if (status.ok()) {
    out << "GET key0211 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26743: put
  db->Put(write_options, "key4221", "value90325");

  // op 26744: put
  db->Put(write_options, "key6271", "value34344");

  // op 26745: get
  status = db->Get(read_options, "key1386", &value);
  if (status.ok()) {
    out << "GET key1386 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26746: get
  status = db->Get(read_options, "key8862", &value);
  if (status.ok()) {
    out << "GET key8862 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26747: get
  status = db->Get(read_options, "key1135", &value);
  if (status.ok()) {
    out << "GET key1135 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26748: get
  status = db->Get(read_options, "key6890", &value);
  if (status.ok()) {
    out << "GET key6890 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26749: delete_range
  status = db->DeleteRange(write_options, "key2248", "key3349");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 26750: delete
  db->Delete(write_options, "key4290");

  // op 26751: put
  db->Put(write_options, "key2138", "value33298");

  // op 26752: put
  db->Put(write_options, "key6267", "value88632");

  // op 26753: delete_range
  status = db->DeleteRange(write_options, "key3187", "key10220");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 26754: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1421", "key2102", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1421, key2102) size=" << scan_result.size() << std::endl;
  }

  // op 26755: get
  status = db->Get(read_options, "key5959", &value);
  if (status.ok()) {
    out << "GET key5959 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26756: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 26757: get
  status = db->Get(read_options, "key2270", &value);
  if (status.ok()) {
    out << "GET key2270 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26758: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8115", "key8828", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8115, key8828) size=" << scan_result.size() << std::endl;
  }

  // op 26759: put
  db->Put(write_options, "key10314", "value87727");

  // op 26760: get
  status = db->Get(read_options, "key9225", &value);
  if (status.ok()) {
    out << "GET key9225 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26761: put
  db->Put(write_options, "key4503", "value67689");

  // op 26762: put
  db->Put(write_options, "key9268", "value64268");

  // op 26763: put
  db->Put(write_options, "key7087", "value48767");

  // op 26764: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6495", "key7503", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6495, key7503) size=" << scan_result.size() << std::endl;
  }

  // op 26765: put
  db->Put(write_options, "key11622", "value112752");

  // op 26766: put
  db->Put(write_options, "key8664", "value45313");

  // op 26767: put
  db->Put(write_options, "key10673", "value54772");

  // op 26768: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 26769: delete_range
  status = db->DeleteRange(write_options, "key1323", "key5849");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 26770: put
  db->Put(write_options, "key8449", "value15891");

  // op 26771: delete_range
  status = db->DeleteRange(write_options, "key3579", "key3730");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 26772: put
  db->Put(write_options, "key10318", "value35931");

  // op 26773: put
  db->Put(write_options, "key10051", "value79079");

  // op 26774: get
  status = db->Get(read_options, "key1489", &value);
  if (status.ok()) {
    out << "GET key1489 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26775: put
  db->Put(write_options, "key9818", "value1994");

  // op 26776: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6036", "key10155", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6036, key10155) size=" << scan_result.size() << std::endl;
  }

  // op 26777: get
  status = db->Get(read_options, "key2683", &value);
  if (status.ok()) {
    out << "GET key2683 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26778: delete_range
  status = db->DeleteRange(write_options, "key8560", "key11361");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 26779: put
  db->Put(write_options, "key0025", "value10872");

  // op 26780: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3225", "key3661", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3225, key3661) size=" << scan_result.size() << std::endl;
  }

  // op 26781: get
  status = db->Get(read_options, "key2846", &value);
  if (status.ok()) {
    out << "GET key2846 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26782: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1354", "key6969", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1354, key6969) size=" << scan_result.size() << std::endl;
  }

  // op 26783: delete
  db->Delete(write_options, "key1538");

  // op 26784: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3998", "key8034", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3998, key8034) size=" << scan_result.size() << std::endl;
  }

  // op 26785: get
  status = db->Get(read_options, "key9734", &value);
  if (status.ok()) {
    out << "GET key9734 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26786: put
  db->Put(write_options, "key8063", "value98394");

  // op 26787: get
  status = db->Get(read_options, "key5355", &value);
  if (status.ok()) {
    out << "GET key5355 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26788: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2360", "key11013", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2360, key11013) size=" << scan_result.size() << std::endl;
  }

  // op 26789: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0807", "key3968", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0807, key3968) size=" << scan_result.size() << std::endl;
  }

  // op 26790: delete
  db->Delete(write_options, "key3781");

  // op 26791: delete
  db->Delete(write_options, "key11456");

  // op 26792: put
  db->Put(write_options, "key5620", "value39358");

  // op 26793: get
  status = db->Get(read_options, "key8566", &value);
  if (status.ok()) {
    out << "GET key8566 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26794: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2923", "key7917", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2923, key7917) size=" << scan_result.size() << std::endl;
  }

  // op 26795: put
  db->Put(write_options, "key10045", "value102076");

  // op 26796: get
  status = db->Get(read_options, "key11721", &value);
  if (status.ok()) {
    out << "GET key11721 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26797: put
  db->Put(write_options, "key4145", "value105493");

  // op 26798: get
  status = db->Get(read_options, "key4343", &value);
  if (status.ok()) {
    out << "GET key4343 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26799: put
  db->Put(write_options, "key10312", "value105851");

  // op 26800: get
  status = db->Get(read_options, "key11589", &value);
  if (status.ok()) {
    out << "GET key11589 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26801: put
  db->Put(write_options, "key8803", "value20838");

  // op 26802: delete
  db->Delete(write_options, "key3080");

  // op 26803: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2638", "key6878", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2638, key6878) size=" << scan_result.size() << std::endl;
  }

  // op 26804: get
  status = db->Get(read_options, "key8953", &value);
  if (status.ok()) {
    out << "GET key8953 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26805: delete_range
  status = db->DeleteRange(write_options, "key2355", "key2694");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 26806: delete_range
  status = db->DeleteRange(write_options, "key4286", "key9652");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 26807: delete_range
  status = db->DeleteRange(write_options, "key0861", "key9971");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 26808: put
  db->Put(write_options, "key11490", "value12652");

  // op 26809: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0789", "key7976", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0789, key7976) size=" << scan_result.size() << std::endl;
  }

  // op 26810: get
  status = db->Get(read_options, "key5308", &value);
  if (status.ok()) {
    out << "GET key5308 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26811: put
  db->Put(write_options, "key9446", "value79840");

  // op 26812: get
  status = db->Get(read_options, "key7488", &value);
  if (status.ok()) {
    out << "GET key7488 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26813: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 26814: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3007", "key4174", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3007, key4174) size=" << scan_result.size() << std::endl;
  }

  // op 26815: put
  db->Put(write_options, "key9058", "value50241");

  // op 26816: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2432", "key6927", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2432, key6927) size=" << scan_result.size() << std::endl;
  }

  // op 26817: put
  db->Put(write_options, "key5900", "value44699");

  // op 26818: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8453", "key11063", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8453, key11063) size=" << scan_result.size() << std::endl;
  }

  // op 26819: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1767", "key4104", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1767, key4104) size=" << scan_result.size() << std::endl;
  }

  // op 26820: put
  db->Put(write_options, "key10524", "value58324");

  // op 26821: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8996", "key9179", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8996, key9179) size=" << scan_result.size() << std::endl;
  }

  // op 26822: get
  status = db->Get(read_options, "key6335", &value);
  if (status.ok()) {
    out << "GET key6335 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26823: get
  status = db->Get(read_options, "key4734", &value);
  if (status.ok()) {
    out << "GET key4734 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26824: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 26825: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5035", "key8156", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5035, key8156) size=" << scan_result.size() << std::endl;
  }

  // op 26826: get
  status = db->Get(read_options, "key2590", &value);
  if (status.ok()) {
    out << "GET key2590 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26827: delete
  db->Delete(write_options, "key4103");

  // op 26828: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0788", "key9172", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0788, key9172) size=" << scan_result.size() << std::endl;
  }

  // op 26829: get
  status = db->Get(read_options, "key2326", &value);
  if (status.ok()) {
    out << "GET key2326 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26830: delete
  db->Delete(write_options, "key10116");

  // op 26831: put
  db->Put(write_options, "key0745", "value85644");

  // op 26832: put
  db->Put(write_options, "key5816", "value95295");

  // op 26833: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0007", "key3703", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0007, key3703) size=" << scan_result.size() << std::endl;
  }

  // op 26834: put
  db->Put(write_options, "key3164", "value18212");

  // op 26835: put
  db->Put(write_options, "key4501", "value29348");

  // op 26836: put
  db->Put(write_options, "key10328", "value109132");

  // op 26837: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4094", "key5596", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4094, key5596) size=" << scan_result.size() << std::endl;
  }

  // op 26838: delete
  db->Delete(write_options, "key3313");

  // op 26839: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 26840: put
  db->Put(write_options, "key7965", "value74283");

  // op 26841: put
  db->Put(write_options, "key5128", "value17772");

  // op 26842: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3259", "key4438", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3259, key4438) size=" << scan_result.size() << std::endl;
  }

  // op 26843: put
  db->Put(write_options, "key10705", "value49981");

  // op 26844: get
  status = db->Get(read_options, "key8964", &value);
  if (status.ok()) {
    out << "GET key8964 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26845: put
  db->Put(write_options, "key5038", "value41603");

  // op 26846: put
  db->Put(write_options, "key2825", "value44498");

  // op 26847: put
  db->Put(write_options, "key4381", "value42432");

  // op 26848: put
  db->Put(write_options, "key4516", "value80719");

  // op 26849: get
  status = db->Get(read_options, "key8968", &value);
  if (status.ok()) {
    out << "GET key8968 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26850: put
  db->Put(write_options, "key11015", "value29970");

  // op 26851: put
  db->Put(write_options, "key10842", "value45666");

  // op 26852: delete
  db->Delete(write_options, "key9391");

  // op 26853: put
  db->Put(write_options, "key2565", "value108353");

  // op 26854: put
  db->Put(write_options, "key0070", "value10693");

  // op 26855: put
  db->Put(write_options, "key1812", "value34259");

  // op 26856: delete_range
  status = db->DeleteRange(write_options, "key11062", "key11122");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 26857: get
  status = db->Get(read_options, "key5949", &value);
  if (status.ok()) {
    out << "GET key5949 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26858: put
  db->Put(write_options, "key9177", "value18833");

  // op 26859: delete_range
  status = db->DeleteRange(write_options, "key10043", "key10753");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 26860: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4662", "key10932", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4662, key10932) size=" << scan_result.size() << std::endl;
  }

  // op 26861: put
  db->Put(write_options, "key9166", "value80156");

  // op 26862: put
  db->Put(write_options, "key10473", "value89946");

  // op 26863: get
  status = db->Get(read_options, "key5704", &value);
  if (status.ok()) {
    out << "GET key5704 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26864: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 26865: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0198", "key4996", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0198, key4996) size=" << scan_result.size() << std::endl;
  }

  // op 26866: put
  db->Put(write_options, "key10948", "value47127");

  // op 26867: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4133", "key9563", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4133, key9563) size=" << scan_result.size() << std::endl;
  }

  // op 26868: get
  status = db->Get(read_options, "key9110", &value);
  if (status.ok()) {
    out << "GET key9110 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26869: put
  db->Put(write_options, "key9285", "value93528");

  // op 26870: put
  db->Put(write_options, "key5514", "value24040");

  // op 26871: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5160", "key7954", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5160, key7954) size=" << scan_result.size() << std::endl;
  }

  // op 26872: put
  db->Put(write_options, "key6333", "value1227");

  // op 26873: put
  db->Put(write_options, "key0242", "value38491");

  // op 26874: put
  db->Put(write_options, "key10677", "value18672");

  // op 26875: delete_range
  status = db->DeleteRange(write_options, "key1321", "key10888");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 26876: put
  db->Put(write_options, "key9477", "value94625");

  // op 26877: get
  status = db->Get(read_options, "key3881", &value);
  if (status.ok()) {
    out << "GET key3881 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26878: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1757", "key6945", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1757, key6945) size=" << scan_result.size() << std::endl;
  }

  // op 26879: get
  status = db->Get(read_options, "key7720", &value);
  if (status.ok()) {
    out << "GET key7720 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26880: delete_range
  status = db->DeleteRange(write_options, "key1962", "key4399");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 26881: put
  db->Put(write_options, "key5871", "value12276");

  // op 26882: put
  db->Put(write_options, "key11415", "value4392");

  // op 26883: get
  status = db->Get(read_options, "key9331", &value);
  if (status.ok()) {
    out << "GET key9331 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26884: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 26885: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1285", "key7321", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1285, key7321) size=" << scan_result.size() << std::endl;
  }

  // op 26886: get
  status = db->Get(read_options, "key0013", &value);
  if (status.ok()) {
    out << "GET key0013 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26887: put
  db->Put(write_options, "key0939", "value31286");

  // op 26888: put
  db->Put(write_options, "key7211", "value100755");

  // op 26889: put
  db->Put(write_options, "key6943", "value74223");

  // op 26890: put
  db->Put(write_options, "key8003", "value44727");

  // op 26891: delete_range
  status = db->DeleteRange(write_options, "key1521", "key3798");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 26892: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 26893: get
  status = db->Get(read_options, "key1667", &value);
  if (status.ok()) {
    out << "GET key1667 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26894: delete_range
  status = db->DeleteRange(write_options, "key7209", "key11656");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 26895: put
  db->Put(write_options, "key11295", "value62557");

  // op 26896: delete_range
  status = db->DeleteRange(write_options, "key2005", "key11434");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 26897: get
  status = db->Get(read_options, "key4631", &value);
  if (status.ok()) {
    out << "GET key4631 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26898: put
  db->Put(write_options, "key7545", "value89345");

  // op 26899: get
  status = db->Get(read_options, "key1075", &value);
  if (status.ok()) {
    out << "GET key1075 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26900: delete
  db->Delete(write_options, "key4310");

  // op 26901: put
  db->Put(write_options, "key9091", "value57216");

  // op 26902: put
  db->Put(write_options, "key5298", "value67826");

  // op 26903: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3213", "key9447", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3213, key9447) size=" << scan_result.size() << std::endl;
  }

  // op 26904: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0415", "key3337", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0415, key3337) size=" << scan_result.size() << std::endl;
  }

  // op 26905: put
  db->Put(write_options, "key5141", "value8589");

  // op 26906: put
  db->Put(write_options, "key2853", "value96045");

  // op 26907: put
  db->Put(write_options, "key10141", "value20832");

  // op 26908: delete_range
  status = db->DeleteRange(write_options, "key3865", "key4163");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 26909: put
  db->Put(write_options, "key3813", "value16940");

  // op 26910: get
  status = db->Get(read_options, "key7773", &value);
  if (status.ok()) {
    out << "GET key7773 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26911: delete
  db->Delete(write_options, "key9920");

  // op 26912: put
  db->Put(write_options, "key6086", "value84198");

  // op 26913: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 26914: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1688", "key6881", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1688, key6881) size=" << scan_result.size() << std::endl;
  }

  // op 26915: get
  status = db->Get(read_options, "key1158", &value);
  if (status.ok()) {
    out << "GET key1158 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26916: delete_range
  status = db->DeleteRange(write_options, "key0013", "key2034");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 26917: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 26918: put
  db->Put(write_options, "key5743", "value51947");

  // op 26919: get
  status = db->Get(read_options, "key0581", &value);
  if (status.ok()) {
    out << "GET key0581 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26920: put
  db->Put(write_options, "key7057", "value97043");

  // op 26921: delete
  db->Delete(write_options, "key5497");

  // op 26922: put
  db->Put(write_options, "key2253", "value53310");

  // op 26923: put
  db->Put(write_options, "key5322", "value105131");

  // op 26924: put
  db->Put(write_options, "key2667", "value45500");

  // op 26925: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3347", "key8114", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3347, key8114) size=" << scan_result.size() << std::endl;
  }

  // op 26926: put
  db->Put(write_options, "key3608", "value110543");

  // op 26927: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 26928: delete_range
  status = db->DeleteRange(write_options, "key0576", "key4670");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 26929: delete
  db->Delete(write_options, "key8369");

  // op 26930: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1325", "key7772", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1325, key7772) size=" << scan_result.size() << std::endl;
  }

  // op 26931: put
  db->Put(write_options, "key9287", "value2197");

  // op 26932: put
  db->Put(write_options, "key11034", "value73023");

  // op 26933: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2140", "key10066", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2140, key10066) size=" << scan_result.size() << std::endl;
  }

  // op 26934: delete_range
  status = db->DeleteRange(write_options, "key5814", "key10097");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 26935: put
  db->Put(write_options, "key2184", "value34452");

  // op 26936: delete_range
  status = db->DeleteRange(write_options, "key7409", "key8286");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 26937: put
  db->Put(write_options, "key10801", "value2590");

  // op 26938: get
  status = db->Get(read_options, "key11524", &value);
  if (status.ok()) {
    out << "GET key11524 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26939: put
  db->Put(write_options, "key6111", "value102388");

  // op 26940: put
  db->Put(write_options, "key9341", "value63005");

  // op 26941: put
  db->Put(write_options, "key6977", "value87407");

  // op 26942: put
  db->Put(write_options, "key0536", "value46644");

  // op 26943: delete
  db->Delete(write_options, "key0139");

  // op 26944: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0213", "key3789", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0213, key3789) size=" << scan_result.size() << std::endl;
  }

  // op 26945: put
  db->Put(write_options, "key2735", "value21736");

  // op 26946: get
  status = db->Get(read_options, "key0373", &value);
  if (status.ok()) {
    out << "GET key0373 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26947: get
  status = db->Get(read_options, "key4355", &value);
  if (status.ok()) {
    out << "GET key4355 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26948: put
  db->Put(write_options, "key5174", "value79705");

  // op 26949: put
  db->Put(write_options, "key1316", "value0355");

  // op 26950: put
  db->Put(write_options, "key9955", "value34185");

  // op 26951: delete
  db->Delete(write_options, "key3082");

  // op 26952: put
  db->Put(write_options, "key6060", "value44101");

  // op 26953: put
  db->Put(write_options, "key3872", "value82299");

  // op 26954: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0910", "key4159", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0910, key4159) size=" << scan_result.size() << std::endl;
  }

  // op 26955: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 26956: get
  status = db->Get(read_options, "key0899", &value);
  if (status.ok()) {
    out << "GET key0899 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26957: get
  status = db->Get(read_options, "key3428", &value);
  if (status.ok()) {
    out << "GET key3428 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26958: put
  db->Put(write_options, "key3235", "value77166");

  // op 26959: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0642", "key6564", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0642, key6564) size=" << scan_result.size() << std::endl;
  }

  // op 26960: get
  status = db->Get(read_options, "key0287", &value);
  if (status.ok()) {
    out << "GET key0287 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26961: get
  status = db->Get(read_options, "key3178", &value);
  if (status.ok()) {
    out << "GET key3178 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26962: put
  db->Put(write_options, "key9090", "value86621");

  // op 26963: get
  status = db->Get(read_options, "key2870", &value);
  if (status.ok()) {
    out << "GET key2870 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26964: put
  db->Put(write_options, "key9418", "value40757");

  // op 26965: put
  db->Put(write_options, "key0129", "value14376");

  // op 26966: delete
  db->Delete(write_options, "key1052");

  // op 26967: put
  db->Put(write_options, "key1219", "value7866");

  // op 26968: put
  db->Put(write_options, "key6918", "value86377");

  // op 26969: delete
  db->Delete(write_options, "key3107");

  // op 26970: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5416", "key8818", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5416, key8818) size=" << scan_result.size() << std::endl;
  }

  // op 26971: delete_range
  status = db->DeleteRange(write_options, "key6455", "key7521");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 26972: put
  db->Put(write_options, "key10235", "value66479");

  // op 26973: delete_range
  status = db->DeleteRange(write_options, "key4675", "key11271");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 26974: get
  status = db->Get(read_options, "key5797", &value);
  if (status.ok()) {
    out << "GET key5797 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26975: put
  db->Put(write_options, "key0846", "value82209");

  // op 26976: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6405", "key6693", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6405, key6693) size=" << scan_result.size() << std::endl;
  }

  // op 26977: delete_range
  status = db->DeleteRange(write_options, "key6996", "key7225");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 26978: get
  status = db->Get(read_options, "key6479", &value);
  if (status.ok()) {
    out << "GET key6479 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26979: put
  db->Put(write_options, "key8886", "value7843");

  // op 26980: delete_range
  status = db->DeleteRange(write_options, "key2796", "key3226");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 26981: put
  db->Put(write_options, "key5850", "value60989");

  // op 26982: get
  status = db->Get(read_options, "key5276", &value);
  if (status.ok()) {
    out << "GET key5276 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26983: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2049", "key6544", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2049, key6544) size=" << scan_result.size() << std::endl;
  }

  // op 26984: put
  db->Put(write_options, "key7624", "value75575");

  // op 26985: put
  db->Put(write_options, "key5823", "value11272");

  // op 26986: get
  status = db->Get(read_options, "key6442", &value);
  if (status.ok()) {
    out << "GET key6442 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26987: delete_range
  status = db->DeleteRange(write_options, "key8439", "key10984");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 26988: delete_range
  status = db->DeleteRange(write_options, "key3207", "key4672");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 26989: put
  db->Put(write_options, "key5905", "value113273");

  // op 26990: put
  db->Put(write_options, "key3186", "value7220");

  // op 26991: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0253", "key2336", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0253, key2336) size=" << scan_result.size() << std::endl;
  }

  // op 26992: get
  status = db->Get(read_options, "key11268", &value);
  if (status.ok()) {
    out << "GET key11268 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26993: get
  status = db->Get(read_options, "key7059", &value);
  if (status.ok()) {
    out << "GET key7059 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26994: put
  db->Put(write_options, "key3527", "value67842");

  // op 26995: get
  status = db->Get(read_options, "key3576", &value);
  if (status.ok()) {
    out << "GET key3576 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26996: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4429", "key4997", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4429, key4997) size=" << scan_result.size() << std::endl;
  }

  // op 26997: get
  status = db->Get(read_options, "key10624", &value);
  if (status.ok()) {
    out << "GET key10624 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 26998: put
  db->Put(write_options, "key9376", "value14589");

  // op 26999: put
  db->Put(write_options, "key4572", "value9103");

  // op 27000: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 27001: put
  db->Put(write_options, "key4205", "value92307");

  // op 27002: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 27003: put
  db->Put(write_options, "key5421", "value0802");

  // op 27004: delete
  db->Delete(write_options, "key4320");

  // op 27005: get
  status = db->Get(read_options, "key10895", &value);
  if (status.ok()) {
    out << "GET key10895 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27006: put
  db->Put(write_options, "key1552", "value11639");

  // op 27007: put
  db->Put(write_options, "key11453", "value26343");

  // op 27008: put
  db->Put(write_options, "key3699", "value56503");

  // op 27009: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0611", "key4738", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0611, key4738) size=" << scan_result.size() << std::endl;
  }

  // op 27010: get
  status = db->Get(read_options, "key10345", &value);
  if (status.ok()) {
    out << "GET key10345 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27011: put
  db->Put(write_options, "key1499", "value112305");

  // op 27012: put
  db->Put(write_options, "key7444", "value49153");

  // op 27013: put
  db->Put(write_options, "key1670", "value30236");

  // op 27014: delete_range
  status = db->DeleteRange(write_options, "key3541", "key10834");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 27015: get
  status = db->Get(read_options, "key0375", &value);
  if (status.ok()) {
    out << "GET key0375 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27016: put
  db->Put(write_options, "key9277", "value96160");

  // op 27017: put
  db->Put(write_options, "key8368", "value97466");

  // op 27018: get
  status = db->Get(read_options, "key2350", &value);
  if (status.ok()) {
    out << "GET key2350 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27019: put
  db->Put(write_options, "key10315", "value75301");

  // op 27020: put
  db->Put(write_options, "key5851", "value114238");

  // op 27021: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8533", "key9427", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8533, key9427) size=" << scan_result.size() << std::endl;
  }

  // op 27022: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8303", "key10624", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8303, key10624) size=" << scan_result.size() << std::endl;
  }

  // op 27023: delete_range
  status = db->DeleteRange(write_options, "key3714", "key10029");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 27024: put
  db->Put(write_options, "key3176", "value11866");

  // op 27025: put
  db->Put(write_options, "key1927", "value40752");

  // op 27026: delete
  db->Delete(write_options, "key11155");

  // op 27027: put
  db->Put(write_options, "key5360", "value50419");

  // op 27028: delete_range
  status = db->DeleteRange(write_options, "key1250", "key6766");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 27029: delete
  db->Delete(write_options, "key11472");

  // op 27030: delete_range
  status = db->DeleteRange(write_options, "key5723", "key9401");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 27031: delete_range
  status = db->DeleteRange(write_options, "key0789", "key9326");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 27032: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6184", "key6322", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6184, key6322) size=" << scan_result.size() << std::endl;
  }

  // op 27033: get
  status = db->Get(read_options, "key1556", &value);
  if (status.ok()) {
    out << "GET key1556 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27034: delete_range
  status = db->DeleteRange(write_options, "key2839", "key7151");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 27035: put
  db->Put(write_options, "key2461", "value84079");

  // op 27036: get
  status = db->Get(read_options, "key5924", &value);
  if (status.ok()) {
    out << "GET key5924 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27037: delete
  db->Delete(write_options, "key2140");

  // op 27038: put
  db->Put(write_options, "key3531", "value48598");

  // op 27039: delete_range
  status = db->DeleteRange(write_options, "key3724", "key5115");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 27040: get
  status = db->Get(read_options, "key7070", &value);
  if (status.ok()) {
    out << "GET key7070 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27041: put
  db->Put(write_options, "key8805", "value81594");

  // op 27042: put
  db->Put(write_options, "key7173", "value103024");

  // op 27043: put
  db->Put(write_options, "key9375", "value86762");

  // op 27044: put
  db->Put(write_options, "key3546", "value37137");

  // op 27045: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 27046: delete_range
  status = db->DeleteRange(write_options, "key8972", "key11200");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 27047: delete
  db->Delete(write_options, "key1728");

  // op 27048: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 27049: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7765", "key8415", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7765, key8415) size=" << scan_result.size() << std::endl;
  }

  // op 27050: put
  db->Put(write_options, "key3322", "value89365");

  // op 27051: put
  db->Put(write_options, "key3238", "value73914");

  // op 27052: get
  status = db->Get(read_options, "key7969", &value);
  if (status.ok()) {
    out << "GET key7969 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27053: put
  db->Put(write_options, "key2196", "value93767");

  // op 27054: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 27055: put
  db->Put(write_options, "key2879", "value97895");

  // op 27056: put
  db->Put(write_options, "key9219", "value40477");

  // op 27057: put
  db->Put(write_options, "key8030", "value32055");

  // op 27058: put
  db->Put(write_options, "key5378", "value6983");

  // op 27059: delete
  db->Delete(write_options, "key7818");

  // op 27060: get
  status = db->Get(read_options, "key10702", &value);
  if (status.ok()) {
    out << "GET key10702 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27061: put
  db->Put(write_options, "key8064", "value56682");

  // op 27062: get
  status = db->Get(read_options, "key11406", &value);
  if (status.ok()) {
    out << "GET key11406 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27063: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3735", "key10087", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3735, key10087) size=" << scan_result.size() << std::endl;
  }

  // op 27064: put
  db->Put(write_options, "key3527", "value48264");

  // op 27065: delete
  db->Delete(write_options, "key6550");

  // op 27066: get
  status = db->Get(read_options, "key2350", &value);
  if (status.ok()) {
    out << "GET key2350 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27067: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2932", "key3561", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2932, key3561) size=" << scan_result.size() << std::endl;
  }

  // op 27068: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3154", "key10470", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3154, key10470) size=" << scan_result.size() << std::endl;
  }

  // op 27069: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3701", "key4236", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3701, key4236) size=" << scan_result.size() << std::endl;
  }

  // op 27070: put
  db->Put(write_options, "key10830", "value36365");

  // op 27071: put
  db->Put(write_options, "key5170", "value51317");

  // op 27072: put
  db->Put(write_options, "key1621", "value91541");

  // op 27073: put
  db->Put(write_options, "key10382", "value107188");

  // op 27074: put
  db->Put(write_options, "key11112", "value88734");

  // op 27075: delete
  db->Delete(write_options, "key9347");

  // op 27076: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3036", "key6618", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3036, key6618) size=" << scan_result.size() << std::endl;
  }

  // op 27077: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2294", "key6994", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2294, key6994) size=" << scan_result.size() << std::endl;
  }

  // op 27078: put
  db->Put(write_options, "key6648", "value85155");

  // op 27079: delete
  db->Delete(write_options, "key3234");

  // op 27080: delete
  db->Delete(write_options, "key4948");

  // op 27081: delete
  db->Delete(write_options, "key2835");

  // op 27082: put
  db->Put(write_options, "key8186", "value55519");

  // op 27083: put
  db->Put(write_options, "key4049", "value51241");

  // op 27084: get
  status = db->Get(read_options, "key3790", &value);
  if (status.ok()) {
    out << "GET key3790 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27085: put
  db->Put(write_options, "key0274", "value68850");

  // op 27086: get
  status = db->Get(read_options, "key1847", &value);
  if (status.ok()) {
    out << "GET key1847 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27087: put
  db->Put(write_options, "key4862", "value13974");

  // op 27088: put
  db->Put(write_options, "key0547", "value67384");

  // op 27089: get
  status = db->Get(read_options, "key1347", &value);
  if (status.ok()) {
    out << "GET key1347 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27090: put
  db->Put(write_options, "key8744", "value3433");

  // op 27091: put
  db->Put(write_options, "key6361", "value107159");

  // op 27092: delete
  db->Delete(write_options, "key6973");

  // op 27093: put
  db->Put(write_options, "key7461", "value45292");

  // op 27094: delete
  db->Delete(write_options, "key7710");

  // op 27095: get
  status = db->Get(read_options, "key6486", &value);
  if (status.ok()) {
    out << "GET key6486 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27096: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0267", "key11025", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0267, key11025) size=" << scan_result.size() << std::endl;
  }

  // op 27097: delete
  db->Delete(write_options, "key2844");

  // op 27098: get
  status = db->Get(read_options, "key0781", &value);
  if (status.ok()) {
    out << "GET key0781 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27099: get
  status = db->Get(read_options, "key3859", &value);
  if (status.ok()) {
    out << "GET key3859 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27100: delete
  db->Delete(write_options, "key8177");

  // op 27101: put
  db->Put(write_options, "key10532", "value25514");

  // op 27102: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3805", "key4331", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3805, key4331) size=" << scan_result.size() << std::endl;
  }

  // op 27103: put
  db->Put(write_options, "key3281", "value92208");

  // op 27104: get
  status = db->Get(read_options, "key2351", &value);
  if (status.ok()) {
    out << "GET key2351 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27105: put
  db->Put(write_options, "key1769", "value47344");

  // op 27106: put
  db->Put(write_options, "key0274", "value109251");

  // op 27107: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6946", "key10995", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6946, key10995) size=" << scan_result.size() << std::endl;
  }

  // op 27108: put
  db->Put(write_options, "key1461", "value102532");

  // op 27109: get
  status = db->Get(read_options, "key0302", &value);
  if (status.ok()) {
    out << "GET key0302 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27110: put
  db->Put(write_options, "key2833", "value2677");

  // op 27111: delete_range
  status = db->DeleteRange(write_options, "key3704", "key6509");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 27112: delete_range
  status = db->DeleteRange(write_options, "key4042", "key8169");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 27113: get
  status = db->Get(read_options, "key3472", &value);
  if (status.ok()) {
    out << "GET key3472 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27114: delete
  db->Delete(write_options, "key2630");

  // op 27115: delete
  db->Delete(write_options, "key4806");

  // op 27116: put
  db->Put(write_options, "key5358", "value86419");

  // op 27117: delete
  db->Delete(write_options, "key5241");

  // op 27118: put
  db->Put(write_options, "key3097", "value17202");

  // op 27119: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4149", "key8306", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4149, key8306) size=" << scan_result.size() << std::endl;
  }

  // op 27120: put
  db->Put(write_options, "key7298", "value28105");

  // op 27121: delete_range
  status = db->DeleteRange(write_options, "key5302", "key7023");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 27122: put
  db->Put(write_options, "key7984", "value80679");

  // op 27123: put
  db->Put(write_options, "key2754", "value12540");

  // op 27124: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1800", "key7522", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1800, key7522) size=" << scan_result.size() << std::endl;
  }

  // op 27125: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0554", "key7397", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0554, key7397) size=" << scan_result.size() << std::endl;
  }

  // op 27126: get
  status = db->Get(read_options, "key7087", &value);
  if (status.ok()) {
    out << "GET key7087 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27127: get
  status = db->Get(read_options, "key8739", &value);
  if (status.ok()) {
    out << "GET key8739 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27128: put
  db->Put(write_options, "key5327", "value117733");

  // op 27129: put
  db->Put(write_options, "key7263", "value89105");

  // op 27130: put
  db->Put(write_options, "key3812", "value84203");

  // op 27131: delete
  db->Delete(write_options, "key7621");

  // op 27132: put
  db->Put(write_options, "key6947", "value8917");

  // op 27133: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 27134: put
  db->Put(write_options, "key5150", "value88226");

  // op 27135: put
  db->Put(write_options, "key4880", "value110037");

  // op 27136: get
  status = db->Get(read_options, "key0458", &value);
  if (status.ok()) {
    out << "GET key0458 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27137: delete_range
  status = db->DeleteRange(write_options, "key2290", "key9486");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 27138: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2074", "key11555", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2074, key11555) size=" << scan_result.size() << std::endl;
  }

  // op 27139: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3621", "key4934", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3621, key4934) size=" << scan_result.size() << std::endl;
  }

  // op 27140: get
  status = db->Get(read_options, "key0918", &value);
  if (status.ok()) {
    out << "GET key0918 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27141: put
  db->Put(write_options, "key5148", "value47855");

  // op 27142: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4283", "key4804", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4283, key4804) size=" << scan_result.size() << std::endl;
  }

  // op 27143: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3567", "key10884", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3567, key10884) size=" << scan_result.size() << std::endl;
  }

  // op 27144: put
  db->Put(write_options, "key11494", "value86804");

  // op 27145: delete_range
  status = db->DeleteRange(write_options, "key1258", "key5862");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 27146: put
  db->Put(write_options, "key6849", "value81896");

  // op 27147: put
  db->Put(write_options, "key1984", "value52118");

  // op 27148: get
  status = db->Get(read_options, "key4575", &value);
  if (status.ok()) {
    out << "GET key4575 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27149: put
  db->Put(write_options, "key8920", "value78745");

  // op 27150: get
  status = db->Get(read_options, "key8953", &value);
  if (status.ok()) {
    out << "GET key8953 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27151: put
  db->Put(write_options, "key3283", "value47612");

  // op 27152: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4064", "key4719", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4064, key4719) size=" << scan_result.size() << std::endl;
  }

  // op 27153: get
  status = db->Get(read_options, "key9612", &value);
  if (status.ok()) {
    out << "GET key9612 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27154: delete_range
  status = db->DeleteRange(write_options, "key8152", "key11412");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 27155: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2056", "key9102", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2056, key9102) size=" << scan_result.size() << std::endl;
  }

  // op 27156: put
  db->Put(write_options, "key10631", "value34185");

  // op 27157: get
  status = db->Get(read_options, "key4572", &value);
  if (status.ok()) {
    out << "GET key4572 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27158: delete
  db->Delete(write_options, "key3468");

  // op 27159: delete_range
  status = db->DeleteRange(write_options, "key1267", "key9093");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 27160: delete
  db->Delete(write_options, "key6738");

  // op 27161: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5007", "key9956", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5007, key9956) size=" << scan_result.size() << std::endl;
  }

  // op 27162: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7655", "key11463", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7655, key11463) size=" << scan_result.size() << std::endl;
  }

  // op 27163: get
  status = db->Get(read_options, "key5869", &value);
  if (status.ok()) {
    out << "GET key5869 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27164: put
  db->Put(write_options, "key11715", "value0927");

  // op 27165: put
  db->Put(write_options, "key6767", "value0143");

  // op 27166: put
  db->Put(write_options, "key11529", "value57135");

  // op 27167: get
  status = db->Get(read_options, "key7189", &value);
  if (status.ok()) {
    out << "GET key7189 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27168: put
  db->Put(write_options, "key3964", "value37480");

  // op 27169: put
  db->Put(write_options, "key2174", "value63232");

  // op 27170: put
  db->Put(write_options, "key8166", "value6710");

  // op 27171: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4129", "key8397", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4129, key8397) size=" << scan_result.size() << std::endl;
  }

  // op 27172: put
  db->Put(write_options, "key10958", "value39893");

  // op 27173: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1557", "key3562", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1557, key3562) size=" << scan_result.size() << std::endl;
  }

  // op 27174: put
  db->Put(write_options, "key10005", "value22314");

  // op 27175: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2677", "key5347", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2677, key5347) size=" << scan_result.size() << std::endl;
  }

  // op 27176: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 27177: put
  db->Put(write_options, "key7354", "value52091");

  // op 27178: put
  db->Put(write_options, "key7807", "value99566");

  // op 27179: put
  db->Put(write_options, "key1113", "value6879");

  // op 27180: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0596", "key10426", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0596, key10426) size=" << scan_result.size() << std::endl;
  }

  // op 27181: put
  db->Put(write_options, "key4500", "value114019");

  // op 27182: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 27183: put
  db->Put(write_options, "key8928", "value37035");

  // op 27184: delete_range
  status = db->DeleteRange(write_options, "key6889", "key10139");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 27185: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2130", "key7832", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2130, key7832) size=" << scan_result.size() << std::endl;
  }

  // op 27186: put
  db->Put(write_options, "key4844", "value93605");

  // op 27187: put
  db->Put(write_options, "key10023", "value47868");

  // op 27188: put
  db->Put(write_options, "key2027", "value29292");

  // op 27189: get
  status = db->Get(read_options, "key2991", &value);
  if (status.ok()) {
    out << "GET key2991 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27190: get
  status = db->Get(read_options, "key7801", &value);
  if (status.ok()) {
    out << "GET key7801 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27191: put
  db->Put(write_options, "key2348", "value23989");

  // op 27192: put
  db->Put(write_options, "key8273", "value98333");

  // op 27193: put
  db->Put(write_options, "key8001", "value51381");

  // op 27194: get
  status = db->Get(read_options, "key9872", &value);
  if (status.ok()) {
    out << "GET key9872 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27195: delete_range
  status = db->DeleteRange(write_options, "key2773", "key6668");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 27196: get
  status = db->Get(read_options, "key9134", &value);
  if (status.ok()) {
    out << "GET key9134 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27197: get
  status = db->Get(read_options, "key11544", &value);
  if (status.ok()) {
    out << "GET key11544 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27198: put
  db->Put(write_options, "key8643", "value32718");

  // op 27199: delete_range
  status = db->DeleteRange(write_options, "key0381", "key10595");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 27200: get
  status = db->Get(read_options, "key3857", &value);
  if (status.ok()) {
    out << "GET key3857 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27201: put
  db->Put(write_options, "key6124", "value57729");

  // op 27202: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 27203: put
  db->Put(write_options, "key7957", "value110928");

  // op 27204: delete
  db->Delete(write_options, "key3267");

  // op 27205: put
  db->Put(write_options, "key2467", "value115939");

  // op 27206: get
  status = db->Get(read_options, "key0668", &value);
  if (status.ok()) {
    out << "GET key0668 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27207: put
  db->Put(write_options, "key9314", "value73618");

  // op 27208: delete_range
  status = db->DeleteRange(write_options, "key4227", "key7927");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 27209: delete_range
  status = db->DeleteRange(write_options, "key3328", "key3464");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 27210: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3011", "key7705", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3011, key7705) size=" << scan_result.size() << std::endl;
  }

  // op 27211: get
  status = db->Get(read_options, "key1182", &value);
  if (status.ok()) {
    out << "GET key1182 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27212: put
  db->Put(write_options, "key1512", "value75066");

  // op 27213: put
  db->Put(write_options, "key4561", "value17486");

  // op 27214: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5976", "key10425", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5976, key10425) size=" << scan_result.size() << std::endl;
  }

  // op 27215: get
  status = db->Get(read_options, "key6239", &value);
  if (status.ok()) {
    out << "GET key6239 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27216: get
  status = db->Get(read_options, "key10516", &value);
  if (status.ok()) {
    out << "GET key10516 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27217: get
  status = db->Get(read_options, "key10545", &value);
  if (status.ok()) {
    out << "GET key10545 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27218: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 27219: delete_range
  status = db->DeleteRange(write_options, "key5142", "key10784");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 27220: delete
  db->Delete(write_options, "key1817");

  // op 27221: put
  db->Put(write_options, "key6707", "value15675");

  // op 27222: get
  status = db->Get(read_options, "key10192", &value);
  if (status.ok()) {
    out << "GET key10192 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27223: put
  db->Put(write_options, "key5263", "value37642");

  // op 27224: get
  status = db->Get(read_options, "key7578", &value);
  if (status.ok()) {
    out << "GET key7578 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27225: get
  status = db->Get(read_options, "key9542", &value);
  if (status.ok()) {
    out << "GET key9542 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27226: get
  status = db->Get(read_options, "key0341", &value);
  if (status.ok()) {
    out << "GET key0341 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27227: get
  status = db->Get(read_options, "key11543", &value);
  if (status.ok()) {
    out << "GET key11543 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27228: delete_range
  status = db->DeleteRange(write_options, "key10891", "key11231");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 27229: put
  db->Put(write_options, "key5310", "value37465");

  // op 27230: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8768", "key10460", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8768, key10460) size=" << scan_result.size() << std::endl;
  }

  // op 27231: delete
  db->Delete(write_options, "key7610");

  // op 27232: delete_range
  status = db->DeleteRange(write_options, "key0475", "key9594");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 27233: put
  db->Put(write_options, "key8355", "value63621");

  // op 27234: get
  status = db->Get(read_options, "key5011", &value);
  if (status.ok()) {
    out << "GET key5011 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27235: put
  db->Put(write_options, "key10366", "value55597");

  // op 27236: get
  status = db->Get(read_options, "key11200", &value);
  if (status.ok()) {
    out << "GET key11200 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27237: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 27238: put
  db->Put(write_options, "key11433", "value93119");

  // op 27239: delete_range
  status = db->DeleteRange(write_options, "key6452", "key10938");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 27240: put
  db->Put(write_options, "key4404", "value25033");

  // op 27241: put
  db->Put(write_options, "key9662", "value88840");

  // op 27242: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0167", "key4073", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0167, key4073) size=" << scan_result.size() << std::endl;
  }

  // op 27243: delete_range
  status = db->DeleteRange(write_options, "key6584", "key10864");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 27244: get
  status = db->Get(read_options, "key2087", &value);
  if (status.ok()) {
    out << "GET key2087 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27245: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4495", "key10994", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4495, key10994) size=" << scan_result.size() << std::endl;
  }

  // op 27246: put
  db->Put(write_options, "key2155", "value104881");

  // op 27247: delete
  db->Delete(write_options, "key2137");

  // op 27248: put
  db->Put(write_options, "key4474", "value21209");

  // op 27249: delete_range
  status = db->DeleteRange(write_options, "key9277", "key10754");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 27250: put
  db->Put(write_options, "key10683", "value13038");

  // op 27251: get
  status = db->Get(read_options, "key6055", &value);
  if (status.ok()) {
    out << "GET key6055 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27252: put
  db->Put(write_options, "key3751", "value69076");

  // op 27253: put
  db->Put(write_options, "key9960", "value63439");

  // op 27254: put
  db->Put(write_options, "key6603", "value42128");

  // op 27255: get
  status = db->Get(read_options, "key9623", &value);
  if (status.ok()) {
    out << "GET key9623 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27256: put
  db->Put(write_options, "key0316", "value39393");

  // op 27257: put
  db->Put(write_options, "key1180", "value71490");

  // op 27258: delete
  db->Delete(write_options, "key9395");

  // op 27259: put
  db->Put(write_options, "key0178", "value8362");

  // op 27260: get
  status = db->Get(read_options, "key0608", &value);
  if (status.ok()) {
    out << "GET key0608 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27261: put
  db->Put(write_options, "key8762", "value74726");

  // op 27262: get
  status = db->Get(read_options, "key5793", &value);
  if (status.ok()) {
    out << "GET key5793 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27263: get
  status = db->Get(read_options, "key2623", &value);
  if (status.ok()) {
    out << "GET key2623 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27264: put
  db->Put(write_options, "key10007", "value20220");

  // op 27265: put
  db->Put(write_options, "key8959", "value38736");

  // op 27266: get
  status = db->Get(read_options, "key10267", &value);
  if (status.ok()) {
    out << "GET key10267 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27267: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7786", "key7948", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7786, key7948) size=" << scan_result.size() << std::endl;
  }

  // op 27268: get
  status = db->Get(read_options, "key7623", &value);
  if (status.ok()) {
    out << "GET key7623 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27269: get
  status = db->Get(read_options, "key4729", &value);
  if (status.ok()) {
    out << "GET key4729 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27270: delete_range
  status = db->DeleteRange(write_options, "key2810", "key9696");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 27271: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6340", "key7946", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6340, key7946) size=" << scan_result.size() << std::endl;
  }

  // op 27272: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 27273: delete
  db->Delete(write_options, "key1831");

  // op 27274: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10866", "key11515", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10866, key11515) size=" << scan_result.size() << std::endl;
  }

  // op 27275: put
  db->Put(write_options, "key7944", "value47058");

  // op 27276: put
  db->Put(write_options, "key8527", "value60194");

  // op 27277: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 27278: put
  db->Put(write_options, "key3102", "value68291");

  // op 27279: put
  db->Put(write_options, "key6176", "value94762");

  // op 27280: put
  db->Put(write_options, "key2088", "value66809");

  // op 27281: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3436", "key9848", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3436, key9848) size=" << scan_result.size() << std::endl;
  }

  // op 27282: put
  db->Put(write_options, "key1507", "value99854");

  // op 27283: put
  db->Put(write_options, "key1841", "value69664");

  // op 27284: delete
  db->Delete(write_options, "key9746");

  // op 27285: delete
  db->Delete(write_options, "key0498");

  // op 27286: put
  db->Put(write_options, "key10999", "value41210");

  // op 27287: get
  status = db->Get(read_options, "key4891", &value);
  if (status.ok()) {
    out << "GET key4891 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27288: put
  db->Put(write_options, "key0695", "value52200");

  // op 27289: put
  db->Put(write_options, "key5698", "value40013");

  // op 27290: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 27291: get
  status = db->Get(read_options, "key10151", &value);
  if (status.ok()) {
    out << "GET key10151 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27292: get
  status = db->Get(read_options, "key5740", &value);
  if (status.ok()) {
    out << "GET key5740 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27293: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3603", "key5555", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3603, key5555) size=" << scan_result.size() << std::endl;
  }

  // op 27294: get
  status = db->Get(read_options, "key8290", &value);
  if (status.ok()) {
    out << "GET key8290 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27295: delete_range
  status = db->DeleteRange(write_options, "key2900", "key2961");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 27296: put
  db->Put(write_options, "key8703", "value47940");

  // op 27297: get
  status = db->Get(read_options, "key6627", &value);
  if (status.ok()) {
    out << "GET key6627 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27298: put
  db->Put(write_options, "key3442", "value1049");

  // op 27299: put
  db->Put(write_options, "key9554", "value76744");

  // op 27300: put
  db->Put(write_options, "key11608", "value112367");

  // op 27301: put
  db->Put(write_options, "key9134", "value8233");

  // op 27302: delete_range
  status = db->DeleteRange(write_options, "key2461", "key8916");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 27303: put
  db->Put(write_options, "key7744", "value60526");

  // op 27304: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0122", "key10899", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0122, key10899) size=" << scan_result.size() << std::endl;
  }

  // op 27305: delete_range
  status = db->DeleteRange(write_options, "key2823", "key4947");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 27306: get
  status = db->Get(read_options, "key7452", &value);
  if (status.ok()) {
    out << "GET key7452 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27307: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0306", "key2903", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0306, key2903) size=" << scan_result.size() << std::endl;
  }

  // op 27308: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 27309: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 27310: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8889", "key9942", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8889, key9942) size=" << scan_result.size() << std::endl;
  }

  // op 27311: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7075", "key8439", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7075, key8439) size=" << scan_result.size() << std::endl;
  }

  // op 27312: delete_range
  status = db->DeleteRange(write_options, "key1196", "key9804");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 27313: delete
  db->Delete(write_options, "key3880");

  // op 27314: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10102", "key10628", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10102, key10628) size=" << scan_result.size() << std::endl;
  }

  // op 27315: get
  status = db->Get(read_options, "key6226", &value);
  if (status.ok()) {
    out << "GET key6226 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27316: put
  db->Put(write_options, "key2116", "value62207");

  // op 27317: get
  status = db->Get(read_options, "key4187", &value);
  if (status.ok()) {
    out << "GET key4187 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27318: put
  db->Put(write_options, "key2391", "value64028");

  // op 27319: get
  status = db->Get(read_options, "key1830", &value);
  if (status.ok()) {
    out << "GET key1830 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27320: put
  db->Put(write_options, "key9580", "value49792");

  // op 27321: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1722", "key2452", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1722, key2452) size=" << scan_result.size() << std::endl;
  }

  // op 27322: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3362", "key8596", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3362, key8596) size=" << scan_result.size() << std::endl;
  }

  // op 27323: put
  db->Put(write_options, "key1095", "value92340");

  // op 27324: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 27325: put
  db->Put(write_options, "key8217", "value52258");

  // op 27326: put
  db->Put(write_options, "key5965", "value106561");

  // op 27327: put
  db->Put(write_options, "key5596", "value43113");

  // op 27328: put
  db->Put(write_options, "key3583", "value28339");

  // op 27329: get
  status = db->Get(read_options, "key8988", &value);
  if (status.ok()) {
    out << "GET key8988 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27330: delete
  db->Delete(write_options, "key4217");

  // op 27331: put
  db->Put(write_options, "key3019", "value109173");

  // op 27332: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6636", "key9060", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6636, key9060) size=" << scan_result.size() << std::endl;
  }

  // op 27333: delete_range
  status = db->DeleteRange(write_options, "key0503", "key1419");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 27334: put
  db->Put(write_options, "key6371", "value88835");

  // op 27335: get
  status = db->Get(read_options, "key2364", &value);
  if (status.ok()) {
    out << "GET key2364 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27336: put
  db->Put(write_options, "key9875", "value110419");

  // op 27337: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 27338: put
  db->Put(write_options, "key7493", "value91321");

  // op 27339: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1239", "key9619", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1239, key9619) size=" << scan_result.size() << std::endl;
  }

  // op 27340: get
  status = db->Get(read_options, "key4580", &value);
  if (status.ok()) {
    out << "GET key4580 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27341: put
  db->Put(write_options, "key4306", "value38942");

  // op 27342: put
  db->Put(write_options, "key9551", "value45951");

  // op 27343: put
  db->Put(write_options, "key3647", "value8512");

  // op 27344: put
  db->Put(write_options, "key0373", "value73342");

  // op 27345: get
  status = db->Get(read_options, "key7394", &value);
  if (status.ok()) {
    out << "GET key7394 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27346: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1800", "key9866", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1800, key9866) size=" << scan_result.size() << std::endl;
  }

  // op 27347: put
  db->Put(write_options, "key8807", "value109679");

  // op 27348: put
  db->Put(write_options, "key9293", "value85940");

  // op 27349: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1558", "key8202", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1558, key8202) size=" << scan_result.size() << std::endl;
  }

  // op 27350: put
  db->Put(write_options, "key7347", "value103453");

  // op 27351: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4674", "key9070", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4674, key9070) size=" << scan_result.size() << std::endl;
  }

  // op 27352: put
  db->Put(write_options, "key9977", "value95365");

  // op 27353: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8151", "key10643", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8151, key10643) size=" << scan_result.size() << std::endl;
  }

  // op 27354: put
  db->Put(write_options, "key2755", "value17166");

  // op 27355: put
  db->Put(write_options, "key10533", "value84570");

  // op 27356: get
  status = db->Get(read_options, "key5169", &value);
  if (status.ok()) {
    out << "GET key5169 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27357: delete
  db->Delete(write_options, "key8262");

  // op 27358: put
  db->Put(write_options, "key1846", "value94317");

  // op 27359: delete_range
  status = db->DeleteRange(write_options, "key0270", "key9384");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 27360: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3577", "key8240", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3577, key8240) size=" << scan_result.size() << std::endl;
  }

  // op 27361: put
  db->Put(write_options, "key3229", "value101968");

  // op 27362: get
  status = db->Get(read_options, "key2880", &value);
  if (status.ok()) {
    out << "GET key2880 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27363: delete
  db->Delete(write_options, "key6343");

  // op 27364: delete
  db->Delete(write_options, "key0729");

  // op 27365: delete
  db->Delete(write_options, "key10839");

  // op 27366: get
  status = db->Get(read_options, "key0480", &value);
  if (status.ok()) {
    out << "GET key0480 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27367: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1392", "key9884", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1392, key9884) size=" << scan_result.size() << std::endl;
  }

  // op 27368: put
  db->Put(write_options, "key11292", "value98562");

  // op 27369: put
  db->Put(write_options, "key9907", "value57368");

  // op 27370: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10025", "key10992", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10025, key10992) size=" << scan_result.size() << std::endl;
  }

  // op 27371: get
  status = db->Get(read_options, "key8978", &value);
  if (status.ok()) {
    out << "GET key8978 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27372: put
  db->Put(write_options, "key9977", "value87337");

  // op 27373: put
  db->Put(write_options, "key9106", "value35150");

  // op 27374: delete_range
  status = db->DeleteRange(write_options, "key3640", "key5136");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 27375: put
  db->Put(write_options, "key6281", "value47645");

  // op 27376: get
  status = db->Get(read_options, "key8148", &value);
  if (status.ok()) {
    out << "GET key8148 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27377: put
  db->Put(write_options, "key9815", "value100410");

  // op 27378: put
  db->Put(write_options, "key11725", "value68921");

  // op 27379: delete
  db->Delete(write_options, "key2524");

  // op 27380: put
  db->Put(write_options, "key1119", "value63283");

  // op 27381: put
  db->Put(write_options, "key5866", "value109556");

  // op 27382: put
  db->Put(write_options, "key3659", "value8725");

  // op 27383: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9667", "key10301", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9667, key10301) size=" << scan_result.size() << std::endl;
  }

  // op 27384: put
  db->Put(write_options, "key9758", "value67428");

  // op 27385: get
  status = db->Get(read_options, "key8925", &value);
  if (status.ok()) {
    out << "GET key8925 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27386: get
  status = db->Get(read_options, "key10380", &value);
  if (status.ok()) {
    out << "GET key10380 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27387: delete
  db->Delete(write_options, "key4684");

  // op 27388: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0120", "key3373", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0120, key3373) size=" << scan_result.size() << std::endl;
  }

  // op 27389: put
  db->Put(write_options, "key4006", "value112948");

  // op 27390: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2305", "key6113", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2305, key6113) size=" << scan_result.size() << std::endl;
  }

  // op 27391: delete
  db->Delete(write_options, "key5532");

  // op 27392: delete
  db->Delete(write_options, "key2681");

  // op 27393: delete
  db->Delete(write_options, "key9104");

  // op 27394: delete_range
  status = db->DeleteRange(write_options, "key3145", "key3365");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 27395: get
  status = db->Get(read_options, "key8850", &value);
  if (status.ok()) {
    out << "GET key8850 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27396: delete
  db->Delete(write_options, "key4672");

  // op 27397: put
  db->Put(write_options, "key4286", "value1999");

  // op 27398: put
  db->Put(write_options, "key9571", "value106100");

  // op 27399: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 27400: put
  db->Put(write_options, "key10764", "value2311");

  // op 27401: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1321", "key11723", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1321, key11723) size=" << scan_result.size() << std::endl;
  }

  // op 27402: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7012", "key10713", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7012, key10713) size=" << scan_result.size() << std::endl;
  }

  // op 27403: put
  db->Put(write_options, "key1280", "value101931");

  // op 27404: put
  db->Put(write_options, "key5184", "value51897");

  // op 27405: put
  db->Put(write_options, "key8656", "value68981");

  // op 27406: put
  db->Put(write_options, "key8086", "value66237");

  // op 27407: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6599", "key8332", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6599, key8332) size=" << scan_result.size() << std::endl;
  }

  // op 27408: put
  db->Put(write_options, "key8860", "value25194");

  // op 27409: delete_range
  status = db->DeleteRange(write_options, "key2880", "key7201");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 27410: delete
  db->Delete(write_options, "key7090");

  // op 27411: delete
  db->Delete(write_options, "key7634");

  // op 27412: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1334", "key2386", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1334, key2386) size=" << scan_result.size() << std::endl;
  }

  // op 27413: get
  status = db->Get(read_options, "key10590", &value);
  if (status.ok()) {
    out << "GET key10590 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27414: get
  status = db->Get(read_options, "key7983", &value);
  if (status.ok()) {
    out << "GET key7983 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27415: get
  status = db->Get(read_options, "key2702", &value);
  if (status.ok()) {
    out << "GET key2702 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27416: put
  db->Put(write_options, "key6921", "value10344");

  // op 27417: get
  status = db->Get(read_options, "key4534", &value);
  if (status.ok()) {
    out << "GET key4534 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27418: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1632", "key10993", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1632, key10993) size=" << scan_result.size() << std::endl;
  }

  // op 27419: put
  db->Put(write_options, "key0648", "value33180");

  // op 27420: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5606", "key9276", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5606, key9276) size=" << scan_result.size() << std::endl;
  }

  // op 27421: put
  db->Put(write_options, "key5583", "value32004");

  // op 27422: get
  status = db->Get(read_options, "key9360", &value);
  if (status.ok()) {
    out << "GET key9360 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27423: get
  status = db->Get(read_options, "key4739", &value);
  if (status.ok()) {
    out << "GET key4739 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27424: get
  status = db->Get(read_options, "key1294", &value);
  if (status.ok()) {
    out << "GET key1294 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27425: get
  status = db->Get(read_options, "key2125", &value);
  if (status.ok()) {
    out << "GET key2125 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27426: delete_range
  status = db->DeleteRange(write_options, "key3394", "key9362");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 27427: get
  status = db->Get(read_options, "key11336", &value);
  if (status.ok()) {
    out << "GET key11336 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27428: put
  db->Put(write_options, "key7867", "value45089");

  // op 27429: get
  status = db->Get(read_options, "key11557", &value);
  if (status.ok()) {
    out << "GET key11557 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27430: delete_range
  status = db->DeleteRange(write_options, "key1921", "key10628");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 27431: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9340", "key10911", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9340, key10911) size=" << scan_result.size() << std::endl;
  }

  // op 27432: put
  db->Put(write_options, "key5815", "value5759");

  // op 27433: get
  status = db->Get(read_options, "key11467", &value);
  if (status.ok()) {
    out << "GET key11467 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27434: get
  status = db->Get(read_options, "key3785", &value);
  if (status.ok()) {
    out << "GET key3785 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27435: delete_range
  status = db->DeleteRange(write_options, "key5169", "key10143");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 27436: get
  status = db->Get(read_options, "key10143", &value);
  if (status.ok()) {
    out << "GET key10143 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27437: put
  db->Put(write_options, "key6004", "value50756");

  // op 27438: delete_range
  status = db->DeleteRange(write_options, "key3511", "key6057");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 27439: put
  db->Put(write_options, "key10791", "value16776");

  // op 27440: get
  status = db->Get(read_options, "key0575", &value);
  if (status.ok()) {
    out << "GET key0575 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27441: put
  db->Put(write_options, "key1084", "value45167");

  // op 27442: put
  db->Put(write_options, "key7232", "value50069");

  // op 27443: put
  db->Put(write_options, "key2207", "value47300");

  // op 27444: get
  status = db->Get(read_options, "key4838", &value);
  if (status.ok()) {
    out << "GET key4838 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27445: put
  db->Put(write_options, "key11557", "value90100");

  // op 27446: get
  status = db->Get(read_options, "key1656", &value);
  if (status.ok()) {
    out << "GET key1656 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27447: put
  db->Put(write_options, "key5437", "value6781");

  // op 27448: get
  status = db->Get(read_options, "key10049", &value);
  if (status.ok()) {
    out << "GET key10049 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27449: get
  status = db->Get(read_options, "key7139", &value);
  if (status.ok()) {
    out << "GET key7139 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27450: put
  db->Put(write_options, "key0344", "value102927");

  // op 27451: put
  db->Put(write_options, "key0200", "value71917");

  // op 27452: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3896", "key7991", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3896, key7991) size=" << scan_result.size() << std::endl;
  }

  // op 27453: delete
  db->Delete(write_options, "key5126");

  // op 27454: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 27455: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1755", "key6671", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1755, key6671) size=" << scan_result.size() << std::endl;
  }

  // op 27456: put
  db->Put(write_options, "key1774", "value72125");

  // op 27457: delete
  db->Delete(write_options, "key10587");

  // op 27458: put
  db->Put(write_options, "key9322", "value86426");

  // op 27459: put
  db->Put(write_options, "key8778", "value105181");

  // op 27460: delete
  db->Delete(write_options, "key3492");

  // op 27461: put
  db->Put(write_options, "key9579", "value15819");

  // op 27462: put
  db->Put(write_options, "key6125", "value101611");

  // op 27463: delete
  db->Delete(write_options, "key7659");

  // op 27464: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0045", "key3373", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0045, key3373) size=" << scan_result.size() << std::endl;
  }

  // op 27465: put
  db->Put(write_options, "key4483", "value55320");

  // op 27466: delete_range
  status = db->DeleteRange(write_options, "key0431", "key11335");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 27467: get
  status = db->Get(read_options, "key10504", &value);
  if (status.ok()) {
    out << "GET key10504 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27468: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 27469: delete
  db->Delete(write_options, "key10588");

  // op 27470: delete_range
  status = db->DeleteRange(write_options, "key1550", "key7178");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 27471: get
  status = db->Get(read_options, "key4051", &value);
  if (status.ok()) {
    out << "GET key4051 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27472: get
  status = db->Get(read_options, "key0381", &value);
  if (status.ok()) {
    out << "GET key0381 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27473: put
  db->Put(write_options, "key10612", "value86960");

  // op 27474: delete_range
  status = db->DeleteRange(write_options, "key2747", "key10945");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 27475: put
  db->Put(write_options, "key8457", "value23632");

  // op 27476: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0358", "key6921", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0358, key6921) size=" << scan_result.size() << std::endl;
  }

  // op 27477: delete
  db->Delete(write_options, "key11425");

  // op 27478: put
  db->Put(write_options, "key10250", "value59551");

  // op 27479: get
  status = db->Get(read_options, "key5737", &value);
  if (status.ok()) {
    out << "GET key5737 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27480: put
  db->Put(write_options, "key0109", "value19333");

  // op 27481: delete
  db->Delete(write_options, "key3809");

  // op 27482: delete
  db->Delete(write_options, "key10288");

  // op 27483: delete_range
  status = db->DeleteRange(write_options, "key5909", "key8355");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 27484: put
  db->Put(write_options, "key9539", "value20333");

  // op 27485: delete_range
  status = db->DeleteRange(write_options, "key3196", "key7901");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 27486: get
  status = db->Get(read_options, "key11013", &value);
  if (status.ok()) {
    out << "GET key11013 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27487: put
  db->Put(write_options, "key11015", "value22055");

  // op 27488: get
  status = db->Get(read_options, "key5788", &value);
  if (status.ok()) {
    out << "GET key5788 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27489: delete
  db->Delete(write_options, "key4357");

  // op 27490: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4563", "key5655", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4563, key5655) size=" << scan_result.size() << std::endl;
  }

  // op 27491: put
  db->Put(write_options, "key0863", "value1633");

  // op 27492: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9838", "key11295", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9838, key11295) size=" << scan_result.size() << std::endl;
  }

  // op 27493: put
  db->Put(write_options, "key7601", "value93570");

  // op 27494: put
  db->Put(write_options, "key11354", "value27949");

  // op 27495: put
  db->Put(write_options, "key5034", "value48841");

  // op 27496: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2981", "key11116", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2981, key11116) size=" << scan_result.size() << std::endl;
  }

  // op 27497: put
  db->Put(write_options, "key1243", "value22503");

  // op 27498: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0188", "key2243", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0188, key2243) size=" << scan_result.size() << std::endl;
  }

  // op 27499: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6554", "key10280", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6554, key10280) size=" << scan_result.size() << std::endl;
  }

  // op 27500: put
  db->Put(write_options, "key11604", "value64555");

  // op 27501: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3745", "key3986", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3745, key3986) size=" << scan_result.size() << std::endl;
  }

  // op 27502: get
  status = db->Get(read_options, "key3392", &value);
  if (status.ok()) {
    out << "GET key3392 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27503: put
  db->Put(write_options, "key4276", "value97630");

  // op 27504: get
  status = db->Get(read_options, "key10007", &value);
  if (status.ok()) {
    out << "GET key10007 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27505: put
  db->Put(write_options, "key9499", "value98964");

  // op 27506: put
  db->Put(write_options, "key4195", "value77650");

  // op 27507: put
  db->Put(write_options, "key1157", "value56670");

  // op 27508: put
  db->Put(write_options, "key0909", "value99574");

  // op 27509: delete_range
  status = db->DeleteRange(write_options, "key2224", "key4779");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 27510: get
  status = db->Get(read_options, "key9878", &value);
  if (status.ok()) {
    out << "GET key9878 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27511: delete_range
  status = db->DeleteRange(write_options, "key6522", "key7389");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 27512: put
  db->Put(write_options, "key2812", "value59066");

  // op 27513: get
  status = db->Get(read_options, "key7596", &value);
  if (status.ok()) {
    out << "GET key7596 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27514: put
  db->Put(write_options, "key4875", "value73462");

  // op 27515: put
  db->Put(write_options, "key2868", "value0293");

  // op 27516: put
  db->Put(write_options, "key4559", "value94783");

  // op 27517: get
  status = db->Get(read_options, "key6828", &value);
  if (status.ok()) {
    out << "GET key6828 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27518: get
  status = db->Get(read_options, "key8853", &value);
  if (status.ok()) {
    out << "GET key8853 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27519: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0216", "key7453", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0216, key7453) size=" << scan_result.size() << std::endl;
  }

  // op 27520: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5243", "key9572", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5243, key9572) size=" << scan_result.size() << std::endl;
  }

  // op 27521: get
  status = db->Get(read_options, "key10797", &value);
  if (status.ok()) {
    out << "GET key10797 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27522: put
  db->Put(write_options, "key10004", "value32464");

  // op 27523: get
  status = db->Get(read_options, "key2077", &value);
  if (status.ok()) {
    out << "GET key2077 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27524: put
  db->Put(write_options, "key3374", "value36429");

  // op 27525: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 27526: delete_range
  status = db->DeleteRange(write_options, "key6940", "key7374");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 27527: get
  status = db->Get(read_options, "key10395", &value);
  if (status.ok()) {
    out << "GET key10395 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27528: get
  status = db->Get(read_options, "key4361", &value);
  if (status.ok()) {
    out << "GET key4361 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27529: put
  db->Put(write_options, "key2747", "value90011");

  // op 27530: put
  db->Put(write_options, "key1352", "value51941");

  // op 27531: delete_range
  status = db->DeleteRange(write_options, "key2405", "key9542");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 27532: put
  db->Put(write_options, "key8110", "value112420");

  // op 27533: put
  db->Put(write_options, "key0633", "value46965");

  // op 27534: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1302", "key6588", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1302, key6588) size=" << scan_result.size() << std::endl;
  }

  // op 27535: get
  status = db->Get(read_options, "key4265", &value);
  if (status.ok()) {
    out << "GET key4265 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27536: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0663", "key6822", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0663, key6822) size=" << scan_result.size() << std::endl;
  }

  // op 27537: put
  db->Put(write_options, "key4617", "value73891");

  // op 27538: put
  db->Put(write_options, "key11491", "value13806");

  // op 27539: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 27540: get
  status = db->Get(read_options, "key3448", &value);
  if (status.ok()) {
    out << "GET key3448 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27541: put
  db->Put(write_options, "key2636", "value78992");

  // op 27542: put
  db->Put(write_options, "key0073", "value74324");

  // op 27543: delete_range
  status = db->DeleteRange(write_options, "key0421", "key10664");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 27544: put
  db->Put(write_options, "key11423", "value22993");

  // op 27545: delete_range
  status = db->DeleteRange(write_options, "key9000", "key9422");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 27546: get
  status = db->Get(read_options, "key0855", &value);
  if (status.ok()) {
    out << "GET key0855 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27547: delete_range
  status = db->DeleteRange(write_options, "key2384", "key11536");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 27548: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7371", "key11655", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7371, key11655) size=" << scan_result.size() << std::endl;
  }

  // op 27549: get
  status = db->Get(read_options, "key5216", &value);
  if (status.ok()) {
    out << "GET key5216 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27550: delete
  db->Delete(write_options, "key8745");

  // op 27551: put
  db->Put(write_options, "key9414", "value68278");

  // op 27552: put
  db->Put(write_options, "key8091", "value106319");

  // op 27553: get
  status = db->Get(read_options, "key6429", &value);
  if (status.ok()) {
    out << "GET key6429 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27554: delete_range
  status = db->DeleteRange(write_options, "key0147", "key4190");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 27555: put
  db->Put(write_options, "key7513", "value63059");

  // op 27556: get
  status = db->Get(read_options, "key9439", &value);
  if (status.ok()) {
    out << "GET key9439 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27557: put
  db->Put(write_options, "key0283", "value85635");

  // op 27558: get
  status = db->Get(read_options, "key0188", &value);
  if (status.ok()) {
    out << "GET key0188 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27559: get
  status = db->Get(read_options, "key0839", &value);
  if (status.ok()) {
    out << "GET key0839 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27560: delete
  db->Delete(write_options, "key6445");

  // op 27561: put
  db->Put(write_options, "key9709", "value75851");

  // op 27562: get
  status = db->Get(read_options, "key8066", &value);
  if (status.ok()) {
    out << "GET key8066 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27563: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6352", "key7740", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6352, key7740) size=" << scan_result.size() << std::endl;
  }

  // op 27564: get
  status = db->Get(read_options, "key2343", &value);
  if (status.ok()) {
    out << "GET key2343 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27565: get
  status = db->Get(read_options, "key9895", &value);
  if (status.ok()) {
    out << "GET key9895 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27566: delete
  db->Delete(write_options, "key7995");

  // op 27567: get
  status = db->Get(read_options, "key3716", &value);
  if (status.ok()) {
    out << "GET key3716 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27568: put
  db->Put(write_options, "key10933", "value29410");

  // op 27569: get
  status = db->Get(read_options, "key7015", &value);
  if (status.ok()) {
    out << "GET key7015 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27570: put
  db->Put(write_options, "key4809", "value91934");

  // op 27571: get
  status = db->Get(read_options, "key2757", &value);
  if (status.ok()) {
    out << "GET key2757 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27572: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0489", "key8841", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0489, key8841) size=" << scan_result.size() << std::endl;
  }

  // op 27573: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3344", "key6731", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3344, key6731) size=" << scan_result.size() << std::endl;
  }

  // op 27574: put
  db->Put(write_options, "key1616", "value18271");

  // op 27575: put
  db->Put(write_options, "key1706", "value14302");

  // op 27576: put
  db->Put(write_options, "key2693", "value45487");

  // op 27577: delete_range
  status = db->DeleteRange(write_options, "key1507", "key4513");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 27578: put
  db->Put(write_options, "key7423", "value62471");

  // op 27579: put
  db->Put(write_options, "key10897", "value106334");

  // op 27580: put
  db->Put(write_options, "key7792", "value112007");

  // op 27581: put
  db->Put(write_options, "key7603", "value26226");

  // op 27582: put
  db->Put(write_options, "key11283", "value16488");

  // op 27583: put
  db->Put(write_options, "key10322", "value115942");

  // op 27584: put
  db->Put(write_options, "key5257", "value51614");

  // op 27585: put
  db->Put(write_options, "key0788", "value30295");

  // op 27586: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4082", "key9347", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4082, key9347) size=" << scan_result.size() << std::endl;
  }

  // op 27587: put
  db->Put(write_options, "key4594", "value33318");

  // op 27588: put
  db->Put(write_options, "key3152", "value40543");

  // op 27589: delete_range
  status = db->DeleteRange(write_options, "key10717", "key11295");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 27590: delete
  db->Delete(write_options, "key0703");

  // op 27591: delete_range
  status = db->DeleteRange(write_options, "key5431", "key7056");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 27592: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5075", "key11579", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5075, key11579) size=" << scan_result.size() << std::endl;
  }

  // op 27593: delete
  db->Delete(write_options, "key8686");

  // op 27594: put
  db->Put(write_options, "key5113", "value5282");

  // op 27595: delete
  db->Delete(write_options, "key7389");

  // op 27596: put
  db->Put(write_options, "key11521", "value3231");

  // op 27597: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 27598: put
  db->Put(write_options, "key1888", "value11367");

  // op 27599: get
  status = db->Get(read_options, "key9631", &value);
  if (status.ok()) {
    out << "GET key9631 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27600: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4180", "key6476", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4180, key6476) size=" << scan_result.size() << std::endl;
  }

  // op 27601: put
  db->Put(write_options, "key0536", "value1902");

  // op 27602: put
  db->Put(write_options, "key10222", "value103185");

  // op 27603: put
  db->Put(write_options, "key6875", "value89068");

  // op 27604: get
  status = db->Get(read_options, "key3882", &value);
  if (status.ok()) {
    out << "GET key3882 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27605: get
  status = db->Get(read_options, "key10034", &value);
  if (status.ok()) {
    out << "GET key10034 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27606: put
  db->Put(write_options, "key5957", "value73639");

  // op 27607: put
  db->Put(write_options, "key11640", "value92940");

  // op 27608: get
  status = db->Get(read_options, "key0570", &value);
  if (status.ok()) {
    out << "GET key0570 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27609: put
  db->Put(write_options, "key5878", "value92735");

  // op 27610: delete
  db->Delete(write_options, "key3123");

  // op 27611: put
  db->Put(write_options, "key8616", "value60600");

  // op 27612: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6356", "key7465", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6356, key7465) size=" << scan_result.size() << std::endl;
  }

  // op 27613: put
  db->Put(write_options, "key6351", "value83542");

  // op 27614: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7092", "key8606", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7092, key8606) size=" << scan_result.size() << std::endl;
  }

  // op 27615: put
  db->Put(write_options, "key9900", "value112448");

  // op 27616: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1979", "key8558", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1979, key8558) size=" << scan_result.size() << std::endl;
  }

  // op 27617: get
  status = db->Get(read_options, "key2905", &value);
  if (status.ok()) {
    out << "GET key2905 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27618: get
  status = db->Get(read_options, "key8377", &value);
  if (status.ok()) {
    out << "GET key8377 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27619: delete
  db->Delete(write_options, "key5852");

  // op 27620: put
  db->Put(write_options, "key3260", "value84191");

  // op 27621: put
  db->Put(write_options, "key2369", "value40786");

  // op 27622: delete_range
  status = db->DeleteRange(write_options, "key2021", "key11113");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 27623: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1597", "key9594", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1597, key9594) size=" << scan_result.size() << std::endl;
  }

  // op 27624: put
  db->Put(write_options, "key8004", "value14523");

  // op 27625: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7425", "key10710", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7425, key10710) size=" << scan_result.size() << std::endl;
  }

  // op 27626: put
  db->Put(write_options, "key0323", "value90569");

  // op 27627: get
  status = db->Get(read_options, "key7126", &value);
  if (status.ok()) {
    out << "GET key7126 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27628: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5274", "key10949", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5274, key10949) size=" << scan_result.size() << std::endl;
  }

  // op 27629: get
  status = db->Get(read_options, "key9967", &value);
  if (status.ok()) {
    out << "GET key9967 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27630: put
  db->Put(write_options, "key4343", "value100075");

  // op 27631: put
  db->Put(write_options, "key0780", "value68352");

  // op 27632: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2668", "key9144", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2668, key9144) size=" << scan_result.size() << std::endl;
  }

  // op 27633: delete_range
  status = db->DeleteRange(write_options, "key1167", "key11573");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 27634: get
  status = db->Get(read_options, "key6930", &value);
  if (status.ok()) {
    out << "GET key6930 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27635: put
  db->Put(write_options, "key6156", "value21659");

  // op 27636: put
  db->Put(write_options, "key7299", "value7481");

  // op 27637: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3796", "key4505", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3796, key4505) size=" << scan_result.size() << std::endl;
  }

  // op 27638: put
  db->Put(write_options, "key3020", "value31563");

  // op 27639: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 27640: put
  db->Put(write_options, "key2448", "value11801");

  // op 27641: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8179", "key11467", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8179, key11467) size=" << scan_result.size() << std::endl;
  }

  // op 27642: put
  db->Put(write_options, "key8165", "value42426");

  // op 27643: get
  status = db->Get(read_options, "key5193", &value);
  if (status.ok()) {
    out << "GET key5193 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27644: get
  status = db->Get(read_options, "key2334", &value);
  if (status.ok()) {
    out << "GET key2334 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27645: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8235", "key8375", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8235, key8375) size=" << scan_result.size() << std::endl;
  }

  // op 27646: get
  status = db->Get(read_options, "key4514", &value);
  if (status.ok()) {
    out << "GET key4514 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27647: get
  status = db->Get(read_options, "key11519", &value);
  if (status.ok()) {
    out << "GET key11519 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27648: get
  status = db->Get(read_options, "key6118", &value);
  if (status.ok()) {
    out << "GET key6118 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27649: put
  db->Put(write_options, "key3307", "value90617");

  // op 27650: put
  db->Put(write_options, "key8299", "value10022");

  // op 27651: get
  status = db->Get(read_options, "key0127", &value);
  if (status.ok()) {
    out << "GET key0127 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27652: put
  db->Put(write_options, "key0658", "value91896");

  // op 27653: put
  db->Put(write_options, "key2064", "value116959");

  // op 27654: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 27655: get
  status = db->Get(read_options, "key3827", &value);
  if (status.ok()) {
    out << "GET key3827 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27656: put
  db->Put(write_options, "key8230", "value43532");

  // op 27657: get
  status = db->Get(read_options, "key10230", &value);
  if (status.ok()) {
    out << "GET key10230 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27658: get
  status = db->Get(read_options, "key8721", &value);
  if (status.ok()) {
    out << "GET key8721 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27659: delete_range
  status = db->DeleteRange(write_options, "key1812", "key7081");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 27660: put
  db->Put(write_options, "key4507", "value30651");

  // op 27661: put
  db->Put(write_options, "key2238", "value30150");

  // op 27662: get
  status = db->Get(read_options, "key11718", &value);
  if (status.ok()) {
    out << "GET key11718 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27663: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5143", "key7524", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5143, key7524) size=" << scan_result.size() << std::endl;
  }

  // op 27664: get
  status = db->Get(read_options, "key5869", &value);
  if (status.ok()) {
    out << "GET key5869 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27665: get
  status = db->Get(read_options, "key9263", &value);
  if (status.ok()) {
    out << "GET key9263 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27666: get
  status = db->Get(read_options, "key0080", &value);
  if (status.ok()) {
    out << "GET key0080 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27667: delete
  db->Delete(write_options, "key10062");

  // op 27668: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0026", "key7362", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0026, key7362) size=" << scan_result.size() << std::endl;
  }

  // op 27669: put
  db->Put(write_options, "key9935", "value40934");

  // op 27670: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3744", "key8392", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3744, key8392) size=" << scan_result.size() << std::endl;
  }

  // op 27671: put
  db->Put(write_options, "key7134", "value99846");

  // op 27672: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8848", "key10930", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8848, key10930) size=" << scan_result.size() << std::endl;
  }

  // op 27673: get
  status = db->Get(read_options, "key1175", &value);
  if (status.ok()) {
    out << "GET key1175 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27674: put
  db->Put(write_options, "key3235", "value29236");

  // op 27675: delete
  db->Delete(write_options, "key1859");

  // op 27676: put
  db->Put(write_options, "key2441", "value62101");

  // op 27677: put
  db->Put(write_options, "key7468", "value54524");

  // op 27678: get
  status = db->Get(read_options, "key8815", &value);
  if (status.ok()) {
    out << "GET key8815 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27679: get
  status = db->Get(read_options, "key2045", &value);
  if (status.ok()) {
    out << "GET key2045 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27680: get
  status = db->Get(read_options, "key5356", &value);
  if (status.ok()) {
    out << "GET key5356 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27681: get
  status = db->Get(read_options, "key10275", &value);
  if (status.ok()) {
    out << "GET key10275 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27682: get
  status = db->Get(read_options, "key11266", &value);
  if (status.ok()) {
    out << "GET key11266 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27683: get
  status = db->Get(read_options, "key10371", &value);
  if (status.ok()) {
    out << "GET key10371 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27684: put
  db->Put(write_options, "key9950", "value14249");

  // op 27685: put
  db->Put(write_options, "key0508", "value101325");

  // op 27686: put
  db->Put(write_options, "key8804", "value33582");

  // op 27687: delete_range
  status = db->DeleteRange(write_options, "key4407", "key9953");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 27688: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1882", "key4338", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1882, key4338) size=" << scan_result.size() << std::endl;
  }

  // op 27689: delete
  db->Delete(write_options, "key6910");

  // op 27690: put
  db->Put(write_options, "key5725", "value25185");

  // op 27691: put
  db->Put(write_options, "key0516", "value27256");

  // op 27692: delete_range
  status = db->DeleteRange(write_options, "key2237", "key10202");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 27693: put
  db->Put(write_options, "key7651", "value103358");

  // op 27694: get
  status = db->Get(read_options, "key2550", &value);
  if (status.ok()) {
    out << "GET key2550 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27695: put
  db->Put(write_options, "key3388", "value34451");

  // op 27696: put
  db->Put(write_options, "key4977", "value48946");

  // op 27697: get
  status = db->Get(read_options, "key10832", &value);
  if (status.ok()) {
    out << "GET key10832 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27698: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 27699: put
  db->Put(write_options, "key4438", "value12246");

  // op 27700: get
  status = db->Get(read_options, "key8182", &value);
  if (status.ok()) {
    out << "GET key8182 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27701: delete
  db->Delete(write_options, "key1549");

  // op 27702: delete
  db->Delete(write_options, "key0712");

  // op 27703: delete_range
  status = db->DeleteRange(write_options, "key4389", "key6205");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 27704: get
  status = db->Get(read_options, "key4032", &value);
  if (status.ok()) {
    out << "GET key4032 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27705: delete
  db->Delete(write_options, "key5376");

  // op 27706: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 27707: put
  db->Put(write_options, "key9350", "value102874");

  // op 27708: put
  db->Put(write_options, "key8055", "value113920");

  // op 27709: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6192", "key8655", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6192, key8655) size=" << scan_result.size() << std::endl;
  }

  // op 27710: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10714", "key10877", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10714, key10877) size=" << scan_result.size() << std::endl;
  }

  // op 27711: delete
  db->Delete(write_options, "key8662");

  // op 27712: put
  db->Put(write_options, "key0266", "value60984");

  // op 27713: get
  status = db->Get(read_options, "key0250", &value);
  if (status.ok()) {
    out << "GET key0250 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27714: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 27715: delete
  db->Delete(write_options, "key5119");

  // op 27716: get
  status = db->Get(read_options, "key1429", &value);
  if (status.ok()) {
    out << "GET key1429 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27717: delete
  db->Delete(write_options, "key7545");

  // op 27718: put
  db->Put(write_options, "key8256", "value106644");

  // op 27719: put
  db->Put(write_options, "key10170", "value62392");

  // op 27720: delete_range
  status = db->DeleteRange(write_options, "key7527", "key11234");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 27721: get
  status = db->Get(read_options, "key6462", &value);
  if (status.ok()) {
    out << "GET key6462 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27722: put
  db->Put(write_options, "key3792", "value53966");

  // op 27723: put
  db->Put(write_options, "key11017", "value80622");

  // op 27724: put
  db->Put(write_options, "key9892", "value6104");

  // op 27725: delete_range
  status = db->DeleteRange(write_options, "key8938", "key9646");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 27726: put
  db->Put(write_options, "key8302", "value62645");

  // op 27727: put
  db->Put(write_options, "key5540", "value94437");

  // op 27728: put
  db->Put(write_options, "key10488", "value89298");

  // op 27729: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0368", "key3125", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0368, key3125) size=" << scan_result.size() << std::endl;
  }

  // op 27730: put
  db->Put(write_options, "key1855", "value57476");

  // op 27731: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 27732: get
  status = db->Get(read_options, "key8084", &value);
  if (status.ok()) {
    out << "GET key8084 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27733: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 27734: put
  db->Put(write_options, "key10403", "value51868");

  // op 27735: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8439", "key10441", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8439, key10441) size=" << scan_result.size() << std::endl;
  }

  // op 27736: put
  db->Put(write_options, "key3333", "value61621");

  // op 27737: get
  status = db->Get(read_options, "key4036", &value);
  if (status.ok()) {
    out << "GET key4036 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27738: put
  db->Put(write_options, "key5848", "value21597");

  // op 27739: get
  status = db->Get(read_options, "key1585", &value);
  if (status.ok()) {
    out << "GET key1585 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27740: delete_range
  status = db->DeleteRange(write_options, "key2012", "key10827");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 27741: put
  db->Put(write_options, "key11208", "value50095");

  // op 27742: put
  db->Put(write_options, "key11394", "value37738");

  // op 27743: get
  status = db->Get(read_options, "key7510", &value);
  if (status.ok()) {
    out << "GET key7510 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27744: put
  db->Put(write_options, "key1566", "value36168");

  // op 27745: put
  db->Put(write_options, "key4431", "value62232");

  // op 27746: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0081", "key1459", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0081, key1459) size=" << scan_result.size() << std::endl;
  }

  // op 27747: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7207", "key11559", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7207, key11559) size=" << scan_result.size() << std::endl;
  }

  // op 27748: delete_range
  status = db->DeleteRange(write_options, "key7602", "key7912");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 27749: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3005", "key9269", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3005, key9269) size=" << scan_result.size() << std::endl;
  }

  // op 27750: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1556", "key2831", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1556, key2831) size=" << scan_result.size() << std::endl;
  }

  // op 27751: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0577", "key4346", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0577, key4346) size=" << scan_result.size() << std::endl;
  }

  // op 27752: put
  db->Put(write_options, "key6904", "value31178");

  // op 27753: put
  db->Put(write_options, "key7549", "value65660");

  // op 27754: get
  status = db->Get(read_options, "key1609", &value);
  if (status.ok()) {
    out << "GET key1609 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27755: put
  db->Put(write_options, "key1093", "value28120");

  // op 27756: put
  db->Put(write_options, "key8644", "value56896");

  // op 27757: get
  status = db->Get(read_options, "key10262", &value);
  if (status.ok()) {
    out << "GET key10262 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27758: put
  db->Put(write_options, "key9355", "value105017");

  // op 27759: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 27760: put
  db->Put(write_options, "key11201", "value30704");

  // op 27761: put
  db->Put(write_options, "key0978", "value6133");

  // op 27762: get
  status = db->Get(read_options, "key8999", &value);
  if (status.ok()) {
    out << "GET key8999 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27763: put
  db->Put(write_options, "key7193", "value51181");

  // op 27764: get
  status = db->Get(read_options, "key5722", &value);
  if (status.ok()) {
    out << "GET key5722 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27765: get
  status = db->Get(read_options, "key1004", &value);
  if (status.ok()) {
    out << "GET key1004 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27766: delete_range
  status = db->DeleteRange(write_options, "key6571", "key8671");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 27767: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 27768: delete
  db->Delete(write_options, "key10473");

  // op 27769: get
  status = db->Get(read_options, "key8952", &value);
  if (status.ok()) {
    out << "GET key8952 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27770: delete_range
  status = db->DeleteRange(write_options, "key7444", "key7922");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 27771: get
  status = db->Get(read_options, "key7532", &value);
  if (status.ok()) {
    out << "GET key7532 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27772: get
  status = db->Get(read_options, "key8599", &value);
  if (status.ok()) {
    out << "GET key8599 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27773: put
  db->Put(write_options, "key2457", "value109058");

  // op 27774: delete
  db->Delete(write_options, "key7021");

  // op 27775: get
  status = db->Get(read_options, "key4014", &value);
  if (status.ok()) {
    out << "GET key4014 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27776: get
  status = db->Get(read_options, "key6326", &value);
  if (status.ok()) {
    out << "GET key6326 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27777: delete
  db->Delete(write_options, "key1278");

  // op 27778: put
  db->Put(write_options, "key5496", "value84609");

  // op 27779: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 27780: put
  db->Put(write_options, "key9455", "value23832");

  // op 27781: get
  status = db->Get(read_options, "key3210", &value);
  if (status.ok()) {
    out << "GET key3210 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27782: put
  db->Put(write_options, "key10855", "value62375");

  // op 27783: delete
  db->Delete(write_options, "key8013");

  // op 27784: get
  status = db->Get(read_options, "key6775", &value);
  if (status.ok()) {
    out << "GET key6775 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27785: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3544", "key9406", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3544, key9406) size=" << scan_result.size() << std::endl;
  }

  // op 27786: get
  status = db->Get(read_options, "key4319", &value);
  if (status.ok()) {
    out << "GET key4319 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27787: delete_range
  status = db->DeleteRange(write_options, "key5802", "key9324");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 27788: put
  db->Put(write_options, "key3362", "value95597");

  // op 27789: get
  status = db->Get(read_options, "key11236", &value);
  if (status.ok()) {
    out << "GET key11236 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27790: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 27791: put
  db->Put(write_options, "key4027", "value47706");

  // op 27792: delete_range
  status = db->DeleteRange(write_options, "key0817", "key8532");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 27793: get
  status = db->Get(read_options, "key10248", &value);
  if (status.ok()) {
    out << "GET key10248 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27794: put
  db->Put(write_options, "key5537", "value14425");

  // op 27795: get
  status = db->Get(read_options, "key9502", &value);
  if (status.ok()) {
    out << "GET key9502 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27796: get
  status = db->Get(read_options, "key10368", &value);
  if (status.ok()) {
    out << "GET key10368 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27797: put
  db->Put(write_options, "key9369", "value72306");

  // op 27798: get
  status = db->Get(read_options, "key3906", &value);
  if (status.ok()) {
    out << "GET key3906 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27799: get
  status = db->Get(read_options, "key5427", &value);
  if (status.ok()) {
    out << "GET key5427 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27800: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9364", "key9582", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9364, key9582) size=" << scan_result.size() << std::endl;
  }

  // op 27801: put
  db->Put(write_options, "key0146", "value90189");

  // op 27802: delete_range
  status = db->DeleteRange(write_options, "key0953", "key10261");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 27803: put
  db->Put(write_options, "key8004", "value86790");

  // op 27804: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4042", "key8005", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4042, key8005) size=" << scan_result.size() << std::endl;
  }

  // op 27805: delete
  db->Delete(write_options, "key0511");

  // op 27806: put
  db->Put(write_options, "key6953", "value9581");

  // op 27807: put
  db->Put(write_options, "key6938", "value89487");

  // op 27808: put
  db->Put(write_options, "key4357", "value84784");

  // op 27809: get
  status = db->Get(read_options, "key0838", &value);
  if (status.ok()) {
    out << "GET key0838 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27810: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4962", "key5410", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4962, key5410) size=" << scan_result.size() << std::endl;
  }

  // op 27811: put
  db->Put(write_options, "key3069", "value97328");

  // op 27812: put
  db->Put(write_options, "key1474", "value106863");

  // op 27813: delete
  db->Delete(write_options, "key7238");

  // op 27814: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 27815: get
  status = db->Get(read_options, "key6910", &value);
  if (status.ok()) {
    out << "GET key6910 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27816: put
  db->Put(write_options, "key9559", "value22302");

  // op 27817: put
  db->Put(write_options, "key5436", "value99692");

  // op 27818: put
  db->Put(write_options, "key7497", "value88986");

  // op 27819: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7333", "key9995", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7333, key9995) size=" << scan_result.size() << std::endl;
  }

  // op 27820: put
  db->Put(write_options, "key4257", "value66005");

  // op 27821: get
  status = db->Get(read_options, "key0081", &value);
  if (status.ok()) {
    out << "GET key0081 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27822: put
  db->Put(write_options, "key0148", "value73303");

  // op 27823: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2745", "key10149", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2745, key10149) size=" << scan_result.size() << std::endl;
  }

  // op 27824: get
  status = db->Get(read_options, "key5377", &value);
  if (status.ok()) {
    out << "GET key5377 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27825: put
  db->Put(write_options, "key0894", "value49044");

  // op 27826: put
  db->Put(write_options, "key5890", "value9642");

  // op 27827: get
  status = db->Get(read_options, "key10487", &value);
  if (status.ok()) {
    out << "GET key10487 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27828: delete
  db->Delete(write_options, "key7830");

  // op 27829: put
  db->Put(write_options, "key4721", "value9173");

  // op 27830: get
  status = db->Get(read_options, "key10118", &value);
  if (status.ok()) {
    out << "GET key10118 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27831: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1854", "key2880", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1854, key2880) size=" << scan_result.size() << std::endl;
  }

  // op 27832: put
  db->Put(write_options, "key0188", "value66075");

  // op 27833: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3622", "key8204", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3622, key8204) size=" << scan_result.size() << std::endl;
  }

  // op 27834: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4386", "key11408", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4386, key11408) size=" << scan_result.size() << std::endl;
  }

  // op 27835: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0136", "key7004", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0136, key7004) size=" << scan_result.size() << std::endl;
  }

  // op 27836: get
  status = db->Get(read_options, "key11521", &value);
  if (status.ok()) {
    out << "GET key11521 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27837: put
  db->Put(write_options, "key11515", "value10870");

  // op 27838: get
  status = db->Get(read_options, "key0978", &value);
  if (status.ok()) {
    out << "GET key0978 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27839: get
  status = db->Get(read_options, "key7292", &value);
  if (status.ok()) {
    out << "GET key7292 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27840: put
  db->Put(write_options, "key1284", "value92982");

  // op 27841: put
  db->Put(write_options, "key0710", "value62078");

  // op 27842: put
  db->Put(write_options, "key3928", "value41959");

  // op 27843: put
  db->Put(write_options, "key9063", "value21327");

  // op 27844: delete
  db->Delete(write_options, "key0865");

  // op 27845: put
  db->Put(write_options, "key1236", "value110350");

  // op 27846: put
  db->Put(write_options, "key1461", "value77442");

  // op 27847: get
  status = db->Get(read_options, "key0010", &value);
  if (status.ok()) {
    out << "GET key0010 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27848: put
  db->Put(write_options, "key6889", "value56562");

  // op 27849: get
  status = db->Get(read_options, "key5688", &value);
  if (status.ok()) {
    out << "GET key5688 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27850: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0978", "key10532", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0978, key10532) size=" << scan_result.size() << std::endl;
  }

  // op 27851: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10378", "key10446", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10378, key10446) size=" << scan_result.size() << std::endl;
  }

  // op 27852: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 27853: delete
  db->Delete(write_options, "key6762");

  // op 27854: put
  db->Put(write_options, "key7009", "value30024");

  // op 27855: put
  db->Put(write_options, "key6093", "value110472");

  // op 27856: delete_range
  status = db->DeleteRange(write_options, "key8433", "key11511");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 27857: put
  db->Put(write_options, "key2619", "value19958");

  // op 27858: delete_range
  status = db->DeleteRange(write_options, "key6231", "key8405");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 27859: put
  db->Put(write_options, "key5982", "value72818");

  // op 27860: put
  db->Put(write_options, "key11119", "value45246");

  // op 27861: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 27862: delete
  db->Delete(write_options, "key8772");

  // op 27863: put
  db->Put(write_options, "key5257", "value66264");

  // op 27864: put
  db->Put(write_options, "key0976", "value44753");

  // op 27865: put
  db->Put(write_options, "key10960", "value61707");

  // op 27866: delete
  db->Delete(write_options, "key1466");

  // op 27867: delete
  db->Delete(write_options, "key10111");

  // op 27868: put
  db->Put(write_options, "key7489", "value117001");

  // op 27869: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1805", "key2712", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1805, key2712) size=" << scan_result.size() << std::endl;
  }

  // op 27870: get
  status = db->Get(read_options, "key4183", &value);
  if (status.ok()) {
    out << "GET key4183 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27871: put
  db->Put(write_options, "key0019", "value74967");

  // op 27872: delete
  db->Delete(write_options, "key2772");

  // op 27873: delete_range
  status = db->DeleteRange(write_options, "key0835", "key9219");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 27874: put
  db->Put(write_options, "key9762", "value2793");

  // op 27875: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0175", "key10072", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0175, key10072) size=" << scan_result.size() << std::endl;
  }

  // op 27876: put
  db->Put(write_options, "key4816", "value19188");

  // op 27877: put
  db->Put(write_options, "key5307", "value107969");

  // op 27878: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4768", "key5517", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4768, key5517) size=" << scan_result.size() << std::endl;
  }

  // op 27879: put
  db->Put(write_options, "key0824", "value68829");

  // op 27880: get
  status = db->Get(read_options, "key0402", &value);
  if (status.ok()) {
    out << "GET key0402 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27881: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4291", "key8099", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4291, key8099) size=" << scan_result.size() << std::endl;
  }

  // op 27882: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3503", "key4039", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3503, key4039) size=" << scan_result.size() << std::endl;
  }

  // op 27883: put
  db->Put(write_options, "key9961", "value24150");

  // op 27884: get
  status = db->Get(read_options, "key0856", &value);
  if (status.ok()) {
    out << "GET key0856 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27885: get
  status = db->Get(read_options, "key2882", &value);
  if (status.ok()) {
    out << "GET key2882 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27886: put
  db->Put(write_options, "key8977", "value110887");

  // op 27887: put
  db->Put(write_options, "key4380", "value65445");

  // op 27888: put
  db->Put(write_options, "key1066", "value3751");

  // op 27889: put
  db->Put(write_options, "key8938", "value41179");

  // op 27890: delete
  db->Delete(write_options, "key0327");

  // op 27891: delete_range
  status = db->DeleteRange(write_options, "key7126", "key9337");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 27892: get
  status = db->Get(read_options, "key9181", &value);
  if (status.ok()) {
    out << "GET key9181 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27893: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0010", "key8771", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0010, key8771) size=" << scan_result.size() << std::endl;
  }

  // op 27894: get
  status = db->Get(read_options, "key9909", &value);
  if (status.ok()) {
    out << "GET key9909 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27895: get
  status = db->Get(read_options, "key4096", &value);
  if (status.ok()) {
    out << "GET key4096 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27896: get
  status = db->Get(read_options, "key9145", &value);
  if (status.ok()) {
    out << "GET key9145 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27897: get
  status = db->Get(read_options, "key10866", &value);
  if (status.ok()) {
    out << "GET key10866 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27898: put
  db->Put(write_options, "key3791", "value50023");

  // op 27899: put
  db->Put(write_options, "key9425", "value76333");

  // op 27900: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 27901: delete_range
  status = db->DeleteRange(write_options, "key9392", "key10019");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 27902: get
  status = db->Get(read_options, "key4193", &value);
  if (status.ok()) {
    out << "GET key4193 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27903: put
  db->Put(write_options, "key10378", "value78486");

  // op 27904: delete_range
  status = db->DeleteRange(write_options, "key3886", "key9121");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 27905: get
  status = db->Get(read_options, "key8395", &value);
  if (status.ok()) {
    out << "GET key8395 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27906: put
  db->Put(write_options, "key11112", "value1765");

  // op 27907: put
  db->Put(write_options, "key3550", "value53780");

  // op 27908: put
  db->Put(write_options, "key7051", "value28281");

  // op 27909: put
  db->Put(write_options, "key11118", "value109626");

  // op 27910: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3046", "key7540", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3046, key7540) size=" << scan_result.size() << std::endl;
  }

  // op 27911: put
  db->Put(write_options, "key7770", "value52621");

  // op 27912: put
  db->Put(write_options, "key4890", "value45109");

  // op 27913: put
  db->Put(write_options, "key5005", "value106303");

  // op 27914: put
  db->Put(write_options, "key1400", "value56421");

  // op 27915: delete
  db->Delete(write_options, "key1192");

  // op 27916: get
  status = db->Get(read_options, "key9750", &value);
  if (status.ok()) {
    out << "GET key9750 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27917: put
  db->Put(write_options, "key9415", "value70899");

  // op 27918: get
  status = db->Get(read_options, "key5519", &value);
  if (status.ok()) {
    out << "GET key5519 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27919: put
  db->Put(write_options, "key8718", "value89301");

  // op 27920: put
  db->Put(write_options, "key5616", "value50879");

  // op 27921: delete
  db->Delete(write_options, "key4896");

  // op 27922: get
  status = db->Get(read_options, "key3393", &value);
  if (status.ok()) {
    out << "GET key3393 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27923: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5103", "key5553", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5103, key5553) size=" << scan_result.size() << std::endl;
  }

  // op 27924: put
  db->Put(write_options, "key1964", "value80009");

  // op 27925: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 27926: delete
  db->Delete(write_options, "key7585");

  // op 27927: get
  status = db->Get(read_options, "key8238", &value);
  if (status.ok()) {
    out << "GET key8238 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27928: delete_range
  status = db->DeleteRange(write_options, "key2080", "key5127");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 27929: put
  db->Put(write_options, "key4549", "value83260");

  // op 27930: put
  db->Put(write_options, "key1330", "value50587");

  // op 27931: get
  status = db->Get(read_options, "key8523", &value);
  if (status.ok()) {
    out << "GET key8523 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27932: delete_range
  status = db->DeleteRange(write_options, "key0505", "key9368");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 27933: get
  status = db->Get(read_options, "key10738", &value);
  if (status.ok()) {
    out << "GET key10738 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27934: get
  status = db->Get(read_options, "key4711", &value);
  if (status.ok()) {
    out << "GET key4711 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27935: delete
  db->Delete(write_options, "key4916");

  // op 27936: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2400", "key5203", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2400, key5203) size=" << scan_result.size() << std::endl;
  }

  // op 27937: put
  db->Put(write_options, "key7746", "value39753");

  // op 27938: put
  db->Put(write_options, "key7106", "value40671");

  // op 27939: delete_range
  status = db->DeleteRange(write_options, "key1710", "key8988");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 27940: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5922", "key7376", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5922, key7376) size=" << scan_result.size() << std::endl;
  }

  // op 27941: put
  db->Put(write_options, "key4125", "value76811");

  // op 27942: put
  db->Put(write_options, "key10463", "value116938");

  // op 27943: delete_range
  status = db->DeleteRange(write_options, "key0061", "key9923");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 27944: put
  db->Put(write_options, "key9830", "value85515");

  // op 27945: delete_range
  status = db->DeleteRange(write_options, "key9549", "key10367");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 27946: put
  db->Put(write_options, "key2352", "value70525");

  // op 27947: put
  db->Put(write_options, "key7386", "value16370");

  // op 27948: get
  status = db->Get(read_options, "key2511", &value);
  if (status.ok()) {
    out << "GET key2511 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27949: get
  status = db->Get(read_options, "key1206", &value);
  if (status.ok()) {
    out << "GET key1206 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27950: put
  db->Put(write_options, "key0966", "value80774");

  // op 27951: get
  status = db->Get(read_options, "key11131", &value);
  if (status.ok()) {
    out << "GET key11131 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27952: delete
  db->Delete(write_options, "key7404");

  // op 27953: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2795", "key4892", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2795, key4892) size=" << scan_result.size() << std::endl;
  }

  // op 27954: put
  db->Put(write_options, "key5367", "value22936");

  // op 27955: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 27956: put
  db->Put(write_options, "key11726", "value22471");

  // op 27957: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2944", "key3408", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2944, key3408) size=" << scan_result.size() << std::endl;
  }

  // op 27958: get
  status = db->Get(read_options, "key2514", &value);
  if (status.ok()) {
    out << "GET key2514 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27959: delete_range
  status = db->DeleteRange(write_options, "key2395", "key2610");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 27960: delete
  db->Delete(write_options, "key3020");

  // op 27961: put
  db->Put(write_options, "key10785", "value21250");

  // op 27962: put
  db->Put(write_options, "key6488", "value84942");

  // op 27963: get
  status = db->Get(read_options, "key5298", &value);
  if (status.ok()) {
    out << "GET key5298 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27964: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 27965: get
  status = db->Get(read_options, "key4577", &value);
  if (status.ok()) {
    out << "GET key4577 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27966: get
  status = db->Get(read_options, "key1652", &value);
  if (status.ok()) {
    out << "GET key1652 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27967: put
  db->Put(write_options, "key4478", "value91293");

  // op 27968: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7678", "key9737", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7678, key9737) size=" << scan_result.size() << std::endl;
  }

  // op 27969: put
  db->Put(write_options, "key10127", "value61974");

  // op 27970: put
  db->Put(write_options, "key1046", "value114617");

  // op 27971: put
  db->Put(write_options, "key3030", "value61609");

  // op 27972: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7260", "key8541", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7260, key8541) size=" << scan_result.size() << std::endl;
  }

  // op 27973: delete
  db->Delete(write_options, "key5897");

  // op 27974: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0105", "key8236", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0105, key8236) size=" << scan_result.size() << std::endl;
  }

  // op 27975: put
  db->Put(write_options, "key6520", "value109102");

  // op 27976: delete
  db->Delete(write_options, "key7268");

  // op 27977: delete_range
  status = db->DeleteRange(write_options, "key1240", "key9083");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 27978: get
  status = db->Get(read_options, "key5666", &value);
  if (status.ok()) {
    out << "GET key5666 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27979: delete
  db->Delete(write_options, "key5631");

  // op 27980: put
  db->Put(write_options, "key5915", "value107121");

  // op 27981: delete
  db->Delete(write_options, "key8684");

  // op 27982: put
  db->Put(write_options, "key7129", "value58521");

  // op 27983: put
  db->Put(write_options, "key6768", "value53009");

  // op 27984: put
  db->Put(write_options, "key4979", "value34567");

  // op 27985: delete_range
  status = db->DeleteRange(write_options, "key2205", "key3460");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 27986: put
  db->Put(write_options, "key0744", "value51761");

  // op 27987: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2482", "key8439", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2482, key8439) size=" << scan_result.size() << std::endl;
  }

  // op 27988: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5461", "key7074", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5461, key7074) size=" << scan_result.size() << std::endl;
  }

  // op 27989: put
  db->Put(write_options, "key2593", "value72166");

  // op 27990: put
  db->Put(write_options, "key11276", "value31295");

  // op 27991: delete
  db->Delete(write_options, "key3639");

  // op 27992: delete_range
  status = db->DeleteRange(write_options, "key1489", "key3693");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 27993: get
  status = db->Get(read_options, "key0813", &value);
  if (status.ok()) {
    out << "GET key0813 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 27994: put
  db->Put(write_options, "key2044", "value63367");

  // op 27995: delete_range
  status = db->DeleteRange(write_options, "key4554", "key8953");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 27996: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7450", "key7552", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7450, key7552) size=" << scan_result.size() << std::endl;
  }

  // op 27997: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4073", "key11613", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4073, key11613) size=" << scan_result.size() << std::endl;
  }

  // op 27998: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8184", "key10120", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8184, key10120) size=" << scan_result.size() << std::endl;
  }

  // op 27999: put
  db->Put(write_options, "key10566", "value90223");

  // op 28000: put
  db->Put(write_options, "key4002", "value78228");

  // op 28001: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3057", "key9742", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3057, key9742) size=" << scan_result.size() << std::endl;
  }

  // op 28002: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0714", "key10334", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0714, key10334) size=" << scan_result.size() << std::endl;
  }

  // op 28003: put
  db->Put(write_options, "key5103", "value101712");

  // op 28004: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 28005: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3212", "key8578", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3212, key8578) size=" << scan_result.size() << std::endl;
  }

  // op 28006: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2143", "key7849", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2143, key7849) size=" << scan_result.size() << std::endl;
  }

  // op 28007: put
  db->Put(write_options, "key3491", "value78593");

  // op 28008: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1578", "key7955", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1578, key7955) size=" << scan_result.size() << std::endl;
  }

  // op 28009: put
  db->Put(write_options, "key2879", "value21856");

  // op 28010: delete
  db->Delete(write_options, "key10677");

  // op 28011: get
  status = db->Get(read_options, "key8095", &value);
  if (status.ok()) {
    out << "GET key8095 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28012: delete_range
  status = db->DeleteRange(write_options, "key2459", "key10378");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 28013: get
  status = db->Get(read_options, "key5040", &value);
  if (status.ok()) {
    out << "GET key5040 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28014: get
  status = db->Get(read_options, "key7262", &value);
  if (status.ok()) {
    out << "GET key7262 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28015: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0502", "key10857", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0502, key10857) size=" << scan_result.size() << std::endl;
  }

  // op 28016: delete
  db->Delete(write_options, "key11430");

  // op 28017: put
  db->Put(write_options, "key1555", "value106913");

  // op 28018: delete_range
  status = db->DeleteRange(write_options, "key1327", "key10552");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 28019: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 28020: delete
  db->Delete(write_options, "key6203");

  // op 28021: put
  db->Put(write_options, "key7515", "value24329");

  // op 28022: put
  db->Put(write_options, "key10994", "value23127");

  // op 28023: put
  db->Put(write_options, "key10795", "value39183");

  // op 28024: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 28025: delete
  db->Delete(write_options, "key0054");

  // op 28026: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10067", "key10664", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10067, key10664) size=" << scan_result.size() << std::endl;
  }

  // op 28027: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9840", "key10096", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9840, key10096) size=" << scan_result.size() << std::endl;
  }

  // op 28028: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2105", "key10933", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2105, key10933) size=" << scan_result.size() << std::endl;
  }

  // op 28029: get
  status = db->Get(read_options, "key4067", &value);
  if (status.ok()) {
    out << "GET key4067 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28030: put
  db->Put(write_options, "key2416", "value104178");

  // op 28031: put
  db->Put(write_options, "key9294", "value107718");

  // op 28032: put
  db->Put(write_options, "key8962", "value115821");

  // op 28033: put
  db->Put(write_options, "key2763", "value12803");

  // op 28034: get
  status = db->Get(read_options, "key1346", &value);
  if (status.ok()) {
    out << "GET key1346 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28035: delete_range
  status = db->DeleteRange(write_options, "key8123", "key10440");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 28036: put
  db->Put(write_options, "key2747", "value77761");

  // op 28037: put
  db->Put(write_options, "key7595", "value5033");

  // op 28038: put
  db->Put(write_options, "key1609", "value28852");

  // op 28039: get
  status = db->Get(read_options, "key4186", &value);
  if (status.ok()) {
    out << "GET key4186 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28040: delete_range
  status = db->DeleteRange(write_options, "key2651", "key8676");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 28041: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6661", "key11673", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6661, key11673) size=" << scan_result.size() << std::endl;
  }

  // op 28042: put
  db->Put(write_options, "key3223", "value66615");

  // op 28043: put
  db->Put(write_options, "key7368", "value2495");

  // op 28044: put
  db->Put(write_options, "key9630", "value91137");

  // op 28045: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6483", "key10027", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6483, key10027) size=" << scan_result.size() << std::endl;
  }

  // op 28046: delete
  db->Delete(write_options, "key9516");

  // op 28047: get
  status = db->Get(read_options, "key9879", &value);
  if (status.ok()) {
    out << "GET key9879 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28048: get
  status = db->Get(read_options, "key4936", &value);
  if (status.ok()) {
    out << "GET key4936 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28049: delete
  db->Delete(write_options, "key5233");

  // op 28050: delete_range
  status = db->DeleteRange(write_options, "key0142", "key1136");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 28051: get
  status = db->Get(read_options, "key0173", &value);
  if (status.ok()) {
    out << "GET key0173 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28052: delete
  db->Delete(write_options, "key9245");

  // op 28053: delete
  db->Delete(write_options, "key4717");

  // op 28054: put
  db->Put(write_options, "key7637", "value88742");

  // op 28055: delete
  db->Delete(write_options, "key1571");

  // op 28056: get
  status = db->Get(read_options, "key5105", &value);
  if (status.ok()) {
    out << "GET key5105 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28057: put
  db->Put(write_options, "key8750", "value114548");

  // op 28058: put
  db->Put(write_options, "key0277", "value67341");

  // op 28059: get
  status = db->Get(read_options, "key10450", &value);
  if (status.ok()) {
    out << "GET key10450 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28060: delete
  db->Delete(write_options, "key5941");

  // op 28061: put
  db->Put(write_options, "key6824", "value74735");

  // op 28062: put
  db->Put(write_options, "key8848", "value16843");

  // op 28063: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7595", "key8611", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7595, key8611) size=" << scan_result.size() << std::endl;
  }

  // op 28064: put
  db->Put(write_options, "key11173", "value70096");

  // op 28065: put
  db->Put(write_options, "key7974", "value41597");

  // op 28066: put
  db->Put(write_options, "key10732", "value11624");

  // op 28067: delete_range
  status = db->DeleteRange(write_options, "key1150", "key3923");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 28068: delete
  db->Delete(write_options, "key5037");

  // op 28069: delete
  db->Delete(write_options, "key2268");

  // op 28070: put
  db->Put(write_options, "key8996", "value11368");

  // op 28071: put
  db->Put(write_options, "key5450", "value86134");

  // op 28072: put
  db->Put(write_options, "key5157", "value41523");

  // op 28073: get
  status = db->Get(read_options, "key7065", &value);
  if (status.ok()) {
    out << "GET key7065 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28074: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 28075: get
  status = db->Get(read_options, "key1604", &value);
  if (status.ok()) {
    out << "GET key1604 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28076: delete
  db->Delete(write_options, "key11382");

  // op 28077: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5758", "key11331", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5758, key11331) size=" << scan_result.size() << std::endl;
  }

  // op 28078: delete_range
  status = db->DeleteRange(write_options, "key0143", "key9365");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 28079: put
  db->Put(write_options, "key3169", "value7585");

  // op 28080: put
  db->Put(write_options, "key3945", "value39313");

  // op 28081: put
  db->Put(write_options, "key4668", "value34704");

  // op 28082: put
  db->Put(write_options, "key9317", "value101236");

  // op 28083: get
  status = db->Get(read_options, "key0656", &value);
  if (status.ok()) {
    out << "GET key0656 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28084: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 28085: get
  status = db->Get(read_options, "key1187", &value);
  if (status.ok()) {
    out << "GET key1187 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28086: put
  db->Put(write_options, "key2199", "value64623");

  // op 28087: put
  db->Put(write_options, "key5627", "value110915");

  // op 28088: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0140", "key9662", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0140, key9662) size=" << scan_result.size() << std::endl;
  }

  // op 28089: put
  db->Put(write_options, "key9865", "value10256");

  // op 28090: put
  db->Put(write_options, "key5863", "value34014");

  // op 28091: get
  status = db->Get(read_options, "key0154", &value);
  if (status.ok()) {
    out << "GET key0154 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28092: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2127", "key6222", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2127, key6222) size=" << scan_result.size() << std::endl;
  }

  // op 28093: get
  status = db->Get(read_options, "key6838", &value);
  if (status.ok()) {
    out << "GET key6838 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28094: delete_range
  status = db->DeleteRange(write_options, "key3150", "key6078");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 28095: delete_range
  status = db->DeleteRange(write_options, "key4010", "key9311");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 28096: get
  status = db->Get(read_options, "key3400", &value);
  if (status.ok()) {
    out << "GET key3400 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28097: get
  status = db->Get(read_options, "key10734", &value);
  if (status.ok()) {
    out << "GET key10734 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28098: get
  status = db->Get(read_options, "key10586", &value);
  if (status.ok()) {
    out << "GET key10586 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28099: put
  db->Put(write_options, "key1931", "value11133");

  // op 28100: put
  db->Put(write_options, "key7763", "value43725");

  // op 28101: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 28102: delete
  db->Delete(write_options, "key6836");

  // op 28103: get
  status = db->Get(read_options, "key8664", &value);
  if (status.ok()) {
    out << "GET key8664 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28104: delete_range
  status = db->DeleteRange(write_options, "key3487", "key10281");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 28105: get
  status = db->Get(read_options, "key8596", &value);
  if (status.ok()) {
    out << "GET key8596 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28106: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 28107: put
  db->Put(write_options, "key9113", "value53322");

  // op 28108: delete
  db->Delete(write_options, "key11520");

  // op 28109: delete_range
  status = db->DeleteRange(write_options, "key0609", "key3664");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 28110: delete_range
  status = db->DeleteRange(write_options, "key3682", "key6721");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 28111: put
  db->Put(write_options, "key9074", "value76534");

  // op 28112: get
  status = db->Get(read_options, "key1915", &value);
  if (status.ok()) {
    out << "GET key1915 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28113: delete_range
  status = db->DeleteRange(write_options, "key6355", "key7406");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 28114: delete_range
  status = db->DeleteRange(write_options, "key1523", "key9037");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 28115: get
  status = db->Get(read_options, "key2607", &value);
  if (status.ok()) {
    out << "GET key2607 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28116: put
  db->Put(write_options, "key10228", "value31977");

  // op 28117: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0287", "key9497", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0287, key9497) size=" << scan_result.size() << std::endl;
  }

  // op 28118: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1443", "key9538", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1443, key9538) size=" << scan_result.size() << std::endl;
  }

  // op 28119: put
  db->Put(write_options, "key4456", "value62647");

  // op 28120: delete
  db->Delete(write_options, "key4989");

  // op 28121: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3560", "key5728", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3560, key5728) size=" << scan_result.size() << std::endl;
  }

  // op 28122: put
  db->Put(write_options, "key11181", "value63989");

  // op 28123: put
  db->Put(write_options, "key2717", "value62242");

  // op 28124: put
  db->Put(write_options, "key4867", "value52836");

  // op 28125: get
  status = db->Get(read_options, "key6813", &value);
  if (status.ok()) {
    out << "GET key6813 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28126: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10396", "key11481", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10396, key11481) size=" << scan_result.size() << std::endl;
  }

  // op 28127: delete
  db->Delete(write_options, "key3090");

  // op 28128: put
  db->Put(write_options, "key2326", "value107507");

  // op 28129: get
  status = db->Get(read_options, "key9955", &value);
  if (status.ok()) {
    out << "GET key9955 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28130: put
  db->Put(write_options, "key1293", "value76364");

  // op 28131: put
  db->Put(write_options, "key0398", "value23347");

  // op 28132: get
  status = db->Get(read_options, "key4487", &value);
  if (status.ok()) {
    out << "GET key4487 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28133: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4215", "key6136", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4215, key6136) size=" << scan_result.size() << std::endl;
  }

  // op 28134: put
  db->Put(write_options, "key4993", "value57376");

  // op 28135: get
  status = db->Get(read_options, "key10083", &value);
  if (status.ok()) {
    out << "GET key10083 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28136: put
  db->Put(write_options, "key11562", "value34488");

  // op 28137: put
  db->Put(write_options, "key2020", "value29587");

  // op 28138: put
  db->Put(write_options, "key0450", "value54552");

  // op 28139: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 28140: get
  status = db->Get(read_options, "key9271", &value);
  if (status.ok()) {
    out << "GET key9271 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28141: put
  db->Put(write_options, "key11002", "value70925");

  // op 28142: put
  db->Put(write_options, "key10156", "value3078");

  // op 28143: put
  db->Put(write_options, "key10739", "value90760");

  // op 28144: delete_range
  status = db->DeleteRange(write_options, "key3790", "key6773");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 28145: put
  db->Put(write_options, "key6558", "value35772");

  // op 28146: get
  status = db->Get(read_options, "key5580", &value);
  if (status.ok()) {
    out << "GET key5580 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28147: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 28148: delete_range
  status = db->DeleteRange(write_options, "key3375", "key11146");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 28149: put
  db->Put(write_options, "key2130", "value77852");

  // op 28150: put
  db->Put(write_options, "key9675", "value43739");

  // op 28151: get
  status = db->Get(read_options, "key8435", &value);
  if (status.ok()) {
    out << "GET key8435 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28152: put
  db->Put(write_options, "key9537", "value109276");

  // op 28153: put
  db->Put(write_options, "key1438", "value5303");

  // op 28154: delete_range
  status = db->DeleteRange(write_options, "key0291", "key9597");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 28155: put
  db->Put(write_options, "key4134", "value5256");

  // op 28156: put
  db->Put(write_options, "key0127", "value51105");

  // op 28157: delete
  db->Delete(write_options, "key8688");

  // op 28158: put
  db->Put(write_options, "key8396", "value81126");

  // op 28159: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9521", "key10614", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9521, key10614) size=" << scan_result.size() << std::endl;
  }

  // op 28160: get
  status = db->Get(read_options, "key1421", &value);
  if (status.ok()) {
    out << "GET key1421 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28161: delete_range
  status = db->DeleteRange(write_options, "key9450", "key10894");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 28162: put
  db->Put(write_options, "key3947", "value65819");

  // op 28163: put
  db->Put(write_options, "key1621", "value35530");

  // op 28164: put
  db->Put(write_options, "key6548", "value60232");

  // op 28165: delete_range
  status = db->DeleteRange(write_options, "key2721", "key5020");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 28166: put
  db->Put(write_options, "key6228", "value41757");

  // op 28167: get
  status = db->Get(read_options, "key3393", &value);
  if (status.ok()) {
    out << "GET key3393 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28168: delete_range
  status = db->DeleteRange(write_options, "key2287", "key10734");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 28169: delete_range
  status = db->DeleteRange(write_options, "key0000", "key1907");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 28170: put
  db->Put(write_options, "key5769", "value38569");

  // op 28171: put
  db->Put(write_options, "key3756", "value61183");

  // op 28172: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2410", "key9447", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2410, key9447) size=" << scan_result.size() << std::endl;
  }

  // op 28173: put
  db->Put(write_options, "key7411", "value56096");

  // op 28174: put
  db->Put(write_options, "key7220", "value1420");

  // op 28175: get
  status = db->Get(read_options, "key1402", &value);
  if (status.ok()) {
    out << "GET key1402 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28176: get
  status = db->Get(read_options, "key0202", &value);
  if (status.ok()) {
    out << "GET key0202 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28177: get
  status = db->Get(read_options, "key4748", &value);
  if (status.ok()) {
    out << "GET key4748 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28178: put
  db->Put(write_options, "key3965", "value111460");

  // op 28179: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4419", "key11626", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4419, key11626) size=" << scan_result.size() << std::endl;
  }

  // op 28180: delete_range
  status = db->DeleteRange(write_options, "key4223", "key6199");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 28181: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4149", "key7873", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4149, key7873) size=" << scan_result.size() << std::endl;
  }

  // op 28182: get
  status = db->Get(read_options, "key6508", &value);
  if (status.ok()) {
    out << "GET key6508 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28183: put
  db->Put(write_options, "key5027", "value49389");

  // op 28184: get
  status = db->Get(read_options, "key1612", &value);
  if (status.ok()) {
    out << "GET key1612 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28185: delete_range
  status = db->DeleteRange(write_options, "key6023", "key7411");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 28186: put
  db->Put(write_options, "key7010", "value33378");

  // op 28187: put
  db->Put(write_options, "key1033", "value81794");

  // op 28188: put
  db->Put(write_options, "key10069", "value5833");

  // op 28189: get
  status = db->Get(read_options, "key6215", &value);
  if (status.ok()) {
    out << "GET key6215 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28190: delete_range
  status = db->DeleteRange(write_options, "key1677", "key10166");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 28191: put
  db->Put(write_options, "key0524", "value112910");

  // op 28192: delete_range
  status = db->DeleteRange(write_options, "key0449", "key9794");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 28193: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 28194: put
  db->Put(write_options, "key6225", "value62972");

  // op 28195: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8822", "key10094", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8822, key10094) size=" << scan_result.size() << std::endl;
  }

  // op 28196: put
  db->Put(write_options, "key1775", "value38378");

  // op 28197: delete
  db->Delete(write_options, "key3082");

  // op 28198: put
  db->Put(write_options, "key11264", "value4174");

  // op 28199: get
  status = db->Get(read_options, "key9331", &value);
  if (status.ok()) {
    out << "GET key9331 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28200: put
  db->Put(write_options, "key8034", "value76864");

  // op 28201: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4505", "key8380", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4505, key8380) size=" << scan_result.size() << std::endl;
  }

  // op 28202: get
  status = db->Get(read_options, "key8010", &value);
  if (status.ok()) {
    out << "GET key8010 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28203: put
  db->Put(write_options, "key10903", "value39308");

  // op 28204: put
  db->Put(write_options, "key4827", "value37683");

  // op 28205: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8256", "key9237", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8256, key9237) size=" << scan_result.size() << std::endl;
  }

  // op 28206: put
  db->Put(write_options, "key7293", "value84223");

  // op 28207: get
  status = db->Get(read_options, "key5934", &value);
  if (status.ok()) {
    out << "GET key5934 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28208: get
  status = db->Get(read_options, "key10098", &value);
  if (status.ok()) {
    out << "GET key10098 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28209: put
  db->Put(write_options, "key4774", "value115562");

  // op 28210: delete
  db->Delete(write_options, "key1817");

  // op 28211: get
  status = db->Get(read_options, "key2483", &value);
  if (status.ok()) {
    out << "GET key2483 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28212: put
  db->Put(write_options, "key7907", "value102629");

  // op 28213: put
  db->Put(write_options, "key10631", "value38834");

  // op 28214: delete_range
  status = db->DeleteRange(write_options, "key4878", "key6529");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 28215: put
  db->Put(write_options, "key11001", "value21039");

  // op 28216: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3780", "key9367", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3780, key9367) size=" << scan_result.size() << std::endl;
  }

  // op 28217: put
  db->Put(write_options, "key4193", "value83624");

  // op 28218: put
  db->Put(write_options, "key8876", "value50517");

  // op 28219: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2788", "key7349", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2788, key7349) size=" << scan_result.size() << std::endl;
  }

  // op 28220: put
  db->Put(write_options, "key10714", "value110737");

  // op 28221: put
  db->Put(write_options, "key6004", "value14483");

  // op 28222: delete_range
  status = db->DeleteRange(write_options, "key2680", "key10772");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 28223: delete_range
  status = db->DeleteRange(write_options, "key6194", "key7172");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 28224: put
  db->Put(write_options, "key10404", "value74247");

  // op 28225: put
  db->Put(write_options, "key8218", "value96878");

  // op 28226: delete
  db->Delete(write_options, "key2733");

  // op 28227: put
  db->Put(write_options, "key6097", "value102880");

  // op 28228: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7143", "key9193", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7143, key9193) size=" << scan_result.size() << std::endl;
  }

  // op 28229: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4813", "key7334", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4813, key7334) size=" << scan_result.size() << std::endl;
  }

  // op 28230: delete
  db->Delete(write_options, "key6979");

  // op 28231: put
  db->Put(write_options, "key10145", "value58700");

  // op 28232: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6184", "key10476", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6184, key10476) size=" << scan_result.size() << std::endl;
  }

  // op 28233: delete
  db->Delete(write_options, "key7077");

  // op 28234: get
  status = db->Get(read_options, "key1628", &value);
  if (status.ok()) {
    out << "GET key1628 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28235: delete
  db->Delete(write_options, "key5563");

  // op 28236: put
  db->Put(write_options, "key4651", "value31808");

  // op 28237: put
  db->Put(write_options, "key0874", "value104294");

  // op 28238: put
  db->Put(write_options, "key8670", "value67319");

  // op 28239: put
  db->Put(write_options, "key8604", "value27412");

  // op 28240: put
  db->Put(write_options, "key0527", "value79148");

  // op 28241: delete_range
  status = db->DeleteRange(write_options, "key7557", "key10232");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 28242: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 28243: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2168", "key5470", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2168, key5470) size=" << scan_result.size() << std::endl;
  }

  // op 28244: delete
  db->Delete(write_options, "key5728");

  // op 28245: put
  db->Put(write_options, "key9630", "value50625");

  // op 28246: get
  status = db->Get(read_options, "key10356", &value);
  if (status.ok()) {
    out << "GET key10356 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28247: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1039", "key7116", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1039, key7116) size=" << scan_result.size() << std::endl;
  }

  // op 28248: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4078", "key10524", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4078, key10524) size=" << scan_result.size() << std::endl;
  }

  // op 28249: delete_range
  status = db->DeleteRange(write_options, "key7642", "key10015");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 28250: delete
  db->Delete(write_options, "key2436");

  // op 28251: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0677", "key10408", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0677, key10408) size=" << scan_result.size() << std::endl;
  }

  // op 28252: put
  db->Put(write_options, "key4954", "value40807");

  // op 28253: put
  db->Put(write_options, "key7436", "value31191");

  // op 28254: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1548", "key4192", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1548, key4192) size=" << scan_result.size() << std::endl;
  }

  // op 28255: delete
  db->Delete(write_options, "key4909");

  // op 28256: delete
  db->Delete(write_options, "key6481");

  // op 28257: get
  status = db->Get(read_options, "key1612", &value);
  if (status.ok()) {
    out << "GET key1612 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28258: delete
  db->Delete(write_options, "key8087");

  // op 28259: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4262", "key5921", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4262, key5921) size=" << scan_result.size() << std::endl;
  }

  // op 28260: put
  db->Put(write_options, "key10563", "value35854");

  // op 28261: put
  db->Put(write_options, "key2327", "value35283");

  // op 28262: put
  db->Put(write_options, "key11434", "value34657");

  // op 28263: put
  db->Put(write_options, "key0773", "value24026");

  // op 28264: delete_range
  status = db->DeleteRange(write_options, "key4108", "key4778");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 28265: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9527", "key11684", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9527, key11684) size=" << scan_result.size() << std::endl;
  }

  // op 28266: delete
  db->Delete(write_options, "key3791");

  // op 28267: put
  db->Put(write_options, "key4376", "value27681");

  // op 28268: put
  db->Put(write_options, "key5889", "value90768");

  // op 28269: put
  db->Put(write_options, "key6509", "value111806");

  // op 28270: put
  db->Put(write_options, "key1531", "value10331");

  // op 28271: delete
  db->Delete(write_options, "key0101");

  // op 28272: put
  db->Put(write_options, "key1050", "value23703");

  // op 28273: delete
  db->Delete(write_options, "key6250");

  // op 28274: get
  status = db->Get(read_options, "key11563", &value);
  if (status.ok()) {
    out << "GET key11563 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28275: put
  db->Put(write_options, "key6507", "value89485");

  // op 28276: get
  status = db->Get(read_options, "key10554", &value);
  if (status.ok()) {
    out << "GET key10554 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28277: delete_range
  status = db->DeleteRange(write_options, "key0148", "key6264");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 28278: put
  db->Put(write_options, "key10970", "value3757");

  // op 28279: put
  db->Put(write_options, "key1266", "value107687");

  // op 28280: put
  db->Put(write_options, "key3502", "value113970");

  // op 28281: delete
  db->Delete(write_options, "key8189");

  // op 28282: get
  status = db->Get(read_options, "key2633", &value);
  if (status.ok()) {
    out << "GET key2633 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28283: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0490", "key5512", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0490, key5512) size=" << scan_result.size() << std::endl;
  }

  // op 28284: put
  db->Put(write_options, "key5587", "value83896");

  // op 28285: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0278", "key2118", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0278, key2118) size=" << scan_result.size() << std::endl;
  }

  // op 28286: put
  db->Put(write_options, "key8141", "value96924");

  // op 28287: put
  db->Put(write_options, "key10617", "value97949");

  // op 28288: get
  status = db->Get(read_options, "key0687", &value);
  if (status.ok()) {
    out << "GET key0687 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28289: put
  db->Put(write_options, "key0271", "value81687");

  // op 28290: put
  db->Put(write_options, "key3557", "value63098");

  // op 28291: put
  db->Put(write_options, "key8322", "value94518");

  // op 28292: delete
  db->Delete(write_options, "key7144");

  // op 28293: put
  db->Put(write_options, "key2532", "value20349");

  // op 28294: delete
  db->Delete(write_options, "key2022");

  // op 28295: put
  db->Put(write_options, "key9102", "value111694");

  // op 28296: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 28297: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0354", "key3629", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0354, key3629) size=" << scan_result.size() << std::endl;
  }

  // op 28298: put
  db->Put(write_options, "key7408", "value37167");

  // op 28299: put
  db->Put(write_options, "key11277", "value109297");

  // op 28300: get
  status = db->Get(read_options, "key0315", &value);
  if (status.ok()) {
    out << "GET key0315 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28301: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5976", "key9075", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5976, key9075) size=" << scan_result.size() << std::endl;
  }

  // op 28302: put
  db->Put(write_options, "key6790", "value102917");

  // op 28303: put
  db->Put(write_options, "key6763", "value18294");

  // op 28304: get
  status = db->Get(read_options, "key3726", &value);
  if (status.ok()) {
    out << "GET key3726 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28305: put
  db->Put(write_options, "key5067", "value66244");

  // op 28306: put
  db->Put(write_options, "key10199", "value13768");

  // op 28307: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6823", "key10971", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6823, key10971) size=" << scan_result.size() << std::endl;
  }

  // op 28308: put
  db->Put(write_options, "key5361", "value113026");

  // op 28309: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 28310: get
  status = db->Get(read_options, "key5595", &value);
  if (status.ok()) {
    out << "GET key5595 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28311: get
  status = db->Get(read_options, "key9411", &value);
  if (status.ok()) {
    out << "GET key9411 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28312: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 28313: put
  db->Put(write_options, "key0930", "value56491");

  // op 28314: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6081", "key9046", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6081, key9046) size=" << scan_result.size() << std::endl;
  }

  // op 28315: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 28316: put
  db->Put(write_options, "key1314", "value32719");

  // op 28317: put
  db->Put(write_options, "key9832", "value112113");

  // op 28318: put
  db->Put(write_options, "key7029", "value0679");

  // op 28319: put
  db->Put(write_options, "key6917", "value105368");

  // op 28320: get
  status = db->Get(read_options, "key3038", &value);
  if (status.ok()) {
    out << "GET key3038 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28321: put
  db->Put(write_options, "key6244", "value34265");

  // op 28322: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0057", "key0692", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0057, key0692) size=" << scan_result.size() << std::endl;
  }

  // op 28323: put
  db->Put(write_options, "key9218", "value16442");

  // op 28324: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3008", "key9510", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3008, key9510) size=" << scan_result.size() << std::endl;
  }

  // op 28325: put
  db->Put(write_options, "key2249", "value58194");

  // op 28326: delete
  db->Delete(write_options, "key2047");

  // op 28327: put
  db->Put(write_options, "key9108", "value69388");

  // op 28328: get
  status = db->Get(read_options, "key9515", &value);
  if (status.ok()) {
    out << "GET key9515 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28329: get
  status = db->Get(read_options, "key9197", &value);
  if (status.ok()) {
    out << "GET key9197 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28330: put
  db->Put(write_options, "key6775", "value24836");

  // op 28331: put
  db->Put(write_options, "key1162", "value63277");

  // op 28332: delete
  db->Delete(write_options, "key4989");

  // op 28333: put
  db->Put(write_options, "key11073", "value26106");

  // op 28334: put
  db->Put(write_options, "key9769", "value99750");

  // op 28335: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 28336: get
  status = db->Get(read_options, "key9603", &value);
  if (status.ok()) {
    out << "GET key9603 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28337: get
  status = db->Get(read_options, "key6822", &value);
  if (status.ok()) {
    out << "GET key6822 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28338: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0134", "key2991", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0134, key2991) size=" << scan_result.size() << std::endl;
  }

  // op 28339: put
  db->Put(write_options, "key0044", "value82170");

  // op 28340: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 28341: get
  status = db->Get(read_options, "key5725", &value);
  if (status.ok()) {
    out << "GET key5725 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28342: put
  db->Put(write_options, "key2985", "value62267");

  // op 28343: delete_range
  status = db->DeleteRange(write_options, "key0178", "key3582");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 28344: delete_range
  status = db->DeleteRange(write_options, "key1116", "key7043");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 28345: put
  db->Put(write_options, "key8264", "value15515");

  // op 28346: put
  db->Put(write_options, "key3283", "value95311");

  // op 28347: get
  status = db->Get(read_options, "key6638", &value);
  if (status.ok()) {
    out << "GET key6638 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28348: put
  db->Put(write_options, "key4724", "value93693");

  // op 28349: put
  db->Put(write_options, "key1307", "value42656");

  // op 28350: get
  status = db->Get(read_options, "key6954", &value);
  if (status.ok()) {
    out << "GET key6954 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28351: put
  db->Put(write_options, "key8012", "value86059");

  // op 28352: put
  db->Put(write_options, "key9708", "value67597");

  // op 28353: put
  db->Put(write_options, "key6683", "value93385");

  // op 28354: get
  status = db->Get(read_options, "key9385", &value);
  if (status.ok()) {
    out << "GET key9385 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28355: put
  db->Put(write_options, "key0576", "value56539");

  // op 28356: put
  db->Put(write_options, "key10108", "value45131");

  // op 28357: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 28358: get
  status = db->Get(read_options, "key11322", &value);
  if (status.ok()) {
    out << "GET key11322 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28359: put
  db->Put(write_options, "key7449", "value106176");

  // op 28360: put
  db->Put(write_options, "key5873", "value10520");

  // op 28361: get
  status = db->Get(read_options, "key3418", &value);
  if (status.ok()) {
    out << "GET key3418 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28362: put
  db->Put(write_options, "key9400", "value113758");

  // op 28363: delete_range
  status = db->DeleteRange(write_options, "key6969", "key10816");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 28364: get
  status = db->Get(read_options, "key4997", &value);
  if (status.ok()) {
    out << "GET key4997 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28365: get
  status = db->Get(read_options, "key3273", &value);
  if (status.ok()) {
    out << "GET key3273 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28366: put
  db->Put(write_options, "key8631", "value31882");

  // op 28367: delete_range
  status = db->DeleteRange(write_options, "key4803", "key7421");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 28368: get
  status = db->Get(read_options, "key10007", &value);
  if (status.ok()) {
    out << "GET key10007 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28369: delete_range
  status = db->DeleteRange(write_options, "key7872", "key8672");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 28370: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4064", "key11224", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4064, key11224) size=" << scan_result.size() << std::endl;
  }

  // op 28371: delete
  db->Delete(write_options, "key8285");

  // op 28372: put
  db->Put(write_options, "key6913", "value86413");

  // op 28373: delete_range
  status = db->DeleteRange(write_options, "key1900", "key10941");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 28374: put
  db->Put(write_options, "key11664", "value50396");

  // op 28375: delete_range
  status = db->DeleteRange(write_options, "key4882", "key7596");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 28376: get
  status = db->Get(read_options, "key8562", &value);
  if (status.ok()) {
    out << "GET key8562 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28377: put
  db->Put(write_options, "key3798", "value115284");

  // op 28378: delete
  db->Delete(write_options, "key10854");

  // op 28379: put
  db->Put(write_options, "key4355", "value20750");

  // op 28380: put
  db->Put(write_options, "key0884", "value100356");

  // op 28381: put
  db->Put(write_options, "key7339", "value70413");

  // op 28382: put
  db->Put(write_options, "key11132", "value24680");

  // op 28383: delete
  db->Delete(write_options, "key5634");

  // op 28384: delete_range
  status = db->DeleteRange(write_options, "key1887", "key11716");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 28385: get
  status = db->Get(read_options, "key0513", &value);
  if (status.ok()) {
    out << "GET key0513 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28386: delete
  db->Delete(write_options, "key2181");

  // op 28387: delete
  db->Delete(write_options, "key9201");

  // op 28388: get
  status = db->Get(read_options, "key2778", &value);
  if (status.ok()) {
    out << "GET key2778 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28389: put
  db->Put(write_options, "key10599", "value84337");

  // op 28390: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9469", "key11205", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9469, key11205) size=" << scan_result.size() << std::endl;
  }

  // op 28391: delete
  db->Delete(write_options, "key7909");

  // op 28392: get
  status = db->Get(read_options, "key3783", &value);
  if (status.ok()) {
    out << "GET key3783 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28393: put
  db->Put(write_options, "key8705", "value98741");

  // op 28394: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9311", "key11349", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9311, key11349) size=" << scan_result.size() << std::endl;
  }

  // op 28395: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6037", "key7244", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6037, key7244) size=" << scan_result.size() << std::endl;
  }

  // op 28396: get
  status = db->Get(read_options, "key3213", &value);
  if (status.ok()) {
    out << "GET key3213 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28397: put
  db->Put(write_options, "key2733", "value44456");

  // op 28398: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1399", "key6448", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1399, key6448) size=" << scan_result.size() << std::endl;
  }

  // op 28399: put
  db->Put(write_options, "key1971", "value41811");

  // op 28400: put
  db->Put(write_options, "key6465", "value99906");

  // op 28401: get
  status = db->Get(read_options, "key1010", &value);
  if (status.ok()) {
    out << "GET key1010 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28402: put
  db->Put(write_options, "key2851", "value24812");

  // op 28403: put
  db->Put(write_options, "key11667", "value99206");

  // op 28404: get
  status = db->Get(read_options, "key6417", &value);
  if (status.ok()) {
    out << "GET key6417 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28405: put
  db->Put(write_options, "key5346", "value56498");

  // op 28406: get
  status = db->Get(read_options, "key3301", &value);
  if (status.ok()) {
    out << "GET key3301 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28407: get
  status = db->Get(read_options, "key4608", &value);
  if (status.ok()) {
    out << "GET key4608 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28408: delete_range
  status = db->DeleteRange(write_options, "key3483", "key5754");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 28409: get
  status = db->Get(read_options, "key3653", &value);
  if (status.ok()) {
    out << "GET key3653 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28410: put
  db->Put(write_options, "key9201", "value77494");

  // op 28411: get
  status = db->Get(read_options, "key9510", &value);
  if (status.ok()) {
    out << "GET key9510 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28412: put
  db->Put(write_options, "key5021", "value66649");

  // op 28413: get
  status = db->Get(read_options, "key8786", &value);
  if (status.ok()) {
    out << "GET key8786 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28414: delete
  db->Delete(write_options, "key7162");

  // op 28415: get
  status = db->Get(read_options, "key5167", &value);
  if (status.ok()) {
    out << "GET key5167 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28416: put
  db->Put(write_options, "key10990", "value93054");

  // op 28417: put
  db->Put(write_options, "key8135", "value23356");

  // op 28418: get
  status = db->Get(read_options, "key2719", &value);
  if (status.ok()) {
    out << "GET key2719 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28419: put
  db->Put(write_options, "key6676", "value114359");

  // op 28420: put
  db->Put(write_options, "key6570", "value56130");

  // op 28421: put
  db->Put(write_options, "key9354", "value112190");

  // op 28422: put
  db->Put(write_options, "key10513", "value92633");

  // op 28423: put
  db->Put(write_options, "key2332", "value68108");

  // op 28424: delete
  db->Delete(write_options, "key3883");

  // op 28425: get
  status = db->Get(read_options, "key9427", &value);
  if (status.ok()) {
    out << "GET key9427 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28426: get
  status = db->Get(read_options, "key6865", &value);
  if (status.ok()) {
    out << "GET key6865 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28427: delete_range
  status = db->DeleteRange(write_options, "key1549", "key10904");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 28428: get
  status = db->Get(read_options, "key5850", &value);
  if (status.ok()) {
    out << "GET key5850 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28429: put
  db->Put(write_options, "key2119", "value57886");

  // op 28430: delete_range
  status = db->DeleteRange(write_options, "key0321", "key9268");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 28431: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5710", "key7165", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5710, key7165) size=" << scan_result.size() << std::endl;
  }

  // op 28432: get
  status = db->Get(read_options, "key10633", &value);
  if (status.ok()) {
    out << "GET key10633 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28433: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4777", "key9505", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4777, key9505) size=" << scan_result.size() << std::endl;
  }

  // op 28434: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3957", "key9836", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3957, key9836) size=" << scan_result.size() << std::endl;
  }

  // op 28435: delete_range
  status = db->DeleteRange(write_options, "key0333", "key8974");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 28436: put
  db->Put(write_options, "key11294", "value108809");

  // op 28437: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1617", "key2307", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1617, key2307) size=" << scan_result.size() << std::endl;
  }

  // op 28438: put
  db->Put(write_options, "key4313", "value76253");

  // op 28439: put
  db->Put(write_options, "key3266", "value93510");

  // op 28440: get
  status = db->Get(read_options, "key0388", &value);
  if (status.ok()) {
    out << "GET key0388 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28441: put
  db->Put(write_options, "key3394", "value65047");

  // op 28442: delete_range
  status = db->DeleteRange(write_options, "key6878", "key11702");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 28443: get
  status = db->Get(read_options, "key7719", &value);
  if (status.ok()) {
    out << "GET key7719 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28444: put
  db->Put(write_options, "key5513", "value53728");

  // op 28445: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1711", "key8377", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1711, key8377) size=" << scan_result.size() << std::endl;
  }

  // op 28446: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 28447: put
  db->Put(write_options, "key1848", "value54116");

  // op 28448: delete
  db->Delete(write_options, "key1869");

  // op 28449: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 28450: delete
  db->Delete(write_options, "key5024");

  // op 28451: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 28452: put
  db->Put(write_options, "key0946", "value103473");

  // op 28453: put
  db->Put(write_options, "key1715", "value2285");

  // op 28454: delete
  db->Delete(write_options, "key2955");

  // op 28455: put
  db->Put(write_options, "key6281", "value88137");

  // op 28456: get
  status = db->Get(read_options, "key3572", &value);
  if (status.ok()) {
    out << "GET key3572 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28457: delete_range
  status = db->DeleteRange(write_options, "key1883", "key4525");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 28458: delete
  db->Delete(write_options, "key4930");

  // op 28459: put
  db->Put(write_options, "key5891", "value104440");

  // op 28460: put
  db->Put(write_options, "key10885", "value95975");

  // op 28461: put
  db->Put(write_options, "key10451", "value55635");

  // op 28462: put
  db->Put(write_options, "key0740", "value60486");

  // op 28463: put
  db->Put(write_options, "key1837", "value69807");

  // op 28464: get
  status = db->Get(read_options, "key8667", &value);
  if (status.ok()) {
    out << "GET key8667 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28465: put
  db->Put(write_options, "key1258", "value38333");

  // op 28466: get
  status = db->Get(read_options, "key4161", &value);
  if (status.ok()) {
    out << "GET key4161 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28467: put
  db->Put(write_options, "key4010", "value72863");

  // op 28468: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4095", "key8038", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4095, key8038) size=" << scan_result.size() << std::endl;
  }

  // op 28469: get
  status = db->Get(read_options, "key6950", &value);
  if (status.ok()) {
    out << "GET key6950 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28470: get
  status = db->Get(read_options, "key0977", &value);
  if (status.ok()) {
    out << "GET key0977 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28471: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 28472: delete
  db->Delete(write_options, "key8741");

  // op 28473: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2378", "key10729", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2378, key10729) size=" << scan_result.size() << std::endl;
  }

  // op 28474: delete_range
  status = db->DeleteRange(write_options, "key0845", "key4112");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 28475: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0296", "key4484", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0296, key4484) size=" << scan_result.size() << std::endl;
  }

  // op 28476: put
  db->Put(write_options, "key2690", "value71636");

  // op 28477: get
  status = db->Get(read_options, "key9056", &value);
  if (status.ok()) {
    out << "GET key9056 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28478: delete
  db->Delete(write_options, "key1580");

  // op 28479: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1831", "key5155", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1831, key5155) size=" << scan_result.size() << std::endl;
  }

  // op 28480: put
  db->Put(write_options, "key4192", "value11112");

  // op 28481: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 28482: put
  db->Put(write_options, "key9376", "value36360");

  // op 28483: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 28484: get
  status = db->Get(read_options, "key5838", &value);
  if (status.ok()) {
    out << "GET key5838 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28485: put
  db->Put(write_options, "key8945", "value87352");

  // op 28486: delete_range
  status = db->DeleteRange(write_options, "key8415", "key9546");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 28487: delete
  db->Delete(write_options, "key1877");

  // op 28488: get
  status = db->Get(read_options, "key4079", &value);
  if (status.ok()) {
    out << "GET key4079 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28489: put
  db->Put(write_options, "key11451", "value81877");

  // op 28490: get
  status = db->Get(read_options, "key8685", &value);
  if (status.ok()) {
    out << "GET key8685 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28491: delete_range
  status = db->DeleteRange(write_options, "key5759", "key6878");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 28492: put
  db->Put(write_options, "key3861", "value28745");

  // op 28493: put
  db->Put(write_options, "key5058", "value24897");

  // op 28494: get
  status = db->Get(read_options, "key3993", &value);
  if (status.ok()) {
    out << "GET key3993 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28495: delete_range
  status = db->DeleteRange(write_options, "key1885", "key10691");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 28496: put
  db->Put(write_options, "key11122", "value52842");

  // op 28497: get
  status = db->Get(read_options, "key5889", &value);
  if (status.ok()) {
    out << "GET key5889 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28498: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2570", "key9403", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2570, key9403) size=" << scan_result.size() << std::endl;
  }

  // op 28499: put
  db->Put(write_options, "key6596", "value82290");

  // op 28500: put
  db->Put(write_options, "key0210", "value86006");

  // op 28501: put
  db->Put(write_options, "key9437", "value11399");

  // op 28502: put
  db->Put(write_options, "key8645", "value30631");

  // op 28503: put
  db->Put(write_options, "key6664", "value74724");

  // op 28504: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5500", "key5804", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5500, key5804) size=" << scan_result.size() << std::endl;
  }

  // op 28505: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2808", "key8859", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2808, key8859) size=" << scan_result.size() << std::endl;
  }

  // op 28506: get
  status = db->Get(read_options, "key2435", &value);
  if (status.ok()) {
    out << "GET key2435 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28507: get
  status = db->Get(read_options, "key7095", &value);
  if (status.ok()) {
    out << "GET key7095 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28508: delete
  db->Delete(write_options, "key5412");

  // op 28509: put
  db->Put(write_options, "key6868", "value65488");

  // op 28510: put
  db->Put(write_options, "key9190", "value9330");

  // op 28511: put
  db->Put(write_options, "key8761", "value41523");

  // op 28512: delete_range
  status = db->DeleteRange(write_options, "key2318", "key7507");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 28513: delete_range
  status = db->DeleteRange(write_options, "key4492", "key7030");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 28514: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4612", "key5207", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4612, key5207) size=" << scan_result.size() << std::endl;
  }

  // op 28515: put
  db->Put(write_options, "key2601", "value32349");

  // op 28516: delete_range
  status = db->DeleteRange(write_options, "key5861", "key11119");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 28517: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5137", "key10895", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5137, key10895) size=" << scan_result.size() << std::endl;
  }

  // op 28518: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 28519: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1156", "key3237", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1156, key3237) size=" << scan_result.size() << std::endl;
  }

  // op 28520: put
  db->Put(write_options, "key6936", "value33671");

  // op 28521: put
  db->Put(write_options, "key7932", "value106834");

  // op 28522: put
  db->Put(write_options, "key6525", "value91443");

  // op 28523: put
  db->Put(write_options, "key8149", "value14147");

  // op 28524: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1183", "key11557", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1183, key11557) size=" << scan_result.size() << std::endl;
  }

  // op 28525: get
  status = db->Get(read_options, "key6192", &value);
  if (status.ok()) {
    out << "GET key6192 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28526: get
  status = db->Get(read_options, "key4870", &value);
  if (status.ok()) {
    out << "GET key4870 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28527: put
  db->Put(write_options, "key2574", "value107187");

  // op 28528: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0349", "key7946", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0349, key7946) size=" << scan_result.size() << std::endl;
  }

  // op 28529: put
  db->Put(write_options, "key2185", "value31824");

  // op 28530: delete
  db->Delete(write_options, "key7654");

  // op 28531: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 28532: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 28533: delete
  db->Delete(write_options, "key11356");

  // op 28534: put
  db->Put(write_options, "key9557", "value99260");

  // op 28535: put
  db->Put(write_options, "key11213", "value62757");

  // op 28536: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6151", "key11485", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6151, key11485) size=" << scan_result.size() << std::endl;
  }

  // op 28537: delete_range
  status = db->DeleteRange(write_options, "key2050", "key5217");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 28538: put
  db->Put(write_options, "key2284", "value90938");

  // op 28539: get
  status = db->Get(read_options, "key7277", &value);
  if (status.ok()) {
    out << "GET key7277 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28540: put
  db->Put(write_options, "key10174", "value70798");

  // op 28541: delete
  db->Delete(write_options, "key10196");

  // op 28542: put
  db->Put(write_options, "key1606", "value109125");

  // op 28543: delete
  db->Delete(write_options, "key8978");

  // op 28544: put
  db->Put(write_options, "key9020", "value106756");

  // op 28545: put
  db->Put(write_options, "key6908", "value10553");

  // op 28546: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2237", "key3950", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2237, key3950) size=" << scan_result.size() << std::endl;
  }

  // op 28547: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2187", "key4743", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2187, key4743) size=" << scan_result.size() << std::endl;
  }

  // op 28548: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6793", "key8095", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6793, key8095) size=" << scan_result.size() << std::endl;
  }

  // op 28549: put
  db->Put(write_options, "key3819", "value56669");

  // op 28550: put
  db->Put(write_options, "key5271", "value101350");

  // op 28551: put
  db->Put(write_options, "key0021", "value115410");

  // op 28552: put
  db->Put(write_options, "key8596", "value98459");

  // op 28553: delete
  db->Delete(write_options, "key3874");

  // op 28554: delete_range
  status = db->DeleteRange(write_options, "key9293", "key9703");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 28555: get
  status = db->Get(read_options, "key4107", &value);
  if (status.ok()) {
    out << "GET key4107 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28556: get
  status = db->Get(read_options, "key1263", &value);
  if (status.ok()) {
    out << "GET key1263 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28557: put
  db->Put(write_options, "key10642", "value0799");

  // op 28558: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7542", "key9889", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7542, key9889) size=" << scan_result.size() << std::endl;
  }

  // op 28559: delete
  db->Delete(write_options, "key1672");

  // op 28560: get
  status = db->Get(read_options, "key6987", &value);
  if (status.ok()) {
    out << "GET key6987 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28561: delete_range
  status = db->DeleteRange(write_options, "key6876", "key9717");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 28562: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 28563: delete
  db->Delete(write_options, "key7906");

  // op 28564: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7533", "key11272", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7533, key11272) size=" << scan_result.size() << std::endl;
  }

  // op 28565: put
  db->Put(write_options, "key6417", "value8120");

  // op 28566: put
  db->Put(write_options, "key8343", "value92809");

  // op 28567: put
  db->Put(write_options, "key6993", "value86169");

  // op 28568: get
  status = db->Get(read_options, "key6712", &value);
  if (status.ok()) {
    out << "GET key6712 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28569: put
  db->Put(write_options, "key9173", "value8661");

  // op 28570: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5444", "key7469", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5444, key7469) size=" << scan_result.size() << std::endl;
  }

  // op 28571: put
  db->Put(write_options, "key0464", "value60982");

  // op 28572: put
  db->Put(write_options, "key11479", "value85770");

  // op 28573: put
  db->Put(write_options, "key5046", "value47353");

  // op 28574: delete
  db->Delete(write_options, "key10070");

  // op 28575: get
  status = db->Get(read_options, "key0442", &value);
  if (status.ok()) {
    out << "GET key0442 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28576: put
  db->Put(write_options, "key3081", "value40493");

  // op 28577: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 28578: delete_range
  status = db->DeleteRange(write_options, "key0085", "key9329");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 28579: delete
  db->Delete(write_options, "key11603");

  // op 28580: put
  db->Put(write_options, "key10784", "value104201");

  // op 28581: put
  db->Put(write_options, "key10354", "value28899");

  // op 28582: get
  status = db->Get(read_options, "key11303", &value);
  if (status.ok()) {
    out << "GET key11303 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28583: put
  db->Put(write_options, "key3384", "value84792");

  // op 28584: delete
  db->Delete(write_options, "key10298");

  // op 28585: put
  db->Put(write_options, "key10279", "value22007");

  // op 28586: delete_range
  status = db->DeleteRange(write_options, "key2398", "key9209");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 28587: get
  status = db->Get(read_options, "key6052", &value);
  if (status.ok()) {
    out << "GET key6052 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28588: put
  db->Put(write_options, "key8599", "value15381");

  // op 28589: get
  status = db->Get(read_options, "key4141", &value);
  if (status.ok()) {
    out << "GET key4141 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28590: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0192", "key2688", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0192, key2688) size=" << scan_result.size() << std::endl;
  }

  // op 28591: delete_range
  status = db->DeleteRange(write_options, "key2555", "key7995");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 28592: put
  db->Put(write_options, "key7676", "value74237");

  // op 28593: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 28594: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6459", "key11673", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6459, key11673) size=" << scan_result.size() << std::endl;
  }

  // op 28595: delete_range
  status = db->DeleteRange(write_options, "key1148", "key6144");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 28596: put
  db->Put(write_options, "key8848", "value26578");

  // op 28597: put
  db->Put(write_options, "key10883", "value32821");

  // op 28598: put
  db->Put(write_options, "key3466", "value86251");

  // op 28599: put
  db->Put(write_options, "key7604", "value52272");

  // op 28600: put
  db->Put(write_options, "key6753", "value113049");

  // op 28601: put
  db->Put(write_options, "key0111", "value85206");

  // op 28602: get
  status = db->Get(read_options, "key4483", &value);
  if (status.ok()) {
    out << "GET key4483 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28603: put
  db->Put(write_options, "key10330", "value39408");

  // op 28604: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7572", "key8492", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7572, key8492) size=" << scan_result.size() << std::endl;
  }

  // op 28605: get
  status = db->Get(read_options, "key6315", &value);
  if (status.ok()) {
    out << "GET key6315 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28606: get
  status = db->Get(read_options, "key7734", &value);
  if (status.ok()) {
    out << "GET key7734 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28607: put
  db->Put(write_options, "key5347", "value43667");

  // op 28608: put
  db->Put(write_options, "key3271", "value110513");

  // op 28609: delete_range
  status = db->DeleteRange(write_options, "key6535", "key7860");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 28610: put
  db->Put(write_options, "key1456", "value63368");

  // op 28611: put
  db->Put(write_options, "key7168", "value44003");

  // op 28612: delete_range
  status = db->DeleteRange(write_options, "key8340", "key8448");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 28613: get
  status = db->Get(read_options, "key10730", &value);
  if (status.ok()) {
    out << "GET key10730 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28614: put
  db->Put(write_options, "key2960", "value32256");

  // op 28615: delete
  db->Delete(write_options, "key9804");

  // op 28616: get
  status = db->Get(read_options, "key2050", &value);
  if (status.ok()) {
    out << "GET key2050 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28617: get
  status = db->Get(read_options, "key5322", &value);
  if (status.ok()) {
    out << "GET key5322 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28618: put
  db->Put(write_options, "key7135", "value96965");

  // op 28619: put
  db->Put(write_options, "key5521", "value40058");

  // op 28620: get
  status = db->Get(read_options, "key4766", &value);
  if (status.ok()) {
    out << "GET key4766 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28621: put
  db->Put(write_options, "key3998", "value64747");

  // op 28622: get
  status = db->Get(read_options, "key5788", &value);
  if (status.ok()) {
    out << "GET key5788 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28623: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 28624: get
  status = db->Get(read_options, "key1571", &value);
  if (status.ok()) {
    out << "GET key1571 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28625: put
  db->Put(write_options, "key0056", "value115156");

  // op 28626: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0696", "key9500", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0696, key9500) size=" << scan_result.size() << std::endl;
  }

  // op 28627: delete
  db->Delete(write_options, "key6100");

  // op 28628: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6296", "key11202", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6296, key11202) size=" << scan_result.size() << std::endl;
  }

  // op 28629: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3224", "key10422", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3224, key10422) size=" << scan_result.size() << std::endl;
  }

  // op 28630: put
  db->Put(write_options, "key2890", "value33616");

  // op 28631: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1058", "key6494", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1058, key6494) size=" << scan_result.size() << std::endl;
  }

  // op 28632: put
  db->Put(write_options, "key0823", "value74029");

  // op 28633: delete_range
  status = db->DeleteRange(write_options, "key2362", "key5345");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 28634: put
  db->Put(write_options, "key8360", "value48010");

  // op 28635: put
  db->Put(write_options, "key3573", "value85261");

  // op 28636: put
  db->Put(write_options, "key7754", "value5322");

  // op 28637: delete_range
  status = db->DeleteRange(write_options, "key4880", "key5874");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 28638: put
  db->Put(write_options, "key9156", "value67775");

  // op 28639: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1321", "key6495", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1321, key6495) size=" << scan_result.size() << std::endl;
  }

  // op 28640: put
  db->Put(write_options, "key8662", "value34925");

  // op 28641: delete
  db->Delete(write_options, "key2088");

  // op 28642: delete_range
  status = db->DeleteRange(write_options, "key0905", "key10127");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 28643: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 28644: put
  db->Put(write_options, "key5892", "value47655");

  // op 28645: get
  status = db->Get(read_options, "key6784", &value);
  if (status.ok()) {
    out << "GET key6784 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28646: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2510", "key9783", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2510, key9783) size=" << scan_result.size() << std::endl;
  }

  // op 28647: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 28648: put
  db->Put(write_options, "key9990", "value94911");

  // op 28649: put
  db->Put(write_options, "key1842", "value50833");

  // op 28650: get
  status = db->Get(read_options, "key1212", &value);
  if (status.ok()) {
    out << "GET key1212 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28651: put
  db->Put(write_options, "key4498", "value0464");

  // op 28652: get
  status = db->Get(read_options, "key1739", &value);
  if (status.ok()) {
    out << "GET key1739 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28653: put
  db->Put(write_options, "key2504", "value32249");

  // op 28654: delete
  db->Delete(write_options, "key11650");

  // op 28655: get
  status = db->Get(read_options, "key10604", &value);
  if (status.ok()) {
    out << "GET key10604 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28656: put
  db->Put(write_options, "key6471", "value99532");

  // op 28657: put
  db->Put(write_options, "key7146", "value107069");

  // op 28658: put
  db->Put(write_options, "key2274", "value32665");

  // op 28659: put
  db->Put(write_options, "key7859", "value70450");

  // op 28660: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7239", "key10094", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7239, key10094) size=" << scan_result.size() << std::endl;
  }

  // op 28661: delete
  db->Delete(write_options, "key4432");

  // op 28662: delete
  db->Delete(write_options, "key8179");

  // op 28663: delete
  db->Delete(write_options, "key7166");

  // op 28664: delete
  db->Delete(write_options, "key4634");

  // op 28665: get
  status = db->Get(read_options, "key9743", &value);
  if (status.ok()) {
    out << "GET key9743 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28666: put
  db->Put(write_options, "key10623", "value65752");

  // op 28667: get
  status = db->Get(read_options, "key11241", &value);
  if (status.ok()) {
    out << "GET key11241 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28668: put
  db->Put(write_options, "key6754", "value114880");

  // op 28669: put
  db->Put(write_options, "key9431", "value18287");

  // op 28670: put
  db->Put(write_options, "key11360", "value23277");

  // op 28671: put
  db->Put(write_options, "key7505", "value96143");

  // op 28672: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0050", "key7015", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0050, key7015) size=" << scan_result.size() << std::endl;
  }

  // op 28673: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1118", "key8684", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1118, key8684) size=" << scan_result.size() << std::endl;
  }

  // op 28674: delete
  db->Delete(write_options, "key5968");

  // op 28675: delete_range
  status = db->DeleteRange(write_options, "key6875", "key6904");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 28676: put
  db->Put(write_options, "key8334", "value82233");

  // op 28677: get
  status = db->Get(read_options, "key4814", &value);
  if (status.ok()) {
    out << "GET key4814 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28678: delete_range
  status = db->DeleteRange(write_options, "key7579", "key8957");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 28679: put
  db->Put(write_options, "key10379", "value83863");

  // op 28680: delete
  db->Delete(write_options, "key4229");

  // op 28681: get
  status = db->Get(read_options, "key4543", &value);
  if (status.ok()) {
    out << "GET key4543 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28682: delete_range
  status = db->DeleteRange(write_options, "key1137", "key6860");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 28683: put
  db->Put(write_options, "key4855", "value76017");

  // op 28684: put
  db->Put(write_options, "key3793", "value86078");

  // op 28685: get
  status = db->Get(read_options, "key8625", &value);
  if (status.ok()) {
    out << "GET key8625 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28686: get
  status = db->Get(read_options, "key2968", &value);
  if (status.ok()) {
    out << "GET key2968 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28687: put
  db->Put(write_options, "key6659", "value114104");

  // op 28688: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 28689: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 28690: delete
  db->Delete(write_options, "key11562");

  // op 28691: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 28692: get
  status = db->Get(read_options, "key9040", &value);
  if (status.ok()) {
    out << "GET key9040 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28693: delete
  db->Delete(write_options, "key8865");

  // op 28694: put
  db->Put(write_options, "key1575", "value24954");

  // op 28695: put
  db->Put(write_options, "key9516", "value3451");

  // op 28696: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1953", "key8400", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1953, key8400) size=" << scan_result.size() << std::endl;
  }

  // op 28697: put
  db->Put(write_options, "key8839", "value93001");

  // op 28698: put
  db->Put(write_options, "key1303", "value115647");

  // op 28699: delete_range
  status = db->DeleteRange(write_options, "key6560", "key10874");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 28700: get
  status = db->Get(read_options, "key0780", &value);
  if (status.ok()) {
    out << "GET key0780 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28701: get
  status = db->Get(read_options, "key8377", &value);
  if (status.ok()) {
    out << "GET key8377 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28702: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2880", "key11190", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2880, key11190) size=" << scan_result.size() << std::endl;
  }

  // op 28703: put
  db->Put(write_options, "key9456", "value13308");

  // op 28704: put
  db->Put(write_options, "key11510", "value101318");

  // op 28705: get
  status = db->Get(read_options, "key11397", &value);
  if (status.ok()) {
    out << "GET key11397 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28706: put
  db->Put(write_options, "key2204", "value42280");

  // op 28707: get
  status = db->Get(read_options, "key0097", &value);
  if (status.ok()) {
    out << "GET key0097 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28708: put
  db->Put(write_options, "key11561", "value80897");

  // op 28709: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 28710: put
  db->Put(write_options, "key1447", "value51170");

  // op 28711: delete
  db->Delete(write_options, "key1467");

  // op 28712: delete_range
  status = db->DeleteRange(write_options, "key4983", "key7509");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 28713: delete_range
  status = db->DeleteRange(write_options, "key0292", "key6021");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 28714: delete_range
  status = db->DeleteRange(write_options, "key5506", "key9916");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 28715: put
  db->Put(write_options, "key3840", "value57433");

  // op 28716: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3414", "key9705", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3414, key9705) size=" << scan_result.size() << std::endl;
  }

  // op 28717: put
  db->Put(write_options, "key4166", "value96429");

  // op 28718: put
  db->Put(write_options, "key1647", "value13427");

  // op 28719: get
  status = db->Get(read_options, "key3957", &value);
  if (status.ok()) {
    out << "GET key3957 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28720: put
  db->Put(write_options, "key3489", "value18007");

  // op 28721: get
  status = db->Get(read_options, "key3236", &value);
  if (status.ok()) {
    out << "GET key3236 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28722: delete
  db->Delete(write_options, "key4573");

  // op 28723: get
  status = db->Get(read_options, "key9104", &value);
  if (status.ok()) {
    out << "GET key9104 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28724: delete
  db->Delete(write_options, "key4900");

  // op 28725: get
  status = db->Get(read_options, "key0611", &value);
  if (status.ok()) {
    out << "GET key0611 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28726: put
  db->Put(write_options, "key9772", "value15516");

  // op 28727: delete_range
  status = db->DeleteRange(write_options, "key3531", "key4156");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 28728: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2296", "key10298", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2296, key10298) size=" << scan_result.size() << std::endl;
  }

  // op 28729: get
  status = db->Get(read_options, "key5517", &value);
  if (status.ok()) {
    out << "GET key5517 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28730: put
  db->Put(write_options, "key1072", "value98126");

  // op 28731: get
  status = db->Get(read_options, "key11588", &value);
  if (status.ok()) {
    out << "GET key11588 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28732: get
  status = db->Get(read_options, "key1766", &value);
  if (status.ok()) {
    out << "GET key1766 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28733: get
  status = db->Get(read_options, "key8123", &value);
  if (status.ok()) {
    out << "GET key8123 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28734: put
  db->Put(write_options, "key5532", "value44468");

  // op 28735: put
  db->Put(write_options, "key1430", "value75160");

  // op 28736: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2262", "key11608", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2262, key11608) size=" << scan_result.size() << std::endl;
  }

  // op 28737: delete_range
  status = db->DeleteRange(write_options, "key3554", "key3961");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 28738: put
  db->Put(write_options, "key3483", "value42806");

  // op 28739: put
  db->Put(write_options, "key0029", "value30400");

  // op 28740: put
  db->Put(write_options, "key6512", "value7492");

  // op 28741: put
  db->Put(write_options, "key0010", "value78920");

  // op 28742: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4505", "key7477", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4505, key7477) size=" << scan_result.size() << std::endl;
  }

  // op 28743: get
  status = db->Get(read_options, "key1626", &value);
  if (status.ok()) {
    out << "GET key1626 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28744: get
  status = db->Get(read_options, "key2149", &value);
  if (status.ok()) {
    out << "GET key2149 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28745: get
  status = db->Get(read_options, "key3321", &value);
  if (status.ok()) {
    out << "GET key3321 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28746: put
  db->Put(write_options, "key10319", "value4891");

  // op 28747: put
  db->Put(write_options, "key4099", "value89807");

  // op 28748: put
  db->Put(write_options, "key10105", "value62304");

  // op 28749: put
  db->Put(write_options, "key1889", "value30516");

  // op 28750: delete_range
  status = db->DeleteRange(write_options, "key3490", "key9696");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 28751: put
  db->Put(write_options, "key8163", "value88264");

  // op 28752: put
  db->Put(write_options, "key6318", "value57833");

  // op 28753: delete_range
  status = db->DeleteRange(write_options, "key1992", "key8485");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 28754: put
  db->Put(write_options, "key2320", "value78760");

  // op 28755: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10613", "key11321", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10613, key11321) size=" << scan_result.size() << std::endl;
  }

  // op 28756: put
  db->Put(write_options, "key6260", "value75623");

  // op 28757: put
  db->Put(write_options, "key3122", "value10895");

  // op 28758: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0169", "key10997", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0169, key10997) size=" << scan_result.size() << std::endl;
  }

  // op 28759: delete
  db->Delete(write_options, "key7864");

  // op 28760: put
  db->Put(write_options, "key0524", "value37239");

  // op 28761: put
  db->Put(write_options, "key3600", "value117209");

  // op 28762: get
  status = db->Get(read_options, "key3404", &value);
  if (status.ok()) {
    out << "GET key3404 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28763: put
  db->Put(write_options, "key1406", "value85985");

  // op 28764: delete_range
  status = db->DeleteRange(write_options, "key7692", "key7725");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 28765: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1608", "key4815", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1608, key4815) size=" << scan_result.size() << std::endl;
  }

  // op 28766: put
  db->Put(write_options, "key7557", "value36838");

  // op 28767: put
  db->Put(write_options, "key0362", "value8948");

  // op 28768: get
  status = db->Get(read_options, "key3784", &value);
  if (status.ok()) {
    out << "GET key3784 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28769: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10416", "key10561", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10416, key10561) size=" << scan_result.size() << std::endl;
  }

  // op 28770: put
  db->Put(write_options, "key5587", "value93680");

  // op 28771: put
  db->Put(write_options, "key4827", "value103414");

  // op 28772: put
  db->Put(write_options, "key4907", "value52155");

  // op 28773: delete_range
  status = db->DeleteRange(write_options, "key8648", "key9871");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 28774: get
  status = db->Get(read_options, "key7107", &value);
  if (status.ok()) {
    out << "GET key7107 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28775: get
  status = db->Get(read_options, "key0284", &value);
  if (status.ok()) {
    out << "GET key0284 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28776: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2810", "key10591", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2810, key10591) size=" << scan_result.size() << std::endl;
  }

  // op 28777: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 28778: delete_range
  status = db->DeleteRange(write_options, "key1225", "key9736");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 28779: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1664", "key4733", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1664, key4733) size=" << scan_result.size() << std::endl;
  }

  // op 28780: put
  db->Put(write_options, "key10195", "value66302");

  // op 28781: put
  db->Put(write_options, "key6670", "value112630");

  // op 28782: put
  db->Put(write_options, "key8127", "value58670");

  // op 28783: put
  db->Put(write_options, "key9264", "value35169");

  // op 28784: put
  db->Put(write_options, "key2435", "value66467");

  // op 28785: put
  db->Put(write_options, "key10445", "value10490");

  // op 28786: delete_range
  status = db->DeleteRange(write_options, "key7254", "key10378");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 28787: get
  status = db->Get(read_options, "key5616", &value);
  if (status.ok()) {
    out << "GET key5616 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28788: put
  db->Put(write_options, "key6372", "value89309");

  // op 28789: put
  db->Put(write_options, "key5523", "value29101");

  // op 28790: delete_range
  status = db->DeleteRange(write_options, "key4667", "key10759");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 28791: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 28792: get
  status = db->Get(read_options, "key5374", &value);
  if (status.ok()) {
    out << "GET key5374 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28793: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4694", "key7448", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4694, key7448) size=" << scan_result.size() << std::endl;
  }

  // op 28794: put
  db->Put(write_options, "key4853", "value81310");

  // op 28795: put
  db->Put(write_options, "key11221", "value84430");

  // op 28796: put
  db->Put(write_options, "key2371", "value6921");

  // op 28797: delete
  db->Delete(write_options, "key6008");

  // op 28798: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2883", "key10987", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2883, key10987) size=" << scan_result.size() << std::endl;
  }

  // op 28799: delete
  db->Delete(write_options, "key5122");

  // op 28800: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2483", "key4417", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2483, key4417) size=" << scan_result.size() << std::endl;
  }

  // op 28801: put
  db->Put(write_options, "key7085", "value0299");

  // op 28802: delete
  db->Delete(write_options, "key4818");

  // op 28803: get
  status = db->Get(read_options, "key9450", &value);
  if (status.ok()) {
    out << "GET key9450 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28804: put
  db->Put(write_options, "key6542", "value103299");

  // op 28805: get
  status = db->Get(read_options, "key10441", &value);
  if (status.ok()) {
    out << "GET key10441 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28806: put
  db->Put(write_options, "key5307", "value40635");

  // op 28807: put
  db->Put(write_options, "key6580", "value16845");

  // op 28808: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 28809: put
  db->Put(write_options, "key6334", "value63203");

  // op 28810: put
  db->Put(write_options, "key10572", "value88552");

  // op 28811: put
  db->Put(write_options, "key9000", "value30837");

  // op 28812: get
  status = db->Get(read_options, "key1275", &value);
  if (status.ok()) {
    out << "GET key1275 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28813: get
  status = db->Get(read_options, "key0554", &value);
  if (status.ok()) {
    out << "GET key0554 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28814: get
  status = db->Get(read_options, "key7309", &value);
  if (status.ok()) {
    out << "GET key7309 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28815: put
  db->Put(write_options, "key4781", "value102453");

  // op 28816: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6424", "key7691", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6424, key7691) size=" << scan_result.size() << std::endl;
  }

  // op 28817: get
  status = db->Get(read_options, "key10140", &value);
  if (status.ok()) {
    out << "GET key10140 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28818: delete
  db->Delete(write_options, "key8080");

  // op 28819: get
  status = db->Get(read_options, "key7567", &value);
  if (status.ok()) {
    out << "GET key7567 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28820: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7279", "key8980", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7279, key8980) size=" << scan_result.size() << std::endl;
  }

  // op 28821: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 28822: put
  db->Put(write_options, "key4386", "value6108");

  // op 28823: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 28824: get
  status = db->Get(read_options, "key2216", &value);
  if (status.ok()) {
    out << "GET key2216 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28825: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 28826: put
  db->Put(write_options, "key3985", "value26862");

  // op 28827: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 28828: get
  status = db->Get(read_options, "key0114", &value);
  if (status.ok()) {
    out << "GET key0114 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28829: get
  status = db->Get(read_options, "key9953", &value);
  if (status.ok()) {
    out << "GET key9953 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28830: get
  status = db->Get(read_options, "key6478", &value);
  if (status.ok()) {
    out << "GET key6478 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28831: put
  db->Put(write_options, "key1229", "value109368");

  // op 28832: delete_range
  status = db->DeleteRange(write_options, "key0147", "key0552");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 28833: put
  db->Put(write_options, "key4505", "value16604");

  // op 28834: delete_range
  status = db->DeleteRange(write_options, "key6195", "key7137");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 28835: delete
  db->Delete(write_options, "key9844");

  // op 28836: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0954", "key1332", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0954, key1332) size=" << scan_result.size() << std::endl;
  }

  // op 28837: delete
  db->Delete(write_options, "key5466");

  // op 28838: put
  db->Put(write_options, "key5376", "value77364");

  // op 28839: put
  db->Put(write_options, "key6522", "value41895");

  // op 28840: delete_range
  status = db->DeleteRange(write_options, "key2078", "key10023");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 28841: put
  db->Put(write_options, "key4539", "value22020");

  // op 28842: get
  status = db->Get(read_options, "key0049", &value);
  if (status.ok()) {
    out << "GET key0049 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28843: get
  status = db->Get(read_options, "key1878", &value);
  if (status.ok()) {
    out << "GET key1878 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28844: delete
  db->Delete(write_options, "key3510");

  // op 28845: put
  db->Put(write_options, "key7704", "value1147");

  // op 28846: get
  status = db->Get(read_options, "key3585", &value);
  if (status.ok()) {
    out << "GET key3585 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28847: put
  db->Put(write_options, "key10133", "value72780");

  // op 28848: delete_range
  status = db->DeleteRange(write_options, "key1630", "key6254");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 28849: put
  db->Put(write_options, "key7711", "value73756");

  // op 28850: get
  status = db->Get(read_options, "key4662", &value);
  if (status.ok()) {
    out << "GET key4662 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28851: put
  db->Put(write_options, "key8530", "value86854");

  // op 28852: delete_range
  status = db->DeleteRange(write_options, "key3995", "key10586");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 28853: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1212", "key2714", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1212, key2714) size=" << scan_result.size() << std::endl;
  }

  // op 28854: put
  db->Put(write_options, "key2621", "value32236");

  // op 28855: get
  status = db->Get(read_options, "key3100", &value);
  if (status.ok()) {
    out << "GET key3100 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28856: put
  db->Put(write_options, "key1845", "value78669");

  // op 28857: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2116", "key9959", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2116, key9959) size=" << scan_result.size() << std::endl;
  }

  // op 28858: put
  db->Put(write_options, "key6136", "value14510");

  // op 28859: put
  db->Put(write_options, "key11110", "value79912");

  // op 28860: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6341", "key9692", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6341, key9692) size=" << scan_result.size() << std::endl;
  }

  // op 28861: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 28862: put
  db->Put(write_options, "key10037", "value77419");

  // op 28863: get
  status = db->Get(read_options, "key3009", &value);
  if (status.ok()) {
    out << "GET key3009 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28864: get
  status = db->Get(read_options, "key8686", &value);
  if (status.ok()) {
    out << "GET key8686 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28865: get
  status = db->Get(read_options, "key5789", &value);
  if (status.ok()) {
    out << "GET key5789 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28866: get
  status = db->Get(read_options, "key11005", &value);
  if (status.ok()) {
    out << "GET key11005 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28867: put
  db->Put(write_options, "key5693", "value5900");

  // op 28868: put
  db->Put(write_options, "key10337", "value33474");

  // op 28869: put
  db->Put(write_options, "key5959", "value15475");

  // op 28870: put
  db->Put(write_options, "key3532", "value86692");

  // op 28871: get
  status = db->Get(read_options, "key6370", &value);
  if (status.ok()) {
    out << "GET key6370 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28872: put
  db->Put(write_options, "key4460", "value99466");

  // op 28873: put
  db->Put(write_options, "key11197", "value6708");

  // op 28874: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 28875: get
  status = db->Get(read_options, "key10148", &value);
  if (status.ok()) {
    out << "GET key10148 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28876: put
  db->Put(write_options, "key5953", "value91282");

  // op 28877: put
  db->Put(write_options, "key6249", "value84728");

  // op 28878: put
  db->Put(write_options, "key5350", "value89637");

  // op 28879: put
  db->Put(write_options, "key2461", "value50059");

  // op 28880: put
  db->Put(write_options, "key8877", "value109701");

  // op 28881: put
  db->Put(write_options, "key9181", "value111197");

  // op 28882: put
  db->Put(write_options, "key9829", "value95347");

  // op 28883: put
  db->Put(write_options, "key4702", "value47320");

  // op 28884: put
  db->Put(write_options, "key11655", "value6954");

  // op 28885: put
  db->Put(write_options, "key11433", "value63062");

  // op 28886: put
  db->Put(write_options, "key6706", "value10180");

  // op 28887: get
  status = db->Get(read_options, "key6554", &value);
  if (status.ok()) {
    out << "GET key6554 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28888: delete
  db->Delete(write_options, "key0086");

  // op 28889: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3254", "key9967", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3254, key9967) size=" << scan_result.size() << std::endl;
  }

  // op 28890: delete
  db->Delete(write_options, "key5508");

  // op 28891: put
  db->Put(write_options, "key10217", "value98370");

  // op 28892: get
  status = db->Get(read_options, "key5297", &value);
  if (status.ok()) {
    out << "GET key5297 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28893: put
  db->Put(write_options, "key1818", "value67351");

  // op 28894: get
  status = db->Get(read_options, "key10733", &value);
  if (status.ok()) {
    out << "GET key10733 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28895: delete
  db->Delete(write_options, "key2823");

  // op 28896: get
  status = db->Get(read_options, "key8277", &value);
  if (status.ok()) {
    out << "GET key8277 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28897: put
  db->Put(write_options, "key11696", "value94231");

  // op 28898: get
  status = db->Get(read_options, "key8405", &value);
  if (status.ok()) {
    out << "GET key8405 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28899: delete
  db->Delete(write_options, "key8660");

  // op 28900: get
  status = db->Get(read_options, "key1780", &value);
  if (status.ok()) {
    out << "GET key1780 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28901: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9659", "key11682", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9659, key11682) size=" << scan_result.size() << std::endl;
  }

  // op 28902: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2650", "key6508", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2650, key6508) size=" << scan_result.size() << std::endl;
  }

  // op 28903: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4992", "key8568", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4992, key8568) size=" << scan_result.size() << std::endl;
  }

  // op 28904: get
  status = db->Get(read_options, "key10149", &value);
  if (status.ok()) {
    out << "GET key10149 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28905: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3285", "key3522", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3285, key3522) size=" << scan_result.size() << std::endl;
  }

  // op 28906: get
  status = db->Get(read_options, "key0169", &value);
  if (status.ok()) {
    out << "GET key0169 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28907: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 28908: put
  db->Put(write_options, "key1246", "value41101");

  // op 28909: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9652", "key9910", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9652, key9910) size=" << scan_result.size() << std::endl;
  }

  // op 28910: put
  db->Put(write_options, "key7255", "value103690");

  // op 28911: get
  status = db->Get(read_options, "key3431", &value);
  if (status.ok()) {
    out << "GET key3431 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28912: put
  db->Put(write_options, "key5309", "value30809");

  // op 28913: put
  db->Put(write_options, "key5134", "value112999");

  // op 28914: delete_range
  status = db->DeleteRange(write_options, "key1421", "key5836");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 28915: put
  db->Put(write_options, "key8463", "value35701");

  // op 28916: delete_range
  status = db->DeleteRange(write_options, "key9743", "key10739");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 28917: put
  db->Put(write_options, "key9499", "value13435");

  // op 28918: put
  db->Put(write_options, "key8555", "value71741");

  // op 28919: get
  status = db->Get(read_options, "key2066", &value);
  if (status.ok()) {
    out << "GET key2066 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28920: put
  db->Put(write_options, "key7693", "value21233");

  // op 28921: put
  db->Put(write_options, "key8845", "value58166");

  // op 28922: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1809", "key2330", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1809, key2330) size=" << scan_result.size() << std::endl;
  }

  // op 28923: put
  db->Put(write_options, "key4022", "value21664");

  // op 28924: get
  status = db->Get(read_options, "key6429", &value);
  if (status.ok()) {
    out << "GET key6429 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28925: put
  db->Put(write_options, "key7452", "value59175");

  // op 28926: put
  db->Put(write_options, "key9154", "value95632");

  // op 28927: delete_range
  status = db->DeleteRange(write_options, "key2843", "key3889");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 28928: put
  db->Put(write_options, "key9217", "value71936");

  // op 28929: get
  status = db->Get(read_options, "key7095", &value);
  if (status.ok()) {
    out << "GET key7095 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28930: put
  db->Put(write_options, "key4322", "value114684");

  // op 28931: get
  status = db->Get(read_options, "key1008", &value);
  if (status.ok()) {
    out << "GET key1008 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28932: delete
  db->Delete(write_options, "key11376");

  // op 28933: put
  db->Put(write_options, "key7482", "value79326");

  // op 28934: put
  db->Put(write_options, "key1775", "value43420");

  // op 28935: put
  db->Put(write_options, "key9256", "value33689");

  // op 28936: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 28937: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 28938: get
  status = db->Get(read_options, "key4311", &value);
  if (status.ok()) {
    out << "GET key4311 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28939: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 28940: put
  db->Put(write_options, "key11304", "value3574");

  // op 28941: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7620", "key7898", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7620, key7898) size=" << scan_result.size() << std::endl;
  }

  // op 28942: put
  db->Put(write_options, "key7910", "value13810");

  // op 28943: delete
  db->Delete(write_options, "key3979");

  // op 28944: delete
  db->Delete(write_options, "key4218");

  // op 28945: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8325", "key9384", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8325, key9384) size=" << scan_result.size() << std::endl;
  }

  // op 28946: delete
  db->Delete(write_options, "key8920");

  // op 28947: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3689", "key9287", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3689, key9287) size=" << scan_result.size() << std::endl;
  }

  // op 28948: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3152", "key3805", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3152, key3805) size=" << scan_result.size() << std::endl;
  }

  // op 28949: delete
  db->Delete(write_options, "key2402");

  // op 28950: get
  status = db->Get(read_options, "key9544", &value);
  if (status.ok()) {
    out << "GET key9544 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28951: put
  db->Put(write_options, "key0447", "value37765");

  // op 28952: put
  db->Put(write_options, "key5113", "value20762");

  // op 28953: put
  db->Put(write_options, "key7776", "value98843");

  // op 28954: delete_range
  status = db->DeleteRange(write_options, "key0610", "key5313");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 28955: delete
  db->Delete(write_options, "key1556");

  // op 28956: put
  db->Put(write_options, "key2634", "value76727");

  // op 28957: put
  db->Put(write_options, "key8095", "value99133");

  // op 28958: put
  db->Put(write_options, "key11277", "value22455");

  // op 28959: delete
  db->Delete(write_options, "key0358");

  // op 28960: put
  db->Put(write_options, "key9816", "value59275");

  // op 28961: delete
  db->Delete(write_options, "key6729");

  // op 28962: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5843", "key9562", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5843, key9562) size=" << scan_result.size() << std::endl;
  }

  // op 28963: delete
  db->Delete(write_options, "key1874");

  // op 28964: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0607", "key1678", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0607, key1678) size=" << scan_result.size() << std::endl;
  }

  // op 28965: get
  status = db->Get(read_options, "key7587", &value);
  if (status.ok()) {
    out << "GET key7587 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28966: put
  db->Put(write_options, "key0732", "value52676");

  // op 28967: delete
  db->Delete(write_options, "key5470");

  // op 28968: delete_range
  status = db->DeleteRange(write_options, "key8533", "key10576");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 28969: put
  db->Put(write_options, "key4196", "value82431");

  // op 28970: put
  db->Put(write_options, "key1529", "value32448");

  // op 28971: put
  db->Put(write_options, "key3584", "value94697");

  // op 28972: put
  db->Put(write_options, "key10891", "value111102");

  // op 28973: delete
  db->Delete(write_options, "key2840");

  // op 28974: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 28975: delete_range
  status = db->DeleteRange(write_options, "key6507", "key7909");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 28976: put
  db->Put(write_options, "key2086", "value114612");

  // op 28977: put
  db->Put(write_options, "key11299", "value116244");

  // op 28978: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1168", "key5857", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1168, key5857) size=" << scan_result.size() << std::endl;
  }

  // op 28979: delete
  db->Delete(write_options, "key1062");

  // op 28980: get
  status = db->Get(read_options, "key7990", &value);
  if (status.ok()) {
    out << "GET key7990 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28981: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3119", "key6748", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3119, key6748) size=" << scan_result.size() << std::endl;
  }

  // op 28982: get
  status = db->Get(read_options, "key6430", &value);
  if (status.ok()) {
    out << "GET key6430 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28983: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 28984: put
  db->Put(write_options, "key10969", "value21484");

  // op 28985: put
  db->Put(write_options, "key6298", "value104537");

  // op 28986: get
  status = db->Get(read_options, "key11434", &value);
  if (status.ok()) {
    out << "GET key11434 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28987: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 28988: put
  db->Put(write_options, "key3914", "value78119");

  // op 28989: put
  db->Put(write_options, "key6562", "value48275");

  // op 28990: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5738", "key8215", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5738, key8215) size=" << scan_result.size() << std::endl;
  }

  // op 28991: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 28992: get
  status = db->Get(read_options, "key2202", &value);
  if (status.ok()) {
    out << "GET key2202 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28993: get
  status = db->Get(read_options, "key1004", &value);
  if (status.ok()) {
    out << "GET key1004 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28994: get
  status = db->Get(read_options, "key7280", &value);
  if (status.ok()) {
    out << "GET key7280 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28995: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 28996: put
  db->Put(write_options, "key10685", "value98817");

  // op 28997: put
  db->Put(write_options, "key3905", "value70085");

  // op 28998: get
  status = db->Get(read_options, "key7544", &value);
  if (status.ok()) {
    out << "GET key7544 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 28999: put
  db->Put(write_options, "key10287", "value14696");

  // op 29000: get
  status = db->Get(read_options, "key11569", &value);
  if (status.ok()) {
    out << "GET key11569 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29001: put
  db->Put(write_options, "key1773", "value21773");

  // op 29002: put
  db->Put(write_options, "key11165", "value86252");

  // op 29003: put
  db->Put(write_options, "key0914", "value86153");

  // op 29004: get
  status = db->Get(read_options, "key5965", &value);
  if (status.ok()) {
    out << "GET key5965 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29005: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2271", "key8941", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2271, key8941) size=" << scan_result.size() << std::endl;
  }

  // op 29006: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0090", "key0453", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0090, key0453) size=" << scan_result.size() << std::endl;
  }

  // op 29007: put
  db->Put(write_options, "key6176", "value102499");

  // op 29008: delete_range
  status = db->DeleteRange(write_options, "key9851", "key10773");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29009: delete_range
  status = db->DeleteRange(write_options, "key0428", "key7211");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29010: delete_range
  status = db->DeleteRange(write_options, "key0321", "key4471");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29011: get
  status = db->Get(read_options, "key11025", &value);
  if (status.ok()) {
    out << "GET key11025 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29012: delete
  db->Delete(write_options, "key11502");

  // op 29013: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 29014: put
  db->Put(write_options, "key10571", "value19989");

  // op 29015: get
  status = db->Get(read_options, "key11412", &value);
  if (status.ok()) {
    out << "GET key11412 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29016: put
  db->Put(write_options, "key2364", "value59043");

  // op 29017: delete
  db->Delete(write_options, "key8619");

  // op 29018: delete_range
  status = db->DeleteRange(write_options, "key0384", "key7692");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29019: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7337", "key9084", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7337, key9084) size=" << scan_result.size() << std::endl;
  }

  // op 29020: put
  db->Put(write_options, "key1821", "value21781");

  // op 29021: put
  db->Put(write_options, "key5469", "value46185");

  // op 29022: put
  db->Put(write_options, "key6187", "value112546");

  // op 29023: put
  db->Put(write_options, "key8836", "value67841");

  // op 29024: put
  db->Put(write_options, "key1647", "value48012");

  // op 29025: get
  status = db->Get(read_options, "key6208", &value);
  if (status.ok()) {
    out << "GET key6208 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29026: get
  status = db->Get(read_options, "key3597", &value);
  if (status.ok()) {
    out << "GET key3597 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29027: put
  db->Put(write_options, "key3611", "value38924");

  // op 29028: get
  status = db->Get(read_options, "key2729", &value);
  if (status.ok()) {
    out << "GET key2729 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29029: delete
  db->Delete(write_options, "key0938");

  // op 29030: put
  db->Put(write_options, "key8444", "value20321");

  // op 29031: delete
  db->Delete(write_options, "key7301");

  // op 29032: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6093", "key6141", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6093, key6141) size=" << scan_result.size() << std::endl;
  }

  // op 29033: get
  status = db->Get(read_options, "key8284", &value);
  if (status.ok()) {
    out << "GET key8284 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29034: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 29035: put
  db->Put(write_options, "key8780", "value54241");

  // op 29036: get
  status = db->Get(read_options, "key9710", &value);
  if (status.ok()) {
    out << "GET key9710 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29037: put
  db->Put(write_options, "key9901", "value73442");

  // op 29038: delete_range
  status = db->DeleteRange(write_options, "key0663", "key6864");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29039: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 29040: put
  db->Put(write_options, "key9449", "value51740");

  // op 29041: get
  status = db->Get(read_options, "key3579", &value);
  if (status.ok()) {
    out << "GET key3579 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29042: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10836", "key11727", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10836, key11727) size=" << scan_result.size() << std::endl;
  }

  // op 29043: put
  db->Put(write_options, "key9411", "value117620");

  // op 29044: delete_range
  status = db->DeleteRange(write_options, "key3212", "key8028");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29045: delete
  db->Delete(write_options, "key8441");

  // op 29046: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6242", "key9125", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6242, key9125) size=" << scan_result.size() << std::endl;
  }

  // op 29047: get
  status = db->Get(read_options, "key5158", &value);
  if (status.ok()) {
    out << "GET key5158 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29048: put
  db->Put(write_options, "key1992", "value112670");

  // op 29049: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 29050: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0262", "key7829", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0262, key7829) size=" << scan_result.size() << std::endl;
  }

  // op 29051: put
  db->Put(write_options, "key10011", "value41968");

  // op 29052: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4712", "key7125", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4712, key7125) size=" << scan_result.size() << std::endl;
  }

  // op 29053: delete_range
  status = db->DeleteRange(write_options, "key5827", "key9676");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29054: get
  status = db->Get(read_options, "key9292", &value);
  if (status.ok()) {
    out << "GET key9292 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29055: delete_range
  status = db->DeleteRange(write_options, "key3311", "key5242");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29056: put
  db->Put(write_options, "key10404", "value56495");

  // op 29057: put
  db->Put(write_options, "key0560", "value21635");

  // op 29058: put
  db->Put(write_options, "key0665", "value104792");

  // op 29059: get
  status = db->Get(read_options, "key0992", &value);
  if (status.ok()) {
    out << "GET key0992 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29060: put
  db->Put(write_options, "key8902", "value2038");

  // op 29061: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 29062: delete
  db->Delete(write_options, "key0419");

  // op 29063: get
  status = db->Get(read_options, "key9033", &value);
  if (status.ok()) {
    out << "GET key9033 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29064: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1008", "key4351", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1008, key4351) size=" << scan_result.size() << std::endl;
  }

  // op 29065: put
  db->Put(write_options, "key6469", "value10862");

  // op 29066: delete_range
  status = db->DeleteRange(write_options, "key2926", "key6943");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29067: put
  db->Put(write_options, "key7919", "value12610");

  // op 29068: delete_range
  status = db->DeleteRange(write_options, "key8264", "key9984");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29069: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3309", "key5902", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3309, key5902) size=" << scan_result.size() << std::endl;
  }

  // op 29070: put
  db->Put(write_options, "key5621", "value35822");

  // op 29071: get
  status = db->Get(read_options, "key6195", &value);
  if (status.ok()) {
    out << "GET key6195 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29072: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 29073: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1678", "key7280", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1678, key7280) size=" << scan_result.size() << std::endl;
  }

  // op 29074: put
  db->Put(write_options, "key1252", "value0028");

  // op 29075: get
  status = db->Get(read_options, "key6503", &value);
  if (status.ok()) {
    out << "GET key6503 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29076: put
  db->Put(write_options, "key2409", "value72087");

  // op 29077: put
  db->Put(write_options, "key9534", "value33861");

  // op 29078: put
  db->Put(write_options, "key8875", "value37819");

  // op 29079: put
  db->Put(write_options, "key1660", "value26864");

  // op 29080: get
  status = db->Get(read_options, "key1531", &value);
  if (status.ok()) {
    out << "GET key1531 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29081: put
  db->Put(write_options, "key1737", "value66840");

  // op 29082: put
  db->Put(write_options, "key3094", "value57656");

  // op 29083: delete_range
  status = db->DeleteRange(write_options, "key2003", "key8048");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29084: get
  status = db->Get(read_options, "key11152", &value);
  if (status.ok()) {
    out << "GET key11152 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29085: get
  status = db->Get(read_options, "key3241", &value);
  if (status.ok()) {
    out << "GET key3241 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29086: put
  db->Put(write_options, "key7385", "value27624");

  // op 29087: delete_range
  status = db->DeleteRange(write_options, "key6683", "key8687");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29088: put
  db->Put(write_options, "key9523", "value90801");

  // op 29089: put
  db->Put(write_options, "key8587", "value0679");

  // op 29090: put
  db->Put(write_options, "key8984", "value20740");

  // op 29091: delete_range
  status = db->DeleteRange(write_options, "key1092", "key7041");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29092: put
  db->Put(write_options, "key5239", "value109155");

  // op 29093: delete
  db->Delete(write_options, "key4780");

  // op 29094: delete_range
  status = db->DeleteRange(write_options, "key5997", "key10735");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29095: delete_range
  status = db->DeleteRange(write_options, "key6272", "key8551");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29096: delete_range
  status = db->DeleteRange(write_options, "key4994", "key9398");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29097: put
  db->Put(write_options, "key2898", "value35277");

  // op 29098: get
  status = db->Get(read_options, "key9527", &value);
  if (status.ok()) {
    out << "GET key9527 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29099: get
  status = db->Get(read_options, "key7869", &value);
  if (status.ok()) {
    out << "GET key7869 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29100: put
  db->Put(write_options, "key1812", "value6605");

  // op 29101: get
  status = db->Get(read_options, "key5479", &value);
  if (status.ok()) {
    out << "GET key5479 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29102: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8582", "key9314", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8582, key9314) size=" << scan_result.size() << std::endl;
  }

  // op 29103: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0893", "key5852", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0893, key5852) size=" << scan_result.size() << std::endl;
  }

  // op 29104: put
  db->Put(write_options, "key4918", "value58743");

  // op 29105: get
  status = db->Get(read_options, "key10418", &value);
  if (status.ok()) {
    out << "GET key10418 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29106: delete
  db->Delete(write_options, "key3941");

  // op 29107: put
  db->Put(write_options, "key6514", "value70405");

  // op 29108: put
  db->Put(write_options, "key4916", "value56356");

  // op 29109: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4746", "key8097", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4746, key8097) size=" << scan_result.size() << std::endl;
  }

  // op 29110: put
  db->Put(write_options, "key3089", "value66158");

  // op 29111: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0129", "key0837", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0129, key0837) size=" << scan_result.size() << std::endl;
  }

  // op 29112: put
  db->Put(write_options, "key1842", "value34969");

  // op 29113: get
  status = db->Get(read_options, "key0519", &value);
  if (status.ok()) {
    out << "GET key0519 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29114: put
  db->Put(write_options, "key4723", "value62841");

  // op 29115: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 29116: put
  db->Put(write_options, "key6677", "value91278");

  // op 29117: delete_range
  status = db->DeleteRange(write_options, "key2091", "key3509");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29118: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8944", "key11715", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8944, key11715) size=" << scan_result.size() << std::endl;
  }

  // op 29119: delete_range
  status = db->DeleteRange(write_options, "key4873", "key6058");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29120: delete_range
  status = db->DeleteRange(write_options, "key3887", "key11197");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29121: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2863", "key4268", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2863, key4268) size=" << scan_result.size() << std::endl;
  }

  // op 29122: delete_range
  status = db->DeleteRange(write_options, "key2580", "key6585");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29123: delete
  db->Delete(write_options, "key10054");

  // op 29124: get
  status = db->Get(read_options, "key2372", &value);
  if (status.ok()) {
    out << "GET key2372 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29125: put
  db->Put(write_options, "key3969", "value28572");

  // op 29126: get
  status = db->Get(read_options, "key2111", &value);
  if (status.ok()) {
    out << "GET key2111 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29127: get
  status = db->Get(read_options, "key0639", &value);
  if (status.ok()) {
    out << "GET key0639 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29128: get
  status = db->Get(read_options, "key4178", &value);
  if (status.ok()) {
    out << "GET key4178 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29129: put
  db->Put(write_options, "key9981", "value19992");

  // op 29130: put
  db->Put(write_options, "key4966", "value87069");

  // op 29131: delete_range
  status = db->DeleteRange(write_options, "key5875", "key8772");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29132: put
  db->Put(write_options, "key5919", "value52537");

  // op 29133: delete
  db->Delete(write_options, "key5881");

  // op 29134: delete_range
  status = db->DeleteRange(write_options, "key6299", "key9201");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29135: put
  db->Put(write_options, "key5549", "value18961");

  // op 29136: delete
  db->Delete(write_options, "key7062");

  // op 29137: put
  db->Put(write_options, "key9590", "value40035");

  // op 29138: put
  db->Put(write_options, "key8126", "value103762");

  // op 29139: put
  db->Put(write_options, "key4825", "value81913");

  // op 29140: put
  db->Put(write_options, "key4267", "value53081");

  // op 29141: delete
  db->Delete(write_options, "key6038");

  // op 29142: put
  db->Put(write_options, "key5821", "value77090");

  // op 29143: get
  status = db->Get(read_options, "key8965", &value);
  if (status.ok()) {
    out << "GET key8965 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29144: delete
  db->Delete(write_options, "key3862");

  // op 29145: put
  db->Put(write_options, "key11031", "value8672");

  // op 29146: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 29147: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 29148: put
  db->Put(write_options, "key2763", "value55197");

  // op 29149: put
  db->Put(write_options, "key5055", "value34542");

  // op 29150: put
  db->Put(write_options, "key3305", "value33809");

  // op 29151: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0087", "key10825", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0087, key10825) size=" << scan_result.size() << std::endl;
  }

  // op 29152: put
  db->Put(write_options, "key5135", "value54675");

  // op 29153: put
  db->Put(write_options, "key2092", "value115725");

  // op 29154: put
  db->Put(write_options, "key8430", "value96109");

  // op 29155: delete_range
  status = db->DeleteRange(write_options, "key3887", "key9328");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29156: put
  db->Put(write_options, "key4401", "value37099");

  // op 29157: put
  db->Put(write_options, "key2834", "value102766");

  // op 29158: get
  status = db->Get(read_options, "key9647", &value);
  if (status.ok()) {
    out << "GET key9647 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29159: delete
  db->Delete(write_options, "key3137");

  // op 29160: delete
  db->Delete(write_options, "key9645");

  // op 29161: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6722", "key7120", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6722, key7120) size=" << scan_result.size() << std::endl;
  }

  // op 29162: get
  status = db->Get(read_options, "key4949", &value);
  if (status.ok()) {
    out << "GET key4949 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29163: delete
  db->Delete(write_options, "key11360");

  // op 29164: put
  db->Put(write_options, "key2848", "value101210");

  // op 29165: delete
  db->Delete(write_options, "key6025");

  // op 29166: get
  status = db->Get(read_options, "key0535", &value);
  if (status.ok()) {
    out << "GET key0535 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29167: get
  status = db->Get(read_options, "key8020", &value);
  if (status.ok()) {
    out << "GET key8020 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29168: delete
  db->Delete(write_options, "key8501");

  // op 29169: put
  db->Put(write_options, "key11711", "value40940");

  // op 29170: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7669", "key10672", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7669, key10672) size=" << scan_result.size() << std::endl;
  }

  // op 29171: get
  status = db->Get(read_options, "key2469", &value);
  if (status.ok()) {
    out << "GET key2469 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29172: put
  db->Put(write_options, "key7133", "value51452");

  // op 29173: delete
  db->Delete(write_options, "key0436");

  // op 29174: put
  db->Put(write_options, "key2582", "value40611");

  // op 29175: put
  db->Put(write_options, "key5486", "value58106");

  // op 29176: get
  status = db->Get(read_options, "key7746", &value);
  if (status.ok()) {
    out << "GET key7746 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29177: put
  db->Put(write_options, "key8652", "value33648");

  // op 29178: get
  status = db->Get(read_options, "key1398", &value);
  if (status.ok()) {
    out << "GET key1398 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29179: put
  db->Put(write_options, "key2173", "value10134");

  // op 29180: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 29181: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 29182: put
  db->Put(write_options, "key2353", "value64103");

  // op 29183: get
  status = db->Get(read_options, "key0706", &value);
  if (status.ok()) {
    out << "GET key0706 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29184: get
  status = db->Get(read_options, "key9913", &value);
  if (status.ok()) {
    out << "GET key9913 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29185: put
  db->Put(write_options, "key2782", "value60262");

  // op 29186: delete
  db->Delete(write_options, "key7321");

  // op 29187: put
  db->Put(write_options, "key4115", "value38861");

  // op 29188: get
  status = db->Get(read_options, "key5418", &value);
  if (status.ok()) {
    out << "GET key5418 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29189: get
  status = db->Get(read_options, "key0308", &value);
  if (status.ok()) {
    out << "GET key0308 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29190: delete
  db->Delete(write_options, "key1981");

  // op 29191: put
  db->Put(write_options, "key11141", "value108548");

  // op 29192: put
  db->Put(write_options, "key6456", "value72536");

  // op 29193: put
  db->Put(write_options, "key7410", "value40552");

  // op 29194: put
  db->Put(write_options, "key2894", "value27733");

  // op 29195: put
  db->Put(write_options, "key9795", "value2125");

  // op 29196: get
  status = db->Get(read_options, "key0734", &value);
  if (status.ok()) {
    out << "GET key0734 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29197: delete_range
  status = db->DeleteRange(write_options, "key1867", "key10207");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29198: put
  db->Put(write_options, "key6113", "value103966");

  // op 29199: put
  db->Put(write_options, "key4069", "value0721");

  // op 29200: put
  db->Put(write_options, "key3565", "value68536");

  // op 29201: put
  db->Put(write_options, "key0437", "value82724");

  // op 29202: put
  db->Put(write_options, "key5040", "value117280");

  // op 29203: delete_range
  status = db->DeleteRange(write_options, "key4501", "key7107");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29204: put
  db->Put(write_options, "key1437", "value68601");

  // op 29205: put
  db->Put(write_options, "key8244", "value78532");

  // op 29206: put
  db->Put(write_options, "key3439", "value33085");

  // op 29207: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6080", "key6763", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6080, key6763) size=" << scan_result.size() << std::endl;
  }

  // op 29208: delete
  db->Delete(write_options, "key9355");

  // op 29209: delete_range
  status = db->DeleteRange(write_options, "key8456", "key11175");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29210: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3474", "key5513", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3474, key5513) size=" << scan_result.size() << std::endl;
  }

  // op 29211: put
  db->Put(write_options, "key10811", "value11747");

  // op 29212: put
  db->Put(write_options, "key0641", "value3704");

  // op 29213: put
  db->Put(write_options, "key8000", "value108844");

  // op 29214: put
  db->Put(write_options, "key2808", "value23294");

  // op 29215: put
  db->Put(write_options, "key1264", "value18825");

  // op 29216: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 29217: put
  db->Put(write_options, "key1234", "value30099");

  // op 29218: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8024", "key10267", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8024, key10267) size=" << scan_result.size() << std::endl;
  }

  // op 29219: get
  status = db->Get(read_options, "key9952", &value);
  if (status.ok()) {
    out << "GET key9952 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29220: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0043", "key5456", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0043, key5456) size=" << scan_result.size() << std::endl;
  }

  // op 29221: get
  status = db->Get(read_options, "key8371", &value);
  if (status.ok()) {
    out << "GET key8371 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29222: get
  status = db->Get(read_options, "key9160", &value);
  if (status.ok()) {
    out << "GET key9160 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29223: put
  db->Put(write_options, "key6428", "value113233");

  // op 29224: put
  db->Put(write_options, "key7867", "value70787");

  // op 29225: put
  db->Put(write_options, "key2296", "value25537");

  // op 29226: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2256", "key5409", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2256, key5409) size=" << scan_result.size() << std::endl;
  }

  // op 29227: delete_range
  status = db->DeleteRange(write_options, "key7111", "key7275");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29228: get
  status = db->Get(read_options, "key7644", &value);
  if (status.ok()) {
    out << "GET key7644 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29229: put
  db->Put(write_options, "key1095", "value70935");

  // op 29230: get
  status = db->Get(read_options, "key3516", &value);
  if (status.ok()) {
    out << "GET key3516 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29231: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 29232: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4049", "key5136", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4049, key5136) size=" << scan_result.size() << std::endl;
  }

  // op 29233: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2686", "key5509", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2686, key5509) size=" << scan_result.size() << std::endl;
  }

  // op 29234: put
  db->Put(write_options, "key4998", "value41099");

  // op 29235: put
  db->Put(write_options, "key2368", "value96543");

  // op 29236: delete_range
  status = db->DeleteRange(write_options, "key1923", "key10257");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29237: delete_range
  status = db->DeleteRange(write_options, "key1674", "key4398");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29238: get
  status = db->Get(read_options, "key8168", &value);
  if (status.ok()) {
    out << "GET key8168 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29239: delete_range
  status = db->DeleteRange(write_options, "key7989", "key10952");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29240: delete_range
  status = db->DeleteRange(write_options, "key10607", "key10626");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29241: get
  status = db->Get(read_options, "key7515", &value);
  if (status.ok()) {
    out << "GET key7515 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29242: get
  status = db->Get(read_options, "key10843", &value);
  if (status.ok()) {
    out << "GET key10843 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29243: get
  status = db->Get(read_options, "key7776", &value);
  if (status.ok()) {
    out << "GET key7776 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29244: delete_range
  status = db->DeleteRange(write_options, "key8473", "key9470");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29245: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3687", "key6711", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3687, key6711) size=" << scan_result.size() << std::endl;
  }

  // op 29246: put
  db->Put(write_options, "key10859", "value29174");

  // op 29247: put
  db->Put(write_options, "key5730", "value3052");

  // op 29248: put
  db->Put(write_options, "key4667", "value60510");

  // op 29249: put
  db->Put(write_options, "key8321", "value22859");

  // op 29250: put
  db->Put(write_options, "key1334", "value4609");

  // op 29251: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6726", "key8412", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6726, key8412) size=" << scan_result.size() << std::endl;
  }

  // op 29252: delete
  db->Delete(write_options, "key5928");

  // op 29253: put
  db->Put(write_options, "key10976", "value1889");

  // op 29254: delete
  db->Delete(write_options, "key5597");

  // op 29255: put
  db->Put(write_options, "key11071", "value117706");

  // op 29256: put
  db->Put(write_options, "key1729", "value90018");

  // op 29257: put
  db->Put(write_options, "key10359", "value58453");

  // op 29258: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0609", "key8931", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0609, key8931) size=" << scan_result.size() << std::endl;
  }

  // op 29259: put
  db->Put(write_options, "key1549", "value96537");

  // op 29260: delete
  db->Delete(write_options, "key9629");

  // op 29261: get
  status = db->Get(read_options, "key9197", &value);
  if (status.ok()) {
    out << "GET key9197 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29262: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3726", "key4518", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3726, key4518) size=" << scan_result.size() << std::endl;
  }

  // op 29263: put
  db->Put(write_options, "key0269", "value8587");

  // op 29264: delete
  db->Delete(write_options, "key6710");

  // op 29265: get
  status = db->Get(read_options, "key0852", &value);
  if (status.ok()) {
    out << "GET key0852 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29266: put
  db->Put(write_options, "key8867", "value41879");

  // op 29267: put
  db->Put(write_options, "key4826", "value58898");

  // op 29268: delete
  db->Delete(write_options, "key7686");

  // op 29269: put
  db->Put(write_options, "key1747", "value83971");

  // op 29270: get
  status = db->Get(read_options, "key4935", &value);
  if (status.ok()) {
    out << "GET key4935 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29271: delete
  db->Delete(write_options, "key5321");

  // op 29272: get
  status = db->Get(read_options, "key0106", &value);
  if (status.ok()) {
    out << "GET key0106 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29273: delete_range
  status = db->DeleteRange(write_options, "key2689", "key9365");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29274: put
  db->Put(write_options, "key10028", "value82843");

  // op 29275: delete_range
  status = db->DeleteRange(write_options, "key6029", "key7621");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29276: delete
  db->Delete(write_options, "key3266");

  // op 29277: put
  db->Put(write_options, "key8031", "value78074");

  // op 29278: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 29279: delete
  db->Delete(write_options, "key2747");

  // op 29280: put
  db->Put(write_options, "key8267", "value87584");

  // op 29281: put
  db->Put(write_options, "key7531", "value114557");

  // op 29282: put
  db->Put(write_options, "key0151", "value21827");

  // op 29283: get
  status = db->Get(read_options, "key1358", &value);
  if (status.ok()) {
    out << "GET key1358 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29284: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4751", "key11470", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4751, key11470) size=" << scan_result.size() << std::endl;
  }

  // op 29285: put
  db->Put(write_options, "key11682", "value24206");

  // op 29286: delete_range
  status = db->DeleteRange(write_options, "key3800", "key8989");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29287: delete_range
  status = db->DeleteRange(write_options, "key2570", "key2974");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29288: put
  db->Put(write_options, "key1205", "value20377");

  // op 29289: put
  db->Put(write_options, "key5658", "value10852");

  // op 29290: get
  status = db->Get(read_options, "key3250", &value);
  if (status.ok()) {
    out << "GET key3250 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29291: put
  db->Put(write_options, "key9943", "value85787");

  // op 29292: delete_range
  status = db->DeleteRange(write_options, "key0686", "key2378");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29293: delete
  db->Delete(write_options, "key1737");

  // op 29294: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0652", "key10393", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0652, key10393) size=" << scan_result.size() << std::endl;
  }

  // op 29295: put
  db->Put(write_options, "key5810", "value8507");

  // op 29296: put
  db->Put(write_options, "key4365", "value17346");

  // op 29297: delete_range
  status = db->DeleteRange(write_options, "key4111", "key10372");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29298: get
  status = db->Get(read_options, "key0984", &value);
  if (status.ok()) {
    out << "GET key0984 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29299: get
  status = db->Get(read_options, "key7242", &value);
  if (status.ok()) {
    out << "GET key7242 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29300: put
  db->Put(write_options, "key10163", "value86861");

  // op 29301: get
  status = db->Get(read_options, "key7126", &value);
  if (status.ok()) {
    out << "GET key7126 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29302: put
  db->Put(write_options, "key5291", "value77084");

  // op 29303: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6104", "key11118", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6104, key11118) size=" << scan_result.size() << std::endl;
  }

  // op 29304: delete
  db->Delete(write_options, "key11350");

  // op 29305: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1658", "key3145", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1658, key3145) size=" << scan_result.size() << std::endl;
  }

  // op 29306: put
  db->Put(write_options, "key0342", "value45660");

  // op 29307: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8877", "key10367", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8877, key10367) size=" << scan_result.size() << std::endl;
  }

  // op 29308: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4688", "key8242", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4688, key8242) size=" << scan_result.size() << std::endl;
  }

  // op 29309: put
  db->Put(write_options, "key2122", "value68806");

  // op 29310: put
  db->Put(write_options, "key11544", "value37329");

  // op 29311: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1456", "key3723", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1456, key3723) size=" << scan_result.size() << std::endl;
  }

  // op 29312: put
  db->Put(write_options, "key5495", "value50221");

  // op 29313: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3248", "key8400", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3248, key8400) size=" << scan_result.size() << std::endl;
  }

  // op 29314: get
  status = db->Get(read_options, "key3709", &value);
  if (status.ok()) {
    out << "GET key3709 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29315: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2472", "key3793", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2472, key3793) size=" << scan_result.size() << std::endl;
  }

  // op 29316: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4913", "key9008", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4913, key9008) size=" << scan_result.size() << std::endl;
  }

  // op 29317: put
  db->Put(write_options, "key5716", "value22817");

  // op 29318: put
  db->Put(write_options, "key7573", "value3052");

  // op 29319: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7895", "key10023", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7895, key10023) size=" << scan_result.size() << std::endl;
  }

  // op 29320: put
  db->Put(write_options, "key11592", "value73161");

  // op 29321: get
  status = db->Get(read_options, "key1992", &value);
  if (status.ok()) {
    out << "GET key1992 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29322: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 29323: delete
  db->Delete(write_options, "key1589");

  // op 29324: put
  db->Put(write_options, "key1628", "value96569");

  // op 29325: delete_range
  status = db->DeleteRange(write_options, "key8139", "key10308");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29326: get
  status = db->Get(read_options, "key8637", &value);
  if (status.ok()) {
    out << "GET key8637 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29327: put
  db->Put(write_options, "key2353", "value36066");

  // op 29328: get
  status = db->Get(read_options, "key2691", &value);
  if (status.ok()) {
    out << "GET key2691 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29329: delete_range
  status = db->DeleteRange(write_options, "key5915", "key11139");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29330: get
  status = db->Get(read_options, "key8788", &value);
  if (status.ok()) {
    out << "GET key8788 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29331: put
  db->Put(write_options, "key3716", "value112477");

  // op 29332: get
  status = db->Get(read_options, "key7578", &value);
  if (status.ok()) {
    out << "GET key7578 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29333: get
  status = db->Get(read_options, "key6671", &value);
  if (status.ok()) {
    out << "GET key6671 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29334: put
  db->Put(write_options, "key2226", "value46448");

  // op 29335: get
  status = db->Get(read_options, "key5103", &value);
  if (status.ok()) {
    out << "GET key5103 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29336: put
  db->Put(write_options, "key11509", "value40628");

  // op 29337: put
  db->Put(write_options, "key4929", "value107034");

  // op 29338: delete
  db->Delete(write_options, "key2748");

  // op 29339: put
  db->Put(write_options, "key11408", "value116767");

  // op 29340: put
  db->Put(write_options, "key4507", "value25171");

  // op 29341: put
  db->Put(write_options, "key3495", "value9241");

  // op 29342: put
  db->Put(write_options, "key7805", "value104736");

  // op 29343: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 29344: put
  db->Put(write_options, "key9621", "value59071");

  // op 29345: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10408", "key10777", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10408, key10777) size=" << scan_result.size() << std::endl;
  }

  // op 29346: put
  db->Put(write_options, "key4049", "value43508");

  // op 29347: get
  status = db->Get(read_options, "key8500", &value);
  if (status.ok()) {
    out << "GET key8500 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29348: delete_range
  status = db->DeleteRange(write_options, "key5407", "key10266");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29349: delete_range
  status = db->DeleteRange(write_options, "key6256", "key8185");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29350: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 29351: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 29352: put
  db->Put(write_options, "key6703", "value54385");

  // op 29353: delete
  db->Delete(write_options, "key4596");

  // op 29354: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4740", "key10172", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4740, key10172) size=" << scan_result.size() << std::endl;
  }

  // op 29355: put
  db->Put(write_options, "key7055", "value38162");

  // op 29356: get
  status = db->Get(read_options, "key3089", &value);
  if (status.ok()) {
    out << "GET key3089 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29357: delete_range
  status = db->DeleteRange(write_options, "key6417", "key11473");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29358: get
  status = db->Get(read_options, "key3978", &value);
  if (status.ok()) {
    out << "GET key3978 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29359: put
  db->Put(write_options, "key0363", "value71095");

  // op 29360: put
  db->Put(write_options, "key4854", "value4731");

  // op 29361: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1939", "key9092", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1939, key9092) size=" << scan_result.size() << std::endl;
  }

  // op 29362: get
  status = db->Get(read_options, "key2463", &value);
  if (status.ok()) {
    out << "GET key2463 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29363: put
  db->Put(write_options, "key5652", "value39413");

  // op 29364: put
  db->Put(write_options, "key7317", "value35399");

  // op 29365: put
  db->Put(write_options, "key8141", "value42817");

  // op 29366: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0870", "key3928", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0870, key3928) size=" << scan_result.size() << std::endl;
  }

  // op 29367: get
  status = db->Get(read_options, "key5577", &value);
  if (status.ok()) {
    out << "GET key5577 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29368: put
  db->Put(write_options, "key6211", "value20447");

  // op 29369: get
  status = db->Get(read_options, "key6205", &value);
  if (status.ok()) {
    out << "GET key6205 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29370: delete_range
  status = db->DeleteRange(write_options, "key0911", "key7214");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29371: put
  db->Put(write_options, "key11601", "value52508");

  // op 29372: get
  status = db->Get(read_options, "key5145", &value);
  if (status.ok()) {
    out << "GET key5145 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29373: delete_range
  status = db->DeleteRange(write_options, "key7827", "key9118");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29374: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5833", "key10474", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5833, key10474) size=" << scan_result.size() << std::endl;
  }

  // op 29375: get
  status = db->Get(read_options, "key4355", &value);
  if (status.ok()) {
    out << "GET key4355 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29376: put
  db->Put(write_options, "key4780", "value92545");

  // op 29377: put
  db->Put(write_options, "key9237", "value1288");

  // op 29378: put
  db->Put(write_options, "key7609", "value33143");

  // op 29379: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 29380: get
  status = db->Get(read_options, "key1863", &value);
  if (status.ok()) {
    out << "GET key1863 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29381: delete
  db->Delete(write_options, "key4566");

  // op 29382: get
  status = db->Get(read_options, "key6593", &value);
  if (status.ok()) {
    out << "GET key6593 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29383: put
  db->Put(write_options, "key2231", "value116744");

  // op 29384: get
  status = db->Get(read_options, "key11364", &value);
  if (status.ok()) {
    out << "GET key11364 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29385: put
  db->Put(write_options, "key1576", "value84951");

  // op 29386: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2697", "key10586", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2697, key10586) size=" << scan_result.size() << std::endl;
  }

  // op 29387: put
  db->Put(write_options, "key0791", "value89007");

  // op 29388: put
  db->Put(write_options, "key1519", "value3705");

  // op 29389: put
  db->Put(write_options, "key6422", "value71011");

  // op 29390: put
  db->Put(write_options, "key11109", "value37174");

  // op 29391: put
  db->Put(write_options, "key3888", "value44576");

  // op 29392: delete
  db->Delete(write_options, "key5384");

  // op 29393: put
  db->Put(write_options, "key0449", "value66855");

  // op 29394: put
  db->Put(write_options, "key10249", "value42330");

  // op 29395: put
  db->Put(write_options, "key8557", "value44083");

  // op 29396: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5634", "key6884", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5634, key6884) size=" << scan_result.size() << std::endl;
  }

  // op 29397: put
  db->Put(write_options, "key11063", "value57215");

  // op 29398: delete_range
  status = db->DeleteRange(write_options, "key7700", "key9799");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29399: put
  db->Put(write_options, "key2430", "value46479");

  // op 29400: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1989", "key5355", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1989, key5355) size=" << scan_result.size() << std::endl;
  }

  // op 29401: put
  db->Put(write_options, "key6861", "value23715");

  // op 29402: put
  db->Put(write_options, "key2754", "value113114");

  // op 29403: put
  db->Put(write_options, "key9920", "value38245");

  // op 29404: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3604", "key9022", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3604, key9022) size=" << scan_result.size() << std::endl;
  }

  // op 29405: delete
  db->Delete(write_options, "key10356");

  // op 29406: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7199", "key8354", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7199, key8354) size=" << scan_result.size() << std::endl;
  }

  // op 29407: delete
  db->Delete(write_options, "key4089");

  // op 29408: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3903", "key5512", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3903, key5512) size=" << scan_result.size() << std::endl;
  }

  // op 29409: delete_range
  status = db->DeleteRange(write_options, "key8595", "key8965");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29410: put
  db->Put(write_options, "key0144", "value38680");

  // op 29411: delete_range
  status = db->DeleteRange(write_options, "key4578", "key11093");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29412: delete_range
  status = db->DeleteRange(write_options, "key7871", "key9094");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29413: put
  db->Put(write_options, "key9215", "value95888");

  // op 29414: put
  db->Put(write_options, "key11163", "value50622");

  // op 29415: put
  db->Put(write_options, "key5172", "value45743");

  // op 29416: get
  status = db->Get(read_options, "key3446", &value);
  if (status.ok()) {
    out << "GET key3446 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29417: put
  db->Put(write_options, "key1132", "value73118");

  // op 29418: put
  db->Put(write_options, "key1728", "value70783");

  // op 29419: get
  status = db->Get(read_options, "key6538", &value);
  if (status.ok()) {
    out << "GET key6538 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29420: delete
  db->Delete(write_options, "key3212");

  // op 29421: get
  status = db->Get(read_options, "key6569", &value);
  if (status.ok()) {
    out << "GET key6569 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29422: put
  db->Put(write_options, "key10214", "value89781");

  // op 29423: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2853", "key8429", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2853, key8429) size=" << scan_result.size() << std::endl;
  }

  // op 29424: delete
  db->Delete(write_options, "key3698");

  // op 29425: delete_range
  status = db->DeleteRange(write_options, "key0191", "key3854");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29426: delete
  db->Delete(write_options, "key7402");

  // op 29427: put
  db->Put(write_options, "key5670", "value109795");

  // op 29428: get
  status = db->Get(read_options, "key11214", &value);
  if (status.ok()) {
    out << "GET key11214 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29429: delete
  db->Delete(write_options, "key8046");

  // op 29430: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2374", "key7042", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2374, key7042) size=" << scan_result.size() << std::endl;
  }

  // op 29431: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 29432: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4313", "key9958", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4313, key9958) size=" << scan_result.size() << std::endl;
  }

  // op 29433: get
  status = db->Get(read_options, "key5148", &value);
  if (status.ok()) {
    out << "GET key5148 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29434: put
  db->Put(write_options, "key4724", "value54347");

  // op 29435: delete
  db->Delete(write_options, "key7525");

  // op 29436: delete_range
  status = db->DeleteRange(write_options, "key6369", "key10256");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29437: scan
  scan_result.clear();
  status = db->Scan(read_options, "key11489", "key11601", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key11489, key11601) size=" << scan_result.size() << std::endl;
  }

  // op 29438: delete_range
  status = db->DeleteRange(write_options, "key1954", "key10100");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29439: get
  status = db->Get(read_options, "key8678", &value);
  if (status.ok()) {
    out << "GET key8678 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29440: get
  status = db->Get(read_options, "key1109", &value);
  if (status.ok()) {
    out << "GET key1109 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29441: put
  db->Put(write_options, "key2624", "value68858");

  // op 29442: put
  db->Put(write_options, "key10152", "value42910");

  // op 29443: put
  db->Put(write_options, "key2203", "value0355");

  // op 29444: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 29445: delete
  db->Delete(write_options, "key7963");

  // op 29446: put
  db->Put(write_options, "key3974", "value32929");

  // op 29447: delete_range
  status = db->DeleteRange(write_options, "key5481", "key8945");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29448: get
  status = db->Get(read_options, "key0241", &value);
  if (status.ok()) {
    out << "GET key0241 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29449: delete
  db->Delete(write_options, "key7578");

  // op 29450: put
  db->Put(write_options, "key9807", "value106450");

  // op 29451: get
  status = db->Get(read_options, "key1918", &value);
  if (status.ok()) {
    out << "GET key1918 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29452: put
  db->Put(write_options, "key6327", "value6110");

  // op 29453: get
  status = db->Get(read_options, "key1276", &value);
  if (status.ok()) {
    out << "GET key1276 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29454: get
  status = db->Get(read_options, "key10767", &value);
  if (status.ok()) {
    out << "GET key10767 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29455: get
  status = db->Get(read_options, "key7583", &value);
  if (status.ok()) {
    out << "GET key7583 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29456: put
  db->Put(write_options, "key6848", "value23024");

  // op 29457: get
  status = db->Get(read_options, "key2301", &value);
  if (status.ok()) {
    out << "GET key2301 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29458: delete_range
  status = db->DeleteRange(write_options, "key5615", "key10838");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29459: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10071", "key11705", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10071, key11705) size=" << scan_result.size() << std::endl;
  }

  // op 29460: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4039", "key6889", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4039, key6889) size=" << scan_result.size() << std::endl;
  }

  // op 29461: put
  db->Put(write_options, "key4310", "value90312");

  // op 29462: delete_range
  status = db->DeleteRange(write_options, "key4999", "key11723");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29463: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8230", "key8796", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8230, key8796) size=" << scan_result.size() << std::endl;
  }

  // op 29464: put
  db->Put(write_options, "key4545", "value68299");

  // op 29465: put
  db->Put(write_options, "key6055", "value87226");

  // op 29466: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1460", "key9372", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1460, key9372) size=" << scan_result.size() << std::endl;
  }

  // op 29467: put
  db->Put(write_options, "key9695", "value90202");

  // op 29468: delete_range
  status = db->DeleteRange(write_options, "key2908", "key8897");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29469: delete_range
  status = db->DeleteRange(write_options, "key0307", "key10270");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29470: delete
  db->Delete(write_options, "key3404");

  // op 29471: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2259", "key9442", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2259, key9442) size=" << scan_result.size() << std::endl;
  }

  // op 29472: put
  db->Put(write_options, "key11439", "value37874");

  // op 29473: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0673", "key9167", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0673, key9167) size=" << scan_result.size() << std::endl;
  }

  // op 29474: put
  db->Put(write_options, "key10385", "value23825");

  // op 29475: get
  status = db->Get(read_options, "key7538", &value);
  if (status.ok()) {
    out << "GET key7538 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29476: put
  db->Put(write_options, "key1717", "value71479");

  // op 29477: put
  db->Put(write_options, "key4163", "value82608");

  // op 29478: delete_range
  status = db->DeleteRange(write_options, "key7696", "key8798");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29479: get
  status = db->Get(read_options, "key6316", &value);
  if (status.ok()) {
    out << "GET key6316 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29480: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9460", "key9487", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9460, key9487) size=" << scan_result.size() << std::endl;
  }

  // op 29481: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 29482: put
  db->Put(write_options, "key7690", "value34746");

  // op 29483: put
  db->Put(write_options, "key6762", "value22535");

  // op 29484: put
  db->Put(write_options, "key7572", "value94765");

  // op 29485: put
  db->Put(write_options, "key0939", "value35220");

  // op 29486: delete_range
  status = db->DeleteRange(write_options, "key3885", "key6611");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29487: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0839", "key10461", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0839, key10461) size=" << scan_result.size() << std::endl;
  }

  // op 29488: get
  status = db->Get(read_options, "key9058", &value);
  if (status.ok()) {
    out << "GET key9058 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29489: put
  db->Put(write_options, "key8982", "value21117");

  // op 29490: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1719", "key5936", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1719, key5936) size=" << scan_result.size() << std::endl;
  }

  // op 29491: get
  status = db->Get(read_options, "key5749", &value);
  if (status.ok()) {
    out << "GET key5749 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29492: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0386", "key5935", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0386, key5935) size=" << scan_result.size() << std::endl;
  }

  // op 29493: put
  db->Put(write_options, "key6204", "value38010");

  // op 29494: put
  db->Put(write_options, "key7107", "value98692");

  // op 29495: get
  status = db->Get(read_options, "key0902", &value);
  if (status.ok()) {
    out << "GET key0902 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29496: put
  db->Put(write_options, "key8342", "value78106");

  // op 29497: get
  status = db->Get(read_options, "key4093", &value);
  if (status.ok()) {
    out << "GET key4093 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29498: delete_range
  status = db->DeleteRange(write_options, "key2315", "key5571");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29499: put
  db->Put(write_options, "key7500", "value49385");

  // op 29500: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 29501: put
  db->Put(write_options, "key7547", "value71570");

  // op 29502: put
  db->Put(write_options, "key9749", "value41545");

  // op 29503: delete_range
  status = db->DeleteRange(write_options, "key1063", "key5896");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29504: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 29505: put
  db->Put(write_options, "key2458", "value52376");

  // op 29506: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0605", "key9693", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0605, key9693) size=" << scan_result.size() << std::endl;
  }

  // op 29507: put
  db->Put(write_options, "key4083", "value30891");

  // op 29508: delete
  db->Delete(write_options, "key3325");

  // op 29509: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0042", "key4503", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0042, key4503) size=" << scan_result.size() << std::endl;
  }

  // op 29510: delete
  db->Delete(write_options, "key7061");

  // op 29511: get
  status = db->Get(read_options, "key10990", &value);
  if (status.ok()) {
    out << "GET key10990 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29512: put
  db->Put(write_options, "key1353", "value7371");

  // op 29513: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7464", "key7528", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7464, key7528) size=" << scan_result.size() << std::endl;
  }

  // op 29514: get
  status = db->Get(read_options, "key7187", &value);
  if (status.ok()) {
    out << "GET key7187 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29515: put
  db->Put(write_options, "key6228", "value22767");

  // op 29516: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2966", "key5628", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2966, key5628) size=" << scan_result.size() << std::endl;
  }

  // op 29517: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 29518: delete
  db->Delete(write_options, "key3685");

  // op 29519: get
  status = db->Get(read_options, "key9339", &value);
  if (status.ok()) {
    out << "GET key9339 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29520: delete
  db->Delete(write_options, "key2004");

  // op 29521: get
  status = db->Get(read_options, "key7546", &value);
  if (status.ok()) {
    out << "GET key7546 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29522: get
  status = db->Get(read_options, "key4497", &value);
  if (status.ok()) {
    out << "GET key4497 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29523: put
  db->Put(write_options, "key0011", "value14247");

  // op 29524: put
  db->Put(write_options, "key5857", "value96297");

  // op 29525: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2935", "key10390", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2935, key10390) size=" << scan_result.size() << std::endl;
  }

  // op 29526: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5860", "key9064", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5860, key9064) size=" << scan_result.size() << std::endl;
  }

  // op 29527: put
  db->Put(write_options, "key8746", "value12279");

  // op 29528: put
  db->Put(write_options, "key9054", "value33971");

  // op 29529: get
  status = db->Get(read_options, "key4117", &value);
  if (status.ok()) {
    out << "GET key4117 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29530: delete_range
  status = db->DeleteRange(write_options, "key1395", "key2699");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29531: delete_range
  status = db->DeleteRange(write_options, "key2506", "key4520");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29532: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 29533: put
  db->Put(write_options, "key8262", "value6684");

  // op 29534: put
  db->Put(write_options, "key11448", "value117501");

  // op 29535: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3880", "key5891", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3880, key5891) size=" << scan_result.size() << std::endl;
  }

  // op 29536: put
  db->Put(write_options, "key8287", "value40149");

  // op 29537: get
  status = db->Get(read_options, "key7975", &value);
  if (status.ok()) {
    out << "GET key7975 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29538: put
  db->Put(write_options, "key6870", "value61336");

  // op 29539: put
  db->Put(write_options, "key10888", "value113687");

  // op 29540: get
  status = db->Get(read_options, "key3437", &value);
  if (status.ok()) {
    out << "GET key3437 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29541: delete
  db->Delete(write_options, "key6845");

  // op 29542: delete
  db->Delete(write_options, "key6054");

  // op 29543: put
  db->Put(write_options, "key4739", "value109934");

  // op 29544: delete_range
  status = db->DeleteRange(write_options, "key0240", "key6824");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29545: put
  db->Put(write_options, "key1761", "value32025");

  // op 29546: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2289", "key2643", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2289, key2643) size=" << scan_result.size() << std::endl;
  }

  // op 29547: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0957", "key7545", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0957, key7545) size=" << scan_result.size() << std::endl;
  }

  // op 29548: get
  status = db->Get(read_options, "key11080", &value);
  if (status.ok()) {
    out << "GET key11080 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29549: put
  db->Put(write_options, "key4879", "value33260");

  // op 29550: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4367", "key9435", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4367, key9435) size=" << scan_result.size() << std::endl;
  }

  // op 29551: delete_range
  status = db->DeleteRange(write_options, "key0942", "key6116");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29552: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7676", "key10384", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7676, key10384) size=" << scan_result.size() << std::endl;
  }

  // op 29553: put
  db->Put(write_options, "key8530", "value56673");

  // op 29554: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8280", "key11167", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8280, key11167) size=" << scan_result.size() << std::endl;
  }

  // op 29555: put
  db->Put(write_options, "key1661", "value33372");

  // op 29556: put
  db->Put(write_options, "key4860", "value66377");

  // op 29557: put
  db->Put(write_options, "key3591", "value89370");

  // op 29558: delete_range
  status = db->DeleteRange(write_options, "key5075", "key6418");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29559: get
  status = db->Get(read_options, "key0220", &value);
  if (status.ok()) {
    out << "GET key0220 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29560: get
  status = db->Get(read_options, "key1203", &value);
  if (status.ok()) {
    out << "GET key1203 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29561: delete
  db->Delete(write_options, "key5479");

  // op 29562: delete
  db->Delete(write_options, "key1378");

  // op 29563: put
  db->Put(write_options, "key11231", "value78822");

  // op 29564: put
  db->Put(write_options, "key6543", "value43792");

  // op 29565: get
  status = db->Get(read_options, "key2171", &value);
  if (status.ok()) {
    out << "GET key2171 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29566: put
  db->Put(write_options, "key6224", "value89881");

  // op 29567: put
  db->Put(write_options, "key5270", "value88988");

  // op 29568: get
  status = db->Get(read_options, "key0762", &value);
  if (status.ok()) {
    out << "GET key0762 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29569: put
  db->Put(write_options, "key6160", "value45360");

  // op 29570: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5370", "key6456", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5370, key6456) size=" << scan_result.size() << std::endl;
  }

  // op 29571: get
  status = db->Get(read_options, "key7852", &value);
  if (status.ok()) {
    out << "GET key7852 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29572: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3700", "key9733", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3700, key9733) size=" << scan_result.size() << std::endl;
  }

  // op 29573: put
  db->Put(write_options, "key6417", "value99618");

  // op 29574: get
  status = db->Get(read_options, "key9564", &value);
  if (status.ok()) {
    out << "GET key9564 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29575: put
  db->Put(write_options, "key4043", "value117522");

  // op 29576: delete
  db->Delete(write_options, "key1533");

  // op 29577: put
  db->Put(write_options, "key6987", "value33033");

  // op 29578: get
  status = db->Get(read_options, "key1742", &value);
  if (status.ok()) {
    out << "GET key1742 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29579: delete_range
  status = db->DeleteRange(write_options, "key2733", "key11531");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29580: put
  db->Put(write_options, "key6146", "value17959");

  // op 29581: put
  db->Put(write_options, "key9697", "value89153");

  // op 29582: get
  status = db->Get(read_options, "key5907", &value);
  if (status.ok()) {
    out << "GET key5907 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29583: delete_range
  status = db->DeleteRange(write_options, "key1028", "key11214");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29584: delete
  db->Delete(write_options, "key3739");

  // op 29585: delete
  db->Delete(write_options, "key1477");

  // op 29586: put
  db->Put(write_options, "key5616", "value15676");

  // op 29587: put
  db->Put(write_options, "key6506", "value95429");

  // op 29588: get
  status = db->Get(read_options, "key4911", &value);
  if (status.ok()) {
    out << "GET key4911 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29589: put
  db->Put(write_options, "key10821", "value61155");

  // op 29590: delete_range
  status = db->DeleteRange(write_options, "key0302", "key5642");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29591: put
  db->Put(write_options, "key5310", "value12460");

  // op 29592: put
  db->Put(write_options, "key7413", "value6780");

  // op 29593: put
  db->Put(write_options, "key2035", "value52762");

  // op 29594: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 29595: put
  db->Put(write_options, "key11536", "value51043");

  // op 29596: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0561", "key8845", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0561, key8845) size=" << scan_result.size() << std::endl;
  }

  // op 29597: delete
  db->Delete(write_options, "key3996");

  // op 29598: get
  status = db->Get(read_options, "key6588", &value);
  if (status.ok()) {
    out << "GET key6588 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29599: get
  status = db->Get(read_options, "key2000", &value);
  if (status.ok()) {
    out << "GET key2000 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29600: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0255", "key10330", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0255, key10330) size=" << scan_result.size() << std::endl;
  }

  // op 29601: put
  db->Put(write_options, "key9739", "value105072");

  // op 29602: get
  status = db->Get(read_options, "key1625", &value);
  if (status.ok()) {
    out << "GET key1625 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29603: put
  db->Put(write_options, "key4266", "value94612");

  // op 29604: delete_range
  status = db->DeleteRange(write_options, "key7933", "key11102");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29605: put
  db->Put(write_options, "key1178", "value50968");

  // op 29606: delete_range
  status = db->DeleteRange(write_options, "key8129", "key11137");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29607: get
  status = db->Get(read_options, "key10870", &value);
  if (status.ok()) {
    out << "GET key10870 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29608: delete
  db->Delete(write_options, "key5134");

  // op 29609: put
  db->Put(write_options, "key6165", "value4933");

  // op 29610: delete
  db->Delete(write_options, "key7480");

  // op 29611: put
  db->Put(write_options, "key10460", "value91948");

  // op 29612: delete_range
  status = db->DeleteRange(write_options, "key9059", "key10241");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29613: put
  db->Put(write_options, "key9667", "value43856");

  // op 29614: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 29615: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8456", "key10052", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8456, key10052) size=" << scan_result.size() << std::endl;
  }

  // op 29616: get
  status = db->Get(read_options, "key0376", &value);
  if (status.ok()) {
    out << "GET key0376 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29617: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2489", "key8632", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2489, key8632) size=" << scan_result.size() << std::endl;
  }

  // op 29618: put
  db->Put(write_options, "key0066", "value46125");

  // op 29619: delete_range
  status = db->DeleteRange(write_options, "key0436", "key4521");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29620: delete
  db->Delete(write_options, "key10390");

  // op 29621: put
  db->Put(write_options, "key7968", "value57143");

  // op 29622: put
  db->Put(write_options, "key7860", "value33663");

  // op 29623: get
  status = db->Get(read_options, "key9961", &value);
  if (status.ok()) {
    out << "GET key9961 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29624: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6194", "key6576", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6194, key6576) size=" << scan_result.size() << std::endl;
  }

  // op 29625: put
  db->Put(write_options, "key7920", "value41473");

  // op 29626: put
  db->Put(write_options, "key11605", "value64787");

  // op 29627: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0034", "key3418", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0034, key3418) size=" << scan_result.size() << std::endl;
  }

  // op 29628: put
  db->Put(write_options, "key7592", "value33946");

  // op 29629: get
  status = db->Get(read_options, "key0121", &value);
  if (status.ok()) {
    out << "GET key0121 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29630: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0297", "key5593", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0297, key5593) size=" << scan_result.size() << std::endl;
  }

  // op 29631: put
  db->Put(write_options, "key9198", "value75378");

  // op 29632: get
  status = db->Get(read_options, "key0112", &value);
  if (status.ok()) {
    out << "GET key0112 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29633: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5435", "key5955", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5435, key5955) size=" << scan_result.size() << std::endl;
  }

  // op 29634: put
  db->Put(write_options, "key6323", "value94145");

  // op 29635: put
  db->Put(write_options, "key11723", "value56628");

  // op 29636: get
  status = db->Get(read_options, "key8025", &value);
  if (status.ok()) {
    out << "GET key8025 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29637: delete_range
  status = db->DeleteRange(write_options, "key0925", "key6256");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29638: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0455", "key3659", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0455, key3659) size=" << scan_result.size() << std::endl;
  }

  // op 29639: get
  status = db->Get(read_options, "key1623", &value);
  if (status.ok()) {
    out << "GET key1623 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29640: delete
  db->Delete(write_options, "key4938");

  // op 29641: get
  status = db->Get(read_options, "key2907", &value);
  if (status.ok()) {
    out << "GET key2907 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29642: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4971", "key9449", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4971, key9449) size=" << scan_result.size() << std::endl;
  }

  // op 29643: delete_range
  status = db->DeleteRange(write_options, "key4067", "key5680");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29644: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6781", "key10750", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6781, key10750) size=" << scan_result.size() << std::endl;
  }

  // op 29645: delete
  db->Delete(write_options, "key10680");

  // op 29646: put
  db->Put(write_options, "key10855", "value102760");

  // op 29647: delete
  db->Delete(write_options, "key5232");

  // op 29648: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4111", "key5881", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4111, key5881) size=" << scan_result.size() << std::endl;
  }

  // op 29649: get
  status = db->Get(read_options, "key8378", &value);
  if (status.ok()) {
    out << "GET key8378 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29650: delete_range
  status = db->DeleteRange(write_options, "key1769", "key10902");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29651: put
  db->Put(write_options, "key0621", "value100865");

  // op 29652: put
  db->Put(write_options, "key3782", "value113313");

  // op 29653: put
  db->Put(write_options, "key8984", "value88149");

  // op 29654: put
  db->Put(write_options, "key2230", "value92684");

  // op 29655: get
  status = db->Get(read_options, "key0926", &value);
  if (status.ok()) {
    out << "GET key0926 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29656: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1616", "key6464", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1616, key6464) size=" << scan_result.size() << std::endl;
  }

  // op 29657: put
  db->Put(write_options, "key4469", "value112493");

  // op 29658: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2144", "key5176", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2144, key5176) size=" << scan_result.size() << std::endl;
  }

  // op 29659: get
  status = db->Get(read_options, "key9471", &value);
  if (status.ok()) {
    out << "GET key9471 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29660: delete
  db->Delete(write_options, "key1197");

  // op 29661: delete
  db->Delete(write_options, "key4953");

  // op 29662: put
  db->Put(write_options, "key3575", "value111718");

  // op 29663: put
  db->Put(write_options, "key1348", "value45867");

  // op 29664: get
  status = db->Get(read_options, "key2220", &value);
  if (status.ok()) {
    out << "GET key2220 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29665: put
  db->Put(write_options, "key2496", "value5838");

  // op 29666: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3490", "key10444", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3490, key10444) size=" << scan_result.size() << std::endl;
  }

  // op 29667: put
  db->Put(write_options, "key11088", "value42011");

  // op 29668: put
  db->Put(write_options, "key10835", "value73638");

  // op 29669: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1938", "key6452", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1938, key6452) size=" << scan_result.size() << std::endl;
  }

  // op 29670: put
  db->Put(write_options, "key4606", "value24623");

  // op 29671: put
  db->Put(write_options, "key6489", "value66688");

  // op 29672: get
  status = db->Get(read_options, "key7419", &value);
  if (status.ok()) {
    out << "GET key7419 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29673: put
  db->Put(write_options, "key5740", "value46360");

  // op 29674: delete_range
  status = db->DeleteRange(write_options, "key5303", "key7406");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29675: get
  status = db->Get(read_options, "key5688", &value);
  if (status.ok()) {
    out << "GET key5688 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29676: delete_range
  status = db->DeleteRange(write_options, "key1732", "key6279");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29677: put
  db->Put(write_options, "key3747", "value110490");

  // op 29678: delete_range
  status = db->DeleteRange(write_options, "key8280", "key9435");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29679: delete_range
  status = db->DeleteRange(write_options, "key2136", "key2296");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29680: delete_range
  status = db->DeleteRange(write_options, "key1738", "key9493");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29681: put
  db->Put(write_options, "key9849", "value5527");

  // op 29682: get
  status = db->Get(read_options, "key10897", &value);
  if (status.ok()) {
    out << "GET key10897 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29683: delete_range
  status = db->DeleteRange(write_options, "key8715", "key11253");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29684: put
  db->Put(write_options, "key6819", "value30547");

  // op 29685: delete
  db->Delete(write_options, "key8870");

  // op 29686: get
  status = db->Get(read_options, "key0654", &value);
  if (status.ok()) {
    out << "GET key0654 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29687: put
  db->Put(write_options, "key2780", "value115104");

  // op 29688: put
  db->Put(write_options, "key6196", "value74221");

  // op 29689: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 29690: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2195", "key7330", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2195, key7330) size=" << scan_result.size() << std::endl;
  }

  // op 29691: delete_range
  status = db->DeleteRange(write_options, "key6465", "key9157");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29692: delete
  db->Delete(write_options, "key10319");

  // op 29693: put
  db->Put(write_options, "key3499", "value1217");

  // op 29694: get
  status = db->Get(read_options, "key2186", &value);
  if (status.ok()) {
    out << "GET key2186 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29695: put
  db->Put(write_options, "key1423", "value36564");

  // op 29696: put
  db->Put(write_options, "key11132", "value55024");

  // op 29697: put
  db->Put(write_options, "key9930", "value15379");

  // op 29698: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3212", "key10409", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3212, key10409) size=" << scan_result.size() << std::endl;
  }

  // op 29699: delete
  db->Delete(write_options, "key9147");

  // op 29700: delete
  db->Delete(write_options, "key8374");

  // op 29701: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0371", "key8733", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0371, key8733) size=" << scan_result.size() << std::endl;
  }

  // op 29702: delete
  db->Delete(write_options, "key3348");

  // op 29703: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4558", "key11018", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4558, key11018) size=" << scan_result.size() << std::endl;
  }

  // op 29704: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 29705: get
  status = db->Get(read_options, "key2950", &value);
  if (status.ok()) {
    out << "GET key2950 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29706: put
  db->Put(write_options, "key9787", "value5745");

  // op 29707: delete_range
  status = db->DeleteRange(write_options, "key2037", "key4433");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29708: put
  db->Put(write_options, "key8237", "value56476");

  // op 29709: put
  db->Put(write_options, "key11316", "value47173");

  // op 29710: get
  status = db->Get(read_options, "key9694", &value);
  if (status.ok()) {
    out << "GET key9694 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29711: put
  db->Put(write_options, "key6650", "value84450");

  // op 29712: put
  db->Put(write_options, "key9726", "value82307");

  // op 29713: put
  db->Put(write_options, "key6250", "value58124");

  // op 29714: put
  db->Put(write_options, "key10614", "value21474");

  // op 29715: put
  db->Put(write_options, "key7169", "value110893");

  // op 29716: get
  status = db->Get(read_options, "key9490", &value);
  if (status.ok()) {
    out << "GET key9490 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29717: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1217", "key7967", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1217, key7967) size=" << scan_result.size() << std::endl;
  }

  // op 29718: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1932", "key4308", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1932, key4308) size=" << scan_result.size() << std::endl;
  }

  // op 29719: delete
  db->Delete(write_options, "key6430");

  // op 29720: put
  db->Put(write_options, "key3814", "value12285");

  // op 29721: get
  status = db->Get(read_options, "key5005", &value);
  if (status.ok()) {
    out << "GET key5005 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29722: put
  db->Put(write_options, "key1937", "value31001");

  // op 29723: get
  status = db->Get(read_options, "key10636", &value);
  if (status.ok()) {
    out << "GET key10636 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29724: delete_range
  status = db->DeleteRange(write_options, "key7724", "key9392");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29725: put
  db->Put(write_options, "key6283", "value79452");

  // op 29726: delete_range
  status = db->DeleteRange(write_options, "key11383", "key11550");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29727: put
  db->Put(write_options, "key9965", "value79061");

  // op 29728: put
  db->Put(write_options, "key9478", "value110256");

  // op 29729: put
  db->Put(write_options, "key1746", "value29127");

  // op 29730: get
  status = db->Get(read_options, "key3859", &value);
  if (status.ok()) {
    out << "GET key3859 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29731: get
  status = db->Get(read_options, "key0444", &value);
  if (status.ok()) {
    out << "GET key0444 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29732: delete_range
  status = db->DeleteRange(write_options, "key5876", "key8970");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29733: put
  db->Put(write_options, "key2038", "value10946");

  // op 29734: put
  db->Put(write_options, "key10403", "value65794");

  // op 29735: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2939", "key8283", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2939, key8283) size=" << scan_result.size() << std::endl;
  }

  // op 29736: put
  db->Put(write_options, "key5773", "value1687");

  // op 29737: put
  db->Put(write_options, "key8004", "value28187");

  // op 29738: get
  status = db->Get(read_options, "key0742", &value);
  if (status.ok()) {
    out << "GET key0742 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29739: delete
  db->Delete(write_options, "key8512");

  // op 29740: get
  status = db->Get(read_options, "key2030", &value);
  if (status.ok()) {
    out << "GET key2030 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29741: put
  db->Put(write_options, "key11259", "value17452");

  // op 29742: delete_range
  status = db->DeleteRange(write_options, "key7589", "key9695");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29743: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 29744: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8639", "key9736", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8639, key9736) size=" << scan_result.size() << std::endl;
  }

  // op 29745: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6360", "key9346", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6360, key9346) size=" << scan_result.size() << std::endl;
  }

  // op 29746: delete_range
  status = db->DeleteRange(write_options, "key7368", "key9321");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29747: delete_range
  status = db->DeleteRange(write_options, "key4547", "key6929");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29748: put
  db->Put(write_options, "key2624", "value50415");

  // op 29749: get
  status = db->Get(read_options, "key0337", &value);
  if (status.ok()) {
    out << "GET key0337 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29750: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 29751: get
  status = db->Get(read_options, "key10846", &value);
  if (status.ok()) {
    out << "GET key10846 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29752: put
  db->Put(write_options, "key9010", "value74190");

  // op 29753: get
  status = db->Get(read_options, "key2462", &value);
  if (status.ok()) {
    out << "GET key2462 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29754: get
  status = db->Get(read_options, "key5041", &value);
  if (status.ok()) {
    out << "GET key5041 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29755: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5106", "key9558", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5106, key9558) size=" << scan_result.size() << std::endl;
  }

  // op 29756: delete
  db->Delete(write_options, "key10565");

  // op 29757: put
  db->Put(write_options, "key2237", "value110170");

  // op 29758: get
  status = db->Get(read_options, "key10161", &value);
  if (status.ok()) {
    out << "GET key10161 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29759: delete_range
  status = db->DeleteRange(write_options, "key3087", "key9108");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29760: get
  status = db->Get(read_options, "key2576", &value);
  if (status.ok()) {
    out << "GET key2576 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29761: delete
  db->Delete(write_options, "key1262");

  // op 29762: put
  db->Put(write_options, "key9043", "value37869");

  // op 29763: put
  db->Put(write_options, "key9940", "value28282");

  // op 29764: get
  status = db->Get(read_options, "key11580", &value);
  if (status.ok()) {
    out << "GET key11580 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29765: put
  db->Put(write_options, "key8374", "value53744");

  // op 29766: put
  db->Put(write_options, "key4792", "value6617");

  // op 29767: delete_range
  status = db->DeleteRange(write_options, "key0016", "key11557");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29768: get
  status = db->Get(read_options, "key5047", &value);
  if (status.ok()) {
    out << "GET key5047 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29769: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1558", "key11610", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1558, key11610) size=" << scan_result.size() << std::endl;
  }

  // op 29770: put
  db->Put(write_options, "key9648", "value74504");

  // op 29771: delete_range
  status = db->DeleteRange(write_options, "key6246", "key9766");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29772: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0648", "key5574", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0648, key5574) size=" << scan_result.size() << std::endl;
  }

  // op 29773: put
  db->Put(write_options, "key8065", "value8361");

  // op 29774: put
  db->Put(write_options, "key5005", "value77838");

  // op 29775: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1692", "key8665", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1692, key8665) size=" << scan_result.size() << std::endl;
  }

  // op 29776: put
  db->Put(write_options, "key0088", "value35059");

  // op 29777: put
  db->Put(write_options, "key5730", "value109873");

  // op 29778: put
  db->Put(write_options, "key6258", "value39880");

  // op 29779: get
  status = db->Get(read_options, "key1200", &value);
  if (status.ok()) {
    out << "GET key1200 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29780: put
  db->Put(write_options, "key0353", "value17916");

  // op 29781: put
  db->Put(write_options, "key8762", "value45395");

  // op 29782: delete_range
  status = db->DeleteRange(write_options, "key0468", "key11253");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29783: put
  db->Put(write_options, "key10964", "value65474");

  // op 29784: delete_range
  status = db->DeleteRange(write_options, "key4291", "key8999");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29785: put
  db->Put(write_options, "key4322", "value41996");

  // op 29786: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3355", "key10581", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3355, key10581) size=" << scan_result.size() << std::endl;
  }

  // op 29787: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10482", "key11470", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10482, key11470) size=" << scan_result.size() << std::endl;
  }

  // op 29788: get
  status = db->Get(read_options, "key5517", &value);
  if (status.ok()) {
    out << "GET key5517 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29789: put
  db->Put(write_options, "key11391", "value83277");

  // op 29790: put
  db->Put(write_options, "key4846", "value101075");

  // op 29791: put
  db->Put(write_options, "key10593", "value111592");

  // op 29792: put
  db->Put(write_options, "key3074", "value34327");

  // op 29793: delete_range
  status = db->DeleteRange(write_options, "key6834", "key11640");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29794: get
  status = db->Get(read_options, "key10515", &value);
  if (status.ok()) {
    out << "GET key10515 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29795: delete_range
  status = db->DeleteRange(write_options, "key0582", "key1589");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29796: put
  db->Put(write_options, "key1464", "value103451");

  // op 29797: put
  db->Put(write_options, "key8529", "value81084");

  // op 29798: put
  db->Put(write_options, "key8971", "value110031");

  // op 29799: put
  db->Put(write_options, "key3926", "value96744");

  // op 29800: put
  db->Put(write_options, "key2348", "value43048");

  // op 29801: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1098", "key3286", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1098, key3286) size=" << scan_result.size() << std::endl;
  }

  // op 29802: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6257", "key10763", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6257, key10763) size=" << scan_result.size() << std::endl;
  }

  // op 29803: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2238", "key6349", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2238, key6349) size=" << scan_result.size() << std::endl;
  }

  // op 29804: delete_range
  status = db->DeleteRange(write_options, "key0201", "key1882");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29805: delete_range
  status = db->DeleteRange(write_options, "key2697", "key11189");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29806: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0260", "key9260", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0260, key9260) size=" << scan_result.size() << std::endl;
  }

  // op 29807: get
  status = db->Get(read_options, "key3431", &value);
  if (status.ok()) {
    out << "GET key3431 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29808: put
  db->Put(write_options, "key4563", "value28539");

  // op 29809: delete
  db->Delete(write_options, "key0439");

  // op 29810: get
  status = db->Get(read_options, "key6998", &value);
  if (status.ok()) {
    out << "GET key6998 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29811: put
  db->Put(write_options, "key0843", "value94468");

  // op 29812: delete
  db->Delete(write_options, "key11432");

  // op 29813: put
  db->Put(write_options, "key6285", "value31393");

  // op 29814: delete_range
  status = db->DeleteRange(write_options, "key3264", "key7718");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29815: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 29816: put
  db->Put(write_options, "key9882", "value85286");

  // op 29817: get
  status = db->Get(read_options, "key8666", &value);
  if (status.ok()) {
    out << "GET key8666 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29818: put
  db->Put(write_options, "key2802", "value109886");

  // op 29819: delete_range
  status = db->DeleteRange(write_options, "key5035", "key11522");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29820: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6196", "key7629", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6196, key7629) size=" << scan_result.size() << std::endl;
  }

  // op 29821: delete_range
  status = db->DeleteRange(write_options, "key7287", "key10618");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29822: put
  db->Put(write_options, "key7916", "value114345");

  // op 29823: get
  status = db->Get(read_options, "key1250", &value);
  if (status.ok()) {
    out << "GET key1250 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29824: delete_range
  status = db->DeleteRange(write_options, "key1430", "key3639");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29825: put
  db->Put(write_options, "key1116", "value96285");

  // op 29826: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3402", "key5671", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3402, key5671) size=" << scan_result.size() << std::endl;
  }

  // op 29827: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 29828: get
  status = db->Get(read_options, "key7632", &value);
  if (status.ok()) {
    out << "GET key7632 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29829: put
  db->Put(write_options, "key8564", "value108669");

  // op 29830: put
  db->Put(write_options, "key9706", "value84795");

  // op 29831: put
  db->Put(write_options, "key7644", "value52508");

  // op 29832: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0310", "key0341", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0310, key0341) size=" << scan_result.size() << std::endl;
  }

  // op 29833: put
  db->Put(write_options, "key6847", "value85363");

  // op 29834: put
  db->Put(write_options, "key4944", "value73988");

  // op 29835: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 29836: put
  db->Put(write_options, "key3854", "value73912");

  // op 29837: put
  db->Put(write_options, "key5203", "value5246");

  // op 29838: put
  db->Put(write_options, "key0404", "value62468");

  // op 29839: delete
  db->Delete(write_options, "key0691");

  // op 29840: delete_range
  status = db->DeleteRange(write_options, "key0654", "key9437");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29841: put
  db->Put(write_options, "key11700", "value70871");

  // op 29842: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1585", "key8886", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1585, key8886) size=" << scan_result.size() << std::endl;
  }

  // op 29843: put
  db->Put(write_options, "key9116", "value98174");

  // op 29844: put
  db->Put(write_options, "key1025", "value93733");

  // op 29845: put
  db->Put(write_options, "key5705", "value2402");

  // op 29846: get
  status = db->Get(read_options, "key1754", &value);
  if (status.ok()) {
    out << "GET key1754 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29847: put
  db->Put(write_options, "key5646", "value77098");

  // op 29848: delete
  db->Delete(write_options, "key11698");

  // op 29849: get
  status = db->Get(read_options, "key2843", &value);
  if (status.ok()) {
    out << "GET key2843 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29850: put
  db->Put(write_options, "key0508", "value110428");

  // op 29851: put
  db->Put(write_options, "key4655", "value10873");

  // op 29852: put
  db->Put(write_options, "key3810", "value26550");

  // op 29853: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1487", "key6272", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1487, key6272) size=" << scan_result.size() << std::endl;
  }

  // op 29854: get
  status = db->Get(read_options, "key6912", &value);
  if (status.ok()) {
    out << "GET key6912 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29855: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2397", "key6896", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2397, key6896) size=" << scan_result.size() << std::endl;
  }

  // op 29856: delete_range
  status = db->DeleteRange(write_options, "key10573", "key10635");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29857: delete
  db->Delete(write_options, "key4491");

  // op 29858: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0912", "key6078", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0912, key6078) size=" << scan_result.size() << std::endl;
  }

  // op 29859: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 29860: get
  status = db->Get(read_options, "key1132", &value);
  if (status.ok()) {
    out << "GET key1132 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29861: delete_range
  status = db->DeleteRange(write_options, "key6216", "key9534");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29862: delete_range
  status = db->DeleteRange(write_options, "key4503", "key8806");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29863: get
  status = db->Get(read_options, "key2611", &value);
  if (status.ok()) {
    out << "GET key2611 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29864: delete_range
  status = db->DeleteRange(write_options, "key4574", "key6496");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29865: put
  db->Put(write_options, "key4910", "value28583");

  // op 29866: delete_range
  status = db->DeleteRange(write_options, "key3614", "key7376");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29867: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 29868: put
  db->Put(write_options, "key3869", "value84229");

  // op 29869: put
  db->Put(write_options, "key2575", "value112871");

  // op 29870: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2178", "key8707", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2178, key8707) size=" << scan_result.size() << std::endl;
  }

  // op 29871: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 29872: get
  status = db->Get(read_options, "key11189", &value);
  if (status.ok()) {
    out << "GET key11189 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29873: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6094", "key11028", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6094, key11028) size=" << scan_result.size() << std::endl;
  }

  // op 29874: put
  db->Put(write_options, "key8163", "value48420");

  // op 29875: get
  status = db->Get(read_options, "key4315", &value);
  if (status.ok()) {
    out << "GET key4315 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29876: delete_range
  status = db->DeleteRange(write_options, "key4419", "key10073");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29877: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10014", "key11315", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10014, key11315) size=" << scan_result.size() << std::endl;
  }

  // op 29878: get
  status = db->Get(read_options, "key10127", &value);
  if (status.ok()) {
    out << "GET key10127 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29879: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1552", "key10375", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1552, key10375) size=" << scan_result.size() << std::endl;
  }

  // op 29880: put
  db->Put(write_options, "key7176", "value82463");

  // op 29881: delete
  db->Delete(write_options, "key0173");

  // op 29882: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2800", "key5782", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2800, key5782) size=" << scan_result.size() << std::endl;
  }

  // op 29883: put
  db->Put(write_options, "key8696", "value61302");

  // op 29884: delete_range
  status = db->DeleteRange(write_options, "key2365", "key4359");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29885: put
  db->Put(write_options, "key7155", "value53056");

  // op 29886: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 29887: get
  status = db->Get(read_options, "key11429", &value);
  if (status.ok()) {
    out << "GET key11429 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29888: put
  db->Put(write_options, "key5587", "value79184");

  // op 29889: get
  status = db->Get(read_options, "key8798", &value);
  if (status.ok()) {
    out << "GET key8798 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29890: put
  db->Put(write_options, "key9318", "value33733");

  // op 29891: delete_range
  status = db->DeleteRange(write_options, "key7639", "key8458");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29892: get
  status = db->Get(read_options, "key5972", &value);
  if (status.ok()) {
    out << "GET key5972 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29893: put
  db->Put(write_options, "key8508", "value2419");

  // op 29894: delete
  db->Delete(write_options, "key9695");

  // op 29895: get
  status = db->Get(read_options, "key9487", &value);
  if (status.ok()) {
    out << "GET key9487 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29896: put
  db->Put(write_options, "key7217", "value12239");

  // op 29897: put
  db->Put(write_options, "key3168", "value32576");

  // op 29898: put
  db->Put(write_options, "key9858", "value61508");

  // op 29899: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3460", "key8871", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3460, key8871) size=" << scan_result.size() << std::endl;
  }

  // op 29900: get
  status = db->Get(read_options, "key8569", &value);
  if (status.ok()) {
    out << "GET key8569 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29901: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0321", "key6277", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0321, key6277) size=" << scan_result.size() << std::endl;
  }

  // op 29902: get
  status = db->Get(read_options, "key9771", &value);
  if (status.ok()) {
    out << "GET key9771 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29903: get
  status = db->Get(read_options, "key5549", &value);
  if (status.ok()) {
    out << "GET key5549 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29904: put
  db->Put(write_options, "key11502", "value25076");

  // op 29905: delete_range
  status = db->DeleteRange(write_options, "key2311", "key11203");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29906: delete
  db->Delete(write_options, "key10343");

  // op 29907: delete
  db->Delete(write_options, "key11650");

  // op 29908: get
  status = db->Get(read_options, "key6988", &value);
  if (status.ok()) {
    out << "GET key6988 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29909: put
  db->Put(write_options, "key9321", "value82438");

  // op 29910: put
  db->Put(write_options, "key5014", "value93128");

  // op 29911: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7223", "key9592", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7223, key9592) size=" << scan_result.size() << std::endl;
  }

  // op 29912: put
  db->Put(write_options, "key4929", "value83519");

  // op 29913: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5147", "key10714", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5147, key10714) size=" << scan_result.size() << std::endl;
  }

  // op 29914: put
  db->Put(write_options, "key8683", "value75474");

  // op 29915: put
  db->Put(write_options, "key2010", "value2105");

  // op 29916: delete
  db->Delete(write_options, "key2019");

  // op 29917: get
  status = db->Get(read_options, "key2877", &value);
  if (status.ok()) {
    out << "GET key2877 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29918: delete_range
  status = db->DeleteRange(write_options, "key0892", "key8367");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29919: put
  db->Put(write_options, "key5018", "value73745");

  // op 29920: put
  db->Put(write_options, "key2505", "value49728");

  // op 29921: put
  db->Put(write_options, "key9684", "value17649");

  // op 29922: delete_range
  status = db->DeleteRange(write_options, "key1516", "key5154");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29923: put
  db->Put(write_options, "key9271", "value13394");

  // op 29924: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3505", "key7579", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3505, key7579) size=" << scan_result.size() << std::endl;
  }

  // op 29925: get
  status = db->Get(read_options, "key4986", &value);
  if (status.ok()) {
    out << "GET key4986 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29926: put
  db->Put(write_options, "key6819", "value64417");

  // op 29927: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3359", "key11178", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3359, key11178) size=" << scan_result.size() << std::endl;
  }

  // op 29928: put
  db->Put(write_options, "key5971", "value116436");

  // op 29929: put
  db->Put(write_options, "key0919", "value98066");

  // op 29930: delete
  db->Delete(write_options, "key4803");

  // op 29931: delete_range
  status = db->DeleteRange(write_options, "key1580", "key10770");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29932: put
  db->Put(write_options, "key3470", "value23281");

  // op 29933: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4044", "key4803", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4044, key4803) size=" << scan_result.size() << std::endl;
  }

  // op 29934: put
  db->Put(write_options, "key6332", "value116298");

  // op 29935: get
  status = db->Get(read_options, "key10023", &value);
  if (status.ok()) {
    out << "GET key10023 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29936: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1864", "key7169", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1864, key7169) size=" << scan_result.size() << std::endl;
  }

  // op 29937: put
  db->Put(write_options, "key2650", "value21511");

  // op 29938: put
  db->Put(write_options, "key9418", "value117977");

  // op 29939: put
  db->Put(write_options, "key5556", "value17965");

  // op 29940: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2140", "key4265", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2140, key4265) size=" << scan_result.size() << std::endl;
  }

  // op 29941: delete
  db->Delete(write_options, "key4093");

  // op 29942: put
  db->Put(write_options, "key5415", "value88475");

  // op 29943: delete_range
  status = db->DeleteRange(write_options, "key3048", "key3712");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29944: get
  status = db->Get(read_options, "key6140", &value);
  if (status.ok()) {
    out << "GET key6140 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29945: put
  db->Put(write_options, "key9070", "value14328");

  // op 29946: get
  status = db->Get(read_options, "key6393", &value);
  if (status.ok()) {
    out << "GET key6393 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29947: put
  db->Put(write_options, "key4982", "value23763");

  // op 29948: put
  db->Put(write_options, "key6110", "value88203");

  // op 29949: put
  db->Put(write_options, "key2336", "value94409");

  // op 29950: delete_range
  status = db->DeleteRange(write_options, "key5474", "key7416");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29951: get
  status = db->Get(read_options, "key3380", &value);
  if (status.ok()) {
    out << "GET key3380 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29952: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7772", "key11105", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7772, key11105) size=" << scan_result.size() << std::endl;
  }

  // op 29953: delete
  db->Delete(write_options, "key5147");

  // op 29954: delete
  db->Delete(write_options, "key10780");

  // op 29955: put
  db->Put(write_options, "key10597", "value56870");

  // op 29956: put
  db->Put(write_options, "key0211", "value84947");

  // op 29957: delete
  db->Delete(write_options, "key2278");

  // op 29958: get
  status = db->Get(read_options, "key4816", &value);
  if (status.ok()) {
    out << "GET key4816 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29959: get
  status = db->Get(read_options, "key8829", &value);
  if (status.ok()) {
    out << "GET key8829 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29960: get
  status = db->Get(read_options, "key9217", &value);
  if (status.ok()) {
    out << "GET key9217 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29961: put
  db->Put(write_options, "key11020", "value29775");

  // op 29962: get
  status = db->Get(read_options, "key2030", &value);
  if (status.ok()) {
    out << "GET key2030 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29963: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5511", "key6698", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5511, key6698) size=" << scan_result.size() << std::endl;
  }

  // op 29964: put
  db->Put(write_options, "key0428", "value56557");

  // op 29965: put
  db->Put(write_options, "key3714", "value50727");

  // op 29966: delete
  db->Delete(write_options, "key8190");

  // op 29967: put
  db->Put(write_options, "key0161", "value51684");

  // op 29968: put
  db->Put(write_options, "key2317", "value71705");

  // op 29969: get
  status = db->Get(read_options, "key7329", &value);
  if (status.ok()) {
    out << "GET key7329 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29970: get
  status = db->Get(read_options, "key2342", &value);
  if (status.ok()) {
    out << "GET key2342 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29971: get
  status = db->Get(read_options, "key6758", &value);
  if (status.ok()) {
    out << "GET key6758 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29972: put
  db->Put(write_options, "key2610", "value55637");

  // op 29973: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4343", "key5096", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4343, key5096) size=" << scan_result.size() << std::endl;
  }

  // op 29974: get
  status = db->Get(read_options, "key11634", &value);
  if (status.ok()) {
    out << "GET key11634 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29975: put
  db->Put(write_options, "key10006", "value70709");

  // op 29976: get
  status = db->Get(read_options, "key1134", &value);
  if (status.ok()) {
    out << "GET key1134 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29977: get
  status = db->Get(read_options, "key8295", &value);
  if (status.ok()) {
    out << "GET key8295 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29978: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 29979: get
  status = db->Get(read_options, "key1047", &value);
  if (status.ok()) {
    out << "GET key1047 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29980: get
  status = db->Get(read_options, "key2140", &value);
  if (status.ok()) {
    out << "GET key2140 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29981: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 29982: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 29983: get
  status = db->Get(read_options, "key3676", &value);
  if (status.ok()) {
    out << "GET key3676 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29984: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1032", "key4080", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1032, key4080) size=" << scan_result.size() << std::endl;
  }

  // op 29985: get
  status = db->Get(read_options, "key9222", &value);
  if (status.ok()) {
    out << "GET key9222 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29986: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1407", "key5705", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1407, key5705) size=" << scan_result.size() << std::endl;
  }

  // op 29987: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 29988: get
  status = db->Get(read_options, "key0896", &value);
  if (status.ok()) {
    out << "GET key0896 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 29989: put
  db->Put(write_options, "key2735", "value95537");

  // op 29990: put
  db->Put(write_options, "key4020", "value99824");

  // op 29991: put
  db->Put(write_options, "key5480", "value98751");

  // op 29992: delete_range
  status = db->DeleteRange(write_options, "key0502", "key1708");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 29993: delete
  db->Delete(write_options, "key6399");

  // op 29994: put
  db->Put(write_options, "key4887", "value39164");

  // op 29995: delete
  db->Delete(write_options, "key8690");

  // op 29996: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4365", "key5877", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4365, key5877) size=" << scan_result.size() << std::endl;
  }

  // op 29997: put
  db->Put(write_options, "key3603", "value10831");

  // op 29998: put
  db->Put(write_options, "key9009", "value103665");

  // op 29999: delete
  db->Delete(write_options, "key6717");

  // op 30000: delete
  db->Delete(write_options, "key6851");

  // op 30001: put
  db->Put(write_options, "key6308", "value47048");

  // op 30002: put
  db->Put(write_options, "key4773", "value36478");

  // op 30003: get
  status = db->Get(read_options, "key8327", &value);
  if (status.ok()) {
    out << "GET key8327 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30004: get
  status = db->Get(read_options, "key5270", &value);
  if (status.ok()) {
    out << "GET key5270 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30005: put
  db->Put(write_options, "key10730", "value18447");

  // op 30006: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5652", "key11643", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5652, key11643) size=" << scan_result.size() << std::endl;
  }

  // op 30007: put
  db->Put(write_options, "key9259", "value3069");

  // op 30008: put
  db->Put(write_options, "key4393", "value72239");

  // op 30009: put
  db->Put(write_options, "key0796", "value30198");

  // op 30010: get
  status = db->Get(read_options, "key11323", &value);
  if (status.ok()) {
    out << "GET key11323 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30011: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 30012: get
  status = db->Get(read_options, "key8818", &value);
  if (status.ok()) {
    out << "GET key8818 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30013: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10240", "key10784", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10240, key10784) size=" << scan_result.size() << std::endl;
  }

  // op 30014: get
  status = db->Get(read_options, "key2318", &value);
  if (status.ok()) {
    out << "GET key2318 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30015: put
  db->Put(write_options, "key0778", "value117263");

  // op 30016: get
  status = db->Get(read_options, "key0820", &value);
  if (status.ok()) {
    out << "GET key0820 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30017: put
  db->Put(write_options, "key0143", "value32231");

  // op 30018: put
  db->Put(write_options, "key2297", "value47283");

  // op 30019: delete_range
  status = db->DeleteRange(write_options, "key1081", "key8433");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 30020: get
  status = db->Get(read_options, "key7751", &value);
  if (status.ok()) {
    out << "GET key7751 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30021: get
  status = db->Get(read_options, "key4162", &value);
  if (status.ok()) {
    out << "GET key4162 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30022: get
  status = db->Get(read_options, "key11205", &value);
  if (status.ok()) {
    out << "GET key11205 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30023: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7255", "key10118", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7255, key10118) size=" << scan_result.size() << std::endl;
  }

  // op 30024: get
  status = db->Get(read_options, "key9596", &value);
  if (status.ok()) {
    out << "GET key9596 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30025: get
  status = db->Get(read_options, "key7561", &value);
  if (status.ok()) {
    out << "GET key7561 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30026: get
  status = db->Get(read_options, "key6075", &value);
  if (status.ok()) {
    out << "GET key6075 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30027: delete
  db->Delete(write_options, "key6067");

  // op 30028: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4424", "key10081", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4424, key10081) size=" << scan_result.size() << std::endl;
  }

  // op 30029: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 30030: get
  status = db->Get(read_options, "key10954", &value);
  if (status.ok()) {
    out << "GET key10954 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30031: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 30032: get
  status = db->Get(read_options, "key3492", &value);
  if (status.ok()) {
    out << "GET key3492 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30033: put
  db->Put(write_options, "key6162", "value89254");

  // op 30034: put
  db->Put(write_options, "key11014", "value82210");

  // op 30035: put
  db->Put(write_options, "key2531", "value21200");

  // op 30036: delete
  db->Delete(write_options, "key6570");

  // op 30037: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2243", "key3933", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2243, key3933) size=" << scan_result.size() << std::endl;
  }

  // op 30038: get
  status = db->Get(read_options, "key5761", &value);
  if (status.ok()) {
    out << "GET key5761 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30039: put
  db->Put(write_options, "key2261", "value44581");

  // op 30040: put
  db->Put(write_options, "key1842", "value96885");

  // op 30041: delete
  db->Delete(write_options, "key3149");

  // op 30042: delete_range
  status = db->DeleteRange(write_options, "key7383", "key9242");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 30043: put
  db->Put(write_options, "key4298", "value28369");

  // op 30044: put
  db->Put(write_options, "key8739", "value6400");

  // op 30045: get
  status = db->Get(read_options, "key7457", &value);
  if (status.ok()) {
    out << "GET key7457 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30046: get
  status = db->Get(read_options, "key11012", &value);
  if (status.ok()) {
    out << "GET key11012 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30047: put
  db->Put(write_options, "key9114", "value104679");

  // op 30048: put
  db->Put(write_options, "key9909", "value40756");

  // op 30049: delete_range
  status = db->DeleteRange(write_options, "key1037", "key4421");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 30050: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8503", "key11503", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8503, key11503) size=" << scan_result.size() << std::endl;
  }

  // op 30051: delete_range
  status = db->DeleteRange(write_options, "key3888", "key9138");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 30052: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 30053: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1355", "key6253", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1355, key6253) size=" << scan_result.size() << std::endl;
  }

  // op 30054: put
  db->Put(write_options, "key5547", "value21598");

  // op 30055: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0100", "key1781", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0100, key1781) size=" << scan_result.size() << std::endl;
  }

  // op 30056: get
  status = db->Get(read_options, "key1618", &value);
  if (status.ok()) {
    out << "GET key1618 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30057: put
  db->Put(write_options, "key10401", "value112467");

  // op 30058: put
  db->Put(write_options, "key9155", "value26011");

  // op 30059: put
  db->Put(write_options, "key8575", "value64778");

  // op 30060: put
  db->Put(write_options, "key8569", "value48848");

  // op 30061: delete
  db->Delete(write_options, "key10920");

  // op 30062: put
  db->Put(write_options, "key5121", "value22110");

  // op 30063: delete
  db->Delete(write_options, "key10320");

  // op 30064: delete
  db->Delete(write_options, "key7663");

  // op 30065: put
  db->Put(write_options, "key2924", "value37169");

  // op 30066: delete_range
  status = db->DeleteRange(write_options, "key7990", "key11606");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 30067: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5842", "key8656", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5842, key8656) size=" << scan_result.size() << std::endl;
  }

  // op 30068: put
  db->Put(write_options, "key6670", "value110440");

  // op 30069: get
  status = db->Get(read_options, "key5719", &value);
  if (status.ok()) {
    out << "GET key5719 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30070: put
  db->Put(write_options, "key3852", "value60536");

  // op 30071: put
  db->Put(write_options, "key1305", "value15660");

  // op 30072: get
  status = db->Get(read_options, "key5000", &value);
  if (status.ok()) {
    out << "GET key5000 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30073: delete_range
  status = db->DeleteRange(write_options, "key2016", "key3654");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 30074: get
  status = db->Get(read_options, "key5339", &value);
  if (status.ok()) {
    out << "GET key5339 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30075: get
  status = db->Get(read_options, "key4437", &value);
  if (status.ok()) {
    out << "GET key4437 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30076: get
  status = db->Get(read_options, "key10182", &value);
  if (status.ok()) {
    out << "GET key10182 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30077: put
  db->Put(write_options, "key8430", "value78470");

  // op 30078: delete
  db->Delete(write_options, "key5888");

  // op 30079: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3985", "key4114", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3985, key4114) size=" << scan_result.size() << std::endl;
  }

  // op 30080: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1824", "key5772", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1824, key5772) size=" << scan_result.size() << std::endl;
  }

  // op 30081: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1268", "key2872", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1268, key2872) size=" << scan_result.size() << std::endl;
  }

  // op 30082: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 30083: put
  db->Put(write_options, "key0573", "value4533");

  // op 30084: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3571", "key7358", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3571, key7358) size=" << scan_result.size() << std::endl;
  }

  // op 30085: delete
  db->Delete(write_options, "key3038");

  // op 30086: put
  db->Put(write_options, "key1894", "value56897");

  // op 30087: get
  status = db->Get(read_options, "key0826", &value);
  if (status.ok()) {
    out << "GET key0826 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30088: delete
  db->Delete(write_options, "key3120");

  // op 30089: put
  db->Put(write_options, "key0318", "value75216");

  // op 30090: put
  db->Put(write_options, "key9312", "value1532");

  // op 30091: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3049", "key7016", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3049, key7016) size=" << scan_result.size() << std::endl;
  }

  // op 30092: get
  status = db->Get(read_options, "key0772", &value);
  if (status.ok()) {
    out << "GET key0772 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30093: get
  status = db->Get(read_options, "key0967", &value);
  if (status.ok()) {
    out << "GET key0967 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30094: put
  db->Put(write_options, "key6295", "value16383");

  // op 30095: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 30096: delete
  db->Delete(write_options, "key3937");

  // op 30097: put
  db->Put(write_options, "key7593", "value112860");

  // op 30098: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5125", "key10646", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5125, key10646) size=" << scan_result.size() << std::endl;
  }

  // op 30099: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0277", "key6559", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0277, key6559) size=" << scan_result.size() << std::endl;
  }

  // op 30100: put
  db->Put(write_options, "key10307", "value53244");

  // op 30101: put
  db->Put(write_options, "key11273", "value89854");

  // op 30102: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4145", "key4847", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4145, key4847) size=" << scan_result.size() << std::endl;
  }

  // op 30103: delete_range
  status = db->DeleteRange(write_options, "key5957", "key8114");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 30104: put
  db->Put(write_options, "key7841", "value88155");

  // op 30105: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 30106: put
  db->Put(write_options, "key10366", "value87856");

  // op 30107: put
  db->Put(write_options, "key2564", "value40726");

  // op 30108: delete
  db->Delete(write_options, "key5953");

  // op 30109: get
  status = db->Get(read_options, "key6793", &value);
  if (status.ok()) {
    out << "GET key6793 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30110: put
  db->Put(write_options, "key8947", "value76801");

  // op 30111: get
  status = db->Get(read_options, "key2560", &value);
  if (status.ok()) {
    out << "GET key2560 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30112: get
  status = db->Get(read_options, "key7758", &value);
  if (status.ok()) {
    out << "GET key7758 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30113: delete
  db->Delete(write_options, "key6916");

  // op 30114: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 30115: put
  db->Put(write_options, "key2141", "value72471");

  // op 30116: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0565", "key9542", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0565, key9542) size=" << scan_result.size() << std::endl;
  }

  // op 30117: put
  db->Put(write_options, "key9849", "value17918");

  // op 30118: put
  db->Put(write_options, "key0779", "value65191");

  // op 30119: delete_range
  status = db->DeleteRange(write_options, "key1665", "key6586");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 30120: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4231", "key7142", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4231, key7142) size=" << scan_result.size() << std::endl;
  }

  // op 30121: put
  db->Put(write_options, "key0342", "value95131");

  // op 30122: put
  db->Put(write_options, "key3728", "value5062");

  // op 30123: put
  db->Put(write_options, "key10999", "value28312");

  // op 30124: put
  db->Put(write_options, "key1679", "value18820");

  // op 30125: get
  status = db->Get(read_options, "key2581", &value);
  if (status.ok()) {
    out << "GET key2581 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30126: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0386", "key7631", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0386, key7631) size=" << scan_result.size() << std::endl;
  }

  // op 30127: get
  status = db->Get(read_options, "key5718", &value);
  if (status.ok()) {
    out << "GET key5718 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30128: delete_range
  status = db->DeleteRange(write_options, "key6498", "key7399");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 30129: put
  db->Put(write_options, "key3786", "value83575");

  // op 30130: get
  status = db->Get(read_options, "key7035", &value);
  if (status.ok()) {
    out << "GET key7035 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30131: delete_range
  status = db->DeleteRange(write_options, "key6954", "key7661");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 30132: put
  db->Put(write_options, "key10133", "value114571");

  // op 30133: get
  status = db->Get(read_options, "key7388", &value);
  if (status.ok()) {
    out << "GET key7388 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30134: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4297", "key7824", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4297, key7824) size=" << scan_result.size() << std::endl;
  }

  // op 30135: put
  db->Put(write_options, "key4397", "value111888");

  // op 30136: get
  status = db->Get(read_options, "key11493", &value);
  if (status.ok()) {
    out << "GET key11493 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30137: put
  db->Put(write_options, "key2285", "value65573");

  // op 30138: delete
  db->Delete(write_options, "key2246");

  // op 30139: delete
  db->Delete(write_options, "key7767");

  // op 30140: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 30141: get
  status = db->Get(read_options, "key6294", &value);
  if (status.ok()) {
    out << "GET key6294 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30142: put
  db->Put(write_options, "key4483", "value100789");

  // op 30143: delete
  db->Delete(write_options, "key2636");

  // op 30144: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4814", "key4860", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4814, key4860) size=" << scan_result.size() << std::endl;
  }

  // op 30145: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0201", "key1043", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0201, key1043) size=" << scan_result.size() << std::endl;
  }

  // op 30146: get
  status = db->Get(read_options, "key10104", &value);
  if (status.ok()) {
    out << "GET key10104 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30147: get
  status = db->Get(read_options, "key9209", &value);
  if (status.ok()) {
    out << "GET key9209 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30148: get
  status = db->Get(read_options, "key8547", &value);
  if (status.ok()) {
    out << "GET key8547 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30149: put
  db->Put(write_options, "key1450", "value104678");

  // op 30150: get
  status = db->Get(read_options, "key9913", &value);
  if (status.ok()) {
    out << "GET key9913 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30151: put
  db->Put(write_options, "key1086", "value29481");

  // op 30152: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5996", "key9184", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5996, key9184) size=" << scan_result.size() << std::endl;
  }

  // op 30153: get
  status = db->Get(read_options, "key11683", &value);
  if (status.ok()) {
    out << "GET key11683 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30154: put
  db->Put(write_options, "key10645", "value74465");

  // op 30155: delete
  db->Delete(write_options, "key8815");

  // op 30156: put
  db->Put(write_options, "key8727", "value57020");

  // op 30157: put
  db->Put(write_options, "key2821", "value49163");

  // op 30158: get
  status = db->Get(read_options, "key0758", &value);
  if (status.ok()) {
    out << "GET key0758 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30159: get
  status = db->Get(read_options, "key11601", &value);
  if (status.ok()) {
    out << "GET key11601 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30160: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 30161: delete_range
  status = db->DeleteRange(write_options, "key0953", "key8900");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 30162: get
  status = db->Get(read_options, "key6109", &value);
  if (status.ok()) {
    out << "GET key6109 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30163: put
  db->Put(write_options, "key6891", "value115014");

  // op 30164: get
  status = db->Get(read_options, "key1047", &value);
  if (status.ok()) {
    out << "GET key1047 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30165: delete
  db->Delete(write_options, "key3601");

  // op 30166: get
  status = db->Get(read_options, "key4462", &value);
  if (status.ok()) {
    out << "GET key4462 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30167: put
  db->Put(write_options, "key11285", "value79041");

  // op 30168: put
  db->Put(write_options, "key8572", "value28369");

  // op 30169: get
  status = db->Get(read_options, "key2030", &value);
  if (status.ok()) {
    out << "GET key2030 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30170: delete_range
  status = db->DeleteRange(write_options, "key3289", "key6803");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 30171: put
  db->Put(write_options, "key1601", "value43580");

  // op 30172: put
  db->Put(write_options, "key1311", "value29613");

  // op 30173: put
  db->Put(write_options, "key9369", "value88110");

  // op 30174: get
  status = db->Get(read_options, "key1466", &value);
  if (status.ok()) {
    out << "GET key1466 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30175: delete
  db->Delete(write_options, "key8937");

  // op 30176: put
  db->Put(write_options, "key9402", "value104547");

  // op 30177: put
  db->Put(write_options, "key7257", "value24198");

  // op 30178: put
  db->Put(write_options, "key9255", "value28189");

  // op 30179: put
  db->Put(write_options, "key2867", "value25720");

  // op 30180: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2550", "key5848", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2550, key5848) size=" << scan_result.size() << std::endl;
  }

  // op 30181: get
  status = db->Get(read_options, "key11532", &value);
  if (status.ok()) {
    out << "GET key11532 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30182: delete_range
  status = db->DeleteRange(write_options, "key0697", "key7223");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 30183: put
  db->Put(write_options, "key5370", "value49006");

  // op 30184: get
  status = db->Get(read_options, "key1984", &value);
  if (status.ok()) {
    out << "GET key1984 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30185: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5158", "key11006", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5158, key11006) size=" << scan_result.size() << std::endl;
  }

  // op 30186: put
  db->Put(write_options, "key2460", "value87579");

  // op 30187: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5003", "key10596", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5003, key10596) size=" << scan_result.size() << std::endl;
  }

  // op 30188: delete_range
  status = db->DeleteRange(write_options, "key0930", "key11366");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 30189: put
  db->Put(write_options, "key9047", "value22627");

  // op 30190: delete
  db->Delete(write_options, "key6676");

  // op 30191: put
  db->Put(write_options, "key7072", "value58518");

  // op 30192: delete
  db->Delete(write_options, "key2272");

  // op 30193: get
  status = db->Get(read_options, "key11467", &value);
  if (status.ok()) {
    out << "GET key11467 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30194: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1083", "key8668", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1083, key8668) size=" << scan_result.size() << std::endl;
  }

  // op 30195: put
  db->Put(write_options, "key9697", "value21575");

  // op 30196: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9074", "key11538", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9074, key11538) size=" << scan_result.size() << std::endl;
  }

  // op 30197: get
  status = db->Get(read_options, "key11171", &value);
  if (status.ok()) {
    out << "GET key11171 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30198: delete
  db->Delete(write_options, "key10795");

  // op 30199: put
  db->Put(write_options, "key6252", "value15674");

  // op 30200: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0141", "key2748", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0141, key2748) size=" << scan_result.size() << std::endl;
  }

  // op 30201: delete_range
  status = db->DeleteRange(write_options, "key0564", "key8776");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 30202: put
  db->Put(write_options, "key8192", "value17784");

  // op 30203: delete
  db->Delete(write_options, "key10370");

  // op 30204: put
  db->Put(write_options, "key6294", "value70182");

  // op 30205: get
  status = db->Get(read_options, "key7827", &value);
  if (status.ok()) {
    out << "GET key7827 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30206: get
  status = db->Get(read_options, "key9443", &value);
  if (status.ok()) {
    out << "GET key9443 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30207: get
  status = db->Get(read_options, "key8126", &value);
  if (status.ok()) {
    out << "GET key8126 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30208: delete_range
  status = db->DeleteRange(write_options, "key3046", "key7554");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 30209: delete
  db->Delete(write_options, "key9148");

  // op 30210: get
  status = db->Get(read_options, "key3817", &value);
  if (status.ok()) {
    out << "GET key3817 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30211: get
  status = db->Get(read_options, "key7388", &value);
  if (status.ok()) {
    out << "GET key7388 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30212: put
  db->Put(write_options, "key2795", "value66173");

  // op 30213: delete
  db->Delete(write_options, "key9019");

  // op 30214: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3231", "key11497", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3231, key11497) size=" << scan_result.size() << std::endl;
  }

  // op 30215: get
  status = db->Get(read_options, "key2521", &value);
  if (status.ok()) {
    out << "GET key2521 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30216: delete_range
  status = db->DeleteRange(write_options, "key2463", "key7148");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 30217: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10393", "key11028", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10393, key11028) size=" << scan_result.size() << std::endl;
  }

  // op 30218: get
  status = db->Get(read_options, "key1435", &value);
  if (status.ok()) {
    out << "GET key1435 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30219: delete_range
  status = db->DeleteRange(write_options, "key4535", "key7310");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 30220: put
  db->Put(write_options, "key7621", "value116477");

  // op 30221: delete
  db->Delete(write_options, "key1570");

  // op 30222: put
  db->Put(write_options, "key9954", "value88890");

  // op 30223: put
  db->Put(write_options, "key3386", "value83043");

  // op 30224: put
  db->Put(write_options, "key8137", "value9269");

  // op 30225: delete_range
  status = db->DeleteRange(write_options, "key3650", "key6801");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 30226: get
  status = db->Get(read_options, "key3951", &value);
  if (status.ok()) {
    out << "GET key3951 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30227: get
  status = db->Get(read_options, "key3085", &value);
  if (status.ok()) {
    out << "GET key3085 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30228: put
  db->Put(write_options, "key7678", "value96095");

  // op 30229: delete
  db->Delete(write_options, "key10319");

  // op 30230: put
  db->Put(write_options, "key2515", "value35520");

  // op 30231: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2462", "key8065", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2462, key8065) size=" << scan_result.size() << std::endl;
  }

  // op 30232: put
  db->Put(write_options, "key10537", "value70966");

  // op 30233: put
  db->Put(write_options, "key9201", "value13262");

  // op 30234: delete
  db->Delete(write_options, "key8140");

  // op 30235: put
  db->Put(write_options, "key0894", "value108005");

  // op 30236: put
  db->Put(write_options, "key11115", "value96838");

  // op 30237: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0845", "key4644", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0845, key4644) size=" << scan_result.size() << std::endl;
  }

  // op 30238: delete
  db->Delete(write_options, "key4171");

  // op 30239: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4836", "key8281", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4836, key8281) size=" << scan_result.size() << std::endl;
  }

  // op 30240: get
  status = db->Get(read_options, "key5675", &value);
  if (status.ok()) {
    out << "GET key5675 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30241: delete
  db->Delete(write_options, "key4685");

  // op 30242: put
  db->Put(write_options, "key2703", "value8062");

  // op 30243: put
  db->Put(write_options, "key2855", "value89943");

  // op 30244: put
  db->Put(write_options, "key7485", "value9746");

  // op 30245: put
  db->Put(write_options, "key11479", "value63182");

  // op 30246: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4297", "key10700", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4297, key10700) size=" << scan_result.size() << std::endl;
  }

  // op 30247: put
  db->Put(write_options, "key6823", "value86670");

  // op 30248: put
  db->Put(write_options, "key6761", "value46212");

  // op 30249: get
  status = db->Get(read_options, "key10536", &value);
  if (status.ok()) {
    out << "GET key10536 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30250: delete_range
  status = db->DeleteRange(write_options, "key0321", "key0404");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 30251: delete
  db->Delete(write_options, "key1766");

  // op 30252: delete_range
  status = db->DeleteRange(write_options, "key3853", "key9504");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 30253: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 30254: get
  status = db->Get(read_options, "key11291", &value);
  if (status.ok()) {
    out << "GET key11291 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30255: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8447", "key8458", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8447, key8458) size=" << scan_result.size() << std::endl;
  }

  // op 30256: put
  db->Put(write_options, "key7374", "value30548");

  // op 30257: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2671", "key7252", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2671, key7252) size=" << scan_result.size() << std::endl;
  }

  // op 30258: get
  status = db->Get(read_options, "key2294", &value);
  if (status.ok()) {
    out << "GET key2294 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30259: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3098", "key11016", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3098, key11016) size=" << scan_result.size() << std::endl;
  }

  // op 30260: put
  db->Put(write_options, "key6419", "value112829");

  // op 30261: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0686", "key9519", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0686, key9519) size=" << scan_result.size() << std::endl;
  }

  // op 30262: put
  db->Put(write_options, "key7541", "value93549");

  // op 30263: delete_range
  status = db->DeleteRange(write_options, "key3922", "key11187");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 30264: delete
  db->Delete(write_options, "key9287");

  // op 30265: put
  db->Put(write_options, "key0379", "value69630");

  // op 30266: put
  db->Put(write_options, "key0236", "value0843");

  // op 30267: get
  status = db->Get(read_options, "key1816", &value);
  if (status.ok()) {
    out << "GET key1816 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30268: delete
  db->Delete(write_options, "key10619");

  // op 30269: put
  db->Put(write_options, "key5871", "value96829");

  // op 30270: delete_range
  status = db->DeleteRange(write_options, "key0247", "key4325");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 30271: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0395", "key6234", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0395, key6234) size=" << scan_result.size() << std::endl;
  }

  // op 30272: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 30273: put
  db->Put(write_options, "key2313", "value7651");

  // op 30274: get
  status = db->Get(read_options, "key10303", &value);
  if (status.ok()) {
    out << "GET key10303 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30275: put
  db->Put(write_options, "key9957", "value84428");

  // op 30276: put
  db->Put(write_options, "key6404", "value41516");

  // op 30277: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 30278: put
  db->Put(write_options, "key10995", "value79350");

  // op 30279: delete_range
  status = db->DeleteRange(write_options, "key7200", "key11685");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 30280: put
  db->Put(write_options, "key1485", "value11849");

  // op 30281: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 30282: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2691", "key11567", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2691, key11567) size=" << scan_result.size() << std::endl;
  }

  // op 30283: put
  db->Put(write_options, "key4341", "value109066");

  // op 30284: put
  db->Put(write_options, "key3952", "value13381");

  // op 30285: put
  db->Put(write_options, "key1874", "value54229");

  // op 30286: get
  status = db->Get(read_options, "key7048", &value);
  if (status.ok()) {
    out << "GET key7048 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30287: get
  status = db->Get(read_options, "key9940", &value);
  if (status.ok()) {
    out << "GET key9940 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30288: put
  db->Put(write_options, "key10585", "value11908");

  // op 30289: put
  db->Put(write_options, "key2762", "value113066");

  // op 30290: delete_range
  status = db->DeleteRange(write_options, "key4454", "key5816");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 30291: delete_range
  status = db->DeleteRange(write_options, "key1207", "key9873");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 30292: delete
  db->Delete(write_options, "key1802");

  // op 30293: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1897", "key6698", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1897, key6698) size=" << scan_result.size() << std::endl;
  }

  // op 30294: get
  status = db->Get(read_options, "key0723", &value);
  if (status.ok()) {
    out << "GET key0723 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30295: get
  status = db->Get(read_options, "key11035", &value);
  if (status.ok()) {
    out << "GET key11035 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30296: delete_range
  status = db->DeleteRange(write_options, "key2248", "key7701");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 30297: put
  db->Put(write_options, "key9029", "value108955");

  // op 30298: put
  db->Put(write_options, "key4135", "value11267");

  // op 30299: get
  status = db->Get(read_options, "key6384", &value);
  if (status.ok()) {
    out << "GET key6384 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30300: get
  status = db->Get(read_options, "key8650", &value);
  if (status.ok()) {
    out << "GET key8650 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30301: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0713", "key8490", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0713, key8490) size=" << scan_result.size() << std::endl;
  }

  // op 30302: delete_range
  status = db->DeleteRange(write_options, "key3211", "key5215");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 30303: get
  status = db->Get(read_options, "key9675", &value);
  if (status.ok()) {
    out << "GET key9675 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30304: put
  db->Put(write_options, "key3203", "value22692");

  // op 30305: put
  db->Put(write_options, "key2172", "value38107");

  // op 30306: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0734", "key7300", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0734, key7300) size=" << scan_result.size() << std::endl;
  }

  // op 30307: delete
  db->Delete(write_options, "key3296");

  // op 30308: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3183", "key6928", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3183, key6928) size=" << scan_result.size() << std::endl;
  }

  // op 30309: put
  db->Put(write_options, "key3879", "value60472");

  // op 30310: delete_range
  status = db->DeleteRange(write_options, "key2851", "key8735");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 30311: put
  db->Put(write_options, "key10311", "value54614");

  // op 30312: get
  status = db->Get(read_options, "key6227", &value);
  if (status.ok()) {
    out << "GET key6227 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30313: put
  db->Put(write_options, "key5055", "value22005");

  // op 30314: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1917", "key5496", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1917, key5496) size=" << scan_result.size() << std::endl;
  }

  // op 30315: delete_range
  status = db->DeleteRange(write_options, "key1278", "key2239");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 30316: get
  status = db->Get(read_options, "key4606", &value);
  if (status.ok()) {
    out << "GET key4606 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30317: put
  db->Put(write_options, "key8383", "value73524");

  // op 30318: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4333", "key8759", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4333, key8759) size=" << scan_result.size() << std::endl;
  }

  // op 30319: get
  status = db->Get(read_options, "key0741", &value);
  if (status.ok()) {
    out << "GET key0741 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30320: put
  db->Put(write_options, "key5678", "value74050");

  // op 30321: put
  db->Put(write_options, "key7317", "value38982");

  // op 30322: get
  status = db->Get(read_options, "key5019", &value);
  if (status.ok()) {
    out << "GET key5019 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30323: delete_range
  status = db->DeleteRange(write_options, "key1915", "key7181");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 30324: delete_range
  status = db->DeleteRange(write_options, "key7916", "key9138");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 30325: put
  db->Put(write_options, "key2542", "value72096");

  // op 30326: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1689", "key11617", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1689, key11617) size=" << scan_result.size() << std::endl;
  }

  // op 30327: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8139", "key9461", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8139, key9461) size=" << scan_result.size() << std::endl;
  }

  // op 30328: delete
  db->Delete(write_options, "key9822");

  // op 30329: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3795", "key4911", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3795, key4911) size=" << scan_result.size() << std::endl;
  }

  // op 30330: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5962", "key11130", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5962, key11130) size=" << scan_result.size() << std::endl;
  }

  // op 30331: put
  db->Put(write_options, "key5491", "value80712");

  // op 30332: put
  db->Put(write_options, "key11230", "value54632");

  // op 30333: put
  db->Put(write_options, "key11720", "value31616");

  // op 30334: put
  db->Put(write_options, "key0889", "value32186");

  // op 30335: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 30336: get
  status = db->Get(read_options, "key1380", &value);
  if (status.ok()) {
    out << "GET key1380 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30337: delete
  db->Delete(write_options, "key1573");

  // op 30338: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4930", "key10010", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4930, key10010) size=" << scan_result.size() << std::endl;
  }

  // op 30339: delete_range
  status = db->DeleteRange(write_options, "key2705", "key4429");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 30340: put
  db->Put(write_options, "key1542", "value51969");

  // op 30341: delete
  db->Delete(write_options, "key11716");

  // op 30342: put
  db->Put(write_options, "key1442", "value58172");

  // op 30343: put
  db->Put(write_options, "key11312", "value28212");

  // op 30344: put
  db->Put(write_options, "key10841", "value99350");

  // op 30345: put
  db->Put(write_options, "key11415", "value81528");

  // op 30346: put
  db->Put(write_options, "key8570", "value85966");

  // op 30347: put
  db->Put(write_options, "key5076", "value92442");

  // op 30348: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3787", "key4632", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3787, key4632) size=" << scan_result.size() << std::endl;
  }

  // op 30349: get
  status = db->Get(read_options, "key5380", &value);
  if (status.ok()) {
    out << "GET key5380 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30350: put
  db->Put(write_options, "key8434", "value94147");

  // op 30351: put
  db->Put(write_options, "key0136", "value38070");

  // op 30352: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0891", "key6621", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0891, key6621) size=" << scan_result.size() << std::endl;
  }

  // op 30353: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1692", "key5709", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1692, key5709) size=" << scan_result.size() << std::endl;
  }

  // op 30354: get
  status = db->Get(read_options, "key0634", &value);
  if (status.ok()) {
    out << "GET key0634 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30355: get
  status = db->Get(read_options, "key2100", &value);
  if (status.ok()) {
    out << "GET key2100 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30356: put
  db->Put(write_options, "key10568", "value36850");

  // op 30357: put
  db->Put(write_options, "key3967", "value93482");

  // op 30358: get
  status = db->Get(read_options, "key9075", &value);
  if (status.ok()) {
    out << "GET key9075 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30359: put
  db->Put(write_options, "key10342", "value11957");

  // op 30360: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5170", "key8440", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5170, key8440) size=" << scan_result.size() << std::endl;
  }

  // op 30361: delete_range
  status = db->DeleteRange(write_options, "key7380", "key10873");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 30362: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 30363: put
  db->Put(write_options, "key5496", "value86334");

  // op 30364: put
  db->Put(write_options, "key4811", "value83993");

  // op 30365: put
  db->Put(write_options, "key1275", "value110943");

  // op 30366: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7136", "key10132", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7136, key10132) size=" << scan_result.size() << std::endl;
  }

  // op 30367: delete_range
  status = db->DeleteRange(write_options, "key2512", "key2727");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 30368: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 30369: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3399", "key7811", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3399, key7811) size=" << scan_result.size() << std::endl;
  }

  // op 30370: put
  db->Put(write_options, "key11538", "value24527");

  // op 30371: delete
  db->Delete(write_options, "key5383");

  // op 30372: put
  db->Put(write_options, "key1094", "value34989");

  // op 30373: delete
  db->Delete(write_options, "key1523");

  // op 30374: delete_range
  status = db->DeleteRange(write_options, "key1045", "key7984");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 30375: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1741", "key5078", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1741, key5078) size=" << scan_result.size() << std::endl;
  }

  // op 30376: delete
  db->Delete(write_options, "key8264");

  // op 30377: get
  status = db->Get(read_options, "key9404", &value);
  if (status.ok()) {
    out << "GET key9404 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30378: delete_range
  status = db->DeleteRange(write_options, "key4898", "key7674");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 30379: get
  status = db->Get(read_options, "key5482", &value);
  if (status.ok()) {
    out << "GET key5482 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30380: get
  status = db->Get(read_options, "key3653", &value);
  if (status.ok()) {
    out << "GET key3653 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30381: get
  status = db->Get(read_options, "key5756", &value);
  if (status.ok()) {
    out << "GET key5756 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30382: put
  db->Put(write_options, "key2630", "value112666");

  // op 30383: put
  db->Put(write_options, "key8563", "value64341");

  // op 30384: put
  db->Put(write_options, "key8343", "value94147");

  // op 30385: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2266", "key11582", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2266, key11582) size=" << scan_result.size() << std::endl;
  }

  // op 30386: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 30387: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 30388: put
  db->Put(write_options, "key2784", "value118183");

  // op 30389: delete
  db->Delete(write_options, "key1818");

  // op 30390: put
  db->Put(write_options, "key0265", "value51184");

  // op 30391: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 30392: get
  status = db->Get(read_options, "key5767", &value);
  if (status.ok()) {
    out << "GET key5767 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30393: put
  db->Put(write_options, "key1098", "value114980");

  // op 30394: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5467", "key6116", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5467, key6116) size=" << scan_result.size() << std::endl;
  }

  // op 30395: put
  db->Put(write_options, "key0471", "value93147");

  // op 30396: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0448", "key7189", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0448, key7189) size=" << scan_result.size() << std::endl;
  }

  // op 30397: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2991", "key7197", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2991, key7197) size=" << scan_result.size() << std::endl;
  }

  // op 30398: get
  status = db->Get(read_options, "key7855", &value);
  if (status.ok()) {
    out << "GET key7855 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30399: get
  status = db->Get(read_options, "key3393", &value);
  if (status.ok()) {
    out << "GET key3393 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30400: put
  db->Put(write_options, "key11247", "value27025");

  // op 30401: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 30402: put
  db->Put(write_options, "key11616", "value23304");

  // op 30403: get
  status = db->Get(read_options, "key7793", &value);
  if (status.ok()) {
    out << "GET key7793 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30404: put
  db->Put(write_options, "key5466", "value83975");

  // op 30405: put
  db->Put(write_options, "key11504", "value48369");

  // op 30406: get
  status = db->Get(read_options, "key0778", &value);
  if (status.ok()) {
    out << "GET key0778 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30407: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 30408: put
  db->Put(write_options, "key1998", "value8089");

  // op 30409: get
  status = db->Get(read_options, "key10556", &value);
  if (status.ok()) {
    out << "GET key10556 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30410: get
  status = db->Get(read_options, "key3381", &value);
  if (status.ok()) {
    out << "GET key3381 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30411: delete
  db->Delete(write_options, "key8239");

  // op 30412: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9676", "key10368", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9676, key10368) size=" << scan_result.size() << std::endl;
  }

  // op 30413: delete_range
  status = db->DeleteRange(write_options, "key3123", "key4096");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 30414: put
  db->Put(write_options, "key3289", "value63735");

  // op 30415: delete
  db->Delete(write_options, "key0954");

  // op 30416: put
  db->Put(write_options, "key0839", "value84749");

  // op 30417: get
  status = db->Get(read_options, "key11584", &value);
  if (status.ok()) {
    out << "GET key11584 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30418: get
  status = db->Get(read_options, "key2212", &value);
  if (status.ok()) {
    out << "GET key2212 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30419: put
  db->Put(write_options, "key8043", "value41700");

  // op 30420: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4070", "key8300", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4070, key8300) size=" << scan_result.size() << std::endl;
  }

  // op 30421: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4552", "key8860", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4552, key8860) size=" << scan_result.size() << std::endl;
  }

  // op 30422: put
  db->Put(write_options, "key3040", "value0292");

  // op 30423: put
  db->Put(write_options, "key11390", "value97878");

  // op 30424: put
  db->Put(write_options, "key9449", "value31606");

  // op 30425: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0526", "key3587", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0526, key3587) size=" << scan_result.size() << std::endl;
  }

  // op 30426: get
  status = db->Get(read_options, "key9554", &value);
  if (status.ok()) {
    out << "GET key9554 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30427: put
  db->Put(write_options, "key4444", "value108892");

  // op 30428: get
  status = db->Get(read_options, "key0509", &value);
  if (status.ok()) {
    out << "GET key0509 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30429: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3756", "key5256", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3756, key5256) size=" << scan_result.size() << std::endl;
  }

  // op 30430: get
  status = db->Get(read_options, "key11599", &value);
  if (status.ok()) {
    out << "GET key11599 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30431: put
  db->Put(write_options, "key10148", "value78844");

  // op 30432: put
  db->Put(write_options, "key7245", "value30809");

  // op 30433: put
  db->Put(write_options, "key0683", "value98339");

  // op 30434: put
  db->Put(write_options, "key11697", "value55118");

  // op 30435: put
  db->Put(write_options, "key7328", "value66171");

  // op 30436: delete
  db->Delete(write_options, "key4580");

  // op 30437: put
  db->Put(write_options, "key5339", "value1880");

  // op 30438: delete_range
  status = db->DeleteRange(write_options, "key0966", "key1908");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 30439: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1596", "key4900", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1596, key4900) size=" << scan_result.size() << std::endl;
  }

  // op 30440: get
  status = db->Get(read_options, "key6097", &value);
  if (status.ok()) {
    out << "GET key6097 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30441: delete
  db->Delete(write_options, "key4262");

  // op 30442: delete_range
  status = db->DeleteRange(write_options, "key4154", "key10897");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 30443: get
  status = db->Get(read_options, "key7570", &value);
  if (status.ok()) {
    out << "GET key7570 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30444: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5116", "key6699", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5116, key6699) size=" << scan_result.size() << std::endl;
  }

  // op 30445: get
  status = db->Get(read_options, "key11651", &value);
  if (status.ok()) {
    out << "GET key11651 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30446: delete
  db->Delete(write_options, "key9015");

  // op 30447: put
  db->Put(write_options, "key9200", "value51059");

  // op 30448: delete
  db->Delete(write_options, "key11137");

  // op 30449: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7441", "key10250", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7441, key10250) size=" << scan_result.size() << std::endl;
  }

  // op 30450: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 30451: get
  status = db->Get(read_options, "key3125", &value);
  if (status.ok()) {
    out << "GET key3125 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30452: put
  db->Put(write_options, "key9209", "value22183");

  // op 30453: delete
  db->Delete(write_options, "key3417");

  // op 30454: get
  status = db->Get(read_options, "key8233", &value);
  if (status.ok()) {
    out << "GET key8233 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30455: delete
  db->Delete(write_options, "key6424");

  // op 30456: delete
  db->Delete(write_options, "key4212");

  // op 30457: get
  status = db->Get(read_options, "key0349", &value);
  if (status.ok()) {
    out << "GET key0349 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30458: put
  db->Put(write_options, "key5906", "value66243");

  // op 30459: put
  db->Put(write_options, "key1820", "value22881");

  // op 30460: put
  db->Put(write_options, "key11158", "value111629");

  // op 30461: get
  status = db->Get(read_options, "key5474", &value);
  if (status.ok()) {
    out << "GET key5474 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30462: get
  status = db->Get(read_options, "key10007", &value);
  if (status.ok()) {
    out << "GET key10007 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30463: get
  status = db->Get(read_options, "key1426", &value);
  if (status.ok()) {
    out << "GET key1426 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30464: get
  status = db->Get(read_options, "key0341", &value);
  if (status.ok()) {
    out << "GET key0341 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30465: put
  db->Put(write_options, "key8153", "value72244");

  // op 30466: put
  db->Put(write_options, "key0349", "value66355");

  // op 30467: delete_range
  status = db->DeleteRange(write_options, "key7134", "key8753");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 30468: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 30469: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1601", "key9816", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1601, key9816) size=" << scan_result.size() << std::endl;
  }

  // op 30470: delete
  db->Delete(write_options, "key4645");

  // op 30471: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2851", "key3477", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2851, key3477) size=" << scan_result.size() << std::endl;
  }

  // op 30472: delete_range
  status = db->DeleteRange(write_options, "key5076", "key6424");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 30473: delete_range
  status = db->DeleteRange(write_options, "key5322", "key6649");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 30474: get
  status = db->Get(read_options, "key11037", &value);
  if (status.ok()) {
    out << "GET key11037 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30475: put
  db->Put(write_options, "key0071", "value109701");

  // op 30476: put
  db->Put(write_options, "key2444", "value112046");

  // op 30477: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5164", "key10048", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5164, key10048) size=" << scan_result.size() << std::endl;
  }

  // op 30478: delete
  db->Delete(write_options, "key1510");

  // op 30479: put
  db->Put(write_options, "key10048", "value93772");

  // op 30480: delete_range
  status = db->DeleteRange(write_options, "key10931", "key11072");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 30481: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0914", "key2582", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0914, key2582) size=" << scan_result.size() << std::endl;
  }

  // op 30482: get
  status = db->Get(read_options, "key8541", &value);
  if (status.ok()) {
    out << "GET key8541 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30483: get
  status = db->Get(read_options, "key2523", &value);
  if (status.ok()) {
    out << "GET key2523 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30484: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0433", "key11441", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0433, key11441) size=" << scan_result.size() << std::endl;
  }

  // op 30485: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 30486: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 30487: put
  db->Put(write_options, "key7860", "value77753");

  // op 30488: get
  status = db->Get(read_options, "key1774", &value);
  if (status.ok()) {
    out << "GET key1774 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30489: put
  db->Put(write_options, "key0085", "value22587");

  // op 30490: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7369", "key9332", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7369, key9332) size=" << scan_result.size() << std::endl;
  }

  // op 30491: delete
  db->Delete(write_options, "key4086");

  // op 30492: put
  db->Put(write_options, "key3861", "value75930");

  // op 30493: put
  db->Put(write_options, "key4834", "value16583");

  // op 30494: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 30495: put
  db->Put(write_options, "key3218", "value61786");

  // op 30496: get
  status = db->Get(read_options, "key4531", &value);
  if (status.ok()) {
    out << "GET key4531 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30497: get
  status = db->Get(read_options, "key1324", &value);
  if (status.ok()) {
    out << "GET key1324 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30498: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 30499: put
  db->Put(write_options, "key10381", "value107715");

  // op 30500: put
  db->Put(write_options, "key8115", "value28729");

  // op 30501: get
  status = db->Get(read_options, "key10878", &value);
  if (status.ok()) {
    out << "GET key10878 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30502: delete_range
  status = db->DeleteRange(write_options, "key4988", "key11013");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 30503: delete_range
  status = db->DeleteRange(write_options, "key4470", "key9837");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 30504: delete_range
  status = db->DeleteRange(write_options, "key1996", "key5484");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 30505: put
  db->Put(write_options, "key4327", "value76539");

  // op 30506: delete_range
  status = db->DeleteRange(write_options, "key1837", "key7417");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 30507: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1176", "key7917", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1176, key7917) size=" << scan_result.size() << std::endl;
  }

  // op 30508: delete
  db->Delete(write_options, "key2985");

  // op 30509: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1907", "key8530", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1907, key8530) size=" << scan_result.size() << std::endl;
  }

  // op 30510: delete
  db->Delete(write_options, "key8340");

  // op 30511: put
  db->Put(write_options, "key9667", "value49049");

  // op 30512: delete
  db->Delete(write_options, "key0230");

  // op 30513: put
  db->Put(write_options, "key8795", "value67355");

  // op 30514: put
  db->Put(write_options, "key2501", "value33425");

  // op 30515: put
  db->Put(write_options, "key9555", "value16237");

  // op 30516: put
  db->Put(write_options, "key9827", "value26978");

  // op 30517: get
  status = db->Get(read_options, "key0933", &value);
  if (status.ok()) {
    out << "GET key0933 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30518: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0485", "key3651", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0485, key3651) size=" << scan_result.size() << std::endl;
  }

  // op 30519: get
  status = db->Get(read_options, "key5908", &value);
  if (status.ok()) {
    out << "GET key5908 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30520: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2745", "key9236", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2745, key9236) size=" << scan_result.size() << std::endl;
  }

  // op 30521: delete
  db->Delete(write_options, "key3579");

  // op 30522: delete
  db->Delete(write_options, "key1734");

  // op 30523: delete_range
  status = db->DeleteRange(write_options, "key2011", "key11011");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 30524: get
  status = db->Get(read_options, "key6163", &value);
  if (status.ok()) {
    out << "GET key6163 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30525: delete_range
  status = db->DeleteRange(write_options, "key7194", "key10742");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 30526: put
  db->Put(write_options, "key9889", "value43452");

  // op 30527: delete_range
  status = db->DeleteRange(write_options, "key2344", "key3244");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 30528: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5629", "key11624", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5629, key11624) size=" << scan_result.size() << std::endl;
  }

  // op 30529: delete_range
  status = db->DeleteRange(write_options, "key0984", "key4412");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 30530: get
  status = db->Get(read_options, "key1597", &value);
  if (status.ok()) {
    out << "GET key1597 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30531: put
  db->Put(write_options, "key8552", "value109391");

  // op 30532: put
  db->Put(write_options, "key6358", "value34858");

  // op 30533: put
  db->Put(write_options, "key10743", "value111963");

  // op 30534: put
  db->Put(write_options, "key6871", "value57072");

  // op 30535: delete
  db->Delete(write_options, "key8768");

  // op 30536: put
  db->Put(write_options, "key10524", "value111612");

  // op 30537: get
  status = db->Get(read_options, "key11450", &value);
  if (status.ok()) {
    out << "GET key11450 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30538: delete
  db->Delete(write_options, "key5898");

  // op 30539: put
  db->Put(write_options, "key8859", "value113048");

  // op 30540: delete
  db->Delete(write_options, "key9496");

  // op 30541: put
  db->Put(write_options, "key6418", "value23977");

  // op 30542: delete
  db->Delete(write_options, "key7094");

  // op 30543: get
  status = db->Get(read_options, "key1559", &value);
  if (status.ok()) {
    out << "GET key1559 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30544: put
  db->Put(write_options, "key6850", "value63827");

  // op 30545: put
  db->Put(write_options, "key4850", "value33925");

  // op 30546: delete_range
  status = db->DeleteRange(write_options, "key1023", "key4003");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 30547: put
  db->Put(write_options, "key2082", "value13484");

  // op 30548: put
  db->Put(write_options, "key4363", "value24092");

  // op 30549: put
  db->Put(write_options, "key11047", "value87927");

  // op 30550: delete
  db->Delete(write_options, "key6403");

  // op 30551: put
  db->Put(write_options, "key0775", "value75943");

  // op 30552: put
  db->Put(write_options, "key1797", "value12398");

  // op 30553: delete_range
  status = db->DeleteRange(write_options, "key6643", "key10855");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 30554: get
  status = db->Get(read_options, "key7288", &value);
  if (status.ok()) {
    out << "GET key7288 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30555: delete
  db->Delete(write_options, "key0279");

  // op 30556: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4119", "key8491", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4119, key8491) size=" << scan_result.size() << std::endl;
  }

  // op 30557: delete
  db->Delete(write_options, "key4018");

  // op 30558: put
  db->Put(write_options, "key2349", "value66123");

  // op 30559: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7545", "key8717", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7545, key8717) size=" << scan_result.size() << std::endl;
  }

  // op 30560: put
  db->Put(write_options, "key9909", "value47868");

  // op 30561: get
  status = db->Get(read_options, "key2552", &value);
  if (status.ok()) {
    out << "GET key2552 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30562: get
  status = db->Get(read_options, "key9596", &value);
  if (status.ok()) {
    out << "GET key9596 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30563: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0954", "key11664", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0954, key11664) size=" << scan_result.size() << std::endl;
  }

  // op 30564: put
  db->Put(write_options, "key4935", "value60607");

  // op 30565: delete_range
  status = db->DeleteRange(write_options, "key4535", "key10139");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 30566: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7444", "key10979", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7444, key10979) size=" << scan_result.size() << std::endl;
  }

  // op 30567: get
  status = db->Get(read_options, "key4596", &value);
  if (status.ok()) {
    out << "GET key4596 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30568: get
  status = db->Get(read_options, "key4144", &value);
  if (status.ok()) {
    out << "GET key4144 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30569: get
  status = db->Get(read_options, "key11246", &value);
  if (status.ok()) {
    out << "GET key11246 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30570: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1297", "key1385", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1297, key1385) size=" << scan_result.size() << std::endl;
  }

  // op 30571: delete
  db->Delete(write_options, "key9916");

  // op 30572: get
  status = db->Get(read_options, "key10228", &value);
  if (status.ok()) {
    out << "GET key10228 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30573: put
  db->Put(write_options, "key0084", "value58581");

  // op 30574: delete_range
  status = db->DeleteRange(write_options, "key3369", "key8744");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 30575: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 30576: put
  db->Put(write_options, "key7614", "value33113");

  // op 30577: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5264", "key6856", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5264, key6856) size=" << scan_result.size() << std::endl;
  }

  // op 30578: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3083", "key4713", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3083, key4713) size=" << scan_result.size() << std::endl;
  }

  // op 30579: get
  status = db->Get(read_options, "key2149", &value);
  if (status.ok()) {
    out << "GET key2149 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30580: put
  db->Put(write_options, "key2421", "value24520");

  // op 30581: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5891", "key11560", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5891, key11560) size=" << scan_result.size() << std::endl;
  }

  // op 30582: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5729", "key11289", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5729, key11289) size=" << scan_result.size() << std::endl;
  }

  // op 30583: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 30584: put
  db->Put(write_options, "key2080", "value75603");

  // op 30585: put
  db->Put(write_options, "key3396", "value117421");

  // op 30586: put
  db->Put(write_options, "key10110", "value40413");

  // op 30587: put
  db->Put(write_options, "key1939", "value81632");

  // op 30588: delete_range
  status = db->DeleteRange(write_options, "key5558", "key7972");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 30589: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 30590: delete_range
  status = db->DeleteRange(write_options, "key6363", "key7554");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 30591: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2705", "key6079", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2705, key6079) size=" << scan_result.size() << std::endl;
  }

  // op 30592: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 30593: delete_range
  status = db->DeleteRange(write_options, "key11147", "key11491");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 30594: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 30595: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6224", "key7307", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6224, key7307) size=" << scan_result.size() << std::endl;
  }

  // op 30596: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 30597: delete_range
  status = db->DeleteRange(write_options, "key1554", "key4630");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 30598: put
  db->Put(write_options, "key4000", "value54588");

  // op 30599: put
  db->Put(write_options, "key5533", "value42704");

  // op 30600: put
  db->Put(write_options, "key1306", "value88232");

  // op 30601: put
  db->Put(write_options, "key5250", "value2456");

  // op 30602: put
  db->Put(write_options, "key1674", "value47885");

  // op 30603: delete
  db->Delete(write_options, "key8818");

  // op 30604: get
  status = db->Get(read_options, "key11543", &value);
  if (status.ok()) {
    out << "GET key11543 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30605: put
  db->Put(write_options, "key9304", "value48326");

  // op 30606: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 30607: get
  status = db->Get(read_options, "key10581", &value);
  if (status.ok()) {
    out << "GET key10581 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30608: put
  db->Put(write_options, "key3862", "value27568");

  // op 30609: delete
  db->Delete(write_options, "key4941");

  // op 30610: put
  db->Put(write_options, "key7600", "value100730");

  // op 30611: get
  status = db->Get(read_options, "key0962", &value);
  if (status.ok()) {
    out << "GET key0962 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30612: put
  db->Put(write_options, "key4509", "value91470");

  // op 30613: put
  db->Put(write_options, "key10350", "value51246");

  // op 30614: delete
  db->Delete(write_options, "key8321");

  // op 30615: delete_range
  status = db->DeleteRange(write_options, "key9270", "key11039");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 30616: get
  status = db->Get(read_options, "key3165", &value);
  if (status.ok()) {
    out << "GET key3165 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30617: put
  db->Put(write_options, "key0256", "value28785");

  // op 30618: delete_range
  status = db->DeleteRange(write_options, "key6022", "key6486");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 30619: get
  status = db->Get(read_options, "key0234", &value);
  if (status.ok()) {
    out << "GET key0234 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30620: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6188", "key10644", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6188, key10644) size=" << scan_result.size() << std::endl;
  }

  // op 30621: get
  status = db->Get(read_options, "key2000", &value);
  if (status.ok()) {
    out << "GET key2000 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30622: put
  db->Put(write_options, "key5492", "value31450");

  // op 30623: get
  status = db->Get(read_options, "key6265", &value);
  if (status.ok()) {
    out << "GET key6265 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30624: put
  db->Put(write_options, "key9598", "value81632");

  // op 30625: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4021", "key7491", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4021, key7491) size=" << scan_result.size() << std::endl;
  }

  // op 30626: put
  db->Put(write_options, "key10979", "value35184");

  // op 30627: put
  db->Put(write_options, "key10347", "value30704");

  // op 30628: delete_range
  status = db->DeleteRange(write_options, "key4138", "key11534");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 30629: put
  db->Put(write_options, "key0803", "value35428");

  // op 30630: get
  status = db->Get(read_options, "key9542", &value);
  if (status.ok()) {
    out << "GET key9542 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30631: put
  db->Put(write_options, "key4263", "value26826");

  // op 30632: put
  db->Put(write_options, "key9936", "value90916");

  // op 30633: put
  db->Put(write_options, "key3722", "value3051");

  // op 30634: delete_range
  status = db->DeleteRange(write_options, "key0329", "key8921");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 30635: delete_range
  status = db->DeleteRange(write_options, "key3105", "key7819");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 30636: get
  status = db->Get(read_options, "key5725", &value);
  if (status.ok()) {
    out << "GET key5725 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30637: put
  db->Put(write_options, "key9210", "value89792");

  // op 30638: get
  status = db->Get(read_options, "key0906", &value);
  if (status.ok()) {
    out << "GET key0906 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30639: put
  db->Put(write_options, "key6255", "value91955");

  // op 30640: put
  db->Put(write_options, "key4692", "value47258");

  // op 30641: put
  db->Put(write_options, "key4632", "value0871");

  // op 30642: get
  status = db->Get(read_options, "key6266", &value);
  if (status.ok()) {
    out << "GET key6266 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30643: get
  status = db->Get(read_options, "key2866", &value);
  if (status.ok()) {
    out << "GET key2866 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30644: put
  db->Put(write_options, "key4848", "value16571");

  // op 30645: delete_range
  status = db->DeleteRange(write_options, "key3040", "key11389");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 30646: put
  db->Put(write_options, "key4233", "value98380");

  // op 30647: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0389", "key5386", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0389, key5386) size=" << scan_result.size() << std::endl;
  }

  // op 30648: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 30649: delete_range
  status = db->DeleteRange(write_options, "key1017", "key4697");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 30650: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1737", "key5170", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1737, key5170) size=" << scan_result.size() << std::endl;
  }

  // op 30651: put
  db->Put(write_options, "key9782", "value11831");

  // op 30652: put
  db->Put(write_options, "key2774", "value114537");

  // op 30653: get
  status = db->Get(read_options, "key4802", &value);
  if (status.ok()) {
    out << "GET key4802 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30654: delete
  db->Delete(write_options, "key10511");

  // op 30655: delete_range
  status = db->DeleteRange(write_options, "key5428", "key10280");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 30656: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6054", "key8053", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6054, key8053) size=" << scan_result.size() << std::endl;
  }

  // op 30657: delete
  db->Delete(write_options, "key11550");

  // op 30658: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2811", "key6165", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2811, key6165) size=" << scan_result.size() << std::endl;
  }

  // op 30659: delete
  db->Delete(write_options, "key10144");

  // op 30660: get
  status = db->Get(read_options, "key8832", &value);
  if (status.ok()) {
    out << "GET key8832 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30661: delete_range
  status = db->DeleteRange(write_options, "key1023", "key8002");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 30662: put
  db->Put(write_options, "key0768", "value103887");

  // op 30663: put
  db->Put(write_options, "key1443", "value96137");

  // op 30664: delete
  db->Delete(write_options, "key5679");

  // op 30665: put
  db->Put(write_options, "key4401", "value81725");

  // op 30666: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1316", "key3967", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1316, key3967) size=" << scan_result.size() << std::endl;
  }

  // op 30667: get
  status = db->Get(read_options, "key8926", &value);
  if (status.ok()) {
    out << "GET key8926 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30668: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6020", "key11207", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6020, key11207) size=" << scan_result.size() << std::endl;
  }

  // op 30669: get
  status = db->Get(read_options, "key11557", &value);
  if (status.ok()) {
    out << "GET key11557 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30670: get
  status = db->Get(read_options, "key2972", &value);
  if (status.ok()) {
    out << "GET key2972 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30671: delete
  db->Delete(write_options, "key9548");

  // op 30672: put
  db->Put(write_options, "key5264", "value80936");

  // op 30673: put
  db->Put(write_options, "key1220", "value61125");

  // op 30674: get
  status = db->Get(read_options, "key9973", &value);
  if (status.ok()) {
    out << "GET key9973 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30675: put
  db->Put(write_options, "key8450", "value8280");

  // op 30676: put
  db->Put(write_options, "key10104", "value27124");

  // op 30677: delete_range
  status = db->DeleteRange(write_options, "key2704", "key10919");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 30678: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7098", "key10173", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7098, key10173) size=" << scan_result.size() << std::endl;
  }

  // op 30679: put
  db->Put(write_options, "key10321", "value79974");

  // op 30680: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4099", "key10092", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4099, key10092) size=" << scan_result.size() << std::endl;
  }

  // op 30681: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9540", "key10851", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9540, key10851) size=" << scan_result.size() << std::endl;
  }

  // op 30682: put
  db->Put(write_options, "key10929", "value115611");

  // op 30683: get
  status = db->Get(read_options, "key11263", &value);
  if (status.ok()) {
    out << "GET key11263 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30684: put
  db->Put(write_options, "key4621", "value78455");

  // op 30685: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 30686: delete
  db->Delete(write_options, "key0254");

  // op 30687: put
  db->Put(write_options, "key8789", "value4027");

  // op 30688: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2327", "key6569", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2327, key6569) size=" << scan_result.size() << std::endl;
  }

  // op 30689: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0702", "key3967", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0702, key3967) size=" << scan_result.size() << std::endl;
  }

  // op 30690: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3597", "key6268", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3597, key6268) size=" << scan_result.size() << std::endl;
  }

  // op 30691: get
  status = db->Get(read_options, "key6365", &value);
  if (status.ok()) {
    out << "GET key6365 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30692: get
  status = db->Get(read_options, "key4124", &value);
  if (status.ok()) {
    out << "GET key4124 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30693: put
  db->Put(write_options, "key4935", "value87353");

  // op 30694: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 30695: put
  db->Put(write_options, "key1913", "value24438");

  // op 30696: delete_range
  status = db->DeleteRange(write_options, "key7233", "key7390");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 30697: delete
  db->Delete(write_options, "key8998");

  // op 30698: delete
  db->Delete(write_options, "key5644");

  // op 30699: put
  db->Put(write_options, "key3168", "value108591");

  // op 30700: put
  db->Put(write_options, "key5562", "value47494");

  // op 30701: get
  status = db->Get(read_options, "key5353", &value);
  if (status.ok()) {
    out << "GET key5353 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30702: get
  status = db->Get(read_options, "key10580", &value);
  if (status.ok()) {
    out << "GET key10580 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30703: put
  db->Put(write_options, "key7303", "value89364");

  // op 30704: delete_range
  status = db->DeleteRange(write_options, "key1369", "key2897");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 30705: get
  status = db->Get(read_options, "key8064", &value);
  if (status.ok()) {
    out << "GET key8064 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30706: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0878", "key0995", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0878, key0995) size=" << scan_result.size() << std::endl;
  }

  // op 30707: delete
  db->Delete(write_options, "key7984");

  // op 30708: put
  db->Put(write_options, "key4434", "value110714");

  // op 30709: put
  db->Put(write_options, "key2182", "value39835");

  // op 30710: delete
  db->Delete(write_options, "key1447");

  // op 30711: get
  status = db->Get(read_options, "key4044", &value);
  if (status.ok()) {
    out << "GET key4044 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30712: get
  status = db->Get(read_options, "key8930", &value);
  if (status.ok()) {
    out << "GET key8930 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30713: put
  db->Put(write_options, "key4641", "value1873");

  // op 30714: put
  db->Put(write_options, "key2526", "value54436");

  // op 30715: put
  db->Put(write_options, "key1795", "value62145");

  // op 30716: get
  status = db->Get(read_options, "key9880", &value);
  if (status.ok()) {
    out << "GET key9880 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30717: put
  db->Put(write_options, "key11036", "value38893");

  // op 30718: get
  status = db->Get(read_options, "key11243", &value);
  if (status.ok()) {
    out << "GET key11243 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30719: put
  db->Put(write_options, "key10930", "value104119");

  // op 30720: put
  db->Put(write_options, "key4232", "value9082");

  // op 30721: get
  status = db->Get(read_options, "key9092", &value);
  if (status.ok()) {
    out << "GET key9092 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30722: delete_range
  status = db->DeleteRange(write_options, "key5563", "key9785");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 30723: delete
  db->Delete(write_options, "key3570");

  // op 30724: get
  status = db->Get(read_options, "key4205", &value);
  if (status.ok()) {
    out << "GET key4205 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30725: put
  db->Put(write_options, "key8493", "value45152");

  // op 30726: delete_range
  status = db->DeleteRange(write_options, "key0752", "key3098");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 30727: put
  db->Put(write_options, "key4829", "value3079");

  // op 30728: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 30729: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 30730: get
  status = db->Get(read_options, "key7570", &value);
  if (status.ok()) {
    out << "GET key7570 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30731: put
  db->Put(write_options, "key1858", "value49874");

  // op 30732: get
  status = db->Get(read_options, "key10966", &value);
  if (status.ok()) {
    out << "GET key10966 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30733: put
  db->Put(write_options, "key5893", "value54440");

  // op 30734: get
  status = db->Get(read_options, "key4086", &value);
  if (status.ok()) {
    out << "GET key4086 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30735: put
  db->Put(write_options, "key9717", "value89001");

  // op 30736: delete_range
  status = db->DeleteRange(write_options, "key3234", "key9811");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 30737: delete_range
  status = db->DeleteRange(write_options, "key4134", "key8145");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 30738: delete_range
  status = db->DeleteRange(write_options, "key3785", "key8090");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 30739: get
  status = db->Get(read_options, "key5836", &value);
  if (status.ok()) {
    out << "GET key5836 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30740: put
  db->Put(write_options, "key4252", "value53089");

  // op 30741: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9503", "key11131", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9503, key11131) size=" << scan_result.size() << std::endl;
  }

  // op 30742: put
  db->Put(write_options, "key4168", "value106082");

  // op 30743: put
  db->Put(write_options, "key10081", "value73309");

  // op 30744: get
  status = db->Get(read_options, "key4447", &value);
  if (status.ok()) {
    out << "GET key4447 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30745: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6449", "key6841", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6449, key6841) size=" << scan_result.size() << std::endl;
  }

  // op 30746: put
  db->Put(write_options, "key5467", "value19712");

  // op 30747: delete
  db->Delete(write_options, "key8524");

  // op 30748: get
  status = db->Get(read_options, "key10046", &value);
  if (status.ok()) {
    out << "GET key10046 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30749: delete
  db->Delete(write_options, "key0698");

  // op 30750: put
  db->Put(write_options, "key6475", "value58001");

  // op 30751: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3096", "key10502", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3096, key10502) size=" << scan_result.size() << std::endl;
  }

  // op 30752: delete_range
  status = db->DeleteRange(write_options, "key7347", "key7886");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 30753: put
  db->Put(write_options, "key1658", "value28018");

  // op 30754: delete
  db->Delete(write_options, "key4845");

  // op 30755: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10908", "key11543", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10908, key11543) size=" << scan_result.size() << std::endl;
  }

  // op 30756: delete_range
  status = db->DeleteRange(write_options, "key2731", "key3177");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 30757: get
  status = db->Get(read_options, "key11425", &value);
  if (status.ok()) {
    out << "GET key11425 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30758: get
  status = db->Get(read_options, "key6328", &value);
  if (status.ok()) {
    out << "GET key6328 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30759: put
  db->Put(write_options, "key8077", "value57978");

  // op 30760: put
  db->Put(write_options, "key4090", "value82921");

  // op 30761: put
  db->Put(write_options, "key10479", "value97039");

  // op 30762: put
  db->Put(write_options, "key2874", "value86028");

  // op 30763: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3195", "key9974", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3195, key9974) size=" << scan_result.size() << std::endl;
  }

  // op 30764: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4593", "key10213", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4593, key10213) size=" << scan_result.size() << std::endl;
  }

  // op 30765: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0054", "key10614", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0054, key10614) size=" << scan_result.size() << std::endl;
  }

  // op 30766: put
  db->Put(write_options, "key8447", "value54564");

  // op 30767: put
  db->Put(write_options, "key2250", "value86184");

  // op 30768: delete_range
  status = db->DeleteRange(write_options, "key5410", "key7933");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 30769: put
  db->Put(write_options, "key3600", "value106487");

  // op 30770: put
  db->Put(write_options, "key9299", "value81716");

  // op 30771: put
  db->Put(write_options, "key4258", "value105698");

  // op 30772: put
  db->Put(write_options, "key4116", "value114642");

  // op 30773: get
  status = db->Get(read_options, "key0024", &value);
  if (status.ok()) {
    out << "GET key0024 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30774: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3628", "key9083", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3628, key9083) size=" << scan_result.size() << std::endl;
  }

  // op 30775: get
  status = db->Get(read_options, "key9182", &value);
  if (status.ok()) {
    out << "GET key9182 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30776: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0248", "key3823", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0248, key3823) size=" << scan_result.size() << std::endl;
  }

  // op 30777: put
  db->Put(write_options, "key10383", "value28232");

  // op 30778: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2144", "key8611", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2144, key8611) size=" << scan_result.size() << std::endl;
  }

  // op 30779: put
  db->Put(write_options, "key4376", "value6797");

  // op 30780: put
  db->Put(write_options, "key7426", "value115997");

  // op 30781: get
  status = db->Get(read_options, "key6853", &value);
  if (status.ok()) {
    out << "GET key6853 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30782: put
  db->Put(write_options, "key9268", "value113803");

  // op 30783: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 30784: get
  status = db->Get(read_options, "key10995", &value);
  if (status.ok()) {
    out << "GET key10995 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30785: put
  db->Put(write_options, "key8350", "value28474");

  // op 30786: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3211", "key3811", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3211, key3811) size=" << scan_result.size() << std::endl;
  }

  // op 30787: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1375", "key10696", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1375, key10696) size=" << scan_result.size() << std::endl;
  }

  // op 30788: get
  status = db->Get(read_options, "key7519", &value);
  if (status.ok()) {
    out << "GET key7519 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30789: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 30790: put
  db->Put(write_options, "key9928", "value48407");

  // op 30791: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7973", "key8290", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7973, key8290) size=" << scan_result.size() << std::endl;
  }

  // op 30792: get
  status = db->Get(read_options, "key3611", &value);
  if (status.ok()) {
    out << "GET key3611 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30793: put
  db->Put(write_options, "key3567", "value84825");

  // op 30794: put
  db->Put(write_options, "key11082", "value70239");

  // op 30795: get
  status = db->Get(read_options, "key1604", &value);
  if (status.ok()) {
    out << "GET key1604 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30796: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1988", "key5021", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1988, key5021) size=" << scan_result.size() << std::endl;
  }

  // op 30797: delete_range
  status = db->DeleteRange(write_options, "key6866", "key7995");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 30798: delete_range
  status = db->DeleteRange(write_options, "key5561", "key11086");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 30799: put
  db->Put(write_options, "key2416", "value30770");

  // op 30800: get
  status = db->Get(read_options, "key1299", &value);
  if (status.ok()) {
    out << "GET key1299 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30801: put
  db->Put(write_options, "key1163", "value5542");

  // op 30802: put
  db->Put(write_options, "key5393", "value76977");

  // op 30803: put
  db->Put(write_options, "key10497", "value93371");

  // op 30804: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5732", "key10660", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5732, key10660) size=" << scan_result.size() << std::endl;
  }

  // op 30805: put
  db->Put(write_options, "key10572", "value5838");

  // op 30806: get
  status = db->Get(read_options, "key3368", &value);
  if (status.ok()) {
    out << "GET key3368 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30807: put
  db->Put(write_options, "key6857", "value54703");

  // op 30808: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0153", "key0162", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0153, key0162) size=" << scan_result.size() << std::endl;
  }

  // op 30809: get
  status = db->Get(read_options, "key4190", &value);
  if (status.ok()) {
    out << "GET key4190 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30810: put
  db->Put(write_options, "key5703", "value78811");

  // op 30811: delete
  db->Delete(write_options, "key3994");

  // op 30812: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3740", "key10118", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3740, key10118) size=" << scan_result.size() << std::endl;
  }

  // op 30813: delete_range
  status = db->DeleteRange(write_options, "key6916", "key7668");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 30814: put
  db->Put(write_options, "key2991", "value7282");

  // op 30815: put
  db->Put(write_options, "key2873", "value105351");

  // op 30816: put
  db->Put(write_options, "key8829", "value19324");

  // op 30817: put
  db->Put(write_options, "key1527", "value57311");

  // op 30818: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1491", "key9629", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1491, key9629) size=" << scan_result.size() << std::endl;
  }

  // op 30819: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 30820: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 30821: put
  db->Put(write_options, "key5675", "value3825");

  // op 30822: put
  db->Put(write_options, "key3218", "value21354");

  // op 30823: get
  status = db->Get(read_options, "key10205", &value);
  if (status.ok()) {
    out << "GET key10205 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30824: delete_range
  status = db->DeleteRange(write_options, "key1242", "key1763");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 30825: put
  db->Put(write_options, "key7576", "value24532");

  // op 30826: get
  status = db->Get(read_options, "key3271", &value);
  if (status.ok()) {
    out << "GET key3271 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30827: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 30828: delete_range
  status = db->DeleteRange(write_options, "key10160", "key11009");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 30829: put
  db->Put(write_options, "key8712", "value97689");

  // op 30830: delete
  db->Delete(write_options, "key3850");

  // op 30831: put
  db->Put(write_options, "key9312", "value116445");

  // op 30832: put
  db->Put(write_options, "key0935", "value7543");

  // op 30833: delete
  db->Delete(write_options, "key6202");

  // op 30834: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 30835: put
  db->Put(write_options, "key1292", "value21621");

  // op 30836: delete
  db->Delete(write_options, "key5817");

  // op 30837: put
  db->Put(write_options, "key8002", "value48589");

  // op 30838: get
  status = db->Get(read_options, "key7649", &value);
  if (status.ok()) {
    out << "GET key7649 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30839: put
  db->Put(write_options, "key5872", "value29161");

  // op 30840: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7068", "key10412", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7068, key10412) size=" << scan_result.size() << std::endl;
  }

  // op 30841: get
  status = db->Get(read_options, "key9237", &value);
  if (status.ok()) {
    out << "GET key9237 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30842: put
  db->Put(write_options, "key2257", "value102668");

  // op 30843: delete_range
  status = db->DeleteRange(write_options, "key3396", "key10304");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 30844: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4746", "key9251", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4746, key9251) size=" << scan_result.size() << std::endl;
  }

  // op 30845: put
  db->Put(write_options, "key5886", "value88084");

  // op 30846: put
  db->Put(write_options, "key11680", "value115644");

  // op 30847: put
  db->Put(write_options, "key2765", "value30599");

  // op 30848: delete_range
  status = db->DeleteRange(write_options, "key0774", "key7577");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 30849: get
  status = db->Get(read_options, "key10581", &value);
  if (status.ok()) {
    out << "GET key10581 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30850: delete
  db->Delete(write_options, "key9150");

  // op 30851: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6163", "key11044", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6163, key11044) size=" << scan_result.size() << std::endl;
  }

  // op 30852: delete_range
  status = db->DeleteRange(write_options, "key0309", "key2251");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 30853: put
  db->Put(write_options, "key4947", "value33215");

  // op 30854: put
  db->Put(write_options, "key6750", "value10052");

  // op 30855: put
  db->Put(write_options, "key5857", "value78033");

  // op 30856: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1553", "key10889", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1553, key10889) size=" << scan_result.size() << std::endl;
  }

  // op 30857: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0876", "key2143", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0876, key2143) size=" << scan_result.size() << std::endl;
  }

  // op 30858: delete_range
  status = db->DeleteRange(write_options, "key8998", "key10124");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 30859: put
  db->Put(write_options, "key3551", "value79325");

  // op 30860: get
  status = db->Get(read_options, "key1982", &value);
  if (status.ok()) {
    out << "GET key1982 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30861: get
  status = db->Get(read_options, "key11518", &value);
  if (status.ok()) {
    out << "GET key11518 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30862: put
  db->Put(write_options, "key7273", "value3874");

  // op 30863: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6936", "key7573", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6936, key7573) size=" << scan_result.size() << std::endl;
  }

  // op 30864: put
  db->Put(write_options, "key6976", "value23231");

  // op 30865: put
  db->Put(write_options, "key9157", "value30306");

  // op 30866: put
  db->Put(write_options, "key9886", "value67395");

  // op 30867: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6577", "key8532", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6577, key8532) size=" << scan_result.size() << std::endl;
  }

  // op 30868: delete
  db->Delete(write_options, "key8287");

  // op 30869: put
  db->Put(write_options, "key9919", "value37841");

  // op 30870: get
  status = db->Get(read_options, "key1106", &value);
  if (status.ok()) {
    out << "GET key1106 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30871: delete
  db->Delete(write_options, "key6957");

  // op 30872: put
  db->Put(write_options, "key10948", "value43724");

  // op 30873: delete
  db->Delete(write_options, "key5835");

  // op 30874: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4651", "key10802", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4651, key10802) size=" << scan_result.size() << std::endl;
  }

  // op 30875: put
  db->Put(write_options, "key9344", "value68488");

  // op 30876: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3616", "key7928", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3616, key7928) size=" << scan_result.size() << std::endl;
  }

  // op 30877: get
  status = db->Get(read_options, "key0283", &value);
  if (status.ok()) {
    out << "GET key0283 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30878: delete_range
  status = db->DeleteRange(write_options, "key9321", "key11085");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 30879: delete_range
  status = db->DeleteRange(write_options, "key5057", "key5717");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 30880: delete
  db->Delete(write_options, "key9399");

  // op 30881: put
  db->Put(write_options, "key2113", "value60754");

  // op 30882: put
  db->Put(write_options, "key5981", "value70730");

  // op 30883: get
  status = db->Get(read_options, "key5810", &value);
  if (status.ok()) {
    out << "GET key5810 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30884: get
  status = db->Get(read_options, "key11701", &value);
  if (status.ok()) {
    out << "GET key11701 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30885: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 30886: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3537", "key8351", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3537, key8351) size=" << scan_result.size() << std::endl;
  }

  // op 30887: get
  status = db->Get(read_options, "key3006", &value);
  if (status.ok()) {
    out << "GET key3006 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30888: delete_range
  status = db->DeleteRange(write_options, "key1530", "key8142");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 30889: put
  db->Put(write_options, "key2382", "value70541");

  // op 30890: put
  db->Put(write_options, "key9612", "value112192");

  // op 30891: get
  status = db->Get(read_options, "key8253", &value);
  if (status.ok()) {
    out << "GET key8253 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30892: get
  status = db->Get(read_options, "key6838", &value);
  if (status.ok()) {
    out << "GET key6838 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30893: delete_range
  status = db->DeleteRange(write_options, "key0132", "key0905");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 30894: put
  db->Put(write_options, "key7404", "value29473");

  // op 30895: get
  status = db->Get(read_options, "key5895", &value);
  if (status.ok()) {
    out << "GET key5895 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30896: get
  status = db->Get(read_options, "key5975", &value);
  if (status.ok()) {
    out << "GET key5975 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30897: put
  db->Put(write_options, "key4125", "value26701");

  // op 30898: delete_range
  status = db->DeleteRange(write_options, "key5694", "key7450");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 30899: put
  db->Put(write_options, "key7234", "value21206");

  // op 30900: put
  db->Put(write_options, "key6619", "value12430");

  // op 30901: put
  db->Put(write_options, "key6562", "value8328");

  // op 30902: put
  db->Put(write_options, "key2954", "value46976");

  // op 30903: get
  status = db->Get(read_options, "key4283", &value);
  if (status.ok()) {
    out << "GET key4283 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30904: put
  db->Put(write_options, "key0084", "value116612");

  // op 30905: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 30906: delete
  db->Delete(write_options, "key5911");

  // op 30907: delete_range
  status = db->DeleteRange(write_options, "key2878", "key10110");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 30908: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8111", "key8485", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8111, key8485) size=" << scan_result.size() << std::endl;
  }

  // op 30909: get
  status = db->Get(read_options, "key2719", &value);
  if (status.ok()) {
    out << "GET key2719 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30910: get
  status = db->Get(read_options, "key2578", &value);
  if (status.ok()) {
    out << "GET key2578 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30911: delete_range
  status = db->DeleteRange(write_options, "key5458", "key6706");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 30912: get
  status = db->Get(read_options, "key9558", &value);
  if (status.ok()) {
    out << "GET key9558 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30913: put
  db->Put(write_options, "key0516", "value71404");

  // op 30914: put
  db->Put(write_options, "key4376", "value101925");

  // op 30915: delete_range
  status = db->DeleteRange(write_options, "key5384", "key7708");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 30916: delete
  db->Delete(write_options, "key6672");

  // op 30917: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6777", "key8291", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6777, key8291) size=" << scan_result.size() << std::endl;
  }

  // op 30918: put
  db->Put(write_options, "key11672", "value11544");

  // op 30919: delete_range
  status = db->DeleteRange(write_options, "key2786", "key10877");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 30920: get
  status = db->Get(read_options, "key7006", &value);
  if (status.ok()) {
    out << "GET key7006 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30921: put
  db->Put(write_options, "key7397", "value71802");

  // op 30922: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3289", "key5070", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3289, key5070) size=" << scan_result.size() << std::endl;
  }

  // op 30923: put
  db->Put(write_options, "key2994", "value64523");

  // op 30924: put
  db->Put(write_options, "key10021", "value46597");

  // op 30925: delete
  db->Delete(write_options, "key9531");

  // op 30926: get
  status = db->Get(read_options, "key4757", &value);
  if (status.ok()) {
    out << "GET key4757 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30927: delete_range
  status = db->DeleteRange(write_options, "key0159", "key4364");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 30928: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1941", "key6383", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1941, key6383) size=" << scan_result.size() << std::endl;
  }

  // op 30929: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2404", "key9356", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2404, key9356) size=" << scan_result.size() << std::endl;
  }

  // op 30930: put
  db->Put(write_options, "key5307", "value36459");

  // op 30931: put
  db->Put(write_options, "key8077", "value51976");

  // op 30932: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3680", "key8892", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3680, key8892) size=" << scan_result.size() << std::endl;
  }

  // op 30933: delete
  db->Delete(write_options, "key10804");

  // op 30934: put
  db->Put(write_options, "key2776", "value62361");

  // op 30935: delete
  db->Delete(write_options, "key10433");

  // op 30936: get
  status = db->Get(read_options, "key1640", &value);
  if (status.ok()) {
    out << "GET key1640 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30937: put
  db->Put(write_options, "key8175", "value47065");

  // op 30938: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8980", "key9325", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8980, key9325) size=" << scan_result.size() << std::endl;
  }

  // op 30939: delete_range
  status = db->DeleteRange(write_options, "key6993", "key10917");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 30940: get
  status = db->Get(read_options, "key10364", &value);
  if (status.ok()) {
    out << "GET key10364 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30941: put
  db->Put(write_options, "key0510", "value46081");

  // op 30942: get
  status = db->Get(read_options, "key5340", &value);
  if (status.ok()) {
    out << "GET key5340 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30943: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 30944: get
  status = db->Get(read_options, "key5843", &value);
  if (status.ok()) {
    out << "GET key5843 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30945: put
  db->Put(write_options, "key11066", "value74143");

  // op 30946: put
  db->Put(write_options, "key1944", "value25900");

  // op 30947: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5652", "key6369", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5652, key6369) size=" << scan_result.size() << std::endl;
  }

  // op 30948: put
  db->Put(write_options, "key6289", "value44120");

  // op 30949: put
  db->Put(write_options, "key11680", "value85845");

  // op 30950: put
  db->Put(write_options, "key6187", "value40708");

  // op 30951: put
  db->Put(write_options, "key11057", "value57520");

  // op 30952: delete
  db->Delete(write_options, "key10101");

  // op 30953: put
  db->Put(write_options, "key0172", "value20513");

  // op 30954: delete
  db->Delete(write_options, "key3240");

  // op 30955: put
  db->Put(write_options, "key5859", "value1907");

  // op 30956: get
  status = db->Get(read_options, "key7789", &value);
  if (status.ok()) {
    out << "GET key7789 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30957: delete
  db->Delete(write_options, "key1447");

  // op 30958: delete
  db->Delete(write_options, "key2797");

  // op 30959: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9567", "key11364", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9567, key11364) size=" << scan_result.size() << std::endl;
  }

  // op 30960: put
  db->Put(write_options, "key9128", "value38266");

  // op 30961: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6976", "key11014", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6976, key11014) size=" << scan_result.size() << std::endl;
  }

  // op 30962: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3140", "key10205", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3140, key10205) size=" << scan_result.size() << std::endl;
  }

  // op 30963: delete
  db->Delete(write_options, "key9611");

  // op 30964: delete_range
  status = db->DeleteRange(write_options, "key4595", "key7495");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 30965: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0066", "key11355", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0066, key11355) size=" << scan_result.size() << std::endl;
  }

  // op 30966: delete
  db->Delete(write_options, "key3098");

  // op 30967: delete
  db->Delete(write_options, "key0497");

  // op 30968: put
  db->Put(write_options, "key1864", "value96889");

  // op 30969: put
  db->Put(write_options, "key7694", "value102210");

  // op 30970: delete
  db->Delete(write_options, "key1056");

  // op 30971: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4608", "key4695", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4608, key4695) size=" << scan_result.size() << std::endl;
  }

  // op 30972: get
  status = db->Get(read_options, "key8243", &value);
  if (status.ok()) {
    out << "GET key8243 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30973: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2060", "key6817", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2060, key6817) size=" << scan_result.size() << std::endl;
  }

  // op 30974: put
  db->Put(write_options, "key3472", "value101758");

  // op 30975: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5187", "key8335", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5187, key8335) size=" << scan_result.size() << std::endl;
  }

  // op 30976: put
  db->Put(write_options, "key5466", "value34405");

  // op 30977: delete
  db->Delete(write_options, "key1739");

  // op 30978: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5131", "key8727", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5131, key8727) size=" << scan_result.size() << std::endl;
  }

  // op 30979: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3916", "key7770", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3916, key7770) size=" << scan_result.size() << std::endl;
  }

  // op 30980: delete_range
  status = db->DeleteRange(write_options, "key10047", "key11603");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 30981: put
  db->Put(write_options, "key9318", "value86418");

  // op 30982: put
  db->Put(write_options, "key10634", "value20527");

  // op 30983: put
  db->Put(write_options, "key6294", "value79370");

  // op 30984: get
  status = db->Get(read_options, "key3484", &value);
  if (status.ok()) {
    out << "GET key3484 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30985: put
  db->Put(write_options, "key4817", "value14071");

  // op 30986: put
  db->Put(write_options, "key9981", "value79155");

  // op 30987: put
  db->Put(write_options, "key5988", "value88557");

  // op 30988: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2919", "key7941", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2919, key7941) size=" << scan_result.size() << std::endl;
  }

  // op 30989: put
  db->Put(write_options, "key0812", "value49080");

  // op 30990: get
  status = db->Get(read_options, "key8705", &value);
  if (status.ok()) {
    out << "GET key8705 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30991: get
  status = db->Get(read_options, "key4828", &value);
  if (status.ok()) {
    out << "GET key4828 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 30992: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 30993: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0353", "key11471", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0353, key11471) size=" << scan_result.size() << std::endl;
  }

  // op 30994: put
  db->Put(write_options, "key10623", "value41355");

  // op 30995: delete_range
  status = db->DeleteRange(write_options, "key1482", "key10274");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 30996: put
  db->Put(write_options, "key6230", "value22208");

  // op 30997: put
  db->Put(write_options, "key11153", "value40336");

  // op 30998: put
  db->Put(write_options, "key0865", "value17289");

  // op 30999: put
  db->Put(write_options, "key11367", "value6147");

  // op 31000: put
  db->Put(write_options, "key6013", "value31793");

  // op 31001: put
  db->Put(write_options, "key6433", "value47169");

  // op 31002: put
  db->Put(write_options, "key9151", "value84763");

  // op 31003: put
  db->Put(write_options, "key5831", "value28050");

  // op 31004: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2990", "key10407", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2990, key10407) size=" << scan_result.size() << std::endl;
  }

  // op 31005: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6056", "key11273", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6056, key11273) size=" << scan_result.size() << std::endl;
  }

  // op 31006: get
  status = db->Get(read_options, "key0810", &value);
  if (status.ok()) {
    out << "GET key0810 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31007: put
  db->Put(write_options, "key1604", "value115198");

  // op 31008: put
  db->Put(write_options, "key4742", "value94002");

  // op 31009: put
  db->Put(write_options, "key4582", "value47507");

  // op 31010: put
  db->Put(write_options, "key7848", "value48254");

  // op 31011: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 31012: put
  db->Put(write_options, "key7695", "value3363");

  // op 31013: put
  db->Put(write_options, "key8042", "value7747");

  // op 31014: get
  status = db->Get(read_options, "key3404", &value);
  if (status.ok()) {
    out << "GET key3404 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31015: put
  db->Put(write_options, "key4342", "value91637");

  // op 31016: put
  db->Put(write_options, "key0947", "value101350");

  // op 31017: put
  db->Put(write_options, "key8217", "value79396");

  // op 31018: put
  db->Put(write_options, "key2257", "value63584");

  // op 31019: put
  db->Put(write_options, "key4843", "value103661");

  // op 31020: get
  status = db->Get(read_options, "key10671", &value);
  if (status.ok()) {
    out << "GET key10671 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31021: put
  db->Put(write_options, "key4016", "value37132");

  // op 31022: put
  db->Put(write_options, "key3913", "value35541");

  // op 31023: delete
  db->Delete(write_options, "key8553");

  // op 31024: put
  db->Put(write_options, "key4305", "value17162");

  // op 31025: put
  db->Put(write_options, "key2396", "value7561");

  // op 31026: put
  db->Put(write_options, "key8556", "value49916");

  // op 31027: put
  db->Put(write_options, "key10053", "value50095");

  // op 31028: get
  status = db->Get(read_options, "key3064", &value);
  if (status.ok()) {
    out << "GET key3064 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31029: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2293", "key4150", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2293, key4150) size=" << scan_result.size() << std::endl;
  }

  // op 31030: put
  db->Put(write_options, "key6218", "value21048");

  // op 31031: put
  db->Put(write_options, "key9351", "value47927");

  // op 31032: get
  status = db->Get(read_options, "key11433", &value);
  if (status.ok()) {
    out << "GET key11433 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31033: put
  db->Put(write_options, "key6721", "value42107");

  // op 31034: delete
  db->Delete(write_options, "key3937");

  // op 31035: put
  db->Put(write_options, "key8386", "value80336");

  // op 31036: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4584", "key4931", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4584, key4931) size=" << scan_result.size() << std::endl;
  }

  // op 31037: delete_range
  status = db->DeleteRange(write_options, "key0477", "key7102");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 31038: delete
  db->Delete(write_options, "key10619");

  // op 31039: put
  db->Put(write_options, "key6265", "value100380");

  // op 31040: delete
  db->Delete(write_options, "key6648");

  // op 31041: put
  db->Put(write_options, "key11638", "value79622");

  // op 31042: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4112", "key5888", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4112, key5888) size=" << scan_result.size() << std::endl;
  }

  // op 31043: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5984", "key8318", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5984, key8318) size=" << scan_result.size() << std::endl;
  }

  // op 31044: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0688", "key11272", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0688, key11272) size=" << scan_result.size() << std::endl;
  }

  // op 31045: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0453", "key10405", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0453, key10405) size=" << scan_result.size() << std::endl;
  }

  // op 31046: get
  status = db->Get(read_options, "key9741", &value);
  if (status.ok()) {
    out << "GET key9741 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31047: get
  status = db->Get(read_options, "key6592", &value);
  if (status.ok()) {
    out << "GET key6592 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31048: get
  status = db->Get(read_options, "key6737", &value);
  if (status.ok()) {
    out << "GET key6737 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31049: delete
  db->Delete(write_options, "key9914");

  // op 31050: get
  status = db->Get(read_options, "key0547", &value);
  if (status.ok()) {
    out << "GET key0547 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31051: put
  db->Put(write_options, "key1512", "value116963");

  // op 31052: get
  status = db->Get(read_options, "key6954", &value);
  if (status.ok()) {
    out << "GET key6954 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31053: get
  status = db->Get(read_options, "key7101", &value);
  if (status.ok()) {
    out << "GET key7101 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31054: get
  status = db->Get(read_options, "key5411", &value);
  if (status.ok()) {
    out << "GET key5411 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31055: put
  db->Put(write_options, "key1702", "value102813");

  // op 31056: put
  db->Put(write_options, "key3521", "value73784");

  // op 31057: put
  db->Put(write_options, "key0612", "value65582");

  // op 31058: get
  status = db->Get(read_options, "key8596", &value);
  if (status.ok()) {
    out << "GET key8596 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31059: put
  db->Put(write_options, "key2397", "value77549");

  // op 31060: put
  db->Put(write_options, "key0112", "value117439");

  // op 31061: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8513", "key11498", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8513, key11498) size=" << scan_result.size() << std::endl;
  }

  // op 31062: put
  db->Put(write_options, "key10777", "value44797");

  // op 31063: delete_range
  status = db->DeleteRange(write_options, "key2205", "key9572");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 31064: get
  status = db->Get(read_options, "key9491", &value);
  if (status.ok()) {
    out << "GET key9491 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31065: get
  status = db->Get(read_options, "key0099", &value);
  if (status.ok()) {
    out << "GET key0099 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31066: delete
  db->Delete(write_options, "key0263");

  // op 31067: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4016", "key5240", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4016, key5240) size=" << scan_result.size() << std::endl;
  }

  // op 31068: put
  db->Put(write_options, "key8904", "value53211");

  // op 31069: delete
  db->Delete(write_options, "key7139");

  // op 31070: put
  db->Put(write_options, "key3062", "value80844");

  // op 31071: delete_range
  status = db->DeleteRange(write_options, "key9205", "key10282");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 31072: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1139", "key2631", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1139, key2631) size=" << scan_result.size() << std::endl;
  }

  // op 31073: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3119", "key8435", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3119, key8435) size=" << scan_result.size() << std::endl;
  }

  // op 31074: put
  db->Put(write_options, "key5998", "value57230");

  // op 31075: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3671", "key3685", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3671, key3685) size=" << scan_result.size() << std::endl;
  }

  // op 31076: put
  db->Put(write_options, "key10027", "value0136");

  // op 31077: put
  db->Put(write_options, "key7340", "value39293");

  // op 31078: put
  db->Put(write_options, "key4946", "value63947");

  // op 31079: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 31080: put
  db->Put(write_options, "key4013", "value11280");

  // op 31081: put
  db->Put(write_options, "key6501", "value18691");

  // op 31082: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 31083: delete_range
  status = db->DeleteRange(write_options, "key2839", "key6434");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 31084: delete
  db->Delete(write_options, "key9956");

  // op 31085: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5041", "key9417", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5041, key9417) size=" << scan_result.size() << std::endl;
  }

  // op 31086: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4268", "key6017", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4268, key6017) size=" << scan_result.size() << std::endl;
  }

  // op 31087: delete_range
  status = db->DeleteRange(write_options, "key1723", "key7010");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 31088: get
  status = db->Get(read_options, "key3444", &value);
  if (status.ok()) {
    out << "GET key3444 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31089: put
  db->Put(write_options, "key0574", "value4695");

  // op 31090: get
  status = db->Get(read_options, "key8378", &value);
  if (status.ok()) {
    out << "GET key8378 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31091: put
  db->Put(write_options, "key3510", "value54135");

  // op 31092: put
  db->Put(write_options, "key4614", "value109150");

  // op 31093: put
  db->Put(write_options, "key2120", "value64653");

  // op 31094: delete_range
  status = db->DeleteRange(write_options, "key1930", "key6282");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 31095: put
  db->Put(write_options, "key2333", "value14946");

  // op 31096: delete_range
  status = db->DeleteRange(write_options, "key5251", "key7820");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 31097: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5904", "key10701", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5904, key10701) size=" << scan_result.size() << std::endl;
  }

  // op 31098: delete_range
  status = db->DeleteRange(write_options, "key2996", "key5169");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 31099: get
  status = db->Get(read_options, "key2910", &value);
  if (status.ok()) {
    out << "GET key2910 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31100: delete_range
  status = db->DeleteRange(write_options, "key1440", "key8471");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 31101: delete
  db->Delete(write_options, "key8055");

  // op 31102: delete_range
  status = db->DeleteRange(write_options, "key5233", "key6339");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 31103: get
  status = db->Get(read_options, "key10476", &value);
  if (status.ok()) {
    out << "GET key10476 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31104: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4008", "key7248", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4008, key7248) size=" << scan_result.size() << std::endl;
  }

  // op 31105: put
  db->Put(write_options, "key5903", "value18289");

  // op 31106: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3725", "key4227", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3725, key4227) size=" << scan_result.size() << std::endl;
  }

  // op 31107: put
  db->Put(write_options, "key4428", "value7007");

  // op 31108: put
  db->Put(write_options, "key2287", "value0190");

  // op 31109: put
  db->Put(write_options, "key4424", "value11028");

  // op 31110: delete_range
  status = db->DeleteRange(write_options, "key5276", "key10802");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 31111: put
  db->Put(write_options, "key6097", "value21742");

  // op 31112: put
  db->Put(write_options, "key10660", "value105192");

  // op 31113: delete
  db->Delete(write_options, "key10801");

  // op 31114: delete
  db->Delete(write_options, "key8561");

  // op 31115: put
  db->Put(write_options, "key1344", "value100878");

  // op 31116: put
  db->Put(write_options, "key9360", "value97960");

  // op 31117: get
  status = db->Get(read_options, "key8873", &value);
  if (status.ok()) {
    out << "GET key8873 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31118: put
  db->Put(write_options, "key6005", "value41922");

  // op 31119: put
  db->Put(write_options, "key9335", "value34089");

  // op 31120: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2030", "key2852", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2030, key2852) size=" << scan_result.size() << std::endl;
  }

  // op 31121: get
  status = db->Get(read_options, "key5675", &value);
  if (status.ok()) {
    out << "GET key5675 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31122: delete_range
  status = db->DeleteRange(write_options, "key1755", "key3485");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 31123: get
  status = db->Get(read_options, "key5683", &value);
  if (status.ok()) {
    out << "GET key5683 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31124: delete_range
  status = db->DeleteRange(write_options, "key1338", "key8538");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 31125: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3883", "key8630", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3883, key8630) size=" << scan_result.size() << std::endl;
  }

  // op 31126: put
  db->Put(write_options, "key7014", "value75744");

  // op 31127: get
  status = db->Get(read_options, "key4408", &value);
  if (status.ok()) {
    out << "GET key4408 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31128: put
  db->Put(write_options, "key0502", "value70623");

  // op 31129: delete_range
  status = db->DeleteRange(write_options, "key9890", "key10490");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 31130: put
  db->Put(write_options, "key1808", "value58540");

  // op 31131: delete_range
  status = db->DeleteRange(write_options, "key6883", "key11131");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 31132: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 31133: get
  status = db->Get(read_options, "key6547", &value);
  if (status.ok()) {
    out << "GET key6547 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31134: put
  db->Put(write_options, "key6012", "value92551");

  // op 31135: put
  db->Put(write_options, "key2640", "value54628");

  // op 31136: get
  status = db->Get(read_options, "key7965", &value);
  if (status.ok()) {
    out << "GET key7965 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31137: delete_range
  status = db->DeleteRange(write_options, "key8756", "key10876");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 31138: put
  db->Put(write_options, "key3799", "value62919");

  // op 31139: put
  db->Put(write_options, "key3235", "value50988");

  // op 31140: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 31141: delete
  db->Delete(write_options, "key10090");

  // op 31142: get
  status = db->Get(read_options, "key10068", &value);
  if (status.ok()) {
    out << "GET key10068 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31143: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1447", "key2878", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1447, key2878) size=" << scan_result.size() << std::endl;
  }

  // op 31144: delete_range
  status = db->DeleteRange(write_options, "key8535", "key11367");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 31145: put
  db->Put(write_options, "key9997", "value84379");

  // op 31146: put
  db->Put(write_options, "key0900", "value106437");

  // op 31147: delete_range
  status = db->DeleteRange(write_options, "key3234", "key10744");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 31148: put
  db->Put(write_options, "key9460", "value30897");

  // op 31149: delete_range
  status = db->DeleteRange(write_options, "key0699", "key11127");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 31150: put
  db->Put(write_options, "key2020", "value5550");

  // op 31151: put
  db->Put(write_options, "key6536", "value81095");

  // op 31152: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5340", "key10376", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5340, key10376) size=" << scan_result.size() << std::endl;
  }

  // op 31153: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3893", "key6716", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3893, key6716) size=" << scan_result.size() << std::endl;
  }

  // op 31154: delete
  db->Delete(write_options, "key5632");

  // op 31155: put
  db->Put(write_options, "key10522", "value15929");

  // op 31156: put
  db->Put(write_options, "key10930", "value40587");

  // op 31157: put
  db->Put(write_options, "key6861", "value12951");

  // op 31158: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 31159: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2558", "key9508", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2558, key9508) size=" << scan_result.size() << std::endl;
  }

  // op 31160: get
  status = db->Get(read_options, "key9351", &value);
  if (status.ok()) {
    out << "GET key9351 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31161: delete
  db->Delete(write_options, "key9138");

  // op 31162: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0906", "key6212", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0906, key6212) size=" << scan_result.size() << std::endl;
  }

  // op 31163: put
  db->Put(write_options, "key5374", "value67075");

  // op 31164: get
  status = db->Get(read_options, "key3653", &value);
  if (status.ok()) {
    out << "GET key3653 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31165: delete
  db->Delete(write_options, "key6540");

  // op 31166: put
  db->Put(write_options, "key5426", "value117057");

  // op 31167: delete_range
  status = db->DeleteRange(write_options, "key2630", "key3613");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 31168: delete
  db->Delete(write_options, "key0863");

  // op 31169: put
  db->Put(write_options, "key10307", "value104838");

  // op 31170: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1291", "key6705", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1291, key6705) size=" << scan_result.size() << std::endl;
  }

  // op 31171: put
  db->Put(write_options, "key5275", "value78680");

  // op 31172: put
  db->Put(write_options, "key4049", "value11499");

  // op 31173: delete
  db->Delete(write_options, "key6865");

  // op 31174: get
  status = db->Get(read_options, "key0088", &value);
  if (status.ok()) {
    out << "GET key0088 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31175: delete_range
  status = db->DeleteRange(write_options, "key9540", "key10931");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 31176: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1145", "key6447", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1145, key6447) size=" << scan_result.size() << std::endl;
  }

  // op 31177: put
  db->Put(write_options, "key9665", "value82684");

  // op 31178: put
  db->Put(write_options, "key2612", "value100102");

  // op 31179: put
  db->Put(write_options, "key6332", "value40073");

  // op 31180: put
  db->Put(write_options, "key11193", "value96834");

  // op 31181: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 31182: put
  db->Put(write_options, "key7555", "value105537");

  // op 31183: put
  db->Put(write_options, "key2476", "value21064");

  // op 31184: put
  db->Put(write_options, "key2178", "value62400");

  // op 31185: get
  status = db->Get(read_options, "key6548", &value);
  if (status.ok()) {
    out << "GET key6548 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31186: put
  db->Put(write_options, "key8189", "value112465");

  // op 31187: put
  db->Put(write_options, "key5794", "value81761");

  // op 31188: delete
  db->Delete(write_options, "key1774");

  // op 31189: put
  db->Put(write_options, "key3535", "value25978");

  // op 31190: get
  status = db->Get(read_options, "key6731", &value);
  if (status.ok()) {
    out << "GET key6731 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31191: get
  status = db->Get(read_options, "key4130", &value);
  if (status.ok()) {
    out << "GET key4130 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31192: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5636", "key8120", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5636, key8120) size=" << scan_result.size() << std::endl;
  }

  // op 31193: put
  db->Put(write_options, "key4442", "value80010");

  // op 31194: put
  db->Put(write_options, "key10276", "value58412");

  // op 31195: get
  status = db->Get(read_options, "key7681", &value);
  if (status.ok()) {
    out << "GET key7681 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31196: get
  status = db->Get(read_options, "key5139", &value);
  if (status.ok()) {
    out << "GET key5139 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31197: put
  db->Put(write_options, "key4498", "value53989");

  // op 31198: put
  db->Put(write_options, "key4186", "value75570");

  // op 31199: delete
  db->Delete(write_options, "key9421");

  // op 31200: get
  status = db->Get(read_options, "key11502", &value);
  if (status.ok()) {
    out << "GET key11502 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31201: put
  db->Put(write_options, "key9705", "value20210");

  // op 31202: put
  db->Put(write_options, "key9361", "value107274");

  // op 31203: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4315", "key10640", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4315, key10640) size=" << scan_result.size() << std::endl;
  }

  // op 31204: put
  db->Put(write_options, "key10651", "value4330");

  // op 31205: put
  db->Put(write_options, "key0532", "value1703");

  // op 31206: put
  db->Put(write_options, "key0201", "value56694");

  // op 31207: get
  status = db->Get(read_options, "key2463", &value);
  if (status.ok()) {
    out << "GET key2463 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31208: put
  db->Put(write_options, "key5560", "value82834");

  // op 31209: put
  db->Put(write_options, "key7068", "value44914");

  // op 31210: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7190", "key7277", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7190, key7277) size=" << scan_result.size() << std::endl;
  }

  // op 31211: get
  status = db->Get(read_options, "key1702", &value);
  if (status.ok()) {
    out << "GET key1702 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31212: get
  status = db->Get(read_options, "key9857", &value);
  if (status.ok()) {
    out << "GET key9857 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31213: delete
  db->Delete(write_options, "key10674");

  // op 31214: put
  db->Put(write_options, "key10500", "value85834");

  // op 31215: delete
  db->Delete(write_options, "key10642");

  // op 31216: get
  status = db->Get(read_options, "key5131", &value);
  if (status.ok()) {
    out << "GET key5131 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31217: put
  db->Put(write_options, "key3982", "value9640");

  // op 31218: get
  status = db->Get(read_options, "key9136", &value);
  if (status.ok()) {
    out << "GET key9136 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31219: delete_range
  status = db->DeleteRange(write_options, "key1111", "key4247");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 31220: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2601", "key5565", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2601, key5565) size=" << scan_result.size() << std::endl;
  }

  // op 31221: put
  db->Put(write_options, "key6208", "value63872");

  // op 31222: put
  db->Put(write_options, "key1589", "value41827");

  // op 31223: put
  db->Put(write_options, "key11395", "value1369");

  // op 31224: delete
  db->Delete(write_options, "key2845");

  // op 31225: delete_range
  status = db->DeleteRange(write_options, "key5813", "key8128");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 31226: delete_range
  status = db->DeleteRange(write_options, "key4008", "key6520");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 31227: put
  db->Put(write_options, "key5922", "value83211");

  // op 31228: get
  status = db->Get(read_options, "key6386", &value);
  if (status.ok()) {
    out << "GET key6386 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31229: put
  db->Put(write_options, "key1655", "value48427");

  // op 31230: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2888", "key9562", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2888, key9562) size=" << scan_result.size() << std::endl;
  }

  // op 31231: get
  status = db->Get(read_options, "key5122", &value);
  if (status.ok()) {
    out << "GET key5122 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31232: delete_range
  status = db->DeleteRange(write_options, "key1016", "key3359");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 31233: delete
  db->Delete(write_options, "key7529");

  // op 31234: put
  db->Put(write_options, "key0569", "value33643");

  // op 31235: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0431", "key10242", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0431, key10242) size=" << scan_result.size() << std::endl;
  }

  // op 31236: get
  status = db->Get(read_options, "key3855", &value);
  if (status.ok()) {
    out << "GET key3855 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31237: delete
  db->Delete(write_options, "key1776");

  // op 31238: delete_range
  status = db->DeleteRange(write_options, "key10170", "key11216");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 31239: put
  db->Put(write_options, "key2903", "value90492");

  // op 31240: put
  db->Put(write_options, "key5036", "value76354");

  // op 31241: get
  status = db->Get(read_options, "key10612", &value);
  if (status.ok()) {
    out << "GET key10612 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31242: get
  status = db->Get(read_options, "key8206", &value);
  if (status.ok()) {
    out << "GET key8206 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31243: get
  status = db->Get(read_options, "key11470", &value);
  if (status.ok()) {
    out << "GET key11470 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31244: put
  db->Put(write_options, "key5015", "value12593");

  // op 31245: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2407", "key11460", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2407, key11460) size=" << scan_result.size() << std::endl;
  }

  // op 31246: put
  db->Put(write_options, "key1079", "value0153");

  // op 31247: put
  db->Put(write_options, "key3368", "value109446");

  // op 31248: put
  db->Put(write_options, "key0917", "value22155");

  // op 31249: put
  db->Put(write_options, "key11094", "value93553");

  // op 31250: put
  db->Put(write_options, "key7067", "value13995");

  // op 31251: get
  status = db->Get(read_options, "key6392", &value);
  if (status.ok()) {
    out << "GET key6392 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31252: get
  status = db->Get(read_options, "key7389", &value);
  if (status.ok()) {
    out << "GET key7389 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31253: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5150", "key10560", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5150, key10560) size=" << scan_result.size() << std::endl;
  }

  // op 31254: put
  db->Put(write_options, "key0797", "value35622");

  // op 31255: put
  db->Put(write_options, "key10026", "value30478");

  // op 31256: put
  db->Put(write_options, "key4146", "value15009");

  // op 31257: get
  status = db->Get(read_options, "key0902", &value);
  if (status.ok()) {
    out << "GET key0902 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31258: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 31259: delete_range
  status = db->DeleteRange(write_options, "key8208", "key11389");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 31260: put
  db->Put(write_options, "key6850", "value48233");

  // op 31261: put
  db->Put(write_options, "key1507", "value38162");

  // op 31262: put
  db->Put(write_options, "key5045", "value75601");

  // op 31263: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8827", "key9795", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8827, key9795) size=" << scan_result.size() << std::endl;
  }

  // op 31264: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1236", "key2113", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1236, key2113) size=" << scan_result.size() << std::endl;
  }

  // op 31265: delete
  db->Delete(write_options, "key7514");

  // op 31266: put
  db->Put(write_options, "key5219", "value83307");

  // op 31267: get
  status = db->Get(read_options, "key7146", &value);
  if (status.ok()) {
    out << "GET key7146 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31268: put
  db->Put(write_options, "key1630", "value39633");

  // op 31269: put
  db->Put(write_options, "key11369", "value25502");

  // op 31270: delete
  db->Delete(write_options, "key0450");

  // op 31271: delete_range
  status = db->DeleteRange(write_options, "key4919", "key8439");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 31272: delete_range
  status = db->DeleteRange(write_options, "key6770", "key7535");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 31273: put
  db->Put(write_options, "key2961", "value46689");

  // op 31274: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 31275: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1758", "key11609", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1758, key11609) size=" << scan_result.size() << std::endl;
  }

  // op 31276: get
  status = db->Get(read_options, "key2704", &value);
  if (status.ok()) {
    out << "GET key2704 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31277: put
  db->Put(write_options, "key7865", "value66207");

  // op 31278: get
  status = db->Get(read_options, "key1847", &value);
  if (status.ok()) {
    out << "GET key1847 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31279: put
  db->Put(write_options, "key0322", "value35660");

  // op 31280: delete
  db->Delete(write_options, "key8215");

  // op 31281: get
  status = db->Get(read_options, "key3600", &value);
  if (status.ok()) {
    out << "GET key3600 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31282: put
  db->Put(write_options, "key10356", "value52365");

  // op 31283: put
  db->Put(write_options, "key11368", "value76219");

  // op 31284: get
  status = db->Get(read_options, "key6471", &value);
  if (status.ok()) {
    out << "GET key6471 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31285: put
  db->Put(write_options, "key1544", "value25166");

  // op 31286: delete
  db->Delete(write_options, "key8895");

  // op 31287: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0369", "key3432", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0369, key3432) size=" << scan_result.size() << std::endl;
  }

  // op 31288: delete
  db->Delete(write_options, "key0232");

  // op 31289: put
  db->Put(write_options, "key8301", "value52949");

  // op 31290: put
  db->Put(write_options, "key8469", "value105212");

  // op 31291: get
  status = db->Get(read_options, "key11480", &value);
  if (status.ok()) {
    out << "GET key11480 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31292: delete
  db->Delete(write_options, "key1682");

  // op 31293: put
  db->Put(write_options, "key5730", "value29421");

  // op 31294: put
  db->Put(write_options, "key11350", "value35131");

  // op 31295: delete_range
  status = db->DeleteRange(write_options, "key7577", "key7871");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 31296: get
  status = db->Get(read_options, "key4448", &value);
  if (status.ok()) {
    out << "GET key4448 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31297: get
  status = db->Get(read_options, "key2803", &value);
  if (status.ok()) {
    out << "GET key2803 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31298: put
  db->Put(write_options, "key8967", "value74444");

  // op 31299: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7185", "key7852", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7185, key7852) size=" << scan_result.size() << std::endl;
  }

  // op 31300: delete_range
  status = db->DeleteRange(write_options, "key0164", "key3130");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 31301: delete_range
  status = db->DeleteRange(write_options, "key2894", "key10322");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 31302: put
  db->Put(write_options, "key4436", "value101276");

  // op 31303: put
  db->Put(write_options, "key9264", "value100181");

  // op 31304: put
  db->Put(write_options, "key2682", "value87214");

  // op 31305: delete_range
  status = db->DeleteRange(write_options, "key10854", "key11575");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 31306: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4074", "key5183", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4074, key5183) size=" << scan_result.size() << std::endl;
  }

  // op 31307: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10659", "key11265", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10659, key11265) size=" << scan_result.size() << std::endl;
  }

  // op 31308: put
  db->Put(write_options, "key0745", "value85947");

  // op 31309: delete
  db->Delete(write_options, "key5941");

  // op 31310: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1476", "key9412", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1476, key9412) size=" << scan_result.size() << std::endl;
  }

  // op 31311: delete_range
  status = db->DeleteRange(write_options, "key7409", "key8103");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 31312: put
  db->Put(write_options, "key3092", "value39132");

  // op 31313: put
  db->Put(write_options, "key8935", "value96157");

  // op 31314: put
  db->Put(write_options, "key5715", "value61160");

  // op 31315: delete
  db->Delete(write_options, "key0863");

  // op 31316: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2035", "key6001", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2035, key6001) size=" << scan_result.size() << std::endl;
  }

  // op 31317: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5241", "key8483", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5241, key8483) size=" << scan_result.size() << std::endl;
  }

  // op 31318: put
  db->Put(write_options, "key9801", "value37151");

  // op 31319: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2685", "key2821", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2685, key2821) size=" << scan_result.size() << std::endl;
  }

  // op 31320: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9741", "key9823", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9741, key9823) size=" << scan_result.size() << std::endl;
  }

  // op 31321: delete_range
  status = db->DeleteRange(write_options, "key2935", "key7504");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 31322: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 31323: put
  db->Put(write_options, "key7959", "value29871");

  // op 31324: put
  db->Put(write_options, "key1191", "value41717");

  // op 31325: put
  db->Put(write_options, "key9119", "value62300");

  // op 31326: put
  db->Put(write_options, "key9818", "value50086");

  // op 31327: get
  status = db->Get(read_options, "key7009", &value);
  if (status.ok()) {
    out << "GET key7009 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31328: put
  db->Put(write_options, "key6962", "value2537");

  // op 31329: delete_range
  status = db->DeleteRange(write_options, "key1394", "key10465");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 31330: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1062", "key5778", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1062, key5778) size=" << scan_result.size() << std::endl;
  }

  // op 31331: delete
  db->Delete(write_options, "key6967");

  // op 31332: put
  db->Put(write_options, "key1534", "value27293");

  // op 31333: delete_range
  status = db->DeleteRange(write_options, "key5500", "key7556");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 31334: put
  db->Put(write_options, "key5955", "value8268");

  // op 31335: get
  status = db->Get(read_options, "key8398", &value);
  if (status.ok()) {
    out << "GET key8398 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31336: put
  db->Put(write_options, "key7638", "value99675");

  // op 31337: put
  db->Put(write_options, "key9457", "value97735");

  // op 31338: get
  status = db->Get(read_options, "key4614", &value);
  if (status.ok()) {
    out << "GET key4614 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31339: delete
  db->Delete(write_options, "key10201");

  // op 31340: get
  status = db->Get(read_options, "key10653", &value);
  if (status.ok()) {
    out << "GET key10653 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31341: put
  db->Put(write_options, "key11204", "value57227");

  // op 31342: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 31343: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0854", "key3755", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0854, key3755) size=" << scan_result.size() << std::endl;
  }

  // op 31344: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 31345: get
  status = db->Get(read_options, "key1404", &value);
  if (status.ok()) {
    out << "GET key1404 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31346: get
  status = db->Get(read_options, "key10481", &value);
  if (status.ok()) {
    out << "GET key10481 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31347: delete_range
  status = db->DeleteRange(write_options, "key0729", "key3934");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 31348: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5875", "key10850", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5875, key10850) size=" << scan_result.size() << std::endl;
  }

  // op 31349: put
  db->Put(write_options, "key2866", "value28071");

  // op 31350: put
  db->Put(write_options, "key8968", "value102214");

  // op 31351: put
  db->Put(write_options, "key3170", "value86282");

  // op 31352: put
  db->Put(write_options, "key2365", "value51833");

  // op 31353: put
  db->Put(write_options, "key10487", "value23948");

  // op 31354: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 31355: put
  db->Put(write_options, "key7090", "value112977");

  // op 31356: get
  status = db->Get(read_options, "key9514", &value);
  if (status.ok()) {
    out << "GET key9514 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31357: get
  status = db->Get(read_options, "key7086", &value);
  if (status.ok()) {
    out << "GET key7086 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31358: put
  db->Put(write_options, "key5628", "value116561");

  // op 31359: put
  db->Put(write_options, "key6298", "value105258");

  // op 31360: get
  status = db->Get(read_options, "key8354", &value);
  if (status.ok()) {
    out << "GET key8354 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31361: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2719", "key7956", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2719, key7956) size=" << scan_result.size() << std::endl;
  }

  // op 31362: get
  status = db->Get(read_options, "key7300", &value);
  if (status.ok()) {
    out << "GET key7300 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31363: put
  db->Put(write_options, "key9455", "value91453");

  // op 31364: put
  db->Put(write_options, "key6814", "value52480");

  // op 31365: put
  db->Put(write_options, "key1233", "value61540");

  // op 31366: get
  status = db->Get(read_options, "key11601", &value);
  if (status.ok()) {
    out << "GET key11601 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31367: delete_range
  status = db->DeleteRange(write_options, "key0156", "key9743");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 31368: put
  db->Put(write_options, "key10446", "value55124");

  // op 31369: get
  status = db->Get(read_options, "key2756", &value);
  if (status.ok()) {
    out << "GET key2756 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31370: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 31371: put
  db->Put(write_options, "key10059", "value32696");

  // op 31372: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 31373: delete_range
  status = db->DeleteRange(write_options, "key3396", "key7108");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 31374: put
  db->Put(write_options, "key3821", "value95886");

  // op 31375: get
  status = db->Get(read_options, "key8383", &value);
  if (status.ok()) {
    out << "GET key8383 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31376: put
  db->Put(write_options, "key6197", "value95118");

  // op 31377: put
  db->Put(write_options, "key10202", "value51193");

  // op 31378: get
  status = db->Get(read_options, "key1529", &value);
  if (status.ok()) {
    out << "GET key1529 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31379: put
  db->Put(write_options, "key4552", "value46879");

  // op 31380: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0050", "key7734", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0050, key7734) size=" << scan_result.size() << std::endl;
  }

  // op 31381: get
  status = db->Get(read_options, "key0550", &value);
  if (status.ok()) {
    out << "GET key0550 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31382: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0969", "key7752", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0969, key7752) size=" << scan_result.size() << std::endl;
  }

  // op 31383: put
  db->Put(write_options, "key7928", "value20906");

  // op 31384: get
  status = db->Get(read_options, "key8753", &value);
  if (status.ok()) {
    out << "GET key8753 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31385: get
  status = db->Get(read_options, "key8467", &value);
  if (status.ok()) {
    out << "GET key8467 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31386: delete_range
  status = db->DeleteRange(write_options, "key3180", "key10520");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 31387: put
  db->Put(write_options, "key3393", "value48647");

  // op 31388: put
  db->Put(write_options, "key0851", "value103655");

  // op 31389: put
  db->Put(write_options, "key8485", "value93874");

  // op 31390: delete_range
  status = db->DeleteRange(write_options, "key4302", "key4576");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 31391: put
  db->Put(write_options, "key1806", "value34122");

  // op 31392: put
  db->Put(write_options, "key10695", "value68744");

  // op 31393: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4163", "key4629", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4163, key4629) size=" << scan_result.size() << std::endl;
  }

  // op 31394: delete_range
  status = db->DeleteRange(write_options, "key10439", "key11619");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 31395: delete
  db->Delete(write_options, "key6892");

  // op 31396: put
  db->Put(write_options, "key5475", "value44726");

  // op 31397: put
  db->Put(write_options, "key5215", "value17451");

  // op 31398: put
  db->Put(write_options, "key4413", "value31628");

  // op 31399: delete_range
  status = db->DeleteRange(write_options, "key3783", "key6443");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 31400: delete
  db->Delete(write_options, "key8503");

  // op 31401: put
  db->Put(write_options, "key0359", "value29377");

  // op 31402: delete_range
  status = db->DeleteRange(write_options, "key3104", "key3740");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 31403: delete
  db->Delete(write_options, "key0831");

  // op 31404: put
  db->Put(write_options, "key10472", "value63473");

  // op 31405: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 31406: get
  status = db->Get(read_options, "key8042", &value);
  if (status.ok()) {
    out << "GET key8042 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31407: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 31408: put
  db->Put(write_options, "key2664", "value53466");

  // op 31409: delete
  db->Delete(write_options, "key10942");

  // op 31410: put
  db->Put(write_options, "key1133", "value73950");

  // op 31411: put
  db->Put(write_options, "key6055", "value65011");

  // op 31412: put
  db->Put(write_options, "key9695", "value41006");

  // op 31413: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7277", "key7333", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7277, key7333) size=" << scan_result.size() << std::endl;
  }

  // op 31414: put
  db->Put(write_options, "key5128", "value39565");

  // op 31415: put
  db->Put(write_options, "key1328", "value77790");

  // op 31416: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0590", "key7149", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0590, key7149) size=" << scan_result.size() << std::endl;
  }

  // op 31417: get
  status = db->Get(read_options, "key5273", &value);
  if (status.ok()) {
    out << "GET key5273 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31418: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4107", "key11517", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4107, key11517) size=" << scan_result.size() << std::endl;
  }

  // op 31419: put
  db->Put(write_options, "key11468", "value92120");

  // op 31420: put
  db->Put(write_options, "key4864", "value52806");

  // op 31421: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4306", "key7875", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4306, key7875) size=" << scan_result.size() << std::endl;
  }

  // op 31422: put
  db->Put(write_options, "key4861", "value63467");

  // op 31423: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6059", "key10939", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6059, key10939) size=" << scan_result.size() << std::endl;
  }

  // op 31424: put
  db->Put(write_options, "key7998", "value96600");

  // op 31425: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7706", "key9873", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7706, key9873) size=" << scan_result.size() << std::endl;
  }

  // op 31426: get
  status = db->Get(read_options, "key5951", &value);
  if (status.ok()) {
    out << "GET key5951 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31427: put
  db->Put(write_options, "key3308", "value55919");

  // op 31428: get
  status = db->Get(read_options, "key6662", &value);
  if (status.ok()) {
    out << "GET key6662 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31429: get
  status = db->Get(read_options, "key1676", &value);
  if (status.ok()) {
    out << "GET key1676 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31430: put
  db->Put(write_options, "key4971", "value116854");

  // op 31431: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 31432: get
  status = db->Get(read_options, "key7867", &value);
  if (status.ok()) {
    out << "GET key7867 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31433: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7883", "key10131", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7883, key10131) size=" << scan_result.size() << std::endl;
  }

  // op 31434: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 31435: put
  db->Put(write_options, "key0426", "value97898");

  // op 31436: put
  db->Put(write_options, "key3146", "value47087");

  // op 31437: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2189", "key6537", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2189, key6537) size=" << scan_result.size() << std::endl;
  }

  // op 31438: delete
  db->Delete(write_options, "key1726");

  // op 31439: delete_range
  status = db->DeleteRange(write_options, "key2237", "key7590");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 31440: delete_range
  status = db->DeleteRange(write_options, "key9939", "key10258");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 31441: put
  db->Put(write_options, "key1205", "value29822");

  // op 31442: delete
  db->Delete(write_options, "key5531");

  // op 31443: put
  db->Put(write_options, "key2131", "value102902");

  // op 31444: get
  status = db->Get(read_options, "key7980", &value);
  if (status.ok()) {
    out << "GET key7980 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31445: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 31446: delete
  db->Delete(write_options, "key0226");

  // op 31447: delete_range
  status = db->DeleteRange(write_options, "key0816", "key5667");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 31448: put
  db->Put(write_options, "key7117", "value25566");

  // op 31449: delete_range
  status = db->DeleteRange(write_options, "key6971", "key10897");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 31450: get
  status = db->Get(read_options, "key0893", &value);
  if (status.ok()) {
    out << "GET key0893 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31451: put
  db->Put(write_options, "key8897", "value82877");

  // op 31452: put
  db->Put(write_options, "key3865", "value16184");

  // op 31453: get
  status = db->Get(read_options, "key3235", &value);
  if (status.ok()) {
    out << "GET key3235 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31454: put
  db->Put(write_options, "key7284", "value12801");

  // op 31455: delete
  db->Delete(write_options, "key4088");

  // op 31456: put
  db->Put(write_options, "key1595", "value46817");

  // op 31457: put
  db->Put(write_options, "key1715", "value5935");

  // op 31458: delete
  db->Delete(write_options, "key10722");

  // op 31459: put
  db->Put(write_options, "key1760", "value81796");

  // op 31460: put
  db->Put(write_options, "key5698", "value71553");

  // op 31461: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8769", "key11321", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8769, key11321) size=" << scan_result.size() << std::endl;
  }

  // op 31462: delete_range
  status = db->DeleteRange(write_options, "key0001", "key2195");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 31463: put
  db->Put(write_options, "key0871", "value66175");

  // op 31464: put
  db->Put(write_options, "key2976", "value34223");

  // op 31465: delete_range
  status = db->DeleteRange(write_options, "key6379", "key9453");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 31466: put
  db->Put(write_options, "key11485", "value46964");

  // op 31467: put
  db->Put(write_options, "key1734", "value8427");

  // op 31468: put
  db->Put(write_options, "key6832", "value46812");

  // op 31469: get
  status = db->Get(read_options, "key2134", &value);
  if (status.ok()) {
    out << "GET key2134 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31470: put
  db->Put(write_options, "key6060", "value14558");

  // op 31471: put
  db->Put(write_options, "key8324", "value106384");

  // op 31472: delete_range
  status = db->DeleteRange(write_options, "key3656", "key6712");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 31473: put
  db->Put(write_options, "key11016", "value67022");

  // op 31474: get
  status = db->Get(read_options, "key0773", &value);
  if (status.ok()) {
    out << "GET key0773 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31475: put
  db->Put(write_options, "key1227", "value69951");

  // op 31476: delete_range
  status = db->DeleteRange(write_options, "key2348", "key8189");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 31477: get
  status = db->Get(read_options, "key9495", &value);
  if (status.ok()) {
    out << "GET key9495 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31478: get
  status = db->Get(read_options, "key10930", &value);
  if (status.ok()) {
    out << "GET key10930 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31479: put
  db->Put(write_options, "key8536", "value90804");

  // op 31480: get
  status = db->Get(read_options, "key4779", &value);
  if (status.ok()) {
    out << "GET key4779 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31481: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0224", "key1311", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0224, key1311) size=" << scan_result.size() << std::endl;
  }

  // op 31482: put
  db->Put(write_options, "key7050", "value57033");

  // op 31483: delete
  db->Delete(write_options, "key0689");

  // op 31484: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 31485: put
  db->Put(write_options, "key2486", "value92012");

  // op 31486: put
  db->Put(write_options, "key2549", "value5237");

  // op 31487: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3234", "key9217", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3234, key9217) size=" << scan_result.size() << std::endl;
  }

  // op 31488: delete_range
  status = db->DeleteRange(write_options, "key0990", "key7435");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 31489: put
  db->Put(write_options, "key0279", "value70628");

  // op 31490: put
  db->Put(write_options, "key2820", "value111251");

  // op 31491: delete_range
  status = db->DeleteRange(write_options, "key7342", "key10944");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 31492: put
  db->Put(write_options, "key10856", "value46430");

  // op 31493: delete
  db->Delete(write_options, "key2166");

  // op 31494: put
  db->Put(write_options, "key9036", "value68052");

  // op 31495: put
  db->Put(write_options, "key8227", "value96866");

  // op 31496: put
  db->Put(write_options, "key2574", "value28116");

  // op 31497: get
  status = db->Get(read_options, "key8494", &value);
  if (status.ok()) {
    out << "GET key8494 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31498: get
  status = db->Get(read_options, "key10948", &value);
  if (status.ok()) {
    out << "GET key10948 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31499: get
  status = db->Get(read_options, "key3436", &value);
  if (status.ok()) {
    out << "GET key3436 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31500: delete_range
  status = db->DeleteRange(write_options, "key2008", "key2723");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 31501: put
  db->Put(write_options, "key4842", "value28071");

  // op 31502: delete
  db->Delete(write_options, "key5870");

  // op 31503: put
  db->Put(write_options, "key10658", "value24540");

  // op 31504: put
  db->Put(write_options, "key2776", "value6690");

  // op 31505: put
  db->Put(write_options, "key0166", "value27482");

  // op 31506: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 31507: get
  status = db->Get(read_options, "key3955", &value);
  if (status.ok()) {
    out << "GET key3955 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31508: put
  db->Put(write_options, "key6811", "value23296");

  // op 31509: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2799", "key11174", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2799, key11174) size=" << scan_result.size() << std::endl;
  }

  // op 31510: put
  db->Put(write_options, "key0384", "value92653");

  // op 31511: put
  db->Put(write_options, "key1445", "value4710");

  // op 31512: put
  db->Put(write_options, "key6992", "value42406");

  // op 31513: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5837", "key6356", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5837, key6356) size=" << scan_result.size() << std::endl;
  }

  // op 31514: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5084", "key6296", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5084, key6296) size=" << scan_result.size() << std::endl;
  }

  // op 31515: put
  db->Put(write_options, "key8884", "value85457");

  // op 31516: get
  status = db->Get(read_options, "key5175", &value);
  if (status.ok()) {
    out << "GET key5175 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31517: delete_range
  status = db->DeleteRange(write_options, "key2906", "key10401");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 31518: put
  db->Put(write_options, "key3170", "value86573");

  // op 31519: put
  db->Put(write_options, "key9876", "value53763");

  // op 31520: get
  status = db->Get(read_options, "key9418", &value);
  if (status.ok()) {
    out << "GET key9418 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31521: put
  db->Put(write_options, "key2508", "value58320");

  // op 31522: put
  db->Put(write_options, "key8112", "value21756");

  // op 31523: put
  db->Put(write_options, "key11086", "value33785");

  // op 31524: put
  db->Put(write_options, "key0264", "value9246");

  // op 31525: put
  db->Put(write_options, "key5112", "value0952");

  // op 31526: delete
  db->Delete(write_options, "key1841");

  // op 31527: get
  status = db->Get(read_options, "key6784", &value);
  if (status.ok()) {
    out << "GET key6784 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31528: put
  db->Put(write_options, "key6658", "value20348");

  // op 31529: put
  db->Put(write_options, "key0284", "value55593");

  // op 31530: put
  db->Put(write_options, "key10219", "value113887");

  // op 31531: delete
  db->Delete(write_options, "key9931");

  // op 31532: put
  db->Put(write_options, "key1063", "value16491");

  // op 31533: put
  db->Put(write_options, "key4502", "value37338");

  // op 31534: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 31535: put
  db->Put(write_options, "key4845", "value92912");

  // op 31536: put
  db->Put(write_options, "key8495", "value7791");

  // op 31537: get
  status = db->Get(read_options, "key8739", &value);
  if (status.ok()) {
    out << "GET key8739 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31538: put
  db->Put(write_options, "key10870", "value79215");

  // op 31539: get
  status = db->Get(read_options, "key8371", &value);
  if (status.ok()) {
    out << "GET key8371 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31540: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2108", "key2955", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2108, key2955) size=" << scan_result.size() << std::endl;
  }

  // op 31541: put
  db->Put(write_options, "key8288", "value31075");

  // op 31542: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1319", "key10268", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1319, key10268) size=" << scan_result.size() << std::endl;
  }

  // op 31543: get
  status = db->Get(read_options, "key2984", &value);
  if (status.ok()) {
    out << "GET key2984 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31544: put
  db->Put(write_options, "key6210", "value112721");

  // op 31545: put
  db->Put(write_options, "key6065", "value63553");

  // op 31546: put
  db->Put(write_options, "key6876", "value58773");

  // op 31547: put
  db->Put(write_options, "key5488", "value59931");

  // op 31548: delete
  db->Delete(write_options, "key10272");

  // op 31549: put
  db->Put(write_options, "key2868", "value45785");

  // op 31550: delete
  db->Delete(write_options, "key4409");

  // op 31551: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5306", "key10151", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5306, key10151) size=" << scan_result.size() << std::endl;
  }

  // op 31552: delete_range
  status = db->DeleteRange(write_options, "key7554", "key10827");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 31553: put
  db->Put(write_options, "key0786", "value23644");

  // op 31554: get
  status = db->Get(read_options, "key3660", &value);
  if (status.ok()) {
    out << "GET key3660 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31555: get
  status = db->Get(read_options, "key9627", &value);
  if (status.ok()) {
    out << "GET key9627 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31556: put
  db->Put(write_options, "key6935", "value27986");

  // op 31557: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7813", "key10336", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7813, key10336) size=" << scan_result.size() << std::endl;
  }

  // op 31558: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4765", "key5370", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4765, key5370) size=" << scan_result.size() << std::endl;
  }

  // op 31559: delete
  db->Delete(write_options, "key4248");

  // op 31560: get
  status = db->Get(read_options, "key6917", &value);
  if (status.ok()) {
    out << "GET key6917 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31561: put
  db->Put(write_options, "key4684", "value4094");

  // op 31562: get
  status = db->Get(read_options, "key7107", &value);
  if (status.ok()) {
    out << "GET key7107 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31563: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 31564: delete_range
  status = db->DeleteRange(write_options, "key2647", "key2730");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 31565: put
  db->Put(write_options, "key11366", "value100060");

  // op 31566: put
  db->Put(write_options, "key2646", "value78617");

  // op 31567: put
  db->Put(write_options, "key3636", "value31616");

  // op 31568: put
  db->Put(write_options, "key1504", "value101527");

  // op 31569: put
  db->Put(write_options, "key2480", "value15334");

  // op 31570: put
  db->Put(write_options, "key1781", "value8957");

  // op 31571: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4997", "key9676", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4997, key9676) size=" << scan_result.size() << std::endl;
  }

  // op 31572: put
  db->Put(write_options, "key10441", "value70338");

  // op 31573: put
  db->Put(write_options, "key9234", "value95353");

  // op 31574: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0405", "key0710", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0405, key0710) size=" << scan_result.size() << std::endl;
  }

  // op 31575: put
  db->Put(write_options, "key2625", "value48425");

  // op 31576: get
  status = db->Get(read_options, "key11248", &value);
  if (status.ok()) {
    out << "GET key11248 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31577: get
  status = db->Get(read_options, "key3815", &value);
  if (status.ok()) {
    out << "GET key3815 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31578: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3245", "key4336", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3245, key4336) size=" << scan_result.size() << std::endl;
  }

  // op 31579: delete
  db->Delete(write_options, "key7562");

  // op 31580: put
  db->Put(write_options, "key8557", "value76131");

  // op 31581: put
  db->Put(write_options, "key6884", "value36201");

  // op 31582: put
  db->Put(write_options, "key4132", "value78956");

  // op 31583: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 31584: put
  db->Put(write_options, "key8057", "value28956");

  // op 31585: put
  db->Put(write_options, "key1747", "value34323");

  // op 31586: put
  db->Put(write_options, "key2173", "value87715");

  // op 31587: get
  status = db->Get(read_options, "key3648", &value);
  if (status.ok()) {
    out << "GET key3648 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31588: get
  status = db->Get(read_options, "key5129", &value);
  if (status.ok()) {
    out << "GET key5129 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31589: put
  db->Put(write_options, "key6772", "value70417");

  // op 31590: put
  db->Put(write_options, "key11178", "value116145");

  // op 31591: get
  status = db->Get(read_options, "key10832", &value);
  if (status.ok()) {
    out << "GET key10832 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31592: delete_range
  status = db->DeleteRange(write_options, "key5403", "key11535");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 31593: put
  db->Put(write_options, "key4819", "value9616");

  // op 31594: delete
  db->Delete(write_options, "key2175");

  // op 31595: put
  db->Put(write_options, "key3459", "value13209");

  // op 31596: delete
  db->Delete(write_options, "key10596");

  // op 31597: put
  db->Put(write_options, "key11418", "value87305");

  // op 31598: put
  db->Put(write_options, "key1927", "value18797");

  // op 31599: get
  status = db->Get(read_options, "key1889", &value);
  if (status.ok()) {
    out << "GET key1889 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31600: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 31601: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 31602: put
  db->Put(write_options, "key5321", "value68782");

  // op 31603: put
  db->Put(write_options, "key1855", "value9732");

  // op 31604: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0588", "key8857", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0588, key8857) size=" << scan_result.size() << std::endl;
  }

  // op 31605: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2049", "key8691", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2049, key8691) size=" << scan_result.size() << std::endl;
  }

  // op 31606: delete_range
  status = db->DeleteRange(write_options, "key0862", "key9715");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 31607: get
  status = db->Get(read_options, "key10917", &value);
  if (status.ok()) {
    out << "GET key10917 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31608: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 31609: put
  db->Put(write_options, "key7589", "value91916");

  // op 31610: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0119", "key11651", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0119, key11651) size=" << scan_result.size() << std::endl;
  }

  // op 31611: delete
  db->Delete(write_options, "key1583");

  // op 31612: put
  db->Put(write_options, "key1166", "value63604");

  // op 31613: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3603", "key4148", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3603, key4148) size=" << scan_result.size() << std::endl;
  }

  // op 31614: put
  db->Put(write_options, "key3228", "value16899");

  // op 31615: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0835", "key8336", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0835, key8336) size=" << scan_result.size() << std::endl;
  }

  // op 31616: delete
  db->Delete(write_options, "key11207");

  // op 31617: put
  db->Put(write_options, "key0391", "value38776");

  // op 31618: put
  db->Put(write_options, "key0434", "value99715");

  // op 31619: put
  db->Put(write_options, "key4031", "value117806");

  // op 31620: put
  db->Put(write_options, "key7645", "value86025");

  // op 31621: delete
  db->Delete(write_options, "key1595");

  // op 31622: delete_range
  status = db->DeleteRange(write_options, "key1280", "key8654");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 31623: put
  db->Put(write_options, "key9573", "value42609");

  // op 31624: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2024", "key2516", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2024, key2516) size=" << scan_result.size() << std::endl;
  }

  // op 31625: get
  status = db->Get(read_options, "key8075", &value);
  if (status.ok()) {
    out << "GET key8075 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31626: put
  db->Put(write_options, "key6357", "value35742");

  // op 31627: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2002", "key2374", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2002, key2374) size=" << scan_result.size() << std::endl;
  }

  // op 31628: put
  db->Put(write_options, "key5086", "value56393");

  // op 31629: put
  db->Put(write_options, "key3113", "value88590");

  // op 31630: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 31631: get
  status = db->Get(read_options, "key0998", &value);
  if (status.ok()) {
    out << "GET key0998 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31632: delete
  db->Delete(write_options, "key1914");

  // op 31633: delete
  db->Delete(write_options, "key11665");

  // op 31634: put
  db->Put(write_options, "key8753", "value64894");

  // op 31635: get
  status = db->Get(read_options, "key2558", &value);
  if (status.ok()) {
    out << "GET key2558 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31636: put
  db->Put(write_options, "key1336", "value79345");

  // op 31637: put
  db->Put(write_options, "key9962", "value47909");

  // op 31638: put
  db->Put(write_options, "key9414", "value14469");

  // op 31639: delete_range
  status = db->DeleteRange(write_options, "key6883", "key9709");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 31640: put
  db->Put(write_options, "key0181", "value21204");

  // op 31641: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3344", "key5731", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3344, key5731) size=" << scan_result.size() << std::endl;
  }

  // op 31642: delete
  db->Delete(write_options, "key0188");

  // op 31643: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 31644: put
  db->Put(write_options, "key9166", "value54512");

  // op 31645: put
  db->Put(write_options, "key6719", "value34627");

  // op 31646: put
  db->Put(write_options, "key0570", "value54951");

  // op 31647: put
  db->Put(write_options, "key6502", "value5084");

  // op 31648: get
  status = db->Get(read_options, "key6042", &value);
  if (status.ok()) {
    out << "GET key6042 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31649: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 31650: get
  status = db->Get(read_options, "key9377", &value);
  if (status.ok()) {
    out << "GET key9377 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31651: put
  db->Put(write_options, "key0612", "value15389");

  // op 31652: delete_range
  status = db->DeleteRange(write_options, "key5791", "key9592");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 31653: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4697", "key8350", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4697, key8350) size=" << scan_result.size() << std::endl;
  }

  // op 31654: get
  status = db->Get(read_options, "key2202", &value);
  if (status.ok()) {
    out << "GET key2202 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31655: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 31656: put
  db->Put(write_options, "key11298", "value95141");

  // op 31657: put
  db->Put(write_options, "key10868", "value23744");

  // op 31658: put
  db->Put(write_options, "key0180", "value94585");

  // op 31659: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 31660: put
  db->Put(write_options, "key6347", "value74728");

  // op 31661: delete
  db->Delete(write_options, "key2978");

  // op 31662: get
  status = db->Get(read_options, "key6816", &value);
  if (status.ok()) {
    out << "GET key6816 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31663: get
  status = db->Get(read_options, "key3887", &value);
  if (status.ok()) {
    out << "GET key3887 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31664: put
  db->Put(write_options, "key4981", "value61439");

  // op 31665: delete_range
  status = db->DeleteRange(write_options, "key2810", "key5770");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 31666: get
  status = db->Get(read_options, "key8729", &value);
  if (status.ok()) {
    out << "GET key8729 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31667: delete
  db->Delete(write_options, "key11216");

  // op 31668: put
  db->Put(write_options, "key3158", "value109512");

  // op 31669: put
  db->Put(write_options, "key6384", "value101718");

  // op 31670: get
  status = db->Get(read_options, "key10720", &value);
  if (status.ok()) {
    out << "GET key10720 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31671: delete_range
  status = db->DeleteRange(write_options, "key3435", "key6183");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 31672: get
  status = db->Get(read_options, "key0483", &value);
  if (status.ok()) {
    out << "GET key0483 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31673: put
  db->Put(write_options, "key1594", "value28095");

  // op 31674: put
  db->Put(write_options, "key9318", "value2819");

  // op 31675: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7855", "key8465", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7855, key8465) size=" << scan_result.size() << std::endl;
  }

  // op 31676: delete_range
  status = db->DeleteRange(write_options, "key2487", "key2667");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 31677: delete_range
  status = db->DeleteRange(write_options, "key0849", "key5799");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 31678: put
  db->Put(write_options, "key3745", "value15067");

  // op 31679: get
  status = db->Get(read_options, "key2457", &value);
  if (status.ok()) {
    out << "GET key2457 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31680: put
  db->Put(write_options, "key1305", "value48232");

  // op 31681: put
  db->Put(write_options, "key3228", "value106606");

  // op 31682: put
  db->Put(write_options, "key3989", "value19168");

  // op 31683: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1179", "key5257", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1179, key5257) size=" << scan_result.size() << std::endl;
  }

  // op 31684: put
  db->Put(write_options, "key4299", "value64826");

  // op 31685: get
  status = db->Get(read_options, "key2899", &value);
  if (status.ok()) {
    out << "GET key2899 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31686: get
  status = db->Get(read_options, "key11530", &value);
  if (status.ok()) {
    out << "GET key11530 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31687: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5423", "key6598", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5423, key6598) size=" << scan_result.size() << std::endl;
  }

  // op 31688: put
  db->Put(write_options, "key10336", "value22196");

  // op 31689: put
  db->Put(write_options, "key4999", "value56536");

  // op 31690: delete_range
  status = db->DeleteRange(write_options, "key4905", "key5261");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 31691: delete
  db->Delete(write_options, "key8162");

  // op 31692: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2528", "key5085", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2528, key5085) size=" << scan_result.size() << std::endl;
  }

  // op 31693: put
  db->Put(write_options, "key4850", "value82035");

  // op 31694: put
  db->Put(write_options, "key6563", "value19699");

  // op 31695: put
  db->Put(write_options, "key9149", "value72505");

  // op 31696: get
  status = db->Get(read_options, "key11172", &value);
  if (status.ok()) {
    out << "GET key11172 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31697: delete
  db->Delete(write_options, "key2881");

  // op 31698: delete_range
  status = db->DeleteRange(write_options, "key2912", "key5085");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 31699: put
  db->Put(write_options, "key2762", "value108896");

  // op 31700: put
  db->Put(write_options, "key10960", "value83332");

  // op 31701: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 31702: put
  db->Put(write_options, "key1570", "value18853");

  // op 31703: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1121", "key11488", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1121, key11488) size=" << scan_result.size() << std::endl;
  }

  // op 31704: delete_range
  status = db->DeleteRange(write_options, "key3338", "key7572");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 31705: put
  db->Put(write_options, "key3110", "value84897");

  // op 31706: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 31707: put
  db->Put(write_options, "key3788", "value16702");

  // op 31708: put
  db->Put(write_options, "key3979", "value59887");

  // op 31709: get
  status = db->Get(read_options, "key9773", &value);
  if (status.ok()) {
    out << "GET key9773 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31710: put
  db->Put(write_options, "key5214", "value90212");

  // op 31711: put
  db->Put(write_options, "key9231", "value81690");

  // op 31712: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5281", "key8426", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5281, key8426) size=" << scan_result.size() << std::endl;
  }

  // op 31713: put
  db->Put(write_options, "key0252", "value67420");

  // op 31714: get
  status = db->Get(read_options, "key6621", &value);
  if (status.ok()) {
    out << "GET key6621 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31715: put
  db->Put(write_options, "key2186", "value83802");

  // op 31716: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 31717: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 31718: put
  db->Put(write_options, "key3117", "value68193");

  // op 31719: put
  db->Put(write_options, "key1546", "value112527");

  // op 31720: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 31721: put
  db->Put(write_options, "key0028", "value27673");

  // op 31722: get
  status = db->Get(read_options, "key3929", &value);
  if (status.ok()) {
    out << "GET key3929 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31723: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 31724: put
  db->Put(write_options, "key8006", "value46146");

  // op 31725: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0373", "key4427", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0373, key4427) size=" << scan_result.size() << std::endl;
  }

  // op 31726: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0048", "key8343", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0048, key8343) size=" << scan_result.size() << std::endl;
  }

  // op 31727: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2166", "key9515", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2166, key9515) size=" << scan_result.size() << std::endl;
  }

  // op 31728: put
  db->Put(write_options, "key8120", "value39092");

  // op 31729: put
  db->Put(write_options, "key6855", "value54439");

  // op 31730: get
  status = db->Get(read_options, "key9439", &value);
  if (status.ok()) {
    out << "GET key9439 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31731: delete
  db->Delete(write_options, "key4565");

  // op 31732: delete
  db->Delete(write_options, "key9222");

  // op 31733: put
  db->Put(write_options, "key10939", "value89453");

  // op 31734: delete_range
  status = db->DeleteRange(write_options, "key1096", "key4850");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 31735: delete_range
  status = db->DeleteRange(write_options, "key10016", "key11080");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 31736: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7514", "key10344", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7514, key10344) size=" << scan_result.size() << std::endl;
  }

  // op 31737: put
  db->Put(write_options, "key4509", "value71378");

  // op 31738: get
  status = db->Get(read_options, "key3440", &value);
  if (status.ok()) {
    out << "GET key3440 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31739: get
  status = db->Get(read_options, "key8668", &value);
  if (status.ok()) {
    out << "GET key8668 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31740: get
  status = db->Get(read_options, "key8882", &value);
  if (status.ok()) {
    out << "GET key8882 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31741: put
  db->Put(write_options, "key9901", "value39888");

  // op 31742: get
  status = db->Get(read_options, "key0001", &value);
  if (status.ok()) {
    out << "GET key0001 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31743: delete_range
  status = db->DeleteRange(write_options, "key2509", "key8425");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 31744: put
  db->Put(write_options, "key3531", "value59340");

  // op 31745: delete
  db->Delete(write_options, "key11676");

  // op 31746: put
  db->Put(write_options, "key11645", "value112236");

  // op 31747: delete
  db->Delete(write_options, "key3924");

  // op 31748: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 31749: delete_range
  status = db->DeleteRange(write_options, "key4014", "key4726");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 31750: get
  status = db->Get(read_options, "key4362", &value);
  if (status.ok()) {
    out << "GET key4362 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31751: delete_range
  status = db->DeleteRange(write_options, "key8357", "key9432");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 31752: get
  status = db->Get(read_options, "key10816", &value);
  if (status.ok()) {
    out << "GET key10816 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31753: get
  status = db->Get(read_options, "key2566", &value);
  if (status.ok()) {
    out << "GET key2566 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31754: put
  db->Put(write_options, "key5657", "value36025");

  // op 31755: put
  db->Put(write_options, "key5863", "value42447");

  // op 31756: put
  db->Put(write_options, "key6883", "value80119");

  // op 31757: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0277", "key10408", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0277, key10408) size=" << scan_result.size() << std::endl;
  }

  // op 31758: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0858", "key3991", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0858, key3991) size=" << scan_result.size() << std::endl;
  }

  // op 31759: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2580", "key10727", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2580, key10727) size=" << scan_result.size() << std::endl;
  }

  // op 31760: put
  db->Put(write_options, "key1589", "value96109");

  // op 31761: put
  db->Put(write_options, "key8047", "value71179");

  // op 31762: get
  status = db->Get(read_options, "key6685", &value);
  if (status.ok()) {
    out << "GET key6685 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31763: delete_range
  status = db->DeleteRange(write_options, "key3905", "key7562");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 31764: get
  status = db->Get(read_options, "key1795", &value);
  if (status.ok()) {
    out << "GET key1795 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31765: put
  db->Put(write_options, "key10282", "value49425");

  // op 31766: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0123", "key7201", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0123, key7201) size=" << scan_result.size() << std::endl;
  }

  // op 31767: put
  db->Put(write_options, "key8250", "value41030");

  // op 31768: put
  db->Put(write_options, "key9991", "value110464");

  // op 31769: put
  db->Put(write_options, "key9486", "value63489");

  // op 31770: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 31771: get
  status = db->Get(read_options, "key3274", &value);
  if (status.ok()) {
    out << "GET key3274 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31772: get
  status = db->Get(read_options, "key9996", &value);
  if (status.ok()) {
    out << "GET key9996 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31773: put
  db->Put(write_options, "key2027", "value77460");

  // op 31774: delete
  db->Delete(write_options, "key4203");

  // op 31775: put
  db->Put(write_options, "key6441", "value49163");

  // op 31776: delete
  db->Delete(write_options, "key5451");

  // op 31777: get
  status = db->Get(read_options, "key3955", &value);
  if (status.ok()) {
    out << "GET key3955 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31778: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1522", "key6192", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1522, key6192) size=" << scan_result.size() << std::endl;
  }

  // op 31779: put
  db->Put(write_options, "key1363", "value21176");

  // op 31780: put
  db->Put(write_options, "key3544", "value76052");

  // op 31781: put
  db->Put(write_options, "key7010", "value42942");

  // op 31782: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4072", "key4211", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4072, key4211) size=" << scan_result.size() << std::endl;
  }

  // op 31783: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6157", "key11428", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6157, key11428) size=" << scan_result.size() << std::endl;
  }

  // op 31784: put
  db->Put(write_options, "key0170", "value91596");

  // op 31785: delete_range
  status = db->DeleteRange(write_options, "key1107", "key10343");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 31786: delete
  db->Delete(write_options, "key2374");

  // op 31787: get
  status = db->Get(read_options, "key3237", &value);
  if (status.ok()) {
    out << "GET key3237 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31788: get
  status = db->Get(read_options, "key3577", &value);
  if (status.ok()) {
    out << "GET key3577 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31789: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2255", "key10695", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2255, key10695) size=" << scan_result.size() << std::endl;
  }

  // op 31790: put
  db->Put(write_options, "key8076", "value110243");

  // op 31791: delete
  db->Delete(write_options, "key2103");

  // op 31792: get
  status = db->Get(read_options, "key1436", &value);
  if (status.ok()) {
    out << "GET key1436 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31793: delete_range
  status = db->DeleteRange(write_options, "key4769", "key9509");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 31794: put
  db->Put(write_options, "key1406", "value94445");

  // op 31795: delete_range
  status = db->DeleteRange(write_options, "key0698", "key7228");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 31796: get
  status = db->Get(read_options, "key3286", &value);
  if (status.ok()) {
    out << "GET key3286 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31797: put
  db->Put(write_options, "key6176", "value110361");

  // op 31798: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0785", "key7345", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0785, key7345) size=" << scan_result.size() << std::endl;
  }

  // op 31799: delete_range
  status = db->DeleteRange(write_options, "key5938", "key8620");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 31800: put
  db->Put(write_options, "key2665", "value49250");

  // op 31801: get
  status = db->Get(read_options, "key8356", &value);
  if (status.ok()) {
    out << "GET key8356 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31802: put
  db->Put(write_options, "key4228", "value72676");

  // op 31803: put
  db->Put(write_options, "key11567", "value0600");

  // op 31804: get
  status = db->Get(read_options, "key7347", &value);
  if (status.ok()) {
    out << "GET key7347 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31805: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9250", "key11040", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9250, key11040) size=" << scan_result.size() << std::endl;
  }

  // op 31806: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 31807: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0934", "key11601", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0934, key11601) size=" << scan_result.size() << std::endl;
  }

  // op 31808: put
  db->Put(write_options, "key0041", "value60868");

  // op 31809: put
  db->Put(write_options, "key0763", "value84326");

  // op 31810: delete
  db->Delete(write_options, "key8982");

  // op 31811: put
  db->Put(write_options, "key9525", "value67630");

  // op 31812: put
  db->Put(write_options, "key6087", "value67334");

  // op 31813: put
  db->Put(write_options, "key8385", "value90163");

  // op 31814: get
  status = db->Get(read_options, "key5937", &value);
  if (status.ok()) {
    out << "GET key5937 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31815: get
  status = db->Get(read_options, "key6608", &value);
  if (status.ok()) {
    out << "GET key6608 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31816: delete_range
  status = db->DeleteRange(write_options, "key3374", "key11465");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 31817: get
  status = db->Get(read_options, "key6196", &value);
  if (status.ok()) {
    out << "GET key6196 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31818: get
  status = db->Get(read_options, "key0741", &value);
  if (status.ok()) {
    out << "GET key0741 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31819: get
  status = db->Get(read_options, "key2531", &value);
  if (status.ok()) {
    out << "GET key2531 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31820: put
  db->Put(write_options, "key5799", "value105243");

  // op 31821: put
  db->Put(write_options, "key9075", "value47375");

  // op 31822: put
  db->Put(write_options, "key9890", "value84202");

  // op 31823: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 31824: delete_range
  status = db->DeleteRange(write_options, "key0485", "key1406");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 31825: put
  db->Put(write_options, "key11686", "value42860");

  // op 31826: put
  db->Put(write_options, "key8377", "value115615");

  // op 31827: delete
  db->Delete(write_options, "key0563");

  // op 31828: delete_range
  status = db->DeleteRange(write_options, "key5036", "key8159");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 31829: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4643", "key10204", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4643, key10204) size=" << scan_result.size() << std::endl;
  }

  // op 31830: put
  db->Put(write_options, "key7458", "value91855");

  // op 31831: get
  status = db->Get(read_options, "key9655", &value);
  if (status.ok()) {
    out << "GET key9655 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31832: put
  db->Put(write_options, "key9506", "value49162");

  // op 31833: get
  status = db->Get(read_options, "key9292", &value);
  if (status.ok()) {
    out << "GET key9292 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31834: get
  status = db->Get(read_options, "key8669", &value);
  if (status.ok()) {
    out << "GET key8669 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31835: put
  db->Put(write_options, "key4035", "value76736");

  // op 31836: put
  db->Put(write_options, "key6268", "value34079");

  // op 31837: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0320", "key5494", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0320, key5494) size=" << scan_result.size() << std::endl;
  }

  // op 31838: get
  status = db->Get(read_options, "key0856", &value);
  if (status.ok()) {
    out << "GET key0856 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31839: get
  status = db->Get(read_options, "key1143", &value);
  if (status.ok()) {
    out << "GET key1143 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31840: put
  db->Put(write_options, "key5493", "value4104");

  // op 31841: delete_range
  status = db->DeleteRange(write_options, "key0661", "key7524");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 31842: put
  db->Put(write_options, "key0705", "value17193");

  // op 31843: get
  status = db->Get(read_options, "key0158", &value);
  if (status.ok()) {
    out << "GET key0158 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31844: delete_range
  status = db->DeleteRange(write_options, "key4281", "key11035");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 31845: put
  db->Put(write_options, "key3004", "value94680");

  // op 31846: delete
  db->Delete(write_options, "key6721");

  // op 31847: put
  db->Put(write_options, "key4444", "value62361");

  // op 31848: put
  db->Put(write_options, "key2422", "value79665");

  // op 31849: put
  db->Put(write_options, "key1577", "value95100");

  // op 31850: delete_range
  status = db->DeleteRange(write_options, "key2974", "key6108");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 31851: get
  status = db->Get(read_options, "key10635", &value);
  if (status.ok()) {
    out << "GET key10635 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31852: get
  status = db->Get(read_options, "key9347", &value);
  if (status.ok()) {
    out << "GET key9347 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31853: put
  db->Put(write_options, "key3919", "value41188");

  // op 31854: delete
  db->Delete(write_options, "key5320");

  // op 31855: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2573", "key3587", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2573, key3587) size=" << scan_result.size() << std::endl;
  }

  // op 31856: put
  db->Put(write_options, "key8221", "value80651");

  // op 31857: delete
  db->Delete(write_options, "key2423");

  // op 31858: delete
  db->Delete(write_options, "key0981");

  // op 31859: put
  db->Put(write_options, "key5816", "value73285");

  // op 31860: put
  db->Put(write_options, "key8921", "value63126");

  // op 31861: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2751", "key10468", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2751, key10468) size=" << scan_result.size() << std::endl;
  }

  // op 31862: put
  db->Put(write_options, "key8008", "value51302");

  // op 31863: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2268", "key3440", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2268, key3440) size=" << scan_result.size() << std::endl;
  }

  // op 31864: put
  db->Put(write_options, "key9021", "value1424");

  // op 31865: delete
  db->Delete(write_options, "key9466");

  // op 31866: put
  db->Put(write_options, "key11142", "value83115");

  // op 31867: get
  status = db->Get(read_options, "key1109", &value);
  if (status.ok()) {
    out << "GET key1109 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31868: put
  db->Put(write_options, "key11592", "value31973");

  // op 31869: put
  db->Put(write_options, "key1595", "value112011");

  // op 31870: get
  status = db->Get(read_options, "key10589", &value);
  if (status.ok()) {
    out << "GET key10589 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31871: delete
  db->Delete(write_options, "key4088");

  // op 31872: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0391", "key4840", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0391, key4840) size=" << scan_result.size() << std::endl;
  }

  // op 31873: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4012", "key8804", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4012, key8804) size=" << scan_result.size() << std::endl;
  }

  // op 31874: put
  db->Put(write_options, "key9856", "value49665");

  // op 31875: put
  db->Put(write_options, "key5863", "value34573");

  // op 31876: put
  db->Put(write_options, "key2641", "value78123");

  // op 31877: put
  db->Put(write_options, "key1957", "value37731");

  // op 31878: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5223", "key9040", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5223, key9040) size=" << scan_result.size() << std::endl;
  }

  // op 31879: put
  db->Put(write_options, "key7038", "value94165");

  // op 31880: put
  db->Put(write_options, "key4449", "value44463");

  // op 31881: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3909", "key6362", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3909, key6362) size=" << scan_result.size() << std::endl;
  }

  // op 31882: delete_range
  status = db->DeleteRange(write_options, "key2563", "key10524");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 31883: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 31884: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 31885: put
  db->Put(write_options, "key6020", "value2535");

  // op 31886: get
  status = db->Get(read_options, "key8246", &value);
  if (status.ok()) {
    out << "GET key8246 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31887: delete_range
  status = db->DeleteRange(write_options, "key0319", "key6760");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 31888: put
  db->Put(write_options, "key7962", "value105686");

  // op 31889: delete_range
  status = db->DeleteRange(write_options, "key1228", "key10451");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 31890: get
  status = db->Get(read_options, "key10230", &value);
  if (status.ok()) {
    out << "GET key10230 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31891: get
  status = db->Get(read_options, "key1635", &value);
  if (status.ok()) {
    out << "GET key1635 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31892: put
  db->Put(write_options, "key7885", "value107369");

  // op 31893: put
  db->Put(write_options, "key6125", "value95828");

  // op 31894: get
  status = db->Get(read_options, "key9764", &value);
  if (status.ok()) {
    out << "GET key9764 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31895: delete
  db->Delete(write_options, "key11155");

  // op 31896: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6090", "key10259", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6090, key10259) size=" << scan_result.size() << std::endl;
  }

  // op 31897: get
  status = db->Get(read_options, "key5532", &value);
  if (status.ok()) {
    out << "GET key5532 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31898: delete_range
  status = db->DeleteRange(write_options, "key2241", "key3735");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 31899: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6037", "key10092", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6037, key10092) size=" << scan_result.size() << std::endl;
  }

  // op 31900: delete
  db->Delete(write_options, "key0167");

  // op 31901: put
  db->Put(write_options, "key10427", "value8018");

  // op 31902: put
  db->Put(write_options, "key6314", "value90798");

  // op 31903: put
  db->Put(write_options, "key7235", "value59134");

  // op 31904: delete_range
  status = db->DeleteRange(write_options, "key0915", "key3455");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 31905: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0653", "key3556", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0653, key3556) size=" << scan_result.size() << std::endl;
  }

  // op 31906: get
  status = db->Get(read_options, "key7146", &value);
  if (status.ok()) {
    out << "GET key7146 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31907: put
  db->Put(write_options, "key2741", "value28929");

  // op 31908: put
  db->Put(write_options, "key9126", "value88775");

  // op 31909: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 31910: delete
  db->Delete(write_options, "key6479");

  // op 31911: put
  db->Put(write_options, "key6140", "value113455");

  // op 31912: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6040", "key6064", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6040, key6064) size=" << scan_result.size() << std::endl;
  }

  // op 31913: delete
  db->Delete(write_options, "key6326");

  // op 31914: get
  status = db->Get(read_options, "key5089", &value);
  if (status.ok()) {
    out << "GET key5089 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31915: put
  db->Put(write_options, "key1185", "value16707");

  // op 31916: delete
  db->Delete(write_options, "key3317");

  // op 31917: put
  db->Put(write_options, "key3031", "value103518");

  // op 31918: delete
  db->Delete(write_options, "key5323");

  // op 31919: delete
  db->Delete(write_options, "key1128");

  // op 31920: get
  status = db->Get(read_options, "key3676", &value);
  if (status.ok()) {
    out << "GET key3676 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31921: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 31922: put
  db->Put(write_options, "key2823", "value53864");

  // op 31923: put
  db->Put(write_options, "key0531", "value97406");

  // op 31924: put
  db->Put(write_options, "key2466", "value34576");

  // op 31925: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0492", "key1184", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0492, key1184) size=" << scan_result.size() << std::endl;
  }

  // op 31926: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1304", "key8447", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1304, key8447) size=" << scan_result.size() << std::endl;
  }

  // op 31927: delete_range
  status = db->DeleteRange(write_options, "key4743", "key6092");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 31928: get
  status = db->Get(read_options, "key3713", &value);
  if (status.ok()) {
    out << "GET key3713 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31929: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4296", "key11050", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4296, key11050) size=" << scan_result.size() << std::endl;
  }

  // op 31930: delete_range
  status = db->DeleteRange(write_options, "key2132", "key4747");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 31931: delete
  db->Delete(write_options, "key6962");

  // op 31932: put
  db->Put(write_options, "key2121", "value70694");

  // op 31933: put
  db->Put(write_options, "key9747", "value51364");

  // op 31934: get
  status = db->Get(read_options, "key10549", &value);
  if (status.ok()) {
    out << "GET key10549 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31935: delete_range
  status = db->DeleteRange(write_options, "key5378", "key11052");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 31936: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1812", "key4553", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1812, key4553) size=" << scan_result.size() << std::endl;
  }

  // op 31937: put
  db->Put(write_options, "key4619", "value49752");

  // op 31938: put
  db->Put(write_options, "key2776", "value78522");

  // op 31939: get
  status = db->Get(read_options, "key9076", &value);
  if (status.ok()) {
    out << "GET key9076 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31940: put
  db->Put(write_options, "key9554", "value52711");

  // op 31941: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8151", "key8658", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8151, key8658) size=" << scan_result.size() << std::endl;
  }

  // op 31942: delete_range
  status = db->DeleteRange(write_options, "key2488", "key4486");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 31943: get
  status = db->Get(read_options, "key5599", &value);
  if (status.ok()) {
    out << "GET key5599 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31944: put
  db->Put(write_options, "key8744", "value68934");

  // op 31945: put
  db->Put(write_options, "key6063", "value31452");

  // op 31946: put
  db->Put(write_options, "key11272", "value34657");

  // op 31947: delete
  db->Delete(write_options, "key3097");

  // op 31948: get
  status = db->Get(read_options, "key10389", &value);
  if (status.ok()) {
    out << "GET key10389 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31949: put
  db->Put(write_options, "key0798", "value100900");

  // op 31950: put
  db->Put(write_options, "key6794", "value70735");

  // op 31951: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 31952: put
  db->Put(write_options, "key4546", "value43187");

  // op 31953: delete_range
  status = db->DeleteRange(write_options, "key0418", "key9711");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 31954: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 31955: put
  db->Put(write_options, "key4689", "value117961");

  // op 31956: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6408", "key10891", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6408, key10891) size=" << scan_result.size() << std::endl;
  }

  // op 31957: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0588", "key4004", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0588, key4004) size=" << scan_result.size() << std::endl;
  }

  // op 31958: put
  db->Put(write_options, "key10761", "value73129");

  // op 31959: put
  db->Put(write_options, "key0696", "value98754");

  // op 31960: delete
  db->Delete(write_options, "key9031");

  // op 31961: put
  db->Put(write_options, "key4319", "value47908");

  // op 31962: delete
  db->Delete(write_options, "key0663");

  // op 31963: delete
  db->Delete(write_options, "key9956");

  // op 31964: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7898", "key11087", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7898, key11087) size=" << scan_result.size() << std::endl;
  }

  // op 31965: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5674", "key7547", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5674, key7547) size=" << scan_result.size() << std::endl;
  }

  // op 31966: get
  status = db->Get(read_options, "key9063", &value);
  if (status.ok()) {
    out << "GET key9063 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31967: delete
  db->Delete(write_options, "key3299");

  // op 31968: put
  db->Put(write_options, "key5704", "value12234");

  // op 31969: delete_range
  status = db->DeleteRange(write_options, "key0891", "key4602");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 31970: put
  db->Put(write_options, "key5575", "value81598");

  // op 31971: put
  db->Put(write_options, "key9843", "value7398");

  // op 31972: delete
  db->Delete(write_options, "key2496");

  // op 31973: put
  db->Put(write_options, "key9722", "value2325");

  // op 31974: put
  db->Put(write_options, "key1960", "value22015");

  // op 31975: delete_range
  status = db->DeleteRange(write_options, "key0584", "key5963");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 31976: delete_range
  status = db->DeleteRange(write_options, "key4013", "key7723");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 31977: put
  db->Put(write_options, "key3585", "value45415");

  // op 31978: delete_range
  status = db->DeleteRange(write_options, "key4243", "key5933");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 31979: put
  db->Put(write_options, "key5152", "value64572");

  // op 31980: put
  db->Put(write_options, "key2727", "value94555");

  // op 31981: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0186", "key0598", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0186, key0598) size=" << scan_result.size() << std::endl;
  }

  // op 31982: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3414", "key6912", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3414, key6912) size=" << scan_result.size() << std::endl;
  }

  // op 31983: put
  db->Put(write_options, "key8235", "value5634");

  // op 31984: put
  db->Put(write_options, "key9503", "value86974");

  // op 31985: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 31986: get
  status = db->Get(read_options, "key7108", &value);
  if (status.ok()) {
    out << "GET key7108 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31987: put
  db->Put(write_options, "key2514", "value31927");

  // op 31988: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 31989: delete_range
  status = db->DeleteRange(write_options, "key7340", "key11364");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 31990: get
  status = db->Get(read_options, "key11715", &value);
  if (status.ok()) {
    out << "GET key11715 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31991: get
  status = db->Get(read_options, "key0460", &value);
  if (status.ok()) {
    out << "GET key0460 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31992: put
  db->Put(write_options, "key9538", "value15067");

  // op 31993: delete_range
  status = db->DeleteRange(write_options, "key9712", "key11597");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 31994: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0176", "key1461", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0176, key1461) size=" << scan_result.size() << std::endl;
  }

  // op 31995: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0496", "key4026", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0496, key4026) size=" << scan_result.size() << std::endl;
  }

  // op 31996: put
  db->Put(write_options, "key4580", "value53004");

  // op 31997: get
  status = db->Get(read_options, "key7117", &value);
  if (status.ok()) {
    out << "GET key7117 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 31998: delete_range
  status = db->DeleteRange(write_options, "key1119", "key11281");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 31999: delete
  db->Delete(write_options, "key2388");

  // op 32000: delete_range
  status = db->DeleteRange(write_options, "key7743", "key10631");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 32001: put
  db->Put(write_options, "key7601", "value49928");

  // op 32002: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4854", "key9062", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4854, key9062) size=" << scan_result.size() << std::endl;
  }

  // op 32003: delete
  db->Delete(write_options, "key10568");

  // op 32004: get
  status = db->Get(read_options, "key11246", &value);
  if (status.ok()) {
    out << "GET key11246 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32005: put
  db->Put(write_options, "key5137", "value80614");

  // op 32006: get
  status = db->Get(read_options, "key9192", &value);
  if (status.ok()) {
    out << "GET key9192 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32007: get
  status = db->Get(read_options, "key2725", &value);
  if (status.ok()) {
    out << "GET key2725 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32008: get
  status = db->Get(read_options, "key3228", &value);
  if (status.ok()) {
    out << "GET key3228 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32009: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 32010: get
  status = db->Get(read_options, "key4785", &value);
  if (status.ok()) {
    out << "GET key4785 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32011: delete_range
  status = db->DeleteRange(write_options, "key6928", "key10462");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 32012: delete_range
  status = db->DeleteRange(write_options, "key4040", "key8971");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 32013: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4594", "key7064", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4594, key7064) size=" << scan_result.size() << std::endl;
  }

  // op 32014: put
  db->Put(write_options, "key8540", "value101638");

  // op 32015: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4467", "key4482", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4467, key4482) size=" << scan_result.size() << std::endl;
  }

  // op 32016: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8521", "key9746", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8521, key9746) size=" << scan_result.size() << std::endl;
  }

  // op 32017: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4175", "key8070", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4175, key8070) size=" << scan_result.size() << std::endl;
  }

  // op 32018: put
  db->Put(write_options, "key0696", "value87462");

  // op 32019: get
  status = db->Get(read_options, "key1593", &value);
  if (status.ok()) {
    out << "GET key1593 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32020: delete_range
  status = db->DeleteRange(write_options, "key3793", "key5937");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 32021: put
  db->Put(write_options, "key11013", "value18706");

  // op 32022: put
  db->Put(write_options, "key7012", "value33737");

  // op 32023: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1534", "key8285", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1534, key8285) size=" << scan_result.size() << std::endl;
  }

  // op 32024: put
  db->Put(write_options, "key11248", "value27055");

  // op 32025: get
  status = db->Get(read_options, "key9762", &value);
  if (status.ok()) {
    out << "GET key9762 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32026: put
  db->Put(write_options, "key11060", "value15632");

  // op 32027: delete_range
  status = db->DeleteRange(write_options, "key4249", "key5023");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 32028: put
  db->Put(write_options, "key10561", "value73154");

  // op 32029: put
  db->Put(write_options, "key4839", "value114329");

  // op 32030: put
  db->Put(write_options, "key9450", "value44328");

  // op 32031: put
  db->Put(write_options, "key3360", "value42008");

  // op 32032: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6918", "key9214", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6918, key9214) size=" << scan_result.size() << std::endl;
  }

  // op 32033: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1338", "key2206", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1338, key2206) size=" << scan_result.size() << std::endl;
  }

  // op 32034: put
  db->Put(write_options, "key0179", "value15556");

  // op 32035: put
  db->Put(write_options, "key5800", "value22518");

  // op 32036: put
  db->Put(write_options, "key1836", "value104673");

  // op 32037: get
  status = db->Get(read_options, "key6806", &value);
  if (status.ok()) {
    out << "GET key6806 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32038: put
  db->Put(write_options, "key2991", "value24844");

  // op 32039: delete_range
  status = db->DeleteRange(write_options, "key1569", "key10918");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 32040: put
  db->Put(write_options, "key0857", "value41588");

  // op 32041: put
  db->Put(write_options, "key9350", "value61157");

  // op 32042: put
  db->Put(write_options, "key4351", "value60095");

  // op 32043: put
  db->Put(write_options, "key11014", "value35616");

  // op 32044: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3911", "key9699", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3911, key9699) size=" << scan_result.size() << std::endl;
  }

  // op 32045: put
  db->Put(write_options, "key2283", "value17680");

  // op 32046: get
  status = db->Get(read_options, "key2618", &value);
  if (status.ok()) {
    out << "GET key2618 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32047: put
  db->Put(write_options, "key4725", "value61624");

  // op 32048: delete_range
  status = db->DeleteRange(write_options, "key2704", "key8923");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 32049: get
  status = db->Get(read_options, "key9630", &value);
  if (status.ok()) {
    out << "GET key9630 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32050: get
  status = db->Get(read_options, "key3809", &value);
  if (status.ok()) {
    out << "GET key3809 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32051: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0652", "key3384", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0652, key3384) size=" << scan_result.size() << std::endl;
  }

  // op 32052: put
  db->Put(write_options, "key1354", "value20633");

  // op 32053: delete
  db->Delete(write_options, "key0579");

  // op 32054: delete_range
  status = db->DeleteRange(write_options, "key5210", "key7430");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 32055: get
  status = db->Get(read_options, "key0507", &value);
  if (status.ok()) {
    out << "GET key0507 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32056: put
  db->Put(write_options, "key10564", "value90582");

  // op 32057: put
  db->Put(write_options, "key11216", "value60693");

  // op 32058: put
  db->Put(write_options, "key5006", "value108606");

  // op 32059: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0525", "key0585", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0525, key0585) size=" << scan_result.size() << std::endl;
  }

  // op 32060: put
  db->Put(write_options, "key7031", "value39869");

  // op 32061: delete
  db->Delete(write_options, "key8641");

  // op 32062: put
  db->Put(write_options, "key4089", "value33431");

  // op 32063: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6352", "key11002", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6352, key11002) size=" << scan_result.size() << std::endl;
  }

  // op 32064: put
  db->Put(write_options, "key7227", "value76369");

  // op 32065: put
  db->Put(write_options, "key5715", "value109426");

  // op 32066: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 32067: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 32068: put
  db->Put(write_options, "key5440", "value103913");

  // op 32069: put
  db->Put(write_options, "key6412", "value62418");

  // op 32070: put
  db->Put(write_options, "key5863", "value106931");

  // op 32071: get
  status = db->Get(read_options, "key2697", &value);
  if (status.ok()) {
    out << "GET key2697 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32072: put
  db->Put(write_options, "key2193", "value95170");

  // op 32073: delete
  db->Delete(write_options, "key2490");

  // op 32074: get
  status = db->Get(read_options, "key7488", &value);
  if (status.ok()) {
    out << "GET key7488 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32075: delete_range
  status = db->DeleteRange(write_options, "key4312", "key9342");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 32076: put
  db->Put(write_options, "key4576", "value49738");

  // op 32077: get
  status = db->Get(read_options, "key8095", &value);
  if (status.ok()) {
    out << "GET key8095 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32078: delete
  db->Delete(write_options, "key4264");

  // op 32079: put
  db->Put(write_options, "key2997", "value111662");

  // op 32080: delete
  db->Delete(write_options, "key5219");

  // op 32081: put
  db->Put(write_options, "key11419", "value10270");

  // op 32082: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5778", "key8998", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5778, key8998) size=" << scan_result.size() << std::endl;
  }

  // op 32083: put
  db->Put(write_options, "key10455", "value107101");

  // op 32084: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7574", "key7967", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7574, key7967) size=" << scan_result.size() << std::endl;
  }

  // op 32085: put
  db->Put(write_options, "key0632", "value80560");

  // op 32086: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3637", "key11455", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3637, key11455) size=" << scan_result.size() << std::endl;
  }

  // op 32087: delete_range
  status = db->DeleteRange(write_options, "key8877", "key9759");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 32088: put
  db->Put(write_options, "key8553", "value46021");

  // op 32089: delete
  db->Delete(write_options, "key4473");

  // op 32090: put
  db->Put(write_options, "key4325", "value5039");

  // op 32091: put
  db->Put(write_options, "key7935", "value48151");

  // op 32092: put
  db->Put(write_options, "key4230", "value114216");

  // op 32093: put
  db->Put(write_options, "key9785", "value10181");

  // op 32094: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6781", "key8491", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6781, key8491) size=" << scan_result.size() << std::endl;
  }

  // op 32095: get
  status = db->Get(read_options, "key8367", &value);
  if (status.ok()) {
    out << "GET key8367 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32096: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9850", "key10129", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9850, key10129) size=" << scan_result.size() << std::endl;
  }

  // op 32097: put
  db->Put(write_options, "key4382", "value98948");

  // op 32098: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 32099: delete
  db->Delete(write_options, "key5549");

  // op 32100: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1325", "key6700", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1325, key6700) size=" << scan_result.size() << std::endl;
  }

  // op 32101: put
  db->Put(write_options, "key10254", "value76562");

  // op 32102: put
  db->Put(write_options, "key7022", "value54886");

  // op 32103: put
  db->Put(write_options, "key3010", "value12351");

  // op 32104: put
  db->Put(write_options, "key3229", "value26477");

  // op 32105: get
  status = db->Get(read_options, "key9141", &value);
  if (status.ok()) {
    out << "GET key9141 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32106: delete_range
  status = db->DeleteRange(write_options, "key5787", "key11192");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 32107: get
  status = db->Get(read_options, "key1557", &value);
  if (status.ok()) {
    out << "GET key1557 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32108: put
  db->Put(write_options, "key7366", "value25243");

  // op 32109: put
  db->Put(write_options, "key3899", "value104143");

  // op 32110: get
  status = db->Get(read_options, "key0169", &value);
  if (status.ok()) {
    out << "GET key0169 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32111: delete
  db->Delete(write_options, "key2267");

  // op 32112: put
  db->Put(write_options, "key5465", "value71583");

  // op 32113: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10077", "key11404", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10077, key11404) size=" << scan_result.size() << std::endl;
  }

  // op 32114: delete
  db->Delete(write_options, "key8946");

  // op 32115: put
  db->Put(write_options, "key7666", "value106886");

  // op 32116: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2765", "key11715", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2765, key11715) size=" << scan_result.size() << std::endl;
  }

  // op 32117: get
  status = db->Get(read_options, "key2327", &value);
  if (status.ok()) {
    out << "GET key2327 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32118: put
  db->Put(write_options, "key9355", "value101271");

  // op 32119: put
  db->Put(write_options, "key0156", "value114111");

  // op 32120: delete_range
  status = db->DeleteRange(write_options, "key2297", "key8647");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 32121: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 32122: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7061", "key11559", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7061, key11559) size=" << scan_result.size() << std::endl;
  }

  // op 32123: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3500", "key8791", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3500, key8791) size=" << scan_result.size() << std::endl;
  }

  // op 32124: delete
  db->Delete(write_options, "key2684");

  // op 32125: put
  db->Put(write_options, "key1758", "value4755");

  // op 32126: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4590", "key7041", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4590, key7041) size=" << scan_result.size() << std::endl;
  }

  // op 32127: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4555", "key8237", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4555, key8237) size=" << scan_result.size() << std::endl;
  }

  // op 32128: put
  db->Put(write_options, "key4558", "value32029");

  // op 32129: get
  status = db->Get(read_options, "key1232", &value);
  if (status.ok()) {
    out << "GET key1232 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32130: delete
  db->Delete(write_options, "key3496");

  // op 32131: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 32132: put
  db->Put(write_options, "key10004", "value48926");

  // op 32133: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6638", "key7098", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6638, key7098) size=" << scan_result.size() << std::endl;
  }

  // op 32134: get
  status = db->Get(read_options, "key6622", &value);
  if (status.ok()) {
    out << "GET key6622 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32135: put
  db->Put(write_options, "key8788", "value84892");

  // op 32136: delete
  db->Delete(write_options, "key0090");

  // op 32137: get
  status = db->Get(read_options, "key6389", &value);
  if (status.ok()) {
    out << "GET key6389 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32138: delete_range
  status = db->DeleteRange(write_options, "key3651", "key11075");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 32139: put
  db->Put(write_options, "key9362", "value6157");

  // op 32140: delete
  db->Delete(write_options, "key0154");

  // op 32141: put
  db->Put(write_options, "key8508", "value73064");

  // op 32142: get
  status = db->Get(read_options, "key9727", &value);
  if (status.ok()) {
    out << "GET key9727 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32143: put
  db->Put(write_options, "key3001", "value11421");

  // op 32144: delete_range
  status = db->DeleteRange(write_options, "key3074", "key8017");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 32145: get
  status = db->Get(read_options, "key2709", &value);
  if (status.ok()) {
    out << "GET key2709 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32146: get
  status = db->Get(read_options, "key5651", &value);
  if (status.ok()) {
    out << "GET key5651 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32147: put
  db->Put(write_options, "key8996", "value59167");

  // op 32148: get
  status = db->Get(read_options, "key10032", &value);
  if (status.ok()) {
    out << "GET key10032 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32149: get
  status = db->Get(read_options, "key9103", &value);
  if (status.ok()) {
    out << "GET key9103 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32150: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6344", "key8893", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6344, key8893) size=" << scan_result.size() << std::endl;
  }

  // op 32151: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1583", "key7005", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1583, key7005) size=" << scan_result.size() << std::endl;
  }

  // op 32152: delete_range
  status = db->DeleteRange(write_options, "key2172", "key4662");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 32153: get
  status = db->Get(read_options, "key1604", &value);
  if (status.ok()) {
    out << "GET key1604 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32154: put
  db->Put(write_options, "key9532", "value44653");

  // op 32155: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1811", "key9119", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1811, key9119) size=" << scan_result.size() << std::endl;
  }

  // op 32156: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 32157: put
  db->Put(write_options, "key2670", "value15549");

  // op 32158: put
  db->Put(write_options, "key1261", "value81147");

  // op 32159: put
  db->Put(write_options, "key5991", "value77166");

  // op 32160: delete
  db->Delete(write_options, "key11458");

  // op 32161: put
  db->Put(write_options, "key10224", "value3339");

  // op 32162: delete_range
  status = db->DeleteRange(write_options, "key4096", "key8219");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 32163: delete
  db->Delete(write_options, "key6974");

  // op 32164: put
  db->Put(write_options, "key6451", "value92032");

  // op 32165: get
  status = db->Get(read_options, "key5818", &value);
  if (status.ok()) {
    out << "GET key5818 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32166: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1176", "key1595", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1176, key1595) size=" << scan_result.size() << std::endl;
  }

  // op 32167: delete_range
  status = db->DeleteRange(write_options, "key1394", "key1931");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 32168: put
  db->Put(write_options, "key0801", "value54814");

  // op 32169: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1156", "key9636", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1156, key9636) size=" << scan_result.size() << std::endl;
  }

  // op 32170: put
  db->Put(write_options, "key11314", "value96331");

  // op 32171: put
  db->Put(write_options, "key7361", "value5706");

  // op 32172: put
  db->Put(write_options, "key4533", "value82087");

  // op 32173: get
  status = db->Get(read_options, "key5499", &value);
  if (status.ok()) {
    out << "GET key5499 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32174: put
  db->Put(write_options, "key10738", "value60209");

  // op 32175: put
  db->Put(write_options, "key5457", "value15311");

  // op 32176: put
  db->Put(write_options, "key5741", "value102886");

  // op 32177: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1479", "key2614", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1479, key2614) size=" << scan_result.size() << std::endl;
  }

  // op 32178: put
  db->Put(write_options, "key1845", "value70266");

  // op 32179: delete
  db->Delete(write_options, "key9740");

  // op 32180: get
  status = db->Get(read_options, "key7663", &value);
  if (status.ok()) {
    out << "GET key7663 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32181: get
  status = db->Get(read_options, "key3289", &value);
  if (status.ok()) {
    out << "GET key3289 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32182: delete_range
  status = db->DeleteRange(write_options, "key2533", "key11106");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 32183: delete_range
  status = db->DeleteRange(write_options, "key3827", "key5780");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 32184: put
  db->Put(write_options, "key7209", "value49405");

  // op 32185: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5790", "key11348", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5790, key11348) size=" << scan_result.size() << std::endl;
  }

  // op 32186: delete
  db->Delete(write_options, "key4404");

  // op 32187: put
  db->Put(write_options, "key2043", "value13513");

  // op 32188: put
  db->Put(write_options, "key1408", "value92742");

  // op 32189: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3516", "key11054", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3516, key11054) size=" << scan_result.size() << std::endl;
  }

  // op 32190: delete
  db->Delete(write_options, "key0646");

  // op 32191: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 32192: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1080", "key1956", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1080, key1956) size=" << scan_result.size() << std::endl;
  }

  // op 32193: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1953", "key5008", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1953, key5008) size=" << scan_result.size() << std::endl;
  }

  // op 32194: get
  status = db->Get(read_options, "key6982", &value);
  if (status.ok()) {
    out << "GET key6982 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32195: put
  db->Put(write_options, "key9748", "value106059");

  // op 32196: put
  db->Put(write_options, "key10773", "value113058");

  // op 32197: put
  db->Put(write_options, "key9131", "value116277");

  // op 32198: delete_range
  status = db->DeleteRange(write_options, "key0520", "key10454");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 32199: get
  status = db->Get(read_options, "key11650", &value);
  if (status.ok()) {
    out << "GET key11650 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32200: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 32201: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7576", "key8288", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7576, key8288) size=" << scan_result.size() << std::endl;
  }

  // op 32202: delete_range
  status = db->DeleteRange(write_options, "key1184", "key7055");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 32203: delete_range
  status = db->DeleteRange(write_options, "key3419", "key8793");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 32204: get
  status = db->Get(read_options, "key2752", &value);
  if (status.ok()) {
    out << "GET key2752 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32205: get
  status = db->Get(read_options, "key4997", &value);
  if (status.ok()) {
    out << "GET key4997 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32206: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3708", "key9455", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3708, key9455) size=" << scan_result.size() << std::endl;
  }

  // op 32207: delete
  db->Delete(write_options, "key6836");

  // op 32208: delete_range
  status = db->DeleteRange(write_options, "key0147", "key2510");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 32209: put
  db->Put(write_options, "key10240", "value14345");

  // op 32210: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9011", "key11294", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9011, key11294) size=" << scan_result.size() << std::endl;
  }

  // op 32211: get
  status = db->Get(read_options, "key9067", &value);
  if (status.ok()) {
    out << "GET key9067 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32212: put
  db->Put(write_options, "key5057", "value42378");

  // op 32213: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7550", "key7892", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7550, key7892) size=" << scan_result.size() << std::endl;
  }

  // op 32214: delete
  db->Delete(write_options, "key3037");

  // op 32215: put
  db->Put(write_options, "key5941", "value86911");

  // op 32216: get
  status = db->Get(read_options, "key7943", &value);
  if (status.ok()) {
    out << "GET key7943 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32217: put
  db->Put(write_options, "key3053", "value1122");

  // op 32218: put
  db->Put(write_options, "key9357", "value71408");

  // op 32219: get
  status = db->Get(read_options, "key9704", &value);
  if (status.ok()) {
    out << "GET key9704 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32220: put
  db->Put(write_options, "key4260", "value49840");

  // op 32221: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1925", "key11116", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1925, key11116) size=" << scan_result.size() << std::endl;
  }

  // op 32222: put
  db->Put(write_options, "key9367", "value36682");

  // op 32223: delete_range
  status = db->DeleteRange(write_options, "key3916", "key4375");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 32224: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6199", "key8408", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6199, key8408) size=" << scan_result.size() << std::endl;
  }

  // op 32225: put
  db->Put(write_options, "key0007", "value97584");

  // op 32226: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3468", "key9798", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3468, key9798) size=" << scan_result.size() << std::endl;
  }

  // op 32227: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8724", "key10912", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8724, key10912) size=" << scan_result.size() << std::endl;
  }

  // op 32228: put
  db->Put(write_options, "key8527", "value78370");

  // op 32229: delete
  db->Delete(write_options, "key10883");

  // op 32230: put
  db->Put(write_options, "key7140", "value26731");

  // op 32231: put
  db->Put(write_options, "key1222", "value89854");

  // op 32232: put
  db->Put(write_options, "key9757", "value71985");

  // op 32233: put
  db->Put(write_options, "key10286", "value55434");

  // op 32234: get
  status = db->Get(read_options, "key8621", &value);
  if (status.ok()) {
    out << "GET key8621 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32235: get
  status = db->Get(read_options, "key0793", &value);
  if (status.ok()) {
    out << "GET key0793 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32236: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0580", "key7816", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0580, key7816) size=" << scan_result.size() << std::endl;
  }

  // op 32237: put
  db->Put(write_options, "key11457", "value94739");

  // op 32238: put
  db->Put(write_options, "key7618", "value0925");

  // op 32239: get
  status = db->Get(read_options, "key10544", &value);
  if (status.ok()) {
    out << "GET key10544 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32240: put
  db->Put(write_options, "key4330", "value111858");

  // op 32241: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7236", "key7416", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7236, key7416) size=" << scan_result.size() << std::endl;
  }

  // op 32242: get
  status = db->Get(read_options, "key2670", &value);
  if (status.ok()) {
    out << "GET key2670 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32243: get
  status = db->Get(read_options, "key4142", &value);
  if (status.ok()) {
    out << "GET key4142 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32244: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 32245: get
  status = db->Get(read_options, "key9399", &value);
  if (status.ok()) {
    out << "GET key9399 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32246: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 32247: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4946", "key6216", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4946, key6216) size=" << scan_result.size() << std::endl;
  }

  // op 32248: put
  db->Put(write_options, "key4677", "value98736");

  // op 32249: delete
  db->Delete(write_options, "key6296");

  // op 32250: delete
  db->Delete(write_options, "key3743");

  // op 32251: delete_range
  status = db->DeleteRange(write_options, "key2888", "key9587");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 32252: get
  status = db->Get(read_options, "key8169", &value);
  if (status.ok()) {
    out << "GET key8169 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32253: get
  status = db->Get(read_options, "key3448", &value);
  if (status.ok()) {
    out << "GET key3448 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32254: get
  status = db->Get(read_options, "key7140", &value);
  if (status.ok()) {
    out << "GET key7140 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32255: put
  db->Put(write_options, "key10360", "value45581");

  // op 32256: get
  status = db->Get(read_options, "key11469", &value);
  if (status.ok()) {
    out << "GET key11469 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32257: delete
  db->Delete(write_options, "key6428");

  // op 32258: get
  status = db->Get(read_options, "key5153", &value);
  if (status.ok()) {
    out << "GET key5153 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32259: put
  db->Put(write_options, "key7711", "value44861");

  // op 32260: get
  status = db->Get(read_options, "key6474", &value);
  if (status.ok()) {
    out << "GET key6474 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32261: delete
  db->Delete(write_options, "key7643");

  // op 32262: put
  db->Put(write_options, "key1888", "value31293");

  // op 32263: put
  db->Put(write_options, "key11287", "value25741");

  // op 32264: put
  db->Put(write_options, "key3712", "value33680");

  // op 32265: delete
  db->Delete(write_options, "key8807");

  // op 32266: put
  db->Put(write_options, "key0302", "value61294");

  // op 32267: delete_range
  status = db->DeleteRange(write_options, "key3820", "key7005");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 32268: put
  db->Put(write_options, "key4286", "value21727");

  // op 32269: get
  status = db->Get(read_options, "key1528", &value);
  if (status.ok()) {
    out << "GET key1528 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32270: delete
  db->Delete(write_options, "key8906");

  // op 32271: put
  db->Put(write_options, "key2791", "value111102");

  // op 32272: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8900", "key9705", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8900, key9705) size=" << scan_result.size() << std::endl;
  }

  // op 32273: get
  status = db->Get(read_options, "key1177", &value);
  if (status.ok()) {
    out << "GET key1177 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32274: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1273", "key6412", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1273, key6412) size=" << scan_result.size() << std::endl;
  }

  // op 32275: delete_range
  status = db->DeleteRange(write_options, "key1521", "key10108");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 32276: get
  status = db->Get(read_options, "key1035", &value);
  if (status.ok()) {
    out << "GET key1035 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32277: put
  db->Put(write_options, "key3642", "value69022");

  // op 32278: delete_range
  status = db->DeleteRange(write_options, "key7171", "key11468");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 32279: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 32280: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 32281: put
  db->Put(write_options, "key10212", "value69235");

  // op 32282: delete
  db->Delete(write_options, "key8305");

  // op 32283: get
  status = db->Get(read_options, "key10770", &value);
  if (status.ok()) {
    out << "GET key10770 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32284: put
  db->Put(write_options, "key0446", "value84805");

  // op 32285: put
  db->Put(write_options, "key11611", "value89233");

  // op 32286: put
  db->Put(write_options, "key2563", "value45902");

  // op 32287: get
  status = db->Get(read_options, "key4341", &value);
  if (status.ok()) {
    out << "GET key4341 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32288: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 32289: put
  db->Put(write_options, "key8689", "value12060");

  // op 32290: put
  db->Put(write_options, "key7136", "value100598");

  // op 32291: put
  db->Put(write_options, "key0561", "value107517");

  // op 32292: get
  status = db->Get(read_options, "key9376", &value);
  if (status.ok()) {
    out << "GET key9376 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32293: put
  db->Put(write_options, "key9778", "value19032");

  // op 32294: put
  db->Put(write_options, "key1350", "value116816");

  // op 32295: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8898", "key9163", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8898, key9163) size=" << scan_result.size() << std::endl;
  }

  // op 32296: delete
  db->Delete(write_options, "key5405");

  // op 32297: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5711", "key7288", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5711, key7288) size=" << scan_result.size() << std::endl;
  }

  // op 32298: put
  db->Put(write_options, "key3346", "value115695");

  // op 32299: put
  db->Put(write_options, "key8808", "value8394");

  // op 32300: put
  db->Put(write_options, "key5770", "value17794");

  // op 32301: put
  db->Put(write_options, "key8519", "value60836");

  // op 32302: get
  status = db->Get(read_options, "key5651", &value);
  if (status.ok()) {
    out << "GET key5651 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32303: get
  status = db->Get(read_options, "key6059", &value);
  if (status.ok()) {
    out << "GET key6059 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32304: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4173", "key8840", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4173, key8840) size=" << scan_result.size() << std::endl;
  }

  // op 32305: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7888", "key7929", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7888, key7929) size=" << scan_result.size() << std::endl;
  }

  // op 32306: delete
  db->Delete(write_options, "key4069");

  // op 32307: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2930", "key8798", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2930, key8798) size=" << scan_result.size() << std::endl;
  }

  // op 32308: delete
  db->Delete(write_options, "key7450");

  // op 32309: delete
  db->Delete(write_options, "key5092");

  // op 32310: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0036", "key9183", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0036, key9183) size=" << scan_result.size() << std::endl;
  }

  // op 32311: put
  db->Put(write_options, "key9935", "value108141");

  // op 32312: put
  db->Put(write_options, "key9333", "value13267");

  // op 32313: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3655", "key10712", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3655, key10712) size=" << scan_result.size() << std::endl;
  }

  // op 32314: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3928", "key11451", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3928, key11451) size=" << scan_result.size() << std::endl;
  }

  // op 32315: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 32316: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0918", "key11251", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0918, key11251) size=" << scan_result.size() << std::endl;
  }

  // op 32317: delete_range
  status = db->DeleteRange(write_options, "key4217", "key9394");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 32318: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6035", "key10588", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6035, key10588) size=" << scan_result.size() << std::endl;
  }

  // op 32319: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1310", "key2420", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1310, key2420) size=" << scan_result.size() << std::endl;
  }

  // op 32320: put
  db->Put(write_options, "key0971", "value110230");

  // op 32321: put
  db->Put(write_options, "key5145", "value22061");

  // op 32322: put
  db->Put(write_options, "key7389", "value85544");

  // op 32323: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3930", "key10331", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3930, key10331) size=" << scan_result.size() << std::endl;
  }

  // op 32324: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2534", "key11365", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2534, key11365) size=" << scan_result.size() << std::endl;
  }

  // op 32325: get
  status = db->Get(read_options, "key3626", &value);
  if (status.ok()) {
    out << "GET key3626 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32326: put
  db->Put(write_options, "key4226", "value60083");

  // op 32327: delete_range
  status = db->DeleteRange(write_options, "key9906", "key11594");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 32328: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0989", "key8169", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0989, key8169) size=" << scan_result.size() << std::endl;
  }

  // op 32329: delete
  db->Delete(write_options, "key9838");

  // op 32330: put
  db->Put(write_options, "key3126", "value98524");

  // op 32331: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0985", "key7961", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0985, key7961) size=" << scan_result.size() << std::endl;
  }

  // op 32332: put
  db->Put(write_options, "key10845", "value3512");

  // op 32333: put
  db->Put(write_options, "key5252", "value99389");

  // op 32334: put
  db->Put(write_options, "key5404", "value29291");

  // op 32335: put
  db->Put(write_options, "key9715", "value7418");

  // op 32336: put
  db->Put(write_options, "key10680", "value109563");

  // op 32337: put
  db->Put(write_options, "key2595", "value25726");

  // op 32338: delete
  db->Delete(write_options, "key6912");

  // op 32339: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 32340: delete
  db->Delete(write_options, "key0116");

  // op 32341: put
  db->Put(write_options, "key9282", "value70561");

  // op 32342: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 32343: put
  db->Put(write_options, "key3277", "value54752");

  // op 32344: delete_range
  status = db->DeleteRange(write_options, "key4021", "key4874");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 32345: delete
  db->Delete(write_options, "key10272");

  // op 32346: put
  db->Put(write_options, "key4193", "value23911");

  // op 32347: put
  db->Put(write_options, "key2172", "value17299");

  // op 32348: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 32349: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8082", "key9756", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8082, key9756) size=" << scan_result.size() << std::endl;
  }

  // op 32350: get
  status = db->Get(read_options, "key1569", &value);
  if (status.ok()) {
    out << "GET key1569 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32351: put
  db->Put(write_options, "key2458", "value42745");

  // op 32352: delete
  db->Delete(write_options, "key3445");

  // op 32353: get
  status = db->Get(read_options, "key11136", &value);
  if (status.ok()) {
    out << "GET key11136 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32354: put
  db->Put(write_options, "key0525", "value111064");

  // op 32355: delete_range
  status = db->DeleteRange(write_options, "key2164", "key8744");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 32356: put
  db->Put(write_options, "key6458", "value112815");

  // op 32357: put
  db->Put(write_options, "key8380", "value79311");

  // op 32358: put
  db->Put(write_options, "key0552", "value83408");

  // op 32359: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2994", "key9540", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2994, key9540) size=" << scan_result.size() << std::endl;
  }

  // op 32360: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0772", "key0896", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0772, key0896) size=" << scan_result.size() << std::endl;
  }

  // op 32361: put
  db->Put(write_options, "key5570", "value17606");

  // op 32362: put
  db->Put(write_options, "key5464", "value31270");

  // op 32363: delete_range
  status = db->DeleteRange(write_options, "key1691", "key3340");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 32364: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 32365: put
  db->Put(write_options, "key5890", "value87627");

  // op 32366: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3800", "key10771", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3800, key10771) size=" << scan_result.size() << std::endl;
  }

  // op 32367: put
  db->Put(write_options, "key2242", "value12767");

  // op 32368: put
  db->Put(write_options, "key6833", "value41046");

  // op 32369: put
  db->Put(write_options, "key7225", "value90442");

  // op 32370: get
  status = db->Get(read_options, "key7396", &value);
  if (status.ok()) {
    out << "GET key7396 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32371: delete
  db->Delete(write_options, "key8028");

  // op 32372: get
  status = db->Get(read_options, "key2685", &value);
  if (status.ok()) {
    out << "GET key2685 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32373: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0938", "key10910", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0938, key10910) size=" << scan_result.size() << std::endl;
  }

  // op 32374: get
  status = db->Get(read_options, "key2145", &value);
  if (status.ok()) {
    out << "GET key2145 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32375: delete
  db->Delete(write_options, "key2175");

  // op 32376: put
  db->Put(write_options, "key2208", "value40287");

  // op 32377: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7316", "key8670", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7316, key8670) size=" << scan_result.size() << std::endl;
  }

  // op 32378: put
  db->Put(write_options, "key10687", "value58924");

  // op 32379: put
  db->Put(write_options, "key2671", "value3151");

  // op 32380: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3380", "key3557", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3380, key3557) size=" << scan_result.size() << std::endl;
  }

  // op 32381: put
  db->Put(write_options, "key9870", "value37926");

  // op 32382: get
  status = db->Get(read_options, "key10261", &value);
  if (status.ok()) {
    out << "GET key10261 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32383: put
  db->Put(write_options, "key3820", "value76304");

  // op 32384: delete_range
  status = db->DeleteRange(write_options, "key8538", "key10105");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 32385: get
  status = db->Get(read_options, "key2174", &value);
  if (status.ok()) {
    out << "GET key2174 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32386: delete
  db->Delete(write_options, "key4063");

  // op 32387: get
  status = db->Get(read_options, "key2811", &value);
  if (status.ok()) {
    out << "GET key2811 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32388: put
  db->Put(write_options, "key11502", "value8298");

  // op 32389: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 32390: get
  status = db->Get(read_options, "key7355", &value);
  if (status.ok()) {
    out << "GET key7355 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32391: delete
  db->Delete(write_options, "key5549");

  // op 32392: put
  db->Put(write_options, "key6003", "value93382");

  // op 32393: put
  db->Put(write_options, "key6077", "value15179");

  // op 32394: get
  status = db->Get(read_options, "key7762", &value);
  if (status.ok()) {
    out << "GET key7762 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32395: put
  db->Put(write_options, "key7313", "value116353");

  // op 32396: delete
  db->Delete(write_options, "key8411");

  // op 32397: get
  status = db->Get(read_options, "key4367", &value);
  if (status.ok()) {
    out << "GET key4367 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32398: put
  db->Put(write_options, "key2859", "value67476");

  // op 32399: delete_range
  status = db->DeleteRange(write_options, "key3215", "key4606");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 32400: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2125", "key4245", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2125, key4245) size=" << scan_result.size() << std::endl;
  }

  // op 32401: delete
  db->Delete(write_options, "key6349");

  // op 32402: delete_range
  status = db->DeleteRange(write_options, "key5728", "key7812");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 32403: delete_range
  status = db->DeleteRange(write_options, "key1510", "key1842");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 32404: get
  status = db->Get(read_options, "key11335", &value);
  if (status.ok()) {
    out << "GET key11335 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32405: put
  db->Put(write_options, "key10630", "value84688");

  // op 32406: get
  status = db->Get(read_options, "key1727", &value);
  if (status.ok()) {
    out << "GET key1727 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32407: put
  db->Put(write_options, "key7525", "value105277");

  // op 32408: put
  db->Put(write_options, "key1464", "value48578");

  // op 32409: put
  db->Put(write_options, "key9289", "value102961");

  // op 32410: put
  db->Put(write_options, "key2704", "value15406");

  // op 32411: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3460", "key7224", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3460, key7224) size=" << scan_result.size() << std::endl;
  }

  // op 32412: get
  status = db->Get(read_options, "key8940", &value);
  if (status.ok()) {
    out << "GET key8940 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32413: delete_range
  status = db->DeleteRange(write_options, "key2052", "key10306");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 32414: put
  db->Put(write_options, "key11663", "value59029");

  // op 32415: put
  db->Put(write_options, "key10315", "value84769");

  // op 32416: get
  status = db->Get(read_options, "key11484", &value);
  if (status.ok()) {
    out << "GET key11484 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32417: put
  db->Put(write_options, "key4992", "value76288");

  // op 32418: get
  status = db->Get(read_options, "key10010", &value);
  if (status.ok()) {
    out << "GET key10010 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32419: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1455", "key3432", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1455, key3432) size=" << scan_result.size() << std::endl;
  }

  // op 32420: get
  status = db->Get(read_options, "key6389", &value);
  if (status.ok()) {
    out << "GET key6389 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32421: delete_range
  status = db->DeleteRange(write_options, "key3623", "key5595");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 32422: get
  status = db->Get(read_options, "key4525", &value);
  if (status.ok()) {
    out << "GET key4525 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32423: put
  db->Put(write_options, "key0139", "value11201");

  // op 32424: put
  db->Put(write_options, "key6374", "value35452");

  // op 32425: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5037", "key5574", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5037, key5574) size=" << scan_result.size() << std::endl;
  }

  // op 32426: get
  status = db->Get(read_options, "key9288", &value);
  if (status.ok()) {
    out << "GET key9288 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32427: get
  status = db->Get(read_options, "key3488", &value);
  if (status.ok()) {
    out << "GET key3488 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32428: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9413", "key11698", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9413, key11698) size=" << scan_result.size() << std::endl;
  }

  // op 32429: put
  db->Put(write_options, "key0077", "value57925");

  // op 32430: put
  db->Put(write_options, "key1064", "value44411");

  // op 32431: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 32432: put
  db->Put(write_options, "key8195", "value73231");

  // op 32433: get
  status = db->Get(read_options, "key4568", &value);
  if (status.ok()) {
    out << "GET key4568 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32434: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1055", "key11446", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1055, key11446) size=" << scan_result.size() << std::endl;
  }

  // op 32435: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 32436: put
  db->Put(write_options, "key6273", "value107378");

  // op 32437: put
  db->Put(write_options, "key3529", "value13945");

  // op 32438: get
  status = db->Get(read_options, "key0520", &value);
  if (status.ok()) {
    out << "GET key0520 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32439: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7638", "key9795", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7638, key9795) size=" << scan_result.size() << std::endl;
  }

  // op 32440: delete_range
  status = db->DeleteRange(write_options, "key0205", "key0375");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 32441: delete_range
  status = db->DeleteRange(write_options, "key5040", "key10704");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 32442: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4221", "key7607", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4221, key7607) size=" << scan_result.size() << std::endl;
  }

  // op 32443: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4322", "key7955", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4322, key7955) size=" << scan_result.size() << std::endl;
  }

  // op 32444: put
  db->Put(write_options, "key9053", "value70326");

  // op 32445: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5895", "key9914", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5895, key9914) size=" << scan_result.size() << std::endl;
  }

  // op 32446: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3235", "key11000", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3235, key11000) size=" << scan_result.size() << std::endl;
  }

  // op 32447: get
  status = db->Get(read_options, "key10751", &value);
  if (status.ok()) {
    out << "GET key10751 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32448: put
  db->Put(write_options, "key4327", "value35829");

  // op 32449: put
  db->Put(write_options, "key11347", "value2300");

  // op 32450: put
  db->Put(write_options, "key0328", "value48905");

  // op 32451: get
  status = db->Get(read_options, "key1843", &value);
  if (status.ok()) {
    out << "GET key1843 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32452: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4277", "key9651", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4277, key9651) size=" << scan_result.size() << std::endl;
  }

  // op 32453: put
  db->Put(write_options, "key1679", "value90939");

  // op 32454: get
  status = db->Get(read_options, "key7366", &value);
  if (status.ok()) {
    out << "GET key7366 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32455: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 32456: put
  db->Put(write_options, "key4876", "value71354");

  // op 32457: put
  db->Put(write_options, "key2875", "value98072");

  // op 32458: get
  status = db->Get(read_options, "key2504", &value);
  if (status.ok()) {
    out << "GET key2504 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32459: put
  db->Put(write_options, "key7869", "value97918");

  // op 32460: delete
  db->Delete(write_options, "key5135");

  // op 32461: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 32462: put
  db->Put(write_options, "key6750", "value94568");

  // op 32463: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7759", "key9145", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7759, key9145) size=" << scan_result.size() << std::endl;
  }

  // op 32464: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4473", "key10106", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4473, key10106) size=" << scan_result.size() << std::endl;
  }

  // op 32465: delete_range
  status = db->DeleteRange(write_options, "key10817", "key11428");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 32466: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 32467: get
  status = db->Get(read_options, "key9981", &value);
  if (status.ok()) {
    out << "GET key9981 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32468: delete_range
  status = db->DeleteRange(write_options, "key2849", "key7750");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 32469: put
  db->Put(write_options, "key6197", "value95674");

  // op 32470: get
  status = db->Get(read_options, "key6917", &value);
  if (status.ok()) {
    out << "GET key6917 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32471: delete
  db->Delete(write_options, "key8299");

  // op 32472: get
  status = db->Get(read_options, "key2437", &value);
  if (status.ok()) {
    out << "GET key2437 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32473: delete
  db->Delete(write_options, "key1916");

  // op 32474: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 32475: put
  db->Put(write_options, "key9127", "value90134");

  // op 32476: get
  status = db->Get(read_options, "key11067", &value);
  if (status.ok()) {
    out << "GET key11067 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32477: put
  db->Put(write_options, "key6822", "value33805");

  // op 32478: get
  status = db->Get(read_options, "key8323", &value);
  if (status.ok()) {
    out << "GET key8323 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32479: get
  status = db->Get(read_options, "key1506", &value);
  if (status.ok()) {
    out << "GET key1506 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32480: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9718", "key11661", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9718, key11661) size=" << scan_result.size() << std::endl;
  }

  // op 32481: get
  status = db->Get(read_options, "key2787", &value);
  if (status.ok()) {
    out << "GET key2787 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32482: get
  status = db->Get(read_options, "key6928", &value);
  if (status.ok()) {
    out << "GET key6928 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32483: delete
  db->Delete(write_options, "key10226");

  // op 32484: get
  status = db->Get(read_options, "key1927", &value);
  if (status.ok()) {
    out << "GET key1927 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32485: put
  db->Put(write_options, "key10540", "value53196");

  // op 32486: put
  db->Put(write_options, "key5899", "value45271");

  // op 32487: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5366", "key7141", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5366, key7141) size=" << scan_result.size() << std::endl;
  }

  // op 32488: put
  db->Put(write_options, "key2992", "value112877");

  // op 32489: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 32490: get
  status = db->Get(read_options, "key9883", &value);
  if (status.ok()) {
    out << "GET key9883 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32491: put
  db->Put(write_options, "key6865", "value114747");

  // op 32492: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 32493: put
  db->Put(write_options, "key9797", "value35920");

  // op 32494: put
  db->Put(write_options, "key8743", "value96885");

  // op 32495: put
  db->Put(write_options, "key4758", "value81173");

  // op 32496: put
  db->Put(write_options, "key1181", "value90062");

  // op 32497: put
  db->Put(write_options, "key6920", "value25061");

  // op 32498: delete
  db->Delete(write_options, "key10768");

  // op 32499: put
  db->Put(write_options, "key6304", "value46482");

  // op 32500: get
  status = db->Get(read_options, "key2654", &value);
  if (status.ok()) {
    out << "GET key2654 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32501: delete
  db->Delete(write_options, "key10257");

  // op 32502: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3524", "key4850", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3524, key4850) size=" << scan_result.size() << std::endl;
  }

  // op 32503: delete_range
  status = db->DeleteRange(write_options, "key6957", "key11683");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 32504: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0107", "key6861", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0107, key6861) size=" << scan_result.size() << std::endl;
  }

  // op 32505: get
  status = db->Get(read_options, "key9892", &value);
  if (status.ok()) {
    out << "GET key9892 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32506: delete_range
  status = db->DeleteRange(write_options, "key1297", "key3819");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 32507: put
  db->Put(write_options, "key3253", "value51739");

  // op 32508: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10744", "key10900", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10744, key10900) size=" << scan_result.size() << std::endl;
  }

  // op 32509: get
  status = db->Get(read_options, "key6361", &value);
  if (status.ok()) {
    out << "GET key6361 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32510: put
  db->Put(write_options, "key4979", "value113060");

  // op 32511: put
  db->Put(write_options, "key9345", "value51858");

  // op 32512: put
  db->Put(write_options, "key5040", "value91581");

  // op 32513: put
  db->Put(write_options, "key1997", "value35927");

  // op 32514: delete
  db->Delete(write_options, "key7889");

  // op 32515: put
  db->Put(write_options, "key6841", "value69861");

  // op 32516: put
  db->Put(write_options, "key8334", "value116479");

  // op 32517: put
  db->Put(write_options, "key6451", "value38067");

  // op 32518: put
  db->Put(write_options, "key1063", "value91953");

  // op 32519: put
  db->Put(write_options, "key7782", "value75520");

  // op 32520: delete_range
  status = db->DeleteRange(write_options, "key1143", "key6264");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 32521: get
  status = db->Get(read_options, "key0874", &value);
  if (status.ok()) {
    out << "GET key0874 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32522: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2435", "key3150", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2435, key3150) size=" << scan_result.size() << std::endl;
  }

  // op 32523: put
  db->Put(write_options, "key5514", "value104751");

  // op 32524: get
  status = db->Get(read_options, "key3821", &value);
  if (status.ok()) {
    out << "GET key3821 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32525: put
  db->Put(write_options, "key1453", "value52947");

  // op 32526: get
  status = db->Get(read_options, "key1364", &value);
  if (status.ok()) {
    out << "GET key1364 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32527: get
  status = db->Get(read_options, "key10117", &value);
  if (status.ok()) {
    out << "GET key10117 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32528: delete
  db->Delete(write_options, "key6745");

  // op 32529: put
  db->Put(write_options, "key9964", "value5049");

  // op 32530: delete_range
  status = db->DeleteRange(write_options, "key7057", "key9730");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 32531: get
  status = db->Get(read_options, "key2022", &value);
  if (status.ok()) {
    out << "GET key2022 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32532: delete
  db->Delete(write_options, "key1451");

  // op 32533: put
  db->Put(write_options, "key2378", "value63510");

  // op 32534: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 32535: put
  db->Put(write_options, "key3635", "value15711");

  // op 32536: delete_range
  status = db->DeleteRange(write_options, "key1108", "key4573");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 32537: get
  status = db->Get(read_options, "key9519", &value);
  if (status.ok()) {
    out << "GET key9519 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32538: get
  status = db->Get(read_options, "key7261", &value);
  if (status.ok()) {
    out << "GET key7261 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32539: put
  db->Put(write_options, "key11249", "value80820");

  // op 32540: put
  db->Put(write_options, "key3715", "value111379");

  // op 32541: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6085", "key6844", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6085, key6844) size=" << scan_result.size() << std::endl;
  }

  // op 32542: put
  db->Put(write_options, "key8135", "value86395");

  // op 32543: delete
  db->Delete(write_options, "key4778");

  // op 32544: put
  db->Put(write_options, "key2900", "value55453");

  // op 32545: get
  status = db->Get(read_options, "key0969", &value);
  if (status.ok()) {
    out << "GET key0969 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32546: put
  db->Put(write_options, "key9837", "value59120");

  // op 32547: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3405", "key8454", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3405, key8454) size=" << scan_result.size() << std::endl;
  }

  // op 32548: get
  status = db->Get(read_options, "key2851", &value);
  if (status.ok()) {
    out << "GET key2851 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32549: delete_range
  status = db->DeleteRange(write_options, "key7889", "key9379");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 32550: put
  db->Put(write_options, "key0142", "value90866");

  // op 32551: put
  db->Put(write_options, "key8877", "value47171");

  // op 32552: put
  db->Put(write_options, "key2378", "value108138");

  // op 32553: put
  db->Put(write_options, "key6959", "value31958");

  // op 32554: get
  status = db->Get(read_options, "key11182", &value);
  if (status.ok()) {
    out << "GET key11182 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32555: put
  db->Put(write_options, "key1297", "value60180");

  // op 32556: get
  status = db->Get(read_options, "key2542", &value);
  if (status.ok()) {
    out << "GET key2542 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32557: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0378", "key6114", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0378, key6114) size=" << scan_result.size() << std::endl;
  }

  // op 32558: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 32559: get
  status = db->Get(read_options, "key8750", &value);
  if (status.ok()) {
    out << "GET key8750 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32560: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7172", "key8171", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7172, key8171) size=" << scan_result.size() << std::endl;
  }

  // op 32561: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5179", "key6143", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5179, key6143) size=" << scan_result.size() << std::endl;
  }

  // op 32562: delete_range
  status = db->DeleteRange(write_options, "key0625", "key4662");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 32563: put
  db->Put(write_options, "key8020", "value92397");

  // op 32564: get
  status = db->Get(read_options, "key2327", &value);
  if (status.ok()) {
    out << "GET key2327 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32565: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7570", "key11429", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7570, key11429) size=" << scan_result.size() << std::endl;
  }

  // op 32566: put
  db->Put(write_options, "key6628", "value40267");

  // op 32567: get
  status = db->Get(read_options, "key2323", &value);
  if (status.ok()) {
    out << "GET key2323 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32568: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2603", "key6280", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2603, key6280) size=" << scan_result.size() << std::endl;
  }

  // op 32569: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 32570: put
  db->Put(write_options, "key7837", "value49588");

  // op 32571: delete_range
  status = db->DeleteRange(write_options, "key2341", "key3243");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 32572: delete
  db->Delete(write_options, "key8712");

  // op 32573: get
  status = db->Get(read_options, "key0141", &value);
  if (status.ok()) {
    out << "GET key0141 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32574: delete_range
  status = db->DeleteRange(write_options, "key5135", "key11082");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 32575: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1092", "key3507", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1092, key3507) size=" << scan_result.size() << std::endl;
  }

  // op 32576: put
  db->Put(write_options, "key3656", "value102319");

  // op 32577: put
  db->Put(write_options, "key2297", "value90073");

  // op 32578: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1607", "key6715", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1607, key6715) size=" << scan_result.size() << std::endl;
  }

  // op 32579: get
  status = db->Get(read_options, "key3419", &value);
  if (status.ok()) {
    out << "GET key3419 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32580: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 32581: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 32582: put
  db->Put(write_options, "key8378", "value4422");

  // op 32583: put
  db->Put(write_options, "key9248", "value26245");

  // op 32584: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5526", "key8225", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5526, key8225) size=" << scan_result.size() << std::endl;
  }

  // op 32585: get
  status = db->Get(read_options, "key8351", &value);
  if (status.ok()) {
    out << "GET key8351 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32586: put
  db->Put(write_options, "key2855", "value80037");

  // op 32587: get
  status = db->Get(read_options, "key5941", &value);
  if (status.ok()) {
    out << "GET key5941 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32588: delete
  db->Delete(write_options, "key7043");

  // op 32589: delete_range
  status = db->DeleteRange(write_options, "key2929", "key3732");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 32590: get
  status = db->Get(read_options, "key5772", &value);
  if (status.ok()) {
    out << "GET key5772 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32591: put
  db->Put(write_options, "key7549", "value104568");

  // op 32592: get
  status = db->Get(read_options, "key2426", &value);
  if (status.ok()) {
    out << "GET key2426 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32593: get
  status = db->Get(read_options, "key10148", &value);
  if (status.ok()) {
    out << "GET key10148 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32594: get
  status = db->Get(read_options, "key6827", &value);
  if (status.ok()) {
    out << "GET key6827 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32595: delete
  db->Delete(write_options, "key4751");

  // op 32596: delete_range
  status = db->DeleteRange(write_options, "key3122", "key10993");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 32597: put
  db->Put(write_options, "key1395", "value71577");

  // op 32598: put
  db->Put(write_options, "key4148", "value75120");

  // op 32599: get
  status = db->Get(read_options, "key3148", &value);
  if (status.ok()) {
    out << "GET key3148 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32600: delete
  db->Delete(write_options, "key3563");

  // op 32601: put
  db->Put(write_options, "key7300", "value14182");

  // op 32602: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1329", "key3156", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1329, key3156) size=" << scan_result.size() << std::endl;
  }

  // op 32603: put
  db->Put(write_options, "key2432", "value50883");

  // op 32604: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 32605: put
  db->Put(write_options, "key6241", "value6577");

  // op 32606: get
  status = db->Get(read_options, "key3940", &value);
  if (status.ok()) {
    out << "GET key3940 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32607: delete
  db->Delete(write_options, "key0011");

  // op 32608: get
  status = db->Get(read_options, "key9378", &value);
  if (status.ok()) {
    out << "GET key9378 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32609: put
  db->Put(write_options, "key0896", "value98288");

  // op 32610: delete
  db->Delete(write_options, "key4654");

  // op 32611: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7014", "key11649", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7014, key11649) size=" << scan_result.size() << std::endl;
  }

  // op 32612: put
  db->Put(write_options, "key9736", "value54949");

  // op 32613: delete_range
  status = db->DeleteRange(write_options, "key1190", "key10847");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 32614: put
  db->Put(write_options, "key0929", "value97552");

  // op 32615: put
  db->Put(write_options, "key6102", "value72520");

  // op 32616: get
  status = db->Get(read_options, "key5429", &value);
  if (status.ok()) {
    out << "GET key5429 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32617: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 32618: put
  db->Put(write_options, "key7092", "value46152");

  // op 32619: get
  status = db->Get(read_options, "key3185", &value);
  if (status.ok()) {
    out << "GET key3185 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32620: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2792", "key9474", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2792, key9474) size=" << scan_result.size() << std::endl;
  }

  // op 32621: delete
  db->Delete(write_options, "key4622");

  // op 32622: put
  db->Put(write_options, "key0230", "value73957");

  // op 32623: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 32624: put
  db->Put(write_options, "key5478", "value3326");

  // op 32625: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6791", "key11578", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6791, key11578) size=" << scan_result.size() << std::endl;
  }

  // op 32626: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 32627: put
  db->Put(write_options, "key4350", "value63371");

  // op 32628: get
  status = db->Get(read_options, "key3179", &value);
  if (status.ok()) {
    out << "GET key3179 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32629: put
  db->Put(write_options, "key0844", "value44486");

  // op 32630: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3474", "key6932", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3474, key6932) size=" << scan_result.size() << std::endl;
  }

  // op 32631: put
  db->Put(write_options, "key9624", "value65042");

  // op 32632: delete
  db->Delete(write_options, "key10618");

  // op 32633: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 32634: put
  db->Put(write_options, "key0798", "value23573");

  // op 32635: get
  status = db->Get(read_options, "key5909", &value);
  if (status.ok()) {
    out << "GET key5909 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32636: put
  db->Put(write_options, "key11033", "value44013");

  // op 32637: put
  db->Put(write_options, "key9025", "value49450");

  // op 32638: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 32639: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 32640: delete
  db->Delete(write_options, "key2719");

  // op 32641: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8144", "key9947", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8144, key9947) size=" << scan_result.size() << std::endl;
  }

  // op 32642: put
  db->Put(write_options, "key3105", "value5452");

  // op 32643: delete_range
  status = db->DeleteRange(write_options, "key1628", "key9570");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 32644: get
  status = db->Get(read_options, "key11101", &value);
  if (status.ok()) {
    out << "GET key11101 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32645: delete
  db->Delete(write_options, "key1658");

  // op 32646: put
  db->Put(write_options, "key3080", "value56847");

  // op 32647: put
  db->Put(write_options, "key2081", "value88698");

  // op 32648: delete
  db->Delete(write_options, "key6320");

  // op 32649: put
  db->Put(write_options, "key8428", "value56166");

  // op 32650: delete_range
  status = db->DeleteRange(write_options, "key3052", "key11405");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 32651: put
  db->Put(write_options, "key6076", "value68641");

  // op 32652: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4180", "key11728", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4180, key11728) size=" << scan_result.size() << std::endl;
  }

  // op 32653: get
  status = db->Get(read_options, "key8860", &value);
  if (status.ok()) {
    out << "GET key8860 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32654: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 32655: put
  db->Put(write_options, "key7491", "value57549");

  // op 32656: get
  status = db->Get(read_options, "key3045", &value);
  if (status.ok()) {
    out << "GET key3045 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32657: get
  status = db->Get(read_options, "key0690", &value);
  if (status.ok()) {
    out << "GET key0690 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32658: delete
  db->Delete(write_options, "key5576");

  // op 32659: get
  status = db->Get(read_options, "key2826", &value);
  if (status.ok()) {
    out << "GET key2826 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32660: put
  db->Put(write_options, "key2723", "value67690");

  // op 32661: get
  status = db->Get(read_options, "key10554", &value);
  if (status.ok()) {
    out << "GET key10554 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32662: put
  db->Put(write_options, "key9210", "value61988");

  // op 32663: put
  db->Put(write_options, "key10612", "value62683");

  // op 32664: put
  db->Put(write_options, "key1657", "value40121");

  // op 32665: put
  db->Put(write_options, "key5063", "value98709");

  // op 32666: put
  db->Put(write_options, "key4474", "value18410");

  // op 32667: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 32668: get
  status = db->Get(read_options, "key4632", &value);
  if (status.ok()) {
    out << "GET key4632 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32669: delete_range
  status = db->DeleteRange(write_options, "key8407", "key8805");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 32670: put
  db->Put(write_options, "key0987", "value38122");

  // op 32671: delete
  db->Delete(write_options, "key0732");

  // op 32672: put
  db->Put(write_options, "key10942", "value102201");

  // op 32673: delete
  db->Delete(write_options, "key6186");

  // op 32674: get
  status = db->Get(read_options, "key7198", &value);
  if (status.ok()) {
    out << "GET key7198 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32675: delete
  db->Delete(write_options, "key1248");

  // op 32676: delete_range
  status = db->DeleteRange(write_options, "key3858", "key10611");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 32677: delete_range
  status = db->DeleteRange(write_options, "key1859", "key7544");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 32678: delete
  db->Delete(write_options, "key0056");

  // op 32679: get
  status = db->Get(read_options, "key6294", &value);
  if (status.ok()) {
    out << "GET key6294 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32680: put
  db->Put(write_options, "key8140", "value42397");

  // op 32681: put
  db->Put(write_options, "key10627", "value14242");

  // op 32682: get
  status = db->Get(read_options, "key6484", &value);
  if (status.ok()) {
    out << "GET key6484 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32683: delete_range
  status = db->DeleteRange(write_options, "key4152", "key6650");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 32684: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0231", "key3762", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0231, key3762) size=" << scan_result.size() << std::endl;
  }

  // op 32685: delete
  db->Delete(write_options, "key4623");

  // op 32686: get
  status = db->Get(read_options, "key1305", &value);
  if (status.ok()) {
    out << "GET key1305 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32687: put
  db->Put(write_options, "key9004", "value49589");

  // op 32688: put
  db->Put(write_options, "key8061", "value4132");

  // op 32689: delete_range
  status = db->DeleteRange(write_options, "key1264", "key7512");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 32690: put
  db->Put(write_options, "key8113", "value62268");

  // op 32691: put
  db->Put(write_options, "key8979", "value55878");

  // op 32692: put
  db->Put(write_options, "key11297", "value85968");

  // op 32693: put
  db->Put(write_options, "key9413", "value39992");

  // op 32694: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6796", "key9372", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6796, key9372) size=" << scan_result.size() << std::endl;
  }

  // op 32695: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0308", "key10813", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0308, key10813) size=" << scan_result.size() << std::endl;
  }

  // op 32696: put
  db->Put(write_options, "key4440", "value71152");

  // op 32697: get
  status = db->Get(read_options, "key4946", &value);
  if (status.ok()) {
    out << "GET key4946 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32698: put
  db->Put(write_options, "key5965", "value12631");

  // op 32699: put
  db->Put(write_options, "key11121", "value90755");

  // op 32700: put
  db->Put(write_options, "key4184", "value89976");

  // op 32701: delete
  db->Delete(write_options, "key8832");

  // op 32702: put
  db->Put(write_options, "key1791", "value72452");

  // op 32703: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6665", "key11377", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6665, key11377) size=" << scan_result.size() << std::endl;
  }

  // op 32704: get
  status = db->Get(read_options, "key0622", &value);
  if (status.ok()) {
    out << "GET key0622 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32705: put
  db->Put(write_options, "key0128", "value112022");

  // op 32706: put
  db->Put(write_options, "key2591", "value44340");

  // op 32707: delete
  db->Delete(write_options, "key5771");

  // op 32708: get
  status = db->Get(read_options, "key10391", &value);
  if (status.ok()) {
    out << "GET key10391 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32709: delete
  db->Delete(write_options, "key10544");

  // op 32710: put
  db->Put(write_options, "key1809", "value78531");

  // op 32711: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0052", "key2838", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0052, key2838) size=" << scan_result.size() << std::endl;
  }

  // op 32712: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7821", "key9511", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7821, key9511) size=" << scan_result.size() << std::endl;
  }

  // op 32713: get
  status = db->Get(read_options, "key9342", &value);
  if (status.ok()) {
    out << "GET key9342 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32714: delete
  db->Delete(write_options, "key7677");

  // op 32715: put
  db->Put(write_options, "key9481", "value26452");

  // op 32716: put
  db->Put(write_options, "key6391", "value62109");

  // op 32717: put
  db->Put(write_options, "key9534", "value84659");

  // op 32718: get
  status = db->Get(read_options, "key6637", &value);
  if (status.ok()) {
    out << "GET key6637 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32719: delete_range
  status = db->DeleteRange(write_options, "key2214", "key6381");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 32720: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7534", "key11114", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7534, key11114) size=" << scan_result.size() << std::endl;
  }

  // op 32721: get
  status = db->Get(read_options, "key1324", &value);
  if (status.ok()) {
    out << "GET key1324 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32722: get
  status = db->Get(read_options, "key6067", &value);
  if (status.ok()) {
    out << "GET key6067 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32723: put
  db->Put(write_options, "key3391", "value48016");

  // op 32724: get
  status = db->Get(read_options, "key9991", &value);
  if (status.ok()) {
    out << "GET key9991 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32725: get
  status = db->Get(read_options, "key0253", &value);
  if (status.ok()) {
    out << "GET key0253 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32726: delete_range
  status = db->DeleteRange(write_options, "key6932", "key7716");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 32727: put
  db->Put(write_options, "key3845", "value118235");

  // op 32728: delete
  db->Delete(write_options, "key6437");

  // op 32729: put
  db->Put(write_options, "key7301", "value17427");

  // op 32730: get
  status = db->Get(read_options, "key1170", &value);
  if (status.ok()) {
    out << "GET key1170 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32731: get
  status = db->Get(read_options, "key2949", &value);
  if (status.ok()) {
    out << "GET key2949 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32732: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2728", "key7483", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2728, key7483) size=" << scan_result.size() << std::endl;
  }

  // op 32733: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 32734: put
  db->Put(write_options, "key1679", "value104701");

  // op 32735: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2188", "key8696", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2188, key8696) size=" << scan_result.size() << std::endl;
  }

  // op 32736: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 32737: delete_range
  status = db->DeleteRange(write_options, "key5901", "key10105");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 32738: put
  db->Put(write_options, "key0300", "value117249");

  // op 32739: get
  status = db->Get(read_options, "key4997", &value);
  if (status.ok()) {
    out << "GET key4997 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32740: put
  db->Put(write_options, "key7973", "value51166");

  // op 32741: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3529", "key10900", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3529, key10900) size=" << scan_result.size() << std::endl;
  }

  // op 32742: put
  db->Put(write_options, "key7401", "value72801");

  // op 32743: get
  status = db->Get(read_options, "key4357", &value);
  if (status.ok()) {
    out << "GET key4357 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32744: put
  db->Put(write_options, "key1052", "value103436");

  // op 32745: get
  status = db->Get(read_options, "key4601", &value);
  if (status.ok()) {
    out << "GET key4601 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32746: put
  db->Put(write_options, "key8778", "value87105");

  // op 32747: get
  status = db->Get(read_options, "key8659", &value);
  if (status.ok()) {
    out << "GET key8659 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32748: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5575", "key6371", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5575, key6371) size=" << scan_result.size() << std::endl;
  }

  // op 32749: get
  status = db->Get(read_options, "key3536", &value);
  if (status.ok()) {
    out << "GET key3536 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32750: put
  db->Put(write_options, "key10235", "value24554");

  // op 32751: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5748", "key8387", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5748, key8387) size=" << scan_result.size() << std::endl;
  }

  // op 32752: put
  db->Put(write_options, "key5470", "value113737");

  // op 32753: put
  db->Put(write_options, "key10765", "value102374");

  // op 32754: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3754", "key11204", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3754, key11204) size=" << scan_result.size() << std::endl;
  }

  // op 32755: delete_range
  status = db->DeleteRange(write_options, "key10378", "key10967");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 32756: put
  db->Put(write_options, "key8677", "value44435");

  // op 32757: put
  db->Put(write_options, "key2818", "value1919");

  // op 32758: put
  db->Put(write_options, "key8750", "value5571");

  // op 32759: delete
  db->Delete(write_options, "key10710");

  // op 32760: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2039", "key4720", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2039, key4720) size=" << scan_result.size() << std::endl;
  }

  // op 32761: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 32762: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 32763: get
  status = db->Get(read_options, "key7736", &value);
  if (status.ok()) {
    out << "GET key7736 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32764: put
  db->Put(write_options, "key6614", "value39143");

  // op 32765: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2190", "key4730", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2190, key4730) size=" << scan_result.size() << std::endl;
  }

  // op 32766: put
  db->Put(write_options, "key6631", "value47561");

  // op 32767: put
  db->Put(write_options, "key10290", "value70646");

  // op 32768: put
  db->Put(write_options, "key0079", "value52226");

  // op 32769: put
  db->Put(write_options, "key11210", "value27779");

  // op 32770: put
  db->Put(write_options, "key6668", "value31129");

  // op 32771: get
  status = db->Get(read_options, "key5256", &value);
  if (status.ok()) {
    out << "GET key5256 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32772: get
  status = db->Get(read_options, "key4075", &value);
  if (status.ok()) {
    out << "GET key4075 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32773: put
  db->Put(write_options, "key9429", "value83964");

  // op 32774: put
  db->Put(write_options, "key2264", "value48609");

  // op 32775: put
  db->Put(write_options, "key3715", "value45808");

  // op 32776: put
  db->Put(write_options, "key6153", "value87050");

  // op 32777: get
  status = db->Get(read_options, "key0200", &value);
  if (status.ok()) {
    out << "GET key0200 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32778: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 32779: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5315", "key9921", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5315, key9921) size=" << scan_result.size() << std::endl;
  }

  // op 32780: put
  db->Put(write_options, "key2338", "value68224");

  // op 32781: put
  db->Put(write_options, "key2253", "value63577");

  // op 32782: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1086", "key8095", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1086, key8095) size=" << scan_result.size() << std::endl;
  }

  // op 32783: get
  status = db->Get(read_options, "key10614", &value);
  if (status.ok()) {
    out << "GET key10614 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32784: put
  db->Put(write_options, "key5524", "value47990");

  // op 32785: put
  db->Put(write_options, "key11492", "value29092");

  // op 32786: put
  db->Put(write_options, "key0369", "value25702");

  // op 32787: delete_range
  status = db->DeleteRange(write_options, "key0356", "key5174");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 32788: put
  db->Put(write_options, "key7969", "value58979");

  // op 32789: get
  status = db->Get(read_options, "key4477", &value);
  if (status.ok()) {
    out << "GET key4477 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32790: put
  db->Put(write_options, "key0277", "value22116");

  // op 32791: put
  db->Put(write_options, "key8722", "value25832");

  // op 32792: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3875", "key10882", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3875, key10882) size=" << scan_result.size() << std::endl;
  }

  // op 32793: put
  db->Put(write_options, "key1339", "value90687");

  // op 32794: get
  status = db->Get(read_options, "key11136", &value);
  if (status.ok()) {
    out << "GET key11136 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32795: delete
  db->Delete(write_options, "key4733");

  // op 32796: put
  db->Put(write_options, "key4274", "value85981");

  // op 32797: delete
  db->Delete(write_options, "key5535");

  // op 32798: get
  status = db->Get(read_options, "key9102", &value);
  if (status.ok()) {
    out << "GET key9102 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32799: get
  status = db->Get(read_options, "key10796", &value);
  if (status.ok()) {
    out << "GET key10796 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32800: put
  db->Put(write_options, "key6813", "value88864");

  // op 32801: put
  db->Put(write_options, "key7434", "value12093");

  // op 32802: put
  db->Put(write_options, "key7718", "value62151");

  // op 32803: put
  db->Put(write_options, "key7193", "value17267");

  // op 32804: put
  db->Put(write_options, "key7985", "value93704");

  // op 32805: delete
  db->Delete(write_options, "key7591");

  // op 32806: get
  status = db->Get(read_options, "key6386", &value);
  if (status.ok()) {
    out << "GET key6386 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32807: delete
  db->Delete(write_options, "key9765");

  // op 32808: delete
  db->Delete(write_options, "key7508");

  // op 32809: put
  db->Put(write_options, "key7385", "value95808");

  // op 32810: delete_range
  status = db->DeleteRange(write_options, "key6005", "key8609");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 32811: put
  db->Put(write_options, "key8457", "value17838");

  // op 32812: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8799", "key10839", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8799, key10839) size=" << scan_result.size() << std::endl;
  }

  // op 32813: get
  status = db->Get(read_options, "key4969", &value);
  if (status.ok()) {
    out << "GET key4969 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32814: put
  db->Put(write_options, "key10615", "value71351");

  // op 32815: get
  status = db->Get(read_options, "key2714", &value);
  if (status.ok()) {
    out << "GET key2714 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32816: delete
  db->Delete(write_options, "key11660");

  // op 32817: put
  db->Put(write_options, "key6325", "value77872");

  // op 32818: put
  db->Put(write_options, "key4470", "value114268");

  // op 32819: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1147", "key1405", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1147, key1405) size=" << scan_result.size() << std::endl;
  }

  // op 32820: get
  status = db->Get(read_options, "key6874", &value);
  if (status.ok()) {
    out << "GET key6874 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32821: put
  db->Put(write_options, "key6812", "value52797");

  // op 32822: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2512", "key4125", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2512, key4125) size=" << scan_result.size() << std::endl;
  }

  // op 32823: put
  db->Put(write_options, "key3492", "value67097");

  // op 32824: put
  db->Put(write_options, "key9878", "value17167");

  // op 32825: put
  db->Put(write_options, "key6177", "value71723");

  // op 32826: put
  db->Put(write_options, "key10484", "value41721");

  // op 32827: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 32828: put
  db->Put(write_options, "key3544", "value4055");

  // op 32829: delete
  db->Delete(write_options, "key2919");

  // op 32830: delete_range
  status = db->DeleteRange(write_options, "key2444", "key7348");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 32831: put
  db->Put(write_options, "key0853", "value76861");

  // op 32832: delete
  db->Delete(write_options, "key4778");

  // op 32833: put
  db->Put(write_options, "key6591", "value9120");

  // op 32834: get
  status = db->Get(read_options, "key4612", &value);
  if (status.ok()) {
    out << "GET key4612 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32835: put
  db->Put(write_options, "key0070", "value99093");

  // op 32836: get
  status = db->Get(read_options, "key4417", &value);
  if (status.ok()) {
    out << "GET key4417 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32837: put
  db->Put(write_options, "key1930", "value80147");

  // op 32838: put
  db->Put(write_options, "key4295", "value50792");

  // op 32839: delete
  db->Delete(write_options, "key2101");

  // op 32840: put
  db->Put(write_options, "key6256", "value86169");

  // op 32841: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0461", "key6538", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0461, key6538) size=" << scan_result.size() << std::endl;
  }

  // op 32842: put
  db->Put(write_options, "key3868", "value99924");

  // op 32843: put
  db->Put(write_options, "key6882", "value19455");

  // op 32844: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2015", "key9507", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2015, key9507) size=" << scan_result.size() << std::endl;
  }

  // op 32845: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 32846: get
  status = db->Get(read_options, "key11599", &value);
  if (status.ok()) {
    out << "GET key11599 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32847: get
  status = db->Get(read_options, "key6865", &value);
  if (status.ok()) {
    out << "GET key6865 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32848: get
  status = db->Get(read_options, "key3264", &value);
  if (status.ok()) {
    out << "GET key3264 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32849: put
  db->Put(write_options, "key6760", "value14657");

  // op 32850: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2972", "key5497", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2972, key5497) size=" << scan_result.size() << std::endl;
  }

  // op 32851: put
  db->Put(write_options, "key9255", "value81612");

  // op 32852: get
  status = db->Get(read_options, "key7579", &value);
  if (status.ok()) {
    out << "GET key7579 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32853: delete
  db->Delete(write_options, "key5958");

  // op 32854: put
  db->Put(write_options, "key4894", "value111513");

  // op 32855: put
  db->Put(write_options, "key5453", "value97126");

  // op 32856: delete_range
  status = db->DeleteRange(write_options, "key2424", "key8519");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 32857: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5827", "key11633", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5827, key11633) size=" << scan_result.size() << std::endl;
  }

  // op 32858: put
  db->Put(write_options, "key10374", "value1574");

  // op 32859: get
  status = db->Get(read_options, "key4042", &value);
  if (status.ok()) {
    out << "GET key4042 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32860: delete_range
  status = db->DeleteRange(write_options, "key9576", "key11672");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 32861: put
  db->Put(write_options, "key0836", "value42216");

  // op 32862: put
  db->Put(write_options, "key8640", "value19013");

  // op 32863: get
  status = db->Get(read_options, "key6969", &value);
  if (status.ok()) {
    out << "GET key6969 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32864: put
  db->Put(write_options, "key6900", "value66424");

  // op 32865: get
  status = db->Get(read_options, "key4535", &value);
  if (status.ok()) {
    out << "GET key4535 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32866: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1217", "key2148", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1217, key2148) size=" << scan_result.size() << std::endl;
  }

  // op 32867: delete_range
  status = db->DeleteRange(write_options, "key4621", "key7980");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 32868: delete
  db->Delete(write_options, "key2822");

  // op 32869: delete_range
  status = db->DeleteRange(write_options, "key1577", "key5646");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 32870: get
  status = db->Get(read_options, "key10823", &value);
  if (status.ok()) {
    out << "GET key10823 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32871: put
  db->Put(write_options, "key8168", "value25564");

  // op 32872: delete
  db->Delete(write_options, "key11309");

  // op 32873: put
  db->Put(write_options, "key0367", "value4050");

  // op 32874: put
  db->Put(write_options, "key2608", "value17012");

  // op 32875: delete
  db->Delete(write_options, "key11701");

  // op 32876: put
  db->Put(write_options, "key8983", "value96260");

  // op 32877: put
  db->Put(write_options, "key9168", "value53887");

  // op 32878: put
  db->Put(write_options, "key6405", "value101597");

  // op 32879: get
  status = db->Get(read_options, "key8247", &value);
  if (status.ok()) {
    out << "GET key8247 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32880: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 32881: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8837", "key10569", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8837, key10569) size=" << scan_result.size() << std::endl;
  }

  // op 32882: put
  db->Put(write_options, "key2624", "value22563");

  // op 32883: put
  db->Put(write_options, "key4231", "value81899");

  // op 32884: delete_range
  status = db->DeleteRange(write_options, "key9949", "key10801");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 32885: delete_range
  status = db->DeleteRange(write_options, "key4755", "key6758");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 32886: put
  db->Put(write_options, "key1203", "value31460");

  // op 32887: get
  status = db->Get(read_options, "key2039", &value);
  if (status.ok()) {
    out << "GET key2039 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32888: delete
  db->Delete(write_options, "key2378");

  // op 32889: delete_range
  status = db->DeleteRange(write_options, "key7013", "key10906");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 32890: put
  db->Put(write_options, "key7051", "value116480");

  // op 32891: delete_range
  status = db->DeleteRange(write_options, "key8347", "key9856");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 32892: put
  db->Put(write_options, "key10606", "value38111");

  // op 32893: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5352", "key7015", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5352, key7015) size=" << scan_result.size() << std::endl;
  }

  // op 32894: get
  status = db->Get(read_options, "key4909", &value);
  if (status.ok()) {
    out << "GET key4909 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32895: get
  status = db->Get(read_options, "key4077", &value);
  if (status.ok()) {
    out << "GET key4077 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32896: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 32897: put
  db->Put(write_options, "key11076", "value32565");

  // op 32898: delete_range
  status = db->DeleteRange(write_options, "key0479", "key5698");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 32899: delete_range
  status = db->DeleteRange(write_options, "key1624", "key2894");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 32900: put
  db->Put(write_options, "key2103", "value63350");

  // op 32901: put
  db->Put(write_options, "key11349", "value95374");

  // op 32902: put
  db->Put(write_options, "key3038", "value37843");

  // op 32903: delete_range
  status = db->DeleteRange(write_options, "key1123", "key10023");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 32904: get
  status = db->Get(read_options, "key8767", &value);
  if (status.ok()) {
    out << "GET key8767 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32905: get
  status = db->Get(read_options, "key7201", &value);
  if (status.ok()) {
    out << "GET key7201 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32906: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 32907: put
  db->Put(write_options, "key1136", "value87443");

  // op 32908: delete
  db->Delete(write_options, "key8222");

  // op 32909: put
  db->Put(write_options, "key1935", "value98812");

  // op 32910: put
  db->Put(write_options, "key6122", "value48059");

  // op 32911: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2713", "key10349", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2713, key10349) size=" << scan_result.size() << std::endl;
  }

  // op 32912: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2236", "key3541", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2236, key3541) size=" << scan_result.size() << std::endl;
  }

  // op 32913: delete
  db->Delete(write_options, "key6133");

  // op 32914: put
  db->Put(write_options, "key7120", "value62846");

  // op 32915: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 32916: put
  db->Put(write_options, "key4853", "value82793");

  // op 32917: delete
  db->Delete(write_options, "key9153");

  // op 32918: delete_range
  status = db->DeleteRange(write_options, "key2922", "key8094");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 32919: get
  status = db->Get(read_options, "key3621", &value);
  if (status.ok()) {
    out << "GET key3621 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32920: delete_range
  status = db->DeleteRange(write_options, "key8465", "key9236");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 32921: put
  db->Put(write_options, "key2961", "value22296");

  // op 32922: put
  db->Put(write_options, "key3389", "value52178");

  // op 32923: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4783", "key8770", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4783, key8770) size=" << scan_result.size() << std::endl;
  }

  // op 32924: delete_range
  status = db->DeleteRange(write_options, "key1538", "key6412");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 32925: put
  db->Put(write_options, "key1033", "value44496");

  // op 32926: get
  status = db->Get(read_options, "key7303", &value);
  if (status.ok()) {
    out << "GET key7303 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32927: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2276", "key11394", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2276, key11394) size=" << scan_result.size() << std::endl;
  }

  // op 32928: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2397", "key7518", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2397, key7518) size=" << scan_result.size() << std::endl;
  }

  // op 32929: get
  status = db->Get(read_options, "key7095", &value);
  if (status.ok()) {
    out << "GET key7095 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32930: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1066", "key5965", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1066, key5965) size=" << scan_result.size() << std::endl;
  }

  // op 32931: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4992", "key8643", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4992, key8643) size=" << scan_result.size() << std::endl;
  }

  // op 32932: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 32933: put
  db->Put(write_options, "key2783", "value31056");

  // op 32934: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 32935: put
  db->Put(write_options, "key3505", "value64674");

  // op 32936: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0864", "key3095", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0864, key3095) size=" << scan_result.size() << std::endl;
  }

  // op 32937: delete_range
  status = db->DeleteRange(write_options, "key9080", "key9772");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 32938: put
  db->Put(write_options, "key10997", "value19487");

  // op 32939: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2994", "key6411", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2994, key6411) size=" << scan_result.size() << std::endl;
  }

  // op 32940: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7834", "key11200", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7834, key11200) size=" << scan_result.size() << std::endl;
  }

  // op 32941: get
  status = db->Get(read_options, "key3810", &value);
  if (status.ok()) {
    out << "GET key3810 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32942: put
  db->Put(write_options, "key11584", "value21291");

  // op 32943: delete
  db->Delete(write_options, "key0405");

  // op 32944: put
  db->Put(write_options, "key11033", "value1897");

  // op 32945: delete
  db->Delete(write_options, "key7059");

  // op 32946: get
  status = db->Get(read_options, "key2173", &value);
  if (status.ok()) {
    out << "GET key2173 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32947: put
  db->Put(write_options, "key7999", "value108732");

  // op 32948: put
  db->Put(write_options, "key0572", "value5990");

  // op 32949: get
  status = db->Get(read_options, "key2060", &value);
  if (status.ok()) {
    out << "GET key2060 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32950: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 32951: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5887", "key7651", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5887, key7651) size=" << scan_result.size() << std::endl;
  }

  // op 32952: put
  db->Put(write_options, "key5455", "value93309");

  // op 32953: delete
  db->Delete(write_options, "key5396");

  // op 32954: put
  db->Put(write_options, "key0532", "value28118");

  // op 32955: put
  db->Put(write_options, "key9959", "value20753");

  // op 32956: delete
  db->Delete(write_options, "key5320");

  // op 32957: delete_range
  status = db->DeleteRange(write_options, "key3169", "key3664");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 32958: put
  db->Put(write_options, "key8321", "value21981");

  // op 32959: put
  db->Put(write_options, "key8490", "value108285");

  // op 32960: put
  db->Put(write_options, "key0768", "value92271");

  // op 32961: delete
  db->Delete(write_options, "key5764");

  // op 32962: delete_range
  status = db->DeleteRange(write_options, "key0010", "key2921");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 32963: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0233", "key8046", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0233, key8046) size=" << scan_result.size() << std::endl;
  }

  // op 32964: put
  db->Put(write_options, "key11331", "value42736");

  // op 32965: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4717", "key8541", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4717, key8541) size=" << scan_result.size() << std::endl;
  }

  // op 32966: get
  status = db->Get(read_options, "key1759", &value);
  if (status.ok()) {
    out << "GET key1759 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32967: get
  status = db->Get(read_options, "key11085", &value);
  if (status.ok()) {
    out << "GET key11085 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32968: put
  db->Put(write_options, "key9962", "value65894");

  // op 32969: get
  status = db->Get(read_options, "key4819", &value);
  if (status.ok()) {
    out << "GET key4819 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32970: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 32971: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 32972: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 32973: put
  db->Put(write_options, "key6398", "value76136");

  // op 32974: put
  db->Put(write_options, "key7861", "value54610");

  // op 32975: get
  status = db->Get(read_options, "key9247", &value);
  if (status.ok()) {
    out << "GET key9247 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32976: put
  db->Put(write_options, "key5760", "value11250");

  // op 32977: put
  db->Put(write_options, "key7771", "value60591");

  // op 32978: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 32979: put
  db->Put(write_options, "key0770", "value20974");

  // op 32980: get
  status = db->Get(read_options, "key8248", &value);
  if (status.ok()) {
    out << "GET key8248 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32981: get
  status = db->Get(read_options, "key8648", &value);
  if (status.ok()) {
    out << "GET key8648 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32982: delete
  db->Delete(write_options, "key1168");

  // op 32983: put
  db->Put(write_options, "key2409", "value56696");

  // op 32984: get
  status = db->Get(read_options, "key1946", &value);
  if (status.ok()) {
    out << "GET key1946 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 32985: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6663", "key7991", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6663, key7991) size=" << scan_result.size() << std::endl;
  }

  // op 32986: delete_range
  status = db->DeleteRange(write_options, "key0781", "key9229");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 32987: delete
  db->Delete(write_options, "key3654");

  // op 32988: put
  db->Put(write_options, "key10159", "value35755");

  // op 32989: put
  db->Put(write_options, "key4484", "value87368");

  // op 32990: put
  db->Put(write_options, "key3241", "value26823");

  // op 32991: put
  db->Put(write_options, "key11629", "value65834");

  // op 32992: put
  db->Put(write_options, "key0361", "value22003");

  // op 32993: put
  db->Put(write_options, "key7904", "value4203");

  // op 32994: delete_range
  status = db->DeleteRange(write_options, "key5380", "key9848");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 32995: put
  db->Put(write_options, "key2671", "value82107");

  // op 32996: scan
  scan_result.clear();
  status = db->Scan(read_options, "key11135", "key11513", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key11135, key11513) size=" << scan_result.size() << std::endl;
  }

  // op 32997: put
  db->Put(write_options, "key2401", "value26602");

  // op 32998: put
  db->Put(write_options, "key0376", "value92718");

  // op 32999: delete_range
  status = db->DeleteRange(write_options, "key11194", "key11267");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 33000: get
  status = db->Get(read_options, "key8286", &value);
  if (status.ok()) {
    out << "GET key8286 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33001: get
  status = db->Get(read_options, "key4615", &value);
  if (status.ok()) {
    out << "GET key4615 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33002: put
  db->Put(write_options, "key5973", "value108482");

  // op 33003: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 33004: get
  status = db->Get(read_options, "key5585", &value);
  if (status.ok()) {
    out << "GET key5585 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33005: put
  db->Put(write_options, "key2787", "value103401");

  // op 33006: put
  db->Put(write_options, "key11370", "value72252");

  // op 33007: put
  db->Put(write_options, "key2861", "value110003");

  // op 33008: get
  status = db->Get(read_options, "key5549", &value);
  if (status.ok()) {
    out << "GET key5549 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33009: delete
  db->Delete(write_options, "key5698");

  // op 33010: get
  status = db->Get(read_options, "key4504", &value);
  if (status.ok()) {
    out << "GET key4504 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33011: delete
  db->Delete(write_options, "key8183");

  // op 33012: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4308", "key5231", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4308, key5231) size=" << scan_result.size() << std::endl;
  }

  // op 33013: get
  status = db->Get(read_options, "key6432", &value);
  if (status.ok()) {
    out << "GET key6432 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33014: put
  db->Put(write_options, "key4246", "value48305");

  // op 33015: get
  status = db->Get(read_options, "key10786", &value);
  if (status.ok()) {
    out << "GET key10786 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33016: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5106", "key8322", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5106, key8322) size=" << scan_result.size() << std::endl;
  }

  // op 33017: put
  db->Put(write_options, "key2270", "value63515");

  // op 33018: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0923", "key2325", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0923, key2325) size=" << scan_result.size() << std::endl;
  }

  // op 33019: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8618", "key11675", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8618, key11675) size=" << scan_result.size() << std::endl;
  }

  // op 33020: put
  db->Put(write_options, "key11377", "value49875");

  // op 33021: put
  db->Put(write_options, "key5083", "value27024");

  // op 33022: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1561", "key2290", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1561, key2290) size=" << scan_result.size() << std::endl;
  }

  // op 33023: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5434", "key6655", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5434, key6655) size=" << scan_result.size() << std::endl;
  }

  // op 33024: put
  db->Put(write_options, "key9718", "value109271");

  // op 33025: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 33026: put
  db->Put(write_options, "key11417", "value46071");

  // op 33027: put
  db->Put(write_options, "key9055", "value108854");

  // op 33028: get
  status = db->Get(read_options, "key0866", &value);
  if (status.ok()) {
    out << "GET key0866 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33029: put
  db->Put(write_options, "key7437", "value109709");

  // op 33030: put
  db->Put(write_options, "key2432", "value91247");

  // op 33031: scan
  scan_result.clear();
  status = db->Scan(read_options, "key11244", "key11389", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key11244, key11389) size=" << scan_result.size() << std::endl;
  }

  // op 33032: put
  db->Put(write_options, "key2850", "value105506");

  // op 33033: get
  status = db->Get(read_options, "key1322", &value);
  if (status.ok()) {
    out << "GET key1322 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33034: put
  db->Put(write_options, "key4071", "value12497");

  // op 33035: put
  db->Put(write_options, "key9551", "value22766");

  // op 33036: delete
  db->Delete(write_options, "key8113");

  // op 33037: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4493", "key9707", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4493, key9707) size=" << scan_result.size() << std::endl;
  }

  // op 33038: put
  db->Put(write_options, "key5435", "value70864");

  // op 33039: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1937", "key2510", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1937, key2510) size=" << scan_result.size() << std::endl;
  }

  // op 33040: put
  db->Put(write_options, "key11494", "value9311");

  // op 33041: put
  db->Put(write_options, "key7061", "value15968");

  // op 33042: get
  status = db->Get(read_options, "key11428", &value);
  if (status.ok()) {
    out << "GET key11428 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33043: delete_range
  status = db->DeleteRange(write_options, "key7615", "key10248");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 33044: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0757", "key7915", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0757, key7915) size=" << scan_result.size() << std::endl;
  }

  // op 33045: put
  db->Put(write_options, "key0673", "value8517");

  // op 33046: put
  db->Put(write_options, "key1893", "value37251");

  // op 33047: put
  db->Put(write_options, "key6820", "value110374");

  // op 33048: put
  db->Put(write_options, "key11650", "value109719");

  // op 33049: put
  db->Put(write_options, "key4328", "value9253");

  // op 33050: put
  db->Put(write_options, "key4497", "value82592");

  // op 33051: put
  db->Put(write_options, "key6803", "value49137");

  // op 33052: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4273", "key11715", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4273, key11715) size=" << scan_result.size() << std::endl;
  }

  // op 33053: put
  db->Put(write_options, "key9329", "value48866");

  // op 33054: put
  db->Put(write_options, "key8314", "value6851");

  // op 33055: put
  db->Put(write_options, "key8286", "value57027");

  // op 33056: get
  status = db->Get(read_options, "key11025", &value);
  if (status.ok()) {
    out << "GET key11025 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33057: get
  status = db->Get(read_options, "key4659", &value);
  if (status.ok()) {
    out << "GET key4659 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33058: get
  status = db->Get(read_options, "key6965", &value);
  if (status.ok()) {
    out << "GET key6965 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33059: put
  db->Put(write_options, "key11468", "value89789");

  // op 33060: delete_range
  status = db->DeleteRange(write_options, "key7871", "key8823");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 33061: get
  status = db->Get(read_options, "key11098", &value);
  if (status.ok()) {
    out << "GET key11098 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33062: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 33063: put
  db->Put(write_options, "key2707", "value75510");

  // op 33064: get
  status = db->Get(read_options, "key8575", &value);
  if (status.ok()) {
    out << "GET key8575 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33065: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3908", "key5811", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3908, key5811) size=" << scan_result.size() << std::endl;
  }

  // op 33066: put
  db->Put(write_options, "key0807", "value29971");

  // op 33067: put
  db->Put(write_options, "key4420", "value30491");

  // op 33068: get
  status = db->Get(read_options, "key4063", &value);
  if (status.ok()) {
    out << "GET key4063 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33069: get
  status = db->Get(read_options, "key4673", &value);
  if (status.ok()) {
    out << "GET key4673 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33070: put
  db->Put(write_options, "key9276", "value54246");

  // op 33071: put
  db->Put(write_options, "key1164", "value97007");

  // op 33072: get
  status = db->Get(read_options, "key1992", &value);
  if (status.ok()) {
    out << "GET key1992 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33073: put
  db->Put(write_options, "key6943", "value76559");

  // op 33074: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0014", "key5546", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0014, key5546) size=" << scan_result.size() << std::endl;
  }

  // op 33075: get
  status = db->Get(read_options, "key3099", &value);
  if (status.ok()) {
    out << "GET key3099 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33076: get
  status = db->Get(read_options, "key8671", &value);
  if (status.ok()) {
    out << "GET key8671 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33077: delete_range
  status = db->DeleteRange(write_options, "key6661", "key9665");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 33078: get
  status = db->Get(read_options, "key0860", &value);
  if (status.ok()) {
    out << "GET key0860 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33079: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3033", "key7111", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3033, key7111) size=" << scan_result.size() << std::endl;
  }

  // op 33080: put
  db->Put(write_options, "key5310", "value102018");

  // op 33081: delete
  db->Delete(write_options, "key7020");

  // op 33082: delete_range
  status = db->DeleteRange(write_options, "key1881", "key7335");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 33083: put
  db->Put(write_options, "key5598", "value12732");

  // op 33084: put
  db->Put(write_options, "key1774", "value103773");

  // op 33085: put
  db->Put(write_options, "key5828", "value6921");

  // op 33086: get
  status = db->Get(read_options, "key0938", &value);
  if (status.ok()) {
    out << "GET key0938 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33087: get
  status = db->Get(read_options, "key0349", &value);
  if (status.ok()) {
    out << "GET key0349 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33088: put
  db->Put(write_options, "key7503", "value101714");

  // op 33089: get
  status = db->Get(read_options, "key8784", &value);
  if (status.ok()) {
    out << "GET key8784 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33090: get
  status = db->Get(read_options, "key4904", &value);
  if (status.ok()) {
    out << "GET key4904 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33091: get
  status = db->Get(read_options, "key7458", &value);
  if (status.ok()) {
    out << "GET key7458 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33092: get
  status = db->Get(read_options, "key4059", &value);
  if (status.ok()) {
    out << "GET key4059 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33093: put
  db->Put(write_options, "key6214", "value7940");

  // op 33094: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 33095: put
  db->Put(write_options, "key4598", "value19752");

  // op 33096: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3060", "key8103", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3060, key8103) size=" << scan_result.size() << std::endl;
  }

  // op 33097: put
  db->Put(write_options, "key5254", "value6047");

  // op 33098: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 33099: delete
  db->Delete(write_options, "key10305");

  // op 33100: put
  db->Put(write_options, "key1259", "value28213");

  // op 33101: get
  status = db->Get(read_options, "key6343", &value);
  if (status.ok()) {
    out << "GET key6343 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33102: delete_range
  status = db->DeleteRange(write_options, "key4121", "key5909");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 33103: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0357", "key3708", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0357, key3708) size=" << scan_result.size() << std::endl;
  }

  // op 33104: get
  status = db->Get(read_options, "key3429", &value);
  if (status.ok()) {
    out << "GET key3429 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33105: delete
  db->Delete(write_options, "key6198");

  // op 33106: put
  db->Put(write_options, "key7201", "value45276");

  // op 33107: get
  status = db->Get(read_options, "key5274", &value);
  if (status.ok()) {
    out << "GET key5274 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33108: delete_range
  status = db->DeleteRange(write_options, "key1147", "key4790");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 33109: delete_range
  status = db->DeleteRange(write_options, "key10968", "key11404");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 33110: put
  db->Put(write_options, "key6924", "value7733");

  // op 33111: put
  db->Put(write_options, "key3279", "value37868");

  // op 33112: delete
  db->Delete(write_options, "key4542");

  // op 33113: put
  db->Put(write_options, "key9383", "value1973");

  // op 33114: put
  db->Put(write_options, "key0590", "value113864");

  // op 33115: get
  status = db->Get(read_options, "key9059", &value);
  if (status.ok()) {
    out << "GET key9059 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33116: put
  db->Put(write_options, "key10702", "value37735");

  // op 33117: get
  status = db->Get(read_options, "key6580", &value);
  if (status.ok()) {
    out << "GET key6580 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33118: put
  db->Put(write_options, "key7029", "value84798");

  // op 33119: delete_range
  status = db->DeleteRange(write_options, "key4948", "key6232");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 33120: delete
  db->Delete(write_options, "key6386");

  // op 33121: put
  db->Put(write_options, "key2658", "value5079");

  // op 33122: put
  db->Put(write_options, "key6201", "value61139");

  // op 33123: put
  db->Put(write_options, "key4386", "value82787");

  // op 33124: put
  db->Put(write_options, "key10206", "value60220");

  // op 33125: put
  db->Put(write_options, "key4949", "value37206");

  // op 33126: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3657", "key8389", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3657, key8389) size=" << scan_result.size() << std::endl;
  }

  // op 33127: put
  db->Put(write_options, "key5293", "value95258");

  // op 33128: put
  db->Put(write_options, "key9192", "value82279");

  // op 33129: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3528", "key8160", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3528, key8160) size=" << scan_result.size() << std::endl;
  }

  // op 33130: delete
  db->Delete(write_options, "key9000");

  // op 33131: get
  status = db->Get(read_options, "key4685", &value);
  if (status.ok()) {
    out << "GET key4685 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33132: get
  status = db->Get(read_options, "key7915", &value);
  if (status.ok()) {
    out << "GET key7915 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33133: delete
  db->Delete(write_options, "key6160");

  // op 33134: get
  status = db->Get(read_options, "key11355", &value);
  if (status.ok()) {
    out << "GET key11355 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33135: get
  status = db->Get(read_options, "key0901", &value);
  if (status.ok()) {
    out << "GET key0901 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33136: delete_range
  status = db->DeleteRange(write_options, "key1793", "key2677");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 33137: get
  status = db->Get(read_options, "key2775", &value);
  if (status.ok()) {
    out << "GET key2775 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33138: put
  db->Put(write_options, "key2688", "value62110");

  // op 33139: put
  db->Put(write_options, "key2612", "value19423");

  // op 33140: put
  db->Put(write_options, "key11259", "value109378");

  // op 33141: put
  db->Put(write_options, "key10175", "value52642");

  // op 33142: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5467", "key7058", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5467, key7058) size=" << scan_result.size() << std::endl;
  }

  // op 33143: delete
  db->Delete(write_options, "key4964");

  // op 33144: put
  db->Put(write_options, "key8744", "value67216");

  // op 33145: put
  db->Put(write_options, "key10064", "value83764");

  // op 33146: delete
  db->Delete(write_options, "key2717");

  // op 33147: delete_range
  status = db->DeleteRange(write_options, "key7053", "key8884");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 33148: get
  status = db->Get(read_options, "key6006", &value);
  if (status.ok()) {
    out << "GET key6006 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33149: delete_range
  status = db->DeleteRange(write_options, "key1527", "key9488");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 33150: get
  status = db->Get(read_options, "key11168", &value);
  if (status.ok()) {
    out << "GET key11168 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33151: get
  status = db->Get(read_options, "key2789", &value);
  if (status.ok()) {
    out << "GET key2789 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33152: get
  status = db->Get(read_options, "key0547", &value);
  if (status.ok()) {
    out << "GET key0547 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33153: delete_range
  status = db->DeleteRange(write_options, "key2554", "key3619");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 33154: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 33155: put
  db->Put(write_options, "key3548", "value66192");

  // op 33156: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 33157: put
  db->Put(write_options, "key11645", "value28469");

  // op 33158: put
  db->Put(write_options, "key11612", "value2909");

  // op 33159: put
  db->Put(write_options, "key7173", "value13677");

  // op 33160: delete_range
  status = db->DeleteRange(write_options, "key3176", "key9673");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 33161: put
  db->Put(write_options, "key9443", "value19927");

  // op 33162: put
  db->Put(write_options, "key7977", "value39325");

  // op 33163: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2530", "key7917", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2530, key7917) size=" << scan_result.size() << std::endl;
  }

  // op 33164: put
  db->Put(write_options, "key5070", "value75320");

  // op 33165: put
  db->Put(write_options, "key4064", "value12645");

  // op 33166: get
  status = db->Get(read_options, "key4825", &value);
  if (status.ok()) {
    out << "GET key4825 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33167: get
  status = db->Get(read_options, "key9277", &value);
  if (status.ok()) {
    out << "GET key9277 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33168: put
  db->Put(write_options, "key0806", "value13000");

  // op 33169: delete
  db->Delete(write_options, "key4663");

  // op 33170: delete
  db->Delete(write_options, "key3537");

  // op 33171: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0036", "key7718", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0036, key7718) size=" << scan_result.size() << std::endl;
  }

  // op 33172: put
  db->Put(write_options, "key6187", "value91133");

  // op 33173: put
  db->Put(write_options, "key1344", "value100420");

  // op 33174: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 33175: put
  db->Put(write_options, "key7945", "value6443");

  // op 33176: put
  db->Put(write_options, "key11563", "value19270");

  // op 33177: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3024", "key6514", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3024, key6514) size=" << scan_result.size() << std::endl;
  }

  // op 33178: put
  db->Put(write_options, "key1325", "value58792");

  // op 33179: delete_range
  status = db->DeleteRange(write_options, "key5107", "key5635");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 33180: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 33181: get
  status = db->Get(read_options, "key8575", &value);
  if (status.ok()) {
    out << "GET key8575 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33182: get
  status = db->Get(read_options, "key3337", &value);
  if (status.ok()) {
    out << "GET key3337 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33183: put
  db->Put(write_options, "key0102", "value57892");

  // op 33184: delete
  db->Delete(write_options, "key1499");

  // op 33185: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8653", "key10522", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8653, key10522) size=" << scan_result.size() << std::endl;
  }

  // op 33186: put
  db->Put(write_options, "key0680", "value105754");

  // op 33187: put
  db->Put(write_options, "key2196", "value12647");

  // op 33188: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1543", "key2012", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1543, key2012) size=" << scan_result.size() << std::endl;
  }

  // op 33189: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1910", "key10637", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1910, key10637) size=" << scan_result.size() << std::endl;
  }

  // op 33190: put
  db->Put(write_options, "key5091", "value60322");

  // op 33191: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 33192: put
  db->Put(write_options, "key3614", "value34494");

  // op 33193: get
  status = db->Get(read_options, "key2630", &value);
  if (status.ok()) {
    out << "GET key2630 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33194: put
  db->Put(write_options, "key5649", "value106343");

  // op 33195: put
  db->Put(write_options, "key6589", "value21542");

  // op 33196: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 33197: delete_range
  status = db->DeleteRange(write_options, "key1539", "key10184");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 33198: delete_range
  status = db->DeleteRange(write_options, "key8785", "key11494");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 33199: delete
  db->Delete(write_options, "key5293");

  // op 33200: delete_range
  status = db->DeleteRange(write_options, "key6936", "key8548");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 33201: put
  db->Put(write_options, "key3889", "value98742");

  // op 33202: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6959", "key8677", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6959, key8677) size=" << scan_result.size() << std::endl;
  }

  // op 33203: get
  status = db->Get(read_options, "key4379", &value);
  if (status.ok()) {
    out << "GET key4379 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33204: delete
  db->Delete(write_options, "key9101");

  // op 33205: get
  status = db->Get(read_options, "key1202", &value);
  if (status.ok()) {
    out << "GET key1202 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33206: delete
  db->Delete(write_options, "key2893");

  // op 33207: get
  status = db->Get(read_options, "key4727", &value);
  if (status.ok()) {
    out << "GET key4727 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33208: get
  status = db->Get(read_options, "key1169", &value);
  if (status.ok()) {
    out << "GET key1169 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33209: put
  db->Put(write_options, "key2916", "value23524");

  // op 33210: get
  status = db->Get(read_options, "key2854", &value);
  if (status.ok()) {
    out << "GET key2854 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33211: put
  db->Put(write_options, "key11570", "value73130");

  // op 33212: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1736", "key5978", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1736, key5978) size=" << scan_result.size() << std::endl;
  }

  // op 33213: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4750", "key11601", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4750, key11601) size=" << scan_result.size() << std::endl;
  }

  // op 33214: delete_range
  status = db->DeleteRange(write_options, "key3466", "key4107");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 33215: delete_range
  status = db->DeleteRange(write_options, "key4113", "key11491");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 33216: delete
  db->Delete(write_options, "key0698");

  // op 33217: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 33218: get
  status = db->Get(read_options, "key11138", &value);
  if (status.ok()) {
    out << "GET key11138 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33219: put
  db->Put(write_options, "key10152", "value109282");

  // op 33220: get
  status = db->Get(read_options, "key6537", &value);
  if (status.ok()) {
    out << "GET key6537 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33221: get
  status = db->Get(read_options, "key6046", &value);
  if (status.ok()) {
    out << "GET key6046 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33222: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3968", "key9379", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3968, key9379) size=" << scan_result.size() << std::endl;
  }

  // op 33223: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2496", "key2705", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2496, key2705) size=" << scan_result.size() << std::endl;
  }

  // op 33224: put
  db->Put(write_options, "key8296", "value61342");

  // op 33225: delete_range
  status = db->DeleteRange(write_options, "key9672", "key11694");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 33226: put
  db->Put(write_options, "key4741", "value17943");

  // op 33227: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3628", "key6791", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3628, key6791) size=" << scan_result.size() << std::endl;
  }

  // op 33228: put
  db->Put(write_options, "key3527", "value102886");

  // op 33229: get
  status = db->Get(read_options, "key5859", &value);
  if (status.ok()) {
    out << "GET key5859 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33230: put
  db->Put(write_options, "key2207", "value44392");

  // op 33231: get
  status = db->Get(read_options, "key0085", &value);
  if (status.ok()) {
    out << "GET key0085 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33232: get
  status = db->Get(read_options, "key8678", &value);
  if (status.ok()) {
    out << "GET key8678 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33233: get
  status = db->Get(read_options, "key1378", &value);
  if (status.ok()) {
    out << "GET key1378 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33234: delete
  db->Delete(write_options, "key3562");

  // op 33235: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0562", "key10506", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0562, key10506) size=" << scan_result.size() << std::endl;
  }

  // op 33236: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1993", "key8114", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1993, key8114) size=" << scan_result.size() << std::endl;
  }

  // op 33237: get
  status = db->Get(read_options, "key4716", &value);
  if (status.ok()) {
    out << "GET key4716 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33238: delete
  db->Delete(write_options, "key7917");

  // op 33239: put
  db->Put(write_options, "key1944", "value21726");

  // op 33240: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0225", "key11400", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0225, key11400) size=" << scan_result.size() << std::endl;
  }

  // op 33241: delete_range
  status = db->DeleteRange(write_options, "key2466", "key3797");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 33242: get
  status = db->Get(read_options, "key7735", &value);
  if (status.ok()) {
    out << "GET key7735 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33243: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 33244: put
  db->Put(write_options, "key0220", "value15778");

  // op 33245: get
  status = db->Get(read_options, "key2581", &value);
  if (status.ok()) {
    out << "GET key2581 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33246: delete_range
  status = db->DeleteRange(write_options, "key5476", "key10794");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 33247: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8012", "key10784", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8012, key10784) size=" << scan_result.size() << std::endl;
  }

  // op 33248: put
  db->Put(write_options, "key4686", "value98761");

  // op 33249: put
  db->Put(write_options, "key9144", "value20254");

  // op 33250: put
  db->Put(write_options, "key11251", "value45466");

  // op 33251: delete
  db->Delete(write_options, "key9502");

  // op 33252: delete_range
  status = db->DeleteRange(write_options, "key3822", "key8109");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 33253: delete
  db->Delete(write_options, "key4708");

  // op 33254: put
  db->Put(write_options, "key0095", "value48883");

  // op 33255: put
  db->Put(write_options, "key4853", "value79140");

  // op 33256: delete_range
  status = db->DeleteRange(write_options, "key1927", "key3982");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 33257: delete
  db->Delete(write_options, "key5975");

  // op 33258: delete_range
  status = db->DeleteRange(write_options, "key1718", "key11301");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 33259: get
  status = db->Get(read_options, "key0947", &value);
  if (status.ok()) {
    out << "GET key0947 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33260: put
  db->Put(write_options, "key5240", "value109532");

  // op 33261: put
  db->Put(write_options, "key7034", "value7686");

  // op 33262: put
  db->Put(write_options, "key9312", "value5179");

  // op 33263: put
  db->Put(write_options, "key8000", "value4921");

  // op 33264: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1999", "key7613", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1999, key7613) size=" << scan_result.size() << std::endl;
  }

  // op 33265: get
  status = db->Get(read_options, "key0808", &value);
  if (status.ok()) {
    out << "GET key0808 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33266: put
  db->Put(write_options, "key11499", "value52072");

  // op 33267: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1999", "key10883", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1999, key10883) size=" << scan_result.size() << std::endl;
  }

  // op 33268: put
  db->Put(write_options, "key0018", "value94070");

  // op 33269: get
  status = db->Get(read_options, "key4385", &value);
  if (status.ok()) {
    out << "GET key4385 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33270: delete_range
  status = db->DeleteRange(write_options, "key8927", "key9549");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 33271: put
  db->Put(write_options, "key5734", "value13165");

  // op 33272: delete
  db->Delete(write_options, "key0583");

  // op 33273: put
  db->Put(write_options, "key5669", "value2516");

  // op 33274: put
  db->Put(write_options, "key5067", "value31353");

  // op 33275: get
  status = db->Get(read_options, "key0247", &value);
  if (status.ok()) {
    out << "GET key0247 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33276: get
  status = db->Get(read_options, "key3750", &value);
  if (status.ok()) {
    out << "GET key3750 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33277: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3822", "key9506", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3822, key9506) size=" << scan_result.size() << std::endl;
  }

  // op 33278: delete
  db->Delete(write_options, "key1766");

  // op 33279: get
  status = db->Get(read_options, "key1915", &value);
  if (status.ok()) {
    out << "GET key1915 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33280: put
  db->Put(write_options, "key5323", "value48399");

  // op 33281: put
  db->Put(write_options, "key0581", "value103500");

  // op 33282: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3654", "key7411", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3654, key7411) size=" << scan_result.size() << std::endl;
  }

  // op 33283: get
  status = db->Get(read_options, "key2585", &value);
  if (status.ok()) {
    out << "GET key2585 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33284: put
  db->Put(write_options, "key8899", "value91471");

  // op 33285: get
  status = db->Get(read_options, "key9913", &value);
  if (status.ok()) {
    out << "GET key9913 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33286: get
  status = db->Get(read_options, "key1961", &value);
  if (status.ok()) {
    out << "GET key1961 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33287: delete
  db->Delete(write_options, "key7296");

  // op 33288: put
  db->Put(write_options, "key4987", "value43479");

  // op 33289: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7537", "key9668", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7537, key9668) size=" << scan_result.size() << std::endl;
  }

  // op 33290: put
  db->Put(write_options, "key4111", "value84935");

  // op 33291: get
  status = db->Get(read_options, "key2652", &value);
  if (status.ok()) {
    out << "GET key2652 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33292: put
  db->Put(write_options, "key8005", "value6191");

  // op 33293: get
  status = db->Get(read_options, "key5934", &value);
  if (status.ok()) {
    out << "GET key5934 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33294: delete_range
  status = db->DeleteRange(write_options, "key2644", "key7404");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 33295: get
  status = db->Get(read_options, "key11106", &value);
  if (status.ok()) {
    out << "GET key11106 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33296: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6931", "key9614", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6931, key9614) size=" << scan_result.size() << std::endl;
  }

  // op 33297: delete_range
  status = db->DeleteRange(write_options, "key1588", "key3637");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 33298: get
  status = db->Get(read_options, "key4396", &value);
  if (status.ok()) {
    out << "GET key4396 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33299: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3636", "key4819", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3636, key4819) size=" << scan_result.size() << std::endl;
  }

  // op 33300: get
  status = db->Get(read_options, "key2670", &value);
  if (status.ok()) {
    out << "GET key2670 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33301: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4789", "key10454", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4789, key10454) size=" << scan_result.size() << std::endl;
  }

  // op 33302: delete
  db->Delete(write_options, "key2267");

  // op 33303: put
  db->Put(write_options, "key9263", "value65686");

  // op 33304: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0073", "key0128", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0073, key0128) size=" << scan_result.size() << std::endl;
  }

  // op 33305: get
  status = db->Get(read_options, "key4219", &value);
  if (status.ok()) {
    out << "GET key4219 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33306: put
  db->Put(write_options, "key4923", "value6765");

  // op 33307: delete_range
  status = db->DeleteRange(write_options, "key7666", "key11206");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 33308: put
  db->Put(write_options, "key7419", "value11361");

  // op 33309: put
  db->Put(write_options, "key5951", "value43767");

  // op 33310: get
  status = db->Get(read_options, "key10267", &value);
  if (status.ok()) {
    out << "GET key10267 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33311: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6607", "key9612", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6607, key9612) size=" << scan_result.size() << std::endl;
  }

  // op 33312: put
  db->Put(write_options, "key11721", "value42132");

  // op 33313: delete_range
  status = db->DeleteRange(write_options, "key3506", "key7020");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 33314: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 33315: get
  status = db->Get(read_options, "key7106", &value);
  if (status.ok()) {
    out << "GET key7106 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33316: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 33317: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 33318: put
  db->Put(write_options, "key8901", "value67087");

  // op 33319: put
  db->Put(write_options, "key1122", "value27611");

  // op 33320: delete_range
  status = db->DeleteRange(write_options, "key2647", "key5925");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 33321: delete_range
  status = db->DeleteRange(write_options, "key0016", "key3891");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 33322: delete
  db->Delete(write_options, "key0353");

  // op 33323: put
  db->Put(write_options, "key7864", "value101622");

  // op 33324: put
  db->Put(write_options, "key5856", "value80638");

  // op 33325: put
  db->Put(write_options, "key7816", "value68318");

  // op 33326: put
  db->Put(write_options, "key1786", "value68855");

  // op 33327: put
  db->Put(write_options, "key3404", "value40917");

  // op 33328: get
  status = db->Get(read_options, "key2332", &value);
  if (status.ok()) {
    out << "GET key2332 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33329: delete
  db->Delete(write_options, "key10542");

  // op 33330: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 33331: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1623", "key8383", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1623, key8383) size=" << scan_result.size() << std::endl;
  }

  // op 33332: put
  db->Put(write_options, "key2214", "value81367");

  // op 33333: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7478", "key11703", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7478, key11703) size=" << scan_result.size() << std::endl;
  }

  // op 33334: put
  db->Put(write_options, "key9021", "value59600");

  // op 33335: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2027", "key11122", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2027, key11122) size=" << scan_result.size() << std::endl;
  }

  // op 33336: delete_range
  status = db->DeleteRange(write_options, "key5758", "key6503");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 33337: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3957", "key4553", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3957, key4553) size=" << scan_result.size() << std::endl;
  }

  // op 33338: put
  db->Put(write_options, "key4638", "value110413");

  // op 33339: put
  db->Put(write_options, "key6305", "value71763");

  // op 33340: get
  status = db->Get(read_options, "key1750", &value);
  if (status.ok()) {
    out << "GET key1750 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33341: delete_range
  status = db->DeleteRange(write_options, "key0108", "key2520");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 33342: get
  status = db->Get(read_options, "key8147", &value);
  if (status.ok()) {
    out << "GET key8147 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33343: put
  db->Put(write_options, "key0359", "value113790");

  // op 33344: delete
  db->Delete(write_options, "key1471");

  // op 33345: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 33346: put
  db->Put(write_options, "key0968", "value85961");

  // op 33347: put
  db->Put(write_options, "key11230", "value8571");

  // op 33348: get
  status = db->Get(read_options, "key1418", &value);
  if (status.ok()) {
    out << "GET key1418 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33349: put
  db->Put(write_options, "key11504", "value13572");

  // op 33350: put
  db->Put(write_options, "key2800", "value60526");

  // op 33351: delete_range
  status = db->DeleteRange(write_options, "key6700", "key7248");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 33352: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2381", "key7263", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2381, key7263) size=" << scan_result.size() << std::endl;
  }

  // op 33353: delete_range
  status = db->DeleteRange(write_options, "key6203", "key11201");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 33354: put
  db->Put(write_options, "key2172", "value59582");

  // op 33355: put
  db->Put(write_options, "key1985", "value116778");

  // op 33356: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8616", "key10432", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8616, key10432) size=" << scan_result.size() << std::endl;
  }

  // op 33357: get
  status = db->Get(read_options, "key9388", &value);
  if (status.ok()) {
    out << "GET key9388 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33358: put
  db->Put(write_options, "key4164", "value72551");

  // op 33359: put
  db->Put(write_options, "key2633", "value81195");

  // op 33360: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 33361: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6455", "key8240", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6455, key8240) size=" << scan_result.size() << std::endl;
  }

  // op 33362: delete
  db->Delete(write_options, "key3181");

  // op 33363: put
  db->Put(write_options, "key7986", "value17711");

  // op 33364: delete_range
  status = db->DeleteRange(write_options, "key2749", "key7428");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 33365: put
  db->Put(write_options, "key4260", "value8014");

  // op 33366: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3124", "key8933", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3124, key8933) size=" << scan_result.size() << std::endl;
  }

  // op 33367: put
  db->Put(write_options, "key2762", "value11648");

  // op 33368: put
  db->Put(write_options, "key4592", "value85662");

  // op 33369: put
  db->Put(write_options, "key4655", "value117925");

  // op 33370: put
  db->Put(write_options, "key9054", "value13635");

  // op 33371: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0962", "key1074", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0962, key1074) size=" << scan_result.size() << std::endl;
  }

  // op 33372: put
  db->Put(write_options, "key0185", "value14649");

  // op 33373: put
  db->Put(write_options, "key3708", "value80625");

  // op 33374: get
  status = db->Get(read_options, "key9154", &value);
  if (status.ok()) {
    out << "GET key9154 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33375: get
  status = db->Get(read_options, "key4011", &value);
  if (status.ok()) {
    out << "GET key4011 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33376: put
  db->Put(write_options, "key8797", "value97547");

  // op 33377: put
  db->Put(write_options, "key7060", "value101070");

  // op 33378: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5248", "key6892", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5248, key6892) size=" << scan_result.size() << std::endl;
  }

  // op 33379: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8983", "key10076", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8983, key10076) size=" << scan_result.size() << std::endl;
  }

  // op 33380: get
  status = db->Get(read_options, "key10711", &value);
  if (status.ok()) {
    out << "GET key10711 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33381: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9086", "key9189", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9086, key9189) size=" << scan_result.size() << std::endl;
  }

  // op 33382: delete
  db->Delete(write_options, "key1208");

  // op 33383: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0838", "key7444", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0838, key7444) size=" << scan_result.size() << std::endl;
  }

  // op 33384: delete
  db->Delete(write_options, "key9767");

  // op 33385: put
  db->Put(write_options, "key6399", "value101394");

  // op 33386: get
  status = db->Get(read_options, "key8156", &value);
  if (status.ok()) {
    out << "GET key8156 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33387: delete
  db->Delete(write_options, "key1448");

  // op 33388: get
  status = db->Get(read_options, "key2489", &value);
  if (status.ok()) {
    out << "GET key2489 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33389: put
  db->Put(write_options, "key4405", "value21966");

  // op 33390: delete
  db->Delete(write_options, "key10416");

  // op 33391: put
  db->Put(write_options, "key8244", "value90738");

  // op 33392: get
  status = db->Get(read_options, "key10499", &value);
  if (status.ok()) {
    out << "GET key10499 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33393: put
  db->Put(write_options, "key5534", "value76457");

  // op 33394: get
  status = db->Get(read_options, "key2283", &value);
  if (status.ok()) {
    out << "GET key2283 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33395: put
  db->Put(write_options, "key2802", "value67495");

  // op 33396: get
  status = db->Get(read_options, "key2782", &value);
  if (status.ok()) {
    out << "GET key2782 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33397: put
  db->Put(write_options, "key3291", "value24004");

  // op 33398: put
  db->Put(write_options, "key10035", "value80533");

  // op 33399: put
  db->Put(write_options, "key7939", "value63693");

  // op 33400: put
  db->Put(write_options, "key7447", "value79524");

  // op 33401: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 33402: put
  db->Put(write_options, "key0694", "value15052");

  // op 33403: put
  db->Put(write_options, "key1094", "value20748");

  // op 33404: get
  status = db->Get(read_options, "key10183", &value);
  if (status.ok()) {
    out << "GET key10183 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33405: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3241", "key7281", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3241, key7281) size=" << scan_result.size() << std::endl;
  }

  // op 33406: get
  status = db->Get(read_options, "key8742", &value);
  if (status.ok()) {
    out << "GET key8742 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33407: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6954", "key7693", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6954, key7693) size=" << scan_result.size() << std::endl;
  }

  // op 33408: put
  db->Put(write_options, "key5249", "value23683");

  // op 33409: delete_range
  status = db->DeleteRange(write_options, "key4530", "key5181");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 33410: get
  status = db->Get(read_options, "key11544", &value);
  if (status.ok()) {
    out << "GET key11544 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33411: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3423", "key4725", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3423, key4725) size=" << scan_result.size() << std::endl;
  }

  // op 33412: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 33413: get
  status = db->Get(read_options, "key10401", &value);
  if (status.ok()) {
    out << "GET key10401 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33414: put
  db->Put(write_options, "key9165", "value91162");

  // op 33415: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4081", "key8596", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4081, key8596) size=" << scan_result.size() << std::endl;
  }

  // op 33416: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8605", "key8931", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8605, key8931) size=" << scan_result.size() << std::endl;
  }

  // op 33417: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7098", "key10340", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7098, key10340) size=" << scan_result.size() << std::endl;
  }

  // op 33418: put
  db->Put(write_options, "key3424", "value111675");

  // op 33419: delete
  db->Delete(write_options, "key10877");

  // op 33420: delete_range
  status = db->DeleteRange(write_options, "key1133", "key10277");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 33421: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8711", "key11367", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8711, key11367) size=" << scan_result.size() << std::endl;
  }

  // op 33422: put
  db->Put(write_options, "key1138", "value31520");

  // op 33423: put
  db->Put(write_options, "key6628", "value106691");

  // op 33424: put
  db->Put(write_options, "key5744", "value111368");

  // op 33425: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0225", "key10390", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0225, key10390) size=" << scan_result.size() << std::endl;
  }

  // op 33426: put
  db->Put(write_options, "key4696", "value2991");

  // op 33427: delete
  db->Delete(write_options, "key9936");

  // op 33428: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5316", "key5540", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5316, key5540) size=" << scan_result.size() << std::endl;
  }

  // op 33429: put
  db->Put(write_options, "key5143", "value62036");

  // op 33430: delete
  db->Delete(write_options, "key10549");

  // op 33431: put
  db->Put(write_options, "key0542", "value37382");

  // op 33432: delete_range
  status = db->DeleteRange(write_options, "key0313", "key6213");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 33433: put
  db->Put(write_options, "key3763", "value11562");

  // op 33434: put
  db->Put(write_options, "key1297", "value5879");

  // op 33435: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1068", "key10970", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1068, key10970) size=" << scan_result.size() << std::endl;
  }

  // op 33436: get
  status = db->Get(read_options, "key5389", &value);
  if (status.ok()) {
    out << "GET key5389 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33437: put
  db->Put(write_options, "key6212", "value20750");

  // op 33438: put
  db->Put(write_options, "key0244", "value53023");

  // op 33439: delete
  db->Delete(write_options, "key5416");

  // op 33440: delete
  db->Delete(write_options, "key4678");

  // op 33441: get
  status = db->Get(read_options, "key6405", &value);
  if (status.ok()) {
    out << "GET key6405 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33442: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0406", "key2339", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0406, key2339) size=" << scan_result.size() << std::endl;
  }

  // op 33443: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 33444: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2259", "key3936", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2259, key3936) size=" << scan_result.size() << std::endl;
  }

  // op 33445: get
  status = db->Get(read_options, "key8262", &value);
  if (status.ok()) {
    out << "GET key8262 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33446: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6403", "key8495", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6403, key8495) size=" << scan_result.size() << std::endl;
  }

  // op 33447: put
  db->Put(write_options, "key0667", "value19382");

  // op 33448: put
  db->Put(write_options, "key7402", "value16952");

  // op 33449: get
  status = db->Get(read_options, "key2954", &value);
  if (status.ok()) {
    out << "GET key2954 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33450: get
  status = db->Get(read_options, "key6902", &value);
  if (status.ok()) {
    out << "GET key6902 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33451: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 33452: put
  db->Put(write_options, "key10150", "value21814");

  // op 33453: delete
  db->Delete(write_options, "key4357");

  // op 33454: delete_range
  status = db->DeleteRange(write_options, "key1615", "key8888");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 33455: put
  db->Put(write_options, "key5793", "value24798");

  // op 33456: get
  status = db->Get(read_options, "key5623", &value);
  if (status.ok()) {
    out << "GET key5623 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33457: get
  status = db->Get(read_options, "key2983", &value);
  if (status.ok()) {
    out << "GET key2983 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33458: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0917", "key5677", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0917, key5677) size=" << scan_result.size() << std::endl;
  }

  // op 33459: delete
  db->Delete(write_options, "key8740");

  // op 33460: get
  status = db->Get(read_options, "key2785", &value);
  if (status.ok()) {
    out << "GET key2785 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33461: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2694", "key11698", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2694, key11698) size=" << scan_result.size() << std::endl;
  }

  // op 33462: put
  db->Put(write_options, "key4594", "value31458");

  // op 33463: get
  status = db->Get(read_options, "key7437", &value);
  if (status.ok()) {
    out << "GET key7437 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33464: delete
  db->Delete(write_options, "key6331");

  // op 33465: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0202", "key6948", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0202, key6948) size=" << scan_result.size() << std::endl;
  }

  // op 33466: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0264", "key4418", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0264, key4418) size=" << scan_result.size() << std::endl;
  }

  // op 33467: put
  db->Put(write_options, "key4732", "value109631");

  // op 33468: get
  status = db->Get(read_options, "key11218", &value);
  if (status.ok()) {
    out << "GET key11218 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33469: delete_range
  status = db->DeleteRange(write_options, "key10009", "key11550");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 33470: delete
  db->Delete(write_options, "key8871");

  // op 33471: delete
  db->Delete(write_options, "key8859");

  // op 33472: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5679", "key8342", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5679, key8342) size=" << scan_result.size() << std::endl;
  }

  // op 33473: put
  db->Put(write_options, "key10771", "value56864");

  // op 33474: get
  status = db->Get(read_options, "key10226", &value);
  if (status.ok()) {
    out << "GET key10226 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33475: delete
  db->Delete(write_options, "key1647");

  // op 33476: put
  db->Put(write_options, "key8912", "value60218");

  // op 33477: get
  status = db->Get(read_options, "key5725", &value);
  if (status.ok()) {
    out << "GET key5725 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33478: delete_range
  status = db->DeleteRange(write_options, "key3889", "key10983");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 33479: put
  db->Put(write_options, "key11672", "value107844");

  // op 33480: put
  db->Put(write_options, "key3679", "value88866");

  // op 33481: put
  db->Put(write_options, "key9871", "value2480");

  // op 33482: delete_range
  status = db->DeleteRange(write_options, "key4202", "key6474");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 33483: put
  db->Put(write_options, "key1693", "value4116");

  // op 33484: get
  status = db->Get(read_options, "key8109", &value);
  if (status.ok()) {
    out << "GET key8109 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33485: delete
  db->Delete(write_options, "key9116");

  // op 33486: delete_range
  status = db->DeleteRange(write_options, "key2473", "key9273");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 33487: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5756", "key8816", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5756, key8816) size=" << scan_result.size() << std::endl;
  }

  // op 33488: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1952", "key10083", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1952, key10083) size=" << scan_result.size() << std::endl;
  }

  // op 33489: get
  status = db->Get(read_options, "key3196", &value);
  if (status.ok()) {
    out << "GET key3196 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33490: put
  db->Put(write_options, "key11356", "value60038");

  // op 33491: delete
  db->Delete(write_options, "key8686");

  // op 33492: put
  db->Put(write_options, "key7353", "value10654");

  // op 33493: put
  db->Put(write_options, "key3555", "value24997");

  // op 33494: delete
  db->Delete(write_options, "key8777");

  // op 33495: delete
  db->Delete(write_options, "key5416");

  // op 33496: get
  status = db->Get(read_options, "key7192", &value);
  if (status.ok()) {
    out << "GET key7192 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33497: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 33498: delete_range
  status = db->DeleteRange(write_options, "key0660", "key10330");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 33499: get
  status = db->Get(read_options, "key2612", &value);
  if (status.ok()) {
    out << "GET key2612 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33500: put
  db->Put(write_options, "key10311", "value35987");

  // op 33501: delete
  db->Delete(write_options, "key6251");

  // op 33502: delete_range
  status = db->DeleteRange(write_options, "key0786", "key2848");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 33503: get
  status = db->Get(read_options, "key2318", &value);
  if (status.ok()) {
    out << "GET key2318 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33504: delete_range
  status = db->DeleteRange(write_options, "key4045", "key9470");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 33505: delete_range
  status = db->DeleteRange(write_options, "key3849", "key8088");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 33506: delete_range
  status = db->DeleteRange(write_options, "key0721", "key3047");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 33507: get
  status = db->Get(read_options, "key7002", &value);
  if (status.ok()) {
    out << "GET key7002 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33508: put
  db->Put(write_options, "key7598", "value45298");

  // op 33509: put
  db->Put(write_options, "key6636", "value95161");

  // op 33510: put
  db->Put(write_options, "key0514", "value111794");

  // op 33511: get
  status = db->Get(read_options, "key11583", &value);
  if (status.ok()) {
    out << "GET key11583 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33512: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8540", "key8808", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8540, key8808) size=" << scan_result.size() << std::endl;
  }

  // op 33513: put
  db->Put(write_options, "key1303", "value56111");

  // op 33514: delete
  db->Delete(write_options, "key8220");

  // op 33515: put
  db->Put(write_options, "key0240", "value6654");

  // op 33516: delete
  db->Delete(write_options, "key4141");

  // op 33517: put
  db->Put(write_options, "key0184", "value50261");

  // op 33518: put
  db->Put(write_options, "key4719", "value106505");

  // op 33519: delete_range
  status = db->DeleteRange(write_options, "key4178", "key6229");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 33520: delete
  db->Delete(write_options, "key1936");

  // op 33521: put
  db->Put(write_options, "key5553", "value27116");

  // op 33522: put
  db->Put(write_options, "key2663", "value25755");

  // op 33523: get
  status = db->Get(read_options, "key1419", &value);
  if (status.ok()) {
    out << "GET key1419 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33524: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2859", "key4634", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2859, key4634) size=" << scan_result.size() << std::endl;
  }

  // op 33525: delete
  db->Delete(write_options, "key4093");

  // op 33526: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1990", "key4062", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1990, key4062) size=" << scan_result.size() << std::endl;
  }

  // op 33527: delete
  db->Delete(write_options, "key8909");

  // op 33528: scan
  scan_result.clear();
  status = db->Scan(read_options, "key11108", "key11113", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key11108, key11113) size=" << scan_result.size() << std::endl;
  }

  // op 33529: put
  db->Put(write_options, "key0546", "value77988");

  // op 33530: get
  status = db->Get(read_options, "key7104", &value);
  if (status.ok()) {
    out << "GET key7104 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33531: get
  status = db->Get(read_options, "key4031", &value);
  if (status.ok()) {
    out << "GET key4031 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33532: get
  status = db->Get(read_options, "key9760", &value);
  if (status.ok()) {
    out << "GET key9760 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33533: put
  db->Put(write_options, "key0667", "value34716");

  // op 33534: delete_range
  status = db->DeleteRange(write_options, "key9580", "key10771");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 33535: put
  db->Put(write_options, "key6090", "value18198");

  // op 33536: get
  status = db->Get(read_options, "key1772", &value);
  if (status.ok()) {
    out << "GET key1772 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33537: put
  db->Put(write_options, "key11269", "value97261");

  // op 33538: get
  status = db->Get(read_options, "key0634", &value);
  if (status.ok()) {
    out << "GET key0634 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33539: put
  db->Put(write_options, "key5018", "value28145");

  // op 33540: put
  db->Put(write_options, "key3121", "value15086");

  // op 33541: put
  db->Put(write_options, "key5099", "value13346");

  // op 33542: put
  db->Put(write_options, "key8541", "value28908");

  // op 33543: put
  db->Put(write_options, "key4688", "value5305");

  // op 33544: get
  status = db->Get(read_options, "key0836", &value);
  if (status.ok()) {
    out << "GET key0836 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33545: get
  status = db->Get(read_options, "key7520", &value);
  if (status.ok()) {
    out << "GET key7520 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33546: put
  db->Put(write_options, "key10206", "value6755");

  // op 33547: get
  status = db->Get(read_options, "key1519", &value);
  if (status.ok()) {
    out << "GET key1519 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33548: get
  status = db->Get(read_options, "key1920", &value);
  if (status.ok()) {
    out << "GET key1920 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33549: get
  status = db->Get(read_options, "key5606", &value);
  if (status.ok()) {
    out << "GET key5606 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33550: get
  status = db->Get(read_options, "key8101", &value);
  if (status.ok()) {
    out << "GET key8101 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33551: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7676", "key8482", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7676, key8482) size=" << scan_result.size() << std::endl;
  }

  // op 33552: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 33553: put
  db->Put(write_options, "key7050", "value13015");

  // op 33554: put
  db->Put(write_options, "key2862", "value38196");

  // op 33555: get
  status = db->Get(read_options, "key5963", &value);
  if (status.ok()) {
    out << "GET key5963 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33556: put
  db->Put(write_options, "key8673", "value49764");

  // op 33557: put
  db->Put(write_options, "key0104", "value61788");

  // op 33558: delete_range
  status = db->DeleteRange(write_options, "key2135", "key3310");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 33559: get
  status = db->Get(read_options, "key4023", &value);
  if (status.ok()) {
    out << "GET key4023 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33560: delete_range
  status = db->DeleteRange(write_options, "key3891", "key6652");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 33561: put
  db->Put(write_options, "key2421", "value107118");

  // op 33562: delete
  db->Delete(write_options, "key8775");

  // op 33563: get
  status = db->Get(read_options, "key2978", &value);
  if (status.ok()) {
    out << "GET key2978 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33564: put
  db->Put(write_options, "key1929", "value27235");

  // op 33565: delete_range
  status = db->DeleteRange(write_options, "key0005", "key4178");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 33566: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0971", "key0989", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0971, key0989) size=" << scan_result.size() << std::endl;
  }

  // op 33567: put
  db->Put(write_options, "key7680", "value61293");

  // op 33568: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 33569: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0309", "key3760", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0309, key3760) size=" << scan_result.size() << std::endl;
  }

  // op 33570: delete
  db->Delete(write_options, "key0666");

  // op 33571: delete_range
  status = db->DeleteRange(write_options, "key10718", "key10992");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 33572: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0489", "key2518", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0489, key2518) size=" << scan_result.size() << std::endl;
  }

  // op 33573: get
  status = db->Get(read_options, "key2623", &value);
  if (status.ok()) {
    out << "GET key2623 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33574: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9464", "key10802", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9464, key10802) size=" << scan_result.size() << std::endl;
  }

  // op 33575: delete
  db->Delete(write_options, "key11124");

  // op 33576: delete
  db->Delete(write_options, "key4329");

  // op 33577: put
  db->Put(write_options, "key9319", "value99618");

  // op 33578: delete
  db->Delete(write_options, "key7123");

  // op 33579: put
  db->Put(write_options, "key7629", "value60123");

  // op 33580: get
  status = db->Get(read_options, "key3538", &value);
  if (status.ok()) {
    out << "GET key3538 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33581: put
  db->Put(write_options, "key10278", "value107412");

  // op 33582: put
  db->Put(write_options, "key2316", "value0513");

  // op 33583: delete_range
  status = db->DeleteRange(write_options, "key7688", "key11288");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 33584: get
  status = db->Get(read_options, "key3093", &value);
  if (status.ok()) {
    out << "GET key3093 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33585: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5215", "key5973", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5215, key5973) size=" << scan_result.size() << std::endl;
  }

  // op 33586: put
  db->Put(write_options, "key9418", "value45379");

  // op 33587: get
  status = db->Get(read_options, "key5626", &value);
  if (status.ok()) {
    out << "GET key5626 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33588: put
  db->Put(write_options, "key8857", "value64096");

  // op 33589: get
  status = db->Get(read_options, "key6889", &value);
  if (status.ok()) {
    out << "GET key6889 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33590: put
  db->Put(write_options, "key6321", "value105302");

  // op 33591: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2512", "key6640", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2512, key6640) size=" << scan_result.size() << std::endl;
  }

  // op 33592: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1848", "key5602", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1848, key5602) size=" << scan_result.size() << std::endl;
  }

  // op 33593: put
  db->Put(write_options, "key4905", "value65596");

  // op 33594: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8246", "key11373", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8246, key11373) size=" << scan_result.size() << std::endl;
  }

  // op 33595: get
  status = db->Get(read_options, "key9793", &value);
  if (status.ok()) {
    out << "GET key9793 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33596: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2333", "key4807", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2333, key4807) size=" << scan_result.size() << std::endl;
  }

  // op 33597: delete_range
  status = db->DeleteRange(write_options, "key0713", "key10913");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 33598: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4602", "key8848", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4602, key8848) size=" << scan_result.size() << std::endl;
  }

  // op 33599: put
  db->Put(write_options, "key9981", "value6144");

  // op 33600: put
  db->Put(write_options, "key5675", "value117152");

  // op 33601: get
  status = db->Get(read_options, "key5195", &value);
  if (status.ok()) {
    out << "GET key5195 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33602: get
  status = db->Get(read_options, "key7357", &value);
  if (status.ok()) {
    out << "GET key7357 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33603: delete_range
  status = db->DeleteRange(write_options, "key1107", "key2175");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 33604: get
  status = db->Get(read_options, "key6827", &value);
  if (status.ok()) {
    out << "GET key6827 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33605: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1169", "key1688", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1169, key1688) size=" << scan_result.size() << std::endl;
  }

  // op 33606: put
  db->Put(write_options, "key4981", "value53324");

  // op 33607: get
  status = db->Get(read_options, "key8263", &value);
  if (status.ok()) {
    out << "GET key8263 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33608: get
  status = db->Get(read_options, "key5426", &value);
  if (status.ok()) {
    out << "GET key5426 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33609: delete_range
  status = db->DeleteRange(write_options, "key6566", "key11020");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 33610: put
  db->Put(write_options, "key10984", "value89937");

  // op 33611: put
  db->Put(write_options, "key9879", "value19771");

  // op 33612: put
  db->Put(write_options, "key8223", "value105909");

  // op 33613: put
  db->Put(write_options, "key2284", "value62765");

  // op 33614: put
  db->Put(write_options, "key0354", "value24360");

  // op 33615: put
  db->Put(write_options, "key2106", "value109630");

  // op 33616: get
  status = db->Get(read_options, "key11697", &value);
  if (status.ok()) {
    out << "GET key11697 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33617: get
  status = db->Get(read_options, "key8810", &value);
  if (status.ok()) {
    out << "GET key8810 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33618: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6175", "key11046", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6175, key11046) size=" << scan_result.size() << std::endl;
  }

  // op 33619: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7081", "key11063", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7081, key11063) size=" << scan_result.size() << std::endl;
  }

  // op 33620: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5861", "key6018", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5861, key6018) size=" << scan_result.size() << std::endl;
  }

  // op 33621: put
  db->Put(write_options, "key7431", "value73569");

  // op 33622: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2471", "key7785", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2471, key7785) size=" << scan_result.size() << std::endl;
  }

  // op 33623: put
  db->Put(write_options, "key6960", "value46346");

  // op 33624: get
  status = db->Get(read_options, "key0320", &value);
  if (status.ok()) {
    out << "GET key0320 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33625: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1409", "key3961", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1409, key3961) size=" << scan_result.size() << std::endl;
  }

  // op 33626: put
  db->Put(write_options, "key3483", "value89355");

  // op 33627: get
  status = db->Get(read_options, "key5584", &value);
  if (status.ok()) {
    out << "GET key5584 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33628: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3484", "key10436", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3484, key10436) size=" << scan_result.size() << std::endl;
  }

  // op 33629: put
  db->Put(write_options, "key3725", "value33118");

  // op 33630: put
  db->Put(write_options, "key2601", "value4612");

  // op 33631: put
  db->Put(write_options, "key2908", "value83099");

  // op 33632: get
  status = db->Get(read_options, "key7492", &value);
  if (status.ok()) {
    out << "GET key7492 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33633: put
  db->Put(write_options, "key5450", "value103306");

  // op 33634: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3683", "key8430", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3683, key8430) size=" << scan_result.size() << std::endl;
  }

  // op 33635: put
  db->Put(write_options, "key2953", "value69331");

  // op 33636: put
  db->Put(write_options, "key3841", "value21654");

  // op 33637: put
  db->Put(write_options, "key2264", "value111028");

  // op 33638: put
  db->Put(write_options, "key6508", "value65594");

  // op 33639: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0947", "key2632", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0947, key2632) size=" << scan_result.size() << std::endl;
  }

  // op 33640: put
  db->Put(write_options, "key3126", "value32814");

  // op 33641: put
  db->Put(write_options, "key6931", "value46431");

  // op 33642: put
  db->Put(write_options, "key0124", "value112139");

  // op 33643: put
  db->Put(write_options, "key1956", "value106718");

  // op 33644: put
  db->Put(write_options, "key8137", "value17566");

  // op 33645: delete_range
  status = db->DeleteRange(write_options, "key5675", "key9873");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 33646: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0297", "key8370", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0297, key8370) size=" << scan_result.size() << std::endl;
  }

  // op 33647: get
  status = db->Get(read_options, "key2693", &value);
  if (status.ok()) {
    out << "GET key2693 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33648: put
  db->Put(write_options, "key9868", "value85911");

  // op 33649: put
  db->Put(write_options, "key4793", "value27698");

  // op 33650: put
  db->Put(write_options, "key3679", "value58428");

  // op 33651: delete
  db->Delete(write_options, "key2269");

  // op 33652: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6391", "key10253", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6391, key10253) size=" << scan_result.size() << std::endl;
  }

  // op 33653: put
  db->Put(write_options, "key3654", "value10795");

  // op 33654: get
  status = db->Get(read_options, "key9153", &value);
  if (status.ok()) {
    out << "GET key9153 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33655: put
  db->Put(write_options, "key6023", "value77674");

  // op 33656: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2874", "key9734", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2874, key9734) size=" << scan_result.size() << std::endl;
  }

  // op 33657: get
  status = db->Get(read_options, "key9404", &value);
  if (status.ok()) {
    out << "GET key9404 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33658: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 33659: put
  db->Put(write_options, "key11494", "value57750");

  // op 33660: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5250", "key6602", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5250, key6602) size=" << scan_result.size() << std::endl;
  }

  // op 33661: get
  status = db->Get(read_options, "key11419", &value);
  if (status.ok()) {
    out << "GET key11419 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33662: delete_range
  status = db->DeleteRange(write_options, "key1568", "key7454");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 33663: get
  status = db->Get(read_options, "key11629", &value);
  if (status.ok()) {
    out << "GET key11629 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33664: put
  db->Put(write_options, "key2067", "value3525");

  // op 33665: get
  status = db->Get(read_options, "key9947", &value);
  if (status.ok()) {
    out << "GET key9947 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33666: get
  status = db->Get(read_options, "key3558", &value);
  if (status.ok()) {
    out << "GET key3558 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33667: delete
  db->Delete(write_options, "key7826");

  // op 33668: put
  db->Put(write_options, "key5248", "value15518");

  // op 33669: put
  db->Put(write_options, "key9307", "value20039");

  // op 33670: put
  db->Put(write_options, "key9080", "value15251");

  // op 33671: get
  status = db->Get(read_options, "key5034", &value);
  if (status.ok()) {
    out << "GET key5034 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33672: put
  db->Put(write_options, "key7372", "value67101");

  // op 33673: put
  db->Put(write_options, "key7360", "value103152");

  // op 33674: get
  status = db->Get(read_options, "key0171", &value);
  if (status.ok()) {
    out << "GET key0171 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33675: put
  db->Put(write_options, "key5326", "value111809");

  // op 33676: put
  db->Put(write_options, "key1017", "value86955");

  // op 33677: put
  db->Put(write_options, "key11226", "value72024");

  // op 33678: put
  db->Put(write_options, "key7077", "value15639");

  // op 33679: put
  db->Put(write_options, "key1008", "value18609");

  // op 33680: delete_range
  status = db->DeleteRange(write_options, "key4486", "key8561");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 33681: delete_range
  status = db->DeleteRange(write_options, "key5354", "key10679");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 33682: put
  db->Put(write_options, "key1636", "value12927");

  // op 33683: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 33684: put
  db->Put(write_options, "key3842", "value34743");

  // op 33685: get
  status = db->Get(read_options, "key8022", &value);
  if (status.ok()) {
    out << "GET key8022 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33686: delete_range
  status = db->DeleteRange(write_options, "key8291", "key9357");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 33687: get
  status = db->Get(read_options, "key11290", &value);
  if (status.ok()) {
    out << "GET key11290 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33688: delete
  db->Delete(write_options, "key5221");

  // op 33689: get
  status = db->Get(read_options, "key4248", &value);
  if (status.ok()) {
    out << "GET key4248 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33690: get
  status = db->Get(read_options, "key5828", &value);
  if (status.ok()) {
    out << "GET key5828 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33691: get
  status = db->Get(read_options, "key3207", &value);
  if (status.ok()) {
    out << "GET key3207 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33692: delete
  db->Delete(write_options, "key0148");

  // op 33693: put
  db->Put(write_options, "key4192", "value59504");

  // op 33694: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7925", "key11378", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7925, key11378) size=" << scan_result.size() << std::endl;
  }

  // op 33695: get
  status = db->Get(read_options, "key10133", &value);
  if (status.ok()) {
    out << "GET key10133 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33696: delete_range
  status = db->DeleteRange(write_options, "key5536", "key11444");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 33697: delete
  db->Delete(write_options, "key1459");

  // op 33698: delete_range
  status = db->DeleteRange(write_options, "key4548", "key7029");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 33699: get
  status = db->Get(read_options, "key8892", &value);
  if (status.ok()) {
    out << "GET key8892 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33700: put
  db->Put(write_options, "key2520", "value108177");

  // op 33701: get
  status = db->Get(read_options, "key3216", &value);
  if (status.ok()) {
    out << "GET key3216 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33702: get
  status = db->Get(read_options, "key10848", &value);
  if (status.ok()) {
    out << "GET key10848 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33703: put
  db->Put(write_options, "key1096", "value54994");

  // op 33704: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 33705: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7333", "key8485", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7333, key8485) size=" << scan_result.size() << std::endl;
  }

  // op 33706: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 33707: put
  db->Put(write_options, "key4592", "value68303");

  // op 33708: get
  status = db->Get(read_options, "key3891", &value);
  if (status.ok()) {
    out << "GET key3891 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33709: delete_range
  status = db->DeleteRange(write_options, "key7852", "key9206");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 33710: get
  status = db->Get(read_options, "key5938", &value);
  if (status.ok()) {
    out << "GET key5938 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33711: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9071", "key10790", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9071, key10790) size=" << scan_result.size() << std::endl;
  }

  // op 33712: delete_range
  status = db->DeleteRange(write_options, "key1911", "key10064");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 33713: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3561", "key11289", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3561, key11289) size=" << scan_result.size() << std::endl;
  }

  // op 33714: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4031", "key7354", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4031, key7354) size=" << scan_result.size() << std::endl;
  }

  // op 33715: put
  db->Put(write_options, "key0367", "value114640");

  // op 33716: get
  status = db->Get(read_options, "key9699", &value);
  if (status.ok()) {
    out << "GET key9699 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33717: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 33718: get
  status = db->Get(read_options, "key10010", &value);
  if (status.ok()) {
    out << "GET key10010 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33719: put
  db->Put(write_options, "key0780", "value68933");

  // op 33720: put
  db->Put(write_options, "key2244", "value11768");

  // op 33721: put
  db->Put(write_options, "key7090", "value93444");

  // op 33722: put
  db->Put(write_options, "key9606", "value115399");

  // op 33723: put
  db->Put(write_options, "key4390", "value107095");

  // op 33724: get
  status = db->Get(read_options, "key1180", &value);
  if (status.ok()) {
    out << "GET key1180 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33725: delete_range
  status = db->DeleteRange(write_options, "key2450", "key7985");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 33726: delete
  db->Delete(write_options, "key3268");

  // op 33727: put
  db->Put(write_options, "key6038", "value10743");

  // op 33728: put
  db->Put(write_options, "key4912", "value89055");

  // op 33729: put
  db->Put(write_options, "key5626", "value76802");

  // op 33730: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0109", "key2698", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0109, key2698) size=" << scan_result.size() << std::endl;
  }

  // op 33731: put
  db->Put(write_options, "key6121", "value48862");

  // op 33732: delete
  db->Delete(write_options, "key2241");

  // op 33733: put
  db->Put(write_options, "key7388", "value87162");

  // op 33734: delete
  db->Delete(write_options, "key2760");

  // op 33735: put
  db->Put(write_options, "key8881", "value24218");

  // op 33736: delete
  db->Delete(write_options, "key4161");

  // op 33737: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4464", "key7338", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4464, key7338) size=" << scan_result.size() << std::endl;
  }

  // op 33738: get
  status = db->Get(read_options, "key1112", &value);
  if (status.ok()) {
    out << "GET key1112 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33739: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5705", "key6215", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5705, key6215) size=" << scan_result.size() << std::endl;
  }

  // op 33740: put
  db->Put(write_options, "key4282", "value104332");

  // op 33741: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0923", "key0997", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0923, key0997) size=" << scan_result.size() << std::endl;
  }

  // op 33742: put
  db->Put(write_options, "key7781", "value31479");

  // op 33743: put
  db->Put(write_options, "key6170", "value99002");

  // op 33744: put
  db->Put(write_options, "key1313", "value6019");

  // op 33745: delete
  db->Delete(write_options, "key4956");

  // op 33746: put
  db->Put(write_options, "key5499", "value18318");

  // op 33747: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1677", "key5448", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1677, key5448) size=" << scan_result.size() << std::endl;
  }

  // op 33748: put
  db->Put(write_options, "key2391", "value38555");

  // op 33749: put
  db->Put(write_options, "key9056", "value82809");

  // op 33750: put
  db->Put(write_options, "key1051", "value84012");

  // op 33751: get
  status = db->Get(read_options, "key11160", &value);
  if (status.ok()) {
    out << "GET key11160 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33752: delete
  db->Delete(write_options, "key0017");

  // op 33753: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1856", "key6968", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1856, key6968) size=" << scan_result.size() << std::endl;
  }

  // op 33754: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1768", "key6404", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1768, key6404) size=" << scan_result.size() << std::endl;
  }

  // op 33755: get
  status = db->Get(read_options, "key11222", &value);
  if (status.ok()) {
    out << "GET key11222 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33756: put
  db->Put(write_options, "key8795", "value36443");

  // op 33757: put
  db->Put(write_options, "key10013", "value94891");

  // op 33758: put
  db->Put(write_options, "key9543", "value41845");

  // op 33759: get
  status = db->Get(read_options, "key6249", &value);
  if (status.ok()) {
    out << "GET key6249 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33760: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5831", "key7300", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5831, key7300) size=" << scan_result.size() << std::endl;
  }

  // op 33761: get
  status = db->Get(read_options, "key8667", &value);
  if (status.ok()) {
    out << "GET key8667 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33762: put
  db->Put(write_options, "key6463", "value41030");

  // op 33763: get
  status = db->Get(read_options, "key3930", &value);
  if (status.ok()) {
    out << "GET key3930 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33764: get
  status = db->Get(read_options, "key1705", &value);
  if (status.ok()) {
    out << "GET key1705 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33765: put
  db->Put(write_options, "key0499", "value70816");

  // op 33766: put
  db->Put(write_options, "key11082", "value30595");

  // op 33767: get
  status = db->Get(read_options, "key7581", &value);
  if (status.ok()) {
    out << "GET key7581 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33768: put
  db->Put(write_options, "key1392", "value76613");

  // op 33769: get
  status = db->Get(read_options, "key2808", &value);
  if (status.ok()) {
    out << "GET key2808 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33770: put
  db->Put(write_options, "key6181", "value117820");

  // op 33771: put
  db->Put(write_options, "key3870", "value64367");

  // op 33772: delete
  db->Delete(write_options, "key2932");

  // op 33773: put
  db->Put(write_options, "key3369", "value18158");

  // op 33774: put
  db->Put(write_options, "key8566", "value68326");

  // op 33775: put
  db->Put(write_options, "key0777", "value111482");

  // op 33776: get
  status = db->Get(read_options, "key3269", &value);
  if (status.ok()) {
    out << "GET key3269 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33777: put
  db->Put(write_options, "key1669", "value12678");

  // op 33778: get
  status = db->Get(read_options, "key5408", &value);
  if (status.ok()) {
    out << "GET key5408 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33779: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 33780: delete_range
  status = db->DeleteRange(write_options, "key2273", "key11154");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 33781: put
  db->Put(write_options, "key7853", "value68878");

  // op 33782: get
  status = db->Get(read_options, "key7260", &value);
  if (status.ok()) {
    out << "GET key7260 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33783: put
  db->Put(write_options, "key7642", "value96616");

  // op 33784: put
  db->Put(write_options, "key6754", "value1072");

  // op 33785: put
  db->Put(write_options, "key6741", "value100497");

  // op 33786: put
  db->Put(write_options, "key5069", "value30345");

  // op 33787: put
  db->Put(write_options, "key9551", "value29555");

  // op 33788: put
  db->Put(write_options, "key8656", "value110634");

  // op 33789: get
  status = db->Get(read_options, "key5125", &value);
  if (status.ok()) {
    out << "GET key5125 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33790: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 33791: get
  status = db->Get(read_options, "key8160", &value);
  if (status.ok()) {
    out << "GET key8160 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33792: delete
  db->Delete(write_options, "key6294");

  // op 33793: delete
  db->Delete(write_options, "key11400");

  // op 33794: put
  db->Put(write_options, "key5201", "value110104");

  // op 33795: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7878", "key10202", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7878, key10202) size=" << scan_result.size() << std::endl;
  }

  // op 33796: put
  db->Put(write_options, "key7553", "value113561");

  // op 33797: put
  db->Put(write_options, "key6453", "value111492");

  // op 33798: delete_range
  status = db->DeleteRange(write_options, "key2468", "key10722");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 33799: put
  db->Put(write_options, "key3148", "value32218");

  // op 33800: put
  db->Put(write_options, "key0457", "value22665");

  // op 33801: get
  status = db->Get(read_options, "key2084", &value);
  if (status.ok()) {
    out << "GET key2084 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33802: get
  status = db->Get(read_options, "key9458", &value);
  if (status.ok()) {
    out << "GET key9458 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33803: delete_range
  status = db->DeleteRange(write_options, "key8946", "key9886");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 33804: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1207", "key3197", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1207, key3197) size=" << scan_result.size() << std::endl;
  }

  // op 33805: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3106", "key11622", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3106, key11622) size=" << scan_result.size() << std::endl;
  }

  // op 33806: delete_range
  status = db->DeleteRange(write_options, "key0002", "key0919");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 33807: delete_range
  status = db->DeleteRange(write_options, "key4402", "key8550");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 33808: put
  db->Put(write_options, "key9589", "value48132");

  // op 33809: put
  db->Put(write_options, "key5445", "value95321");

  // op 33810: get
  status = db->Get(read_options, "key4026", &value);
  if (status.ok()) {
    out << "GET key4026 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33811: put
  db->Put(write_options, "key5295", "value100384");

  // op 33812: get
  status = db->Get(read_options, "key6665", &value);
  if (status.ok()) {
    out << "GET key6665 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33813: get
  status = db->Get(read_options, "key4565", &value);
  if (status.ok()) {
    out << "GET key4565 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33814: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3867", "key4618", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3867, key4618) size=" << scan_result.size() << std::endl;
  }

  // op 33815: delete_range
  status = db->DeleteRange(write_options, "key4221", "key8405");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 33816: get
  status = db->Get(read_options, "key4597", &value);
  if (status.ok()) {
    out << "GET key4597 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33817: put
  db->Put(write_options, "key2976", "value3569");

  // op 33818: put
  db->Put(write_options, "key11539", "value26628");

  // op 33819: put
  db->Put(write_options, "key4584", "value64750");

  // op 33820: put
  db->Put(write_options, "key1073", "value16893");

  // op 33821: delete
  db->Delete(write_options, "key9754");

  // op 33822: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 33823: put
  db->Put(write_options, "key11516", "value36705");

  // op 33824: delete
  db->Delete(write_options, "key11494");

  // op 33825: put
  db->Put(write_options, "key8312", "value83618");

  // op 33826: delete
  db->Delete(write_options, "key9716");

  // op 33827: put
  db->Put(write_options, "key3113", "value67427");

  // op 33828: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 33829: delete_range
  status = db->DeleteRange(write_options, "key6002", "key6662");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 33830: put
  db->Put(write_options, "key0907", "value24459");

  // op 33831: put
  db->Put(write_options, "key4006", "value52425");

  // op 33832: delete_range
  status = db->DeleteRange(write_options, "key1146", "key6312");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 33833: get
  status = db->Get(read_options, "key4951", &value);
  if (status.ok()) {
    out << "GET key4951 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33834: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 33835: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 33836: delete
  db->Delete(write_options, "key7426");

  // op 33837: put
  db->Put(write_options, "key11140", "value90985");

  // op 33838: delete_range
  status = db->DeleteRange(write_options, "key5700", "key7451");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 33839: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4330", "key6902", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4330, key6902) size=" << scan_result.size() << std::endl;
  }

  // op 33840: delete
  db->Delete(write_options, "key4489");

  // op 33841: get
  status = db->Get(read_options, "key3462", &value);
  if (status.ok()) {
    out << "GET key3462 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33842: delete_range
  status = db->DeleteRange(write_options, "key1397", "key6844");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 33843: put
  db->Put(write_options, "key4640", "value113103");

  // op 33844: get
  status = db->Get(read_options, "key0347", &value);
  if (status.ok()) {
    out << "GET key0347 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33845: put
  db->Put(write_options, "key10955", "value5964");

  // op 33846: get
  status = db->Get(read_options, "key6235", &value);
  if (status.ok()) {
    out << "GET key6235 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33847: put
  db->Put(write_options, "key10338", "value53117");

  // op 33848: put
  db->Put(write_options, "key10500", "value42018");

  // op 33849: put
  db->Put(write_options, "key0496", "value110185");

  // op 33850: delete
  db->Delete(write_options, "key1468");

  // op 33851: put
  db->Put(write_options, "key2966", "value12838");

  // op 33852: put
  db->Put(write_options, "key6775", "value31785");

  // op 33853: put
  db->Put(write_options, "key8197", "value84033");

  // op 33854: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2429", "key6611", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2429, key6611) size=" << scan_result.size() << std::endl;
  }

  // op 33855: get
  status = db->Get(read_options, "key11579", &value);
  if (status.ok()) {
    out << "GET key11579 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33856: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5208", "key9812", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5208, key9812) size=" << scan_result.size() << std::endl;
  }

  // op 33857: get
  status = db->Get(read_options, "key3697", &value);
  if (status.ok()) {
    out << "GET key3697 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33858: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5047", "key10314", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5047, key10314) size=" << scan_result.size() << std::endl;
  }

  // op 33859: get
  status = db->Get(read_options, "key6013", &value);
  if (status.ok()) {
    out << "GET key6013 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33860: put
  db->Put(write_options, "key8666", "value42610");

  // op 33861: delete_range
  status = db->DeleteRange(write_options, "key5436", "key10663");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 33862: delete_range
  status = db->DeleteRange(write_options, "key0527", "key8797");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 33863: get
  status = db->Get(read_options, "key11165", &value);
  if (status.ok()) {
    out << "GET key11165 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33864: put
  db->Put(write_options, "key7676", "value15329");

  // op 33865: delete
  db->Delete(write_options, "key3992");

  // op 33866: put
  db->Put(write_options, "key0530", "value70123");

  // op 33867: get
  status = db->Get(read_options, "key4384", &value);
  if (status.ok()) {
    out << "GET key4384 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33868: delete
  db->Delete(write_options, "key3967");

  // op 33869: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1191", "key7626", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1191, key7626) size=" << scan_result.size() << std::endl;
  }

  // op 33870: get
  status = db->Get(read_options, "key4795", &value);
  if (status.ok()) {
    out << "GET key4795 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33871: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1202", "key7252", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1202, key7252) size=" << scan_result.size() << std::endl;
  }

  // op 33872: put
  db->Put(write_options, "key0167", "value18123");

  // op 33873: delete
  db->Delete(write_options, "key6049");

  // op 33874: put
  db->Put(write_options, "key6923", "value19377");

  // op 33875: put
  db->Put(write_options, "key8892", "value14654");

  // op 33876: put
  db->Put(write_options, "key9824", "value16203");

  // op 33877: get
  status = db->Get(read_options, "key9045", &value);
  if (status.ok()) {
    out << "GET key9045 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33878: delete
  db->Delete(write_options, "key1486");

  // op 33879: put
  db->Put(write_options, "key7132", "value67289");

  // op 33880: put
  db->Put(write_options, "key8988", "value107708");

  // op 33881: get
  status = db->Get(read_options, "key0310", &value);
  if (status.ok()) {
    out << "GET key0310 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33882: put
  db->Put(write_options, "key7350", "value82353");

  // op 33883: put
  db->Put(write_options, "key2336", "value82875");

  // op 33884: delete
  db->Delete(write_options, "key0304");

  // op 33885: put
  db->Put(write_options, "key11040", "value9291");

  // op 33886: get
  status = db->Get(read_options, "key9808", &value);
  if (status.ok()) {
    out << "GET key9808 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33887: delete
  db->Delete(write_options, "key3046");

  // op 33888: put
  db->Put(write_options, "key3915", "value12075");

  // op 33889: delete_range
  status = db->DeleteRange(write_options, "key1533", "key5930");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 33890: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1352", "key6289", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1352, key6289) size=" << scan_result.size() << std::endl;
  }

  // op 33891: get
  status = db->Get(read_options, "key11081", &value);
  if (status.ok()) {
    out << "GET key11081 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33892: delete
  db->Delete(write_options, "key0984");

  // op 33893: put
  db->Put(write_options, "key8469", "value47969");

  // op 33894: put
  db->Put(write_options, "key3808", "value54099");

  // op 33895: put
  db->Put(write_options, "key8319", "value64711");

  // op 33896: delete
  db->Delete(write_options, "key5676");

  // op 33897: put
  db->Put(write_options, "key6893", "value104137");

  // op 33898: put
  db->Put(write_options, "key11661", "value29787");

  // op 33899: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 33900: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 33901: put
  db->Put(write_options, "key10647", "value23153");

  // op 33902: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3861", "key11042", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3861, key11042) size=" << scan_result.size() << std::endl;
  }

  // op 33903: put
  db->Put(write_options, "key3219", "value43124");

  // op 33904: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6030", "key7601", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6030, key7601) size=" << scan_result.size() << std::endl;
  }

  // op 33905: delete
  db->Delete(write_options, "key0455");

  // op 33906: get
  status = db->Get(read_options, "key6041", &value);
  if (status.ok()) {
    out << "GET key6041 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33907: put
  db->Put(write_options, "key0846", "value45680");

  // op 33908: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 33909: put
  db->Put(write_options, "key5093", "value67049");

  // op 33910: get
  status = db->Get(read_options, "key5681", &value);
  if (status.ok()) {
    out << "GET key5681 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33911: put
  db->Put(write_options, "key1534", "value14873");

  // op 33912: delete
  db->Delete(write_options, "key4613");

  // op 33913: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1776", "key11256", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1776, key11256) size=" << scan_result.size() << std::endl;
  }

  // op 33914: get
  status = db->Get(read_options, "key2004", &value);
  if (status.ok()) {
    out << "GET key2004 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33915: put
  db->Put(write_options, "key9369", "value29077");

  // op 33916: put
  db->Put(write_options, "key0499", "value0871");

  // op 33917: put
  db->Put(write_options, "key11148", "value80539");

  // op 33918: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6006", "key10505", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6006, key10505) size=" << scan_result.size() << std::endl;
  }

  // op 33919: get
  status = db->Get(read_options, "key3390", &value);
  if (status.ok()) {
    out << "GET key3390 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33920: get
  status = db->Get(read_options, "key1785", &value);
  if (status.ok()) {
    out << "GET key1785 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33921: delete_range
  status = db->DeleteRange(write_options, "key5168", "key9788");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 33922: delete_range
  status = db->DeleteRange(write_options, "key3873", "key9852");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 33923: delete
  db->Delete(write_options, "key4563");

  // op 33924: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1286", "key9532", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1286, key9532) size=" << scan_result.size() << std::endl;
  }

  // op 33925: get
  status = db->Get(read_options, "key2749", &value);
  if (status.ok()) {
    out << "GET key2749 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33926: get
  status = db->Get(read_options, "key5825", &value);
  if (status.ok()) {
    out << "GET key5825 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33927: put
  db->Put(write_options, "key7177", "value8077");

  // op 33928: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4435", "key9448", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4435, key9448) size=" << scan_result.size() << std::endl;
  }

  // op 33929: put
  db->Put(write_options, "key5403", "value60779");

  // op 33930: get
  status = db->Get(read_options, "key9760", &value);
  if (status.ok()) {
    out << "GET key9760 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33931: put
  db->Put(write_options, "key3828", "value5250");

  // op 33932: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0787", "key10101", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0787, key10101) size=" << scan_result.size() << std::endl;
  }

  // op 33933: put
  db->Put(write_options, "key5195", "value95568");

  // op 33934: get
  status = db->Get(read_options, "key8475", &value);
  if (status.ok()) {
    out << "GET key8475 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33935: get
  status = db->Get(read_options, "key5907", &value);
  if (status.ok()) {
    out << "GET key5907 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33936: delete
  db->Delete(write_options, "key5617");

  // op 33937: put
  db->Put(write_options, "key11555", "value77629");

  // op 33938: put
  db->Put(write_options, "key7363", "value15801");

  // op 33939: delete_range
  status = db->DeleteRange(write_options, "key7071", "key11328");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 33940: get
  status = db->Get(read_options, "key2496", &value);
  if (status.ok()) {
    out << "GET key2496 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33941: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 33942: delete
  db->Delete(write_options, "key4060");

  // op 33943: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6440", "key9978", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6440, key9978) size=" << scan_result.size() << std::endl;
  }

  // op 33944: put
  db->Put(write_options, "key8747", "value46771");

  // op 33945: put
  db->Put(write_options, "key4066", "value58875");

  // op 33946: put
  db->Put(write_options, "key5039", "value78708");

  // op 33947: put
  db->Put(write_options, "key9688", "value112360");

  // op 33948: delete
  db->Delete(write_options, "key7718");

  // op 33949: delete_range
  status = db->DeleteRange(write_options, "key1492", "key3446");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 33950: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3712", "key7072", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3712, key7072) size=" << scan_result.size() << std::endl;
  }

  // op 33951: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0664", "key9625", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0664, key9625) size=" << scan_result.size() << std::endl;
  }

  // op 33952: delete_range
  status = db->DeleteRange(write_options, "key0838", "key4901");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 33953: get
  status = db->Get(read_options, "key10478", &value);
  if (status.ok()) {
    out << "GET key10478 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33954: get
  status = db->Get(read_options, "key9411", &value);
  if (status.ok()) {
    out << "GET key9411 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33955: put
  db->Put(write_options, "key9933", "value74560");

  // op 33956: delete_range
  status = db->DeleteRange(write_options, "key4706", "key5719");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 33957: put
  db->Put(write_options, "key5543", "value64857");

  // op 33958: put
  db->Put(write_options, "key1363", "value101788");

  // op 33959: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 33960: delete_range
  status = db->DeleteRange(write_options, "key0467", "key0991");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 33961: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1035", "key11282", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1035, key11282) size=" << scan_result.size() << std::endl;
  }

  // op 33962: put
  db->Put(write_options, "key8717", "value58052");

  // op 33963: put
  db->Put(write_options, "key10116", "value32731");

  // op 33964: put
  db->Put(write_options, "key1791", "value38988");

  // op 33965: put
  db->Put(write_options, "key0125", "value61317");

  // op 33966: get
  status = db->Get(read_options, "key10373", &value);
  if (status.ok()) {
    out << "GET key10373 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33967: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 33968: get
  status = db->Get(read_options, "key11108", &value);
  if (status.ok()) {
    out << "GET key11108 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33969: put
  db->Put(write_options, "key7030", "value49589");

  // op 33970: get
  status = db->Get(read_options, "key0364", &value);
  if (status.ok()) {
    out << "GET key0364 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33971: delete_range
  status = db->DeleteRange(write_options, "key7536", "key8883");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 33972: put
  db->Put(write_options, "key5558", "value54000");

  // op 33973: get
  status = db->Get(read_options, "key8071", &value);
  if (status.ok()) {
    out << "GET key8071 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33974: put
  db->Put(write_options, "key2992", "value63269");

  // op 33975: put
  db->Put(write_options, "key0742", "value76217");

  // op 33976: delete_range
  status = db->DeleteRange(write_options, "key2394", "key9058");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 33977: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 33978: get
  status = db->Get(read_options, "key1050", &value);
  if (status.ok()) {
    out << "GET key1050 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33979: put
  db->Put(write_options, "key0511", "value18963");

  // op 33980: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2775", "key9146", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2775, key9146) size=" << scan_result.size() << std::endl;
  }

  // op 33981: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0170", "key6842", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0170, key6842) size=" << scan_result.size() << std::endl;
  }

  // op 33982: put
  db->Put(write_options, "key3554", "value51888");

  // op 33983: get
  status = db->Get(read_options, "key11725", &value);
  if (status.ok()) {
    out << "GET key11725 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33984: get
  status = db->Get(read_options, "key5048", &value);
  if (status.ok()) {
    out << "GET key5048 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33985: delete
  db->Delete(write_options, "key11256");

  // op 33986: get
  status = db->Get(read_options, "key10010", &value);
  if (status.ok()) {
    out << "GET key10010 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33987: get
  status = db->Get(read_options, "key8572", &value);
  if (status.ok()) {
    out << "GET key8572 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33988: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5565", "key9300", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5565, key9300) size=" << scan_result.size() << std::endl;
  }

  // op 33989: put
  db->Put(write_options, "key9156", "value44842");

  // op 33990: put
  db->Put(write_options, "key10860", "value70376");

  // op 33991: get
  status = db->Get(read_options, "key1885", &value);
  if (status.ok()) {
    out << "GET key1885 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33992: delete_range
  status = db->DeleteRange(write_options, "key2190", "key3755");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 33993: get
  status = db->Get(read_options, "key1587", &value);
  if (status.ok()) {
    out << "GET key1587 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 33994: put
  db->Put(write_options, "key5579", "value90713");

  // op 33995: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0113", "key10290", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0113, key10290) size=" << scan_result.size() << std::endl;
  }

  // op 33996: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0346", "key6435", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0346, key6435) size=" << scan_result.size() << std::endl;
  }

  // op 33997: delete_range
  status = db->DeleteRange(write_options, "key3443", "key10300");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 33998: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0876", "key3716", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0876, key3716) size=" << scan_result.size() << std::endl;
  }

  // op 33999: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3850", "key7698", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3850, key7698) size=" << scan_result.size() << std::endl;
  }

  // op 34000: put
  db->Put(write_options, "key11330", "value61809");

  // op 34001: put
  db->Put(write_options, "key0067", "value113708");

  // op 34002: put
  db->Put(write_options, "key8537", "value47701");

  // op 34003: put
  db->Put(write_options, "key5111", "value31907");

  // op 34004: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0284", "key1648", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0284, key1648) size=" << scan_result.size() << std::endl;
  }

  // op 34005: delete
  db->Delete(write_options, "key0875");

  // op 34006: delete
  db->Delete(write_options, "key1297");

  // op 34007: put
  db->Put(write_options, "key1010", "value40314");

  // op 34008: get
  status = db->Get(read_options, "key2739", &value);
  if (status.ok()) {
    out << "GET key2739 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34009: put
  db->Put(write_options, "key3025", "value78719");

  // op 34010: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9169", "key11227", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9169, key11227) size=" << scan_result.size() << std::endl;
  }

  // op 34011: get
  status = db->Get(read_options, "key4693", &value);
  if (status.ok()) {
    out << "GET key4693 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34012: put
  db->Put(write_options, "key3968", "value88863");

  // op 34013: delete_range
  status = db->DeleteRange(write_options, "key5077", "key10434");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 34014: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4019", "key5114", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4019, key5114) size=" << scan_result.size() << std::endl;
  }

  // op 34015: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8396", "key9340", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8396, key9340) size=" << scan_result.size() << std::endl;
  }

  // op 34016: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8210", "key10652", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8210, key10652) size=" << scan_result.size() << std::endl;
  }

  // op 34017: put
  db->Put(write_options, "key8976", "value0718");

  // op 34018: get
  status = db->Get(read_options, "key5100", &value);
  if (status.ok()) {
    out << "GET key5100 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34019: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2362", "key8232", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2362, key8232) size=" << scan_result.size() << std::endl;
  }

  // op 34020: put
  db->Put(write_options, "key1210", "value25419");

  // op 34021: delete
  db->Delete(write_options, "key6138");

  // op 34022: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0962", "key8474", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0962, key8474) size=" << scan_result.size() << std::endl;
  }

  // op 34023: put
  db->Put(write_options, "key8780", "value52608");

  // op 34024: delete
  db->Delete(write_options, "key8471");

  // op 34025: get
  status = db->Get(read_options, "key9096", &value);
  if (status.ok()) {
    out << "GET key9096 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34026: delete_range
  status = db->DeleteRange(write_options, "key5523", "key6373");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 34027: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 34028: put
  db->Put(write_options, "key10222", "value33790");

  // op 34029: put
  db->Put(write_options, "key5567", "value80409");

  // op 34030: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7015", "key9302", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7015, key9302) size=" << scan_result.size() << std::endl;
  }

  // op 34031: delete
  db->Delete(write_options, "key1084");

  // op 34032: get
  status = db->Get(read_options, "key4717", &value);
  if (status.ok()) {
    out << "GET key4717 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34033: put
  db->Put(write_options, "key3044", "value70830");

  // op 34034: put
  db->Put(write_options, "key9251", "value11534");

  // op 34035: get
  status = db->Get(read_options, "key2474", &value);
  if (status.ok()) {
    out << "GET key2474 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34036: get
  status = db->Get(read_options, "key6957", &value);
  if (status.ok()) {
    out << "GET key6957 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34037: delete_range
  status = db->DeleteRange(write_options, "key3522", "key9992");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 34038: get
  status = db->Get(read_options, "key8694", &value);
  if (status.ok()) {
    out << "GET key8694 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34039: get
  status = db->Get(read_options, "key10345", &value);
  if (status.ok()) {
    out << "GET key10345 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34040: delete
  db->Delete(write_options, "key0369");

  // op 34041: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 34042: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9359", "key10535", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9359, key10535) size=" << scan_result.size() << std::endl;
  }

  // op 34043: put
  db->Put(write_options, "key11652", "value49461");

  // op 34044: put
  db->Put(write_options, "key6887", "value106716");

  // op 34045: get
  status = db->Get(read_options, "key7759", &value);
  if (status.ok()) {
    out << "GET key7759 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34046: put
  db->Put(write_options, "key7276", "value8125");

  // op 34047: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 34048: put
  db->Put(write_options, "key8493", "value93124");

  // op 34049: put
  db->Put(write_options, "key10724", "value3839");

  // op 34050: put
  db->Put(write_options, "key10128", "value31878");

  // op 34051: put
  db->Put(write_options, "key4999", "value28294");

  // op 34052: put
  db->Put(write_options, "key2108", "value98907");

  // op 34053: put
  db->Put(write_options, "key4163", "value61329");

  // op 34054: put
  db->Put(write_options, "key3593", "value84899");

  // op 34055: delete_range
  status = db->DeleteRange(write_options, "key3710", "key11289");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 34056: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 34057: put
  db->Put(write_options, "key3048", "value116485");

  // op 34058: put
  db->Put(write_options, "key0934", "value117351");

  // op 34059: put
  db->Put(write_options, "key9648", "value62416");

  // op 34060: put
  db->Put(write_options, "key9795", "value109804");

  // op 34061: put
  db->Put(write_options, "key8333", "value1222");

  // op 34062: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2832", "key5660", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2832, key5660) size=" << scan_result.size() << std::endl;
  }

  // op 34063: delete_range
  status = db->DeleteRange(write_options, "key0150", "key10807");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 34064: put
  db->Put(write_options, "key5137", "value21124");

  // op 34065: put
  db->Put(write_options, "key9568", "value71025");

  // op 34066: put
  db->Put(write_options, "key2991", "value55325");

  // op 34067: delete
  db->Delete(write_options, "key3992");

  // op 34068: put
  db->Put(write_options, "key1188", "value82216");

  // op 34069: delete_range
  status = db->DeleteRange(write_options, "key4663", "key4979");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 34070: delete_range
  status = db->DeleteRange(write_options, "key3883", "key10066");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 34071: put
  db->Put(write_options, "key7611", "value111383");

  // op 34072: put
  db->Put(write_options, "key0551", "value106795");

  // op 34073: delete
  db->Delete(write_options, "key1903");

  // op 34074: put
  db->Put(write_options, "key3851", "value42898");

  // op 34075: delete_range
  status = db->DeleteRange(write_options, "key6684", "key9761");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 34076: put
  db->Put(write_options, "key2922", "value22270");

  // op 34077: delete_range
  status = db->DeleteRange(write_options, "key4409", "key10785");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 34078: put
  db->Put(write_options, "key5922", "value100614");

  // op 34079: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0601", "key4046", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0601, key4046) size=" << scan_result.size() << std::endl;
  }

  // op 34080: delete_range
  status = db->DeleteRange(write_options, "key8349", "key10050");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 34081: put
  db->Put(write_options, "key7680", "value81811");

  // op 34082: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6642", "key7734", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6642, key7734) size=" << scan_result.size() << std::endl;
  }

  // op 34083: delete
  db->Delete(write_options, "key9945");

  // op 34084: delete
  db->Delete(write_options, "key0303");

  // op 34085: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1357", "key9140", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1357, key9140) size=" << scan_result.size() << std::endl;
  }

  // op 34086: put
  db->Put(write_options, "key2417", "value61728");

  // op 34087: delete_range
  status = db->DeleteRange(write_options, "key4594", "key6435");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 34088: put
  db->Put(write_options, "key8261", "value18486");

  // op 34089: put
  db->Put(write_options, "key10108", "value69112");

  // op 34090: put
  db->Put(write_options, "key2527", "value41446");

  // op 34091: put
  db->Put(write_options, "key0910", "value0650");

  // op 34092: get
  status = db->Get(read_options, "key2305", &value);
  if (status.ok()) {
    out << "GET key2305 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34093: put
  db->Put(write_options, "key0340", "value51648");

  // op 34094: put
  db->Put(write_options, "key5859", "value21369");

  // op 34095: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2561", "key9601", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2561, key9601) size=" << scan_result.size() << std::endl;
  }

  // op 34096: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6033", "key9117", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6033, key9117) size=" << scan_result.size() << std::endl;
  }

  // op 34097: put
  db->Put(write_options, "key8845", "value57070");

  // op 34098: get
  status = db->Get(read_options, "key8595", &value);
  if (status.ok()) {
    out << "GET key8595 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34099: put
  db->Put(write_options, "key5171", "value91145");

  // op 34100: put
  db->Put(write_options, "key1899", "value32073");

  // op 34101: delete_range
  status = db->DeleteRange(write_options, "key0382", "key6928");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 34102: get
  status = db->Get(read_options, "key7958", &value);
  if (status.ok()) {
    out << "GET key7958 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34103: get
  status = db->Get(read_options, "key10307", &value);
  if (status.ok()) {
    out << "GET key10307 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34104: get
  status = db->Get(read_options, "key9470", &value);
  if (status.ok()) {
    out << "GET key9470 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34105: delete_range
  status = db->DeleteRange(write_options, "key0662", "key3988");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 34106: put
  db->Put(write_options, "key9201", "value32687");

  // op 34107: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4363", "key8156", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4363, key8156) size=" << scan_result.size() << std::endl;
  }

  // op 34108: delete_range
  status = db->DeleteRange(write_options, "key8611", "key10916");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 34109: get
  status = db->Get(read_options, "key5188", &value);
  if (status.ok()) {
    out << "GET key5188 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34110: get
  status = db->Get(read_options, "key1578", &value);
  if (status.ok()) {
    out << "GET key1578 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34111: put
  db->Put(write_options, "key4756", "value108017");

  // op 34112: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 34113: put
  db->Put(write_options, "key0932", "value100628");

  // op 34114: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4467", "key7738", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4467, key7738) size=" << scan_result.size() << std::endl;
  }

  // op 34115: put
  db->Put(write_options, "key6685", "value87301");

  // op 34116: delete_range
  status = db->DeleteRange(write_options, "key7731", "key10698");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 34117: put
  db->Put(write_options, "key3010", "value3058");

  // op 34118: put
  db->Put(write_options, "key9673", "value38697");

  // op 34119: put
  db->Put(write_options, "key2767", "value112239");

  // op 34120: delete
  db->Delete(write_options, "key10942");

  // op 34121: get
  status = db->Get(read_options, "key5463", &value);
  if (status.ok()) {
    out << "GET key5463 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34122: put
  db->Put(write_options, "key10072", "value36899");

  // op 34123: put
  db->Put(write_options, "key5941", "value41389");

  // op 34124: delete_range
  status = db->DeleteRange(write_options, "key6449", "key7949");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 34125: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6133", "key11369", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6133, key11369) size=" << scan_result.size() << std::endl;
  }

  // op 34126: get
  status = db->Get(read_options, "key8164", &value);
  if (status.ok()) {
    out << "GET key8164 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34127: put
  db->Put(write_options, "key10781", "value22254");

  // op 34128: put
  db->Put(write_options, "key10368", "value4112");

  // op 34129: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 34130: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 34131: get
  status = db->Get(read_options, "key7176", &value);
  if (status.ok()) {
    out << "GET key7176 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34132: delete_range
  status = db->DeleteRange(write_options, "key5060", "key10334");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 34133: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0349", "key9681", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0349, key9681) size=" << scan_result.size() << std::endl;
  }

  // op 34134: put
  db->Put(write_options, "key9094", "value69683");

  // op 34135: put
  db->Put(write_options, "key8338", "value116673");

  // op 34136: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3791", "key10518", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3791, key10518) size=" << scan_result.size() << std::endl;
  }

  // op 34137: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1299", "key8927", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1299, key8927) size=" << scan_result.size() << std::endl;
  }

  // op 34138: delete_range
  status = db->DeleteRange(write_options, "key7686", "key8742");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 34139: get
  status = db->Get(read_options, "key11112", &value);
  if (status.ok()) {
    out << "GET key11112 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34140: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3309", "key9020", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3309, key9020) size=" << scan_result.size() << std::endl;
  }

  // op 34141: put
  db->Put(write_options, "key4361", "value98164");

  // op 34142: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1580", "key3689", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1580, key3689) size=" << scan_result.size() << std::endl;
  }

  // op 34143: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3087", "key5823", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3087, key5823) size=" << scan_result.size() << std::endl;
  }

  // op 34144: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7622", "key11599", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7622, key11599) size=" << scan_result.size() << std::endl;
  }

  // op 34145: put
  db->Put(write_options, "key10524", "value111945");

  // op 34146: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2329", "key5359", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2329, key5359) size=" << scan_result.size() << std::endl;
  }

  // op 34147: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 34148: get
  status = db->Get(read_options, "key0456", &value);
  if (status.ok()) {
    out << "GET key0456 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34149: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1114", "key4375", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1114, key4375) size=" << scan_result.size() << std::endl;
  }

  // op 34150: delete_range
  status = db->DeleteRange(write_options, "key8217", "key11504");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 34151: put
  db->Put(write_options, "key1856", "value72186");

  // op 34152: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 34153: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 34154: put
  db->Put(write_options, "key2376", "value95510");

  // op 34155: get
  status = db->Get(read_options, "key5065", &value);
  if (status.ok()) {
    out << "GET key5065 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34156: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0088", "key1698", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0088, key1698) size=" << scan_result.size() << std::endl;
  }

  // op 34157: put
  db->Put(write_options, "key9398", "value22886");

  // op 34158: delete
  db->Delete(write_options, "key0594");

  // op 34159: put
  db->Put(write_options, "key10541", "value110361");

  // op 34160: get
  status = db->Get(read_options, "key3707", &value);
  if (status.ok()) {
    out << "GET key3707 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34161: delete
  db->Delete(write_options, "key5773");

  // op 34162: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6807", "key8794", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6807, key8794) size=" << scan_result.size() << std::endl;
  }

  // op 34163: delete
  db->Delete(write_options, "key0897");

  // op 34164: delete_range
  status = db->DeleteRange(write_options, "key0063", "key4468");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 34165: put
  db->Put(write_options, "key6788", "value46900");

  // op 34166: delete
  db->Delete(write_options, "key7822");

  // op 34167: put
  db->Put(write_options, "key5251", "value63794");

  // op 34168: delete_range
  status = db->DeleteRange(write_options, "key1559", "key5527");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 34169: delete
  db->Delete(write_options, "key5374");

  // op 34170: put
  db->Put(write_options, "key7171", "value44970");

  // op 34171: delete
  db->Delete(write_options, "key1518");

  // op 34172: get
  status = db->Get(read_options, "key7937", &value);
  if (status.ok()) {
    out << "GET key7937 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34173: get
  status = db->Get(read_options, "key6375", &value);
  if (status.ok()) {
    out << "GET key6375 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34174: get
  status = db->Get(read_options, "key6377", &value);
  if (status.ok()) {
    out << "GET key6377 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34175: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2664", "key6308", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2664, key6308) size=" << scan_result.size() << std::endl;
  }

  // op 34176: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2674", "key4223", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2674, key4223) size=" << scan_result.size() << std::endl;
  }

  // op 34177: delete_range
  status = db->DeleteRange(write_options, "key5347", "key5689");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 34178: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4793", "key4819", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4793, key4819) size=" << scan_result.size() << std::endl;
  }

  // op 34179: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 34180: get
  status = db->Get(read_options, "key4007", &value);
  if (status.ok()) {
    out << "GET key4007 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34181: put
  db->Put(write_options, "key4371", "value38963");

  // op 34182: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1476", "key9222", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1476, key9222) size=" << scan_result.size() << std::endl;
  }

  // op 34183: put
  db->Put(write_options, "key3564", "value67449");

  // op 34184: delete
  db->Delete(write_options, "key0838");

  // op 34185: delete
  db->Delete(write_options, "key6294");

  // op 34186: get
  status = db->Get(read_options, "key11519", &value);
  if (status.ok()) {
    out << "GET key11519 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34187: get
  status = db->Get(read_options, "key4582", &value);
  if (status.ok()) {
    out << "GET key4582 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34188: put
  db->Put(write_options, "key2488", "value108618");

  // op 34189: put
  db->Put(write_options, "key9805", "value95184");

  // op 34190: delete
  db->Delete(write_options, "key10353");

  // op 34191: put
  db->Put(write_options, "key5589", "value45756");

  // op 34192: put
  db->Put(write_options, "key9971", "value97535");

  // op 34193: delete
  db->Delete(write_options, "key6970");

  // op 34194: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1845", "key6959", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1845, key6959) size=" << scan_result.size() << std::endl;
  }

  // op 34195: delete_range
  status = db->DeleteRange(write_options, "key3961", "key7035");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 34196: delete
  db->Delete(write_options, "key3553");

  // op 34197: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 34198: delete
  db->Delete(write_options, "key4133");

  // op 34199: put
  db->Put(write_options, "key1152", "value13237");

  // op 34200: delete
  db->Delete(write_options, "key3012");

  // op 34201: put
  db->Put(write_options, "key6130", "value18088");

  // op 34202: put
  db->Put(write_options, "key6832", "value112413");

  // op 34203: get
  status = db->Get(read_options, "key9869", &value);
  if (status.ok()) {
    out << "GET key9869 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34204: put
  db->Put(write_options, "key0013", "value56666");

  // op 34205: delete
  db->Delete(write_options, "key10845");

  // op 34206: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1349", "key5661", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1349, key5661) size=" << scan_result.size() << std::endl;
  }

  // op 34207: put
  db->Put(write_options, "key7086", "value16498");

  // op 34208: get
  status = db->Get(read_options, "key4023", &value);
  if (status.ok()) {
    out << "GET key4023 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34209: put
  db->Put(write_options, "key5330", "value93108");

  // op 34210: get
  status = db->Get(read_options, "key2256", &value);
  if (status.ok()) {
    out << "GET key2256 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34211: delete_range
  status = db->DeleteRange(write_options, "key6254", "key7819");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 34212: delete
  db->Delete(write_options, "key8724");

  // op 34213: get
  status = db->Get(read_options, "key0778", &value);
  if (status.ok()) {
    out << "GET key0778 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34214: delete_range
  status = db->DeleteRange(write_options, "key8058", "key9055");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 34215: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10816", "key11353", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10816, key11353) size=" << scan_result.size() << std::endl;
  }

  // op 34216: delete_range
  status = db->DeleteRange(write_options, "key0723", "key10920");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 34217: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1061", "key2465", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1061, key2465) size=" << scan_result.size() << std::endl;
  }

  // op 34218: get
  status = db->Get(read_options, "key0343", &value);
  if (status.ok()) {
    out << "GET key0343 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34219: put
  db->Put(write_options, "key3334", "value91933");

  // op 34220: put
  db->Put(write_options, "key10913", "value94774");

  // op 34221: put
  db->Put(write_options, "key1622", "value99092");

  // op 34222: delete
  db->Delete(write_options, "key2356");

  // op 34223: get
  status = db->Get(read_options, "key6166", &value);
  if (status.ok()) {
    out << "GET key6166 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34224: delete_range
  status = db->DeleteRange(write_options, "key0293", "key2031");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 34225: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4928", "key10805", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4928, key10805) size=" << scan_result.size() << std::endl;
  }

  // op 34226: get
  status = db->Get(read_options, "key2132", &value);
  if (status.ok()) {
    out << "GET key2132 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34227: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 34228: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4518", "key7684", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4518, key7684) size=" << scan_result.size() << std::endl;
  }

  // op 34229: put
  db->Put(write_options, "key3086", "value115830");

  // op 34230: get
  status = db->Get(read_options, "key7956", &value);
  if (status.ok()) {
    out << "GET key7956 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34231: put
  db->Put(write_options, "key3706", "value88598");

  // op 34232: get
  status = db->Get(read_options, "key9823", &value);
  if (status.ok()) {
    out << "GET key9823 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34233: put
  db->Put(write_options, "key4158", "value28498");

  // op 34234: delete_range
  status = db->DeleteRange(write_options, "key7925", "key10974");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 34235: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9858", "key10364", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9858, key10364) size=" << scan_result.size() << std::endl;
  }

  // op 34236: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10530", "key10569", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10530, key10569) size=" << scan_result.size() << std::endl;
  }

  // op 34237: delete_range
  status = db->DeleteRange(write_options, "key8322", "key10501");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 34238: put
  db->Put(write_options, "key11379", "value78020");

  // op 34239: delete_range
  status = db->DeleteRange(write_options, "key1179", "key11563");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 34240: delete_range
  status = db->DeleteRange(write_options, "key8000", "key11358");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 34241: get
  status = db->Get(read_options, "key1881", &value);
  if (status.ok()) {
    out << "GET key1881 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34242: put
  db->Put(write_options, "key2017", "value62131");

  // op 34243: put
  db->Put(write_options, "key10126", "value69011");

  // op 34244: delete_range
  status = db->DeleteRange(write_options, "key4782", "key4927");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 34245: get
  status = db->Get(read_options, "key3546", &value);
  if (status.ok()) {
    out << "GET key3546 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34246: get
  status = db->Get(read_options, "key11422", &value);
  if (status.ok()) {
    out << "GET key11422 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34247: put
  db->Put(write_options, "key6898", "value86107");

  // op 34248: get
  status = db->Get(read_options, "key0077", &value);
  if (status.ok()) {
    out << "GET key0077 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34249: put
  db->Put(write_options, "key0543", "value61280");

  // op 34250: put
  db->Put(write_options, "key11024", "value40909");

  // op 34251: get
  status = db->Get(read_options, "key5561", &value);
  if (status.ok()) {
    out << "GET key5561 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34252: put
  db->Put(write_options, "key0183", "value40203");

  // op 34253: put
  db->Put(write_options, "key1541", "value32231");

  // op 34254: delete
  db->Delete(write_options, "key2832");

  // op 34255: put
  db->Put(write_options, "key0290", "value1516");

  // op 34256: delete_range
  status = db->DeleteRange(write_options, "key1436", "key8421");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 34257: get
  status = db->Get(read_options, "key11216", &value);
  if (status.ok()) {
    out << "GET key11216 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34258: put
  db->Put(write_options, "key0137", "value98975");

  // op 34259: put
  db->Put(write_options, "key11020", "value82081");

  // op 34260: put
  db->Put(write_options, "key5979", "value51471");

  // op 34261: delete_range
  status = db->DeleteRange(write_options, "key2125", "key10949");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 34262: get
  status = db->Get(read_options, "key5211", &value);
  if (status.ok()) {
    out << "GET key5211 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34263: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3212", "key7208", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3212, key7208) size=" << scan_result.size() << std::endl;
  }

  // op 34264: put
  db->Put(write_options, "key2043", "value15448");

  // op 34265: put
  db->Put(write_options, "key10170", "value98080");

  // op 34266: get
  status = db->Get(read_options, "key6320", &value);
  if (status.ok()) {
    out << "GET key6320 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34267: get
  status = db->Get(read_options, "key6401", &value);
  if (status.ok()) {
    out << "GET key6401 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34268: put
  db->Put(write_options, "key11611", "value84739");

  // op 34269: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 34270: delete
  db->Delete(write_options, "key4632");

  // op 34271: put
  db->Put(write_options, "key5940", "value1918");

  // op 34272: put
  db->Put(write_options, "key3804", "value45661");

  // op 34273: put
  db->Put(write_options, "key5661", "value86522");

  // op 34274: get
  status = db->Get(read_options, "key9828", &value);
  if (status.ok()) {
    out << "GET key9828 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34275: delete
  db->Delete(write_options, "key10239");

  // op 34276: get
  status = db->Get(read_options, "key10231", &value);
  if (status.ok()) {
    out << "GET key10231 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34277: get
  status = db->Get(read_options, "key2986", &value);
  if (status.ok()) {
    out << "GET key2986 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34278: put
  db->Put(write_options, "key4864", "value4318");

  // op 34279: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0354", "key4842", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0354, key4842) size=" << scan_result.size() << std::endl;
  }

  // op 34280: delete
  db->Delete(write_options, "key1758");

  // op 34281: get
  status = db->Get(read_options, "key10533", &value);
  if (status.ok()) {
    out << "GET key10533 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34282: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7599", "key9284", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7599, key9284) size=" << scan_result.size() << std::endl;
  }

  // op 34283: delete_range
  status = db->DeleteRange(write_options, "key2974", "key9080");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 34284: get
  status = db->Get(read_options, "key9978", &value);
  if (status.ok()) {
    out << "GET key9978 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34285: put
  db->Put(write_options, "key0807", "value39446");

  // op 34286: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 34287: get
  status = db->Get(read_options, "key8082", &value);
  if (status.ok()) {
    out << "GET key8082 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34288: delete_range
  status = db->DeleteRange(write_options, "key4744", "key10861");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 34289: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 34290: delete
  db->Delete(write_options, "key0173");

  // op 34291: put
  db->Put(write_options, "key9459", "value45303");

  // op 34292: put
  db->Put(write_options, "key3451", "value7326");

  // op 34293: put
  db->Put(write_options, "key11261", "value44341");

  // op 34294: get
  status = db->Get(read_options, "key1914", &value);
  if (status.ok()) {
    out << "GET key1914 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34295: get
  status = db->Get(read_options, "key7704", &value);
  if (status.ok()) {
    out << "GET key7704 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34296: put
  db->Put(write_options, "key5597", "value72241");

  // op 34297: put
  db->Put(write_options, "key7877", "value32132");

  // op 34298: put
  db->Put(write_options, "key4610", "value44021");

  // op 34299: delete_range
  status = db->DeleteRange(write_options, "key9825", "key10558");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 34300: put
  db->Put(write_options, "key3914", "value43951");

  // op 34301: get
  status = db->Get(read_options, "key4472", &value);
  if (status.ok()) {
    out << "GET key4472 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34302: delete_range
  status = db->DeleteRange(write_options, "key4129", "key8287");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 34303: get
  status = db->Get(read_options, "key0921", &value);
  if (status.ok()) {
    out << "GET key0921 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34304: get
  status = db->Get(read_options, "key4382", &value);
  if (status.ok()) {
    out << "GET key4382 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34305: put
  db->Put(write_options, "key7970", "value20804");

  // op 34306: delete
  db->Delete(write_options, "key2834");

  // op 34307: put
  db->Put(write_options, "key0230", "value90745");

  // op 34308: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0522", "key6722", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0522, key6722) size=" << scan_result.size() << std::endl;
  }

  // op 34309: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10229", "key11182", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10229, key11182) size=" << scan_result.size() << std::endl;
  }

  // op 34310: put
  db->Put(write_options, "key3122", "value93850");

  // op 34311: put
  db->Put(write_options, "key8949", "value81294");

  // op 34312: get
  status = db->Get(read_options, "key1273", &value);
  if (status.ok()) {
    out << "GET key1273 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34313: delete
  db->Delete(write_options, "key10976");

  // op 34314: put
  db->Put(write_options, "key6732", "value15783");

  // op 34315: put
  db->Put(write_options, "key5068", "value47954");

  // op 34316: delete_range
  status = db->DeleteRange(write_options, "key3197", "key9062");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 34317: delete
  db->Delete(write_options, "key10564");

  // op 34318: put
  db->Put(write_options, "key6917", "value73070");

  // op 34319: delete
  db->Delete(write_options, "key8084");

  // op 34320: put
  db->Put(write_options, "key4224", "value93275");

  // op 34321: put
  db->Put(write_options, "key6634", "value32787");

  // op 34322: get
  status = db->Get(read_options, "key11273", &value);
  if (status.ok()) {
    out << "GET key11273 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34323: get
  status = db->Get(read_options, "key10241", &value);
  if (status.ok()) {
    out << "GET key10241 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34324: delete_range
  status = db->DeleteRange(write_options, "key2064", "key10369");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 34325: put
  db->Put(write_options, "key10863", "value95591");

  // op 34326: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2836", "key10093", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2836, key10093) size=" << scan_result.size() << std::endl;
  }

  // op 34327: put
  db->Put(write_options, "key5709", "value25675");

  // op 34328: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9410", "key10372", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9410, key10372) size=" << scan_result.size() << std::endl;
  }

  // op 34329: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 34330: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 34331: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8615", "key10716", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8615, key10716) size=" << scan_result.size() << std::endl;
  }

  // op 34332: put
  db->Put(write_options, "key4074", "value52513");

  // op 34333: get
  status = db->Get(read_options, "key3779", &value);
  if (status.ok()) {
    out << "GET key3779 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34334: put
  db->Put(write_options, "key10360", "value39291");

  // op 34335: put
  db->Put(write_options, "key0495", "value8389");

  // op 34336: delete_range
  status = db->DeleteRange(write_options, "key8055", "key8481");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 34337: delete_range
  status = db->DeleteRange(write_options, "key1344", "key9465");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 34338: put
  db->Put(write_options, "key11478", "value12891");

  // op 34339: put
  db->Put(write_options, "key2981", "value113509");

  // op 34340: put
  db->Put(write_options, "key11672", "value104867");

  // op 34341: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2889", "key5378", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2889, key5378) size=" << scan_result.size() << std::endl;
  }

  // op 34342: put
  db->Put(write_options, "key7828", "value111474");

  // op 34343: get
  status = db->Get(read_options, "key9508", &value);
  if (status.ok()) {
    out << "GET key9508 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34344: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8086", "key9713", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8086, key9713) size=" << scan_result.size() << std::endl;
  }

  // op 34345: put
  db->Put(write_options, "key1769", "value50162");

  // op 34346: put
  db->Put(write_options, "key5920", "value95091");

  // op 34347: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2244", "key11029", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2244, key11029) size=" << scan_result.size() << std::endl;
  }

  // op 34348: get
  status = db->Get(read_options, "key3647", &value);
  if (status.ok()) {
    out << "GET key3647 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34349: put
  db->Put(write_options, "key2805", "value64855");

  // op 34350: put
  db->Put(write_options, "key3570", "value52804");

  // op 34351: put
  db->Put(write_options, "key10810", "value43816");

  // op 34352: put
  db->Put(write_options, "key3320", "value49613");

  // op 34353: get
  status = db->Get(read_options, "key2989", &value);
  if (status.ok()) {
    out << "GET key2989 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34354: get
  status = db->Get(read_options, "key0870", &value);
  if (status.ok()) {
    out << "GET key0870 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34355: get
  status = db->Get(read_options, "key10117", &value);
  if (status.ok()) {
    out << "GET key10117 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34356: put
  db->Put(write_options, "key5371", "value96998");

  // op 34357: get
  status = db->Get(read_options, "key4926", &value);
  if (status.ok()) {
    out << "GET key4926 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34358: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4048", "key11352", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4048, key11352) size=" << scan_result.size() << std::endl;
  }

  // op 34359: delete
  db->Delete(write_options, "key6894");

  // op 34360: put
  db->Put(write_options, "key5592", "value103994");

  // op 34361: put
  db->Put(write_options, "key0517", "value15682");

  // op 34362: put
  db->Put(write_options, "key10413", "value111709");

  // op 34363: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 34364: get
  status = db->Get(read_options, "key10412", &value);
  if (status.ok()) {
    out << "GET key10412 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34365: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 34366: put
  db->Put(write_options, "key7298", "value99576");

  // op 34367: get
  status = db->Get(read_options, "key8272", &value);
  if (status.ok()) {
    out << "GET key8272 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34368: put
  db->Put(write_options, "key6048", "value101390");

  // op 34369: delete_range
  status = db->DeleteRange(write_options, "key4245", "key7441");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 34370: put
  db->Put(write_options, "key7605", "value92064");

  // op 34371: get
  status = db->Get(read_options, "key9443", &value);
  if (status.ok()) {
    out << "GET key9443 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34372: get
  status = db->Get(read_options, "key2397", &value);
  if (status.ok()) {
    out << "GET key2397 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34373: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7987", "key11148", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7987, key11148) size=" << scan_result.size() << std::endl;
  }

  // op 34374: delete_range
  status = db->DeleteRange(write_options, "key9547", "key10165");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 34375: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5571", "key5989", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5571, key5989) size=" << scan_result.size() << std::endl;
  }

  // op 34376: get
  status = db->Get(read_options, "key4833", &value);
  if (status.ok()) {
    out << "GET key4833 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34377: get
  status = db->Get(read_options, "key3380", &value);
  if (status.ok()) {
    out << "GET key3380 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34378: put
  db->Put(write_options, "key8625", "value0091");

  // op 34379: get
  status = db->Get(read_options, "key6779", &value);
  if (status.ok()) {
    out << "GET key6779 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34380: delete
  db->Delete(write_options, "key9392");

  // op 34381: delete
  db->Delete(write_options, "key7510");

  // op 34382: get
  status = db->Get(read_options, "key8872", &value);
  if (status.ok()) {
    out << "GET key8872 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34383: get
  status = db->Get(read_options, "key2202", &value);
  if (status.ok()) {
    out << "GET key2202 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34384: delete_range
  status = db->DeleteRange(write_options, "key0206", "key3567");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 34385: put
  db->Put(write_options, "key11314", "value15609");

  // op 34386: get
  status = db->Get(read_options, "key9341", &value);
  if (status.ok()) {
    out << "GET key9341 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34387: get
  status = db->Get(read_options, "key2243", &value);
  if (status.ok()) {
    out << "GET key2243 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34388: put
  db->Put(write_options, "key3121", "value101026");

  // op 34389: put
  db->Put(write_options, "key9578", "value47914");

  // op 34390: put
  db->Put(write_options, "key11476", "value103374");

  // op 34391: put
  db->Put(write_options, "key11536", "value38822");

  // op 34392: delete_range
  status = db->DeleteRange(write_options, "key7917", "key8536");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 34393: delete
  db->Delete(write_options, "key11591");

  // op 34394: delete
  db->Delete(write_options, "key4288");

  // op 34395: put
  db->Put(write_options, "key7214", "value87359");

  // op 34396: put
  db->Put(write_options, "key5922", "value36235");

  // op 34397: get
  status = db->Get(read_options, "key9177", &value);
  if (status.ok()) {
    out << "GET key9177 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34398: put
  db->Put(write_options, "key0663", "value96431");

  // op 34399: delete_range
  status = db->DeleteRange(write_options, "key8970", "key9079");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 34400: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 34401: get
  status = db->Get(read_options, "key9972", &value);
  if (status.ok()) {
    out << "GET key9972 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34402: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2287", "key2617", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2287, key2617) size=" << scan_result.size() << std::endl;
  }

  // op 34403: put
  db->Put(write_options, "key3974", "value14816");

  // op 34404: put
  db->Put(write_options, "key6078", "value116776");

  // op 34405: put
  db->Put(write_options, "key6933", "value12654");

  // op 34406: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0080", "key5693", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0080, key5693) size=" << scan_result.size() << std::endl;
  }

  // op 34407: put
  db->Put(write_options, "key10114", "value27727");

  // op 34408: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3450", "key10640", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3450, key10640) size=" << scan_result.size() << std::endl;
  }

  // op 34409: get
  status = db->Get(read_options, "key8410", &value);
  if (status.ok()) {
    out << "GET key8410 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34410: get
  status = db->Get(read_options, "key5325", &value);
  if (status.ok()) {
    out << "GET key5325 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34411: put
  db->Put(write_options, "key10952", "value82050");

  // op 34412: delete_range
  status = db->DeleteRange(write_options, "key2392", "key7238");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 34413: get
  status = db->Get(read_options, "key6016", &value);
  if (status.ok()) {
    out << "GET key6016 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34414: delete_range
  status = db->DeleteRange(write_options, "key2321", "key7567");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 34415: put
  db->Put(write_options, "key3128", "value60939");

  // op 34416: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1846", "key8033", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1846, key8033) size=" << scan_result.size() << std::endl;
  }

  // op 34417: put
  db->Put(write_options, "key9300", "value47237");

  // op 34418: delete_range
  status = db->DeleteRange(write_options, "key2547", "key11150");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 34419: get
  status = db->Get(read_options, "key5364", &value);
  if (status.ok()) {
    out << "GET key5364 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34420: put
  db->Put(write_options, "key2970", "value96789");

  // op 34421: put
  db->Put(write_options, "key11632", "value4722");

  // op 34422: delete_range
  status = db->DeleteRange(write_options, "key7561", "key11722");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 34423: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 34424: delete_range
  status = db->DeleteRange(write_options, "key6703", "key9172");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 34425: put
  db->Put(write_options, "key6161", "value68863");

  // op 34426: get
  status = db->Get(read_options, "key6355", &value);
  if (status.ok()) {
    out << "GET key6355 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34427: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0680", "key3222", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0680, key3222) size=" << scan_result.size() << std::endl;
  }

  // op 34428: put
  db->Put(write_options, "key7633", "value46788");

  // op 34429: put
  db->Put(write_options, "key3227", "value113956");

  // op 34430: put
  db->Put(write_options, "key0189", "value71310");

  // op 34431: get
  status = db->Get(read_options, "key0922", &value);
  if (status.ok()) {
    out << "GET key0922 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34432: delete
  db->Delete(write_options, "key11493");

  // op 34433: get
  status = db->Get(read_options, "key4439", &value);
  if (status.ok()) {
    out << "GET key4439 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34434: delete_range
  status = db->DeleteRange(write_options, "key9180", "key10732");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 34435: put
  db->Put(write_options, "key7899", "value50182");

  // op 34436: get
  status = db->Get(read_options, "key9997", &value);
  if (status.ok()) {
    out << "GET key9997 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34437: put
  db->Put(write_options, "key8315", "value106317");

  // op 34438: delete
  db->Delete(write_options, "key1541");

  // op 34439: put
  db->Put(write_options, "key2070", "value2384");

  // op 34440: get
  status = db->Get(read_options, "key5486", &value);
  if (status.ok()) {
    out << "GET key5486 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34441: get
  status = db->Get(read_options, "key5433", &value);
  if (status.ok()) {
    out << "GET key5433 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34442: get
  status = db->Get(read_options, "key1828", &value);
  if (status.ok()) {
    out << "GET key1828 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34443: get
  status = db->Get(read_options, "key2603", &value);
  if (status.ok()) {
    out << "GET key2603 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34444: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1519", "key7474", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1519, key7474) size=" << scan_result.size() << std::endl;
  }

  // op 34445: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0989", "key6857", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0989, key6857) size=" << scan_result.size() << std::endl;
  }

  // op 34446: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0416", "key1379", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0416, key1379) size=" << scan_result.size() << std::endl;
  }

  // op 34447: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2103", "key5667", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2103, key5667) size=" << scan_result.size() << std::endl;
  }

  // op 34448: put
  db->Put(write_options, "key2997", "value24799");

  // op 34449: delete_range
  status = db->DeleteRange(write_options, "key0026", "key6475");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 34450: get
  status = db->Get(read_options, "key9946", &value);
  if (status.ok()) {
    out << "GET key9946 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34451: put
  db->Put(write_options, "key10486", "value6110");

  // op 34452: put
  db->Put(write_options, "key9760", "value48177");

  // op 34453: put
  db->Put(write_options, "key7145", "value59475");

  // op 34454: put
  db->Put(write_options, "key2251", "value83297");

  // op 34455: get
  status = db->Get(read_options, "key6520", &value);
  if (status.ok()) {
    out << "GET key6520 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34456: put
  db->Put(write_options, "key3786", "value5509");

  // op 34457: put
  db->Put(write_options, "key8928", "value41985");

  // op 34458: put
  db->Put(write_options, "key2632", "value101929");

  // op 34459: delete_range
  status = db->DeleteRange(write_options, "key5441", "key11347");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 34460: delete_range
  status = db->DeleteRange(write_options, "key6725", "key10829");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 34461: put
  db->Put(write_options, "key5838", "value82230");

  // op 34462: get
  status = db->Get(read_options, "key4441", &value);
  if (status.ok()) {
    out << "GET key4441 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34463: delete_range
  status = db->DeleteRange(write_options, "key0258", "key5292");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 34464: get
  status = db->Get(read_options, "key3450", &value);
  if (status.ok()) {
    out << "GET key3450 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34465: put
  db->Put(write_options, "key3802", "value7444");

  // op 34466: put
  db->Put(write_options, "key7789", "value2944");

  // op 34467: delete
  db->Delete(write_options, "key8555");

  // op 34468: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4509", "key5980", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4509, key5980) size=" << scan_result.size() << std::endl;
  }

  // op 34469: get
  status = db->Get(read_options, "key5442", &value);
  if (status.ok()) {
    out << "GET key5442 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34470: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 34471: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4066", "key10355", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4066, key10355) size=" << scan_result.size() << std::endl;
  }

  // op 34472: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 34473: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 34474: put
  db->Put(write_options, "key2411", "value14917");

  // op 34475: get
  status = db->Get(read_options, "key3429", &value);
  if (status.ok()) {
    out << "GET key3429 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34476: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0325", "key0601", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0325, key0601) size=" << scan_result.size() << std::endl;
  }

  // op 34477: get
  status = db->Get(read_options, "key3685", &value);
  if (status.ok()) {
    out << "GET key3685 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34478: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0107", "key4120", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0107, key4120) size=" << scan_result.size() << std::endl;
  }

  // op 34479: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4180", "key4838", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4180, key4838) size=" << scan_result.size() << std::endl;
  }

  // op 34480: delete
  db->Delete(write_options, "key0744");

  // op 34481: get
  status = db->Get(read_options, "key7690", &value);
  if (status.ok()) {
    out << "GET key7690 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34482: put
  db->Put(write_options, "key11046", "value82696");

  // op 34483: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7056", "key10738", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7056, key10738) size=" << scan_result.size() << std::endl;
  }

  // op 34484: delete
  db->Delete(write_options, "key3558");

  // op 34485: put
  db->Put(write_options, "key3390", "value35163");

  // op 34486: put
  db->Put(write_options, "key5400", "value102317");

  // op 34487: delete
  db->Delete(write_options, "key7095");

  // op 34488: get
  status = db->Get(read_options, "key6148", &value);
  if (status.ok()) {
    out << "GET key6148 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34489: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5198", "key7543", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5198, key7543) size=" << scan_result.size() << std::endl;
  }

  // op 34490: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4035", "key9319", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4035, key9319) size=" << scan_result.size() << std::endl;
  }

  // op 34491: get
  status = db->Get(read_options, "key1904", &value);
  if (status.ok()) {
    out << "GET key1904 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34492: put
  db->Put(write_options, "key6708", "value40470");

  // op 34493: get
  status = db->Get(read_options, "key6934", &value);
  if (status.ok()) {
    out << "GET key6934 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34494: put
  db->Put(write_options, "key5813", "value54124");

  // op 34495: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0151", "key0979", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0151, key0979) size=" << scan_result.size() << std::endl;
  }

  // op 34496: put
  db->Put(write_options, "key6319", "value17827");

  // op 34497: put
  db->Put(write_options, "key6529", "value95100");

  // op 34498: get
  status = db->Get(read_options, "key4795", &value);
  if (status.ok()) {
    out << "GET key4795 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34499: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5475", "key7402", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5475, key7402) size=" << scan_result.size() << std::endl;
  }

  // op 34500: get
  status = db->Get(read_options, "key10748", &value);
  if (status.ok()) {
    out << "GET key10748 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34501: get
  status = db->Get(read_options, "key3889", &value);
  if (status.ok()) {
    out << "GET key3889 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34502: put
  db->Put(write_options, "key10811", "value50707");

  // op 34503: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5159", "key8368", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5159, key8368) size=" << scan_result.size() << std::endl;
  }

  // op 34504: put
  db->Put(write_options, "key3776", "value21452");

  // op 34505: put
  db->Put(write_options, "key9801", "value17597");

  // op 34506: get
  status = db->Get(read_options, "key3616", &value);
  if (status.ok()) {
    out << "GET key3616 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34507: get
  status = db->Get(read_options, "key2705", &value);
  if (status.ok()) {
    out << "GET key2705 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34508: get
  status = db->Get(read_options, "key7763", &value);
  if (status.ok()) {
    out << "GET key7763 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34509: put
  db->Put(write_options, "key8953", "value57717");

  // op 34510: get
  status = db->Get(read_options, "key7259", &value);
  if (status.ok()) {
    out << "GET key7259 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34511: delete_range
  status = db->DeleteRange(write_options, "key7811", "key11427");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 34512: put
  db->Put(write_options, "key10634", "value107662");

  // op 34513: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3336", "key8234", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3336, key8234) size=" << scan_result.size() << std::endl;
  }

  // op 34514: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0142", "key1166", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0142, key1166) size=" << scan_result.size() << std::endl;
  }

  // op 34515: put
  db->Put(write_options, "key2934", "value84558");

  // op 34516: put
  db->Put(write_options, "key5530", "value13486");

  // op 34517: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0067", "key1966", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0067, key1966) size=" << scan_result.size() << std::endl;
  }

  // op 34518: put
  db->Put(write_options, "key5641", "value66640");

  // op 34519: delete
  db->Delete(write_options, "key8814");

  // op 34520: put
  db->Put(write_options, "key4660", "value7736");

  // op 34521: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1660", "key10998", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1660, key10998) size=" << scan_result.size() << std::endl;
  }

  // op 34522: delete
  db->Delete(write_options, "key6634");

  // op 34523: delete_range
  status = db->DeleteRange(write_options, "key4474", "key11562");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 34524: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4614", "key8530", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4614, key8530) size=" << scan_result.size() << std::endl;
  }

  // op 34525: put
  db->Put(write_options, "key6897", "value108644");

  // op 34526: put
  db->Put(write_options, "key11116", "value66072");

  // op 34527: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5523", "key7853", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5523, key7853) size=" << scan_result.size() << std::endl;
  }

  // op 34528: put
  db->Put(write_options, "key8650", "value8732");

  // op 34529: delete
  db->Delete(write_options, "key0291");

  // op 34530: get
  status = db->Get(read_options, "key3584", &value);
  if (status.ok()) {
    out << "GET key3584 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34531: put
  db->Put(write_options, "key5578", "value1597");

  // op 34532: get
  status = db->Get(read_options, "key1948", &value);
  if (status.ok()) {
    out << "GET key1948 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34533: put
  db->Put(write_options, "key5107", "value109697");

  // op 34534: get
  status = db->Get(read_options, "key5820", &value);
  if (status.ok()) {
    out << "GET key5820 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34535: delete_range
  status = db->DeleteRange(write_options, "key6480", "key10818");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 34536: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5045", "key9816", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5045, key9816) size=" << scan_result.size() << std::endl;
  }

  // op 34537: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 34538: put
  db->Put(write_options, "key11360", "value57465");

  // op 34539: put
  db->Put(write_options, "key4519", "value42153");

  // op 34540: put
  db->Put(write_options, "key11049", "value34050");

  // op 34541: put
  db->Put(write_options, "key3538", "value30369");

  // op 34542: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3385", "key11691", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3385, key11691) size=" << scan_result.size() << std::endl;
  }

  // op 34543: delete_range
  status = db->DeleteRange(write_options, "key4701", "key7606");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 34544: get
  status = db->Get(read_options, "key4367", &value);
  if (status.ok()) {
    out << "GET key4367 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34545: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 34546: get
  status = db->Get(read_options, "key9958", &value);
  if (status.ok()) {
    out << "GET key9958 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34547: put
  db->Put(write_options, "key4156", "value3863");

  // op 34548: delete
  db->Delete(write_options, "key4995");

  // op 34549: put
  db->Put(write_options, "key5371", "value25391");

  // op 34550: delete
  db->Delete(write_options, "key8608");

  // op 34551: delete
  db->Delete(write_options, "key7662");

  // op 34552: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9443", "key9571", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9443, key9571) size=" << scan_result.size() << std::endl;
  }

  // op 34553: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6001", "key11205", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6001, key11205) size=" << scan_result.size() << std::endl;
  }

  // op 34554: put
  db->Put(write_options, "key10287", "value97577");

  // op 34555: delete_range
  status = db->DeleteRange(write_options, "key0729", "key2127");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 34556: get
  status = db->Get(read_options, "key8405", &value);
  if (status.ok()) {
    out << "GET key8405 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34557: get
  status = db->Get(read_options, "key4653", &value);
  if (status.ok()) {
    out << "GET key4653 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34558: get
  status = db->Get(read_options, "key11450", &value);
  if (status.ok()) {
    out << "GET key11450 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34559: delete
  db->Delete(write_options, "key3825");

  // op 34560: delete
  db->Delete(write_options, "key8765");

  // op 34561: put
  db->Put(write_options, "key1642", "value101922");

  // op 34562: get
  status = db->Get(read_options, "key2531", &value);
  if (status.ok()) {
    out << "GET key2531 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34563: put
  db->Put(write_options, "key1277", "value69325");

  // op 34564: put
  db->Put(write_options, "key9336", "value29540");

  // op 34565: put
  db->Put(write_options, "key5816", "value100405");

  // op 34566: get
  status = db->Get(read_options, "key4605", &value);
  if (status.ok()) {
    out << "GET key4605 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34567: get
  status = db->Get(read_options, "key6573", &value);
  if (status.ok()) {
    out << "GET key6573 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34568: put
  db->Put(write_options, "key11639", "value92504");

  // op 34569: delete_range
  status = db->DeleteRange(write_options, "key5876", "key10353");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 34570: put
  db->Put(write_options, "key11413", "value45220");

  // op 34571: put
  db->Put(write_options, "key10849", "value85515");

  // op 34572: get
  status = db->Get(read_options, "key6544", &value);
  if (status.ok()) {
    out << "GET key6544 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34573: delete
  db->Delete(write_options, "key0239");

  // op 34574: put
  db->Put(write_options, "key3577", "value101576");

  // op 34575: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10363", "key10710", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10363, key10710) size=" << scan_result.size() << std::endl;
  }

  // op 34576: put
  db->Put(write_options, "key10791", "value88493");

  // op 34577: put
  db->Put(write_options, "key1084", "value43051");

  // op 34578: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 34579: put
  db->Put(write_options, "key9691", "value74348");

  // op 34580: put
  db->Put(write_options, "key7003", "value101809");

  // op 34581: put
  db->Put(write_options, "key4303", "value49620");

  // op 34582: put
  db->Put(write_options, "key7596", "value44149");

  // op 34583: get
  status = db->Get(read_options, "key7002", &value);
  if (status.ok()) {
    out << "GET key7002 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34584: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2007", "key7577", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2007, key7577) size=" << scan_result.size() << std::endl;
  }

  // op 34585: put
  db->Put(write_options, "key9772", "value0124");

  // op 34586: delete
  db->Delete(write_options, "key3675");

  // op 34587: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 34588: put
  db->Put(write_options, "key6307", "value95535");

  // op 34589: put
  db->Put(write_options, "key7845", "value68798");

  // op 34590: put
  db->Put(write_options, "key6933", "value15229");

  // op 34591: put
  db->Put(write_options, "key0592", "value82095");

  // op 34592: delete_range
  status = db->DeleteRange(write_options, "key2696", "key11643");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 34593: put
  db->Put(write_options, "key7935", "value83223");

  // op 34594: put
  db->Put(write_options, "key2105", "value63051");

  // op 34595: put
  db->Put(write_options, "key3514", "value88749");

  // op 34596: put
  db->Put(write_options, "key7291", "value22066");

  // op 34597: put
  db->Put(write_options, "key2110", "value67071");

  // op 34598: put
  db->Put(write_options, "key2701", "value109500");

  // op 34599: delete_range
  status = db->DeleteRange(write_options, "key0137", "key4251");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 34600: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4545", "key7220", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4545, key7220) size=" << scan_result.size() << std::endl;
  }

  // op 34601: get
  status = db->Get(read_options, "key3760", &value);
  if (status.ok()) {
    out << "GET key3760 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34602: get
  status = db->Get(read_options, "key1824", &value);
  if (status.ok()) {
    out << "GET key1824 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34603: put
  db->Put(write_options, "key0024", "value109635");

  // op 34604: put
  db->Put(write_options, "key4940", "value95039");

  // op 34605: get
  status = db->Get(read_options, "key0351", &value);
  if (status.ok()) {
    out << "GET key0351 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34606: get
  status = db->Get(read_options, "key3506", &value);
  if (status.ok()) {
    out << "GET key3506 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34607: get
  status = db->Get(read_options, "key1068", &value);
  if (status.ok()) {
    out << "GET key1068 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34608: put
  db->Put(write_options, "key2247", "value9349");

  // op 34609: get
  status = db->Get(read_options, "key6513", &value);
  if (status.ok()) {
    out << "GET key6513 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34610: put
  db->Put(write_options, "key8702", "value2260");

  // op 34611: put
  db->Put(write_options, "key2291", "value99792");

  // op 34612: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1276", "key9751", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1276, key9751) size=" << scan_result.size() << std::endl;
  }

  // op 34613: delete
  db->Delete(write_options, "key10112");

  // op 34614: delete
  db->Delete(write_options, "key5945");

  // op 34615: put
  db->Put(write_options, "key2228", "value91563");

  // op 34616: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2413", "key11668", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2413, key11668) size=" << scan_result.size() << std::endl;
  }

  // op 34617: delete
  db->Delete(write_options, "key1470");

  // op 34618: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1464", "key6642", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1464, key6642) size=" << scan_result.size() << std::endl;
  }

  // op 34619: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4167", "key8178", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4167, key8178) size=" << scan_result.size() << std::endl;
  }

  // op 34620: get
  status = db->Get(read_options, "key2620", &value);
  if (status.ok()) {
    out << "GET key2620 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34621: get
  status = db->Get(read_options, "key10706", &value);
  if (status.ok()) {
    out << "GET key10706 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34622: get
  status = db->Get(read_options, "key9353", &value);
  if (status.ok()) {
    out << "GET key9353 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34623: put
  db->Put(write_options, "key8842", "value89907");

  // op 34624: put
  db->Put(write_options, "key8324", "value47970");

  // op 34625: put
  db->Put(write_options, "key11619", "value93113");

  // op 34626: put
  db->Put(write_options, "key3085", "value18919");

  // op 34627: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7303", "key11085", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7303, key11085) size=" << scan_result.size() << std::endl;
  }

  // op 34628: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4935", "key10673", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4935, key10673) size=" << scan_result.size() << std::endl;
  }

  // op 34629: get
  status = db->Get(read_options, "key1441", &value);
  if (status.ok()) {
    out << "GET key1441 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34630: put
  db->Put(write_options, "key9426", "value36075");

  // op 34631: delete
  db->Delete(write_options, "key9310");

  // op 34632: get
  status = db->Get(read_options, "key11571", &value);
  if (status.ok()) {
    out << "GET key11571 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34633: delete_range
  status = db->DeleteRange(write_options, "key8084", "key10261");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 34634: delete_range
  status = db->DeleteRange(write_options, "key7192", "key10351");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 34635: delete_range
  status = db->DeleteRange(write_options, "key5408", "key11434");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 34636: put
  db->Put(write_options, "key0594", "value31645");

  // op 34637: put
  db->Put(write_options, "key4652", "value77001");

  // op 34638: put
  db->Put(write_options, "key4695", "value96492");

  // op 34639: get
  status = db->Get(read_options, "key6737", &value);
  if (status.ok()) {
    out << "GET key6737 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34640: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8917", "key11472", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8917, key11472) size=" << scan_result.size() << std::endl;
  }

  // op 34641: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2626", "key8588", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2626, key8588) size=" << scan_result.size() << std::endl;
  }

  // op 34642: delete_range
  status = db->DeleteRange(write_options, "key3520", "key6582");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 34643: delete_range
  status = db->DeleteRange(write_options, "key3900", "key5218");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 34644: put
  db->Put(write_options, "key1882", "value99095");

  // op 34645: put
  db->Put(write_options, "key2208", "value100211");

  // op 34646: put
  db->Put(write_options, "key2371", "value109310");

  // op 34647: get
  status = db->Get(read_options, "key0405", &value);
  if (status.ok()) {
    out << "GET key0405 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34648: put
  db->Put(write_options, "key6861", "value105655");

  // op 34649: delete_range
  status = db->DeleteRange(write_options, "key2759", "key4302");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 34650: put
  db->Put(write_options, "key9378", "value21000");

  // op 34651: put
  db->Put(write_options, "key5646", "value116998");

  // op 34652: delete_range
  status = db->DeleteRange(write_options, "key0272", "key0556");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 34653: get
  status = db->Get(read_options, "key6964", &value);
  if (status.ok()) {
    out << "GET key6964 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34654: put
  db->Put(write_options, "key8015", "value5323");

  // op 34655: put
  db->Put(write_options, "key10465", "value25774");

  // op 34656: put
  db->Put(write_options, "key7242", "value98985");

  // op 34657: put
  db->Put(write_options, "key3007", "value68251");

  // op 34658: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 34659: delete
  db->Delete(write_options, "key3171");

  // op 34660: get
  status = db->Get(read_options, "key4149", &value);
  if (status.ok()) {
    out << "GET key4149 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34661: put
  db->Put(write_options, "key11670", "value19955");

  // op 34662: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2104", "key4865", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2104, key4865) size=" << scan_result.size() << std::endl;
  }

  // op 34663: delete
  db->Delete(write_options, "key1282");

  // op 34664: delete_range
  status = db->DeleteRange(write_options, "key1772", "key10950");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 34665: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1077", "key2905", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1077, key2905) size=" << scan_result.size() << std::endl;
  }

  // op 34666: put
  db->Put(write_options, "key2846", "value56491");

  // op 34667: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2031", "key2127", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2031, key2127) size=" << scan_result.size() << std::endl;
  }

  // op 34668: put
  db->Put(write_options, "key6966", "value1352");

  // op 34669: put
  db->Put(write_options, "key2659", "value24607");

  // op 34670: delete
  db->Delete(write_options, "key0059");

  // op 34671: delete_range
  status = db->DeleteRange(write_options, "key2876", "key7650");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 34672: put
  db->Put(write_options, "key5988", "value49799");

  // op 34673: put
  db->Put(write_options, "key0640", "value117629");

  // op 34674: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 34675: get
  status = db->Get(read_options, "key3753", &value);
  if (status.ok()) {
    out << "GET key3753 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34676: put
  db->Put(write_options, "key11029", "value98465");

  // op 34677: delete_range
  status = db->DeleteRange(write_options, "key2998", "key3374");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 34678: delete
  db->Delete(write_options, "key11564");

  // op 34679: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1819", "key6997", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1819, key6997) size=" << scan_result.size() << std::endl;
  }

  // op 34680: put
  db->Put(write_options, "key6983", "value37927");

  // op 34681: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 34682: get
  status = db->Get(read_options, "key5398", &value);
  if (status.ok()) {
    out << "GET key5398 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34683: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6834", "key7091", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6834, key7091) size=" << scan_result.size() << std::endl;
  }

  // op 34684: get
  status = db->Get(read_options, "key10494", &value);
  if (status.ok()) {
    out << "GET key10494 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34685: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0507", "key6038", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0507, key6038) size=" << scan_result.size() << std::endl;
  }

  // op 34686: get
  status = db->Get(read_options, "key1277", &value);
  if (status.ok()) {
    out << "GET key1277 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34687: get
  status = db->Get(read_options, "key7818", &value);
  if (status.ok()) {
    out << "GET key7818 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34688: delete_range
  status = db->DeleteRange(write_options, "key4471", "key9213");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 34689: get
  status = db->Get(read_options, "key7108", &value);
  if (status.ok()) {
    out << "GET key7108 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34690: get
  status = db->Get(read_options, "key0618", &value);
  if (status.ok()) {
    out << "GET key0618 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34691: put
  db->Put(write_options, "key4316", "value60054");

  // op 34692: get
  status = db->Get(read_options, "key0187", &value);
  if (status.ok()) {
    out << "GET key0187 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34693: delete
  db->Delete(write_options, "key6149");

  // op 34694: put
  db->Put(write_options, "key6251", "value101322");

  // op 34695: put
  db->Put(write_options, "key5777", "value64667");

  // op 34696: put
  db->Put(write_options, "key9603", "value66278");

  // op 34697: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8174", "key9881", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8174, key9881) size=" << scan_result.size() << std::endl;
  }

  // op 34698: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7373", "key9447", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7373, key9447) size=" << scan_result.size() << std::endl;
  }

  // op 34699: get
  status = db->Get(read_options, "key10740", &value);
  if (status.ok()) {
    out << "GET key10740 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34700: put
  db->Put(write_options, "key10081", "value19696");

  // op 34701: delete
  db->Delete(write_options, "key2046");

  // op 34702: get
  status = db->Get(read_options, "key7656", &value);
  if (status.ok()) {
    out << "GET key7656 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34703: get
  status = db->Get(read_options, "key9525", &value);
  if (status.ok()) {
    out << "GET key9525 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34704: get
  status = db->Get(read_options, "key4726", &value);
  if (status.ok()) {
    out << "GET key4726 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34705: put
  db->Put(write_options, "key2325", "value91305");

  // op 34706: put
  db->Put(write_options, "key1201", "value17734");

  // op 34707: get
  status = db->Get(read_options, "key11627", &value);
  if (status.ok()) {
    out << "GET key11627 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34708: put
  db->Put(write_options, "key9856", "value60378");

  // op 34709: get
  status = db->Get(read_options, "key4332", &value);
  if (status.ok()) {
    out << "GET key4332 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34710: put
  db->Put(write_options, "key0543", "value72890");

  // op 34711: put
  db->Put(write_options, "key10865", "value12682");

  // op 34712: delete
  db->Delete(write_options, "key7281");

  // op 34713: get
  status = db->Get(read_options, "key1532", &value);
  if (status.ok()) {
    out << "GET key1532 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34714: put
  db->Put(write_options, "key6698", "value88537");

  // op 34715: put
  db->Put(write_options, "key9317", "value72468");

  // op 34716: put
  db->Put(write_options, "key1756", "value46083");

  // op 34717: get
  status = db->Get(read_options, "key10087", &value);
  if (status.ok()) {
    out << "GET key10087 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34718: put
  db->Put(write_options, "key6126", "value47054");

  // op 34719: get
  status = db->Get(read_options, "key3665", &value);
  if (status.ok()) {
    out << "GET key3665 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34720: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3808", "key4083", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3808, key4083) size=" << scan_result.size() << std::endl;
  }

  // op 34721: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2859", "key6207", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2859, key6207) size=" << scan_result.size() << std::endl;
  }

  // op 34722: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5795", "key10390", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5795, key10390) size=" << scan_result.size() << std::endl;
  }

  // op 34723: put
  db->Put(write_options, "key7325", "value96006");

  // op 34724: put
  db->Put(write_options, "key9043", "value82718");

  // op 34725: delete
  db->Delete(write_options, "key10350");

  // op 34726: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1276", "key3951", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1276, key3951) size=" << scan_result.size() << std::endl;
  }

  // op 34727: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5766", "key7454", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5766, key7454) size=" << scan_result.size() << std::endl;
  }

  // op 34728: put
  db->Put(write_options, "key8208", "value86881");

  // op 34729: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0972", "key10285", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0972, key10285) size=" << scan_result.size() << std::endl;
  }

  // op 34730: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 34731: put
  db->Put(write_options, "key4130", "value76737");

  // op 34732: put
  db->Put(write_options, "key1339", "value9265");

  // op 34733: delete_range
  status = db->DeleteRange(write_options, "key2397", "key6082");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 34734: put
  db->Put(write_options, "key9029", "value55847");

  // op 34735: put
  db->Put(write_options, "key5058", "value28144");

  // op 34736: put
  db->Put(write_options, "key4957", "value63765");

  // op 34737: delete
  db->Delete(write_options, "key1583");

  // op 34738: put
  db->Put(write_options, "key3708", "value47690");

  // op 34739: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2747", "key6991", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2747, key6991) size=" << scan_result.size() << std::endl;
  }

  // op 34740: get
  status = db->Get(read_options, "key5457", &value);
  if (status.ok()) {
    out << "GET key5457 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34741: put
  db->Put(write_options, "key7782", "value48960");

  // op 34742: delete_range
  status = db->DeleteRange(write_options, "key4006", "key11431");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 34743: put
  db->Put(write_options, "key7048", "value61877");

  // op 34744: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 34745: put
  db->Put(write_options, "key7830", "value71675");

  // op 34746: get
  status = db->Get(read_options, "key6704", &value);
  if (status.ok()) {
    out << "GET key6704 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34747: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4053", "key11090", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4053, key11090) size=" << scan_result.size() << std::endl;
  }

  // op 34748: get
  status = db->Get(read_options, "key7260", &value);
  if (status.ok()) {
    out << "GET key7260 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34749: put
  db->Put(write_options, "key2820", "value67815");

  // op 34750: delete
  db->Delete(write_options, "key3730");

  // op 34751: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1010", "key1773", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1010, key1773) size=" << scan_result.size() << std::endl;
  }

  // op 34752: get
  status = db->Get(read_options, "key0173", &value);
  if (status.ok()) {
    out << "GET key0173 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34753: put
  db->Put(write_options, "key6670", "value38273");

  // op 34754: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3497", "key7225", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3497, key7225) size=" << scan_result.size() << std::endl;
  }

  // op 34755: put
  db->Put(write_options, "key9568", "value68940");

  // op 34756: get
  status = db->Get(read_options, "key4438", &value);
  if (status.ok()) {
    out << "GET key4438 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34757: get
  status = db->Get(read_options, "key3358", &value);
  if (status.ok()) {
    out << "GET key3358 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34758: get
  status = db->Get(read_options, "key8703", &value);
  if (status.ok()) {
    out << "GET key8703 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34759: delete_range
  status = db->DeleteRange(write_options, "key3832", "key9187");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 34760: put
  db->Put(write_options, "key4364", "value8714");

  // op 34761: put
  db->Put(write_options, "key4117", "value117703");

  // op 34762: get
  status = db->Get(read_options, "key9919", &value);
  if (status.ok()) {
    out << "GET key9919 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34763: put
  db->Put(write_options, "key11488", "value68466");

  // op 34764: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6266", "key6275", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6266, key6275) size=" << scan_result.size() << std::endl;
  }

  // op 34765: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4950", "key6905", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4950, key6905) size=" << scan_result.size() << std::endl;
  }

  // op 34766: get
  status = db->Get(read_options, "key9388", &value);
  if (status.ok()) {
    out << "GET key9388 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34767: delete
  db->Delete(write_options, "key5717");

  // op 34768: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3650", "key8332", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3650, key8332) size=" << scan_result.size() << std::endl;
  }

  // op 34769: put
  db->Put(write_options, "key7627", "value111758");

  // op 34770: get
  status = db->Get(read_options, "key5836", &value);
  if (status.ok()) {
    out << "GET key5836 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34771: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8816", "key11148", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8816, key11148) size=" << scan_result.size() << std::endl;
  }

  // op 34772: put
  db->Put(write_options, "key8617", "value32124");

  // op 34773: delete_range
  status = db->DeleteRange(write_options, "key3966", "key5701");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 34774: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 34775: get
  status = db->Get(read_options, "key6118", &value);
  if (status.ok()) {
    out << "GET key6118 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34776: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6661", "key7962", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6661, key7962) size=" << scan_result.size() << std::endl;
  }

  // op 34777: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1665", "key11660", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1665, key11660) size=" << scan_result.size() << std::endl;
  }

  // op 34778: get
  status = db->Get(read_options, "key7250", &value);
  if (status.ok()) {
    out << "GET key7250 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34779: delete
  db->Delete(write_options, "key0045");

  // op 34780: delete_range
  status = db->DeleteRange(write_options, "key4632", "key8773");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 34781: put
  db->Put(write_options, "key11017", "value112265");

  // op 34782: put
  db->Put(write_options, "key3021", "value44647");

  // op 34783: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5759", "key7284", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5759, key7284) size=" << scan_result.size() << std::endl;
  }

  // op 34784: get
  status = db->Get(read_options, "key1628", &value);
  if (status.ok()) {
    out << "GET key1628 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34785: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6242", "key9689", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6242, key9689) size=" << scan_result.size() << std::endl;
  }

  // op 34786: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2270", "key2529", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2270, key2529) size=" << scan_result.size() << std::endl;
  }

  // op 34787: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0978", "key11055", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0978, key11055) size=" << scan_result.size() << std::endl;
  }

  // op 34788: get
  status = db->Get(read_options, "key0476", &value);
  if (status.ok()) {
    out << "GET key0476 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34789: get
  status = db->Get(read_options, "key3689", &value);
  if (status.ok()) {
    out << "GET key3689 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34790: put
  db->Put(write_options, "key3249", "value6877");

  // op 34791: put
  db->Put(write_options, "key8460", "value60597");

  // op 34792: get
  status = db->Get(read_options, "key4567", &value);
  if (status.ok()) {
    out << "GET key4567 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34793: put
  db->Put(write_options, "key11268", "value18540");

  // op 34794: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9055", "key9717", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9055, key9717) size=" << scan_result.size() << std::endl;
  }

  // op 34795: delete_range
  status = db->DeleteRange(write_options, "key2851", "key9609");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 34796: put
  db->Put(write_options, "key3794", "value17624");

  // op 34797: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5692", "key10131", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5692, key10131) size=" << scan_result.size() << std::endl;
  }

  // op 34798: delete_range
  status = db->DeleteRange(write_options, "key2753", "key5116");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 34799: delete
  db->Delete(write_options, "key3221");

  // op 34800: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1459", "key11072", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1459, key11072) size=" << scan_result.size() << std::endl;
  }

  // op 34801: put
  db->Put(write_options, "key0777", "value6128");

  // op 34802: put
  db->Put(write_options, "key11268", "value103709");

  // op 34803: put
  db->Put(write_options, "key11497", "value46677");

  // op 34804: put
  db->Put(write_options, "key3491", "value73158");

  // op 34805: put
  db->Put(write_options, "key4138", "value112219");

  // op 34806: get
  status = db->Get(read_options, "key9108", &value);
  if (status.ok()) {
    out << "GET key9108 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34807: delete
  db->Delete(write_options, "key2806");

  // op 34808: put
  db->Put(write_options, "key4266", "value6152");

  // op 34809: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0579", "key2383", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0579, key2383) size=" << scan_result.size() << std::endl;
  }

  // op 34810: get
  status = db->Get(read_options, "key5429", &value);
  if (status.ok()) {
    out << "GET key5429 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34811: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 34812: delete_range
  status = db->DeleteRange(write_options, "key2077", "key10143");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 34813: get
  status = db->Get(read_options, "key9139", &value);
  if (status.ok()) {
    out << "GET key9139 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34814: put
  db->Put(write_options, "key6918", "value109897");

  // op 34815: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5836", "key9970", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5836, key9970) size=" << scan_result.size() << std::endl;
  }

  // op 34816: delete_range
  status = db->DeleteRange(write_options, "key1935", "key8092");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 34817: put
  db->Put(write_options, "key2237", "value106859");

  // op 34818: delete
  db->Delete(write_options, "key2025");

  // op 34819: delete_range
  status = db->DeleteRange(write_options, "key8266", "key10329");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 34820: delete
  db->Delete(write_options, "key7524");

  // op 34821: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3834", "key9608", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3834, key9608) size=" << scan_result.size() << std::endl;
  }

  // op 34822: get
  status = db->Get(read_options, "key1551", &value);
  if (status.ok()) {
    out << "GET key1551 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34823: put
  db->Put(write_options, "key0200", "value7227");

  // op 34824: delete
  db->Delete(write_options, "key4804");

  // op 34825: delete_range
  status = db->DeleteRange(write_options, "key8565", "key11165");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 34826: delete
  db->Delete(write_options, "key1175");

  // op 34827: delete_range
  status = db->DeleteRange(write_options, "key4668", "key8965");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 34828: delete_range
  status = db->DeleteRange(write_options, "key1616", "key9785");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 34829: get
  status = db->Get(read_options, "key5017", &value);
  if (status.ok()) {
    out << "GET key5017 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34830: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0674", "key9501", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0674, key9501) size=" << scan_result.size() << std::endl;
  }

  // op 34831: get
  status = db->Get(read_options, "key2578", &value);
  if (status.ok()) {
    out << "GET key2578 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34832: put
  db->Put(write_options, "key9622", "value95005");

  // op 34833: put
  db->Put(write_options, "key4158", "value97687");

  // op 34834: put
  db->Put(write_options, "key4997", "value13134");

  // op 34835: put
  db->Put(write_options, "key9438", "value9675");

  // op 34836: get
  status = db->Get(read_options, "key2383", &value);
  if (status.ok()) {
    out << "GET key2383 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34837: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2173", "key9479", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2173, key9479) size=" << scan_result.size() << std::endl;
  }

  // op 34838: get
  status = db->Get(read_options, "key7823", &value);
  if (status.ok()) {
    out << "GET key7823 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34839: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6008", "key9116", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6008, key9116) size=" << scan_result.size() << std::endl;
  }

  // op 34840: put
  db->Put(write_options, "key7597", "value110297");

  // op 34841: put
  db->Put(write_options, "key5984", "value56087");

  // op 34842: get
  status = db->Get(read_options, "key5090", &value);
  if (status.ok()) {
    out << "GET key5090 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34843: delete
  db->Delete(write_options, "key0569");

  // op 34844: get
  status = db->Get(read_options, "key2410", &value);
  if (status.ok()) {
    out << "GET key2410 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34845: put
  db->Put(write_options, "key1513", "value4631");

  // op 34846: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8590", "key10250", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8590, key10250) size=" << scan_result.size() << std::endl;
  }

  // op 34847: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 34848: put
  db->Put(write_options, "key6442", "value92037");

  // op 34849: get
  status = db->Get(read_options, "key0097", &value);
  if (status.ok()) {
    out << "GET key0097 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34850: get
  status = db->Get(read_options, "key7556", &value);
  if (status.ok()) {
    out << "GET key7556 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34851: put
  db->Put(write_options, "key6606", "value14102");

  // op 34852: put
  db->Put(write_options, "key5976", "value3413");

  // op 34853: put
  db->Put(write_options, "key5570", "value116863");

  // op 34854: get
  status = db->Get(read_options, "key8950", &value);
  if (status.ok()) {
    out << "GET key8950 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34855: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0062", "key8840", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0062, key8840) size=" << scan_result.size() << std::endl;
  }

  // op 34856: put
  db->Put(write_options, "key4877", "value91091");

  // op 34857: delete_range
  status = db->DeleteRange(write_options, "key1202", "key3763");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 34858: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2546", "key8020", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2546, key8020) size=" << scan_result.size() << std::endl;
  }

  // op 34859: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0175", "key9637", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0175, key9637) size=" << scan_result.size() << std::endl;
  }

  // op 34860: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1558", "key9451", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1558, key9451) size=" << scan_result.size() << std::endl;
  }

  // op 34861: delete
  db->Delete(write_options, "key2543");

  // op 34862: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5732", "key10374", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5732, key10374) size=" << scan_result.size() << std::endl;
  }

  // op 34863: get
  status = db->Get(read_options, "key7228", &value);
  if (status.ok()) {
    out << "GET key7228 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34864: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1690", "key7910", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1690, key7910) size=" << scan_result.size() << std::endl;
  }

  // op 34865: delete_range
  status = db->DeleteRange(write_options, "key3435", "key11352");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 34866: delete
  db->Delete(write_options, "key4249");

  // op 34867: put
  db->Put(write_options, "key0662", "value72604");

  // op 34868: delete_range
  status = db->DeleteRange(write_options, "key1907", "key9114");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 34869: delete_range
  status = db->DeleteRange(write_options, "key8024", "key9480");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 34870: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4284", "key11309", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4284, key11309) size=" << scan_result.size() << std::endl;
  }

  // op 34871: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0986", "key9536", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0986, key9536) size=" << scan_result.size() << std::endl;
  }

  // op 34872: delete_range
  status = db->DeleteRange(write_options, "key2516", "key6358");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 34873: get
  status = db->Get(read_options, "key7486", &value);
  if (status.ok()) {
    out << "GET key7486 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34874: delete_range
  status = db->DeleteRange(write_options, "key2841", "key10304");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 34875: get
  status = db->Get(read_options, "key3939", &value);
  if (status.ok()) {
    out << "GET key3939 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34876: put
  db->Put(write_options, "key0545", "value26335");

  // op 34877: delete_range
  status = db->DeleteRange(write_options, "key9649", "key10004");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 34878: put
  db->Put(write_options, "key7976", "value49619");

  // op 34879: put
  db->Put(write_options, "key6791", "value19324");

  // op 34880: get
  status = db->Get(read_options, "key11232", &value);
  if (status.ok()) {
    out << "GET key11232 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34881: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2456", "key6820", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2456, key6820) size=" << scan_result.size() << std::endl;
  }

  // op 34882: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 34883: put
  db->Put(write_options, "key5676", "value63668");

  // op 34884: put
  db->Put(write_options, "key2347", "value3860");

  // op 34885: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1417", "key7200", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1417, key7200) size=" << scan_result.size() << std::endl;
  }

  // op 34886: get
  status = db->Get(read_options, "key5162", &value);
  if (status.ok()) {
    out << "GET key5162 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34887: put
  db->Put(write_options, "key7001", "value31312");

  // op 34888: delete
  db->Delete(write_options, "key8965");

  // op 34889: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8354", "key8953", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8354, key8953) size=" << scan_result.size() << std::endl;
  }

  // op 34890: put
  db->Put(write_options, "key3897", "value30614");

  // op 34891: put
  db->Put(write_options, "key6232", "value84230");

  // op 34892: get
  status = db->Get(read_options, "key11687", &value);
  if (status.ok()) {
    out << "GET key11687 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34893: get
  status = db->Get(read_options, "key8623", &value);
  if (status.ok()) {
    out << "GET key8623 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34894: put
  db->Put(write_options, "key10182", "value44355");

  // op 34895: put
  db->Put(write_options, "key0890", "value13660");

  // op 34896: get
  status = db->Get(read_options, "key9408", &value);
  if (status.ok()) {
    out << "GET key9408 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34897: delete
  db->Delete(write_options, "key1784");

  // op 34898: put
  db->Put(write_options, "key1734", "value61061");

  // op 34899: delete_range
  status = db->DeleteRange(write_options, "key2539", "key9714");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 34900: delete_range
  status = db->DeleteRange(write_options, "key8968", "key9598");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 34901: get
  status = db->Get(read_options, "key1168", &value);
  if (status.ok()) {
    out << "GET key1168 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34902: put
  db->Put(write_options, "key10497", "value94435");

  // op 34903: get
  status = db->Get(read_options, "key0472", &value);
  if (status.ok()) {
    out << "GET key0472 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34904: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 34905: delete_range
  status = db->DeleteRange(write_options, "key3501", "key6078");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 34906: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7407", "key9961", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7407, key9961) size=" << scan_result.size() << std::endl;
  }

  // op 34907: delete_range
  status = db->DeleteRange(write_options, "key8444", "key10735");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 34908: put
  db->Put(write_options, "key10027", "value13193");

  // op 34909: put
  db->Put(write_options, "key10813", "value19218");

  // op 34910: put
  db->Put(write_options, "key2422", "value77101");

  // op 34911: get
  status = db->Get(read_options, "key7146", &value);
  if (status.ok()) {
    out << "GET key7146 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34912: put
  db->Put(write_options, "key0738", "value97591");

  // op 34913: delete
  db->Delete(write_options, "key10079");

  // op 34914: put
  db->Put(write_options, "key11041", "value95802");

  // op 34915: put
  db->Put(write_options, "key9180", "value91499");

  // op 34916: put
  db->Put(write_options, "key10486", "value73945");

  // op 34917: put
  db->Put(write_options, "key7470", "value114350");

  // op 34918: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 34919: delete_range
  status = db->DeleteRange(write_options, "key0247", "key7386");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 34920: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1709", "key5823", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1709, key5823) size=" << scan_result.size() << std::endl;
  }

  // op 34921: delete
  db->Delete(write_options, "key7913");

  // op 34922: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7082", "key9137", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7082, key9137) size=" << scan_result.size() << std::endl;
  }

  // op 34923: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6696", "key11053", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6696, key11053) size=" << scan_result.size() << std::endl;
  }

  // op 34924: delete_range
  status = db->DeleteRange(write_options, "key10750", "key11284");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 34925: put
  db->Put(write_options, "key6971", "value40608");

  // op 34926: put
  db->Put(write_options, "key7456", "value46984");

  // op 34927: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 34928: get
  status = db->Get(read_options, "key5784", &value);
  if (status.ok()) {
    out << "GET key5784 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34929: put
  db->Put(write_options, "key7996", "value69806");

  // op 34930: delete_range
  status = db->DeleteRange(write_options, "key0808", "key5102");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 34931: put
  db->Put(write_options, "key7759", "value30072");

  // op 34932: delete_range
  status = db->DeleteRange(write_options, "key1305", "key7828");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 34933: delete
  db->Delete(write_options, "key11363");

  // op 34934: put
  db->Put(write_options, "key6873", "value12112");

  // op 34935: delete_range
  status = db->DeleteRange(write_options, "key0566", "key4299");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 34936: delete_range
  status = db->DeleteRange(write_options, "key5729", "key8014");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 34937: delete
  db->Delete(write_options, "key2546");

  // op 34938: put
  db->Put(write_options, "key4871", "value77614");

  // op 34939: get
  status = db->Get(read_options, "key2667", &value);
  if (status.ok()) {
    out << "GET key2667 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34940: get
  status = db->Get(read_options, "key9366", &value);
  if (status.ok()) {
    out << "GET key9366 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34941: delete
  db->Delete(write_options, "key1009");

  // op 34942: delete
  db->Delete(write_options, "key4726");

  // op 34943: put
  db->Put(write_options, "key6287", "value103534");

  // op 34944: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3784", "key9422", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3784, key9422) size=" << scan_result.size() << std::endl;
  }

  // op 34945: get
  status = db->Get(read_options, "key8289", &value);
  if (status.ok()) {
    out << "GET key8289 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34946: delete_range
  status = db->DeleteRange(write_options, "key0595", "key11705");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 34947: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0135", "key8379", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0135, key8379) size=" << scan_result.size() << std::endl;
  }

  // op 34948: delete
  db->Delete(write_options, "key10998");

  // op 34949: get
  status = db->Get(read_options, "key3763", &value);
  if (status.ok()) {
    out << "GET key3763 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34950: get
  status = db->Get(read_options, "key1750", &value);
  if (status.ok()) {
    out << "GET key1750 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34951: put
  db->Put(write_options, "key0317", "value28143");

  // op 34952: delete_range
  status = db->DeleteRange(write_options, "key3776", "key7486");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 34953: put
  db->Put(write_options, "key7003", "value23500");

  // op 34954: put
  db->Put(write_options, "key7147", "value97538");

  // op 34955: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6394", "key8760", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6394, key8760) size=" << scan_result.size() << std::endl;
  }

  // op 34956: put
  db->Put(write_options, "key1367", "value75815");

  // op 34957: put
  db->Put(write_options, "key2689", "value107114");

  // op 34958: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 34959: put
  db->Put(write_options, "key5719", "value69193");

  // op 34960: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 34961: delete
  db->Delete(write_options, "key7983");

  // op 34962: put
  db->Put(write_options, "key8245", "value2229");

  // op 34963: put
  db->Put(write_options, "key10011", "value112108");

  // op 34964: delete_range
  status = db->DeleteRange(write_options, "key7019", "key8818");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 34965: delete
  db->Delete(write_options, "key8372");

  // op 34966: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0025", "key11536", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0025, key11536) size=" << scan_result.size() << std::endl;
  }

  // op 34967: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 34968: delete_range
  status = db->DeleteRange(write_options, "key6080", "key7411");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 34969: put
  db->Put(write_options, "key10001", "value38241");

  // op 34970: delete_range
  status = db->DeleteRange(write_options, "key6363", "key11639");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 34971: delete_range
  status = db->DeleteRange(write_options, "key2293", "key4666");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 34972: put
  db->Put(write_options, "key8436", "value98676");

  // op 34973: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 34974: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5793", "key8800", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5793, key8800) size=" << scan_result.size() << std::endl;
  }

  // op 34975: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 34976: put
  db->Put(write_options, "key1595", "value82003");

  // op 34977: put
  db->Put(write_options, "key7433", "value29171");

  // op 34978: put
  db->Put(write_options, "key11283", "value38479");

  // op 34979: put
  db->Put(write_options, "key1477", "value91502");

  // op 34980: delete
  db->Delete(write_options, "key6481");

  // op 34981: put
  db->Put(write_options, "key1278", "value96447");

  // op 34982: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5693", "key11203", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5693, key11203) size=" << scan_result.size() << std::endl;
  }

  // op 34983: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1173", "key5121", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1173, key5121) size=" << scan_result.size() << std::endl;
  }

  // op 34984: delete
  db->Delete(write_options, "key3194");

  // op 34985: put
  db->Put(write_options, "key1279", "value0541");

  // op 34986: get
  status = db->Get(read_options, "key3962", &value);
  if (status.ok()) {
    out << "GET key3962 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34987: put
  db->Put(write_options, "key0390", "value23327");

  // op 34988: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5336", "key11614", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5336, key11614) size=" << scan_result.size() << std::endl;
  }

  // op 34989: get
  status = db->Get(read_options, "key5738", &value);
  if (status.ok()) {
    out << "GET key5738 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34990: put
  db->Put(write_options, "key5898", "value49903");

  // op 34991: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 34992: put
  db->Put(write_options, "key0821", "value117960");

  // op 34993: put
  db->Put(write_options, "key7764", "value68573");

  // op 34994: get
  status = db->Get(read_options, "key2389", &value);
  if (status.ok()) {
    out << "GET key2389 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34995: get
  status = db->Get(read_options, "key4587", &value);
  if (status.ok()) {
    out << "GET key4587 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 34996: delete
  db->Delete(write_options, "key6004");

  // op 34997: delete
  db->Delete(write_options, "key11500");

  // op 34998: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5619", "key9577", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5619, key9577) size=" << scan_result.size() << std::endl;
  }

  // op 34999: put
  db->Put(write_options, "key4455", "value63040");

  // op 35000: put
  db->Put(write_options, "key4114", "value25065");

  // op 35001: put
  db->Put(write_options, "key3986", "value41168");

  // op 35002: get
  status = db->Get(read_options, "key4613", &value);
  if (status.ok()) {
    out << "GET key4613 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35003: put
  db->Put(write_options, "key7901", "value15747");

  // op 35004: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0952", "key9011", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0952, key9011) size=" << scan_result.size() << std::endl;
  }

  // op 35005: put
  db->Put(write_options, "key9560", "value76449");

  // op 35006: delete
  db->Delete(write_options, "key6169");

  // op 35007: put
  db->Put(write_options, "key2860", "value60495");

  // op 35008: put
  db->Put(write_options, "key9532", "value91426");

  // op 35009: delete
  db->Delete(write_options, "key10620");

  // op 35010: put
  db->Put(write_options, "key0330", "value90219");

  // op 35011: delete
  db->Delete(write_options, "key6336");

  // op 35012: put
  db->Put(write_options, "key3232", "value7172");

  // op 35013: put
  db->Put(write_options, "key5124", "value49225");

  // op 35014: delete_range
  status = db->DeleteRange(write_options, "key5555", "key7282");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 35015: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 35016: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2036", "key2273", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2036, key2273) size=" << scan_result.size() << std::endl;
  }

  // op 35017: put
  db->Put(write_options, "key0044", "value65846");

  // op 35018: put
  db->Put(write_options, "key0582", "value31194");

  // op 35019: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 35020: put
  db->Put(write_options, "key3704", "value36965");

  // op 35021: get
  status = db->Get(read_options, "key8989", &value);
  if (status.ok()) {
    out << "GET key8989 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35022: put
  db->Put(write_options, "key3046", "value70600");

  // op 35023: delete_range
  status = db->DeleteRange(write_options, "key1306", "key2818");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 35024: delete
  db->Delete(write_options, "key1129");

  // op 35025: put
  db->Put(write_options, "key5882", "value45025");

  // op 35026: put
  db->Put(write_options, "key11331", "value55487");

  // op 35027: put
  db->Put(write_options, "key1760", "value35795");

  // op 35028: put
  db->Put(write_options, "key10566", "value33118");

  // op 35029: get
  status = db->Get(read_options, "key10623", &value);
  if (status.ok()) {
    out << "GET key10623 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35030: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4524", "key5506", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4524, key5506) size=" << scan_result.size() << std::endl;
  }

  // op 35031: put
  db->Put(write_options, "key3226", "value113360");

  // op 35032: put
  db->Put(write_options, "key3489", "value114047");

  // op 35033: delete_range
  status = db->DeleteRange(write_options, "key1742", "key6057");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 35034: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5660", "key7718", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5660, key7718) size=" << scan_result.size() << std::endl;
  }

  // op 35035: put
  db->Put(write_options, "key11359", "value82571");

  // op 35036: delete_range
  status = db->DeleteRange(write_options, "key7020", "key10474");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 35037: delete_range
  status = db->DeleteRange(write_options, "key4283", "key6153");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 35038: delete_range
  status = db->DeleteRange(write_options, "key2772", "key8803");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 35039: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2875", "key3059", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2875, key3059) size=" << scan_result.size() << std::endl;
  }

  // op 35040: put
  db->Put(write_options, "key2953", "value79263");

  // op 35041: put
  db->Put(write_options, "key2114", "value38040");

  // op 35042: get
  status = db->Get(read_options, "key1470", &value);
  if (status.ok()) {
    out << "GET key1470 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35043: put
  db->Put(write_options, "key6358", "value109452");

  // op 35044: get
  status = db->Get(read_options, "key3968", &value);
  if (status.ok()) {
    out << "GET key3968 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35045: get
  status = db->Get(read_options, "key0246", &value);
  if (status.ok()) {
    out << "GET key0246 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35046: put
  db->Put(write_options, "key8712", "value50392");

  // op 35047: put
  db->Put(write_options, "key9198", "value82723");

  // op 35048: put
  db->Put(write_options, "key7332", "value65157");

  // op 35049: put
  db->Put(write_options, "key8846", "value41469");

  // op 35050: put
  db->Put(write_options, "key6913", "value91101");

  // op 35051: put
  db->Put(write_options, "key5111", "value101779");

  // op 35052: put
  db->Put(write_options, "key6158", "value95683");

  // op 35053: put
  db->Put(write_options, "key9796", "value42182");

  // op 35054: delete
  db->Delete(write_options, "key6892");

  // op 35055: put
  db->Put(write_options, "key10710", "value39082");

  // op 35056: delete
  db->Delete(write_options, "key10131");

  // op 35057: get
  status = db->Get(read_options, "key8526", &value);
  if (status.ok()) {
    out << "GET key8526 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35058: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6378", "key9881", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6378, key9881) size=" << scan_result.size() << std::endl;
  }

  // op 35059: put
  db->Put(write_options, "key0853", "value14255");

  // op 35060: delete
  db->Delete(write_options, "key5024");

  // op 35061: put
  db->Put(write_options, "key10646", "value24827");

  // op 35062: get
  status = db->Get(read_options, "key10992", &value);
  if (status.ok()) {
    out << "GET key10992 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35063: get
  status = db->Get(read_options, "key0197", &value);
  if (status.ok()) {
    out << "GET key0197 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35064: delete_range
  status = db->DeleteRange(write_options, "key2134", "key11231");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 35065: delete_range
  status = db->DeleteRange(write_options, "key2013", "key3205");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 35066: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3529", "key3952", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3529, key3952) size=" << scan_result.size() << std::endl;
  }

  // op 35067: put
  db->Put(write_options, "key4849", "value112430");

  // op 35068: put
  db->Put(write_options, "key3285", "value98832");

  // op 35069: put
  db->Put(write_options, "key2928", "value15224");

  // op 35070: put
  db->Put(write_options, "key7108", "value49497");

  // op 35071: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6192", "key8338", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6192, key8338) size=" << scan_result.size() << std::endl;
  }

  // op 35072: get
  status = db->Get(read_options, "key8130", &value);
  if (status.ok()) {
    out << "GET key8130 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35073: put
  db->Put(write_options, "key6558", "value53814");

  // op 35074: delete
  db->Delete(write_options, "key8267");

  // op 35075: get
  status = db->Get(read_options, "key10191", &value);
  if (status.ok()) {
    out << "GET key10191 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35076: put
  db->Put(write_options, "key2563", "value42381");

  // op 35077: put
  db->Put(write_options, "key0126", "value104968");

  // op 35078: put
  db->Put(write_options, "key11705", "value59990");

  // op 35079: put
  db->Put(write_options, "key7394", "value115339");

  // op 35080: delete
  db->Delete(write_options, "key6816");

  // op 35081: put
  db->Put(write_options, "key10002", "value69282");

  // op 35082: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0727", "key10172", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0727, key10172) size=" << scan_result.size() << std::endl;
  }

  // op 35083: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2498", "key8896", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2498, key8896) size=" << scan_result.size() << std::endl;
  }

  // op 35084: delete_range
  status = db->DeleteRange(write_options, "key6878", "key8437");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 35085: put
  db->Put(write_options, "key1728", "value8869");

  // op 35086: delete_range
  status = db->DeleteRange(write_options, "key4126", "key5006");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 35087: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1840", "key2580", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1840, key2580) size=" << scan_result.size() << std::endl;
  }

  // op 35088: get
  status = db->Get(read_options, "key5809", &value);
  if (status.ok()) {
    out << "GET key5809 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35089: get
  status = db->Get(read_options, "key2210", &value);
  if (status.ok()) {
    out << "GET key2210 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35090: put
  db->Put(write_options, "key5591", "value4591");

  // op 35091: put
  db->Put(write_options, "key6260", "value2751");

  // op 35092: put
  db->Put(write_options, "key6079", "value46933");

  // op 35093: put
  db->Put(write_options, "key2058", "value9187");

  // op 35094: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6822", "key9421", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6822, key9421) size=" << scan_result.size() << std::endl;
  }

  // op 35095: delete_range
  status = db->DeleteRange(write_options, "key5392", "key11248");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 35096: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 35097: put
  db->Put(write_options, "key2763", "value75639");

  // op 35098: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4674", "key6900", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4674, key6900) size=" << scan_result.size() << std::endl;
  }

  // op 35099: get
  status = db->Get(read_options, "key8562", &value);
  if (status.ok()) {
    out << "GET key8562 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35100: get
  status = db->Get(read_options, "key8575", &value);
  if (status.ok()) {
    out << "GET key8575 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35101: put
  db->Put(write_options, "key11643", "value51696");

  // op 35102: put
  db->Put(write_options, "key9680", "value81239");

  // op 35103: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1737", "key6674", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1737, key6674) size=" << scan_result.size() << std::endl;
  }

  // op 35104: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 35105: put
  db->Put(write_options, "key7916", "value90925");

  // op 35106: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 35107: put
  db->Put(write_options, "key10541", "value70093");

  // op 35108: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1072", "key8693", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1072, key8693) size=" << scan_result.size() << std::endl;
  }

  // op 35109: delete
  db->Delete(write_options, "key2841");

  // op 35110: put
  db->Put(write_options, "key2359", "value62980");

  // op 35111: put
  db->Put(write_options, "key7176", "value58759");

  // op 35112: put
  db->Put(write_options, "key1844", "value39787");

  // op 35113: put
  db->Put(write_options, "key6996", "value33842");

  // op 35114: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4794", "key5390", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4794, key5390) size=" << scan_result.size() << std::endl;
  }

  // op 35115: get
  status = db->Get(read_options, "key8091", &value);
  if (status.ok()) {
    out << "GET key8091 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35116: delete_range
  status = db->DeleteRange(write_options, "key5343", "key10192");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 35117: get
  status = db->Get(read_options, "key3615", &value);
  if (status.ok()) {
    out << "GET key3615 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35118: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3841", "key3923", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3841, key3923) size=" << scan_result.size() << std::endl;
  }

  // op 35119: put
  db->Put(write_options, "key8812", "value74763");

  // op 35120: get
  status = db->Get(read_options, "key9171", &value);
  if (status.ok()) {
    out << "GET key9171 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35121: get
  status = db->Get(read_options, "key10851", &value);
  if (status.ok()) {
    out << "GET key10851 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35122: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4231", "key10312", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4231, key10312) size=" << scan_result.size() << std::endl;
  }

  // op 35123: get
  status = db->Get(read_options, "key1907", &value);
  if (status.ok()) {
    out << "GET key1907 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35124: put
  db->Put(write_options, "key8976", "value36950");

  // op 35125: delete
  db->Delete(write_options, "key2458");

  // op 35126: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 35127: put
  db->Put(write_options, "key7839", "value68714");

  // op 35128: delete
  db->Delete(write_options, "key1332");

  // op 35129: delete_range
  status = db->DeleteRange(write_options, "key3588", "key11379");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 35130: delete_range
  status = db->DeleteRange(write_options, "key1526", "key11440");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 35131: get
  status = db->Get(read_options, "key0637", &value);
  if (status.ok()) {
    out << "GET key0637 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35132: put
  db->Put(write_options, "key8086", "value11380");

  // op 35133: put
  db->Put(write_options, "key8686", "value58405");

  // op 35134: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5761", "key11191", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5761, key11191) size=" << scan_result.size() << std::endl;
  }

  // op 35135: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3950", "key6823", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3950, key6823) size=" << scan_result.size() << std::endl;
  }

  // op 35136: get
  status = db->Get(read_options, "key10729", &value);
  if (status.ok()) {
    out << "GET key10729 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35137: put
  db->Put(write_options, "key5508", "value107604");

  // op 35138: get
  status = db->Get(read_options, "key4673", &value);
  if (status.ok()) {
    out << "GET key4673 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35139: put
  db->Put(write_options, "key3398", "value48553");

  // op 35140: put
  db->Put(write_options, "key5242", "value103353");

  // op 35141: put
  db->Put(write_options, "key4629", "value13796");

  // op 35142: delete
  db->Delete(write_options, "key2523");

  // op 35143: put
  db->Put(write_options, "key0761", "value43579");

  // op 35144: put
  db->Put(write_options, "key6217", "value59635");

  // op 35145: put
  db->Put(write_options, "key10068", "value14750");

  // op 35146: delete
  db->Delete(write_options, "key11650");

  // op 35147: get
  status = db->Get(read_options, "key3397", &value);
  if (status.ok()) {
    out << "GET key3397 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35148: put
  db->Put(write_options, "key11344", "value101772");

  // op 35149: put
  db->Put(write_options, "key9895", "value91358");

  // op 35150: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 35151: put
  db->Put(write_options, "key9755", "value76881");

  // op 35152: put
  db->Put(write_options, "key10610", "value8652");

  // op 35153: put
  db->Put(write_options, "key9846", "value53782");

  // op 35154: get
  status = db->Get(read_options, "key7366", &value);
  if (status.ok()) {
    out << "GET key7366 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35155: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 35156: put
  db->Put(write_options, "key8079", "value116126");

  // op 35157: put
  db->Put(write_options, "key3529", "value29769");

  // op 35158: put
  db->Put(write_options, "key5762", "value15403");

  // op 35159: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8929", "key10742", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8929, key10742) size=" << scan_result.size() << std::endl;
  }

  // op 35160: put
  db->Put(write_options, "key11627", "value0088");

  // op 35161: put
  db->Put(write_options, "key10437", "value64376");

  // op 35162: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 35163: get
  status = db->Get(read_options, "key3277", &value);
  if (status.ok()) {
    out << "GET key3277 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35164: put
  db->Put(write_options, "key1371", "value10212");

  // op 35165: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1924", "key9381", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1924, key9381) size=" << scan_result.size() << std::endl;
  }

  // op 35166: delete
  db->Delete(write_options, "key0524");

  // op 35167: put
  db->Put(write_options, "key3879", "value100873");

  // op 35168: delete_range
  status = db->DeleteRange(write_options, "key0977", "key4991");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 35169: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 35170: get
  status = db->Get(read_options, "key1694", &value);
  if (status.ok()) {
    out << "GET key1694 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35171: delete
  db->Delete(write_options, "key3670");

  // op 35172: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 35173: get
  status = db->Get(read_options, "key10910", &value);
  if (status.ok()) {
    out << "GET key10910 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35174: put
  db->Put(write_options, "key3894", "value55915");

  // op 35175: get
  status = db->Get(read_options, "key5542", &value);
  if (status.ok()) {
    out << "GET key5542 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35176: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3748", "key11621", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3748, key11621) size=" << scan_result.size() << std::endl;
  }

  // op 35177: put
  db->Put(write_options, "key5138", "value89487");

  // op 35178: put
  db->Put(write_options, "key1105", "value35886");

  // op 35179: delete
  db->Delete(write_options, "key7970");

  // op 35180: put
  db->Put(write_options, "key8363", "value48591");

  // op 35181: get
  status = db->Get(read_options, "key10740", &value);
  if (status.ok()) {
    out << "GET key10740 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35182: delete
  db->Delete(write_options, "key1050");

  // op 35183: put
  db->Put(write_options, "key1097", "value103973");

  // op 35184: put
  db->Put(write_options, "key10856", "value94179");

  // op 35185: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6863", "key7702", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6863, key7702) size=" << scan_result.size() << std::endl;
  }

  // op 35186: put
  db->Put(write_options, "key4798", "value31205");

  // op 35187: put
  db->Put(write_options, "key6887", "value95831");

  // op 35188: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1982", "key5562", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1982, key5562) size=" << scan_result.size() << std::endl;
  }

  // op 35189: put
  db->Put(write_options, "key1454", "value7883");

  // op 35190: delete
  db->Delete(write_options, "key4560");

  // op 35191: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 35192: put
  db->Put(write_options, "key10115", "value26211");

  // op 35193: delete_range
  status = db->DeleteRange(write_options, "key5143", "key5665");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 35194: put
  db->Put(write_options, "key11108", "value34305");

  // op 35195: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3361", "key8477", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3361, key8477) size=" << scan_result.size() << std::endl;
  }

  // op 35196: get
  status = db->Get(read_options, "key0203", &value);
  if (status.ok()) {
    out << "GET key0203 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35197: delete
  db->Delete(write_options, "key10734");

  // op 35198: put
  db->Put(write_options, "key7572", "value8588");

  // op 35199: get
  status = db->Get(read_options, "key0660", &value);
  if (status.ok()) {
    out << "GET key0660 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35200: get
  status = db->Get(read_options, "key0255", &value);
  if (status.ok()) {
    out << "GET key0255 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35201: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1401", "key4067", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1401, key4067) size=" << scan_result.size() << std::endl;
  }

  // op 35202: put
  db->Put(write_options, "key11497", "value88925");

  // op 35203: put
  db->Put(write_options, "key1174", "value59710");

  // op 35204: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7326", "key8380", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7326, key8380) size=" << scan_result.size() << std::endl;
  }

  // op 35205: put
  db->Put(write_options, "key11561", "value114541");

  // op 35206: put
  db->Put(write_options, "key4861", "value55134");

  // op 35207: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0718", "key6294", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0718, key6294) size=" << scan_result.size() << std::endl;
  }

  // op 35208: put
  db->Put(write_options, "key11712", "value82276");

  // op 35209: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0413", "key5819", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0413, key5819) size=" << scan_result.size() << std::endl;
  }

  // op 35210: delete
  db->Delete(write_options, "key8081");

  // op 35211: put
  db->Put(write_options, "key2895", "value59697");

  // op 35212: put
  db->Put(write_options, "key6552", "value59186");

  // op 35213: put
  db->Put(write_options, "key9690", "value38247");

  // op 35214: put
  db->Put(write_options, "key5246", "value117258");

  // op 35215: delete_range
  status = db->DeleteRange(write_options, "key0394", "key0604");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 35216: put
  db->Put(write_options, "key4470", "value56059");

  // op 35217: delete
  db->Delete(write_options, "key4181");

  // op 35218: get
  status = db->Get(read_options, "key1568", &value);
  if (status.ok()) {
    out << "GET key1568 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35219: delete
  db->Delete(write_options, "key2732");

  // op 35220: delete_range
  status = db->DeleteRange(write_options, "key8079", "key10974");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 35221: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 35222: put
  db->Put(write_options, "key3054", "value83246");

  // op 35223: put
  db->Put(write_options, "key7060", "value7464");

  // op 35224: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5032", "key11692", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5032, key11692) size=" << scan_result.size() << std::endl;
  }

  // op 35225: delete
  db->Delete(write_options, "key11659");

  // op 35226: delete_range
  status = db->DeleteRange(write_options, "key3458", "key8952");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 35227: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 35228: put
  db->Put(write_options, "key6350", "value77207");

  // op 35229: get
  status = db->Get(read_options, "key8750", &value);
  if (status.ok()) {
    out << "GET key8750 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35230: get
  status = db->Get(read_options, "key0866", &value);
  if (status.ok()) {
    out << "GET key0866 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35231: put
  db->Put(write_options, "key0857", "value37765");

  // op 35232: get
  status = db->Get(read_options, "key5393", &value);
  if (status.ok()) {
    out << "GET key5393 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35233: get
  status = db->Get(read_options, "key10840", &value);
  if (status.ok()) {
    out << "GET key10840 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35234: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1600", "key6200", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1600, key6200) size=" << scan_result.size() << std::endl;
  }

  // op 35235: get
  status = db->Get(read_options, "key1292", &value);
  if (status.ok()) {
    out << "GET key1292 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35236: put
  db->Put(write_options, "key9130", "value98093");

  // op 35237: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3954", "key10403", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3954, key10403) size=" << scan_result.size() << std::endl;
  }

  // op 35238: delete
  db->Delete(write_options, "key10360");

  // op 35239: get
  status = db->Get(read_options, "key4426", &value);
  if (status.ok()) {
    out << "GET key4426 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35240: delete_range
  status = db->DeleteRange(write_options, "key3665", "key6351");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 35241: put
  db->Put(write_options, "key0974", "value40900");

  // op 35242: put
  db->Put(write_options, "key7712", "value28694");

  // op 35243: get
  status = db->Get(read_options, "key11691", &value);
  if (status.ok()) {
    out << "GET key11691 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35244: put
  db->Put(write_options, "key2994", "value107821");

  // op 35245: delete_range
  status = db->DeleteRange(write_options, "key3010", "key8251");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 35246: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1638", "key8645", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1638, key8645) size=" << scan_result.size() << std::endl;
  }

  // op 35247: delete
  db->Delete(write_options, "key11183");

  // op 35248: get
  status = db->Get(read_options, "key1869", &value);
  if (status.ok()) {
    out << "GET key1869 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35249: delete
  db->Delete(write_options, "key1433");

  // op 35250: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 35251: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0858", "key7712", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0858, key7712) size=" << scan_result.size() << std::endl;
  }

  // op 35252: put
  db->Put(write_options, "key2125", "value74292");

  // op 35253: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0209", "key10431", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0209, key10431) size=" << scan_result.size() << std::endl;
  }

  // op 35254: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3921", "key11415", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3921, key11415) size=" << scan_result.size() << std::endl;
  }

  // op 35255: get
  status = db->Get(read_options, "key10224", &value);
  if (status.ok()) {
    out << "GET key10224 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35256: put
  db->Put(write_options, "key7643", "value110243");

  // op 35257: delete_range
  status = db->DeleteRange(write_options, "key4699", "key10600");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 35258: get
  status = db->Get(read_options, "key9822", &value);
  if (status.ok()) {
    out << "GET key9822 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35259: get
  status = db->Get(read_options, "key9952", &value);
  if (status.ok()) {
    out << "GET key9952 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35260: put
  db->Put(write_options, "key10822", "value96317");

  // op 35261: put
  db->Put(write_options, "key0735", "value78510");

  // op 35262: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3401", "key3710", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3401, key3710) size=" << scan_result.size() << std::endl;
  }

  // op 35263: get
  status = db->Get(read_options, "key4634", &value);
  if (status.ok()) {
    out << "GET key4634 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35264: get
  status = db->Get(read_options, "key7616", &value);
  if (status.ok()) {
    out << "GET key7616 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35265: put
  db->Put(write_options, "key8335", "value76342");

  // op 35266: put
  db->Put(write_options, "key5569", "value107150");

  // op 35267: put
  db->Put(write_options, "key1442", "value20760");

  // op 35268: get
  status = db->Get(read_options, "key1367", &value);
  if (status.ok()) {
    out << "GET key1367 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35269: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4788", "key11039", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4788, key11039) size=" << scan_result.size() << std::endl;
  }

  // op 35270: put
  db->Put(write_options, "key3005", "value50370");

  // op 35271: put
  db->Put(write_options, "key9254", "value16447");

  // op 35272: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4937", "key6573", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4937, key6573) size=" << scan_result.size() << std::endl;
  }

  // op 35273: get
  status = db->Get(read_options, "key4584", &value);
  if (status.ok()) {
    out << "GET key4584 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35274: put
  db->Put(write_options, "key9392", "value79373");

  // op 35275: put
  db->Put(write_options, "key4248", "value116753");

  // op 35276: delete
  db->Delete(write_options, "key9577");

  // op 35277: put
  db->Put(write_options, "key2048", "value40341");

  // op 35278: delete_range
  status = db->DeleteRange(write_options, "key2698", "key10462");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 35279: put
  db->Put(write_options, "key8459", "value69770");

  // op 35280: put
  db->Put(write_options, "key3222", "value68580");

  // op 35281: get
  status = db->Get(read_options, "key11234", &value);
  if (status.ok()) {
    out << "GET key11234 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35282: get
  status = db->Get(read_options, "key0113", &value);
  if (status.ok()) {
    out << "GET key0113 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35283: delete_range
  status = db->DeleteRange(write_options, "key4281", "key5838");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 35284: put
  db->Put(write_options, "key3683", "value34674");

  // op 35285: delete_range
  status = db->DeleteRange(write_options, "key2678", "key6882");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 35286: put
  db->Put(write_options, "key9590", "value55668");

  // op 35287: put
  db->Put(write_options, "key0566", "value33718");

  // op 35288: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 35289: put
  db->Put(write_options, "key2789", "value28156");

  // op 35290: get
  status = db->Get(read_options, "key9258", &value);
  if (status.ok()) {
    out << "GET key9258 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35291: delete
  db->Delete(write_options, "key5153");

  // op 35292: delete
  db->Delete(write_options, "key3911");

  // op 35293: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 35294: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10974", "key11213", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10974, key11213) size=" << scan_result.size() << std::endl;
  }

  // op 35295: delete
  db->Delete(write_options, "key2119");

  // op 35296: get
  status = db->Get(read_options, "key1278", &value);
  if (status.ok()) {
    out << "GET key1278 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35297: put
  db->Put(write_options, "key0219", "value83175");

  // op 35298: put
  db->Put(write_options, "key4023", "value93296");

  // op 35299: get
  status = db->Get(read_options, "key2536", &value);
  if (status.ok()) {
    out << "GET key2536 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35300: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3872", "key9845", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3872, key9845) size=" << scan_result.size() << std::endl;
  }

  // op 35301: put
  db->Put(write_options, "key4115", "value26215");

  // op 35302: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5853", "key6563", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5853, key6563) size=" << scan_result.size() << std::endl;
  }

  // op 35303: get
  status = db->Get(read_options, "key10123", &value);
  if (status.ok()) {
    out << "GET key10123 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35304: delete
  db->Delete(write_options, "key7273");

  // op 35305: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 35306: get
  status = db->Get(read_options, "key9101", &value);
  if (status.ok()) {
    out << "GET key9101 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35307: put
  db->Put(write_options, "key3534", "value50860");

  // op 35308: get
  status = db->Get(read_options, "key11265", &value);
  if (status.ok()) {
    out << "GET key11265 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35309: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0147", "key4904", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0147, key4904) size=" << scan_result.size() << std::endl;
  }

  // op 35310: delete_range
  status = db->DeleteRange(write_options, "key4812", "key6145");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 35311: delete_range
  status = db->DeleteRange(write_options, "key3680", "key7709");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 35312: delete_range
  status = db->DeleteRange(write_options, "key5374", "key7883");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 35313: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1789", "key5317", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1789, key5317) size=" << scan_result.size() << std::endl;
  }

  // op 35314: get
  status = db->Get(read_options, "key4447", &value);
  if (status.ok()) {
    out << "GET key4447 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35315: get
  status = db->Get(read_options, "key1301", &value);
  if (status.ok()) {
    out << "GET key1301 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35316: get
  status = db->Get(read_options, "key8367", &value);
  if (status.ok()) {
    out << "GET key8367 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35317: put
  db->Put(write_options, "key11052", "value100492");

  // op 35318: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3543", "key10126", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3543, key10126) size=" << scan_result.size() << std::endl;
  }

  // op 35319: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 35320: delete_range
  status = db->DeleteRange(write_options, "key0114", "key4734");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 35321: delete
  db->Delete(write_options, "key1703");

  // op 35322: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 35323: get
  status = db->Get(read_options, "key11397", &value);
  if (status.ok()) {
    out << "GET key11397 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35324: get
  status = db->Get(read_options, "key6065", &value);
  if (status.ok()) {
    out << "GET key6065 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35325: delete_range
  status = db->DeleteRange(write_options, "key7228", "key9588");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 35326: get
  status = db->Get(read_options, "key7719", &value);
  if (status.ok()) {
    out << "GET key7719 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35327: put
  db->Put(write_options, "key10516", "value90948");

  // op 35328: put
  db->Put(write_options, "key8190", "value53758");

  // op 35329: put
  db->Put(write_options, "key3576", "value41096");

  // op 35330: get
  status = db->Get(read_options, "key4071", &value);
  if (status.ok()) {
    out << "GET key4071 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35331: get
  status = db->Get(read_options, "key5700", &value);
  if (status.ok()) {
    out << "GET key5700 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35332: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 35333: put
  db->Put(write_options, "key1868", "value40267");

  // op 35334: get
  status = db->Get(read_options, "key8644", &value);
  if (status.ok()) {
    out << "GET key8644 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35335: delete_range
  status = db->DeleteRange(write_options, "key1394", "key8332");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 35336: get
  status = db->Get(read_options, "key0963", &value);
  if (status.ok()) {
    out << "GET key0963 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35337: delete
  db->Delete(write_options, "key2513");

  // op 35338: put
  db->Put(write_options, "key9563", "value56099");

  // op 35339: delete_range
  status = db->DeleteRange(write_options, "key1018", "key1709");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 35340: put
  db->Put(write_options, "key10372", "value17649");

  // op 35341: put
  db->Put(write_options, "key0832", "value81608");

  // op 35342: get
  status = db->Get(read_options, "key5464", &value);
  if (status.ok()) {
    out << "GET key5464 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35343: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 35344: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5934", "key6123", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5934, key6123) size=" << scan_result.size() << std::endl;
  }

  // op 35345: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5922", "key10822", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5922, key10822) size=" << scan_result.size() << std::endl;
  }

  // op 35346: get
  status = db->Get(read_options, "key11693", &value);
  if (status.ok()) {
    out << "GET key11693 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35347: get
  status = db->Get(read_options, "key9004", &value);
  if (status.ok()) {
    out << "GET key9004 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35348: delete_range
  status = db->DeleteRange(write_options, "key3510", "key9202");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 35349: delete_range
  status = db->DeleteRange(write_options, "key1087", "key1864");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 35350: put
  db->Put(write_options, "key0889", "value57555");

  // op 35351: put
  db->Put(write_options, "key10128", "value103230");

  // op 35352: put
  db->Put(write_options, "key8101", "value113735");

  // op 35353: put
  db->Put(write_options, "key4852", "value10206");

  // op 35354: delete_range
  status = db->DeleteRange(write_options, "key2444", "key11562");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 35355: put
  db->Put(write_options, "key5195", "value110608");

  // op 35356: put
  db->Put(write_options, "key7691", "value67188");

  // op 35357: get
  status = db->Get(read_options, "key7745", &value);
  if (status.ok()) {
    out << "GET key7745 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35358: put
  db->Put(write_options, "key0498", "value10227");

  // op 35359: put
  db->Put(write_options, "key7669", "value28927");

  // op 35360: delete_range
  status = db->DeleteRange(write_options, "key0472", "key3777");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 35361: get
  status = db->Get(read_options, "key3048", &value);
  if (status.ok()) {
    out << "GET key3048 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35362: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 35363: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 35364: put
  db->Put(write_options, "key7755", "value50772");

  // op 35365: get
  status = db->Get(read_options, "key10890", &value);
  if (status.ok()) {
    out << "GET key10890 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35366: delete_range
  status = db->DeleteRange(write_options, "key2596", "key3740");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 35367: get
  status = db->Get(read_options, "key9180", &value);
  if (status.ok()) {
    out << "GET key9180 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35368: put
  db->Put(write_options, "key6053", "value65250");

  // op 35369: put
  db->Put(write_options, "key6259", "value85737");

  // op 35370: delete
  db->Delete(write_options, "key8916");

  // op 35371: get
  status = db->Get(read_options, "key9884", &value);
  if (status.ok()) {
    out << "GET key9884 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35372: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1875", "key10665", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1875, key10665) size=" << scan_result.size() << std::endl;
  }

  // op 35373: put
  db->Put(write_options, "key10541", "value95267");

  // op 35374: put
  db->Put(write_options, "key7006", "value90663");

  // op 35375: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 35376: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5358", "key8060", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5358, key8060) size=" << scan_result.size() << std::endl;
  }

  // op 35377: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3774", "key11549", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3774, key11549) size=" << scan_result.size() << std::endl;
  }

  // op 35378: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 35379: put
  db->Put(write_options, "key2612", "value16136");

  // op 35380: put
  db->Put(write_options, "key0196", "value108576");

  // op 35381: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8645", "key9488", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8645, key9488) size=" << scan_result.size() << std::endl;
  }

  // op 35382: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0672", "key4330", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0672, key4330) size=" << scan_result.size() << std::endl;
  }

  // op 35383: get
  status = db->Get(read_options, "key1220", &value);
  if (status.ok()) {
    out << "GET key1220 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35384: get
  status = db->Get(read_options, "key8503", &value);
  if (status.ok()) {
    out << "GET key8503 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35385: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4290", "key11091", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4290, key11091) size=" << scan_result.size() << std::endl;
  }

  // op 35386: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4347", "key11656", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4347, key11656) size=" << scan_result.size() << std::endl;
  }

  // op 35387: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 35388: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 35389: put
  db->Put(write_options, "key8456", "value59791");

  // op 35390: put
  db->Put(write_options, "key4665", "value6282");

  // op 35391: put
  db->Put(write_options, "key11564", "value87589");

  // op 35392: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4769", "key10372", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4769, key10372) size=" << scan_result.size() << std::endl;
  }

  // op 35393: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2641", "key3680", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2641, key3680) size=" << scan_result.size() << std::endl;
  }

  // op 35394: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4819", "key8295", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4819, key8295) size=" << scan_result.size() << std::endl;
  }

  // op 35395: put
  db->Put(write_options, "key5921", "value54223");

  // op 35396: get
  status = db->Get(read_options, "key1256", &value);
  if (status.ok()) {
    out << "GET key1256 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35397: get
  status = db->Get(read_options, "key3595", &value);
  if (status.ok()) {
    out << "GET key3595 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35398: put
  db->Put(write_options, "key7443", "value115384");

  // op 35399: get
  status = db->Get(read_options, "key9957", &value);
  if (status.ok()) {
    out << "GET key9957 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35400: delete_range
  status = db->DeleteRange(write_options, "key4228", "key11146");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 35401: put
  db->Put(write_options, "key9714", "value55131");

  // op 35402: get
  status = db->Get(read_options, "key0399", &value);
  if (status.ok()) {
    out << "GET key0399 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35403: delete_range
  status = db->DeleteRange(write_options, "key5795", "key5908");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 35404: put
  db->Put(write_options, "key0245", "value70423");

  // op 35405: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5503", "key10137", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5503, key10137) size=" << scan_result.size() << std::endl;
  }

  // op 35406: delete
  db->Delete(write_options, "key8229");

  // op 35407: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3930", "key4760", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3930, key4760) size=" << scan_result.size() << std::endl;
  }

  // op 35408: get
  status = db->Get(read_options, "key10320", &value);
  if (status.ok()) {
    out << "GET key10320 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35409: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 35410: put
  db->Put(write_options, "key9945", "value6121");

  // op 35411: put
  db->Put(write_options, "key4225", "value113240");

  // op 35412: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2000", "key8834", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2000, key8834) size=" << scan_result.size() << std::endl;
  }

  // op 35413: delete_range
  status = db->DeleteRange(write_options, "key0020", "key0521");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 35414: delete_range
  status = db->DeleteRange(write_options, "key0981", "key8994");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 35415: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6419", "key8680", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6419, key8680) size=" << scan_result.size() << std::endl;
  }

  // op 35416: delete
  db->Delete(write_options, "key4838");

  // op 35417: put
  db->Put(write_options, "key8398", "value117069");

  // op 35418: put
  db->Put(write_options, "key6525", "value108846");

  // op 35419: get
  status = db->Get(read_options, "key8620", &value);
  if (status.ok()) {
    out << "GET key8620 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35420: delete
  db->Delete(write_options, "key7528");

  // op 35421: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5124", "key9907", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5124, key9907) size=" << scan_result.size() << std::endl;
  }

  // op 35422: delete_range
  status = db->DeleteRange(write_options, "key10291", "key11597");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 35423: delete
  db->Delete(write_options, "key0806");

  // op 35424: put
  db->Put(write_options, "key3517", "value11615");

  // op 35425: delete
  db->Delete(write_options, "key1502");

  // op 35426: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 35427: get
  status = db->Get(read_options, "key4768", &value);
  if (status.ok()) {
    out << "GET key4768 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35428: get
  status = db->Get(read_options, "key7750", &value);
  if (status.ok()) {
    out << "GET key7750 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35429: put
  db->Put(write_options, "key4461", "value36328");

  // op 35430: delete
  db->Delete(write_options, "key5411");

  // op 35431: delete
  db->Delete(write_options, "key4258");

  // op 35432: delete_range
  status = db->DeleteRange(write_options, "key0241", "key7105");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 35433: put
  db->Put(write_options, "key8523", "value95322");

  // op 35434: put
  db->Put(write_options, "key9503", "value81198");

  // op 35435: delete_range
  status = db->DeleteRange(write_options, "key5678", "key6371");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 35436: put
  db->Put(write_options, "key6109", "value76896");

  // op 35437: delete_range
  status = db->DeleteRange(write_options, "key0084", "key0404");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 35438: put
  db->Put(write_options, "key6998", "value78680");

  // op 35439: get
  status = db->Get(read_options, "key10707", &value);
  if (status.ok()) {
    out << "GET key10707 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35440: delete
  db->Delete(write_options, "key9613");

  // op 35441: put
  db->Put(write_options, "key1523", "value39608");

  // op 35442: put
  db->Put(write_options, "key10655", "value28586");

  // op 35443: put
  db->Put(write_options, "key9152", "value3918");

  // op 35444: put
  db->Put(write_options, "key3644", "value113166");

  // op 35445: put
  db->Put(write_options, "key9653", "value79746");

  // op 35446: put
  db->Put(write_options, "key11588", "value45481");

  // op 35447: put
  db->Put(write_options, "key1792", "value25932");

  // op 35448: put
  db->Put(write_options, "key6225", "value16794");

  // op 35449: get
  status = db->Get(read_options, "key3275", &value);
  if (status.ok()) {
    out << "GET key3275 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35450: put
  db->Put(write_options, "key0801", "value109827");

  // op 35451: put
  db->Put(write_options, "key9881", "value5978");

  // op 35452: get
  status = db->Get(read_options, "key11600", &value);
  if (status.ok()) {
    out << "GET key11600 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35453: get
  status = db->Get(read_options, "key4473", &value);
  if (status.ok()) {
    out << "GET key4473 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35454: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0202", "key9060", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0202, key9060) size=" << scan_result.size() << std::endl;
  }

  // op 35455: delete_range
  status = db->DeleteRange(write_options, "key0395", "key10057");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 35456: put
  db->Put(write_options, "key8576", "value74148");

  // op 35457: delete_range
  status = db->DeleteRange(write_options, "key1318", "key10741");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 35458: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 35459: put
  db->Put(write_options, "key4195", "value1440");

  // op 35460: delete_range
  status = db->DeleteRange(write_options, "key1444", "key2447");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 35461: delete
  db->Delete(write_options, "key4553");

  // op 35462: put
  db->Put(write_options, "key3230", "value78836");

  // op 35463: put
  db->Put(write_options, "key10498", "value108272");

  // op 35464: delete
  db->Delete(write_options, "key8481");

  // op 35465: get
  status = db->Get(read_options, "key8315", &value);
  if (status.ok()) {
    out << "GET key8315 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35466: put
  db->Put(write_options, "key9349", "value17766");

  // op 35467: put
  db->Put(write_options, "key1461", "value4129");

  // op 35468: put
  db->Put(write_options, "key0914", "value65153");

  // op 35469: put
  db->Put(write_options, "key7599", "value48561");

  // op 35470: put
  db->Put(write_options, "key11210", "value47674");

  // op 35471: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1459", "key8672", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1459, key8672) size=" << scan_result.size() << std::endl;
  }

  // op 35472: delete_range
  status = db->DeleteRange(write_options, "key2422", "key6240");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 35473: put
  db->Put(write_options, "key11404", "value19221");

  // op 35474: put
  db->Put(write_options, "key2953", "value18641");

  // op 35475: delete
  db->Delete(write_options, "key2769");

  // op 35476: put
  db->Put(write_options, "key0464", "value59985");

  // op 35477: put
  db->Put(write_options, "key8257", "value0354");

  // op 35478: get
  status = db->Get(read_options, "key11499", &value);
  if (status.ok()) {
    out << "GET key11499 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35479: put
  db->Put(write_options, "key10602", "value69211");

  // op 35480: put
  db->Put(write_options, "key11552", "value45988");

  // op 35481: put
  db->Put(write_options, "key7924", "value77457");

  // op 35482: put
  db->Put(write_options, "key2174", "value47159");

  // op 35483: put
  db->Put(write_options, "key6974", "value83454");

  // op 35484: get
  status = db->Get(read_options, "key11253", &value);
  if (status.ok()) {
    out << "GET key11253 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35485: delete
  db->Delete(write_options, "key6503");

  // op 35486: delete
  db->Delete(write_options, "key1729");

  // op 35487: put
  db->Put(write_options, "key7959", "value68703");

  // op 35488: put
  db->Put(write_options, "key6668", "value52357");

  // op 35489: get
  status = db->Get(read_options, "key6315", &value);
  if (status.ok()) {
    out << "GET key6315 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35490: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1896", "key10075", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1896, key10075) size=" << scan_result.size() << std::endl;
  }

  // op 35491: put
  db->Put(write_options, "key6214", "value81796");

  // op 35492: put
  db->Put(write_options, "key1215", "value1419");

  // op 35493: get
  status = db->Get(read_options, "key4418", &value);
  if (status.ok()) {
    out << "GET key4418 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35494: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7211", "key10835", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7211, key10835) size=" << scan_result.size() << std::endl;
  }

  // op 35495: get
  status = db->Get(read_options, "key4226", &value);
  if (status.ok()) {
    out << "GET key4226 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35496: delete
  db->Delete(write_options, "key6193");

  // op 35497: delete
  db->Delete(write_options, "key8086");

  // op 35498: get
  status = db->Get(read_options, "key8514", &value);
  if (status.ok()) {
    out << "GET key8514 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35499: put
  db->Put(write_options, "key7585", "value55176");

  // op 35500: delete_range
  status = db->DeleteRange(write_options, "key4043", "key8674");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 35501: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4248", "key6966", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4248, key6966) size=" << scan_result.size() << std::endl;
  }

  // op 35502: put
  db->Put(write_options, "key0332", "value113084");

  // op 35503: put
  db->Put(write_options, "key4549", "value108695");

  // op 35504: put
  db->Put(write_options, "key7734", "value19365");

  // op 35505: get
  status = db->Get(read_options, "key4027", &value);
  if (status.ok()) {
    out << "GET key4027 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35506: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5480", "key10757", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5480, key10757) size=" << scan_result.size() << std::endl;
  }

  // op 35507: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4985", "key8258", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4985, key8258) size=" << scan_result.size() << std::endl;
  }

  // op 35508: delete_range
  status = db->DeleteRange(write_options, "key3291", "key5457");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 35509: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 35510: get
  status = db->Get(read_options, "key10564", &value);
  if (status.ok()) {
    out << "GET key10564 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35511: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 35512: delete
  db->Delete(write_options, "key9459");

  // op 35513: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2407", "key7623", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2407, key7623) size=" << scan_result.size() << std::endl;
  }

  // op 35514: put
  db->Put(write_options, "key7594", "value33189");

  // op 35515: delete
  db->Delete(write_options, "key10798");

  // op 35516: delete_range
  status = db->DeleteRange(write_options, "key0267", "key7581");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 35517: get
  status = db->Get(read_options, "key2282", &value);
  if (status.ok()) {
    out << "GET key2282 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35518: put
  db->Put(write_options, "key0701", "value64008");

  // op 35519: put
  db->Put(write_options, "key6669", "value77641");

  // op 35520: put
  db->Put(write_options, "key8189", "value61589");

  // op 35521: put
  db->Put(write_options, "key6851", "value5414");

  // op 35522: get
  status = db->Get(read_options, "key11394", &value);
  if (status.ok()) {
    out << "GET key11394 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35523: get
  status = db->Get(read_options, "key10337", &value);
  if (status.ok()) {
    out << "GET key10337 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35524: delete_range
  status = db->DeleteRange(write_options, "key5087", "key8303");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 35525: get
  status = db->Get(read_options, "key6300", &value);
  if (status.ok()) {
    out << "GET key6300 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35526: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1611", "key10465", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1611, key10465) size=" << scan_result.size() << std::endl;
  }

  // op 35527: put
  db->Put(write_options, "key2815", "value105857");

  // op 35528: put
  db->Put(write_options, "key8762", "value41869");

  // op 35529: put
  db->Put(write_options, "key9178", "value57186");

  // op 35530: delete_range
  status = db->DeleteRange(write_options, "key0446", "key1043");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 35531: delete_range
  status = db->DeleteRange(write_options, "key9665", "key11132");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 35532: put
  db->Put(write_options, "key10415", "value64278");

  // op 35533: put
  db->Put(write_options, "key8155", "value104243");

  // op 35534: put
  db->Put(write_options, "key5281", "value17204");

  // op 35535: put
  db->Put(write_options, "key11217", "value108522");

  // op 35536: delete_range
  status = db->DeleteRange(write_options, "key0899", "key4021");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 35537: put
  db->Put(write_options, "key7401", "value72413");

  // op 35538: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8023", "key8096", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8023, key8096) size=" << scan_result.size() << std::endl;
  }

  // op 35539: get
  status = db->Get(read_options, "key3534", &value);
  if (status.ok()) {
    out << "GET key3534 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35540: put
  db->Put(write_options, "key10827", "value38425");

  // op 35541: delete
  db->Delete(write_options, "key1789");

  // op 35542: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3245", "key3500", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3245, key3500) size=" << scan_result.size() << std::endl;
  }

  // op 35543: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4966", "key5335", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4966, key5335) size=" << scan_result.size() << std::endl;
  }

  // op 35544: get
  status = db->Get(read_options, "key2040", &value);
  if (status.ok()) {
    out << "GET key2040 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35545: put
  db->Put(write_options, "key5172", "value12005");

  // op 35546: get
  status = db->Get(read_options, "key2512", &value);
  if (status.ok()) {
    out << "GET key2512 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35547: put
  db->Put(write_options, "key5176", "value32886");

  // op 35548: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 35549: get
  status = db->Get(read_options, "key6271", &value);
  if (status.ok()) {
    out << "GET key6271 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35550: get
  status = db->Get(read_options, "key0061", &value);
  if (status.ok()) {
    out << "GET key0061 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35551: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0192", "key6679", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0192, key6679) size=" << scan_result.size() << std::endl;
  }

  // op 35552: put
  db->Put(write_options, "key11396", "value45722");

  // op 35553: put
  db->Put(write_options, "key6376", "value87342");

  // op 35554: put
  db->Put(write_options, "key5431", "value92630");

  // op 35555: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4901", "key6201", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4901, key6201) size=" << scan_result.size() << std::endl;
  }

  // op 35556: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 35557: put
  db->Put(write_options, "key3802", "value25381");

  // op 35558: put
  db->Put(write_options, "key10663", "value117766");

  // op 35559: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4699", "key11319", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4699, key11319) size=" << scan_result.size() << std::endl;
  }

  // op 35560: put
  db->Put(write_options, "key2353", "value81578");

  // op 35561: put
  db->Put(write_options, "key7204", "value76922");

  // op 35562: put
  db->Put(write_options, "key4077", "value63754");

  // op 35563: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8155", "key9231", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8155, key9231) size=" << scan_result.size() << std::endl;
  }

  // op 35564: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0533", "key5509", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0533, key5509) size=" << scan_result.size() << std::endl;
  }

  // op 35565: delete_range
  status = db->DeleteRange(write_options, "key2679", "key4102");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 35566: delete
  db->Delete(write_options, "key0325");

  // op 35567: put
  db->Put(write_options, "key8525", "value9615");

  // op 35568: put
  db->Put(write_options, "key2053", "value39328");

  // op 35569: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2165", "key8772", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2165, key8772) size=" << scan_result.size() << std::endl;
  }

  // op 35570: get
  status = db->Get(read_options, "key1273", &value);
  if (status.ok()) {
    out << "GET key1273 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35571: delete_range
  status = db->DeleteRange(write_options, "key1987", "key2690");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 35572: get
  status = db->Get(read_options, "key3621", &value);
  if (status.ok()) {
    out << "GET key3621 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35573: put
  db->Put(write_options, "key8592", "value41147");

  // op 35574: put
  db->Put(write_options, "key2707", "value96878");

  // op 35575: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 35576: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 35577: get
  status = db->Get(read_options, "key11307", &value);
  if (status.ok()) {
    out << "GET key11307 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35578: get
  status = db->Get(read_options, "key2931", &value);
  if (status.ok()) {
    out << "GET key2931 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35579: get
  status = db->Get(read_options, "key8617", &value);
  if (status.ok()) {
    out << "GET key8617 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35580: get
  status = db->Get(read_options, "key9489", &value);
  if (status.ok()) {
    out << "GET key9489 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35581: put
  db->Put(write_options, "key8138", "value32830");

  // op 35582: delete_range
  status = db->DeleteRange(write_options, "key3270", "key4824");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 35583: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 35584: delete_range
  status = db->DeleteRange(write_options, "key0300", "key3021");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 35585: put
  db->Put(write_options, "key1016", "value75322");

  // op 35586: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4565", "key10935", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4565, key10935) size=" << scan_result.size() << std::endl;
  }

  // op 35587: put
  db->Put(write_options, "key0180", "value53774");

  // op 35588: put
  db->Put(write_options, "key7576", "value35507");

  // op 35589: delete
  db->Delete(write_options, "key11328");

  // op 35590: get
  status = db->Get(read_options, "key1050", &value);
  if (status.ok()) {
    out << "GET key1050 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35591: put
  db->Put(write_options, "key11367", "value12852");

  // op 35592: delete
  db->Delete(write_options, "key7420");

  // op 35593: delete_range
  status = db->DeleteRange(write_options, "key0147", "key7948");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 35594: put
  db->Put(write_options, "key0167", "value28084");

  // op 35595: put
  db->Put(write_options, "key0964", "value92138");

  // op 35596: get
  status = db->Get(read_options, "key7038", &value);
  if (status.ok()) {
    out << "GET key7038 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35597: get
  status = db->Get(read_options, "key9284", &value);
  if (status.ok()) {
    out << "GET key9284 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35598: put
  db->Put(write_options, "key1942", "value52346");

  // op 35599: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2987", "key8245", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2987, key8245) size=" << scan_result.size() << std::endl;
  }

  // op 35600: get
  status = db->Get(read_options, "key5513", &value);
  if (status.ok()) {
    out << "GET key5513 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35601: get
  status = db->Get(read_options, "key0796", &value);
  if (status.ok()) {
    out << "GET key0796 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35602: delete_range
  status = db->DeleteRange(write_options, "key0609", "key7850");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 35603: put
  db->Put(write_options, "key4477", "value93021");

  // op 35604: get
  status = db->Get(read_options, "key7110", &value);
  if (status.ok()) {
    out << "GET key7110 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35605: put
  db->Put(write_options, "key6435", "value108643");

  // op 35606: get
  status = db->Get(read_options, "key5289", &value);
  if (status.ok()) {
    out << "GET key5289 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35607: delete
  db->Delete(write_options, "key11233");

  // op 35608: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3142", "key7379", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3142, key7379) size=" << scan_result.size() << std::endl;
  }

  // op 35609: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 35610: put
  db->Put(write_options, "key8033", "value100060");

  // op 35611: get
  status = db->Get(read_options, "key6312", &value);
  if (status.ok()) {
    out << "GET key6312 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35612: put
  db->Put(write_options, "key7267", "value19903");

  // op 35613: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3625", "key7210", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3625, key7210) size=" << scan_result.size() << std::endl;
  }

  // op 35614: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7476", "key11672", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7476, key11672) size=" << scan_result.size() << std::endl;
  }

  // op 35615: get
  status = db->Get(read_options, "key5414", &value);
  if (status.ok()) {
    out << "GET key5414 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35616: put
  db->Put(write_options, "key0608", "value36712");

  // op 35617: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7921", "key8443", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7921, key8443) size=" << scan_result.size() << std::endl;
  }

  // op 35618: put
  db->Put(write_options, "key4668", "value57679");

  // op 35619: get
  status = db->Get(read_options, "key3322", &value);
  if (status.ok()) {
    out << "GET key3322 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35620: put
  db->Put(write_options, "key7345", "value114359");

  // op 35621: get
  status = db->Get(read_options, "key9430", &value);
  if (status.ok()) {
    out << "GET key9430 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35622: delete
  db->Delete(write_options, "key9419");

  // op 35623: put
  db->Put(write_options, "key1153", "value48512");

  // op 35624: get
  status = db->Get(read_options, "key6687", &value);
  if (status.ok()) {
    out << "GET key6687 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35625: get
  status = db->Get(read_options, "key1535", &value);
  if (status.ok()) {
    out << "GET key1535 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35626: put
  db->Put(write_options, "key3599", "value117506");

  // op 35627: get
  status = db->Get(read_options, "key6198", &value);
  if (status.ok()) {
    out << "GET key6198 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35628: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7767", "key10813", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7767, key10813) size=" << scan_result.size() << std::endl;
  }

  // op 35629: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9670", "key9843", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9670, key9843) size=" << scan_result.size() << std::endl;
  }

  // op 35630: put
  db->Put(write_options, "key4392", "value14951");

  // op 35631: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2062", "key7144", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2062, key7144) size=" << scan_result.size() << std::endl;
  }

  // op 35632: put
  db->Put(write_options, "key4671", "value116459");

  // op 35633: put
  db->Put(write_options, "key10452", "value53817");

  // op 35634: delete_range
  status = db->DeleteRange(write_options, "key2217", "key4680");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 35635: put
  db->Put(write_options, "key9014", "value55339");

  // op 35636: put
  db->Put(write_options, "key8518", "value110741");

  // op 35637: put
  db->Put(write_options, "key2050", "value44690");

  // op 35638: delete
  db->Delete(write_options, "key7776");

  // op 35639: delete
  db->Delete(write_options, "key10541");

  // op 35640: put
  db->Put(write_options, "key8263", "value56408");

  // op 35641: delete_range
  status = db->DeleteRange(write_options, "key4076", "key8442");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 35642: put
  db->Put(write_options, "key9852", "value12350");

  // op 35643: put
  db->Put(write_options, "key5631", "value45325");

  // op 35644: get
  status = db->Get(read_options, "key1770", &value);
  if (status.ok()) {
    out << "GET key1770 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35645: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6539", "key7127", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6539, key7127) size=" << scan_result.size() << std::endl;
  }

  // op 35646: put
  db->Put(write_options, "key1696", "value86490");

  // op 35647: put
  db->Put(write_options, "key2100", "value62684");

  // op 35648: delete_range
  status = db->DeleteRange(write_options, "key9970", "key10442");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 35649: put
  db->Put(write_options, "key4372", "value35773");

  // op 35650: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 35651: put
  db->Put(write_options, "key0918", "value43374");

  // op 35652: delete_range
  status = db->DeleteRange(write_options, "key6629", "key10043");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 35653: delete
  db->Delete(write_options, "key0595");

  // op 35654: delete
  db->Delete(write_options, "key11513");

  // op 35655: get
  status = db->Get(read_options, "key5545", &value);
  if (status.ok()) {
    out << "GET key5545 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35656: delete_range
  status = db->DeleteRange(write_options, "key0584", "key5741");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 35657: get
  status = db->Get(read_options, "key0817", &value);
  if (status.ok()) {
    out << "GET key0817 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35658: delete_range
  status = db->DeleteRange(write_options, "key5386", "key8170");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 35659: put
  db->Put(write_options, "key8183", "value33235");

  // op 35660: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2070", "key5792", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2070, key5792) size=" << scan_result.size() << std::endl;
  }

  // op 35661: put
  db->Put(write_options, "key10914", "value84980");

  // op 35662: get
  status = db->Get(read_options, "key5296", &value);
  if (status.ok()) {
    out << "GET key5296 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35663: delete
  db->Delete(write_options, "key2748");

  // op 35664: put
  db->Put(write_options, "key0895", "value3885");

  // op 35665: put
  db->Put(write_options, "key2821", "value2992");

  // op 35666: delete_range
  status = db->DeleteRange(write_options, "key10208", "key10524");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 35667: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 35668: delete_range
  status = db->DeleteRange(write_options, "key2398", "key5652");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 35669: delete_range
  status = db->DeleteRange(write_options, "key3002", "key8495");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 35670: get
  status = db->Get(read_options, "key8517", &value);
  if (status.ok()) {
    out << "GET key8517 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35671: delete
  db->Delete(write_options, "key4654");

  // op 35672: put
  db->Put(write_options, "key1946", "value61238");

  // op 35673: get
  status = db->Get(read_options, "key10956", &value);
  if (status.ok()) {
    out << "GET key10956 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35674: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10389", "key10630", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10389, key10630) size=" << scan_result.size() << std::endl;
  }

  // op 35675: put
  db->Put(write_options, "key11425", "value5179");

  // op 35676: delete_range
  status = db->DeleteRange(write_options, "key8883", "key11112");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 35677: delete
  db->Delete(write_options, "key6376");

  // op 35678: delete
  db->Delete(write_options, "key10776");

  // op 35679: delete_range
  status = db->DeleteRange(write_options, "key2962", "key7976");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 35680: get
  status = db->Get(read_options, "key2497", &value);
  if (status.ok()) {
    out << "GET key2497 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35681: put
  db->Put(write_options, "key1082", "value19862");

  // op 35682: get
  status = db->Get(read_options, "key9717", &value);
  if (status.ok()) {
    out << "GET key9717 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35683: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2370", "key3068", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2370, key3068) size=" << scan_result.size() << std::endl;
  }

  // op 35684: get
  status = db->Get(read_options, "key9166", &value);
  if (status.ok()) {
    out << "GET key9166 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35685: put
  db->Put(write_options, "key1072", "value95245");

  // op 35686: put
  db->Put(write_options, "key4634", "value41540");

  // op 35687: get
  status = db->Get(read_options, "key7655", &value);
  if (status.ok()) {
    out << "GET key7655 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35688: put
  db->Put(write_options, "key11555", "value25210");

  // op 35689: put
  db->Put(write_options, "key0431", "value12905");

  // op 35690: put
  db->Put(write_options, "key9502", "value41788");

  // op 35691: put
  db->Put(write_options, "key10831", "value62161");

  // op 35692: put
  db->Put(write_options, "key4769", "value117948");

  // op 35693: delete
  db->Delete(write_options, "key10361");

  // op 35694: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0493", "key3941", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0493, key3941) size=" << scan_result.size() << std::endl;
  }

  // op 35695: put
  db->Put(write_options, "key2288", "value43929");

  // op 35696: get
  status = db->Get(read_options, "key4739", &value);
  if (status.ok()) {
    out << "GET key4739 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35697: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5074", "key10578", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5074, key10578) size=" << scan_result.size() << std::endl;
  }

  // op 35698: delete
  db->Delete(write_options, "key9529");

  // op 35699: delete_range
  status = db->DeleteRange(write_options, "key4158", "key6791");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 35700: delete
  db->Delete(write_options, "key9559");

  // op 35701: put
  db->Put(write_options, "key10612", "value11659");

  // op 35702: put
  db->Put(write_options, "key1998", "value47555");

  // op 35703: get
  status = db->Get(read_options, "key8986", &value);
  if (status.ok()) {
    out << "GET key8986 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35704: delete_range
  status = db->DeleteRange(write_options, "key3871", "key8680");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 35705: delete
  db->Delete(write_options, "key5900");

  // op 35706: put
  db->Put(write_options, "key9580", "value117073");

  // op 35707: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0697", "key3571", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0697, key3571) size=" << scan_result.size() << std::endl;
  }

  // op 35708: put
  db->Put(write_options, "key11281", "value3437");

  // op 35709: get
  status = db->Get(read_options, "key10585", &value);
  if (status.ok()) {
    out << "GET key10585 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35710: get
  status = db->Get(read_options, "key11091", &value);
  if (status.ok()) {
    out << "GET key11091 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35711: put
  db->Put(write_options, "key2780", "value32086");

  // op 35712: delete_range
  status = db->DeleteRange(write_options, "key5850", "key6001");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 35713: put
  db->Put(write_options, "key2231", "value19610");

  // op 35714: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1740", "key8015", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1740, key8015) size=" << scan_result.size() << std::endl;
  }

  // op 35715: put
  db->Put(write_options, "key5462", "value107070");

  // op 35716: put
  db->Put(write_options, "key3785", "value68278");

  // op 35717: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6778", "key9380", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6778, key9380) size=" << scan_result.size() << std::endl;
  }

  // op 35718: put
  db->Put(write_options, "key3442", "value85925");

  // op 35719: put
  db->Put(write_options, "key1407", "value82059");

  // op 35720: put
  db->Put(write_options, "key4406", "value21082");

  // op 35721: delete
  db->Delete(write_options, "key2273");

  // op 35722: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1256", "key7694", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1256, key7694) size=" << scan_result.size() << std::endl;
  }

  // op 35723: put
  db->Put(write_options, "key9825", "value57060");

  // op 35724: put
  db->Put(write_options, "key2774", "value8530");

  // op 35725: put
  db->Put(write_options, "key3991", "value94266");

  // op 35726: get
  status = db->Get(read_options, "key5416", &value);
  if (status.ok()) {
    out << "GET key5416 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35727: get
  status = db->Get(read_options, "key6291", &value);
  if (status.ok()) {
    out << "GET key6291 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35728: delete
  db->Delete(write_options, "key2796");

  // op 35729: put
  db->Put(write_options, "key6774", "value96605");

  // op 35730: delete_range
  status = db->DeleteRange(write_options, "key4017", "key7598");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 35731: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1511", "key4718", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1511, key4718) size=" << scan_result.size() << std::endl;
  }

  // op 35732: delete
  db->Delete(write_options, "key7440");

  // op 35733: put
  db->Put(write_options, "key10308", "value83619");

  // op 35734: put
  db->Put(write_options, "key6350", "value47040");

  // op 35735: put
  db->Put(write_options, "key8631", "value88861");

  // op 35736: get
  status = db->Get(read_options, "key11550", &value);
  if (status.ok()) {
    out << "GET key11550 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35737: delete
  db->Delete(write_options, "key6769");

  // op 35738: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3922", "key5714", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3922, key5714) size=" << scan_result.size() << std::endl;
  }

  // op 35739: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3377", "key7880", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3377, key7880) size=" << scan_result.size() << std::endl;
  }

  // op 35740: put
  db->Put(write_options, "key6176", "value17966");

  // op 35741: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5627", "key6361", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5627, key6361) size=" << scan_result.size() << std::endl;
  }

  // op 35742: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 35743: get
  status = db->Get(read_options, "key2322", &value);
  if (status.ok()) {
    out << "GET key2322 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35744: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 35745: get
  status = db->Get(read_options, "key0017", &value);
  if (status.ok()) {
    out << "GET key0017 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35746: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2202", "key6760", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2202, key6760) size=" << scan_result.size() << std::endl;
  }

  // op 35747: get
  status = db->Get(read_options, "key6676", &value);
  if (status.ok()) {
    out << "GET key6676 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35748: put
  db->Put(write_options, "key5090", "value56587");

  // op 35749: get
  status = db->Get(read_options, "key1465", &value);
  if (status.ok()) {
    out << "GET key1465 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35750: put
  db->Put(write_options, "key10257", "value106304");

  // op 35751: put
  db->Put(write_options, "key0547", "value99433");

  // op 35752: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4785", "key8626", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4785, key8626) size=" << scan_result.size() << std::endl;
  }

  // op 35753: put
  db->Put(write_options, "key0152", "value55405");

  // op 35754: put
  db->Put(write_options, "key3820", "value1288");

  // op 35755: get
  status = db->Get(read_options, "key1240", &value);
  if (status.ok()) {
    out << "GET key1240 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35756: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 35757: put
  db->Put(write_options, "key10035", "value39754");

  // op 35758: delete_range
  status = db->DeleteRange(write_options, "key0672", "key1194");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 35759: put
  db->Put(write_options, "key9811", "value48333");

  // op 35760: put
  db->Put(write_options, "key1509", "value88122");

  // op 35761: put
  db->Put(write_options, "key3826", "value32252");

  // op 35762: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9235", "key11523", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9235, key11523) size=" << scan_result.size() << std::endl;
  }

  // op 35763: delete_range
  status = db->DeleteRange(write_options, "key1535", "key6136");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 35764: delete
  db->Delete(write_options, "key5998");

  // op 35765: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3066", "key3567", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3066, key3567) size=" << scan_result.size() << std::endl;
  }

  // op 35766: delete_range
  status = db->DeleteRange(write_options, "key5736", "key9395");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 35767: get
  status = db->Get(read_options, "key8933", &value);
  if (status.ok()) {
    out << "GET key8933 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35768: delete
  db->Delete(write_options, "key9556");

  // op 35769: get
  status = db->Get(read_options, "key11335", &value);
  if (status.ok()) {
    out << "GET key11335 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35770: get
  status = db->Get(read_options, "key4275", &value);
  if (status.ok()) {
    out << "GET key4275 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35771: put
  db->Put(write_options, "key9105", "value60508");

  // op 35772: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1143", "key7607", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1143, key7607) size=" << scan_result.size() << std::endl;
  }

  // op 35773: put
  db->Put(write_options, "key6872", "value86400");

  // op 35774: delete_range
  status = db->DeleteRange(write_options, "key5065", "key10274");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 35775: put
  db->Put(write_options, "key4261", "value98990");

  // op 35776: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0839", "key8026", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0839, key8026) size=" << scan_result.size() << std::endl;
  }

  // op 35777: put
  db->Put(write_options, "key7559", "value113501");

  // op 35778: put
  db->Put(write_options, "key1419", "value32563");

  // op 35779: delete
  db->Delete(write_options, "key3938");

  // op 35780: get
  status = db->Get(read_options, "key9181", &value);
  if (status.ok()) {
    out << "GET key9181 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35781: delete_range
  status = db->DeleteRange(write_options, "key1515", "key8846");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 35782: put
  db->Put(write_options, "key2302", "value113991");

  // op 35783: put
  db->Put(write_options, "key2158", "value32304");

  // op 35784: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0928", "key1109", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0928, key1109) size=" << scan_result.size() << std::endl;
  }

  // op 35785: put
  db->Put(write_options, "key4500", "value51122");

  // op 35786: put
  db->Put(write_options, "key1019", "value94611");

  // op 35787: put
  db->Put(write_options, "key4781", "value70492");

  // op 35788: get
  status = db->Get(read_options, "key3774", &value);
  if (status.ok()) {
    out << "GET key3774 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35789: delete
  db->Delete(write_options, "key4576");

  // op 35790: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 35791: get
  status = db->Get(read_options, "key3668", &value);
  if (status.ok()) {
    out << "GET key3668 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35792: put
  db->Put(write_options, "key11695", "value112681");

  // op 35793: put
  db->Put(write_options, "key11280", "value34062");

  // op 35794: get
  status = db->Get(read_options, "key6873", &value);
  if (status.ok()) {
    out << "GET key6873 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35795: put
  db->Put(write_options, "key4100", "value30858");

  // op 35796: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0941", "key5636", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0941, key5636) size=" << scan_result.size() << std::endl;
  }

  // op 35797: put
  db->Put(write_options, "key10880", "value5909");

  // op 35798: put
  db->Put(write_options, "key4418", "value60342");

  // op 35799: put
  db->Put(write_options, "key7871", "value39946");

  // op 35800: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0869", "key8447", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0869, key8447) size=" << scan_result.size() << std::endl;
  }

  // op 35801: put
  db->Put(write_options, "key2805", "value7024");

  // op 35802: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 35803: put
  db->Put(write_options, "key2102", "value27327");

  // op 35804: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9206", "key11030", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9206, key11030) size=" << scan_result.size() << std::endl;
  }

  // op 35805: put
  db->Put(write_options, "key2169", "value26035");

  // op 35806: delete
  db->Delete(write_options, "key9759");

  // op 35807: delete_range
  status = db->DeleteRange(write_options, "key2167", "key6004");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 35808: delete_range
  status = db->DeleteRange(write_options, "key2258", "key9840");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 35809: delete
  db->Delete(write_options, "key5662");

  // op 35810: delete_range
  status = db->DeleteRange(write_options, "key8768", "key10286");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 35811: delete_range
  status = db->DeleteRange(write_options, "key0660", "key3788");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 35812: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7315", "key8470", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7315, key8470) size=" << scan_result.size() << std::endl;
  }

  // op 35813: delete_range
  status = db->DeleteRange(write_options, "key5652", "key7033");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 35814: put
  db->Put(write_options, "key3626", "value109003");

  // op 35815: delete_range
  status = db->DeleteRange(write_options, "key8002", "key9234");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 35816: put
  db->Put(write_options, "key0568", "value65258");

  // op 35817: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3913", "key9726", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3913, key9726) size=" << scan_result.size() << std::endl;
  }

  // op 35818: put
  db->Put(write_options, "key4606", "value76477");

  // op 35819: delete_range
  status = db->DeleteRange(write_options, "key0111", "key3292");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 35820: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4558", "key5679", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4558, key5679) size=" << scan_result.size() << std::endl;
  }

  // op 35821: delete
  db->Delete(write_options, "key3171");

  // op 35822: get
  status = db->Get(read_options, "key7203", &value);
  if (status.ok()) {
    out << "GET key7203 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35823: get
  status = db->Get(read_options, "key10874", &value);
  if (status.ok()) {
    out << "GET key10874 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35824: put
  db->Put(write_options, "key1532", "value47158");

  // op 35825: delete
  db->Delete(write_options, "key5656");

  // op 35826: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 35827: put
  db->Put(write_options, "key3303", "value64589");

  // op 35828: put
  db->Put(write_options, "key5760", "value47730");

  // op 35829: put
  db->Put(write_options, "key7409", "value82276");

  // op 35830: put
  db->Put(write_options, "key11406", "value55429");

  // op 35831: delete_range
  status = db->DeleteRange(write_options, "key2612", "key3670");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 35832: put
  db->Put(write_options, "key6946", "value88606");

  // op 35833: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0817", "key9588", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0817, key9588) size=" << scan_result.size() << std::endl;
  }

  // op 35834: put
  db->Put(write_options, "key0408", "value18017");

  // op 35835: get
  status = db->Get(read_options, "key7146", &value);
  if (status.ok()) {
    out << "GET key7146 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35836: put
  db->Put(write_options, "key7272", "value11009");

  // op 35837: delete
  db->Delete(write_options, "key4701");

  // op 35838: put
  db->Put(write_options, "key7501", "value98507");

  // op 35839: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1677", "key4571", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1677, key4571) size=" << scan_result.size() << std::endl;
  }

  // op 35840: put
  db->Put(write_options, "key0626", "value108739");

  // op 35841: put
  db->Put(write_options, "key10554", "value14877");

  // op 35842: get
  status = db->Get(read_options, "key9312", &value);
  if (status.ok()) {
    out << "GET key9312 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35843: put
  db->Put(write_options, "key4874", "value6557");

  // op 35844: get
  status = db->Get(read_options, "key0242", &value);
  if (status.ok()) {
    out << "GET key0242 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35845: put
  db->Put(write_options, "key9174", "value81867");

  // op 35846: put
  db->Put(write_options, "key8458", "value13016");

  // op 35847: put
  db->Put(write_options, "key2659", "value85603");

  // op 35848: put
  db->Put(write_options, "key5538", "value112323");

  // op 35849: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 35850: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2072", "key5353", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2072, key5353) size=" << scan_result.size() << std::endl;
  }

  // op 35851: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5945", "key11215", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5945, key11215) size=" << scan_result.size() << std::endl;
  }

  // op 35852: get
  status = db->Get(read_options, "key0467", &value);
  if (status.ok()) {
    out << "GET key0467 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35853: delete
  db->Delete(write_options, "key2732");

  // op 35854: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6559", "key9237", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6559, key9237) size=" << scan_result.size() << std::endl;
  }

  // op 35855: put
  db->Put(write_options, "key10966", "value42965");

  // op 35856: delete_range
  status = db->DeleteRange(write_options, "key8524", "key11150");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 35857: delete_range
  status = db->DeleteRange(write_options, "key0558", "key10811");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 35858: put
  db->Put(write_options, "key11166", "value72079");

  // op 35859: get
  status = db->Get(read_options, "key7554", &value);
  if (status.ok()) {
    out << "GET key7554 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35860: put
  db->Put(write_options, "key7383", "value34827");

  // op 35861: get
  status = db->Get(read_options, "key2475", &value);
  if (status.ok()) {
    out << "GET key2475 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35862: put
  db->Put(write_options, "key7520", "value70964");

  // op 35863: get
  status = db->Get(read_options, "key6368", &value);
  if (status.ok()) {
    out << "GET key6368 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35864: put
  db->Put(write_options, "key8396", "value34986");

  // op 35865: put
  db->Put(write_options, "key2387", "value95918");

  // op 35866: put
  db->Put(write_options, "key5573", "value72810");

  // op 35867: put
  db->Put(write_options, "key11047", "value10783");

  // op 35868: put
  db->Put(write_options, "key5847", "value75406");

  // op 35869: put
  db->Put(write_options, "key8737", "value48139");

  // op 35870: put
  db->Put(write_options, "key0387", "value95146");

  // op 35871: delete
  db->Delete(write_options, "key0818");

  // op 35872: delete_range
  status = db->DeleteRange(write_options, "key0455", "key4169");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 35873: get
  status = db->Get(read_options, "key4115", &value);
  if (status.ok()) {
    out << "GET key4115 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35874: put
  db->Put(write_options, "key8699", "value102407");

  // op 35875: put
  db->Put(write_options, "key6411", "value110999");

  // op 35876: get
  status = db->Get(read_options, "key8228", &value);
  if (status.ok()) {
    out << "GET key8228 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35877: get
  status = db->Get(read_options, "key10426", &value);
  if (status.ok()) {
    out << "GET key10426 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35878: delete
  db->Delete(write_options, "key0568");

  // op 35879: put
  db->Put(write_options, "key3474", "value38680");

  // op 35880: get
  status = db->Get(read_options, "key0084", &value);
  if (status.ok()) {
    out << "GET key0084 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35881: put
  db->Put(write_options, "key7387", "value90331");

  // op 35882: put
  db->Put(write_options, "key4801", "value57565");

  // op 35883: put
  db->Put(write_options, "key5228", "value61276");

  // op 35884: delete
  db->Delete(write_options, "key3379");

  // op 35885: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5674", "key9529", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5674, key9529) size=" << scan_result.size() << std::endl;
  }

  // op 35886: put
  db->Put(write_options, "key9941", "value84497");

  // op 35887: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 35888: put
  db->Put(write_options, "key8469", "value50984");

  // op 35889: get
  status = db->Get(read_options, "key2482", &value);
  if (status.ok()) {
    out << "GET key2482 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35890: get
  status = db->Get(read_options, "key5903", &value);
  if (status.ok()) {
    out << "GET key5903 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35891: delete
  db->Delete(write_options, "key6257");

  // op 35892: put
  db->Put(write_options, "key11035", "value80448");

  // op 35893: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7309", "key10296", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7309, key10296) size=" << scan_result.size() << std::endl;
  }

  // op 35894: put
  db->Put(write_options, "key7656", "value36946");

  // op 35895: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7603", "key8256", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7603, key8256) size=" << scan_result.size() << std::endl;
  }

  // op 35896: delete
  db->Delete(write_options, "key3615");

  // op 35897: delete_range
  status = db->DeleteRange(write_options, "key6530", "key9686");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 35898: delete
  db->Delete(write_options, "key4664");

  // op 35899: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9424", "key11119", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9424, key11119) size=" << scan_result.size() << std::endl;
  }

  // op 35900: delete
  db->Delete(write_options, "key6877");

  // op 35901: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 35902: put
  db->Put(write_options, "key2930", "value39180");

  // op 35903: put
  db->Put(write_options, "key6543", "value28809");

  // op 35904: put
  db->Put(write_options, "key6067", "value56831");

  // op 35905: delete
  db->Delete(write_options, "key2986");

  // op 35906: get
  status = db->Get(read_options, "key8229", &value);
  if (status.ok()) {
    out << "GET key8229 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35907: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 35908: delete_range
  status = db->DeleteRange(write_options, "key1926", "key11555");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 35909: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8306", "key11548", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8306, key11548) size=" << scan_result.size() << std::endl;
  }

  // op 35910: put
  db->Put(write_options, "key0847", "value32476");

  // op 35911: put
  db->Put(write_options, "key3299", "value25210");

  // op 35912: put
  db->Put(write_options, "key10923", "value38761");

  // op 35913: put
  db->Put(write_options, "key0044", "value105762");

  // op 35914: put
  db->Put(write_options, "key10952", "value6761");

  // op 35915: delete_range
  status = db->DeleteRange(write_options, "key1323", "key8166");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 35916: get
  status = db->Get(read_options, "key10759", &value);
  if (status.ok()) {
    out << "GET key10759 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35917: get
  status = db->Get(read_options, "key3758", &value);
  if (status.ok()) {
    out << "GET key3758 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35918: get
  status = db->Get(read_options, "key5733", &value);
  if (status.ok()) {
    out << "GET key5733 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35919: put
  db->Put(write_options, "key2858", "value10394");

  // op 35920: put
  db->Put(write_options, "key11043", "value25174");

  // op 35921: get
  status = db->Get(read_options, "key7516", &value);
  if (status.ok()) {
    out << "GET key7516 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35922: put
  db->Put(write_options, "key1299", "value55057");

  // op 35923: put
  db->Put(write_options, "key2605", "value31547");

  // op 35924: put
  db->Put(write_options, "key8893", "value75755");

  // op 35925: put
  db->Put(write_options, "key5441", "value46136");

  // op 35926: put
  db->Put(write_options, "key5194", "value38301");

  // op 35927: delete
  db->Delete(write_options, "key5162");

  // op 35928: delete
  db->Delete(write_options, "key0805");

  // op 35929: get
  status = db->Get(read_options, "key2022", &value);
  if (status.ok()) {
    out << "GET key2022 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35930: get
  status = db->Get(read_options, "key1846", &value);
  if (status.ok()) {
    out << "GET key1846 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35931: delete
  db->Delete(write_options, "key7554");

  // op 35932: delete
  db->Delete(write_options, "key11631");

  // op 35933: put
  db->Put(write_options, "key5822", "value76860");

  // op 35934: delete
  db->Delete(write_options, "key7298");

  // op 35935: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 35936: get
  status = db->Get(read_options, "key10960", &value);
  if (status.ok()) {
    out << "GET key10960 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35937: get
  status = db->Get(read_options, "key5423", &value);
  if (status.ok()) {
    out << "GET key5423 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35938: get
  status = db->Get(read_options, "key11498", &value);
  if (status.ok()) {
    out << "GET key11498 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35939: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9563", "key9956", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9563, key9956) size=" << scan_result.size() << std::endl;
  }

  // op 35940: delete_range
  status = db->DeleteRange(write_options, "key1395", "key9973");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 35941: put
  db->Put(write_options, "key5388", "value80567");

  // op 35942: delete_range
  status = db->DeleteRange(write_options, "key3512", "key4576");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 35943: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 35944: delete
  db->Delete(write_options, "key0558");

  // op 35945: put
  db->Put(write_options, "key7415", "value71278");

  // op 35946: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 35947: put
  db->Put(write_options, "key7047", "value104327");

  // op 35948: get
  status = db->Get(read_options, "key2343", &value);
  if (status.ok()) {
    out << "GET key2343 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35949: put
  db->Put(write_options, "key8976", "value90341");

  // op 35950: get
  status = db->Get(read_options, "key6030", &value);
  if (status.ok()) {
    out << "GET key6030 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35951: put
  db->Put(write_options, "key3113", "value68841");

  // op 35952: put
  db->Put(write_options, "key4367", "value94224");

  // op 35953: put
  db->Put(write_options, "key5740", "value82082");

  // op 35954: get
  status = db->Get(read_options, "key7823", &value);
  if (status.ok()) {
    out << "GET key7823 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35955: put
  db->Put(write_options, "key3157", "value108177");

  // op 35956: put
  db->Put(write_options, "key0904", "value38464");

  // op 35957: delete_range
  status = db->DeleteRange(write_options, "key1001", "key1803");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 35958: get
  status = db->Get(read_options, "key5778", &value);
  if (status.ok()) {
    out << "GET key5778 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35959: put
  db->Put(write_options, "key0477", "value56021");

  // op 35960: put
  db->Put(write_options, "key1798", "value94524");

  // op 35961: put
  db->Put(write_options, "key4234", "value74191");

  // op 35962: put
  db->Put(write_options, "key3002", "value63000");

  // op 35963: put
  db->Put(write_options, "key6647", "value76692");

  // op 35964: get
  status = db->Get(read_options, "key9872", &value);
  if (status.ok()) {
    out << "GET key9872 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35965: get
  status = db->Get(read_options, "key3543", &value);
  if (status.ok()) {
    out << "GET key3543 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35966: get
  status = db->Get(read_options, "key0692", &value);
  if (status.ok()) {
    out << "GET key0692 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35967: get
  status = db->Get(read_options, "key3539", &value);
  if (status.ok()) {
    out << "GET key3539 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35968: get
  status = db->Get(read_options, "key1245", &value);
  if (status.ok()) {
    out << "GET key1245 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35969: delete_range
  status = db->DeleteRange(write_options, "key4666", "key7796");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 35970: put
  db->Put(write_options, "key6185", "value100306");

  // op 35971: put
  db->Put(write_options, "key11349", "value116120");

  // op 35972: get
  status = db->Get(read_options, "key11151", &value);
  if (status.ok()) {
    out << "GET key11151 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35973: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 35974: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9492", "key10418", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9492, key10418) size=" << scan_result.size() << std::endl;
  }

  // op 35975: delete_range
  status = db->DeleteRange(write_options, "key4072", "key5356");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 35976: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3197", "key10639", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3197, key10639) size=" << scan_result.size() << std::endl;
  }

  // op 35977: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 35978: put
  db->Put(write_options, "key6958", "value114982");

  // op 35979: put
  db->Put(write_options, "key5714", "value2087");

  // op 35980: put
  db->Put(write_options, "key9649", "value35562");

  // op 35981: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3839", "key6184", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3839, key6184) size=" << scan_result.size() << std::endl;
  }

  // op 35982: get
  status = db->Get(read_options, "key1982", &value);
  if (status.ok()) {
    out << "GET key1982 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35983: get
  status = db->Get(read_options, "key0974", &value);
  if (status.ok()) {
    out << "GET key0974 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35984: put
  db->Put(write_options, "key10839", "value58347");

  // op 35985: put
  db->Put(write_options, "key4620", "value31455");

  // op 35986: put
  db->Put(write_options, "key4079", "value37135");

  // op 35987: get
  status = db->Get(read_options, "key6126", &value);
  if (status.ok()) {
    out << "GET key6126 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35988: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 35989: put
  db->Put(write_options, "key10044", "value83488");

  // op 35990: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2670", "key9049", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2670, key9049) size=" << scan_result.size() << std::endl;
  }

  // op 35991: put
  db->Put(write_options, "key0678", "value50043");

  // op 35992: delete
  db->Delete(write_options, "key10914");

  // op 35993: get
  status = db->Get(read_options, "key8208", &value);
  if (status.ok()) {
    out << "GET key8208 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35994: get
  status = db->Get(read_options, "key11143", &value);
  if (status.ok()) {
    out << "GET key11143 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 35995: put
  db->Put(write_options, "key1914", "value4982");

  // op 35996: put
  db->Put(write_options, "key8158", "value81727");

  // op 35997: put
  db->Put(write_options, "key6365", "value117848");

  // op 35998: delete
  db->Delete(write_options, "key3116");

  // op 35999: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3430", "key8437", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3430, key8437) size=" << scan_result.size() << std::endl;
  }

  // op 36000: get
  status = db->Get(read_options, "key10651", &value);
  if (status.ok()) {
    out << "GET key10651 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36001: put
  db->Put(write_options, "key6121", "value36433");

  // op 36002: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7502", "key11491", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7502, key11491) size=" << scan_result.size() << std::endl;
  }

  // op 36003: put
  db->Put(write_options, "key11679", "value62477");

  // op 36004: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2872", "key3931", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2872, key3931) size=" << scan_result.size() << std::endl;
  }

  // op 36005: put
  db->Put(write_options, "key1788", "value80867");

  // op 36006: delete_range
  status = db->DeleteRange(write_options, "key0189", "key10849");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 36007: get
  status = db->Get(read_options, "key0382", &value);
  if (status.ok()) {
    out << "GET key0382 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36008: delete
  db->Delete(write_options, "key5827");

  // op 36009: delete
  db->Delete(write_options, "key9491");

  // op 36010: put
  db->Put(write_options, "key8301", "value29636");

  // op 36011: get
  status = db->Get(read_options, "key10732", &value);
  if (status.ok()) {
    out << "GET key10732 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36012: get
  status = db->Get(read_options, "key0799", &value);
  if (status.ok()) {
    out << "GET key0799 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36013: delete
  db->Delete(write_options, "key5347");

  // op 36014: get
  status = db->Get(read_options, "key8410", &value);
  if (status.ok()) {
    out << "GET key8410 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36015: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2221", "key11597", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2221, key11597) size=" << scan_result.size() << std::endl;
  }

  // op 36016: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 36017: delete
  db->Delete(write_options, "key9664");

  // op 36018: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 36019: delete_range
  status = db->DeleteRange(write_options, "key4843", "key6228");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 36020: put
  db->Put(write_options, "key7301", "value76240");

  // op 36021: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1482", "key11725", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1482, key11725) size=" << scan_result.size() << std::endl;
  }

  // op 36022: delete_range
  status = db->DeleteRange(write_options, "key3628", "key6476");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 36023: get
  status = db->Get(read_options, "key7834", &value);
  if (status.ok()) {
    out << "GET key7834 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36024: delete
  db->Delete(write_options, "key5833");

  // op 36025: get
  status = db->Get(read_options, "key1879", &value);
  if (status.ok()) {
    out << "GET key1879 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36026: put
  db->Put(write_options, "key11534", "value78974");

  // op 36027: get
  status = db->Get(read_options, "key4391", &value);
  if (status.ok()) {
    out << "GET key4391 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36028: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0942", "key6621", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0942, key6621) size=" << scan_result.size() << std::endl;
  }

  // op 36029: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0702", "key8334", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0702, key8334) size=" << scan_result.size() << std::endl;
  }

  // op 36030: put
  db->Put(write_options, "key3368", "value9334");

  // op 36031: delete
  db->Delete(write_options, "key2617");

  // op 36032: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4285", "key6442", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4285, key6442) size=" << scan_result.size() << std::endl;
  }

  // op 36033: get
  status = db->Get(read_options, "key6104", &value);
  if (status.ok()) {
    out << "GET key6104 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36034: delete
  db->Delete(write_options, "key5187");

  // op 36035: put
  db->Put(write_options, "key3430", "value17752");

  // op 36036: put
  db->Put(write_options, "key1227", "value115082");

  // op 36037: get
  status = db->Get(read_options, "key8880", &value);
  if (status.ok()) {
    out << "GET key8880 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36038: get
  status = db->Get(read_options, "key5359", &value);
  if (status.ok()) {
    out << "GET key5359 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36039: put
  db->Put(write_options, "key3938", "value46065");

  // op 36040: delete_range
  status = db->DeleteRange(write_options, "key1475", "key2238");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 36041: put
  db->Put(write_options, "key7672", "value40046");

  // op 36042: delete
  db->Delete(write_options, "key10732");

  // op 36043: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3589", "key11332", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3589, key11332) size=" << scan_result.size() << std::endl;
  }

  // op 36044: get
  status = db->Get(read_options, "key3023", &value);
  if (status.ok()) {
    out << "GET key3023 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36045: get
  status = db->Get(read_options, "key1493", &value);
  if (status.ok()) {
    out << "GET key1493 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36046: put
  db->Put(write_options, "key6734", "value66661");

  // op 36047: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7517", "key10355", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7517, key10355) size=" << scan_result.size() << std::endl;
  }

  // op 36048: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3625", "key9925", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3625, key9925) size=" << scan_result.size() << std::endl;
  }

  // op 36049: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0434", "key4693", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0434, key4693) size=" << scan_result.size() << std::endl;
  }

  // op 36050: put
  db->Put(write_options, "key7689", "value116212");

  // op 36051: put
  db->Put(write_options, "key3109", "value116079");

  // op 36052: get
  status = db->Get(read_options, "key10885", &value);
  if (status.ok()) {
    out << "GET key10885 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36053: put
  db->Put(write_options, "key4329", "value78646");

  // op 36054: get
  status = db->Get(read_options, "key2072", &value);
  if (status.ok()) {
    out << "GET key2072 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36055: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7403", "key10486", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7403, key10486) size=" << scan_result.size() << std::endl;
  }

  // op 36056: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 36057: put
  db->Put(write_options, "key4420", "value104845");

  // op 36058: get
  status = db->Get(read_options, "key9932", &value);
  if (status.ok()) {
    out << "GET key9932 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36059: put
  db->Put(write_options, "key0065", "value48983");

  // op 36060: delete_range
  status = db->DeleteRange(write_options, "key6171", "key6268");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 36061: put
  db->Put(write_options, "key10508", "value57936");

  // op 36062: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1714", "key6765", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1714, key6765) size=" << scan_result.size() << std::endl;
  }

  // op 36063: delete_range
  status = db->DeleteRange(write_options, "key8449", "key9176");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 36064: get
  status = db->Get(read_options, "key7533", &value);
  if (status.ok()) {
    out << "GET key7533 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36065: delete_range
  status = db->DeleteRange(write_options, "key0596", "key11224");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 36066: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2233", "key4540", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2233, key4540) size=" << scan_result.size() << std::endl;
  }

  // op 36067: put
  db->Put(write_options, "key7668", "value117559");

  // op 36068: get
  status = db->Get(read_options, "key10910", &value);
  if (status.ok()) {
    out << "GET key10910 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36069: get
  status = db->Get(read_options, "key9533", &value);
  if (status.ok()) {
    out << "GET key9533 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36070: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7384", "key10091", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7384, key10091) size=" << scan_result.size() << std::endl;
  }

  // op 36071: put
  db->Put(write_options, "key1544", "value26321");

  // op 36072: put
  db->Put(write_options, "key11500", "value26073");

  // op 36073: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3078", "key5489", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3078, key5489) size=" << scan_result.size() << std::endl;
  }

  // op 36074: put
  db->Put(write_options, "key4034", "value19703");

  // op 36075: delete_range
  status = db->DeleteRange(write_options, "key4786", "key6603");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 36076: get
  status = db->Get(read_options, "key5000", &value);
  if (status.ok()) {
    out << "GET key5000 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36077: put
  db->Put(write_options, "key10772", "value64290");

  // op 36078: put
  db->Put(write_options, "key10017", "value34237");

  // op 36079: get
  status = db->Get(read_options, "key9608", &value);
  if (status.ok()) {
    out << "GET key9608 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36080: get
  status = db->Get(read_options, "key5046", &value);
  if (status.ok()) {
    out << "GET key5046 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36081: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1670", "key11164", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1670, key11164) size=" << scan_result.size() << std::endl;
  }

  // op 36082: delete
  db->Delete(write_options, "key2718");

  // op 36083: put
  db->Put(write_options, "key2683", "value113284");

  // op 36084: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 36085: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1876", "key2211", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1876, key2211) size=" << scan_result.size() << std::endl;
  }

  // op 36086: get
  status = db->Get(read_options, "key1886", &value);
  if (status.ok()) {
    out << "GET key1886 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36087: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1183", "key7645", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1183, key7645) size=" << scan_result.size() << std::endl;
  }

  // op 36088: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0917", "key1234", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0917, key1234) size=" << scan_result.size() << std::endl;
  }

  // op 36089: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0190", "key2612", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0190, key2612) size=" << scan_result.size() << std::endl;
  }

  // op 36090: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8632", "key9265", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8632, key9265) size=" << scan_result.size() << std::endl;
  }

  // op 36091: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7933", "key9936", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7933, key9936) size=" << scan_result.size() << std::endl;
  }

  // op 36092: put
  db->Put(write_options, "key9222", "value74131");

  // op 36093: put
  db->Put(write_options, "key4269", "value64305");

  // op 36094: put
  db->Put(write_options, "key10202", "value96267");

  // op 36095: get
  status = db->Get(read_options, "key4023", &value);
  if (status.ok()) {
    out << "GET key4023 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36096: delete
  db->Delete(write_options, "key5183");

  // op 36097: get
  status = db->Get(read_options, "key2449", &value);
  if (status.ok()) {
    out << "GET key2449 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36098: put
  db->Put(write_options, "key0599", "value87726");

  // op 36099: delete
  db->Delete(write_options, "key4159");

  // op 36100: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4717", "key4781", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4717, key4781) size=" << scan_result.size() << std::endl;
  }

  // op 36101: put
  db->Put(write_options, "key0681", "value72653");

  // op 36102: get
  status = db->Get(read_options, "key2611", &value);
  if (status.ok()) {
    out << "GET key2611 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36103: put
  db->Put(write_options, "key9298", "value97652");

  // op 36104: get
  status = db->Get(read_options, "key6893", &value);
  if (status.ok()) {
    out << "GET key6893 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36105: get
  status = db->Get(read_options, "key1654", &value);
  if (status.ok()) {
    out << "GET key1654 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36106: delete_range
  status = db->DeleteRange(write_options, "key3215", "key5223");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 36107: delete
  db->Delete(write_options, "key2080");

  // op 36108: put
  db->Put(write_options, "key1189", "value92823");

  // op 36109: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5144", "key10633", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5144, key10633) size=" << scan_result.size() << std::endl;
  }

  // op 36110: put
  db->Put(write_options, "key10234", "value111749");

  // op 36111: put
  db->Put(write_options, "key5321", "value58462");

  // op 36112: put
  db->Put(write_options, "key6534", "value93780");

  // op 36113: put
  db->Put(write_options, "key6847", "value98120");

  // op 36114: get
  status = db->Get(read_options, "key2259", &value);
  if (status.ok()) {
    out << "GET key2259 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36115: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1493", "key4459", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1493, key4459) size=" << scan_result.size() << std::endl;
  }

  // op 36116: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 36117: put
  db->Put(write_options, "key9630", "value3628");

  // op 36118: put
  db->Put(write_options, "key7763", "value16781");

  // op 36119: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0632", "key5804", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0632, key5804) size=" << scan_result.size() << std::endl;
  }

  // op 36120: put
  db->Put(write_options, "key8229", "value61193");

  // op 36121: put
  db->Put(write_options, "key0214", "value58576");

  // op 36122: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 36123: get
  status = db->Get(read_options, "key1371", &value);
  if (status.ok()) {
    out << "GET key1371 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36124: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3552", "key6019", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3552, key6019) size=" << scan_result.size() << std::endl;
  }

  // op 36125: put
  db->Put(write_options, "key10340", "value62769");

  // op 36126: get
  status = db->Get(read_options, "key2170", &value);
  if (status.ok()) {
    out << "GET key2170 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36127: put
  db->Put(write_options, "key6218", "value74899");

  // op 36128: put
  db->Put(write_options, "key2651", "value63335");

  // op 36129: put
  db->Put(write_options, "key8097", "value105300");

  // op 36130: put
  db->Put(write_options, "key0449", "value100322");

  // op 36131: put
  db->Put(write_options, "key0941", "value35244");

  // op 36132: put
  db->Put(write_options, "key8051", "value25441");

  // op 36133: put
  db->Put(write_options, "key6924", "value6959");

  // op 36134: get
  status = db->Get(read_options, "key8834", &value);
  if (status.ok()) {
    out << "GET key8834 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36135: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 36136: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4157", "key6593", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4157, key6593) size=" << scan_result.size() << std::endl;
  }

  // op 36137: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7620", "key11532", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7620, key11532) size=" << scan_result.size() << std::endl;
  }

  // op 36138: delete
  db->Delete(write_options, "key10530");

  // op 36139: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 36140: put
  db->Put(write_options, "key1259", "value95699");

  // op 36141: delete
  db->Delete(write_options, "key2271");

  // op 36142: put
  db->Put(write_options, "key3332", "value113469");

  // op 36143: delete
  db->Delete(write_options, "key7290");

  // op 36144: put
  db->Put(write_options, "key9660", "value105740");

  // op 36145: delete_range
  status = db->DeleteRange(write_options, "key5685", "key9164");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 36146: put
  db->Put(write_options, "key7145", "value45255");

  // op 36147: get
  status = db->Get(read_options, "key2943", &value);
  if (status.ok()) {
    out << "GET key2943 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36148: put
  db->Put(write_options, "key10195", "value93641");

  // op 36149: put
  db->Put(write_options, "key7287", "value73338");

  // op 36150: delete
  db->Delete(write_options, "key3368");

  // op 36151: put
  db->Put(write_options, "key3270", "value19695");

  // op 36152: delete_range
  status = db->DeleteRange(write_options, "key1894", "key10482");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 36153: put
  db->Put(write_options, "key2679", "value40731");

  // op 36154: delete_range
  status = db->DeleteRange(write_options, "key10283", "key11543");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 36155: delete
  db->Delete(write_options, "key4790");

  // op 36156: get
  status = db->Get(read_options, "key3436", &value);
  if (status.ok()) {
    out << "GET key3436 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36157: put
  db->Put(write_options, "key10116", "value112143");

  // op 36158: put
  db->Put(write_options, "key11463", "value103162");

  // op 36159: put
  db->Put(write_options, "key10839", "value100965");

  // op 36160: delete
  db->Delete(write_options, "key5316");

  // op 36161: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1276", "key2452", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1276, key2452) size=" << scan_result.size() << std::endl;
  }

  // op 36162: delete_range
  status = db->DeleteRange(write_options, "key1740", "key3731");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 36163: get
  status = db->Get(read_options, "key11024", &value);
  if (status.ok()) {
    out << "GET key11024 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36164: put
  db->Put(write_options, "key5527", "value86346");

  // op 36165: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0516", "key11118", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0516, key11118) size=" << scan_result.size() << std::endl;
  }

  // op 36166: delete
  db->Delete(write_options, "key0499");

  // op 36167: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5648", "key5790", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5648, key5790) size=" << scan_result.size() << std::endl;
  }

  // op 36168: get
  status = db->Get(read_options, "key11396", &value);
  if (status.ok()) {
    out << "GET key11396 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36169: delete
  db->Delete(write_options, "key3192");

  // op 36170: get
  status = db->Get(read_options, "key6749", &value);
  if (status.ok()) {
    out << "GET key6749 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36171: get
  status = db->Get(read_options, "key8459", &value);
  if (status.ok()) {
    out << "GET key8459 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36172: delete
  db->Delete(write_options, "key8223");

  // op 36173: get
  status = db->Get(read_options, "key7041", &value);
  if (status.ok()) {
    out << "GET key7041 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36174: get
  status = db->Get(read_options, "key10732", &value);
  if (status.ok()) {
    out << "GET key10732 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36175: delete
  db->Delete(write_options, "key8367");

  // op 36176: put
  db->Put(write_options, "key0808", "value94286");

  // op 36177: get
  status = db->Get(read_options, "key0391", &value);
  if (status.ok()) {
    out << "GET key0391 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36178: put
  db->Put(write_options, "key3854", "value40456");

  // op 36179: put
  db->Put(write_options, "key10707", "value13892");

  // op 36180: put
  db->Put(write_options, "key10498", "value50809");

  // op 36181: delete_range
  status = db->DeleteRange(write_options, "key2580", "key8913");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 36182: put
  db->Put(write_options, "key6875", "value21829");

  // op 36183: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2789", "key7698", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2789, key7698) size=" << scan_result.size() << std::endl;
  }

  // op 36184: put
  db->Put(write_options, "key2041", "value107872");

  // op 36185: put
  db->Put(write_options, "key10020", "value82894");

  // op 36186: put
  db->Put(write_options, "key2247", "value108195");

  // op 36187: put
  db->Put(write_options, "key5850", "value46045");

  // op 36188: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3176", "key10737", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3176, key10737) size=" << scan_result.size() << std::endl;
  }

  // op 36189: get
  status = db->Get(read_options, "key1723", &value);
  if (status.ok()) {
    out << "GET key1723 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36190: delete_range
  status = db->DeleteRange(write_options, "key1379", "key7983");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 36191: delete_range
  status = db->DeleteRange(write_options, "key8524", "key9000");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 36192: delete
  db->Delete(write_options, "key8849");

  // op 36193: put
  db->Put(write_options, "key11430", "value54532");

  // op 36194: delete_range
  status = db->DeleteRange(write_options, "key6837", "key7829");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 36195: delete_range
  status = db->DeleteRange(write_options, "key9413", "key10469");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 36196: delete_range
  status = db->DeleteRange(write_options, "key2452", "key3828");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 36197: get
  status = db->Get(read_options, "key8588", &value);
  if (status.ok()) {
    out << "GET key8588 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36198: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3522", "key9585", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3522, key9585) size=" << scan_result.size() << std::endl;
  }

  // op 36199: put
  db->Put(write_options, "key0344", "value5166");

  // op 36200: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 36201: delete
  db->Delete(write_options, "key3101");

  // op 36202: get
  status = db->Get(read_options, "key4386", &value);
  if (status.ok()) {
    out << "GET key4386 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36203: put
  db->Put(write_options, "key8898", "value37024");

  // op 36204: put
  db->Put(write_options, "key9296", "value106313");

  // op 36205: put
  db->Put(write_options, "key8972", "value48453");

  // op 36206: delete
  db->Delete(write_options, "key5879");

  // op 36207: put
  db->Put(write_options, "key5410", "value49783");

  // op 36208: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2036", "key6720", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2036, key6720) size=" << scan_result.size() << std::endl;
  }

  // op 36209: delete
  db->Delete(write_options, "key8903");

  // op 36210: delete
  db->Delete(write_options, "key8388");

  // op 36211: put
  db->Put(write_options, "key6147", "value76116");

  // op 36212: put
  db->Put(write_options, "key5588", "value78244");

  // op 36213: put
  db->Put(write_options, "key6060", "value19716");

  // op 36214: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3543", "key6567", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3543, key6567) size=" << scan_result.size() << std::endl;
  }

  // op 36215: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2524", "key5899", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2524, key5899) size=" << scan_result.size() << std::endl;
  }

  // op 36216: delete
  db->Delete(write_options, "key0994");

  // op 36217: put
  db->Put(write_options, "key8087", "value94968");

  // op 36218: put
  db->Put(write_options, "key8401", "value70554");

  // op 36219: get
  status = db->Get(read_options, "key0338", &value);
  if (status.ok()) {
    out << "GET key0338 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36220: put
  db->Put(write_options, "key9127", "value39571");

  // op 36221: delete_range
  status = db->DeleteRange(write_options, "key8581", "key10060");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 36222: get
  status = db->Get(read_options, "key11436", &value);
  if (status.ok()) {
    out << "GET key11436 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36223: put
  db->Put(write_options, "key3319", "value96022");

  // op 36224: put
  db->Put(write_options, "key7265", "value45173");

  // op 36225: put
  db->Put(write_options, "key6769", "value43663");

  // op 36226: delete
  db->Delete(write_options, "key8469");

  // op 36227: get
  status = db->Get(read_options, "key7200", &value);
  if (status.ok()) {
    out << "GET key7200 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36228: put
  db->Put(write_options, "key10881", "value65995");

  // op 36229: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2034", "key5456", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2034, key5456) size=" << scan_result.size() << std::endl;
  }

  // op 36230: delete
  db->Delete(write_options, "key11214");

  // op 36231: get
  status = db->Get(read_options, "key8264", &value);
  if (status.ok()) {
    out << "GET key8264 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36232: put
  db->Put(write_options, "key8320", "value95443");

  // op 36233: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5014", "key7423", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5014, key7423) size=" << scan_result.size() << std::endl;
  }

  // op 36234: put
  db->Put(write_options, "key1730", "value13714");

  // op 36235: delete_range
  status = db->DeleteRange(write_options, "key8838", "key11575");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 36236: put
  db->Put(write_options, "key4166", "value32382");

  // op 36237: put
  db->Put(write_options, "key5766", "value20090");

  // op 36238: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4812", "key5388", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4812, key5388) size=" << scan_result.size() << std::endl;
  }

  // op 36239: get
  status = db->Get(read_options, "key3302", &value);
  if (status.ok()) {
    out << "GET key3302 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36240: get
  status = db->Get(read_options, "key4229", &value);
  if (status.ok()) {
    out << "GET key4229 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36241: put
  db->Put(write_options, "key10592", "value6423");

  // op 36242: delete_range
  status = db->DeleteRange(write_options, "key8565", "key11133");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 36243: delete_range
  status = db->DeleteRange(write_options, "key0430", "key0461");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 36244: put
  db->Put(write_options, "key4305", "value56363");

  // op 36245: put
  db->Put(write_options, "key4351", "value117658");

  // op 36246: put
  db->Put(write_options, "key1597", "value6754");

  // op 36247: put
  db->Put(write_options, "key6643", "value93903");

  // op 36248: delete_range
  status = db->DeleteRange(write_options, "key0672", "key2337");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 36249: put
  db->Put(write_options, "key5810", "value58546");

  // op 36250: put
  db->Put(write_options, "key8251", "value77507");

  // op 36251: delete
  db->Delete(write_options, "key0762");

  // op 36252: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1557", "key5753", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1557, key5753) size=" << scan_result.size() << std::endl;
  }

  // op 36253: get
  status = db->Get(read_options, "key10433", &value);
  if (status.ok()) {
    out << "GET key10433 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36254: delete
  db->Delete(write_options, "key10123");

  // op 36255: delete_range
  status = db->DeleteRange(write_options, "key0327", "key3704");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 36256: put
  db->Put(write_options, "key8467", "value116372");

  // op 36257: delete_range
  status = db->DeleteRange(write_options, "key1373", "key3079");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 36258: put
  db->Put(write_options, "key9063", "value16807");

  // op 36259: delete
  db->Delete(write_options, "key4016");

  // op 36260: put
  db->Put(write_options, "key0452", "value41546");

  // op 36261: put
  db->Put(write_options, "key2440", "value60389");

  // op 36262: put
  db->Put(write_options, "key6208", "value51428");

  // op 36263: get
  status = db->Get(read_options, "key10485", &value);
  if (status.ok()) {
    out << "GET key10485 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36264: delete
  db->Delete(write_options, "key3841");

  // op 36265: delete
  db->Delete(write_options, "key1579");

  // op 36266: delete_range
  status = db->DeleteRange(write_options, "key4619", "key4678");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 36267: delete_range
  status = db->DeleteRange(write_options, "key4358", "key10548");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 36268: delete
  db->Delete(write_options, "key7589");

  // op 36269: put
  db->Put(write_options, "key1897", "value107062");

  // op 36270: delete_range
  status = db->DeleteRange(write_options, "key0825", "key8401");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 36271: delete
  db->Delete(write_options, "key0273");

  // op 36272: put
  db->Put(write_options, "key10493", "value11080");

  // op 36273: put
  db->Put(write_options, "key9316", "value76298");

  // op 36274: put
  db->Put(write_options, "key8016", "value100794");

  // op 36275: put
  db->Put(write_options, "key5921", "value62760");

  // op 36276: put
  db->Put(write_options, "key6176", "value41938");

  // op 36277: delete
  db->Delete(write_options, "key3870");

  // op 36278: put
  db->Put(write_options, "key4202", "value4849");

  // op 36279: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2471", "key6100", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2471, key6100) size=" << scan_result.size() << std::endl;
  }

  // op 36280: put
  db->Put(write_options, "key10925", "value53154");

  // op 36281: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 36282: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7390", "key9297", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7390, key9297) size=" << scan_result.size() << std::endl;
  }

  // op 36283: put
  db->Put(write_options, "key5727", "value95954");

  // op 36284: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7984", "key8608", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7984, key8608) size=" << scan_result.size() << std::endl;
  }

  // op 36285: get
  status = db->Get(read_options, "key4471", &value);
  if (status.ok()) {
    out << "GET key4471 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36286: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9806", "key10328", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9806, key10328) size=" << scan_result.size() << std::endl;
  }

  // op 36287: delete_range
  status = db->DeleteRange(write_options, "key6195", "key6345");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 36288: delete_range
  status = db->DeleteRange(write_options, "key3235", "key8785");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 36289: delete
  db->Delete(write_options, "key0311");

  // op 36290: get
  status = db->Get(read_options, "key7831", &value);
  if (status.ok()) {
    out << "GET key7831 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36291: delete_range
  status = db->DeleteRange(write_options, "key1025", "key5509");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 36292: put
  db->Put(write_options, "key5714", "value75311");

  // op 36293: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3633", "key7012", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3633, key7012) size=" << scan_result.size() << std::endl;
  }

  // op 36294: put
  db->Put(write_options, "key5817", "value112315");

  // op 36295: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3230", "key5057", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3230, key5057) size=" << scan_result.size() << std::endl;
  }

  // op 36296: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1761", "key4718", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1761, key4718) size=" << scan_result.size() << std::endl;
  }

  // op 36297: put
  db->Put(write_options, "key10530", "value72603");

  // op 36298: delete_range
  status = db->DeleteRange(write_options, "key9551", "key10783");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 36299: get
  status = db->Get(read_options, "key5905", &value);
  if (status.ok()) {
    out << "GET key5905 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36300: put
  db->Put(write_options, "key0471", "value45003");

  // op 36301: delete
  db->Delete(write_options, "key11472");

  // op 36302: put
  db->Put(write_options, "key8845", "value73141");

  // op 36303: get
  status = db->Get(read_options, "key11185", &value);
  if (status.ok()) {
    out << "GET key11185 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36304: delete
  db->Delete(write_options, "key0282");

  // op 36305: get
  status = db->Get(read_options, "key6640", &value);
  if (status.ok()) {
    out << "GET key6640 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36306: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 36307: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8101", "key10565", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8101, key10565) size=" << scan_result.size() << std::endl;
  }

  // op 36308: put
  db->Put(write_options, "key2846", "value37954");

  // op 36309: put
  db->Put(write_options, "key8443", "value80866");

  // op 36310: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3294", "key10550", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3294, key10550) size=" << scan_result.size() << std::endl;
  }

  // op 36311: delete_range
  status = db->DeleteRange(write_options, "key6756", "key9069");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 36312: get
  status = db->Get(read_options, "key1348", &value);
  if (status.ok()) {
    out << "GET key1348 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36313: put
  db->Put(write_options, "key5606", "value117928");

  // op 36314: delete
  db->Delete(write_options, "key6267");

  // op 36315: delete_range
  status = db->DeleteRange(write_options, "key5070", "key11334");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 36316: delete_range
  status = db->DeleteRange(write_options, "key0927", "key2997");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 36317: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 36318: put
  db->Put(write_options, "key9793", "value21398");

  // op 36319: put
  db->Put(write_options, "key11581", "value47123");

  // op 36320: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2991", "key10287", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2991, key10287) size=" << scan_result.size() << std::endl;
  }

  // op 36321: put
  db->Put(write_options, "key1015", "value45307");

  // op 36322: delete_range
  status = db->DeleteRange(write_options, "key1679", "key8169");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 36323: delete_range
  status = db->DeleteRange(write_options, "key2954", "key4377");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 36324: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2010", "key11374", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2010, key11374) size=" << scan_result.size() << std::endl;
  }

  // op 36325: get
  status = db->Get(read_options, "key8090", &value);
  if (status.ok()) {
    out << "GET key8090 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36326: get
  status = db->Get(read_options, "key4327", &value);
  if (status.ok()) {
    out << "GET key4327 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36327: put
  db->Put(write_options, "key11078", "value20463");

  // op 36328: get
  status = db->Get(read_options, "key9280", &value);
  if (status.ok()) {
    out << "GET key9280 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36329: put
  db->Put(write_options, "key6032", "value14572");

  // op 36330: get
  status = db->Get(read_options, "key1670", &value);
  if (status.ok()) {
    out << "GET key1670 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36331: get
  status = db->Get(read_options, "key5532", &value);
  if (status.ok()) {
    out << "GET key5532 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36332: get
  status = db->Get(read_options, "key5881", &value);
  if (status.ok()) {
    out << "GET key5881 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36333: delete_range
  status = db->DeleteRange(write_options, "key2145", "key8790");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 36334: put
  db->Put(write_options, "key2738", "value93639");

  // op 36335: put
  db->Put(write_options, "key4132", "value38097");

  // op 36336: delete_range
  status = db->DeleteRange(write_options, "key5938", "key11277");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 36337: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7067", "key8907", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7067, key8907) size=" << scan_result.size() << std::endl;
  }

  // op 36338: put
  db->Put(write_options, "key0155", "value39942");

  // op 36339: put
  db->Put(write_options, "key1925", "value94250");

  // op 36340: put
  db->Put(write_options, "key3693", "value109044");

  // op 36341: put
  db->Put(write_options, "key2285", "value23040");

  // op 36342: get
  status = db->Get(read_options, "key2911", &value);
  if (status.ok()) {
    out << "GET key2911 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36343: delete
  db->Delete(write_options, "key0215");

  // op 36344: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3177", "key5381", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3177, key5381) size=" << scan_result.size() << std::endl;
  }

  // op 36345: delete
  db->Delete(write_options, "key1401");

  // op 36346: delete_range
  status = db->DeleteRange(write_options, "key4492", "key11224");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 36347: get
  status = db->Get(read_options, "key7393", &value);
  if (status.ok()) {
    out << "GET key7393 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36348: put
  db->Put(write_options, "key5699", "value25062");

  // op 36349: put
  db->Put(write_options, "key4447", "value112558");

  // op 36350: put
  db->Put(write_options, "key4933", "value104994");

  // op 36351: delete
  db->Delete(write_options, "key1957");

  // op 36352: get
  status = db->Get(read_options, "key11386", &value);
  if (status.ok()) {
    out << "GET key11386 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36353: get
  status = db->Get(read_options, "key8278", &value);
  if (status.ok()) {
    out << "GET key8278 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36354: put
  db->Put(write_options, "key2133", "value21685");

  // op 36355: get
  status = db->Get(read_options, "key8868", &value);
  if (status.ok()) {
    out << "GET key8868 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36356: get
  status = db->Get(read_options, "key1690", &value);
  if (status.ok()) {
    out << "GET key1690 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36357: get
  status = db->Get(read_options, "key0429", &value);
  if (status.ok()) {
    out << "GET key0429 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36358: delete
  db->Delete(write_options, "key7627");

  // op 36359: get
  status = db->Get(read_options, "key6955", &value);
  if (status.ok()) {
    out << "GET key6955 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36360: put
  db->Put(write_options, "key4321", "value32035");

  // op 36361: get
  status = db->Get(read_options, "key1021", &value);
  if (status.ok()) {
    out << "GET key1021 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36362: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10507", "key11086", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10507, key11086) size=" << scan_result.size() << std::endl;
  }

  // op 36363: put
  db->Put(write_options, "key0500", "value53945");

  // op 36364: put
  db->Put(write_options, "key9992", "value90781");

  // op 36365: delete
  db->Delete(write_options, "key0369");

  // op 36366: put
  db->Put(write_options, "key8210", "value51492");

  // op 36367: put
  db->Put(write_options, "key6825", "value43624");

  // op 36368: put
  db->Put(write_options, "key0413", "value6968");

  // op 36369: put
  db->Put(write_options, "key0183", "value32750");

  // op 36370: put
  db->Put(write_options, "key10157", "value100634");

  // op 36371: put
  db->Put(write_options, "key7748", "value24491");

  // op 36372: delete
  db->Delete(write_options, "key5737");

  // op 36373: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3583", "key11369", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3583, key11369) size=" << scan_result.size() << std::endl;
  }

  // op 36374: get
  status = db->Get(read_options, "key6496", &value);
  if (status.ok()) {
    out << "GET key6496 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36375: delete
  db->Delete(write_options, "key0833");

  // op 36376: put
  db->Put(write_options, "key5571", "value39989");

  // op 36377: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3623", "key4540", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3623, key4540) size=" << scan_result.size() << std::endl;
  }

  // op 36378: put
  db->Put(write_options, "key11523", "value116665");

  // op 36379: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 36380: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3940", "key8616", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3940, key8616) size=" << scan_result.size() << std::endl;
  }

  // op 36381: put
  db->Put(write_options, "key5541", "value15527");

  // op 36382: get
  status = db->Get(read_options, "key9355", &value);
  if (status.ok()) {
    out << "GET key9355 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36383: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0991", "key4605", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0991, key4605) size=" << scan_result.size() << std::endl;
  }

  // op 36384: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 36385: put
  db->Put(write_options, "key8900", "value93572");

  // op 36386: put
  db->Put(write_options, "key9538", "value98524");

  // op 36387: put
  db->Put(write_options, "key1004", "value41730");

  // op 36388: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2327", "key5005", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2327, key5005) size=" << scan_result.size() << std::endl;
  }

  // op 36389: delete
  db->Delete(write_options, "key0945");

  // op 36390: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0101", "key11095", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0101, key11095) size=" << scan_result.size() << std::endl;
  }

  // op 36391: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 36392: get
  status = db->Get(read_options, "key1633", &value);
  if (status.ok()) {
    out << "GET key1633 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36393: put
  db->Put(write_options, "key5517", "value91005");

  // op 36394: put
  db->Put(write_options, "key0983", "value35692");

  // op 36395: delete
  db->Delete(write_options, "key2159");

  // op 36396: put
  db->Put(write_options, "key2745", "value118273");

  // op 36397: put
  db->Put(write_options, "key11016", "value57557");

  // op 36398: delete_range
  status = db->DeleteRange(write_options, "key3065", "key10693");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 36399: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0227", "key7274", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0227, key7274) size=" << scan_result.size() << std::endl;
  }

  // op 36400: get
  status = db->Get(read_options, "key8870", &value);
  if (status.ok()) {
    out << "GET key8870 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36401: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6553", "key10242", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6553, key10242) size=" << scan_result.size() << std::endl;
  }

  // op 36402: put
  db->Put(write_options, "key9242", "value116588");

  // op 36403: delete
  db->Delete(write_options, "key9237");

  // op 36404: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3597", "key8279", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3597, key8279) size=" << scan_result.size() << std::endl;
  }

  // op 36405: get
  status = db->Get(read_options, "key5877", &value);
  if (status.ok()) {
    out << "GET key5877 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36406: delete
  db->Delete(write_options, "key3244");

  // op 36407: delete_range
  status = db->DeleteRange(write_options, "key3535", "key5573");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 36408: put
  db->Put(write_options, "key1172", "value57392");

  // op 36409: put
  db->Put(write_options, "key10384", "value76291");

  // op 36410: get
  status = db->Get(read_options, "key3792", &value);
  if (status.ok()) {
    out << "GET key3792 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36411: put
  db->Put(write_options, "key6304", "value17180");

  // op 36412: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5203", "key5451", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5203, key5451) size=" << scan_result.size() << std::endl;
  }

  // op 36413: put
  db->Put(write_options, "key0083", "value53470");

  // op 36414: put
  db->Put(write_options, "key6613", "value27815");

  // op 36415: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 36416: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6932", "key8489", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6932, key8489) size=" << scan_result.size() << std::endl;
  }

  // op 36417: delete_range
  status = db->DeleteRange(write_options, "key0470", "key9249");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 36418: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0149", "key1481", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0149, key1481) size=" << scan_result.size() << std::endl;
  }

  // op 36419: get
  status = db->Get(read_options, "key8994", &value);
  if (status.ok()) {
    out << "GET key8994 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36420: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 36421: delete_range
  status = db->DeleteRange(write_options, "key10023", "key11241");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 36422: put
  db->Put(write_options, "key11189", "value54072");

  // op 36423: get
  status = db->Get(read_options, "key4725", &value);
  if (status.ok()) {
    out << "GET key4725 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36424: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0847", "key0986", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0847, key0986) size=" << scan_result.size() << std::endl;
  }

  // op 36425: get
  status = db->Get(read_options, "key6633", &value);
  if (status.ok()) {
    out << "GET key6633 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36426: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3031", "key9507", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3031, key9507) size=" << scan_result.size() << std::endl;
  }

  // op 36427: put
  db->Put(write_options, "key10790", "value38713");

  // op 36428: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3834", "key5496", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3834, key5496) size=" << scan_result.size() << std::endl;
  }

  // op 36429: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4098", "key5682", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4098, key5682) size=" << scan_result.size() << std::endl;
  }

  // op 36430: delete
  db->Delete(write_options, "key5763");

  // op 36431: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1244", "key4541", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1244, key4541) size=" << scan_result.size() << std::endl;
  }

  // op 36432: get
  status = db->Get(read_options, "key10100", &value);
  if (status.ok()) {
    out << "GET key10100 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36433: put
  db->Put(write_options, "key8870", "value113295");

  // op 36434: put
  db->Put(write_options, "key7752", "value4991");

  // op 36435: delete
  db->Delete(write_options, "key8908");

  // op 36436: put
  db->Put(write_options, "key4955", "value46023");

  // op 36437: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 36438: get
  status = db->Get(read_options, "key3441", &value);
  if (status.ok()) {
    out << "GET key3441 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36439: put
  db->Put(write_options, "key7796", "value50186");

  // op 36440: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0036", "key10206", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0036, key10206) size=" << scan_result.size() << std::endl;
  }

  // op 36441: put
  db->Put(write_options, "key0769", "value13180");

  // op 36442: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 36443: put
  db->Put(write_options, "key0811", "value65748");

  // op 36444: put
  db->Put(write_options, "key6021", "value114636");

  // op 36445: put
  db->Put(write_options, "key9182", "value6254");

  // op 36446: put
  db->Put(write_options, "key3395", "value108767");

  // op 36447: put
  db->Put(write_options, "key11191", "value98655");

  // op 36448: delete_range
  status = db->DeleteRange(write_options, "key1220", "key2839");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 36449: put
  db->Put(write_options, "key3653", "value110753");

  // op 36450: get
  status = db->Get(read_options, "key5890", &value);
  if (status.ok()) {
    out << "GET key5890 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36451: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6714", "key7096", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6714, key7096) size=" << scan_result.size() << std::endl;
  }

  // op 36452: get
  status = db->Get(read_options, "key4167", &value);
  if (status.ok()) {
    out << "GET key4167 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36453: delete_range
  status = db->DeleteRange(write_options, "key6335", "key10265");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 36454: put
  db->Put(write_options, "key6935", "value35826");

  // op 36455: put
  db->Put(write_options, "key4109", "value97122");

  // op 36456: delete_range
  status = db->DeleteRange(write_options, "key3121", "key5531");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 36457: put
  db->Put(write_options, "key4154", "value51707");

  // op 36458: put
  db->Put(write_options, "key3214", "value48260");

  // op 36459: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0257", "key9121", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0257, key9121) size=" << scan_result.size() << std::endl;
  }

  // op 36460: put
  db->Put(write_options, "key5985", "value75562");

  // op 36461: put
  db->Put(write_options, "key0359", "value104074");

  // op 36462: put
  db->Put(write_options, "key0560", "value75145");

  // op 36463: get
  status = db->Get(read_options, "key0534", &value);
  if (status.ok()) {
    out << "GET key0534 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36464: put
  db->Put(write_options, "key11017", "value57621");

  // op 36465: get
  status = db->Get(read_options, "key9709", &value);
  if (status.ok()) {
    out << "GET key9709 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36466: put
  db->Put(write_options, "key1784", "value44407");

  // op 36467: put
  db->Put(write_options, "key8866", "value25479");

  // op 36468: get
  status = db->Get(read_options, "key9480", &value);
  if (status.ok()) {
    out << "GET key9480 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36469: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0835", "key7527", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0835, key7527) size=" << scan_result.size() << std::endl;
  }

  // op 36470: get
  status = db->Get(read_options, "key2853", &value);
  if (status.ok()) {
    out << "GET key2853 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36471: put
  db->Put(write_options, "key6338", "value31747");

  // op 36472: get
  status = db->Get(read_options, "key4230", &value);
  if (status.ok()) {
    out << "GET key4230 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36473: put
  db->Put(write_options, "key0803", "value114846");

  // op 36474: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7377", "key9650", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7377, key9650) size=" << scan_result.size() << std::endl;
  }

  // op 36475: put
  db->Put(write_options, "key1835", "value47029");

  // op 36476: delete_range
  status = db->DeleteRange(write_options, "key1984", "key2583");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 36477: put
  db->Put(write_options, "key4210", "value90138");

  // op 36478: get
  status = db->Get(read_options, "key6493", &value);
  if (status.ok()) {
    out << "GET key6493 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36479: put
  db->Put(write_options, "key0054", "value48236");

  // op 36480: delete_range
  status = db->DeleteRange(write_options, "key6037", "key9158");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 36481: get
  status = db->Get(read_options, "key8909", &value);
  if (status.ok()) {
    out << "GET key8909 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36482: put
  db->Put(write_options, "key11569", "value100448");

  // op 36483: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 36484: delete
  db->Delete(write_options, "key8578");

  // op 36485: put
  db->Put(write_options, "key5877", "value47631");

  // op 36486: delete_range
  status = db->DeleteRange(write_options, "key3089", "key7477");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 36487: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 36488: put
  db->Put(write_options, "key11021", "value0527");

  // op 36489: put
  db->Put(write_options, "key6002", "value14180");

  // op 36490: put
  db->Put(write_options, "key2823", "value94469");

  // op 36491: put
  db->Put(write_options, "key2382", "value103497");

  // op 36492: get
  status = db->Get(read_options, "key9765", &value);
  if (status.ok()) {
    out << "GET key9765 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36493: put
  db->Put(write_options, "key0770", "value85527");

  // op 36494: delete_range
  status = db->DeleteRange(write_options, "key2895", "key9758");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 36495: delete_range
  status = db->DeleteRange(write_options, "key1963", "key9692");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 36496: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2761", "key9026", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2761, key9026) size=" << scan_result.size() << std::endl;
  }

  // op 36497: get
  status = db->Get(read_options, "key9151", &value);
  if (status.ok()) {
    out << "GET key9151 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36498: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 36499: delete_range
  status = db->DeleteRange(write_options, "key2721", "key3854");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 36500: put
  db->Put(write_options, "key1515", "value68418");

  // op 36501: get
  status = db->Get(read_options, "key7384", &value);
  if (status.ok()) {
    out << "GET key7384 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36502: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5741", "key11712", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5741, key11712) size=" << scan_result.size() << std::endl;
  }

  // op 36503: delete_range
  status = db->DeleteRange(write_options, "key1860", "key3605");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 36504: get
  status = db->Get(read_options, "key1290", &value);
  if (status.ok()) {
    out << "GET key1290 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36505: delete_range
  status = db->DeleteRange(write_options, "key2925", "key5517");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 36506: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5476", "key7945", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5476, key7945) size=" << scan_result.size() << std::endl;
  }

  // op 36507: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6472", "key10775", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6472, key10775) size=" << scan_result.size() << std::endl;
  }

  // op 36508: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9969", "key11384", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9969, key11384) size=" << scan_result.size() << std::endl;
  }

  // op 36509: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5746", "key9220", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5746, key9220) size=" << scan_result.size() << std::endl;
  }

  // op 36510: put
  db->Put(write_options, "key1831", "value21797");

  // op 36511: get
  status = db->Get(read_options, "key7683", &value);
  if (status.ok()) {
    out << "GET key7683 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36512: put
  db->Put(write_options, "key4876", "value43635");

  // op 36513: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0558", "key9656", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0558, key9656) size=" << scan_result.size() << std::endl;
  }

  // op 36514: put
  db->Put(write_options, "key3583", "value34663");

  // op 36515: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 36516: put
  db->Put(write_options, "key3134", "value28482");

  // op 36517: put
  db->Put(write_options, "key3995", "value14339");

  // op 36518: put
  db->Put(write_options, "key9697", "value26723");

  // op 36519: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1557", "key7999", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1557, key7999) size=" << scan_result.size() << std::endl;
  }

  // op 36520: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1492", "key2949", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1492, key2949) size=" << scan_result.size() << std::endl;
  }

  // op 36521: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4748", "key9240", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4748, key9240) size=" << scan_result.size() << std::endl;
  }

  // op 36522: put
  db->Put(write_options, "key1494", "value3412");

  // op 36523: get
  status = db->Get(read_options, "key0235", &value);
  if (status.ok()) {
    out << "GET key0235 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36524: put
  db->Put(write_options, "key9726", "value46811");

  // op 36525: delete_range
  status = db->DeleteRange(write_options, "key5129", "key7872");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 36526: get
  status = db->Get(read_options, "key6062", &value);
  if (status.ok()) {
    out << "GET key6062 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36527: delete_range
  status = db->DeleteRange(write_options, "key0609", "key2593");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 36528: put
  db->Put(write_options, "key2857", "value47363");

  // op 36529: put
  db->Put(write_options, "key6275", "value40355");

  // op 36530: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4479", "key10100", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4479, key10100) size=" << scan_result.size() << std::endl;
  }

  // op 36531: delete_range
  status = db->DeleteRange(write_options, "key3631", "key6490");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 36532: delete_range
  status = db->DeleteRange(write_options, "key0097", "key5066");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 36533: delete_range
  status = db->DeleteRange(write_options, "key1414", "key1896");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 36534: delete
  db->Delete(write_options, "key7681");

  // op 36535: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 36536: put
  db->Put(write_options, "key9221", "value37075");

  // op 36537: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 36538: delete_range
  status = db->DeleteRange(write_options, "key3464", "key4190");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 36539: get
  status = db->Get(read_options, "key8866", &value);
  if (status.ok()) {
    out << "GET key8866 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36540: put
  db->Put(write_options, "key4440", "value77868");

  // op 36541: put
  db->Put(write_options, "key6872", "value38553");

  // op 36542: delete
  db->Delete(write_options, "key3218");

  // op 36543: delete_range
  status = db->DeleteRange(write_options, "key2596", "key9151");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 36544: put
  db->Put(write_options, "key10014", "value64827");

  // op 36545: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2455", "key11360", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2455, key11360) size=" << scan_result.size() << std::endl;
  }

  // op 36546: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6186", "key8057", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6186, key8057) size=" << scan_result.size() << std::endl;
  }

  // op 36547: put
  db->Put(write_options, "key2501", "value39881");

  // op 36548: put
  db->Put(write_options, "key0827", "value34487");

  // op 36549: delete
  db->Delete(write_options, "key7910");

  // op 36550: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 36551: delete_range
  status = db->DeleteRange(write_options, "key10063", "key10237");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 36552: put
  db->Put(write_options, "key3375", "value27615");

  // op 36553: get
  status = db->Get(read_options, "key8545", &value);
  if (status.ok()) {
    out << "GET key8545 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36554: get
  status = db->Get(read_options, "key2213", &value);
  if (status.ok()) {
    out << "GET key2213 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36555: put
  db->Put(write_options, "key10740", "value107688");

  // op 36556: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0321", "key5362", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0321, key5362) size=" << scan_result.size() << std::endl;
  }

  // op 36557: put
  db->Put(write_options, "key0046", "value4170");

  // op 36558: delete
  db->Delete(write_options, "key1380");

  // op 36559: delete_range
  status = db->DeleteRange(write_options, "key1534", "key7234");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 36560: get
  status = db->Get(read_options, "key0368", &value);
  if (status.ok()) {
    out << "GET key0368 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36561: put
  db->Put(write_options, "key5489", "value23872");

  // op 36562: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 36563: put
  db->Put(write_options, "key3111", "value6537");

  // op 36564: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2731", "key3648", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2731, key3648) size=" << scan_result.size() << std::endl;
  }

  // op 36565: put
  db->Put(write_options, "key2538", "value63165");

  // op 36566: get
  status = db->Get(read_options, "key1997", &value);
  if (status.ok()) {
    out << "GET key1997 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36567: get
  status = db->Get(read_options, "key1718", &value);
  if (status.ok()) {
    out << "GET key1718 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36568: put
  db->Put(write_options, "key2305", "value20426");

  // op 36569: put
  db->Put(write_options, "key2118", "value108781");

  // op 36570: delete
  db->Delete(write_options, "key7959");

  // op 36571: put
  db->Put(write_options, "key10561", "value79595");

  // op 36572: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6002", "key6174", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6002, key6174) size=" << scan_result.size() << std::endl;
  }

  // op 36573: put
  db->Put(write_options, "key11546", "value21539");

  // op 36574: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5094", "key9182", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5094, key9182) size=" << scan_result.size() << std::endl;
  }

  // op 36575: delete_range
  status = db->DeleteRange(write_options, "key4096", "key5093");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 36576: get
  status = db->Get(read_options, "key8372", &value);
  if (status.ok()) {
    out << "GET key8372 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36577: put
  db->Put(write_options, "key9923", "value7076");

  // op 36578: put
  db->Put(write_options, "key3092", "value49589");

  // op 36579: delete
  db->Delete(write_options, "key9812");

  // op 36580: put
  db->Put(write_options, "key3417", "value22723");

  // op 36581: put
  db->Put(write_options, "key1164", "value4055");

  // op 36582: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0705", "key4985", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0705, key4985) size=" << scan_result.size() << std::endl;
  }

  // op 36583: get
  status = db->Get(read_options, "key3328", &value);
  if (status.ok()) {
    out << "GET key3328 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36584: put
  db->Put(write_options, "key1993", "value56882");

  // op 36585: put
  db->Put(write_options, "key1348", "value42768");

  // op 36586: put
  db->Put(write_options, "key2999", "value28657");

  // op 36587: delete_range
  status = db->DeleteRange(write_options, "key0601", "key6599");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 36588: get
  status = db->Get(read_options, "key8513", &value);
  if (status.ok()) {
    out << "GET key8513 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36589: put
  db->Put(write_options, "key8742", "value54211");

  // op 36590: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1392", "key10516", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1392, key10516) size=" << scan_result.size() << std::endl;
  }

  // op 36591: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3013", "key3340", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3013, key3340) size=" << scan_result.size() << std::endl;
  }

  // op 36592: put
  db->Put(write_options, "key5907", "value26584");

  // op 36593: put
  db->Put(write_options, "key4016", "value58686");

  // op 36594: delete
  db->Delete(write_options, "key4761");

  // op 36595: get
  status = db->Get(read_options, "key1295", &value);
  if (status.ok()) {
    out << "GET key1295 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36596: delete
  db->Delete(write_options, "key6966");

  // op 36597: put
  db->Put(write_options, "key10492", "value44814");

  // op 36598: delete
  db->Delete(write_options, "key2709");

  // op 36599: put
  db->Put(write_options, "key2402", "value10245");

  // op 36600: delete_range
  status = db->DeleteRange(write_options, "key6545", "key9310");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 36601: put
  db->Put(write_options, "key2041", "value116927");

  // op 36602: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3626", "key9983", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3626, key9983) size=" << scan_result.size() << std::endl;
  }

  // op 36603: put
  db->Put(write_options, "key9932", "value108660");

  // op 36604: put
  db->Put(write_options, "key4667", "value41372");

  // op 36605: delete
  db->Delete(write_options, "key0145");

  // op 36606: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1010", "key8623", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1010, key8623) size=" << scan_result.size() << std::endl;
  }

  // op 36607: get
  status = db->Get(read_options, "key3628", &value);
  if (status.ok()) {
    out << "GET key3628 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36608: get
  status = db->Get(read_options, "key2906", &value);
  if (status.ok()) {
    out << "GET key2906 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36609: put
  db->Put(write_options, "key0153", "value87667");

  // op 36610: delete
  db->Delete(write_options, "key11253");

  // op 36611: put
  db->Put(write_options, "key3769", "value26069");

  // op 36612: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 36613: put
  db->Put(write_options, "key7413", "value110811");

  // op 36614: delete_range
  status = db->DeleteRange(write_options, "key2685", "key4537");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 36615: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7231", "key9617", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7231, key9617) size=" << scan_result.size() << std::endl;
  }

  // op 36616: delete
  db->Delete(write_options, "key5368");

  // op 36617: get
  status = db->Get(read_options, "key4868", &value);
  if (status.ok()) {
    out << "GET key4868 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36618: get
  status = db->Get(read_options, "key11000", &value);
  if (status.ok()) {
    out << "GET key11000 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36619: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0634", "key4108", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0634, key4108) size=" << scan_result.size() << std::endl;
  }

  // op 36620: get
  status = db->Get(read_options, "key7272", &value);
  if (status.ok()) {
    out << "GET key7272 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36621: get
  status = db->Get(read_options, "key10910", &value);
  if (status.ok()) {
    out << "GET key10910 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36622: put
  db->Put(write_options, "key6931", "value58705");

  // op 36623: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6979", "key10052", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6979, key10052) size=" << scan_result.size() << std::endl;
  }

  // op 36624: put
  db->Put(write_options, "key7318", "value101428");

  // op 36625: put
  db->Put(write_options, "key5389", "value74998");

  // op 36626: put
  db->Put(write_options, "key8307", "value16119");

  // op 36627: delete_range
  status = db->DeleteRange(write_options, "key0201", "key1093");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 36628: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10929", "key11115", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10929, key11115) size=" << scan_result.size() << std::endl;
  }

  // op 36629: put
  db->Put(write_options, "key6076", "value95875");

  // op 36630: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0642", "key2817", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0642, key2817) size=" << scan_result.size() << std::endl;
  }

  // op 36631: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 36632: get
  status = db->Get(read_options, "key0038", &value);
  if (status.ok()) {
    out << "GET key0038 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36633: put
  db->Put(write_options, "key11498", "value85211");

  // op 36634: put
  db->Put(write_options, "key6684", "value100884");

  // op 36635: put
  db->Put(write_options, "key8739", "value32816");

  // op 36636: put
  db->Put(write_options, "key9422", "value108623");

  // op 36637: delete_range
  status = db->DeleteRange(write_options, "key7314", "key7323");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 36638: put
  db->Put(write_options, "key3508", "value2919");

  // op 36639: put
  db->Put(write_options, "key0446", "value41178");

  // op 36640: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 36641: put
  db->Put(write_options, "key11488", "value68376");

  // op 36642: put
  db->Put(write_options, "key2330", "value78748");

  // op 36643: get
  status = db->Get(read_options, "key5741", &value);
  if (status.ok()) {
    out << "GET key5741 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36644: put
  db->Put(write_options, "key10909", "value79469");

  // op 36645: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3168", "key3902", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3168, key3902) size=" << scan_result.size() << std::endl;
  }

  // op 36646: put
  db->Put(write_options, "key8105", "value96534");

  // op 36647: put
  db->Put(write_options, "key0314", "value9386");

  // op 36648: put
  db->Put(write_options, "key6378", "value117248");

  // op 36649: get
  status = db->Get(read_options, "key3250", &value);
  if (status.ok()) {
    out << "GET key3250 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36650: get
  status = db->Get(read_options, "key8125", &value);
  if (status.ok()) {
    out << "GET key8125 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36651: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0917", "key7885", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0917, key7885) size=" << scan_result.size() << std::endl;
  }

  // op 36652: put
  db->Put(write_options, "key6776", "value108821");

  // op 36653: put
  db->Put(write_options, "key6835", "value114210");

  // op 36654: put
  db->Put(write_options, "key8626", "value68461");

  // op 36655: delete_range
  status = db->DeleteRange(write_options, "key2863", "key10093");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 36656: put
  db->Put(write_options, "key8914", "value1482");

  // op 36657: get
  status = db->Get(read_options, "key3892", &value);
  if (status.ok()) {
    out << "GET key3892 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36658: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2603", "key10529", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2603, key10529) size=" << scan_result.size() << std::endl;
  }

  // op 36659: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 36660: get
  status = db->Get(read_options, "key3517", &value);
  if (status.ok()) {
    out << "GET key3517 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36661: delete_range
  status = db->DeleteRange(write_options, "key4431", "key9508");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 36662: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5623", "key6060", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5623, key6060) size=" << scan_result.size() << std::endl;
  }

  // op 36663: put
  db->Put(write_options, "key2341", "value23756");

  // op 36664: put
  db->Put(write_options, "key7287", "value3204");

  // op 36665: put
  db->Put(write_options, "key7888", "value42986");

  // op 36666: get
  status = db->Get(read_options, "key7094", &value);
  if (status.ok()) {
    out << "GET key7094 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36667: put
  db->Put(write_options, "key10526", "value89971");

  // op 36668: put
  db->Put(write_options, "key4273", "value85185");

  // op 36669: put
  db->Put(write_options, "key4114", "value8596");

  // op 36670: put
  db->Put(write_options, "key9552", "value12919");

  // op 36671: get
  status = db->Get(read_options, "key7249", &value);
  if (status.ok()) {
    out << "GET key7249 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36672: put
  db->Put(write_options, "key10296", "value105360");

  // op 36673: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5152", "key6664", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5152, key6664) size=" << scan_result.size() << std::endl;
  }

  // op 36674: delete
  db->Delete(write_options, "key2061");

  // op 36675: get
  status = db->Get(read_options, "key0757", &value);
  if (status.ok()) {
    out << "GET key0757 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36676: put
  db->Put(write_options, "key4501", "value60651");

  // op 36677: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0168", "key6811", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0168, key6811) size=" << scan_result.size() << std::endl;
  }

  // op 36678: put
  db->Put(write_options, "key8001", "value102428");

  // op 36679: get
  status = db->Get(read_options, "key11528", &value);
  if (status.ok()) {
    out << "GET key11528 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36680: delete_range
  status = db->DeleteRange(write_options, "key3215", "key4840");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 36681: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1624", "key10038", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1624, key10038) size=" << scan_result.size() << std::endl;
  }

  // op 36682: get
  status = db->Get(read_options, "key5894", &value);
  if (status.ok()) {
    out << "GET key5894 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36683: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0329", "key11140", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0329, key11140) size=" << scan_result.size() << std::endl;
  }

  // op 36684: put
  db->Put(write_options, "key6165", "value102119");

  // op 36685: delete_range
  status = db->DeleteRange(write_options, "key4388", "key10740");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 36686: put
  db->Put(write_options, "key0337", "value77091");

  // op 36687: put
  db->Put(write_options, "key7998", "value25778");

  // op 36688: put
  db->Put(write_options, "key3705", "value81519");

  // op 36689: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9855", "key11222", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9855, key11222) size=" << scan_result.size() << std::endl;
  }

  // op 36690: put
  db->Put(write_options, "key8021", "value33828");

  // op 36691: put
  db->Put(write_options, "key0675", "value44249");

  // op 36692: delete_range
  status = db->DeleteRange(write_options, "key8937", "key9663");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 36693: get
  status = db->Get(read_options, "key0236", &value);
  if (status.ok()) {
    out << "GET key0236 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36694: put
  db->Put(write_options, "key8161", "value100761");

  // op 36695: put
  db->Put(write_options, "key3887", "value4415");

  // op 36696: put
  db->Put(write_options, "key8532", "value94341");

  // op 36697: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6395", "key10444", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6395, key10444) size=" << scan_result.size() << std::endl;
  }

  // op 36698: put
  db->Put(write_options, "key9575", "value63319");

  // op 36699: delete
  db->Delete(write_options, "key9150");

  // op 36700: get
  status = db->Get(read_options, "key10245", &value);
  if (status.ok()) {
    out << "GET key10245 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36701: put
  db->Put(write_options, "key9252", "value109658");

  // op 36702: put
  db->Put(write_options, "key3024", "value87640");

  // op 36703: put
  db->Put(write_options, "key3281", "value51916");

  // op 36704: delete
  db->Delete(write_options, "key4808");

  // op 36705: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4061", "key8482", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4061, key8482) size=" << scan_result.size() << std::endl;
  }

  // op 36706: put
  db->Put(write_options, "key9322", "value109401");

  // op 36707: get
  status = db->Get(read_options, "key2893", &value);
  if (status.ok()) {
    out << "GET key2893 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36708: put
  db->Put(write_options, "key2726", "value60164");

  // op 36709: delete
  db->Delete(write_options, "key9991");

  // op 36710: put
  db->Put(write_options, "key10218", "value106724");

  // op 36711: scan
  scan_result.clear();
  status = db->Scan(read_options, "key11250", "key11715", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key11250, key11715) size=" << scan_result.size() << std::endl;
  }

  // op 36712: delete
  db->Delete(write_options, "key2262");

  // op 36713: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0451", "key9238", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0451, key9238) size=" << scan_result.size() << std::endl;
  }

  // op 36714: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 36715: delete
  db->Delete(write_options, "key3196");

  // op 36716: put
  db->Put(write_options, "key8033", "value85706");

  // op 36717: delete_range
  status = db->DeleteRange(write_options, "key2563", "key11469");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 36718: put
  db->Put(write_options, "key5484", "value47217");

  // op 36719: put
  db->Put(write_options, "key5743", "value47741");

  // op 36720: delete
  db->Delete(write_options, "key0072");

  // op 36721: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3136", "key6388", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3136, key6388) size=" << scan_result.size() << std::endl;
  }

  // op 36722: delete_range
  status = db->DeleteRange(write_options, "key0224", "key6545");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 36723: put
  db->Put(write_options, "key4523", "value91767");

  // op 36724: delete_range
  status = db->DeleteRange(write_options, "key9096", "key11409");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 36725: get
  status = db->Get(read_options, "key3432", &value);
  if (status.ok()) {
    out << "GET key3432 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36726: get
  status = db->Get(read_options, "key7306", &value);
  if (status.ok()) {
    out << "GET key7306 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36727: get
  status = db->Get(read_options, "key6753", &value);
  if (status.ok()) {
    out << "GET key6753 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36728: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 36729: get
  status = db->Get(read_options, "key9765", &value);
  if (status.ok()) {
    out << "GET key9765 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36730: delete_range
  status = db->DeleteRange(write_options, "key6282", "key11543");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 36731: get
  status = db->Get(read_options, "key9762", &value);
  if (status.ok()) {
    out << "GET key9762 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36732: put
  db->Put(write_options, "key4163", "value30634");

  // op 36733: delete_range
  status = db->DeleteRange(write_options, "key3229", "key6904");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 36734: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5566", "key6821", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5566, key6821) size=" << scan_result.size() << std::endl;
  }

  // op 36735: put
  db->Put(write_options, "key1616", "value67431");

  // op 36736: put
  db->Put(write_options, "key10219", "value27598");

  // op 36737: get
  status = db->Get(read_options, "key7571", &value);
  if (status.ok()) {
    out << "GET key7571 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36738: get
  status = db->Get(read_options, "key6141", &value);
  if (status.ok()) {
    out << "GET key6141 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36739: put
  db->Put(write_options, "key8866", "value95006");

  // op 36740: get
  status = db->Get(read_options, "key7919", &value);
  if (status.ok()) {
    out << "GET key7919 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36741: put
  db->Put(write_options, "key0399", "value112892");

  // op 36742: delete_range
  status = db->DeleteRange(write_options, "key1525", "key5683");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 36743: put
  db->Put(write_options, "key1260", "value57915");

  // op 36744: put
  db->Put(write_options, "key8822", "value109125");

  // op 36745: delete
  db->Delete(write_options, "key9611");

  // op 36746: put
  db->Put(write_options, "key3573", "value20647");

  // op 36747: put
  db->Put(write_options, "key8762", "value81941");

  // op 36748: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1897", "key6215", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1897, key6215) size=" << scan_result.size() << std::endl;
  }

  // op 36749: get
  status = db->Get(read_options, "key3090", &value);
  if (status.ok()) {
    out << "GET key3090 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36750: put
  db->Put(write_options, "key10678", "value52918");

  // op 36751: delete_range
  status = db->DeleteRange(write_options, "key8162", "key11216");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 36752: get
  status = db->Get(read_options, "key2566", &value);
  if (status.ok()) {
    out << "GET key2566 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36753: put
  db->Put(write_options, "key1597", "value7161");

  // op 36754: delete
  db->Delete(write_options, "key1652");

  // op 36755: delete_range
  status = db->DeleteRange(write_options, "key7934", "key9131");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 36756: delete_range
  status = db->DeleteRange(write_options, "key4166", "key5426");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 36757: put
  db->Put(write_options, "key5743", "value93179");

  // op 36758: get
  status = db->Get(read_options, "key2096", &value);
  if (status.ok()) {
    out << "GET key2096 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36759: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 36760: put
  db->Put(write_options, "key11213", "value78402");

  // op 36761: get
  status = db->Get(read_options, "key6527", &value);
  if (status.ok()) {
    out << "GET key6527 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36762: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8145", "key10581", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8145, key10581) size=" << scan_result.size() << std::endl;
  }

  // op 36763: put
  db->Put(write_options, "key5094", "value85513");

  // op 36764: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1002", "key9181", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1002, key9181) size=" << scan_result.size() << std::endl;
  }

  // op 36765: delete_range
  status = db->DeleteRange(write_options, "key1353", "key10442");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 36766: put
  db->Put(write_options, "key5272", "value64982");

  // op 36767: delete
  db->Delete(write_options, "key5613");

  // op 36768: get
  status = db->Get(read_options, "key7236", &value);
  if (status.ok()) {
    out << "GET key7236 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36769: put
  db->Put(write_options, "key11332", "value31384");

  // op 36770: put
  db->Put(write_options, "key1123", "value96715");

  // op 36771: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4091", "key4436", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4091, key4436) size=" << scan_result.size() << std::endl;
  }

  // op 36772: put
  db->Put(write_options, "key10416", "value1077");

  // op 36773: get
  status = db->Get(read_options, "key7440", &value);
  if (status.ok()) {
    out << "GET key7440 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36774: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3182", "key10129", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3182, key10129) size=" << scan_result.size() << std::endl;
  }

  // op 36775: put
  db->Put(write_options, "key3513", "value13389");

  // op 36776: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3742", "key9118", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3742, key9118) size=" << scan_result.size() << std::endl;
  }

  // op 36777: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5351", "key7647", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5351, key7647) size=" << scan_result.size() << std::endl;
  }

  // op 36778: put
  db->Put(write_options, "key2391", "value82023");

  // op 36779: put
  db->Put(write_options, "key5369", "value88138");

  // op 36780: get
  status = db->Get(read_options, "key5136", &value);
  if (status.ok()) {
    out << "GET key5136 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36781: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 36782: delete_range
  status = db->DeleteRange(write_options, "key6505", "key11011");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 36783: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2449", "key3566", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2449, key3566) size=" << scan_result.size() << std::endl;
  }

  // op 36784: delete
  db->Delete(write_options, "key9646");

  // op 36785: put
  db->Put(write_options, "key9863", "value26834");

  // op 36786: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 36787: delete_range
  status = db->DeleteRange(write_options, "key1589", "key3707");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 36788: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 36789: delete_range
  status = db->DeleteRange(write_options, "key5247", "key11511");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 36790: delete
  db->Delete(write_options, "key3891");

  // op 36791: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 36792: delete_range
  status = db->DeleteRange(write_options, "key1367", "key4875");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 36793: get
  status = db->Get(read_options, "key11137", &value);
  if (status.ok()) {
    out << "GET key11137 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36794: put
  db->Put(write_options, "key10662", "value3742");

  // op 36795: get
  status = db->Get(read_options, "key4245", &value);
  if (status.ok()) {
    out << "GET key4245 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36796: put
  db->Put(write_options, "key1925", "value46523");

  // op 36797: delete
  db->Delete(write_options, "key7711");

  // op 36798: put
  db->Put(write_options, "key11343", "value87846");

  // op 36799: put
  db->Put(write_options, "key0574", "value23311");

  // op 36800: get
  status = db->Get(read_options, "key2092", &value);
  if (status.ok()) {
    out << "GET key2092 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36801: put
  db->Put(write_options, "key3013", "value86063");

  // op 36802: put
  db->Put(write_options, "key1672", "value28162");

  // op 36803: get
  status = db->Get(read_options, "key7498", &value);
  if (status.ok()) {
    out << "GET key7498 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36804: get
  status = db->Get(read_options, "key11093", &value);
  if (status.ok()) {
    out << "GET key11093 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36805: put
  db->Put(write_options, "key4193", "value73473");

  // op 36806: get
  status = db->Get(read_options, "key5153", &value);
  if (status.ok()) {
    out << "GET key5153 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36807: get
  status = db->Get(read_options, "key8804", &value);
  if (status.ok()) {
    out << "GET key8804 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36808: get
  status = db->Get(read_options, "key0890", &value);
  if (status.ok()) {
    out << "GET key0890 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36809: put
  db->Put(write_options, "key5594", "value27504");

  // op 36810: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7212", "key11393", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7212, key11393) size=" << scan_result.size() << std::endl;
  }

  // op 36811: put
  db->Put(write_options, "key4400", "value57865");

  // op 36812: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 36813: delete_range
  status = db->DeleteRange(write_options, "key5808", "key10617");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 36814: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2061", "key11025", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2061, key11025) size=" << scan_result.size() << std::endl;
  }

  // op 36815: put
  db->Put(write_options, "key7897", "value92589");

  // op 36816: put
  db->Put(write_options, "key2732", "value54766");

  // op 36817: delete_range
  status = db->DeleteRange(write_options, "key10860", "key10956");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 36818: get
  status = db->Get(read_options, "key7438", &value);
  if (status.ok()) {
    out << "GET key7438 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36819: put
  db->Put(write_options, "key5270", "value76406");

  // op 36820: get
  status = db->Get(read_options, "key2587", &value);
  if (status.ok()) {
    out << "GET key2587 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36821: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 36822: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 36823: put
  db->Put(write_options, "key2571", "value40348");

  // op 36824: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5252", "key8991", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5252, key8991) size=" << scan_result.size() << std::endl;
  }

  // op 36825: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3463", "key6948", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3463, key6948) size=" << scan_result.size() << std::endl;
  }

  // op 36826: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1007", "key11628", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1007, key11628) size=" << scan_result.size() << std::endl;
  }

  // op 36827: delete_range
  status = db->DeleteRange(write_options, "key3760", "key4523");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 36828: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2020", "key2621", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2020, key2621) size=" << scan_result.size() << std::endl;
  }

  // op 36829: put
  db->Put(write_options, "key0940", "value4025");

  // op 36830: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 36831: get
  status = db->Get(read_options, "key11588", &value);
  if (status.ok()) {
    out << "GET key11588 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36832: delete
  db->Delete(write_options, "key2050");

  // op 36833: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0408", "key5803", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0408, key5803) size=" << scan_result.size() << std::endl;
  }

  // op 36834: get
  status = db->Get(read_options, "key7512", &value);
  if (status.ok()) {
    out << "GET key7512 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36835: delete
  db->Delete(write_options, "key11018");

  // op 36836: put
  db->Put(write_options, "key4794", "value59163");

  // op 36837: put
  db->Put(write_options, "key1800", "value106472");

  // op 36838: delete
  db->Delete(write_options, "key2206");

  // op 36839: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6437", "key7588", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6437, key7588) size=" << scan_result.size() << std::endl;
  }

  // op 36840: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6570", "key10969", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6570, key10969) size=" << scan_result.size() << std::endl;
  }

  // op 36841: put
  db->Put(write_options, "key2394", "value13304");

  // op 36842: get
  status = db->Get(read_options, "key1478", &value);
  if (status.ok()) {
    out << "GET key1478 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36843: put
  db->Put(write_options, "key0687", "value15203");

  // op 36844: delete
  db->Delete(write_options, "key7168");

  // op 36845: delete_range
  status = db->DeleteRange(write_options, "key0690", "key3500");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 36846: put
  db->Put(write_options, "key3810", "value98527");

  // op 36847: delete_range
  status = db->DeleteRange(write_options, "key5000", "key8771");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 36848: delete
  db->Delete(write_options, "key7404");

  // op 36849: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8053", "key11642", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8053, key11642) size=" << scan_result.size() << std::endl;
  }

  // op 36850: get
  status = db->Get(read_options, "key8707", &value);
  if (status.ok()) {
    out << "GET key8707 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36851: put
  db->Put(write_options, "key6719", "value13329");

  // op 36852: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5448", "key8743", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5448, key8743) size=" << scan_result.size() << std::endl;
  }

  // op 36853: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3722", "key6982", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3722, key6982) size=" << scan_result.size() << std::endl;
  }

  // op 36854: put
  db->Put(write_options, "key6799", "value45777");

  // op 36855: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5210", "key5946", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5210, key5946) size=" << scan_result.size() << std::endl;
  }

  // op 36856: get
  status = db->Get(read_options, "key8518", &value);
  if (status.ok()) {
    out << "GET key8518 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36857: delete_range
  status = db->DeleteRange(write_options, "key0039", "key9991");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 36858: put
  db->Put(write_options, "key8331", "value24871");

  // op 36859: get
  status = db->Get(read_options, "key0591", &value);
  if (status.ok()) {
    out << "GET key0591 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36860: put
  db->Put(write_options, "key3305", "value99530");

  // op 36861: get
  status = db->Get(read_options, "key10579", &value);
  if (status.ok()) {
    out << "GET key10579 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36862: delete
  db->Delete(write_options, "key11217");

  // op 36863: get
  status = db->Get(read_options, "key0422", &value);
  if (status.ok()) {
    out << "GET key0422 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36864: put
  db->Put(write_options, "key7507", "value111974");

  // op 36865: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4616", "key8589", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4616, key8589) size=" << scan_result.size() << std::endl;
  }

  // op 36866: put
  db->Put(write_options, "key11198", "value72747");

  // op 36867: get
  status = db->Get(read_options, "key6389", &value);
  if (status.ok()) {
    out << "GET key6389 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36868: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2911", "key5046", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2911, key5046) size=" << scan_result.size() << std::endl;
  }

  // op 36869: delete
  db->Delete(write_options, "key7246");

  // op 36870: get
  status = db->Get(read_options, "key3355", &value);
  if (status.ok()) {
    out << "GET key3355 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36871: get
  status = db->Get(read_options, "key3277", &value);
  if (status.ok()) {
    out << "GET key3277 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36872: put
  db->Put(write_options, "key7954", "value64066");

  // op 36873: put
  db->Put(write_options, "key7661", "value78823");

  // op 36874: put
  db->Put(write_options, "key7689", "value12665");

  // op 36875: get
  status = db->Get(read_options, "key9565", &value);
  if (status.ok()) {
    out << "GET key9565 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36876: get
  status = db->Get(read_options, "key6903", &value);
  if (status.ok()) {
    out << "GET key6903 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36877: put
  db->Put(write_options, "key0792", "value16068");

  // op 36878: put
  db->Put(write_options, "key5185", "value21834");

  // op 36879: put
  db->Put(write_options, "key8217", "value7980");

  // op 36880: put
  db->Put(write_options, "key8123", "value110757");

  // op 36881: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7267", "key11143", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7267, key11143) size=" << scan_result.size() << std::endl;
  }

  // op 36882: put
  db->Put(write_options, "key3503", "value42799");

  // op 36883: get
  status = db->Get(read_options, "key0831", &value);
  if (status.ok()) {
    out << "GET key0831 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36884: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1046", "key4172", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1046, key4172) size=" << scan_result.size() << std::endl;
  }

  // op 36885: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0184", "key6864", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0184, key6864) size=" << scan_result.size() << std::endl;
  }

  // op 36886: delete
  db->Delete(write_options, "key9290");

  // op 36887: put
  db->Put(write_options, "key5104", "value41822");

  // op 36888: get
  status = db->Get(read_options, "key8729", &value);
  if (status.ok()) {
    out << "GET key8729 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36889: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0253", "key3647", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0253, key3647) size=" << scan_result.size() << std::endl;
  }

  // op 36890: put
  db->Put(write_options, "key10003", "value61808");

  // op 36891: put
  db->Put(write_options, "key2771", "value27816");

  // op 36892: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3140", "key3290", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3140, key3290) size=" << scan_result.size() << std::endl;
  }

  // op 36893: put
  db->Put(write_options, "key4801", "value2274");

  // op 36894: get
  status = db->Get(read_options, "key2141", &value);
  if (status.ok()) {
    out << "GET key2141 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36895: put
  db->Put(write_options, "key7352", "value87484");

  // op 36896: put
  db->Put(write_options, "key6423", "value3837");

  // op 36897: put
  db->Put(write_options, "key5021", "value39434");

  // op 36898: put
  db->Put(write_options, "key6457", "value54689");

  // op 36899: get
  status = db->Get(read_options, "key10080", &value);
  if (status.ok()) {
    out << "GET key10080 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36900: put
  db->Put(write_options, "key1795", "value99969");

  // op 36901: delete
  db->Delete(write_options, "key9457");

  // op 36902: get
  status = db->Get(read_options, "key3793", &value);
  if (status.ok()) {
    out << "GET key3793 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36903: put
  db->Put(write_options, "key8640", "value53766");

  // op 36904: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 36905: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3128", "key3569", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3128, key3569) size=" << scan_result.size() << std::endl;
  }

  // op 36906: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 36907: get
  status = db->Get(read_options, "key1300", &value);
  if (status.ok()) {
    out << "GET key1300 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36908: delete
  db->Delete(write_options, "key3863");

  // op 36909: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4289", "key8473", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4289, key8473) size=" << scan_result.size() << std::endl;
  }

  // op 36910: delete
  db->Delete(write_options, "key3628");

  // op 36911: put
  db->Put(write_options, "key6396", "value15096");

  // op 36912: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5057", "key6019", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5057, key6019) size=" << scan_result.size() << std::endl;
  }

  // op 36913: put
  db->Put(write_options, "key8110", "value84387");

  // op 36914: put
  db->Put(write_options, "key2732", "value112420");

  // op 36915: delete
  db->Delete(write_options, "key3833");

  // op 36916: put
  db->Put(write_options, "key10500", "value2195");

  // op 36917: get
  status = db->Get(read_options, "key2419", &value);
  if (status.ok()) {
    out << "GET key2419 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36918: delete_range
  status = db->DeleteRange(write_options, "key1113", "key2028");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 36919: delete
  db->Delete(write_options, "key2856");

  // op 36920: delete_range
  status = db->DeleteRange(write_options, "key8068", "key8719");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 36921: put
  db->Put(write_options, "key5265", "value67188");

  // op 36922: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 36923: put
  db->Put(write_options, "key5478", "value67780");

  // op 36924: put
  db->Put(write_options, "key10606", "value112247");

  // op 36925: delete_range
  status = db->DeleteRange(write_options, "key10372", "key10592");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 36926: put
  db->Put(write_options, "key10464", "value60804");

  // op 36927: put
  db->Put(write_options, "key4349", "value101531");

  // op 36928: put
  db->Put(write_options, "key5350", "value101240");

  // op 36929: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 36930: put
  db->Put(write_options, "key2535", "value117925");

  // op 36931: put
  db->Put(write_options, "key10630", "value93160");

  // op 36932: delete_range
  status = db->DeleteRange(write_options, "key2837", "key6377");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 36933: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0032", "key4564", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0032, key4564) size=" << scan_result.size() << std::endl;
  }

  // op 36934: put
  db->Put(write_options, "key0265", "value46265");

  // op 36935: delete
  db->Delete(write_options, "key5393");

  // op 36936: delete_range
  status = db->DeleteRange(write_options, "key3563", "key6313");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 36937: put
  db->Put(write_options, "key0302", "value40010");

  // op 36938: put
  db->Put(write_options, "key0286", "value75781");

  // op 36939: put
  db->Put(write_options, "key7206", "value23595");

  // op 36940: put
  db->Put(write_options, "key6962", "value34694");

  // op 36941: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4338", "key9707", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4338, key9707) size=" << scan_result.size() << std::endl;
  }

  // op 36942: put
  db->Put(write_options, "key11319", "value96401");

  // op 36943: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7306", "key10038", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7306, key10038) size=" << scan_result.size() << std::endl;
  }

  // op 36944: put
  db->Put(write_options, "key10024", "value40264");

  // op 36945: get
  status = db->Get(read_options, "key6269", &value);
  if (status.ok()) {
    out << "GET key6269 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36946: delete_range
  status = db->DeleteRange(write_options, "key2496", "key2554");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 36947: get
  status = db->Get(read_options, "key2383", &value);
  if (status.ok()) {
    out << "GET key2383 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36948: delete_range
  status = db->DeleteRange(write_options, "key10104", "key10518");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 36949: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1089", "key2248", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1089, key2248) size=" << scan_result.size() << std::endl;
  }

  // op 36950: delete
  db->Delete(write_options, "key11165");

  // op 36951: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3203", "key4064", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3203, key4064) size=" << scan_result.size() << std::endl;
  }

  // op 36952: scan
  scan_result.clear();
  status = db->Scan(read_options, "key11341", "key11512", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key11341, key11512) size=" << scan_result.size() << std::endl;
  }

  // op 36953: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1797", "key3641", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1797, key3641) size=" << scan_result.size() << std::endl;
  }

  // op 36954: delete
  db->Delete(write_options, "key0265");

  // op 36955: put
  db->Put(write_options, "key11655", "value70222");

  // op 36956: put
  db->Put(write_options, "key4860", "value59858");

  // op 36957: delete_range
  status = db->DeleteRange(write_options, "key4714", "key6799");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 36958: put
  db->Put(write_options, "key3565", "value105122");

  // op 36959: put
  db->Put(write_options, "key5512", "value31733");

  // op 36960: put
  db->Put(write_options, "key3173", "value74146");

  // op 36961: delete_range
  status = db->DeleteRange(write_options, "key1411", "key8595");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 36962: get
  status = db->Get(read_options, "key6118", &value);
  if (status.ok()) {
    out << "GET key6118 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36963: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5775", "key10104", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5775, key10104) size=" << scan_result.size() << std::endl;
  }

  // op 36964: put
  db->Put(write_options, "key5910", "value89147");

  // op 36965: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3066", "key5705", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3066, key5705) size=" << scan_result.size() << std::endl;
  }

  // op 36966: put
  db->Put(write_options, "key8488", "value81920");

  // op 36967: get
  status = db->Get(read_options, "key1628", &value);
  if (status.ok()) {
    out << "GET key1628 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36968: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 36969: put
  db->Put(write_options, "key0941", "value60854");

  // op 36970: delete_range
  status = db->DeleteRange(write_options, "key5831", "key9120");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 36971: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6430", "key6968", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6430, key6968) size=" << scan_result.size() << std::endl;
  }

  // op 36972: delete
  db->Delete(write_options, "key4218");

  // op 36973: get
  status = db->Get(read_options, "key9370", &value);
  if (status.ok()) {
    out << "GET key9370 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36974: put
  db->Put(write_options, "key6262", "value40851");

  // op 36975: delete_range
  status = db->DeleteRange(write_options, "key6614", "key8074");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 36976: get
  status = db->Get(read_options, "key10700", &value);
  if (status.ok()) {
    out << "GET key10700 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36977: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7874", "key9863", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7874, key9863) size=" << scan_result.size() << std::endl;
  }

  // op 36978: get
  status = db->Get(read_options, "key1590", &value);
  if (status.ok()) {
    out << "GET key1590 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36979: get
  status = db->Get(read_options, "key6235", &value);
  if (status.ok()) {
    out << "GET key6235 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36980: delete
  db->Delete(write_options, "key0259");

  // op 36981: put
  db->Put(write_options, "key1601", "value61700");

  // op 36982: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8447", "key11448", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8447, key11448) size=" << scan_result.size() << std::endl;
  }

  // op 36983: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5456", "key9234", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5456, key9234) size=" << scan_result.size() << std::endl;
  }

  // op 36984: delete_range
  status = db->DeleteRange(write_options, "key1523", "key9462");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 36985: delete_range
  status = db->DeleteRange(write_options, "key7464", "key9178");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 36986: get
  status = db->Get(read_options, "key4882", &value);
  if (status.ok()) {
    out << "GET key4882 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36987: put
  db->Put(write_options, "key0597", "value69464");

  // op 36988: put
  db->Put(write_options, "key5887", "value40285");

  // op 36989: put
  db->Put(write_options, "key1217", "value108738");

  // op 36990: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2486", "key7357", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2486, key7357) size=" << scan_result.size() << std::endl;
  }

  // op 36991: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3564", "key9452", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3564, key9452) size=" << scan_result.size() << std::endl;
  }

  // op 36992: put
  db->Put(write_options, "key8878", "value60658");

  // op 36993: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1558", "key1967", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1558, key1967) size=" << scan_result.size() << std::endl;
  }

  // op 36994: delete_range
  status = db->DeleteRange(write_options, "key4937", "key7448");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 36995: delete_range
  status = db->DeleteRange(write_options, "key2992", "key7740");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 36996: get
  status = db->Get(read_options, "key1856", &value);
  if (status.ok()) {
    out << "GET key1856 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36997: get
  status = db->Get(read_options, "key6304", &value);
  if (status.ok()) {
    out << "GET key6304 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 36998: delete
  db->Delete(write_options, "key5943");

  // op 36999: put
  db->Put(write_options, "key6958", "value16090");

  // op 37000: delete_range
  status = db->DeleteRange(write_options, "key0466", "key11514");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 37001: put
  db->Put(write_options, "key5132", "value9625");

  // op 37002: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1355", "key1820", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1355, key1820) size=" << scan_result.size() << std::endl;
  }

  // op 37003: put
  db->Put(write_options, "key3440", "value116815");

  // op 37004: put
  db->Put(write_options, "key6218", "value67206");

  // op 37005: get
  status = db->Get(read_options, "key3022", &value);
  if (status.ok()) {
    out << "GET key3022 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37006: put
  db->Put(write_options, "key10982", "value74601");

  // op 37007: delete
  db->Delete(write_options, "key9176");

  // op 37008: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1065", "key8268", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1065, key8268) size=" << scan_result.size() << std::endl;
  }

  // op 37009: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5303", "key9901", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5303, key9901) size=" << scan_result.size() << std::endl;
  }

  // op 37010: put
  db->Put(write_options, "key4501", "value55140");

  // op 37011: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6013", "key11687", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6013, key11687) size=" << scan_result.size() << std::endl;
  }

  // op 37012: get
  status = db->Get(read_options, "key0909", &value);
  if (status.ok()) {
    out << "GET key0909 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37013: delete_range
  status = db->DeleteRange(write_options, "key2812", "key3059");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 37014: get
  status = db->Get(read_options, "key11707", &value);
  if (status.ok()) {
    out << "GET key11707 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37015: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0672", "key9080", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0672, key9080) size=" << scan_result.size() << std::endl;
  }

  // op 37016: delete
  db->Delete(write_options, "key7979");

  // op 37017: put
  db->Put(write_options, "key4691", "value28655");

  // op 37018: delete_range
  status = db->DeleteRange(write_options, "key10724", "key11142");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 37019: delete
  db->Delete(write_options, "key5021");

  // op 37020: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3421", "key11570", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3421, key11570) size=" << scan_result.size() << std::endl;
  }

  // op 37021: delete
  db->Delete(write_options, "key10174");

  // op 37022: put
  db->Put(write_options, "key10431", "value84465");

  // op 37023: put
  db->Put(write_options, "key11551", "value52240");

  // op 37024: delete_range
  status = db->DeleteRange(write_options, "key10493", "key10899");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 37025: put
  db->Put(write_options, "key1801", "value57534");

  // op 37026: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 37027: put
  db->Put(write_options, "key0927", "value15662");

  // op 37028: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0578", "key3120", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0578, key3120) size=" << scan_result.size() << std::endl;
  }

  // op 37029: put
  db->Put(write_options, "key1927", "value117816");

  // op 37030: put
  db->Put(write_options, "key4263", "value35857");

  // op 37031: get
  status = db->Get(read_options, "key9116", &value);
  if (status.ok()) {
    out << "GET key9116 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37032: put
  db->Put(write_options, "key4938", "value79033");

  // op 37033: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3265", "key8736", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3265, key8736) size=" << scan_result.size() << std::endl;
  }

  // op 37034: put
  db->Put(write_options, "key2954", "value4708");

  // op 37035: get
  status = db->Get(read_options, "key5115", &value);
  if (status.ok()) {
    out << "GET key5115 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37036: get
  status = db->Get(read_options, "key2276", &value);
  if (status.ok()) {
    out << "GET key2276 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37037: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2185", "key6089", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2185, key6089) size=" << scan_result.size() << std::endl;
  }

  // op 37038: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9682", "key10600", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9682, key10600) size=" << scan_result.size() << std::endl;
  }

  // op 37039: get
  status = db->Get(read_options, "key8476", &value);
  if (status.ok()) {
    out << "GET key8476 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37040: delete
  db->Delete(write_options, "key0969");

  // op 37041: get
  status = db->Get(read_options, "key5525", &value);
  if (status.ok()) {
    out << "GET key5525 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37042: delete
  db->Delete(write_options, "key7524");

  // op 37043: delete
  db->Delete(write_options, "key10569");

  // op 37044: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 37045: get
  status = db->Get(read_options, "key8792", &value);
  if (status.ok()) {
    out << "GET key8792 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37046: delete
  db->Delete(write_options, "key5290");

  // op 37047: get
  status = db->Get(read_options, "key8958", &value);
  if (status.ok()) {
    out << "GET key8958 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37048: put
  db->Put(write_options, "key0296", "value111009");

  // op 37049: delete_range
  status = db->DeleteRange(write_options, "key3454", "key6244");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 37050: delete
  db->Delete(write_options, "key10968");

  // op 37051: put
  db->Put(write_options, "key10716", "value18174");

  // op 37052: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 37053: delete_range
  status = db->DeleteRange(write_options, "key0931", "key11043");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 37054: put
  db->Put(write_options, "key4497", "value73701");

  // op 37055: get
  status = db->Get(read_options, "key11567", &value);
  if (status.ok()) {
    out << "GET key11567 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37056: put
  db->Put(write_options, "key1607", "value65073");

  // op 37057: put
  db->Put(write_options, "key0567", "value98540");

  // op 37058: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0595", "key3759", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0595, key3759) size=" << scan_result.size() << std::endl;
  }

  // op 37059: delete
  db->Delete(write_options, "key5052");

  // op 37060: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2103", "key4155", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2103, key4155) size=" << scan_result.size() << std::endl;
  }

  // op 37061: delete_range
  status = db->DeleteRange(write_options, "key1984", "key10700");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 37062: put
  db->Put(write_options, "key7557", "value77253");

  // op 37063: delete_range
  status = db->DeleteRange(write_options, "key7914", "key10768");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 37064: get
  status = db->Get(read_options, "key10682", &value);
  if (status.ok()) {
    out << "GET key10682 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37065: put
  db->Put(write_options, "key8211", "value21438");

  // op 37066: get
  status = db->Get(read_options, "key3720", &value);
  if (status.ok()) {
    out << "GET key3720 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37067: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 37068: delete_range
  status = db->DeleteRange(write_options, "key3217", "key4727");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 37069: get
  status = db->Get(read_options, "key7497", &value);
  if (status.ok()) {
    out << "GET key7497 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37070: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 37071: put
  db->Put(write_options, "key0108", "value46869");

  // op 37072: get
  status = db->Get(read_options, "key7491", &value);
  if (status.ok()) {
    out << "GET key7491 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37073: delete
  db->Delete(write_options, "key11445");

  // op 37074: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3132", "key5248", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3132, key5248) size=" << scan_result.size() << std::endl;
  }

  // op 37075: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0545", "key4374", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0545, key4374) size=" << scan_result.size() << std::endl;
  }

  // op 37076: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 37077: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4201", "key9957", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4201, key9957) size=" << scan_result.size() << std::endl;
  }

  // op 37078: put
  db->Put(write_options, "key8324", "value95014");

  // op 37079: get
  status = db->Get(read_options, "key9461", &value);
  if (status.ok()) {
    out << "GET key9461 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37080: get
  status = db->Get(read_options, "key9058", &value);
  if (status.ok()) {
    out << "GET key9058 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37081: get
  status = db->Get(read_options, "key11523", &value);
  if (status.ok()) {
    out << "GET key11523 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37082: get
  status = db->Get(read_options, "key7341", &value);
  if (status.ok()) {
    out << "GET key7341 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37083: put
  db->Put(write_options, "key1968", "value3499");

  // op 37084: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0692", "key3984", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0692, key3984) size=" << scan_result.size() << std::endl;
  }

  // op 37085: put
  db->Put(write_options, "key7769", "value94639");

  // op 37086: put
  db->Put(write_options, "key4437", "value74067");

  // op 37087: put
  db->Put(write_options, "key10666", "value98202");

  // op 37088: put
  db->Put(write_options, "key6392", "value21270");

  // op 37089: put
  db->Put(write_options, "key9020", "value83593");

  // op 37090: put
  db->Put(write_options, "key7178", "value33551");

  // op 37091: put
  db->Put(write_options, "key4133", "value58357");

  // op 37092: delete
  db->Delete(write_options, "key9225");

  // op 37093: get
  status = db->Get(read_options, "key2390", &value);
  if (status.ok()) {
    out << "GET key2390 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37094: delete
  db->Delete(write_options, "key5382");

  // op 37095: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 37096: delete
  db->Delete(write_options, "key0939");

  // op 37097: get
  status = db->Get(read_options, "key9695", &value);
  if (status.ok()) {
    out << "GET key9695 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37098: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8039", "key9837", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8039, key9837) size=" << scan_result.size() << std::endl;
  }

  // op 37099: put
  db->Put(write_options, "key7649", "value41699");

  // op 37100: put
  db->Put(write_options, "key5472", "value16326");

  // op 37101: put
  db->Put(write_options, "key5892", "value4173");

  // op 37102: delete_range
  status = db->DeleteRange(write_options, "key3832", "key4939");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 37103: put
  db->Put(write_options, "key0720", "value48877");

  // op 37104: scan
  scan_result.clear();
  status = db->Scan(read_options, "key11429", "key11728", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key11429, key11728) size=" << scan_result.size() << std::endl;
  }

  // op 37105: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1128", "key10478", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1128, key10478) size=" << scan_result.size() << std::endl;
  }

  // op 37106: put
  db->Put(write_options, "key2283", "value108625");

  // op 37107: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6724", "key7238", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6724, key7238) size=" << scan_result.size() << std::endl;
  }

  // op 37108: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1350", "key1716", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1350, key1716) size=" << scan_result.size() << std::endl;
  }

  // op 37109: delete
  db->Delete(write_options, "key5794");

  // op 37110: get
  status = db->Get(read_options, "key2230", &value);
  if (status.ok()) {
    out << "GET key2230 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37111: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5088", "key10622", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5088, key10622) size=" << scan_result.size() << std::endl;
  }

  // op 37112: put
  db->Put(write_options, "key2035", "value96859");

  // op 37113: put
  db->Put(write_options, "key7016", "value113784");

  // op 37114: delete
  db->Delete(write_options, "key0218");

  // op 37115: put
  db->Put(write_options, "key1950", "value28098");

  // op 37116: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1710", "key10140", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1710, key10140) size=" << scan_result.size() << std::endl;
  }

  // op 37117: delete_range
  status = db->DeleteRange(write_options, "key0641", "key6189");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 37118: put
  db->Put(write_options, "key11660", "value86983");

  // op 37119: delete
  db->Delete(write_options, "key10651");

  // op 37120: delete
  db->Delete(write_options, "key8144");

  // op 37121: put
  db->Put(write_options, "key9882", "value78644");

  // op 37122: put
  db->Put(write_options, "key10019", "value101915");

  // op 37123: put
  db->Put(write_options, "key6163", "value113296");

  // op 37124: put
  db->Put(write_options, "key0689", "value116261");

  // op 37125: get
  status = db->Get(read_options, "key4867", &value);
  if (status.ok()) {
    out << "GET key4867 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37126: get
  status = db->Get(read_options, "key4975", &value);
  if (status.ok()) {
    out << "GET key4975 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37127: delete
  db->Delete(write_options, "key1860");

  // op 37128: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2396", "key3826", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2396, key3826) size=" << scan_result.size() << std::endl;
  }

  // op 37129: put
  db->Put(write_options, "key4682", "value34313");

  // op 37130: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 37131: get
  status = db->Get(read_options, "key0823", &value);
  if (status.ok()) {
    out << "GET key0823 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37132: get
  status = db->Get(read_options, "key9130", &value);
  if (status.ok()) {
    out << "GET key9130 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37133: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1818", "key3663", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1818, key3663) size=" << scan_result.size() << std::endl;
  }

  // op 37134: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8264", "key10590", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8264, key10590) size=" << scan_result.size() << std::endl;
  }

  // op 37135: put
  db->Put(write_options, "key0750", "value65048");

  // op 37136: get
  status = db->Get(read_options, "key3432", &value);
  if (status.ok()) {
    out << "GET key3432 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37137: delete_range
  status = db->DeleteRange(write_options, "key4099", "key4212");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 37138: get
  status = db->Get(read_options, "key4252", &value);
  if (status.ok()) {
    out << "GET key4252 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37139: put
  db->Put(write_options, "key5808", "value32077");

  // op 37140: put
  db->Put(write_options, "key9205", "value94892");

  // op 37141: put
  db->Put(write_options, "key4568", "value84922");

  // op 37142: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3994", "key8598", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3994, key8598) size=" << scan_result.size() << std::endl;
  }

  // op 37143: get
  status = db->Get(read_options, "key3518", &value);
  if (status.ok()) {
    out << "GET key3518 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37144: get
  status = db->Get(read_options, "key9966", &value);
  if (status.ok()) {
    out << "GET key9966 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37145: put
  db->Put(write_options, "key4182", "value56330");

  // op 37146: delete_range
  status = db->DeleteRange(write_options, "key7512", "key8599");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 37147: delete_range
  status = db->DeleteRange(write_options, "key2036", "key3305");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 37148: get
  status = db->Get(read_options, "key0248", &value);
  if (status.ok()) {
    out << "GET key0248 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37149: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8415", "key10625", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8415, key10625) size=" << scan_result.size() << std::endl;
  }

  // op 37150: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 37151: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1029", "key1300", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1029, key1300) size=" << scan_result.size() << std::endl;
  }

  // op 37152: put
  db->Put(write_options, "key5113", "value74658");

  // op 37153: delete_range
  status = db->DeleteRange(write_options, "key5071", "key7031");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 37154: put
  db->Put(write_options, "key1018", "value101556");

  // op 37155: put
  db->Put(write_options, "key6092", "value33401");

  // op 37156: get
  status = db->Get(read_options, "key8119", &value);
  if (status.ok()) {
    out << "GET key8119 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37157: get
  status = db->Get(read_options, "key9107", &value);
  if (status.ok()) {
    out << "GET key9107 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37158: get
  status = db->Get(read_options, "key0361", &value);
  if (status.ok()) {
    out << "GET key0361 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37159: put
  db->Put(write_options, "key3905", "value81890");

  // op 37160: get
  status = db->Get(read_options, "key9963", &value);
  if (status.ok()) {
    out << "GET key9963 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37161: delete
  db->Delete(write_options, "key2042");

  // op 37162: delete_range
  status = db->DeleteRange(write_options, "key5110", "key11720");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 37163: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4075", "key7119", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4075, key7119) size=" << scan_result.size() << std::endl;
  }

  // op 37164: put
  db->Put(write_options, "key5634", "value53916");

  // op 37165: put
  db->Put(write_options, "key11505", "value82715");

  // op 37166: put
  db->Put(write_options, "key1586", "value101834");

  // op 37167: delete
  db->Delete(write_options, "key9517");

  // op 37168: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1581", "key7792", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1581, key7792) size=" << scan_result.size() << std::endl;
  }

  // op 37169: get
  status = db->Get(read_options, "key1562", &value);
  if (status.ok()) {
    out << "GET key1562 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37170: put
  db->Put(write_options, "key2674", "value25636");

  // op 37171: put
  db->Put(write_options, "key3837", "value21583");

  // op 37172: delete_range
  status = db->DeleteRange(write_options, "key6758", "key9799");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 37173: delete_range
  status = db->DeleteRange(write_options, "key6184", "key7471");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 37174: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5169", "key6840", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5169, key6840) size=" << scan_result.size() << std::endl;
  }

  // op 37175: delete_range
  status = db->DeleteRange(write_options, "key5994", "key11308");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 37176: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0970", "key3374", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0970, key3374) size=" << scan_result.size() << std::endl;
  }

  // op 37177: delete
  db->Delete(write_options, "key4658");

  // op 37178: delete
  db->Delete(write_options, "key6339");

  // op 37179: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1643", "key3605", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1643, key3605) size=" << scan_result.size() << std::endl;
  }

  // op 37180: delete
  db->Delete(write_options, "key6316");

  // op 37181: put
  db->Put(write_options, "key7005", "value69649");

  // op 37182: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7344", "key9590", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7344, key9590) size=" << scan_result.size() << std::endl;
  }

  // op 37183: put
  db->Put(write_options, "key2133", "value18183");

  // op 37184: put
  db->Put(write_options, "key6336", "value35515");

  // op 37185: delete
  db->Delete(write_options, "key1474");

  // op 37186: delete_range
  status = db->DeleteRange(write_options, "key0743", "key6197");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 37187: put
  db->Put(write_options, "key11488", "value75909");

  // op 37188: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4553", "key10100", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4553, key10100) size=" << scan_result.size() << std::endl;
  }

  // op 37189: put
  db->Put(write_options, "key8584", "value2735");

  // op 37190: get
  status = db->Get(read_options, "key6961", &value);
  if (status.ok()) {
    out << "GET key6961 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37191: get
  status = db->Get(read_options, "key10305", &value);
  if (status.ok()) {
    out << "GET key10305 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37192: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 37193: put
  db->Put(write_options, "key4143", "value64281");

  // op 37194: put
  db->Put(write_options, "key2713", "value17477");

  // op 37195: delete
  db->Delete(write_options, "key8621");

  // op 37196: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1925", "key7075", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1925, key7075) size=" << scan_result.size() << std::endl;
  }

  // op 37197: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0654", "key6037", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0654, key6037) size=" << scan_result.size() << std::endl;
  }

  // op 37198: put
  db->Put(write_options, "key5590", "value59338");

  // op 37199: get
  status = db->Get(read_options, "key10732", &value);
  if (status.ok()) {
    out << "GET key10732 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37200: get
  status = db->Get(read_options, "key6120", &value);
  if (status.ok()) {
    out << "GET key6120 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37201: put
  db->Put(write_options, "key9772", "value33689");

  // op 37202: put
  db->Put(write_options, "key5557", "value34832");

  // op 37203: delete_range
  status = db->DeleteRange(write_options, "key1267", "key6204");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 37204: put
  db->Put(write_options, "key8255", "value104729");

  // op 37205: get
  status = db->Get(read_options, "key10871", &value);
  if (status.ok()) {
    out << "GET key10871 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37206: delete
  db->Delete(write_options, "key7739");

  // op 37207: delete_range
  status = db->DeleteRange(write_options, "key1755", "key2285");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 37208: put
  db->Put(write_options, "key9380", "value37243");

  // op 37209: put
  db->Put(write_options, "key6677", "value63624");

  // op 37210: put
  db->Put(write_options, "key3239", "value12656");

  // op 37211: put
  db->Put(write_options, "key8758", "value77134");

  // op 37212: put
  db->Put(write_options, "key5293", "value14409");

  // op 37213: get
  status = db->Get(read_options, "key9448", &value);
  if (status.ok()) {
    out << "GET key9448 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37214: put
  db->Put(write_options, "key0741", "value22188");

  // op 37215: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 37216: get
  status = db->Get(read_options, "key1580", &value);
  if (status.ok()) {
    out << "GET key1580 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37217: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0828", "key7223", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0828, key7223) size=" << scan_result.size() << std::endl;
  }

  // op 37218: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0100", "key1333", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0100, key1333) size=" << scan_result.size() << std::endl;
  }

  // op 37219: get
  status = db->Get(read_options, "key5548", &value);
  if (status.ok()) {
    out << "GET key5548 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37220: put
  db->Put(write_options, "key9991", "value82948");

  // op 37221: delete
  db->Delete(write_options, "key10465");

  // op 37222: get
  status = db->Get(read_options, "key2749", &value);
  if (status.ok()) {
    out << "GET key2749 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37223: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10505", "key11670", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10505, key11670) size=" << scan_result.size() << std::endl;
  }

  // op 37224: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 37225: get
  status = db->Get(read_options, "key11320", &value);
  if (status.ok()) {
    out << "GET key11320 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37226: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2702", "key6481", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2702, key6481) size=" << scan_result.size() << std::endl;
  }

  // op 37227: get
  status = db->Get(read_options, "key10278", &value);
  if (status.ok()) {
    out << "GET key10278 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37228: put
  db->Put(write_options, "key11287", "value66815");

  // op 37229: put
  db->Put(write_options, "key2387", "value51942");

  // op 37230: delete_range
  status = db->DeleteRange(write_options, "key1820", "key6278");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 37231: put
  db->Put(write_options, "key0251", "value66974");

  // op 37232: put
  db->Put(write_options, "key1367", "value81025");

  // op 37233: put
  db->Put(write_options, "key11410", "value26420");

  // op 37234: delete
  db->Delete(write_options, "key4703");

  // op 37235: delete
  db->Delete(write_options, "key8646");

  // op 37236: get
  status = db->Get(read_options, "key0870", &value);
  if (status.ok()) {
    out << "GET key0870 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37237: put
  db->Put(write_options, "key8424", "value61268");

  // op 37238: delete_range
  status = db->DeleteRange(write_options, "key1889", "key4569");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 37239: get
  status = db->Get(read_options, "key11702", &value);
  if (status.ok()) {
    out << "GET key11702 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37240: delete_range
  status = db->DeleteRange(write_options, "key6360", "key10374");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 37241: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 37242: get
  status = db->Get(read_options, "key1650", &value);
  if (status.ok()) {
    out << "GET key1650 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37243: put
  db->Put(write_options, "key7031", "value93112");

  // op 37244: get
  status = db->Get(read_options, "key11539", &value);
  if (status.ok()) {
    out << "GET key11539 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37245: delete
  db->Delete(write_options, "key1413");

  // op 37246: put
  db->Put(write_options, "key7982", "value5903");

  // op 37247: put
  db->Put(write_options, "key4227", "value51117");

  // op 37248: delete
  db->Delete(write_options, "key1785");

  // op 37249: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0356", "key8111", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0356, key8111) size=" << scan_result.size() << std::endl;
  }

  // op 37250: put
  db->Put(write_options, "key10711", "value93069");

  // op 37251: put
  db->Put(write_options, "key0063", "value56713");

  // op 37252: delete
  db->Delete(write_options, "key5124");

  // op 37253: get
  status = db->Get(read_options, "key10826", &value);
  if (status.ok()) {
    out << "GET key10826 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37254: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 37255: put
  db->Put(write_options, "key1560", "value36033");

  // op 37256: delete_range
  status = db->DeleteRange(write_options, "key0210", "key8128");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 37257: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0599", "key7020", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0599, key7020) size=" << scan_result.size() << std::endl;
  }

  // op 37258: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5174", "key8213", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5174, key8213) size=" << scan_result.size() << std::endl;
  }

  // op 37259: put
  db->Put(write_options, "key8564", "value45253");

  // op 37260: put
  db->Put(write_options, "key0481", "value75752");

  // op 37261: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0408", "key5443", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0408, key5443) size=" << scan_result.size() << std::endl;
  }

  // op 37262: put
  db->Put(write_options, "key6434", "value86824");

  // op 37263: put
  db->Put(write_options, "key10574", "value105910");

  // op 37264: put
  db->Put(write_options, "key8689", "value16876");

  // op 37265: get
  status = db->Get(read_options, "key1922", &value);
  if (status.ok()) {
    out << "GET key1922 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37266: get
  status = db->Get(read_options, "key4861", &value);
  if (status.ok()) {
    out << "GET key4861 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37267: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5203", "key10668", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5203, key10668) size=" << scan_result.size() << std::endl;
  }

  // op 37268: put
  db->Put(write_options, "key3302", "value62848");

  // op 37269: put
  db->Put(write_options, "key5164", "value68862");

  // op 37270: put
  db->Put(write_options, "key2195", "value0024");

  // op 37271: put
  db->Put(write_options, "key6231", "value16295");

  // op 37272: delete
  db->Delete(write_options, "key10281");

  // op 37273: put
  db->Put(write_options, "key5811", "value87118");

  // op 37274: put
  db->Put(write_options, "key7255", "value117245");

  // op 37275: delete
  db->Delete(write_options, "key1510");

  // op 37276: delete_range
  status = db->DeleteRange(write_options, "key3239", "key6767");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 37277: get
  status = db->Get(read_options, "key1050", &value);
  if (status.ok()) {
    out << "GET key1050 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37278: get
  status = db->Get(read_options, "key3845", &value);
  if (status.ok()) {
    out << "GET key3845 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37279: get
  status = db->Get(read_options, "key2571", &value);
  if (status.ok()) {
    out << "GET key2571 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37280: get
  status = db->Get(read_options, "key8782", &value);
  if (status.ok()) {
    out << "GET key8782 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37281: put
  db->Put(write_options, "key9282", "value1242");

  // op 37282: get
  status = db->Get(read_options, "key5270", &value);
  if (status.ok()) {
    out << "GET key5270 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37283: put
  db->Put(write_options, "key6444", "value73807");

  // op 37284: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8579", "key10005", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8579, key10005) size=" << scan_result.size() << std::endl;
  }

  // op 37285: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1210", "key5119", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1210, key5119) size=" << scan_result.size() << std::endl;
  }

  // op 37286: put
  db->Put(write_options, "key1531", "value112666");

  // op 37287: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1074", "key4992", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1074, key4992) size=" << scan_result.size() << std::endl;
  }

  // op 37288: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4011", "key9353", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4011, key9353) size=" << scan_result.size() << std::endl;
  }

  // op 37289: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1392", "key2101", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1392, key2101) size=" << scan_result.size() << std::endl;
  }

  // op 37290: put
  db->Put(write_options, "key3354", "value48024");

  // op 37291: put
  db->Put(write_options, "key11240", "value95767");

  // op 37292: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0521", "key10407", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0521, key10407) size=" << scan_result.size() << std::endl;
  }

  // op 37293: get
  status = db->Get(read_options, "key3828", &value);
  if (status.ok()) {
    out << "GET key3828 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37294: put
  db->Put(write_options, "key8558", "value5553");

  // op 37295: put
  db->Put(write_options, "key7667", "value90437");

  // op 37296: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8582", "key9646", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8582, key9646) size=" << scan_result.size() << std::endl;
  }

  // op 37297: put
  db->Put(write_options, "key2395", "value108985");

  // op 37298: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0521", "key8092", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0521, key8092) size=" << scan_result.size() << std::endl;
  }

  // op 37299: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 37300: put
  db->Put(write_options, "key11333", "value56640");

  // op 37301: put
  db->Put(write_options, "key4848", "value56320");

  // op 37302: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3634", "key9214", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3634, key9214) size=" << scan_result.size() << std::endl;
  }

  // op 37303: put
  db->Put(write_options, "key9991", "value51627");

  // op 37304: put
  db->Put(write_options, "key9929", "value51221");

  // op 37305: get
  status = db->Get(read_options, "key7914", &value);
  if (status.ok()) {
    out << "GET key7914 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37306: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1177", "key11484", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1177, key11484) size=" << scan_result.size() << std::endl;
  }

  // op 37307: put
  db->Put(write_options, "key4349", "value4435");

  // op 37308: get
  status = db->Get(read_options, "key2465", &value);
  if (status.ok()) {
    out << "GET key2465 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37309: put
  db->Put(write_options, "key7319", "value71273");

  // op 37310: put
  db->Put(write_options, "key10065", "value12937");

  // op 37311: get
  status = db->Get(read_options, "key8618", &value);
  if (status.ok()) {
    out << "GET key8618 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37312: delete_range
  status = db->DeleteRange(write_options, "key6080", "key7532");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 37313: put
  db->Put(write_options, "key5434", "value55357");

  // op 37314: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6429", "key11496", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6429, key11496) size=" << scan_result.size() << std::endl;
  }

  // op 37315: put
  db->Put(write_options, "key1850", "value69493");

  // op 37316: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3589", "key4576", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3589, key4576) size=" << scan_result.size() << std::endl;
  }

  // op 37317: get
  status = db->Get(read_options, "key9801", &value);
  if (status.ok()) {
    out << "GET key9801 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37318: get
  status = db->Get(read_options, "key6411", &value);
  if (status.ok()) {
    out << "GET key6411 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37319: put
  db->Put(write_options, "key0037", "value80597");

  // op 37320: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3077", "key7390", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3077, key7390) size=" << scan_result.size() << std::endl;
  }

  // op 37321: put
  db->Put(write_options, "key8460", "value21810");

  // op 37322: put
  db->Put(write_options, "key8891", "value16807");

  // op 37323: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 37324: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1274", "key2791", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1274, key2791) size=" << scan_result.size() << std::endl;
  }

  // op 37325: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6817", "key8126", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6817, key8126) size=" << scan_result.size() << std::endl;
  }

  // op 37326: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 37327: put
  db->Put(write_options, "key2251", "value0753");

  // op 37328: delete_range
  status = db->DeleteRange(write_options, "key1161", "key6941");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 37329: put
  db->Put(write_options, "key4112", "value6589");

  // op 37330: get
  status = db->Get(read_options, "key11131", &value);
  if (status.ok()) {
    out << "GET key11131 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37331: get
  status = db->Get(read_options, "key8588", &value);
  if (status.ok()) {
    out << "GET key8588 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37332: get
  status = db->Get(read_options, "key3334", &value);
  if (status.ok()) {
    out << "GET key3334 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37333: delete_range
  status = db->DeleteRange(write_options, "key2424", "key10194");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 37334: put
  db->Put(write_options, "key1807", "value25307");

  // op 37335: delete
  db->Delete(write_options, "key9397");

  // op 37336: get
  status = db->Get(read_options, "key10087", &value);
  if (status.ok()) {
    out << "GET key10087 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37337: delete_range
  status = db->DeleteRange(write_options, "key7861", "key10290");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 37338: get
  status = db->Get(read_options, "key8716", &value);
  if (status.ok()) {
    out << "GET key8716 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37339: put
  db->Put(write_options, "key10148", "value112252");

  // op 37340: delete_range
  status = db->DeleteRange(write_options, "key8976", "key10840");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 37341: put
  db->Put(write_options, "key0411", "value72718");

  // op 37342: put
  db->Put(write_options, "key2830", "value95390");

  // op 37343: put
  db->Put(write_options, "key1661", "value73725");

  // op 37344: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 37345: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4394", "key9406", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4394, key9406) size=" << scan_result.size() << std::endl;
  }

  // op 37346: delete_range
  status = db->DeleteRange(write_options, "key3455", "key8638");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 37347: put
  db->Put(write_options, "key5118", "value1973");

  // op 37348: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3043", "key9062", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3043, key9062) size=" << scan_result.size() << std::endl;
  }

  // op 37349: put
  db->Put(write_options, "key5190", "value8649");

  // op 37350: delete
  db->Delete(write_options, "key2890");

  // op 37351: delete_range
  status = db->DeleteRange(write_options, "key2084", "key9035");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 37352: put
  db->Put(write_options, "key7292", "value117676");

  // op 37353: put
  db->Put(write_options, "key6132", "value111743");

  // op 37354: put
  db->Put(write_options, "key4423", "value60223");

  // op 37355: put
  db->Put(write_options, "key4509", "value33518");

  // op 37356: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0118", "key6305", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0118, key6305) size=" << scan_result.size() << std::endl;
  }

  // op 37357: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 37358: get
  status = db->Get(read_options, "key3152", &value);
  if (status.ok()) {
    out << "GET key3152 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37359: get
  status = db->Get(read_options, "key4672", &value);
  if (status.ok()) {
    out << "GET key4672 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37360: put
  db->Put(write_options, "key3463", "value31027");

  // op 37361: put
  db->Put(write_options, "key4869", "value70728");

  // op 37362: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2346", "key3045", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2346, key3045) size=" << scan_result.size() << std::endl;
  }

  // op 37363: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6810", "key9548", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6810, key9548) size=" << scan_result.size() << std::endl;
  }

  // op 37364: delete
  db->Delete(write_options, "key8399");

  // op 37365: get
  status = db->Get(read_options, "key3294", &value);
  if (status.ok()) {
    out << "GET key3294 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37366: delete_range
  status = db->DeleteRange(write_options, "key0109", "key6531");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 37367: get
  status = db->Get(read_options, "key1902", &value);
  if (status.ok()) {
    out << "GET key1902 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37368: put
  db->Put(write_options, "key3095", "value31283");

  // op 37369: delete_range
  status = db->DeleteRange(write_options, "key4395", "key6569");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 37370: get
  status = db->Get(read_options, "key8057", &value);
  if (status.ok()) {
    out << "GET key8057 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37371: put
  db->Put(write_options, "key9714", "value98316");

  // op 37372: put
  db->Put(write_options, "key4805", "value23189");

  // op 37373: put
  db->Put(write_options, "key10802", "value50848");

  // op 37374: put
  db->Put(write_options, "key4353", "value87792");

  // op 37375: put
  db->Put(write_options, "key1972", "value21226");

  // op 37376: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3775", "key11443", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3775, key11443) size=" << scan_result.size() << std::endl;
  }

  // op 37377: get
  status = db->Get(read_options, "key10860", &value);
  if (status.ok()) {
    out << "GET key10860 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37378: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2969", "key6086", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2969, key6086) size=" << scan_result.size() << std::endl;
  }

  // op 37379: put
  db->Put(write_options, "key9406", "value115672");

  // op 37380: put
  db->Put(write_options, "key10531", "value106977");

  // op 37381: delete_range
  status = db->DeleteRange(write_options, "key6378", "key6749");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 37382: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3219", "key11294", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3219, key11294) size=" << scan_result.size() << std::endl;
  }

  // op 37383: put
  db->Put(write_options, "key2981", "value65903");

  // op 37384: put
  db->Put(write_options, "key5955", "value66091");

  // op 37385: delete_range
  status = db->DeleteRange(write_options, "key3220", "key10424");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 37386: put
  db->Put(write_options, "key9829", "value55424");

  // op 37387: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4709", "key6503", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4709, key6503) size=" << scan_result.size() << std::endl;
  }

  // op 37388: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2948", "key6749", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2948, key6749) size=" << scan_result.size() << std::endl;
  }

  // op 37389: put
  db->Put(write_options, "key7994", "value79502");

  // op 37390: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 37391: get
  status = db->Get(read_options, "key9002", &value);
  if (status.ok()) {
    out << "GET key9002 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37392: put
  db->Put(write_options, "key10843", "value34984");

  // op 37393: put
  db->Put(write_options, "key10048", "value59052");

  // op 37394: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 37395: put
  db->Put(write_options, "key2737", "value38527");

  // op 37396: put
  db->Put(write_options, "key6492", "value41950");

  // op 37397: delete
  db->Delete(write_options, "key8816");

  // op 37398: get
  status = db->Get(read_options, "key1245", &value);
  if (status.ok()) {
    out << "GET key1245 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37399: put
  db->Put(write_options, "key5607", "value57024");

  // op 37400: put
  db->Put(write_options, "key11200", "value106929");

  // op 37401: put
  db->Put(write_options, "key4101", "value58386");

  // op 37402: put
  db->Put(write_options, "key0562", "value66294");

  // op 37403: get
  status = db->Get(read_options, "key1171", &value);
  if (status.ok()) {
    out << "GET key1171 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37404: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2280", "key6619", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2280, key6619) size=" << scan_result.size() << std::endl;
  }

  // op 37405: put
  db->Put(write_options, "key7504", "value59713");

  // op 37406: put
  db->Put(write_options, "key10223", "value117500");

  // op 37407: delete
  db->Delete(write_options, "key2424");

  // op 37408: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 37409: put
  db->Put(write_options, "key6633", "value74117");

  // op 37410: put
  db->Put(write_options, "key11235", "value108645");

  // op 37411: put
  db->Put(write_options, "key0344", "value16329");

  // op 37412: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7171", "key9029", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7171, key9029) size=" << scan_result.size() << std::endl;
  }

  // op 37413: get
  status = db->Get(read_options, "key9393", &value);
  if (status.ok()) {
    out << "GET key9393 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37414: delete
  db->Delete(write_options, "key1632");

  // op 37415: get
  status = db->Get(read_options, "key10824", &value);
  if (status.ok()) {
    out << "GET key10824 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37416: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2999", "key9809", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2999, key9809) size=" << scan_result.size() << std::endl;
  }

  // op 37417: put
  db->Put(write_options, "key4636", "value97343");

  // op 37418: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1106", "key8956", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1106, key8956) size=" << scan_result.size() << std::endl;
  }

  // op 37419: put
  db->Put(write_options, "key5968", "value76016");

  // op 37420: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3676", "key9726", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3676, key9726) size=" << scan_result.size() << std::endl;
  }

  // op 37421: put
  db->Put(write_options, "key5112", "value5016");

  // op 37422: delete
  db->Delete(write_options, "key9650");

  // op 37423: put
  db->Put(write_options, "key3320", "value20862");

  // op 37424: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2056", "key11287", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2056, key11287) size=" << scan_result.size() << std::endl;
  }

  // op 37425: get
  status = db->Get(read_options, "key2572", &value);
  if (status.ok()) {
    out << "GET key2572 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37426: delete_range
  status = db->DeleteRange(write_options, "key2250", "key7968");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 37427: delete_range
  status = db->DeleteRange(write_options, "key0385", "key2375");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 37428: get
  status = db->Get(read_options, "key10372", &value);
  if (status.ok()) {
    out << "GET key10372 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37429: get
  status = db->Get(read_options, "key2165", &value);
  if (status.ok()) {
    out << "GET key2165 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37430: get
  status = db->Get(read_options, "key3978", &value);
  if (status.ok()) {
    out << "GET key3978 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37431: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 37432: delete
  db->Delete(write_options, "key1922");

  // op 37433: delete_range
  status = db->DeleteRange(write_options, "key1387", "key11490");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 37434: put
  db->Put(write_options, "key1212", "value106020");

  // op 37435: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 37436: get
  status = db->Get(read_options, "key5001", &value);
  if (status.ok()) {
    out << "GET key5001 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37437: get
  status = db->Get(read_options, "key9103", &value);
  if (status.ok()) {
    out << "GET key9103 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37438: delete_range
  status = db->DeleteRange(write_options, "key4755", "key11211");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 37439: put
  db->Put(write_options, "key4897", "value38793");

  // op 37440: get
  status = db->Get(read_options, "key5135", &value);
  if (status.ok()) {
    out << "GET key5135 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37441: delete
  db->Delete(write_options, "key7337");

  // op 37442: put
  db->Put(write_options, "key10833", "value85806");

  // op 37443: put
  db->Put(write_options, "key4883", "value97729");

  // op 37444: put
  db->Put(write_options, "key2605", "value21833");

  // op 37445: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3698", "key4547", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3698, key4547) size=" << scan_result.size() << std::endl;
  }

  // op 37446: get
  status = db->Get(read_options, "key3586", &value);
  if (status.ok()) {
    out << "GET key3586 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37447: put
  db->Put(write_options, "key10035", "value15759");

  // op 37448: get
  status = db->Get(read_options, "key1018", &value);
  if (status.ok()) {
    out << "GET key1018 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37449: get
  status = db->Get(read_options, "key6179", &value);
  if (status.ok()) {
    out << "GET key6179 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37450: put
  db->Put(write_options, "key7554", "value55183");

  // op 37451: put
  db->Put(write_options, "key3149", "value84667");

  // op 37452: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6881", "key10940", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6881, key10940) size=" << scan_result.size() << std::endl;
  }

  // op 37453: delete_range
  status = db->DeleteRange(write_options, "key9001", "key11036");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 37454: delete_range
  status = db->DeleteRange(write_options, "key4399", "key8701");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 37455: put
  db->Put(write_options, "key6544", "value64051");

  // op 37456: delete_range
  status = db->DeleteRange(write_options, "key1838", "key6692");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 37457: get
  status = db->Get(read_options, "key2917", &value);
  if (status.ok()) {
    out << "GET key2917 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37458: put
  db->Put(write_options, "key9434", "value28239");

  // op 37459: get
  status = db->Get(read_options, "key1936", &value);
  if (status.ok()) {
    out << "GET key1936 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37460: put
  db->Put(write_options, "key2501", "value85598");

  // op 37461: put
  db->Put(write_options, "key7389", "value6470");

  // op 37462: get
  status = db->Get(read_options, "key6123", &value);
  if (status.ok()) {
    out << "GET key6123 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37463: put
  db->Put(write_options, "key1378", "value92838");

  // op 37464: put
  db->Put(write_options, "key9577", "value90924");

  // op 37465: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3935", "key5209", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3935, key5209) size=" << scan_result.size() << std::endl;
  }

  // op 37466: put
  db->Put(write_options, "key11502", "value91860");

  // op 37467: put
  db->Put(write_options, "key11695", "value100907");

  // op 37468: put
  db->Put(write_options, "key2019", "value32659");

  // op 37469: put
  db->Put(write_options, "key4154", "value54426");

  // op 37470: put
  db->Put(write_options, "key0797", "value83452");

  // op 37471: delete
  db->Delete(write_options, "key6992");

  // op 37472: put
  db->Put(write_options, "key9543", "value35947");

  // op 37473: get
  status = db->Get(read_options, "key5560", &value);
  if (status.ok()) {
    out << "GET key5560 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37474: delete_range
  status = db->DeleteRange(write_options, "key7237", "key7333");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 37475: delete
  db->Delete(write_options, "key10976");

  // op 37476: delete
  db->Delete(write_options, "key9664");

  // op 37477: delete
  db->Delete(write_options, "key0835");

  // op 37478: put
  db->Put(write_options, "key6837", "value109747");

  // op 37479: get
  status = db->Get(read_options, "key7819", &value);
  if (status.ok()) {
    out << "GET key7819 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37480: delete_range
  status = db->DeleteRange(write_options, "key2305", "key11680");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 37481: put
  db->Put(write_options, "key11592", "value72163");

  // op 37482: put
  db->Put(write_options, "key0241", "value16236");

  // op 37483: put
  db->Put(write_options, "key2685", "value29273");

  // op 37484: put
  db->Put(write_options, "key3318", "value36247");

  // op 37485: get
  status = db->Get(read_options, "key10581", &value);
  if (status.ok()) {
    out << "GET key10581 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37486: put
  db->Put(write_options, "key5469", "value100741");

  // op 37487: put
  db->Put(write_options, "key3044", "value90777");

  // op 37488: put
  db->Put(write_options, "key8983", "value86876");

  // op 37489: put
  db->Put(write_options, "key1423", "value73686");

  // op 37490: delete_range
  status = db->DeleteRange(write_options, "key1331", "key2813");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 37491: delete_range
  status = db->DeleteRange(write_options, "key2822", "key4950");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 37492: delete
  db->Delete(write_options, "key0283");

  // op 37493: delete_range
  status = db->DeleteRange(write_options, "key4111", "key7419");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 37494: put
  db->Put(write_options, "key11541", "value39891");

  // op 37495: put
  db->Put(write_options, "key9235", "value43578");

  // op 37496: put
  db->Put(write_options, "key0350", "value67210");

  // op 37497: delete_range
  status = db->DeleteRange(write_options, "key0949", "key2089");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 37498: put
  db->Put(write_options, "key3249", "value89935");

  // op 37499: get
  status = db->Get(read_options, "key6981", &value);
  if (status.ok()) {
    out << "GET key6981 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37500: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3039", "key5618", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3039, key5618) size=" << scan_result.size() << std::endl;
  }

  // op 37501: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7339", "key8380", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7339, key8380) size=" << scan_result.size() << std::endl;
  }

  // op 37502: put
  db->Put(write_options, "key11137", "value45975");

  // op 37503: put
  db->Put(write_options, "key6962", "value87698");

  // op 37504: put
  db->Put(write_options, "key4191", "value112861");

  // op 37505: get
  status = db->Get(read_options, "key0202", &value);
  if (status.ok()) {
    out << "GET key0202 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37506: put
  db->Put(write_options, "key6516", "value95663");

  // op 37507: delete
  db->Delete(write_options, "key6005");

  // op 37508: get
  status = db->Get(read_options, "key9733", &value);
  if (status.ok()) {
    out << "GET key9733 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37509: put
  db->Put(write_options, "key3669", "value82977");

  // op 37510: get
  status = db->Get(read_options, "key10871", &value);
  if (status.ok()) {
    out << "GET key10871 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37511: delete_range
  status = db->DeleteRange(write_options, "key3159", "key5803");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 37512: get
  status = db->Get(read_options, "key11364", &value);
  if (status.ok()) {
    out << "GET key11364 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37513: get
  status = db->Get(read_options, "key3203", &value);
  if (status.ok()) {
    out << "GET key3203 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37514: delete_range
  status = db->DeleteRange(write_options, "key0591", "key3004");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 37515: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 37516: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 37517: put
  db->Put(write_options, "key0570", "value10703");

  // op 37518: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7557", "key11652", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7557, key11652) size=" << scan_result.size() << std::endl;
  }

  // op 37519: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5212", "key9652", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5212, key9652) size=" << scan_result.size() << std::endl;
  }

  // op 37520: put
  db->Put(write_options, "key3351", "value87876");

  // op 37521: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2947", "key8115", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2947, key8115) size=" << scan_result.size() << std::endl;
  }

  // op 37522: get
  status = db->Get(read_options, "key11166", &value);
  if (status.ok()) {
    out << "GET key11166 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37523: put
  db->Put(write_options, "key8315", "value14566");

  // op 37524: put
  db->Put(write_options, "key5501", "value48265");

  // op 37525: put
  db->Put(write_options, "key11259", "value52563");

  // op 37526: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2009", "key2307", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2009, key2307) size=" << scan_result.size() << std::endl;
  }

  // op 37527: put
  db->Put(write_options, "key4165", "value27125");

  // op 37528: put
  db->Put(write_options, "key0700", "value52864");

  // op 37529: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1667", "key10210", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1667, key10210) size=" << scan_result.size() << std::endl;
  }

  // op 37530: get
  status = db->Get(read_options, "key3610", &value);
  if (status.ok()) {
    out << "GET key3610 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37531: delete_range
  status = db->DeleteRange(write_options, "key4009", "key11591");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 37532: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 37533: delete
  db->Delete(write_options, "key7094");

  // op 37534: delete_range
  status = db->DeleteRange(write_options, "key1181", "key8593");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 37535: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3983", "key5221", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3983, key5221) size=" << scan_result.size() << std::endl;
  }

  // op 37536: delete
  db->Delete(write_options, "key7611");

  // op 37537: delete
  db->Delete(write_options, "key8296");

  // op 37538: get
  status = db->Get(read_options, "key10741", &value);
  if (status.ok()) {
    out << "GET key10741 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37539: delete_range
  status = db->DeleteRange(write_options, "key3486", "key11287");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 37540: get
  status = db->Get(read_options, "key0535", &value);
  if (status.ok()) {
    out << "GET key0535 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37541: put
  db->Put(write_options, "key8082", "value68505");

  // op 37542: put
  db->Put(write_options, "key6917", "value51194");

  // op 37543: get
  status = db->Get(read_options, "key6001", &value);
  if (status.ok()) {
    out << "GET key6001 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37544: delete
  db->Delete(write_options, "key0752");

  // op 37545: delete_range
  status = db->DeleteRange(write_options, "key2193", "key2817");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 37546: put
  db->Put(write_options, "key2720", "value93442");

  // op 37547: delete
  db->Delete(write_options, "key10473");

  // op 37548: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2908", "key5109", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2908, key5109) size=" << scan_result.size() << std::endl;
  }

  // op 37549: put
  db->Put(write_options, "key8501", "value108782");

  // op 37550: delete
  db->Delete(write_options, "key4846");

  // op 37551: delete
  db->Delete(write_options, "key5940");

  // op 37552: put
  db->Put(write_options, "key6600", "value25325");

  // op 37553: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 37554: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2922", "key11546", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2922, key11546) size=" << scan_result.size() << std::endl;
  }

  // op 37555: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1217", "key9707", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1217, key9707) size=" << scan_result.size() << std::endl;
  }

  // op 37556: get
  status = db->Get(read_options, "key10982", &value);
  if (status.ok()) {
    out << "GET key10982 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37557: get
  status = db->Get(read_options, "key0686", &value);
  if (status.ok()) {
    out << "GET key0686 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37558: get
  status = db->Get(read_options, "key2667", &value);
  if (status.ok()) {
    out << "GET key2667 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37559: put
  db->Put(write_options, "key1212", "value88223");

  // op 37560: put
  db->Put(write_options, "key8007", "value89582");

  // op 37561: put
  db->Put(write_options, "key5757", "value82297");

  // op 37562: put
  db->Put(write_options, "key11057", "value23114");

  // op 37563: put
  db->Put(write_options, "key9310", "value75681");

  // op 37564: put
  db->Put(write_options, "key8556", "value22031");

  // op 37565: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3543", "key9324", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3543, key9324) size=" << scan_result.size() << std::endl;
  }

  // op 37566: delete_range
  status = db->DeleteRange(write_options, "key0524", "key9247");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 37567: put
  db->Put(write_options, "key10217", "value82618");

  // op 37568: put
  db->Put(write_options, "key6981", "value49390");

  // op 37569: delete_range
  status = db->DeleteRange(write_options, "key2418", "key11024");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 37570: put
  db->Put(write_options, "key2124", "value98478");

  // op 37571: delete_range
  status = db->DeleteRange(write_options, "key7062", "key9499");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 37572: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1054", "key7569", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1054, key7569) size=" << scan_result.size() << std::endl;
  }

  // op 37573: get
  status = db->Get(read_options, "key5247", &value);
  if (status.ok()) {
    out << "GET key5247 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37574: put
  db->Put(write_options, "key11691", "value55948");

  // op 37575: get
  status = db->Get(read_options, "key8281", &value);
  if (status.ok()) {
    out << "GET key8281 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37576: put
  db->Put(write_options, "key8382", "value63132");

  // op 37577: put
  db->Put(write_options, "key2985", "value35752");

  // op 37578: put
  db->Put(write_options, "key0613", "value17573");

  // op 37579: put
  db->Put(write_options, "key2458", "value101415");

  // op 37580: put
  db->Put(write_options, "key9437", "value10041");

  // op 37581: get
  status = db->Get(read_options, "key6145", &value);
  if (status.ok()) {
    out << "GET key6145 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37582: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7389", "key10819", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7389, key10819) size=" << scan_result.size() << std::endl;
  }

  // op 37583: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8117", "key8788", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8117, key8788) size=" << scan_result.size() << std::endl;
  }

  // op 37584: put
  db->Put(write_options, "key2593", "value65437");

  // op 37585: put
  db->Put(write_options, "key7135", "value112440");

  // op 37586: put
  db->Put(write_options, "key2345", "value5736");

  // op 37587: delete
  db->Delete(write_options, "key11369");

  // op 37588: put
  db->Put(write_options, "key7264", "value87215");

  // op 37589: get
  status = db->Get(read_options, "key5742", &value);
  if (status.ok()) {
    out << "GET key5742 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37590: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0933", "key7589", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0933, key7589) size=" << scan_result.size() << std::endl;
  }

  // op 37591: delete_range
  status = db->DeleteRange(write_options, "key6943", "key9674");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 37592: put
  db->Put(write_options, "key5483", "value117077");

  // op 37593: put
  db->Put(write_options, "key2309", "value47986");

  // op 37594: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1178", "key8697", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1178, key8697) size=" << scan_result.size() << std::endl;
  }

  // op 37595: put
  db->Put(write_options, "key0273", "value25447");

  // op 37596: put
  db->Put(write_options, "key8403", "value14167");

  // op 37597: get
  status = db->Get(read_options, "key5849", &value);
  if (status.ok()) {
    out << "GET key5849 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37598: put
  db->Put(write_options, "key4357", "value22250");

  // op 37599: get
  status = db->Get(read_options, "key8905", &value);
  if (status.ok()) {
    out << "GET key8905 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37600: put
  db->Put(write_options, "key1528", "value95157");

  // op 37601: delete
  db->Delete(write_options, "key4817");

  // op 37602: put
  db->Put(write_options, "key0354", "value3303");

  // op 37603: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0491", "key3109", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0491, key3109) size=" << scan_result.size() << std::endl;
  }

  // op 37604: delete
  db->Delete(write_options, "key1353");

  // op 37605: put
  db->Put(write_options, "key0891", "value84549");

  // op 37606: put
  db->Put(write_options, "key0138", "value97522");

  // op 37607: delete_range
  status = db->DeleteRange(write_options, "key2763", "key3041");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 37608: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9378", "key9632", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9378, key9632) size=" << scan_result.size() << std::endl;
  }

  // op 37609: put
  db->Put(write_options, "key6776", "value48001");

  // op 37610: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2252", "key4362", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2252, key4362) size=" << scan_result.size() << std::endl;
  }

  // op 37611: delete_range
  status = db->DeleteRange(write_options, "key1517", "key5924");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 37612: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3700", "key6218", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3700, key6218) size=" << scan_result.size() << std::endl;
  }

  // op 37613: put
  db->Put(write_options, "key5640", "value97881");

  // op 37614: put
  db->Put(write_options, "key2284", "value45339");

  // op 37615: delete_range
  status = db->DeleteRange(write_options, "key0403", "key11727");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 37616: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3073", "key4285", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3073, key4285) size=" << scan_result.size() << std::endl;
  }

  // op 37617: get
  status = db->Get(read_options, "key8476", &value);
  if (status.ok()) {
    out << "GET key8476 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37618: put
  db->Put(write_options, "key10636", "value14857");

  // op 37619: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10017", "key11262", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10017, key11262) size=" << scan_result.size() << std::endl;
  }

  // op 37620: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 37621: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6396", "key8066", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6396, key8066) size=" << scan_result.size() << std::endl;
  }

  // op 37622: put
  db->Put(write_options, "key10416", "value100052");

  // op 37623: put
  db->Put(write_options, "key8067", "value67342");

  // op 37624: put
  db->Put(write_options, "key5241", "value102109");

  // op 37625: delete
  db->Delete(write_options, "key1142");

  // op 37626: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3649", "key9953", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3649, key9953) size=" << scan_result.size() << std::endl;
  }

  // op 37627: put
  db->Put(write_options, "key6619", "value9198");

  // op 37628: put
  db->Put(write_options, "key0891", "value80462");

  // op 37629: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 37630: get
  status = db->Get(read_options, "key0437", &value);
  if (status.ok()) {
    out << "GET key0437 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37631: get
  status = db->Get(read_options, "key11311", &value);
  if (status.ok()) {
    out << "GET key11311 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37632: put
  db->Put(write_options, "key10358", "value86131");

  // op 37633: put
  db->Put(write_options, "key4994", "value47457");

  // op 37634: delete
  db->Delete(write_options, "key1197");

  // op 37635: put
  db->Put(write_options, "key3516", "value64462");

  // op 37636: put
  db->Put(write_options, "key10552", "value45468");

  // op 37637: get
  status = db->Get(read_options, "key4722", &value);
  if (status.ok()) {
    out << "GET key4722 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37638: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 37639: put
  db->Put(write_options, "key2066", "value103748");

  // op 37640: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 37641: put
  db->Put(write_options, "key7521", "value79137");

  // op 37642: put
  db->Put(write_options, "key9870", "value16222");

  // op 37643: put
  db->Put(write_options, "key7701", "value117459");

  // op 37644: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1598", "key8453", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1598, key8453) size=" << scan_result.size() << std::endl;
  }

  // op 37645: put
  db->Put(write_options, "key1855", "value108166");

  // op 37646: delete
  db->Delete(write_options, "key0089");

  // op 37647: put
  db->Put(write_options, "key1335", "value58516");

  // op 37648: delete
  db->Delete(write_options, "key5362");

  // op 37649: delete
  db->Delete(write_options, "key11160");

  // op 37650: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0135", "key6711", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0135, key6711) size=" << scan_result.size() << std::endl;
  }

  // op 37651: delete_range
  status = db->DeleteRange(write_options, "key4077", "key4411");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 37652: put
  db->Put(write_options, "key8833", "value103048");

  // op 37653: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5898", "key8835", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5898, key8835) size=" << scan_result.size() << std::endl;
  }

  // op 37654: get
  status = db->Get(read_options, "key9332", &value);
  if (status.ok()) {
    out << "GET key9332 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37655: put
  db->Put(write_options, "key0259", "value55238");

  // op 37656: get
  status = db->Get(read_options, "key0178", &value);
  if (status.ok()) {
    out << "GET key0178 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37657: put
  db->Put(write_options, "key9063", "value47146");

  // op 37658: delete
  db->Delete(write_options, "key8225");

  // op 37659: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 37660: delete_range
  status = db->DeleteRange(write_options, "key1099", "key10573");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 37661: delete_range
  status = db->DeleteRange(write_options, "key7217", "key11624");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 37662: put
  db->Put(write_options, "key0810", "value88037");

  // op 37663: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2497", "key4242", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2497, key4242) size=" << scan_result.size() << std::endl;
  }

  // op 37664: put
  db->Put(write_options, "key8461", "value86819");

  // op 37665: put
  db->Put(write_options, "key0957", "value102609");

  // op 37666: put
  db->Put(write_options, "key4147", "value102067");

  // op 37667: put
  db->Put(write_options, "key5862", "value44040");

  // op 37668: delete_range
  status = db->DeleteRange(write_options, "key5105", "key11375");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 37669: put
  db->Put(write_options, "key4042", "value33661");

  // op 37670: put
  db->Put(write_options, "key10910", "value64342");

  // op 37671: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 37672: get
  status = db->Get(read_options, "key9097", &value);
  if (status.ok()) {
    out << "GET key9097 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37673: get
  status = db->Get(read_options, "key6488", &value);
  if (status.ok()) {
    out << "GET key6488 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37674: put
  db->Put(write_options, "key2156", "value17578");

  // op 37675: get
  status = db->Get(read_options, "key10360", &value);
  if (status.ok()) {
    out << "GET key10360 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37676: delete
  db->Delete(write_options, "key4976");

  // op 37677: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3492", "key3814", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3492, key3814) size=" << scan_result.size() << std::endl;
  }

  // op 37678: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2627", "key8705", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2627, key8705) size=" << scan_result.size() << std::endl;
  }

  // op 37679: put
  db->Put(write_options, "key1351", "value92871");

  // op 37680: get
  status = db->Get(read_options, "key10147", &value);
  if (status.ok()) {
    out << "GET key10147 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37681: put
  db->Put(write_options, "key7142", "value91411");

  // op 37682: get
  status = db->Get(read_options, "key9503", &value);
  if (status.ok()) {
    out << "GET key9503 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37683: put
  db->Put(write_options, "key6424", "value15895");

  // op 37684: get
  status = db->Get(read_options, "key1560", &value);
  if (status.ok()) {
    out << "GET key1560 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37685: get
  status = db->Get(read_options, "key10354", &value);
  if (status.ok()) {
    out << "GET key10354 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37686: put
  db->Put(write_options, "key4129", "value103902");

  // op 37687: put
  db->Put(write_options, "key9677", "value115196");

  // op 37688: delete
  db->Delete(write_options, "key8685");

  // op 37689: get
  status = db->Get(read_options, "key4882", &value);
  if (status.ok()) {
    out << "GET key4882 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37690: put
  db->Put(write_options, "key10317", "value97718");

  // op 37691: put
  db->Put(write_options, "key9142", "value79550");

  // op 37692: delete_range
  status = db->DeleteRange(write_options, "key3836", "key6522");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 37693: get
  status = db->Get(read_options, "key8283", &value);
  if (status.ok()) {
    out << "GET key8283 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37694: get
  status = db->Get(read_options, "key2641", &value);
  if (status.ok()) {
    out << "GET key2641 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37695: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6913", "key10429", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6913, key10429) size=" << scan_result.size() << std::endl;
  }

  // op 37696: get
  status = db->Get(read_options, "key5944", &value);
  if (status.ok()) {
    out << "GET key5944 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37697: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3425", "key10705", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3425, key10705) size=" << scan_result.size() << std::endl;
  }

  // op 37698: put
  db->Put(write_options, "key9842", "value43842");

  // op 37699: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5621", "key9430", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5621, key9430) size=" << scan_result.size() << std::endl;
  }

  // op 37700: delete_range
  status = db->DeleteRange(write_options, "key3255", "key3608");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 37701: delete
  db->Delete(write_options, "key0592");

  // op 37702: put
  db->Put(write_options, "key2345", "value15237");

  // op 37703: get
  status = db->Get(read_options, "key8631", &value);
  if (status.ok()) {
    out << "GET key8631 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37704: get
  status = db->Get(read_options, "key7469", &value);
  if (status.ok()) {
    out << "GET key7469 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37705: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1403", "key2083", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1403, key2083) size=" << scan_result.size() << std::endl;
  }

  // op 37706: put
  db->Put(write_options, "key0480", "value11694");

  // op 37707: get
  status = db->Get(read_options, "key4681", &value);
  if (status.ok()) {
    out << "GET key4681 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37708: put
  db->Put(write_options, "key5064", "value25864");

  // op 37709: delete_range
  status = db->DeleteRange(write_options, "key0588", "key1902");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 37710: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6567", "key11618", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6567, key11618) size=" << scan_result.size() << std::endl;
  }

  // op 37711: put
  db->Put(write_options, "key2150", "value92260");

  // op 37712: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 37713: get
  status = db->Get(read_options, "key1439", &value);
  if (status.ok()) {
    out << "GET key1439 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37714: put
  db->Put(write_options, "key10837", "value109253");

  // op 37715: put
  db->Put(write_options, "key6573", "value97428");

  // op 37716: put
  db->Put(write_options, "key2135", "value106983");

  // op 37717: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1115", "key7189", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1115, key7189) size=" << scan_result.size() << std::endl;
  }

  // op 37718: delete_range
  status = db->DeleteRange(write_options, "key0915", "key0932");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 37719: get
  status = db->Get(read_options, "key7486", &value);
  if (status.ok()) {
    out << "GET key7486 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37720: put
  db->Put(write_options, "key0512", "value112883");

  // op 37721: delete
  db->Delete(write_options, "key5157");

  // op 37722: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 37723: put
  db->Put(write_options, "key6714", "value25189");

  // op 37724: put
  db->Put(write_options, "key0175", "value6884");

  // op 37725: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1591", "key3487", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1591, key3487) size=" << scan_result.size() << std::endl;
  }

  // op 37726: delete
  db->Delete(write_options, "key3906");

  // op 37727: delete
  db->Delete(write_options, "key11132");

  // op 37728: put
  db->Put(write_options, "key3191", "value23792");

  // op 37729: get
  status = db->Get(read_options, "key11073", &value);
  if (status.ok()) {
    out << "GET key11073 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37730: put
  db->Put(write_options, "key3768", "value75056");

  // op 37731: put
  db->Put(write_options, "key6288", "value49954");

  // op 37732: put
  db->Put(write_options, "key2847", "value86339");

  // op 37733: put
  db->Put(write_options, "key11380", "value62717");

  // op 37734: get
  status = db->Get(read_options, "key7610", &value);
  if (status.ok()) {
    out << "GET key7610 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37735: get
  status = db->Get(read_options, "key6536", &value);
  if (status.ok()) {
    out << "GET key6536 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37736: put
  db->Put(write_options, "key4919", "value5439");

  // op 37737: put
  db->Put(write_options, "key3003", "value34462");

  // op 37738: get
  status = db->Get(read_options, "key10473", &value);
  if (status.ok()) {
    out << "GET key10473 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37739: put
  db->Put(write_options, "key4761", "value41466");

  // op 37740: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 37741: put
  db->Put(write_options, "key3425", "value78249");

  // op 37742: put
  db->Put(write_options, "key10139", "value25535");

  // op 37743: put
  db->Put(write_options, "key6439", "value75391");

  // op 37744: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3837", "key5812", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3837, key5812) size=" << scan_result.size() << std::endl;
  }

  // op 37745: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 37746: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1291", "key1507", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1291, key1507) size=" << scan_result.size() << std::endl;
  }

  // op 37747: put
  db->Put(write_options, "key0556", "value113350");

  // op 37748: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1302", "key10847", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1302, key10847) size=" << scan_result.size() << std::endl;
  }

  // op 37749: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 37750: put
  db->Put(write_options, "key5349", "value110618");

  // op 37751: get
  status = db->Get(read_options, "key4217", &value);
  if (status.ok()) {
    out << "GET key4217 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37752: put
  db->Put(write_options, "key6071", "value17303");

  // op 37753: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2256", "key7168", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2256, key7168) size=" << scan_result.size() << std::endl;
  }

  // op 37754: put
  db->Put(write_options, "key3191", "value45002");

  // op 37755: delete
  db->Delete(write_options, "key6364");

  // op 37756: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4938", "key10495", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4938, key10495) size=" << scan_result.size() << std::endl;
  }

  // op 37757: put
  db->Put(write_options, "key2845", "value66891");

  // op 37758: get
  status = db->Get(read_options, "key9999", &value);
  if (status.ok()) {
    out << "GET key9999 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37759: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4184", "key4849", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4184, key4849) size=" << scan_result.size() << std::endl;
  }

  // op 37760: delete_range
  status = db->DeleteRange(write_options, "key2300", "key4453");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 37761: put
  db->Put(write_options, "key0877", "value6457");

  // op 37762: get
  status = db->Get(read_options, "key2637", &value);
  if (status.ok()) {
    out << "GET key2637 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37763: put
  db->Put(write_options, "key5856", "value75272");

  // op 37764: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 37765: put
  db->Put(write_options, "key11447", "value2358");

  // op 37766: get
  status = db->Get(read_options, "key7646", &value);
  if (status.ok()) {
    out << "GET key7646 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37767: delete
  db->Delete(write_options, "key3081");

  // op 37768: put
  db->Put(write_options, "key9437", "value75586");

  // op 37769: put
  db->Put(write_options, "key0061", "value109374");

  // op 37770: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8633", "key9131", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8633, key9131) size=" << scan_result.size() << std::endl;
  }

  // op 37771: put
  db->Put(write_options, "key3869", "value78508");

  // op 37772: get
  status = db->Get(read_options, "key5940", &value);
  if (status.ok()) {
    out << "GET key5940 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37773: put
  db->Put(write_options, "key10300", "value91562");

  // op 37774: put
  db->Put(write_options, "key3366", "value76346");

  // op 37775: delete
  db->Delete(write_options, "key2770");

  // op 37776: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5172", "key9111", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5172, key9111) size=" << scan_result.size() << std::endl;
  }

  // op 37777: put
  db->Put(write_options, "key0127", "value9397");

  // op 37778: get
  status = db->Get(read_options, "key1448", &value);
  if (status.ok()) {
    out << "GET key1448 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37779: put
  db->Put(write_options, "key8105", "value61451");

  // op 37780: put
  db->Put(write_options, "key1417", "value89201");

  // op 37781: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 37782: get
  status = db->Get(read_options, "key4625", &value);
  if (status.ok()) {
    out << "GET key4625 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37783: delete_range
  status = db->DeleteRange(write_options, "key0912", "key4221");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 37784: get
  status = db->Get(read_options, "key0043", &value);
  if (status.ok()) {
    out << "GET key0043 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37785: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3478", "key7881", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3478, key7881) size=" << scan_result.size() << std::endl;
  }

  // op 37786: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1298", "key2069", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1298, key2069) size=" << scan_result.size() << std::endl;
  }

  // op 37787: delete_range
  status = db->DeleteRange(write_options, "key4141", "key8090");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 37788: get
  status = db->Get(read_options, "key4046", &value);
  if (status.ok()) {
    out << "GET key4046 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37789: put
  db->Put(write_options, "key4239", "value41055");

  // op 37790: get
  status = db->Get(read_options, "key6629", &value);
  if (status.ok()) {
    out << "GET key6629 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37791: get
  status = db->Get(read_options, "key7721", &value);
  if (status.ok()) {
    out << "GET key7721 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37792: put
  db->Put(write_options, "key6736", "value108773");

  // op 37793: put
  db->Put(write_options, "key5634", "value48966");

  // op 37794: get
  status = db->Get(read_options, "key6204", &value);
  if (status.ok()) {
    out << "GET key6204 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37795: delete_range
  status = db->DeleteRange(write_options, "key11425", "key11473");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 37796: put
  db->Put(write_options, "key0185", "value14492");

  // op 37797: get
  status = db->Get(read_options, "key6839", &value);
  if (status.ok()) {
    out << "GET key6839 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37798: delete
  db->Delete(write_options, "key9725");

  // op 37799: get
  status = db->Get(read_options, "key0533", &value);
  if (status.ok()) {
    out << "GET key0533 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37800: get
  status = db->Get(read_options, "key5525", &value);
  if (status.ok()) {
    out << "GET key5525 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37801: get
  status = db->Get(read_options, "key5380", &value);
  if (status.ok()) {
    out << "GET key5380 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37802: put
  db->Put(write_options, "key8097", "value107380");

  // op 37803: put
  db->Put(write_options, "key0914", "value67245");

  // op 37804: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3760", "key11280", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3760, key11280) size=" << scan_result.size() << std::endl;
  }

  // op 37805: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7758", "key9506", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7758, key9506) size=" << scan_result.size() << std::endl;
  }

  // op 37806: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10308", "key10769", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10308, key10769) size=" << scan_result.size() << std::endl;
  }

  // op 37807: get
  status = db->Get(read_options, "key9703", &value);
  if (status.ok()) {
    out << "GET key9703 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37808: delete
  db->Delete(write_options, "key2715");

  // op 37809: delete
  db->Delete(write_options, "key10486");

  // op 37810: put
  db->Put(write_options, "key2531", "value95288");

  // op 37811: delete
  db->Delete(write_options, "key0056");

  // op 37812: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 37813: put
  db->Put(write_options, "key9749", "value74303");

  // op 37814: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 37815: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1690", "key8519", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1690, key8519) size=" << scan_result.size() << std::endl;
  }

  // op 37816: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 37817: put
  db->Put(write_options, "key3717", "value36700");

  // op 37818: delete_range
  status = db->DeleteRange(write_options, "key1386", "key10201");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 37819: get
  status = db->Get(read_options, "key10653", &value);
  if (status.ok()) {
    out << "GET key10653 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37820: put
  db->Put(write_options, "key7320", "value0344");

  // op 37821: delete
  db->Delete(write_options, "key4529");

  // op 37822: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1379", "key5263", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1379, key5263) size=" << scan_result.size() << std::endl;
  }

  // op 37823: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3922", "key8235", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3922, key8235) size=" << scan_result.size() << std::endl;
  }

  // op 37824: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4399", "key6442", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4399, key6442) size=" << scan_result.size() << std::endl;
  }

  // op 37825: put
  db->Put(write_options, "key6018", "value7468");

  // op 37826: get
  status = db->Get(read_options, "key6662", &value);
  if (status.ok()) {
    out << "GET key6662 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37827: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0795", "key7045", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0795, key7045) size=" << scan_result.size() << std::endl;
  }

  // op 37828: get
  status = db->Get(read_options, "key5018", &value);
  if (status.ok()) {
    out << "GET key5018 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37829: get
  status = db->Get(read_options, "key7844", &value);
  if (status.ok()) {
    out << "GET key7844 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37830: put
  db->Put(write_options, "key2331", "value103428");

  // op 37831: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0468", "key9542", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0468, key9542) size=" << scan_result.size() << std::endl;
  }

  // op 37832: delete
  db->Delete(write_options, "key6433");

  // op 37833: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2865", "key11730", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2865, key11730) size=" << scan_result.size() << std::endl;
  }

  // op 37834: get
  status = db->Get(read_options, "key8062", &value);
  if (status.ok()) {
    out << "GET key8062 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37835: get
  status = db->Get(read_options, "key9118", &value);
  if (status.ok()) {
    out << "GET key9118 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37836: get
  status = db->Get(read_options, "key1415", &value);
  if (status.ok()) {
    out << "GET key1415 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37837: put
  db->Put(write_options, "key3856", "value57609");

  // op 37838: put
  db->Put(write_options, "key0740", "value2638");

  // op 37839: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6711", "key8001", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6711, key8001) size=" << scan_result.size() << std::endl;
  }

  // op 37840: put
  db->Put(write_options, "key2189", "value78674");

  // op 37841: put
  db->Put(write_options, "key9379", "value2205");

  // op 37842: delete
  db->Delete(write_options, "key0534");

  // op 37843: put
  db->Put(write_options, "key10426", "value88979");

  // op 37844: get
  status = db->Get(read_options, "key1335", &value);
  if (status.ok()) {
    out << "GET key1335 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37845: put
  db->Put(write_options, "key7642", "value6491");

  // op 37846: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 37847: put
  db->Put(write_options, "key10474", "value116102");

  // op 37848: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 37849: put
  db->Put(write_options, "key9660", "value18857");

  // op 37850: put
  db->Put(write_options, "key8370", "value86679");

  // op 37851: get
  status = db->Get(read_options, "key11356", &value);
  if (status.ok()) {
    out << "GET key11356 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37852: put
  db->Put(write_options, "key10040", "value109158");

  // op 37853: put
  db->Put(write_options, "key11627", "value39869");

  // op 37854: get
  status = db->Get(read_options, "key4324", &value);
  if (status.ok()) {
    out << "GET key4324 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37855: delete
  db->Delete(write_options, "key5019");

  // op 37856: put
  db->Put(write_options, "key5921", "value51763");

  // op 37857: delete
  db->Delete(write_options, "key11000");

  // op 37858: put
  db->Put(write_options, "key11398", "value85871");

  // op 37859: put
  db->Put(write_options, "key1709", "value29805");

  // op 37860: put
  db->Put(write_options, "key7879", "value79253");

  // op 37861: get
  status = db->Get(read_options, "key6650", &value);
  if (status.ok()) {
    out << "GET key6650 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37862: delete_range
  status = db->DeleteRange(write_options, "key2347", "key11719");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 37863: put
  db->Put(write_options, "key4930", "value60062");

  // op 37864: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 37865: put
  db->Put(write_options, "key7727", "value44432");

  // op 37866: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3204", "key8745", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3204, key8745) size=" << scan_result.size() << std::endl;
  }

  // op 37867: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9374", "key10884", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9374, key10884) size=" << scan_result.size() << std::endl;
  }

  // op 37868: delete_range
  status = db->DeleteRange(write_options, "key1502", "key4967");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 37869: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7777", "key8619", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7777, key8619) size=" << scan_result.size() << std::endl;
  }

  // op 37870: delete_range
  status = db->DeleteRange(write_options, "key4930", "key10738");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 37871: delete_range
  status = db->DeleteRange(write_options, "key4877", "key7162");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 37872: delete_range
  status = db->DeleteRange(write_options, "key1386", "key6697");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 37873: put
  db->Put(write_options, "key11370", "value86312");

  // op 37874: get
  status = db->Get(read_options, "key1123", &value);
  if (status.ok()) {
    out << "GET key1123 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37875: put
  db->Put(write_options, "key2752", "value103064");

  // op 37876: put
  db->Put(write_options, "key5622", "value62557");

  // op 37877: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2205", "key8713", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2205, key8713) size=" << scan_result.size() << std::endl;
  }

  // op 37878: put
  db->Put(write_options, "key8435", "value26587");

  // op 37879: delete_range
  status = db->DeleteRange(write_options, "key5176", "key8842");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 37880: put
  db->Put(write_options, "key1856", "value27253");

  // op 37881: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0862", "key3666", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0862, key3666) size=" << scan_result.size() << std::endl;
  }

  // op 37882: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7753", "key9265", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7753, key9265) size=" << scan_result.size() << std::endl;
  }

  // op 37883: delete
  db->Delete(write_options, "key4095");

  // op 37884: put
  db->Put(write_options, "key1747", "value52897");

  // op 37885: put
  db->Put(write_options, "key0391", "value114273");

  // op 37886: delete
  db->Delete(write_options, "key11522");

  // op 37887: delete_range
  status = db->DeleteRange(write_options, "key4713", "key7619");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 37888: get
  status = db->Get(read_options, "key3941", &value);
  if (status.ok()) {
    out << "GET key3941 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37889: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1616", "key7043", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1616, key7043) size=" << scan_result.size() << std::endl;
  }

  // op 37890: get
  status = db->Get(read_options, "key1193", &value);
  if (status.ok()) {
    out << "GET key1193 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37891: delete
  db->Delete(write_options, "key1968");

  // op 37892: get
  status = db->Get(read_options, "key1905", &value);
  if (status.ok()) {
    out << "GET key1905 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37893: put
  db->Put(write_options, "key9453", "value4348");

  // op 37894: put
  db->Put(write_options, "key9745", "value40152");

  // op 37895: put
  db->Put(write_options, "key1874", "value54970");

  // op 37896: put
  db->Put(write_options, "key0270", "value110232");

  // op 37897: put
  db->Put(write_options, "key5465", "value57270");

  // op 37898: get
  status = db->Get(read_options, "key5876", &value);
  if (status.ok()) {
    out << "GET key5876 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37899: delete_range
  status = db->DeleteRange(write_options, "key1899", "key11351");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 37900: get
  status = db->Get(read_options, "key4428", &value);
  if (status.ok()) {
    out << "GET key4428 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37901: put
  db->Put(write_options, "key6339", "value28113");

  // op 37902: get
  status = db->Get(read_options, "key9111", &value);
  if (status.ok()) {
    out << "GET key9111 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37903: get
  status = db->Get(read_options, "key7389", &value);
  if (status.ok()) {
    out << "GET key7389 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37904: put
  db->Put(write_options, "key4332", "value85080");

  // op 37905: put
  db->Put(write_options, "key1533", "value52813");

  // op 37906: put
  db->Put(write_options, "key11671", "value97204");

  // op 37907: get
  status = db->Get(read_options, "key2692", &value);
  if (status.ok()) {
    out << "GET key2692 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37908: get
  status = db->Get(read_options, "key2021", &value);
  if (status.ok()) {
    out << "GET key2021 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37909: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5236", "key9451", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5236, key9451) size=" << scan_result.size() << std::endl;
  }

  // op 37910: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0866", "key7971", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0866, key7971) size=" << scan_result.size() << std::endl;
  }

  // op 37911: put
  db->Put(write_options, "key2266", "value24805");

  // op 37912: delete_range
  status = db->DeleteRange(write_options, "key3211", "key9715");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 37913: put
  db->Put(write_options, "key8059", "value61537");

  // op 37914: put
  db->Put(write_options, "key9250", "value116292");

  // op 37915: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1850", "key9410", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1850, key9410) size=" << scan_result.size() << std::endl;
  }

  // op 37916: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2928", "key4728", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2928, key4728) size=" << scan_result.size() << std::endl;
  }

  // op 37917: delete_range
  status = db->DeleteRange(write_options, "key4418", "key9754");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 37918: put
  db->Put(write_options, "key8203", "value29693");

  // op 37919: put
  db->Put(write_options, "key11396", "value71529");

  // op 37920: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2709", "key3257", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2709, key3257) size=" << scan_result.size() << std::endl;
  }

  // op 37921: put
  db->Put(write_options, "key5044", "value49166");

  // op 37922: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 37923: put
  db->Put(write_options, "key9567", "value51076");

  // op 37924: get
  status = db->Get(read_options, "key2616", &value);
  if (status.ok()) {
    out << "GET key2616 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37925: get
  status = db->Get(read_options, "key9713", &value);
  if (status.ok()) {
    out << "GET key9713 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37926: put
  db->Put(write_options, "key6280", "value115921");

  // op 37927: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 37928: put
  db->Put(write_options, "key3269", "value50337");

  // op 37929: put
  db->Put(write_options, "key6823", "value115074");

  // op 37930: put
  db->Put(write_options, "key0838", "value34573");

  // op 37931: get
  status = db->Get(read_options, "key7156", &value);
  if (status.ok()) {
    out << "GET key7156 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37932: delete
  db->Delete(write_options, "key9664");

  // op 37933: put
  db->Put(write_options, "key3225", "value1412");

  // op 37934: put
  db->Put(write_options, "key2793", "value46048");

  // op 37935: delete_range
  status = db->DeleteRange(write_options, "key8611", "key10719");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 37936: put
  db->Put(write_options, "key0095", "value9360");

  // op 37937: put
  db->Put(write_options, "key4494", "value45670");

  // op 37938: delete
  db->Delete(write_options, "key9953");

  // op 37939: delete
  db->Delete(write_options, "key0806");

  // op 37940: get
  status = db->Get(read_options, "key7887", &value);
  if (status.ok()) {
    out << "GET key7887 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37941: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 37942: put
  db->Put(write_options, "key9830", "value52692");

  // op 37943: put
  db->Put(write_options, "key1727", "value88530");

  // op 37944: delete_range
  status = db->DeleteRange(write_options, "key7877", "key10948");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 37945: put
  db->Put(write_options, "key0808", "value2312");

  // op 37946: delete_range
  status = db->DeleteRange(write_options, "key4774", "key8390");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 37947: get
  status = db->Get(read_options, "key6222", &value);
  if (status.ok()) {
    out << "GET key6222 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37948: get
  status = db->Get(read_options, "key9399", &value);
  if (status.ok()) {
    out << "GET key9399 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37949: put
  db->Put(write_options, "key8625", "value56114");

  // op 37950: put
  db->Put(write_options, "key2939", "value68897");

  // op 37951: put
  db->Put(write_options, "key4014", "value69847");

  // op 37952: put
  db->Put(write_options, "key7977", "value30726");

  // op 37953: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4651", "key10198", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4651, key10198) size=" << scan_result.size() << std::endl;
  }

  // op 37954: put
  db->Put(write_options, "key3320", "value48778");

  // op 37955: put
  db->Put(write_options, "key4183", "value53174");

  // op 37956: get
  status = db->Get(read_options, "key2627", &value);
  if (status.ok()) {
    out << "GET key2627 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37957: delete_range
  status = db->DeleteRange(write_options, "key10645", "key11017");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 37958: put
  db->Put(write_options, "key7376", "value114267");

  // op 37959: put
  db->Put(write_options, "key5916", "value58893");

  // op 37960: get
  status = db->Get(read_options, "key5159", &value);
  if (status.ok()) {
    out << "GET key5159 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37961: put
  db->Put(write_options, "key2050", "value58587");

  // op 37962: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0301", "key1402", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0301, key1402) size=" << scan_result.size() << std::endl;
  }

  // op 37963: delete
  db->Delete(write_options, "key4616");

  // op 37964: get
  status = db->Get(read_options, "key8378", &value);
  if (status.ok()) {
    out << "GET key8378 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37965: put
  db->Put(write_options, "key7808", "value49618");

  // op 37966: get
  status = db->Get(read_options, "key9938", &value);
  if (status.ok()) {
    out << "GET key9938 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37967: put
  db->Put(write_options, "key1744", "value92683");

  // op 37968: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6913", "key11406", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6913, key11406) size=" << scan_result.size() << std::endl;
  }

  // op 37969: delete
  db->Delete(write_options, "key10787");

  // op 37970: put
  db->Put(write_options, "key4292", "value14065");

  // op 37971: delete_range
  status = db->DeleteRange(write_options, "key2116", "key5439");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 37972: put
  db->Put(write_options, "key4138", "value86428");

  // op 37973: put
  db->Put(write_options, "key4205", "value92219");

  // op 37974: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3760", "key6821", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3760, key6821) size=" << scan_result.size() << std::endl;
  }

  // op 37975: put
  db->Put(write_options, "key11452", "value30075");

  // op 37976: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9442", "key11134", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9442, key11134) size=" << scan_result.size() << std::endl;
  }

  // op 37977: delete
  db->Delete(write_options, "key2037");

  // op 37978: put
  db->Put(write_options, "key4412", "value104028");

  // op 37979: put
  db->Put(write_options, "key11429", "value31114");

  // op 37980: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4904", "key9037", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4904, key9037) size=" << scan_result.size() << std::endl;
  }

  // op 37981: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 37982: put
  db->Put(write_options, "key4512", "value114909");

  // op 37983: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1402", "key10105", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1402, key10105) size=" << scan_result.size() << std::endl;
  }

  // op 37984: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3098", "key9214", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3098, key9214) size=" << scan_result.size() << std::endl;
  }

  // op 37985: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 37986: put
  db->Put(write_options, "key4691", "value51152");

  // op 37987: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 37988: delete
  db->Delete(write_options, "key9935");

  // op 37989: put
  db->Put(write_options, "key1226", "value3698");

  // op 37990: put
  db->Put(write_options, "key9168", "value94362");

  // op 37991: delete
  db->Delete(write_options, "key6998");

  // op 37992: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0581", "key10743", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0581, key10743) size=" << scan_result.size() << std::endl;
  }

  // op 37993: get
  status = db->Get(read_options, "key11256", &value);
  if (status.ok()) {
    out << "GET key11256 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 37994: put
  db->Put(write_options, "key9298", "value49576");

  // op 37995: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 37996: put
  db->Put(write_options, "key11269", "value11718");

  // op 37997: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3286", "key7858", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3286, key7858) size=" << scan_result.size() << std::endl;
  }

  // op 37998: delete
  db->Delete(write_options, "key9288");

  // op 37999: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0967", "key11323", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0967, key11323) size=" << scan_result.size() << std::endl;
  }

  // op 38000: put
  db->Put(write_options, "key4913", "value106921");

  // op 38001: get
  status = db->Get(read_options, "key9182", &value);
  if (status.ok()) {
    out << "GET key9182 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38002: delete
  db->Delete(write_options, "key9487");

  // op 38003: put
  db->Put(write_options, "key0475", "value26034");

  // op 38004: put
  db->Put(write_options, "key6770", "value118016");

  // op 38005: put
  db->Put(write_options, "key3826", "value117856");

  // op 38006: get
  status = db->Get(read_options, "key2465", &value);
  if (status.ok()) {
    out << "GET key2465 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38007: put
  db->Put(write_options, "key9319", "value87032");

  // op 38008: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 38009: put
  db->Put(write_options, "key6316", "value63294");

  // op 38010: delete
  db->Delete(write_options, "key3140");

  // op 38011: put
  db->Put(write_options, "key3125", "value70742");

  // op 38012: get
  status = db->Get(read_options, "key0506", &value);
  if (status.ok()) {
    out << "GET key0506 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38013: get
  status = db->Get(read_options, "key10762", &value);
  if (status.ok()) {
    out << "GET key10762 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38014: get
  status = db->Get(read_options, "key9712", &value);
  if (status.ok()) {
    out << "GET key9712 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38015: get
  status = db->Get(read_options, "key1768", &value);
  if (status.ok()) {
    out << "GET key1768 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38016: delete
  db->Delete(write_options, "key7862");

  // op 38017: put
  db->Put(write_options, "key5450", "value60910");

  // op 38018: put
  db->Put(write_options, "key8556", "value16303");

  // op 38019: delete_range
  status = db->DeleteRange(write_options, "key0660", "key6611");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 38020: put
  db->Put(write_options, "key0923", "value12948");

  // op 38021: put
  db->Put(write_options, "key7566", "value75225");

  // op 38022: put
  db->Put(write_options, "key3530", "value46191");

  // op 38023: put
  db->Put(write_options, "key2726", "value102554");

  // op 38024: put
  db->Put(write_options, "key7509", "value96327");

  // op 38025: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5257", "key10317", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5257, key10317) size=" << scan_result.size() << std::endl;
  }

  // op 38026: delete
  db->Delete(write_options, "key6441");

  // op 38027: put
  db->Put(write_options, "key0152", "value95231");

  // op 38028: put
  db->Put(write_options, "key4507", "value45429");

  // op 38029: put
  db->Put(write_options, "key9846", "value77512");

  // op 38030: get
  status = db->Get(read_options, "key1906", &value);
  if (status.ok()) {
    out << "GET key1906 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38031: delete_range
  status = db->DeleteRange(write_options, "key5075", "key6811");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 38032: get
  status = db->Get(read_options, "key8952", &value);
  if (status.ok()) {
    out << "GET key8952 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38033: put
  db->Put(write_options, "key3089", "value20539");

  // op 38034: put
  db->Put(write_options, "key8530", "value36469");

  // op 38035: delete
  db->Delete(write_options, "key8358");

  // op 38036: put
  db->Put(write_options, "key4353", "value80449");

  // op 38037: get
  status = db->Get(read_options, "key5097", &value);
  if (status.ok()) {
    out << "GET key5097 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38038: get
  status = db->Get(read_options, "key0106", &value);
  if (status.ok()) {
    out << "GET key0106 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38039: get
  status = db->Get(read_options, "key4840", &value);
  if (status.ok()) {
    out << "GET key4840 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38040: get
  status = db->Get(read_options, "key0685", &value);
  if (status.ok()) {
    out << "GET key0685 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38041: delete
  db->Delete(write_options, "key6012");

  // op 38042: put
  db->Put(write_options, "key1814", "value25631");

  // op 38043: get
  status = db->Get(read_options, "key6127", &value);
  if (status.ok()) {
    out << "GET key6127 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38044: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8768", "key11050", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8768, key11050) size=" << scan_result.size() << std::endl;
  }

  // op 38045: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4900", "key11542", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4900, key11542) size=" << scan_result.size() << std::endl;
  }

  // op 38046: put
  db->Put(write_options, "key3543", "value73851");

  // op 38047: get
  status = db->Get(read_options, "key6941", &value);
  if (status.ok()) {
    out << "GET key6941 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38048: put
  db->Put(write_options, "key10968", "value108048");

  // op 38049: delete_range
  status = db->DeleteRange(write_options, "key6643", "key9798");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 38050: put
  db->Put(write_options, "key4419", "value106148");

  // op 38051: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2179", "key10050", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2179, key10050) size=" << scan_result.size() << std::endl;
  }

  // op 38052: delete_range
  status = db->DeleteRange(write_options, "key4332", "key11257");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 38053: delete_range
  status = db->DeleteRange(write_options, "key4258", "key5178");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 38054: delete_range
  status = db->DeleteRange(write_options, "key0398", "key5155");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 38055: delete_range
  status = db->DeleteRange(write_options, "key8085", "key11094");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 38056: delete
  db->Delete(write_options, "key8319");

  // op 38057: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5514", "key11723", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5514, key11723) size=" << scan_result.size() << std::endl;
  }

  // op 38058: delete
  db->Delete(write_options, "key7853");

  // op 38059: put
  db->Put(write_options, "key0557", "value74863");

  // op 38060: put
  db->Put(write_options, "key9395", "value80569");

  // op 38061: delete_range
  status = db->DeleteRange(write_options, "key1182", "key4482");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 38062: get
  status = db->Get(read_options, "key3499", &value);
  if (status.ok()) {
    out << "GET key3499 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38063: put
  db->Put(write_options, "key6288", "value13814");

  // op 38064: delete
  db->Delete(write_options, "key0906");

  // op 38065: delete
  db->Delete(write_options, "key0069");

  // op 38066: get
  status = db->Get(read_options, "key0019", &value);
  if (status.ok()) {
    out << "GET key0019 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38067: put
  db->Put(write_options, "key10807", "value114994");

  // op 38068: put
  db->Put(write_options, "key6247", "value36363");

  // op 38069: put
  db->Put(write_options, "key10932", "value91787");

  // op 38070: put
  db->Put(write_options, "key7165", "value101318");

  // op 38071: get
  status = db->Get(read_options, "key10171", &value);
  if (status.ok()) {
    out << "GET key10171 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38072: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1631", "key2940", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1631, key2940) size=" << scan_result.size() << std::endl;
  }

  // op 38073: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10298", "key11056", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10298, key11056) size=" << scan_result.size() << std::endl;
  }

  // op 38074: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1640", "key4007", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1640, key4007) size=" << scan_result.size() << std::endl;
  }

  // op 38075: put
  db->Put(write_options, "key2772", "value69701");

  // op 38076: put
  db->Put(write_options, "key7832", "value27046");

  // op 38077: get
  status = db->Get(read_options, "key9920", &value);
  if (status.ok()) {
    out << "GET key9920 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38078: get
  status = db->Get(read_options, "key0366", &value);
  if (status.ok()) {
    out << "GET key0366 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38079: put
  db->Put(write_options, "key3055", "value65343");

  // op 38080: delete
  db->Delete(write_options, "key6168");

  // op 38081: put
  db->Put(write_options, "key6541", "value17096");

  // op 38082: get
  status = db->Get(read_options, "key0032", &value);
  if (status.ok()) {
    out << "GET key0032 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38083: put
  db->Put(write_options, "key1862", "value12820");

  // op 38084: get
  status = db->Get(read_options, "key10655", &value);
  if (status.ok()) {
    out << "GET key10655 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38085: put
  db->Put(write_options, "key6882", "value108977");

  // op 38086: get
  status = db->Get(read_options, "key9437", &value);
  if (status.ok()) {
    out << "GET key9437 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38087: put
  db->Put(write_options, "key4832", "value19607");

  // op 38088: put
  db->Put(write_options, "key9475", "value43389");

  // op 38089: put
  db->Put(write_options, "key10003", "value38073");

  // op 38090: get
  status = db->Get(read_options, "key1254", &value);
  if (status.ok()) {
    out << "GET key1254 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38091: put
  db->Put(write_options, "key10579", "value39626");

  // op 38092: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6098", "key9751", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6098, key9751) size=" << scan_result.size() << std::endl;
  }

  // op 38093: get
  status = db->Get(read_options, "key6231", &value);
  if (status.ok()) {
    out << "GET key6231 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38094: delete_range
  status = db->DeleteRange(write_options, "key5800", "key9949");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 38095: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 38096: delete
  db->Delete(write_options, "key7733");

  // op 38097: put
  db->Put(write_options, "key4976", "value96674");

  // op 38098: put
  db->Put(write_options, "key11035", "value69253");

  // op 38099: put
  db->Put(write_options, "key1059", "value42745");

  // op 38100: put
  db->Put(write_options, "key4509", "value22497");

  // op 38101: put
  db->Put(write_options, "key5985", "value53576");

  // op 38102: delete_range
  status = db->DeleteRange(write_options, "key4614", "key11135");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 38103: put
  db->Put(write_options, "key8810", "value23939");

  // op 38104: get
  status = db->Get(read_options, "key6555", &value);
  if (status.ok()) {
    out << "GET key6555 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38105: put
  db->Put(write_options, "key0088", "value38434");

  // op 38106: get
  status = db->Get(read_options, "key7504", &value);
  if (status.ok()) {
    out << "GET key7504 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38107: get
  status = db->Get(read_options, "key10465", &value);
  if (status.ok()) {
    out << "GET key10465 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38108: delete
  db->Delete(write_options, "key0228");

  // op 38109: delete
  db->Delete(write_options, "key5233");

  // op 38110: put
  db->Put(write_options, "key9149", "value44928");

  // op 38111: put
  db->Put(write_options, "key8490", "value16948");

  // op 38112: put
  db->Put(write_options, "key3919", "value29838");

  // op 38113: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2480", "key4695", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2480, key4695) size=" << scan_result.size() << std::endl;
  }

  // op 38114: get
  status = db->Get(read_options, "key0342", &value);
  if (status.ok()) {
    out << "GET key0342 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38115: get
  status = db->Get(read_options, "key10784", &value);
  if (status.ok()) {
    out << "GET key10784 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38116: get
  status = db->Get(read_options, "key6089", &value);
  if (status.ok()) {
    out << "GET key6089 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38117: put
  db->Put(write_options, "key6475", "value103364");

  // op 38118: put
  db->Put(write_options, "key8986", "value87810");

  // op 38119: put
  db->Put(write_options, "key5738", "value61699");

  // op 38120: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5714", "key11443", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5714, key11443) size=" << scan_result.size() << std::endl;
  }

  // op 38121: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0663", "key3310", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0663, key3310) size=" << scan_result.size() << std::endl;
  }

  // op 38122: put
  db->Put(write_options, "key8651", "value86702");

  // op 38123: put
  db->Put(write_options, "key3915", "value73398");

  // op 38124: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3097", "key4183", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3097, key4183) size=" << scan_result.size() << std::endl;
  }

  // op 38125: get
  status = db->Get(read_options, "key3250", &value);
  if (status.ok()) {
    out << "GET key3250 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38126: delete_range
  status = db->DeleteRange(write_options, "key6221", "key10436");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 38127: get
  status = db->Get(read_options, "key1239", &value);
  if (status.ok()) {
    out << "GET key1239 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38128: delete_range
  status = db->DeleteRange(write_options, "key9789", "key11364");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 38129: put
  db->Put(write_options, "key5235", "value117228");

  // op 38130: delete
  db->Delete(write_options, "key8904");

  // op 38131: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 38132: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1512", "key2426", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1512, key2426) size=" << scan_result.size() << std::endl;
  }

  // op 38133: put
  db->Put(write_options, "key9420", "value116335");

  // op 38134: delete
  db->Delete(write_options, "key4806");

  // op 38135: delete_range
  status = db->DeleteRange(write_options, "key1205", "key5638");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 38136: put
  db->Put(write_options, "key8955", "value4656");

  // op 38137: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0005", "key1036", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0005, key1036) size=" << scan_result.size() << std::endl;
  }

  // op 38138: delete_range
  status = db->DeleteRange(write_options, "key1263", "key7099");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 38139: get
  status = db->Get(read_options, "key1794", &value);
  if (status.ok()) {
    out << "GET key1794 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38140: get
  status = db->Get(read_options, "key0737", &value);
  if (status.ok()) {
    out << "GET key0737 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38141: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 38142: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9218", "key10401", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9218, key10401) size=" << scan_result.size() << std::endl;
  }

  // op 38143: put
  db->Put(write_options, "key5410", "value68881");

  // op 38144: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 38145: get
  status = db->Get(read_options, "key0276", &value);
  if (status.ok()) {
    out << "GET key0276 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38146: delete
  db->Delete(write_options, "key2614");

  // op 38147: get
  status = db->Get(read_options, "key0758", &value);
  if (status.ok()) {
    out << "GET key0758 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38148: put
  db->Put(write_options, "key4913", "value74343");

  // op 38149: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8074", "key8363", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8074, key8363) size=" << scan_result.size() << std::endl;
  }

  // op 38150: put
  db->Put(write_options, "key7202", "value55992");

  // op 38151: put
  db->Put(write_options, "key1199", "value1667");

  // op 38152: delete
  db->Delete(write_options, "key0048");

  // op 38153: put
  db->Put(write_options, "key1798", "value72713");

  // op 38154: delete_range
  status = db->DeleteRange(write_options, "key4542", "key6657");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 38155: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 38156: delete
  db->Delete(write_options, "key8012");

  // op 38157: get
  status = db->Get(read_options, "key7126", &value);
  if (status.ok()) {
    out << "GET key7126 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38158: put
  db->Put(write_options, "key6536", "value10038");

  // op 38159: put
  db->Put(write_options, "key1589", "value5372");

  // op 38160: put
  db->Put(write_options, "key9941", "value51958");

  // op 38161: put
  db->Put(write_options, "key6669", "value71978");

  // op 38162: put
  db->Put(write_options, "key5673", "value18993");

  // op 38163: put
  db->Put(write_options, "key7315", "value74748");

  // op 38164: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 38165: get
  status = db->Get(read_options, "key5962", &value);
  if (status.ok()) {
    out << "GET key5962 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38166: put
  db->Put(write_options, "key6591", "value43950");

  // op 38167: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 38168: put
  db->Put(write_options, "key4118", "value117565");

  // op 38169: put
  db->Put(write_options, "key11379", "value79147");

  // op 38170: put
  db->Put(write_options, "key5465", "value35260");

  // op 38171: put
  db->Put(write_options, "key10258", "value98956");

  // op 38172: put
  db->Put(write_options, "key11153", "value54223");

  // op 38173: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 38174: put
  db->Put(write_options, "key1341", "value31628");

  // op 38175: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4005", "key4881", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4005, key4881) size=" << scan_result.size() << std::endl;
  }

  // op 38176: put
  db->Put(write_options, "key1895", "value68039");

  // op 38177: get
  status = db->Get(read_options, "key7923", &value);
  if (status.ok()) {
    out << "GET key7923 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38178: put
  db->Put(write_options, "key11272", "value64749");

  // op 38179: put
  db->Put(write_options, "key8735", "value103111");

  // op 38180: put
  db->Put(write_options, "key0696", "value54588");

  // op 38181: get
  status = db->Get(read_options, "key1594", &value);
  if (status.ok()) {
    out << "GET key1594 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38182: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 38183: put
  db->Put(write_options, "key9802", "value41758");

  // op 38184: get
  status = db->Get(read_options, "key4821", &value);
  if (status.ok()) {
    out << "GET key4821 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38185: put
  db->Put(write_options, "key0879", "value6935");

  // op 38186: delete_range
  status = db->DeleteRange(write_options, "key8118", "key9829");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 38187: get
  status = db->Get(read_options, "key5129", &value);
  if (status.ok()) {
    out << "GET key5129 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38188: get
  status = db->Get(read_options, "key0904", &value);
  if (status.ok()) {
    out << "GET key0904 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38189: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2623", "key5136", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2623, key5136) size=" << scan_result.size() << std::endl;
  }

  // op 38190: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1614", "key6964", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1614, key6964) size=" << scan_result.size() << std::endl;
  }

  // op 38191: put
  db->Put(write_options, "key10115", "value39311");

  // op 38192: delete
  db->Delete(write_options, "key7076");

  // op 38193: get
  status = db->Get(read_options, "key4774", &value);
  if (status.ok()) {
    out << "GET key4774 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38194: get
  status = db->Get(read_options, "key7464", &value);
  if (status.ok()) {
    out << "GET key7464 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38195: put
  db->Put(write_options, "key6293", "value72344");

  // op 38196: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1868", "key11485", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1868, key11485) size=" << scan_result.size() << std::endl;
  }

  // op 38197: put
  db->Put(write_options, "key7018", "value38673");

  // op 38198: delete_range
  status = db->DeleteRange(write_options, "key1491", "key9443");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 38199: delete_range
  status = db->DeleteRange(write_options, "key7867", "key9578");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 38200: delete_range
  status = db->DeleteRange(write_options, "key1492", "key8644");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 38201: delete_range
  status = db->DeleteRange(write_options, "key0406", "key11600");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 38202: delete
  db->Delete(write_options, "key9157");

  // op 38203: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0199", "key4452", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0199, key4452) size=" << scan_result.size() << std::endl;
  }

  // op 38204: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5104", "key11040", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5104, key11040) size=" << scan_result.size() << std::endl;
  }

  // op 38205: delete
  db->Delete(write_options, "key5329");

  // op 38206: delete
  db->Delete(write_options, "key10480");

  // op 38207: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4354", "key11308", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4354, key11308) size=" << scan_result.size() << std::endl;
  }

  // op 38208: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3221", "key4703", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3221, key4703) size=" << scan_result.size() << std::endl;
  }

  // op 38209: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3672", "key7492", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3672, key7492) size=" << scan_result.size() << std::endl;
  }

  // op 38210: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9176", "key11138", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9176, key11138) size=" << scan_result.size() << std::endl;
  }

  // op 38211: delete_range
  status = db->DeleteRange(write_options, "key2129", "key8654");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 38212: put
  db->Put(write_options, "key6535", "value86829");

  // op 38213: get
  status = db->Get(read_options, "key7955", &value);
  if (status.ok()) {
    out << "GET key7955 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38214: put
  db->Put(write_options, "key5797", "value9274");

  // op 38215: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4502", "key7765", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4502, key7765) size=" << scan_result.size() << std::endl;
  }

  // op 38216: put
  db->Put(write_options, "key3957", "value63083");

  // op 38217: delete_range
  status = db->DeleteRange(write_options, "key6125", "key9792");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 38218: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 38219: put
  db->Put(write_options, "key8525", "value101770");

  // op 38220: put
  db->Put(write_options, "key3171", "value54026");

  // op 38221: delete
  db->Delete(write_options, "key1942");

  // op 38222: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1608", "key10977", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1608, key10977) size=" << scan_result.size() << std::endl;
  }

  // op 38223: get
  status = db->Get(read_options, "key0614", &value);
  if (status.ok()) {
    out << "GET key0614 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38224: delete
  db->Delete(write_options, "key10259");

  // op 38225: delete_range
  status = db->DeleteRange(write_options, "key4863", "key9971");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 38226: get
  status = db->Get(read_options, "key4286", &value);
  if (status.ok()) {
    out << "GET key4286 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38227: get
  status = db->Get(read_options, "key3265", &value);
  if (status.ok()) {
    out << "GET key3265 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38228: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6222", "key9014", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6222, key9014) size=" << scan_result.size() << std::endl;
  }

  // op 38229: put
  db->Put(write_options, "key8224", "value102333");

  // op 38230: put
  db->Put(write_options, "key0677", "value63795");

  // op 38231: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2979", "key4931", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2979, key4931) size=" << scan_result.size() << std::endl;
  }

  // op 38232: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3586", "key10022", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3586, key10022) size=" << scan_result.size() << std::endl;
  }

  // op 38233: get
  status = db->Get(read_options, "key4301", &value);
  if (status.ok()) {
    out << "GET key4301 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38234: delete_range
  status = db->DeleteRange(write_options, "key9401", "key10089");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 38235: get
  status = db->Get(read_options, "key3216", &value);
  if (status.ok()) {
    out << "GET key3216 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38236: get
  status = db->Get(read_options, "key0715", &value);
  if (status.ok()) {
    out << "GET key0715 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38237: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6263", "key7318", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6263, key7318) size=" << scan_result.size() << std::endl;
  }

  // op 38238: put
  db->Put(write_options, "key9467", "value45734");

  // op 38239: put
  db->Put(write_options, "key1581", "value90372");

  // op 38240: put
  db->Put(write_options, "key6867", "value105550");

  // op 38241: put
  db->Put(write_options, "key7929", "value90667");

  // op 38242: put
  db->Put(write_options, "key1835", "value85915");

  // op 38243: get
  status = db->Get(read_options, "key10138", &value);
  if (status.ok()) {
    out << "GET key10138 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38244: put
  db->Put(write_options, "key4680", "value116988");

  // op 38245: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2225", "key9973", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2225, key9973) size=" << scan_result.size() << std::endl;
  }

  // op 38246: get
  status = db->Get(read_options, "key1678", &value);
  if (status.ok()) {
    out << "GET key1678 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38247: get
  status = db->Get(read_options, "key4211", &value);
  if (status.ok()) {
    out << "GET key4211 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38248: put
  db->Put(write_options, "key7697", "value31803");

  // op 38249: get
  status = db->Get(read_options, "key3264", &value);
  if (status.ok()) {
    out << "GET key3264 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38250: put
  db->Put(write_options, "key7864", "value45141");

  // op 38251: delete
  db->Delete(write_options, "key7107");

  // op 38252: put
  db->Put(write_options, "key6609", "value1428");

  // op 38253: get
  status = db->Get(read_options, "key10839", &value);
  if (status.ok()) {
    out << "GET key10839 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38254: get
  status = db->Get(read_options, "key6768", &value);
  if (status.ok()) {
    out << "GET key6768 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38255: put
  db->Put(write_options, "key5134", "value87507");

  // op 38256: put
  db->Put(write_options, "key4729", "value50215");

  // op 38257: get
  status = db->Get(read_options, "key11423", &value);
  if (status.ok()) {
    out << "GET key11423 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38258: put
  db->Put(write_options, "key3361", "value49150");

  // op 38259: delete_range
  status = db->DeleteRange(write_options, "key4286", "key8428");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 38260: get
  status = db->Get(read_options, "key4795", &value);
  if (status.ok()) {
    out << "GET key4795 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38261: put
  db->Put(write_options, "key3437", "value41783");

  // op 38262: put
  db->Put(write_options, "key9283", "value103531");

  // op 38263: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3254", "key10556", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3254, key10556) size=" << scan_result.size() << std::endl;
  }

  // op 38264: put
  db->Put(write_options, "key0944", "value91690");

  // op 38265: get
  status = db->Get(read_options, "key6710", &value);
  if (status.ok()) {
    out << "GET key6710 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38266: put
  db->Put(write_options, "key0548", "value76092");

  // op 38267: put
  db->Put(write_options, "key2037", "value51831");

  // op 38268: put
  db->Put(write_options, "key11722", "value97335");

  // op 38269: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0751", "key5887", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0751, key5887) size=" << scan_result.size() << std::endl;
  }

  // op 38270: get
  status = db->Get(read_options, "key11191", &value);
  if (status.ok()) {
    out << "GET key11191 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38271: put
  db->Put(write_options, "key10895", "value78747");

  // op 38272: get
  status = db->Get(read_options, "key3802", &value);
  if (status.ok()) {
    out << "GET key3802 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38273: delete_range
  status = db->DeleteRange(write_options, "key1958", "key4583");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 38274: get
  status = db->Get(read_options, "key2414", &value);
  if (status.ok()) {
    out << "GET key2414 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38275: get
  status = db->Get(read_options, "key5009", &value);
  if (status.ok()) {
    out << "GET key5009 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38276: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3307", "key9988", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3307, key9988) size=" << scan_result.size() << std::endl;
  }

  // op 38277: put
  db->Put(write_options, "key6205", "value74365");

  // op 38278: put
  db->Put(write_options, "key9134", "value111937");

  // op 38279: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2654", "key4094", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2654, key4094) size=" << scan_result.size() << std::endl;
  }

  // op 38280: put
  db->Put(write_options, "key7876", "value107656");

  // op 38281: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9227", "key11529", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9227, key11529) size=" << scan_result.size() << std::endl;
  }

  // op 38282: get
  status = db->Get(read_options, "key1146", &value);
  if (status.ok()) {
    out << "GET key1146 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38283: delete_range
  status = db->DeleteRange(write_options, "key0181", "key2222");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 38284: put
  db->Put(write_options, "key6454", "value68816");

  // op 38285: put
  db->Put(write_options, "key5674", "value61101");

  // op 38286: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2199", "key3657", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2199, key3657) size=" << scan_result.size() << std::endl;
  }

  // op 38287: put
  db->Put(write_options, "key11020", "value52237");

  // op 38288: put
  db->Put(write_options, "key2561", "value83000");

  // op 38289: put
  db->Put(write_options, "key5346", "value86170");

  // op 38290: delete_range
  status = db->DeleteRange(write_options, "key5137", "key5469");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 38291: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2162", "key6956", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2162, key6956) size=" << scan_result.size() << std::endl;
  }

  // op 38292: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 38293: put
  db->Put(write_options, "key4816", "value97666");

  // op 38294: get
  status = db->Get(read_options, "key9639", &value);
  if (status.ok()) {
    out << "GET key9639 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38295: delete_range
  status = db->DeleteRange(write_options, "key0862", "key8791");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 38296: delete
  db->Delete(write_options, "key3976");

  // op 38297: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1475", "key4581", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1475, key4581) size=" << scan_result.size() << std::endl;
  }

  // op 38298: put
  db->Put(write_options, "key3956", "value48743");

  // op 38299: delete
  db->Delete(write_options, "key3193");

  // op 38300: get
  status = db->Get(read_options, "key8036", &value);
  if (status.ok()) {
    out << "GET key8036 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38301: put
  db->Put(write_options, "key9799", "value71423");

  // op 38302: get
  status = db->Get(read_options, "key2968", &value);
  if (status.ok()) {
    out << "GET key2968 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38303: get
  status = db->Get(read_options, "key6220", &value);
  if (status.ok()) {
    out << "GET key6220 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38304: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 38305: delete_range
  status = db->DeleteRange(write_options, "key6289", "key10902");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 38306: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 38307: get
  status = db->Get(read_options, "key9412", &value);
  if (status.ok()) {
    out << "GET key9412 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38308: put
  db->Put(write_options, "key5622", "value31496");

  // op 38309: put
  db->Put(write_options, "key3215", "value52324");

  // op 38310: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 38311: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4307", "key9900", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4307, key9900) size=" << scan_result.size() << std::endl;
  }

  // op 38312: get
  status = db->Get(read_options, "key10605", &value);
  if (status.ok()) {
    out << "GET key10605 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38313: put
  db->Put(write_options, "key2629", "value3773");

  // op 38314: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6559", "key9205", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6559, key9205) size=" << scan_result.size() << std::endl;
  }

  // op 38315: put
  db->Put(write_options, "key10897", "value8805");

  // op 38316: delete
  db->Delete(write_options, "key11345");

  // op 38317: get
  status = db->Get(read_options, "key8312", &value);
  if (status.ok()) {
    out << "GET key8312 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38318: delete_range
  status = db->DeleteRange(write_options, "key3890", "key6429");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 38319: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3792", "key7877", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3792, key7877) size=" << scan_result.size() << std::endl;
  }

  // op 38320: delete_range
  status = db->DeleteRange(write_options, "key6317", "key11535");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 38321: put
  db->Put(write_options, "key9455", "value77731");

  // op 38322: delete
  db->Delete(write_options, "key10912");

  // op 38323: get
  status = db->Get(read_options, "key9480", &value);
  if (status.ok()) {
    out << "GET key9480 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38324: put
  db->Put(write_options, "key0262", "value36439");

  // op 38325: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 38326: put
  db->Put(write_options, "key10180", "value98409");

  // op 38327: put
  db->Put(write_options, "key10624", "value90609");

  // op 38328: get
  status = db->Get(read_options, "key10593", &value);
  if (status.ok()) {
    out << "GET key10593 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38329: put
  db->Put(write_options, "key6056", "value52600");

  // op 38330: put
  db->Put(write_options, "key3866", "value90047");

  // op 38331: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6192", "key11154", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6192, key11154) size=" << scan_result.size() << std::endl;
  }

  // op 38332: put
  db->Put(write_options, "key3037", "value39890");

  // op 38333: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2551", "key6073", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2551, key6073) size=" << scan_result.size() << std::endl;
  }

  // op 38334: get
  status = db->Get(read_options, "key8311", &value);
  if (status.ok()) {
    out << "GET key8311 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38335: put
  db->Put(write_options, "key1948", "value74237");

  // op 38336: put
  db->Put(write_options, "key7818", "value106893");

  // op 38337: put
  db->Put(write_options, "key3040", "value105353");

  // op 38338: get
  status = db->Get(read_options, "key1642", &value);
  if (status.ok()) {
    out << "GET key1642 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38339: put
  db->Put(write_options, "key7328", "value93919");

  // op 38340: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4344", "key6274", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4344, key6274) size=" << scan_result.size() << std::endl;
  }

  // op 38341: delete_range
  status = db->DeleteRange(write_options, "key1086", "key2918");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 38342: put
  db->Put(write_options, "key6222", "value78614");

  // op 38343: get
  status = db->Get(read_options, "key7320", &value);
  if (status.ok()) {
    out << "GET key7320 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38344: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0588", "key8215", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0588, key8215) size=" << scan_result.size() << std::endl;
  }

  // op 38345: put
  db->Put(write_options, "key9571", "value80092");

  // op 38346: get
  status = db->Get(read_options, "key0328", &value);
  if (status.ok()) {
    out << "GET key0328 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38347: put
  db->Put(write_options, "key5135", "value67890");

  // op 38348: delete
  db->Delete(write_options, "key0734");

  // op 38349: delete_range
  status = db->DeleteRange(write_options, "key2439", "key10657");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 38350: put
  db->Put(write_options, "key0273", "value50960");

  // op 38351: put
  db->Put(write_options, "key6155", "value54736");

  // op 38352: put
  db->Put(write_options, "key3170", "value76864");

  // op 38353: put
  db->Put(write_options, "key8919", "value7505");

  // op 38354: delete_range
  status = db->DeleteRange(write_options, "key0675", "key7421");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 38355: put
  db->Put(write_options, "key6404", "value15495");

  // op 38356: put
  db->Put(write_options, "key5788", "value5370");

  // op 38357: delete
  db->Delete(write_options, "key8213");

  // op 38358: put
  db->Put(write_options, "key7631", "value87953");

  // op 38359: get
  status = db->Get(read_options, "key9751", &value);
  if (status.ok()) {
    out << "GET key9751 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38360: put
  db->Put(write_options, "key7296", "value23501");

  // op 38361: get
  status = db->Get(read_options, "key1402", &value);
  if (status.ok()) {
    out << "GET key1402 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38362: delete
  db->Delete(write_options, "key0345");

  // op 38363: delete
  db->Delete(write_options, "key5740");

  // op 38364: get
  status = db->Get(read_options, "key4919", &value);
  if (status.ok()) {
    out << "GET key4919 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38365: put
  db->Put(write_options, "key7265", "value39431");

  // op 38366: delete_range
  status = db->DeleteRange(write_options, "key1624", "key7956");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 38367: put
  db->Put(write_options, "key9982", "value7861");

  // op 38368: delete_range
  status = db->DeleteRange(write_options, "key2886", "key6259");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 38369: put
  db->Put(write_options, "key5884", "value58264");

  // op 38370: get
  status = db->Get(read_options, "key2554", &value);
  if (status.ok()) {
    out << "GET key2554 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38371: delete
  db->Delete(write_options, "key9155");

  // op 38372: delete_range
  status = db->DeleteRange(write_options, "key5932", "key7245");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 38373: put
  db->Put(write_options, "key5903", "value24152");

  // op 38374: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6506", "key9435", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6506, key9435) size=" << scan_result.size() << std::endl;
  }

  // op 38375: put
  db->Put(write_options, "key6531", "value89336");

  // op 38376: put
  db->Put(write_options, "key2405", "value106701");

  // op 38377: put
  db->Put(write_options, "key9140", "value53247");

  // op 38378: put
  db->Put(write_options, "key10360", "value30107");

  // op 38379: put
  db->Put(write_options, "key1451", "value34589");

  // op 38380: put
  db->Put(write_options, "key6869", "value67437");

  // op 38381: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 38382: delete
  db->Delete(write_options, "key8611");

  // op 38383: put
  db->Put(write_options, "key7782", "value53341");

  // op 38384: put
  db->Put(write_options, "key7988", "value63234");

  // op 38385: get
  status = db->Get(read_options, "key6417", &value);
  if (status.ok()) {
    out << "GET key6417 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38386: delete
  db->Delete(write_options, "key4080");

  // op 38387: delete_range
  status = db->DeleteRange(write_options, "key9613", "key11378");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 38388: get
  status = db->Get(read_options, "key11465", &value);
  if (status.ok()) {
    out << "GET key11465 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38389: put
  db->Put(write_options, "key5385", "value37102");

  // op 38390: delete
  db->Delete(write_options, "key5151");

  // op 38391: put
  db->Put(write_options, "key10565", "value108783");

  // op 38392: get
  status = db->Get(read_options, "key6622", &value);
  if (status.ok()) {
    out << "GET key6622 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38393: get
  status = db->Get(read_options, "key9958", &value);
  if (status.ok()) {
    out << "GET key9958 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38394: put
  db->Put(write_options, "key0451", "value16050");

  // op 38395: get
  status = db->Get(read_options, "key8919", &value);
  if (status.ok()) {
    out << "GET key8919 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38396: get
  status = db->Get(read_options, "key4591", &value);
  if (status.ok()) {
    out << "GET key4591 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38397: delete
  db->Delete(write_options, "key10869");

  // op 38398: get
  status = db->Get(read_options, "key10418", &value);
  if (status.ok()) {
    out << "GET key10418 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38399: put
  db->Put(write_options, "key10012", "value44439");

  // op 38400: get
  status = db->Get(read_options, "key2371", &value);
  if (status.ok()) {
    out << "GET key2371 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38401: delete
  db->Delete(write_options, "key7696");

  // op 38402: get
  status = db->Get(read_options, "key4168", &value);
  if (status.ok()) {
    out << "GET key4168 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38403: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7570", "key9904", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7570, key9904) size=" << scan_result.size() << std::endl;
  }

  // op 38404: put
  db->Put(write_options, "key11168", "value84455");

  // op 38405: delete
  db->Delete(write_options, "key9893");

  // op 38406: get
  status = db->Get(read_options, "key7745", &value);
  if (status.ok()) {
    out << "GET key7745 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38407: get
  status = db->Get(read_options, "key9856", &value);
  if (status.ok()) {
    out << "GET key9856 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38408: put
  db->Put(write_options, "key7565", "value117276");

  // op 38409: put
  db->Put(write_options, "key6030", "value83777");

  // op 38410: get
  status = db->Get(read_options, "key0361", &value);
  if (status.ok()) {
    out << "GET key0361 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38411: put
  db->Put(write_options, "key2715", "value43835");

  // op 38412: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0331", "key3668", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0331, key3668) size=" << scan_result.size() << std::endl;
  }

  // op 38413: put
  db->Put(write_options, "key5769", "value1459");

  // op 38414: put
  db->Put(write_options, "key9501", "value103106");

  // op 38415: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1291", "key8798", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1291, key8798) size=" << scan_result.size() << std::endl;
  }

  // op 38416: put
  db->Put(write_options, "key9748", "value68458");

  // op 38417: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6437", "key8146", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6437, key8146) size=" << scan_result.size() << std::endl;
  }

  // op 38418: put
  db->Put(write_options, "key4859", "value67371");

  // op 38419: get
  status = db->Get(read_options, "key2397", &value);
  if (status.ok()) {
    out << "GET key2397 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38420: delete_range
  status = db->DeleteRange(write_options, "key3818", "key3883");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 38421: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0479", "key7073", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0479, key7073) size=" << scan_result.size() << std::endl;
  }

  // op 38422: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4003", "key11666", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4003, key11666) size=" << scan_result.size() << std::endl;
  }

  // op 38423: put
  db->Put(write_options, "key10810", "value113796");

  // op 38424: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2588", "key3261", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2588, key3261) size=" << scan_result.size() << std::endl;
  }

  // op 38425: put
  db->Put(write_options, "key7964", "value1160");

  // op 38426: delete
  db->Delete(write_options, "key5656");

  // op 38427: delete
  db->Delete(write_options, "key11102");

  // op 38428: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3317", "key10158", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3317, key10158) size=" << scan_result.size() << std::endl;
  }

  // op 38429: put
  db->Put(write_options, "key5082", "value27630");

  // op 38430: put
  db->Put(write_options, "key1222", "value83949");

  // op 38431: get
  status = db->Get(read_options, "key6361", &value);
  if (status.ok()) {
    out << "GET key6361 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38432: put
  db->Put(write_options, "key1418", "value19108");

  // op 38433: delete
  db->Delete(write_options, "key0613");

  // op 38434: put
  db->Put(write_options, "key3801", "value41246");

  // op 38435: put
  db->Put(write_options, "key7938", "value27520");

  // op 38436: delete_range
  status = db->DeleteRange(write_options, "key3873", "key10538");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 38437: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8226", "key11037", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8226, key11037) size=" << scan_result.size() << std::endl;
  }

  // op 38438: delete_range
  status = db->DeleteRange(write_options, "key4546", "key5697");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 38439: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2511", "key5290", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2511, key5290) size=" << scan_result.size() << std::endl;
  }

  // op 38440: put
  db->Put(write_options, "key3134", "value69289");

  // op 38441: get
  status = db->Get(read_options, "key8241", &value);
  if (status.ok()) {
    out << "GET key8241 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38442: get
  status = db->Get(read_options, "key4553", &value);
  if (status.ok()) {
    out << "GET key4553 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38443: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 38444: get
  status = db->Get(read_options, "key2594", &value);
  if (status.ok()) {
    out << "GET key2594 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38445: put
  db->Put(write_options, "key4442", "value46370");

  // op 38446: put
  db->Put(write_options, "key3274", "value19519");

  // op 38447: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 38448: delete_range
  status = db->DeleteRange(write_options, "key3945", "key10632");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 38449: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3201", "key3479", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3201, key3479) size=" << scan_result.size() << std::endl;
  }

  // op 38450: get
  status = db->Get(read_options, "key5363", &value);
  if (status.ok()) {
    out << "GET key5363 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38451: get
  status = db->Get(read_options, "key5377", &value);
  if (status.ok()) {
    out << "GET key5377 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38452: put
  db->Put(write_options, "key11610", "value110433");

  // op 38453: delete_range
  status = db->DeleteRange(write_options, "key4418", "key5595");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 38454: put
  db->Put(write_options, "key1025", "value53227");

  // op 38455: put
  db->Put(write_options, "key3781", "value115787");

  // op 38456: get
  status = db->Get(read_options, "key10936", &value);
  if (status.ok()) {
    out << "GET key10936 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38457: delete
  db->Delete(write_options, "key3725");

  // op 38458: put
  db->Put(write_options, "key5489", "value82172");

  // op 38459: put
  db->Put(write_options, "key4174", "value42082");

  // op 38460: delete_range
  status = db->DeleteRange(write_options, "key3513", "key11387");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 38461: put
  db->Put(write_options, "key8758", "value70258");

  // op 38462: put
  db->Put(write_options, "key7328", "value33758");

  // op 38463: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5210", "key6624", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5210, key6624) size=" << scan_result.size() << std::endl;
  }

  // op 38464: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0055", "key8758", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0055, key8758) size=" << scan_result.size() << std::endl;
  }

  // op 38465: get
  status = db->Get(read_options, "key3387", &value);
  if (status.ok()) {
    out << "GET key3387 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38466: get
  status = db->Get(read_options, "key0741", &value);
  if (status.ok()) {
    out << "GET key0741 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38467: put
  db->Put(write_options, "key6814", "value19694");

  // op 38468: get
  status = db->Get(read_options, "key0398", &value);
  if (status.ok()) {
    out << "GET key0398 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38469: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 38470: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6413", "key8804", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6413, key8804) size=" << scan_result.size() << std::endl;
  }

  // op 38471: put
  db->Put(write_options, "key8361", "value20856");

  // op 38472: delete_range
  status = db->DeleteRange(write_options, "key0876", "key10037");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 38473: delete_range
  status = db->DeleteRange(write_options, "key2624", "key10146");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 38474: get
  status = db->Get(read_options, "key8860", &value);
  if (status.ok()) {
    out << "GET key8860 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38475: put
  db->Put(write_options, "key11080", "value114296");

  // op 38476: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10448", "key11065", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10448, key11065) size=" << scan_result.size() << std::endl;
  }

  // op 38477: get
  status = db->Get(read_options, "key10808", &value);
  if (status.ok()) {
    out << "GET key10808 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38478: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10501", "key10642", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10501, key10642) size=" << scan_result.size() << std::endl;
  }

  // op 38479: put
  db->Put(write_options, "key9253", "value57073");

  // op 38480: put
  db->Put(write_options, "key2884", "value28269");

  // op 38481: delete_range
  status = db->DeleteRange(write_options, "key2199", "key9950");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 38482: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7340", "key11537", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7340, key11537) size=" << scan_result.size() << std::endl;
  }

  // op 38483: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1341", "key7205", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1341, key7205) size=" << scan_result.size() << std::endl;
  }

  // op 38484: get
  status = db->Get(read_options, "key1560", &value);
  if (status.ok()) {
    out << "GET key1560 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38485: get
  status = db->Get(read_options, "key9988", &value);
  if (status.ok()) {
    out << "GET key9988 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38486: put
  db->Put(write_options, "key3609", "value1214");

  // op 38487: put
  db->Put(write_options, "key4859", "value115029");

  // op 38488: delete
  db->Delete(write_options, "key7667");

  // op 38489: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1833", "key8175", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1833, key8175) size=" << scan_result.size() << std::endl;
  }

  // op 38490: delete_range
  status = db->DeleteRange(write_options, "key5535", "key6379");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 38491: put
  db->Put(write_options, "key8263", "value31043");

  // op 38492: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2633", "key10122", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2633, key10122) size=" << scan_result.size() << std::endl;
  }

  // op 38493: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8794", "key9991", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8794, key9991) size=" << scan_result.size() << std::endl;
  }

  // op 38494: delete
  db->Delete(write_options, "key9265");

  // op 38495: get
  status = db->Get(read_options, "key2966", &value);
  if (status.ok()) {
    out << "GET key2966 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38496: put
  db->Put(write_options, "key4359", "value117153");

  // op 38497: put
  db->Put(write_options, "key8167", "value112582");

  // op 38498: get
  status = db->Get(read_options, "key9242", &value);
  if (status.ok()) {
    out << "GET key9242 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38499: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3430", "key5303", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3430, key5303) size=" << scan_result.size() << std::endl;
  }

  // op 38500: put
  db->Put(write_options, "key3963", "value79849");

  // op 38501: delete_range
  status = db->DeleteRange(write_options, "key3985", "key9223");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 38502: delete
  db->Delete(write_options, "key11071");

  // op 38503: put
  db->Put(write_options, "key2585", "value103292");

  // op 38504: delete_range
  status = db->DeleteRange(write_options, "key7527", "key7937");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 38505: delete
  db->Delete(write_options, "key10651");

  // op 38506: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1317", "key5620", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1317, key5620) size=" << scan_result.size() << std::endl;
  }

  // op 38507: put
  db->Put(write_options, "key4480", "value1479");

  // op 38508: put
  db->Put(write_options, "key4274", "value87631");

  // op 38509: put
  db->Put(write_options, "key2212", "value58353");

  // op 38510: delete_range
  status = db->DeleteRange(write_options, "key7029", "key10959");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 38511: put
  db->Put(write_options, "key6638", "value56882");

  // op 38512: put
  db->Put(write_options, "key9119", "value16273");

  // op 38513: get
  status = db->Get(read_options, "key7069", &value);
  if (status.ok()) {
    out << "GET key7069 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38514: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6334", "key11433", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6334, key11433) size=" << scan_result.size() << std::endl;
  }

  // op 38515: put
  db->Put(write_options, "key0569", "value80278");

  // op 38516: put
  db->Put(write_options, "key5162", "value11101");

  // op 38517: put
  db->Put(write_options, "key10092", "value91180");

  // op 38518: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5828", "key10350", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5828, key10350) size=" << scan_result.size() << std::endl;
  }

  // op 38519: delete
  db->Delete(write_options, "key5915");

  // op 38520: put
  db->Put(write_options, "key3034", "value99510");

  // op 38521: get
  status = db->Get(read_options, "key4267", &value);
  if (status.ok()) {
    out << "GET key4267 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38522: delete
  db->Delete(write_options, "key10185");

  // op 38523: get
  status = db->Get(read_options, "key9395", &value);
  if (status.ok()) {
    out << "GET key9395 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38524: put
  db->Put(write_options, "key8169", "value98621");

  // op 38525: get
  status = db->Get(read_options, "key5091", &value);
  if (status.ok()) {
    out << "GET key5091 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38526: get
  status = db->Get(read_options, "key5521", &value);
  if (status.ok()) {
    out << "GET key5521 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38527: put
  db->Put(write_options, "key3506", "value82869");

  // op 38528: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2912", "key7814", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2912, key7814) size=" << scan_result.size() << std::endl;
  }

  // op 38529: put
  db->Put(write_options, "key3556", "value6956");

  // op 38530: get
  status = db->Get(read_options, "key2833", &value);
  if (status.ok()) {
    out << "GET key2833 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38531: put
  db->Put(write_options, "key8992", "value72909");

  // op 38532: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6047", "key11422", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6047, key11422) size=" << scan_result.size() << std::endl;
  }

  // op 38533: put
  db->Put(write_options, "key8606", "value8436");

  // op 38534: get
  status = db->Get(read_options, "key0217", &value);
  if (status.ok()) {
    out << "GET key0217 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38535: put
  db->Put(write_options, "key7256", "value100410");

  // op 38536: put
  db->Put(write_options, "key6632", "value2883");

  // op 38537: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 38538: delete
  db->Delete(write_options, "key3055");

  // op 38539: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0716", "key5195", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0716, key5195) size=" << scan_result.size() << std::endl;
  }

  // op 38540: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 38541: delete_range
  status = db->DeleteRange(write_options, "key0733", "key6660");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 38542: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8997", "key10749", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8997, key10749) size=" << scan_result.size() << std::endl;
  }

  // op 38543: put
  db->Put(write_options, "key2081", "value105433");

  // op 38544: put
  db->Put(write_options, "key7184", "value31337");

  // op 38545: delete_range
  status = db->DeleteRange(write_options, "key9253", "key11206");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 38546: delete_range
  status = db->DeleteRange(write_options, "key2482", "key2767");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 38547: put
  db->Put(write_options, "key5878", "value61584");

  // op 38548: delete
  db->Delete(write_options, "key3422");

  // op 38549: get
  status = db->Get(read_options, "key2554", &value);
  if (status.ok()) {
    out << "GET key2554 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38550: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9512", "key11167", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9512, key11167) size=" << scan_result.size() << std::endl;
  }

  // op 38551: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3144", "key10515", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3144, key10515) size=" << scan_result.size() << std::endl;
  }

  // op 38552: delete
  db->Delete(write_options, "key5363");

  // op 38553: delete
  db->Delete(write_options, "key10371");

  // op 38554: put
  db->Put(write_options, "key5960", "value112823");

  // op 38555: put
  db->Put(write_options, "key8999", "value104910");

  // op 38556: delete_range
  status = db->DeleteRange(write_options, "key5555", "key6437");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 38557: put
  db->Put(write_options, "key2796", "value58064");

  // op 38558: delete
  db->Delete(write_options, "key9699");

  // op 38559: get
  status = db->Get(read_options, "key2932", &value);
  if (status.ok()) {
    out << "GET key2932 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38560: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 38561: get
  status = db->Get(read_options, "key1621", &value);
  if (status.ok()) {
    out << "GET key1621 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38562: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0003", "key9821", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0003, key9821) size=" << scan_result.size() << std::endl;
  }

  // op 38563: put
  db->Put(write_options, "key4939", "value70217");

  // op 38564: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5183", "key7808", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5183, key7808) size=" << scan_result.size() << std::endl;
  }

  // op 38565: delete
  db->Delete(write_options, "key8677");

  // op 38566: get
  status = db->Get(read_options, "key7854", &value);
  if (status.ok()) {
    out << "GET key7854 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38567: put
  db->Put(write_options, "key6893", "value105500");

  // op 38568: put
  db->Put(write_options, "key0173", "value79989");

  // op 38569: put
  db->Put(write_options, "key5938", "value39466");

  // op 38570: get
  status = db->Get(read_options, "key7144", &value);
  if (status.ok()) {
    out << "GET key7144 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38571: get
  status = db->Get(read_options, "key0601", &value);
  if (status.ok()) {
    out << "GET key0601 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38572: delete
  db->Delete(write_options, "key4271");

  // op 38573: put
  db->Put(write_options, "key3233", "value71829");

  // op 38574: put
  db->Put(write_options, "key3616", "value51216");

  // op 38575: get
  status = db->Get(read_options, "key8601", &value);
  if (status.ok()) {
    out << "GET key8601 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38576: put
  db->Put(write_options, "key5183", "value95407");

  // op 38577: delete
  db->Delete(write_options, "key2199");

  // op 38578: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0076", "key6866", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0076, key6866) size=" << scan_result.size() << std::endl;
  }

  // op 38579: put
  db->Put(write_options, "key1919", "value21825");

  // op 38580: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1254", "key9770", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1254, key9770) size=" << scan_result.size() << std::endl;
  }

  // op 38581: put
  db->Put(write_options, "key4870", "value87140");

  // op 38582: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1005", "key4037", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1005, key4037) size=" << scan_result.size() << std::endl;
  }

  // op 38583: delete_range
  status = db->DeleteRange(write_options, "key0557", "key7464");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 38584: put
  db->Put(write_options, "key3219", "value83387");

  // op 38585: get
  status = db->Get(read_options, "key8415", &value);
  if (status.ok()) {
    out << "GET key8415 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38586: delete_range
  status = db->DeleteRange(write_options, "key0664", "key3116");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 38587: delete
  db->Delete(write_options, "key8970");

  // op 38588: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0361", "key8131", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0361, key8131) size=" << scan_result.size() << std::endl;
  }

  // op 38589: delete
  db->Delete(write_options, "key9649");

  // op 38590: get
  status = db->Get(read_options, "key9604", &value);
  if (status.ok()) {
    out << "GET key9604 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38591: put
  db->Put(write_options, "key6980", "value86960");

  // op 38592: put
  db->Put(write_options, "key9412", "value45775");

  // op 38593: get
  status = db->Get(read_options, "key2656", &value);
  if (status.ok()) {
    out << "GET key2656 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38594: put
  db->Put(write_options, "key1693", "value58079");

  // op 38595: get
  status = db->Get(read_options, "key10275", &value);
  if (status.ok()) {
    out << "GET key10275 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38596: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3211", "key10101", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3211, key10101) size=" << scan_result.size() << std::endl;
  }

  // op 38597: put
  db->Put(write_options, "key1093", "value22990");

  // op 38598: put
  db->Put(write_options, "key11009", "value29079");

  // op 38599: put
  db->Put(write_options, "key2353", "value113856");

  // op 38600: delete_range
  status = db->DeleteRange(write_options, "key3731", "key6392");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 38601: get
  status = db->Get(read_options, "key5236", &value);
  if (status.ok()) {
    out << "GET key5236 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38602: put
  db->Put(write_options, "key7769", "value48636");

  // op 38603: put
  db->Put(write_options, "key1953", "value30652");

  // op 38604: put
  db->Put(write_options, "key5742", "value0784");

  // op 38605: delete
  db->Delete(write_options, "key0875");

  // op 38606: put
  db->Put(write_options, "key1974", "value36560");

  // op 38607: delete_range
  status = db->DeleteRange(write_options, "key1889", "key4664");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 38608: put
  db->Put(write_options, "key8661", "value63563");

  // op 38609: get
  status = db->Get(read_options, "key1748", &value);
  if (status.ok()) {
    out << "GET key1748 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38610: get
  status = db->Get(read_options, "key11068", &value);
  if (status.ok()) {
    out << "GET key11068 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38611: put
  db->Put(write_options, "key2664", "value95589");

  // op 38612: put
  db->Put(write_options, "key9779", "value54060");

  // op 38613: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1220", "key4801", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1220, key4801) size=" << scan_result.size() << std::endl;
  }

  // op 38614: get
  status = db->Get(read_options, "key7980", &value);
  if (status.ok()) {
    out << "GET key7980 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38615: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4796", "key6096", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4796, key6096) size=" << scan_result.size() << std::endl;
  }

  // op 38616: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3586", "key4566", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3586, key4566) size=" << scan_result.size() << std::endl;
  }

  // op 38617: delete_range
  status = db->DeleteRange(write_options, "key9354", "key10878");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 38618: put
  db->Put(write_options, "key0617", "value61884");

  // op 38619: put
  db->Put(write_options, "key1488", "value48491");

  // op 38620: get
  status = db->Get(read_options, "key11354", &value);
  if (status.ok()) {
    out << "GET key11354 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38621: put
  db->Put(write_options, "key6587", "value42767");

  // op 38622: put
  db->Put(write_options, "key4521", "value5630");

  // op 38623: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0674", "key6559", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0674, key6559) size=" << scan_result.size() << std::endl;
  }

  // op 38624: put
  db->Put(write_options, "key0640", "value45610");

  // op 38625: get
  status = db->Get(read_options, "key3951", &value);
  if (status.ok()) {
    out << "GET key3951 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38626: get
  status = db->Get(read_options, "key7612", &value);
  if (status.ok()) {
    out << "GET key7612 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38627: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 38628: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 38629: put
  db->Put(write_options, "key6398", "value71350");

  // op 38630: get
  status = db->Get(read_options, "key5465", &value);
  if (status.ok()) {
    out << "GET key5465 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38631: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2873", "key3909", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2873, key3909) size=" << scan_result.size() << std::endl;
  }

  // op 38632: delete
  db->Delete(write_options, "key9729");

  // op 38633: put
  db->Put(write_options, "key6526", "value18832");

  // op 38634: get
  status = db->Get(read_options, "key7152", &value);
  if (status.ok()) {
    out << "GET key7152 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38635: get
  status = db->Get(read_options, "key2898", &value);
  if (status.ok()) {
    out << "GET key2898 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38636: put
  db->Put(write_options, "key6082", "value65870");

  // op 38637: put
  db->Put(write_options, "key7835", "value62248");

  // op 38638: get
  status = db->Get(read_options, "key9989", &value);
  if (status.ok()) {
    out << "GET key9989 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38639: put
  db->Put(write_options, "key4802", "value48370");

  // op 38640: get
  status = db->Get(read_options, "key3634", &value);
  if (status.ok()) {
    out << "GET key3634 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38641: put
  db->Put(write_options, "key3602", "value65343");

  // op 38642: put
  db->Put(write_options, "key7647", "value14537");

  // op 38643: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 38644: put
  db->Put(write_options, "key0457", "value111033");

  // op 38645: delete
  db->Delete(write_options, "key1383");

  // op 38646: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0509", "key8850", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0509, key8850) size=" << scan_result.size() << std::endl;
  }

  // op 38647: put
  db->Put(write_options, "key6376", "value92881");

  // op 38648: put
  db->Put(write_options, "key6888", "value69499");

  // op 38649: get
  status = db->Get(read_options, "key0771", &value);
  if (status.ok()) {
    out << "GET key0771 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38650: put
  db->Put(write_options, "key2938", "value3201");

  // op 38651: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2378", "key6755", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2378, key6755) size=" << scan_result.size() << std::endl;
  }

  // op 38652: put
  db->Put(write_options, "key2571", "value64472");

  // op 38653: put
  db->Put(write_options, "key2163", "value109644");

  // op 38654: delete_range
  status = db->DeleteRange(write_options, "key5284", "key10451");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 38655: put
  db->Put(write_options, "key11337", "value114455");

  // op 38656: put
  db->Put(write_options, "key10293", "value111805");

  // op 38657: put
  db->Put(write_options, "key11053", "value72113");

  // op 38658: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0296", "key6735", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0296, key6735) size=" << scan_result.size() << std::endl;
  }

  // op 38659: put
  db->Put(write_options, "key4552", "value27645");

  // op 38660: delete
  db->Delete(write_options, "key1968");

  // op 38661: delete
  db->Delete(write_options, "key5635");

  // op 38662: get
  status = db->Get(read_options, "key5911", &value);
  if (status.ok()) {
    out << "GET key5911 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38663: put
  db->Put(write_options, "key0087", "value89885");

  // op 38664: delete
  db->Delete(write_options, "key0213");

  // op 38665: put
  db->Put(write_options, "key10014", "value8181");

  // op 38666: put
  db->Put(write_options, "key7332", "value52880");

  // op 38667: delete
  db->Delete(write_options, "key3459");

  // op 38668: delete_range
  status = db->DeleteRange(write_options, "key1467", "key4735");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 38669: get
  status = db->Get(read_options, "key11698", &value);
  if (status.ok()) {
    out << "GET key11698 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38670: delete_range
  status = db->DeleteRange(write_options, "key1085", "key5206");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 38671: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2214", "key8387", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2214, key8387) size=" << scan_result.size() << std::endl;
  }

  // op 38672: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2042", "key2073", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2042, key2073) size=" << scan_result.size() << std::endl;
  }

  // op 38673: put
  db->Put(write_options, "key0073", "value63904");

  // op 38674: put
  db->Put(write_options, "key5141", "value36164");

  // op 38675: delete_range
  status = db->DeleteRange(write_options, "key4714", "key9770");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 38676: put
  db->Put(write_options, "key9867", "value115200");

  // op 38677: put
  db->Put(write_options, "key6497", "value84361");

  // op 38678: put
  db->Put(write_options, "key8690", "value2325");

  // op 38679: put
  db->Put(write_options, "key7006", "value50412");

  // op 38680: put
  db->Put(write_options, "key10824", "value38847");

  // op 38681: get
  status = db->Get(read_options, "key3420", &value);
  if (status.ok()) {
    out << "GET key3420 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38682: delete
  db->Delete(write_options, "key8980");

  // op 38683: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5205", "key5591", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5205, key5591) size=" << scan_result.size() << std::endl;
  }

  // op 38684: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0372", "key3096", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0372, key3096) size=" << scan_result.size() << std::endl;
  }

  // op 38685: put
  db->Put(write_options, "key7647", "value38660");

  // op 38686: delete
  db->Delete(write_options, "key2709");

  // op 38687: delete_range
  status = db->DeleteRange(write_options, "key7406", "key9093");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 38688: delete
  db->Delete(write_options, "key3130");

  // op 38689: delete
  db->Delete(write_options, "key7174");

  // op 38690: put
  db->Put(write_options, "key1330", "value45146");

  // op 38691: put
  db->Put(write_options, "key8138", "value109879");

  // op 38692: put
  db->Put(write_options, "key4626", "value14424");

  // op 38693: put
  db->Put(write_options, "key7022", "value101778");

  // op 38694: get
  status = db->Get(read_options, "key0716", &value);
  if (status.ok()) {
    out << "GET key0716 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38695: put
  db->Put(write_options, "key0428", "value68139");

  // op 38696: get
  status = db->Get(read_options, "key8770", &value);
  if (status.ok()) {
    out << "GET key8770 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38697: put
  db->Put(write_options, "key6429", "value13827");

  // op 38698: put
  db->Put(write_options, "key6780", "value94194");

  // op 38699: put
  db->Put(write_options, "key2242", "value33850");

  // op 38700: delete
  db->Delete(write_options, "key8734");

  // op 38701: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 38702: put
  db->Put(write_options, "key10492", "value78995");

  // op 38703: delete_range
  status = db->DeleteRange(write_options, "key7306", "key11385");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 38704: get
  status = db->Get(read_options, "key4655", &value);
  if (status.ok()) {
    out << "GET key4655 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38705: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0024", "key8292", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0024, key8292) size=" << scan_result.size() << std::endl;
  }

  // op 38706: put
  db->Put(write_options, "key0922", "value47538");

  // op 38707: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9790", "key10191", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9790, key10191) size=" << scan_result.size() << std::endl;
  }

  // op 38708: get
  status = db->Get(read_options, "key11144", &value);
  if (status.ok()) {
    out << "GET key11144 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38709: put
  db->Put(write_options, "key4034", "value115907");

  // op 38710: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3573", "key6240", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3573, key6240) size=" << scan_result.size() << std::endl;
  }

  // op 38711: get
  status = db->Get(read_options, "key11521", &value);
  if (status.ok()) {
    out << "GET key11521 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38712: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8000", "key10112", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8000, key10112) size=" << scan_result.size() << std::endl;
  }

  // op 38713: put
  db->Put(write_options, "key9244", "value99497");

  // op 38714: put
  db->Put(write_options, "key8768", "value36340");

  // op 38715: put
  db->Put(write_options, "key11458", "value51437");

  // op 38716: delete_range
  status = db->DeleteRange(write_options, "key3881", "key9400");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 38717: put
  db->Put(write_options, "key2130", "value14587");

  // op 38718: delete_range
  status = db->DeleteRange(write_options, "key2164", "key8581");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 38719: delete
  db->Delete(write_options, "key2725");

  // op 38720: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2720", "key8585", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2720, key8585) size=" << scan_result.size() << std::endl;
  }

  // op 38721: get
  status = db->Get(read_options, "key8957", &value);
  if (status.ok()) {
    out << "GET key8957 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38722: delete_range
  status = db->DeleteRange(write_options, "key7319", "key10762");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 38723: delete
  db->Delete(write_options, "key6823");

  // op 38724: put
  db->Put(write_options, "key9358", "value68865");

  // op 38725: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1783", "key2057", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1783, key2057) size=" << scan_result.size() << std::endl;
  }

  // op 38726: delete
  db->Delete(write_options, "key8848");

  // op 38727: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 38728: get
  status = db->Get(read_options, "key7283", &value);
  if (status.ok()) {
    out << "GET key7283 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38729: put
  db->Put(write_options, "key4337", "value110134");

  // op 38730: put
  db->Put(write_options, "key9246", "value30524");

  // op 38731: get
  status = db->Get(read_options, "key2913", &value);
  if (status.ok()) {
    out << "GET key2913 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38732: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8882", "key10571", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8882, key10571) size=" << scan_result.size() << std::endl;
  }

  // op 38733: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 38734: delete
  db->Delete(write_options, "key4621");

  // op 38735: get
  status = db->Get(read_options, "key5753", &value);
  if (status.ok()) {
    out << "GET key5753 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38736: put
  db->Put(write_options, "key1237", "value8724");

  // op 38737: get
  status = db->Get(read_options, "key11342", &value);
  if (status.ok()) {
    out << "GET key11342 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38738: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 38739: put
  db->Put(write_options, "key8304", "value35175");

  // op 38740: delete_range
  status = db->DeleteRange(write_options, "key1071", "key1567");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 38741: put
  db->Put(write_options, "key3181", "value66482");

  // op 38742: put
  db->Put(write_options, "key5688", "value58929");

  // op 38743: put
  db->Put(write_options, "key11026", "value71332");

  // op 38744: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 38745: put
  db->Put(write_options, "key6764", "value56381");

  // op 38746: put
  db->Put(write_options, "key8166", "value89174");

  // op 38747: put
  db->Put(write_options, "key9368", "value102612");

  // op 38748: put
  db->Put(write_options, "key0606", "value8983");

  // op 38749: put
  db->Put(write_options, "key5729", "value109870");

  // op 38750: put
  db->Put(write_options, "key10764", "value38568");

  // op 38751: put
  db->Put(write_options, "key5081", "value14278");

  // op 38752: put
  db->Put(write_options, "key0338", "value113266");

  // op 38753: delete
  db->Delete(write_options, "key10192");

  // op 38754: put
  db->Put(write_options, "key6658", "value62896");

  // op 38755: put
  db->Put(write_options, "key8788", "value70834");

  // op 38756: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1390", "key6852", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1390, key6852) size=" << scan_result.size() << std::endl;
  }

  // op 38757: delete_range
  status = db->DeleteRange(write_options, "key2655", "key5253");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 38758: get
  status = db->Get(read_options, "key10742", &value);
  if (status.ok()) {
    out << "GET key10742 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38759: put
  db->Put(write_options, "key9897", "value2768");

  // op 38760: delete_range
  status = db->DeleteRange(write_options, "key0459", "key2214");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 38761: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7252", "key8835", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7252, key8835) size=" << scan_result.size() << std::endl;
  }

  // op 38762: get
  status = db->Get(read_options, "key9555", &value);
  if (status.ok()) {
    out << "GET key9555 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38763: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0601", "key4053", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0601, key4053) size=" << scan_result.size() << std::endl;
  }

  // op 38764: delete_range
  status = db->DeleteRange(write_options, "key2688", "key5319");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 38765: delete_range
  status = db->DeleteRange(write_options, "key1907", "key9949");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 38766: put
  db->Put(write_options, "key4120", "value45349");

  // op 38767: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5920", "key7133", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5920, key7133) size=" << scan_result.size() << std::endl;
  }

  // op 38768: get
  status = db->Get(read_options, "key0571", &value);
  if (status.ok()) {
    out << "GET key0571 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38769: delete
  db->Delete(write_options, "key11007");

  // op 38770: put
  db->Put(write_options, "key3682", "value15244");

  // op 38771: put
  db->Put(write_options, "key2554", "value115992");

  // op 38772: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 38773: delete
  db->Delete(write_options, "key11327");

  // op 38774: put
  db->Put(write_options, "key10936", "value8295");

  // op 38775: delete_range
  status = db->DeleteRange(write_options, "key3154", "key8195");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 38776: delete
  db->Delete(write_options, "key2250");

  // op 38777: delete_range
  status = db->DeleteRange(write_options, "key6133", "key10402");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 38778: delete_range
  status = db->DeleteRange(write_options, "key1230", "key8182");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 38779: put
  db->Put(write_options, "key3706", "value41703");

  // op 38780: get
  status = db->Get(read_options, "key7545", &value);
  if (status.ok()) {
    out << "GET key7545 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38781: delete_range
  status = db->DeleteRange(write_options, "key11188", "key11252");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 38782: put
  db->Put(write_options, "key10555", "value77156");

  // op 38783: put
  db->Put(write_options, "key9822", "value9184");

  // op 38784: put
  db->Put(write_options, "key1680", "value112477");

  // op 38785: put
  db->Put(write_options, "key7843", "value34743");

  // op 38786: delete
  db->Delete(write_options, "key3387");

  // op 38787: delete_range
  status = db->DeleteRange(write_options, "key0552", "key5711");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 38788: put
  db->Put(write_options, "key9828", "value109894");

  // op 38789: put
  db->Put(write_options, "key11628", "value103831");

  // op 38790: get
  status = db->Get(read_options, "key10678", &value);
  if (status.ok()) {
    out << "GET key10678 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38791: put
  db->Put(write_options, "key1396", "value110748");

  // op 38792: put
  db->Put(write_options, "key9465", "value76148");

  // op 38793: get
  status = db->Get(read_options, "key3171", &value);
  if (status.ok()) {
    out << "GET key3171 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38794: put
  db->Put(write_options, "key2268", "value18537");

  // op 38795: put
  db->Put(write_options, "key0250", "value19622");

  // op 38796: put
  db->Put(write_options, "key8048", "value88900");

  // op 38797: get
  status = db->Get(read_options, "key3809", &value);
  if (status.ok()) {
    out << "GET key3809 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38798: put
  db->Put(write_options, "key0544", "value74793");

  // op 38799: put
  db->Put(write_options, "key10677", "value86212");

  // op 38800: delete_range
  status = db->DeleteRange(write_options, "key3581", "key10367");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 38801: put
  db->Put(write_options, "key1920", "value30537");

  // op 38802: put
  db->Put(write_options, "key8731", "value61653");

  // op 38803: delete
  db->Delete(write_options, "key3857");

  // op 38804: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 38805: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2728", "key5706", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2728, key5706) size=" << scan_result.size() << std::endl;
  }

  // op 38806: put
  db->Put(write_options, "key7308", "value117973");

  // op 38807: delete_range
  status = db->DeleteRange(write_options, "key6642", "key7036");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 38808: delete_range
  status = db->DeleteRange(write_options, "key3820", "key10348");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 38809: delete_range
  status = db->DeleteRange(write_options, "key0384", "key6319");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 38810: delete_range
  status = db->DeleteRange(write_options, "key1442", "key10491");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 38811: put
  db->Put(write_options, "key0045", "value104640");

  // op 38812: put
  db->Put(write_options, "key1446", "value92119");

  // op 38813: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0510", "key4237", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0510, key4237) size=" << scan_result.size() << std::endl;
  }

  // op 38814: put
  db->Put(write_options, "key6230", "value68697");

  // op 38815: get
  status = db->Get(read_options, "key7279", &value);
  if (status.ok()) {
    out << "GET key7279 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38816: get
  status = db->Get(read_options, "key4378", &value);
  if (status.ok()) {
    out << "GET key4378 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38817: delete_range
  status = db->DeleteRange(write_options, "key4945", "key9482");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 38818: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1411", "key5517", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1411, key5517) size=" << scan_result.size() << std::endl;
  }

  // op 38819: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5957", "key10892", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5957, key10892) size=" << scan_result.size() << std::endl;
  }

  // op 38820: put
  db->Put(write_options, "key11022", "value60565");

  // op 38821: delete
  db->Delete(write_options, "key0029");

  // op 38822: put
  db->Put(write_options, "key2570", "value85937");

  // op 38823: put
  db->Put(write_options, "key7576", "value76647");

  // op 38824: put
  db->Put(write_options, "key11094", "value50154");

  // op 38825: put
  db->Put(write_options, "key2719", "value58531");

  // op 38826: delete_range
  status = db->DeleteRange(write_options, "key7077", "key11449");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 38827: put
  db->Put(write_options, "key7120", "value66809");

  // op 38828: put
  db->Put(write_options, "key0071", "value110280");

  // op 38829: put
  db->Put(write_options, "key10837", "value83765");

  // op 38830: delete_range
  status = db->DeleteRange(write_options, "key2889", "key3930");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 38831: delete
  db->Delete(write_options, "key10720");

  // op 38832: put
  db->Put(write_options, "key4276", "value13027");

  // op 38833: put
  db->Put(write_options, "key1027", "value79315");

  // op 38834: get
  status = db->Get(read_options, "key1837", &value);
  if (status.ok()) {
    out << "GET key1837 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38835: put
  db->Put(write_options, "key5559", "value14111");

  // op 38836: delete
  db->Delete(write_options, "key6504");

  // op 38837: delete_range
  status = db->DeleteRange(write_options, "key4490", "key8620");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 38838: put
  db->Put(write_options, "key6000", "value39346");

  // op 38839: put
  db->Put(write_options, "key10461", "value115221");

  // op 38840: delete
  db->Delete(write_options, "key5369");

  // op 38841: put
  db->Put(write_options, "key11294", "value113393");

  // op 38842: get
  status = db->Get(read_options, "key5366", &value);
  if (status.ok()) {
    out << "GET key5366 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38843: put
  db->Put(write_options, "key2831", "value3065");

  // op 38844: put
  db->Put(write_options, "key4772", "value41110");

  // op 38845: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0276", "key11137", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0276, key11137) size=" << scan_result.size() << std::endl;
  }

  // op 38846: put
  db->Put(write_options, "key9349", "value80109");

  // op 38847: delete
  db->Delete(write_options, "key3243");

  // op 38848: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8498", "key9529", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8498, key9529) size=" << scan_result.size() << std::endl;
  }

  // op 38849: delete
  db->Delete(write_options, "key1110");

  // op 38850: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5725", "key10158", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5725, key10158) size=" << scan_result.size() << std::endl;
  }

  // op 38851: get
  status = db->Get(read_options, "key6131", &value);
  if (status.ok()) {
    out << "GET key6131 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38852: put
  db->Put(write_options, "key5018", "value112700");

  // op 38853: get
  status = db->Get(read_options, "key2812", &value);
  if (status.ok()) {
    out << "GET key2812 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38854: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1965", "key4501", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1965, key4501) size=" << scan_result.size() << std::endl;
  }

  // op 38855: delete_range
  status = db->DeleteRange(write_options, "key6044", "key7834");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 38856: put
  db->Put(write_options, "key6462", "value47460");

  // op 38857: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2229", "key7724", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2229, key7724) size=" << scan_result.size() << std::endl;
  }

  // op 38858: put
  db->Put(write_options, "key2976", "value5266");

  // op 38859: get
  status = db->Get(read_options, "key8818", &value);
  if (status.ok()) {
    out << "GET key8818 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38860: delete_range
  status = db->DeleteRange(write_options, "key2256", "key7053");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 38861: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2345", "key4130", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2345, key4130) size=" << scan_result.size() << std::endl;
  }

  // op 38862: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 38863: put
  db->Put(write_options, "key4673", "value9574");

  // op 38864: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2758", "key4185", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2758, key4185) size=" << scan_result.size() << std::endl;
  }

  // op 38865: get
  status = db->Get(read_options, "key2064", &value);
  if (status.ok()) {
    out << "GET key2064 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38866: delete
  db->Delete(write_options, "key8037");

  // op 38867: delete
  db->Delete(write_options, "key4324");

  // op 38868: put
  db->Put(write_options, "key5751", "value51611");

  // op 38869: put
  db->Put(write_options, "key1293", "value59422");

  // op 38870: put
  db->Put(write_options, "key1628", "value4630");

  // op 38871: put
  db->Put(write_options, "key0461", "value98279");

  // op 38872: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2388", "key4701", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2388, key4701) size=" << scan_result.size() << std::endl;
  }

  // op 38873: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5373", "key11493", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5373, key11493) size=" << scan_result.size() << std::endl;
  }

  // op 38874: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1947", "key3064", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1947, key3064) size=" << scan_result.size() << std::endl;
  }

  // op 38875: get
  status = db->Get(read_options, "key3419", &value);
  if (status.ok()) {
    out << "GET key3419 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38876: get
  status = db->Get(read_options, "key9548", &value);
  if (status.ok()) {
    out << "GET key9548 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38877: get
  status = db->Get(read_options, "key8446", &value);
  if (status.ok()) {
    out << "GET key8446 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38878: put
  db->Put(write_options, "key11713", "value56086");

  // op 38879: put
  db->Put(write_options, "key8591", "value81223");

  // op 38880: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2245", "key10415", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2245, key10415) size=" << scan_result.size() << std::endl;
  }

  // op 38881: delete_range
  status = db->DeleteRange(write_options, "key4937", "key6938");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 38882: get
  status = db->Get(read_options, "key9913", &value);
  if (status.ok()) {
    out << "GET key9913 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38883: delete
  db->Delete(write_options, "key9714");

  // op 38884: put
  db->Put(write_options, "key2965", "value78350");

  // op 38885: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 38886: put
  db->Put(write_options, "key5717", "value104280");

  // op 38887: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2278", "key4059", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2278, key4059) size=" << scan_result.size() << std::endl;
  }

  // op 38888: put
  db->Put(write_options, "key11461", "value98629");

  // op 38889: put
  db->Put(write_options, "key4996", "value56869");

  // op 38890: put
  db->Put(write_options, "key6726", "value22557");

  // op 38891: delete_range
  status = db->DeleteRange(write_options, "key1267", "key7570");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 38892: get
  status = db->Get(read_options, "key2355", &value);
  if (status.ok()) {
    out << "GET key2355 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38893: put
  db->Put(write_options, "key8493", "value18504");

  // op 38894: get
  status = db->Get(read_options, "key6881", &value);
  if (status.ok()) {
    out << "GET key6881 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38895: put
  db->Put(write_options, "key1854", "value102534");

  // op 38896: put
  db->Put(write_options, "key8152", "value33787");

  // op 38897: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2622", "key7481", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2622, key7481) size=" << scan_result.size() << std::endl;
  }

  // op 38898: put
  db->Put(write_options, "key5848", "value104762");

  // op 38899: put
  db->Put(write_options, "key7655", "value66053");

  // op 38900: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 38901: put
  db->Put(write_options, "key9258", "value70422");

  // op 38902: put
  db->Put(write_options, "key0434", "value74055");

  // op 38903: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10692", "key11356", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10692, key11356) size=" << scan_result.size() << std::endl;
  }

  // op 38904: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 38905: put
  db->Put(write_options, "key6431", "value98295");

  // op 38906: put
  db->Put(write_options, "key5587", "value38422");

  // op 38907: put
  db->Put(write_options, "key5867", "value79992");

  // op 38908: get
  status = db->Get(read_options, "key10827", &value);
  if (status.ok()) {
    out << "GET key10827 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38909: get
  status = db->Get(read_options, "key11329", &value);
  if (status.ok()) {
    out << "GET key11329 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38910: get
  status = db->Get(read_options, "key8989", &value);
  if (status.ok()) {
    out << "GET key8989 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38911: put
  db->Put(write_options, "key11496", "value67546");

  // op 38912: put
  db->Put(write_options, "key5591", "value103156");

  // op 38913: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7490", "key8627", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7490, key8627) size=" << scan_result.size() << std::endl;
  }

  // op 38914: put
  db->Put(write_options, "key10445", "value69153");

  // op 38915: put
  db->Put(write_options, "key1822", "value59807");

  // op 38916: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8266", "key8988", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8266, key8988) size=" << scan_result.size() << std::endl;
  }

  // op 38917: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1076", "key10513", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1076, key10513) size=" << scan_result.size() << std::endl;
  }

  // op 38918: delete_range
  status = db->DeleteRange(write_options, "key2912", "key8793");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 38919: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0735", "key2926", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0735, key2926) size=" << scan_result.size() << std::endl;
  }

  // op 38920: put
  db->Put(write_options, "key5940", "value26080");

  // op 38921: put
  db->Put(write_options, "key1417", "value62774");

  // op 38922: get
  status = db->Get(read_options, "key0546", &value);
  if (status.ok()) {
    out << "GET key0546 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38923: put
  db->Put(write_options, "key6471", "value58604");

  // op 38924: delete_range
  status = db->DeleteRange(write_options, "key0193", "key1358");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 38925: delete
  db->Delete(write_options, "key3866");

  // op 38926: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3368", "key10701", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3368, key10701) size=" << scan_result.size() << std::endl;
  }

  // op 38927: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 38928: put
  db->Put(write_options, "key10379", "value55127");

  // op 38929: put
  db->Put(write_options, "key7527", "value27869");

  // op 38930: put
  db->Put(write_options, "key10188", "value99099");

  // op 38931: put
  db->Put(write_options, "key7161", "value47943");

  // op 38932: put
  db->Put(write_options, "key4891", "value38108");

  // op 38933: put
  db->Put(write_options, "key2575", "value50101");

  // op 38934: put
  db->Put(write_options, "key11680", "value106414");

  // op 38935: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7985", "key8097", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7985, key8097) size=" << scan_result.size() << std::endl;
  }

  // op 38936: put
  db->Put(write_options, "key11021", "value11972");

  // op 38937: put
  db->Put(write_options, "key8650", "value104973");

  // op 38938: delete_range
  status = db->DeleteRange(write_options, "key5517", "key7138");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 38939: delete_range
  status = db->DeleteRange(write_options, "key3613", "key7877");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 38940: put
  db->Put(write_options, "key7972", "value37444");

  // op 38941: delete_range
  status = db->DeleteRange(write_options, "key1798", "key3221");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 38942: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0176", "key6703", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0176, key6703) size=" << scan_result.size() << std::endl;
  }

  // op 38943: put
  db->Put(write_options, "key4483", "value103274");

  // op 38944: put
  db->Put(write_options, "key10642", "value56711");

  // op 38945: get
  status = db->Get(read_options, "key9473", &value);
  if (status.ok()) {
    out << "GET key9473 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38946: get
  status = db->Get(read_options, "key4748", &value);
  if (status.ok()) {
    out << "GET key4748 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38947: delete
  db->Delete(write_options, "key0202");

  // op 38948: get
  status = db->Get(read_options, "key3104", &value);
  if (status.ok()) {
    out << "GET key3104 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38949: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5456", "key9627", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5456, key9627) size=" << scan_result.size() << std::endl;
  }

  // op 38950: delete_range
  status = db->DeleteRange(write_options, "key5280", "key11177");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 38951: delete_range
  status = db->DeleteRange(write_options, "key0015", "key6858");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 38952: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4971", "key8391", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4971, key8391) size=" << scan_result.size() << std::endl;
  }

  // op 38953: put
  db->Put(write_options, "key7252", "value22988");

  // op 38954: put
  db->Put(write_options, "key10033", "value1822");

  // op 38955: get
  status = db->Get(read_options, "key6731", &value);
  if (status.ok()) {
    out << "GET key6731 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38956: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0057", "key8512", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0057, key8512) size=" << scan_result.size() << std::endl;
  }

  // op 38957: get
  status = db->Get(read_options, "key9481", &value);
  if (status.ok()) {
    out << "GET key9481 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38958: put
  db->Put(write_options, "key10282", "value6889");

  // op 38959: put
  db->Put(write_options, "key6804", "value47104");

  // op 38960: put
  db->Put(write_options, "key5101", "value44489");

  // op 38961: put
  db->Put(write_options, "key9879", "value69546");

  // op 38962: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9077", "key11358", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9077, key11358) size=" << scan_result.size() << std::endl;
  }

  // op 38963: get
  status = db->Get(read_options, "key2659", &value);
  if (status.ok()) {
    out << "GET key2659 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38964: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0849", "key8724", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0849, key8724) size=" << scan_result.size() << std::endl;
  }

  // op 38965: put
  db->Put(write_options, "key6220", "value24601");

  // op 38966: put
  db->Put(write_options, "key11316", "value100138");

  // op 38967: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6655", "key10182", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6655, key10182) size=" << scan_result.size() << std::endl;
  }

  // op 38968: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2163", "key7780", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2163, key7780) size=" << scan_result.size() << std::endl;
  }

  // op 38969: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2261", "key10261", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2261, key10261) size=" << scan_result.size() << std::endl;
  }

  // op 38970: put
  db->Put(write_options, "key3460", "value61918");

  // op 38971: delete_range
  status = db->DeleteRange(write_options, "key0463", "key5976");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 38972: delete_range
  status = db->DeleteRange(write_options, "key1117", "key6949");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 38973: put
  db->Put(write_options, "key9009", "value112312");

  // op 38974: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1565", "key3954", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1565, key3954) size=" << scan_result.size() << std::endl;
  }

  // op 38975: put
  db->Put(write_options, "key2299", "value32816");

  // op 38976: get
  status = db->Get(read_options, "key2450", &value);
  if (status.ok()) {
    out << "GET key2450 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38977: put
  db->Put(write_options, "key5474", "value68450");

  // op 38978: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4134", "key8818", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4134, key8818) size=" << scan_result.size() << std::endl;
  }

  // op 38979: put
  db->Put(write_options, "key1256", "value53785");

  // op 38980: put
  db->Put(write_options, "key10435", "value71928");

  // op 38981: put
  db->Put(write_options, "key6951", "value118098");

  // op 38982: delete
  db->Delete(write_options, "key3195");

  // op 38983: put
  db->Put(write_options, "key1031", "value82482");

  // op 38984: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 38985: delete_range
  status = db->DeleteRange(write_options, "key1240", "key8048");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 38986: put
  db->Put(write_options, "key0155", "value110195");

  // op 38987: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6700", "key11175", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6700, key11175) size=" << scan_result.size() << std::endl;
  }

  // op 38988: put
  db->Put(write_options, "key3288", "value55118");

  // op 38989: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1032", "key9399", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1032, key9399) size=" << scan_result.size() << std::endl;
  }

  // op 38990: put
  db->Put(write_options, "key10098", "value101908");

  // op 38991: get
  status = db->Get(read_options, "key7776", &value);
  if (status.ok()) {
    out << "GET key7776 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38992: put
  db->Put(write_options, "key1275", "value73856");

  // op 38993: put
  db->Put(write_options, "key10422", "value40013");

  // op 38994: get
  status = db->Get(read_options, "key8878", &value);
  if (status.ok()) {
    out << "GET key8878 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38995: get
  status = db->Get(read_options, "key10762", &value);
  if (status.ok()) {
    out << "GET key10762 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38996: get
  status = db->Get(read_options, "key7408", &value);
  if (status.ok()) {
    out << "GET key7408 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 38997: delete_range
  status = db->DeleteRange(write_options, "key7780", "key9309");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 38998: put
  db->Put(write_options, "key11270", "value50298");

  // op 38999: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 39000: delete
  db->Delete(write_options, "key5920");

  // op 39001: delete_range
  status = db->DeleteRange(write_options, "key0209", "key11268");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39002: delete
  db->Delete(write_options, "key2454");

  // op 39003: delete
  db->Delete(write_options, "key10585");

  // op 39004: delete
  db->Delete(write_options, "key4025");

  // op 39005: get
  status = db->Get(read_options, "key9887", &value);
  if (status.ok()) {
    out << "GET key9887 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39006: put
  db->Put(write_options, "key1340", "value20293");

  // op 39007: get
  status = db->Get(read_options, "key3119", &value);
  if (status.ok()) {
    out << "GET key3119 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39008: put
  db->Put(write_options, "key6232", "value20617");

  // op 39009: put
  db->Put(write_options, "key8733", "value73707");

  // op 39010: put
  db->Put(write_options, "key8874", "value22636");

  // op 39011: get
  status = db->Get(read_options, "key4432", &value);
  if (status.ok()) {
    out << "GET key4432 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39012: put
  db->Put(write_options, "key1856", "value0170");

  // op 39013: get
  status = db->Get(read_options, "key5202", &value);
  if (status.ok()) {
    out << "GET key5202 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39014: put
  db->Put(write_options, "key6252", "value97890");

  // op 39015: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6282", "key6401", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6282, key6401) size=" << scan_result.size() << std::endl;
  }

  // op 39016: get
  status = db->Get(read_options, "key0633", &value);
  if (status.ok()) {
    out << "GET key0633 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39017: get
  status = db->Get(read_options, "key0223", &value);
  if (status.ok()) {
    out << "GET key0223 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39018: put
  db->Put(write_options, "key2826", "value71451");

  // op 39019: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1801", "key6874", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1801, key6874) size=" << scan_result.size() << std::endl;
  }

  // op 39020: get
  status = db->Get(read_options, "key8734", &value);
  if (status.ok()) {
    out << "GET key8734 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39021: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5438", "key5472", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5438, key5472) size=" << scan_result.size() << std::endl;
  }

  // op 39022: get
  status = db->Get(read_options, "key4114", &value);
  if (status.ok()) {
    out << "GET key4114 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39023: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3497", "key6693", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3497, key6693) size=" << scan_result.size() << std::endl;
  }

  // op 39024: get
  status = db->Get(read_options, "key1120", &value);
  if (status.ok()) {
    out << "GET key1120 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39025: put
  db->Put(write_options, "key10937", "value85031");

  // op 39026: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10052", "key10907", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10052, key10907) size=" << scan_result.size() << std::endl;
  }

  // op 39027: delete_range
  status = db->DeleteRange(write_options, "key10750", "key11434");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39028: put
  db->Put(write_options, "key0654", "value55273");

  // op 39029: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6961", "key8472", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6961, key8472) size=" << scan_result.size() << std::endl;
  }

  // op 39030: delete_range
  status = db->DeleteRange(write_options, "key5722", "key9180");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39031: put
  db->Put(write_options, "key5766", "value93729");

  // op 39032: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9285", "key10800", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9285, key10800) size=" << scan_result.size() << std::endl;
  }

  // op 39033: get
  status = db->Get(read_options, "key0342", &value);
  if (status.ok()) {
    out << "GET key0342 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39034: get
  status = db->Get(read_options, "key6829", &value);
  if (status.ok()) {
    out << "GET key6829 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39035: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 39036: get
  status = db->Get(read_options, "key8596", &value);
  if (status.ok()) {
    out << "GET key8596 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39037: put
  db->Put(write_options, "key6987", "value44969");

  // op 39038: delete_range
  status = db->DeleteRange(write_options, "key6388", "key6748");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39039: get
  status = db->Get(read_options, "key9789", &value);
  if (status.ok()) {
    out << "GET key9789 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39040: delete_range
  status = db->DeleteRange(write_options, "key8738", "key10022");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39041: delete_range
  status = db->DeleteRange(write_options, "key3437", "key9014");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39042: put
  db->Put(write_options, "key10815", "value49462");

  // op 39043: delete
  db->Delete(write_options, "key1961");

  // op 39044: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1005", "key4677", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1005, key4677) size=" << scan_result.size() << std::endl;
  }

  // op 39045: delete_range
  status = db->DeleteRange(write_options, "key7257", "key10722");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39046: get
  status = db->Get(read_options, "key9280", &value);
  if (status.ok()) {
    out << "GET key9280 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39047: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3005", "key7326", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3005, key7326) size=" << scan_result.size() << std::endl;
  }

  // op 39048: put
  db->Put(write_options, "key0076", "value60367");

  // op 39049: get
  status = db->Get(read_options, "key2883", &value);
  if (status.ok()) {
    out << "GET key2883 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39050: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 39051: put
  db->Put(write_options, "key4706", "value37027");

  // op 39052: get
  status = db->Get(read_options, "key8075", &value);
  if (status.ok()) {
    out << "GET key8075 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39053: put
  db->Put(write_options, "key0897", "value20937");

  // op 39054: delete
  db->Delete(write_options, "key4231");

  // op 39055: put
  db->Put(write_options, "key1563", "value18577");

  // op 39056: delete_range
  status = db->DeleteRange(write_options, "key1272", "key10948");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39057: delete
  db->Delete(write_options, "key2098");

  // op 39058: get
  status = db->Get(read_options, "key8980", &value);
  if (status.ok()) {
    out << "GET key8980 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39059: get
  status = db->Get(read_options, "key4119", &value);
  if (status.ok()) {
    out << "GET key4119 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39060: get
  status = db->Get(read_options, "key3336", &value);
  if (status.ok()) {
    out << "GET key3336 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39061: put
  db->Put(write_options, "key10589", "value97733");

  // op 39062: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 39063: delete
  db->Delete(write_options, "key6519");

  // op 39064: get
  status = db->Get(read_options, "key3168", &value);
  if (status.ok()) {
    out << "GET key3168 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39065: put
  db->Put(write_options, "key10409", "value87356");

  // op 39066: get
  status = db->Get(read_options, "key6614", &value);
  if (status.ok()) {
    out << "GET key6614 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39067: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7568", "key8075", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7568, key8075) size=" << scan_result.size() << std::endl;
  }

  // op 39068: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6358", "key10161", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6358, key10161) size=" << scan_result.size() << std::endl;
  }

  // op 39069: get
  status = db->Get(read_options, "key6493", &value);
  if (status.ok()) {
    out << "GET key6493 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39070: delete
  db->Delete(write_options, "key0927");

  // op 39071: put
  db->Put(write_options, "key3757", "value74595");

  // op 39072: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 39073: put
  db->Put(write_options, "key10288", "value67387");

  // op 39074: get
  status = db->Get(read_options, "key2403", &value);
  if (status.ok()) {
    out << "GET key2403 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39075: put
  db->Put(write_options, "key9384", "value5437");

  // op 39076: get
  status = db->Get(read_options, "key8002", &value);
  if (status.ok()) {
    out << "GET key8002 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39077: put
  db->Put(write_options, "key3783", "value116754");

  // op 39078: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4914", "key7641", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4914, key7641) size=" << scan_result.size() << std::endl;
  }

  // op 39079: delete
  db->Delete(write_options, "key4963");

  // op 39080: put
  db->Put(write_options, "key1959", "value22631");

  // op 39081: get
  status = db->Get(read_options, "key1588", &value);
  if (status.ok()) {
    out << "GET key1588 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39082: put
  db->Put(write_options, "key9429", "value56117");

  // op 39083: put
  db->Put(write_options, "key11147", "value93211");

  // op 39084: get
  status = db->Get(read_options, "key3949", &value);
  if (status.ok()) {
    out << "GET key3949 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39085: delete_range
  status = db->DeleteRange(write_options, "key9965", "key11175");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39086: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 39087: put
  db->Put(write_options, "key5443", "value117615");

  // op 39088: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 39089: get
  status = db->Get(read_options, "key4869", &value);
  if (status.ok()) {
    out << "GET key4869 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39090: get
  status = db->Get(read_options, "key2022", &value);
  if (status.ok()) {
    out << "GET key2022 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39091: put
  db->Put(write_options, "key11350", "value108490");

  // op 39092: put
  db->Put(write_options, "key5232", "value46879");

  // op 39093: delete_range
  status = db->DeleteRange(write_options, "key6057", "key8860");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39094: get
  status = db->Get(read_options, "key11197", &value);
  if (status.ok()) {
    out << "GET key11197 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39095: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 39096: get
  status = db->Get(read_options, "key4021", &value);
  if (status.ok()) {
    out << "GET key4021 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39097: put
  db->Put(write_options, "key6518", "value19336");

  // op 39098: put
  db->Put(write_options, "key1697", "value94843");

  // op 39099: put
  db->Put(write_options, "key2085", "value51082");

  // op 39100: get
  status = db->Get(read_options, "key2383", &value);
  if (status.ok()) {
    out << "GET key2383 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39101: get
  status = db->Get(read_options, "key9954", &value);
  if (status.ok()) {
    out << "GET key9954 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39102: put
  db->Put(write_options, "key2083", "value111070");

  // op 39103: put
  db->Put(write_options, "key3019", "value10556");

  // op 39104: put
  db->Put(write_options, "key4293", "value56698");

  // op 39105: delete
  db->Delete(write_options, "key1213");

  // op 39106: get
  status = db->Get(read_options, "key1627", &value);
  if (status.ok()) {
    out << "GET key1627 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39107: get
  status = db->Get(read_options, "key1155", &value);
  if (status.ok()) {
    out << "GET key1155 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39108: put
  db->Put(write_options, "key10894", "value103937");

  // op 39109: get
  status = db->Get(read_options, "key6601", &value);
  if (status.ok()) {
    out << "GET key6601 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39110: delete
  db->Delete(write_options, "key11167");

  // op 39111: put
  db->Put(write_options, "key4798", "value4477");

  // op 39112: delete_range
  status = db->DeleteRange(write_options, "key1294", "key9856");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39113: delete_range
  status = db->DeleteRange(write_options, "key2069", "key11397");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39114: get
  status = db->Get(read_options, "key7760", &value);
  if (status.ok()) {
    out << "GET key7760 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39115: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0756", "key4315", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0756, key4315) size=" << scan_result.size() << std::endl;
  }

  // op 39116: get
  status = db->Get(read_options, "key8658", &value);
  if (status.ok()) {
    out << "GET key8658 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39117: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3763", "key4925", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3763, key4925) size=" << scan_result.size() << std::endl;
  }

  // op 39118: put
  db->Put(write_options, "key3933", "value35317");

  // op 39119: delete
  db->Delete(write_options, "key5352");

  // op 39120: put
  db->Put(write_options, "key5835", "value16955");

  // op 39121: put
  db->Put(write_options, "key11170", "value86485");

  // op 39122: get
  status = db->Get(read_options, "key8662", &value);
  if (status.ok()) {
    out << "GET key8662 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39123: delete_range
  status = db->DeleteRange(write_options, "key3694", "key7959");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39124: get
  status = db->Get(read_options, "key5802", &value);
  if (status.ok()) {
    out << "GET key5802 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39125: get
  status = db->Get(read_options, "key8050", &value);
  if (status.ok()) {
    out << "GET key8050 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39126: put
  db->Put(write_options, "key2570", "value105621");

  // op 39127: put
  db->Put(write_options, "key10906", "value45168");

  // op 39128: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8226", "key9619", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8226, key9619) size=" << scan_result.size() << std::endl;
  }

  // op 39129: get
  status = db->Get(read_options, "key11698", &value);
  if (status.ok()) {
    out << "GET key11698 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39130: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3784", "key5578", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3784, key5578) size=" << scan_result.size() << std::endl;
  }

  // op 39131: delete_range
  status = db->DeleteRange(write_options, "key2290", "key8297");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39132: put
  db->Put(write_options, "key9681", "value77135");

  // op 39133: delete_range
  status = db->DeleteRange(write_options, "key4243", "key6008");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39134: put
  db->Put(write_options, "key5740", "value113817");

  // op 39135: put
  db->Put(write_options, "key2348", "value27490");

  // op 39136: put
  db->Put(write_options, "key6984", "value15455");

  // op 39137: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3690", "key7557", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3690, key7557) size=" << scan_result.size() << std::endl;
  }

  // op 39138: get
  status = db->Get(read_options, "key5385", &value);
  if (status.ok()) {
    out << "GET key5385 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39139: delete
  db->Delete(write_options, "key0773");

  // op 39140: get
  status = db->Get(read_options, "key11479", &value);
  if (status.ok()) {
    out << "GET key11479 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39141: get
  status = db->Get(read_options, "key7171", &value);
  if (status.ok()) {
    out << "GET key7171 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39142: put
  db->Put(write_options, "key10764", "value77882");

  // op 39143: put
  db->Put(write_options, "key7064", "value30698");

  // op 39144: get
  status = db->Get(read_options, "key10991", &value);
  if (status.ok()) {
    out << "GET key10991 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39145: put
  db->Put(write_options, "key1549", "value11546");

  // op 39146: delete_range
  status = db->DeleteRange(write_options, "key3299", "key4637");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39147: put
  db->Put(write_options, "key7103", "value12910");

  // op 39148: get
  status = db->Get(read_options, "key2363", &value);
  if (status.ok()) {
    out << "GET key2363 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39149: put
  db->Put(write_options, "key1320", "value16123");

  // op 39150: get
  status = db->Get(read_options, "key5170", &value);
  if (status.ok()) {
    out << "GET key5170 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39151: put
  db->Put(write_options, "key7132", "value39287");

  // op 39152: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 39153: delete
  db->Delete(write_options, "key8163");

  // op 39154: get
  status = db->Get(read_options, "key1646", &value);
  if (status.ok()) {
    out << "GET key1646 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39155: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5551", "key10189", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5551, key10189) size=" << scan_result.size() << std::endl;
  }

  // op 39156: get
  status = db->Get(read_options, "key10848", &value);
  if (status.ok()) {
    out << "GET key10848 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39157: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6840", "key8630", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6840, key8630) size=" << scan_result.size() << std::endl;
  }

  // op 39158: delete
  db->Delete(write_options, "key10790");

  // op 39159: put
  db->Put(write_options, "key6873", "value10263");

  // op 39160: delete_range
  status = db->DeleteRange(write_options, "key0956", "key5579");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39161: put
  db->Put(write_options, "key7667", "value80059");

  // op 39162: put
  db->Put(write_options, "key3176", "value73727");

  // op 39163: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4500", "key5977", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4500, key5977) size=" << scan_result.size() << std::endl;
  }

  // op 39164: put
  db->Put(write_options, "key7300", "value84226");

  // op 39165: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9749", "key10970", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9749, key10970) size=" << scan_result.size() << std::endl;
  }

  // op 39166: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1269", "key3182", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1269, key3182) size=" << scan_result.size() << std::endl;
  }

  // op 39167: get
  status = db->Get(read_options, "key6673", &value);
  if (status.ok()) {
    out << "GET key6673 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39168: put
  db->Put(write_options, "key4617", "value52707");

  // op 39169: get
  status = db->Get(read_options, "key6071", &value);
  if (status.ok()) {
    out << "GET key6071 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39170: put
  db->Put(write_options, "key3812", "value87051");

  // op 39171: put
  db->Put(write_options, "key9446", "value50674");

  // op 39172: get
  status = db->Get(read_options, "key4997", &value);
  if (status.ok()) {
    out << "GET key4997 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39173: get
  status = db->Get(read_options, "key10322", &value);
  if (status.ok()) {
    out << "GET key10322 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39174: delete_range
  status = db->DeleteRange(write_options, "key1265", "key4225");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39175: put
  db->Put(write_options, "key11379", "value21469");

  // op 39176: get
  status = db->Get(read_options, "key8073", &value);
  if (status.ok()) {
    out << "GET key8073 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39177: delete
  db->Delete(write_options, "key5337");

  // op 39178: delete
  db->Delete(write_options, "key5531");

  // op 39179: get
  status = db->Get(read_options, "key2297", &value);
  if (status.ok()) {
    out << "GET key2297 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39180: put
  db->Put(write_options, "key4626", "value116521");

  // op 39181: get
  status = db->Get(read_options, "key11103", &value);
  if (status.ok()) {
    out << "GET key11103 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39182: get
  status = db->Get(read_options, "key8278", &value);
  if (status.ok()) {
    out << "GET key8278 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39183: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4366", "key10935", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4366, key10935) size=" << scan_result.size() << std::endl;
  }

  // op 39184: put
  db->Put(write_options, "key11222", "value20397");

  // op 39185: put
  db->Put(write_options, "key9939", "value70670");

  // op 39186: put
  db->Put(write_options, "key4193", "value76337");

  // op 39187: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8987", "key11088", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8987, key11088) size=" << scan_result.size() << std::endl;
  }

  // op 39188: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8116", "key8467", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8116, key8467) size=" << scan_result.size() << std::endl;
  }

  // op 39189: put
  db->Put(write_options, "key3435", "value13321");

  // op 39190: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2113", "key6821", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2113, key6821) size=" << scan_result.size() << std::endl;
  }

  // op 39191: put
  db->Put(write_options, "key0878", "value15090");

  // op 39192: put
  db->Put(write_options, "key7970", "value51415");

  // op 39193: delete_range
  status = db->DeleteRange(write_options, "key4728", "key10399");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39194: delete
  db->Delete(write_options, "key6158");

  // op 39195: put
  db->Put(write_options, "key5967", "value63790");

  // op 39196: delete_range
  status = db->DeleteRange(write_options, "key2146", "key10725");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39197: delete
  db->Delete(write_options, "key4028");

  // op 39198: get
  status = db->Get(read_options, "key9792", &value);
  if (status.ok()) {
    out << "GET key9792 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39199: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4345", "key6588", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4345, key6588) size=" << scan_result.size() << std::endl;
  }

  // op 39200: get
  status = db->Get(read_options, "key5143", &value);
  if (status.ok()) {
    out << "GET key5143 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39201: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3067", "key7856", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3067, key7856) size=" << scan_result.size() << std::endl;
  }

  // op 39202: put
  db->Put(write_options, "key7018", "value86754");

  // op 39203: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7372", "key8079", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7372, key8079) size=" << scan_result.size() << std::endl;
  }

  // op 39204: put
  db->Put(write_options, "key0718", "value56103");

  // op 39205: put
  db->Put(write_options, "key8526", "value27236");

  // op 39206: delete
  db->Delete(write_options, "key5616");

  // op 39207: delete
  db->Delete(write_options, "key10712");

  // op 39208: put
  db->Put(write_options, "key5337", "value70934");

  // op 39209: delete
  db->Delete(write_options, "key11187");

  // op 39210: delete
  db->Delete(write_options, "key11288");

  // op 39211: get
  status = db->Get(read_options, "key5489", &value);
  if (status.ok()) {
    out << "GET key5489 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39212: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0976", "key10997", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0976, key10997) size=" << scan_result.size() << std::endl;
  }

  // op 39213: delete_range
  status = db->DeleteRange(write_options, "key3481", "key10443");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39214: put
  db->Put(write_options, "key9930", "value59942");

  // op 39215: get
  status = db->Get(read_options, "key7764", &value);
  if (status.ok()) {
    out << "GET key7764 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39216: get
  status = db->Get(read_options, "key4387", &value);
  if (status.ok()) {
    out << "GET key4387 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39217: put
  db->Put(write_options, "key6223", "value43886");

  // op 39218: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 39219: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1385", "key10559", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1385, key10559) size=" << scan_result.size() << std::endl;
  }

  // op 39220: delete_range
  status = db->DeleteRange(write_options, "key4337", "key10421");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39221: put
  db->Put(write_options, "key8938", "value34209");

  // op 39222: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3681", "key9610", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3681, key9610) size=" << scan_result.size() << std::endl;
  }

  // op 39223: delete_range
  status = db->DeleteRange(write_options, "key5011", "key7102");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39224: delete
  db->Delete(write_options, "key0809");

  // op 39225: get
  status = db->Get(read_options, "key6201", &value);
  if (status.ok()) {
    out << "GET key6201 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39226: delete
  db->Delete(write_options, "key7025");

  // op 39227: put
  db->Put(write_options, "key10653", "value48699");

  // op 39228: delete_range
  status = db->DeleteRange(write_options, "key2339", "key10793");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39229: get
  status = db->Get(read_options, "key3087", &value);
  if (status.ok()) {
    out << "GET key3087 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39230: put
  db->Put(write_options, "key1388", "value105914");

  // op 39231: get
  status = db->Get(read_options, "key11424", &value);
  if (status.ok()) {
    out << "GET key11424 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39232: put
  db->Put(write_options, "key11317", "value52511");

  // op 39233: get
  status = db->Get(read_options, "key6312", &value);
  if (status.ok()) {
    out << "GET key6312 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39234: delete_range
  status = db->DeleteRange(write_options, "key7137", "key8045");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39235: put
  db->Put(write_options, "key1820", "value27316");

  // op 39236: put
  db->Put(write_options, "key10665", "value4555");

  // op 39237: put
  db->Put(write_options, "key6427", "value67735");

  // op 39238: put
  db->Put(write_options, "key0862", "value30425");

  // op 39239: get
  status = db->Get(read_options, "key5070", &value);
  if (status.ok()) {
    out << "GET key5070 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39240: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 39241: put
  db->Put(write_options, "key7228", "value55449");

  // op 39242: delete_range
  status = db->DeleteRange(write_options, "key2183", "key7546");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39243: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0355", "key5134", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0355, key5134) size=" << scan_result.size() << std::endl;
  }

  // op 39244: put
  db->Put(write_options, "key4092", "value61617");

  // op 39245: delete
  db->Delete(write_options, "key3581");

  // op 39246: get
  status = db->Get(read_options, "key8302", &value);
  if (status.ok()) {
    out << "GET key8302 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39247: delete_range
  status = db->DeleteRange(write_options, "key1201", "key8815");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39248: put
  db->Put(write_options, "key5736", "value51290");

  // op 39249: put
  db->Put(write_options, "key2779", "value29930");

  // op 39250: put
  db->Put(write_options, "key2192", "value106595");

  // op 39251: get
  status = db->Get(read_options, "key9731", &value);
  if (status.ok()) {
    out << "GET key9731 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39252: delete_range
  status = db->DeleteRange(write_options, "key4266", "key5115");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39253: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5991", "key11668", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5991, key11668) size=" << scan_result.size() << std::endl;
  }

  // op 39254: delete
  db->Delete(write_options, "key10880");

  // op 39255: get
  status = db->Get(read_options, "key3071", &value);
  if (status.ok()) {
    out << "GET key3071 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39256: delete_range
  status = db->DeleteRange(write_options, "key0002", "key11709");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39257: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1706", "key6091", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1706, key6091) size=" << scan_result.size() << std::endl;
  }

  // op 39258: put
  db->Put(write_options, "key6516", "value84694");

  // op 39259: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1066", "key9671", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1066, key9671) size=" << scan_result.size() << std::endl;
  }

  // op 39260: delete_range
  status = db->DeleteRange(write_options, "key4137", "key10704");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39261: put
  db->Put(write_options, "key8580", "value57204");

  // op 39262: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0773", "key7844", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0773, key7844) size=" << scan_result.size() << std::endl;
  }

  // op 39263: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 39264: delete_range
  status = db->DeleteRange(write_options, "key7288", "key8823");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39265: put
  db->Put(write_options, "key9090", "value51239");

  // op 39266: put
  db->Put(write_options, "key6577", "value98761");

  // op 39267: delete
  db->Delete(write_options, "key1636");

  // op 39268: put
  db->Put(write_options, "key3881", "value88820");

  // op 39269: put
  db->Put(write_options, "key8671", "value10475");

  // op 39270: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2554", "key9098", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2554, key9098) size=" << scan_result.size() << std::endl;
  }

  // op 39271: put
  db->Put(write_options, "key1341", "value28646");

  // op 39272: put
  db->Put(write_options, "key1758", "value91241");

  // op 39273: put
  db->Put(write_options, "key6581", "value15394");

  // op 39274: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5354", "key6456", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5354, key6456) size=" << scan_result.size() << std::endl;
  }

  // op 39275: put
  db->Put(write_options, "key3738", "value52425");

  // op 39276: get
  status = db->Get(read_options, "key4583", &value);
  if (status.ok()) {
    out << "GET key4583 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39277: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8110", "key11724", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8110, key11724) size=" << scan_result.size() << std::endl;
  }

  // op 39278: get
  status = db->Get(read_options, "key5808", &value);
  if (status.ok()) {
    out << "GET key5808 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39279: delete
  db->Delete(write_options, "key3928");

  // op 39280: put
  db->Put(write_options, "key4751", "value15843");

  // op 39281: put
  db->Put(write_options, "key4024", "value101960");

  // op 39282: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10411", "key10823", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10411, key10823) size=" << scan_result.size() << std::endl;
  }

  // op 39283: put
  db->Put(write_options, "key10313", "value61569");

  // op 39284: put
  db->Put(write_options, "key2591", "value0162");

  // op 39285: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 39286: put
  db->Put(write_options, "key11666", "value105772");

  // op 39287: put
  db->Put(write_options, "key8516", "value100337");

  // op 39288: put
  db->Put(write_options, "key0090", "value85365");

  // op 39289: put
  db->Put(write_options, "key4937", "value60783");

  // op 39290: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2824", "key10336", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2824, key10336) size=" << scan_result.size() << std::endl;
  }

  // op 39291: get
  status = db->Get(read_options, "key8067", &value);
  if (status.ok()) {
    out << "GET key8067 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39292: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4534", "key9673", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4534, key9673) size=" << scan_result.size() << std::endl;
  }

  // op 39293: get
  status = db->Get(read_options, "key0266", &value);
  if (status.ok()) {
    out << "GET key0266 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39294: put
  db->Put(write_options, "key0295", "value2820");

  // op 39295: delete_range
  status = db->DeleteRange(write_options, "key3849", "key9372");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39296: delete_range
  status = db->DeleteRange(write_options, "key4903", "key10697");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39297: put
  db->Put(write_options, "key7439", "value4726");

  // op 39298: put
  db->Put(write_options, "key6822", "value46936");

  // op 39299: put
  db->Put(write_options, "key0943", "value67284");

  // op 39300: delete_range
  status = db->DeleteRange(write_options, "key1097", "key6262");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39301: get
  status = db->Get(read_options, "key0823", &value);
  if (status.ok()) {
    out << "GET key0823 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39302: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0363", "key0906", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0363, key0906) size=" << scan_result.size() << std::endl;
  }

  // op 39303: delete
  db->Delete(write_options, "key4689");

  // op 39304: put
  db->Put(write_options, "key8670", "value26254");

  // op 39305: delete
  db->Delete(write_options, "key0879");

  // op 39306: get
  status = db->Get(read_options, "key1033", &value);
  if (status.ok()) {
    out << "GET key1033 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39307: put
  db->Put(write_options, "key1525", "value7384");

  // op 39308: put
  db->Put(write_options, "key11252", "value23819");

  // op 39309: put
  db->Put(write_options, "key1951", "value96968");

  // op 39310: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 39311: put
  db->Put(write_options, "key2370", "value57155");

  // op 39312: put
  db->Put(write_options, "key8357", "value73829");

  // op 39313: put
  db->Put(write_options, "key3818", "value115302");

  // op 39314: put
  db->Put(write_options, "key8536", "value46208");

  // op 39315: put
  db->Put(write_options, "key1022", "value40737");

  // op 39316: put
  db->Put(write_options, "key4255", "value56430");

  // op 39317: put
  db->Put(write_options, "key1649", "value7248");

  // op 39318: delete_range
  status = db->DeleteRange(write_options, "key2936", "key6295");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39319: put
  db->Put(write_options, "key10359", "value83406");

  // op 39320: put
  db->Put(write_options, "key6329", "value62180");

  // op 39321: get
  status = db->Get(read_options, "key9062", &value);
  if (status.ok()) {
    out << "GET key9062 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39322: put
  db->Put(write_options, "key1321", "value78627");

  // op 39323: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 39324: put
  db->Put(write_options, "key1136", "value23796");

  // op 39325: put
  db->Put(write_options, "key9596", "value45701");

  // op 39326: delete_range
  status = db->DeleteRange(write_options, "key6410", "key10510");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39327: delete_range
  status = db->DeleteRange(write_options, "key10258", "key11085");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39328: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7003", "key9729", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7003, key9729) size=" << scan_result.size() << std::endl;
  }

  // op 39329: put
  db->Put(write_options, "key2434", "value11865");

  // op 39330: put
  db->Put(write_options, "key5055", "value110911");

  // op 39331: put
  db->Put(write_options, "key5267", "value25271");

  // op 39332: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 39333: delete_range
  status = db->DeleteRange(write_options, "key2067", "key3883");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39334: put
  db->Put(write_options, "key8886", "value117616");

  // op 39335: delete
  db->Delete(write_options, "key2338");

  // op 39336: put
  db->Put(write_options, "key5059", "value45530");

  // op 39337: get
  status = db->Get(read_options, "key10306", &value);
  if (status.ok()) {
    out << "GET key10306 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39338: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0740", "key10574", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0740, key10574) size=" << scan_result.size() << std::endl;
  }

  // op 39339: delete_range
  status = db->DeleteRange(write_options, "key3109", "key4393");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39340: put
  db->Put(write_options, "key0819", "value59297");

  // op 39341: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10429", "key10889", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10429, key10889) size=" << scan_result.size() << std::endl;
  }

  // op 39342: get
  status = db->Get(read_options, "key2833", &value);
  if (status.ok()) {
    out << "GET key2833 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39343: put
  db->Put(write_options, "key1876", "value60166");

  // op 39344: get
  status = db->Get(read_options, "key10078", &value);
  if (status.ok()) {
    out << "GET key10078 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39345: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0642", "key5727", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0642, key5727) size=" << scan_result.size() << std::endl;
  }

  // op 39346: put
  db->Put(write_options, "key3424", "value41576");

  // op 39347: get
  status = db->Get(read_options, "key7764", &value);
  if (status.ok()) {
    out << "GET key7764 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39348: get
  status = db->Get(read_options, "key6307", &value);
  if (status.ok()) {
    out << "GET key6307 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39349: delete_range
  status = db->DeleteRange(write_options, "key8825", "key9122");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39350: put
  db->Put(write_options, "key10866", "value57546");

  // op 39351: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1613", "key7044", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1613, key7044) size=" << scan_result.size() << std::endl;
  }

  // op 39352: get
  status = db->Get(read_options, "key4150", &value);
  if (status.ok()) {
    out << "GET key4150 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39353: get
  status = db->Get(read_options, "key9193", &value);
  if (status.ok()) {
    out << "GET key9193 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39354: delete_range
  status = db->DeleteRange(write_options, "key5793", "key7547");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39355: delete_range
  status = db->DeleteRange(write_options, "key3405", "key9080");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39356: get
  status = db->Get(read_options, "key4956", &value);
  if (status.ok()) {
    out << "GET key4956 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39357: get
  status = db->Get(read_options, "key5246", &value);
  if (status.ok()) {
    out << "GET key5246 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39358: put
  db->Put(write_options, "key3599", "value17539");

  // op 39359: delete
  db->Delete(write_options, "key8991");

  // op 39360: put
  db->Put(write_options, "key9607", "value79311");

  // op 39361: delete
  db->Delete(write_options, "key7249");

  // op 39362: delete
  db->Delete(write_options, "key5132");

  // op 39363: put
  db->Put(write_options, "key4775", "value4840");

  // op 39364: put
  db->Put(write_options, "key6746", "value70744");

  // op 39365: delete
  db->Delete(write_options, "key9899");

  // op 39366: get
  status = db->Get(read_options, "key4734", &value);
  if (status.ok()) {
    out << "GET key4734 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39367: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2266", "key5532", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2266, key5532) size=" << scan_result.size() << std::endl;
  }

  // op 39368: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 39369: put
  db->Put(write_options, "key6045", "value5522");

  // op 39370: put
  db->Put(write_options, "key7434", "value67052");

  // op 39371: delete_range
  status = db->DeleteRange(write_options, "key7292", "key8730");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39372: put
  db->Put(write_options, "key1173", "value5627");

  // op 39373: delete
  db->Delete(write_options, "key7940");

  // op 39374: put
  db->Put(write_options, "key4416", "value111158");

  // op 39375: delete
  db->Delete(write_options, "key5621");

  // op 39376: delete_range
  status = db->DeleteRange(write_options, "key5314", "key9961");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39377: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1565", "key7659", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1565, key7659) size=" << scan_result.size() << std::endl;
  }

  // op 39378: put
  db->Put(write_options, "key2448", "value67892");

  // op 39379: delete
  db->Delete(write_options, "key8286");

  // op 39380: put
  db->Put(write_options, "key1613", "value4268");

  // op 39381: put
  db->Put(write_options, "key8494", "value41985");

  // op 39382: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1155", "key4497", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1155, key4497) size=" << scan_result.size() << std::endl;
  }

  // op 39383: put
  db->Put(write_options, "key10834", "value98195");

  // op 39384: delete
  db->Delete(write_options, "key2949");

  // op 39385: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 39386: put
  db->Put(write_options, "key0616", "value110524");

  // op 39387: put
  db->Put(write_options, "key5373", "value54867");

  // op 39388: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1547", "key5838", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1547, key5838) size=" << scan_result.size() << std::endl;
  }

  // op 39389: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6872", "key11306", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6872, key11306) size=" << scan_result.size() << std::endl;
  }

  // op 39390: put
  db->Put(write_options, "key0791", "value19091");

  // op 39391: get
  status = db->Get(read_options, "key3497", &value);
  if (status.ok()) {
    out << "GET key3497 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39392: get
  status = db->Get(read_options, "key4580", &value);
  if (status.ok()) {
    out << "GET key4580 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39393: put
  db->Put(write_options, "key3547", "value57983");

  // op 39394: put
  db->Put(write_options, "key2719", "value68384");

  // op 39395: get
  status = db->Get(read_options, "key1761", &value);
  if (status.ok()) {
    out << "GET key1761 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39396: delete
  db->Delete(write_options, "key5820");

  // op 39397: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 39398: delete_range
  status = db->DeleteRange(write_options, "key7624", "key10910");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39399: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 39400: delete
  db->Delete(write_options, "key9919");

  // op 39401: put
  db->Put(write_options, "key0347", "value107539");

  // op 39402: delete_range
  status = db->DeleteRange(write_options, "key1126", "key10478");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39403: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 39404: get
  status = db->Get(read_options, "key9178", &value);
  if (status.ok()) {
    out << "GET key9178 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39405: get
  status = db->Get(read_options, "key8534", &value);
  if (status.ok()) {
    out << "GET key8534 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39406: put
  db->Put(write_options, "key7497", "value47883");

  // op 39407: put
  db->Put(write_options, "key11551", "value10282");

  // op 39408: get
  status = db->Get(read_options, "key6616", &value);
  if (status.ok()) {
    out << "GET key6616 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39409: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3629", "key4838", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3629, key4838) size=" << scan_result.size() << std::endl;
  }

  // op 39410: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4022", "key7561", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4022, key7561) size=" << scan_result.size() << std::endl;
  }

  // op 39411: put
  db->Put(write_options, "key2084", "value96914");

  // op 39412: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9425", "key9539", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9425, key9539) size=" << scan_result.size() << std::endl;
  }

  // op 39413: delete
  db->Delete(write_options, "key9975");

  // op 39414: get
  status = db->Get(read_options, "key11221", &value);
  if (status.ok()) {
    out << "GET key11221 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39415: get
  status = db->Get(read_options, "key3321", &value);
  if (status.ok()) {
    out << "GET key3321 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39416: get
  status = db->Get(read_options, "key3838", &value);
  if (status.ok()) {
    out << "GET key3838 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39417: get
  status = db->Get(read_options, "key2330", &value);
  if (status.ok()) {
    out << "GET key2330 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39418: delete_range
  status = db->DeleteRange(write_options, "key9064", "key9194");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39419: get
  status = db->Get(read_options, "key8523", &value);
  if (status.ok()) {
    out << "GET key8523 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39420: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3962", "key4832", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3962, key4832) size=" << scan_result.size() << std::endl;
  }

  // op 39421: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3221", "key11065", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3221, key11065) size=" << scan_result.size() << std::endl;
  }

  // op 39422: get
  status = db->Get(read_options, "key2101", &value);
  if (status.ok()) {
    out << "GET key2101 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39423: delete_range
  status = db->DeleteRange(write_options, "key4849", "key8229");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39424: delete_range
  status = db->DeleteRange(write_options, "key2007", "key11282");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39425: get
  status = db->Get(read_options, "key2295", &value);
  if (status.ok()) {
    out << "GET key2295 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39426: put
  db->Put(write_options, "key8394", "value21216");

  // op 39427: delete_range
  status = db->DeleteRange(write_options, "key3333", "key4083");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39428: put
  db->Put(write_options, "key10486", "value72513");

  // op 39429: get
  status = db->Get(read_options, "key4740", &value);
  if (status.ok()) {
    out << "GET key4740 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39430: put
  db->Put(write_options, "key1838", "value0438");

  // op 39431: put
  db->Put(write_options, "key2665", "value40158");

  // op 39432: put
  db->Put(write_options, "key2422", "value113860");

  // op 39433: get
  status = db->Get(read_options, "key6695", &value);
  if (status.ok()) {
    out << "GET key6695 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39434: put
  db->Put(write_options, "key8458", "value13925");

  // op 39435: get
  status = db->Get(read_options, "key8286", &value);
  if (status.ok()) {
    out << "GET key8286 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39436: put
  db->Put(write_options, "key0721", "value117663");

  // op 39437: delete_range
  status = db->DeleteRange(write_options, "key2814", "key5989");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39438: put
  db->Put(write_options, "key10050", "value82066");

  // op 39439: delete
  db->Delete(write_options, "key10884");

  // op 39440: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2236", "key7315", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2236, key7315) size=" << scan_result.size() << std::endl;
  }

  // op 39441: delete
  db->Delete(write_options, "key3690");

  // op 39442: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9286", "key9619", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9286, key9619) size=" << scan_result.size() << std::endl;
  }

  // op 39443: put
  db->Put(write_options, "key7041", "value49654");

  // op 39444: delete_range
  status = db->DeleteRange(write_options, "key8745", "key8769");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39445: delete
  db->Delete(write_options, "key5787");

  // op 39446: put
  db->Put(write_options, "key4688", "value26089");

  // op 39447: delete_range
  status = db->DeleteRange(write_options, "key4487", "key5968");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39448: put
  db->Put(write_options, "key9936", "value2828");

  // op 39449: delete_range
  status = db->DeleteRange(write_options, "key0633", "key5703");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39450: delete_range
  status = db->DeleteRange(write_options, "key5226", "key8843");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39451: delete
  db->Delete(write_options, "key5968");

  // op 39452: put
  db->Put(write_options, "key5274", "value76764");

  // op 39453: get
  status = db->Get(read_options, "key4205", &value);
  if (status.ok()) {
    out << "GET key4205 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39454: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5981", "key8672", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5981, key8672) size=" << scan_result.size() << std::endl;
  }

  // op 39455: get
  status = db->Get(read_options, "key3898", &value);
  if (status.ok()) {
    out << "GET key3898 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39456: delete
  db->Delete(write_options, "key11134");

  // op 39457: delete
  db->Delete(write_options, "key1761");

  // op 39458: delete
  db->Delete(write_options, "key10943");

  // op 39459: put
  db->Put(write_options, "key7391", "value73554");

  // op 39460: put
  db->Put(write_options, "key6755", "value82211");

  // op 39461: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4130", "key9629", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4130, key9629) size=" << scan_result.size() << std::endl;
  }

  // op 39462: get
  status = db->Get(read_options, "key3184", &value);
  if (status.ok()) {
    out << "GET key3184 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39463: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7253", "key9872", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7253, key9872) size=" << scan_result.size() << std::endl;
  }

  // op 39464: put
  db->Put(write_options, "key11104", "value33671");

  // op 39465: put
  db->Put(write_options, "key10589", "value24776");

  // op 39466: delete
  db->Delete(write_options, "key3378");

  // op 39467: get
  status = db->Get(read_options, "key10923", &value);
  if (status.ok()) {
    out << "GET key10923 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39468: put
  db->Put(write_options, "key6645", "value95437");

  // op 39469: put
  db->Put(write_options, "key10187", "value117354");

  // op 39470: delete
  db->Delete(write_options, "key6591");

  // op 39471: put
  db->Put(write_options, "key7222", "value84297");

  // op 39472: delete_range
  status = db->DeleteRange(write_options, "key8610", "key8894");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39473: put
  db->Put(write_options, "key7414", "value53528");

  // op 39474: get
  status = db->Get(read_options, "key6081", &value);
  if (status.ok()) {
    out << "GET key6081 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39475: put
  db->Put(write_options, "key8977", "value2129");

  // op 39476: delete
  db->Delete(write_options, "key1620");

  // op 39477: get
  status = db->Get(read_options, "key3635", &value);
  if (status.ok()) {
    out << "GET key3635 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39478: put
  db->Put(write_options, "key11061", "value36796");

  // op 39479: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 39480: get
  status = db->Get(read_options, "key6338", &value);
  if (status.ok()) {
    out << "GET key6338 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39481: get
  status = db->Get(read_options, "key4205", &value);
  if (status.ok()) {
    out << "GET key4205 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39482: delete
  db->Delete(write_options, "key10479");

  // op 39483: put
  db->Put(write_options, "key10959", "value41328");

  // op 39484: put
  db->Put(write_options, "key4192", "value58868");

  // op 39485: put
  db->Put(write_options, "key8520", "value48149");

  // op 39486: delete_range
  status = db->DeleteRange(write_options, "key6831", "key9210");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39487: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 39488: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1736", "key6143", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1736, key6143) size=" << scan_result.size() << std::endl;
  }

  // op 39489: delete
  db->Delete(write_options, "key11168");

  // op 39490: delete_range
  status = db->DeleteRange(write_options, "key7222", "key10815");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39491: put
  db->Put(write_options, "key11003", "value49923");

  // op 39492: delete
  db->Delete(write_options, "key5955");

  // op 39493: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7931", "key9663", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7931, key9663) size=" << scan_result.size() << std::endl;
  }

  // op 39494: delete
  db->Delete(write_options, "key11179");

  // op 39495: delete
  db->Delete(write_options, "key9523");

  // op 39496: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4630", "key7073", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4630, key7073) size=" << scan_result.size() << std::endl;
  }

  // op 39497: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 39498: get
  status = db->Get(read_options, "key5357", &value);
  if (status.ok()) {
    out << "GET key5357 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39499: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1979", "key5867", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1979, key5867) size=" << scan_result.size() << std::endl;
  }

  // op 39500: delete_range
  status = db->DeleteRange(write_options, "key0122", "key0849");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39501: get
  status = db->Get(read_options, "key4306", &value);
  if (status.ok()) {
    out << "GET key4306 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39502: delete
  db->Delete(write_options, "key11258");

  // op 39503: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3452", "key10034", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3452, key10034) size=" << scan_result.size() << std::endl;
  }

  // op 39504: put
  db->Put(write_options, "key6797", "value66882");

  // op 39505: put
  db->Put(write_options, "key4124", "value30979");

  // op 39506: get
  status = db->Get(read_options, "key2839", &value);
  if (status.ok()) {
    out << "GET key2839 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39507: get
  status = db->Get(read_options, "key6366", &value);
  if (status.ok()) {
    out << "GET key6366 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39508: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6811", "key7888", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6811, key7888) size=" << scan_result.size() << std::endl;
  }

  // op 39509: put
  db->Put(write_options, "key11171", "value29093");

  // op 39510: put
  db->Put(write_options, "key5677", "value107050");

  // op 39511: get
  status = db->Get(read_options, "key4941", &value);
  if (status.ok()) {
    out << "GET key4941 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39512: put
  db->Put(write_options, "key10218", "value24480");

  // op 39513: put
  db->Put(write_options, "key9262", "value48213");

  // op 39514: put
  db->Put(write_options, "key4696", "value18185");

  // op 39515: put
  db->Put(write_options, "key8816", "value16781");

  // op 39516: put
  db->Put(write_options, "key11635", "value53601");

  // op 39517: get
  status = db->Get(read_options, "key10623", &value);
  if (status.ok()) {
    out << "GET key10623 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39518: delete_range
  status = db->DeleteRange(write_options, "key4631", "key10002");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39519: put
  db->Put(write_options, "key9961", "value73642");

  // op 39520: put
  db->Put(write_options, "key9933", "value19546");

  // op 39521: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 39522: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3249", "key10653", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3249, key10653) size=" << scan_result.size() << std::endl;
  }

  // op 39523: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8162", "key11407", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8162, key11407) size=" << scan_result.size() << std::endl;
  }

  // op 39524: put
  db->Put(write_options, "key2356", "value4940");

  // op 39525: get
  status = db->Get(read_options, "key7471", &value);
  if (status.ok()) {
    out << "GET key7471 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39526: put
  db->Put(write_options, "key5453", "value62059");

  // op 39527: delete_range
  status = db->DeleteRange(write_options, "key9012", "key10848");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39528: get
  status = db->Get(read_options, "key11673", &value);
  if (status.ok()) {
    out << "GET key11673 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39529: get
  status = db->Get(read_options, "key5279", &value);
  if (status.ok()) {
    out << "GET key5279 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39530: put
  db->Put(write_options, "key5283", "value69223");

  // op 39531: put
  db->Put(write_options, "key9082", "value117045");

  // op 39532: put
  db->Put(write_options, "key1978", "value75456");

  // op 39533: put
  db->Put(write_options, "key7218", "value84808");

  // op 39534: put
  db->Put(write_options, "key4227", "value53867");

  // op 39535: put
  db->Put(write_options, "key2352", "value97795");

  // op 39536: put
  db->Put(write_options, "key6314", "value13824");

  // op 39537: put
  db->Put(write_options, "key10595", "value41545");

  // op 39538: get
  status = db->Get(read_options, "key0568", &value);
  if (status.ok()) {
    out << "GET key0568 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39539: get
  status = db->Get(read_options, "key1399", &value);
  if (status.ok()) {
    out << "GET key1399 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39540: get
  status = db->Get(read_options, "key6876", &value);
  if (status.ok()) {
    out << "GET key6876 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39541: delete
  db->Delete(write_options, "key4658");

  // op 39542: put
  db->Put(write_options, "key1696", "value39119");

  // op 39543: get
  status = db->Get(read_options, "key6311", &value);
  if (status.ok()) {
    out << "GET key6311 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39544: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1456", "key8466", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1456, key8466) size=" << scan_result.size() << std::endl;
  }

  // op 39545: delete_range
  status = db->DeleteRange(write_options, "key5048", "key6659");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39546: put
  db->Put(write_options, "key2198", "value61721");

  // op 39547: get
  status = db->Get(read_options, "key8335", &value);
  if (status.ok()) {
    out << "GET key8335 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39548: put
  db->Put(write_options, "key2305", "value68994");

  // op 39549: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2098", "key10713", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2098, key10713) size=" << scan_result.size() << std::endl;
  }

  // op 39550: put
  db->Put(write_options, "key8478", "value84741");

  // op 39551: put
  db->Put(write_options, "key5714", "value13140");

  // op 39552: delete_range
  status = db->DeleteRange(write_options, "key6399", "key9321");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39553: get
  status = db->Get(read_options, "key0019", &value);
  if (status.ok()) {
    out << "GET key0019 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39554: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6142", "key6150", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6142, key6150) size=" << scan_result.size() << std::endl;
  }

  // op 39555: delete_range
  status = db->DeleteRange(write_options, "key2823", "key4721");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39556: put
  db->Put(write_options, "key1988", "value19514");

  // op 39557: put
  db->Put(write_options, "key3954", "value69251");

  // op 39558: get
  status = db->Get(read_options, "key7313", &value);
  if (status.ok()) {
    out << "GET key7313 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39559: put
  db->Put(write_options, "key3404", "value80421");

  // op 39560: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0008", "key6886", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0008, key6886) size=" << scan_result.size() << std::endl;
  }

  // op 39561: put
  db->Put(write_options, "key6380", "value80397");

  // op 39562: put
  db->Put(write_options, "key1554", "value59908");

  // op 39563: put
  db->Put(write_options, "key3813", "value45308");

  // op 39564: put
  db->Put(write_options, "key11726", "value105292");

  // op 39565: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 39566: get
  status = db->Get(read_options, "key4387", &value);
  if (status.ok()) {
    out << "GET key4387 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39567: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4880", "key10087", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4880, key10087) size=" << scan_result.size() << std::endl;
  }

  // op 39568: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4412", "key5460", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4412, key5460) size=" << scan_result.size() << std::endl;
  }

  // op 39569: put
  db->Put(write_options, "key1138", "value30056");

  // op 39570: put
  db->Put(write_options, "key10733", "value55839");

  // op 39571: put
  db->Put(write_options, "key9940", "value10595");

  // op 39572: put
  db->Put(write_options, "key5952", "value11693");

  // op 39573: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1885", "key4800", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1885, key4800) size=" << scan_result.size() << std::endl;
  }

  // op 39574: put
  db->Put(write_options, "key0459", "value33674");

  // op 39575: get
  status = db->Get(read_options, "key3304", &value);
  if (status.ok()) {
    out << "GET key3304 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39576: delete
  db->Delete(write_options, "key10955");

  // op 39577: get
  status = db->Get(read_options, "key7994", &value);
  if (status.ok()) {
    out << "GET key7994 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39578: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5107", "key11621", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5107, key11621) size=" << scan_result.size() << std::endl;
  }

  // op 39579: delete_range
  status = db->DeleteRange(write_options, "key3823", "key5524");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39580: put
  db->Put(write_options, "key0157", "value83130");

  // op 39581: delete_range
  status = db->DeleteRange(write_options, "key6578", "key6619");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39582: put
  db->Put(write_options, "key11406", "value80308");

  // op 39583: delete_range
  status = db->DeleteRange(write_options, "key4649", "key8804");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39584: put
  db->Put(write_options, "key7343", "value29394");

  // op 39585: delete
  db->Delete(write_options, "key8388");

  // op 39586: put
  db->Put(write_options, "key7517", "value49849");

  // op 39587: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0368", "key10653", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0368, key10653) size=" << scan_result.size() << std::endl;
  }

  // op 39588: get
  status = db->Get(read_options, "key11476", &value);
  if (status.ok()) {
    out << "GET key11476 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39589: put
  db->Put(write_options, "key2976", "value86745");

  // op 39590: put
  db->Put(write_options, "key11639", "value104795");

  // op 39591: put
  db->Put(write_options, "key7591", "value36843");

  // op 39592: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3349", "key11506", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3349, key11506) size=" << scan_result.size() << std::endl;
  }

  // op 39593: get
  status = db->Get(read_options, "key9443", &value);
  if (status.ok()) {
    out << "GET key9443 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39594: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7463", "key10058", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7463, key10058) size=" << scan_result.size() << std::endl;
  }

  // op 39595: put
  db->Put(write_options, "key1516", "value76566");

  // op 39596: put
  db->Put(write_options, "key8435", "value42640");

  // op 39597: put
  db->Put(write_options, "key10290", "value25681");

  // op 39598: put
  db->Put(write_options, "key3193", "value90828");

  // op 39599: put
  db->Put(write_options, "key9475", "value74530");

  // op 39600: delete_range
  status = db->DeleteRange(write_options, "key0820", "key7928");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39601: put
  db->Put(write_options, "key6039", "value31632");

  // op 39602: delete_range
  status = db->DeleteRange(write_options, "key2923", "key9298");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39603: delete
  db->Delete(write_options, "key3242");

  // op 39604: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8250", "key9270", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8250, key9270) size=" << scan_result.size() << std::endl;
  }

  // op 39605: put
  db->Put(write_options, "key0599", "value76624");

  // op 39606: put
  db->Put(write_options, "key6052", "value57546");

  // op 39607: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0598", "key0606", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0598, key0606) size=" << scan_result.size() << std::endl;
  }

  // op 39608: put
  db->Put(write_options, "key6572", "value62606");

  // op 39609: delete
  db->Delete(write_options, "key5738");

  // op 39610: delete
  db->Delete(write_options, "key2696");

  // op 39611: put
  db->Put(write_options, "key4081", "value81340");

  // op 39612: put
  db->Put(write_options, "key7681", "value85656");

  // op 39613: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3355", "key3613", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3355, key3613) size=" << scan_result.size() << std::endl;
  }

  // op 39614: get
  status = db->Get(read_options, "key11703", &value);
  if (status.ok()) {
    out << "GET key11703 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39615: put
  db->Put(write_options, "key4011", "value23308");

  // op 39616: put
  db->Put(write_options, "key4664", "value90751");

  // op 39617: put
  db->Put(write_options, "key2808", "value32516");

  // op 39618: put
  db->Put(write_options, "key7660", "value0664");

  // op 39619: get
  status = db->Get(read_options, "key10167", &value);
  if (status.ok()) {
    out << "GET key10167 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39620: get
  status = db->Get(read_options, "key3598", &value);
  if (status.ok()) {
    out << "GET key3598 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39621: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 39622: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3536", "key6658", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3536, key6658) size=" << scan_result.size() << std::endl;
  }

  // op 39623: get
  status = db->Get(read_options, "key0545", &value);
  if (status.ok()) {
    out << "GET key0545 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39624: get
  status = db->Get(read_options, "key4710", &value);
  if (status.ok()) {
    out << "GET key4710 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39625: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7404", "key9875", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7404, key9875) size=" << scan_result.size() << std::endl;
  }

  // op 39626: put
  db->Put(write_options, "key2338", "value22343");

  // op 39627: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 39628: put
  db->Put(write_options, "key0704", "value84017");

  // op 39629: put
  db->Put(write_options, "key5801", "value36583");

  // op 39630: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3826", "key9382", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3826, key9382) size=" << scan_result.size() << std::endl;
  }

  // op 39631: delete_range
  status = db->DeleteRange(write_options, "key3699", "key7709");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39632: delete_range
  status = db->DeleteRange(write_options, "key4371", "key11378");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39633: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4998", "key5237", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4998, key5237) size=" << scan_result.size() << std::endl;
  }

  // op 39634: put
  db->Put(write_options, "key7098", "value110939");

  // op 39635: put
  db->Put(write_options, "key0084", "value116726");

  // op 39636: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3213", "key4402", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3213, key4402) size=" << scan_result.size() << std::endl;
  }

  // op 39637: get
  status = db->Get(read_options, "key9784", &value);
  if (status.ok()) {
    out << "GET key9784 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39638: put
  db->Put(write_options, "key11242", "value53112");

  // op 39639: put
  db->Put(write_options, "key1242", "value47624");

  // op 39640: put
  db->Put(write_options, "key2913", "value79306");

  // op 39641: put
  db->Put(write_options, "key10205", "value85742");

  // op 39642: put
  db->Put(write_options, "key7024", "value27321");

  // op 39643: get
  status = db->Get(read_options, "key4074", &value);
  if (status.ok()) {
    out << "GET key4074 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39644: put
  db->Put(write_options, "key6288", "value20538");

  // op 39645: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8000", "key10985", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8000, key10985) size=" << scan_result.size() << std::endl;
  }

  // op 39646: get
  status = db->Get(read_options, "key0429", &value);
  if (status.ok()) {
    out << "GET key0429 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39647: get
  status = db->Get(read_options, "key6013", &value);
  if (status.ok()) {
    out << "GET key6013 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39648: put
  db->Put(write_options, "key10356", "value0452");

  // op 39649: put
  db->Put(write_options, "key8680", "value66063");

  // op 39650: delete_range
  status = db->DeleteRange(write_options, "key10007", "key11576");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39651: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3634", "key10245", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3634, key10245) size=" << scan_result.size() << std::endl;
  }

  // op 39652: delete_range
  status = db->DeleteRange(write_options, "key5337", "key7050");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39653: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3976", "key10427", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3976, key10427) size=" << scan_result.size() << std::endl;
  }

  // op 39654: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4224", "key10198", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4224, key10198) size=" << scan_result.size() << std::endl;
  }

  // op 39655: put
  db->Put(write_options, "key2523", "value92787");

  // op 39656: put
  db->Put(write_options, "key8105", "value16137");

  // op 39657: delete
  db->Delete(write_options, "key7491");

  // op 39658: get
  status = db->Get(read_options, "key10978", &value);
  if (status.ok()) {
    out << "GET key10978 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39659: delete_range
  status = db->DeleteRange(write_options, "key0922", "key7602");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39660: put
  db->Put(write_options, "key0652", "value95858");

  // op 39661: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10151", "key10191", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10151, key10191) size=" << scan_result.size() << std::endl;
  }

  // op 39662: delete_range
  status = db->DeleteRange(write_options, "key8595", "key11191");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39663: put
  db->Put(write_options, "key11665", "value77600");

  // op 39664: put
  db->Put(write_options, "key1744", "value29047");

  // op 39665: put
  db->Put(write_options, "key8376", "value82931");

  // op 39666: put
  db->Put(write_options, "key0004", "value36140");

  // op 39667: delete_range
  status = db->DeleteRange(write_options, "key2224", "key6862");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39668: delete
  db->Delete(write_options, "key5470");

  // op 39669: put
  db->Put(write_options, "key10259", "value113638");

  // op 39670: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 39671: get
  status = db->Get(read_options, "key5060", &value);
  if (status.ok()) {
    out << "GET key5060 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39672: get
  status = db->Get(read_options, "key11385", &value);
  if (status.ok()) {
    out << "GET key11385 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39673: delete
  db->Delete(write_options, "key9105");

  // op 39674: get
  status = db->Get(read_options, "key6320", &value);
  if (status.ok()) {
    out << "GET key6320 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39675: put
  db->Put(write_options, "key11035", "value58091");

  // op 39676: put
  db->Put(write_options, "key11707", "value3141");

  // op 39677: put
  db->Put(write_options, "key2348", "value114760");

  // op 39678: delete_range
  status = db->DeleteRange(write_options, "key0867", "key5534");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39679: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6420", "key6820", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6420, key6820) size=" << scan_result.size() << std::endl;
  }

  // op 39680: delete_range
  status = db->DeleteRange(write_options, "key6662", "key7062");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39681: put
  db->Put(write_options, "key5281", "value68023");

  // op 39682: delete_range
  status = db->DeleteRange(write_options, "key1903", "key9992");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39683: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1083", "key9514", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1083, key9514) size=" << scan_result.size() << std::endl;
  }

  // op 39684: put
  db->Put(write_options, "key8438", "value51199");

  // op 39685: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1251", "key3022", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1251, key3022) size=" << scan_result.size() << std::endl;
  }

  // op 39686: delete
  db->Delete(write_options, "key9729");

  // op 39687: put
  db->Put(write_options, "key9488", "value110117");

  // op 39688: put
  db->Put(write_options, "key8287", "value74518");

  // op 39689: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0765", "key5036", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0765, key5036) size=" << scan_result.size() << std::endl;
  }

  // op 39690: put
  db->Put(write_options, "key5019", "value63227");

  // op 39691: put
  db->Put(write_options, "key8900", "value0539");

  // op 39692: put
  db->Put(write_options, "key1036", "value106571");

  // op 39693: get
  status = db->Get(read_options, "key5216", &value);
  if (status.ok()) {
    out << "GET key5216 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39694: get
  status = db->Get(read_options, "key2127", &value);
  if (status.ok()) {
    out << "GET key2127 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39695: put
  db->Put(write_options, "key0989", "value107622");

  // op 39696: get
  status = db->Get(read_options, "key8746", &value);
  if (status.ok()) {
    out << "GET key8746 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39697: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0416", "key5627", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0416, key5627) size=" << scan_result.size() << std::endl;
  }

  // op 39698: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8635", "key11583", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8635, key11583) size=" << scan_result.size() << std::endl;
  }

  // op 39699: delete_range
  status = db->DeleteRange(write_options, "key5392", "key10184");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39700: put
  db->Put(write_options, "key4639", "value42381");

  // op 39701: put
  db->Put(write_options, "key2412", "value87019");

  // op 39702: delete
  db->Delete(write_options, "key0337");

  // op 39703: get
  status = db->Get(read_options, "key7915", &value);
  if (status.ok()) {
    out << "GET key7915 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39704: put
  db->Put(write_options, "key1228", "value17714");

  // op 39705: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 39706: get
  status = db->Get(read_options, "key8944", &value);
  if (status.ok()) {
    out << "GET key8944 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39707: put
  db->Put(write_options, "key2825", "value82309");

  // op 39708: put
  db->Put(write_options, "key2715", "value52915");

  // op 39709: put
  db->Put(write_options, "key11190", "value5461");

  // op 39710: get
  status = db->Get(read_options, "key1976", &value);
  if (status.ok()) {
    out << "GET key1976 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39711: get
  status = db->Get(read_options, "key2092", &value);
  if (status.ok()) {
    out << "GET key2092 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39712: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2705", "key5955", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2705, key5955) size=" << scan_result.size() << std::endl;
  }

  // op 39713: put
  db->Put(write_options, "key6352", "value98518");

  // op 39714: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6548", "key10025", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6548, key10025) size=" << scan_result.size() << std::endl;
  }

  // op 39715: put
  db->Put(write_options, "key3456", "value28200");

  // op 39716: get
  status = db->Get(read_options, "key11169", &value);
  if (status.ok()) {
    out << "GET key11169 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39717: get
  status = db->Get(read_options, "key9906", &value);
  if (status.ok()) {
    out << "GET key9906 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39718: put
  db->Put(write_options, "key7662", "value94814");

  // op 39719: put
  db->Put(write_options, "key9266", "value63077");

  // op 39720: put
  db->Put(write_options, "key0404", "value101356");

  // op 39721: delete
  db->Delete(write_options, "key7114");

  // op 39722: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 39723: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5540", "key8176", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5540, key8176) size=" << scan_result.size() << std::endl;
  }

  // op 39724: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0206", "key7663", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0206, key7663) size=" << scan_result.size() << std::endl;
  }

  // op 39725: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5313", "key6025", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5313, key6025) size=" << scan_result.size() << std::endl;
  }

  // op 39726: delete_range
  status = db->DeleteRange(write_options, "key1644", "key2872");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39727: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3292", "key7043", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3292, key7043) size=" << scan_result.size() << std::endl;
  }

  // op 39728: put
  db->Put(write_options, "key11545", "value107908");

  // op 39729: delete
  db->Delete(write_options, "key2928");

  // op 39730: get
  status = db->Get(read_options, "key9137", &value);
  if (status.ok()) {
    out << "GET key9137 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39731: delete_range
  status = db->DeleteRange(write_options, "key2834", "key8611");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39732: put
  db->Put(write_options, "key9762", "value80352");

  // op 39733: put
  db->Put(write_options, "key4989", "value0936");

  // op 39734: delete_range
  status = db->DeleteRange(write_options, "key5889", "key7235");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39735: put
  db->Put(write_options, "key9472", "value114185");

  // op 39736: delete
  db->Delete(write_options, "key7199");

  // op 39737: put
  db->Put(write_options, "key9691", "value78079");

  // op 39738: delete
  db->Delete(write_options, "key9847");

  // op 39739: delete
  db->Delete(write_options, "key7576");

  // op 39740: delete_range
  status = db->DeleteRange(write_options, "key0642", "key4824");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39741: delete_range
  status = db->DeleteRange(write_options, "key1365", "key8921");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39742: put
  db->Put(write_options, "key4705", "value33510");

  // op 39743: put
  db->Put(write_options, "key6077", "value4980");

  // op 39744: delete
  db->Delete(write_options, "key10510");

  // op 39745: get
  status = db->Get(read_options, "key4455", &value);
  if (status.ok()) {
    out << "GET key4455 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39746: put
  db->Put(write_options, "key0374", "value30571");

  // op 39747: get
  status = db->Get(read_options, "key10419", &value);
  if (status.ok()) {
    out << "GET key10419 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39748: delete
  db->Delete(write_options, "key9661");

  // op 39749: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 39750: put
  db->Put(write_options, "key2699", "value29029");

  // op 39751: delete_range
  status = db->DeleteRange(write_options, "key2179", "key8312");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39752: put
  db->Put(write_options, "key11297", "value68708");

  // op 39753: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10033", "key11013", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10033, key11013) size=" << scan_result.size() << std::endl;
  }

  // op 39754: put
  db->Put(write_options, "key8639", "value66897");

  // op 39755: get
  status = db->Get(read_options, "key9968", &value);
  if (status.ok()) {
    out << "GET key9968 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39756: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1876", "key8293", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1876, key8293) size=" << scan_result.size() << std::endl;
  }

  // op 39757: delete
  db->Delete(write_options, "key8116");

  // op 39758: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5904", "key7732", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5904, key7732) size=" << scan_result.size() << std::endl;
  }

  // op 39759: put
  db->Put(write_options, "key6561", "value20744");

  // op 39760: get
  status = db->Get(read_options, "key9399", &value);
  if (status.ok()) {
    out << "GET key9399 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39761: delete
  db->Delete(write_options, "key6121");

  // op 39762: get
  status = db->Get(read_options, "key5246", &value);
  if (status.ok()) {
    out << "GET key5246 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39763: put
  db->Put(write_options, "key0378", "value59880");

  // op 39764: put
  db->Put(write_options, "key4594", "value10619");

  // op 39765: get
  status = db->Get(read_options, "key5860", &value);
  if (status.ok()) {
    out << "GET key5860 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39766: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5879", "key6946", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5879, key6946) size=" << scan_result.size() << std::endl;
  }

  // op 39767: get
  status = db->Get(read_options, "key9453", &value);
  if (status.ok()) {
    out << "GET key9453 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39768: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0387", "key3327", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0387, key3327) size=" << scan_result.size() << std::endl;
  }

  // op 39769: put
  db->Put(write_options, "key9808", "value52068");

  // op 39770: get
  status = db->Get(read_options, "key9182", &value);
  if (status.ok()) {
    out << "GET key9182 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39771: delete
  db->Delete(write_options, "key0598");

  // op 39772: put
  db->Put(write_options, "key9931", "value40062");

  // op 39773: put
  db->Put(write_options, "key5643", "value55332");

  // op 39774: delete_range
  status = db->DeleteRange(write_options, "key0831", "key11087");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39775: put
  db->Put(write_options, "key0712", "value23455");

  // op 39776: get
  status = db->Get(read_options, "key0481", &value);
  if (status.ok()) {
    out << "GET key0481 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39777: put
  db->Put(write_options, "key1219", "value38796");

  // op 39778: put
  db->Put(write_options, "key3126", "value86532");

  // op 39779: delete_range
  status = db->DeleteRange(write_options, "key0482", "key6517");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39780: put
  db->Put(write_options, "key8775", "value77077");

  // op 39781: put
  db->Put(write_options, "key0779", "value70394");

  // op 39782: put
  db->Put(write_options, "key0469", "value51202");

  // op 39783: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 39784: get
  status = db->Get(read_options, "key6015", &value);
  if (status.ok()) {
    out << "GET key6015 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39785: delete_range
  status = db->DeleteRange(write_options, "key9479", "key11615");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39786: get
  status = db->Get(read_options, "key10389", &value);
  if (status.ok()) {
    out << "GET key10389 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39787: put
  db->Put(write_options, "key9582", "value53788");

  // op 39788: put
  db->Put(write_options, "key8228", "value41822");

  // op 39789: delete_range
  status = db->DeleteRange(write_options, "key5871", "key9486");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39790: delete_range
  status = db->DeleteRange(write_options, "key0196", "key5428");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39791: delete_range
  status = db->DeleteRange(write_options, "key2552", "key10307");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39792: put
  db->Put(write_options, "key0438", "value84666");

  // op 39793: delete_range
  status = db->DeleteRange(write_options, "key6632", "key7030");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39794: put
  db->Put(write_options, "key1019", "value30681");

  // op 39795: delete
  db->Delete(write_options, "key6241");

  // op 39796: get
  status = db->Get(read_options, "key0040", &value);
  if (status.ok()) {
    out << "GET key0040 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39797: put
  db->Put(write_options, "key6735", "value9697");

  // op 39798: put
  db->Put(write_options, "key7681", "value17149");

  // op 39799: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3278", "key6409", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3278, key6409) size=" << scan_result.size() << std::endl;
  }

  // op 39800: put
  db->Put(write_options, "key6256", "value56361");

  // op 39801: delete_range
  status = db->DeleteRange(write_options, "key3433", "key6375");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39802: delete_range
  status = db->DeleteRange(write_options, "key4224", "key5157");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39803: delete
  db->Delete(write_options, "key0031");

  // op 39804: put
  db->Put(write_options, "key3330", "value71729");

  // op 39805: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3316", "key7428", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3316, key7428) size=" << scan_result.size() << std::endl;
  }

  // op 39806: get
  status = db->Get(read_options, "key10138", &value);
  if (status.ok()) {
    out << "GET key10138 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39807: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9122", "key10214", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9122, key10214) size=" << scan_result.size() << std::endl;
  }

  // op 39808: get
  status = db->Get(read_options, "key10932", &value);
  if (status.ok()) {
    out << "GET key10932 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39809: delete_range
  status = db->DeleteRange(write_options, "key3248", "key9167");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39810: put
  db->Put(write_options, "key10994", "value55296");

  // op 39811: put
  db->Put(write_options, "key8956", "value95560");

  // op 39812: delete_range
  status = db->DeleteRange(write_options, "key4257", "key7405");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39813: get
  status = db->Get(read_options, "key5043", &value);
  if (status.ok()) {
    out << "GET key5043 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39814: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2558", "key8691", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2558, key8691) size=" << scan_result.size() << std::endl;
  }

  // op 39815: put
  db->Put(write_options, "key7492", "value57992");

  // op 39816: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3905", "key8586", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3905, key8586) size=" << scan_result.size() << std::endl;
  }

  // op 39817: put
  db->Put(write_options, "key8304", "value61170");

  // op 39818: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0142", "key8425", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0142, key8425) size=" << scan_result.size() << std::endl;
  }

  // op 39819: put
  db->Put(write_options, "key10343", "value63584");

  // op 39820: put
  db->Put(write_options, "key9934", "value52263");

  // op 39821: delete
  db->Delete(write_options, "key6843");

  // op 39822: put
  db->Put(write_options, "key5363", "value80132");

  // op 39823: get
  status = db->Get(read_options, "key11296", &value);
  if (status.ok()) {
    out << "GET key11296 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39824: put
  db->Put(write_options, "key4572", "value49515");

  // op 39825: delete_range
  status = db->DeleteRange(write_options, "key5995", "key10444");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39826: get
  status = db->Get(read_options, "key9638", &value);
  if (status.ok()) {
    out << "GET key9638 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39827: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3030", "key3812", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3030, key3812) size=" << scan_result.size() << std::endl;
  }

  // op 39828: delete_range
  status = db->DeleteRange(write_options, "key2761", "key5011");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39829: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0168", "key11018", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0168, key11018) size=" << scan_result.size() << std::endl;
  }

  // op 39830: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2150", "key2418", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2150, key2418) size=" << scan_result.size() << std::endl;
  }

  // op 39831: put
  db->Put(write_options, "key6716", "value111926");

  // op 39832: put
  db->Put(write_options, "key8105", "value77948");

  // op 39833: get
  status = db->Get(read_options, "key11096", &value);
  if (status.ok()) {
    out << "GET key11096 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39834: delete_range
  status = db->DeleteRange(write_options, "key8358", "key11501");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39835: put
  db->Put(write_options, "key0345", "value34392");

  // op 39836: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8016", "key9785", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8016, key9785) size=" << scan_result.size() << std::endl;
  }

  // op 39837: delete_range
  status = db->DeleteRange(write_options, "key1787", "key8088");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39838: delete_range
  status = db->DeleteRange(write_options, "key0362", "key10071");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39839: get
  status = db->Get(read_options, "key2089", &value);
  if (status.ok()) {
    out << "GET key2089 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39840: put
  db->Put(write_options, "key10650", "value110655");

  // op 39841: put
  db->Put(write_options, "key7313", "value29067");

  // op 39842: put
  db->Put(write_options, "key9640", "value86821");

  // op 39843: put
  db->Put(write_options, "key0577", "value82602");

  // op 39844: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6372", "key9533", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6372, key9533) size=" << scan_result.size() << std::endl;
  }

  // op 39845: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0709", "key1593", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0709, key1593) size=" << scan_result.size() << std::endl;
  }

  // op 39846: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2060", "key3099", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2060, key3099) size=" << scan_result.size() << std::endl;
  }

  // op 39847: delete
  db->Delete(write_options, "key4012");

  // op 39848: delete
  db->Delete(write_options, "key6127");

  // op 39849: put
  db->Put(write_options, "key10430", "value90839");

  // op 39850: put
  db->Put(write_options, "key3760", "value53802");

  // op 39851: delete
  db->Delete(write_options, "key7886");

  // op 39852: put
  db->Put(write_options, "key6490", "value54142");

  // op 39853: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7827", "key11094", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7827, key11094) size=" << scan_result.size() << std::endl;
  }

  // op 39854: put
  db->Put(write_options, "key3762", "value100449");

  // op 39855: get
  status = db->Get(read_options, "key6665", &value);
  if (status.ok()) {
    out << "GET key6665 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39856: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 39857: put
  db->Put(write_options, "key5848", "value21681");

  // op 39858: get
  status = db->Get(read_options, "key11691", &value);
  if (status.ok()) {
    out << "GET key11691 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39859: put
  db->Put(write_options, "key1066", "value66731");

  // op 39860: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0489", "key10242", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0489, key10242) size=" << scan_result.size() << std::endl;
  }

  // op 39861: delete
  db->Delete(write_options, "key8345");

  // op 39862: put
  db->Put(write_options, "key7635", "value73877");

  // op 39863: put
  db->Put(write_options, "key10832", "value21771");

  // op 39864: put
  db->Put(write_options, "key10537", "value6407");

  // op 39865: put
  db->Put(write_options, "key5126", "value85980");

  // op 39866: get
  status = db->Get(read_options, "key9730", &value);
  if (status.ok()) {
    out << "GET key9730 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39867: get
  status = db->Get(read_options, "key4692", &value);
  if (status.ok()) {
    out << "GET key4692 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39868: get
  status = db->Get(read_options, "key10030", &value);
  if (status.ok()) {
    out << "GET key10030 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39869: delete_range
  status = db->DeleteRange(write_options, "key1904", "key5871");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39870: put
  db->Put(write_options, "key3035", "value4248");

  // op 39871: put
  db->Put(write_options, "key7669", "value105544");

  // op 39872: put
  db->Put(write_options, "key2677", "value25705");

  // op 39873: delete_range
  status = db->DeleteRange(write_options, "key7656", "key11269");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39874: delete_range
  status = db->DeleteRange(write_options, "key1523", "key4003");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39875: put
  db->Put(write_options, "key4540", "value67950");

  // op 39876: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 39877: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7535", "key10194", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7535, key10194) size=" << scan_result.size() << std::endl;
  }

  // op 39878: put
  db->Put(write_options, "key8054", "value12780");

  // op 39879: get
  status = db->Get(read_options, "key8607", &value);
  if (status.ok()) {
    out << "GET key8607 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39880: get
  status = db->Get(read_options, "key4287", &value);
  if (status.ok()) {
    out << "GET key4287 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39881: put
  db->Put(write_options, "key3577", "value0291");

  // op 39882: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 39883: delete_range
  status = db->DeleteRange(write_options, "key4447", "key8831");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39884: put
  db->Put(write_options, "key0495", "value97632");

  // op 39885: put
  db->Put(write_options, "key4796", "value2855");

  // op 39886: put
  db->Put(write_options, "key4095", "value39128");

  // op 39887: put
  db->Put(write_options, "key3051", "value33964");

  // op 39888: put
  db->Put(write_options, "key1836", "value69813");

  // op 39889: get
  status = db->Get(read_options, "key10107", &value);
  if (status.ok()) {
    out << "GET key10107 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39890: delete_range
  status = db->DeleteRange(write_options, "key4750", "key7834");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39891: put
  db->Put(write_options, "key9980", "value61065");

  // op 39892: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0816", "key8259", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0816, key8259) size=" << scan_result.size() << std::endl;
  }

  // op 39893: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2110", "key10269", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2110, key10269) size=" << scan_result.size() << std::endl;
  }

  // op 39894: put
  db->Put(write_options, "key9372", "value52085");

  // op 39895: put
  db->Put(write_options, "key0024", "value107435");

  // op 39896: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0578", "key4497", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0578, key4497) size=" << scan_result.size() << std::endl;
  }

  // op 39897: get
  status = db->Get(read_options, "key10064", &value);
  if (status.ok()) {
    out << "GET key10064 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39898: get
  status = db->Get(read_options, "key0614", &value);
  if (status.ok()) {
    out << "GET key0614 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39899: get
  status = db->Get(read_options, "key7341", &value);
  if (status.ok()) {
    out << "GET key7341 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39900: delete_range
  status = db->DeleteRange(write_options, "key2250", "key7222");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39901: put
  db->Put(write_options, "key9868", "value88049");

  // op 39902: delete_range
  status = db->DeleteRange(write_options, "key7806", "key11539");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39903: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 39904: put
  db->Put(write_options, "key7861", "value112503");

  // op 39905: get
  status = db->Get(read_options, "key11289", &value);
  if (status.ok()) {
    out << "GET key11289 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39906: delete
  db->Delete(write_options, "key11072");

  // op 39907: delete_range
  status = db->DeleteRange(write_options, "key3358", "key5463");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39908: put
  db->Put(write_options, "key0051", "value13581");

  // op 39909: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1100", "key4973", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1100, key4973) size=" << scan_result.size() << std::endl;
  }

  // op 39910: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1336", "key10840", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1336, key10840) size=" << scan_result.size() << std::endl;
  }

  // op 39911: put
  db->Put(write_options, "key5476", "value31491");

  // op 39912: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5676", "key7819", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5676, key7819) size=" << scan_result.size() << std::endl;
  }

  // op 39913: delete_range
  status = db->DeleteRange(write_options, "key4149", "key6725");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39914: put
  db->Put(write_options, "key1917", "value11593");

  // op 39915: put
  db->Put(write_options, "key8275", "value60352");

  // op 39916: get
  status = db->Get(read_options, "key10130", &value);
  if (status.ok()) {
    out << "GET key10130 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39917: delete
  db->Delete(write_options, "key9531");

  // op 39918: get
  status = db->Get(read_options, "key9228", &value);
  if (status.ok()) {
    out << "GET key9228 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39919: get
  status = db->Get(read_options, "key3123", &value);
  if (status.ok()) {
    out << "GET key3123 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39920: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8523", "key8796", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8523, key8796) size=" << scan_result.size() << std::endl;
  }

  // op 39921: put
  db->Put(write_options, "key8541", "value56437");

  // op 39922: delete_range
  status = db->DeleteRange(write_options, "key0476", "key6331");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39923: get
  status = db->Get(read_options, "key4014", &value);
  if (status.ok()) {
    out << "GET key4014 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39924: delete_range
  status = db->DeleteRange(write_options, "key7414", "key11650");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39925: put
  db->Put(write_options, "key5308", "value3402");

  // op 39926: delete
  db->Delete(write_options, "key2027");

  // op 39927: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5926", "key9136", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5926, key9136) size=" << scan_result.size() << std::endl;
  }

  // op 39928: delete_range
  status = db->DeleteRange(write_options, "key2869", "key10321");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39929: delete
  db->Delete(write_options, "key4303");

  // op 39930: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 39931: delete
  db->Delete(write_options, "key6640");

  // op 39932: put
  db->Put(write_options, "key1527", "value90801");

  // op 39933: put
  db->Put(write_options, "key5060", "value6261");

  // op 39934: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6227", "key10456", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6227, key10456) size=" << scan_result.size() << std::endl;
  }

  // op 39935: delete
  db->Delete(write_options, "key9452");

  // op 39936: put
  db->Put(write_options, "key7245", "value54828");

  // op 39937: put
  db->Put(write_options, "key2596", "value88000");

  // op 39938: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1857", "key9729", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1857, key9729) size=" << scan_result.size() << std::endl;
  }

  // op 39939: put
  db->Put(write_options, "key6321", "value84050");

  // op 39940: get
  status = db->Get(read_options, "key1009", &value);
  if (status.ok()) {
    out << "GET key1009 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39941: delete_range
  status = db->DeleteRange(write_options, "key8440", "key10756");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39942: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 39943: put
  db->Put(write_options, "key5953", "value108647");

  // op 39944: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 39945: put
  db->Put(write_options, "key3316", "value107105");

  // op 39946: delete
  db->Delete(write_options, "key1487");

  // op 39947: put
  db->Put(write_options, "key3732", "value16899");

  // op 39948: put
  db->Put(write_options, "key10495", "value39203");

  // op 39949: put
  db->Put(write_options, "key1036", "value92479");

  // op 39950: put
  db->Put(write_options, "key6748", "value23505");

  // op 39951: put
  db->Put(write_options, "key5297", "value111772");

  // op 39952: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6541", "key11454", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6541, key11454) size=" << scan_result.size() << std::endl;
  }

  // op 39953: put
  db->Put(write_options, "key5433", "value110623");

  // op 39954: put
  db->Put(write_options, "key0893", "value116585");

  // op 39955: delete
  db->Delete(write_options, "key3292");

  // op 39956: put
  db->Put(write_options, "key3519", "value6324");

  // op 39957: delete_range
  status = db->DeleteRange(write_options, "key1438", "key11666");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39958: put
  db->Put(write_options, "key5105", "value25110");

  // op 39959: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5007", "key7788", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5007, key7788) size=" << scan_result.size() << std::endl;
  }

  // op 39960: delete_range
  status = db->DeleteRange(write_options, "key0923", "key8927");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39961: delete
  db->Delete(write_options, "key3831");

  // op 39962: put
  db->Put(write_options, "key8284", "value93713");

  // op 39963: put
  db->Put(write_options, "key4047", "value96923");

  // op 39964: get
  status = db->Get(read_options, "key3112", &value);
  if (status.ok()) {
    out << "GET key3112 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39965: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8947", "key9753", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8947, key9753) size=" << scan_result.size() << std::endl;
  }

  // op 39966: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0988", "key2610", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0988, key2610) size=" << scan_result.size() << std::endl;
  }

  // op 39967: put
  db->Put(write_options, "key9266", "value64204");

  // op 39968: get
  status = db->Get(read_options, "key6832", &value);
  if (status.ok()) {
    out << "GET key6832 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39969: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0120", "key0591", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0120, key0591) size=" << scan_result.size() << std::endl;
  }

  // op 39970: put
  db->Put(write_options, "key4252", "value83289");

  // op 39971: get
  status = db->Get(read_options, "key1600", &value);
  if (status.ok()) {
    out << "GET key1600 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39972: put
  db->Put(write_options, "key10417", "value75368");

  // op 39973: delete_range
  status = db->DeleteRange(write_options, "key0110", "key4434");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39974: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 39975: put
  db->Put(write_options, "key5774", "value105805");

  // op 39976: get
  status = db->Get(read_options, "key0212", &value);
  if (status.ok()) {
    out << "GET key0212 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39977: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5537", "key6277", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5537, key6277) size=" << scan_result.size() << std::endl;
  }

  // op 39978: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1442", "key4703", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1442, key4703) size=" << scan_result.size() << std::endl;
  }

  // op 39979: get
  status = db->Get(read_options, "key8710", &value);
  if (status.ok()) {
    out << "GET key8710 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39980: put
  db->Put(write_options, "key0478", "value107635");

  // op 39981: put
  db->Put(write_options, "key11611", "value25119");

  // op 39982: put
  db->Put(write_options, "key10958", "value70159");

  // op 39983: delete_range
  status = db->DeleteRange(write_options, "key6653", "key11357");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39984: put
  db->Put(write_options, "key10349", "value60599");

  // op 39985: put
  db->Put(write_options, "key3139", "value24690");

  // op 39986: put
  db->Put(write_options, "key5238", "value88444");

  // op 39987: get
  status = db->Get(read_options, "key5124", &value);
  if (status.ok()) {
    out << "GET key5124 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39988: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3040", "key3915", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3040, key3915) size=" << scan_result.size() << std::endl;
  }

  // op 39989: get
  status = db->Get(read_options, "key11009", &value);
  if (status.ok()) {
    out << "GET key11009 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39990: put
  db->Put(write_options, "key6019", "value45652");

  // op 39991: delete_range
  status = db->DeleteRange(write_options, "key2102", "key6593");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 39992: get
  status = db->Get(read_options, "key1897", &value);
  if (status.ok()) {
    out << "GET key1897 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 39993: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0183", "key4461", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0183, key4461) size=" << scan_result.size() << std::endl;
  }

  // op 39994: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0463", "key8891", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0463, key8891) size=" << scan_result.size() << std::endl;
  }

  // op 39995: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0059", "key4101", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0059, key4101) size=" << scan_result.size() << std::endl;
  }

  // op 39996: put
  db->Put(write_options, "key1334", "value6403");

  // op 39997: put
  db->Put(write_options, "key11288", "value97654");

  // op 39998: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0906", "key4567", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0906, key4567) size=" << scan_result.size() << std::endl;
  }

  // op 39999: get
  status = db->Get(read_options, "key10306", &value);
  if (status.ok()) {
    out << "GET key10306 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40000: get
  status = db->Get(read_options, "key0851", &value);
  if (status.ok()) {
    out << "GET key0851 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40001: put
  db->Put(write_options, "key5455", "value105723");

  // op 40002: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 40003: get
  status = db->Get(read_options, "key1682", &value);
  if (status.ok()) {
    out << "GET key1682 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40004: delete_range
  status = db->DeleteRange(write_options, "key6202", "key8731");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40005: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0045", "key7120", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0045, key7120) size=" << scan_result.size() << std::endl;
  }

  // op 40006: delete_range
  status = db->DeleteRange(write_options, "key3426", "key10765");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40007: get
  status = db->Get(read_options, "key8209", &value);
  if (status.ok()) {
    out << "GET key8209 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40008: get
  status = db->Get(read_options, "key5288", &value);
  if (status.ok()) {
    out << "GET key5288 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40009: get
  status = db->Get(read_options, "key4261", &value);
  if (status.ok()) {
    out << "GET key4261 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40010: delete
  db->Delete(write_options, "key11301");

  // op 40011: put
  db->Put(write_options, "key3579", "value107309");

  // op 40012: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3781", "key6519", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3781, key6519) size=" << scan_result.size() << std::endl;
  }

  // op 40013: put
  db->Put(write_options, "key9237", "value10046");

  // op 40014: put
  db->Put(write_options, "key1094", "value97795");

  // op 40015: delete
  db->Delete(write_options, "key5098");

  // op 40016: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 40017: put
  db->Put(write_options, "key3309", "value39847");

  // op 40018: get
  status = db->Get(read_options, "key9512", &value);
  if (status.ok()) {
    out << "GET key9512 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40019: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4563", "key5885", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4563, key5885) size=" << scan_result.size() << std::endl;
  }

  // op 40020: put
  db->Put(write_options, "key3780", "value53870");

  // op 40021: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0346", "key3063", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0346, key3063) size=" << scan_result.size() << std::endl;
  }

  // op 40022: put
  db->Put(write_options, "key2924", "value103979");

  // op 40023: put
  db->Put(write_options, "key4973", "value68625");

  // op 40024: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4645", "key7943", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4645, key7943) size=" << scan_result.size() << std::endl;
  }

  // op 40025: put
  db->Put(write_options, "key10910", "value46703");

  // op 40026: get
  status = db->Get(read_options, "key2715", &value);
  if (status.ok()) {
    out << "GET key2715 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40027: put
  db->Put(write_options, "key0617", "value43270");

  // op 40028: put
  db->Put(write_options, "key3484", "value37035");

  // op 40029: get
  status = db->Get(read_options, "key4467", &value);
  if (status.ok()) {
    out << "GET key4467 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40030: get
  status = db->Get(read_options, "key4331", &value);
  if (status.ok()) {
    out << "GET key4331 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40031: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5744", "key8346", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5744, key8346) size=" << scan_result.size() << std::endl;
  }

  // op 40032: put
  db->Put(write_options, "key2070", "value55020");

  // op 40033: get
  status = db->Get(read_options, "key5265", &value);
  if (status.ok()) {
    out << "GET key5265 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40034: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3580", "key10184", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3580, key10184) size=" << scan_result.size() << std::endl;
  }

  // op 40035: put
  db->Put(write_options, "key5097", "value91178");

  // op 40036: put
  db->Put(write_options, "key10802", "value84052");

  // op 40037: put
  db->Put(write_options, "key10691", "value36051");

  // op 40038: delete_range
  status = db->DeleteRange(write_options, "key5579", "key8894");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40039: get
  status = db->Get(read_options, "key4647", &value);
  if (status.ok()) {
    out << "GET key4647 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40040: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 40041: delete_range
  status = db->DeleteRange(write_options, "key7575", "key7826");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40042: put
  db->Put(write_options, "key8430", "value33774");

  // op 40043: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 40044: put
  db->Put(write_options, "key7612", "value51679");

  // op 40045: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 40046: delete_range
  status = db->DeleteRange(write_options, "key3988", "key11149");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40047: put
  db->Put(write_options, "key4784", "value6099");

  // op 40048: put
  db->Put(write_options, "key9329", "value7495");

  // op 40049: put
  db->Put(write_options, "key2817", "value86778");

  // op 40050: delete_range
  status = db->DeleteRange(write_options, "key0944", "key4295");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40051: put
  db->Put(write_options, "key0120", "value53204");

  // op 40052: put
  db->Put(write_options, "key8375", "value70774");

  // op 40053: put
  db->Put(write_options, "key11457", "value45631");

  // op 40054: delete
  db->Delete(write_options, "key0607");

  // op 40055: put
  db->Put(write_options, "key9903", "value102695");

  // op 40056: put
  db->Put(write_options, "key4037", "value64896");

  // op 40057: put
  db->Put(write_options, "key4017", "value107623");

  // op 40058: put
  db->Put(write_options, "key11354", "value16112");

  // op 40059: put
  db->Put(write_options, "key6999", "value101524");

  // op 40060: get
  status = db->Get(read_options, "key5053", &value);
  if (status.ok()) {
    out << "GET key5053 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40061: delete_range
  status = db->DeleteRange(write_options, "key4229", "key11681");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40062: put
  db->Put(write_options, "key6162", "value117033");

  // op 40063: put
  db->Put(write_options, "key9563", "value33309");

  // op 40064: delete_range
  status = db->DeleteRange(write_options, "key2672", "key4241");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40065: delete
  db->Delete(write_options, "key7274");

  // op 40066: get
  status = db->Get(read_options, "key6756", &value);
  if (status.ok()) {
    out << "GET key6756 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40067: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 40068: delete_range
  status = db->DeleteRange(write_options, "key3638", "key3920");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40069: delete_range
  status = db->DeleteRange(write_options, "key0263", "key10457");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40070: get
  status = db->Get(read_options, "key1961", &value);
  if (status.ok()) {
    out << "GET key1961 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40071: get
  status = db->Get(read_options, "key2157", &value);
  if (status.ok()) {
    out << "GET key2157 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40072: get
  status = db->Get(read_options, "key7337", &value);
  if (status.ok()) {
    out << "GET key7337 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40073: put
  db->Put(write_options, "key5238", "value22535");

  // op 40074: get
  status = db->Get(read_options, "key10234", &value);
  if (status.ok()) {
    out << "GET key10234 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40075: get
  status = db->Get(read_options, "key8900", &value);
  if (status.ok()) {
    out << "GET key8900 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40076: put
  db->Put(write_options, "key5132", "value7198");

  // op 40077: delete_range
  status = db->DeleteRange(write_options, "key0948", "key11610");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40078: get
  status = db->Get(read_options, "key4994", &value);
  if (status.ok()) {
    out << "GET key4994 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40079: put
  db->Put(write_options, "key8161", "value73055");

  // op 40080: get
  status = db->Get(read_options, "key11195", &value);
  if (status.ok()) {
    out << "GET key11195 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40081: get
  status = db->Get(read_options, "key11472", &value);
  if (status.ok()) {
    out << "GET key11472 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40082: put
  db->Put(write_options, "key10555", "value82158");

  // op 40083: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6531", "key9407", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6531, key9407) size=" << scan_result.size() << std::endl;
  }

  // op 40084: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1839", "key8775", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1839, key8775) size=" << scan_result.size() << std::endl;
  }

  // op 40085: delete_range
  status = db->DeleteRange(write_options, "key5711", "key9818");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40086: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8076", "key8684", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8076, key8684) size=" << scan_result.size() << std::endl;
  }

  // op 40087: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1640", "key5768", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1640, key5768) size=" << scan_result.size() << std::endl;
  }

  // op 40088: delete
  db->Delete(write_options, "key3845");

  // op 40089: delete_range
  status = db->DeleteRange(write_options, "key4963", "key10795");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40090: put
  db->Put(write_options, "key9754", "value49181");

  // op 40091: get
  status = db->Get(read_options, "key9990", &value);
  if (status.ok()) {
    out << "GET key9990 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40092: get
  status = db->Get(read_options, "key3534", &value);
  if (status.ok()) {
    out << "GET key3534 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40093: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0244", "key6679", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0244, key6679) size=" << scan_result.size() << std::endl;
  }

  // op 40094: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1124", "key4732", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1124, key4732) size=" << scan_result.size() << std::endl;
  }

  // op 40095: delete_range
  status = db->DeleteRange(write_options, "key1882", "key6645");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40096: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4462", "key4593", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4462, key4593) size=" << scan_result.size() << std::endl;
  }

  // op 40097: put
  db->Put(write_options, "key8413", "value90157");

  // op 40098: get
  status = db->Get(read_options, "key3754", &value);
  if (status.ok()) {
    out << "GET key3754 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40099: put
  db->Put(write_options, "key1108", "value101452");

  // op 40100: delete_range
  status = db->DeleteRange(write_options, "key5953", "key10141");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40101: delete_range
  status = db->DeleteRange(write_options, "key3191", "key10566");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40102: put
  db->Put(write_options, "key4377", "value26959");

  // op 40103: put
  db->Put(write_options, "key11598", "value40655");

  // op 40104: put
  db->Put(write_options, "key7525", "value104445");

  // op 40105: put
  db->Put(write_options, "key7009", "value16091");

  // op 40106: get
  status = db->Get(read_options, "key9978", &value);
  if (status.ok()) {
    out << "GET key9978 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40107: put
  db->Put(write_options, "key6758", "value96404");

  // op 40108: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0407", "key8265", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0407, key8265) size=" << scan_result.size() << std::endl;
  }

  // op 40109: delete
  db->Delete(write_options, "key8546");

  // op 40110: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 40111: put
  db->Put(write_options, "key10727", "value65731");

  // op 40112: delete
  db->Delete(write_options, "key5089");

  // op 40113: delete
  db->Delete(write_options, "key4795");

  // op 40114: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4393", "key8770", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4393, key8770) size=" << scan_result.size() << std::endl;
  }

  // op 40115: put
  db->Put(write_options, "key6928", "value93958");

  // op 40116: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 40117: get
  status = db->Get(read_options, "key2637", &value);
  if (status.ok()) {
    out << "GET key2637 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40118: put
  db->Put(write_options, "key7897", "value17413");

  // op 40119: get
  status = db->Get(read_options, "key4785", &value);
  if (status.ok()) {
    out << "GET key4785 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40120: delete
  db->Delete(write_options, "key9648");

  // op 40121: get
  status = db->Get(read_options, "key9876", &value);
  if (status.ok()) {
    out << "GET key9876 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40122: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 40123: delete
  db->Delete(write_options, "key2469");

  // op 40124: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 40125: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 40126: put
  db->Put(write_options, "key6448", "value81136");

  // op 40127: delete_range
  status = db->DeleteRange(write_options, "key4799", "key8576");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40128: get
  status = db->Get(read_options, "key9268", &value);
  if (status.ok()) {
    out << "GET key9268 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40129: put
  db->Put(write_options, "key9949", "value13754");

  // op 40130: put
  db->Put(write_options, "key6035", "value75219");

  // op 40131: put
  db->Put(write_options, "key9102", "value95904");

  // op 40132: put
  db->Put(write_options, "key10870", "value68608");

  // op 40133: put
  db->Put(write_options, "key0862", "value5360");

  // op 40134: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 40135: get
  status = db->Get(read_options, "key9756", &value);
  if (status.ok()) {
    out << "GET key9756 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40136: get
  status = db->Get(read_options, "key7614", &value);
  if (status.ok()) {
    out << "GET key7614 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40137: delete
  db->Delete(write_options, "key5759");

  // op 40138: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2155", "key11629", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2155, key11629) size=" << scan_result.size() << std::endl;
  }

  // op 40139: get
  status = db->Get(read_options, "key5575", &value);
  if (status.ok()) {
    out << "GET key5575 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40140: put
  db->Put(write_options, "key6901", "value20709");

  // op 40141: put
  db->Put(write_options, "key7252", "value98562");

  // op 40142: delete
  db->Delete(write_options, "key9563");

  // op 40143: put
  db->Put(write_options, "key7726", "value84782");

  // op 40144: get
  status = db->Get(read_options, "key6004", &value);
  if (status.ok()) {
    out << "GET key6004 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40145: put
  db->Put(write_options, "key7565", "value77503");

  // op 40146: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1829", "key3085", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1829, key3085) size=" << scan_result.size() << std::endl;
  }

  // op 40147: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0185", "key6026", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0185, key6026) size=" << scan_result.size() << std::endl;
  }

  // op 40148: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0493", "key10277", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0493, key10277) size=" << scan_result.size() << std::endl;
  }

  // op 40149: put
  db->Put(write_options, "key10323", "value17413");

  // op 40150: delete
  db->Delete(write_options, "key8546");

  // op 40151: get
  status = db->Get(read_options, "key7470", &value);
  if (status.ok()) {
    out << "GET key7470 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40152: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1400", "key10720", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1400, key10720) size=" << scan_result.size() << std::endl;
  }

  // op 40153: delete_range
  status = db->DeleteRange(write_options, "key1557", "key4526");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40154: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4349", "key6626", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4349, key6626) size=" << scan_result.size() << std::endl;
  }

  // op 40155: put
  db->Put(write_options, "key8660", "value84793");

  // op 40156: put
  db->Put(write_options, "key8774", "value13841");

  // op 40157: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5404", "key8092", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5404, key8092) size=" << scan_result.size() << std::endl;
  }

  // op 40158: delete_range
  status = db->DeleteRange(write_options, "key1838", "key5232");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40159: delete
  db->Delete(write_options, "key1333");

  // op 40160: put
  db->Put(write_options, "key0736", "value48181");

  // op 40161: put
  db->Put(write_options, "key8170", "value114445");

  // op 40162: delete_range
  status = db->DeleteRange(write_options, "key0702", "key10216");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40163: put
  db->Put(write_options, "key1059", "value98848");

  // op 40164: put
  db->Put(write_options, "key1372", "value19781");

  // op 40165: get
  status = db->Get(read_options, "key2487", &value);
  if (status.ok()) {
    out << "GET key2487 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40166: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4213", "key9718", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4213, key9718) size=" << scan_result.size() << std::endl;
  }

  // op 40167: put
  db->Put(write_options, "key10949", "value35512");

  // op 40168: get
  status = db->Get(read_options, "key11592", &value);
  if (status.ok()) {
    out << "GET key11592 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40169: put
  db->Put(write_options, "key2038", "value89546");

  // op 40170: put
  db->Put(write_options, "key9506", "value86819");

  // op 40171: delete_range
  status = db->DeleteRange(write_options, "key2476", "key9654");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40172: put
  db->Put(write_options, "key0808", "value109923");

  // op 40173: get
  status = db->Get(read_options, "key8192", &value);
  if (status.ok()) {
    out << "GET key8192 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40174: get
  status = db->Get(read_options, "key10697", &value);
  if (status.ok()) {
    out << "GET key10697 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40175: put
  db->Put(write_options, "key5338", "value102277");

  // op 40176: put
  db->Put(write_options, "key4540", "value49281");

  // op 40177: delete_range
  status = db->DeleteRange(write_options, "key5644", "key7834");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40178: delete_range
  status = db->DeleteRange(write_options, "key3606", "key5026");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40179: delete_range
  status = db->DeleteRange(write_options, "key1120", "key2291");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40180: get
  status = db->Get(read_options, "key11300", &value);
  if (status.ok()) {
    out << "GET key11300 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40181: get
  status = db->Get(read_options, "key4654", &value);
  if (status.ok()) {
    out << "GET key4654 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40182: put
  db->Put(write_options, "key2355", "value20720");

  // op 40183: put
  db->Put(write_options, "key9995", "value62947");

  // op 40184: delete_range
  status = db->DeleteRange(write_options, "key1809", "key5367");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40185: get
  status = db->Get(read_options, "key8869", &value);
  if (status.ok()) {
    out << "GET key8869 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40186: put
  db->Put(write_options, "key6533", "value80927");

  // op 40187: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4256", "key9876", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4256, key9876) size=" << scan_result.size() << std::endl;
  }

  // op 40188: get
  status = db->Get(read_options, "key6603", &value);
  if (status.ok()) {
    out << "GET key6603 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40189: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5358", "key7298", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5358, key7298) size=" << scan_result.size() << std::endl;
  }

  // op 40190: get
  status = db->Get(read_options, "key8541", &value);
  if (status.ok()) {
    out << "GET key8541 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40191: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5486", "key9168", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5486, key9168) size=" << scan_result.size() << std::endl;
  }

  // op 40192: put
  db->Put(write_options, "key9677", "value41799");

  // op 40193: get
  status = db->Get(read_options, "key9896", &value);
  if (status.ok()) {
    out << "GET key9896 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40194: delete
  db->Delete(write_options, "key11660");

  // op 40195: delete
  db->Delete(write_options, "key3934");

  // op 40196: delete
  db->Delete(write_options, "key11171");

  // op 40197: put
  db->Put(write_options, "key4725", "value7590");

  // op 40198: put
  db->Put(write_options, "key5940", "value63589");

  // op 40199: delete_range
  status = db->DeleteRange(write_options, "key2530", "key3780");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40200: get
  status = db->Get(read_options, "key2070", &value);
  if (status.ok()) {
    out << "GET key2070 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40201: put
  db->Put(write_options, "key10145", "value89590");

  // op 40202: delete
  db->Delete(write_options, "key7209");

  // op 40203: put
  db->Put(write_options, "key8306", "value71897");

  // op 40204: delete
  db->Delete(write_options, "key2820");

  // op 40205: put
  db->Put(write_options, "key9507", "value26233");

  // op 40206: get
  status = db->Get(read_options, "key11557", &value);
  if (status.ok()) {
    out << "GET key11557 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40207: get
  status = db->Get(read_options, "key10545", &value);
  if (status.ok()) {
    out << "GET key10545 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40208: get
  status = db->Get(read_options, "key5282", &value);
  if (status.ok()) {
    out << "GET key5282 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40209: get
  status = db->Get(read_options, "key10459", &value);
  if (status.ok()) {
    out << "GET key10459 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40210: get
  status = db->Get(read_options, "key10393", &value);
  if (status.ok()) {
    out << "GET key10393 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40211: put
  db->Put(write_options, "key7097", "value74635");

  // op 40212: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9930", "key10334", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9930, key10334) size=" << scan_result.size() << std::endl;
  }

  // op 40213: put
  db->Put(write_options, "key7733", "value16564");

  // op 40214: get
  status = db->Get(read_options, "key8128", &value);
  if (status.ok()) {
    out << "GET key8128 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40215: delete
  db->Delete(write_options, "key1665");

  // op 40216: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2705", "key6652", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2705, key6652) size=" << scan_result.size() << std::endl;
  }

  // op 40217: delete_range
  status = db->DeleteRange(write_options, "key0202", "key7511");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40218: delete
  db->Delete(write_options, "key3711");

  // op 40219: put
  db->Put(write_options, "key5705", "value113347");

  // op 40220: get
  status = db->Get(read_options, "key5985", &value);
  if (status.ok()) {
    out << "GET key5985 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40221: delete_range
  status = db->DeleteRange(write_options, "key2092", "key3462");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40222: put
  db->Put(write_options, "key6536", "value114254");

  // op 40223: delete_range
  status = db->DeleteRange(write_options, "key8795", "key10569");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40224: get
  status = db->Get(read_options, "key11659", &value);
  if (status.ok()) {
    out << "GET key11659 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40225: get
  status = db->Get(read_options, "key3325", &value);
  if (status.ok()) {
    out << "GET key3325 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40226: put
  db->Put(write_options, "key1688", "value12500");

  // op 40227: delete
  db->Delete(write_options, "key1913");

  // op 40228: get
  status = db->Get(read_options, "key4234", &value);
  if (status.ok()) {
    out << "GET key4234 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40229: put
  db->Put(write_options, "key5971", "value89371");

  // op 40230: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 40231: put
  db->Put(write_options, "key5872", "value93827");

  // op 40232: put
  db->Put(write_options, "key5853", "value65120");

  // op 40233: delete
  db->Delete(write_options, "key7859");

  // op 40234: get
  status = db->Get(read_options, "key7114", &value);
  if (status.ok()) {
    out << "GET key7114 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40235: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5773", "key8241", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5773, key8241) size=" << scan_result.size() << std::endl;
  }

  // op 40236: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 40237: delete
  db->Delete(write_options, "key1604");

  // op 40238: put
  db->Put(write_options, "key1469", "value39156");

  // op 40239: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 40240: delete_range
  status = db->DeleteRange(write_options, "key1438", "key4918");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40241: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3509", "key7627", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3509, key7627) size=" << scan_result.size() << std::endl;
  }

  // op 40242: put
  db->Put(write_options, "key5510", "value15671");

  // op 40243: put
  db->Put(write_options, "key5574", "value98823");

  // op 40244: put
  db->Put(write_options, "key8318", "value72663");

  // op 40245: delete_range
  status = db->DeleteRange(write_options, "key4056", "key8149");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40246: put
  db->Put(write_options, "key10285", "value77040");

  // op 40247: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2347", "key6030", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2347, key6030) size=" << scan_result.size() << std::endl;
  }

  // op 40248: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9584", "key11572", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9584, key11572) size=" << scan_result.size() << std::endl;
  }

  // op 40249: delete_range
  status = db->DeleteRange(write_options, "key1568", "key10124");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40250: get
  status = db->Get(read_options, "key10723", &value);
  if (status.ok()) {
    out << "GET key10723 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40251: put
  db->Put(write_options, "key10165", "value111511");

  // op 40252: get
  status = db->Get(read_options, "key1426", &value);
  if (status.ok()) {
    out << "GET key1426 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40253: delete_range
  status = db->DeleteRange(write_options, "key5594", "key10473");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40254: delete_range
  status = db->DeleteRange(write_options, "key5813", "key6090");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40255: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0156", "key10544", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0156, key10544) size=" << scan_result.size() << std::endl;
  }

  // op 40256: put
  db->Put(write_options, "key10167", "value8254");

  // op 40257: put
  db->Put(write_options, "key1607", "value31556");

  // op 40258: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2079", "key4330", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2079, key4330) size=" << scan_result.size() << std::endl;
  }

  // op 40259: delete_range
  status = db->DeleteRange(write_options, "key1369", "key2904");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40260: get
  status = db->Get(read_options, "key6698", &value);
  if (status.ok()) {
    out << "GET key6698 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40261: put
  db->Put(write_options, "key6476", "value22768");

  // op 40262: put
  db->Put(write_options, "key10421", "value103202");

  // op 40263: get
  status = db->Get(read_options, "key0996", &value);
  if (status.ok()) {
    out << "GET key0996 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40264: put
  db->Put(write_options, "key0011", "value0906");

  // op 40265: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 40266: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 40267: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1464", "key6641", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1464, key6641) size=" << scan_result.size() << std::endl;
  }

  // op 40268: get
  status = db->Get(read_options, "key9674", &value);
  if (status.ok()) {
    out << "GET key9674 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40269: put
  db->Put(write_options, "key3586", "value114916");

  // op 40270: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4922", "key10878", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4922, key10878) size=" << scan_result.size() << std::endl;
  }

  // op 40271: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3983", "key4389", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3983, key4389) size=" << scan_result.size() << std::endl;
  }

  // op 40272: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1254", "key6316", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1254, key6316) size=" << scan_result.size() << std::endl;
  }

  // op 40273: put
  db->Put(write_options, "key6665", "value19073");

  // op 40274: get
  status = db->Get(read_options, "key5530", &value);
  if (status.ok()) {
    out << "GET key5530 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40275: delete_range
  status = db->DeleteRange(write_options, "key3134", "key9421");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40276: put
  db->Put(write_options, "key6891", "value32065");

  // op 40277: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3317", "key5056", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3317, key5056) size=" << scan_result.size() << std::endl;
  }

  // op 40278: delete_range
  status = db->DeleteRange(write_options, "key6361", "key6421");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40279: put
  db->Put(write_options, "key4405", "value45366");

  // op 40280: put
  db->Put(write_options, "key6210", "value109483");

  // op 40281: delete
  db->Delete(write_options, "key0452");

  // op 40282: get
  status = db->Get(read_options, "key11662", &value);
  if (status.ok()) {
    out << "GET key11662 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40283: get
  status = db->Get(read_options, "key11561", &value);
  if (status.ok()) {
    out << "GET key11561 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40284: put
  db->Put(write_options, "key4297", "value49945");

  // op 40285: delete_range
  status = db->DeleteRange(write_options, "key4655", "key4722");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40286: get
  status = db->Get(read_options, "key3099", &value);
  if (status.ok()) {
    out << "GET key3099 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40287: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 40288: delete_range
  status = db->DeleteRange(write_options, "key2397", "key9310");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40289: put
  db->Put(write_options, "key7931", "value56786");

  // op 40290: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0805", "key5189", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0805, key5189) size=" << scan_result.size() << std::endl;
  }

  // op 40291: get
  status = db->Get(read_options, "key10375", &value);
  if (status.ok()) {
    out << "GET key10375 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40292: put
  db->Put(write_options, "key8016", "value2815");

  // op 40293: get
  status = db->Get(read_options, "key10290", &value);
  if (status.ok()) {
    out << "GET key10290 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40294: put
  db->Put(write_options, "key5916", "value90864");

  // op 40295: get
  status = db->Get(read_options, "key5936", &value);
  if (status.ok()) {
    out << "GET key5936 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40296: put
  db->Put(write_options, "key1516", "value93889");

  // op 40297: put
  db->Put(write_options, "key8136", "value35790");

  // op 40298: put
  db->Put(write_options, "key0778", "value100152");

  // op 40299: delete_range
  status = db->DeleteRange(write_options, "key2213", "key10361");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40300: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7247", "key10071", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7247, key10071) size=" << scan_result.size() << std::endl;
  }

  // op 40301: put
  db->Put(write_options, "key1536", "value12479");

  // op 40302: delete_range
  status = db->DeleteRange(write_options, "key3769", "key11295");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40303: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 40304: get
  status = db->Get(read_options, "key0731", &value);
  if (status.ok()) {
    out << "GET key0731 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40305: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8471", "key9914", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8471, key9914) size=" << scan_result.size() << std::endl;
  }

  // op 40306: put
  db->Put(write_options, "key10029", "value48656");

  // op 40307: delete_range
  status = db->DeleteRange(write_options, "key0781", "key9063");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40308: put
  db->Put(write_options, "key0395", "value12745");

  // op 40309: put
  db->Put(write_options, "key6050", "value110727");

  // op 40310: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5959", "key9420", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5959, key9420) size=" << scan_result.size() << std::endl;
  }

  // op 40311: get
  status = db->Get(read_options, "key11429", &value);
  if (status.ok()) {
    out << "GET key11429 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40312: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5127", "key6221", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5127, key6221) size=" << scan_result.size() << std::endl;
  }

  // op 40313: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3636", "key9224", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3636, key9224) size=" << scan_result.size() << std::endl;
  }

  // op 40314: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6737", "key8667", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6737, key8667) size=" << scan_result.size() << std::endl;
  }

  // op 40315: put
  db->Put(write_options, "key5330", "value56563");

  // op 40316: put
  db->Put(write_options, "key0273", "value75255");

  // op 40317: put
  db->Put(write_options, "key7631", "value90850");

  // op 40318: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6534", "key8792", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6534, key8792) size=" << scan_result.size() << std::endl;
  }

  // op 40319: get
  status = db->Get(read_options, "key2212", &value);
  if (status.ok()) {
    out << "GET key2212 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40320: delete_range
  status = db->DeleteRange(write_options, "key6813", "key8749");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40321: get
  status = db->Get(read_options, "key1030", &value);
  if (status.ok()) {
    out << "GET key1030 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40322: get
  status = db->Get(read_options, "key7794", &value);
  if (status.ok()) {
    out << "GET key7794 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40323: put
  db->Put(write_options, "key10551", "value94052");

  // op 40324: put
  db->Put(write_options, "key1058", "value48877");

  // op 40325: put
  db->Put(write_options, "key9723", "value1911");

  // op 40326: put
  db->Put(write_options, "key8186", "value53880");

  // op 40327: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7948", "key8474", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7948, key8474) size=" << scan_result.size() << std::endl;
  }

  // op 40328: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9162", "key9763", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9162, key9763) size=" << scan_result.size() << std::endl;
  }

  // op 40329: delete
  db->Delete(write_options, "key7602");

  // op 40330: get
  status = db->Get(read_options, "key10170", &value);
  if (status.ok()) {
    out << "GET key10170 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40331: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7887", "key9661", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7887, key9661) size=" << scan_result.size() << std::endl;
  }

  // op 40332: get
  status = db->Get(read_options, "key8138", &value);
  if (status.ok()) {
    out << "GET key8138 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40333: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2881", "key6420", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2881, key6420) size=" << scan_result.size() << std::endl;
  }

  // op 40334: get
  status = db->Get(read_options, "key1121", &value);
  if (status.ok()) {
    out << "GET key1121 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40335: put
  db->Put(write_options, "key8538", "value26841");

  // op 40336: delete_range
  status = db->DeleteRange(write_options, "key0473", "key2703");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40337: put
  db->Put(write_options, "key5317", "value27403");

  // op 40338: delete_range
  status = db->DeleteRange(write_options, "key6017", "key9262");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40339: put
  db->Put(write_options, "key9435", "value76091");

  // op 40340: put
  db->Put(write_options, "key6918", "value107833");

  // op 40341: put
  db->Put(write_options, "key1433", "value111221");

  // op 40342: put
  db->Put(write_options, "key2517", "value56505");

  // op 40343: put
  db->Put(write_options, "key1858", "value67788");

  // op 40344: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1444", "key5216", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1444, key5216) size=" << scan_result.size() << std::endl;
  }

  // op 40345: put
  db->Put(write_options, "key1676", "value85289");

  // op 40346: delete_range
  status = db->DeleteRange(write_options, "key6506", "key8232");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40347: delete_range
  status = db->DeleteRange(write_options, "key1959", "key10328");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40348: get
  status = db->Get(read_options, "key9133", &value);
  if (status.ok()) {
    out << "GET key9133 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40349: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6085", "key6413", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6085, key6413) size=" << scan_result.size() << std::endl;
  }

  // op 40350: put
  db->Put(write_options, "key6085", "value72348");

  // op 40351: get
  status = db->Get(read_options, "key9556", &value);
  if (status.ok()) {
    out << "GET key9556 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40352: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2672", "key8735", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2672, key8735) size=" << scan_result.size() << std::endl;
  }

  // op 40353: get
  status = db->Get(read_options, "key1302", &value);
  if (status.ok()) {
    out << "GET key1302 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40354: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 40355: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9504", "key11577", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9504, key11577) size=" << scan_result.size() << std::endl;
  }

  // op 40356: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3233", "key6959", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3233, key6959) size=" << scan_result.size() << std::endl;
  }

  // op 40357: put
  db->Put(write_options, "key5040", "value1138");

  // op 40358: get
  status = db->Get(read_options, "key9288", &value);
  if (status.ok()) {
    out << "GET key9288 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40359: put
  db->Put(write_options, "key1406", "value15510");

  // op 40360: put
  db->Put(write_options, "key3500", "value91457");

  // op 40361: delete_range
  status = db->DeleteRange(write_options, "key1496", "key10710");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40362: delete
  db->Delete(write_options, "key9878");

  // op 40363: get
  status = db->Get(read_options, "key10120", &value);
  if (status.ok()) {
    out << "GET key10120 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40364: put
  db->Put(write_options, "key3259", "value105473");

  // op 40365: get
  status = db->Get(read_options, "key10261", &value);
  if (status.ok()) {
    out << "GET key10261 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40366: delete_range
  status = db->DeleteRange(write_options, "key3500", "key5055");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40367: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 40368: get
  status = db->Get(read_options, "key9726", &value);
  if (status.ok()) {
    out << "GET key9726 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40369: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3444", "key4850", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3444, key4850) size=" << scan_result.size() << std::endl;
  }

  // op 40370: delete
  db->Delete(write_options, "key8421");

  // op 40371: put
  db->Put(write_options, "key0675", "value103326");

  // op 40372: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4411", "key10301", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4411, key10301) size=" << scan_result.size() << std::endl;
  }

  // op 40373: put
  db->Put(write_options, "key0669", "value2120");

  // op 40374: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0495", "key1754", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0495, key1754) size=" << scan_result.size() << std::endl;
  }

  // op 40375: delete
  db->Delete(write_options, "key4933");

  // op 40376: put
  db->Put(write_options, "key6180", "value24748");

  // op 40377: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 40378: put
  db->Put(write_options, "key0744", "value29128");

  // op 40379: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0263", "key2531", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0263, key2531) size=" << scan_result.size() << std::endl;
  }

  // op 40380: put
  db->Put(write_options, "key7334", "value33872");

  // op 40381: get
  status = db->Get(read_options, "key4750", &value);
  if (status.ok()) {
    out << "GET key4750 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40382: put
  db->Put(write_options, "key0835", "value82000");

  // op 40383: delete
  db->Delete(write_options, "key0234");

  // op 40384: get
  status = db->Get(read_options, "key6765", &value);
  if (status.ok()) {
    out << "GET key6765 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40385: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3580", "key8322", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3580, key8322) size=" << scan_result.size() << std::endl;
  }

  // op 40386: put
  db->Put(write_options, "key0517", "value94224");

  // op 40387: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4495", "key8867", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4495, key8867) size=" << scan_result.size() << std::endl;
  }

  // op 40388: get
  status = db->Get(read_options, "key10232", &value);
  if (status.ok()) {
    out << "GET key10232 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40389: get
  status = db->Get(read_options, "key4721", &value);
  if (status.ok()) {
    out << "GET key4721 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40390: put
  db->Put(write_options, "key11175", "value15234");

  // op 40391: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2637", "key4874", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2637, key4874) size=" << scan_result.size() << std::endl;
  }

  // op 40392: put
  db->Put(write_options, "key1465", "value1834");

  // op 40393: put
  db->Put(write_options, "key11441", "value53722");

  // op 40394: get
  status = db->Get(read_options, "key6746", &value);
  if (status.ok()) {
    out << "GET key6746 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40395: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3950", "key11318", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3950, key11318) size=" << scan_result.size() << std::endl;
  }

  // op 40396: get
  status = db->Get(read_options, "key10713", &value);
  if (status.ok()) {
    out << "GET key10713 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40397: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 40398: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1333", "key10638", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1333, key10638) size=" << scan_result.size() << std::endl;
  }

  // op 40399: get
  status = db->Get(read_options, "key1475", &value);
  if (status.ok()) {
    out << "GET key1475 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40400: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5708", "key6958", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5708, key6958) size=" << scan_result.size() << std::endl;
  }

  // op 40401: put
  db->Put(write_options, "key0298", "value88393");

  // op 40402: put
  db->Put(write_options, "key10784", "value75583");

  // op 40403: put
  db->Put(write_options, "key11070", "value12866");

  // op 40404: delete_range
  status = db->DeleteRange(write_options, "key2230", "key8004");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40405: delete_range
  status = db->DeleteRange(write_options, "key7953", "key9416");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40406: put
  db->Put(write_options, "key10593", "value25694");

  // op 40407: delete
  db->Delete(write_options, "key8401");

  // op 40408: delete_range
  status = db->DeleteRange(write_options, "key0937", "key3185");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40409: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8657", "key9662", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8657, key9662) size=" << scan_result.size() << std::endl;
  }

  // op 40410: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 40411: put
  db->Put(write_options, "key6002", "value59522");

  // op 40412: put
  db->Put(write_options, "key10200", "value13949");

  // op 40413: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4704", "key5634", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4704, key5634) size=" << scan_result.size() << std::endl;
  }

  // op 40414: put
  db->Put(write_options, "key11718", "value77815");

  // op 40415: put
  db->Put(write_options, "key9499", "value90278");

  // op 40416: delete
  db->Delete(write_options, "key9646");

  // op 40417: put
  db->Put(write_options, "key2328", "value102359");

  // op 40418: delete
  db->Delete(write_options, "key2453");

  // op 40419: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 40420: put
  db->Put(write_options, "key6409", "value80911");

  // op 40421: put
  db->Put(write_options, "key6043", "value43520");

  // op 40422: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0077", "key4276", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0077, key4276) size=" << scan_result.size() << std::endl;
  }

  // op 40423: put
  db->Put(write_options, "key7945", "value5152");

  // op 40424: get
  status = db->Get(read_options, "key5084", &value);
  if (status.ok()) {
    out << "GET key5084 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40425: get
  status = db->Get(read_options, "key5560", &value);
  if (status.ok()) {
    out << "GET key5560 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40426: delete_range
  status = db->DeleteRange(write_options, "key8145", "key9135");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40427: put
  db->Put(write_options, "key0998", "value82711");

  // op 40428: put
  db->Put(write_options, "key6526", "value16231");

  // op 40429: delete_range
  status = db->DeleteRange(write_options, "key3354", "key8689");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40430: put
  db->Put(write_options, "key2541", "value48004");

  // op 40431: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3893", "key6192", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3893, key6192) size=" << scan_result.size() << std::endl;
  }

  // op 40432: get
  status = db->Get(read_options, "key10046", &value);
  if (status.ok()) {
    out << "GET key10046 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40433: put
  db->Put(write_options, "key5048", "value62604");

  // op 40434: put
  db->Put(write_options, "key8013", "value98104");

  // op 40435: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0703", "key10109", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0703, key10109) size=" << scan_result.size() << std::endl;
  }

  // op 40436: put
  db->Put(write_options, "key10164", "value87000");

  // op 40437: put
  db->Put(write_options, "key0439", "value108271");

  // op 40438: put
  db->Put(write_options, "key5311", "value82065");

  // op 40439: delete_range
  status = db->DeleteRange(write_options, "key0456", "key8024");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40440: put
  db->Put(write_options, "key2252", "value72864");

  // op 40441: get
  status = db->Get(read_options, "key9558", &value);
  if (status.ok()) {
    out << "GET key9558 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40442: get
  status = db->Get(read_options, "key10295", &value);
  if (status.ok()) {
    out << "GET key10295 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40443: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1030", "key10151", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1030, key10151) size=" << scan_result.size() << std::endl;
  }

  // op 40444: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 40445: delete_range
  status = db->DeleteRange(write_options, "key1929", "key4356");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40446: put
  db->Put(write_options, "key4750", "value54752");

  // op 40447: delete
  db->Delete(write_options, "key1589");

  // op 40448: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 40449: get
  status = db->Get(read_options, "key3866", &value);
  if (status.ok()) {
    out << "GET key3866 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40450: get
  status = db->Get(read_options, "key0562", &value);
  if (status.ok()) {
    out << "GET key0562 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40451: put
  db->Put(write_options, "key5144", "value74187");

  // op 40452: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 40453: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 40454: put
  db->Put(write_options, "key7951", "value25994");

  // op 40455: get
  status = db->Get(read_options, "key8828", &value);
  if (status.ok()) {
    out << "GET key8828 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40456: delete
  db->Delete(write_options, "key1937");

  // op 40457: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 40458: put
  db->Put(write_options, "key3281", "value10631");

  // op 40459: put
  db->Put(write_options, "key8061", "value8218");

  // op 40460: put
  db->Put(write_options, "key0599", "value36385");

  // op 40461: get
  status = db->Get(read_options, "key4747", &value);
  if (status.ok()) {
    out << "GET key4747 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40462: put
  db->Put(write_options, "key2149", "value11560");

  // op 40463: put
  db->Put(write_options, "key5486", "value71238");

  // op 40464: delete
  db->Delete(write_options, "key5100");

  // op 40465: put
  db->Put(write_options, "key11702", "value44096");

  // op 40466: put
  db->Put(write_options, "key7802", "value39876");

  // op 40467: put
  db->Put(write_options, "key0841", "value56207");

  // op 40468: delete
  db->Delete(write_options, "key8936");

  // op 40469: delete_range
  status = db->DeleteRange(write_options, "key3894", "key8569");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40470: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 40471: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0300", "key5311", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0300, key5311) size=" << scan_result.size() << std::endl;
  }

  // op 40472: delete_range
  status = db->DeleteRange(write_options, "key7021", "key10951");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40473: put
  db->Put(write_options, "key4231", "value72270");

  // op 40474: get
  status = db->Get(read_options, "key6083", &value);
  if (status.ok()) {
    out << "GET key6083 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40475: put
  db->Put(write_options, "key6588", "value43361");

  // op 40476: delete_range
  status = db->DeleteRange(write_options, "key0501", "key1532");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40477: get
  status = db->Get(read_options, "key0178", &value);
  if (status.ok()) {
    out << "GET key0178 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40478: delete_range
  status = db->DeleteRange(write_options, "key5780", "key9983");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40479: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1338", "key8725", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1338, key8725) size=" << scan_result.size() << std::endl;
  }

  // op 40480: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 40481: put
  db->Put(write_options, "key9782", "value83203");

  // op 40482: put
  db->Put(write_options, "key10186", "value101777");

  // op 40483: put
  db->Put(write_options, "key7636", "value12128");

  // op 40484: put
  db->Put(write_options, "key6869", "value91770");

  // op 40485: delete
  db->Delete(write_options, "key8862");

  // op 40486: get
  status = db->Get(read_options, "key1892", &value);
  if (status.ok()) {
    out << "GET key1892 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40487: put
  db->Put(write_options, "key6868", "value53811");

  // op 40488: put
  db->Put(write_options, "key9587", "value26510");

  // op 40489: get
  status = db->Get(read_options, "key7013", &value);
  if (status.ok()) {
    out << "GET key7013 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40490: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7605", "key7663", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7605, key7663) size=" << scan_result.size() << std::endl;
  }

  // op 40491: put
  db->Put(write_options, "key5431", "value25161");

  // op 40492: put
  db->Put(write_options, "key10523", "value67238");

  // op 40493: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5722", "key8388", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5722, key8388) size=" << scan_result.size() << std::endl;
  }

  // op 40494: get
  status = db->Get(read_options, "key2375", &value);
  if (status.ok()) {
    out << "GET key2375 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40495: put
  db->Put(write_options, "key5212", "value91351");

  // op 40496: get
  status = db->Get(read_options, "key5003", &value);
  if (status.ok()) {
    out << "GET key5003 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40497: delete_range
  status = db->DeleteRange(write_options, "key6488", "key10153");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40498: put
  db->Put(write_options, "key5498", "value24410");

  // op 40499: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 40500: put
  db->Put(write_options, "key11134", "value79631");

  // op 40501: put
  db->Put(write_options, "key7113", "value106589");

  // op 40502: put
  db->Put(write_options, "key3449", "value48597");

  // op 40503: put
  db->Put(write_options, "key6292", "value18083");

  // op 40504: put
  db->Put(write_options, "key6929", "value0176");

  // op 40505: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2449", "key7608", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2449, key7608) size=" << scan_result.size() << std::endl;
  }

  // op 40506: delete
  db->Delete(write_options, "key10809");

  // op 40507: put
  db->Put(write_options, "key1472", "value112556");

  // op 40508: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4088", "key10130", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4088, key10130) size=" << scan_result.size() << std::endl;
  }

  // op 40509: delete
  db->Delete(write_options, "key9348");

  // op 40510: delete
  db->Delete(write_options, "key9367");

  // op 40511: put
  db->Put(write_options, "key1539", "value58561");

  // op 40512: put
  db->Put(write_options, "key8146", "value5123");

  // op 40513: get
  status = db->Get(read_options, "key2455", &value);
  if (status.ok()) {
    out << "GET key2455 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40514: put
  db->Put(write_options, "key10532", "value25835");

  // op 40515: put
  db->Put(write_options, "key8107", "value35452");

  // op 40516: put
  db->Put(write_options, "key1577", "value3129");

  // op 40517: put
  db->Put(write_options, "key10355", "value57384");

  // op 40518: put
  db->Put(write_options, "key8561", "value0666");

  // op 40519: delete
  db->Delete(write_options, "key1361");

  // op 40520: get
  status = db->Get(read_options, "key1086", &value);
  if (status.ok()) {
    out << "GET key1086 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40521: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 40522: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7137", "key8925", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7137, key8925) size=" << scan_result.size() << std::endl;
  }

  // op 40523: delete_range
  status = db->DeleteRange(write_options, "key1067", "key6746");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40524: delete_range
  status = db->DeleteRange(write_options, "key2359", "key3304");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40525: put
  db->Put(write_options, "key6444", "value97700");

  // op 40526: delete_range
  status = db->DeleteRange(write_options, "key2958", "key7309");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40527: get
  status = db->Get(read_options, "key9275", &value);
  if (status.ok()) {
    out << "GET key9275 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40528: put
  db->Put(write_options, "key11270", "value86904");

  // op 40529: put
  db->Put(write_options, "key5340", "value52208");

  // op 40530: put
  db->Put(write_options, "key10145", "value94601");

  // op 40531: delete
  db->Delete(write_options, "key3093");

  // op 40532: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5588", "key8286", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5588, key8286) size=" << scan_result.size() << std::endl;
  }

  // op 40533: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2832", "key11589", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2832, key11589) size=" << scan_result.size() << std::endl;
  }

  // op 40534: get
  status = db->Get(read_options, "key9174", &value);
  if (status.ok()) {
    out << "GET key9174 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40535: put
  db->Put(write_options, "key4243", "value10208");

  // op 40536: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1947", "key11230", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1947, key11230) size=" << scan_result.size() << std::endl;
  }

  // op 40537: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 40538: get
  status = db->Get(read_options, "key9059", &value);
  if (status.ok()) {
    out << "GET key9059 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40539: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3686", "key10109", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3686, key10109) size=" << scan_result.size() << std::endl;
  }

  // op 40540: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3333", "key4436", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3333, key4436) size=" << scan_result.size() << std::endl;
  }

  // op 40541: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 40542: put
  db->Put(write_options, "key0621", "value117606");

  // op 40543: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0785", "key6914", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0785, key6914) size=" << scan_result.size() << std::endl;
  }

  // op 40544: delete_range
  status = db->DeleteRange(write_options, "key5876", "key9921");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40545: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0212", "key3180", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0212, key3180) size=" << scan_result.size() << std::endl;
  }

  // op 40546: put
  db->Put(write_options, "key10265", "value106535");

  // op 40547: put
  db->Put(write_options, "key11474", "value6226");

  // op 40548: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 40549: put
  db->Put(write_options, "key6924", "value16294");

  // op 40550: delete
  db->Delete(write_options, "key2540");

  // op 40551: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 40552: put
  db->Put(write_options, "key10224", "value87225");

  // op 40553: get
  status = db->Get(read_options, "key9413", &value);
  if (status.ok()) {
    out << "GET key9413 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40554: put
  db->Put(write_options, "key7931", "value8406");

  // op 40555: delete
  db->Delete(write_options, "key8628");

  // op 40556: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6694", "key8574", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6694, key8574) size=" << scan_result.size() << std::endl;
  }

  // op 40557: put
  db->Put(write_options, "key5583", "value45735");

  // op 40558: put
  db->Put(write_options, "key11607", "value22852");

  // op 40559: get
  status = db->Get(read_options, "key3569", &value);
  if (status.ok()) {
    out << "GET key3569 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40560: put
  db->Put(write_options, "key5429", "value0514");

  // op 40561: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0642", "key1659", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0642, key1659) size=" << scan_result.size() << std::endl;
  }

  // op 40562: get
  status = db->Get(read_options, "key3171", &value);
  if (status.ok()) {
    out << "GET key3171 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40563: put
  db->Put(write_options, "key5364", "value4378");

  // op 40564: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2401", "key10659", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2401, key10659) size=" << scan_result.size() << std::endl;
  }

  // op 40565: put
  db->Put(write_options, "key6909", "value42526");

  // op 40566: get
  status = db->Get(read_options, "key11397", &value);
  if (status.ok()) {
    out << "GET key11397 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40567: put
  db->Put(write_options, "key11286", "value116870");

  // op 40568: get
  status = db->Get(read_options, "key0140", &value);
  if (status.ok()) {
    out << "GET key0140 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40569: delete_range
  status = db->DeleteRange(write_options, "key10683", "key11125");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40570: delete_range
  status = db->DeleteRange(write_options, "key1367", "key8007");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40571: delete_range
  status = db->DeleteRange(write_options, "key1781", "key3460");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40572: get
  status = db->Get(read_options, "key6163", &value);
  if (status.ok()) {
    out << "GET key6163 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40573: get
  status = db->Get(read_options, "key10193", &value);
  if (status.ok()) {
    out << "GET key10193 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40574: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 40575: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2109", "key11166", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2109, key11166) size=" << scan_result.size() << std::endl;
  }

  // op 40576: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 40577: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4118", "key8551", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4118, key8551) size=" << scan_result.size() << std::endl;
  }

  // op 40578: delete
  db->Delete(write_options, "key5752");

  // op 40579: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8720", "key10662", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8720, key10662) size=" << scan_result.size() << std::endl;
  }

  // op 40580: get
  status = db->Get(read_options, "key9940", &value);
  if (status.ok()) {
    out << "GET key9940 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40581: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2933", "key5766", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2933, key5766) size=" << scan_result.size() << std::endl;
  }

  // op 40582: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3000", "key5650", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3000, key5650) size=" << scan_result.size() << std::endl;
  }

  // op 40583: delete
  db->Delete(write_options, "key2503");

  // op 40584: put
  db->Put(write_options, "key5699", "value21424");

  // op 40585: put
  db->Put(write_options, "key9264", "value54678");

  // op 40586: get
  status = db->Get(read_options, "key10917", &value);
  if (status.ok()) {
    out << "GET key10917 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40587: delete_range
  status = db->DeleteRange(write_options, "key9903", "key10493");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40588: delete_range
  status = db->DeleteRange(write_options, "key9972", "key11334");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40589: put
  db->Put(write_options, "key4124", "value64124");

  // op 40590: put
  db->Put(write_options, "key0132", "value22850");

  // op 40591: get
  status = db->Get(read_options, "key4897", &value);
  if (status.ok()) {
    out << "GET key4897 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40592: put
  db->Put(write_options, "key6897", "value47528");

  // op 40593: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7033", "key8782", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7033, key8782) size=" << scan_result.size() << std::endl;
  }

  // op 40594: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9184", "key10546", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9184, key10546) size=" << scan_result.size() << std::endl;
  }

  // op 40595: put
  db->Put(write_options, "key5331", "value25160");

  // op 40596: put
  db->Put(write_options, "key1981", "value50787");

  // op 40597: put
  db->Put(write_options, "key3656", "value31191");

  // op 40598: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7642", "key11516", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7642, key11516) size=" << scan_result.size() << std::endl;
  }

  // op 40599: put
  db->Put(write_options, "key7240", "value106217");

  // op 40600: get
  status = db->Get(read_options, "key9640", &value);
  if (status.ok()) {
    out << "GET key9640 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40601: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0641", "key3752", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0641, key3752) size=" << scan_result.size() << std::endl;
  }

  // op 40602: put
  db->Put(write_options, "key2471", "value55493");

  // op 40603: put
  db->Put(write_options, "key2358", "value47020");

  // op 40604: put
  db->Put(write_options, "key7316", "value102234");

  // op 40605: put
  db->Put(write_options, "key2876", "value30010");

  // op 40606: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 40607: put
  db->Put(write_options, "key2028", "value35470");

  // op 40608: get
  status = db->Get(read_options, "key5901", &value);
  if (status.ok()) {
    out << "GET key5901 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40609: put
  db->Put(write_options, "key4228", "value79964");

  // op 40610: get
  status = db->Get(read_options, "key7176", &value);
  if (status.ok()) {
    out << "GET key7176 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40611: delete
  db->Delete(write_options, "key10297");

  // op 40612: put
  db->Put(write_options, "key3220", "value29401");

  // op 40613: put
  db->Put(write_options, "key8910", "value22237");

  // op 40614: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10893", "key11280", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10893, key11280) size=" << scan_result.size() << std::endl;
  }

  // op 40615: get
  status = db->Get(read_options, "key2132", &value);
  if (status.ok()) {
    out << "GET key2132 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40616: put
  db->Put(write_options, "key1265", "value74200");

  // op 40617: delete
  db->Delete(write_options, "key1966");

  // op 40618: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6086", "key10932", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6086, key10932) size=" << scan_result.size() << std::endl;
  }

  // op 40619: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1773", "key5163", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1773, key5163) size=" << scan_result.size() << std::endl;
  }

  // op 40620: delete_range
  status = db->DeleteRange(write_options, "key5028", "key5378");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40621: get
  status = db->Get(read_options, "key4247", &value);
  if (status.ok()) {
    out << "GET key4247 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40622: put
  db->Put(write_options, "key0475", "value21195");

  // op 40623: get
  status = db->Get(read_options, "key4035", &value);
  if (status.ok()) {
    out << "GET key4035 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40624: put
  db->Put(write_options, "key5692", "value93265");

  // op 40625: get
  status = db->Get(read_options, "key8447", &value);
  if (status.ok()) {
    out << "GET key8447 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40626: delete_range
  status = db->DeleteRange(write_options, "key3371", "key11316");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40627: put
  db->Put(write_options, "key8366", "value50199");

  // op 40628: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9151", "key10942", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9151, key10942) size=" << scan_result.size() << std::endl;
  }

  // op 40629: put
  db->Put(write_options, "key10805", "value114243");

  // op 40630: delete_range
  status = db->DeleteRange(write_options, "key5130", "key6818");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40631: put
  db->Put(write_options, "key11435", "value46760");

  // op 40632: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0136", "key7453", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0136, key7453) size=" << scan_result.size() << std::endl;
  }

  // op 40633: put
  db->Put(write_options, "key3481", "value96300");

  // op 40634: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1027", "key2783", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1027, key2783) size=" << scan_result.size() << std::endl;
  }

  // op 40635: put
  db->Put(write_options, "key8433", "value23512");

  // op 40636: put
  db->Put(write_options, "key9119", "value115518");

  // op 40637: put
  db->Put(write_options, "key6956", "value101621");

  // op 40638: get
  status = db->Get(read_options, "key9695", &value);
  if (status.ok()) {
    out << "GET key9695 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40639: put
  db->Put(write_options, "key7377", "value49093");

  // op 40640: put
  db->Put(write_options, "key6386", "value76737");

  // op 40641: get
  status = db->Get(read_options, "key2672", &value);
  if (status.ok()) {
    out << "GET key2672 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40642: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 40643: delete_range
  status = db->DeleteRange(write_options, "key4887", "key11576");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40644: put
  db->Put(write_options, "key5267", "value105479");

  // op 40645: delete
  db->Delete(write_options, "key2470");

  // op 40646: put
  db->Put(write_options, "key7690", "value88759");

  // op 40647: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10016", "key10518", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10016, key10518) size=" << scan_result.size() << std::endl;
  }

  // op 40648: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9093", "key11239", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9093, key11239) size=" << scan_result.size() << std::endl;
  }

  // op 40649: put
  db->Put(write_options, "key4451", "value48467");

  // op 40650: delete_range
  status = db->DeleteRange(write_options, "key4468", "key6145");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40651: delete_range
  status = db->DeleteRange(write_options, "key1040", "key3256");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40652: put
  db->Put(write_options, "key1325", "value42908");

  // op 40653: get
  status = db->Get(read_options, "key8053", &value);
  if (status.ok()) {
    out << "GET key8053 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40654: put
  db->Put(write_options, "key10073", "value71017");

  // op 40655: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1479", "key10289", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1479, key10289) size=" << scan_result.size() << std::endl;
  }

  // op 40656: put
  db->Put(write_options, "key0912", "value77495");

  // op 40657: put
  db->Put(write_options, "key6934", "value39937");

  // op 40658: get
  status = db->Get(read_options, "key3262", &value);
  if (status.ok()) {
    out << "GET key3262 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40659: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8097", "key8182", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8097, key8182) size=" << scan_result.size() << std::endl;
  }

  // op 40660: delete
  db->Delete(write_options, "key2937");

  // op 40661: get
  status = db->Get(read_options, "key1808", &value);
  if (status.ok()) {
    out << "GET key1808 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40662: put
  db->Put(write_options, "key10552", "value115712");

  // op 40663: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0412", "key1457", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0412, key1457) size=" << scan_result.size() << std::endl;
  }

  // op 40664: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 40665: delete_range
  status = db->DeleteRange(write_options, "key9948", "key11586");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40666: delete_range
  status = db->DeleteRange(write_options, "key1110", "key3685");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40667: put
  db->Put(write_options, "key7720", "value38859");

  // op 40668: put
  db->Put(write_options, "key5410", "value104601");

  // op 40669: put
  db->Put(write_options, "key5720", "value95486");

  // op 40670: put
  db->Put(write_options, "key4536", "value99998");

  // op 40671: put
  db->Put(write_options, "key5268", "value108699");

  // op 40672: get
  status = db->Get(read_options, "key1797", &value);
  if (status.ok()) {
    out << "GET key1797 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40673: put
  db->Put(write_options, "key3850", "value67368");

  // op 40674: delete
  db->Delete(write_options, "key4852");

  // op 40675: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1368", "key6883", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1368, key6883) size=" << scan_result.size() << std::endl;
  }

  // op 40676: put
  db->Put(write_options, "key3399", "value115747");

  // op 40677: put
  db->Put(write_options, "key11593", "value67860");

  // op 40678: get
  status = db->Get(read_options, "key8130", &value);
  if (status.ok()) {
    out << "GET key8130 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40679: put
  db->Put(write_options, "key11073", "value0243");

  // op 40680: delete_range
  status = db->DeleteRange(write_options, "key4033", "key10087");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40681: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 40682: put
  db->Put(write_options, "key5895", "value21641");

  // op 40683: put
  db->Put(write_options, "key10687", "value62772");

  // op 40684: put
  db->Put(write_options, "key4679", "value8167");

  // op 40685: get
  status = db->Get(read_options, "key5372", &value);
  if (status.ok()) {
    out << "GET key5372 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40686: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4978", "key8540", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4978, key8540) size=" << scan_result.size() << std::endl;
  }

  // op 40687: get
  status = db->Get(read_options, "key1364", &value);
  if (status.ok()) {
    out << "GET key1364 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40688: put
  db->Put(write_options, "key4195", "value16571");

  // op 40689: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0642", "key4857", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0642, key4857) size=" << scan_result.size() << std::endl;
  }

  // op 40690: put
  db->Put(write_options, "key2529", "value111709");

  // op 40691: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6398", "key6764", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6398, key6764) size=" << scan_result.size() << std::endl;
  }

  // op 40692: delete_range
  status = db->DeleteRange(write_options, "key5284", "key11349");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40693: delete_range
  status = db->DeleteRange(write_options, "key0247", "key7494");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40694: put
  db->Put(write_options, "key4174", "value92035");

  // op 40695: get
  status = db->Get(read_options, "key2685", &value);
  if (status.ok()) {
    out << "GET key2685 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40696: put
  db->Put(write_options, "key6307", "value74591");

  // op 40697: put
  db->Put(write_options, "key1707", "value76410");

  // op 40698: get
  status = db->Get(read_options, "key2502", &value);
  if (status.ok()) {
    out << "GET key2502 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40699: put
  db->Put(write_options, "key0375", "value60651");

  // op 40700: get
  status = db->Get(read_options, "key8598", &value);
  if (status.ok()) {
    out << "GET key8598 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40701: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 40702: put
  db->Put(write_options, "key0865", "value11058");

  // op 40703: get
  status = db->Get(read_options, "key4593", &value);
  if (status.ok()) {
    out << "GET key4593 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40704: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4031", "key6397", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4031, key6397) size=" << scan_result.size() << std::endl;
  }

  // op 40705: get
  status = db->Get(read_options, "key9630", &value);
  if (status.ok()) {
    out << "GET key9630 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40706: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3803", "key9644", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3803, key9644) size=" << scan_result.size() << std::endl;
  }

  // op 40707: put
  db->Put(write_options, "key6004", "value30350");

  // op 40708: delete_range
  status = db->DeleteRange(write_options, "key5229", "key8464");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40709: put
  db->Put(write_options, "key7045", "value25148");

  // op 40710: put
  db->Put(write_options, "key10141", "value52506");

  // op 40711: get
  status = db->Get(read_options, "key6753", &value);
  if (status.ok()) {
    out << "GET key6753 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40712: put
  db->Put(write_options, "key2832", "value76366");

  // op 40713: put
  db->Put(write_options, "key0117", "value51761");

  // op 40714: delete
  db->Delete(write_options, "key2617");

  // op 40715: delete
  db->Delete(write_options, "key5428");

  // op 40716: delete
  db->Delete(write_options, "key9235");

  // op 40717: get
  status = db->Get(read_options, "key4951", &value);
  if (status.ok()) {
    out << "GET key4951 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40718: put
  db->Put(write_options, "key7279", "value34298");

  // op 40719: get
  status = db->Get(read_options, "key11592", &value);
  if (status.ok()) {
    out << "GET key11592 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40720: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 40721: delete
  db->Delete(write_options, "key2338");

  // op 40722: get
  status = db->Get(read_options, "key2831", &value);
  if (status.ok()) {
    out << "GET key2831 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40723: put
  db->Put(write_options, "key3449", "value41082");

  // op 40724: put
  db->Put(write_options, "key6344", "value40263");

  // op 40725: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0523", "key1517", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0523, key1517) size=" << scan_result.size() << std::endl;
  }

  // op 40726: delete
  db->Delete(write_options, "key0072");

  // op 40727: put
  db->Put(write_options, "key1722", "value25718");

  // op 40728: get
  status = db->Get(read_options, "key6895", &value);
  if (status.ok()) {
    out << "GET key6895 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40729: put
  db->Put(write_options, "key8319", "value60506");

  // op 40730: get
  status = db->Get(read_options, "key5709", &value);
  if (status.ok()) {
    out << "GET key5709 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40731: put
  db->Put(write_options, "key10924", "value79005");

  // op 40732: get
  status = db->Get(read_options, "key6523", &value);
  if (status.ok()) {
    out << "GET key6523 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40733: delete_range
  status = db->DeleteRange(write_options, "key0835", "key10081");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40734: put
  db->Put(write_options, "key1364", "value28398");

  // op 40735: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 40736: delete_range
  status = db->DeleteRange(write_options, "key4945", "key11647");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40737: put
  db->Put(write_options, "key1575", "value90432");

  // op 40738: delete_range
  status = db->DeleteRange(write_options, "key2330", "key2599");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40739: put
  db->Put(write_options, "key2194", "value86817");

  // op 40740: delete
  db->Delete(write_options, "key7190");

  // op 40741: get
  status = db->Get(read_options, "key5969", &value);
  if (status.ok()) {
    out << "GET key5969 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40742: get
  status = db->Get(read_options, "key9880", &value);
  if (status.ok()) {
    out << "GET key9880 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40743: put
  db->Put(write_options, "key11648", "value9053");

  // op 40744: put
  db->Put(write_options, "key8758", "value65455");

  // op 40745: put
  db->Put(write_options, "key4537", "value113164");

  // op 40746: put
  db->Put(write_options, "key3420", "value111947");

  // op 40747: delete_range
  status = db->DeleteRange(write_options, "key9784", "key11131");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40748: put
  db->Put(write_options, "key7574", "value74621");

  // op 40749: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0054", "key6560", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0054, key6560) size=" << scan_result.size() << std::endl;
  }

  // op 40750: delete
  db->Delete(write_options, "key5963");

  // op 40751: delete
  db->Delete(write_options, "key10432");

  // op 40752: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8432", "key10127", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8432, key10127) size=" << scan_result.size() << std::endl;
  }

  // op 40753: put
  db->Put(write_options, "key5392", "value28037");

  // op 40754: delete_range
  status = db->DeleteRange(write_options, "key2723", "key6485");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40755: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 40756: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1786", "key4882", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1786, key4882) size=" << scan_result.size() << std::endl;
  }

  // op 40757: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3535", "key8505", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3535, key8505) size=" << scan_result.size() << std::endl;
  }

  // op 40758: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2833", "key7820", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2833, key7820) size=" << scan_result.size() << std::endl;
  }

  // op 40759: get
  status = db->Get(read_options, "key4175", &value);
  if (status.ok()) {
    out << "GET key4175 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40760: delete_range
  status = db->DeleteRange(write_options, "key3392", "key4468");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40761: put
  db->Put(write_options, "key5479", "value66607");

  // op 40762: put
  db->Put(write_options, "key9650", "value9833");

  // op 40763: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 40764: put
  db->Put(write_options, "key4059", "value37223");

  // op 40765: put
  db->Put(write_options, "key1767", "value34965");

  // op 40766: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9249", "key9637", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9249, key9637) size=" << scan_result.size() << std::endl;
  }

  // op 40767: put
  db->Put(write_options, "key3790", "value51818");

  // op 40768: put
  db->Put(write_options, "key10928", "value72436");

  // op 40769: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4716", "key10606", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4716, key10606) size=" << scan_result.size() << std::endl;
  }

  // op 40770: put
  db->Put(write_options, "key0299", "value101129");

  // op 40771: put
  db->Put(write_options, "key2875", "value108204");

  // op 40772: get
  status = db->Get(read_options, "key10260", &value);
  if (status.ok()) {
    out << "GET key10260 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40773: put
  db->Put(write_options, "key4927", "value0793");

  // op 40774: put
  db->Put(write_options, "key6533", "value70608");

  // op 40775: put
  db->Put(write_options, "key2369", "value17659");

  // op 40776: get
  status = db->Get(read_options, "key6774", &value);
  if (status.ok()) {
    out << "GET key6774 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40777: put
  db->Put(write_options, "key11196", "value48950");

  // op 40778: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0202", "key9528", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0202, key9528) size=" << scan_result.size() << std::endl;
  }

  // op 40779: put
  db->Put(write_options, "key9038", "value21400");

  // op 40780: delete_range
  status = db->DeleteRange(write_options, "key1210", "key4211");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40781: put
  db->Put(write_options, "key2553", "value39238");

  // op 40782: delete_range
  status = db->DeleteRange(write_options, "key9657", "key11648");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40783: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0051", "key3560", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0051, key3560) size=" << scan_result.size() << std::endl;
  }

  // op 40784: delete_range
  status = db->DeleteRange(write_options, "key6931", "key11569");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40785: delete
  db->Delete(write_options, "key6989");

  // op 40786: delete
  db->Delete(write_options, "key4481");

  // op 40787: put
  db->Put(write_options, "key6503", "value60556");

  // op 40788: delete
  db->Delete(write_options, "key3042");

  // op 40789: put
  db->Put(write_options, "key7701", "value27862");

  // op 40790: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2924", "key11537", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2924, key11537) size=" << scan_result.size() << std::endl;
  }

  // op 40791: get
  status = db->Get(read_options, "key6755", &value);
  if (status.ok()) {
    out << "GET key6755 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40792: delete
  db->Delete(write_options, "key5894");

  // op 40793: put
  db->Put(write_options, "key6675", "value4102");

  // op 40794: get
  status = db->Get(read_options, "key1918", &value);
  if (status.ok()) {
    out << "GET key1918 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40795: put
  db->Put(write_options, "key5738", "value105409");

  // op 40796: delete_range
  status = db->DeleteRange(write_options, "key0438", "key9986");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40797: delete
  db->Delete(write_options, "key7574");

  // op 40798: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 40799: get
  status = db->Get(read_options, "key4571", &value);
  if (status.ok()) {
    out << "GET key4571 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40800: delete_range
  status = db->DeleteRange(write_options, "key5558", "key7451");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40801: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3525", "key11023", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3525, key11023) size=" << scan_result.size() << std::endl;
  }

  // op 40802: put
  db->Put(write_options, "key0719", "value38384");

  // op 40803: delete
  db->Delete(write_options, "key9477");

  // op 40804: get
  status = db->Get(read_options, "key4018", &value);
  if (status.ok()) {
    out << "GET key4018 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40805: put
  db->Put(write_options, "key2114", "value53992");

  // op 40806: put
  db->Put(write_options, "key11694", "value117668");

  // op 40807: put
  db->Put(write_options, "key7647", "value99384");

  // op 40808: put
  db->Put(write_options, "key10971", "value70506");

  // op 40809: put
  db->Put(write_options, "key11558", "value63771");

  // op 40810: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3398", "key7225", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3398, key7225) size=" << scan_result.size() << std::endl;
  }

  // op 40811: put
  db->Put(write_options, "key4189", "value56184");

  // op 40812: delete
  db->Delete(write_options, "key0541");

  // op 40813: delete
  db->Delete(write_options, "key8545");

  // op 40814: put
  db->Put(write_options, "key0154", "value34216");

  // op 40815: delete_range
  status = db->DeleteRange(write_options, "key0413", "key2430");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40816: delete
  db->Delete(write_options, "key7799");

  // op 40817: delete
  db->Delete(write_options, "key10854");

  // op 40818: delete
  db->Delete(write_options, "key1582");

  // op 40819: get
  status = db->Get(read_options, "key0699", &value);
  if (status.ok()) {
    out << "GET key0699 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40820: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3168", "key3926", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3168, key3926) size=" << scan_result.size() << std::endl;
  }

  // op 40821: put
  db->Put(write_options, "key9239", "value82111");

  // op 40822: put
  db->Put(write_options, "key0158", "value15108");

  // op 40823: get
  status = db->Get(read_options, "key4978", &value);
  if (status.ok()) {
    out << "GET key4978 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40824: put
  db->Put(write_options, "key5503", "value111984");

  // op 40825: delete_range
  status = db->DeleteRange(write_options, "key0963", "key8203");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40826: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9773", "key10443", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9773, key10443) size=" << scan_result.size() << std::endl;
  }

  // op 40827: delete
  db->Delete(write_options, "key3126");

  // op 40828: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0273", "key3209", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0273, key3209) size=" << scan_result.size() << std::endl;
  }

  // op 40829: put
  db->Put(write_options, "key3840", "value62647");

  // op 40830: delete
  db->Delete(write_options, "key8619");

  // op 40831: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4516", "key6751", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4516, key6751) size=" << scan_result.size() << std::endl;
  }

  // op 40832: get
  status = db->Get(read_options, "key4883", &value);
  if (status.ok()) {
    out << "GET key4883 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40833: put
  db->Put(write_options, "key2421", "value39336");

  // op 40834: get
  status = db->Get(read_options, "key11237", &value);
  if (status.ok()) {
    out << "GET key11237 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40835: put
  db->Put(write_options, "key10398", "value80651");

  // op 40836: put
  db->Put(write_options, "key1816", "value26045");

  // op 40837: delete_range
  status = db->DeleteRange(write_options, "key0070", "key0368");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40838: delete_range
  status = db->DeleteRange(write_options, "key0268", "key4386");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40839: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2323", "key7111", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2323, key7111) size=" << scan_result.size() << std::endl;
  }

  // op 40840: delete_range
  status = db->DeleteRange(write_options, "key5185", "key11327");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40841: delete_range
  status = db->DeleteRange(write_options, "key3861", "key8656");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40842: put
  db->Put(write_options, "key7032", "value31771");

  // op 40843: put
  db->Put(write_options, "key4995", "value34770");

  // op 40844: put
  db->Put(write_options, "key11671", "value55489");

  // op 40845: delete_range
  status = db->DeleteRange(write_options, "key7249", "key10896");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40846: get
  status = db->Get(read_options, "key5308", &value);
  if (status.ok()) {
    out << "GET key5308 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40847: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 40848: put
  db->Put(write_options, "key8198", "value109433");

  // op 40849: get
  status = db->Get(read_options, "key1851", &value);
  if (status.ok()) {
    out << "GET key1851 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40850: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 40851: get
  status = db->Get(read_options, "key5684", &value);
  if (status.ok()) {
    out << "GET key5684 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40852: get
  status = db->Get(read_options, "key0763", &value);
  if (status.ok()) {
    out << "GET key0763 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40853: delete_range
  status = db->DeleteRange(write_options, "key4597", "key10209");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40854: delete
  db->Delete(write_options, "key7651");

  // op 40855: put
  db->Put(write_options, "key6089", "value1223");

  // op 40856: put
  db->Put(write_options, "key1318", "value97833");

  // op 40857: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8721", "key8900", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8721, key8900) size=" << scan_result.size() << std::endl;
  }

  // op 40858: put
  db->Put(write_options, "key3692", "value1593");

  // op 40859: get
  status = db->Get(read_options, "key2965", &value);
  if (status.ok()) {
    out << "GET key2965 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40860: put
  db->Put(write_options, "key6223", "value63648");

  // op 40861: get
  status = db->Get(read_options, "key5186", &value);
  if (status.ok()) {
    out << "GET key5186 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40862: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0026", "key0210", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0026, key0210) size=" << scan_result.size() << std::endl;
  }

  // op 40863: put
  db->Put(write_options, "key11538", "value116051");

  // op 40864: put
  db->Put(write_options, "key3472", "value12597");

  // op 40865: delete_range
  status = db->DeleteRange(write_options, "key8140", "key10656");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40866: get
  status = db->Get(read_options, "key5643", &value);
  if (status.ok()) {
    out << "GET key5643 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40867: delete
  db->Delete(write_options, "key7216");

  // op 40868: put
  db->Put(write_options, "key9769", "value106759");

  // op 40869: get
  status = db->Get(read_options, "key3130", &value);
  if (status.ok()) {
    out << "GET key3130 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40870: delete_range
  status = db->DeleteRange(write_options, "key5583", "key10601");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40871: put
  db->Put(write_options, "key1683", "value97035");

  // op 40872: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 40873: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1493", "key7544", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1493, key7544) size=" << scan_result.size() << std::endl;
  }

  // op 40874: delete_range
  status = db->DeleteRange(write_options, "key4168", "key10672");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40875: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3637", "key8942", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3637, key8942) size=" << scan_result.size() << std::endl;
  }

  // op 40876: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1565", "key1693", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1565, key1693) size=" << scan_result.size() << std::endl;
  }

  // op 40877: delete
  db->Delete(write_options, "key6826");

  // op 40878: put
  db->Put(write_options, "key11523", "value51682");

  // op 40879: put
  db->Put(write_options, "key4464", "value78473");

  // op 40880: delete
  db->Delete(write_options, "key9454");

  // op 40881: put
  db->Put(write_options, "key2903", "value25541");

  // op 40882: put
  db->Put(write_options, "key9267", "value53464");

  // op 40883: delete_range
  status = db->DeleteRange(write_options, "key2502", "key10664");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40884: get
  status = db->Get(read_options, "key4616", &value);
  if (status.ok()) {
    out << "GET key4616 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40885: put
  db->Put(write_options, "key11374", "value91295");

  // op 40886: get
  status = db->Get(read_options, "key0841", &value);
  if (status.ok()) {
    out << "GET key0841 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40887: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1376", "key6414", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1376, key6414) size=" << scan_result.size() << std::endl;
  }

  // op 40888: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0448", "key1016", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0448, key1016) size=" << scan_result.size() << std::endl;
  }

  // op 40889: put
  db->Put(write_options, "key5999", "value57919");

  // op 40890: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1308", "key8556", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1308, key8556) size=" << scan_result.size() << std::endl;
  }

  // op 40891: put
  db->Put(write_options, "key4415", "value112960");

  // op 40892: delete
  db->Delete(write_options, "key6179");

  // op 40893: delete
  db->Delete(write_options, "key4627");

  // op 40894: get
  status = db->Get(read_options, "key11050", &value);
  if (status.ok()) {
    out << "GET key11050 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40895: put
  db->Put(write_options, "key7613", "value39291");

  // op 40896: put
  db->Put(write_options, "key10469", "value70972");

  // op 40897: delete
  db->Delete(write_options, "key6476");

  // op 40898: put
  db->Put(write_options, "key5169", "value94642");

  // op 40899: put
  db->Put(write_options, "key3749", "value69996");

  // op 40900: get
  status = db->Get(read_options, "key1318", &value);
  if (status.ok()) {
    out << "GET key1318 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40901: put
  db->Put(write_options, "key4440", "value63734");

  // op 40902: get
  status = db->Get(read_options, "key2415", &value);
  if (status.ok()) {
    out << "GET key2415 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40903: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8943", "key9418", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8943, key9418) size=" << scan_result.size() << std::endl;
  }

  // op 40904: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1694", "key1907", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1694, key1907) size=" << scan_result.size() << std::endl;
  }

  // op 40905: delete
  db->Delete(write_options, "key7425");

  // op 40906: delete_range
  status = db->DeleteRange(write_options, "key9643", "key10893");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40907: delete_range
  status = db->DeleteRange(write_options, "key4559", "key5441");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40908: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 40909: put
  db->Put(write_options, "key1728", "value37268");

  // op 40910: get
  status = db->Get(read_options, "key9949", &value);
  if (status.ok()) {
    out << "GET key9949 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40911: get
  status = db->Get(read_options, "key9321", &value);
  if (status.ok()) {
    out << "GET key9321 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40912: put
  db->Put(write_options, "key6497", "value65010");

  // op 40913: delete
  db->Delete(write_options, "key2421");

  // op 40914: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9286", "key10198", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9286, key10198) size=" << scan_result.size() << std::endl;
  }

  // op 40915: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8042", "key9073", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8042, key9073) size=" << scan_result.size() << std::endl;
  }

  // op 40916: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 40917: delete_range
  status = db->DeleteRange(write_options, "key0451", "key3841");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40918: get
  status = db->Get(read_options, "key6987", &value);
  if (status.ok()) {
    out << "GET key6987 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40919: put
  db->Put(write_options, "key2995", "value44959");

  // op 40920: delete_range
  status = db->DeleteRange(write_options, "key7712", "key11451");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40921: get
  status = db->Get(read_options, "key5841", &value);
  if (status.ok()) {
    out << "GET key5841 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40922: get
  status = db->Get(read_options, "key0942", &value);
  if (status.ok()) {
    out << "GET key0942 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40923: delete
  db->Delete(write_options, "key11173");

  // op 40924: get
  status = db->Get(read_options, "key7231", &value);
  if (status.ok()) {
    out << "GET key7231 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40925: put
  db->Put(write_options, "key6926", "value106199");

  // op 40926: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 40927: get
  status = db->Get(read_options, "key8169", &value);
  if (status.ok()) {
    out << "GET key8169 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40928: delete
  db->Delete(write_options, "key9232");

  // op 40929: delete
  db->Delete(write_options, "key5502");

  // op 40930: put
  db->Put(write_options, "key5086", "value1880");

  // op 40931: put
  db->Put(write_options, "key0372", "value33309");

  // op 40932: delete_range
  status = db->DeleteRange(write_options, "key9822", "key10952");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40933: put
  db->Put(write_options, "key6015", "value61371");

  // op 40934: put
  db->Put(write_options, "key1169", "value79925");

  // op 40935: put
  db->Put(write_options, "key4552", "value9800");

  // op 40936: put
  db->Put(write_options, "key0832", "value33267");

  // op 40937: put
  db->Put(write_options, "key9501", "value116276");

  // op 40938: delete_range
  status = db->DeleteRange(write_options, "key4753", "key7673");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40939: put
  db->Put(write_options, "key3685", "value51707");

  // op 40940: put
  db->Put(write_options, "key8648", "value100094");

  // op 40941: delete
  db->Delete(write_options, "key9351");

  // op 40942: put
  db->Put(write_options, "key4771", "value102383");

  // op 40943: get
  status = db->Get(read_options, "key2921", &value);
  if (status.ok()) {
    out << "GET key2921 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40944: put
  db->Put(write_options, "key8848", "value117944");

  // op 40945: delete_range
  status = db->DeleteRange(write_options, "key2296", "key8338");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40946: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 40947: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5268", "key5422", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5268, key5422) size=" << scan_result.size() << std::endl;
  }

  // op 40948: put
  db->Put(write_options, "key3350", "value19907");

  // op 40949: get
  status = db->Get(read_options, "key0225", &value);
  if (status.ok()) {
    out << "GET key0225 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40950: delete
  db->Delete(write_options, "key5650");

  // op 40951: get
  status = db->Get(read_options, "key1627", &value);
  if (status.ok()) {
    out << "GET key1627 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40952: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4930", "key9747", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4930, key9747) size=" << scan_result.size() << std::endl;
  }

  // op 40953: put
  db->Put(write_options, "key1351", "value74994");

  // op 40954: put
  db->Put(write_options, "key1394", "value76285");

  // op 40955: delete
  db->Delete(write_options, "key1802");

  // op 40956: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1287", "key11231", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1287, key11231) size=" << scan_result.size() << std::endl;
  }

  // op 40957: get
  status = db->Get(read_options, "key1637", &value);
  if (status.ok()) {
    out << "GET key1637 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40958: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 40959: put
  db->Put(write_options, "key5845", "value114661");

  // op 40960: put
  db->Put(write_options, "key6901", "value107619");

  // op 40961: put
  db->Put(write_options, "key11693", "value81102");

  // op 40962: delete
  db->Delete(write_options, "key3912");

  // op 40963: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6182", "key8250", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6182, key8250) size=" << scan_result.size() << std::endl;
  }

  // op 40964: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4093", "key4432", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4093, key4432) size=" << scan_result.size() << std::endl;
  }

  // op 40965: put
  db->Put(write_options, "key1537", "value1296");

  // op 40966: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4377", "key6334", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4377, key6334) size=" << scan_result.size() << std::endl;
  }

  // op 40967: get
  status = db->Get(read_options, "key2471", &value);
  if (status.ok()) {
    out << "GET key2471 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40968: delete
  db->Delete(write_options, "key3309");

  // op 40969: put
  db->Put(write_options, "key8921", "value18411");

  // op 40970: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1199", "key8499", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1199, key8499) size=" << scan_result.size() << std::endl;
  }

  // op 40971: delete
  db->Delete(write_options, "key5110");

  // op 40972: get
  status = db->Get(read_options, "key10562", &value);
  if (status.ok()) {
    out << "GET key10562 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40973: delete_range
  status = db->DeleteRange(write_options, "key2627", "key9375");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40974: put
  db->Put(write_options, "key4040", "value2918");

  // op 40975: get
  status = db->Get(read_options, "key6773", &value);
  if (status.ok()) {
    out << "GET key6773 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40976: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3786", "key6490", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3786, key6490) size=" << scan_result.size() << std::endl;
  }

  // op 40977: put
  db->Put(write_options, "key4986", "value109469");

  // op 40978: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3063", "key5336", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3063, key5336) size=" << scan_result.size() << std::endl;
  }

  // op 40979: delete_range
  status = db->DeleteRange(write_options, "key5722", "key10256");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40980: put
  db->Put(write_options, "key6787", "value12587");

  // op 40981: put
  db->Put(write_options, "key1412", "value73494");

  // op 40982: put
  db->Put(write_options, "key5998", "value56534");

  // op 40983: put
  db->Put(write_options, "key7786", "value86751");

  // op 40984: delete_range
  status = db->DeleteRange(write_options, "key3515", "key6463");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40985: put
  db->Put(write_options, "key4956", "value59373");

  // op 40986: put
  db->Put(write_options, "key9482", "value74197");

  // op 40987: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0047", "key1131", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0047, key1131) size=" << scan_result.size() << std::endl;
  }

  // op 40988: delete
  db->Delete(write_options, "key10330");

  // op 40989: delete
  db->Delete(write_options, "key2353");

  // op 40990: put
  db->Put(write_options, "key6887", "value12850");

  // op 40991: get
  status = db->Get(read_options, "key6384", &value);
  if (status.ok()) {
    out << "GET key6384 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40992: put
  db->Put(write_options, "key4670", "value25727");

  // op 40993: delete_range
  status = db->DeleteRange(write_options, "key5856", "key5973");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 40994: put
  db->Put(write_options, "key2316", "value23511");

  // op 40995: get
  status = db->Get(read_options, "key6725", &value);
  if (status.ok()) {
    out << "GET key6725 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 40996: put
  db->Put(write_options, "key10636", "value91088");

  // op 40997: put
  db->Put(write_options, "key11172", "value3899");

  // op 40998: put
  db->Put(write_options, "key8531", "value78733");

  // op 40999: put
  db->Put(write_options, "key5287", "value2792");

  // op 41000: put
  db->Put(write_options, "key5013", "value60939");

  // op 41001: put
  db->Put(write_options, "key7625", "value89671");

  // op 41002: put
  db->Put(write_options, "key5613", "value50955");

  // op 41003: delete_range
  status = db->DeleteRange(write_options, "key6423", "key10945");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 41004: put
  db->Put(write_options, "key2896", "value18249");

  // op 41005: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9424", "key9489", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9424, key9489) size=" << scan_result.size() << std::endl;
  }

  // op 41006: get
  status = db->Get(read_options, "key7005", &value);
  if (status.ok()) {
    out << "GET key7005 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41007: get
  status = db->Get(read_options, "key5597", &value);
  if (status.ok()) {
    out << "GET key5597 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41008: get
  status = db->Get(read_options, "key0313", &value);
  if (status.ok()) {
    out << "GET key0313 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41009: put
  db->Put(write_options, "key7721", "value58855");

  // op 41010: get
  status = db->Get(read_options, "key1402", &value);
  if (status.ok()) {
    out << "GET key1402 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41011: put
  db->Put(write_options, "key8441", "value48136");

  // op 41012: put
  db->Put(write_options, "key5113", "value43984");

  // op 41013: get
  status = db->Get(read_options, "key1168", &value);
  if (status.ok()) {
    out << "GET key1168 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41014: put
  db->Put(write_options, "key11031", "value9597");

  // op 41015: delete_range
  status = db->DeleteRange(write_options, "key6186", "key8406");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 41016: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6639", "key9937", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6639, key9937) size=" << scan_result.size() << std::endl;
  }

  // op 41017: put
  db->Put(write_options, "key5046", "value13561");

  // op 41018: put
  db->Put(write_options, "key1682", "value20471");

  // op 41019: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 41020: get
  status = db->Get(read_options, "key4135", &value);
  if (status.ok()) {
    out << "GET key4135 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41021: delete_range
  status = db->DeleteRange(write_options, "key0806", "key7961");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 41022: put
  db->Put(write_options, "key7348", "value53767");

  // op 41023: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10519", "key11361", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10519, key11361) size=" << scan_result.size() << std::endl;
  }

  // op 41024: delete
  db->Delete(write_options, "key0670");

  // op 41025: delete_range
  status = db->DeleteRange(write_options, "key2764", "key10684");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 41026: put
  db->Put(write_options, "key8621", "value101531");

  // op 41027: get
  status = db->Get(read_options, "key10113", &value);
  if (status.ok()) {
    out << "GET key10113 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41028: put
  db->Put(write_options, "key7500", "value95766");

  // op 41029: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3429", "key5753", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3429, key5753) size=" << scan_result.size() << std::endl;
  }

  // op 41030: delete
  db->Delete(write_options, "key1636");

  // op 41031: get
  status = db->Get(read_options, "key11359", &value);
  if (status.ok()) {
    out << "GET key11359 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41032: get
  status = db->Get(read_options, "key7874", &value);
  if (status.ok()) {
    out << "GET key7874 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41033: get
  status = db->Get(read_options, "key0402", &value);
  if (status.ok()) {
    out << "GET key0402 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41034: delete
  db->Delete(write_options, "key4536");

  // op 41035: delete_range
  status = db->DeleteRange(write_options, "key2770", "key10130");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 41036: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 41037: get
  status = db->Get(read_options, "key10221", &value);
  if (status.ok()) {
    out << "GET key10221 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41038: put
  db->Put(write_options, "key8910", "value61504");

  // op 41039: get
  status = db->Get(read_options, "key5720", &value);
  if (status.ok()) {
    out << "GET key5720 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41040: delete
  db->Delete(write_options, "key0623");

  // op 41041: put
  db->Put(write_options, "key1292", "value109855");

  // op 41042: delete_range
  status = db->DeleteRange(write_options, "key3119", "key3352");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 41043: get
  status = db->Get(read_options, "key0025", &value);
  if (status.ok()) {
    out << "GET key0025 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41044: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1975", "key3255", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1975, key3255) size=" << scan_result.size() << std::endl;
  }

  // op 41045: get
  status = db->Get(read_options, "key9834", &value);
  if (status.ok()) {
    out << "GET key9834 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41046: put
  db->Put(write_options, "key1083", "value117689");

  // op 41047: put
  db->Put(write_options, "key0044", "value104882");

  // op 41048: put
  db->Put(write_options, "key2008", "value43673");

  // op 41049: delete_range
  status = db->DeleteRange(write_options, "key3127", "key7815");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 41050: put
  db->Put(write_options, "key8214", "value62790");

  // op 41051: delete_range
  status = db->DeleteRange(write_options, "key2363", "key9007");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 41052: get
  status = db->Get(read_options, "key2582", &value);
  if (status.ok()) {
    out << "GET key2582 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41053: get
  status = db->Get(read_options, "key4432", &value);
  if (status.ok()) {
    out << "GET key4432 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41054: get
  status = db->Get(read_options, "key5255", &value);
  if (status.ok()) {
    out << "GET key5255 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41055: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7240", "key10093", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7240, key10093) size=" << scan_result.size() << std::endl;
  }

  // op 41056: put
  db->Put(write_options, "key4072", "value12893");

  // op 41057: get
  status = db->Get(read_options, "key0256", &value);
  if (status.ok()) {
    out << "GET key0256 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41058: delete
  db->Delete(write_options, "key5726");

  // op 41059: get
  status = db->Get(read_options, "key7526", &value);
  if (status.ok()) {
    out << "GET key7526 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41060: delete
  db->Delete(write_options, "key1455");

  // op 41061: get
  status = db->Get(read_options, "key6890", &value);
  if (status.ok()) {
    out << "GET key6890 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41062: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 41063: put
  db->Put(write_options, "key9156", "value43186");

  // op 41064: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6466", "key7712", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6466, key7712) size=" << scan_result.size() << std::endl;
  }

  // op 41065: put
  db->Put(write_options, "key3259", "value18908");

  // op 41066: put
  db->Put(write_options, "key6645", "value50081");

  // op 41067: put
  db->Put(write_options, "key1247", "value53841");

  // op 41068: put
  db->Put(write_options, "key11322", "value56682");

  // op 41069: delete_range
  status = db->DeleteRange(write_options, "key4162", "key4368");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 41070: delete_range
  status = db->DeleteRange(write_options, "key1172", "key3663");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 41071: delete_range
  status = db->DeleteRange(write_options, "key8642", "key8873");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 41072: get
  status = db->Get(read_options, "key0706", &value);
  if (status.ok()) {
    out << "GET key0706 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41073: delete
  db->Delete(write_options, "key3613");

  // op 41074: put
  db->Put(write_options, "key5286", "value42114");

  // op 41075: put
  db->Put(write_options, "key4155", "value50924");

  // op 41076: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0115", "key10612", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0115, key10612) size=" << scan_result.size() << std::endl;
  }

  // op 41077: delete_range
  status = db->DeleteRange(write_options, "key5238", "key5247");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 41078: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0358", "key4638", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0358, key4638) size=" << scan_result.size() << std::endl;
  }

  // op 41079: put
  db->Put(write_options, "key3315", "value41557");

  // op 41080: delete
  db->Delete(write_options, "key6690");

  // op 41081: delete
  db->Delete(write_options, "key7121");

  // op 41082: delete
  db->Delete(write_options, "key7355");

  // op 41083: put
  db->Put(write_options, "key0810", "value11964");

  // op 41084: delete_range
  status = db->DeleteRange(write_options, "key7661", "key11109");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 41085: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 41086: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 41087: put
  db->Put(write_options, "key5079", "value108617");

  // op 41088: delete_range
  status = db->DeleteRange(write_options, "key5229", "key6578");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 41089: put
  db->Put(write_options, "key10141", "value61487");

  // op 41090: get
  status = db->Get(read_options, "key6159", &value);
  if (status.ok()) {
    out << "GET key6159 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41091: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6271", "key10160", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6271, key10160) size=" << scan_result.size() << std::endl;
  }

  // op 41092: delete
  db->Delete(write_options, "key5227");

  // op 41093: put
  db->Put(write_options, "key6888", "value117896");

  // op 41094: put
  db->Put(write_options, "key9456", "value14310");

  // op 41095: get
  status = db->Get(read_options, "key8910", &value);
  if (status.ok()) {
    out << "GET key8910 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41096: put
  db->Put(write_options, "key3072", "value69903");

  // op 41097: delete_range
  status = db->DeleteRange(write_options, "key0646", "key10850");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 41098: get
  status = db->Get(read_options, "key6591", &value);
  if (status.ok()) {
    out << "GET key6591 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41099: put
  db->Put(write_options, "key2754", "value7475");

  // op 41100: get
  status = db->Get(read_options, "key9471", &value);
  if (status.ok()) {
    out << "GET key9471 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41101: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0754", "key9642", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0754, key9642) size=" << scan_result.size() << std::endl;
  }

  // op 41102: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3343", "key8535", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3343, key8535) size=" << scan_result.size() << std::endl;
  }

  // op 41103: get
  status = db->Get(read_options, "key3113", &value);
  if (status.ok()) {
    out << "GET key3113 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41104: put
  db->Put(write_options, "key11544", "value91338");

  // op 41105: put
  db->Put(write_options, "key1574", "value90331");

  // op 41106: delete
  db->Delete(write_options, "key5554");

  // op 41107: put
  db->Put(write_options, "key9992", "value60122");

  // op 41108: put
  db->Put(write_options, "key5996", "value10652");

  // op 41109: delete_range
  status = db->DeleteRange(write_options, "key1481", "key8429");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 41110: put
  db->Put(write_options, "key10716", "value59411");

  // op 41111: put
  db->Put(write_options, "key2561", "value48321");

  // op 41112: put
  db->Put(write_options, "key3031", "value81841");

  // op 41113: get
  status = db->Get(read_options, "key6612", &value);
  if (status.ok()) {
    out << "GET key6612 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41114: put
  db->Put(write_options, "key0149", "value14927");

  // op 41115: delete_range
  status = db->DeleteRange(write_options, "key7620", "key9390");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 41116: delete_range
  status = db->DeleteRange(write_options, "key4291", "key6024");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 41117: put
  db->Put(write_options, "key5936", "value56409");

  // op 41118: put
  db->Put(write_options, "key7226", "value109148");

  // op 41119: get
  status = db->Get(read_options, "key2631", &value);
  if (status.ok()) {
    out << "GET key2631 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41120: put
  db->Put(write_options, "key10654", "value21736");

  // op 41121: get
  status = db->Get(read_options, "key5794", &value);
  if (status.ok()) {
    out << "GET key5794 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41122: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5105", "key6724", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5105, key6724) size=" << scan_result.size() << std::endl;
  }

  // op 41123: put
  db->Put(write_options, "key9535", "value14466");

  // op 41124: get
  status = db->Get(read_options, "key7570", &value);
  if (status.ok()) {
    out << "GET key7570 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41125: put
  db->Put(write_options, "key8716", "value65099");

  // op 41126: get
  status = db->Get(read_options, "key7039", &value);
  if (status.ok()) {
    out << "GET key7039 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41127: put
  db->Put(write_options, "key10778", "value63222");

  // op 41128: delete
  db->Delete(write_options, "key4243");

  // op 41129: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2488", "key11522", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2488, key11522) size=" << scan_result.size() << std::endl;
  }

  // op 41130: put
  db->Put(write_options, "key2112", "value43977");

  // op 41131: delete
  db->Delete(write_options, "key1914");

  // op 41132: get
  status = db->Get(read_options, "key6821", &value);
  if (status.ok()) {
    out << "GET key6821 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41133: delete
  db->Delete(write_options, "key1194");

  // op 41134: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1174", "key8944", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1174, key8944) size=" << scan_result.size() << std::endl;
  }

  // op 41135: put
  db->Put(write_options, "key0560", "value6854");

  // op 41136: put
  db->Put(write_options, "key2778", "value56875");

  // op 41137: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1947", "key8188", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1947, key8188) size=" << scan_result.size() << std::endl;
  }

  // op 41138: delete_range
  status = db->DeleteRange(write_options, "key8675", "key11061");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 41139: get
  status = db->Get(read_options, "key4598", &value);
  if (status.ok()) {
    out << "GET key4598 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41140: put
  db->Put(write_options, "key0527", "value51904");

  // op 41141: put
  db->Put(write_options, "key0512", "value80839");

  // op 41142: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 41143: put
  db->Put(write_options, "key8661", "value47420");

  // op 41144: delete_range
  status = db->DeleteRange(write_options, "key3695", "key9552");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 41145: get
  status = db->Get(read_options, "key4870", &value);
  if (status.ok()) {
    out << "GET key4870 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41146: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 41147: put
  db->Put(write_options, "key4188", "value54075");

  // op 41148: put
  db->Put(write_options, "key1612", "value92057");

  // op 41149: put
  db->Put(write_options, "key8560", "value91612");

  // op 41150: put
  db->Put(write_options, "key4068", "value52558");

  // op 41151: get
  status = db->Get(read_options, "key2121", &value);
  if (status.ok()) {
    out << "GET key2121 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41152: put
  db->Put(write_options, "key2989", "value102025");

  // op 41153: get
  status = db->Get(read_options, "key4439", &value);
  if (status.ok()) {
    out << "GET key4439 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41154: put
  db->Put(write_options, "key7024", "value61104");

  // op 41155: delete_range
  status = db->DeleteRange(write_options, "key0884", "key6065");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 41156: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3625", "key11346", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3625, key11346) size=" << scan_result.size() << std::endl;
  }

  // op 41157: put
  db->Put(write_options, "key0820", "value36977");

  // op 41158: put
  db->Put(write_options, "key6508", "value5989");

  // op 41159: get
  status = db->Get(read_options, "key8710", &value);
  if (status.ok()) {
    out << "GET key8710 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41160: put
  db->Put(write_options, "key8860", "value18782");

  // op 41161: put
  db->Put(write_options, "key8226", "value80946");

  // op 41162: delete_range
  status = db->DeleteRange(write_options, "key6239", "key8077");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 41163: put
  db->Put(write_options, "key11086", "value7317");

  // op 41164: put
  db->Put(write_options, "key2412", "value90250");

  // op 41165: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 41166: put
  db->Put(write_options, "key10691", "value14604");

  // op 41167: put
  db->Put(write_options, "key4475", "value69905");

  // op 41168: get
  status = db->Get(read_options, "key10125", &value);
  if (status.ok()) {
    out << "GET key10125 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41169: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 41170: get
  status = db->Get(read_options, "key4152", &value);
  if (status.ok()) {
    out << "GET key4152 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41171: put
  db->Put(write_options, "key3626", "value20183");

  // op 41172: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 41173: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 41174: get
  status = db->Get(read_options, "key8295", &value);
  if (status.ok()) {
    out << "GET key8295 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41175: put
  db->Put(write_options, "key2008", "value2410");

  // op 41176: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9105", "key9654", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9105, key9654) size=" << scan_result.size() << std::endl;
  }

  // op 41177: get
  status = db->Get(read_options, "key1773", &value);
  if (status.ok()) {
    out << "GET key1773 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41178: delete_range
  status = db->DeleteRange(write_options, "key0392", "key2215");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 41179: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0746", "key4400", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0746, key4400) size=" << scan_result.size() << std::endl;
  }

  // op 41180: get
  status = db->Get(read_options, "key6409", &value);
  if (status.ok()) {
    out << "GET key6409 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41181: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1371", "key8503", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1371, key8503) size=" << scan_result.size() << std::endl;
  }

  // op 41182: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 41183: put
  db->Put(write_options, "key2943", "value47264");

  // op 41184: put
  db->Put(write_options, "key3792", "value69101");

  // op 41185: put
  db->Put(write_options, "key9479", "value6344");

  // op 41186: put
  db->Put(write_options, "key8192", "value2831");

  // op 41187: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0019", "key11116", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0019, key11116) size=" << scan_result.size() << std::endl;
  }

  // op 41188: put
  db->Put(write_options, "key0182", "value92433");

  // op 41189: put
  db->Put(write_options, "key0850", "value22554");

  // op 41190: get
  status = db->Get(read_options, "key9378", &value);
  if (status.ok()) {
    out << "GET key9378 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41191: get
  status = db->Get(read_options, "key11121", &value);
  if (status.ok()) {
    out << "GET key11121 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41192: put
  db->Put(write_options, "key9210", "value93327");

  // op 41193: get
  status = db->Get(read_options, "key6023", &value);
  if (status.ok()) {
    out << "GET key6023 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41194: delete
  db->Delete(write_options, "key1876");

  // op 41195: put
  db->Put(write_options, "key7689", "value91399");

  // op 41196: put
  db->Put(write_options, "key10904", "value51351");

  // op 41197: put
  db->Put(write_options, "key10013", "value56124");

  // op 41198: put
  db->Put(write_options, "key9847", "value15735");

  // op 41199: get
  status = db->Get(read_options, "key7307", &value);
  if (status.ok()) {
    out << "GET key7307 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41200: delete
  db->Delete(write_options, "key2041");

  // op 41201: delete_range
  status = db->DeleteRange(write_options, "key0538", "key4469");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 41202: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1662", "key4119", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1662, key4119) size=" << scan_result.size() << std::endl;
  }

  // op 41203: get
  status = db->Get(read_options, "key1713", &value);
  if (status.ok()) {
    out << "GET key1713 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41204: put
  db->Put(write_options, "key6680", "value78587");

  // op 41205: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6700", "key9333", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6700, key9333) size=" << scan_result.size() << std::endl;
  }

  // op 41206: delete
  db->Delete(write_options, "key7553");

  // op 41207: get
  status = db->Get(read_options, "key3768", &value);
  if (status.ok()) {
    out << "GET key3768 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41208: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0513", "key10140", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0513, key10140) size=" << scan_result.size() << std::endl;
  }

  // op 41209: get
  status = db->Get(read_options, "key6645", &value);
  if (status.ok()) {
    out << "GET key6645 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41210: delete_range
  status = db->DeleteRange(write_options, "key6748", "key6779");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 41211: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3622", "key4918", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3622, key4918) size=" << scan_result.size() << std::endl;
  }

  // op 41212: put
  db->Put(write_options, "key1210", "value34267");

  // op 41213: delete_range
  status = db->DeleteRange(write_options, "key2198", "key3237");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 41214: put
  db->Put(write_options, "key7033", "value86299");

  // op 41215: put
  db->Put(write_options, "key7287", "value105903");

  // op 41216: delete_range
  status = db->DeleteRange(write_options, "key8083", "key11289");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 41217: delete
  db->Delete(write_options, "key6171");

  // op 41218: get
  status = db->Get(read_options, "key5616", &value);
  if (status.ok()) {
    out << "GET key5616 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41219: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 41220: get
  status = db->Get(read_options, "key10301", &value);
  if (status.ok()) {
    out << "GET key10301 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41221: put
  db->Put(write_options, "key9194", "value55166");

  // op 41222: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8632", "key10610", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8632, key10610) size=" << scan_result.size() << std::endl;
  }

  // op 41223: put
  db->Put(write_options, "key11338", "value95207");

  // op 41224: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3114", "key10060", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3114, key10060) size=" << scan_result.size() << std::endl;
  }

  // op 41225: put
  db->Put(write_options, "key2839", "value91178");

  // op 41226: delete_range
  status = db->DeleteRange(write_options, "key0065", "key11158");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 41227: get
  status = db->Get(read_options, "key3099", &value);
  if (status.ok()) {
    out << "GET key3099 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41228: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 41229: delete
  db->Delete(write_options, "key7112");

  // op 41230: put
  db->Put(write_options, "key8323", "value96752");

  // op 41231: delete_range
  status = db->DeleteRange(write_options, "key1234", "key5836");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 41232: get
  status = db->Get(read_options, "key5612", &value);
  if (status.ok()) {
    out << "GET key5612 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41233: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0166", "key1884", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0166, key1884) size=" << scan_result.size() << std::endl;
  }

  // op 41234: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8768", "key10490", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8768, key10490) size=" << scan_result.size() << std::endl;
  }

  // op 41235: put
  db->Put(write_options, "key1414", "value85345");

  // op 41236: put
  db->Put(write_options, "key4198", "value43661");

  // op 41237: get
  status = db->Get(read_options, "key11541", &value);
  if (status.ok()) {
    out << "GET key11541 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41238: delete
  db->Delete(write_options, "key3611");

  // op 41239: put
  db->Put(write_options, "key7511", "value28542");

  // op 41240: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 41241: delete
  db->Delete(write_options, "key5652");

  // op 41242: put
  db->Put(write_options, "key10721", "value1727");

  // op 41243: delete_range
  status = db->DeleteRange(write_options, "key6757", "key8356");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 41244: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6669", "key9162", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6669, key9162) size=" << scan_result.size() << std::endl;
  }

  // op 41245: put
  db->Put(write_options, "key4079", "value78080");

  // op 41246: get
  status = db->Get(read_options, "key3789", &value);
  if (status.ok()) {
    out << "GET key3789 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41247: put
  db->Put(write_options, "key11206", "value28968");

  // op 41248: put
  db->Put(write_options, "key0648", "value75883");

  // op 41249: put
  db->Put(write_options, "key6180", "value2802");

  // op 41250: put
  db->Put(write_options, "key0990", "value42876");

  // op 41251: delete_range
  status = db->DeleteRange(write_options, "key8737", "key9141");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 41252: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4236", "key9092", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4236, key9092) size=" << scan_result.size() << std::endl;
  }

  // op 41253: get
  status = db->Get(read_options, "key6825", &value);
  if (status.ok()) {
    out << "GET key6825 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41254: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1428", "key8460", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1428, key8460) size=" << scan_result.size() << std::endl;
  }

  // op 41255: put
  db->Put(write_options, "key9888", "value75199");

  // op 41256: put
  db->Put(write_options, "key6147", "value56018");

  // op 41257: get
  status = db->Get(read_options, "key8224", &value);
  if (status.ok()) {
    out << "GET key8224 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41258: delete_range
  status = db->DeleteRange(write_options, "key2261", "key10936");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 41259: put
  db->Put(write_options, "key10279", "value24751");

  // op 41260: put
  db->Put(write_options, "key0425", "value103818");

  // op 41261: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1904", "key9755", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1904, key9755) size=" << scan_result.size() << std::endl;
  }

  // op 41262: delete
  db->Delete(write_options, "key2402");

  // op 41263: put
  db->Put(write_options, "key10624", "value92291");

  // op 41264: delete
  db->Delete(write_options, "key7026");

  // op 41265: get
  status = db->Get(read_options, "key9869", &value);
  if (status.ok()) {
    out << "GET key9869 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41266: put
  db->Put(write_options, "key9047", "value70118");

  // op 41267: delete
  db->Delete(write_options, "key5377");

  // op 41268: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6289", "key8763", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6289, key8763) size=" << scan_result.size() << std::endl;
  }

  // op 41269: get
  status = db->Get(read_options, "key5239", &value);
  if (status.ok()) {
    out << "GET key5239 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41270: put
  db->Put(write_options, "key1334", "value34044");

  // op 41271: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6769", "key10989", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6769, key10989) size=" << scan_result.size() << std::endl;
  }

  // op 41272: delete_range
  status = db->DeleteRange(write_options, "key2065", "key7054");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 41273: get
  status = db->Get(read_options, "key11443", &value);
  if (status.ok()) {
    out << "GET key11443 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41274: get
  status = db->Get(read_options, "key10740", &value);
  if (status.ok()) {
    out << "GET key10740 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41275: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8999", "key11073", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8999, key11073) size=" << scan_result.size() << std::endl;
  }

  // op 41276: delete
  db->Delete(write_options, "key2028");

  // op 41277: put
  db->Put(write_options, "key4465", "value13973");

  // op 41278: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0474", "key1251", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0474, key1251) size=" << scan_result.size() << std::endl;
  }

  // op 41279: put
  db->Put(write_options, "key4831", "value52680");

  // op 41280: get
  status = db->Get(read_options, "key10473", &value);
  if (status.ok()) {
    out << "GET key10473 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41281: get
  status = db->Get(read_options, "key6793", &value);
  if (status.ok()) {
    out << "GET key6793 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41282: get
  status = db->Get(read_options, "key0217", &value);
  if (status.ok()) {
    out << "GET key0217 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41283: put
  db->Put(write_options, "key6645", "value21360");

  // op 41284: put
  db->Put(write_options, "key7250", "value88961");

  // op 41285: put
  db->Put(write_options, "key0892", "value27526");

  // op 41286: put
  db->Put(write_options, "key10152", "value103613");

  // op 41287: put
  db->Put(write_options, "key3674", "value113239");

  // op 41288: delete_range
  status = db->DeleteRange(write_options, "key0597", "key4353");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 41289: put
  db->Put(write_options, "key9180", "value41934");

  // op 41290: delete
  db->Delete(write_options, "key4586");

  // op 41291: get
  status = db->Get(read_options, "key4239", &value);
  if (status.ok()) {
    out << "GET key4239 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41292: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 41293: delete_range
  status = db->DeleteRange(write_options, "key6921", "key8831");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 41294: put
  db->Put(write_options, "key2887", "value19964");

  // op 41295: get
  status = db->Get(read_options, "key0327", &value);
  if (status.ok()) {
    out << "GET key0327 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41296: put
  db->Put(write_options, "key10304", "value78026");

  // op 41297: get
  status = db->Get(read_options, "key1317", &value);
  if (status.ok()) {
    out << "GET key1317 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41298: put
  db->Put(write_options, "key5718", "value13076");

  // op 41299: get
  status = db->Get(read_options, "key4821", &value);
  if (status.ok()) {
    out << "GET key4821 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41300: get
  status = db->Get(read_options, "key8032", &value);
  if (status.ok()) {
    out << "GET key8032 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41301: put
  db->Put(write_options, "key5375", "value102367");

  // op 41302: delete
  db->Delete(write_options, "key1550");

  // op 41303: put
  db->Put(write_options, "key10746", "value74734");

  // op 41304: put
  db->Put(write_options, "key5563", "value19479");

  // op 41305: put
  db->Put(write_options, "key4526", "value41596");

  // op 41306: put
  db->Put(write_options, "key10900", "value59609");

  // op 41307: put
  db->Put(write_options, "key3143", "value51265");

  // op 41308: put
  db->Put(write_options, "key9419", "value33126");

  // op 41309: put
  db->Put(write_options, "key6798", "value90525");

  // op 41310: put
  db->Put(write_options, "key0632", "value31555");

  // op 41311: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2510", "key9885", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2510, key9885) size=" << scan_result.size() << std::endl;
  }

  // op 41312: delete
  db->Delete(write_options, "key0328");

  // op 41313: put
  db->Put(write_options, "key1701", "value108359");

  // op 41314: put
  db->Put(write_options, "key1760", "value109590");

  // op 41315: delete
  db->Delete(write_options, "key9388");

  // op 41316: delete_range
  status = db->DeleteRange(write_options, "key3124", "key7992");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 41317: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4622", "key10867", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4622, key10867) size=" << scan_result.size() << std::endl;
  }

  // op 41318: get
  status = db->Get(read_options, "key4094", &value);
  if (status.ok()) {
    out << "GET key4094 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41319: put
  db->Put(write_options, "key9296", "value23493");

  // op 41320: put
  db->Put(write_options, "key7168", "value62990");

  // op 41321: get
  status = db->Get(read_options, "key8525", &value);
  if (status.ok()) {
    out << "GET key8525 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41322: put
  db->Put(write_options, "key4304", "value44147");

  // op 41323: put
  db->Put(write_options, "key5173", "value45065");

  // op 41324: put
  db->Put(write_options, "key2595", "value26050");

  // op 41325: put
  db->Put(write_options, "key10545", "value43287");

  // op 41326: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0666", "key6988", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0666, key6988) size=" << scan_result.size() << std::endl;
  }

  // op 41327: put
  db->Put(write_options, "key5548", "value85452");

  // op 41328: put
  db->Put(write_options, "key5841", "value83427");

  // op 41329: put
  db->Put(write_options, "key6239", "value42102");

  // op 41330: get
  status = db->Get(read_options, "key7323", &value);
  if (status.ok()) {
    out << "GET key7323 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41331: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1141", "key6134", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1141, key6134) size=" << scan_result.size() << std::endl;
  }

  // op 41332: put
  db->Put(write_options, "key10643", "value10822");

  // op 41333: put
  db->Put(write_options, "key3368", "value10477");

  // op 41334: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 41335: put
  db->Put(write_options, "key10994", "value95354");

  // op 41336: get
  status = db->Get(read_options, "key1741", &value);
  if (status.ok()) {
    out << "GET key1741 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41337: get
  status = db->Get(read_options, "key0560", &value);
  if (status.ok()) {
    out << "GET key0560 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41338: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4929", "key8510", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4929, key8510) size=" << scan_result.size() << std::endl;
  }

  // op 41339: put
  db->Put(write_options, "key0969", "value32228");

  // op 41340: get
  status = db->Get(read_options, "key0438", &value);
  if (status.ok()) {
    out << "GET key0438 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41341: delete_range
  status = db->DeleteRange(write_options, "key1743", "key5652");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 41342: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0896", "key9496", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0896, key9496) size=" << scan_result.size() << std::endl;
  }

  // op 41343: put
  db->Put(write_options, "key0638", "value91999");

  // op 41344: put
  db->Put(write_options, "key3056", "value93306");

  // op 41345: put
  db->Put(write_options, "key4863", "value59895");

  // op 41346: delete
  db->Delete(write_options, "key5817");

  // op 41347: delete
  db->Delete(write_options, "key5461");

  // op 41348: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1311", "key4660", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1311, key4660) size=" << scan_result.size() << std::endl;
  }

  // op 41349: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4841", "key9475", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4841, key9475) size=" << scan_result.size() << std::endl;
  }

  // op 41350: put
  db->Put(write_options, "key4754", "value85138");

  // op 41351: get
  status = db->Get(read_options, "key4404", &value);
  if (status.ok()) {
    out << "GET key4404 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41352: delete_range
  status = db->DeleteRange(write_options, "key4423", "key5153");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 41353: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5553", "key9919", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5553, key9919) size=" << scan_result.size() << std::endl;
  }

  // op 41354: get
  status = db->Get(read_options, "key6070", &value);
  if (status.ok()) {
    out << "GET key6070 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41355: delete
  db->Delete(write_options, "key0028");

  // op 41356: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 41357: delete_range
  status = db->DeleteRange(write_options, "key4498", "key8527");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 41358: get
  status = db->Get(read_options, "key8116", &value);
  if (status.ok()) {
    out << "GET key8116 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41359: put
  db->Put(write_options, "key5230", "value60567");

  // op 41360: delete_range
  status = db->DeleteRange(write_options, "key2062", "key9508");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 41361: delete_range
  status = db->DeleteRange(write_options, "key4433", "key5039");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 41362: put
  db->Put(write_options, "key4945", "value118208");

  // op 41363: get
  status = db->Get(read_options, "key4561", &value);
  if (status.ok()) {
    out << "GET key4561 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41364: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2542", "key7037", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2542, key7037) size=" << scan_result.size() << std::endl;
  }

  // op 41365: put
  db->Put(write_options, "key2529", "value104710");

  // op 41366: delete_range
  status = db->DeleteRange(write_options, "key1202", "key9741");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 41367: put
  db->Put(write_options, "key10988", "value62103");

  // op 41368: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 41369: put
  db->Put(write_options, "key4116", "value17822");

  // op 41370: put
  db->Put(write_options, "key0661", "value10283");

  // op 41371: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 41372: get
  status = db->Get(read_options, "key4646", &value);
  if (status.ok()) {
    out << "GET key4646 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41373: delete
  db->Delete(write_options, "key10518");

  // op 41374: delete_range
  status = db->DeleteRange(write_options, "key3802", "key6739");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 41375: delete
  db->Delete(write_options, "key3986");

  // op 41376: put
  db->Put(write_options, "key3184", "value95525");

  // op 41377: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1276", "key11290", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1276, key11290) size=" << scan_result.size() << std::endl;
  }

  // op 41378: delete_range
  status = db->DeleteRange(write_options, "key4121", "key10329");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 41379: delete
  db->Delete(write_options, "key1132");

  // op 41380: get
  status = db->Get(read_options, "key4913", &value);
  if (status.ok()) {
    out << "GET key4913 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41381: get
  status = db->Get(read_options, "key2861", &value);
  if (status.ok()) {
    out << "GET key2861 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41382: put
  db->Put(write_options, "key11246", "value102994");

  // op 41383: delete_range
  status = db->DeleteRange(write_options, "key1267", "key8872");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 41384: put
  db->Put(write_options, "key8140", "value26124");

  // op 41385: get
  status = db->Get(read_options, "key11010", &value);
  if (status.ok()) {
    out << "GET key11010 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41386: delete
  db->Delete(write_options, "key8670");

  // op 41387: delete
  db->Delete(write_options, "key7596");

  // op 41388: get
  status = db->Get(read_options, "key2414", &value);
  if (status.ok()) {
    out << "GET key2414 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41389: put
  db->Put(write_options, "key5600", "value5306");

  // op 41390: get
  status = db->Get(read_options, "key0225", &value);
  if (status.ok()) {
    out << "GET key0225 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41391: delete_range
  status = db->DeleteRange(write_options, "key0326", "key11401");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 41392: delete_range
  status = db->DeleteRange(write_options, "key3245", "key11110");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 41393: put
  db->Put(write_options, "key0805", "value86364");

  // op 41394: put
  db->Put(write_options, "key0888", "value48942");

  // op 41395: put
  db->Put(write_options, "key1782", "value107233");

  // op 41396: delete_range
  status = db->DeleteRange(write_options, "key8360", "key8435");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 41397: put
  db->Put(write_options, "key6964", "value73011");

  // op 41398: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 41399: delete_range
  status = db->DeleteRange(write_options, "key2518", "key11042");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 41400: put
  db->Put(write_options, "key8378", "value12834");

  // op 41401: put
  db->Put(write_options, "key10546", "value113923");

  // op 41402: delete
  db->Delete(write_options, "key7484");

  // op 41403: put
  db->Put(write_options, "key9636", "value97219");

  // op 41404: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7412", "key9951", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7412, key9951) size=" << scan_result.size() << std::endl;
  }

  // op 41405: get
  status = db->Get(read_options, "key5897", &value);
  if (status.ok()) {
    out << "GET key5897 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41406: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4862", "key7738", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4862, key7738) size=" << scan_result.size() << std::endl;
  }

  // op 41407: get
  status = db->Get(read_options, "key0911", &value);
  if (status.ok()) {
    out << "GET key0911 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41408: put
  db->Put(write_options, "key9329", "value17140");

  // op 41409: get
  status = db->Get(read_options, "key6785", &value);
  if (status.ok()) {
    out << "GET key6785 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41410: delete_range
  status = db->DeleteRange(write_options, "key5427", "key7317");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 41411: delete_range
  status = db->DeleteRange(write_options, "key3469", "key5483");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 41412: put
  db->Put(write_options, "key1544", "value68240");

  // op 41413: put
  db->Put(write_options, "key3212", "value15149");

  // op 41414: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6381", "key11164", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6381, key11164) size=" << scan_result.size() << std::endl;
  }

  // op 41415: put
  db->Put(write_options, "key4046", "value107173");

  // op 41416: get
  status = db->Get(read_options, "key7542", &value);
  if (status.ok()) {
    out << "GET key7542 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41417: get
  status = db->Get(read_options, "key3294", &value);
  if (status.ok()) {
    out << "GET key3294 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41418: delete_range
  status = db->DeleteRange(write_options, "key2941", "key11034");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 41419: delete
  db->Delete(write_options, "key4300");

  // op 41420: delete
  db->Delete(write_options, "key3082");

  // op 41421: put
  db->Put(write_options, "key7366", "value6542");

  // op 41422: get
  status = db->Get(read_options, "key2325", &value);
  if (status.ok()) {
    out << "GET key2325 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41423: get
  status = db->Get(read_options, "key8363", &value);
  if (status.ok()) {
    out << "GET key8363 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41424: delete_range
  status = db->DeleteRange(write_options, "key3945", "key10724");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 41425: get
  status = db->Get(read_options, "key0621", &value);
  if (status.ok()) {
    out << "GET key0621 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41426: delete_range
  status = db->DeleteRange(write_options, "key4033", "key4732");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 41427: put
  db->Put(write_options, "key0183", "value18078");

  // op 41428: get
  status = db->Get(read_options, "key10037", &value);
  if (status.ok()) {
    out << "GET key10037 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41429: get
  status = db->Get(read_options, "key10523", &value);
  if (status.ok()) {
    out << "GET key10523 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41430: put
  db->Put(write_options, "key6509", "value13842");

  // op 41431: put
  db->Put(write_options, "key2838", "value91006");

  // op 41432: get
  status = db->Get(read_options, "key5968", &value);
  if (status.ok()) {
    out << "GET key5968 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41433: put
  db->Put(write_options, "key3157", "value105072");

  // op 41434: put
  db->Put(write_options, "key3156", "value34696");

  // op 41435: put
  db->Put(write_options, "key10469", "value49827");

  // op 41436: put
  db->Put(write_options, "key3995", "value46149");

  // op 41437: put
  db->Put(write_options, "key0429", "value109733");

  // op 41438: delete_range
  status = db->DeleteRange(write_options, "key5166", "key9264");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 41439: delete_range
  status = db->DeleteRange(write_options, "key0533", "key4296");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 41440: get
  status = db->Get(read_options, "key11611", &value);
  if (status.ok()) {
    out << "GET key11611 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41441: put
  db->Put(write_options, "key10938", "value35841");

  // op 41442: put
  db->Put(write_options, "key3756", "value61358");

  // op 41443: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5853", "key7160", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5853, key7160) size=" << scan_result.size() << std::endl;
  }

  // op 41444: put
  db->Put(write_options, "key5570", "value3067");

  // op 41445: get
  status = db->Get(read_options, "key5171", &value);
  if (status.ok()) {
    out << "GET key5171 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41446: get
  status = db->Get(read_options, "key11538", &value);
  if (status.ok()) {
    out << "GET key11538 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41447: delete_range
  status = db->DeleteRange(write_options, "key4783", "key7800");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 41448: delete_range
  status = db->DeleteRange(write_options, "key7540", "key9700");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 41449: delete
  db->Delete(write_options, "key1248");

  // op 41450: delete
  db->Delete(write_options, "key8907");

  // op 41451: put
  db->Put(write_options, "key6997", "value106529");

  // op 41452: put
  db->Put(write_options, "key5267", "value81406");

  // op 41453: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0303", "key8069", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0303, key8069) size=" << scan_result.size() << std::endl;
  }

  // op 41454: delete
  db->Delete(write_options, "key8004");

  // op 41455: get
  status = db->Get(read_options, "key8800", &value);
  if (status.ok()) {
    out << "GET key8800 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41456: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0881", "key6100", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0881, key6100) size=" << scan_result.size() << std::endl;
  }

  // op 41457: get
  status = db->Get(read_options, "key4257", &value);
  if (status.ok()) {
    out << "GET key4257 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41458: delete
  db->Delete(write_options, "key8163");

  // op 41459: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 41460: put
  db->Put(write_options, "key7685", "value102819");

  // op 41461: get
  status = db->Get(read_options, "key0974", &value);
  if (status.ok()) {
    out << "GET key0974 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41462: get
  status = db->Get(read_options, "key7423", &value);
  if (status.ok()) {
    out << "GET key7423 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41463: put
  db->Put(write_options, "key1227", "value41665");

  // op 41464: put
  db->Put(write_options, "key3906", "value72745");

  // op 41465: put
  db->Put(write_options, "key3149", "value50198");

  // op 41466: delete
  db->Delete(write_options, "key7164");

  // op 41467: put
  db->Put(write_options, "key7018", "value50437");

  // op 41468: put
  db->Put(write_options, "key3159", "value51844");

  // op 41469: delete_range
  status = db->DeleteRange(write_options, "key0747", "key5527");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 41470: get
  status = db->Get(read_options, "key7451", &value);
  if (status.ok()) {
    out << "GET key7451 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41471: put
  db->Put(write_options, "key10486", "value64556");

  // op 41472: put
  db->Put(write_options, "key1358", "value62201");

  // op 41473: put
  db->Put(write_options, "key5848", "value110567");

  // op 41474: delete
  db->Delete(write_options, "key0699");

  // op 41475: delete
  db->Delete(write_options, "key1420");

  // op 41476: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3176", "key5922", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3176, key5922) size=" << scan_result.size() << std::endl;
  }

  // op 41477: put
  db->Put(write_options, "key11712", "value32535");

  // op 41478: get
  status = db->Get(read_options, "key1163", &value);
  if (status.ok()) {
    out << "GET key1163 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41479: put
  db->Put(write_options, "key7807", "value49553");

  // op 41480: put
  db->Put(write_options, "key7841", "value110043");

  // op 41481: delete
  db->Delete(write_options, "key6965");

  // op 41482: put
  db->Put(write_options, "key4226", "value94962");

  // op 41483: get
  status = db->Get(read_options, "key4819", &value);
  if (status.ok()) {
    out << "GET key4819 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41484: delete_range
  status = db->DeleteRange(write_options, "key6413", "key11357");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 41485: put
  db->Put(write_options, "key2776", "value79649");

  // op 41486: put
  db->Put(write_options, "key8016", "value40207");

  // op 41487: get
  status = db->Get(read_options, "key4993", &value);
  if (status.ok()) {
    out << "GET key4993 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41488: put
  db->Put(write_options, "key9473", "value12744");

  // op 41489: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2798", "key8130", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2798, key8130) size=" << scan_result.size() << std::endl;
  }

  // op 41490: put
  db->Put(write_options, "key5914", "value39821");

  // op 41491: delete_range
  status = db->DeleteRange(write_options, "key3544", "key9787");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 41492: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 41493: put
  db->Put(write_options, "key0762", "value47463");

  // op 41494: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5211", "key5235", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5211, key5235) size=" << scan_result.size() << std::endl;
  }

  // op 41495: delete_range
  status = db->DeleteRange(write_options, "key0165", "key9854");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 41496: put
  db->Put(write_options, "key6736", "value4564");

  // op 41497: put
  db->Put(write_options, "key9376", "value37909");

  // op 41498: get
  status = db->Get(read_options, "key5440", &value);
  if (status.ok()) {
    out << "GET key5440 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41499: put
  db->Put(write_options, "key9131", "value94963");

  // op 41500: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4564", "key10805", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4564, key10805) size=" << scan_result.size() << std::endl;
  }

  // op 41501: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0905", "key7676", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0905, key7676) size=" << scan_result.size() << std::endl;
  }

  // op 41502: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 41503: put
  db->Put(write_options, "key10563", "value62769");

  // op 41504: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0437", "key3436", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0437, key3436) size=" << scan_result.size() << std::endl;
  }

  // op 41505: put
  db->Put(write_options, "key0229", "value54619");

  // op 41506: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1947", "key6688", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1947, key6688) size=" << scan_result.size() << std::endl;
  }

  // op 41507: put
  db->Put(write_options, "key9896", "value2333");

  // op 41508: put
  db->Put(write_options, "key10178", "value90612");

  // op 41509: put
  db->Put(write_options, "key0768", "value92910");

  // op 41510: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 41511: delete_range
  status = db->DeleteRange(write_options, "key1150", "key4157");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 41512: put
  db->Put(write_options, "key11269", "value72620");

  // op 41513: put
  db->Put(write_options, "key4718", "value19082");

  // op 41514: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1804", "key11551", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1804, key11551) size=" << scan_result.size() << std::endl;
  }

  // op 41515: put
  db->Put(write_options, "key6001", "value0648");

  // op 41516: put
  db->Put(write_options, "key7084", "value88276");

  // op 41517: delete_range
  status = db->DeleteRange(write_options, "key6528", "key8202");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 41518: put
  db->Put(write_options, "key0512", "value51698");

  // op 41519: put
  db->Put(write_options, "key7678", "value85188");

  // op 41520: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0332", "key6298", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0332, key6298) size=" << scan_result.size() << std::endl;
  }

  // op 41521: put
  db->Put(write_options, "key9261", "value48489");

  // op 41522: put
  db->Put(write_options, "key5007", "value20601");

  // op 41523: put
  db->Put(write_options, "key3160", "value56919");

  // op 41524: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2461", "key3511", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2461, key3511) size=" << scan_result.size() << std::endl;
  }

  // op 41525: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4006", "key10473", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4006, key10473) size=" << scan_result.size() << std::endl;
  }

  // op 41526: get
  status = db->Get(read_options, "key0977", &value);
  if (status.ok()) {
    out << "GET key0977 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41527: put
  db->Put(write_options, "key0534", "value43194");

  // op 41528: delete_range
  status = db->DeleteRange(write_options, "key2860", "key4178");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 41529: put
  db->Put(write_options, "key9101", "value104627");

  // op 41530: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0824", "key10481", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0824, key10481) size=" << scan_result.size() << std::endl;
  }

  // op 41531: get
  status = db->Get(read_options, "key4812", &value);
  if (status.ok()) {
    out << "GET key4812 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41532: get
  status = db->Get(read_options, "key5762", &value);
  if (status.ok()) {
    out << "GET key5762 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41533: put
  db->Put(write_options, "key1141", "value16252");

  // op 41534: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2080", "key9606", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2080, key9606) size=" << scan_result.size() << std::endl;
  }

  // op 41535: put
  db->Put(write_options, "key1921", "value28096");

  // op 41536: get
  status = db->Get(read_options, "key2208", &value);
  if (status.ok()) {
    out << "GET key2208 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41537: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 41538: delete
  db->Delete(write_options, "key6813");

  // op 41539: delete_range
  status = db->DeleteRange(write_options, "key9420", "key11228");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 41540: delete_range
  status = db->DeleteRange(write_options, "key7498", "key11683");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 41541: put
  db->Put(write_options, "key4353", "value36068");

  // op 41542: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0917", "key8482", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0917, key8482) size=" << scan_result.size() << std::endl;
  }

  // op 41543: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4935", "key7046", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4935, key7046) size=" << scan_result.size() << std::endl;
  }

  // op 41544: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3495", "key6495", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3495, key6495) size=" << scan_result.size() << std::endl;
  }

  // op 41545: put
  db->Put(write_options, "key2369", "value45668");

  // op 41546: get
  status = db->Get(read_options, "key0995", &value);
  if (status.ok()) {
    out << "GET key0995 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41547: delete
  db->Delete(write_options, "key11032");

  // op 41548: put
  db->Put(write_options, "key2752", "value109083");

  // op 41549: put
  db->Put(write_options, "key0772", "value14000");

  // op 41550: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0011", "key7034", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0011, key7034) size=" << scan_result.size() << std::endl;
  }

  // op 41551: delete
  db->Delete(write_options, "key3841");

  // op 41552: put
  db->Put(write_options, "key3974", "value114495");

  // op 41553: delete_range
  status = db->DeleteRange(write_options, "key2941", "key11603");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 41554: put
  db->Put(write_options, "key7698", "value76170");

  // op 41555: put
  db->Put(write_options, "key5538", "value3700");

  // op 41556: put
  db->Put(write_options, "key5510", "value59361");

  // op 41557: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 41558: put
  db->Put(write_options, "key2374", "value75844");

  // op 41559: get
  status = db->Get(read_options, "key3343", &value);
  if (status.ok()) {
    out << "GET key3343 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41560: put
  db->Put(write_options, "key6465", "value15693");

  // op 41561: get
  status = db->Get(read_options, "key3338", &value);
  if (status.ok()) {
    out << "GET key3338 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41562: put
  db->Put(write_options, "key2011", "value72703");

  // op 41563: delete
  db->Delete(write_options, "key2835");

  // op 41564: put
  db->Put(write_options, "key6949", "value107306");

  // op 41565: get
  status = db->Get(read_options, "key5767", &value);
  if (status.ok()) {
    out << "GET key5767 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41566: delete
  db->Delete(write_options, "key9154");

  // op 41567: get
  status = db->Get(read_options, "key3130", &value);
  if (status.ok()) {
    out << "GET key3130 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41568: put
  db->Put(write_options, "key10148", "value99495");

  // op 41569: put
  db->Put(write_options, "key10186", "value77625");

  // op 41570: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4565", "key8109", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4565, key8109) size=" << scan_result.size() << std::endl;
  }

  // op 41571: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1005", "key4559", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1005, key4559) size=" << scan_result.size() << std::endl;
  }

  // op 41572: put
  db->Put(write_options, "key11391", "value3011");

  // op 41573: get
  status = db->Get(read_options, "key6398", &value);
  if (status.ok()) {
    out << "GET key6398 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41574: put
  db->Put(write_options, "key6768", "value10953");

  // op 41575: put
  db->Put(write_options, "key4007", "value108621");

  // op 41576: put
  db->Put(write_options, "key10860", "value81790");

  // op 41577: delete_range
  status = db->DeleteRange(write_options, "key0947", "key8948");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 41578: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5316", "key11134", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5316, key11134) size=" << scan_result.size() << std::endl;
  }

  // op 41579: get
  status = db->Get(read_options, "key7677", &value);
  if (status.ok()) {
    out << "GET key7677 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41580: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9390", "key9556", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9390, key9556) size=" << scan_result.size() << std::endl;
  }

  // op 41581: put
  db->Put(write_options, "key4742", "value13858");

  // op 41582: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0298", "key10699", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0298, key10699) size=" << scan_result.size() << std::endl;
  }

  // op 41583: put
  db->Put(write_options, "key10194", "value79822");

  // op 41584: put
  db->Put(write_options, "key2028", "value20356");

  // op 41585: delete_range
  status = db->DeleteRange(write_options, "key1725", "key2509");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 41586: get
  status = db->Get(read_options, "key1122", &value);
  if (status.ok()) {
    out << "GET key1122 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41587: delete_range
  status = db->DeleteRange(write_options, "key2991", "key11102");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 41588: get
  status = db->Get(read_options, "key3919", &value);
  if (status.ok()) {
    out << "GET key3919 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41589: delete
  db->Delete(write_options, "key3478");

  // op 41590: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4251", "key11421", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4251, key11421) size=" << scan_result.size() << std::endl;
  }

  // op 41591: delete_range
  status = db->DeleteRange(write_options, "key7995", "key9780");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 41592: put
  db->Put(write_options, "key7333", "value0308");

  // op 41593: delete_range
  status = db->DeleteRange(write_options, "key2700", "key10777");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 41594: put
  db->Put(write_options, "key4497", "value50600");

  // op 41595: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0744", "key1378", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0744, key1378) size=" << scan_result.size() << std::endl;
  }

  // op 41596: delete_range
  status = db->DeleteRange(write_options, "key6086", "key9238");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 41597: put
  db->Put(write_options, "key8557", "value75873");

  // op 41598: get
  status = db->Get(read_options, "key2182", &value);
  if (status.ok()) {
    out << "GET key2182 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41599: delete_range
  status = db->DeleteRange(write_options, "key2064", "key11473");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 41600: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0791", "key5603", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0791, key5603) size=" << scan_result.size() << std::endl;
  }

  // op 41601: put
  db->Put(write_options, "key6359", "value38026");

  // op 41602: put
  db->Put(write_options, "key2626", "value39603");

  // op 41603: put
  db->Put(write_options, "key5446", "value44942");

  // op 41604: get
  status = db->Get(read_options, "key7378", &value);
  if (status.ok()) {
    out << "GET key7378 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41605: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 41606: put
  db->Put(write_options, "key9489", "value30108");

  // op 41607: put
  db->Put(write_options, "key2236", "value78866");

  // op 41608: put
  db->Put(write_options, "key7480", "value41127");

  // op 41609: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4133", "key8204", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4133, key8204) size=" << scan_result.size() << std::endl;
  }

  // op 41610: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2623", "key11282", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2623, key11282) size=" << scan_result.size() << std::endl;
  }

  // op 41611: put
  db->Put(write_options, "key1206", "value15425");

  // op 41612: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 41613: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5287", "key10647", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5287, key10647) size=" << scan_result.size() << std::endl;
  }

  // op 41614: get
  status = db->Get(read_options, "key7642", &value);
  if (status.ok()) {
    out << "GET key7642 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41615: put
  db->Put(write_options, "key4232", "value77396");

  // op 41616: get
  status = db->Get(read_options, "key0542", &value);
  if (status.ok()) {
    out << "GET key0542 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41617: get
  status = db->Get(read_options, "key8888", &value);
  if (status.ok()) {
    out << "GET key8888 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41618: delete
  db->Delete(write_options, "key2179");

  // op 41619: put
  db->Put(write_options, "key8162", "value112400");

  // op 41620: put
  db->Put(write_options, "key6659", "value27996");

  // op 41621: get
  status = db->Get(read_options, "key7727", &value);
  if (status.ok()) {
    out << "GET key7727 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41622: delete_range
  status = db->DeleteRange(write_options, "key1845", "key8179");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 41623: put
  db->Put(write_options, "key10907", "value65937");

  // op 41624: delete_range
  status = db->DeleteRange(write_options, "key1585", "key11420");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 41625: get
  status = db->Get(read_options, "key4860", &value);
  if (status.ok()) {
    out << "GET key4860 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41626: put
  db->Put(write_options, "key7259", "value39982");

  // op 41627: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1428", "key4735", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1428, key4735) size=" << scan_result.size() << std::endl;
  }

  // op 41628: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4072", "key4149", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4072, key4149) size=" << scan_result.size() << std::endl;
  }

  // op 41629: get
  status = db->Get(read_options, "key6837", &value);
  if (status.ok()) {
    out << "GET key6837 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41630: get
  status = db->Get(read_options, "key8203", &value);
  if (status.ok()) {
    out << "GET key8203 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41631: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3204", "key11361", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3204, key11361) size=" << scan_result.size() << std::endl;
  }

  // op 41632: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0488", "key7487", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0488, key7487) size=" << scan_result.size() << std::endl;
  }

  // op 41633: delete
  db->Delete(write_options, "key6477");

  // op 41634: delete
  db->Delete(write_options, "key8096");

  // op 41635: delete_range
  status = db->DeleteRange(write_options, "key2490", "key6884");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 41636: put
  db->Put(write_options, "key1441", "value76022");

  // op 41637: put
  db->Put(write_options, "key9140", "value42562");

  // op 41638: get
  status = db->Get(read_options, "key1017", &value);
  if (status.ok()) {
    out << "GET key1017 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41639: put
  db->Put(write_options, "key10358", "value114118");

  // op 41640: put
  db->Put(write_options, "key7380", "value13157");

  // op 41641: put
  db->Put(write_options, "key2229", "value44058");

  // op 41642: get
  status = db->Get(read_options, "key10556", &value);
  if (status.ok()) {
    out << "GET key10556 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41643: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 41644: put
  db->Put(write_options, "key4292", "value67173");

  // op 41645: get
  status = db->Get(read_options, "key6950", &value);
  if (status.ok()) {
    out << "GET key6950 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41646: put
  db->Put(write_options, "key3538", "value68100");

  // op 41647: delete
  db->Delete(write_options, "key0829");

  // op 41648: delete
  db->Delete(write_options, "key1329");

  // op 41649: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0673", "key1776", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0673, key1776) size=" << scan_result.size() << std::endl;
  }

  // op 41650: delete_range
  status = db->DeleteRange(write_options, "key0821", "key5515");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 41651: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 41652: put
  db->Put(write_options, "key0795", "value10095");

  // op 41653: put
  db->Put(write_options, "key7718", "value90616");

  // op 41654: put
  db->Put(write_options, "key10364", "value13705");

  // op 41655: delete
  db->Delete(write_options, "key6862");

  // op 41656: get
  status = db->Get(read_options, "key6800", &value);
  if (status.ok()) {
    out << "GET key6800 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41657: put
  db->Put(write_options, "key1883", "value11429");

  // op 41658: get
  status = db->Get(read_options, "key5312", &value);
  if (status.ok()) {
    out << "GET key5312 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41659: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4614", "key10377", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4614, key10377) size=" << scan_result.size() << std::endl;
  }

  // op 41660: put
  db->Put(write_options, "key4212", "value33193");

  // op 41661: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0095", "key3069", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0095, key3069) size=" << scan_result.size() << std::endl;
  }

  // op 41662: get
  status = db->Get(read_options, "key0203", &value);
  if (status.ok()) {
    out << "GET key0203 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41663: put
  db->Put(write_options, "key5870", "value45560");

  // op 41664: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 41665: get
  status = db->Get(read_options, "key2656", &value);
  if (status.ok()) {
    out << "GET key2656 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41666: put
  db->Put(write_options, "key11554", "value78961");

  // op 41667: get
  status = db->Get(read_options, "key5284", &value);
  if (status.ok()) {
    out << "GET key5284 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41668: put
  db->Put(write_options, "key9610", "value8406");

  // op 41669: put
  db->Put(write_options, "key11483", "value2367");

  // op 41670: get
  status = db->Get(read_options, "key6444", &value);
  if (status.ok()) {
    out << "GET key6444 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41671: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0646", "key7871", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0646, key7871) size=" << scan_result.size() << std::endl;
  }

  // op 41672: put
  db->Put(write_options, "key0730", "value109522");

  // op 41673: delete
  db->Delete(write_options, "key11006");

  // op 41674: get
  status = db->Get(read_options, "key10602", &value);
  if (status.ok()) {
    out << "GET key10602 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41675: get
  status = db->Get(read_options, "key3258", &value);
  if (status.ok()) {
    out << "GET key3258 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41676: delete
  db->Delete(write_options, "key1955");

  // op 41677: get
  status = db->Get(read_options, "key0672", &value);
  if (status.ok()) {
    out << "GET key0672 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41678: get
  status = db->Get(read_options, "key5718", &value);
  if (status.ok()) {
    out << "GET key5718 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41679: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2171", "key10255", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2171, key10255) size=" << scan_result.size() << std::endl;
  }

  // op 41680: put
  db->Put(write_options, "key6226", "value59029");

  // op 41681: get
  status = db->Get(read_options, "key3510", &value);
  if (status.ok()) {
    out << "GET key3510 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41682: put
  db->Put(write_options, "key8589", "value33274");

  // op 41683: put
  db->Put(write_options, "key4774", "value1239");

  // op 41684: put
  db->Put(write_options, "key4645", "value38542");

  // op 41685: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 41686: delete_range
  status = db->DeleteRange(write_options, "key0767", "key3321");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 41687: delete
  db->Delete(write_options, "key2055");

  // op 41688: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6683", "key11526", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6683, key11526) size=" << scan_result.size() << std::endl;
  }

  // op 41689: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2169", "key2919", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2169, key2919) size=" << scan_result.size() << std::endl;
  }

  // op 41690: get
  status = db->Get(read_options, "key7372", &value);
  if (status.ok()) {
    out << "GET key7372 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41691: put
  db->Put(write_options, "key7555", "value10227");

  // op 41692: put
  db->Put(write_options, "key0878", "value33676");

  // op 41693: delete
  db->Delete(write_options, "key8880");

  // op 41694: delete_range
  status = db->DeleteRange(write_options, "key0693", "key6407");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 41695: delete
  db->Delete(write_options, "key0469");

  // op 41696: delete
  db->Delete(write_options, "key4484");

  // op 41697: put
  db->Put(write_options, "key8219", "value27198");

  // op 41698: get
  status = db->Get(read_options, "key5106", &value);
  if (status.ok()) {
    out << "GET key5106 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41699: get
  status = db->Get(read_options, "key3413", &value);
  if (status.ok()) {
    out << "GET key3413 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41700: delete
  db->Delete(write_options, "key8239");

  // op 41701: put
  db->Put(write_options, "key6150", "value24896");

  // op 41702: put
  db->Put(write_options, "key4694", "value52543");

  // op 41703: delete_range
  status = db->DeleteRange(write_options, "key1528", "key10949");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 41704: put
  db->Put(write_options, "key6417", "value90977");

  // op 41705: put
  db->Put(write_options, "key8973", "value103433");

  // op 41706: put
  db->Put(write_options, "key2473", "value59564");

  // op 41707: get
  status = db->Get(read_options, "key5481", &value);
  if (status.ok()) {
    out << "GET key5481 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41708: put
  db->Put(write_options, "key2248", "value105096");

  // op 41709: put
  db->Put(write_options, "key4051", "value8063");

  // op 41710: put
  db->Put(write_options, "key9181", "value27673");

  // op 41711: put
  db->Put(write_options, "key2694", "value96680");

  // op 41712: put
  db->Put(write_options, "key3568", "value104106");

  // op 41713: delete
  db->Delete(write_options, "key10296");

  // op 41714: delete_range
  status = db->DeleteRange(write_options, "key2679", "key4420");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 41715: put
  db->Put(write_options, "key4052", "value32270");

  // op 41716: get
  status = db->Get(read_options, "key0456", &value);
  if (status.ok()) {
    out << "GET key0456 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41717: delete_range
  status = db->DeleteRange(write_options, "key9023", "key11174");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 41718: delete_range
  status = db->DeleteRange(write_options, "key5673", "key7155");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 41719: put
  db->Put(write_options, "key5454", "value41190");

  // op 41720: put
  db->Put(write_options, "key3331", "value78509");

  // op 41721: delete_range
  status = db->DeleteRange(write_options, "key6411", "key8735");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 41722: put
  db->Put(write_options, "key7540", "value43868");

  // op 41723: delete
  db->Delete(write_options, "key10825");

  // op 41724: delete
  db->Delete(write_options, "key2793");

  // op 41725: get
  status = db->Get(read_options, "key4753", &value);
  if (status.ok()) {
    out << "GET key4753 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41726: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 41727: put
  db->Put(write_options, "key5562", "value89204");

  // op 41728: get
  status = db->Get(read_options, "key5572", &value);
  if (status.ok()) {
    out << "GET key5572 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41729: delete_range
  status = db->DeleteRange(write_options, "key2437", "key10325");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 41730: put
  db->Put(write_options, "key9566", "value56988");

  // op 41731: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2623", "key10694", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2623, key10694) size=" << scan_result.size() << std::endl;
  }

  // op 41732: put
  db->Put(write_options, "key11621", "value83605");

  // op 41733: put
  db->Put(write_options, "key1175", "value25361");

  // op 41734: put
  db->Put(write_options, "key4437", "value52552");

  // op 41735: put
  db->Put(write_options, "key4474", "value61453");

  // op 41736: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 41737: delete
  db->Delete(write_options, "key2768");

  // op 41738: put
  db->Put(write_options, "key7645", "value10727");

  // op 41739: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0903", "key9366", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0903, key9366) size=" << scan_result.size() << std::endl;
  }

  // op 41740: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 41741: put
  db->Put(write_options, "key8247", "value95458");

  // op 41742: put
  db->Put(write_options, "key11371", "value78244");

  // op 41743: get
  status = db->Get(read_options, "key7055", &value);
  if (status.ok()) {
    out << "GET key7055 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41744: put
  db->Put(write_options, "key7145", "value89346");

  // op 41745: put
  db->Put(write_options, "key1930", "value106821");

  // op 41746: get
  status = db->Get(read_options, "key5094", &value);
  if (status.ok()) {
    out << "GET key5094 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41747: delete_range
  status = db->DeleteRange(write_options, "key3367", "key6726");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 41748: put
  db->Put(write_options, "key2922", "value86078");

  // op 41749: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5761", "key6034", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5761, key6034) size=" << scan_result.size() << std::endl;
  }

  // op 41750: delete
  db->Delete(write_options, "key7804");

  // op 41751: get
  status = db->Get(read_options, "key9288", &value);
  if (status.ok()) {
    out << "GET key9288 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41752: delete_range
  status = db->DeleteRange(write_options, "key0953", "key3011");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 41753: delete
  db->Delete(write_options, "key2780");

  // op 41754: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3410", "key8640", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3410, key8640) size=" << scan_result.size() << std::endl;
  }

  // op 41755: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0833", "key4447", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0833, key4447) size=" << scan_result.size() << std::endl;
  }

  // op 41756: put
  db->Put(write_options, "key8274", "value12994");

  // op 41757: put
  db->Put(write_options, "key2192", "value93519");

  // op 41758: put
  db->Put(write_options, "key4065", "value87086");

  // op 41759: get
  status = db->Get(read_options, "key10297", &value);
  if (status.ok()) {
    out << "GET key10297 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41760: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0438", "key6137", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0438, key6137) size=" << scan_result.size() << std::endl;
  }

  // op 41761: delete_range
  status = db->DeleteRange(write_options, "key5284", "key7520");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 41762: get
  status = db->Get(read_options, "key5344", &value);
  if (status.ok()) {
    out << "GET key5344 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41763: put
  db->Put(write_options, "key9486", "value102358");

  // op 41764: get
  status = db->Get(read_options, "key7780", &value);
  if (status.ok()) {
    out << "GET key7780 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41765: delete
  db->Delete(write_options, "key6097");

  // op 41766: put
  db->Put(write_options, "key10001", "value63755");

  // op 41767: put
  db->Put(write_options, "key5654", "value93154");

  // op 41768: get
  status = db->Get(read_options, "key10406", &value);
  if (status.ok()) {
    out << "GET key10406 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41769: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2271", "key8275", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2271, key8275) size=" << scan_result.size() << std::endl;
  }

  // op 41770: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 41771: put
  db->Put(write_options, "key1285", "value31524");

  // op 41772: delete_range
  status = db->DeleteRange(write_options, "key0439", "key0818");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 41773: put
  db->Put(write_options, "key3567", "value116269");

  // op 41774: put
  db->Put(write_options, "key2450", "value19181");

  // op 41775: get
  status = db->Get(read_options, "key1337", &value);
  if (status.ok()) {
    out << "GET key1337 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41776: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6136", "key8751", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6136, key8751) size=" << scan_result.size() << std::endl;
  }

  // op 41777: put
  db->Put(write_options, "key10527", "value4983");

  // op 41778: put
  db->Put(write_options, "key0351", "value19895");

  // op 41779: put
  db->Put(write_options, "key5784", "value96558");

  // op 41780: delete
  db->Delete(write_options, "key1841");

  // op 41781: delete
  db->Delete(write_options, "key6487");

  // op 41782: delete_range
  status = db->DeleteRange(write_options, "key2865", "key9104");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 41783: get
  status = db->Get(read_options, "key4151", &value);
  if (status.ok()) {
    out << "GET key4151 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41784: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5498", "key7432", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5498, key7432) size=" << scan_result.size() << std::endl;
  }

  // op 41785: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0015", "key11350", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0015, key11350) size=" << scan_result.size() << std::endl;
  }

  // op 41786: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1094", "key4138", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1094, key4138) size=" << scan_result.size() << std::endl;
  }

  // op 41787: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 41788: put
  db->Put(write_options, "key2851", "value54540");

  // op 41789: put
  db->Put(write_options, "key4449", "value9756");

  // op 41790: put
  db->Put(write_options, "key10212", "value62632");

  // op 41791: get
  status = db->Get(read_options, "key8779", &value);
  if (status.ok()) {
    out << "GET key8779 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41792: put
  db->Put(write_options, "key2544", "value38086");

  // op 41793: put
  db->Put(write_options, "key2118", "value109706");

  // op 41794: put
  db->Put(write_options, "key5928", "value69184");

  // op 41795: put
  db->Put(write_options, "key7168", "value71029");

  // op 41796: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6982", "key10600", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6982, key10600) size=" << scan_result.size() << std::endl;
  }

  // op 41797: delete
  db->Delete(write_options, "key2294");

  // op 41798: put
  db->Put(write_options, "key0102", "value99915");

  // op 41799: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 41800: delete_range
  status = db->DeleteRange(write_options, "key2865", "key11555");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 41801: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2666", "key4223", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2666, key4223) size=" << scan_result.size() << std::endl;
  }

  // op 41802: get
  status = db->Get(read_options, "key9433", &value);
  if (status.ok()) {
    out << "GET key9433 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41803: get
  status = db->Get(read_options, "key8535", &value);
  if (status.ok()) {
    out << "GET key8535 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41804: put
  db->Put(write_options, "key9534", "value99068");

  // op 41805: delete_range
  status = db->DeleteRange(write_options, "key2299", "key10958");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 41806: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1038", "key7937", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1038, key7937) size=" << scan_result.size() << std::endl;
  }

  // op 41807: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7659", "key8039", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7659, key8039) size=" << scan_result.size() << std::endl;
  }

  // op 41808: delete_range
  status = db->DeleteRange(write_options, "key1498", "key10057");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 41809: put
  db->Put(write_options, "key9740", "value5983");

  // op 41810: delete_range
  status = db->DeleteRange(write_options, "key2418", "key8565");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 41811: put
  db->Put(write_options, "key4975", "value106192");

  // op 41812: delete
  db->Delete(write_options, "key3715");

  // op 41813: get
  status = db->Get(read_options, "key4315", &value);
  if (status.ok()) {
    out << "GET key4315 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41814: put
  db->Put(write_options, "key0223", "value74759");

  // op 41815: put
  db->Put(write_options, "key2491", "value100738");

  // op 41816: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8146", "key10734", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8146, key10734) size=" << scan_result.size() << std::endl;
  }

  // op 41817: put
  db->Put(write_options, "key9095", "value41597");

  // op 41818: get
  status = db->Get(read_options, "key4719", &value);
  if (status.ok()) {
    out << "GET key4719 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41819: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5997", "key6883", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5997, key6883) size=" << scan_result.size() << std::endl;
  }

  // op 41820: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7406", "key9153", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7406, key9153) size=" << scan_result.size() << std::endl;
  }

  // op 41821: get
  status = db->Get(read_options, "key4046", &value);
  if (status.ok()) {
    out << "GET key4046 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41822: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0532", "key11087", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0532, key11087) size=" << scan_result.size() << std::endl;
  }

  // op 41823: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7368", "key9861", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7368, key9861) size=" << scan_result.size() << std::endl;
  }

  // op 41824: delete_range
  status = db->DeleteRange(write_options, "key0451", "key10287");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 41825: put
  db->Put(write_options, "key2723", "value90310");

  // op 41826: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 41827: put
  db->Put(write_options, "key5190", "value68234");

  // op 41828: put
  db->Put(write_options, "key6271", "value65761");

  // op 41829: get
  status = db->Get(read_options, "key6083", &value);
  if (status.ok()) {
    out << "GET key6083 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41830: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 41831: get
  status = db->Get(read_options, "key11267", &value);
  if (status.ok()) {
    out << "GET key11267 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41832: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3118", "key9112", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3118, key9112) size=" << scan_result.size() << std::endl;
  }

  // op 41833: put
  db->Put(write_options, "key11616", "value54860");

  // op 41834: put
  db->Put(write_options, "key3087", "value35123");

  // op 41835: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3652", "key9157", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3652, key9157) size=" << scan_result.size() << std::endl;
  }

  // op 41836: put
  db->Put(write_options, "key9815", "value3519");

  // op 41837: delete
  db->Delete(write_options, "key10682");

  // op 41838: get
  status = db->Get(read_options, "key3750", &value);
  if (status.ok()) {
    out << "GET key3750 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41839: put
  db->Put(write_options, "key6892", "value51701");

  // op 41840: get
  status = db->Get(read_options, "key2205", &value);
  if (status.ok()) {
    out << "GET key2205 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41841: get
  status = db->Get(read_options, "key4269", &value);
  if (status.ok()) {
    out << "GET key4269 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41842: put
  db->Put(write_options, "key10551", "value115158");

  // op 41843: put
  db->Put(write_options, "key2315", "value37291");

  // op 41844: get
  status = db->Get(read_options, "key3295", &value);
  if (status.ok()) {
    out << "GET key3295 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41845: delete_range
  status = db->DeleteRange(write_options, "key0780", "key5938");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 41846: get
  status = db->Get(read_options, "key3811", &value);
  if (status.ok()) {
    out << "GET key3811 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41847: delete
  db->Delete(write_options, "key0593");

  // op 41848: delete
  db->Delete(write_options, "key2346");

  // op 41849: put
  db->Put(write_options, "key11400", "value64728");

  // op 41850: delete_range
  status = db->DeleteRange(write_options, "key6715", "key11063");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 41851: delete_range
  status = db->DeleteRange(write_options, "key5901", "key9751");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 41852: get
  status = db->Get(read_options, "key0657", &value);
  if (status.ok()) {
    out << "GET key0657 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41853: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3123", "key7742", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3123, key7742) size=" << scan_result.size() << std::endl;
  }

  // op 41854: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1174", "key3491", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1174, key3491) size=" << scan_result.size() << std::endl;
  }

  // op 41855: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 41856: put
  db->Put(write_options, "key1461", "value19207");

  // op 41857: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9873", "key10827", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9873, key10827) size=" << scan_result.size() << std::endl;
  }

  // op 41858: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1091", "key10602", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1091, key10602) size=" << scan_result.size() << std::endl;
  }

  // op 41859: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 41860: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0411", "key5447", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0411, key5447) size=" << scan_result.size() << std::endl;
  }

  // op 41861: put
  db->Put(write_options, "key5174", "value96965");

  // op 41862: delete
  db->Delete(write_options, "key7802");

  // op 41863: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4347", "key10511", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4347, key10511) size=" << scan_result.size() << std::endl;
  }

  // op 41864: get
  status = db->Get(read_options, "key4431", &value);
  if (status.ok()) {
    out << "GET key4431 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41865: get
  status = db->Get(read_options, "key5584", &value);
  if (status.ok()) {
    out << "GET key5584 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41866: put
  db->Put(write_options, "key3174", "value24268");

  // op 41867: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 41868: get
  status = db->Get(read_options, "key7039", &value);
  if (status.ok()) {
    out << "GET key7039 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41869: put
  db->Put(write_options, "key1033", "value65079");

  // op 41870: get
  status = db->Get(read_options, "key2188", &value);
  if (status.ok()) {
    out << "GET key2188 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41871: put
  db->Put(write_options, "key0034", "value23890");

  // op 41872: put
  db->Put(write_options, "key1271", "value49124");

  // op 41873: put
  db->Put(write_options, "key3888", "value103236");

  // op 41874: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8981", "key10495", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8981, key10495) size=" << scan_result.size() << std::endl;
  }

  // op 41875: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8185", "key10515", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8185, key10515) size=" << scan_result.size() << std::endl;
  }

  // op 41876: delete_range
  status = db->DeleteRange(write_options, "key3947", "key4439");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 41877: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0595", "key3311", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0595, key3311) size=" << scan_result.size() << std::endl;
  }

  // op 41878: put
  db->Put(write_options, "key0528", "value59852");

  // op 41879: put
  db->Put(write_options, "key5895", "value5518");

  // op 41880: delete
  db->Delete(write_options, "key10520");

  // op 41881: get
  status = db->Get(read_options, "key6583", &value);
  if (status.ok()) {
    out << "GET key6583 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41882: put
  db->Put(write_options, "key1713", "value22011");

  // op 41883: put
  db->Put(write_options, "key11449", "value101525");

  // op 41884: put
  db->Put(write_options, "key6752", "value44446");

  // op 41885: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 41886: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4333", "key7275", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4333, key7275) size=" << scan_result.size() << std::endl;
  }

  // op 41887: delete_range
  status = db->DeleteRange(write_options, "key0603", "key3571");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 41888: put
  db->Put(write_options, "key7670", "value89224");

  // op 41889: put
  db->Put(write_options, "key9080", "value59300");

  // op 41890: put
  db->Put(write_options, "key8145", "value81445");

  // op 41891: put
  db->Put(write_options, "key0081", "value69756");

  // op 41892: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6364", "key9757", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6364, key9757) size=" << scan_result.size() << std::endl;
  }

  // op 41893: put
  db->Put(write_options, "key3643", "value63605");

  // op 41894: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4860", "key8948", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4860, key8948) size=" << scan_result.size() << std::endl;
  }

  // op 41895: put
  db->Put(write_options, "key2229", "value96446");

  // op 41896: delete
  db->Delete(write_options, "key3149");

  // op 41897: delete_range
  status = db->DeleteRange(write_options, "key7798", "key7980");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 41898: delete
  db->Delete(write_options, "key5281");

  // op 41899: get
  status = db->Get(read_options, "key6763", &value);
  if (status.ok()) {
    out << "GET key6763 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41900: put
  db->Put(write_options, "key2262", "value110536");

  // op 41901: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4826", "key8115", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4826, key8115) size=" << scan_result.size() << std::endl;
  }

  // op 41902: put
  db->Put(write_options, "key3205", "value117287");

  // op 41903: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4871", "key7635", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4871, key7635) size=" << scan_result.size() << std::endl;
  }

  // op 41904: put
  db->Put(write_options, "key2447", "value1072");

  // op 41905: put
  db->Put(write_options, "key7874", "value9919");

  // op 41906: put
  db->Put(write_options, "key1404", "value69709");

  // op 41907: get
  status = db->Get(read_options, "key1080", &value);
  if (status.ok()) {
    out << "GET key1080 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41908: get
  status = db->Get(read_options, "key9955", &value);
  if (status.ok()) {
    out << "GET key9955 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41909: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8799", "key9934", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8799, key9934) size=" << scan_result.size() << std::endl;
  }

  // op 41910: delete_range
  status = db->DeleteRange(write_options, "key3379", "key3675");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 41911: put
  db->Put(write_options, "key9267", "value93121");

  // op 41912: get
  status = db->Get(read_options, "key6988", &value);
  if (status.ok()) {
    out << "GET key6988 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41913: put
  db->Put(write_options, "key3627", "value8384");

  // op 41914: get
  status = db->Get(read_options, "key3293", &value);
  if (status.ok()) {
    out << "GET key3293 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41915: put
  db->Put(write_options, "key8874", "value32230");

  // op 41916: delete_range
  status = db->DeleteRange(write_options, "key6286", "key6911");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 41917: get
  status = db->Get(read_options, "key2792", &value);
  if (status.ok()) {
    out << "GET key2792 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41918: put
  db->Put(write_options, "key8397", "value8759");

  // op 41919: get
  status = db->Get(read_options, "key11595", &value);
  if (status.ok()) {
    out << "GET key11595 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41920: put
  db->Put(write_options, "key3430", "value53244");

  // op 41921: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2897", "key3300", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2897, key3300) size=" << scan_result.size() << std::endl;
  }

  // op 41922: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2767", "key10946", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2767, key10946) size=" << scan_result.size() << std::endl;
  }

  // op 41923: put
  db->Put(write_options, "key0712", "value89714");

  // op 41924: put
  db->Put(write_options, "key9761", "value44529");

  // op 41925: get
  status = db->Get(read_options, "key0172", &value);
  if (status.ok()) {
    out << "GET key0172 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41926: delete
  db->Delete(write_options, "key5943");

  // op 41927: get
  status = db->Get(read_options, "key5238", &value);
  if (status.ok()) {
    out << "GET key5238 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41928: get
  status = db->Get(read_options, "key0810", &value);
  if (status.ok()) {
    out << "GET key0810 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41929: get
  status = db->Get(read_options, "key10308", &value);
  if (status.ok()) {
    out << "GET key10308 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41930: put
  db->Put(write_options, "key9310", "value96562");

  // op 41931: delete_range
  status = db->DeleteRange(write_options, "key4547", "key5648");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 41932: get
  status = db->Get(read_options, "key0054", &value);
  if (status.ok()) {
    out << "GET key0054 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41933: put
  db->Put(write_options, "key2251", "value75049");

  // op 41934: put
  db->Put(write_options, "key8545", "value75814");

  // op 41935: put
  db->Put(write_options, "key1743", "value99539");

  // op 41936: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 41937: put
  db->Put(write_options, "key10991", "value92412");

  // op 41938: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2686", "key5612", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2686, key5612) size=" << scan_result.size() << std::endl;
  }

  // op 41939: get
  status = db->Get(read_options, "key4312", &value);
  if (status.ok()) {
    out << "GET key4312 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41940: delete
  db->Delete(write_options, "key11377");

  // op 41941: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4567", "key8581", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4567, key8581) size=" << scan_result.size() << std::endl;
  }

  // op 41942: put
  db->Put(write_options, "key1340", "value68484");

  // op 41943: get
  status = db->Get(read_options, "key0195", &value);
  if (status.ok()) {
    out << "GET key0195 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41944: put
  db->Put(write_options, "key9222", "value37915");

  // op 41945: put
  db->Put(write_options, "key9914", "value116507");

  // op 41946: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7569", "key9579", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7569, key9579) size=" << scan_result.size() << std::endl;
  }

  // op 41947: put
  db->Put(write_options, "key5765", "value16124");

  // op 41948: delete_range
  status = db->DeleteRange(write_options, "key1158", "key4388");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 41949: delete
  db->Delete(write_options, "key0245");

  // op 41950: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 41951: delete
  db->Delete(write_options, "key1013");

  // op 41952: get
  status = db->Get(read_options, "key6426", &value);
  if (status.ok()) {
    out << "GET key6426 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41953: put
  db->Put(write_options, "key4041", "value5881");

  // op 41954: put
  db->Put(write_options, "key6948", "value11916");

  // op 41955: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 41956: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2672", "key6082", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2672, key6082) size=" << scan_result.size() << std::endl;
  }

  // op 41957: delete
  db->Delete(write_options, "key4123");

  // op 41958: get
  status = db->Get(read_options, "key8881", &value);
  if (status.ok()) {
    out << "GET key8881 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41959: put
  db->Put(write_options, "key2492", "value6637");

  // op 41960: delete
  db->Delete(write_options, "key3098");

  // op 41961: delete
  db->Delete(write_options, "key0477");

  // op 41962: put
  db->Put(write_options, "key5910", "value70745");

  // op 41963: put
  db->Put(write_options, "key11124", "value41043");

  // op 41964: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0168", "key7391", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0168, key7391) size=" << scan_result.size() << std::endl;
  }

  // op 41965: put
  db->Put(write_options, "key2034", "value111556");

  // op 41966: put
  db->Put(write_options, "key11425", "value87106");

  // op 41967: put
  db->Put(write_options, "key2620", "value2360");

  // op 41968: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 41969: get
  status = db->Get(read_options, "key3099", &value);
  if (status.ok()) {
    out << "GET key3099 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41970: put
  db->Put(write_options, "key5273", "value61631");

  // op 41971: put
  db->Put(write_options, "key10333", "value47048");

  // op 41972: get
  status = db->Get(read_options, "key7490", &value);
  if (status.ok()) {
    out << "GET key7490 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41973: put
  db->Put(write_options, "key3122", "value113997");

  // op 41974: put
  db->Put(write_options, "key10092", "value25981");

  // op 41975: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 41976: get
  status = db->Get(read_options, "key1970", &value);
  if (status.ok()) {
    out << "GET key1970 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41977: delete
  db->Delete(write_options, "key3185");

  // op 41978: put
  db->Put(write_options, "key5389", "value27365");

  // op 41979: delete_range
  status = db->DeleteRange(write_options, "key2603", "key7371");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 41980: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2971", "key3484", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2971, key3484) size=" << scan_result.size() << std::endl;
  }

  // op 41981: get
  status = db->Get(read_options, "key9910", &value);
  if (status.ok()) {
    out << "GET key9910 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 41982: put
  db->Put(write_options, "key5420", "value30414");

  // op 41983: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4397", "key9850", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4397, key9850) size=" << scan_result.size() << std::endl;
  }

  // op 41984: put
  db->Put(write_options, "key5398", "value37961");

  // op 41985: put
  db->Put(write_options, "key8003", "value28121");

  // op 41986: put
  db->Put(write_options, "key10530", "value84871");

  // op 41987: put
  db->Put(write_options, "key1724", "value42741");

  // op 41988: put
  db->Put(write_options, "key0784", "value23015");

  // op 41989: delete
  db->Delete(write_options, "key1361");

  // op 41990: put
  db->Put(write_options, "key6118", "value107516");

  // op 41991: put
  db->Put(write_options, "key4315", "value117577");

  // op 41992: put
  db->Put(write_options, "key10904", "value117196");

  // op 41993: put
  db->Put(write_options, "key3569", "value47483");

  // op 41994: delete_range
  status = db->DeleteRange(write_options, "key4088", "key10705");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 41995: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 41996: delete_range
  status = db->DeleteRange(write_options, "key1799", "key3412");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 41997: put
  db->Put(write_options, "key6668", "value84988");

  // op 41998: put
  db->Put(write_options, "key5639", "value78835");

  // op 41999: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0975", "key10767", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0975, key10767) size=" << scan_result.size() << std::endl;
  }

  // op 42000: put
  db->Put(write_options, "key0766", "value97996");

  // op 42001: put
  db->Put(write_options, "key11599", "value1195");

  // op 42002: put
  db->Put(write_options, "key3349", "value65100");

  // op 42003: put
  db->Put(write_options, "key10667", "value30808");

  // op 42004: put
  db->Put(write_options, "key11362", "value58558");

  // op 42005: put
  db->Put(write_options, "key8562", "value75940");

  // op 42006: get
  status = db->Get(read_options, "key8584", &value);
  if (status.ok()) {
    out << "GET key8584 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42007: put
  db->Put(write_options, "key11675", "value17547");

  // op 42008: put
  db->Put(write_options, "key6720", "value67594");

  // op 42009: put
  db->Put(write_options, "key10437", "value109691");

  // op 42010: delete
  db->Delete(write_options, "key10729");

  // op 42011: delete
  db->Delete(write_options, "key8204");

  // op 42012: put
  db->Put(write_options, "key0408", "value93408");

  // op 42013: put
  db->Put(write_options, "key6240", "value62798");

  // op 42014: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 42015: delete_range
  status = db->DeleteRange(write_options, "key9942", "key10838");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 42016: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2128", "key3769", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2128, key3769) size=" << scan_result.size() << std::endl;
  }

  // op 42017: put
  db->Put(write_options, "key3085", "value56554");

  // op 42018: put
  db->Put(write_options, "key8067", "value113143");

  // op 42019: get
  status = db->Get(read_options, "key1958", &value);
  if (status.ok()) {
    out << "GET key1958 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42020: put
  db->Put(write_options, "key0414", "value18663");

  // op 42021: delete
  db->Delete(write_options, "key7977");

  // op 42022: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0890", "key4674", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0890, key4674) size=" << scan_result.size() << std::endl;
  }

  // op 42023: delete_range
  status = db->DeleteRange(write_options, "key8553", "key9891");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 42024: get
  status = db->Get(read_options, "key5965", &value);
  if (status.ok()) {
    out << "GET key5965 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42025: get
  status = db->Get(read_options, "key5775", &value);
  if (status.ok()) {
    out << "GET key5775 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42026: put
  db->Put(write_options, "key0696", "value66432");

  // op 42027: put
  db->Put(write_options, "key5483", "value26734");

  // op 42028: put
  db->Put(write_options, "key8081", "value1845");

  // op 42029: get
  status = db->Get(read_options, "key10749", &value);
  if (status.ok()) {
    out << "GET key10749 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42030: delete_range
  status = db->DeleteRange(write_options, "key0203", "key6715");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 42031: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7181", "key10550", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7181, key10550) size=" << scan_result.size() << std::endl;
  }

  // op 42032: delete
  db->Delete(write_options, "key6687");

  // op 42033: put
  db->Put(write_options, "key10600", "value32313");

  // op 42034: get
  status = db->Get(read_options, "key11365", &value);
  if (status.ok()) {
    out << "GET key11365 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42035: put
  db->Put(write_options, "key1237", "value91927");

  // op 42036: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8346", "key10252", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8346, key10252) size=" << scan_result.size() << std::endl;
  }

  // op 42037: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1314", "key2247", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1314, key2247) size=" << scan_result.size() << std::endl;
  }

  // op 42038: delete_range
  status = db->DeleteRange(write_options, "key0762", "key5500");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 42039: get
  status = db->Get(read_options, "key5969", &value);
  if (status.ok()) {
    out << "GET key5969 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42040: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 42041: get
  status = db->Get(read_options, "key5953", &value);
  if (status.ok()) {
    out << "GET key5953 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42042: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4307", "key8199", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4307, key8199) size=" << scan_result.size() << std::endl;
  }

  // op 42043: put
  db->Put(write_options, "key7965", "value8349");

  // op 42044: put
  db->Put(write_options, "key10390", "value7235");

  // op 42045: put
  db->Put(write_options, "key8644", "value6950");

  // op 42046: delete_range
  status = db->DeleteRange(write_options, "key2503", "key9618");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 42047: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1066", "key3020", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1066, key3020) size=" << scan_result.size() << std::endl;
  }

  // op 42048: delete_range
  status = db->DeleteRange(write_options, "key2694", "key8786");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 42049: delete
  db->Delete(write_options, "key4165");

  // op 42050: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8476", "key8519", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8476, key8519) size=" << scan_result.size() << std::endl;
  }

  // op 42051: get
  status = db->Get(read_options, "key5769", &value);
  if (status.ok()) {
    out << "GET key5769 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42052: put
  db->Put(write_options, "key4455", "value76507");

  // op 42053: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 42054: put
  db->Put(write_options, "key10669", "value9300");

  // op 42055: put
  db->Put(write_options, "key4770", "value81936");

  // op 42056: delete_range
  status = db->DeleteRange(write_options, "key6048", "key7219");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 42057: get
  status = db->Get(read_options, "key6593", &value);
  if (status.ok()) {
    out << "GET key6593 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42058: delete
  db->Delete(write_options, "key10219");

  // op 42059: get
  status = db->Get(read_options, "key2322", &value);
  if (status.ok()) {
    out << "GET key2322 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42060: put
  db->Put(write_options, "key1920", "value55003");

  // op 42061: put
  db->Put(write_options, "key2865", "value65177");

  // op 42062: delete
  db->Delete(write_options, "key3932");

  // op 42063: delete_range
  status = db->DeleteRange(write_options, "key4524", "key9252");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 42064: get
  status = db->Get(read_options, "key1184", &value);
  if (status.ok()) {
    out << "GET key1184 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42065: put
  db->Put(write_options, "key11639", "value47306");

  // op 42066: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8845", "key9529", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8845, key9529) size=" << scan_result.size() << std::endl;
  }

  // op 42067: delete
  db->Delete(write_options, "key9441");

  // op 42068: put
  db->Put(write_options, "key8899", "value78014");

  // op 42069: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 42070: put
  db->Put(write_options, "key3075", "value71308");

  // op 42071: put
  db->Put(write_options, "key1928", "value64661");

  // op 42072: delete
  db->Delete(write_options, "key0201");

  // op 42073: get
  status = db->Get(read_options, "key6199", &value);
  if (status.ok()) {
    out << "GET key6199 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42074: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 42075: put
  db->Put(write_options, "key4733", "value85020");

  // op 42076: put
  db->Put(write_options, "key9458", "value12329");

  // op 42077: delete_range
  status = db->DeleteRange(write_options, "key0085", "key10807");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 42078: put
  db->Put(write_options, "key2692", "value65355");

  // op 42079: put
  db->Put(write_options, "key5555", "value44442");

  // op 42080: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7375", "key10785", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7375, key10785) size=" << scan_result.size() << std::endl;
  }

  // op 42081: delete
  db->Delete(write_options, "key6479");

  // op 42082: put
  db->Put(write_options, "key11448", "value112468");

  // op 42083: put
  db->Put(write_options, "key2637", "value114412");

  // op 42084: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3181", "key10142", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3181, key10142) size=" << scan_result.size() << std::endl;
  }

  // op 42085: delete_range
  status = db->DeleteRange(write_options, "key0363", "key10060");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 42086: put
  db->Put(write_options, "key3095", "value37249");

  // op 42087: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5362", "key10454", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5362, key10454) size=" << scan_result.size() << std::endl;
  }

  // op 42088: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1030", "key6692", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1030, key6692) size=" << scan_result.size() << std::endl;
  }

  // op 42089: delete
  db->Delete(write_options, "key8158");

  // op 42090: delete_range
  status = db->DeleteRange(write_options, "key0761", "key8374");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 42091: put
  db->Put(write_options, "key2128", "value25530");

  // op 42092: put
  db->Put(write_options, "key11580", "value106579");

  // op 42093: put
  db->Put(write_options, "key7373", "value25787");

  // op 42094: get
  status = db->Get(read_options, "key6712", &value);
  if (status.ok()) {
    out << "GET key6712 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42095: get
  status = db->Get(read_options, "key7676", &value);
  if (status.ok()) {
    out << "GET key7676 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42096: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4515", "key4980", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4515, key4980) size=" << scan_result.size() << std::endl;
  }

  // op 42097: delete_range
  status = db->DeleteRange(write_options, "key5631", "key6592");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 42098: delete
  db->Delete(write_options, "key11714");

  // op 42099: delete_range
  status = db->DeleteRange(write_options, "key0808", "key4968");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 42100: put
  db->Put(write_options, "key11253", "value16102");

  // op 42101: delete
  db->Delete(write_options, "key3753");

  // op 42102: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0653", "key10150", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0653, key10150) size=" << scan_result.size() << std::endl;
  }

  // op 42103: delete
  db->Delete(write_options, "key2883");

  // op 42104: delete_range
  status = db->DeleteRange(write_options, "key0707", "key6204");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 42105: put
  db->Put(write_options, "key4529", "value78698");

  // op 42106: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7203", "key10259", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7203, key10259) size=" << scan_result.size() << std::endl;
  }

  // op 42107: put
  db->Put(write_options, "key2502", "value52603");

  // op 42108: get
  status = db->Get(read_options, "key9772", &value);
  if (status.ok()) {
    out << "GET key9772 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42109: get
  status = db->Get(read_options, "key5987", &value);
  if (status.ok()) {
    out << "GET key5987 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42110: get
  status = db->Get(read_options, "key0573", &value);
  if (status.ok()) {
    out << "GET key0573 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42111: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 42112: delete_range
  status = db->DeleteRange(write_options, "key0668", "key2335");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 42113: put
  db->Put(write_options, "key5203", "value57090");

  // op 42114: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4339", "key10591", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4339, key10591) size=" << scan_result.size() << std::endl;
  }

  // op 42115: put
  db->Put(write_options, "key9718", "value21106");

  // op 42116: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 42117: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3389", "key7552", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3389, key7552) size=" << scan_result.size() << std::endl;
  }

  // op 42118: get
  status = db->Get(read_options, "key7824", &value);
  if (status.ok()) {
    out << "GET key7824 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42119: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0440", "key8667", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0440, key8667) size=" << scan_result.size() << std::endl;
  }

  // op 42120: get
  status = db->Get(read_options, "key7255", &value);
  if (status.ok()) {
    out << "GET key7255 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42121: delete_range
  status = db->DeleteRange(write_options, "key2566", "key2596");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 42122: delete_range
  status = db->DeleteRange(write_options, "key3423", "key6293");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 42123: put
  db->Put(write_options, "key3941", "value10649");

  // op 42124: get
  status = db->Get(read_options, "key4339", &value);
  if (status.ok()) {
    out << "GET key4339 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42125: delete_range
  status = db->DeleteRange(write_options, "key8708", "key9479");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 42126: get
  status = db->Get(read_options, "key11665", &value);
  if (status.ok()) {
    out << "GET key11665 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42127: put
  db->Put(write_options, "key7817", "value57913");

  // op 42128: put
  db->Put(write_options, "key3113", "value108499");

  // op 42129: put
  db->Put(write_options, "key4503", "value71499");

  // op 42130: get
  status = db->Get(read_options, "key4491", &value);
  if (status.ok()) {
    out << "GET key4491 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42131: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 42132: put
  db->Put(write_options, "key6468", "value50278");

  // op 42133: put
  db->Put(write_options, "key11291", "value73475");

  // op 42134: put
  db->Put(write_options, "key2974", "value31984");

  // op 42135: put
  db->Put(write_options, "key3044", "value80963");

  // op 42136: put
  db->Put(write_options, "key10528", "value36856");

  // op 42137: delete_range
  status = db->DeleteRange(write_options, "key0154", "key8803");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 42138: put
  db->Put(write_options, "key1936", "value13180");

  // op 42139: delete
  db->Delete(write_options, "key6119");

  // op 42140: put
  db->Put(write_options, "key4493", "value109250");

  // op 42141: get
  status = db->Get(read_options, "key11120", &value);
  if (status.ok()) {
    out << "GET key11120 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42142: put
  db->Put(write_options, "key10875", "value27268");

  // op 42143: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2378", "key5245", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2378, key5245) size=" << scan_result.size() << std::endl;
  }

  // op 42144: put
  db->Put(write_options, "key3908", "value25299");

  // op 42145: put
  db->Put(write_options, "key10749", "value73843");

  // op 42146: delete
  db->Delete(write_options, "key5440");

  // op 42147: delete
  db->Delete(write_options, "key7195");

  // op 42148: delete_range
  status = db->DeleteRange(write_options, "key1184", "key1847");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 42149: get
  status = db->Get(read_options, "key8869", &value);
  if (status.ok()) {
    out << "GET key8869 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42150: put
  db->Put(write_options, "key11372", "value35067");

  // op 42151: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1913", "key11217", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1913, key11217) size=" << scan_result.size() << std::endl;
  }

  // op 42152: put
  db->Put(write_options, "key1895", "value69484");

  // op 42153: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2232", "key2576", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2232, key2576) size=" << scan_result.size() << std::endl;
  }

  // op 42154: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2852", "key10971", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2852, key10971) size=" << scan_result.size() << std::endl;
  }

  // op 42155: delete_range
  status = db->DeleteRange(write_options, "key3520", "key5250");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 42156: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2420", "key4773", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2420, key4773) size=" << scan_result.size() << std::endl;
  }

  // op 42157: put
  db->Put(write_options, "key1595", "value9961");

  // op 42158: get
  status = db->Get(read_options, "key0790", &value);
  if (status.ok()) {
    out << "GET key0790 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42159: put
  db->Put(write_options, "key4720", "value78602");

  // op 42160: put
  db->Put(write_options, "key7518", "value111284");

  // op 42161: put
  db->Put(write_options, "key0996", "value20917");

  // op 42162: put
  db->Put(write_options, "key5846", "value34967");

  // op 42163: put
  db->Put(write_options, "key8149", "value115320");

  // op 42164: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7376", "key10708", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7376, key10708) size=" << scan_result.size() << std::endl;
  }

  // op 42165: put
  db->Put(write_options, "key9542", "value93712");

  // op 42166: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9218", "key10854", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9218, key10854) size=" << scan_result.size() << std::endl;
  }

  // op 42167: delete
  db->Delete(write_options, "key8977");

  // op 42168: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 42169: delete
  db->Delete(write_options, "key6798");

  // op 42170: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 42171: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1246", "key4734", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1246, key4734) size=" << scan_result.size() << std::endl;
  }

  // op 42172: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1085", "key3383", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1085, key3383) size=" << scan_result.size() << std::endl;
  }

  // op 42173: put
  db->Put(write_options, "key7176", "value74410");

  // op 42174: put
  db->Put(write_options, "key9501", "value53473");

  // op 42175: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1073", "key4400", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1073, key4400) size=" << scan_result.size() << std::endl;
  }

  // op 42176: get
  status = db->Get(read_options, "key0839", &value);
  if (status.ok()) {
    out << "GET key0839 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42177: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2834", "key8852", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2834, key8852) size=" << scan_result.size() << std::endl;
  }

  // op 42178: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0882", "key9731", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0882, key9731) size=" << scan_result.size() << std::endl;
  }

  // op 42179: delete
  db->Delete(write_options, "key11151");

  // op 42180: put
  db->Put(write_options, "key3298", "value106801");

  // op 42181: put
  db->Put(write_options, "key6440", "value63112");

  // op 42182: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2608", "key3020", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2608, key3020) size=" << scan_result.size() << std::endl;
  }

  // op 42183: put
  db->Put(write_options, "key6739", "value11573");

  // op 42184: put
  db->Put(write_options, "key2198", "value98100");

  // op 42185: get
  status = db->Get(read_options, "key3277", &value);
  if (status.ok()) {
    out << "GET key3277 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42186: put
  db->Put(write_options, "key1018", "value75058");

  // op 42187: get
  status = db->Get(read_options, "key3201", &value);
  if (status.ok()) {
    out << "GET key3201 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42188: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4228", "key7004", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4228, key7004) size=" << scan_result.size() << std::endl;
  }

  // op 42189: delete
  db->Delete(write_options, "key10843");

  // op 42190: put
  db->Put(write_options, "key11461", "value110647");

  // op 42191: put
  db->Put(write_options, "key2702", "value7702");

  // op 42192: get
  status = db->Get(read_options, "key7590", &value);
  if (status.ok()) {
    out << "GET key7590 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42193: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 42194: put
  db->Put(write_options, "key3794", "value8521");

  // op 42195: put
  db->Put(write_options, "key4889", "value113974");

  // op 42196: get
  status = db->Get(read_options, "key3272", &value);
  if (status.ok()) {
    out << "GET key3272 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42197: put
  db->Put(write_options, "key4465", "value7697");

  // op 42198: put
  db->Put(write_options, "key9697", "value43098");

  // op 42199: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0007", "key3227", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0007, key3227) size=" << scan_result.size() << std::endl;
  }

  // op 42200: put
  db->Put(write_options, "key10333", "value36878");

  // op 42201: put
  db->Put(write_options, "key4865", "value76497");

  // op 42202: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3793", "key10090", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3793, key10090) size=" << scan_result.size() << std::endl;
  }

  // op 42203: put
  db->Put(write_options, "key11041", "value71694");

  // op 42204: delete_range
  status = db->DeleteRange(write_options, "key2804", "key9960");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 42205: get
  status = db->Get(read_options, "key9125", &value);
  if (status.ok()) {
    out << "GET key9125 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42206: get
  status = db->Get(read_options, "key1997", &value);
  if (status.ok()) {
    out << "GET key1997 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42207: put
  db->Put(write_options, "key1314", "value63141");

  // op 42208: get
  status = db->Get(read_options, "key1286", &value);
  if (status.ok()) {
    out << "GET key1286 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42209: put
  db->Put(write_options, "key4515", "value112561");

  // op 42210: put
  db->Put(write_options, "key10222", "value3961");

  // op 42211: put
  db->Put(write_options, "key4769", "value12078");

  // op 42212: get
  status = db->Get(read_options, "key8804", &value);
  if (status.ok()) {
    out << "GET key8804 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42213: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 42214: delete_range
  status = db->DeleteRange(write_options, "key2769", "key8056");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 42215: put
  db->Put(write_options, "key8976", "value87494");

  // op 42216: put
  db->Put(write_options, "key1093", "value29263");

  // op 42217: delete_range
  status = db->DeleteRange(write_options, "key0308", "key1172");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 42218: put
  db->Put(write_options, "key3354", "value52816");

  // op 42219: put
  db->Put(write_options, "key6936", "value113655");

  // op 42220: put
  db->Put(write_options, "key10638", "value36484");

  // op 42221: put
  db->Put(write_options, "key6404", "value85505");

  // op 42222: delete_range
  status = db->DeleteRange(write_options, "key3490", "key7866");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 42223: put
  db->Put(write_options, "key9062", "value9962");

  // op 42224: delete_range
  status = db->DeleteRange(write_options, "key7833", "key8813");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 42225: delete
  db->Delete(write_options, "key7305");

  // op 42226: put
  db->Put(write_options, "key8067", "value61750");

  // op 42227: delete_range
  status = db->DeleteRange(write_options, "key6785", "key11013");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 42228: put
  db->Put(write_options, "key8976", "value12594");

  // op 42229: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1456", "key1635", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1456, key1635) size=" << scan_result.size() << std::endl;
  }

  // op 42230: get
  status = db->Get(read_options, "key0828", &value);
  if (status.ok()) {
    out << "GET key0828 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42231: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 42232: get
  status = db->Get(read_options, "key2586", &value);
  if (status.ok()) {
    out << "GET key2586 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42233: put
  db->Put(write_options, "key0712", "value94419");

  // op 42234: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0399", "key0818", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0399, key0818) size=" << scan_result.size() << std::endl;
  }

  // op 42235: get
  status = db->Get(read_options, "key11389", &value);
  if (status.ok()) {
    out << "GET key11389 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42236: put
  db->Put(write_options, "key2752", "value111983");

  // op 42237: delete_range
  status = db->DeleteRange(write_options, "key9061", "key11509");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 42238: delete_range
  status = db->DeleteRange(write_options, "key2136", "key8522");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 42239: delete
  db->Delete(write_options, "key9676");

  // op 42240: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 42241: put
  db->Put(write_options, "key11429", "value67763");

  // op 42242: put
  db->Put(write_options, "key7457", "value80641");

  // op 42243: put
  db->Put(write_options, "key1483", "value89442");

  // op 42244: get
  status = db->Get(read_options, "key9075", &value);
  if (status.ok()) {
    out << "GET key9075 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42245: put
  db->Put(write_options, "key2278", "value5171");

  // op 42246: put
  db->Put(write_options, "key2496", "value13118");

  // op 42247: put
  db->Put(write_options, "key8703", "value51570");

  // op 42248: put
  db->Put(write_options, "key1650", "value56065");

  // op 42249: put
  db->Put(write_options, "key8981", "value24782");

  // op 42250: get
  status = db->Get(read_options, "key0101", &value);
  if (status.ok()) {
    out << "GET key0101 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42251: put
  db->Put(write_options, "key1119", "value18430");

  // op 42252: delete_range
  status = db->DeleteRange(write_options, "key9682", "key10923");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 42253: put
  db->Put(write_options, "key4959", "value49290");

  // op 42254: delete_range
  status = db->DeleteRange(write_options, "key1882", "key2119");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 42255: put
  db->Put(write_options, "key2523", "value114511");

  // op 42256: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0943", "key2788", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0943, key2788) size=" << scan_result.size() << std::endl;
  }

  // op 42257: delete
  db->Delete(write_options, "key5683");

  // op 42258: put
  db->Put(write_options, "key9052", "value113506");

  // op 42259: put
  db->Put(write_options, "key3764", "value61950");

  // op 42260: put
  db->Put(write_options, "key9260", "value101869");

  // op 42261: put
  db->Put(write_options, "key4512", "value28976");

  // op 42262: delete_range
  status = db->DeleteRange(write_options, "key4176", "key5931");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 42263: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1132", "key9279", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1132, key9279) size=" << scan_result.size() << std::endl;
  }

  // op 42264: delete
  db->Delete(write_options, "key10463");

  // op 42265: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4966", "key10397", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4966, key10397) size=" << scan_result.size() << std::endl;
  }

  // op 42266: delete
  db->Delete(write_options, "key0207");

  // op 42267: put
  db->Put(write_options, "key5850", "value9183");

  // op 42268: get
  status = db->Get(read_options, "key6925", &value);
  if (status.ok()) {
    out << "GET key6925 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42269: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6456", "key10412", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6456, key10412) size=" << scan_result.size() << std::endl;
  }

  // op 42270: delete_range
  status = db->DeleteRange(write_options, "key6336", "key7005");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 42271: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6290", "key8974", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6290, key8974) size=" << scan_result.size() << std::endl;
  }

  // op 42272: put
  db->Put(write_options, "key10906", "value50655");

  // op 42273: put
  db->Put(write_options, "key4284", "value64476");

  // op 42274: delete
  db->Delete(write_options, "key9785");

  // op 42275: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0318", "key4314", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0318, key4314) size=" << scan_result.size() << std::endl;
  }

  // op 42276: delete_range
  status = db->DeleteRange(write_options, "key6688", "key11566");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 42277: delete
  db->Delete(write_options, "key8369");

  // op 42278: put
  db->Put(write_options, "key2446", "value80190");

  // op 42279: get
  status = db->Get(read_options, "key6376", &value);
  if (status.ok()) {
    out << "GET key6376 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42280: get
  status = db->Get(read_options, "key2120", &value);
  if (status.ok()) {
    out << "GET key2120 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42281: put
  db->Put(write_options, "key5682", "value42818");

  // op 42282: put
  db->Put(write_options, "key1841", "value90762");

  // op 42283: put
  db->Put(write_options, "key5963", "value113120");

  // op 42284: delete
  db->Delete(write_options, "key9554");

  // op 42285: delete
  db->Delete(write_options, "key2982");

  // op 42286: put
  db->Put(write_options, "key11704", "value22206");

  // op 42287: put
  db->Put(write_options, "key11671", "value74608");

  // op 42288: put
  db->Put(write_options, "key8344", "value63232");

  // op 42289: delete
  db->Delete(write_options, "key5926");

  // op 42290: get
  status = db->Get(read_options, "key3989", &value);
  if (status.ok()) {
    out << "GET key3989 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42291: delete
  db->Delete(write_options, "key10554");

  // op 42292: put
  db->Put(write_options, "key0438", "value17615");

  // op 42293: put
  db->Put(write_options, "key11565", "value8797");

  // op 42294: get
  status = db->Get(read_options, "key0505", &value);
  if (status.ok()) {
    out << "GET key0505 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42295: get
  status = db->Get(read_options, "key1353", &value);
  if (status.ok()) {
    out << "GET key1353 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42296: put
  db->Put(write_options, "key10228", "value114214");

  // op 42297: delete_range
  status = db->DeleteRange(write_options, "key8441", "key9914");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 42298: put
  db->Put(write_options, "key7899", "value23210");

  // op 42299: delete_range
  status = db->DeleteRange(write_options, "key9853", "key10600");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 42300: put
  db->Put(write_options, "key1011", "value31049");

  // op 42301: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5360", "key9248", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5360, key9248) size=" << scan_result.size() << std::endl;
  }

  // op 42302: put
  db->Put(write_options, "key11547", "value62353");

  // op 42303: delete
  db->Delete(write_options, "key2358");

  // op 42304: put
  db->Put(write_options, "key6317", "value84677");

  // op 42305: delete_range
  status = db->DeleteRange(write_options, "key2798", "key8926");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 42306: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 42307: put
  db->Put(write_options, "key8980", "value77753");

  // op 42308: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4944", "key6472", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4944, key6472) size=" << scan_result.size() << std::endl;
  }

  // op 42309: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 42310: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 42311: get
  status = db->Get(read_options, "key0502", &value);
  if (status.ok()) {
    out << "GET key0502 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42312: put
  db->Put(write_options, "key9578", "value116793");

  // op 42313: put
  db->Put(write_options, "key3561", "value106634");

  // op 42314: put
  db->Put(write_options, "key2122", "value88102");

  // op 42315: put
  db->Put(write_options, "key10771", "value66498");

  // op 42316: get
  status = db->Get(read_options, "key0246", &value);
  if (status.ok()) {
    out << "GET key0246 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42317: put
  db->Put(write_options, "key4066", "value107620");

  // op 42318: put
  db->Put(write_options, "key5939", "value77743");

  // op 42319: get
  status = db->Get(read_options, "key2779", &value);
  if (status.ok()) {
    out << "GET key2779 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42320: put
  db->Put(write_options, "key0798", "value33898");

  // op 42321: put
  db->Put(write_options, "key6080", "value31459");

  // op 42322: put
  db->Put(write_options, "key1169", "value53209");

  // op 42323: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0179", "key0797", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0179, key0797) size=" << scan_result.size() << std::endl;
  }

  // op 42324: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3507", "key5148", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3507, key5148) size=" << scan_result.size() << std::endl;
  }

  // op 42325: put
  db->Put(write_options, "key11584", "value14217");

  // op 42326: delete_range
  status = db->DeleteRange(write_options, "key10885", "key10899");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 42327: put
  db->Put(write_options, "key3710", "value58375");

  // op 42328: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7090", "key8698", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7090, key8698) size=" << scan_result.size() << std::endl;
  }

  // op 42329: put
  db->Put(write_options, "key3012", "value93786");

  // op 42330: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0265", "key8681", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0265, key8681) size=" << scan_result.size() << std::endl;
  }

  // op 42331: put
  db->Put(write_options, "key5451", "value54296");

  // op 42332: get
  status = db->Get(read_options, "key3854", &value);
  if (status.ok()) {
    out << "GET key3854 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42333: get
  status = db->Get(read_options, "key7789", &value);
  if (status.ok()) {
    out << "GET key7789 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42334: get
  status = db->Get(read_options, "key7563", &value);
  if (status.ok()) {
    out << "GET key7563 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42335: put
  db->Put(write_options, "key1212", "value31357");

  // op 42336: get
  status = db->Get(read_options, "key9674", &value);
  if (status.ok()) {
    out << "GET key9674 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42337: delete_range
  status = db->DeleteRange(write_options, "key1565", "key4335");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 42338: put
  db->Put(write_options, "key4310", "value83840");

  // op 42339: put
  db->Put(write_options, "key11293", "value19034");

  // op 42340: put
  db->Put(write_options, "key7191", "value14731");

  // op 42341: put
  db->Put(write_options, "key5534", "value85337");

  // op 42342: put
  db->Put(write_options, "key7231", "value75721");

  // op 42343: delete_range
  status = db->DeleteRange(write_options, "key2097", "key2537");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 42344: put
  db->Put(write_options, "key11149", "value60897");

  // op 42345: put
  db->Put(write_options, "key5371", "value115830");

  // op 42346: put
  db->Put(write_options, "key4294", "value14311");

  // op 42347: put
  db->Put(write_options, "key0295", "value42517");

  // op 42348: put
  db->Put(write_options, "key11695", "value75465");

  // op 42349: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5486", "key7394", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5486, key7394) size=" << scan_result.size() << std::endl;
  }

  // op 42350: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5844", "key7141", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5844, key7141) size=" << scan_result.size() << std::endl;
  }

  // op 42351: put
  db->Put(write_options, "key6709", "value39647");

  // op 42352: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 42353: get
  status = db->Get(read_options, "key1264", &value);
  if (status.ok()) {
    out << "GET key1264 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42354: delete
  db->Delete(write_options, "key6025");

  // op 42355: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1115", "key1755", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1115, key1755) size=" << scan_result.size() << std::endl;
  }

  // op 42356: delete
  db->Delete(write_options, "key2962");

  // op 42357: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3003", "key10858", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3003, key10858) size=" << scan_result.size() << std::endl;
  }

  // op 42358: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6721", "key11716", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6721, key11716) size=" << scan_result.size() << std::endl;
  }

  // op 42359: put
  db->Put(write_options, "key4580", "value78758");

  // op 42360: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1772", "key9302", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1772, key9302) size=" << scan_result.size() << std::endl;
  }

  // op 42361: get
  status = db->Get(read_options, "key5684", &value);
  if (status.ok()) {
    out << "GET key5684 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42362: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0175", "key8004", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0175, key8004) size=" << scan_result.size() << std::endl;
  }

  // op 42363: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9600", "key10827", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9600, key10827) size=" << scan_result.size() << std::endl;
  }

  // op 42364: get
  status = db->Get(read_options, "key5839", &value);
  if (status.ok()) {
    out << "GET key5839 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42365: delete
  db->Delete(write_options, "key1884");

  // op 42366: put
  db->Put(write_options, "key10457", "value43186");

  // op 42367: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2153", "key9421", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2153, key9421) size=" << scan_result.size() << std::endl;
  }

  // op 42368: get
  status = db->Get(read_options, "key10033", &value);
  if (status.ok()) {
    out << "GET key10033 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42369: put
  db->Put(write_options, "key11018", "value98116");

  // op 42370: get
  status = db->Get(read_options, "key4243", &value);
  if (status.ok()) {
    out << "GET key4243 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42371: put
  db->Put(write_options, "key5257", "value111445");

  // op 42372: put
  db->Put(write_options, "key9669", "value99828");

  // op 42373: put
  db->Put(write_options, "key6690", "value38545");

  // op 42374: delete_range
  status = db->DeleteRange(write_options, "key0825", "key5629");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 42375: get
  status = db->Get(read_options, "key8239", &value);
  if (status.ok()) {
    out << "GET key8239 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42376: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 42377: put
  db->Put(write_options, "key1165", "value6092");

  // op 42378: put
  db->Put(write_options, "key1678", "value77082");

  // op 42379: delete
  db->Delete(write_options, "key9023");

  // op 42380: put
  db->Put(write_options, "key3035", "value17675");

  // op 42381: put
  db->Put(write_options, "key5932", "value80291");

  // op 42382: put
  db->Put(write_options, "key10462", "value90716");

  // op 42383: put
  db->Put(write_options, "key0991", "value15712");

  // op 42384: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 42385: get
  status = db->Get(read_options, "key10279", &value);
  if (status.ok()) {
    out << "GET key10279 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42386: delete
  db->Delete(write_options, "key4075");

  // op 42387: put
  db->Put(write_options, "key6482", "value110666");

  // op 42388: delete_range
  status = db->DeleteRange(write_options, "key3363", "key8783");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 42389: put
  db->Put(write_options, "key5650", "value78486");

  // op 42390: put
  db->Put(write_options, "key11726", "value113668");

  // op 42391: put
  db->Put(write_options, "key6698", "value73271");

  // op 42392: delete
  db->Delete(write_options, "key2022");

  // op 42393: put
  db->Put(write_options, "key6860", "value30673");

  // op 42394: get
  status = db->Get(read_options, "key5741", &value);
  if (status.ok()) {
    out << "GET key5741 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42395: delete
  db->Delete(write_options, "key7703");

  // op 42396: delete_range
  status = db->DeleteRange(write_options, "key5074", "key11354");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 42397: delete_range
  status = db->DeleteRange(write_options, "key1754", "key8730");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 42398: put
  db->Put(write_options, "key3249", "value76811");

  // op 42399: get
  status = db->Get(read_options, "key4927", &value);
  if (status.ok()) {
    out << "GET key4927 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42400: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0238", "key10773", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0238, key10773) size=" << scan_result.size() << std::endl;
  }

  // op 42401: get
  status = db->Get(read_options, "key10141", &value);
  if (status.ok()) {
    out << "GET key10141 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42402: get
  status = db->Get(read_options, "key4017", &value);
  if (status.ok()) {
    out << "GET key4017 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42403: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0341", "key8290", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0341, key8290) size=" << scan_result.size() << std::endl;
  }

  // op 42404: put
  db->Put(write_options, "key9955", "value64295");

  // op 42405: delete
  db->Delete(write_options, "key3636");

  // op 42406: get
  status = db->Get(read_options, "key5135", &value);
  if (status.ok()) {
    out << "GET key5135 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42407: delete
  db->Delete(write_options, "key8281");

  // op 42408: put
  db->Put(write_options, "key10235", "value35643");

  // op 42409: get
  status = db->Get(read_options, "key10052", &value);
  if (status.ok()) {
    out << "GET key10052 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42410: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 42411: get
  status = db->Get(read_options, "key1573", &value);
  if (status.ok()) {
    out << "GET key1573 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42412: put
  db->Put(write_options, "key2450", "value70997");

  // op 42413: delete
  db->Delete(write_options, "key2915");

  // op 42414: put
  db->Put(write_options, "key1618", "value41938");

  // op 42415: put
  db->Put(write_options, "key3226", "value89307");

  // op 42416: put
  db->Put(write_options, "key7145", "value65087");

  // op 42417: get
  status = db->Get(read_options, "key9109", &value);
  if (status.ok()) {
    out << "GET key9109 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42418: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2871", "key10791", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2871, key10791) size=" << scan_result.size() << std::endl;
  }

  // op 42419: delete
  db->Delete(write_options, "key8203");

  // op 42420: put
  db->Put(write_options, "key7462", "value91109");

  // op 42421: put
  db->Put(write_options, "key7184", "value29517");

  // op 42422: get
  status = db->Get(read_options, "key7081", &value);
  if (status.ok()) {
    out << "GET key7081 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42423: put
  db->Put(write_options, "key4601", "value89810");

  // op 42424: put
  db->Put(write_options, "key5894", "value94793");

  // op 42425: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0191", "key9553", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0191, key9553) size=" << scan_result.size() << std::endl;
  }

  // op 42426: get
  status = db->Get(read_options, "key8577", &value);
  if (status.ok()) {
    out << "GET key8577 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42427: put
  db->Put(write_options, "key5298", "value69012");

  // op 42428: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1445", "key7341", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1445, key7341) size=" << scan_result.size() << std::endl;
  }

  // op 42429: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2863", "key7233", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2863, key7233) size=" << scan_result.size() << std::endl;
  }

  // op 42430: put
  db->Put(write_options, "key0146", "value17147");

  // op 42431: get
  status = db->Get(read_options, "key1710", &value);
  if (status.ok()) {
    out << "GET key1710 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42432: get
  status = db->Get(read_options, "key6055", &value);
  if (status.ok()) {
    out << "GET key6055 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42433: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9447", "key9664", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9447, key9664) size=" << scan_result.size() << std::endl;
  }

  // op 42434: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9611", "key9798", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9611, key9798) size=" << scan_result.size() << std::endl;
  }

  // op 42435: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 42436: put
  db->Put(write_options, "key9734", "value67238");

  // op 42437: get
  status = db->Get(read_options, "key6863", &value);
  if (status.ok()) {
    out << "GET key6863 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42438: get
  status = db->Get(read_options, "key10331", &value);
  if (status.ok()) {
    out << "GET key10331 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42439: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10473", "key10516", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10473, key10516) size=" << scan_result.size() << std::endl;
  }

  // op 42440: put
  db->Put(write_options, "key8259", "value112506");

  // op 42441: put
  db->Put(write_options, "key0120", "value23608");

  // op 42442: delete
  db->Delete(write_options, "key5822");

  // op 42443: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 42444: put
  db->Put(write_options, "key1451", "value75418");

  // op 42445: get
  status = db->Get(read_options, "key7681", &value);
  if (status.ok()) {
    out << "GET key7681 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42446: put
  db->Put(write_options, "key6381", "value85043");

  // op 42447: get
  status = db->Get(read_options, "key3818", &value);
  if (status.ok()) {
    out << "GET key3818 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42448: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1158", "key7338", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1158, key7338) size=" << scan_result.size() << std::endl;
  }

  // op 42449: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8794", "key10715", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8794, key10715) size=" << scan_result.size() << std::endl;
  }

  // op 42450: get
  status = db->Get(read_options, "key4124", &value);
  if (status.ok()) {
    out << "GET key4124 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42451: put
  db->Put(write_options, "key8347", "value102678");

  // op 42452: get
  status = db->Get(read_options, "key10816", &value);
  if (status.ok()) {
    out << "GET key10816 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42453: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 42454: get
  status = db->Get(read_options, "key0565", &value);
  if (status.ok()) {
    out << "GET key0565 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42455: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5960", "key6126", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5960, key6126) size=" << scan_result.size() << std::endl;
  }

  // op 42456: get
  status = db->Get(read_options, "key11535", &value);
  if (status.ok()) {
    out << "GET key11535 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42457: get
  status = db->Get(read_options, "key10027", &value);
  if (status.ok()) {
    out << "GET key10027 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42458: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6837", "key9564", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6837, key9564) size=" << scan_result.size() << std::endl;
  }

  // op 42459: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2601", "key4897", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2601, key4897) size=" << scan_result.size() << std::endl;
  }

  // op 42460: put
  db->Put(write_options, "key6380", "value96411");

  // op 42461: get
  status = db->Get(read_options, "key8706", &value);
  if (status.ok()) {
    out << "GET key8706 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42462: get
  status = db->Get(read_options, "key3658", &value);
  if (status.ok()) {
    out << "GET key3658 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42463: put
  db->Put(write_options, "key1957", "value111509");

  // op 42464: put
  db->Put(write_options, "key10621", "value30167");

  // op 42465: put
  db->Put(write_options, "key5357", "value47658");

  // op 42466: put
  db->Put(write_options, "key7188", "value53915");

  // op 42467: delete_range
  status = db->DeleteRange(write_options, "key2114", "key8712");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 42468: delete_range
  status = db->DeleteRange(write_options, "key1858", "key6882");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 42469: put
  db->Put(write_options, "key8468", "value3924");

  // op 42470: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3506", "key6789", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3506, key6789) size=" << scan_result.size() << std::endl;
  }

  // op 42471: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10570", "key11212", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10570, key11212) size=" << scan_result.size() << std::endl;
  }

  // op 42472: get
  status = db->Get(read_options, "key8223", &value);
  if (status.ok()) {
    out << "GET key8223 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42473: get
  status = db->Get(read_options, "key7000", &value);
  if (status.ok()) {
    out << "GET key7000 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42474: delete
  db->Delete(write_options, "key7766");

  // op 42475: put
  db->Put(write_options, "key5937", "value4901");

  // op 42476: put
  db->Put(write_options, "key10516", "value44136");

  // op 42477: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1701", "key3492", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1701, key3492) size=" << scan_result.size() << std::endl;
  }

  // op 42478: put
  db->Put(write_options, "key2176", "value21058");

  // op 42479: delete_range
  status = db->DeleteRange(write_options, "key3938", "key8167");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 42480: get
  status = db->Get(read_options, "key8310", &value);
  if (status.ok()) {
    out << "GET key8310 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42481: put
  db->Put(write_options, "key8591", "value42340");

  // op 42482: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 42483: get
  status = db->Get(read_options, "key2044", &value);
  if (status.ok()) {
    out << "GET key2044 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42484: get
  status = db->Get(read_options, "key8976", &value);
  if (status.ok()) {
    out << "GET key8976 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42485: get
  status = db->Get(read_options, "key5391", &value);
  if (status.ok()) {
    out << "GET key5391 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42486: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3704", "key9851", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3704, key9851) size=" << scan_result.size() << std::endl;
  }

  // op 42487: put
  db->Put(write_options, "key2778", "value108623");

  // op 42488: get
  status = db->Get(read_options, "key9420", &value);
  if (status.ok()) {
    out << "GET key9420 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42489: put
  db->Put(write_options, "key3450", "value72377");

  // op 42490: put
  db->Put(write_options, "key10513", "value20041");

  // op 42491: put
  db->Put(write_options, "key4044", "value32775");

  // op 42492: delete_range
  status = db->DeleteRange(write_options, "key5461", "key8107");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 42493: put
  db->Put(write_options, "key5454", "value106270");

  // op 42494: get
  status = db->Get(read_options, "key0305", &value);
  if (status.ok()) {
    out << "GET key0305 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42495: put
  db->Put(write_options, "key6575", "value98260");

  // op 42496: get
  status = db->Get(read_options, "key11624", &value);
  if (status.ok()) {
    out << "GET key11624 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42497: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9558", "key9795", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9558, key9795) size=" << scan_result.size() << std::endl;
  }

  // op 42498: get
  status = db->Get(read_options, "key1354", &value);
  if (status.ok()) {
    out << "GET key1354 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42499: get
  status = db->Get(read_options, "key0559", &value);
  if (status.ok()) {
    out << "GET key0559 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42500: delete_range
  status = db->DeleteRange(write_options, "key0090", "key8889");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 42501: delete
  db->Delete(write_options, "key6205");

  // op 42502: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2866", "key4875", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2866, key4875) size=" << scan_result.size() << std::endl;
  }

  // op 42503: put
  db->Put(write_options, "key10482", "value99903");

  // op 42504: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4474", "key9597", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4474, key9597) size=" << scan_result.size() << std::endl;
  }

  // op 42505: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2577", "key3563", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2577, key3563) size=" << scan_result.size() << std::endl;
  }

  // op 42506: put
  db->Put(write_options, "key0489", "value85838");

  // op 42507: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0584", "key11071", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0584, key11071) size=" << scan_result.size() << std::endl;
  }

  // op 42508: delete_range
  status = db->DeleteRange(write_options, "key5999", "key7085");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 42509: put
  db->Put(write_options, "key5408", "value51056");

  // op 42510: get
  status = db->Get(read_options, "key11725", &value);
  if (status.ok()) {
    out << "GET key11725 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42511: delete
  db->Delete(write_options, "key9657");

  // op 42512: put
  db->Put(write_options, "key3515", "value50500");

  // op 42513: get
  status = db->Get(read_options, "key11501", &value);
  if (status.ok()) {
    out << "GET key11501 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42514: get
  status = db->Get(read_options, "key4809", &value);
  if (status.ok()) {
    out << "GET key4809 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42515: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 42516: put
  db->Put(write_options, "key11609", "value68817");

  // op 42517: delete
  db->Delete(write_options, "key2980");

  // op 42518: put
  db->Put(write_options, "key10746", "value65601");

  // op 42519: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 42520: get
  status = db->Get(read_options, "key10325", &value);
  if (status.ok()) {
    out << "GET key10325 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42521: put
  db->Put(write_options, "key8804", "value102344");

  // op 42522: delete_range
  status = db->DeleteRange(write_options, "key8261", "key9004");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 42523: put
  db->Put(write_options, "key9339", "value113383");

  // op 42524: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4025", "key11033", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4025, key11033) size=" << scan_result.size() << std::endl;
  }

  // op 42525: put
  db->Put(write_options, "key1821", "value98210");

  // op 42526: get
  status = db->Get(read_options, "key3338", &value);
  if (status.ok()) {
    out << "GET key3338 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42527: delete_range
  status = db->DeleteRange(write_options, "key5275", "key9393");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 42528: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2542", "key3790", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2542, key3790) size=" << scan_result.size() << std::endl;
  }

  // op 42529: delete
  db->Delete(write_options, "key9932");

  // op 42530: get
  status = db->Get(read_options, "key0352", &value);
  if (status.ok()) {
    out << "GET key0352 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42531: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8208", "key11333", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8208, key11333) size=" << scan_result.size() << std::endl;
  }

  // op 42532: get
  status = db->Get(read_options, "key6655", &value);
  if (status.ok()) {
    out << "GET key6655 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42533: delete_range
  status = db->DeleteRange(write_options, "key9152", "key10102");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 42534: get
  status = db->Get(read_options, "key4006", &value);
  if (status.ok()) {
    out << "GET key4006 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42535: put
  db->Put(write_options, "key3729", "value39374");

  // op 42536: put
  db->Put(write_options, "key2013", "value98247");

  // op 42537: put
  db->Put(write_options, "key9351", "value113686");

  // op 42538: get
  status = db->Get(read_options, "key7637", &value);
  if (status.ok()) {
    out << "GET key7637 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42539: delete_range
  status = db->DeleteRange(write_options, "key0933", "key10600");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 42540: delete
  db->Delete(write_options, "key0657");

  // op 42541: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2739", "key10685", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2739, key10685) size=" << scan_result.size() << std::endl;
  }

  // op 42542: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5007", "key11505", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5007, key11505) size=" << scan_result.size() << std::endl;
  }

  // op 42543: put
  db->Put(write_options, "key9425", "value100674");

  // op 42544: delete
  db->Delete(write_options, "key3683");

  // op 42545: put
  db->Put(write_options, "key0197", "value105774");

  // op 42546: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6818", "key9458", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6818, key9458) size=" << scan_result.size() << std::endl;
  }

  // op 42547: get
  status = db->Get(read_options, "key9911", &value);
  if (status.ok()) {
    out << "GET key9911 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42548: put
  db->Put(write_options, "key9333", "value60400");

  // op 42549: put
  db->Put(write_options, "key0901", "value109576");

  // op 42550: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 42551: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 42552: delete_range
  status = db->DeleteRange(write_options, "key2579", "key11495");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 42553: put
  db->Put(write_options, "key4862", "value7482");

  // op 42554: put
  db->Put(write_options, "key4604", "value7748");

  // op 42555: get
  status = db->Get(read_options, "key5015", &value);
  if (status.ok()) {
    out << "GET key5015 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42556: put
  db->Put(write_options, "key0729", "value91032");

  // op 42557: delete
  db->Delete(write_options, "key4845");

  // op 42558: put
  db->Put(write_options, "key7036", "value51140");

  // op 42559: put
  db->Put(write_options, "key6381", "value14799");

  // op 42560: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0122", "key9218", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0122, key9218) size=" << scan_result.size() << std::endl;
  }

  // op 42561: put
  db->Put(write_options, "key4653", "value92461");

  // op 42562: get
  status = db->Get(read_options, "key7618", &value);
  if (status.ok()) {
    out << "GET key7618 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42563: delete_range
  status = db->DeleteRange(write_options, "key2360", "key6737");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 42564: delete
  db->Delete(write_options, "key3881");

  // op 42565: put
  db->Put(write_options, "key2160", "value56605");

  // op 42566: delete_range
  status = db->DeleteRange(write_options, "key3436", "key6557");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 42567: delete_range
  status = db->DeleteRange(write_options, "key7206", "key8740");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 42568: delete
  db->Delete(write_options, "key2143");

  // op 42569: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5346", "key5409", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5346, key5409) size=" << scan_result.size() << std::endl;
  }

  // op 42570: put
  db->Put(write_options, "key4846", "value60147");

  // op 42571: get
  status = db->Get(read_options, "key8496", &value);
  if (status.ok()) {
    out << "GET key8496 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42572: delete
  db->Delete(write_options, "key4308");

  // op 42573: put
  db->Put(write_options, "key7987", "value117379");

  // op 42574: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 42575: put
  db->Put(write_options, "key1286", "value12524");

  // op 42576: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2917", "key8618", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2917, key8618) size=" << scan_result.size() << std::endl;
  }

  // op 42577: delete
  db->Delete(write_options, "key2483");

  // op 42578: put
  db->Put(write_options, "key0755", "value99388");

  // op 42579: delete_range
  status = db->DeleteRange(write_options, "key1024", "key8908");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 42580: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 42581: delete_range
  status = db->DeleteRange(write_options, "key5590", "key7751");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 42582: get
  status = db->Get(read_options, "key10708", &value);
  if (status.ok()) {
    out << "GET key10708 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42583: get
  status = db->Get(read_options, "key6914", &value);
  if (status.ok()) {
    out << "GET key6914 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42584: get
  status = db->Get(read_options, "key2055", &value);
  if (status.ok()) {
    out << "GET key2055 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42585: put
  db->Put(write_options, "key2034", "value31708");

  // op 42586: get
  status = db->Get(read_options, "key8084", &value);
  if (status.ok()) {
    out << "GET key8084 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42587: delete_range
  status = db->DeleteRange(write_options, "key3938", "key4459");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 42588: put
  db->Put(write_options, "key4028", "value80185");

  // op 42589: get
  status = db->Get(read_options, "key9046", &value);
  if (status.ok()) {
    out << "GET key9046 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42590: get
  status = db->Get(read_options, "key7707", &value);
  if (status.ok()) {
    out << "GET key7707 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42591: delete
  db->Delete(write_options, "key6847");

  // op 42592: put
  db->Put(write_options, "key2187", "value4994");

  // op 42593: put
  db->Put(write_options, "key1369", "value75220");

  // op 42594: put
  db->Put(write_options, "key9713", "value77265");

  // op 42595: put
  db->Put(write_options, "key9515", "value67111");

  // op 42596: put
  db->Put(write_options, "key11381", "value18277");

  // op 42597: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6357", "key9618", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6357, key9618) size=" << scan_result.size() << std::endl;
  }

  // op 42598: delete_range
  status = db->DeleteRange(write_options, "key5518", "key9375");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 42599: delete
  db->Delete(write_options, "key5133");

  // op 42600: delete
  db->Delete(write_options, "key3591");

  // op 42601: put
  db->Put(write_options, "key0132", "value42151");

  // op 42602: get
  status = db->Get(read_options, "key4454", &value);
  if (status.ok()) {
    out << "GET key4454 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42603: put
  db->Put(write_options, "key9863", "value115058");

  // op 42604: delete_range
  status = db->DeleteRange(write_options, "key3213", "key7717");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 42605: put
  db->Put(write_options, "key9404", "value40273");

  // op 42606: put
  db->Put(write_options, "key7170", "value53566");

  // op 42607: put
  db->Put(write_options, "key9849", "value90998");

  // op 42608: get
  status = db->Get(read_options, "key0838", &value);
  if (status.ok()) {
    out << "GET key0838 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42609: delete
  db->Delete(write_options, "key7025");

  // op 42610: get
  status = db->Get(read_options, "key5103", &value);
  if (status.ok()) {
    out << "GET key5103 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42611: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 42612: delete
  db->Delete(write_options, "key10208");

  // op 42613: get
  status = db->Get(read_options, "key8981", &value);
  if (status.ok()) {
    out << "GET key8981 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42614: delete
  db->Delete(write_options, "key9200");

  // op 42615: put
  db->Put(write_options, "key0246", "value9925");

  // op 42616: delete_range
  status = db->DeleteRange(write_options, "key9269", "key11704");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 42617: put
  db->Put(write_options, "key9297", "value106828");

  // op 42618: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5258", "key8021", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5258, key8021) size=" << scan_result.size() << std::endl;
  }

  // op 42619: put
  db->Put(write_options, "key2463", "value6139");

  // op 42620: delete
  db->Delete(write_options, "key0015");

  // op 42621: put
  db->Put(write_options, "key2951", "value102958");

  // op 42622: put
  db->Put(write_options, "key5145", "value10938");

  // op 42623: put
  db->Put(write_options, "key4820", "value61304");

  // op 42624: put
  db->Put(write_options, "key6587", "value102927");

  // op 42625: put
  db->Put(write_options, "key5329", "value37416");

  // op 42626: put
  db->Put(write_options, "key7784", "value18346");

  // op 42627: put
  db->Put(write_options, "key10906", "value80097");

  // op 42628: put
  db->Put(write_options, "key10937", "value95339");

  // op 42629: put
  db->Put(write_options, "key1315", "value92423");

  // op 42630: delete_range
  status = db->DeleteRange(write_options, "key2854", "key5747");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 42631: delete
  db->Delete(write_options, "key7297");

  // op 42632: delete_range
  status = db->DeleteRange(write_options, "key2813", "key11106");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 42633: get
  status = db->Get(read_options, "key4708", &value);
  if (status.ok()) {
    out << "GET key4708 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42634: get
  status = db->Get(read_options, "key0627", &value);
  if (status.ok()) {
    out << "GET key0627 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42635: put
  db->Put(write_options, "key9495", "value17557");

  // op 42636: get
  status = db->Get(read_options, "key8575", &value);
  if (status.ok()) {
    out << "GET key8575 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42637: delete_range
  status = db->DeleteRange(write_options, "key4770", "key6667");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 42638: put
  db->Put(write_options, "key7312", "value108275");

  // op 42639: put
  db->Put(write_options, "key0138", "value65155");

  // op 42640: put
  db->Put(write_options, "key11687", "value96440");

  // op 42641: delete_range
  status = db->DeleteRange(write_options, "key5074", "key7871");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 42642: put
  db->Put(write_options, "key2354", "value61543");

  // op 42643: put
  db->Put(write_options, "key7657", "value101908");

  // op 42644: delete
  db->Delete(write_options, "key8971");

  // op 42645: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5488", "key11636", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5488, key11636) size=" << scan_result.size() << std::endl;
  }

  // op 42646: delete_range
  status = db->DeleteRange(write_options, "key3174", "key7832");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 42647: get
  status = db->Get(read_options, "key11702", &value);
  if (status.ok()) {
    out << "GET key11702 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42648: put
  db->Put(write_options, "key11571", "value82806");

  // op 42649: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6485", "key7907", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6485, key7907) size=" << scan_result.size() << std::endl;
  }

  // op 42650: put
  db->Put(write_options, "key2064", "value55936");

  // op 42651: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 42652: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 42653: put
  db->Put(write_options, "key1085", "value3053");

  // op 42654: put
  db->Put(write_options, "key6547", "value34115");

  // op 42655: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1599", "key5200", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1599, key5200) size=" << scan_result.size() << std::endl;
  }

  // op 42656: put
  db->Put(write_options, "key1852", "value82315");

  // op 42657: get
  status = db->Get(read_options, "key3143", &value);
  if (status.ok()) {
    out << "GET key3143 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42658: get
  status = db->Get(read_options, "key1501", &value);
  if (status.ok()) {
    out << "GET key1501 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42659: put
  db->Put(write_options, "key5363", "value116420");

  // op 42660: get
  status = db->Get(read_options, "key10860", &value);
  if (status.ok()) {
    out << "GET key10860 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42661: delete_range
  status = db->DeleteRange(write_options, "key2945", "key7164");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 42662: put
  db->Put(write_options, "key5504", "value65507");

  // op 42663: delete_range
  status = db->DeleteRange(write_options, "key4616", "key10325");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 42664: put
  db->Put(write_options, "key5186", "value80934");

  // op 42665: delete
  db->Delete(write_options, "key8938");

  // op 42666: get
  status = db->Get(read_options, "key0573", &value);
  if (status.ok()) {
    out << "GET key0573 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42667: put
  db->Put(write_options, "key6434", "value81557");

  // op 42668: put
  db->Put(write_options, "key3700", "value73296");

  // op 42669: put
  db->Put(write_options, "key10537", "value78338");

  // op 42670: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2766", "key7203", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2766, key7203) size=" << scan_result.size() << std::endl;
  }

  // op 42671: delete
  db->Delete(write_options, "key7768");

  // op 42672: get
  status = db->Get(read_options, "key4901", &value);
  if (status.ok()) {
    out << "GET key4901 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42673: put
  db->Put(write_options, "key11204", "value114997");

  // op 42674: put
  db->Put(write_options, "key0486", "value27725");

  // op 42675: put
  db->Put(write_options, "key7260", "value29531");

  // op 42676: delete
  db->Delete(write_options, "key11689");

  // op 42677: put
  db->Put(write_options, "key11100", "value61168");

  // op 42678: delete
  db->Delete(write_options, "key11390");

  // op 42679: delete
  db->Delete(write_options, "key1509");

  // op 42680: put
  db->Put(write_options, "key0920", "value16898");

  // op 42681: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1428", "key4836", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1428, key4836) size=" << scan_result.size() << std::endl;
  }

  // op 42682: put
  db->Put(write_options, "key9989", "value25022");

  // op 42683: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2855", "key10351", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2855, key10351) size=" << scan_result.size() << std::endl;
  }

  // op 42684: delete_range
  status = db->DeleteRange(write_options, "key6309", "key6581");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 42685: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9141", "key11508", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9141, key11508) size=" << scan_result.size() << std::endl;
  }

  // op 42686: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1185", "key1582", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1185, key1582) size=" << scan_result.size() << std::endl;
  }

  // op 42687: put
  db->Put(write_options, "key3242", "value87358");

  // op 42688: delete
  db->Delete(write_options, "key2019");

  // op 42689: get
  status = db->Get(read_options, "key2811", &value);
  if (status.ok()) {
    out << "GET key2811 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42690: put
  db->Put(write_options, "key3009", "value84264");

  // op 42691: put
  db->Put(write_options, "key5348", "value68411");

  // op 42692: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 42693: get
  status = db->Get(read_options, "key9760", &value);
  if (status.ok()) {
    out << "GET key9760 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42694: delete
  db->Delete(write_options, "key4422");

  // op 42695: put
  db->Put(write_options, "key5497", "value33477");

  // op 42696: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0259", "key2816", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0259, key2816) size=" << scan_result.size() << std::endl;
  }

  // op 42697: put
  db->Put(write_options, "key1870", "value63656");

  // op 42698: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 42699: delete_range
  status = db->DeleteRange(write_options, "key0938", "key4745");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 42700: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 42701: put
  db->Put(write_options, "key0028", "value93484");

  // op 42702: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2845", "key3696", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2845, key3696) size=" << scan_result.size() << std::endl;
  }

  // op 42703: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7428", "key8472", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7428, key8472) size=" << scan_result.size() << std::endl;
  }

  // op 42704: put
  db->Put(write_options, "key10230", "value46093");

  // op 42705: delete_range
  status = db->DeleteRange(write_options, "key5408", "key9252");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 42706: put
  db->Put(write_options, "key10154", "value12317");

  // op 42707: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8182", "key10370", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8182, key10370) size=" << scan_result.size() << std::endl;
  }

  // op 42708: delete_range
  status = db->DeleteRange(write_options, "key1990", "key8965");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 42709: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4184", "key7893", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4184, key7893) size=" << scan_result.size() << std::endl;
  }

  // op 42710: put
  db->Put(write_options, "key6891", "value69815");

  // op 42711: put
  db->Put(write_options, "key11193", "value28831");

  // op 42712: get
  status = db->Get(read_options, "key10179", &value);
  if (status.ok()) {
    out << "GET key10179 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42713: put
  db->Put(write_options, "key9227", "value108142");

  // op 42714: get
  status = db->Get(read_options, "key3064", &value);
  if (status.ok()) {
    out << "GET key3064 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42715: delete
  db->Delete(write_options, "key3735");

  // op 42716: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0884", "key8826", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0884, key8826) size=" << scan_result.size() << std::endl;
  }

  // op 42717: put
  db->Put(write_options, "key9770", "value14440");

  // op 42718: put
  db->Put(write_options, "key4425", "value46313");

  // op 42719: put
  db->Put(write_options, "key5118", "value58087");

  // op 42720: put
  db->Put(write_options, "key2634", "value70652");

  // op 42721: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1850", "key8184", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1850, key8184) size=" << scan_result.size() << std::endl;
  }

  // op 42722: delete
  db->Delete(write_options, "key8617");

  // op 42723: get
  status = db->Get(read_options, "key6427", &value);
  if (status.ok()) {
    out << "GET key6427 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42724: get
  status = db->Get(read_options, "key1476", &value);
  if (status.ok()) {
    out << "GET key1476 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42725: put
  db->Put(write_options, "key2980", "value20167");

  // op 42726: get
  status = db->Get(read_options, "key10838", &value);
  if (status.ok()) {
    out << "GET key10838 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42727: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2310", "key3295", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2310, key3295) size=" << scan_result.size() << std::endl;
  }

  // op 42728: get
  status = db->Get(read_options, "key9789", &value);
  if (status.ok()) {
    out << "GET key9789 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42729: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0524", "key3079", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0524, key3079) size=" << scan_result.size() << std::endl;
  }

  // op 42730: put
  db->Put(write_options, "key0765", "value105160");

  // op 42731: get
  status = db->Get(read_options, "key9766", &value);
  if (status.ok()) {
    out << "GET key9766 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42732: delete
  db->Delete(write_options, "key10849");

  // op 42733: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4746", "key9172", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4746, key9172) size=" << scan_result.size() << std::endl;
  }

  // op 42734: delete_range
  status = db->DeleteRange(write_options, "key0038", "key0985");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 42735: delete
  db->Delete(write_options, "key7763");

  // op 42736: get
  status = db->Get(read_options, "key3992", &value);
  if (status.ok()) {
    out << "GET key3992 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42737: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2283", "key8287", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2283, key8287) size=" << scan_result.size() << std::endl;
  }

  // op 42738: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8028", "key9462", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8028, key9462) size=" << scan_result.size() << std::endl;
  }

  // op 42739: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 42740: get
  status = db->Get(read_options, "key6647", &value);
  if (status.ok()) {
    out << "GET key6647 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42741: put
  db->Put(write_options, "key8555", "value23724");

  // op 42742: get
  status = db->Get(read_options, "key5622", &value);
  if (status.ok()) {
    out << "GET key5622 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42743: put
  db->Put(write_options, "key2089", "value26344");

  // op 42744: get
  status = db->Get(read_options, "key1874", &value);
  if (status.ok()) {
    out << "GET key1874 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42745: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1543", "key3594", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1543, key3594) size=" << scan_result.size() << std::endl;
  }

  // op 42746: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1274", "key11480", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1274, key11480) size=" << scan_result.size() << std::endl;
  }

  // op 42747: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10929", "key11046", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10929, key11046) size=" << scan_result.size() << std::endl;
  }

  // op 42748: put
  db->Put(write_options, "key7812", "value90179");

  // op 42749: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4459", "key6191", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4459, key6191) size=" << scan_result.size() << std::endl;
  }

  // op 42750: get
  status = db->Get(read_options, "key6813", &value);
  if (status.ok()) {
    out << "GET key6813 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42751: get
  status = db->Get(read_options, "key10544", &value);
  if (status.ok()) {
    out << "GET key10544 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42752: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 42753: get
  status = db->Get(read_options, "key8621", &value);
  if (status.ok()) {
    out << "GET key8621 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42754: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3024", "key6680", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3024, key6680) size=" << scan_result.size() << std::endl;
  }

  // op 42755: put
  db->Put(write_options, "key9752", "value117887");

  // op 42756: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 42757: put
  db->Put(write_options, "key2024", "value18627");

  // op 42758: delete_range
  status = db->DeleteRange(write_options, "key0143", "key0623");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 42759: put
  db->Put(write_options, "key6917", "value102897");

  // op 42760: delete_range
  status = db->DeleteRange(write_options, "key4327", "key6824");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 42761: get
  status = db->Get(read_options, "key0173", &value);
  if (status.ok()) {
    out << "GET key0173 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42762: put
  db->Put(write_options, "key1537", "value103161");

  // op 42763: delete_range
  status = db->DeleteRange(write_options, "key1143", "key11497");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 42764: get
  status = db->Get(read_options, "key7261", &value);
  if (status.ok()) {
    out << "GET key7261 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42765: put
  db->Put(write_options, "key1729", "value81090");

  // op 42766: delete_range
  status = db->DeleteRange(write_options, "key10427", "key10923");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 42767: put
  db->Put(write_options, "key8222", "value69721");

  // op 42768: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4376", "key7566", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4376, key7566) size=" << scan_result.size() << std::endl;
  }

  // op 42769: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3044", "key11445", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3044, key11445) size=" << scan_result.size() << std::endl;
  }

  // op 42770: get
  status = db->Get(read_options, "key2812", &value);
  if (status.ok()) {
    out << "GET key2812 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42771: put
  db->Put(write_options, "key5352", "value100540");

  // op 42772: put
  db->Put(write_options, "key1221", "value46646");

  // op 42773: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6884", "key10349", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6884, key10349) size=" << scan_result.size() << std::endl;
  }

  // op 42774: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4961", "key6615", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4961, key6615) size=" << scan_result.size() << std::endl;
  }

  // op 42775: delete
  db->Delete(write_options, "key10222");

  // op 42776: put
  db->Put(write_options, "key4665", "value113349");

  // op 42777: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7766", "key10224", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7766, key10224) size=" << scan_result.size() << std::endl;
  }

  // op 42778: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4914", "key8814", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4914, key8814) size=" << scan_result.size() << std::endl;
  }

  // op 42779: put
  db->Put(write_options, "key1722", "value71958");

  // op 42780: get
  status = db->Get(read_options, "key10640", &value);
  if (status.ok()) {
    out << "GET key10640 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42781: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5021", "key8761", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5021, key8761) size=" << scan_result.size() << std::endl;
  }

  // op 42782: put
  db->Put(write_options, "key5559", "value38356");

  // op 42783: delete
  db->Delete(write_options, "key8112");

  // op 42784: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6403", "key6447", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6403, key6447) size=" << scan_result.size() << std::endl;
  }

  // op 42785: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 42786: delete_range
  status = db->DeleteRange(write_options, "key7758", "key10173");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 42787: put
  db->Put(write_options, "key6743", "value0493");

  // op 42788: put
  db->Put(write_options, "key11104", "value58419");

  // op 42789: put
  db->Put(write_options, "key1182", "value13514");

  // op 42790: delete
  db->Delete(write_options, "key5098");

  // op 42791: delete_range
  status = db->DeleteRange(write_options, "key2046", "key6405");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 42792: put
  db->Put(write_options, "key10379", "value27194");

  // op 42793: get
  status = db->Get(read_options, "key3773", &value);
  if (status.ok()) {
    out << "GET key3773 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42794: get
  status = db->Get(read_options, "key5644", &value);
  if (status.ok()) {
    out << "GET key5644 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42795: delete
  db->Delete(write_options, "key8982");

  // op 42796: put
  db->Put(write_options, "key4270", "value85866");

  // op 42797: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0473", "key9258", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0473, key9258) size=" << scan_result.size() << std::endl;
  }

  // op 42798: put
  db->Put(write_options, "key11404", "value34049");

  // op 42799: get
  status = db->Get(read_options, "key6848", &value);
  if (status.ok()) {
    out << "GET key6848 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42800: put
  db->Put(write_options, "key1752", "value93651");

  // op 42801: put
  db->Put(write_options, "key7242", "value19594");

  // op 42802: get
  status = db->Get(read_options, "key0864", &value);
  if (status.ok()) {
    out << "GET key0864 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42803: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1949", "key9363", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1949, key9363) size=" << scan_result.size() << std::endl;
  }

  // op 42804: delete_range
  status = db->DeleteRange(write_options, "key3989", "key10305");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 42805: delete_range
  status = db->DeleteRange(write_options, "key10236", "key11506");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 42806: put
  db->Put(write_options, "key1022", "value64614");

  // op 42807: put
  db->Put(write_options, "key0452", "value16582");

  // op 42808: get
  status = db->Get(read_options, "key2942", &value);
  if (status.ok()) {
    out << "GET key2942 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42809: delete_range
  status = db->DeleteRange(write_options, "key1238", "key7030");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 42810: put
  db->Put(write_options, "key3792", "value114706");

  // op 42811: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3812", "key9281", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3812, key9281) size=" << scan_result.size() << std::endl;
  }

  // op 42812: put
  db->Put(write_options, "key0939", "value24529");

  // op 42813: put
  db->Put(write_options, "key0576", "value69502");

  // op 42814: get
  status = db->Get(read_options, "key7558", &value);
  if (status.ok()) {
    out << "GET key7558 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42815: get
  status = db->Get(read_options, "key4620", &value);
  if (status.ok()) {
    out << "GET key4620 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42816: delete_range
  status = db->DeleteRange(write_options, "key4088", "key5433");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 42817: get
  status = db->Get(read_options, "key7642", &value);
  if (status.ok()) {
    out << "GET key7642 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42818: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0971", "key9512", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0971, key9512) size=" << scan_result.size() << std::endl;
  }

  // op 42819: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6946", "key9341", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6946, key9341) size=" << scan_result.size() << std::endl;
  }

  // op 42820: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1060", "key11678", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1060, key11678) size=" << scan_result.size() << std::endl;
  }

  // op 42821: delete_range
  status = db->DeleteRange(write_options, "key1811", "key6086");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 42822: put
  db->Put(write_options, "key4363", "value114756");

  // op 42823: put
  db->Put(write_options, "key9559", "value45207");

  // op 42824: delete
  db->Delete(write_options, "key8171");

  // op 42825: get
  status = db->Get(read_options, "key6652", &value);
  if (status.ok()) {
    out << "GET key6652 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42826: delete
  db->Delete(write_options, "key10386");

  // op 42827: get
  status = db->Get(read_options, "key8478", &value);
  if (status.ok()) {
    out << "GET key8478 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42828: put
  db->Put(write_options, "key11653", "value78446");

  // op 42829: get
  status = db->Get(read_options, "key11141", &value);
  if (status.ok()) {
    out << "GET key11141 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42830: put
  db->Put(write_options, "key11161", "value11824");

  // op 42831: get
  status = db->Get(read_options, "key11653", &value);
  if (status.ok()) {
    out << "GET key11653 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42832: put
  db->Put(write_options, "key4012", "value29006");

  // op 42833: get
  status = db->Get(read_options, "key7360", &value);
  if (status.ok()) {
    out << "GET key7360 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42834: delete_range
  status = db->DeleteRange(write_options, "key2511", "key4983");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 42835: put
  db->Put(write_options, "key6687", "value63687");

  // op 42836: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3581", "key7375", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3581, key7375) size=" << scan_result.size() << std::endl;
  }

  // op 42837: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2091", "key11130", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2091, key11130) size=" << scan_result.size() << std::endl;
  }

  // op 42838: get
  status = db->Get(read_options, "key9386", &value);
  if (status.ok()) {
    out << "GET key9386 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42839: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0289", "key6483", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0289, key6483) size=" << scan_result.size() << std::endl;
  }

  // op 42840: get
  status = db->Get(read_options, "key10050", &value);
  if (status.ok()) {
    out << "GET key10050 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42841: put
  db->Put(write_options, "key9626", "value88379");

  // op 42842: get
  status = db->Get(read_options, "key5270", &value);
  if (status.ok()) {
    out << "GET key5270 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42843: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0202", "key4809", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0202, key4809) size=" << scan_result.size() << std::endl;
  }

  // op 42844: put
  db->Put(write_options, "key1448", "value68885");

  // op 42845: delete
  db->Delete(write_options, "key7324");

  // op 42846: get
  status = db->Get(read_options, "key10274", &value);
  if (status.ok()) {
    out << "GET key10274 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42847: put
  db->Put(write_options, "key0078", "value115898");

  // op 42848: delete_range
  status = db->DeleteRange(write_options, "key8344", "key9104");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 42849: put
  db->Put(write_options, "key5245", "value42131");

  // op 42850: delete_range
  status = db->DeleteRange(write_options, "key7901", "key10424");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 42851: put
  db->Put(write_options, "key6824", "value53585");

  // op 42852: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2063", "key2097", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2063, key2097) size=" << scan_result.size() << std::endl;
  }

  // op 42853: put
  db->Put(write_options, "key3883", "value89586");

  // op 42854: get
  status = db->Get(read_options, "key2051", &value);
  if (status.ok()) {
    out << "GET key2051 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42855: delete
  db->Delete(write_options, "key3086");

  // op 42856: get
  status = db->Get(read_options, "key8314", &value);
  if (status.ok()) {
    out << "GET key8314 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42857: delete_range
  status = db->DeleteRange(write_options, "key0050", "key11368");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 42858: get
  status = db->Get(read_options, "key5709", &value);
  if (status.ok()) {
    out << "GET key5709 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42859: get
  status = db->Get(read_options, "key1005", &value);
  if (status.ok()) {
    out << "GET key1005 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42860: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2294", "key5735", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2294, key5735) size=" << scan_result.size() << std::endl;
  }

  // op 42861: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2137", "key9382", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2137, key9382) size=" << scan_result.size() << std::endl;
  }

  // op 42862: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1768", "key6890", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1768, key6890) size=" << scan_result.size() << std::endl;
  }

  // op 42863: get
  status = db->Get(read_options, "key0603", &value);
  if (status.ok()) {
    out << "GET key0603 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42864: put
  db->Put(write_options, "key1345", "value52320");

  // op 42865: get
  status = db->Get(read_options, "key10427", &value);
  if (status.ok()) {
    out << "GET key10427 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42866: put
  db->Put(write_options, "key7449", "value51968");

  // op 42867: put
  db->Put(write_options, "key6183", "value19159");

  // op 42868: put
  db->Put(write_options, "key5091", "value53059");

  // op 42869: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8603", "key11390", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8603, key11390) size=" << scan_result.size() << std::endl;
  }

  // op 42870: put
  db->Put(write_options, "key2103", "value17949");

  // op 42871: delete
  db->Delete(write_options, "key4966");

  // op 42872: delete_range
  status = db->DeleteRange(write_options, "key0141", "key4944");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 42873: get
  status = db->Get(read_options, "key0454", &value);
  if (status.ok()) {
    out << "GET key0454 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42874: put
  db->Put(write_options, "key5803", "value99002");

  // op 42875: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 42876: get
  status = db->Get(read_options, "key2458", &value);
  if (status.ok()) {
    out << "GET key2458 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42877: get
  status = db->Get(read_options, "key2588", &value);
  if (status.ok()) {
    out << "GET key2588 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42878: get
  status = db->Get(read_options, "key0465", &value);
  if (status.ok()) {
    out << "GET key0465 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42879: put
  db->Put(write_options, "key5221", "value53938");

  // op 42880: delete
  db->Delete(write_options, "key9566");

  // op 42881: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8181", "key9486", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8181, key9486) size=" << scan_result.size() << std::endl;
  }

  // op 42882: put
  db->Put(write_options, "key2014", "value5883");

  // op 42883: get
  status = db->Get(read_options, "key4947", &value);
  if (status.ok()) {
    out << "GET key4947 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42884: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6277", "key6737", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6277, key6737) size=" << scan_result.size() << std::endl;
  }

  // op 42885: delete
  db->Delete(write_options, "key7604");

  // op 42886: delete_range
  status = db->DeleteRange(write_options, "key4338", "key6892");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 42887: put
  db->Put(write_options, "key6352", "value100881");

  // op 42888: delete
  db->Delete(write_options, "key8250");

  // op 42889: put
  db->Put(write_options, "key8816", "value54840");

  // op 42890: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6943", "key10423", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6943, key10423) size=" << scan_result.size() << std::endl;
  }

  // op 42891: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 42892: get
  status = db->Get(read_options, "key2772", &value);
  if (status.ok()) {
    out << "GET key2772 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42893: get
  status = db->Get(read_options, "key3564", &value);
  if (status.ok()) {
    out << "GET key3564 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42894: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6515", "key7946", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6515, key7946) size=" << scan_result.size() << std::endl;
  }

  // op 42895: get
  status = db->Get(read_options, "key3999", &value);
  if (status.ok()) {
    out << "GET key3999 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42896: put
  db->Put(write_options, "key3595", "value53786");

  // op 42897: delete
  db->Delete(write_options, "key0781");

  // op 42898: put
  db->Put(write_options, "key11596", "value44751");

  // op 42899: put
  db->Put(write_options, "key2938", "value49735");

  // op 42900: put
  db->Put(write_options, "key8092", "value27671");

  // op 42901: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6857", "key11684", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6857, key11684) size=" << scan_result.size() << std::endl;
  }

  // op 42902: put
  db->Put(write_options, "key0280", "value31767");

  // op 42903: delete
  db->Delete(write_options, "key3465");

  // op 42904: put
  db->Put(write_options, "key7436", "value51076");

  // op 42905: put
  db->Put(write_options, "key2633", "value72709");

  // op 42906: delete_range
  status = db->DeleteRange(write_options, "key4466", "key10965");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 42907: put
  db->Put(write_options, "key2430", "value36597");

  // op 42908: put
  db->Put(write_options, "key8535", "value57213");

  // op 42909: put
  db->Put(write_options, "key2127", "value97564");

  // op 42910: put
  db->Put(write_options, "key8336", "value21085");

  // op 42911: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1849", "key4913", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1849, key4913) size=" << scan_result.size() << std::endl;
  }

  // op 42912: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5528", "key11210", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5528, key11210) size=" << scan_result.size() << std::endl;
  }

  // op 42913: put
  db->Put(write_options, "key3501", "value11763");

  // op 42914: delete_range
  status = db->DeleteRange(write_options, "key7325", "key7858");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 42915: get
  status = db->Get(read_options, "key6694", &value);
  if (status.ok()) {
    out << "GET key6694 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42916: get
  status = db->Get(read_options, "key5047", &value);
  if (status.ok()) {
    out << "GET key5047 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42917: get
  status = db->Get(read_options, "key10697", &value);
  if (status.ok()) {
    out << "GET key10697 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42918: put
  db->Put(write_options, "key10184", "value92160");

  // op 42919: delete_range
  status = db->DeleteRange(write_options, "key7531", "key10895");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 42920: get
  status = db->Get(read_options, "key10170", &value);
  if (status.ok()) {
    out << "GET key10170 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42921: get
  status = db->Get(read_options, "key2181", &value);
  if (status.ok()) {
    out << "GET key2181 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42922: put
  db->Put(write_options, "key8822", "value47459");

  // op 42923: get
  status = db->Get(read_options, "key5195", &value);
  if (status.ok()) {
    out << "GET key5195 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42924: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2606", "key7473", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2606, key7473) size=" << scan_result.size() << std::endl;
  }

  // op 42925: put
  db->Put(write_options, "key9682", "value38021");

  // op 42926: get
  status = db->Get(read_options, "key7481", &value);
  if (status.ok()) {
    out << "GET key7481 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42927: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 42928: get
  status = db->Get(read_options, "key0178", &value);
  if (status.ok()) {
    out << "GET key0178 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42929: put
  db->Put(write_options, "key1569", "value15891");

  // op 42930: put
  db->Put(write_options, "key6800", "value7876");

  // op 42931: put
  db->Put(write_options, "key2609", "value45707");

  // op 42932: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 42933: get
  status = db->Get(read_options, "key7723", &value);
  if (status.ok()) {
    out << "GET key7723 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42934: put
  db->Put(write_options, "key7883", "value20672");

  // op 42935: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3918", "key8461", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3918, key8461) size=" << scan_result.size() << std::endl;
  }

  // op 42936: put
  db->Put(write_options, "key3904", "value44447");

  // op 42937: delete_range
  status = db->DeleteRange(write_options, "key2891", "key10837");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 42938: get
  status = db->Get(read_options, "key11289", &value);
  if (status.ok()) {
    out << "GET key11289 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42939: get
  status = db->Get(read_options, "key6191", &value);
  if (status.ok()) {
    out << "GET key6191 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42940: get
  status = db->Get(read_options, "key3537", &value);
  if (status.ok()) {
    out << "GET key3537 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42941: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9278", "key9440", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9278, key9440) size=" << scan_result.size() << std::endl;
  }

  // op 42942: get
  status = db->Get(read_options, "key0166", &value);
  if (status.ok()) {
    out << "GET key0166 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42943: get
  status = db->Get(read_options, "key1183", &value);
  if (status.ok()) {
    out << "GET key1183 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42944: get
  status = db->Get(read_options, "key2698", &value);
  if (status.ok()) {
    out << "GET key2698 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42945: put
  db->Put(write_options, "key1062", "value87092");

  // op 42946: put
  db->Put(write_options, "key11429", "value90902");

  // op 42947: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 42948: put
  db->Put(write_options, "key0754", "value59020");

  // op 42949: get
  status = db->Get(read_options, "key8383", &value);
  if (status.ok()) {
    out << "GET key8383 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42950: get
  status = db->Get(read_options, "key5199", &value);
  if (status.ok()) {
    out << "GET key5199 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42951: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8404", "key8746", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8404, key8746) size=" << scan_result.size() << std::endl;
  }

  // op 42952: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 42953: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3853", "key9865", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3853, key9865) size=" << scan_result.size() << std::endl;
  }

  // op 42954: put
  db->Put(write_options, "key4034", "value36949");

  // op 42955: delete_range
  status = db->DeleteRange(write_options, "key1655", "key5925");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 42956: get
  status = db->Get(read_options, "key9666", &value);
  if (status.ok()) {
    out << "GET key9666 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42957: put
  db->Put(write_options, "key3830", "value55776");

  // op 42958: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 42959: put
  db->Put(write_options, "key3588", "value31746");

  // op 42960: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9481", "key10907", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9481, key10907) size=" << scan_result.size() << std::endl;
  }

  // op 42961: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5230", "key11659", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5230, key11659) size=" << scan_result.size() << std::endl;
  }

  // op 42962: delete_range
  status = db->DeleteRange(write_options, "key8313", "key10090");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 42963: get
  status = db->Get(read_options, "key1489", &value);
  if (status.ok()) {
    out << "GET key1489 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42964: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7867", "key11043", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7867, key11043) size=" << scan_result.size() << std::endl;
  }

  // op 42965: put
  db->Put(write_options, "key3823", "value36014");

  // op 42966: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5084", "key7298", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5084, key7298) size=" << scan_result.size() << std::endl;
  }

  // op 42967: put
  db->Put(write_options, "key1768", "value87780");

  // op 42968: put
  db->Put(write_options, "key6822", "value98805");

  // op 42969: get
  status = db->Get(read_options, "key3691", &value);
  if (status.ok()) {
    out << "GET key3691 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42970: get
  status = db->Get(read_options, "key0385", &value);
  if (status.ok()) {
    out << "GET key0385 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42971: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1709", "key10125", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1709, key10125) size=" << scan_result.size() << std::endl;
  }

  // op 42972: delete_range
  status = db->DeleteRange(write_options, "key3333", "key11602");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 42973: get
  status = db->Get(read_options, "key4828", &value);
  if (status.ok()) {
    out << "GET key4828 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42974: delete
  db->Delete(write_options, "key8242");

  // op 42975: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 42976: put
  db->Put(write_options, "key6606", "value82247");

  // op 42977: delete
  db->Delete(write_options, "key1004");

  // op 42978: delete
  db->Delete(write_options, "key11543");

  // op 42979: get
  status = db->Get(read_options, "key5537", &value);
  if (status.ok()) {
    out << "GET key5537 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42980: delete
  db->Delete(write_options, "key1681");

  // op 42981: put
  db->Put(write_options, "key8190", "value113667");

  // op 42982: get
  status = db->Get(read_options, "key10943", &value);
  if (status.ok()) {
    out << "GET key10943 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42983: delete_range
  status = db->DeleteRange(write_options, "key9293", "key9492");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 42984: put
  db->Put(write_options, "key1241", "value66740");

  // op 42985: get
  status = db->Get(read_options, "key5160", &value);
  if (status.ok()) {
    out << "GET key5160 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42986: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10739", "key10909", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10739, key10909) size=" << scan_result.size() << std::endl;
  }

  // op 42987: delete
  db->Delete(write_options, "key2751");

  // op 42988: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0998", "key11015", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0998, key11015) size=" << scan_result.size() << std::endl;
  }

  // op 42989: put
  db->Put(write_options, "key6256", "value8035");

  // op 42990: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8831", "key9027", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8831, key9027) size=" << scan_result.size() << std::endl;
  }

  // op 42991: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 42992: put
  db->Put(write_options, "key9234", "value13357");

  // op 42993: get
  status = db->Get(read_options, "key8260", &value);
  if (status.ok()) {
    out << "GET key8260 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42994: put
  db->Put(write_options, "key4263", "value102008");

  // op 42995: delete
  db->Delete(write_options, "key1640");

  // op 42996: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8462", "key10242", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8462, key10242) size=" << scan_result.size() << std::endl;
  }

  // op 42997: get
  status = db->Get(read_options, "key6704", &value);
  if (status.ok()) {
    out << "GET key6704 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 42998: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3277", "key9456", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3277, key9456) size=" << scan_result.size() << std::endl;
  }

  // op 42999: put
  db->Put(write_options, "key0862", "value31950");

  // op 43000: get
  status = db->Get(read_options, "key9557", &value);
  if (status.ok()) {
    out << "GET key9557 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43001: get
  status = db->Get(read_options, "key9386", &value);
  if (status.ok()) {
    out << "GET key9386 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43002: put
  db->Put(write_options, "key10455", "value72135");

  // op 43003: put
  db->Put(write_options, "key5577", "value102877");

  // op 43004: put
  db->Put(write_options, "key3588", "value19625");

  // op 43005: put
  db->Put(write_options, "key2951", "value100328");

  // op 43006: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5786", "key7034", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5786, key7034) size=" << scan_result.size() << std::endl;
  }

  // op 43007: put
  db->Put(write_options, "key8363", "value103987");

  // op 43008: put
  db->Put(write_options, "key7062", "value18198");

  // op 43009: put
  db->Put(write_options, "key3329", "value105862");

  // op 43010: put
  db->Put(write_options, "key9190", "value23523");

  // op 43011: put
  db->Put(write_options, "key8916", "value88700");

  // op 43012: delete
  db->Delete(write_options, "key2491");

  // op 43013: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 43014: put
  db->Put(write_options, "key1298", "value56786");

  // op 43015: put
  db->Put(write_options, "key10058", "value83526");

  // op 43016: delete_range
  status = db->DeleteRange(write_options, "key1109", "key1238");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 43017: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0203", "key3560", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0203, key3560) size=" << scan_result.size() << std::endl;
  }

  // op 43018: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3270", "key7391", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3270, key7391) size=" << scan_result.size() << std::endl;
  }

  // op 43019: delete_range
  status = db->DeleteRange(write_options, "key4435", "key6265");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 43020: delete
  db->Delete(write_options, "key8453");

  // op 43021: put
  db->Put(write_options, "key1273", "value83506");

  // op 43022: delete_range
  status = db->DeleteRange(write_options, "key1338", "key6838");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 43023: put
  db->Put(write_options, "key0876", "value67078");

  // op 43024: put
  db->Put(write_options, "key6687", "value19794");

  // op 43025: get
  status = db->Get(read_options, "key10438", &value);
  if (status.ok()) {
    out << "GET key10438 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43026: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 43027: put
  db->Put(write_options, "key4736", "value49037");

  // op 43028: get
  status = db->Get(read_options, "key1898", &value);
  if (status.ok()) {
    out << "GET key1898 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43029: delete_range
  status = db->DeleteRange(write_options, "key1122", "key2050");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 43030: delete_range
  status = db->DeleteRange(write_options, "key6114", "key9590");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 43031: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5457", "key6199", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5457, key6199) size=" << scan_result.size() << std::endl;
  }

  // op 43032: get
  status = db->Get(read_options, "key4539", &value);
  if (status.ok()) {
    out << "GET key4539 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43033: put
  db->Put(write_options, "key6018", "value79974");

  // op 43034: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7793", "key9088", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7793, key9088) size=" << scan_result.size() << std::endl;
  }

  // op 43035: put
  db->Put(write_options, "key3811", "value88921");

  // op 43036: put
  db->Put(write_options, "key9919", "value15732");

  // op 43037: get
  status = db->Get(read_options, "key6586", &value);
  if (status.ok()) {
    out << "GET key6586 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43038: put
  db->Put(write_options, "key11704", "value88189");

  // op 43039: put
  db->Put(write_options, "key4261", "value36264");

  // op 43040: put
  db->Put(write_options, "key11234", "value42537");

  // op 43041: delete_range
  status = db->DeleteRange(write_options, "key0075", "key4681");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 43042: get
  status = db->Get(read_options, "key7787", &value);
  if (status.ok()) {
    out << "GET key7787 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43043: put
  db->Put(write_options, "key5367", "value63393");

  // op 43044: delete
  db->Delete(write_options, "key10093");

  // op 43045: get
  status = db->Get(read_options, "key2982", &value);
  if (status.ok()) {
    out << "GET key2982 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43046: delete
  db->Delete(write_options, "key10093");

  // op 43047: put
  db->Put(write_options, "key6806", "value5981");

  // op 43048: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1843", "key4188", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1843, key4188) size=" << scan_result.size() << std::endl;
  }

  // op 43049: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4147", "key7135", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4147, key7135) size=" << scan_result.size() << std::endl;
  }

  // op 43050: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1675", "key7482", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1675, key7482) size=" << scan_result.size() << std::endl;
  }

  // op 43051: put
  db->Put(write_options, "key0049", "value85992");

  // op 43052: get
  status = db->Get(read_options, "key8004", &value);
  if (status.ok()) {
    out << "GET key8004 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43053: get
  status = db->Get(read_options, "key5940", &value);
  if (status.ok()) {
    out << "GET key5940 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43054: delete
  db->Delete(write_options, "key4411");

  // op 43055: put
  db->Put(write_options, "key2288", "value33131");

  // op 43056: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1145", "key4584", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1145, key4584) size=" << scan_result.size() << std::endl;
  }

  // op 43057: put
  db->Put(write_options, "key0062", "value17769");

  // op 43058: delete
  db->Delete(write_options, "key10810");

  // op 43059: delete_range
  status = db->DeleteRange(write_options, "key8560", "key9146");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 43060: put
  db->Put(write_options, "key5379", "value68129");

  // op 43061: get
  status = db->Get(read_options, "key0278", &value);
  if (status.ok()) {
    out << "GET key0278 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43062: delete_range
  status = db->DeleteRange(write_options, "key0070", "key7176");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 43063: delete_range
  status = db->DeleteRange(write_options, "key3215", "key5144");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 43064: put
  db->Put(write_options, "key5750", "value63578");

  // op 43065: put
  db->Put(write_options, "key5835", "value64702");

  // op 43066: put
  db->Put(write_options, "key8712", "value30779");

  // op 43067: put
  db->Put(write_options, "key3362", "value67894");

  // op 43068: delete
  db->Delete(write_options, "key10888");

  // op 43069: put
  db->Put(write_options, "key10632", "value1784");

  // op 43070: put
  db->Put(write_options, "key2338", "value53815");

  // op 43071: delete
  db->Delete(write_options, "key10967");

  // op 43072: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1704", "key6482", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1704, key6482) size=" << scan_result.size() << std::endl;
  }

  // op 43073: get
  status = db->Get(read_options, "key1346", &value);
  if (status.ok()) {
    out << "GET key1346 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43074: get
  status = db->Get(read_options, "key5498", &value);
  if (status.ok()) {
    out << "GET key5498 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43075: put
  db->Put(write_options, "key11196", "value66737");

  // op 43076: delete
  db->Delete(write_options, "key10874");

  // op 43077: put
  db->Put(write_options, "key0697", "value17353");

  // op 43078: put
  db->Put(write_options, "key5995", "value115423");

  // op 43079: get
  status = db->Get(read_options, "key4034", &value);
  if (status.ok()) {
    out << "GET key4034 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43080: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10714", "key11180", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10714, key11180) size=" << scan_result.size() << std::endl;
  }

  // op 43081: put
  db->Put(write_options, "key3471", "value26987");

  // op 43082: get
  status = db->Get(read_options, "key4905", &value);
  if (status.ok()) {
    out << "GET key4905 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43083: put
  db->Put(write_options, "key0589", "value115822");

  // op 43084: put
  db->Put(write_options, "key1798", "value115418");

  // op 43085: delete_range
  status = db->DeleteRange(write_options, "key6393", "key9811");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 43086: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1136", "key5088", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1136, key5088) size=" << scan_result.size() << std::endl;
  }

  // op 43087: put
  db->Put(write_options, "key4386", "value47085");

  // op 43088: get
  status = db->Get(read_options, "key6649", &value);
  if (status.ok()) {
    out << "GET key6649 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43089: delete_range
  status = db->DeleteRange(write_options, "key1693", "key6421");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 43090: put
  db->Put(write_options, "key2224", "value38668");

  // op 43091: get
  status = db->Get(read_options, "key7499", &value);
  if (status.ok()) {
    out << "GET key7499 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43092: get
  status = db->Get(read_options, "key2271", &value);
  if (status.ok()) {
    out << "GET key2271 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43093: put
  db->Put(write_options, "key7951", "value92105");

  // op 43094: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 43095: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 43096: get
  status = db->Get(read_options, "key8600", &value);
  if (status.ok()) {
    out << "GET key8600 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43097: delete
  db->Delete(write_options, "key7176");

  // op 43098: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7970", "key11353", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7970, key11353) size=" << scan_result.size() << std::endl;
  }

  // op 43099: put
  db->Put(write_options, "key10708", "value9274");

  // op 43100: delete
  db->Delete(write_options, "key5196");

  // op 43101: delete
  db->Delete(write_options, "key6623");

  // op 43102: put
  db->Put(write_options, "key10935", "value82014");

  // op 43103: get
  status = db->Get(read_options, "key3106", &value);
  if (status.ok()) {
    out << "GET key3106 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43104: get
  status = db->Get(read_options, "key11479", &value);
  if (status.ok()) {
    out << "GET key11479 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43105: put
  db->Put(write_options, "key2772", "value49658");

  // op 43106: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1924", "key8720", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1924, key8720) size=" << scan_result.size() << std::endl;
  }

  // op 43107: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0695", "key0816", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0695, key0816) size=" << scan_result.size() << std::endl;
  }

  // op 43108: get
  status = db->Get(read_options, "key10563", &value);
  if (status.ok()) {
    out << "GET key10563 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43109: put
  db->Put(write_options, "key10929", "value11824");

  // op 43110: put
  db->Put(write_options, "key3003", "value104622");

  // op 43111: put
  db->Put(write_options, "key0083", "value37568");

  // op 43112: put
  db->Put(write_options, "key6050", "value93585");

  // op 43113: delete_range
  status = db->DeleteRange(write_options, "key2367", "key10826");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 43114: put
  db->Put(write_options, "key4362", "value84323");

  // op 43115: get
  status = db->Get(read_options, "key10254", &value);
  if (status.ok()) {
    out << "GET key10254 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43116: delete_range
  status = db->DeleteRange(write_options, "key1577", "key8337");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 43117: delete
  db->Delete(write_options, "key6548");

  // op 43118: get
  status = db->Get(read_options, "key0860", &value);
  if (status.ok()) {
    out << "GET key0860 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43119: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1735", "key6460", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1735, key6460) size=" << scan_result.size() << std::endl;
  }

  // op 43120: get
  status = db->Get(read_options, "key8066", &value);
  if (status.ok()) {
    out << "GET key8066 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43121: put
  db->Put(write_options, "key10134", "value90435");

  // op 43122: get
  status = db->Get(read_options, "key7285", &value);
  if (status.ok()) {
    out << "GET key7285 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43123: put
  db->Put(write_options, "key6421", "value86599");

  // op 43124: get
  status = db->Get(read_options, "key1041", &value);
  if (status.ok()) {
    out << "GET key1041 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43125: put
  db->Put(write_options, "key7184", "value32165");

  // op 43126: delete
  db->Delete(write_options, "key0708");

  // op 43127: put
  db->Put(write_options, "key5789", "value27057");

  // op 43128: delete
  db->Delete(write_options, "key8403");

  // op 43129: delete_range
  status = db->DeleteRange(write_options, "key7555", "key10405");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 43130: get
  status = db->Get(read_options, "key4816", &value);
  if (status.ok()) {
    out << "GET key4816 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43131: put
  db->Put(write_options, "key1665", "value78959");

  // op 43132: delete_range
  status = db->DeleteRange(write_options, "key1539", "key6777");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 43133: put
  db->Put(write_options, "key9534", "value62314");

  // op 43134: put
  db->Put(write_options, "key0435", "value79885");

  // op 43135: put
  db->Put(write_options, "key11175", "value113692");

  // op 43136: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 43137: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3976", "key6290", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3976, key6290) size=" << scan_result.size() << std::endl;
  }

  // op 43138: delete
  db->Delete(write_options, "key4257");

  // op 43139: put
  db->Put(write_options, "key10724", "value72200");

  // op 43140: get
  status = db->Get(read_options, "key1580", &value);
  if (status.ok()) {
    out << "GET key1580 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43141: delete_range
  status = db->DeleteRange(write_options, "key2587", "key7285");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 43142: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2937", "key3113", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2937, key3113) size=" << scan_result.size() << std::endl;
  }

  // op 43143: put
  db->Put(write_options, "key3674", "value113311");

  // op 43144: delete_range
  status = db->DeleteRange(write_options, "key0920", "key10933");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 43145: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4275", "key10318", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4275, key10318) size=" << scan_result.size() << std::endl;
  }

  // op 43146: put
  db->Put(write_options, "key9471", "value12200");

  // op 43147: get
  status = db->Get(read_options, "key1719", &value);
  if (status.ok()) {
    out << "GET key1719 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43148: put
  db->Put(write_options, "key1194", "value56695");

  // op 43149: put
  db->Put(write_options, "key6976", "value54633");

  // op 43150: get
  status = db->Get(read_options, "key3768", &value);
  if (status.ok()) {
    out << "GET key3768 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43151: put
  db->Put(write_options, "key2584", "value42528");

  // op 43152: put
  db->Put(write_options, "key8898", "value30792");

  // op 43153: delete_range
  status = db->DeleteRange(write_options, "key5240", "key11620");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 43154: get
  status = db->Get(read_options, "key9164", &value);
  if (status.ok()) {
    out << "GET key9164 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43155: delete
  db->Delete(write_options, "key8796");

  // op 43156: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5150", "key10930", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5150, key10930) size=" << scan_result.size() << std::endl;
  }

  // op 43157: delete_range
  status = db->DeleteRange(write_options, "key4915", "key4923");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 43158: get
  status = db->Get(read_options, "key1045", &value);
  if (status.ok()) {
    out << "GET key1045 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43159: put
  db->Put(write_options, "key1980", "value18694");

  // op 43160: delete
  db->Delete(write_options, "key9996");

  // op 43161: delete
  db->Delete(write_options, "key5404");

  // op 43162: put
  db->Put(write_options, "key11474", "value112718");

  // op 43163: put
  db->Put(write_options, "key1846", "value84115");

  // op 43164: put
  db->Put(write_options, "key9886", "value91565");

  // op 43165: delete
  db->Delete(write_options, "key5323");

  // op 43166: put
  db->Put(write_options, "key11695", "value36299");

  // op 43167: get
  status = db->Get(read_options, "key10023", &value);
  if (status.ok()) {
    out << "GET key10023 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43168: delete
  db->Delete(write_options, "key3733");

  // op 43169: put
  db->Put(write_options, "key8525", "value92243");

  // op 43170: put
  db->Put(write_options, "key3363", "value111321");

  // op 43171: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0000", "key8750", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0000, key8750) size=" << scan_result.size() << std::endl;
  }

  // op 43172: delete
  db->Delete(write_options, "key1860");

  // op 43173: put
  db->Put(write_options, "key4123", "value85770");

  // op 43174: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7179", "key11236", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7179, key11236) size=" << scan_result.size() << std::endl;
  }

  // op 43175: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2872", "key10155", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2872, key10155) size=" << scan_result.size() << std::endl;
  }

  // op 43176: put
  db->Put(write_options, "key9671", "value24548");

  // op 43177: get
  status = db->Get(read_options, "key5416", &value);
  if (status.ok()) {
    out << "GET key5416 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43178: get
  status = db->Get(read_options, "key8678", &value);
  if (status.ok()) {
    out << "GET key8678 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43179: put
  db->Put(write_options, "key6832", "value81266");

  // op 43180: get
  status = db->Get(read_options, "key8544", &value);
  if (status.ok()) {
    out << "GET key8544 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43181: put
  db->Put(write_options, "key7823", "value41261");

  // op 43182: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1318", "key4911", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1318, key4911) size=" << scan_result.size() << std::endl;
  }

  // op 43183: get
  status = db->Get(read_options, "key6862", &value);
  if (status.ok()) {
    out << "GET key6862 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43184: get
  status = db->Get(read_options, "key8298", &value);
  if (status.ok()) {
    out << "GET key8298 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43185: put
  db->Put(write_options, "key2635", "value100347");

  // op 43186: put
  db->Put(write_options, "key4118", "value79596");

  // op 43187: get
  status = db->Get(read_options, "key5941", &value);
  if (status.ok()) {
    out << "GET key5941 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43188: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1914", "key9160", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1914, key9160) size=" << scan_result.size() << std::endl;
  }

  // op 43189: put
  db->Put(write_options, "key7075", "value100080");

  // op 43190: put
  db->Put(write_options, "key2089", "value17190");

  // op 43191: put
  db->Put(write_options, "key6233", "value18530");

  // op 43192: put
  db->Put(write_options, "key9776", "value70339");

  // op 43193: delete_range
  status = db->DeleteRange(write_options, "key0133", "key7299");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 43194: put
  db->Put(write_options, "key5706", "value74139");

  // op 43195: get
  status = db->Get(read_options, "key6874", &value);
  if (status.ok()) {
    out << "GET key6874 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43196: get
  status = db->Get(read_options, "key6387", &value);
  if (status.ok()) {
    out << "GET key6387 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43197: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 43198: delete
  db->Delete(write_options, "key7733");

  // op 43199: put
  db->Put(write_options, "key7515", "value95846");

  // op 43200: put
  db->Put(write_options, "key4997", "value76288");

  // op 43201: put
  db->Put(write_options, "key0005", "value21712");

  // op 43202: put
  db->Put(write_options, "key9321", "value24760");

  // op 43203: get
  status = db->Get(read_options, "key1138", &value);
  if (status.ok()) {
    out << "GET key1138 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43204: put
  db->Put(write_options, "key6633", "value100546");

  // op 43205: delete_range
  status = db->DeleteRange(write_options, "key1866", "key9934");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 43206: put
  db->Put(write_options, "key8269", "value32139");

  // op 43207: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 43208: put
  db->Put(write_options, "key7694", "value6021");

  // op 43209: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0133", "key10911", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0133, key10911) size=" << scan_result.size() << std::endl;
  }

  // op 43210: get
  status = db->Get(read_options, "key7009", &value);
  if (status.ok()) {
    out << "GET key7009 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43211: put
  db->Put(write_options, "key4014", "value65490");

  // op 43212: put
  db->Put(write_options, "key11020", "value50722");

  // op 43213: put
  db->Put(write_options, "key11551", "value44723");

  // op 43214: get
  status = db->Get(read_options, "key4063", &value);
  if (status.ok()) {
    out << "GET key4063 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43215: put
  db->Put(write_options, "key10829", "value87147");

  // op 43216: put
  db->Put(write_options, "key3452", "value26707");

  // op 43217: delete_range
  status = db->DeleteRange(write_options, "key6413", "key11084");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 43218: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0594", "key3333", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0594, key3333) size=" << scan_result.size() << std::endl;
  }

  // op 43219: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 43220: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0587", "key1282", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0587, key1282) size=" << scan_result.size() << std::endl;
  }

  // op 43221: delete
  db->Delete(write_options, "key6899");

  // op 43222: put
  db->Put(write_options, "key2610", "value106000");

  // op 43223: put
  db->Put(write_options, "key4341", "value88939");

  // op 43224: get
  status = db->Get(read_options, "key8675", &value);
  if (status.ok()) {
    out << "GET key8675 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43225: put
  db->Put(write_options, "key7128", "value61730");

  // op 43226: put
  db->Put(write_options, "key6962", "value115787");

  // op 43227: delete
  db->Delete(write_options, "key2129");

  // op 43228: delete
  db->Delete(write_options, "key2655");

  // op 43229: get
  status = db->Get(read_options, "key9398", &value);
  if (status.ok()) {
    out << "GET key9398 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43230: put
  db->Put(write_options, "key10730", "value54636");

  // op 43231: delete
  db->Delete(write_options, "key7574");

  // op 43232: delete
  db->Delete(write_options, "key4317");

  // op 43233: put
  db->Put(write_options, "key5588", "value14551");

  // op 43234: put
  db->Put(write_options, "key5566", "value25709");

  // op 43235: put
  db->Put(write_options, "key4049", "value73844");

  // op 43236: put
  db->Put(write_options, "key5047", "value73115");

  // op 43237: get
  status = db->Get(read_options, "key1810", &value);
  if (status.ok()) {
    out << "GET key1810 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43238: put
  db->Put(write_options, "key11345", "value78732");

  // op 43239: put
  db->Put(write_options, "key8178", "value46813");

  // op 43240: delete
  db->Delete(write_options, "key5592");

  // op 43241: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 43242: put
  db->Put(write_options, "key11237", "value4194");

  // op 43243: put
  db->Put(write_options, "key3133", "value105088");

  // op 43244: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4848", "key8325", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4848, key8325) size=" << scan_result.size() << std::endl;
  }

  // op 43245: put
  db->Put(write_options, "key2707", "value101591");

  // op 43246: put
  db->Put(write_options, "key11012", "value35865");

  // op 43247: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1005", "key10946", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1005, key10946) size=" << scan_result.size() << std::endl;
  }

  // op 43248: delete_range
  status = db->DeleteRange(write_options, "key10511", "key11258");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 43249: put
  db->Put(write_options, "key10536", "value36568");

  // op 43250: get
  status = db->Get(read_options, "key3753", &value);
  if (status.ok()) {
    out << "GET key3753 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43251: get
  status = db->Get(read_options, "key11124", &value);
  if (status.ok()) {
    out << "GET key11124 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43252: delete
  db->Delete(write_options, "key10207");

  // op 43253: put
  db->Put(write_options, "key0475", "value3577");

  // op 43254: put
  db->Put(write_options, "key4523", "value96106");

  // op 43255: put
  db->Put(write_options, "key5763", "value22417");

  // op 43256: delete
  db->Delete(write_options, "key0939");

  // op 43257: get
  status = db->Get(read_options, "key5487", &value);
  if (status.ok()) {
    out << "GET key5487 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43258: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8299", "key9157", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8299, key9157) size=" << scan_result.size() << std::endl;
  }

  // op 43259: put
  db->Put(write_options, "key8021", "value87072");

  // op 43260: put
  db->Put(write_options, "key0025", "value15273");

  // op 43261: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7501", "key7578", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7501, key7578) size=" << scan_result.size() << std::endl;
  }

  // op 43262: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1660", "key3092", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1660, key3092) size=" << scan_result.size() << std::endl;
  }

  // op 43263: put
  db->Put(write_options, "key0559", "value97307");

  // op 43264: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3706", "key9970", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3706, key9970) size=" << scan_result.size() << std::endl;
  }

  // op 43265: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3820", "key10007", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3820, key10007) size=" << scan_result.size() << std::endl;
  }

  // op 43266: delete_range
  status = db->DeleteRange(write_options, "key4112", "key10127");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 43267: delete_range
  status = db->DeleteRange(write_options, "key4335", "key10297");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 43268: put
  db->Put(write_options, "key10602", "value45919");

  // op 43269: put
  db->Put(write_options, "key6054", "value73386");

  // op 43270: put
  db->Put(write_options, "key2979", "value96381");

  // op 43271: get
  status = db->Get(read_options, "key4356", &value);
  if (status.ok()) {
    out << "GET key4356 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43272: put
  db->Put(write_options, "key10968", "value103604");

  // op 43273: put
  db->Put(write_options, "key8592", "value54743");

  // op 43274: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9457", "key11584", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9457, key11584) size=" << scan_result.size() << std::endl;
  }

  // op 43275: put
  db->Put(write_options, "key1238", "value52503");

  // op 43276: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5328", "key7103", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5328, key7103) size=" << scan_result.size() << std::endl;
  }

  // op 43277: put
  db->Put(write_options, "key6342", "value57658");

  // op 43278: put
  db->Put(write_options, "key6909", "value5511");

  // op 43279: put
  db->Put(write_options, "key6119", "value43081");

  // op 43280: delete
  db->Delete(write_options, "key0096");

  // op 43281: get
  status = db->Get(read_options, "key7731", &value);
  if (status.ok()) {
    out << "GET key7731 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43282: put
  db->Put(write_options, "key5088", "value2555");

  // op 43283: delete
  db->Delete(write_options, "key7323");

  // op 43284: put
  db->Put(write_options, "key6314", "value28129");

  // op 43285: put
  db->Put(write_options, "key11686", "value74824");

  // op 43286: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7859", "key9190", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7859, key9190) size=" << scan_result.size() << std::endl;
  }

  // op 43287: put
  db->Put(write_options, "key2241", "value12837");

  // op 43288: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6509", "key7686", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6509, key7686) size=" << scan_result.size() << std::endl;
  }

  // op 43289: delete
  db->Delete(write_options, "key2892");

  // op 43290: delete
  db->Delete(write_options, "key6708");

  // op 43291: put
  db->Put(write_options, "key11658", "value88007");

  // op 43292: put
  db->Put(write_options, "key5030", "value70516");

  // op 43293: put
  db->Put(write_options, "key1730", "value101287");

  // op 43294: put
  db->Put(write_options, "key3918", "value72495");

  // op 43295: delete
  db->Delete(write_options, "key9434");

  // op 43296: put
  db->Put(write_options, "key7868", "value29643");

  // op 43297: delete_range
  status = db->DeleteRange(write_options, "key2337", "key9794");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 43298: get
  status = db->Get(read_options, "key7519", &value);
  if (status.ok()) {
    out << "GET key7519 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43299: delete
  db->Delete(write_options, "key7451");

  // op 43300: put
  db->Put(write_options, "key7815", "value37637");

  // op 43301: delete
  db->Delete(write_options, "key9334");

  // op 43302: delete_range
  status = db->DeleteRange(write_options, "key5208", "key8384");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 43303: put
  db->Put(write_options, "key11492", "value51237");

  // op 43304: put
  db->Put(write_options, "key0784", "value20268");

  // op 43305: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3337", "key8380", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3337, key8380) size=" << scan_result.size() << std::endl;
  }

  // op 43306: put
  db->Put(write_options, "key6088", "value76798");

  // op 43307: put
  db->Put(write_options, "key3189", "value86809");

  // op 43308: put
  db->Put(write_options, "key1154", "value46340");

  // op 43309: delete_range
  status = db->DeleteRange(write_options, "key0853", "key6839");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 43310: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0814", "key3760", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0814, key3760) size=" << scan_result.size() << std::endl;
  }

  // op 43311: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1913", "key9530", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1913, key9530) size=" << scan_result.size() << std::endl;
  }

  // op 43312: put
  db->Put(write_options, "key6520", "value26100");

  // op 43313: put
  db->Put(write_options, "key8474", "value19862");

  // op 43314: put
  db->Put(write_options, "key2889", "value111543");

  // op 43315: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 43316: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3910", "key6739", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3910, key6739) size=" << scan_result.size() << std::endl;
  }

  // op 43317: put
  db->Put(write_options, "key3661", "value33917");

  // op 43318: put
  db->Put(write_options, "key4277", "value8473");

  // op 43319: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2007", "key2547", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2007, key2547) size=" << scan_result.size() << std::endl;
  }

  // op 43320: put
  db->Put(write_options, "key10442", "value92437");

  // op 43321: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1188", "key6878", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1188, key6878) size=" << scan_result.size() << std::endl;
  }

  // op 43322: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 43323: put
  db->Put(write_options, "key10474", "value63195");

  // op 43324: delete_range
  status = db->DeleteRange(write_options, "key0801", "key8044");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 43325: put
  db->Put(write_options, "key10979", "value65293");

  // op 43326: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9177", "key11147", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9177, key11147) size=" << scan_result.size() << std::endl;
  }

  // op 43327: put
  db->Put(write_options, "key5935", "value27653");

  // op 43328: put
  db->Put(write_options, "key9800", "value47088");

  // op 43329: put
  db->Put(write_options, "key0267", "value37374");

  // op 43330: get
  status = db->Get(read_options, "key1373", &value);
  if (status.ok()) {
    out << "GET key1373 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43331: get
  status = db->Get(read_options, "key7704", &value);
  if (status.ok()) {
    out << "GET key7704 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43332: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3054", "key6666", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3054, key6666) size=" << scan_result.size() << std::endl;
  }

  // op 43333: put
  db->Put(write_options, "key0624", "value23877");

  // op 43334: get
  status = db->Get(read_options, "key6686", &value);
  if (status.ok()) {
    out << "GET key6686 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43335: put
  db->Put(write_options, "key7254", "value90265");

  // op 43336: put
  db->Put(write_options, "key7885", "value80500");

  // op 43337: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 43338: delete
  db->Delete(write_options, "key10699");

  // op 43339: get
  status = db->Get(read_options, "key4998", &value);
  if (status.ok()) {
    out << "GET key4998 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43340: put
  db->Put(write_options, "key2690", "value24399");

  // op 43341: put
  db->Put(write_options, "key6248", "value58226");

  // op 43342: put
  db->Put(write_options, "key3812", "value67606");

  // op 43343: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 43344: put
  db->Put(write_options, "key4951", "value81370");

  // op 43345: get
  status = db->Get(read_options, "key4975", &value);
  if (status.ok()) {
    out << "GET key4975 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43346: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2325", "key3617", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2325, key3617) size=" << scan_result.size() << std::endl;
  }

  // op 43347: put
  db->Put(write_options, "key9415", "value92484");

  // op 43348: get
  status = db->Get(read_options, "key5197", &value);
  if (status.ok()) {
    out << "GET key5197 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43349: put
  db->Put(write_options, "key9885", "value80387");

  // op 43350: get
  status = db->Get(read_options, "key4830", &value);
  if (status.ok()) {
    out << "GET key4830 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43351: put
  db->Put(write_options, "key8223", "value81025");

  // op 43352: put
  db->Put(write_options, "key6379", "value53005");

  // op 43353: put
  db->Put(write_options, "key1746", "value81349");

  // op 43354: put
  db->Put(write_options, "key0590", "value22405");

  // op 43355: delete
  db->Delete(write_options, "key1981");

  // op 43356: put
  db->Put(write_options, "key7574", "value2794");

  // op 43357: get
  status = db->Get(read_options, "key0857", &value);
  if (status.ok()) {
    out << "GET key0857 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43358: delete
  db->Delete(write_options, "key10344");

  // op 43359: get
  status = db->Get(read_options, "key11389", &value);
  if (status.ok()) {
    out << "GET key11389 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43360: put
  db->Put(write_options, "key1866", "value26578");

  // op 43361: put
  db->Put(write_options, "key2246", "value11568");

  // op 43362: delete
  db->Delete(write_options, "key6516");

  // op 43363: delete
  db->Delete(write_options, "key3916");

  // op 43364: put
  db->Put(write_options, "key4312", "value90756");

  // op 43365: put
  db->Put(write_options, "key6902", "value115197");

  // op 43366: put
  db->Put(write_options, "key6280", "value100551");

  // op 43367: delete_range
  status = db->DeleteRange(write_options, "key0480", "key3961");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 43368: put
  db->Put(write_options, "key3656", "value67274");

  // op 43369: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10041", "key10771", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10041, key10771) size=" << scan_result.size() << std::endl;
  }

  // op 43370: put
  db->Put(write_options, "key6592", "value66392");

  // op 43371: get
  status = db->Get(read_options, "key1744", &value);
  if (status.ok()) {
    out << "GET key1744 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43372: get
  status = db->Get(read_options, "key2523", &value);
  if (status.ok()) {
    out << "GET key2523 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43373: delete
  db->Delete(write_options, "key6314");

  // op 43374: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4603", "key5115", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4603, key5115) size=" << scan_result.size() << std::endl;
  }

  // op 43375: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 43376: put
  db->Put(write_options, "key10357", "value76679");

  // op 43377: put
  db->Put(write_options, "key9879", "value50947");

  // op 43378: put
  db->Put(write_options, "key5264", "value58073");

  // op 43379: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5765", "key8262", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5765, key8262) size=" << scan_result.size() << std::endl;
  }

  // op 43380: put
  db->Put(write_options, "key4302", "value63503");

  // op 43381: put
  db->Put(write_options, "key6502", "value113217");

  // op 43382: put
  db->Put(write_options, "key4820", "value32431");

  // op 43383: put
  db->Put(write_options, "key11324", "value77659");

  // op 43384: put
  db->Put(write_options, "key0696", "value45976");

  // op 43385: put
  db->Put(write_options, "key1761", "value113902");

  // op 43386: get
  status = db->Get(read_options, "key8592", &value);
  if (status.ok()) {
    out << "GET key8592 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43387: put
  db->Put(write_options, "key11390", "value3525");

  // op 43388: put
  db->Put(write_options, "key11129", "value22335");

  // op 43389: put
  db->Put(write_options, "key9053", "value95261");

  // op 43390: put
  db->Put(write_options, "key10360", "value116032");

  // op 43391: delete_range
  status = db->DeleteRange(write_options, "key9514", "key10969");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 43392: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7062", "key9505", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7062, key9505) size=" << scan_result.size() << std::endl;
  }

  // op 43393: get
  status = db->Get(read_options, "key8322", &value);
  if (status.ok()) {
    out << "GET key8322 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43394: get
  status = db->Get(read_options, "key5897", &value);
  if (status.ok()) {
    out << "GET key5897 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43395: get
  status = db->Get(read_options, "key7535", &value);
  if (status.ok()) {
    out << "GET key7535 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43396: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0017", "key5550", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0017, key5550) size=" << scan_result.size() << std::endl;
  }

  // op 43397: put
  db->Put(write_options, "key1906", "value68175");

  // op 43398: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5811", "key6858", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5811, key6858) size=" << scan_result.size() << std::endl;
  }

  // op 43399: get
  status = db->Get(read_options, "key9859", &value);
  if (status.ok()) {
    out << "GET key9859 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43400: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7389", "key10438", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7389, key10438) size=" << scan_result.size() << std::endl;
  }

  // op 43401: get
  status = db->Get(read_options, "key9999", &value);
  if (status.ok()) {
    out << "GET key9999 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43402: delete
  db->Delete(write_options, "key3590");

  // op 43403: delete
  db->Delete(write_options, "key1652");

  // op 43404: get
  status = db->Get(read_options, "key8882", &value);
  if (status.ok()) {
    out << "GET key8882 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43405: get
  status = db->Get(read_options, "key0846", &value);
  if (status.ok()) {
    out << "GET key0846 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43406: get
  status = db->Get(read_options, "key5109", &value);
  if (status.ok()) {
    out << "GET key5109 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43407: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0840", "key3644", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0840, key3644) size=" << scan_result.size() << std::endl;
  }

  // op 43408: put
  db->Put(write_options, "key4600", "value81104");

  // op 43409: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2551", "key4755", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2551, key4755) size=" << scan_result.size() << std::endl;
  }

  // op 43410: get
  status = db->Get(read_options, "key7581", &value);
  if (status.ok()) {
    out << "GET key7581 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43411: put
  db->Put(write_options, "key2422", "value54148");

  // op 43412: put
  db->Put(write_options, "key9647", "value68201");

  // op 43413: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7640", "key11697", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7640, key11697) size=" << scan_result.size() << std::endl;
  }

  // op 43414: put
  db->Put(write_options, "key7522", "value70081");

  // op 43415: put
  db->Put(write_options, "key2290", "value55004");

  // op 43416: put
  db->Put(write_options, "key8072", "value64374");

  // op 43417: put
  db->Put(write_options, "key10560", "value111887");

  // op 43418: get
  status = db->Get(read_options, "key1162", &value);
  if (status.ok()) {
    out << "GET key1162 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43419: delete
  db->Delete(write_options, "key6932");

  // op 43420: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 43421: delete_range
  status = db->DeleteRange(write_options, "key6912", "key8514");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 43422: delete_range
  status = db->DeleteRange(write_options, "key0976", "key11335");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 43423: put
  db->Put(write_options, "key0099", "value94938");

  // op 43424: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3099", "key3149", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3099, key3149) size=" << scan_result.size() << std::endl;
  }

  // op 43425: put
  db->Put(write_options, "key3742", "value16021");

  // op 43426: delete
  db->Delete(write_options, "key8172");

  // op 43427: delete_range
  status = db->DeleteRange(write_options, "key4148", "key8202");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 43428: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1553", "key9021", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1553, key9021) size=" << scan_result.size() << std::endl;
  }

  // op 43429: put
  db->Put(write_options, "key8826", "value27589");

  // op 43430: delete_range
  status = db->DeleteRange(write_options, "key7888", "key11108");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 43431: put
  db->Put(write_options, "key8967", "value67440");

  // op 43432: delete_range
  status = db->DeleteRange(write_options, "key5462", "key7181");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 43433: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5108", "key7115", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5108, key7115) size=" << scan_result.size() << std::endl;
  }

  // op 43434: put
  db->Put(write_options, "key9319", "value43255");

  // op 43435: delete
  db->Delete(write_options, "key10598");

  // op 43436: put
  db->Put(write_options, "key2078", "value16607");

  // op 43437: put
  db->Put(write_options, "key11386", "value68250");

  // op 43438: put
  db->Put(write_options, "key0061", "value85051");

  // op 43439: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7093", "key11145", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7093, key11145) size=" << scan_result.size() << std::endl;
  }

  // op 43440: get
  status = db->Get(read_options, "key9181", &value);
  if (status.ok()) {
    out << "GET key9181 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43441: put
  db->Put(write_options, "key10985", "value98719");

  // op 43442: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2865", "key4382", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2865, key4382) size=" << scan_result.size() << std::endl;
  }

  // op 43443: get
  status = db->Get(read_options, "key7681", &value);
  if (status.ok()) {
    out << "GET key7681 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43444: put
  db->Put(write_options, "key4186", "value98738");

  // op 43445: put
  db->Put(write_options, "key11363", "value16435");

  // op 43446: put
  db->Put(write_options, "key9840", "value31855");

  // op 43447: delete_range
  status = db->DeleteRange(write_options, "key8975", "key10359");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 43448: delete
  db->Delete(write_options, "key10200");

  // op 43449: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 43450: delete_range
  status = db->DeleteRange(write_options, "key1382", "key4253");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 43451: put
  db->Put(write_options, "key11079", "value32655");

  // op 43452: put
  db->Put(write_options, "key0528", "value56273");

  // op 43453: put
  db->Put(write_options, "key10817", "value67680");

  // op 43454: put
  db->Put(write_options, "key6446", "value33170");

  // op 43455: get
  status = db->Get(read_options, "key8687", &value);
  if (status.ok()) {
    out << "GET key8687 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43456: delete
  db->Delete(write_options, "key2584");

  // op 43457: put
  db->Put(write_options, "key9793", "value50813");

  // op 43458: delete_range
  status = db->DeleteRange(write_options, "key3554", "key11636");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 43459: delete_range
  status = db->DeleteRange(write_options, "key5463", "key10172");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 43460: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 43461: get
  status = db->Get(read_options, "key2051", &value);
  if (status.ok()) {
    out << "GET key2051 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43462: delete
  db->Delete(write_options, "key0835");

  // op 43463: get
  status = db->Get(read_options, "key10645", &value);
  if (status.ok()) {
    out << "GET key10645 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43464: get
  status = db->Get(read_options, "key4086", &value);
  if (status.ok()) {
    out << "GET key4086 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43465: get
  status = db->Get(read_options, "key9054", &value);
  if (status.ok()) {
    out << "GET key9054 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43466: delete_range
  status = db->DeleteRange(write_options, "key7063", "key9964");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 43467: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1576", "key7707", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1576, key7707) size=" << scan_result.size() << std::endl;
  }

  // op 43468: put
  db->Put(write_options, "key2808", "value83329");

  // op 43469: get
  status = db->Get(read_options, "key7672", &value);
  if (status.ok()) {
    out << "GET key7672 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43470: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5475", "key9629", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5475, key9629) size=" << scan_result.size() << std::endl;
  }

  // op 43471: put
  db->Put(write_options, "key8667", "value13972");

  // op 43472: put
  db->Put(write_options, "key5406", "value77343");

  // op 43473: delete
  db->Delete(write_options, "key8732");

  // op 43474: delete
  db->Delete(write_options, "key9809");

  // op 43475: delete
  db->Delete(write_options, "key11465");

  // op 43476: put
  db->Put(write_options, "key11668", "value97206");

  // op 43477: put
  db->Put(write_options, "key6823", "value3032");

  // op 43478: get
  status = db->Get(read_options, "key0520", &value);
  if (status.ok()) {
    out << "GET key0520 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43479: delete
  db->Delete(write_options, "key5440");

  // op 43480: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0599", "key3039", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0599, key3039) size=" << scan_result.size() << std::endl;
  }

  // op 43481: put
  db->Put(write_options, "key1698", "value86128");

  // op 43482: delete_range
  status = db->DeleteRange(write_options, "key2946", "key5419");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 43483: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2053", "key4301", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2053, key4301) size=" << scan_result.size() << std::endl;
  }

  // op 43484: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 43485: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3781", "key5779", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3781, key5779) size=" << scan_result.size() << std::endl;
  }

  // op 43486: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0499", "key3520", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0499, key3520) size=" << scan_result.size() << std::endl;
  }

  // op 43487: put
  db->Put(write_options, "key7234", "value18706");

  // op 43488: delete_range
  status = db->DeleteRange(write_options, "key0286", "key2871");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 43489: get
  status = db->Get(read_options, "key1551", &value);
  if (status.ok()) {
    out << "GET key1551 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43490: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4836", "key5641", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4836, key5641) size=" << scan_result.size() << std::endl;
  }

  // op 43491: put
  db->Put(write_options, "key11685", "value14365");

  // op 43492: get
  status = db->Get(read_options, "key2671", &value);
  if (status.ok()) {
    out << "GET key2671 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43493: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 43494: get
  status = db->Get(read_options, "key0260", &value);
  if (status.ok()) {
    out << "GET key0260 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43495: put
  db->Put(write_options, "key10450", "value45201");

  // op 43496: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8286", "key11269", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8286, key11269) size=" << scan_result.size() << std::endl;
  }

  // op 43497: put
  db->Put(write_options, "key6118", "value22811");

  // op 43498: put
  db->Put(write_options, "key7533", "value112722");

  // op 43499: put
  db->Put(write_options, "key3623", "value70428");

  // op 43500: put
  db->Put(write_options, "key7799", "value35917");

  // op 43501: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4093", "key10399", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4093, key10399) size=" << scan_result.size() << std::endl;
  }

  // op 43502: get
  status = db->Get(read_options, "key0250", &value);
  if (status.ok()) {
    out << "GET key0250 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43503: get
  status = db->Get(read_options, "key5340", &value);
  if (status.ok()) {
    out << "GET key5340 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43504: put
  db->Put(write_options, "key6939", "value89038");

  // op 43505: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 43506: delete_range
  status = db->DeleteRange(write_options, "key5938", "key7475");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 43507: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0307", "key5996", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0307, key5996) size=" << scan_result.size() << std::endl;
  }

  // op 43508: get
  status = db->Get(read_options, "key3112", &value);
  if (status.ok()) {
    out << "GET key3112 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43509: get
  status = db->Get(read_options, "key1085", &value);
  if (status.ok()) {
    out << "GET key1085 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43510: put
  db->Put(write_options, "key11264", "value46522");

  // op 43511: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 43512: delete_range
  status = db->DeleteRange(write_options, "key1197", "key10503");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 43513: delete_range
  status = db->DeleteRange(write_options, "key4019", "key8191");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 43514: delete_range
  status = db->DeleteRange(write_options, "key1228", "key10503");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 43515: get
  status = db->Get(read_options, "key6960", &value);
  if (status.ok()) {
    out << "GET key6960 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43516: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10619", "key10967", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10619, key10967) size=" << scan_result.size() << std::endl;
  }

  // op 43517: get
  status = db->Get(read_options, "key5152", &value);
  if (status.ok()) {
    out << "GET key5152 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43518: put
  db->Put(write_options, "key10503", "value117830");

  // op 43519: delete
  db->Delete(write_options, "key1360");

  // op 43520: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0853", "key10936", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0853, key10936) size=" << scan_result.size() << std::endl;
  }

  // op 43521: get
  status = db->Get(read_options, "key6145", &value);
  if (status.ok()) {
    out << "GET key6145 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43522: put
  db->Put(write_options, "key11577", "value94873");

  // op 43523: put
  db->Put(write_options, "key5542", "value94166");

  // op 43524: put
  db->Put(write_options, "key8681", "value104807");

  // op 43525: put
  db->Put(write_options, "key5929", "value57281");

  // op 43526: get
  status = db->Get(read_options, "key3311", &value);
  if (status.ok()) {
    out << "GET key3311 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43527: get
  status = db->Get(read_options, "key0373", &value);
  if (status.ok()) {
    out << "GET key0373 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43528: get
  status = db->Get(read_options, "key1993", &value);
  if (status.ok()) {
    out << "GET key1993 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43529: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6339", "key9320", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6339, key9320) size=" << scan_result.size() << std::endl;
  }

  // op 43530: delete_range
  status = db->DeleteRange(write_options, "key1039", "key10078");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 43531: put
  db->Put(write_options, "key7675", "value46185");

  // op 43532: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10769", "key11045", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10769, key11045) size=" << scan_result.size() << std::endl;
  }

  // op 43533: put
  db->Put(write_options, "key7747", "value100867");

  // op 43534: get
  status = db->Get(read_options, "key3290", &value);
  if (status.ok()) {
    out << "GET key3290 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43535: put
  db->Put(write_options, "key4871", "value51132");

  // op 43536: get
  status = db->Get(read_options, "key2821", &value);
  if (status.ok()) {
    out << "GET key2821 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43537: put
  db->Put(write_options, "key6201", "value17086");

  // op 43538: get
  status = db->Get(read_options, "key3610", &value);
  if (status.ok()) {
    out << "GET key3610 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43539: put
  db->Put(write_options, "key4553", "value106839");

  // op 43540: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1113", "key5449", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1113, key5449) size=" << scan_result.size() << std::endl;
  }

  // op 43541: put
  db->Put(write_options, "key7805", "value21028");

  // op 43542: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0358", "key1771", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0358, key1771) size=" << scan_result.size() << std::endl;
  }

  // op 43543: put
  db->Put(write_options, "key5747", "value85781");

  // op 43544: get
  status = db->Get(read_options, "key8501", &value);
  if (status.ok()) {
    out << "GET key8501 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43545: delete
  db->Delete(write_options, "key9041");

  // op 43546: put
  db->Put(write_options, "key9999", "value46688");

  // op 43547: put
  db->Put(write_options, "key2719", "value26293");

  // op 43548: put
  db->Put(write_options, "key2154", "value45914");

  // op 43549: delete
  db->Delete(write_options, "key4489");

  // op 43550: get
  status = db->Get(read_options, "key9897", &value);
  if (status.ok()) {
    out << "GET key9897 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43551: put
  db->Put(write_options, "key3722", "value79330");

  // op 43552: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 43553: put
  db->Put(write_options, "key9053", "value98469");

  // op 43554: put
  db->Put(write_options, "key8251", "value86975");

  // op 43555: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0160", "key9499", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0160, key9499) size=" << scan_result.size() << std::endl;
  }

  // op 43556: delete_range
  status = db->DeleteRange(write_options, "key2577", "key9277");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 43557: get
  status = db->Get(read_options, "key3313", &value);
  if (status.ok()) {
    out << "GET key3313 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43558: get
  status = db->Get(read_options, "key3004", &value);
  if (status.ok()) {
    out << "GET key3004 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43559: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0267", "key0460", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0267, key0460) size=" << scan_result.size() << std::endl;
  }

  // op 43560: put
  db->Put(write_options, "key0754", "value28471");

  // op 43561: delete
  db->Delete(write_options, "key6402");

  // op 43562: delete
  db->Delete(write_options, "key8229");

  // op 43563: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 43564: put
  db->Put(write_options, "key5160", "value16375");

  // op 43565: get
  status = db->Get(read_options, "key0746", &value);
  if (status.ok()) {
    out << "GET key0746 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43566: put
  db->Put(write_options, "key6681", "value5966");

  // op 43567: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9085", "key10767", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9085, key10767) size=" << scan_result.size() << std::endl;
  }

  // op 43568: put
  db->Put(write_options, "key7893", "value99826");

  // op 43569: delete_range
  status = db->DeleteRange(write_options, "key7371", "key9382");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 43570: put
  db->Put(write_options, "key2476", "value24321");

  // op 43571: put
  db->Put(write_options, "key1172", "value55960");

  // op 43572: put
  db->Put(write_options, "key5791", "value35043");

  // op 43573: delete_range
  status = db->DeleteRange(write_options, "key2543", "key9484");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 43574: get
  status = db->Get(read_options, "key9496", &value);
  if (status.ok()) {
    out << "GET key9496 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43575: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 43576: put
  db->Put(write_options, "key10793", "value76470");

  // op 43577: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0248", "key0659", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0248, key0659) size=" << scan_result.size() << std::endl;
  }

  // op 43578: put
  db->Put(write_options, "key1363", "value65996");

  // op 43579: put
  db->Put(write_options, "key0823", "value44736");

  // op 43580: get
  status = db->Get(read_options, "key9645", &value);
  if (status.ok()) {
    out << "GET key9645 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43581: put
  db->Put(write_options, "key0972", "value3808");

  // op 43582: put
  db->Put(write_options, "key5221", "value3415");

  // op 43583: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 43584: get
  status = db->Get(read_options, "key10577", &value);
  if (status.ok()) {
    out << "GET key10577 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43585: put
  db->Put(write_options, "key10277", "value33979");

  // op 43586: get
  status = db->Get(read_options, "key6442", &value);
  if (status.ok()) {
    out << "GET key6442 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43587: delete_range
  status = db->DeleteRange(write_options, "key1770", "key9893");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 43588: put
  db->Put(write_options, "key7985", "value82144");

  // op 43589: delete_range
  status = db->DeleteRange(write_options, "key8940", "key10822");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 43590: put
  db->Put(write_options, "key2539", "value47988");

  // op 43591: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3666", "key8653", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3666, key8653) size=" << scan_result.size() << std::endl;
  }

  // op 43592: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 43593: delete_range
  status = db->DeleteRange(write_options, "key0816", "key2559");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 43594: put
  db->Put(write_options, "key4006", "value5038");

  // op 43595: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0343", "key9497", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0343, key9497) size=" << scan_result.size() << std::endl;
  }

  // op 43596: put
  db->Put(write_options, "key8320", "value59082");

  // op 43597: get
  status = db->Get(read_options, "key2493", &value);
  if (status.ok()) {
    out << "GET key2493 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43598: delete_range
  status = db->DeleteRange(write_options, "key6118", "key11133");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 43599: get
  status = db->Get(read_options, "key10797", &value);
  if (status.ok()) {
    out << "GET key10797 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43600: put
  db->Put(write_options, "key9593", "value11630");

  // op 43601: put
  db->Put(write_options, "key0697", "value34840");

  // op 43602: delete_range
  status = db->DeleteRange(write_options, "key5455", "key6336");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 43603: get
  status = db->Get(read_options, "key10056", &value);
  if (status.ok()) {
    out << "GET key10056 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43604: put
  db->Put(write_options, "key6250", "value84700");

  // op 43605: put
  db->Put(write_options, "key9041", "value32885");

  // op 43606: delete
  db->Delete(write_options, "key9330");

  // op 43607: get
  status = db->Get(read_options, "key11397", &value);
  if (status.ok()) {
    out << "GET key11397 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43608: put
  db->Put(write_options, "key2768", "value43607");

  // op 43609: put
  db->Put(write_options, "key5394", "value9859");

  // op 43610: get
  status = db->Get(read_options, "key4386", &value);
  if (status.ok()) {
    out << "GET key4386 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43611: delete_range
  status = db->DeleteRange(write_options, "key8264", "key8809");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 43612: delete_range
  status = db->DeleteRange(write_options, "key8391", "key11699");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 43613: delete_range
  status = db->DeleteRange(write_options, "key3402", "key5398");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 43614: put
  db->Put(write_options, "key3118", "value17170");

  // op 43615: put
  db->Put(write_options, "key9660", "value16725");

  // op 43616: put
  db->Put(write_options, "key4680", "value110109");

  // op 43617: delete_range
  status = db->DeleteRange(write_options, "key5940", "key8847");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 43618: put
  db->Put(write_options, "key4780", "value107714");

  // op 43619: put
  db->Put(write_options, "key8198", "value27487");

  // op 43620: put
  db->Put(write_options, "key3244", "value100526");

  // op 43621: delete
  db->Delete(write_options, "key8927");

  // op 43622: put
  db->Put(write_options, "key7680", "value61334");

  // op 43623: get
  status = db->Get(read_options, "key1804", &value);
  if (status.ok()) {
    out << "GET key1804 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43624: put
  db->Put(write_options, "key6651", "value21431");

  // op 43625: delete_range
  status = db->DeleteRange(write_options, "key7789", "key11376");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 43626: put
  db->Put(write_options, "key8344", "value62928");

  // op 43627: put
  db->Put(write_options, "key0766", "value71037");

  // op 43628: delete_range
  status = db->DeleteRange(write_options, "key7510", "key8863");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 43629: put
  db->Put(write_options, "key5433", "value45755");

  // op 43630: delete
  db->Delete(write_options, "key0426");

  // op 43631: put
  db->Put(write_options, "key3551", "value15194");

  // op 43632: get
  status = db->Get(read_options, "key11089", &value);
  if (status.ok()) {
    out << "GET key11089 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43633: put
  db->Put(write_options, "key1331", "value112054");

  // op 43634: get
  status = db->Get(read_options, "key2881", &value);
  if (status.ok()) {
    out << "GET key2881 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43635: put
  db->Put(write_options, "key1207", "value52315");

  // op 43636: put
  db->Put(write_options, "key0107", "value78329");

  // op 43637: get
  status = db->Get(read_options, "key6303", &value);
  if (status.ok()) {
    out << "GET key6303 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43638: delete
  db->Delete(write_options, "key8449");

  // op 43639: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8839", "key9370", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8839, key9370) size=" << scan_result.size() << std::endl;
  }

  // op 43640: put
  db->Put(write_options, "key6041", "value95564");

  // op 43641: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6493", "key9513", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6493, key9513) size=" << scan_result.size() << std::endl;
  }

  // op 43642: get
  status = db->Get(read_options, "key3502", &value);
  if (status.ok()) {
    out << "GET key3502 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43643: get
  status = db->Get(read_options, "key6684", &value);
  if (status.ok()) {
    out << "GET key6684 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43644: delete_range
  status = db->DeleteRange(write_options, "key4434", "key10111");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 43645: delete_range
  status = db->DeleteRange(write_options, "key4189", "key8678");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 43646: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5066", "key9664", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5066, key9664) size=" << scan_result.size() << std::endl;
  }

  // op 43647: get
  status = db->Get(read_options, "key1834", &value);
  if (status.ok()) {
    out << "GET key1834 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43648: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 43649: get
  status = db->Get(read_options, "key0781", &value);
  if (status.ok()) {
    out << "GET key0781 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43650: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1453", "key6229", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1453, key6229) size=" << scan_result.size() << std::endl;
  }

  // op 43651: put
  db->Put(write_options, "key3318", "value65910");

  // op 43652: delete_range
  status = db->DeleteRange(write_options, "key7715", "key9170");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 43653: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 43654: put
  db->Put(write_options, "key11015", "value105050");

  // op 43655: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 43656: put
  db->Put(write_options, "key5275", "value12424");

  // op 43657: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 43658: put
  db->Put(write_options, "key10828", "value1451");

  // op 43659: put
  db->Put(write_options, "key10838", "value38183");

  // op 43660: put
  db->Put(write_options, "key4369", "value70692");

  // op 43661: put
  db->Put(write_options, "key6902", "value114549");

  // op 43662: get
  status = db->Get(read_options, "key0589", &value);
  if (status.ok()) {
    out << "GET key0589 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43663: delete
  db->Delete(write_options, "key9348");

  // op 43664: delete
  db->Delete(write_options, "key3671");

  // op 43665: put
  db->Put(write_options, "key11254", "value35077");

  // op 43666: get
  status = db->Get(read_options, "key0814", &value);
  if (status.ok()) {
    out << "GET key0814 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43667: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8745", "key10464", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8745, key10464) size=" << scan_result.size() << std::endl;
  }

  // op 43668: get
  status = db->Get(read_options, "key7267", &value);
  if (status.ok()) {
    out << "GET key7267 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43669: delete_range
  status = db->DeleteRange(write_options, "key5493", "key7275");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 43670: get
  status = db->Get(read_options, "key3181", &value);
  if (status.ok()) {
    out << "GET key3181 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43671: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 43672: put
  db->Put(write_options, "key8751", "value114311");

  // op 43673: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3992", "key7351", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3992, key7351) size=" << scan_result.size() << std::endl;
  }

  // op 43674: get
  status = db->Get(read_options, "key6608", &value);
  if (status.ok()) {
    out << "GET key6608 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43675: get
  status = db->Get(read_options, "key5846", &value);
  if (status.ok()) {
    out << "GET key5846 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43676: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6553", "key8547", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6553, key8547) size=" << scan_result.size() << std::endl;
  }

  // op 43677: put
  db->Put(write_options, "key1725", "value16854");

  // op 43678: put
  db->Put(write_options, "key2575", "value104929");

  // op 43679: put
  db->Put(write_options, "key4792", "value97663");

  // op 43680: get
  status = db->Get(read_options, "key1664", &value);
  if (status.ok()) {
    out << "GET key1664 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43681: put
  db->Put(write_options, "key2471", "value26068");

  // op 43682: put
  db->Put(write_options, "key3490", "value8053");

  // op 43683: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6173", "key10962", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6173, key10962) size=" << scan_result.size() << std::endl;
  }

  // op 43684: delete_range
  status = db->DeleteRange(write_options, "key0652", "key2213");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 43685: get
  status = db->Get(read_options, "key5524", &value);
  if (status.ok()) {
    out << "GET key5524 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43686: delete
  db->Delete(write_options, "key11199");

  // op 43687: delete_range
  status = db->DeleteRange(write_options, "key1466", "key8127");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 43688: put
  db->Put(write_options, "key8417", "value52804");

  // op 43689: put
  db->Put(write_options, "key10022", "value96874");

  // op 43690: get
  status = db->Get(read_options, "key4134", &value);
  if (status.ok()) {
    out << "GET key4134 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43691: delete
  db->Delete(write_options, "key6947");

  // op 43692: put
  db->Put(write_options, "key4857", "value53249");

  // op 43693: get
  status = db->Get(read_options, "key2373", &value);
  if (status.ok()) {
    out << "GET key2373 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43694: get
  status = db->Get(read_options, "key5515", &value);
  if (status.ok()) {
    out << "GET key5515 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43695: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5775", "key6431", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5775, key6431) size=" << scan_result.size() << std::endl;
  }

  // op 43696: put
  db->Put(write_options, "key6182", "value70844");

  // op 43697: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 43698: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4804", "key7286", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4804, key7286) size=" << scan_result.size() << std::endl;
  }

  // op 43699: put
  db->Put(write_options, "key8764", "value82631");

  // op 43700: put
  db->Put(write_options, "key3835", "value84835");

  // op 43701: get
  status = db->Get(read_options, "key9253", &value);
  if (status.ok()) {
    out << "GET key9253 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43702: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 43703: get
  status = db->Get(read_options, "key6331", &value);
  if (status.ok()) {
    out << "GET key6331 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43704: put
  db->Put(write_options, "key10130", "value21176");

  // op 43705: put
  db->Put(write_options, "key3972", "value61336");

  // op 43706: delete
  db->Delete(write_options, "key2026");

  // op 43707: get
  status = db->Get(read_options, "key10022", &value);
  if (status.ok()) {
    out << "GET key10022 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43708: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1500", "key7883", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1500, key7883) size=" << scan_result.size() << std::endl;
  }

  // op 43709: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5697", "key7846", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5697, key7846) size=" << scan_result.size() << std::endl;
  }

  // op 43710: get
  status = db->Get(read_options, "key3067", &value);
  if (status.ok()) {
    out << "GET key3067 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43711: get
  status = db->Get(read_options, "key9390", &value);
  if (status.ok()) {
    out << "GET key9390 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43712: put
  db->Put(write_options, "key10012", "value90752");

  // op 43713: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0024", "key8131", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0024, key8131) size=" << scan_result.size() << std::endl;
  }

  // op 43714: delete
  db->Delete(write_options, "key6815");

  // op 43715: get
  status = db->Get(read_options, "key7945", &value);
  if (status.ok()) {
    out << "GET key7945 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43716: put
  db->Put(write_options, "key8948", "value78342");

  // op 43717: delete
  db->Delete(write_options, "key4538");

  // op 43718: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3618", "key10984", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3618, key10984) size=" << scan_result.size() << std::endl;
  }

  // op 43719: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 43720: put
  db->Put(write_options, "key3069", "value4255");

  // op 43721: delete_range
  status = db->DeleteRange(write_options, "key3237", "key9001");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 43722: delete
  db->Delete(write_options, "key6101");

  // op 43723: get
  status = db->Get(read_options, "key4599", &value);
  if (status.ok()) {
    out << "GET key4599 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43724: get
  status = db->Get(read_options, "key4496", &value);
  if (status.ok()) {
    out << "GET key4496 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43725: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2891", "key8768", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2891, key8768) size=" << scan_result.size() << std::endl;
  }

  // op 43726: put
  db->Put(write_options, "key7422", "value111199");

  // op 43727: delete
  db->Delete(write_options, "key5093");

  // op 43728: put
  db->Put(write_options, "key1759", "value9538");

  // op 43729: get
  status = db->Get(read_options, "key8546", &value);
  if (status.ok()) {
    out << "GET key8546 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43730: put
  db->Put(write_options, "key6796", "value64938");

  // op 43731: delete_range
  status = db->DeleteRange(write_options, "key4665", "key7731");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 43732: delete
  db->Delete(write_options, "key6989");

  // op 43733: get
  status = db->Get(read_options, "key7625", &value);
  if (status.ok()) {
    out << "GET key7625 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43734: put
  db->Put(write_options, "key8334", "value112321");

  // op 43735: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2807", "key9183", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2807, key9183) size=" << scan_result.size() << std::endl;
  }

  // op 43736: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7712", "key9138", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7712, key9138) size=" << scan_result.size() << std::endl;
  }

  // op 43737: put
  db->Put(write_options, "key7250", "value99903");

  // op 43738: delete
  db->Delete(write_options, "key5291");

  // op 43739: put
  db->Put(write_options, "key7612", "value108820");

  // op 43740: put
  db->Put(write_options, "key9228", "value97977");

  // op 43741: delete_range
  status = db->DeleteRange(write_options, "key0549", "key6054");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 43742: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7096", "key9199", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7096, key9199) size=" << scan_result.size() << std::endl;
  }

  // op 43743: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1628", "key6877", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1628, key6877) size=" << scan_result.size() << std::endl;
  }

  // op 43744: put
  db->Put(write_options, "key5489", "value12006");

  // op 43745: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 43746: delete_range
  status = db->DeleteRange(write_options, "key5065", "key8449");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 43747: get
  status = db->Get(read_options, "key8767", &value);
  if (status.ok()) {
    out << "GET key8767 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43748: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6106", "key8419", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6106, key8419) size=" << scan_result.size() << std::endl;
  }

  // op 43749: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2161", "key3119", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2161, key3119) size=" << scan_result.size() << std::endl;
  }

  // op 43750: put
  db->Put(write_options, "key5921", "value5098");

  // op 43751: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2956", "key8378", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2956, key8378) size=" << scan_result.size() << std::endl;
  }

  // op 43752: get
  status = db->Get(read_options, "key5381", &value);
  if (status.ok()) {
    out << "GET key5381 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43753: delete_range
  status = db->DeleteRange(write_options, "key3328", "key8445");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 43754: put
  db->Put(write_options, "key8282", "value44894");

  // op 43755: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 43756: delete_range
  status = db->DeleteRange(write_options, "key3542", "key5306");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 43757: put
  db->Put(write_options, "key7318", "value91723");

  // op 43758: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 43759: get
  status = db->Get(read_options, "key3835", &value);
  if (status.ok()) {
    out << "GET key3835 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43760: put
  db->Put(write_options, "key11659", "value84346");

  // op 43761: put
  db->Put(write_options, "key6792", "value32578");

  // op 43762: put
  db->Put(write_options, "key0993", "value82351");

  // op 43763: put
  db->Put(write_options, "key10366", "value59089");

  // op 43764: put
  db->Put(write_options, "key3773", "value75013");

  // op 43765: delete_range
  status = db->DeleteRange(write_options, "key4278", "key9804");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 43766: get
  status = db->Get(read_options, "key10069", &value);
  if (status.ok()) {
    out << "GET key10069 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43767: delete_range
  status = db->DeleteRange(write_options, "key1644", "key8839");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 43768: get
  status = db->Get(read_options, "key6076", &value);
  if (status.ok()) {
    out << "GET key6076 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43769: get
  status = db->Get(read_options, "key0607", &value);
  if (status.ok()) {
    out << "GET key0607 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43770: put
  db->Put(write_options, "key8915", "value33516");

  // op 43771: get
  status = db->Get(read_options, "key2175", &value);
  if (status.ok()) {
    out << "GET key2175 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43772: delete
  db->Delete(write_options, "key2138");

  // op 43773: get
  status = db->Get(read_options, "key9841", &value);
  if (status.ok()) {
    out << "GET key9841 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43774: delete
  db->Delete(write_options, "key4775");

  // op 43775: put
  db->Put(write_options, "key3105", "value104364");

  // op 43776: put
  db->Put(write_options, "key4122", "value22613");

  // op 43777: put
  db->Put(write_options, "key1114", "value114262");

  // op 43778: put
  db->Put(write_options, "key1295", "value106324");

  // op 43779: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0886", "key1787", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0886, key1787) size=" << scan_result.size() << std::endl;
  }

  // op 43780: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1391", "key1593", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1391, key1593) size=" << scan_result.size() << std::endl;
  }

  // op 43781: put
  db->Put(write_options, "key8780", "value98750");

  // op 43782: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 43783: put
  db->Put(write_options, "key8114", "value44684");

  // op 43784: put
  db->Put(write_options, "key5276", "value81972");

  // op 43785: put
  db->Put(write_options, "key8734", "value88885");

  // op 43786: put
  db->Put(write_options, "key5645", "value88173");

  // op 43787: get
  status = db->Get(read_options, "key5178", &value);
  if (status.ok()) {
    out << "GET key5178 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43788: put
  db->Put(write_options, "key0885", "value61144");

  // op 43789: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5235", "key9998", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5235, key9998) size=" << scan_result.size() << std::endl;
  }

  // op 43790: get
  status = db->Get(read_options, "key11153", &value);
  if (status.ok()) {
    out << "GET key11153 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43791: put
  db->Put(write_options, "key7503", "value86305");

  // op 43792: put
  db->Put(write_options, "key4044", "value17482");

  // op 43793: get
  status = db->Get(read_options, "key3342", &value);
  if (status.ok()) {
    out << "GET key3342 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43794: put
  db->Put(write_options, "key0791", "value78798");

  // op 43795: put
  db->Put(write_options, "key4999", "value117405");

  // op 43796: delete
  db->Delete(write_options, "key3157");

  // op 43797: delete
  db->Delete(write_options, "key4698");

  // op 43798: get
  status = db->Get(read_options, "key0395", &value);
  if (status.ok()) {
    out << "GET key0395 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43799: put
  db->Put(write_options, "key7123", "value9516");

  // op 43800: put
  db->Put(write_options, "key6396", "value116528");

  // op 43801: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9768", "key10616", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9768, key10616) size=" << scan_result.size() << std::endl;
  }

  // op 43802: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 43803: get
  status = db->Get(read_options, "key1028", &value);
  if (status.ok()) {
    out << "GET key1028 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43804: put
  db->Put(write_options, "key7871", "value98196");

  // op 43805: put
  db->Put(write_options, "key11526", "value69907");

  // op 43806: put
  db->Put(write_options, "key1647", "value77549");

  // op 43807: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 43808: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2824", "key4517", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2824, key4517) size=" << scan_result.size() << std::endl;
  }

  // op 43809: put
  db->Put(write_options, "key6403", "value34620");

  // op 43810: get
  status = db->Get(read_options, "key0395", &value);
  if (status.ok()) {
    out << "GET key0395 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43811: delete
  db->Delete(write_options, "key5795");

  // op 43812: put
  db->Put(write_options, "key0586", "value36542");

  // op 43813: get
  status = db->Get(read_options, "key2972", &value);
  if (status.ok()) {
    out << "GET key2972 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43814: put
  db->Put(write_options, "key5047", "value49595");

  // op 43815: put
  db->Put(write_options, "key4749", "value32475");

  // op 43816: put
  db->Put(write_options, "key8463", "value83805");

  // op 43817: put
  db->Put(write_options, "key5002", "value6051");

  // op 43818: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5079", "key9807", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5079, key9807) size=" << scan_result.size() << std::endl;
  }

  // op 43819: put
  db->Put(write_options, "key7709", "value60375");

  // op 43820: get
  status = db->Get(read_options, "key8357", &value);
  if (status.ok()) {
    out << "GET key8357 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43821: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 43822: get
  status = db->Get(read_options, "key5964", &value);
  if (status.ok()) {
    out << "GET key5964 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43823: put
  db->Put(write_options, "key1270", "value12043");

  // op 43824: delete
  db->Delete(write_options, "key3649");

  // op 43825: get
  status = db->Get(read_options, "key8850", &value);
  if (status.ok()) {
    out << "GET key8850 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43826: delete_range
  status = db->DeleteRange(write_options, "key5590", "key11484");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 43827: delete
  db->Delete(write_options, "key4213");

  // op 43828: get
  status = db->Get(read_options, "key7727", &value);
  if (status.ok()) {
    out << "GET key7727 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43829: delete
  db->Delete(write_options, "key8347");

  // op 43830: get
  status = db->Get(read_options, "key4614", &value);
  if (status.ok()) {
    out << "GET key4614 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43831: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1149", "key4763", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1149, key4763) size=" << scan_result.size() << std::endl;
  }

  // op 43832: delete_range
  status = db->DeleteRange(write_options, "key7447", "key9665");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 43833: put
  db->Put(write_options, "key10167", "value114666");

  // op 43834: put
  db->Put(write_options, "key4794", "value53066");

  // op 43835: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4990", "key11275", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4990, key11275) size=" << scan_result.size() << std::endl;
  }

  // op 43836: delete_range
  status = db->DeleteRange(write_options, "key6424", "key8461");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 43837: put
  db->Put(write_options, "key8332", "value25927");

  // op 43838: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1944", "key3348", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1944, key3348) size=" << scan_result.size() << std::endl;
  }

  // op 43839: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5138", "key8451", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5138, key8451) size=" << scan_result.size() << std::endl;
  }

  // op 43840: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 43841: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1287", "key6889", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1287, key6889) size=" << scan_result.size() << std::endl;
  }

  // op 43842: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0078", "key7953", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0078, key7953) size=" << scan_result.size() << std::endl;
  }

  // op 43843: put
  db->Put(write_options, "key6909", "value51621");

  // op 43844: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1063", "key5566", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1063, key5566) size=" << scan_result.size() << std::endl;
  }

  // op 43845: put
  db->Put(write_options, "key7559", "value79786");

  // op 43846: put
  db->Put(write_options, "key5242", "value38264");

  // op 43847: put
  db->Put(write_options, "key3786", "value32596");

  // op 43848: get
  status = db->Get(read_options, "key1988", &value);
  if (status.ok()) {
    out << "GET key1988 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43849: delete_range
  status = db->DeleteRange(write_options, "key2702", "key10840");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 43850: put
  db->Put(write_options, "key5394", "value39475");

  // op 43851: delete
  db->Delete(write_options, "key2370");

  // op 43852: delete_range
  status = db->DeleteRange(write_options, "key0717", "key8130");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 43853: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0620", "key8952", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0620, key8952) size=" << scan_result.size() << std::endl;
  }

  // op 43854: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 43855: put
  db->Put(write_options, "key3322", "value11270");

  // op 43856: put
  db->Put(write_options, "key11355", "value52502");

  // op 43857: get
  status = db->Get(read_options, "key0323", &value);
  if (status.ok()) {
    out << "GET key0323 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43858: get
  status = db->Get(read_options, "key2398", &value);
  if (status.ok()) {
    out << "GET key2398 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43859: put
  db->Put(write_options, "key7079", "value35491");

  // op 43860: delete
  db->Delete(write_options, "key3546");

  // op 43861: delete_range
  status = db->DeleteRange(write_options, "key4787", "key5592");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 43862: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1685", "key3261", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1685, key3261) size=" << scan_result.size() << std::endl;
  }

  // op 43863: get
  status = db->Get(read_options, "key10918", &value);
  if (status.ok()) {
    out << "GET key10918 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43864: get
  status = db->Get(read_options, "key3296", &value);
  if (status.ok()) {
    out << "GET key3296 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43865: put
  db->Put(write_options, "key9699", "value12464");

  // op 43866: put
  db->Put(write_options, "key3441", "value18200");

  // op 43867: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0326", "key7997", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0326, key7997) size=" << scan_result.size() << std::endl;
  }

  // op 43868: put
  db->Put(write_options, "key7261", "value65142");

  // op 43869: put
  db->Put(write_options, "key0510", "value99753");

  // op 43870: put
  db->Put(write_options, "key7111", "value97898");

  // op 43871: delete_range
  status = db->DeleteRange(write_options, "key0447", "key10689");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 43872: delete_range
  status = db->DeleteRange(write_options, "key0884", "key8840");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 43873: delete_range
  status = db->DeleteRange(write_options, "key6053", "key10630");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 43874: get
  status = db->Get(read_options, "key1943", &value);
  if (status.ok()) {
    out << "GET key1943 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43875: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0563", "key4859", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0563, key4859) size=" << scan_result.size() << std::endl;
  }

  // op 43876: get
  status = db->Get(read_options, "key0874", &value);
  if (status.ok()) {
    out << "GET key0874 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43877: delete
  db->Delete(write_options, "key4218");

  // op 43878: get
  status = db->Get(read_options, "key0840", &value);
  if (status.ok()) {
    out << "GET key0840 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43879: put
  db->Put(write_options, "key6237", "value4666");

  // op 43880: put
  db->Put(write_options, "key7571", "value73864");

  // op 43881: put
  db->Put(write_options, "key9984", "value116647");

  // op 43882: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 43883: get
  status = db->Get(read_options, "key10164", &value);
  if (status.ok()) {
    out << "GET key10164 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43884: put
  db->Put(write_options, "key2634", "value74668");

  // op 43885: put
  db->Put(write_options, "key9862", "value39495");

  // op 43886: put
  db->Put(write_options, "key8519", "value68497");

  // op 43887: put
  db->Put(write_options, "key8251", "value114558");

  // op 43888: delete_range
  status = db->DeleteRange(write_options, "key8759", "key11229");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 43889: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5474", "key11602", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5474, key11602) size=" << scan_result.size() << std::endl;
  }

  // op 43890: put
  db->Put(write_options, "key4004", "value32461");

  // op 43891: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 43892: delete
  db->Delete(write_options, "key2673");

  // op 43893: put
  db->Put(write_options, "key2553", "value7323");

  // op 43894: put
  db->Put(write_options, "key3855", "value111855");

  // op 43895: put
  db->Put(write_options, "key1493", "value97099");

  // op 43896: delete
  db->Delete(write_options, "key2965");

  // op 43897: delete
  db->Delete(write_options, "key9744");

  // op 43898: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4034", "key8941", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4034, key8941) size=" << scan_result.size() << std::endl;
  }

  // op 43899: put
  db->Put(write_options, "key2927", "value37254");

  // op 43900: put
  db->Put(write_options, "key3207", "value66347");

  // op 43901: put
  db->Put(write_options, "key1530", "value64809");

  // op 43902: delete
  db->Delete(write_options, "key4686");

  // op 43903: put
  db->Put(write_options, "key2307", "value76890");

  // op 43904: delete_range
  status = db->DeleteRange(write_options, "key0793", "key4177");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 43905: delete
  db->Delete(write_options, "key1256");

  // op 43906: delete
  db->Delete(write_options, "key9496");

  // op 43907: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0920", "key2798", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0920, key2798) size=" << scan_result.size() << std::endl;
  }

  // op 43908: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 43909: get
  status = db->Get(read_options, "key6144", &value);
  if (status.ok()) {
    out << "GET key6144 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43910: get
  status = db->Get(read_options, "key4452", &value);
  if (status.ok()) {
    out << "GET key4452 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43911: get
  status = db->Get(read_options, "key7196", &value);
  if (status.ok()) {
    out << "GET key7196 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43912: put
  db->Put(write_options, "key6648", "value57753");

  // op 43913: put
  db->Put(write_options, "key0564", "value20686");

  // op 43914: delete
  db->Delete(write_options, "key10571");

  // op 43915: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0133", "key4075", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0133, key4075) size=" << scan_result.size() << std::endl;
  }

  // op 43916: put
  db->Put(write_options, "key1314", "value68023");

  // op 43917: put
  db->Put(write_options, "key1564", "value73155");

  // op 43918: put
  db->Put(write_options, "key9749", "value85337");

  // op 43919: get
  status = db->Get(read_options, "key6313", &value);
  if (status.ok()) {
    out << "GET key6313 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43920: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0838", "key3811", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0838, key3811) size=" << scan_result.size() << std::endl;
  }

  // op 43921: put
  db->Put(write_options, "key8971", "value18682");

  // op 43922: put
  db->Put(write_options, "key2482", "value12386");

  // op 43923: put
  db->Put(write_options, "key8362", "value1328");

  // op 43924: put
  db->Put(write_options, "key5223", "value40026");

  // op 43925: get
  status = db->Get(read_options, "key9845", &value);
  if (status.ok()) {
    out << "GET key9845 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43926: get
  status = db->Get(read_options, "key10571", &value);
  if (status.ok()) {
    out << "GET key10571 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43927: get
  status = db->Get(read_options, "key7280", &value);
  if (status.ok()) {
    out << "GET key7280 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43928: delete_range
  status = db->DeleteRange(write_options, "key4546", "key9134");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 43929: put
  db->Put(write_options, "key8228", "value58263");

  // op 43930: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4597", "key5236", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4597, key5236) size=" << scan_result.size() << std::endl;
  }

  // op 43931: get
  status = db->Get(read_options, "key8510", &value);
  if (status.ok()) {
    out << "GET key8510 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43932: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3965", "key6416", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3965, key6416) size=" << scan_result.size() << std::endl;
  }

  // op 43933: get
  status = db->Get(read_options, "key2557", &value);
  if (status.ok()) {
    out << "GET key2557 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43934: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1634", "key3168", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1634, key3168) size=" << scan_result.size() << std::endl;
  }

  // op 43935: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1392", "key2522", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1392, key2522) size=" << scan_result.size() << std::endl;
  }

  // op 43936: delete
  db->Delete(write_options, "key0706");

  // op 43937: get
  status = db->Get(read_options, "key0842", &value);
  if (status.ok()) {
    out << "GET key0842 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43938: delete
  db->Delete(write_options, "key6343");

  // op 43939: get
  status = db->Get(read_options, "key7860", &value);
  if (status.ok()) {
    out << "GET key7860 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43940: get
  status = db->Get(read_options, "key5125", &value);
  if (status.ok()) {
    out << "GET key5125 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43941: get
  status = db->Get(read_options, "key8723", &value);
  if (status.ok()) {
    out << "GET key8723 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43942: delete_range
  status = db->DeleteRange(write_options, "key3854", "key10820");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 43943: delete_range
  status = db->DeleteRange(write_options, "key3988", "key4549");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 43944: delete_range
  status = db->DeleteRange(write_options, "key1947", "key4200");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 43945: put
  db->Put(write_options, "key6809", "value37152");

  // op 43946: get
  status = db->Get(read_options, "key8159", &value);
  if (status.ok()) {
    out << "GET key8159 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43947: put
  db->Put(write_options, "key3888", "value61576");

  // op 43948: delete
  db->Delete(write_options, "key8858");

  // op 43949: delete_range
  status = db->DeleteRange(write_options, "key5921", "key8571");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 43950: put
  db->Put(write_options, "key7227", "value14025");

  // op 43951: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5325", "key6198", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5325, key6198) size=" << scan_result.size() << std::endl;
  }

  // op 43952: put
  db->Put(write_options, "key3136", "value74050");

  // op 43953: delete_range
  status = db->DeleteRange(write_options, "key1396", "key7273");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 43954: get
  status = db->Get(read_options, "key11526", &value);
  if (status.ok()) {
    out << "GET key11526 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43955: get
  status = db->Get(read_options, "key1454", &value);
  if (status.ok()) {
    out << "GET key1454 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43956: delete
  db->Delete(write_options, "key3741");

  // op 43957: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 43958: delete_range
  status = db->DeleteRange(write_options, "key7362", "key10097");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 43959: get
  status = db->Get(read_options, "key11433", &value);
  if (status.ok()) {
    out << "GET key11433 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43960: put
  db->Put(write_options, "key3752", "value33048");

  // op 43961: put
  db->Put(write_options, "key1928", "value42787");

  // op 43962: put
  db->Put(write_options, "key8078", "value67706");

  // op 43963: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1570", "key6711", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1570, key6711) size=" << scan_result.size() << std::endl;
  }

  // op 43964: delete_range
  status = db->DeleteRange(write_options, "key2007", "key5513");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 43965: get
  status = db->Get(read_options, "key2111", &value);
  if (status.ok()) {
    out << "GET key2111 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43966: put
  db->Put(write_options, "key2229", "value76318");

  // op 43967: put
  db->Put(write_options, "key6472", "value69188");

  // op 43968: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0251", "key9265", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0251, key9265) size=" << scan_result.size() << std::endl;
  }

  // op 43969: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6652", "key10671", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6652, key10671) size=" << scan_result.size() << std::endl;
  }

  // op 43970: delete
  db->Delete(write_options, "key5206");

  // op 43971: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0929", "key3230", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0929, key3230) size=" << scan_result.size() << std::endl;
  }

  // op 43972: put
  db->Put(write_options, "key9688", "value112194");

  // op 43973: delete_range
  status = db->DeleteRange(write_options, "key9135", "key10571");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 43974: get
  status = db->Get(read_options, "key6677", &value);
  if (status.ok()) {
    out << "GET key6677 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43975: get
  status = db->Get(read_options, "key9436", &value);
  if (status.ok()) {
    out << "GET key9436 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43976: delete
  db->Delete(write_options, "key6031");

  // op 43977: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2415", "key5978", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2415, key5978) size=" << scan_result.size() << std::endl;
  }

  // op 43978: put
  db->Put(write_options, "key9543", "value46858");

  // op 43979: delete
  db->Delete(write_options, "key10499");

  // op 43980: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2204", "key10401", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2204, key10401) size=" << scan_result.size() << std::endl;
  }

  // op 43981: get
  status = db->Get(read_options, "key10611", &value);
  if (status.ok()) {
    out << "GET key10611 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43982: delete_range
  status = db->DeleteRange(write_options, "key4190", "key9991");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 43983: put
  db->Put(write_options, "key5890", "value74568");

  // op 43984: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9931", "key11451", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9931, key11451) size=" << scan_result.size() << std::endl;
  }

  // op 43985: put
  db->Put(write_options, "key10608", "value51194");

  // op 43986: put
  db->Put(write_options, "key10310", "value24928");

  // op 43987: put
  db->Put(write_options, "key4588", "value106382");

  // op 43988: put
  db->Put(write_options, "key6934", "value62323");

  // op 43989: put
  db->Put(write_options, "key1625", "value48774");

  // op 43990: get
  status = db->Get(read_options, "key1740", &value);
  if (status.ok()) {
    out << "GET key1740 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43991: put
  db->Put(write_options, "key9087", "value36021");

  // op 43992: put
  db->Put(write_options, "key1732", "value53355");

  // op 43993: get
  status = db->Get(read_options, "key9991", &value);
  if (status.ok()) {
    out << "GET key9991 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43994: put
  db->Put(write_options, "key4477", "value88447");

  // op 43995: put
  db->Put(write_options, "key7531", "value0812");

  // op 43996: get
  status = db->Get(read_options, "key1898", &value);
  if (status.ok()) {
    out << "GET key1898 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43997: get
  status = db->Get(read_options, "key9737", &value);
  if (status.ok()) {
    out << "GET key9737 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 43998: put
  db->Put(write_options, "key6764", "value67661");

  // op 43999: put
  db->Put(write_options, "key11418", "value115382");

  // op 44000: get
  status = db->Get(read_options, "key3485", &value);
  if (status.ok()) {
    out << "GET key3485 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44001: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3680", "key10104", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3680, key10104) size=" << scan_result.size() << std::endl;
  }

  // op 44002: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 44003: get
  status = db->Get(read_options, "key7494", &value);
  if (status.ok()) {
    out << "GET key7494 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44004: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4366", "key10060", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4366, key10060) size=" << scan_result.size() << std::endl;
  }

  // op 44005: put
  db->Put(write_options, "key1747", "value5772");

  // op 44006: put
  db->Put(write_options, "key4220", "value115638");

  // op 44007: delete_range
  status = db->DeleteRange(write_options, "key8840", "key10882");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 44008: put
  db->Put(write_options, "key5343", "value78482");

  // op 44009: put
  db->Put(write_options, "key4372", "value30725");

  // op 44010: delete_range
  status = db->DeleteRange(write_options, "key5409", "key7193");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 44011: get
  status = db->Get(read_options, "key2682", &value);
  if (status.ok()) {
    out << "GET key2682 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44012: get
  status = db->Get(read_options, "key6425", &value);
  if (status.ok()) {
    out << "GET key6425 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44013: put
  db->Put(write_options, "key9074", "value82736");

  // op 44014: delete_range
  status = db->DeleteRange(write_options, "key3350", "key7341");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 44015: delete
  db->Delete(write_options, "key1441");

  // op 44016: put
  db->Put(write_options, "key1254", "value62180");

  // op 44017: get
  status = db->Get(read_options, "key1112", &value);
  if (status.ok()) {
    out << "GET key1112 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44018: put
  db->Put(write_options, "key6264", "value26233");

  // op 44019: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1778", "key9950", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1778, key9950) size=" << scan_result.size() << std::endl;
  }

  // op 44020: get
  status = db->Get(read_options, "key7988", &value);
  if (status.ok()) {
    out << "GET key7988 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44021: delete
  db->Delete(write_options, "key8393");

  // op 44022: put
  db->Put(write_options, "key1875", "value88530");

  // op 44023: get
  status = db->Get(read_options, "key4752", &value);
  if (status.ok()) {
    out << "GET key4752 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44024: delete_range
  status = db->DeleteRange(write_options, "key9183", "key10047");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 44025: delete_range
  status = db->DeleteRange(write_options, "key4195", "key6532");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 44026: delete_range
  status = db->DeleteRange(write_options, "key3981", "key5038");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 44027: put
  db->Put(write_options, "key5534", "value91377");

  // op 44028: get
  status = db->Get(read_options, "key2465", &value);
  if (status.ok()) {
    out << "GET key2465 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44029: put
  db->Put(write_options, "key6039", "value93442");

  // op 44030: get
  status = db->Get(read_options, "key0667", &value);
  if (status.ok()) {
    out << "GET key0667 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44031: get
  status = db->Get(read_options, "key7836", &value);
  if (status.ok()) {
    out << "GET key7836 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44032: put
  db->Put(write_options, "key1399", "value43214");

  // op 44033: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2800", "key10811", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2800, key10811) size=" << scan_result.size() << std::endl;
  }

  // op 44034: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 44035: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 44036: get
  status = db->Get(read_options, "key10697", &value);
  if (status.ok()) {
    out << "GET key10697 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44037: delete
  db->Delete(write_options, "key9910");

  // op 44038: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1608", "key6206", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1608, key6206) size=" << scan_result.size() << std::endl;
  }

  // op 44039: delete_range
  status = db->DeleteRange(write_options, "key6959", "key10141");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 44040: delete_range
  status = db->DeleteRange(write_options, "key1731", "key9985");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 44041: delete_range
  status = db->DeleteRange(write_options, "key6459", "key10464");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 44042: put
  db->Put(write_options, "key5507", "value80826");

  // op 44043: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8641", "key9021", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8641, key9021) size=" << scan_result.size() << std::endl;
  }

  // op 44044: delete
  db->Delete(write_options, "key2292");

  // op 44045: get
  status = db->Get(read_options, "key6790", &value);
  if (status.ok()) {
    out << "GET key6790 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44046: put
  db->Put(write_options, "key6505", "value90670");

  // op 44047: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0099", "key2427", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0099, key2427) size=" << scan_result.size() << std::endl;
  }

  // op 44048: get
  status = db->Get(read_options, "key6231", &value);
  if (status.ok()) {
    out << "GET key6231 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44049: put
  db->Put(write_options, "key5408", "value37783");

  // op 44050: put
  db->Put(write_options, "key1911", "value14474");

  // op 44051: delete_range
  status = db->DeleteRange(write_options, "key2911", "key5434");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 44052: put
  db->Put(write_options, "key7069", "value66741");

  // op 44053: put
  db->Put(write_options, "key9686", "value71339");

  // op 44054: delete_range
  status = db->DeleteRange(write_options, "key5969", "key6601");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 44055: put
  db->Put(write_options, "key8516", "value46052");

  // op 44056: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1070", "key5933", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1070, key5933) size=" << scan_result.size() << std::endl;
  }

  // op 44057: put
  db->Put(write_options, "key3656", "value74231");

  // op 44058: put
  db->Put(write_options, "key3412", "value66935");

  // op 44059: delete
  db->Delete(write_options, "key3596");

  // op 44060: get
  status = db->Get(read_options, "key6979", &value);
  if (status.ok()) {
    out << "GET key6979 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44061: put
  db->Put(write_options, "key0230", "value3662");

  // op 44062: put
  db->Put(write_options, "key3545", "value78231");

  // op 44063: delete_range
  status = db->DeleteRange(write_options, "key1925", "key6233");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 44064: put
  db->Put(write_options, "key7549", "value33165");

  // op 44065: put
  db->Put(write_options, "key3352", "value114086");

  // op 44066: get
  status = db->Get(read_options, "key6533", &value);
  if (status.ok()) {
    out << "GET key6533 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44067: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6450", "key6624", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6450, key6624) size=" << scan_result.size() << std::endl;
  }

  // op 44068: delete
  db->Delete(write_options, "key8194");

  // op 44069: put
  db->Put(write_options, "key7414", "value15562");

  // op 44070: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5374", "key10158", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5374, key10158) size=" << scan_result.size() << std::endl;
  }

  // op 44071: delete
  db->Delete(write_options, "key5943");

  // op 44072: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0522", "key7165", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0522, key7165) size=" << scan_result.size() << std::endl;
  }

  // op 44073: delete
  db->Delete(write_options, "key7978");

  // op 44074: put
  db->Put(write_options, "key7874", "value92996");

  // op 44075: delete
  db->Delete(write_options, "key11076");

  // op 44076: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9457", "key11727", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9457, key11727) size=" << scan_result.size() << std::endl;
  }

  // op 44077: put
  db->Put(write_options, "key6596", "value44449");

  // op 44078: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10137", "key10474", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10137, key10474) size=" << scan_result.size() << std::endl;
  }

  // op 44079: get
  status = db->Get(read_options, "key8452", &value);
  if (status.ok()) {
    out << "GET key8452 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44080: delete_range
  status = db->DeleteRange(write_options, "key9308", "key10837");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 44081: delete
  db->Delete(write_options, "key9585");

  // op 44082: put
  db->Put(write_options, "key5725", "value11299");

  // op 44083: delete
  db->Delete(write_options, "key0644");

  // op 44084: put
  db->Put(write_options, "key9626", "value39949");

  // op 44085: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8551", "key8931", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8551, key8931) size=" << scan_result.size() << std::endl;
  }

  // op 44086: put
  db->Put(write_options, "key11013", "value11243");

  // op 44087: put
  db->Put(write_options, "key7733", "value61016");

  // op 44088: get
  status = db->Get(read_options, "key11670", &value);
  if (status.ok()) {
    out << "GET key11670 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44089: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 44090: get
  status = db->Get(read_options, "key0842", &value);
  if (status.ok()) {
    out << "GET key0842 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44091: get
  status = db->Get(read_options, "key6968", &value);
  if (status.ok()) {
    out << "GET key6968 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44092: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10815", "key11379", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10815, key11379) size=" << scan_result.size() << std::endl;
  }

  // op 44093: put
  db->Put(write_options, "key8879", "value42636");

  // op 44094: put
  db->Put(write_options, "key5382", "value86430");

  // op 44095: get
  status = db->Get(read_options, "key3827", &value);
  if (status.ok()) {
    out << "GET key3827 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44096: get
  status = db->Get(read_options, "key6292", &value);
  if (status.ok()) {
    out << "GET key6292 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44097: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2204", "key11186", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2204, key11186) size=" << scan_result.size() << std::endl;
  }

  // op 44098: get
  status = db->Get(read_options, "key5923", &value);
  if (status.ok()) {
    out << "GET key5923 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44099: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2183", "key4620", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2183, key4620) size=" << scan_result.size() << std::endl;
  }

  // op 44100: delete_range
  status = db->DeleteRange(write_options, "key3472", "key5394");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 44101: put
  db->Put(write_options, "key2291", "value40761");

  // op 44102: put
  db->Put(write_options, "key5575", "value30101");

  // op 44103: get
  status = db->Get(read_options, "key2314", &value);
  if (status.ok()) {
    out << "GET key2314 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44104: put
  db->Put(write_options, "key2390", "value11363");

  // op 44105: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2202", "key9410", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2202, key9410) size=" << scan_result.size() << std::endl;
  }

  // op 44106: get
  status = db->Get(read_options, "key3190", &value);
  if (status.ok()) {
    out << "GET key3190 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44107: put
  db->Put(write_options, "key5393", "value96945");

  // op 44108: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7439", "key7907", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7439, key7907) size=" << scan_result.size() << std::endl;
  }

  // op 44109: put
  db->Put(write_options, "key2924", "value105023");

  // op 44110: get
  status = db->Get(read_options, "key1796", &value);
  if (status.ok()) {
    out << "GET key1796 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44111: get
  status = db->Get(read_options, "key9914", &value);
  if (status.ok()) {
    out << "GET key9914 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44112: delete_range
  status = db->DeleteRange(write_options, "key7231", "key7945");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 44113: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0853", "key4512", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0853, key4512) size=" << scan_result.size() << std::endl;
  }

  // op 44114: put
  db->Put(write_options, "key11101", "value35473");

  // op 44115: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1928", "key10942", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1928, key10942) size=" << scan_result.size() << std::endl;
  }

  // op 44116: delete_range
  status = db->DeleteRange(write_options, "key1272", "key10844");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 44117: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2601", "key5560", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2601, key5560) size=" << scan_result.size() << std::endl;
  }

  // op 44118: get
  status = db->Get(read_options, "key4381", &value);
  if (status.ok()) {
    out << "GET key4381 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44119: put
  db->Put(write_options, "key9211", "value48527");

  // op 44120: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1647", "key2217", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1647, key2217) size=" << scan_result.size() << std::endl;
  }

  // op 44121: delete
  db->Delete(write_options, "key3738");

  // op 44122: get
  status = db->Get(read_options, "key8390", &value);
  if (status.ok()) {
    out << "GET key8390 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44123: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1884", "key7970", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1884, key7970) size=" << scan_result.size() << std::endl;
  }

  // op 44124: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1454", "key9619", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1454, key9619) size=" << scan_result.size() << std::endl;
  }

  // op 44125: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3701", "key5768", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3701, key5768) size=" << scan_result.size() << std::endl;
  }

  // op 44126: put
  db->Put(write_options, "key2447", "value49189");

  // op 44127: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 44128: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3126", "key7880", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3126, key7880) size=" << scan_result.size() << std::endl;
  }

  // op 44129: get
  status = db->Get(read_options, "key4630", &value);
  if (status.ok()) {
    out << "GET key4630 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44130: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2505", "key11323", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2505, key11323) size=" << scan_result.size() << std::endl;
  }

  // op 44131: delete
  db->Delete(write_options, "key10305");

  // op 44132: delete_range
  status = db->DeleteRange(write_options, "key5187", "key10419");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 44133: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2460", "key10872", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2460, key10872) size=" << scan_result.size() << std::endl;
  }

  // op 44134: get
  status = db->Get(read_options, "key6274", &value);
  if (status.ok()) {
    out << "GET key6274 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44135: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 44136: get
  status = db->Get(read_options, "key5556", &value);
  if (status.ok()) {
    out << "GET key5556 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44137: get
  status = db->Get(read_options, "key2819", &value);
  if (status.ok()) {
    out << "GET key2819 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44138: put
  db->Put(write_options, "key1530", "value89674");

  // op 44139: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3044", "key4340", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3044, key4340) size=" << scan_result.size() << std::endl;
  }

  // op 44140: put
  db->Put(write_options, "key9263", "value7289");

  // op 44141: delete_range
  status = db->DeleteRange(write_options, "key5944", "key10770");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 44142: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1247", "key6438", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1247, key6438) size=" << scan_result.size() << std::endl;
  }

  // op 44143: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7397", "key11392", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7397, key11392) size=" << scan_result.size() << std::endl;
  }

  // op 44144: put
  db->Put(write_options, "key11466", "value19695");

  // op 44145: put
  db->Put(write_options, "key8292", "value76884");

  // op 44146: delete
  db->Delete(write_options, "key0287");

  // op 44147: put
  db->Put(write_options, "key0818", "value49912");

  // op 44148: put
  db->Put(write_options, "key1579", "value80407");

  // op 44149: put
  db->Put(write_options, "key0403", "value111762");

  // op 44150: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3428", "key8235", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3428, key8235) size=" << scan_result.size() << std::endl;
  }

  // op 44151: delete
  db->Delete(write_options, "key1265");

  // op 44152: put
  db->Put(write_options, "key8926", "value4537");

  // op 44153: put
  db->Put(write_options, "key1244", "value23679");

  // op 44154: put
  db->Put(write_options, "key2662", "value91240");

  // op 44155: put
  db->Put(write_options, "key7151", "value25578");

  // op 44156: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2884", "key4715", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2884, key4715) size=" << scan_result.size() << std::endl;
  }

  // op 44157: delete_range
  status = db->DeleteRange(write_options, "key6180", "key7992");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 44158: put
  db->Put(write_options, "key3067", "value83076");

  // op 44159: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 44160: put
  db->Put(write_options, "key4248", "value2247");

  // op 44161: put
  db->Put(write_options, "key3411", "value100073");

  // op 44162: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3704", "key9496", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3704, key9496) size=" << scan_result.size() << std::endl;
  }

  // op 44163: get
  status = db->Get(read_options, "key5457", &value);
  if (status.ok()) {
    out << "GET key5457 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44164: get
  status = db->Get(read_options, "key0674", &value);
  if (status.ok()) {
    out << "GET key0674 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44165: put
  db->Put(write_options, "key3281", "value116422");

  // op 44166: get
  status = db->Get(read_options, "key10452", &value);
  if (status.ok()) {
    out << "GET key10452 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44167: delete_range
  status = db->DeleteRange(write_options, "key4760", "key10811");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 44168: put
  db->Put(write_options, "key6763", "value46196");

  // op 44169: get
  status = db->Get(read_options, "key2530", &value);
  if (status.ok()) {
    out << "GET key2530 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44170: put
  db->Put(write_options, "key9442", "value15399");

  // op 44171: put
  db->Put(write_options, "key8050", "value94091");

  // op 44172: get
  status = db->Get(read_options, "key7593", &value);
  if (status.ok()) {
    out << "GET key7593 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44173: put
  db->Put(write_options, "key1407", "value19979");

  // op 44174: put
  db->Put(write_options, "key9086", "value9264");

  // op 44175: put
  db->Put(write_options, "key3051", "value41260");

  // op 44176: put
  db->Put(write_options, "key1695", "value72225");

  // op 44177: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5536", "key11522", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5536, key11522) size=" << scan_result.size() << std::endl;
  }

  // op 44178: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 44179: put
  db->Put(write_options, "key7489", "value84645");

  // op 44180: put
  db->Put(write_options, "key0639", "value75797");

  // op 44181: put
  db->Put(write_options, "key2220", "value51247");

  // op 44182: put
  db->Put(write_options, "key8717", "value36829");

  // op 44183: put
  db->Put(write_options, "key3775", "value36383");

  // op 44184: put
  db->Put(write_options, "key8214", "value30996");

  // op 44185: put
  db->Put(write_options, "key5000", "value43222");

  // op 44186: put
  db->Put(write_options, "key4833", "value38755");

  // op 44187: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1137", "key3930", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1137, key3930) size=" << scan_result.size() << std::endl;
  }

  // op 44188: get
  status = db->Get(read_options, "key8703", &value);
  if (status.ok()) {
    out << "GET key8703 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44189: put
  db->Put(write_options, "key8330", "value108741");

  // op 44190: delete_range
  status = db->DeleteRange(write_options, "key5928", "key7624");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 44191: get
  status = db->Get(read_options, "key0274", &value);
  if (status.ok()) {
    out << "GET key0274 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44192: get
  status = db->Get(read_options, "key3019", &value);
  if (status.ok()) {
    out << "GET key3019 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44193: delete
  db->Delete(write_options, "key11337");

  // op 44194: get
  status = db->Get(read_options, "key0673", &value);
  if (status.ok()) {
    out << "GET key0673 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44195: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2324", "key7451", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2324, key7451) size=" << scan_result.size() << std::endl;
  }

  // op 44196: get
  status = db->Get(read_options, "key8712", &value);
  if (status.ok()) {
    out << "GET key8712 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44197: put
  db->Put(write_options, "key5606", "value81916");

  // op 44198: delete_range
  status = db->DeleteRange(write_options, "key6777", "key11676");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 44199: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 44200: delete_range
  status = db->DeleteRange(write_options, "key7968", "key9516");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 44201: delete_range
  status = db->DeleteRange(write_options, "key0244", "key10364");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 44202: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1470", "key11244", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1470, key11244) size=" << scan_result.size() << std::endl;
  }

  // op 44203: delete
  db->Delete(write_options, "key10741");

  // op 44204: put
  db->Put(write_options, "key0182", "value35525");

  // op 44205: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6333", "key10408", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6333, key10408) size=" << scan_result.size() << std::endl;
  }

  // op 44206: put
  db->Put(write_options, "key5554", "value67939");

  // op 44207: get
  status = db->Get(read_options, "key11453", &value);
  if (status.ok()) {
    out << "GET key11453 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44208: delete
  db->Delete(write_options, "key2655");

  // op 44209: delete_range
  status = db->DeleteRange(write_options, "key4438", "key5636");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 44210: delete
  db->Delete(write_options, "key8564");

  // op 44211: delete
  db->Delete(write_options, "key8642");

  // op 44212: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6461", "key8149", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6461, key8149) size=" << scan_result.size() << std::endl;
  }

  // op 44213: put
  db->Put(write_options, "key6228", "value92148");

  // op 44214: put
  db->Put(write_options, "key4341", "value2478");

  // op 44215: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1231", "key5287", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1231, key5287) size=" << scan_result.size() << std::endl;
  }

  // op 44216: delete
  db->Delete(write_options, "key8628");

  // op 44217: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7008", "key7218", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7008, key7218) size=" << scan_result.size() << std::endl;
  }

  // op 44218: delete_range
  status = db->DeleteRange(write_options, "key8010", "key9390");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 44219: delete
  db->Delete(write_options, "key7078");

  // op 44220: get
  status = db->Get(read_options, "key11361", &value);
  if (status.ok()) {
    out << "GET key11361 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44221: put
  db->Put(write_options, "key7957", "value104946");

  // op 44222: put
  db->Put(write_options, "key8000", "value110247");

  // op 44223: delete
  db->Delete(write_options, "key0081");

  // op 44224: put
  db->Put(write_options, "key9142", "value78160");

  // op 44225: put
  db->Put(write_options, "key5100", "value71638");

  // op 44226: get
  status = db->Get(read_options, "key6217", &value);
  if (status.ok()) {
    out << "GET key6217 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44227: get
  status = db->Get(read_options, "key3404", &value);
  if (status.ok()) {
    out << "GET key3404 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44228: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 44229: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3267", "key4489", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3267, key4489) size=" << scan_result.size() << std::endl;
  }

  // op 44230: put
  db->Put(write_options, "key3839", "value55203");

  // op 44231: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7139", "key10517", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7139, key10517) size=" << scan_result.size() << std::endl;
  }

  // op 44232: put
  db->Put(write_options, "key8036", "value6602");

  // op 44233: delete_range
  status = db->DeleteRange(write_options, "key3950", "key6086");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 44234: delete
  db->Delete(write_options, "key5215");

  // op 44235: get
  status = db->Get(read_options, "key9807", &value);
  if (status.ok()) {
    out << "GET key9807 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44236: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9246", "key11310", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9246, key11310) size=" << scan_result.size() << std::endl;
  }

  // op 44237: put
  db->Put(write_options, "key1928", "value82188");

  // op 44238: get
  status = db->Get(read_options, "key0354", &value);
  if (status.ok()) {
    out << "GET key0354 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44239: get
  status = db->Get(read_options, "key6504", &value);
  if (status.ok()) {
    out << "GET key6504 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44240: put
  db->Put(write_options, "key9797", "value106943");

  // op 44241: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3747", "key4116", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3747, key4116) size=" << scan_result.size() << std::endl;
  }

  // op 44242: delete_range
  status = db->DeleteRange(write_options, "key0036", "key2253");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 44243: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0629", "key3496", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0629, key3496) size=" << scan_result.size() << std::endl;
  }

  // op 44244: put
  db->Put(write_options, "key2372", "value99663");

  // op 44245: get
  status = db->Get(read_options, "key8791", &value);
  if (status.ok()) {
    out << "GET key8791 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44246: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 44247: put
  db->Put(write_options, "key3766", "value93851");

  // op 44248: put
  db->Put(write_options, "key6979", "value99465");

  // op 44249: put
  db->Put(write_options, "key0151", "value78316");

  // op 44250: put
  db->Put(write_options, "key11052", "value66603");

  // op 44251: delete
  db->Delete(write_options, "key0550");

  // op 44252: get
  status = db->Get(read_options, "key1301", &value);
  if (status.ok()) {
    out << "GET key1301 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44253: get
  status = db->Get(read_options, "key0035", &value);
  if (status.ok()) {
    out << "GET key0035 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44254: get
  status = db->Get(read_options, "key1600", &value);
  if (status.ok()) {
    out << "GET key1600 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44255: delete_range
  status = db->DeleteRange(write_options, "key3200", "key5954");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 44256: delete
  db->Delete(write_options, "key10392");

  // op 44257: put
  db->Put(write_options, "key10900", "value10225");

  // op 44258: delete_range
  status = db->DeleteRange(write_options, "key3391", "key9261");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 44259: put
  db->Put(write_options, "key1741", "value113328");

  // op 44260: get
  status = db->Get(read_options, "key11576", &value);
  if (status.ok()) {
    out << "GET key11576 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44261: put
  db->Put(write_options, "key1437", "value6623");

  // op 44262: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 44263: get
  status = db->Get(read_options, "key3831", &value);
  if (status.ok()) {
    out << "GET key3831 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44264: delete
  db->Delete(write_options, "key9620");

  // op 44265: get
  status = db->Get(read_options, "key5423", &value);
  if (status.ok()) {
    out << "GET key5423 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44266: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1497", "key2570", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1497, key2570) size=" << scan_result.size() << std::endl;
  }

  // op 44267: get
  status = db->Get(read_options, "key10245", &value);
  if (status.ok()) {
    out << "GET key10245 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44268: put
  db->Put(write_options, "key0762", "value85789");

  // op 44269: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7643", "key10343", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7643, key10343) size=" << scan_result.size() << std::endl;
  }

  // op 44270: delete_range
  status = db->DeleteRange(write_options, "key4766", "key9932");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 44271: put
  db->Put(write_options, "key2119", "value19371");

  // op 44272: get
  status = db->Get(read_options, "key2320", &value);
  if (status.ok()) {
    out << "GET key2320 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44273: put
  db->Put(write_options, "key5911", "value23453");

  // op 44274: put
  db->Put(write_options, "key1707", "value35570");

  // op 44275: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0155", "key0205", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0155, key0205) size=" << scan_result.size() << std::endl;
  }

  // op 44276: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1353", "key11290", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1353, key11290) size=" << scan_result.size() << std::endl;
  }

  // op 44277: get
  status = db->Get(read_options, "key1434", &value);
  if (status.ok()) {
    out << "GET key1434 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44278: put
  db->Put(write_options, "key7244", "value25693");

  // op 44279: put
  db->Put(write_options, "key5061", "value17558");

  // op 44280: delete
  db->Delete(write_options, "key4881");

  // op 44281: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4809", "key10510", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4809, key10510) size=" << scan_result.size() << std::endl;
  }

  // op 44282: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2379", "key3798", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2379, key3798) size=" << scan_result.size() << std::endl;
  }

  // op 44283: get
  status = db->Get(read_options, "key7632", &value);
  if (status.ok()) {
    out << "GET key7632 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44284: get
  status = db->Get(read_options, "key0351", &value);
  if (status.ok()) {
    out << "GET key0351 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44285: put
  db->Put(write_options, "key9010", "value88821");

  // op 44286: get
  status = db->Get(read_options, "key4521", &value);
  if (status.ok()) {
    out << "GET key4521 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44287: put
  db->Put(write_options, "key10813", "value31223");

  // op 44288: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0046", "key3007", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0046, key3007) size=" << scan_result.size() << std::endl;
  }

  // op 44289: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6846", "key10682", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6846, key10682) size=" << scan_result.size() << std::endl;
  }

  // op 44290: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3341", "key10329", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3341, key10329) size=" << scan_result.size() << std::endl;
  }

  // op 44291: delete_range
  status = db->DeleteRange(write_options, "key0260", "key9279");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 44292: delete_range
  status = db->DeleteRange(write_options, "key1946", "key10141");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 44293: put
  db->Put(write_options, "key8541", "value15848");

  // op 44294: delete_range
  status = db->DeleteRange(write_options, "key1049", "key1339");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 44295: put
  db->Put(write_options, "key11550", "value14451");

  // op 44296: put
  db->Put(write_options, "key1499", "value101010");

  // op 44297: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9205", "key10516", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9205, key10516) size=" << scan_result.size() << std::endl;
  }

  // op 44298: get
  status = db->Get(read_options, "key10733", &value);
  if (status.ok()) {
    out << "GET key10733 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44299: put
  db->Put(write_options, "key8796", "value31177");

  // op 44300: delete
  db->Delete(write_options, "key4695");

  // op 44301: put
  db->Put(write_options, "key1166", "value65308");

  // op 44302: delete_range
  status = db->DeleteRange(write_options, "key1331", "key11347");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 44303: delete_range
  status = db->DeleteRange(write_options, "key6128", "key8309");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 44304: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2676", "key10265", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2676, key10265) size=" << scan_result.size() << std::endl;
  }

  // op 44305: put
  db->Put(write_options, "key7987", "value51686");

  // op 44306: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3632", "key4437", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3632, key4437) size=" << scan_result.size() << std::endl;
  }

  // op 44307: put
  db->Put(write_options, "key7356", "value112019");

  // op 44308: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 44309: get
  status = db->Get(read_options, "key5528", &value);
  if (status.ok()) {
    out << "GET key5528 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44310: put
  db->Put(write_options, "key11239", "value22164");

  // op 44311: get
  status = db->Get(read_options, "key1427", &value);
  if (status.ok()) {
    out << "GET key1427 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44312: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1091", "key6146", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1091, key6146) size=" << scan_result.size() << std::endl;
  }

  // op 44313: delete_range
  status = db->DeleteRange(write_options, "key1430", "key8178");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 44314: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8229", "key11611", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8229, key11611) size=" << scan_result.size() << std::endl;
  }

  // op 44315: get
  status = db->Get(read_options, "key8497", &value);
  if (status.ok()) {
    out << "GET key8497 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44316: delete_range
  status = db->DeleteRange(write_options, "key2735", "key7754");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 44317: delete
  db->Delete(write_options, "key6847");

  // op 44318: get
  status = db->Get(read_options, "key10008", &value);
  if (status.ok()) {
    out << "GET key10008 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44319: get
  status = db->Get(read_options, "key10681", &value);
  if (status.ok()) {
    out << "GET key10681 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44320: delete
  db->Delete(write_options, "key3134");

  // op 44321: get
  status = db->Get(read_options, "key3818", &value);
  if (status.ok()) {
    out << "GET key3818 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44322: delete_range
  status = db->DeleteRange(write_options, "key0292", "key8495");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 44323: put
  db->Put(write_options, "key11493", "value56417");

  // op 44324: get
  status = db->Get(read_options, "key7668", &value);
  if (status.ok()) {
    out << "GET key7668 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44325: put
  db->Put(write_options, "key8320", "value13652");

  // op 44326: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 44327: get
  status = db->Get(read_options, "key2892", &value);
  if (status.ok()) {
    out << "GET key2892 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44328: delete_range
  status = db->DeleteRange(write_options, "key1094", "key11218");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 44329: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 44330: delete_range
  status = db->DeleteRange(write_options, "key9393", "key10570");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 44331: put
  db->Put(write_options, "key8249", "value38427");

  // op 44332: put
  db->Put(write_options, "key2725", "value95120");

  // op 44333: get
  status = db->Get(read_options, "key11690", &value);
  if (status.ok()) {
    out << "GET key11690 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44334: put
  db->Put(write_options, "key2898", "value12476");

  // op 44335: delete_range
  status = db->DeleteRange(write_options, "key0322", "key5460");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 44336: put
  db->Put(write_options, "key6587", "value82607");

  // op 44337: put
  db->Put(write_options, "key6420", "value4452");

  // op 44338: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0857", "key10986", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0857, key10986) size=" << scan_result.size() << std::endl;
  }

  // op 44339: put
  db->Put(write_options, "key1214", "value54264");

  // op 44340: put
  db->Put(write_options, "key0309", "value18243");

  // op 44341: put
  db->Put(write_options, "key9936", "value74470");

  // op 44342: get
  status = db->Get(read_options, "key5996", &value);
  if (status.ok()) {
    out << "GET key5996 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44343: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7511", "key11630", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7511, key11630) size=" << scan_result.size() << std::endl;
  }

  // op 44344: delete_range
  status = db->DeleteRange(write_options, "key4096", "key7218");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 44345: put
  db->Put(write_options, "key2558", "value17278");

  // op 44346: get
  status = db->Get(read_options, "key4546", &value);
  if (status.ok()) {
    out << "GET key4546 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44347: delete_range
  status = db->DeleteRange(write_options, "key0485", "key4183");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 44348: put
  db->Put(write_options, "key9464", "value5756");

  // op 44349: put
  db->Put(write_options, "key1764", "value1220");

  // op 44350: put
  db->Put(write_options, "key6820", "value24045");

  // op 44351: put
  db->Put(write_options, "key11307", "value94412");

  // op 44352: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 44353: get
  status = db->Get(read_options, "key11708", &value);
  if (status.ok()) {
    out << "GET key11708 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44354: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5311", "key7918", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5311, key7918) size=" << scan_result.size() << std::endl;
  }

  // op 44355: get
  status = db->Get(read_options, "key9694", &value);
  if (status.ok()) {
    out << "GET key9694 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44356: get
  status = db->Get(read_options, "key3538", &value);
  if (status.ok()) {
    out << "GET key3538 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44357: get
  status = db->Get(read_options, "key10961", &value);
  if (status.ok()) {
    out << "GET key10961 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44358: get
  status = db->Get(read_options, "key4912", &value);
  if (status.ok()) {
    out << "GET key4912 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44359: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9769", "key10810", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9769, key10810) size=" << scan_result.size() << std::endl;
  }

  // op 44360: put
  db->Put(write_options, "key1219", "value96880");

  // op 44361: put
  db->Put(write_options, "key6451", "value39223");

  // op 44362: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0535", "key7348", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0535, key7348) size=" << scan_result.size() << std::endl;
  }

  // op 44363: get
  status = db->Get(read_options, "key0311", &value);
  if (status.ok()) {
    out << "GET key0311 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44364: get
  status = db->Get(read_options, "key5975", &value);
  if (status.ok()) {
    out << "GET key5975 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44365: delete_range
  status = db->DeleteRange(write_options, "key1344", "key6089");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 44366: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5090", "key6724", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5090, key6724) size=" << scan_result.size() << std::endl;
  }

  // op 44367: put
  db->Put(write_options, "key11674", "value38993");

  // op 44368: put
  db->Put(write_options, "key10609", "value106835");

  // op 44369: delete
  db->Delete(write_options, "key7790");

  // op 44370: get
  status = db->Get(read_options, "key8555", &value);
  if (status.ok()) {
    out << "GET key8555 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44371: put
  db->Put(write_options, "key2876", "value51108");

  // op 44372: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7545", "key7596", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7545, key7596) size=" << scan_result.size() << std::endl;
  }

  // op 44373: put
  db->Put(write_options, "key10020", "value18267");

  // op 44374: put
  db->Put(write_options, "key5972", "value76527");

  // op 44375: put
  db->Put(write_options, "key5023", "value83197");

  // op 44376: put
  db->Put(write_options, "key3232", "value57458");

  // op 44377: delete_range
  status = db->DeleteRange(write_options, "key2563", "key4481");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 44378: put
  db->Put(write_options, "key0919", "value113918");

  // op 44379: get
  status = db->Get(read_options, "key10355", &value);
  if (status.ok()) {
    out << "GET key10355 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44380: delete_range
  status = db->DeleteRange(write_options, "key1771", "key11712");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 44381: put
  db->Put(write_options, "key11334", "value56650");

  // op 44382: put
  db->Put(write_options, "key7953", "value60680");

  // op 44383: put
  db->Put(write_options, "key0739", "value101744");

  // op 44384: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2708", "key10528", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2708, key10528) size=" << scan_result.size() << std::endl;
  }

  // op 44385: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2271", "key6397", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2271, key6397) size=" << scan_result.size() << std::endl;
  }

  // op 44386: put
  db->Put(write_options, "key5867", "value72596");

  // op 44387: put
  db->Put(write_options, "key5235", "value66989");

  // op 44388: delete_range
  status = db->DeleteRange(write_options, "key3585", "key7438");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 44389: put
  db->Put(write_options, "key3435", "value16817");

  // op 44390: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 44391: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 44392: put
  db->Put(write_options, "key4187", "value101860");

  // op 44393: put
  db->Put(write_options, "key8223", "value62607");

  // op 44394: delete
  db->Delete(write_options, "key9657");

  // op 44395: put
  db->Put(write_options, "key9990", "value7335");

  // op 44396: put
  db->Put(write_options, "key8607", "value41887");

  // op 44397: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0900", "key5889", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0900, key5889) size=" << scan_result.size() << std::endl;
  }

  // op 44398: delete
  db->Delete(write_options, "key2216");

  // op 44399: put
  db->Put(write_options, "key8111", "value36251");

  // op 44400: put
  db->Put(write_options, "key9935", "value111121");

  // op 44401: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 44402: get
  status = db->Get(read_options, "key2074", &value);
  if (status.ok()) {
    out << "GET key2074 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44403: put
  db->Put(write_options, "key2382", "value107541");

  // op 44404: get
  status = db->Get(read_options, "key5487", &value);
  if (status.ok()) {
    out << "GET key5487 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44405: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0808", "key2911", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0808, key2911) size=" << scan_result.size() << std::endl;
  }

  // op 44406: get
  status = db->Get(read_options, "key5303", &value);
  if (status.ok()) {
    out << "GET key5303 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44407: put
  db->Put(write_options, "key6082", "value24665");

  // op 44408: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4412", "key9541", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4412, key9541) size=" << scan_result.size() << std::endl;
  }

  // op 44409: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7367", "key8927", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7367, key8927) size=" << scan_result.size() << std::endl;
  }

  // op 44410: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5372", "key10584", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5372, key10584) size=" << scan_result.size() << std::endl;
  }

  // op 44411: put
  db->Put(write_options, "key9823", "value107608");

  // op 44412: delete
  db->Delete(write_options, "key8726");

  // op 44413: get
  status = db->Get(read_options, "key4466", &value);
  if (status.ok()) {
    out << "GET key4466 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44414: put
  db->Put(write_options, "key5358", "value88682");

  // op 44415: put
  db->Put(write_options, "key1704", "value109992");

  // op 44416: delete_range
  status = db->DeleteRange(write_options, "key10269", "key11068");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 44417: get
  status = db->Get(read_options, "key8426", &value);
  if (status.ok()) {
    out << "GET key8426 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44418: delete
  db->Delete(write_options, "key2606");

  // op 44419: put
  db->Put(write_options, "key2331", "value4146");

  // op 44420: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7070", "key8666", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7070, key8666) size=" << scan_result.size() << std::endl;
  }

  // op 44421: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3891", "key11146", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3891, key11146) size=" << scan_result.size() << std::endl;
  }

  // op 44422: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7224", "key7847", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7224, key7847) size=" << scan_result.size() << std::endl;
  }

  // op 44423: put
  db->Put(write_options, "key0100", "value43587");

  // op 44424: delete_range
  status = db->DeleteRange(write_options, "key9248", "key10634");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 44425: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 44426: get
  status = db->Get(read_options, "key1793", &value);
  if (status.ok()) {
    out << "GET key1793 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44427: delete_range
  status = db->DeleteRange(write_options, "key1320", "key5322");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 44428: get
  status = db->Get(read_options, "key6657", &value);
  if (status.ok()) {
    out << "GET key6657 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44429: delete_range
  status = db->DeleteRange(write_options, "key4771", "key5737");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 44430: delete
  db->Delete(write_options, "key8187");

  // op 44431: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 44432: put
  db->Put(write_options, "key8458", "value23327");

  // op 44433: delete
  db->Delete(write_options, "key8574");

  // op 44434: delete
  db->Delete(write_options, "key6094");

  // op 44435: put
  db->Put(write_options, "key8486", "value4578");

  // op 44436: put
  db->Put(write_options, "key8032", "value24877");

  // op 44437: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4395", "key11109", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4395, key11109) size=" << scan_result.size() << std::endl;
  }

  // op 44438: delete_range
  status = db->DeleteRange(write_options, "key2451", "key10864");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 44439: put
  db->Put(write_options, "key7705", "value16591");

  // op 44440: put
  db->Put(write_options, "key3187", "value94797");

  // op 44441: get
  status = db->Get(read_options, "key8779", &value);
  if (status.ok()) {
    out << "GET key8779 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44442: get
  status = db->Get(read_options, "key2060", &value);
  if (status.ok()) {
    out << "GET key2060 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44443: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1743", "key4143", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1743, key4143) size=" << scan_result.size() << std::endl;
  }

  // op 44444: delete
  db->Delete(write_options, "key10412");

  // op 44445: get
  status = db->Get(read_options, "key10673", &value);
  if (status.ok()) {
    out << "GET key10673 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44446: get
  status = db->Get(read_options, "key0671", &value);
  if (status.ok()) {
    out << "GET key0671 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44447: put
  db->Put(write_options, "key2430", "value28145");

  // op 44448: get
  status = db->Get(read_options, "key6831", &value);
  if (status.ok()) {
    out << "GET key6831 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44449: put
  db->Put(write_options, "key11464", "value28392");

  // op 44450: put
  db->Put(write_options, "key11435", "value60515");

  // op 44451: delete_range
  status = db->DeleteRange(write_options, "key3589", "key7258");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 44452: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2605", "key5946", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2605, key5946) size=" << scan_result.size() << std::endl;
  }

  // op 44453: put
  db->Put(write_options, "key1217", "value97666");

  // op 44454: delete
  db->Delete(write_options, "key6562");

  // op 44455: put
  db->Put(write_options, "key2710", "value109288");

  // op 44456: get
  status = db->Get(read_options, "key3227", &value);
  if (status.ok()) {
    out << "GET key3227 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44457: get
  status = db->Get(read_options, "key7222", &value);
  if (status.ok()) {
    out << "GET key7222 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44458: delete_range
  status = db->DeleteRange(write_options, "key5630", "key9720");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 44459: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1747", "key3276", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1747, key3276) size=" << scan_result.size() << std::endl;
  }

  // op 44460: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 44461: delete_range
  status = db->DeleteRange(write_options, "key0723", "key5871");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 44462: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 44463: get
  status = db->Get(read_options, "key4773", &value);
  if (status.ok()) {
    out << "GET key4773 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44464: get
  status = db->Get(read_options, "key9982", &value);
  if (status.ok()) {
    out << "GET key9982 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44465: put
  db->Put(write_options, "key9352", "value66136");

  // op 44466: put
  db->Put(write_options, "key5533", "value48615");

  // op 44467: delete_range
  status = db->DeleteRange(write_options, "key6464", "key9941");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 44468: put
  db->Put(write_options, "key2129", "value65564");

  // op 44469: delete
  db->Delete(write_options, "key1330");

  // op 44470: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2400", "key3695", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2400, key3695) size=" << scan_result.size() << std::endl;
  }

  // op 44471: delete_range
  status = db->DeleteRange(write_options, "key0836", "key11287");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 44472: put
  db->Put(write_options, "key8001", "value24811");

  // op 44473: delete_range
  status = db->DeleteRange(write_options, "key4379", "key5922");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 44474: delete
  db->Delete(write_options, "key4768");

  // op 44475: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1773", "key1832", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1773, key1832) size=" << scan_result.size() << std::endl;
  }

  // op 44476: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0261", "key3197", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0261, key3197) size=" << scan_result.size() << std::endl;
  }

  // op 44477: put
  db->Put(write_options, "key4541", "value59942");

  // op 44478: put
  db->Put(write_options, "key7756", "value23780");

  // op 44479: get
  status = db->Get(read_options, "key4188", &value);
  if (status.ok()) {
    out << "GET key4188 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44480: get
  status = db->Get(read_options, "key5616", &value);
  if (status.ok()) {
    out << "GET key5616 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44481: put
  db->Put(write_options, "key3591", "value100543");

  // op 44482: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2160", "key6318", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2160, key6318) size=" << scan_result.size() << std::endl;
  }

  // op 44483: get
  status = db->Get(read_options, "key0535", &value);
  if (status.ok()) {
    out << "GET key0535 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44484: get
  status = db->Get(read_options, "key7489", &value);
  if (status.ok()) {
    out << "GET key7489 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44485: put
  db->Put(write_options, "key7316", "value3744");

  // op 44486: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 44487: put
  db->Put(write_options, "key11235", "value55016");

  // op 44488: put
  db->Put(write_options, "key1861", "value109801");

  // op 44489: put
  db->Put(write_options, "key4117", "value27879");

  // op 44490: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0242", "key5178", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0242, key5178) size=" << scan_result.size() << std::endl;
  }

  // op 44491: get
  status = db->Get(read_options, "key5569", &value);
  if (status.ok()) {
    out << "GET key5569 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44492: put
  db->Put(write_options, "key0935", "value58398");

  // op 44493: put
  db->Put(write_options, "key8857", "value45334");

  // op 44494: delete_range
  status = db->DeleteRange(write_options, "key0623", "key2273");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 44495: delete
  db->Delete(write_options, "key5958");

  // op 44496: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 44497: get
  status = db->Get(read_options, "key7365", &value);
  if (status.ok()) {
    out << "GET key7365 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44498: get
  status = db->Get(read_options, "key9390", &value);
  if (status.ok()) {
    out << "GET key9390 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44499: put
  db->Put(write_options, "key7960", "value116101");

  // op 44500: delete
  db->Delete(write_options, "key10601");

  // op 44501: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 44502: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 44503: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7390", "key11498", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7390, key11498) size=" << scan_result.size() << std::endl;
  }

  // op 44504: put
  db->Put(write_options, "key3813", "value11939");

  // op 44505: put
  db->Put(write_options, "key3571", "value1170");

  // op 44506: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7416", "key10846", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7416, key10846) size=" << scan_result.size() << std::endl;
  }

  // op 44507: put
  db->Put(write_options, "key10363", "value10881");

  // op 44508: delete_range
  status = db->DeleteRange(write_options, "key9145", "key10915");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 44509: put
  db->Put(write_options, "key6469", "value75806");

  // op 44510: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 44511: put
  db->Put(write_options, "key10338", "value81988");

  // op 44512: get
  status = db->Get(read_options, "key4379", &value);
  if (status.ok()) {
    out << "GET key4379 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44513: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2861", "key10676", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2861, key10676) size=" << scan_result.size() << std::endl;
  }

  // op 44514: put
  db->Put(write_options, "key0488", "value65584");

  // op 44515: delete
  db->Delete(write_options, "key5023");

  // op 44516: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 44517: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1725", "key10073", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1725, key10073) size=" << scan_result.size() << std::endl;
  }

  // op 44518: get
  status = db->Get(read_options, "key0892", &value);
  if (status.ok()) {
    out << "GET key0892 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44519: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0139", "key9665", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0139, key9665) size=" << scan_result.size() << std::endl;
  }

  // op 44520: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1716", "key7459", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1716, key7459) size=" << scan_result.size() << std::endl;
  }

  // op 44521: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3475", "key7743", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3475, key7743) size=" << scan_result.size() << std::endl;
  }

  // op 44522: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2675", "key9372", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2675, key9372) size=" << scan_result.size() << std::endl;
  }

  // op 44523: delete_range
  status = db->DeleteRange(write_options, "key1015", "key9529");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 44524: delete_range
  status = db->DeleteRange(write_options, "key0181", "key4530");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 44525: put
  db->Put(write_options, "key6462", "value65900");

  // op 44526: put
  db->Put(write_options, "key11614", "value95665");

  // op 44527: put
  db->Put(write_options, "key10454", "value39780");

  // op 44528: delete_range
  status = db->DeleteRange(write_options, "key4531", "key10700");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 44529: put
  db->Put(write_options, "key10586", "value116133");

  // op 44530: put
  db->Put(write_options, "key9784", "value40163");

  // op 44531: put
  db->Put(write_options, "key9494", "value104064");

  // op 44532: put
  db->Put(write_options, "key3460", "value66156");

  // op 44533: delete_range
  status = db->DeleteRange(write_options, "key3741", "key3822");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 44534: get
  status = db->Get(read_options, "key7230", &value);
  if (status.ok()) {
    out << "GET key7230 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44535: put
  db->Put(write_options, "key6389", "value101884");

  // op 44536: delete_range
  status = db->DeleteRange(write_options, "key5661", "key9291");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 44537: get
  status = db->Get(read_options, "key4488", &value);
  if (status.ok()) {
    out << "GET key4488 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44538: put
  db->Put(write_options, "key2033", "value52293");

  // op 44539: get
  status = db->Get(read_options, "key7966", &value);
  if (status.ok()) {
    out << "GET key7966 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44540: delete
  db->Delete(write_options, "key7070");

  // op 44541: delete_range
  status = db->DeleteRange(write_options, "key0546", "key5985");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 44542: get
  status = db->Get(read_options, "key3621", &value);
  if (status.ok()) {
    out << "GET key3621 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44543: put
  db->Put(write_options, "key4098", "value31535");

  // op 44544: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9190", "key9800", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9190, key9800) size=" << scan_result.size() << std::endl;
  }

  // op 44545: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3573", "key9647", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3573, key9647) size=" << scan_result.size() << std::endl;
  }

  // op 44546: put
  db->Put(write_options, "key0623", "value59502");

  // op 44547: get
  status = db->Get(read_options, "key3631", &value);
  if (status.ok()) {
    out << "GET key3631 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44548: get
  status = db->Get(read_options, "key0420", &value);
  if (status.ok()) {
    out << "GET key0420 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44549: delete
  db->Delete(write_options, "key4901");

  // op 44550: get
  status = db->Get(read_options, "key0002", &value);
  if (status.ok()) {
    out << "GET key0002 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44551: put
  db->Put(write_options, "key9606", "value79360");

  // op 44552: put
  db->Put(write_options, "key5513", "value75810");

  // op 44553: put
  db->Put(write_options, "key7365", "value85640");

  // op 44554: put
  db->Put(write_options, "key9028", "value43019");

  // op 44555: get
  status = db->Get(read_options, "key1941", &value);
  if (status.ok()) {
    out << "GET key1941 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44556: put
  db->Put(write_options, "key10971", "value51704");

  // op 44557: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5152", "key9425", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5152, key9425) size=" << scan_result.size() << std::endl;
  }

  // op 44558: put
  db->Put(write_options, "key6545", "value111776");

  // op 44559: delete
  db->Delete(write_options, "key8168");

  // op 44560: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 44561: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 44562: put
  db->Put(write_options, "key4854", "value33637");

  // op 44563: get
  status = db->Get(read_options, "key3940", &value);
  if (status.ok()) {
    out << "GET key3940 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44564: put
  db->Put(write_options, "key5471", "value36513");

  // op 44565: delete
  db->Delete(write_options, "key10482");

  // op 44566: delete_range
  status = db->DeleteRange(write_options, "key5267", "key7125");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 44567: put
  db->Put(write_options, "key4661", "value23136");

  // op 44568: delete
  db->Delete(write_options, "key10973");

  // op 44569: put
  db->Put(write_options, "key6602", "value109295");

  // op 44570: get
  status = db->Get(read_options, "key11296", &value);
  if (status.ok()) {
    out << "GET key11296 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44571: put
  db->Put(write_options, "key1226", "value21090");

  // op 44572: delete
  db->Delete(write_options, "key9011");

  // op 44573: put
  db->Put(write_options, "key4143", "value67330");

  // op 44574: put
  db->Put(write_options, "key6035", "value69751");

  // op 44575: put
  db->Put(write_options, "key5732", "value22528");

  // op 44576: get
  status = db->Get(read_options, "key3154", &value);
  if (status.ok()) {
    out << "GET key3154 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44577: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2454", "key10655", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2454, key10655) size=" << scan_result.size() << std::endl;
  }

  // op 44578: get
  status = db->Get(read_options, "key6646", &value);
  if (status.ok()) {
    out << "GET key6646 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44579: put
  db->Put(write_options, "key0884", "value112939");

  // op 44580: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 44581: get
  status = db->Get(read_options, "key11274", &value);
  if (status.ok()) {
    out << "GET key11274 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44582: delete
  db->Delete(write_options, "key9245");

  // op 44583: put
  db->Put(write_options, "key3697", "value28252");

  // op 44584: delete_range
  status = db->DeleteRange(write_options, "key1374", "key5913");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 44585: put
  db->Put(write_options, "key0359", "value103179");

  // op 44586: delete_range
  status = db->DeleteRange(write_options, "key6537", "key6928");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 44587: get
  status = db->Get(read_options, "key1438", &value);
  if (status.ok()) {
    out << "GET key1438 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44588: delete
  db->Delete(write_options, "key2418");

  // op 44589: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 44590: put
  db->Put(write_options, "key11384", "value12510");

  // op 44591: delete_range
  status = db->DeleteRange(write_options, "key0300", "key10069");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 44592: delete
  db->Delete(write_options, "key2660");

  // op 44593: get
  status = db->Get(read_options, "key2530", &value);
  if (status.ok()) {
    out << "GET key2530 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44594: put
  db->Put(write_options, "key4058", "value62551");

  // op 44595: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6306", "key8173", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6306, key8173) size=" << scan_result.size() << std::endl;
  }

  // op 44596: get
  status = db->Get(read_options, "key9865", &value);
  if (status.ok()) {
    out << "GET key9865 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44597: delete_range
  status = db->DeleteRange(write_options, "key3431", "key7168");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 44598: put
  db->Put(write_options, "key1543", "value99289");

  // op 44599: get
  status = db->Get(read_options, "key4033", &value);
  if (status.ok()) {
    out << "GET key4033 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44600: delete_range
  status = db->DeleteRange(write_options, "key1992", "key9742");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 44601: put
  db->Put(write_options, "key7115", "value64738");

  // op 44602: delete
  db->Delete(write_options, "key10709");

  // op 44603: put
  db->Put(write_options, "key10720", "value46480");

  // op 44604: delete_range
  status = db->DeleteRange(write_options, "key4642", "key6266");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 44605: get
  status = db->Get(read_options, "key9896", &value);
  if (status.ok()) {
    out << "GET key9896 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44606: delete
  db->Delete(write_options, "key4053");

  // op 44607: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4120", "key5055", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4120, key5055) size=" << scan_result.size() << std::endl;
  }

  // op 44608: put
  db->Put(write_options, "key1292", "value88662");

  // op 44609: delete_range
  status = db->DeleteRange(write_options, "key8159", "key11287");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 44610: delete
  db->Delete(write_options, "key6763");

  // op 44611: put
  db->Put(write_options, "key8118", "value114423");

  // op 44612: put
  db->Put(write_options, "key1380", "value67843");

  // op 44613: get
  status = db->Get(read_options, "key4127", &value);
  if (status.ok()) {
    out << "GET key4127 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44614: delete
  db->Delete(write_options, "key11622");

  // op 44615: delete
  db->Delete(write_options, "key3266");

  // op 44616: delete
  db->Delete(write_options, "key9473");

  // op 44617: put
  db->Put(write_options, "key10778", "value60276");

  // op 44618: delete
  db->Delete(write_options, "key9303");

  // op 44619: put
  db->Put(write_options, "key6597", "value46364");

  // op 44620: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7610", "key10624", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7610, key10624) size=" << scan_result.size() << std::endl;
  }

  // op 44621: delete_range
  status = db->DeleteRange(write_options, "key5252", "key9290");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 44622: put
  db->Put(write_options, "key1848", "value75841");

  // op 44623: put
  db->Put(write_options, "key3641", "value108896");

  // op 44624: put
  db->Put(write_options, "key11120", "value15496");

  // op 44625: get
  status = db->Get(read_options, "key6605", &value);
  if (status.ok()) {
    out << "GET key6605 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44626: put
  db->Put(write_options, "key2087", "value19118");

  // op 44627: delete_range
  status = db->DeleteRange(write_options, "key2218", "key6938");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 44628: delete_range
  status = db->DeleteRange(write_options, "key4999", "key5190");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 44629: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0767", "key10110", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0767, key10110) size=" << scan_result.size() << std::endl;
  }

  // op 44630: get
  status = db->Get(read_options, "key9038", &value);
  if (status.ok()) {
    out << "GET key9038 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44631: put
  db->Put(write_options, "key7386", "value9983");

  // op 44632: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4430", "key9125", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4430, key9125) size=" << scan_result.size() << std::endl;
  }

  // op 44633: delete
  db->Delete(write_options, "key7924");

  // op 44634: put
  db->Put(write_options, "key0172", "value6342");

  // op 44635: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1763", "key1965", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1763, key1965) size=" << scan_result.size() << std::endl;
  }

  // op 44636: get
  status = db->Get(read_options, "key9119", &value);
  if (status.ok()) {
    out << "GET key9119 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44637: put
  db->Put(write_options, "key8185", "value1744");

  // op 44638: put
  db->Put(write_options, "key6056", "value78346");

  // op 44639: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5302", "key5354", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5302, key5354) size=" << scan_result.size() << std::endl;
  }

  // op 44640: delete_range
  status = db->DeleteRange(write_options, "key6664", "key9753");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 44641: put
  db->Put(write_options, "key2853", "value2800");

  // op 44642: put
  db->Put(write_options, "key8285", "value97813");

  // op 44643: put
  db->Put(write_options, "key4910", "value18762");

  // op 44644: put
  db->Put(write_options, "key11044", "value108132");

  // op 44645: put
  db->Put(write_options, "key2707", "value82226");

  // op 44646: put
  db->Put(write_options, "key11267", "value6186");

  // op 44647: delete
  db->Delete(write_options, "key10554");

  // op 44648: delete_range
  status = db->DeleteRange(write_options, "key4309", "key9341");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 44649: get
  status = db->Get(read_options, "key5954", &value);
  if (status.ok()) {
    out << "GET key5954 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44650: put
  db->Put(write_options, "key9614", "value77316");

  // op 44651: delete_range
  status = db->DeleteRange(write_options, "key0525", "key8320");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 44652: get
  status = db->Get(read_options, "key4227", &value);
  if (status.ok()) {
    out << "GET key4227 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44653: get
  status = db->Get(read_options, "key6161", &value);
  if (status.ok()) {
    out << "GET key6161 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44654: get
  status = db->Get(read_options, "key4470", &value);
  if (status.ok()) {
    out << "GET key4470 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44655: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8203", "key10136", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8203, key10136) size=" << scan_result.size() << std::endl;
  }

  // op 44656: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 44657: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 44658: put
  db->Put(write_options, "key8336", "value110882");

  // op 44659: put
  db->Put(write_options, "key11227", "value104576");

  // op 44660: delete_range
  status = db->DeleteRange(write_options, "key0205", "key9813");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 44661: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4736", "key9167", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4736, key9167) size=" << scan_result.size() << std::endl;
  }

  // op 44662: delete
  db->Delete(write_options, "key1940");

  // op 44663: get
  status = db->Get(read_options, "key1009", &value);
  if (status.ok()) {
    out << "GET key1009 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44664: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9174", "key10151", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9174, key10151) size=" << scan_result.size() << std::endl;
  }

  // op 44665: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 44666: delete_range
  status = db->DeleteRange(write_options, "key1668", "key7050");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 44667: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2868", "key8696", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2868, key8696) size=" << scan_result.size() << std::endl;
  }

  // op 44668: get
  status = db->Get(read_options, "key4764", &value);
  if (status.ok()) {
    out << "GET key4764 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44669: delete_range
  status = db->DeleteRange(write_options, "key1421", "key8970");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 44670: put
  db->Put(write_options, "key6900", "value56785");

  // op 44671: put
  db->Put(write_options, "key4783", "value77932");

  // op 44672: put
  db->Put(write_options, "key0879", "value31290");

  // op 44673: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7648", "key7778", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7648, key7778) size=" << scan_result.size() << std::endl;
  }

  // op 44674: put
  db->Put(write_options, "key4983", "value59477");

  // op 44675: put
  db->Put(write_options, "key4775", "value105727");

  // op 44676: put
  db->Put(write_options, "key6455", "value30961");

  // op 44677: delete
  db->Delete(write_options, "key7912");

  // op 44678: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 44679: put
  db->Put(write_options, "key10997", "value50965");

  // op 44680: get
  status = db->Get(read_options, "key0622", &value);
  if (status.ok()) {
    out << "GET key0622 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44681: get
  status = db->Get(read_options, "key7524", &value);
  if (status.ok()) {
    out << "GET key7524 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44682: put
  db->Put(write_options, "key3767", "value25659");

  // op 44683: put
  db->Put(write_options, "key6597", "value1301");

  // op 44684: delete
  db->Delete(write_options, "key1493");

  // op 44685: put
  db->Put(write_options, "key3862", "value12816");

  // op 44686: put
  db->Put(write_options, "key3496", "value78082");

  // op 44687: delete_range
  status = db->DeleteRange(write_options, "key3352", "key5719");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 44688: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 44689: get
  status = db->Get(read_options, "key7796", &value);
  if (status.ok()) {
    out << "GET key7796 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44690: put
  db->Put(write_options, "key0193", "value13047");

  // op 44691: get
  status = db->Get(read_options, "key7712", &value);
  if (status.ok()) {
    out << "GET key7712 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44692: put
  db->Put(write_options, "key7346", "value21161");

  // op 44693: put
  db->Put(write_options, "key2082", "value39557");

  // op 44694: delete
  db->Delete(write_options, "key0404");

  // op 44695: delete
  db->Delete(write_options, "key7550");

  // op 44696: delete_range
  status = db->DeleteRange(write_options, "key5663", "key7787");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 44697: delete_range
  status = db->DeleteRange(write_options, "key7906", "key10803");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 44698: delete
  db->Delete(write_options, "key5864");

  // op 44699: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8198", "key10255", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8198, key10255) size=" << scan_result.size() << std::endl;
  }

  // op 44700: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1610", "key6501", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1610, key6501) size=" << scan_result.size() << std::endl;
  }

  // op 44701: get
  status = db->Get(read_options, "key7143", &value);
  if (status.ok()) {
    out << "GET key7143 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44702: put
  db->Put(write_options, "key5196", "value106836");

  // op 44703: put
  db->Put(write_options, "key6388", "value59357");

  // op 44704: put
  db->Put(write_options, "key7295", "value1061");

  // op 44705: put
  db->Put(write_options, "key1715", "value27252");

  // op 44706: put
  db->Put(write_options, "key10859", "value5246");

  // op 44707: delete_range
  status = db->DeleteRange(write_options, "key3302", "key8855");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 44708: get
  status = db->Get(read_options, "key2120", &value);
  if (status.ok()) {
    out << "GET key2120 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44709: get
  status = db->Get(read_options, "key10911", &value);
  if (status.ok()) {
    out << "GET key10911 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44710: put
  db->Put(write_options, "key8752", "value7464");

  // op 44711: delete_range
  status = db->DeleteRange(write_options, "key2036", "key11132");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 44712: put
  db->Put(write_options, "key4234", "value10708");

  // op 44713: delete_range
  status = db->DeleteRange(write_options, "key3979", "key11362");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 44714: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 44715: delete
  db->Delete(write_options, "key6447");

  // op 44716: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6088", "key10582", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6088, key10582) size=" << scan_result.size() << std::endl;
  }

  // op 44717: delete
  db->Delete(write_options, "key4644");

  // op 44718: put
  db->Put(write_options, "key6182", "value74802");

  // op 44719: delete_range
  status = db->DeleteRange(write_options, "key6831", "key11573");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 44720: put
  db->Put(write_options, "key7980", "value33822");

  // op 44721: put
  db->Put(write_options, "key2494", "value60623");

  // op 44722: delete_range
  status = db->DeleteRange(write_options, "key10767", "key10903");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 44723: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5356", "key6508", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5356, key6508) size=" << scan_result.size() << std::endl;
  }

  // op 44724: put
  db->Put(write_options, "key2787", "value11985");

  // op 44725: put
  db->Put(write_options, "key1819", "value65918");

  // op 44726: put
  db->Put(write_options, "key1917", "value81344");

  // op 44727: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0902", "key5432", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0902, key5432) size=" << scan_result.size() << std::endl;
  }

  // op 44728: delete_range
  status = db->DeleteRange(write_options, "key2219", "key4076");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 44729: put
  db->Put(write_options, "key8910", "value98255");

  // op 44730: get
  status = db->Get(read_options, "key8582", &value);
  if (status.ok()) {
    out << "GET key8582 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44731: put
  db->Put(write_options, "key5576", "value93102");

  // op 44732: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4215", "key7322", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4215, key7322) size=" << scan_result.size() << std::endl;
  }

  // op 44733: get
  status = db->Get(read_options, "key6908", &value);
  if (status.ok()) {
    out << "GET key6908 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44734: get
  status = db->Get(read_options, "key11691", &value);
  if (status.ok()) {
    out << "GET key11691 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44735: get
  status = db->Get(read_options, "key9113", &value);
  if (status.ok()) {
    out << "GET key9113 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44736: put
  db->Put(write_options, "key11612", "value107804");

  // op 44737: delete
  db->Delete(write_options, "key3816");

  // op 44738: get
  status = db->Get(read_options, "key0945", &value);
  if (status.ok()) {
    out << "GET key0945 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44739: delete
  db->Delete(write_options, "key4828");

  // op 44740: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6231", "key8524", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6231, key8524) size=" << scan_result.size() << std::endl;
  }

  // op 44741: delete
  db->Delete(write_options, "key4609");

  // op 44742: put
  db->Put(write_options, "key5902", "value3350");

  // op 44743: delete_range
  status = db->DeleteRange(write_options, "key4466", "key5647");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 44744: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 44745: put
  db->Put(write_options, "key11391", "value64024");

  // op 44746: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1150", "key11629", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1150, key11629) size=" << scan_result.size() << std::endl;
  }

  // op 44747: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 44748: put
  db->Put(write_options, "key9648", "value103164");

  // op 44749: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 44750: put
  db->Put(write_options, "key1154", "value58814");

  // op 44751: put
  db->Put(write_options, "key8714", "value53146");

  // op 44752: get
  status = db->Get(read_options, "key7979", &value);
  if (status.ok()) {
    out << "GET key7979 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44753: get
  status = db->Get(read_options, "key0414", &value);
  if (status.ok()) {
    out << "GET key0414 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44754: get
  status = db->Get(read_options, "key2116", &value);
  if (status.ok()) {
    out << "GET key2116 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44755: put
  db->Put(write_options, "key10377", "value28441");

  // op 44756: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2109", "key5964", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2109, key5964) size=" << scan_result.size() << std::endl;
  }

  // op 44757: put
  db->Put(write_options, "key0769", "value19593");

  // op 44758: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8663", "key10593", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8663, key10593) size=" << scan_result.size() << std::endl;
  }

  // op 44759: put
  db->Put(write_options, "key6273", "value49351");

  // op 44760: get
  status = db->Get(read_options, "key7311", &value);
  if (status.ok()) {
    out << "GET key7311 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44761: get
  status = db->Get(read_options, "key4259", &value);
  if (status.ok()) {
    out << "GET key4259 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44762: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0915", "key9076", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0915, key9076) size=" << scan_result.size() << std::endl;
  }

  // op 44763: delete
  db->Delete(write_options, "key8431");

  // op 44764: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 44765: get
  status = db->Get(read_options, "key1707", &value);
  if (status.ok()) {
    out << "GET key1707 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44766: put
  db->Put(write_options, "key11722", "value17724");

  // op 44767: put
  db->Put(write_options, "key9917", "value27387");

  // op 44768: delete
  db->Delete(write_options, "key10695");

  // op 44769: put
  db->Put(write_options, "key6415", "value45843");

  // op 44770: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 44771: delete
  db->Delete(write_options, "key10988");

  // op 44772: put
  db->Put(write_options, "key11265", "value6115");

  // op 44773: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5757", "key6349", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5757, key6349) size=" << scan_result.size() << std::endl;
  }

  // op 44774: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4149", "key10117", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4149, key10117) size=" << scan_result.size() << std::endl;
  }

  // op 44775: put
  db->Put(write_options, "key0228", "value117354");

  // op 44776: put
  db->Put(write_options, "key0588", "value80120");

  // op 44777: put
  db->Put(write_options, "key1399", "value46980");

  // op 44778: put
  db->Put(write_options, "key1109", "value11669");

  // op 44779: get
  status = db->Get(read_options, "key2680", &value);
  if (status.ok()) {
    out << "GET key2680 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44780: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9901", "key10865", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9901, key10865) size=" << scan_result.size() << std::endl;
  }

  // op 44781: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5863", "key6947", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5863, key6947) size=" << scan_result.size() << std::endl;
  }

  // op 44782: get
  status = db->Get(read_options, "key9331", &value);
  if (status.ok()) {
    out << "GET key9331 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44783: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0932", "key2830", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0932, key2830) size=" << scan_result.size() << std::endl;
  }

  // op 44784: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1993", "key5762", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1993, key5762) size=" << scan_result.size() << std::endl;
  }

  // op 44785: get
  status = db->Get(read_options, "key6344", &value);
  if (status.ok()) {
    out << "GET key6344 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44786: put
  db->Put(write_options, "key10694", "value43415");

  // op 44787: get
  status = db->Get(read_options, "key0790", &value);
  if (status.ok()) {
    out << "GET key0790 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44788: get
  status = db->Get(read_options, "key2489", &value);
  if (status.ok()) {
    out << "GET key2489 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44789: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2248", "key5646", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2248, key5646) size=" << scan_result.size() << std::endl;
  }

  // op 44790: delete
  db->Delete(write_options, "key8040");

  // op 44791: delete_range
  status = db->DeleteRange(write_options, "key0697", "key6541");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 44792: get
  status = db->Get(read_options, "key9720", &value);
  if (status.ok()) {
    out << "GET key9720 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44793: put
  db->Put(write_options, "key11534", "value54809");

  // op 44794: delete
  db->Delete(write_options, "key0400");

  // op 44795: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3236", "key11319", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3236, key11319) size=" << scan_result.size() << std::endl;
  }

  // op 44796: get
  status = db->Get(read_options, "key5642", &value);
  if (status.ok()) {
    out << "GET key5642 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44797: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 44798: put
  db->Put(write_options, "key10815", "value26923");

  // op 44799: put
  db->Put(write_options, "key7016", "value113061");

  // op 44800: put
  db->Put(write_options, "key0906", "value103546");

  // op 44801: delete_range
  status = db->DeleteRange(write_options, "key1315", "key1394");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 44802: get
  status = db->Get(read_options, "key10645", &value);
  if (status.ok()) {
    out << "GET key10645 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44803: put
  db->Put(write_options, "key11196", "value0664");

  // op 44804: delete_range
  status = db->DeleteRange(write_options, "key2717", "key3317");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 44805: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2189", "key10768", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2189, key10768) size=" << scan_result.size() << std::endl;
  }

  // op 44806: put
  db->Put(write_options, "key8681", "value116467");

  // op 44807: get
  status = db->Get(read_options, "key10764", &value);
  if (status.ok()) {
    out << "GET key10764 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44808: delete_range
  status = db->DeleteRange(write_options, "key1113", "key7166");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 44809: delete_range
  status = db->DeleteRange(write_options, "key0498", "key7133");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 44810: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5806", "key6609", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5806, key6609) size=" << scan_result.size() << std::endl;
  }

  // op 44811: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2893", "key5553", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2893, key5553) size=" << scan_result.size() << std::endl;
  }

  // op 44812: get
  status = db->Get(read_options, "key7107", &value);
  if (status.ok()) {
    out << "GET key7107 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44813: put
  db->Put(write_options, "key1333", "value48111");

  // op 44814: get
  status = db->Get(read_options, "key9865", &value);
  if (status.ok()) {
    out << "GET key9865 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44815: get
  status = db->Get(read_options, "key9374", &value);
  if (status.ok()) {
    out << "GET key9374 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44816: get
  status = db->Get(read_options, "key7380", &value);
  if (status.ok()) {
    out << "GET key7380 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44817: put
  db->Put(write_options, "key6951", "value73121");

  // op 44818: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9155", "key10350", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9155, key10350) size=" << scan_result.size() << std::endl;
  }

  // op 44819: get
  status = db->Get(read_options, "key8492", &value);
  if (status.ok()) {
    out << "GET key8492 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44820: delete
  db->Delete(write_options, "key2813");

  // op 44821: delete
  db->Delete(write_options, "key11579");

  // op 44822: put
  db->Put(write_options, "key9950", "value84790");

  // op 44823: put
  db->Put(write_options, "key7198", "value39714");

  // op 44824: put
  db->Put(write_options, "key4087", "value68676");

  // op 44825: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3252", "key5896", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3252, key5896) size=" << scan_result.size() << std::endl;
  }

  // op 44826: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6846", "key9921", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6846, key9921) size=" << scan_result.size() << std::endl;
  }

  // op 44827: put
  db->Put(write_options, "key4635", "value73875");

  // op 44828: delete
  db->Delete(write_options, "key2164");

  // op 44829: put
  db->Put(write_options, "key10490", "value49000");

  // op 44830: delete_range
  status = db->DeleteRange(write_options, "key3637", "key8885");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 44831: get
  status = db->Get(read_options, "key9319", &value);
  if (status.ok()) {
    out << "GET key9319 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44832: put
  db->Put(write_options, "key9701", "value42139");

  // op 44833: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2647", "key4087", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2647, key4087) size=" << scan_result.size() << std::endl;
  }

  // op 44834: put
  db->Put(write_options, "key2803", "value54748");

  // op 44835: delete
  db->Delete(write_options, "key11628");

  // op 44836: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 44837: get
  status = db->Get(read_options, "key6151", &value);
  if (status.ok()) {
    out << "GET key6151 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44838: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6236", "key7257", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6236, key7257) size=" << scan_result.size() << std::endl;
  }

  // op 44839: put
  db->Put(write_options, "key5016", "value11695");

  // op 44840: put
  db->Put(write_options, "key4546", "value39977");

  // op 44841: get
  status = db->Get(read_options, "key7713", &value);
  if (status.ok()) {
    out << "GET key7713 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44842: get
  status = db->Get(read_options, "key8308", &value);
  if (status.ok()) {
    out << "GET key8308 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44843: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 44844: delete
  db->Delete(write_options, "key1427");

  // op 44845: put
  db->Put(write_options, "key0945", "value99069");

  // op 44846: delete_range
  status = db->DeleteRange(write_options, "key1153", "key10692");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 44847: put
  db->Put(write_options, "key0603", "value1164");

  // op 44848: delete_range
  status = db->DeleteRange(write_options, "key10476", "key10797");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 44849: put
  db->Put(write_options, "key1168", "value9346");

  // op 44850: put
  db->Put(write_options, "key0656", "value40958");

  // op 44851: put
  db->Put(write_options, "key1211", "value107016");

  // op 44852: delete_range
  status = db->DeleteRange(write_options, "key9718", "key11059");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 44853: put
  db->Put(write_options, "key0023", "value52992");

  // op 44854: get
  status = db->Get(read_options, "key9183", &value);
  if (status.ok()) {
    out << "GET key9183 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44855: put
  db->Put(write_options, "key8871", "value69828");

  // op 44856: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4468", "key5065", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4468, key5065) size=" << scan_result.size() << std::endl;
  }

  // op 44857: put
  db->Put(write_options, "key0670", "value109247");

  // op 44858: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2954", "key9741", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2954, key9741) size=" << scan_result.size() << std::endl;
  }

  // op 44859: put
  db->Put(write_options, "key4332", "value37581");

  // op 44860: put
  db->Put(write_options, "key4941", "value3344");

  // op 44861: put
  db->Put(write_options, "key0513", "value41200");

  // op 44862: put
  db->Put(write_options, "key11667", "value20409");

  // op 44863: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1721", "key3039", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1721, key3039) size=" << scan_result.size() << std::endl;
  }

  // op 44864: get
  status = db->Get(read_options, "key3829", &value);
  if (status.ok()) {
    out << "GET key3829 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44865: get
  status = db->Get(read_options, "key4779", &value);
  if (status.ok()) {
    out << "GET key4779 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44866: put
  db->Put(write_options, "key11152", "value74028");

  // op 44867: put
  db->Put(write_options, "key4290", "value49797");

  // op 44868: get
  status = db->Get(read_options, "key7340", &value);
  if (status.ok()) {
    out << "GET key7340 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44869: get
  status = db->Get(read_options, "key3475", &value);
  if (status.ok()) {
    out << "GET key3475 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44870: get
  status = db->Get(read_options, "key0569", &value);
  if (status.ok()) {
    out << "GET key0569 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44871: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1713", "key2303", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1713, key2303) size=" << scan_result.size() << std::endl;
  }

  // op 44872: delete_range
  status = db->DeleteRange(write_options, "key1183", "key11392");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 44873: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4458", "key9685", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4458, key9685) size=" << scan_result.size() << std::endl;
  }

  // op 44874: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2242", "key10167", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2242, key10167) size=" << scan_result.size() << std::endl;
  }

  // op 44875: put
  db->Put(write_options, "key3552", "value2653");

  // op 44876: delete_range
  status = db->DeleteRange(write_options, "key0810", "key4491");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 44877: get
  status = db->Get(read_options, "key6223", &value);
  if (status.ok()) {
    out << "GET key6223 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44878: delete
  db->Delete(write_options, "key2123");

  // op 44879: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5747", "key7315", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5747, key7315) size=" << scan_result.size() << std::endl;
  }

  // op 44880: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1812", "key11188", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1812, key11188) size=" << scan_result.size() << std::endl;
  }

  // op 44881: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2655", "key3275", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2655, key3275) size=" << scan_result.size() << std::endl;
  }

  // op 44882: put
  db->Put(write_options, "key1928", "value54985");

  // op 44883: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0441", "key4598", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0441, key4598) size=" << scan_result.size() << std::endl;
  }

  // op 44884: delete_range
  status = db->DeleteRange(write_options, "key3279", "key7276");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 44885: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1233", "key1880", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1233, key1880) size=" << scan_result.size() << std::endl;
  }

  // op 44886: put
  db->Put(write_options, "key4668", "value12446");

  // op 44887: put
  db->Put(write_options, "key5530", "value71441");

  // op 44888: put
  db->Put(write_options, "key11167", "value0639");

  // op 44889: put
  db->Put(write_options, "key8388", "value30386");

  // op 44890: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 44891: put
  db->Put(write_options, "key11422", "value86748");

  // op 44892: get
  status = db->Get(read_options, "key7685", &value);
  if (status.ok()) {
    out << "GET key7685 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44893: put
  db->Put(write_options, "key0983", "value46728");

  // op 44894: put
  db->Put(write_options, "key9279", "value30309");

  // op 44895: put
  db->Put(write_options, "key6395", "value0079");

  // op 44896: put
  db->Put(write_options, "key2410", "value46748");

  // op 44897: delete_range
  status = db->DeleteRange(write_options, "key6863", "key11377");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 44898: put
  db->Put(write_options, "key2428", "value74915");

  // op 44899: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 44900: get
  status = db->Get(read_options, "key3942", &value);
  if (status.ok()) {
    out << "GET key3942 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44901: put
  db->Put(write_options, "key9662", "value15199");

  // op 44902: delete_range
  status = db->DeleteRange(write_options, "key6059", "key6298");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 44903: delete
  db->Delete(write_options, "key2952");

  // op 44904: delete_range
  status = db->DeleteRange(write_options, "key2613", "key7822");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 44905: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 44906: delete_range
  status = db->DeleteRange(write_options, "key5656", "key9811");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 44907: put
  db->Put(write_options, "key6510", "value116929");

  // op 44908: put
  db->Put(write_options, "key4273", "value72153");

  // op 44909: put
  db->Put(write_options, "key4969", "value16936");

  // op 44910: delete
  db->Delete(write_options, "key10910");

  // op 44911: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9127", "key10984", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9127, key10984) size=" << scan_result.size() << std::endl;
  }

  // op 44912: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4356", "key9658", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4356, key9658) size=" << scan_result.size() << std::endl;
  }

  // op 44913: delete
  db->Delete(write_options, "key1709");

  // op 44914: put
  db->Put(write_options, "key8156", "value24353");

  // op 44915: put
  db->Put(write_options, "key7308", "value28085");

  // op 44916: put
  db->Put(write_options, "key3999", "value86171");

  // op 44917: put
  db->Put(write_options, "key9780", "value21060");

  // op 44918: delete
  db->Delete(write_options, "key7058");

  // op 44919: get
  status = db->Get(read_options, "key6771", &value);
  if (status.ok()) {
    out << "GET key6771 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44920: put
  db->Put(write_options, "key11594", "value10118");

  // op 44921: delete
  db->Delete(write_options, "key1856");

  // op 44922: put
  db->Put(write_options, "key11171", "value25480");

  // op 44923: delete_range
  status = db->DeleteRange(write_options, "key7536", "key7924");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 44924: put
  db->Put(write_options, "key9770", "value95686");

  // op 44925: put
  db->Put(write_options, "key1607", "value6996");

  // op 44926: delete
  db->Delete(write_options, "key2374");

  // op 44927: delete
  db->Delete(write_options, "key5834");

  // op 44928: put
  db->Put(write_options, "key7293", "value34233");

  // op 44929: delete
  db->Delete(write_options, "key2020");

  // op 44930: get
  status = db->Get(read_options, "key11165", &value);
  if (status.ok()) {
    out << "GET key11165 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44931: get
  status = db->Get(read_options, "key8029", &value);
  if (status.ok()) {
    out << "GET key8029 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44932: put
  db->Put(write_options, "key4533", "value47360");

  // op 44933: put
  db->Put(write_options, "key3159", "value50532");

  // op 44934: delete
  db->Delete(write_options, "key4461");

  // op 44935: delete_range
  status = db->DeleteRange(write_options, "key1956", "key4864");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 44936: get
  status = db->Get(read_options, "key11395", &value);
  if (status.ok()) {
    out << "GET key11395 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44937: put
  db->Put(write_options, "key11650", "value102038");

  // op 44938: put
  db->Put(write_options, "key8783", "value14925");

  // op 44939: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7736", "key9008", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7736, key9008) size=" << scan_result.size() << std::endl;
  }

  // op 44940: put
  db->Put(write_options, "key1508", "value99997");

  // op 44941: get
  status = db->Get(read_options, "key3815", &value);
  if (status.ok()) {
    out << "GET key3815 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44942: put
  db->Put(write_options, "key7212", "value85814");

  // op 44943: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 44944: get
  status = db->Get(read_options, "key8818", &value);
  if (status.ok()) {
    out << "GET key8818 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44945: put
  db->Put(write_options, "key7116", "value87337");

  // op 44946: get
  status = db->Get(read_options, "key2943", &value);
  if (status.ok()) {
    out << "GET key2943 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44947: get
  status = db->Get(read_options, "key9227", &value);
  if (status.ok()) {
    out << "GET key9227 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44948: get
  status = db->Get(read_options, "key0394", &value);
  if (status.ok()) {
    out << "GET key0394 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44949: delete
  db->Delete(write_options, "key2188");

  // op 44950: put
  db->Put(write_options, "key7210", "value17546");

  // op 44951: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1509", "key2135", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1509, key2135) size=" << scan_result.size() << std::endl;
  }

  // op 44952: put
  db->Put(write_options, "key1821", "value110327");

  // op 44953: put
  db->Put(write_options, "key5711", "value84893");

  // op 44954: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2604", "key5468", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2604, key5468) size=" << scan_result.size() << std::endl;
  }

  // op 44955: get
  status = db->Get(read_options, "key0557", &value);
  if (status.ok()) {
    out << "GET key0557 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44956: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1431", "key6216", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1431, key6216) size=" << scan_result.size() << std::endl;
  }

  // op 44957: get
  status = db->Get(read_options, "key10227", &value);
  if (status.ok()) {
    out << "GET key10227 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44958: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3780", "key4689", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3780, key4689) size=" << scan_result.size() << std::endl;
  }

  // op 44959: put
  db->Put(write_options, "key3883", "value53929");

  // op 44960: put
  db->Put(write_options, "key10074", "value73515");

  // op 44961: get
  status = db->Get(read_options, "key4905", &value);
  if (status.ok()) {
    out << "GET key4905 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44962: put
  db->Put(write_options, "key4601", "value36139");

  // op 44963: delete_range
  status = db->DeleteRange(write_options, "key10118", "key10710");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 44964: put
  db->Put(write_options, "key2873", "value37019");

  // op 44965: delete
  db->Delete(write_options, "key11568");

  // op 44966: put
  db->Put(write_options, "key1376", "value6605");

  // op 44967: put
  db->Put(write_options, "key8449", "value32100");

  // op 44968: get
  status = db->Get(read_options, "key4370", &value);
  if (status.ok()) {
    out << "GET key4370 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44969: put
  db->Put(write_options, "key4512", "value30895");

  // op 44970: put
  db->Put(write_options, "key0713", "value81938");

  // op 44971: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 44972: delete
  db->Delete(write_options, "key8517");

  // op 44973: get
  status = db->Get(read_options, "key8595", &value);
  if (status.ok()) {
    out << "GET key8595 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44974: put
  db->Put(write_options, "key0310", "value94103");

  // op 44975: put
  db->Put(write_options, "key8523", "value107748");

  // op 44976: get
  status = db->Get(read_options, "key2562", &value);
  if (status.ok()) {
    out << "GET key2562 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44977: get
  status = db->Get(read_options, "key7054", &value);
  if (status.ok()) {
    out << "GET key7054 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44978: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 44979: put
  db->Put(write_options, "key2853", "value100605");

  // op 44980: put
  db->Put(write_options, "key8207", "value3254");

  // op 44981: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6124", "key11014", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6124, key11014) size=" << scan_result.size() << std::endl;
  }

  // op 44982: get
  status = db->Get(read_options, "key11196", &value);
  if (status.ok()) {
    out << "GET key11196 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44983: put
  db->Put(write_options, "key3729", "value91939");

  // op 44984: delete
  db->Delete(write_options, "key4012");

  // op 44985: put
  db->Put(write_options, "key4332", "value47927");

  // op 44986: put
  db->Put(write_options, "key4947", "value49880");

  // op 44987: put
  db->Put(write_options, "key3345", "value31814");

  // op 44988: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5869", "key11321", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5869, key11321) size=" << scan_result.size() << std::endl;
  }

  // op 44989: put
  db->Put(write_options, "key6864", "value41882");

  // op 44990: put
  db->Put(write_options, "key1342", "value44783");

  // op 44991: get
  status = db->Get(read_options, "key10907", &value);
  if (status.ok()) {
    out << "GET key10907 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44992: put
  db->Put(write_options, "key8585", "value72073");

  // op 44993: delete_range
  status = db->DeleteRange(write_options, "key7438", "key8019");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 44994: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2477", "key11664", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2477, key11664) size=" << scan_result.size() << std::endl;
  }

  // op 44995: get
  status = db->Get(read_options, "key6152", &value);
  if (status.ok()) {
    out << "GET key6152 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44996: get
  status = db->Get(read_options, "key2995", &value);
  if (status.ok()) {
    out << "GET key2995 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 44997: put
  db->Put(write_options, "key7801", "value6164");

  // op 44998: put
  db->Put(write_options, "key5487", "value39950");

  // op 44999: put
  db->Put(write_options, "key7532", "value94473");

  // op 45000: put
  db->Put(write_options, "key0080", "value115631");

  // op 45001: put
  db->Put(write_options, "key10695", "value75521");

  // op 45002: delete
  db->Delete(write_options, "key8286");

  // op 45003: put
  db->Put(write_options, "key2841", "value88235");

  // op 45004: get
  status = db->Get(read_options, "key0445", &value);
  if (status.ok()) {
    out << "GET key0445 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45005: put
  db->Put(write_options, "key7021", "value112961");

  // op 45006: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8884", "key9092", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8884, key9092) size=" << scan_result.size() << std::endl;
  }

  // op 45007: delete
  db->Delete(write_options, "key8365");

  // op 45008: delete_range
  status = db->DeleteRange(write_options, "key1449", "key11537");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 45009: put
  db->Put(write_options, "key9947", "value68972");

  // op 45010: delete
  db->Delete(write_options, "key8297");

  // op 45011: delete_range
  status = db->DeleteRange(write_options, "key2544", "key3580");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 45012: get
  status = db->Get(read_options, "key6354", &value);
  if (status.ok()) {
    out << "GET key6354 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45013: put
  db->Put(write_options, "key3293", "value30321");

  // op 45014: get
  status = db->Get(read_options, "key0729", &value);
  if (status.ok()) {
    out << "GET key0729 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45015: get
  status = db->Get(read_options, "key1017", &value);
  if (status.ok()) {
    out << "GET key1017 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45016: get
  status = db->Get(read_options, "key1139", &value);
  if (status.ok()) {
    out << "GET key1139 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45017: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1976", "key3328", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1976, key3328) size=" << scan_result.size() << std::endl;
  }

  // op 45018: put
  db->Put(write_options, "key9512", "value110638");

  // op 45019: put
  db->Put(write_options, "key8214", "value65860");

  // op 45020: get
  status = db->Get(read_options, "key6480", &value);
  if (status.ok()) {
    out << "GET key6480 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45021: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9547", "key10719", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9547, key10719) size=" << scan_result.size() << std::endl;
  }

  // op 45022: put
  db->Put(write_options, "key8031", "value11242");

  // op 45023: put
  db->Put(write_options, "key1614", "value71791");

  // op 45024: get
  status = db->Get(read_options, "key8015", &value);
  if (status.ok()) {
    out << "GET key8015 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45025: put
  db->Put(write_options, "key2195", "value60574");

  // op 45026: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4781", "key4787", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4781, key4787) size=" << scan_result.size() << std::endl;
  }

  // op 45027: delete_range
  status = db->DeleteRange(write_options, "key6276", "key10447");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 45028: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9413", "key11028", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9413, key11028) size=" << scan_result.size() << std::endl;
  }

  // op 45029: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8641", "key8754", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8641, key8754) size=" << scan_result.size() << std::endl;
  }

  // op 45030: put
  db->Put(write_options, "key3016", "value65735");

  // op 45031: put
  db->Put(write_options, "key10621", "value94575");

  // op 45032: delete
  db->Delete(write_options, "key6045");

  // op 45033: put
  db->Put(write_options, "key0764", "value37450");

  // op 45034: put
  db->Put(write_options, "key10293", "value62978");

  // op 45035: put
  db->Put(write_options, "key4739", "value109880");

  // op 45036: get
  status = db->Get(read_options, "key0885", &value);
  if (status.ok()) {
    out << "GET key0885 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45037: delete
  db->Delete(write_options, "key9362");

  // op 45038: put
  db->Put(write_options, "key7774", "value12972");

  // op 45039: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2277", "key8335", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2277, key8335) size=" << scan_result.size() << std::endl;
  }

  // op 45040: delete
  db->Delete(write_options, "key8630");

  // op 45041: delete_range
  status = db->DeleteRange(write_options, "key6046", "key7144");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 45042: put
  db->Put(write_options, "key7637", "value110227");

  // op 45043: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 45044: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7180", "key7433", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7180, key7433) size=" << scan_result.size() << std::endl;
  }

  // op 45045: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0650", "key5376", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0650, key5376) size=" << scan_result.size() << std::endl;
  }

  // op 45046: get
  status = db->Get(read_options, "key7743", &value);
  if (status.ok()) {
    out << "GET key7743 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45047: put
  db->Put(write_options, "key7873", "value51540");

  // op 45048: delete_range
  status = db->DeleteRange(write_options, "key2116", "key11394");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 45049: get
  status = db->Get(read_options, "key9722", &value);
  if (status.ok()) {
    out << "GET key9722 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45050: put
  db->Put(write_options, "key6699", "value44565");

  // op 45051: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3040", "key4030", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3040, key4030) size=" << scan_result.size() << std::endl;
  }

  // op 45052: delete
  db->Delete(write_options, "key6304");

  // op 45053: put
  db->Put(write_options, "key4086", "value55220");

  // op 45054: delete
  db->Delete(write_options, "key9261");

  // op 45055: get
  status = db->Get(read_options, "key5824", &value);
  if (status.ok()) {
    out << "GET key5824 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45056: delete
  db->Delete(write_options, "key7532");

  // op 45057: delete
  db->Delete(write_options, "key11177");

  // op 45058: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1375", "key4923", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1375, key4923) size=" << scan_result.size() << std::endl;
  }

  // op 45059: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5811", "key9592", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5811, key9592) size=" << scan_result.size() << std::endl;
  }

  // op 45060: delete_range
  status = db->DeleteRange(write_options, "key2533", "key11556");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 45061: put
  db->Put(write_options, "key5138", "value76176");

  // op 45062: delete_range
  status = db->DeleteRange(write_options, "key7877", "key10800");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 45063: put
  db->Put(write_options, "key5968", "value84358");

  // op 45064: delete_range
  status = db->DeleteRange(write_options, "key1123", "key2934");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 45065: delete_range
  status = db->DeleteRange(write_options, "key3104", "key7873");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 45066: delete_range
  status = db->DeleteRange(write_options, "key2874", "key8390");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 45067: delete_range
  status = db->DeleteRange(write_options, "key6493", "key10688");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 45068: put
  db->Put(write_options, "key9858", "value66492");

  // op 45069: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0660", "key6703", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0660, key6703) size=" << scan_result.size() << std::endl;
  }

  // op 45070: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 45071: get
  status = db->Get(read_options, "key8839", &value);
  if (status.ok()) {
    out << "GET key8839 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45072: put
  db->Put(write_options, "key0550", "value110483");

  // op 45073: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 45074: put
  db->Put(write_options, "key8839", "value77756");

  // op 45075: put
  db->Put(write_options, "key7598", "value31772");

  // op 45076: put
  db->Put(write_options, "key0448", "value102487");

  // op 45077: put
  db->Put(write_options, "key3113", "value14908");

  // op 45078: put
  db->Put(write_options, "key10115", "value96572");

  // op 45079: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 45080: get
  status = db->Get(read_options, "key10206", &value);
  if (status.ok()) {
    out << "GET key10206 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45081: delete
  db->Delete(write_options, "key1829");

  // op 45082: put
  db->Put(write_options, "key10011", "value104295");

  // op 45083: put
  db->Put(write_options, "key4356", "value80763");

  // op 45084: get
  status = db->Get(read_options, "key5622", &value);
  if (status.ok()) {
    out << "GET key5622 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45085: put
  db->Put(write_options, "key0613", "value84200");

  // op 45086: delete
  db->Delete(write_options, "key5395");

  // op 45087: put
  db->Put(write_options, "key8278", "value23504");

  // op 45088: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 45089: get
  status = db->Get(read_options, "key5827", &value);
  if (status.ok()) {
    out << "GET key5827 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45090: put
  db->Put(write_options, "key4654", "value69601");

  // op 45091: get
  status = db->Get(read_options, "key3068", &value);
  if (status.ok()) {
    out << "GET key3068 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45092: delete
  db->Delete(write_options, "key3086");

  // op 45093: delete
  db->Delete(write_options, "key6780");

  // op 45094: delete
  db->Delete(write_options, "key4543");

  // op 45095: get
  status = db->Get(read_options, "key8318", &value);
  if (status.ok()) {
    out << "GET key8318 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45096: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8083", "key10581", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8083, key10581) size=" << scan_result.size() << std::endl;
  }

  // op 45097: put
  db->Put(write_options, "key11492", "value100154");

  // op 45098: put
  db->Put(write_options, "key8781", "value47150");

  // op 45099: delete
  db->Delete(write_options, "key8676");

  // op 45100: delete
  db->Delete(write_options, "key9245");

  // op 45101: get
  status = db->Get(read_options, "key6665", &value);
  if (status.ok()) {
    out << "GET key6665 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45102: put
  db->Put(write_options, "key7862", "value74998");

  // op 45103: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 45104: delete_range
  status = db->DeleteRange(write_options, "key6775", "key10273");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 45105: put
  db->Put(write_options, "key3164", "value23168");

  // op 45106: get
  status = db->Get(read_options, "key2640", &value);
  if (status.ok()) {
    out << "GET key2640 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45107: put
  db->Put(write_options, "key1163", "value26366");

  // op 45108: get
  status = db->Get(read_options, "key0742", &value);
  if (status.ok()) {
    out << "GET key0742 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45109: put
  db->Put(write_options, "key9449", "value19654");

  // op 45110: put
  db->Put(write_options, "key9828", "value107835");

  // op 45111: put
  db->Put(write_options, "key2516", "value49260");

  // op 45112: get
  status = db->Get(read_options, "key9513", &value);
  if (status.ok()) {
    out << "GET key9513 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45113: put
  db->Put(write_options, "key5604", "value5186");

  // op 45114: put
  db->Put(write_options, "key6192", "value16804");

  // op 45115: put
  db->Put(write_options, "key2520", "value111929");

  // op 45116: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2148", "key9386", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2148, key9386) size=" << scan_result.size() << std::endl;
  }

  // op 45117: put
  db->Put(write_options, "key3301", "value65864");

  // op 45118: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 45119: put
  db->Put(write_options, "key0208", "value8575");

  // op 45120: delete_range
  status = db->DeleteRange(write_options, "key2100", "key9421");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 45121: put
  db->Put(write_options, "key10425", "value76835");

  // op 45122: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2316", "key5123", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2316, key5123) size=" << scan_result.size() << std::endl;
  }

  // op 45123: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7402", "key10049", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7402, key10049) size=" << scan_result.size() << std::endl;
  }

  // op 45124: get
  status = db->Get(read_options, "key3861", &value);
  if (status.ok()) {
    out << "GET key3861 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45125: put
  db->Put(write_options, "key6408", "value57712");

  // op 45126: get
  status = db->Get(read_options, "key2879", &value);
  if (status.ok()) {
    out << "GET key2879 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45127: put
  db->Put(write_options, "key3828", "value31236");

  // op 45128: get
  status = db->Get(read_options, "key10777", &value);
  if (status.ok()) {
    out << "GET key10777 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45129: put
  db->Put(write_options, "key11209", "value47085");

  // op 45130: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6667", "key10302", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6667, key10302) size=" << scan_result.size() << std::endl;
  }

  // op 45131: put
  db->Put(write_options, "key6769", "value24850");

  // op 45132: delete
  db->Delete(write_options, "key6542");

  // op 45133: delete_range
  status = db->DeleteRange(write_options, "key1963", "key7338");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 45134: get
  status = db->Get(read_options, "key5670", &value);
  if (status.ok()) {
    out << "GET key5670 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45135: delete_range
  status = db->DeleteRange(write_options, "key0734", "key5095");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 45136: delete_range
  status = db->DeleteRange(write_options, "key2542", "key5288");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 45137: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1935", "key11376", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1935, key11376) size=" << scan_result.size() << std::endl;
  }

  // op 45138: delete
  db->Delete(write_options, "key10995");

  // op 45139: get
  status = db->Get(read_options, "key9956", &value);
  if (status.ok()) {
    out << "GET key9956 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45140: put
  db->Put(write_options, "key6538", "value83262");

  // op 45141: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3767", "key7169", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3767, key7169) size=" << scan_result.size() << std::endl;
  }

  // op 45142: put
  db->Put(write_options, "key11154", "value30717");

  // op 45143: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4542", "key11260", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4542, key11260) size=" << scan_result.size() << std::endl;
  }

  // op 45144: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0510", "key11499", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0510, key11499) size=" << scan_result.size() << std::endl;
  }

  // op 45145: delete
  db->Delete(write_options, "key7244");

  // op 45146: put
  db->Put(write_options, "key2242", "value3277");

  // op 45147: get
  status = db->Get(read_options, "key8108", &value);
  if (status.ok()) {
    out << "GET key8108 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45148: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 45149: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1837", "key9228", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1837, key9228) size=" << scan_result.size() << std::endl;
  }

  // op 45150: put
  db->Put(write_options, "key6816", "value49209");

  // op 45151: delete
  db->Delete(write_options, "key8656");

  // op 45152: get
  status = db->Get(read_options, "key9719", &value);
  if (status.ok()) {
    out << "GET key9719 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45153: put
  db->Put(write_options, "key3330", "value38663");

  // op 45154: put
  db->Put(write_options, "key8554", "value107969");

  // op 45155: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0543", "key10541", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0543, key10541) size=" << scan_result.size() << std::endl;
  }

  // op 45156: put
  db->Put(write_options, "key11324", "value36999");

  // op 45157: delete_range
  status = db->DeleteRange(write_options, "key7650", "key9274");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 45158: get
  status = db->Get(read_options, "key2392", &value);
  if (status.ok()) {
    out << "GET key2392 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45159: put
  db->Put(write_options, "key8091", "value111103");

  // op 45160: delete
  db->Delete(write_options, "key5644");

  // op 45161: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4851", "key10736", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4851, key10736) size=" << scan_result.size() << std::endl;
  }

  // op 45162: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1532", "key3350", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1532, key3350) size=" << scan_result.size() << std::endl;
  }

  // op 45163: delete
  db->Delete(write_options, "key5448");

  // op 45164: get
  status = db->Get(read_options, "key0120", &value);
  if (status.ok()) {
    out << "GET key0120 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45165: delete_range
  status = db->DeleteRange(write_options, "key3389", "key4209");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 45166: put
  db->Put(write_options, "key1507", "value77115");

  // op 45167: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3538", "key3771", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3538, key3771) size=" << scan_result.size() << std::endl;
  }

  // op 45168: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0009", "key7809", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0009, key7809) size=" << scan_result.size() << std::endl;
  }

  // op 45169: get
  status = db->Get(read_options, "key7173", &value);
  if (status.ok()) {
    out << "GET key7173 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45170: put
  db->Put(write_options, "key8411", "value66820");

  // op 45171: delete
  db->Delete(write_options, "key6720");

  // op 45172: put
  db->Put(write_options, "key0746", "value57482");

  // op 45173: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1339", "key5316", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1339, key5316) size=" << scan_result.size() << std::endl;
  }

  // op 45174: get
  status = db->Get(read_options, "key5291", &value);
  if (status.ok()) {
    out << "GET key5291 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45175: delete
  db->Delete(write_options, "key4865");

  // op 45176: put
  db->Put(write_options, "key6456", "value83950");

  // op 45177: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5668", "key6427", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5668, key6427) size=" << scan_result.size() << std::endl;
  }

  // op 45178: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0910", "key4617", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0910, key4617) size=" << scan_result.size() << std::endl;
  }

  // op 45179: put
  db->Put(write_options, "key3665", "value117145");

  // op 45180: get
  status = db->Get(read_options, "key0481", &value);
  if (status.ok()) {
    out << "GET key0481 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45181: get
  status = db->Get(read_options, "key4554", &value);
  if (status.ok()) {
    out << "GET key4554 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45182: put
  db->Put(write_options, "key9211", "value93802");

  // op 45183: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2569", "key7199", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2569, key7199) size=" << scan_result.size() << std::endl;
  }

  // op 45184: get
  status = db->Get(read_options, "key4595", &value);
  if (status.ok()) {
    out << "GET key4595 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45185: delete
  db->Delete(write_options, "key6070");

  // op 45186: put
  db->Put(write_options, "key6377", "value44594");

  // op 45187: put
  db->Put(write_options, "key9786", "value1263");

  // op 45188: put
  db->Put(write_options, "key3225", "value27580");

  // op 45189: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7805", "key9944", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7805, key9944) size=" << scan_result.size() << std::endl;
  }

  // op 45190: delete_range
  status = db->DeleteRange(write_options, "key2614", "key3841");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 45191: put
  db->Put(write_options, "key9142", "value80264");

  // op 45192: put
  db->Put(write_options, "key2341", "value76855");

  // op 45193: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 45194: put
  db->Put(write_options, "key2314", "value91436");

  // op 45195: put
  db->Put(write_options, "key7807", "value96713");

  // op 45196: put
  db->Put(write_options, "key7973", "value83066");

  // op 45197: put
  db->Put(write_options, "key3973", "value62350");

  // op 45198: put
  db->Put(write_options, "key4699", "value5243");

  // op 45199: get
  status = db->Get(read_options, "key8030", &value);
  if (status.ok()) {
    out << "GET key8030 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45200: get
  status = db->Get(read_options, "key0793", &value);
  if (status.ok()) {
    out << "GET key0793 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45201: get
  status = db->Get(read_options, "key0654", &value);
  if (status.ok()) {
    out << "GET key0654 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45202: delete
  db->Delete(write_options, "key3009");

  // op 45203: get
  status = db->Get(read_options, "key6540", &value);
  if (status.ok()) {
    out << "GET key6540 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45204: put
  db->Put(write_options, "key5822", "value89964");

  // op 45205: get
  status = db->Get(read_options, "key7494", &value);
  if (status.ok()) {
    out << "GET key7494 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45206: get
  status = db->Get(read_options, "key4494", &value);
  if (status.ok()) {
    out << "GET key4494 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45207: put
  db->Put(write_options, "key0223", "value76331");

  // op 45208: get
  status = db->Get(read_options, "key3377", &value);
  if (status.ok()) {
    out << "GET key3377 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45209: delete_range
  status = db->DeleteRange(write_options, "key0693", "key9799");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 45210: put
  db->Put(write_options, "key7031", "value52207");

  // op 45211: put
  db->Put(write_options, "key6787", "value107747");

  // op 45212: delete_range
  status = db->DeleteRange(write_options, "key0912", "key8928");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 45213: put
  db->Put(write_options, "key8789", "value81011");

  // op 45214: put
  db->Put(write_options, "key4062", "value42068");

  // op 45215: get
  status = db->Get(read_options, "key3246", &value);
  if (status.ok()) {
    out << "GET key3246 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45216: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 45217: put
  db->Put(write_options, "key0865", "value74829");

  // op 45218: put
  db->Put(write_options, "key2439", "value2882");

  // op 45219: delete_range
  status = db->DeleteRange(write_options, "key5690", "key10773");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 45220: put
  db->Put(write_options, "key2881", "value17609");

  // op 45221: put
  db->Put(write_options, "key4582", "value73928");

  // op 45222: get
  status = db->Get(read_options, "key6756", &value);
  if (status.ok()) {
    out << "GET key6756 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45223: put
  db->Put(write_options, "key0449", "value59748");

  // op 45224: get
  status = db->Get(read_options, "key11026", &value);
  if (status.ok()) {
    out << "GET key11026 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45225: get
  status = db->Get(read_options, "key10219", &value);
  if (status.ok()) {
    out << "GET key10219 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45226: put
  db->Put(write_options, "key11105", "value12035");

  // op 45227: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9452", "key10393", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9452, key10393) size=" << scan_result.size() << std::endl;
  }

  // op 45228: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0688", "key2812", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0688, key2812) size=" << scan_result.size() << std::endl;
  }

  // op 45229: delete
  db->Delete(write_options, "key8007");

  // op 45230: put
  db->Put(write_options, "key9935", "value25722");

  // op 45231: put
  db->Put(write_options, "key0347", "value94386");

  // op 45232: put
  db->Put(write_options, "key2065", "value105878");

  // op 45233: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2793", "key11413", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2793, key11413) size=" << scan_result.size() << std::endl;
  }

  // op 45234: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7774", "key10868", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7774, key10868) size=" << scan_result.size() << std::endl;
  }

  // op 45235: get
  status = db->Get(read_options, "key6955", &value);
  if (status.ok()) {
    out << "GET key6955 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45236: put
  db->Put(write_options, "key5240", "value116967");

  // op 45237: get
  status = db->Get(read_options, "key1052", &value);
  if (status.ok()) {
    out << "GET key1052 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45238: delete
  db->Delete(write_options, "key7242");

  // op 45239: put
  db->Put(write_options, "key0559", "value100804");

  // op 45240: put
  db->Put(write_options, "key11022", "value17135");

  // op 45241: put
  db->Put(write_options, "key6323", "value33123");

  // op 45242: get
  status = db->Get(read_options, "key7452", &value);
  if (status.ok()) {
    out << "GET key7452 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45243: put
  db->Put(write_options, "key6054", "value85497");

  // op 45244: delete
  db->Delete(write_options, "key8681");

  // op 45245: get
  status = db->Get(read_options, "key2640", &value);
  if (status.ok()) {
    out << "GET key2640 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45246: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3454", "key6213", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3454, key6213) size=" << scan_result.size() << std::endl;
  }

  // op 45247: get
  status = db->Get(read_options, "key0968", &value);
  if (status.ok()) {
    out << "GET key0968 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45248: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8027", "key11330", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8027, key11330) size=" << scan_result.size() << std::endl;
  }

  // op 45249: delete_range
  status = db->DeleteRange(write_options, "key3035", "key3282");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 45250: put
  db->Put(write_options, "key1366", "value118063");

  // op 45251: put
  db->Put(write_options, "key0138", "value8818");

  // op 45252: put
  db->Put(write_options, "key0965", "value99791");

  // op 45253: get
  status = db->Get(read_options, "key5576", &value);
  if (status.ok()) {
    out << "GET key5576 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45254: delete_range
  status = db->DeleteRange(write_options, "key0401", "key8739");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 45255: put
  db->Put(write_options, "key4223", "value59383");

  // op 45256: put
  db->Put(write_options, "key9697", "value5595");

  // op 45257: delete_range
  status = db->DeleteRange(write_options, "key6460", "key6483");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 45258: get
  status = db->Get(read_options, "key5064", &value);
  if (status.ok()) {
    out << "GET key5064 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45259: put
  db->Put(write_options, "key3856", "value90045");

  // op 45260: delete
  db->Delete(write_options, "key5092");

  // op 45261: get
  status = db->Get(read_options, "key7466", &value);
  if (status.ok()) {
    out << "GET key7466 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45262: delete
  db->Delete(write_options, "key9680");

  // op 45263: put
  db->Put(write_options, "key3020", "value103127");

  // op 45264: delete_range
  status = db->DeleteRange(write_options, "key1960", "key8242");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 45265: delete
  db->Delete(write_options, "key5862");

  // op 45266: delete
  db->Delete(write_options, "key9503");

  // op 45267: put
  db->Put(write_options, "key8690", "value76199");

  // op 45268: put
  db->Put(write_options, "key11697", "value60295");

  // op 45269: put
  db->Put(write_options, "key10161", "value93656");

  // op 45270: put
  db->Put(write_options, "key1487", "value24911");

  // op 45271: get
  status = db->Get(read_options, "key3997", &value);
  if (status.ok()) {
    out << "GET key3997 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45272: delete
  db->Delete(write_options, "key6112");

  // op 45273: put
  db->Put(write_options, "key8777", "value76735");

  // op 45274: put
  db->Put(write_options, "key4337", "value29458");

  // op 45275: get
  status = db->Get(read_options, "key5008", &value);
  if (status.ok()) {
    out << "GET key5008 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45276: put
  db->Put(write_options, "key8922", "value15372");

  // op 45277: delete_range
  status = db->DeleteRange(write_options, "key4266", "key4788");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 45278: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 45279: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3669", "key10560", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3669, key10560) size=" << scan_result.size() << std::endl;
  }

  // op 45280: get
  status = db->Get(read_options, "key7144", &value);
  if (status.ok()) {
    out << "GET key7144 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45281: delete
  db->Delete(write_options, "key2653");

  // op 45282: delete
  db->Delete(write_options, "key0310");

  // op 45283: get
  status = db->Get(read_options, "key5160", &value);
  if (status.ok()) {
    out << "GET key5160 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45284: get
  status = db->Get(read_options, "key4912", &value);
  if (status.ok()) {
    out << "GET key4912 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45285: get
  status = db->Get(read_options, "key4269", &value);
  if (status.ok()) {
    out << "GET key4269 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45286: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 45287: put
  db->Put(write_options, "key1327", "value8619");

  // op 45288: get
  status = db->Get(read_options, "key0366", &value);
  if (status.ok()) {
    out << "GET key0366 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45289: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3409", "key6217", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3409, key6217) size=" << scan_result.size() << std::endl;
  }

  // op 45290: put
  db->Put(write_options, "key10606", "value55066");

  // op 45291: delete
  db->Delete(write_options, "key0695");

  // op 45292: put
  db->Put(write_options, "key8657", "value49063");

  // op 45293: put
  db->Put(write_options, "key9650", "value76102");

  // op 45294: put
  db->Put(write_options, "key4547", "value47968");

  // op 45295: delete
  db->Delete(write_options, "key8874");

  // op 45296: get
  status = db->Get(read_options, "key5652", &value);
  if (status.ok()) {
    out << "GET key5652 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45297: put
  db->Put(write_options, "key10700", "value39939");

  // op 45298: put
  db->Put(write_options, "key8052", "value95303");

  // op 45299: delete_range
  status = db->DeleteRange(write_options, "key6201", "key7312");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 45300: delete_range
  status = db->DeleteRange(write_options, "key1039", "key5641");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 45301: put
  db->Put(write_options, "key1842", "value82341");

  // op 45302: get
  status = db->Get(read_options, "key2080", &value);
  if (status.ok()) {
    out << "GET key2080 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45303: put
  db->Put(write_options, "key1843", "value1486");

  // op 45304: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3538", "key9222", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3538, key9222) size=" << scan_result.size() << std::endl;
  }

  // op 45305: put
  db->Put(write_options, "key1187", "value115416");

  // op 45306: put
  db->Put(write_options, "key7104", "value102572");

  // op 45307: delete_range
  status = db->DeleteRange(write_options, "key8981", "key9683");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 45308: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1730", "key8341", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1730, key8341) size=" << scan_result.size() << std::endl;
  }

  // op 45309: put
  db->Put(write_options, "key4952", "value9386");

  // op 45310: put
  db->Put(write_options, "key6024", "value52133");

  // op 45311: delete
  db->Delete(write_options, "key2942");

  // op 45312: delete_range
  status = db->DeleteRange(write_options, "key0079", "key2600");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 45313: get
  status = db->Get(read_options, "key5273", &value);
  if (status.ok()) {
    out << "GET key5273 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45314: delete
  db->Delete(write_options, "key3178");

  // op 45315: put
  db->Put(write_options, "key2195", "value24218");

  // op 45316: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 45317: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1284", "key8686", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1284, key8686) size=" << scan_result.size() << std::endl;
  }

  // op 45318: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3109", "key3775", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3109, key3775) size=" << scan_result.size() << std::endl;
  }

  // op 45319: get
  status = db->Get(read_options, "key7124", &value);
  if (status.ok()) {
    out << "GET key7124 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45320: put
  db->Put(write_options, "key2334", "value21603");

  // op 45321: delete_range
  status = db->DeleteRange(write_options, "key5184", "key7039");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 45322: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7528", "key10168", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7528, key10168) size=" << scan_result.size() << std::endl;
  }

  // op 45323: put
  db->Put(write_options, "key7060", "value34892");

  // op 45324: delete_range
  status = db->DeleteRange(write_options, "key5642", "key10254");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 45325: put
  db->Put(write_options, "key7200", "value22099");

  // op 45326: put
  db->Put(write_options, "key9472", "value89238");

  // op 45327: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4121", "key8591", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4121, key8591) size=" << scan_result.size() << std::endl;
  }

  // op 45328: put
  db->Put(write_options, "key6305", "value5789");

  // op 45329: put
  db->Put(write_options, "key9950", "value21531");

  // op 45330: put
  db->Put(write_options, "key4212", "value6455");

  // op 45331: put
  db->Put(write_options, "key0111", "value106400");

  // op 45332: put
  db->Put(write_options, "key8523", "value75386");

  // op 45333: delete
  db->Delete(write_options, "key5807");

  // op 45334: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3273", "key5397", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3273, key5397) size=" << scan_result.size() << std::endl;
  }

  // op 45335: put
  db->Put(write_options, "key2057", "value62647");

  // op 45336: delete_range
  status = db->DeleteRange(write_options, "key4842", "key8070");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 45337: put
  db->Put(write_options, "key8768", "value61446");

  // op 45338: get
  status = db->Get(read_options, "key9268", &value);
  if (status.ok()) {
    out << "GET key9268 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45339: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 45340: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1869", "key7852", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1869, key7852) size=" << scan_result.size() << std::endl;
  }

  // op 45341: get
  status = db->Get(read_options, "key3686", &value);
  if (status.ok()) {
    out << "GET key3686 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45342: delete
  db->Delete(write_options, "key7872");

  // op 45343: delete
  db->Delete(write_options, "key1685");

  // op 45344: get
  status = db->Get(read_options, "key9156", &value);
  if (status.ok()) {
    out << "GET key9156 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45345: delete
  db->Delete(write_options, "key2232");

  // op 45346: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1508", "key3997", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1508, key3997) size=" << scan_result.size() << std::endl;
  }

  // op 45347: put
  db->Put(write_options, "key9235", "value24061");

  // op 45348: put
  db->Put(write_options, "key3865", "value114601");

  // op 45349: put
  db->Put(write_options, "key2702", "value20147");

  // op 45350: put
  db->Put(write_options, "key8988", "value58610");

  // op 45351: delete_range
  status = db->DeleteRange(write_options, "key3294", "key8924");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 45352: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3045", "key5891", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3045, key5891) size=" << scan_result.size() << std::endl;
  }

  // op 45353: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0830", "key6815", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0830, key6815) size=" << scan_result.size() << std::endl;
  }

  // op 45354: put
  db->Put(write_options, "key1835", "value69170");

  // op 45355: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2087", "key10433", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2087, key10433) size=" << scan_result.size() << std::endl;
  }

  // op 45356: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10295", "key10510", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10295, key10510) size=" << scan_result.size() << std::endl;
  }

  // op 45357: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0063", "key10497", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0063, key10497) size=" << scan_result.size() << std::endl;
  }

  // op 45358: put
  db->Put(write_options, "key10400", "value32177");

  // op 45359: put
  db->Put(write_options, "key10722", "value74718");

  // op 45360: get
  status = db->Get(read_options, "key0452", &value);
  if (status.ok()) {
    out << "GET key0452 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45361: put
  db->Put(write_options, "key0220", "value69659");

  // op 45362: put
  db->Put(write_options, "key3764", "value102836");

  // op 45363: delete
  db->Delete(write_options, "key11693");

  // op 45364: get
  status = db->Get(read_options, "key11298", &value);
  if (status.ok()) {
    out << "GET key11298 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45365: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4441", "key8036", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4441, key8036) size=" << scan_result.size() << std::endl;
  }

  // op 45366: put
  db->Put(write_options, "key6864", "value45478");

  // op 45367: delete_range
  status = db->DeleteRange(write_options, "key9292", "key10081");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 45368: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3316", "key5038", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3316, key5038) size=" << scan_result.size() << std::endl;
  }

  // op 45369: put
  db->Put(write_options, "key10025", "value28387");

  // op 45370: put
  db->Put(write_options, "key11355", "value24727");

  // op 45371: delete_range
  status = db->DeleteRange(write_options, "key5354", "key11093");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 45372: put
  db->Put(write_options, "key3162", "value3397");

  // op 45373: get
  status = db->Get(read_options, "key1787", &value);
  if (status.ok()) {
    out << "GET key1787 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45374: get
  status = db->Get(read_options, "key1340", &value);
  if (status.ok()) {
    out << "GET key1340 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45375: put
  db->Put(write_options, "key9760", "value53661");

  // op 45376: put
  db->Put(write_options, "key4708", "value58214");

  // op 45377: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2533", "key10348", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2533, key10348) size=" << scan_result.size() << std::endl;
  }

  // op 45378: put
  db->Put(write_options, "key0513", "value43515");

  // op 45379: get
  status = db->Get(read_options, "key10082", &value);
  if (status.ok()) {
    out << "GET key10082 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45380: put
  db->Put(write_options, "key0794", "value51898");

  // op 45381: put
  db->Put(write_options, "key1035", "value90613");

  // op 45382: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2391", "key2839", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2391, key2839) size=" << scan_result.size() << std::endl;
  }

  // op 45383: get
  status = db->Get(read_options, "key6968", &value);
  if (status.ok()) {
    out << "GET key6968 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45384: put
  db->Put(write_options, "key5957", "value73558");

  // op 45385: put
  db->Put(write_options, "key3312", "value77120");

  // op 45386: put
  db->Put(write_options, "key8395", "value117445");

  // op 45387: get
  status = db->Get(read_options, "key2398", &value);
  if (status.ok()) {
    out << "GET key2398 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45388: get
  status = db->Get(read_options, "key11100", &value);
  if (status.ok()) {
    out << "GET key11100 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45389: get
  status = db->Get(read_options, "key5294", &value);
  if (status.ok()) {
    out << "GET key5294 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45390: delete_range
  status = db->DeleteRange(write_options, "key3793", "key9513");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 45391: put
  db->Put(write_options, "key4117", "value101488");

  // op 45392: get
  status = db->Get(read_options, "key1973", &value);
  if (status.ok()) {
    out << "GET key1973 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45393: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4065", "key5523", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4065, key5523) size=" << scan_result.size() << std::endl;
  }

  // op 45394: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3496", "key8745", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3496, key8745) size=" << scan_result.size() << std::endl;
  }

  // op 45395: put
  db->Put(write_options, "key3598", "value8699");

  // op 45396: delete
  db->Delete(write_options, "key10467");

  // op 45397: put
  db->Put(write_options, "key2537", "value23611");

  // op 45398: get
  status = db->Get(read_options, "key1107", &value);
  if (status.ok()) {
    out << "GET key1107 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45399: put
  db->Put(write_options, "key3905", "value46213");

  // op 45400: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6020", "key8248", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6020, key8248) size=" << scan_result.size() << std::endl;
  }

  // op 45401: put
  db->Put(write_options, "key11115", "value87522");

  // op 45402: put
  db->Put(write_options, "key2014", "value24541");

  // op 45403: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3617", "key7814", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3617, key7814) size=" << scan_result.size() << std::endl;
  }

  // op 45404: get
  status = db->Get(read_options, "key3129", &value);
  if (status.ok()) {
    out << "GET key3129 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45405: get
  status = db->Get(read_options, "key11640", &value);
  if (status.ok()) {
    out << "GET key11640 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45406: put
  db->Put(write_options, "key8951", "value6559");

  // op 45407: put
  db->Put(write_options, "key0401", "value59355");

  // op 45408: put
  db->Put(write_options, "key3874", "value106398");

  // op 45409: put
  db->Put(write_options, "key7761", "value115158");

  // op 45410: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1329", "key10583", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1329, key10583) size=" << scan_result.size() << std::endl;
  }

  // op 45411: delete_range
  status = db->DeleteRange(write_options, "key1129", "key9094");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 45412: get
  status = db->Get(read_options, "key11027", &value);
  if (status.ok()) {
    out << "GET key11027 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45413: put
  db->Put(write_options, "key9289", "value24670");

  // op 45414: get
  status = db->Get(read_options, "key6599", &value);
  if (status.ok()) {
    out << "GET key6599 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45415: delete_range
  status = db->DeleteRange(write_options, "key3682", "key7808");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 45416: delete_range
  status = db->DeleteRange(write_options, "key0182", "key10802");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 45417: put
  db->Put(write_options, "key6880", "value95301");

  // op 45418: delete
  db->Delete(write_options, "key8013");

  // op 45419: delete
  db->Delete(write_options, "key4673");

  // op 45420: delete
  db->Delete(write_options, "key8711");

  // op 45421: put
  db->Put(write_options, "key9541", "value105897");

  // op 45422: get
  status = db->Get(read_options, "key9800", &value);
  if (status.ok()) {
    out << "GET key9800 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45423: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6992", "key7379", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6992, key7379) size=" << scan_result.size() << std::endl;
  }

  // op 45424: delete
  db->Delete(write_options, "key9772");

  // op 45425: put
  db->Put(write_options, "key5029", "value37564");

  // op 45426: put
  db->Put(write_options, "key8221", "value32207");

  // op 45427: put
  db->Put(write_options, "key0715", "value104722");

  // op 45428: put
  db->Put(write_options, "key6538", "value72296");

  // op 45429: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 45430: put
  db->Put(write_options, "key2249", "value40240");

  // op 45431: get
  status = db->Get(read_options, "key7898", &value);
  if (status.ok()) {
    out << "GET key7898 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45432: put
  db->Put(write_options, "key7731", "value65350");

  // op 45433: delete
  db->Delete(write_options, "key8142");

  // op 45434: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4458", "key7111", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4458, key7111) size=" << scan_result.size() << std::endl;
  }

  // op 45435: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 45436: delete_range
  status = db->DeleteRange(write_options, "key5235", "key5849");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 45437: get
  status = db->Get(read_options, "key10698", &value);
  if (status.ok()) {
    out << "GET key10698 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45438: get
  status = db->Get(read_options, "key7795", &value);
  if (status.ok()) {
    out << "GET key7795 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45439: get
  status = db->Get(read_options, "key0641", &value);
  if (status.ok()) {
    out << "GET key0641 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45440: put
  db->Put(write_options, "key0227", "value35951");

  // op 45441: delete_range
  status = db->DeleteRange(write_options, "key3944", "key8798");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 45442: delete
  db->Delete(write_options, "key7656");

  // op 45443: put
  db->Put(write_options, "key0820", "value56565");

  // op 45444: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2864", "key6710", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2864, key6710) size=" << scan_result.size() << std::endl;
  }

  // op 45445: delete
  db->Delete(write_options, "key1021");

  // op 45446: get
  status = db->Get(read_options, "key8578", &value);
  if (status.ok()) {
    out << "GET key8578 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45447: put
  db->Put(write_options, "key3429", "value63626");

  // op 45448: put
  db->Put(write_options, "key10949", "value17181");

  // op 45449: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6199", "key9554", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6199, key9554) size=" << scan_result.size() << std::endl;
  }

  // op 45450: put
  db->Put(write_options, "key8522", "value26427");

  // op 45451: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0970", "key3111", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0970, key3111) size=" << scan_result.size() << std::endl;
  }

  // op 45452: delete_range
  status = db->DeleteRange(write_options, "key3835", "key4528");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 45453: delete_range
  status = db->DeleteRange(write_options, "key5272", "key6505");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 45454: put
  db->Put(write_options, "key11704", "value97474");

  // op 45455: put
  db->Put(write_options, "key4578", "value16715");

  // op 45456: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 45457: put
  db->Put(write_options, "key3325", "value33539");

  // op 45458: put
  db->Put(write_options, "key5297", "value32448");

  // op 45459: put
  db->Put(write_options, "key10606", "value25955");

  // op 45460: put
  db->Put(write_options, "key6287", "value105137");

  // op 45461: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6100", "key11714", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6100, key11714) size=" << scan_result.size() << std::endl;
  }

  // op 45462: put
  db->Put(write_options, "key8917", "value44884");

  // op 45463: delete_range
  status = db->DeleteRange(write_options, "key0569", "key0613");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 45464: put
  db->Put(write_options, "key7693", "value27247");

  // op 45465: put
  db->Put(write_options, "key4219", "value29269");

  // op 45466: put
  db->Put(write_options, "key4734", "value72136");

  // op 45467: get
  status = db->Get(read_options, "key1300", &value);
  if (status.ok()) {
    out << "GET key1300 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45468: put
  db->Put(write_options, "key3629", "value91088");

  // op 45469: put
  db->Put(write_options, "key5366", "value75355");

  // op 45470: put
  db->Put(write_options, "key9758", "value67121");

  // op 45471: get
  status = db->Get(read_options, "key4464", &value);
  if (status.ok()) {
    out << "GET key4464 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45472: get
  status = db->Get(read_options, "key4337", &value);
  if (status.ok()) {
    out << "GET key4337 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45473: put
  db->Put(write_options, "key3221", "value48177");

  // op 45474: put
  db->Put(write_options, "key1881", "value36129");

  // op 45475: put
  db->Put(write_options, "key7865", "value19206");

  // op 45476: get
  status = db->Get(read_options, "key3366", &value);
  if (status.ok()) {
    out << "GET key3366 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45477: delete_range
  status = db->DeleteRange(write_options, "key1597", "key9377");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 45478: get
  status = db->Get(read_options, "key9338", &value);
  if (status.ok()) {
    out << "GET key9338 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45479: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5549", "key7161", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5549, key7161) size=" << scan_result.size() << std::endl;
  }

  // op 45480: put
  db->Put(write_options, "key3551", "value25678");

  // op 45481: get
  status = db->Get(read_options, "key8708", &value);
  if (status.ok()) {
    out << "GET key8708 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45482: get
  status = db->Get(read_options, "key7683", &value);
  if (status.ok()) {
    out << "GET key7683 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45483: get
  status = db->Get(read_options, "key4946", &value);
  if (status.ok()) {
    out << "GET key4946 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45484: put
  db->Put(write_options, "key7404", "value106868");

  // op 45485: delete_range
  status = db->DeleteRange(write_options, "key3029", "key10342");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 45486: put
  db->Put(write_options, "key1958", "value85569");

  // op 45487: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1435", "key5584", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1435, key5584) size=" << scan_result.size() << std::endl;
  }

  // op 45488: put
  db->Put(write_options, "key11446", "value30157");

  // op 45489: get
  status = db->Get(read_options, "key1832", &value);
  if (status.ok()) {
    out << "GET key1832 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45490: put
  db->Put(write_options, "key1496", "value43602");

  // op 45491: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1873", "key4629", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1873, key4629) size=" << scan_result.size() << std::endl;
  }

  // op 45492: delete_range
  status = db->DeleteRange(write_options, "key4679", "key10809");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 45493: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 45494: put
  db->Put(write_options, "key9246", "value113332");

  // op 45495: delete
  db->Delete(write_options, "key7956");

  // op 45496: put
  db->Put(write_options, "key9754", "value19745");

  // op 45497: delete
  db->Delete(write_options, "key3338");

  // op 45498: delete_range
  status = db->DeleteRange(write_options, "key1505", "key9412");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 45499: get
  status = db->Get(read_options, "key8192", &value);
  if (status.ok()) {
    out << "GET key8192 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45500: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7968", "key11682", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7968, key11682) size=" << scan_result.size() << std::endl;
  }

  // op 45501: delete_range
  status = db->DeleteRange(write_options, "key3953", "key8659");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 45502: get
  status = db->Get(read_options, "key6436", &value);
  if (status.ok()) {
    out << "GET key6436 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45503: put
  db->Put(write_options, "key6584", "value21370");

  // op 45504: delete
  db->Delete(write_options, "key8088");

  // op 45505: put
  db->Put(write_options, "key1290", "value23486");

  // op 45506: put
  db->Put(write_options, "key1573", "value89685");

  // op 45507: put
  db->Put(write_options, "key8419", "value105870");

  // op 45508: delete_range
  status = db->DeleteRange(write_options, "key1875", "key3619");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 45509: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3788", "key4532", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3788, key4532) size=" << scan_result.size() << std::endl;
  }

  // op 45510: get
  status = db->Get(read_options, "key8675", &value);
  if (status.ok()) {
    out << "GET key8675 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45511: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3247", "key6078", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3247, key6078) size=" << scan_result.size() << std::endl;
  }

  // op 45512: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5726", "key7178", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5726, key7178) size=" << scan_result.size() << std::endl;
  }

  // op 45513: put
  db->Put(write_options, "key8642", "value37485");

  // op 45514: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0318", "key3389", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0318, key3389) size=" << scan_result.size() << std::endl;
  }

  // op 45515: put
  db->Put(write_options, "key2953", "value19982");

  // op 45516: delete_range
  status = db->DeleteRange(write_options, "key0638", "key11492");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 45517: put
  db->Put(write_options, "key2743", "value100169");

  // op 45518: delete_range
  status = db->DeleteRange(write_options, "key1418", "key6704");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 45519: delete
  db->Delete(write_options, "key1187");

  // op 45520: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5773", "key10289", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5773, key10289) size=" << scan_result.size() << std::endl;
  }

  // op 45521: put
  db->Put(write_options, "key4069", "value46862");

  // op 45522: delete_range
  status = db->DeleteRange(write_options, "key2841", "key3416");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 45523: put
  db->Put(write_options, "key6483", "value116580");

  // op 45524: put
  db->Put(write_options, "key2584", "value69760");

  // op 45525: get
  status = db->Get(read_options, "key3805", &value);
  if (status.ok()) {
    out << "GET key3805 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45526: delete
  db->Delete(write_options, "key3227");

  // op 45527: get
  status = db->Get(read_options, "key7819", &value);
  if (status.ok()) {
    out << "GET key7819 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45528: put
  db->Put(write_options, "key6556", "value14307");

  // op 45529: delete
  db->Delete(write_options, "key4915");

  // op 45530: delete_range
  status = db->DeleteRange(write_options, "key8223", "key8450");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 45531: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 45532: put
  db->Put(write_options, "key8529", "value58394");

  // op 45533: put
  db->Put(write_options, "key11188", "value94111");

  // op 45534: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2466", "key10748", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2466, key10748) size=" << scan_result.size() << std::endl;
  }

  // op 45535: get
  status = db->Get(read_options, "key9193", &value);
  if (status.ok()) {
    out << "GET key9193 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45536: put
  db->Put(write_options, "key5030", "value51788");

  // op 45537: delete_range
  status = db->DeleteRange(write_options, "key7112", "key8909");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 45538: get
  status = db->Get(read_options, "key4197", &value);
  if (status.ok()) {
    out << "GET key4197 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45539: put
  db->Put(write_options, "key3716", "value36279");

  // op 45540: get
  status = db->Get(read_options, "key3545", &value);
  if (status.ok()) {
    out << "GET key3545 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45541: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6195", "key6833", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6195, key6833) size=" << scan_result.size() << std::endl;
  }

  // op 45542: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10954", "key11418", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10954, key11418) size=" << scan_result.size() << std::endl;
  }

  // op 45543: put
  db->Put(write_options, "key3469", "value85869");

  // op 45544: get
  status = db->Get(read_options, "key8223", &value);
  if (status.ok()) {
    out << "GET key8223 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45545: put
  db->Put(write_options, "key10447", "value81732");

  // op 45546: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 45547: get
  status = db->Get(read_options, "key4605", &value);
  if (status.ok()) {
    out << "GET key4605 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45548: get
  status = db->Get(read_options, "key9814", &value);
  if (status.ok()) {
    out << "GET key9814 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45549: get
  status = db->Get(read_options, "key7585", &value);
  if (status.ok()) {
    out << "GET key7585 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45550: get
  status = db->Get(read_options, "key4138", &value);
  if (status.ok()) {
    out << "GET key4138 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45551: get
  status = db->Get(read_options, "key3020", &value);
  if (status.ok()) {
    out << "GET key3020 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45552: put
  db->Put(write_options, "key2037", "value26539");

  // op 45553: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2514", "key11167", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2514, key11167) size=" << scan_result.size() << std::endl;
  }

  // op 45554: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8027", "key11287", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8027, key11287) size=" << scan_result.size() << std::endl;
  }

  // op 45555: delete_range
  status = db->DeleteRange(write_options, "key7141", "key9716");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 45556: delete
  db->Delete(write_options, "key9583");

  // op 45557: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0529", "key4318", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0529, key4318) size=" << scan_result.size() << std::endl;
  }

  // op 45558: put
  db->Put(write_options, "key6295", "value49529");

  // op 45559: put
  db->Put(write_options, "key10649", "value107881");

  // op 45560: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1413", "key5549", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1413, key5549) size=" << scan_result.size() << std::endl;
  }

  // op 45561: put
  db->Put(write_options, "key6094", "value24959");

  // op 45562: put
  db->Put(write_options, "key9424", "value75223");

  // op 45563: get
  status = db->Get(read_options, "key9301", &value);
  if (status.ok()) {
    out << "GET key9301 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45564: put
  db->Put(write_options, "key3058", "value1340");

  // op 45565: delete_range
  status = db->DeleteRange(write_options, "key10788", "key11658");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 45566: put
  db->Put(write_options, "key6147", "value33183");

  // op 45567: get
  status = db->Get(read_options, "key4074", &value);
  if (status.ok()) {
    out << "GET key4074 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45568: scan
  scan_result.clear();
  status = db->Scan(read_options, "key11043", "key11359", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key11043, key11359) size=" << scan_result.size() << std::endl;
  }

  // op 45569: put
  db->Put(write_options, "key2118", "value57211");

  // op 45570: delete_range
  status = db->DeleteRange(write_options, "key8605", "key9535");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 45571: delete
  db->Delete(write_options, "key11298");

  // op 45572: put
  db->Put(write_options, "key9964", "value54105");

  // op 45573: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3701", "key6228", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3701, key6228) size=" << scan_result.size() << std::endl;
  }

  // op 45574: put
  db->Put(write_options, "key8328", "value53990");

  // op 45575: delete_range
  status = db->DeleteRange(write_options, "key1565", "key9845");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 45576: get
  status = db->Get(read_options, "key8620", &value);
  if (status.ok()) {
    out << "GET key8620 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45577: get
  status = db->Get(read_options, "key5105", &value);
  if (status.ok()) {
    out << "GET key5105 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45578: put
  db->Put(write_options, "key4480", "value63881");

  // op 45579: delete
  db->Delete(write_options, "key4226");

  // op 45580: put
  db->Put(write_options, "key11085", "value115992");

  // op 45581: delete
  db->Delete(write_options, "key2758");

  // op 45582: put
  db->Put(write_options, "key3304", "value103314");

  // op 45583: delete
  db->Delete(write_options, "key3562");

  // op 45584: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 45585: put
  db->Put(write_options, "key9018", "value102660");

  // op 45586: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5307", "key6522", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5307, key6522) size=" << scan_result.size() << std::endl;
  }

  // op 45587: get
  status = db->Get(read_options, "key5832", &value);
  if (status.ok()) {
    out << "GET key5832 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45588: put
  db->Put(write_options, "key10877", "value17302");

  // op 45589: put
  db->Put(write_options, "key7058", "value1410");

  // op 45590: put
  db->Put(write_options, "key10975", "value56743");

  // op 45591: delete_range
  status = db->DeleteRange(write_options, "key7877", "key9050");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 45592: delete_range
  status = db->DeleteRange(write_options, "key2457", "key7036");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 45593: put
  db->Put(write_options, "key11285", "value54507");

  // op 45594: get
  status = db->Get(read_options, "key5235", &value);
  if (status.ok()) {
    out << "GET key5235 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45595: get
  status = db->Get(read_options, "key3421", &value);
  if (status.ok()) {
    out << "GET key3421 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45596: put
  db->Put(write_options, "key6768", "value6482");

  // op 45597: get
  status = db->Get(read_options, "key5181", &value);
  if (status.ok()) {
    out << "GET key5181 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45598: put
  db->Put(write_options, "key6369", "value41624");

  // op 45599: put
  db->Put(write_options, "key7888", "value64318");

  // op 45600: put
  db->Put(write_options, "key8601", "value109830");

  // op 45601: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0154", "key11504", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0154, key11504) size=" << scan_result.size() << std::endl;
  }

  // op 45602: get
  status = db->Get(read_options, "key10615", &value);
  if (status.ok()) {
    out << "GET key10615 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45603: get
  status = db->Get(read_options, "key3875", &value);
  if (status.ok()) {
    out << "GET key3875 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45604: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 45605: delete
  db->Delete(write_options, "key3680");

  // op 45606: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1026", "key7369", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1026, key7369) size=" << scan_result.size() << std::endl;
  }

  // op 45607: put
  db->Put(write_options, "key1638", "value34436");

  // op 45608: get
  status = db->Get(read_options, "key0070", &value);
  if (status.ok()) {
    out << "GET key0070 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45609: get
  status = db->Get(read_options, "key5747", &value);
  if (status.ok()) {
    out << "GET key5747 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45610: get
  status = db->Get(read_options, "key10415", &value);
  if (status.ok()) {
    out << "GET key10415 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45611: delete
  db->Delete(write_options, "key1967");

  // op 45612: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5771", "key9692", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5771, key9692) size=" << scan_result.size() << std::endl;
  }

  // op 45613: get
  status = db->Get(read_options, "key11699", &value);
  if (status.ok()) {
    out << "GET key11699 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45614: put
  db->Put(write_options, "key5628", "value91072");

  // op 45615: delete
  db->Delete(write_options, "key9129");

  // op 45616: get
  status = db->Get(read_options, "key1922", &value);
  if (status.ok()) {
    out << "GET key1922 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45617: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1957", "key4541", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1957, key4541) size=" << scan_result.size() << std::endl;
  }

  // op 45618: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1201", "key8426", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1201, key8426) size=" << scan_result.size() << std::endl;
  }

  // op 45619: delete
  db->Delete(write_options, "key8971");

  // op 45620: put
  db->Put(write_options, "key0755", "value53030");

  // op 45621: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3998", "key7685", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3998, key7685) size=" << scan_result.size() << std::endl;
  }

  // op 45622: get
  status = db->Get(read_options, "key3515", &value);
  if (status.ok()) {
    out << "GET key3515 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45623: delete
  db->Delete(write_options, "key4031");

  // op 45624: put
  db->Put(write_options, "key8251", "value29985");

  // op 45625: get
  status = db->Get(read_options, "key2947", &value);
  if (status.ok()) {
    out << "GET key2947 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45626: put
  db->Put(write_options, "key10052", "value76024");

  // op 45627: put
  db->Put(write_options, "key6430", "value7754");

  // op 45628: put
  db->Put(write_options, "key0726", "value8650");

  // op 45629: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9234", "key10007", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9234, key10007) size=" << scan_result.size() << std::endl;
  }

  // op 45630: put
  db->Put(write_options, "key8803", "value44619");

  // op 45631: put
  db->Put(write_options, "key9996", "value107628");

  // op 45632: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6205", "key6250", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6205, key6250) size=" << scan_result.size() << std::endl;
  }

  // op 45633: delete_range
  status = db->DeleteRange(write_options, "key1942", "key3135");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 45634: get
  status = db->Get(read_options, "key11107", &value);
  if (status.ok()) {
    out << "GET key11107 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45635: delete
  db->Delete(write_options, "key0695");

  // op 45636: delete
  db->Delete(write_options, "key7731");

  // op 45637: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1744", "key9395", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1744, key9395) size=" << scan_result.size() << std::endl;
  }

  // op 45638: get
  status = db->Get(read_options, "key4836", &value);
  if (status.ok()) {
    out << "GET key4836 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45639: get
  status = db->Get(read_options, "key8092", &value);
  if (status.ok()) {
    out << "GET key8092 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45640: put
  db->Put(write_options, "key6456", "value23848");

  // op 45641: get
  status = db->Get(read_options, "key5295", &value);
  if (status.ok()) {
    out << "GET key5295 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45642: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 45643: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9961", "key10920", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9961, key10920) size=" << scan_result.size() << std::endl;
  }

  // op 45644: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0739", "key9667", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0739, key9667) size=" << scan_result.size() << std::endl;
  }

  // op 45645: delete_range
  status = db->DeleteRange(write_options, "key0497", "key11543");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 45646: put
  db->Put(write_options, "key8078", "value17483");

  // op 45647: get
  status = db->Get(read_options, "key9454", &value);
  if (status.ok()) {
    out << "GET key9454 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45648: delete_range
  status = db->DeleteRange(write_options, "key0593", "key4982");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 45649: put
  db->Put(write_options, "key5646", "value25789");

  // op 45650: get
  status = db->Get(read_options, "key0891", &value);
  if (status.ok()) {
    out << "GET key0891 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45651: put
  db->Put(write_options, "key4250", "value70879");

  // op 45652: get
  status = db->Get(read_options, "key1633", &value);
  if (status.ok()) {
    out << "GET key1633 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45653: put
  db->Put(write_options, "key4579", "value70993");

  // op 45654: put
  db->Put(write_options, "key7527", "value54006");

  // op 45655: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0751", "key5626", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0751, key5626) size=" << scan_result.size() << std::endl;
  }

  // op 45656: put
  db->Put(write_options, "key6585", "value19168");

  // op 45657: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2717", "key7300", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2717, key7300) size=" << scan_result.size() << std::endl;
  }

  // op 45658: get
  status = db->Get(read_options, "key1978", &value);
  if (status.ok()) {
    out << "GET key1978 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45659: delete_range
  status = db->DeleteRange(write_options, "key0497", "key7463");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 45660: get
  status = db->Get(read_options, "key8090", &value);
  if (status.ok()) {
    out << "GET key8090 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45661: get
  status = db->Get(read_options, "key4559", &value);
  if (status.ok()) {
    out << "GET key4559 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45662: get
  status = db->Get(read_options, "key3917", &value);
  if (status.ok()) {
    out << "GET key3917 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45663: delete
  db->Delete(write_options, "key3074");

  // op 45664: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 45665: put
  db->Put(write_options, "key11685", "value97695");

  // op 45666: put
  db->Put(write_options, "key10440", "value61926");

  // op 45667: put
  db->Put(write_options, "key5722", "value39090");

  // op 45668: delete_range
  status = db->DeleteRange(write_options, "key4154", "key7627");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 45669: put
  db->Put(write_options, "key8877", "value92916");

  // op 45670: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0934", "key6611", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0934, key6611) size=" << scan_result.size() << std::endl;
  }

  // op 45671: put
  db->Put(write_options, "key4548", "value61887");

  // op 45672: get
  status = db->Get(read_options, "key10775", &value);
  if (status.ok()) {
    out << "GET key10775 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45673: get
  status = db->Get(read_options, "key3849", &value);
  if (status.ok()) {
    out << "GET key3849 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45674: get
  status = db->Get(read_options, "key9628", &value);
  if (status.ok()) {
    out << "GET key9628 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45675: get
  status = db->Get(read_options, "key3895", &value);
  if (status.ok()) {
    out << "GET key3895 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45676: put
  db->Put(write_options, "key2239", "value18019");

  // op 45677: get
  status = db->Get(read_options, "key1329", &value);
  if (status.ok()) {
    out << "GET key1329 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45678: put
  db->Put(write_options, "key1828", "value26039");

  // op 45679: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1456", "key1553", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1456, key1553) size=" << scan_result.size() << std::endl;
  }

  // op 45680: get
  status = db->Get(read_options, "key1940", &value);
  if (status.ok()) {
    out << "GET key1940 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45681: put
  db->Put(write_options, "key5793", "value113826");

  // op 45682: put
  db->Put(write_options, "key4306", "value105518");

  // op 45683: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2200", "key11095", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2200, key11095) size=" << scan_result.size() << std::endl;
  }

  // op 45684: get
  status = db->Get(read_options, "key8293", &value);
  if (status.ok()) {
    out << "GET key8293 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45685: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0404", "key9676", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0404, key9676) size=" << scan_result.size() << std::endl;
  }

  // op 45686: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1143", "key7306", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1143, key7306) size=" << scan_result.size() << std::endl;
  }

  // op 45687: get
  status = db->Get(read_options, "key2775", &value);
  if (status.ok()) {
    out << "GET key2775 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45688: delete
  db->Delete(write_options, "key2619");

  // op 45689: put
  db->Put(write_options, "key10221", "value41570");

  // op 45690: delete_range
  status = db->DeleteRange(write_options, "key5441", "key8966");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 45691: put
  db->Put(write_options, "key3530", "value101898");

  // op 45692: delete
  db->Delete(write_options, "key3246");

  // op 45693: put
  db->Put(write_options, "key5798", "value28608");

  // op 45694: delete
  db->Delete(write_options, "key0599");

  // op 45695: put
  db->Put(write_options, "key9639", "value27772");

  // op 45696: put
  db->Put(write_options, "key2666", "value36338");

  // op 45697: put
  db->Put(write_options, "key9058", "value70961");

  // op 45698: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5880", "key10539", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5880, key10539) size=" << scan_result.size() << std::endl;
  }

  // op 45699: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10149", "key11702", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10149, key11702) size=" << scan_result.size() << std::endl;
  }

  // op 45700: put
  db->Put(write_options, "key6303", "value103202");

  // op 45701: delete_range
  status = db->DeleteRange(write_options, "key4346", "key11000");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 45702: get
  status = db->Get(read_options, "key11367", &value);
  if (status.ok()) {
    out << "GET key11367 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45703: get
  status = db->Get(read_options, "key9805", &value);
  if (status.ok()) {
    out << "GET key9805 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45704: put
  db->Put(write_options, "key2632", "value13099");

  // op 45705: get
  status = db->Get(read_options, "key10060", &value);
  if (status.ok()) {
    out << "GET key10060 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45706: get
  status = db->Get(read_options, "key8279", &value);
  if (status.ok()) {
    out << "GET key8279 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45707: get
  status = db->Get(read_options, "key6697", &value);
  if (status.ok()) {
    out << "GET key6697 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45708: get
  status = db->Get(read_options, "key5407", &value);
  if (status.ok()) {
    out << "GET key5407 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45709: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5583", "key5830", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5583, key5830) size=" << scan_result.size() << std::endl;
  }

  // op 45710: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 45711: put
  db->Put(write_options, "key4078", "value60182");

  // op 45712: put
  db->Put(write_options, "key3272", "value51749");

  // op 45713: get
  status = db->Get(read_options, "key10948", &value);
  if (status.ok()) {
    out << "GET key10948 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45714: delete
  db->Delete(write_options, "key6108");

  // op 45715: put
  db->Put(write_options, "key8366", "value102689");

  // op 45716: get
  status = db->Get(read_options, "key4995", &value);
  if (status.ok()) {
    out << "GET key4995 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45717: get
  status = db->Get(read_options, "key7717", &value);
  if (status.ok()) {
    out << "GET key7717 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45718: get
  status = db->Get(read_options, "key5238", &value);
  if (status.ok()) {
    out << "GET key5238 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45719: delete
  db->Delete(write_options, "key0930");

  // op 45720: put
  db->Put(write_options, "key10521", "value68708");

  // op 45721: delete
  db->Delete(write_options, "key1593");

  // op 45722: put
  db->Put(write_options, "key2053", "value52741");

  // op 45723: get
  status = db->Get(read_options, "key1295", &value);
  if (status.ok()) {
    out << "GET key1295 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45724: put
  db->Put(write_options, "key3193", "value60257");

  // op 45725: put
  db->Put(write_options, "key10294", "value117094");

  // op 45726: get
  status = db->Get(read_options, "key5132", &value);
  if (status.ok()) {
    out << "GET key5132 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45727: get
  status = db->Get(read_options, "key2990", &value);
  if (status.ok()) {
    out << "GET key2990 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45728: delete_range
  status = db->DeleteRange(write_options, "key9521", "key9855");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 45729: delete_range
  status = db->DeleteRange(write_options, "key1462", "key2937");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 45730: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1313", "key9581", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1313, key9581) size=" << scan_result.size() << std::endl;
  }

  // op 45731: get
  status = db->Get(read_options, "key0654", &value);
  if (status.ok()) {
    out << "GET key0654 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45732: get
  status = db->Get(read_options, "key7294", &value);
  if (status.ok()) {
    out << "GET key7294 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45733: put
  db->Put(write_options, "key11376", "value21877");

  // op 45734: put
  db->Put(write_options, "key1741", "value100831");

  // op 45735: delete
  db->Delete(write_options, "key6095");

  // op 45736: put
  db->Put(write_options, "key9543", "value77487");

  // op 45737: put
  db->Put(write_options, "key5957", "value5222");

  // op 45738: put
  db->Put(write_options, "key9838", "value62251");

  // op 45739: get
  status = db->Get(read_options, "key3289", &value);
  if (status.ok()) {
    out << "GET key3289 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45740: get
  status = db->Get(read_options, "key0313", &value);
  if (status.ok()) {
    out << "GET key0313 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45741: put
  db->Put(write_options, "key8729", "value110774");

  // op 45742: get
  status = db->Get(read_options, "key3381", &value);
  if (status.ok()) {
    out << "GET key3381 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45743: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2244", "key6241", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2244, key6241) size=" << scan_result.size() << std::endl;
  }

  // op 45744: put
  db->Put(write_options, "key5671", "value107175");

  // op 45745: put
  db->Put(write_options, "key4799", "value47499");

  // op 45746: put
  db->Put(write_options, "key7179", "value59257");

  // op 45747: get
  status = db->Get(read_options, "key11651", &value);
  if (status.ok()) {
    out << "GET key11651 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45748: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0953", "key3280", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0953, key3280) size=" << scan_result.size() << std::endl;
  }

  // op 45749: put
  db->Put(write_options, "key4270", "value26387");

  // op 45750: delete_range
  status = db->DeleteRange(write_options, "key9122", "key9908");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 45751: get
  status = db->Get(read_options, "key4178", &value);
  if (status.ok()) {
    out << "GET key4178 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45752: put
  db->Put(write_options, "key1391", "value42048");

  // op 45753: put
  db->Put(write_options, "key10093", "value77035");

  // op 45754: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1108", "key5204", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1108, key5204) size=" << scan_result.size() << std::endl;
  }

  // op 45755: get
  status = db->Get(read_options, "key5419", &value);
  if (status.ok()) {
    out << "GET key5419 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45756: delete
  db->Delete(write_options, "key10377");

  // op 45757: delete_range
  status = db->DeleteRange(write_options, "key3142", "key8810");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 45758: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8594", "key11075", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8594, key11075) size=" << scan_result.size() << std::endl;
  }

  // op 45759: put
  db->Put(write_options, "key3261", "value111264");

  // op 45760: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3825", "key7304", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3825, key7304) size=" << scan_result.size() << std::endl;
  }

  // op 45761: get
  status = db->Get(read_options, "key2594", &value);
  if (status.ok()) {
    out << "GET key2594 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45762: put
  db->Put(write_options, "key6301", "value4228");

  // op 45763: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5390", "key6823", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5390, key6823) size=" << scan_result.size() << std::endl;
  }

  // op 45764: put
  db->Put(write_options, "key3651", "value70967");

  // op 45765: put
  db->Put(write_options, "key0852", "value114834");

  // op 45766: get
  status = db->Get(read_options, "key0274", &value);
  if (status.ok()) {
    out << "GET key0274 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45767: delete
  db->Delete(write_options, "key10354");

  // op 45768: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1698", "key8907", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1698, key8907) size=" << scan_result.size() << std::endl;
  }

  // op 45769: put
  db->Put(write_options, "key0917", "value58828");

  // op 45770: get
  status = db->Get(read_options, "key10009", &value);
  if (status.ok()) {
    out << "GET key10009 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45771: delete
  db->Delete(write_options, "key3573");

  // op 45772: put
  db->Put(write_options, "key7181", "value10199");

  // op 45773: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1631", "key9155", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1631, key9155) size=" << scan_result.size() << std::endl;
  }

  // op 45774: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0671", "key7245", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0671, key7245) size=" << scan_result.size() << std::endl;
  }

  // op 45775: put
  db->Put(write_options, "key10238", "value91910");

  // op 45776: put
  db->Put(write_options, "key10910", "value45374");

  // op 45777: delete_range
  status = db->DeleteRange(write_options, "key6649", "key8330");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 45778: delete_range
  status = db->DeleteRange(write_options, "key1525", "key3352");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 45779: delete
  db->Delete(write_options, "key4524");

  // op 45780: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8134", "key10258", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8134, key10258) size=" << scan_result.size() << std::endl;
  }

  // op 45781: delete_range
  status = db->DeleteRange(write_options, "key5480", "key7903");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 45782: get
  status = db->Get(read_options, "key11467", &value);
  if (status.ok()) {
    out << "GET key11467 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45783: get
  status = db->Get(read_options, "key10406", &value);
  if (status.ok()) {
    out << "GET key10406 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45784: delete
  db->Delete(write_options, "key2373");

  // op 45785: get
  status = db->Get(read_options, "key7609", &value);
  if (status.ok()) {
    out << "GET key7609 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45786: get
  status = db->Get(read_options, "key5669", &value);
  if (status.ok()) {
    out << "GET key5669 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45787: put
  db->Put(write_options, "key1432", "value87096");

  // op 45788: put
  db->Put(write_options, "key7530", "value111744");

  // op 45789: put
  db->Put(write_options, "key4831", "value32866");

  // op 45790: delete
  db->Delete(write_options, "key5210");

  // op 45791: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2072", "key6812", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2072, key6812) size=" << scan_result.size() << std::endl;
  }

  // op 45792: get
  status = db->Get(read_options, "key0528", &value);
  if (status.ok()) {
    out << "GET key0528 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45793: put
  db->Put(write_options, "key8200", "value21611");

  // op 45794: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2573", "key3603", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2573, key3603) size=" << scan_result.size() << std::endl;
  }

  // op 45795: put
  db->Put(write_options, "key5062", "value35811");

  // op 45796: put
  db->Put(write_options, "key4798", "value103731");

  // op 45797: delete
  db->Delete(write_options, "key1591");

  // op 45798: delete_range
  status = db->DeleteRange(write_options, "key8790", "key9326");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 45799: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8760", "key9491", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8760, key9491) size=" << scan_result.size() << std::endl;
  }

  // op 45800: put
  db->Put(write_options, "key6811", "value38984");

  // op 45801: put
  db->Put(write_options, "key8066", "value80963");

  // op 45802: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6011", "key10816", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6011, key10816) size=" << scan_result.size() << std::endl;
  }

  // op 45803: get
  status = db->Get(read_options, "key3934", &value);
  if (status.ok()) {
    out << "GET key3934 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45804: delete_range
  status = db->DeleteRange(write_options, "key3656", "key6856");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 45805: put
  db->Put(write_options, "key4501", "value21113");

  // op 45806: delete_range
  status = db->DeleteRange(write_options, "key6393", "key9874");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 45807: put
  db->Put(write_options, "key8487", "value109765");

  // op 45808: get
  status = db->Get(read_options, "key6697", &value);
  if (status.ok()) {
    out << "GET key6697 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45809: put
  db->Put(write_options, "key7894", "value86696");

  // op 45810: get
  status = db->Get(read_options, "key6957", &value);
  if (status.ok()) {
    out << "GET key6957 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45811: put
  db->Put(write_options, "key6427", "value67125");

  // op 45812: delete_range
  status = db->DeleteRange(write_options, "key7051", "key7588");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 45813: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2036", "key3670", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2036, key3670) size=" << scan_result.size() << std::endl;
  }

  // op 45814: get
  status = db->Get(read_options, "key9150", &value);
  if (status.ok()) {
    out << "GET key9150 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45815: put
  db->Put(write_options, "key6183", "value109800");

  // op 45816: get
  status = db->Get(read_options, "key10716", &value);
  if (status.ok()) {
    out << "GET key10716 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45817: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0959", "key10986", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0959, key10986) size=" << scan_result.size() << std::endl;
  }

  // op 45818: put
  db->Put(write_options, "key2531", "value100056");

  // op 45819: get
  status = db->Get(read_options, "key2110", &value);
  if (status.ok()) {
    out << "GET key2110 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45820: delete
  db->Delete(write_options, "key2834");

  // op 45821: delete_range
  status = db->DeleteRange(write_options, "key2987", "key9247");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 45822: put
  db->Put(write_options, "key4525", "value102475");

  // op 45823: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 45824: get
  status = db->Get(read_options, "key8169", &value);
  if (status.ok()) {
    out << "GET key8169 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45825: delete
  db->Delete(write_options, "key3798");

  // op 45826: put
  db->Put(write_options, "key9061", "value82770");

  // op 45827: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9781", "key11462", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9781, key11462) size=" << scan_result.size() << std::endl;
  }

  // op 45828: put
  db->Put(write_options, "key9687", "value18276");

  // op 45829: put
  db->Put(write_options, "key8469", "value114321");

  // op 45830: delete_range
  status = db->DeleteRange(write_options, "key2099", "key10482");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 45831: put
  db->Put(write_options, "key9452", "value100973");

  // op 45832: get
  status = db->Get(read_options, "key4080", &value);
  if (status.ok()) {
    out << "GET key4080 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45833: delete_range
  status = db->DeleteRange(write_options, "key7159", "key8622");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 45834: put
  db->Put(write_options, "key5304", "value117959");

  // op 45835: get
  status = db->Get(read_options, "key1912", &value);
  if (status.ok()) {
    out << "GET key1912 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45836: put
  db->Put(write_options, "key6747", "value92570");

  // op 45837: put
  db->Put(write_options, "key6531", "value6794");

  // op 45838: put
  db->Put(write_options, "key6032", "value83252");

  // op 45839: put
  db->Put(write_options, "key4408", "value104320");

  // op 45840: put
  db->Put(write_options, "key0052", "value77491");

  // op 45841: put
  db->Put(write_options, "key4987", "value73728");

  // op 45842: put
  db->Put(write_options, "key6188", "value79928");

  // op 45843: put
  db->Put(write_options, "key5475", "value83095");

  // op 45844: get
  status = db->Get(read_options, "key4423", &value);
  if (status.ok()) {
    out << "GET key4423 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45845: get
  status = db->Get(read_options, "key3998", &value);
  if (status.ok()) {
    out << "GET key3998 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45846: put
  db->Put(write_options, "key3470", "value65468");

  // op 45847: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4825", "key7013", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4825, key7013) size=" << scan_result.size() << std::endl;
  }

  // op 45848: put
  db->Put(write_options, "key3556", "value14842");

  // op 45849: get
  status = db->Get(read_options, "key7945", &value);
  if (status.ok()) {
    out << "GET key7945 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45850: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2630", "key9927", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2630, key9927) size=" << scan_result.size() << std::endl;
  }

  // op 45851: put
  db->Put(write_options, "key10802", "value44928");

  // op 45852: delete
  db->Delete(write_options, "key11299");

  // op 45853: delete_range
  status = db->DeleteRange(write_options, "key2655", "key9776");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 45854: delete
  db->Delete(write_options, "key0450");

  // op 45855: put
  db->Put(write_options, "key1562", "value79686");

  // op 45856: put
  db->Put(write_options, "key1246", "value9103");

  // op 45857: delete_range
  status = db->DeleteRange(write_options, "key2333", "key5788");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 45858: get
  status = db->Get(read_options, "key4220", &value);
  if (status.ok()) {
    out << "GET key4220 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45859: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1020", "key8716", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1020, key8716) size=" << scan_result.size() << std::endl;
  }

  // op 45860: get
  status = db->Get(read_options, "key11279", &value);
  if (status.ok()) {
    out << "GET key11279 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45861: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7252", "key10555", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7252, key10555) size=" << scan_result.size() << std::endl;
  }

  // op 45862: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4620", "key5538", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4620, key5538) size=" << scan_result.size() << std::endl;
  }

  // op 45863: delete_range
  status = db->DeleteRange(write_options, "key2248", "key10504");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 45864: get
  status = db->Get(read_options, "key7791", &value);
  if (status.ok()) {
    out << "GET key7791 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45865: delete_range
  status = db->DeleteRange(write_options, "key2504", "key3024");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 45866: get
  status = db->Get(read_options, "key4546", &value);
  if (status.ok()) {
    out << "GET key4546 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45867: put
  db->Put(write_options, "key10616", "value2854");

  // op 45868: put
  db->Put(write_options, "key8879", "value63666");

  // op 45869: put
  db->Put(write_options, "key9420", "value72903");

  // op 45870: put
  db->Put(write_options, "key10934", "value85270");

  // op 45871: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4921", "key9137", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4921, key9137) size=" << scan_result.size() << std::endl;
  }

  // op 45872: put
  db->Put(write_options, "key6903", "value84149");

  // op 45873: delete_range
  status = db->DeleteRange(write_options, "key5974", "key6798");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 45874: get
  status = db->Get(read_options, "key7348", &value);
  if (status.ok()) {
    out << "GET key7348 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45875: get
  status = db->Get(read_options, "key9332", &value);
  if (status.ok()) {
    out << "GET key9332 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45876: put
  db->Put(write_options, "key2347", "value78658");

  // op 45877: put
  db->Put(write_options, "key8177", "value51021");

  // op 45878: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0873", "key9736", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0873, key9736) size=" << scan_result.size() << std::endl;
  }

  // op 45879: get
  status = db->Get(read_options, "key8595", &value);
  if (status.ok()) {
    out << "GET key8595 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45880: put
  db->Put(write_options, "key5077", "value40023");

  // op 45881: delete_range
  status = db->DeleteRange(write_options, "key8612", "key9781");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 45882: put
  db->Put(write_options, "key10018", "value46882");

  // op 45883: delete
  db->Delete(write_options, "key3430");

  // op 45884: put
  db->Put(write_options, "key8497", "value36152");

  // op 45885: put
  db->Put(write_options, "key6440", "value81860");

  // op 45886: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0361", "key8055", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0361, key8055) size=" << scan_result.size() << std::endl;
  }

  // op 45887: put
  db->Put(write_options, "key8206", "value54434");

  // op 45888: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1431", "key10339", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1431, key10339) size=" << scan_result.size() << std::endl;
  }

  // op 45889: put
  db->Put(write_options, "key2573", "value4970");

  // op 45890: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2476", "key8512", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2476, key8512) size=" << scan_result.size() << std::endl;
  }

  // op 45891: put
  db->Put(write_options, "key2965", "value76126");

  // op 45892: put
  db->Put(write_options, "key4015", "value48614");

  // op 45893: put
  db->Put(write_options, "key11629", "value14306");

  // op 45894: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 45895: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0052", "key4928", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0052, key4928) size=" << scan_result.size() << std::endl;
  }

  // op 45896: put
  db->Put(write_options, "key6046", "value63004");

  // op 45897: put
  db->Put(write_options, "key4124", "value95597");

  // op 45898: put
  db->Put(write_options, "key10468", "value60921");

  // op 45899: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4163", "key5833", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4163, key5833) size=" << scan_result.size() << std::endl;
  }

  // op 45900: put
  db->Put(write_options, "key10425", "value36816");

  // op 45901: get
  status = db->Get(read_options, "key7949", &value);
  if (status.ok()) {
    out << "GET key7949 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45902: put
  db->Put(write_options, "key7411", "value56140");

  // op 45903: put
  db->Put(write_options, "key1502", "value24496");

  // op 45904: get
  status = db->Get(read_options, "key2927", &value);
  if (status.ok()) {
    out << "GET key2927 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45905: put
  db->Put(write_options, "key1892", "value52059");

  // op 45906: put
  db->Put(write_options, "key8046", "value96492");

  // op 45907: put
  db->Put(write_options, "key1538", "value110736");

  // op 45908: delete_range
  status = db->DeleteRange(write_options, "key0649", "key8037");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 45909: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1228", "key7711", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1228, key7711) size=" << scan_result.size() << std::endl;
  }

  // op 45910: put
  db->Put(write_options, "key6617", "value52018");

  // op 45911: get
  status = db->Get(read_options, "key1902", &value);
  if (status.ok()) {
    out << "GET key1902 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45912: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5187", "key10058", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5187, key10058) size=" << scan_result.size() << std::endl;
  }

  // op 45913: put
  db->Put(write_options, "key11017", "value29654");

  // op 45914: put
  db->Put(write_options, "key11081", "value99182");

  // op 45915: get
  status = db->Get(read_options, "key1691", &value);
  if (status.ok()) {
    out << "GET key1691 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45916: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0392", "key8319", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0392, key8319) size=" << scan_result.size() << std::endl;
  }

  // op 45917: get
  status = db->Get(read_options, "key1370", &value);
  if (status.ok()) {
    out << "GET key1370 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45918: put
  db->Put(write_options, "key3118", "value76733");

  // op 45919: put
  db->Put(write_options, "key3697", "value4821");

  // op 45920: get
  status = db->Get(read_options, "key1198", &value);
  if (status.ok()) {
    out << "GET key1198 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45921: put
  db->Put(write_options, "key10235", "value31694");

  // op 45922: delete
  db->Delete(write_options, "key5023");

  // op 45923: delete_range
  status = db->DeleteRange(write_options, "key6351", "key9855");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 45924: delete
  db->Delete(write_options, "key0469");

  // op 45925: delete
  db->Delete(write_options, "key11119");

  // op 45926: put
  db->Put(write_options, "key7673", "value59488");

  // op 45927: get
  status = db->Get(read_options, "key3761", &value);
  if (status.ok()) {
    out << "GET key3761 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45928: delete_range
  status = db->DeleteRange(write_options, "key0884", "key3847");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 45929: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2199", "key4845", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2199, key4845) size=" << scan_result.size() << std::endl;
  }

  // op 45930: get
  status = db->Get(read_options, "key6605", &value);
  if (status.ok()) {
    out << "GET key6605 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45931: put
  db->Put(write_options, "key10271", "value117902");

  // op 45932: put
  db->Put(write_options, "key0451", "value100791");

  // op 45933: get
  status = db->Get(read_options, "key4109", &value);
  if (status.ok()) {
    out << "GET key4109 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45934: get
  status = db->Get(read_options, "key9151", &value);
  if (status.ok()) {
    out << "GET key9151 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45935: get
  status = db->Get(read_options, "key10162", &value);
  if (status.ok()) {
    out << "GET key10162 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45936: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3983", "key6317", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3983, key6317) size=" << scan_result.size() << std::endl;
  }

  // op 45937: put
  db->Put(write_options, "key8325", "value9127");

  // op 45938: delete
  db->Delete(write_options, "key8384");

  // op 45939: get
  status = db->Get(read_options, "key9655", &value);
  if (status.ok()) {
    out << "GET key9655 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45940: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1567", "key8947", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1567, key8947) size=" << scan_result.size() << std::endl;
  }

  // op 45941: delete_range
  status = db->DeleteRange(write_options, "key2807", "key9373");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 45942: put
  db->Put(write_options, "key10957", "value36330");

  // op 45943: put
  db->Put(write_options, "key5082", "value64601");

  // op 45944: put
  db->Put(write_options, "key11556", "value53418");

  // op 45945: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7341", "key7769", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7341, key7769) size=" << scan_result.size() << std::endl;
  }

  // op 45946: get
  status = db->Get(read_options, "key0582", &value);
  if (status.ok()) {
    out << "GET key0582 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45947: get
  status = db->Get(read_options, "key9553", &value);
  if (status.ok()) {
    out << "GET key9553 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45948: delete_range
  status = db->DeleteRange(write_options, "key5994", "key11479");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 45949: put
  db->Put(write_options, "key6505", "value37065");

  // op 45950: delete
  db->Delete(write_options, "key6307");

  // op 45951: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 45952: delete
  db->Delete(write_options, "key9288");

  // op 45953: get
  status = db->Get(read_options, "key2112", &value);
  if (status.ok()) {
    out << "GET key2112 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45954: delete_range
  status = db->DeleteRange(write_options, "key2425", "key2762");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 45955: put
  db->Put(write_options, "key8037", "value88789");

  // op 45956: delete
  db->Delete(write_options, "key1619");

  // op 45957: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9006", "key9185", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9006, key9185) size=" << scan_result.size() << std::endl;
  }

  // op 45958: put
  db->Put(write_options, "key0838", "value25196");

  // op 45959: put
  db->Put(write_options, "key4449", "value92326");

  // op 45960: put
  db->Put(write_options, "key8224", "value24125");

  // op 45961: put
  db->Put(write_options, "key2798", "value58606");

  // op 45962: put
  db->Put(write_options, "key2941", "value60182");

  // op 45963: put
  db->Put(write_options, "key4755", "value12770");

  // op 45964: delete_range
  status = db->DeleteRange(write_options, "key4384", "key9884");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 45965: get
  status = db->Get(read_options, "key10944", &value);
  if (status.ok()) {
    out << "GET key10944 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45966: get
  status = db->Get(read_options, "key1948", &value);
  if (status.ok()) {
    out << "GET key1948 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45967: put
  db->Put(write_options, "key1582", "value10048");

  // op 45968: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3487", "key7849", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3487, key7849) size=" << scan_result.size() << std::endl;
  }

  // op 45969: get
  status = db->Get(read_options, "key5123", &value);
  if (status.ok()) {
    out << "GET key5123 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45970: get
  status = db->Get(read_options, "key7744", &value);
  if (status.ok()) {
    out << "GET key7744 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45971: put
  db->Put(write_options, "key0881", "value40613");

  // op 45972: delete_range
  status = db->DeleteRange(write_options, "key3939", "key9639");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 45973: get
  status = db->Get(read_options, "key10407", &value);
  if (status.ok()) {
    out << "GET key10407 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45974: put
  db->Put(write_options, "key11444", "value87241");

  // op 45975: put
  db->Put(write_options, "key9412", "value20209");

  // op 45976: put
  db->Put(write_options, "key2707", "value37404");

  // op 45977: put
  db->Put(write_options, "key0528", "value36884");

  // op 45978: get
  status = db->Get(read_options, "key2410", &value);
  if (status.ok()) {
    out << "GET key2410 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45979: put
  db->Put(write_options, "key8090", "value117976");

  // op 45980: put
  db->Put(write_options, "key8314", "value28287");

  // op 45981: put
  db->Put(write_options, "key7357", "value85920");

  // op 45982: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3124", "key3820", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3124, key3820) size=" << scan_result.size() << std::endl;
  }

  // op 45983: get
  status = db->Get(read_options, "key9869", &value);
  if (status.ok()) {
    out << "GET key9869 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45984: put
  db->Put(write_options, "key3703", "value58084");

  // op 45985: put
  db->Put(write_options, "key1908", "value8185");

  // op 45986: get
  status = db->Get(read_options, "key8608", &value);
  if (status.ok()) {
    out << "GET key8608 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45987: put
  db->Put(write_options, "key6271", "value30205");

  // op 45988: get
  status = db->Get(read_options, "key11337", &value);
  if (status.ok()) {
    out << "GET key11337 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45989: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 45990: get
  status = db->Get(read_options, "key11350", &value);
  if (status.ok()) {
    out << "GET key11350 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45991: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3274", "key8690", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3274, key8690) size=" << scan_result.size() << std::endl;
  }

  // op 45992: get
  status = db->Get(read_options, "key1989", &value);
  if (status.ok()) {
    out << "GET key1989 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45993: put
  db->Put(write_options, "key3760", "value91896");

  // op 45994: get
  status = db->Get(read_options, "key9943", &value);
  if (status.ok()) {
    out << "GET key9943 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45995: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6282", "key10451", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6282, key10451) size=" << scan_result.size() << std::endl;
  }

  // op 45996: put
  db->Put(write_options, "key4854", "value40806");

  // op 45997: get
  status = db->Get(read_options, "key1447", &value);
  if (status.ok()) {
    out << "GET key1447 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 45998: put
  db->Put(write_options, "key7165", "value73786");

  // op 45999: delete
  db->Delete(write_options, "key7304");

  // op 46000: put
  db->Put(write_options, "key10975", "value34627");

  // op 46001: get
  status = db->Get(read_options, "key10618", &value);
  if (status.ok()) {
    out << "GET key10618 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46002: delete_range
  status = db->DeleteRange(write_options, "key3627", "key9793");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 46003: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 46004: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 46005: delete
  db->Delete(write_options, "key8374");

  // op 46006: put
  db->Put(write_options, "key7624", "value74205");

  // op 46007: get
  status = db->Get(read_options, "key1471", &value);
  if (status.ok()) {
    out << "GET key1471 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46008: get
  status = db->Get(read_options, "key0842", &value);
  if (status.ok()) {
    out << "GET key0842 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46009: delete
  db->Delete(write_options, "key0266");

  // op 46010: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4078", "key9241", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4078, key9241) size=" << scan_result.size() << std::endl;
  }

  // op 46011: get
  status = db->Get(read_options, "key6719", &value);
  if (status.ok()) {
    out << "GET key6719 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46012: put
  db->Put(write_options, "key10483", "value47947");

  // op 46013: put
  db->Put(write_options, "key10329", "value77411");

  // op 46014: get
  status = db->Get(read_options, "key0602", &value);
  if (status.ok()) {
    out << "GET key0602 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46015: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8710", "key11556", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8710, key11556) size=" << scan_result.size() << std::endl;
  }

  // op 46016: get
  status = db->Get(read_options, "key4446", &value);
  if (status.ok()) {
    out << "GET key4446 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46017: get
  status = db->Get(read_options, "key4967", &value);
  if (status.ok()) {
    out << "GET key4967 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46018: delete
  db->Delete(write_options, "key2170");

  // op 46019: get
  status = db->Get(read_options, "key3324", &value);
  if (status.ok()) {
    out << "GET key3324 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46020: put
  db->Put(write_options, "key0319", "value41543");

  // op 46021: get
  status = db->Get(read_options, "key1452", &value);
  if (status.ok()) {
    out << "GET key1452 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46022: put
  db->Put(write_options, "key7574", "value23215");

  // op 46023: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0355", "key8806", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0355, key8806) size=" << scan_result.size() << std::endl;
  }

  // op 46024: delete
  db->Delete(write_options, "key11363");

  // op 46025: put
  db->Put(write_options, "key9833", "value79068");

  // op 46026: delete
  db->Delete(write_options, "key7514");

  // op 46027: delete_range
  status = db->DeleteRange(write_options, "key6117", "key6994");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 46028: put
  db->Put(write_options, "key9065", "value62610");

  // op 46029: put
  db->Put(write_options, "key5275", "value112066");

  // op 46030: put
  db->Put(write_options, "key5702", "value20417");

  // op 46031: put
  db->Put(write_options, "key0261", "value109764");

  // op 46032: delete
  db->Delete(write_options, "key1065");

  // op 46033: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5054", "key6880", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5054, key6880) size=" << scan_result.size() << std::endl;
  }

  // op 46034: delete
  db->Delete(write_options, "key5472");

  // op 46035: delete
  db->Delete(write_options, "key4866");

  // op 46036: put
  db->Put(write_options, "key6973", "value110621");

  // op 46037: delete_range
  status = db->DeleteRange(write_options, "key10240", "key11559");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 46038: put
  db->Put(write_options, "key9151", "value67174");

  // op 46039: delete
  db->Delete(write_options, "key4750");

  // op 46040: put
  db->Put(write_options, "key6408", "value111322");

  // op 46041: delete
  db->Delete(write_options, "key10249");

  // op 46042: get
  status = db->Get(read_options, "key6793", &value);
  if (status.ok()) {
    out << "GET key6793 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46043: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3655", "key4167", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3655, key4167) size=" << scan_result.size() << std::endl;
  }

  // op 46044: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 46045: put
  db->Put(write_options, "key4073", "value86492");

  // op 46046: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5255", "key9970", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5255, key9970) size=" << scan_result.size() << std::endl;
  }

  // op 46047: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2980", "key5027", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2980, key5027) size=" << scan_result.size() << std::endl;
  }

  // op 46048: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5512", "key8254", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5512, key8254) size=" << scan_result.size() << std::endl;
  }

  // op 46049: delete
  db->Delete(write_options, "key5019");

  // op 46050: put
  db->Put(write_options, "key7631", "value15567");

  // op 46051: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 46052: get
  status = db->Get(read_options, "key3124", &value);
  if (status.ok()) {
    out << "GET key3124 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46053: put
  db->Put(write_options, "key7429", "value13785");

  // op 46054: get
  status = db->Get(read_options, "key3555", &value);
  if (status.ok()) {
    out << "GET key3555 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46055: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3691", "key7259", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3691, key7259) size=" << scan_result.size() << std::endl;
  }

  // op 46056: put
  db->Put(write_options, "key7885", "value100068");

  // op 46057: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0369", "key8809", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0369, key8809) size=" << scan_result.size() << std::endl;
  }

  // op 46058: get
  status = db->Get(read_options, "key10573", &value);
  if (status.ok()) {
    out << "GET key10573 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46059: delete_range
  status = db->DeleteRange(write_options, "key7487", "key11706");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 46060: delete
  db->Delete(write_options, "key5881");

  // op 46061: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2174", "key7691", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2174, key7691) size=" << scan_result.size() << std::endl;
  }

  // op 46062: delete_range
  status = db->DeleteRange(write_options, "key4042", "key6127");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 46063: get
  status = db->Get(read_options, "key0620", &value);
  if (status.ok()) {
    out << "GET key0620 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46064: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5685", "key7174", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5685, key7174) size=" << scan_result.size() << std::endl;
  }

  // op 46065: put
  db->Put(write_options, "key7264", "value61733");

  // op 46066: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3440", "key7289", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3440, key7289) size=" << scan_result.size() << std::endl;
  }

  // op 46067: put
  db->Put(write_options, "key8454", "value86268");

  // op 46068: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 46069: get
  status = db->Get(read_options, "key11124", &value);
  if (status.ok()) {
    out << "GET key11124 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46070: put
  db->Put(write_options, "key1714", "value66405");

  // op 46071: delete_range
  status = db->DeleteRange(write_options, "key9564", "key10081");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 46072: put
  db->Put(write_options, "key6817", "value65323");

  // op 46073: delete_range
  status = db->DeleteRange(write_options, "key0744", "key11401");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 46074: delete
  db->Delete(write_options, "key10704");

  // op 46075: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 46076: put
  db->Put(write_options, "key0313", "value95853");

  // op 46077: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3981", "key10331", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3981, key10331) size=" << scan_result.size() << std::endl;
  }

  // op 46078: put
  db->Put(write_options, "key1603", "value34564");

  // op 46079: delete_range
  status = db->DeleteRange(write_options, "key2521", "key6344");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 46080: delete
  db->Delete(write_options, "key3099");

  // op 46081: delete
  db->Delete(write_options, "key10545");

  // op 46082: delete
  db->Delete(write_options, "key6014");

  // op 46083: delete_range
  status = db->DeleteRange(write_options, "key3033", "key10172");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 46084: delete_range
  status = db->DeleteRange(write_options, "key2876", "key10676");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 46085: get
  status = db->Get(read_options, "key8872", &value);
  if (status.ok()) {
    out << "GET key8872 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46086: put
  db->Put(write_options, "key11525", "value10491");

  // op 46087: delete
  db->Delete(write_options, "key8513");

  // op 46088: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4302", "key5871", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4302, key5871) size=" << scan_result.size() << std::endl;
  }

  // op 46089: put
  db->Put(write_options, "key4418", "value77480");

  // op 46090: put
  db->Put(write_options, "key1647", "value8815");

  // op 46091: delete_range
  status = db->DeleteRange(write_options, "key3326", "key8059");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 46092: delete_range
  status = db->DeleteRange(write_options, "key4732", "key8537");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 46093: delete_range
  status = db->DeleteRange(write_options, "key3892", "key10673");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 46094: get
  status = db->Get(read_options, "key1747", &value);
  if (status.ok()) {
    out << "GET key1747 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46095: put
  db->Put(write_options, "key7463", "value25351");

  // op 46096: get
  status = db->Get(read_options, "key7577", &value);
  if (status.ok()) {
    out << "GET key7577 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46097: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 46098: put
  db->Put(write_options, "key10429", "value36403");

  // op 46099: delete
  db->Delete(write_options, "key9929");

  // op 46100: delete
  db->Delete(write_options, "key1629");

  // op 46101: put
  db->Put(write_options, "key6120", "value55598");

  // op 46102: put
  db->Put(write_options, "key3451", "value45291");

  // op 46103: get
  status = db->Get(read_options, "key3417", &value);
  if (status.ok()) {
    out << "GET key3417 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46104: put
  db->Put(write_options, "key8615", "value91333");

  // op 46105: put
  db->Put(write_options, "key8603", "value82953");

  // op 46106: put
  db->Put(write_options, "key6986", "value63172");

  // op 46107: delete_range
  status = db->DeleteRange(write_options, "key1483", "key8703");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 46108: put
  db->Put(write_options, "key11383", "value25252");

  // op 46109: put
  db->Put(write_options, "key0034", "value91761");

  // op 46110: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6549", "key7894", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6549, key7894) size=" << scan_result.size() << std::endl;
  }

  // op 46111: delete
  db->Delete(write_options, "key3850");

  // op 46112: put
  db->Put(write_options, "key7675", "value96975");

  // op 46113: get
  status = db->Get(read_options, "key7153", &value);
  if (status.ok()) {
    out << "GET key7153 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46114: put
  db->Put(write_options, "key6227", "value18722");

  // op 46115: delete_range
  status = db->DeleteRange(write_options, "key9475", "key9660");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 46116: get
  status = db->Get(read_options, "key11305", &value);
  if (status.ok()) {
    out << "GET key11305 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46117: delete_range
  status = db->DeleteRange(write_options, "key1104", "key5848");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 46118: put
  db->Put(write_options, "key5211", "value74317");

  // op 46119: get
  status = db->Get(read_options, "key4923", &value);
  if (status.ok()) {
    out << "GET key4923 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46120: put
  db->Put(write_options, "key7961", "value107110");

  // op 46121: delete
  db->Delete(write_options, "key9872");

  // op 46122: put
  db->Put(write_options, "key3269", "value77927");

  // op 46123: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4267", "key5209", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4267, key5209) size=" << scan_result.size() << std::endl;
  }

  // op 46124: put
  db->Put(write_options, "key4408", "value14840");

  // op 46125: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9564", "key11529", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9564, key11529) size=" << scan_result.size() << std::endl;
  }

  // op 46126: get
  status = db->Get(read_options, "key1726", &value);
  if (status.ok()) {
    out << "GET key1726 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46127: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1205", "key9932", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1205, key9932) size=" << scan_result.size() << std::endl;
  }

  // op 46128: put
  db->Put(write_options, "key2716", "value107196");

  // op 46129: get
  status = db->Get(read_options, "key2438", &value);
  if (status.ok()) {
    out << "GET key2438 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46130: put
  db->Put(write_options, "key1101", "value38624");

  // op 46131: get
  status = db->Get(read_options, "key9808", &value);
  if (status.ok()) {
    out << "GET key9808 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46132: put
  db->Put(write_options, "key5420", "value113573");

  // op 46133: delete
  db->Delete(write_options, "key5141");

  // op 46134: put
  db->Put(write_options, "key7933", "value3310");

  // op 46135: put
  db->Put(write_options, "key9461", "value60488");

  // op 46136: put
  db->Put(write_options, "key10024", "value117368");

  // op 46137: get
  status = db->Get(read_options, "key2580", &value);
  if (status.ok()) {
    out << "GET key2580 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46138: delete
  db->Delete(write_options, "key0949");

  // op 46139: put
  db->Put(write_options, "key2592", "value117267");

  // op 46140: get
  status = db->Get(read_options, "key10439", &value);
  if (status.ok()) {
    out << "GET key10439 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46141: get
  status = db->Get(read_options, "key1219", &value);
  if (status.ok()) {
    out << "GET key1219 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46142: get
  status = db->Get(read_options, "key5934", &value);
  if (status.ok()) {
    out << "GET key5934 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46143: put
  db->Put(write_options, "key3950", "value77335");

  // op 46144: put
  db->Put(write_options, "key4349", "value3459");

  // op 46145: delete
  db->Delete(write_options, "key6246");

  // op 46146: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0158", "key7155", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0158, key7155) size=" << scan_result.size() << std::endl;
  }

  // op 46147: put
  db->Put(write_options, "key4068", "value10405");

  // op 46148: delete_range
  status = db->DeleteRange(write_options, "key3362", "key8916");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 46149: put
  db->Put(write_options, "key4693", "value3038");

  // op 46150: put
  db->Put(write_options, "key0831", "value40330");

  // op 46151: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0671", "key7931", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0671, key7931) size=" << scan_result.size() << std::endl;
  }

  // op 46152: put
  db->Put(write_options, "key0537", "value100701");

  // op 46153: get
  status = db->Get(read_options, "key10792", &value);
  if (status.ok()) {
    out << "GET key10792 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46154: get
  status = db->Get(read_options, "key6105", &value);
  if (status.ok()) {
    out << "GET key6105 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46155: put
  db->Put(write_options, "key0642", "value111359");

  // op 46156: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0820", "key11167", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0820, key11167) size=" << scan_result.size() << std::endl;
  }

  // op 46157: get
  status = db->Get(read_options, "key1853", &value);
  if (status.ok()) {
    out << "GET key1853 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46158: delete
  db->Delete(write_options, "key0120");

  // op 46159: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5458", "key11303", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5458, key11303) size=" << scan_result.size() << std::endl;
  }

  // op 46160: put
  db->Put(write_options, "key8962", "value13112");

  // op 46161: put
  db->Put(write_options, "key5395", "value30667");

  // op 46162: put
  db->Put(write_options, "key4104", "value92490");

  // op 46163: get
  status = db->Get(read_options, "key10276", &value);
  if (status.ok()) {
    out << "GET key10276 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46164: put
  db->Put(write_options, "key0901", "value47042");

  // op 46165: delete_range
  status = db->DeleteRange(write_options, "key6068", "key8217");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 46166: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9123", "key9711", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9123, key9711) size=" << scan_result.size() << std::endl;
  }

  // op 46167: delete_range
  status = db->DeleteRange(write_options, "key6225", "key7812");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 46168: put
  db->Put(write_options, "key11288", "value26148");

  // op 46169: delete_range
  status = db->DeleteRange(write_options, "key4746", "key7497");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 46170: get
  status = db->Get(read_options, "key0987", &value);
  if (status.ok()) {
    out << "GET key0987 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46171: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4094", "key7630", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4094, key7630) size=" << scan_result.size() << std::endl;
  }

  // op 46172: put
  db->Put(write_options, "key9427", "value85622");

  // op 46173: get
  status = db->Get(read_options, "key4186", &value);
  if (status.ok()) {
    out << "GET key4186 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46174: put
  db->Put(write_options, "key2664", "value34287");

  // op 46175: delete
  db->Delete(write_options, "key0921");

  // op 46176: get
  status = db->Get(read_options, "key5469", &value);
  if (status.ok()) {
    out << "GET key5469 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46177: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 46178: get
  status = db->Get(read_options, "key4166", &value);
  if (status.ok()) {
    out << "GET key4166 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46179: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2839", "key9529", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2839, key9529) size=" << scan_result.size() << std::endl;
  }

  // op 46180: put
  db->Put(write_options, "key5723", "value69822");

  // op 46181: put
  db->Put(write_options, "key9279", "value12697");

  // op 46182: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8350", "key9290", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8350, key9290) size=" << scan_result.size() << std::endl;
  }

  // op 46183: put
  db->Put(write_options, "key2985", "value37490");

  // op 46184: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2990", "key3373", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2990, key3373) size=" << scan_result.size() << std::endl;
  }

  // op 46185: get
  status = db->Get(read_options, "key11429", &value);
  if (status.ok()) {
    out << "GET key11429 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46186: get
  status = db->Get(read_options, "key9459", &value);
  if (status.ok()) {
    out << "GET key9459 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46187: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4092", "key10877", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4092, key10877) size=" << scan_result.size() << std::endl;
  }

  // op 46188: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 46189: get
  status = db->Get(read_options, "key11374", &value);
  if (status.ok()) {
    out << "GET key11374 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46190: put
  db->Put(write_options, "key2964", "value2237");

  // op 46191: put
  db->Put(write_options, "key9213", "value22892");

  // op 46192: delete_range
  status = db->DeleteRange(write_options, "key3570", "key10006");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 46193: put
  db->Put(write_options, "key5562", "value39105");

  // op 46194: get
  status = db->Get(read_options, "key8714", &value);
  if (status.ok()) {
    out << "GET key8714 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46195: put
  db->Put(write_options, "key6283", "value92049");

  // op 46196: put
  db->Put(write_options, "key11670", "value18988");

  // op 46197: delete_range
  status = db->DeleteRange(write_options, "key6478", "key6516");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 46198: put
  db->Put(write_options, "key9138", "value73720");

  // op 46199: put
  db->Put(write_options, "key5400", "value91769");

  // op 46200: put
  db->Put(write_options, "key10146", "value33704");

  // op 46201: delete_range
  status = db->DeleteRange(write_options, "key6656", "key11037");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 46202: put
  db->Put(write_options, "key10158", "value70207");

  // op 46203: put
  db->Put(write_options, "key7275", "value115472");

  // op 46204: put
  db->Put(write_options, "key1936", "value74344");

  // op 46205: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1128", "key6489", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1128, key6489) size=" << scan_result.size() << std::endl;
  }

  // op 46206: put
  db->Put(write_options, "key9316", "value77744");

  // op 46207: put
  db->Put(write_options, "key8662", "value59931");

  // op 46208: put
  db->Put(write_options, "key2210", "value88988");

  // op 46209: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0534", "key3712", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0534, key3712) size=" << scan_result.size() << std::endl;
  }

  // op 46210: put
  db->Put(write_options, "key4748", "value7632");

  // op 46211: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4208", "key8394", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4208, key8394) size=" << scan_result.size() << std::endl;
  }

  // op 46212: put
  db->Put(write_options, "key9979", "value1882");

  // op 46213: put
  db->Put(write_options, "key8851", "value54310");

  // op 46214: get
  status = db->Get(read_options, "key5926", &value);
  if (status.ok()) {
    out << "GET key5926 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46215: put
  db->Put(write_options, "key5455", "value98425");

  // op 46216: put
  db->Put(write_options, "key3239", "value33666");

  // op 46217: delete_range
  status = db->DeleteRange(write_options, "key1752", "key3922");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 46218: get
  status = db->Get(read_options, "key6394", &value);
  if (status.ok()) {
    out << "GET key6394 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46219: get
  status = db->Get(read_options, "key9783", &value);
  if (status.ok()) {
    out << "GET key9783 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46220: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0203", "key5455", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0203, key5455) size=" << scan_result.size() << std::endl;
  }

  // op 46221: put
  db->Put(write_options, "key2366", "value25963");

  // op 46222: put
  db->Put(write_options, "key0381", "value38482");

  // op 46223: delete
  db->Delete(write_options, "key6066");

  // op 46224: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1951", "key9956", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1951, key9956) size=" << scan_result.size() << std::endl;
  }

  // op 46225: put
  db->Put(write_options, "key7572", "value38812");

  // op 46226: put
  db->Put(write_options, "key11447", "value91971");

  // op 46227: delete_range
  status = db->DeleteRange(write_options, "key4097", "key10601");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 46228: delete
  db->Delete(write_options, "key6807");

  // op 46229: delete_range
  status = db->DeleteRange(write_options, "key0871", "key4845");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 46230: get
  status = db->Get(read_options, "key6292", &value);
  if (status.ok()) {
    out << "GET key6292 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46231: delete_range
  status = db->DeleteRange(write_options, "key0608", "key10686");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 46232: put
  db->Put(write_options, "key0780", "value39159");

  // op 46233: put
  db->Put(write_options, "key1347", "value62226");

  // op 46234: get
  status = db->Get(read_options, "key7499", &value);
  if (status.ok()) {
    out << "GET key7499 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46235: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2711", "key11174", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2711, key11174) size=" << scan_result.size() << std::endl;
  }

  // op 46236: delete_range
  status = db->DeleteRange(write_options, "key3697", "key8411");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 46237: put
  db->Put(write_options, "key6802", "value9940");

  // op 46238: delete_range
  status = db->DeleteRange(write_options, "key9268", "key10623");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 46239: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 46240: get
  status = db->Get(read_options, "key4537", &value);
  if (status.ok()) {
    out << "GET key4537 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46241: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 46242: put
  db->Put(write_options, "key0567", "value55138");

  // op 46243: put
  db->Put(write_options, "key8124", "value113250");

  // op 46244: put
  db->Put(write_options, "key6846", "value4580");

  // op 46245: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9344", "key11057", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9344, key11057) size=" << scan_result.size() << std::endl;
  }

  // op 46246: get
  status = db->Get(read_options, "key9575", &value);
  if (status.ok()) {
    out << "GET key9575 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46247: put
  db->Put(write_options, "key11029", "value5324");

  // op 46248: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 46249: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0575", "key1543", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0575, key1543) size=" << scan_result.size() << std::endl;
  }

  // op 46250: put
  db->Put(write_options, "key3425", "value38618");

  // op 46251: put
  db->Put(write_options, "key2386", "value23160");

  // op 46252: put
  db->Put(write_options, "key8418", "value21488");

  // op 46253: put
  db->Put(write_options, "key4255", "value1198");

  // op 46254: put
  db->Put(write_options, "key8648", "value20581");

  // op 46255: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0248", "key10405", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0248, key10405) size=" << scan_result.size() << std::endl;
  }

  // op 46256: get
  status = db->Get(read_options, "key8706", &value);
  if (status.ok()) {
    out << "GET key8706 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46257: delete_range
  status = db->DeleteRange(write_options, "key7004", "key9294");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 46258: put
  db->Put(write_options, "key11679", "value47251");

  // op 46259: put
  db->Put(write_options, "key4703", "value25991");

  // op 46260: delete
  db->Delete(write_options, "key8196");

  // op 46261: put
  db->Put(write_options, "key10105", "value116191");

  // op 46262: put
  db->Put(write_options, "key5752", "value60375");

  // op 46263: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 46264: get
  status = db->Get(read_options, "key8872", &value);
  if (status.ok()) {
    out << "GET key8872 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46265: put
  db->Put(write_options, "key7402", "value64674");

  // op 46266: put
  db->Put(write_options, "key6502", "value7736");

  // op 46267: delete
  db->Delete(write_options, "key0792");

  // op 46268: get
  status = db->Get(read_options, "key6204", &value);
  if (status.ok()) {
    out << "GET key6204 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46269: put
  db->Put(write_options, "key10201", "value104772");

  // op 46270: delete_range
  status = db->DeleteRange(write_options, "key2291", "key9623");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 46271: delete
  db->Delete(write_options, "key6681");

  // op 46272: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4077", "key7979", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4077, key7979) size=" << scan_result.size() << std::endl;
  }

  // op 46273: put
  db->Put(write_options, "key3312", "value67014");

  // op 46274: put
  db->Put(write_options, "key2654", "value8570");

  // op 46275: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8681", "key11057", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8681, key11057) size=" << scan_result.size() << std::endl;
  }

  // op 46276: get
  status = db->Get(read_options, "key6580", &value);
  if (status.ok()) {
    out << "GET key6580 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46277: delete_range
  status = db->DeleteRange(write_options, "key4035", "key5401");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 46278: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6046", "key11115", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6046, key11115) size=" << scan_result.size() << std::endl;
  }

  // op 46279: get
  status = db->Get(read_options, "key7269", &value);
  if (status.ok()) {
    out << "GET key7269 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46280: get
  status = db->Get(read_options, "key10239", &value);
  if (status.ok()) {
    out << "GET key10239 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46281: get
  status = db->Get(read_options, "key11220", &value);
  if (status.ok()) {
    out << "GET key11220 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46282: put
  db->Put(write_options, "key11301", "value53811");

  // op 46283: put
  db->Put(write_options, "key7859", "value96757");

  // op 46284: put
  db->Put(write_options, "key11167", "value61360");

  // op 46285: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1208", "key10221", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1208, key10221) size=" << scan_result.size() << std::endl;
  }

  // op 46286: put
  db->Put(write_options, "key1535", "value44769");

  // op 46287: put
  db->Put(write_options, "key8502", "value23737");

  // op 46288: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9439", "key10579", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9439, key10579) size=" << scan_result.size() << std::endl;
  }

  // op 46289: put
  db->Put(write_options, "key9362", "value42921");

  // op 46290: get
  status = db->Get(read_options, "key9362", &value);
  if (status.ok()) {
    out << "GET key9362 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46291: put
  db->Put(write_options, "key2682", "value93093");

  // op 46292: delete_range
  status = db->DeleteRange(write_options, "key8921", "key11328");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 46293: put
  db->Put(write_options, "key8824", "value71037");

  // op 46294: get
  status = db->Get(read_options, "key9635", &value);
  if (status.ok()) {
    out << "GET key9635 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46295: put
  db->Put(write_options, "key5816", "value24257");

  // op 46296: get
  status = db->Get(read_options, "key7869", &value);
  if (status.ok()) {
    out << "GET key7869 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46297: put
  db->Put(write_options, "key1638", "value103283");

  // op 46298: delete
  db->Delete(write_options, "key7552");

  // op 46299: delete
  db->Delete(write_options, "key10593");

  // op 46300: put
  db->Put(write_options, "key11644", "value33861");

  // op 46301: get
  status = db->Get(read_options, "key8199", &value);
  if (status.ok()) {
    out << "GET key8199 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46302: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0729", "key3948", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0729, key3948) size=" << scan_result.size() << std::endl;
  }

  // op 46303: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 46304: get
  status = db->Get(read_options, "key5966", &value);
  if (status.ok()) {
    out << "GET key5966 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46305: get
  status = db->Get(read_options, "key4785", &value);
  if (status.ok()) {
    out << "GET key4785 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46306: put
  db->Put(write_options, "key6639", "value56681");

  // op 46307: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2965", "key6867", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2965, key6867) size=" << scan_result.size() << std::endl;
  }

  // op 46308: put
  db->Put(write_options, "key4602", "value110465");

  // op 46309: put
  db->Put(write_options, "key6417", "value77206");

  // op 46310: delete
  db->Delete(write_options, "key9103");

  // op 46311: get
  status = db->Get(read_options, "key0675", &value);
  if (status.ok()) {
    out << "GET key0675 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46312: delete
  db->Delete(write_options, "key9511");

  // op 46313: put
  db->Put(write_options, "key11170", "value9604");

  // op 46314: delete
  db->Delete(write_options, "key2395");

  // op 46315: get
  status = db->Get(read_options, "key7030", &value);
  if (status.ok()) {
    out << "GET key7030 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46316: put
  db->Put(write_options, "key1881", "value103781");

  // op 46317: delete
  db->Delete(write_options, "key9619");

  // op 46318: delete_range
  status = db->DeleteRange(write_options, "key8314", "key8930");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 46319: delete
  db->Delete(write_options, "key3259");

  // op 46320: delete
  db->Delete(write_options, "key1719");

  // op 46321: put
  db->Put(write_options, "key2464", "value13846");

  // op 46322: put
  db->Put(write_options, "key2047", "value32811");

  // op 46323: put
  db->Put(write_options, "key2890", "value45776");

  // op 46324: delete
  db->Delete(write_options, "key0105");

  // op 46325: get
  status = db->Get(read_options, "key3537", &value);
  if (status.ok()) {
    out << "GET key3537 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46326: put
  db->Put(write_options, "key6665", "value91004");

  // op 46327: get
  status = db->Get(read_options, "key5051", &value);
  if (status.ok()) {
    out << "GET key5051 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46328: delete_range
  status = db->DeleteRange(write_options, "key1860", "key3247");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 46329: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0117", "key7846", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0117, key7846) size=" << scan_result.size() << std::endl;
  }

  // op 46330: get
  status = db->Get(read_options, "key0161", &value);
  if (status.ok()) {
    out << "GET key0161 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46331: put
  db->Put(write_options, "key0486", "value31080");

  // op 46332: put
  db->Put(write_options, "key9008", "value85816");

  // op 46333: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9395", "key11346", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9395, key11346) size=" << scan_result.size() << std::endl;
  }

  // op 46334: get
  status = db->Get(read_options, "key2494", &value);
  if (status.ok()) {
    out << "GET key2494 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46335: put
  db->Put(write_options, "key2363", "value74248");

  // op 46336: delete_range
  status = db->DeleteRange(write_options, "key0470", "key2407");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 46337: put
  db->Put(write_options, "key11218", "value44949");

  // op 46338: put
  db->Put(write_options, "key2254", "value47378");

  // op 46339: put
  db->Put(write_options, "key2144", "value105872");

  // op 46340: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0889", "key6459", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0889, key6459) size=" << scan_result.size() << std::endl;
  }

  // op 46341: get
  status = db->Get(read_options, "key10815", &value);
  if (status.ok()) {
    out << "GET key10815 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46342: delete_range
  status = db->DeleteRange(write_options, "key2083", "key5218");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 46343: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2203", "key8693", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2203, key8693) size=" << scan_result.size() << std::endl;
  }

  // op 46344: get
  status = db->Get(read_options, "key7283", &value);
  if (status.ok()) {
    out << "GET key7283 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46345: put
  db->Put(write_options, "key6589", "value91937");

  // op 46346: delete_range
  status = db->DeleteRange(write_options, "key3443", "key8143");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 46347: put
  db->Put(write_options, "key10483", "value69331");

  // op 46348: put
  db->Put(write_options, "key7685", "value72664");

  // op 46349: delete
  db->Delete(write_options, "key9128");

  // op 46350: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 46351: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2994", "key6688", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2994, key6688) size=" << scan_result.size() << std::endl;
  }

  // op 46352: delete_range
  status = db->DeleteRange(write_options, "key2775", "key3361");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 46353: delete_range
  status = db->DeleteRange(write_options, "key1253", "key8659");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 46354: delete
  db->Delete(write_options, "key4832");

  // op 46355: put
  db->Put(write_options, "key6184", "value0087");

  // op 46356: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1469", "key11494", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1469, key11494) size=" << scan_result.size() << std::endl;
  }

  // op 46357: get
  status = db->Get(read_options, "key7136", &value);
  if (status.ok()) {
    out << "GET key7136 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46358: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5639", "key7032", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5639, key7032) size=" << scan_result.size() << std::endl;
  }

  // op 46359: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1493", "key6662", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1493, key6662) size=" << scan_result.size() << std::endl;
  }

  // op 46360: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0106", "key5127", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0106, key5127) size=" << scan_result.size() << std::endl;
  }

  // op 46361: delete
  db->Delete(write_options, "key9922");

  // op 46362: get
  status = db->Get(read_options, "key9325", &value);
  if (status.ok()) {
    out << "GET key9325 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46363: put
  db->Put(write_options, "key9557", "value74142");

  // op 46364: delete
  db->Delete(write_options, "key6819");

  // op 46365: delete
  db->Delete(write_options, "key6410");

  // op 46366: delete
  db->Delete(write_options, "key8306");

  // op 46367: delete
  db->Delete(write_options, "key4313");

  // op 46368: delete_range
  status = db->DeleteRange(write_options, "key4916", "key5719");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 46369: delete
  db->Delete(write_options, "key10774");

  // op 46370: get
  status = db->Get(read_options, "key3882", &value);
  if (status.ok()) {
    out << "GET key3882 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46371: put
  db->Put(write_options, "key3222", "value116683");

  // op 46372: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6282", "key8657", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6282, key8657) size=" << scan_result.size() << std::endl;
  }

  // op 46373: delete
  db->Delete(write_options, "key0445");

  // op 46374: put
  db->Put(write_options, "key2615", "value115670");

  // op 46375: put
  db->Put(write_options, "key10390", "value41976");

  // op 46376: get
  status = db->Get(read_options, "key0477", &value);
  if (status.ok()) {
    out << "GET key0477 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46377: put
  db->Put(write_options, "key10994", "value39002");

  // op 46378: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 46379: put
  db->Put(write_options, "key10273", "value78769");

  // op 46380: put
  db->Put(write_options, "key4453", "value101767");

  // op 46381: get
  status = db->Get(read_options, "key1673", &value);
  if (status.ok()) {
    out << "GET key1673 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46382: delete
  db->Delete(write_options, "key8898");

  // op 46383: put
  db->Put(write_options, "key9797", "value114508");

  // op 46384: delete_range
  status = db->DeleteRange(write_options, "key1303", "key4438");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 46385: delete
  db->Delete(write_options, "key0343");

  // op 46386: get
  status = db->Get(read_options, "key8129", &value);
  if (status.ok()) {
    out << "GET key8129 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46387: delete_range
  status = db->DeleteRange(write_options, "key1201", "key2658");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 46388: put
  db->Put(write_options, "key3902", "value78541");

  // op 46389: get
  status = db->Get(read_options, "key8785", &value);
  if (status.ok()) {
    out << "GET key8785 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46390: put
  db->Put(write_options, "key3376", "value49673");

  // op 46391: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1830", "key4945", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1830, key4945) size=" << scan_result.size() << std::endl;
  }

  // op 46392: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4236", "key10111", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4236, key10111) size=" << scan_result.size() << std::endl;
  }

  // op 46393: put
  db->Put(write_options, "key0168", "value31123");

  // op 46394: put
  db->Put(write_options, "key3725", "value117903");

  // op 46395: put
  db->Put(write_options, "key7147", "value70989");

  // op 46396: put
  db->Put(write_options, "key10347", "value24236");

  // op 46397: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 46398: put
  db->Put(write_options, "key9538", "value6728");

  // op 46399: put
  db->Put(write_options, "key8115", "value4997");

  // op 46400: delete_range
  status = db->DeleteRange(write_options, "key7302", "key8704");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 46401: put
  db->Put(write_options, "key11035", "value19645");

  // op 46402: put
  db->Put(write_options, "key6021", "value52802");

  // op 46403: get
  status = db->Get(read_options, "key3678", &value);
  if (status.ok()) {
    out << "GET key3678 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46404: put
  db->Put(write_options, "key5471", "value108339");

  // op 46405: put
  db->Put(write_options, "key2235", "value93261");

  // op 46406: get
  status = db->Get(read_options, "key6457", &value);
  if (status.ok()) {
    out << "GET key6457 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46407: put
  db->Put(write_options, "key2122", "value32576");

  // op 46408: get
  status = db->Get(read_options, "key10037", &value);
  if (status.ok()) {
    out << "GET key10037 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46409: put
  db->Put(write_options, "key6883", "value112941");

  // op 46410: delete_range
  status = db->DeleteRange(write_options, "key3033", "key8361");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 46411: put
  db->Put(write_options, "key4147", "value61566");

  // op 46412: put
  db->Put(write_options, "key4960", "value108786");

  // op 46413: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4538", "key7566", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4538, key7566) size=" << scan_result.size() << std::endl;
  }

  // op 46414: delete_range
  status = db->DeleteRange(write_options, "key3226", "key4604");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 46415: delete
  db->Delete(write_options, "key6980");

  // op 46416: get
  status = db->Get(read_options, "key8978", &value);
  if (status.ok()) {
    out << "GET key8978 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46417: delete_range
  status = db->DeleteRange(write_options, "key3267", "key5103");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 46418: get
  status = db->Get(read_options, "key9083", &value);
  if (status.ok()) {
    out << "GET key9083 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46419: get
  status = db->Get(read_options, "key1409", &value);
  if (status.ok()) {
    out << "GET key1409 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46420: put
  db->Put(write_options, "key2858", "value27769");

  // op 46421: put
  db->Put(write_options, "key4379", "value96792");

  // op 46422: put
  db->Put(write_options, "key6932", "value34849");

  // op 46423: put
  db->Put(write_options, "key10095", "value91682");

  // op 46424: delete
  db->Delete(write_options, "key2982");

  // op 46425: put
  db->Put(write_options, "key3459", "value26428");

  // op 46426: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0123", "key0207", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0123, key0207) size=" << scan_result.size() << std::endl;
  }

  // op 46427: get
  status = db->Get(read_options, "key10099", &value);
  if (status.ok()) {
    out << "GET key10099 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46428: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0501", "key6345", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0501, key6345) size=" << scan_result.size() << std::endl;
  }

  // op 46429: put
  db->Put(write_options, "key7691", "value24849");

  // op 46430: get
  status = db->Get(read_options, "key5469", &value);
  if (status.ok()) {
    out << "GET key5469 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46431: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4829", "key8667", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4829, key8667) size=" << scan_result.size() << std::endl;
  }

  // op 46432: put
  db->Put(write_options, "key6970", "value107384");

  // op 46433: delete_range
  status = db->DeleteRange(write_options, "key6451", "key10724");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 46434: delete_range
  status = db->DeleteRange(write_options, "key1095", "key7782");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 46435: delete_range
  status = db->DeleteRange(write_options, "key3751", "key7942");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 46436: put
  db->Put(write_options, "key9192", "value6932");

  // op 46437: put
  db->Put(write_options, "key0051", "value48302");

  // op 46438: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 46439: put
  db->Put(write_options, "key10521", "value91119");

  // op 46440: delete
  db->Delete(write_options, "key2252");

  // op 46441: put
  db->Put(write_options, "key4392", "value72355");

  // op 46442: delete_range
  status = db->DeleteRange(write_options, "key0497", "key8609");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 46443: get
  status = db->Get(read_options, "key6758", &value);
  if (status.ok()) {
    out << "GET key6758 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46444: delete_range
  status = db->DeleteRange(write_options, "key7196", "key10705");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 46445: get
  status = db->Get(read_options, "key5034", &value);
  if (status.ok()) {
    out << "GET key5034 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46446: delete_range
  status = db->DeleteRange(write_options, "key5301", "key8033");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 46447: put
  db->Put(write_options, "key9066", "value101992");

  // op 46448: put
  db->Put(write_options, "key7694", "value57466");

  // op 46449: get
  status = db->Get(read_options, "key5219", &value);
  if (status.ok()) {
    out << "GET key5219 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46450: put
  db->Put(write_options, "key5172", "value2615");

  // op 46451: put
  db->Put(write_options, "key8863", "value111720");

  // op 46452: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2935", "key7526", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2935, key7526) size=" << scan_result.size() << std::endl;
  }

  // op 46453: put
  db->Put(write_options, "key11181", "value60372");

  // op 46454: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0541", "key5184", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0541, key5184) size=" << scan_result.size() << std::endl;
  }

  // op 46455: put
  db->Put(write_options, "key6646", "value97398");

  // op 46456: put
  db->Put(write_options, "key1994", "value10819");

  // op 46457: get
  status = db->Get(read_options, "key8288", &value);
  if (status.ok()) {
    out << "GET key8288 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46458: put
  db->Put(write_options, "key10977", "value103321");

  // op 46459: get
  status = db->Get(read_options, "key3562", &value);
  if (status.ok()) {
    out << "GET key3562 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46460: put
  db->Put(write_options, "key4425", "value59721");

  // op 46461: put
  db->Put(write_options, "key6598", "value83363");

  // op 46462: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4405", "key7079", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4405, key7079) size=" << scan_result.size() << std::endl;
  }

  // op 46463: delete_range
  status = db->DeleteRange(write_options, "key2856", "key6225");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 46464: put
  db->Put(write_options, "key1490", "value31376");

  // op 46465: get
  status = db->Get(read_options, "key6224", &value);
  if (status.ok()) {
    out << "GET key6224 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46466: delete
  db->Delete(write_options, "key0155");

  // op 46467: delete
  db->Delete(write_options, "key6818");

  // op 46468: put
  db->Put(write_options, "key10153", "value1667");

  // op 46469: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8727", "key9550", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8727, key9550) size=" << scan_result.size() << std::endl;
  }

  // op 46470: delete_range
  status = db->DeleteRange(write_options, "key0359", "key10226");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 46471: delete_range
  status = db->DeleteRange(write_options, "key4494", "key10001");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 46472: put
  db->Put(write_options, "key8505", "value29544");

  // op 46473: put
  db->Put(write_options, "key5771", "value84812");

  // op 46474: get
  status = db->Get(read_options, "key9549", &value);
  if (status.ok()) {
    out << "GET key9549 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46475: get
  status = db->Get(read_options, "key7323", &value);
  if (status.ok()) {
    out << "GET key7323 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46476: put
  db->Put(write_options, "key1351", "value1354");

  // op 46477: put
  db->Put(write_options, "key10642", "value106943");

  // op 46478: put
  db->Put(write_options, "key7701", "value110921");

  // op 46479: delete
  db->Delete(write_options, "key4967");

  // op 46480: delete_range
  status = db->DeleteRange(write_options, "key4800", "key7174");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 46481: get
  status = db->Get(read_options, "key1663", &value);
  if (status.ok()) {
    out << "GET key1663 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46482: put
  db->Put(write_options, "key9173", "value111996");

  // op 46483: delete
  db->Delete(write_options, "key9916");

  // op 46484: put
  db->Put(write_options, "key0300", "value50593");

  // op 46485: put
  db->Put(write_options, "key0677", "value37907");

  // op 46486: get
  status = db->Get(read_options, "key5009", &value);
  if (status.ok()) {
    out << "GET key5009 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46487: put
  db->Put(write_options, "key8407", "value48545");

  // op 46488: put
  db->Put(write_options, "key11501", "value72472");

  // op 46489: put
  db->Put(write_options, "key10387", "value14084");

  // op 46490: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9500", "key10104", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9500, key10104) size=" << scan_result.size() << std::endl;
  }

  // op 46491: get
  status = db->Get(read_options, "key6487", &value);
  if (status.ok()) {
    out << "GET key6487 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46492: put
  db->Put(write_options, "key3637", "value14269");

  // op 46493: put
  db->Put(write_options, "key5167", "value16668");

  // op 46494: get
  status = db->Get(read_options, "key0173", &value);
  if (status.ok()) {
    out << "GET key0173 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46495: delete_range
  status = db->DeleteRange(write_options, "key1000", "key5360");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 46496: put
  db->Put(write_options, "key3592", "value28871");

  // op 46497: get
  status = db->Get(read_options, "key9628", &value);
  if (status.ok()) {
    out << "GET key9628 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46498: delete
  db->Delete(write_options, "key1928");

  // op 46499: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8029", "key10680", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8029, key10680) size=" << scan_result.size() << std::endl;
  }

  // op 46500: get
  status = db->Get(read_options, "key3992", &value);
  if (status.ok()) {
    out << "GET key3992 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46501: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 46502: delete_range
  status = db->DeleteRange(write_options, "key8562", "key9884");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 46503: delete_range
  status = db->DeleteRange(write_options, "key4977", "key10909");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 46504: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1850", "key4906", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1850, key4906) size=" << scan_result.size() << std::endl;
  }

  // op 46505: put
  db->Put(write_options, "key1678", "value35818");

  // op 46506: put
  db->Put(write_options, "key5830", "value43029");

  // op 46507: delete
  db->Delete(write_options, "key8923");

  // op 46508: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5689", "key7822", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5689, key7822) size=" << scan_result.size() << std::endl;
  }

  // op 46509: put
  db->Put(write_options, "key5678", "value67168");

  // op 46510: put
  db->Put(write_options, "key3097", "value75784");

  // op 46511: delete_range
  status = db->DeleteRange(write_options, "key0346", "key5555");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 46512: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2037", "key5882", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2037, key5882) size=" << scan_result.size() << std::endl;
  }

  // op 46513: put
  db->Put(write_options, "key9837", "value68819");

  // op 46514: put
  db->Put(write_options, "key1414", "value33573");

  // op 46515: put
  db->Put(write_options, "key10801", "value94668");

  // op 46516: put
  db->Put(write_options, "key5180", "value60725");

  // op 46517: put
  db->Put(write_options, "key9368", "value39428");

  // op 46518: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0614", "key3120", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0614, key3120) size=" << scan_result.size() << std::endl;
  }

  // op 46519: put
  db->Put(write_options, "key9019", "value62959");

  // op 46520: delete
  db->Delete(write_options, "key1594");

  // op 46521: put
  db->Put(write_options, "key3270", "value32204");

  // op 46522: put
  db->Put(write_options, "key6747", "value86511");

  // op 46523: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 46524: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4588", "key7442", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4588, key7442) size=" << scan_result.size() << std::endl;
  }

  // op 46525: get
  status = db->Get(read_options, "key2664", &value);
  if (status.ok()) {
    out << "GET key2664 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46526: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3071", "key10227", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3071, key10227) size=" << scan_result.size() << std::endl;
  }

  // op 46527: delete
  db->Delete(write_options, "key1280");

  // op 46528: delete
  db->Delete(write_options, "key10866");

  // op 46529: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5032", "key9215", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5032, key9215) size=" << scan_result.size() << std::endl;
  }

  // op 46530: put
  db->Put(write_options, "key11249", "value17944");

  // op 46531: put
  db->Put(write_options, "key1732", "value62134");

  // op 46532: get
  status = db->Get(read_options, "key4842", &value);
  if (status.ok()) {
    out << "GET key4842 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46533: delete_range
  status = db->DeleteRange(write_options, "key6686", "key8363");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 46534: get
  status = db->Get(read_options, "key10701", &value);
  if (status.ok()) {
    out << "GET key10701 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46535: put
  db->Put(write_options, "key4445", "value54612");

  // op 46536: put
  db->Put(write_options, "key3473", "value106707");

  // op 46537: put
  db->Put(write_options, "key10494", "value49660");

  // op 46538: put
  db->Put(write_options, "key8274", "value86375");

  // op 46539: get
  status = db->Get(read_options, "key11698", &value);
  if (status.ok()) {
    out << "GET key11698 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46540: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 46541: get
  status = db->Get(read_options, "key2663", &value);
  if (status.ok()) {
    out << "GET key2663 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46542: put
  db->Put(write_options, "key8609", "value7451");

  // op 46543: put
  db->Put(write_options, "key3570", "value45629");

  // op 46544: delete
  db->Delete(write_options, "key9597");

  // op 46545: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1714", "key2726", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1714, key2726) size=" << scan_result.size() << std::endl;
  }

  // op 46546: put
  db->Put(write_options, "key10272", "value105952");

  // op 46547: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 46548: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4833", "key7943", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4833, key7943) size=" << scan_result.size() << std::endl;
  }

  // op 46549: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1151", "key1901", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1151, key1901) size=" << scan_result.size() << std::endl;
  }

  // op 46550: put
  db->Put(write_options, "key5170", "value51042");

  // op 46551: put
  db->Put(write_options, "key5152", "value114512");

  // op 46552: delete
  db->Delete(write_options, "key0481");

  // op 46553: put
  db->Put(write_options, "key2954", "value51851");

  // op 46554: get
  status = db->Get(read_options, "key11618", &value);
  if (status.ok()) {
    out << "GET key11618 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46555: delete
  db->Delete(write_options, "key9615");

  // op 46556: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7796", "key9756", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7796, key9756) size=" << scan_result.size() << std::endl;
  }

  // op 46557: put
  db->Put(write_options, "key0153", "value71854");

  // op 46558: get
  status = db->Get(read_options, "key1745", &value);
  if (status.ok()) {
    out << "GET key1745 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46559: put
  db->Put(write_options, "key2710", "value5464");

  // op 46560: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5103", "key9825", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5103, key9825) size=" << scan_result.size() << std::endl;
  }

  // op 46561: put
  db->Put(write_options, "key1355", "value72150");

  // op 46562: delete_range
  status = db->DeleteRange(write_options, "key1262", "key9187");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 46563: put
  db->Put(write_options, "key3598", "value7803");

  // op 46564: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1376", "key5855", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1376, key5855) size=" << scan_result.size() << std::endl;
  }

  // op 46565: put
  db->Put(write_options, "key2167", "value75388");

  // op 46566: put
  db->Put(write_options, "key6786", "value75191");

  // op 46567: get
  status = db->Get(read_options, "key5134", &value);
  if (status.ok()) {
    out << "GET key5134 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46568: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3479", "key7087", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3479, key7087) size=" << scan_result.size() << std::endl;
  }

  // op 46569: put
  db->Put(write_options, "key1661", "value18783");

  // op 46570: put
  db->Put(write_options, "key5314", "value63319");

  // op 46571: delete_range
  status = db->DeleteRange(write_options, "key3676", "key10270");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 46572: get
  status = db->Get(read_options, "key1593", &value);
  if (status.ok()) {
    out << "GET key1593 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46573: put
  db->Put(write_options, "key1811", "value20001");

  // op 46574: put
  db->Put(write_options, "key5291", "value38094");

  // op 46575: delete_range
  status = db->DeleteRange(write_options, "key3600", "key4066");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 46576: delete
  db->Delete(write_options, "key9376");

  // op 46577: get
  status = db->Get(read_options, "key9749", &value);
  if (status.ok()) {
    out << "GET key9749 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46578: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4262", "key9086", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4262, key9086) size=" << scan_result.size() << std::endl;
  }

  // op 46579: put
  db->Put(write_options, "key11569", "value48898");

  // op 46580: put
  db->Put(write_options, "key5966", "value81707");

  // op 46581: put
  db->Put(write_options, "key5702", "value3119");

  // op 46582: put
  db->Put(write_options, "key11155", "value113571");

  // op 46583: put
  db->Put(write_options, "key1174", "value84624");

  // op 46584: put
  db->Put(write_options, "key8927", "value26752");

  // op 46585: put
  db->Put(write_options, "key11328", "value58894");

  // op 46586: delete
  db->Delete(write_options, "key10551");

  // op 46587: put
  db->Put(write_options, "key7348", "value35557");

  // op 46588: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8572", "key9315", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8572, key9315) size=" << scan_result.size() << std::endl;
  }

  // op 46589: get
  status = db->Get(read_options, "key9859", &value);
  if (status.ok()) {
    out << "GET key9859 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46590: get
  status = db->Get(read_options, "key4181", &value);
  if (status.ok()) {
    out << "GET key4181 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46591: delete
  db->Delete(write_options, "key4267");

  // op 46592: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 46593: put
  db->Put(write_options, "key9681", "value65317");

  // op 46594: put
  db->Put(write_options, "key0120", "value42733");

  // op 46595: delete
  db->Delete(write_options, "key3533");

  // op 46596: put
  db->Put(write_options, "key10420", "value71284");

  // op 46597: get
  status = db->Get(read_options, "key2996", &value);
  if (status.ok()) {
    out << "GET key2996 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46598: delete_range
  status = db->DeleteRange(write_options, "key4179", "key5517");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 46599: delete_range
  status = db->DeleteRange(write_options, "key0883", "key6995");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 46600: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1891", "key5035", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1891, key5035) size=" << scan_result.size() << std::endl;
  }

  // op 46601: get
  status = db->Get(read_options, "key7023", &value);
  if (status.ok()) {
    out << "GET key7023 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46602: put
  db->Put(write_options, "key8963", "value28238");

  // op 46603: get
  status = db->Get(read_options, "key3914", &value);
  if (status.ok()) {
    out << "GET key3914 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46604: delete
  db->Delete(write_options, "key11446");

  // op 46605: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1221", "key7957", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1221, key7957) size=" << scan_result.size() << std::endl;
  }

  // op 46606: put
  db->Put(write_options, "key3481", "value7871");

  // op 46607: put
  db->Put(write_options, "key9274", "value64231");

  // op 46608: get
  status = db->Get(read_options, "key4917", &value);
  if (status.ok()) {
    out << "GET key4917 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46609: put
  db->Put(write_options, "key4194", "value71935");

  // op 46610: put
  db->Put(write_options, "key4680", "value78008");

  // op 46611: put
  db->Put(write_options, "key9201", "value87598");

  // op 46612: delete_range
  status = db->DeleteRange(write_options, "key5837", "key6851");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 46613: put
  db->Put(write_options, "key11116", "value88985");

  // op 46614: get
  status = db->Get(read_options, "key6514", &value);
  if (status.ok()) {
    out << "GET key6514 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46615: get
  status = db->Get(read_options, "key11459", &value);
  if (status.ok()) {
    out << "GET key11459 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46616: delete
  db->Delete(write_options, "key6605");

  // op 46617: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3808", "key6001", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3808, key6001) size=" << scan_result.size() << std::endl;
  }

  // op 46618: put
  db->Put(write_options, "key9200", "value100537");

  // op 46619: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2263", "key9729", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2263, key9729) size=" << scan_result.size() << std::endl;
  }

  // op 46620: delete
  db->Delete(write_options, "key4679");

  // op 46621: put
  db->Put(write_options, "key1098", "value63381");

  // op 46622: put
  db->Put(write_options, "key10720", "value14389");

  // op 46623: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0834", "key10279", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0834, key10279) size=" << scan_result.size() << std::endl;
  }

  // op 46624: delete
  db->Delete(write_options, "key8373");

  // op 46625: delete
  db->Delete(write_options, "key4365");

  // op 46626: get
  status = db->Get(read_options, "key7465", &value);
  if (status.ok()) {
    out << "GET key7465 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46627: put
  db->Put(write_options, "key6700", "value85922");

  // op 46628: delete
  db->Delete(write_options, "key1716");

  // op 46629: put
  db->Put(write_options, "key7628", "value85056");

  // op 46630: delete
  db->Delete(write_options, "key0553");

  // op 46631: delete
  db->Delete(write_options, "key3673");

  // op 46632: put
  db->Put(write_options, "key7211", "value11501");

  // op 46633: put
  db->Put(write_options, "key10121", "value79843");

  // op 46634: delete_range
  status = db->DeleteRange(write_options, "key9305", "key11612");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 46635: put
  db->Put(write_options, "key2876", "value23475");

  // op 46636: put
  db->Put(write_options, "key5777", "value99922");

  // op 46637: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 46638: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5936", "key6351", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5936, key6351) size=" << scan_result.size() << std::endl;
  }

  // op 46639: get
  status = db->Get(read_options, "key4136", &value);
  if (status.ok()) {
    out << "GET key4136 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46640: put
  db->Put(write_options, "key8701", "value68612");

  // op 46641: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 46642: delete
  db->Delete(write_options, "key9694");

  // op 46643: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0355", "key0752", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0355, key0752) size=" << scan_result.size() << std::endl;
  }

  // op 46644: get
  status = db->Get(read_options, "key6890", &value);
  if (status.ok()) {
    out << "GET key6890 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46645: put
  db->Put(write_options, "key1283", "value44355");

  // op 46646: put
  db->Put(write_options, "key11060", "value102597");

  // op 46647: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8607", "key10643", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8607, key10643) size=" << scan_result.size() << std::endl;
  }

  // op 46648: scan
  scan_result.clear();
  status = db->Scan(read_options, "key11409", "key11500", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key11409, key11500) size=" << scan_result.size() << std::endl;
  }

  // op 46649: get
  status = db->Get(read_options, "key2190", &value);
  if (status.ok()) {
    out << "GET key2190 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46650: delete
  db->Delete(write_options, "key0858");

  // op 46651: delete
  db->Delete(write_options, "key9976");

  // op 46652: put
  db->Put(write_options, "key4192", "value11703");

  // op 46653: delete_range
  status = db->DeleteRange(write_options, "key3996", "key8511");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 46654: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0806", "key3817", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0806, key3817) size=" << scan_result.size() << std::endl;
  }

  // op 46655: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1879", "key10311", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1879, key10311) size=" << scan_result.size() << std::endl;
  }

  // op 46656: put
  db->Put(write_options, "key6955", "value54150");

  // op 46657: delete
  db->Delete(write_options, "key10336");

  // op 46658: delete_range
  status = db->DeleteRange(write_options, "key0052", "key6863");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 46659: put
  db->Put(write_options, "key9407", "value37891");

  // op 46660: put
  db->Put(write_options, "key1726", "value106197");

  // op 46661: delete
  db->Delete(write_options, "key6611");

  // op 46662: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 46663: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4710", "key6980", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4710, key6980) size=" << scan_result.size() << std::endl;
  }

  // op 46664: put
  db->Put(write_options, "key9929", "value43699");

  // op 46665: delete_range
  status = db->DeleteRange(write_options, "key3695", "key8715");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 46666: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7001", "key7972", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7001, key7972) size=" << scan_result.size() << std::endl;
  }

  // op 46667: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5375", "key8163", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5375, key8163) size=" << scan_result.size() << std::endl;
  }

  // op 46668: delete
  db->Delete(write_options, "key4566");

  // op 46669: put
  db->Put(write_options, "key11319", "value109434");

  // op 46670: put
  db->Put(write_options, "key11293", "value75486");

  // op 46671: put
  db->Put(write_options, "key1275", "value12000");

  // op 46672: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 46673: delete
  db->Delete(write_options, "key7468");

  // op 46674: delete_range
  status = db->DeleteRange(write_options, "key5901", "key8975");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 46675: put
  db->Put(write_options, "key7344", "value73806");

  // op 46676: get
  status = db->Get(read_options, "key9639", &value);
  if (status.ok()) {
    out << "GET key9639 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46677: put
  db->Put(write_options, "key2114", "value110939");

  // op 46678: put
  db->Put(write_options, "key8140", "value14492");

  // op 46679: put
  db->Put(write_options, "key10411", "value92852");

  // op 46680: get
  status = db->Get(read_options, "key4269", &value);
  if (status.ok()) {
    out << "GET key4269 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46681: get
  status = db->Get(read_options, "key6232", &value);
  if (status.ok()) {
    out << "GET key6232 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46682: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8927", "key8952", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8927, key8952) size=" << scan_result.size() << std::endl;
  }

  // op 46683: put
  db->Put(write_options, "key3042", "value58597");

  // op 46684: put
  db->Put(write_options, "key10225", "value55074");

  // op 46685: put
  db->Put(write_options, "key8951", "value53399");

  // op 46686: put
  db->Put(write_options, "key7409", "value57957");

  // op 46687: put
  db->Put(write_options, "key8753", "value45497");

  // op 46688: get
  status = db->Get(read_options, "key3242", &value);
  if (status.ok()) {
    out << "GET key3242 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46689: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5272", "key7444", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5272, key7444) size=" << scan_result.size() << std::endl;
  }

  // op 46690: put
  db->Put(write_options, "key8512", "value72363");

  // op 46691: put
  db->Put(write_options, "key2148", "value81270");

  // op 46692: delete_range
  status = db->DeleteRange(write_options, "key3667", "key6899");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 46693: get
  status = db->Get(read_options, "key0387", &value);
  if (status.ok()) {
    out << "GET key0387 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46694: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 46695: put
  db->Put(write_options, "key2610", "value90047");

  // op 46696: get
  status = db->Get(read_options, "key10438", &value);
  if (status.ok()) {
    out << "GET key10438 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46697: put
  db->Put(write_options, "key3014", "value78387");

  // op 46698: put
  db->Put(write_options, "key10745", "value107366");

  // op 46699: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1676", "key9649", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1676, key9649) size=" << scan_result.size() << std::endl;
  }

  // op 46700: delete
  db->Delete(write_options, "key2485");

  // op 46701: delete
  db->Delete(write_options, "key4348");

  // op 46702: put
  db->Put(write_options, "key9740", "value54361");

  // op 46703: put
  db->Put(write_options, "key2796", "value64492");

  // op 46704: put
  db->Put(write_options, "key9939", "value49678");

  // op 46705: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3660", "key3836", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3660, key3836) size=" << scan_result.size() << std::endl;
  }

  // op 46706: delete_range
  status = db->DeleteRange(write_options, "key9816", "key11359");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 46707: put
  db->Put(write_options, "key7915", "value6852");

  // op 46708: delete_range
  status = db->DeleteRange(write_options, "key8621", "key10850");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 46709: put
  db->Put(write_options, "key3920", "value17874");

  // op 46710: put
  db->Put(write_options, "key8782", "value57045");

  // op 46711: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5730", "key7518", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5730, key7518) size=" << scan_result.size() << std::endl;
  }

  // op 46712: get
  status = db->Get(read_options, "key7461", &value);
  if (status.ok()) {
    out << "GET key7461 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46713: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4971", "key5698", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4971, key5698) size=" << scan_result.size() << std::endl;
  }

  // op 46714: put
  db->Put(write_options, "key4705", "value87339");

  // op 46715: put
  db->Put(write_options, "key10151", "value91650");

  // op 46716: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7964", "key10275", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7964, key10275) size=" << scan_result.size() << std::endl;
  }

  // op 46717: put
  db->Put(write_options, "key0824", "value117992");

  // op 46718: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 46719: delete_range
  status = db->DeleteRange(write_options, "key1575", "key7181");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 46720: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 46721: delete_range
  status = db->DeleteRange(write_options, "key8597", "key10930");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 46722: get
  status = db->Get(read_options, "key9680", &value);
  if (status.ok()) {
    out << "GET key9680 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46723: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0090", "key0287", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0090, key0287) size=" << scan_result.size() << std::endl;
  }

  // op 46724: delete_range
  status = db->DeleteRange(write_options, "key0179", "key6049");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 46725: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 46726: delete
  db->Delete(write_options, "key4665");

  // op 46727: delete
  db->Delete(write_options, "key4037");

  // op 46728: delete
  db->Delete(write_options, "key3759");

  // op 46729: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5044", "key9963", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5044, key9963) size=" << scan_result.size() << std::endl;
  }

  // op 46730: delete_range
  status = db->DeleteRange(write_options, "key5781", "key7080");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 46731: put
  db->Put(write_options, "key1332", "value42100");

  // op 46732: put
  db->Put(write_options, "key5177", "value32187");

  // op 46733: put
  db->Put(write_options, "key0736", "value56837");

  // op 46734: get
  status = db->Get(read_options, "key10378", &value);
  if (status.ok()) {
    out << "GET key10378 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46735: put
  db->Put(write_options, "key7905", "value90616");

  // op 46736: put
  db->Put(write_options, "key8081", "value110277");

  // op 46737: put
  db->Put(write_options, "key11521", "value101460");

  // op 46738: put
  db->Put(write_options, "key4805", "value62786");

  // op 46739: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2918", "key10444", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2918, key10444) size=" << scan_result.size() << std::endl;
  }

  // op 46740: put
  db->Put(write_options, "key1232", "value8055");

  // op 46741: put
  db->Put(write_options, "key1951", "value29397");

  // op 46742: delete_range
  status = db->DeleteRange(write_options, "key7825", "key10845");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 46743: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 46744: put
  db->Put(write_options, "key2769", "value30948");

  // op 46745: delete
  db->Delete(write_options, "key4680");

  // op 46746: put
  db->Put(write_options, "key0601", "value67930");

  // op 46747: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1063", "key3465", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1063, key3465) size=" << scan_result.size() << std::endl;
  }

  // op 46748: put
  db->Put(write_options, "key8580", "value93159");

  // op 46749: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 46750: put
  db->Put(write_options, "key3337", "value88218");

  // op 46751: put
  db->Put(write_options, "key11429", "value27950");

  // op 46752: delete
  db->Delete(write_options, "key1184");

  // op 46753: get
  status = db->Get(read_options, "key5099", &value);
  if (status.ok()) {
    out << "GET key5099 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46754: delete
  db->Delete(write_options, "key8427");

  // op 46755: put
  db->Put(write_options, "key10100", "value61946");

  // op 46756: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5186", "key8654", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5186, key8654) size=" << scan_result.size() << std::endl;
  }

  // op 46757: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4788", "key11716", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4788, key11716) size=" << scan_result.size() << std::endl;
  }

  // op 46758: delete_range
  status = db->DeleteRange(write_options, "key5014", "key9738");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 46759: delete_range
  status = db->DeleteRange(write_options, "key5759", "key5823");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 46760: get
  status = db->Get(read_options, "key1898", &value);
  if (status.ok()) {
    out << "GET key1898 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46761: put
  db->Put(write_options, "key4225", "value36111");

  // op 46762: delete
  db->Delete(write_options, "key0333");

  // op 46763: delete
  db->Delete(write_options, "key9749");

  // op 46764: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6751", "key7020", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6751, key7020) size=" << scan_result.size() << std::endl;
  }

  // op 46765: put
  db->Put(write_options, "key1449", "value115186");

  // op 46766: put
  db->Put(write_options, "key5053", "value39147");

  // op 46767: put
  db->Put(write_options, "key5898", "value32116");

  // op 46768: delete_range
  status = db->DeleteRange(write_options, "key3874", "key11498");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 46769: delete_range
  status = db->DeleteRange(write_options, "key4921", "key5993");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 46770: delete
  db->Delete(write_options, "key8477");

  // op 46771: delete
  db->Delete(write_options, "key3536");

  // op 46772: get
  status = db->Get(read_options, "key5428", &value);
  if (status.ok()) {
    out << "GET key5428 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46773: put
  db->Put(write_options, "key7843", "value86340");

  // op 46774: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4345", "key8831", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4345, key8831) size=" << scan_result.size() << std::endl;
  }

  // op 46775: get
  status = db->Get(read_options, "key11275", &value);
  if (status.ok()) {
    out << "GET key11275 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46776: put
  db->Put(write_options, "key4210", "value11370");

  // op 46777: put
  db->Put(write_options, "key9494", "value96449");

  // op 46778: put
  db->Put(write_options, "key6412", "value35304");

  // op 46779: get
  status = db->Get(read_options, "key9224", &value);
  if (status.ok()) {
    out << "GET key9224 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46780: put
  db->Put(write_options, "key3848", "value39701");

  // op 46781: put
  db->Put(write_options, "key11326", "value104519");

  // op 46782: put
  db->Put(write_options, "key0330", "value96075");

  // op 46783: put
  db->Put(write_options, "key11389", "value83099");

  // op 46784: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5417", "key6187", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5417, key6187) size=" << scan_result.size() << std::endl;
  }

  // op 46785: delete_range
  status = db->DeleteRange(write_options, "key3564", "key5433");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 46786: put
  db->Put(write_options, "key7739", "value58630");

  // op 46787: put
  db->Put(write_options, "key8825", "value117266");

  // op 46788: put
  db->Put(write_options, "key3499", "value95382");

  // op 46789: put
  db->Put(write_options, "key9069", "value113769");

  // op 46790: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5159", "key9623", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5159, key9623) size=" << scan_result.size() << std::endl;
  }

  // op 46791: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0294", "key6533", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0294, key6533) size=" << scan_result.size() << std::endl;
  }

  // op 46792: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2465", "key6711", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2465, key6711) size=" << scan_result.size() << std::endl;
  }

  // op 46793: delete
  db->Delete(write_options, "key6257");

  // op 46794: delete
  db->Delete(write_options, "key6168");

  // op 46795: get
  status = db->Get(read_options, "key11250", &value);
  if (status.ok()) {
    out << "GET key11250 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46796: put
  db->Put(write_options, "key2538", "value81087");

  // op 46797: delete_range
  status = db->DeleteRange(write_options, "key2184", "key4399");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 46798: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 46799: delete
  db->Delete(write_options, "key9629");

  // op 46800: put
  db->Put(write_options, "key2951", "value79888");

  // op 46801: delete_range
  status = db->DeleteRange(write_options, "key2550", "key5743");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 46802: put
  db->Put(write_options, "key9179", "value107791");

  // op 46803: put
  db->Put(write_options, "key7009", "value4856");

  // op 46804: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4562", "key9683", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4562, key9683) size=" << scan_result.size() << std::endl;
  }

  // op 46805: delete_range
  status = db->DeleteRange(write_options, "key4382", "key8578");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 46806: put
  db->Put(write_options, "key1364", "value82827");

  // op 46807: delete_range
  status = db->DeleteRange(write_options, "key5375", "key11270");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 46808: delete_range
  status = db->DeleteRange(write_options, "key2413", "key5910");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 46809: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7606", "key9730", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7606, key9730) size=" << scan_result.size() << std::endl;
  }

  // op 46810: get
  status = db->Get(read_options, "key0717", &value);
  if (status.ok()) {
    out << "GET key0717 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46811: put
  db->Put(write_options, "key1680", "value106440");

  // op 46812: put
  db->Put(write_options, "key1651", "value30233");

  // op 46813: put
  db->Put(write_options, "key5436", "value69845");

  // op 46814: delete
  db->Delete(write_options, "key5200");

  // op 46815: put
  db->Put(write_options, "key3518", "value73354");

  // op 46816: put
  db->Put(write_options, "key2136", "value53842");

  // op 46817: put
  db->Put(write_options, "key3488", "value35804");

  // op 46818: put
  db->Put(write_options, "key9920", "value61824");

  // op 46819: get
  status = db->Get(read_options, "key1146", &value);
  if (status.ok()) {
    out << "GET key1146 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46820: put
  db->Put(write_options, "key6584", "value32224");

  // op 46821: get
  status = db->Get(read_options, "key3990", &value);
  if (status.ok()) {
    out << "GET key3990 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46822: delete
  db->Delete(write_options, "key8369");

  // op 46823: put
  db->Put(write_options, "key11458", "value46052");

  // op 46824: put
  db->Put(write_options, "key1780", "value58139");

  // op 46825: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4323", "key8580", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4323, key8580) size=" << scan_result.size() << std::endl;
  }

  // op 46826: put
  db->Put(write_options, "key5673", "value71358");

  // op 46827: put
  db->Put(write_options, "key2498", "value76362");

  // op 46828: put
  db->Put(write_options, "key8193", "value63020");

  // op 46829: get
  status = db->Get(read_options, "key11019", &value);
  if (status.ok()) {
    out << "GET key11019 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46830: delete
  db->Delete(write_options, "key5202");

  // op 46831: put
  db->Put(write_options, "key10492", "value57964");

  // op 46832: get
  status = db->Get(read_options, "key7533", &value);
  if (status.ok()) {
    out << "GET key7533 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46833: put
  db->Put(write_options, "key10421", "value54134");

  // op 46834: get
  status = db->Get(read_options, "key9078", &value);
  if (status.ok()) {
    out << "GET key9078 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46835: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 46836: delete_range
  status = db->DeleteRange(write_options, "key4360", "key8751");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 46837: delete
  db->Delete(write_options, "key6337");

  // op 46838: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 46839: put
  db->Put(write_options, "key11011", "value33097");

  // op 46840: delete_range
  status = db->DeleteRange(write_options, "key7379", "key10641");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 46841: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4510", "key9043", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4510, key9043) size=" << scan_result.size() << std::endl;
  }

  // op 46842: get
  status = db->Get(read_options, "key1081", &value);
  if (status.ok()) {
    out << "GET key1081 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46843: put
  db->Put(write_options, "key7605", "value73761");

  // op 46844: get
  status = db->Get(read_options, "key5451", &value);
  if (status.ok()) {
    out << "GET key5451 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46845: get
  status = db->Get(read_options, "key6905", &value);
  if (status.ok()) {
    out << "GET key6905 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46846: put
  db->Put(write_options, "key3397", "value51284");

  // op 46847: put
  db->Put(write_options, "key11613", "value110600");

  // op 46848: delete_range
  status = db->DeleteRange(write_options, "key7102", "key8469");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 46849: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 46850: put
  db->Put(write_options, "key11238", "value92815");

  // op 46851: put
  db->Put(write_options, "key1835", "value76517");

  // op 46852: delete
  db->Delete(write_options, "key0213");

  // op 46853: delete
  db->Delete(write_options, "key10714");

  // op 46854: put
  db->Put(write_options, "key8396", "value60871");

  // op 46855: put
  db->Put(write_options, "key8033", "value58933");

  // op 46856: get
  status = db->Get(read_options, "key3208", &value);
  if (status.ok()) {
    out << "GET key3208 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46857: get
  status = db->Get(read_options, "key3188", &value);
  if (status.ok()) {
    out << "GET key3188 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46858: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6028", "key9937", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6028, key9937) size=" << scan_result.size() << std::endl;
  }

  // op 46859: put
  db->Put(write_options, "key6408", "value109240");

  // op 46860: delete
  db->Delete(write_options, "key3168");

  // op 46861: put
  db->Put(write_options, "key0170", "value44952");

  // op 46862: put
  db->Put(write_options, "key3206", "value4995");

  // op 46863: put
  db->Put(write_options, "key0511", "value65524");

  // op 46864: put
  db->Put(write_options, "key1094", "value40932");

  // op 46865: put
  db->Put(write_options, "key7812", "value24083");

  // op 46866: delete
  db->Delete(write_options, "key2865");

  // op 46867: delete
  db->Delete(write_options, "key8804");

  // op 46868: delete
  db->Delete(write_options, "key3251");

  // op 46869: put
  db->Put(write_options, "key4146", "value51077");

  // op 46870: delete
  db->Delete(write_options, "key7527");

  // op 46871: delete
  db->Delete(write_options, "key0003");

  // op 46872: put
  db->Put(write_options, "key5148", "value3556");

  // op 46873: delete_range
  status = db->DeleteRange(write_options, "key10219", "key11402");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 46874: get
  status = db->Get(read_options, "key11314", &value);
  if (status.ok()) {
    out << "GET key11314 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46875: get
  status = db->Get(read_options, "key1664", &value);
  if (status.ok()) {
    out << "GET key1664 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46876: put
  db->Put(write_options, "key3879", "value74528");

  // op 46877: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8483", "key8850", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8483, key8850) size=" << scan_result.size() << std::endl;
  }

  // op 46878: put
  db->Put(write_options, "key1128", "value28617");

  // op 46879: put
  db->Put(write_options, "key10460", "value2233");

  // op 46880: delete_range
  status = db->DeleteRange(write_options, "key6447", "key8445");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 46881: delete_range
  status = db->DeleteRange(write_options, "key1396", "key10966");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 46882: put
  db->Put(write_options, "key10287", "value91790");

  // op 46883: put
  db->Put(write_options, "key1193", "value17684");

  // op 46884: put
  db->Put(write_options, "key9207", "value22145");

  // op 46885: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0967", "key3605", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0967, key3605) size=" << scan_result.size() << std::endl;
  }

  // op 46886: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6022", "key6344", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6022, key6344) size=" << scan_result.size() << std::endl;
  }

  // op 46887: delete
  db->Delete(write_options, "key6093");

  // op 46888: put
  db->Put(write_options, "key10275", "value104507");

  // op 46889: put
  db->Put(write_options, "key5120", "value87485");

  // op 46890: get
  status = db->Get(read_options, "key2760", &value);
  if (status.ok()) {
    out << "GET key2760 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46891: delete_range
  status = db->DeleteRange(write_options, "key1881", "key5485");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 46892: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2442", "key7035", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2442, key7035) size=" << scan_result.size() << std::endl;
  }

  // op 46893: put
  db->Put(write_options, "key5436", "value92734");

  // op 46894: put
  db->Put(write_options, "key0630", "value65504");

  // op 46895: delete
  db->Delete(write_options, "key5479");

  // op 46896: delete
  db->Delete(write_options, "key0351");

  // op 46897: put
  db->Put(write_options, "key0045", "value1344");

  // op 46898: get
  status = db->Get(read_options, "key4603", &value);
  if (status.ok()) {
    out << "GET key4603 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46899: delete
  db->Delete(write_options, "key11721");

  // op 46900: put
  db->Put(write_options, "key10457", "value76345");

  // op 46901: get
  status = db->Get(read_options, "key0227", &value);
  if (status.ok()) {
    out << "GET key0227 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46902: put
  db->Put(write_options, "key7332", "value93103");

  // op 46903: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4484", "key7919", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4484, key7919) size=" << scan_result.size() << std::endl;
  }

  // op 46904: put
  db->Put(write_options, "key0173", "value33037");

  // op 46905: delete_range
  status = db->DeleteRange(write_options, "key11402", "key11698");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 46906: put
  db->Put(write_options, "key6410", "value81895");

  // op 46907: get
  status = db->Get(read_options, "key3549", &value);
  if (status.ok()) {
    out << "GET key3549 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46908: put
  db->Put(write_options, "key1780", "value59735");

  // op 46909: get
  status = db->Get(read_options, "key10049", &value);
  if (status.ok()) {
    out << "GET key10049 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46910: put
  db->Put(write_options, "key7977", "value60509");

  // op 46911: get
  status = db->Get(read_options, "key5301", &value);
  if (status.ok()) {
    out << "GET key5301 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46912: get
  status = db->Get(read_options, "key4571", &value);
  if (status.ok()) {
    out << "GET key4571 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46913: delete_range
  status = db->DeleteRange(write_options, "key1006", "key7107");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 46914: delete
  db->Delete(write_options, "key0689");

  // op 46915: put
  db->Put(write_options, "key1104", "value107678");

  // op 46916: get
  status = db->Get(read_options, "key1870", &value);
  if (status.ok()) {
    out << "GET key1870 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46917: get
  status = db->Get(read_options, "key10953", &value);
  if (status.ok()) {
    out << "GET key10953 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46918: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6081", "key10165", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6081, key10165) size=" << scan_result.size() << std::endl;
  }

  // op 46919: get
  status = db->Get(read_options, "key4575", &value);
  if (status.ok()) {
    out << "GET key4575 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46920: put
  db->Put(write_options, "key11366", "value2960");

  // op 46921: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 46922: put
  db->Put(write_options, "key10906", "value90287");

  // op 46923: put
  db->Put(write_options, "key10872", "value70459");

  // op 46924: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 46925: get
  status = db->Get(read_options, "key2598", &value);
  if (status.ok()) {
    out << "GET key2598 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46926: delete_range
  status = db->DeleteRange(write_options, "key6399", "key7238");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 46927: put
  db->Put(write_options, "key9035", "value19010");

  // op 46928: put
  db->Put(write_options, "key8036", "value30414");

  // op 46929: put
  db->Put(write_options, "key4967", "value65362");

  // op 46930: get
  status = db->Get(read_options, "key10880", &value);
  if (status.ok()) {
    out << "GET key10880 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46931: get
  status = db->Get(read_options, "key0843", &value);
  if (status.ok()) {
    out << "GET key0843 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46932: put
  db->Put(write_options, "key0293", "value38499");

  // op 46933: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2433", "key7210", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2433, key7210) size=" << scan_result.size() << std::endl;
  }

  // op 46934: delete
  db->Delete(write_options, "key0998");

  // op 46935: put
  db->Put(write_options, "key1421", "value70844");

  // op 46936: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1005", "key10150", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1005, key10150) size=" << scan_result.size() << std::endl;
  }

  // op 46937: get
  status = db->Get(read_options, "key9195", &value);
  if (status.ok()) {
    out << "GET key9195 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46938: delete_range
  status = db->DeleteRange(write_options, "key10377", "key10885");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 46939: get
  status = db->Get(read_options, "key7221", &value);
  if (status.ok()) {
    out << "GET key7221 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46940: put
  db->Put(write_options, "key6174", "value3983");

  // op 46941: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9530", "key10930", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9530, key10930) size=" << scan_result.size() << std::endl;
  }

  // op 46942: delete_range
  status = db->DeleteRange(write_options, "key1023", "key5161");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 46943: put
  db->Put(write_options, "key1054", "value32644");

  // op 46944: put
  db->Put(write_options, "key0242", "value89108");

  // op 46945: delete_range
  status = db->DeleteRange(write_options, "key9945", "key10624");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 46946: put
  db->Put(write_options, "key4205", "value70749");

  // op 46947: delete
  db->Delete(write_options, "key1331");

  // op 46948: put
  db->Put(write_options, "key0114", "value90600");

  // op 46949: get
  status = db->Get(read_options, "key4518", &value);
  if (status.ok()) {
    out << "GET key4518 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46950: put
  db->Put(write_options, "key1260", "value0117");

  // op 46951: put
  db->Put(write_options, "key1578", "value59624");

  // op 46952: put
  db->Put(write_options, "key10156", "value60824");

  // op 46953: put
  db->Put(write_options, "key7960", "value101875");

  // op 46954: put
  db->Put(write_options, "key5125", "value18719");

  // op 46955: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10314", "key10574", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10314, key10574) size=" << scan_result.size() << std::endl;
  }

  // op 46956: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6412", "key6604", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6412, key6604) size=" << scan_result.size() << std::endl;
  }

  // op 46957: put
  db->Put(write_options, "key1869", "value74944");

  // op 46958: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 46959: put
  db->Put(write_options, "key1263", "value31727");

  // op 46960: get
  status = db->Get(read_options, "key3804", &value);
  if (status.ok()) {
    out << "GET key3804 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46961: delete
  db->Delete(write_options, "key7227");

  // op 46962: get
  status = db->Get(read_options, "key10484", &value);
  if (status.ok()) {
    out << "GET key10484 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46963: put
  db->Put(write_options, "key3957", "value115015");

  // op 46964: get
  status = db->Get(read_options, "key7722", &value);
  if (status.ok()) {
    out << "GET key7722 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46965: put
  db->Put(write_options, "key0304", "value78097");

  // op 46966: delete_range
  status = db->DeleteRange(write_options, "key8478", "key10861");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 46967: delete_range
  status = db->DeleteRange(write_options, "key2403", "key2582");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 46968: get
  status = db->Get(read_options, "key9549", &value);
  if (status.ok()) {
    out << "GET key9549 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46969: put
  db->Put(write_options, "key5661", "value5882");

  // op 46970: get
  status = db->Get(read_options, "key7420", &value);
  if (status.ok()) {
    out << "GET key7420 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46971: put
  db->Put(write_options, "key9571", "value41717");

  // op 46972: put
  db->Put(write_options, "key2095", "value6079");

  // op 46973: delete
  db->Delete(write_options, "key3105");

  // op 46974: delete
  db->Delete(write_options, "key1879");

  // op 46975: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0373", "key6038", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0373, key6038) size=" << scan_result.size() << std::endl;
  }

  // op 46976: get
  status = db->Get(read_options, "key9810", &value);
  if (status.ok()) {
    out << "GET key9810 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46977: delete
  db->Delete(write_options, "key6148");

  // op 46978: delete_range
  status = db->DeleteRange(write_options, "key10008", "key10641");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 46979: delete_range
  status = db->DeleteRange(write_options, "key3660", "key9692");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 46980: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6016", "key8378", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6016, key8378) size=" << scan_result.size() << std::endl;
  }

  // op 46981: get
  status = db->Get(read_options, "key0921", &value);
  if (status.ok()) {
    out << "GET key0921 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46982: get
  status = db->Get(read_options, "key7292", &value);
  if (status.ok()) {
    out << "GET key7292 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46983: get
  status = db->Get(read_options, "key1939", &value);
  if (status.ok()) {
    out << "GET key1939 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46984: put
  db->Put(write_options, "key2226", "value80305");

  // op 46985: get
  status = db->Get(read_options, "key11470", &value);
  if (status.ok()) {
    out << "GET key11470 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46986: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6405", "key8641", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6405, key8641) size=" << scan_result.size() << std::endl;
  }

  // op 46987: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3988", "key7159", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3988, key7159) size=" << scan_result.size() << std::endl;
  }

  // op 46988: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3673", "key4082", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3673, key4082) size=" << scan_result.size() << std::endl;
  }

  // op 46989: delete
  db->Delete(write_options, "key9699");

  // op 46990: delete
  db->Delete(write_options, "key6210");

  // op 46991: put
  db->Put(write_options, "key0606", "value9857");

  // op 46992: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3430", "key4775", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3430, key4775) size=" << scan_result.size() << std::endl;
  }

  // op 46993: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6458", "key8994", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6458, key8994) size=" << scan_result.size() << std::endl;
  }

  // op 46994: put
  db->Put(write_options, "key6032", "value98360");

  // op 46995: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10845", "key11510", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10845, key11510) size=" << scan_result.size() << std::endl;
  }

  // op 46996: put
  db->Put(write_options, "key2026", "value5482");

  // op 46997: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5749", "key7323", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5749, key7323) size=" << scan_result.size() << std::endl;
  }

  // op 46998: get
  status = db->Get(read_options, "key4174", &value);
  if (status.ok()) {
    out << "GET key4174 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 46999: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1374", "key3497", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1374, key3497) size=" << scan_result.size() << std::endl;
  }

  // op 47000: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2883", "key5659", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2883, key5659) size=" << scan_result.size() << std::endl;
  }

  // op 47001: delete_range
  status = db->DeleteRange(write_options, "key0998", "key3014");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 47002: delete
  db->Delete(write_options, "key8916");

  // op 47003: put
  db->Put(write_options, "key8387", "value6916");

  // op 47004: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 47005: delete_range
  status = db->DeleteRange(write_options, "key5510", "key7227");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 47006: put
  db->Put(write_options, "key10154", "value62632");

  // op 47007: put
  db->Put(write_options, "key1552", "value48682");

  // op 47008: get
  status = db->Get(read_options, "key5641", &value);
  if (status.ok()) {
    out << "GET key5641 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47009: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5386", "key10475", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5386, key10475) size=" << scan_result.size() << std::endl;
  }

  // op 47010: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4070", "key5245", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4070, key5245) size=" << scan_result.size() << std::endl;
  }

  // op 47011: put
  db->Put(write_options, "key0037", "value114364");

  // op 47012: delete
  db->Delete(write_options, "key11191");

  // op 47013: put
  db->Put(write_options, "key7626", "value28692");

  // op 47014: get
  status = db->Get(read_options, "key2192", &value);
  if (status.ok()) {
    out << "GET key2192 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47015: put
  db->Put(write_options, "key10403", "value84408");

  // op 47016: get
  status = db->Get(read_options, "key4846", &value);
  if (status.ok()) {
    out << "GET key4846 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47017: delete_range
  status = db->DeleteRange(write_options, "key0720", "key8886");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 47018: delete_range
  status = db->DeleteRange(write_options, "key7360", "key9042");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 47019: put
  db->Put(write_options, "key10615", "value16753");

  // op 47020: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 47021: get
  status = db->Get(read_options, "key10221", &value);
  if (status.ok()) {
    out << "GET key10221 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47022: delete_range
  status = db->DeleteRange(write_options, "key0387", "key4236");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 47023: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2146", "key4676", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2146, key4676) size=" << scan_result.size() << std::endl;
  }

  // op 47024: get
  status = db->Get(read_options, "key0376", &value);
  if (status.ok()) {
    out << "GET key0376 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47025: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3778", "key6872", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3778, key6872) size=" << scan_result.size() << std::endl;
  }

  // op 47026: put
  db->Put(write_options, "key7439", "value78126");

  // op 47027: delete
  db->Delete(write_options, "key5913");

  // op 47028: get
  status = db->Get(read_options, "key7647", &value);
  if (status.ok()) {
    out << "GET key7647 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47029: delete
  db->Delete(write_options, "key8290");

  // op 47030: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2366", "key8713", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2366, key8713) size=" << scan_result.size() << std::endl;
  }

  // op 47031: put
  db->Put(write_options, "key8819", "value100559");

  // op 47032: put
  db->Put(write_options, "key11109", "value98367");

  // op 47033: delete
  db->Delete(write_options, "key8809");

  // op 47034: delete
  db->Delete(write_options, "key5311");

  // op 47035: put
  db->Put(write_options, "key9703", "value55461");

  // op 47036: delete_range
  status = db->DeleteRange(write_options, "key8040", "key10489");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 47037: put
  db->Put(write_options, "key2860", "value103450");

  // op 47038: put
  db->Put(write_options, "key1090", "value58564");

  // op 47039: put
  db->Put(write_options, "key0816", "value26226");

  // op 47040: get
  status = db->Get(read_options, "key0766", &value);
  if (status.ok()) {
    out << "GET key0766 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47041: get
  status = db->Get(read_options, "key11320", &value);
  if (status.ok()) {
    out << "GET key11320 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47042: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4886", "key9948", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4886, key9948) size=" << scan_result.size() << std::endl;
  }

  // op 47043: put
  db->Put(write_options, "key10205", "value48807");

  // op 47044: put
  db->Put(write_options, "key8375", "value91827");

  // op 47045: get
  status = db->Get(read_options, "key8347", &value);
  if (status.ok()) {
    out << "GET key8347 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47046: put
  db->Put(write_options, "key9809", "value71648");

  // op 47047: put
  db->Put(write_options, "key5539", "value30998");

  // op 47048: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 47049: delete_range
  status = db->DeleteRange(write_options, "key1918", "key7681");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 47050: delete_range
  status = db->DeleteRange(write_options, "key0933", "key6748");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 47051: put
  db->Put(write_options, "key7623", "value108461");

  // op 47052: put
  db->Put(write_options, "key10566", "value70086");

  // op 47053: put
  db->Put(write_options, "key11351", "value28434");

  // op 47054: put
  db->Put(write_options, "key5856", "value25304");

  // op 47055: get
  status = db->Get(read_options, "key9894", &value);
  if (status.ok()) {
    out << "GET key9894 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47056: get
  status = db->Get(read_options, "key9573", &value);
  if (status.ok()) {
    out << "GET key9573 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47057: delete
  db->Delete(write_options, "key10201");

  // op 47058: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5661", "key10967", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5661, key10967) size=" << scan_result.size() << std::endl;
  }

  // op 47059: get
  status = db->Get(read_options, "key4815", &value);
  if (status.ok()) {
    out << "GET key4815 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47060: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3953", "key4314", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3953, key4314) size=" << scan_result.size() << std::endl;
  }

  // op 47061: put
  db->Put(write_options, "key8007", "value24388");

  // op 47062: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3271", "key9410", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3271, key9410) size=" << scan_result.size() << std::endl;
  }

  // op 47063: delete_range
  status = db->DeleteRange(write_options, "key0795", "key4095");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 47064: put
  db->Put(write_options, "key1040", "value96790");

  // op 47065: get
  status = db->Get(read_options, "key4029", &value);
  if (status.ok()) {
    out << "GET key4029 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47066: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4319", "key5289", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4319, key5289) size=" << scan_result.size() << std::endl;
  }

  // op 47067: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 47068: delete
  db->Delete(write_options, "key0117");

  // op 47069: delete
  db->Delete(write_options, "key7702");

  // op 47070: put
  db->Put(write_options, "key5632", "value104272");

  // op 47071: get
  status = db->Get(read_options, "key6886", &value);
  if (status.ok()) {
    out << "GET key6886 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47072: delete_range
  status = db->DeleteRange(write_options, "key1898", "key9467");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 47073: delete_range
  status = db->DeleteRange(write_options, "key6673", "key7331");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 47074: put
  db->Put(write_options, "key7284", "value17906");

  // op 47075: get
  status = db->Get(read_options, "key3563", &value);
  if (status.ok()) {
    out << "GET key3563 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47076: get
  status = db->Get(read_options, "key8534", &value);
  if (status.ok()) {
    out << "GET key8534 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47077: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 47078: get
  status = db->Get(read_options, "key2857", &value);
  if (status.ok()) {
    out << "GET key2857 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47079: get
  status = db->Get(read_options, "key6954", &value);
  if (status.ok()) {
    out << "GET key6954 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47080: put
  db->Put(write_options, "key0369", "value79792");

  // op 47081: delete_range
  status = db->DeleteRange(write_options, "key3477", "key4632");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 47082: put
  db->Put(write_options, "key9552", "value81197");

  // op 47083: put
  db->Put(write_options, "key3105", "value29782");

  // op 47084: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 47085: get
  status = db->Get(read_options, "key9873", &value);
  if (status.ok()) {
    out << "GET key9873 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47086: put
  db->Put(write_options, "key8617", "value66566");

  // op 47087: get
  status = db->Get(read_options, "key1552", &value);
  if (status.ok()) {
    out << "GET key1552 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47088: delete
  db->Delete(write_options, "key6974");

  // op 47089: get
  status = db->Get(read_options, "key5969", &value);
  if (status.ok()) {
    out << "GET key5969 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47090: put
  db->Put(write_options, "key3354", "value101659");

  // op 47091: put
  db->Put(write_options, "key5576", "value32720");

  // op 47092: put
  db->Put(write_options, "key2422", "value45559");

  // op 47093: put
  db->Put(write_options, "key0366", "value76651");

  // op 47094: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0591", "key5259", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0591, key5259) size=" << scan_result.size() << std::endl;
  }

  // op 47095: delete_range
  status = db->DeleteRange(write_options, "key2644", "key10619");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 47096: put
  db->Put(write_options, "key7616", "value48580");

  // op 47097: get
  status = db->Get(read_options, "key4333", &value);
  if (status.ok()) {
    out << "GET key4333 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47098: put
  db->Put(write_options, "key7425", "value37677");

  // op 47099: put
  db->Put(write_options, "key0745", "value70039");

  // op 47100: get
  status = db->Get(read_options, "key5797", &value);
  if (status.ok()) {
    out << "GET key5797 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47101: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 47102: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8279", "key11664", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8279, key11664) size=" << scan_result.size() << std::endl;
  }

  // op 47103: delete
  db->Delete(write_options, "key1278");

  // op 47104: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1283", "key6410", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1283, key6410) size=" << scan_result.size() << std::endl;
  }

  // op 47105: put
  db->Put(write_options, "key11616", "value45932");

  // op 47106: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0448", "key8599", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0448, key8599) size=" << scan_result.size() << std::endl;
  }

  // op 47107: put
  db->Put(write_options, "key9167", "value17491");

  // op 47108: put
  db->Put(write_options, "key10926", "value17289");

  // op 47109: put
  db->Put(write_options, "key2448", "value37170");

  // op 47110: delete
  db->Delete(write_options, "key6527");

  // op 47111: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6219", "key6267", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6219, key6267) size=" << scan_result.size() << std::endl;
  }

  // op 47112: delete_range
  status = db->DeleteRange(write_options, "key3947", "key5684");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 47113: put
  db->Put(write_options, "key1592", "value53764");

  // op 47114: put
  db->Put(write_options, "key6315", "value101953");

  // op 47115: get
  status = db->Get(read_options, "key6485", &value);
  if (status.ok()) {
    out << "GET key6485 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47116: delete_range
  status = db->DeleteRange(write_options, "key3096", "key8520");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 47117: put
  db->Put(write_options, "key3746", "value89362");

  // op 47118: get
  status = db->Get(read_options, "key3583", &value);
  if (status.ok()) {
    out << "GET key3583 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47119: delete
  db->Delete(write_options, "key7608");

  // op 47120: put
  db->Put(write_options, "key1533", "value76390");

  // op 47121: get
  status = db->Get(read_options, "key9230", &value);
  if (status.ok()) {
    out << "GET key9230 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47122: get
  status = db->Get(read_options, "key8805", &value);
  if (status.ok()) {
    out << "GET key8805 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47123: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0220", "key6587", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0220, key6587) size=" << scan_result.size() << std::endl;
  }

  // op 47124: get
  status = db->Get(read_options, "key7764", &value);
  if (status.ok()) {
    out << "GET key7764 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47125: put
  db->Put(write_options, "key4731", "value109690");

  // op 47126: get
  status = db->Get(read_options, "key4158", &value);
  if (status.ok()) {
    out << "GET key4158 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47127: delete
  db->Delete(write_options, "key10071");

  // op 47128: get
  status = db->Get(read_options, "key8701", &value);
  if (status.ok()) {
    out << "GET key8701 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47129: put
  db->Put(write_options, "key2507", "value107747");

  // op 47130: get
  status = db->Get(read_options, "key6787", &value);
  if (status.ok()) {
    out << "GET key6787 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47131: put
  db->Put(write_options, "key1292", "value107420");

  // op 47132: get
  status = db->Get(read_options, "key0542", &value);
  if (status.ok()) {
    out << "GET key0542 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47133: delete
  db->Delete(write_options, "key0065");

  // op 47134: put
  db->Put(write_options, "key5964", "value21121");

  // op 47135: get
  status = db->Get(read_options, "key8433", &value);
  if (status.ok()) {
    out << "GET key8433 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47136: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0294", "key2680", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0294, key2680) size=" << scan_result.size() << std::endl;
  }

  // op 47137: delete_range
  status = db->DeleteRange(write_options, "key1166", "key6265");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 47138: delete
  db->Delete(write_options, "key9972");

  // op 47139: delete_range
  status = db->DeleteRange(write_options, "key4673", "key10129");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 47140: put
  db->Put(write_options, "key0183", "value116623");

  // op 47141: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1165", "key7992", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1165, key7992) size=" << scan_result.size() << std::endl;
  }

  // op 47142: get
  status = db->Get(read_options, "key6250", &value);
  if (status.ok()) {
    out << "GET key6250 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47143: delete
  db->Delete(write_options, "key5316");

  // op 47144: put
  db->Put(write_options, "key6180", "value2257");

  // op 47145: put
  db->Put(write_options, "key3905", "value70092");

  // op 47146: put
  db->Put(write_options, "key1586", "value57340");

  // op 47147: delete
  db->Delete(write_options, "key10859");

  // op 47148: put
  db->Put(write_options, "key8191", "value3909");

  // op 47149: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2714", "key7902", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2714, key7902) size=" << scan_result.size() << std::endl;
  }

  // op 47150: get
  status = db->Get(read_options, "key4312", &value);
  if (status.ok()) {
    out << "GET key4312 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47151: delete
  db->Delete(write_options, "key6601");

  // op 47152: delete
  db->Delete(write_options, "key3057");

  // op 47153: get
  status = db->Get(read_options, "key8697", &value);
  if (status.ok()) {
    out << "GET key8697 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47154: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5968", "key9910", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5968, key9910) size=" << scan_result.size() << std::endl;
  }

  // op 47155: put
  db->Put(write_options, "key3819", "value81298");

  // op 47156: get
  status = db->Get(read_options, "key0564", &value);
  if (status.ok()) {
    out << "GET key0564 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47157: get
  status = db->Get(read_options, "key7628", &value);
  if (status.ok()) {
    out << "GET key7628 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47158: put
  db->Put(write_options, "key1959", "value30971");

  // op 47159: delete
  db->Delete(write_options, "key4660");

  // op 47160: put
  db->Put(write_options, "key11127", "value16920");

  // op 47161: get
  status = db->Get(read_options, "key11064", &value);
  if (status.ok()) {
    out << "GET key11064 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47162: delete_range
  status = db->DeleteRange(write_options, "key5847", "key10111");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 47163: put
  db->Put(write_options, "key0271", "value42505");

  // op 47164: put
  db->Put(write_options, "key6658", "value35394");

  // op 47165: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4454", "key7919", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4454, key7919) size=" << scan_result.size() << std::endl;
  }

  // op 47166: put
  db->Put(write_options, "key3168", "value44622");

  // op 47167: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5741", "key11397", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5741, key11397) size=" << scan_result.size() << std::endl;
  }

  // op 47168: put
  db->Put(write_options, "key3863", "value94832");

  // op 47169: get
  status = db->Get(read_options, "key5959", &value);
  if (status.ok()) {
    out << "GET key5959 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47170: put
  db->Put(write_options, "key2250", "value96754");

  // op 47171: get
  status = db->Get(read_options, "key2288", &value);
  if (status.ok()) {
    out << "GET key2288 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47172: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 47173: put
  db->Put(write_options, "key5776", "value12549");

  // op 47174: put
  db->Put(write_options, "key7579", "value13871");

  // op 47175: put
  db->Put(write_options, "key6044", "value88332");

  // op 47176: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4302", "key7381", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4302, key7381) size=" << scan_result.size() << std::endl;
  }

  // op 47177: put
  db->Put(write_options, "key3926", "value21849");

  // op 47178: put
  db->Put(write_options, "key1716", "value102963");

  // op 47179: delete
  db->Delete(write_options, "key2007");

  // op 47180: put
  db->Put(write_options, "key11301", "value3825");

  // op 47181: put
  db->Put(write_options, "key0251", "value36359");

  // op 47182: get
  status = db->Get(read_options, "key6409", &value);
  if (status.ok()) {
    out << "GET key6409 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47183: delete
  db->Delete(write_options, "key10583");

  // op 47184: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1650", "key10217", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1650, key10217) size=" << scan_result.size() << std::endl;
  }

  // op 47185: put
  db->Put(write_options, "key4797", "value53025");

  // op 47186: put
  db->Put(write_options, "key2567", "value96384");

  // op 47187: put
  db->Put(write_options, "key8215", "value7339");

  // op 47188: delete_range
  status = db->DeleteRange(write_options, "key3305", "key7318");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 47189: put
  db->Put(write_options, "key4841", "value117168");

  // op 47190: put
  db->Put(write_options, "key6389", "value101138");

  // op 47191: put
  db->Put(write_options, "key7245", "value68295");

  // op 47192: get
  status = db->Get(read_options, "key9382", &value);
  if (status.ok()) {
    out << "GET key9382 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47193: put
  db->Put(write_options, "key10771", "value15343");

  // op 47194: put
  db->Put(write_options, "key0948", "value29071");

  // op 47195: put
  db->Put(write_options, "key11178", "value71594");

  // op 47196: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 47197: delete
  db->Delete(write_options, "key8775");

  // op 47198: get
  status = db->Get(read_options, "key2258", &value);
  if (status.ok()) {
    out << "GET key2258 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47199: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0970", "key5960", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0970, key5960) size=" << scan_result.size() << std::endl;
  }

  // op 47200: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0034", "key10528", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0034, key10528) size=" << scan_result.size() << std::endl;
  }

  // op 47201: get
  status = db->Get(read_options, "key9308", &value);
  if (status.ok()) {
    out << "GET key9308 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47202: put
  db->Put(write_options, "key1198", "value98378");

  // op 47203: delete_range
  status = db->DeleteRange(write_options, "key0030", "key1487");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 47204: get
  status = db->Get(read_options, "key6860", &value);
  if (status.ok()) {
    out << "GET key6860 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47205: put
  db->Put(write_options, "key4166", "value88148");

  // op 47206: get
  status = db->Get(read_options, "key8711", &value);
  if (status.ok()) {
    out << "GET key8711 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47207: put
  db->Put(write_options, "key8834", "value108545");

  // op 47208: get
  status = db->Get(read_options, "key4844", &value);
  if (status.ok()) {
    out << "GET key4844 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47209: get
  status = db->Get(read_options, "key4213", &value);
  if (status.ok()) {
    out << "GET key4213 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47210: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9319", "key10145", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9319, key10145) size=" << scan_result.size() << std::endl;
  }

  // op 47211: get
  status = db->Get(read_options, "key7184", &value);
  if (status.ok()) {
    out << "GET key7184 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47212: put
  db->Put(write_options, "key9088", "value46783");

  // op 47213: delete
  db->Delete(write_options, "key1004");

  // op 47214: delete_range
  status = db->DeleteRange(write_options, "key3548", "key8788");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 47215: put
  db->Put(write_options, "key2223", "value52542");

  // op 47216: put
  db->Put(write_options, "key7747", "value93628");

  // op 47217: delete_range
  status = db->DeleteRange(write_options, "key3004", "key3378");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 47218: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2736", "key6145", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2736, key6145) size=" << scan_result.size() << std::endl;
  }

  // op 47219: delete
  db->Delete(write_options, "key5075");

  // op 47220: get
  status = db->Get(read_options, "key7705", &value);
  if (status.ok()) {
    out << "GET key7705 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47221: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3780", "key10961", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3780, key10961) size=" << scan_result.size() << std::endl;
  }

  // op 47222: put
  db->Put(write_options, "key5225", "value83454");

  // op 47223: put
  db->Put(write_options, "key10813", "value28742");

  // op 47224: delete
  db->Delete(write_options, "key1981");

  // op 47225: get
  status = db->Get(read_options, "key0772", &value);
  if (status.ok()) {
    out << "GET key0772 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47226: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7794", "key10215", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7794, key10215) size=" << scan_result.size() << std::endl;
  }

  // op 47227: put
  db->Put(write_options, "key6473", "value25722");

  // op 47228: put
  db->Put(write_options, "key1755", "value83546");

  // op 47229: put
  db->Put(write_options, "key3959", "value88642");

  // op 47230: delete
  db->Delete(write_options, "key10579");

  // op 47231: get
  status = db->Get(read_options, "key10068", &value);
  if (status.ok()) {
    out << "GET key10068 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47232: put
  db->Put(write_options, "key0451", "value11332");

  // op 47233: get
  status = db->Get(read_options, "key10456", &value);
  if (status.ok()) {
    out << "GET key10456 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47234: put
  db->Put(write_options, "key4984", "value116951");

  // op 47235: get
  status = db->Get(read_options, "key9981", &value);
  if (status.ok()) {
    out << "GET key9981 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47236: put
  db->Put(write_options, "key9091", "value79411");

  // op 47237: get
  status = db->Get(read_options, "key5681", &value);
  if (status.ok()) {
    out << "GET key5681 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47238: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9425", "key10758", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9425, key10758) size=" << scan_result.size() << std::endl;
  }

  // op 47239: put
  db->Put(write_options, "key1780", "value2323");

  // op 47240: put
  db->Put(write_options, "key1848", "value8916");

  // op 47241: delete
  db->Delete(write_options, "key1763");

  // op 47242: delete
  db->Delete(write_options, "key9721");

  // op 47243: put
  db->Put(write_options, "key9264", "value70586");

  // op 47244: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5427", "key8092", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5427, key8092) size=" << scan_result.size() << std::endl;
  }

  // op 47245: get
  status = db->Get(read_options, "key9214", &value);
  if (status.ok()) {
    out << "GET key9214 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47246: get
  status = db->Get(read_options, "key7070", &value);
  if (status.ok()) {
    out << "GET key7070 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47247: delete_range
  status = db->DeleteRange(write_options, "key1989", "key10361");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 47248: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5990", "key6804", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5990, key6804) size=" << scan_result.size() << std::endl;
  }

  // op 47249: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1231", "key8666", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1231, key8666) size=" << scan_result.size() << std::endl;
  }

  // op 47250: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 47251: put
  db->Put(write_options, "key5030", "value102681");

  // op 47252: put
  db->Put(write_options, "key5328", "value70391");

  // op 47253: put
  db->Put(write_options, "key10790", "value50698");

  // op 47254: put
  db->Put(write_options, "key6983", "value71789");

  // op 47255: put
  db->Put(write_options, "key10027", "value49962");

  // op 47256: delete_range
  status = db->DeleteRange(write_options, "key0605", "key8019");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 47257: get
  status = db->Get(read_options, "key1489", &value);
  if (status.ok()) {
    out << "GET key1489 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47258: get
  status = db->Get(read_options, "key6747", &value);
  if (status.ok()) {
    out << "GET key6747 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47259: get
  status = db->Get(read_options, "key0072", &value);
  if (status.ok()) {
    out << "GET key0072 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47260: put
  db->Put(write_options, "key0181", "value116454");

  // op 47261: put
  db->Put(write_options, "key0894", "value86221");

  // op 47262: get
  status = db->Get(read_options, "key6250", &value);
  if (status.ok()) {
    out << "GET key6250 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47263: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1655", "key3647", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1655, key3647) size=" << scan_result.size() << std::endl;
  }

  // op 47264: get
  status = db->Get(read_options, "key8745", &value);
  if (status.ok()) {
    out << "GET key8745 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47265: delete_range
  status = db->DeleteRange(write_options, "key6098", "key6250");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 47266: delete
  db->Delete(write_options, "key1120");

  // op 47267: get
  status = db->Get(read_options, "key10733", &value);
  if (status.ok()) {
    out << "GET key10733 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47268: put
  db->Put(write_options, "key2973", "value28465");

  // op 47269: get
  status = db->Get(read_options, "key7278", &value);
  if (status.ok()) {
    out << "GET key7278 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47270: get
  status = db->Get(read_options, "key4350", &value);
  if (status.ok()) {
    out << "GET key4350 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47271: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2220", "key9022", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2220, key9022) size=" << scan_result.size() << std::endl;
  }

  // op 47272: delete_range
  status = db->DeleteRange(write_options, "key3513", "key10122");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 47273: get
  status = db->Get(read_options, "key7200", &value);
  if (status.ok()) {
    out << "GET key7200 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47274: put
  db->Put(write_options, "key3542", "value78201");

  // op 47275: get
  status = db->Get(read_options, "key10984", &value);
  if (status.ok()) {
    out << "GET key10984 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47276: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2111", "key2991", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2111, key2991) size=" << scan_result.size() << std::endl;
  }

  // op 47277: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 47278: put
  db->Put(write_options, "key10168", "value41227");

  // op 47279: delete_range
  status = db->DeleteRange(write_options, "key7068", "key9176");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 47280: put
  db->Put(write_options, "key9396", "value20373");

  // op 47281: put
  db->Put(write_options, "key9742", "value27041");

  // op 47282: get
  status = db->Get(read_options, "key10793", &value);
  if (status.ok()) {
    out << "GET key10793 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47283: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2649", "key5853", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2649, key5853) size=" << scan_result.size() << std::endl;
  }

  // op 47284: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 47285: delete
  db->Delete(write_options, "key4753");

  // op 47286: put
  db->Put(write_options, "key2423", "value18703");

  // op 47287: put
  db->Put(write_options, "key4238", "value86221");

  // op 47288: put
  db->Put(write_options, "key3353", "value63810");

  // op 47289: get
  status = db->Get(read_options, "key4434", &value);
  if (status.ok()) {
    out << "GET key4434 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47290: get
  status = db->Get(read_options, "key1411", &value);
  if (status.ok()) {
    out << "GET key1411 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47291: delete
  db->Delete(write_options, "key3119");

  // op 47292: get
  status = db->Get(read_options, "key0232", &value);
  if (status.ok()) {
    out << "GET key0232 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47293: put
  db->Put(write_options, "key4763", "value10343");

  // op 47294: get
  status = db->Get(read_options, "key0699", &value);
  if (status.ok()) {
    out << "GET key0699 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47295: get
  status = db->Get(read_options, "key2478", &value);
  if (status.ok()) {
    out << "GET key2478 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47296: put
  db->Put(write_options, "key10520", "value74525");

  // op 47297: delete
  db->Delete(write_options, "key8102");

  // op 47298: delete_range
  status = db->DeleteRange(write_options, "key5222", "key11267");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 47299: get
  status = db->Get(read_options, "key11581", &value);
  if (status.ok()) {
    out << "GET key11581 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47300: get
  status = db->Get(read_options, "key8726", &value);
  if (status.ok()) {
    out << "GET key8726 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47301: get
  status = db->Get(read_options, "key4877", &value);
  if (status.ok()) {
    out << "GET key4877 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47302: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1958", "key9554", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1958, key9554) size=" << scan_result.size() << std::endl;
  }

  // op 47303: get
  status = db->Get(read_options, "key0420", &value);
  if (status.ok()) {
    out << "GET key0420 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47304: put
  db->Put(write_options, "key5100", "value63656");

  // op 47305: delete_range
  status = db->DeleteRange(write_options, "key2586", "key4642");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 47306: put
  db->Put(write_options, "key1340", "value110882");

  // op 47307: get
  status = db->Get(read_options, "key10288", &value);
  if (status.ok()) {
    out << "GET key10288 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47308: delete
  db->Delete(write_options, "key10259");

  // op 47309: put
  db->Put(write_options, "key8706", "value66919");

  // op 47310: put
  db->Put(write_options, "key11272", "value48650");

  // op 47311: put
  db->Put(write_options, "key11644", "value52441");

  // op 47312: put
  db->Put(write_options, "key2157", "value37907");

  // op 47313: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2418", "key7353", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2418, key7353) size=" << scan_result.size() << std::endl;
  }

  // op 47314: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5301", "key6698", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5301, key6698) size=" << scan_result.size() << std::endl;
  }

  // op 47315: delete
  db->Delete(write_options, "key8774");

  // op 47316: put
  db->Put(write_options, "key4671", "value32376");

  // op 47317: get
  status = db->Get(read_options, "key6204", &value);
  if (status.ok()) {
    out << "GET key6204 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47318: get
  status = db->Get(read_options, "key8015", &value);
  if (status.ok()) {
    out << "GET key8015 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47319: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7946", "key9002", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7946, key9002) size=" << scan_result.size() << std::endl;
  }

  // op 47320: put
  db->Put(write_options, "key0080", "value85752");

  // op 47321: put
  db->Put(write_options, "key7367", "value40157");

  // op 47322: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2143", "key9985", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2143, key9985) size=" << scan_result.size() << std::endl;
  }

  // op 47323: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5772", "key11362", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5772, key11362) size=" << scan_result.size() << std::endl;
  }

  // op 47324: delete
  db->Delete(write_options, "key2440");

  // op 47325: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4170", "key4511", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4170, key4511) size=" << scan_result.size() << std::endl;
  }

  // op 47326: get
  status = db->Get(read_options, "key7223", &value);
  if (status.ok()) {
    out << "GET key7223 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47327: put
  db->Put(write_options, "key7196", "value72195");

  // op 47328: put
  db->Put(write_options, "key5304", "value60420");

  // op 47329: get
  status = db->Get(read_options, "key5464", &value);
  if (status.ok()) {
    out << "GET key5464 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47330: put
  db->Put(write_options, "key10771", "value107718");

  // op 47331: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1588", "key5458", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1588, key5458) size=" << scan_result.size() << std::endl;
  }

  // op 47332: put
  db->Put(write_options, "key1273", "value84600");

  // op 47333: get
  status = db->Get(read_options, "key10910", &value);
  if (status.ok()) {
    out << "GET key10910 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47334: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0470", "key1847", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0470, key1847) size=" << scan_result.size() << std::endl;
  }

  // op 47335: get
  status = db->Get(read_options, "key1548", &value);
  if (status.ok()) {
    out << "GET key1548 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47336: delete
  db->Delete(write_options, "key4323");

  // op 47337: put
  db->Put(write_options, "key10878", "value92480");

  // op 47338: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4110", "key4766", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4110, key4766) size=" << scan_result.size() << std::endl;
  }

  // op 47339: delete_range
  status = db->DeleteRange(write_options, "key1489", "key7288");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 47340: delete
  db->Delete(write_options, "key0651");

  // op 47341: delete_range
  status = db->DeleteRange(write_options, "key1356", "key8281");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 47342: put
  db->Put(write_options, "key2983", "value28439");

  // op 47343: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7802", "key8131", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7802, key8131) size=" << scan_result.size() << std::endl;
  }

  // op 47344: put
  db->Put(write_options, "key2608", "value54199");

  // op 47345: get
  status = db->Get(read_options, "key4115", &value);
  if (status.ok()) {
    out << "GET key4115 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47346: get
  status = db->Get(read_options, "key10644", &value);
  if (status.ok()) {
    out << "GET key10644 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47347: get
  status = db->Get(read_options, "key7064", &value);
  if (status.ok()) {
    out << "GET key7064 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47348: put
  db->Put(write_options, "key7546", "value53856");

  // op 47349: get
  status = db->Get(read_options, "key9437", &value);
  if (status.ok()) {
    out << "GET key9437 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47350: put
  db->Put(write_options, "key3498", "value19107");

  // op 47351: put
  db->Put(write_options, "key11676", "value60313");

  // op 47352: put
  db->Put(write_options, "key4817", "value46717");

  // op 47353: delete_range
  status = db->DeleteRange(write_options, "key1608", "key10993");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 47354: put
  db->Put(write_options, "key9720", "value27634");

  // op 47355: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 47356: get
  status = db->Get(read_options, "key5967", &value);
  if (status.ok()) {
    out << "GET key5967 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47357: put
  db->Put(write_options, "key7623", "value80554");

  // op 47358: put
  db->Put(write_options, "key6452", "value112002");

  // op 47359: delete_range
  status = db->DeleteRange(write_options, "key0392", "key7284");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 47360: delete_range
  status = db->DeleteRange(write_options, "key1417", "key11009");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 47361: delete
  db->Delete(write_options, "key8739");

  // op 47362: put
  db->Put(write_options, "key7809", "value56085");

  // op 47363: put
  db->Put(write_options, "key8066", "value39928");

  // op 47364: delete_range
  status = db->DeleteRange(write_options, "key7819", "key9733");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 47365: get
  status = db->Get(read_options, "key4326", &value);
  if (status.ok()) {
    out << "GET key4326 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47366: put
  db->Put(write_options, "key2654", "value63935");

  // op 47367: put
  db->Put(write_options, "key11519", "value62356");

  // op 47368: delete_range
  status = db->DeleteRange(write_options, "key4115", "key8559");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 47369: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0151", "key9890", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0151, key9890) size=" << scan_result.size() << std::endl;
  }

  // op 47370: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2307", "key7163", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2307, key7163) size=" << scan_result.size() << std::endl;
  }

  // op 47371: put
  db->Put(write_options, "key2750", "value58647");

  // op 47372: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1649", "key2575", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1649, key2575) size=" << scan_result.size() << std::endl;
  }

  // op 47373: put
  db->Put(write_options, "key1511", "value25656");

  // op 47374: delete_range
  status = db->DeleteRange(write_options, "key1791", "key6736");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 47375: put
  db->Put(write_options, "key8874", "value8109");

  // op 47376: put
  db->Put(write_options, "key8976", "value75717");

  // op 47377: put
  db->Put(write_options, "key9811", "value41613");

  // op 47378: put
  db->Put(write_options, "key7068", "value82231");

  // op 47379: delete
  db->Delete(write_options, "key11293");

  // op 47380: delete
  db->Delete(write_options, "key10575");

  // op 47381: delete
  db->Delete(write_options, "key2901");

  // op 47382: get
  status = db->Get(read_options, "key4034", &value);
  if (status.ok()) {
    out << "GET key4034 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47383: put
  db->Put(write_options, "key7893", "value20500");

  // op 47384: put
  db->Put(write_options, "key7638", "value98353");

  // op 47385: put
  db->Put(write_options, "key9179", "value8664");

  // op 47386: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 47387: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2396", "key5609", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2396, key5609) size=" << scan_result.size() << std::endl;
  }

  // op 47388: put
  db->Put(write_options, "key7392", "value12148");

  // op 47389: delete_range
  status = db->DeleteRange(write_options, "key0250", "key1448");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 47390: delete_range
  status = db->DeleteRange(write_options, "key0792", "key9773");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 47391: get
  status = db->Get(read_options, "key0011", &value);
  if (status.ok()) {
    out << "GET key0011 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47392: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 47393: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0877", "key4649", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0877, key4649) size=" << scan_result.size() << std::endl;
  }

  // op 47394: delete
  db->Delete(write_options, "key5958");

  // op 47395: put
  db->Put(write_options, "key7800", "value90639");

  // op 47396: put
  db->Put(write_options, "key10543", "value35261");

  // op 47397: delete_range
  status = db->DeleteRange(write_options, "key5986", "key6946");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 47398: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9194", "key10586", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9194, key10586) size=" << scan_result.size() << std::endl;
  }

  // op 47399: put
  db->Put(write_options, "key1821", "value2796");

  // op 47400: put
  db->Put(write_options, "key7547", "value75581");

  // op 47401: delete
  db->Delete(write_options, "key7814");

  // op 47402: put
  db->Put(write_options, "key0868", "value73476");

  // op 47403: delete_range
  status = db->DeleteRange(write_options, "key11641", "key11675");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 47404: put
  db->Put(write_options, "key8373", "value49206");

  // op 47405: delete
  db->Delete(write_options, "key1457");

  // op 47406: delete_range
  status = db->DeleteRange(write_options, "key2279", "key5169");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 47407: get
  status = db->Get(read_options, "key1674", &value);
  if (status.ok()) {
    out << "GET key1674 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47408: put
  db->Put(write_options, "key1486", "value96730");

  // op 47409: delete
  db->Delete(write_options, "key11372");

  // op 47410: delete_range
  status = db->DeleteRange(write_options, "key8511", "key10441");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 47411: put
  db->Put(write_options, "key7755", "value22112");

  // op 47412: get
  status = db->Get(read_options, "key5363", &value);
  if (status.ok()) {
    out << "GET key5363 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47413: put
  db->Put(write_options, "key6364", "value78015");

  // op 47414: get
  status = db->Get(read_options, "key10852", &value);
  if (status.ok()) {
    out << "GET key10852 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47415: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1382", "key6265", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1382, key6265) size=" << scan_result.size() << std::endl;
  }

  // op 47416: put
  db->Put(write_options, "key11531", "value25297");

  // op 47417: put
  db->Put(write_options, "key3044", "value41691");

  // op 47418: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4397", "key11504", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4397, key11504) size=" << scan_result.size() << std::endl;
  }

  // op 47419: get
  status = db->Get(read_options, "key8083", &value);
  if (status.ok()) {
    out << "GET key8083 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47420: put
  db->Put(write_options, "key6874", "value83612");

  // op 47421: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4530", "key9411", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4530, key9411) size=" << scan_result.size() << std::endl;
  }

  // op 47422: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2185", "key6279", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2185, key6279) size=" << scan_result.size() << std::endl;
  }

  // op 47423: delete
  db->Delete(write_options, "key0341");

  // op 47424: delete
  db->Delete(write_options, "key2835");

  // op 47425: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4757", "key5511", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4757, key5511) size=" << scan_result.size() << std::endl;
  }

  // op 47426: put
  db->Put(write_options, "key2499", "value68139");

  // op 47427: put
  db->Put(write_options, "key8725", "value6247");

  // op 47428: put
  db->Put(write_options, "key6963", "value96546");

  // op 47429: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6754", "key6797", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6754, key6797) size=" << scan_result.size() << std::endl;
  }

  // op 47430: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0523", "key7806", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0523, key7806) size=" << scan_result.size() << std::endl;
  }

  // op 47431: put
  db->Put(write_options, "key0199", "value70750");

  // op 47432: get
  status = db->Get(read_options, "key1309", &value);
  if (status.ok()) {
    out << "GET key1309 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47433: put
  db->Put(write_options, "key9463", "value6273");

  // op 47434: put
  db->Put(write_options, "key7162", "value115735");

  // op 47435: delete_range
  status = db->DeleteRange(write_options, "key5992", "key7628");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 47436: delete_range
  status = db->DeleteRange(write_options, "key1463", "key2205");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 47437: put
  db->Put(write_options, "key6049", "value7864");

  // op 47438: delete_range
  status = db->DeleteRange(write_options, "key8949", "key8987");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 47439: delete_range
  status = db->DeleteRange(write_options, "key7148", "key11685");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 47440: get
  status = db->Get(read_options, "key2607", &value);
  if (status.ok()) {
    out << "GET key2607 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47441: put
  db->Put(write_options, "key1584", "value42812");

  // op 47442: put
  db->Put(write_options, "key0029", "value116424");

  // op 47443: put
  db->Put(write_options, "key4639", "value57683");

  // op 47444: put
  db->Put(write_options, "key2949", "value71156");

  // op 47445: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 47446: delete
  db->Delete(write_options, "key10703");

  // op 47447: get
  status = db->Get(read_options, "key7393", &value);
  if (status.ok()) {
    out << "GET key7393 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47448: put
  db->Put(write_options, "key2484", "value72642");

  // op 47449: delete_range
  status = db->DeleteRange(write_options, "key3541", "key10504");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 47450: delete
  db->Delete(write_options, "key2461");

  // op 47451: put
  db->Put(write_options, "key11317", "value16179");

  // op 47452: delete
  db->Delete(write_options, "key11330");

  // op 47453: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6332", "key7351", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6332, key7351) size=" << scan_result.size() << std::endl;
  }

  // op 47454: delete
  db->Delete(write_options, "key1619");

  // op 47455: put
  db->Put(write_options, "key10307", "value112112");

  // op 47456: get
  status = db->Get(read_options, "key3451", &value);
  if (status.ok()) {
    out << "GET key3451 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47457: get
  status = db->Get(read_options, "key8720", &value);
  if (status.ok()) {
    out << "GET key8720 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47458: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3165", "key11206", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3165, key11206) size=" << scan_result.size() << std::endl;
  }

  // op 47459: put
  db->Put(write_options, "key9518", "value16046");

  // op 47460: put
  db->Put(write_options, "key8209", "value94060");

  // op 47461: put
  db->Put(write_options, "key4314", "value71980");

  // op 47462: delete
  db->Delete(write_options, "key6780");

  // op 47463: delete_range
  status = db->DeleteRange(write_options, "key4059", "key5606");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 47464: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6786", "key7199", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6786, key7199) size=" << scan_result.size() << std::endl;
  }

  // op 47465: put
  db->Put(write_options, "key5616", "value67557");

  // op 47466: put
  db->Put(write_options, "key4231", "value98479");

  // op 47467: put
  db->Put(write_options, "key5350", "value65375");

  // op 47468: get
  status = db->Get(read_options, "key3305", &value);
  if (status.ok()) {
    out << "GET key3305 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47469: put
  db->Put(write_options, "key7366", "value59499");

  // op 47470: get
  status = db->Get(read_options, "key1845", &value);
  if (status.ok()) {
    out << "GET key1845 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47471: put
  db->Put(write_options, "key1351", "value88475");

  // op 47472: get
  status = db->Get(read_options, "key1809", &value);
  if (status.ok()) {
    out << "GET key1809 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47473: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 47474: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7092", "key9244", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7092, key9244) size=" << scan_result.size() << std::endl;
  }

  // op 47475: delete_range
  status = db->DeleteRange(write_options, "key4306", "key6261");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 47476: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 47477: put
  db->Put(write_options, "key3570", "value4099");

  // op 47478: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 47479: put
  db->Put(write_options, "key9435", "value77008");

  // op 47480: delete_range
  status = db->DeleteRange(write_options, "key1296", "key9017");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 47481: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 47482: get
  status = db->Get(read_options, "key0962", &value);
  if (status.ok()) {
    out << "GET key0962 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47483: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1101", "key5865", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1101, key5865) size=" << scan_result.size() << std::endl;
  }

  // op 47484: put
  db->Put(write_options, "key1485", "value9783");

  // op 47485: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 47486: put
  db->Put(write_options, "key0121", "value41383");

  // op 47487: put
  db->Put(write_options, "key2917", "value85948");

  // op 47488: get
  status = db->Get(read_options, "key4721", &value);
  if (status.ok()) {
    out << "GET key4721 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47489: get
  status = db->Get(read_options, "key7110", &value);
  if (status.ok()) {
    out << "GET key7110 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47490: get
  status = db->Get(read_options, "key8118", &value);
  if (status.ok()) {
    out << "GET key8118 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47491: delete_range
  status = db->DeleteRange(write_options, "key3993", "key5117");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 47492: put
  db->Put(write_options, "key0969", "value12131");

  // op 47493: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8982", "key9417", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8982, key9417) size=" << scan_result.size() << std::endl;
  }

  // op 47494: get
  status = db->Get(read_options, "key4652", &value);
  if (status.ok()) {
    out << "GET key4652 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47495: put
  db->Put(write_options, "key6763", "value78348");

  // op 47496: get
  status = db->Get(read_options, "key4033", &value);
  if (status.ok()) {
    out << "GET key4033 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47497: delete
  db->Delete(write_options, "key4800");

  // op 47498: delete
  db->Delete(write_options, "key10454");

  // op 47499: get
  status = db->Get(read_options, "key2497", &value);
  if (status.ok()) {
    out << "GET key2497 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47500: get
  status = db->Get(read_options, "key2156", &value);
  if (status.ok()) {
    out << "GET key2156 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47501: put
  db->Put(write_options, "key2693", "value30662");

  // op 47502: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6166", "key9187", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6166, key9187) size=" << scan_result.size() << std::endl;
  }

  // op 47503: put
  db->Put(write_options, "key7278", "value109323");

  // op 47504: put
  db->Put(write_options, "key3090", "value63653");

  // op 47505: delete
  db->Delete(write_options, "key8878");

  // op 47506: put
  db->Put(write_options, "key11193", "value30573");

  // op 47507: get
  status = db->Get(read_options, "key6997", &value);
  if (status.ok()) {
    out << "GET key6997 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47508: put
  db->Put(write_options, "key4975", "value93553");

  // op 47509: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8156", "key10511", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8156, key10511) size=" << scan_result.size() << std::endl;
  }

  // op 47510: get
  status = db->Get(read_options, "key7867", &value);
  if (status.ok()) {
    out << "GET key7867 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47511: delete
  db->Delete(write_options, "key0436");

  // op 47512: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 47513: put
  db->Put(write_options, "key10512", "value38683");

  // op 47514: get
  status = db->Get(read_options, "key4668", &value);
  if (status.ok()) {
    out << "GET key4668 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47515: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 47516: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4489", "key11534", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4489, key11534) size=" << scan_result.size() << std::endl;
  }

  // op 47517: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 47518: put
  db->Put(write_options, "key3118", "value35063");

  // op 47519: put
  db->Put(write_options, "key7343", "value9837");

  // op 47520: delete_range
  status = db->DeleteRange(write_options, "key1076", "key2157");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 47521: put
  db->Put(write_options, "key8952", "value71716");

  // op 47522: delete_range
  status = db->DeleteRange(write_options, "key2386", "key9603");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 47523: delete_range
  status = db->DeleteRange(write_options, "key8948", "key10946");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 47524: delete
  db->Delete(write_options, "key1605");

  // op 47525: get
  status = db->Get(read_options, "key2004", &value);
  if (status.ok()) {
    out << "GET key2004 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47526: put
  db->Put(write_options, "key4416", "value2850");

  // op 47527: put
  db->Put(write_options, "key2707", "value116912");

  // op 47528: delete
  db->Delete(write_options, "key8803");

  // op 47529: put
  db->Put(write_options, "key2408", "value14036");

  // op 47530: put
  db->Put(write_options, "key10941", "value19645");

  // op 47531: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4267", "key11602", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4267, key11602) size=" << scan_result.size() << std::endl;
  }

  // op 47532: put
  db->Put(write_options, "key0940", "value109055");

  // op 47533: put
  db->Put(write_options, "key8029", "value76553");

  // op 47534: put
  db->Put(write_options, "key6122", "value93275");

  // op 47535: get
  status = db->Get(read_options, "key4420", &value);
  if (status.ok()) {
    out << "GET key4420 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47536: get
  status = db->Get(read_options, "key7422", &value);
  if (status.ok()) {
    out << "GET key7422 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47537: get
  status = db->Get(read_options, "key1100", &value);
  if (status.ok()) {
    out << "GET key1100 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47538: put
  db->Put(write_options, "key4163", "value62819");

  // op 47539: delete
  db->Delete(write_options, "key0894");

  // op 47540: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 47541: delete_range
  status = db->DeleteRange(write_options, "key1388", "key4155");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 47542: put
  db->Put(write_options, "key5437", "value65484");

  // op 47543: put
  db->Put(write_options, "key7638", "value66580");

  // op 47544: delete
  db->Delete(write_options, "key0830");

  // op 47545: delete_range
  status = db->DeleteRange(write_options, "key3261", "key3779");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 47546: put
  db->Put(write_options, "key10446", "value116559");

  // op 47547: delete_range
  status = db->DeleteRange(write_options, "key7681", "key10806");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 47548: get
  status = db->Get(read_options, "key3643", &value);
  if (status.ok()) {
    out << "GET key3643 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47549: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 47550: delete
  db->Delete(write_options, "key3735");

  // op 47551: put
  db->Put(write_options, "key6125", "value87692");

  // op 47552: get
  status = db->Get(read_options, "key4484", &value);
  if (status.ok()) {
    out << "GET key4484 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47553: get
  status = db->Get(read_options, "key11368", &value);
  if (status.ok()) {
    out << "GET key11368 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47554: put
  db->Put(write_options, "key7812", "value89860");

  // op 47555: put
  db->Put(write_options, "key10178", "value68827");

  // op 47556: delete
  db->Delete(write_options, "key6513");

  // op 47557: put
  db->Put(write_options, "key10911", "value75171");

  // op 47558: delete_range
  status = db->DeleteRange(write_options, "key0755", "key2689");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 47559: put
  db->Put(write_options, "key5617", "value117625");

  // op 47560: delete
  db->Delete(write_options, "key7865");

  // op 47561: delete_range
  status = db->DeleteRange(write_options, "key5762", "key10455");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 47562: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1685", "key2992", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1685, key2992) size=" << scan_result.size() << std::endl;
  }

  // op 47563: put
  db->Put(write_options, "key11689", "value62681");

  // op 47564: put
  db->Put(write_options, "key1872", "value63922");

  // op 47565: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 47566: get
  status = db->Get(read_options, "key6149", &value);
  if (status.ok()) {
    out << "GET key6149 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47567: put
  db->Put(write_options, "key7829", "value85696");

  // op 47568: put
  db->Put(write_options, "key0768", "value75675");

  // op 47569: put
  db->Put(write_options, "key4899", "value49037");

  // op 47570: delete_range
  status = db->DeleteRange(write_options, "key3613", "key6408");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 47571: put
  db->Put(write_options, "key4361", "value86252");

  // op 47572: put
  db->Put(write_options, "key5509", "value44563");

  // op 47573: delete_range
  status = db->DeleteRange(write_options, "key6105", "key10709");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 47574: delete_range
  status = db->DeleteRange(write_options, "key2652", "key11194");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 47575: put
  db->Put(write_options, "key10401", "value21813");

  // op 47576: put
  db->Put(write_options, "key10214", "value42013");

  // op 47577: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 47578: put
  db->Put(write_options, "key9250", "value33627");

  // op 47579: get
  status = db->Get(read_options, "key9678", &value);
  if (status.ok()) {
    out << "GET key9678 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47580: get
  status = db->Get(read_options, "key1191", &value);
  if (status.ok()) {
    out << "GET key1191 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47581: delete_range
  status = db->DeleteRange(write_options, "key8060", "key11446");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 47582: put
  db->Put(write_options, "key0298", "value111090");

  // op 47583: delete_range
  status = db->DeleteRange(write_options, "key5280", "key7141");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 47584: get
  status = db->Get(read_options, "key6408", &value);
  if (status.ok()) {
    out << "GET key6408 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47585: put
  db->Put(write_options, "key3856", "value109595");

  // op 47586: get
  status = db->Get(read_options, "key8582", &value);
  if (status.ok()) {
    out << "GET key8582 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47587: delete
  db->Delete(write_options, "key2449");

  // op 47588: get
  status = db->Get(read_options, "key11651", &value);
  if (status.ok()) {
    out << "GET key11651 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47589: delete_range
  status = db->DeleteRange(write_options, "key4261", "key8789");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 47590: put
  db->Put(write_options, "key0230", "value81240");

  // op 47591: get
  status = db->Get(read_options, "key5477", &value);
  if (status.ok()) {
    out << "GET key5477 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47592: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3809", "key5321", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3809, key5321) size=" << scan_result.size() << std::endl;
  }

  // op 47593: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10979", "key11530", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10979, key11530) size=" << scan_result.size() << std::endl;
  }

  // op 47594: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6091", "key10248", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6091, key10248) size=" << scan_result.size() << std::endl;
  }

  // op 47595: put
  db->Put(write_options, "key0855", "value44405");

  // op 47596: put
  db->Put(write_options, "key8776", "value62157");

  // op 47597: get
  status = db->Get(read_options, "key8628", &value);
  if (status.ok()) {
    out << "GET key8628 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47598: get
  status = db->Get(read_options, "key4402", &value);
  if (status.ok()) {
    out << "GET key4402 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47599: put
  db->Put(write_options, "key8297", "value86189");

  // op 47600: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 47601: put
  db->Put(write_options, "key9478", "value56044");

  // op 47602: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0035", "key3378", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0035, key3378) size=" << scan_result.size() << std::endl;
  }

  // op 47603: put
  db->Put(write_options, "key11088", "value62364");

  // op 47604: get
  status = db->Get(read_options, "key7337", &value);
  if (status.ok()) {
    out << "GET key7337 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47605: put
  db->Put(write_options, "key5038", "value1694");

  // op 47606: delete_range
  status = db->DeleteRange(write_options, "key9142", "key10874");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 47607: put
  db->Put(write_options, "key8748", "value91400");

  // op 47608: put
  db->Put(write_options, "key9491", "value27476");

  // op 47609: get
  status = db->Get(read_options, "key1444", &value);
  if (status.ok()) {
    out << "GET key1444 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47610: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10187", "key11600", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10187, key11600) size=" << scan_result.size() << std::endl;
  }

  // op 47611: put
  db->Put(write_options, "key2554", "value71346");

  // op 47612: put
  db->Put(write_options, "key3360", "value110959");

  // op 47613: delete_range
  status = db->DeleteRange(write_options, "key6922", "key9861");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 47614: put
  db->Put(write_options, "key10628", "value111557");

  // op 47615: delete
  db->Delete(write_options, "key6951");

  // op 47616: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0676", "key5492", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0676, key5492) size=" << scan_result.size() << std::endl;
  }

  // op 47617: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0911", "key3170", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0911, key3170) size=" << scan_result.size() << std::endl;
  }

  // op 47618: put
  db->Put(write_options, "key0676", "value29779");

  // op 47619: get
  status = db->Get(read_options, "key1978", &value);
  if (status.ok()) {
    out << "GET key1978 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47620: delete_range
  status = db->DeleteRange(write_options, "key2929", "key5721");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 47621: get
  status = db->Get(read_options, "key7963", &value);
  if (status.ok()) {
    out << "GET key7963 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47622: get
  status = db->Get(read_options, "key9707", &value);
  if (status.ok()) {
    out << "GET key9707 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47623: put
  db->Put(write_options, "key3355", "value12631");

  // op 47624: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6824", "key8570", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6824, key8570) size=" << scan_result.size() << std::endl;
  }

  // op 47625: get
  status = db->Get(read_options, "key10850", &value);
  if (status.ok()) {
    out << "GET key10850 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47626: put
  db->Put(write_options, "key6716", "value28942");

  // op 47627: get
  status = db->Get(read_options, "key7764", &value);
  if (status.ok()) {
    out << "GET key7764 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47628: put
  db->Put(write_options, "key9778", "value107349");

  // op 47629: put
  db->Put(write_options, "key9975", "value29397");

  // op 47630: put
  db->Put(write_options, "key4771", "value0493");

  // op 47631: get
  status = db->Get(read_options, "key7855", &value);
  if (status.ok()) {
    out << "GET key7855 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47632: put
  db->Put(write_options, "key10452", "value47132");

  // op 47633: put
  db->Put(write_options, "key6245", "value40198");

  // op 47634: put
  db->Put(write_options, "key0833", "value17000");

  // op 47635: put
  db->Put(write_options, "key2522", "value96475");

  // op 47636: delete_range
  status = db->DeleteRange(write_options, "key2431", "key5605");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 47637: put
  db->Put(write_options, "key2761", "value44822");

  // op 47638: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0899", "key2836", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0899, key2836) size=" << scan_result.size() << std::endl;
  }

  // op 47639: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1111", "key9899", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1111, key9899) size=" << scan_result.size() << std::endl;
  }

  // op 47640: put
  db->Put(write_options, "key11169", "value77036");

  // op 47641: get
  status = db->Get(read_options, "key0095", &value);
  if (status.ok()) {
    out << "GET key0095 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47642: put
  db->Put(write_options, "key1645", "value20365");

  // op 47643: get
  status = db->Get(read_options, "key10741", &value);
  if (status.ok()) {
    out << "GET key10741 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47644: delete
  db->Delete(write_options, "key0350");

  // op 47645: delete_range
  status = db->DeleteRange(write_options, "key5171", "key11686");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 47646: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2371", "key4656", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2371, key4656) size=" << scan_result.size() << std::endl;
  }

  // op 47647: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 47648: get
  status = db->Get(read_options, "key11251", &value);
  if (status.ok()) {
    out << "GET key11251 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47649: get
  status = db->Get(read_options, "key1233", &value);
  if (status.ok()) {
    out << "GET key1233 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47650: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6824", "key8761", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6824, key8761) size=" << scan_result.size() << std::endl;
  }

  // op 47651: get
  status = db->Get(read_options, "key5007", &value);
  if (status.ok()) {
    out << "GET key5007 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47652: put
  db->Put(write_options, "key6808", "value105007");

  // op 47653: put
  db->Put(write_options, "key3544", "value14532");

  // op 47654: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4600", "key10238", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4600, key10238) size=" << scan_result.size() << std::endl;
  }

  // op 47655: put
  db->Put(write_options, "key6521", "value61428");

  // op 47656: delete_range
  status = db->DeleteRange(write_options, "key8058", "key11193");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 47657: get
  status = db->Get(read_options, "key4847", &value);
  if (status.ok()) {
    out << "GET key4847 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47658: put
  db->Put(write_options, "key9642", "value79320");

  // op 47659: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4534", "key10883", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4534, key10883) size=" << scan_result.size() << std::endl;
  }

  // op 47660: delete
  db->Delete(write_options, "key2155");

  // op 47661: delete
  db->Delete(write_options, "key6877");

  // op 47662: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2923", "key4313", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2923, key4313) size=" << scan_result.size() << std::endl;
  }

  // op 47663: get
  status = db->Get(read_options, "key7020", &value);
  if (status.ok()) {
    out << "GET key7020 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47664: put
  db->Put(write_options, "key7931", "value101758");

  // op 47665: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2786", "key6172", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2786, key6172) size=" << scan_result.size() << std::endl;
  }

  // op 47666: put
  db->Put(write_options, "key4837", "value80121");

  // op 47667: delete_range
  status = db->DeleteRange(write_options, "key4054", "key9895");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 47668: put
  db->Put(write_options, "key10408", "value109592");

  // op 47669: delete_range
  status = db->DeleteRange(write_options, "key2885", "key11233");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 47670: get
  status = db->Get(read_options, "key7216", &value);
  if (status.ok()) {
    out << "GET key7216 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47671: put
  db->Put(write_options, "key6833", "value42315");

  // op 47672: get
  status = db->Get(read_options, "key7105", &value);
  if (status.ok()) {
    out << "GET key7105 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47673: put
  db->Put(write_options, "key1012", "value7929");

  // op 47674: put
  db->Put(write_options, "key8447", "value8193");

  // op 47675: put
  db->Put(write_options, "key8810", "value45022");

  // op 47676: get
  status = db->Get(read_options, "key4292", &value);
  if (status.ok()) {
    out << "GET key4292 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47677: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6724", "key7581", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6724, key7581) size=" << scan_result.size() << std::endl;
  }

  // op 47678: delete
  db->Delete(write_options, "key7607");

  // op 47679: delete_range
  status = db->DeleteRange(write_options, "key4012", "key8915");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 47680: get
  status = db->Get(read_options, "key0027", &value);
  if (status.ok()) {
    out << "GET key0027 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47681: put
  db->Put(write_options, "key4992", "value105311");

  // op 47682: get
  status = db->Get(read_options, "key3872", &value);
  if (status.ok()) {
    out << "GET key3872 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47683: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6387", "key7891", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6387, key7891) size=" << scan_result.size() << std::endl;
  }

  // op 47684: put
  db->Put(write_options, "key5290", "value96838");

  // op 47685: put
  db->Put(write_options, "key8788", "value11392");

  // op 47686: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1439", "key9347", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1439, key9347) size=" << scan_result.size() << std::endl;
  }

  // op 47687: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4605", "key10186", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4605, key10186) size=" << scan_result.size() << std::endl;
  }

  // op 47688: get
  status = db->Get(read_options, "key2463", &value);
  if (status.ok()) {
    out << "GET key2463 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47689: get
  status = db->Get(read_options, "key5709", &value);
  if (status.ok()) {
    out << "GET key5709 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47690: get
  status = db->Get(read_options, "key9921", &value);
  if (status.ok()) {
    out << "GET key9921 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47691: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 47692: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3784", "key4630", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3784, key4630) size=" << scan_result.size() << std::endl;
  }

  // op 47693: put
  db->Put(write_options, "key3322", "value96478");

  // op 47694: get
  status = db->Get(read_options, "key0260", &value);
  if (status.ok()) {
    out << "GET key0260 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47695: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3871", "key9771", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3871, key9771) size=" << scan_result.size() << std::endl;
  }

  // op 47696: put
  db->Put(write_options, "key1112", "value109964");

  // op 47697: get
  status = db->Get(read_options, "key2593", &value);
  if (status.ok()) {
    out << "GET key2593 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47698: put
  db->Put(write_options, "key1201", "value32741");

  // op 47699: put
  db->Put(write_options, "key1780", "value47458");

  // op 47700: put
  db->Put(write_options, "key5997", "value96254");

  // op 47701: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 47702: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3953", "key4428", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3953, key4428) size=" << scan_result.size() << std::endl;
  }

  // op 47703: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5446", "key9674", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5446, key9674) size=" << scan_result.size() << std::endl;
  }

  // op 47704: put
  db->Put(write_options, "key9348", "value7595");

  // op 47705: put
  db->Put(write_options, "key10988", "value112598");

  // op 47706: get
  status = db->Get(read_options, "key0849", &value);
  if (status.ok()) {
    out << "GET key0849 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47707: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5958", "key7166", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5958, key7166) size=" << scan_result.size() << std::endl;
  }

  // op 47708: put
  db->Put(write_options, "key1819", "value2980");

  // op 47709: put
  db->Put(write_options, "key9025", "value68702");

  // op 47710: put
  db->Put(write_options, "key11475", "value117205");

  // op 47711: delete
  db->Delete(write_options, "key4199");

  // op 47712: delete_range
  status = db->DeleteRange(write_options, "key8486", "key9039");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 47713: put
  db->Put(write_options, "key11317", "value90946");

  // op 47714: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5698", "key9809", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5698, key9809) size=" << scan_result.size() << std::endl;
  }

  // op 47715: get
  status = db->Get(read_options, "key9107", &value);
  if (status.ok()) {
    out << "GET key9107 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47716: delete_range
  status = db->DeleteRange(write_options, "key3331", "key4068");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 47717: put
  db->Put(write_options, "key10138", "value43384");

  // op 47718: get
  status = db->Get(read_options, "key0948", &value);
  if (status.ok()) {
    out << "GET key0948 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47719: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1487", "key11240", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1487, key11240) size=" << scan_result.size() << std::endl;
  }

  // op 47720: get
  status = db->Get(read_options, "key2651", &value);
  if (status.ok()) {
    out << "GET key2651 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47721: delete
  db->Delete(write_options, "key6773");

  // op 47722: delete
  db->Delete(write_options, "key9125");

  // op 47723: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4473", "key8363", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4473, key8363) size=" << scan_result.size() << std::endl;
  }

  // op 47724: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1443", "key3687", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1443, key3687) size=" << scan_result.size() << std::endl;
  }

  // op 47725: put
  db->Put(write_options, "key9586", "value77261");

  // op 47726: get
  status = db->Get(read_options, "key9853", &value);
  if (status.ok()) {
    out << "GET key9853 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47727: delete_range
  status = db->DeleteRange(write_options, "key1280", "key2741");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 47728: delete
  db->Delete(write_options, "key0658");

  // op 47729: delete_range
  status = db->DeleteRange(write_options, "key0487", "key2298");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 47730: put
  db->Put(write_options, "key7629", "value25924");

  // op 47731: put
  db->Put(write_options, "key7237", "value35041");

  // op 47732: get
  status = db->Get(read_options, "key0086", &value);
  if (status.ok()) {
    out << "GET key0086 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47733: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 47734: delete
  db->Delete(write_options, "key11381");

  // op 47735: get
  status = db->Get(read_options, "key7996", &value);
  if (status.ok()) {
    out << "GET key7996 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47736: delete_range
  status = db->DeleteRange(write_options, "key4120", "key6166");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 47737: put
  db->Put(write_options, "key0883", "value48415");

  // op 47738: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3854", "key8672", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3854, key8672) size=" << scan_result.size() << std::endl;
  }

  // op 47739: delete_range
  status = db->DeleteRange(write_options, "key9068", "key11381");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 47740: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0497", "key1104", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0497, key1104) size=" << scan_result.size() << std::endl;
  }

  // op 47741: put
  db->Put(write_options, "key4778", "value34824");

  // op 47742: put
  db->Put(write_options, "key2746", "value38804");

  // op 47743: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3684", "key6473", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3684, key6473) size=" << scan_result.size() << std::endl;
  }

  // op 47744: get
  status = db->Get(read_options, "key6794", &value);
  if (status.ok()) {
    out << "GET key6794 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47745: get
  status = db->Get(read_options, "key3227", &value);
  if (status.ok()) {
    out << "GET key3227 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47746: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2066", "key4601", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2066, key4601) size=" << scan_result.size() << std::endl;
  }

  // op 47747: get
  status = db->Get(read_options, "key8487", &value);
  if (status.ok()) {
    out << "GET key8487 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47748: put
  db->Put(write_options, "key4411", "value19094");

  // op 47749: put
  db->Put(write_options, "key7432", "value14679");

  // op 47750: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0923", "key2312", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0923, key2312) size=" << scan_result.size() << std::endl;
  }

  // op 47751: delete
  db->Delete(write_options, "key6558");

  // op 47752: put
  db->Put(write_options, "key10259", "value34094");

  // op 47753: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 47754: put
  db->Put(write_options, "key5747", "value3432");

  // op 47755: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 47756: delete
  db->Delete(write_options, "key9069");

  // op 47757: get
  status = db->Get(read_options, "key0034", &value);
  if (status.ok()) {
    out << "GET key0034 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47758: get
  status = db->Get(read_options, "key3994", &value);
  if (status.ok()) {
    out << "GET key3994 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47759: put
  db->Put(write_options, "key0346", "value112754");

  // op 47760: put
  db->Put(write_options, "key2277", "value32992");

  // op 47761: put
  db->Put(write_options, "key6004", "value96501");

  // op 47762: get
  status = db->Get(read_options, "key1127", &value);
  if (status.ok()) {
    out << "GET key1127 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47763: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 47764: put
  db->Put(write_options, "key3427", "value105827");

  // op 47765: put
  db->Put(write_options, "key2456", "value49447");

  // op 47766: put
  db->Put(write_options, "key10427", "value113052");

  // op 47767: put
  db->Put(write_options, "key10623", "value84787");

  // op 47768: delete
  db->Delete(write_options, "key5764");

  // op 47769: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6421", "key10124", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6421, key10124) size=" << scan_result.size() << std::endl;
  }

  // op 47770: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1252", "key2143", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1252, key2143) size=" << scan_result.size() << std::endl;
  }

  // op 47771: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3435", "key7771", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3435, key7771) size=" << scan_result.size() << std::endl;
  }

  // op 47772: put
  db->Put(write_options, "key8059", "value74746");

  // op 47773: delete
  db->Delete(write_options, "key7543");

  // op 47774: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0695", "key4306", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0695, key4306) size=" << scan_result.size() << std::endl;
  }

  // op 47775: put
  db->Put(write_options, "key8393", "value72035");

  // op 47776: delete
  db->Delete(write_options, "key9059");

  // op 47777: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 47778: get
  status = db->Get(read_options, "key11646", &value);
  if (status.ok()) {
    out << "GET key11646 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47779: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0177", "key3805", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0177, key3805) size=" << scan_result.size() << std::endl;
  }

  // op 47780: put
  db->Put(write_options, "key2939", "value16146");

  // op 47781: delete
  db->Delete(write_options, "key8968");

  // op 47782: get
  status = db->Get(read_options, "key6114", &value);
  if (status.ok()) {
    out << "GET key6114 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47783: delete
  db->Delete(write_options, "key6695");

  // op 47784: put
  db->Put(write_options, "key1079", "value65147");

  // op 47785: delete
  db->Delete(write_options, "key10133");

  // op 47786: delete_range
  status = db->DeleteRange(write_options, "key2862", "key7277");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 47787: put
  db->Put(write_options, "key1347", "value89731");

  // op 47788: get
  status = db->Get(read_options, "key9144", &value);
  if (status.ok()) {
    out << "GET key9144 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47789: get
  status = db->Get(read_options, "key4071", &value);
  if (status.ok()) {
    out << "GET key4071 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47790: put
  db->Put(write_options, "key9416", "value34935");

  // op 47791: delete_range
  status = db->DeleteRange(write_options, "key4972", "key7140");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 47792: delete_range
  status = db->DeleteRange(write_options, "key8649", "key8684");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 47793: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2007", "key9446", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2007, key9446) size=" << scan_result.size() << std::endl;
  }

  // op 47794: put
  db->Put(write_options, "key7416", "value1338");

  // op 47795: put
  db->Put(write_options, "key9204", "value81630");

  // op 47796: put
  db->Put(write_options, "key10249", "value86795");

  // op 47797: get
  status = db->Get(read_options, "key4842", &value);
  if (status.ok()) {
    out << "GET key4842 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47798: put
  db->Put(write_options, "key3233", "value116318");

  // op 47799: delete_range
  status = db->DeleteRange(write_options, "key2121", "key9329");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 47800: put
  db->Put(write_options, "key10561", "value16174");

  // op 47801: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0497", "key7062", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0497, key7062) size=" << scan_result.size() << std::endl;
  }

  // op 47802: delete_range
  status = db->DeleteRange(write_options, "key6788", "key10330");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 47803: put
  db->Put(write_options, "key1915", "value40425");

  // op 47804: put
  db->Put(write_options, "key0610", "value58983");

  // op 47805: put
  db->Put(write_options, "key5702", "value104637");

  // op 47806: get
  status = db->Get(read_options, "key3799", &value);
  if (status.ok()) {
    out << "GET key3799 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47807: delete_range
  status = db->DeleteRange(write_options, "key0762", "key7088");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 47808: put
  db->Put(write_options, "key7916", "value56472");

  // op 47809: put
  db->Put(write_options, "key1620", "value2916");

  // op 47810: put
  db->Put(write_options, "key5283", "value44861");

  // op 47811: put
  db->Put(write_options, "key11316", "value1634");

  // op 47812: get
  status = db->Get(read_options, "key10408", &value);
  if (status.ok()) {
    out << "GET key10408 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47813: get
  status = db->Get(read_options, "key1794", &value);
  if (status.ok()) {
    out << "GET key1794 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47814: get
  status = db->Get(read_options, "key7440", &value);
  if (status.ok()) {
    out << "GET key7440 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47815: put
  db->Put(write_options, "key10688", "value16185");

  // op 47816: get
  status = db->Get(read_options, "key8446", &value);
  if (status.ok()) {
    out << "GET key8446 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47817: put
  db->Put(write_options, "key3993", "value91072");

  // op 47818: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4482", "key4802", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4482, key4802) size=" << scan_result.size() << std::endl;
  }

  // op 47819: put
  db->Put(write_options, "key3732", "value94311");

  // op 47820: get
  status = db->Get(read_options, "key1587", &value);
  if (status.ok()) {
    out << "GET key1587 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47821: delete
  db->Delete(write_options, "key11239");

  // op 47822: put
  db->Put(write_options, "key10600", "value43121");

  // op 47823: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8880", "key11651", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8880, key11651) size=" << scan_result.size() << std::endl;
  }

  // op 47824: get
  status = db->Get(read_options, "key7748", &value);
  if (status.ok()) {
    out << "GET key7748 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47825: put
  db->Put(write_options, "key1350", "value63411");

  // op 47826: delete
  db->Delete(write_options, "key6556");

  // op 47827: delete
  db->Delete(write_options, "key10301");

  // op 47828: put
  db->Put(write_options, "key2498", "value52578");

  // op 47829: delete
  db->Delete(write_options, "key2911");

  // op 47830: put
  db->Put(write_options, "key3406", "value84684");

  // op 47831: put
  db->Put(write_options, "key1551", "value99723");

  // op 47832: get
  status = db->Get(read_options, "key5051", &value);
  if (status.ok()) {
    out << "GET key5051 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47833: get
  status = db->Get(read_options, "key10398", &value);
  if (status.ok()) {
    out << "GET key10398 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47834: get
  status = db->Get(read_options, "key5683", &value);
  if (status.ok()) {
    out << "GET key5683 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47835: put
  db->Put(write_options, "key10175", "value114902");

  // op 47836: put
  db->Put(write_options, "key4097", "value102249");

  // op 47837: put
  db->Put(write_options, "key0935", "value51484");

  // op 47838: put
  db->Put(write_options, "key5957", "value51647");

  // op 47839: delete
  db->Delete(write_options, "key10240");

  // op 47840: put
  db->Put(write_options, "key0849", "value47868");

  // op 47841: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 47842: delete_range
  status = db->DeleteRange(write_options, "key0642", "key2737");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 47843: put
  db->Put(write_options, "key10741", "value4867");

  // op 47844: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0615", "key11283", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0615, key11283) size=" << scan_result.size() << std::endl;
  }

  // op 47845: get
  status = db->Get(read_options, "key2053", &value);
  if (status.ok()) {
    out << "GET key2053 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47846: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 47847: put
  db->Put(write_options, "key7943", "value12731");

  // op 47848: put
  db->Put(write_options, "key11144", "value98383");

  // op 47849: get
  status = db->Get(read_options, "key5879", &value);
  if (status.ok()) {
    out << "GET key5879 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47850: put
  db->Put(write_options, "key5249", "value98090");

  // op 47851: delete
  db->Delete(write_options, "key0553");

  // op 47852: put
  db->Put(write_options, "key6504", "value36867");

  // op 47853: get
  status = db->Get(read_options, "key4274", &value);
  if (status.ok()) {
    out << "GET key4274 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47854: delete_range
  status = db->DeleteRange(write_options, "key2188", "key10111");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 47855: put
  db->Put(write_options, "key8366", "value23236");

  // op 47856: get
  status = db->Get(read_options, "key11393", &value);
  if (status.ok()) {
    out << "GET key11393 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47857: delete_range
  status = db->DeleteRange(write_options, "key4410", "key7202");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 47858: get
  status = db->Get(read_options, "key11708", &value);
  if (status.ok()) {
    out << "GET key11708 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47859: delete_range
  status = db->DeleteRange(write_options, "key1647", "key9255");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 47860: put
  db->Put(write_options, "key1078", "value13149");

  // op 47861: put
  db->Put(write_options, "key10903", "value46670");

  // op 47862: put
  db->Put(write_options, "key8407", "value14138");

  // op 47863: get
  status = db->Get(read_options, "key3913", &value);
  if (status.ok()) {
    out << "GET key3913 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47864: put
  db->Put(write_options, "key10232", "value98698");

  // op 47865: put
  db->Put(write_options, "key3358", "value4381");

  // op 47866: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7276", "key7291", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7276, key7291) size=" << scan_result.size() << std::endl;
  }

  // op 47867: put
  db->Put(write_options, "key3050", "value85997");

  // op 47868: put
  db->Put(write_options, "key5386", "value98559");

  // op 47869: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 47870: delete_range
  status = db->DeleteRange(write_options, "key3998", "key10087");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 47871: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1252", "key3104", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1252, key3104) size=" << scan_result.size() << std::endl;
  }

  // op 47872: get
  status = db->Get(read_options, "key4419", &value);
  if (status.ok()) {
    out << "GET key4419 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47873: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4920", "key6758", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4920, key6758) size=" << scan_result.size() << std::endl;
  }

  // op 47874: delete
  db->Delete(write_options, "key8522");

  // op 47875: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7962", "key10360", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7962, key10360) size=" << scan_result.size() << std::endl;
  }

  // op 47876: put
  db->Put(write_options, "key7411", "value97396");

  // op 47877: get
  status = db->Get(read_options, "key4797", &value);
  if (status.ok()) {
    out << "GET key4797 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47878: put
  db->Put(write_options, "key8426", "value58719");

  // op 47879: put
  db->Put(write_options, "key6760", "value32674");

  // op 47880: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3343", "key4218", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3343, key4218) size=" << scan_result.size() << std::endl;
  }

  // op 47881: put
  db->Put(write_options, "key4884", "value51102");

  // op 47882: delete_range
  status = db->DeleteRange(write_options, "key0151", "key4126");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 47883: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 47884: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2923", "key8858", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2923, key8858) size=" << scan_result.size() << std::endl;
  }

  // op 47885: put
  db->Put(write_options, "key0951", "value118115");

  // op 47886: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5751", "key8749", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5751, key8749) size=" << scan_result.size() << std::endl;
  }

  // op 47887: get
  status = db->Get(read_options, "key2305", &value);
  if (status.ok()) {
    out << "GET key2305 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47888: get
  status = db->Get(read_options, "key5994", &value);
  if (status.ok()) {
    out << "GET key5994 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47889: delete
  db->Delete(write_options, "key3743");

  // op 47890: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8363", "key9335", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8363, key9335) size=" << scan_result.size() << std::endl;
  }

  // op 47891: get
  status = db->Get(read_options, "key6355", &value);
  if (status.ok()) {
    out << "GET key6355 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47892: delete
  db->Delete(write_options, "key6646");

  // op 47893: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0183", "key4316", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0183, key4316) size=" << scan_result.size() << std::endl;
  }

  // op 47894: get
  status = db->Get(read_options, "key7966", &value);
  if (status.ok()) {
    out << "GET key7966 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47895: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8537", "key10622", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8537, key10622) size=" << scan_result.size() << std::endl;
  }

  // op 47896: get
  status = db->Get(read_options, "key11511", &value);
  if (status.ok()) {
    out << "GET key11511 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47897: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2206", "key4780", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2206, key4780) size=" << scan_result.size() << std::endl;
  }

  // op 47898: delete_range
  status = db->DeleteRange(write_options, "key6604", "key8356");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 47899: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 47900: get
  status = db->Get(read_options, "key1077", &value);
  if (status.ok()) {
    out << "GET key1077 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47901: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8933", "key9340", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8933, key9340) size=" << scan_result.size() << std::endl;
  }

  // op 47902: get
  status = db->Get(read_options, "key10908", &value);
  if (status.ok()) {
    out << "GET key10908 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47903: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7322", "key9636", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7322, key9636) size=" << scan_result.size() << std::endl;
  }

  // op 47904: get
  status = db->Get(read_options, "key9159", &value);
  if (status.ok()) {
    out << "GET key9159 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47905: delete_range
  status = db->DeleteRange(write_options, "key0911", "key1109");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 47906: get
  status = db->Get(read_options, "key0777", &value);
  if (status.ok()) {
    out << "GET key0777 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47907: get
  status = db->Get(read_options, "key5508", &value);
  if (status.ok()) {
    out << "GET key5508 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47908: put
  db->Put(write_options, "key4569", "value42898");

  // op 47909: delete_range
  status = db->DeleteRange(write_options, "key4314", "key5178");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 47910: put
  db->Put(write_options, "key10890", "value63658");

  // op 47911: delete_range
  status = db->DeleteRange(write_options, "key2671", "key11479");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 47912: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 47913: delete
  db->Delete(write_options, "key9551");

  // op 47914: delete_range
  status = db->DeleteRange(write_options, "key1288", "key7499");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 47915: put
  db->Put(write_options, "key2228", "value97112");

  // op 47916: delete
  db->Delete(write_options, "key0087");

  // op 47917: put
  db->Put(write_options, "key9638", "value94830");

  // op 47918: delete_range
  status = db->DeleteRange(write_options, "key11095", "key11467");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 47919: put
  db->Put(write_options, "key1945", "value32918");

  // op 47920: put
  db->Put(write_options, "key0375", "value83141");

  // op 47921: put
  db->Put(write_options, "key11260", "value94169");

  // op 47922: get
  status = db->Get(read_options, "key0365", &value);
  if (status.ok()) {
    out << "GET key0365 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47923: delete
  db->Delete(write_options, "key0221");

  // op 47924: put
  db->Put(write_options, "key10028", "value2417");

  // op 47925: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 47926: put
  db->Put(write_options, "key11451", "value114356");

  // op 47927: put
  db->Put(write_options, "key4975", "value62401");

  // op 47928: put
  db->Put(write_options, "key6074", "value68323");

  // op 47929: get
  status = db->Get(read_options, "key10546", &value);
  if (status.ok()) {
    out << "GET key10546 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47930: get
  status = db->Get(read_options, "key6752", &value);
  if (status.ok()) {
    out << "GET key6752 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47931: put
  db->Put(write_options, "key4835", "value66875");

  // op 47932: put
  db->Put(write_options, "key3972", "value106135");

  // op 47933: delete_range
  status = db->DeleteRange(write_options, "key8573", "key10572");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 47934: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6317", "key8789", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6317, key8789) size=" << scan_result.size() << std::endl;
  }

  // op 47935: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4112", "key7780", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4112, key7780) size=" << scan_result.size() << std::endl;
  }

  // op 47936: put
  db->Put(write_options, "key3346", "value36864");

  // op 47937: get
  status = db->Get(read_options, "key1544", &value);
  if (status.ok()) {
    out << "GET key1544 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47938: put
  db->Put(write_options, "key1744", "value52574");

  // op 47939: put
  db->Put(write_options, "key1728", "value102014");

  // op 47940: put
  db->Put(write_options, "key3906", "value4114");

  // op 47941: delete
  db->Delete(write_options, "key10303");

  // op 47942: put
  db->Put(write_options, "key6518", "value67640");

  // op 47943: put
  db->Put(write_options, "key6855", "value43378");

  // op 47944: get
  status = db->Get(read_options, "key11105", &value);
  if (status.ok()) {
    out << "GET key11105 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47945: put
  db->Put(write_options, "key3592", "value21349");

  // op 47946: put
  db->Put(write_options, "key6700", "value69965");

  // op 47947: put
  db->Put(write_options, "key1337", "value100498");

  // op 47948: delete
  db->Delete(write_options, "key3796");

  // op 47949: get
  status = db->Get(read_options, "key3087", &value);
  if (status.ok()) {
    out << "GET key3087 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47950: put
  db->Put(write_options, "key3380", "value117200");

  // op 47951: put
  db->Put(write_options, "key9223", "value115158");

  // op 47952: delete_range
  status = db->DeleteRange(write_options, "key0624", "key9191");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 47953: delete_range
  status = db->DeleteRange(write_options, "key3162", "key3388");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 47954: put
  db->Put(write_options, "key5057", "value3991");

  // op 47955: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4312", "key9223", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4312, key9223) size=" << scan_result.size() << std::endl;
  }

  // op 47956: get
  status = db->Get(read_options, "key5427", &value);
  if (status.ok()) {
    out << "GET key5427 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47957: put
  db->Put(write_options, "key8036", "value53930");

  // op 47958: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7568", "key8382", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7568, key8382) size=" << scan_result.size() << std::endl;
  }

  // op 47959: delete_range
  status = db->DeleteRange(write_options, "key4455", "key10965");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 47960: put
  db->Put(write_options, "key8278", "value104478");

  // op 47961: put
  db->Put(write_options, "key0328", "value102275");

  // op 47962: put
  db->Put(write_options, "key7081", "value38062");

  // op 47963: get
  status = db->Get(read_options, "key7380", &value);
  if (status.ok()) {
    out << "GET key7380 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47964: put
  db->Put(write_options, "key7422", "value78037");

  // op 47965: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0063", "key7394", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0063, key7394) size=" << scan_result.size() << std::endl;
  }

  // op 47966: put
  db->Put(write_options, "key0186", "value23847");

  // op 47967: put
  db->Put(write_options, "key4247", "value0641");

  // op 47968: get
  status = db->Get(read_options, "key5912", &value);
  if (status.ok()) {
    out << "GET key5912 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47969: put
  db->Put(write_options, "key11238", "value44775");

  // op 47970: put
  db->Put(write_options, "key2281", "value95986");

  // op 47971: delete_range
  status = db->DeleteRange(write_options, "key6081", "key7793");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 47972: get
  status = db->Get(read_options, "key6020", &value);
  if (status.ok()) {
    out << "GET key6020 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47973: put
  db->Put(write_options, "key8473", "value57209");

  // op 47974: get
  status = db->Get(read_options, "key10021", &value);
  if (status.ok()) {
    out << "GET key10021 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47975: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1130", "key7018", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1130, key7018) size=" << scan_result.size() << std::endl;
  }

  // op 47976: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1077", "key8113", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1077, key8113) size=" << scan_result.size() << std::endl;
  }

  // op 47977: get
  status = db->Get(read_options, "key4894", &value);
  if (status.ok()) {
    out << "GET key4894 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47978: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 47979: delete
  db->Delete(write_options, "key10166");

  // op 47980: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5924", "key10673", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5924, key10673) size=" << scan_result.size() << std::endl;
  }

  // op 47981: put
  db->Put(write_options, "key5484", "value46218");

  // op 47982: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3693", "key8011", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3693, key8011) size=" << scan_result.size() << std::endl;
  }

  // op 47983: get
  status = db->Get(read_options, "key0884", &value);
  if (status.ok()) {
    out << "GET key0884 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47984: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5409", "key7311", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5409, key7311) size=" << scan_result.size() << std::endl;
  }

  // op 47985: put
  db->Put(write_options, "key3280", "value90218");

  // op 47986: get
  status = db->Get(read_options, "key4176", &value);
  if (status.ok()) {
    out << "GET key4176 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47987: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2229", "key11344", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2229, key11344) size=" << scan_result.size() << std::endl;
  }

  // op 47988: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5607", "key11363", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5607, key11363) size=" << scan_result.size() << std::endl;
  }

  // op 47989: put
  db->Put(write_options, "key7778", "value0626");

  // op 47990: put
  db->Put(write_options, "key8334", "value85299");

  // op 47991: put
  db->Put(write_options, "key11014", "value112187");

  // op 47992: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5282", "key9468", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5282, key9468) size=" << scan_result.size() << std::endl;
  }

  // op 47993: put
  db->Put(write_options, "key3943", "value111781");

  // op 47994: get
  status = db->Get(read_options, "key9671", &value);
  if (status.ok()) {
    out << "GET key9671 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47995: put
  db->Put(write_options, "key10984", "value94707");

  // op 47996: get
  status = db->Get(read_options, "key6904", &value);
  if (status.ok()) {
    out << "GET key6904 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 47997: put
  db->Put(write_options, "key8702", "value6927");

  // op 47998: put
  db->Put(write_options, "key6106", "value100233");

  // op 47999: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1281", "key11641", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1281, key11641) size=" << scan_result.size() << std::endl;
  }

  // op 48000: delete_range
  status = db->DeleteRange(write_options, "key7330", "key10842");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 48001: get
  status = db->Get(read_options, "key10390", &value);
  if (status.ok()) {
    out << "GET key10390 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48002: get
  status = db->Get(read_options, "key11199", &value);
  if (status.ok()) {
    out << "GET key11199 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48003: put
  db->Put(write_options, "key7029", "value4964");

  // op 48004: put
  db->Put(write_options, "key1094", "value84307");

  // op 48005: get
  status = db->Get(read_options, "key5637", &value);
  if (status.ok()) {
    out << "GET key5637 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48006: get
  status = db->Get(read_options, "key5197", &value);
  if (status.ok()) {
    out << "GET key5197 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48007: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5793", "key6069", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5793, key6069) size=" << scan_result.size() << std::endl;
  }

  // op 48008: put
  db->Put(write_options, "key1389", "value78570");

  // op 48009: put
  db->Put(write_options, "key3634", "value111759");

  // op 48010: put
  db->Put(write_options, "key6241", "value75620");

  // op 48011: put
  db->Put(write_options, "key9846", "value34197");

  // op 48012: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3413", "key5255", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3413, key5255) size=" << scan_result.size() << std::endl;
  }

  // op 48013: put
  db->Put(write_options, "key7888", "value70093");

  // op 48014: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2716", "key4151", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2716, key4151) size=" << scan_result.size() << std::endl;
  }

  // op 48015: put
  db->Put(write_options, "key8688", "value16218");

  // op 48016: put
  db->Put(write_options, "key5427", "value77905");

  // op 48017: delete
  db->Delete(write_options, "key9958");

  // op 48018: put
  db->Put(write_options, "key5769", "value47482");

  // op 48019: put
  db->Put(write_options, "key6086", "value71225");

  // op 48020: put
  db->Put(write_options, "key9530", "value88162");

  // op 48021: put
  db->Put(write_options, "key6923", "value97272");

  // op 48022: get
  status = db->Get(read_options, "key7773", &value);
  if (status.ok()) {
    out << "GET key7773 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48023: delete_range
  status = db->DeleteRange(write_options, "key9384", "key10316");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 48024: delete_range
  status = db->DeleteRange(write_options, "key3823", "key6855");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 48025: delete_range
  status = db->DeleteRange(write_options, "key6179", "key11342");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 48026: put
  db->Put(write_options, "key1648", "value38193");

  // op 48027: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9846", "key10830", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9846, key10830) size=" << scan_result.size() << std::endl;
  }

  // op 48028: put
  db->Put(write_options, "key4894", "value105742");

  // op 48029: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1458", "key7052", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1458, key7052) size=" << scan_result.size() << std::endl;
  }

  // op 48030: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 48031: put
  db->Put(write_options, "key9373", "value75539");

  // op 48032: delete_range
  status = db->DeleteRange(write_options, "key5299", "key7734");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 48033: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2827", "key8871", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2827, key8871) size=" << scan_result.size() << std::endl;
  }

  // op 48034: get
  status = db->Get(read_options, "key1371", &value);
  if (status.ok()) {
    out << "GET key1371 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48035: put
  db->Put(write_options, "key11509", "value8121");

  // op 48036: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8146", "key9848", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8146, key9848) size=" << scan_result.size() << std::endl;
  }

  // op 48037: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3551", "key10877", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3551, key10877) size=" << scan_result.size() << std::endl;
  }

  // op 48038: get
  status = db->Get(read_options, "key1194", &value);
  if (status.ok()) {
    out << "GET key1194 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48039: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1674", "key2918", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1674, key2918) size=" << scan_result.size() << std::endl;
  }

  // op 48040: delete_range
  status = db->DeleteRange(write_options, "key6035", "key9603");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 48041: delete_range
  status = db->DeleteRange(write_options, "key7480", "key9295");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 48042: delete
  db->Delete(write_options, "key9300");

  // op 48043: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7691", "key10968", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7691, key10968) size=" << scan_result.size() << std::endl;
  }

  // op 48044: put
  db->Put(write_options, "key4452", "value96597");

  // op 48045: put
  db->Put(write_options, "key2244", "value114154");

  // op 48046: get
  status = db->Get(read_options, "key9536", &value);
  if (status.ok()) {
    out << "GET key9536 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48047: delete_range
  status = db->DeleteRange(write_options, "key7944", "key10271");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 48048: put
  db->Put(write_options, "key9056", "value26125");

  // op 48049: put
  db->Put(write_options, "key5799", "value57295");

  // op 48050: get
  status = db->Get(read_options, "key3547", &value);
  if (status.ok()) {
    out << "GET key3547 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48051: put
  db->Put(write_options, "key1095", "value68782");

  // op 48052: put
  db->Put(write_options, "key2458", "value64600");

  // op 48053: put
  db->Put(write_options, "key1632", "value24308");

  // op 48054: put
  db->Put(write_options, "key4771", "value104600");

  // op 48055: put
  db->Put(write_options, "key10798", "value106004");

  // op 48056: delete_range
  status = db->DeleteRange(write_options, "key1803", "key5963");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 48057: delete_range
  status = db->DeleteRange(write_options, "key2798", "key11424");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 48058: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5243", "key8462", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5243, key8462) size=" << scan_result.size() << std::endl;
  }

  // op 48059: put
  db->Put(write_options, "key1613", "value4233");

  // op 48060: delete_range
  status = db->DeleteRange(write_options, "key6561", "key7180");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 48061: put
  db->Put(write_options, "key7918", "value80401");

  // op 48062: put
  db->Put(write_options, "key1286", "value86620");

  // op 48063: get
  status = db->Get(read_options, "key9037", &value);
  if (status.ok()) {
    out << "GET key9037 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48064: get
  status = db->Get(read_options, "key11161", &value);
  if (status.ok()) {
    out << "GET key11161 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48065: put
  db->Put(write_options, "key6487", "value71130");

  // op 48066: put
  db->Put(write_options, "key1204", "value56365");

  // op 48067: delete_range
  status = db->DeleteRange(write_options, "key1022", "key8095");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 48068: put
  db->Put(write_options, "key1607", "value80072");

  // op 48069: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 48070: get
  status = db->Get(read_options, "key7206", &value);
  if (status.ok()) {
    out << "GET key7206 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48071: delete_range
  status = db->DeleteRange(write_options, "key3541", "key7648");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 48072: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1210", "key10565", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1210, key10565) size=" << scan_result.size() << std::endl;
  }

  // op 48073: get
  status = db->Get(read_options, "key3458", &value);
  if (status.ok()) {
    out << "GET key3458 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48074: put
  db->Put(write_options, "key2275", "value45331");

  // op 48075: put
  db->Put(write_options, "key4286", "value34453");

  // op 48076: put
  db->Put(write_options, "key2562", "value78835");

  // op 48077: delete_range
  status = db->DeleteRange(write_options, "key2104", "key2572");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 48078: put
  db->Put(write_options, "key5652", "value47335");

  // op 48079: put
  db->Put(write_options, "key9632", "value39669");

  // op 48080: put
  db->Put(write_options, "key8334", "value61302");

  // op 48081: put
  db->Put(write_options, "key1699", "value70051");

  // op 48082: put
  db->Put(write_options, "key1220", "value43321");

  // op 48083: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1638", "key7799", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1638, key7799) size=" << scan_result.size() << std::endl;
  }

  // op 48084: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 48085: put
  db->Put(write_options, "key0173", "value41060");

  // op 48086: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0247", "key9083", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0247, key9083) size=" << scan_result.size() << std::endl;
  }

  // op 48087: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5657", "key9950", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5657, key9950) size=" << scan_result.size() << std::endl;
  }

  // op 48088: get
  status = db->Get(read_options, "key9863", &value);
  if (status.ok()) {
    out << "GET key9863 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48089: put
  db->Put(write_options, "key3227", "value60615");

  // op 48090: put
  db->Put(write_options, "key7177", "value20839");

  // op 48091: delete
  db->Delete(write_options, "key10220");

  // op 48092: get
  status = db->Get(read_options, "key9922", &value);
  if (status.ok()) {
    out << "GET key9922 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48093: put
  db->Put(write_options, "key1718", "value28410");

  // op 48094: get
  status = db->Get(read_options, "key5242", &value);
  if (status.ok()) {
    out << "GET key5242 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48095: get
  status = db->Get(read_options, "key11329", &value);
  if (status.ok()) {
    out << "GET key11329 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48096: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6816", "key11545", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6816, key11545) size=" << scan_result.size() << std::endl;
  }

  // op 48097: delete_range
  status = db->DeleteRange(write_options, "key2776", "key4380");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 48098: delete
  db->Delete(write_options, "key6098");

  // op 48099: put
  db->Put(write_options, "key6719", "value26850");

  // op 48100: get
  status = db->Get(read_options, "key8776", &value);
  if (status.ok()) {
    out << "GET key8776 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48101: put
  db->Put(write_options, "key7100", "value45256");

  // op 48102: put
  db->Put(write_options, "key6154", "value64081");

  // op 48103: get
  status = db->Get(read_options, "key0221", &value);
  if (status.ok()) {
    out << "GET key0221 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48104: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2732", "key6592", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2732, key6592) size=" << scan_result.size() << std::endl;
  }

  // op 48105: put
  db->Put(write_options, "key2090", "value97919");

  // op 48106: get
  status = db->Get(read_options, "key10338", &value);
  if (status.ok()) {
    out << "GET key10338 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48107: put
  db->Put(write_options, "key0438", "value57278");

  // op 48108: delete
  db->Delete(write_options, "key2080");

  // op 48109: delete_range
  status = db->DeleteRange(write_options, "key2084", "key5772");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 48110: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2975", "key8009", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2975, key8009) size=" << scan_result.size() << std::endl;
  }

  // op 48111: put
  db->Put(write_options, "key8600", "value102086");

  // op 48112: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4825", "key5995", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4825, key5995) size=" << scan_result.size() << std::endl;
  }

  // op 48113: put
  db->Put(write_options, "key11187", "value115784");

  // op 48114: get
  status = db->Get(read_options, "key4005", &value);
  if (status.ok()) {
    out << "GET key4005 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48115: delete
  db->Delete(write_options, "key4251");

  // op 48116: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 48117: delete
  db->Delete(write_options, "key3998");

  // op 48118: delete_range
  status = db->DeleteRange(write_options, "key1317", "key10938");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 48119: get
  status = db->Get(read_options, "key7862", &value);
  if (status.ok()) {
    out << "GET key7862 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48120: put
  db->Put(write_options, "key6652", "value63204");

  // op 48121: put
  db->Put(write_options, "key1740", "value99803");

  // op 48122: delete
  db->Delete(write_options, "key6200");

  // op 48123: delete_range
  status = db->DeleteRange(write_options, "key3395", "key7830");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 48124: delete_range
  status = db->DeleteRange(write_options, "key4157", "key11116");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 48125: put
  db->Put(write_options, "key9156", "value110753");

  // op 48126: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 48127: put
  db->Put(write_options, "key8950", "value20261");

  // op 48128: delete
  db->Delete(write_options, "key0110");

  // op 48129: get
  status = db->Get(read_options, "key11252", &value);
  if (status.ok()) {
    out << "GET key11252 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48130: put
  db->Put(write_options, "key9774", "value62479");

  // op 48131: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 48132: delete_range
  status = db->DeleteRange(write_options, "key8415", "key9659");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 48133: put
  db->Put(write_options, "key8097", "value76938");

  // op 48134: put
  db->Put(write_options, "key3647", "value75599");

  // op 48135: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4003", "key11690", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4003, key11690) size=" << scan_result.size() << std::endl;
  }

  // op 48136: delete_range
  status = db->DeleteRange(write_options, "key1784", "key7672");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 48137: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 48138: put
  db->Put(write_options, "key9723", "value38551");

  // op 48139: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8795", "key11016", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8795, key11016) size=" << scan_result.size() << std::endl;
  }

  // op 48140: get
  status = db->Get(read_options, "key6911", &value);
  if (status.ok()) {
    out << "GET key6911 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48141: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5389", "key5666", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5389, key5666) size=" << scan_result.size() << std::endl;
  }

  // op 48142: get
  status = db->Get(read_options, "key6805", &value);
  if (status.ok()) {
    out << "GET key6805 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48143: get
  status = db->Get(read_options, "key0679", &value);
  if (status.ok()) {
    out << "GET key0679 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48144: put
  db->Put(write_options, "key3496", "value88025");

  // op 48145: delete_range
  status = db->DeleteRange(write_options, "key2173", "key4101");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 48146: put
  db->Put(write_options, "key3022", "value53791");

  // op 48147: put
  db->Put(write_options, "key3577", "value37896");

  // op 48148: get
  status = db->Get(read_options, "key5850", &value);
  if (status.ok()) {
    out << "GET key5850 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48149: delete
  db->Delete(write_options, "key10106");

  // op 48150: put
  db->Put(write_options, "key8172", "value13587");

  // op 48151: put
  db->Put(write_options, "key11300", "value107652");

  // op 48152: put
  db->Put(write_options, "key3954", "value46245");

  // op 48153: get
  status = db->Get(read_options, "key7655", &value);
  if (status.ok()) {
    out << "GET key7655 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48154: get
  status = db->Get(read_options, "key9479", &value);
  if (status.ok()) {
    out << "GET key9479 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48155: get
  status = db->Get(read_options, "key3516", &value);
  if (status.ok()) {
    out << "GET key3516 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48156: get
  status = db->Get(read_options, "key7896", &value);
  if (status.ok()) {
    out << "GET key7896 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48157: put
  db->Put(write_options, "key9486", "value17741");

  // op 48158: put
  db->Put(write_options, "key11288", "value32398");

  // op 48159: put
  db->Put(write_options, "key5522", "value73322");

  // op 48160: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2198", "key2952", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2198, key2952) size=" << scan_result.size() << std::endl;
  }

  // op 48161: put
  db->Put(write_options, "key11117", "value28597");

  // op 48162: put
  db->Put(write_options, "key2200", "value81681");

  // op 48163: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2773", "key7209", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2773, key7209) size=" << scan_result.size() << std::endl;
  }

  // op 48164: get
  status = db->Get(read_options, "key5511", &value);
  if (status.ok()) {
    out << "GET key5511 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48165: delete_range
  status = db->DeleteRange(write_options, "key3908", "key4403");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 48166: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4963", "key5648", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4963, key5648) size=" << scan_result.size() << std::endl;
  }

  // op 48167: delete
  db->Delete(write_options, "key3044");

  // op 48168: put
  db->Put(write_options, "key3652", "value103325");

  // op 48169: put
  db->Put(write_options, "key11262", "value2777");

  // op 48170: get
  status = db->Get(read_options, "key4957", &value);
  if (status.ok()) {
    out << "GET key4957 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48171: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3004", "key7843", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3004, key7843) size=" << scan_result.size() << std::endl;
  }

  // op 48172: put
  db->Put(write_options, "key7715", "value110019");

  // op 48173: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 48174: put
  db->Put(write_options, "key6411", "value107066");

  // op 48175: put
  db->Put(write_options, "key6226", "value51398");

  // op 48176: put
  db->Put(write_options, "key0502", "value52104");

  // op 48177: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1099", "key1986", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1099, key1986) size=" << scan_result.size() << std::endl;
  }

  // op 48178: delete_range
  status = db->DeleteRange(write_options, "key0221", "key5611");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 48179: put
  db->Put(write_options, "key8730", "value19147");

  // op 48180: get
  status = db->Get(read_options, "key10326", &value);
  if (status.ok()) {
    out << "GET key10326 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48181: put
  db->Put(write_options, "key4884", "value60727");

  // op 48182: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0315", "key1479", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0315, key1479) size=" << scan_result.size() << std::endl;
  }

  // op 48183: delete
  db->Delete(write_options, "key0556");

  // op 48184: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2350", "key10449", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2350, key10449) size=" << scan_result.size() << std::endl;
  }

  // op 48185: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 48186: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 48187: put
  db->Put(write_options, "key0277", "value59653");

  // op 48188: put
  db->Put(write_options, "key6767", "value60012");

  // op 48189: put
  db->Put(write_options, "key7978", "value38389");

  // op 48190: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1286", "key3132", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1286, key3132) size=" << scan_result.size() << std::endl;
  }

  // op 48191: put
  db->Put(write_options, "key8894", "value115225");

  // op 48192: put
  db->Put(write_options, "key0014", "value109583");

  // op 48193: get
  status = db->Get(read_options, "key0204", &value);
  if (status.ok()) {
    out << "GET key0204 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48194: delete
  db->Delete(write_options, "key1819");

  // op 48195: put
  db->Put(write_options, "key0849", "value109284");

  // op 48196: get
  status = db->Get(read_options, "key3523", &value);
  if (status.ok()) {
    out << "GET key3523 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48197: delete
  db->Delete(write_options, "key0649");

  // op 48198: put
  db->Put(write_options, "key4344", "value38340");

  // op 48199: get
  status = db->Get(read_options, "key6371", &value);
  if (status.ok()) {
    out << "GET key6371 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48200: put
  db->Put(write_options, "key1267", "value50347");

  // op 48201: put
  db->Put(write_options, "key2351", "value107315");

  // op 48202: put
  db->Put(write_options, "key5276", "value61986");

  // op 48203: get
  status = db->Get(read_options, "key9708", &value);
  if (status.ok()) {
    out << "GET key9708 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48204: put
  db->Put(write_options, "key10937", "value42677");

  // op 48205: put
  db->Put(write_options, "key1523", "value27612");

  // op 48206: get
  status = db->Get(read_options, "key4726", &value);
  if (status.ok()) {
    out << "GET key4726 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48207: put
  db->Put(write_options, "key1012", "value64526");

  // op 48208: get
  status = db->Get(read_options, "key7880", &value);
  if (status.ok()) {
    out << "GET key7880 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48209: delete_range
  status = db->DeleteRange(write_options, "key5378", "key10218");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 48210: put
  db->Put(write_options, "key4139", "value21438");

  // op 48211: put
  db->Put(write_options, "key6186", "value64872");

  // op 48212: put
  db->Put(write_options, "key2425", "value57998");

  // op 48213: put
  db->Put(write_options, "key9182", "value34630");

  // op 48214: get
  status = db->Get(read_options, "key7768", &value);
  if (status.ok()) {
    out << "GET key7768 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48215: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1705", "key2405", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1705, key2405) size=" << scan_result.size() << std::endl;
  }

  // op 48216: delete_range
  status = db->DeleteRange(write_options, "key7729", "key9044");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 48217: put
  db->Put(write_options, "key11560", "value90297");

  // op 48218: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6387", "key9983", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6387, key9983) size=" << scan_result.size() << std::endl;
  }

  // op 48219: delete
  db->Delete(write_options, "key2833");

  // op 48220: put
  db->Put(write_options, "key0535", "value29847");

  // op 48221: delete_range
  status = db->DeleteRange(write_options, "key1450", "key6602");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 48222: put
  db->Put(write_options, "key9856", "value27461");

  // op 48223: put
  db->Put(write_options, "key4361", "value115736");

  // op 48224: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0428", "key5597", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0428, key5597) size=" << scan_result.size() << std::endl;
  }

  // op 48225: put
  db->Put(write_options, "key10871", "value22638");

  // op 48226: put
  db->Put(write_options, "key8743", "value97554");

  // op 48227: put
  db->Put(write_options, "key7800", "value8089");

  // op 48228: put
  db->Put(write_options, "key5048", "value9514");

  // op 48229: put
  db->Put(write_options, "key5697", "value104760");

  // op 48230: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 48231: get
  status = db->Get(read_options, "key5140", &value);
  if (status.ok()) {
    out << "GET key5140 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48232: get
  status = db->Get(read_options, "key4576", &value);
  if (status.ok()) {
    out << "GET key4576 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48233: delete_range
  status = db->DeleteRange(write_options, "key4976", "key11628");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 48234: put
  db->Put(write_options, "key0895", "value14092");

  // op 48235: get
  status = db->Get(read_options, "key0564", &value);
  if (status.ok()) {
    out << "GET key0564 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48236: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7877", "key8503", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7877, key8503) size=" << scan_result.size() << std::endl;
  }

  // op 48237: put
  db->Put(write_options, "key10989", "value98569");

  // op 48238: put
  db->Put(write_options, "key9634", "value71336");

  // op 48239: put
  db->Put(write_options, "key10161", "value96499");

  // op 48240: put
  db->Put(write_options, "key1563", "value81701");

  // op 48241: get
  status = db->Get(read_options, "key9517", &value);
  if (status.ok()) {
    out << "GET key9517 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48242: get
  status = db->Get(read_options, "key3646", &value);
  if (status.ok()) {
    out << "GET key3646 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48243: get
  status = db->Get(read_options, "key6410", &value);
  if (status.ok()) {
    out << "GET key6410 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48244: put
  db->Put(write_options, "key3466", "value76210");

  // op 48245: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1020", "key11078", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1020, key11078) size=" << scan_result.size() << std::endl;
  }

  // op 48246: put
  db->Put(write_options, "key10463", "value5621");

  // op 48247: put
  db->Put(write_options, "key3009", "value60815");

  // op 48248: put
  db->Put(write_options, "key3958", "value70261");

  // op 48249: put
  db->Put(write_options, "key7822", "value105790");

  // op 48250: get
  status = db->Get(read_options, "key3245", &value);
  if (status.ok()) {
    out << "GET key3245 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48251: get
  status = db->Get(read_options, "key11063", &value);
  if (status.ok()) {
    out << "GET key11063 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48252: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2161", "key9508", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2161, key9508) size=" << scan_result.size() << std::endl;
  }

  // op 48253: put
  db->Put(write_options, "key9428", "value99069");

  // op 48254: put
  db->Put(write_options, "key7992", "value8101");

  // op 48255: put
  db->Put(write_options, "key2750", "value9782");

  // op 48256: put
  db->Put(write_options, "key7461", "value42529");

  // op 48257: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8886", "key10992", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8886, key10992) size=" << scan_result.size() << std::endl;
  }

  // op 48258: delete_range
  status = db->DeleteRange(write_options, "key3336", "key5065");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 48259: delete_range
  status = db->DeleteRange(write_options, "key3416", "key9844");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 48260: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1927", "key4613", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1927, key4613) size=" << scan_result.size() << std::endl;
  }

  // op 48261: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 48262: put
  db->Put(write_options, "key9541", "value39983");

  // op 48263: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 48264: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7114", "key7297", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7114, key7297) size=" << scan_result.size() << std::endl;
  }

  // op 48265: put
  db->Put(write_options, "key3344", "value31189");

  // op 48266: put
  db->Put(write_options, "key0875", "value104915");

  // op 48267: delete
  db->Delete(write_options, "key0752");

  // op 48268: get
  status = db->Get(read_options, "key5615", &value);
  if (status.ok()) {
    out << "GET key5615 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48269: put
  db->Put(write_options, "key4306", "value16612");

  // op 48270: put
  db->Put(write_options, "key9314", "value23456");

  // op 48271: put
  db->Put(write_options, "key11441", "value7091");

  // op 48272: delete_range
  status = db->DeleteRange(write_options, "key8178", "key11168");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 48273: put
  db->Put(write_options, "key8377", "value46075");

  // op 48274: get
  status = db->Get(read_options, "key6054", &value);
  if (status.ok()) {
    out << "GET key6054 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48275: get
  status = db->Get(read_options, "key9446", &value);
  if (status.ok()) {
    out << "GET key9446 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48276: put
  db->Put(write_options, "key3429", "value116614");

  // op 48277: get
  status = db->Get(read_options, "key6153", &value);
  if (status.ok()) {
    out << "GET key6153 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48278: get
  status = db->Get(read_options, "key4210", &value);
  if (status.ok()) {
    out << "GET key4210 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48279: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1726", "key4098", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1726, key4098) size=" << scan_result.size() << std::endl;
  }

  // op 48280: get
  status = db->Get(read_options, "key10813", &value);
  if (status.ok()) {
    out << "GET key10813 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48281: delete_range
  status = db->DeleteRange(write_options, "key9437", "key11244");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 48282: get
  status = db->Get(read_options, "key8876", &value);
  if (status.ok()) {
    out << "GET key8876 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48283: put
  db->Put(write_options, "key1597", "value23583");

  // op 48284: get
  status = db->Get(read_options, "key7113", &value);
  if (status.ok()) {
    out << "GET key7113 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48285: put
  db->Put(write_options, "key4956", "value40477");

  // op 48286: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10911", "key11300", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10911, key11300) size=" << scan_result.size() << std::endl;
  }

  // op 48287: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6577", "key6660", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6577, key6660) size=" << scan_result.size() << std::endl;
  }

  // op 48288: delete
  db->Delete(write_options, "key8330");

  // op 48289: put
  db->Put(write_options, "key7344", "value75710");

  // op 48290: put
  db->Put(write_options, "key3658", "value50399");

  // op 48291: put
  db->Put(write_options, "key1005", "value75092");

  // op 48292: get
  status = db->Get(read_options, "key4474", &value);
  if (status.ok()) {
    out << "GET key4474 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48293: put
  db->Put(write_options, "key4801", "value4893");

  // op 48294: put
  db->Put(write_options, "key7654", "value97706");

  // op 48295: put
  db->Put(write_options, "key3030", "value39228");

  // op 48296: get
  status = db->Get(read_options, "key8691", &value);
  if (status.ok()) {
    out << "GET key8691 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48297: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1765", "key8143", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1765, key8143) size=" << scan_result.size() << std::endl;
  }

  // op 48298: put
  db->Put(write_options, "key4615", "value101964");

  // op 48299: delete_range
  status = db->DeleteRange(write_options, "key3970", "key11129");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 48300: get
  status = db->Get(read_options, "key3608", &value);
  if (status.ok()) {
    out << "GET key3608 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48301: put
  db->Put(write_options, "key3278", "value106065");

  // op 48302: put
  db->Put(write_options, "key6193", "value40985");

  // op 48303: put
  db->Put(write_options, "key4678", "value21147");

  // op 48304: put
  db->Put(write_options, "key7953", "value32740");

  // op 48305: get
  status = db->Get(read_options, "key10679", &value);
  if (status.ok()) {
    out << "GET key10679 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48306: put
  db->Put(write_options, "key0655", "value21500");

  // op 48307: put
  db->Put(write_options, "key8479", "value73245");

  // op 48308: get
  status = db->Get(read_options, "key11547", &value);
  if (status.ok()) {
    out << "GET key11547 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48309: put
  db->Put(write_options, "key5782", "value4627");

  // op 48310: delete
  db->Delete(write_options, "key7481");

  // op 48311: get
  status = db->Get(read_options, "key5115", &value);
  if (status.ok()) {
    out << "GET key5115 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48312: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3336", "key9514", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3336, key9514) size=" << scan_result.size() << std::endl;
  }

  // op 48313: get
  status = db->Get(read_options, "key3690", &value);
  if (status.ok()) {
    out << "GET key3690 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48314: delete_range
  status = db->DeleteRange(write_options, "key0178", "key11400");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 48315: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2922", "key6309", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2922, key6309) size=" << scan_result.size() << std::endl;
  }

  // op 48316: put
  db->Put(write_options, "key10906", "value54585");

  // op 48317: get
  status = db->Get(read_options, "key0396", &value);
  if (status.ok()) {
    out << "GET key0396 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48318: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1517", "key9633", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1517, key9633) size=" << scan_result.size() << std::endl;
  }

  // op 48319: delete
  db->Delete(write_options, "key8806");

  // op 48320: put
  db->Put(write_options, "key6415", "value71727");

  // op 48321: put
  db->Put(write_options, "key2208", "value17887");

  // op 48322: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2753", "key11066", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2753, key11066) size=" << scan_result.size() << std::endl;
  }

  // op 48323: put
  db->Put(write_options, "key5352", "value50261");

  // op 48324: put
  db->Put(write_options, "key3092", "value108176");

  // op 48325: put
  db->Put(write_options, "key9009", "value59361");

  // op 48326: put
  db->Put(write_options, "key9691", "value54139");

  // op 48327: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 48328: get
  status = db->Get(read_options, "key3148", &value);
  if (status.ok()) {
    out << "GET key3148 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48329: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0960", "key10800", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0960, key10800) size=" << scan_result.size() << std::endl;
  }

  // op 48330: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 48331: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1015", "key11005", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1015, key11005) size=" << scan_result.size() << std::endl;
  }

  // op 48332: delete_range
  status = db->DeleteRange(write_options, "key3067", "key7622");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 48333: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1683", "key9036", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1683, key9036) size=" << scan_result.size() << std::endl;
  }

  // op 48334: put
  db->Put(write_options, "key11564", "value6265");

  // op 48335: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 48336: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6339", "key8410", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6339, key8410) size=" << scan_result.size() << std::endl;
  }

  // op 48337: get
  status = db->Get(read_options, "key1081", &value);
  if (status.ok()) {
    out << "GET key1081 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48338: delete
  db->Delete(write_options, "key2051");

  // op 48339: put
  db->Put(write_options, "key2448", "value45540");

  // op 48340: delete
  db->Delete(write_options, "key5832");

  // op 48341: put
  db->Put(write_options, "key2318", "value67493");

  // op 48342: get
  status = db->Get(read_options, "key3695", &value);
  if (status.ok()) {
    out << "GET key3695 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48343: put
  db->Put(write_options, "key8034", "value101274");

  // op 48344: get
  status = db->Get(read_options, "key9519", &value);
  if (status.ok()) {
    out << "GET key9519 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48345: put
  db->Put(write_options, "key6988", "value114910");

  // op 48346: put
  db->Put(write_options, "key11564", "value35850");

  // op 48347: get
  status = db->Get(read_options, "key10572", &value);
  if (status.ok()) {
    out << "GET key10572 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48348: delete_range
  status = db->DeleteRange(write_options, "key1312", "key8493");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 48349: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6813", "key9782", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6813, key9782) size=" << scan_result.size() << std::endl;
  }

  // op 48350: delete_range
  status = db->DeleteRange(write_options, "key9222", "key10421");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 48351: put
  db->Put(write_options, "key6259", "value23686");

  // op 48352: delete_range
  status = db->DeleteRange(write_options, "key1001", "key6285");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 48353: get
  status = db->Get(read_options, "key1972", &value);
  if (status.ok()) {
    out << "GET key1972 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48354: put
  db->Put(write_options, "key10851", "value11331");

  // op 48355: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9779", "key11003", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9779, key11003) size=" << scan_result.size() << std::endl;
  }

  // op 48356: put
  db->Put(write_options, "key5827", "value115823");

  // op 48357: put
  db->Put(write_options, "key11423", "value84829");

  // op 48358: put
  db->Put(write_options, "key3269", "value74089");

  // op 48359: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0640", "key6023", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0640, key6023) size=" << scan_result.size() << std::endl;
  }

  // op 48360: put
  db->Put(write_options, "key11518", "value98668");

  // op 48361: get
  status = db->Get(read_options, "key4905", &value);
  if (status.ok()) {
    out << "GET key4905 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48362: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 48363: put
  db->Put(write_options, "key2735", "value76012");

  // op 48364: get
  status = db->Get(read_options, "key8200", &value);
  if (status.ok()) {
    out << "GET key8200 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48365: delete_range
  status = db->DeleteRange(write_options, "key4618", "key7173");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 48366: put
  db->Put(write_options, "key8039", "value0029");

  // op 48367: delete
  db->Delete(write_options, "key6563");

  // op 48368: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0081", "key1934", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0081, key1934) size=" << scan_result.size() << std::endl;
  }

  // op 48369: put
  db->Put(write_options, "key6283", "value33788");

  // op 48370: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3037", "key8095", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3037, key8095) size=" << scan_result.size() << std::endl;
  }

  // op 48371: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 48372: put
  db->Put(write_options, "key7564", "value78835");

  // op 48373: put
  db->Put(write_options, "key4489", "value73357");

  // op 48374: put
  db->Put(write_options, "key7682", "value26059");

  // op 48375: delete
  db->Delete(write_options, "key1183");

  // op 48376: put
  db->Put(write_options, "key8338", "value13259");

  // op 48377: delete_range
  status = db->DeleteRange(write_options, "key0572", "key10200");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 48378: delete
  db->Delete(write_options, "key10733");

  // op 48379: delete_range
  status = db->DeleteRange(write_options, "key5439", "key9203");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 48380: get
  status = db->Get(read_options, "key7309", &value);
  if (status.ok()) {
    out << "GET key7309 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48381: put
  db->Put(write_options, "key5497", "value117172");

  // op 48382: put
  db->Put(write_options, "key0147", "value23145");

  // op 48383: get
  status = db->Get(read_options, "key10699", &value);
  if (status.ok()) {
    out << "GET key10699 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48384: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3794", "key7424", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3794, key7424) size=" << scan_result.size() << std::endl;
  }

  // op 48385: put
  db->Put(write_options, "key7772", "value29980");

  // op 48386: put
  db->Put(write_options, "key0397", "value53949");

  // op 48387: get
  status = db->Get(read_options, "key4010", &value);
  if (status.ok()) {
    out << "GET key4010 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48388: get
  status = db->Get(read_options, "key6340", &value);
  if (status.ok()) {
    out << "GET key6340 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48389: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0648", "key5749", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0648, key5749) size=" << scan_result.size() << std::endl;
  }

  // op 48390: get
  status = db->Get(read_options, "key9553", &value);
  if (status.ok()) {
    out << "GET key9553 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48391: delete_range
  status = db->DeleteRange(write_options, "key3685", "key9929");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 48392: put
  db->Put(write_options, "key2204", "value37926");

  // op 48393: put
  db->Put(write_options, "key2947", "value68884");

  // op 48394: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2554", "key5962", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2554, key5962) size=" << scan_result.size() << std::endl;
  }

  // op 48395: put
  db->Put(write_options, "key5661", "value57617");

  // op 48396: put
  db->Put(write_options, "key4291", "value97243");

  // op 48397: put
  db->Put(write_options, "key11512", "value26711");

  // op 48398: put
  db->Put(write_options, "key10398", "value46455");

  // op 48399: put
  db->Put(write_options, "key3869", "value38024");

  // op 48400: put
  db->Put(write_options, "key11639", "value112651");

  // op 48401: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5436", "key7813", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5436, key7813) size=" << scan_result.size() << std::endl;
  }

  // op 48402: put
  db->Put(write_options, "key2831", "value14865");

  // op 48403: put
  db->Put(write_options, "key9634", "value87644");

  // op 48404: put
  db->Put(write_options, "key8818", "value37884");

  // op 48405: delete_range
  status = db->DeleteRange(write_options, "key1290", "key10689");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 48406: put
  db->Put(write_options, "key0142", "value84995");

  // op 48407: put
  db->Put(write_options, "key1175", "value32412");

  // op 48408: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 48409: put
  db->Put(write_options, "key8185", "value24759");

  // op 48410: put
  db->Put(write_options, "key9468", "value74582");

  // op 48411: put
  db->Put(write_options, "key0892", "value84309");

  // op 48412: put
  db->Put(write_options, "key11606", "value36367");

  // op 48413: get
  status = db->Get(read_options, "key5445", &value);
  if (status.ok()) {
    out << "GET key5445 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48414: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1972", "key6833", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1972, key6833) size=" << scan_result.size() << std::endl;
  }

  // op 48415: delete
  db->Delete(write_options, "key2125");

  // op 48416: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 48417: get
  status = db->Get(read_options, "key9988", &value);
  if (status.ok()) {
    out << "GET key9988 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48418: delete
  db->Delete(write_options, "key1025");

  // op 48419: get
  status = db->Get(read_options, "key7290", &value);
  if (status.ok()) {
    out << "GET key7290 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48420: delete
  db->Delete(write_options, "key4012");

  // op 48421: delete_range
  status = db->DeleteRange(write_options, "key0242", "key6595");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 48422: get
  status = db->Get(read_options, "key4961", &value);
  if (status.ok()) {
    out << "GET key4961 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48423: put
  db->Put(write_options, "key3194", "value24263");

  // op 48424: delete
  db->Delete(write_options, "key9532");

  // op 48425: put
  db->Put(write_options, "key5851", "value102024");

  // op 48426: delete_range
  status = db->DeleteRange(write_options, "key3721", "key4765");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 48427: put
  db->Put(write_options, "key8624", "value109472");

  // op 48428: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6114", "key8115", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6114, key8115) size=" << scan_result.size() << std::endl;
  }

  // op 48429: put
  db->Put(write_options, "key10158", "value101119");

  // op 48430: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 48431: put
  db->Put(write_options, "key1365", "value28940");

  // op 48432: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7237", "key9347", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7237, key9347) size=" << scan_result.size() << std::endl;
  }

  // op 48433: get
  status = db->Get(read_options, "key10577", &value);
  if (status.ok()) {
    out << "GET key10577 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48434: get
  status = db->Get(read_options, "key6367", &value);
  if (status.ok()) {
    out << "GET key6367 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48435: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0630", "key3253", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0630, key3253) size=" << scan_result.size() << std::endl;
  }

  // op 48436: get
  status = db->Get(read_options, "key9223", &value);
  if (status.ok()) {
    out << "GET key9223 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48437: delete_range
  status = db->DeleteRange(write_options, "key5916", "key7856");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 48438: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1036", "key10892", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1036, key10892) size=" << scan_result.size() << std::endl;
  }

  // op 48439: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1223", "key11451", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1223, key11451) size=" << scan_result.size() << std::endl;
  }

  // op 48440: get
  status = db->Get(read_options, "key2142", &value);
  if (status.ok()) {
    out << "GET key2142 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48441: get
  status = db->Get(read_options, "key7145", &value);
  if (status.ok()) {
    out << "GET key7145 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48442: delete
  db->Delete(write_options, "key3480");

  // op 48443: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3027", "key3200", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3027, key3200) size=" << scan_result.size() << std::endl;
  }

  // op 48444: put
  db->Put(write_options, "key6858", "value55949");

  // op 48445: put
  db->Put(write_options, "key3394", "value51211");

  // op 48446: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4706", "key10697", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4706, key10697) size=" << scan_result.size() << std::endl;
  }

  // op 48447: put
  db->Put(write_options, "key8345", "value29577");

  // op 48448: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 48449: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4504", "key5144", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4504, key5144) size=" << scan_result.size() << std::endl;
  }

  // op 48450: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5321", "key8497", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5321, key8497) size=" << scan_result.size() << std::endl;
  }

  // op 48451: get
  status = db->Get(read_options, "key9696", &value);
  if (status.ok()) {
    out << "GET key9696 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48452: delete
  db->Delete(write_options, "key4784");

  // op 48453: put
  db->Put(write_options, "key9421", "value12437");

  // op 48454: delete_range
  status = db->DeleteRange(write_options, "key3300", "key7127");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 48455: put
  db->Put(write_options, "key6065", "value47111");

  // op 48456: delete
  db->Delete(write_options, "key1078");

  // op 48457: put
  db->Put(write_options, "key5927", "value39423");

  // op 48458: get
  status = db->Get(read_options, "key2403", &value);
  if (status.ok()) {
    out << "GET key2403 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48459: put
  db->Put(write_options, "key6130", "value25772");

  // op 48460: delete
  db->Delete(write_options, "key5487");

  // op 48461: delete_range
  status = db->DeleteRange(write_options, "key4497", "key5291");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 48462: delete
  db->Delete(write_options, "key8310");

  // op 48463: put
  db->Put(write_options, "key11203", "value115663");

  // op 48464: get
  status = db->Get(read_options, "key4381", &value);
  if (status.ok()) {
    out << "GET key4381 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48465: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3331", "key3436", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3331, key3436) size=" << scan_result.size() << std::endl;
  }

  // op 48466: put
  db->Put(write_options, "key1001", "value10664");

  // op 48467: put
  db->Put(write_options, "key0451", "value73364");

  // op 48468: put
  db->Put(write_options, "key8586", "value46068");

  // op 48469: delete
  db->Delete(write_options, "key5390");

  // op 48470: delete
  db->Delete(write_options, "key2949");

  // op 48471: put
  db->Put(write_options, "key7517", "value106976");

  // op 48472: get
  status = db->Get(read_options, "key1391", &value);
  if (status.ok()) {
    out << "GET key1391 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48473: get
  status = db->Get(read_options, "key7691", &value);
  if (status.ok()) {
    out << "GET key7691 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48474: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0870", "key6220", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0870, key6220) size=" << scan_result.size() << std::endl;
  }

  // op 48475: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3093", "key5138", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3093, key5138) size=" << scan_result.size() << std::endl;
  }

  // op 48476: delete_range
  status = db->DeleteRange(write_options, "key3508", "key7385");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 48477: put
  db->Put(write_options, "key11423", "value113350");

  // op 48478: put
  db->Put(write_options, "key7231", "value14741");

  // op 48479: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6116", "key9198", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6116, key9198) size=" << scan_result.size() << std::endl;
  }

  // op 48480: delete_range
  status = db->DeleteRange(write_options, "key0002", "key10398");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 48481: get
  status = db->Get(read_options, "key4248", &value);
  if (status.ok()) {
    out << "GET key4248 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48482: put
  db->Put(write_options, "key3396", "value24691");

  // op 48483: get
  status = db->Get(read_options, "key5999", &value);
  if (status.ok()) {
    out << "GET key5999 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48484: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1670", "key6740", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1670, key6740) size=" << scan_result.size() << std::endl;
  }

  // op 48485: put
  db->Put(write_options, "key1380", "value69962");

  // op 48486: put
  db->Put(write_options, "key4495", "value31579");

  // op 48487: get
  status = db->Get(read_options, "key8708", &value);
  if (status.ok()) {
    out << "GET key8708 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48488: get
  status = db->Get(read_options, "key10155", &value);
  if (status.ok()) {
    out << "GET key10155 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48489: get
  status = db->Get(read_options, "key5889", &value);
  if (status.ok()) {
    out << "GET key5889 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48490: get
  status = db->Get(read_options, "key8300", &value);
  if (status.ok()) {
    out << "GET key8300 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48491: put
  db->Put(write_options, "key2576", "value41772");

  // op 48492: delete_range
  status = db->DeleteRange(write_options, "key3648", "key6250");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 48493: delete_range
  status = db->DeleteRange(write_options, "key4446", "key4653");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 48494: get
  status = db->Get(read_options, "key3964", &value);
  if (status.ok()) {
    out << "GET key3964 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48495: get
  status = db->Get(read_options, "key9022", &value);
  if (status.ok()) {
    out << "GET key9022 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48496: put
  db->Put(write_options, "key0923", "value94703");

  // op 48497: put
  db->Put(write_options, "key4283", "value73002");

  // op 48498: delete_range
  status = db->DeleteRange(write_options, "key5188", "key7688");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 48499: put
  db->Put(write_options, "key2715", "value62939");

  // op 48500: get
  status = db->Get(read_options, "key3844", &value);
  if (status.ok()) {
    out << "GET key3844 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48501: put
  db->Put(write_options, "key4498", "value3444");

  // op 48502: put
  db->Put(write_options, "key5433", "value82079");

  // op 48503: put
  db->Put(write_options, "key2376", "value56275");

  // op 48504: get
  status = db->Get(read_options, "key1152", &value);
  if (status.ok()) {
    out << "GET key1152 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48505: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2586", "key5906", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2586, key5906) size=" << scan_result.size() << std::endl;
  }

  // op 48506: put
  db->Put(write_options, "key5142", "value72303");

  // op 48507: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8382", "key8934", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8382, key8934) size=" << scan_result.size() << std::endl;
  }

  // op 48508: put
  db->Put(write_options, "key5846", "value117912");

  // op 48509: put
  db->Put(write_options, "key8724", "value70562");

  // op 48510: put
  db->Put(write_options, "key11401", "value67053");

  // op 48511: get
  status = db->Get(read_options, "key11678", &value);
  if (status.ok()) {
    out << "GET key11678 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48512: put
  db->Put(write_options, "key11291", "value81522");

  // op 48513: put
  db->Put(write_options, "key2870", "value99464");

  // op 48514: put
  db->Put(write_options, "key9824", "value98914");

  // op 48515: put
  db->Put(write_options, "key2222", "value62235");

  // op 48516: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 48517: put
  db->Put(write_options, "key3975", "value103616");

  // op 48518: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7510", "key9242", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7510, key9242) size=" << scan_result.size() << std::endl;
  }

  // op 48519: delete
  db->Delete(write_options, "key1000");

  // op 48520: put
  db->Put(write_options, "key9953", "value95134");

  // op 48521: put
  db->Put(write_options, "key7299", "value18371");

  // op 48522: put
  db->Put(write_options, "key9681", "value106102");

  // op 48523: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5449", "key5695", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5449, key5695) size=" << scan_result.size() << std::endl;
  }

  // op 48524: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 48525: get
  status = db->Get(read_options, "key7967", &value);
  if (status.ok()) {
    out << "GET key7967 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48526: get
  status = db->Get(read_options, "key7101", &value);
  if (status.ok()) {
    out << "GET key7101 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48527: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5532", "key6503", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5532, key6503) size=" << scan_result.size() << std::endl;
  }

  // op 48528: put
  db->Put(write_options, "key7578", "value0314");

  // op 48529: get
  status = db->Get(read_options, "key10490", &value);
  if (status.ok()) {
    out << "GET key10490 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48530: put
  db->Put(write_options, "key2880", "value85400");

  // op 48531: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6871", "key7722", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6871, key7722) size=" << scan_result.size() << std::endl;
  }

  // op 48532: get
  status = db->Get(read_options, "key3406", &value);
  if (status.ok()) {
    out << "GET key3406 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48533: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 48534: delete
  db->Delete(write_options, "key11674");

  // op 48535: delete_range
  status = db->DeleteRange(write_options, "key4045", "key9776");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 48536: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 48537: get
  status = db->Get(read_options, "key10918", &value);
  if (status.ok()) {
    out << "GET key10918 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48538: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1520", "key4665", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1520, key4665) size=" << scan_result.size() << std::endl;
  }

  // op 48539: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4216", "key10209", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4216, key10209) size=" << scan_result.size() << std::endl;
  }

  // op 48540: delete_range
  status = db->DeleteRange(write_options, "key1758", "key4263");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 48541: delete
  db->Delete(write_options, "key8835");

  // op 48542: delete
  db->Delete(write_options, "key4645");

  // op 48543: delete
  db->Delete(write_options, "key10144");

  // op 48544: get
  status = db->Get(read_options, "key4865", &value);
  if (status.ok()) {
    out << "GET key4865 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48545: delete_range
  status = db->DeleteRange(write_options, "key7938", "key10386");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 48546: delete
  db->Delete(write_options, "key2399");

  // op 48547: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1829", "key5926", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1829, key5926) size=" << scan_result.size() << std::endl;
  }

  // op 48548: delete
  db->Delete(write_options, "key0995");

  // op 48549: put
  db->Put(write_options, "key9370", "value80797");

  // op 48550: put
  db->Put(write_options, "key3387", "value116051");

  // op 48551: put
  db->Put(write_options, "key7972", "value62627");

  // op 48552: put
  db->Put(write_options, "key4168", "value41149");

  // op 48553: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2708", "key7687", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2708, key7687) size=" << scan_result.size() << std::endl;
  }

  // op 48554: put
  db->Put(write_options, "key9642", "value114334");

  // op 48555: delete
  db->Delete(write_options, "key9457");

  // op 48556: put
  db->Put(write_options, "key8234", "value8450");

  // op 48557: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 48558: put
  db->Put(write_options, "key5736", "value90150");

  // op 48559: delete_range
  status = db->DeleteRange(write_options, "key0038", "key4689");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 48560: put
  db->Put(write_options, "key4176", "value81818");

  // op 48561: put
  db->Put(write_options, "key2964", "value79359");

  // op 48562: get
  status = db->Get(read_options, "key3426", &value);
  if (status.ok()) {
    out << "GET key3426 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48563: put
  db->Put(write_options, "key6126", "value46084");

  // op 48564: get
  status = db->Get(read_options, "key6339", &value);
  if (status.ok()) {
    out << "GET key6339 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48565: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4180", "key4343", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4180, key4343) size=" << scan_result.size() << std::endl;
  }

  // op 48566: delete
  db->Delete(write_options, "key6105");

  // op 48567: delete
  db->Delete(write_options, "key6130");

  // op 48568: delete
  db->Delete(write_options, "key5951");

  // op 48569: get
  status = db->Get(read_options, "key10460", &value);
  if (status.ok()) {
    out << "GET key10460 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48570: put
  db->Put(write_options, "key8190", "value27268");

  // op 48571: get
  status = db->Get(read_options, "key0726", &value);
  if (status.ok()) {
    out << "GET key0726 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48572: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1403", "key4200", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1403, key4200) size=" << scan_result.size() << std::endl;
  }

  // op 48573: get
  status = db->Get(read_options, "key7832", &value);
  if (status.ok()) {
    out << "GET key7832 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48574: get
  status = db->Get(read_options, "key7131", &value);
  if (status.ok()) {
    out << "GET key7131 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48575: put
  db->Put(write_options, "key8330", "value77038");

  // op 48576: put
  db->Put(write_options, "key9004", "value111114");

  // op 48577: put
  db->Put(write_options, "key3218", "value54870");

  // op 48578: put
  db->Put(write_options, "key6862", "value33588");

  // op 48579: get
  status = db->Get(read_options, "key3585", &value);
  if (status.ok()) {
    out << "GET key3585 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48580: put
  db->Put(write_options, "key3092", "value87028");

  // op 48581: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 48582: delete
  db->Delete(write_options, "key6550");

  // op 48583: delete_range
  status = db->DeleteRange(write_options, "key7662", "key8565");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 48584: delete_range
  status = db->DeleteRange(write_options, "key4781", "key9834");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 48585: delete
  db->Delete(write_options, "key4634");

  // op 48586: delete_range
  status = db->DeleteRange(write_options, "key5494", "key7921");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 48587: delete_range
  status = db->DeleteRange(write_options, "key0719", "key9181");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 48588: delete_range
  status = db->DeleteRange(write_options, "key0896", "key8179");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 48589: delete_range
  status = db->DeleteRange(write_options, "key3889", "key4274");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 48590: get
  status = db->Get(read_options, "key5081", &value);
  if (status.ok()) {
    out << "GET key5081 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48591: put
  db->Put(write_options, "key10906", "value85717");

  // op 48592: put
  db->Put(write_options, "key4079", "value111784");

  // op 48593: get
  status = db->Get(read_options, "key4765", &value);
  if (status.ok()) {
    out << "GET key4765 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48594: put
  db->Put(write_options, "key0546", "value1617");

  // op 48595: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1418", "key3228", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1418, key3228) size=" << scan_result.size() << std::endl;
  }

  // op 48596: delete_range
  status = db->DeleteRange(write_options, "key9053", "key10644");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 48597: put
  db->Put(write_options, "key1000", "value37339");

  // op 48598: get
  status = db->Get(read_options, "key3697", &value);
  if (status.ok()) {
    out << "GET key3697 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48599: get
  status = db->Get(read_options, "key1964", &value);
  if (status.ok()) {
    out << "GET key1964 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48600: get
  status = db->Get(read_options, "key11575", &value);
  if (status.ok()) {
    out << "GET key11575 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48601: put
  db->Put(write_options, "key6563", "value4948");

  // op 48602: put
  db->Put(write_options, "key3850", "value92340");

  // op 48603: delete
  db->Delete(write_options, "key2456");

  // op 48604: delete
  db->Delete(write_options, "key7366");

  // op 48605: put
  db->Put(write_options, "key2838", "value73015");

  // op 48606: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0744", "key5747", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0744, key5747) size=" << scan_result.size() << std::endl;
  }

  // op 48607: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5199", "key11377", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5199, key11377) size=" << scan_result.size() << std::endl;
  }

  // op 48608: put
  db->Put(write_options, "key11499", "value110888");

  // op 48609: put
  db->Put(write_options, "key5515", "value29033");

  // op 48610: put
  db->Put(write_options, "key11604", "value51332");

  // op 48611: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2013", "key2862", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2013, key2862) size=" << scan_result.size() << std::endl;
  }

  // op 48612: delete_range
  status = db->DeleteRange(write_options, "key4360", "key7273");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 48613: put
  db->Put(write_options, "key2703", "value34949");

  // op 48614: put
  db->Put(write_options, "key1927", "value82259");

  // op 48615: get
  status = db->Get(read_options, "key6360", &value);
  if (status.ok()) {
    out << "GET key6360 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48616: get
  status = db->Get(read_options, "key11692", &value);
  if (status.ok()) {
    out << "GET key11692 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48617: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2409", "key3743", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2409, key3743) size=" << scan_result.size() << std::endl;
  }

  // op 48618: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3410", "key4726", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3410, key4726) size=" << scan_result.size() << std::endl;
  }

  // op 48619: put
  db->Put(write_options, "key1956", "value9201");

  // op 48620: get
  status = db->Get(read_options, "key8254", &value);
  if (status.ok()) {
    out << "GET key8254 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48621: put
  db->Put(write_options, "key8272", "value25546");

  // op 48622: delete
  db->Delete(write_options, "key3332");

  // op 48623: delete
  db->Delete(write_options, "key0505");

  // op 48624: put
  db->Put(write_options, "key7322", "value2838");

  // op 48625: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0687", "key8433", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0687, key8433) size=" << scan_result.size() << std::endl;
  }

  // op 48626: get
  status = db->Get(read_options, "key7457", &value);
  if (status.ok()) {
    out << "GET key7457 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48627: put
  db->Put(write_options, "key5642", "value53023");

  // op 48628: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6683", "key6720", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6683, key6720) size=" << scan_result.size() << std::endl;
  }

  // op 48629: get
  status = db->Get(read_options, "key3372", &value);
  if (status.ok()) {
    out << "GET key3372 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48630: get
  status = db->Get(read_options, "key9540", &value);
  if (status.ok()) {
    out << "GET key9540 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48631: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0192", "key1505", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0192, key1505) size=" << scan_result.size() << std::endl;
  }

  // op 48632: put
  db->Put(write_options, "key8123", "value104093");

  // op 48633: delete_range
  status = db->DeleteRange(write_options, "key2019", "key9214");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 48634: put
  db->Put(write_options, "key1794", "value61708");

  // op 48635: delete
  db->Delete(write_options, "key0424");

  // op 48636: put
  db->Put(write_options, "key7175", "value95613");

  // op 48637: delete
  db->Delete(write_options, "key1416");

  // op 48638: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1760", "key3420", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1760, key3420) size=" << scan_result.size() << std::endl;
  }

  // op 48639: delete_range
  status = db->DeleteRange(write_options, "key3836", "key7172");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 48640: get
  status = db->Get(read_options, "key1879", &value);
  if (status.ok()) {
    out << "GET key1879 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48641: put
  db->Put(write_options, "key8102", "value28147");

  // op 48642: put
  db->Put(write_options, "key3204", "value116322");

  // op 48643: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 48644: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4438", "key8121", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4438, key8121) size=" << scan_result.size() << std::endl;
  }

  // op 48645: get
  status = db->Get(read_options, "key9775", &value);
  if (status.ok()) {
    out << "GET key9775 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48646: get
  status = db->Get(read_options, "key11302", &value);
  if (status.ok()) {
    out << "GET key11302 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48647: put
  db->Put(write_options, "key6997", "value98443");

  // op 48648: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2351", "key8930", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2351, key8930) size=" << scan_result.size() << std::endl;
  }

  // op 48649: put
  db->Put(write_options, "key9621", "value35945");

  // op 48650: delete_range
  status = db->DeleteRange(write_options, "key6142", "key8145");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 48651: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7566", "key8216", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7566, key8216) size=" << scan_result.size() << std::endl;
  }

  // op 48652: put
  db->Put(write_options, "key7138", "value19744");

  // op 48653: put
  db->Put(write_options, "key10814", "value99316");

  // op 48654: get
  status = db->Get(read_options, "key2309", &value);
  if (status.ok()) {
    out << "GET key2309 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48655: delete
  db->Delete(write_options, "key8693");

  // op 48656: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1997", "key10082", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1997, key10082) size=" << scan_result.size() << std::endl;
  }

  // op 48657: delete
  db->Delete(write_options, "key4936");

  // op 48658: delete_range
  status = db->DeleteRange(write_options, "key0471", "key3008");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 48659: delete_range
  status = db->DeleteRange(write_options, "key2379", "key8224");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 48660: put
  db->Put(write_options, "key11638", "value0446");

  // op 48661: put
  db->Put(write_options, "key1358", "value25596");

  // op 48662: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5215", "key11336", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5215, key11336) size=" << scan_result.size() << std::endl;
  }

  // op 48663: put
  db->Put(write_options, "key2568", "value38845");

  // op 48664: get
  status = db->Get(read_options, "key1601", &value);
  if (status.ok()) {
    out << "GET key1601 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48665: delete_range
  status = db->DeleteRange(write_options, "key7824", "key11423");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 48666: put
  db->Put(write_options, "key0707", "value10664");

  // op 48667: get
  status = db->Get(read_options, "key9630", &value);
  if (status.ok()) {
    out << "GET key9630 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48668: delete_range
  status = db->DeleteRange(write_options, "key4330", "key5629");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 48669: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3328", "key8207", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3328, key8207) size=" << scan_result.size() << std::endl;
  }

  // op 48670: get
  status = db->Get(read_options, "key1544", &value);
  if (status.ok()) {
    out << "GET key1544 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48671: put
  db->Put(write_options, "key9840", "value28165");

  // op 48672: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8350", "key10802", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8350, key10802) size=" << scan_result.size() << std::endl;
  }

  // op 48673: put
  db->Put(write_options, "key3923", "value22422");

  // op 48674: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5409", "key7782", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5409, key7782) size=" << scan_result.size() << std::endl;
  }

  // op 48675: put
  db->Put(write_options, "key6490", "value70093");

  // op 48676: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 48677: put
  db->Put(write_options, "key5088", "value30399");

  // op 48678: put
  db->Put(write_options, "key11700", "value6915");

  // op 48679: put
  db->Put(write_options, "key6113", "value110224");

  // op 48680: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 48681: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 48682: put
  db->Put(write_options, "key4793", "value59384");

  // op 48683: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1258", "key11395", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1258, key11395) size=" << scan_result.size() << std::endl;
  }

  // op 48684: delete_range
  status = db->DeleteRange(write_options, "key2621", "key5206");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 48685: get
  status = db->Get(read_options, "key3552", &value);
  if (status.ok()) {
    out << "GET key3552 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48686: put
  db->Put(write_options, "key0241", "value98295");

  // op 48687: delete
  db->Delete(write_options, "key5025");

  // op 48688: delete
  db->Delete(write_options, "key9966");

  // op 48689: put
  db->Put(write_options, "key9512", "value68388");

  // op 48690: delete_range
  status = db->DeleteRange(write_options, "key7912", "key10135");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 48691: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0087", "key5048", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0087, key5048) size=" << scan_result.size() << std::endl;
  }

  // op 48692: put
  db->Put(write_options, "key6669", "value99274");

  // op 48693: put
  db->Put(write_options, "key3428", "value104507");

  // op 48694: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 48695: delete_range
  status = db->DeleteRange(write_options, "key4801", "key5460");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 48696: put
  db->Put(write_options, "key3031", "value57651");

  // op 48697: put
  db->Put(write_options, "key10534", "value28105");

  // op 48698: delete_range
  status = db->DeleteRange(write_options, "key0862", "key10383");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 48699: get
  status = db->Get(read_options, "key2807", &value);
  if (status.ok()) {
    out << "GET key2807 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48700: put
  db->Put(write_options, "key0810", "value57013");

  // op 48701: delete
  db->Delete(write_options, "key5568");

  // op 48702: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 48703: delete_range
  status = db->DeleteRange(write_options, "key1428", "key4336");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 48704: put
  db->Put(write_options, "key7782", "value24227");

  // op 48705: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 48706: put
  db->Put(write_options, "key2630", "value27365");

  // op 48707: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0811", "key5886", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0811, key5886) size=" << scan_result.size() << std::endl;
  }

  // op 48708: delete
  db->Delete(write_options, "key3431");

  // op 48709: put
  db->Put(write_options, "key4515", "value61484");

  // op 48710: put
  db->Put(write_options, "key4045", "value53480");

  // op 48711: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1761", "key5266", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1761, key5266) size=" << scan_result.size() << std::endl;
  }

  // op 48712: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5765", "key8405", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5765, key8405) size=" << scan_result.size() << std::endl;
  }

  // op 48713: delete_range
  status = db->DeleteRange(write_options, "key3420", "key6511");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 48714: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0639", "key7043", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0639, key7043) size=" << scan_result.size() << std::endl;
  }

  // op 48715: put
  db->Put(write_options, "key0989", "value104443");

  // op 48716: get
  status = db->Get(read_options, "key1731", &value);
  if (status.ok()) {
    out << "GET key1731 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48717: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6095", "key9249", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6095, key9249) size=" << scan_result.size() << std::endl;
  }

  // op 48718: get
  status = db->Get(read_options, "key1211", &value);
  if (status.ok()) {
    out << "GET key1211 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48719: get
  status = db->Get(read_options, "key8225", &value);
  if (status.ok()) {
    out << "GET key8225 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48720: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 48721: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2182", "key3753", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2182, key3753) size=" << scan_result.size() << std::endl;
  }

  // op 48722: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2639", "key8237", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2639, key8237) size=" << scan_result.size() << std::endl;
  }

  // op 48723: get
  status = db->Get(read_options, "key2515", &value);
  if (status.ok()) {
    out << "GET key2515 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48724: put
  db->Put(write_options, "key2306", "value92376");

  // op 48725: put
  db->Put(write_options, "key6674", "value22618");

  // op 48726: put
  db->Put(write_options, "key6651", "value114170");

  // op 48727: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9037", "key9206", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9037, key9206) size=" << scan_result.size() << std::endl;
  }

  // op 48728: put
  db->Put(write_options, "key9566", "value98135");

  // op 48729: delete_range
  status = db->DeleteRange(write_options, "key2579", "key3137");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 48730: put
  db->Put(write_options, "key5521", "value0761");

  // op 48731: put
  db->Put(write_options, "key2814", "value102176");

  // op 48732: put
  db->Put(write_options, "key0013", "value117316");

  // op 48733: delete_range
  status = db->DeleteRange(write_options, "key0473", "key6391");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 48734: delete_range
  status = db->DeleteRange(write_options, "key0800", "key5811");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 48735: get
  status = db->Get(read_options, "key5070", &value);
  if (status.ok()) {
    out << "GET key5070 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48736: delete_range
  status = db->DeleteRange(write_options, "key3892", "key4208");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 48737: delete
  db->Delete(write_options, "key5511");

  // op 48738: put
  db->Put(write_options, "key0215", "value88926");

  // op 48739: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 48740: delete_range
  status = db->DeleteRange(write_options, "key4361", "key4804");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 48741: put
  db->Put(write_options, "key4005", "value64321");

  // op 48742: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3810", "key10015", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3810, key10015) size=" << scan_result.size() << std::endl;
  }

  // op 48743: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 48744: delete_range
  status = db->DeleteRange(write_options, "key1614", "key4560");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 48745: put
  db->Put(write_options, "key8069", "value114269");

  // op 48746: put
  db->Put(write_options, "key8434", "value16766");

  // op 48747: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3725", "key6729", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3725, key6729) size=" << scan_result.size() << std::endl;
  }

  // op 48748: put
  db->Put(write_options, "key9607", "value21974");

  // op 48749: get
  status = db->Get(read_options, "key2738", &value);
  if (status.ok()) {
    out << "GET key2738 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48750: get
  status = db->Get(read_options, "key10062", &value);
  if (status.ok()) {
    out << "GET key10062 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48751: put
  db->Put(write_options, "key1438", "value8299");

  // op 48752: put
  db->Put(write_options, "key4832", "value21124");

  // op 48753: put
  db->Put(write_options, "key10863", "value54634");

  // op 48754: delete_range
  status = db->DeleteRange(write_options, "key10278", "key10502");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 48755: get
  status = db->Get(read_options, "key9955", &value);
  if (status.ok()) {
    out << "GET key9955 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48756: put
  db->Put(write_options, "key0095", "value82614");

  // op 48757: delete_range
  status = db->DeleteRange(write_options, "key3028", "key10326");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 48758: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1984", "key6351", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1984, key6351) size=" << scan_result.size() << std::endl;
  }

  // op 48759: put
  db->Put(write_options, "key6837", "value42141");

  // op 48760: get
  status = db->Get(read_options, "key8760", &value);
  if (status.ok()) {
    out << "GET key8760 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48761: delete_range
  status = db->DeleteRange(write_options, "key6287", "key6756");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 48762: put
  db->Put(write_options, "key10351", "value24530");

  // op 48763: delete
  db->Delete(write_options, "key3303");

  // op 48764: delete_range
  status = db->DeleteRange(write_options, "key1732", "key4779");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 48765: put
  db->Put(write_options, "key10394", "value51847");

  // op 48766: get
  status = db->Get(read_options, "key8427", &value);
  if (status.ok()) {
    out << "GET key8427 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48767: get
  status = db->Get(read_options, "key10735", &value);
  if (status.ok()) {
    out << "GET key10735 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48768: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0060", "key6881", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0060, key6881) size=" << scan_result.size() << std::endl;
  }

  // op 48769: put
  db->Put(write_options, "key0569", "value48972");

  // op 48770: put
  db->Put(write_options, "key6562", "value114891");

  // op 48771: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 48772: get
  status = db->Get(read_options, "key2150", &value);
  if (status.ok()) {
    out << "GET key2150 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48773: get
  status = db->Get(read_options, "key5805", &value);
  if (status.ok()) {
    out << "GET key5805 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48774: put
  db->Put(write_options, "key4277", "value73613");

  // op 48775: delete
  db->Delete(write_options, "key8392");

  // op 48776: put
  db->Put(write_options, "key5892", "value74664");

  // op 48777: delete
  db->Delete(write_options, "key8540");

  // op 48778: put
  db->Put(write_options, "key9752", "value6774");

  // op 48779: get
  status = db->Get(read_options, "key10262", &value);
  if (status.ok()) {
    out << "GET key10262 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48780: delete
  db->Delete(write_options, "key9509");

  // op 48781: put
  db->Put(write_options, "key5804", "value68988");

  // op 48782: put
  db->Put(write_options, "key6028", "value99575");

  // op 48783: get
  status = db->Get(read_options, "key0462", &value);
  if (status.ok()) {
    out << "GET key0462 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48784: delete_range
  status = db->DeleteRange(write_options, "key1128", "key6852");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 48785: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 48786: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3830", "key11527", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3830, key11527) size=" << scan_result.size() << std::endl;
  }

  // op 48787: delete_range
  status = db->DeleteRange(write_options, "key1661", "key8777");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 48788: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1995", "key11464", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1995, key11464) size=" << scan_result.size() << std::endl;
  }

  // op 48789: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3607", "key7611", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3607, key7611) size=" << scan_result.size() << std::endl;
  }

  // op 48790: put
  db->Put(write_options, "key6032", "value78545");

  // op 48791: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9038", "key10239", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9038, key10239) size=" << scan_result.size() << std::endl;
  }

  // op 48792: get
  status = db->Get(read_options, "key9030", &value);
  if (status.ok()) {
    out << "GET key9030 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48793: delete_range
  status = db->DeleteRange(write_options, "key7244", "key10224");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 48794: delete_range
  status = db->DeleteRange(write_options, "key5241", "key8901");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 48795: get
  status = db->Get(read_options, "key2736", &value);
  if (status.ok()) {
    out << "GET key2736 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48796: get
  status = db->Get(read_options, "key2143", &value);
  if (status.ok()) {
    out << "GET key2143 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48797: put
  db->Put(write_options, "key7845", "value59861");

  // op 48798: put
  db->Put(write_options, "key7249", "value35618");

  // op 48799: put
  db->Put(write_options, "key9481", "value69514");

  // op 48800: delete_range
  status = db->DeleteRange(write_options, "key10414", "key10443");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 48801: put
  db->Put(write_options, "key5584", "value5671");

  // op 48802: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3942", "key6719", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3942, key6719) size=" << scan_result.size() << std::endl;
  }

  // op 48803: put
  db->Put(write_options, "key4109", "value83769");

  // op 48804: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 48805: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0793", "key3867", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0793, key3867) size=" << scan_result.size() << std::endl;
  }

  // op 48806: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 48807: get
  status = db->Get(read_options, "key11248", &value);
  if (status.ok()) {
    out << "GET key11248 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48808: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0293", "key6123", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0293, key6123) size=" << scan_result.size() << std::endl;
  }

  // op 48809: delete
  db->Delete(write_options, "key6860");

  // op 48810: delete
  db->Delete(write_options, "key2257");

  // op 48811: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0265", "key8241", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0265, key8241) size=" << scan_result.size() << std::endl;
  }

  // op 48812: put
  db->Put(write_options, "key2325", "value26417");

  // op 48813: delete
  db->Delete(write_options, "key7441");

  // op 48814: put
  db->Put(write_options, "key8467", "value76088");

  // op 48815: put
  db->Put(write_options, "key6640", "value56476");

  // op 48816: get
  status = db->Get(read_options, "key5262", &value);
  if (status.ok()) {
    out << "GET key5262 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48817: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1205", "key2621", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1205, key2621) size=" << scan_result.size() << std::endl;
  }

  // op 48818: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 48819: delete
  db->Delete(write_options, "key5756");

  // op 48820: put
  db->Put(write_options, "key0629", "value66118");

  // op 48821: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2526", "key5844", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2526, key5844) size=" << scan_result.size() << std::endl;
  }

  // op 48822: get
  status = db->Get(read_options, "key9134", &value);
  if (status.ok()) {
    out << "GET key9134 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48823: put
  db->Put(write_options, "key1289", "value64030");

  // op 48824: put
  db->Put(write_options, "key1907", "value111837");

  // op 48825: get
  status = db->Get(read_options, "key8626", &value);
  if (status.ok()) {
    out << "GET key8626 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48826: put
  db->Put(write_options, "key8158", "value25886");

  // op 48827: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8112", "key9418", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8112, key9418) size=" << scan_result.size() << std::endl;
  }

  // op 48828: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7585", "key9198", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7585, key9198) size=" << scan_result.size() << std::endl;
  }

  // op 48829: delete_range
  status = db->DeleteRange(write_options, "key3232", "key6208");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 48830: get
  status = db->Get(read_options, "key3658", &value);
  if (status.ok()) {
    out << "GET key3658 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48831: delete_range
  status = db->DeleteRange(write_options, "key3323", "key10077");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 48832: put
  db->Put(write_options, "key5732", "value92754");

  // op 48833: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1157", "key7955", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1157, key7955) size=" << scan_result.size() << std::endl;
  }

  // op 48834: put
  db->Put(write_options, "key2296", "value67687");

  // op 48835: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0668", "key9289", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0668, key9289) size=" << scan_result.size() << std::endl;
  }

  // op 48836: delete_range
  status = db->DeleteRange(write_options, "key10011", "key10862");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 48837: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6432", "key11122", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6432, key11122) size=" << scan_result.size() << std::endl;
  }

  // op 48838: get
  status = db->Get(read_options, "key4491", &value);
  if (status.ok()) {
    out << "GET key4491 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48839: put
  db->Put(write_options, "key9595", "value84787");

  // op 48840: put
  db->Put(write_options, "key2261", "value49986");

  // op 48841: put
  db->Put(write_options, "key5861", "value64828");

  // op 48842: get
  status = db->Get(read_options, "key0869", &value);
  if (status.ok()) {
    out << "GET key0869 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48843: get
  status = db->Get(read_options, "key2233", &value);
  if (status.ok()) {
    out << "GET key2233 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48844: put
  db->Put(write_options, "key6364", "value51377");

  // op 48845: delete
  db->Delete(write_options, "key0353");

  // op 48846: delete_range
  status = db->DeleteRange(write_options, "key0786", "key5590");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 48847: put
  db->Put(write_options, "key6249", "value76745");

  // op 48848: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10471", "key11679", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10471, key11679) size=" << scan_result.size() << std::endl;
  }

  // op 48849: put
  db->Put(write_options, "key1609", "value51910");

  // op 48850: put
  db->Put(write_options, "key2255", "value21103");

  // op 48851: put
  db->Put(write_options, "key9451", "value24257");

  // op 48852: put
  db->Put(write_options, "key1088", "value28811");

  // op 48853: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5315", "key8581", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5315, key8581) size=" << scan_result.size() << std::endl;
  }

  // op 48854: put
  db->Put(write_options, "key9830", "value5159");

  // op 48855: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2729", "key5177", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2729, key5177) size=" << scan_result.size() << std::endl;
  }

  // op 48856: delete_range
  status = db->DeleteRange(write_options, "key0992", "key8748");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 48857: put
  db->Put(write_options, "key11625", "value0342");

  // op 48858: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9170", "key11577", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9170, key11577) size=" << scan_result.size() << std::endl;
  }

  // op 48859: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10804", "key10857", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10804, key10857) size=" << scan_result.size() << std::endl;
  }

  // op 48860: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4044", "key9900", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4044, key9900) size=" << scan_result.size() << std::endl;
  }

  // op 48861: put
  db->Put(write_options, "key6995", "value69598");

  // op 48862: get
  status = db->Get(read_options, "key10242", &value);
  if (status.ok()) {
    out << "GET key10242 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48863: delete_range
  status = db->DeleteRange(write_options, "key2502", "key7512");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 48864: put
  db->Put(write_options, "key7647", "value112034");

  // op 48865: put
  db->Put(write_options, "key10824", "value44726");

  // op 48866: delete_range
  status = db->DeleteRange(write_options, "key0191", "key3979");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 48867: get
  status = db->Get(read_options, "key10538", &value);
  if (status.ok()) {
    out << "GET key10538 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48868: put
  db->Put(write_options, "key6956", "value29750");

  // op 48869: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0176", "key11262", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0176, key11262) size=" << scan_result.size() << std::endl;
  }

  // op 48870: delete_range
  status = db->DeleteRange(write_options, "key0590", "key9119");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 48871: put
  db->Put(write_options, "key1683", "value85819");

  // op 48872: get
  status = db->Get(read_options, "key7379", &value);
  if (status.ok()) {
    out << "GET key7379 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48873: delete
  db->Delete(write_options, "key2442");

  // op 48874: get
  status = db->Get(read_options, "key6903", &value);
  if (status.ok()) {
    out << "GET key6903 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48875: put
  db->Put(write_options, "key11211", "value23058");

  // op 48876: delete_range
  status = db->DeleteRange(write_options, "key3506", "key8174");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 48877: put
  db->Put(write_options, "key11248", "value39500");

  // op 48878: put
  db->Put(write_options, "key0621", "value99679");

  // op 48879: put
  db->Put(write_options, "key3594", "value12119");

  // op 48880: delete_range
  status = db->DeleteRange(write_options, "key8468", "key10539");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 48881: get
  status = db->Get(read_options, "key7712", &value);
  if (status.ok()) {
    out << "GET key7712 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48882: get
  status = db->Get(read_options, "key11501", &value);
  if (status.ok()) {
    out << "GET key11501 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48883: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 48884: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0733", "key0864", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0733, key0864) size=" << scan_result.size() << std::endl;
  }

  // op 48885: get
  status = db->Get(read_options, "key4208", &value);
  if (status.ok()) {
    out << "GET key4208 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48886: get
  status = db->Get(read_options, "key8653", &value);
  if (status.ok()) {
    out << "GET key8653 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48887: get
  status = db->Get(read_options, "key1368", &value);
  if (status.ok()) {
    out << "GET key1368 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48888: delete
  db->Delete(write_options, "key4965");

  // op 48889: put
  db->Put(write_options, "key5010", "value82972");

  // op 48890: put
  db->Put(write_options, "key1144", "value32019");

  // op 48891: put
  db->Put(write_options, "key5557", "value8085");

  // op 48892: delete
  db->Delete(write_options, "key5112");

  // op 48893: delete
  db->Delete(write_options, "key0808");

  // op 48894: delete
  db->Delete(write_options, "key5794");

  // op 48895: put
  db->Put(write_options, "key3584", "value88565");

  // op 48896: get
  status = db->Get(read_options, "key6787", &value);
  if (status.ok()) {
    out << "GET key6787 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48897: put
  db->Put(write_options, "key11372", "value102915");

  // op 48898: put
  db->Put(write_options, "key9038", "value87908");

  // op 48899: get
  status = db->Get(read_options, "key9091", &value);
  if (status.ok()) {
    out << "GET key9091 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48900: get
  status = db->Get(read_options, "key0648", &value);
  if (status.ok()) {
    out << "GET key0648 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48901: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 48902: get
  status = db->Get(read_options, "key1091", &value);
  if (status.ok()) {
    out << "GET key1091 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48903: get
  status = db->Get(read_options, "key9885", &value);
  if (status.ok()) {
    out << "GET key9885 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48904: put
  db->Put(write_options, "key10797", "value45425");

  // op 48905: put
  db->Put(write_options, "key5875", "value51246");

  // op 48906: get
  status = db->Get(read_options, "key11252", &value);
  if (status.ok()) {
    out << "GET key11252 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48907: put
  db->Put(write_options, "key0342", "value35129");

  // op 48908: put
  db->Put(write_options, "key11252", "value110304");

  // op 48909: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7918", "key10703", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7918, key10703) size=" << scan_result.size() << std::endl;
  }

  // op 48910: put
  db->Put(write_options, "key10314", "value10087");

  // op 48911: put
  db->Put(write_options, "key4124", "value68539");

  // op 48912: put
  db->Put(write_options, "key5479", "value84460");

  // op 48913: put
  db->Put(write_options, "key4456", "value61381");

  // op 48914: delete_range
  status = db->DeleteRange(write_options, "key9823", "key11214");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 48915: delete
  db->Delete(write_options, "key10344");

  // op 48916: delete
  db->Delete(write_options, "key2184");

  // op 48917: put
  db->Put(write_options, "key1005", "value7771");

  // op 48918: put
  db->Put(write_options, "key5385", "value70426");

  // op 48919: put
  db->Put(write_options, "key8331", "value48089");

  // op 48920: get
  status = db->Get(read_options, "key1534", &value);
  if (status.ok()) {
    out << "GET key1534 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48921: put
  db->Put(write_options, "key0445", "value58397");

  // op 48922: put
  db->Put(write_options, "key2397", "value103673");

  // op 48923: put
  db->Put(write_options, "key5402", "value111312");

  // op 48924: put
  db->Put(write_options, "key10035", "value62612");

  // op 48925: delete_range
  status = db->DeleteRange(write_options, "key0350", "key4061");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 48926: put
  db->Put(write_options, "key5855", "value22326");

  // op 48927: put
  db->Put(write_options, "key9011", "value81710");

  // op 48928: get
  status = db->Get(read_options, "key0667", &value);
  if (status.ok()) {
    out << "GET key0667 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48929: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1228", "key7308", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1228, key7308) size=" << scan_result.size() << std::endl;
  }

  // op 48930: delete_range
  status = db->DeleteRange(write_options, "key4135", "key5386");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 48931: get
  status = db->Get(read_options, "key9590", &value);
  if (status.ok()) {
    out << "GET key9590 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48932: put
  db->Put(write_options, "key5878", "value33468");

  // op 48933: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1138", "key3252", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1138, key3252) size=" << scan_result.size() << std::endl;
  }

  // op 48934: put
  db->Put(write_options, "key0343", "value77787");

  // op 48935: get
  status = db->Get(read_options, "key9944", &value);
  if (status.ok()) {
    out << "GET key9944 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48936: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0407", "key4769", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0407, key4769) size=" << scan_result.size() << std::endl;
  }

  // op 48937: put
  db->Put(write_options, "key1163", "value36725");

  // op 48938: get
  status = db->Get(read_options, "key7397", &value);
  if (status.ok()) {
    out << "GET key7397 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48939: put
  db->Put(write_options, "key3145", "value35791");

  // op 48940: delete
  db->Delete(write_options, "key8163");

  // op 48941: delete_range
  status = db->DeleteRange(write_options, "key0301", "key6411");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 48942: delete_range
  status = db->DeleteRange(write_options, "key1902", "key3046");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 48943: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6087", "key10117", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6087, key10117) size=" << scan_result.size() << std::endl;
  }

  // op 48944: get
  status = db->Get(read_options, "key2668", &value);
  if (status.ok()) {
    out << "GET key2668 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48945: put
  db->Put(write_options, "key10358", "value2787");

  // op 48946: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 48947: put
  db->Put(write_options, "key7665", "value17323");

  // op 48948: get
  status = db->Get(read_options, "key9079", &value);
  if (status.ok()) {
    out << "GET key9079 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48949: delete
  db->Delete(write_options, "key4013");

  // op 48950: put
  db->Put(write_options, "key2036", "value51067");

  // op 48951: delete_range
  status = db->DeleteRange(write_options, "key0892", "key11678");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 48952: put
  db->Put(write_options, "key10196", "value35586");

  // op 48953: put
  db->Put(write_options, "key0335", "value61518");

  // op 48954: delete
  db->Delete(write_options, "key4190");

  // op 48955: put
  db->Put(write_options, "key3593", "value115399");

  // op 48956: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 48957: put
  db->Put(write_options, "key7992", "value61709");

  // op 48958: get
  status = db->Get(read_options, "key3358", &value);
  if (status.ok()) {
    out << "GET key3358 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48959: delete_range
  status = db->DeleteRange(write_options, "key0116", "key7949");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 48960: delete_range
  status = db->DeleteRange(write_options, "key5164", "key11173");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 48961: put
  db->Put(write_options, "key2408", "value114540");

  // op 48962: get
  status = db->Get(read_options, "key0199", &value);
  if (status.ok()) {
    out << "GET key0199 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48963: put
  db->Put(write_options, "key5382", "value34026");

  // op 48964: get
  status = db->Get(read_options, "key2761", &value);
  if (status.ok()) {
    out << "GET key2761 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48965: put
  db->Put(write_options, "key7300", "value79559");

  // op 48966: get
  status = db->Get(read_options, "key8929", &value);
  if (status.ok()) {
    out << "GET key8929 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48967: delete_range
  status = db->DeleteRange(write_options, "key9150", "key10184");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 48968: put
  db->Put(write_options, "key1724", "value27608");

  // op 48969: get
  status = db->Get(read_options, "key4479", &value);
  if (status.ok()) {
    out << "GET key4479 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48970: get
  status = db->Get(read_options, "key3323", &value);
  if (status.ok()) {
    out << "GET key3323 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48971: put
  db->Put(write_options, "key11562", "value7466");

  // op 48972: get
  status = db->Get(read_options, "key3706", &value);
  if (status.ok()) {
    out << "GET key3706 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48973: get
  status = db->Get(read_options, "key7755", &value);
  if (status.ok()) {
    out << "GET key7755 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48974: put
  db->Put(write_options, "key1730", "value70319");

  // op 48975: delete
  db->Delete(write_options, "key1023");

  // op 48976: put
  db->Put(write_options, "key0198", "value41830");

  // op 48977: delete
  db->Delete(write_options, "key0417");

  // op 48978: delete
  db->Delete(write_options, "key2488");

  // op 48979: get
  status = db->Get(read_options, "key0305", &value);
  if (status.ok()) {
    out << "GET key0305 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48980: get
  status = db->Get(read_options, "key5801", &value);
  if (status.ok()) {
    out << "GET key5801 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48981: put
  db->Put(write_options, "key6348", "value12332");

  // op 48982: delete_range
  status = db->DeleteRange(write_options, "key2161", "key5700");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 48983: delete_range
  status = db->DeleteRange(write_options, "key1249", "key5823");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 48984: put
  db->Put(write_options, "key0765", "value56309");

  // op 48985: put
  db->Put(write_options, "key1118", "value86398");

  // op 48986: put
  db->Put(write_options, "key11454", "value94537");

  // op 48987: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 48988: put
  db->Put(write_options, "key0425", "value114916");

  // op 48989: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1619", "key8903", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1619, key8903) size=" << scan_result.size() << std::endl;
  }

  // op 48990: delete
  db->Delete(write_options, "key2785");

  // op 48991: put
  db->Put(write_options, "key8556", "value25422");

  // op 48992: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1832", "key5830", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1832, key5830) size=" << scan_result.size() << std::endl;
  }

  // op 48993: put
  db->Put(write_options, "key1590", "value75683");

  // op 48994: put
  db->Put(write_options, "key8739", "value110287");

  // op 48995: put
  db->Put(write_options, "key7493", "value89924");

  // op 48996: put
  db->Put(write_options, "key11626", "value87721");

  // op 48997: delete
  db->Delete(write_options, "key2497");

  // op 48998: get
  status = db->Get(read_options, "key3271", &value);
  if (status.ok()) {
    out << "GET key3271 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 48999: delete
  db->Delete(write_options, "key0694");

  // op 49000: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4074", "key6593", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4074, key6593) size=" << scan_result.size() << std::endl;
  }

  // op 49001: put
  db->Put(write_options, "key7220", "value81419");

  // op 49002: put
  db->Put(write_options, "key2711", "value89117");

  // op 49003: put
  db->Put(write_options, "key9713", "value24928");

  // op 49004: put
  db->Put(write_options, "key10689", "value79590");

  // op 49005: delete
  db->Delete(write_options, "key7113");

  // op 49006: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 49007: put
  db->Put(write_options, "key3521", "value61472");

  // op 49008: delete_range
  status = db->DeleteRange(write_options, "key6612", "key9304");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 49009: put
  db->Put(write_options, "key5278", "value108089");

  // op 49010: put
  db->Put(write_options, "key10840", "value5073");

  // op 49011: get
  status = db->Get(read_options, "key11678", &value);
  if (status.ok()) {
    out << "GET key11678 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49012: put
  db->Put(write_options, "key3957", "value93852");

  // op 49013: get
  status = db->Get(read_options, "key2935", &value);
  if (status.ok()) {
    out << "GET key2935 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49014: get
  status = db->Get(read_options, "key7988", &value);
  if (status.ok()) {
    out << "GET key7988 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49015: get
  status = db->Get(read_options, "key3459", &value);
  if (status.ok()) {
    out << "GET key3459 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49016: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0946", "key1840", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0946, key1840) size=" << scan_result.size() << std::endl;
  }

  // op 49017: put
  db->Put(write_options, "key6810", "value91930");

  // op 49018: delete
  db->Delete(write_options, "key0236");

  // op 49019: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2090", "key2968", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2090, key2968) size=" << scan_result.size() << std::endl;
  }

  // op 49020: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9343", "key9687", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9343, key9687) size=" << scan_result.size() << std::endl;
  }

  // op 49021: put
  db->Put(write_options, "key3803", "value34347");

  // op 49022: get
  status = db->Get(read_options, "key3962", &value);
  if (status.ok()) {
    out << "GET key3962 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49023: put
  db->Put(write_options, "key2803", "value63461");

  // op 49024: put
  db->Put(write_options, "key5684", "value35852");

  // op 49025: put
  db->Put(write_options, "key5357", "value21191");

  // op 49026: put
  db->Put(write_options, "key0109", "value96208");

  // op 49027: delete_range
  status = db->DeleteRange(write_options, "key4255", "key8311");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 49028: put
  db->Put(write_options, "key0411", "value3533");

  // op 49029: get
  status = db->Get(read_options, "key10941", &value);
  if (status.ok()) {
    out << "GET key10941 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49030: delete_range
  status = db->DeleteRange(write_options, "key8056", "key11519");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 49031: put
  db->Put(write_options, "key4828", "value38180");

  // op 49032: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3294", "key3875", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3294, key3875) size=" << scan_result.size() << std::endl;
  }

  // op 49033: delete
  db->Delete(write_options, "key9170");

  // op 49034: put
  db->Put(write_options, "key5907", "value50121");

  // op 49035: delete
  db->Delete(write_options, "key2488");

  // op 49036: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7909", "key8127", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7909, key8127) size=" << scan_result.size() << std::endl;
  }

  // op 49037: put
  db->Put(write_options, "key5263", "value65119");

  // op 49038: get
  status = db->Get(read_options, "key0308", &value);
  if (status.ok()) {
    out << "GET key0308 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49039: get
  status = db->Get(read_options, "key7891", &value);
  if (status.ok()) {
    out << "GET key7891 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49040: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2808", "key5954", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2808, key5954) size=" << scan_result.size() << std::endl;
  }

  // op 49041: delete_range
  status = db->DeleteRange(write_options, "key5384", "key6583");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 49042: delete
  db->Delete(write_options, "key10147");

  // op 49043: delete_range
  status = db->DeleteRange(write_options, "key2305", "key7694");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 49044: delete
  db->Delete(write_options, "key8249");

  // op 49045: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 49046: delete_range
  status = db->DeleteRange(write_options, "key5199", "key11020");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 49047: get
  status = db->Get(read_options, "key3166", &value);
  if (status.ok()) {
    out << "GET key3166 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49048: put
  db->Put(write_options, "key3335", "value18352");

  // op 49049: get
  status = db->Get(read_options, "key9848", &value);
  if (status.ok()) {
    out << "GET key9848 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49050: get
  status = db->Get(read_options, "key11554", &value);
  if (status.ok()) {
    out << "GET key11554 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49051: put
  db->Put(write_options, "key4317", "value54217");

  // op 49052: put
  db->Put(write_options, "key11516", "value24282");

  // op 49053: delete
  db->Delete(write_options, "key10926");

  // op 49054: delete
  db->Delete(write_options, "key11360");

  // op 49055: put
  db->Put(write_options, "key11060", "value19006");

  // op 49056: delete_range
  status = db->DeleteRange(write_options, "key2685", "key9533");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 49057: put
  db->Put(write_options, "key4329", "value50536");

  // op 49058: put
  db->Put(write_options, "key7265", "value72687");

  // op 49059: get
  status = db->Get(read_options, "key3354", &value);
  if (status.ok()) {
    out << "GET key3354 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49060: get
  status = db->Get(read_options, "key11347", &value);
  if (status.ok()) {
    out << "GET key11347 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49061: put
  db->Put(write_options, "key5903", "value77736");

  // op 49062: delete_range
  status = db->DeleteRange(write_options, "key2794", "key10300");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 49063: put
  db->Put(write_options, "key6785", "value1803");

  // op 49064: delete
  db->Delete(write_options, "key5362");

  // op 49065: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6916", "key8697", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6916, key8697) size=" << scan_result.size() << std::endl;
  }

  // op 49066: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6658", "key11586", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6658, key11586) size=" << scan_result.size() << std::endl;
  }

  // op 49067: get
  status = db->Get(read_options, "key6316", &value);
  if (status.ok()) {
    out << "GET key6316 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49068: delete
  db->Delete(write_options, "key4394");

  // op 49069: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6537", "key6595", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6537, key6595) size=" << scan_result.size() << std::endl;
  }

  // op 49070: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 49071: put
  db->Put(write_options, "key10422", "value65723");

  // op 49072: get
  status = db->Get(read_options, "key10408", &value);
  if (status.ok()) {
    out << "GET key10408 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49073: put
  db->Put(write_options, "key10957", "value56484");

  // op 49074: delete_range
  status = db->DeleteRange(write_options, "key0410", "key9086");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 49075: put
  db->Put(write_options, "key11493", "value77341");

  // op 49076: put
  db->Put(write_options, "key8132", "value11580");

  // op 49077: get
  status = db->Get(read_options, "key9520", &value);
  if (status.ok()) {
    out << "GET key9520 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49078: put
  db->Put(write_options, "key4768", "value103429");

  // op 49079: put
  db->Put(write_options, "key11525", "value18137");

  // op 49080: put
  db->Put(write_options, "key6834", "value3647");

  // op 49081: put
  db->Put(write_options, "key1321", "value77661");

  // op 49082: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 49083: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1597", "key7954", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1597, key7954) size=" << scan_result.size() << std::endl;
  }

  // op 49084: get
  status = db->Get(read_options, "key11381", &value);
  if (status.ok()) {
    out << "GET key11381 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49085: put
  db->Put(write_options, "key1257", "value16451");

  // op 49086: get
  status = db->Get(read_options, "key9031", &value);
  if (status.ok()) {
    out << "GET key9031 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49087: get
  status = db->Get(read_options, "key3611", &value);
  if (status.ok()) {
    out << "GET key3611 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49088: delete_range
  status = db->DeleteRange(write_options, "key6093", "key6391");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 49089: put
  db->Put(write_options, "key4829", "value87314");

  // op 49090: put
  db->Put(write_options, "key3260", "value110907");

  // op 49091: get
  status = db->Get(read_options, "key0291", &value);
  if (status.ok()) {
    out << "GET key0291 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49092: delete_range
  status = db->DeleteRange(write_options, "key7400", "key11113");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 49093: put
  db->Put(write_options, "key8818", "value91454");

  // op 49094: delete
  db->Delete(write_options, "key2882");

  // op 49095: delete_range
  status = db->DeleteRange(write_options, "key3650", "key8604");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 49096: put
  db->Put(write_options, "key3043", "value86752");

  // op 49097: put
  db->Put(write_options, "key2975", "value17947");

  // op 49098: get
  status = db->Get(read_options, "key3017", &value);
  if (status.ok()) {
    out << "GET key3017 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49099: get
  status = db->Get(read_options, "key8394", &value);
  if (status.ok()) {
    out << "GET key8394 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49100: get
  status = db->Get(read_options, "key3764", &value);
  if (status.ok()) {
    out << "GET key3764 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49101: get
  status = db->Get(read_options, "key1927", &value);
  if (status.ok()) {
    out << "GET key1927 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49102: put
  db->Put(write_options, "key8489", "value11046");

  // op 49103: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 49104: put
  db->Put(write_options, "key9855", "value78586");

  // op 49105: get
  status = db->Get(read_options, "key0799", &value);
  if (status.ok()) {
    out << "GET key0799 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49106: put
  db->Put(write_options, "key1831", "value11168");

  // op 49107: put
  db->Put(write_options, "key9278", "value61513");

  // op 49108: delete
  db->Delete(write_options, "key8553");

  // op 49109: put
  db->Put(write_options, "key3953", "value64959");

  // op 49110: delete
  db->Delete(write_options, "key9639");

  // op 49111: put
  db->Put(write_options, "key2206", "value60278");

  // op 49112: get
  status = db->Get(read_options, "key8002", &value);
  if (status.ok()) {
    out << "GET key8002 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49113: put
  db->Put(write_options, "key1377", "value8405");

  // op 49114: get
  status = db->Get(read_options, "key6136", &value);
  if (status.ok()) {
    out << "GET key6136 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49115: put
  db->Put(write_options, "key10773", "value110839");

  // op 49116: delete
  db->Delete(write_options, "key9155");

  // op 49117: put
  db->Put(write_options, "key4941", "value103364");

  // op 49118: put
  db->Put(write_options, "key8324", "value50821");

  // op 49119: put
  db->Put(write_options, "key1184", "value37919");

  // op 49120: delete_range
  status = db->DeleteRange(write_options, "key1718", "key4321");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 49121: delete
  db->Delete(write_options, "key10809");

  // op 49122: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 49123: get
  status = db->Get(read_options, "key3785", &value);
  if (status.ok()) {
    out << "GET key3785 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49124: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3153", "key5800", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3153, key5800) size=" << scan_result.size() << std::endl;
  }

  // op 49125: delete_range
  status = db->DeleteRange(write_options, "key6557", "key11646");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 49126: put
  db->Put(write_options, "key1534", "value69608");

  // op 49127: put
  db->Put(write_options, "key4754", "value113024");

  // op 49128: get
  status = db->Get(read_options, "key1640", &value);
  if (status.ok()) {
    out << "GET key1640 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49129: delete_range
  status = db->DeleteRange(write_options, "key5238", "key8803");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 49130: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3665", "key7662", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3665, key7662) size=" << scan_result.size() << std::endl;
  }

  // op 49131: put
  db->Put(write_options, "key2292", "value57265");

  // op 49132: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9242", "key9654", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9242, key9654) size=" << scan_result.size() << std::endl;
  }

  // op 49133: put
  db->Put(write_options, "key8468", "value38644");

  // op 49134: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0757", "key0944", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0757, key0944) size=" << scan_result.size() << std::endl;
  }

  // op 49135: put
  db->Put(write_options, "key5949", "value105917");

  // op 49136: put
  db->Put(write_options, "key2773", "value95905");

  // op 49137: put
  db->Put(write_options, "key11655", "value113601");

  // op 49138: get
  status = db->Get(read_options, "key4432", &value);
  if (status.ok()) {
    out << "GET key4432 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49139: put
  db->Put(write_options, "key3123", "value30132");

  // op 49140: put
  db->Put(write_options, "key0016", "value34901");

  // op 49141: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6061", "key7740", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6061, key7740) size=" << scan_result.size() << std::endl;
  }

  // op 49142: put
  db->Put(write_options, "key8215", "value84225");

  // op 49143: delete
  db->Delete(write_options, "key0500");

  // op 49144: delete_range
  status = db->DeleteRange(write_options, "key7086", "key8239");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 49145: get
  status = db->Get(read_options, "key1863", &value);
  if (status.ok()) {
    out << "GET key1863 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49146: put
  db->Put(write_options, "key9240", "value34940");

  // op 49147: put
  db->Put(write_options, "key3734", "value115872");

  // op 49148: put
  db->Put(write_options, "key3382", "value86392");

  // op 49149: put
  db->Put(write_options, "key0102", "value59257");

  // op 49150: put
  db->Put(write_options, "key9691", "value78705");

  // op 49151: put
  db->Put(write_options, "key6897", "value8703");

  // op 49152: get
  status = db->Get(read_options, "key9260", &value);
  if (status.ok()) {
    out << "GET key9260 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49153: put
  db->Put(write_options, "key2605", "value32242");

  // op 49154: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6771", "key8817", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6771, key8817) size=" << scan_result.size() << std::endl;
  }

  // op 49155: put
  db->Put(write_options, "key11018", "value24201");

  // op 49156: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1312", "key5314", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1312, key5314) size=" << scan_result.size() << std::endl;
  }

  // op 49157: put
  db->Put(write_options, "key11198", "value16541");

  // op 49158: put
  db->Put(write_options, "key10632", "value26948");

  // op 49159: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 49160: put
  db->Put(write_options, "key0753", "value31842");

  // op 49161: put
  db->Put(write_options, "key2909", "value59701");

  // op 49162: delete
  db->Delete(write_options, "key2818");

  // op 49163: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5668", "key6321", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5668, key6321) size=" << scan_result.size() << std::endl;
  }

  // op 49164: put
  db->Put(write_options, "key9170", "value64856");

  // op 49165: get
  status = db->Get(read_options, "key1545", &value);
  if (status.ok()) {
    out << "GET key1545 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49166: delete_range
  status = db->DeleteRange(write_options, "key3715", "key10217");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 49167: get
  status = db->Get(read_options, "key5175", &value);
  if (status.ok()) {
    out << "GET key5175 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49168: put
  db->Put(write_options, "key10027", "value5063");

  // op 49169: put
  db->Put(write_options, "key8010", "value14842");

  // op 49170: get
  status = db->Get(read_options, "key9746", &value);
  if (status.ok()) {
    out << "GET key9746 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49171: get
  status = db->Get(read_options, "key7199", &value);
  if (status.ok()) {
    out << "GET key7199 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49172: get
  status = db->Get(read_options, "key11634", &value);
  if (status.ok()) {
    out << "GET key11634 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49173: get
  status = db->Get(read_options, "key7184", &value);
  if (status.ok()) {
    out << "GET key7184 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49174: put
  db->Put(write_options, "key11409", "value49839");

  // op 49175: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6754", "key10971", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6754, key10971) size=" << scan_result.size() << std::endl;
  }

  // op 49176: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0504", "key9382", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0504, key9382) size=" << scan_result.size() << std::endl;
  }

  // op 49177: get
  status = db->Get(read_options, "key9021", &value);
  if (status.ok()) {
    out << "GET key9021 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49178: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6064", "key9146", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6064, key9146) size=" << scan_result.size() << std::endl;
  }

  // op 49179: put
  db->Put(write_options, "key5737", "value56576");

  // op 49180: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2082", "key4288", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2082, key4288) size=" << scan_result.size() << std::endl;
  }

  // op 49181: put
  db->Put(write_options, "key3960", "value102349");

  // op 49182: put
  db->Put(write_options, "key10746", "value7937");

  // op 49183: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 49184: get
  status = db->Get(read_options, "key3223", &value);
  if (status.ok()) {
    out << "GET key3223 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49185: put
  db->Put(write_options, "key4733", "value26939");

  // op 49186: put
  db->Put(write_options, "key1444", "value11321");

  // op 49187: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0338", "key2127", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0338, key2127) size=" << scan_result.size() << std::endl;
  }

  // op 49188: put
  db->Put(write_options, "key9625", "value69883");

  // op 49189: delete_range
  status = db->DeleteRange(write_options, "key6890", "key8332");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 49190: put
  db->Put(write_options, "key10474", "value44823");

  // op 49191: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3538", "key11036", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3538, key11036) size=" << scan_result.size() << std::endl;
  }

  // op 49192: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1528", "key3003", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1528, key3003) size=" << scan_result.size() << std::endl;
  }

  // op 49193: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0267", "key2852", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0267, key2852) size=" << scan_result.size() << std::endl;
  }

  // op 49194: put
  db->Put(write_options, "key1455", "value25235");

  // op 49195: get
  status = db->Get(read_options, "key6662", &value);
  if (status.ok()) {
    out << "GET key6662 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49196: get
  status = db->Get(read_options, "key8254", &value);
  if (status.ok()) {
    out << "GET key8254 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49197: put
  db->Put(write_options, "key2098", "value2019");

  // op 49198: delete_range
  status = db->DeleteRange(write_options, "key1201", "key1369");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 49199: put
  db->Put(write_options, "key11471", "value97807");

  // op 49200: get
  status = db->Get(read_options, "key6304", &value);
  if (status.ok()) {
    out << "GET key6304 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49201: put
  db->Put(write_options, "key1529", "value54247");

  // op 49202: put
  db->Put(write_options, "key9479", "value40478");

  // op 49203: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10332", "key10425", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10332, key10425) size=" << scan_result.size() << std::endl;
  }

  // op 49204: delete_range
  status = db->DeleteRange(write_options, "key3171", "key11725");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 49205: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0888", "key4833", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0888, key4833) size=" << scan_result.size() << std::endl;
  }

  // op 49206: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9840", "key11663", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9840, key11663) size=" << scan_result.size() << std::endl;
  }

  // op 49207: get
  status = db->Get(read_options, "key11381", &value);
  if (status.ok()) {
    out << "GET key11381 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49208: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 49209: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6479", "key8996", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6479, key8996) size=" << scan_result.size() << std::endl;
  }

  // op 49210: put
  db->Put(write_options, "key11588", "value42432");

  // op 49211: get
  status = db->Get(read_options, "key6547", &value);
  if (status.ok()) {
    out << "GET key6547 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49212: get
  status = db->Get(read_options, "key7306", &value);
  if (status.ok()) {
    out << "GET key7306 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49213: put
  db->Put(write_options, "key10621", "value17075");

  // op 49214: get
  status = db->Get(read_options, "key9377", &value);
  if (status.ok()) {
    out << "GET key9377 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49215: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5735", "key11193", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5735, key11193) size=" << scan_result.size() << std::endl;
  }

  // op 49216: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6246", "key7090", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6246, key7090) size=" << scan_result.size() << std::endl;
  }

  // op 49217: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 49218: delete
  db->Delete(write_options, "key3870");

  // op 49219: get
  status = db->Get(read_options, "key9880", &value);
  if (status.ok()) {
    out << "GET key9880 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49220: delete_range
  status = db->DeleteRange(write_options, "key4405", "key9879");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 49221: delete
  db->Delete(write_options, "key5606");

  // op 49222: put
  db->Put(write_options, "key2464", "value3198");

  // op 49223: put
  db->Put(write_options, "key6530", "value90132");

  // op 49224: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0017", "key3551", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0017, key3551) size=" << scan_result.size() << std::endl;
  }

  // op 49225: delete
  db->Delete(write_options, "key9053");

  // op 49226: delete_range
  status = db->DeleteRange(write_options, "key5767", "key7146");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 49227: put
  db->Put(write_options, "key7484", "value68027");

  // op 49228: put
  db->Put(write_options, "key11511", "value108167");

  // op 49229: put
  db->Put(write_options, "key1441", "value3436");

  // op 49230: get
  status = db->Get(read_options, "key4547", &value);
  if (status.ok()) {
    out << "GET key4547 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49231: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8180", "key9387", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8180, key9387) size=" << scan_result.size() << std::endl;
  }

  // op 49232: put
  db->Put(write_options, "key10371", "value27493");

  // op 49233: put
  db->Put(write_options, "key3206", "value76759");

  // op 49234: delete
  db->Delete(write_options, "key0882");

  // op 49235: put
  db->Put(write_options, "key11609", "value114666");

  // op 49236: get
  status = db->Get(read_options, "key11112", &value);
  if (status.ok()) {
    out << "GET key11112 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49237: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7190", "key11398", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7190, key11398) size=" << scan_result.size() << std::endl;
  }

  // op 49238: put
  db->Put(write_options, "key2543", "value37464");

  // op 49239: delete
  db->Delete(write_options, "key4956");

  // op 49240: put
  db->Put(write_options, "key0891", "value15295");

  // op 49241: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1124", "key2266", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1124, key2266) size=" << scan_result.size() << std::endl;
  }

  // op 49242: put
  db->Put(write_options, "key3416", "value43480");

  // op 49243: get
  status = db->Get(read_options, "key5241", &value);
  if (status.ok()) {
    out << "GET key5241 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49244: put
  db->Put(write_options, "key9451", "value14801");

  // op 49245: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1360", "key7218", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1360, key7218) size=" << scan_result.size() << std::endl;
  }

  // op 49246: get
  status = db->Get(read_options, "key4488", &value);
  if (status.ok()) {
    out << "GET key4488 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49247: put
  db->Put(write_options, "key2466", "value95768");

  // op 49248: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6042", "key10471", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6042, key10471) size=" << scan_result.size() << std::endl;
  }

  // op 49249: put
  db->Put(write_options, "key8972", "value82262");

  // op 49250: put
  db->Put(write_options, "key9981", "value42133");

  // op 49251: put
  db->Put(write_options, "key7345", "value71060");

  // op 49252: put
  db->Put(write_options, "key9913", "value67776");

  // op 49253: put
  db->Put(write_options, "key1194", "value29800");

  // op 49254: put
  db->Put(write_options, "key9283", "value37724");

  // op 49255: delete
  db->Delete(write_options, "key10503");

  // op 49256: put
  db->Put(write_options, "key7115", "value102322");

  // op 49257: put
  db->Put(write_options, "key2888", "value63256");

  // op 49258: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2755", "key9851", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2755, key9851) size=" << scan_result.size() << std::endl;
  }

  // op 49259: delete_range
  status = db->DeleteRange(write_options, "key0978", "key5898");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 49260: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0712", "key3039", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0712, key3039) size=" << scan_result.size() << std::endl;
  }

  // op 49261: put
  db->Put(write_options, "key6787", "value25849");

  // op 49262: delete_range
  status = db->DeleteRange(write_options, "key4768", "key11335");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 49263: put
  db->Put(write_options, "key6397", "value38929");

  // op 49264: put
  db->Put(write_options, "key6670", "value111964");

  // op 49265: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 49266: delete
  db->Delete(write_options, "key10683");

  // op 49267: put
  db->Put(write_options, "key1612", "value71907");

  // op 49268: delete
  db->Delete(write_options, "key2882");

  // op 49269: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 49270: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1409", "key5484", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1409, key5484) size=" << scan_result.size() << std::endl;
  }

  // op 49271: put
  db->Put(write_options, "key7541", "value39268");

  // op 49272: delete
  db->Delete(write_options, "key1507");

  // op 49273: get
  status = db->Get(read_options, "key8031", &value);
  if (status.ok()) {
    out << "GET key8031 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49274: delete_range
  status = db->DeleteRange(write_options, "key4467", "key11581");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 49275: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2831", "key11464", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2831, key11464) size=" << scan_result.size() << std::endl;
  }

  // op 49276: get
  status = db->Get(read_options, "key8083", &value);
  if (status.ok()) {
    out << "GET key8083 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49277: put
  db->Put(write_options, "key10806", "value49707");

  // op 49278: put
  db->Put(write_options, "key5493", "value28110");

  // op 49279: get
  status = db->Get(read_options, "key1513", &value);
  if (status.ok()) {
    out << "GET key1513 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49280: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4714", "key9468", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4714, key9468) size=" << scan_result.size() << std::endl;
  }

  // op 49281: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5265", "key9479", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5265, key9479) size=" << scan_result.size() << std::endl;
  }

  // op 49282: put
  db->Put(write_options, "key4308", "value38871");

  // op 49283: put
  db->Put(write_options, "key9283", "value106503");

  // op 49284: get
  status = db->Get(read_options, "key4242", &value);
  if (status.ok()) {
    out << "GET key4242 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49285: delete
  db->Delete(write_options, "key8767");

  // op 49286: put
  db->Put(write_options, "key1384", "value31113");

  // op 49287: put
  db->Put(write_options, "key0348", "value30162");

  // op 49288: put
  db->Put(write_options, "key4274", "value93075");

  // op 49289: delete_range
  status = db->DeleteRange(write_options, "key3272", "key11377");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 49290: delete
  db->Delete(write_options, "key0597");

  // op 49291: delete
  db->Delete(write_options, "key8568");

  // op 49292: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4166", "key6468", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4166, key6468) size=" << scan_result.size() << std::endl;
  }

  // op 49293: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 49294: delete_range
  status = db->DeleteRange(write_options, "key1690", "key9052");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 49295: put
  db->Put(write_options, "key0266", "value79039");

  // op 49296: get
  status = db->Get(read_options, "key9469", &value);
  if (status.ok()) {
    out << "GET key9469 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49297: delete_range
  status = db->DeleteRange(write_options, "key0342", "key4748");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 49298: delete_range
  status = db->DeleteRange(write_options, "key6345", "key9151");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 49299: put
  db->Put(write_options, "key1354", "value41597");

  // op 49300: get
  status = db->Get(read_options, "key6611", &value);
  if (status.ok()) {
    out << "GET key6611 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49301: put
  db->Put(write_options, "key7678", "value89009");

  // op 49302: put
  db->Put(write_options, "key7217", "value85471");

  // op 49303: put
  db->Put(write_options, "key3449", "value13188");

  // op 49304: put
  db->Put(write_options, "key7641", "value113442");

  // op 49305: get
  status = db->Get(read_options, "key8220", &value);
  if (status.ok()) {
    out << "GET key8220 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49306: put
  db->Put(write_options, "key3869", "value52375");

  // op 49307: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0799", "key2416", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0799, key2416) size=" << scan_result.size() << std::endl;
  }

  // op 49308: put
  db->Put(write_options, "key1398", "value79971");

  // op 49309: put
  db->Put(write_options, "key0501", "value92378");

  // op 49310: put
  db->Put(write_options, "key8990", "value75250");

  // op 49311: put
  db->Put(write_options, "key8526", "value79643");

  // op 49312: delete_range
  status = db->DeleteRange(write_options, "key1900", "key2255");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 49313: put
  db->Put(write_options, "key0319", "value8342");

  // op 49314: put
  db->Put(write_options, "key0301", "value46679");

  // op 49315: delete
  db->Delete(write_options, "key0160");

  // op 49316: put
  db->Put(write_options, "key7990", "value67057");

  // op 49317: get
  status = db->Get(read_options, "key4097", &value);
  if (status.ok()) {
    out << "GET key4097 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49318: delete
  db->Delete(write_options, "key10531");

  // op 49319: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6974", "key10148", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6974, key10148) size=" << scan_result.size() << std::endl;
  }

  // op 49320: put
  db->Put(write_options, "key0975", "value5913");

  // op 49321: put
  db->Put(write_options, "key3730", "value25660");

  // op 49322: delete_range
  status = db->DeleteRange(write_options, "key7144", "key9565");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 49323: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0554", "key6145", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0554, key6145) size=" << scan_result.size() << std::endl;
  }

  // op 49324: put
  db->Put(write_options, "key11391", "value86515");

  // op 49325: get
  status = db->Get(read_options, "key2850", &value);
  if (status.ok()) {
    out << "GET key2850 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49326: put
  db->Put(write_options, "key5647", "value4032");

  // op 49327: get
  status = db->Get(read_options, "key0319", &value);
  if (status.ok()) {
    out << "GET key0319 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49328: put
  db->Put(write_options, "key5996", "value10379");

  // op 49329: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4441", "key4768", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4441, key4768) size=" << scan_result.size() << std::endl;
  }

  // op 49330: put
  db->Put(write_options, "key5364", "value12199");

  // op 49331: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3682", "key4218", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3682, key4218) size=" << scan_result.size() << std::endl;
  }

  // op 49332: put
  db->Put(write_options, "key2558", "value32966");

  // op 49333: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 49334: get
  status = db->Get(read_options, "key9594", &value);
  if (status.ok()) {
    out << "GET key9594 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49335: get
  status = db->Get(read_options, "key4941", &value);
  if (status.ok()) {
    out << "GET key4941 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49336: put
  db->Put(write_options, "key6745", "value20723");

  // op 49337: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 49338: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1405", "key4320", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1405, key4320) size=" << scan_result.size() << std::endl;
  }

  // op 49339: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3748", "key6398", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3748, key6398) size=" << scan_result.size() << std::endl;
  }

  // op 49340: get
  status = db->Get(read_options, "key1413", &value);
  if (status.ok()) {
    out << "GET key1413 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49341: get
  status = db->Get(read_options, "key5057", &value);
  if (status.ok()) {
    out << "GET key5057 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49342: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0784", "key5375", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0784, key5375) size=" << scan_result.size() << std::endl;
  }

  // op 49343: get
  status = db->Get(read_options, "key8524", &value);
  if (status.ok()) {
    out << "GET key8524 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49344: put
  db->Put(write_options, "key5059", "value39687");

  // op 49345: delete_range
  status = db->DeleteRange(write_options, "key6127", "key7721");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 49346: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3068", "key4673", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3068, key4673) size=" << scan_result.size() << std::endl;
  }

  // op 49347: put
  db->Put(write_options, "key5509", "value6541");

  // op 49348: delete
  db->Delete(write_options, "key3840");

  // op 49349: get
  status = db->Get(read_options, "key3906", &value);
  if (status.ok()) {
    out << "GET key3906 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49350: put
  db->Put(write_options, "key3121", "value25485");

  // op 49351: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 49352: put
  db->Put(write_options, "key4464", "value102674");

  // op 49353: get
  status = db->Get(read_options, "key7919", &value);
  if (status.ok()) {
    out << "GET key7919 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49354: delete_range
  status = db->DeleteRange(write_options, "key0388", "key10111");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 49355: delete
  db->Delete(write_options, "key0662");

  // op 49356: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4617", "key5876", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4617, key5876) size=" << scan_result.size() << std::endl;
  }

  // op 49357: get
  status = db->Get(read_options, "key2243", &value);
  if (status.ok()) {
    out << "GET key2243 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49358: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6306", "key9520", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6306, key9520) size=" << scan_result.size() << std::endl;
  }

  // op 49359: get
  status = db->Get(read_options, "key11181", &value);
  if (status.ok()) {
    out << "GET key11181 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49360: put
  db->Put(write_options, "key1336", "value18461");

  // op 49361: put
  db->Put(write_options, "key3954", "value75381");

  // op 49362: get
  status = db->Get(read_options, "key3317", &value);
  if (status.ok()) {
    out << "GET key3317 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49363: get
  status = db->Get(read_options, "key9531", &value);
  if (status.ok()) {
    out << "GET key9531 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49364: put
  db->Put(write_options, "key11601", "value46067");

  // op 49365: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3869", "key4609", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3869, key4609) size=" << scan_result.size() << std::endl;
  }

  // op 49366: delete_range
  status = db->DeleteRange(write_options, "key2782", "key5742");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 49367: put
  db->Put(write_options, "key9613", "value16495");

  // op 49368: put
  db->Put(write_options, "key4173", "value10432");

  // op 49369: put
  db->Put(write_options, "key8792", "value93958");

  // op 49370: put
  db->Put(write_options, "key11102", "value18550");

  // op 49371: put
  db->Put(write_options, "key2268", "value44909");

  // op 49372: get
  status = db->Get(read_options, "key5573", &value);
  if (status.ok()) {
    out << "GET key5573 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49373: delete_range
  status = db->DeleteRange(write_options, "key5182", "key8297");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 49374: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1704", "key9854", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1704, key9854) size=" << scan_result.size() << std::endl;
  }

  // op 49375: put
  db->Put(write_options, "key6663", "value117137");

  // op 49376: delete
  db->Delete(write_options, "key4857");

  // op 49377: put
  db->Put(write_options, "key2864", "value46711");

  // op 49378: put
  db->Put(write_options, "key7236", "value97538");

  // op 49379: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 49380: get
  status = db->Get(read_options, "key3784", &value);
  if (status.ok()) {
    out << "GET key3784 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49381: put
  db->Put(write_options, "key2373", "value15998");

  // op 49382: delete_range
  status = db->DeleteRange(write_options, "key1225", "key8796");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 49383: put
  db->Put(write_options, "key5467", "value114240");

  // op 49384: get
  status = db->Get(read_options, "key7964", &value);
  if (status.ok()) {
    out << "GET key7964 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49385: delete_range
  status = db->DeleteRange(write_options, "key4385", "key5467");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 49386: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1968", "key5736", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1968, key5736) size=" << scan_result.size() << std::endl;
  }

  // op 49387: put
  db->Put(write_options, "key3265", "value118055");

  // op 49388: put
  db->Put(write_options, "key5035", "value88305");

  // op 49389: get
  status = db->Get(read_options, "key8810", &value);
  if (status.ok()) {
    out << "GET key8810 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49390: delete
  db->Delete(write_options, "key11085");

  // op 49391: delete
  db->Delete(write_options, "key10774");

  // op 49392: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 49393: get
  status = db->Get(read_options, "key10978", &value);
  if (status.ok()) {
    out << "GET key10978 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49394: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8553", "key8818", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8553, key8818) size=" << scan_result.size() << std::endl;
  }

  // op 49395: delete
  db->Delete(write_options, "key10239");

  // op 49396: delete
  db->Delete(write_options, "key9509");

  // op 49397: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 49398: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1035", "key6332", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1035, key6332) size=" << scan_result.size() << std::endl;
  }

  // op 49399: put
  db->Put(write_options, "key1613", "value80970");

  // op 49400: put
  db->Put(write_options, "key7779", "value85915");

  // op 49401: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6032", "key7332", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6032, key7332) size=" << scan_result.size() << std::endl;
  }

  // op 49402: delete_range
  status = db->DeleteRange(write_options, "key5377", "key5781");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 49403: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2661", "key5381", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2661, key5381) size=" << scan_result.size() << std::endl;
  }

  // op 49404: get
  status = db->Get(read_options, "key2521", &value);
  if (status.ok()) {
    out << "GET key2521 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49405: put
  db->Put(write_options, "key9182", "value77258");

  // op 49406: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 49407: put
  db->Put(write_options, "key9393", "value18944");

  // op 49408: delete
  db->Delete(write_options, "key8658");

  // op 49409: put
  db->Put(write_options, "key7896", "value67815");

  // op 49410: put
  db->Put(write_options, "key0173", "value108487");

  // op 49411: put
  db->Put(write_options, "key1425", "value102863");

  // op 49412: put
  db->Put(write_options, "key10138", "value98104");

  // op 49413: put
  db->Put(write_options, "key1036", "value54759");

  // op 49414: put
  db->Put(write_options, "key2602", "value100694");

  // op 49415: put
  db->Put(write_options, "key4820", "value73394");

  // op 49416: put
  db->Put(write_options, "key1694", "value84684");

  // op 49417: delete_range
  status = db->DeleteRange(write_options, "key2173", "key11533");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 49418: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0340", "key3648", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0340, key3648) size=" << scan_result.size() << std::endl;
  }

  // op 49419: get
  status = db->Get(read_options, "key0315", &value);
  if (status.ok()) {
    out << "GET key0315 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49420: put
  db->Put(write_options, "key0348", "value33982");

  // op 49421: put
  db->Put(write_options, "key2647", "value41194");

  // op 49422: delete_range
  status = db->DeleteRange(write_options, "key0686", "key5437");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 49423: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0630", "key0980", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0630, key0980) size=" << scan_result.size() << std::endl;
  }

  // op 49424: put
  db->Put(write_options, "key4508", "value65858");

  // op 49425: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1716", "key6036", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1716, key6036) size=" << scan_result.size() << std::endl;
  }

  // op 49426: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0534", "key7191", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0534, key7191) size=" << scan_result.size() << std::endl;
  }

  // op 49427: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4242", "key10278", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4242, key10278) size=" << scan_result.size() << std::endl;
  }

  // op 49428: delete_range
  status = db->DeleteRange(write_options, "key1469", "key9564");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 49429: get
  status = db->Get(read_options, "key9940", &value);
  if (status.ok()) {
    out << "GET key9940 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49430: put
  db->Put(write_options, "key3313", "value45312");

  // op 49431: put
  db->Put(write_options, "key11295", "value68350");

  // op 49432: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0493", "key10176", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0493, key10176) size=" << scan_result.size() << std::endl;
  }

  // op 49433: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4802", "key10652", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4802, key10652) size=" << scan_result.size() << std::endl;
  }

  // op 49434: put
  db->Put(write_options, "key2825", "value76089");

  // op 49435: put
  db->Put(write_options, "key0258", "value111673");

  // op 49436: put
  db->Put(write_options, "key4321", "value87245");

  // op 49437: put
  db->Put(write_options, "key9705", "value75387");

  // op 49438: put
  db->Put(write_options, "key7262", "value62971");

  // op 49439: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 49440: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1165", "key9286", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1165, key9286) size=" << scan_result.size() << std::endl;
  }

  // op 49441: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0471", "key10387", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0471, key10387) size=" << scan_result.size() << std::endl;
  }

  // op 49442: put
  db->Put(write_options, "key5146", "value40362");

  // op 49443: put
  db->Put(write_options, "key0421", "value92077");

  // op 49444: delete_range
  status = db->DeleteRange(write_options, "key3228", "key8336");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 49445: put
  db->Put(write_options, "key2688", "value78619");

  // op 49446: delete_range
  status = db->DeleteRange(write_options, "key3127", "key6218");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 49447: get
  status = db->Get(read_options, "key1986", &value);
  if (status.ok()) {
    out << "GET key1986 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49448: put
  db->Put(write_options, "key9912", "value44254");

  // op 49449: delete
  db->Delete(write_options, "key10343");

  // op 49450: put
  db->Put(write_options, "key10252", "value87936");

  // op 49451: get
  status = db->Get(read_options, "key6009", &value);
  if (status.ok()) {
    out << "GET key6009 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49452: put
  db->Put(write_options, "key4328", "value94253");

  // op 49453: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 49454: put
  db->Put(write_options, "key9593", "value108653");

  // op 49455: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 49456: delete
  db->Delete(write_options, "key11122");

  // op 49457: delete
  db->Delete(write_options, "key5068");

  // op 49458: put
  db->Put(write_options, "key0052", "value68201");

  // op 49459: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1999", "key8430", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1999, key8430) size=" << scan_result.size() << std::endl;
  }

  // op 49460: put
  db->Put(write_options, "key1196", "value25275");

  // op 49461: get
  status = db->Get(read_options, "key11082", &value);
  if (status.ok()) {
    out << "GET key11082 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49462: delete_range
  status = db->DeleteRange(write_options, "key6524", "key6950");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 49463: delete_range
  status = db->DeleteRange(write_options, "key5663", "key6307");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 49464: put
  db->Put(write_options, "key8551", "value104185");

  // op 49465: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 49466: delete_range
  status = db->DeleteRange(write_options, "key3619", "key5196");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 49467: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0320", "key5337", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0320, key5337) size=" << scan_result.size() << std::endl;
  }

  // op 49468: put
  db->Put(write_options, "key3243", "value27794");

  // op 49469: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0950", "key5514", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0950, key5514) size=" << scan_result.size() << std::endl;
  }

  // op 49470: delete
  db->Delete(write_options, "key8909");

  // op 49471: get
  status = db->Get(read_options, "key10013", &value);
  if (status.ok()) {
    out << "GET key10013 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49472: delete_range
  status = db->DeleteRange(write_options, "key7379", "key10545");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 49473: delete_range
  status = db->DeleteRange(write_options, "key5344", "key6572");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 49474: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1446", "key4343", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1446, key4343) size=" << scan_result.size() << std::endl;
  }

  // op 49475: put
  db->Put(write_options, "key2804", "value107650");

  // op 49476: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 49477: delete_range
  status = db->DeleteRange(write_options, "key0231", "key9109");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 49478: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9508", "key9807", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9508, key9807) size=" << scan_result.size() << std::endl;
  }

  // op 49479: put
  db->Put(write_options, "key4834", "value7088");

  // op 49480: get
  status = db->Get(read_options, "key5740", &value);
  if (status.ok()) {
    out << "GET key5740 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49481: delete
  db->Delete(write_options, "key3744");

  // op 49482: put
  db->Put(write_options, "key4779", "value31802");

  // op 49483: get
  status = db->Get(read_options, "key6981", &value);
  if (status.ok()) {
    out << "GET key6981 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49484: get
  status = db->Get(read_options, "key2515", &value);
  if (status.ok()) {
    out << "GET key2515 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49485: delete_range
  status = db->DeleteRange(write_options, "key0387", "key1767");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 49486: delete_range
  status = db->DeleteRange(write_options, "key4295", "key7581");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 49487: put
  db->Put(write_options, "key4390", "value23455");

  // op 49488: get
  status = db->Get(read_options, "key4514", &value);
  if (status.ok()) {
    out << "GET key4514 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49489: put
  db->Put(write_options, "key7922", "value103943");

  // op 49490: put
  db->Put(write_options, "key11482", "value56875");

  // op 49491: get
  status = db->Get(read_options, "key10393", &value);
  if (status.ok()) {
    out << "GET key10393 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49492: delete
  db->Delete(write_options, "key2252");

  // op 49493: put
  db->Put(write_options, "key2075", "value99021");

  // op 49494: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9685", "key10571", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9685, key10571) size=" << scan_result.size() << std::endl;
  }

  // op 49495: delete
  db->Delete(write_options, "key1427");

  // op 49496: get
  status = db->Get(read_options, "key1856", &value);
  if (status.ok()) {
    out << "GET key1856 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49497: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 49498: delete
  db->Delete(write_options, "key9771");

  // op 49499: put
  db->Put(write_options, "key9215", "value34556");

  // op 49500: put
  db->Put(write_options, "key3414", "value92869");

  // op 49501: put
  db->Put(write_options, "key2592", "value52500");

  // op 49502: put
  db->Put(write_options, "key4656", "value79080");

  // op 49503: put
  db->Put(write_options, "key3815", "value14633");

  // op 49504: put
  db->Put(write_options, "key10690", "value95149");

  // op 49505: put
  db->Put(write_options, "key10972", "value97616");

  // op 49506: put
  db->Put(write_options, "key5113", "value25111");

  // op 49507: get
  status = db->Get(read_options, "key7168", &value);
  if (status.ok()) {
    out << "GET key7168 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49508: put
  db->Put(write_options, "key8240", "value17493");

  // op 49509: put
  db->Put(write_options, "key6500", "value11091");

  // op 49510: put
  db->Put(write_options, "key5988", "value55295");

  // op 49511: put
  db->Put(write_options, "key9532", "value8263");

  // op 49512: put
  db->Put(write_options, "key2740", "value32351");

  // op 49513: put
  db->Put(write_options, "key2198", "value63625");

  // op 49514: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0501", "key9586", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0501, key9586) size=" << scan_result.size() << std::endl;
  }

  // op 49515: get
  status = db->Get(read_options, "key8493", &value);
  if (status.ok()) {
    out << "GET key8493 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49516: delete_range
  status = db->DeleteRange(write_options, "key9965", "key10041");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 49517: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3550", "key8255", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3550, key8255) size=" << scan_result.size() << std::endl;
  }

  // op 49518: delete_range
  status = db->DeleteRange(write_options, "key7243", "key7496");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 49519: put
  db->Put(write_options, "key4554", "value52495");

  // op 49520: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 49521: get
  status = db->Get(read_options, "key2433", &value);
  if (status.ok()) {
    out << "GET key2433 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49522: delete
  db->Delete(write_options, "key4525");

  // op 49523: delete
  db->Delete(write_options, "key0447");

  // op 49524: delete_range
  status = db->DeleteRange(write_options, "key11100", "key11395");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 49525: get
  status = db->Get(read_options, "key6856", &value);
  if (status.ok()) {
    out << "GET key6856 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49526: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 49527: delete_range
  status = db->DeleteRange(write_options, "key3648", "key5893");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 49528: get
  status = db->Get(read_options, "key1653", &value);
  if (status.ok()) {
    out << "GET key1653 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49529: put
  db->Put(write_options, "key2877", "value104994");

  // op 49530: get
  status = db->Get(read_options, "key0269", &value);
  if (status.ok()) {
    out << "GET key0269 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49531: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2592", "key5190", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2592, key5190) size=" << scan_result.size() << std::endl;
  }

  // op 49532: delete
  db->Delete(write_options, "key9086");

  // op 49533: put
  db->Put(write_options, "key7818", "value84332");

  // op 49534: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 49535: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0084", "key3262", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0084, key3262) size=" << scan_result.size() << std::endl;
  }

  // op 49536: put
  db->Put(write_options, "key4293", "value47960");

  // op 49537: get
  status = db->Get(read_options, "key2652", &value);
  if (status.ok()) {
    out << "GET key2652 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49538: put
  db->Put(write_options, "key1934", "value32238");

  // op 49539: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6972", "key9439", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6972, key9439) size=" << scan_result.size() << std::endl;
  }

  // op 49540: delete_range
  status = db->DeleteRange(write_options, "key3403", "key5819");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 49541: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2607", "key8602", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2607, key8602) size=" << scan_result.size() << std::endl;
  }

  // op 49542: get
  status = db->Get(read_options, "key0731", &value);
  if (status.ok()) {
    out << "GET key0731 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49543: put
  db->Put(write_options, "key6358", "value13606");

  // op 49544: get
  status = db->Get(read_options, "key9490", &value);
  if (status.ok()) {
    out << "GET key9490 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49545: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 49546: put
  db->Put(write_options, "key5592", "value117736");

  // op 49547: put
  db->Put(write_options, "key11640", "value102031");

  // op 49548: put
  db->Put(write_options, "key11379", "value104542");

  // op 49549: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0754", "key10940", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0754, key10940) size=" << scan_result.size() << std::endl;
  }

  // op 49550: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 49551: get
  status = db->Get(read_options, "key3148", &value);
  if (status.ok()) {
    out << "GET key3148 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49552: put
  db->Put(write_options, "key10884", "value53294");

  // op 49553: get
  status = db->Get(read_options, "key7694", &value);
  if (status.ok()) {
    out << "GET key7694 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49554: put
  db->Put(write_options, "key9996", "value89343");

  // op 49555: put
  db->Put(write_options, "key1062", "value42248");

  // op 49556: put
  db->Put(write_options, "key5060", "value112385");

  // op 49557: get
  status = db->Get(read_options, "key3641", &value);
  if (status.ok()) {
    out << "GET key3641 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49558: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3572", "key5020", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3572, key5020) size=" << scan_result.size() << std::endl;
  }

  // op 49559: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0969", "key9462", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0969, key9462) size=" << scan_result.size() << std::endl;
  }

  // op 49560: get
  status = db->Get(read_options, "key7631", &value);
  if (status.ok()) {
    out << "GET key7631 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49561: put
  db->Put(write_options, "key5853", "value51915");

  // op 49562: put
  db->Put(write_options, "key2446", "value95125");

  // op 49563: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3288", "key3968", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3288, key3968) size=" << scan_result.size() << std::endl;
  }

  // op 49564: put
  db->Put(write_options, "key7581", "value103820");

  // op 49565: delete
  db->Delete(write_options, "key3703");

  // op 49566: get
  status = db->Get(read_options, "key7634", &value);
  if (status.ok()) {
    out << "GET key7634 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49567: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0392", "key3391", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0392, key3391) size=" << scan_result.size() << std::endl;
  }

  // op 49568: put
  db->Put(write_options, "key0655", "value55169");

  // op 49569: put
  db->Put(write_options, "key6272", "value104595");

  // op 49570: get
  status = db->Get(read_options, "key7597", &value);
  if (status.ok()) {
    out << "GET key7597 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49571: get
  status = db->Get(read_options, "key1375", &value);
  if (status.ok()) {
    out << "GET key1375 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49572: delete_range
  status = db->DeleteRange(write_options, "key3289", "key6311");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 49573: put
  db->Put(write_options, "key9898", "value118240");

  // op 49574: delete
  db->Delete(write_options, "key5336");

  // op 49575: delete_range
  status = db->DeleteRange(write_options, "key4079", "key5697");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 49576: put
  db->Put(write_options, "key3152", "value20180");

  // op 49577: put
  db->Put(write_options, "key10698", "value7155");

  // op 49578: put
  db->Put(write_options, "key8544", "value17514");

  // op 49579: delete
  db->Delete(write_options, "key9341");

  // op 49580: delete
  db->Delete(write_options, "key9995");

  // op 49581: get
  status = db->Get(read_options, "key0007", &value);
  if (status.ok()) {
    out << "GET key0007 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49582: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0517", "key0883", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0517, key0883) size=" << scan_result.size() << std::endl;
  }

  // op 49583: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4531", "key6362", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4531, key6362) size=" << scan_result.size() << std::endl;
  }

  // op 49584: put
  db->Put(write_options, "key4936", "value102768");

  // op 49585: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0493", "key7100", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0493, key7100) size=" << scan_result.size() << std::endl;
  }

  // op 49586: put
  db->Put(write_options, "key3926", "value99380");

  // op 49587: put
  db->Put(write_options, "key7705", "value5023");

  // op 49588: get
  status = db->Get(read_options, "key11530", &value);
  if (status.ok()) {
    out << "GET key11530 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49589: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0147", "key5812", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0147, key5812) size=" << scan_result.size() << std::endl;
  }

  // op 49590: delete_range
  status = db->DeleteRange(write_options, "key5677", "key7088");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 49591: put
  db->Put(write_options, "key4505", "value6382");

  // op 49592: get
  status = db->Get(read_options, "key2010", &value);
  if (status.ok()) {
    out << "GET key2010 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49593: put
  db->Put(write_options, "key1152", "value12031");

  // op 49594: delete_range
  status = db->DeleteRange(write_options, "key2540", "key7419");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 49595: delete_range
  status = db->DeleteRange(write_options, "key2071", "key8088");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 49596: get
  status = db->Get(read_options, "key9492", &value);
  if (status.ok()) {
    out << "GET key9492 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49597: delete_range
  status = db->DeleteRange(write_options, "key8967", "key11162");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 49598: get
  status = db->Get(read_options, "key0730", &value);
  if (status.ok()) {
    out << "GET key0730 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49599: delete
  db->Delete(write_options, "key8465");

  // op 49600: put
  db->Put(write_options, "key6926", "value60357");

  // op 49601: put
  db->Put(write_options, "key3212", "value56743");

  // op 49602: put
  db->Put(write_options, "key8078", "value36175");

  // op 49603: put
  db->Put(write_options, "key5311", "value81170");

  // op 49604: get
  status = db->Get(read_options, "key6766", &value);
  if (status.ok()) {
    out << "GET key6766 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49605: get
  status = db->Get(read_options, "key9345", &value);
  if (status.ok()) {
    out << "GET key9345 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49606: put
  db->Put(write_options, "key4123", "value5185");

  // op 49607: put
  db->Put(write_options, "key4652", "value51532");

  // op 49608: put
  db->Put(write_options, "key6499", "value74160");

  // op 49609: delete_range
  status = db->DeleteRange(write_options, "key6102", "key8471");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 49610: get
  status = db->Get(read_options, "key3399", &value);
  if (status.ok()) {
    out << "GET key3399 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49611: delete
  db->Delete(write_options, "key11470");

  // op 49612: put
  db->Put(write_options, "key7830", "value52797");

  // op 49613: delete_range
  status = db->DeleteRange(write_options, "key0926", "key9391");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 49614: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7141", "key9087", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7141, key9087) size=" << scan_result.size() << std::endl;
  }

  // op 49615: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 49616: get
  status = db->Get(read_options, "key4289", &value);
  if (status.ok()) {
    out << "GET key4289 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49617: get
  status = db->Get(read_options, "key8839", &value);
  if (status.ok()) {
    out << "GET key8839 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49618: put
  db->Put(write_options, "key7455", "value49655");

  // op 49619: put
  db->Put(write_options, "key2535", "value67012");

  // op 49620: put
  db->Put(write_options, "key8295", "value111967");

  // op 49621: delete
  db->Delete(write_options, "key11092");

  // op 49622: get
  status = db->Get(read_options, "key5543", &value);
  if (status.ok()) {
    out << "GET key5543 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49623: put
  db->Put(write_options, "key6801", "value76064");

  // op 49624: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0326", "key0977", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0326, key0977) size=" << scan_result.size() << std::endl;
  }

  // op 49625: put
  db->Put(write_options, "key9875", "value69830");

  // op 49626: delete
  db->Delete(write_options, "key11247");

  // op 49627: put
  db->Put(write_options, "key0307", "value12296");

  // op 49628: get
  status = db->Get(read_options, "key2377", &value);
  if (status.ok()) {
    out << "GET key2377 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49629: get
  status = db->Get(read_options, "key7142", &value);
  if (status.ok()) {
    out << "GET key7142 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49630: get
  status = db->Get(read_options, "key7753", &value);
  if (status.ok()) {
    out << "GET key7753 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49631: put
  db->Put(write_options, "key10433", "value1509");

  // op 49632: put
  db->Put(write_options, "key3816", "value6415");

  // op 49633: delete_range
  status = db->DeleteRange(write_options, "key0900", "key8890");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 49634: delete
  db->Delete(write_options, "key2946");

  // op 49635: get
  status = db->Get(read_options, "key0078", &value);
  if (status.ok()) {
    out << "GET key0078 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49636: put
  db->Put(write_options, "key11140", "value56837");

  // op 49637: delete_range
  status = db->DeleteRange(write_options, "key3778", "key4660");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 49638: get
  status = db->Get(read_options, "key7841", &value);
  if (status.ok()) {
    out << "GET key7841 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49639: delete_range
  status = db->DeleteRange(write_options, "key2092", "key10788");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 49640: get
  status = db->Get(read_options, "key10338", &value);
  if (status.ok()) {
    out << "GET key10338 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49641: put
  db->Put(write_options, "key7993", "value27182");

  // op 49642: delete_range
  status = db->DeleteRange(write_options, "key7053", "key9363");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 49643: delete_range
  status = db->DeleteRange(write_options, "key1679", "key11409");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 49644: delete
  db->Delete(write_options, "key3678");

  // op 49645: get
  status = db->Get(read_options, "key4659", &value);
  if (status.ok()) {
    out << "GET key4659 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49646: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5538", "key9826", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5538, key9826) size=" << scan_result.size() << std::endl;
  }

  // op 49647: get
  status = db->Get(read_options, "key9376", &value);
  if (status.ok()) {
    out << "GET key9376 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49648: get
  status = db->Get(read_options, "key10116", &value);
  if (status.ok()) {
    out << "GET key10116 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49649: put
  db->Put(write_options, "key7674", "value51270");

  // op 49650: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9185", "key10890", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9185, key10890) size=" << scan_result.size() << std::endl;
  }

  // op 49651: get
  status = db->Get(read_options, "key10996", &value);
  if (status.ok()) {
    out << "GET key10996 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49652: put
  db->Put(write_options, "key10074", "value83251");

  // op 49653: put
  db->Put(write_options, "key9037", "value93365");

  // op 49654: put
  db->Put(write_options, "key0128", "value95963");

  // op 49655: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0909", "key7438", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0909, key7438) size=" << scan_result.size() << std::endl;
  }

  // op 49656: get
  status = db->Get(read_options, "key4339", &value);
  if (status.ok()) {
    out << "GET key4339 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49657: delete
  db->Delete(write_options, "key1625");

  // op 49658: get
  status = db->Get(read_options, "key1755", &value);
  if (status.ok()) {
    out << "GET key1755 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49659: put
  db->Put(write_options, "key6917", "value11742");

  // op 49660: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 49661: put
  db->Put(write_options, "key6738", "value65783");

  // op 49662: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 49663: get
  status = db->Get(read_options, "key2071", &value);
  if (status.ok()) {
    out << "GET key2071 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49664: put
  db->Put(write_options, "key5079", "value10316");

  // op 49665: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1711", "key9751", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1711, key9751) size=" << scan_result.size() << std::endl;
  }

  // op 49666: put
  db->Put(write_options, "key1868", "value79781");

  // op 49667: get
  status = db->Get(read_options, "key1505", &value);
  if (status.ok()) {
    out << "GET key1505 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49668: delete
  db->Delete(write_options, "key8022");

  // op 49669: get
  status = db->Get(read_options, "key5010", &value);
  if (status.ok()) {
    out << "GET key5010 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49670: put
  db->Put(write_options, "key9079", "value106216");

  // op 49671: get
  status = db->Get(read_options, "key9551", &value);
  if (status.ok()) {
    out << "GET key9551 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49672: get
  status = db->Get(read_options, "key6073", &value);
  if (status.ok()) {
    out << "GET key6073 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49673: put
  db->Put(write_options, "key0315", "value104685");

  // op 49674: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6810", "key8913", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6810, key8913) size=" << scan_result.size() << std::endl;
  }

  // op 49675: put
  db->Put(write_options, "key10201", "value11816");

  // op 49676: get
  status = db->Get(read_options, "key5713", &value);
  if (status.ok()) {
    out << "GET key5713 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49677: put
  db->Put(write_options, "key4025", "value111227");

  // op 49678: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1074", "key7876", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1074, key7876) size=" << scan_result.size() << std::endl;
  }

  // op 49679: delete_range
  status = db->DeleteRange(write_options, "key6245", "key9657");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 49680: put
  db->Put(write_options, "key8415", "value90083");

  // op 49681: delete
  db->Delete(write_options, "key6237");

  // op 49682: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6006", "key6211", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6006, key6211) size=" << scan_result.size() << std::endl;
  }

  // op 49683: get
  status = db->Get(read_options, "key9355", &value);
  if (status.ok()) {
    out << "GET key9355 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49684: get
  status = db->Get(read_options, "key9539", &value);
  if (status.ok()) {
    out << "GET key9539 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49685: get
  status = db->Get(read_options, "key6139", &value);
  if (status.ok()) {
    out << "GET key6139 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49686: put
  db->Put(write_options, "key3311", "value5736");

  // op 49687: put
  db->Put(write_options, "key4323", "value18250");

  // op 49688: delete_range
  status = db->DeleteRange(write_options, "key0757", "key2283");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 49689: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5683", "key6329", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5683, key6329) size=" << scan_result.size() << std::endl;
  }

  // op 49690: delete
  db->Delete(write_options, "key11129");

  // op 49691: get
  status = db->Get(read_options, "key4832", &value);
  if (status.ok()) {
    out << "GET key4832 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49692: delete_range
  status = db->DeleteRange(write_options, "key0814", "key2013");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 49693: get
  status = db->Get(read_options, "key3009", &value);
  if (status.ok()) {
    out << "GET key3009 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49694: put
  db->Put(write_options, "key3645", "value61227");

  // op 49695: put
  db->Put(write_options, "key10593", "value28610");

  // op 49696: get
  status = db->Get(read_options, "key4530", &value);
  if (status.ok()) {
    out << "GET key4530 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49697: put
  db->Put(write_options, "key11671", "value45444");

  // op 49698: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1113", "key8284", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1113, key8284) size=" << scan_result.size() << std::endl;
  }

  // op 49699: get
  status = db->Get(read_options, "key8008", &value);
  if (status.ok()) {
    out << "GET key8008 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49700: get
  status = db->Get(read_options, "key5533", &value);
  if (status.ok()) {
    out << "GET key5533 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49701: get
  status = db->Get(read_options, "key6035", &value);
  if (status.ok()) {
    out << "GET key6035 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49702: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 49703: put
  db->Put(write_options, "key10052", "value4827");

  // op 49704: put
  db->Put(write_options, "key6663", "value100832");

  // op 49705: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3569", "key5269", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3569, key5269) size=" << scan_result.size() << std::endl;
  }

  // op 49706: put
  db->Put(write_options, "key6198", "value89816");

  // op 49707: get
  status = db->Get(read_options, "key10475", &value);
  if (status.ok()) {
    out << "GET key10475 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49708: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4870", "key4962", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4870, key4962) size=" << scan_result.size() << std::endl;
  }

  // op 49709: get
  status = db->Get(read_options, "key7596", &value);
  if (status.ok()) {
    out << "GET key7596 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49710: delete
  db->Delete(write_options, "key6593");

  // op 49711: get
  status = db->Get(read_options, "key3141", &value);
  if (status.ok()) {
    out << "GET key3141 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49712: delete_range
  status = db->DeleteRange(write_options, "key3076", "key4802");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 49713: get
  status = db->Get(read_options, "key11450", &value);
  if (status.ok()) {
    out << "GET key11450 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49714: delete_range
  status = db->DeleteRange(write_options, "key6214", "key7540");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 49715: get
  status = db->Get(read_options, "key10583", &value);
  if (status.ok()) {
    out << "GET key10583 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49716: delete_range
  status = db->DeleteRange(write_options, "key6304", "key8871");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 49717: put
  db->Put(write_options, "key8760", "value26033");

  // op 49718: get
  status = db->Get(read_options, "key4027", &value);
  if (status.ok()) {
    out << "GET key4027 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49719: put
  db->Put(write_options, "key10937", "value58903");

  // op 49720: put
  db->Put(write_options, "key0420", "value67927");

  // op 49721: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1420", "key11465", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1420, key11465) size=" << scan_result.size() << std::endl;
  }

  // op 49722: put
  db->Put(write_options, "key11208", "value75719");

  // op 49723: delete_range
  status = db->DeleteRange(write_options, "key7939", "key10786");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 49724: put
  db->Put(write_options, "key3928", "value81385");

  // op 49725: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0218", "key1514", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0218, key1514) size=" << scan_result.size() << std::endl;
  }

  // op 49726: put
  db->Put(write_options, "key1933", "value94596");

  // op 49727: put
  db->Put(write_options, "key5883", "value74183");

  // op 49728: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0533", "key0971", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0533, key0971) size=" << scan_result.size() << std::endl;
  }

  // op 49729: put
  db->Put(write_options, "key2534", "value91872");

  // op 49730: get
  status = db->Get(read_options, "key1648", &value);
  if (status.ok()) {
    out << "GET key1648 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49731: put
  db->Put(write_options, "key4243", "value77536");

  // op 49732: get
  status = db->Get(read_options, "key6815", &value);
  if (status.ok()) {
    out << "GET key6815 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49733: put
  db->Put(write_options, "key2858", "value88231");

  // op 49734: put
  db->Put(write_options, "key10541", "value1905");

  // op 49735: delete
  db->Delete(write_options, "key5153");

  // op 49736: put
  db->Put(write_options, "key7416", "value1003");

  // op 49737: put
  db->Put(write_options, "key9200", "value1760");

  // op 49738: delete
  db->Delete(write_options, "key9283");

  // op 49739: put
  db->Put(write_options, "key3471", "value39446");

  // op 49740: delete_range
  status = db->DeleteRange(write_options, "key7390", "key7708");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 49741: put
  db->Put(write_options, "key8120", "value79471");

  // op 49742: get
  status = db->Get(read_options, "key8171", &value);
  if (status.ok()) {
    out << "GET key8171 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49743: put
  db->Put(write_options, "key7012", "value29199");

  // op 49744: delete
  db->Delete(write_options, "key10071");

  // op 49745: put
  db->Put(write_options, "key9074", "value89391");

  // op 49746: get
  status = db->Get(read_options, "key7407", &value);
  if (status.ok()) {
    out << "GET key7407 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49747: put
  db->Put(write_options, "key5429", "value86681");

  // op 49748: scan
  scan_result.clear();
  status = db->Scan(read_options, "key11091", "key11242", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key11091, key11242) size=" << scan_result.size() << std::endl;
  }

  // op 49749: put
  db->Put(write_options, "key6136", "value44690");

  // op 49750: put
  db->Put(write_options, "key3591", "value44766");

  // op 49751: put
  db->Put(write_options, "key11150", "value116145");

  // op 49752: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3585", "key9582", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3585, key9582) size=" << scan_result.size() << std::endl;
  }

  // op 49753: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9963", "key10567", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9963, key10567) size=" << scan_result.size() << std::endl;
  }

  // op 49754: delete
  db->Delete(write_options, "key0540");

  // op 49755: put
  db->Put(write_options, "key3860", "value18620");

  // op 49756: put
  db->Put(write_options, "key11172", "value72780");

  // op 49757: put
  db->Put(write_options, "key3638", "value42830");

  // op 49758: get
  status = db->Get(read_options, "key6548", &value);
  if (status.ok()) {
    out << "GET key6548 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49759: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1854", "key4239", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1854, key4239) size=" << scan_result.size() << std::endl;
  }

  // op 49760: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 49761: delete
  db->Delete(write_options, "key6819");

  // op 49762: put
  db->Put(write_options, "key8381", "value89708");

  // op 49763: put
  db->Put(write_options, "key10101", "value59112");

  // op 49764: put
  db->Put(write_options, "key4268", "value30867");

  // op 49765: get
  status = db->Get(read_options, "key3400", &value);
  if (status.ok()) {
    out << "GET key3400 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49766: put
  db->Put(write_options, "key2538", "value44587");

  // op 49767: put
  db->Put(write_options, "key1406", "value49696");

  // op 49768: put
  db->Put(write_options, "key7163", "value99170");

  // op 49769: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 49770: put
  db->Put(write_options, "key8679", "value92325");

  // op 49771: delete
  db->Delete(write_options, "key11141");

  // op 49772: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7999", "key11068", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7999, key11068) size=" << scan_result.size() << std::endl;
  }

  // op 49773: put
  db->Put(write_options, "key9273", "value65441");

  // op 49774: get
  status = db->Get(read_options, "key3038", &value);
  if (status.ok()) {
    out << "GET key3038 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49775: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2111", "key10800", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2111, key10800) size=" << scan_result.size() << std::endl;
  }

  // op 49776: delete
  db->Delete(write_options, "key3212");

  // op 49777: delete
  db->Delete(write_options, "key5507");

  // op 49778: put
  db->Put(write_options, "key3326", "value0235");

  // op 49779: get
  status = db->Get(read_options, "key1939", &value);
  if (status.ok()) {
    out << "GET key1939 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49780: get
  status = db->Get(read_options, "key3475", &value);
  if (status.ok()) {
    out << "GET key3475 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49781: get
  status = db->Get(read_options, "key9477", &value);
  if (status.ok()) {
    out << "GET key9477 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49782: put
  db->Put(write_options, "key3607", "value67594");

  // op 49783: put
  db->Put(write_options, "key5434", "value12380");

  // op 49784: delete
  db->Delete(write_options, "key3437");

  // op 49785: put
  db->Put(write_options, "key3057", "value15442");

  // op 49786: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4382", "key6634", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4382, key6634) size=" << scan_result.size() << std::endl;
  }

  // op 49787: delete_range
  status = db->DeleteRange(write_options, "key4585", "key10029");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 49788: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 49789: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4404", "key4463", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4404, key4463) size=" << scan_result.size() << std::endl;
  }

  // op 49790: put
  db->Put(write_options, "key2570", "value109355");

  // op 49791: put
  db->Put(write_options, "key8754", "value46803");

  // op 49792: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 49793: put
  db->Put(write_options, "key5068", "value49932");

  // op 49794: get
  status = db->Get(read_options, "key7938", &value);
  if (status.ok()) {
    out << "GET key7938 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49795: put
  db->Put(write_options, "key8684", "value115267");

  // op 49796: put
  db->Put(write_options, "key3738", "value76123");

  // op 49797: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6662", "key11208", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6662, key11208) size=" << scan_result.size() << std::endl;
  }

  // op 49798: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 49799: get
  status = db->Get(read_options, "key10292", &value);
  if (status.ok()) {
    out << "GET key10292 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49800: get
  status = db->Get(read_options, "key3845", &value);
  if (status.ok()) {
    out << "GET key3845 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49801: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5311", "key9092", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5311, key9092) size=" << scan_result.size() << std::endl;
  }

  // op 49802: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3325", "key7421", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3325, key7421) size=" << scan_result.size() << std::endl;
  }

  // op 49803: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4718", "key6148", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4718, key6148) size=" << scan_result.size() << std::endl;
  }

  // op 49804: get
  status = db->Get(read_options, "key8963", &value);
  if (status.ok()) {
    out << "GET key8963 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49805: delete
  db->Delete(write_options, "key4094");

  // op 49806: put
  db->Put(write_options, "key11555", "value90689");

  // op 49807: get
  status = db->Get(read_options, "key0970", &value);
  if (status.ok()) {
    out << "GET key0970 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49808: delete_range
  status = db->DeleteRange(write_options, "key5364", "key11303");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 49809: put
  db->Put(write_options, "key1712", "value38123");

  // op 49810: delete_range
  status = db->DeleteRange(write_options, "key1853", "key1872");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 49811: delete
  db->Delete(write_options, "key8788");

  // op 49812: put
  db->Put(write_options, "key6171", "value36130");

  // op 49813: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 49814: get
  status = db->Get(read_options, "key1006", &value);
  if (status.ok()) {
    out << "GET key1006 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49815: put
  db->Put(write_options, "key1213", "value88313");

  // op 49816: put
  db->Put(write_options, "key2070", "value80566");

  // op 49817: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1195", "key3990", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1195, key3990) size=" << scan_result.size() << std::endl;
  }

  // op 49818: put
  db->Put(write_options, "key3154", "value3459");

  // op 49819: put
  db->Put(write_options, "key2614", "value91972");

  // op 49820: put
  db->Put(write_options, "key9086", "value81553");

  // op 49821: get
  status = db->Get(read_options, "key8356", &value);
  if (status.ok()) {
    out << "GET key8356 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49822: put
  db->Put(write_options, "key4149", "value61243");

  // op 49823: get
  status = db->Get(read_options, "key8515", &value);
  if (status.ok()) {
    out << "GET key8515 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49824: put
  db->Put(write_options, "key1351", "value0117");

  // op 49825: delete
  db->Delete(write_options, "key11653");

  // op 49826: delete_range
  status = db->DeleteRange(write_options, "key3298", "key8581");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 49827: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 49828: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6397", "key6618", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6397, key6618) size=" << scan_result.size() << std::endl;
  }

  // op 49829: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6620", "key7723", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6620, key7723) size=" << scan_result.size() << std::endl;
  }

  // op 49830: put
  db->Put(write_options, "key9744", "value60360");

  // op 49831: put
  db->Put(write_options, "key7826", "value43856");

  // op 49832: put
  db->Put(write_options, "key1901", "value112249");

  // op 49833: get
  status = db->Get(read_options, "key0029", &value);
  if (status.ok()) {
    out << "GET key0029 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49834: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1956", "key3197", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1956, key3197) size=" << scan_result.size() << std::endl;
  }

  // op 49835: put
  db->Put(write_options, "key8879", "value70154");

  // op 49836: delete_range
  status = db->DeleteRange(write_options, "key5141", "key6940");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 49837: get
  status = db->Get(read_options, "key2086", &value);
  if (status.ok()) {
    out << "GET key2086 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49838: delete_range
  status = db->DeleteRange(write_options, "key3750", "key10537");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 49839: get
  status = db->Get(read_options, "key2750", &value);
  if (status.ok()) {
    out << "GET key2750 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49840: put
  db->Put(write_options, "key4435", "value4204");

  // op 49841: put
  db->Put(write_options, "key7873", "value49169");

  // op 49842: put
  db->Put(write_options, "key2208", "value9312");

  // op 49843: put
  db->Put(write_options, "key7346", "value15438");

  // op 49844: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3335", "key3898", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3335, key3898) size=" << scan_result.size() << std::endl;
  }

  // op 49845: get
  status = db->Get(read_options, "key0014", &value);
  if (status.ok()) {
    out << "GET key0014 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49846: get
  status = db->Get(read_options, "key5344", &value);
  if (status.ok()) {
    out << "GET key5344 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49847: get
  status = db->Get(read_options, "key9768", &value);
  if (status.ok()) {
    out << "GET key9768 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49848: put
  db->Put(write_options, "key3286", "value94515");

  // op 49849: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3833", "key10029", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3833, key10029) size=" << scan_result.size() << std::endl;
  }

  // op 49850: delete
  db->Delete(write_options, "key8322");

  // op 49851: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 49852: get
  status = db->Get(read_options, "key4755", &value);
  if (status.ok()) {
    out << "GET key4755 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49853: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 49854: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1209", "key5960", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1209, key5960) size=" << scan_result.size() << std::endl;
  }

  // op 49855: delete
  db->Delete(write_options, "key8475");

  // op 49856: put
  db->Put(write_options, "key3237", "value80033");

  // op 49857: put
  db->Put(write_options, "key0191", "value68451");

  // op 49858: delete
  db->Delete(write_options, "key9430");

  // op 49859: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1697", "key3688", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1697, key3688) size=" << scan_result.size() << std::endl;
  }

  // op 49860: delete
  db->Delete(write_options, "key4453");

  // op 49861: get
  status = db->Get(read_options, "key0917", &value);
  if (status.ok()) {
    out << "GET key0917 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49862: put
  db->Put(write_options, "key5953", "value54894");

  // op 49863: put
  db->Put(write_options, "key0879", "value48119");

  // op 49864: delete_range
  status = db->DeleteRange(write_options, "key4244", "key5764");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 49865: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8071", "key10029", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8071, key10029) size=" << scan_result.size() << std::endl;
  }

  // op 49866: delete
  db->Delete(write_options, "key10110");

  // op 49867: put
  db->Put(write_options, "key2978", "value57163");

  // op 49868: delete_range
  status = db->DeleteRange(write_options, "key2238", "key6283");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 49869: delete_range
  status = db->DeleteRange(write_options, "key0717", "key10321");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 49870: delete_range
  status = db->DeleteRange(write_options, "key3406", "key9994");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 49871: get
  status = db->Get(read_options, "key6643", &value);
  if (status.ok()) {
    out << "GET key6643 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49872: delete_range
  status = db->DeleteRange(write_options, "key6828", "key10713");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 49873: put
  db->Put(write_options, "key7644", "value67409");

  // op 49874: put
  db->Put(write_options, "key10297", "value90774");

  // op 49875: put
  db->Put(write_options, "key3819", "value63046");

  // op 49876: get
  status = db->Get(read_options, "key4242", &value);
  if (status.ok()) {
    out << "GET key4242 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49877: get
  status = db->Get(read_options, "key8400", &value);
  if (status.ok()) {
    out << "GET key8400 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49878: delete
  db->Delete(write_options, "key11548");

  // op 49879: put
  db->Put(write_options, "key3553", "value27831");

  // op 49880: get
  status = db->Get(read_options, "key0586", &value);
  if (status.ok()) {
    out << "GET key0586 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49881: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3387", "key8190", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3387, key8190) size=" << scan_result.size() << std::endl;
  }

  // op 49882: get
  status = db->Get(read_options, "key4391", &value);
  if (status.ok()) {
    out << "GET key4391 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49883: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 49884: delete
  db->Delete(write_options, "key6980");

  // op 49885: put
  db->Put(write_options, "key7752", "value69274");

  // op 49886: put
  db->Put(write_options, "key10241", "value102266");

  // op 49887: delete_range
  status = db->DeleteRange(write_options, "key0547", "key1625");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 49888: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6290", "key9259", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6290, key9259) size=" << scan_result.size() << std::endl;
  }

  // op 49889: put
  db->Put(write_options, "key6709", "value45410");

  // op 49890: put
  db->Put(write_options, "key7531", "value113795");

  // op 49891: get
  status = db->Get(read_options, "key3432", &value);
  if (status.ok()) {
    out << "GET key3432 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49892: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2602", "key9640", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2602, key9640) size=" << scan_result.size() << std::endl;
  }

  // op 49893: put
  db->Put(write_options, "key3901", "value84968");

  // op 49894: delete_range
  status = db->DeleteRange(write_options, "key6345", "key8192");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 49895: get
  status = db->Get(read_options, "key9554", &value);
  if (status.ok()) {
    out << "GET key9554 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49896: put
  db->Put(write_options, "key1042", "value95693");

  // op 49897: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2358", "key3982", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2358, key3982) size=" << scan_result.size() << std::endl;
  }

  // op 49898: get
  status = db->Get(read_options, "key9750", &value);
  if (status.ok()) {
    out << "GET key9750 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49899: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5827", "key8750", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5827, key8750) size=" << scan_result.size() << std::endl;
  }

  // op 49900: get
  status = db->Get(read_options, "key5097", &value);
  if (status.ok()) {
    out << "GET key5097 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49901: put
  db->Put(write_options, "key7234", "value31748");

  // op 49902: delete_range
  status = db->DeleteRange(write_options, "key2523", "key8863");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 49903: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1374", "key7300", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1374, key7300) size=" << scan_result.size() << std::endl;
  }

  // op 49904: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8735", "key10877", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8735, key10877) size=" << scan_result.size() << std::endl;
  }

  // op 49905: put
  db->Put(write_options, "key2110", "value20085");

  // op 49906: put
  db->Put(write_options, "key4577", "value105784");

  // op 49907: put
  db->Put(write_options, "key8563", "value112218");

  // op 49908: delete_range
  status = db->DeleteRange(write_options, "key4982", "key7143");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 49909: put
  db->Put(write_options, "key1570", "value57772");

  // op 49910: put
  db->Put(write_options, "key9188", "value39930");

  // op 49911: put
  db->Put(write_options, "key9893", "value76367");

  // op 49912: put
  db->Put(write_options, "key0797", "value25913");

  // op 49913: put
  db->Put(write_options, "key6935", "value21279");

  // op 49914: put
  db->Put(write_options, "key7384", "value110872");

  // op 49915: put
  db->Put(write_options, "key5843", "value32641");

  // op 49916: delete
  db->Delete(write_options, "key1690");

  // op 49917: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6591", "key11431", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6591, key11431) size=" << scan_result.size() << std::endl;
  }

  // op 49918: put
  db->Put(write_options, "key8135", "value20359");

  // op 49919: get
  status = db->Get(read_options, "key6499", &value);
  if (status.ok()) {
    out << "GET key6499 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49920: get
  status = db->Get(read_options, "key6392", &value);
  if (status.ok()) {
    out << "GET key6392 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49921: put
  db->Put(write_options, "key7625", "value114305");

  // op 49922: get
  status = db->Get(read_options, "key7979", &value);
  if (status.ok()) {
    out << "GET key7979 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49923: get
  status = db->Get(read_options, "key0452", &value);
  if (status.ok()) {
    out << "GET key0452 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49924: put
  db->Put(write_options, "key8924", "value88975");

  // op 49925: delete
  db->Delete(write_options, "key5821");

  // op 49926: get
  status = db->Get(read_options, "key4744", &value);
  if (status.ok()) {
    out << "GET key4744 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49927: get
  status = db->Get(read_options, "key2272", &value);
  if (status.ok()) {
    out << "GET key2272 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49928: put
  db->Put(write_options, "key6827", "value73852");

  // op 49929: delete
  db->Delete(write_options, "key6062");

  // op 49930: get
  status = db->Get(read_options, "key4974", &value);
  if (status.ok()) {
    out << "GET key4974 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49931: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4394", "key10710", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4394, key10710) size=" << scan_result.size() << std::endl;
  }

  // op 49932: put
  db->Put(write_options, "key1208", "value52532");

  // op 49933: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1216", "key3243", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1216, key3243) size=" << scan_result.size() << std::endl;
  }

  // op 49934: put
  db->Put(write_options, "key7994", "value51181");

  // op 49935: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6391", "key9415", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6391, key9415) size=" << scan_result.size() << std::endl;
  }

  // op 49936: put
  db->Put(write_options, "key11411", "value9186");

  // op 49937: delete
  db->Delete(write_options, "key0531");

  // op 49938: get
  status = db->Get(read_options, "key10715", &value);
  if (status.ok()) {
    out << "GET key10715 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49939: get
  status = db->Get(read_options, "key7254", &value);
  if (status.ok()) {
    out << "GET key7254 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49940: put
  db->Put(write_options, "key6930", "value8634");

  // op 49941: delete_range
  status = db->DeleteRange(write_options, "key2612", "key4278");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 49942: get
  status = db->Get(read_options, "key1689", &value);
  if (status.ok()) {
    out << "GET key1689 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49943: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2968", "key4740", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2968, key4740) size=" << scan_result.size() << std::endl;
  }

  // op 49944: get
  status = db->Get(read_options, "key2422", &value);
  if (status.ok()) {
    out << "GET key2422 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49945: put
  db->Put(write_options, "key1790", "value53856");

  // op 49946: put
  db->Put(write_options, "key3296", "value78176");

  // op 49947: put
  db->Put(write_options, "key4392", "value12792");

  // op 49948: put
  db->Put(write_options, "key3597", "value73801");

  // op 49949: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10796", "key11475", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10796, key11475) size=" << scan_result.size() << std::endl;
  }

  // op 49950: get
  status = db->Get(read_options, "key3992", &value);
  if (status.ok()) {
    out << "GET key3992 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49951: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5045", "key10634", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5045, key10634) size=" << scan_result.size() << std::endl;
  }

  // op 49952: put
  db->Put(write_options, "key2379", "value33682");

  // op 49953: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1723", "key10700", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1723, key10700) size=" << scan_result.size() << std::endl;
  }

  // op 49954: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1344", "key2791", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1344, key2791) size=" << scan_result.size() << std::endl;
  }

  // op 49955: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1776", "key5877", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1776, key5877) size=" << scan_result.size() << std::endl;
  }

  // op 49956: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 49957: put
  db->Put(write_options, "key8614", "value81783");

  // op 49958: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4670", "key5946", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4670, key5946) size=" << scan_result.size() << std::endl;
  }

  // op 49959: put
  db->Put(write_options, "key5634", "value50000");

  // op 49960: put
  db->Put(write_options, "key8525", "value93866");

  // op 49961: get
  status = db->Get(read_options, "key10666", &value);
  if (status.ok()) {
    out << "GET key10666 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49962: get
  status = db->Get(read_options, "key3177", &value);
  if (status.ok()) {
    out << "GET key3177 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49963: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 49964: delete
  db->Delete(write_options, "key2943");

  // op 49965: get
  status = db->Get(read_options, "key5621", &value);
  if (status.ok()) {
    out << "GET key5621 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49966: put
  db->Put(write_options, "key9373", "value65537");

  // op 49967: delete
  db->Delete(write_options, "key9928");

  // op 49968: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 49969: get
  status = db->Get(read_options, "key10091", &value);
  if (status.ok()) {
    out << "GET key10091 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49970: get
  status = db->Get(read_options, "key3382", &value);
  if (status.ok()) {
    out << "GET key3382 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49971: get
  status = db->Get(read_options, "key4361", &value);
  if (status.ok()) {
    out << "GET key4361 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49972: put
  db->Put(write_options, "key7830", "value95092");

  // op 49973: put
  db->Put(write_options, "key0838", "value24675");

  // op 49974: put
  db->Put(write_options, "key0431", "value106959");

  // op 49975: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4353", "key11037", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4353, key11037) size=" << scan_result.size() << std::endl;
  }

  // op 49976: delete_range
  status = db->DeleteRange(write_options, "key1808", "key7529");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 49977: get
  status = db->Get(read_options, "key8033", &value);
  if (status.ok()) {
    out << "GET key8033 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49978: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0000", "key1309", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0000, key1309) size=" << scan_result.size() << std::endl;
  }

  // op 49979: put
  db->Put(write_options, "key6207", "value96476");

  // op 49980: get
  status = db->Get(read_options, "key0350", &value);
  if (status.ok()) {
    out << "GET key0350 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49981: put
  db->Put(write_options, "key3283", "value44140");

  // op 49982: put
  db->Put(write_options, "key0471", "value11363");

  // op 49983: put
  db->Put(write_options, "key9382", "value37055");

  // op 49984: put
  db->Put(write_options, "key4808", "value59237");

  // op 49985: delete
  db->Delete(write_options, "key1666");

  // op 49986: get
  status = db->Get(read_options, "key3972", &value);
  if (status.ok()) {
    out << "GET key3972 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49987: delete_range
  status = db->DeleteRange(write_options, "key0635", "key3022");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 49988: put
  db->Put(write_options, "key8641", "value109109");

  // op 49989: delete_range
  status = db->DeleteRange(write_options, "key0870", "key5343");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 49990: put
  db->Put(write_options, "key1145", "value95069");

  // op 49991: put
  db->Put(write_options, "key1899", "value53969");

  // op 49992: put
  db->Put(write_options, "key11578", "value49546");

  // op 49993: put
  db->Put(write_options, "key10658", "value97529");

  // op 49994: put
  db->Put(write_options, "key2068", "value111476");

  // op 49995: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0217", "key9895", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0217, key9895) size=" << scan_result.size() << std::endl;
  }

  // op 49996: delete
  db->Delete(write_options, "key7110");

  // op 49997: put
  db->Put(write_options, "key9509", "value93999");

  // op 49998: get
  status = db->Get(read_options, "key5805", &value);
  if (status.ok()) {
    out << "GET key5805 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 49999: put
  db->Put(write_options, "key10088", "value78428");

  // op 50000: put
  db->Put(write_options, "key9094", "value58753");

  // op 50001: delete_range
  status = db->DeleteRange(write_options, "key7860", "key7951");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 50002: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0383", "key11391", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0383, key11391) size=" << scan_result.size() << std::endl;
  }

  // op 50003: get
  status = db->Get(read_options, "key5061", &value);
  if (status.ok()) {
    out << "GET key5061 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50004: put
  db->Put(write_options, "key5138", "value65353");

  // op 50005: put
  db->Put(write_options, "key9819", "value54313");

  // op 50006: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4734", "key11110", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4734, key11110) size=" << scan_result.size() << std::endl;
  }

  // op 50007: put
  db->Put(write_options, "key6943", "value93013");

  // op 50008: delete
  db->Delete(write_options, "key0087");

  // op 50009: put
  db->Put(write_options, "key7572", "value8371");

  // op 50010: put
  db->Put(write_options, "key11726", "value63906");

  // op 50011: delete_range
  status = db->DeleteRange(write_options, "key3984", "key7586");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 50012: delete_range
  status = db->DeleteRange(write_options, "key8149", "key8897");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 50013: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4189", "key5262", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4189, key5262) size=" << scan_result.size() << std::endl;
  }

  // op 50014: put
  db->Put(write_options, "key7312", "value86985");

  // op 50015: delete
  db->Delete(write_options, "key6087");

  // op 50016: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1755", "key5799", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1755, key5799) size=" << scan_result.size() << std::endl;
  }

  // op 50017: put
  db->Put(write_options, "key3093", "value111215");

  // op 50018: get
  status = db->Get(read_options, "key2425", &value);
  if (status.ok()) {
    out << "GET key2425 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50019: put
  db->Put(write_options, "key7719", "value23870");

  // op 50020: delete_range
  status = db->DeleteRange(write_options, "key2391", "key11595");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 50021: delete_range
  status = db->DeleteRange(write_options, "key4546", "key7348");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 50022: delete_range
  status = db->DeleteRange(write_options, "key1806", "key4900");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 50023: put
  db->Put(write_options, "key0407", "value66427");

  // op 50024: put
  db->Put(write_options, "key9015", "value50544");

  // op 50025: put
  db->Put(write_options, "key0493", "value80170");

  // op 50026: put
  db->Put(write_options, "key9614", "value14818");

  // op 50027: delete
  db->Delete(write_options, "key6955");

  // op 50028: put
  db->Put(write_options, "key3120", "value30854");

  // op 50029: get
  status = db->Get(read_options, "key5802", &value);
  if (status.ok()) {
    out << "GET key5802 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50030: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0145", "key11087", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0145, key11087) size=" << scan_result.size() << std::endl;
  }

  // op 50031: put
  db->Put(write_options, "key8332", "value101693");

  // op 50032: get
  status = db->Get(read_options, "key8754", &value);
  if (status.ok()) {
    out << "GET key8754 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50033: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 50034: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 50035: get
  status = db->Get(read_options, "key7307", &value);
  if (status.ok()) {
    out << "GET key7307 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50036: put
  db->Put(write_options, "key10726", "value7948");

  // op 50037: get
  status = db->Get(read_options, "key0710", &value);
  if (status.ok()) {
    out << "GET key0710 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50038: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 50039: delete
  db->Delete(write_options, "key0812");

  // op 50040: get
  status = db->Get(read_options, "key1924", &value);
  if (status.ok()) {
    out << "GET key1924 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50041: put
  db->Put(write_options, "key3534", "value44753");

  // op 50042: delete
  db->Delete(write_options, "key2774");

  // op 50043: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1725", "key1744", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1725, key1744) size=" << scan_result.size() << std::endl;
  }

  // op 50044: put
  db->Put(write_options, "key1183", "value22296");

  // op 50045: delete
  db->Delete(write_options, "key0118");

  // op 50046: get
  status = db->Get(read_options, "key1894", &value);
  if (status.ok()) {
    out << "GET key1894 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50047: put
  db->Put(write_options, "key10787", "value0721");

  // op 50048: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2386", "key10928", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2386, key10928) size=" << scan_result.size() << std::endl;
  }

  // op 50049: put
  db->Put(write_options, "key10022", "value99287");

  // op 50050: get
  status = db->Get(read_options, "key4257", &value);
  if (status.ok()) {
    out << "GET key4257 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50051: put
  db->Put(write_options, "key5687", "value23339");

  // op 50052: get
  status = db->Get(read_options, "key5399", &value);
  if (status.ok()) {
    out << "GET key5399 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50053: put
  db->Put(write_options, "key9914", "value86741");

  // op 50054: put
  db->Put(write_options, "key1356", "value74745");

  // op 50055: delete_range
  status = db->DeleteRange(write_options, "key5727", "key11004");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 50056: delete_range
  status = db->DeleteRange(write_options, "key0699", "key3478");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 50057: put
  db->Put(write_options, "key0950", "value81848");

  // op 50058: put
  db->Put(write_options, "key9484", "value0050");

  // op 50059: put
  db->Put(write_options, "key10984", "value100650");

  // op 50060: get
  status = db->Get(read_options, "key8777", &value);
  if (status.ok()) {
    out << "GET key8777 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50061: delete_range
  status = db->DeleteRange(write_options, "key1843", "key4317");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 50062: delete
  db->Delete(write_options, "key8092");

  // op 50063: delete_range
  status = db->DeleteRange(write_options, "key9128", "key9245");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 50064: put
  db->Put(write_options, "key10182", "value109547");

  // op 50065: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1579", "key9782", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1579, key9782) size=" << scan_result.size() << std::endl;
  }

  // op 50066: get
  status = db->Get(read_options, "key2712", &value);
  if (status.ok()) {
    out << "GET key2712 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50067: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8517", "key11544", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8517, key11544) size=" << scan_result.size() << std::endl;
  }

  // op 50068: delete
  db->Delete(write_options, "key7388");

  // op 50069: put
  db->Put(write_options, "key1778", "value75881");

  // op 50070: get
  status = db->Get(read_options, "key6901", &value);
  if (status.ok()) {
    out << "GET key6901 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50071: put
  db->Put(write_options, "key2701", "value15176");

  // op 50072: put
  db->Put(write_options, "key4458", "value50845");

  // op 50073: delete_range
  status = db->DeleteRange(write_options, "key1045", "key5793");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 50074: put
  db->Put(write_options, "key8041", "value34327");

  // op 50075: put
  db->Put(write_options, "key8154", "value18377");

  // op 50076: get
  status = db->Get(read_options, "key7246", &value);
  if (status.ok()) {
    out << "GET key7246 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50077: get
  status = db->Get(read_options, "key4104", &value);
  if (status.ok()) {
    out << "GET key4104 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50078: delete_range
  status = db->DeleteRange(write_options, "key2570", "key11374");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 50079: delete_range
  status = db->DeleteRange(write_options, "key9427", "key10966");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 50080: put
  db->Put(write_options, "key4942", "value98932");

  // op 50081: delete_range
  status = db->DeleteRange(write_options, "key1923", "key2273");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 50082: put
  db->Put(write_options, "key1185", "value30743");

  // op 50083: delete_range
  status = db->DeleteRange(write_options, "key2142", "key6912");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 50084: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1387", "key11536", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1387, key11536) size=" << scan_result.size() << std::endl;
  }

  // op 50085: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1832", "key3149", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1832, key3149) size=" << scan_result.size() << std::endl;
  }

  // op 50086: put
  db->Put(write_options, "key1593", "value48408");

  // op 50087: get
  status = db->Get(read_options, "key5285", &value);
  if (status.ok()) {
    out << "GET key5285 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50088: put
  db->Put(write_options, "key3255", "value115788");

  // op 50089: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0985", "key7988", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0985, key7988) size=" << scan_result.size() << std::endl;
  }

  // op 50090: get
  status = db->Get(read_options, "key9817", &value);
  if (status.ok()) {
    out << "GET key9817 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50091: get
  status = db->Get(read_options, "key2655", &value);
  if (status.ok()) {
    out << "GET key2655 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50092: get
  status = db->Get(read_options, "key7995", &value);
  if (status.ok()) {
    out << "GET key7995 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50093: delete_range
  status = db->DeleteRange(write_options, "key6283", "key6565");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 50094: delete
  db->Delete(write_options, "key2116");

  // op 50095: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0021", "key1306", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0021, key1306) size=" << scan_result.size() << std::endl;
  }

  // op 50096: delete
  db->Delete(write_options, "key0811");

  // op 50097: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 50098: get
  status = db->Get(read_options, "key8726", &value);
  if (status.ok()) {
    out << "GET key8726 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50099: put
  db->Put(write_options, "key4939", "value53518");

  // op 50100: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2769", "key3402", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2769, key3402) size=" << scan_result.size() << std::endl;
  }

  // op 50101: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2620", "key2840", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2620, key2840) size=" << scan_result.size() << std::endl;
  }

  // op 50102: get
  status = db->Get(read_options, "key6415", &value);
  if (status.ok()) {
    out << "GET key6415 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50103: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 50104: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7212", "key8808", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7212, key8808) size=" << scan_result.size() << std::endl;
  }

  // op 50105: get
  status = db->Get(read_options, "key5647", &value);
  if (status.ok()) {
    out << "GET key5647 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50106: put
  db->Put(write_options, "key11389", "value110160");

  // op 50107: delete
  db->Delete(write_options, "key4325");

  // op 50108: put
  db->Put(write_options, "key10282", "value65758");

  // op 50109: put
  db->Put(write_options, "key3037", "value1170");

  // op 50110: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0546", "key9219", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0546, key9219) size=" << scan_result.size() << std::endl;
  }

  // op 50111: delete_range
  status = db->DeleteRange(write_options, "key0684", "key7887");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 50112: put
  db->Put(write_options, "key2489", "value5894");

  // op 50113: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7888", "key9803", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7888, key9803) size=" << scan_result.size() << std::endl;
  }

  // op 50114: delete_range
  status = db->DeleteRange(write_options, "key10132", "key11009");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 50115: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 50116: put
  db->Put(write_options, "key0838", "value74426");

  // op 50117: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 50118: put
  db->Put(write_options, "key1857", "value37938");

  // op 50119: get
  status = db->Get(read_options, "key0962", &value);
  if (status.ok()) {
    out << "GET key0962 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50120: delete
  db->Delete(write_options, "key8340");

  // op 50121: put
  db->Put(write_options, "key6649", "value52488");

  // op 50122: put
  db->Put(write_options, "key4395", "value88305");

  // op 50123: get
  status = db->Get(read_options, "key7215", &value);
  if (status.ok()) {
    out << "GET key7215 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50124: delete_range
  status = db->DeleteRange(write_options, "key0343", "key2236");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 50125: put
  db->Put(write_options, "key0167", "value74280");

  // op 50126: get
  status = db->Get(read_options, "key6908", &value);
  if (status.ok()) {
    out << "GET key6908 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50127: delete_range
  status = db->DeleteRange(write_options, "key0470", "key8258");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 50128: put
  db->Put(write_options, "key1859", "value116631");

  // op 50129: get
  status = db->Get(read_options, "key7645", &value);
  if (status.ok()) {
    out << "GET key7645 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50130: delete
  db->Delete(write_options, "key1614");

  // op 50131: put
  db->Put(write_options, "key6087", "value97118");

  // op 50132: get
  status = db->Get(read_options, "key2253", &value);
  if (status.ok()) {
    out << "GET key2253 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50133: put
  db->Put(write_options, "key10187", "value4437");

  // op 50134: put
  db->Put(write_options, "key11210", "value115639");

  // op 50135: put
  db->Put(write_options, "key4487", "value56977");

  // op 50136: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0592", "key2211", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0592, key2211) size=" << scan_result.size() << std::endl;
  }

  // op 50137: put
  db->Put(write_options, "key0389", "value113717");

  // op 50138: put
  db->Put(write_options, "key10108", "value63881");

  // op 50139: delete
  db->Delete(write_options, "key6373");

  // op 50140: put
  db->Put(write_options, "key1133", "value68245");

  // op 50141: put
  db->Put(write_options, "key7288", "value65872");

  // op 50142: delete
  db->Delete(write_options, "key2386");

  // op 50143: delete_range
  status = db->DeleteRange(write_options, "key6276", "key8121");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 50144: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 50145: put
  db->Put(write_options, "key5961", "value40905");

  // op 50146: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1395", "key11276", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1395, key11276) size=" << scan_result.size() << std::endl;
  }

  // op 50147: delete_range
  status = db->DeleteRange(write_options, "key7898", "key10511");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 50148: put
  db->Put(write_options, "key5821", "value62922");

  // op 50149: delete
  db->Delete(write_options, "key1267");

  // op 50150: put
  db->Put(write_options, "key0346", "value19551");

  // op 50151: delete
  db->Delete(write_options, "key8389");

  // op 50152: put
  db->Put(write_options, "key7137", "value115050");

  // op 50153: put
  db->Put(write_options, "key5054", "value109373");

  // op 50154: put
  db->Put(write_options, "key4750", "value29597");

  // op 50155: delete
  db->Delete(write_options, "key5818");

  // op 50156: get
  status = db->Get(read_options, "key1262", &value);
  if (status.ok()) {
    out << "GET key1262 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50157: get
  status = db->Get(read_options, "key3995", &value);
  if (status.ok()) {
    out << "GET key3995 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50158: put
  db->Put(write_options, "key3739", "value2536");

  // op 50159: put
  db->Put(write_options, "key7201", "value13538");

  // op 50160: delete_range
  status = db->DeleteRange(write_options, "key6326", "key10078");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 50161: put
  db->Put(write_options, "key6436", "value79814");

  // op 50162: put
  db->Put(write_options, "key2995", "value109181");

  // op 50163: put
  db->Put(write_options, "key4004", "value34267");

  // op 50164: delete_range
  status = db->DeleteRange(write_options, "key2142", "key3492");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 50165: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0594", "key7910", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0594, key7910) size=" << scan_result.size() << std::endl;
  }

  // op 50166: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0094", "key10913", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0094, key10913) size=" << scan_result.size() << std::endl;
  }

  // op 50167: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0337", "key8323", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0337, key8323) size=" << scan_result.size() << std::endl;
  }

  // op 50168: get
  status = db->Get(read_options, "key3582", &value);
  if (status.ok()) {
    out << "GET key3582 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50169: get
  status = db->Get(read_options, "key11616", &value);
  if (status.ok()) {
    out << "GET key11616 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50170: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 50171: delete_range
  status = db->DeleteRange(write_options, "key5035", "key8364");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 50172: put
  db->Put(write_options, "key7337", "value79754");

  // op 50173: put
  db->Put(write_options, "key8902", "value50858");

  // op 50174: delete_range
  status = db->DeleteRange(write_options, "key0074", "key0497");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 50175: get
  status = db->Get(read_options, "key0474", &value);
  if (status.ok()) {
    out << "GET key0474 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50176: put
  db->Put(write_options, "key6518", "value69929");

  // op 50177: get
  status = db->Get(read_options, "key11163", &value);
  if (status.ok()) {
    out << "GET key11163 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50178: get
  status = db->Get(read_options, "key5517", &value);
  if (status.ok()) {
    out << "GET key5517 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50179: put
  db->Put(write_options, "key10124", "value23006");

  // op 50180: delete
  db->Delete(write_options, "key2563");

  // op 50181: get
  status = db->Get(read_options, "key1145", &value);
  if (status.ok()) {
    out << "GET key1145 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50182: get
  status = db->Get(read_options, "key2516", &value);
  if (status.ok()) {
    out << "GET key2516 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50183: delete
  db->Delete(write_options, "key2260");

  // op 50184: get
  status = db->Get(read_options, "key0759", &value);
  if (status.ok()) {
    out << "GET key0759 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50185: delete_range
  status = db->DeleteRange(write_options, "key2430", "key10740");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 50186: put
  db->Put(write_options, "key8084", "value23030");

  // op 50187: put
  db->Put(write_options, "key7782", "value22218");

  // op 50188: put
  db->Put(write_options, "key8591", "value95236");

  // op 50189: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 50190: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4234", "key4956", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4234, key4956) size=" << scan_result.size() << std::endl;
  }

  // op 50191: delete
  db->Delete(write_options, "key3874");

  // op 50192: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7090", "key8247", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7090, key8247) size=" << scan_result.size() << std::endl;
  }

  // op 50193: get
  status = db->Get(read_options, "key9709", &value);
  if (status.ok()) {
    out << "GET key9709 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50194: delete_range
  status = db->DeleteRange(write_options, "key8134", "key8869");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 50195: put
  db->Put(write_options, "key2023", "value29991");

  // op 50196: get
  status = db->Get(read_options, "key6202", &value);
  if (status.ok()) {
    out << "GET key6202 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50197: put
  db->Put(write_options, "key8321", "value112983");

  // op 50198: put
  db->Put(write_options, "key7988", "value77261");

  // op 50199: put
  db->Put(write_options, "key8133", "value56901");

  // op 50200: put
  db->Put(write_options, "key11189", "value30308");

  // op 50201: put
  db->Put(write_options, "key10183", "value31199");

  // op 50202: put
  db->Put(write_options, "key4237", "value60055");

  // op 50203: get
  status = db->Get(read_options, "key11557", &value);
  if (status.ok()) {
    out << "GET key11557 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50204: delete_range
  status = db->DeleteRange(write_options, "key0475", "key3378");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 50205: put
  db->Put(write_options, "key2347", "value77996");

  // op 50206: put
  db->Put(write_options, "key3477", "value71051");

  // op 50207: get
  status = db->Get(read_options, "key4259", &value);
  if (status.ok()) {
    out << "GET key4259 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50208: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7329", "key8834", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7329, key8834) size=" << scan_result.size() << std::endl;
  }

  // op 50209: delete
  db->Delete(write_options, "key10290");

  // op 50210: delete_range
  status = db->DeleteRange(write_options, "key4743", "key8595");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 50211: put
  db->Put(write_options, "key7854", "value80033");

  // op 50212: delete_range
  status = db->DeleteRange(write_options, "key3049", "key7896");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 50213: put
  db->Put(write_options, "key9272", "value20090");

  // op 50214: put
  db->Put(write_options, "key4864", "value23819");

  // op 50215: put
  db->Put(write_options, "key4462", "value78585");

  // op 50216: put
  db->Put(write_options, "key11637", "value68641");

  // op 50217: delete_range
  status = db->DeleteRange(write_options, "key5144", "key11680");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 50218: put
  db->Put(write_options, "key3956", "value2133");

  // op 50219: get
  status = db->Get(read_options, "key5898", &value);
  if (status.ok()) {
    out << "GET key5898 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50220: put
  db->Put(write_options, "key3973", "value78221");

  // op 50221: put
  db->Put(write_options, "key3565", "value3809");

  // op 50222: put
  db->Put(write_options, "key8273", "value114936");

  // op 50223: delete
  db->Delete(write_options, "key3314");

  // op 50224: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 50225: put
  db->Put(write_options, "key1755", "value100243");

  // op 50226: put
  db->Put(write_options, "key11240", "value92297");

  // op 50227: put
  db->Put(write_options, "key6125", "value56670");

  // op 50228: put
  db->Put(write_options, "key10877", "value53116");

  // op 50229: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3264", "key7699", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3264, key7699) size=" << scan_result.size() << std::endl;
  }

  // op 50230: delete_range
  status = db->DeleteRange(write_options, "key1560", "key9321");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 50231: put
  db->Put(write_options, "key6334", "value107198");

  // op 50232: delete_range
  status = db->DeleteRange(write_options, "key6643", "key8032");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 50233: get
  status = db->Get(read_options, "key3140", &value);
  if (status.ok()) {
    out << "GET key3140 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50234: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3078", "key10908", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3078, key10908) size=" << scan_result.size() << std::endl;
  }

  // op 50235: put
  db->Put(write_options, "key8029", "value1544");

  // op 50236: delete
  db->Delete(write_options, "key4328");

  // op 50237: get
  status = db->Get(read_options, "key3376", &value);
  if (status.ok()) {
    out << "GET key3376 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50238: put
  db->Put(write_options, "key4176", "value17983");

  // op 50239: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3070", "key5561", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3070, key5561) size=" << scan_result.size() << std::endl;
  }

  // op 50240: put
  db->Put(write_options, "key4769", "value5165");

  // op 50241: get
  status = db->Get(read_options, "key1419", &value);
  if (status.ok()) {
    out << "GET key1419 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50242: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5293", "key6265", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5293, key6265) size=" << scan_result.size() << std::endl;
  }

  // op 50243: get
  status = db->Get(read_options, "key11642", &value);
  if (status.ok()) {
    out << "GET key11642 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50244: delete_range
  status = db->DeleteRange(write_options, "key4823", "key6626");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 50245: get
  status = db->Get(read_options, "key4174", &value);
  if (status.ok()) {
    out << "GET key4174 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50246: get
  status = db->Get(read_options, "key2814", &value);
  if (status.ok()) {
    out << "GET key2814 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50247: put
  db->Put(write_options, "key3370", "value112448");

  // op 50248: get
  status = db->Get(read_options, "key3482", &value);
  if (status.ok()) {
    out << "GET key3482 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50249: put
  db->Put(write_options, "key1687", "value100512");

  // op 50250: put
  db->Put(write_options, "key4347", "value110073");

  // op 50251: delete_range
  status = db->DeleteRange(write_options, "key9387", "key9564");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 50252: get
  status = db->Get(read_options, "key9686", &value);
  if (status.ok()) {
    out << "GET key9686 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50253: put
  db->Put(write_options, "key9190", "value31236");

  // op 50254: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2200", "key6601", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2200, key6601) size=" << scan_result.size() << std::endl;
  }

  // op 50255: put
  db->Put(write_options, "key8525", "value39839");

  // op 50256: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 50257: delete
  db->Delete(write_options, "key1638");

  // op 50258: get
  status = db->Get(read_options, "key9447", &value);
  if (status.ok()) {
    out << "GET key9447 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50259: delete
  db->Delete(write_options, "key1753");

  // op 50260: get
  status = db->Get(read_options, "key0771", &value);
  if (status.ok()) {
    out << "GET key0771 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50261: get
  status = db->Get(read_options, "key4901", &value);
  if (status.ok()) {
    out << "GET key4901 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50262: put
  db->Put(write_options, "key6548", "value117485");

  // op 50263: delete
  db->Delete(write_options, "key6325");

  // op 50264: get
  status = db->Get(read_options, "key1720", &value);
  if (status.ok()) {
    out << "GET key1720 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50265: get
  status = db->Get(read_options, "key0937", &value);
  if (status.ok()) {
    out << "GET key0937 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50266: put
  db->Put(write_options, "key7640", "value16971");

  // op 50267: put
  db->Put(write_options, "key0854", "value91911");

  // op 50268: put
  db->Put(write_options, "key7923", "value105958");

  // op 50269: put
  db->Put(write_options, "key3449", "value27648");

  // op 50270: put
  db->Put(write_options, "key7694", "value72726");

  // op 50271: delete
  db->Delete(write_options, "key9870");

  // op 50272: get
  status = db->Get(read_options, "key9909", &value);
  if (status.ok()) {
    out << "GET key9909 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50273: delete
  db->Delete(write_options, "key8564");

  // op 50274: put
  db->Put(write_options, "key8789", "value88956");

  // op 50275: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0107", "key3812", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0107, key3812) size=" << scan_result.size() << std::endl;
  }

  // op 50276: put
  db->Put(write_options, "key3805", "value84865");

  // op 50277: delete
  db->Delete(write_options, "key0477");

  // op 50278: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 50279: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 50280: get
  status = db->Get(read_options, "key5392", &value);
  if (status.ok()) {
    out << "GET key5392 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50281: put
  db->Put(write_options, "key0244", "value79677");

  // op 50282: get
  status = db->Get(read_options, "key4882", &value);
  if (status.ok()) {
    out << "GET key4882 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50283: put
  db->Put(write_options, "key5261", "value73272");

  // op 50284: delete_range
  status = db->DeleteRange(write_options, "key2484", "key8721");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 50285: put
  db->Put(write_options, "key11443", "value79835");

  // op 50286: put
  db->Put(write_options, "key4839", "value82351");

  // op 50287: put
  db->Put(write_options, "key3097", "value112278");

  // op 50288: delete_range
  status = db->DeleteRange(write_options, "key6289", "key11047");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 50289: put
  db->Put(write_options, "key9167", "value47582");

  // op 50290: get
  status = db->Get(read_options, "key6478", &value);
  if (status.ok()) {
    out << "GET key6478 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50291: get
  status = db->Get(read_options, "key5074", &value);
  if (status.ok()) {
    out << "GET key5074 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50292: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 50293: delete
  db->Delete(write_options, "key1928");

  // op 50294: delete_range
  status = db->DeleteRange(write_options, "key8250", "key10815");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 50295: get
  status = db->Get(read_options, "key3905", &value);
  if (status.ok()) {
    out << "GET key3905 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50296: delete
  db->Delete(write_options, "key1127");

  // op 50297: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0689", "key10513", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0689, key10513) size=" << scan_result.size() << std::endl;
  }

  // op 50298: put
  db->Put(write_options, "key1786", "value95987");

  // op 50299: get
  status = db->Get(read_options, "key0081", &value);
  if (status.ok()) {
    out << "GET key0081 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50300: get
  status = db->Get(read_options, "key11302", &value);
  if (status.ok()) {
    out << "GET key11302 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50301: put
  db->Put(write_options, "key0563", "value8960");

  // op 50302: delete_range
  status = db->DeleteRange(write_options, "key7072", "key9318");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 50303: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1602", "key3325", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1602, key3325) size=" << scan_result.size() << std::endl;
  }

  // op 50304: put
  db->Put(write_options, "key4086", "value1372");

  // op 50305: put
  db->Put(write_options, "key5191", "value14945");

  // op 50306: delete_range
  status = db->DeleteRange(write_options, "key1922", "key1988");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 50307: put
  db->Put(write_options, "key7827", "value114983");

  // op 50308: get
  status = db->Get(read_options, "key8964", &value);
  if (status.ok()) {
    out << "GET key8964 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50309: put
  db->Put(write_options, "key9451", "value5998");

  // op 50310: put
  db->Put(write_options, "key7929", "value61620");

  // op 50311: put
  db->Put(write_options, "key10405", "value45904");

  // op 50312: delete
  db->Delete(write_options, "key3870");

  // op 50313: get
  status = db->Get(read_options, "key5549", &value);
  if (status.ok()) {
    out << "GET key5549 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50314: put
  db->Put(write_options, "key1259", "value115039");

  // op 50315: get
  status = db->Get(read_options, "key6516", &value);
  if (status.ok()) {
    out << "GET key6516 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50316: put
  db->Put(write_options, "key3764", "value30475");

  // op 50317: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1434", "key11640", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1434, key11640) size=" << scan_result.size() << std::endl;
  }

  // op 50318: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2835", "key7602", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2835, key7602) size=" << scan_result.size() << std::endl;
  }

  // op 50319: put
  db->Put(write_options, "key6314", "value19142");

  // op 50320: put
  db->Put(write_options, "key9355", "value92078");

  // op 50321: put
  db->Put(write_options, "key1276", "value18984");

  // op 50322: get
  status = db->Get(read_options, "key1231", &value);
  if (status.ok()) {
    out << "GET key1231 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50323: put
  db->Put(write_options, "key10699", "value38871");

  // op 50324: put
  db->Put(write_options, "key9684", "value71796");

  // op 50325: put
  db->Put(write_options, "key1905", "value100954");

  // op 50326: put
  db->Put(write_options, "key3583", "value9587");

  // op 50327: delete
  db->Delete(write_options, "key9044");

  // op 50328: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 50329: get
  status = db->Get(read_options, "key11320", &value);
  if (status.ok()) {
    out << "GET key11320 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50330: get
  status = db->Get(read_options, "key1912", &value);
  if (status.ok()) {
    out << "GET key1912 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50331: put
  db->Put(write_options, "key9878", "value75027");

  // op 50332: put
  db->Put(write_options, "key9342", "value77384");

  // op 50333: put
  db->Put(write_options, "key8476", "value57299");

  // op 50334: delete
  db->Delete(write_options, "key8989");

  // op 50335: put
  db->Put(write_options, "key10945", "value89666");

  // op 50336: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 50337: delete
  db->Delete(write_options, "key9942");

  // op 50338: get
  status = db->Get(read_options, "key3014", &value);
  if (status.ok()) {
    out << "GET key3014 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50339: get
  status = db->Get(read_options, "key9782", &value);
  if (status.ok()) {
    out << "GET key9782 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50340: delete_range
  status = db->DeleteRange(write_options, "key4810", "key9801");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 50341: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8662", "key11653", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8662, key11653) size=" << scan_result.size() << std::endl;
  }

  // op 50342: put
  db->Put(write_options, "key1424", "value8908");

  // op 50343: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0420", "key10245", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0420, key10245) size=" << scan_result.size() << std::endl;
  }

  // op 50344: delete
  db->Delete(write_options, "key5502");

  // op 50345: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1204", "key3062", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1204, key3062) size=" << scan_result.size() << std::endl;
  }

  // op 50346: put
  db->Put(write_options, "key5435", "value110669");

  // op 50347: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2918", "key6926", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2918, key6926) size=" << scan_result.size() << std::endl;
  }

  // op 50348: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2292", "key3740", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2292, key3740) size=" << scan_result.size() << std::endl;
  }

  // op 50349: put
  db->Put(write_options, "key4738", "value94033");

  // op 50350: put
  db->Put(write_options, "key8331", "value46330");

  // op 50351: get
  status = db->Get(read_options, "key3257", &value);
  if (status.ok()) {
    out << "GET key3257 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50352: get
  status = db->Get(read_options, "key8420", &value);
  if (status.ok()) {
    out << "GET key8420 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50353: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3866", "key7397", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3866, key7397) size=" << scan_result.size() << std::endl;
  }

  // op 50354: put
  db->Put(write_options, "key4720", "value47513");

  // op 50355: get
  status = db->Get(read_options, "key10983", &value);
  if (status.ok()) {
    out << "GET key10983 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50356: delete_range
  status = db->DeleteRange(write_options, "key3025", "key9324");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 50357: get
  status = db->Get(read_options, "key2123", &value);
  if (status.ok()) {
    out << "GET key2123 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50358: put
  db->Put(write_options, "key0834", "value34036");

  // op 50359: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2692", "key5206", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2692, key5206) size=" << scan_result.size() << std::endl;
  }

  // op 50360: put
  db->Put(write_options, "key1838", "value23942");

  // op 50361: get
  status = db->Get(read_options, "key11291", &value);
  if (status.ok()) {
    out << "GET key11291 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50362: get
  status = db->Get(read_options, "key4592", &value);
  if (status.ok()) {
    out << "GET key4592 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50363: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1666", "key10042", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1666, key10042) size=" << scan_result.size() << std::endl;
  }

  // op 50364: delete_range
  status = db->DeleteRange(write_options, "key7078", "key8242");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 50365: delete
  db->Delete(write_options, "key7495");

  // op 50366: delete
  db->Delete(write_options, "key11516");

  // op 50367: delete_range
  status = db->DeleteRange(write_options, "key4124", "key6697");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 50368: get
  status = db->Get(read_options, "key1033", &value);
  if (status.ok()) {
    out << "GET key1033 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50369: delete_range
  status = db->DeleteRange(write_options, "key2091", "key10382");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 50370: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3128", "key8224", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3128, key8224) size=" << scan_result.size() << std::endl;
  }

  // op 50371: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 50372: put
  db->Put(write_options, "key8816", "value80003");

  // op 50373: get
  status = db->Get(read_options, "key2979", &value);
  if (status.ok()) {
    out << "GET key2979 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50374: delete
  db->Delete(write_options, "key4052");

  // op 50375: put
  db->Put(write_options, "key5497", "value55353");

  // op 50376: delete_range
  status = db->DeleteRange(write_options, "key0420", "key7504");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 50377: get
  status = db->Get(read_options, "key5143", &value);
  if (status.ok()) {
    out << "GET key5143 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50378: put
  db->Put(write_options, "key11658", "value90795");

  // op 50379: put
  db->Put(write_options, "key2054", "value20818");

  // op 50380: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2505", "key6040", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2505, key6040) size=" << scan_result.size() << std::endl;
  }

  // op 50381: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0561", "key7448", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0561, key7448) size=" << scan_result.size() << std::endl;
  }

  // op 50382: put
  db->Put(write_options, "key10611", "value9109");

  // op 50383: get
  status = db->Get(read_options, "key2796", &value);
  if (status.ok()) {
    out << "GET key2796 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50384: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1574", "key8472", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1574, key8472) size=" << scan_result.size() << std::endl;
  }

  // op 50385: get
  status = db->Get(read_options, "key2894", &value);
  if (status.ok()) {
    out << "GET key2894 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50386: put
  db->Put(write_options, "key10460", "value32195");

  // op 50387: put
  db->Put(write_options, "key4389", "value94787");

  // op 50388: get
  status = db->Get(read_options, "key3778", &value);
  if (status.ok()) {
    out << "GET key3778 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50389: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5749", "key9677", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5749, key9677) size=" << scan_result.size() << std::endl;
  }

  // op 50390: get
  status = db->Get(read_options, "key0390", &value);
  if (status.ok()) {
    out << "GET key0390 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50391: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1644", "key7544", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1644, key7544) size=" << scan_result.size() << std::endl;
  }

  // op 50392: get
  status = db->Get(read_options, "key7492", &value);
  if (status.ok()) {
    out << "GET key7492 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50393: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0134", "key10147", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0134, key10147) size=" << scan_result.size() << std::endl;
  }

  // op 50394: put
  db->Put(write_options, "key1262", "value40308");

  // op 50395: get
  status = db->Get(read_options, "key5025", &value);
  if (status.ok()) {
    out << "GET key5025 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50396: put
  db->Put(write_options, "key4621", "value43407");

  // op 50397: delete_range
  status = db->DeleteRange(write_options, "key5001", "key5316");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 50398: get
  status = db->Get(read_options, "key4290", &value);
  if (status.ok()) {
    out << "GET key4290 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50399: get
  status = db->Get(read_options, "key9122", &value);
  if (status.ok()) {
    out << "GET key9122 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50400: put
  db->Put(write_options, "key11309", "value2251");

  // op 50401: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 50402: put
  db->Put(write_options, "key11652", "value113755");

  // op 50403: get
  status = db->Get(read_options, "key0510", &value);
  if (status.ok()) {
    out << "GET key0510 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50404: put
  db->Put(write_options, "key0505", "value64614");

  // op 50405: get
  status = db->Get(read_options, "key10306", &value);
  if (status.ok()) {
    out << "GET key10306 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50406: delete
  db->Delete(write_options, "key3740");

  // op 50407: put
  db->Put(write_options, "key1591", "value59266");

  // op 50408: put
  db->Put(write_options, "key8922", "value111788");

  // op 50409: delete
  db->Delete(write_options, "key1422");

  // op 50410: put
  db->Put(write_options, "key10400", "value92500");

  // op 50411: delete
  db->Delete(write_options, "key11723");

  // op 50412: put
  db->Put(write_options, "key8290", "value97949");

  // op 50413: get
  status = db->Get(read_options, "key8030", &value);
  if (status.ok()) {
    out << "GET key8030 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50414: get
  status = db->Get(read_options, "key2117", &value);
  if (status.ok()) {
    out << "GET key2117 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50415: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0432", "key8861", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0432, key8861) size=" << scan_result.size() << std::endl;
  }

  // op 50416: get
  status = db->Get(read_options, "key9992", &value);
  if (status.ok()) {
    out << "GET key9992 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50417: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 50418: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1795", "key6727", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1795, key6727) size=" << scan_result.size() << std::endl;
  }

  // op 50419: delete_range
  status = db->DeleteRange(write_options, "key0575", "key10445");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 50420: put
  db->Put(write_options, "key9302", "value78446");

  // op 50421: put
  db->Put(write_options, "key5544", "value42057");

  // op 50422: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4672", "key10863", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4672, key10863) size=" << scan_result.size() << std::endl;
  }

  // op 50423: delete_range
  status = db->DeleteRange(write_options, "key7360", "key10139");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 50424: delete_range
  status = db->DeleteRange(write_options, "key2744", "key6938");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 50425: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 50426: put
  db->Put(write_options, "key1453", "value105392");

  // op 50427: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3971", "key8315", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3971, key8315) size=" << scan_result.size() << std::endl;
  }

  // op 50428: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0836", "key1183", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0836, key1183) size=" << scan_result.size() << std::endl;
  }

  // op 50429: delete_range
  status = db->DeleteRange(write_options, "key4922", "key7121");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 50430: put
  db->Put(write_options, "key4894", "value56794");

  // op 50431: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0515", "key2807", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0515, key2807) size=" << scan_result.size() << std::endl;
  }

  // op 50432: delete
  db->Delete(write_options, "key10298");

  // op 50433: put
  db->Put(write_options, "key10983", "value87626");

  // op 50434: delete
  db->Delete(write_options, "key9451");

  // op 50435: put
  db->Put(write_options, "key0490", "value37857");

  // op 50436: put
  db->Put(write_options, "key2324", "value73969");

  // op 50437: delete_range
  status = db->DeleteRange(write_options, "key6659", "key7551");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 50438: put
  db->Put(write_options, "key3417", "value35016");

  // op 50439: put
  db->Put(write_options, "key0451", "value26077");

  // op 50440: put
  db->Put(write_options, "key7591", "value3316");

  // op 50441: get
  status = db->Get(read_options, "key6076", &value);
  if (status.ok()) {
    out << "GET key6076 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50442: put
  db->Put(write_options, "key9048", "value75896");

  // op 50443: delete
  db->Delete(write_options, "key3706");

  // op 50444: put
  db->Put(write_options, "key6631", "value80307");

  // op 50445: put
  db->Put(write_options, "key4232", "value86672");

  // op 50446: get
  status = db->Get(read_options, "key5365", &value);
  if (status.ok()) {
    out << "GET key5365 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50447: put
  db->Put(write_options, "key7400", "value9608");

  // op 50448: put
  db->Put(write_options, "key9174", "value110497");

  // op 50449: put
  db->Put(write_options, "key8277", "value100656");

  // op 50450: put
  db->Put(write_options, "key4558", "value97067");

  // op 50451: delete
  db->Delete(write_options, "key3619");

  // op 50452: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 50453: delete
  db->Delete(write_options, "key2423");

  // op 50454: delete_range
  status = db->DeleteRange(write_options, "key2117", "key9056");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 50455: delete_range
  status = db->DeleteRange(write_options, "key4474", "key11496");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 50456: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3189", "key4017", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3189, key4017) size=" << scan_result.size() << std::endl;
  }

  // op 50457: get
  status = db->Get(read_options, "key1502", &value);
  if (status.ok()) {
    out << "GET key1502 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50458: get
  status = db->Get(read_options, "key4131", &value);
  if (status.ok()) {
    out << "GET key4131 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50459: put
  db->Put(write_options, "key9044", "value105968");

  // op 50460: get
  status = db->Get(read_options, "key9667", &value);
  if (status.ok()) {
    out << "GET key9667 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50461: get
  status = db->Get(read_options, "key7815", &value);
  if (status.ok()) {
    out << "GET key7815 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50462: put
  db->Put(write_options, "key4372", "value89634");

  // op 50463: put
  db->Put(write_options, "key4782", "value109289");

  // op 50464: put
  db->Put(write_options, "key0925", "value36253");

  // op 50465: put
  db->Put(write_options, "key5641", "value69487");

  // op 50466: get
  status = db->Get(read_options, "key9042", &value);
  if (status.ok()) {
    out << "GET key9042 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50467: get
  status = db->Get(read_options, "key0803", &value);
  if (status.ok()) {
    out << "GET key0803 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50468: put
  db->Put(write_options, "key11489", "value107939");

  // op 50469: delete
  db->Delete(write_options, "key8677");

  // op 50470: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 50471: put
  db->Put(write_options, "key3833", "value26591");

  // op 50472: put
  db->Put(write_options, "key8371", "value74381");

  // op 50473: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1082", "key11532", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1082, key11532) size=" << scan_result.size() << std::endl;
  }

  // op 50474: delete
  db->Delete(write_options, "key9935");

  // op 50475: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2579", "key7118", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2579, key7118) size=" << scan_result.size() << std::endl;
  }

  // op 50476: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1250", "key1263", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1250, key1263) size=" << scan_result.size() << std::endl;
  }

  // op 50477: put
  db->Put(write_options, "key1354", "value99179");

  // op 50478: put
  db->Put(write_options, "key5830", "value33512");

  // op 50479: put
  db->Put(write_options, "key5151", "value37670");

  // op 50480: put
  db->Put(write_options, "key11316", "value52568");

  // op 50481: get
  status = db->Get(read_options, "key10289", &value);
  if (status.ok()) {
    out << "GET key10289 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50482: get
  status = db->Get(read_options, "key3370", &value);
  if (status.ok()) {
    out << "GET key3370 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50483: put
  db->Put(write_options, "key4187", "value49560");

  // op 50484: get
  status = db->Get(read_options, "key9588", &value);
  if (status.ok()) {
    out << "GET key9588 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50485: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0708", "key4891", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0708, key4891) size=" << scan_result.size() << std::endl;
  }

  // op 50486: delete_range
  status = db->DeleteRange(write_options, "key0193", "key10813");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 50487: put
  db->Put(write_options, "key9742", "value107867");

  // op 50488: put
  db->Put(write_options, "key10978", "value94745");

  // op 50489: put
  db->Put(write_options, "key1603", "value57320");

  // op 50490: put
  db->Put(write_options, "key6729", "value65044");

  // op 50491: put
  db->Put(write_options, "key11534", "value39008");

  // op 50492: put
  db->Put(write_options, "key4813", "value3465");

  // op 50493: put
  db->Put(write_options, "key11272", "value15951");

  // op 50494: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 50495: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 50496: get
  status = db->Get(read_options, "key8663", &value);
  if (status.ok()) {
    out << "GET key8663 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50497: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4002", "key6650", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4002, key6650) size=" << scan_result.size() << std::endl;
  }

  // op 50498: put
  db->Put(write_options, "key0756", "value24260");

  // op 50499: put
  db->Put(write_options, "key11620", "value75286");

  // op 50500: delete
  db->Delete(write_options, "key3675");

  // op 50501: delete
  db->Delete(write_options, "key11340");

  // op 50502: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4983", "key8570", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4983, key8570) size=" << scan_result.size() << std::endl;
  }

  // op 50503: delete
  db->Delete(write_options, "key10944");

  // op 50504: delete_range
  status = db->DeleteRange(write_options, "key2439", "key2793");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 50505: delete
  db->Delete(write_options, "key11245");

  // op 50506: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 50507: get
  status = db->Get(read_options, "key3174", &value);
  if (status.ok()) {
    out << "GET key3174 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50508: put
  db->Put(write_options, "key5585", "value38999");

  // op 50509: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3404", "key9949", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3404, key9949) size=" << scan_result.size() << std::endl;
  }

  // op 50510: delete_range
  status = db->DeleteRange(write_options, "key5478", "key10603");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 50511: delete
  db->Delete(write_options, "key0353");

  // op 50512: put
  db->Put(write_options, "key0370", "value108253");

  // op 50513: put
  db->Put(write_options, "key4039", "value83767");

  // op 50514: get
  status = db->Get(read_options, "key6593", &value);
  if (status.ok()) {
    out << "GET key6593 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50515: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2636", "key9661", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2636, key9661) size=" << scan_result.size() << std::endl;
  }

  // op 50516: delete_range
  status = db->DeleteRange(write_options, "key2410", "key11628");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 50517: put
  db->Put(write_options, "key2612", "value6889");

  // op 50518: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10553", "key11336", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10553, key11336) size=" << scan_result.size() << std::endl;
  }

  // op 50519: delete_range
  status = db->DeleteRange(write_options, "key4137", "key6904");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 50520: get
  status = db->Get(read_options, "key11563", &value);
  if (status.ok()) {
    out << "GET key11563 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50521: get
  status = db->Get(read_options, "key8113", &value);
  if (status.ok()) {
    out << "GET key8113 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50522: delete
  db->Delete(write_options, "key5642");

  // op 50523: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3349", "key9831", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3349, key9831) size=" << scan_result.size() << std::endl;
  }

  // op 50524: get
  status = db->Get(read_options, "key11334", &value);
  if (status.ok()) {
    out << "GET key11334 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50525: get
  status = db->Get(read_options, "key3716", &value);
  if (status.ok()) {
    out << "GET key3716 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50526: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1153", "key4868", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1153, key4868) size=" << scan_result.size() << std::endl;
  }

  // op 50527: delete_range
  status = db->DeleteRange(write_options, "key0337", "key6213");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 50528: put
  db->Put(write_options, "key6426", "value32395");

  // op 50529: get
  status = db->Get(read_options, "key8213", &value);
  if (status.ok()) {
    out << "GET key8213 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50530: get
  status = db->Get(read_options, "key11422", &value);
  if (status.ok()) {
    out << "GET key11422 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50531: delete_range
  status = db->DeleteRange(write_options, "key1863", "key6006");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 50532: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8779", "key11535", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8779, key11535) size=" << scan_result.size() << std::endl;
  }

  // op 50533: put
  db->Put(write_options, "key1999", "value54928");

  // op 50534: put
  db->Put(write_options, "key3715", "value84576");

  // op 50535: get
  status = db->Get(read_options, "key2642", &value);
  if (status.ok()) {
    out << "GET key2642 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50536: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0416", "key4036", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0416, key4036) size=" << scan_result.size() << std::endl;
  }

  // op 50537: put
  db->Put(write_options, "key10887", "value1460");

  // op 50538: put
  db->Put(write_options, "key11294", "value109617");

  // op 50539: get
  status = db->Get(read_options, "key10481", &value);
  if (status.ok()) {
    out << "GET key10481 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50540: get
  status = db->Get(read_options, "key0866", &value);
  if (status.ok()) {
    out << "GET key0866 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50541: get
  status = db->Get(read_options, "key0791", &value);
  if (status.ok()) {
    out << "GET key0791 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50542: delete_range
  status = db->DeleteRange(write_options, "key8385", "key10438");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 50543: put
  db->Put(write_options, "key5647", "value61413");

  // op 50544: get
  status = db->Get(read_options, "key3317", &value);
  if (status.ok()) {
    out << "GET key3317 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50545: put
  db->Put(write_options, "key1464", "value67549");

  // op 50546: delete_range
  status = db->DeleteRange(write_options, "key9099", "key9759");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 50547: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7445", "key11153", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7445, key11153) size=" << scan_result.size() << std::endl;
  }

  // op 50548: get
  status = db->Get(read_options, "key5464", &value);
  if (status.ok()) {
    out << "GET key5464 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50549: delete
  db->Delete(write_options, "key0172");

  // op 50550: put
  db->Put(write_options, "key3113", "value72116");

  // op 50551: get
  status = db->Get(read_options, "key11363", &value);
  if (status.ok()) {
    out << "GET key11363 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50552: delete_range
  status = db->DeleteRange(write_options, "key6406", "key7294");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 50553: delete_range
  status = db->DeleteRange(write_options, "key0813", "key6966");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 50554: put
  db->Put(write_options, "key10620", "value0715");

  // op 50555: put
  db->Put(write_options, "key10177", "value110710");

  // op 50556: delete_range
  status = db->DeleteRange(write_options, "key3969", "key6736");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 50557: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0580", "key9403", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0580, key9403) size=" << scan_result.size() << std::endl;
  }

  // op 50558: delete_range
  status = db->DeleteRange(write_options, "key1242", "key1623");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 50559: delete
  db->Delete(write_options, "key1576");

  // op 50560: put
  db->Put(write_options, "key4973", "value108620");

  // op 50561: put
  db->Put(write_options, "key7427", "value117071");

  // op 50562: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 50563: get
  status = db->Get(read_options, "key10309", &value);
  if (status.ok()) {
    out << "GET key10309 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50564: put
  db->Put(write_options, "key3133", "value78965");

  // op 50565: put
  db->Put(write_options, "key1112", "value16790");

  // op 50566: get
  status = db->Get(read_options, "key6416", &value);
  if (status.ok()) {
    out << "GET key6416 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50567: delete
  db->Delete(write_options, "key10236");

  // op 50568: put
  db->Put(write_options, "key3537", "value76178");

  // op 50569: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1064", "key4908", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1064, key4908) size=" << scan_result.size() << std::endl;
  }

  // op 50570: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10367", "key10768", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10367, key10768) size=" << scan_result.size() << std::endl;
  }

  // op 50571: delete
  db->Delete(write_options, "key1542");

  // op 50572: delete
  db->Delete(write_options, "key10744");

  // op 50573: get
  status = db->Get(read_options, "key1455", &value);
  if (status.ok()) {
    out << "GET key1455 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50574: delete
  db->Delete(write_options, "key10606");

  // op 50575: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6174", "key10591", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6174, key10591) size=" << scan_result.size() << std::endl;
  }

  // op 50576: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 50577: put
  db->Put(write_options, "key6124", "value70723");

  // op 50578: put
  db->Put(write_options, "key8921", "value88518");

  // op 50579: put
  db->Put(write_options, "key9417", "value2160");

  // op 50580: delete_range
  status = db->DeleteRange(write_options, "key1569", "key2449");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 50581: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 50582: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3424", "key8696", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3424, key8696) size=" << scan_result.size() << std::endl;
  }

  // op 50583: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4129", "key10304", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4129, key10304) size=" << scan_result.size() << std::endl;
  }

  // op 50584: get
  status = db->Get(read_options, "key6130", &value);
  if (status.ok()) {
    out << "GET key6130 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50585: put
  db->Put(write_options, "key2976", "value85206");

  // op 50586: get
  status = db->Get(read_options, "key7287", &value);
  if (status.ok()) {
    out << "GET key7287 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50587: put
  db->Put(write_options, "key3994", "value56823");

  // op 50588: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 50589: put
  db->Put(write_options, "key1094", "value10768");

  // op 50590: put
  db->Put(write_options, "key0395", "value85277");

  // op 50591: get
  status = db->Get(read_options, "key1462", &value);
  if (status.ok()) {
    out << "GET key1462 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50592: put
  db->Put(write_options, "key1193", "value76485");

  // op 50593: put
  db->Put(write_options, "key4068", "value59684");

  // op 50594: put
  db->Put(write_options, "key10673", "value117532");

  // op 50595: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4921", "key11635", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4921, key11635) size=" << scan_result.size() << std::endl;
  }

  // op 50596: put
  db->Put(write_options, "key9372", "value84666");

  // op 50597: put
  db->Put(write_options, "key4084", "value51695");

  // op 50598: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7743", "key11124", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7743, key11124) size=" << scan_result.size() << std::endl;
  }

  // op 50599: delete_range
  status = db->DeleteRange(write_options, "key2180", "key4870");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 50600: get
  status = db->Get(read_options, "key2922", &value);
  if (status.ok()) {
    out << "GET key2922 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50601: get
  status = db->Get(read_options, "key3336", &value);
  if (status.ok()) {
    out << "GET key3336 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50602: delete_range
  status = db->DeleteRange(write_options, "key7799", "key8079");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 50603: put
  db->Put(write_options, "key4856", "value44794");

  // op 50604: put
  db->Put(write_options, "key5767", "value7118");

  // op 50605: get
  status = db->Get(read_options, "key0031", &value);
  if (status.ok()) {
    out << "GET key0031 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50606: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6684", "key7524", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6684, key7524) size=" << scan_result.size() << std::endl;
  }

  // op 50607: put
  db->Put(write_options, "key6227", "value113692");

  // op 50608: put
  db->Put(write_options, "key9729", "value22093");

  // op 50609: put
  db->Put(write_options, "key2177", "value102440");

  // op 50610: put
  db->Put(write_options, "key0291", "value41339");

  // op 50611: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1549", "key10818", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1549, key10818) size=" << scan_result.size() << std::endl;
  }

  // op 50612: delete_range
  status = db->DeleteRange(write_options, "key0106", "key6167");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 50613: put
  db->Put(write_options, "key0923", "value77692");

  // op 50614: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9131", "key9662", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9131, key9662) size=" << scan_result.size() << std::endl;
  }

  // op 50615: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1917", "key11693", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1917, key11693) size=" << scan_result.size() << std::endl;
  }

  // op 50616: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0533", "key6907", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0533, key6907) size=" << scan_result.size() << std::endl;
  }

  // op 50617: put
  db->Put(write_options, "key1695", "value15410");

  // op 50618: put
  db->Put(write_options, "key2085", "value21502");

  // op 50619: get
  status = db->Get(read_options, "key6858", &value);
  if (status.ok()) {
    out << "GET key6858 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50620: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3074", "key8140", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3074, key8140) size=" << scan_result.size() << std::endl;
  }

  // op 50621: delete
  db->Delete(write_options, "key0634");

  // op 50622: delete
  db->Delete(write_options, "key4380");

  // op 50623: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4929", "key5703", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4929, key5703) size=" << scan_result.size() << std::endl;
  }

  // op 50624: delete_range
  status = db->DeleteRange(write_options, "key2669", "key4411");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 50625: scan
  scan_result.clear();
  status = db->Scan(read_options, "key11082", "key11095", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key11082, key11095) size=" << scan_result.size() << std::endl;
  }

  // op 50626: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9829", "key10702", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9829, key10702) size=" << scan_result.size() << std::endl;
  }

  // op 50627: delete_range
  status = db->DeleteRange(write_options, "key0237", "key1515");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 50628: put
  db->Put(write_options, "key7434", "value31307");

  // op 50629: put
  db->Put(write_options, "key11144", "value79860");

  // op 50630: put
  db->Put(write_options, "key8004", "value40057");

  // op 50631: delete_range
  status = db->DeleteRange(write_options, "key6258", "key9411");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 50632: put
  db->Put(write_options, "key3774", "value80583");

  // op 50633: put
  db->Put(write_options, "key1262", "value72257");

  // op 50634: get
  status = db->Get(read_options, "key4878", &value);
  if (status.ok()) {
    out << "GET key4878 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50635: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0797", "key4183", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0797, key4183) size=" << scan_result.size() << std::endl;
  }

  // op 50636: put
  db->Put(write_options, "key3131", "value53865");

  // op 50637: put
  db->Put(write_options, "key0458", "value57162");

  // op 50638: put
  db->Put(write_options, "key4505", "value72643");

  // op 50639: put
  db->Put(write_options, "key8218", "value88484");

  // op 50640: put
  db->Put(write_options, "key2063", "value116422");

  // op 50641: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4020", "key8088", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4020, key8088) size=" << scan_result.size() << std::endl;
  }

  // op 50642: put
  db->Put(write_options, "key2047", "value98914");

  // op 50643: get
  status = db->Get(read_options, "key2677", &value);
  if (status.ok()) {
    out << "GET key2677 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50644: put
  db->Put(write_options, "key7348", "value95908");

  // op 50645: delete_range
  status = db->DeleteRange(write_options, "key1096", "key9545");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 50646: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 50647: put
  db->Put(write_options, "key5670", "value110856");

  // op 50648: get
  status = db->Get(read_options, "key3509", &value);
  if (status.ok()) {
    out << "GET key3509 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50649: put
  db->Put(write_options, "key1566", "value64631");

  // op 50650: put
  db->Put(write_options, "key7431", "value0137");

  // op 50651: put
  db->Put(write_options, "key7358", "value93469");

  // op 50652: delete
  db->Delete(write_options, "key9971");

  // op 50653: get
  status = db->Get(read_options, "key7350", &value);
  if (status.ok()) {
    out << "GET key7350 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50654: get
  status = db->Get(read_options, "key3792", &value);
  if (status.ok()) {
    out << "GET key3792 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50655: get
  status = db->Get(read_options, "key4993", &value);
  if (status.ok()) {
    out << "GET key4993 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50656: put
  db->Put(write_options, "key4265", "value110492");

  // op 50657: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0611", "key8534", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0611, key8534) size=" << scan_result.size() << std::endl;
  }

  // op 50658: delete
  db->Delete(write_options, "key4737");

  // op 50659: get
  status = db->Get(read_options, "key3544", &value);
  if (status.ok()) {
    out << "GET key3544 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50660: get
  status = db->Get(read_options, "key1076", &value);
  if (status.ok()) {
    out << "GET key1076 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50661: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6765", "key9890", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6765, key9890) size=" << scan_result.size() << std::endl;
  }

  // op 50662: get
  status = db->Get(read_options, "key6956", &value);
  if (status.ok()) {
    out << "GET key6956 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50663: put
  db->Put(write_options, "key11126", "value107415");

  // op 50664: delete_range
  status = db->DeleteRange(write_options, "key3535", "key4790");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 50665: put
  db->Put(write_options, "key5193", "value3169");

  // op 50666: get
  status = db->Get(read_options, "key2465", &value);
  if (status.ok()) {
    out << "GET key2465 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50667: get
  status = db->Get(read_options, "key10748", &value);
  if (status.ok()) {
    out << "GET key10748 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50668: delete_range
  status = db->DeleteRange(write_options, "key1591", "key1667");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 50669: put
  db->Put(write_options, "key5111", "value23201");

  // op 50670: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7317", "key10243", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7317, key10243) size=" << scan_result.size() << std::endl;
  }

  // op 50671: put
  db->Put(write_options, "key7928", "value57200");

  // op 50672: delete
  db->Delete(write_options, "key3236");

  // op 50673: delete_range
  status = db->DeleteRange(write_options, "key5246", "key10804");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 50674: get
  status = db->Get(read_options, "key2336", &value);
  if (status.ok()) {
    out << "GET key2336 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50675: get
  status = db->Get(read_options, "key4714", &value);
  if (status.ok()) {
    out << "GET key4714 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50676: put
  db->Put(write_options, "key7564", "value13241");

  // op 50677: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2805", "key6809", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2805, key6809) size=" << scan_result.size() << std::endl;
  }

  // op 50678: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7607", "key9929", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7607, key9929) size=" << scan_result.size() << std::endl;
  }

  // op 50679: put
  db->Put(write_options, "key8440", "value112867");

  // op 50680: get
  status = db->Get(read_options, "key8577", &value);
  if (status.ok()) {
    out << "GET key8577 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50681: get
  status = db->Get(read_options, "key6118", &value);
  if (status.ok()) {
    out << "GET key6118 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50682: get
  status = db->Get(read_options, "key11267", &value);
  if (status.ok()) {
    out << "GET key11267 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50683: put
  db->Put(write_options, "key2976", "value73482");

  // op 50684: put
  db->Put(write_options, "key10356", "value111880");

  // op 50685: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1243", "key6740", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1243, key6740) size=" << scan_result.size() << std::endl;
  }

  // op 50686: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 50687: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10259", "key10285", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10259, key10285) size=" << scan_result.size() << std::endl;
  }

  // op 50688: put
  db->Put(write_options, "key9491", "value116429");

  // op 50689: put
  db->Put(write_options, "key5113", "value79010");

  // op 50690: put
  db->Put(write_options, "key0725", "value31946");

  // op 50691: get
  status = db->Get(read_options, "key4123", &value);
  if (status.ok()) {
    out << "GET key4123 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50692: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3557", "key3592", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3557, key3592) size=" << scan_result.size() << std::endl;
  }

  // op 50693: delete
  db->Delete(write_options, "key0399");

  // op 50694: put
  db->Put(write_options, "key0726", "value18349");

  // op 50695: get
  status = db->Get(read_options, "key11659", &value);
  if (status.ok()) {
    out << "GET key11659 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50696: put
  db->Put(write_options, "key7848", "value43336");

  // op 50697: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0097", "key0795", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0097, key0795) size=" << scan_result.size() << std::endl;
  }

  // op 50698: put
  db->Put(write_options, "key10680", "value11826");

  // op 50699: get
  status = db->Get(read_options, "key10113", &value);
  if (status.ok()) {
    out << "GET key10113 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50700: put
  db->Put(write_options, "key0046", "value27981");

  // op 50701: put
  db->Put(write_options, "key8888", "value105753");

  // op 50702: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0795", "key7807", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0795, key7807) size=" << scan_result.size() << std::endl;
  }

  // op 50703: get
  status = db->Get(read_options, "key3430", &value);
  if (status.ok()) {
    out << "GET key3430 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50704: delete_range
  status = db->DeleteRange(write_options, "key5204", "key9220");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 50705: delete
  db->Delete(write_options, "key7025");

  // op 50706: get
  status = db->Get(read_options, "key8737", &value);
  if (status.ok()) {
    out << "GET key8737 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50707: put
  db->Put(write_options, "key10021", "value101247");

  // op 50708: put
  db->Put(write_options, "key2784", "value21071");

  // op 50709: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4999", "key5439", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4999, key5439) size=" << scan_result.size() << std::endl;
  }

  // op 50710: get
  status = db->Get(read_options, "key11223", &value);
  if (status.ok()) {
    out << "GET key11223 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50711: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0376", "key2318", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0376, key2318) size=" << scan_result.size() << std::endl;
  }

  // op 50712: delete_range
  status = db->DeleteRange(write_options, "key5651", "key7319");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 50713: delete
  db->Delete(write_options, "key10925");

  // op 50714: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4242", "key4879", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4242, key4879) size=" << scan_result.size() << std::endl;
  }

  // op 50715: put
  db->Put(write_options, "key8535", "value51647");

  // op 50716: put
  db->Put(write_options, "key11583", "value28328");

  // op 50717: put
  db->Put(write_options, "key1567", "value12722");

  // op 50718: get
  status = db->Get(read_options, "key6669", &value);
  if (status.ok()) {
    out << "GET key6669 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50719: get
  status = db->Get(read_options, "key7071", &value);
  if (status.ok()) {
    out << "GET key7071 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50720: delete
  db->Delete(write_options, "key10295");

  // op 50721: put
  db->Put(write_options, "key11355", "value92328");

  // op 50722: delete_range
  status = db->DeleteRange(write_options, "key4557", "key10102");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 50723: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4141", "key7171", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4141, key7171) size=" << scan_result.size() << std::endl;
  }

  // op 50724: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4711", "key9286", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4711, key9286) size=" << scan_result.size() << std::endl;
  }

  // op 50725: put
  db->Put(write_options, "key4152", "value27027");

  // op 50726: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4919", "key11014", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4919, key11014) size=" << scan_result.size() << std::endl;
  }

  // op 50727: put
  db->Put(write_options, "key4497", "value38896");

  // op 50728: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0062", "key11286", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0062, key11286) size=" << scan_result.size() << std::endl;
  }

  // op 50729: put
  db->Put(write_options, "key9833", "value83383");

  // op 50730: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 50731: put
  db->Put(write_options, "key3223", "value91793");

  // op 50732: get
  status = db->Get(read_options, "key4500", &value);
  if (status.ok()) {
    out << "GET key4500 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50733: get
  status = db->Get(read_options, "key6980", &value);
  if (status.ok()) {
    out << "GET key6980 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50734: put
  db->Put(write_options, "key5063", "value34333");

  // op 50735: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2725", "key3655", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2725, key3655) size=" << scan_result.size() << std::endl;
  }

  // op 50736: get
  status = db->Get(read_options, "key10572", &value);
  if (status.ok()) {
    out << "GET key10572 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50737: put
  db->Put(write_options, "key2517", "value14851");

  // op 50738: delete
  db->Delete(write_options, "key10968");

  // op 50739: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3759", "key11298", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3759, key11298) size=" << scan_result.size() << std::endl;
  }

  // op 50740: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 50741: put
  db->Put(write_options, "key8603", "value58620");

  // op 50742: get
  status = db->Get(read_options, "key7511", &value);
  if (status.ok()) {
    out << "GET key7511 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50743: put
  db->Put(write_options, "key2108", "value104508");

  // op 50744: put
  db->Put(write_options, "key3813", "value62118");

  // op 50745: get
  status = db->Get(read_options, "key5182", &value);
  if (status.ok()) {
    out << "GET key5182 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50746: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1927", "key4942", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1927, key4942) size=" << scan_result.size() << std::endl;
  }

  // op 50747: put
  db->Put(write_options, "key7985", "value64224");

  // op 50748: put
  db->Put(write_options, "key0199", "value56123");

  // op 50749: put
  db->Put(write_options, "key4149", "value60527");

  // op 50750: get
  status = db->Get(read_options, "key6362", &value);
  if (status.ok()) {
    out << "GET key6362 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50751: put
  db->Put(write_options, "key11546", "value45442");

  // op 50752: put
  db->Put(write_options, "key6734", "value36101");

  // op 50753: put
  db->Put(write_options, "key7678", "value103978");

  // op 50754: delete
  db->Delete(write_options, "key11562");

  // op 50755: delete_range
  status = db->DeleteRange(write_options, "key1941", "key8883");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 50756: get
  status = db->Get(read_options, "key9545", &value);
  if (status.ok()) {
    out << "GET key9545 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50757: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0760", "key5698", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0760, key5698) size=" << scan_result.size() << std::endl;
  }

  // op 50758: put
  db->Put(write_options, "key1473", "value52448");

  // op 50759: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5570", "key7339", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5570, key7339) size=" << scan_result.size() << std::endl;
  }

  // op 50760: delete_range
  status = db->DeleteRange(write_options, "key0088", "key6837");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 50761: put
  db->Put(write_options, "key8669", "value102305");

  // op 50762: delete
  db->Delete(write_options, "key10154");

  // op 50763: put
  db->Put(write_options, "key7193", "value108138");

  // op 50764: get
  status = db->Get(read_options, "key8675", &value);
  if (status.ok()) {
    out << "GET key8675 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50765: put
  db->Put(write_options, "key5210", "value107032");

  // op 50766: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2293", "key3817", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2293, key3817) size=" << scan_result.size() << std::endl;
  }

  // op 50767: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 50768: put
  db->Put(write_options, "key3825", "value55399");

  // op 50769: get
  status = db->Get(read_options, "key7591", &value);
  if (status.ok()) {
    out << "GET key7591 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50770: delete_range
  status = db->DeleteRange(write_options, "key6355", "key11622");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 50771: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2186", "key5036", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2186, key5036) size=" << scan_result.size() << std::endl;
  }

  // op 50772: get
  status = db->Get(read_options, "key0406", &value);
  if (status.ok()) {
    out << "GET key0406 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50773: put
  db->Put(write_options, "key6087", "value35623");

  // op 50774: delete
  db->Delete(write_options, "key7555");

  // op 50775: put
  db->Put(write_options, "key6720", "value13165");

  // op 50776: put
  db->Put(write_options, "key8782", "value118204");

  // op 50777: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3799", "key10900", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3799, key10900) size=" << scan_result.size() << std::endl;
  }

  // op 50778: get
  status = db->Get(read_options, "key0001", &value);
  if (status.ok()) {
    out << "GET key0001 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50779: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 50780: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4317", "key6643", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4317, key6643) size=" << scan_result.size() << std::endl;
  }

  // op 50781: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1902", "key4779", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1902, key4779) size=" << scan_result.size() << std::endl;
  }

  // op 50782: put
  db->Put(write_options, "key8890", "value94234");

  // op 50783: put
  db->Put(write_options, "key9986", "value72567");

  // op 50784: put
  db->Put(write_options, "key0078", "value41782");

  // op 50785: put
  db->Put(write_options, "key2140", "value93782");

  // op 50786: put
  db->Put(write_options, "key2227", "value68888");

  // op 50787: put
  db->Put(write_options, "key11509", "value86815");

  // op 50788: get
  status = db->Get(read_options, "key10193", &value);
  if (status.ok()) {
    out << "GET key10193 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50789: put
  db->Put(write_options, "key8257", "value30262");

  // op 50790: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6493", "key10060", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6493, key10060) size=" << scan_result.size() << std::endl;
  }

  // op 50791: get
  status = db->Get(read_options, "key4681", &value);
  if (status.ok()) {
    out << "GET key4681 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50792: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2141", "key9110", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2141, key9110) size=" << scan_result.size() << std::endl;
  }

  // op 50793: get
  status = db->Get(read_options, "key11039", &value);
  if (status.ok()) {
    out << "GET key11039 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50794: put
  db->Put(write_options, "key8149", "value93523");

  // op 50795: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 50796: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6567", "key11496", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6567, key11496) size=" << scan_result.size() << std::endl;
  }

  // op 50797: delete
  db->Delete(write_options, "key2843");

  // op 50798: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1776", "key9479", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1776, key9479) size=" << scan_result.size() << std::endl;
  }

  // op 50799: put
  db->Put(write_options, "key4894", "value78531");

  // op 50800: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 50801: get
  status = db->Get(read_options, "key10710", &value);
  if (status.ok()) {
    out << "GET key10710 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50802: delete
  db->Delete(write_options, "key9478");

  // op 50803: delete_range
  status = db->DeleteRange(write_options, "key2444", "key9999");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 50804: get
  status = db->Get(read_options, "key10379", &value);
  if (status.ok()) {
    out << "GET key10379 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50805: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0377", "key2934", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0377, key2934) size=" << scan_result.size() << std::endl;
  }

  // op 50806: put
  db->Put(write_options, "key5015", "value87141");

  // op 50807: get
  status = db->Get(read_options, "key0181", &value);
  if (status.ok()) {
    out << "GET key0181 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50808: put
  db->Put(write_options, "key1852", "value13328");

  // op 50809: put
  db->Put(write_options, "key11713", "value76545");

  // op 50810: get
  status = db->Get(read_options, "key10804", &value);
  if (status.ok()) {
    out << "GET key10804 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50811: put
  db->Put(write_options, "key1059", "value23406");

  // op 50812: put
  db->Put(write_options, "key3456", "value117558");

  // op 50813: put
  db->Put(write_options, "key3699", "value81240");

  // op 50814: get
  status = db->Get(read_options, "key7756", &value);
  if (status.ok()) {
    out << "GET key7756 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50815: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6783", "key8278", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6783, key8278) size=" << scan_result.size() << std::endl;
  }

  // op 50816: put
  db->Put(write_options, "key3202", "value17631");

  // op 50817: delete_range
  status = db->DeleteRange(write_options, "key8204", "key9508");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 50818: get
  status = db->Get(read_options, "key0160", &value);
  if (status.ok()) {
    out << "GET key0160 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50819: delete_range
  status = db->DeleteRange(write_options, "key1382", "key2103");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 50820: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3586", "key3615", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3586, key3615) size=" << scan_result.size() << std::endl;
  }

  // op 50821: delete
  db->Delete(write_options, "key8685");

  // op 50822: put
  db->Put(write_options, "key1187", "value19283");

  // op 50823: delete
  db->Delete(write_options, "key8081");

  // op 50824: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 50825: get
  status = db->Get(read_options, "key0768", &value);
  if (status.ok()) {
    out << "GET key0768 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50826: delete_range
  status = db->DeleteRange(write_options, "key7242", "key9654");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 50827: put
  db->Put(write_options, "key10873", "value1086");

  // op 50828: delete
  db->Delete(write_options, "key4760");

  // op 50829: put
  db->Put(write_options, "key6378", "value89080");

  // op 50830: put
  db->Put(write_options, "key5839", "value31758");

  // op 50831: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6019", "key6141", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6019, key6141) size=" << scan_result.size() << std::endl;
  }

  // op 50832: get
  status = db->Get(read_options, "key1455", &value);
  if (status.ok()) {
    out << "GET key1455 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50833: get
  status = db->Get(read_options, "key0127", &value);
  if (status.ok()) {
    out << "GET key0127 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50834: delete
  db->Delete(write_options, "key2392");

  // op 50835: put
  db->Put(write_options, "key0154", "value36821");

  // op 50836: put
  db->Put(write_options, "key3446", "value1345");

  // op 50837: put
  db->Put(write_options, "key0502", "value16315");

  // op 50838: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0046", "key7424", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0046, key7424) size=" << scan_result.size() << std::endl;
  }

  // op 50839: get
  status = db->Get(read_options, "key3784", &value);
  if (status.ok()) {
    out << "GET key3784 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50840: get
  status = db->Get(read_options, "key0202", &value);
  if (status.ok()) {
    out << "GET key0202 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50841: delete
  db->Delete(write_options, "key0058");

  // op 50842: put
  db->Put(write_options, "key5115", "value83307");

  // op 50843: delete
  db->Delete(write_options, "key3323");

  // op 50844: delete
  db->Delete(write_options, "key8154");

  // op 50845: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0250", "key9961", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0250, key9961) size=" << scan_result.size() << std::endl;
  }

  // op 50846: delete
  db->Delete(write_options, "key10906");

  // op 50847: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9995", "key11702", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9995, key11702) size=" << scan_result.size() << std::endl;
  }

  // op 50848: get
  status = db->Get(read_options, "key3108", &value);
  if (status.ok()) {
    out << "GET key3108 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50849: get
  status = db->Get(read_options, "key5291", &value);
  if (status.ok()) {
    out << "GET key5291 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50850: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 50851: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 50852: get
  status = db->Get(read_options, "key4938", &value);
  if (status.ok()) {
    out << "GET key4938 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50853: get
  status = db->Get(read_options, "key4830", &value);
  if (status.ok()) {
    out << "GET key4830 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50854: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6912", "key10068", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6912, key10068) size=" << scan_result.size() << std::endl;
  }

  // op 50855: put
  db->Put(write_options, "key0625", "value83747");

  // op 50856: put
  db->Put(write_options, "key11591", "value110183");

  // op 50857: put
  db->Put(write_options, "key4490", "value106101");

  // op 50858: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5280", "key6255", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5280, key6255) size=" << scan_result.size() << std::endl;
  }

  // op 50859: get
  status = db->Get(read_options, "key4380", &value);
  if (status.ok()) {
    out << "GET key4380 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50860: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9626", "key10220", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9626, key10220) size=" << scan_result.size() << std::endl;
  }

  // op 50861: get
  status = db->Get(read_options, "key0607", &value);
  if (status.ok()) {
    out << "GET key0607 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50862: get
  status = db->Get(read_options, "key5945", &value);
  if (status.ok()) {
    out << "GET key5945 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50863: get
  status = db->Get(read_options, "key2575", &value);
  if (status.ok()) {
    out << "GET key2575 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50864: put
  db->Put(write_options, "key0690", "value59201");

  // op 50865: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2167", "key9571", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2167, key9571) size=" << scan_result.size() << std::endl;
  }

  // op 50866: delete_range
  status = db->DeleteRange(write_options, "key0056", "key0280");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 50867: delete
  db->Delete(write_options, "key5646");

  // op 50868: get
  status = db->Get(read_options, "key8406", &value);
  if (status.ok()) {
    out << "GET key8406 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50869: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 50870: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 50871: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5117", "key11148", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5117, key11148) size=" << scan_result.size() << std::endl;
  }

  // op 50872: put
  db->Put(write_options, "key10304", "value67382");

  // op 50873: put
  db->Put(write_options, "key10562", "value20442");

  // op 50874: get
  status = db->Get(read_options, "key11317", &value);
  if (status.ok()) {
    out << "GET key11317 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50875: get
  status = db->Get(read_options, "key6746", &value);
  if (status.ok()) {
    out << "GET key6746 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50876: get
  status = db->Get(read_options, "key1805", &value);
  if (status.ok()) {
    out << "GET key1805 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50877: put
  db->Put(write_options, "key5595", "value83117");

  // op 50878: put
  db->Put(write_options, "key10003", "value82092");

  // op 50879: put
  db->Put(write_options, "key1091", "value112166");

  // op 50880: put
  db->Put(write_options, "key3253", "value45574");

  // op 50881: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7759", "key10018", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7759, key10018) size=" << scan_result.size() << std::endl;
  }

  // op 50882: put
  db->Put(write_options, "key10930", "value101128");

  // op 50883: get
  status = db->Get(read_options, "key0508", &value);
  if (status.ok()) {
    out << "GET key0508 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50884: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3560", "key6892", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3560, key6892) size=" << scan_result.size() << std::endl;
  }

  // op 50885: delete_range
  status = db->DeleteRange(write_options, "key4063", "key9779");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 50886: get
  status = db->Get(read_options, "key4661", &value);
  if (status.ok()) {
    out << "GET key4661 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50887: put
  db->Put(write_options, "key5090", "value25405");

  // op 50888: put
  db->Put(write_options, "key3545", "value79824");

  // op 50889: put
  db->Put(write_options, "key10001", "value3770");

  // op 50890: put
  db->Put(write_options, "key8674", "value81681");

  // op 50891: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0362", "key4896", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0362, key4896) size=" << scan_result.size() << std::endl;
  }

  // op 50892: put
  db->Put(write_options, "key0249", "value89682");

  // op 50893: put
  db->Put(write_options, "key6663", "value50716");

  // op 50894: put
  db->Put(write_options, "key2987", "value88648");

  // op 50895: get
  status = db->Get(read_options, "key4409", &value);
  if (status.ok()) {
    out << "GET key4409 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50896: get
  status = db->Get(read_options, "key1595", &value);
  if (status.ok()) {
    out << "GET key1595 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50897: put
  db->Put(write_options, "key9336", "value70030");

  // op 50898: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5083", "key5287", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5083, key5287) size=" << scan_result.size() << std::endl;
  }

  // op 50899: delete_range
  status = db->DeleteRange(write_options, "key4716", "key9075");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 50900: put
  db->Put(write_options, "key2302", "value27845");

  // op 50901: put
  db->Put(write_options, "key2342", "value24481");

  // op 50902: put
  db->Put(write_options, "key3157", "value14164");

  // op 50903: get
  status = db->Get(read_options, "key9531", &value);
  if (status.ok()) {
    out << "GET key9531 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50904: delete
  db->Delete(write_options, "key5160");

  // op 50905: put
  db->Put(write_options, "key8137", "value20499");

  // op 50906: get
  status = db->Get(read_options, "key7813", &value);
  if (status.ok()) {
    out << "GET key7813 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50907: get
  status = db->Get(read_options, "key3590", &value);
  if (status.ok()) {
    out << "GET key3590 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50908: get
  status = db->Get(read_options, "key9797", &value);
  if (status.ok()) {
    out << "GET key9797 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50909: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0564", "key8444", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0564, key8444) size=" << scan_result.size() << std::endl;
  }

  // op 50910: put
  db->Put(write_options, "key2106", "value62712");

  // op 50911: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5301", "key10089", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5301, key10089) size=" << scan_result.size() << std::endl;
  }

  // op 50912: put
  db->Put(write_options, "key8118", "value62918");

  // op 50913: put
  db->Put(write_options, "key7464", "value55285");

  // op 50914: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1268", "key8766", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1268, key8766) size=" << scan_result.size() << std::endl;
  }

  // op 50915: put
  db->Put(write_options, "key5584", "value108880");

  // op 50916: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 50917: delete
  db->Delete(write_options, "key5634");

  // op 50918: get
  status = db->Get(read_options, "key0093", &value);
  if (status.ok()) {
    out << "GET key0093 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50919: get
  status = db->Get(read_options, "key8353", &value);
  if (status.ok()) {
    out << "GET key8353 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50920: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 50921: get
  status = db->Get(read_options, "key7831", &value);
  if (status.ok()) {
    out << "GET key7831 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50922: get
  status = db->Get(read_options, "key11564", &value);
  if (status.ok()) {
    out << "GET key11564 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50923: put
  db->Put(write_options, "key7723", "value79547");

  // op 50924: delete_range
  status = db->DeleteRange(write_options, "key0465", "key2866");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 50925: get
  status = db->Get(read_options, "key2381", &value);
  if (status.ok()) {
    out << "GET key2381 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50926: put
  db->Put(write_options, "key4661", "value95949");

  // op 50927: put
  db->Put(write_options, "key8121", "value101533");

  // op 50928: delete_range
  status = db->DeleteRange(write_options, "key1299", "key11483");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 50929: put
  db->Put(write_options, "key6637", "value29569");

  // op 50930: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2580", "key3845", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2580, key3845) size=" << scan_result.size() << std::endl;
  }

  // op 50931: get
  status = db->Get(read_options, "key10054", &value);
  if (status.ok()) {
    out << "GET key10054 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50932: put
  db->Put(write_options, "key10269", "value25090");

  // op 50933: put
  db->Put(write_options, "key10995", "value6349");

  // op 50934: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4907", "key5049", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4907, key5049) size=" << scan_result.size() << std::endl;
  }

  // op 50935: put
  db->Put(write_options, "key0657", "value52315");

  // op 50936: put
  db->Put(write_options, "key4514", "value97724");

  // op 50937: put
  db->Put(write_options, "key3870", "value22924");

  // op 50938: put
  db->Put(write_options, "key7348", "value68873");

  // op 50939: put
  db->Put(write_options, "key5782", "value112542");

  // op 50940: put
  db->Put(write_options, "key6802", "value74555");

  // op 50941: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 50942: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5727", "key7605", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5727, key7605) size=" << scan_result.size() << std::endl;
  }

  // op 50943: delete_range
  status = db->DeleteRange(write_options, "key0275", "key8182");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 50944: put
  db->Put(write_options, "key4210", "value62748");

  // op 50945: get
  status = db->Get(read_options, "key3052", &value);
  if (status.ok()) {
    out << "GET key3052 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50946: put
  db->Put(write_options, "key7758", "value109486");

  // op 50947: delete_range
  status = db->DeleteRange(write_options, "key3194", "key9815");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 50948: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 50949: get
  status = db->Get(read_options, "key7489", &value);
  if (status.ok()) {
    out << "GET key7489 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50950: get
  status = db->Get(read_options, "key2315", &value);
  if (status.ok()) {
    out << "GET key2315 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50951: get
  status = db->Get(read_options, "key2069", &value);
  if (status.ok()) {
    out << "GET key2069 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50952: delete_range
  status = db->DeleteRange(write_options, "key4315", "key8779");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 50953: get
  status = db->Get(read_options, "key2028", &value);
  if (status.ok()) {
    out << "GET key2028 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50954: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0926", "key8956", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0926, key8956) size=" << scan_result.size() << std::endl;
  }

  // op 50955: put
  db->Put(write_options, "key6779", "value115420");

  // op 50956: delete
  db->Delete(write_options, "key2556");

  // op 50957: get
  status = db->Get(read_options, "key3166", &value);
  if (status.ok()) {
    out << "GET key3166 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50958: get
  status = db->Get(read_options, "key5079", &value);
  if (status.ok()) {
    out << "GET key5079 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50959: get
  status = db->Get(read_options, "key5170", &value);
  if (status.ok()) {
    out << "GET key5170 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50960: put
  db->Put(write_options, "key9193", "value91001");

  // op 50961: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4341", "key5115", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4341, key5115) size=" << scan_result.size() << std::endl;
  }

  // op 50962: put
  db->Put(write_options, "key0429", "value100106");

  // op 50963: put
  db->Put(write_options, "key5052", "value103606");

  // op 50964: put
  db->Put(write_options, "key1207", "value101515");

  // op 50965: put
  db->Put(write_options, "key9143", "value116500");

  // op 50966: delete
  db->Delete(write_options, "key0003");

  // op 50967: delete
  db->Delete(write_options, "key7075");

  // op 50968: put
  db->Put(write_options, "key8075", "value28829");

  // op 50969: delete_range
  status = db->DeleteRange(write_options, "key1723", "key10259");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 50970: put
  db->Put(write_options, "key4170", "value23401");

  // op 50971: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9902", "key10753", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9902, key10753) size=" << scan_result.size() << std::endl;
  }

  // op 50972: put
  db->Put(write_options, "key3071", "value35745");

  // op 50973: get
  status = db->Get(read_options, "key7615", &value);
  if (status.ok()) {
    out << "GET key7615 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50974: put
  db->Put(write_options, "key10715", "value61320");

  // op 50975: put
  db->Put(write_options, "key9368", "value54444");

  // op 50976: put
  db->Put(write_options, "key0660", "value33245");

  // op 50977: put
  db->Put(write_options, "key5695", "value68290");

  // op 50978: put
  db->Put(write_options, "key11052", "value5721");

  // op 50979: delete
  db->Delete(write_options, "key2777");

  // op 50980: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3183", "key7662", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3183, key7662) size=" << scan_result.size() << std::endl;
  }

  // op 50981: put
  db->Put(write_options, "key8104", "value68536");

  // op 50982: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6167", "key9477", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6167, key9477) size=" << scan_result.size() << std::endl;
  }

  // op 50983: delete_range
  status = db->DeleteRange(write_options, "key5475", "key9758");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 50984: put
  db->Put(write_options, "key10729", "value95446");

  // op 50985: put
  db->Put(write_options, "key2611", "value86416");

  // op 50986: put
  db->Put(write_options, "key5775", "value7659");

  // op 50987: get
  status = db->Get(read_options, "key6050", &value);
  if (status.ok()) {
    out << "GET key6050 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50988: delete_range
  status = db->DeleteRange(write_options, "key6020", "key11112");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 50989: put
  db->Put(write_options, "key5721", "value67482");

  // op 50990: put
  db->Put(write_options, "key8708", "value69097");

  // op 50991: put
  db->Put(write_options, "key2532", "value90742");

  // op 50992: put
  db->Put(write_options, "key9732", "value56043");

  // op 50993: put
  db->Put(write_options, "key8800", "value88943");

  // op 50994: put
  db->Put(write_options, "key10716", "value66997");

  // op 50995: get
  status = db->Get(read_options, "key2391", &value);
  if (status.ok()) {
    out << "GET key2391 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50996: delete
  db->Delete(write_options, "key2349");

  // op 50997: get
  status = db->Get(read_options, "key5021", &value);
  if (status.ok()) {
    out << "GET key5021 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50998: get
  status = db->Get(read_options, "key10468", &value);
  if (status.ok()) {
    out << "GET key10468 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 50999: get
  status = db->Get(read_options, "key7519", &value);
  if (status.ok()) {
    out << "GET key7519 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51000: delete_range
  status = db->DeleteRange(write_options, "key9205", "key10355");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 51001: delete_range
  status = db->DeleteRange(write_options, "key0036", "key9054");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 51002: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6421", "key10543", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6421, key10543) size=" << scan_result.size() << std::endl;
  }

  // op 51003: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6964", "key8391", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6964, key8391) size=" << scan_result.size() << std::endl;
  }

  // op 51004: get
  status = db->Get(read_options, "key7176", &value);
  if (status.ok()) {
    out << "GET key7176 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51005: delete_range
  status = db->DeleteRange(write_options, "key3736", "key7977");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 51006: put
  db->Put(write_options, "key8688", "value61080");

  // op 51007: put
  db->Put(write_options, "key7177", "value89317");

  // op 51008: get
  status = db->Get(read_options, "key2312", &value);
  if (status.ok()) {
    out << "GET key2312 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51009: delete
  db->Delete(write_options, "key9048");

  // op 51010: put
  db->Put(write_options, "key0866", "value117928");

  // op 51011: put
  db->Put(write_options, "key4418", "value80021");

  // op 51012: put
  db->Put(write_options, "key11565", "value57097");

  // op 51013: put
  db->Put(write_options, "key0566", "value19921");

  // op 51014: put
  db->Put(write_options, "key9365", "value92535");

  // op 51015: put
  db->Put(write_options, "key11659", "value41860");

  // op 51016: get
  status = db->Get(read_options, "key6801", &value);
  if (status.ok()) {
    out << "GET key6801 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51017: get
  status = db->Get(read_options, "key6996", &value);
  if (status.ok()) {
    out << "GET key6996 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51018: delete
  db->Delete(write_options, "key4134");

  // op 51019: put
  db->Put(write_options, "key3233", "value55710");

  // op 51020: put
  db->Put(write_options, "key3816", "value107678");

  // op 51021: put
  db->Put(write_options, "key5927", "value25522");

  // op 51022: get
  status = db->Get(read_options, "key10205", &value);
  if (status.ok()) {
    out << "GET key10205 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51023: delete_range
  status = db->DeleteRange(write_options, "key0572", "key8844");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 51024: put
  db->Put(write_options, "key3366", "value57952");

  // op 51025: put
  db->Put(write_options, "key3414", "value113917");

  // op 51026: delete_range
  status = db->DeleteRange(write_options, "key4574", "key9230");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 51027: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7334", "key10218", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7334, key10218) size=" << scan_result.size() << std::endl;
  }

  // op 51028: put
  db->Put(write_options, "key7298", "value97608");

  // op 51029: delete_range
  status = db->DeleteRange(write_options, "key5208", "key9822");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 51030: put
  db->Put(write_options, "key0944", "value10829");

  // op 51031: put
  db->Put(write_options, "key6390", "value72459");

  // op 51032: get
  status = db->Get(read_options, "key9891", &value);
  if (status.ok()) {
    out << "GET key9891 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51033: get
  status = db->Get(read_options, "key3454", &value);
  if (status.ok()) {
    out << "GET key3454 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51034: put
  db->Put(write_options, "key8569", "value21375");

  // op 51035: put
  db->Put(write_options, "key1782", "value83379");

  // op 51036: put
  db->Put(write_options, "key6869", "value64391");

  // op 51037: get
  status = db->Get(read_options, "key0606", &value);
  if (status.ok()) {
    out << "GET key0606 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51038: get
  status = db->Get(read_options, "key0870", &value);
  if (status.ok()) {
    out << "GET key0870 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51039: get
  status = db->Get(read_options, "key8568", &value);
  if (status.ok()) {
    out << "GET key8568 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51040: get
  status = db->Get(read_options, "key4879", &value);
  if (status.ok()) {
    out << "GET key4879 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51041: get
  status = db->Get(read_options, "key6902", &value);
  if (status.ok()) {
    out << "GET key6902 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51042: put
  db->Put(write_options, "key3999", "value3817");

  // op 51043: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3616", "key9467", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3616, key9467) size=" << scan_result.size() << std::endl;
  }

  // op 51044: get
  status = db->Get(read_options, "key0645", &value);
  if (status.ok()) {
    out << "GET key0645 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51045: put
  db->Put(write_options, "key5675", "value12703");

  // op 51046: put
  db->Put(write_options, "key4788", "value27576");

  // op 51047: put
  db->Put(write_options, "key11621", "value84676");

  // op 51048: put
  db->Put(write_options, "key11267", "value28937");

  // op 51049: put
  db->Put(write_options, "key2115", "value11098");

  // op 51050: delete_range
  status = db->DeleteRange(write_options, "key5573", "key5949");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 51051: put
  db->Put(write_options, "key9664", "value3546");

  // op 51052: delete_range
  status = db->DeleteRange(write_options, "key1412", "key10141");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 51053: delete_range
  status = db->DeleteRange(write_options, "key7056", "key8525");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 51054: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2892", "key5527", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2892, key5527) size=" << scan_result.size() << std::endl;
  }

  // op 51055: delete
  db->Delete(write_options, "key3936");

  // op 51056: get
  status = db->Get(read_options, "key11639", &value);
  if (status.ok()) {
    out << "GET key11639 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51057: put
  db->Put(write_options, "key2600", "value47762");

  // op 51058: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6519", "key11288", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6519, key11288) size=" << scan_result.size() << std::endl;
  }

  // op 51059: put
  db->Put(write_options, "key4198", "value3328");

  // op 51060: get
  status = db->Get(read_options, "key6656", &value);
  if (status.ok()) {
    out << "GET key6656 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51061: put
  db->Put(write_options, "key11303", "value75676");

  // op 51062: get
  status = db->Get(read_options, "key9664", &value);
  if (status.ok()) {
    out << "GET key9664 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51063: put
  db->Put(write_options, "key9708", "value107096");

  // op 51064: put
  db->Put(write_options, "key4926", "value82146");

  // op 51065: put
  db->Put(write_options, "key2193", "value65130");

  // op 51066: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5907", "key11528", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5907, key11528) size=" << scan_result.size() << std::endl;
  }

  // op 51067: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10261", "key11514", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10261, key11514) size=" << scan_result.size() << std::endl;
  }

  // op 51068: put
  db->Put(write_options, "key10669", "value81868");

  // op 51069: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4522", "key10708", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4522, key10708) size=" << scan_result.size() << std::endl;
  }

  // op 51070: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2206", "key7896", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2206, key7896) size=" << scan_result.size() << std::endl;
  }

  // op 51071: delete_range
  status = db->DeleteRange(write_options, "key1905", "key5031");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 51072: get
  status = db->Get(read_options, "key9520", &value);
  if (status.ok()) {
    out << "GET key9520 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51073: delete
  db->Delete(write_options, "key1519");

  // op 51074: put
  db->Put(write_options, "key8780", "value93858");

  // op 51075: get
  status = db->Get(read_options, "key6630", &value);
  if (status.ok()) {
    out << "GET key6630 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51076: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3093", "key10107", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3093, key10107) size=" << scan_result.size() << std::endl;
  }

  // op 51077: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 51078: put
  db->Put(write_options, "key2441", "value66642");

  // op 51079: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7753", "key11439", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7753, key11439) size=" << scan_result.size() << std::endl;
  }

  // op 51080: put
  db->Put(write_options, "key6416", "value33573");

  // op 51081: put
  db->Put(write_options, "key11093", "value14145");

  // op 51082: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3977", "key11174", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3977, key11174) size=" << scan_result.size() << std::endl;
  }

  // op 51083: delete
  db->Delete(write_options, "key11410");

  // op 51084: get
  status = db->Get(read_options, "key10907", &value);
  if (status.ok()) {
    out << "GET key10907 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51085: put
  db->Put(write_options, "key8573", "value106549");

  // op 51086: get
  status = db->Get(read_options, "key5412", &value);
  if (status.ok()) {
    out << "GET key5412 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51087: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 51088: put
  db->Put(write_options, "key8970", "value98841");

  // op 51089: put
  db->Put(write_options, "key4939", "value93902");

  // op 51090: put
  db->Put(write_options, "key0794", "value106402");

  // op 51091: get
  status = db->Get(read_options, "key1492", &value);
  if (status.ok()) {
    out << "GET key1492 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51092: put
  db->Put(write_options, "key0459", "value79190");

  // op 51093: get
  status = db->Get(read_options, "key5388", &value);
  if (status.ok()) {
    out << "GET key5388 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51094: put
  db->Put(write_options, "key3071", "value55280");

  // op 51095: put
  db->Put(write_options, "key10309", "value38421");

  // op 51096: delete_range
  status = db->DeleteRange(write_options, "key8933", "key10746");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 51097: get
  status = db->Get(read_options, "key0479", &value);
  if (status.ok()) {
    out << "GET key0479 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51098: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 51099: put
  db->Put(write_options, "key6252", "value90295");

  // op 51100: delete
  db->Delete(write_options, "key7871");

  // op 51101: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0433", "key10675", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0433, key10675) size=" << scan_result.size() << std::endl;
  }

  // op 51102: put
  db->Put(write_options, "key7470", "value10372");

  // op 51103: delete
  db->Delete(write_options, "key2405");

  // op 51104: put
  db->Put(write_options, "key0226", "value33099");

  // op 51105: delete
  db->Delete(write_options, "key7989");

  // op 51106: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5614", "key7786", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5614, key7786) size=" << scan_result.size() << std::endl;
  }

  // op 51107: put
  db->Put(write_options, "key4524", "value26934");

  // op 51108: get
  status = db->Get(read_options, "key6338", &value);
  if (status.ok()) {
    out << "GET key6338 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51109: put
  db->Put(write_options, "key9478", "value13344");

  // op 51110: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0636", "key8545", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0636, key8545) size=" << scan_result.size() << std::endl;
  }

  // op 51111: get
  status = db->Get(read_options, "key2150", &value);
  if (status.ok()) {
    out << "GET key2150 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51112: delete_range
  status = db->DeleteRange(write_options, "key8109", "key11654");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 51113: delete_range
  status = db->DeleteRange(write_options, "key2277", "key10768");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 51114: put
  db->Put(write_options, "key8931", "value108759");

  // op 51115: get
  status = db->Get(read_options, "key8671", &value);
  if (status.ok()) {
    out << "GET key8671 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51116: get
  status = db->Get(read_options, "key0237", &value);
  if (status.ok()) {
    out << "GET key0237 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51117: put
  db->Put(write_options, "key9811", "value55573");

  // op 51118: put
  db->Put(write_options, "key7890", "value43702");

  // op 51119: delete
  db->Delete(write_options, "key8602");

  // op 51120: get
  status = db->Get(read_options, "key7894", &value);
  if (status.ok()) {
    out << "GET key7894 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51121: get
  status = db->Get(read_options, "key3348", &value);
  if (status.ok()) {
    out << "GET key3348 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51122: put
  db->Put(write_options, "key2229", "value4328");

  // op 51123: delete
  db->Delete(write_options, "key1604");

  // op 51124: delete_range
  status = db->DeleteRange(write_options, "key9168", "key9550");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 51125: delete_range
  status = db->DeleteRange(write_options, "key3783", "key4979");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 51126: get
  status = db->Get(read_options, "key6784", &value);
  if (status.ok()) {
    out << "GET key6784 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51127: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7482", "key11674", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7482, key11674) size=" << scan_result.size() << std::endl;
  }

  // op 51128: delete
  db->Delete(write_options, "key3471");

  // op 51129: put
  db->Put(write_options, "key1177", "value108471");

  // op 51130: put
  db->Put(write_options, "key3963", "value34658");

  // op 51131: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6396", "key11565", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6396, key11565) size=" << scan_result.size() << std::endl;
  }

  // op 51132: put
  db->Put(write_options, "key3676", "value8297");

  // op 51133: delete
  db->Delete(write_options, "key5136");

  // op 51134: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2295", "key10930", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2295, key10930) size=" << scan_result.size() << std::endl;
  }

  // op 51135: get
  status = db->Get(read_options, "key1135", &value);
  if (status.ok()) {
    out << "GET key1135 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51136: delete_range
  status = db->DeleteRange(write_options, "key3724", "key10949");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 51137: delete_range
  status = db->DeleteRange(write_options, "key6645", "key9499");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 51138: delete
  db->Delete(write_options, "key4865");

  // op 51139: get
  status = db->Get(read_options, "key6739", &value);
  if (status.ok()) {
    out << "GET key6739 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51140: put
  db->Put(write_options, "key11701", "value100671");

  // op 51141: get
  status = db->Get(read_options, "key5927", &value);
  if (status.ok()) {
    out << "GET key5927 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51142: get
  status = db->Get(read_options, "key0267", &value);
  if (status.ok()) {
    out << "GET key0267 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51143: put
  db->Put(write_options, "key11318", "value111675");

  // op 51144: delete_range
  status = db->DeleteRange(write_options, "key3457", "key8449");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 51145: put
  db->Put(write_options, "key10396", "value76696");

  // op 51146: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 51147: put
  db->Put(write_options, "key3632", "value13776");

  // op 51148: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3811", "key7675", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3811, key7675) size=" << scan_result.size() << std::endl;
  }

  // op 51149: delete_range
  status = db->DeleteRange(write_options, "key2072", "key6415");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 51150: delete_range
  status = db->DeleteRange(write_options, "key1039", "key2164");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 51151: put
  db->Put(write_options, "key2138", "value69993");

  // op 51152: put
  db->Put(write_options, "key10895", "value112074");

  // op 51153: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0123", "key2193", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0123, key2193) size=" << scan_result.size() << std::endl;
  }

  // op 51154: delete
  db->Delete(write_options, "key6868");

  // op 51155: get
  status = db->Get(read_options, "key1419", &value);
  if (status.ok()) {
    out << "GET key1419 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51156: get
  status = db->Get(read_options, "key0440", &value);
  if (status.ok()) {
    out << "GET key0440 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51157: get
  status = db->Get(read_options, "key2561", &value);
  if (status.ok()) {
    out << "GET key2561 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51158: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10778", "key11250", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10778, key11250) size=" << scan_result.size() << std::endl;
  }

  // op 51159: get
  status = db->Get(read_options, "key7481", &value);
  if (status.ok()) {
    out << "GET key7481 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51160: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7926", "key8349", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7926, key8349) size=" << scan_result.size() << std::endl;
  }

  // op 51161: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6097", "key10357", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6097, key10357) size=" << scan_result.size() << std::endl;
  }

  // op 51162: put
  db->Put(write_options, "key2585", "value60271");

  // op 51163: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 51164: put
  db->Put(write_options, "key2732", "value3201");

  // op 51165: delete
  db->Delete(write_options, "key11642");

  // op 51166: delete_range
  status = db->DeleteRange(write_options, "key3968", "key8767");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 51167: put
  db->Put(write_options, "key10218", "value41065");

  // op 51168: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 51169: delete
  db->Delete(write_options, "key0576");

  // op 51170: put
  db->Put(write_options, "key3461", "value99917");

  // op 51171: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3982", "key11468", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3982, key11468) size=" << scan_result.size() << std::endl;
  }

  // op 51172: delete
  db->Delete(write_options, "key11045");

  // op 51173: delete_range
  status = db->DeleteRange(write_options, "key1036", "key3260");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 51174: get
  status = db->Get(read_options, "key10436", &value);
  if (status.ok()) {
    out << "GET key10436 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51175: get
  status = db->Get(read_options, "key1560", &value);
  if (status.ok()) {
    out << "GET key1560 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51176: put
  db->Put(write_options, "key5202", "value71522");

  // op 51177: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4779", "key8898", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4779, key8898) size=" << scan_result.size() << std::endl;
  }

  // op 51178: get
  status = db->Get(read_options, "key0588", &value);
  if (status.ok()) {
    out << "GET key0588 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51179: get
  status = db->Get(read_options, "key3550", &value);
  if (status.ok()) {
    out << "GET key3550 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51180: get
  status = db->Get(read_options, "key4687", &value);
  if (status.ok()) {
    out << "GET key4687 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51181: get
  status = db->Get(read_options, "key6335", &value);
  if (status.ok()) {
    out << "GET key6335 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51182: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1026", "key1580", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1026, key1580) size=" << scan_result.size() << std::endl;
  }

  // op 51183: put
  db->Put(write_options, "key7737", "value78527");

  // op 51184: put
  db->Put(write_options, "key11510", "value20040");

  // op 51185: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 51186: put
  db->Put(write_options, "key2118", "value117149");

  // op 51187: delete
  db->Delete(write_options, "key3496");

  // op 51188: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0422", "key4028", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0422, key4028) size=" << scan_result.size() << std::endl;
  }

  // op 51189: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1388", "key9005", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1388, key9005) size=" << scan_result.size() << std::endl;
  }

  // op 51190: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2383", "key3712", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2383, key3712) size=" << scan_result.size() << std::endl;
  }

  // op 51191: put
  db->Put(write_options, "key1261", "value115441");

  // op 51192: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1640", "key8579", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1640, key8579) size=" << scan_result.size() << std::endl;
  }

  // op 51193: put
  db->Put(write_options, "key4323", "value91030");

  // op 51194: get
  status = db->Get(read_options, "key11261", &value);
  if (status.ok()) {
    out << "GET key11261 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51195: put
  db->Put(write_options, "key0415", "value56466");

  // op 51196: delete_range
  status = db->DeleteRange(write_options, "key2134", "key8748");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 51197: put
  db->Put(write_options, "key2055", "value53383");

  // op 51198: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6518", "key9403", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6518, key9403) size=" << scan_result.size() << std::endl;
  }

  // op 51199: get
  status = db->Get(read_options, "key11399", &value);
  if (status.ok()) {
    out << "GET key11399 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51200: put
  db->Put(write_options, "key6300", "value58844");

  // op 51201: get
  status = db->Get(read_options, "key0144", &value);
  if (status.ok()) {
    out << "GET key0144 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51202: put
  db->Put(write_options, "key9398", "value97011");

  // op 51203: put
  db->Put(write_options, "key1618", "value15328");

  // op 51204: put
  db->Put(write_options, "key10566", "value71314");

  // op 51205: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6801", "key7002", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6801, key7002) size=" << scan_result.size() << std::endl;
  }

  // op 51206: get
  status = db->Get(read_options, "key2968", &value);
  if (status.ok()) {
    out << "GET key2968 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51207: put
  db->Put(write_options, "key5294", "value103314");

  // op 51208: put
  db->Put(write_options, "key4724", "value46732");

  // op 51209: put
  db->Put(write_options, "key3650", "value29950");

  // op 51210: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0997", "key6778", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0997, key6778) size=" << scan_result.size() << std::endl;
  }

  // op 51211: put
  db->Put(write_options, "key3669", "value64763");

  // op 51212: delete_range
  status = db->DeleteRange(write_options, "key2757", "key6063");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 51213: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5342", "key7110", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5342, key7110) size=" << scan_result.size() << std::endl;
  }

  // op 51214: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 51215: put
  db->Put(write_options, "key7315", "value70245");

  // op 51216: put
  db->Put(write_options, "key0189", "value58401");

  // op 51217: get
  status = db->Get(read_options, "key0903", &value);
  if (status.ok()) {
    out << "GET key0903 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51218: delete
  db->Delete(write_options, "key9360");

  // op 51219: delete
  db->Delete(write_options, "key10484");

  // op 51220: delete_range
  status = db->DeleteRange(write_options, "key6988", "key10869");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 51221: delete_range
  status = db->DeleteRange(write_options, "key4280", "key6864");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 51222: put
  db->Put(write_options, "key10053", "value70401");

  // op 51223: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7444", "key11192", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7444, key11192) size=" << scan_result.size() << std::endl;
  }

  // op 51224: delete
  db->Delete(write_options, "key9464");

  // op 51225: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1206", "key10004", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1206, key10004) size=" << scan_result.size() << std::endl;
  }

  // op 51226: delete_range
  status = db->DeleteRange(write_options, "key3942", "key6875");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 51227: delete
  db->Delete(write_options, "key4990");

  // op 51228: put
  db->Put(write_options, "key3034", "value18992");

  // op 51229: put
  db->Put(write_options, "key3580", "value79062");

  // op 51230: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2542", "key6144", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2542, key6144) size=" << scan_result.size() << std::endl;
  }

  // op 51231: delete_range
  status = db->DeleteRange(write_options, "key3523", "key4883");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 51232: put
  db->Put(write_options, "key3561", "value62055");

  // op 51233: put
  db->Put(write_options, "key0039", "value47635");

  // op 51234: put
  db->Put(write_options, "key7252", "value106797");

  // op 51235: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0010", "key4029", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0010, key4029) size=" << scan_result.size() << std::endl;
  }

  // op 51236: put
  db->Put(write_options, "key7210", "value49741");

  // op 51237: put
  db->Put(write_options, "key1705", "value61990");

  // op 51238: put
  db->Put(write_options, "key6238", "value83674");

  // op 51239: get
  status = db->Get(read_options, "key3835", &value);
  if (status.ok()) {
    out << "GET key3835 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51240: get
  status = db->Get(read_options, "key6676", &value);
  if (status.ok()) {
    out << "GET key6676 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51241: put
  db->Put(write_options, "key1322", "value69028");

  // op 51242: delete
  db->Delete(write_options, "key7138");

  // op 51243: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0815", "key2178", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0815, key2178) size=" << scan_result.size() << std::endl;
  }

  // op 51244: delete
  db->Delete(write_options, "key5678");

  // op 51245: get
  status = db->Get(read_options, "key0678", &value);
  if (status.ok()) {
    out << "GET key0678 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51246: put
  db->Put(write_options, "key1908", "value30234");

  // op 51247: get
  status = db->Get(read_options, "key5337", &value);
  if (status.ok()) {
    out << "GET key5337 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51248: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 51249: delete_range
  status = db->DeleteRange(write_options, "key1961", "key2461");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 51250: put
  db->Put(write_options, "key2696", "value24610");

  // op 51251: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 51252: put
  db->Put(write_options, "key9351", "value71158");

  // op 51253: get
  status = db->Get(read_options, "key7170", &value);
  if (status.ok()) {
    out << "GET key7170 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51254: get
  status = db->Get(read_options, "key3582", &value);
  if (status.ok()) {
    out << "GET key3582 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51255: delete_range
  status = db->DeleteRange(write_options, "key6443", "key9809");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 51256: get
  status = db->Get(read_options, "key7850", &value);
  if (status.ok()) {
    out << "GET key7850 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51257: get
  status = db->Get(read_options, "key7089", &value);
  if (status.ok()) {
    out << "GET key7089 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51258: get
  status = db->Get(read_options, "key7818", &value);
  if (status.ok()) {
    out << "GET key7818 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51259: get
  status = db->Get(read_options, "key2560", &value);
  if (status.ok()) {
    out << "GET key2560 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51260: delete
  db->Delete(write_options, "key0727");

  // op 51261: get
  status = db->Get(read_options, "key11122", &value);
  if (status.ok()) {
    out << "GET key11122 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51262: put
  db->Put(write_options, "key1162", "value16825");

  // op 51263: put
  db->Put(write_options, "key0248", "value21594");

  // op 51264: get
  status = db->Get(read_options, "key2969", &value);
  if (status.ok()) {
    out << "GET key2969 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51265: get
  status = db->Get(read_options, "key4865", &value);
  if (status.ok()) {
    out << "GET key4865 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51266: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 51267: get
  status = db->Get(read_options, "key9457", &value);
  if (status.ok()) {
    out << "GET key9457 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51268: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0913", "key4262", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0913, key4262) size=" << scan_result.size() << std::endl;
  }

  // op 51269: delete
  db->Delete(write_options, "key10898");

  // op 51270: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 51271: delete_range
  status = db->DeleteRange(write_options, "key1924", "key4165");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 51272: delete
  db->Delete(write_options, "key11562");

  // op 51273: delete
  db->Delete(write_options, "key9372");

  // op 51274: put
  db->Put(write_options, "key1878", "value28782");

  // op 51275: delete
  db->Delete(write_options, "key8258");

  // op 51276: put
  db->Put(write_options, "key11607", "value29572");

  // op 51277: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0423", "key1831", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0423, key1831) size=" << scan_result.size() << std::endl;
  }

  // op 51278: put
  db->Put(write_options, "key0530", "value93102");

  // op 51279: put
  db->Put(write_options, "key3873", "value108925");

  // op 51280: delete_range
  status = db->DeleteRange(write_options, "key6921", "key11694");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 51281: put
  db->Put(write_options, "key11723", "value23248");

  // op 51282: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5309", "key7544", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5309, key7544) size=" << scan_result.size() << std::endl;
  }

  // op 51283: delete
  db->Delete(write_options, "key0607");

  // op 51284: put
  db->Put(write_options, "key9203", "value81192");

  // op 51285: put
  db->Put(write_options, "key10994", "value57429");

  // op 51286: put
  db->Put(write_options, "key9015", "value13336");

  // op 51287: put
  db->Put(write_options, "key10676", "value60916");

  // op 51288: get
  status = db->Get(read_options, "key5712", &value);
  if (status.ok()) {
    out << "GET key5712 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51289: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 51290: put
  db->Put(write_options, "key9855", "value88001");

  // op 51291: put
  db->Put(write_options, "key3928", "value73775");

  // op 51292: put
  db->Put(write_options, "key8849", "value97651");

  // op 51293: delete
  db->Delete(write_options, "key3835");

  // op 51294: get
  status = db->Get(read_options, "key3287", &value);
  if (status.ok()) {
    out << "GET key3287 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51295: put
  db->Put(write_options, "key4913", "value106689");

  // op 51296: delete
  db->Delete(write_options, "key9363");

  // op 51297: delete_range
  status = db->DeleteRange(write_options, "key1446", "key5599");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 51298: put
  db->Put(write_options, "key3189", "value113446");

  // op 51299: get
  status = db->Get(read_options, "key5519", &value);
  if (status.ok()) {
    out << "GET key5519 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51300: put
  db->Put(write_options, "key1255", "value111146");

  // op 51301: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 51302: delete_range
  status = db->DeleteRange(write_options, "key0034", "key0893");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 51303: put
  db->Put(write_options, "key0605", "value4090");

  // op 51304: put
  db->Put(write_options, "key6042", "value35236");

  // op 51305: put
  db->Put(write_options, "key9323", "value45155");

  // op 51306: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5879", "key10712", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5879, key10712) size=" << scan_result.size() << std::endl;
  }

  // op 51307: put
  db->Put(write_options, "key8727", "value29157");

  // op 51308: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7131", "key11111", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7131, key11111) size=" << scan_result.size() << std::endl;
  }

  // op 51309: get
  status = db->Get(read_options, "key9159", &value);
  if (status.ok()) {
    out << "GET key9159 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51310: delete_range
  status = db->DeleteRange(write_options, "key3288", "key8504");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 51311: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 51312: put
  db->Put(write_options, "key6259", "value5181");

  // op 51313: delete
  db->Delete(write_options, "key9686");

  // op 51314: delete_range
  status = db->DeleteRange(write_options, "key5258", "key7509");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 51315: put
  db->Put(write_options, "key4275", "value66097");

  // op 51316: put
  db->Put(write_options, "key7926", "value23103");

  // op 51317: delete
  db->Delete(write_options, "key5750");

  // op 51318: put
  db->Put(write_options, "key1422", "value41984");

  // op 51319: delete
  db->Delete(write_options, "key10248");

  // op 51320: put
  db->Put(write_options, "key3040", "value4179");

  // op 51321: put
  db->Put(write_options, "key0913", "value82556");

  // op 51322: get
  status = db->Get(read_options, "key9829", &value);
  if (status.ok()) {
    out << "GET key9829 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51323: put
  db->Put(write_options, "key3934", "value35025");

  // op 51324: get
  status = db->Get(read_options, "key6329", &value);
  if (status.ok()) {
    out << "GET key6329 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51325: put
  db->Put(write_options, "key1429", "value96435");

  // op 51326: delete
  db->Delete(write_options, "key3616");

  // op 51327: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5941", "key11055", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5941, key11055) size=" << scan_result.size() << std::endl;
  }

  // op 51328: put
  db->Put(write_options, "key3332", "value5647");

  // op 51329: put
  db->Put(write_options, "key2585", "value97243");

  // op 51330: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0070", "key2337", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0070, key2337) size=" << scan_result.size() << std::endl;
  }

  // op 51331: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0293", "key3707", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0293, key3707) size=" << scan_result.size() << std::endl;
  }

  // op 51332: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2526", "key9794", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2526, key9794) size=" << scan_result.size() << std::endl;
  }

  // op 51333: get
  status = db->Get(read_options, "key7988", &value);
  if (status.ok()) {
    out << "GET key7988 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51334: put
  db->Put(write_options, "key2382", "value11530");

  // op 51335: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1014", "key5449", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1014, key5449) size=" << scan_result.size() << std::endl;
  }

  // op 51336: put
  db->Put(write_options, "key11364", "value96431");

  // op 51337: put
  db->Put(write_options, "key5019", "value79976");

  // op 51338: put
  db->Put(write_options, "key5601", "value108011");

  // op 51339: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8613", "key10031", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8613, key10031) size=" << scan_result.size() << std::endl;
  }

  // op 51340: delete
  db->Delete(write_options, "key8219");

  // op 51341: put
  db->Put(write_options, "key8217", "value24785");

  // op 51342: delete
  db->Delete(write_options, "key7622");

  // op 51343: put
  db->Put(write_options, "key3636", "value47665");

  // op 51344: put
  db->Put(write_options, "key9003", "value47130");

  // op 51345: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8717", "key9435", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8717, key9435) size=" << scan_result.size() << std::endl;
  }

  // op 51346: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3324", "key5519", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3324, key5519) size=" << scan_result.size() << std::endl;
  }

  // op 51347: put
  db->Put(write_options, "key4010", "value49538");

  // op 51348: delete_range
  status = db->DeleteRange(write_options, "key1702", "key6967");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 51349: put
  db->Put(write_options, "key4959", "value87827");

  // op 51350: put
  db->Put(write_options, "key11245", "value0661");

  // op 51351: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6490", "key8617", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6490, key8617) size=" << scan_result.size() << std::endl;
  }

  // op 51352: delete_range
  status = db->DeleteRange(write_options, "key7197", "key7244");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 51353: put
  db->Put(write_options, "key8787", "value11579");

  // op 51354: get
  status = db->Get(read_options, "key8437", &value);
  if (status.ok()) {
    out << "GET key8437 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51355: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6431", "key8813", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6431, key8813) size=" << scan_result.size() << std::endl;
  }

  // op 51356: get
  status = db->Get(read_options, "key5692", &value);
  if (status.ok()) {
    out << "GET key5692 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51357: delete
  db->Delete(write_options, "key2014");

  // op 51358: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 51359: put
  db->Put(write_options, "key7321", "value16413");

  // op 51360: get
  status = db->Get(read_options, "key10251", &value);
  if (status.ok()) {
    out << "GET key10251 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51361: put
  db->Put(write_options, "key8892", "value62572");

  // op 51362: delete
  db->Delete(write_options, "key0525");

  // op 51363: put
  db->Put(write_options, "key6687", "value56103");

  // op 51364: put
  db->Put(write_options, "key1490", "value102330");

  // op 51365: put
  db->Put(write_options, "key1557", "value102308");

  // op 51366: get
  status = db->Get(read_options, "key4619", &value);
  if (status.ok()) {
    out << "GET key4619 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51367: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 51368: get
  status = db->Get(read_options, "key8700", &value);
  if (status.ok()) {
    out << "GET key8700 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51369: put
  db->Put(write_options, "key3285", "value104582");

  // op 51370: put
  db->Put(write_options, "key0931", "value81427");

  // op 51371: get
  status = db->Get(read_options, "key3604", &value);
  if (status.ok()) {
    out << "GET key3604 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51372: delete_range
  status = db->DeleteRange(write_options, "key6221", "key8680");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 51373: put
  db->Put(write_options, "key6235", "value18362");

  // op 51374: put
  db->Put(write_options, "key7311", "value12249");

  // op 51375: delete
  db->Delete(write_options, "key9706");

  // op 51376: delete_range
  status = db->DeleteRange(write_options, "key4532", "key7118");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 51377: get
  status = db->Get(read_options, "key1159", &value);
  if (status.ok()) {
    out << "GET key1159 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51378: put
  db->Put(write_options, "key4701", "value106729");

  // op 51379: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1794", "key6804", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1794, key6804) size=" << scan_result.size() << std::endl;
  }

  // op 51380: get
  status = db->Get(read_options, "key7595", &value);
  if (status.ok()) {
    out << "GET key7595 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51381: put
  db->Put(write_options, "key10684", "value16343");

  // op 51382: put
  db->Put(write_options, "key9680", "value55824");

  // op 51383: delete
  db->Delete(write_options, "key8103");

  // op 51384: put
  db->Put(write_options, "key5838", "value60506");

  // op 51385: get
  status = db->Get(read_options, "key7990", &value);
  if (status.ok()) {
    out << "GET key7990 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51386: get
  status = db->Get(read_options, "key5729", &value);
  if (status.ok()) {
    out << "GET key5729 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51387: put
  db->Put(write_options, "key8028", "value11877");

  // op 51388: delete
  db->Delete(write_options, "key0545");

  // op 51389: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 51390: delete
  db->Delete(write_options, "key9969");

  // op 51391: delete_range
  status = db->DeleteRange(write_options, "key7869", "key11615");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 51392: put
  db->Put(write_options, "key6750", "value64250");

  // op 51393: get
  status = db->Get(read_options, "key3689", &value);
  if (status.ok()) {
    out << "GET key3689 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51394: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5948", "key5990", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5948, key5990) size=" << scan_result.size() << std::endl;
  }

  // op 51395: put
  db->Put(write_options, "key8683", "value39866");

  // op 51396: delete
  db->Delete(write_options, "key3554");

  // op 51397: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0553", "key9632", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0553, key9632) size=" << scan_result.size() << std::endl;
  }

  // op 51398: get
  status = db->Get(read_options, "key9503", &value);
  if (status.ok()) {
    out << "GET key9503 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51399: get
  status = db->Get(read_options, "key8778", &value);
  if (status.ok()) {
    out << "GET key8778 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51400: get
  status = db->Get(read_options, "key3462", &value);
  if (status.ok()) {
    out << "GET key3462 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51401: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7513", "key7628", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7513, key7628) size=" << scan_result.size() << std::endl;
  }

  // op 51402: delete
  db->Delete(write_options, "key8827");

  // op 51403: put
  db->Put(write_options, "key11547", "value9747");

  // op 51404: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2504", "key9431", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2504, key9431) size=" << scan_result.size() << std::endl;
  }

  // op 51405: put
  db->Put(write_options, "key6404", "value19402");

  // op 51406: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4492", "key10832", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4492, key10832) size=" << scan_result.size() << std::endl;
  }

  // op 51407: put
  db->Put(write_options, "key1049", "value6216");

  // op 51408: delete
  db->Delete(write_options, "key3487");

  // op 51409: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3188", "key4760", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3188, key4760) size=" << scan_result.size() << std::endl;
  }

  // op 51410: delete_range
  status = db->DeleteRange(write_options, "key4109", "key10956");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 51411: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5657", "key6490", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5657, key6490) size=" << scan_result.size() << std::endl;
  }

  // op 51412: put
  db->Put(write_options, "key5765", "value38527");

  // op 51413: put
  db->Put(write_options, "key3586", "value28062");

  // op 51414: get
  status = db->Get(read_options, "key2184", &value);
  if (status.ok()) {
    out << "GET key2184 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51415: put
  db->Put(write_options, "key3627", "value20037");

  // op 51416: delete_range
  status = db->DeleteRange(write_options, "key1311", "key10104");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 51417: get
  status = db->Get(read_options, "key2381", &value);
  if (status.ok()) {
    out << "GET key2381 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51418: delete
  db->Delete(write_options, "key10305");

  // op 51419: get
  status = db->Get(read_options, "key10399", &value);
  if (status.ok()) {
    out << "GET key10399 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51420: get
  status = db->Get(read_options, "key7006", &value);
  if (status.ok()) {
    out << "GET key7006 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51421: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 51422: put
  db->Put(write_options, "key6648", "value52801");

  // op 51423: put
  db->Put(write_options, "key2626", "value24616");

  // op 51424: put
  db->Put(write_options, "key8367", "value85617");

  // op 51425: get
  status = db->Get(read_options, "key8557", &value);
  if (status.ok()) {
    out << "GET key8557 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51426: put
  db->Put(write_options, "key7756", "value31943");

  // op 51427: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0350", "key3960", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0350, key3960) size=" << scan_result.size() << std::endl;
  }

  // op 51428: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0303", "key11321", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0303, key11321) size=" << scan_result.size() << std::endl;
  }

  // op 51429: get
  status = db->Get(read_options, "key9793", &value);
  if (status.ok()) {
    out << "GET key9793 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51430: put
  db->Put(write_options, "key0692", "value38430");

  // op 51431: delete_range
  status = db->DeleteRange(write_options, "key0685", "key11628");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 51432: delete_range
  status = db->DeleteRange(write_options, "key5105", "key11641");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 51433: put
  db->Put(write_options, "key2538", "value46741");

  // op 51434: get
  status = db->Get(read_options, "key1312", &value);
  if (status.ok()) {
    out << "GET key1312 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51435: delete_range
  status = db->DeleteRange(write_options, "key0722", "key11189");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 51436: get
  status = db->Get(read_options, "key1684", &value);
  if (status.ok()) {
    out << "GET key1684 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51437: put
  db->Put(write_options, "key4872", "value35785");

  // op 51438: put
  db->Put(write_options, "key9175", "value38068");

  // op 51439: put
  db->Put(write_options, "key2819", "value98920");

  // op 51440: put
  db->Put(write_options, "key5094", "value87579");

  // op 51441: delete_range
  status = db->DeleteRange(write_options, "key7907", "key10313");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 51442: delete
  db->Delete(write_options, "key9995");

  // op 51443: get
  status = db->Get(read_options, "key7222", &value);
  if (status.ok()) {
    out << "GET key7222 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51444: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 51445: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0823", "key10331", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0823, key10331) size=" << scan_result.size() << std::endl;
  }

  // op 51446: get
  status = db->Get(read_options, "key3880", &value);
  if (status.ok()) {
    out << "GET key3880 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51447: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1233", "key5964", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1233, key5964) size=" << scan_result.size() << std::endl;
  }

  // op 51448: put
  db->Put(write_options, "key9164", "value3295");

  // op 51449: get
  status = db->Get(read_options, "key8973", &value);
  if (status.ok()) {
    out << "GET key8973 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51450: put
  db->Put(write_options, "key11471", "value115092");

  // op 51451: get
  status = db->Get(read_options, "key1107", &value);
  if (status.ok()) {
    out << "GET key1107 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51452: put
  db->Put(write_options, "key1082", "value46948");

  // op 51453: put
  db->Put(write_options, "key10788", "value72416");

  // op 51454: put
  db->Put(write_options, "key2118", "value78055");

  // op 51455: delete_range
  status = db->DeleteRange(write_options, "key0873", "key8370");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 51456: put
  db->Put(write_options, "key11076", "value110045");

  // op 51457: put
  db->Put(write_options, "key5879", "value45749");

  // op 51458: put
  db->Put(write_options, "key8293", "value77106");

  // op 51459: put
  db->Put(write_options, "key7477", "value103774");

  // op 51460: get
  status = db->Get(read_options, "key3597", &value);
  if (status.ok()) {
    out << "GET key3597 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51461: put
  db->Put(write_options, "key0507", "value19380");

  // op 51462: put
  db->Put(write_options, "key2801", "value44074");

  // op 51463: put
  db->Put(write_options, "key1168", "value112752");

  // op 51464: get
  status = db->Get(read_options, "key1307", &value);
  if (status.ok()) {
    out << "GET key1307 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51465: delete_range
  status = db->DeleteRange(write_options, "key1795", "key7960");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 51466: get
  status = db->Get(read_options, "key10099", &value);
  if (status.ok()) {
    out << "GET key10099 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51467: put
  db->Put(write_options, "key8306", "value55582");

  // op 51468: put
  db->Put(write_options, "key3282", "value32467");

  // op 51469: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1768", "key5496", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1768, key5496) size=" << scan_result.size() << std::endl;
  }

  // op 51470: put
  db->Put(write_options, "key4815", "value26143");

  // op 51471: get
  status = db->Get(read_options, "key0166", &value);
  if (status.ok()) {
    out << "GET key0166 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51472: get
  status = db->Get(read_options, "key0890", &value);
  if (status.ok()) {
    out << "GET key0890 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51473: get
  status = db->Get(read_options, "key3774", &value);
  if (status.ok()) {
    out << "GET key3774 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51474: get
  status = db->Get(read_options, "key10891", &value);
  if (status.ok()) {
    out << "GET key10891 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51475: put
  db->Put(write_options, "key0546", "value54829");

  // op 51476: get
  status = db->Get(read_options, "key10562", &value);
  if (status.ok()) {
    out << "GET key10562 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51477: delete_range
  status = db->DeleteRange(write_options, "key8112", "key10809");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 51478: put
  db->Put(write_options, "key2157", "value77598");

  // op 51479: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0885", "key6757", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0885, key6757) size=" << scan_result.size() << std::endl;
  }

  // op 51480: put
  db->Put(write_options, "key1192", "value38788");

  // op 51481: delete
  db->Delete(write_options, "key4906");

  // op 51482: delete_range
  status = db->DeleteRange(write_options, "key0347", "key2803");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 51483: delete
  db->Delete(write_options, "key2155");

  // op 51484: get
  status = db->Get(read_options, "key6712", &value);
  if (status.ok()) {
    out << "GET key6712 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51485: put
  db->Put(write_options, "key11654", "value115700");

  // op 51486: put
  db->Put(write_options, "key5173", "value110832");

  // op 51487: delete_range
  status = db->DeleteRange(write_options, "key1322", "key5923");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 51488: delete
  db->Delete(write_options, "key6183");

  // op 51489: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8717", "key11532", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8717, key11532) size=" << scan_result.size() << std::endl;
  }

  // op 51490: delete_range
  status = db->DeleteRange(write_options, "key4235", "key6750");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 51491: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10398", "key11277", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10398, key11277) size=" << scan_result.size() << std::endl;
  }

  // op 51492: put
  db->Put(write_options, "key0998", "value37340");

  // op 51493: get
  status = db->Get(read_options, "key10690", &value);
  if (status.ok()) {
    out << "GET key10690 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51494: delete
  db->Delete(write_options, "key1485");

  // op 51495: put
  db->Put(write_options, "key8053", "value101242");

  // op 51496: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8286", "key9372", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8286, key9372) size=" << scan_result.size() << std::endl;
  }

  // op 51497: get
  status = db->Get(read_options, "key9671", &value);
  if (status.ok()) {
    out << "GET key9671 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51498: put
  db->Put(write_options, "key7451", "value102410");

  // op 51499: put
  db->Put(write_options, "key0715", "value57858");

  // op 51500: get
  status = db->Get(read_options, "key11628", &value);
  if (status.ok()) {
    out << "GET key11628 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51501: put
  db->Put(write_options, "key6032", "value36762");

  // op 51502: get
  status = db->Get(read_options, "key2964", &value);
  if (status.ok()) {
    out << "GET key2964 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51503: get
  status = db->Get(read_options, "key11698", &value);
  if (status.ok()) {
    out << "GET key11698 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51504: delete_range
  status = db->DeleteRange(write_options, "key7363", "key9403");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 51505: get
  status = db->Get(read_options, "key1245", &value);
  if (status.ok()) {
    out << "GET key1245 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51506: delete
  db->Delete(write_options, "key9979");

  // op 51507: delete
  db->Delete(write_options, "key5769");

  // op 51508: delete
  db->Delete(write_options, "key5643");

  // op 51509: put
  db->Put(write_options, "key2321", "value83600");

  // op 51510: get
  status = db->Get(read_options, "key6240", &value);
  if (status.ok()) {
    out << "GET key6240 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51511: delete
  db->Delete(write_options, "key10477");

  // op 51512: put
  db->Put(write_options, "key9303", "value58271");

  // op 51513: put
  db->Put(write_options, "key4631", "value24703");

  // op 51514: delete
  db->Delete(write_options, "key9770");

  // op 51515: put
  db->Put(write_options, "key9156", "value50793");

  // op 51516: put
  db->Put(write_options, "key6764", "value67017");

  // op 51517: get
  status = db->Get(read_options, "key6231", &value);
  if (status.ok()) {
    out << "GET key6231 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51518: get
  status = db->Get(read_options, "key9842", &value);
  if (status.ok()) {
    out << "GET key9842 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51519: get
  status = db->Get(read_options, "key11511", &value);
  if (status.ok()) {
    out << "GET key11511 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51520: get
  status = db->Get(read_options, "key5941", &value);
  if (status.ok()) {
    out << "GET key5941 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51521: get
  status = db->Get(read_options, "key6001", &value);
  if (status.ok()) {
    out << "GET key6001 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51522: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1283", "key7992", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1283, key7992) size=" << scan_result.size() << std::endl;
  }

  // op 51523: put
  db->Put(write_options, "key6599", "value58083");

  // op 51524: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1301", "key2161", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1301, key2161) size=" << scan_result.size() << std::endl;
  }

  // op 51525: put
  db->Put(write_options, "key6554", "value24315");

  // op 51526: get
  status = db->Get(read_options, "key2854", &value);
  if (status.ok()) {
    out << "GET key2854 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51527: put
  db->Put(write_options, "key7641", "value95502");

  // op 51528: delete
  db->Delete(write_options, "key11116");

  // op 51529: delete_range
  status = db->DeleteRange(write_options, "key4015", "key4924");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 51530: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 51531: get
  status = db->Get(read_options, "key7672", &value);
  if (status.ok()) {
    out << "GET key7672 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51532: put
  db->Put(write_options, "key3537", "value116317");

  // op 51533: delete
  db->Delete(write_options, "key3372");

  // op 51534: delete
  db->Delete(write_options, "key6608");

  // op 51535: put
  db->Put(write_options, "key4291", "value52155");

  // op 51536: put
  db->Put(write_options, "key5520", "value30357");

  // op 51537: delete
  db->Delete(write_options, "key6058");

  // op 51538: put
  db->Put(write_options, "key5374", "value23252");

  // op 51539: get
  status = db->Get(read_options, "key7651", &value);
  if (status.ok()) {
    out << "GET key7651 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51540: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4387", "key9634", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4387, key9634) size=" << scan_result.size() << std::endl;
  }

  // op 51541: delete
  db->Delete(write_options, "key11579");

  // op 51542: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5847", "key9891", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5847, key9891) size=" << scan_result.size() << std::endl;
  }

  // op 51543: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4133", "key6186", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4133, key6186) size=" << scan_result.size() << std::endl;
  }

  // op 51544: put
  db->Put(write_options, "key4080", "value81968");

  // op 51545: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3181", "key11193", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3181, key11193) size=" << scan_result.size() << std::endl;
  }

  // op 51546: get
  status = db->Get(read_options, "key5547", &value);
  if (status.ok()) {
    out << "GET key5547 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51547: put
  db->Put(write_options, "key1464", "value67796");

  // op 51548: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 51549: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4811", "key6404", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4811, key6404) size=" << scan_result.size() << std::endl;
  }

  // op 51550: put
  db->Put(write_options, "key5975", "value81536");

  // op 51551: delete
  db->Delete(write_options, "key10111");

  // op 51552: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3933", "key7624", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3933, key7624) size=" << scan_result.size() << std::endl;
  }

  // op 51553: put
  db->Put(write_options, "key5373", "value56617");

  // op 51554: put
  db->Put(write_options, "key0180", "value100491");

  // op 51555: get
  status = db->Get(read_options, "key9361", &value);
  if (status.ok()) {
    out << "GET key9361 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51556: get
  status = db->Get(read_options, "key8963", &value);
  if (status.ok()) {
    out << "GET key8963 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51557: put
  db->Put(write_options, "key6887", "value95981");

  // op 51558: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7905", "key8561", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7905, key8561) size=" << scan_result.size() << std::endl;
  }

  // op 51559: scan
  scan_result.clear();
  status = db->Scan(read_options, "key11041", "key11079", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key11041, key11079) size=" << scan_result.size() << std::endl;
  }

  // op 51560: delete
  db->Delete(write_options, "key4189");

  // op 51561: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0649", "key2993", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0649, key2993) size=" << scan_result.size() << std::endl;
  }

  // op 51562: put
  db->Put(write_options, "key5122", "value59096");

  // op 51563: put
  db->Put(write_options, "key8368", "value52246");

  // op 51564: delete
  db->Delete(write_options, "key10399");

  // op 51565: get
  status = db->Get(read_options, "key3130", &value);
  if (status.ok()) {
    out << "GET key3130 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51566: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 51567: put
  db->Put(write_options, "key2172", "value47410");

  // op 51568: get
  status = db->Get(read_options, "key1225", &value);
  if (status.ok()) {
    out << "GET key1225 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51569: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0495", "key9636", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0495, key9636) size=" << scan_result.size() << std::endl;
  }

  // op 51570: put
  db->Put(write_options, "key2616", "value29480");

  // op 51571: put
  db->Put(write_options, "key7803", "value109452");

  // op 51572: put
  db->Put(write_options, "key10861", "value6264");

  // op 51573: put
  db->Put(write_options, "key1667", "value49995");

  // op 51574: put
  db->Put(write_options, "key5447", "value102136");

  // op 51575: get
  status = db->Get(read_options, "key5064", &value);
  if (status.ok()) {
    out << "GET key5064 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51576: put
  db->Put(write_options, "key11434", "value24504");

  // op 51577: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4129", "key7447", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4129, key7447) size=" << scan_result.size() << std::endl;
  }

  // op 51578: delete
  db->Delete(write_options, "key10735");

  // op 51579: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1736", "key9024", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1736, key9024) size=" << scan_result.size() << std::endl;
  }

  // op 51580: put
  db->Put(write_options, "key3854", "value92126");

  // op 51581: put
  db->Put(write_options, "key4584", "value47718");

  // op 51582: put
  db->Put(write_options, "key5013", "value66846");

  // op 51583: put
  db->Put(write_options, "key0530", "value26212");

  // op 51584: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 51585: put
  db->Put(write_options, "key3538", "value28769");

  // op 51586: get
  status = db->Get(read_options, "key4926", &value);
  if (status.ok()) {
    out << "GET key4926 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51587: get
  status = db->Get(read_options, "key8265", &value);
  if (status.ok()) {
    out << "GET key8265 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51588: put
  db->Put(write_options, "key5134", "value56292");

  // op 51589: get
  status = db->Get(read_options, "key5233", &value);
  if (status.ok()) {
    out << "GET key5233 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51590: get
  status = db->Get(read_options, "key10721", &value);
  if (status.ok()) {
    out << "GET key10721 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51591: get
  status = db->Get(read_options, "key11503", &value);
  if (status.ok()) {
    out << "GET key11503 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51592: delete_range
  status = db->DeleteRange(write_options, "key8054", "key9821");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 51593: put
  db->Put(write_options, "key9757", "value58860");

  // op 51594: put
  db->Put(write_options, "key7107", "value68674");

  // op 51595: get
  status = db->Get(read_options, "key4340", &value);
  if (status.ok()) {
    out << "GET key4340 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51596: get
  status = db->Get(read_options, "key8659", &value);
  if (status.ok()) {
    out << "GET key8659 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51597: put
  db->Put(write_options, "key3129", "value25392");

  // op 51598: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 51599: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 51600: put
  db->Put(write_options, "key11461", "value32778");

  // op 51601: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5332", "key10655", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5332, key10655) size=" << scan_result.size() << std::endl;
  }

  // op 51602: put
  db->Put(write_options, "key1855", "value64356");

  // op 51603: put
  db->Put(write_options, "key5372", "value46966");

  // op 51604: put
  db->Put(write_options, "key2529", "value59645");

  // op 51605: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0023", "key10286", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0023, key10286) size=" << scan_result.size() << std::endl;
  }

  // op 51606: put
  db->Put(write_options, "key6887", "value109089");

  // op 51607: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2150", "key7291", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2150, key7291) size=" << scan_result.size() << std::endl;
  }

  // op 51608: delete
  db->Delete(write_options, "key10844");

  // op 51609: delete
  db->Delete(write_options, "key5195");

  // op 51610: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 51611: get
  status = db->Get(read_options, "key0423", &value);
  if (status.ok()) {
    out << "GET key0423 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51612: put
  db->Put(write_options, "key10543", "value70030");

  // op 51613: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10072", "key11660", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10072, key11660) size=" << scan_result.size() << std::endl;
  }

  // op 51614: put
  db->Put(write_options, "key4801", "value97358");

  // op 51615: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 51616: put
  db->Put(write_options, "key6667", "value62509");

  // op 51617: put
  db->Put(write_options, "key11547", "value13895");

  // op 51618: get
  status = db->Get(read_options, "key9557", &value);
  if (status.ok()) {
    out << "GET key9557 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51619: delete_range
  status = db->DeleteRange(write_options, "key0034", "key1189");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 51620: delete_range
  status = db->DeleteRange(write_options, "key0940", "key10979");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 51621: put
  db->Put(write_options, "key3076", "value4272");

  // op 51622: get
  status = db->Get(read_options, "key4244", &value);
  if (status.ok()) {
    out << "GET key4244 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51623: put
  db->Put(write_options, "key2174", "value64692");

  // op 51624: put
  db->Put(write_options, "key0719", "value77492");

  // op 51625: delete
  db->Delete(write_options, "key8846");

  // op 51626: delete
  db->Delete(write_options, "key0758");

  // op 51627: put
  db->Put(write_options, "key5685", "value55413");

  // op 51628: get
  status = db->Get(read_options, "key11651", &value);
  if (status.ok()) {
    out << "GET key11651 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51629: delete
  db->Delete(write_options, "key11619");

  // op 51630: get
  status = db->Get(read_options, "key4441", &value);
  if (status.ok()) {
    out << "GET key4441 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51631: delete
  db->Delete(write_options, "key4493");

  // op 51632: delete
  db->Delete(write_options, "key4451");

  // op 51633: delete
  db->Delete(write_options, "key2209");

  // op 51634: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2625", "key8355", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2625, key8355) size=" << scan_result.size() << std::endl;
  }

  // op 51635: delete
  db->Delete(write_options, "key0592");

  // op 51636: put
  db->Put(write_options, "key6550", "value38796");

  // op 51637: get
  status = db->Get(read_options, "key2025", &value);
  if (status.ok()) {
    out << "GET key2025 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51638: put
  db->Put(write_options, "key4391", "value80283");

  // op 51639: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6895", "key9563", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6895, key9563) size=" << scan_result.size() << std::endl;
  }

  // op 51640: put
  db->Put(write_options, "key5017", "value81854");

  // op 51641: delete
  db->Delete(write_options, "key7646");

  // op 51642: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4947", "key10239", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4947, key10239) size=" << scan_result.size() << std::endl;
  }

  // op 51643: put
  db->Put(write_options, "key3599", "value75246");

  // op 51644: get
  status = db->Get(read_options, "key10459", &value);
  if (status.ok()) {
    out << "GET key10459 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51645: put
  db->Put(write_options, "key10174", "value75230");

  // op 51646: get
  status = db->Get(read_options, "key9244", &value);
  if (status.ok()) {
    out << "GET key9244 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51647: delete
  db->Delete(write_options, "key3276");

  // op 51648: put
  db->Put(write_options, "key10109", "value100075");

  // op 51649: put
  db->Put(write_options, "key5263", "value33599");

  // op 51650: put
  db->Put(write_options, "key7476", "value21596");

  // op 51651: get
  status = db->Get(read_options, "key0370", &value);
  if (status.ok()) {
    out << "GET key0370 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51652: get
  status = db->Get(read_options, "key1275", &value);
  if (status.ok()) {
    out << "GET key1275 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51653: get
  status = db->Get(read_options, "key9719", &value);
  if (status.ok()) {
    out << "GET key9719 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51654: put
  db->Put(write_options, "key6270", "value77587");

  // op 51655: put
  db->Put(write_options, "key4049", "value79897");

  // op 51656: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10326", "key11000", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10326, key11000) size=" << scan_result.size() << std::endl;
  }

  // op 51657: put
  db->Put(write_options, "key0951", "value16497");

  // op 51658: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7156", "key8763", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7156, key8763) size=" << scan_result.size() << std::endl;
  }

  // op 51659: delete
  db->Delete(write_options, "key2712");

  // op 51660: put
  db->Put(write_options, "key7369", "value24618");

  // op 51661: delete_range
  status = db->DeleteRange(write_options, "key4760", "key8431");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 51662: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5417", "key10850", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5417, key10850) size=" << scan_result.size() << std::endl;
  }

  // op 51663: put
  db->Put(write_options, "key11375", "value8114");

  // op 51664: delete_range
  status = db->DeleteRange(write_options, "key8681", "key11611");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 51665: get
  status = db->Get(read_options, "key1435", &value);
  if (status.ok()) {
    out << "GET key1435 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51666: get
  status = db->Get(read_options, "key1270", &value);
  if (status.ok()) {
    out << "GET key1270 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51667: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 51668: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7427", "key9154", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7427, key9154) size=" << scan_result.size() << std::endl;
  }

  // op 51669: put
  db->Put(write_options, "key4640", "value64705");

  // op 51670: delete_range
  status = db->DeleteRange(write_options, "key0571", "key6430");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 51671: get
  status = db->Get(read_options, "key11219", &value);
  if (status.ok()) {
    out << "GET key11219 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51672: delete
  db->Delete(write_options, "key0518");

  // op 51673: delete_range
  status = db->DeleteRange(write_options, "key0578", "key1559");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 51674: get
  status = db->Get(read_options, "key9131", &value);
  if (status.ok()) {
    out << "GET key9131 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51675: put
  db->Put(write_options, "key7664", "value97507");

  // op 51676: delete_range
  status = db->DeleteRange(write_options, "key2060", "key11159");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 51677: get
  status = db->Get(read_options, "key11209", &value);
  if (status.ok()) {
    out << "GET key11209 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51678: put
  db->Put(write_options, "key2315", "value11875");

  // op 51679: get
  status = db->Get(read_options, "key4476", &value);
  if (status.ok()) {
    out << "GET key4476 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51680: get
  status = db->Get(read_options, "key10769", &value);
  if (status.ok()) {
    out << "GET key10769 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51681: get
  status = db->Get(read_options, "key0237", &value);
  if (status.ok()) {
    out << "GET key0237 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51682: put
  db->Put(write_options, "key6854", "value104425");

  // op 51683: delete
  db->Delete(write_options, "key0368");

  // op 51684: delete_range
  status = db->DeleteRange(write_options, "key3949", "key11628");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 51685: delete
  db->Delete(write_options, "key3726");

  // op 51686: put
  db->Put(write_options, "key0316", "value85634");

  // op 51687: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8816", "key9802", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8816, key9802) size=" << scan_result.size() << std::endl;
  }

  // op 51688: put
  db->Put(write_options, "key11321", "value23703");

  // op 51689: put
  db->Put(write_options, "key7171", "value3904");

  // op 51690: get
  status = db->Get(read_options, "key11126", &value);
  if (status.ok()) {
    out << "GET key11126 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51691: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0604", "key9924", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0604, key9924) size=" << scan_result.size() << std::endl;
  }

  // op 51692: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1505", "key1643", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1505, key1643) size=" << scan_result.size() << std::endl;
  }

  // op 51693: put
  db->Put(write_options, "key5894", "value38910");

  // op 51694: put
  db->Put(write_options, "key2688", "value53371");

  // op 51695: put
  db->Put(write_options, "key2137", "value6293");

  // op 51696: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1236", "key9438", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1236, key9438) size=" << scan_result.size() << std::endl;
  }

  // op 51697: delete_range
  status = db->DeleteRange(write_options, "key7262", "key7585");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 51698: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 51699: delete_range
  status = db->DeleteRange(write_options, "key0560", "key9712");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 51700: get
  status = db->Get(read_options, "key1980", &value);
  if (status.ok()) {
    out << "GET key1980 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51701: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 51702: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1604", "key6288", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1604, key6288) size=" << scan_result.size() << std::endl;
  }

  // op 51703: get
  status = db->Get(read_options, "key8830", &value);
  if (status.ok()) {
    out << "GET key8830 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51704: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1307", "key3460", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1307, key3460) size=" << scan_result.size() << std::endl;
  }

  // op 51705: delete
  db->Delete(write_options, "key9506");

  // op 51706: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 51707: put
  db->Put(write_options, "key11072", "value64767");

  // op 51708: get
  status = db->Get(read_options, "key6225", &value);
  if (status.ok()) {
    out << "GET key6225 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51709: put
  db->Put(write_options, "key9013", "value91822");

  // op 51710: put
  db->Put(write_options, "key5275", "value3002");

  // op 51711: delete
  db->Delete(write_options, "key10793");

  // op 51712: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1525", "key7535", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1525, key7535) size=" << scan_result.size() << std::endl;
  }

  // op 51713: delete
  db->Delete(write_options, "key2787");

  // op 51714: put
  db->Put(write_options, "key4005", "value46813");

  // op 51715: get
  status = db->Get(read_options, "key5785", &value);
  if (status.ok()) {
    out << "GET key5785 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51716: get
  status = db->Get(read_options, "key2272", &value);
  if (status.ok()) {
    out << "GET key2272 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51717: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5717", "key10889", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5717, key10889) size=" << scan_result.size() << std::endl;
  }

  // op 51718: put
  db->Put(write_options, "key5042", "value66764");

  // op 51719: delete_range
  status = db->DeleteRange(write_options, "key4627", "key8971");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 51720: put
  db->Put(write_options, "key9604", "value117114");

  // op 51721: put
  db->Put(write_options, "key4939", "value21534");

  // op 51722: put
  db->Put(write_options, "key9233", "value100579");

  // op 51723: put
  db->Put(write_options, "key8842", "value36267");

  // op 51724: put
  db->Put(write_options, "key3807", "value89337");

  // op 51725: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 51726: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2531", "key9409", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2531, key9409) size=" << scan_result.size() << std::endl;
  }

  // op 51727: get
  status = db->Get(read_options, "key4374", &value);
  if (status.ok()) {
    out << "GET key4374 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51728: get
  status = db->Get(read_options, "key10362", &value);
  if (status.ok()) {
    out << "GET key10362 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51729: delete_range
  status = db->DeleteRange(write_options, "key0149", "key4840");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 51730: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4124", "key6215", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4124, key6215) size=" << scan_result.size() << std::endl;
  }

  // op 51731: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0075", "key9065", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0075, key9065) size=" << scan_result.size() << std::endl;
  }

  // op 51732: delete
  db->Delete(write_options, "key10654");

  // op 51733: put
  db->Put(write_options, "key5215", "value113549");

  // op 51734: delete_range
  status = db->DeleteRange(write_options, "key7214", "key11637");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 51735: delete
  db->Delete(write_options, "key0407");

  // op 51736: get
  status = db->Get(read_options, "key4693", &value);
  if (status.ok()) {
    out << "GET key4693 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51737: get
  status = db->Get(read_options, "key10091", &value);
  if (status.ok()) {
    out << "GET key10091 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51738: get
  status = db->Get(read_options, "key11507", &value);
  if (status.ok()) {
    out << "GET key11507 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51739: put
  db->Put(write_options, "key8646", "value52574");

  // op 51740: put
  db->Put(write_options, "key2011", "value79048");

  // op 51741: put
  db->Put(write_options, "key3300", "value16809");

  // op 51742: put
  db->Put(write_options, "key5867", "value26676");

  // op 51743: delete_range
  status = db->DeleteRange(write_options, "key3888", "key3972");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 51744: get
  status = db->Get(read_options, "key8378", &value);
  if (status.ok()) {
    out << "GET key8378 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51745: put
  db->Put(write_options, "key4488", "value40916");

  // op 51746: get
  status = db->Get(read_options, "key9285", &value);
  if (status.ok()) {
    out << "GET key9285 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51747: put
  db->Put(write_options, "key5752", "value0357");

  // op 51748: put
  db->Put(write_options, "key8584", "value67963");

  // op 51749: put
  db->Put(write_options, "key6838", "value58937");

  // op 51750: get
  status = db->Get(read_options, "key11221", &value);
  if (status.ok()) {
    out << "GET key11221 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51751: put
  db->Put(write_options, "key10157", "value1490");

  // op 51752: get
  status = db->Get(read_options, "key0417", &value);
  if (status.ok()) {
    out << "GET key0417 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51753: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3013", "key9697", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3013, key9697) size=" << scan_result.size() << std::endl;
  }

  // op 51754: put
  db->Put(write_options, "key2129", "value8757");

  // op 51755: delete_range
  status = db->DeleteRange(write_options, "key3219", "key4650");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 51756: put
  db->Put(write_options, "key8957", "value18672");

  // op 51757: put
  db->Put(write_options, "key1601", "value18108");

  // op 51758: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 51759: delete
  db->Delete(write_options, "key11665");

  // op 51760: put
  db->Put(write_options, "key10013", "value113339");

  // op 51761: put
  db->Put(write_options, "key6564", "value44135");

  // op 51762: delete_range
  status = db->DeleteRange(write_options, "key7392", "key7510");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 51763: put
  db->Put(write_options, "key6205", "value33641");

  // op 51764: put
  db->Put(write_options, "key8002", "value79895");

  // op 51765: get
  status = db->Get(read_options, "key4631", &value);
  if (status.ok()) {
    out << "GET key4631 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51766: get
  status = db->Get(read_options, "key2492", &value);
  if (status.ok()) {
    out << "GET key2492 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51767: put
  db->Put(write_options, "key4396", "value100491");

  // op 51768: get
  status = db->Get(read_options, "key5989", &value);
  if (status.ok()) {
    out << "GET key5989 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51769: put
  db->Put(write_options, "key10547", "value59371");

  // op 51770: put
  db->Put(write_options, "key9535", "value68085");

  // op 51771: delete
  db->Delete(write_options, "key4944");

  // op 51772: put
  db->Put(write_options, "key5101", "value91842");

  // op 51773: put
  db->Put(write_options, "key9242", "value42027");

  // op 51774: put
  db->Put(write_options, "key0257", "value111646");

  // op 51775: get
  status = db->Get(read_options, "key7667", &value);
  if (status.ok()) {
    out << "GET key7667 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51776: delete_range
  status = db->DeleteRange(write_options, "key1902", "key6048");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 51777: put
  db->Put(write_options, "key11056", "value44984");

  // op 51778: get
  status = db->Get(read_options, "key3796", &value);
  if (status.ok()) {
    out << "GET key3796 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51779: put
  db->Put(write_options, "key8308", "value6782");

  // op 51780: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2844", "key8479", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2844, key8479) size=" << scan_result.size() << std::endl;
  }

  // op 51781: delete_range
  status = db->DeleteRange(write_options, "key4168", "key9079");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 51782: put
  db->Put(write_options, "key7918", "value108039");

  // op 51783: delete
  db->Delete(write_options, "key9374");

  // op 51784: put
  db->Put(write_options, "key0641", "value34405");

  // op 51785: get
  status = db->Get(read_options, "key1145", &value);
  if (status.ok()) {
    out << "GET key1145 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51786: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3042", "key11421", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3042, key11421) size=" << scan_result.size() << std::endl;
  }

  // op 51787: delete
  db->Delete(write_options, "key9204");

  // op 51788: put
  db->Put(write_options, "key8959", "value76441");

  // op 51789: get
  status = db->Get(read_options, "key4134", &value);
  if (status.ok()) {
    out << "GET key4134 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51790: get
  status = db->Get(read_options, "key11542", &value);
  if (status.ok()) {
    out << "GET key11542 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51791: delete_range
  status = db->DeleteRange(write_options, "key8446", "key9585");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 51792: get
  status = db->Get(read_options, "key7136", &value);
  if (status.ok()) {
    out << "GET key7136 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51793: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6114", "key9507", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6114, key9507) size=" << scan_result.size() << std::endl;
  }

  // op 51794: put
  db->Put(write_options, "key2410", "value105199");

  // op 51795: put
  db->Put(write_options, "key10603", "value42426");

  // op 51796: delete
  db->Delete(write_options, "key4923");

  // op 51797: get
  status = db->Get(read_options, "key11201", &value);
  if (status.ok()) {
    out << "GET key11201 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51798: put
  db->Put(write_options, "key1240", "value50135");

  // op 51799: delete_range
  status = db->DeleteRange(write_options, "key4932", "key10411");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 51800: put
  db->Put(write_options, "key2307", "value75243");

  // op 51801: put
  db->Put(write_options, "key5228", "value90471");

  // op 51802: get
  status = db->Get(read_options, "key6357", &value);
  if (status.ok()) {
    out << "GET key6357 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51803: delete_range
  status = db->DeleteRange(write_options, "key2393", "key4848");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 51804: put
  db->Put(write_options, "key7624", "value61278");

  // op 51805: get
  status = db->Get(read_options, "key5485", &value);
  if (status.ok()) {
    out << "GET key5485 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51806: delete_range
  status = db->DeleteRange(write_options, "key0452", "key7088");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 51807: delete
  db->Delete(write_options, "key1122");

  // op 51808: delete_range
  status = db->DeleteRange(write_options, "key10212", "key11571");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 51809: put
  db->Put(write_options, "key11430", "value29363");

  // op 51810: delete
  db->Delete(write_options, "key7646");

  // op 51811: put
  db->Put(write_options, "key5851", "value13657");

  // op 51812: put
  db->Put(write_options, "key1754", "value28198");

  // op 51813: put
  db->Put(write_options, "key11651", "value96819");

  // op 51814: delete_range
  status = db->DeleteRange(write_options, "key0069", "key2715");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 51815: put
  db->Put(write_options, "key7555", "value72891");

  // op 51816: delete_range
  status = db->DeleteRange(write_options, "key3455", "key8290");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 51817: get
  status = db->Get(read_options, "key1920", &value);
  if (status.ok()) {
    out << "GET key1920 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51818: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2024", "key6154", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2024, key6154) size=" << scan_result.size() << std::endl;
  }

  // op 51819: put
  db->Put(write_options, "key5779", "value105612");

  // op 51820: delete_range
  status = db->DeleteRange(write_options, "key7906", "key10925");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 51821: put
  db->Put(write_options, "key8068", "value4523");

  // op 51822: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 51823: put
  db->Put(write_options, "key1562", "value92941");

  // op 51824: get
  status = db->Get(read_options, "key0368", &value);
  if (status.ok()) {
    out << "GET key0368 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51825: put
  db->Put(write_options, "key1644", "value107929");

  // op 51826: put
  db->Put(write_options, "key9621", "value35456");

  // op 51827: put
  db->Put(write_options, "key10442", "value46134");

  // op 51828: get
  status = db->Get(read_options, "key5940", &value);
  if (status.ok()) {
    out << "GET key5940 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51829: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 51830: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0449", "key10056", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0449, key10056) size=" << scan_result.size() << std::endl;
  }

  // op 51831: put
  db->Put(write_options, "key10900", "value42963");

  // op 51832: put
  db->Put(write_options, "key8195", "value65688");

  // op 51833: put
  db->Put(write_options, "key6680", "value15256");

  // op 51834: delete
  db->Delete(write_options, "key4510");

  // op 51835: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2555", "key10229", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2555, key10229) size=" << scan_result.size() << std::endl;
  }

  // op 51836: put
  db->Put(write_options, "key1430", "value25734");

  // op 51837: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0316", "key5433", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0316, key5433) size=" << scan_result.size() << std::endl;
  }

  // op 51838: put
  db->Put(write_options, "key3841", "value88688");

  // op 51839: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 51840: delete_range
  status = db->DeleteRange(write_options, "key5927", "key6672");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 51841: put
  db->Put(write_options, "key0280", "value106385");

  // op 51842: get
  status = db->Get(read_options, "key7208", &value);
  if (status.ok()) {
    out << "GET key7208 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51843: delete_range
  status = db->DeleteRange(write_options, "key0962", "key6694");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 51844: get
  status = db->Get(read_options, "key10215", &value);
  if (status.ok()) {
    out << "GET key10215 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51845: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4788", "key6841", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4788, key6841) size=" << scan_result.size() << std::endl;
  }

  // op 51846: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1016", "key9161", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1016, key9161) size=" << scan_result.size() << std::endl;
  }

  // op 51847: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 51848: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3296", "key4779", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3296, key4779) size=" << scan_result.size() << std::endl;
  }

  // op 51849: put
  db->Put(write_options, "key3334", "value71619");

  // op 51850: get
  status = db->Get(read_options, "key9082", &value);
  if (status.ok()) {
    out << "GET key9082 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51851: get
  status = db->Get(read_options, "key5926", &value);
  if (status.ok()) {
    out << "GET key5926 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51852: delete_range
  status = db->DeleteRange(write_options, "key1030", "key5612");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 51853: get
  status = db->Get(read_options, "key9530", &value);
  if (status.ok()) {
    out << "GET key9530 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51854: delete
  db->Delete(write_options, "key2725");

  // op 51855: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 51856: get
  status = db->Get(read_options, "key6115", &value);
  if (status.ok()) {
    out << "GET key6115 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51857: put
  db->Put(write_options, "key11700", "value46927");

  // op 51858: delete_range
  status = db->DeleteRange(write_options, "key4852", "key8689");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 51859: delete_range
  status = db->DeleteRange(write_options, "key3740", "key8353");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 51860: put
  db->Put(write_options, "key4788", "value69292");

  // op 51861: get
  status = db->Get(read_options, "key8486", &value);
  if (status.ok()) {
    out << "GET key8486 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51862: put
  db->Put(write_options, "key5490", "value16304");

  // op 51863: put
  db->Put(write_options, "key2020", "value2726");

  // op 51864: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3913", "key9583", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3913, key9583) size=" << scan_result.size() << std::endl;
  }

  // op 51865: get
  status = db->Get(read_options, "key7417", &value);
  if (status.ok()) {
    out << "GET key7417 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51866: get
  status = db->Get(read_options, "key6859", &value);
  if (status.ok()) {
    out << "GET key6859 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51867: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9355", "key9782", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9355, key9782) size=" << scan_result.size() << std::endl;
  }

  // op 51868: get
  status = db->Get(read_options, "key3867", &value);
  if (status.ok()) {
    out << "GET key3867 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51869: delete
  db->Delete(write_options, "key6627");

  // op 51870: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6346", "key9723", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6346, key9723) size=" << scan_result.size() << std::endl;
  }

  // op 51871: delete_range
  status = db->DeleteRange(write_options, "key2870", "key8343");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 51872: get
  status = db->Get(read_options, "key4963", &value);
  if (status.ok()) {
    out << "GET key4963 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51873: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2890", "key7249", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2890, key7249) size=" << scan_result.size() << std::endl;
  }

  // op 51874: get
  status = db->Get(read_options, "key0486", &value);
  if (status.ok()) {
    out << "GET key0486 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51875: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 51876: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 51877: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 51878: get
  status = db->Get(read_options, "key2215", &value);
  if (status.ok()) {
    out << "GET key2215 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51879: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3976", "key9932", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3976, key9932) size=" << scan_result.size() << std::endl;
  }

  // op 51880: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0179", "key7523", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0179, key7523) size=" << scan_result.size() << std::endl;
  }

  // op 51881: get
  status = db->Get(read_options, "key10808", &value);
  if (status.ok()) {
    out << "GET key10808 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51882: get
  status = db->Get(read_options, "key6738", &value);
  if (status.ok()) {
    out << "GET key6738 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51883: put
  db->Put(write_options, "key0224", "value3378");

  // op 51884: put
  db->Put(write_options, "key11156", "value45115");

  // op 51885: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0536", "key2335", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0536, key2335) size=" << scan_result.size() << std::endl;
  }

  // op 51886: put
  db->Put(write_options, "key7415", "value80620");

  // op 51887: get
  status = db->Get(read_options, "key2054", &value);
  if (status.ok()) {
    out << "GET key2054 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51888: delete_range
  status = db->DeleteRange(write_options, "key3303", "key6511");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 51889: get
  status = db->Get(read_options, "key10125", &value);
  if (status.ok()) {
    out << "GET key10125 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51890: put
  db->Put(write_options, "key8554", "value5256");

  // op 51891: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8214", "key10690", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8214, key10690) size=" << scan_result.size() << std::endl;
  }

  // op 51892: delete_range
  status = db->DeleteRange(write_options, "key3808", "key8397");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 51893: delete_range
  status = db->DeleteRange(write_options, "key0687", "key7662");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 51894: put
  db->Put(write_options, "key5858", "value75995");

  // op 51895: delete
  db->Delete(write_options, "key7137");

  // op 51896: put
  db->Put(write_options, "key6172", "value78129");

  // op 51897: put
  db->Put(write_options, "key8650", "value12676");

  // op 51898: put
  db->Put(write_options, "key3998", "value106023");

  // op 51899: delete
  db->Delete(write_options, "key0135");

  // op 51900: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 51901: put
  db->Put(write_options, "key6917", "value66055");

  // op 51902: delete_range
  status = db->DeleteRange(write_options, "key9002", "key10353");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 51903: get
  status = db->Get(read_options, "key10879", &value);
  if (status.ok()) {
    out << "GET key10879 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51904: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 51905: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6086", "key7791", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6086, key7791) size=" << scan_result.size() << std::endl;
  }

  // op 51906: get
  status = db->Get(read_options, "key10423", &value);
  if (status.ok()) {
    out << "GET key10423 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51907: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2475", "key10997", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2475, key10997) size=" << scan_result.size() << std::endl;
  }

  // op 51908: put
  db->Put(write_options, "key4226", "value92544");

  // op 51909: put
  db->Put(write_options, "key0869", "value81337");

  // op 51910: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7159", "key8555", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7159, key8555) size=" << scan_result.size() << std::endl;
  }

  // op 51911: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2025", "key7475", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2025, key7475) size=" << scan_result.size() << std::endl;
  }

  // op 51912: delete
  db->Delete(write_options, "key9486");

  // op 51913: put
  db->Put(write_options, "key0409", "value107869");

  // op 51914: put
  db->Put(write_options, "key0587", "value72493");

  // op 51915: delete_range
  status = db->DeleteRange(write_options, "key3481", "key10859");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 51916: get
  status = db->Get(read_options, "key1199", &value);
  if (status.ok()) {
    out << "GET key1199 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51917: put
  db->Put(write_options, "key10817", "value4794");

  // op 51918: get
  status = db->Get(read_options, "key11044", &value);
  if (status.ok()) {
    out << "GET key11044 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51919: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7967", "key10032", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7967, key10032) size=" << scan_result.size() << std::endl;
  }

  // op 51920: get
  status = db->Get(read_options, "key10733", &value);
  if (status.ok()) {
    out << "GET key10733 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51921: delete
  db->Delete(write_options, "key5190");

  // op 51922: put
  db->Put(write_options, "key5275", "value118110");

  // op 51923: get
  status = db->Get(read_options, "key11623", &value);
  if (status.ok()) {
    out << "GET key11623 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51924: put
  db->Put(write_options, "key6460", "value40710");

  // op 51925: put
  db->Put(write_options, "key0918", "value60540");

  // op 51926: put
  db->Put(write_options, "key8604", "value1241");

  // op 51927: get
  status = db->Get(read_options, "key6181", &value);
  if (status.ok()) {
    out << "GET key6181 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51928: put
  db->Put(write_options, "key6472", "value47087");

  // op 51929: put
  db->Put(write_options, "key8916", "value58541");

  // op 51930: get
  status = db->Get(read_options, "key10868", &value);
  if (status.ok()) {
    out << "GET key10868 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51931: get
  status = db->Get(read_options, "key3673", &value);
  if (status.ok()) {
    out << "GET key3673 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51932: get
  status = db->Get(read_options, "key1657", &value);
  if (status.ok()) {
    out << "GET key1657 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51933: get
  status = db->Get(read_options, "key3848", &value);
  if (status.ok()) {
    out << "GET key3848 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51934: delete_range
  status = db->DeleteRange(write_options, "key6616", "key8831");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 51935: get
  status = db->Get(read_options, "key5367", &value);
  if (status.ok()) {
    out << "GET key5367 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51936: put
  db->Put(write_options, "key10181", "value77365");

  // op 51937: delete
  db->Delete(write_options, "key10206");

  // op 51938: put
  db->Put(write_options, "key3514", "value45788");

  // op 51939: delete
  db->Delete(write_options, "key8656");

  // op 51940: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7253", "key11125", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7253, key11125) size=" << scan_result.size() << std::endl;
  }

  // op 51941: put
  db->Put(write_options, "key9599", "value44867");

  // op 51942: put
  db->Put(write_options, "key11209", "value23177");

  // op 51943: get
  status = db->Get(read_options, "key9425", &value);
  if (status.ok()) {
    out << "GET key9425 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51944: put
  db->Put(write_options, "key9365", "value23078");

  // op 51945: get
  status = db->Get(read_options, "key0163", &value);
  if (status.ok()) {
    out << "GET key0163 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51946: put
  db->Put(write_options, "key7196", "value69938");

  // op 51947: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2774", "key5935", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2774, key5935) size=" << scan_result.size() << std::endl;
  }

  // op 51948: put
  db->Put(write_options, "key7777", "value56868");

  // op 51949: put
  db->Put(write_options, "key2106", "value75646");

  // op 51950: put
  db->Put(write_options, "key8369", "value24791");

  // op 51951: put
  db->Put(write_options, "key4142", "value29868");

  // op 51952: put
  db->Put(write_options, "key3441", "value32684");

  // op 51953: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 51954: delete_range
  status = db->DeleteRange(write_options, "key3206", "key8275");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 51955: delete
  db->Delete(write_options, "key7081");

  // op 51956: put
  db->Put(write_options, "key6033", "value47891");

  // op 51957: get
  status = db->Get(read_options, "key1063", &value);
  if (status.ok()) {
    out << "GET key1063 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51958: put
  db->Put(write_options, "key7022", "value24172");

  // op 51959: get
  status = db->Get(read_options, "key7639", &value);
  if (status.ok()) {
    out << "GET key7639 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51960: delete
  db->Delete(write_options, "key10974");

  // op 51961: get
  status = db->Get(read_options, "key7722", &value);
  if (status.ok()) {
    out << "GET key7722 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51962: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3436", "key4766", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3436, key4766) size=" << scan_result.size() << std::endl;
  }

  // op 51963: get
  status = db->Get(read_options, "key10840", &value);
  if (status.ok()) {
    out << "GET key10840 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51964: delete_range
  status = db->DeleteRange(write_options, "key6616", "key8986");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 51965: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0453", "key4390", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0453, key4390) size=" << scan_result.size() << std::endl;
  }

  // op 51966: delete_range
  status = db->DeleteRange(write_options, "key8171", "key9537");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 51967: put
  db->Put(write_options, "key0587", "value100499");

  // op 51968: get
  status = db->Get(read_options, "key6452", &value);
  if (status.ok()) {
    out << "GET key6452 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51969: delete
  db->Delete(write_options, "key9464");

  // op 51970: get
  status = db->Get(read_options, "key7189", &value);
  if (status.ok()) {
    out << "GET key7189 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51971: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6263", "key6325", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6263, key6325) size=" << scan_result.size() << std::endl;
  }

  // op 51972: put
  db->Put(write_options, "key11281", "value85498");

  // op 51973: put
  db->Put(write_options, "key11397", "value31647");

  // op 51974: put
  db->Put(write_options, "key11549", "value87012");

  // op 51975: put
  db->Put(write_options, "key10357", "value113466");

  // op 51976: put
  db->Put(write_options, "key0972", "value5646");

  // op 51977: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 51978: get
  status = db->Get(read_options, "key8005", &value);
  if (status.ok()) {
    out << "GET key8005 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51979: get
  status = db->Get(read_options, "key10923", &value);
  if (status.ok()) {
    out << "GET key10923 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51980: put
  db->Put(write_options, "key9647", "value96587");

  // op 51981: put
  db->Put(write_options, "key6785", "value90189");

  // op 51982: put
  db->Put(write_options, "key0560", "value10426");

  // op 51983: put
  db->Put(write_options, "key5783", "value12437");

  // op 51984: put
  db->Put(write_options, "key4732", "value98197");

  // op 51985: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6065", "key7762", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6065, key7762) size=" << scan_result.size() << std::endl;
  }

  // op 51986: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1944", "key10700", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1944, key10700) size=" << scan_result.size() << std::endl;
  }

  // op 51987: put
  db->Put(write_options, "key1314", "value62265");

  // op 51988: get
  status = db->Get(read_options, "key5432", &value);
  if (status.ok()) {
    out << "GET key5432 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51989: get
  status = db->Get(read_options, "key6266", &value);
  if (status.ok()) {
    out << "GET key6266 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51990: get
  status = db->Get(read_options, "key9950", &value);
  if (status.ok()) {
    out << "GET key9950 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51991: put
  db->Put(write_options, "key4967", "value56510");

  // op 51992: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6915", "key7443", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6915, key7443) size=" << scan_result.size() << std::endl;
  }

  // op 51993: delete
  db->Delete(write_options, "key3408");

  // op 51994: get
  status = db->Get(read_options, "key2311", &value);
  if (status.ok()) {
    out << "GET key2311 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51995: put
  db->Put(write_options, "key3522", "value17035");

  // op 51996: get
  status = db->Get(read_options, "key10757", &value);
  if (status.ok()) {
    out << "GET key10757 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 51997: put
  db->Put(write_options, "key7897", "value46127");

  // op 51998: put
  db->Put(write_options, "key6774", "value0292");

  // op 51999: delete
  db->Delete(write_options, "key9672");

  // op 52000: put
  db->Put(write_options, "key0270", "value81522");

  // op 52001: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0708", "key1766", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0708, key1766) size=" << scan_result.size() << std::endl;
  }

  // op 52002: put
  db->Put(write_options, "key1525", "value112042");

  // op 52003: get
  status = db->Get(read_options, "key8296", &value);
  if (status.ok()) {
    out << "GET key8296 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52004: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 52005: delete
  db->Delete(write_options, "key8426");

  // op 52006: put
  db->Put(write_options, "key8376", "value15553");

  // op 52007: put
  db->Put(write_options, "key6490", "value12827");

  // op 52008: delete_range
  status = db->DeleteRange(write_options, "key0500", "key11192");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 52009: put
  db->Put(write_options, "key10232", "value53763");

  // op 52010: put
  db->Put(write_options, "key0337", "value104834");

  // op 52011: get
  status = db->Get(read_options, "key10306", &value);
  if (status.ok()) {
    out << "GET key10306 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52012: get
  status = db->Get(read_options, "key0619", &value);
  if (status.ok()) {
    out << "GET key0619 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52013: put
  db->Put(write_options, "key1961", "value13505");

  // op 52014: put
  db->Put(write_options, "key8043", "value72901");

  // op 52015: put
  db->Put(write_options, "key10353", "value2063");

  // op 52016: put
  db->Put(write_options, "key8503", "value4280");

  // op 52017: put
  db->Put(write_options, "key5565", "value69136");

  // op 52018: put
  db->Put(write_options, "key6775", "value115226");

  // op 52019: put
  db->Put(write_options, "key0107", "value1363");

  // op 52020: put
  db->Put(write_options, "key8988", "value87514");

  // op 52021: put
  db->Put(write_options, "key10549", "value80198");

  // op 52022: get
  status = db->Get(read_options, "key11405", &value);
  if (status.ok()) {
    out << "GET key11405 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52023: put
  db->Put(write_options, "key3436", "value43387");

  // op 52024: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3457", "key9221", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3457, key9221) size=" << scan_result.size() << std::endl;
  }

  // op 52025: put
  db->Put(write_options, "key2975", "value71700");

  // op 52026: put
  db->Put(write_options, "key7895", "value13598");

  // op 52027: put
  db->Put(write_options, "key0166", "value102965");

  // op 52028: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1682", "key3628", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1682, key3628) size=" << scan_result.size() << std::endl;
  }

  // op 52029: put
  db->Put(write_options, "key3710", "value53767");

  // op 52030: delete
  db->Delete(write_options, "key5052");

  // op 52031: delete
  db->Delete(write_options, "key4900");

  // op 52032: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5544", "key8418", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5544, key8418) size=" << scan_result.size() << std::endl;
  }

  // op 52033: put
  db->Put(write_options, "key10001", "value47182");

  // op 52034: put
  db->Put(write_options, "key4425", "value39825");

  // op 52035: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1467", "key7063", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1467, key7063) size=" << scan_result.size() << std::endl;
  }

  // op 52036: put
  db->Put(write_options, "key6646", "value100294");

  // op 52037: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 52038: get
  status = db->Get(read_options, "key7050", &value);
  if (status.ok()) {
    out << "GET key7050 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52039: get
  status = db->Get(read_options, "key8519", &value);
  if (status.ok()) {
    out << "GET key8519 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52040: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1850", "key7582", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1850, key7582) size=" << scan_result.size() << std::endl;
  }

  // op 52041: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 52042: put
  db->Put(write_options, "key0241", "value42331");

  // op 52043: delete
  db->Delete(write_options, "key10481");

  // op 52044: delete_range
  status = db->DeleteRange(write_options, "key4542", "key11057");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 52045: put
  db->Put(write_options, "key7013", "value20101");

  // op 52046: get
  status = db->Get(read_options, "key2495", &value);
  if (status.ok()) {
    out << "GET key2495 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52047: put
  db->Put(write_options, "key11678", "value66100");

  // op 52048: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2877", "key3244", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2877, key3244) size=" << scan_result.size() << std::endl;
  }

  // op 52049: get
  status = db->Get(read_options, "key0265", &value);
  if (status.ok()) {
    out << "GET key0265 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52050: put
  db->Put(write_options, "key1537", "value6362");

  // op 52051: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 52052: get
  status = db->Get(read_options, "key0876", &value);
  if (status.ok()) {
    out << "GET key0876 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52053: delete_range
  status = db->DeleteRange(write_options, "key1822", "key5729");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 52054: get
  status = db->Get(read_options, "key1368", &value);
  if (status.ok()) {
    out << "GET key1368 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52055: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 52056: put
  db->Put(write_options, "key3210", "value32323");

  // op 52057: put
  db->Put(write_options, "key10897", "value100868");

  // op 52058: put
  db->Put(write_options, "key8185", "value9635");

  // op 52059: put
  db->Put(write_options, "key1015", "value49311");

  // op 52060: put
  db->Put(write_options, "key8140", "value37557");

  // op 52061: put
  db->Put(write_options, "key1446", "value77838");

  // op 52062: put
  db->Put(write_options, "key4145", "value105847");

  // op 52063: put
  db->Put(write_options, "key4841", "value12255");

  // op 52064: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1510", "key11425", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1510, key11425) size=" << scan_result.size() << std::endl;
  }

  // op 52065: put
  db->Put(write_options, "key0487", "value24669");

  // op 52066: put
  db->Put(write_options, "key11615", "value13677");

  // op 52067: put
  db->Put(write_options, "key11495", "value107946");

  // op 52068: get
  status = db->Get(read_options, "key1756", &value);
  if (status.ok()) {
    out << "GET key1756 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52069: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3341", "key6980", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3341, key6980) size=" << scan_result.size() << std::endl;
  }

  // op 52070: get
  status = db->Get(read_options, "key8007", &value);
  if (status.ok()) {
    out << "GET key8007 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52071: put
  db->Put(write_options, "key11409", "value36192");

  // op 52072: put
  db->Put(write_options, "key8306", "value98630");

  // op 52073: get
  status = db->Get(read_options, "key3525", &value);
  if (status.ok()) {
    out << "GET key3525 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52074: put
  db->Put(write_options, "key5727", "value55067");

  // op 52075: put
  db->Put(write_options, "key4983", "value101965");

  // op 52076: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5301", "key6171", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5301, key6171) size=" << scan_result.size() << std::endl;
  }

  // op 52077: get
  status = db->Get(read_options, "key10306", &value);
  if (status.ok()) {
    out << "GET key10306 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52078: put
  db->Put(write_options, "key2638", "value87535");

  // op 52079: put
  db->Put(write_options, "key8477", "value64429");

  // op 52080: put
  db->Put(write_options, "key1129", "value44981");

  // op 52081: get
  status = db->Get(read_options, "key4794", &value);
  if (status.ok()) {
    out << "GET key4794 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52082: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 52083: put
  db->Put(write_options, "key3081", "value40996");

  // op 52084: get
  status = db->Get(read_options, "key5860", &value);
  if (status.ok()) {
    out << "GET key5860 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52085: delete_range
  status = db->DeleteRange(write_options, "key0073", "key6478");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 52086: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9938", "key10083", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9938, key10083) size=" << scan_result.size() << std::endl;
  }

  // op 52087: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0265", "key10641", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0265, key10641) size=" << scan_result.size() << std::endl;
  }

  // op 52088: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2783", "key3803", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2783, key3803) size=" << scan_result.size() << std::endl;
  }

  // op 52089: get
  status = db->Get(read_options, "key1707", &value);
  if (status.ok()) {
    out << "GET key1707 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52090: put
  db->Put(write_options, "key4086", "value73160");

  // op 52091: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 52092: delete
  db->Delete(write_options, "key11040");

  // op 52093: get
  status = db->Get(read_options, "key4497", &value);
  if (status.ok()) {
    out << "GET key4497 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52094: get
  status = db->Get(read_options, "key10371", &value);
  if (status.ok()) {
    out << "GET key10371 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52095: delete_range
  status = db->DeleteRange(write_options, "key4378", "key8292");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 52096: put
  db->Put(write_options, "key9178", "value88930");

  // op 52097: put
  db->Put(write_options, "key3125", "value5492");

  // op 52098: put
  db->Put(write_options, "key4581", "value92456");

  // op 52099: delete_range
  status = db->DeleteRange(write_options, "key4220", "key5708");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 52100: put
  db->Put(write_options, "key1926", "value95828");

  // op 52101: put
  db->Put(write_options, "key10698", "value20797");

  // op 52102: delete_range
  status = db->DeleteRange(write_options, "key4120", "key9213");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 52103: get
  status = db->Get(read_options, "key0860", &value);
  if (status.ok()) {
    out << "GET key0860 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52104: delete
  db->Delete(write_options, "key2381");

  // op 52105: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 52106: put
  db->Put(write_options, "key7636", "value102116");

  // op 52107: delete_range
  status = db->DeleteRange(write_options, "key0128", "key2499");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 52108: put
  db->Put(write_options, "key0321", "value34475");

  // op 52109: get
  status = db->Get(read_options, "key9819", &value);
  if (status.ok()) {
    out << "GET key9819 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52110: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1190", "key11509", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1190, key11509) size=" << scan_result.size() << std::endl;
  }

  // op 52111: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4692", "key5644", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4692, key5644) size=" << scan_result.size() << std::endl;
  }

  // op 52112: delete_range
  status = db->DeleteRange(write_options, "key2844", "key6011");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 52113: delete
  db->Delete(write_options, "key6322");

  // op 52114: get
  status = db->Get(read_options, "key2425", &value);
  if (status.ok()) {
    out << "GET key2425 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52115: put
  db->Put(write_options, "key9254", "value8450");

  // op 52116: get
  status = db->Get(read_options, "key4921", &value);
  if (status.ok()) {
    out << "GET key4921 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52117: put
  db->Put(write_options, "key1006", "value36503");

  // op 52118: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6570", "key11078", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6570, key11078) size=" << scan_result.size() << std::endl;
  }

  // op 52119: get
  status = db->Get(read_options, "key5607", &value);
  if (status.ok()) {
    out << "GET key5607 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52120: get
  status = db->Get(read_options, "key4697", &value);
  if (status.ok()) {
    out << "GET key4697 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52121: put
  db->Put(write_options, "key5770", "value60364");

  // op 52122: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5491", "key11475", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5491, key11475) size=" << scan_result.size() << std::endl;
  }

  // op 52123: get
  status = db->Get(read_options, "key6818", &value);
  if (status.ok()) {
    out << "GET key6818 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52124: get
  status = db->Get(read_options, "key5186", &value);
  if (status.ok()) {
    out << "GET key5186 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52125: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5451", "key8851", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5451, key8851) size=" << scan_result.size() << std::endl;
  }

  // op 52126: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 52127: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5465", "key7915", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5465, key7915) size=" << scan_result.size() << std::endl;
  }

  // op 52128: put
  db->Put(write_options, "key6184", "value43998");

  // op 52129: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4667", "key5384", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4667, key5384) size=" << scan_result.size() << std::endl;
  }

  // op 52130: put
  db->Put(write_options, "key7685", "value50753");

  // op 52131: put
  db->Put(write_options, "key6611", "value44629");

  // op 52132: get
  status = db->Get(read_options, "key6827", &value);
  if (status.ok()) {
    out << "GET key6827 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52133: put
  db->Put(write_options, "key2209", "value6240");

  // op 52134: get
  status = db->Get(read_options, "key3841", &value);
  if (status.ok()) {
    out << "GET key3841 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52135: get
  status = db->Get(read_options, "key2551", &value);
  if (status.ok()) {
    out << "GET key2551 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52136: put
  db->Put(write_options, "key0012", "value68454");

  // op 52137: delete
  db->Delete(write_options, "key0142");

  // op 52138: put
  db->Put(write_options, "key0559", "value100002");

  // op 52139: get
  status = db->Get(read_options, "key6168", &value);
  if (status.ok()) {
    out << "GET key6168 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52140: put
  db->Put(write_options, "key10591", "value101035");

  // op 52141: put
  db->Put(write_options, "key9025", "value34124");

  // op 52142: delete_range
  status = db->DeleteRange(write_options, "key4419", "key6169");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 52143: put
  db->Put(write_options, "key0887", "value72557");

  // op 52144: get
  status = db->Get(read_options, "key4678", &value);
  if (status.ok()) {
    out << "GET key4678 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52145: delete
  db->Delete(write_options, "key11659");

  // op 52146: get
  status = db->Get(read_options, "key8154", &value);
  if (status.ok()) {
    out << "GET key8154 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52147: get
  status = db->Get(read_options, "key11224", &value);
  if (status.ok()) {
    out << "GET key11224 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52148: delete_range
  status = db->DeleteRange(write_options, "key2502", "key6241");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 52149: get
  status = db->Get(read_options, "key3593", &value);
  if (status.ok()) {
    out << "GET key3593 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52150: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1800", "key3502", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1800, key3502) size=" << scan_result.size() << std::endl;
  }

  // op 52151: put
  db->Put(write_options, "key9283", "value75143");

  // op 52152: put
  db->Put(write_options, "key10186", "value102904");

  // op 52153: delete_range
  status = db->DeleteRange(write_options, "key3832", "key6265");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 52154: delete_range
  status = db->DeleteRange(write_options, "key2755", "key3681");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 52155: get
  status = db->Get(read_options, "key11497", &value);
  if (status.ok()) {
    out << "GET key11497 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52156: delete_range
  status = db->DeleteRange(write_options, "key2251", "key4125");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 52157: delete
  db->Delete(write_options, "key2397");

  // op 52158: put
  db->Put(write_options, "key4852", "value6759");

  // op 52159: put
  db->Put(write_options, "key11419", "value86177");

  // op 52160: put
  db->Put(write_options, "key3695", "value42080");

  // op 52161: get
  status = db->Get(read_options, "key0562", &value);
  if (status.ok()) {
    out << "GET key0562 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52162: get
  status = db->Get(read_options, "key1468", &value);
  if (status.ok()) {
    out << "GET key1468 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52163: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7283", "key7926", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7283, key7926) size=" << scan_result.size() << std::endl;
  }

  // op 52164: put
  db->Put(write_options, "key0919", "value79153");

  // op 52165: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 52166: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4582", "key5585", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4582, key5585) size=" << scan_result.size() << std::endl;
  }

  // op 52167: put
  db->Put(write_options, "key0458", "value86202");

  // op 52168: get
  status = db->Get(read_options, "key1550", &value);
  if (status.ok()) {
    out << "GET key1550 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52169: put
  db->Put(write_options, "key0051", "value85494");

  // op 52170: put
  db->Put(write_options, "key10909", "value85925");

  // op 52171: put
  db->Put(write_options, "key3386", "value99013");

  // op 52172: delete
  db->Delete(write_options, "key1195");

  // op 52173: put
  db->Put(write_options, "key10817", "value115968");

  // op 52174: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7015", "key10102", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7015, key10102) size=" << scan_result.size() << std::endl;
  }

  // op 52175: put
  db->Put(write_options, "key1149", "value107772");

  // op 52176: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 52177: put
  db->Put(write_options, "key7863", "value15061");

  // op 52178: put
  db->Put(write_options, "key8822", "value98577");

  // op 52179: get
  status = db->Get(read_options, "key5915", &value);
  if (status.ok()) {
    out << "GET key5915 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52180: put
  db->Put(write_options, "key1856", "value96711");

  // op 52181: delete_range
  status = db->DeleteRange(write_options, "key8640", "key9427");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 52182: delete
  db->Delete(write_options, "key10952");

  // op 52183: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2499", "key7057", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2499, key7057) size=" << scan_result.size() << std::endl;
  }

  // op 52184: delete_range
  status = db->DeleteRange(write_options, "key0896", "key1651");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 52185: put
  db->Put(write_options, "key10906", "value89282");

  // op 52186: put
  db->Put(write_options, "key8676", "value60633");

  // op 52187: put
  db->Put(write_options, "key10002", "value83725");

  // op 52188: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1803", "key5071", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1803, key5071) size=" << scan_result.size() << std::endl;
  }

  // op 52189: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8392", "key10792", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8392, key10792) size=" << scan_result.size() << std::endl;
  }

  // op 52190: get
  status = db->Get(read_options, "key1969", &value);
  if (status.ok()) {
    out << "GET key1969 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52191: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4849", "key7488", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4849, key7488) size=" << scan_result.size() << std::endl;
  }

  // op 52192: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0636", "key7326", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0636, key7326) size=" << scan_result.size() << std::endl;
  }

  // op 52193: get
  status = db->Get(read_options, "key0003", &value);
  if (status.ok()) {
    out << "GET key0003 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52194: delete
  db->Delete(write_options, "key11025");

  // op 52195: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8442", "key11673", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8442, key11673) size=" << scan_result.size() << std::endl;
  }

  // op 52196: delete_range
  status = db->DeleteRange(write_options, "key1427", "key10353");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 52197: delete
  db->Delete(write_options, "key10899");

  // op 52198: put
  db->Put(write_options, "key9061", "value8210");

  // op 52199: get
  status = db->Get(read_options, "key9117", &value);
  if (status.ok()) {
    out << "GET key9117 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52200: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3153", "key4994", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3153, key4994) size=" << scan_result.size() << std::endl;
  }

  // op 52201: put
  db->Put(write_options, "key1464", "value115433");

  // op 52202: put
  db->Put(write_options, "key7360", "value84801");

  // op 52203: get
  status = db->Get(read_options, "key10363", &value);
  if (status.ok()) {
    out << "GET key10363 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52204: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 52205: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6628", "key7109", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6628, key7109) size=" << scan_result.size() << std::endl;
  }

  // op 52206: put
  db->Put(write_options, "key6886", "value47066");

  // op 52207: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9508", "key10585", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9508, key10585) size=" << scan_result.size() << std::endl;
  }

  // op 52208: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7928", "key11214", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7928, key11214) size=" << scan_result.size() << std::endl;
  }

  // op 52209: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 52210: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4948", "key6957", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4948, key6957) size=" << scan_result.size() << std::endl;
  }

  // op 52211: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 52212: put
  db->Put(write_options, "key10466", "value86716");

  // op 52213: get
  status = db->Get(read_options, "key3536", &value);
  if (status.ok()) {
    out << "GET key3536 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52214: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6008", "key8519", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6008, key8519) size=" << scan_result.size() << std::endl;
  }

  // op 52215: put
  db->Put(write_options, "key7871", "value107887");

  // op 52216: delete
  db->Delete(write_options, "key6503");

  // op 52217: delete_range
  status = db->DeleteRange(write_options, "key6301", "key6317");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 52218: put
  db->Put(write_options, "key9689", "value108439");

  // op 52219: put
  db->Put(write_options, "key8792", "value64958");

  // op 52220: put
  db->Put(write_options, "key8392", "value51964");

  // op 52221: delete_range
  status = db->DeleteRange(write_options, "key7384", "key9725");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 52222: put
  db->Put(write_options, "key6347", "value91766");

  // op 52223: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2177", "key10015", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2177, key10015) size=" << scan_result.size() << std::endl;
  }

  // op 52224: put
  db->Put(write_options, "key7390", "value39742");

  // op 52225: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8507", "key8750", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8507, key8750) size=" << scan_result.size() << std::endl;
  }

  // op 52226: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8601", "key11404", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8601, key11404) size=" << scan_result.size() << std::endl;
  }

  // op 52227: put
  db->Put(write_options, "key5192", "value101925");

  // op 52228: put
  db->Put(write_options, "key3631", "value16428");

  // op 52229: put
  db->Put(write_options, "key3133", "value16285");

  // op 52230: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 52231: put
  db->Put(write_options, "key6953", "value0285");

  // op 52232: put
  db->Put(write_options, "key10068", "value44493");

  // op 52233: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6964", "key10268", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6964, key10268) size=" << scan_result.size() << std::endl;
  }

  // op 52234: get
  status = db->Get(read_options, "key4946", &value);
  if (status.ok()) {
    out << "GET key4946 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52235: put
  db->Put(write_options, "key0729", "value95813");

  // op 52236: put
  db->Put(write_options, "key6794", "value95980");

  // op 52237: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0960", "key3029", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0960, key3029) size=" << scan_result.size() << std::endl;
  }

  // op 52238: put
  db->Put(write_options, "key0844", "value38952");

  // op 52239: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3174", "key9795", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3174, key9795) size=" << scan_result.size() << std::endl;
  }

  // op 52240: put
  db->Put(write_options, "key10280", "value82200");

  // op 52241: delete_range
  status = db->DeleteRange(write_options, "key1393", "key2998");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 52242: delete_range
  status = db->DeleteRange(write_options, "key0311", "key3780");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 52243: put
  db->Put(write_options, "key10375", "value97463");

  // op 52244: put
  db->Put(write_options, "key9446", "value47801");

  // op 52245: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4504", "key10308", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4504, key10308) size=" << scan_result.size() << std::endl;
  }

  // op 52246: delete_range
  status = db->DeleteRange(write_options, "key0280", "key10540");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 52247: get
  status = db->Get(read_options, "key4647", &value);
  if (status.ok()) {
    out << "GET key4647 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52248: put
  db->Put(write_options, "key6856", "value69576");

  // op 52249: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 52250: put
  db->Put(write_options, "key3187", "value27855");

  // op 52251: put
  db->Put(write_options, "key9556", "value86929");

  // op 52252: delete_range
  status = db->DeleteRange(write_options, "key2181", "key3097");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 52253: delete
  db->Delete(write_options, "key8851");

  // op 52254: put
  db->Put(write_options, "key3848", "value91140");

  // op 52255: put
  db->Put(write_options, "key2961", "value59323");

  // op 52256: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6195", "key7483", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6195, key7483) size=" << scan_result.size() << std::endl;
  }

  // op 52257: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0045", "key6886", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0045, key6886) size=" << scan_result.size() << std::endl;
  }

  // op 52258: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1029", "key10356", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1029, key10356) size=" << scan_result.size() << std::endl;
  }

  // op 52259: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 52260: delete_range
  status = db->DeleteRange(write_options, "key2180", "key6494");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 52261: get
  status = db->Get(read_options, "key1351", &value);
  if (status.ok()) {
    out << "GET key1351 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52262: get
  status = db->Get(read_options, "key4567", &value);
  if (status.ok()) {
    out << "GET key4567 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52263: get
  status = db->Get(read_options, "key1175", &value);
  if (status.ok()) {
    out << "GET key1175 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52264: put
  db->Put(write_options, "key4454", "value94013");

  // op 52265: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9296", "key11481", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9296, key11481) size=" << scan_result.size() << std::endl;
  }

  // op 52266: put
  db->Put(write_options, "key7116", "value5113");

  // op 52267: delete
  db->Delete(write_options, "key5667");

  // op 52268: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5964", "key7495", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5964, key7495) size=" << scan_result.size() << std::endl;
  }

  // op 52269: put
  db->Put(write_options, "key5806", "value95841");

  // op 52270: get
  status = db->Get(read_options, "key0397", &value);
  if (status.ok()) {
    out << "GET key0397 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52271: put
  db->Put(write_options, "key6773", "value114766");

  // op 52272: delete_range
  status = db->DeleteRange(write_options, "key1616", "key6475");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 52273: put
  db->Put(write_options, "key5358", "value14236");

  // op 52274: delete
  db->Delete(write_options, "key2308");

  // op 52275: delete_range
  status = db->DeleteRange(write_options, "key4110", "key10472");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 52276: get
  status = db->Get(read_options, "key4236", &value);
  if (status.ok()) {
    out << "GET key4236 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52277: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4721", "key10140", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4721, key10140) size=" << scan_result.size() << std::endl;
  }

  // op 52278: delete
  db->Delete(write_options, "key0418");

  // op 52279: put
  db->Put(write_options, "key10129", "value16158");

  // op 52280: put
  db->Put(write_options, "key10029", "value83143");

  // op 52281: put
  db->Put(write_options, "key3512", "value99161");

  // op 52282: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5381", "key6992", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5381, key6992) size=" << scan_result.size() << std::endl;
  }

  // op 52283: put
  db->Put(write_options, "key2056", "value101379");

  // op 52284: put
  db->Put(write_options, "key0553", "value117188");

  // op 52285: delete
  db->Delete(write_options, "key3093");

  // op 52286: put
  db->Put(write_options, "key3319", "value71054");

  // op 52287: delete_range
  status = db->DeleteRange(write_options, "key7520", "key11308");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 52288: put
  db->Put(write_options, "key1677", "value38969");

  // op 52289: put
  db->Put(write_options, "key4029", "value80349");

  // op 52290: get
  status = db->Get(read_options, "key6943", &value);
  if (status.ok()) {
    out << "GET key6943 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52291: put
  db->Put(write_options, "key3760", "value0736");

  // op 52292: get
  status = db->Get(read_options, "key7523", &value);
  if (status.ok()) {
    out << "GET key7523 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52293: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0873", "key3238", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0873, key3238) size=" << scan_result.size() << std::endl;
  }

  // op 52294: put
  db->Put(write_options, "key6972", "value89992");

  // op 52295: put
  db->Put(write_options, "key4513", "value52575");

  // op 52296: get
  status = db->Get(read_options, "key6507", &value);
  if (status.ok()) {
    out << "GET key6507 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52297: put
  db->Put(write_options, "key11492", "value1509");

  // op 52298: put
  db->Put(write_options, "key9431", "value74051");

  // op 52299: put
  db->Put(write_options, "key9184", "value57048");

  // op 52300: put
  db->Put(write_options, "key6579", "value24873");

  // op 52301: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5482", "key6028", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5482, key6028) size=" << scan_result.size() << std::endl;
  }

  // op 52302: delete
  db->Delete(write_options, "key3560");

  // op 52303: get
  status = db->Get(read_options, "key7928", &value);
  if (status.ok()) {
    out << "GET key7928 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52304: put
  db->Put(write_options, "key3881", "value76294");

  // op 52305: delete_range
  status = db->DeleteRange(write_options, "key4604", "key5541");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 52306: delete_range
  status = db->DeleteRange(write_options, "key2245", "key4321");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 52307: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 52308: put
  db->Put(write_options, "key3582", "value64753");

  // op 52309: get
  status = db->Get(read_options, "key4059", &value);
  if (status.ok()) {
    out << "GET key4059 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52310: put
  db->Put(write_options, "key1292", "value112978");

  // op 52311: delete
  db->Delete(write_options, "key0551");

  // op 52312: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1831", "key7426", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1831, key7426) size=" << scan_result.size() << std::endl;
  }

  // op 52313: put
  db->Put(write_options, "key5593", "value94595");

  // op 52314: get
  status = db->Get(read_options, "key8607", &value);
  if (status.ok()) {
    out << "GET key8607 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52315: delete
  db->Delete(write_options, "key4380");

  // op 52316: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2711", "key5018", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2711, key5018) size=" << scan_result.size() << std::endl;
  }

  // op 52317: put
  db->Put(write_options, "key3223", "value116553");

  // op 52318: delete_range
  status = db->DeleteRange(write_options, "key9252", "key10963");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 52319: get
  status = db->Get(read_options, "key6440", &value);
  if (status.ok()) {
    out << "GET key6440 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52320: get
  status = db->Get(read_options, "key4759", &value);
  if (status.ok()) {
    out << "GET key4759 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52321: put
  db->Put(write_options, "key9603", "value68293");

  // op 52322: delete_range
  status = db->DeleteRange(write_options, "key6565", "key9019");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 52323: delete_range
  status = db->DeleteRange(write_options, "key8255", "key11508");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 52324: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1613", "key2340", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1613, key2340) size=" << scan_result.size() << std::endl;
  }

  // op 52325: delete
  db->Delete(write_options, "key4398");

  // op 52326: put
  db->Put(write_options, "key0010", "value63313");

  // op 52327: delete
  db->Delete(write_options, "key0068");

  // op 52328: delete_range
  status = db->DeleteRange(write_options, "key3858", "key6293");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 52329: put
  db->Put(write_options, "key11535", "value103815");

  // op 52330: put
  db->Put(write_options, "key7293", "value71526");

  // op 52331: put
  db->Put(write_options, "key8620", "value109104");

  // op 52332: put
  db->Put(write_options, "key5067", "value20341");

  // op 52333: put
  db->Put(write_options, "key7846", "value56472");

  // op 52334: get
  status = db->Get(read_options, "key2616", &value);
  if (status.ok()) {
    out << "GET key2616 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52335: put
  db->Put(write_options, "key3147", "value65291");

  // op 52336: put
  db->Put(write_options, "key5148", "value112509");

  // op 52337: get
  status = db->Get(read_options, "key2707", &value);
  if (status.ok()) {
    out << "GET key2707 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52338: put
  db->Put(write_options, "key10798", "value76683");

  // op 52339: put
  db->Put(write_options, "key10775", "value25297");

  // op 52340: put
  db->Put(write_options, "key9633", "value117245");

  // op 52341: get
  status = db->Get(read_options, "key4393", &value);
  if (status.ok()) {
    out << "GET key4393 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52342: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2592", "key3193", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2592, key3193) size=" << scan_result.size() << std::endl;
  }

  // op 52343: get
  status = db->Get(read_options, "key7792", &value);
  if (status.ok()) {
    out << "GET key7792 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52344: put
  db->Put(write_options, "key5182", "value15733");

  // op 52345: put
  db->Put(write_options, "key9507", "value102359");

  // op 52346: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6018", "key9480", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6018, key9480) size=" << scan_result.size() << std::endl;
  }

  // op 52347: put
  db->Put(write_options, "key7321", "value27257");

  // op 52348: get
  status = db->Get(read_options, "key4449", &value);
  if (status.ok()) {
    out << "GET key4449 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52349: put
  db->Put(write_options, "key11554", "value89085");

  // op 52350: put
  db->Put(write_options, "key5904", "value96216");

  // op 52351: put
  db->Put(write_options, "key1172", "value109133");

  // op 52352: put
  db->Put(write_options, "key8978", "value47161");

  // op 52353: delete_range
  status = db->DeleteRange(write_options, "key3762", "key5391");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 52354: get
  status = db->Get(read_options, "key6896", &value);
  if (status.ok()) {
    out << "GET key6896 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52355: put
  db->Put(write_options, "key0407", "value51557");

  // op 52356: put
  db->Put(write_options, "key3699", "value101065");

  // op 52357: put
  db->Put(write_options, "key2653", "value56405");

  // op 52358: delete_range
  status = db->DeleteRange(write_options, "key7803", "key8807");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 52359: get
  status = db->Get(read_options, "key11018", &value);
  if (status.ok()) {
    out << "GET key11018 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52360: put
  db->Put(write_options, "key8818", "value1843");

  // op 52361: delete
  db->Delete(write_options, "key5005");

  // op 52362: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8195", "key11034", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8195, key11034) size=" << scan_result.size() << std::endl;
  }

  // op 52363: put
  db->Put(write_options, "key5017", "value66897");

  // op 52364: put
  db->Put(write_options, "key8890", "value62190");

  // op 52365: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4472", "key5591", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4472, key5591) size=" << scan_result.size() << std::endl;
  }

  // op 52366: put
  db->Put(write_options, "key2106", "value1941");

  // op 52367: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6492", "key8615", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6492, key8615) size=" << scan_result.size() << std::endl;
  }

  // op 52368: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1926", "key10257", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1926, key10257) size=" << scan_result.size() << std::endl;
  }

  // op 52369: put
  db->Put(write_options, "key0354", "value15771");

  // op 52370: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 52371: put
  db->Put(write_options, "key4114", "value71346");

  // op 52372: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3143", "key3564", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3143, key3564) size=" << scan_result.size() << std::endl;
  }

  // op 52373: put
  db->Put(write_options, "key6818", "value35744");

  // op 52374: get
  status = db->Get(read_options, "key10668", &value);
  if (status.ok()) {
    out << "GET key10668 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52375: put
  db->Put(write_options, "key5102", "value86007");

  // op 52376: delete_range
  status = db->DeleteRange(write_options, "key3576", "key9958");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 52377: put
  db->Put(write_options, "key6414", "value71748");

  // op 52378: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4864", "key11006", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4864, key11006) size=" << scan_result.size() << std::endl;
  }

  // op 52379: delete
  db->Delete(write_options, "key8881");

  // op 52380: put
  db->Put(write_options, "key7020", "value106154");

  // op 52381: put
  db->Put(write_options, "key3037", "value17022");

  // op 52382: delete_range
  status = db->DeleteRange(write_options, "key0790", "key9444");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 52383: delete
  db->Delete(write_options, "key8518");

  // op 52384: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6621", "key8089", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6621, key8089) size=" << scan_result.size() << std::endl;
  }

  // op 52385: put
  db->Put(write_options, "key4874", "value49577");

  // op 52386: delete_range
  status = db->DeleteRange(write_options, "key2987", "key10776");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 52387: put
  db->Put(write_options, "key6108", "value100243");

  // op 52388: get
  status = db->Get(read_options, "key7711", &value);
  if (status.ok()) {
    out << "GET key7711 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52389: put
  db->Put(write_options, "key1736", "value60585");

  // op 52390: put
  db->Put(write_options, "key7722", "value116417");

  // op 52391: get
  status = db->Get(read_options, "key4860", &value);
  if (status.ok()) {
    out << "GET key4860 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52392: delete_range
  status = db->DeleteRange(write_options, "key6678", "key8903");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 52393: delete_range
  status = db->DeleteRange(write_options, "key0051", "key6001");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 52394: put
  db->Put(write_options, "key11595", "value76121");

  // op 52395: put
  db->Put(write_options, "key9353", "value28797");

  // op 52396: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8447", "key11591", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8447, key11591) size=" << scan_result.size() << std::endl;
  }

  // op 52397: get
  status = db->Get(read_options, "key9829", &value);
  if (status.ok()) {
    out << "GET key9829 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52398: get
  status = db->Get(read_options, "key8820", &value);
  if (status.ok()) {
    out << "GET key8820 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52399: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7178", "key8617", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7178, key8617) size=" << scan_result.size() << std::endl;
  }

  // op 52400: delete_range
  status = db->DeleteRange(write_options, "key2992", "key6460");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 52401: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0636", "key4926", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0636, key4926) size=" << scan_result.size() << std::endl;
  }

  // op 52402: get
  status = db->Get(read_options, "key0917", &value);
  if (status.ok()) {
    out << "GET key0917 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52403: put
  db->Put(write_options, "key7511", "value11598");

  // op 52404: get
  status = db->Get(read_options, "key4300", &value);
  if (status.ok()) {
    out << "GET key4300 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52405: delete_range
  status = db->DeleteRange(write_options, "key3278", "key4341");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 52406: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7709", "key8544", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7709, key8544) size=" << scan_result.size() << std::endl;
  }

  // op 52407: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8434", "key11107", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8434, key11107) size=" << scan_result.size() << std::endl;
  }

  // op 52408: put
  db->Put(write_options, "key6705", "value48217");

  // op 52409: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1443", "key3997", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1443, key3997) size=" << scan_result.size() << std::endl;
  }

  // op 52410: get
  status = db->Get(read_options, "key7354", &value);
  if (status.ok()) {
    out << "GET key7354 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52411: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 52412: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1499", "key11220", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1499, key11220) size=" << scan_result.size() << std::endl;
  }

  // op 52413: put
  db->Put(write_options, "key8793", "value20438");

  // op 52414: put
  db->Put(write_options, "key2490", "value113927");

  // op 52415: delete_range
  status = db->DeleteRange(write_options, "key5291", "key7389");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 52416: get
  status = db->Get(read_options, "key4844", &value);
  if (status.ok()) {
    out << "GET key4844 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52417: get
  status = db->Get(read_options, "key5663", &value);
  if (status.ok()) {
    out << "GET key5663 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52418: get
  status = db->Get(read_options, "key9516", &value);
  if (status.ok()) {
    out << "GET key9516 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52419: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7047", "key8340", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7047, key8340) size=" << scan_result.size() << std::endl;
  }

  // op 52420: get
  status = db->Get(read_options, "key1480", &value);
  if (status.ok()) {
    out << "GET key1480 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52421: get
  status = db->Get(read_options, "key1071", &value);
  if (status.ok()) {
    out << "GET key1071 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52422: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0629", "key5340", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0629, key5340) size=" << scan_result.size() << std::endl;
  }

  // op 52423: put
  db->Put(write_options, "key4093", "value73672");

  // op 52424: get
  status = db->Get(read_options, "key4243", &value);
  if (status.ok()) {
    out << "GET key4243 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52425: delete_range
  status = db->DeleteRange(write_options, "key2901", "key6637");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 52426: put
  db->Put(write_options, "key2506", "value27366");

  // op 52427: delete_range
  status = db->DeleteRange(write_options, "key3686", "key8125");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 52428: delete_range
  status = db->DeleteRange(write_options, "key3227", "key4770");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 52429: delete_range
  status = db->DeleteRange(write_options, "key1175", "key5116");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 52430: put
  db->Put(write_options, "key4416", "value34491");

  // op 52431: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0715", "key3764", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0715, key3764) size=" << scan_result.size() << std::endl;
  }

  // op 52432: put
  db->Put(write_options, "key6579", "value118174");

  // op 52433: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6107", "key7039", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6107, key7039) size=" << scan_result.size() << std::endl;
  }

  // op 52434: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0468", "key9464", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0468, key9464) size=" << scan_result.size() << std::endl;
  }

  // op 52435: delete
  db->Delete(write_options, "key10159");

  // op 52436: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6508", "key10285", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6508, key10285) size=" << scan_result.size() << std::endl;
  }

  // op 52437: put
  db->Put(write_options, "key0930", "value72547");

  // op 52438: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 52439: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 52440: get
  status = db->Get(read_options, "key8566", &value);
  if (status.ok()) {
    out << "GET key8566 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52441: put
  db->Put(write_options, "key11383", "value50516");

  // op 52442: put
  db->Put(write_options, "key6300", "value20301");

  // op 52443: put
  db->Put(write_options, "key10559", "value52746");

  // op 52444: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3152", "key9643", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3152, key9643) size=" << scan_result.size() << std::endl;
  }

  // op 52445: put
  db->Put(write_options, "key7950", "value77386");

  // op 52446: get
  status = db->Get(read_options, "key10796", &value);
  if (status.ok()) {
    out << "GET key10796 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52447: put
  db->Put(write_options, "key10542", "value44481");

  // op 52448: put
  db->Put(write_options, "key0820", "value71900");

  // op 52449: delete
  db->Delete(write_options, "key11393");

  // op 52450: delete_range
  status = db->DeleteRange(write_options, "key0984", "key2249");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 52451: get
  status = db->Get(read_options, "key1268", &value);
  if (status.ok()) {
    out << "GET key1268 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52452: delete
  db->Delete(write_options, "key11494");

  // op 52453: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4442", "key9048", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4442, key9048) size=" << scan_result.size() << std::endl;
  }

  // op 52454: delete
  db->Delete(write_options, "key10010");

  // op 52455: put
  db->Put(write_options, "key6221", "value5984");

  // op 52456: get
  status = db->Get(read_options, "key2798", &value);
  if (status.ok()) {
    out << "GET key2798 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52457: put
  db->Put(write_options, "key0679", "value35268");

  // op 52458: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1161", "key1481", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1161, key1481) size=" << scan_result.size() << std::endl;
  }

  // op 52459: delete
  db->Delete(write_options, "key0995");

  // op 52460: delete
  db->Delete(write_options, "key3506");

  // op 52461: delete
  db->Delete(write_options, "key9200");

  // op 52462: get
  status = db->Get(read_options, "key10351", &value);
  if (status.ok()) {
    out << "GET key10351 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52463: put
  db->Put(write_options, "key4622", "value98905");

  // op 52464: put
  db->Put(write_options, "key8979", "value18921");

  // op 52465: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 52466: put
  db->Put(write_options, "key9021", "value45905");

  // op 52467: delete_range
  status = db->DeleteRange(write_options, "key1659", "key8483");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 52468: delete
  db->Delete(write_options, "key1379");

  // op 52469: get
  status = db->Get(read_options, "key0120", &value);
  if (status.ok()) {
    out << "GET key0120 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52470: delete_range
  status = db->DeleteRange(write_options, "key5189", "key10805");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 52471: get
  status = db->Get(read_options, "key4375", &value);
  if (status.ok()) {
    out << "GET key4375 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52472: get
  status = db->Get(read_options, "key2889", &value);
  if (status.ok()) {
    out << "GET key2889 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52473: put
  db->Put(write_options, "key3191", "value90925");

  // op 52474: get
  status = db->Get(read_options, "key5995", &value);
  if (status.ok()) {
    out << "GET key5995 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52475: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 52476: put
  db->Put(write_options, "key1958", "value51255");

  // op 52477: put
  db->Put(write_options, "key11114", "value53062");

  // op 52478: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2870", "key5262", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2870, key5262) size=" << scan_result.size() << std::endl;
  }

  // op 52479: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4816", "key6242", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4816, key6242) size=" << scan_result.size() << std::endl;
  }

  // op 52480: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5635", "key11201", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5635, key11201) size=" << scan_result.size() << std::endl;
  }

  // op 52481: put
  db->Put(write_options, "key7360", "value83034");

  // op 52482: put
  db->Put(write_options, "key4249", "value98590");

  // op 52483: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2488", "key8344", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2488, key8344) size=" << scan_result.size() << std::endl;
  }

  // op 52484: delete_range
  status = db->DeleteRange(write_options, "key3559", "key5331");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 52485: put
  db->Put(write_options, "key5843", "value43300");

  // op 52486: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2963", "key5421", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2963, key5421) size=" << scan_result.size() << std::endl;
  }

  // op 52487: put
  db->Put(write_options, "key8766", "value51937");

  // op 52488: get
  status = db->Get(read_options, "key8886", &value);
  if (status.ok()) {
    out << "GET key8886 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52489: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1244", "key5767", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1244, key5767) size=" << scan_result.size() << std::endl;
  }

  // op 52490: get
  status = db->Get(read_options, "key2428", &value);
  if (status.ok()) {
    out << "GET key2428 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52491: put
  db->Put(write_options, "key6867", "value36441");

  // op 52492: delete
  db->Delete(write_options, "key9053");

  // op 52493: get
  status = db->Get(read_options, "key9388", &value);
  if (status.ok()) {
    out << "GET key9388 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52494: delete
  db->Delete(write_options, "key6869");

  // op 52495: put
  db->Put(write_options, "key6363", "value47614");

  // op 52496: put
  db->Put(write_options, "key3791", "value109119");

  // op 52497: delete
  db->Delete(write_options, "key0407");

  // op 52498: put
  db->Put(write_options, "key11627", "value20454");

  // op 52499: put
  db->Put(write_options, "key8539", "value111350");

  // op 52500: delete
  db->Delete(write_options, "key3550");

  // op 52501: put
  db->Put(write_options, "key10942", "value102896");

  // op 52502: delete
  db->Delete(write_options, "key1582");

  // op 52503: put
  db->Put(write_options, "key10393", "value10861");

  // op 52504: put
  db->Put(write_options, "key1477", "value91861");

  // op 52505: put
  db->Put(write_options, "key9589", "value15668");

  // op 52506: put
  db->Put(write_options, "key2733", "value93184");

  // op 52507: put
  db->Put(write_options, "key7613", "value102689");

  // op 52508: put
  db->Put(write_options, "key3262", "value117804");

  // op 52509: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 52510: delete
  db->Delete(write_options, "key9939");

  // op 52511: put
  db->Put(write_options, "key9154", "value41826");

  // op 52512: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1927", "key3940", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1927, key3940) size=" << scan_result.size() << std::endl;
  }

  // op 52513: put
  db->Put(write_options, "key1632", "value2811");

  // op 52514: put
  db->Put(write_options, "key7636", "value22071");

  // op 52515: get
  status = db->Get(read_options, "key8263", &value);
  if (status.ok()) {
    out << "GET key8263 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52516: delete_range
  status = db->DeleteRange(write_options, "key4368", "key4761");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 52517: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0089", "key4495", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0089, key4495) size=" << scan_result.size() << std::endl;
  }

  // op 52518: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 52519: get
  status = db->Get(read_options, "key1992", &value);
  if (status.ok()) {
    out << "GET key1992 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52520: delete_range
  status = db->DeleteRange(write_options, "key7236", "key7940");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 52521: get
  status = db->Get(read_options, "key6017", &value);
  if (status.ok()) {
    out << "GET key6017 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52522: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5768", "key9547", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5768, key9547) size=" << scan_result.size() << std::endl;
  }

  // op 52523: put
  db->Put(write_options, "key0643", "value5231");

  // op 52524: get
  status = db->Get(read_options, "key2120", &value);
  if (status.ok()) {
    out << "GET key2120 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52525: delete_range
  status = db->DeleteRange(write_options, "key2718", "key9817");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 52526: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 52527: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1103", "key6191", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1103, key6191) size=" << scan_result.size() << std::endl;
  }

  // op 52528: delete_range
  status = db->DeleteRange(write_options, "key2056", "key8629");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 52529: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1454", "key10943", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1454, key10943) size=" << scan_result.size() << std::endl;
  }

  // op 52530: put
  db->Put(write_options, "key2210", "value37987");

  // op 52531: delete
  db->Delete(write_options, "key3919");

  // op 52532: put
  db->Put(write_options, "key7697", "value34769");

  // op 52533: delete_range
  status = db->DeleteRange(write_options, "key6209", "key9124");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 52534: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1049", "key10476", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1049, key10476) size=" << scan_result.size() << std::endl;
  }

  // op 52535: delete_range
  status = db->DeleteRange(write_options, "key1944", "key9037");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 52536: put
  db->Put(write_options, "key1716", "value72551");

  // op 52537: put
  db->Put(write_options, "key3437", "value16346");

  // op 52538: put
  db->Put(write_options, "key5813", "value26726");

  // op 52539: delete_range
  status = db->DeleteRange(write_options, "key4485", "key7801");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 52540: get
  status = db->Get(read_options, "key10859", &value);
  if (status.ok()) {
    out << "GET key10859 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52541: put
  db->Put(write_options, "key11721", "value108325");

  // op 52542: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9342", "key10115", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9342, key10115) size=" << scan_result.size() << std::endl;
  }

  // op 52543: put
  db->Put(write_options, "key2018", "value20606");

  // op 52544: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6785", "key7784", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6785, key7784) size=" << scan_result.size() << std::endl;
  }

  // op 52545: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 52546: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3263", "key10380", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3263, key10380) size=" << scan_result.size() << std::endl;
  }

  // op 52547: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3226", "key3295", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3226, key3295) size=" << scan_result.size() << std::endl;
  }

  // op 52548: put
  db->Put(write_options, "key10322", "value68156");

  // op 52549: delete
  db->Delete(write_options, "key7524");

  // op 52550: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0362", "key11583", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0362, key11583) size=" << scan_result.size() << std::endl;
  }

  // op 52551: get
  status = db->Get(read_options, "key5452", &value);
  if (status.ok()) {
    out << "GET key5452 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52552: delete
  db->Delete(write_options, "key10267");

  // op 52553: put
  db->Put(write_options, "key8495", "value87488");

  // op 52554: put
  db->Put(write_options, "key1101", "value62450");

  // op 52555: get
  status = db->Get(read_options, "key1857", &value);
  if (status.ok()) {
    out << "GET key1857 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52556: get
  status = db->Get(read_options, "key10976", &value);
  if (status.ok()) {
    out << "GET key10976 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52557: get
  status = db->Get(read_options, "key0891", &value);
  if (status.ok()) {
    out << "GET key0891 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52558: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 52559: put
  db->Put(write_options, "key6439", "value88343");

  // op 52560: put
  db->Put(write_options, "key10627", "value56801");

  // op 52561: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 52562: put
  db->Put(write_options, "key1746", "value115547");

  // op 52563: put
  db->Put(write_options, "key5122", "value51282");

  // op 52564: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1127", "key10515", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1127, key10515) size=" << scan_result.size() << std::endl;
  }

  // op 52565: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6293", "key7030", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6293, key7030) size=" << scan_result.size() << std::endl;
  }

  // op 52566: delete_range
  status = db->DeleteRange(write_options, "key0966", "key9181");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 52567: get
  status = db->Get(read_options, "key3126", &value);
  if (status.ok()) {
    out << "GET key3126 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52568: delete_range
  status = db->DeleteRange(write_options, "key5295", "key7118");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 52569: put
  db->Put(write_options, "key7964", "value43749");

  // op 52570: put
  db->Put(write_options, "key8574", "value63986");

  // op 52571: put
  db->Put(write_options, "key6629", "value49925");

  // op 52572: get
  status = db->Get(read_options, "key5875", &value);
  if (status.ok()) {
    out << "GET key5875 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52573: put
  db->Put(write_options, "key10538", "value56417");

  // op 52574: put
  db->Put(write_options, "key4207", "value31864");

  // op 52575: delete_range
  status = db->DeleteRange(write_options, "key5342", "key8992");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 52576: delete_range
  status = db->DeleteRange(write_options, "key1922", "key8833");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 52577: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1960", "key5490", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1960, key5490) size=" << scan_result.size() << std::endl;
  }

  // op 52578: put
  db->Put(write_options, "key1051", "value72588");

  // op 52579: get
  status = db->Get(read_options, "key9583", &value);
  if (status.ok()) {
    out << "GET key9583 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52580: delete
  db->Delete(write_options, "key3011");

  // op 52581: put
  db->Put(write_options, "key7050", "value55906");

  // op 52582: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2573", "key3015", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2573, key3015) size=" << scan_result.size() << std::endl;
  }

  // op 52583: delete_range
  status = db->DeleteRange(write_options, "key8685", "key9833");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 52584: get
  status = db->Get(read_options, "key1405", &value);
  if (status.ok()) {
    out << "GET key1405 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52585: put
  db->Put(write_options, "key4866", "value113307");

  // op 52586: put
  db->Put(write_options, "key6031", "value108998");

  // op 52587: put
  db->Put(write_options, "key1968", "value97722");

  // op 52588: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3519", "key8032", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3519, key8032) size=" << scan_result.size() << std::endl;
  }

  // op 52589: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1690", "key6232", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1690, key6232) size=" << scan_result.size() << std::endl;
  }

  // op 52590: put
  db->Put(write_options, "key1391", "value8808");

  // op 52591: put
  db->Put(write_options, "key5452", "value9352");

  // op 52592: put
  db->Put(write_options, "key10381", "value84038");

  // op 52593: put
  db->Put(write_options, "key3202", "value66844");

  // op 52594: delete
  db->Delete(write_options, "key9247");

  // op 52595: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 52596: delete
  db->Delete(write_options, "key3449");

  // op 52597: delete
  db->Delete(write_options, "key3809");

  // op 52598: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1716", "key9000", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1716, key9000) size=" << scan_result.size() << std::endl;
  }

  // op 52599: put
  db->Put(write_options, "key0113", "value42567");

  // op 52600: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1834", "key1870", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1834, key1870) size=" << scan_result.size() << std::endl;
  }

  // op 52601: delete
  db->Delete(write_options, "key7364");

  // op 52602: put
  db->Put(write_options, "key8848", "value22261");

  // op 52603: get
  status = db->Get(read_options, "key4616", &value);
  if (status.ok()) {
    out << "GET key4616 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52604: put
  db->Put(write_options, "key10688", "value30026");

  // op 52605: get
  status = db->Get(read_options, "key2549", &value);
  if (status.ok()) {
    out << "GET key2549 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52606: get
  status = db->Get(read_options, "key3926", &value);
  if (status.ok()) {
    out << "GET key3926 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52607: put
  db->Put(write_options, "key7557", "value90200");

  // op 52608: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 52609: get
  status = db->Get(read_options, "key2599", &value);
  if (status.ok()) {
    out << "GET key2599 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52610: get
  status = db->Get(read_options, "key2622", &value);
  if (status.ok()) {
    out << "GET key2622 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52611: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0598", "key7522", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0598, key7522) size=" << scan_result.size() << std::endl;
  }

  // op 52612: put
  db->Put(write_options, "key11253", "value38160");

  // op 52613: put
  db->Put(write_options, "key2276", "value55503");

  // op 52614: put
  db->Put(write_options, "key9879", "value90677");

  // op 52615: get
  status = db->Get(read_options, "key11228", &value);
  if (status.ok()) {
    out << "GET key11228 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52616: put
  db->Put(write_options, "key9101", "value12192");

  // op 52617: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 52618: get
  status = db->Get(read_options, "key4283", &value);
  if (status.ok()) {
    out << "GET key4283 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52619: delete
  db->Delete(write_options, "key1715");

  // op 52620: put
  db->Put(write_options, "key6871", "value44279");

  // op 52621: get
  status = db->Get(read_options, "key11034", &value);
  if (status.ok()) {
    out << "GET key11034 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52622: get
  status = db->Get(read_options, "key10673", &value);
  if (status.ok()) {
    out << "GET key10673 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52623: get
  status = db->Get(read_options, "key6188", &value);
  if (status.ok()) {
    out << "GET key6188 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52624: put
  db->Put(write_options, "key5253", "value62421");

  // op 52625: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7363", "key9011", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7363, key9011) size=" << scan_result.size() << std::endl;
  }

  // op 52626: delete
  db->Delete(write_options, "key11114");

  // op 52627: delete_range
  status = db->DeleteRange(write_options, "key3517", "key10820");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 52628: get
  status = db->Get(read_options, "key6818", &value);
  if (status.ok()) {
    out << "GET key6818 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52629: delete
  db->Delete(write_options, "key6545");

  // op 52630: put
  db->Put(write_options, "key11216", "value86480");

  // op 52631: delete_range
  status = db->DeleteRange(write_options, "key1268", "key7774");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 52632: delete_range
  status = db->DeleteRange(write_options, "key5822", "key7022");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 52633: get
  status = db->Get(read_options, "key4127", &value);
  if (status.ok()) {
    out << "GET key4127 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52634: put
  db->Put(write_options, "key9721", "value113029");

  // op 52635: put
  db->Put(write_options, "key5915", "value65731");

  // op 52636: delete_range
  status = db->DeleteRange(write_options, "key5692", "key9370");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 52637: get
  status = db->Get(read_options, "key4302", &value);
  if (status.ok()) {
    out << "GET key4302 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52638: put
  db->Put(write_options, "key10261", "value13620");

  // op 52639: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0522", "key5214", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0522, key5214) size=" << scan_result.size() << std::endl;
  }

  // op 52640: delete
  db->Delete(write_options, "key6282");

  // op 52641: put
  db->Put(write_options, "key3144", "value99431");

  // op 52642: put
  db->Put(write_options, "key5941", "value74432");

  // op 52643: put
  db->Put(write_options, "key8851", "value39931");

  // op 52644: put
  db->Put(write_options, "key11483", "value115538");

  // op 52645: delete
  db->Delete(write_options, "key2179");

  // op 52646: put
  db->Put(write_options, "key8573", "value14841");

  // op 52647: put
  db->Put(write_options, "key2180", "value73351");

  // op 52648: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 52649: put
  db->Put(write_options, "key1646", "value109910");

  // op 52650: delete_range
  status = db->DeleteRange(write_options, "key3501", "key6746");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 52651: get
  status = db->Get(read_options, "key7009", &value);
  if (status.ok()) {
    out << "GET key7009 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52652: put
  db->Put(write_options, "key7590", "value48953");

  // op 52653: get
  status = db->Get(read_options, "key9216", &value);
  if (status.ok()) {
    out << "GET key9216 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52654: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4267", "key8402", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4267, key8402) size=" << scan_result.size() << std::endl;
  }

  // op 52655: get
  status = db->Get(read_options, "key5281", &value);
  if (status.ok()) {
    out << "GET key5281 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52656: put
  db->Put(write_options, "key3815", "value101248");

  // op 52657: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8713", "key8942", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8713, key8942) size=" << scan_result.size() << std::endl;
  }

  // op 52658: delete_range
  status = db->DeleteRange(write_options, "key1225", "key3577");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 52659: get
  status = db->Get(read_options, "key3877", &value);
  if (status.ok()) {
    out << "GET key3877 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52660: get
  status = db->Get(read_options, "key4496", &value);
  if (status.ok()) {
    out << "GET key4496 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52661: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2825", "key7342", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2825, key7342) size=" << scan_result.size() << std::endl;
  }

  // op 52662: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9022", "key11047", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9022, key11047) size=" << scan_result.size() << std::endl;
  }

  // op 52663: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 52664: put
  db->Put(write_options, "key11671", "value85868");

  // op 52665: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3911", "key7233", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3911, key7233) size=" << scan_result.size() << std::endl;
  }

  // op 52666: put
  db->Put(write_options, "key10798", "value104874");

  // op 52667: delete
  db->Delete(write_options, "key9355");

  // op 52668: get
  status = db->Get(read_options, "key8369", &value);
  if (status.ok()) {
    out << "GET key8369 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52669: put
  db->Put(write_options, "key0291", "value53949");

  // op 52670: put
  db->Put(write_options, "key5069", "value104440");

  // op 52671: put
  db->Put(write_options, "key3581", "value68276");

  // op 52672: delete
  db->Delete(write_options, "key0835");

  // op 52673: put
  db->Put(write_options, "key9336", "value57070");

  // op 52674: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3787", "key9570", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3787, key9570) size=" << scan_result.size() << std::endl;
  }

  // op 52675: get
  status = db->Get(read_options, "key1417", &value);
  if (status.ok()) {
    out << "GET key1417 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52676: delete
  db->Delete(write_options, "key8013");

  // op 52677: put
  db->Put(write_options, "key4078", "value5538");

  // op 52678: put
  db->Put(write_options, "key8250", "value56776");

  // op 52679: get
  status = db->Get(read_options, "key8945", &value);
  if (status.ok()) {
    out << "GET key8945 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52680: get
  status = db->Get(read_options, "key6642", &value);
  if (status.ok()) {
    out << "GET key6642 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52681: put
  db->Put(write_options, "key4048", "value75409");

  // op 52682: put
  db->Put(write_options, "key1259", "value50954");

  // op 52683: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1878", "key11593", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1878, key11593) size=" << scan_result.size() << std::endl;
  }

  // op 52684: get
  status = db->Get(read_options, "key4286", &value);
  if (status.ok()) {
    out << "GET key4286 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52685: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0283", "key0638", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0283, key0638) size=" << scan_result.size() << std::endl;
  }

  // op 52686: put
  db->Put(write_options, "key0537", "value39519");

  // op 52687: put
  db->Put(write_options, "key3036", "value37964");

  // op 52688: get
  status = db->Get(read_options, "key10896", &value);
  if (status.ok()) {
    out << "GET key10896 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52689: put
  db->Put(write_options, "key9467", "value31822");

  // op 52690: put
  db->Put(write_options, "key0520", "value22931");

  // op 52691: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0573", "key4578", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0573, key4578) size=" << scan_result.size() << std::endl;
  }

  // op 52692: delete_range
  status = db->DeleteRange(write_options, "key6278", "key11521");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 52693: get
  status = db->Get(read_options, "key3794", &value);
  if (status.ok()) {
    out << "GET key3794 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52694: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2364", "key7015", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2364, key7015) size=" << scan_result.size() << std::endl;
  }

  // op 52695: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4354", "key7985", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4354, key7985) size=" << scan_result.size() << std::endl;
  }

  // op 52696: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9691", "key10717", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9691, key10717) size=" << scan_result.size() << std::endl;
  }

  // op 52697: get
  status = db->Get(read_options, "key2217", &value);
  if (status.ok()) {
    out << "GET key2217 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52698: put
  db->Put(write_options, "key5431", "value65152");

  // op 52699: put
  db->Put(write_options, "key0596", "value45910");

  // op 52700: put
  db->Put(write_options, "key4351", "value38157");

  // op 52701: delete
  db->Delete(write_options, "key1767");

  // op 52702: delete_range
  status = db->DeleteRange(write_options, "key7455", "key8477");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 52703: put
  db->Put(write_options, "key6668", "value105930");

  // op 52704: get
  status = db->Get(read_options, "key10190", &value);
  if (status.ok()) {
    out << "GET key10190 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52705: put
  db->Put(write_options, "key4542", "value96427");

  // op 52706: get
  status = db->Get(read_options, "key9464", &value);
  if (status.ok()) {
    out << "GET key9464 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52707: put
  db->Put(write_options, "key5426", "value27049");

  // op 52708: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2917", "key6514", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2917, key6514) size=" << scan_result.size() << std::endl;
  }

  // op 52709: put
  db->Put(write_options, "key10311", "value61019");

  // op 52710: delete_range
  status = db->DeleteRange(write_options, "key0291", "key5204");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 52711: delete
  db->Delete(write_options, "key9171");

  // op 52712: delete
  db->Delete(write_options, "key11393");

  // op 52713: put
  db->Put(write_options, "key1856", "value55975");

  // op 52714: get
  status = db->Get(read_options, "key8377", &value);
  if (status.ok()) {
    out << "GET key8377 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52715: get
  status = db->Get(read_options, "key10904", &value);
  if (status.ok()) {
    out << "GET key10904 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52716: delete_range
  status = db->DeleteRange(write_options, "key5422", "key6231");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 52717: put
  db->Put(write_options, "key10345", "value24724");

  // op 52718: get
  status = db->Get(read_options, "key3577", &value);
  if (status.ok()) {
    out << "GET key3577 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52719: delete
  db->Delete(write_options, "key11356");

  // op 52720: put
  db->Put(write_options, "key9588", "value60499");

  // op 52721: get
  status = db->Get(read_options, "key1084", &value);
  if (status.ok()) {
    out << "GET key1084 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52722: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8748", "key8827", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8748, key8827) size=" << scan_result.size() << std::endl;
  }

  // op 52723: get
  status = db->Get(read_options, "key10311", &value);
  if (status.ok()) {
    out << "GET key10311 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52724: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 52725: get
  status = db->Get(read_options, "key8335", &value);
  if (status.ok()) {
    out << "GET key8335 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52726: put
  db->Put(write_options, "key5271", "value82585");

  // op 52727: put
  db->Put(write_options, "key7339", "value48233");

  // op 52728: delete_range
  status = db->DeleteRange(write_options, "key2777", "key7381");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 52729: put
  db->Put(write_options, "key2849", "value65966");

  // op 52730: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1838", "key7449", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1838, key7449) size=" << scan_result.size() << std::endl;
  }

  // op 52731: delete_range
  status = db->DeleteRange(write_options, "key4020", "key6131");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 52732: put
  db->Put(write_options, "key3729", "value9975");

  // op 52733: put
  db->Put(write_options, "key11355", "value99549");

  // op 52734: put
  db->Put(write_options, "key6477", "value78562");

  // op 52735: put
  db->Put(write_options, "key1182", "value53167");

  // op 52736: put
  db->Put(write_options, "key8444", "value41087");

  // op 52737: delete_range
  status = db->DeleteRange(write_options, "key0034", "key11666");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 52738: put
  db->Put(write_options, "key5898", "value36007");

  // op 52739: get
  status = db->Get(read_options, "key0140", &value);
  if (status.ok()) {
    out << "GET key0140 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52740: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9317", "key11195", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9317, key11195) size=" << scan_result.size() << std::endl;
  }

  // op 52741: get
  status = db->Get(read_options, "key3751", &value);
  if (status.ok()) {
    out << "GET key3751 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52742: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3939", "key11135", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3939, key11135) size=" << scan_result.size() << std::endl;
  }

  // op 52743: get
  status = db->Get(read_options, "key7024", &value);
  if (status.ok()) {
    out << "GET key7024 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52744: put
  db->Put(write_options, "key4139", "value9981");

  // op 52745: delete_range
  status = db->DeleteRange(write_options, "key4477", "key8229");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 52746: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1709", "key2523", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1709, key2523) size=" << scan_result.size() << std::endl;
  }

  // op 52747: put
  db->Put(write_options, "key9743", "value11896");

  // op 52748: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1084", "key5659", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1084, key5659) size=" << scan_result.size() << std::endl;
  }

  // op 52749: get
  status = db->Get(read_options, "key3014", &value);
  if (status.ok()) {
    out << "GET key3014 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52750: get
  status = db->Get(read_options, "key4064", &value);
  if (status.ok()) {
    out << "GET key4064 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52751: put
  db->Put(write_options, "key0069", "value19437");

  // op 52752: delete
  db->Delete(write_options, "key3275");

  // op 52753: put
  db->Put(write_options, "key8030", "value51601");

  // op 52754: get
  status = db->Get(read_options, "key5126", &value);
  if (status.ok()) {
    out << "GET key5126 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52755: put
  db->Put(write_options, "key11336", "value22381");

  // op 52756: delete
  db->Delete(write_options, "key3483");

  // op 52757: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 52758: put
  db->Put(write_options, "key3712", "value82660");

  // op 52759: put
  db->Put(write_options, "key8491", "value114754");

  // op 52760: put
  db->Put(write_options, "key5874", "value102263");

  // op 52761: put
  db->Put(write_options, "key8496", "value1549");

  // op 52762: get
  status = db->Get(read_options, "key0673", &value);
  if (status.ok()) {
    out << "GET key0673 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52763: get
  status = db->Get(read_options, "key5933", &value);
  if (status.ok()) {
    out << "GET key5933 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52764: put
  db->Put(write_options, "key8308", "value74072");

  // op 52765: get
  status = db->Get(read_options, "key8051", &value);
  if (status.ok()) {
    out << "GET key8051 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52766: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1645", "key9010", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1645, key9010) size=" << scan_result.size() << std::endl;
  }

  // op 52767: put
  db->Put(write_options, "key7189", "value3386");

  // op 52768: get
  status = db->Get(read_options, "key6827", &value);
  if (status.ok()) {
    out << "GET key6827 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52769: delete_range
  status = db->DeleteRange(write_options, "key2257", "key10929");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 52770: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 52771: get
  status = db->Get(read_options, "key2740", &value);
  if (status.ok()) {
    out << "GET key2740 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52772: get
  status = db->Get(read_options, "key6212", &value);
  if (status.ok()) {
    out << "GET key6212 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52773: put
  db->Put(write_options, "key8581", "value24598");

  // op 52774: delete_range
  status = db->DeleteRange(write_options, "key4418", "key5419");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 52775: get
  status = db->Get(read_options, "key2585", &value);
  if (status.ok()) {
    out << "GET key2585 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52776: put
  db->Put(write_options, "key4482", "value41500");

  // op 52777: get
  status = db->Get(read_options, "key11374", &value);
  if (status.ok()) {
    out << "GET key11374 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52778: get
  status = db->Get(read_options, "key8032", &value);
  if (status.ok()) {
    out << "GET key8032 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52779: put
  db->Put(write_options, "key4832", "value81151");

  // op 52780: put
  db->Put(write_options, "key9117", "value32398");

  // op 52781: get
  status = db->Get(read_options, "key4693", &value);
  if (status.ok()) {
    out << "GET key4693 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52782: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1880", "key7472", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1880, key7472) size=" << scan_result.size() << std::endl;
  }

  // op 52783: delete_range
  status = db->DeleteRange(write_options, "key2864", "key7323");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 52784: get
  status = db->Get(read_options, "key7372", &value);
  if (status.ok()) {
    out << "GET key7372 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52785: put
  db->Put(write_options, "key11383", "value70857");

  // op 52786: get
  status = db->Get(read_options, "key9217", &value);
  if (status.ok()) {
    out << "GET key9217 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52787: get
  status = db->Get(read_options, "key4086", &value);
  if (status.ok()) {
    out << "GET key4086 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52788: put
  db->Put(write_options, "key7999", "value72708");

  // op 52789: get
  status = db->Get(read_options, "key5966", &value);
  if (status.ok()) {
    out << "GET key5966 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52790: get
  status = db->Get(read_options, "key10326", &value);
  if (status.ok()) {
    out << "GET key10326 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52791: delete_range
  status = db->DeleteRange(write_options, "key6432", "key8771");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 52792: delete
  db->Delete(write_options, "key3109");

  // op 52793: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0509", "key9976", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0509, key9976) size=" << scan_result.size() << std::endl;
  }

  // op 52794: get
  status = db->Get(read_options, "key4177", &value);
  if (status.ok()) {
    out << "GET key4177 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52795: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9187", "key9218", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9187, key9218) size=" << scan_result.size() << std::endl;
  }

  // op 52796: get
  status = db->Get(read_options, "key1701", &value);
  if (status.ok()) {
    out << "GET key1701 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52797: put
  db->Put(write_options, "key11673", "value116781");

  // op 52798: delete_range
  status = db->DeleteRange(write_options, "key5261", "key8023");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 52799: put
  db->Put(write_options, "key8160", "value45416");

  // op 52800: put
  db->Put(write_options, "key7136", "value24783");

  // op 52801: put
  db->Put(write_options, "key2681", "value105479");

  // op 52802: get
  status = db->Get(read_options, "key10288", &value);
  if (status.ok()) {
    out << "GET key10288 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52803: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0433", "key9415", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0433, key9415) size=" << scan_result.size() << std::endl;
  }

  // op 52804: put
  db->Put(write_options, "key10865", "value83187");

  // op 52805: put
  db->Put(write_options, "key4172", "value12402");

  // op 52806: put
  db->Put(write_options, "key2906", "value102942");

  // op 52807: get
  status = db->Get(read_options, "key4048", &value);
  if (status.ok()) {
    out << "GET key4048 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52808: put
  db->Put(write_options, "key0940", "value48426");

  // op 52809: delete
  db->Delete(write_options, "key6022");

  // op 52810: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4049", "key6752", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4049, key6752) size=" << scan_result.size() << std::endl;
  }

  // op 52811: delete_range
  status = db->DeleteRange(write_options, "key7504", "key7612");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 52812: delete_range
  status = db->DeleteRange(write_options, "key0756", "key4689");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 52813: delete_range
  status = db->DeleteRange(write_options, "key0130", "key2375");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 52814: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0145", "key8645", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0145, key8645) size=" << scan_result.size() << std::endl;
  }

  // op 52815: get
  status = db->Get(read_options, "key2879", &value);
  if (status.ok()) {
    out << "GET key2879 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52816: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 52817: put
  db->Put(write_options, "key0288", "value42815");

  // op 52818: put
  db->Put(write_options, "key7583", "value78126");

  // op 52819: put
  db->Put(write_options, "key2418", "value96912");

  // op 52820: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3448", "key6527", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3448, key6527) size=" << scan_result.size() << std::endl;
  }

  // op 52821: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5577", "key9988", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5577, key9988) size=" << scan_result.size() << std::endl;
  }

  // op 52822: delete
  db->Delete(write_options, "key6143");

  // op 52823: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0276", "key3353", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0276, key3353) size=" << scan_result.size() << std::endl;
  }

  // op 52824: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5618", "key11492", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5618, key11492) size=" << scan_result.size() << std::endl;
  }

  // op 52825: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 52826: put
  db->Put(write_options, "key7283", "value32225");

  // op 52827: put
  db->Put(write_options, "key8576", "value25125");

  // op 52828: put
  db->Put(write_options, "key8983", "value93290");

  // op 52829: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6935", "key10432", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6935, key10432) size=" << scan_result.size() << std::endl;
  }

  // op 52830: delete
  db->Delete(write_options, "key10436");

  // op 52831: get
  status = db->Get(read_options, "key1996", &value);
  if (status.ok()) {
    out << "GET key1996 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52832: get
  status = db->Get(read_options, "key2791", &value);
  if (status.ok()) {
    out << "GET key2791 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52833: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2599", "key3855", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2599, key3855) size=" << scan_result.size() << std::endl;
  }

  // op 52834: put
  db->Put(write_options, "key5221", "value78381");

  // op 52835: delete
  db->Delete(write_options, "key1430");

  // op 52836: delete
  db->Delete(write_options, "key0206");

  // op 52837: get
  status = db->Get(read_options, "key2716", &value);
  if (status.ok()) {
    out << "GET key2716 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52838: put
  db->Put(write_options, "key10453", "value0697");

  // op 52839: get
  status = db->Get(read_options, "key6113", &value);
  if (status.ok()) {
    out << "GET key6113 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52840: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 52841: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3455", "key8977", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3455, key8977) size=" << scan_result.size() << std::endl;
  }

  // op 52842: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6568", "key9775", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6568, key9775) size=" << scan_result.size() << std::endl;
  }

  // op 52843: get
  status = db->Get(read_options, "key3667", &value);
  if (status.ok()) {
    out << "GET key3667 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52844: get
  status = db->Get(read_options, "key9732", &value);
  if (status.ok()) {
    out << "GET key9732 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52845: put
  db->Put(write_options, "key5000", "value82245");

  // op 52846: put
  db->Put(write_options, "key4838", "value110416");

  // op 52847: put
  db->Put(write_options, "key6996", "value6622");

  // op 52848: delete_range
  status = db->DeleteRange(write_options, "key1162", "key8029");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 52849: put
  db->Put(write_options, "key0130", "value19114");

  // op 52850: get
  status = db->Get(read_options, "key2919", &value);
  if (status.ok()) {
    out << "GET key2919 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52851: put
  db->Put(write_options, "key8813", "value89019");

  // op 52852: put
  db->Put(write_options, "key9920", "value108438");

  // op 52853: put
  db->Put(write_options, "key10152", "value92995");

  // op 52854: put
  db->Put(write_options, "key5511", "value27349");

  // op 52855: put
  db->Put(write_options, "key7479", "value4866");

  // op 52856: delete_range
  status = db->DeleteRange(write_options, "key7568", "key9460");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 52857: get
  status = db->Get(read_options, "key10010", &value);
  if (status.ok()) {
    out << "GET key10010 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52858: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10073", "key11441", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10073, key11441) size=" << scan_result.size() << std::endl;
  }

  // op 52859: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1324", "key11696", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1324, key11696) size=" << scan_result.size() << std::endl;
  }

  // op 52860: put
  db->Put(write_options, "key4532", "value110975");

  // op 52861: put
  db->Put(write_options, "key7927", "value67883");

  // op 52862: put
  db->Put(write_options, "key6197", "value50749");

  // op 52863: put
  db->Put(write_options, "key2948", "value37692");

  // op 52864: put
  db->Put(write_options, "key10428", "value28420");

  // op 52865: put
  db->Put(write_options, "key5185", "value60328");

  // op 52866: get
  status = db->Get(read_options, "key10306", &value);
  if (status.ok()) {
    out << "GET key10306 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52867: get
  status = db->Get(read_options, "key3153", &value);
  if (status.ok()) {
    out << "GET key3153 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52868: put
  db->Put(write_options, "key4691", "value43316");

  // op 52869: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 52870: get
  status = db->Get(read_options, "key0557", &value);
  if (status.ok()) {
    out << "GET key0557 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52871: get
  status = db->Get(read_options, "key2187", &value);
  if (status.ok()) {
    out << "GET key2187 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52872: delete_range
  status = db->DeleteRange(write_options, "key3412", "key7328");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 52873: put
  db->Put(write_options, "key2143", "value4127");

  // op 52874: put
  db->Put(write_options, "key9590", "value58558");

  // op 52875: delete_range
  status = db->DeleteRange(write_options, "key7732", "key11122");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 52876: delete_range
  status = db->DeleteRange(write_options, "key3644", "key11113");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 52877: put
  db->Put(write_options, "key3228", "value7043");

  // op 52878: put
  db->Put(write_options, "key6036", "value103360");

  // op 52879: put
  db->Put(write_options, "key7831", "value72107");

  // op 52880: get
  status = db->Get(read_options, "key4457", &value);
  if (status.ok()) {
    out << "GET key4457 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52881: put
  db->Put(write_options, "key2254", "value1062");

  // op 52882: put
  db->Put(write_options, "key0141", "value88651");

  // op 52883: get
  status = db->Get(read_options, "key1505", &value);
  if (status.ok()) {
    out << "GET key1505 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52884: get
  status = db->Get(read_options, "key11057", &value);
  if (status.ok()) {
    out << "GET key11057 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52885: delete
  db->Delete(write_options, "key2799");

  // op 52886: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0336", "key9891", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0336, key9891) size=" << scan_result.size() << std::endl;
  }

  // op 52887: put
  db->Put(write_options, "key0716", "value31739");

  // op 52888: put
  db->Put(write_options, "key9829", "value67856");

  // op 52889: put
  db->Put(write_options, "key2674", "value45755");

  // op 52890: get
  status = db->Get(read_options, "key5491", &value);
  if (status.ok()) {
    out << "GET key5491 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52891: delete
  db->Delete(write_options, "key11492");

  // op 52892: get
  status = db->Get(read_options, "key3061", &value);
  if (status.ok()) {
    out << "GET key3061 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52893: get
  status = db->Get(read_options, "key5450", &value);
  if (status.ok()) {
    out << "GET key5450 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52894: put
  db->Put(write_options, "key10911", "value13114");

  // op 52895: get
  status = db->Get(read_options, "key9568", &value);
  if (status.ok()) {
    out << "GET key9568 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52896: delete
  db->Delete(write_options, "key5432");

  // op 52897: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2488", "key10168", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2488, key10168) size=" << scan_result.size() << std::endl;
  }

  // op 52898: put
  db->Put(write_options, "key1539", "value68828");

  // op 52899: get
  status = db->Get(read_options, "key4245", &value);
  if (status.ok()) {
    out << "GET key4245 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52900: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8246", "key11084", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8246, key11084) size=" << scan_result.size() << std::endl;
  }

  // op 52901: get
  status = db->Get(read_options, "key6204", &value);
  if (status.ok()) {
    out << "GET key6204 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52902: put
  db->Put(write_options, "key10306", "value29573");

  // op 52903: delete
  db->Delete(write_options, "key4619");

  // op 52904: put
  db->Put(write_options, "key3351", "value32133");

  // op 52905: get
  status = db->Get(read_options, "key3700", &value);
  if (status.ok()) {
    out << "GET key3700 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52906: put
  db->Put(write_options, "key1923", "value101105");

  // op 52907: put
  db->Put(write_options, "key4423", "value68574");

  // op 52908: put
  db->Put(write_options, "key4432", "value81504");

  // op 52909: put
  db->Put(write_options, "key9092", "value85277");

  // op 52910: put
  db->Put(write_options, "key7194", "value62459");

  // op 52911: delete
  db->Delete(write_options, "key2127");

  // op 52912: put
  db->Put(write_options, "key0687", "value104177");

  // op 52913: delete_range
  status = db->DeleteRange(write_options, "key1491", "key9099");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 52914: get
  status = db->Get(read_options, "key6272", &value);
  if (status.ok()) {
    out << "GET key6272 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52915: get
  status = db->Get(read_options, "key4019", &value);
  if (status.ok()) {
    out << "GET key4019 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52916: delete
  db->Delete(write_options, "key9533");

  // op 52917: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0017", "key2278", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0017, key2278) size=" << scan_result.size() << std::endl;
  }

  // op 52918: delete
  db->Delete(write_options, "key0457");

  // op 52919: delete_range
  status = db->DeleteRange(write_options, "key3213", "key8235");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 52920: delete
  db->Delete(write_options, "key4084");

  // op 52921: put
  db->Put(write_options, "key0895", "value111454");

  // op 52922: get
  status = db->Get(read_options, "key8862", &value);
  if (status.ok()) {
    out << "GET key8862 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52923: put
  db->Put(write_options, "key7192", "value69968");

  // op 52924: delete_range
  status = db->DeleteRange(write_options, "key7026", "key10658");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 52925: put
  db->Put(write_options, "key1541", "value60356");

  // op 52926: put
  db->Put(write_options, "key4070", "value51491");

  // op 52927: put
  db->Put(write_options, "key10054", "value41034");

  // op 52928: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 52929: put
  db->Put(write_options, "key7329", "value40205");

  // op 52930: get
  status = db->Get(read_options, "key9847", &value);
  if (status.ok()) {
    out << "GET key9847 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52931: put
  db->Put(write_options, "key2273", "value89221");

  // op 52932: put
  db->Put(write_options, "key7287", "value38137");

  // op 52933: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1910", "key5033", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1910, key5033) size=" << scan_result.size() << std::endl;
  }

  // op 52934: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4111", "key4575", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4111, key4575) size=" << scan_result.size() << std::endl;
  }

  // op 52935: delete
  db->Delete(write_options, "key3236");

  // op 52936: put
  db->Put(write_options, "key0505", "value91680");

  // op 52937: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 52938: delete
  db->Delete(write_options, "key1620");

  // op 52939: get
  status = db->Get(read_options, "key4241", &value);
  if (status.ok()) {
    out << "GET key4241 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52940: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3649", "key4800", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3649, key4800) size=" << scan_result.size() << std::endl;
  }

  // op 52941: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 52942: put
  db->Put(write_options, "key2992", "value59225");

  // op 52943: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2423", "key10724", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2423, key10724) size=" << scan_result.size() << std::endl;
  }

  // op 52944: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2850", "key9844", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2850, key9844) size=" << scan_result.size() << std::endl;
  }

  // op 52945: get
  status = db->Get(read_options, "key4847", &value);
  if (status.ok()) {
    out << "GET key4847 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52946: put
  db->Put(write_options, "key4607", "value105612");

  // op 52947: put
  db->Put(write_options, "key4363", "value16537");

  // op 52948: put
  db->Put(write_options, "key4046", "value28935");

  // op 52949: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2360", "key8501", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2360, key8501) size=" << scan_result.size() << std::endl;
  }

  // op 52950: delete
  db->Delete(write_options, "key1813");

  // op 52951: delete_range
  status = db->DeleteRange(write_options, "key3293", "key11451");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 52952: delete_range
  status = db->DeleteRange(write_options, "key7704", "key9616");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 52953: put
  db->Put(write_options, "key2718", "value118247");

  // op 52954: put
  db->Put(write_options, "key8401", "value100798");

  // op 52955: put
  db->Put(write_options, "key7635", "value94034");

  // op 52956: get
  status = db->Get(read_options, "key10367", &value);
  if (status.ok()) {
    out << "GET key10367 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52957: put
  db->Put(write_options, "key11291", "value40821");

  // op 52958: get
  status = db->Get(read_options, "key3080", &value);
  if (status.ok()) {
    out << "GET key3080 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52959: delete
  db->Delete(write_options, "key2048");

  // op 52960: delete_range
  status = db->DeleteRange(write_options, "key2015", "key8670");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 52961: delete_range
  status = db->DeleteRange(write_options, "key0453", "key11579");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 52962: get
  status = db->Get(read_options, "key11428", &value);
  if (status.ok()) {
    out << "GET key11428 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52963: put
  db->Put(write_options, "key10103", "value89413");

  // op 52964: delete
  db->Delete(write_options, "key3281");

  // op 52965: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9400", "key10764", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9400, key10764) size=" << scan_result.size() << std::endl;
  }

  // op 52966: put
  db->Put(write_options, "key1281", "value76211");

  // op 52967: put
  db->Put(write_options, "key5578", "value2479");

  // op 52968: put
  db->Put(write_options, "key0489", "value31202");

  // op 52969: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9143", "key10250", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9143, key10250) size=" << scan_result.size() << std::endl;
  }

  // op 52970: delete_range
  status = db->DeleteRange(write_options, "key0810", "key3931");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 52971: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 52972: put
  db->Put(write_options, "key9388", "value28635");

  // op 52973: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2140", "key8520", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2140, key8520) size=" << scan_result.size() << std::endl;
  }

  // op 52974: delete_range
  status = db->DeleteRange(write_options, "key2253", "key2843");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 52975: get
  status = db->Get(read_options, "key3293", &value);
  if (status.ok()) {
    out << "GET key3293 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52976: delete
  db->Delete(write_options, "key5374");

  // op 52977: get
  status = db->Get(read_options, "key3709", &value);
  if (status.ok()) {
    out << "GET key3709 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52978: get
  status = db->Get(read_options, "key5498", &value);
  if (status.ok()) {
    out << "GET key5498 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52979: delete
  db->Delete(write_options, "key8112");

  // op 52980: get
  status = db->Get(read_options, "key9110", &value);
  if (status.ok()) {
    out << "GET key9110 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52981: delete
  db->Delete(write_options, "key10700");

  // op 52982: put
  db->Put(write_options, "key5070", "value81358");

  // op 52983: put
  db->Put(write_options, "key3160", "value72877");

  // op 52984: delete
  db->Delete(write_options, "key10486");

  // op 52985: get
  status = db->Get(read_options, "key7462", &value);
  if (status.ok()) {
    out << "GET key7462 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52986: put
  db->Put(write_options, "key0397", "value102667");

  // op 52987: get
  status = db->Get(read_options, "key10534", &value);
  if (status.ok()) {
    out << "GET key10534 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52988: put
  db->Put(write_options, "key9223", "value70475");

  // op 52989: get
  status = db->Get(read_options, "key7233", &value);
  if (status.ok()) {
    out << "GET key7233 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52990: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3469", "key8407", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3469, key8407) size=" << scan_result.size() << std::endl;
  }

  // op 52991: put
  db->Put(write_options, "key10597", "value91082");

  // op 52992: put
  db->Put(write_options, "key6682", "value114871");

  // op 52993: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3077", "key8721", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3077, key8721) size=" << scan_result.size() << std::endl;
  }

  // op 52994: delete
  db->Delete(write_options, "key11620");

  // op 52995: put
  db->Put(write_options, "key8173", "value12891");

  // op 52996: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 52997: put
  db->Put(write_options, "key11513", "value20353");

  // op 52998: get
  status = db->Get(read_options, "key3899", &value);
  if (status.ok()) {
    out << "GET key3899 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 52999: get
  status = db->Get(read_options, "key5259", &value);
  if (status.ok()) {
    out << "GET key5259 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53000: put
  db->Put(write_options, "key11718", "value109661");

  // op 53001: put
  db->Put(write_options, "key8212", "value16749");

  // op 53002: delete
  db->Delete(write_options, "key3453");

  // op 53003: put
  db->Put(write_options, "key9007", "value41032");

  // op 53004: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3714", "key9494", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3714, key9494) size=" << scan_result.size() << std::endl;
  }

  // op 53005: put
  db->Put(write_options, "key6581", "value97419");

  // op 53006: delete
  db->Delete(write_options, "key2594");

  // op 53007: put
  db->Put(write_options, "key9940", "value101543");

  // op 53008: put
  db->Put(write_options, "key10029", "value60008");

  // op 53009: put
  db->Put(write_options, "key9699", "value63338");

  // op 53010: delete_range
  status = db->DeleteRange(write_options, "key9903", "key10837");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 53011: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8033", "key8157", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8033, key8157) size=" << scan_result.size() << std::endl;
  }

  // op 53012: delete
  db->Delete(write_options, "key10550");

  // op 53013: put
  db->Put(write_options, "key9346", "value95121");

  // op 53014: put
  db->Put(write_options, "key6813", "value80460");

  // op 53015: get
  status = db->Get(read_options, "key7288", &value);
  if (status.ok()) {
    out << "GET key7288 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53016: get
  status = db->Get(read_options, "key10432", &value);
  if (status.ok()) {
    out << "GET key10432 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53017: get
  status = db->Get(read_options, "key1827", &value);
  if (status.ok()) {
    out << "GET key1827 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53018: delete_range
  status = db->DeleteRange(write_options, "key3543", "key11142");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 53019: delete_range
  status = db->DeleteRange(write_options, "key8984", "key11221");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 53020: put
  db->Put(write_options, "key8679", "value48863");

  // op 53021: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 53022: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 53023: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1510", "key2501", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1510, key2501) size=" << scan_result.size() << std::endl;
  }

  // op 53024: put
  db->Put(write_options, "key1546", "value23171");

  // op 53025: put
  db->Put(write_options, "key0969", "value76961");

  // op 53026: put
  db->Put(write_options, "key6636", "value113320");

  // op 53027: put
  db->Put(write_options, "key10335", "value6604");

  // op 53028: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3948", "key6862", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3948, key6862) size=" << scan_result.size() << std::endl;
  }

  // op 53029: put
  db->Put(write_options, "key7029", "value44996");

  // op 53030: put
  db->Put(write_options, "key3152", "value8787");

  // op 53031: delete
  db->Delete(write_options, "key10205");

  // op 53032: put
  db->Put(write_options, "key10355", "value3219");

  // op 53033: put
  db->Put(write_options, "key1312", "value68298");

  // op 53034: put
  db->Put(write_options, "key10104", "value81056");

  // op 53035: put
  db->Put(write_options, "key5427", "value37936");

  // op 53036: delete
  db->Delete(write_options, "key6465");

  // op 53037: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4536", "key4875", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4536, key4875) size=" << scan_result.size() << std::endl;
  }

  // op 53038: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9341", "key10768", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9341, key10768) size=" << scan_result.size() << std::endl;
  }

  // op 53039: get
  status = db->Get(read_options, "key0577", &value);
  if (status.ok()) {
    out << "GET key0577 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53040: put
  db->Put(write_options, "key2406", "value45753");

  // op 53041: put
  db->Put(write_options, "key7050", "value55417");

  // op 53042: get
  status = db->Get(read_options, "key9332", &value);
  if (status.ok()) {
    out << "GET key9332 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53043: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2783", "key7784", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2783, key7784) size=" << scan_result.size() << std::endl;
  }

  // op 53044: put
  db->Put(write_options, "key8131", "value51281");

  // op 53045: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5453", "key6979", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5453, key6979) size=" << scan_result.size() << std::endl;
  }

  // op 53046: put
  db->Put(write_options, "key0060", "value104845");

  // op 53047: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4803", "key5454", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4803, key5454) size=" << scan_result.size() << std::endl;
  }

  // op 53048: put
  db->Put(write_options, "key6748", "value67992");

  // op 53049: put
  db->Put(write_options, "key1634", "value19324");

  // op 53050: get
  status = db->Get(read_options, "key3687", &value);
  if (status.ok()) {
    out << "GET key3687 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53051: delete
  db->Delete(write_options, "key11171");

  // op 53052: delete_range
  status = db->DeleteRange(write_options, "key1335", "key8805");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 53053: get
  status = db->Get(read_options, "key0077", &value);
  if (status.ok()) {
    out << "GET key0077 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53054: get
  status = db->Get(read_options, "key11096", &value);
  if (status.ok()) {
    out << "GET key11096 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53055: delete_range
  status = db->DeleteRange(write_options, "key4608", "key6082");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 53056: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5773", "key7327", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5773, key7327) size=" << scan_result.size() << std::endl;
  }

  // op 53057: put
  db->Put(write_options, "key6697", "value63489");

  // op 53058: put
  db->Put(write_options, "key1694", "value12041");

  // op 53059: put
  db->Put(write_options, "key11645", "value68786");

  // op 53060: put
  db->Put(write_options, "key7025", "value22403");

  // op 53061: put
  db->Put(write_options, "key5998", "value91037");

  // op 53062: delete
  db->Delete(write_options, "key3691");

  // op 53063: delete_range
  status = db->DeleteRange(write_options, "key6843", "key9703");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 53064: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3109", "key9232", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3109, key9232) size=" << scan_result.size() << std::endl;
  }

  // op 53065: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 53066: get
  status = db->Get(read_options, "key11061", &value);
  if (status.ok()) {
    out << "GET key11061 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53067: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2189", "key2788", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2189, key2788) size=" << scan_result.size() << std::endl;
  }

  // op 53068: put
  db->Put(write_options, "key8061", "value16191");

  // op 53069: put
  db->Put(write_options, "key2295", "value10166");

  // op 53070: delete_range
  status = db->DeleteRange(write_options, "key1196", "key8071");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 53071: delete_range
  status = db->DeleteRange(write_options, "key5552", "key10059");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 53072: delete
  db->Delete(write_options, "key5283");

  // op 53073: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0795", "key11066", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0795, key11066) size=" << scan_result.size() << std::endl;
  }

  // op 53074: put
  db->Put(write_options, "key8380", "value56459");

  // op 53075: put
  db->Put(write_options, "key10530", "value102494");

  // op 53076: delete
  db->Delete(write_options, "key4330");

  // op 53077: delete_range
  status = db->DeleteRange(write_options, "key10020", "key10819");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 53078: put
  db->Put(write_options, "key2571", "value114760");

  // op 53079: put
  db->Put(write_options, "key7572", "value107767");

  // op 53080: delete
  db->Delete(write_options, "key6639");

  // op 53081: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5606", "key6230", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5606, key6230) size=" << scan_result.size() << std::endl;
  }

  // op 53082: put
  db->Put(write_options, "key9694", "value22360");

  // op 53083: put
  db->Put(write_options, "key0414", "value18362");

  // op 53084: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1963", "key7708", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1963, key7708) size=" << scan_result.size() << std::endl;
  }

  // op 53085: delete_range
  status = db->DeleteRange(write_options, "key3234", "key8127");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 53086: put
  db->Put(write_options, "key8550", "value45538");

  // op 53087: put
  db->Put(write_options, "key5590", "value9902");

  // op 53088: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1274", "key6266", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1274, key6266) size=" << scan_result.size() << std::endl;
  }

  // op 53089: get
  status = db->Get(read_options, "key4538", &value);
  if (status.ok()) {
    out << "GET key4538 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53090: get
  status = db->Get(read_options, "key5089", &value);
  if (status.ok()) {
    out << "GET key5089 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53091: delete_range
  status = db->DeleteRange(write_options, "key1403", "key3758");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 53092: put
  db->Put(write_options, "key2826", "value70011");

  // op 53093: put
  db->Put(write_options, "key4054", "value39585");

  // op 53094: put
  db->Put(write_options, "key0411", "value95803");

  // op 53095: put
  db->Put(write_options, "key6872", "value5198");

  // op 53096: delete_range
  status = db->DeleteRange(write_options, "key2183", "key11124");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 53097: put
  db->Put(write_options, "key2216", "value108189");

  // op 53098: put
  db->Put(write_options, "key2584", "value103791");

  // op 53099: put
  db->Put(write_options, "key11721", "value33166");

  // op 53100: put
  db->Put(write_options, "key6793", "value60566");

  // op 53101: put
  db->Put(write_options, "key6709", "value96622");

  // op 53102: get
  status = db->Get(read_options, "key5359", &value);
  if (status.ok()) {
    out << "GET key5359 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53103: put
  db->Put(write_options, "key1530", "value61809");

  // op 53104: get
  status = db->Get(read_options, "key5100", &value);
  if (status.ok()) {
    out << "GET key5100 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53105: put
  db->Put(write_options, "key2867", "value82736");

  // op 53106: put
  db->Put(write_options, "key11631", "value113503");

  // op 53107: put
  db->Put(write_options, "key8480", "value86705");

  // op 53108: put
  db->Put(write_options, "key10326", "value104469");

  // op 53109: put
  db->Put(write_options, "key6971", "value4865");

  // op 53110: put
  db->Put(write_options, "key1241", "value0533");

  // op 53111: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0169", "key1776", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0169, key1776) size=" << scan_result.size() << std::endl;
  }

  // op 53112: put
  db->Put(write_options, "key8767", "value76809");

  // op 53113: put
  db->Put(write_options, "key9445", "value12058");

  // op 53114: put
  db->Put(write_options, "key9282", "value115888");

  // op 53115: put
  db->Put(write_options, "key10319", "value10233");

  // op 53116: get
  status = db->Get(read_options, "key5172", &value);
  if (status.ok()) {
    out << "GET key5172 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53117: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5156", "key5873", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5156, key5873) size=" << scan_result.size() << std::endl;
  }

  // op 53118: put
  db->Put(write_options, "key9492", "value82444");

  // op 53119: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2524", "key5775", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2524, key5775) size=" << scan_result.size() << std::endl;
  }

  // op 53120: get
  status = db->Get(read_options, "key6428", &value);
  if (status.ok()) {
    out << "GET key6428 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53121: put
  db->Put(write_options, "key8814", "value73037");

  // op 53122: get
  status = db->Get(read_options, "key8193", &value);
  if (status.ok()) {
    out << "GET key8193 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53123: get
  status = db->Get(read_options, "key6352", &value);
  if (status.ok()) {
    out << "GET key6352 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53124: get
  status = db->Get(read_options, "key11722", &value);
  if (status.ok()) {
    out << "GET key11722 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53125: get
  status = db->Get(read_options, "key8532", &value);
  if (status.ok()) {
    out << "GET key8532 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53126: put
  db->Put(write_options, "key3504", "value87455");

  // op 53127: get
  status = db->Get(read_options, "key0487", &value);
  if (status.ok()) {
    out << "GET key0487 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53128: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 53129: get
  status = db->Get(read_options, "key10411", &value);
  if (status.ok()) {
    out << "GET key10411 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53130: put
  db->Put(write_options, "key8270", "value29800");

  // op 53131: put
  db->Put(write_options, "key3865", "value44790");

  // op 53132: delete_range
  status = db->DeleteRange(write_options, "key1370", "key7069");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 53133: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2435", "key8268", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2435, key8268) size=" << scan_result.size() << std::endl;
  }

  // op 53134: delete_range
  status = db->DeleteRange(write_options, "key3352", "key10704");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 53135: delete_range
  status = db->DeleteRange(write_options, "key0203", "key5888");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 53136: get
  status = db->Get(read_options, "key10768", &value);
  if (status.ok()) {
    out << "GET key10768 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53137: delete
  db->Delete(write_options, "key0659");

  // op 53138: put
  db->Put(write_options, "key11415", "value66732");

  // op 53139: delete
  db->Delete(write_options, "key8011");

  // op 53140: put
  db->Put(write_options, "key11320", "value116439");

  // op 53141: put
  db->Put(write_options, "key0882", "value21976");

  // op 53142: put
  db->Put(write_options, "key1617", "value45119");

  // op 53143: put
  db->Put(write_options, "key6630", "value70821");

  // op 53144: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10937", "key10984", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10937, key10984) size=" << scan_result.size() << std::endl;
  }

  // op 53145: put
  db->Put(write_options, "key0927", "value115383");

  // op 53146: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6386", "key6513", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6386, key6513) size=" << scan_result.size() << std::endl;
  }

  // op 53147: delete
  db->Delete(write_options, "key4819");

  // op 53148: put
  db->Put(write_options, "key4437", "value42643");

  // op 53149: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5775", "key7159", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5775, key7159) size=" << scan_result.size() << std::endl;
  }

  // op 53150: put
  db->Put(write_options, "key0876", "value9621");

  // op 53151: delete
  db->Delete(write_options, "key7623");

  // op 53152: get
  status = db->Get(read_options, "key6313", &value);
  if (status.ok()) {
    out << "GET key6313 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53153: get
  status = db->Get(read_options, "key3526", &value);
  if (status.ok()) {
    out << "GET key3526 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53154: delete_range
  status = db->DeleteRange(write_options, "key2054", "key8722");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 53155: put
  db->Put(write_options, "key10361", "value49085");

  // op 53156: get
  status = db->Get(read_options, "key11343", &value);
  if (status.ok()) {
    out << "GET key11343 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53157: put
  db->Put(write_options, "key7529", "value50457");

  // op 53158: put
  db->Put(write_options, "key9444", "value88793");

  // op 53159: delete_range
  status = db->DeleteRange(write_options, "key4654", "key6188");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 53160: put
  db->Put(write_options, "key2151", "value39650");

  // op 53161: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 53162: put
  db->Put(write_options, "key7154", "value20510");

  // op 53163: delete_range
  status = db->DeleteRange(write_options, "key0283", "key9255");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 53164: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6914", "key11638", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6914, key11638) size=" << scan_result.size() << std::endl;
  }

  // op 53165: put
  db->Put(write_options, "key7860", "value28670");

  // op 53166: put
  db->Put(write_options, "key0192", "value2815");

  // op 53167: get
  status = db->Get(read_options, "key6603", &value);
  if (status.ok()) {
    out << "GET key6603 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53168: get
  status = db->Get(read_options, "key7294", &value);
  if (status.ok()) {
    out << "GET key7294 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53169: put
  db->Put(write_options, "key6268", "value71945");

  // op 53170: delete_range
  status = db->DeleteRange(write_options, "key4472", "key4585");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 53171: delete_range
  status = db->DeleteRange(write_options, "key1895", "key2411");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 53172: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 53173: delete
  db->Delete(write_options, "key2448");

  // op 53174: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5250", "key7866", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5250, key7866) size=" << scan_result.size() << std::endl;
  }

  // op 53175: put
  db->Put(write_options, "key3257", "value44974");

  // op 53176: get
  status = db->Get(read_options, "key6238", &value);
  if (status.ok()) {
    out << "GET key6238 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53177: get
  status = db->Get(read_options, "key6581", &value);
  if (status.ok()) {
    out << "GET key6581 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53178: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2323", "key3870", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2323, key3870) size=" << scan_result.size() << std::endl;
  }

  // op 53179: put
  db->Put(write_options, "key3322", "value49501");

  // op 53180: delete
  db->Delete(write_options, "key4298");

  // op 53181: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 53182: delete
  db->Delete(write_options, "key10430");

  // op 53183: put
  db->Put(write_options, "key0207", "value50258");

  // op 53184: get
  status = db->Get(read_options, "key7018", &value);
  if (status.ok()) {
    out << "GET key7018 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53185: put
  db->Put(write_options, "key10617", "value0663");

  // op 53186: put
  db->Put(write_options, "key5976", "value75691");

  // op 53187: get
  status = db->Get(read_options, "key7431", &value);
  if (status.ok()) {
    out << "GET key7431 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53188: delete_range
  status = db->DeleteRange(write_options, "key2248", "key5653");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 53189: delete
  db->Delete(write_options, "key10427");

  // op 53190: put
  db->Put(write_options, "key4619", "value37006");

  // op 53191: put
  db->Put(write_options, "key2688", "value67287");

  // op 53192: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 53193: get
  status = db->Get(read_options, "key0596", &value);
  if (status.ok()) {
    out << "GET key0596 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53194: get
  status = db->Get(read_options, "key4434", &value);
  if (status.ok()) {
    out << "GET key4434 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53195: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0239", "key3714", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0239, key3714) size=" << scan_result.size() << std::endl;
  }

  // op 53196: delete
  db->Delete(write_options, "key6844");

  // op 53197: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 53198: get
  status = db->Get(read_options, "key7137", &value);
  if (status.ok()) {
    out << "GET key7137 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53199: get
  status = db->Get(read_options, "key0399", &value);
  if (status.ok()) {
    out << "GET key0399 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53200: get
  status = db->Get(read_options, "key11728", &value);
  if (status.ok()) {
    out << "GET key11728 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53201: put
  db->Put(write_options, "key1444", "value97341");

  // op 53202: get
  status = db->Get(read_options, "key2073", &value);
  if (status.ok()) {
    out << "GET key2073 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53203: delete
  db->Delete(write_options, "key7904");

  // op 53204: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 53205: get
  status = db->Get(read_options, "key10499", &value);
  if (status.ok()) {
    out << "GET key10499 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53206: put
  db->Put(write_options, "key11601", "value111101");

  // op 53207: put
  db->Put(write_options, "key10567", "value40919");

  // op 53208: get
  status = db->Get(read_options, "key7181", &value);
  if (status.ok()) {
    out << "GET key7181 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53209: put
  db->Put(write_options, "key11690", "value113769");

  // op 53210: get
  status = db->Get(read_options, "key4104", &value);
  if (status.ok()) {
    out << "GET key4104 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53211: delete
  db->Delete(write_options, "key9277");

  // op 53212: get
  status = db->Get(read_options, "key11624", &value);
  if (status.ok()) {
    out << "GET key11624 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53213: put
  db->Put(write_options, "key1992", "value100468");

  // op 53214: put
  db->Put(write_options, "key4792", "value18031");

  // op 53215: get
  status = db->Get(read_options, "key1703", &value);
  if (status.ok()) {
    out << "GET key1703 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53216: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3594", "key11022", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3594, key11022) size=" << scan_result.size() << std::endl;
  }

  // op 53217: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5863", "key6910", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5863, key6910) size=" << scan_result.size() << std::endl;
  }

  // op 53218: delete
  db->Delete(write_options, "key5710");

  // op 53219: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1155", "key6372", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1155, key6372) size=" << scan_result.size() << std::endl;
  }

  // op 53220: delete_range
  status = db->DeleteRange(write_options, "key2669", "key4875");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 53221: put
  db->Put(write_options, "key4936", "value30406");

  // op 53222: delete
  db->Delete(write_options, "key5751");

  // op 53223: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 53224: put
  db->Put(write_options, "key3509", "value9356");

  // op 53225: delete
  db->Delete(write_options, "key4004");

  // op 53226: put
  db->Put(write_options, "key2522", "value102257");

  // op 53227: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3841", "key4506", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3841, key4506) size=" << scan_result.size() << std::endl;
  }

  // op 53228: put
  db->Put(write_options, "key8404", "value1387");

  // op 53229: delete
  db->Delete(write_options, "key4809");

  // op 53230: get
  status = db->Get(read_options, "key3511", &value);
  if (status.ok()) {
    out << "GET key3511 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53231: delete
  db->Delete(write_options, "key1631");

  // op 53232: put
  db->Put(write_options, "key7651", "value68814");

  // op 53233: get
  status = db->Get(read_options, "key6379", &value);
  if (status.ok()) {
    out << "GET key6379 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53234: put
  db->Put(write_options, "key3769", "value28776");

  // op 53235: delete
  db->Delete(write_options, "key5551");

  // op 53236: delete
  db->Delete(write_options, "key5524");

  // op 53237: delete_range
  status = db->DeleteRange(write_options, "key0053", "key4601");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 53238: delete
  db->Delete(write_options, "key0849");

  // op 53239: delete_range
  status = db->DeleteRange(write_options, "key5208", "key7412");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 53240: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3313", "key10152", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3313, key10152) size=" << scan_result.size() << std::endl;
  }

  // op 53241: put
  db->Put(write_options, "key2769", "value110952");

  // op 53242: put
  db->Put(write_options, "key4244", "value10572");

  // op 53243: delete_range
  status = db->DeleteRange(write_options, "key0128", "key8523");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 53244: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3286", "key4712", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3286, key4712) size=" << scan_result.size() << std::endl;
  }

  // op 53245: put
  db->Put(write_options, "key6888", "value96474");

  // op 53246: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1877", "key11701", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1877, key11701) size=" << scan_result.size() << std::endl;
  }

  // op 53247: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 53248: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4956", "key6805", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4956, key6805) size=" << scan_result.size() << std::endl;
  }

  // op 53249: put
  db->Put(write_options, "key11411", "value52743");

  // op 53250: put
  db->Put(write_options, "key9885", "value11611");

  // op 53251: get
  status = db->Get(read_options, "key3585", &value);
  if (status.ok()) {
    out << "GET key3585 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53252: put
  db->Put(write_options, "key5669", "value30156");

  // op 53253: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 53254: get
  status = db->Get(read_options, "key7266", &value);
  if (status.ok()) {
    out << "GET key7266 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53255: put
  db->Put(write_options, "key4520", "value37663");

  // op 53256: delete
  db->Delete(write_options, "key0349");

  // op 53257: get
  status = db->Get(read_options, "key2745", &value);
  if (status.ok()) {
    out << "GET key2745 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53258: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4761", "key6869", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4761, key6869) size=" << scan_result.size() << std::endl;
  }

  // op 53259: put
  db->Put(write_options, "key7203", "value107205");

  // op 53260: get
  status = db->Get(read_options, "key2838", &value);
  if (status.ok()) {
    out << "GET key2838 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53261: delete
  db->Delete(write_options, "key11067");

  // op 53262: put
  db->Put(write_options, "key5533", "value25090");

  // op 53263: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2379", "key9656", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2379, key9656) size=" << scan_result.size() << std::endl;
  }

  // op 53264: get
  status = db->Get(read_options, "key8251", &value);
  if (status.ok()) {
    out << "GET key8251 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53265: put
  db->Put(write_options, "key1230", "value14256");

  // op 53266: get
  status = db->Get(read_options, "key4239", &value);
  if (status.ok()) {
    out << "GET key4239 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53267: delete_range
  status = db->DeleteRange(write_options, "key3935", "key8495");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 53268: put
  db->Put(write_options, "key3504", "value34250");

  // op 53269: put
  db->Put(write_options, "key1390", "value46586");

  // op 53270: get
  status = db->Get(read_options, "key2158", &value);
  if (status.ok()) {
    out << "GET key2158 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53271: get
  status = db->Get(read_options, "key1136", &value);
  if (status.ok()) {
    out << "GET key1136 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53272: put
  db->Put(write_options, "key1137", "value21010");

  // op 53273: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3520", "key9472", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3520, key9472) size=" << scan_result.size() << std::endl;
  }

  // op 53274: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6057", "key11396", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6057, key11396) size=" << scan_result.size() << std::endl;
  }

  // op 53275: delete_range
  status = db->DeleteRange(write_options, "key2945", "key7219");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 53276: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6745", "key7939", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6745, key7939) size=" << scan_result.size() << std::endl;
  }

  // op 53277: put
  db->Put(write_options, "key10445", "value8893");

  // op 53278: delete_range
  status = db->DeleteRange(write_options, "key9955", "key10356");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 53279: put
  db->Put(write_options, "key0846", "value81265");

  // op 53280: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2316", "key2664", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2316, key2664) size=" << scan_result.size() << std::endl;
  }

  // op 53281: get
  status = db->Get(read_options, "key8949", &value);
  if (status.ok()) {
    out << "GET key8949 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53282: put
  db->Put(write_options, "key10909", "value112699");

  // op 53283: put
  db->Put(write_options, "key4579", "value35896");

  // op 53284: get
  status = db->Get(read_options, "key1705", &value);
  if (status.ok()) {
    out << "GET key1705 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53285: put
  db->Put(write_options, "key2121", "value87967");

  // op 53286: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8339", "key9642", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8339, key9642) size=" << scan_result.size() << std::endl;
  }

  // op 53287: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 53288: put
  db->Put(write_options, "key8923", "value73869");

  // op 53289: get
  status = db->Get(read_options, "key4658", &value);
  if (status.ok()) {
    out << "GET key4658 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53290: put
  db->Put(write_options, "key9403", "value8659");

  // op 53291: put
  db->Put(write_options, "key0893", "value90702");

  // op 53292: delete_range
  status = db->DeleteRange(write_options, "key9945", "key11210");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 53293: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 53294: delete
  db->Delete(write_options, "key3426");

  // op 53295: get
  status = db->Get(read_options, "key2749", &value);
  if (status.ok()) {
    out << "GET key2749 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53296: get
  status = db->Get(read_options, "key3531", &value);
  if (status.ok()) {
    out << "GET key3531 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53297: get
  status = db->Get(read_options, "key6898", &value);
  if (status.ok()) {
    out << "GET key6898 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53298: get
  status = db->Get(read_options, "key0697", &value);
  if (status.ok()) {
    out << "GET key0697 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53299: put
  db->Put(write_options, "key3439", "value114924");

  // op 53300: put
  db->Put(write_options, "key7559", "value62048");

  // op 53301: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5078", "key9224", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5078, key9224) size=" << scan_result.size() << std::endl;
  }

  // op 53302: get
  status = db->Get(read_options, "key3491", &value);
  if (status.ok()) {
    out << "GET key3491 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53303: get
  status = db->Get(read_options, "key0180", &value);
  if (status.ok()) {
    out << "GET key0180 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53304: get
  status = db->Get(read_options, "key9746", &value);
  if (status.ok()) {
    out << "GET key9746 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53305: get
  status = db->Get(read_options, "key11422", &value);
  if (status.ok()) {
    out << "GET key11422 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53306: delete_range
  status = db->DeleteRange(write_options, "key2644", "key2802");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 53307: put
  db->Put(write_options, "key10449", "value30116");

  // op 53308: get
  status = db->Get(read_options, "key8111", &value);
  if (status.ok()) {
    out << "GET key8111 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53309: delete_range
  status = db->DeleteRange(write_options, "key1174", "key7405");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 53310: get
  status = db->Get(read_options, "key1753", &value);
  if (status.ok()) {
    out << "GET key1753 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53311: put
  db->Put(write_options, "key4476", "value25130");

  // op 53312: put
  db->Put(write_options, "key8027", "value107648");

  // op 53313: put
  db->Put(write_options, "key10553", "value106943");

  // op 53314: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3933", "key5599", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3933, key5599) size=" << scan_result.size() << std::endl;
  }

  // op 53315: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0563", "key0911", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0563, key0911) size=" << scan_result.size() << std::endl;
  }

  // op 53316: put
  db->Put(write_options, "key0758", "value105755");

  // op 53317: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1162", "key10385", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1162, key10385) size=" << scan_result.size() << std::endl;
  }

  // op 53318: delete_range
  status = db->DeleteRange(write_options, "key4865", "key6508");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 53319: put
  db->Put(write_options, "key9520", "value91290");

  // op 53320: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 53321: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9153", "key11729", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9153, key11729) size=" << scan_result.size() << std::endl;
  }

  // op 53322: delete_range
  status = db->DeleteRange(write_options, "key7584", "key10301");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 53323: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3412", "key10070", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3412, key10070) size=" << scan_result.size() << std::endl;
  }

  // op 53324: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2098", "key4030", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2098, key4030) size=" << scan_result.size() << std::endl;
  }

  // op 53325: put
  db->Put(write_options, "key5184", "value25038");

  // op 53326: get
  status = db->Get(read_options, "key1133", &value);
  if (status.ok()) {
    out << "GET key1133 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53327: get
  status = db->Get(read_options, "key10436", &value);
  if (status.ok()) {
    out << "GET key10436 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53328: delete_range
  status = db->DeleteRange(write_options, "key8706", "key11223");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 53329: delete
  db->Delete(write_options, "key11211");

  // op 53330: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5167", "key8115", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5167, key8115) size=" << scan_result.size() << std::endl;
  }

  // op 53331: put
  db->Put(write_options, "key6102", "value19576");

  // op 53332: delete
  db->Delete(write_options, "key5705");

  // op 53333: delete
  db->Delete(write_options, "key2822");

  // op 53334: put
  db->Put(write_options, "key7821", "value9730");

  // op 53335: put
  db->Put(write_options, "key9178", "value33131");

  // op 53336: get
  status = db->Get(read_options, "key3837", &value);
  if (status.ok()) {
    out << "GET key3837 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53337: delete_range
  status = db->DeleteRange(write_options, "key2845", "key5134");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 53338: put
  db->Put(write_options, "key7129", "value91315");

  // op 53339: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3537", "key6056", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3537, key6056) size=" << scan_result.size() << std::endl;
  }

  // op 53340: get
  status = db->Get(read_options, "key0028", &value);
  if (status.ok()) {
    out << "GET key0028 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53341: get
  status = db->Get(read_options, "key4153", &value);
  if (status.ok()) {
    out << "GET key4153 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53342: put
  db->Put(write_options, "key1551", "value95580");

  // op 53343: put
  db->Put(write_options, "key9294", "value86190");

  // op 53344: put
  db->Put(write_options, "key2053", "value43533");

  // op 53345: delete_range
  status = db->DeleteRange(write_options, "key7902", "key9125");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 53346: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9943", "key10067", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9943, key10067) size=" << scan_result.size() << std::endl;
  }

  // op 53347: delete
  db->Delete(write_options, "key3141");

  // op 53348: put
  db->Put(write_options, "key2587", "value38371");

  // op 53349: get
  status = db->Get(read_options, "key0476", &value);
  if (status.ok()) {
    out << "GET key0476 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53350: put
  db->Put(write_options, "key8656", "value55352");

  // op 53351: delete
  db->Delete(write_options, "key4830");

  // op 53352: put
  db->Put(write_options, "key0326", "value114702");

  // op 53353: delete
  db->Delete(write_options, "key3028");

  // op 53354: put
  db->Put(write_options, "key8908", "value33912");

  // op 53355: delete_range
  status = db->DeleteRange(write_options, "key4773", "key7288");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 53356: put
  db->Put(write_options, "key10300", "value14736");

  // op 53357: get
  status = db->Get(read_options, "key3692", &value);
  if (status.ok()) {
    out << "GET key3692 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53358: delete
  db->Delete(write_options, "key2526");

  // op 53359: put
  db->Put(write_options, "key6463", "value74869");

  // op 53360: put
  db->Put(write_options, "key0188", "value104931");

  // op 53361: put
  db->Put(write_options, "key11209", "value66305");

  // op 53362: put
  db->Put(write_options, "key3084", "value77862");

  // op 53363: put
  db->Put(write_options, "key1692", "value55528");

  // op 53364: delete_range
  status = db->DeleteRange(write_options, "key6648", "key8421");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 53365: get
  status = db->Get(read_options, "key7479", &value);
  if (status.ok()) {
    out << "GET key7479 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53366: put
  db->Put(write_options, "key11453", "value33759");

  // op 53367: delete
  db->Delete(write_options, "key1573");

  // op 53368: put
  db->Put(write_options, "key2265", "value52415");

  // op 53369: delete
  db->Delete(write_options, "key8904");

  // op 53370: put
  db->Put(write_options, "key2933", "value102204");

  // op 53371: put
  db->Put(write_options, "key5789", "value107316");

  // op 53372: delete_range
  status = db->DeleteRange(write_options, "key0479", "key2381");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 53373: put
  db->Put(write_options, "key11522", "value111587");

  // op 53374: delete_range
  status = db->DeleteRange(write_options, "key4494", "key9092");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 53375: get
  status = db->Get(read_options, "key3312", &value);
  if (status.ok()) {
    out << "GET key3312 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53376: put
  db->Put(write_options, "key11508", "value42841");

  // op 53377: get
  status = db->Get(read_options, "key2087", &value);
  if (status.ok()) {
    out << "GET key2087 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53378: get
  status = db->Get(read_options, "key11666", &value);
  if (status.ok()) {
    out << "GET key11666 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53379: put
  db->Put(write_options, "key1900", "value60474");

  // op 53380: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6180", "key7332", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6180, key7332) size=" << scan_result.size() << std::endl;
  }

  // op 53381: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3287", "key5506", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3287, key5506) size=" << scan_result.size() << std::endl;
  }

  // op 53382: put
  db->Put(write_options, "key4617", "value90185");

  // op 53383: put
  db->Put(write_options, "key4933", "value2219");

  // op 53384: delete
  db->Delete(write_options, "key0404");

  // op 53385: put
  db->Put(write_options, "key1944", "value91115");

  // op 53386: put
  db->Put(write_options, "key2776", "value83444");

  // op 53387: put
  db->Put(write_options, "key5334", "value24301");

  // op 53388: get
  status = db->Get(read_options, "key9741", &value);
  if (status.ok()) {
    out << "GET key9741 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53389: delete_range
  status = db->DeleteRange(write_options, "key1598", "key6453");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 53390: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8399", "key9326", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8399, key9326) size=" << scan_result.size() << std::endl;
  }

  // op 53391: get
  status = db->Get(read_options, "key9211", &value);
  if (status.ok()) {
    out << "GET key9211 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53392: delete
  db->Delete(write_options, "key8296");

  // op 53393: put
  db->Put(write_options, "key6691", "value18491");

  // op 53394: delete_range
  status = db->DeleteRange(write_options, "key6858", "key9474");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 53395: delete
  db->Delete(write_options, "key9819");

  // op 53396: delete
  db->Delete(write_options, "key6845");

  // op 53397: put
  db->Put(write_options, "key0675", "value82827");

  // op 53398: put
  db->Put(write_options, "key5140", "value45462");

  // op 53399: put
  db->Put(write_options, "key1800", "value17465");

  // op 53400: put
  db->Put(write_options, "key1454", "value29130");

  // op 53401: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4561", "key7635", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4561, key7635) size=" << scan_result.size() << std::endl;
  }

  // op 53402: delete
  db->Delete(write_options, "key5960");

  // op 53403: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1908", "key7434", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1908, key7434) size=" << scan_result.size() << std::endl;
  }

  // op 53404: put
  db->Put(write_options, "key6080", "value106219");

  // op 53405: put
  db->Put(write_options, "key9273", "value63398");

  // op 53406: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5550", "key9064", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5550, key9064) size=" << scan_result.size() << std::endl;
  }

  // op 53407: put
  db->Put(write_options, "key4882", "value89665");

  // op 53408: put
  db->Put(write_options, "key7956", "value13456");

  // op 53409: delete_range
  status = db->DeleteRange(write_options, "key6885", "key10755");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 53410: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7271", "key7534", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7271, key7534) size=" << scan_result.size() << std::endl;
  }

  // op 53411: get
  status = db->Get(read_options, "key2119", &value);
  if (status.ok()) {
    out << "GET key2119 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53412: put
  db->Put(write_options, "key11676", "value56351");

  // op 53413: get
  status = db->Get(read_options, "key2463", &value);
  if (status.ok()) {
    out << "GET key2463 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53414: delete_range
  status = db->DeleteRange(write_options, "key0360", "key7206");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 53415: get
  status = db->Get(read_options, "key4660", &value);
  if (status.ok()) {
    out << "GET key4660 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53416: get
  status = db->Get(read_options, "key9550", &value);
  if (status.ok()) {
    out << "GET key9550 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53417: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9499", "key10728", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9499, key10728) size=" << scan_result.size() << std::endl;
  }

  // op 53418: delete_range
  status = db->DeleteRange(write_options, "key3848", "key10167");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 53419: put
  db->Put(write_options, "key1272", "value29872");

  // op 53420: get
  status = db->Get(read_options, "key10718", &value);
  if (status.ok()) {
    out << "GET key10718 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53421: put
  db->Put(write_options, "key3586", "value57020");

  // op 53422: put
  db->Put(write_options, "key1308", "value3137");

  // op 53423: delete
  db->Delete(write_options, "key5803");

  // op 53424: put
  db->Put(write_options, "key2842", "value84935");

  // op 53425: get
  status = db->Get(read_options, "key9212", &value);
  if (status.ok()) {
    out << "GET key9212 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53426: put
  db->Put(write_options, "key11313", "value96225");

  // op 53427: put
  db->Put(write_options, "key7154", "value17433");

  // op 53428: get
  status = db->Get(read_options, "key9333", &value);
  if (status.ok()) {
    out << "GET key9333 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53429: put
  db->Put(write_options, "key8953", "value1839");

  // op 53430: delete
  db->Delete(write_options, "key1721");

  // op 53431: delete
  db->Delete(write_options, "key4486");

  // op 53432: get
  status = db->Get(read_options, "key2053", &value);
  if (status.ok()) {
    out << "GET key2053 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53433: get
  status = db->Get(read_options, "key1032", &value);
  if (status.ok()) {
    out << "GET key1032 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53434: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8934", "key11365", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8934, key11365) size=" << scan_result.size() << std::endl;
  }

  // op 53435: get
  status = db->Get(read_options, "key2970", &value);
  if (status.ok()) {
    out << "GET key2970 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53436: delete
  db->Delete(write_options, "key6592");

  // op 53437: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0193", "key7252", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0193, key7252) size=" << scan_result.size() << std::endl;
  }

  // op 53438: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1889", "key9797", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1889, key9797) size=" << scan_result.size() << std::endl;
  }

  // op 53439: put
  db->Put(write_options, "key1962", "value78566");

  // op 53440: get
  status = db->Get(read_options, "key5847", &value);
  if (status.ok()) {
    out << "GET key5847 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53441: delete_range
  status = db->DeleteRange(write_options, "key0511", "key10215");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 53442: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 53443: put
  db->Put(write_options, "key3179", "value51918");

  // op 53444: get
  status = db->Get(read_options, "key9820", &value);
  if (status.ok()) {
    out << "GET key9820 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53445: put
  db->Put(write_options, "key2564", "value42461");

  // op 53446: delete_range
  status = db->DeleteRange(write_options, "key2175", "key8450");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 53447: put
  db->Put(write_options, "key2354", "value12301");

  // op 53448: put
  db->Put(write_options, "key4440", "value56545");

  // op 53449: delete_range
  status = db->DeleteRange(write_options, "key3165", "key10212");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 53450: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 53451: put
  db->Put(write_options, "key11685", "value47288");

  // op 53452: delete
  db->Delete(write_options, "key0150");

  // op 53453: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1127", "key9061", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1127, key9061) size=" << scan_result.size() << std::endl;
  }

  // op 53454: delete
  db->Delete(write_options, "key10687");

  // op 53455: put
  db->Put(write_options, "key11123", "value24610");

  // op 53456: delete_range
  status = db->DeleteRange(write_options, "key2846", "key5473");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 53457: put
  db->Put(write_options, "key8234", "value26125");

  // op 53458: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8836", "key10879", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8836, key10879) size=" << scan_result.size() << std::endl;
  }

  // op 53459: delete_range
  status = db->DeleteRange(write_options, "key6595", "key9451");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 53460: put
  db->Put(write_options, "key4403", "value63988");

  // op 53461: get
  status = db->Get(read_options, "key11645", &value);
  if (status.ok()) {
    out << "GET key11645 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53462: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 53463: delete
  db->Delete(write_options, "key8677");

  // op 53464: put
  db->Put(write_options, "key11007", "value35310");

  // op 53465: put
  db->Put(write_options, "key0712", "value82904");

  // op 53466: get
  status = db->Get(read_options, "key9604", &value);
  if (status.ok()) {
    out << "GET key9604 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53467: put
  db->Put(write_options, "key2615", "value110898");

  // op 53468: put
  db->Put(write_options, "key3523", "value75996");

  // op 53469: get
  status = db->Get(read_options, "key9714", &value);
  if (status.ok()) {
    out << "GET key9714 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53470: put
  db->Put(write_options, "key9761", "value35059");

  // op 53471: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 53472: get
  status = db->Get(read_options, "key11541", &value);
  if (status.ok()) {
    out << "GET key11541 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53473: put
  db->Put(write_options, "key8005", "value96335");

  // op 53474: put
  db->Put(write_options, "key11416", "value111425");

  // op 53475: delete
  db->Delete(write_options, "key6493");

  // op 53476: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5411", "key6545", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5411, key6545) size=" << scan_result.size() << std::endl;
  }

  // op 53477: get
  status = db->Get(read_options, "key8266", &value);
  if (status.ok()) {
    out << "GET key8266 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53478: put
  db->Put(write_options, "key2494", "value111965");

  // op 53479: put
  db->Put(write_options, "key0717", "value91105");

  // op 53480: put
  db->Put(write_options, "key2857", "value72263");

  // op 53481: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3309", "key7592", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3309, key7592) size=" << scan_result.size() << std::endl;
  }

  // op 53482: get
  status = db->Get(read_options, "key10734", &value);
  if (status.ok()) {
    out << "GET key10734 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53483: delete
  db->Delete(write_options, "key10901");

  // op 53484: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1318", "key1529", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1318, key1529) size=" << scan_result.size() << std::endl;
  }

  // op 53485: delete_range
  status = db->DeleteRange(write_options, "key8014", "key10521");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 53486: delete
  db->Delete(write_options, "key5451");

  // op 53487: delete
  db->Delete(write_options, "key0639");

  // op 53488: get
  status = db->Get(read_options, "key11728", &value);
  if (status.ok()) {
    out << "GET key11728 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53489: get
  status = db->Get(read_options, "key9321", &value);
  if (status.ok()) {
    out << "GET key9321 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53490: delete_range
  status = db->DeleteRange(write_options, "key2206", "key8536");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 53491: put
  db->Put(write_options, "key3758", "value69758");

  // op 53492: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6706", "key10504", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6706, key10504) size=" << scan_result.size() << std::endl;
  }

  // op 53493: get
  status = db->Get(read_options, "key8833", &value);
  if (status.ok()) {
    out << "GET key8833 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53494: put
  db->Put(write_options, "key6819", "value90699");

  // op 53495: put
  db->Put(write_options, "key3012", "value22143");

  // op 53496: get
  status = db->Get(read_options, "key4519", &value);
  if (status.ok()) {
    out << "GET key4519 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53497: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2687", "key6656", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2687, key6656) size=" << scan_result.size() << std::endl;
  }

  // op 53498: put
  db->Put(write_options, "key4374", "value69197");

  // op 53499: put
  db->Put(write_options, "key7250", "value57049");

  // op 53500: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 53501: put
  db->Put(write_options, "key11170", "value40909");

  // op 53502: delete_range
  status = db->DeleteRange(write_options, "key0509", "key2271");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 53503: put
  db->Put(write_options, "key5645", "value38814");

  // op 53504: get
  status = db->Get(read_options, "key11046", &value);
  if (status.ok()) {
    out << "GET key11046 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53505: get
  status = db->Get(read_options, "key10528", &value);
  if (status.ok()) {
    out << "GET key10528 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53506: put
  db->Put(write_options, "key8741", "value20355");

  // op 53507: put
  db->Put(write_options, "key0749", "value72889");

  // op 53508: get
  status = db->Get(read_options, "key4495", &value);
  if (status.ok()) {
    out << "GET key4495 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53509: get
  status = db->Get(read_options, "key2503", &value);
  if (status.ok()) {
    out << "GET key2503 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53510: delete_range
  status = db->DeleteRange(write_options, "key3087", "key5154");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 53511: put
  db->Put(write_options, "key7451", "value67506");

  // op 53512: put
  db->Put(write_options, "key8370", "value52746");

  // op 53513: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5745", "key6052", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5745, key6052) size=" << scan_result.size() << std::endl;
  }

  // op 53514: delete_range
  status = db->DeleteRange(write_options, "key1789", "key3877");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 53515: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2259", "key8259", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2259, key8259) size=" << scan_result.size() << std::endl;
  }

  // op 53516: put
  db->Put(write_options, "key3353", "value99146");

  // op 53517: delete
  db->Delete(write_options, "key7910");

  // op 53518: put
  db->Put(write_options, "key7296", "value67868");

  // op 53519: put
  db->Put(write_options, "key8259", "value26182");

  // op 53520: put
  db->Put(write_options, "key10761", "value77873");

  // op 53521: put
  db->Put(write_options, "key2066", "value62390");

  // op 53522: put
  db->Put(write_options, "key1659", "value69431");

  // op 53523: delete
  db->Delete(write_options, "key10682");

  // op 53524: put
  db->Put(write_options, "key7895", "value24743");

  // op 53525: put
  db->Put(write_options, "key8212", "value115222");

  // op 53526: put
  db->Put(write_options, "key0294", "value98317");

  // op 53527: put
  db->Put(write_options, "key7722", "value106322");

  // op 53528: put
  db->Put(write_options, "key1693", "value24605");

  // op 53529: put
  db->Put(write_options, "key1073", "value71084");

  // op 53530: delete
  db->Delete(write_options, "key4863");

  // op 53531: put
  db->Put(write_options, "key4499", "value63778");

  // op 53532: put
  db->Put(write_options, "key10632", "value10782");

  // op 53533: get
  status = db->Get(read_options, "key7210", &value);
  if (status.ok()) {
    out << "GET key7210 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53534: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5459", "key10933", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5459, key10933) size=" << scan_result.size() << std::endl;
  }

  // op 53535: delete_range
  status = db->DeleteRange(write_options, "key6620", "key11180");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 53536: get
  status = db->Get(read_options, "key5374", &value);
  if (status.ok()) {
    out << "GET key5374 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53537: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7977", "key8285", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7977, key8285) size=" << scan_result.size() << std::endl;
  }

  // op 53538: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0993", "key8528", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0993, key8528) size=" << scan_result.size() << std::endl;
  }

  // op 53539: delete_range
  status = db->DeleteRange(write_options, "key0214", "key6389");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 53540: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2825", "key6327", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2825, key6327) size=" << scan_result.size() << std::endl;
  }

  // op 53541: put
  db->Put(write_options, "key11678", "value94533");

  // op 53542: get
  status = db->Get(read_options, "key9108", &value);
  if (status.ok()) {
    out << "GET key9108 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53543: put
  db->Put(write_options, "key3479", "value39610");

  // op 53544: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3288", "key3747", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3288, key3747) size=" << scan_result.size() << std::endl;
  }

  // op 53545: put
  db->Put(write_options, "key6224", "value101882");

  // op 53546: put
  db->Put(write_options, "key1706", "value14700");

  // op 53547: put
  db->Put(write_options, "key2337", "value67890");

  // op 53548: put
  db->Put(write_options, "key1169", "value63679");

  // op 53549: put
  db->Put(write_options, "key1573", "value8749");

  // op 53550: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 53551: get
  status = db->Get(read_options, "key11666", &value);
  if (status.ok()) {
    out << "GET key11666 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53552: put
  db->Put(write_options, "key10535", "value17704");

  // op 53553: delete_range
  status = db->DeleteRange(write_options, "key1148", "key5001");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 53554: put
  db->Put(write_options, "key4269", "value88100");

  // op 53555: put
  db->Put(write_options, "key8843", "value21923");

  // op 53556: put
  db->Put(write_options, "key0952", "value70285");

  // op 53557: get
  status = db->Get(read_options, "key0325", &value);
  if (status.ok()) {
    out << "GET key0325 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53558: put
  db->Put(write_options, "key4586", "value117711");

  // op 53559: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5110", "key5734", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5110, key5734) size=" << scan_result.size() << std::endl;
  }

  // op 53560: get
  status = db->Get(read_options, "key1862", &value);
  if (status.ok()) {
    out << "GET key1862 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53561: delete_range
  status = db->DeleteRange(write_options, "key1106", "key4396");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 53562: put
  db->Put(write_options, "key6321", "value55101");

  // op 53563: put
  db->Put(write_options, "key1051", "value5332");

  // op 53564: get
  status = db->Get(read_options, "key0286", &value);
  if (status.ok()) {
    out << "GET key0286 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53565: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 53566: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3151", "key5001", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3151, key5001) size=" << scan_result.size() << std::endl;
  }

  // op 53567: put
  db->Put(write_options, "key10103", "value64450");

  // op 53568: put
  db->Put(write_options, "key8070", "value73226");

  // op 53569: get
  status = db->Get(read_options, "key8178", &value);
  if (status.ok()) {
    out << "GET key8178 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53570: put
  db->Put(write_options, "key1802", "value69321");

  // op 53571: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0958", "key1722", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0958, key1722) size=" << scan_result.size() << std::endl;
  }

  // op 53572: delete
  db->Delete(write_options, "key2836");

  // op 53573: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2566", "key9444", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2566, key9444) size=" << scan_result.size() << std::endl;
  }

  // op 53574: put
  db->Put(write_options, "key3359", "value62600");

  // op 53575: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4175", "key8617", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4175, key8617) size=" << scan_result.size() << std::endl;
  }

  // op 53576: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0316", "key9265", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0316, key9265) size=" << scan_result.size() << std::endl;
  }

  // op 53577: put
  db->Put(write_options, "key8896", "value43997");

  // op 53578: get
  status = db->Get(read_options, "key10897", &value);
  if (status.ok()) {
    out << "GET key10897 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53579: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0321", "key8544", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0321, key8544) size=" << scan_result.size() << std::endl;
  }

  // op 53580: delete
  db->Delete(write_options, "key5476");

  // op 53581: put
  db->Put(write_options, "key1750", "value1414");

  // op 53582: put
  db->Put(write_options, "key11240", "value16023");

  // op 53583: get
  status = db->Get(read_options, "key4124", &value);
  if (status.ok()) {
    out << "GET key4124 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53584: put
  db->Put(write_options, "key4941", "value71217");

  // op 53585: put
  db->Put(write_options, "key7499", "value10687");

  // op 53586: put
  db->Put(write_options, "key7575", "value81577");

  // op 53587: put
  db->Put(write_options, "key11634", "value10537");

  // op 53588: get
  status = db->Get(read_options, "key9368", &value);
  if (status.ok()) {
    out << "GET key9368 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53589: delete
  db->Delete(write_options, "key5895");

  // op 53590: delete
  db->Delete(write_options, "key0134");

  // op 53591: put
  db->Put(write_options, "key4871", "value0676");

  // op 53592: get
  status = db->Get(read_options, "key9191", &value);
  if (status.ok()) {
    out << "GET key9191 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53593: put
  db->Put(write_options, "key1482", "value111198");

  // op 53594: put
  db->Put(write_options, "key7041", "value6176");

  // op 53595: get
  status = db->Get(read_options, "key4096", &value);
  if (status.ok()) {
    out << "GET key4096 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53596: put
  db->Put(write_options, "key6707", "value78101");

  // op 53597: get
  status = db->Get(read_options, "key9331", &value);
  if (status.ok()) {
    out << "GET key9331 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53598: put
  db->Put(write_options, "key8418", "value53611");

  // op 53599: put
  db->Put(write_options, "key0266", "value53522");

  // op 53600: put
  db->Put(write_options, "key11384", "value108757");

  // op 53601: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0695", "key2884", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0695, key2884) size=" << scan_result.size() << std::endl;
  }

  // op 53602: get
  status = db->Get(read_options, "key0848", &value);
  if (status.ok()) {
    out << "GET key0848 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53603: put
  db->Put(write_options, "key0403", "value2116");

  // op 53604: put
  db->Put(write_options, "key5133", "value23752");

  // op 53605: put
  db->Put(write_options, "key6140", "value45420");

  // op 53606: delete
  db->Delete(write_options, "key4161");

  // op 53607: put
  db->Put(write_options, "key9778", "value100801");

  // op 53608: get
  status = db->Get(read_options, "key1022", &value);
  if (status.ok()) {
    out << "GET key1022 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53609: delete
  db->Delete(write_options, "key11682");

  // op 53610: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1931", "key8281", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1931, key8281) size=" << scan_result.size() << std::endl;
  }

  // op 53611: put
  db->Put(write_options, "key6966", "value24490");

  // op 53612: delete_range
  status = db->DeleteRange(write_options, "key4488", "key7886");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 53613: get
  status = db->Get(read_options, "key11572", &value);
  if (status.ok()) {
    out << "GET key11572 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53614: delete
  db->Delete(write_options, "key1222");

  // op 53615: get
  status = db->Get(read_options, "key11407", &value);
  if (status.ok()) {
    out << "GET key11407 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53616: put
  db->Put(write_options, "key8256", "value35703");

  // op 53617: delete
  db->Delete(write_options, "key3948");

  // op 53618: delete_range
  status = db->DeleteRange(write_options, "key10847", "key11064");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 53619: delete_range
  status = db->DeleteRange(write_options, "key2268", "key4878");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 53620: get
  status = db->Get(read_options, "key10387", &value);
  if (status.ok()) {
    out << "GET key10387 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53621: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3118", "key10870", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3118, key10870) size=" << scan_result.size() << std::endl;
  }

  // op 53622: get
  status = db->Get(read_options, "key6174", &value);
  if (status.ok()) {
    out << "GET key6174 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53623: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2193", "key5460", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2193, key5460) size=" << scan_result.size() << std::endl;
  }

  // op 53624: get
  status = db->Get(read_options, "key4296", &value);
  if (status.ok()) {
    out << "GET key4296 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53625: get
  status = db->Get(read_options, "key2356", &value);
  if (status.ok()) {
    out << "GET key2356 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53626: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 53627: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7740", "key8837", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7740, key8837) size=" << scan_result.size() << std::endl;
  }

  // op 53628: put
  db->Put(write_options, "key3847", "value36241");

  // op 53629: delete_range
  status = db->DeleteRange(write_options, "key0862", "key10614");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 53630: delete_range
  status = db->DeleteRange(write_options, "key9333", "key11110");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 53631: put
  db->Put(write_options, "key3401", "value51496");

  // op 53632: get
  status = db->Get(read_options, "key10418", &value);
  if (status.ok()) {
    out << "GET key10418 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53633: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7091", "key10814", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7091, key10814) size=" << scan_result.size() << std::endl;
  }

  // op 53634: put
  db->Put(write_options, "key2434", "value46306");

  // op 53635: put
  db->Put(write_options, "key9627", "value89411");

  // op 53636: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 53637: delete
  db->Delete(write_options, "key2787");

  // op 53638: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7856", "key9953", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7856, key9953) size=" << scan_result.size() << std::endl;
  }

  // op 53639: put
  db->Put(write_options, "key9587", "value113204");

  // op 53640: put
  db->Put(write_options, "key5756", "value72362");

  // op 53641: put
  db->Put(write_options, "key10547", "value88641");

  // op 53642: delete
  db->Delete(write_options, "key9033");

  // op 53643: put
  db->Put(write_options, "key2300", "value52312");

  // op 53644: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0004", "key8322", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0004, key8322) size=" << scan_result.size() << std::endl;
  }

  // op 53645: get
  status = db->Get(read_options, "key6608", &value);
  if (status.ok()) {
    out << "GET key6608 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53646: put
  db->Put(write_options, "key5188", "value63466");

  // op 53647: put
  db->Put(write_options, "key8645", "value54016");

  // op 53648: get
  status = db->Get(read_options, "key2061", &value);
  if (status.ok()) {
    out << "GET key2061 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53649: delete_range
  status = db->DeleteRange(write_options, "key4243", "key11110");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 53650: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0836", "key11570", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0836, key11570) size=" << scan_result.size() << std::endl;
  }

  // op 53651: delete
  db->Delete(write_options, "key0911");

  // op 53652: put
  db->Put(write_options, "key3599", "value49577");

  // op 53653: put
  db->Put(write_options, "key3125", "value28316");

  // op 53654: put
  db->Put(write_options, "key6757", "value10852");

  // op 53655: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 53656: get
  status = db->Get(read_options, "key3896", &value);
  if (status.ok()) {
    out << "GET key3896 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53657: put
  db->Put(write_options, "key3491", "value52467");

  // op 53658: put
  db->Put(write_options, "key0213", "value18551");

  // op 53659: put
  db->Put(write_options, "key3544", "value25157");

  // op 53660: put
  db->Put(write_options, "key2828", "value0620");

  // op 53661: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3957", "key8101", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3957, key8101) size=" << scan_result.size() << std::endl;
  }

  // op 53662: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6008", "key6180", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6008, key6180) size=" << scan_result.size() << std::endl;
  }

  // op 53663: put
  db->Put(write_options, "key10000", "value87387");

  // op 53664: put
  db->Put(write_options, "key11449", "value78720");

  // op 53665: put
  db->Put(write_options, "key10964", "value104329");

  // op 53666: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1261", "key6389", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1261, key6389) size=" << scan_result.size() << std::endl;
  }

  // op 53667: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0804", "key9450", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0804, key9450) size=" << scan_result.size() << std::endl;
  }

  // op 53668: get
  status = db->Get(read_options, "key1922", &value);
  if (status.ok()) {
    out << "GET key1922 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53669: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1407", "key4330", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1407, key4330) size=" << scan_result.size() << std::endl;
  }

  // op 53670: delete
  db->Delete(write_options, "key9409");

  // op 53671: put
  db->Put(write_options, "key7152", "value84060");

  // op 53672: put
  db->Put(write_options, "key0665", "value39054");

  // op 53673: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1688", "key5257", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1688, key5257) size=" << scan_result.size() << std::endl;
  }

  // op 53674: put
  db->Put(write_options, "key0815", "value40754");

  // op 53675: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3054", "key4131", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3054, key4131) size=" << scan_result.size() << std::endl;
  }

  // op 53676: put
  db->Put(write_options, "key0847", "value71748");

  // op 53677: get
  status = db->Get(read_options, "key0781", &value);
  if (status.ok()) {
    out << "GET key0781 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53678: get
  status = db->Get(read_options, "key11628", &value);
  if (status.ok()) {
    out << "GET key11628 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53679: put
  db->Put(write_options, "key10813", "value7997");

  // op 53680: get
  status = db->Get(read_options, "key11425", &value);
  if (status.ok()) {
    out << "GET key11425 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53681: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2863", "key10663", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2863, key10663) size=" << scan_result.size() << std::endl;
  }

  // op 53682: delete
  db->Delete(write_options, "key6054");

  // op 53683: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0307", "key8930", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0307, key8930) size=" << scan_result.size() << std::endl;
  }

  // op 53684: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3993", "key11035", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3993, key11035) size=" << scan_result.size() << std::endl;
  }

  // op 53685: get
  status = db->Get(read_options, "key10226", &value);
  if (status.ok()) {
    out << "GET key10226 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53686: get
  status = db->Get(read_options, "key3770", &value);
  if (status.ok()) {
    out << "GET key3770 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53687: put
  db->Put(write_options, "key10995", "value35146");

  // op 53688: delete_range
  status = db->DeleteRange(write_options, "key0703", "key2574");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 53689: delete_range
  status = db->DeleteRange(write_options, "key2028", "key8007");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 53690: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 53691: get
  status = db->Get(read_options, "key0440", &value);
  if (status.ok()) {
    out << "GET key0440 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53692: put
  db->Put(write_options, "key0085", "value34484");

  // op 53693: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9037", "key11275", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9037, key11275) size=" << scan_result.size() << std::endl;
  }

  // op 53694: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7712", "key11622", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7712, key11622) size=" << scan_result.size() << std::endl;
  }

  // op 53695: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 53696: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3450", "key6184", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3450, key6184) size=" << scan_result.size() << std::endl;
  }

  // op 53697: delete
  db->Delete(write_options, "key4900");

  // op 53698: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0182", "key4305", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0182, key4305) size=" << scan_result.size() << std::endl;
  }

  // op 53699: put
  db->Put(write_options, "key1422", "value83926");

  // op 53700: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8512", "key9116", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8512, key9116) size=" << scan_result.size() << std::endl;
  }

  // op 53701: put
  db->Put(write_options, "key8491", "value3290");

  // op 53702: put
  db->Put(write_options, "key2214", "value58184");

  // op 53703: put
  db->Put(write_options, "key8804", "value64592");

  // op 53704: delete_range
  status = db->DeleteRange(write_options, "key2384", "key4580");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 53705: put
  db->Put(write_options, "key1107", "value60764");

  // op 53706: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 53707: delete
  db->Delete(write_options, "key5937");

  // op 53708: get
  status = db->Get(read_options, "key2646", &value);
  if (status.ok()) {
    out << "GET key2646 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53709: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1516", "key2608", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1516, key2608) size=" << scan_result.size() << std::endl;
  }

  // op 53710: put
  db->Put(write_options, "key9010", "value43783");

  // op 53711: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 53712: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0212", "key10889", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0212, key10889) size=" << scan_result.size() << std::endl;
  }

  // op 53713: get
  status = db->Get(read_options, "key8701", &value);
  if (status.ok()) {
    out << "GET key8701 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53714: get
  status = db->Get(read_options, "key2080", &value);
  if (status.ok()) {
    out << "GET key2080 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53715: put
  db->Put(write_options, "key5249", "value37549");

  // op 53716: put
  db->Put(write_options, "key3524", "value107676");

  // op 53717: delete_range
  status = db->DeleteRange(write_options, "key1728", "key3199");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 53718: get
  status = db->Get(read_options, "key9405", &value);
  if (status.ok()) {
    out << "GET key9405 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53719: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1324", "key10713", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1324, key10713) size=" << scan_result.size() << std::endl;
  }

  // op 53720: delete
  db->Delete(write_options, "key1341");

  // op 53721: put
  db->Put(write_options, "key0662", "value96543");

  // op 53722: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0603", "key2008", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0603, key2008) size=" << scan_result.size() << std::endl;
  }

  // op 53723: put
  db->Put(write_options, "key11646", "value14070");

  // op 53724: get
  status = db->Get(read_options, "key0507", &value);
  if (status.ok()) {
    out << "GET key0507 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53725: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1120", "key2126", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1120, key2126) size=" << scan_result.size() << std::endl;
  }

  // op 53726: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3369", "key10832", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3369, key10832) size=" << scan_result.size() << std::endl;
  }

  // op 53727: get
  status = db->Get(read_options, "key6572", &value);
  if (status.ok()) {
    out << "GET key6572 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53728: get
  status = db->Get(read_options, "key11619", &value);
  if (status.ok()) {
    out << "GET key11619 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53729: put
  db->Put(write_options, "key4322", "value98039");

  // op 53730: put
  db->Put(write_options, "key0629", "value74974");

  // op 53731: put
  db->Put(write_options, "key7164", "value91291");

  // op 53732: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4551", "key6370", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4551, key6370) size=" << scan_result.size() << std::endl;
  }

  // op 53733: put
  db->Put(write_options, "key2494", "value91317");

  // op 53734: put
  db->Put(write_options, "key3070", "value66085");

  // op 53735: get
  status = db->Get(read_options, "key1293", &value);
  if (status.ok()) {
    out << "GET key1293 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53736: get
  status = db->Get(read_options, "key9901", &value);
  if (status.ok()) {
    out << "GET key9901 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53737: put
  db->Put(write_options, "key1779", "value63722");

  // op 53738: put
  db->Put(write_options, "key3671", "value37811");

  // op 53739: put
  db->Put(write_options, "key9396", "value108319");

  // op 53740: put
  db->Put(write_options, "key4596", "value116091");

  // op 53741: put
  db->Put(write_options, "key11707", "value23551");

  // op 53742: delete_range
  status = db->DeleteRange(write_options, "key0051", "key10982");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 53743: delete_range
  status = db->DeleteRange(write_options, "key11126", "key11525");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 53744: get
  status = db->Get(read_options, "key5299", &value);
  if (status.ok()) {
    out << "GET key5299 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53745: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1533", "key3469", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1533, key3469) size=" << scan_result.size() << std::endl;
  }

  // op 53746: delete_range
  status = db->DeleteRange(write_options, "key6209", "key9676");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 53747: put
  db->Put(write_options, "key10197", "value109873");

  // op 53748: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9153", "key9966", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9153, key9966) size=" << scan_result.size() << std::endl;
  }

  // op 53749: delete_range
  status = db->DeleteRange(write_options, "key4795", "key5254");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 53750: delete
  db->Delete(write_options, "key7883");

  // op 53751: put
  db->Put(write_options, "key10559", "value73354");

  // op 53752: put
  db->Put(write_options, "key2555", "value85754");

  // op 53753: get
  status = db->Get(read_options, "key0307", &value);
  if (status.ok()) {
    out << "GET key0307 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53754: put
  db->Put(write_options, "key9540", "value59967");

  // op 53755: delete
  db->Delete(write_options, "key11694");

  // op 53756: put
  db->Put(write_options, "key0670", "value92851");

  // op 53757: delete
  db->Delete(write_options, "key1479");

  // op 53758: put
  db->Put(write_options, "key1193", "value79058");

  // op 53759: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0765", "key2941", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0765, key2941) size=" << scan_result.size() << std::endl;
  }

  // op 53760: delete
  db->Delete(write_options, "key3227");

  // op 53761: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 53762: delete_range
  status = db->DeleteRange(write_options, "key2747", "key3174");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 53763: put
  db->Put(write_options, "key9976", "value99155");

  // op 53764: get
  status = db->Get(read_options, "key3819", &value);
  if (status.ok()) {
    out << "GET key3819 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53765: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3651", "key8255", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3651, key8255) size=" << scan_result.size() << std::endl;
  }

  // op 53766: put
  db->Put(write_options, "key0829", "value93473");

  // op 53767: put
  db->Put(write_options, "key1774", "value38224");

  // op 53768: put
  db->Put(write_options, "key7519", "value3200");

  // op 53769: get
  status = db->Get(read_options, "key4090", &value);
  if (status.ok()) {
    out << "GET key4090 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53770: put
  db->Put(write_options, "key4986", "value112936");

  // op 53771: put
  db->Put(write_options, "key7260", "value111878");

  // op 53772: put
  db->Put(write_options, "key8179", "value19558");

  // op 53773: get
  status = db->Get(read_options, "key6190", &value);
  if (status.ok()) {
    out << "GET key6190 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53774: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1593", "key7184", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1593, key7184) size=" << scan_result.size() << std::endl;
  }

  // op 53775: put
  db->Put(write_options, "key1875", "value1322");

  // op 53776: get
  status = db->Get(read_options, "key1296", &value);
  if (status.ok()) {
    out << "GET key1296 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53777: put
  db->Put(write_options, "key2413", "value47903");

  // op 53778: put
  db->Put(write_options, "key11279", "value71327");

  // op 53779: get
  status = db->Get(read_options, "key9379", &value);
  if (status.ok()) {
    out << "GET key9379 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53780: put
  db->Put(write_options, "key9605", "value19665");

  // op 53781: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 53782: put
  db->Put(write_options, "key5662", "value44195");

  // op 53783: get
  status = db->Get(read_options, "key5115", &value);
  if (status.ok()) {
    out << "GET key5115 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53784: put
  db->Put(write_options, "key8968", "value88670");

  // op 53785: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 53786: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0422", "key4309", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0422, key4309) size=" << scan_result.size() << std::endl;
  }

  // op 53787: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0239", "key9687", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0239, key9687) size=" << scan_result.size() << std::endl;
  }

  // op 53788: delete
  db->Delete(write_options, "key0509");

  // op 53789: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1164", "key10615", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1164, key10615) size=" << scan_result.size() << std::endl;
  }

  // op 53790: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 53791: put
  db->Put(write_options, "key2558", "value86087");

  // op 53792: get
  status = db->Get(read_options, "key8947", &value);
  if (status.ok()) {
    out << "GET key8947 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53793: delete
  db->Delete(write_options, "key1973");

  // op 53794: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3844", "key11072", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3844, key11072) size=" << scan_result.size() << std::endl;
  }

  // op 53795: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7470", "key9032", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7470, key9032) size=" << scan_result.size() << std::endl;
  }

  // op 53796: get
  status = db->Get(read_options, "key2985", &value);
  if (status.ok()) {
    out << "GET key2985 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53797: put
  db->Put(write_options, "key8702", "value60146");

  // op 53798: put
  db->Put(write_options, "key11516", "value17202");

  // op 53799: put
  db->Put(write_options, "key3364", "value78308");

  // op 53800: delete_range
  status = db->DeleteRange(write_options, "key6457", "key8705");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 53801: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7235", "key7353", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7235, key7353) size=" << scan_result.size() << std::endl;
  }

  // op 53802: delete
  db->Delete(write_options, "key1588");

  // op 53803: put
  db->Put(write_options, "key9640", "value61588");

  // op 53804: get
  status = db->Get(read_options, "key4459", &value);
  if (status.ok()) {
    out << "GET key4459 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53805: put
  db->Put(write_options, "key9637", "value117284");

  // op 53806: put
  db->Put(write_options, "key6073", "value53705");

  // op 53807: put
  db->Put(write_options, "key5813", "value115989");

  // op 53808: delete_range
  status = db->DeleteRange(write_options, "key0925", "key9701");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 53809: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7224", "key7338", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7224, key7338) size=" << scan_result.size() << std::endl;
  }

  // op 53810: put
  db->Put(write_options, "key1525", "value8230");

  // op 53811: get
  status = db->Get(read_options, "key2447", &value);
  if (status.ok()) {
    out << "GET key2447 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53812: put
  db->Put(write_options, "key5061", "value72094");

  // op 53813: put
  db->Put(write_options, "key8359", "value20866");

  // op 53814: delete
  db->Delete(write_options, "key10977");

  // op 53815: get
  status = db->Get(read_options, "key6468", &value);
  if (status.ok()) {
    out << "GET key6468 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53816: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7953", "key10233", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7953, key10233) size=" << scan_result.size() << std::endl;
  }

  // op 53817: put
  db->Put(write_options, "key9623", "value36120");

  // op 53818: put
  db->Put(write_options, "key3141", "value113212");

  // op 53819: delete_range
  status = db->DeleteRange(write_options, "key2646", "key8311");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 53820: get
  status = db->Get(read_options, "key3961", &value);
  if (status.ok()) {
    out << "GET key3961 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53821: put
  db->Put(write_options, "key1146", "value111591");

  // op 53822: put
  db->Put(write_options, "key8615", "value7612");

  // op 53823: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 53824: get
  status = db->Get(read_options, "key0738", &value);
  if (status.ok()) {
    out << "GET key0738 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53825: put
  db->Put(write_options, "key4521", "value1148");

  // op 53826: put
  db->Put(write_options, "key3367", "value27804");

  // op 53827: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3191", "key7585", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3191, key7585) size=" << scan_result.size() << std::endl;
  }

  // op 53828: put
  db->Put(write_options, "key9982", "value111348");

  // op 53829: delete_range
  status = db->DeleteRange(write_options, "key5839", "key11327");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 53830: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7415", "key10746", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7415, key10746) size=" << scan_result.size() << std::endl;
  }

  // op 53831: get
  status = db->Get(read_options, "key7103", &value);
  if (status.ok()) {
    out << "GET key7103 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53832: put
  db->Put(write_options, "key9938", "value56852");

  // op 53833: get
  status = db->Get(read_options, "key11598", &value);
  if (status.ok()) {
    out << "GET key11598 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53834: put
  db->Put(write_options, "key6470", "value31096");

  // op 53835: put
  db->Put(write_options, "key8127", "value107170");

  // op 53836: get
  status = db->Get(read_options, "key8966", &value);
  if (status.ok()) {
    out << "GET key8966 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53837: put
  db->Put(write_options, "key9214", "value77906");

  // op 53838: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1330", "key1481", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1330, key1481) size=" << scan_result.size() << std::endl;
  }

  // op 53839: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0673", "key11054", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0673, key11054) size=" << scan_result.size() << std::endl;
  }

  // op 53840: get
  status = db->Get(read_options, "key8818", &value);
  if (status.ok()) {
    out << "GET key8818 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53841: delete
  db->Delete(write_options, "key7317");

  // op 53842: get
  status = db->Get(read_options, "key3589", &value);
  if (status.ok()) {
    out << "GET key3589 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53843: get
  status = db->Get(read_options, "key0529", &value);
  if (status.ok()) {
    out << "GET key0529 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53844: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2682", "key10720", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2682, key10720) size=" << scan_result.size() << std::endl;
  }

  // op 53845: delete
  db->Delete(write_options, "key9272");

  // op 53846: delete
  db->Delete(write_options, "key11286");

  // op 53847: delete_range
  status = db->DeleteRange(write_options, "key2376", "key10659");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 53848: get
  status = db->Get(read_options, "key4199", &value);
  if (status.ok()) {
    out << "GET key4199 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53849: get
  status = db->Get(read_options, "key3818", &value);
  if (status.ok()) {
    out << "GET key3818 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53850: put
  db->Put(write_options, "key11343", "value9413");

  // op 53851: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4979", "key10562", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4979, key10562) size=" << scan_result.size() << std::endl;
  }

  // op 53852: put
  db->Put(write_options, "key8632", "value29602");

  // op 53853: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 53854: delete
  db->Delete(write_options, "key4157");

  // op 53855: put
  db->Put(write_options, "key5537", "value21599");

  // op 53856: delete_range
  status = db->DeleteRange(write_options, "key5073", "key9157");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 53857: get
  status = db->Get(read_options, "key2189", &value);
  if (status.ok()) {
    out << "GET key2189 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53858: put
  db->Put(write_options, "key4042", "value51885");

  // op 53859: delete_range
  status = db->DeleteRange(write_options, "key9627", "key11200");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 53860: delete_range
  status = db->DeleteRange(write_options, "key4792", "key5976");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 53861: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 53862: get
  status = db->Get(read_options, "key10398", &value);
  if (status.ok()) {
    out << "GET key10398 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53863: get
  status = db->Get(read_options, "key9266", &value);
  if (status.ok()) {
    out << "GET key9266 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53864: get
  status = db->Get(read_options, "key4063", &value);
  if (status.ok()) {
    out << "GET key4063 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53865: delete_range
  status = db->DeleteRange(write_options, "key9386", "key11615");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 53866: get
  status = db->Get(read_options, "key11723", &value);
  if (status.ok()) {
    out << "GET key11723 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53867: get
  status = db->Get(read_options, "key9464", &value);
  if (status.ok()) {
    out << "GET key9464 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53868: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1522", "key7479", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1522, key7479) size=" << scan_result.size() << std::endl;
  }

  // op 53869: put
  db->Put(write_options, "key5497", "value12291");

  // op 53870: put
  db->Put(write_options, "key4699", "value109395");

  // op 53871: get
  status = db->Get(read_options, "key2377", &value);
  if (status.ok()) {
    out << "GET key2377 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53872: get
  status = db->Get(read_options, "key0063", &value);
  if (status.ok()) {
    out << "GET key0063 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53873: get
  status = db->Get(read_options, "key1973", &value);
  if (status.ok()) {
    out << "GET key1973 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53874: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 53875: put
  db->Put(write_options, "key9453", "value105085");

  // op 53876: delete
  db->Delete(write_options, "key1475");

  // op 53877: get
  status = db->Get(read_options, "key1185", &value);
  if (status.ok()) {
    out << "GET key1185 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53878: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2649", "key5165", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2649, key5165) size=" << scan_result.size() << std::endl;
  }

  // op 53879: put
  db->Put(write_options, "key10743", "value2977");

  // op 53880: put
  db->Put(write_options, "key8583", "value97969");

  // op 53881: put
  db->Put(write_options, "key9205", "value86115");

  // op 53882: put
  db->Put(write_options, "key7991", "value30959");

  // op 53883: get
  status = db->Get(read_options, "key10904", &value);
  if (status.ok()) {
    out << "GET key10904 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53884: delete_range
  status = db->DeleteRange(write_options, "key0991", "key4684");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 53885: put
  db->Put(write_options, "key9969", "value102504");

  // op 53886: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2315", "key4104", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2315, key4104) size=" << scan_result.size() << std::endl;
  }

  // op 53887: put
  db->Put(write_options, "key1245", "value85806");

  // op 53888: put
  db->Put(write_options, "key11057", "value6269");

  // op 53889: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 53890: delete
  db->Delete(write_options, "key0342");

  // op 53891: get
  status = db->Get(read_options, "key2199", &value);
  if (status.ok()) {
    out << "GET key2199 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53892: delete
  db->Delete(write_options, "key10501");

  // op 53893: delete
  db->Delete(write_options, "key10353");

  // op 53894: put
  db->Put(write_options, "key11163", "value28840");

  // op 53895: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3634", "key9592", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3634, key9592) size=" << scan_result.size() << std::endl;
  }

  // op 53896: put
  db->Put(write_options, "key4540", "value28174");

  // op 53897: put
  db->Put(write_options, "key4339", "value105619");

  // op 53898: delete_range
  status = db->DeleteRange(write_options, "key2284", "key7864");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 53899: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3749", "key11048", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3749, key11048) size=" << scan_result.size() << std::endl;
  }

  // op 53900: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4142", "key8000", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4142, key8000) size=" << scan_result.size() << std::endl;
  }

  // op 53901: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4044", "key6226", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4044, key6226) size=" << scan_result.size() << std::endl;
  }

  // op 53902: put
  db->Put(write_options, "key9145", "value64377");

  // op 53903: get
  status = db->Get(read_options, "key8458", &value);
  if (status.ok()) {
    out << "GET key8458 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53904: get
  status = db->Get(read_options, "key4657", &value);
  if (status.ok()) {
    out << "GET key4657 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53905: put
  db->Put(write_options, "key4026", "value94456");

  // op 53906: get
  status = db->Get(read_options, "key1402", &value);
  if (status.ok()) {
    out << "GET key1402 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53907: delete
  db->Delete(write_options, "key2404");

  // op 53908: put
  db->Put(write_options, "key7626", "value10837");

  // op 53909: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 53910: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3105", "key7701", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3105, key7701) size=" << scan_result.size() << std::endl;
  }

  // op 53911: get
  status = db->Get(read_options, "key0718", &value);
  if (status.ok()) {
    out << "GET key0718 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53912: delete_range
  status = db->DeleteRange(write_options, "key7753", "key11250");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 53913: get
  status = db->Get(read_options, "key9917", &value);
  if (status.ok()) {
    out << "GET key9917 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53914: put
  db->Put(write_options, "key6924", "value98654");

  // op 53915: put
  db->Put(write_options, "key7455", "value44963");

  // op 53916: get
  status = db->Get(read_options, "key3317", &value);
  if (status.ok()) {
    out << "GET key3317 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53917: get
  status = db->Get(read_options, "key11684", &value);
  if (status.ok()) {
    out << "GET key11684 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53918: put
  db->Put(write_options, "key6338", "value9989");

  // op 53919: delete
  db->Delete(write_options, "key0308");

  // op 53920: get
  status = db->Get(read_options, "key8541", &value);
  if (status.ok()) {
    out << "GET key8541 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53921: put
  db->Put(write_options, "key6996", "value65583");

  // op 53922: put
  db->Put(write_options, "key4954", "value97996");

  // op 53923: delete_range
  status = db->DeleteRange(write_options, "key9033", "key9650");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 53924: put
  db->Put(write_options, "key10984", "value106405");

  // op 53925: get
  status = db->Get(read_options, "key4655", &value);
  if (status.ok()) {
    out << "GET key4655 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53926: delete_range
  status = db->DeleteRange(write_options, "key3643", "key11270");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 53927: put
  db->Put(write_options, "key7396", "value96671");

  // op 53928: get
  status = db->Get(read_options, "key11377", &value);
  if (status.ok()) {
    out << "GET key11377 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53929: delete
  db->Delete(write_options, "key2036");

  // op 53930: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2360", "key6487", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2360, key6487) size=" << scan_result.size() << std::endl;
  }

  // op 53931: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 53932: delete_range
  status = db->DeleteRange(write_options, "key4056", "key8086");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 53933: put
  db->Put(write_options, "key8375", "value57558");

  // op 53934: put
  db->Put(write_options, "key0844", "value96551");

  // op 53935: put
  db->Put(write_options, "key2361", "value9166");

  // op 53936: delete_range
  status = db->DeleteRange(write_options, "key3332", "key9557");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 53937: delete
  db->Delete(write_options, "key5957");

  // op 53938: put
  db->Put(write_options, "key9608", "value70789");

  // op 53939: put
  db->Put(write_options, "key11538", "value65453");

  // op 53940: put
  db->Put(write_options, "key2196", "value28208");

  // op 53941: put
  db->Put(write_options, "key2345", "value88705");

  // op 53942: put
  db->Put(write_options, "key8998", "value87218");

  // op 53943: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1080", "key4579", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1080, key4579) size=" << scan_result.size() << std::endl;
  }

  // op 53944: put
  db->Put(write_options, "key5818", "value4138");

  // op 53945: delete_range
  status = db->DeleteRange(write_options, "key10463", "key11604");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 53946: put
  db->Put(write_options, "key6091", "value76620");

  // op 53947: get
  status = db->Get(read_options, "key5037", &value);
  if (status.ok()) {
    out << "GET key5037 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53948: delete_range
  status = db->DeleteRange(write_options, "key4405", "key10293");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 53949: put
  db->Put(write_options, "key4836", "value32794");

  // op 53950: put
  db->Put(write_options, "key11090", "value2130");

  // op 53951: put
  db->Put(write_options, "key8769", "value92958");

  // op 53952: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 53953: put
  db->Put(write_options, "key1563", "value4952");

  // op 53954: put
  db->Put(write_options, "key7406", "value108231");

  // op 53955: put
  db->Put(write_options, "key11034", "value112647");

  // op 53956: get
  status = db->Get(read_options, "key8388", &value);
  if (status.ok()) {
    out << "GET key8388 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53957: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0223", "key0385", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0223, key0385) size=" << scan_result.size() << std::endl;
  }

  // op 53958: put
  db->Put(write_options, "key5471", "value42178");

  // op 53959: delete_range
  status = db->DeleteRange(write_options, "key2884", "key6899");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 53960: put
  db->Put(write_options, "key6153", "value78273");

  // op 53961: put
  db->Put(write_options, "key10436", "value116669");

  // op 53962: put
  db->Put(write_options, "key1864", "value38097");

  // op 53963: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8229", "key10215", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8229, key10215) size=" << scan_result.size() << std::endl;
  }

  // op 53964: put
  db->Put(write_options, "key9903", "value96719");

  // op 53965: put
  db->Put(write_options, "key3953", "value92084");

  // op 53966: scan
  scan_result.clear();
  status = db->Scan(read_options, "key8625", "key10487", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key8625, key10487) size=" << scan_result.size() << std::endl;
  }

  // op 53967: put
  db->Put(write_options, "key9651", "value92242");

  // op 53968: delete_range
  status = db->DeleteRange(write_options, "key0016", "key11290");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 53969: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1681", "key1898", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1681, key1898) size=" << scan_result.size() << std::endl;
  }

  // op 53970: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 53971: delete_range
  status = db->DeleteRange(write_options, "key7199", "key10322");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 53972: put
  db->Put(write_options, "key6869", "value19493");

  // op 53973: put
  db->Put(write_options, "key1523", "value41552");

  // op 53974: put
  db->Put(write_options, "key9450", "value64293");

  // op 53975: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 53976: put
  db->Put(write_options, "key6851", "value5244");

  // op 53977: put
  db->Put(write_options, "key5997", "value75720");

  // op 53978: put
  db->Put(write_options, "key10299", "value51241");

  // op 53979: get
  status = db->Get(read_options, "key2632", &value);
  if (status.ok()) {
    out << "GET key2632 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53980: get
  status = db->Get(read_options, "key6567", &value);
  if (status.ok()) {
    out << "GET key6567 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53981: put
  db->Put(write_options, "key10199", "value98186");

  // op 53982: put
  db->Put(write_options, "key0944", "value32614");

  // op 53983: put
  db->Put(write_options, "key9176", "value60153");

  // op 53984: put
  db->Put(write_options, "key8450", "value16011");

  // op 53985: get
  status = db->Get(read_options, "key9836", &value);
  if (status.ok()) {
    out << "GET key9836 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53986: put
  db->Put(write_options, "key5920", "value50520");

  // op 53987: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1728", "key6384", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1728, key6384) size=" << scan_result.size() << std::endl;
  }

  // op 53988: put
  db->Put(write_options, "key7217", "value10681");

  // op 53989: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5351", "key5872", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5351, key5872) size=" << scan_result.size() << std::endl;
  }

  // op 53990: get
  status = db->Get(read_options, "key9419", &value);
  if (status.ok()) {
    out << "GET key9419 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53991: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 53992: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2428", "key8655", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2428, key8655) size=" << scan_result.size() << std::endl;
  }

  // op 53993: put
  db->Put(write_options, "key4875", "value53762");

  // op 53994: put
  db->Put(write_options, "key1248", "value100584");

  // op 53995: put
  db->Put(write_options, "key1893", "value79154");

  // op 53996: get
  status = db->Get(read_options, "key4353", &value);
  if (status.ok()) {
    out << "GET key4353 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53997: get
  status = db->Get(read_options, "key0948", &value);
  if (status.ok()) {
    out << "GET key0948 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 53998: delete_range
  status = db->DeleteRange(write_options, "key11224", "key11628");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 53999: put
  db->Put(write_options, "key5530", "value66009");

  // op 54000: put
  db->Put(write_options, "key9492", "value30881");

  // op 54001: put
  db->Put(write_options, "key9032", "value116267");

  // op 54002: get
  status = db->Get(read_options, "key6971", &value);
  if (status.ok()) {
    out << "GET key6971 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 54003: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 54004: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7260", "key9121", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7260, key9121) size=" << scan_result.size() << std::endl;
  }

  // op 54005: get
  status = db->Get(read_options, "key2958", &value);
  if (status.ok()) {
    out << "GET key2958 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 54006: delete_range
  status = db->DeleteRange(write_options, "key0242", "key5638");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 54007: put
  db->Put(write_options, "key4713", "value68635");

  // op 54008: put
  db->Put(write_options, "key11564", "value78915");

  // op 54009: put
  db->Put(write_options, "key8614", "value38894");

  // op 54010: put
  db->Put(write_options, "key0153", "value102696");

  // op 54011: put
  db->Put(write_options, "key5241", "value77115");

  // op 54012: delete
  db->Delete(write_options, "key7691");

  // op 54013: delete_range
  status = db->DeleteRange(write_options, "key0850", "key10423");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 54014: put
  db->Put(write_options, "key5798", "value57742");

  // op 54015: put
  db->Put(write_options, "key8473", "value81663");

  // op 54016: put
  db->Put(write_options, "key3268", "value40051");

  // op 54017: put
  db->Put(write_options, "key9562", "value118249");

  // op 54018: get
  status = db->Get(read_options, "key3609", &value);
  if (status.ok()) {
    out << "GET key3609 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 54019: delete
  db->Delete(write_options, "key9578");

  // op 54020: get
  status = db->Get(read_options, "key1734", &value);
  if (status.ok()) {
    out << "GET key1734 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 54021: put
  db->Put(write_options, "key0392", "value30765");

  // op 54022: put
  db->Put(write_options, "key8965", "value82565");

  // op 54023: put
  db->Put(write_options, "key3976", "value92515");

  // op 54024: put
  db->Put(write_options, "key11244", "value32838");

  // op 54025: put
  db->Put(write_options, "key7717", "value98703");

  // op 54026: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 54027: get
  status = db->Get(read_options, "key1123", &value);
  if (status.ok()) {
    out << "GET key1123 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 54028: put
  db->Put(write_options, "key9948", "value47064");

  // op 54029: get
  status = db->Get(read_options, "key9636", &value);
  if (status.ok()) {
    out << "GET key9636 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 54030: put
  db->Put(write_options, "key0603", "value93912");

  // op 54031: delete_range
  status = db->DeleteRange(write_options, "key1513", "key6290");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 54032: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7774", "key9095", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7774, key9095) size=" << scan_result.size() << std::endl;
  }

  // op 54033: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3565", "key7336", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3565, key7336) size=" << scan_result.size() << std::endl;
  }

  // op 54034: get
  status = db->Get(read_options, "key0753", &value);
  if (status.ok()) {
    out << "GET key0753 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 54035: put
  db->Put(write_options, "key5552", "value48136");

  // op 54036: delete_range
  status = db->DeleteRange(write_options, "key2738", "key9863");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 54037: put
  db->Put(write_options, "key6648", "value59119");

  // op 54038: put
  db->Put(write_options, "key6872", "value73656");

  // op 54039: put
  db->Put(write_options, "key2624", "value53340");

  // op 54040: put
  db->Put(write_options, "key1448", "value108626");

  // op 54041: get
  status = db->Get(read_options, "key9786", &value);
  if (status.ok()) {
    out << "GET key9786 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 54042: get
  status = db->Get(read_options, "key9232", &value);
  if (status.ok()) {
    out << "GET key9232 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 54043: put
  db->Put(write_options, "key10458", "value61117");

  // op 54044: put
  db->Put(write_options, "key2536", "value38279");

  // op 54045: put
  db->Put(write_options, "key9398", "value44016");

  // op 54046: put
  db->Put(write_options, "key4754", "value31488");

  // op 54047: put
  db->Put(write_options, "key3472", "value79751");

  // op 54048: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1377", "key9861", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1377, key9861) size=" << scan_result.size() << std::endl;
  }

  // op 54049: delete
  db->Delete(write_options, "key4752");

  // op 54050: put
  db->Put(write_options, "key1729", "value56376");

  // op 54051: get
  status = db->Get(read_options, "key9189", &value);
  if (status.ok()) {
    out << "GET key9189 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 54052: get
  status = db->Get(read_options, "key10962", &value);
  if (status.ok()) {
    out << "GET key10962 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 54053: get
  status = db->Get(read_options, "key3762", &value);
  if (status.ok()) {
    out << "GET key3762 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 54054: delete
  db->Delete(write_options, "key3775");

  // op 54055: get
  status = db->Get(read_options, "key8046", &value);
  if (status.ok()) {
    out << "GET key8046 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 54056: put
  db->Put(write_options, "key7052", "value94399");

  // op 54057: get
  status = db->Get(read_options, "key3164", &value);
  if (status.ok()) {
    out << "GET key3164 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 54058: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 54059: put
  db->Put(write_options, "key1187", "value1130");

  // op 54060: put
  db->Put(write_options, "key10826", "value91302");

  // op 54061: put
  db->Put(write_options, "key5474", "value74547");

  // op 54062: get
  status = db->Get(read_options, "key3004", &value);
  if (status.ok()) {
    out << "GET key3004 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 54063: delete
  db->Delete(write_options, "key1586");

  // op 54064: put
  db->Put(write_options, "key5251", "value93159");

  // op 54065: put
  db->Put(write_options, "key9138", "value5027");

  // op 54066: put
  db->Put(write_options, "key1420", "value95607");

  // op 54067: put
  db->Put(write_options, "key4453", "value91722");

  // op 54068: put
  db->Put(write_options, "key0939", "value56868");

  // op 54069: put
  db->Put(write_options, "key5500", "value106300");

  // op 54070: delete_range
  status = db->DeleteRange(write_options, "key5092", "key5356");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 54071: put
  db->Put(write_options, "key8090", "value62701");

  // op 54072: put
  db->Put(write_options, "key4164", "value99351");

  // op 54073: put
  db->Put(write_options, "key1053", "value91257");

  // op 54074: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1436", "key4168", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1436, key4168) size=" << scan_result.size() << std::endl;
  }

  // op 54075: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 54076: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6818", "key8841", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6818, key8841) size=" << scan_result.size() << std::endl;
  }

  // op 54077: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0060", "key4062", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0060, key4062) size=" << scan_result.size() << std::endl;
  }

  // op 54078: put
  db->Put(write_options, "key2149", "value9457");

  // op 54079: put
  db->Put(write_options, "key1046", "value88491");

  // op 54080: delete_range
  status = db->DeleteRange(write_options, "key1495", "key7370");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 54081: put
  db->Put(write_options, "key3982", "value26815");

  // op 54082: get
  status = db->Get(read_options, "key4499", &value);
  if (status.ok()) {
    out << "GET key4499 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 54083: delete_range
  status = db->DeleteRange(write_options, "key4930", "key9585");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 54084: put
  db->Put(write_options, "key6266", "value63443");

  // op 54085: put
  db->Put(write_options, "key2702", "value19690");

  // op 54086: get
  status = db->Get(read_options, "key0662", &value);
  if (status.ok()) {
    out << "GET key0662 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 54087: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2557", "key10423", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2557, key10423) size=" << scan_result.size() << std::endl;
  }

  // op 54088: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5829", "key11482", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5829, key11482) size=" << scan_result.size() << std::endl;
  }

  // op 54089: put
  db->Put(write_options, "key2131", "value63027");

  // op 54090: get
  status = db->Get(read_options, "key10540", &value);
  if (status.ok()) {
    out << "GET key10540 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 54091: delete_range
  status = db->DeleteRange(write_options, "key9286", "key11625");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 54092: delete_range
  status = db->DeleteRange(write_options, "key1304", "key3070");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 54093: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7263", "key11686", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7263, key11686) size=" << scan_result.size() << std::endl;
  }

  // op 54094: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2154", "key6453", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2154, key6453) size=" << scan_result.size() << std::endl;
  }

  // op 54095: put
  db->Put(write_options, "key10737", "value110582");

  // op 54096: delete_range
  status = db->DeleteRange(write_options, "key2208", "key8567");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 54097: put
  db->Put(write_options, "key1303", "value91867");

  // op 54098: put
  db->Put(write_options, "key8420", "value45298");

  // op 54099: put
  db->Put(write_options, "key0361", "value105503");

  // op 54100: put
  db->Put(write_options, "key10378", "value82607");

  // op 54101: put
  db->Put(write_options, "key2114", "value30351");

  // op 54102: get
  status = db->Get(read_options, "key3002", &value);
  if (status.ok()) {
    out << "GET key3002 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 54103: delete
  db->Delete(write_options, "key9387");

  // op 54104: put
  db->Put(write_options, "key3074", "value76872");

  // op 54105: delete
  db->Delete(write_options, "key6128");

  // op 54106: put
  db->Put(write_options, "key6095", "value56321");

  // op 54107: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6157", "key8032", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6157, key8032) size=" << scan_result.size() << std::endl;
  }

  // op 54108: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 54109: get
  status = db->Get(read_options, "key8755", &value);
  if (status.ok()) {
    out << "GET key8755 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 54110: delete_range
  status = db->DeleteRange(write_options, "key4847", "key6226");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 54111: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5740", "key6240", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5740, key6240) size=" << scan_result.size() << std::endl;
  }

  // op 54112: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3735", "key10740", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3735, key10740) size=" << scan_result.size() << std::endl;
  }

  // op 54113: delete
  db->Delete(write_options, "key0149");

  // op 54114: get
  status = db->Get(read_options, "key6634", &value);
  if (status.ok()) {
    out << "GET key6634 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 54115: delete_range
  status = db->DeleteRange(write_options, "key5588", "key10140");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 54116: get
  status = db->Get(read_options, "key5484", &value);
  if (status.ok()) {
    out << "GET key5484 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 54117: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0863", "key9633", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0863, key9633) size=" << scan_result.size() << std::endl;
  }

  // op 54118: put
  db->Put(write_options, "key9839", "value115434");

  // op 54119: put
  db->Put(write_options, "key2118", "value82428");

  // op 54120: get
  status = db->Get(read_options, "key4434", &value);
  if (status.ok()) {
    out << "GET key4434 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 54121: get
  status = db->Get(read_options, "key5176", &value);
  if (status.ok()) {
    out << "GET key5176 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 54122: get
  status = db->Get(read_options, "key1218", &value);
  if (status.ok()) {
    out << "GET key1218 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 54123: delete_range
  status = db->DeleteRange(write_options, "key4193", "key10238");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 54124: put
  db->Put(write_options, "key4079", "value38201");

  // op 54125: get
  status = db->Get(read_options, "key6625", &value);
  if (status.ok()) {
    out << "GET key6625 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 54126: put
  db->Put(write_options, "key5305", "value58830");

  // op 54127: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2657", "key5693", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2657, key5693) size=" << scan_result.size() << std::endl;
  }

  // op 54128: put
  db->Put(write_options, "key10425", "value16359");

  // op 54129: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2452", "key8838", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2452, key8838) size=" << scan_result.size() << std::endl;
  }

  // op 54130: put
  db->Put(write_options, "key5862", "value20784");

  // op 54131: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 54132: delete_range
  status = db->DeleteRange(write_options, "key4161", "key9372");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 54133: put
  db->Put(write_options, "key10248", "value65573");

  // op 54134: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9391", "key10745", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9391, key10745) size=" << scan_result.size() << std::endl;
  }

  // op 54135: put
  db->Put(write_options, "key8240", "value51198");

  // op 54136: delete
  db->Delete(write_options, "key3769");

  // op 54137: put
  db->Put(write_options, "key0814", "value69316");

  // op 54138: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4814", "key8015", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4814, key8015) size=" << scan_result.size() << std::endl;
  }

  // op 54139: get
  status = db->Get(read_options, "key3969", &value);
  if (status.ok()) {
    out << "GET key3969 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 54140: delete
  db->Delete(write_options, "key9019");

  // op 54141: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2412", "key4400", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2412, key4400) size=" << scan_result.size() << std::endl;
  }

  // op 54142: delete_range
  status = db->DeleteRange(write_options, "key7930", "key11408");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 54143: put
  db->Put(write_options, "key10226", "value0454");

  // op 54144: put
  db->Put(write_options, "key5571", "value90661");

  // op 54145: put
  db->Put(write_options, "key5747", "value69027");

  // op 54146: get
  status = db->Get(read_options, "key5913", &value);
  if (status.ok()) {
    out << "GET key5913 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 54147: delete_range
  status = db->DeleteRange(write_options, "key2646", "key4913");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 54148: get
  status = db->Get(read_options, "key7461", &value);
  if (status.ok()) {
    out << "GET key7461 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 54149: get
  status = db->Get(read_options, "key3052", &value);
  if (status.ok()) {
    out << "GET key3052 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 54150: delete_range
  status = db->DeleteRange(write_options, "key6122", "key10687");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 54151: put
  db->Put(write_options, "key2671", "value112853");

  // op 54152: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2062", "key11544", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2062, key11544) size=" << scan_result.size() << std::endl;
  }

  // op 54153: delete_range
  status = db->DeleteRange(write_options, "key4280", "key7837");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 54154: get
  status = db->Get(read_options, "key3632", &value);
  if (status.ok()) {
    out << "GET key3632 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 54155: put
  db->Put(write_options, "key1367", "value115832");

  // op 54156: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 54157: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6011", "key6371", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6011, key6371) size=" << scan_result.size() << std::endl;
  }

  // op 54158: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 54159: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0525", "key9810", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0525, key9810) size=" << scan_result.size() << std::endl;
  }

  // op 54160: delete
  db->Delete(write_options, "key5505");

  // op 54161: get
  status = db->Get(read_options, "key0106", &value);
  if (status.ok()) {
    out << "GET key0106 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 54162: put
  db->Put(write_options, "key8298", "value114958");

  // op 54163: delete_range
  status = db->DeleteRange(write_options, "key1944", "key4531");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 54164: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 54165: delete_range
  status = db->DeleteRange(write_options, "key0981", "key4228");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 54166: delete_range
  status = db->DeleteRange(write_options, "key3497", "key7670");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 54167: put
  db->Put(write_options, "key5013", "value91546");

  // op 54168: compaction
  status = db->ForceFullCompaction();
  if (!status.ok()) {
    std::cerr << "ForceFullCompaction failed: " << status.ToString() << std::endl;
  }

  // op 54169: delete_range
  status = db->DeleteRange(write_options, "key6508", "key10142");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 54170: put
  db->Put(write_options, "key9305", "value99146");

  // op 54171: put
  db->Put(write_options, "key7335", "value32220");

  // op 54172: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6503", "key7374", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6503, key7374) size=" << scan_result.size() << std::endl;
  }

  // op 54173: get
  status = db->Get(read_options, "key2570", &value);
  if (status.ok()) {
    out << "GET key2570 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 54174: put
  db->Put(write_options, "key5210", "value11833");

  // op 54175: delete_range
  status = db->DeleteRange(write_options, "key1359", "key11187");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 54176: put
  db->Put(write_options, "key4808", "value85103");

  // op 54177: scan
  scan_result.clear();
  status = db->Scan(read_options, "key6448", "key8256", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key6448, key8256) size=" << scan_result.size() << std::endl;
  }

  // op 54178: delete
  db->Delete(write_options, "key7820");

  // op 54179: put
  db->Put(write_options, "key9169", "value28592");

  // op 54180: put
  db->Put(write_options, "key11717", "value60263");

  // op 54181: put
  db->Put(write_options, "key5060", "value109584");

  // op 54182: scan
  scan_result.clear();
  status = db->Scan(read_options, "key1445", "key9894", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key1445, key9894) size=" << scan_result.size() << std::endl;
  }

  // op 54183: get
  status = db->Get(read_options, "key8749", &value);
  if (status.ok()) {
    out << "GET key8749 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 54184: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9198", "key11442", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9198, key11442) size=" << scan_result.size() << std::endl;
  }

  // op 54185: delete
  db->Delete(write_options, "key7344");

  // op 54186: delete
  db->Delete(write_options, "key1830");

  // op 54187: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0430", "key0672", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0430, key0672) size=" << scan_result.size() << std::endl;
  }

  // op 54188: get
  status = db->Get(read_options, "key8177", &value);
  if (status.ok()) {
    out << "GET key8177 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 54189: put
  db->Put(write_options, "key9437", "value103795");

  // op 54190: put
  db->Put(write_options, "key9494", "value84876");

  // op 54191: scan
  scan_result.clear();
  status = db->Scan(read_options, "key2021", "key8392", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key2021, key8392) size=" << scan_result.size() << std::endl;
  }

  // op 54192: delete
  db->Delete(write_options, "key8739");

  // op 54193: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7264", "key10725", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7264, key10725) size=" << scan_result.size() << std::endl;
  }

  // op 54194: put
  db->Put(write_options, "key1022", "value88865");

  // op 54195: put
  db->Put(write_options, "key9461", "value52484");

  // op 54196: put
  db->Put(write_options, "key11463", "value36852");

  // op 54197: put
  db->Put(write_options, "key7089", "value26588");

  // op 54198: get
  status = db->Get(read_options, "key7518", &value);
  if (status.ok()) {
    out << "GET key7518 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 54199: get
  status = db->Get(read_options, "key8654", &value);
  if (status.ok()) {
    out << "GET key8654 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 54200: put
  db->Put(write_options, "key8654", "value72125");

  // op 54201: put
  db->Put(write_options, "key1054", "value100723");

  // op 54202: put
  db->Put(write_options, "key8200", "value75256");

  // op 54203: put
  db->Put(write_options, "key11528", "value48694");

  // op 54204: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3840", "key8543", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3840, key8543) size=" << scan_result.size() << std::endl;
  }

  // op 54205: get
  status = db->Get(read_options, "key4322", &value);
  if (status.ok()) {
    out << "GET key4322 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 54206: put
  db->Put(write_options, "key9946", "value77470");

  // op 54207: put
  db->Put(write_options, "key7418", "value93917");

  // op 54208: scan
  scan_result.clear();
  status = db->Scan(read_options, "key5347", "key11557", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key5347, key11557) size=" << scan_result.size() << std::endl;
  }

  // op 54209: get
  status = db->Get(read_options, "key3182", &value);
  if (status.ok()) {
    out << "GET key3182 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 54210: scan
  scan_result.clear();
  status = db->Scan(read_options, "key7010", "key10515", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key7010, key10515) size=" << scan_result.size() << std::endl;
  }

  // op 54211: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0925", "key10260", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0925, key10260) size=" << scan_result.size() << std::endl;
  }

  // op 54212: delete_range
  status = db->DeleteRange(write_options, "key0018", "key3959");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 54213: delete_range
  status = db->DeleteRange(write_options, "key8962", "key10705");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 54214: put
  db->Put(write_options, "key3550", "value56139");

  // op 54215: put
  db->Put(write_options, "key0902", "value47507");

  // op 54216: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 54217: scan
  scan_result.clear();
  status = db->Scan(read_options, "key10117", "key10872", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key10117, key10872) size=" << scan_result.size() << std::endl;
  }

  // op 54218: put
  db->Put(write_options, "key3940", "value63424");

  // op 54219: get
  status = db->Get(read_options, "key6392", &value);
  if (status.ok()) {
    out << "GET key6392 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 54220: get
  status = db->Get(read_options, "key1074", &value);
  if (status.ok()) {
    out << "GET key1074 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 54221: put
  db->Put(write_options, "key0803", "value37778");

  // op 54222: put
  db->Put(write_options, "key6783", "value6410");

  // op 54223: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3813", "key4333", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3813, key4333) size=" << scan_result.size() << std::endl;
  }

  // op 54224: delete_range
  status = db->DeleteRange(write_options, "key2479", "key10084");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 54225: put
  db->Put(write_options, "key9433", "value111693");

  // op 54226: put
  db->Put(write_options, "key9102", "value52503");

  // op 54227: get
  status = db->Get(read_options, "key6803", &value);
  if (status.ok()) {
    out << "GET key6803 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 54228: put
  db->Put(write_options, "key0032", "value113471");

  // op 54229: delete_range
  status = db->DeleteRange(write_options, "key5133", "key10696");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 54230: put
  db->Put(write_options, "key7376", "value54459");

  // op 54231: scan
  scan_result.clear();
  status = db->Scan(read_options, "key4062", "key6916", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key4062, key6916) size=" << scan_result.size() << std::endl;
  }

  // op 54232: delete_range
  status = db->DeleteRange(write_options, "key1707", "key2080");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 54233: get
  status = db->Get(read_options, "key11450", &value);
  if (status.ok()) {
    out << "GET key11450 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 54234: delete
  db->Delete(write_options, "key7771");

  // op 54235: get
  status = db->Get(read_options, "key2141", &value);
  if (status.ok()) {
    out << "GET key2141 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 54236: scan
  scan_result.clear();
  status = db->Scan(read_options, "key3287", "key6690", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key3287, key6690) size=" << scan_result.size() << std::endl;
  }

  // op 54237: delete
  db->Delete(write_options, "key11236");

  // op 54238: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9201", "key9468", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9201, key9468) size=" << scan_result.size() << std::endl;
  }

  // op 54239: get
  status = db->Get(read_options, "key10195", &value);
  if (status.ok()) {
    out << "GET key10195 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 54240: get
  status = db->Get(read_options, "key6342", &value);
  if (status.ok()) {
    out << "GET key6342 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 54241: put
  db->Put(write_options, "key5976", "value61730");

  // op 54242: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0792", "key10578", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0792, key10578) size=" << scan_result.size() << std::endl;
  }

  // op 54243: get
  status = db->Get(read_options, "key11158", &value);
  if (status.ok()) {
    out << "GET key11158 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 54244: put
  db->Put(write_options, "key5688", "value4087");

  // op 54245: get
  status = db->Get(read_options, "key7454", &value);
  if (status.ok()) {
    out << "GET key7454 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 54246: put
  db->Put(write_options, "key3231", "value87375");

  // op 54247: get
  status = db->Get(read_options, "key3416", &value);
  if (status.ok()) {
    out << "GET key3416 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 54248: iterate
  {
    auto* it = db->NewIterator(read_options);
    size_t iter_count = 0;
    for (it->SeekToFirst(); it->Valid(); it->Next()) {
      ++iter_count;
    }
    if (!it->status().ok()) {
      std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
    }
    out << "ITERATE count=" << iter_count << std::endl;
    delete it;
  }

  // op 54249: put
  db->Put(write_options, "key7761", "value67021");

  // op 54250: delete
  db->Delete(write_options, "key1307");

  // op 54251: put
  db->Put(write_options, "key1510", "value87360");

  // op 54252: get
  status = db->Get(read_options, "key6667", &value);
  if (status.ok()) {
    out << "GET key6667 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 54253: delete_range
  status = db->DeleteRange(write_options, "key3355", "key6671");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 54254: get
  status = db->Get(read_options, "key6873", &value);
  if (status.ok()) {
    out << "GET key6873 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 54255: put
  db->Put(write_options, "key6207", "value81634");

  // op 54256: put
  db->Put(write_options, "key1992", "value14095");

  // op 54257: scan
  scan_result.clear();
  status = db->Scan(read_options, "key9797", "key11464", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key9797, key11464) size=" << scan_result.size() << std::endl;
  }

  // op 54258: delete
  db->Delete(write_options, "key6093");

  // op 54259: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0523", "key7680", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0523, key7680) size=" << scan_result.size() << std::endl;
  }

  // op 54260: get
  status = db->Get(read_options, "key0943", &value);
  if (status.ok()) {
    out << "GET key0943 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 54261: put
  db->Put(write_options, "key6429", "value64555");

  // op 54262: put
  db->Put(write_options, "key10541", "value114491");

  // op 54263: delete_range
  status = db->DeleteRange(write_options, "key0346", "key6997");
  if (!status.ok()) {
    std::cerr << "DeleteRange failed: " << status.ToString() << std::endl;
  }

  // op 54264: get
  status = db->Get(read_options, "key2327", &value);
  if (status.ok()) {
    out << "GET key2327 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 54265: get
  status = db->Get(read_options, "key3636", &value);
  if (status.ok()) {
    out << "GET key3636 => " << value << std::endl;
  } else if (!status.IsNotFound()) {
    std::cerr << "Get failed: " << status.ToString() << std::endl;
  }

  // op 54266: delete
  db->Delete(write_options, "key1785");

  // op 54267: put
  db->Put(write_options, "key4181", "value96658");

  // op 54268: put
  db->Put(write_options, "key6842", "value104699");

  // op 54269: scan
  scan_result.clear();
  status = db->Scan(read_options, "key0916", "key2093", &scan_result);
  if (!status.ok()) {
    std::cerr << "Scan failed: " << status.ToString() << std::endl;
  } else {
    out << "SCAN [key0916, key2093) size=" << scan_result.size() << std::endl;
  }

  // Final full scan
  it = db->NewIterator(read_options);
  for (it->SeekToFirst(); it->Valid(); it->Next()) {
    out << it->key().ToString() << " => " << it->value().ToString() << std::endl;
  }
  if (!it->status().ok()) {
    std::cerr << "Iterator error: " << it->status().ToString() << std::endl;
  }
  delete it;

  delete db;
  return 0;
}
